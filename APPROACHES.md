# APPROACHES.md — Canonical merged survey of top-level JC2 approaches

Date: 2026-08-21. Merge of three independently produced surveys:
**Sol** (S1–S48, [xmodel/sol-approaches.md](xmodel/sol-approaches.md)),
**Grok** (G1–G40, [xmodel/grok-approaches.md](xmodel/grok-approaches.md)),
**Fable** (F1–F18, list delivered 2026-08-19/21, reproduced in provenance note at bottom).
The dated 46-row inventory and original scores trace to these three inputs;
later correction and live-overlay sections cite post-survey campaign evidence.

## Superseding strategy overlay (2026-08-30 04:42Z — attachment dichotomy promoted)

- **Reduce the finite smooth quadratic branch to two geometric clients.** A
  different-model-confirmed theorem now says that every in-scope survivor
  has reducible reduced ramification or infinity type `F5`; irreducible
  ramification forces `F5`, where all eight boundary intersection units sit
  at one triple point. Run the one-chart `F5` local-different test first, in
  parallel with an effective decomposition of `R_pi=2A+B` in the conic-
  bundle component lattice.
- **Retire raw class-group rank as an elimination gate.** The unit argument
  does inject ramification components into `Cl(Y)`, but the smooth quadratic
  incidence has Picard rank eleven and deleting at most three infinity
  components leaves rank at least eight. Use actual divisor classes,
  effectivity, adjunction, and allowed attachment sets; a claimed rank at
  most one is now an error diagnostic, not a possible theorem.
- **Intersect the attachment and `DISC8-INDEX` data.** The local-different
  packet must keep source ramification/different separate from target
  discriminant and normalization index. The component-lattice packet should
  carry the degree-eight discriminant and degree-four index constraints as
  extra finite filters. Review can remain in the background once a packet is
  provisionally sound; heavy elimination remains AWS-only after controls.
- **Preserve the quarantined branches and independent fronts.** `F3,F6` are
  projective-basepoint/nonfinite types, while nonreduced infinity, singular
  closure, and degree drops remain separate. K00 occurrence/truncation images
  and the pair-square ancestry stalk continue independently.

## Superseding strategy overlay (2026-08-30 04:36Z — intrinsic discriminant/index client)

- **Replace the provisional conductor slogan by the exact
  `DISC8-INDEX` theorem.** Different-model review confirms that the full
  trace-discriminant ideal/divisor of a rank-three finite-flat algebra over
  `C[u,v]` is basis-independent. Quadratic Miranda coefficients force degree
  at most eight, and normalization contributes an even square factor whose
  index divisor has degree at most four. The useful inequality is one-way:
  `d_min>=ceil(deg Delta/4)`; low discriminant degree does not produce a
  low-degree basis.
- **Type four geometric objects before using the bound.** Reduced source
  ramification, the source different, the target discriminant divisor, and
  the reduced branch locus are not interchangeable. Likewise the
  normalization index is a Fitting/length divisor, not the conductor.
  Require the two strict-henselian controls in the binding integration before
  any elimination, and require Tor-free slicing plus the appropriate
  Gorenstein hypothesis before translating to curve delta.
- **Intersect invariants, not just coefficient strata.** On each reviewed
  `F1`--`F7` infinity type, jointly record the full discriminant at infinity,
  the degree-at-most-four index divisor, ramification classes/attachments,
  and the resolved full-boundary graph. Continue the ramification-attachment
  review in the background. Any universal realization/elimination is
  AWS-only after the controls; no broad compute is licensed yet.
- **Keep independent fronts moving.** K00's next high-information target is a
  truncation-image/occurrence theorem rather than another isolated jet, and
  pair-square still needs one actual nearby/vanishing-cycle stalk. Neither
  waits for the discriminant geometry. Reviews remain asynchronous.

## Superseding strategy overlay (2026-08-30 04:16Z — promoted quadratic locus and K00 cell; discriminant/attachment gate)

- **Move from infinity-only classification to the full boundary.** The
  different-model-confirmed identity `2=G+B+K` gives exactly seven viable
  reduced bidegree-`(2,3)` rational-tree types and two impossible types.
  Passing this is only necessary. On the smooth projectively finite stratum,
  a new sealed provisional attachment lemma says irreducible ramification
  creates a forbidden cycle in `F1,F2,F4,F7`; within that stratum the
  surviving fork is reducible ramification or the one-point `F5` collision.
  The projective-basepoint types `F3,F6` are outside the finiteness hypothesis
  and remain a separate branch. Review that deduction while computing divisor
  classes and the `F5` local different provisionally.
- **Use a basis-invariant client for nonlinear cubic data.** A sealed
  ideation packet records the provisional exact observation that the full
  binary-cubic discriminant divisor is unchanged by a global trace-zero basis
  change over `C[u,v]`, although coefficient degree is not. A quadratic
  presentation therefore supplies an intrinsic degree-at-most-eight
  discriminant, pending independent review of the typing. Build
  `DISC8-CONDUCTOR` from the full coefficients, order-to-normalization index,
  and resolved union of infinity plus ramification; do not infer Galoisness
  from a square leading discriminant.
- **Ask K00 for compatible lift images, not more isolated points.** Independent
  review now closes the entire normalized `e=2,m=2,h10=1` field-point cell at
  G13. The cheapest new literal face is `h10=2`, but global information gain
  is higher from the scheme-theoretic image of one truncation map on a
  reviewed predecessor and from source occurrence. This targets compatible
  formal branches without confusing a finite cell with an arc or atlas.
- **Retype pair-square through one derived stalk test.** Ordinary
  specialization runs opposite the proposed ancestry set map. On one actual
  strict-henselian conductor stalk, compare nearby/vanishing-cycle ranks,
  marking, and swap/braid equivariance against `w-b`; stop immediately on a
  rank or canonicity mismatch.
- **Keep global and disproof fronts alive.** Audit the precise `A(F)` one-place
  theorem before coupling it to the intrinsic discriminant, spend only a
  bounded lane on the Strinz `q18/B5Q` coordinate crosswalk, and require
  uniform all-order complexity from characteristic-`p`/Witt work. Reviews
  remain asynchronous and all heavy/uncertain computation remains AWS-only.

## Superseding strategy overlay (2026-08-30 03:46Z — affine-linear cubic closure; rational-forest nonlinear gate)

- **Exploit the first genuinely closed cubic subfamily, but keep the basis
  firewall.** Different-model review now closes every proper cubic block whose
  Miranda coefficients are affine-linear in some fixed global trace-zero
  basis. The proof combines the Galois/nonmonogenic block theorem with exact
  cone/incidence geometry and the log first-leg obstruction. This is a real
  block-side theorem, not primitivity: coefficient degree is not invariant
  under basis change, and quadratic or higher coefficients remain.
- **Push the nonlinear route through its finite exceptional boundary, not a
  generic coefficient sweep.** The rational-forest theorem is promoted: any
  resolved target boundary admitting a dominant rational `A^2` first leg has
  only rational components and forest dual graph. It excludes generic
  quadratic infinity by genus two. More generally, it excludes a fixed-basis
  incidence closure that is a smooth irreducible class-`dA+3B` hypersurface
  of literal fibre degree three, for `d>=3`, by positive `p_g`. The reduced
  `(2,3)` survivor is provisionally classified by
  `2=G+B+sum(delta-r+1)`: seven of nine factor partitions remain, including
  irreducible cuspidal cases. Review that finite classification in the
  background, then add the ramification support on each surviving stratum.
  Keep nonreduced infinity, projective coefficient basepoints, singular
  closure, and both target/fibre degree drops as separate clients.
- **At pair-square, prove geometry rather than reverse specialization.** A
  canonical ancestry surjection would give the desired augmentation injection
  and local excess `w_i-b_i` formally, even for unequal fibres. Ordinary
  specialization points generic-to-special and can merge points, so it does
  not supply the required special-to-generic map. Test one actual Section-7
  strict-henselian conductor stalk for unique ancestry, braid/swap descent,
  and blowup invariance before attempting a global event graph.
- **Preserve independent occurrence and K00 fronts.** None of the block or
  log theorems proves that a hypothetical map enters a registered formal
  source packet. Keep the global occurrence/coverage arrow first. K00 G13 is
  now a sealed provisional kill of every fresh `n=5` survivor: a row-six
  quartic eliminates rank one and a second cokernel terminal eliminates rank
  two. Review that producer and its still-provisional old-`n=4` G12 parent in
  the background. Even confirmation closes only the named
  `e=2,m=2,h10=1` finite prefix; source completeness, arcs, and all-depth
  attainment remain separate.
- **Use the quiet external perimeter as intelligence, not a detour.** The
  scheduled sweep finds no external JC2 resolution. A missed August-25 public
  F2 note names two infinity charts and missing values `q18` and `B5Q`; add a
  typed coordinate crosswalk to the next ideation round, but launch no broad
  CAS from an artifact-free prose frontier. Reviews stay asynchronous;
  heavy/uncertain computation remains AWS-only.

## Superseding strategy overlay (2026-08-30 03:00Z — no two-block quotient; log-positive first leg)

- **Keep the global occurrence/coverage arrow first.** The strongest exact
  local and block theorems still do not show that every hypothetical map
  enters a registered formal source cell or proof interface. Finite prefixes
  are not maps, and closed selected cells are not a source atlas.
- **Exploit the new block theorem at the first-leg interface.** Every proper
  intermediate quotient is non-Galois, so a two-block system is impossible;
  fixed divisorial inertia, an infinite missed-boundary class lattice, and a
  nonprincipal different are now reviewed constraints. Separately, the exact
  two-section theorem gives nonnegative log Kodaira dimension as soon as two
  collision fibres occur and forbids any dominant `A^2` first leg. The first
  Fable `AL3-REDUCE` review attempt produced no report after hitting its output
  cap, so the affine-linear Miranda conclusion remains review-gated; rerun a
  shorter bounded packet, and promote the closure only if that review passes.
  A new review-gated rational-forest producer gives the first
  nonlinear filter: any resolved target boundary for an `A^2` first leg must
  have rational components and forest dual graph; the generic quadratic-
  coefficient infinity curve has genus two, while smooth presentations of
  coefficient degree at least three already have positive geometric genus.
  Classify only the degenerate quadratic infinity strata next. Do not infer
  primitive monodromy, `d1!=2`, or general cubic closure.
- **Partition K00 boundaries while testing richer transition state.** Four
  generic unit-ray cells `(2,1),(3,1),(2,2),(3,2)` are reviewed empty at
  G7,G9,G10,G12. On `(2,2),h10=1`, G11 nonemptiness is reviewed; its
  provisional G12 continuation leaves fresh `n=5` rank-one and rank-two
  survivors, so G13 is the next literal gate after review. Cheaper cleanup
  faces `(2,1),h10>=1` and
  `(3,1),h10>=1` remain, while `(4,1)` at G11 is the cheapest untouched
  unit-ray test. The general possible-arrival calendar is reviewed, but it
  proves no finite recurrence or all-ramification theorem.
- **Build pair-square at one actual surface stalk before any global graph.**
  Curve-only augmentation/monodromy cannot prove QCS. On one real quotient
  line, define the scheme-theoretic diagonal marking on the canonical
  normalization/conductor square, construct the braid/swap-equivariant
  generic-to-special cone, and prove its special length `w_i(z)-b_i` plus
  blowup invariance. Use Kummer only as a persistent-event zero control,
  keeping affine ramification and the physical pole distinct. Global marked
  connectivity waits on this stalk gate.
- **Keep review and systems repair asynchronous.** The log theorem, four
  generic K00 cells, and the K10-boundary G11 theorem are promoted; the cubic
  reduction and G12 boundary child remain review-gated, with no review lane
  currently active. The queued systems repair is complete: all adapters now
  inherit an OS-level no-read/no-write boundary for the excluded nested
  repository, including symlink aliases, and immutable prompt custody has
  50/50 regressions in three interpreter modes. The model-side receipt is
  also write-denied and pins pre/post launcher/profile hashes. Heavy or
  uncertain CAS stays AWS-only; no heavy job is currently needed. Grok is
  presently unavailable: its two newest launches returned HTTP 402 before
  model work.

## Superseding strategy overlay (2026-08-30 00:47Z — sharp normal window and étale sandwiches)

- **Advance K00 by source partition, not coefficient-blind induction.** The
  literal normalized integer atlas and every `e=2,m=1` positive/infinite
  late-load face are reviewed empty. The complete `e=3,m=1` cell is now also
  promoted empty at G9, while `e=2,m=2` has a sealed G10 producer with
  genuinely odd coefficient directions and awaits review. Run that review in
  the background and attack the missing `e=3,m=2` residue independently.
  In parallel, enumerate the source-completeness partition—C6=0, K10/Jdet
  boundary faces, other supports, and ramification pairs—because three closed
  calendars do not constitute a global atlas or an occurrence theorem.
- **Use the sharp unloaded filtration at its exact type.** Independent Fable
  and AWS routes give `J^5 subset I subset J^2` sharply, so the true
  projectivized normal cone is empty and centered ideal values lie in the
  window `[2v(J),5v(J)]`. The quartic `Delta` is the discriminant of the seven
  second-normal-jet quadrics, not a true normal-cone nonemptiness locus. Use
  its strata and the sharp window to prune/route literal transition packets;
  do not infer loaded recentering, integral closure, or Rees valuations.
- **Run two proof-side construction gates concurrently.** For the
  off-diagonal pair-square route, define the compactification-independent
  event module and identify exactly where `J(F)=1` enters before attempting
  the QCS injection. The reviewed block route now gives a forced non-`A^2`
  étale sandwich whose nonempty branch divisor is missed by the first map;
  both the codimension-one-image and intermediate-`A^2` bridges to
  minimal-degree primitivity are closed. Attack the sandwich directly,
  provisionally at block index two, while keeping primitivity open. Neither
  proof gate waits on K00 computation. Restrict ACV work to actual Keller
  pairs: the scalar inequality `#atypical>=s-1` is false without the partner.
- **Retire empty clients, preserve broad interfaces.** Simultaneous regularity
  in the literal monomial chart forces contraction of the y-axis, so stop its
  rational-termination descendants. Translated/Puiseux/pole-retaining charts
  remain separate clients. Keep the actual-map occurrence/coverage arrow as
  the highest proof gap; finite formal packets are not maps.
- **Keep reviews and machinery asynchronous.** The repaired independent-
  section assembler remains unwired until stable-basis re-review. Heavy or
  uncertain CAS is AWS-only; r6b is now idle after the sharp-exponent replay.
  Producer-confidence descendants may proceed while hostile reviews run, but
  canonical promotion remains review-gated.

## Superseding strategy overlay (2026-08-29 22:44Z — close the `e=2,m=1` boundary; attack the first resonance)

- **Promote the first ramified exclusion; provisionally generalize only over
  its exact coefficient-independent scope.** Independent reconstruction
  confirms that the normalized `e=2,m=1,ord(k10)=0,B11111` cell is empty at
  G7. A new sealed replay proves that K6, K2, `mu2`, `mu4`, and `mu6` do not
  enter the argument, so every positive-or-infinite boundary load-order face
  at `e=2,m=1`, still on the declared C6/K10/Jdet unit opens, has the same
  empty prefix; Fable is reviewing that enlarged
  corollary after Grok exhausted its balance before model work. Do not
  extrapolate in `e`: an exact `e=3,m=1` full-source fixture
  survives G0--G8 once the second surface coefficient is retained. Attack its
  G9 carry first, while classifying the associated rank-one resonance rather
  than attempting a coefficient-blind induction.
- **Exploit the exact unloaded graph as a recentering engine, with the mixed
  source firewall explicit.** Direct fifth-power certificates prove globally
  that the reduced unloaded K00 zero set is exactly
  `D(S,T)=(2S+S^2,(1+S)T/8,S+16T^2,T,S,2T)`. This licenses exact graph
  recentering whenever an unloaded coefficient block vanishes in a reduced
  ring. It does not put a loaded trajectory on the graph, because its source
  equations contain K10/K6/K2 and targets. The next theorem target is a
  graded induction that isolates precisely when the unloaded block vanishes
  and records resonant load collisions.
- **Keep the R4 review and ramified work asynchronous.** The R4-00 rank-zero
  cell is independently reproduced empty at grade 18. Exact grade-19 unit
  packets now provisionally kill R1+, R1-, and R2; hostile Opus replay runs
  in the background while base jobs continue. Descendant strategy may treat
  those packets as provisionally true, but the literal integer atlas is not
  promoted closed until the unit packets pass source and localizer review.
- **Preserve proof-side breadth.** QCS glue-or-kill work and the global
  actual-map selector remain independent proof avenues. Counterexample-side
  narrowing does not establish source completeness, attainment, or a map.
  Heavy/uncertain algebra remains AWS-only; exact review and new descendants
  proceed concurrently rather than waiting on one queue.

## Superseding strategy overlay (2026-08-29 21:40Z — one literal integer cell)

- **Concentrate the literal V20R2 atlas on `R4-00`.** Independent review
  promotes the uniform grade-15 exclusion of every old-plane
  next-rank-two point, so `R4-02` is empty. With all other integer
  valuations reviewed empty, `R4-00` is the sole literal normalized
  `K[[Lambda]]` cell. Its active grade-12 entry solve is exactly six affine
  quadrics `Q_i(z)+kappa*polar_M2_i(x,z)=0`, row six zero, followed by all
  later literal rows. Do not infer nonemptiness from the packet.
- **Keep `R4-00` and the ramified atlas independent.** Closing `R4-00`
  would finish the integer section functor only. The ramified
  `e=2,m=1,B11111` rank-zero prefix and its grade-7 continuation remain a
  separate source-incidence route, alongside other load faces, `C6=0`, and
  other supports.

## Superseding strategy overlay (2026-08-29 21:31Z — close valuation three)

- **Concentrate the literal integer atlas on valuation four.** Two independent
  hostile reconstructions confirm the complete valuation-three fan and the
  three grade-12 endpoint kills. The stale source-preflight digest is
  quarantined; the theorem is carried by the reconstructions and the sealed
  endpoint replay. Together with valuations one, two, five, infinity, and at
  least six, this leaves only the reviewed
  `R4-00 disjoint-union R4-02` packet on the literal normalized
  `K[[Lambda]]` source. Continue `R4-00` and the review of the provisional
  `R4-02` kill concurrently.
- **Do not confuse the one-cell integer frontier with valuative closure.**
  The separate `e=2,m=1,B11111` packet is already outside the integer
  partition. Its first provisional attack kills nonzero leading ranks at
  grade 3, leaves the old rank-zero plane with a genuine grade-5 prefix
  witness, and makes grade 7 the first sharp K10 successor. Review and that
  continuation run concurrently. Any eventual valuation-four closure would
  finish only the literal section functor, not ramified DVR incidence, other
  load faces, `C6=0`, or other supports.

## Superseding strategy overlay (2026-08-29 21:23Z — close valuation five; enter the ramified atlas)

- **Promote valuation five and concentrate the integer fan on three/four.**
  Independent hostile reconstruction confirms the repaired V20R2
  valuation-five exclusion: ranks two and one die at grade 14, and rank zero
  dies scheme-theoretically at grade 19 by
  `5*Jdet[0] in I_140`. Combined with the already promoted valuation one,
  two, infinity, and at-least-six exclusions, the literal normalized
  `K[[Lambda]]` frontier is now valuations three and four. Valuation three
  has a review-gated grade-12 closure; valuation four has reviewed residual
  `R4-00 disjoint-union R4-02`, with `R4-02` provisionally killed.
  Continue these cells independently, without inferring a recurrence.
- **Treat ramified source incidence as a parallel first-class frontier.**
  `K00-V20R2-VALUATIVE-COMPARISON/v1` returns
  `RAMIFIED_RESIDUAL_EMITTED`, not an unramified-reduction theorem. Its
  first missing cell is `e=2,m=1,B11111`, with 273 equations through
  `t^38`, 326 free columns, and 316 syntactically live columns. Review the
  Kummer action and census in the background while attacking the cell
  provisionally. A surviving finite system is not an arc or point; a cell
  exclusion would still cover only this ramification/load face.
- **Do not let the counterexample-side narrowing absorb proof-side capacity.**
  QCS remains the minimal exact proof-side bridge target, with
  `Xi=d-s-sum_i b_i`, `STRICT-COLLIDE-POLY`, and the pole-incidence /
  suspension-map formulation at their actual tiers. The global
  td/type/entry selector or a bypass remains the outer proof bottleneck.

## Superseding strategy overlay (2026-08-29 20:51Z — promote R4 narrowing; retype QCS)

- **Reduce valuation four to two reviewed residual packets.** The exact
  normalized V20R2 rank fan now leaves only `R4-00` and `R4-02` through grade
  19. Leading nonzero ranks die at grade 12, old-plane next rank one dies at
  grade 15, and next rank two lies on `u_y=0` or `u_y^2=192v_y^2`. Run the
  two cells independently; neither is known nonempty. Valuation three has a
  fresh grade-12 full-fan exclusion under hostile review, and valuation five
  has a repaired full exclusion under hostile review. Keep all three at
  their actual review tiers; infer no cross-valuation periodicity. Even if
  the integer atlas closes, it exhausts only the registered unramified
  `K[[Lambda]]` section functor. Run the provisional
  `K00-V20R2-VALUATIVE-COMPARISON/v1` gate before claiming closure incidence:
  a DVR can have `Lambda=u*t^e`, and `x^2-Lambda` is the minimal failure of
  unramified-section detection.
- **Use the minimal PCB margin before building the full geometry packet.**
  The exact generic margin is `Xi=d-s-sum_i b_i`. It requires an actual pair
  and generic fibre, complete quotient-line/baseline inventory, and `d,s`.
  Build `P_i,d_i,G,n` simultaneously as a collision/Suzuki consistency and
  mechanism packet, but do not let those harder fields block the minimal
  integer. Exit-to-line transport is a third downstream gate. Residue A has
  none of the required common source packet and remains underdetermined.
- **Target polynomial-origin strictness, not atypicality alone.** The exact
  collision formula says Euler jump equals collision capacity minus weight
  excess. A reviewed type-(2,3) analytic Keller surrogate, and the larger
  `g=t` germ family, attain collisions with zero local profile excess. The
  shortest positive target is `STRICT-COLLIDE-POLY`: use actual
  polynomial/rational origin to force strict quotient weight at a collision,
  or falsify it with one source-compatible polynomial-origin germ. Even a
  positive result gives only `E_gen>=#atypical`; the separate global bound
  `#atypical>=s-1` would still be needed for QCS. In parallel, review the
  provisional boundary-complex formulation: a pole-incidence boundary on
  the auxiliary quotient events and a suspension of its cycle space onto
  `H1(F)` would prove QCS by rank-nullity. Bare positive filling data cannot
  force these maps. A provisional exact follow-up already stops the proposed
  matched degree-six PALF test: its local equality germ has genus-two `A4`
  Milnor fibre while the separate passport has genus one, and the Euler five
  is mixed interior/end data. Do not run an unrestricted mapping-class or
  Kirby search; require one common labelled surface/boundary packet first.
- **Keep the outer proof selector and conditional td12 laboratory separate.**
  These new identities sharpen a possible bypass but supply no actual
  td/type/entry/U1 selector, no `PairRef`, and no book-budget charge.
  Conditional sextet, character, scale, and inertia work remains behind its
  source gates. Run reviews and descendants concurrently; promote only after
  binding integration. Heavy or uncertain computation remains AWS-only.

## Superseding strategy overlay (2026-08-29 19:40Z — close K00 valuation two; reject mistyped selector cards)

- **Narrow Avenue 36 on V20R2 to finite valuations 3--5.** Three
  different-model exact reconstructions promote the full valuation-two rank
  fan exclusion through grade eight. Leading rank one/two die at grade six;
  on the old plane all three next-rank cells die at grade eight. The repaired
  composition also kills `d=0` at grade 19, so valuations one, two, every
  valuation at least six, and infinity are closed on this normalized support.
  Valuations 3, 4, and 5 remain possible and none is attained. This narrows
  Avenue 36 without reranking it or constructing a map.
- **Run the remaining literal jet fans under separate review states.** The
  valuation-four producer provisionally excludes leading ranks one/two and
  next rank one, leaving `R4-00/R4-02`; different-model review is the next
  gate. The valuation-five leading exclusions survive an internal repair,
  but its residual hash and coefficient typing fail custody. Require a new
  sealed v2 residual before review or descendants. Launch valuation three
  independently; do not infer a period or renormalization theorem.
- **Keep the global selector ahead of conditional td12 work.** The proposed
  U1 Hamiltonian--Kummer receiver lacks an actual varying D-stable source and
  a typed map to distinct PCB excess events. `EXCESS-e` budget subtraction,
  atypical-to-excess, AM-2 contact feasibility, and inherited sextet
  characters remain conjectural, underdetermined, or conditional. None
  supplies the missing actual td/type/entry/U1 selector.
- **Reject the pole-vertex/root shortcut and its launches.** For a proper
  root `P`, the exact formula sums over its attachment set:
  `E_P=Delta_P/(alpha beta)=sum_(F in S_P)a_Fb_F/nu_F`. Reduced entry files
  generally omit `B`, `mu_P`, and `S_P`. Residue-A attaches both mass-three
  vertices to the Y root, giving root ratio `4/3` while global energy is one.
  Thus the proposed `RPMC(1)` equality, `SCALE-AUDIT`, per-entry root
  assignment, and dependent avenue raises are stopped. The existing capacity
  conjectures retain their prior tiers.
- **Next parallel allocation.** Counterexample work runs valuation three,
  reviews valuation four, and repairs valuation five concurrently. Proof work
  attacks an actual-map global selector or a correctly typed
  `WEIGHT-AT-ATYPICAL`/PCB-excess bridge with prove/falsify/bypass ownership;
  no target inequality is assumed. Conditional character, scale, AM-2, and
  inertia cards stay parked until their named source objects exist. Current
  work is desk-scale; heavy or uncertain computation remains AWS-only.

## Superseding strategy overlay (2026-08-29 17:58Z — reset the global selector; close K00 valuations at least six)

- **Separate the global selector from the conditional td12 laboratory.**  The
  promoted unconditional spine is only
  `JC2 false -> some GGV-minimal standard pair -> the same pair in a
  Sigray-normalized frame with td preserved -> td>=6 -> a finite entry menu
  at each fixed td`.  No reviewed arrow selects `td=12`, type `(2,3)`, three
  poles, U1, B25, or S17.  GGV minimality minimizes its published base-scale
  invariant, not topological degree.  Avenue 2 remains principal, but its
  first target is now an actual, provenance-preserving degree/type/entry/U1
  selector or a proof that no such selection is needed; local td12 work stays
  explicitly conditional.
- **Promote the abstract `U1*(R)` interface no-go, not an occurrence
  theorem.**  For every odd `R>=3`, exact reduced configuration data of
  type `(2,3)` with `R` identical pole entries
  `(a,b,nu_P)=(1,2,3)`, `td=4R`, and a U1 star pass the present local
  merge/trunk laws and have a P1-compatible continuation; the `k=1` family
  also passes the reviewed local T1 row.  Thus those reduced laws cannot by
  themselves select or exclude td12/U1.  These are formal necessary-data
  configurations, not polynomial maps.  The proposed derivation of
  `td >= s+sum wt` from Section 7/Riemann--Hurwitz is false: components and
  finite ramification cannot be dropped.  The bare inequality on actual maps
  remains unsupported and undecided.  `PCB-EXCESS` is only a precisely named
  conjectural repair target.
- **Replace the conditional first-nonneutral quartet by a sextet.**  Given an
  actual td12 three-pole U1 occurrence and retained source provenance, the
  surviving first nonneutral reduced rows are A7, C5, B25, S17, D13, and
  E11.  D13 and E11 cannot be deleted by their immediate terminal budgets:
  continued P1-shaped interior paths can lower terminal `psi`, and exact
  locally T1-compatible witnesses have lower-floor totals reaching the
  shared numerical ceiling.  Hence the
  correct conditional task is
  `TD12-U1-FAMILY-AWARE-DETOUR-EXCLUSION/v2`, covering all four detour
  starts plus W1--W3 and pure-epsilon descendants with full source/Puiseux
  typing.  Reduced tails are non-exclusion witnesses, not realizations.
- **Strengthen Avenue 36's exact source stop and retain the remaining
  valuations.**  On normalized V20R2, `C6=1`, `k6[0]=0`, and
  `Jdet[0]!=0`, grade 12 plus the grade-19 contraction exclude every formal
  solution with valuation `v_Lambda(d)>=6`.  Together with the promoted
  valuation-one kill, only valuations 2, 3, 4, and 5 remain possible on this
  support; none is known to occur.  At valuation two, the old-plane `y=0`
  branch is independently excluded through grade eight.  The proposed
  grade-seven ambient-rank Fitting packet has zero exclusion power and is
  stopped.  Reviewer-derived claims about the nonzero-`y` rank strata remain
  provisional and require a fresh different-model review before use.
- **Rebalance without speculative heavy compute.**  Proof work should attack
  the global actual-realizability/selector interface and the source-typed
  four-family detour exclusion in parallel.  Counterexample work should
  investigate V20R2 valuations 2--5, beginning with an independent review of
  the exact nonzero-`y` valuation-two strata before any successor
  computation.  All current certificates are desk-scale; no campaign-owned
  AWS job is licensed by this delta.  This significant-news checkpoint
  triggers a fresh blind whole-portfolio round immediately after commit.

## Superseding strategy overlay (2026-08-29 16:35Z — close the valuation-one K00 seed; move td12 upstream)

- **Promote grade four and use radical-first scheduling.** Different-model
  review promotes
  `phi(Lambda_(4,r))=Q_r(u-(s^2,st/8,16t^2,0,0,0))`, the proper
  dimension-13 nonreduced survivor, and its prime reduced locus
  `A^12 x G_m`. For every later ideal `J`,
  `sqrt(I4+J)=sqrt(sqrt(I4)+J)`, so scheme and radical inputs cannot differ
  in geometric existence, unit/proper status, reduced minimal primes, or
  dimension. Retain the scheme input only for nilpotent, embedded, tangent,
  multiplicity, or lifting questions.
- **Close exactly the normalized valuation-one K00 seed at grade five.**
  Independent exact reconstructions show that on `D(k10_0)` the grade-five
  locus projects to `s=t=0`. Its fibre there is nonempty, so the bare affine
  ideals remain proper; however `ell(s,t)=0` is precisely the
  higher-valuation leak and is excluded from the valuation-one client.
  Therefore `V(I5) intersect (D(s) union D(t))` is empty: the normalized
  valuation-one, `C6=1`, `k10_0!=0` seed dies. Stop grade-six work against
  that seed and do not spend AWS on its scheme refinements. Avenue 36 as a
  whole remains open for other valuations or supports, but no replacement is
  armed without a separately typed normalization and source client.
- **Put td12 occurrence/coverage ahead of further local rows.** Exact-pair
  completions, trees, and finite coefficient maps already exist after an
  actual boundary vertex is named. What is missing is a theorem selecting
  td12/U1 and a named B25 or S17 occurrence from a hypothetical minimal
  counterexample. `TD12-U1-ACTUAL-LANDING` is the narrow conditional form of
  that same occurrence problem, not a new avenue or cheaper substitute. Do
  not relaunch pair-constructor searches or call an uninstantiated formal jet
  a `PairRef`.
- **Bank the repaired S map and stop first-resonance arithmetic.** The
  reviewed S bridge promotes the shear/floor/sibling/degree theorem and makes
  `FLOOR-COORD` operationally WLOG via an explicit generic source
  translation; future value packets must serialize the gauge and triangular
  transport. Occurrence, positive-order values, cap, `PairRef`, and the
  terminal reduced-deviation row remain open. The reviewed B recurrence makes
  `j=17` an identically vacuous total derivative. The S `j=13` statement is
  conditional on typing the analogous terminal row. The next B algebraic
  resonance is `j=42`, but no positive-order `P_a` values, pinned `C_17`, or
  reviewed index-to-window map exists; do not launch it.
- **Close round `20260829T1517Z` without a numbered-avenue rerank.** The
  radical/scheme geometric split, full period-two K00 renormalization,
  post-G4 translated-quadratic functor, G6 valuation-one continuation,
  translation-Ward row without a packet, cross-grade row proportionality,
  and Hermite slot count are stopped or interface-blocked. Avenue 2 remains
  principal. Trial the reviewed opt-in `CAPRUN/v1` helper without migrating
  callers. No campaign-owned heavy AWS mathematics is currently justified;
  the 512-vCPU quota is a ceiling and separately owned capacity is outside
  campaign inspection.

## Superseding strategy overlay (2026-08-29 15:07Z — close grade-three K00; hold grade five behind checkpoint)

- **Promote the remaining grade-three rank strata with their repairs.**
  Fable 5 independently reconstructs both the maximal-minor classification
  and the rank-at-most-one compatibility theorem.  Thus `I5(A) subset B`,
  every rank-five localizer on `V(B)` is unit, and no full-`P6` rank-five
  chart should run.  On `J1=B+I2(A)`, grade-three compatibility is exactly
  the reduced rank-zero plane
  `(d0_1,...,d5_1)=(2s,t/8,s,t,s,2t)`; every `u` works there and the
  rank-exactly-one compatible locus is empty.  The report's reversed variety
  arrow is corrected to `B subset P6`, hence `V(P6) subset V(B)`; this is a
  text repair and strengthens, rather than weakens, the inherited stop.
- **Use the maximum reviewed grade-three incidence theorem.**  The prior
  exact containments give `rank(A)<=2` on `V(B)`; the reviewed chart-free
  theorem excludes compatible rank two, and the new review excludes
  compatible rank one.  Therefore, over an algebraic closure, the full
  grade-three compatible geometric locus of the leading base `V(B)` is
  exactly that reduced two-plane, with `u` free.  This is a geometric
  incidence statement on `V(B)`, not a full-`P6`, later-grade, source, arc,
  Keller-pair, counterexample, or JC2 theorem.
- **Keep Fable's reviewer-generated refinements provisional.**  Its new
  two-minimal-prime decomposition of `J1` and the claim that fourth power is
  the exact minimal radical exponent have not received different-model
  review and are not used by the promoted core.  The checksum ledger headed
  `charge_basis` is prompt-induced input metadata only; the run correctly
  records `charge_basis=ABSENT` because no exit price is asserted.
- **Grade four survives, but remains a primary.**  Opus's exact desk result
  conditionally identifies the seven plane-specialized grade-four rows with
  the grade-two quadrics at
  `u-(s^2,st/8,16t^2,0,0,0)`.  The `d*_3` and `k10_0` coefficients disappear,
  the localized ideal is proper, and its reduced locus is
  `A^12 x G_m` cut out by two coupled `(s,t,u)` equations; the scheme itself
  is nonreduced of nilpotency index three.  Record this as provisional until
  different-model replay.  Its smallest successor is the two-variant
  `K00-G5-RANK0-PLANE` gate, where `d*_3` and `k10_0` return; do not launch it
  before the present canonical checkpoint is sealed.

## Superseding strategy overlay (2026-08-29 14:40Z — promote B bridge core; keep value gates open)

- **Use the B bridge only after a named actual occurrence.**  Different-model
  review promotes the finite greedy deviation ledger, exact landing at
  `s*=25(i+r)-17`, and the landing ODE.  Its solvability condition is exactly
  `8B=9A`, with a unique on-weight rational solution, so the distant row
  passes on the reviewed T1 cell and does not kill B.  The chart depends on
  the pair plus occurrence and Puiseux prefix; it is not a pair-only or
  uniform constructor.  Keep `OCCURRENCE-B25`, positive-order source values,
  and pair-specific support as the live gates.  Do not infer a uniform
  scheme, cap, serialized `PairRef`, degree bound, or route exclusion.
- **Retain only the repaired C4 operator.**  Rational resonance occurs at
  `j=17 mod 25`, and the first primitive is
  `(p^i T_17)'=RHS_17/(25 i lambda_f)`.  The reviewed bridge does not make
  its residue functionals nonzero.  Keep Sol's exact-derivative claim at
  `j=17` and next-candidate `j=42` provisional; stop the `j=17` descendant
  provisionally and do not launch `j=42` without a source-bearing client.
- **Keep the S route one review tier behind.**  Internal audit finds the
  mathematics repairable but the Opus producer body seal false.  The generic
  source translation `x -> x+c` provisionally gives an Aut-equivalent S17
  normalization with `ord_x f=ord_x g=0`, making `FLOOR-COORD` an
  operational gauge rather than a research fork.  It does not alter fixed
  literal coordinates or transport future frozen values automatically.
  Require a different model and repaired custody before promotion.
- **Continue the conditional K00 grade-four desk lane.**  Opus is testing
  the seven literal grade-four rows over Grok's provisional rank-zero
  two-plane with `k10_0!=0`.  Treat it as an active descendant, not evidence;
  its upstream rank-at-most-one theorem is still under combined Fable review.

## Superseding strategy overlay (2026-08-29 14:30Z — bank finite-s; close K00 rank two)

- **Promote the carrier theorem, not a numerical consumer.**  The reviewed
  rooted-prefix proof works for every finite pole-path union on one fixed
  fibre/component, with attachment
  `a(P)=max_j min(v_j,O(P,P_j))`.  Complete actual exit carriers are
  disjoint and may enter one C7.1 union, so
  `FULL_ACTUAL_EXIT=FULL_ACTUAL_FIRST_SEPARATION` now has finite-s scope.
  It is still lower-floor-only and never attainment.  No current `s>=3`
  book or td12 source object carries the required typed client; bank the
  theorem without repricing, spawning a consumer by analogy, or changing an
  avenue rank.  Generic MP8/MFE remains `REPRESENTATIVE`.
- **Close the K00 grade-three rank-exact-two branch chart-free.**  Independent
  reconstruction confirms `P3=A[:,1..6]u+c3` and, for
  `H=J2+I3([A[:,1..6]|c3])`, the radical equality
  `sqrt(H)=sqrt(J2+I2(A))`.  Thus every putative rank-two lift through grade
  three collapses to rank at most one.  Stop all full-`P6` rank-two minor
  charts and do not rerun the old capped selected chart; its historical
  status stays `RESOURCE_CAP_NO_VERDICT`.  Preserve K00 as a protected
  falsification avenue through the independent rank-at-most-one and
  rank-five branches, both still under combined Fable review.
- **Keep td12 source bridges review-gated and route-separated.**  Fable's B
  global-source bridge is under Grok hostile review, and Opus's sibling
  bridge is under internal audit.  Neither producer report changes the
  canonical route.  A sealed Sol correction provisionally shows that
  Fable's `j=17` RESROW form used the wrong integrating factor: after repair
  the right side is an exact derivative and both residues vanish.  The next
  possible resonance is `j=42`, needs `P_42`, and lies outside B24.  Stop the
  `j=17` descendant provisionally, but launch no `j=42` lane before review
  and an actual source-bearing client.
- **Exploit stops immediately, promote asynchronously.**  The reviewed K00
  rank-two stop frees all computation formerly aimed at minor charts; the
  finite-s theorem needs no computation.  Continue provisional K00 and td12
  descendants in parallel while their hostile reviews run.  No new heavy
  computation is licensed by this delta; heavy or uncertain CAS remains
  AWS-only.

## Superseding strategy overlay (2026-08-29 13:15Z — close 1224 round; move td12 upstream)

- **Keep the numbered portfolio fixed.** Four blind researchers and three
  adversarial cross-pollination passes found no promotion-worthy new avenue.
  Avenue 2 remains principal; Avenue 3 remains mechanism supply pending an
  actual client; avenues 1, 4, 27, and 32 do not move; and Avenue 36/K00 keeps
  protected counterexample-side ownership.
- **Promote LL1-R4 only as its typed software consumer.** Different-model
  hostile review passes all 166 checks, the six-entry manifest, independent
  legacy replay, mutation controls, and ordinary/optimized identity. The
  exact result remains one fixed fibre, the two-pole path union, `td=6`,
  `m=2`, and lower floors only: four movers, six removals, none added, and
  `13 -> 7`. The validator imports the compiler, so do not call it
  code-independent. No attainment is asserted and no surviving row occurs
  merely because it has zero slack.
- **Stop every homogeneous-window td12 cascade descendant.** Opus hostile
  review promotes the route-separated quotient theorem and strengthens the
  formal lift: with clamped floors it solves all homogeneous rows through
  `s<=r=3i/2`. Thus B24 is transparent for even `i>=16` and S16 for even
  `i>=12`; failure at `r+1` is not an obstruction. The next primary is
  `TD12-GLOBAL-SOURCE-BRIDGE/v1`: derive a finite-support,
  source-provenanced `P_0,...,P_i` packet from a hypothetical minimal
  counterexample, or attack the distant inhomogeneous row. Keep B and sibling
  routes separate, and do not call a ready template a `PairRef`.
- **Deduplicate the new proposals aggressively.** Do not relaunch the max12
  U2/U3 passport census, introduce a second dirty-transport charge, or run
  S17 beyond-floor arithmetic without source data. The Bezout identity stays
  family-local; `OFF-DIM2` lacks a complete-map client; and the proposed
  Lipshitz reduction fails because `nu=tK` is variable multiplication and
  coverage is not existential. The exact non-torsion fact in `TWIN-ORDER`
  earns at most a short derive-or-`OPEN` check unless a closure law appears.
- **Run three independent successors without waiting on review queues.** In
  addition to the td12 global source bridge, test the actual finite-pole
  scope of the promoted attachment theorem and run only a stripped
  `TWIN-ORDER` derive-or-`OPEN` check; retain K00 chart-free continuation as
  a separate falsification lane. The five LL1 zero-slack terminal states hide
  six scalar-consistent route profiles, so bank them until an actual
  occurrence/source/decoration client exists. Review remains background debt
  once provisional confidence is reasonable.
- **Trial one bounded systems improvement.** `ROUNDVIEW/v1` may compile the
  newest overlay, exact 46-row inventory, and heading index into a
  hash-bound prompt slice with fail-closed anchors and determinism tests. It
  is non-authoritative and must never become a second queue. Current lanes use
  versioned `FALLACY-v2.md`, which requires charge-basis declarations only
  for new price assertions and direct mathematical-source citations; pinned
  v1 remains immutable for old packet replay. Current work is desk-scale;
  AWS is idle, and all heavy or uncertain CAS remains AWS-only.

## Superseding strategy overlay (2026-08-29 12:20Z — bank U2, source, LL-1, and cascade precursor)

- **Retire the corrected labelled `NESTED-U2-P0-U2-RAY/v1` at td12.** An
  actual landing is impossible by the reviewed representative-AF2 ledger:
  source equality forces three `(1,2,3)` pole chains at `(w,M)=(3/2,2)`;
  the two inner `w=1`, `3|M` arrivals cost at least five each, and the outer
  `w=2` sibling costs at least one, so `11>td-2=10`. Stop source-gluing,
  absolute-index, and realization work on this labelled family. Do not infer
  that other U2 routes, other one-P0 labels, or all td12 configurations die.
- **Keep the carrier distinction explicit.** This kill is a
  `REPRESENTATIVE` selected-exit/MFE consumer. It neither uses nor proves the
  LL-1 `FULL_ACTUAL_FIRST_SEPARATION` bridge. Separately, the stronger
  two-pole attachment theorem now has independent Sol/Opus derivations and a
  clean Grok hostile pass. Promote the dictionary
  `FULL_ACTUAL_EXIT=FULL_ACTUAL_FIRST_SEPARATION` at complete-carrier,
  lower-floor-only scope, and bank the exact LL-1 `13 -> 7` replay. Do not
  infer attainment or turn any surviving reduced row into occurrence.
- **Split the td12 recurrence source by realized route.** The universal
  coefficient formula is shared, but the `nu=25` B route and `nu=17`
  sibling route need separate exact-pair/fibre/completion packets. Only the
  two `nu=17` siblings share one parent jet family; no Galois conjugacy is
  automatic. Fable's reviewed absorption identity proves that the
  same-order Keller equation cannot generate its fresh f-side piece: an
  explicit g-side response absorbs it. On an actual type-`(2,3)` occurrence
  `r=3i/2` is fixed, but the exact pair, scale, contact ledger, lower jets and
  support caps remain absent. Quarantine the proposed one-dimensional
  cascade count. The completed **provisional** typed precursor instead finds
  an injective reduced operator with nominal cokernel two for B and three for
  sibling; its sole leading resonance changes only a sharp endpoint-cap
  count. More decisively, for `r=3i/2 in Z` the truncated binomial response
  absorbs every formal row through `s<=i`, so there is no formal depth-window
  kill under the separate `i>=depth` rider. This same-model result awaits
  hostile review. Prioritize route-separated exact-pair/completion/source-cap
  packets; do not invent missing coefficients or call a formal jet envelope
  a `PairRef`.
- **Trigger the significant-news outer loop after the atomic checkpoint.**
  Ask every research model to reconsider the entire avenue inventory in
  light of the U2 kill and LL-1 bridge closure, including new
  mathematical connections and campaign-software improvements. Reviews stay
  in the background; no current task needs AWS unless that round yields a
  reviewed heavy coefficient/CAS packet.

## Superseding strategy overlay (2026-08-29 10:42Z — sharpen the td12 landing target)

- **Use the repaired arrival-subtree floor, not merge-free M-descent.** An
  actual arrival subtree has pole mass at least `beta`, and at least
  `max(beta,2alpha)` when its arrival multiplicity is at least two.  The
  stronger `min(2beta,mu*alpha)` bound is licensed only by a pole-adjacent,
  edgewise-`G notin V_{2,a}`, or direct `M_H|b_P` certificate.  The corrected
  one-P0 U2 family has three multiplicity-three subtrees, hence `td>=12`, not
  15.  At equality the only pole profile is type `(2,3)` with three
  `(a,b,nu)=(1,2,3)` poles.  Attack its still-unbuilt `w=1` arrivals and the
  necessary dirty `M=2`-to-a-multiple-of-`3` transport/T1/gluing data; do not
  call the formal `w=2/3` ledger a member of the labelled route or an actual
  tree.
- **Bank Q+E5 at fixed-state pattern scope.** The forced-index integer menu
  and no-partition R2.2 predicate have a fresh stable-basis Grok review.
  Preserve the split between pattern-algebra inputs and the sufficient
  route-facing envelope, and keep target `nu_G` distinct from incoming
  `nu_U`.  A standalone exact checker is useful after separate software
  review; do not migrate or bypass `cell_check`, edit `solve_arr`, or change
  `NUCAP=500`.
- **Keep LL-1 at the legacy inventory while attacking its exact bridge.** The
  four nonintegral reprices and 13-to-7 graph replay are conditionally exact,
  but the two-pole ledger is selected-witness typed.  Prove the strong ambient
  orbit-tree attachment theorem or a direct two-pole first-separation-set
  theorem before tagging those consumers `FULL_ACTUAL_EXIT`.
- **Run the shared td12 source recurrence next.** Extract the depth-24 B-child
  vector and both depth-16 sibling vectors from one coupled Keller/source
  recurrence, with review in the background.  In parallel, pursue the newly
  rigid U2 equality landing and the row-to-actual-edge map.  These remain
  desk-scale; AWS stays reserved for a reviewed coefficient/CAS packet.
- **Carry the two new semantic tripwires into every lane.** Merge-free is not
  synonymous with the edgewise `V_{2,a}` hypothesis of Statement 8.5, and a
  fixed target index is not an incoming index or a legacy search variable.

## Superseding strategy overlay (2026-08-29 09:35Z — correct the exit floor; run td12 and both U2 branches)

- **Use the full actual exit set and keep carriers typed.** Multiplicity
  counts cover-level Puiseux series, not physical places; a cv flag counts
  neither. Every physical place through a nonzero up child has a unique
  same-ray cv carrier, and the full set of distinct carriers covers all such
  places, possibly many-to-one. For positive
  `delta=D/m-kbar`, the safe total-exit lower floor is `delta` when integral
  and `ceil(2*delta)` otherwise. It is a floor, not an attained price. The
  proposed general formula `min(num(delta),2*ceil(delta))` is withdrawn.
  “No divergence/no shedding” is always a strictly-below-cv statement;
  exact-level separation and the printed choice-sensitive `kappa` convention
  must stay explicit.
  The reviewed td8 kill and td12 B-charge remain valid after this repair;
  the legacy nonintegral reprice is provisional and awaits a fresh
  stable-basis hostile review before any ledger changes.
- **Put the shared td12 source recurrence first.** The sibling
  `(nu,dp,dq)=(17,68,52)` survives T1 at the rigid ratio
  `B/A=(9+/-3i)/8` and has two nonzero directions of exact charge four.
  Together with the earlier exact B-charge eight, the live necessary tests
  are two depth-16 pure-power sequences and one depth-24 sequence. Extract
  all first-child coefficient vectors from one coupled Keller/source
  recurrence; record vectors as forward-generator data and review in the
  background.
- **Split U2 work by interface.** The direct nested theorem is promoted only
  for an actual adjacent U2-to-U2 edge. A contracted stage-R row still needs
  a refinement carrying adjacency, down-tree typing, full indices/degrees,
  and the last `(nu,kbar)` frame. Separately, the reviewed one-P0 route has
  infinitely many locally legal records: for fixed `t=5 mod 6` it is
  semilinear in `K=1 mod 6`, while varying both parameters is bilinear. Use
  it to attack absolute-index/product-of-`nu` boundedness, source landing,
  and coefficient gluing—not as a contradiction to the direct-edge theorem.
- **Review Q+E5 before touching the engine.** The provisional equal-regime
  consumer fixes `nu_G`, `M_G`, and `kbar_G`, then derives a unique degree
  pair and one bounded-multiplicity existence inequality. Legacy
  `cell_check` searches other indices and omits N1, so it cannot be reused as
  this predicate. Its first hostile-review attempt failed closed on live
  source mutation. Keep `NUCAP=500` and all legacy verdicts until a fresh
  stable-basis review and regression coverage pass.
- **Keep the outer portfolio stable.** Avenue 2 remains principal with the
  internal order td12 recurrence, U2 coverage/gluing, Q+E5, then
  family-aware landing and type/cofinal control. Do not raise avenues 25,
  26, 28, or 31: local flag arity is not target inertia, a type menu, or a
  positive Euler decomposition, and the component-labelled `A(F)` packet is
  still absent. K00 remains the protected bounded characteristic-zero
  counterexample seed.
- **Run the inference firewall as campaign infrastructure.** Every new model
  prompt receives the compact `FALLACY.md` appendix. Exit claims can declare
  an exact machine-checked `charge_basis`; absence is recorded, never
  inferred from prose. All current mathematics is desk-scale. Reserve AWS
  for a reviewed joint coefficient/CAS packet and keep heavy work off the
  local host.

## Superseding strategy overlay (2026-08-29 08:04Z — td8 affine route killed; move to td12 and nested U2)

- **Retire the reviewed `td=8` affine equal-join family.** The independent
  Opus reconstruction confirms the trunk arity correction; Grok reached the
  same verdict but its proof repeated the series/place shortcut and is not a
  second sound reconstruction. For a trunk flag,
  `q=kappa_H/kappa_F>=2` gives weight at least three; if `q=1`, a lone flag
  in the full actual exit set, together with physical-place coverage and no
  conjugate shedding, would retain all `2i` series in one common prefix and
  acquire impossible weight `3/2`, so at
  least two flags contribute total weight at least four. Therefore the
  actual-weight ledger is at least `2+2+3+1=8>7`, uniformly in `t`. The old
  charge-two trunk profile was per ray and undercounted the exit set. Do not
  identify one place/flag with one conjugate Puiseux series. Stop coefficient
  transport, `(0,y)` realization, layer-10, and A-side endpoint work on this
  family except for archival review closure. This kills one formal route,
  not all `td=8` configurations.
- **Bank the A-tower theorem as valid but strategically subsumed.** Grok's
  review confirms that the mandatory `(2,3)` tower forces both A-charges
  exactly two and pins the local first-child discriminant, after proof-text
  repairs. That closes the former A-side ambiguity; the stronger trunk
  contradiction now ends the route before source realization matters.
- **Restore exact zero at every actual pole.** Fable independently confirms
  the universal pole-purity argument. The previous `td=12` proposal to add a
  symmetric `+3` was a pole/interior-child typing regression and is withdrawn;
  no false route kill resulted. The U1 family still survives its first trunk.
  Fable's hostile review now confirms that the B-extra charge is exactly
  eight: distinct-orbit partings cost two flags of at least eight, while
  conjugate shedding forces one flag of at least sixteen. The next necessary
  source test is pure-power children through 24 normalized levels. Obtain the
  first B-child coefficient vector or its coupled Keller recurrence; do not
  return to pole pricing.
- **Promote U2 through integral first boundaries, then attack direct nesting.**
  Fable passes the fixed-data theorem for P0 prefixes reaching either P1 or a
  first `nu>=2` equality boundary with integral `kbar`. A new producer result
  provisionally makes the first direct `nu=1` U2-to-U2 pair finite through an
  exact divisibility equation and an ODE obstruction at its sole resonance.
  Review the edge typing next, then handle intervening P0 chains and recursively
  variable base data; do not claim full-cell finiteness yet.
- **Re-rank the M2 frontier.** Put the reviewed td12 U1 source-jet gate and
  nested-U2 recursion first, followed by Q+E5's `delta=0` existence solve and
  the remaining finite configuration families. The td8 route kill is
  significant-news under the outer loop, so it triggers a fresh whole-portfolio
  ideation round after the atomic checkpoint. Reviews remain background and
  provisional descendants remain allowed.
- **Keep computation proportional.** All current advances are exact desk
  algebra; no AWS launch is warranted yet. The f-only layer-10 construction is
  explicit and, after the route kill, no longer a useful CAS target. Reserve
  AWS for the first reviewed joint coefficient system large enough to benefit.

## Superseding strategy overlay (2026-08-29 07:24Z — transport/root tier survives; price the remaining exits)

- **Retire leading-coefficient transport as a `td=8` kill gate.** Fable's
  Statement-3.9 primary and Opus's hostile review show that the full affine
  family survives formal top/tower transport for every `t>=0`. The repaired
  invariant tower begins `(2,3)`, has only inert `k=1` shifts before the
  `(7,6)` drop, forces the q-side resonant coefficient to zero, and satisfies
  the soluble twin law `(A_1/A_2)^2=omega^(-15)`. This proves neither a source
  initialization nor one polynomial pair; `(0,y)` treetop data, joint subtop
  gluing, exact lambda, and realization are now the live interfaces.
- **Promote the separation calculus, not an exact cost.** Fable independently
  confirms Opus's law
  `Delta=(kappa_H/kappa_F)(tau_0-kbar_F)` after reindexing by the
  `kappa_F`-grading. The merge costs zero. The affine route can meet its
  ceiling only when both A-charges and the trunk charge are exactly two;
  this means `theta>=7` at each A-copy and no trunk ramification jump with
  the exact one-unit area defect. A new Sol producer argument, still awaiting
  different-model reconstruction, uses the full `(2,3)` approximate-root
  tower to force `theta>=7` separately at both A-copies. It rejects the
  formerly free nonzero first discriminant as f-side-only data and would
  make both A-charges exactly two. Until review, retain the reviewed weaker
  statement; if the proof passes, the trunk is the only live budget charge.
- **Narrow U2 instead of declaring it closed.** Fable confirms the absorbed
  normal form `p=Rad^mu`, `q=Rad*S`, its exact ODE/transport, and the stated
  low-degree controls. Opus confirms the separate theorem that fixed
  `(r,mu,w)` and budget admit only finitely many outer `L` along P0-only paths
  ending at P1. Neither review covers a first P2 merge, a nested U2 parameter,
  landing, or a finite full-cell ledger. Sol's equality-boundary extension is
  a repaired producer result awaiting different-model review.
- **Quarantine the current quadratic-cv witness.** Its repeated `(2,1)` tower
  conflicts with the reviewed mandatory `(2,3)` first stage, and it omits the
  allowed `theta=7` split-quadratic endpoint. Fable's review confirms the
  weight/pole census, caps a pure `(2,1)` chain at `m_A in {2,3}`, and repairs
  the endpoint and fibre-constant errors, but its displayed replacement still
  does not impose the common `(2,3)` opener. Thus its local survival verdict
  does not lift to the route. The provisional full-tower lemma above is now
  the cleaner replacement target; review that instead of extending the old
  witness.
- **Retarget the `td=12` route from its trunk to its three pole entries.**
  Opus review sustains Grok's `FAMILY_SURVIVES_FIRST_TRUNK`: the exact T1
  ratio is `B/A=9/8`, the sheet count is `i_F=3n*i_G`, and the cap-free
  13-edge menu has exactly two P1-fitting terminals. But the advertised
  slack assumes all three identical `b=2` pole entries have price zero, which
  the source declares unknown. By symmetry any positive entry price adds at
  least three and kills both fitting one-step terminals. Price that single
  `(a,b,nu)=(1,2,3)` exit set before spending another round on the trunk jet.
- **Treat `(0,y)` treetop compatibility as provisionally solved, realization
  as open.** Grok has constructed explicit case-IV top/tower data for every
  affine parameter and derives the ten silent `kappa_F`-graded layers. This
  primary awaits hostile review; descendants may provisionally move to the
  reduced layer-10 question: can a type-`(2,3)` Newton-rectangle germ with
  nine silent layers realize
  `(eta^17-A)^3(eta^17-(4/3)A)^2`? A negative answer kills every `t`; a
  positive answer upgrades the route to source-germ scope, not to a Keller
  pair.
- **Promote Q+E5 pattern-fibre finiteness, not its migration pseudocode.**
  Different-model review confirms a finite effective fibre for every fixed
  two-pole case-III state/menu and the arity-graded ratio theorem through the
  campaign's `r0<=3` range. The proposed replacement guard still confuses a
  case-I `nu_G=1` row with case III and can discard legal fractional-`kbar`
  cells. Keep `NUCAP=500`; add the case-I negative control before any engine
  edit. The highest-yield merge-local successor is the `delta=0` finite
  `kbar` menu with a complete existence solve.
- **Priority order.** First test reduced layer-10 `(0,y)` realization and the
  `kappa_F`-graded subtop discriminants; price the `b=2` pole
  exit in parallel, repair the cv recursion, and extend U2 through
  source-licensed first boundaries and then nested `nu=1`. Reviews stay
  nonblocking. The first systems-loop trial
  has also replaced whole-worktree lane snapshots by declared-input
  provenance, shrinking new run records from megabytes to roughly 0.5 KiB.

## Superseding strategy overlay (2026-08-29 03:51Z — separation geometry and U2 terminal finiteness)

- **Treat U2 as provisionally reduced, not promoted.** Grok derives the exact
  U2 shape `p=Rad^mu`, `q=Rad*S` and ODE
  `r*Rad*S'-L*Rad'*S=C'`, with arbitrarily large local `L`, but argues that
  only finitely many such members can reach P1 at any fixed entry and budget.
  Fable is attacking the quantifiers, dirty-path induction, and divisibility
  step. Descendants may use the formulas provisionally; no finite-terminal
  theorem enters the ledger before that different-model verdict.
- **Replace “compute the missing jet” by the exact separation gate.** Opus
  shows provisionally that Statement 3.9 pins only the child leading
  coefficient. The exact charge is controlled by the degree profile of the
  branch group. One formal jet gives immediate separation; a second makes
  only the first child polynomial a square and therefore postpones the
  question, but does **not** yet construct a complete late-contact profile or
  charge two. On the td=8 route, survival is knife-edge: both A charges and
  the trunk charge must all equal two. Fable is independently checking the
  descent formula, finite charge menu, recursive jet freedom, and complete
  exit budget.
- **Attack a different consumer in parallel.** While Fable owns td=8
  coefficient transport, Grok now tests the source-pinned td=12 U1 family's
  fixed dirty trunk directly: solve its own Proposition 8.1(iv), determine
  exact cost if possible, and enumerate an alternative reduced trunk only if
  the displayed one dies. This avoids duplicating either transport or the
  U2 review.
- **Keep custody repairs off the mathematical critical path.** Opus found a
  real TRIPLE02 archive path-swap race, so Fable R4 uses one open descriptor
  for hashing and parsing. Opus K0 R4 and Fable Q+E5 R3 remain background.
  None blocks provisional mathematics and none authorizes AWS.
- **Allocation is intentionally peer-heavy.** Fable owns primary transport,
  three different-model reviews/repairs, and exact-cost review; Opus supplied
  the exact-separation primary and owns custody repair/review; Grok supplied
  U2 and owns the independent td=12 trunk consumer. Sol integrates, checks
  scope, and chooses successors instead of repeating their calculations.

## Superseding strategy overlay (2026-08-29 03:30Z — clean round closes; coefficient tier becomes the M2 frontier)

- **Bank the clean `20260829T0250Z` four-model round, with one-law
  accounting.** Fable5 synthesis `d2e34570...`/body `1bc47e35...` verifies
  the sealed packet and all four blind reports; the first Grok submission is
  excluded and was not read. The local `td=8` Proposition 8.1(iv) solve and
  Opus Theorem E are the same U1 rigidity law at
  `(r,mu,eps)=(2,3,0)`, not two independent successes. Keep `i=14`, the
  frame ratio `2/3`, and the ODE ratio `6*nu/(2*nu+1)` distinct.
- **Promote the reviewed local law, not a counterexample.** Two exact local
  formulations pass Fable hostile review. For every member of the reviewed
  `td=8` affine route, the merge equation survives iff its two nonzero orbit
  values are opposite; the solution is unique up to scale. The incoming
  `(21,15)` and trunk `(85,35)` patterns also survive their vertex-local
  equations. No local ODE/log-residue test can kill this family; the next
  proof gates are Statement-3.9 coefficient gluing, exact lambda, or the
  first post-merge trunk consumer. None of this proves landing or existence.
- **Adopt only Grok's repaired U1 semilinear residue.** U1 is a labelled
  arithmetic-progression family with constant reduced successor and lambda,
  periodic `M`/P0 menu, and coefficient shape `Rad(T)=T^r-A`. The unique
  `td=12,m=3,[2,2,2]` row supplies a second infinite pattern-tier
  budget-fitting family. The original Opus report remains
  `REPAIR_REQUIRED`: legality can empty an advertised progression, monotonic
  direction depends on `T`, and Theorem F's incoming-index consumer is
  invalid. U2 (`nu_G=1`, unbounded `lex`) remains separate and open.
- **Floors remain floors.** Grok's exact-lambda primary proves only the
  recorded `2+2+2=6` lower-bound equality. The two incoming linear models
  predict two; the trunk predicts nonintegral `3/2`, so some correction is
  forced but may still round to two. The decisive datum is one first
  extra-branch jet `(r_*,p_*)`; Opus now owns that exact computation.
- **Retype the endpoint, and never restart a numerical-cap campaign.** The
  full-configuration codomain must be a finite residue plus finitely many
  normalized semilinear family records with explicit consumers. U1 is the
  first proved schema; U2 and the Q+E5 case-III layer are the two named leaks.
  No new evidence supports a numerical `kbar` or `nu` cap.
- **Q+E5 theorem yes, engine migration no.** Opus independently confirms the
  two-pole degree-gap/M-graded bounds and exact Q+E5 elimination, including
  that incoming `nu_U` is free. It refutes Fable R2's alleged infinite fixed-
  pattern fibre and finds a `w=2`-only divisibility rule in the migration
  plan. Fable owns R3; `NUCAP=500` and the OPEN fallback remain untouched.
- **Portfolio shift.** Raise avenues 2 and 3 at their coefficient interfaces;
  arm avenue 4 only if cyclic gluing succeeds. Keep the coupled topology-at-
  infinity bet (25--28/30), numerical caps, and broad primitive-group census
  lowered/stopped. Narrow avenue-18/31 observations remain typing notes with
  zero active allocation. Fable/Opus/Grok own the three main M2 primary
  lanes; Sol integrates and adversarially adjudicates.

## Superseding strategy overlay (2026-08-29 02:47Z — equal-join wall becomes two semilinear regimes)

- **Treat Opus's new classification as a provisional root, not a promotion.**
  The exact packet passes locally and the `td=12,m=3,[2,2,2]` entry row is
  real.  If hostile review confirms the algebra, U1 (`nu_G>=2`, equal
  arrivals, `k=lex=0`) has constant reduced successor, periodic `M`, and
  coefficient rigidity `Rad(T)=T^r-A`; the right ledger object is a labelled
  arithmetic-progression family, never a numerical `kbar` cap.
- **Separate U2 immediately.**  At `nu_G=1`, rational `kbar` permits
  unbounded `lex` and infinitely many reduced successor weights.  This is a
  distinct merge-finiteness wall, closed by budget only in the filed td-7
  instance.  It is now a first-rank exact research target alongside
  cross-vertex coefficient transport and exact lambda.
- **Do not promote semilinear sufficiency from the current Opus report.**  It
  invokes Sol's refuted incoming-index theorem for the one consumer that sees
  the full parameter.  Grok review must either replace that dependency with
  the Q+E5 merge-local theorem, retain a semilinear consumer composition, or
  narrow the claim.
- **Keep software repairs off the mathematical critical path.**  LL-1 needs
  only a source-pin/parity R3; K0 and TRIPLE02 each found a fail-closed custody
  defect and are receiving immutable R3 repairs.  None licenses AWS yet, and
  none blocks the active U1/U2, exact-lambda, or transport lanes.

## Superseding strategy overlay (2026-08-29 02:22Z — reduced chain wall closed; parametric cell wall exposed)

- **Treat finite reduced P0 closure as a theorem, not a conjectural avenue.**
  Two different-model reviews reconstruct the cap-free proof. Correct P5,
  REDUCTION Critical 5, and every engine comment: 69 charged states, and
  fixed-budget reduced numerator/`M` are finite. Preserve the exact
  congruence fibres; the legacy engine's extra edges are mathematically
  illegal even though neutral absorption hides them in the reduced map.
- **Move the frontier one type outward.**  The live obstruction is a
  semilinear full-cell record carrying last-vertex `nu`, affine `kbar`, exact
  edge labels, and a finite exceptional table for every consumer. A finite
  reduced state set is necessary infrastructure but not a finite cell book.
- **D2 is promoted at recorded book scope.**  Different-model review
  reconstructs the exact affine identities.  At the unique `td=8,m=2` entry, both
  chains take the `(21,15)` cost-lower-bound-two step to `(3,2/3,3)` and an
  equal join produces an affine family with one reduced successor. A uniform
  `(85,35)` trunk composes at the full-frame level and the recorded lower
  bounds meet the terminal filter at equality. This is formal-cell
  infinitude, not exact cost or realizability. Opus5 and Grok now solve the
  family-specific Prop. 8.1(iv) equation independently.
- **Do not replace the case-III incoming-index cap from Sol R1.**  Hostile
  review identifies the already-resolved H5a notation fork: Q+E5 makes
  `nu_U` free and bounds merge-local `nu_G` through
  `mu0*nu_G*w_U=delta*kbar+mu*w`.  Retain the scoped degree-gap and `kbar`
  inequalities, but rebuild the solver on Q+E5 before any engine change;
  Fable5 owns that R2.
- **Harden, do not reopen, the chain packet.**  R2 fixes predecessor custody
  and pins a lex-active secondary regression. This is software reliability;
  it is not another theorem gate or a reason to pause the parametric merge
  attack.

## Superseding strategy overlay (2026-08-29 01:55Z — M2 chain quotient confirmed; merge wall isolated)

- **Replace the false P5 obstruction at chain scope.**  Different-model
  review confirms that fixed entry and lambda budget give a finite effective
  reduced P0 state set `(w,M)`, with `M<=M_entry*2^B`.  Free local indices
  are arithmetic-progression fibres, not new reduced states.  The uniform
  modulus is `T=P-s*eps`: `M | T` and `M` depends only on `nu mod |T|`.
- **Do not promote Opus's merge-wide constant or case-III index bound.**
  Zero-cost case-III merges have an arriving 0-root with `eps=mu0`; they are
  counterexamples to the proof's `eps=0` premise.  Case-I/II integrality was
  also applied to the case-III zero edge, confusing local `nu_G` with
  incoming `nu_H`; the advertised bound fails on the promoted td-7 book.
- **The remaining M2 wall is now exact.**  Simultaneous `C_e=0` gives the
  equal-`(mu,w)` affine family: infinitely many formal cells but constant
  child `w` and periodic finite `M`.  Determine fixed-entry reachability,
  total/trunk budget, and the correct parametric Prop 8.1(iv) equation rather
  than seeking a false `kbar` cap.  Opus5 owns this primary attack.
- **A direct two-pole case-III repair is under review.**  Sol's independent
  degree-gap theorem bounds incoming `nu_H` in all three multiplicity
  regimes and removes the old `NUCAP=500` fallback if Fable confirms it.
  Keep it provisional; it does not touch equal nonzero joins or multipole
  merge trees.

## Superseding strategy overlay (2026-08-29 01:38Z — landing ledger repaired at its first discriminator)

- **Retain the compiler architecture, delete the alleged new `td=6` gap.**
  Different-model review confirms Fable5's full-frame records,
  `COVERED`/`TERMINAL`/`UNCOVERED` partition, cap firewall, and the complete
  safe arithmetic through the unique IIa child.  These are the right
  instruments for REDUCTION Critical 4; they are not yet a landing theorem.
- **Normalize `nu=1` before classification.**  An eta root of `q` at
  `nu=1` is a root-location choice, not an additional degree slot.  With
  `L=l+1`, the purported odd-`l>=5` family is MULTIPOLE D9 family I:
  odd `L` is MP2-dead and even `L` is killed uniformly by the D9 logarithmic
  obstruction, including `q(0)=0`.  Do not queue a td-7 zero-chain extension
  for these cells.
- **Build LL-1 only against corrected acceptance tests.**  `UNCOVERED` may
  be empty.  A generated candidate that is neither classified nor emitted
  open is a failure, and so is an open row already killed by a promoted
  theorem.  Plant `(dp,dq)=(2,8)` as a required D9 rejection.  Then advance
  to LL-2/`td=7`, where the genuinely open merge and semilinear issues first
  appear.

**Scoring semantics differ and are NOT averaged.** Sol's x/10 = expected research
information from the cheapest first experiment. Grok's x/10 = P(checkable proof or
explicit char-0 counterexample at serious multi-year intensity), given **only** for
campaign-untried lanes. Fable gives qualitative status flags only. Disagreements are
flagged inline as DISSENT.

## Superseding strategy overlay (2026-08-29 01:16Z — two AWS routes fail closed; peer-heavy research continues)

- **TRIPLE02 closed successor — preserve the algebra, rebuild custody.**
  Sol-ultra independently confirms the node-2 seed, rank-six bound,
  saturation theorem, and complete generated chart contracts, but rejects R1
  because its late marker is not bound to the classifier/archive, stage
  identities are replayable across jobs, and supervisor death is not
  fail-safely contained.  Fable5 owns R2.  No Singular launch is licensed.
- **K0 certificate — R1 rehearsal is terminal `NO_VERDICT`, not a GP
  result.**  The AWS contract correctly held one core, 1 GiB, and zero swap,
  but `python3 -I` made the sealed runner's sibling imports unavailable;
  neither engine ran.  Opus5 owns R2, including the pre-existing D1--D8
  repairs.  The promoted paper theorem is unchanged.
- **Keep primary mathematics off the repair critical path.**  Fable's landing
  ledger has isolated one exact uncapped `td=6,m=2` eta-factor family and is
  under Grok review; Opus and Grok independently prove/falsify the cap-free
  `M>=2` budget quotient.  Repairs and hostile reviews remain background
  gates, not reasons to idle the global proof search.

## Superseding strategy overlay (2026-08-29 01:00Z — D43 v3 rejected; K0 rehearsal licensed)

- **D43 emitter — repair again; do not launch v3.**  Opus5 confirms the
  literal mathematical source gates inside v3 but demonstrates that the
  terminal authority accepts unbound candidate values.  A forged positive
  carried a contradictory lease digest, impossible row inventory, and a
  nonexistent named archive.  Fable5 now owns a fresh immutable v4 repair
  with value-level receipt binding, full archive-member equality,
  nonvacuous process-gate provenance, authority-bearing launcher
  post-mortem, and the missing v2 shared-function diff.
- **K0 certificate — one tiny AWS rehearsal is licensed, not a final
  certificate run.**  Fable5 source review passes the sealed R1 packet for a
  pinned one-core PARI 2.15.4 parser/census rehearsal.  Eight fail-closed
  defects, including census-entangled theorem booleans, vacuous structural
  gates, incomplete frame pins, and a GP E banner with no E-reading gate,
  must be repaired in R2.  The already-promoted paper theorem does not wait
  on this lifecycle work.
- **Rail-E exact solve remains sequenced after emitter v4 review.**  No D43
  AWS emission or subresultant solve may consume v3.  Global landing/M>=2
  research continues independently on Fable, Opus, and Grok lanes.

## Superseding strategy overlay (2026-08-29 00:58Z — ACS theorem retained; ceiling/TDIC experiment stopped)

- **Avenues 7/25 — retain the complement-cover theorem as infrastructure,
  not as a rank increase.**  Different-model review confirms that a Keller
  map restricts to a connected degree-`td` finite-etale cover between the
  complements of `F^{-1}(A(F))` and `A(F)`.  It also proves the qualitative
  constraint that `A(F)` of a counterexample is reducible or singular.
- **Stop `ACS-CEIL` at its first discriminator.**  The corrected
  deficit-Euler identity uses compactly supported Euler characteristic on a
  refined constructible stratification.  Its curve-stratum terms can be
  negative, so it is not a positive decomposition of `td-1` and gives no
  cofinal ceiling without genuinely new geometry of `A(F)`.
- **Stop the current residue-A `TDIC-SET` experiment as `NOT-TYPED`.**
  Per-component inertia compatibility is true, but the banked vertical data
  do not identify target components or even determine the grouped meridian
  types, and there is no horizontal finite-end packet.  Building those data
  is the pre-existing Avenue-7 construction debt, not a cheap consequence of
  the covering lemma.  The source/target meridian-custody gap also remains.

## Superseding strategy overlay (2026-08-29 00:44Z — finite C1 refuted; ARITH-SPREAD promoted)

- **Avenue 2 / D43 — delete the two-frame generic-nonemptiness shield.**
  Fable5 found and Grok 4.6 independently confirmed that the provisional C1
  inference was false: a `K0` Bezout certificate can have denominators at
  both observed frames.  The exact constant countermodel is
  `F={105337*105673}`.  The two banked modular zeros remain honest facts about
  the named evaluator, but they do not prove a proper characteristic-zero
  ideal.
- **Retain the finite census only as a certificate-content ledger.**  If one
  frozen system over `A[1/5]` is generically empty, every observed geometric
  `PROPER` place divides its contracted content ideal; the product of residue
  norms divides the 210-free numerator of any cleared certificate content.
  This becomes stronger with more primes but has no finite nonemptiness
  threshold without a separate effective height bound.
- **Promote the infinite ARITH-SPREAD dichotomy at exact scope.**  Chevalley
  on the irreducible arithmetic curve says infinitely many geometric
  `PROPER` fibres force generic nonemptiness, while infinitely many genuine
  unit-ideal fibres force generic emptiness.  A finite run can only populate
  the content ledger.  Missing `F_p` points are not `UNIT`, and the census
  must specialize one frozen finite-type scheme.  Exact-row emission remains
  required before identifying this instrument with the 184/29-row D43
  packet.

## Superseding strategy overlay (2026-08-29 00:33Z — K0 promoted; four-model round collected)

- **Avenue 2 / D43 — delete coefficient-component fanout now.**  Opus5
  theorem `6d3d53c...` has passed independent Grok 4.6 reconstruction
  `07f20be...`/body seal `877e451f...`: `K0` is one Galois, nonabelian
  degree-432 field with base `Q(zeta_168)`, idempotents `0,1`, finite-etale
  model over `Z[1/42]`, and ramification exactly `{2,3,7}`.  Do not issue a
  `K0-SPLIT` factor job or retain dynamic coefficient idempotents.  The
  separate dual-engine packet is valuable replay/custody work but is not a
  theorem-promotion prerequisite.
- **Rail E — retain exactly four conditional one-scale branches.**  For the
  exact bridge polynomial `e1b99600...`, the four values
  `W1/W2=i^k*(A2/A1)*(1+i)*(r3-1)/2` lie in `K0`.  This licenses a
  branchwise univariate design only after exact rows exist.  It does not prove
  tail rank over `K0`, remove Fitting complements, or produce a point.
- **The outer-loop rebalance is now evidenced, not aspirational.**  Fable,
  Opus, Sol-ultra, and Grok each completed a blind 46-avenue scan against the
  same `20260829T0002Z` packet.  Preserve independent primary work across all
  four.  Their new proposals—cap-free rank-or-lasso, arithmetic spreading,
  asymptotic-complement covering/inertia, exact one-scale subresultants,
  landing-ledger instrumentation, and orthogonal falsifiers—remain at
  ideation tier until the active theorem reviews and synthesis separate valid
  mechanisms from analogies and scope conflicts.
- **Execution order stays nonblocking.**  Review the sealed emitter v3 and
  K0 certificate in the background; prepare the TRIPLE02 closed successor;
  send heavy algebra only to AWS after exact source PASS.  The plurality of
  mathematical effort remains on total full-configuration landing and a
  cap-free `M>=2` theorem, not on multiplying descendants of the now-cheaper
  D43 slice.

## Superseding strategy overlay (2026-08-28 23:55Z — TRIPLE02 proper open closes; K0 field theorem provisional)

- **Avenue 1 — retire one more endpoint open, not its component.**  The R5
  TRIPLE02 node-1 resume passed independent Fable5 source review
  `b886d8bb...`, produced terminal archive `4b8ffc1c...` on isolated r6d,
  and passed different-model terminal review `34265f13...`.  Therefore stop
  all work on this literal `D(Delta)` open: all ten endpoint coefficients
  vanish after the exact two-containment, full-transform, and kernel replay.
  Continue only with the disjoint closed successor `V(I,Delta)` and any later
  recursive nodes.  Do not promote this node result to whole TRIPLE02;
  `P`, `C8P02`, `Q1P02`, and the uncovered `Q1P03` remainder also stay open.
- **Avenue 2 — provisionally delete coefficient-component splitting.**  Opus5
  primary report `6d3d53c...` proves on paper that the displayed rank-432
  algebra is a single field:
  `K0=Q(zeta_168,cuberoot(3+sqrt3),cuberoot(3-sqrt3))`.  The decisive
  certificate is the explicit two-way base-field map plus two independent
  cubic-residue characters, not a degree-432 factorization.  Treat this as a
  provisional root with the normal speculative cap while the dual-engine
  packet and different-model review run.  If confirmed, remove `K_i`
  fanout/dynamic idempotents and allow exact nonzero normal forms as units.
- **Rail E becomes potentially one-dimensional.**  Conditional on the
  already reviewed displayed E relation, Opus gives
  `q0=u*(1+i)*(sqrt3-1)/2 in K0` with `q0^4` equal to the required ratio.
  Retaining all four branches `i^k*q0` reduces E to four univariate scale
  problems over `K0`; it does not remove Fitting-drop charts or create a raw-J
  point.  Encode and review the identity before changing solve receipts.
- **Current parallel allocation.**  Fable repairs the exact-row emitter v3;
  Opus produces the K0 certificate; Grok's independent D43 conceptual scan
  found no structural bypass but proposed a post-emission non-unit-ideal
  lemma (refuted and replaced by the 00:44Z overlay above) and a
  Hessian/Kuranishi diagnostic.  Sol coordinates, composes theorem
  interfaces, and launches only reviewed AWS artifacts.

## Superseding strategy overlay (2026-08-28 20:00Z — raw-J/template firewall and seven-condition exact core)

- **Correct the D43 source claim.**  The 184 pristine Euler rows are the
  complete finite **raw-J truncation**, not by themselves the intended
  residue-A/template locus.  Exact bridge `e1b99600...`, independently
  confirmed by Fable5 `69db2cd8...`, proves that on `W1*W2 != 0` the two
  literal E5 rows with existential `HM`, E6, and nonzero scale eliminate
  exactly to
  `(9+5*r3)A1*W1^4+(9-5*r3)A2*W2^4=0` plus one unit condition.  This bridge
  belongs to source existence; the D21/D23/D25 reconstruction and 509/184
  traces remain a separate NF-equivalence project.
- **Promote the exact 22-support discriminator, but repair its packet before
  launch.**  Opus5 audit `456edb3a...` independently finds exactly 29 live
  raw rows in bands 20/30/40, degree at most two, and full rank 22 in the tail
  columns at both witnesses.  Eliminating the tails therefore leaves only
  seven residual conditions in `W1,W2`; the J rows, not E5/E6, pin the common
  W scale.  Remove the false degree-16 coefficient-field estimate.  Build the
  independent f/g sparse jets concurrently on AWS, gate on exact D21 band-20
  equality, then emit all 184 rows from the same immutable inputs without a
  second manifest/rebuild.  Hostile packet review `283d47df...` returns
  `REPAIR/NO_LAUNCH`: v1 retains independent `HW1/HW2` in the rank-6048
  selector algebra while claiming the rank-432 `a00pp` collapse, so its
  raw-D21 self-comparison cannot certify the forecast lane.  It also has
  incompatible post-pilot custody, and an in-place preflight revision broke
  three old hash entries.  V2 must substitute `HW_i=hW_i` before
  multiplication and regress in the collapsed algebra.
- **Demote high-Hensel to a branch-local diagnostic.**  The repaired runner's
  registered `p^2` point satisfies the exact E relation and W-unit gate, but
  Newton corrections still solve only the raw 184 rows.  An `N16` pass is a
  finite raw congruence with a necessary template gate, never branch survival;
  failure obstructs only the deterministic correction history.  Require one
  coherent schema-v2 seal and hostile AWS review before running it.
- **Endpoint remains the independent proof-side discriminator.**  TRIPLE02 R4
  is resealed with unchanged mathematical payload and repaired whole-process
  custody; it awaits independent review before any AWS launch.

## Superseding strategy overlay (2026-08-28 19:33Z — D43 two-track discriminator)

- **Avenue 2 now has two concurrent source-only discriminators.**  The
  high-Hensel core is mathematically reviewed and reproduces the known `p^2`
  point, but its resume provenance and AWS harness must be repaired before
  finite lifting to `p^16`.  Treat a pass only as branch evidence and a later
  failure only as obstruction of the deterministic earlier kernel choices.
  In parallel, exact-sparse preflight `539d6407...` identifies a 22-coordinate
  quadratic route: emit all 184 pristine rows over the exact radical/W
  algebra, impose exact E5 quartics, solve the three band-compatibility blocks,
  and replay exactly.  This can bypass formal smoothness and NF equivalence if
  it succeeds.
- **Prefer support specialization while the full exact compiler is upstream-
  blocked.**  The box01 monolith has seven of nine orbit checkpoints and is
  still inside `GB42`; the final g checkpoint required by a general 19-band
  shard is absent.  Do not consume the incomplete prefix or interrupt the
  monolith.  Build the exact 22-support source directly with every complement
  coordinate pinned to zero; keep the general sharder fail-closed until its
  final input hash exists.
- **Avenue 1's TRIPLE02 algebra is ready, its custody is not.**  Hostile R2
  `fa9c8f00...` passes the repaired saturation/`c4`/transform/kernel/endpoint
  mathematics and blocks only the process supervisor, terminal downgrade,
  and one false mutation.  Repair and rereview those operational gates; do
  not recompute the algebra or launch an uncontained Singular process.
- **Compute capacity is not the bottleneck.**  Live audit gives 336/512 AWS
  vCPUs allocated and 208 currently idle with zero swap.  Once either packet
  passes review, launch it immediately on an isolated, hash/tag-bound worker;
  fan out exact row shards only after the pilot and final-checkpoint gate.

## Superseding strategy overlay (2026-08-28 19:08Z — D43 essential-source repair)

- **Avenue 2 / residue A — the coordinate bridge is closed, not open.**
  Hostile review `9ad0bef5...` proves that the eight nominally omitted D43
  source directions are causally invisible level-71/73 tails.  Removing them
  leaves 182 essential source coordinates; adjoining `uW1,uW2` with the two
  literal inverse equations gives the clean 184-coordinate chart.  Do not
  spend the critical path reconstructing dummy variables or emitting all 34
  parked rows.
- **Attack source-local existence directly.**  The pristine 184 Euler rows
  are the complete finite-D43 source system.  Its rank-129 point has tangent
  dimension 53 and a positive `p^2` correction, but the 55 dependent rows
  still need local-generation/formal-smoothness control.  In parallel, test
  whether the banked sparse origin slice can be solved and replayed directly
  over the exact radical field; an exact point would bypass Hensel entirely.
  A configurable higher-digit source lift is useful only as an obstruction
  detector and must not be promoted from finite precision.
- **Keep NF equivalence off the direct critical path.**  Exact provenance for
  the other 32 parked rows and the 509 reducer/184 raw-to-NF traces remains a
  valid parallel reuse/equivalence project.  It is not required to define or
  find a source point.  Any nonzero x-side lift must incorporate the exact
  `alpha,beta` value formula; freezing them requires fresh rank and lifting
  gates.
- **Portfolio ranking remains unchanged.**  D43 is the deepest residue-A
  coefficient carrier inside principal Avenue 2; the GGV endpoint lane stays
  independent.  A finite D43 characteristic-zero point would advance only the
  finite algebraization ladder, not resolve JC2.

## Superseding strategy overlay (2026-08-28 18:50Z — D43 split gate and endpoint launch block)

- **Avenue 2 — separate source existence from NF-presentation equivalence.**
  Preflight `891096dd...` shows that the 509 exact D23 reducer traces are an
  equivalence cost, not an intrinsic prerequisite for a fresh source-defined
  model.  This overlay initially put a coordinate descent and the `26+3+5`
  parked package before source smoothness; `9ad0bef5...` supersedes that
  ordering because the unmatched tails are invisible and the `uW` variables
  are exact inverse auxiliaries.  Those emissions now belong only to the
  fuller 509-trace NF-equivalence lane.  Skeleton `5420b5c0...` verifies
  the common rank-432 coefficient algebra and registered specializations but
  intentionally cannot seal either mathematical lane yet.  D75 and `B=168`
  remain held.
- **Avenue 1 — keep implementing, but do not launch the endpoint resume.**
  Hostile review `a5400d24...` approves the saturation diagnosis and removal
  of the impossible Delta-power search, while blocking the pilot over an
  invalid `c4` omission and incomplete saturation/provenance/kernel gates.
  Retain `c4`, reconstruct and replay the full right transform, bind exact
  counts and mutations, and obtain a new hostile source pass before AWS.
- **Portfolio ranking is unchanged.**  Source-theoretic Avenue 2 remains the
  principal proof backbone and residue-A D43 the deepest coefficient carrier;
  reducer-safe GGV endpoint recursion remains the independent parallel
  discriminator.  Neither preflight result is a JC2 proof/counterexample
  advance by itself.

## Superseding strategy overlay (2026-08-28 18:29Z — TRIPLE03 geometric cover closes)

- **Avenue 1 — stop the literal TRIPLE03 component.**  Root replay
  `18533774...` passes both quotient-reducer semantic plants and every
  original artifact/kernel identity.  With the planted R5 complement
  `b31a9b14...`, scoped theorem `dd162daa...` proves the endpoint quadratic
  vanishes on the complete certified fiberwise kernel at every complex
  geometric point of literal `q1=c8=q2=0` TRIPLE03.  This is exact
  set-theoretic component-local endpoint death, not ideal/scheme/radical,
  nilpotent, other-component, or ambient death.
- **Do not transfer the verdict to unfinished branches.**  `P`, `C8P02`,
  `Q1P02`, and `TRIPLE02` capped after their certified rank step and remain
  `TIMEOUT_NO_VERDICT`; `Q1P03` has an uncovered proper remainder.  Reuse the
  now validated plant/reducer adapter, but change algebraic strategy before
  repeating the four saturation timeouts.

## Superseding strategy overlay (2026-08-28 18:22Z — history checksum restores the D43 gate)

- **Avenue 2 — keep residue A critical, but restore its actual frontier.**
  Correction `41e17e93...` reclassifies the cross-round's
  `SUBST-TRANSPORT` proposal as a duplicate of TEMPLATE E4--E6 plus the
  existing R1/R6 coefficient program.  The attempted broad
  `m_{G_m}=1` inference is rejected: `J(f,h_1)=2g` does not force the
  leading-pattern bracket to be nonzero, and three live R6 genomes put the
  terminal `q` in `h_2`.  Conditional minimal-genome transport
  `87cfc52b...` survives over a finite complex extension and is a regression
  seed, not a discriminator.
- **Resume at the deepest certified carrier, not the first edge.**  The
  fixed-`B=84` D43 family is nonempty modulo two primes in its complete
  184-variable/218-row presentation and its pristine 184-row source point
  lifts through `p^2`.  This overlay initially queued a common 34-row/509-
  reducer emission and all-218 replay.  Reviews `891096dd...`/`9ad0bef5...`
  later separate that NF-equivalence project from the direct source lane,
  whose smallest missing bridge is rank-129 local generation/formal
  smoothness.  D75 and `B=168` remain held.
- **Retain two independent proof-side tracks.**  The four per-genome R1
  nonlinear cores remain valid coefficient-realizability attacks, while the
  source-theoretic landing/`RPMC(C)`/cofinal walls remain the global proof
  backbone.  Do not rerun E4--E6 or mistake a conditional minimal-genome
  seed for coverage of the three `m_{G_m}=2` towers.  The GGV endpoint
  Fitting recursion continues independently with planted semantic controls
  and chart-complete scope.

## Superseding strategy overlay (2026-08-28 17:53Z — mixed-root edge theorem and `ROOT-W2`)

- **Avenue 2 — the two-pole `td=6` pole-chain root-meet branch is now
  closed.**  The mixed-root theorem `aaa4d179...` gives `0<w<1` on every
  actual contact-zero root edge, for arbitrary `mu`.  The narrow dependency
  integration `645b9424...` verifies that MP1+MP4+MP5/D5 carries each
  forced row-1 branch to a last nonroot parent with exact `W(2)={2}`.
  This is a contradiction at the root.  The repair never invokes `L1a` or
  Proposition 8.4 at `(0,y)` and uses no AWS census.
- **Residue A is consequently the two-pole critical path.**  Literal MFE
  remains `NO HIT` (`2<=3` at R3, `2<=2` at R4), because its q-only
  resonant orbit is not a `T_a` direction.  The four-model cross-round
  (`0002169e...`) finds the Q-level two-parent match symmetric-trivial.
  Run `SUBST-TRANSPORT` instead: actual Puiseux substitution and common
  h-family `ominus`-constant compatibility over `Q(sqrt(3))`.
  Incompatibility kills the cell; compatibility must emit one
  coefficient-complete germ for a guarded jet lift and any independently
  grounded off-tree/asymptotic-value probe.
- **Do not overgeneralize the root theorem.**  It kills any root edge only
  when its actual incoming alphabet is independently certified in
  `[1,infinity)`.  A root contact involving a post-interior suffix and a
  non-pole/northeast direction, all-`mu>=2` off-axis/post-jump alphabets,
  single-pole/SF1 landing, and interior merges remain separate.  The GGV endpoint lane
  continues in parallel.  Its first complete mutation-safe node has passed;
  every promoted branch additionally needs a planted-nonzero NF control and
  a complete Fitting-level remainder cover.

## Superseding strategy overlay (2026-08-28 17:03Z — root layer and multipole MFE restored)

- **Avenue 2 — one false demotion repaired, with strict scope.** A
  dual-model re-audit proves that a genuine root meet is Proposition 9.3
  case I and satisfies `X_root=mu(1-w)`; in the all-`mu=1` anatomy this is
  `w=l/(r+l) in (0,1)`. Since the exact row-1 `td=6,m=2` alphabet is
  `W={2}`, the all-`M=1` root-meet layer is analytically empty. This does
  not kill root `M=1`, mixed-`mu`/off-axis/SF1 roots, or the nonroot
  residue-A survivor. Root-aware AWS runs are diagnostics for those broader
  completeness questions, not a dependency of the repaired theorem.
- **Multipole first-exit gate passed.** The ambient attachment repair
  `9f4526f2...`, Fable5 hostile review `f55a00f5...`, and integration
  `c74fc0f9...` prove unique selected-exit witnesses and shared-suffix
  deduplication at MFE scope. The global selected-exit inequality is now
  green and should be composed immediately with every multipole survivor.
  Equality, printed `(22)`, `(22-cl)`, fixed `kappa`, and MP8's
  no-refinement claim remain quarantined.
- **Avenue 1 remains a parallel discriminator.** The six reducer-safe ranks
  are certificates only. Continue with localized adjugate right kernels,
  endpoint pullback, and recursive rank-chart complements; a dense-open
  endpoint death cannot kill a reducible branch by itself.

## Superseding strategy overlay (2026-08-28 16:22Z — quotient ranks repaired; multipole ownership candidate)

- **Avenue 1 — reducer-safe recursion is operational.**  Exact
  quotient-normal-form residual/total ranks are now `9/104` on `P` and
  `C8P02`, `6/101` on `Q1P02` and `TRIPLE02`, and `4/99` on `Q1P03` and
  `TRIPLE03`.  These are rank certificates only.  The next discriminating
  step is localized right-kernel pullback of `x14*x72+x1*x97` on explicit
  nonzero-minor charts, followed by Fitting recursion on every chart
  complement; do not infer endpoint death from rank or from one dense-open
  chart on a possibly reducible branch.
- **Avenue 2 — source backbone remains principal; multipole budget may
  tighten.**  Actual-weight Corollary 7.1, singleton first exits and the
  corrected `counterexample=>td>=6` theorem remain review-closed at their
  stated scopes.  Candidate `86b491ad...` proposes the missing global
  multipole partition by unique attachment to the rootward-closed union of
  pole paths; it is under Terra hostile review and not yet promoted.
  Separately, exact root repair `93f98e4d...` removes the unbounded MU1 cap.
  The older DEPTH root kill is withdrawn because a contact-zero root merge
  can be case I rather than case IV; its nonroot finite-`w` theorem survives.
  Root/SF1 and two-pole counts remain pending the reviewed AWS recensus.
- **No endpoint shift yet.**  These advances strengthen the finite
  discriminators and remove two false closures, but they prove neither
  realizability nor exclusion at `td=6`.  Landing, `RPMC(C)`, cofinal
  complexity control, and coefficient compatibility remain the large
  proof walls.

## Superseding strategy overlay (2026-08-28 14:00Z — source backbone tightened; rank-drop recursion)

This overlay updates the 12:24Z execution state without changing the broad
avenue ranking.

- **Avenue 2 — retain as principal proof backbone; source risk materially
  reduced.**  The Proposition 5.3(iv) pole degree pin and Proposition 5.4
  `q`-half are promoted (`be9e4b74...`, Opus5 `c3f0bfde...`, integration
  `6375bc05...`).  Proposition 6.7 is review-closed after cyclic-equivariance,
  Statement 3.11 and tower-prefix repairs (`eb37373b...`, correction
  `050ccddd...`, integration `1e66bff1...`).  Proposition 6.8's finite-grid,
  same-branch proof is now review-closed: repaired Lemma 6.1 R2
  `2fdbbee9...`, Opus5 review `5193e7b0...`, correction `607e0dcf...`, and
  integration `bb033a71...` discharge its last rider.  Statement 6.2 remains
  defective in print, but concrete next-vertex consumers establish the
  omitted positive domain through the repaired bridge.  Proposition 5.8's
  source proof remains absent, while its every-fiber campaign replacement is
  promoted twice over: the reviewed relative-surface/Chau proof and the new
  positive-pole transport audit `47eef092...`.  The next highest-value work
  is the active Sections 7 and 8 source audit.  The proof-wide walls remain
  full-configuration landing, `RPMC(C)`, independent type/cofinal control,
  and coefficient realizability—not either of these closed source riders.
- **Avenue 1 — raw cofactor redesign succeeds as a discriminator, not yet as
  an endpoint survivor.**  R4 (`7926216f...`) proves generic rank 105 and a
  two-slot raw cofactor support `{81,93}`, with rank-drop set
  `V(c8*q1*P)` but raw ideal `(M81,M93)` preserved.  Parallel exact AWS
  pullbacks (`e0c50ab...`) make the generic `c8` and `q1` charts endpoint-dead;
  six `P` samples are also zero but are only a screen.  Do not return to blind
  degree/order expansion.  Compute the symbolic generic `P` chart and the
  complete quadratic pullback on every pairwise/triple intersection and
  further Fitting rank jump.  A survivor would be actionable; generic death
  does not license killing intersections with enlarged kernels.
- **External intelligence unchanged.**  Fable5's full-text audit
  `c5655b43...` refutes both Matysiak SSRN papers at their first load-bearing
  steps.  They remain archived with `_unsound` labels and do not reopen any
  proof avenue.

## Superseding strategy overlay (2026-08-28 12:24Z — intrinsic frontier and rank/Fitting redesign)

This overlay supersedes the execution priorities, but not the scoped theorems,
in older overlays below.  Four blind whole-portfolio submissions are closed
and adjudicated in `xmodel/ideation-20260828T1149Z-synthesis.md`
(`c043ab47...`).

- **Avenue 2 — RAISE; principal proof backbone.**  For a normalized Lemma
  2.1 fibre, intrinsic L3/L4/L5-exact and the corrected Proposition 4.2 tower
  are promoted; the reviewed no-first-corner theorem makes condition (7)
  automatic on `T_a^+` for every target shift.  The first large open
  implication is a total,
  provenance-preserving full-configuration landing theorem.  `RPMC(C)`, an
  independently sourced type menu, and a cofinal `td`/complexity ceiling
  remain downstream walls.  The queued source-trust stack through Sections
  5--9 is now review-closed at corrected scope: Proposition 5.4 and the
  6.7/6.8 bridge are promoted; actual-weight Corollary 7.1 repairs singleton
  exit budgets; and corrected Section 9 proves `counterexample => td>=6`.
  Printed (22), fixed-baseline transport, multipole cross-chain disjointness,
  and exhaustive root-aware `td=6` books remain outside that closure.
- **Avenue 1 — CONTINUE ONLY AFTER REDESIGN.**  Stop equivalent arbitrary-`Q`
  Groebner variants and stop increasing isolated lambda-nonzero face order.
  For the frozen `106 x 105` receiver matrix, compute the 106 **raw signed
  cofactors without common-factor cancellation**, recurse on pivot-denominator
  and Fitting strata, and pull the quadratic endpoint
  `x14*x72+x1*x97` back to each kernel.  In the unit-`S` branch, reviewed
  orders four and five are regular and add no cut (`5df1d312...`, integration
  `5888c28a...`); seek a finite-mode recurrence/row-ideal invariance theorem
  or the complete raw window/endpoint incidence rather than order six.
- **Avenue 4 — RAISE NARROWLY.**  Exact terminals now certify a
  characteristic-zero germ from finite data.  The load-bearing remaining
  arrow is still germ-to-polynomial algebraization plus an actual collision
  or nonproper end.
- **Avenues 6, 7 and 31 — BETTER-TYPED PILOTS, NOT GLOBAL BOUNDS.**  Exact
  place/orbit/order data and the promoted Proposition 5.1 sided-threshold /
  non-leakage theorem now type finite punctures and prove Statement 7.2.
  They may now support semigroup, asymptotic-set and Rees tests using the
  reviewed Corollary 7.1 inequality.  Exact per-puncture/slack identities
  remain unavailable, and multipole charge unions require a separate global
  disjointness proof.  The provisional
  `M_F=1` consequence at a later constant corner is only a local
  discriminator; even after review it would not bound conductor or pole mass.
- **Avenue 16 — LOWER execution priority.**  HENS-CT timed out before
  creative telescoping on its charged control.  Do not repeat the generic
  composition backend without a structural adapter and a named client.
- **Avenue 25 — GATED PILOT ONLY; Avenue 26 stays down.**  L5 alone does not
  provide coupled two-coordinate inertia or finite-end provenance.  Do not
  rerun the already negative generic `td=6..9` monodromy census.
- **Avenue 36 / K00 clients — RAISE and protect.**  A source-open,
  rank-exact full-`P6` point plus one prolongation remains the strongest
  bounded counterexample seed.  K00 is not Avenue 19; Avenue 19 remains the
  characteristic-`p`/Witt lane.
- **All negative calls remain negative.**  The Matysiak papers are
  refuted-on-audit and archived only as intelligence.  Fixed-face GGV facts
  do not discharge pure-tree hypotheses; `G2-PSC` and `G2-BD` remain
  separate obligations in hybrid architectures.

## Superseding strategy overlay (2026-08-28 06:23Z — fixed upper endpoint empty; general V0 live)

This is the current authority over the 22:40Z gate-open-status and provisional
bi-face language below.  The blind 22:59Z follow-on round is sealed.  Its
principal claims, auxiliary rank-one controls, and rows-31--34 nonlinear
class descendant are review-closed.  Its zero-tail Artin
algebraic-primitive identity and raw polynomial-window successor are also
review-closed: the class/primitive gates pass, but the raw endpoint is empty.

- **Avenues 1 / 16 — raise fixed-instance nonlinear gate compression.**  The
  correctly transported sector series is
  `C_b(z)=sum_k [p^(-(b+22))q_(b+4k)dX]z^k` in one fixed de Rham receiver;
  `sum_k H^k q_(b+4k)z^k` is not the gate transport.  Different-model review
  confirms that every coordinate of `C_b` is D-finite/P-recursive and that
  exact creative telescoping computes a deciding finite prefix at every fixed
  `H,F` point.  A generic operator/order bound exists on a fixed-`H`
  irreducible cell, but no numerical cell-uniform `N0` follows without new
  specialization control.  The repaired `HENS-CT` bridge now reproduces the
  upstream algebraic-integral example with an exact order-two telescoper and
  nonzero Ore/field certificates.  The charged rank-one control nevertheless
  times out after four hours inside generic multivariate
  `annihilator_of_composition`, before creative telescoping and without a
  candidate.  Treat this as a backend timeout, not a mathematical verdict;
  do not run the real branch or repeat the same composition path until a
  structural adapter avoids that bottleneck.  This proves neither nonlinear
  nonvacuity nor polynomial descent.
- **Avenue 1 source filtration — add the reviewed `nu=n+2-8d` law.**  It
  gives exact homogeneous pieces and receiver-capacity bounds but not generic
  realized ranks, so keep the frozen exact-rank harness as a regression and
  decision instrument.  Every class row `m==4 (mod 8)` is identically dead
  in all degrees; in particular row 28 cannot test nonlinear class carry,
  although raw `D28` may still encode descent/window information.  On branch
  P, row 29's newest-slot map is surjective, making the first honest
  noncancellable nonlinear class candidate row 30 rather than row 28/29.
  The finite raw `D_0..D_34` target dominates the derived tower
  set-theoretically but is not a row-34 cutoff for it.
- **Avenue 1 nonlinear carry — row 30 is now nonvacuous on an exact class
  prefix slice.**  Different-model replay confirms that
  `F2=4u*A^2*A'`, `F4=2u^2*(A')^2`, `F6=v`, with all odd slots through
  `F7` zero and `A=X^4-1`, passes class rows 23--29.  Its row-30 residue
  ideal is `(u*v,u^4)` (radical `(u)`), so `u=1,v=0` is an explicit class
  prefix that fails row 30.  Different-model review now also confirms its
  complete rows-31--34 descendant: rows 31/33 are split-surjective in the
  newest slots, row 32 adds no base cut, and row 34 adds exactly `v^2`.
  The resulting base is the length-five local algebra
  `Q[u,v]/(uv,u^4,v^2)` with nonzero square-zero tangent `v`, and the total
  slice is free in 24 newest-slot coordinates over it.  Freeze this class
  calculation and move to a **genuine reduced raw/polynomial-window seed**.
  The zero-tail Artin section is now review-confirmed raw-dead at `D22`, so
  stop it; this kills only that section, not the 24-slot family.  Do not read
  any class statement as a raw determinant prefix, polynomial-`G` witness,
  endpoint, or family exclusion.
- **Avenue 1 reduced raw endpoint march — retire tails 7, 6, and 5; descend
  to tail 4.**  Different-model Opus5 clean-room review confirms that the whole
  characteristic-zero field-valued tail-7 endpoint stratum is empty in the
  fixed square-tail chart.  The endpoint makes `p87=F7[0]` a unit; sixteen
  homogeneous row-14 quadrics dehomogenize to the frozen eight-variable
  block with an exact identity `1=sum h_i*f_i`.  Row 21 supplies the
  producer's reviewed `p86=0` link, although Opus found it redundant.  Stop
  tail 7; the stronger scheme/positive-characteristic extension remains
  provisional until its new p86-free cofactors are frozen.

  The tail-6 desk compiler is exact and substantially smaller.  Rows 12--13
  force, fieldwise,
  `F6=(X^4-1)^2*V`, `deg V<=2`, and split into
  `V(0)=0` or `V(0)=4*F7(0)`, while the endpoint makes `F7(0)` a unit.  The
  honest row-14/15 targets retain symbolic endpoint carriers and have only
  22/23 variables and 20 independent quadrics.  A frozen tracked-RREF
  certificate reconstructs a literal `F7(0)^2` from source-row
  compatibilities on both branches and combines it with the endpoint into a
  two-generator unit identity, without CAS or AWS.  Different-model Fable5
  review independently rebuilds the raw constraint system, progressive
  eliminations, both source-row witnesses, and live mutations.  Therefore
  the characteristic-zero field-valued tail-6 endpoint stratum is empty;
  stop it.  The row-12/13 radical reductions do not yield upstream scheme
  emptiness.

  Tail 5 is also review-closed.  Its six exact field-radical steps give
  `F5=H*V`, `F6=V/2+H*R`, and `F7=R/2+H*Q`, with
  `c=(3/16)V0(V0+4R0)`.  Rows 16--17 add
  `V0*(b*V0+R0^2)=0`.  A division-free unit excludes `V0=0`; on
  `V0!=0`, exact reduction modulo `12*tau^2+6*tau+1` reconstructs `1` from
  eight literal-provenance row-16--18 compatibilities.  Fable5 review
  `0312ef63...` independently rebuilds the complete raw specialization,
  both certificates, and six live mutations.  Stop tail 5.

  Tail 4 is now review-closed.  Eight field-radical steps reach
  `F7=U/2+H*Q`; `D18` adds `v*(b*v+r^2)=0`.  A division-free endpoint unit
  excludes `v=0`.  On `v!=0`, exact parameterization gives
  `v=-(3/4)*tau^2*P3(tau)`, while a literal-provenance D20 combination is
  `tau^9*P3(tau)^3`, an immediate contradiction.  Grok46 review
  `adbe2aa4...` independently reconstructs every step and mutation.  Stop
  tail 4.  Tail 3 is now the live rung; its `T=0` branch is exactly cutoff
  four, while the projective `T!=0` cover remains under exact reduction.
  Its first six-chart modular AWS launch is operational non-evidence: a live
  audit found that nested GNU-`timeout` PGIDs escaped the recorded RSS/stop
  groups, and every worker was safely terminated before any algebraic marker.
  A fresh immutable R2 runner used `timeout --foreground`; it passed an
  actual RSS-inclusion, TERM/no-orphan regression and independent custody
  replay and ran all six modular charts on r6d with zero swap.  The later
  reviewed full-fixture endpoint contradiction strictly subsumed these
  charts, so R2 was stopped orderly by TERM to exactly the six validated
  worker PGIDs, with no KILL.  No algebraic marker preceded the stop; the
  runner and guard exited naturally, all process censuses are empty, final
  swap is zero, and complete local/remote custody replays.  Classify every
  chart only `OPERATIONAL SUPERSEDED STOP — NON-EVIDENCE`; do not restart it.
  Every tail statement remains confined to the fixed square-tail
  specialization, not full branch P.
- **Avenue 1 full-fixture characteristic ladder — the fixed endpoint is
  review-closed empty; transplant to general `V0`.**  On the complete fixed
  303-variable fixture, reviewed exact
  Laurent numerators and literal raw mutations prove
  `D7=0 => A|T`; after `T=A*V`, they prove
  `D8=0 => A|(F4-V/16-Z^2/64)`.  This simultaneously explains the first
  cutoff-three steps and the cutoff-four D8 step.  The different-model
  producer required two harmless local repairs: the intermediate `L7` sign
  reverses, and `(F^(3/4))_2=3/(32A)+(3/16)AZ` always has pole order one.
  The same full D8 equation then kills the scalar mode `c6`, with raw class
  `-(9/4)c6*A^2*A' mod A^3`.  Only after that mode kill does D9 have leading
  class `(21/4)A*A'*W^2 mod A^2`, forcing `A|W` field-radically.  Grok46
  clean-room review `0a075a73...` independently reconstructs the raw rows,
  full nine-mode causality, both signs, and 189 hostile checks.  Continue the
  paired Laurent/raw compiler at D10, serializing every born and forced
  characteristic mode.

  The reviewed continuation now gives the honest cross-term completions
  `F5=R/2+ZV/64+A^2Q` and
  `F6=Q/2+RZ/8+V^2/256+A^2T`.  D10 and D12 are order-two square defects;
  D11 and D13 force their second `A`.  D11 then kills scalar `c10`, whereas
  D10/D12 have no mode kill and `c12` survives as the additive `G12[X0]`
  gauge.  Grok46 review `9363d7cc...` independently rebuilds D4--D13 and
  passes 499 checks, including literal nonzero-`c16` regressions.

  At D14 the complete pole is
  `3*(F7-T/2-QZ/8-RV/16)^2/(8A^2)+c14/A`.  The square forces a first `A`;
  then the rank-10/nullity-0 polynomial `G14` window forces `c14=0`.  D15
  supplies the second `A`, giving
  `F7=T/2+QZ/8+RV/16+A^2Y`, while `c8` survives and `C13--C15` stay live.
  The former same-row-only assertion that D15 kills `c14` is retired: the
  same-row `-3c14A'` cancels its predecessor mixed `+3c14A'` exactly.
  Grok46 review `e4788bb8...` independently passes 334 checks.  Continue
  provisionally at D16 with the same causal discipline.

  The next reviewed pair is no longer a pure square-defect step.  With
  `B=F8-Y/2-TZ/8-QV/16-R^2/4`, D16 requires
  `3B^2+4c8B+8c16=8A^2M`; `c16` is unique but can be either sign/nonzero.
  The gauge-fixed coordinates are `Bhat=B+2c8/3` and
  `J16=c16-c8^2/6`.  D17's complete linked `c16` mode cancels and leaves
  `(3B+2c8)C-2M=4A^2N`, where `C=F9-QR/2-TV/16-YZ/8`.
  Grok46 review `c2015d74...` passes 361 checks, including both divisibility
  lifts and all literal controls.

  The complete reviewed continuation now closes the endpoint.  D18 and D20
  kill `c18,c20` at their birth windows, while D18--D21 give four exact
  A-squared quotient relations.  Complete `g22` then has minimum A-exponent
  `-2`; because raw `G22` is absent,
  `D22_raw=-L22(g22)` lies in `(A)` and cannot equal target polynomial `1`.
  Grok46 review `a294cdf7...` independently passes 289 checks; a separate
  Sol packet replays all 513 rows and endpoint mutations.  Stop every
  `A=X^4-1,V0=1` tail/solver lane.

  This fixed slice is a q1-negative diagnostic, not a genuine survivor.
  The live successor is the general prefix `F1=A^2V0`.  The exact D7/D8
  producer shows why the fixed-slice argument does not transport verbatim. With
  `C=gcd(A,V0)`, `A=CB`, `V0=CV1`, and `T=BU`, D7 is exactly
  `C | U(K-2UV1)`, and literal raw points have `A` not dividing `T` while
  satisfying every row through D8.  Independent exact hostile review
  `1874b9db...` reconstructs all modes, raw windows, divisor endpoints and
  active-`c2` valuations and returns `REPAIR`: the complete D9 leading
  classes force `A|T` at characteristic-zero field points on the full
  reviewed `c2=0 or A|V0` cover **without q1**.  Neither `R0`,
  `V0=A'R0+2AR0'`, nor D23 is used.  After `T=A*U0` and
  `F4-V0*U0/16-Z^2/64=A*W`, the remaining c2-zero condition is
  `A^2 | W[-16V0W+A(64F5-U0Z)]`; it forces only `B|W`, so the next live
  split only if one continues rowwise.  The stronger unit-root transport is
  now independently reviewed with a repair.  Opus5 clean-room checker
  `cbf8498e...` reconstructs the missing local D7--D15 bridge from the
  general reduced prefix and proves that, at every simple root `alpha` with
  `V0(alpha)!=0`, the complete `g22` has minimum A-exponent `-2`; the
  endpoint row therefore lies in `(X-alpha)` and cannot equal `1`.  Thus,
  for squarefree `A`, every `C=gcd(A,V0)!=A` endpoint stratum is
  field-empty.  The producer's identification of the pre-D7 normalized
  prefix with the pinned post-D7/post-D9 prefix is retired, as is its
  inadequate checker for the endpoint bound.  No q1 or global D9 theorem
  is consumed, but local D7--D9 rungs are load-bearing.  The sole surviving
  endpoint target is now the deep `A|V0` locus.  Analyze that locus directly
  and do not gauge it to `V0=0`
  by the tempting shear `X -> X-a*t`: although the shear preserves the
  determinant equation and target, it creates forbidden lower-X raw slots
  and a bound `G22` receiver.  No scheme emptiness, complete endpoint,
  unrestricted branch-P, Keller, or JC2 conclusion follows yet.

  The first reviewed full-system compression of that deep locus is now
  available.  D23/q1 gives `R0=lambda*A` and `S=3lambda*A'`.  Keep
  `lambda=0` and `lambda!=0` separate: Opus5 r1 `76529aa2...` explicitly
  replays the forbidden `F9[X0]` and `G13[X0]` shear tails.  On the
  `c2!=0`, exact-`D=0` successor, D25/q3 composed with the reviewed D12
  lift determines
  `U=-(3lambda/4)(A'Q+A Q')-(9lambda^3/2)A A'''`; in particular the zero
  branch has `S=U=0`.  Fable5's independent D8--D15 reconstruction
  `042dbacd...` now validates the exact-`D=0` dependency and pinpoints why it
  must remain exact: if `D=A*d1`, D11 retains the generally nonzero pole
  `(N11+4*d1*B11r0)/(4194304*A)`.  Do not extend the result to mere `A|D`;
  route those valuation strata through that extra residue equation.  This
  consumes actual full-system rows D23--D25 and
  cannot be imported into a D22 endpoint-only calculation.  The complete
  zero-branch odd tail through q13 is now independently reviewed
  (`b649e082...`, checker `c063ccc2...`).  For `q_n=p^3h_n`, exactness is
  precisely `h_n=(5A'd+2Ad')/2`; with every literal raw floor retained, the
  five gates q5--q13 always leave at least nine raw dimensions for each fixed
  legal even prefix.  Two exact quartic fixtures have dimension exactly nine.
  Moreover every even q-gate is automatically exact because each contributing
  monomial is polynomial and divisible by `A`.  Thus do not spend another
  lane trying to obtain emptiness from q5--q13 or an even gate alone.  The
  licensed q15 successor (`18831713...`, checker `6c63fe47...`) also leaves
  raw dimension at least five on the exact `r=0` slice, generically five.
  That statement is only about the q-fiber.  The literal raw origin equation
  is
  `F11[X1]*G11[X0]-F7[X0]*G15[X1]=1`; independently reviewed report
  `c940ba04...` and checker `23f46a95...` prove its sign and slot census.
  Hence the all-odd-zero section is empty, because both `F` factors vanish.
  The associated `s=t^2` weighted-Jacobian rewrite is a checksum, not the
  live survivor and not the already closed AS109 sextic normal form.
  The original exact `Q=X` mutation in `c940ba04...` satisfies q7--q15, the
  target after a quadratic constant-field scaling, and G11/G15, but its first
  omitted window fails at `G13[X0]`.  More strongly, Fable5 clean-room review
  `8b91115e...` confirms the entire fixed prefix
  `Q=X,r=e=F8=F10=F12=F14=0` is endpoint-empty: on the full five-dimensional
  q fiber the endpoint lies in the polynomial ideal generated by the G13
  lower-floor coordinate and a G15 pole residue.  The review repairs only the
  mode census wording—c12/c14 are born but have zero relevant odd loads.
  Retain arbitrary `Q,e,F8` in the live intersection; 875 exact quadratic-Q
  navigation fibers are all origin-isotropic but are sampled evidence, not a
  universal theorem.  The AWS general system has been exactly deduplicated
  from 169 listed generators to a replay-equivalent 17-generator core after
  the unreduced serial membership order timed out without a verdict.  Search
  sparse syzygies and alternate block orders on that core before appending
  G16--G21 and the 15-coordinate four-root equations.  Do not generalize the
  fixed `Q=0` G15 residue obstruction.  That obstruction is independently reviewed
  (`37103f26...`, checker `ce1ce5b4...`): it proves only
  `5*D22[X0]+6*F7[X0]*R[X1]=0` on `Q=e=F8=r=0`, and its own mutation grid
  confirms failure at `Q=X`.  In parallel run a direct unit-`S` Newton
  calculation on the nonzero branch, and retain the other D-valuation and
  `c2=0` components as separate uncovered strata.

  Exact symbolic elimination now excludes a nonempty Zariski-open subset of
  the quadratic-Q fixed face.  Report `0775a496...` gives a 106-by-105 matrix
  of generic rank 105, so all 105 odd/primitive/receiver variables vanish.
  This forces every possible quadratic-Q survivor onto the rank-drop variety;
  it does not close that variety.  The coordinate-plane rank drops and 875
  exact sampled fibers are navigation only.  Decompose the maximal-minor
  locus and test the endpoint on each kernel before releasing e/F8/even tails
  or appending G16--G21/four-root.  Omitted c10/c12/c14 are justified by zero
  odd loads, not by calling them unborn.

  On the distinct `lambda!=0` branch, the first unit-`S` correction is now
  independently checked (`c3f03f37...`, review `79076a9c...`).  At a simple
  root, exact `D=0` forces `R=4SU` and `F5=S^2U/128`, so the complete first
  correction factors as `U*tau^3*(S*tau+4A')^2/128`.  The complete mode
  census is regular through relative order three.  Relative order four is
  the first possible pole test, with negative face powers possible only in
  the pure and `c2` modes.  Compute that full order-four coefficient with all
  born modes and raw floors; do not spend lanes rechecking orders one--three,
  and do not infer that the possible order-four poles survive cancellation.
- **Avenue 1 cutoff-two characteristic reduction — firewall the five-mode
  truncation; retain the corrected nine-mode endpoint.**  Exact hostile
  audit finds that a raw polynomial solution can force the rational modes
  `c14,c16,c18,c20` to unique nonzero values.  The elementary literal gauge
  point `U=A^2+t/2`, `F=U^2+t^8`, `G=U^3` passes every row through `D22=0`
  but needs `c8=-3/2,c16=3/8`; the proposed five-mode `Phi22` therefore is
  not the raw characteristic deficit.  Do not consume the advertised 96+5
  reduction or its incomplete INEQ plan.  The independently checked source
  reconstruction, gauges, `R_n` absorption, five **free polynomial** modes,
  corrected full-nine-mode endpoint identity
  `A^5*widehat(Phi22)=X^5/40-X/8+gamma`, at-least-three order-five pole bound,
  and triangular `Xi=G^2-F^3` transport survive.  Any successor must
  serialize all four forced negative-power modes and replay the gauge
  mutation before review.  This gives structure, not cutoff-two emptiness.
- **Avenues 1 / 16 — retain two rank-one controls, not branch-P
  extrapolations.**  One exact `H=X^4`, degree-five prefix chart has eleven
  algebraically independent nonlinear residue functions through row 40, so
  post-linear carry is genuinely nonvacuous on that toy chart.  A separate
  fixed point has the closed form
  `c_n=(2n/(n+2))*binom((n+2)/8,n)` and a factored eight-step recurrence for
  which rows 23--30 suffice.  Use the latter as the first `HENS-CT`
  certificate/mutation fixture.  It already fails row 23, and neither result
  proves cutting on successive survivor loci, branch-P independence, or a
  family-uniform cutoff; do not reprice the endpoint march from these
  controls.
- **Avenues 1 / 4 / 16 — add the repaired algebraic-primitive filter.**  For
  an actual polynomial pair, the explicit F-only algebraic differential
  `alpha_F=-(s^21/16)(d_s(s^2P^2)-2p^2s)dX` must be `d_X`-exact in the
  selected finite field `K(F)(X,s,P,p)`.  Algebraic integration decides this
  in one finite de Rham computation, and a positive certificate implies the
  whole class tower.  The converse is false in shape, and this is now concrete:
  the reviewed zero-tail Artin section passes the entire class tower and has
  an explicit polynomial algebraic primitive, yet the literal `G` window
  makes its raw endpoint empty at `D22`.  Retain ALG-PRIM only as a one-way
  filter and test it on genuine reduced raw/tower survivors.  It supplies
  neither polynomial `G` nor window descent.
- **Avenue 36 / shared-source instruments — promote only the artificial
  `v_7=0` sublocus separation.**  Different-model review confirms the frozen
  442-slot graph fibre product is empty, and extends this exactly to upper
  branch-P data with `v_7=0` crossed with the full lower `(a,b,rho)` torus.
  It refutes any full branch-P separation: the explicit compatible point
  `a=b=1`, `v_7=-14rho`, `z_6=168rho^2`, `tt_9=-560rho^3` satisfies all eight
  boundary rows off that locus.  Carry the `R0_ARTIFICIAL_CUSP_CONTROL`
  firewall and the unreplayable-manifest-history gap.  Do not launch either
  `G2-PSC` or `G2-BD` on this pair.
- **External intelligence — Matysiak SSRN 7229358/7229458 is no JC2
  progress.**  Fable5's full-text first audit finds the headline proof stops
  at algebraic injectivity and simply asserts surjectivity, while its second
  proof is conditional/circular.  The dependency assumes the étale form of
  JC and uses an invalid general-linear Jordan reduction of Družkowski maps.
  Keep the hash-pinned PDFs in `refs/` as `_unsound` threat intelligence,
  run only a bounded different-model spot-check, and do not rerank avenue 17,
  avenue 31, or any live exact lane from these papers.
- **Avenues 18 / 30 / external intelligence — retain van Dobben de Bruyn's
  boundary theorem only as a redesigned secondary bridge.**  The obvious
  dimension-two symmetric-power descent never has `A^2` source: for a
  nontangent hyperplane the complement has Picard group `Z`, while in the
  tangent case it is `A1 x Gm`.  Stop that naïve construction.  The useful
  residue is a precise `KEF-ONE-VERTEX` discriminator: after ordinary
  Ramanujam minimalization of a hypothetical nonproper Keller source
  boundary, one surviving vertex with three nonspherical branches would
  contradict simple connectivity at infinity.  The unproved bridge is
  survival through full-boundary contractions together with pole,
  finite/dicritical and meridian custody; a map-relative nonlinear subgraph
  alone is insufficient.  Run at most one source-tracked full-graph/one-
  vertex test before repricing this lane.  Independent paper audit SHA
  `82a67d20...`.
- **Avenue 2 — remove Proposition 4.2 from the gap list, with the full
  erratum attached.**  Producer `10bc55d5...` and Opus5 review
  `47f2b608...` prove the unique final constant shift `(1,0,c)`, immediate
  termination, terminal degree/`M_F`, and the `T_a^nearrow` location.  The
  repair is not merely `l in N`: delete the circular p. 20 Remark, retain
  condition (7), transport zero-order corners to parents with Fact A,
  exclude them at the axes with Fact B, and cross-multiply the affected
  Proposition 6.2/6.3 identities.  Pole characteristic paths are
  corner-free.  Proposition 4.3, Proposition 5.1 at finite nonzero punctures,
  later source/audit gaps and landing remain open.
- **Primary exact-pair route promoted after Opus5 repair.**  Exact
  upper/LF40/K00 AWS decisions continue independently.  Producer
  `841c848e...`, same-model audit `5e8bc3c5...`, different-model hostile
  review `5d219c53...`, and coordinator-replayed independent checker
  `26e24ec...` remove L3/L4/L5 from the **intrinsic exact-pair** gap list.
  Sigray Proposition 3.1 supplies all-root strict truncation, normalization
  and the Kummer quotient give one terminal leaf orbit per boundary place,
  and the normalized Lemma 2.1 rectangle/NE-corner condition supplies
  two-chart coverage.  A terminal is the computable record
  `(h,chart,kappa,F)` with `deg p_{h,F}=1`; its Hensel continuation uses the
  shifted, `t`-cleared equation.  Orbit size is `e_S`, stabilizer size is
  `kappa/e_S`, and raw cover orders require the same rescaling.  This is
  false as an all-reduced-curves coverage claim, so the normalized-fibre
  hypothesis is part of L4-exact.  Hybrid VGG `H-TRUNC`/source coverage and
  uncertified terminality remain open, as do the remaining ancestor/source
  decorations, landing, `RPMC(C)`, and the cofinal degree ceiling.  A
  pointwise gate recurrence is a finite face instrument, not that global
  closure.

## Superseding strategy overlay (2026-08-27 22:40Z — four-model round closure)

This is the current live avenue overlay.  Full adjudication, model evaluation,
and custody are in
`xmodel/ideation-20260827T2137Z-synthesis-sol.md`; the `22:08Z` section below
is retained as the pre-close state.

- **Avenues 1 / 3 / 16 — raise algebraic gate compression narrowly.**  The
  reviewed all-row Lagrange formula makes the raw `(q_n)` sequence algebraic
  and P-recursive and subsumes the isolated `q2/q3` coefficient guesses.  It
  also exposes the exact E0 correction: every `k_m=1` row with `m>=28` has
  class-exact new-slot linear part.  Frozen degree-one ranks are `26/35`
  through row 34 and `27/37` in total on P/Q; uniform realized equality with
  receiver dimension stops at row 27.  The abstract `42/51` ceilings remain
  valid, and later nonlinear carry may still cut.  Therefore abandon every
  receiver-rate `GATE-MARCH` and build the compression on the full nonlinear
  sector section.  The reviewed raw `q_n` recurrence
  does **not** make de Rham gate coordinates P-recursive: their projection is
  only constant-field linear while the raw recurrence has `X`-dependent
  coefficients.  The next theorem is a creative-telescoping or fixed-
  receiver reduction with explicit singular-index control, first on one
  frozen P/Q stratum.  No effective or uniform finite gate prefix is yet
  known.
- **Avenue 1 lower branch — retain LF40 as a decision computation, discard
  obstruction-budget prediction.**  The uniform two-root endpoint analysis
  confirms the necessary `gamma == 3 (mod 4)` law and redirects local mode
  work, but `29 versus 36` compares localized receiver ceilings with nonfree
  compiler slots and predicts neither unit nor properness.  A provisional
  transformed F-side resonance at raw label 20 and a seven-dimensional
  polynomial compiler cokernel refer to different objects; their interface
  must be typed before use.
- **Avenue 2 — unchanged after the source correction.**  Do not revive
  `C74-PLACE` or a cheaper `EXIT-RPMC(C)`.  The proof route remains the
  intrinsic exact-pair two-chart constructor followed by L3 truncation, L5
  deck/leaf-place fidelity, landing, `RPMC(C)`, and a cofinal degree bound.
- **Avenues 3 / 25 — reject the current nearby-cycle and passport bridges.**
  The proposed zero-locus nearby cycles have the wrong support, consecutive
  `mu4` characters split, and no map identifies face-root loops with Keller
  passport loops.  A future higher-rank client must specify its base,
  functor, and off-diagonal connection explicitly.
- **Avenues 7 / 26 — keep the marked finite-end filter, not a ceiling.**  The
  exact finite-end identity and two-coordinate congruence prune one clean
  census `169 -> 48`; all 48 survive, and nothing bounds genus or `td`.
- **Avenue 36 / shared-source instruments — add the frozen bi-face separation
  as provisional routing information.**  Exact same-model replay finds the
  current upper branch-P and LF40 graph fibre product empty already at
  `f_15_52` (independently at `g_23_80`).  Await different-model promotion;
  even then the result separates only those literal normalized fixtures, not
  arbitrary upper/lower families with extra coordinate freedom.

The primary proof route and primary finite falsifiers are unchanged.  Exact
upper, LF40, and full-`P6` K00 lanes continue on AWS; the new algebraic gate
recurrence is the highest-value desk theorem.  Model roster policy remains in
`COORDINATION.md`, not here.

## Superseding strategy overlay (2026-08-27 22:08Z — reviewed post-seal corrections)

This is the current live avenue overlay; the `21:30Z` section below is retained
as historical provenance.

- **Avenue 2 — lower the VGG-corridor subroute; preserve the backbone.**
  Corollary 7.4 is native same-edge information, not the missing physical
  chart, and minimal VGG re-selection cannot swap the chart.  Retain its
  12/8 common-power custody as L1/L2.  Refile `H-TRUNC`, all-place coverage,
  and deck/leaf fidelity as L3--L5 under the intrinsic exact-pair two-chart
  constructor.  `EXIT-RPMC(C)` does not reduce the cost of `RPMC(C)`.
- **Avenue 7 — modest instrumented raise.**  Bank Chau's exact finite-end
  identity and the marked asymptotic-component divisibility filter.  It
  prunes one passport census `169 -> 48` and nothing more; construction of
  the normalized components and their braid monodromy remains open.
- **Avenue 16 / 3 — raise the exact rank-one target budget, not tower
  independence.**  The etale `mu4` torsor gives an all-row index theorem and
  a four-row target-space budget.  It neither finitely decides the infinite
  tower nor makes the nonlinear rows independent.  The correct full-`F`
  not-before bounds are P58/Q52; no collapse or endpoint result follows.
- **Avenue 25 — add only the reviewed marked-profile congruence.**  Ordinary
  passports forget `e=1` boundary fixed points.  No face-root-to-Keller-sheet
  loop map or primitive-sheet block is promoted.
- **Avenue 26 — unchanged at the cofinal ceiling target.**  Rewriting
  `td=sum e_S-b1+1` is exact but supplies no upper bound on its right-hand
  side.  A local/global mechanism must beat this circular scale before the
  avenue is raised further.

Current primary proof route: exact-pair intrinsic two-chart construction ->
reviewed Sigray decoration and all-root landing -> L3--L5 fidelity ->
`RPMC(C)`/type control -> cofinal degree ceiling.  Current primary finite
falsifiers remain K00 full-`P6` rank-exact incidence, LF40 R1, and the exact
upper endpoint lanes at their stated scopes.

## Superseded strategy overlay (2026-08-27 21:30Z — chart discriminator, executable repair, and source closure)

The transpose discriminator is now decisive.  Different-model Fable5 review
confirms that signed conjugation transports all fourteen GGV5 complete-chain
clauses and VGG 5.17--5.18/(5.9), but only to a conjugated source pair.  No
finite-support pair is simultaneously an x- and y-native `(m,n)`-pair, and
VGG's Laurent reflection sends one infinity divisor to `x=0` rather than to
the other infinity chart of the same fixed polynomial pair.  Stop spending on
a wholesale anti-standard copy of Theorem 2.20 as a fixed-pair coverage
device.

Raise Avenue 2's pure-Sigray branch and narrow its first new theorem.  The
whole exact normalized pair already determines the intrinsic two-chart
Newton--Puiseux forest, pole tags and paths, so that architecture bypasses
`G2-PSC` deliberately.  It still owes the repaired Sigray decoration trust
perimeter, complete landing, bounded delay when invoked, and degree control.
VGG Proposition 7.3/Corollary 7.4 give provisional same-pair opposite-side
common-power corridors—at live `8_28`, `ell(P)=alpha*R^12` and
`ell(Q)=beta*R^8`.  The next target is the smaller `C74-PLACE` all-root
face-to-place/proximity compatibility, then `EXIT-RPMC(C)`: prove that degree
mismatch is supported and quantitatively bounded at exits from proportional
corridors.  Opus5 hostile review is live; neither statement is promoted.

Narrow Avenue 16 at the source-pinned boundary.  R1 capacity subadditivity
holds only for an actual short exact sequence or filtration by the named row
connections; it does not close an arbitrary or irreducible rank-two client.
R2 does close the polynomial algebraic clients: the quarter-root functions
generate regular-holonomic modules over the complex `(X,s)` surface, with
vanishing irregularity complexes at finite divisors and infinity, and
algebraic six operations cannot turn those regular inputs irregular.  Stop
the naive filtered-bundling branch and any attempt to obtain irregularity by
merely renaming an algebraic direct image.  Preserve the rank-one de Rham
index law, genuinely coupled higher-rank clients, and Avenue 25's independent
branch-cycle CSP.  A future irregular successor must exhibit a Keller-forced
irregular input or an operation outside the regularity-preserving closure;
a global rational exponential twist is sufficient but not necessary, since
formal factors can acquire fractional phases after ramification or
modification.  The primary-source review is complete in
`xmodel/ggv-r7r2-r1-r2-source-custody-audit-sol-ultra-20260827.md`
(`58273d10...`).

A compiler-custody correction changes execution, not the lower mathematical
avenue.  The promoted LF40 specification correctly says `Dtil_17=-1`, but R0
dropped its `+1` only when serializing JSON to Singular.  R0 was stopped before
row 17 and yielded no conclusion.  R1 changes exactly that generator, adds a
fail-closed target assertion, and runs under a fresh freeze on AWS.
Different-model Fable5 review `e49ef51e...` independently rebuilds all 740
generators and passes seven target mutations.  Its source-directory
immutability observation was repaired live and the 48-entry freeze replayed.
Keep the lower family endpoint allocated; any terminal ideal verdict remains
subject to independent output review.

Computational allocation remains concentrated on exact survivor objects:
five full exact-Q branch-P lanes are live on Box03, the corrected LF40 lane is
live on r6b, and the reviewed full-`P6` rank-exact K00 successor is live on
r6a.  Two positive-tail upper specializations hit a literal row-22 unit and
only kill those witness shortcuts.  Legacy K00 R5 capped with no verdict and
is not restarted.  Heavy algebra remains AWS-only.

## Superseding strategy overlay (2026-08-27 19:37Z — four-model full round)

The significant-news `20260827T1808Z` round used one sealed 46-avenue packet
and four clean-blind submissions from Sol, Fable5, Opus5, and Grok46, followed
by crossed hostile review.  Fable5 and Opus5 remain equal-standing
whole-portfolio co-researchers.  Fable5 was distinctively strong at theorem-
interface composition, lifecycle correction, scope custody, and K00 symbolic
structure.  Opus5 adds significant unique capability: its rank-one de Rham
index mechanism survived Fable5 reconstruction, including actual row-25/26
derivations, while its broader capacity and geometric readings were narrowed.
Model identity remains neither a vote nor an evidence tier.  The round
synthesis is `xmodel/ideation-20260827T1808Z-synthesis-sol.md`.

The artificial D5G35 target on `H=X^8-1` is no longer open.  It is
`EMPTY-BY-COMPOSITION (R3; R4)`: Opus5 independently finds that the first 626
generators through `D22` already give the unit ideal.  This is a lifecycle
propagation correction, not wrong determinant mathematics.  Retain D5G35 as
the literal compiler, cancel every plan to decide that artificial target, and
re-anchor upper endpoint work to genuine R5/R6 survivor shapes.  The fixed
branch-P endpoint `A=X^4-1`, `V=1`, rows 1--22 is now running on AWS; it is
not the dead fixture.

Opus5's reviewed gate law identifies every R7R1-licensed F-side row `m` with
the class in the rank-one connection

```text
nabla_m=d+(m/4)dH/H,
dim H^1_dR(A^1-Z(H),nabla_m)=r-1+k_m,
k_m=1 iff 4|e_i*m for every root multiplicity e_i.
```

This unifies the R5 rational-mode schedule (`H^0`) with the q obstruction
(`H^1`).  Rows 25/26 have reviewed operators and codimensions.  The frozen
rows-23--34 capacity numbers are repaired to at least `79/70/109/34` free
dimensions on P/Q/`r=1`/the `r=8` control.  Do not overread this: the exact
target licenses an infinite tower, whose eventual capacity is unbounded by
the present theorem.  Avenue 16 therefore reopens around the concrete
de Rham index and a rank-two/irregularity discriminator; the latter is still
provisional.  Avenue 33 reopens only as twisted face-character bookkeeping
absorbed into avenue 1.  Untwisted global exactness stays closed.

The upper cascade through `D6` is promoted only for field-valued points/the
underlying reduced support; its literal full-scheme wording is false at the
cone point.  On that reduced cascade `q2=Z/16`, while `q1` has explicit
five- and four-parameter `F1` handles on squarefree P/Q.  These do not
parametrize the full affine endpoint.  A fixed-`H`, `lambda`-saturated
contracting germ is now an exact finite-scheme client, but it is not global
algebraization.  The corrected endpoint transfer is `D22=-L22(g22)`; its
lower-face analogue was scope-invalid because `G17` has eight free slots.

The lower FACEPIN is promoted conditional on the primary GGV reduction.  It
pins `K=xi(xi-rho)^7`, uses the polygon rather than the source's silently
swapped P/Q letters, and gives a literal 442-slot, 740-generator compiler in
408 variables through row 40.  Its exact-Q AWS lane is live.  Even a unit
result would remove only the conditional `(72,108)` GGV case and raise that
conditional lower bound to 125; it would not resolve JC2.

K00 remains the strongest finite falsification lane, but the common request
for “a rational point on the threefold” was stale: the origin is already a
trivial rational point.  The target is now a **nonzero/projective,
source-open, rank-exact, full-`P6`-compatible** point and then a compatible
prolongation/receiver.  Exact R1/R2 prepasses show rank at most
three and then rank at most two without changing the dimension-three scheme;
Fable5 review `738f4603...` now confirms both containments.  It also confirms
the strict dimension-two `I2` cut, hence distinct radicals and a nonempty
rank-exactly-two stratum over `Qbar`.  The producer's `237/351` outside-minor
census is repaired to `291/351`; a `size()` loop skipped stored slots, so all
successor guards must use `ncols()`.  No explicit or rational point follows.
The reviewed constant K00 covectors are big-ring compatibility instruments
and must not be inserted into the six-variable BASE4 ring.

Allocation after the round:

1. raise and redesign avenue 1 around the genuine upper endpoint and full
   family-pinned lower fixture; stop the dead artificial gate;
2. raise avenue 2 and the degree-ceiling part of avenue 26: universal landing,
   all-root/two-chart `G2-PSC`, and cofinality remain the largest proof gap;
3. raise avenue 3 toward a coupled/depth-three face operator rather than
   adding isolated rank-one rows;
4. narrowly raise avenue 4 at the fixed-`H` saturated germ and avenue 19 at
   the nonzero/source-compatible K00 rank filtration;
5. raise avenue 36 only as theorem-compressed search on those named residuals;
   ambient random sampling remains stopped.

Opus5 hostile review `f7de3ae1...` confirms the typed `G2-PSC` separation
with repairs.  The cumulative source map comes from VGG Propositions
5.17--5.18 rather than GGV5 Theorem 2.20; the local occurrence-to-flag arrow
also needs a common `P/Q` face-root witness and an explicit `H-TRUNC`
certificate.  In the fixed native x-fraction category, one chain cannot
express the other infinity chart.  A second, conjugated y-fraction source run
is the already-filed `TRANSPORT.md` Conjecture A, not a new avenue; a pure
Sigray route is the alternative.  At the live `8_28` corner all four face
roots are one deck orbit and already VGG-admissible, so its remaining
coverage gap is transposed-chart expressibility, pole-path reachability and
gluing, plus corrected Q/jump/max decorations.  General all-root
admissibility remains open.  The target must retain later-book labels and
approximate-root degrees along every pole ancestor path, and inherits the
then-filed Sigray Proposition 4.2 tower gap (subsequently repaired by
`10bc55d5...` / `47f2b608...`).  This promotes the corrected typed
interface and obstruction, not `G2-PSC` itself.

## Superseding strategy overlay (2026-08-27 16:48Z — cross-model promotions and blind model round)

Opus5's different-model promotion cross-audit confirms D5G, R6, and K00
V24R6R1 (`d9e65315...`).  D5G is promoted only as the literal determinant
compiler and global-`H`-multiple custody theorem on the named artificial
fixture `R0_ARTIFICIAL_CUSP_CONTROL`; it does not establish a genuine
`8_28` face, a lower-row solution, or the endpoint target.  R6 is promoted
as complete parameter compression of the rational endpoint survivor locus:
the full monic degree-`h` locus has codimension exactly `floor(h/2)`, and at
degree eight only the perfect-square and quadratic-squarefree-part strata
survive, each of dimension four.  K00 V24R6R1 is promoted as exact emptiness
of the selected leading-base/F10 `D(W)` chart and its normalized
`D(k10_0 W)` embedding.  The reason is that `W` already lies in the leading
base ideal; this invalidates the selected chart, not the chart-free K00
scheme.  Other minors, rank drops, later grades, arcs, receivers, and JC2
remain open.

Fable5's different-model review confirms the corrected R7R1
quarter-root characteristic theorem (`7ab758fa...`).  An exact target gives
the full all-order exact-differential tower on `p^4=H`; a finite target
through weight `N-1` licenses exactly the rows `q_n` with `n+22<N`.
The frozen D3 polygons have determinant ceiling 34, not 35: the apparent
`D35` row cancels identically.  Grok hostile review `ba7162fe...` confirms
D5G35 (`f0fe0f5...`, freeze `1668795f...`) through that ceiling and its
734-generator exact target gate
`D0=...=D21=0`, `D22=1`, `D23=...=D34=0`; at 16:48Z the compiler was
promoted while the target was still labelled unresolved.  That historical
lifecycle statement is superseded by the 19:37Z `EMPTY-BY-COMPOSITION`
correction above.

The corrected audit of Grok's raw-support proposal (`43a949b2...`; review
`5735ee90...`) retains
its exact windows, high-weight maps, mode schedules, and endpoint facts but
withdraws the proposed weight-15-first search.  On the producer's generic
`Q` (`delta=1`) and generic `P` (`delta=2`) strata, compatibility already
appears at weight two:

```text
D1=0  =>  G1=(3/2)H F1,
D2=0  =>  H | F1^2  <=>  AB | F1       for H=A^2B.
```

Thus the claimed universal absence of gates through weight 14 and the
unconditioned sampler are already false on those two strata.  Any raw-jet
search consuming the survivor or de Rham filters must
intersect the sequential prefix scheme beginning at weight one once that
scheme is certified; the old unconditioned
127-parameter sampler and its trials are not campaign evidence.  The
perfect-square branch must separately stratify multiplicity gcd
`delta=2,4,8`: weight-one homogeneous modes occur for `delta=4,8`, so the
displayed implication is not their complete prefix, and for `4|delta` the
first nonpolynomial scheduled mode is weight 13.  Shared roots
give only `gcd(A,B)|v`, not `B|A`; scaling preserves raw support, whereas
translation can leave the frozen windows.

The matched blind `20260827T1606Z` Fable5/Opus5 evaluation used one sealed
packet (`1306771f...`) and is synthesized at `24edb9cf...`.  Both remain
equal-standing co-researchers.  Fable's best
nonduplicate atom is an explicit `q1/q2` gate on the existing upper face;
Opus5 review `46736edc...` confirms its squarefree perfect-square-branch
theorem: `q1` is exact exactly when
`F1` lies in the image of `Q -> 2AQ'-3A'Q`, an injective 13-dimensional
image in the 16-dimensional artificial D3 window.  The gate is licensed
only once `D23=0` is imposed.  The fixed `q2` fill
`A=X^4-1,F1=0,F2=1` has residue `-3/64`, but this is one negative control,
not a branch-level `F1` cut.  Opus's best atom is the previously unused
`(4,-1)` grading of the known lower face.  Grok review `72f10ad7...`
confirms the new chart identity
`Etil=-tau^17 J`, the recurrence, the D3 polygon's `141+301=442` census,
`F17=0`, `G17=Q[xi]_(<=7)`, the live `G18,...,G24` tail, and the unmixed
ODE `4Kg'-3K'g=4K`.  It also confirms only the necessity of
`gamma == 3 (mod 4)` for the two-root ODE shape.  The face itself and its
`B=x(z-1)^7` leading form were already known.

The broader `NU17` card is **not promoted**.  The native control is
`f=B^2-x-y^8`, whose upper face is `X^16-1`, not the monomial obtained from
`B^2` alone.  Lower pinning holds for that explicit control but not for the
entire D3 raw lattice or GGV family.  Row 17 is not a complete fixture:
28 raw `G` slots remain at weights 18 through 24 and determinant rows can
continue through 40.  The advertised general face-endpoint iff, sufficiency
of the multiplicity classification, drop-in D5G regrade, farm-wide filter,
and every family exclusion are withdrawn.

Allocation consequence: keep the total GGV share fixed.  Within it,
prioritize (1) a three-object FACEPIN read separating the native control,
D3 artificial completion, and GGV family, then a sequential lower-face
compiler through all rows `0..40`, (2) the upper-face lower cascade at
weights three through six, carrying the nonunique even-weight kernels, and
(3) a frozen `q1` certificate/intersection only after the relevant prefix
strata are certified.  The advertised F-gates/unique-G/endpoint decoupling
is refuted because lower rows still govern 367 raw slots.  Grok46 review
`56a4b940...` now independently confirms Opus's sharpenings, with the
load-bearing repair that branch Q requires both `A` and `B` squarefree as
well as coprime.  On that locus the branch-Q `q1` image has codimension four;
the branch-P row-three gate is exactly `A^2|F1`; the three relevant linear
intersection dimensions are `9,5,7`; and `q2` has a surjective `F2` handle,
so it cuts no `F1` after `F2` is allowed to vary.  These are promoted only at
their frozen squarefree-prefix/de Rham scopes.  They do not solve the
nonlinear accumulated prefix or the affine endpoint.  Continue the
chart-free K00 atlas independently.  Model identity is not a vote: Fable is
strongest here at exact local theorem extraction and scope custody; Opus
supplies materially nonduplicate integrative geometry and route reframing.

The first exact chart-free K00 prepass V26R1F is now promoted at its
intrinsic six-variable scope (`86742147...`; Fable5 review `6790ec5c...`).
All `6 x 6` minors of the frozen `7 x 7` grade-seven matrix vanish, while
the full ideal `B+I5(A)` is proper of exact dimension three; hence a
three-dimensional rank-`<=4` leading-base scheme survives over an
algebraic closure of `Q`.  The upstream V24R6R1 dependency was already
Opus5-confirmed, so the rollback label is released.  This is a chart-free
survival result, not a rational point, rank-exactly-five chart, full `P6`
source, complete atlas, later-grade lift, jet/arc, receiver, order-two
maximum-12 instance, or JC2 result.  K00 should now intersect this intrinsic
component with the omitted prior rows and the next grade before spending on
another witness minor.  The unrun V27 design
`xmodel/max12-812-order2-k00-v27-rank-compressed-atlas-successor-design-sol-20260827.md`
first tests whether the surviving coefficient-base component is rank-pure,
then exploits the reviewed collapse of all 54 maximal minors to two scalar
classes.  If its exact prepasses pass, this replaces the literal 1,640-chart
atlas by a rank filtration plus at most one nonredundant rank-five chart;
proper superlocus screens remain no-verdict until rank purity is certified.

## Superseding strategy overlay (2026-08-27 15:55Z — multiplicity endpoint promoted; K00 review pending)

Opus5 independently confirms the full R5 mathematics (`fd164042...`, freeze
`3d8ba267...`, review `48fff5d5...`).  For
`H=h0*product(p_i^e_i)`, the weight-`n` rational homogeneous mode exists
exactly when `4 | e_i(12-n)` for every `i`, and the displayed mode lifts to
the full nonlinear homogeneous equation.  The inhomogeneous endpoint is
solvable exactly when, for `H=A^2B` with `B` squarefree,
`A=Bv'+(3/2)B'v`.  In degree eight, only `deg(B)=0,2` survive; in centered
quadratic coordinates over `K` itself, the latter has the sole condition
`4a0+D*a2=0`, equivalently the appropriate residue at infinity vanishes.
Promote this necessary rational-endpoint filter only.  The frozen regression
harness is materially weak, but Opus5 independently reproved and stress-
tested every theorem item; an additive strong harness remains software work.

R5's confirmation releases R6's mathematical dependency (`ed0e3460...`,
freeze `9c23f3f5...`, internal audit `44d2d8a0...`).  Its endpoint survivors are parametrized by
`H=(Bv'+(3/2)B'v)^2B`; fixed-`b` dimensions are understood for the
constructible image/Zariski closure, and the full monic degree-`h` survivor
locus has codimension exactly `floor(h/2)`.  For degree eight the perfect-
square and quadratic-squarefree-part strata both have dimension four.  This
is parameter compression, not existence of a formal jet or raw landing.  The
audit used the producer model family, so R6 remains provisional while the
Opus5 promotion cross-audit is live.

The K00 V24R6R1 exact certificate has also passed a strong internal audit
(`8da4f616...`, result `b0a53786...`, additive syzygy `8cd2afdd...`, review
`f9d2afcb...`).  Over `Q`, the provisional chosen rank-five witness satisfies the global
identity `W=A1*Q1+A3*Q3+A4*Q4`.  Therefore the selected leading-base/F10
scheme has empty `D(W)`, and its full normalized V24 source has empty
`D(k10_0*W)`.  Every remaining prior-prefix point lies on `W=0`; since `W`
already lies in the base ideal, adjoining `W=0` is redundant.  The live
successor is the complete chart-free Fitting/rank atlas, beginning with the
representation-independent `B+I5(A)` rank-`<=4` gate and retaining every
other minor chart.  No statement about that atlas, later grades, arcs,
receivers, order two, maximum twelve, or JC2 follows yet.  Because producer
and auditor share a model family, promotion awaits the live Opus5 cross-audit;
the Fitting descendant is explicitly rollback-tagged.

## Superseding strategy overlay (2026-08-27 15:43Z — direct raw-global internal pass; promotion pending)

D5G now supplies a producer-checked direct raw-global determinant compiler that D3
could not obtain through local Morse data (`59e9bf2c...`, freeze
`838b1160...`, independent internal audit `14913814...`).  Starting only from the
literal D3 `2S/3S` support, the fixed rows `F0=(X^8-1)^2`,
`G0=(X^8-1)^3`, and 400 named positive-weight raw slots, it independently
serializes all `D0,...,D22`, 32,135 final terms, and 58,572 source
contributions.  At the endpoint it freezes both
`D22=H*Q22+R22`, `deg(R22)<8`, and
`D22=M(Y22)+rM22`, `deg(rM22)<7`.  The illustrative custody mutation
`D22 -> D22+H` leaves `R22` fixed but gives exact changes
`Delta Q22=1`, `Delta Y22=X/52`, `Delta rM22=-12/13`.

The internal audit supports raw-coordinate determinant and global-
`H`-multiple custody only, but shares the producer model family; D5G remains
provisional pending the live Opus5 promotion cross-audit.  It does not impose
the Keller rows `D1=...=D21=0`, the target
conditions `R22=1,Q22=0`, or any source specialization.  D5G itself pins
D4R1 only as a lifecycle hash and does not consume its local result.  No
face/family exclusion follows.

D4R1 is now independently Fable5-confirmed (`8b3dd3c...`, freeze
`6beafdb4...`, review `971816d0...`).  The factor-local formal compiler over
`Q[c]/(c^8-1)` has 400 free raw leaves and an exact 49,964-node cleanup DAG;
the legal five-row specialization evaluates to
`U14=c`, `U16=-1/4`, `U20=-c^2/8`, `U22=c/16`.  A live review mutation shows
that dropping `f_2_2` still leaves `U14=c`, so five-row minimality is not
promoted: four legal rows already defeat an argument based only on the absent
raw `F14:X` slot.  Descendants may now consume the reviewed factor-local
compiler, but must separately prove the local/global naturality square, gate
all lower determinant rows, and retain D5G's global `H`-multiple.

R6's complete survivor parametrization passed an independent audit
(`44d2d8a0...`); R5's later confirmation releases it at the scope recorded
above.

R7's core characteristic identity passed audit, but its first freeze is
superseded by additive R7R1 (`9d35c678...`, freeze `489647cf...`; first audit
`2dd9d8f1...`).  With `P=F^(1/8)`, `s=t/P`, `W=G/P^12`, and `Q=P^2`, an
*exact* equation `E=t^22` gives
`w_(n+22)'=-(n+2)q_n/16`; the full homogeneous kernel is
`C_L[[s]]`, not only the rows below 22.  The four-character language requires
a `mu4` base change, and bounded raw support does not truncate the tower or
give a finite decision bound.  A target only modulo `t^23` licenses `q0`
alone; `q2=F2/(4H)-F1^2/(16H^3)` requires rows through weight 24.  Current
D5G stops at `D22`, although the complete D3 slot support can feed a direct
extension through `D35`.  R7R1 is under different-model Fable5 review and
licenses no campaign conclusion yet.

## Superseding strategy overlay (2026-08-27 15:18Z — equal-model round and multiplicity endpoint)

The blind whole-portfolio round `20260827T1349Z` is synthesized in
`xmodel/ideation-20260827T1349Z-synthesis-sol.md`.  Sol, Fable5, Grok, and
the three specialist researchers were clean-blind.  Opus5 disclosed one
repository-search line mentioning the integrating factor `H^(-1/2)` and is
therefore `BLINDNESS-DEGRADED-BY-ONE-LINE`; that observation receives no
novelty credit.  Both Anthropic models nevertheless completed the same
46-avenue contract.  Fable5 remains an equal co-researcher, and Opus5 is
retained as an equal co-researcher because its separately derived closed
superelliptic endpoint criterion survived two independent hostile audits and
is materially nonduplicate.  Model identity is not a vote, and both remain
subject to ordinary history, scope, and different-model review.

For `H=A^2 B` over a characteristic-zero field, with `B` squarefree, the
reviewed endpoint theorem is

`exists g in K(X): 2H g' + H'g = 2H`

if and only if `A = Bv' + (3/2)B'v` for some `v in K[X]`.  A reduced
denominator divides `A` but need not equal it; injectivity/codimension of the
displayed polynomial operator requires `deg(B)>=1`, while constant `B` is a
separate surjective case with kernel.  The bridge from `M(Y)=1` is `g=4HY`.
For `H=X^8-1`, algebraic de Rham reduction on `w^2=H` gives
`[w dX]=-(4/5)[dX/w]!=0`.  Promote this only as a rational endpoint oracle
(`d382c21f...`, independent Grok review `066095c4...`).  General-`H` mode
completeness for the preceding GGV system, raw provenance, landing,
`G2-PSC`, `G2-BD`, cofinality, and every family-level exclusion remain open.

For the same `H=X^8-1`, the reviewed polynomial operator
`M(Y)=4HY'+6H'Y` is injective and
`Q[X] = im(M) direct-sum Q[X]_(degree<=6)`.  Its canonical reductions are
`[X^7]=0` and `[X^n]=((n-7)/(n+5))[X^(n-8)]` for `n>=8`.  The crucial repair
is `r(HX^i)=-(12/(i+13))X^i` for `0<=i<=6`: an unknown global `H`-multiple
spans the entire seven-dimensional complement.  Thus this is a compact final
test only after a raw compiler emits one literal global `E22`; rootwise data
alone do not define an intrinsic seven-vector (`2803b705...`).

Fable5's independently audited R4 table is a complementary exact-Q pruning
rule in the pinned normalized R2 chart.  If branch two has
`U11(c)^2=lambda*c/36`, it can occur on `Phi1` only in square class `1`, on
`Phi2` only in square class `-1`, on `Phi4` only in square class `+/-2`, and
never on `Phi8`; for `lambda=1`, only `X-1` remains.  This arithmetic
obstruction vanishes over `Qbar` and `C`, so it is search metadata rather than
a complex-family exclusion (`39a28831...`).

K00 V24 is now independently confirmed only as a conditional grade-seven
theorem (`d547baa2...`; adjudication `0bd97113...`).  On the normalized
valuation-one source with `k6_0=0`, over the prior ring localized at `W`, the
literal newest-variable matrix has rank five and free rank-two left kernel;
its inhomogeneous system is soluble exactly when `Comp1=Comp2=0`.  This says
nothing about nonemptiness of the localized prior chart, `W=0`, later grades,
an arc, or a receiver.  The old modular-zero field is withdrawn.  The exact-Q
properness/nonemptiness computation remains the sole gate; the chart-free
Fitting atlas stays preregistered and follows only after that binary result.

V43C6 now supplies the reviewed first typed-transport stop (`cee2a228...`,
Fable5 review `ad855507...`).  V20R2 serializes no C5 ring or row map.  On
the frozen V47 `A1D2_R3` endpoint, the honest contact map sends
stage-zero `a1=Az_0` to zero in a nonzero domain localized at
`(-2*rho^2)k10_0`; hence it cannot extend from `S[a1^-1]`, and the pushed C5
identity is only `0=0`.  Promote this unavailable/zero transport for the
named endpoint class only.  It neither refutes C5 nor excludes another typed
receiver, and it gives no Gate T, order-two, maximum-twelve, or JC2 result.

The raw-global GGV successor is now precisely typed: literal raw `2S/3S`
jets must compile, with factor/deck/orientation custody, to one global
polynomial `E22`, after which the seven-coordinate remainder and the
superelliptic oracle are cheap.  D3 correctly stops because no serialized
raw-to-Morse cleanup DAG exists (`c7ea900b...`).  A proposed discriminator has
the exact polynomial identity
`u=H+t^6 X-(1/2)t^8`, `R=t^14 X-(1/4)t^16`,
`u^2+R=H^2+2t^6HX-t^8H+t^12X^2`, but D4's first freeze incorrectly labels
the X-dependent remainder `R` as the Morse critical-value series `U(t)`.
Quarantine it (`bf81b905...`, freeze `8a79c549...`) and stop its obsolete
review.  The additive D4R1 repair now evaluates the real 49,964-node cleanup
DAG under the specialization and provisionally returns `U14=c`, together
with `U16=-1/4`, `U20=-c^2/8`, and `U22=c/16`, in
`Q[c]/(c^8-1)` (`8b3dd3c...`, freeze `6beafdb4...`).  Its fresh Fable5 review
has since passed (`971816d0...`) at the narrow scope recorded above.  Even a reviewed PASS emits local
data, not a global `E22`.  Do not replace that
dependency with local interpolation or a guessed `H`-multiple.  Reviews run
asynchronously; safe descendants name provisional premises and rollback
subtrees.  Heavy algebra remains AWS-only.

R4 is now different-model confirmed (`11cad1db...`, Grok review
`5187676b...`).  For every characteristic-zero field and every nonconstant
squarefree `H`, the complete rational homogeneous modes of the artificial
`F0=H^2,G0=H^3` edge below the target remain the six R3 modes.  The endpoint
reduces to `4HY'+6H'Y=1`, so no polynomial-`X` formal jet with
`E=t^22+O(t^23)` exists when `deg(H)>=2`; degree one is only endpoint-silent.
The exact repeated-root stop is `H=X^2`, where
`t^22 F^(-5/4)=t^22 X^-5` is a new homogeneous mode.  Promote the
general-squarefree formal-edge theorem and that stop only.  Raw `2S/3S`
landing, a genuine GGV family, and all global obligations remain open.

R5 provisionally continues without waiting on further review by classifying
arbitrary factor multiplicities (`fd164042...`, freeze `3d8ba267...`).  If
`H=product p_i^e_i`, the weight-`n` rational mode exists exactly when
`4 | e_i(12-n)` for every factor; exact nonlinear lifting avoids any scalar
root.  Combining this with the audited endpoint criterion for `H=A^2B`
reduces degree-eight edges to squarefree-part degrees `b=0` or `b=2`; the
degree filter excludes `b=4,6,8`, and normalized quadratic `b=2` has the sole
endpoint equation `4a0+D*a2=0`.  Internal audit passes; Opus5 hostile review
is live.  Treat every R5 conclusion as provisional and necessary-only.

## Superseding strategy overlay (2026-08-27 14:03Z — pure total certificate and narrow R3 exclusion promoted)

V43C5 V2 is now different-model confirmed.  In the ordinary polynomial ring
`Q[t,X19_total]`, `t=rho^2`, the frozen literal total raw ordered-`a1` rows
through grade 19 satisfy the exact 25-row identity
`a1^628=sum_i M_i(t,X)R_i(t,X)` (`f8426bcf...`; result `215a64b...`;
Grok review `0ab2a7ef...`).  The converter is division-free in the relevant
sense: it uses no localization and never divides by `t`.  Under
`t -> rho^2` it is the requested pure total certificate with `N=628` and
`W=0`, so the frozen raw `D(a1)` chart is empty.  Promote only this ordinary-
ring membership and its raw-chart saturation consequence.  No source map,
terminal-receiver reachability, normalized K00 identification, Gate T,
order-two, maximum-twelve, or JC2 theorem follows.  The next high-value use is
a fail-closed typed transport of the 25 supported rows into an honest source
or receiver ideal; do not infer that map from shared variable names.

The additive `8_28` R3 rational-mode theorem is also different-model
confirmed (`b1851156...`; Grok review `27fcd256...`).  Over every
characteristic-zero field it excludes polynomial-`X` formal jets with the
*artificial squarefree replacement edge*
`F_0=(X^8-1)^2`, `G_0=(X^8-1)^3` and determinant target
`E=t^22+O(t^23)`.  Its complete six-mode calculation in `K(X)[[t]]` and final
pole/degree obstruction apply only to that frozen replacement edge.  They do
not exclude the original non-Keller `8_28` control, the original `8_28`
Keller family, multiple-root edges, any GGV family, `G2-PSC`, `G2-BD`, or
JC2.  Retain R3 as a sharp fidelity control while raw provenance and the
global `H`-multiple remain the live GGV wall.

## Superseding strategy overlay (2026-08-27 11:56Z — exact generic identity and typed-receiver reset)

The ordered-`a1` generic unit is now a reviewed literal total identity:
`5*t^6*a1^4=sum_i H_i*Tg_i` in `Q[t,X19_total]`, `t=rho^2`, using exactly
eleven grade-11--15 rows (`ae7beed3...`, Grok review `8a34756e...`).  Promote
that identity and its generic-fibre consequence only.  Since its coefficient
vanishes at `t=0`, the chart is not yet closed.  Continue the expanded
special-fibre lift, the exact shared-DAG/circuit replay, and the exhaustive
eleven-row circuit/pole minimizer independently on AWS.  The uncorrected
V43C1/C2 candidate derivation remains exactly invalidated and
different-model confirmed (`59d1e39c...`, review `008e0d52...`): its right
branch crossed a nonzero old-`e1` multiplier.  The distinct repaired C4
successor rebases `e1=G+4*a1*ell1` and `ee0=Q0-4*aa0*ell1` first, and is now
independently confirmed to give `a1^104 in J0` (`3e0e80c0...`, review
`6a8ab201...`).  This restores `M=104` only as the reviewed special-fibre
input.  Run C5's explicit same-ring lift and total converter, then hostile
review, before promoting exponent 628 or ordered-chart closure.

Do not inject the `t^6` leading term into K00.  On the deck-explicit K00
relation, `C6=1` gives `t=-1/4`, a unit, and no row-level map currently sends
the eleven total rows to the six-row K00/receiver complex.  V19 correctly
returns `NOT_TYPED_COMPOSABLE` for all three direction-specific V18 duals.
V21R1 now independently proves all three separate local targets nonzero,
with exact dual pairings and Opus5 hostile review (`b514ef72...`, review
`aa1a57cd...`).  This consumes V18R2's first-incompatible-cutoff results
without waiting for monolithic V17-Q, but it does not make them one common
typed covector.  V20R2 now supplies the reviewed common mixed
`Lambda<=19` source/target compiler (`9e304f58...`, Grok review
`c37cdc4f...`): it contracts to the scalar universal quotient before the
honest weighted parameter map, avoiding the tautological coordinate
projection in the old vector design.  This is source and compiler authority,
not an endpoint.  Run the constructible rank/Fitting stratification
asynchronously, requiring a replayed lift or rational dual at every branch.
V22's first branch excludes only the open `D(F10)` locus and
leaves a proper homogeneous dimension-three closed stratum.  This statement
is now different-model confirmed (`494075c5...`, review `f834cb99...`), so
V23 continues on the closed remainder without a review gate.  Its exact
grade-seven newest-variable block has provisional generic rank five and two
left-cokernel equations; only their contracted, representation-independent
remainders may split the stratum.  A separate cheap bridge may still map
`{t,a1}` and the eleven supported total rows into one frozen K00 endpoint;
absent row provenance fails closed.  If that map exists, it gives ordinary
membership `(5/4096)*phi(a1)^4`, not a `t`-adic normal-cone class.

The AS109 residual `n=6` lane has one new reviewed branch-local theorem
(`c79dfbf...`, Grok `d2b55dce...`): if
`q_6=beta*h^(6/d)` has `v_109(beta)=1`, then
`(bar h)^(6/d)` lies in the derivative image of `F_109[x]`.  This excludes
the corresponding Cartier coefficients and the leading residue classes
`Hbar=108` (`d=6`) and `Hbar=54` (`d=3`) modulo 109.  Intersect only the
three alpha/beta content strata.  Stop top-two-band, W2/W3, and blind support
duplicates; no target-floor-seven or lift-nonexistence claim follows.

The 13:48Z broad web sweep found one exact-conclusion external claim that
requires urgent primary-text audit: Matysiak, SSRN 7229458, advertises a
complete JC2 proof via the endomorphic square-free condition `5''s`.  The
paper and its 34-page dependency are currently blocked by SSRN's Cloudflare
challenge and no lawful mirror was located (`a8a1ae4f...`).  Classify it
`ACTIONABLE-UNAUDITED`, not as a theorem or a solved-problem update.  Preserve
the internal allocation while attempting lawful acquisition; if obtained,
audit the definition/dependency and the first dimension-two step before any
reranking.

V47R1 is now different-model confirmed (`43547b06...`, freeze `bca113ab...`,
Grok review `ff76e8d...`).  Its executable literal 569-tail contraction
replaces the moving-root allocation step for exactly the twelve baseline and
four raised-subtail contacts in the frozen manifest, inheriting every
endpoint's localization and theorem type.  It supplies no new endpoint or fan
cover and leaves `(8,3,8)` unpromoted.  That bounded software client is now
complete and different-model reviewed: V48 independently compiles the exact
four-atom/four-summand universe to 425 nonzero and 40 cancelled primitives,
solves 424 unique-`AC` plus 417 global-pole inequalities, and reproduces the
same 12+4 positive rays and all eight firing negatives (`2d1c9545...`, Grok
review `4c64eb6a...`).  Use it only as a semilinear compiler inside the
registered V47 box; V47/V47R1 remain the endpoint authority, and no fan cover
or new endpoint follows.

The `20260827T1104Z` significant-news synthesis is
`xmodel/ideation-20260827T1104Z-synthesis-sol.md` (`df8c920f...`).  It closes
degraded because Fable5/Opus5 were operationally capped and one K00 report
disclosed snippet contamination, while four other submissions remained
clean-blind.  Current allocation is 28% terminal/K00 mixed reachability, 22%
global landing/`G2-PSC`/`G2-BD`/cofinal ceiling, 20% V43 converter and
circuit compression, 15% uncovered contact/ramified/equality faces, 10%
disciplined disproof, and 5% integration/intelligence.  The first bounded
paired GGV-to-Eggers--Wall/MacLane control is now exact and different-model
reviewed (`7647f2f1...`; Grok `971147c5...`; R1 repair review
`8f20bc6b...`).  On one explicit non-Keller realization of the complete
`8_28` edge, a coefficient mutation invisible to the coarse ledger changes a
pole order and total pole mass.  This proves ledger insufficiency for
arbitrary polynomial controls only—not for Keller pairs and not `G2-PSC`.
The live successor imposes the Keller face recurrences and maps each simple
common-root orbit to the existing cusp `(U,V,W)` receiver with exact
deck/source provenance.

That Keller-face successor now has a reviewed additive type firewall
(`66121bdd...`, Grok review `d40cd78f...`).  The raw `2S/3S` source has only
`F_14~X^2`, `G_21~X^3`, and no `G_22`; the local rootwise interpolant
`U_14=X` is therefore not a direct raw polynomial row.  Moreover the two
minimal normalized scalar-carrier ansatzes both give
`E_22=M(Y)=4HY'+6H'Y`, and the degree law rules out a polynomial unit.
Do not promote this to a face exclusion: lower raw jets and higher-`u`
cleanup may still synthesize the local coefficients.  Continue with the
source-pinned raw-to-`(U,V,W)` normal-form compiler; on failure, bank the
first omitted higher-`u` term as a required `G2-PSC` packet field.

R2 now closes one important loophole in that compiler target
(`0f8f3833...`, Grok review `b271a295...`): after root-local formal Morse
normalization, arbitrary `Q`, `Gamma-1`, and `u^4+` sidecars cannot create a
third constant determinant carrier.  Through weight 22 the factorwise
dichotomy is exactly `6V_8U_14` versus `(9/2)U_11^2`, and different etale
factors may choose different branches.  The remaining target is therefore
sharper, not closed: prove raw polynomial provenance for those factorwise
data and control the global `H`-multiple.  Do not identify a formal local
Morse chart with a global polynomial source automorphism.

## Superseding strategy overlay (2026-08-27 10:57Z — uniform contact transport promoted)

Uniform seven-source naturality and the corrected typed shifted-root interface
are now different-model reviewed and narrowly promoted (`b862e00f...`; reviews
`13450b72...`, `a2a6a5e2...`).  For arbitrary `(a,c,r)`, coefficient
extraction commutes with the explicit lower-contact jet map; on `D(rho)`, the
raw-total and mapped-D1 root maps are separately typed, commute, and invert.
The `a=7` `k6` load-tie is included as an actual representative.  This removes
serial `ACT-TOT-G22/G24+` exporters from the mathematical critical path.

Do not turn the infrastructure theorem into an endpoint union.  Compile each
reviewed D1 family through a frozen per-contact manifest with a complete polar,
load, and target inventory, derived maxima, generated aliases/scales, exact
localization and theorem type, and a semantic comparison to the chamber
compiler.  Keep `(8,3,8)` conditional until it has an authority promotion.
The direct three-row unique-`AC` lemma may optimize twelve low-band baselines
and four raised subtails only after its new fail-closed linker passes hostile
review; contacts failing its global-pole/load/target predicate retain their
moving-root endpoints.  Nothing here covers `rho=0`, equality or
positive-load faces, staged Rees/receiver closure, `G2-PSC`, or `G2-BD`.

## Superseding strategy overlay (2026-08-27 10:52Z — K00 local theorem and pure-sector stop)

The unloaded normalized K00 terminal row is locally generated by the first
six rows at `C6=1`: V14R1's reviewed identity is
`(63*d4+20)r7=sum_i u_i*r_i`, and the clearing factor has constant term 20
(`039590c8...`, Grok review `25e6a3f2...`).  It is a unit modulo every
power of the maximal ideal.  Therefore stop all pure-coefficient D9+
Macaulay work permanently: finite compatibility holds to every order, even
though global polynomial membership fails.

The constant terms of local multipliers are also representation-independent.
The full origin image of
`Syz(r1,...,r7)` is the line through
`(-200,0,-960,0,-5120,0,-40960)` (`65fb96a9...`, Grok review
`dc0b2d80...`).  Hence every local representation has zero constants in the
even slots `M2,M4,M6`; the apparent constant-`M6` freedom from `Q6=0` is only
an initial-form artefact.  Do not infer positive-order representation
rigidity or a mixed formal-deformation solution.

The K00 critical path is therefore mixed deformation theory, not another
unloaded global or filtered order.  Compute each load direction in the six-row quotient
`(R/I)/im(Syz(I)->R/I)`; keep the seven-row syzygies only as a sign and
representation-invariance control, since quotienting by seven rows would be
vacuous.  For any locally nonzero class, find its first failed cumulative
normal degree by exact Macaulay ranks and compatible lifts—never by raw
remainder degree—then restore the `Lambda` weights `2,6,10`, couple the
directions with `mu2,mu4,mu6,Jdet` through order 19, and restrict to the
honest source image.  A nonzero local germ may vanish to high order on a
constrained arc, so only that last coupled reachability test can separate the
closure incidence.

## Superseding strategy overlay (2026-08-27 10:38Z — exact N=6 exclusion and two-fibre route)

The full frozen rho-zero raw ideal through grade 19 now has an exact dual
certificate proving `a1^6 notin J0`; the ideal property strengthens this to
`a1^i notin J0` for every `0<=i<=6` (`a3749a7c...`, Opus5 review
`64e49a36...`, exact R1 result `a8537d4b...`).  R1 replaces the old target-
coefficient mutation by V42's genuine `Tg19_7` source-row mutation, recovers
the original 834-entry source manifest, pins the census addendum, and
discloses the failed first link before the successful exact resume.  Keep
the firewall literal: rho is zero, rows stop at grade 19, and V42's
`a1^8` membership is only in its A1 branch ideal.  This result advances the
total-certificate exponent floor beyond six but is not a negative result at
any higher exponent and is not a total-chart decision.

Do not spend next on a blind sequential N=7 solve.  Its frozen preflight is
telemetry only: 206,086 target-component products, 374,323 monomials, and
924 equations obstruct the literal pullback of the N=6 dual, but no
membership conclusion follows.  Hold the 278,446-unknown correction block
unless the generic route reaches its no-verdict cap or later synthesis needs
a specific thickness bound.

The dominant exact route is the reviewed two-fibre test.  Run the complete
59-nonzero-row dehomogenized generic ideal, including all eight rows that
vanish at rho zero and the `ez9` row, over `Q(t)`, `t=rho^2`.  The sole
`ez9` occurrence is `(3/8)t*a1*ez9` in `Tg19_2`; after `a1=1` it is an exact
linear pivot, leaving 58 rows in 64 variables without dropping any source
constraint.  Since `Q(t)->Q(rho)`, `t->rho^2`, is a faithful field
extension, this decides the required generic `Q(rho)` conjunct.  A completed
full nonunit basis is decisive only for the frozen grade-through-19 corpus.
A unit basis must be followed by tracked multipliers, weighted
rehomogenization, and exact raw-row replay.

In parallel, replay the constructive special-fibre cascade into an unsplit
pure-power certificate.  Its current candidate exponent is 104, not V42's
branch exponent eight.  If the generic cleared cofactor is
`v=t^s q`, `q(0)!=0`, combine the replayed special identity
`a1^M-B=tH` with `v*a1^D in J` to obtain the explicit total identity
`q*a1^(D+Ms) in J`.  No result is promoted until both inputs and this
converter replay exactly.  Selected-row generic unit remains one-sided
decisive; selected-row nonunit remains inconclusive.

## Superseding strategy overlay (2026-08-27 08:57Z — reviewed total-rho criterion and shifted-root repair)

The ordered-`T-a1` total-rho reduction is now independently reviewed over
the correct literal ring (`e3d263d5...`, erratum `d322d417...`, Fable5
`98003865...`).  There are 66 positive variables plus rho, not 65; the sole
extra `ez9` occurs as `(3/8)rho^2*a1*ez9` and is a free spectator after
specialization.  Honest special-fibre emptiness is exactly equivalent to a
typed identity `a1^N U(rho^2) in J`, `U(0)=1`.  Keep `N=6` as the first
possible exponent, but do not wait for the large sparse module solver:
selected-row dehomogenized total-rho eliminations, the rho-zero global-power
lift, and the sparse-core module route run independently on AWS.  A rho-zero
membership or nonmembership alone is not a total-chart decision.

Repair the generic deck/square strategy using Opus5's hostile finding
(`d62b3f22...`, additive erratum `614ddcdb...`).  Finite-flat Kummer/Rees
base change is sound, and the total/charged row identity is sound, but the
old source map annihilates every stage-zero `J1,J2` root coordinate.  It
therefore reaches only the terminal raw-row quotient and none of the six
charts.  Generic-square imports must now be ledgered one contact at a time
with the correct deeper shifted root pairs and terminal-grade custody.
Provisional producers for `(2,3,>=2)` and `(2,4,>=3)` follow this repaired
pattern; both are under independent review.  The next contact
`(2,5,>=3)` requires the separately launched `ACT-TOT-G20` builder.  None of
this touches rho zero or supplies whole-fan coverage.

For K00, stop global monomial-order roulette.  Different-model review
`6c4ebd6d...` confirms exactly that the unloaded terminal polynomial is not
in the first-six ideal over `Q[C6,C6^-1,d]` (and also at `C6=1`), while its
complete filtered class remains compatible through normal degree seven:
`r7 in (r1,...,r6)+(d)^8` at `C6=1` (`d3adc56f...` / `bd1c6368...`).  These
are separate statements.  Opus5 review `7f0c3aea...` now confirms the exact
D8 strengthening `r7 in (r1,...,r6)+(d)^9` (`1b31b4bd...`), including an
independent byte-identical `2996 x 5544` emission, exact-Q rank equality
`2547=2547`, and a replayed 492-entry lift.  A degree-eight mutation confirms
D8 is strictly stronger than D7.  Raw global remainder degree is not a
filtered invariant, and any pure-coefficient filtered obstruction starts no
earlier than degree nine.  Run genuine local-ring membership and the mixed
`Lambda^19` load/target/`Jdet` reachability test independently on AWS; do not
infer formal membership, closure, or Taylor realizability from finite jets.

## Superseding strategy overlay (2026-08-27 08:05Z — raw `T-a1` special fibre closes)

The frozen raw ordered-`a1`, `rho=0` source system through grade 19 has no
characteristic-zero field point on `D(a1)` (`5d4c42ff...`, Opus5 review
`a4f6b931...`).  The exact cascade covers two branches: A2 dies at grade 16,
while A1 has a six-row `a1^8` certificate after its branch equations.  This
implies a non-effective global raw power membership, but not the displayed
exponent eight in the unsplit ideal.  Fable5 separately confirms the sharp
computational boundary: every `a1^i*k^j` with `i>=1,5i+4j<=25` is absent
from the grade-through-19 ordinary ideal, with weights 17--19 final and
20--25 prefix-relative (`97e3af98...` / `68764042...`).  `Tg19_7` is a
genuinely independent weight-19 compatibility condition (`dbec09d...`).

Do **not** convert this into total-chart closure.  Specializing `rho=0` after
`a1`-saturation need not agree with saturating the specialized rows.  The
exact next object is now small and typed: with `t=rho^2`, honest special-
fibre emptiness is equivalent to some identity
`a1^N U(t) in J`, `U(0)=1`, and fixed weight turns this into a finite module
membership problem over the DVR `Q[t]_(t)` (`e3d263d...`).  V38 forces the
first possible exponent to be `N=6`; the weight-30 source/compiler preflight
is running on AWS.  Its different-model ring/map review is live.  A positive
identity closes only the frozen total ordered chart; a negative at `N=6`
advances the exponent floor and immediately retargets the next rung.

The terminal receiver remains independent and open.  Opus5's exact raw
support theorem gives a maximum coordinate-linear zero-section of dimension
43, with `k` free and `a1=0`, but it is not a component or honest receiver.
The literal `K00` point is killed by `Phi7`, while transverse directions can
survive saturation before restriction.  The live receiver discriminator is
therefore the closure-first incidence `H_K00`; exact-Q and finite-field AWS
orders are running.  Stop deeper raw receiver exports, generic-`L43`
evaluation without a collision-to-`[6,2]` map, and the speculative row-32
bridge.

Avenue 19 also sharpens without compute.  Every exact integral AS109 lift now
has `deg_y Q=deg_y B>=6` by Moskowicz 2018 Theorem 2.7 composed with the
reviewed residue-ball Hensel noninjectivity (`3d6d09fd...` / Grok
`bc136621...`).  At the first residual `n=6`, require
`6|deg_x(q_6)`, `deg_y P>=12`, `3|deg_y P`, and the recorded common-core
divisibilities.  Degrees `2`--`5` and every prime target degree are history-
closed for this exact seed, so cancel their Newton/support searches; the
result neither excludes `n=6` nor proves lift nonexistence.

The sealed equal-peer whole-portfolio round is synthesized at
`xmodel/ideation-20260827T0635Z-synthesis-sol.md` (`2e3305cc...`). Fable5
and Opus5 remain standing co-researchers on complementary exact evidence;
model agreement is deduplicated and every promotion retains the different-
model gate.  The landing critical path is now (i) the total `T-a1` DVR
certificate, (ii) closure-first terminal incidence, and (iii) source/landing
coverage plus deck/square, with TD6 and the constrained AS109 disproof lane
continuing independently.

## Superseding strategy overlay (2026-08-27 05:30Z — one `J2` chart closes)

The registered second-stage `T-a0` chart is now completely closed by the
reviewed pure identity `a0^3 in I` (`0a7e904b...`, Grok `c6bb46ae...`, Opus
`1ddb4fcb...`, promotion `3e3aa876...`).  This is a denominator-free
actual-total certificate with no genuine localizer and `W=0`; it persists
after adjoining later rows.  The evidence deliberately excludes V24's own
rho-localized rational-function lift.  The landing critical path is now the
ordered `T-a1` chart plus the terminal receiver, with coverage and the
deck/square bridge pursued in parallel.

The sparse ordered-`T-a1` support is nonempty through grade 17: dual-field
screens return a dimension-one ideal and an exact sigma-scaled rational point.
That point fails at grade 18 only in `Tg18_6`, whose exact Q value is
`16210220612075905068`; the Q and F65521 evidence manifests both replay.
This kills one point, not the curve or chart.  Reduce the grade-18 row
symbolically on the curve before choosing between prolongation, support
enlargement, or a chart-wide certificate search.  Do not infer `T-a1`
emptiness from a failed ray.

The significant-news full round `20260827T0145Z` is closed at synthesis
`ideation-20260827T0145Z-synthesis-sol.md`.  Fable 5 and Opus 5 both remain
standing equal whole-portfolio researchers.  Opus independently adds
material capabilities beyond Fable—an exact AS109 source-gauge section,
grading/covering reframings, and scope-hostile unspecialized certificate
audits—while Fable supplied the decisive `a0^3` discovery and complementary
typing/death-certificate mechanisms.  Model agreement is deduplicated and
neither model receives promotion immunity.

## Superseding strategy overlay (2026-08-27 03:18Z — grade-15 stage-two signal)

The all-depth named-section source census is independently confirmed and
promoted (`172755de...` / `f0c57c1f...` / `01f15a0f...`).  Grade 15 is the
first source grade that sees the two pure `J2` directions:
`Tg15_6(A00)=-1/16` and `Tg15_3(A10)=-1/16` (with two additional
rho-even `A10` values).  This kills the two prefix witnesses, not the two
charts.  The full seven-row grade-15 export is therefore the next typed
stage-two input; its dual-AWS V22R1 run follows a fail-closed serialization-
only repair.

The same theorem exhausts every coefficient grade on `CS0` and the terminal
origin `Z00`: all seven literal source series are identically zero there.
Higher exports of this same finite-tail source cannot change that fact.  Any
attack on those residuals must instead use honest Rees/chart equations, a
routing/receiver theorem, or another source family.  Do not fund a deeper-row
search whose only goal is to break `CS0` or `Z00`.

## Superseding strategy overlay (2026-08-27 02:45Z — first-stage `J1` complete)

The direct ordered `T-cs` theorem is now promoted: exact membership
`cs^447*k^164` in the honest actual-total chart ideal has different-model
hostile confirmation (`7af66e58...` / `f16e1114...` / `ddc3b8b4...`).  It
closes the whole registered ordered `T-cs` stratum on `D(k)`, with `W=0`.
Together with `T-rs`, `T-c0`, and ordered `T-c1`, all four first-stage
`J1=(rs,cs,c0,c1)` chart strata are now closed for the named unit-load
family.  The landing critical path moves to `V(J1)`: two `J2=(a0,a1)`
charts, the terminal receiver, source/landing coverage, and the generic
deck/square comparison.  The global off-family `k10=0` timing fan is not an
on-family fifth chart and remains open.

The 22-row `J1=0` census is separately promoted with a correction
(`bedb8dfe...` / `3ac8aaf5...`).  Twelve rows survive, beginning at grade
11, but exact `A00`, `A10`, and origin/`rho`-line sections show that these
rows cannot empty either `J2` chart or the terminal receiver.  The terminal
witness is the origin, not a consequence of the two unit points.  A dual-AWS
V20 source export now computes every grade-13/14 row and tests all three
sections before any stage-two solver is funded.

Finally, correct the staged-calculus converse (`593f953b...`): fibre
emptiness yields a cleared containment in `J+(rho)`, not automatically a
multiplicative `1+rho*W` certificate.  The forward theorem and every direct
certificate remain intact.  V19 may finish as independent/exponent evidence,
but no successor is justified merely to prove existence of a `T-cs`
certificate.

## Superseding strategy overlay (2026-08-27 02:20Z — prefix routing and unit-load filing)

The frozen 22-row actual-total prefix has three independently confirmed
horizontal zero-sections: `cs=1,k=0`, `a0=1,J1=0`, and `a1=1,J1=0`, with
`rho` free (`0ca4647a...` / `959603e3...`).  Consequently this prefix cannot
empty those loci or supply `rho`-torsion there.  This is a discriminator, not
a full-source nonemptiness theorem: unexported grade-13, other grade-14,
higher, and stage-two equations may still cut them.  Do not spend AWS on a
solver using only the same rows.  First export a genuinely new row or prove a
source-routing statement.

The localizer ledger is now deduplicated (`a09232d6...` / `678a087d...`).
Here `k=k0` is the constant coefficient of the literal series `k10`, not the
series itself.  The named post-`M=0`, unit-`k10` Gate-T family is already the
open `D(k)`, so `V(k)` is not an additional on-family residue and must not be
listed separately from `V(k10)`.  Keep every algebraic factor `k` in the
`T-rs` theorem and any `T-cs` certificate.  Carry one separate global
`k10=0` load-timing sibling fan, without pretending its delayed/positive-
order and zero-leading-load readings have been identified or closed.

The fastest next landing work is therefore parallel: review the provisional
direct `T-cs` total certificate; run the cheap `J1=0` specialization before
any `J2` AWS chart; and identify the first unexported source row that sees the
three prefix sections.  Reviews remain asynchronous and do not block sound
successors.

## Canonical strategy correction (2026-08-23)

This is a superseding clarification of the dated survey, not a retroactive
change to what its three inputs reported.

- `G2-PSC` is the **global packet/sheet compatibility** theorem still missing:
  transport a selected GGV packet/corner, with provenance, into a specified
  decorated Sigray pole tree with enough fidelity for the book machinery.
- `G2-BD` is the **post-residue-A bounded-delay/carrier** obligation. It begins
  only after a residue-A datum has been reached. Neither scoped obligation implies
  the other.
- A hybrid GGV-to-Sigray campaign owes `G2-PSC`. A pure Sigray campaign may
  bypass it by selecting/minimizing the hypothetical counterexample wholly in
  the Sigray frame, but then cannot use the GGV farm as proof input and still
  owes Sigray source, complete landing/coverage, delay where invoked, and
  type/total-degree control.
- The corrected local arrows are
  `DIR(C) <=> PC(C) <=> RPMC(C) => KJN(C)` and
  `A-SCALE => UCD-A-min => G2-BD`. The omitted converse arrows are not proved.
  KJN gives `td <= C alpha beta` at a fixed provenanced type, not an absolute
  or cofinal ceiling without an independent bounded type menu.

Accordingly, later same-day language that called the overloaded label `G2` and G5 two equivalent or
independent “terminal walls,” or said these estimates made the whole ladder
unconditional, is not the canonical reading. The exact pure-boundary and Henon
results are dual-confirmed; the local implication/countermodel/bridge work is
single-model decisive partial unless separately promoted.

## Superseding strategy overlay (2026-08-27 00:40Z — staged certificate calculus)

The landing critical path is now a **staged direct-certificate tree**.  Apply
the reviewed localized rho-unit lemma to the four
`J1=(rs,cs,c0,c1)` charts over the literal source, then to the two
`J2=(a0,a1)` charts over `A/J1`, and keep `V(J1+J2)` as a separate terminal
receiver.  A single blowup of `(J1,J2)` is not the charged geometry.  In each
chart, distinguish the exceptional-coordinate power (absorbed by Rees
saturation), a genuine localizer (which creates a residual closed stratum),
and honest ordered-stratum equations.

A direct total-family certificate now replaces three previously budgeted
tasks **for emptiness only**: special-fibre base-change equality, pairwise
overlap/gluing, and nilpotent analysis.  It does not replace total-source
provenance, the chart certificates, localizer complements, the terminal
receiver, or the generic deck/square bridge.  Review/promotion are
`db160c13...` / `16ec6f54...`.  The `T-rs` theorem remains promoted on
`D(k)`.  New closed-form grade-10 `T-c0/T-c1` candidates were sent to
literal-source review; that review has now promoted the full `T-c0` chart
(`139ecb67...` / `cd6f34da...`) after replacing the false “exceptional
coordinate is inverted” step with an exact saturation certificate.  A later
two-row cubic identity closes the whole ordered registered `T-c1` stratum
by the exceptional power `c1^3`, without inverting `c1` and without a
genuine localizer (`40ea0ff3...` / `133ad884...`).  This supersedes the
interim `V(c1)` residual from the first review; it does not close an
unordered standard chart as a separate object.

Opus5 then found an exact rational witness for the frozen `T-cs`
drop-grade-14 control; different-model review promotes the stronger fibre
description (`7fbd9423...` / `5fd19f35...`).  On `D(cs*k)` the grade-10--12
special fibre is a rational 31-fold, and grade 14 is nowhere zero on it.
This replaces the long negative-control computation and explains why the
row is decisive.  It does **not** yet give the direct total-family identity
required by the staged calculus.  Keep V19 running for explicit cofactors;
then clear its inverse variables and type-check the exceptional power,
genuine `k` localizer, and rho cofactor before any DVR-arc conclusion.

The same ideation synthesis makes two allocation corrections.  First,
Reynolds/parity descent is valuable as a fail-closed **cofactor validator**,
not as proof that the invariant ring is the charged square receiver and not
as a terminal-receiver argument.  Second, the prime-ray `mu_3` projector,
translation-equivariant strip, and `3P-ORE3` are one bounded-band experiment.
Its natural MN-family shift is `gamma -> gamma+3`; `p=7,11` cannot fit the
proposed recurrence and `p=19` cannot validate it.  If the source-typing and
bandwidth preflight pass, use `p=23` for fit-plus-one validation and `p=31`
as holdout.  Full `3P-E31` work remains held behind the globally-minimal scope
firewall.

On the disproof side, full unbounded-`x` exclusion of both primitive
partial-`y` cells `(8,12)` and `(9,12)` would automatically raise the
conditional AS109 correction floor to thirteen; no integral normalization is
needed for that implication.  Do not impose the rigid AS109 congruence on the
current monic leaf clients: no charged `Z_109`-flat comparison model exists,
and the earlier integral max-nine calibration added no rank.  The next small
AS action has now cleared hostile review: every exact integral AS109
polynomial lift has `deg_y(B)>=2` (`4a49110d...` / promotion
`696152da...`).  Combined with the reviewed two-sided floor, this excludes
the floor shapes `(12,0)` and `(12,1)`.  It does not license a new support
rectangle, a charged leaf congruence, or any higher-degree integral routing.

## Superseding strategy overlay (2026-08-26 23:46Z — prime-ray obstruction)

The globally-minimal GGV necessary-chain interface now contains an exact
infinite test family at total scale `3p`.  For each prime `p=3 mod 4`, the
fixed `(3,-1)` edge has `(3p-13)/4` final chains and `(p-3)/2` MN-viable
families, while its Algorithm-6 integer `(3p-1)/4` has unbounded prime-factor
complexity.  Review and promotion (`7f3fd203...` / `2f5859cb...`) therefore
retire two speed strategies: enumerating a p-independent finite list of
literal chains and bounding this factor integer by `C(3)*p^e`.

The positive strategy is parametric: attack the exact two-parameter family
with a uniform functional or licensed degree reduction (`3P-E31`), beginning
with regraded `p=7,11` systems.  Terminal systems are conservative coverings,
so their emptiness is sufficient but their nonemptiness is not a Keller-pair
falsifier.  The family applies only after global-minimal selection; the
nonminimal prime shears used in the cofinal-gcd theorem still require an
arbitrary-standard-pair bridge.

In the landing lane, V17's complete row-5 grade-14 source coefficient is now
independently confirmed by both Fable5 and Opus5 (`b4540858...` /
`f0d8a9cd...`, promotion `2c7f624c...`).  This removes its review gate but
does not itself prove a chart theorem.  Their shared display-only V9 term-
telemetry correction is banked at `8e18cef4...`; coefficient bytes and all
algebra are unchanged.

## Superseding strategy overlay (2026-08-26 23:45Z — TD6 P13 unit)

The all-22-q TD6 client has crossed from a nonzero quotient functional to a
reviewed no-common-zero theorem on its exact normalized slice.  Modulo all 38
original FIRST rows, literal CURRENT degree 13 has E3 coordinate 12 exactly
`(3500000000/9)*(U/V)`, with no q or quotient-variable dependence.  On
`F=0,D(U*H*B3)`, the identities `B3=V^4` and
`U*H=V^2-4U^3` make this a unit.  Thus P13+FIRST is empty there, and P12 is
not needed (review/promotion `cb4a85e...` / `4120d6b6...`).

The controlling H15 terminology is 238 nonzero E3-vector records and 535
nonzero scalar entries.  The strategic successor is not another P12/P13
search: it is repaired H19R1, which must emit the 38 original-FIRST
multipliers and a denominator-cleared identity with total `F`.  Until that
certificate composes with a source/landing cover, q15 remains target-shear
rather than an independent source modulus, omitted moduli and boundary
charts remain open, and there is no whole-TD6, SP-2, or JC2 conclusion.

## Superseding strategy overlay (2026-08-26 23:29Z — first total chart)

The corrected literal-source landing lane now has its first promoted
actual-total chart theorem.  On `D_+(rs) intersect D(k)`, four hash-pinned
grade-10/12 rows give an exact saturation certificate and the explicit rho
inverse `-32*rho*qcs^2*(8*rho^2*qcs^2+3)` (review/promotion
`b32de58c...` / `52b59e8d...`).  This supersedes the older statement that
the `T-rs` implementation was merely live.  It does not remove the explicit
upstream V9 source-provenance dependency and does not prove a comparison map
to a separately specialized chart.

The ordered `T-cs` successor adds the complete fifth-row grade-14 total
coefficient to all 21 grade-10--12 rows.  Its first launch failed closed on a
duplicate Singular identifier before any standard-basis computation; a
frozen syntax-only repair is running over exact Q and F65521 without waiting
for independent Fable5/Opus5 review of that coefficient export.  The actual-
total `c0,c1,a0,a1` charts, closed receiver, scheme-theoretic overlaps,
deck-equivariant square bridge, and `k=0` remain open.  Consequently the
landing program has crossed one real chart boundary but remains far from a
six-chart cover or global Gate T theorem.

## Superseding strategy overlay (2026-08-26 18:29Z — literal landing preflight)

The campaign root is now the fidelity boundary between a literal moving
source and the first total-Rees chart.  Independent audit found that the
naive extension `p=-2*rho^2` deleted the frozen connection jets and could
not specialize back to the owner source.  The corrected family is
`pTot=-2*rho^2+2*sigma*ell1+2*sigma^2*ell2+...`, with `rho` and `sigma`
independent.  Before any saturation or standard basis, a discovery-only
compiler reconstructs all seven frozen Faber rows modulo `sigma^13`, checks
the full `rho=0` prefix, deck invariance, recursive coefficient extraction,
the reviewed cusp identity, two negative controls, and discovers the exact
primitive-jet dependency manifest.  Hostile implementation review confirms
the source/ceiling contract (`51c294e0...`), and dual-good-prime streaming
AWS runs are live.  Only after the manifest is frozen will
exact Q plus a fresh prime license the first `D_+(rs)` Rees/base-change
client.  This sequencing protects source fidelity; it proves no landing
claim by itself.

Parallel local-front progress continues without waiting for that gate.
Eleven strict D1 unique-`AC`, `d=2,3` tails through `a=6` are now promoted
(`841f0d6c...` / `8b92c22b...`), while `(1,3,2)` correctly routes away.
The two `a=7,d=2,3` load-tie cells are separately promoted
(`1ae7f6d9...` / `b5c38f1b...`), as is the exhaustive `a=8,d=2`
load-first split (`0558ea1e...` / `9cc68702...`).  The three residual
`a=8,9` strict cells now use the reviewed row-7 order-three functional at
grade 38, beyond their target shadows and before their first pole-four
grades; a complete independent source replay is live.  The TD6 high-q
total-`F` K repair is
also promoted (`ea95d613...` / `5e924e44...`), but remains confined to its
q2--q14-zero residue block and does not remove `F`.  Affine-Faber H16, the
remaining D1 timing chambers, and a splitting-independent TD6 q14 successor
run asynchronously.  Global `G2-PSC`, any invoked `G2-BD`, the other five
Rees charts and receiver overlaps, a cofinal complexity bound, and JC2
remain open.

## Superseding strategy overlay (2026-08-26 17:58Z — H15 fan and landing gate)

The fixed delayed affine-Faber `H=15,v(a)=3` internal kernel fan is now a
reviewed theorem.  The rational cells `0<q<6`, `q=6`, and `q>6` are removed
by the early quadratic block, the complete grade-42 graph predecessor, and
the unique intrinsic cubic respectively (review/promotion `da35a8c2...` /
`ce4fa70f...`).  This closes every positive kernel order in that named
coefficient-chart graph neighbourhood on `D(E*M)`, but does not place a
literal source arc there.  `q=0`, `K10=0`, factor degeneracies, other slopes,
and total-Rees coverage remain separate.  The first control beyond the
homogeneous range already shows why serial reuse is unsafe: at
`(H,q)=(16,6)` graph deviations absorb the grade-44 quadratic obstruction
on both charts.  The complete `(H,q,v(a))=(16,6,4)` equality successor is
therefore computing without waiting on further strategy review.

The significant-news allocation now runs local algebra and landing in
parallel.  Root owns the post-`M=0`, `D(k10)` collision Gate T.  The abstract
iterated-blowup/DVR coverage theorem is already available; the missing work
is scheme-theoretic base change from one literal `(sigma,rho)` source to its
six Rees charts and terminal exact-square receiver.  The obligation table
at SHA `50db2706...` replaces one monolithic Rees kernel by exact chartwise
saturations and observes that a decisive finite source prefix suffices for
an individual chart.  An independent implementation audit of the first
`D_+(rs)` cusp chart is live.

The other owners continue asynchronously: D1 has a dual-AWS producer for
the eleven stable `a<=6,d=2,3` blocks and has moved to the `a=7` load wall;
TD6 has a frozen high-q total-`F` producer while q14 `(F)` membership is
tested; affine-Faber owns the H16 equality and successor ranking.  Hostile
reviews run in the background and do not block successor computations.
Only explicitly fixed/capped actual-total-degree-at-most-12 specializations
are method controls under the classical cutoff.  The historical
“maximum-12” names here denote the unbounded-total partial-`y` `(8,12)` and
`(9,12)` frontier unless a lane registration says otherwise; those families
are not closed by the total-degree gcd theorem.  Their normalized results
become frontier-relevant only through a literal source/landing composition.
Global `G2-PSC`, any invoked `G2-BD`, a complete source/landing theorem beyond
this chart, a cofinal complexity bound, and JC2 remain open.

## Superseding strategy overlay (2026-08-26 17:06Z — homogeneous A cone and D1 ladder)

The first-block router and homogeneous affine-Faber cone are now reviewed
theorems.  The exact first four negative rows classify an untied square
normal by `Q|N^2`, and every repeated-`A` delayed-source arc with
`0<H<=15`, `p,m` units, `v(a)>=H/3`, and the stated effective-load bound is
excluded.  This includes the zero-`K10` subface.  Review/promotion SHAs are
`85389a28...` / `eb303252...` for the bridge and `9c537ad4...` /
`bde78801...` for the cone.  Independently, the literal `H=15` Gate-A
composition is confirmed (`e9dea9b0...` / `d877d6c5...`): exact square
division and the two-sided coefficient map, rather than a Rees analogy,
place that fixed `D(p*m*K10),ord(a)>=5` source cell into the reviewed
valuative theorem.  The order-two priority moves to the finite
earlier-center fan, the mixed `15<H<=21` regime, and the factor-degenerate
receivers.  Exact graph-relative support has already reduced the first
`H=15,v(a)=3` test to one equality wall `(a,q)=(3,6)`; its complete
grade-42 predecessor is running on AWS.

For D1, the pole-order-three `a>=10` producer and the universal odd-pole
functional are independently confirmed.  Together with explicit narrow
promotions of the fixed `a=2..9` bands, a separate composition review now
closes the entire normalized integral unit-load unique-`AC`, `d=1` cell on
`D(p*k0*J)` (tail review/promotion `a4eff964...` / `d4aceedb...`; final
composition review/promotion `bd5b3dd5...` / `85533441...`).  The next D1
batch should exploit the stronger provisional `c>=a+1` tail to remove the
high-contact portions of `d=2,3`, leaving eighteen small baselines rather
than serially replaying all contacts.

TD6's first high-tail polynomial-inverse shortcut is withdrawn on the
registered open: its exact replay requires the known V83 rank factor
`K=2R38`.  The bounded repair is nevertheless sharper than a new atlas,
because `K-B3=-2F(2CU-V^2+2U^3)`.  Clearing H1's K denominator and adding
`F` may produce a literal `B3` unit without inverting K; alternate-pivot
work continues in parallel.  The reviewed V87R1/V88 theorem is unchanged.

## Superseding strategy overlay (2026-08-26 16:28Z — fixed delayed `A` ray closes)

The separate affine-Faber valuative composition review is now
**CONFIRMED**.  On the fixed delayed-load repeated-`A` neighborhood inside
`D(p*m*K10)`, exact coefficient coordinates, the complement bound, and the
complete ordinary-row predecessor system cover every rational kernel order:
`0<q<6` dies on its homogeneous first face and `q>=6` dies by the reviewed
direct unit.  Review/promotion SHAs are `a6d434b5...` / `0c4d2427...`.

This removes that internal normalized ray, not the literal-source problem.
The highest-leverage order-two task is now the source/fan composition:
classify the first square-normal block by `Q|N^2`, build the repeated-factor
and degenerate factor charts, and combine them with the 371-term relative
Newton support.  Other load slopes contain raw `load+a` and `load+lambda`
competitors, so the fixed-ray unit cannot simply be rescaled.  The `q=5`
raw-K tie is harmless on the promoted ray because the all-seven-row
predecessor face is already empty, but every new slope must reproduce that
predecessor routing.  Total-Rees coverage, `m=0`, `p=0`, `K10=0`, `D=0`,
collision/Pell, terminal/Taylor, order two, maximum twelve, and JC2 remain
open.

## Superseding strategy overlay (2026-08-26 16:25Z — D1 tail and TD6 q-family promoted)

The D1 high-contact tail is now a reviewed theorem.  For every
`a>=13,r>=a` on `D(J)`, the complete source through grade 38 has pole order
at most two, and the exact odd-row recurrence leaves the unit `-J/4` at the
terminal coefficient.  Review/promotion SHAs are `7a067603...` /
`29e2ac11...`.  More importantly for speed, the order-two coefficients are
the cubic truncation of `(1-(p/2)T)^(1/2)` and the complete order-three
candidate coefficients are the truncation of `(1-(p/2)T)^(-1/2)`.  This
has now been derived from the exact Laurent-to-Faber generating transform,
not guessed from two rows.  The preferred D1 strategy is therefore a general
pole-order functional plus an automatic jet ceiling; the complete `a>=10`
order-three AWS replay has now passed exact Q and F65521 with all eleven
primitive source families and every required high jet retained.  Fresh
hostile review and promotion are still required, so contacts `a=10,11,12`
are not yet promoted.

TD6 V87R1 closes the sole q2-specialization custody gap in V87, and fresh
review confirms its composition with the exact V88 q15 shear.  On the
retained `D(U*H*B3)` slice the literal raw identity has remainder
`F*h_F+sum q_e*h_e` for all 22 transverse jets
`e=2..14,16..24`, while q15 is an arbitrary target-shear coordinate.
Review/promotion SHAs are `068b33a7...` / `e3089afe...`.  The next preferred
gate is global q-nilpotence modulo `(P12,FIRST,F)`, because coordinate-power
certificates combined with the promoted total-q identity would avoid 22
unit charts.  The high-tail `q16..q24` block is the cheapest discriminator:
a dual-AWS client has provisionally found an acyclic row graph with `N^2=0`,
so its FIRST inverse is the polynomial `I-N`; literal P12 replay remains
mandatory.

For affine-Faber `A`, the formal identity remains reviewed only at its
polynomial scope while the distinct valuative composition review runs.  A
new dual-AWS support emitter finds 371 exact monomials in `K=E*H3+H5` over
both Q and F65521.  This is navigation only.  Coefficient-factor strata,
predecessor-relative Newton cones, and the literal source/Rees overlap remain
the gates.  The bounded overlap attack is to classify the first square-normal
block by `Q|N^2`, then compose its repeated-factor chart with the relative
Newton fan; degenerate factor types must still route to collision/Pell/`p=0`
receivers.

## Superseding strategy overlay (2026-08-26 15:50Z — formal `A` unit and TD6 repair)

The earlier literal relative-order-six affine-Faber `A` producer is
**REPAIR**, not a full-face theorem.  Its two exact-Q projective charts and
hand identities are correct, but its moving center stopped at `a=s^5*a5`;
the omitted `a6` and, after ramification, other tied center/source/complement
jets were not covered.  Hostile-review SHA is `06e27093...`.  The literal
two-chart calculation survives only at that frozen integral support.

A stronger center-complete producer now avoids that defect.  With
independent formal variables for the moving center, discriminant, normal
scale, both kernel coordinates, all four complements, and all delayed loads,
exact Q proves modulo grade 46
`H3=-(3/8)t^42*lambda^2*M*X*Y-(1/16)t^45*lambda^3*M^3` and
`H5=(3/8)t^42*E*lambda^2*M*X*Y`.  Thus
`[t^45](E*H3+H5)=-E*lambda^3*M^3/16`, uniformly after arbitrary
power-series substitution and hence for every ramified kernel order `q>=6`.
Producer result/freeze SHAs are `5736a3b0...` / `0ef7cfc8...`; hostile
review independently confirms the polynomial identity (SHA `7349330c...`).
It is promoted at that identity-only scope (SHA `d818e828...`).
The distinct two-sided coordinate/complement-absorption plus `q<6`
valuative composition is now the only gate to closing this fixed ray and is
under a separate review.  No whole-`A` promotion precedes that gate.

TD6 V87's exact 22-jet identity has no detected algebraic defect, but its
first hostile review correctly returns **REPAIR** because the claimed
specialization to reviewed V86 was not compared coefficientwise in a common
encoding (review SHA `c4f3ea4a...`).  The bounded comparator is now the
smallest repair.  Independently, V88 has a dual-AWS producer PASS for the
triangular `q15` target shear: all 6,507 transport rows, all 38 FIRST maps,
and literal P12 are invariant, with the unique induced boundary in the
expected q15 slot (result SHA `c34ae948...`).  V87+repair+V88 remains
review-gated; no whole TD6 or SP-2 conclusion follows.

On D1, the complete fixed `a=9` cell is now hostile-review confirmed and
promoted on `D(p*k0)`: the exhaustive `k60,k60_1` split closes at grade 30
(review/promotion SHAs `8524bd1e...` / `dfabe082...`).  A separate dual-AWS
producer now gives a target-free odd grade-38 recurrence excluding every
contact `a>=13,r>=a` (result SHA `09ca3214...`); hostile review is live.  An
order-three strengthening intended to include `a=10,11,12` is being rebuilt
after a correct source-support audit caught omitted high `A` and `k10` jets
in its first compiler.  No result is claimed from that incomplete attempt.

## Superseding strategy overlay (2026-08-26 15:14Z — TD6 total-`q2` gate)

The literal TD6 total-`(F,q2)` identity is now hostile-review confirmed.
On `D(U*H*B3)`, the genuine raw P12 and 38 FIRST rows express the unit
`U^12*H^3*B3` modulo `(F,q2)` with no `F` or `q2` inversion.  This excludes
simultaneous positive valuations of `F` and `q2` on the retained normalized
slice (producer/review/promotion SHAs `22fb1c6c...` / `9e70d6ce...` /
`677513a2...`).

The V87 augmentation from one q jet to all 22 licensed jets
`q2..q14,q16..q24`, with `q15=0` kept explicit, has returned a dual-AWS
producer PASS and is under hostile review (result SHA `6e22c6ac...`).  It
retains every transport and direct-derivative path, uses an untruncated
multivariate q ring, and proves regularity modulo the full augmentation ideal
without coordinate-dependent inverses.  Unit-q fibres, omitted q15, dead
stretch, correction,
orbit/pole, centering, and boundary moduli remain separate routing problems;
no whole TD6, SP-2, or JC2 conclusion follows.

## Superseding strategy overlay (2026-08-26 15:05Z — affine-Faber `K` closes)

The generic corrected affine-Faber component is now hostile-review confirmed
`J`-empty in the completed normalized ordinary-Faber chart on
`D(D*(5D+2s)*(5D-2s))` (producer/review/promotion SHAs `fe1193ab...` /
`08a6bec0...` / `99d0af60...`).  Normalized accessibility is therefore
confined initially to the two resonances `A:5D+2s=0` and `K:5D-2s=0`.

The exceptional delayed-load `K` source ray on `D(D)` is now also closed.
The repaired composite covers the squarefree branch, the repeated-root
discriminant image, the balanced `L!=0` K2 chart, and the early-kernel
`L=0` weighted face.  The first hostile review demanded the missing
`L=0` face; fresh different-model review of the controlling V2 composite is
**CONFIRMED**.  Producer/V2/review/promotion SHAs are `6178089b...` /
`7c731df0...` / `abce4bc1...` / `3b59e6c5...`.  This is a theorem only for
that delayed-load source ray; it is not a cover of `D=0`, other load slopes,
or the square/Pell receiver.

The attempted shortcut from the normalized `A` cubic to the literal delayed-
load source is quarantined.  A legal next constant normal jet cancels its
first proposed repeated-root pole, and arbitrary ramified source arcs have
additional grades before the unit-`J` tie.  Independent audit/design-repair
SHAs are `dfb1cc84...` / `9f91fe43...`; fresh external adjudication SHA
`a4158b74...` returns **PRIOR_REVIEW_REPAIR**.  The direct squarefree rational
witness is still source-rejected, but there is no whole-`A` source exclusion.

The primary normalized order-two task is therefore the correction-complete
exceptional `A` literal-source fan.
Its mandatory skeleton includes `2H,3H,14e,16e,18e,19e,14e+H`, every
actual load-jet valuation, and the mixed cubic parity forms; it is refined by
whether the quadratic `2H` initial vanishes, the squarefree/repeated-root
split, and the `q0`-open/`A` split (support-addendum SHA `aa225650...`).  In particular,
on the repeated `A` null ray `H=5e`, the unloaded cubic normal occurs at
`3H=15e`, after the load face at `14e` and before the grade-`16e` successor;
it cannot be omitted.  The current exhaustive split is `q<6`, `q=6`,
`6<q<15/2`, `q=15/2`, and `q>15/2`.  The open interval is not homogeneous:
at grade `30+2q`, the load-normal jet of relative order `2q-12` can cancel
the even K2 obstruction on both projective kernel charts.  It is therefore a
mixed K2/load-normal successor face requiring its own regraded all-rational-q
client.  The two equality faces retain every kernel/complement, load, and
target jet through the tie.  Build flat two-sided charts and solve every
predecessor grade before using a normalized cubic residual.  In parallel,
TD6 advances from the literal total `(F,q2)` identity
to the all-`q` source lift.  D1 fixed contact `a=9` is now
hostile-review excluded on `D(p*k0*k60)` by its grade-27 exact-contact block
(producer/review/promotion SHAs `3679d0db...` / `95e9990b...` /
`14d01a82...`).  On `V(k60)`, the complete grade-30 producer now kills both
`D(k60_1)` and `V(k60_1)` after the nonempty grade-28/29 collision (result
SHA `bbb1d718...`); hostile review of that composition is live.  No
total-Rees cover, order-two, `(8,12)`, maximum-
twelve, or JC2 conclusion follows.

The current high-leverage `A` candidate avoids a separate obstruction on
every mixed-q face.  In the exact moving-discriminant chart, the complete
inverse-Faber rows appear to satisfy
`H3=-(3/8)s^30*M*X*Y-(1/16)s^45*M^3` and
`H5=(3/8)s^30*E*M*X*Y` modulo `s^46`.  Hence the single source-row
combination `[s^45](E*H3+H5)=-p*m^3/16` is a unit on `D(p*m)`, uniformly
for every rational `q>=6`; it permits the mixed predecessor to exist but
forbids its prolongation.  The controlling design SHA is `78766c9d...`.
This remains producer-tier until complete-source AWS replay and the two-sided
ramified chart/`q<6` coverage argument pass hostile review.

## Superseding strategy overlay (2026-08-26 11:34Z — `p=0` cusp open closes)

The post-`M=0` quadruple-root cusp `D(rs*k0)` is now hostile-review
confirmed and eliminated at absolute sigma-grade twelve.  With `d=rs` and
`u=c1/d`, the raw grade-ten quotient has
`c0=0`, `c1=d*u`, `a0=3*cs*u`, and `5*k0*d+96*u^2=0`; the complete
target-free row `E_(6,12)=-(21/1024)d^3u^2` is a unit before any
grade-eleven pivot.  Producer/review/promotion SHAs are `fadb2794...` /
`d1a0d83e...` / `cf03801e...`.

Together with the reviewed odd-sheet kill, this removes the two regions
where the leading `R` coordinate is nonzero.  It does **not** yet close the
`p=0,D(k0)` special fibre: the residual raw `C` opens, the fixed-`p=0`
high-contact `A` opens, their exact Cech/normalized-Rees compatibility, and
the all-zero higher-contact receiver remain explicit debts.  Positive
moving-`p` valuations require one total unspecialized source family before
empty-special-fibre propagation is legal.  Generic V12/unbounded-`d=1` and
eight-form fan artifacts are quarantined navigation because later
`k6/k2` terms were omitted; they are not inputs to this collision work.
No whole-square, order-two, `(8,12)`, maximum-twelve, or JC2 conclusion
follows.

## Superseding strategy overlay (2026-08-26 10:41Z — `p=0` odd sheet closes)

The normalized quadruple-root odd collision sheet
`V(rs) intersect D(cs*k0)` is now hostile-review confirmed and eliminated in
the terminal `[6,2]` client (promotion/review/replay-freeze SHAs
`f48401b5...` / `5cbfe1d8...` / `9a4f6014...`).  With
`w=e1/cs^2` and `k0=(12/5)w^2`, the raw target-free row is
`E_(5,14)=-(21/320)cs^5w^2=-(7/256)k0*cs^5`, hence a unit on the named
open before the predecessor equation, grade-13 pivots, radical, or
saturation.  Gate A and the finite Taylor pullbacks are therefore retired
only for this dead sheet; they have not been proved.

The `p=0` work now moves directly to the disjoint correction-aware cusp
chart `D(rs)`.  The generic-square recursion remains as in the 10:40Z
overlay: exact `r=1` plus horizontal unique-`AC`/`RC` and exceptional faces.
Positive-order or fractional/ramified load/base slopes and omitted
zero/infinity supports remain separate.  No whole-square, exact-order-two,
`(8,12)`, maximum-twelve, or JC2 closure follows.

## Superseding strategy overlay (2026-08-26 10:40Z — vertical `c=3` closes)

The generic-square vertical face `a=0,c=3,r>=2` on `D(p*k10)` is now
hostile-review confirmed and promoted (producer/review/promotion SHAs
`c03ad204...` / `af10cf34...` / `3c35bb9e...`).  The complete source rows
allocate the two roots of `L`; the grade-fourteen complementary-factor
condition and grade-fifteen cubic contradiction eliminate `r=2`, while
symbolic independence from the next `R` coefficient gives every `r>=3`.

Together with the reviewed low-contact `c=1,2` and high-contact
`c>=4,r>=2` gates, the remaining vertical normalized-`A` work is the exact
`r=1` receiver.  Horizontal positive-`A` contacts still require the
parametric unique-`AC`/`RC` and exceptional-face clients.  This overlay is
arcwise/set-theoretic only and does not cover `p=0`, `k10=0`, zero/infinity
supports, fan exhaustiveness, the square branch, exact order two, or JC2.

## Superseding strategy overlay (2026-08-26 09:45Z — square low contacts close)

The generic-square finite `C` contacts one and two on `D(p*k10)` are now
hostile-review confirmed and promoted (producer/review/promotion SHAs
`b8ae74e5...` / `ca1a669d...` / `3447ce8c...`).  Contact one is integral;
contact two requires the exact proper `p*k10` saturation.  The matching
unlocalized failure is a retained negative control, not a source
counterexample.  The conclusion is arcwise/set-theoretic contact raising and
does not assert a reduced boundary scheme.

The live generic-square recursion is therefore narrower than the 08:39Z
overlay: first run the unique `r=1` receiver.  Reviewed high-contact work then
already covers `c>=4,r>=2`; the remaining vertical `A`-leading source case is
exactly `c=3,r>=2`, split into `r=2` and the uniform `r>=3` triple-pole
module.  Horizontal positive-`A` contact uses the exact `(d,s)` lower-hull
classification and symbolic cone translations, rather than a sampled integer
box.  The `p=0`, `k10=0`, and zero-section terminal/Taylor receivers remain
separate and mandatory.  No square, order-two, or `(8,12)` closure follows.

## Superseding strategy overlay (2026-08-26 09:35Z — zero-load square gate sharpens)

The complete square third-tail source theorem is now hostile-review confirmed
and promoted (producer/review/promotion SHAs `5a966b1d...` / `26273432...` /
`dfc44850...`).  For fixed `p` or the displayed integral tangent
`p=Lambda*p1`, with integral positive-order `k10=Lambda*kappa`, its necessary
condition forces the full linear coordinate `M` to vanish.  At the quadruple
root this uses `z^6|P` and the two independent coefficients `-beta^3` and
`-alpha^3`; the older `beta=0` conclusion was correct but nonsharp.  Six
Laurent rows suffice, while the frozen producer tests seven ordinary rows.

This removes the nonzero zero-load square/discriminant first directions and
their quadruple-root limit only at that integral third-tail gate.  It does not
license importing the result to fractional ramified `p` or `k10` slopes.  The
active square recursion therefore splits cleanly: enumerate the reduced
seven-form positive-order-load lower hull (keeping dominated terms as source
sentinels), and separately advance the post-`M=0` p-zero collision atlas.  A
dual-AWS exact grade-eleven/twelve odd-face client is already live; any
surviving normalized sheet is sent immediately, without reviewer blocking,
to terminal `[6,2]` and both Taylor pullbacks while its hostile review runs in
the background.  No square, order-two, or `(8,12)` closure is inferred.

## Superseding strategy overlay (2026-08-26 08:57Z — TD6 denominator atlas)

The formerly opaque TD6 CURRENT factors now have hostile-review-confirmed
nested identities (review/promotion SHAs `a3e13150...` / `6037e902...`).
They supply a small exceptional-divisor atlas, including the exact normalized
`G=L=0` support `(0,0)` and `x=-4,s^2=-4`, but do not factor the generic
`L=0` hypersurface and do not establish a CURRENT coefficient.

TD6 therefore remains at CURRENT, not K2.  First emit the already-computed
reduced coordinate numerators and factor residues before the fail-closed
assertion.  Then compute a fraction-free conormal line/Fitting support under
source-honest alternate pivots with an explicit Bezout/Cech cover.  Rebuild
original source rows on `F=0`, `G=0`, or generic `L=0` only for factors that
survive that presentation test.  A gcd or matching generic rank is not a
cover; generic `L=0,D(UVFG)` must not be deleted by the norm slogan.

## Superseding strategy overlay (2026-08-26 08:39Z — order-two fan narrows)

The nonsquare discriminant first-normal component is now excluded on its
first-contact open (promotion/review SHAs `96f59880...` / `308691f9...`).
On the remaining generic square component, hostile review confirms both the
half-weight radical `C=R=0` with `A` free (`49744ab9...`) and elimination of
that leading `A` in the high-contact cone, stable under the first moving-`p`
tangent (`00640cd4...`, `a3bbee7b...`, promotion `f499bcba...`).

The primary square task is now a small valuation fan, not another global
saturation.  Run the exact `ord(C)=1,2` clients first.  If they eliminate
those rays, use one complete grades-thirteen-through-fifteen source/Faber
client for all `ord(C)>=3, ord(R)>=1`: through grade fifteen the mixed
denominator-three/four terms occur too late, leaving `-A^3/L^3` as the sole
top-denominator separator.  Reviews remain asynchronous; successful producer
evidence immediately feeds successors.

This overlay is restricted to `D(p*k10)`.  In parallel, design the separate
zero-load square/discriminant-intersection receiver and the `p=0` quadruple-
root chart; neither is covered by the generic fan.  Terminal `[6,2]` and both
Taylor gates attach immediately to any surviving component.  Do not infer
order-two or `(8,12)` closure until those strata and normalized-fan coverage
are exact.

The significant-news whole-portfolio rescan is frozen in
`xmodel/global-ideation-round-20260826-late.md`.  Its working allocation is
`40%` order-two square closure, `25%` TD6 CURRENT/globalization, `15%` bounded
D1/order-one/remaining maximum-twelve coverage, `10%` protected global
landing/ceiling work, and `10%` protected fixed-support counterexample work.
Formalization continues asynchronously on stable reviewed identities and is
not a discovery-path dependency.

## Superseding strategy overlay (2026-08-26 06:52Z — TD6 narrow quadratic gate)

TD6 V84R2 is hostile-review confirmed at one deliberately narrow scope
(producer/review/promotion SHAs `caf1cee1...` / `cbfc5616...` /
`545a342a...`).  On the fixed source-typed A3 presentation and its common
generic principal open, the unique previous/pole leftover pairs to zero with
all ten monomials in `Sym^2 span(q2,q3,q4,q5)`; the denominator is a unit.
This does not cover the other 290 pairs or the current stage.  In particular,
the fixed-section current equations remain inconsistent, so the result is a
staged source-incidence theorem rather than a Kuranishi map at a solution and
licenses no nonlinear-family inference.  TD6 effort therefore stays focused
on the exact current obstruction `S_cur` restricted to the reviewed
24-dimensional Stage-A kernel; no further isolated FIRST-axis work is useful.

## Superseding strategy overlay (2026-08-26 05:49Z — exact order-two component recursion)

The nonzero-`k10` Padé lemma is now hostile-review confirmed
(theorem/review/promotion SHAs `2c917b9f...` / `73c30502...` /
`40790378...`), with an independent exact AWS coefficient replay
(`cd644142...`).  It removes the only residual support left by the earlier
saturation-slice erratum.  The characteristic-zero reduced first-normal
gate now has exactly two components: the arbitrary-load square family and
the zero-load discriminant family.  The modular minimal-prime calculations
are no longer carrying this support conclusion.

Order-two work therefore becomes an exact component recursion.  On the
square component, use the matched normal chart around
`K=(z^2+s)^2`, `N=(z^2+s)(alpha*z+beta)` and compute the next divided jet;
on the discriminant component, use the explicit double-root chart with
`k10=0`.  Retain higher-contact sections, and treat the square/discriminant
intersection and slower Puiseux slopes in separate weighted charts.  Do not
restart broad invariant-quotient or Rees saturations.  Terminal `[6,2]` and
both Taylor gates are imposed as soon as a componentwise lift survives.

## Superseding strategy overlay (2026-08-26 05:18Z — smooth-D1 and TD6 K2 repair)

D1 V9 is now hostile-review confirmed (review/promotion SHAs `f81c5b05...` /
`b8a11227...`): the fixed-chart V8 obstruction transports across the entire
smooth double-root discriminant after localizing at the root-axis coordinate
`a`.  This retires further work on that smooth cell.  D1 effort moves to the
weighted triple-root charts (`v(a)>0`) and only then to moving loads or other
normals; the unit-axis theorem must not be read as excluding positive axis
valuation.

TD6 Stage A is also hostile-review confirmed (producer/review SHAs
`6867c915...` / `b47b613f...`): the full licensed 24-dimensional kernel
survives previous/pole at first order.  V83 gives one exact principal
rank-38 open `D(U*H*B3*R38)`, leaving `R38=0` for alternate-minor/raw-fibre
coverage.  The first K2 pilot is a dual-AWS deployment-negative, not a
mathematical result: an inherited scalar propagator failed on symbolic
coefficients before constructing a quadratic table.  The immediate TD6
successor is therefore the smallest typed symbolic-center source-RHS repair,
followed by the same bounded dual-host K2 pilot and one independent Spencer/
symbol control.  Broad K2 sharding waits only for that repaired pilot, not
for another review round.

## Superseding strategy overlay (2026-08-26 05:15Z — exact order-two/order-one screens)

The order-two first-normal principal-part theorem is now repaired and
hostile-review confirmed (corrected theorem/review SHAs `4c9d9cfc...` /
`08a5ca51...`).  On `k10=0`, the first seven rows are exactly the negative
principal part of `(3/8)N^2/K`, so their vanishing is equivalent to
`K|N^2`.  Slicing and then saturating has precisely the square and
discriminant reduced supports; the same equality holds for the full
saturation on the actual nonzero first-contact open.  The only possible
extra special-fibre support is at `k10=N=0` with nonsquare `K`.  Therefore
the high-value order-two split is now: impose terminal/Taylor/next-jet data
separately on the explicit square and discriminant strata, while proving or
falsifying the nonzero-`k10` Pade support lemma that would remove the residual
zero-normal limit.  Immutable V1 SHA `988d659f...` remains `REPAIR` only at
its withdrawn global saturation-before-slice equality; modular component
agreement remains navigation rather than a characteristic-zero radical.

Order one now has a reviewed exact pre-genus rejector (theorem/review SHAs
`bbd744fa...` / `44af62f2...`).  A genuine source forces the complete
descended `r7` graph on every actual coefficient-component normalization to
be a pure-power map with one zero and one pole; if the source-map degree is
`n`, its degree `d` satisfies `d*n=U-1`.  Complete divisor and critical-value
tests therefore precede generic projection genus, but must retain all
infinity/chart points and must not assume birationality.

The original pair of random fixed-load order-two invariant-quotient probes
both timed out inside source saturation before producing any quotient data
(report/manifest SHAs `394acff2...` / `66985179...`).  This is no geometric
evidence.  Monolithic saturation is deprioritized in favor of the exact
first-normal strata and proof-carrying componentwise successors; independently
capped cross-controls remain navigation only.

## Superseding strategy overlay (2026-08-26 04:54Z — coverage/Kuranishi pivot)

V8 is now hostile-review confirmed: one exact 48-term q-linear row identity
covers every `eta>=0` in the fixed full-Q/R D1 chart.  Further positive-h
grades there are retired.  D1 now receives a smaller, sharper allocation for
coordinate-action transport over the smooth discriminant and the weighted
triple-root charts; a dual-AWS smooth-discriminant transport is already
producer-positive but remains review-gated.

The significant-news synthesis SHA `e16c128c...` pins three clean whole-46
scans and one packet explicitly degraded by sibling-heading exposure.  New
allocation is order two/order one `50%`, D1 coverage `15%`, TD6 Kuranishi
`30%`, protected global/formal work `5%`.  Order two is split into its exact
square/discriminant first-normal branches and next divided jets.  Order one
tests the full divisor/critical values of `r7` before genus.  On the source
the divisor order is `U-1`; on a component normalization reached with degree
`n`, its pure-power degree is `d` with `d*n=U-1`, and `d=U-1` requires a
birational source map.  The explicit correction SHA is `de42e834...`.  TD6 freezes
`Q_prev=Q` and advances to the quadratic Kuranishi map plus one symbol-
involutivity control.  Global landing/coverage and a cofinal ceiling remain
unchanged.

The dual-prime `Q1*` endpoint has two matching modular components, but this
is navigation only.  Principal-part/UFD producer SHA `988d659f...` proves
the exact `k10=0` criterion `K|N^2`; its review and the nonzero-`k10`
Padé/ideal theorem are the live characteristic-zero gates.

## Superseding strategy overlay (2026-08-26 04:32Z — first-contact recursion)

The live order-two family now has a reviewed exact first normal gate.  In the
chart `C=K^2+Lambda*N`, all seven rows divide polynomially by `Lambda^2`, and
their first boundary ideal `Q1*` depends only on the four normal coordinates
and `k10`.  The dual-prime AWS endpoint is a discriminator: a unit pushes all
arcs to higher contact, while each nonunit component becomes an exact stratum
to classify and immediately test against Taylor/terminal constraints.  It is
not itself the full Rees boundary or an order-two verdict.  Promotion SHA is
`26b8a571...`.

The D1 filtered complex has now passed three reviewed grades.  Exact scalar
corrections successively lower the uniform threshold from `eta>5` to
`eta>=15/4` and then `eta>=5/2`.  The next wall is the complete eight-term
`h*q^2*r` grade, which cannot be reached by another scalar multiplier; the
24-column `h*q_j*E_i(0)` map is live on dual AWS.  A support-cutoff or
discriminant-localized finite-determinacy theorem is still required;
independent saturation of already cancelled grades is retired.  The latest
promotion SHA is `a0bb826c...`.

## Superseding strategy overlay (2026-08-26 04:16Z — filtered linear gates)

TD6's packed FIRST map is now reviewed surjective on its exact generic
rank-38 open: `K^132 -> K^38` has zero cokernel, so every well-typed
square-zero perturbation of the same presentation passes FIRST.  Further
generic FIRST-axis work is formally retired; rank-drop fibres remain distinct,
and the live frontier is previous/pole/current followed by quadratic
Kuranishi/Fitting data.  Corollary/review/promotion SHAs are `b1352aa1...`,
`b24ab0d3...`, and `7786868e...`.

In D1, the unchanged full-`q2` witness transports across the first transverse
`h` normal uniformly iff `eta>=15/2`, now hostile-review confirmed.  This is
a threshold of one witness, not a branch below it.  The first sparse
row-module correction is now also reviewed: it cancels the complete worst
`h`-linear Q-R grade and improves the uniform range to `eta>5`.  Later
producer-tier grades lower the threshold further.  This validates the
significant-news decision to recurse through one filtered obstruction complex
instead of independent saturations.  Moving source/load data, equality faces,
full fan coverage, D1, and JC2 remain open.

## Superseding strategy overlay (2026-08-26 04:15Z — significant-news synthesis)

Four independent whole-46 scans after complete exact-order-four elimination,
D1 full-`q2` closure, and TD6 first-kernel closure converge on one allocation.
Exact order two/order one now receives 40% of new attention and capacity,
centered on the universal six-load invariant quotient, the reviewed
one-`Lambda` `U=2,[6,2]` reduction, and a two-class cyclic-receiver falsifier.
D1 receives 30% for the filtered obstruction complex, the combined `eta=5`
grade/equality faces, and discriminant-localized finite determinacy.  TD6
receives 25% for complete previous/pole/current maps and the first licensed
quadratic Kuranishi/Fitting object.  The remaining 5% protects global/AS
diversification and reusable proof interfaces.

No new exact-order-four production is licensed; already running class shards
may finish only as controls.  Generic TD6 FIRST-axis work is retired.  Reviews
remain asynchronous, and genus-zero quotient components are parameterized as
counterexample-side leads rather than discarded.  Synthesis SHA is
`0e19862745acce9fd2b60ba0f562f987902d1481bf732bdc21bcd92fd3612c24`;
its four independently frozen input SHAs are recorded there.  The next blind
whole-portfolio backstop is `2026-08-26T16:05Z`, with immediate rounds on
significant news; the quiet web sweep remains due `2026-08-27T00:59Z`.
All heavy computation is AWS-only.  No global landing, cofinal degree bound,
all-`(8,12)`, maximum-twelve, or JC2 conclusion follows.

## Superseding strategy overlay (2026-08-26 04:02Z — D1 full `q2`)

The corrected D1 witness now has a reviewed complete strict halfspace region.
At `alpha=15/2` it has unique monomial initial term exactly when
`beta>5,delta>5`.  The separately charged control-2 semantics for a later
`q2` activation give `delta>beta`, so the reviewed `q2=0` theorem and the new
cone theorem together close the first omitted-support coordinate throughout
`5<beta<6` on the frozen axis/cusp/load chart.  Result/freeze/review SHAs are
`0ed5eab5...`, `c54c33be...`, and `5f099773...`.

D1 work moves to the transverse `h` normal, then the remaining axis/load and
support coordinates, using sparse filtered syzygy corrections rather than
repeated opaque saturations.  The three equality faces of this witness remain
separate full-initial localization clients.  Nothing here supplies a full
Groebner fan, moving-source theorem, full double-root result, D1, or JC2.

## Superseding strategy overlay (2026-08-26 04:00Z — TD6 first kernel)

The complete 24-dimensional generic transport kernel now survives the first
source gate at reviewed scope.  V82S2 confirms unit-denominator zero first
conormal for the two nontrivial dead-kernel directions `d10,d15`; reviewed
V78 already supplies the same conclusion for all 22 q-directions, and reviewed
V81C identifies their direct sum as the entire transport kernel.  Producer/
review SHAs are `e18147d2...` / `3f62b23d...`; case MANIFEST/FREEZE SHAs are
`7f1eefc7...` / `f683d403...`.

Further isolated first-stage axes are no longer the proof-discriminating TD6
work.  Complete the live previous/pole/current maps on the full kernel, then
form the quadratic Kuranishi/second-conormal object on its surviving kernel.
This is still fixed-A3, generic-field, square-zero source incidence; it gives
no nonlinear neighborhood, family, full TD6, SP-2, or JC2 theorem.

## Superseding strategy overlay (2026-08-26 03:50Z — order four eliminated)

The entire `(8,12)` exact-order-four coefficient-infinity leaf is now
independently confirmed empty for every source degree `U`.  The two exhaustive
load strata close by complementary mechanisms: Hall--Shioda/divisor 19 for
`mu4=0`, and exact source dominance of the lemniscatic elliptic target
`Z^2=X^3-16X` for `mu4!=0`.  Loaded theorem/review/promotion SHAs are
`0ac7da1d...`, `4ac21da8...`, and `d0b2e9bc...`; complementary theorem/review
SHAs are `a5d40fd8...` / `a40827f9...`.

Order-four source projection is therefore retired as a live proof gap.  Its
prime-lift, invariant-target, and genus-obstruction machinery remains reusable,
but the `(8,12)` frontier is now exact order two and order one.  The immediate
order-two target is a divisibility-safe, checkpointed replacement for the timed-
out `U=2,[6,2]` strict-Rees monolith, followed by the complete terminal-profile
and lower-tail/Taylor bridge.  D1 cone recursion and TD6 higher compatibility
continue independently.  A significant-news whole-46 micro-round is in flight;
its synthesis may supersede this provisional allocation.  No all-`(8,12)`,
maximum-twelve, or JC2 conclusion is licensed.

## Superseding strategy overlay (2026-08-26 02:10Z — TD6 dead stretch)

V80B is independently confirmed only as a pure-axis discriminator at the
single center `(C,V,U)=(1,1,1)`, `beta=0`.  There `d10,d15` have zero
compatibility tables and the other nine dead-stretch axes are individually
inconsistent.  Their equal-looking first scalars sit at shifted keys and do
not imply a common form or a joint block obstruction.

The next TD6 gate is therefore the already launched dual-AWS V81B compiler:
one source-provenanced joint matrix for all 22 licensed q directions and all
eleven dead-stretch axes over symbolic centers.  The fixed V80B vector is a
specialization control only.  The two V81A attempts stopped before any
mathematical column, first on a missing dependency and then on scalar/jet
operator dispatch; they are deployment/software controls only.
Producer/review SHAs are `18017371...` / `3dad10e5...`.

## Superseding strategy overlay (2026-08-26 02:07Z — portfolio synthesis)

Four independent whole-46 scans converge on the same immediate ordering.
First close the now-confirmed order-four genus-one theorem's source premise:
race exact componentwise `(q,y)` nonconstancy, prime-lift/good-reduction,
graph, and `a6=0` complement certificates on AWS.  In parallel identify the
signature `(4,4,2)` Kummer target with the fixed lemniscatic elliptic curve
and emit a regular-differential contradiction, but do not let that replace
the source projection proof.

Second, turn D1's reviewed `la^20` endpoint into an explicit syzygy and
strict Groebner cone, then recurse on the exceptional coefficient faces of a
normalized Rees/tropical graph.  Third, replace serial TD6 coordinates by one
source-provenanced 33-axis adjoint/Fitting/Kuranishi object over symbolic
centers.  Fourth, race tree leaf-pruning against Kummer eigensheaf degree
blocks as the uniform terminal-to-lower-tail bridge.  Reviews and small Lean
certificates trail discovery asynchronously.  Synthesis SHA is
`40bd626bd1f1b77c8e5d52d1be35a57f89b2407218ecdcb52b7853b496831022`.
The next blind-round backstop is `2026-08-26T14:07Z`, with immediate rounds
on significant news; quiet web sweep remains due `2026-08-27T00:59Z`.

## Superseding strategy overlay (2026-08-26 01:59Z — D1)

The corrected-A replay and hostile review now isolate the earlier D1 conflict
to one malformed hand-factored E8 equation.  A compiler derived byte-for-byte
from the pinned reviewed-B equations reproduces the same fixed-datum theorem
in independent LPDP-contraction and global-dp encodings: for
`a=1,h=q2=k=nu=0,mu=2/3` and weight
`(la,tau,rho,q1,q0,r2,r1,r0)=(4,1,1,22,22,30,30,30)`, the full weighted
initial ideal contains `la^20`; its all-coordinate torus is therefore empty.
This is one exact support/weight cone, not fan coverage or a D1 theorem.

The next D1 work is proof extraction, not another opaque saturation replay:
recover an explicit syzygy/preimage witnessing `la^20`, derive and audit the
coefficient nonvanishing and strict term-order inequalities defining its
Groebner cone, eliminate the associated principal open, then recurse on the
exceptional coefficient faces.  These AWS jobs run provisionally while
review remains asynchronous.  Corrected result/review SHAs are
`fe96e656...` / `3e881064...`.

## Superseding strategy overlay (2026-08-26 01:46Z — TD6)

The V78B/V78C all-q genuine-P12 tensor is independently `CONFIRMED` on the
fixed-A3 generic open.  Dual simultaneous runs and two dual-host 22-shard
unions give one exact table: the reduced P12 sensitivity is supported through
`q14`, while `q16,...,q24` are P12 source syzygies despite nonzero raw
derivatives and 14 live lambda-prime rows.  This replaces serial uncertainty
about the q support with a frozen finite tensor; it does not promote a
nonlinear neighborhood or another current row.

Allocation now moves immediately to the eleven dead-stretch source slots and
to one joint adjoint-symbol/Fitting construction over the 22 q directions,
dead stretch, centers, and licensed source rows.  High-q P12 zeros must not be
discarded from that joint source space: they can act through other rows and
multiplier variation.  V80B dual AWS shards and the next nonlinear compiler
run without waiting on further prose review.  Reconciliation/review SHAs are
`76a0b548...` / `a3599e65...`.

## Superseding strategy overlay (2026-08-26 01:25Z — formal/API hygiene)

The frozen C1 cube formalization at nested commit `d97de48d...` now has an
AWS `lake build`, direct Lean check, and named axiom audit with rc zero and no
`sorryAx`.  This removes build drift for those three exact declarations but
does not change avenue ranking or replace independent mathematical review;
the moving nested checkout remains a separate input.  Formalization should
continue asynchronously behind discovery.

An exact AWS probe also resolves the Singular `sat` return-type ambiguity:
on the fleet version, assignment of the ideal-valued `sat(I,J)` to a list
wraps the *whole* ideal as `L[1]`.  Existing list-wrapper clients therefore
need no global quarantine.  The D1 A-fast/inverse-B disagreement must be
resolved from its actual saturation, localization, or encoding semantics,
not from a nonexistent generator-truncation bug.  Lean result/freeze SHAs are
`fee3d5d2...` / `035d1860...`; API-control SHA is `efbcdcfa...`.

## Superseding strategy overlay (2026-08-26 01:08Z)

The terminal extremal-abc problem now has two complementary exact frontends.
The independently confirmed logarithmic-derivative theorem writes it as a
signed moment configuration; at `U=3` this gives exactly three order-two and
ten order-four affine terminal classes.  A pulled-forward literature sweep
identifies the same objects with Davenport--Zannier weighted bicolored plane
trees.  Pakovich--Zvonkin supply the tree/polynomial correspondence, while
Lu--Song's 2026 prescribed-passport counting algorithm and constructive
permutation model provide an independent census and a practical generator.

The maximum-twelve lane should therefore use a count-certified tree pipeline:
apply the weighted-tree existence inequality to integer passports, compute an
independent exact class count, construct and canonicalize every tree, realize
its Belyi pair over the actual algebraic field, and shard the remaining tails
and Taylor boundaries by tree on AWS.  Unitrees are only the rational subset
and may not be used as an exhaustiveness shortcut.  In parallel, search for a
tree-local leaf-removal, cut, sign, or path identity expressing the first
unresolved lower-tail obstruction uniformly in `U`; enumeration alone cannot
close unbounded `U`.

The confirmed moment theorem/review SHAs are `8e1535aa...` / `91772fd7...`;
the source-linked sweep SHA is `bb1aa29f...`.  The literature connection is a
terminal frontend and current strategy input, not yet a campaign theorem
about any lower tail.  The terminal/lower-tail firewall remains absolute.

## Superseding strategy overlay (2026-08-26 00:37Z)

The nontrivial `(8,12)` terminal layer is now a classified extremal-abc
problem, not an unrestricted rational-map search.  Independent hostile review
confirmed that every order-two or order-four terminal map is balanced and has
passport
`(alpha_i)|(beta_j)|(U-1,1^(D-U+1))`, with
`U-1<=D<=m(U-1)` and exact Kummer order given by the multiplicity gcd.
For `U=2`, the only clients are order two `D=1` and order four `D=1,3`.

Allocation should exploit this immediately.  The loaded order-four
coefficient curve should be normalized and quotiented by the deck `mu4`, then
tested against the two explicit order-four passports, including infinity and
boundary points.  In parallel, larger `U` work should enumerate extremal-abc
monodromy/passport types and compile the remaining six tails directly on each
type; terminal compatibility alone is never a lower-tail certificate.
Producer/review SHAs are `149691f1...` / `b9cf3070...`.

## Superseding strategy overlay (2026-08-26 00:37Z — D1)

The D1 fan correction is independently `CORRECTION_CONFIRMED`, including a
full-source AWS replay of both exact finite successors.  The tied
`alpha=2*beta` solver remains valid on its own chart, but bounded truncation
and raw-weight case splitting cannot certify coverage of the double-root
point.

The next decisive gate is an exact finite-variable tropical initial-ideal
test at each correction ray, beginning with the target-facing
`(vLambda,vTau,vRho;vQ;vR)=(4,1,1;22;30)` residue.  Survival in the full
initial ideal supplies a genuine Puiseux lift criterion; failure exposes the
next exact obstruction.  After that point test, traverse every support mask
and cone with a certified finite fan.  Dual AWS encodings run without waiting
for the tied solver or review.  Firewall/review SHAs are `466736cd...` /
`ec9fe740...`.

## Superseding strategy overlay (2026-08-26 00:37Z — TD6)

The repaired V77R pure-`q3` source-support theorem is independently
`CONFIRMED` on the fixed-A3 generic open.  It proves the exact P12 and
lambda-prime identities but also confirms that the selected first-minor
derivative is zero, so serial one-coordinate rank growth is not the right
abstraction.  The dual simultaneous 22-slot V78B computation and its 44
proof-carrying shards must first agree exactly.  The next finite source block
is the eleven dead-stretch coefficients `d6,...,d16`, followed by one joint
constructible/Fitting gate over centers, all licensed q slots, and that block.
First-order emptiness at the fixed section is not a nonlinear neighborhood
theorem.

## Superseding strategy overlay (2026-08-26 00:16Z)

The D1 double-root problem is no longer a single-ray problem.  Exact negative
controls show that a hand fan based only on the three raw leading weights
misses correction-enabled cancellations from higher coefficients of an
earlier polynomial `QR/K` layer.  In particular, rays in
`3*beta/2<alpha<2*beta` and on the row-three target face
`alpha=15/2, 5<beta<6` survive the advertised first obstruction.

The live toric elimination remains valid only on its preregistered tied
chart `alpha=2*beta`; it runs as one branch and cannot close the double-root
point.  The priority successor is a correction-aware Newton--Puiseux compiler
that carries the images of every earlier rational layer at each weight,
quotients only genuinely tail-invisible polynomial directions, and proves
fan coverage before branch elimination.  The correction-firewall SHA is
`466736cd...`; the correction is independently confirmed by the 00:37Z
overlay above.

## Superseding strategy overlay (2026-08-26 00:01Z)

The independently confirmed terminal-power theorem gives one common exact
interface for all three `(8,12)` Kummer orders.  For orders four and two, the
terminal tail descends to a rational map satisfying
`d(T')^e=(e*j/8)^e*T^(e-1)`; every surviving degree `U>=2` map is
three-value/Belyi-type, while `U=1` is empty.  For order one, the polynomial
core is now restricted to a nonzero constant or a single-root pure power
`c*(x-a)^U`, `U>=2`.

This changes allocation, not the proof status of `(8,12)`.  Passport and
differential-identity enumeration on the nontrivial leaves can proceed in
parallel with the live coefficient/Taylor clients.  The order-one successor
should split the constant core from the normalized one-place core and retain
all nine Faber constants and both Taylor boundary families.  Neither lane may
infer lower-tail compatibility from the terminal equation alone.  Producer
and hostile-review SHAs are `a4d7d6a1...` / `e322d508...`.

## Superseding strategy overlay (2026-08-25 23:56Z)

The broad V77 q3 source quarantine is retracted.  A dual-AWS negative control
proved that the assigned legacy variable `qd.B` is dead after V77 replaces
the live `Q_PRIME`; V77's transport and direct derivative are pure q3, not
q2+q3.  Its raw/remainder q3 identities and dual-unit computation therefore
return to producer tier.  The selected first-minor digest remains exact zero,
so no nonzero-minor or rank-growth claim is restored.  A corrected V77R race
now injects q2 through the actual live `Q_PRIME[1]` slot, while the independent
V78B P12 race continues.  Neither result is promoted before exact dual-host
agreement and hostile review.

## Superseding strategy overlay (2026-08-25 23:28Z)

The formalization lane has a clean exact checkpoint at nested commit
`5f7cc487...`: an AWS build and axiom audit passed for the 45-theorem aligned
`(6,9)` noncube project and the four-theorem max-11 partial-`y` routing
project.  This removes proof-assistant build drift as a campaign risk for
those exact modules.  It does not change avenue ranking: the `(6,9)` result is
one aligned branch, while the max-11 module composes an abstract `Good`
predicate and therefore certifies routing logic rather than the missing
mathematical hypotheses.  New formal work should continue asynchronously
behind theorem discovery and never gate independent research lanes.

## Superseding strategy overlay (2026-08-25 23:20Z)

The active computational frontier is now three small, source-typed boundary
problems rather than broad support discovery.  D1 is localized at its sole
double-root point: dual AWS Newton analysis leaves one nilpotent ray
`alpha=2 beta`, with the next numerator carrying the quadratic
`5L^2+18L+15`; a finite 125-dimensional local quotient is navigation only.
The next D1 gate must incorporate the remaining load/target interactions and
decide formal liftability, not merely recompute that quotient.

For order-two `(8,12)`, the corrected strict compiler now localizes at
`j!=0` and races the same frozen input on Box03 and r6d.  Its V1 predecessor
is permanently quarantined, and the independent divisor-19 theorem is used
only as a logically separate sub-stratum deletion.  For TD6, only the
correctly typed staged simultaneous all-q producer remains live; every V77
q3-specific datum and the failed V78 p12 path are negative controls.  These
three lanes run concurrently on AWS and do not wait on one another's reviews.

## Superseding strategy overlay (2026-08-25 23:16Z)

The first TD6 transverse diagnostic V77 is quarantined at its q3-specific
scope.  Its adapter inadvertently perturbed the imported q2 coefficient as
well as q3, so the reported derivative is a `q2+q3` direction.  Its quoted
first-minor digest is also exactly the serialized zero scalar, not a nonzero
derivative.  Both defects invalidate every pure-q3 rank or remainder
inference.  The V76 theorem on the fixed q2-beta section and V77's
unperturbed base identity are unchanged.

The simultaneous strategy remains correct but its source typing must make
each coefficient slot explicit.  V78's `p12` lane stopped with a sparse-zero
`KeyError` while indexing the absent derivative; that rc1 is a software
negative control, not evidence that the projection is nonzero.  The independently assembled staged
lane uses the corrected vector convention and remains live on AWS.  It is
producer evidence only until every coordinate basis vector, direct q-prime
term, and original-row projection passes a frozen dual-host audit.

## Superseding strategy overlay (2026-08-25 23:13Z)

Strict D1 is now a one-point problem.  The independently reviewed order-20
normal obstruction excludes every squarefree common-cubic centre, uniformly
in slope and fixed loads.  Since the full reduced exceptional support was
already `P(2,3)`, only its unique double-root point `(-3:2)` can support a
nonunit global endpoint.  Producer/review SHAs are `1b6e629a...` /
`6883ef76...`.

Allocation should therefore stop spending compute on generic common cubics.
The primary D1 lane is the completed local ring/normal cone at the normalized
double root, retaining nilpotents and all weight layers from 21 onward.  The
dual global saturations remain useful only as independent endpoint checks or
as sources of the same local component; their duration does not block the
local attack.  A local unit proves strict-D1 exclusion after the reviewed
bounded split; a local nonunit is only a formal survivor until constant-field
rationality and both Taylor boundaries are proved.

## Superseding strategy overlay (2026-08-25 23:10Z)

The whole order-four `(8,12)` stratum with `mu4=0` is now independently
closed.  Davenport--Stothers equality and Shioda's unique order-four Hall
orbit force the terminal coefficient into a nineteenth-power divisor class;
the reviewed exact-differential orders then force the Kummer core itself to
be a fourth power.  Producer/review SHAs are `a5d40fd8...` / `a40827f9...`.
The same invariant removes the all-six-loads-zero point of the finite
order-two `U=2,[6,2]` client.

This reallocates the `(8,12)` lane sharply: order four now starts at the
genuinely loaded wall `mu4!=0`; order two keeps only nonzero lower/tail loads
and must use the corrected `j`-localized strict compiler plus both finite
Taylor boundaries; order one remains separate.  The classical `U<=2`
bounded-pole closure and terminal divisor theorem should be applied before
any solver launch, so AWS effort is reserved for strict or `U>=3` survivors.

## Superseding strategy overlay (2026-08-25 23:06Z)

The first order-two `U=2,[6,2]` strict-saturation launch is a deliberate
negative control, not a result: it omitted the localization `j!=0`, so its
closure necessarily includes the irrelevant terminal-zero common-quartic
component.  Both V1 AWS jobs were stopped before endpoint use.  The corrected
workflow is to saturate by `j` as part of the source open, then by the strict
parameters, cut the boundary, and only then remove the coefficient origin.
V2 will be source-reviewed and dual-raced independently.  Neither a V1 unit
nor nonunit would have had mathematical standing.

This correction changes no Faber tail, Kummer character, terminal ODE,
common-quartic support, or compiled target row.  It is exactly the kind of
compiler/source boundary that background review is meant to catch while the
proof lanes continue.  The Shioda/divisor theorem review and the order-two
client repair proceed concurrently on AWS-only compute.

## Superseding strategy overlay (2026-08-25 23:02Z)

The repaired whole `(8,12)` coefficient-infinity source audit is now
independently `CONFIRMED` (original/erratum/review SHAs `092dfb6d...` /
`aa90155e...` / `f45836ca...`).  It licenses both nontrivial Kummer source
types, their complete loads and terminal row, the exact bounded-pole split,
the `U=1` residue exclusion, and the common-quartic strict exceptional support
`P(2,3,4)`.  The order-two load differential is nonzero away from the square
quartics `c=0,p^2=4r`; even so its rank is at most three, while the order-four
coefficient differential is zero.  First-order transversality is therefore
not an available shortcut in either client.

The operational fork is now exact.  Dual AWS saturations race on the finite
order-two `U=2,[6,2]` client.  In parallel a Shioda/Davenport--Stothers plus
divisor-19 argument is under hostile review for the entire order-four
`mu4=0` branch, and the remaining loaded/order-four and `U>=3` bounded lanes
stay explicit.  A nonunit saturation will be treated only as a survivor
scheme; a unit result must pass normalized dual-host endpoint comparison
before promotion.  No review blocks these computations.

## Superseding strategy overlay (2026-08-25 22:52Z)

TD6 V76 is now independently `CONFIRMED`: the complete fixed, source-typed A3
section with `q=t+beta*t^2+t^25` is empty for every `(C,V,U,beta)`.  The proof
uses the exact scheme cover `D(UHB3) union V(U) union V(H) union V(B3)` and
reviewed original-source theorems on all four leaves.  Producer/review SHAs
are `0281a5fc...` / `7eff6c61...`.  This removes the fixed section from the
live queue but says nothing about its transverse source moduli.

The first transverse q3-gamma experiment finished independently on two AWS
hosts with matching exact invariants.  Gamma changes the reduced first-stage
remainder by three affine monomials; it does not supply a gamma-independent
unit identity.  Thus the campaign should not serialize q3, q4, and later jets
or infer openness from the fixed slice.  The next TD6 gate is one simultaneous
original-row/Fitting or source-syzygy calculation in the finite 32-slot
remaining-jet block, with denominator strata carried explicitly.  That work
continues independently of D1 and `(8,12)` reviews and remains AWS-only.

## Superseding strategy overlay (2026-08-25 22:45Z)

The whole nontrivial `(8,12)` terminal layer is now exact and independently
reviewed.  In the order-four and order-two Kummer branches it reduces to the
linear differential equations `8hA'+6h'A=j` and `8vA'+4v'A=j`.  Local divisor
orders rule out `U=1` and force a root of multiplicity at least five or six;
the first order-two survivor is the single `U=2`, `[6,2]` divisor profile with
`T^2=(x-b)/(x-a)`.  Producer/review SHAs are `1cd824c9...` / `db67f16d...`.

This changes allocation from an unrestricted Kummer search to two parallel
gates: eliminate the finite `U=2`, `[6,2]` client against every lower row, and
use divisor/Stothers constraints to attack the whole order-four branch while
the common-quartic Rees deformation is compiled independently.  The AWS
compiler for the finite client has passed, but a compiler pass is not an
emptiness result.  The general `(8,12)` source audit remains repair-tier only:
its Kummer typing, targets, common-quartic support, and bounded-degree split
survive, while its old §7 load derivatives must be replaced by the frozen
erratum before promotion.

In parallel, D1 continues without waiting for its long global saturations:
the squarefree common-cubic normal obstruction is under hostile review and
the discriminant point is attacked separately.  TD6 has completed the
generic and whole-`B3` fixed-A3 pieces and is reviewing their full union while
the transverse all-source-jet gate is designed.  All substantive algebra,
formal checking, and solver work remains AWS-only.

## Superseding strategy overlay (2026-08-25 22:25Z)

The common-power boundary mechanism is now a general theorem rather than a
single `(9,12)` calculation.  For a depressed monic degree-`m` polynomial
`f` and `g=F_n(f)`, independent hostile review confirms that the first
`m-1` unloaded tails vanish exactly on reduced support

```text
f=K^(m/d),       g=K^(n/d),       d=gcd(m,n),
```

and that this prime locus is `A^(d-1)`, projectively `P(2,...,d)`.  The
producer/review SHAs are `7d5271e8...` / `29bd2184...`.  It unifies the live
common-cubic `(9,12)` and common-quartic `(8,12)` exceptional supports and
prevents further compute from being wasted rediscovering their radicals.
Each passport must still prove that its own weighted loads and lower Faber
terms disappear on the exceptional fibre; neither scheme thickness nor
normal lifting follows from the theorem.

The cyclic-D1 client has therefore advanced to the exact strict-slope gate.
Different-model review `CONFIRMED` the frozen two-parameter Rees compiler and
the implication of its global projective-boundary ideal (design/review SHAs
`bdb369f1...` / `d6e54b5b...`).  Independent Box02 and r6d characteristic-
zero jobs are computing that same gate in isomorphic coordinates.  `H=(1)`
would exclude all strict finite-load coefficient-infinity arcs in D1;
`H!=(1)` would only expose a survivor scheme for deformation analysis.  Work
continues without waiting on that endpoint: one lane derives the normal cone
and discriminant strata of the common-cubic locus, another builds the
independent `(8,12)` common-quartic coefficient-infinity client, and TD6
finishes its fixed-A3 cover before attacking all remaining source jets in one
simultaneous Fitting gate.  Every substantive computation is AWS-only.

This is a sharper finite set of boundary problems, not a resolution.  The
slope-three/finite-coefficient D1 interfaces, common-root scheme thickness,
Taylor/terminal realization, other passports, full TD6 moduli, global
landing/cofinality, and JC2 remain open.

## Superseding strategy overlay (2026-08-25 21:30Z)

The cyclic-D1 route has split exactly at coefficient infinity.  A
different-model hostile review confirms that, after the licensed source shear,
every Taylor-realizable D1 section satisfying
`d_i<=3(9-i)` has ordinary total degrees `(27,36)` and is therefore
counterexample-closed by GGV/Heitmann.  The producer/review SHAs are
`14240376...` / `423d0615...`.  D1 effort must no longer be spent on bounded
coefficient poles or on additional fixed-load finite-field fingerprints; its
only live target is the strict weighted pole sector.

At that boundary, the strongest provisional successor is now structural
rather than computational.  A Mason--Stothers argument identifies the reduced
eight-tail exceptional support with

```text
C=z^3+pz+r,       f=C^3,       g=C^4,
E_red=P(2,3).
```

The proof uses `r1=...=r8=0` to obtain
`deg(g^3-f^4)<=15`, whereas polynomial abc would require degree at least 16
unless `g^3=f^4`.  It also reduces rational ramification denominators to
`n=1` on the generic common-cubic locus, `n|2` on `r=0`, and `n|3` on
`p=0`.  The numerator remains unbounded, so this is not a finite-jet or D1
closure.  Producer/same-model/different-model review SHAs are `de7dd223...` /
`a7bd6275...` / `a274c8d8...`; the hostile verdict is `CONFIRMED`.  Exact AWS work now targets
scheme thickness and normal deformation of these three strata, using a
two-parameter Rees model rather than an unproved single-parameter weight-20
ansatz.  TD6 continues independently through V73 to the staged V74/V75
source-cover chain.  All heavy work remains AWS-only, and no statement here
closes D1, the other passports, global landing, or JC2.

## Superseding strategy overlay (2026-08-25 20:15Z)

The significant-news synthesis is
[`xmodel/ideation-20260825T2015Z-synthesis.md`](xmodel/ideation-20260825T2015Z-synthesis.md),
SHA-256 `74822d77ed2063ae8eeb3feb8ca4ce9fdd9b8bb448864663c5f33de4c6a7806b`.
It consumes the fixed-total-D12 classical closure below, the blind 19:40Z /
19:50Z mini-round, and two completed hostile reviews.

The first new live theorem is branch-wide.  On every actual nontrivial
order-three partial-`y` `(9,12)` trajectory, universal Faber covariance and
the original terminal row imply

```text
T:=r8^3 in C(x),       h=(j^3/27)*T^2/(T')^3,
```

with `T` nonconstant.  The selected terminal divisor proof therefore applies
without selected Q8, `k=mu=0`, or `nu!=0`: after balancing, `T` is a
three-value Belyi map with passport
`(alpha_i<=3)|(beta_j)|(e,1^(D-e))`, `r+s=e+1`, and
`deg h=3(e+1)`.  Producer/review SHAs are `28f33b99...` / `5da99825...`;
the hostile verdict is `CONFIRMED`.  This is a necessary classification;
cyclic `D=1,2` rows remain terminal positive controls.

The raw `21 x 23` Ore restart and standalone FT2/high-row proposal are
deduplicated.  The reviewed Faber theorem already integrates all high rows
with unbounded coefficient-`x` degree, and the strict-D12 squarefree
recurrence through rows 18--12 is now independently `CONFIRMED` only as a
reduced-point/UFD method control.  The first genuinely new gate must combine
the complete lower fibre

```text
r1=r2=r4=r5=r7=0,       r3=mu,       r6=nu,
```

with `r8`, the original terminal row, and both complete true-center Taylor
families.  It starts in parallel on the unhandled `k!=0` component chart and
on exact Kummer valuation/initial systems for the cyclic passports.  A mere
dimension, another Laurent coefficient, or agreement at sampled total-degree
caps is not a pass.

The next-cycle scheduling target is **50% unbounded order-three lower
fibre/Taylor/passport, 25% TD6 source-complete cover, 15% global
landing/cofinality, and 10% genuinely unbounded AS/support-growth transfer
and certificates**.  Selected-Q8 landing and Double-B membership lie inside
the first allocation and remain live; fixed-D12 theorem search receives zero.
All heavy work is AWS-only and every new lane must record
`--partial-y-degrees 9 12 --total-unbounded` through the frontier gate.

This still leaves the order-one `(9,12)` leaf, both `(8,12)` nontrivial
leaves, power-core strata, and the global landing/cofinality wall.  No item
here proves or disproves JC2.

## Superseding strategy overlay (2026-08-25 19:44Z)

The primary-source-checked GGV/Heitmann theorem requires
`gcd(deg_total(P),deg_total(Q)) >= 16` for every characteristic-zero
counterexample.  An independent hostile audit confirms the immediate routing
consequence: **every** coefficient envelope with both total degrees at most 12
is already counterexample-closed, including the broad fixed-D12 B9/B8 cells,
all their degree-drop strata, and the exact `(9,12)` / `(8,12)` subfamilies.
See
[`xmodel/sol-fixed-total-d12-classical-closure-v2-20260825.md`](xmodel/sol-fixed-total-d12-classical-closure-v2-20260825.md)
(SHA `def1c2a1...`) and its `CONFIRMED` review
[`xmodel/sol-fixed-total-d12-classical-closure-review-as-20260825.md`](xmodel/sol-fixed-total-d12-classical-closure-review-as-20260825.md)
(SHA `511d8a55...`).

This supersedes every allocation or frontier statement below that treats a
finite D12 family as a live counterexample search.  Completed B9/B8
Kuranishi, residue-tower, and binary-cubic recurrence artifacts remain useful
method controls and may imply a non-effective finite death depth for a single
complete nested collision scheme; they do not advance a still-open D12
counterexample frontier.  The only live B9/B8 transfer retains partial-`y`
degrees `(9,12)` or `(8,12)` while allowing unbounded coefficient-`x` and
total degrees.  Such a transfer must rebuild its filtration and centralizer
arguments: the squarefree binary-cubic rational centralizer cannot simply be
relabelled when a partial-`y` leading core is composite.  TD6 and global
landing/cofinality routes are unchanged.

Before new compute, `ops/frontier_gate.py` records the honest degree notion
and refuses classically closed frontier lanes; a closed family may run only as
a cost-capped method control with a named live client.  Significant-news
whole-portfolio reconciliation is now replacing the stale 17:25Z allocation.
No statement here closes the unbounded-total partial-`y` theorem or resolves
JC2.

## Superseding strategy overlay (2026-08-25 17:25Z)

The latest sealed whole-portfolio decision record is
[`xmodel/ideation-20260825T1700Z-synthesis.md`](xmodel/ideation-20260825T1700Z-synthesis.md)
(SHA-256
`a1cd2be8eb99e4ef19338c60d3d3d7721905afcb5268c8f68919ca2df20d74e2`).
Its post-cutoff common-cubic solver paragraph is corrected by
[`xmodel/ideation-20260825T1700Z-synthesis-erratum.md`](xmodel/ideation-20260825T1700Z-synthesis-erratum.md),
SHA-256
`5dfca997914867c122de218c56660dcb9d2b3b01b4ae03030216d9da5ac14b17`.
The corrected finite SAT micro-round is
[`xmodel/ideation-20260825T1700Z-synthesis-postscript-common-cubic-sat.md`](xmodel/ideation-20260825T1700Z-synthesis-postscript-common-cubic-sat.md),
SHA-256
`8acce4e9502aa93f7d766befadabf82b5e33a2b3815554cdd770ae330d6dc265`.
Four independent blind all-46 scans converge on a **core-first** fork:
intersect each complete finite D12 family with its necessary proportional or
common-power leading-form scheme, then apply exact Smith/Lyapunov--Schmidt,
Neron-dilatation/Fitting, or quantitative Hensel analysis to the surviving
scheme.  Stop treating selected digit strings as the arbiter.  The temporary
allocation is **AS/D12 40% / maximum-12 35% (25% fixed-cap order one, 10%
general Q8) / TD6 20% / global 5%**.

- **B9 broad family.**  Different-model review confirms the complete linear
  window through `3^10` and the first nonlinear transition to `3^11` over one
  pinned mod-243 parent.  The latter has `3^109` liftable predecessor digit
  tuples and `3^183` complete next-stage tuples; this is a finite staged
  count, not an affine/smooth/all-depth scheme.  Its displayed `(12,12)`
  witness fails proportionality of the top faces, so only the complete
  family intersection `P_12=cQ_12` remains live.
- **B9 normalized family.**  A complete producer-exact `(P<=9,Q<=12)` first
  nonlinear gate has `3^101` predecessor projections and `3^162` complete
  mod-`3^11` digit tuples, with an honest `(9,12)` witness; different-model
  review now confirms this strict finite-family statement.  The first emitted
  common-cubic formula is **quarantined**: its
  bit-vector helper reduced the modulus constant modulo itself, so bounds and
  divisors used zero and the instant dual-solver `UNSAT` was a trivial
  encoding contradiction.  It proves no family emptiness.  A corrected
  source-independent AWS compiler and different-model review now confirm SAT
  for the intended `P_9=aH^3,Q_12=bH^4` gate: a complete displayed finite
  parameterization of `3^150` mod-`3^11` tuples and a literal witness replaying
  all 276 determinant plus 23 top-core rows.  This is not a scheme dimension
  or an all-depth point.  Separately, different-model review confirms the determinant-only
  exact rational core: rank `142`, right kernel 4, left cokernel 134, and a
  genuinely nonzero 134-row quadratic compatibility system.  It is the next
  exact object, not an emptiness theorem.  At the literal common-cubic witness,
  different-model review confirms core-inclusive rational rank 146 with
  exactly three target-gauge kernel directions, mod-3 rank 94, determinantal
  valuation 205, and maximum Smith valuation 13.  The classical minor test
  fails without deciding liftability.  Different-model review confirms that
  the displayed point itself dies at `3^12` by singleton determinant carry
  `[x^2 y^2]`; that carry varies on the family, so the complete 205-cokernel
  next-digit map over all 150 parameters is the live AWS arbiter.
- **Fixed-cap maximum 12.**  Total degree at most 12 forces the partial-`y`
  routing factor `h` to be constant in both B9 and B8.  These seeds therefore
  lie on the order-one polynomial core, not selected order-three Q8 or B8
  order-two/order-four leaves.  The varying total-homogeneous binary
  cubic/quartic is a separate necessary constraint.  Different-model review
  confirms this corrected conditional interface and the selected-Q8
  `SCOPE-CONFLICT`; selected Q8 remains part of the general unbounded-total
  theorem only.  The denominator-free binary-cubic first-two-band computation
  is review-confirmed on all three root types; its nonreduced obstruction
  schemes have dimensions `7,6,6`, so none is excluded there.  Complete
  degree-16/15 incidence continues on AWS without waiting for review.
- **B8/TD6.**  Both the explicit B8 mod-27 `(8,12)` point and the complete
  fixed-D12 W2/W3 scheme are review-confirmed: the latter is the reduced
  predecessor `A^27` with constant fresh fibre `A^68`, hence `A^95` at W3.
  This is finite-depth, not an inverse limit.  TD6 V70 is review-confirmed
  and repairs whole raw `U=0` only inside the fixed
  source-typed A3 q2-beta section.  V65's separate P3/QH localized
  source-DAG theorems are also review-confirmed.  Their five-piece V71
  composition is now review-confirmed and excludes the entire `H=0` divisor
  in that fixed section only.  V66 is review-confirmed on the generic B3
  chart after inverting its five exact factors, and V67 is review-confirmed
  on `D(w)` of both finite factors.  Boundary routing plus V70 leaves the
  original-source staged-N13 repair on `V=0,C=-5U^2,D(U)` as the precise
  whole-B3 debt.  Rational-lines dependency erratum SHA `9cdcba8c...`
  replaces the stale V33 endpoint citation with reviewed V70 without
  pretending that endpoint repair supplies the missing staged N13 lift.

Reviews run in the background while source-valid research advances.  All
CAS, solver, exact-Python algebra, Lean, and other heavy work is AWS-only;
the Mac is limited to editing, hashes, SSH/status, and lightweight cloud
review.  The next blind-round backstop is `2026-08-26T05:00Z` and the web
sweep remains due `2026-08-26T01:26Z`.  No item here proves or disproves JC2.

## Superseding strategy overlay (2026-08-25 16:45Z)

The latest completed significant-news whole-portfolio decision record is
[`xmodel/ideation-20260825T1550Z-synthesis.md`](xmodel/ideation-20260825T1550Z-synthesis.md)
(SHA-256
`0a96577f557c7fe4f4dadc3bc2ecd74d08e9d9927630b511fa3e8461b4141e91`).
Four blind all-46 scans select the temporary scheduling allocation
**AS/D12 40% / Q8 35% / TD6 20% / global 5%**.  This changes execution, not
theorem status.

- **AS/max-12 disproof pincer.**  Different-model review confirms two exact
  noninjective `F_3` residue maps with unit leading faces at actual degree
  pairs `(9,12)` and oriented `(8,12)`.  They are tame source
  right-compositions/transforms of the Artin--Schreier seed, not group
  conjugates; the `(8,12)` map also uses a determinant-one target
  orientation.  The B9 branch is reviewed through `Z/243`; its complete
  fixed-D12 family then survives producer-exactly through the linear window
  modulo `3^10=59049`.  The current family dimension is 165 in staged
  coordinates.  Since the correction begins at order `243`, its quadratic
  self-term first enters the transition to modulus `3^11=177147`; that full
  family gate is the critical disproof experiment.  B8 independently has a
  producer-exact degree-compatible `Z/27` point from a complete 172-column
  gate; hostile review is live.  Finite survival at any displayed depth is
  not an all-depth point, characteristic-zero map, counterexample, max-12
  theorem, or JC2 result.
- **Q8/max-12 proof interface.**  The new B9 object lies on the integral
  monomial-core/bad-reduction boundary, not the licensed selected order-three
  Kummer open.  Keep the live moving-centre, finite, original-J, and
  coefficient-infinity jobs, while auditing the denominator-cleared original
  rows along `3=p=0`.  Require a horizontal source map or record `NO HIT`;
  shared `(9,12)` numerology is not landing.
- **D7 calibration.**  The complete three-fibre D7 output cones modulo 729
  are now different-model `CONFIRMED`, with final rank/kernel sequences
  `27/45 -> 36/81 -> 47/106` and
  `27/45 -> 43/74 -> 49/97`.  At modulus 2187 the quadratic term survives;
  do not write a fourth global linear Bockstein.  This lane is a mechanism
  donor and explicit finite-death calibration under the reviewed low-`y`
  escape theorem.
- **TD6 correction.**  Hostile review found that the V33/V69 raw-`U=0`
  transport uses two `-C` pivots.  It proves only `U=0,D(C)`, not whole
  `U=0`; `C=U=0` is a separate unrun raw stratum.  Every provisional
  whole-`U=0`, whole-`H=0`, or whole fixed-A3 composition is quarantined
  pending an original-source rebuild.  There is no direct B8/B9-to-TD6
  interface: residue function-field degree, pole source, and p=3 integrality
  all disagree.

Run fast point branches only as finders and complete affine/Fitting families
as arbiters.  Reviews remain asynchronous.  All CAS, exact-Python algebra,
SAT, and Lean builds are AWS-only; the Mac is restricted to editing, hashes,
orchestration, status, and lightweight cloud review.  The next full blind
round is due by `2026-08-26T04:15Z` absent an earlier trigger; the web sweep
remains due by `2026-08-26T01:26Z`.  No item here proves or disproves JC2.

## Superseding strategy overlay (2026-08-25 14:53Z)

The latest completed whole-portfolio decision record is
[`xmodel/ideation-20260825T1440Z-synthesis.md`](xmodel/ideation-20260825T1440Z-synthesis.md)
(SHA-256
`ae92ae4dc7b8612847e2e578948765a0f9e2f4d0d6700f6b63c76f84e4634628`).
Four blind all-46 scans and three post-collection attacks select **Q8 40% /
AS 30% / TD6 25% / global 5%**.  This is scheduling, not theorem evidence.

- **Q8:** finish the exact finite exceptional-`c`, moving-`d4`, and
  source-horizontal projective/infinity cover.  Compare saturation then
  homogenization with homogenization then saturation, retain every source
  row/localizer, and add joint charts for coordinates that scale with `c`.
  A fresh different-model-`CONFIRMED` theorem now proves
  `std(C+(w,u,x1,x3,x5))=(1)` for
  `C=I:(w*x5*(x3-2*x5))^infinity` over polynomial `c`, only at the frozen slice
  `d4=1,d2=2+u` landing centre (producer/review SHAs `46a955fa...` /
  `f5a98513...`).  It does not prove `C=(1)` or global finite-`c` emptiness;
  moving `d4`, full `Hsrc`, and `c=infinity` remain the Q8 critical path.
  Only source-horizontal survivors receive terminal and Taylor tests.
- **AS:** `zero-partial` means `omega` is a surjective affine coordinate, not
  a source-zero class.  Before treating the fresh `27U+81V` equations as one
  global `Z/9` module, compile every mixed Q3-kernel/fresh term: an order-nine
  predecessor direction can leave `(9K)(27U)/243=KU`.  Use a named base
  algebra and Fitting strata if any mixed block survives, and in all cases
  intersect `omega=0` with every downstream Q8--Q3 restoration row.  A new
  different-model-`CONFIRMED` bridge (producer/review SHAs `5b089614...` /
  `c8c12043...`) proves non-effectively that every fixed finite AS support
  with `y`-exponents at most eleven dies at some finite 3-adic depth.  Thus
  D7 computation is now an explicit-certificate/support-growth calibration,
  not a possible all-depth branch; counterexample-relevant AS work must
  eventually leave every such finite envelope or reach `y`-degree at least
  twelve.  Keep the current bounded next-depth gate because its obstruction
  mechanism may quantify that escape, but stop indefinite digit chasing
  after a bounded non-informative pass.
- **TD6:** complete the exact five-piece `H=0` source cover.  V64 gives the
  generic open, and reviewed V62D (review SHA `ec0ada3d...`) now closes
  `V=0,D(U)` for all `beta` by a direct source-unit certificate.  The
  remaining leaves are `P3=0,D(U)`, `QH=0,D(U)`, and raw-source `U=0`.
  The normalized Bezout identity proves the two polynomial divisors
  disjoint, not empty.  Stop pivot proliferation.
- **Shared accelerator/global firewall:** use one source-hashed
  constructible-cover/Fitting compiler with cofactors, proof DAGs,
  denominator radicals, saturation/contraction checks, projective charts,
  and exact cover semantics.  Positive genus needs honest component landing
  and a degree-one/birational map; Greenberg needs a complete integral scheme
  and an effective bound.  Complete landing and a cofinal/absolute complexity
  ceiling remain the proof-wide wall.  The theorem-interface composition
  check found no direct p-adic counterexample chart in Q8 or TD6: Q8 already
  uses nonunit `1/3,1/9` Kummer/depression normalizations in residue
  characteristic three, while TD6 has `3H^3=1` and normalized roots of
  valuation `-1/6`.  Neither frozen jet scheme reconstructs an integral
  global map or preserves an AS collision.  Any reuse requires an explicit
  undo-normalization/integral-descent theorem; this is a scope conflict, not
  a negative theorem about the normalized proof lanes.

All sustained or uncertain computation is AWS-only.  Reviews remain
asynchronous and scope-matched.  The next full ideation round is due by
`2026-08-26T02:53Z`; the broad web sweep remains due by
`2026-08-26T01:26Z`.  No item here resolves JC2.

## Superseding live overlay (reviewed through 2026-08-25 14:17Z)

The 11:40Z whole-portfolio allocation remains Q8 45%, AS 30%, TD6 20%, and
global reserve 5%; the following events sharpen execution without changing
that ranking.

- **Q8 horizontal landing.**  The repaired finite-interior theorem has now
  cleared its second hostile review with no mathematical defect in scope.
  Independently, an exact two-engine local-ring certificate excludes the
  entire finite `x5=0,x3!=0` cylinder from the closure of the selected open;
  hostile review SHA `6a550061...` returned `CONFIRMED` with only expository
  nits.
  At the raw unloaded overlap, the exact Fitting/tangent theorem is now
  different-model `CONFIRMED` (producer SHAs `fca71aaa...` / `e421aec0...`;
  custody-cleaned review pair `0c29a5c1...` / `44985424...`).  The doubled
  normal-rank-drop scheme is `((d2-d4-1)^2)`, and the exact-rank ordinary
  `delta w=1` incidence closures are `K3=(d2,d4)` and `K2=K1=K0=(1)`.
  Thus the only ordinary tangent locus is the `c`-free line `d2=d4=0`, where
  the normal tangent remains in the boundary sheet; `(4,2)` independently
  fails by `729*L7=-432`.  This is not an arc theorem: ramified/weighted
  rank-drop arcs (including through `(2,1)`), exact selected horizontal
  saturation, coefficient infinity, and Taylor/terminal realization remain
  open and are the finite-boundary critical path.
- **AS fixed-support lifting.**  The three full chronological Q4 fibres are
  now Grok-`CONFIRMED`.  Their complete affine kernels all admit exact Q3
  lifts, also Grok-`CONFIRMED`; the Q4-kernel columns vanish at this gate and
  the same ten-column `(H4,J4)` operator controls all three parents.  Literal
  valuations prove that degrees one and two still need earlier order-27
  source pieces.  The combined successor is genuinely nonlinear in canonical
  RREF coordinates, but raw-coordinate branching gives exactly 27 accepted
  first carries per fixed point for the degree-three/two parent.  Its exact
  successor adds every displayed homogeneous degree-one order-27/order-81
  output digit.  Different-model review SHA `922727b3...` now `CONFIRMED`
  that, on the entire displayed Q4/Q3 affine fibre over each of the three
  pinned Q5 predecessor **points**, the `/27` system has rank six on ten
  active coordinates and 81 accepted assignments, while all `243` `/81`
  branches are inconsistent at row 8=`x^2y` with residuals `2,2,1`.
  Degree-one columns are active at both layers and change non-row-8 equations
  and ranks, but never hit row 8.  The review nevertheless rejects the
  **source-complete** reading: degree-at-least-four order-27 digits, source
  reparametrizations, other carry directions, and the rest of the predecessor
  scheme were not compiled.  Do not count this as complete Q3-fibre
  exclusion, exclusion of three structural bases, global normalized-chart
  coverage, or a `79 -> 76` reduction.  The separate global Q9 projection is
  now different-model `CONFIRMED` (producer/review SHAs `2518c72b...` /
  `903d1181...`): over all 79 compatible structural bases of the corrected
  current Q10 source, all 11,881 nonempty Q9 affine fibres are `zero-partial`
  for
  `omega=carry(c2_1+2*d2_0)+2*h*carry(2*c2_2+d2_1) (mod 3)`.  Its exact
  completion histogram is uniform, with
  `2,698,785,475,281` occurrences of each value.  Hence this scalar kills no
  Q9 fibre and should be quotient-coordinated before the next obstruction;
  no Q8-through-Q3 restoration, complete map modulo 243, all-depth lift,
  counterexample, or JC2 claim follows.  The complete fixed-support
  output-digit influence cone has now been built and fresh-hostile-review
  `CONFIRMED` at the three pinned Q3 fibres: all 72 `Z/9` coordinates in
  `F=F_*+27U+81V` are present, all 91 determinant rows vanish modulo 243,
  and the exact mod-3/Bockstein rank/kernel pairs are
  `27/45 -> 36/81,43/74,43/74` (producer/review SHAs `9ac1edbb...` /
  `2322e0bc...`).  Thus those three full cones survive modulo 243 and the
  earlier displayed degree-at-most-three exclusions do not close them.  This
  remains three-fibre evidence only: terminal modulus 729, order-243 digits,
  the rest of the predecessor scheme, and all-depth survival are open.  The
  live successor computes the exact coefficient-scheme Jacobian/SNF and
  quadratic Kuranishi obstruction while composing the
  `omega=0` slice with the actual Q8/Q7/Q6/Q5/Q4/Q3 restoration state before
  full 63-row Fitting strata.  Once a
  complete finite precision lands, run full coefficient-scheme
  Jacobian/Fitting and singular-Hensel tests in parallel with chronological
  continuation and low-height rational reconstruction.
- **TD6 source DAG.**  Frozen V64 and its hostile review (SHA
  `e78af060...`, verdict `CONFIRMED`) now give an exact original-row
  `N13`/genuine-`P12` composition on the fixed source-typed A3 q2-beta
  section at `H=0,D(U*V*P3*QH)`.  The corrected ancestry is current row 13
  through previous row `('X-1',14)` plus first rows; `('X-1',0)` is only a
  separate quadratic control.  P12 counts are `2885/28/1640`, the residual
  is `-k/50`, and the denominator radical is exactly `{U,V,P3,QH}`.  This is
  a localized theorem, not an `H=0` cover: direct source theorems on `U=0`,
  `V=0`, `P3=0`, and `QH=0` must all compose first.  Preserve the review's
  custody/refusal nits: partial top source-manifest inventory despite a live
  parent hash pin, prose-only host IP, cache-first DAG display, marker-only
  lightweight verifier, and no proof that QH is removable.
  Frozen V62D is separately hostile-review `CONFIRMED` (SHA `ec0ada3d...`)
  on `V=H=0,D(U)` for every `beta`: an exact original-row unit certificate
  has denominator radical `{U}` and closes the `V=0,D(U)` leaf.  Its source
  rows are hash-pinned rather than embedded.  Raw `U=0`, `P3=0,D(U)`, and
  `QH=0,D(U)` remain charged, so no whole-H conclusion is licensed.

All substantive computation remains AWS-only.  No event here resolves JC2.

## Superseding live overlay (reviewed through 2026-08-25 11:49Z)

The latest completed whole-portfolio decision record is
[`xmodel/ideation-20260825T1120Z-synthesis.md`](xmodel/ideation-20260825T1120Z-synthesis.md)
(SHA-256
`246281fa867a8e6f015f020af43b972152e30edccff50ddc7b77d39bba914398`).
Four blind all-46 scans and three post-collection adversarial scans agree on
the following allocation; consensus is a scheduling signal, not proof.

- **Q8 horizontal landing complement — 45%.**  The finite affine raw special
  fibre on `D(x5*(x3-2*x5))` is now exactly the reduced length-eight
  `Spec(Q[v]/Q8)`, with `r6` and the relative determinant units and no non-Q8
  point.  Different-model review preserved the theorem with finite custody
  repairs.  The load-bearing remaining distinction is between raw
  specialization and the actual punctured source's horizontal closure:
  compute `I:(w*x5*(x3-2*x5))^infinity` first, then take its finite and
  relative-projective `w=0` boundary.  In parallel retain
  `(I+(x5*(x3-2*x5))):w^infinity` as the horizontal boundary incidence; this
  second object is not, without decomposition, a union of irreducible source
  components.  Attach `r6/r8`, terminal descent, and both true-centre Taylor
  families only to source-horizontal survivors.  Stop further interior point
  counts and raw-special-fibre/projective proliferation.
- **AS whole-Q5 Cartier/Fitting — 30%.**  Collision is automatic only after a
  complete fixed-support all-depth lift, by the reviewed residue-ball theorem.
  The current exact gate is the Q4 `x^2y^2` Cartier section over the entire
  source-complete Q5 predecessor scheme, followed by all remaining Q4 rows
  and chronological Q3--Q0 restoration on its zero strata.  Three structurally
  distinct solver models now pass exact integer replay of all 197 parent rows
  and this Cartier coordinate, including a second state over base 513; hence
  the pinned base-513 obstruction was pointwise.  All three complete
  68-by-12 affine systems now have equal rank/augmented rank and explicit
  `H5,J5` solutions; exact integer replay passes all five Q4, all 63
  recomputed terminal, and all 197 parent rows.  They are chronological Q4
  survivors at producer tier, pending hostile review.  Q3 must quantify over
  each full 8- or 4-dimensional Q4 solution kernel rather than one
  particular.  Another 23 solver UNSAT endpoints remain diagnostic.
  Global affine-matrix/Fitting stratification outranks duplicate solver seeds.
- **TD6 source-DAG/denominator cover — 20%.**  Seven of 40 current rows are
  source-lifted and 33 remain.  V57 must close the original-row dependency DAG
  before any q3 successor.  V54D's exact support `{U,H}` is diagnostic only:
  neither a nonunit pivot product nor a list of support factors is a chart
  cover.  Promotion still requires a denominator-free identity or explicit
  cleared local identities plus replayed Bezout/Cech coverage.
- **Global reserve — 5%.**  Preserve work on `G2-PSC`, every invoked
  `G2-BD`, full landing/coverage, and an absolute or cofinal complexity
  ceiling.  No finite selected max12 or TD6 family closes that global wall.

All sustained or uncertain computation is AWS-only.  The next blind
whole-portfolio round is due by `2026-08-25T23:40Z`; the broad web sweep is
due by `2026-08-26T01:26Z`.  No item here resolves JC2.

## Superseding live overlay (reviewed through 2026-08-25 10:57Z)

This overlay supersedes the allocations and clocks in the older same-day live
overlays below without mutating their historical record.  The latest completed
whole-portfolio decision record is
[`xmodel/ideation-20260825T0948Z-synthesis.md`](xmodel/ideation-20260825T0948Z-synthesis.md)
(SHA-256
`747830c073210174909773f7dde262e6692f9c4c06a0fd1aa81e16afe2f4c6ce`).
Four blind scans and cross-pollination converged on Q8 positive-genus
composition, complete AS fibre restoration, and TD6 source/denominator
closure.  Two post-cutoff events sharpen that ranking.

- **Selected-Q8 contact exclusion is now a reviewed theorem.**  The pinned
  geometrically integral `H/F_127` has positive genus by the exact inequality
  `16,168>16,130`; independent python-flint and pure-Singular AWS
  implementations agree.  The repaired whole-source attachment and
  proper-divisorial/ruled-residue argument excludes an actual order-three
  trajectory on a selected `k=mu=0,nu!=0` component meeting a corrected-Q8
  contact, and reviewed primitivity handles the all-eight/eight-singleton
  alternatives.  The remaining proof-side debt is **coverage/landing beyond
  this strict contact leaf**, not more point counting or generic degree one.
  Projective surjectivity only forces some `w=0` preimage; it does not force a
  corrected-Q8 point because the localized boundary fibre has other points
  and components.  The smallest honest gate is exact saturation/stratification
  of the six-row `w=0` fibre under
  `x5*(x3-2*x5)!=0`, followed by Galois-orbit and Taylor/terminal provenance
  for every non-Q8 stratum.
- **AS collision bookkeeping is conditionally eliminated.**  A reviewed
  residue-ball theorem says that any complete fixed-support all-depth lift of
  `(x-x^3,y)` already has moving unit-separated `3`-adic collisions and hence
  transfers to a complex counterexample.  It cannot consume filtered states.
  The base-513 Q5 witness satisfies all 197 displayed constraints but owes
  Q4--Q0; its displayed Q4 child is exactly obstructed by the constant
  `x^2y^2` divided-determinant coordinate.  The highest-value next experiment
  is to express that Cartier coordinate over the **entire Q5 fibre**, split
  its zero locus if necessary, and continue complete restoration there.
- **TD6 remains a fail-closed third front.**  V55 proves exhaustive coverage
  of the 54 previous rows only.  V56b audits 40 current rows one per AWS
  shard; V57 replaces a timed-out monolithic expansion by an exact proof DAG;
  V54D repairs only reporter serialization in the reverse-pivot lane.  No
  whole-center or whole-atlas conclusion exists until current-row identity,
  dependency closure, and denominator cover compose.
- **Software priority:** make the noninjective-residual-etale oracle reusable;
  compile complete fixed-support transition DAGs with source reconstruction;
  globalize sparse Cartier/adjoint obstructions over constructible rank
  strata; and retain proof-DAG rather than expanded-polynomial certificates
  when exact transitivity is cheaper.  All sustained computation is AWS-only.

The next blind whole-portfolio round is due by `2026-08-25T22:04Z`; the broad
web sweep remains due by `2026-08-26T01:26Z`.  No item here resolves JC2.

## Latest significant-news overlay (reviewed through 2026-08-25 08:53Z)

The `0427Z` full-spectrum ranking remains stable, but three later gates change
the shortest experiments.  This is a micro-round delta, not a replacement for
the next blind whole-portfolio round due by `16:55Z`.

- **Selected Q8 has the shortest proof-side bypass.**  A reviewed residual
  correction puts at least one of the rational contacts `26,58,67` on an
  `H`-supported mod-127 source component using the honest bound
  `40,960>37,010`; the retired `35,582` constant may not be used.  A
  different-model-confirmed arithmetic bridge attaches each reviewed
  characteristic-zero selected contact branch to its full mod-127 point with
  completed local ring `R[[w]]`.  Therefore an exact certificate
  `g(H)>0`, together with the proper-specialization audit now under hostile
  review, can exclude trajectories in both the all-eight and eight-singleton
  primitive alternatives without waiting for generic source degree one.
  AWS runs attack genus by normalization, branch-value counting, and
  independent term orders; the 126-fibre coordinate-graph fanout has passed
  and exact interpolation/modulo-`H` substitution remains a parallel route.
- **AS remains the strongest counterexample-side falsifier.**  Three exact
  terminal-gate SAT states show that the encoded finite gate is nonempty, but
  their first source-complete successors all die pointwise: two at reimposed
  final G8 and one by a sparse Q6/H7,J7 left-null obstruction.  The decisive
  AWS gate now globalizes final-G8/Q6 over alternate accepted states.  The
  reusable mechanism is a restoration-curvature compiler that automatically
  reimposes earlier rows after every later restoration and extracts the
  quotient adjoint/Fitting class; a filtered terminal survivor is never again
  treated as chronological without this closure pass.
- **TD6 remains third and fail-closed at the denominator boundary.**  Earlier
  all-`beta` wording is quarantined.  The duplicated V50 full arbitrary-degree
  original-row lift, independent reverse/deferred-pivot V52, and latency-only
  V53 all run on AWS.  Only a denominator-free identity or an explicit
  cleared-cover/Bezout glue may restore a whole-center claim.
- **Shared software priority:** source-hashed restoration closure, sparse
  adjoint/Fitting extraction, content-addressed AWS dispatch, and automatic
  tainting of descendants now outrank additional unstructured breadth.  These
  instruments apply to AS, TD6, selected Q8, GGV/Sigray reconstruction, and
  common-cubic lower rows.

No item in this overlay proves or disproves JC2.

## Current event synthesis overlay (reviewed through 2026-08-25 05:12Z)

The latest completed all-avenue decision record is
[`xmodel/ideation-20260825T0427Z-synthesis.md`](xmodel/ideation-20260825T0427Z-synthesis.md)
(SHA-256 `239791e44f77eaeeffebf3ecb929d471f8a0b62c5d49ebd610a4d67da16fe22a`).
All four active agents scanned all 46 avenues.  The cube/max12 submission is
narrowly degraded because it accidentally saw Q8/root headings; those Q8
votes receive zero independent credit.  Its AS right-congruence and TD6
valuative-escape proposals remained blind.  Cross-pollination attacked the
merged cards before allocation.  No ideation result proves or disproves JC2.

- **AS canonical right congruence is the strongest counterexample-side
  discriminator.**  The first exact `3^13` Q9 zero-section census correctly
  failed closed in 48 deterministic AWS shards: some canonical Q8 affine
  particulars have Q7 augmented-rank escape, contrary to its preregistered
  premise.  Its partial records are preserved but not aggregated.  The V3
  census classifies compatible and incompatible zero sections, retains their
  canonical integer-carry/source/RREF signatures, and sends only honest
  representatives to the full 19D fibres.  The next gate is a same-signature/
  different-successor witness or exact partition refinement.  A recurrent state
  matters only with a deterministic phase-aware transition section, fixed
  support, and a preserved nonzero collision coordinate; only then would
  eventually periodic ternary digits give coefficients in
  `Q intersect Z_3`.
- **Selected-Q8 remains the fastest bounded proof experiment.**  Moving-`v`
  Hensel orders 16 and 32 are frozen PASS at formal-local mod-127 scope;
  orders 64, 128, and 256 run on AWS.  A simultaneous 1,330-sequence test
  found no unique scalar common denominator in the complete order-16/32
  windows `d<=31,m<=30`.  This is a bounded negative, not component evidence.
  The acceptance gate remains reconstruction of all seven coordinates and
  exact substitution in every original localized row modulo `H`, followed by
  full contact limits and full-source Jacobians.  A completed contact
  idempotent is only a local projector; in a rank-190 algebra a trace-190
  idempotent may be the identity and proves no component.
- **The draft Q8 no-merger lemma is not promoted.**  The coordinator found an
  explicit counterexample to its wording when a higher-dimensional vertical
  special-fibre component passes through a marked point.  The intended
  application is repairable by requiring the candidate curve to be the unique
  special-fibre component through each full contact, which a unit full-source
  internal Jacobian is intended to certify.  Hostile review is active; no
  characteristic-zero component inference is licensed meanwhile.
- **TD6 changes from affine slices to a transverse exact gate.**  Conditional
  on hostile confirmation of the fixed source-typed `A^3_(C,V,U)` union, any
  valuation-ring point in the common beta presentation would reduce to its
  empty special fibre.  Hence every survivor has a negative affine-coordinate
  valuation or leaves the normalization chart.  Before building the Rees
  boundary fan, the cheapest discriminator is the reviewed support-one
  fixed-center compatibility `N13=(k/25)beta`: its arbitrary-center
  original-row lift is running on AWS.  If its coefficient is a unit on a
  complete raw A3 cover, it forces `beta=0` even for pole-valued families and
  composes directly with fixed-A3 emptiness.  Quotient-field unit claims must
  rebuild all numerator/denominator divisors.
- **Software and allocation follow the three exact gates.**  Use proof-carrying
  finite-algebra/state/valuation records, direct source replay, degree-bound
  ledgers, exact saturation, and automatic taint.  Approximate reasoning share
  is 35% Q8, 30% AS, 30% TD6, and 5% landing/cofinality reserve.  Rows
  `2,4,19,25,26,31` are raised as named clients; row 21 is narrow to canonical
  periodic carries and row 38 narrow to the source-derived TD6 Rees fan.
- **All substantive computation remains AWS-only.**  The seven-node fleet
  uses the 512-vCPU quota.  A live audit retired only hash-preserved,
  superseded workers and launched the new Q8/AS/TD6 discriminators into real
  headroom.  The laptop is limited to editing, hashing, orchestration, status,
  cloud review, and short low-memory checks.

The ultimate proof walls remain full landing/coverage and an absolute or
cofinal complexity bound.  The disproof wall remains a bounded all-depth
state plus characteristic-zero polynomial algebraization with collision.
The next completed full scan is due by `2026-08-25T16:55Z`, or immediately
on a Q8 component decision/refutation, fixed-A3/no-merger review reversal,
generic-center `N13`/TD6 escape result, AS recurrence/acyclicity/state
refutation, landing/ceiling theorem, or credible external plane claim.

## Prior event synthesis overlay (reviewed through 2026-08-25 04:24Z)

The latest completed all-avenue decision record is
[`xmodel/ideation-20260825T0126Z-synthesis.md`](xmodel/ideation-20260825T0126Z-synthesis.md)
(SHA-256 `843f9ffd9293b2abebd3a0edb22ae843745a258a1669f258cd5d1ced22e1b2a0`).
All four active research agents submitted full 46-row scans.  One TD6
universal-syzygy suggestion in the TD6-owner note is disclosed as
cross-pollinated and excluded from independent support; three uncontaminated
submissions independently support the same mechanism.  No result here proves
or disproves JC2.

- **AS affine-Kuranishi transition is the strongest disproof/falsification
  experiment.**  The corrected degree-12/11/10 rows, Q9 state gate, and the
  first pointwise Q9-to-Q8 obstruction are different-model confirmed.  The
  first rank-13 Q9 fibre has an exact 13-dimensional affine Q8-survivor locus.
  Different-model review now confirms that at its fixed canonical
  `Q9=e17,Q8=0` point the rank-nine Q7 kernel is exactly
  `c6=0`, `d6_6` free, `div(W5,Z5)=0`, and every one of its `3^9` states has
  `R10=x^10`; the whole nine-dimensional Q6 fibre and every still-lower
  restoration can affect only degrees at most eight, so this one branch is
  terminal outright.  Sixty-six further zero/plus/minus-basis
  lanes sampled the 13 free Q9-locus and 19 free Q8-solution-kernel
  directions: eight die at Q7, and each of the other 58 has the same constant
  `x^10` obstruction on its entire `3^9` Q7 fibre.  This is exhaustive only
  inside each sampled fibre, not in the two predecessor affine spaces.
  The four individually Q7-incompatible predecessor directions were then
  exhausted jointly: their exact cokernel coordinates are
  `s15,2t6,s17,2t8`, with no mixed or quadratic cancellation.  A global
  successor has now crossed each of the remaining 28 coordinates with that
  active four-cube: every one of the 28 five-dimensional slices has exactly
  the three compatible points on its added-coordinate axis, so no added
  coordinate cancels an active obstruction by itself.  A global
  degree-at-most-two reconstruction on all 32 predecessor coordinates has
  now completed on the explicit **unreduced affine-lift chart**.  Its exact
  global cokernel map has only four nonzero coefficients,
  `kappa=(s15,2t6,s17,2t8)`, so its compatibility locus is the
  28-dimensional linear subspace where those four coordinates vanish; all
  squares and cross terms are zero.  This is producer-exact pending hostile
  review.  Canonical digit reduction and division by three can create
  higher-degree carry functions--indeed none of 64 reduced off-grid controls
  survives Q7--so a carry-translation theorem or an enlarged state is still
  required before this zero locus has global canonical meaning; the
  four-coordinate and five-dimensional statements remain exact slices.
  Sampled rank classes already show
  that rank/count alone is not a sufficient transition state.  No finite
  graph has recurrence meaning until representative-independent state
  sufficiency, time-homogeneity, and an exact transition section are proved.
- **Selected-Q8 component geometry is the strongest bounded proof
  experiment.**  Hostile review now confirms the infinity passport and the
  primitive Galois grouping: contacts lie either on one all-eight component
  or on eight singleton components, and the all-eight alternative cannot
  carry a registered actual trajectory because two contact points would both
  have to be the image of source infinity.  The singleton alternative remains
  open.  Separately, pure Singular computed all 126 nonzero `F_127` fibres:
  123 have squarefree degree-190 `v` eliminants, while `w=39,56,125` have
  degree 189 (with quotient dimensions 190,189,189).  The `w=25` partition
  `[2,188]` and `w=47` partition `[1,3,186]` have disjoint proper subset-sum
  sets.  Their interpolated monic candidate `H(w,v)` has `deg_v=190` and
  coefficient `deg_w<=21`.  A true holdout at all three excluded values is
  exact: at `w=56,125` the fibre eliminant divides `H` with quotient `v`, the
  localized-away boundary sheet, while at `w=39` the quotient's root is an
  existing fibre root, exactly matching non-primitivity of `v`.  Direct
  lexicographic conversion of the original ideal has also produced exact
  eight-polynomial shape bases at all 123 primitive fibres, with all 984
  original-row remainders zero.  A new AWS boundary certificate proves
  `H(0,v)=Q8bar(v)C(v)` with the corrected octic occurring exactly once and
  coprime degree-182 cofactor; hence the candidate plane curve passes
  smoothly in the `v` direction through all eight corrected-Q8 projection
  points.  Rational reconstruction and direct substitution of the other
  seven coordinates are active.  Even a successful modulo-127 component
  still needs an exact characteristic-zero lift or a multiplicity-one
  good-reduction/no-merger theorem before it decides the characteristic-zero
  contact partition.  Direct generic membership/basis jobs and 48
  candidate-seeded quotient variants run on AWS.  These fibre data imply no
  generic irreducibility or characteristic-zero component until that bridge
  is exact.  All msolve shape outputs are quarantined after exact failures.
- **TD6 advances from a reviewed two-center theorem to a nearly closed raw
  trivariate atlas and universal-adjoint test.**  The complete source-typed
  `(C,1,U)` section is different-model confirmed.  The frozen trivariate
  checkpoint gives the exact generic unit `-k/50` and closes `U=0,V=0`.
  Exact V17 work over the `P3` quotient field now gives ranks `3470/3602` and
  `38/132`, the genuine P12 remainder `-k/50`, and only a power of `U` in its
  chart factor.  Both pivot orders are now frozen: `P3` off `U=0` is empty,
  and its `U=0` point is the already-closed origin.  The whole set-theoretic
  `P3` curve, hence the remaining `H=0` debt of this fixed source-typed
  section, is now different-model confirmed; a nonmutating erratum changes
  only the origin mechanism label from first-band to the stronger
  transport-band obstruction.  For the former raw-`B3` debt, the V14 local
  output is only a
  denominator diagnostic: it has a 1,681-term nonconstant remainder and says
  `stratum_localization_killed=false`.  A birational raw-B3 replay has now
  reached the exact full-source/P12 remainder `-k/50`; its first run failed
  only in a post-result Python sentinel, and the strengthened clean replay
  plus every exposed chart factor subsequently completed on AWS.  The
  whole quadratic factor `t^2-4t+2` is producer-exactly empty off the
  already-closed `w=0` origin, and both clean pivot orders on `t=1/2` have
  now returned rc zero with the same genuine unit and complete `w`-only
  charts.  The resulting birational cover closes all of `B3=0`; together
  with `D(UHB3)`, the whole `U=0` divisor, and the twice-reviewed whole
  `H=0` divisor, this producer-exactly empties the entire fixed source-typed
  normalized three-center `A^3_(C,V,U)` section.  The union is frozen and
  hostile review is queued before promotion.  The next transverse test adds
  the source-proven `q2` boundary class by a generic-center dual adjoint.
  No gauge, neighborhood, full-TD6, SP-2, or JC2 conclusion is licensed.
- **Software work follows the live mathematical clients.**  Proof-carrying
  integer carry states, cost-balanced AS sharding, a content-addressed AWS
  job/source broker, exact quotient/exceptional-stratum adapters, and a Q8
  normalization/pole compiler are the shared accelerators.  Quarantined
  claims must taint descendants mechanically.
- **All substantive computation is AWS-only.**  The seven-node fleet is at
  the 512-vCPU quota.  Box01/r6b/r6c are saturated; Box02, Box03, and r6d are
  taking the expanding Q8/AS/TD6 exact lanes rather than leaving local CAS
  workers alive.  The laptop is limited to editing, orchestration, hashing,
  status, cloud-review clients, and short low-memory checks.  Reviews remain
  asynchronous and do not normally block reversible producer successors.

The ultimate proof walls remain full landing/coverage and an absolute or
cofinal complexity bound.  The disproof wall remains an all-depth bounded
state plus characteristic-zero polynomial algebraization with collision.
The next full scan is due within twelve hours of this synthesis, or
immediately on a Q8 component decision, closed TD6 trivariate atlas/universal
adjoint, AS recurrence/acyclicity certificate, review reversal, or credible
external plane claim.

## Prior event synthesis overlay (reviewed through 2026-08-24 21:36Z)

The latest completed all-avenue decision record is
[`xmodel/ideation-20260824T2024Z-synthesis.md`](xmodel/ideation-20260824T2024Z-synthesis.md).
All six submissions converged on a residual maximum-12 normalized
differential/Hurwitz atlas, corrected AS residue state, and TD6 transverse
projective escape.  Two AS-owner disposition rows are excluded from blind
vote counts after a disclosed two-line exposure; mathematical claims remain
review-gated regardless of votes.

- **Maximum twelve remains first; `Q12` is quarantined and the genuine `Q8`
  branch has re-rooted the local lane.** Hostile review found that the replay replaced the
  actual-fibre term `x5^2(3v+1)/(9v)` by `x5^2/(27v)`.  Its determinant was
  therefore computed off `r2=r4=0`.  The corrected octic `Q8` replacement is
  frozen, and a fresh different-model review independently reconstructs its
  determinant, every rank-three minor, and the equivariant implicit-function
  argument without consuming a `Q12` descendant.  Every loaded `Q8` parity
  contact of the seven-row fibre lies on a second smooth non-parity formal
  component.  This is not a trajectory; normalization, `r8`, terminal/Kummer
  dynamics, and Taylor reconstruction are active.  The reviewed parity
  genus-five, full-absorption, and DZ20 exclusions remain unaffected.
- **A root-free collision stratifier is now different-model confirmed.**  In the
  quadratic algebra cut out by the even Wronskian, the unordered critical-
  value norm has discriminant `4*s*E^2`.  It gives an exhaustive first split
  into double-root, full-absorption, one-absorption, equal non-1 values, and
  generic unequal values without selecting either root.  Hostile review
  independently reconstructed the source Faber pair, Wronskian, scaling,
  pair norm, and resultant comparison.  The next proof work attaches the
  original fibre/Taylor rows and
  applies cube branch signatures, reduced four-point Hurwitz genus, and the
  terminal differential only on exact normalized leaves.
- **TD6 has a reviewed whole licensed `c1` line theorem.**  The union of raw
  `C=0,3`, the denominator open, and the `J=0` quotient is confirmed empty,
  always by the unit `-k/50`.  This raises the value of avenue 2's finite
  family compiler but not its global coverage tier: simultaneous centers and
  other moduli remain.  The live successor retains `c3` before projective
  closure and tests escape at infinity.
- **AS now has a different-model-confirmed matrix-state compression,
  conditional only in its AS-source interpretation.**  The displayed
  vertical D8 map is described by two adjugate residues, endpoint degree
  caps, and two degenerate strata; independent exhaustive literal-`F3`
  comparison has zero mismatches across all 1,594,323 assignments.  Removing
  the caps produces exactly 202,176 false positives.  The matrix theorem is
  promoted, while treating its regenerated column as the exact AS successor
  still waits for the corrected integer-source review.  The next divided
  carry proceeds provisionally with nilpotents and determinant-zero strata
  retained.

The proof-side ultimate walls remain full landing/coverage and an absolute or
cofinal complexity bound.  The disproof-side wall remains characteristic-zero
polynomial algebraization with collision.  Generic sparse search, untyped
depth, and AWS expansion stay stopped.  No result here proves or disproves
JC2.

## Prior event synthesis overlay (reviewed through 2026-08-24 19:32Z)

The last completed all-avenue decision record is
[`xmodel/ideation-20260824T1820Z-synthesis.md`](xmodel/ideation-20260824T1820Z-synthesis.md).
Six of six scans landed.  The round is formally degraded because two owner
reports had narrow accidental root-snippet exposure, but all four
uncontaminated scans independently selected the same new critical-path client:
the coprime undeformed `(9,12)` order-three fibre should be attacked through
its spectral norm, sharp polynomial-abc equality, and exact degree-36 Belyi
passport.  AS degree ten and the TD6 exceptional atlas remain the orthogonal
disproof/proof roots.  No result below proves or disproves JC2.

- **Every characteristic-zero Keller pair of maximum actual partial
  `y`-degree at most eleven is an automorphism.**  Different-model hostile
  review confirms the cube-trajectory closure and the complete `(6,9),3|H`
  composition.  The nontrivial-Kummer and polynomial-cube branches are
  disjoint and exhaustive; the latter is closed separately for `d!=0` and
  `d=0`, constant and one-root-power `s`, and all mixed `rho3,rho4` strata.
  The degree recursion then closes all 78 unordered / 144 ordered pairs
  through maximum eleven.  This is an unbounded-`x` partial-degree theorem,
  not a new total-degree theorem.  Maximum twelve has exactly two new
  primitive checksums, `(8,12)` and `(9,12)`; neither is yet excluded.
- **The maximum-twelve successor has confirmed routing and universal high-row
  Faber landing; its full lower fibres remain open.**  For primitive degrees `(dr,ds)`, on
  `u^d=h` the first residual is `d*(s*A-r*B)'=0` and
  `delta=s*A-r*B`.  On the monic-core divisor class, every nontrivial Kummer class of order dividing `d`
  forces `delta=0`; the polynomial `d`th-power branch remains weight-
  unforced.  Thus `(8,12)` has orders `4,2,1`, including a mandatory
  quadratic intermediate branch, while `(9,12)` has orders `3,1`.  The raw
  constant-W tangent gate is smaller for `(8,12)` (18 variables/17 rows,
  rank 11) than for `(9,12)` (19/18, rank 11), but the full Kummer branch tree
  is simpler for `(9,12)`.  Different-model review independently confirms the
  shared differential-field theorem: every high-row solution has a unique
  constant-coefficient Faber form, with remaining equations
  `r1'=...=r_(m-2)'=0`, `m*r_(m-1)'=j/u`.  After character-compatible target
  quotient the exact high-row widths are `7,10,16` on the `(8,12)` orders
  `4,2,1` and `9,17` on the `(9,12)` orders `3,1`; local two-row controls give
  `5,8,14` and `7,15`.  Aggregate mandatory-route cost therefore selects
  `(9,12)` for speed, while raw width and the widest leaf can still favor
  `(8,12)`.  This is an allocation rule, not a simpler-cell theorem.
  Producer-exact approximate-cubic equations additionally trap every reduced
  formal arc meeting the common cubic when `k!=0,mu=nu=0`; nilpotents and
  disjoint components remain open.  On the coprime `k=mu=nu=0` face,
  `beta=g^3/f^4` has exact passport
  `(3^12)|(4^9)|(20,1^16)`.  A new stabilizer-aware descent treats every deck
  order `1,2,4` and combines the terminal ODE with divisor congruences to
  exclude that nontrivial-Kummer face.  Hostile review is now **CONFIRMED**
  after independent reconstruction of the passport, isotrivial descent,
  stabilizers, local series, and infinity contradiction.  On an actual
  Keller trajectory coprimality is automatic because `gcd_z(f,g)` divides
  the nonzero `z`-constant Jacobian; projected noncoprime pieces are
  elimination artifacts, not a remaining trajectory branch.  The active
  loaded successor has
  `B=(15/2)*mu*F6'+18*nu*F3'+60*r8`; for `mu=0,nu=1` its first collision
  boundary is `3*p+10*r8=0`.  On the genuine corrected `Q8` parity contacts,
  hostile review now confirms both the normalized non-parity branch jet and
  its root-free local descent: the node is in norm leaf 3, the punctured
  branch in leaf 4, `Delta,tau,q,S` are unit local quotient coordinates, and
  every actual trajectory satisfies `h^3*(S')^9=j^9*S^8`.  The parity-only
  identity `p^9*R6(v)=nu` fails by a unit at second order off parity.  This is
  formal-local and does not exclude the punctured branch; the global quotient
  relation/Taylor center is the active gate.  Nonzero loads, the
  polynomial-core leaf, `(8,12)`, and all remaining Taylor boundaries stay
  charged.
- **Map-only AS has a confirmed deep terminal point and a confirmed infinite
  finite-depth control, but no full-locus obstruction.**  The exact
  `D=7` triangular map survives modulo `3^6` and is cap-terminal at the next
  digit; hostile review independently rebuilt its integer determinant and
  digit enumeration.  A balanced cyclotomic family of degree `2m+1` has
  determinant `1-3^(2m)*x^(4m)`, so finite-depth false positives occur at
  arbitrarily large depths; hostile review confirms the identity, 3-adic
  integrality, special fibre, and same-cap terminality.  Each displayed
  residue remains pointwise terminal through every cap `D'<=4m`, but this is
  not a locus theorem.  Hostile review also confirms that the associated
  layer-`7/6` top scheme has three reduced minimal components and one
  load-bearing embedded primary component.  A proposed 29-row deep-branch
  acceptance scheme remains **quarantined**:
  its universal bracket Cartier-zero lemma is correct, but it omitted the
  divided integer carry `(U_x+V_y-x^2)/3`, whose `p=3` Cartier coefficient is
  `u5_3+v5_2`.  Different-model review now confirms the source-honest
  replacement: the full residual is `L/3+K+C_x+D_y`, the corrected ideal has
  40 variables/30 rows/dimension 18, is nonradical, and has exact cover
  `I=Q0 intersect Q1 intersect E`.  The degree-ten successor retains the
  original ideal.  Its frozen exact rank stratification kills generic cone
  points but leaves a vertical component and two endpoint rays; hostile
  review is active.  Successor work proves that the full vertical degree-nine
  mixed-carry row has a global polynomial section and is universally
  compatible.  Degree eight, where the divided integer carry first enters,
  is the next honest obstruction gate.
- **The licensed TD6 `q2` pencil remains empty, while common centering is
  only a sensitivity gate.**  The frozen full-centering differential
  `E^3 -> E^10` is now different-model **CONFIRMED** with rank three, zero
  kernel, and inconsistent affine equation at the registered nonsolution;
  the reviewer independently rebuilt all 3,602 columns.  This does not kill
  a center family or SP-2.  Dependency census selects `(c1,c3)` over
  `(c1,c2)` by 6,763 fewer typed entries.  Exact FLINT tower arithmetic is
  propagating the nonlinear `c1` line and the `c3` dual thickening.  Nine
  current-band compatibilities provisionally kill the generic `c1` pivot
  open.  Cross-pollination caught that the proposed one-shot 110-by-132
  affine Smith system becomes genuinely nonlinear after later-row
  substitution; that shortcut and its descendants are stopped.  Exceptional
  coverage must instead use the typed stagewise parameterization, recursive
  specialization, or a nonlinear certificate.  On the valid transport-plus-
  first chart, the genuine current `X0,t12` row is literally `-k/50`,
  independent of all remaining parameters; its source-ideal lift is active
  and provisionally prunes every previous/pole and current pivot stratum
  there.  The generic `c1/c3` dual and raw transport fibres `c1=0,3`
  continue.  The generic result is not reversed.
- **Pinchuk 2021 contributes one local identity, not a global reduction.**
  Primary-source audit confirms Theorem 4.1 under matched leading
  coefficients and the extremal number 16 at `(4,3,3)`.  Theorem 3.4's
  printed reduction has a termination gap and exits through rational-power
  q-polynomials; it is not a polynomial normal form, `G2-PSC`, or JC2 route.
  Pakovich--Zvonkin classify unitrees rather than all minimizing pairs, so no
  literature list is imported into the self-contained three-point-cover
  descent.  This paper is unrelated to Avenue 24's real Pinchuk maps.
- **Recent Makar-Limanov papers add conditional Newton-frame restrictions,
  not a campaign bridge.**  Primary-source audits confirm, in the papers'
  specially reduced complex-counterexample frame, the ratio-two/vertical-
  edge exclusion, the admissible-edge bound `s<=Omega(D)-1`, and the shaped
  leading-total-degree condition `Omega(D)>=3`.  The author-reported
  `D<=100` census is not code-reproducible and retains `D=72`.  Existence-
  promised mate recovery gives uniqueness only modulo `C[f]` and no
  unconditional mate-existence algorithm.  No theorem currently transports
  these inputs to GGV packets, Sigray books, TD6, residue-A, or maximum actual
  partial degree.  Typed Newton-row checking and bounded Faber certificate
  packaging are licensed software clients; untyped filtering is not.

Reviews remain background/nonblocking, but promotion remains review-gated.
The next blind all-avenue scan is due no later than twelve hours after the
`19:03Z` synthesis close; broad web sweep #9 is due by `2026-08-24T21:25Z`.
AWS expansion remains stopped because the current gates are exact symbolic
and certificate bottlenecks rather than memory-capacity bottlenecks.

## Prior event synthesis overlay (reviewed through 2026-08-24 14:03Z)

The proof backbone remains the `td=6` global-realizability problem; the
disproof backbone remains an exact finite polynomial AS109 lift. The current
ranked synthesis is
[`xmodel/ideation-20260824T1205Z-event-synthesis.md`](xmodel/ideation-20260824T1205Z-event-synthesis.md).

- **The bounded partial-`y` frontier is historical through eight.** If the
  actual degrees are `da,db`, the top Jacobian row and a large source shear
  make the total-degree gcd `H+dL`, where `H` is the common leading-root
  degree. Nagata's repaired prime-gcd theorem and the independent GGV `2p`
  theorem cover `gcd(H,d)<=2`; target reductions then cover every ordered
  pair with maximum actual `y`-degree at most eight. The first fundamental
  remainder is `(6,9)` with `3|H`. Its leading common-cubic family
  `(K^2,K^3)` is positive-dimensional, so the previous coprime finite-map
  mechanism stops. The first source-honest coefficient gate is now
  dual-confirmed: nontrivial Kummer alignment separates from a live cube-core
  mismatch; one selected boundary root does not license full-cubic reduction;
  the normalized constant-W scheme is exactly common cubic union one
  Davenport--Stothers curve; and the eight high source rows reduce to five
  moving coefficients plus a weight-zero `kappa` in the chosen target pin.
  A later reviewed translation erratum shows that `kappa` itself is gauge,
  while `kappa^2+mu` is invariant. A pure DS trajectory is
  conditionally empty, but its entry is not automatic. The active gate is the
  four lower Pfaffian rows and terminal row, componentwise with the true
  minimal boundary factor, while `delta!=0` runs separately. Our
  cubic-through-sextic proofs remain exact alternate certificates, not first
  exclusions.
- **AS109 is globally constrained but not obstructed.** Any exact lift has
  generic degree `d>=109` and a formal split `E^109 x A_infinity`; the two
  missing global keys are `A_infinity=0` and rational deck descent. The
  natural `xy` client points away from the target field, and the Moskowicz
  no-prime proof is source-audited `REFUTED-AS-PROOF`. The bounded-degree
  history stop first raised the correction floor to nine; the later promoted
  maximum-eleven theorem and reviewed Hensel composition now raise it to at
  least twelve. The wild-symplectic gate is
  dual-confirmed `GAUGE-TRIVIAL / CONTROL-ONLY`: the completed unit bidisc is
  a finite etale Artin--Schreier torsor, but all determinant-one
  restricted-analytic lifts of that special fibre form one near-identity
  symplectic gauge orbit. The forced first-digit monomial is only a floor.
  The bounded successor is now dual-confirmed: the rational cotangent
  basepoint has a reduced exterior affine polar divisor which no polynomial
  Keller right map can cancel. Consequently the unique analytic gauge of any
  hypothetical polynomial lift has canonical truncation degree/support
  tending to infinity; every fixed simultaneous map/gauge degree cap fails
  at finite depth, and the first cap `p,p` fails already at depth three for
  every odd prime. This does not exclude an unbounded polynomial lift or
  identify `A_infinity`. Quantitative conductor growth and a proved
  polar-divisor/`A_infinity` comparison are the next discriminators; no
  `p=109` brute force or exponent rectangle is licensed.
- **TD6 has killed one complete normalized family, not SP-2.** The symbolic
  next-row zero curve, its centered sextic cut, the degree-18 pole field, and
  a nonzero exact left syzygy exclude the whole fixed normalized reduced-
  boundary family. Boundary, dead-stretch, centering, and related moduli were
  fixed. The first bounded q-boundary successor
  `q_B=t+B*t^2+t^25` is now dual-confirmed empty at `B=1` and at one exact
  adaptive value in the degree-18 field, both at the `t^4` centered row; the
  residue is genuinely `B`-sensitive. Two points do not kill the family.
  The active test is fraction-free elimination over `E[B]`, with every
  pivot/rank-jump stratum treated separately; adjoint jets may accelerate
  that elimination but cannot replace it. Full landing/coverage and the
  distinct `G2-PSC` / `G2-BD` obligations remain.

The rational-Catalan length analogy is an accelerator/control only. Secant,
raw projective connectedness, literal-slot, `xy`-membership, unrestricted
completed cohomology, consecutive-degree widening, generic sparse search, and
AWS expansion are stopped unless a new source-derived invariant reopens them.
No result here proves or disproves JC2.

## Prior event micro-round overlay (reviewed through 2026-08-24 10:51Z)

The two live backbones remain `td=6` global realizability on the proof side
and a fixed finite-support AS109 lift on the disproof side. The exact gates
and their different-model hostile reviews sharpened both without resolving
JC2.

- **AS109 independent-slot and bounded-`y` no-gos.** A finite full module of independently
  variable literal monomial slots cannot satisfy the proposed
  `CLOSED-SUPPORT + UNIT-L` certificate: the forced `x^108 y` slot and its
  nonlinear polarizations generate infinitely many `x^(108k)` residuals.
  Genuinely coupled sections are not excluded. Independently, every
  characteristic-zero Keller pair with both coordinate `y`-degrees at most
  five is a polynomial automorphism. The cubic cusp block reduces over the
  function field to `f=z^2+U`, `g=z^3+Vz+W`. The new `(3,4)` block reduces to
  `f=z^3+uz+v`, `g=z^4+az^2+bz+c`; five exact Jacobian rows give a conserved
  product, and a degree-ten monic equation removes the rational depression.
  The remaining polynomial unit product is impossible. The three new
  quintic patterns `(2,5)`, `(3,5)`, `(4,5)` close by exact finite-pole,
  singular-cubic, and weighted-projective-infinity arguments. Hence no exact
  AS109 lift has both correction `y`-degrees at most five. The surviving
  certificate must be genuinely coupled and allow `y`-degree at least six;
  no support search is licensed by this degree floor.
- **TD6 centering escape.** The bare chart `x=t s^4`, `y=s^-1` has a valid
  polynomial divisibility contradiction only after the common lower
  truncation is pinned to zero. LR2 permits the shared truncation `x~y^-1`,
  giving the polynomial Eggers coordinate `T=xy^4-y^3`. Explicit polynomials
  inside the SP-2 rectangles reproduce the three-branch equality mechanism
  with centered Jacobian errors of exact orders three and six. Dropping the
  degree cap permits arbitrary finite one-sided Keller jets. Thus the bare
  obstruction is stopped. The first exact shared-coefficient two-chart
  specialization is nonempty at its first band: rank `3508/3602`, nullity 94,
  with a deterministic rational witness. On that entire affine family the
  next row is nevertheless impossible because
  `[s^-1 t^13]J=-18858/3125`. Both facts are different-model confirmed. They
  kill only the selected centering/orbit/dead-stretch/pole values, not SP-2
  or a terminal class. The next honest gate quantifies those licensed moduli
  inside the same rectangles and preserves the F1-to-r9 transport.
- **Secant-idempotent accelerator.** For a Keller map and any polynomial
  secant matrix `A`, the normalized `det A` is the diagonal projector in the
  self-fiber product and
  `I:Delta = I:Delta^infinity = I+(det A)` scheme-theoretically. This replaces
  saturation by one explicit third generator, but the unit-ideal endpoint is
  injectivity and hence JC2 itself. The identity is standard neighboring
  secant/Bezoutian algebra, not a novelty claim. Its first projective
  connectedness child is dual-confirmed `COSTUME`: unsaturated homogenization
  can contain artificial infinity components even for an automorphism, while
  a genuine collision component may meet the diagonal only at infinity.
  Resurrection requires a source-derived saturated boundary-intersection
  datum, not another naive connectedness argument. Applied directly to
  AS109, the secant row is also dual-confirmed `COSTUME`: on every off sector
  `x-u` is a unit, so the adjugate identity puts `det A` and its tangent row
  inside the localized collision ideal. The 108 projectors merely restate
  the Hensel sheet split and add coefficient rank zero.

The active finite successors are the symbolic-moduli two-chart `td=6` gate,
the AS109 sextic survivor discriminator, and a
conditional Hensel-to-global-degree cross-gate. Reviews remain
background/nonblocking. No held
D-series, unrestricted Witt, generic exponent-rectangle, new book-cell, or
AWS expansion is reopened. No result here proves or disproves JC2.

## Current portfolio overlay (full round closed 2026-08-24 07:55Z)

The authoritative decision record is
[`xmodel/ideation-20260824T0719Z-synthesis.md`](xmodel/ideation-20260824T0719Z-synthesis.md).
Round `20260824T0719Z-c17bd25` collected four complete blind all-46 scans and
is formally `DEGRADED`: a fifth blind lane timed out without freezing a report
and was excluded. Post-collection deduplication, feasibility analysis, and
hostile selection reduced twelve cards to three live mechanisms.

The round also exposed a shared-prior failure. Mapping/topological degree four
is already excluded by Domrina's 2000 general-case theorem, and Żołądek
Theorem 6.12 closes `td <= 5`; the first open sheet degree is six, not four.
All quartic normalization/resolvent/filling cards are known-case reproofs and
are stopped. The proposed first HC4 quintic module and `5 -> 4` Schur descent
also duplicate `xmodel/sol-hc4probe.md`, whose exact verdict was `NO LEVERAGE`
on the JC2 sector. The suggested deformation `x+x^2y+y^k` is not unimodular,
and the claimed Tate horizontal divisor is outside the stated unit bidisc.
Untwisted action residues remain automatic exact-form `COSTUME`.

All three first gates have now returned:

- **H — Hamiltonian/Brieskorn cokernel: stopped after a scoped theorem.**  For
  a unimodular-gradient `P`, the class `kappa(P)=[div V] mod D_P` is well
  defined and vanishes exactly when `P` has a polynomial Jacobian mate, but
  Friedland already supplies the
  cokernel/Gauss--Manin object and no universal receiver was found.  An
  exhaustive weight recurrence proves that every `P=x+x^n y`, `n>=2`, has
  unimodular gradient but no polynomial mate.  Different-model review
  confirmed the identities and scope; this family result is a control, not a
  global advance.
- **P — dual-pencil definition gate: stopped `JUMP-ONLY / TYPE-FAIL`.**  Every
  `aP+bQ` is a submersion and its Suzuki defect is the first Betti number of
  the generic fiber.  Zero defect in one pencil direction is already the
  automorphism endpoint, whereas the universal defect has horizontal support
  and is not the proposed finite divisor.  A hostile source audit corrected
  the sign of the constant-degree jump coefficient but preserved the stop;
  no GRR descendant is licensed.
- **S — characteristic-109 bounded support: stopped
  `NO-FROZEN-GRAMMAR`.**  A cap of eight monomials does not bound their
  exponents: exact triangular source gauges produce infinitely many literal
  representatives and change successor support.  No enumerator ran.  A
  carry audit also quarantines the generic uncarried second-digit equation;
  the actual obstruction family has zero carry integrally, so the grammar
  failure survives.  Positively, an exact fixed-support `Z_109` lift with
  determinant one would, for each fixed residue `b`, map the 109 source balls
  indexed by `a` onto the same target ball and would embed to a complex
  counterexample.  `CLOSED-SUPPORT + UNIT-L` is an
  explicit sufficient contraction certificate, but no support satisfying it
  is known.

The established `td=6` funnel is therefore again the proof backbone.  Its
historical capped nonroot/IV ledger had four single-pole and four two-pole
terminal classes, but that is not an exhaustive current root/SF1 census:
root `M=1` is allowed, the `l>=1` root menu was omitted, and the exact
root-aware AWS rerun is pending review.  An exact Jacobian-one analytic equality control shows that a
multiple direction root need not create strict extra defect, so the cheap
local strictness kill is retired.  The live proof target is global polynomial
realizability/opposite-side balance, together with the distinct `G2-PSC` and
`G2-BD` landing obligations.  The live disproof target is a proved finite
gauge-normal-form or symbolic-motif grammar that can expose a closed support
with unit linear inverse.  No held D representation, K3 compiler,
unrestricted Witt level, generic exponent rectangle, or AWS expansion is
reopened.  No result here proves or disproves JC2.

## Previous portfolio overlay (full round closed 2026-08-24 06:29Z)

The survey scores and shortlist below were historical inputs at this cutoff,
not the live allocation. The then-current decision record was
[`xmodel/ideation-20260824T0453Z-synthesis.md`](xmodel/ideation-20260824T0453Z-synthesis.md);
the preceding records are
[`xmodel/ideation-20260824T0156Z-synthesis.md`](xmodel/ideation-20260824T0156Z-synthesis.md)
and
[`xmodel/ideation-20260824T0035Z-synthesis.md`](xmodel/ideation-20260824T0035Z-synthesis.md).
The newest round landed five blind all-46 submissions but is formally
`DEGRADED`: the Claude/Fable lane failed before producing a report and was
cancelled at the close rather than blocking the other research.

The first launch envelope has run. Exact different-model reviews preserved,
at narrow scope, the external SuperMind/Guo intake, the pointwise one-band
`D25 -> D27` signal, the raw-boundary `COSTUME` kill, and the current-packet
TRACE underdetermination result. They do not supply the GGV transport bridge,
a D27 component or germ, a characteristic-zero lift, or a JC2 implication.

The post-round gates have separated the live questions. The sole full-cell D
gate run so far found a nonempty compatibility locus on one modular cell;
different-model review confirms generic differential rank at least five. Rank
exactness, dimension, persistence, and characteristic zero remain open. Its
different-model confirmed `D-STATE-GATE` found an exact six-band affine law for the pure-`y`
source but stopped at `NO-TYPED-STATIONARITY`: the full source introduces
unclassified `alpha_1,beta_1` directions, so no Ore/Spencer object is licensed.
`NORM-MOMENT-SEP` is different-model confirmed `DIFFERENT-INSUFFICIENT`: the
same abstract local algebra, different, selected contact/Jacobian data, and
first trace principal part do not determine the quadratic coordinate moment;
the coordinate multiplication data differ, and the preregistered native-source
gate was not reached.
The orthogonal odd-prime cap is different-model confirmed `W2-SURVIVOR` over
`Z/9`, with no characteristic-zero inference. A separately different-model
confirmed exact control exhibits compatible lifts at every Witt level for this
seed, but with linearly growing support and a rational/restricted-analytic,
nonpolynomial limit. The
D43 P4P1 source-honesty gate is different-model confirmed `ORIGIN-ONLY`:
its exact sidecar vanishes at the named modular origin but is
load-bearing away from the sidecar zero line, so the old issue was
provenance/scope wording rather than wrong origin math. Any renewed
mathematical root must redesign around (i) a typed
global polynomial-origin source/receiver, (ii) the missing full D-source state,
or (iii) bounded-support/degree Witt descent. `A(F)` remains the canonical
target object but still lacks a cheap complete source/landing construction.

The newest gates changed the ranking again. Exact coframes refine avenues 5
and 39 rather than adding avenue 47: closed determinant-one rows outside
`E_2(C[x,y])` give a counterexample, but Wright's 1978 weak Jacobian theorem
makes the broader `E_2`/automorphism dichotomy classical. Different-model
review confirms that the complete fixed-first-row Broughton/Cohn family
`L(h)C_B` is empty: its closure equation
`(1+2xy)h_y-x^2h_x=6y` has a nonterminating weight recurrence and no
polynomial solution. This closes one left-shear family only; small-word,
low-degree orbit expansion is an instrument/negative control because any
resulting explicit low-degree Keller pair would conflict with the established
plane degree bounds.

The finite-normalization gate refines avenue 31. Its rank-two unit/trace
contradiction is different-model confirmed but rederives the known quadratic
Galois case; Orevkov's primary Theorem 1.1 also excludes generic mapping
degrees two and three. Neither is campaign-new, and no rank-three descendant
is live. The weighted-D level-two gate refines avenue 4: at the frozen basis,
the promoted 25-file producer perimeter has no named full-polynomial source
point or tangent maps through `alpha_2,beta_2`, so no quotient, two-driver,
Hankel, recurrence, band-28, or deeper-D step is licensed. Its level-42/84
formulae are exact ambient acceptance tests only. The degree-three marked
collision preflight refines avenue 19: rank six, tangent dimension ten, and a
vanishing first obstruction leave ten parameters and nine zero-linear rows,
so it stops `COMPILER-READY/HEAVY` without a verticality or characteristic-zero
claim.

A post-gate delta triage nominates avenue 33's first global action-residue
quotient as the next bounded root and avenue 10's HC4 quintic obstruction
module as the orthogonal reserve. This is a queue recommendation, not a launch
or promotion; the Wright/Orevkov priority corrections and four stopped gates
warrant adjudication in the next full-spectrum round.

Standalone TRACE-REG, raw boundary passports, sublinear type, primitive-group
censuses without a proved support bound, new book cells, DIR/A-SCALE expansion,
D75, B=168, and integral D43 are stopped or held at the current gates. None of
these rankings is a mathematical promotion.

## Historical survey executive summary (2026-08-21; superseded by the current overlay)

1. Three independent surveys (Sol 48 entries, Grok 40, Fable 18) merge into **46 distinct top-level approaches**; every row traces to at least one input.
2. All three agree the tried core — GGV corner farm, Sigray sheet books, formal-germ D-series — is a necessary-condition mill stuck on global source/transport and complete landing/coverage, plus no absolute/cofinal topological-degree ceiling. The old “same two gaps” phrasing overloaded `G2-PSC` with the separate route-specific `G2-BD`; see the correction above.
3. The strip/residue-ODE result (S5–S6/G3) was the surveys' one highlighted general theorem in the tried core; it is scope-limited, and the ODE rigidity has Żołądek A.7 priority.
4. The implication ladder above JC2 (Mathieu, GMC n≥3, PC(2), JC(n≥3), Hessian n≠4) is dead post-July-2026 — full consensus; Fable's "no n=2 traction anywhere" matches Grok's "documented massacre".
5. Char-p: the Mondello `F_2` stratum is W2-obstructed (1,152/1,152), but an explicit degree-three `F_3` collision has a confirmed lift at every Witt level when support grows; its limit is restricted-analytic, not polynomial, so bounded-support/degree characteristic-zero polynomial descent remains open.
6. Promise scores are incommensurable across surveys (see semantics note above); this document flags disagreements rather than averaging them.
7. The original top untried consensus pick was **global symplectic primitives / action residues at infinity** (Grok 6/10, his highest untried; Sol's adjacent symplectic lane 7/10).  Its ordinary untwisted gate has since returned exact-form `COSTUME` and is not a live top pick.
8. The original biggest dissent was **HC4 ⇒ JC2** (Sol 9/10 vs Grok's structural hostility).  Its named first quintic module has since run and returned `NO LEVERAGE`; no later observable is currently live.  The other dated dissents were BCW cubic reduction, dim-3 descent, collision ideals, and A(F)-as-object.
9. Unique finds: **Sol 9** (ZMT/Rees integrality, K2 reciprocity, free-associative lift, p-adic model theory, Siegel integral points, Vitushkin pole removal, deformation no-go, motivic/A^1-degree, commuting frames), **Grok 6** (Ritt, finite-field census, Markus–Yamabe, Moskowicz, differential Galois, Lagrangian generating functions), **Fable 2** (D-module/holonomic index; Tsuchimoto p-curvature as a standalone lane).
10. The historical shortlist in section 4 was symplectic residues, HC4 quintic module, primitive-group td bound, JvdK cusp words, and 2D tangent-sweep saturation.  It is provenance, not the current launch queue; the first two entries are now stopped.

---

## 1. Master union table

Tried? legend: **Tried** = substantive campaign computation/proof/audit; **Partial** = restricted client or adjacent work only; **No** = no executed test (proposal notes do not count) — following Sol's and Grok's shared convention. Fable's flags quoted where they differ.

| # | Approach | Essence | Sources | Tried? | Stuck where | Promise (reconciled) |
|--:|---|---|---|---|---|---|
| 1 | GGV Newton-polygon corner families + degree farm | Enumerate corner families below a degree cutoff, transcribe [P,Q]=x^k, empty each leaf | S2+S3+S42 / G1 / F1 | Tried (origin of repo; (72,108) externally certificate-supported conditional on the GGV-Horruitiner reduction/transcription bridge; own cCa2/cCa6 lanes modular only; farm to deg 150) | No degree/td ceiling ("always a next pair", G); GGV Section-4 reductions not a complete engine; Prop 4.3 chain arXiv-only + no general corner endgame (F); char-0 certificate memory wall | — (tried); all three agree on the stuck-point |
| 2 | Sheet-number ladder / Eggers–Wall / dicritical boundary trees (Sigray–Orevkov frame) | Resolve infinity, decorate the boundary tree, exclude td=6,7,8,... | S4+S10+S44 / Grok-survey G2 / F2+F3 | Tried — the campaign's active proof backbone (td≤5 closed by the external theorem ledger; td6 has eight terminal classes: four single-pole and four two-pole, with one intermediate merged datum; one fixed normalized reduced-boundary SP-2 family is now exactly empty at a centered third band; td7 panel and td11 are promoted only at their stated conditional tiers) | Hybrid version lacks `G2-PSC` transport/fidelity; all versions lack a universal full-configuration landing/coverage theorem and an absolute/cofinal td ceiling; residue-A routes additionally owe `G2-BD`. A pure Sigray architecture can bypass `G2-PSC` but cannot use GGV restrictions as input. Local direction-multiplicity strictness is refuted by an exact analytic equality control, and the new family kill freezes boundary/centering/dead-stretch data; global realizability and deformation across those moduli remain. Consistent tree exists at td=9; residue-A coefficient wall / l+ dichotomy (F2); Sigray unrefereed | S:8 for a verified GGV-chain-to-Eggers–Wall transport functor |
| 3 | Vertex-gap / strip ODEs / residue functional R_{k,d2} | Face valuation orders bracket equations; strip block collapses to a rigid ODE | S5+S6 / G3 / — | Tried — "the campaign's actual theorem" (G) | Scope: strips, d1=1, depth two, k≥2 only; y-axis columns block (8,28) subcase (1); ODE rigidity is Żołądek A.7 priority | S:6 for depth-three Ore/resultant extension |
| 4 | Formal-germ certification + algebraization (D-series windows, DEPTH-STAB, Padé, gluing, toric closure) | Prolong J=1 in Puiseux depth; if all depths stay nonempty, algebraize a germ | S43+S45 / G4+G24 / F17 | Tried deeply — "ours/novel" (F) | l+/e+ >= 37 floor, no live square block (S,F); modular nonempty ≠ germ ≠ char-0 ≠ polynomial (G); route-provenance loss in gluing; "the D-series is not JC2" (G) | S:7 (D25 comprehensive triangular decomposition), S:6 (joint Fitting ideal); G: honest disproof lane, currently a local maximum |
| 5 | Jung–van der Kulk degree descent / Aut(C^2) amalgam, polydegree | Produce an elementary automorphism strictly dropping degree of a Keller pair | S1 / G5 / F10 | No (as JC2 program; TRANSPORT.md is normalization, not descent) | GGV cusp leading forms d^m U^n − c^n V^m block triangular shears (S,G); "amalgam rigidity never bites on non-automorphisms" (F) | S:4, G:5 — concordant moderate |
| 6 | Abhyankar–Moh one-place / coordinate recognition | Force a coordinate or one-place curve, finish by AMS | S8 / G6 / — | Tried on the wrong object (both agree: fibers are multi-place; category error documented twice) | Correct one-place objects are A(F) components, whose place data are unpinned | S:6 for place semigroups of asymptotic-set components |
| 7 | Nonproperness / Jelonek asymptotic variety A(F) | JC2 iff A(F) empty; classify its rational one-place components | S9 / G7 / F11 | Partial (consistency checks pass; A(F) never constructed from the books; the raw all-directions pencil defect is `JUMP-ONLY / TYPE-FAIL` as the proposed finite effective divisor) | Computing A(F) is the compactification problem again (G); cross-fiber correspondence underived. F11: "partially adjacent (repo RECON material); undeveloped" | DISSENT: S:8 (construct asymptotic values for smallest mu=6 book) vs G: "not cheaper than rebuilding the compactification; I would not start here" |
| 8 | Formal-inverse combinatorics (BCW tree formula, McKay–Wang, Wright, Singer) | Prove high coefficients of the formal inverse vanish | S26 / G8 / F9 | DISSENT on status: Partial (S, via conjE/Magnus truncation) vs No (G, F) | Cancellation across tree shapes global; no uniform truncation bound; unrestricted vanishing false post-Gao dim-3 CE | S:5, G:4 |
| 9 | Lee–Li Conjecture E / Magnus remainder vanishing | E => D => C => B => A <=> JC2 | S28 / G13 / — | Tried at smallest cap (1 exact certified HOLD row; 5 further rows modular/first-prime trace-supported; 0 observed fails) | No reduction to a finite check; instances scale badly; quantification over all tuples is the theorem | S:7 (sparse-cofactor recurrence hunt) vs G: skeptical, "implication-ladder with a worse generating function" |
| 10 | HC4 => JC2 Hessian bridge (Meng–Yang; quartic by Ni) | Hessian conjecture in 4 variables implies JC2 via doubling | S23 / G12 (status only) / — | First quintic module and constant-linear `5 -> 4` Schur descent executed in `xmodel/sol-hc4probe.md`; exact JC2-sector verdict `NO LEVERAGE` | Degree >= 5: homogeneous layers can cancel; cone condition insufficient; no later observable is presently named | LARGEST DISSENT: S:9 — his original top pick; G folds it into the dead Zhao ladder and gives no score |
| 11 | Mathieu / GMC / Poisson / Zhao image-conjecture ladder | Prove a stronger analytic statement implying JC | S27 / G11+G12 / F6 | Dead as a route: Mathieu false on SU(2), PC(2) false, GMC(n>=3) false (Long 2026); abelian DvdK lemma already consumed | Keller gives one bilinear identity, not an infinite moment tower; F6: "no n=2 traction anywhere" — consensus | — (refuted) |
| 12 | Face isolation / p-adic multinomials (Wilson GMC(2) technique) | Choose a good prime; Kummer carries isolate one Newton face | S27 (part) / G20 / — | Analyzed, not executed as an independent proof | No integer m to send to infinity; surplus already is the face-count of the one identity | Low (both) |
| 13 | Dixmier DC(2) | Every endomorphism of A_2 is an automorphism; DC(2) => JC2 | S29 / G9 / F7 (part) | Tried in bounded slices (deg<=2 classified; deg-3 quantum/classical divergence real; deg-4 parked) | No Schur normal form in A_2; huge noncommutative centralizers; strictly stronger than JC2; deg-4 is a computational black hole | S:5 (Sp4-irreducible decomposition first); G: 3 if restarted |
| 14 | End(A_1) disproof route / Zheglov DC(1) audit (Bavula program) | Non-surjective endomorphism of A_1 would refute JC2 | S29 (audit part) / G10 / F7 | Partial (T1 640/640 passed; T2 symbolic replay of S6 Steps 4–7 named and NOT done) | No candidate non-surjective endo exists; even refuting Zheglov's proof yields no CE; 400-line private-calculus endgame | DISSENT: F flags "best counterexample-side alternative, modular tooling may transfer" vs G: "the paper's whole point is that none exist" — defensive recon only |
| 15 | Spectral surfaces / commuting PDOs in two variables | Build the 2D Burchnall–Chaundy dictionary, run DC(2) there | G26 / — / (F18-adjacent) | No | The dictionary itself is incomplete (Zheglov–Kurke–Osipov research program) | G:3 — "the DC2 degree-3 slice already was the experiment" |
| 16 | D-module / holonomic index | Holonomic index invariant against a Keller endomorphism | — / — / F18 only | No | "Untried, exotic" (F); no concrete invariant named in any input | Unscored; Fable unique find |
| 17 | BCW / Druzkowski / Yagzhev cubic stabilization | Stabilize to cubic-homogeneous/cubic-linear normal form in high dimension | S24+S25 / G14 / — | No (per G: correctly) | Raises dimension; target class now contains genuine CEs (Alpoge/Gao era); plane-origin subclass has no recognizing invariants | DISSENT: S:5 and S:4 (salvage subclass invariants) vs G:1 ("category error") |
| 18 | Graded / equivariant / GIT symmetry | Reduce to weighted-homogeneous Keller maps | S22 / G15 / — | Closed as CE hunt: Shaska 2026 — graded plane Keller maps are automorphisms for every weight pattern | A hypothetical CE has no G_m symmetry; orbit limits lose nonproperness | S:4 (stabilizer/orbit-limit check); G: closed |
| 19 | Char-p counterexamples + Witt lifting | Lift a char-p plane Keller collision to char 0 | S31 / G17 / F16 | Tried (Mondello char-2: 1,152/1,152 W2-obstructed; explicit Artin--Schreier `F_3` collision confirmed through `W_2=Z/9`; the cap-eight `p=109` gate stopped `NO-FROZEN-GRAMMAR`; every hypothetical exact AS109 lift now needs correction `y`-degree at least twelve; the completed-orbit polar conductor is dual-confirmed unbounded) | First-Witt obstruction is not universal. A same-seed tower lifts through every Witt level with growing degree/support and a nonpolynomial rational/restricted-analytic limit. No polynomial Keller gauge cancels its exterior polar divisor, so every fixed simultaneous map/gauge cap eventually fails, but this gives neither `A_infinity=0` nor nonexistence of an unbounded polynomial lift; the first live bounded partial-degree frontier is maximum twelve | Quantify canonical conductor growth, compare its exterior polar divisor with `A_infinity`, or test source-complete routing at the degree-twelve frontier; no automatic all-Witt-to-polynomial inference |
| 20 | Reduction mod p / p-curvature formalism (Tsuchimoto, Belov–Kontsevich) | Use the p-curvature/reduction-mod-p correspondence as the proof engine | F8 / S29 (part) / G9 (part) | No (as formalism) | The known bridge consumes PC(2)/JC(4), both now false (S,G); F elevates it as its own untried lane | Unscored by S/G as standalone; Fable framing find |
| 21 | p-adic injectivity / Hensel / model theory | Uniform p-adic bijectivity for almost all p; transfer to char 0 | S32 only / — / — | Partial: a hypothetical exact determinant-one `Z_109` lift has 109 Hensel sheets, generic degree `d>=109`, and tube split `E^109 x A_infinity`; the promoted maximum-eleven theorem plus Hensel noninjectivity force correction `y`-degree at least twelve | Existence remains open. Hensel does not kill `A_infinity`, descend a deck cycle, or force a degree congruence; `xy` lies outside the target field; the secant projector adds no off-sector rank. Completion alone does not supply a global rational action | S:4; live clients are negative-weight `A_infinity`, bounded analytic-gauge representatives, and rational graph-factor descent |
| 22 | Diophantine integral points / heights on fibers | Siegel-type finiteness forces preimages or properness | S33 / G39 (height-search variant) / — | No | Siegel classifies the wrong direction; uniformity across an unknown pencil inaccessible; no effective smallest-CE bound without JC2 (G) | S:3, G:2 — concordant low |
| 23 | Analytic global inverse / metric completeness / holomorphic analog | Properness via pullback-metric completeness; Hadamard | S34 / G36 / F14 (part) | No | Fatou–Bieberbach: the holomorphic analog is FALSE (G); dim-3 CE kills every dimension-free metric slogan (S) | S:4, G:1 — concordant low |
| 24 | Real JC / Pinchuk deformation | Deform a Pinchuk map to constant J, or use real global-injectivity theory | S35 / G29 / F14 (part) | No | Pinchuk J is nonconstant; complex collisions need no real points; SOS orthogonal to complex emptiness | S:1 proof / 3 disproof-recon, G:3 — concordant low |
| 25 | Fiber monodromy / dessins / Hurwitz passports | Riemann-existence constraints on the td-sheeted \hat g | S12 / G19 / F12 | Tried on residue-A: all 169 passports admit transitive identity tuples — no kill | Riemann existence is generous; single-cover passports ignore the second coordinate and Jacobian contacts | S:7 for the coupled two-coordinate branch-cycle CSP |
| 26 | Primitive-monodromy group bound on td + function-field Galois | Group theory forbids primitive G <= S_td for large td, or forces blocks | S15 / G23 / F12 (part) | Partial structural theorem: every proper block forces a non-`A^2` étale sandwich with missed branch divisor; codim-1-image and intermediate-`A^2` minimality bridges are closed | Primitivity remains open; neither minimal td nor topology presently rules out the forced sandwich; pi_1 of curve complements is a classical monster | First exact successor: rule out block index `d2=2`, or bank a consistent sandwich model |
| 27 | Links at infinity / splice diagrams / plumbing | Exclude every splice diagram compatible with a Keller map | S11 / G21 (part) / F4 | Tried on residue-A (one x-side branch died; template survives) | Admissible diagram ≠ algebraically realizable; genus/moduli explosion (F4); Egorov's 5-sheet exotic cover shows topology alone insufficient (G) | — (tried) |
| 28 | Log surfaces / BMY / log-Kodaira of the resolved pencil | Log-Chern and adjunction inequalities kill the boundary configuration | S14 / G21 / F5 | Partial — BMY marked NEEDS-DATA; Euler/genus ledger "passes identically" (the predicted non-result, G); the dual-pencil raw defect is not the proposed finite effective divisor | B/x-resolution tails unpinned; kappa-bar(C^2) = -infty blocks naive BMY (G); the corrected signed jump cycle has no licensed GRR endpoint; F5: "non-rational-fibration cases" open | S:6 (complete one survivor to all minimal tails and test) |
| 29 | LND / Hamiltonian-derivation completeness / commuting frames | Prove D_P locally nilpotent or the dual frame integrates to G_a^2 | S18+S19 / G22 (part) / — | Scoped gate complete: `kappa(P)=[div V] mod D_P` is classical Gauss--Manin data and vanishes iff a mate exists; exhaustive recurrence proves no mate for all `P=x+x^n y`, `n>=2` | The quotient is an exact reformulation and has no known universal receiver; LND-ness with a slice is essentially the conjecture again; Rentschler classification loops back to JvdK (G) | S:6 and S:5; G:4 (via the ML-invariant experiment) |
| 30 | Affine-surface classification / Makar-Limanov invariant / exotic surfaces, cancellation | Exclude the forced intermediate normal surface in a block étale sandwich | S40 / G22 / F5 (part) | Partial: block descent proves every proper block surface is necessarily non-`A^2`, with nonempty branch divisor missed by an étale open image | `Y isomorphic to A^2` is impossible rather than a recognition target; ML alone does not see the embedding or exclude the sandwich | Study the forced non-`A^2` surface, first at finite-flat block degree two, jointly with Avenues 26/31 |
| 31 | Integrality / Zariski Main / etale finiteness (Rees valuations) | Prove x,y integral over C[P,Q]; finite etale covers of A^2 are trivial | S16 only / — / — | Two concrete scoped clients: sharp K00 filtration `J^5 subset I subset J^2`; block normalization gives a finite-flat second map and étale nonproper first map | Global finiteness remains exactly the missing assertion; ZMT exposes, rather than removes, the forced branch/boundary divisor | Compose the block discriminant with `A(F)` and test the degree-two sandwich; use K00 filtration only at its exact source scope |
| 32 | Off-diagonal collision ideal / injectivity route (Cynk–Rusek, Ax–Grothendieck) | Show the saturated collision ideal is empty; injective => automorphism | S17 / G25 / F13 | Partial, now with a dual-confirmed exact accelerator: for any Keller secant matrix `A`, `I:Delta=I:Delta^infinity=I+(det A)`, so the off scheme has a three-generator presentation without saturation | Proving the three-generator ideal is `(1)` is still injectivity/JC2. Naive projective-CI connectedness is dual-confirmed `COSTUME`: `Z`-torsion can carry connectivity and honest off closures may attach only at infinity. A useful client needs a named bounded family or source-derived saturated boundary datum | DISSENT remains on promise: exact software simplification, no degree/support bound or new global obstruction |
| 33 | Global symplectic exactness / action residues / generating functions | f dg − x dy and g df − y dx are exact; promote residue pins to a global lemma | S36 (part) / G27+G40 / — | Ordinary action-residue gate run: `P dQ-x dy=dS` polynomially, so untwisted divisorial residues are automatic `COSTUME`; pins remain bookkeeping | Poincare lemma yields primitives for CEs too; any revival needs a genuinely twisted/client-specific class; generating functions local, mixed-coordinate (G40) | G:6 — his original highest untried; G:3 for the generating-function chart; consonant with Sol's adjacent 7 |
| 34 | 2D tangent-sweep / pole removal / rational Keller maps (Vitushkin-type) | Adapt the dim-3 sweep mechanism, or clear poles of a rational etale map, inside two variables | S36+S38 / G16 (mechanism inventory) / — | No | Ramification divisor has nowhere to go in 2D; no spare coordinate to absorb a denominator; graded plane maps already automorphic | DISSENT: S:7 (sweep ansatz) and S:6 (pole removal) vs G:2 for the descent framing |
| 35 | Descent of dim >= 3 counterexamples to the plane | Restrict/quotient/project Alpoge–Gao–Gallagher maps | S37 / G16 / — | Recon and negative controls only | Every known mechanism is structurally 3-dimensional (forget-a-root, weighted lifts, duality); Picard obstruction Z/(d−2); constant 3x3 det gives no constant 2x2 minor | DISSENT: S:6 vs G:2 ("tourism") |
| 36 | Guided counterexample search: SAT / fewnomial / sparse supports / small heights | Impose J=1 plus an explicit collision on structured supports | S47 / G18+G39 / F15 | DISSENT on status: F "tried/refined (the books)"; S Partial; G "not tried as a search" (probes tested reduced systems, not random maps) | Below the GGV cutoff theorems already emptied the region; above it the space is enormous and CEs form a thin locus; F: "combinatorics past frontier" | S:6 sparse-algebra / 1 generic-ML; G:2 (worthwhile only as a GGV bug-hunt) |
| 37 | Finite-field census of Keller maps | Enumerate maps over F_q, count bijections, look for extras | G31 / S46 (diagnostics only) / — | No (as census) | Char-p extras will not lift (Mondello lesson); enumeration dies past tiny degree | G:2 |
| 38 | Tropical geometry beyond Newton polygons | Tropicalize the coefficient ideal / the map as PL geometry | S21 / G33 / — | No | Prevariety overapproximates; saturation lost in initial degenerations (S); "no tropical invariant without a Newton name" (G) | Mild DISSENT: S:5 vs G:2 |
| 39 | Cohomological cluster: K2 reciprocity, motivic / A^1-degree, Hodge, anabelian, prismatic | Some cohomology class obstructs a nonproper etale endomorphism | S20+S41 / G34 / — | No | No vanishing theorem attached to J=1; local A^1-degree is +1 everywhere and blind to sheets (S); "refuse to start until a 10-line class is written" (G) | S:3 and S:3, G:1 — concordant low |
| 40 | Free-associative / noncommutative Jacobian lift | Invoke the free Jacobian theorem, abelianize the inverse | S30 only / — / — | No | Commutative det-1 does not imply free invertibility; the correction problem is the whole problem | S:2 |
| 41 | Naive scaling deformation to the linear part | F_t = t^{-1}F(tx) connects to the linear part | S39 only / — / — | No — and should not be pursued | The dim-3 CE deforms to its linear part: the principle is falsified | S:1 |
| 42 | Markus–Yamabe / Hurwitz vector-field realization | Convert Keller maps to vector fields with spectral hypotheses | G32 only / — / — | No | The arrow points the wrong way; constant J does not give the Hurwitz spectral condition | G:2 |
| 43 | Ritt decomposition / composite coordinates | Force f Ritt-prime, reduce composite cases | G30 only / — / — | No | Abhyankar divisibility already coarsely primes both degrees; collapses to GGV after Aut-reduction | G:3 |
| 44 | Moskowicz "no prime td" | Claimed exclusion of prime covering degree (arXiv:2407.13795) | G37 only / — / — | Source-audited, **REFUTED-AS-PROOF**: the first case consumes a false universal degree-two implication; the headline is unestablished, not disproved. The second case repairs independently to `xy in C(P,Q) => automorphism` | Exact Kummer controls satisfy the alleged rare property in every degree; they are not Keller maps. A hypothetical exact AS109 lift separately forces `xy` outside its target field, so the repaired client also stops | Closed as proof input; retain only the repaired membership theorem and the general prime-degree question |
| 45 | Differential Galois / Liouvillian inverse | Kovacic-style analysis of the inverse's ODEs | G38 only / — / — | No | The inverse PDE is tautological; univariate reduction reproduces Zoladek A.7 | G:3 |
| 46 | Lean / AI formal certification vs axiom-laundering | Formalize the pipeline, or "AI will settle JC2" | S48 / G35 / — | S: Tried at the leaf/identity layer (two Lean identities, no sorry); G: not a route | Formalization cannot manufacture the finite universe; laundering an axiom is the failure mode; watched cranks dumped | S:5 rigor / 1 discovery; G:1 as standalone |

---

## 2. Unique finds (approaches surfaced by only one model)

**Sol only (9):**
- S16 Integrality / Zariski Main / Rees-valuation test of finiteness (row 31).
- S19 Commuting frames / polynomial G_a^2-actions (folded into row 29).
- S20 K2 tame symbols / global reciprocity as a concrete program (row 39; Grok's G34 name-drops K-theory only to dismiss it).
- S30 Free-associative / noncommutative Jacobian lift (row 40).
- S32 p-adic injectivity / Hensel / model-theoretic transfer (row 21).
- S33 Siegel integral points / thin sets on fibers (row 22).
- S38 Rational pole removal, Vitushkin-type (row 34).
- S39 Naive scaling deformation — documented as a dead end with the dim-3 CE as refuter (row 41).
- S41 Motivic / A^1-degree / homology at infinity as a specific invariant (row 39).
- Semi-unique: S23 HC4 => JC2 as an *executed program proposal* with a concrete quintic experiment — Grok records HC(4)'s survival as a status fact only.

**Grok only (6):**
- G30 Ritt decomposition / composite coordinates (row 43).
- G31 Finite-field census (row 37; Sol's S46 is diagnostics of already-reduced systems).
- G32 Markus–Yamabe / chain-realization vector fields (row 42).
- G37 Moskowicz "no prime td" claim triage (row 44).
- G38 Differential Galois / Liouvillian inverse (row 45).
- G40 Lagrangian generating functions / 2D Hamilton–Jacobi (row 33).
- Semi-unique: G23's primitive-group-database td bound with the Orevkov (48,64) negative control — Sol's S15 is the adjacent function-field Galois version.

**Fable only (2):**
- F18 D-module / holonomic index (row 16) — the one clean Fable-unique lane; Grok's G26 (commuting PDOs) is adjacent but a different mechanism.
- F8 Reduction mod p / p-curvature (Tsuchimoto) elevated to a standalone formalism lane (row 20) — Sol and Grok mention the correspondence only inside their Dixmier entries, as a bridge that now consumes false statements.

---

## 3. Consensus + dissent

### Consensus (all three, or both scorers, agree)

1. **The tried core is stuck on source/transport, full landing/coverage, and an absolute ceiling.** Fable's old shorthand was "stuck: G1/G2/G5 + residue-A l+ dichotomy"; Grok: REDUCTION.md — no theorem landing arbitrary Keller pairs in the books, no td bound; Sol: "two global bridges" (universal landing/coverage and a finite bound). The surveys shared the diagnosis but not a clean vocabulary: hybrid transport is `G2-PSC`, while bounded delay after residue-A is `G2-BD`. All three also agree more brute force inside a filed sector will not repair the global gaps (Sol: "will not repair either gap"; Grok: "local maximum"; Fable: "stuck").
2. **The Zhao/Mathieu ladder is dead for n=2 leverage.** Long's 2026 refutations (Mathieu on SU(2), PC(2), GMC n>=3) close the prove-something-stronger instinct; F6's "no n=2 traction anywhere" concurs.
3. **AM was run on the wrong object** — Sol and Grok independently and exactly agree: residue-A fibers are multi-place; the one-place theorem applies to A(F) components, which are unpinned.
4. **Char-p is tried; the odd-prime first-Witt refinement has now fired positively.** The smallest typed `F_3` Artin--Schreier cap has a confirmed `W_2` survivor and a confirmed growing-support lift through every Witt level. The open extension is uniform support/degree and a characteristic-zero polynomial limit, not another unrestricted finite-level lift.
5. **Monodromy passports alone are too generous.** Both scorers: Riemann existence fills everything; the untried half is coupled/global (row 26).
6. **Concordant-low band** (both scorers <= 4, no advocate): real/Pinchuk, holomorphic analog, cohomology slogans without a named class, finite-field census, free lift, Markus–Yamabe, naive deformation, tropical-as-renaming (Grok side), integral points, formalization-as-route.
7. **Disproof lanes that are not fantasies** (Grok's list, consistent with Sol and Fable): a nonempty GGV family that algebraizes; a certified germ at infinity that algebraizes; a char-p pair that lifts through all levels with uniformly polynomial complexity. Current bounded results still return “consistent with JC2 true.”

### Dissent

1. **HC4 => JC2** — Sol 9/10, his single best experiment; Grok treats the whole ladder as a "documented massacre" and does not score it. Fable silent. This is a genuine disagreement about whether HC4's survival is signal or survivorship.
2. **BCW cubic stabilization** — Sol 5/4 (isolate the plane-origin subclass with new invariants) vs Grok 1 ("category error that still appears in survey talks").
3. **Dim-3 descent** — Sol 6 (invariant-surface/rank-two-target sweep) vs Grok 2 ("the silence looks structural"; "tourism").
4. **Collision ideals / injectivity** — Sol 7 (Fitting-ideal experiment on open_8_28_c2) vs Grok 2 ("the axiom IS JC2"). Fable F13 sides with the difficulty: "reduces to injectivity, no handle".
5. **A(F)-as-the-object** — Sol 8 (his second-best plane experiment) vs Grok "I would not start here". Fable F11 leans Sol: adjacent repo material, undeveloped.
6. **End(A_1) / Dixmier disproof** — Fable: "best counterexample-side alternative"; Grok: no candidate endo exists and auditing Zheglov is defensive recon; Sol: bounded slices plus expert-gated endgame gaps.
7. **Tropical** — Sol 5 (coefficient-ideal tropicalization retaining saturation) vs Grok 2 (renaming).
8. **Tried-status disagreements**: formal inverse (Sol Partial via conjE vs Grok/Fable Not tried); guided search (Fable "tried/refined — the books" vs Grok "not tried as a search" — they mean different objects: book-guided vs random/SAT); valuation theory (Fable "untried; field" for the Favre–Jonsson tree framing vs Sol/Grok counting the campaign's blowup machinery as tried).
9. **Scoring philosophy** — Sol scores information yield of the first experiment; Grok scores endgame probability. Sol's 9 (HC4) and Grok's 6 (symplectic primitives) are each survey's top untried number and are not on the same scale.

---

## 4. Historical ranked untried shortlist (2026-08-21; superseded)

This ranking records the dated surveys' inputs and is not the live queue.
Items 1 and 2 have since run at their named first gates and stopped
`COSTUME` and `NO LEVERAGE`, respectively.  Ranking originally weighed
cross-model presence, both scores, and Fable's flags; experiments below are
the surveys' own specs, reconciled where both wrote one.

**1. Global symplectic primitives / action residues at infinity** (row 33; G27 6/10 — Grok's top untried; Sol's S36 sibling 7/10).
*Experiment (Grok's spec):* For the elementary automorphism (x, y+x^n) and a tame product of two elementaries of degrees 3,4, compute the polynomial primitives F,G of f dg − x dy and g df − y dx explicitly and their polar residues at infinity in the standard compactification; then do the same formally for the residue-A leading pair without tails. If the polar divisor of F is already illegal for the (2,3) genome, that is a leading-form kill independent of Sigray; if it matches, the primitives see no more than J=1 — the experiment is designed to detect "J=1 in costume".

**2. HC4 => JC2, degree five** (row 10; Sol 9/10 — highest single score in any survey; Grok dissents on the ladder instinct — dissent priced in, ranked second not first).
*Experiment (Sol's spec):* Reproduce Ni's quartic cone/Schur argument symbolically, formulate the exact quintic obstruction module, and decompose its first uncancelled term under GL_4. Strongest adjacent live conjecture with a clean implication to JC2.

**3. Primitive-monodromy td bound + coupled two-cover CSP** (rows 25–26; G23 5/10 — his best cheap test; Sol 7/10 coupled CSP + 5/10 Galois closure; Fable F12: untried, "td IS the covering degree").
*Experiment (reconciled, two stages):* Stage 1, Grok's weekend GAP/Magma script — assume ramification of \hat g supported at <= N places at infinity, Riemann–Hurwitz, transitivity, primitivity; enumerate types for td=6,7,8,9 against the primitive-group databases, with Orevkov's (48,64) td=9 configuration as built-in negative control. Stage 2 (only if the control disciplines rather than kills the idea), Sol's coupled CSP: encode both coordinate fibrations, all infinity contacts, and the Jacobian pairing as one finite branch-cycle CSP on the mu=6 residue-A data.

**4. Jung–van der Kulk cusp-avoidance negative control** (row 5; Sol 4, Grok 5, Fable F10 untried — the most cross-model-consistent moderate pick).
*Experiment (Grok's spec — already written in xmodel/sol-lateral3.md and never run):* Enumerate reduced Jung words of length <= 6 over Q and F_101, triangular exponents 2..8, and test whether any word's (m,n)-initial face lies in the cusp ideal (d^m U^n − c^n V^m) for coprime 2 <= m < n <= 13. A hit kills the degree-lowering strategy in range; no hit is evidence for the cusp-avoidance lemma, which is the actual theorem. Sol's shear-divisibility enumeration on the smallest GGV corner families is the same test run from the other end.

**5. 2D tangent-sweep / pole-cancellation saturation** (row 34; Sol 7 and 6 — his best disproof experiment; Grok scores only the descent framing, at 2 — dissent noted).
*Experiment (Sol's spec):* Write the lowest-degree plane tangent-sweep ansatz with one controlled pole, impose polynomiality and determinant one as divisibility equations, and compute the saturation. Either a survivor reveals a new 2D mechanism, or a short unit identity documents the two-dimensional no-go — valuable either way.

*Honorable mentions:* Zheglov T2 symbolic replay (F7 + G10 — the named-and-not-done CE-side audit); Makar-Limanov invariant of the residue-A leading graded ring (G22, one afternoon, 4/10); Rees valuations / integral dependence on one complete boundary book (S16, 6/10, Sol-unique).

---

## Provenance note

Fable's input list (verbatim source for F1–F18, delivered with the merge charge): 1 Newton-polygon corner-cutting (GGV-Horruitiner); 2 Sheet-number ladder (Sigray frame); 3 Valuation theory/dicritical divisors (Abhyankar, Favre–Jonsson); 4 Links at infinity/splice diagrams (Neumann, Cassou-Nogues, Orevkov); 5 Open-surface/log-Kodaira (Miyanishi, Kaliman); 6 Mathieu subspaces/Zhao image conjectures; 7 Dixmier route: attack End(A_1) directly (Bavula program); 8 Reduction mod p/p-curvature (Tsuchimoto); 9 Formal-inverse combinatorics (BCW tree formula, Wright, Singer); 10 Aut(A^2) structure/polydegree (Jung–van der Kulk, Furter); 11 Nonproperness/asymptotic variety (Jelonek); 12 Monodromy/Galois of generic fiber (Razar, Wright); 13 Ax-Grothendieck/injectivity route (Cynk–Rusek); 14 Analytic/dynamical (Fatou–Bieberbach, Pinchuk constraint); 15 Guided counterexample search beyond frontier; 16 Char-p analogue lifting; 17 Formal-germ certification + algebraization (DEPTH-STAB/Newton/e+); 18 D-module/holonomic index.

Status flags in the table quote each survey's own words; where a Fable status ("untried; field") conflicts with Sol/Grok ("tried"), both are shown and the conflict is listed in section 3. No approach, original survey experiment, or score in the dated inventory originates outside the three inputs; post-survey statuses and corrections are explicitly marked as later campaign evidence.
