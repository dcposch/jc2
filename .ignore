# Independently owned formalization tree: campaign-wide search tools must not
# descend into or enumerate it. This is intentionally stronger than a Git
# ignore rule; ripgrep and compatible tools consume `.ignore` directly.
jc2-lean/
