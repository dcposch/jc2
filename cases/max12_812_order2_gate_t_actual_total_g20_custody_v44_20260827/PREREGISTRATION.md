# ACT-TOT-G20 preregistration

Date: 2026-08-27

This case freezes the minimal documentary lemma needed by the next strict
unit-load unique-`AC` contact:

> **ACT-TOT-G20.**  The current actual-total producer constructs all seven
> unspecialized general-`rho` raw rows through grade 20, before any face
> quotient, from the pinned Kummer source series, all three delayed loads,
> the standard target schedule, and the frozen 569 tails.

The exact-Q and `F_65521` lanes must run on distinct registered AWS hosts.
The producer must fail closed unless all of the following hold:

1. the V33 grade-18 actual-total builder, the reviewed V35 grade-19
   continuation, the sparse replay engine, the V28 literal row builder, the
   typed parser, and the byte-exact 569-tail file match their pinned hashes;
2. the grade-20 source extends the V35 general-`rho` construction exactly in
   every row and every coefficient through grade 19;
3. after, and only after, the seven general-`rho` rows exist, their unrelated
   ordered-`a1`, `rho=0` face agrees with all seven frozen V33 grade-18 rows
   and all seven frozen V35 grade-19 rows;
4. every grade-20 row is nonzero, sigma-weight homogeneous of weight 20, and
   even in `rho`; every source/load family has the correct terminal jet;
5. the exact rational canonical hash of each grade-20 coefficient and of
   each whole row truncation through grade 20 is recorded, together with the
   serialized lane hashes and exact term/support census; and
6. the second-host modular rows are the coefficientwise reductions of the
   exact-Q rows.

The grade-20 rows are target-free because the first target is `mu2` at grade
28.  The target schedule is nevertheless part of the pinned construction.

A pass proves custody only.  It does not by itself prove the formal
total/charged-`D1` source identity, finite-jet fidelity for a contact,
emptiness, contact coverage, the ramified fibre, either global `G2`
obligation, Gate T, order two, maximum twelve, JC2, or a counterexample.

