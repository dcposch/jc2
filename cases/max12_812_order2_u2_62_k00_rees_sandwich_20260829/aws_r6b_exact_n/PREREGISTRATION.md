# K00-REES-SANDWICH exact mixed-power lane

- Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`.
- Registered lane: `K00-REES-SANDWICH-EXACT-N-R6B-20260829`.
- Registered host: campaign instance `r6b`,
  `i-0f089e64c378f5da3` (`r6i.4xlarge`, 16 vCPU / 128 GiB).
- Frozen source: the unloaded characteristic-zero prelude with SHA-256
  `5b0a77e6d16df30de2df344862fc4316aedcc8aeabf1ec81e23d7881ce67ee7a`
  and the 28 already reviewed exact multiplier polynomials certifying
  `f0^5,f1^5,f2^5,f5^5 in I`.
- Ring and ideals:
  `R=Q[d0,...,d5]`, `I=(r1,...,r7)`, and
  `J=(f0,f1,f2,f5)` in the exact triangular normal coordinates of the
  reviewed unloaded-radical theorem.

## Question and exact finite reduction

Find the least `N` with `J^N subset I`.  The four replayed fifth-power
identities prove `J^17 subset I` by pigeonhole.  For a fixed degree `n`, every
monomial in the four generators having an exponent at least five is already
in `I`; hence it is enough to reduce the monomials

`f0^a0*f1^a1*f2^a2*f5^a5`, with `0 <= ai <= 4` and `sum ai=n`,

against one exact characteristic-zero standard basis of `I`.  Start at
`n=16` and descend only while all such survivors reduce to zero.  Stop at the
first nonzero exact normal form.  If that first failure is degree `n`, the
least containment exponent is exactly `n+1`.  The reviewed individual
nonmembership `fa^4 notin I` is independently replayed as a fail-closed lower
endpoint, so the search must stop by degree four.

The same lane also replays `I subset J^2`, `I not subset J^3`, all four
fifth-power identities, a source mutation, and a certificate mutation.  At
`S=T=0` it checks directly that every row restricted to
`e0=4e2, e5=-2e1` has normal order at least three, whereas the degree-five
initial form `e0^5` restricts to `4^5*u^5 != 0`.  This is the registered exact
discriminator showing that the ideal generated by the seven degree-two row
initials is strictly smaller than the full `J`-adic initial ideal.

## Resources and typed endpoints

- Exact `Q` only; no modular or `msolve` output is accepted.
- `CAPRUN/v1`: 1,800 wall seconds, 1,800 CPU seconds, 32 GiB aggregate RSS,
  0.10-second RSS sampling, five-second TERM grace, closed stdin.
- The route runner fails closed outside Linux, outside Amazon EC2, on any
  instance other than `i-0f089e64c378f5da3`, on a lane-tag mismatch, on a
  payload hash mismatch, on reused output names, on a non-normal CAPRUN
  lifecycle, or unless the unique terminal PASS marker is present with no
  FAIL marker.
- `PASS_K00_REES_SANDWICH_EXACT_MINIMAL_N` is exact engine-trust evidence for
  the least exponent and for strictness of the seven-quadric initial ideal.
- Any cap, parser error, missing endpoint, lifecycle error, or mutation-control
  failure is `NO VERDICT`.

This lane does not compute a filtered standard basis or elimination quartic;
the live Fable hostile review owns that separate producer-unreviewed claim.
It makes no loaded, cell, arc, map, closure, or JC2 assertion.
