# Scope erratum: sigma-45 `A^-3` PASS is a strong normal-form slice

Date: 2026-08-26

Status: **CONTROLLING SCOPE REPAIR.**

The immutable producer run and `RESULT.md` prove the exact identity

```text
[sigma^45]H3=-m^3/16
```

in their displayed moving-discriminant plus complementary `R@15/S@30`
normal form.  They do **not** prove that this normal form exhausts every
source arc through grade 45.

The omitted family is the two-dimensional transverse K2 kernel/root-
splitting series at relative sigma orders 1 through 14.  In particular a
first kernel term at relative order six has quadratic valuation twelve,
so its source contribution occurs at absolute grade `30+12=42` and ties
the delayed loaded affine face.  Such a term cannot be removed using only
the unloaded source-zero K2 argument.  A first term at relative order
seven contributes at grade 44.  Their correction series and every load
mode through grade 45 must be replayed before an exhaustive source
exclusion can be claimed.

Arbitrary moving-discriminant tangent series `a(sigma),e(sigma),m(sigma)`
also require either explicit retention or a proved two-sided formal
absorption map.  The current client retains only the leading `a` motion
and constant `e,m`.

Therefore the current PASS is a source-typed invariant and a regression
oracle, not a correction-complete `A`-face theorem.  The `A` face, order
two, and JC2 remain open.  The immediate successor is the loaded K2 kernel
client through grades 42--45, split by the first nonzero kernel order and
projective kernel chart.
