# Affine-Faber `A`: mixed delayed-load/discriminant sigma-45 certificate

Date: 2026-08-26

Preregistered dual-AWS exact-source identity client for the repeated-root
`A` null tower on `D(p*m*K10)`.  It uses the correction-complete local
normal form after all rows below sigma 45: exact moving-discriminant
tangents, the arbitrary quartic complementary linear polynomial `R` at
sigma 15, the arbitrary normal complementary linear polynomial `S` at
sigma 30, every effective delayed-load jet at sigma 42--45, and every
`mu2` target jet at sigma 42--45.  It reconstructs all seven complete
frozen ordinary-Faber rows and tests the exact moving-root analytic
`A^-3` combination.

Preregistered endpoint:

```text
A_SIGMA45_TCOEFF_CONTROL=1
A_SIGMA45_H3_LOWER_ZERO=1
A_SIGMA45_CERT=1
A_SIGMA45_ENDPOINT=PASS_COMPLETE_SOURCE_H3_MINUS_M3_OVER_16
A_SIGMA45_DONE=1
```

The exact-Q lane is the producer.  F65521 is an independent software
control only.  No standard-basis/radical inference from the prime is
allowed.

Scope: fixed delayed-load ray, repeated-root `A` tower with first cubic
normal `m` nonzero, and grades through sigma 45 only.  The `m=0` face,
later sigma grades, terminal/Taylor rows, other load slopes, total fan,
order two, and JC2 are outside scope.
