SOURCE FREEZE — nonlinear-carry checkpoint V2 — 2026-08-25

Immutable diagnostic source.  V1 is consumed only at the pinned SHA in the
manifest.  This checkpoint does not classify the `/81` zero locus.
