# Exact left-kernel/cofactor certificate packet

This case freezes the successful AWS r4 run for the 106-by-105 receiver
matrix.  `PREREGISTRATION.md` and `SOURCE.sha256` are the immutable run
registration.  `RESULT.md` states the exact result and its scope.

The complete remote namespace is preserved verbatim at
`custody/ggv_lambda0_left_kernel_cofactor_r4_20260828T125900Z_r6d.pass.tar.gz`.
Its SHA-256 is recorded in `CUSTODY.sha256`.  The archive contains the raw 106
signed maximal-minor slots, both endpoint row-duals, exact replays, timing and
memory records, process-group telemetry, zero-swap preflight, output hashes,
and the uncancelled Fitting/content decomposition.

