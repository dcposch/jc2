# Preregistration: V11 central-vertex retirement and moving-load discriminator

Date: 2026-08-26 UTC

The reviewed exceptional-support theorem already separates the apparent
central remainder from the live projective problem.  On the reduced
common-cubic boundary, `(p,c)=(0,0)` gives every boundary coefficient zero;
it is the affine cone vertex and the irrelevant point for `Proj`, not a
strict coefficient-infinity leading centre.  Pin that reviewed theorem and
verify the zero coefficient vector exactly.  Do not launch a redundant
central-arc elimination.

For the actual smallest remaining V10 firewall, reconstruct the literal V8
multipliers and V9 universal witness.  Replace the fixed loads by the exact
ordinary full-load rows

```text
kbar=Lambda^6*k,
row 3 target=Lambda^15*mu,
row 6 target=Lambda^18*nu,
row 8 target=Lambda^20*(1+tau).
```

Verify all eight full-load row covariances under
`p=-3a^2,c=2a^3+h`, with load characters zero, and require specialization
`(k,mu,nu)=(0,2/3,0)` to recover the frozen 48-term V9 polynomial exactly.
Then apply the unchanged cleared V8 multipliers and scan every resulting
monomial in the weighted a-chart `wt(a)=1,wt(h)=3`.  Emit either:

1. a full-load uniform endpoint if every non-target term has nonnegative
   closed-corner margin and every equality term has a positive open
   `u/v/eta/T/H` slope; or
2. the complete first obstruction list (negative margins or nonstrict ties),
   which proves only that the unchanged witness is not uniform over load
   space and names the next correction.

Run forward/reverse exact traversals on two registered AWS hosts.  A
custody/hash/covariance/specialization failure is no verdict.  Treat `k,mu,nu`
as constant-field parameters; this does not cover loads that themselves are
formal series.  Nonpositive rescaled Lambda is deliberately a separate
Lambda-leading chart/order question and is not folded into the load test.
Another source chart, landing/accessibility, D1, and JC2 remain open.

