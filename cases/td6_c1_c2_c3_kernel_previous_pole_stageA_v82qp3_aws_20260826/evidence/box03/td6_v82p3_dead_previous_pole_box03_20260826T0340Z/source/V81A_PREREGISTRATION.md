# V81A preregistration — generic-center 33-axis joint transport

Target: over one square-zero ring over `E(C,V,U)`, compile the joint transport
source map for the 22 licensed q axes and all eleven dead-stretch axes.  The
producer must keep `(C,V,U)` symbolic before and after compilation, replay all
3,470 differentiated original pivot rows for every dead axis, reproduce all
eleven frozen V80B tables after specialization `(C,V,U)=(1,1,1)`, and compute
the exact joint rank/kernel and denominator support.

Success is a transport-stage source-incidence certificate only.  It does not
assert first/previous/current compatibility, a q/dead neighborhood, a family
kill, full TD6, SP-2, or JC2.  Every denominator factor outside `U,H,B3` is new
constructible-stratum debt and must be retained.

Controls: q15 exclusion; all 22 q RHS keys; matrix-derivative omission;
wrong dead exponent shift; original-row replay; symbolic-center noncollapse;
V80B point-table byte hashes; exact joint-kernel replay.
