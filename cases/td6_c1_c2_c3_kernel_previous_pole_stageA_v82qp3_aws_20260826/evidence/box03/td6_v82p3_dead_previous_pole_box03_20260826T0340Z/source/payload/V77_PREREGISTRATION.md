# V77 preregistration — q3 gamma dual discriminator

Object: the fixed source-typed A3 chart with

`p=t^15`, `q=t+gamma*t^3+t^25`, zero dead stretch, and frozen F1/pole
data, at `gamma=0`.  The exact coefficient field is `E(C,V,U)` and the
licensed open is `D(U*(C-3U^2)*B3)`.

The run differentiates the complete transport RHS, first-J matrix, and
genuine current P12 source identity.  The q3 source slot is
`('g','X',0,3)` and the direct derivative of `q'` is `3*t^2`.  It retains
the varying source multipliers (`lambda'`) and records every denominator.

Required controls:

1. exact replay of every dual original first row;
2. a direct-qprime omission run inside the same source, required to differ;
3. a lambda-prime omission negative control;
4. preservation of the frozen base P12 digest and 1,489-term base
   multiplier projection;
5. denominator radical contained in `U*(C-3U^2)*B3`.

Interpretation is deliberately narrow.  The base ideal already contains a
unit, so persistence over dual numbers is formally automatic.  A PASS is a
source-support/degree/denominator discriminator for q3 and a compiler gate
for the next untruncated simultaneous-modulus calculation.  It is not a
gamma-family, neighborhood, full-boundary, TD6, SP-2, landing, or JC2
theorem.

Execution is AWS/Linux only, independently on Box02 and Box03 under 12-GiB
caps.  Byte agreement is required after excluding registered provenance
lines.
