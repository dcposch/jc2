# V82 preregistration — cumulative transport/first Fitting gate

Rebuild the fixed source-typed A3 transport parameterization over
`E(C,V,U)` with all 33 licensed square-zero axes

`q2,...,q14,q16,...,q24,d6,...,d16`,

then compile the first X-band equations in the same ring.  Stack the exact
transport and first-stage dependent coordinates and compute their joint
rank, kernel, pivot axes, and denominator support.

Required controls: symbolic centers before/after; q15 exclusion; all direct
q-prime omission columns; all dead source-column omissions; all 3,470
transport pivot-row replays; all first-stage pivot source-combination
replays; the nine-row lower-triangular dead transport sentinel; exact zero
transport columns d10,d15; and reproduction of V78's zero q-only first
conormal.

Every nonunit denominator factor outside `U`, `H=C-3U^2`, and `B3` is new
constructible-stratum debt.  Success is a localized first-order Fitting
discriminator only.  It gives no previous/pole/current, second-order,
nonlinear-family, full TD6, SP-2, or JC2 conclusion.
