# V82S preregistration — d10/d15 first-stage kernel shards

For each of the two exact zero transport columns `d10,d15`, independently
rebuild the symbolic-center transport source module, replay all 3,470 pivot
rows, compile its transported global derivative into the first X-band rows,
and emit the exact first-stage dependent-coordinate column.  Run each axis
on two independent AWS hosts.

The reviewed V78 q-only first conormal is already zero.  These two columns
are the only new first-stage coordinates needed after the exact rank-nine
dead transport sentinel.  Single-axis results must be assembled by exact
row keys before making a joint rank/kernel statement.

Scope is the fixed source-typed A3 generic open `D(U*H*B3)` and first order
only.  No previous/current, second-order, nonlinear-family, full TD6, SP-2,
or JC2 inference is licensed.
