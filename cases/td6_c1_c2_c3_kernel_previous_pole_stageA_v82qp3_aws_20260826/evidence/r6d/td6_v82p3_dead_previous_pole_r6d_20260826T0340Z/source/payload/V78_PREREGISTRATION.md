# V78 preregistration

Object: one square-zero vector-AD run for q exponents
`2..14,16..24` at the generic fixed-A3 base.  q15 is excluded as the reviewed
lower target-shear gauge.

Proof-bearing gates:

1. exact source closure and Linux/registered-AWS preflight;
2. all 22 original transport RHS keys retained;
3. all 22 direct q-prime terms retained and a batched omission changes every
   exponent-keyed first-row column;
4. base ranks 3470, 38, 38, 25 where the later stages are reached;
5. all normalized pivot/source combinations replay exactly;
6. p12 mode replays the genuine 2,893-term P12 against original first rows,
   matches V32 at q2 and V77 at q3, and audits every denominator radical;
7. staged mode prints the joint previous/current conormal rank and exact
   kernel digest table.  If a previous derivative compatibility occurs, it
   stops fail-closed after freezing that earliest obstruction.

This is diagnostic at an already empty base.  It proves no polynomial family
or neighborhood theorem.
