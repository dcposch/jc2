# Corrected-Q8 rational-contact order-16384 successor

This AWS-only successor changes no byte of the active/frozen order-at-most-8192
package.  A hash-pinned adapter widens only the generator's accepted order list
to `16384`; all equations, contact coordinates, and final checks remain those
of the base generator.

Two distinct rational corrected-Q8 contacts are preregistered.  Together with
the separately frozen third rational contact through order 8192, simultaneous
PASS endpoints would give finite contact

```text
16384 + 16384 + 8192 = 40960 > 35582.
```

Conditional on the separately frozen bidegree residual bound and component
theorem, this would force at least one of the three rational Q8 contacts onto
an `H`-supported mod-127 source component.  It would not prove which contact,
formal identity, all-contact coverage, degree one, characteristic-zero
no-merger, or any trajectory conclusion.

## Frozen endpoint

The two Box02 V5 lanes completed rc zero under 128-GiB caps.  The separately
copied linear26 order-8192 lane is the immutable third input.  `replay.py`
checks every endpoint/hash, the exact factorization and distinct roots
`26,58,67`, and the strict arithmetic

```text
16384+16384+8192 = 40960 > 35582.
```

The earlier Box03 64-GiB attempts are retained as memory-failure controls.
The V6 384-GiB mirrors were terminated only after both V5 PASS endpoints were
complete; they are retained as superseded partial controls and contribute no
mathematical evidence.

The composed residual-component conclusion remains provisional because the
separate bidegree bound `35582` is still awaiting hostile review.
