# Historical factored-A impact matrix

The exact AWS identity control proves that the frozen control-2 factored A
source agrees with expanded B on `E1,...,E7,LT` and differs on `E8` by the
nonzero constant

```text
-25134148616192/43046721.
```

The Singular `list L=sat(...); L[1]` wrapper is **not** the cause: the
separate fleet-version return-type control proves that `L[1]` is the whole
ideal.  This matrix quarantines only clients of the malformed factored
control-2 equations.

| source / call | emitted endpoint | disposition | downstream impact |
|---|---|---|---|
| `control2_rees_20260826/compile_control2_rees.py`, `source_a`, emitted `poly E8` at line 12 | Box02 A tag `...control2_rees_20260826T004241Z_box02_A`; A SHA `912a6e27...` (`99a0ed11...` on the other tag; normalized `faee98eb...`) | **Affected.** V1 renderer is source-identical to v2 on this row.  Box02 solve was stopped; V1 wrapper separately fails its timing/stderr contract. | No promotable mathematical endpoint. `SOFTWARE_CONTROL.md` already forbids use. |
| `control2_rees_v2_20260826/compile_control2_rees_v2.py:297-306`, through `compact_row_string` at `:269-275`; emitted `poly E8` at line 12 | Box02 A tag `...rees_v2_20260826T005028Z_box02_A`; A SHA `d72c1e53...` (r6d compiler emission `2b416cb8...`, normalized `8775a483...`) | **Affected.** Registered Box02 Singular process was stopped before an endpoint. | No A mathematical verdict.  The source-review discussion claiming A/B equation equivalence is superseded for A only; that review explicitly based its computational verdict on B. |
| `control2_rees_Afast_20260826/compile_Afast.py`, pinned `base_A.sing` SHA `2b416cb8...` | Box03 tag `...rees_Afast_20260826T011021Z_box03`; printed contraction 398 and `GH[1]=1` | **Affected / immutable negative control.** The unit fibre is explained by the spurious nonzero base constant in `E8`. | Never promoted.  It may be cited only as a wrong-input software control, not a no-lift theorem or a check on B. |
| `control2_rees_source_identity_20260826/compile_source_identity.py`, deliberate comparison of `2b416cb8...` against `c905f1b5...` | r6d tag `...source_identity_20260826T012642Z_r6d`; stdout SHA `7baf7e60...` | **Unaffected diagnostic.** Its intended endpoint is the exact `E8` failure and displayed difference. | Establishes the quarantine boundary; no geometric theorem. |
| `control2_rees_corrected_A_20260826/compile_corrected_A.py` | Box03 LPDP and r6d DP tags listed in `RESULT.md` | **Unaffected.** All nine polynomials are extracted byte-for-byte from B; both direct-saturation endpoints mutually reduce to B's 35-generator fibre. | Supplies the corrected contraction/order corroboration for the B theorem. |
| v2 expanded B, certificate B, and their r6d outputs | B SHA `c905f1b5...`; certificate stdout SHA `d5317aac...` | **Unaffected.** They never consume the factored A equation. | The focused hostile review's B-only fixed-weight/support theorem remains valid. |
| tied `alpha=2 beta` toric-blowup compiler and older weighted-infinity compilers | separate sources/tags | **Not shown affected.** They use independent equation builders. Their list-wrapped `sat` calls are semantically complete on Singular 4.3.2. | The earlier stop of the tied A process on the list-wrapper suspicion was unnecessary; its source/result must be judged on its own merits, not quarantined by this control-2 `E8` defect. |

No top-level promoted theorem consumed the invalid A/A-fast endpoint.  The
only review text requiring correction is the A-source-equivalence portion of
`max12-912-order3-d1-double-root-control2-rees-v2-source-review-grok-20260826.md`;
its B-only result and the later B certificate review remain valid.

