# Promotion after hostile review

Different-model hostile source/certificate review returned
`CORRECTED_A_CONFIRMED` at SHA-256
`3e881064695525137ddc85e705b77be76638224806cb0589d85f568c81d2f5e6`.

## Promoted theorem

For the exact pinned finite control-2 charged equations with

```text
a=1, h=q2=k=nu=0, mu=2/3,
w(la,tau,rho,q1,q0,r2,r1,r0)=(4,1,1,22,22,30,30,30),
```

the full weighted initial ideal contains `la^20`.  Hence its intersection
with the torus where all eight displayed leading coordinates are nonzero is
empty, so no arc with that exact fixed axis, load, support, and weight has
such a leading coefficient.  This is certified by reviewed expanded B plus
direct-saturation A executions in two orders, with exact two-way ideal
reductions against all 35 B special-fibre generators.

This is not a second derivation of the eight charged equations: corrected A
copies them byte-for-byte from reviewed B.  It is not a moving-axis,
moving-load, nonzero-`q2`, other-support, other-weight, whole-cone/fan, D1,
or JC2 theorem.

The historical factored A `E8` and its A-fast endpoint remain immutable
negative controls.  The B-only theorem and focused B certificate review were
unaffected throughout.

