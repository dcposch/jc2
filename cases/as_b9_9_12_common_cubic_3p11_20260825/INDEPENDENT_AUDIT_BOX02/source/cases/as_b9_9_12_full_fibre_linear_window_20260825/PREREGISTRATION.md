# Preregistration — B9 normalized `(9,12)` complete linear window

Consume the complete `3^61` mod-729 family over the displayed B9 mod-243
point.  Retain exactly 55 P coefficients of total degree at most 9 and 91 Q
coefficients of total degree at most 12.  Solve every one of the 276
determinant rows through modulus `3^10`, with all nonlinear pair controls and
literal integer replay at each stage.  Stop at the first inconsistent stage.

The quadratic term first enters the divided carry at `3^10 -> 3^11`; no
claim beyond the linear window is licensed here.
