# Preregistration: V8 unit-axis covariance

Date: 2026-08-26 UTC

Reconstruct the exact frozen V8 witness and charged ordinary-tail rows.
Construct the universal rows at `p=-3a^2,c=2a^3+h`.  Verify all eight row
covariances under the preregistered source scaling, including target/load
terms, then transport the literal V8 multipliers.  Determine the common axis
denominator from every multiplier monomial; require it to be exactly `a^3`.
Verify the fully cleared polynomial identity, the discriminant factorization,
the `a=1` specialization, and the distinguished `a^3*Lambda^20` term.

Run forward and reverse exact traversals on two registered AWS hosts under
caps.  Failure of any coefficient/hash/target check is no verdict.  Success
is a unit-axis/fixed-load covariance theorem only; triple root, moving loads,
other source charts, D1, and JC2 remain outside scope.
