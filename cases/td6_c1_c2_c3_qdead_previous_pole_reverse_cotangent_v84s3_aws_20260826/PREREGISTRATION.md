# V84S3 preregistration — matched reverse presentation, repaired parent tag

Target: independently recompute the V84R2 block `(0,0)` previous/pole table
after reversing original source-row order, choosing maximum unit-base pivots,
and using the matched ascending parameterization.  Every pivot/dependent row
must replay from original source rows.  The exact table must agree between two
AWS hosts and with V84R2 before interpretation.

V84S and V84S2 remain negative controls.  V84S failed its unmatched triangular
contract; V84S2 failed the imported-parent tag preflight before algebra.  V84S3
repairs only that deployment boundary with an explicit parent compatibility
tag while retaining and restoring the registered outer tag.

Scope is the ten `Sym^2(q2,q3,q4,q5)` slots through previous/pole on the fixed
source-typed A3 common generic open.  This is a presentation control, not full
Spencer involutivity.  It gives no current, nonlinear family, TD6, SP-2,
landing, or JC2 conclusion.  AWS/Linux only; 8 GiB cap; six-hour timeout.
