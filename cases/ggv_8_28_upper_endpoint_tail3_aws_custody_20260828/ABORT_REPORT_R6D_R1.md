# Cutoff-three modular r6d r1 containment abort

Classification: **OPERATIONAL ABORT — NON-EVIDENCE**

The discovery-only modular `core` all-six lane started from the frozen
archive `b2dfa880...` in namespace
`tail3_core_mod_r6d_call_20260828T044020Z_380865_cutoff3_core_mod_r6d_r1`.
Its repeated analyzer, source/evidence manifests, host identity, Singular
hash, memory, disk, and zero-swap preflight all passed.  Six charts started
with the registered 40-GiB address-space and 21,600-second caps.

The live process-tree audit then found that GNU `timeout` placed each actual
Singular process in an unrecorded inner PGID.  The registered guard could
therefore undercount RSS and could fail to terminate the actual CAS.  The
lane was stopped fail-closed.  The coordinator stopped the six validated
inner PGIDs; the delegated stop independently validated and stopped the
remaining runner/guard group.  No unvalidated process was signalled, no
KILL was needed, final MemAvailable recovered above 490 GiB, and swap was
zero throughout.

Each chart log contains only its scope/chart/field census, a blank line, and
`halt 1`.  There is no worker rc, completed standard basis, `STD_SECONDS`,
`UNIT`, certificate, point, or terminal mathematical marker.  The literal
log text supplies no algebraic inference.  `RUN_ABORTED_CONTAINMENT_AUDIT`
is the only classification marker.

The remote namespace was hashed before transfer.  Its
`ABORTED_EVIDENCE.sha256` has SHA-256
`3e66e9e7be3c9dc02476a1cd269a936917e917ab4cedf14c3df8b47a2f7bfadd`
and verifies 29 payload files.  Repair requires a process topology in which
the actual Singular RSS and termination path are covered, plus a live
regression proving RSS inclusion and complete descendant death.  No relaunch
is authorized by this report.

