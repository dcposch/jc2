# Registration — `(9,12)` order-three `nu!=0` Belyi collision boundary

Date: `2026-08-24`  
Status: `PRODUCER-EXACT / HOSTILE DIFFERENT-MODEL REVIEW REQUIRED`

## Claim registered

On the reviewed order-three `k=mu=0`, `nu!=0` landing, no actual Keller
trajectory survives if the complete ramification divisor of

```text
B=3*f*g_z-4*g*f_z
```

is absorbed by `W=g^3-f^4=0`. This covers both distinct roots of `B` lying
in `W` and the double root of `B` lying in `W`.

## Scope firewall

This does not exclude the generic quadratic-Wronskian fibre, a single
`B`--`W` collision, a double root of `B` away from `W`, collision of the two
non-`1` critical values, any Taylor boundary, the order-one core, all
`(9,12)`, maximum-twelve automorphy, a counterexample, or JC2.
