# V84S2 preregistration — matched reverse row/column echelon control

V84S failed only because reversed source-row order was combined with the
parent's minimum-pivot/descending-parameterization triangular contract.
V84S2 is the source-honest correction: it processes source rows in reverse,
eliminates existing pivots in reverse variable order, chooses the maximum
unit-base pivot, and parameterizes variables in ascending order.  These two
orders are a matched pair.  Every pivot and dependent row is replayed from
the original rows.

The exact target remains only block `(0,0)`, the ten unordered pairs in
`Sym^2 span(q2,q3,q4,q5)`, through previous/pole on the fixed source-typed
A3 common generic open.  Agreement with V84R2 is a presentation control, not
full Spencer involutivity.  Any triangularity assertion, source replay,
nonzero exit, or table disagreement fails closed with no mathematical
verdict.

Execution is AWS/Linux only with an 8 GiB virtual-memory cap and six-hour
timeout.  The current base remains inconsistent; no current, nonlinear
family, TD6, SP-2, landing, or JC2 inference is licensed.
