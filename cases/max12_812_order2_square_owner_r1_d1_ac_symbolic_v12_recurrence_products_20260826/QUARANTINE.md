# Partial quarantine: unbounded symbolic `d=1` reading

Date: 2026-08-26T11:27Z

The dual-AWS V12 executions remain valid records of the frozen baseline-grade
calculation.  However, **Producer theorem 2 in `RESULT.md` is quarantined in
its claimed unbounded range** `a>=2,c=a+1,r>=a`.  It must not be promoted or
used downstream until the complete lower-load source support is replayed and
stratified.

A post-run source audit found that the shifted V12 client extracts only its
baseline absolute grades after substituting
`A=sigma^2*theta`, `C=sigma^3*theta`, and
`R=sigma^2*theta*eta`.  The full source also contains lower-load terms from
`k6*f^(3/4)`, including contributions with weights represented by
`sigma^(16+2r)*k6*R^2/L` and `sigma^(17+c)*k6*C/L`.  On the registered
`d=1` relation `c=a+1`, the latter has relative gap `7-a` from the `AC` face
and therefore enters or precedes that shifted face for `a>=7`.  The baseline
grade window cannot certify the resulting original higher-grade terms.
`k2`-derived support may enter at still later contacts and is under audit.

The normalized `r=1` calculation is not withdrawn by this specific finding,
but remains merely provisional and under hostile review.  No square-branch,
order-two, maximum-twelve, or JC2 conclusion follows from V12.
