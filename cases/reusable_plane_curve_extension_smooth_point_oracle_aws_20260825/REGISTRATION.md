# Registration

- Kind: reusable AWS-only standalone plane-curve census producer.
- Arithmetic engine: Singular; Python only validates/transcribes and audits.
- Acceptance: exact source/input/output hashes, disjoint full field coverage,
  one PASS marker per shard, no error tokens, and fail-closed aggregate.
- Mathematical firewall: `smooth > q+1` implies positive normalization genus
  only after geometric integrality is independently established.
- Max-12 client 1: pending exact curve table.
- Max-12 client 2: pending exact curve table.

