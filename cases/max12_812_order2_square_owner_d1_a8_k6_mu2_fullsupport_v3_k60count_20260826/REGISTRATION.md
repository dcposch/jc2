# Registration: D1 `a=8` V3 replacement-count repair

Date: 2026-08-26

Status: **PREREGISTERED DUAL-AWS SOFTWARE REPAIR.**

V2 performed the intended six formal-`k60` insertions but failed before CAS
because its postcondition forgot the one legitimate pre-existing occurrence
in the separate `D(k60)` ring.  V3 pins V1, V2, and both negative controls and
changes only the wrapper check to require

```text
old_before=6, new_before=1, old_after=0, new_after=7.
```

The generated Singular bytes are otherwise the V1 bytes with the same six
declaration/map replacements preregistered in V2.  The load equation remains
`k6=sigma*k61+sigma^2*k62` in the `V(k60)` client.

Run exact Q on Box03 and `F_65521` on r6d under 24-GiB, 600-second compile,
and 1800-second engine caps.  Retain every V1 source, row, support, target,
root, deletion, diagnostic, and endpoint sentinel unchanged.

Dual PASS is producer-tier only for fixed
`ord(A)=8,ord(C)=9,ord(R)>=8` on `D(p*k10)`.  No a>=9, square, order-two,
`(8,12)`, maximum-twelve, or JC2 conclusion is licensed, and the tied-load
Chebyshev/Pell survivor remains outside any blanket square-forcing claim.

