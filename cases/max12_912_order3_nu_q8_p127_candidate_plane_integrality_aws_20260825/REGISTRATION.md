# Registration: selected-Q8 p=127 candidate plane integrality

- Lane: `(9,12)` order-three selected-Q8 modular component bridge.
- Tier: producer-exact theorem for the explicit candidate plane curve;
  hostile different-model review required.
- Exact result: the pinned monic degree-190 `H(w,v)` is irreducible over
  `F_127(w)` and geometrically integral over `F_127`.
- Certificates: exact factor partitions `[2,188]` and `[1,3,186]`, disjoint
  proper subset sums, and smooth rational point `(71,50)` with `H_v=104`.
- Scope exclusions: no quotient membership/component, coordinate
  reconstruction, contact grouping, characteristic-zero lift, trajectory,
  maximum-twelve, or JC2 conclusion.
