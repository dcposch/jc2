# Preregistration: D1 a=8,d=3 target-shadow chamber split

Date: 2026-08-26

Status: **PREREGISTERED LITERAL-ROW EXACT-Q AWS PRODUCER WITH TWO FRESH PRIME CONTROLS.**

## Scope

Work only in the strict cell

```
ord(A)=8, ord(C)=11, ord(R)>=8
```

on the already licensed chart `D(p*k0*J)`.  Keep the complete source and
target jets through grade 38.  Do not reuse the falsified fixed-`z` bridge;
read the four odd literal Faber rows directly from each frozen high-memory
parent input.

## Controlling split

At the grade-28 row-1 load/target-shadow wall, require the exact identity

```
[sigma^28] Phi1 = (3/4) k60 c1.
```

The exact-contact condition `(c1,c0)!=(0,0)` is covered by

```
D(c1)  union  (V(c1) intersect D(c0)).
```

- On `D(c1)`, the row-1 equation forces `k60=0` scheme-theoretically.
- On `V(c1) intersect D(c0)`, the corrected moving-connection term vanishes
  because it is proportional to `C'=c1`.

## Terminal identity

Form directly from literal rows

```
S = Phi7 + (p(sigma)/4) Phi5
         + (3 p(sigma)^2/32) Phi3
         + (5 p(sigma)^3/128) Phi1.
```

Preregister the exact residual

```
[sigma^38] S = -(3/32) p eta k60 c1 b0,
S = 0 mod sigma^38.
```

Thus `S` vanishes modulo `sigma^39` on either branch.  In the full odd
equations the sole target is `-sigma^38 J/4`.  Require unit ideals on both
exact-C charts after adjoining the grade-28 row-1 equation, the terminal
coefficient, and inverses for the relevant leading C coefficient and
`p,k0,J`.

## Execution and firewall

Run exact Q on Box02 and independent F65519/F65521 controls on Box03/r6d,
with source hashes, time/RSS/swap custody, explicit reductions, and no qring.
Exact Q alone is the characteristic-zero proof.

A PASS closes only the named a=8,d=3 closed R-tail after its imported source
gates on `D(p*k0*J)`.  It does not cover `J=0`, `p=0`, `k0=0`, another
contact/equality chamber, a=7 or a=9, another D1 face, the entire square
component, maximum twelve, or JC2.
