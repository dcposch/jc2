# Gate T uniform contact naturality schema V46

Date: 2026-08-27

Status: **PRODUCER PASS; PROVISIONAL PENDING DIFFERENT-MODEL REVIEW OF THIS
SCHEMA/VERIFIER.**  The formula-level theorem has already survived Fable 5 hostile
review, but this implementation has not.  This file does not promote any endpoint or
the union of endpoint families.

## Result

`SCHEMA.json` is the normative, implementation-independent source schema requested by
the reviewed uniform-interface theorem.  It fixes the seven actual-total primitives,
the explicit coefficient-ring jet-reindexing map, the forced factors `2` and `4`, all
three load delays, all four target grades, the byte/canonical custody and census of the
569 tails, the total/D1 alias firewall, the shifted-root recurrence, the reviewed polar
inventory, and exact provenance/status for eleven endpoint families.

The schema carries the two review repairs explicitly:

1. coefficient extraction is justified by the displayed jet-reindexing map, not by
   bare sigma-adic continuity; and
2. cancellation can only raise row valuation, so a raw census is safe but
   over-retaining, while a useful moving-`P` ceiling requires the reviewed first polar
   grade.

The desk verifier returned

```text
PASS-UNIFORM-CONTACT-NATURALITY-V46-V0-V4
```

in 4.795 seconds on macOS 14.6 / arm64 with Python 3.9.6.  It checked:

- V0: exact schema, tail, compiler, theorem, and review hashes; the
  `36+54+58+81+89+120+131=569` census; load/target schedules; and selected literal
  compiler-semantics tokens;
- V1: independent total-formula and D1-formula truncated-series builds, followed by
  full evaluation of all 569 tails in all seven rows at one representative of each of
  the eleven endpoint families.  The three shallow representatives ran over exact
  `Q`; the eight deeper representatives ran over `F_1000003`.  All 2,247 compared row
  coefficients agreed, through terminal grades as high as 38;
- V2: the factor-`4`, factor-`2`, moving-`ell1`, terminal-`J`, stage-zero, `k2c` versus
  `k2load`, wrong-ceiling, analytic-only `(8,3)`, and `rho=0` mutations all fired or
  were rejected.  The moving Hensel recurrence was checked through depth 10 on both
  decks, and both shifted root orientations were triangularly inverted through depth
  3 on `D(rho)`;
- V3: the polar support table and exact finite-jet maxima at all eleven representatives
  plus the receding-`AC` sentinel `(20,21,20;T=38)`; and
- V4: 24 endpoint artifacts by exact hash, with lifecycle census ten promoted families
  and one confirmed-but-unpromoted family.  The `(8,3)` filename trap is rejected by
  requiring a null theorem authority and separately pinning the artifact whose status
  is only `PROMOTED ROUTE FALSIFICATION ONLY`.

For the requested grade-20 contact `(a,c,r;T)=(2,5,3;20)`, V3 reproduces exactly

```text
p/A/C=3, R=1, k10=2, k6=0, k2=0,
```

with `k6,k2` explicitly marked inactive rather than falsely claimed to occur.

## Exact scope

This producer verifies the schema interface and finite defenses.  The mathematical
all-grade identity is the explicit formal jet-map theorem, not an extrapolation from
the 2,247 finite comparisons.  The source identity uses no localization and works over
any `Z[1/2]`-algebra.  The shifted-root map separately uses `D(rho)`.

The schema does not reconcile the mixed theorem types/localizations of the eleven D1
endpoints.  In particular, `(a,d,r)=(8,3,8)` remains only
`confirmed_but_unpromoted`, so the eleven-family union is conditional.  Serial
`ACT-TOT-G22/G24/...` builders can leave the mathematical critical path only after
this schema/verifier survives a different-model review and each concrete linker has
mechanically checked its endpoint compiler semantics and provenance.

It says nothing about equality faces, positive-order leading loads, `k=0`, `rho=0`, an
unlisted contact, the six staged Rees charts, the terminal/Taylor receiver, the two
global `G2` obligations, Gate T, order two, maximum twelve, JC2, or a counterexample.

## Immutable evidence

```text
796eb0847e7b0bb7972c2fd7b3db10bd2dcfd2fd85c5bbaa47e3a239f8b02673  SCHEMA.json
8e49fc87ebeffb74e4c33f79ae248846c7de3ce4f4fb2ad07ac85751d8841ec7  verify_uniform_naturality.py
25a5fb3e0a8dff1a0d841823dfe44265775e793431445878ab49193c846d67ce  run_v0_v4/result.json
```

The hashes of all external inputs are normative fields in `SCHEMA.json` and are
rechecked by V0.  `FREEZE.sha256` pins the three artifacts above plus this report.
