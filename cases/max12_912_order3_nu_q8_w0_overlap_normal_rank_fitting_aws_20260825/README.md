# Raw-overlap normal-rank/Fitting split

This exact-Q AWS case computes the normal-rank Fitting stratification of the
corrected-Q8 six-row source along the raw special-fibre overlap
`w=x1=x3=x5=0`.

It proves only a first-order statement.  The rank-three horizontal tangent
incidence is the origin `(d2,d4)=(0,0)`; exact rank two, one, and zero have no
ordinary `delta w=1` tangent.  The normal rank drops scheme-theoretically on
the doubled line `(d2-d4-1)^2`, whose weighted/ramified arcs remain charged.

