# Registration: generic-square high-contact `A` prolongation

Date: 2026-08-26

Status: **PREREGISTERED AWS-ONLY EXACT-Q / GOOD-PRIME SOURCE REPLAY; NO
RESULT.**

The client reconstructs all seven frozen one-parameter source rows, checks
grades fourteen and fifteen in the correction-aware high-contact cone, keeps
the raw grade-fourteen equations, and tests whether the combined localized
source ideal forces the leading linear polynomial `A0` to vanish.  Exact `Q`
is the characteristic-zero endpoint; `F_65521` is an independent software
control only.

No local algebra execution is licensed.  A failed or timed-out lane is no
verdict.  A passing lane concerns only `D(p*k10)` with
`ord_sigma(R)>=2, ord_sigma(C)>=4`; it is not a Newton-fan, square-branch,
order-two, `(8,12)`, maximum-twelve, or JC2 conclusion.
