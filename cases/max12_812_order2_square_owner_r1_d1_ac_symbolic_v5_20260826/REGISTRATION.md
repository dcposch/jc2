# Registration: `r=1` / symbolic-`d=1` V5 proven-safe identifier repair

Date: 2026-08-26

V4 remained fail-closed at the root-chart substitution block with Singular's
`poly ^ number` diagnostic.  The exact source/Faber rows, grade shifts,
recurrences, and residue expressions before that block passed over Q and
`F_65521`.  An independent exact client successfully used the root variable
name `rtx` and underscore-separated temporary identifiers.

V5 pins V4 and changes only `rootlambda987654` to `rtx`, plus the three
temporary identifiers to `D1AC_tmp15`, `D1AC_tmp16`, and `D1AC_flag`, with
exact occurrence counts.  All mathematical expressions, validators, scope,
and AWS caps remain unchanged.  Failure is no mathematical verdict.
