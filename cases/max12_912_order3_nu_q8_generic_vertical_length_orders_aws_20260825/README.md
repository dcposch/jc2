# Generic vertical-length independent order races

This AWS-only successor leaves the original `dp/std` and `lp/std` producers
byte-immutable.  Its hash-pinned adapter changes only the admissible global
term order, variable tie-break order, and `std` versus `slimgb` engine on the
same seven exact localized source generators over `F_127(w)`.

Each lane is fail-closed and capped independently.  A theorem-level endpoint
requires `dimension=0`, `vdim=190`, and `source_fail=0`; any other PASS merely
classifies the resulting exact basis and is not a degree-one conclusion.
