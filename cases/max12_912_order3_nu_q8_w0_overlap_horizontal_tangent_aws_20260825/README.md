# Unloaded-overlap first-order horizontal tangent screen

This case is a strict first-order screen for the corrected-Q8 six-row source
at `w=x1=x3=x5=0`, with `delta w=1`.  Only the diagnostic-free V3
Box02/Box03 lanes are acceptance endpoints.  V1 and V2 are retained as
fail-closed software controls.

The exact result is that the `e2,e4` tangent equations have the reduced
two-point scheme `(d2,d4)=(0,0),(4,2)`.  The first point has only the tangent
to the boundary sheet; the second is contradicted by `729*L7=-432`.

This does not exclude ramified arcs (`ord(w)>1`), higher-order departure,
coefficient infinity, Taylor/terminal realization, or trajectories.

