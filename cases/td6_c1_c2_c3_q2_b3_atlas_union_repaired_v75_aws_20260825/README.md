# TD6 fixed-A3 q2-beta repaired whole `B3=0` atlas union (V74/V75)

Frozen status: **dual-AWS producer-exact dependency union; hostile review
pending.**

V74 composes the V73 original-source unit obstruction on
`V=0,C=-5U^2,D(U)` with the hostile-reviewed V70 original-source origin
obstruction.  The raw line is `Spec Q[U]`; its exact constructible cover is
`D(U) union V(U)`, and its closed endpoint is `C=V=U=0`.  Thus the whole
raw line is producer-exact empty for every beta in the fixed source-typed
A3 q2-beta section.

V75 then replays the raw and normalized `B3` identities and checks the
complete constructible decision tree in that same fixed section:

- reviewed V70 on raw `U=0`;
- reviewed V46 on `V=0,C=-U^2,D(U)`;
- V74 on the whole line `V=0,C=-5U^2`;
- reviewed V67 on `2t-1=0,D(w)` and
  `t^2-4t+2=0,D(w)`;
- reviewed V66 on
  `D(t*w*(t-2)*(2t-1)*(t^2-4t+2))`.

On `D(UV)`, the remaining apparent branches are discharged by exact route
algebra: `b(-5,y)=y^2` rules out `x+5=0`; `t=0` forces `y=0`; and the
line-pencil identity at `t=2` gives `b=-16(x+5)`.  Every theorem owner has
an explicit omission witness.  The lift
`B3(C,V,U)=U^6 b(C/U^2,V^2/U^3)` is replayed coefficient by coefficient.

The first V74 autochain invocation and two preliminary V75 invocations were
deployment/provenance-marker failures before any contradictory algebraic
result.  They remain immutable on AWS but are not theorem evidence.  The
frozen V74 and V75 source closures and outputs are the later clean dual-host
runs; `python3 verify.py` performs a lightweight custody and marker audit.

Exact conclusion: the whole raw divisor `B3=0` is producer-exact empty for
all beta **only inside this fixed source-typed A3 q2-beta section**.  This is
not whole A3, another TD6 modulus, TD6, SP-2, a landing theorem, or JC2.
