# V74 preregistration

Target: compose the V73 original-source unit obstruction on
`V=0,C=-5U^2,D(U)` with the hostile-reviewed V70 original-source origin
certificate.  The exact affine cover is `D(U) union V(U)`, and the closed
member specializes the raw line to `C=V=U=0`.

Required pass conditions: every V73 source-DAG/factor/control marker, every
V70 origin source/wrong-row/denominator-one marker, V70 review verdict
`CONFIRMED`, exact endpoint substitution, and both missing-branch negative
controls.  Failure is fail-closed.  Scope is only this whole raw line in the
fixed source-typed A3 q2-beta section, all beta; no whole-B3 or broader claim.
