# V75 preregistration

Target: a dependency-complete union for the raw divisor `B3=0` inside the
fixed source-typed A3 q2-beta section, all beta.  The checker must replay the
raw and normalized B3 identities and consume exactly these leaves:

- reviewed V70 on `U=0`;
- reviewed V46 on `V=0,C=-U^2,D(U)`;
- V74 on the whole line `V=0,C=-5U^2`;
- reviewed V67 on `2t-1=0,D(w)` and `t^2-4t+2=0,D(w)`;
- reviewed V66 on `D(t*w*(t-2)*(2t-1)*(t^2-4t+2))`.

On `D(UV)`, `x=-5`, `t=0`, and `t=2` must be discharged directly from
`b(-5,y)=y^2`, `y=t(x+5)`, and the line-pencil identity.  Every dependency
is load-bearing and gets an omission control.  Failure is fail-closed.  No
whole-A3, TD6, SP-2, landing, or JC2 claim is licensed.
