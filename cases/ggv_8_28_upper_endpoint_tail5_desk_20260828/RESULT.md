# Exact exclusion of the cutoff-five square tail

Date: 2026-08-28

## Verdict

`PASS — THE FIXED CUTOFF-FIVE ENDPOINT STRATUM HAS NO CHARACTERISTIC-ZERO FIELD POINT.`

An independent standard-library compiler rebuilt the specialization from the
authoritative raw determinant source. Exact rational replay gives a complete
two-branch exclusion:

* on `V0=0`, a division-free polynomial identity expresses `1` using the
  endpoint, the monic carrier reconstruction, and the three scalar core
  equations;
* on `V0!=0`, the core equations reduce to one irreducible quadratic branch,
  and eight literal D16--D18 compatibilities have an exact `K`-linear
  combination equal to `1`.

No local CAS or AWS job was used. No endpoint carrier was normalized. The
nonzero-`V0` unit does not use the endpoint equation and first appears at D18;
D19 and later rows are unnecessary.

This is a field-valued exclusion of the fixed branch-P, square-baseline,
cutoff-five specialization only. It is not a nonreduced-scheme statement and
does not by itself exclude the full branch-P family, a Keller pair, a
counterexample, or JC2.

## 1. Prefix census and endpoint

The independently compiled exact census is

```text
retained raw variables:          252
prefix equations D0,...,D9:      201
prefix rank:                      89
prefix nullity:                  163
remaining equations D10,...,D22: 312
```

The literal endpoint coordinate is

```text
D22[X^0]-1 = -1-p32*p139+p86*p91.
```

Writing

```text
a=p32, b=p139, c=p86, d=p91, e=p120,
```

the endpoint equation is the unnormalized determinant

```text
1+a*b-c*d=0.
```

The carriers and core coordinates have the exact raw-slot meanings

```text
a=p32  = g_1_0 = G15[X^1],
c=p86  = g_0_1 = G11[X^0],
d=p91  = f_1_0 = F11[X^1],
b=p139 = F7[X^0] plus its forced prefix G lift,
e=p120 = (1/2)g_0_3 + g_8_28 - 2g_4_16 + g_0_4,
V0=p162=F5[X^0],
R0=p150-p162/2=F6[X^0]-F5[X^0]/2.
```

The determinant does not make any individual carrier a unit.

## 2. Exact field-radical cascade

Put `C=X^4-1` and `H=C^2`. Exact compatibility-span witnesses give:

```text
D10:     F5^2 mod H = 0              => F5=C*B
D11:     B^2 mod C = 0               => B=C*V, hence F5=H*V
D12:     (F6-V/2)^2 mod H = 0        => F6=V/2+C*W
D13:     W^2 mod C = 0               => W=C*R
D14:     (F7-R/2)^2 mod C = 0        => F7=R/2+C*T
D14+D15: T^2 mod C = 0               => T=C*Q
```

Each implication is for field points and uses squarefreeness of `C`. It is
not promoted to an equality of the original nonreduced schemes.

The exact endpoint-carrier reconstruction is

```text
c=(3/16)*V0*(V0+4*R0).
```

The replay includes a mutation that deletes the `V0^2` term and detects both
the carrier error `(3/16)V0^2` and endpoint error `-(3/16)d*V0^2`.

After the last radical step, preserving `b` gives

```text
T1=-q1, T2=T3=0, T4=b-R0/2, T5=q1.
```

The scalar core through D15 is

```text
E15 = e*b -(3/4)b^2 -(1/8)V0^3,
E13 = e*V0 -(3/4)b*V0 +(3/2)b*R0 -(3/8)R0^2,
E14 = e*R0 +(3/4)b^2 -(3/2)b*R0 +(3/16)R0^2.
```

The older D14--D15 endpoint targets remain frozen as preliminary custody
artifacts. Their core has a rational point, so they do not establish a unit
without later rows.

## 3. Gauge quotient and the D17 invariant

The literal coordinate

```text
p129=F8[X^0]
```

occurs in none of the D10--D22 generators. This is the exact additive
weight-eight determinant gauge: adding a constant multiple of `t^8` to `F`
changes neither `F_X` nor `(t*d/dt-8)F`. Thus `p129=0` is a global additive
`Ga` slice meeting every orbit. It is not a carrier normalization. Replay
checks that reintroducing `p129` changes no literal constraint.

The cumulative post-D15/D16/D17 compatibility span has exact invariant-core
intersection generated by `E13,E14,E15` and

```text
K17=V0*(b*V0+R0^2).
```

`TAIL5_DESK_ANALYSIS.json` stores an exact rational witness for `K17` in the
literal staged compatibilities.

## 4. Closed branch `V0=0`

The following identity is exact before imposing the branch:

```text
3*b^3 = 2*b*E13 + 4*b*E14
        -(4*R0+2*V0)*E15
        -V0^3*(V0/4+R0/2).
```

Together with `V0=0`, the monic relation for `c`, and
`1+a*b-c*d=0`, the geometric identity for `x=a*b` gives a direct
division-free unit. The emitted certificate lists generators and cofactors
and replays their product-sum to the literal constant polynomial `1`.

This branch uses no localization and no choice of carrier.

## 5. Open branch `V0!=0`

Set `tau=R0/V0`. From `K17,E13,E14,E15`, every field solution in this open
branch satisfies

```text
12*tau^2+6*tau+1=0,
V0 =  tau/24,
R0 = -tau/48  - 1/288,
b  = -tau/144 - 1/576,
e  = -tau/192 - 7/4608,
c  =  tau/18432 + 1/36864.
```

The discriminant of the quadratic is `-12`, so

```text
K=Q[tau]/(12*tau^2+6*tau+1)
```

is a quadratic field. Working over `K` keeps both conjugate roots together;
no root is selected or numerically approximated.

After substituting these forms into the exact staged compatibilities and
adjoining each compatibility's `tau` multiple, the exact rational ranks are

```text
through row     base K-polys    expanded Q rows    Q-rank    contains 1
D16                   22              44             20          no
D17                   39              78             38          no
D18                   56             112             58          yes
```

The unit uses eight compatibility polynomials, grouped with their
`c0+c1*tau` cofactors:

```text
D16[4]   :   1119744
D16[12]  :   5038848*tau
D16[16]  :   10077696*tau
D17[1]   :  -85847040/7  - (386311680/7)*tau
D17[5]   :  -2985984     - 13436928*tau
D18[2]   :   103514112/7 + (465813504/7)*tau
D18[6]   :   7962624     + 35831808*tau
D18[10]  :   23887872/7  + (107495424/7)*tau
```

Their reduced product-sum is exactly `1` in `K`, not merely a nonzero
constant up to scaling. The standalone certificate records, for every one
of these eight polynomials:

* its pre-branch and quadratic-reduced terms;
* its reduced SHA-256;
* its exact source-generator indices, `X` degrees, and triangular source-row
  coefficients;
* the grouped `K` cofactor and the equivalent 13-row rational witness.

No endpoint equation, carrier normalization, D19 row, or later row enters
this unit.

## 6. Live regressions

The replay requires all of the following controls to pass:

* deleting the `V0^2` term in `c` changes the carrier and endpoint;
* reintroducing the invisible `p129` gauge changes no literal determinant
  constraint;
* omitting the first charged D18-certificate cofactor destroys the unit;
* replacing the quadratic constant `+1` by `-1` destroys the replayed unit;
* omitting all `tau`-multiple rows leaves `1` outside the rational span.

These are serialized in the two standalone certificate files.

## 7. Replay and hashes

From this case directory run:

```bash
python3 analyze_tail5.py --check --output .
sha256sum -c SOURCE.sha256
sha256sum -c EVIDENCE.sha256
```

All three commands pass. Principal hashes are:

```text
f62fc30a1d26192e24e035d0645af141f90affa04247995d66e927f02cb5497e  analyze_tail5.py
a3382efcd72104d48abb40981a33b87d9dacb5e7a9018a12937bef1f794f942c  TAIL5/TAIL_DEFORMATION_SYSTEM.json
6e6ce211c1152812f639c140b384f65c51ddfedca9add1d9768df6203b224076  TAIL5_DESK_ANALYSIS.json
60bd4ef90fac13412e090af9f8ba6ead0d293e53b53e0b5b7520efe08d3b85e4  CERTIFICATES/tail5_v0_unit.json
87efe8a899471d79347e7273dcd4f9b060e64877f63e974ac98bea85bf5496a2  CERTIFICATES/tail5_v_nonzero_D18_K_unit.json
0766bd5ece21a9ee2f02e5b61c1f5738af7cb5fee07877bce77b57038360b71d  SOURCE.sha256
1668e6a540ed646c11d841b224d724054b4e77723bdebfeda88850dd3da00fe5  EVIDENCE.sha256
```
