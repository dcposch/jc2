# Preregistration: streaming-equivalent `T-rs-0` source discovery

Date: 2026-08-26

Status: **SOFTWARE/MEMORY REPAIR OF THE REVIEWED DISCOVERY CLIENT.  NO REES,
CHART, MOVING-`p`, ORDER-TWO, MAXIMUM-TWELVE, OR JC2 VERDICT.**

The reviewed V0R1 implementation constructs and retains fourteen complete
Faber rows plus every extraction remainder at once.  Its two AWS prime lanes
are mathematically healthy but their resident memory grows during this
layout.  This V2 emitter is required to produce the same polynomial
sentinels while retaining only one total/frozen row and one extraction
quotient at a time.

The source, candidate ceiling, 569-term tails, corrected

```text
pTot=-2*rho^2+2*sigma*ell1+...+2*sigma^12*ell12,
```

and all pass/fail conditions are unchanged.  The 76 candidate names and
seven inactive names are literal constants in both compiler and validator.
The three frozen/total coefficients needed by the cusp combination are
copied before each row is released.  Per-grade nonzero bits are accumulated
and resolved in increasing grade only after all seven rows.

The validator additionally requires:

- the exact hard-coded candidate and inactive lists;
- positive term-count sum iff the corresponding grade bit is one; and
- the stdout SHA recorded by the AWS lane metadata.

V2 first runs at two good primes.  Agreement with each other and with any
completed V0R1 transcript is navigation only.  After freezing the discovered
manifest, the identical streaming layout runs over exact Q and a fresh good
prime.  Any source/hash mismatch, missing sentinel, characteristic mismatch,
timeout, memory exit, transcript disagreement, or validator failure is
`FAIL/UNRESOLVED`.
