# `K3-LOCAL-PREFLIGHT-20260824` preregistration

Status at freeze: scheme and gates fixed before executing `preflight.py`.

## Scope

This is a known-negative verticality/instrument gate at `D=3`, `p=3`.
Established characteristic-zero low-degree results exclude a positive generic
fibre.  A surprising positive computation must freeze as a conflict; it may
not spawn a descendant.

No `W_3`, `D=4`, new seed, new prime, support sweep, fleet job, or generic
surface search is permitted.

## Exact scheme

For every monomial `x^i y^j` of total degree at most three, begin with
coefficients `p_ij,q_ij` over `Z`.  Impose

```text
J(P,Q)=P_x Q_y-P_y Q_x=1,
P(0,0)=P(1,0)=Q(0,0)=Q(1,0)=0.
```

The common target value has therefore been translated to zero; the two source
sections remain the fixed distinct points `(0,0)` and `(1,0)`.

Eliminate the four collision-linear coefficients exactly:

```text
p_00=q_00=0,
p_30=-p_10-p_20,
q_30=-q_10-q_20.
```

The normalized coefficient ring has the ordered variables

```text
p10,p01,p20,p11,p02,p21,p12,p03,
q10,q01,q20,q11,q02,q21,q12,q03.
```

The defining ideal has the fifteen coefficients of `J(P,Q)-1`, in graded
`x,y` monomial order through total degree four.

The special point `s` over `F_3` is

```text
P=x-x^3, Q=y:
p10=1, q01=1, all other normalized variables=0.
```

Let `m_s` be the prime generated by `3`, `p10-1`, `q01-1`, and all other
normalized variables.  The target ring is the completion of the localization
of the normalized coefficient ring at `m_s`.

For first lifting, use the exact-collision integer representative
`P#=x-x^3,Q#=y`.  It is congruent modulo nine to the promoted Teichmuller
representative `x+8x^3,y`.  The registered correction is `B=x^2y`.

## Permitted Stage A

`preflight.py` may perform only exact sparse integer construction and linear
algebra over `F_3`.  It must emit:

1. all fifteen normalized equations;
2. mod-3 and mod-9 control checks;
3. the full tangent matrix, rank, pivots, and a kernel basis;
4. the divided first-error vector and one exact correction; and
5. equation/term counts for a standard-basis estimate.

This stage has a hard expected size below `20 x 20` linear algebra and below
one second.  Any mismatch stops as `CONTROL-FAIL`.

## Stage B launch gate

A localized/global standard-basis certificate attempt is permitted only if
Stage A exhibits an exact unit-pivot elimination leaving **at most eight**
local parameters and **at most eight** residual nonlinear equations, with no
more than 120 total residual terms.  The planned exact engine must also be
able to retain transformation data sufficient to print

```text
g*3^N = sum_i H_i E_i,   g notin m_s.
```

Failure of any size or transformation-data condition returns
`COMPILER-READY/HEAVY`.  Mere failure to find a lift is never a negative
certificate.  No unregistered Gröbner timeout is permitted.

## Verdicts

- `VERTICAL-CERTIFICATE`: explicit `g,N,H_i` identity, independently replayed.
- `GENERIC-FIBRE-CONFLICT`: exact positive generic-fibre certificate; freeze
  with no descendants because it conflicts with known low-degree theorems.
- `COMPILER-READY/HEAVY`: controls and obstruction compiler pass, but Stage B
  size/certificate gate fails.
- `CONTROL-FAIL`: any scheme, tangent, mod-3, or mod-9 mismatch.

