# V39 preregistration: `Tg19_7` compatibility at homogeneous weight 19

Date: 2026-08-27

Let `J_other` be generated by all 69 named frozen ordered-`a1`, `rho=0`
raw source rows through grade 19 except `Tg19_7`; zero rows remain in the
bridge census and contribute no product.  V39 decides the exact question

```text
Tg19_7 in (J_other)_19 ?
```

The weight-19 span contains every product `m*Tg{g}_{r}` with `g<=19`,
`Tg{g}_{r}` nonzero and not `Tg19_7`, and `m` every monomial in all 65
registered row variables of sigma weight `19-g`.  The solver constructs the
complete incidence component seeded by every monomial of `Tg19_7`; it does
not use a guessed support.  The expected full product count is 802 and the
expected union support, including the target, is 5,078 monomials.

Two AWS lanes use distinct selector primes 65,521 and 65,519.  Modular
echelon is only a pivot selector.  The result contains only an exact-Q
certificate: either a coefficientwise membership identity, or a rational
dual functional annihilating every one of the 802 products and taking
`Tg19_7` to one.  The exact validator rebuilds the complete system and the
deterministic certificate independently.  No modular verdict is accepted.

The deterministic reduction eliminates pivot monomials outside the V34
support before those supported on
`{a1,ell2,cs1,rs2,aa0,ee1,ec3}`.  It emits the full exact representative
`R19=Tg19_7-sum(h_i*F_i)` and replays it coefficientwise.  A mutation fixture
deletes a certificate-detected nonzero target coefficient and must be
rejected both by the frozen target bridge and by exact certificate replay.

Before solving, the case replays all 70 raw-Q file hashes and canonical
`rho=0` polynomial hashes, plus canonical reductions of all 70 rows at both
selector primes.  It also reproduces the honest newcomer census, the exact
stationary first-occurrence matrix at grades 14--19,
`Tg11_1=(3/8)*a1*e0`, the vanishing of newcomer coefficients in rows
3--7 only after the resulting `e0=0` ordered-chart quotient is applied as a
control (not to the membership system), and both fraction-free row-2 Bezout
identities.

Scope is only ordinary, unsaturated homogeneous ideal membership at weight
19 in the literal frozen ordered-`a1`, `rho=0` raw-row system.  Membership
would show that this row is redundant at that scope.  Nonmembership would
show it is a new raw compatibility condition at weight 19.  Neither outcome
implies radical or `a1`-saturated membership, honest Rees-chart emptiness,
Gate T, bounded-degree closure, or JC2.
