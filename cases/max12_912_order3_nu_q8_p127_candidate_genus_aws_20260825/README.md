# Candidate-H exact genus diagnostics

This AWS-only case emits four independent pure-Singular diagnostics for the
pinned candidate plane curve `H(w,v)` over `F_127`: `normal.lib` geometric
genus, `brnoeth.lib` adjunction/Hamburger--Noether data, the exact affine and
torus singular locus, and the `v`-discriminant/resultant.  Each mode is a
separate fail-closed lane with source hashes, wall/RSS telemetry and a hard
cap.  A partial basis or interrupted normalization is no result.

The candidate-curve genus alone makes no source-component, specialization,
trajectory, maximum-twelve, or JC2 claim.
