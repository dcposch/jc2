# Repaired V2 custody for the localized `w=0` Q8 fibre theorem

This nonmutating successor addresses the six repairs in the Grok
`CONFIRMED_WITH_REPAIRS` audit of the frozen V1 theorem.  V1 bytes remain
immutable.  V2 adds a fail-closed independent irreducibility certificate,
the two vacuous characteristic-zero non-Q8 `r8` splits, explicit transitive
dependency pins, and corrected scheme/sign wording.

All computation is AWS-only.
