# Selected `(9,12)` `w=0` source/projective escape boundary

This successor classifies exactly the complement left open by the frozen
localized-fibre theorem.  It consumes the same pinned six-row
approximate-cubic compiler and does not mutate that theorem or its case.

The finite affine boundary is covered disjointly by

```text
x5=0, x3!=0;
x5=x3=0;
x5!=0, x3-2*x5=0.
```

The projective job first homogenizes the full six-row family in the six
coefficient-source variables, saturates by the homogenizing coordinate over
`Q[w]`, and only then sets `w=T=0`.  This order is mandatory for horizontal
source escape.

All Singular and substantive Python execution is AWS-only.  Local work is
editing, hashing, shipping, and short status inspection.
