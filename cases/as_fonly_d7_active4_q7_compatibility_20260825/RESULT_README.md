# Result

Box02 exhausted the preregistered 81-point active-coordinate cube with return
code zero and empty stderr.  The Q7 matrix is constant of rank nine.  Its
ten-coordinate compatibility map has exactly four nonzero coordinates:

```text
kappa_5=s15, kappa_6=2*t6, kappa_8=s17, kappa_9=2*t8.
```

Thus only `(t6,t8,s15,s17)=(0,0,0,0)` is compatible on this slice.  See
`bootstrap.json` for all source digits, cokernel values, and compatibility
decisions at all 81 states.

Scope is the frozen four-coordinate slice only; all other predecessor
coordinates and all broader lifting claims remain open.

