# Registration: affine-Faber `A`, `H16/q6/a4` equality wall

Date: 2026-08-26

Status: preregistered fixed-representative V2 producer; no theorem.  V1 is
deployment-negative because its unnamespaced `K10` jet variables collided
with the frozen source generators `k2,k6` before algebra.

Use the literal delayed-load source with the integral valuation
representative

```text
normal H=16, kernel q=6, complements=12, center alpha=4,
affine graph=42, mixed quadratic/deviation predecessor=44,
center/mixed and intrinsic equality=48.
```

Reconstruct all seven frozen complete ordinary-Faber tails.  Parameterize
the exact affine graph by arbitrary truncated moving series in `a,E,M,K10`,
then retain through grade 48:

- both kernel series `X,Y` from grade 6;
- all four complementary series from grade 12;
- all three transverse graph-deviation series `d6,d2,dm` from absolute
  grade 44;
- `mu4` at absolute grade 48, with `mu6,J/4` present at their later typed
  grades;
- every coefficient of all seven source rows at grades 44--48.

Verify grades below 44 vanish, print the complete grade-44 and grade-48
blocks, print the exact moving inverse-Faber functional
`K=E*H3+H5` at grade 48 and its normal form modulo grades 44--47, and test
the full through-48 ideal on both projective residue charts `D(p*m*x0)`
and `D(p*m*y0)`.  Preserve the raw nonreduced predecessor ideal before
either saturation.

Exact Q is mathematical producer evidence.  Characteristic 65521 is a
software/control lane only.  The compiler must refuse non-AWS hosts, pin
the frozen tail source, archive the exact input bytes, fail closed on any
missing telemetry, and report engine rc separately from the mathematical
endpoint.

This fixed representative does not prove rational regrading, the finite
split design, iterative graph absorption, source/Rees coverage, an
exclusion even if both chart ideals are units, order two, maximum twelve,
or JC2.
