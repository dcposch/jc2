# TD6 V23 raw B3 first-minor factor `t=1/2`

This AWS-only run directly rebuilds the B3 birational curve `t=1/2`, hence

```text
(x,y)=(19/9,32/9),
C0=(19/9)U0^2, V0^2=(32/9)U0^3.
```

Equivalently, with `w=V0/U0`,
`U0=9w^2/32`, `V0=9w^3/32`, and `C0=171w^4/1024`.
The implementation audits away both auxiliary quadratic directions and
replays transport, first band, genuine P12, and the complete original-row
source certificate in both pivot orders.  Only its printed localization can
be killed; `w=0` is the separately raw-closed origin.  No whole-B3,
three-center-family, SP2, or JC2 claim is made.
