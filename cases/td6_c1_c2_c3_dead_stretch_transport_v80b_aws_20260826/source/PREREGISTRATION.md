# V80B dead-stretch transport source-incidence preregistration

Audit the eleven licensed pole-chart coefficients in

```text
y=r^5+d6*r^6+...+d16*r^16+zeta*r^17
```

at zero dead stretch and at the fixed source-typed A3 base.  Run one exact
matrix-aware square-zero shard for every level 6 through 16 on each of two
independent AWS hosts.  Every shard must retain derivative-only pole rows,
differentiate the frozen transport echelon, replay all 3,470 original pivot
rows, and emit the remaining exact affine incidence forms on the 132 base
transport parameters.  It must include literal binomial/shift controls, an
omission control proving that the matrix derivative is not silently zero,
and the exact observation that the chart determinant `-25*r^-9` is
independent of dead stretch.  Both hosts consume the same hash-closed
archive; every shard refuses non-Linux execution or a missing registered AWS
tag.

This is the first gate only.  A nonzero incidence form is not a dead-stretch
kill: it may cut the base transport family.  A consistent transport lift is
not a current-row or nonlinear family theorem.  No TD6/SP-2/JC2 inference is
licensed.  All substantive runs are AWS-only and fail closed on non-Linux or
missing registered tags.
