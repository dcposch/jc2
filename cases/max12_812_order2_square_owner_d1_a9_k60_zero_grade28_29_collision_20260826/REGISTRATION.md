# Registration: D1 `a=9`, `V(k60)` grade-28/29 collision

Date: 2026-08-26

Status: **PREREGISTERED DUAL-AWS EXACT-SOURCE CLASSIFICATION.**

## Question

On the already registered fixed-contact D1 chart

```text
ord_sigma(A)=9,  ord_sigma(C)=10,  ord_sigma(R)>=9
```

with `p*k0 != 0`, impose `k60=0` and classify the complete grade-28/29
literal-Faber source ideal.  This is the exact successor to the reviewed
`D(k60)` exclusion.  No emptiness outcome is preregistered.

The client must import the frozen V2 `D(k60)` producer, hence transitively
the corrected parenthesized-load V3 census, without changing its source.
It must retain:

- all seven rows at grades 27, 28, and 29;
- `k60_1,k60_2`, `mu20,mu20_1`, and the moving-connection term;
- every displayed higher `A,C,R,L` correction through the parent grade-31
  window;
- delayed `k20`, `k0_1`, and `k60_4` at grade 31;
- the literal terminal targets `mu4`, `mu6`, and `J/4` at grades 32, 36,
  and 38.

## Preregistered tests

Write `I_full` for `k60` together with every source row at grades 27--29.
The compact ideal is generated by `k60` and

```text
e28z = (3/4)c1*k60_1,
e28c = (3/4)c0*k60_1-mu20,
e29z = (3/4)(a0*c1+a1*c0+c1_1*k60_1+c1*k60_2),
e29c = -(3/8)p*a1*c1+(3/4)a0*c0
       +(3/4)c0_1*k60_1+(3/4)c0*k60_2-mu20_1.
```

Require equality `I_full=I_compact` before radicals.  In particular, the
moving connection must appear through the exact identities

```text
g29_3 = -(p/4)e29z-(ell1/2)e28z,
g29_5 = -(p^2/32)e29z-(p*ell1/8)e28z,
g29_7 = -(p^3/128)e29z-(3*p^2*ell1/64)e28z.
```

The compact exact-contact model must include Bezout coordinates for both
leading pairs `(a1,a0)` and `(c1,c0)`, plus inverses of `p,k0`.  Test both
strata:

1. `D(k60_1)`, with its own inverse;
2. `V(k60_1)`.

For each stratum require a proper ideal and an independently encoded exact
witness.  On `D(k60_1)` additionally verify that the equations force
`c1=0` and `mu20=(3/4)c0*k60_1`; on `V(k60_1)` verify `mu20=0`.

Run exact Q on Box03 and `F_65521` on r6d, independently, under 16-GiB
virtual-memory, 600-second compiler, and 1800-second Singular caps.  Require
rc 0, exact marker cardinalities, no Singular diagnostics, and zero swap.

## Firewall

A nonempty result is only an exact grade-28/29 survivor classification on
the fixed `a=9`, `V(k60)`, `D(p*k0)` chart.  It is not a formal arc through
all orders, and it gives no grade-30, terminal-target, contact-raising,
whole-D1, square-component, order-two, `(8,12)`, maximum-twelve, or JC2
conclusion.  A PASS must route surviving components to the complete grade-30
receiver rather than call the contact solved.

