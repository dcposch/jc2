# V1 fail-closed note

Date: 2026-08-26

Both preregistered AWS lanes compiled and ran with engine rc 0, but both
validators correctly rejected the transcript before any branch verdict.
Singular parsed `p^2/32` and `p^3/128` inside two boolean identities as
exponentiation by rational numbers.  Consequently the grade-28 and grade-29
booleans were undefined and every later branch marker was unavailable.

The failure is implementation-only.  The earlier source replay, the four
leader identities, and the equality of the full and compact source ideals
had printed `1` on both lanes, but none of those partial markers licenses an
endpoint.  V1 is immutable and has **NO MATHEMATICAL VERDICT**.

The isolated compact ring also omitted the symbol `z` used only by its final
Chebyshev/Pell positive control.  That omission was masked by the preceding
parse cascade and must be repaired before rerun.

V2 may change only these generated token sequences:

```text
p^2/32       -> (p^2)/32
p^3/128      -> (p^3)/128
3*p^2*ell1/64 -> 3*(p^2)*ell1/64
(p,k0,...    -> (z,p,k0,...
```

It must assert the exact pre/post replacement cardinalities and rerun both
fields on the same independent AWS hosts.

