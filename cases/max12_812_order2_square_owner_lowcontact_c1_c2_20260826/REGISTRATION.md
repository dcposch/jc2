# Registration: generic-square low-contact `c=1,c=2` fan clients

Date: 2026-08-26

After the reviewed half-weight radical `C=R=0`, let `c=ord_sigma(C)`.
This package reconstructs the complete seven loaded source rows for:

- `c=1`, retaining moving `p`, the first `A,C` corrections and `R=sigma B1`,
  through absolute grades 11 and 12;
- `c=2`, retaining moving `p` through second order, `A` through second
  order, `C` through fourth order, `R=sigma B1+sigma^2 B2`, and moving
  `k10`, through absolute grades 12, 13, and 14.

The exact-Q gate checks whether the localized source radical contains both
coefficients of the leading `C` and whether saturation away from their joint
zero section is the unit ideal.  A nonunit endpoint is a split instruction,
not a survivor or counterexample.  `F_65521` is an independent control only.

Use 32 GiB VM, a 10-minute AWS compiler cap, and a two-hour engine cap.
No normalized-fan, square-branch, order-two, terminal, Taylor, or JC2 claim.
