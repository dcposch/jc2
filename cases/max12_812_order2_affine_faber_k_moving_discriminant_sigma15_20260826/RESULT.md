# `(8,12)` order two: correction-complete moving-discriminant `K` recursion through sigma grade 15

Date: 2026-08-26

Status: **PRODUCER COMPUTATION — EXACT-Q PASS, INDEPENDENT F65521 SOFTWARE CONTROL PASS.**

## Result

On the exact moving-double-root chart

```text
a(s)=a1*s+...+a5*s^5,
e(s)=p+e1*s+...+e5*s^5,
Q=(z-a)^2*((z-a)^2+4*a*(z-a)+e),
N=s^5*lam*z*(z^2+p)+sum_{j=6}^{10} s^j*(n3j*z^3+n2j*z^2+n1j*z+n0j),
```

the ideal generated by all seven complete ordinary-Faber rows in sigma
grades 10 through 15 is the unit ideal after saturation by `p*lam`.
Equivalently, this exact moving-double-root chart has no formal successor
with `p != 0` and nonzero leading exceptional-`K` normal coefficient.

The exact-Q lane printed

```text
K_SIGMA15_TCOEFF_CONTROL=1
K_SIGMA15_LOWER_ZERO=1
K_SIGMA15_STD_BEGIN
1
K_SIGMA15_STD_END
K_SIGMA15_UNIT=1
K_SIGMA15_DIM=-1
K_SIGMA15_ENDPOINT=PASS_COMPLETE_EMISSION_AND_STANDARD_BASIS
```

The characteristic-65521 control printed the same endpoint and unit
standard basis.  The rational result is established by the exact-Q lane;
the prime lane is only an independent software/deployment control.

## Completeness through grade 15

The client retains all 20 normal coefficients in orders 6 through 10.
These are exactly the later normal coefficients that can pair with the
order-5 leading normal in a nonlinear row of order at most 15.  It also
retains all ten coefficients `a1,...,a5,e1,...,e5` of the moving exact
double-root quartic; these are exactly the root-preserving tangent terms
that can modify the order-10 leading quadratic tail through order 15.
All 42 row/grade coefficients `R_l[s^g]`, `1<=l<=7`, `10<=g<=15`, are
emitted before standard-basis computation.  A coefficient-extractor
positive control and the vanishing of every lower coefficient are checked.

Loads are set to zero because the delayed source loads
`k10=Lambda^12*K10`, `k6=Lambda^8*K6`, `k2=Lambda^4*K2` first reach the
absolute source face after this sigma-15 predecessor.  This computation is
therefore a necessary unloaded predecessor, not a total loaded model.

## Frozen inputs and lanes

The compiler charges the complete frozen tails

```text
d72f774c53c7dcb259eefc10ff2a02202e1049b61c17486b1ba22e3773813848
  cases/max12_812_order2_u2_62_strict_rees_20260825/
  aws_compile_v2_jsat/run/output/compiled_v2/tails.json
```

AWS lanes:

```text
Box03 exact Q:
  max12_812_order2_affine_faber_k_moving_disc_sigma15_20260826T141542Z_q
r6d F65521:
  max12_812_order2_affine_faber_k_moving_disc_sigma15_20260826T141542Z_p65521
```

The two remote compiler-stderr files were zero bytes, with SHA-256
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`;
they are recorded by hash rather than by newline stand-ins in local custody.

## Scope firewall

This result covers the exact moving-double-root chart and arbitrary
root-preserving intermediate sigma corrections through grade 15.  It does
not by itself cover a root-splitting chart, prove the K2 chart's valuative
exhaustiveness, treat the exceptional A face, treat other load slopes,
construct a total normalized-Rees fan, or prove order two or JC2.  A
separate composition theorem may subsume this finite calculation by the
reviewed discriminant-K2 support theorem; this package remains independent
corroborating evidence.
