# Registration: D1 `a=9` literal-Faber source-support census

Date: 2026-08-26

Status: **PREREGISTERED DUAL-AWS NAVIGATION CLIENT; NO THEOREM ENDPOINT.**

The reviewed finite contacts reach the first target/load collision at `a=9`:
unit `k6*C/L` starts at grade 27, `mu2` at 28, and unloaded `AC/L` at 29.
Before designing a root/residue client, compile the literal seven frozen
Faber/source tails through grades 27--31 with correction-complete jets:

```text
p through sigma^4;
A=sigma^9(A0+...+sigma^4 A4);
C=sigma^10(C0+...+sigma^4 C4);
R=sigma^9(R0+...+sigma^2 R2);
k10 through sigma^2;
k6,k2 through sigma^4;
mu2 through sigma^3;
mu4,mu6,J retained.
```

Print every divided source row and a complete dependency matrix.  This is a
literal Faber/source census, not an affine Laurent P2/P3 client.  It must
retain the full frozen connection and all lower loads/targets.

Run exact Q on Box03 and `F_65521` on r6d, each under 16-GiB virtual-memory,
600-second compiler, and 1800-second Singular caps.  Require exact source
divisibility and recursive quotient identities, rc 0, no diagnostics, and
zero swap.

## Firewall

The output only determines in-window source support.  It gives no row
elimination, root allocation, arc, scheme, a=9, square, order-two, `(8,12)`,
maximum-twelve, or JC2 conclusion.  No dependency absence/presence may be
turned into a theorem without a separately frozen analytic/Faber bridge and
correction-aware receiver.  The tied-load Chebyshev/Pell solution is an
expected survivor/control.

