# Affine-Faber `A`: loaded K2 kernel predecessor through sigma 45

Date: 2026-08-26

Correction-aware successor to the sigma-45 invariant slice.  In addition
to the exact moving-discriminant base, it retains arbitrary `e,m` tangent
jets through relative order three, both transverse K2 kernel coordinates
in every relative order 6--9, both complementary linear polynomials in
every relative order 12--15, all three delayed-load jets and all `mu2`
target jets in absolute grades 42--45, and all seven complete frozen
ordinary-Faber rows.

The first decisive launch is the two-projective-chart `q=6` face:

```text
q6x: x6!=0, y6=0;   q6y: y6!=0, x6=0.
```

These two charts exhaust the nonzero relative-order-six kernel because the
grade-42 odd K2 row forces `x6*y6=0`.  Each shard emits every row
coefficient in grades 42--45 and computes the raw localized standard
basis.  The compiler also contains navigation-only integral `q=7` and
zero-kernel modes; they are not an exhaustive substitute for the
fractional `q=15/2` face and are not launched as theorem lanes here.  No unit endpoint is
preregistered: support, unit, timeout, and solver failure are distinct
outcomes.  Exact Q is mathematical evidence; F65521 is only a software
control.

Scope: the delayed-load repeated-root `A` relative-order-six kernel face,
including its corrections through sigma 45.  If this face has zero kernel,
the next separate gates are the homogeneous interval `6<q<15/2`, the
ramified `q=15/2` intrinsic tie at grade 45, and the ordinary `q>15/2`
slice.  Those gates, `m=0`, terminal/Taylor, other load slopes, total fan,
order two, and JC2 are outside this registration.
