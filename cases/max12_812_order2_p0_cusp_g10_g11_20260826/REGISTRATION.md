# Registration: `p=0`, `D(rs*k0)` cusp grades 10 and 11

Date: 2026-08-26

Reconstruct all seven canonical loaded source tails after
`Lambda=sigma^2` in the correction-aware quadruple-root cusp chart.  Form
the raw absolute-grade-10 and grade-11 rows before any support substitution.
Require:

- exact equality with the analytic grade-10 receiver and its four raw rows;
- the complete grade-11 receiver, including moving `p,R,k10` and the charged
  `D*K^(1/2)` cross term;
- the lower-unitriangular moving-Faber connection;
- the explicit raw localization and rational cusp parameterization; and
- the four predicted unit pivots on `D(k*tau)`.

Run exact `Q` on Box03 and an independent `F_65521` control on r6d.  The
good-prime lane is a software control only.  This client does not inspect
grade twelve, terminal rows, Taylor conditions, the `k0=0` boundary, the
whole square branch, order two, or JC2.
