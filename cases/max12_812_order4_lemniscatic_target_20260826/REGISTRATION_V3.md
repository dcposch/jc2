# AWS registration: loaded order-four lemniscatic target V3

Date: 2026-08-26

Status: preregistered; no endpoint consumed.

## Scope and V2 delta

V3 repeats the V2 direct cleared-denominator arithmetic certificate for

```text
q=T^2(T-1),
v=5184/((T-1)(21T-22)(3T-2)^6),
S=24(T-1)/(3T-2).
```

It checks exact membership in the residual plane, all charged branch-place
identities, the complete divisor of `v`, the fourth-power removal to
`xi^4=S(S-1)`, the cubic `Z^2=X^3-16X`, and the regular differential
identity.  V3 has exactly two terminal-line deltas from V2: the completion
token is relabeled V3 and Singular's invalid `exit(0)` is replaced by
`quit;`.  Every arithmetic statement is byte-identical.  V1 and V2 remain
immutable **NO VERDICT** controls.

This lane certifies target arithmetic only.  It does not prove the
independent source-component projection/nonconstancy premise and therefore
does not by itself eliminate the loaded order-four leaf.

## Registered AWS lane

Registered tag:
`max12_812_order4_lemniscatic_param_v3_20260826T023700Z_box03`.
Host Box03, instance `i-0ece0b9a3b4a7512f`, public IP `98.80.65.144`,
expected hostname `ip-172-30-0-249`; same-named directory under
`/home/ubuntu/jobs/`; exact Singular 4.3.2; timeout `1800 s`;
virtual-memory cap `8388608 KiB`.

The remote launcher must record actual PID, hostname, UTC start, caps,
archive/input hashes, engine version, return code, and output hashes.  Any
failed identity, source/freeze mismatch, timeout, OOM, engine error, nonzero
return code, or missing final V3 PASS token is **NO VERDICT**.

## Closed launch record

The source archive SHA-256 is
`943001f0d9df92fa6da2c3ebd77f192ad092ce07439a7aca34c0f5e750611d13`.
Both prelaunch and in-lane freeze checks passed.  Host
`ip-172-30-0-249`, launcher PID `142619`, started and ended at
`2026-08-26T02:37:27Z`.  The exact input SHA-256 is
`527e29daf6cc6a183af3fa4304a7ff1470361c38eef753bcde50f21ae574a67a`.

The lane ended `rc=0`, used `11320 KiB` maximum resident memory and no
swap, emitted every registered identity sentinel and the final V3 PASS
token, and emitted no Singular diagnostic.  Its stderr is solely the
`/usr/bin/time -v` accounting block.  Output hashes are:

```text
stdout 18f18c5e164f1645a9a24c983ac0be078ca3c5569c909aacdfa1bc258ffe8f69
stderr 607ade63d4015ed45b68392acc58d34a8c42e1d0c6716512eb67b7759042f27a
meta   fa53b28da1c488cd0570d3962e9c3b3323a86a40c4831881b464908ae715a675
```

At its registered target-only scope, the V3 endpoint is **PASS**.
