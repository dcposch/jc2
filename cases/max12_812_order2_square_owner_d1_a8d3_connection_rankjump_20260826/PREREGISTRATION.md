# Preregistration: a=8,d=3 grade-38 moving-connection rank jump

Date: 2026-08-26

Status: **PREREGISTERED EXACT-Q AWS DIAGNOSTIC WITH TWO FRESH PRIME CONTROLS.**

## Question

After replacing the falsified fixed-`z` `k6RC` row model by the adjudicated
inverse-root-moving rows, can any normalized universal odd-row functional

```
Phi7 + alpha*p*Phi5 + beta*p^2*Phi3 + gamma*p^3*Phi1
```

annihilate both grade-38 families `k6 A^2` and `k6RC` on `D(p)`?

## Exact test

Pin the complete a=8,d=3 source inventory and the separately frozen k6RC
adjudication.  Extract the `a0*a1*k60` column of family 15.  Compare it to
the corrected `b0*c1*k60` column of family 16.  Compute:

1. the two forced values of `alpha`;
2. the determinant of their rows 5 and 7 coefficient matrix;
3. an exact expression of the row-7 target direction in their span.

Run exact Q and independent reductions modulo 65519 and 65521 on AWS.  The
finite fields are software controls only.

## Firewall

A rank jump is a negative verdict only for the universal coefficientwise
odd-functional route.  It is not a solution of the nonlinear source
equations, not a point or component, not an a=8,d=3 cell verdict, and not a
statement about a=7, a=9, another D1 chamber, maximum twelve, or JC2.
