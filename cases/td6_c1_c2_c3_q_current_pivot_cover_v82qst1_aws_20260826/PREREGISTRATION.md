# TD6 V82QST1 reverse/sparse CURRENT pivot pilot

Date: 2026-08-26

## Question

For the exact single-axis CURRENT reductions at `q2` and `q10`, do two
independent pivot policies change the foreign denominator ideal generated by
`F`, `G`, and `L`, or produce an explicit principal-open cover with the
ascending Gate-0 certificate?

## Registered calculation

Run four source-identical exact shards on two independent registered AWS
hosts:

- `q2` with reverse pivots;
- `q2` with sparse/minimum-denominator scalar pivots;
- `q10` with reverse pivots;
- `q10` with sparse/minimum-denominator scalar pivots.

The policy is applied jointly to the raw transport elimination and to every
base-nonzero EJet elimination in FIRST, previous/pole, and CURRENT.  Sparse
pivots must have exactly one nonzero base-field coordinate and are ordered by
denominator degree, numerator degree, serialized size, then variable.  If no
such pivot exists, that shard fails closed.

Each shard retains the Gate-0 pre-assert serialization of every reduced
CURRENT coordinate, its exact numerator and denominator, factorization, and
orders/residues along `F`, `G`, `L`, `U`, and `V`.  The inherited assertion
that rejects foreign denominator factors remains active, so an expected
diagnostic endpoint is `rc=1` after complete evidence emission.

## Decision rule

- Expand to the other seven `q2..q10` axes only if the q2/q10 pilot changes
  the radical factor ideal or emits a credible finite principal-open cover.
- A cover requires exact cleared identities and a localized Bezout/Cech
  certificate.  A gcd or differing selected-minor factorization is not a
  cover in the multivariate center ring.
- If both policies retain the same `F,G,L` debt, stop pivot proliferation and
  proceed to the registered raw divisor clients.

## Scope firewall

This is a denominator/presentation diagnostic on the fixed source-typed A3
section and the already empty base, not a tangent-family, nonlinear-family,
TD6, SP2, or JC2 theorem.  No result is consumed unless exact source closure,
preflights, terminal status, and dual-host custody agree.

