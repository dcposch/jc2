# TD6 V82QST1 reverse/sparse CURRENT pivot pilot

This immutable source copy is governed by the case-level preregistration.
Only q2 and q10 are run, each under `reverse` and `sparse` pivot policies.
The policies change transport and staged EJet pivots jointly.  Every shard
emits exact pre-assert reduced-coordinate/factor data and then retains the
foreign-factor assertion fail closed.  Differing factors do not constitute a
cover without an explicit localized Bezout/Cech identity.  Fixed-A3,
diagnostic-only scope; no family, TD6, SP2, or JC2 conclusion.

