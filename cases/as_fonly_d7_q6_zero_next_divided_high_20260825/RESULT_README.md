# Frozen cumulative-point high-carry result

At the cumulative branch `Q9=e17`, `Q8=0`, `Q7=0`, `Q6=0`, the recursive
fifth-order residual and the literal integer determinant give identical
homogeneous rows in degrees 12 through 7.  The first nonzero row is
`R10=x^10`; additionally `R8=2*x^8`.  Since a cap-seven next digit has
divergence degree at most six, this particular cumulative point is terminal.

The run was executed on Box02 under tag
`as_q6_zero_next_divided_high_20260825T025049Z`.  It is not a classification
of any earlier affine fibre.  Kernel dependence is the immediate successor
gate.
