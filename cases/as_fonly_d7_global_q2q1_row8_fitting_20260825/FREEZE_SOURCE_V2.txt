SOURCE FREEZE V2 — global Q2/Q1 row-8 scalar emitter — 2026-08-25

V1 is retained as a failed brittle-source-splice negative control.  V2 executes
the pinned complete displayed Q5 parent, walks only its explicit nested source
scope, derives the literal degree-one carry, and appends the eliminated scalar
omega=ry-h*rx.  A paired omission-control formula is emitted from the same
source bytes.

This is a necessary zero-locus discriminator, not a Q4/Q3 sufficiency theorem.
