# J2 typed prefix census V23

Date: 2026-08-27

Status: **PREREGISTERED LIGHT EXACT DESIGN CENSUS.**

Input V22R1 is provisional and already has a different-model hostile review
running.  This is one cheap, reversible speculative child permitted by the
campaign protocol.  It must not promote or launch an expensive chart search.

## Question

Using the literal exact-Q source coefficients at every exported grade 10--15,
form the two standard stage-two source presentations over `A/J1`, where
`J1=(rs,cs,c0,c1)`:

1. `T-a0`: substitute `a1=a0*qa1` after setting `J1=0`;
2. ordered `T-a1`: set `J1=0` and `a0=0` (the source restriction of the
   closed complement of the first chart).

Export the specialized rows canonically and census exactly which pure
exceptional monomials survive.  In particular, test whether the grade-15
signals are literal `a0^3`/`a1^3` terms and identify every nuisance variable
that must be eliminated by a future typed certificate.

## Contracts

- Verify the pinned V9, V20, and V22R1 result/manifest hashes and every
  coefficient hash before parsing.
- Use a restricted Python AST and exact `Fraction` arithmetic; reject every
  syntax outside integer constants, names, `+,-,*,/,^`, and nonnegative
  integral powers.
- Recheck rho parity and literal sigma homogeneity before specialization.
- Produce no ideal-membership, radical, chart-emptiness, formal-arc, Gate-T,
  order-two, maximum-twelve, or JC2 verdict.
- Run locally only if the exact census stays below one minute and 1 GiB.  It
  is parsing/rewriting, not CAS.  Any ideal search is a separately frozen AWS
  successor.

The primary negative control is that all grade-10 rows vanish after `J1=0`,
as independently reviewed.  The positive controls are the reviewed V21 pure
grade-15 values at `A00` and `A10`.
