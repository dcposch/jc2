# Registration: generic-square load-unit ladder

Date: 2026-08-26

Scope: exact fourth- and fifth-grade source receivers on the generic square
open after the reduced `M=0` gate.  No square-branch or order-two verdict.

Frozen clients:

```text
Q / Box03 / characteristic 0
F_65521 / r6d / independent good-prime control
```

Each lane must record its tag, host, remote directory, PID, UTC start,
characteristic, source hash check, Singular version, timeout, memory cap,
stdout, stderr, return code, and fail-closed validator.  The exact-Q lane is
the mathematical promotion gate; the finite-field lane is a navigation and
software control only.

Expected source/compiler hashes are in `FREEZE.sha256`.  Both engine jobs use
`ops/aws_exact_lane.sh`, a 16 GiB virtual-memory cap, and a one-hour timeout.

The required endpoint is

```text
SQUARE_LADDER_ENDPOINT=PASS_SOURCE_GRADES4_5_AND_RADICALS
```

with every divisibility, quotient-identity, forbidden-variable, and
per-generator radical-containment sentinel equal to one.
