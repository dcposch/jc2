# Sharpened square third-tail source theorem

Date: 2026-08-26

Status: **FOUR AWS PRODUCER ENDPOINTS PASS, INCLUDING EXACT Q.  SHARPENED
THIRD-TAIL NECESSARY THEOREM; HOSTILE REVIEW PENDING.**

## Endpoints and custody

All four V2 lanes passed the source freezes, sparse-polynomial coefficient
extractor control, exact `Lambda^3` division, complete-source/Laurent row
identities, forbidden-variable sentinels, and the configured divisibility
sentinels.  Every run completed in 0.01 seconds with zero swap.

| field | host | configuration | peak RSS | stdout SHA-256 |
|---|---|---|---:|---|
| `Q` | Box03 | generic `p` | 11,956 KiB | `cf0e22ab0cf4002cb5956298776eb60751b9b089739f612131a223cbbeb0c90d` |
| `Q` | Box03 | `p=Lambda*p1` | 11,196 KiB | `df80ce502ceafc5967ff9a5a49f0194f507d1df19c25ad6bf96413a8d085b7ff` |
| `F_65521` | r6d | generic `p` | 11,304 KiB | `cf0e22ab0cf4002cb5956298776eb60751b9b089739f612131a223cbbeb0c90d` |
| `F_65521` | r6d | `p=Lambda*p1` | 10,780 KiB | `df80ce502ceafc5967ff9a5a49f0194f507d1df19c25ad6bf96413a8d085b7ff` |

V2 source freeze SHA is
`96cb2824fb5c901e7485ba725f5d92e8f116b32a8a9dde7fd6c59cb901be013e`.
The exact-Q lanes carry the characteristic-zero statement; `F_65521` is an
independent software control.

V1 is immutable deployment-negative evidence.  Its generated Singular used
an unavailable `coeff` API, all four validators failed, and it contributes no
mathematical endpoint.  V2 changed only coefficient extraction, using exact
derivative/factorial formulas with a mandatory known-polynomial control.

## Exact source identity

In the full matched square chart

```text
K=L^2+Lambda*R,
N=L*M+Lambda*S,
k10=Lambda*kappa,
T=2*L*R+M,
B=R^2+S,
```

all seven complete frozen rows are exactly divisible by `Lambda^3`.  Their
divided boundary rows are the lower-unitriangular Faber transform of the first
seven Laurent coefficients of

```text
[ P/(16*L^3) ]_-,
P=T*(12*B*L^2-T^2).                                 (1)
```

Every `R,S` coordinate is retained.  The boundary rows are independent of
`kappa,k6,k2,mu2,mu4,mu6,J`: the integral tangent
`k10=Lambda*kappa` contributes only polynomial terms at this grade.  In the
second configuration, tangent motion `p=Lambda*p1` is also absent from the
negative divided grade.  The generic identity

```text
P == -M^3  (mod L)                                  (2)
```

was certified source-side in both fields.

## Sharpened p-zero consequence

At the quadruple quartic, put

```text
L=z^2,
M=alpha*z+beta.
```

The exact source replay verifies

```text
[z^0]P=-beta^3,
[z^3](P|beta=0)=-alpha^3.                           (3)
```

Vanishing of the seven source rows implies vanishing of the first seven
Laurent rows by the unitriangular transform.  Since the proper denominator in
(1) has degree six, this is equivalent to `L^3|P`.  Equation (3) first forces
`beta=0`, then `alpha=0`.  Thus the full third-tail necessary gate forces

```text
M=0
```

also at `p=0`, even with arbitrary `R,S`, the first tangent
`p=Lambda*p1`, and the integral positive-order load
`k10=Lambda*kappa` retained.  This sharpens the older producer theorem's
p-zero conclusion `beta=0`.

For `p!=0`, (2), squarefreeness of `L`, and `deg(M)<=1` give the same
conclusion.  In particular the nonzero zero-load directions at both generic
square/discriminant root allocations and their quadruple-root limit fail the
third necessary tail gate.

## Scope

This theorem concerns fixed `p` or its displayed integral `Lambda` tangent
and `k10` of order at least one in `Lambda`.  It does not cover fractional
ramified slopes of `p` or `k10` relative to `Lambda`; those can tie earlier
normalized rays and require the registered Newton fan.  It does not analyze
the post-`M=0` p-zero cusp/odd faces, establish fan exhaustiveness, impose the
terminal `[6,2]` row or either Taylor family, construct or exclude a strict
arc, close the square component, close order two or `(8,12)`, prove maximum
twelve, or prove JC2.
