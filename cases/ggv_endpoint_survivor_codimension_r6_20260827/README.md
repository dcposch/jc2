# R6 endpoint-survivor codimension

This desk-scale case checks the dimension and parity formulas in the R6
report for degrees 2 through 64.  It is a regression harness, not the proof.

```text
PYTHONDONTWRITEBYTECODE=1 python3 -B verify_r6.py
```

