# Registration: `c>=3,r>=1` V4 root-remainder shard

Date: 2026-08-26

The frozen V3 full-coefficient membership remained in one 12-generator
standard-basis call for thirty minutes.  This V4 pins V3 and replaces only
that endpoint by the weaker sufficient root-remainder identity.  Reduce the
grade-13 remainder, grade-14 remainder, and corrected grade-15 separator
modulo `L0`; each has degree at most one in `z`.  V4 tests both coefficients
of the separator against the four coefficients of the two lower remainders.

Membership in this smaller ideal is stronger than the V3 assertion and is
sufficient because a source arc has the full lower remainders zero.  The
frozen lower-hull reduction `d3d8f30a...` is a scope sentinel: this is only
the vertical unit-load cell `a=0,c>=3,r>=1`, not the horizontal fan and not
square/order-two closure.  Exact Q is producer; `F_65521` is control.

