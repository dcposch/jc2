# TD6 V89H4 high-q total-F denominator repair preregistration

Date: 2026-08-26

Status: producer theorem candidate; no result claimed before dual AWS replay.

## Scope

Set `q2,...,q14=0`, keep `q15` absent under the reviewed target shear, and
retain independent untruncated `q16,...,q24`.  Rebuild the literal V87
transport, all 38 packed raw FIRST maps, genuine raw P12, and the frozen H1
DAG/remainder from original source bytes.

## Exact composition

H1 found a rational identity `1 in (P12,FIRST)` whose common denominator
contains the unregistered factor

```text
K = 2*C*V^2*U + 16*C*U^4 - V^4 - 14*V^2*U^3 + 16*U^6.
```

Do not invert K.  Compute and emit its exact exponent `n` in the audited H1
common denominator.  Clear the whole relation coefficientwise to obtain

```text
K^n*A = aP*P12 + sum_i ai*FIRST_i,
```

where every factor of A is registered and every multiplier is emitted in
original-source coordinates.  Fail unless `n=1` for the frozen H1 bytes.

Then consume the frozen V89K1 identity

```text
B3 = K + 2*F*(2*C*U - V^2 + 2*U^3)
```

to compose, without division by K,

```text
A*B3 = aP*P12 + sum_i ai*FIRST_i
       + F*(2*A*(2*C*U - V^2 + 2*U^3)).
```

The target `A*B3` must factor only through scalar units and registered
`U,H,B3`.  Emit all P12/FIRST/F multipliers coefficientwise, exact digests,
the final denominator audit, and omission controls for P12, one active FIRST
source, and F.  Require no K, F, or q inverse and byte-identical dual-AWS
output.

This can prove only a literal total-F identity on the high-q-only retained
normalized slice.  It is not a P12/FIRST-only collapse, low-q chart cover,
total-Rees theorem, source point, whole fixed-A3, TD6, SP-2, or JC2 result.
