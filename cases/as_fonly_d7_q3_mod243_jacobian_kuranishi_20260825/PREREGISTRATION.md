# Preregistration: D7 mod-243 Jacobian / Kuranishi gate

Date: 2026-08-25

For each of the three frozen literal mod-243 representatives in
`as_fonly_d7_q3_full_output_cone_z9_20260825`, reconstruct the degree-at-most-7
pair from the pinned source chain and compute:

1. the exact integer 91 by 72 Jacobian of every coefficient of
   `det J(P,Q)-1` (total degrees 0 through 12);
2. its exact Smith invariants and the valuation of a source-indexed rank-27
   minor whose reduction modulo 3 is nonzero;
3. the full characteristic-3 tangent kernel and cokernel;
4. the quadratic Kuranishi map `ker(L) -> coker(L)` induced by the determinant
   bracket, including the rank of its coefficient span and exact hashes; and
5. the affine next-digit obstruction `(det J(P,Q)-1)/243 mod 3` for the literal
   representative.

The relative-smoothness shortcut is accepted only if the complete 91-row
ideal is locally generated by a regular row subset.  It is rejected if a
source-supported tangent vector has a nonzero second-order cokernel class.  In
particular the nearby formal control

`P=x-x^3+t*x^7`, `Q=y+2*t*x^6*y` over `F_3[t]`

must be checked directly: its linear divergence vanishes, while its determinant
residual is expected to be `2*t^2*x^12`.  This is a negative control for any
argument that promotes mod-3 rank 27 alone to smoothness.

All substantive execution is AWS-only.  A nonzero affine obstruction kills
only the displayed literal representative, not its full mod-243 solution
fibre.  No all-depth, collision, counterexample, or JC2 conclusion is licensed.
