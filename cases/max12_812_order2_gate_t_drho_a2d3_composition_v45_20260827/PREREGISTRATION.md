# KGT-DRHO-UAC-A2D3 preregistration

Date: 2026-08-27

Conditional on `ACT-TOT-G20` passing, test the next strict unit-load
unique-`AC` contact

```text
ord(A)=2, ord(C)=5, ord(R)>=3; d=3; G=17; T=20.
```

The exact-Q AWS verifier must:

1. pin and replay the seven unspecialized general-`rho` actual-total rows
   through grade 20 and the frozen 569 tails, consuming the repaired V44R3
   exact-Q/finite-field/cross-lane custody rather than either failed
   validator attempt;
2. impose only the lower contact vanishings, then prove directly from every
   surviving row coefficient through grade 20 that no discarded later jet
   occurs;
3. derive, rather than assume, the finite maxima
   `p/A/C=3`, `R=1`, `k10=2`, `k6=k2=0`;
4. derive the shifted map from the pinned endpoint maxima, with factors `4`
   on `R` and `2` on `C`, retain `ell1..ell3`, and compare all 147 total
   coefficients (seven rows, grades 0..20) to a separately coded charged
   `D1` source construction under each of the two deck embeddings (294 exact
   equalities total);
5. pin the reviewed `B23` endpoint inventory and transcript; and
6. check both deck/root orientations, the cubic Hensel root jet, and the
   exact terminal residue `(3/2)*rho^2*cv^2`.

Until a different-model hostile review confirms this composition, the
general total/charged-`D1` source identity and the resulting contact
exclusion remain provisional.  New work may use them provisionally with
that label.

This is one contact only.  A pass is not a strict unique-`AC` cover, a
generic-square cover, a statement on an equality face, a positive-order
load, `k=0`, `rho=0`, any staged Rees chart or terminal receiver, either
global `G2` obligation, Gate T, order two, maximum twelve, JC2, or a
counterexample.
