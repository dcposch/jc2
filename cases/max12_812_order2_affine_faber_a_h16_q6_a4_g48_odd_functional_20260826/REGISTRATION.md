# Registration: full moving `H16/q6/a4` odd-functional emitter

Date: 2026-08-26

Status: preregistered exact-source navigation/certificate client; no theorem.

Reuse the frozen complete `H=16,q=6,alpha=4` source through grade 48, but
remove every standard-basis call.  Emit all seven raw rows at grades 44--48
and, at each grade `g`, the ordinary repeated-root connection combination

```text
O_g = C[g,7] - (p^2/32) C[g,3] + (p^3/64) C[g,1].
```

This is a fail-fast companion to localized V3.  Its purpose is to determine
whether the fixed-slice residual `O_48=-1/32` extends, modulo the complete
grades 44--47 predecessor, to a small unspecialized certificate.  It retains
all moving center, tangent, kernel, complement, load, deviation, and target
jets present in the pinned producer.  Exact `Q` is evidence; characteristic
`65521` is a software control only.

Raw emission is not reduction, chart exhaustion, rational regrading,
source/Rees coverage, order two, maximum twelve, or JC2.
