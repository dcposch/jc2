# Repaired selected-Q8 positive-genus composition

This text-only case freezes the repaired composition at the strict selected
`(9,12)` order-three leaf `k=mu=0, nu!=0`, conditional on meeting a
corrected-Q8 contact.

The repair replaces an informal genus-specialization paragraph by a proper
divisorial-valuation argument.  It first passes to a finite DVR extension so
the geometrically rational generic normalization becomes `P1`, then uses a
proper normal model and the ruled-residue theorem.  It also distinguishes
fibre completions, uses the full `8 x 8` contact unit for the local parameter,
and propagates singleton exclusion only by characteristic-zero Galois
isomorphism.

All computational/arithmetic parents, including the standalone positive-genus
point count, are independently reviewed.  This composition itself awaits a
fresh hostile review.  It makes no claim about components disjoint from Q8,
other maximum-twelve cells, or JC2.

