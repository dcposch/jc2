# Scope erratum: tied double-root ray only

Date: 2026-08-25  
Status: mandatory consumer firewall

The immutable prelaunch freeze is preserved.  Its compiler and emitted
Singular inputs are mathematically correct for the preregistered boundary

```text
alpha=2 beta,
Q0=A*(z-1)*(z+2),
R0(1)=A^2/3,
0<beta<6.
```

However, the sentence in `PREREGISTRATION.md` calling this the “sole
nilpotent valuation survivor” is withdrawn.  Higher coefficients of an
earlier `QR/K` layer can cancel the next `R^2/K^2` or `Q^3/K^2` layer.  Two
exact negative controls are frozen in

```text
466736cdd1a76607e6475c98d9da4b55096e88813d911722954549bdcba702c0  xmodel/max12-912-order3-d1-double-root-newton-fan-correction-firewall-20260825.md
```

Therefore consume either AWS terminal as follows:

- unit: excludes only the displayed tied `alpha=2 beta` chart;
- nonunit: a total-load-space survivor only in that chart;
- neither result closes the correction-enabled double-root Newton branches,
  the whole D1 gate, or JC2.

No equation, coefficient image, target weight, saturation, boundary equation,
or normal direction in the frozen solver source is changed by this erratum.
The error was solely the claimed completeness of the input fan.
