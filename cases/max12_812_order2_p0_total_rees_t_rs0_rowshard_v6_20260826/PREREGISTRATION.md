# Preregistration: seven-way row-sharded `T-rs-0`

Date: 2026-08-26

Status: **NAVIGATION-ONLY PARALLEL SOFTWARE IMPLEMENTATION. NO REES,
CHART, MOVING-`p`, ORDER-TWO, MAXIMUM-TWELVE, OR JC2 VERDICT.**

The reviewed streaming V2 evaluates seven independent Faber rows
sequentially.  V6 asks that frozen compiler to emit its full program, splits
out the seven literal row blocks without reconstructing any formula, and
runs one block per AWS process.  In each shard, the 83 dependency tests use
the V5 formal-derivative test.  The frozen tail census gives tested-atom
exponent bound 72, strictly below every allowed positive characteristic.

Each row independently checks total/frozen specialization, rho-deck
invariance, exact quotient extraction through grade 12, coefficient
specialization, term counts, and all 83 support bits.  The global negative
controls and support/grade bits are OR-combined by a fail-closed parser.
Rows 2, 3, and 6 write the six retained coefficients.  Only after all seven
row transcripts and AWS metadata validate does a separate Singular
certificate recombine those six frozen coefficients and test the reviewed
cusp identity, its omission negative control, and total `Delta` including
rho-evenness, rho-square divisibility, and multiplication-back.

V6 first runs at the same two good primes and must agree with sequential V5
and reviewed V2.  It is intended to make the mandatory exact-Q/fresh-prime
validation parallel.  No row-sharded result can substitute for the actual
`D_+(rs)` total-Rees/base-change computation.
