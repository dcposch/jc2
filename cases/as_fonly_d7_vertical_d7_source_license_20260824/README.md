# Vertical D7 full-E1 source license

This portable supplemental producer independently derives the total-degree
seven part of the exact integer residual `E1+M`.  It closes the source-scope
gap left by the predecessor D9/D8 review and licenses the eight D7 rows used
by the separately frozen cap-boundary component package.

Run:

```sh
./replay_all.sh
```

The comparison to the earlier generator is a regression only.  The source
identity, degree bounds, division by three, vanishing double-Frobenius class,
and full residual are constructed independently over the integers first.

Scope is only the charged vertical `p=3,D=7` normal form at total degree
seven.  No next carry, lift/no-lift, characteristic-zero, counterexample, or
JC2 claim is made.
