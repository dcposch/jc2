# V84R2 block (0,1) preregistration

This is one disjoint quadratic Kuranishi shard after the proof-grade V84R2
block `(0,0)` endpoint.  It consumes the exact same source archive,
`71934436bf932284f136d7742b398208504ef59bc07f29969bad91e6bf32abc3`,
but sets `TD6_K2_BLOCK_A=0` and `TD6_K2_BLOCK_B=1`.

The active axes are `q2,q3,q4,q5,q6,q7,q8,q9`; the registered quadratic
slots are exactly their sixteen cross-block unordered products.  The run must
retain the original transport, FIRST, and previous/pole source-combination
replays and all V84R2 typing/Taylor/omission controls.  Any missing table,
nonzero exit, source-hash mismatch, denominator omission, or control failure
is no mathematical verdict.

Execution is AWS/Linux only under an 8 GiB virtual-memory cap and a six-hour
timeout.  The first producer is placed on r6d; a mirror is a later review gate
if the table contains a nonzero coordinate or if custody is otherwise
ambiguous.

Scope is the fixed source-typed A3 section on the common licensed principal
open, through previous/pole, for these sixteen square-zero quadratic slots
only.  No statement about current rows, rank-drop fibres, formal
integrability, a nonlinear family, TD6, SP-2, landing, or JC2 is licensed.
