# TD6 V84R2 disjoint quadratic block `(0,1)`

This immutable AWS evidence package extends the reviewed V84R2 pilot from
the diagonal q2--q5 block to the disjoint block

```text
{q2,q3,q4,q5} x {q6,q7,q8,q9}.
```

The r6d lane was registered as
`td6_v84r2_k2_r6d_block_0_1_20260826T061826Z`, used an 8 GiB virtual-memory
cap and six-hour timeout, and completed with `rc=0`.  It rebuilt transport,
FIRST, and previous/pole from the same hash-pinned source archive as V84R2
and replayed the original-source combinations at every stage.

All 16 registered quadratic pair counts are exactly zero.  The emitted
table is header-only, SHA-256
`b93bed47384146af9d18c44cc51897aa95455094fa55cec51470d2ea3cc737ab`,
and the denominator is the unit `(1, [])`, file SHA-256
`8041f53d2bb2b7e92f9e047d2ca6bad668bac36b5f2a1e0a146cec71be70af43`.

Scope is the same fixed source-typed A3 common generic principal open and
previous/pole stage as V84R2.  This is a producer-tier disjoint-block
source-incidence result.  It does not cover current, other quadratic pairs,
rank-drop fibres, nonlinear integration, a family, TD6, SP-2, landing, or
JC2.  The current base remains inconsistent.
