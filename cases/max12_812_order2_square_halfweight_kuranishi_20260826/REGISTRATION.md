# Registration: generic-square correction-aware half-weight receiver

Date: 2026-08-26

Scope: the normalized ray `Lambda=sigma^2`, `M=sigma^3*A`, `S=sigma*C`
on `D(p*k10)`.  No claim that this one ray exhausts the square Newton fan.

Run exact `Q` on Box03 and an independent `F_65521` control on r6d.  Both
lanes use `ops/aws_exact_lane.sh`, a 16 GiB cap, and a one-hour timeout.  They
must record the registered tag, host, remote directory, PID, UTC start,
engine version, input hash, stdout/stderr, resource report, return code, and
fail-closed validation.

Required endpoint:

```text
SQUARE_HALFWEIGHT_ENDPOINT=PASS_CORRECTION_AWARE_NORMALIZED_RAY
```

All source/divisibility/identity/forbidden-variable sentinels and both
per-generator radical containments must equal one.  Exact `Q` is the
promotion gate; the good-prime lane is an independent compiler/control lane.
