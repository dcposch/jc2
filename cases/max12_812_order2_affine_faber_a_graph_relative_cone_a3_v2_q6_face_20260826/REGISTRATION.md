# Registration: repaired graph-relative `a=3` cone and `q=6` face

Date: 2026-08-26

Status: preregistered exact support classification; no predecessor/source theorem.

The V1 expectation of a direct unit at `a=3,q=6` failed identically on
both AWS lanes.  In addition to the intrinsic cubic, two nondeviation terms
tie:

```text
-3*X^2*a*lambda^2*M^2/8,
+3*Y^2*a*lambda^2*E/2.
```

V2 preserves that negative control and asks for the exact repaired split:

- at `a=3,q>6`, the intrinsic cubic is unique once `d6,d2,dm` have
  strictly positive excess above weight 42;
- at `a>3,q>=6`, monotonicity in the center order gives the same unit;
- at `a=3,q=6`, emit the exact three-term nontransverse equality face plus
  every strict-deviation base tie for predecessor reduction.

Require dual support custody, exact threshold six, exactly the two displayed
nonstrict competitors, no central `K10` term at or below weight 45, and no
term of base weight below 45.  Run only on AWS.
