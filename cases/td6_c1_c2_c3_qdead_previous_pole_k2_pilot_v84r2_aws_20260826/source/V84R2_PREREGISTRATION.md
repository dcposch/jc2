# V84R2 preregistration — authoritative typed qd source base

V84R is preserved as a failed-control successor. Its additional assertion
equated two different presentations: the normalized typed
`qd.source_rhs(key).value` and the legacy scalar RHS stored on transport rows.
That equality is not part of the reviewed source ancestry outside the X
boundary and failed before any K2 table.

V84R2 makes the smallest correction: the typed `E(C,V,U)` value returned by
`qd.source_rhs` is authoritative, exactly as in the reviewed source compiler.
It retains an exact equality check against the legacy RHS on every X-boundary
row, where the presentations do agree, and retains all V84R controls:

- typed E3 source-base assertion;
- zero quadratic part on every source RHS;
- unique active q-boundary singleton with coefficient one;
- q2/B agreement and per-axis omission controls;
- symbolic-center rank-3470 transport replay;
- quadratic ring, direct q-prime, q15 exclusion, source replay, wrong-row,
  omission, and canonical serialization controls.

The bounded dual-AWS wave remains block `(0,0)` only under 8-GiB/6-hour caps.
No K2 interpretation is permitted unless both hosts emit a real exact table
and agree after host/tag normalization.
