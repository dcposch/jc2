# Vertical full-C5 D7 gate

This portable producer attaches all homogeneous degree-five current-digit
coefficients to the reviewed vertical D8 core and to the source-licensed D7
residual.  It proves the exact `19 x 17` affine gate and performs both a
structural rank compiler and an independent literal-F3 enumeration of all
six first-digit Frobenius values over all `3^7` structural bases.

Run:

```sh
./replay_all.sh
```

Expected runtime is about 30 seconds and peak RSS below 50 MB on the producer
machine.  No Singular, Lean, AWS, or large-memory computation is used.

The visible current-digit count omits six derivative-zero homogeneous
degree-six spectator directions and lower-degree divergence-kernel choices.
The gate is a compatibility theorem, not a count of distinct full maps.

Scope: the charged vertical `p=3,D=7` branch through the D7 boundary only.
No following Cartier/carry, all-depth lift/no-lift, characteristic-zero,
counterexample, or JC2 claim is made.
