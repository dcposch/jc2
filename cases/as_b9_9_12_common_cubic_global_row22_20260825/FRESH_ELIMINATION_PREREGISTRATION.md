# Preregistration — eliminate all 55 final fresh-kernel parameters

Consume the pinned V1 global-family compiler and its exact canonical
`17+78+55` parameterization.  For each deterministic predecessor control in
`17+78` variables, retain the entire 55-dimensional final fresh-kernel fibre.
Project the next `3^12` right-hand side through one fixed 205-dimensional
left-cokernel basis, solve the resulting affine `205 x 55` system, and then
reconstruct all 149 next digits.

Shard the 447 predecessor controls (origin, signed basis, and 256 seeded
mixed controls) by deterministic index.  Every SAT point must literal-replay
all 299 equations modulo `3^12`.  UNSAT controls are sampled pointwise only;
they do not certify the full predecessor family.  The exact symbolic
95-variable coefficient/rank-stratification remains the theorem target.
