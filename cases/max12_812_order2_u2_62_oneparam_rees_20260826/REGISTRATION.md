# Registration: order-two `U=2,[6,2]` one-parameter Rees race

Status: source preregistered; AWS-only endpoints not yet launched.

The compiler consumes the frozen exact seven-tail JSON, verifies its full
hash, canonical all-tail digest, every Faber weight, and affine linearity in
the three lower loads.  It emits the one-parameter family proved equivalent
to the two-parameter strict boundary in the provisional producer theorem.
All seven finite loads remain variables.  No generic-chart load projection
is used.

The first race is exact Q on r6d and characteristic `32003` on Box03.  Each
launch records host, job directory, launcher PID, UTC start, characteristic,
timeout, memory cap, engine version, frozen source check, compiled input
hash, stage sentinels, resource record, and validation.  Both use a two-hour
cap.  A timeout or failed shard is no mathematical verdict.

Scope:

```text
oneparameter_boundary_equivalence=PROVISIONAL_PRODUCER_THEOREM
finite_loads=ALL_RETAINED
strict_rees_endpoint=NOT_YET_RUN
Taylor_x0=NOT_COMPILED
Taylor_x1=NOT_COMPILED
order2_closed=false
JC2=NOT_CLAIMED
```
