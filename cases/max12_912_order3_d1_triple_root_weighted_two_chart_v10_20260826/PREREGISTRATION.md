# Preregistration: weighted triple-root two-chart discriminator V10

Date: 2026-08-26 UTC

Use the weighted blow-up of `(a,h)=(0,0)` with weights `(1,3)`.

In the a-chart put `h=a^3*s`.  Reconstruct the exact V9 universal witness
from source and prove on all 48 terms that, under the normalized V8 cell with
`ell=wt(Lambda)-wt(a)>0`, every axis-valuation coefficient cancels, every
closed-corner margin is nonnegative, and each non-target equality term has a
positive open slope.  This tests the full finite-axis-valuation cone
`wt(h)>=3wt(a)`, not another h grade.

In the h-chart adjoin `t` with `h=t^3` and put `a=t*b`.  Verify the residual
cubic and discriminant exactly.  Recheck from the frozen charged tails that
row `i` has ordinary weight `12+i`; together with the target weights 15, 18,
and 20, this is the exact scaling-covariance bridge needed to apply the
reviewed squarefree order-20 theorem to the residual cubic when the
rescaled Lambda has positive valuation.  Its exceptional residual is
squarefree unless `4b^3+1=0`; that divisor must map to the a-chart boundary
`s=-4` and the opposite double-root axis.  Consume the pinned reviewed
squarefree theorem only at this exact scope.

Run forward/reverse source reconstructions on two AWS hosts.  This is a
directional weighted-blow-up result in the frozen ordinary common-cubic
source, conditional on positive rescaled Lambda and V9 review.  It does not
cover the central arc `a=h=0` identically, nonpositive rescaled Lambda,
moving loads in the V8 witness, another source chart, D1, or JC2.
