# Result: independent exact row certificate for the square A-prolongation

Date: 2026-08-26

Status: **DUAL-AWS PASS; EXACT-Q PRODUCER CERTIFICATE FOR ONE HIGH-CONTACT
CONE.  NO NORMAL-FAN, SQUARE-BRANCH, OR ORDER-TWO VERDICT.**

## 1. Endpoints

| field | host | tag | rc / validator | elapsed | peak RSS | swap |
|---|---|---|---|---:|---:|---:|
| `Q` | Box03 | `max12_812_order2_square_aprolong_owner_v2_q_20260826T075900Z_box03` | `0` / `PASS_SQUARE_APROLONG` | 0.02 s | 13,336 KiB | 0 |
| `F_65521` | r6d | `max12_812_order2_square_aprolong_owner_v2_p65521_20260826T075900Z_r6d` | `0` / `PASS_SQUARE_APROLONG` | 0.01 s | 12,304 KiB | 0 |

The exact-Q stdout SHA is
`24f485efa3a38228c5953d66bf635e4e09c7a22ec38c0ae31924d0bf7dd9bb63`;
the independent good-prime stdout SHA is
`9c38ef1f8f8a0a8191b4c1e5afab6a06f470a44ababa3faf7f174e83a6c0f1e1`.
Both lanes independently verified the complete source freeze before
compilation and used zero swap.

## 2. Exact source identities

On `D(p*k0)`, the registered high-contact cone is

```text
Lambda=sigma^2,
K=L^2+sigma^4*B0+sigma^5*B1+...,
D=L*A0+sigma*L*A1+sigma^4*E0+sigma^5*E1+...,
L=z^2+p/2,
```

with the already-charged load `sigma^4*(k0+sigma*k1)`.

All seven complete frozen loaded source rows are exactly divisible by
`sigma^14`.  After division, their constant and linear coefficients in
`sigma` are the source grades `g14_l,g15_l`.  They contain none of the later
loads `k6,k2,mu2,mu4,mu6,J`.

The client independently formed the Laurent receivers

```text
h14 = [ (3/4) A0 E0/L
       -(3/8) B0 A0^2/L^2
       +(5/32) k0 A0^2/L ]_-,

h15 = [ (3/4)(A0 E1+A1 E0)/L
       -(3/8)(B1 A0^2+2 B0 A0 A1)/L^2
       +(5/32)(k1 A0^2+2 k0 A0 A1)/L
       -(1/16) A0^3/L^3 ]_-.
```

For every `l=1,...,7`, exact coefficientwise characteristic-zero identities
verify

```text
g14_l = sum_(j<=l) T_(l,j)(p) h14_j,
g15_l = sum_(j<=l) T_(l,j)(p) h15_j,
```

where `T` is lower unitriangular and is generated by

```text
q^2+(p/2)*v^2=1,    q=z/w,    v=1/w.
```

The same independently compiled identities passed over `F_65521`.

## 3. Characteristic-zero separator

After clearing `16 L^3`, the grade-fifteen numerator is

```text
Num15 = 12 L^2(A0 E1+A1 E0)
        -6 L(B1 A0^2+2 B0 A0 A1)
        +(5/2)L^2(k1 A0^2+2 k0 A0 A1)
        -A0^3.
```

The exact client checks `Num15 == -A0^3 (mod L)`.  Its degree is at most
the degree six of `L^3`; hence vanishing of the seven negative Laurent
coefficients forces `L^3 | Num15`.  Reduction modulo `L` gives

```text
L | A0^3.
```

On `D(p)`, `L=z^2+p/2` is squarefree of degree two, whereas `A0` has degree
at most one.  Therefore `A0=0`.  This conclusion is insensitive to the full
displayed grade-fourteen correction scheme and to `A1,B0,B1,E0,E1,k1`.

## 4. Firewall

This exact certificate eliminates only the surviving `A0` direction in the
high-contact cone

```text
ord_sigma(R)>=2,    ord_sigma(C)>=4
```

after the reviewed half-weight receiver.  It does not prove that this cone
exhausts the normalized Newton/Rees fan.  Smaller valuations of `R` or `C`,
the degenerate `p=0` square/discriminant intersection, later terminal and
Taylor receivers, the entire square component, all order two, `(8,12)`,
maximum twelve, and JC2 remain open.

This owner-v2 run is an independent row-identity control.  The root-owned
producer under the adjacent non-owner path has separate frozen source and
separate AWS evidence; neither endpoint is inferred from the other.
