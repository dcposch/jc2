# Registration: minimal moving-series odd functional at grade 48

Date: 2026-08-26

Status: preregistered exact-source functional emitter; no theorem.

On the frozen complete `H=16,q=6,alpha=4` source form the series-level
ordinary connection

```text
Fodd = P7 - (E(s)^2/32) P3 + (E(s)^3/64) P1.
```

Emit only its coefficients in grades 44--48.  Using the full moving `E(s)`
absorbs the tangent-connection multiples that are otherwise visible when the
central coefficient `p` is used row by row.  All source loads, targets,
center, kernels, complements, and deviations remain in `P1,P3,P7`; there is
no standard-basis computation or specialization.

The aim is to test whether the fixed-slice `-1/32` residual is the
specialization of a small unspecialized row functional.  Exact `Q` is
evidence and characteristic `65521` is a control.  Printed coefficients are
navigation, not a predecessor reduction, unit theorem, rational regrading,
source/Rees coverage, order two, maximum twelve, or JC2.
