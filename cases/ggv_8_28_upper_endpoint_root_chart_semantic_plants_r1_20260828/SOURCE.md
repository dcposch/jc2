# Source note

`SOURCE.tar.gz` is the immutable AWS payload. It contains the preregistration,
manifest, fail-closed worker and supervisor, transcript gate, hostile and positive
Singular controls, the patch/verification adapter, and the entire original root
terminal archive.

The adapter extracts exactly `output/triple03/chart.sing`, verifies its frozen
SHA, writes those bytes unchanged as `ORIGINAL_TRIPLE03_chart.sing`, and creates a
separately named script by inserting one semantic-control block immediately before
the original endpoint classification prints. A source-level mutation deleting the
required endpoint-plant success marker is rejected before production.

No original archive member, preregistered source, upstream certificate, canonical
ledger, or top-level mathematical file was mutated.
