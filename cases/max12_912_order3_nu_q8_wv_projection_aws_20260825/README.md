# Q8 global `(w,v)` projection (AWS-only)

This exact modular discriminator adjoins the rational continuation

```text
v*x5 = x3-2*x5
```

on the genuine punctured approximate-cubic quotient and eliminates all
other coordinates.  The open chart is imposed fail-closed by

```text
inv*w*x5*(x3-2*x5)=1.
```

The resulting elimination ideal in `(w,v)` is a direct projection of the
open quotient curve.  Its factorization may identify the factor whose
`w=0` fibre contains `Q8`, providing a cheaper component/monodromy route than
full primary decomposition.  Every finite-field result is modular support
only until lifted and substituted exactly in characteristic zero.

Run only on AWS under the supplied 12-hour / 256-GiB guard.

