# Actual-total all-row grade-15 export V22

Date: 2026-08-27

Status: **PREREGISTERED DUAL-AWS PRODUCER; NO FULL-POLYNOMIAL VERDICT YET.**

## Question

The confirmatory V21 section replay predicts that grade 15 is the first
actual-total source grade nonzero on both pure `J2=(a0,a1)` directions:

```text
A00: Tg15_6=-1/16,
A10: Tg15_3=-1/16, Tg15_5=-(3/32)rho^2,
     Tg15_7=-(3/128)rho^4.
```

Export the seven full literal grade-15 polynomials.  They are the first
plausible source input for typed stage-two chart identities.

## Frozen producer contract

1. Reconstruct all 569 canonical tails in all seven rows and enforce tail
   weights `12+j`.
2. Extend the exercised V20 emitter by exactly one jet coefficient:
   `ell15,cs13,rs13,az10,ac10,ez10,ec10,k10_11,k6_3`.  Set the underlying
   sparse-series cutoff to 15 before constructing any series.  No other
   source convention may change.
3. Reproduce the 21 V9 grade-10--12 files and all fourteen V20 grade-13/14
   files byte-for-byte in each characteristic before accepting grade 15.
4. Export `Tg15_1..Tg15_7` over exact `Q` and independently over `F_65521`.
   Record hashes, term counts, supports, and contributing-tail counts.
5. Require rho-deck evenness and exact sigma-weight 15 of every monomial,
   using the literal jet weights induced by the emitter shifts.
6. Require the four section evaluations to agree exactly with V21, including
   full zero residuals for `CS0` and `Z00`.
7. Retain one frozen-tail `+1` source-sensitivity control per row whose old
   V9 bridge must fail.
8. Use only hash-pinned, capped AWS EC2 runs.  No qring, randomization, local
   full export, or `jc2-lean` access is permitted.

## Interpretation fixed before output

- A nonzero constant at `A00` or `A10` kills that point only.  It does not
  empty a `J2` chart or prove an exceptional-power containment.
- The full grade-15 rows may be used to design a typed chart certificate only
  after different-model review.
- A grade-15 row cannot break `CS0` or `Z00` if the V21 bridge passes; that is
  a required positive control, not a failure of this export.
- No output proves Gate T, order two, maximum twelve, or JC2.

