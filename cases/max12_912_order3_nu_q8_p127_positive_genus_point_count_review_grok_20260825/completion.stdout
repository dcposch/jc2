The complete referee report is in `xmodel/max12-912-order3-nu-q8-p127-positive-genus-point-count-review-grok-20260825.md`.

The standalone claim holds: after the consumed geometric-integrality theorem, the smooth projective normalization of pinned `H=0` over `F_127` has positive genus. The quadratic modulus `x^2-x+3` is irreducible by reciprocity; `deg gcd(H(w,v), v^{16129}-v)` counts distinct affine `K`-points and the second gcd with both partials counts singular ones, including empty-fibre degree 0. Both AWS partitions cover `F_{127^2}` and agree at `16174 / 16168 / 6`. Those two runs are identical-source partitioned replay, not independent engines. `16168 > 16130` is a lower bound, so uncounted points at infinity and singular branches only strengthen it. Scope stays the standalone plane curve.

Optional wording notes only: the “independent execution” title, “both singular fibres,” and V1 directories also sitting unpacked in the harvest trees.

CONFIRMED
