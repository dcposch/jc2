# Custody: Grok hostile review of the F_127^2 point-count genus certificate

This directory freezes the control-plane metadata for the different-model
hostile review of the standalone positive-genus producer.  It does not change
the producer case or broaden its claim.

The reviewed theorem is exactly: after consuming the separately reviewed
geometric integrality of the pinned plane curve `H(w,v)=0` over `F_127`, its
smooth projective normalization has positive genus.  The review does not
establish quotient membership, source-component identity, characteristic-zero
specialization, trajectory exclusion, maximum degree twelve, or JC2.

The review ran through the Grok CLI with prompt and target paths recorded in
`RUN.meta`.  The managed session exited zero and named exactly the requested
target report.  The captured completion is in `completion.stdout`.  Its verdict
is `CONFIRMED`.  The reviewer explicitly distinguishes the two AWS executions
as identical-source partitioned replay rather than independent engines.

Only the prompt and requested report were new in the narrowly scoped xmodel
path audit.  This custody supplement was then created separately; no frozen
producer bytes were changed.

