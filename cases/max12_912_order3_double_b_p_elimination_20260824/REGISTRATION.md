# Registration: exact double-`B` `p`-elimination

- Date: 2026-08-24
- Status: producer-exact; hostile different-model review required
- Source: corrected normalized order-three double-`B` saturation at `p`
- Output: one primitive `P(p) in Q[p]`, degree 630, 71 terms `p^(9*k)`, nonzero constant

For any field-valued point of the normalized double-`B` ideal, either `p=0`
or `p` is invertible and the point extends to the saturated system, where the
exact elimination output forces `P(p)=0`.  Thus `p` is algebraic over `Q` at
every point.  Combined with `3*p+10*r8=0` and the reviewed
`9*r8'=j/u!=0`, this provisionally excludes actual trajectories on this leaf.
No larger maximum-12 or JC2 conclusion is registered.

