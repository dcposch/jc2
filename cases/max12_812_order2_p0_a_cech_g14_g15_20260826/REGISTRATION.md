# Registration: `p=0` residual `A`-Cech grade-14/15 replay

Date: 2026-08-26

Specialize the frozen complete high-contact square source to `p=0` before any
localization by `p`.  Reconstruct all seven loaded source rows at absolute
grades fourteen and fifteen, retain every first correction and every later
load/target in the emitter, and verify the source/Faber bridge at the
collision centre.

The two preregistered raw triangular endpoints are

```text
D(a0):
  g15[6]=-(1/16)*a0^3;

V(a0) intersect D(a1):
  g14[2]=-(3/32)*a1^2*br0,
  g14[1]=(3/8)*a1*(e0-a1*bs0),
  g15[3] mod (br0,e0-a1*bs0)=-(1/16)*a1^3.
```

Here `B0=bs0*z+br0/4` and `E0=(e1*z+e0)/2`.  Exact Q carries the producer
claim; `F_65521` is an independent control.  Run on two registered AWS hosts.

This client treats only the fixed `p=0` residual high-contact `A` charts.  It
does not prove their pullback from one total Kummer/Rees family, exclude
positive-valuation moving `p`, handle the all-zero higher-contact receiver,
or close the square branch or order two.

