# Producer result: fixed `p=0` residual `A`-Cech charts

Date: 2026-08-26

Status: **PROVISIONAL PRODUCER RESULT; DUAL SOURCE REPLAY PASSED.  HOSTILE
REVIEW AND TOTAL-REES GLUE ARE REQUIRED BEFORE COMPOSITION.**

The frozen complete source was specialized to `p=0` before any localization
by `p`.  All seven loaded rows were reconstructed, were exactly divisible at
absolute grades fourteen and fifteen, and agreed coefficientwise with the
collision-centre Laurent/Faber receivers.  Every first correction was
retained; `k6`, `k2`, and every target remained in the source and were checked
absent at these grades.

On the constant `A` open, the sixth raw grade-fifteen row is literally

```text
g15[6]=-(1/16)*a0^3.                                (1)
```

It is a unit on `D(a0)`.

On `V(a0) intersect D(a1)`, with
`B0=bs0*z+br0/4` and `E0=(e1*z+e0)/2`, the exact raw grade-fourteen rows are

```text
g14[2]=-(3/32)*a1^2*br0,
g14[1]= (3/8)*a1*(e0-a1*bs0).                      (2)
```

They force `br0=0` and `e0=a1*bs0` without a radical.  After only those raw
triangular equations, the third grade-fifteen row is

```text
g15[3]=-(1/16)*a1^3,                                (3)
```

a unit on `D(a1)`.  Thus the two residual fixed-centre `A` opens are
provisionally empty.

Exact Q on Box03 and an independent `F_65521` control on r6d both returned
`engine_rc=0` and `PASS_P0_A_CECH_G14_G15`, with zero swap.  The exact-Q
printed rows themselves display (1)--(3); no radical, saturation, or stored
PASS token is needed for the algebra.

This result is restricted to the fixed `p=0` high-contact `A` receiver after
the leading `R,C` coordinates have been routed to zero.  It does not prove
that the cusp, odd, `C`, and `A` opens are pullbacks of one total raw
Kummer/Rees family; it therefore does not yet invoke empty-special-fibre
propagation or exclude any positive-valuation moving-`p` arc.  It does not
handle the all-zero higher-contact receiver, `k0=0`, terminal/Taylor closure,
the whole square branch, order two, `(8,12)`, maximum twelve, or JC2.

