# Finite `x5=0` cylinder local-germ producer

This AWS-only case checks that the raw `w=0` line extends to an exact
two-dimensional cylinder in the full six-row source and that the source has
the same regular local germ along every finite point `x3!=0`. It separates
that boundary leaf from closure of the selected `x5!=0` open. See the
preregistration for the strict firewall.

V1 and V2 are preserved negative software controls: V1 had two source-level
Singular construction defects, while V2 proved every mathematical identity
but emitted a harmless `redefining` diagnostic and therefore failed closed.
Only the diagnostic-free V3 Box02/Box03 lanes and the frozen replay are
acceptance endpoints.
