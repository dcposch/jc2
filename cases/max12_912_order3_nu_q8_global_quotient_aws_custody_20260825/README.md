# Q8 global-quotient AWS custody supplement

This directory preserves the exact r6d bytes from tag
`q8_global_quotient_evidence_20260825T000746Z`: parent manifest output,
frozen replay stdout/stderr/meta, independent hostile-probe
stdout/stderr/meta/source, launch metadata, supervisor files, lane log, and
PID ledger.

It supplements the already-CONFIRMED global-quotient gate with execution
custody only and makes no new mathematical claim.  The nonempty replay and
probe stderr files contain only `/usr/bin/time -v` resource accounting with
exit status zero; both supervisor files and `manifest.stderr` are empty.

