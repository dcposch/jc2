# TD6 V89H17 P13 coordinate-8 cubic router

Date: 2026-08-26

Status: preregistered producer theorem; require agreeing dual AWS custody.

Audit the complete frozen P13 normal form coefficientwise at E3 coordinate 8.
Prove that every q-dependent and quotient-variable-dependent coefficient is
zero and factor the sole remaining coefficient exactly as

```text
(437500/3)*(U/V^3)*S(U,V),
S=5*V^6+68*V^4*U^3-368*V^2*U^6+128*U^9.
```

Verify the factorization and irreducibility of `S` over `Q[U,V]` by exact
arithmetic, plus the H16 sample value.  On the frozen `F=0` slice, where
`B3=V^4`, the registered open makes `U` and `V` units; therefore P13/FIRST
can vanish only on `S=0`.  This is a routing divisor, not an exclusion of its
closed residue, a literal total-source theorem, or a later-CURRENT result.
