# TD6 V89H19R1 raw P13 coordinate-12 audit

Date: 2026-08-26

Status: repaired preregistered producer diagnostic; require agreeing dual AWS
custody.  The original H19 package is no-verdict because it did not freeze the
transitive H15/H12 compiler closure and its purported omission control only
dropped a serialized output record.

Rebuild literal total-family CURRENT degree 13 from the frozen compiler and
extract E3 coordinate 12 before FIRST elimination, both over the independent
total base `(C,V,U)` and after exact `F=0` specialization. Retain every 132
transport variable and all 22 q coordinates. Emit every nonzero
`(parameter,q)` coefficient exactly.

## Repaired custody

The client hash-pins and recursively verifies:

1. `SOURCE_P13_FULL_NORMAL_FORM.sha256`;
2. `SOURCE_P12_FULL_NORMAL_FORM.sha256`;
3. `SOURCE_P12_FLAG.sha256`;
4. the complete `PAYLOAD_CLOSURE.sha256` compiler payload;
5. the H15 vector-record-count erratum.

Every manifest entry must pass before compilation. The H15 source archive is
separately hash-bound by the AWS orchestrator.

## Genuine source-term omission control

The replay independently decomposes the literal degree-13 compiler into its
ordered raw equation addends before aggregation. Their exact sum must equal
the frozen H15 compiler output. An AWS-only source census found exactly these
four ordered addends with nonzero specialized coordinate 12:

1. `f1_times_dg2:i=13:j=1`;
2. `2f2_times_dg1:i=12:j=2`;
3. `minus2_df1_times_g2:i=13:j=1`;
4. `minus_df2_times_g1:i=12:j=2`.

The complete ordered census and the first label were fixed before the repaired
dual producer was launched. The producer removes exactly the first
preregistered source addend before summation, recompiles the raw polynomial,
and requires the specialized coordinate-12 record set to change. Removing an
already serialized output record is forbidden.

If the specialized raw coordinate is already the single unit
`(3500000000/9)*(U/V)`, the H18 exclusion is a direct raw P13 row. Otherwise
the client identifies the exact support that FIRST elimination cancels. No
total-F or whole-TD6 claim is authorized.
