# TD6 V89H15 all-q literal-P13 full-normal-form gate

Date: 2026-08-26

Status: producer gate; no result claimed before two independent AWS replays
agree exactly.

In the frozen V89H12 scope, compile literal CURRENT degree 13 from the same
generic `compile_x_current` formula used at degree 12.  Retain all 22
independent, untruncated q coordinates `q2,...,q14,q16,...,q24`, with q15
absent only as the reviewed target shear, impose exact `F=0`, and work on
`D(U*H*B3)`.

Rebuild all 38 literal FIRST rows and the complete 132-coordinate transported
kernel.  Reconstruct the frozen common flag, solve the constant direction and
all 94 quotient directions, directly replay each solution in the original
FIRST matrix, and require the affine pivot-map digest to equal V89H12.  Then
substitute the complete map into every literal P13 term without any q-degree
or parameter-support truncation.

Emit the complete P13 normal form, exact parameter/q/E3-coordinate support,
denominator ledger, and omission controls.  Every denominator factor must
remain in the registered radical generated after `F=0` by `U`, `V`, and
`V^2-4U^3`; any new factor is fail-closed.

A pass proves only a complete canonical P13 normal form modulo original FIRST
in this exact slice.  It does not combine P12 and P13, solve the P12 rational
section, exclude a source point, provide a total-Rees map, close TD6, or
resolve JC2.
