# Frozen order-16/32 hierarchical common-denominator control

This portable custody package records the AWS Box03 replay that refined the
all-1330 simultaneous Padé gate into seven per-coordinate groups, two natural
triples, the six true-centre coordinates, and the global seven-coordinate
group.  Every group returned no unique full-rank denominator throughout the
complete order-32 rectangle `1<=d<=31`, `0<=m<=30`.

This is a bounded negative only.  Rank-deficient affine fits are not excluded,
higher degree is open, and no quotient component, rational graph, original-row
identity, characteristic-zero component, or trajectory conclusion follows.
The source is cross-pinned in the sibling high-order producer package; its
bytes are immutable after this run.

