# Registration — `(9,12)` order-three `nu!=0` parity `x5=0`

Date: `2026-08-24`  
Status: `PRODUCER-EXACT / REVIEW REQUIRED`

On the exact parity specialization `q=x0=x2=x4=k=0`, the `x5=0` branch of
the `k=mu=0`, `nu!=0` fibre forces `r8=0` and therefore contradicts
`9*r8'=j/u`. In the remaining parity system, the linear pivot
`A=x3-2*p*x5` cannot vanish; eliminating `x1` on `A!=0` is reversible.

This does not claim that the parity locus exhausts the loaded fibre or that
the remaining `A*x5!=0` chart is empty.
