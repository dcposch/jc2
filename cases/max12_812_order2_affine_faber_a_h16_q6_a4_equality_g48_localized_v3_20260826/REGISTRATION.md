# Registration: localized V3 for the `H16/q6/a4` equality wall

Date: 2026-08-26

Status: preregistered full fixed-representative producer; no theorem.

V2 retained all complete source rows and jets but saturated only the two
projective kernel charts.  The exact equality cell also has nonzero leading
center coefficient `a0` and lies in the nonzero-`K10` affine graph
neighbourhood.  V3 therefore changes the two chart equations to

```text
satu*p*m*a0*kk0*x0-1,
satu*p*m*a0*kk0*y0-1.
```

V3 also removes the unsaturated navigation computation
`std(IPRE)`/`reduce(K48,GPRE)`, which is not used by either full chart ideal
and dominated V2 runtime.  It retains verbatim the moving series, all seven
rows at grades 44--48, every target/load/deviation/kernel/complement jet,
and both full chart standard bases.  The frozen V2 compiler is pinned and
the generated source transformation must occur exactly once for each of
the three registered replacements.

Exact Q is evidence; F65521 is a control.  Unit/unit is only a
fixed-integral-representative equality-open producer result pending review;
it does not establish rational regrading, source/Rees coverage, order two,
maximum twelve, or JC2.
