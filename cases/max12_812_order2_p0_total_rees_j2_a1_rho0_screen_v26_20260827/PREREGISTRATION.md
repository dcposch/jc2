# V26 preregistration: ordered-a1 closed rho=0 face

Date: 2026-08-27

V24 found `a1^3` only after inverting `rho`; its displayed lift clears to a
rho-torsion identity and is not a chart certificate.  Before funding any
larger ansatz, specialize the same 42 V23R1 rows to the closed face `rho=0`
and test exact homogeneous membership of `a1^3` at sigma weight 15.

- If nonmembership: no ordered-a1 certificate `a1^N(1+rho W)` with `N=3`
  can exist from these rows, because reduction modulo rho would give this
  membership.
- If membership: harvest exact cofactors and use them as the constant term of
  a rho-adic lift; this does not itself prove a finite polynomial unit factor.

Exact Q, AWS only, 30-minute cap, exceptional-square negative control,
source-row positive control, direct lift replay.  Stop after this one face.

