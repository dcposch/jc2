# Global Q4/H5,J5 successor gate

Starting from the source-pinned Q5/H6 accepted-fibre compiler, adjoin the
twelve homogeneous degree-five order-81 coefficients `(H5,J5)`.  Charge:

1. the five degree-four rows
   `G4 + (H5)_x + (J5)_y = 0`;
2. the complete sixty-three displayed terminal rows in degrees 12 through 7,
   recomputed with the `H7+H6+H5` fourth-digit cross-carry;
3. every one of the 197 parent rows and every parent exact-division gate.

The first witness-first formula pins every parent variable to the corrected
V2-replayed base-513 Q5 SAT model of 2026-08-25.  A SAT model must pass a new
literal integer replay of all parent rows, the five Q4 rows, and recursive
versus literal `/243` agreement in degrees 12 through 7.

Strict scope: this restores only Q4 in one displayed parent state.  Q3
through Q0 remain missing, so even a SAT result is not a complete determinant
identity modulo 243 and the residue-ball collision theorem does not yet
attach.

All substantive execution is AWS-only.
