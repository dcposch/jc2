# R5 planted TRIPLE03 closed-complement result

Strict result:

`EXACT_ENDPOINT_DEAD_ON_FINITE_GEOMETRIC_CHART_COVER`

This is the preferred semantic packet for the literal TRIPLE03 root-chart closed
complement `V(Delta)`. It exactly agrees with the R4 eight-node recursion while
adding fail-closed planted endpoint and bordered controls on every proper chart.

- node 1: rank 4, nonempty endpoint-dead chart, both plants PASS;
- nodes 2 through 7: exact empty opens with power-membership exponent 2;
- node 8: rank 0, nonempty endpoint-dead chart, both plants PASS, next delta `1`
  and empty closed successor.

The R5 input begins with the literal ideal `(q2, Delta)`, records `q1=c8=0`, and
binds the root-delta artifact SHA
`9e07a399764ace2008ad89af5c865326b0cdb65ceab1bcc83f5edcc63463144b`.

Terminal archive SHA:
`b31a9b145b3bb8af6b6eda1be45c0b8d1cdea3819509c7e86873eb0284c3a2e9`.

This packet proves endpoint death only on the frozen closed complement through
its explicit finite geometric chart recursion. It makes no scheme-theoretic,
radical, nilpotence, other-component, root-open, or ambient endpoint claim. The
root open is handled separately by the retrospective semantic replay packet.
