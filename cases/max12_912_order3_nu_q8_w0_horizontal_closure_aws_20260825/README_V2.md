# Current interpretation — read before consuming this case

`SEMANTIC_ERRATUM.md` supersedes one sentence in the frozen preregistration.
The exact object

```text
(I + (x5*(x3-2*x5))) : w^infinity
```

is the `w`-horizontal **boundary incidence**, not automatically a union of
irreducible components generically contained in the boundary. The AWS inputs
remain valid; only the stronger component-containment reading is retired.

The selected-open object remains
`I:(w*x5*(x3-2*x5))^infinity`. Neither object is a trajectory theorem until
the relative projective, terminal, Taylor, and coverage gates are supplied.
