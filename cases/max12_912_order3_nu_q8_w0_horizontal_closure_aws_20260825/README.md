# Q8 `w=0` horizontal-closure producer

This AWS-only producer distinguishes the closure of the selected
`D(w*x5*(x3-2*x5))` source open from source components generically contained
in its boundary. See `PREREGISTRATION.md` for the exact firewalls. No output
is a trajectory theorem until terminal, Taylor, projective, and coverage
successors are composed and reviewed.
