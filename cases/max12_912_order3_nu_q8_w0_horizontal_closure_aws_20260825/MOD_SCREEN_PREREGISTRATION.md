# Preregistration: good-prime fused routing screens

Date: 2026-08-25  
Status: **FROZEN BEFORE MODULAR CAS**

The four exact-characteristic-zero fused saturation lanes remained live after
twenty minutes.  For term-order and stratum routing only, run the identical
selected-open and boundary-incidence fused programs in characteristics 127
and 32003.  The only permitted source change is

```text
ring R=0,(...),order  ->  ring R=p,(...),order.
```

Each prime runs both

```text
Hsrc = I:(w*x5*(x3-2*x5))^infinity,
Bsrc = (I+(x5*(x3-2*x5))):w^infinity,
```

followed by the four already-preregistered `w=0` strata.  `Bsrc` retains the
SEMANTIC_ERRATUM boundary-incidence meaning; no component-containment meaning
is licensed.  Every endpoint must be diagnostic-free and report all original
row remainders zero.

These screens are **non-promotional**.  Agreement at two good primes may
select a faster exact-Q term order or identify likely empty strata; it cannot
prove a characteristic-zero saturation, component, boundary, trajectory, or
Keller conclusion.  Exact-Q endpoints remain mandatory.
