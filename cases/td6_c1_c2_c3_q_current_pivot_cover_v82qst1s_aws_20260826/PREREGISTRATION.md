# TD6 V82QST1S staged-only pivot-cover pilot

Date: 2026-08-26

## Repairs and fixed boundary

V82QST1 failed because its alternative staged pivots were reconstructed in
ascending-only variable order.  V82QST1R then crossed the wrong interface by
trying to reinterpret the source-specific raw transport echelon in insertion
order.  Both are preserved as software-negative controls and have no
mathematical verdict.

V82QST1S preserves the reviewed raw transport, restriction, and genuine P12
division in their licensed ascending order.  It changes only the FIRST,
previous/pole, and CURRENT EJet pivot selector.  Those solvers eliminate every
previously inserted pivot before selecting a new one, so reverse insertion
order is their exact affine-form substitution DAG.

## Registered run and decision

On both Box03 and r6d, run `reverse:q2`, `sparse:q2`, `reverse:q10`, and
`sparse:q10`, with 2 GiB/2 h per cell.  Sparse pivots minimize denominator
degree, numerator degree, serialized size, then variable among coefficients
having one nonzero base-field coordinate.  Every source replay, q-prime
omission control, and exact Gate-0 pre-assert numerator/denominator/factor
ledger remains active.

Compare the exact radical factor ideals with ascending Gate 0.  Expand to
q3..q9 only if a pilot changes that ideal or yields a credible explicit
principal-open cover.  A cover requires cleared source identities and a
localized Bezout/Cech certificate; gcd or selected-minor factors alone do not
suffice.  Raw Gate 2 remains conditional on this comparison.

## Reviewed nested-factor pin and scope

The source bundle contains exact copies of the CONFIRMED hostile review
`a3e1315041dafdeefaf26a3a9f2f79e945c2ab70fad18436ddb416f161f9de5b`
and promotion
`6037e902a0e9c9fe3c9115d77753559060913f8d64412455de75430ab526d56b`.
On `D(U)`, `rad(g,ell)=(x,s) intersect (x+4,s^2+4)`; on `F=0`, `F=L=0`
has `x=0` or `x=-8`; generic `L=0` remains a hypersurface.

Fixed source-typed A3/already-empty-base diagnostic only.  No tangent family,
nonlinear family, TD6, SP2, or JC2 inference.

