# Preregistration: full selected saturation on the exceptional `b=1` line

Date: 2026-08-25  
Status: **FROZEN BEFORE CAS**

In the exact pinned six-row source substitute

```text
d4=1, d2=2+u
```

over the exact coefficient field `Q(c)`.  Compute

```text
C = I : (w*x5*(x3-2*x5))^infinity
```

and test the landing centre `(w,u,x1,x3,x5)`.  This is the decisive generic
`c` test for arbitrary ramification at the exceptional normal-rank value
`b=1`; the bounded weighted charts are controls only.

If the landing ideal is unit, generic `c` is excluded but exceptional
finite `c` values remain charged until a polynomial-parameter rebuild.  If
it is nonunit, print its full basis and retain the survivor.  Acceptance
requires two exact-Q AWS engines/orders, fixed source hashes, zero return
codes, and no diagnostics.  No Taylor/terminal or trajectory inference is
licensed here.

