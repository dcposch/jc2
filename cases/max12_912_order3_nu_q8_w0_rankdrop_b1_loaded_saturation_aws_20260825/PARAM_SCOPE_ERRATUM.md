# Scope erratum — frozen-`d4` polynomial-`c` landing test

Date: 2026-08-25  
Status: **NONMUTATING SUCCESSOR; ORIGINAL FROZEN BYTES PRESERVED**

The sentence in `PREREGISTRATION.md` calling the frozen substitution
`d4=1,d2=2+u` “the decisive generic `c` test for arbitrary ramification at
`b=1`” is superseded.  It is decisive only for the selected landing centre
inside that frozen slice.  Nonconstant `d4` drift is outside its source map.

For the polynomial-parameter endpoint, the exact accepted conclusion is

```text
Landing = GC + (w,u,x1,x3,x5) = (1)
```

in `Q[w,u,x1,x3,x5,c]`.  This excludes every finite value of `c` **at that
landing centre**.  It does not assert `GC=(1)` or emptiness of the whole
selected open for every finite `c`.  The output marker
`C_PARAMETER_BASIS_BEGIN` encloses the printed basis `GL` of `Landing`, not a
basis of `C`.

The Box02 result is one `std/dp` endpoint.  Its in-run files do not contain an
independent engine or a Box02 `Singular --version` capture.  Those are custody
debts, not changes to the narrow implication from the printed unit basis.

Moving `d4`, the full rank-drop line/full `Hsrc`, coefficient and other
projective infinity, terminal/Taylor realization, trajectories, the whole
`(9,12)` cell, maximum twelve, and JC2 remain open.
