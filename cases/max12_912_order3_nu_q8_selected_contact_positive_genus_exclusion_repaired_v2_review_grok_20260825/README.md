# Custody: Grok hostile review of the repaired V2 selected-Q8 exclusion

This directory freezes the control-plane custody for the final different-model
review of the repaired V2 composition.  The reviewed report proves only the
following conditional statement:

> An actual order-three trajectory in the selected corrected-Q8 contact leaf
> `k=mu=0, nu!=0`, after the reviewed cube/Faber and selected-global-quotient
> landing, cannot exist.

Grok CLI 1.0.5 read the V2 report, its frozen manifest, the hostile-confirmed
positive-genus certificate, and all parents named by the V1 manifest.  It ran
without Bash, CAS, network access, or file writes and returned `CONFIRMED`.
The exact prompt, report, and adapter run record are pinned by this package.

The review specifically confirms the V2 finite-field-of-definition repair:
the unique geometric `H`-supported source component through the winning full
contact is chosen over `Fbar_127`, descended to a finite residue field, and
only then placed over the corresponding unramified complete/Henselian DVR.
It also confirms the whole-source formal attachment, geometric-Lueroth/ruled-
residue contradiction, and characteristic-zero-only singleton propagation.

No statement is made about selected components disjoint from corrected-Q8
contacts, all `nu!=0` components, the polynomial core, all `(9,12)`, `(8,12)`,
maximum degree twelve, arbitrary Keller pairs, or JC2.

