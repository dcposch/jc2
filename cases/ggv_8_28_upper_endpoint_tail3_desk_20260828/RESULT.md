# Cutoff-three square-tail desk result (provisional)

## Outcome

The fixed cutoff-three branch-P system has been independently compiled and
reduced exactly through `D22` with Python standard-library rational
arithmetic only.  It is not yet closed.  The exact new result is a low-row
field-radical cascade and one genuinely new later invariant, first forced at
`D17`:

```text
K = Q0*(b_bar*Q0 + L0^2),       b_bar = p189 - V0*R0/16.
```

The nonzero-tail `D22` successor is frozen for AWS, but has not been run.

## Literal compile and endpoint

Setting only the weight-two raw coordinates to zero retains 296 variables.
The 71 generators through `D5` have rank 40 and nullity 256.  There are 442
remaining generators from `D6` through `D22`.

The literal endpoint row is

```text
D22[X0]-1 = -1 - p32*p189 + p86*p91,
```

or `1+a*b-c*d=0`, with no individual carrier normalized:

| carrier | prefix coordinate | raw slot | coefficient slot |
|---|---:|---|---|
| `a` | `p32` | `g_1_0` | `G15[X1]` |
| `b` | `p189` | `f_0_1` | `F7[X0]` |
| `c` | `p86` | `g_0_1` | `G11[X0]` |
| `d` | `p91` | `f_1_0` | `F11[X1]` |

## Independently rederived cascade

Write `A=X^4-1`.  Literal staged compatibilities give the following
fieldwise consequences; each square-root step uses that `A` is squarefree.

```text
D7:       T=A*V,                         deg V<=5
D8:       F4=V/16+A*W,                   deg W<=8
D9:       W=A*R,                         deg R<=4
D10:      F5=R/2+A*S,                    deg S<=7
D11:      S=A*Q,                         deg Q<=3
D12:      F6=Q/2+V^2/256+A*U,            deg U<=6
D13:      U=A*L,                         deg L<=2
D14-D15:  F7-L/2-VR/16=A*M,  M=A*N,      deg N<=1
```

`D8` also has one monic noncarrier equation solving `p232`; `D11` has
monic equations solving literal carrier `c=p86` and noncarrier `p106`.
Carrier `c` is restored in the AWS target by retaining both its monic
relation and the literal determinant endpoint.  The additive gauge
`p161=F8[X0]` is absent from every literal `D6`--`D22` generator and is sliced
to zero; this is not a carrier normalization.

The tempting index-shift from cutoff five first fails at `D14`.  In corrected
scalar coordinates, actual `E14` differs from the shifted tail-five form by

```text
-(3/64)*V0*Q0*L0 -(3/8)*R0*Q0^2.
```

The corresponding exact `E15` correction is also recorded in the analysis
JSON.  Thus no tail-five certificate was imported.

## Later invariant and exact provenance

The full `D21` root-coordinate intersection has Q-vector-space rank 17.  The
degree-one ideal module generated by actual `E13,E14,E15` has rank 66;
adjoining the `D21` root core raises the rank to 67.  A representative of the
one-dimensional quotient is `K` above.  It is absent through `D16` and first
appears at `D17`.

An exact 13-term cofactor witness reconstructs `K` from old-core ideal terms
and literal staged compatibilities: `D16` indices 4 and 8, and `D17` indices
1, 5, and 9.  Every staged compatibility separately carries its linear
combination of authoritative raw source rows.  Changing the sign of `L0^2`
in `K` raises the combined rank to 68, so the live mutation is caught.

The scalar field cover of `K` has been checked without CAS:

* If `Q0=0`, `E13` gives `L0*(4*b_bar-L0)=0`.  The `L0=0` branch forces
  `b_bar=0`.  On the nonoverlapping branch `L0=4*b_bar`, `b_bar!=0`, the next
  equations force `e_bar=9*b_bar/16` and `V0=-1/4`.
* If `Q0!=0`, put `tau=L0/Q0`.  Then
  `b_bar=-tau^2*Q0` and
  `e_bar=(3/8)*Q0*tau^2*(4*tau-1)`.  Actual `E14,E15` reduce exactly to

  ```text
  4*tau^2*(12*tau^2+6*tau+1)-V0*tau-8*R0 = 0,
  2*Q0+24*tau^5+6*tau^4+3*R0*(1+4*tau) = 0.
  ```

These three root branches are frozen as diagnostics before the large
endpoint run.

## Branch routing and AWS successor

Since `T=A*V` in a polynomial domain, `T=0` iff all six coefficients of `V`
vanish.  That branch is exactly the cutoff-four specialization; using its
reported closure is explicitly external and review-dependent.  The genuinely
new branch is `V!=0`.

The frozen `V!=0` target has 56 operative variables before a chart inverse.
Its small necessary `core` scope has 36 generators and 120,823 terms: the 17
`D21` root-core consequences, the carrier relation, the literal endpoint, and
17 nonendpoint `D22` rows.  Its `full` fallback has 125 generators and
403,266 terms.  Six localization charts `chartinv*V_i-1` cover `V!=0`; no
coefficient or carrier is set to one.  A unit in the core target proves
emptiness, while a nonunit is inconclusive.

AWS execution is parent-gated.  Modular jobs are reconnaissance only; each
empty chart used requires exact-Q replay with an internally checked
`liftstd` cofactor certificate.  The runner enforces source/binary hashes,
reserved-host identity, swap zero, per-worker and aggregate RAM, cumulative
output reservation, per-launch/live 50-GiB disk and 150-GiB memory floors,
per-job and global wall/CPU/file limits, fresh immutable namespaces, and
validated process-group stop semantics.  Exact charts are host-pinned in
ordered pairs: r6a has 0,1; r6c has 2,3; r6d has 4,5.  Each pair has a
12-hour global cap, and only the validated disjoint union of all three
manifests is coverage-complete.  Periodic telemetry records RAM, swap, disk,
and PGID RSS; `/usr/bin/time -v` records each job.

The packet does not import the hostile-audit-rejected external
five-mode/full-fixture reduction.  In particular, the authoritative literal
staged rows retain the forced coefficients `c14,c16,c18,c20`.

## Replay

From this case directory:

```bash
python3 analyze_tail3.py --check --output .
sha256sum -c SOURCE.sha256
sha256sum -c EVIDENCE.sha256
```

No local CAS or AWS computation was used to obtain this packet.

## Scope firewall

This packet concerns only the fixed branch-P square baseline at cutoff three.
It does not prove cutoff four internally, close full branch P, construct or
exclude a Keller pair, settle a counterexample family, or prove JC2.
