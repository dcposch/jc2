# TD6 V78 simultaneous all-q vector adjoint

Status: **P12 FAIL-CLOSED / STAGED AWS PRODUCER LIVE / NO THEOREM**

This source freeze replaces a serial q4/q5 chain with one exact 22-column
vector-AD calculation for every licensed q-boundary coefficient in the fixed
source-typed A3 section.  The two live AWS modes respectively compute the
original-first-row genuine-P12 source identity and the staged joint conormal
map.  See `PREREGISTRATION.md` for scope and refusal claims and `LAUNCH.md`
for source custody and remote placement.

The P12 lane has since failed at a source-typing control before any P12
claim, exposing the separate V77 q3 erratum.  The staged lane remains a
strictly provisional, correctly typed joint tangent discriminator.  The
exact endpoint and scope firewall are appended to `LAUNCH.md`.

The archive under `source/` is the complete recursively hash-pinned source
closure used by both hosts.  `source/replay.py` and `source/run_v78.sh` are
readable copies of the load-bearing V78 files; `source/SOURCE_BUNDLE.sha256`
is the embedded closure ledger.
