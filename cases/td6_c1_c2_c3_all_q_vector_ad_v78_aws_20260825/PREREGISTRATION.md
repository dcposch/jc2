# V78 simultaneous all-q vector-AD preregistration

Date: 2026-08-25

The source-typed fixed-A3 section is

```
y=s^-1,
x=C*s+V*s^2+U*s^3+t*s^4,
p=t^15,
q=t+sum(a_e*t^e, e in Q)+t^25,
Q={2,...,14,16,...,24}.
```

The coefficient `a_15` is excluded as the reviewed lower target-shear gauge.
The F1/pole orbit and dead stretch remain frozen.  The calculation is over
the generic center open `D(U*(C-3U^2)*B3)`.

One exact square-zero coefficient object carries all 22 labeled directions.
The `p12` lane must retain every transport RHS source key, every direct
q-prime term, all varying first-echelon multipliers, and replay the genuine
2,893-term P12 from original first rows.  Its q2 and q3 projections must
match V32 and V77 exactly.  The `staged` lane must propagate the same joint
jet through first, previous/pole, and current stages and emit an exact
conormal matrix and kernel with a canonical exponent-keyed serialization.

Both modes fail closed on a denominator factor outside `U`, `C-3U^2`, and
`B3`.  Controls include batched direct-qprime omission, source-row omission,
lambda-prime omission, and reversed derivative-column serialization.

This is a tangent/conormal and source-support discriminator at an already
empty base.  It is not a polynomial-family, neighborhood, full-TD6, SP-2,
or JC2 theorem.
