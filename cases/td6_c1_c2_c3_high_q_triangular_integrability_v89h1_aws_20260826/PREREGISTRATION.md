# TD6 V89H1 high-tail triangular integrability preregistration

Date: 2026-08-26

Status: producer diagnostic; no result claimed before dual AWS output.

## Exact question

Set the low transverse coordinates `q2,...,q14` to zero and retain the nine
high coordinates `q16,...,q24` over the untruncated exact coefficient ring.
Rebuild the literal V87 transport, all 38 raw FIRST sources, and genuine raw
P12.

Choose the reviewed q-zero FIRST pivot columns.  If `A_0` is their 38 by 38
coefficient block and `A(q)` is the exact high-q block, form

```text
B(q)=A_0^(-1) A(q)=I+N(q).
```

The first gate is whether the directed support graph of `N` is acyclic.  An
acyclic graph supplies a simultaneous permutation making `N` strictly upper
triangular, hence the exact polynomial inverse

```text
(I+N)^(-1)=I-N+N^2-... .
```

No q expression is then inverted.  Normalize FIRST by this inverse and
reduce literal P12.  If its remainder is exactly the reviewed q-zero unit,
the nine high jets collapse simultaneously at producer tier.  Otherwise
emit the smallest exact positive-q remainder as the integrability
obstruction.  If the graph has a directed cycle, emit the first exact cycle
as an obstruction to this triangular proof only; do not claim
nonintegrability.

## Fail-closed gates

1. Consume V87 through frozen source bytes and reproduce its q-zero source
   digests.
2. Keep all nine transport RHS sources and all nine direct q-prime terms;
   q15 remains absent.
3. Invert only the q-zero 38 by 38 block over E3.  Any attempted q inverse is
   a closed failure.
4. Verify both sides of every matrix inverse and every original-source
   replay coefficientwise.
5. Emit the full `N` support graph, exact cycle/topological order, literal
   P12 remainder, source-multiplier inventory, denominator audit, and exact
   result record.
6. Require all denominator radicals to lie in `U,H,B3` and remain coprime to
   F.
7. Run two byte-identical exact clients on distinct AWS hosts.

## Scope

A collapse applies only to the high-q residue family on the retained fixed
three-center, fixed F1/pole/dead-stretch component over `D(U H B3)`.  A graph
cycle is only a failure of the triangular shortcut.  Neither result covers
the 13 low q jets, q15 beyond V88, omitted moduli, total Rees, whole A3, TD6,
SP-2, or JC2.
