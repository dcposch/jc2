# Producer result: fixed-`p=0` residual `A` chart, `C` contact one

Date: 2026-08-26

Status: **PROVISIONAL EXACT-SOURCE CONTACT-RAISING RESULT; DUAL AWS PASS,
HOSTILE REVIEW REQUIRED.**

After setting the leading `R,C` coordinates and every moving-`p` jet to zero
in the complete seven-row half-weight source, retain an arbitrary first `R`
correction and let

```text
A0=a1*z+a0,       C=sigma*(e1*z+e0)/2+O(sigma^2).
```

The complete absolute grade-eleven rows are exactly

```text
g11_2=(3/8)*a0*e0,
g11_1=(3/8)*(a1*e0+a0*e1).                         (1)
```

On `D(a0)`, (1) is raw triangular and forces `e0=e1=0`.  On
`V(a0) intersect D(a1)`, the second row forces `e0=0`; after only that raw
equation, the complete grade-twelve row is

```text
g12_2=(3/32)*e1^2.                                 (2)
```

Thus `D(e1)` is empty and every DVR arc in either leading-`A` chart raises
`C` contact beyond one.  The rows (1)--(2) are symbolically independent of
the arbitrary first `R` correction, so this covers every `R` contact at
least one.  No radical is used; (2) is a unit after the registered
localization by `a1*e1`.

Exact Q on Box03 and an independent `F_65521` r6d control both pass.  The
compiler retains all seven frozen tails, every available correction,
`k10,k6,k2`, and every target; the later loads and targets are absent at
grades eleven/twelve by exact source extraction.  Generic V12 and the
eight-form fan are not used.

This result treats only fixed `p=0`, leading `A!=0`, and finite `C` contact
one.  It does not cover `C` contacts two or three, the `R`-contact-one
mixed sheet, the high-contact cone except through its separate reviewed
theorem, the total unspecialized-`p` Rees glue, the all-zero all-load
receiver, `k0=0`, order two, maximum twelve, or JC2.
