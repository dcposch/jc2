# Registration: fixed-`p=0` residual `A` chart, `C` contact one

Date: 2026-08-26

In the complete half-weight source set the leading `R,C` coordinates and all
moving-`p` jets to zero, retain arbitrary first `R` correction, and let the
first nonzero possible `C` correction have sigma-order one.  Reconstruct all
seven rows through absolute grades eleven and twelve.

Expected exact raw rows are

```text
g11_2=(3/8)*a0*e0,
g11_1=(3/8)*(a1*e0+a0*e1).
```

Thus `D(a0)` forces `e0=e1=0`.  On `V(a0) intersect D(a1)`, grade eleven
forces `e0=0`, and then

```text
g12_2=(3/32)*e1^2,
```

so `D(e1)` is empty and every DVR arc raises `C` contact.  Exact Q on Box03
is the theorem lane; `F_65521` on r6d is an independent compiler/control
lane.  Keep all corrections, lower loads, and targets in the frozen source.
