# Result: affine-Faber `A`, `a=3,q=6` graph predecessor

Date: 2026-08-26

Status: **DUAL-AWS PASS; EXACT-Q TWO-CHART PREDECESSOR CERTIFIED.**

The controlling V5 replay reconstructs all seven ordinary-Faber rows from
the frozen literal tails on the fixed delayed schedule

```text
normal order 15, center order 3, kernel order 6,
complement order at least 12, graph loads at grade 42.
```

Every coefficient below grade 42 vanishes.  The full grade-42 block agrees
with the seven corrected hand rows `G1,...,G7`; all center, successor,
graph-deviation, and `K10` intrusions are absent.  The three exact
dependencies

```text
G6=(p^2/8)*G2,
G5=(3*p^2/32)*G1-(p/4)*G3,
G7=(p^2/32)*G3-(p^3/64)*G1
```

vanish coefficientwise.  The registered mutation of the leading graph is
detected.  Finally, the ideals generated by `G1,G3,G4,G6` are units on both
projective kernel charts `D(p*m*x6)` and `D(p*m*y6)`, without inverting
`K10`.

Box03 exact Q and r6d `F_65521` each returned engine code zero, passed every
mandatory marker, used no swap, and wrote terminal status `PASS`.  Exact-Q
stdout has SHA-256
`fe5df60b98c9b766ccc436e1f570d41dde4eb92435c23ca7d897e3272bbc184e`;
the software-control stdout has SHA-256
`2c424238536832186ded4db6ee0b69da07724a7a8c70afbfdab3d7aa287801fa`.
The byte-identical validations have SHA-256
`dabf5d678d54130bd11c3854a93cbab90f88c30f514d81a3006de60d029ecd71`.

V2 is retained as a Singular coefficient-syntax negative control.  V3 is
retained as the specification-negative replay that discovered the omitted
but redundant rows.  V4 is retained as positive preformal evidence whose
validator did not yet make the dependency marker mandatory.  None of those
earlier clients is the formal endpoint.

This result certifies only the registered fixed `H=15,a=3,q=6` grade-42
graph predecessor on `D(p*m)` and the two kernel charts.  Composition with
the separately frozen `q<6` lemma and `q>6` support split still requires
hostile review.  No source-cover, total-Rees, order-two, maximum-twelve, or
JC2 claim is made here.
