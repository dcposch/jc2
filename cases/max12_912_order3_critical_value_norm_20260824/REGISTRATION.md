# Registration — maximum-12 unordered critical-value norm

- Date: 2026-08-24
- Evidence: exact algebraic producer checkpoint
- Lifecycle: producer-checked; hostile different-model review required

On the normalized loaded order-three fibre, `B/54=z^2-s` with
`s=-(a7+10*rho)/9`.  Reducing `f` and `g` to quadratic pairs constructs
`C(T)=Norm(g^3-T*f^4)` over the coefficient field.  Its discriminant is
`4*s*(G0*F1-G1*F0)^2`, giving a root-free exhaustive split into double-root,
full-absorption, one-absorption, equal-value, and generic unequal-value
strata, with `B` meeting `f` retained separately.

No stratum is declared empty.  Original Taylor rows, terminal dynamics,
nonzero other loads, the order-one core, `(8,12)`, maximum twelve, and JC2
remain outside this registration.
