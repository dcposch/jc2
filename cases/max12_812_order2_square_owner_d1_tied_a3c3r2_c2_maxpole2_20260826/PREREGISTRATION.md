# Preregistration: fourfold tied `(a,c,r)=(3,3,2)` `C2` face

Date: 2026-08-26

Status: **PREREGISTERED EXACT-SOURCE MAXIMAL-POLE-TWO PRODUCER.**

## Exact scope

Work only on

```text
D(p*k0), ord(A)=3, ord(C)=3, ord(R)=2.
```

At the unit-load lower hull this is the fourfold leading face
`AC=C2=R3=RC`.  No hand-triage endpoint or support-miner conclusion is
imported.  The decisive absolute ceiling is grade 16.

## Independent source census

Enumerate all four frozen binomial summands through grade 16, with padded
bounds.  The accepted source is exactly

```text
(3/4) A*C/L,       (3/8) C^2/L^2,
(5/16) k0*R^3/L,  (5/8) k0*R*C/L.
```

All four first occur in grade 16; `C2` is the unique pole-two family.  No
normal/connection correction, lower load, or target may enter.

## Maximal-pole invariant

Put `L=z^2+p/2` and reconstruct the canonical `L^-2` numerator modulo
`L^2` from all seven literal rows.  Its fully cleared form is

```text
N2_full=(3/4)A*C*L+(3/8)C^2
       +(5/16)k0*R^3*L+(5/8)k0*R*C*L.
```

On the etale cover `p=-2*lambda^2`, the root Faber pair must be exactly

```text
N2(+lambda)=(3/8)C(+lambda)^2,
N2(-lambda)=(3/8)C(-lambda)^2.
```

The evaluation determinant is `2*lambda`.  Exhaust the two exact-`C` charts
`D(c1)` and `D(c0)` and require the ideals generated by both root equations,
the chart inverse, and `lambda^-1` to contain `1` before radicals.  Omitting
`C2` must make both root terminals zero.

## Acceptance

1. Frozen ancestry, exact four-family/padded census, all seven literal rows,
   recursive quotients, and analytic/Faber bridge all pass.
2. The common numerator modulo `L^2`, exact ordinary quotient, pole-two
   recurrence, and both root identities pass in an ordinary polynomial
   ring; no Singular `qring` is allowed.
3. Both exact-`C` charts are unit ideals before radicals, with no inversion
   of a leading `A` or `R` coefficient; the omit-`C2` control passes.
4. Run exact `Q`, `F_65519`, and `F_65521` on three AWS hosts under fresh
   tags, fail-closed validators, and zero swap.

## Firewall

A PASS closes only the displayed fourfold exact contact on `D(p*k0)`.  It
does not prove a generic all-`C2` theorem, cover a neighboring face, a
positive-order load, `p=0`, `k0=0`, the exact-square zero section, a
terminal/global chart, fan exhaustiveness, order two, maximum twelve, or
JC2.
