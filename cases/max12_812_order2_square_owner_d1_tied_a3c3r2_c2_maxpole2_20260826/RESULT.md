# Producer result: tied D1 `(a,c,r)=(3,3,2)` C2 maximal-pole face

Date: 2026-08-26

Status: **EXACT-Q PRODUCER PASS WITH TWO FRESH GOOD-PRIME CONTROLS; HOSTILE
REVIEW REQUIRED BEFORE PROMOTION.**

## Exact result

After the frozen generic-square and D1 source gates, the complete seven
literal Faber equations are empty on the exact contact

```text
D(p*k0), ord(A)=3, ord(C)=3, ord(R)=2.
```

An independent cost-bounded census of all four frozen binomial summands
through absolute grade 16, repeated with padded bounds, finds exactly

```text
(3/4) A*C/L,       (3/8) C^2/L^2,
(5/16) k0*R^3/L,  (5/8) k0*R*C/L.
```

All four occur first in grade 16 and `C2` is the unique pole-two family.
The compiler rebuilds all seven literal source rows and the independent
analytic Laurent source, checks their coefficientwise grade-16 bridge,
exact recursive quotients, and exact absence of normal/connection
corrections, lower loads, and targets at this grade.

For `L=z^2+p/2`, the canonical remainder modulo `L^2` is reconstructed from
the seven ordinary row coefficients and checked against the fully cleared
numerator

```text
N2_full=(3/4)A*C*L+(3/8)C^2
       +(5/16)k0*R^3*L+(5/8)k0*R*C*L.
```

The discarded ordinary quotient is retained exactly and the pole-two row
recurrence passes.  On the finite etale root cover `p=-2*lambda^2`, the two
root Faber functionals are exactly

```text
N2(+lambda)=(3/8)C(+lambda)^2,
N2(-lambda)=(3/8)C(-lambda)^2.
```

The determinant of the two-root evaluation map is a unit on `D(p)`.  The
two coefficient charts `D(c1)` and `D(c0)` exhaust nonzero linear `C`, and
both localized ideals generated by the two square equations contain `1`
before radicals.  Deleting the `C2` source makes both root terminals zero,
so the decisive column is source-required rather than supplied by another
family.

## AWS executions

| endpoint | tag | rc / wall / max RSS KiB / swap | stdout SHA256 |
|---|---|---|---|
| exact `Q`, Box02 | `max12_812_order2_square_d1_tied_a3c3r2_c2_maxpole2_q_box02_20260826T201345Z` | 0 / 0.07 s / 19712 / 0 | `79163938a6596f30c5c643f110ebb7868cfb3be5a084c522cbf7081b3d160c8c` |
| `F_65519`, Box03 | `max12_812_order2_square_d1_tied_a3c3r2_c2_maxpole2_p65519_box03_20260826T201345Z` | 0 / 0.07 s / 20064 / 0 | same |
| `F_65521`, r6d | `max12_812_order2_square_d1_tied_a3c3r2_c2_maxpole2_p65521_r6d_20260826T201345Z` | 0 / 0.07 s / 20076 / 0 | same |

All validators returned `PASS_D1_TIED_A3C3R2_C2_MAXPOLE2_EMPTY`; all
engines returned rc 0 with zero swap.  Exact `Q` is the characteristic-zero
endpoint and the primes are independent software/host controls.  Every
generated program uses an ordinary polynomial ring and explicit standard
bases/reductions; no Singular `qring` occurs.

## Custody and firewall

All hosts received source archive SHA256
`a5380e75cdc897e61d1ce88f508d7f343a90ca43b4e917b9304c2744a9b325c1`
and passed the same `FREEZE.sha256` before compilation.  The evidence and
producer-freeze manifests pin the retrieved executions and this statement.

This producer closes only the displayed exact contact if hostile review
confirms it.  It does not establish a generic all-`C2` theorem, cover a
neighboring face, a positive-order leading load, `p=0`, `k0=0`, the
exact-square zero section, an excluded terminal/global chart, fan
exhaustiveness, order two, maximum twelve, or JC2.
