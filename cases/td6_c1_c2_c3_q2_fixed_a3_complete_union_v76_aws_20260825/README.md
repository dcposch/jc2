# TD6 fixed-A3 q2-beta complete constructible union (V76C)

Frozen status: **dual-AWS producer-exact composition; promotion held for
hostile review of V57 and V75.**

Let

```text
H  = C - 3U^2,
B3 = 4C^2U^2 - 4CV^2U + 24CU^4 + V^4 - 20V^2U^3 + 20U^6.
```

V76 consumes four exact source/dependency theorems in the fixed source-typed
A3 section with `q_beta=t+beta*t^2+t^25`:

- V57 on `D(U*H*B3)`: dual-AWS producer-exact original-source DAG; hostile
  review still missing;
- V70 on `U=0`: hostile-review `CONFIRMED`;
- V71 on `H=0`: hostile-review `CONFIRMED`;
- V75 on `B3=0`: dual-AWS producer-exact repaired atlas; hostile review
  pending, with its load-bearing V73 review already running.

The exact set-theoretic cover is

```text
Spec Q[C,V,U]
  = D(U*H*B3) union V(U) union V(H) union V(B3).
```

V76 verifies the hash-pinned dependency markers and denominator ledgers,
checks this four-branch Boolean cover, and proves that deleting any one
branch leaves its formal constructible witness uncovered.  Its dual outputs
differ only in registered hostname and run tag.  Each recursive source
manifest verifies.

Thus the whole fixed source-typed A3 q2-beta compatibility family is
**provisionally producer-exact empty for all beta**.  The package deliberately
prints `promotion_licensed=false`; the V57 and V75 hostile-review gates must
close before promotion.

This varies only `(C,V,U,beta)` in the displayed normalized section.  It does
not vary any other center, boundary, dead-stretch, F1-orbit, or pole modulus,
and proves no whole TD6, SP-2, landing theorem, or JC2 result.
