# V76 preregistration: fixed-A3 q2-beta four-chart union

Target: compose the exact V57 open theorem on `D(U*H*B3)`, reviewed V70
on `U=0`, reviewed V71 on `H=0`, and producer-exact V75 on `B3=0`.
The set-theoretic identity is

`Spec Q[C,V,U] = D(U*H*B3) union V(U) union V(H) union V(B3)`.

The run must hash-pin every dependency, verify the exact source/scope
markers, prove the Boolean cover, and fire one missing-branch control per
leaf.  V57 and V75 are not yet hostile-reviewed, so a PASS is provisional
producer composition only and must print `promotion_licensed=false`.

Exact possible conclusion: the fixed source-typed A3 q2-beta compatibility
family is producer-exact empty for all beta.  No transverse source modulus,
TD6, SP-2, landing theorem, or JC2 claim is licensed.
