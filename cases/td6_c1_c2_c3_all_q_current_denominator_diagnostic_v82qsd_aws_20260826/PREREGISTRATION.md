# V82QSD preregistration — CURRENT denominator diagnostic

V82QS q3 through q10 reached the CURRENT conormal computation and then failed
closed at the inherited assertion that the common denominator has no radical
factor beyond `U`, `H`, and `B3`.  Those failed bytes remain immutable and have
no coefficient or cover verdict.

V82QSD changes only reporter placement in the hash-pinned parent copy.  Before
the same assertion it writes and prints the exact common denominator, its exact
factorization, conormal coordinate count, rank, kernel dimension, allowed-factor
boolean, file path, and SHA-256.  The assertion remains in place.  Thus a lane
with a foreign factor is expected to exit nonzero, but now preserves the exact
new rank-drop divisor debt.  No factor is absorbed into generic-open wording.

The diagnostic reruns q2 through q10 independently on AWS.  It is not aggregate
authority and does not license a 22- or 24-dimensional kernel, family, TD6,
SP-2, landing, or JC2 conclusion.  Each lane is capped at 2 GiB and two hours.
