# AWS-only Q9 witness Kuranishi gate

Verify the recursive source closure and runner manifest, then run on Box02:

```sh
nohup setsid ./run_remote.sh UNIQUE_UTC_TAG > launch.log 2>&1 < /dev/null &
```

This is a pointwise source/carry discriminator.  Do not infer a full Q9
fibre transition or a lift from its result.
