# Result: D1 primary-C2 `c=8` first-connection split

Date: 2026-08-26

Status: **TRIPLE-AWS EXACT-Q PRODUCER PASS; AWAITING HOSTILE REVIEW.**

## Exact producer theorem

After the frozen generic-square and D1 unit-load source gates, in
characteristic zero on `D(p*k0)`, the complete seven literal Faber equations
have no point on

```text
ord(C)=8, ord(A)>=8, ord(R)>=7.
```

This is the first primary-`C2` contact beyond the promoted target-free
`3<=c<=7` band.  The proof is the exhaustive scheme split
`D(k60) union V(k60)`, where `k6=k60+sigma*k60_1+...`.

## Complete grade-25/26 source

The independent padded census has exactly four primitive families through
grade 26.  The unique grade-25 family is

```text
(3/4)*k60*C/L.
```

At grade 26, after clearing `L^2`, the complete numerator modulo `L^2` is

```text
N2 = (3/4)*A*C*L + (3/8)*C^2 + (5/8)*k0*R*C*L
   + (3/4)*(k60_1*C+k60*Cnext)*L
   - (3/4)*ell1*k60*C.
```

The last summand is the first moving-connection correction for
`p(sigma)=p+2*sigma*ell1+...`.  Both next `C` coefficients, `k60_1`, and
`ell1` enter the compiled numerator.  Omitting the connection makes the
full numerator check fail.  All targets begin after grade 26.

Every source/load/moving-`p` jet is emitted into all seven literal rows at
grades 25 and 26 and bridged coefficientwise to the independent analytic
source.  The programs use an ordinary polynomial ring and explicit
reductions; no quotient-ring normalization semantics occur.

## Load chambers

On `D(k60)`, the seven grade-25 literal equations generated by
`(3/4)k60*C/L` give the unit ideal on both exact-`C` charts

```text
D(c1),  V(c1) intersect D(c0).
```

On `V(k60)`, the moving-root and next-`C` terms proportional to `k60`
vanish.  The free `k60_1*C/L` term remains but is pole one.  On the etale
root cover `lambda^2=-p/2`, the exact pole-two Faber pair is

```text
(3/8)*(lambda*c1+c0)^2,
(3/8)*(-lambda*c1+c0)^2.
```

These two forms give unit ideals on both exact-`C` charts before radicals.
Omitting `C^2` makes both root evaluations zero.  No leading coefficient of
`A` or `R` is inverted.  Raising either contact only delays source monomials
with positive `A` or `R` exponent, so the boundary computation proves the
displayed closed `A,R` tail.

The unconditional fixed-root Faber/analytic equality is deliberately false:
the moving root contributes a nonzero multiple of the earlier `k60*C`
column.  V2 incorrectly required that pre-split equality and is frozen as
no-verdict.  V3 requires and verifies the bridge exactly where it is used,
after adjoining `k60=0`.

## AWS custody and firewall

Exact Q on Box03 and fresh `F_65519`,`F_65521` controls on Box02/r6d all
return rc zero, the terminal PASS marker, empty compiler stderr, and zero
swap.  Their mathematical stdout is byte-identical.  Launch/resource custody
is in `AWS_LAUNCH_METADATA.md`, and every retrieved V3 byte is pinned by
`EVIDENCE.sha256`.

This producer closes only the displayed `c=8,A>=8,R>=7` primary-C2 tail on
`D(p*k0)`, after its frozen gates.  It does not include `c>=9`, another
primary/tied face, positive-order leading load, `p=0`, `k0=0`, the
exact-square zero section, terminal/global charts, fan exhaustiveness, order
two, maximum twelve, or JC2.  Promotion requires an independent hostile
review.
