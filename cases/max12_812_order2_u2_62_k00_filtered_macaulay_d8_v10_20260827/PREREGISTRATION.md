# Preregistration: exact K00 filtered Macaulay degree 8 V10

Date: 2026-08-27

Status: **FROZEN D8 EXTENSION; NO D8 RESULT AT REGISTRATION.**

The accepted exact V9 endpoint proves cumulative compatibility through D7:
the D7 matrix has 1,709 equations, 2,772 multiplier coefficients, and
rank = augmented rank = 1,372; its exact 273-entry multiplier jet replays.

V10 uses the identical frozen K00 normalization and tail expansion, but
extends the complete cumulative map through transverse degree 8.  It includes
every multiplier monomial of degree at most 6.  The frozen shape is

```text
equations=2996,
multiplier_coefficients=5544.
```

An exact-Q AWS lane computes rank and augmented rank.  Equality must be
accompanied by an exact rational multiplier jet replaying all 2,996
equations.  Inequality must be accompanied by an exact left compatibility
functional replay.  A separately serialized `p=65521` lane is navigation
and software control only.

If D8 is incompatible, degree 8 is the first canonical pure-coefficient
filtered obstruction because D2--D7 are already exact-compatible.  If D8 is
compatible, the result raises the lower bound to degree 9.  Neither outcome
decides local-ring membership, the mixed `Lambda`/load/target reachability at
order 19, the K00 closure-first incidence, Taylor realization, or JC2.
