# ACT-TOT-G20 V44R1 preregistration

Date: 2026-08-27

V44R1 is the additive repair of the preserved V44 failed-closed attempt.  It
reuses the byte-pinned V44 producer and changes exactly one mathematical
postcondition: the ten terminal weight-20 source/load jets must be present in
the constructed source series but absent from all seven grade-20 receiver
rows.  Their absence is expected because every frozen tail monomial that can
receive one of these terminal coefficients has positive sigma weight.

The exact-Q and `F_65521` lanes must run on distinct registered AWS hosts.
Both lanes must still reconstruct all seven literal unspecialized
general-`rho` rows through grade 20 before applying any face quotient, verify
140 coefficientwise V35-prefix identities and fourteen frozen face bridges,
check weight homogeneity and `rho` parity, serialize every row, and pin the
canonical rational and whole-truncation hashes.  A cross-lane validator must
then prove coefficientwise reduction modulo 65521.

This repair does not promote any output of the failed V44 attempt.  It reruns
the entire producer and all gates from frozen inputs.  A pass remains a
custody lemma only; it proves no contact-source identity, finite-jet
fidelity, cover, ramified-fibre statement, `G2` obligation, Gate T, order-two
theorem, maximum-twelve theorem, JC2, or counterexample verdict.

