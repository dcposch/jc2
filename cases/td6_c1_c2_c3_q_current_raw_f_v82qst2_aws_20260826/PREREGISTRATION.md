# TD6 V82QST2 raw `F=0` CURRENT client

Date: 2026-08-26

## Question

After imposing the exact raw center relation

```text
F = C0*U0 - V0^2 + U0^3 = 0
```

before transport elimination, does the fixed source-typed A3 system already
have a source-certified CURRENT incompatibility on

```text
D(U0*V0*(V0^2-4U0^3)*(V0^2+8U0^3))?
```

This is the first Gate-2 original-source rebuild from the reviewed CURRENT
divisor tree.  It is launched provisionally while the terminal reverse-pivot
Gate-1 comparison runs, but no result may be consumed unless that comparison
retains `F` as a genuine presentation debt.

## Exact source specialization

Work over `Q(V,U)` inside the existing three-variable rational-function
implementation, retaining formal `C` as an audited unused sentinel:

```text
C0=(V^2-U^3)/U,  V0=V,  U0=U.
```

The client must verify before source construction that

```text
F=0,
G=V^4,
L=V^8*(V^2+8U^3),
H=C0-3U^2=(V^2-4U^3)/U,
B3=V^4.
```

It then rebuilds transport, FIRST, PREVIOUS/POLE, and CURRENT from the frozen
original source compiler.  It may not specialize a generic CURRENT table or
reuse a pivot chosen before `F=0`.

## Certificate and controls

For every base CURRENT incompatibility the client emits:

- the exact staged source-row combination and replay digest;
- an omission negative control;
- all exact coordinates of the residual;
- the cleared common numerator factor and full certificate denominator;
- the factors outside the registered open; and
- an exact Boolean deciding whether the registered generic `F=0` open is
  killed.

Every emitted transport/section/FIRST/PREVIOUS/CURRENT coefficient is audited
to contain no formal `C` sentinel.  The direct `q'` omission control is retained
for q2 and q10.  q2 and q10 are independent source controls; the base
incompatibility itself is not interpreted as a q-family theorem.

## Decision rule

- A nonzero residual whose cleared numerator and certificate denominator have
  support only in `U,V,V^2-4U^3,V^2+8U^3` closes the named generic raw stratum.
- Every other factor is a new raw successor and is retained explicitly.
- A lower rank, source replay failure, use of formal `C`, missing omission
  control, host disagreement, or failed Gate-1 premise is fail-closed.

This client concerns only the fixed source-typed A3 CURRENT system.  It proves
no tangent family, nonlinear family, TD6, SP-2, landing theorem, or JC2.
