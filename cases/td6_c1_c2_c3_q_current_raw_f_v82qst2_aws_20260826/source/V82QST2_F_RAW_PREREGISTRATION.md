# V82QST2 source contract

Rebuild the fixed A3 transport, FIRST, PREVIOUS/POLE, and CURRENT systems only
after the exact raw substitution `C0=(V^2-U^3)/U`, so `F=0`.  Retain formal
`C` solely as an asserted unused sentinel.  Run q2 and q10 with the frozen
direct-q-prime omission control and ascending source elimination.

The registered generic open is
`D(U*V*(V^2-4U^3)*(V^2+8U^3))`.  Emit every CURRENT base incompatibility as
an exact staged-source combination, its omission control, cleared residual
factor, certificate denominator, and all successor factors outside that open.

The launch may overlap the live Gate-1 reverse-pivot comparison, but its
mathematical result is conditional on Gate 1 retaining `F`.  Fail closed on a
source replay mismatch, use of the formal `C` sentinel, an unregistered factor,
host disagreement, or any tangent-family/TD6/SP2/JC2 interpretation.
