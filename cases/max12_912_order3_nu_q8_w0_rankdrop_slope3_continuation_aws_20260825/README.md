# Exceptional slope-three strict-transform checkpoint

Only V3 is accepted.  It saturates the leading chart by the preregistered
`z!=0` condition and independently verifies that the resulting slope-three
candidate has no unramified `x5=t` continuation: the next upper-row ideal is
unit and the final residual is `-108`.

V1/V2 are fail-closed controls.  Arbitrarily ramified arcs remain outside
this theorem and are handled by the separate full selected saturation.

