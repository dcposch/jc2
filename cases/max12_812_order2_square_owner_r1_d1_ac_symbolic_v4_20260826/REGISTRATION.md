# Registration: `r=1` / symbolic-`d=1` V4 neutral-identifier repair

Date: 2026-08-26

V3 preserved the exact source and mathematical program but Singular tokenized
the word `plus` inside two replacement identifiers.  Both exact-Q and
`F_65521` validators rejected the resulting diagnostics.  Every source,
Faber, row, scaling, recurrence, and unmatched-pole sentinel that preceded
the orientation declaration passed in both runs.

V4 pins V3 and changes only three identifiers to the neutral strings
`D1ACxq15`, `D1ACxq16`, and `D1ACxqflag`, enforcing their exact occurrence
counts.  It retains the same fail-closed validator, exact scope, and dual-AWS
caps.  A V4 failure is not a mathematical verdict.
