# Registration: secondary odd grade-48 sparse functional

Date: 2026-08-26

Status: preregistered producer; no full-face claim.

The first sparse functional can vanish on the grade-44 survivor, but complete
seven-row controls remain empty and expose

```text
C3+16*C5+96*C7=-2
```

at both registered null points.  The moving-`E` lift of this row combination
is

```text
G=E(s)^2 P3+16 E(s) P5+96 P7.
```

This client imports the frozen, independently controlled sparse-DAG engine and
extracts only `[s^48]G` from complete frozen tails `3,5,7`.  It retains the
complete moving source graph, all load modes in those rows, and `J` via
`96(P7)=96 tail7-24 J`; `J` starts at grade 57 and cannot reach grade 48.
It never materializes a substituted row or computes a standard basis.

Mandatory controls are three direct abstract raw-tail evaluations, exact-Q
and `F65521` runs, and the four independent Singular fixed slices: the two
old witnesses must evaluate to `-3`, and the two first-functional-null slices
must evaluate to `-2`.

This is a fixed `H=16,q=6,ord(a)=4` raw coefficient producer only.  Reduction
modulo the complete grade-44 predecessor, projective-chart exhaustiveness,
rational regrading, source/Rees coverage, order two, maximum twelve, and JC2
are separate gates.

