# D43 exact a00pp row pipeline v2 preregistration

Status: **review-frozen, AWS-unregistered, and not launchable**.

The review manifest has blank account, region, hostname, instance ID/type,
CPU count, instance-tag key, and tag value.  The live IMDSv2 preflight and all
compute commands therefore refuse it.

After a different-agent hostile PASS, stage the sealed source on an idle
isolated high-memory host and create a new operational manifest.  Its
`REGISTERED.json` must bind the exact manifest digest, operational-source-list
digest, complete expected EC2 identity, real tag key/value, and run-directory
tag.  Do not fill in or mutate this review manifest.

The sole authorized mathematical workflow is:

1. preflight live identity, real instance tag, source hashes, zero swap,
   memory, disk, and absence of competing algebra processes;
2. build the already-collapsed f and g jets concurrently under independent
   limits and receipts;
3. assemble only a fully matching pair;
4. compare its band 20 exactly with the collapsed D21 source bank, including
   required HW-sign and `+42` mutation detection;
5. on PASS inside the same producer and manifest, emit and merge all 184 raw-J
   rows; and
6. finalize atomic terminal and artifact custody.

No solve, candidate claim, or template claim is authorized.
