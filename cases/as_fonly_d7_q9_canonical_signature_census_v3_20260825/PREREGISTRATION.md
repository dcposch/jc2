# V3 preregistration — classify the full Q9 chart without a zero-section premise

V2 correctly failed its preregistered assertion that every canonical Q8
affine particular is Q7-compatible.  It stopped before aggregation; its
partial outputs remain deployment/mathematical negative controls at Box02 tag
`as_q9_signature_census_20260825T0452Z_v2`.

V3 exhausts the same exact `3^13` Q9 chart but classifies, rather than assumes,
each zero RREF-fibre section as:

- `q7_compatible`;
- `q7_incompatible` by deterministic augmented rank after direct source-row
  reconstruction; or
- `q8_incompatible` (retained fail-closed although the chart is expected to
  lie in the Q8 survivor locus).

For compatible states the exact Q7 particular is substituted in all rows. For
incompatible states the exact zero probe is substituted and its nonzero RHS
is matched to the augmented RREF.  Integer divisions precede all reductions.

Signature equality remains zero-section presentation equality only.  An
incompatible zero section does not kill its 19-dimensional Q8 fibre.

