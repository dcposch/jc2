# Tail-7 localized row-14 obstruction: scope and stop rule

Date: 2026-08-28 UTC

This case promotes the already preregistered and audited 8-variable shared-block
calculation from two normalized affine branches to the whole **field-valued**
tail-7 endpoint stratum.  It does not launch or reinterpret a broad endpoint
search.

Frozen input is the exact tail-7 deformation system with all raw parameters of
weight below 7 set to zero.  The only permitted symbolic operations are exact
Gaussian elimination over `Q`, localization at a scalar forced nonzero by the
endpoint equation, homogeneous dehomogenization of a necessary row subsystem,
and exact replay of the already frozen 16-polynomial ideal.

Promotion criterion:

1. Holding `p32` and `p87` out of the triangular pivots must reproduce the
   endpoint equation `-1-p32*p87=0` and the row-21 compatibility
   `-(3/4)*p86*p87^2=0`.
2. Row 14 must produce 16 homogeneous quadrics supported exactly on
   `p78,...,p87`.
3. Substituting `p87=1,p86=0` into those quadrics must be byte-identical, as an
   ordered canonical term list, to the previously frozen 8-variable shared
   block.
4. The exact-Q result must contain either the literal basis element `1`, or a
   separately replayed identity `1=sum h_i f_i`.

If all four pass, stop the tail-7 search and claim only: there is no
field-valued point in this tail-7 endpoint specialization.  Do not claim an
empty scheme, an empty full branch-P endpoint family, or any Jacobian
Conjecture consequence.  If any item fails, retain only the narrower audited
branch calculation and investigate the mismatch.

