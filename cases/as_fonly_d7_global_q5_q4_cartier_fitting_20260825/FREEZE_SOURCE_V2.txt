# V2 source freeze

V2 repairs only the fail-closed nested-scope lookup documented in
`V1_SCOPE_FAIL.md`.  Its source manifest is `SOURCE_MANIFEST.sha256`; the V1
manifest and complete V1 remote job are retained independently.  All
mathematical scope and AWS-only execution rules in `FREEZE_SOURCE.txt` and
`PREREGISTRATION.md` remain unchanged.
