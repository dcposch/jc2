# Result: D1 `a=9`, `V(k60)` grade-30 `C^2` obstruction

Date: 2026-08-26

Status: **DUAL-AWS EXACT-Q PRODUCER PASS; FIXED `a=9` CONTACT CLOSED
WHEN COMPOSED WITH REVIEWED `D(k60)`.**

## Result

The two grade-28/29 survivor strata on `V(k60)` are both empty at grade 30.

On `D(k60_1)`, grade 28 gives `c1=0`.  Grade-30 row 4 is

```text
(3/16)(2*c0^2-p*c1^2),
```

so `c0^2=0`.  Adjoining the Bezout equation for exact nonzero leading
`C=c1*z+c0` gives the unit ideal, without taking a radical.

On `V(k60_1)`, the exact complete-source identities are

```text
g30_3+(p/4)g30_1+(ell1/2)e29z = (3/4)c1*c0,
g30_4                              = (3/16)(2*c0^2-p*c1^2).
```

Thus the core ideal contains `c1*c0` and `2*c0^2-p*c1^2`.  On `D(p)` it
contains both `c0^3` and `c1^3`.  Since exact contact makes `(c1,c0)` the
unit ideal, adjoining its Bezout equation again gives `1`.  This is a
scheme-theoretic nilpotent calculation over Q, not merely a radical or
point count.

The split `D(k60_1)` / `V(k60_1)` is exhaustive for the grade-28 successor.
Therefore `V(k60)` has no fixed-contact `a=9` source solution.  Composed
with the independently reviewed `D(k60)` grade-27 theorem, the fixed D1
contact

```text
ord_sigma(A)=9, ord_sigma(C)=10, ord_sigma(R)>=9
```

is empty on `D(p*k0)`.

## Complete-source identity

Before radicals, the full grade-27--30 source ideal on
`V(k60,k60_1)` equals the compact ideal generated by

```text
k60, k60_1, mu20,
e29z, e29c,
g30_1, g30_2,
c1*c0, 2*c0^2-p*c1^2.
```

Rows 5--7 satisfy the preregistered denominator identities, row 6 is zero,
and the client retained all grade-31 delayed `k20/k0_1/k60_4` terms and the
terminal `mu4,mu6,J/4` targets.  The Chebyshev/Pell positive control passed.

## AWS custody

Exact Q / Box03:

- tag `max12_812_order2_square_d1_a9_vk60_grade30_c2_q_20260826_box03`;
- compiled input SHA-256
  `3e0617f922dbb05592f18dee242d96b5bd7ae026fd48e07673e4b704dd565826`;
- stdout SHA-256
  `d516013b67ef7c534bce7da116abca61354120cf855d6e79cf73efa615c35cfc`;
- metadata SHA-256
  `df2437d4f8f433fb52466814450223e1a808c5eaf7a1eca072e2f772ed0ee015`;
- wall `6.06s`, peak RSS `1255328 KiB`, swaps `0`, engine rc `0`;
- validator `PASS_D1_A9_VK60_GRADE30_C2_EMPTY_BOTH_STRATA`.

`F_65521` / r6d:

- tag
  `max12_812_order2_square_d1_a9_vk60_grade30_c2_p65521_20260826_r6d`;
- compiled input SHA-256
  `fb4faea76e1910eb79c0accf5f2a47fb5ce7cadea748c6b807cc9415f11c47ba`;
- stdout SHA-256
  `838c22f7fd7c8e6ace481ab0fa816efba2c58a4d41ec5bd3d22779c82b25179d`;
- metadata SHA-256
  `c909ffae6e2523a42bc51a9aa28ba2f1cc6203f23d19e4ecc9f9a90d0fb63a90`;
- wall `4.16s`, peak RSS `889128 KiB`, swaps `0`, engine rc `0`;
- validator `PASS_D1_A9_VK60_GRADE30_C2_EMPTY_BOTH_STRATA`.

## Firewall

This excludes only the fixed `a=9` contact on `D(p*k0)`, after the cited
upstream square/D1 gates.  It does not cover contact-raising arcs (`a>=10`),
`p=0`, `k0=0`, other D1 cells, the whole square component, order two,
`(8,12)`, maximum twelve, or JC2.  Different-model hostile review is still
required before campaign-level promotion.

