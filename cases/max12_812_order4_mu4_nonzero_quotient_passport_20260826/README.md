# `(8,12)` order-four nonzero-load quotient/passport gate

Date: 2026-08-26

Status: **SOURCE-TYPED V1 CLIENT; NO COMPUTATIONAL VERDICT.**

## Exact scope

This client starts from the corrected and hostile-confirmed normalized
coefficient fibre

```text
C_4 = V((r_1,r_2,r_3,r_4-1,r_5,r_6):r_7^infinity),   r_7 != 0.
```

It does not assert a Taylor realization or a Keller pair.  It constructs
finite invariant images of this necessary coefficient curve and then asks
questions on their **complete normalizations**.  Genus of the affine
`r_4=1` slice by itself is not a source obstruction.

The charged artifacts are:

```text
c1aa88b40104205f327240dd33a4e674983577ce6d9dfafc82b8547959312621
  xmodel/max12-812-order4-mu4-nonzero-coefficient-curve-client-v2-20260825.md
252bbd07d29084952453b45546cff7e0d8ddda7a72576311d1f982ee01a66200
  xmodel/max12-812-order4-mu4-nonzero-coefficient-curve-review-grok-v2-20260825.md
149691f1784ffccea9473a63efaec4a7d0eea56f9c1e1ba267610b104022920e
  xmodel/max12-812-nontrivial-terminal-extremal-abc-classification-20260826.md
b9cf307094a3128ae459714d6a84e49d4f004308d97b5d5b8298afb1e300b149
  xmodel/max12-812-nontrivial-terminal-extremal-abc-review-grok-20260826.md
5b401beac071a22ec9ad3fc022bf9f87a255e40fdb141359f4bdc40d8547fa47
  emitted corrected-V2 coefficient_curve.sing
```

## The two quotients and invariant images

Write `s=a_5`, `t=a_6`.  On the generic chart `t!=0` put

```text
q=s^2/t^3,     y=t^2,       v=y^4=t^8,
rho=r_7^4,     R=rho^4=r_7^16.                         (1)
```

The residual normalization group `mu_16` acts with weights

```text
wt(s)=3, wt(t)=2, wt(r_7)=19=3 mod 16.
```

Its deck subgroup `mu_4` is the source Kummer deck group.  Thus

```text
q,y,rho are deck-mu_4 invariant;
q,v,R are residual-mu_16 invariant.                    (2)
```

The client computes the exact closures of the two invariant graphs

```text
Y_img = closure image(C_4 --> A^3_(q,y,rho)),
X_img = closure image(C_4 --> A^3_(q,v,R)).             (3)
```

It obtains the graph ideals by adjoining the five polynomial relations in
(1), saturating by `t`, and eliminating all seven `a_i`.  Before doing so
it checks exactly whether saturating the corrected-V2 curve by `t` changes
its ideal.  A change is a fail-closed `A6_CHART_LOSS` token; no global
quotient claim may then be made from this chart alone.

If the exact plane projection `L(C_4)=L(s,t)` is independently certified,
then the complete quotient function fields have particularly small frozen
generators.  With `w=s/t`,

```text
L(C_4)=L(q,v)(w),       w^16=q^8 v,
t=w^2/q,                s=w^3/q;

L(C_4)^mu_4 = L(q,y),   y^4=v,
L(C_4)^mu_16=L(q,v).                                  (4)
```

Indeed the exponent lattices generated by `(2,-3),(0,2)` and
`(2,-3),(0,8)` have indices `4` and `16`, respectively.  Formula (4) is
not promoted until the plane projection is proved birational.  In the
absence of that proof, (3) remains an honest invariant-image gate.

## Completion and the source obstruction

Every affine output is homogenized in an ordinary projective plane and
then normalized.  This gives the unique smooth complete curve with the
computed function field, so all points omitted by `t!=0`, all poles of the
invariant generators, and every point at infinity are restored.  Genus is
always the geometric genus of this complete normalization.

The source Kummer curve maps equivariantly to `C_4`; after dividing by its
deck `mu_4`, the source base `P^1_x` maps rationally to `Y_img`, and properness
extends the rational map across every pole.  Its `rho` coordinate is a
nonzero constant multiple of the terminal map `T=r_7^4`, hence nonconstant.
Therefore:

```text
g(normalization(Y_img)) > 0  =>  no order-four source.
```

The composition to `X_img` is also nonconstant because `R=T^4`; hence a
positive genus for the residual invariant image is already sufficient.
These implications do not require the invariant images to equal the full
finite quotients.

## The `U=2` passport sieve

The confirmed extremal-abc classification leaves exactly

```text
D=1,  h=x^3(x-1)^5,   r_7=-(j/2) u^3/(x^2 (x-1)^4);
D=3,  h=x  (x-1)^7,   r_7=-(j/6) u^3/(x-1)^6.         (5)
```

In both cases `rho=r_7^4` has map degree `D` on `P^1_x`.  If
`delta=deg(rho:Y_tilde-->P^1)` on the complete normalization of `Y_img`
and `n=deg(P^1_x-->Y_tilde)`, then

```text
D=n delta.                                             (6)
```

Thus `delta` must divide `1` or `3`.  In particular `delta=2` or
`delta>=4` eliminates both `U=2` passports; `delta=3` leaves only `[7,1]`;
and `delta=1` leaves a final branch/passport comparison.  Degree and branch
divisors must be computed on the complete normalization, including infinity.

The Kummer cover checks attached to a full quotient calculation are

```text
C_tilde --> Y_tilde:       w^4=q^2 y,
Y_tilde --> X_tilde:       y^4=v,
C_tilde --> X_tilde:       w^16=q^8 v.                (7)
```

At a place with valuation `n` of the right-hand Kummer radicand, the local
ramification index is `N/gcd(N,n)`, and there are `gcd(N,n)` points above
it.  A final Riemann--Hurwitz table must list every finite and infinite
valuation, reproduce both cover degrees, and reproduce `2g-2` exactly.

## Fail-closed tokens

- any charged hash mismatch;
- source-presentation mismatch or lead-relation failure;
- missing `r_7` or `t` saturation;
- `A6_CHART_LOSS=1`;
- a non-prime/reducible image without component-by-component handling;
- affine arithmetic genus substituted for complete geometric genus;
- missing points at infinity in the divisor/Riemann--Hurwitz table;
- timeout, OOM, engine disagreement, or absent exact characteristic-zero
  reconstruction.

Any such outcome is **NO VERDICT**.
