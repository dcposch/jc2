# Actual-total named-section all-depth replay V21

Date: 2026-08-27

Status: **FROZEN CONFIRMATORY EXACT REPLAY; DIFFERENT-MODEL REVIEW REQUIRED.**

## Origin and question

An exploratory desk specialization of the frozen 569 canonical tails found
the following candidate answer.  This V21 run is confirmatory, not a blinded
discovery run: the expected values are frozen here before the replay.

For each of the four named assignments

```text
CS0: cs=1; every other source name except rho is zero,
A00: a0=1; every other source name except rho is zero,
A10: a1=1; every other source name except rho is zero,
Z00: every source name except rho is zero,
```

evaluate the entire finite canonical-tail source as an exact polynomial in
`sigma` and `rho`, with no grade truncation.  Determine every nonzero
coefficient, not just grades 13--15.

## Frozen expected answer

The replay must return exactly

```text
CS0: zero in every row and every sigma grade,
Z00: zero in every row and every sigma grade,
A00: row 6 = -(1/16) sigma^15, every other row zero,
A10: row 3 = -(1/16) sigma^15,
     row 5 = -(3/32) sigma^15 rho^2,
     row 7 = -(3/128) sigma^15 rho^4,
     every other row zero.
```

Thus, if confirmed, grade 15 is the first source grade that kills each of
the two displayed `J2` prefix sections.  The result would also show that no
coefficient grade of this frozen finite-tail source can kill `CS0` or `Z00`.

## Exact source contract

1. Pin the canonical tail JSON at SHA-256
   `d72f774c53c7dcb259eefc10ff2a02202e1049b61c17486b1ba22e3773813848`.
2. Pin the already exercised actual-total source emitter at SHA-256
   `5c233e0f2193bc4cb6384e0ad16f3d8b063494541389463853365a7ac443a587`.
3. Reconstruct its section specializations from the literal formulas for
   `p,c,r,n3,n2,n1,n0`, rather than parsing the grade-13/14 exports.
4. Use an untruncated sparse polynomial ring `Q[sigma,rho]`.  A finite-tail
   term containing any load factor vanishes on all four assignments; every
   other term is multiplied exactly.
5. Record all nonzero tail contributions to each surviving coefficient.
6. Check reduction of the full exact answer modulo 32003 and 65521 and
   require every displayed nonzero coefficient to remain nonzero.
7. Bridge to the V20 exact-Q and F65521 result files, whose hashes are
   `b23ffacd1e4e26abccaeb94a5e83f301cd98a9918a3a84e207fc462a880fe68d`
   and `266130872a10983724a7f9a080b36fef33981ff52192cbdf6c713a75bb7cd07a`:
   those files must report empty residual dictionaries for all four sections
   at grades 13 and 14.

## Interpretation firewall

- Killing `A00` or `A10` proves only that the named horizontal section is
  absent once grade 15 is imposed.  It does not put `a0` or `a1` in a
  radical and does not empty either `J2` Rees chart.
- Persistence of `CS0` and `Z00` is a theorem only about the literal source
  generated by the frozen finite canonical tails under these assignments.
  It does not include unwritten Rees equations, chart equations, routing,
  other source families, or a full formal-arc extension claim.
- `CS0` is outside the named unit-`k10` family on `D(k)`.
- No result here proves Gate T, order two, maximum twelve, or JC2.

