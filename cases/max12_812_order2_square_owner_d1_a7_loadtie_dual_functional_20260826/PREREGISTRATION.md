# Preregistration: D1 `a=7` load-tie dual-functional wall

Date: 2026-08-26

Status: **PREREGISTERED EXACT-Q AWS PRODUCER WITH TWO PRIME SCREENS.**

## Literal scope

Treat separately the two strict cells

```text
ord(A)=7, ord(C)=7+d, ord(R)>=7, d=2 or 3,
```

after the registered square/D1 gates on `D(p*k0)`.  No `a=8,9` cell is in
scope.

The grade windows differ:

| `d` | first load-tie grade `G` | local `C^2` grade `T` |
|---:|---:|---:|
| 2 | 26 | 28 |
| 3 | 27 | 30 |

The row-2 target starts at grade 28 and must be retained through each `T`.
Rows 1, 3, and 4 remain target-free; row 4 starts only at grade 32.

## First-wall rank chambers

At grade `G`, the two first pole-one columns tie and combine as

```text
(3/4)*C*(A+k60)/L.
```

Write `B=A+k60=u*z+v` and `C=x*z+y`.  The proper-numerator equations are

```text
r_z = u*y+v*x,
r_0 = v*y-(p/2)*u*x.
```

Multiplication by `C` in `Q[z]/(z^2+p/2)` has matrix

```text
[ y       x ]
[-(p/2)x  y ]
```

and determinant

```text
Delta_C=y^2+(p/2)*x^2.
```

The client must verify the combined first source column exactly.  It must
name and certify the rank-two chamber `D(Delta_C)`, where `r_z=r_0=0`
forces `B=0`, and retain the rank-one face `Delta_C=0`, where a nonzero `C`
vanishes at exactly one of the two etale roots.

## Dual moving-root functionals

Independently enumerate every primitive and every correction through `T`,
emit all seven literal Faber source rows, and bridge them to an analytic
Laurent source.  The complete source must have global pole ceiling two.
Reconstruct

```text
N=h1*z^3+h2*z^2+(h3+p*h1)*z+(h4+p*h2),
H=N/L^2.
```

For a moving root `lambda(sigma)^2=-p(sigma)/2`, use both left-kernel
functionals

```text
Psi_+ = Phi4+lambda*(Phi3+(p/4)*Phi1)=N(lambda),
Psi_- = Phi4-lambda*(Phi3+(p/4)*Phi1)=N(-lambda).
```

Every pole-one source column, including all corrections of the tied
`k60*C` and `A*C` columns and later loads, must pair to zero through grade
`T`.  The first pole-two column is `(3/8)C^2/L^2`, so at grade `T` the pair
must be exactly

```text
q_+=(3/8)*(c1*lambda+c0)^2,
q_-=(3/8)*(-c1*lambda+c0)^2.
```

Require the ideals generated by `q_+,q_-` to be unit ideals on both exact
contact charts `D(c1)` and `D(c0)`, with `lambda`, `k0` inverted.  This
covers both rank chambers without dividing by `Delta_C`.

## Acceptance

1. Independent padded primitive enumeration gives exactly five columns for
   `d=2` and seven for `d=3`, with global pole ceiling two and sole pole-two
   column `C^2/L^2` at `T`.
2. Every normal/load/moving-`p`/target ceiling is derived mechanically.
3. All seven literal source rows bridge; every sigma quotient and the full
   pole-two recurrence passes.
4. The first tied source column and `Delta_C` rank jump are exact.
5. Both dual functionals have zero lower pairing and the named target pair;
   both exact-contact chart ideals are units.
6. Exact Q runs on AWS.  Two distinct large-prime screens run independently;
   neither substitutes for characteristic zero.
7. Every runner is fail-closed, capped, zero-swap, and pins all evidence.

## Firewall

A PASS closes only the two named `a=7` strict cells after the cited gates.
It does not cover a positive-order leading load, an equality face, `a=8,9`,
the grade-32/34 row-4 target-shadow chambers, another D1 face, `p=0`,
`k0=0`, a terminal/global chart, the whole square component, order two,
`(8,12)`, maximum twelve, or JC2.
