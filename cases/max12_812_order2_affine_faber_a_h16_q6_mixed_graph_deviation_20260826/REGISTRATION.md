# Registration: affine-Faber `A`, mixed graph-deviation/quadratic face

Date: 2026-08-26

Status: preregistered diagnostic source replay; no theorem.

The first deployment pre-created the compiler output directory even though
the frozen compiler requires a fresh path.  Both lanes stopped with the same
`FileExistsError` before emitting algebra.  That V1 archive is deployment-
negative only.  V2 removes only the premature `mkdir`; the registered
mathematical source is unchanged.

Use the representative integral regrading

```text
normal H=16, kernel q=6, complements=12,
leading affine graph=42, transverse graph deviation=44,
center>=6, cubic=48, mu4=48.
```

Thus `2H+2q=44` ties the first transverse deviations `d6,d2,dm`, after
the exact affine graph at grades 42--43 and before every center, cubic, or
later target contribution.  Parameterize the exact graph with arbitrary
moving `E` and `K10` series before adding the three transverse deviations.

Reconstruct all seven complete ordinary source rows from frozen tails,
verify grades below 44 vanish, print the entire grade-44 block, and test
both projective kernel charts after retaining `d6,d2,dm`.  Also eliminate
the deviations to expose the residual kernel ideal.  Exact Q is evidence;
F65521 is a control.

This representative is a coefficient-level diagnostic for the universal
mixed face.  It does not prove invariance under arbitrary rational
regrading, source coverage, or any exclusion.  Run only on AWS.
