# Registration: moving-`p` addendum nonmutating V4 retry

Date: 2026-08-26

V3 failed closed in the AWS compiler before engine launch because its wrapper
looked for the shortened string `SQUARE_APROL_ENDPOINT`, while the frozen
owner-v2 emitter spells the anchor `SQUARE_APROLONG_ENDPOINT`.  V4 pins V3
and changes only this wrapper anchor; the source substitutions, analytic
receivers, row transforms, and sentinels remain byte-derived from frozen V3.

Run exact `Q` on Box03 and `F_65521` on r6d under distinct tags with 16 GiB
VM, 600-second compiler, and 3600-second engine caps.  Required endpoint is
`SQUARE_APROL_PTANGENT_ENDPOINT=PASS_MOVING_P_SOURCE_ROW_ADDENDUM`.

Scope remains only the moving-base tangent addendum to the high-contact
cone.  No fan, square-branch, or order-two verdict.
