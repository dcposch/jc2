# M2 case-III two-pole incoming-index bound R1

This packet encodes a cap-free theorem for the previously capped
`mu0>mu>=2` branch of the two-pole case-III merge classifier.  It bounds the
incoming zero-edge index `h=nu_H`; it must not be confused with the
merge-local pattern index `nu_G`.

Run:

```sh
python3 test_caseiii_two_pole_bound_r1.py
python3 -O test_caseiii_two_pole_bound_r1.py
python3 caseiii_two_pole_bound_r1.py
```

The packet is provisional pending different-model hostile review.  It does
not alter `cases/book_offaxis.py`, certify the old census, handle multipole
merges, bound the equal-nonzero-arrival join family, prove landing, or imply
JC2.
