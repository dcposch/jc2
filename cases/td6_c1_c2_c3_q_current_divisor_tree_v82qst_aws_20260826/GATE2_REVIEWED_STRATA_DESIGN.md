# Gate-2 reviewed divisor-tree design (not launched)

This design hash-pins the CONFIRMED nested-identity review
`a3e1315041dafdeefaf26a3a9f2f79e945c2ab70fad18436ddb416f161f9de5b`
and promotion
`6037e902a0e9c9fe3c9115d77753559060913f8d64412455de75430ab526d56b`.
Gate 2 remains conditional on the terminal Gate-1 factor comparison.

On `D(U)`, put

`x=V^2/U^3`, `s=F/U^3`, `g=G/U^6=x^2+4s^2`, and
`ell=L/U^15=4g^2+x^2(x+4)(x+2s)^2`.

## F divisor on D(UV)

Use the original source rows after the exact substitution
`C=(V^2-U^3)/U`.  Then `g=x^2` is a unit and
`ell=x^4(x+8)`.  The dependency-complete split is therefore

- `F=0`, `D(U*V*(V^2+8U^3))`;
- `F=0`, `V^2+8U^3=0`, `D(U)`.

There is no independent `G=0` subcase on this `D(UV)` divisor.  Any source
pivot factor emitted by either raw client is recursively retained.

## G divisor on D(UVF)

Work over the exact quadratic algebra `w^2+1=0` with
`2F=w V^2` and `C=(V^2-U^3+F)/U`.  Here `1+w` is a unit (norm 2), so the
reviewed identity gives

`L=0` iff `V^2+4U^3=0`.

The split is

- `G=0`, `D(U*V*F*(V^2+4U^3))`;
- `G=0`, `V^2+4U^3=0`, `D(U)`; equivalently `x=-4`, `s^2=-4`.

This is exactly the reviewed sharpening
`rad(g,ell)=(x,s) intersect (x+4,s^2+4)`; `s` is not free on the second
component.

## Generic L divisor on D(UVFG)

On this locus `x`, `g`, `x+4`, and `x+2s` are units.  Adjoin

`z=2g/(x(x+2s))`, so `z^2=-(x+4)`.

The first raw client should work fraction-free in the quotient algebra
generated by

- `g=x^2+4s^2`;
- `z^2+x+4=0`;
- `2g-z*x*(x+2s)=0`;

with the stated product inverted.  This is still a genuine hypersurface
(or finite quadratic cover), not a discharged component.  Do not replace it
by generic-point sampling.

## Coverage and controls

Every client must start from original source rows, include direct-qprime and
wrong-row/omission controls, emit the actual pivot-denominator ledger, and
prove any overlap by cleared source identities.  `U=0` and `V=0` are separate
raw/source theorems and are not silently absorbed here.  The Gate-0/Gate-1
objects are presentation diagnostics on the already-empty fixed A3 base; no
family, TD6, SP2, or JC2 conclusion is licensed.

