# Registration: max12 `(9,12)` order-three Q8 normalization jet

- Date: 2026-08-24
- Status: producer-exact; hostile different-model review required
- Report: `xmodel/max12-912-order3-nu-q8-normalization-jet-20260824.md`
- Reviewed parents: corrected Q8 formal-branch theorem and root-free
  critical-value norm

The registered claim is local: with `p=1` and `t=a0`, the seven rows solve
through even order two and odd order three; `v-v0` and `r8-rho0` have unit
quadratic coefficients.  At the node `s*Norm(F)*Norm(W)` is a unit and
`E=0`; on the punctured branch `E/t` is a unit, so it enters generic leaf 4.
The full Taylor families are retained as
`u^ell*f^(ell)(A/9)/ell!` and `u^ell*g^(ell)(A/9)/ell!`.  Only after this
reconstruction, the terminal row forbids a finite-place encounter with the
node.  The punctured branch and global normalization remain open.
