# Arbitrary-Q origin q15 general r2: frozen AWS result

This directory freezes the completed AWS namespace
`/home/ubuntu/jobs/ggv_origin_q15_general_r2_20260828T101721Z_r6b`.
The byte-identical remote archive is in `custody/`; `job/` is its local
semantic extraction.  The archive SHA-256 is
`d0a722a33e26cd90f8665df234f15036ec133e95636a8f537a253b2aea14595a`.

The remote manifest contains 120 files.  All 104 semantic files replayed
locally.  The remaining 16 are inert 163-byte macOS AppleDouble metadata
sidecars; they remain byte-preserved in the archive, but macOS `bsdtar`
consumes them as metadata instead of materializing them as ordinary files.

See `RESULT.md` for the strict verdict and timeout ledger.

