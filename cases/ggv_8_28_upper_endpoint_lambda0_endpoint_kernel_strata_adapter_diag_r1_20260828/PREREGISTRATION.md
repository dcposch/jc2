# Frozen adapter diagnostic r1

Run the exact tiny field and quotient-ring script on audited idle AWS only,
one core, zero swap, 60-second cap.  This diagnoses the common r2 selfcheck
marker failure only; it produces no mathematical HENS/endpoint verdict.
