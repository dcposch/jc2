# AWS-only Q9 rank-class sampler

`run_remote.sh` launches 27 disjoint Box02 shards and aggregates exact
canonical representatives and Q8 transition signatures.  Each shard has a
4 GiB VM cap and a 12-hour timeout.
