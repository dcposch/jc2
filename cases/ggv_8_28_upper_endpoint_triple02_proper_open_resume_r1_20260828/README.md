# TRIPLE02 proper-open resume R5 custody repair

This case is an implementation/preflight packet for one exact chart only:
TRIPLE02 node 1 on `D(Delta)`.  It repairs the R3 timeout by replacing the
logically futile pure `Delta`-power loop with a self-contained two-containment
saturation certificate, then runs the full c4-preserving right-transform,
adjugate-kernel, and quadratic-endpoint replay.

The archived OPEN_SB is treated only as a hash-bound candidate and provenance.
Production freshly verifies its Delta-stability and obtains a reverse
containment witness for each literal basis generator.  The full ring remains
`Q[q0,q2,c4,c6]`; all 95 pivots, 110 residual entries, 11,025 right-transform
entries, 420 lifted-kernel entries, 424 row checks, and ten endpoint
coefficients are exact fail-closed gates.

The R5 custody repair leaves the generated mathematical Singular program
byte-identical to R4.  It makes observed swap and whole-job timeout faults
sticky terminal inputs; literally parses final `cgroup.procs`; validates and
boundedly reaps the exact launcher PID/start-time pair; and requires successful
generation, complete-census replay, fresh extraction, and embedded replay of
the terminal manifest before promotion.  The worker emits only a classifier
candidate.  The supervisor may promote it only after every historical,
resource, containment, artifact, and archive gate passes, by one final atomic
rename.  Focused no-CAS fixtures mutate all four repaired late-failure gates in
addition to the prior containment and mathematical-source controls.

`AWS_PREREGISTRATION.md` is the controlling scope and resource contract.
`PREFLIGHT_REPORT.md` records the local light checks and custody repair.  The
status is `R5_CUSTODY_REPAIRED_LAUNCH_NOT_AUTHORIZED`: a fresh independent
hostile source review and a separate coordinator `GO` are mandatory before AWS
use.  No local Singular computation was performed, no AWS job was launched,
and no canonical file was edited.
