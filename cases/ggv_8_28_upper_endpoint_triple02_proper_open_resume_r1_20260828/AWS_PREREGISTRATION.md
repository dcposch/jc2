# TRIPLE02 node-1 proper-open resume: AWS preregistration R5

Date: 2026-08-28

Status: `R5_CUSTODY_REPAIRED_LAUNCH_NOT_AUTHORIZED`

No AWS launch is authorized by this document.  Launch requires a new independent
hostile source review with a passing verdict and a separate coordinator `GO`.

## Exact scope

This packet resumes only the TRIPLE02 node-1 open
`D(Delta) intersect Spec(R/I)` over
`R=Q[q0,q2,c4,c6]` with `dp` order.  It does not cover the closed successor
`V(I,Delta)`, a later recursive node, the whole TRIPLE02 stratum, another
component, or the ambient endpoint problem.  It never contacts `jc2-lean`.

The two possible mathematical terminal markers are exactly:

- `EXACT_ENDPOINT_DEAD_ON_NODE1_PROPER_OPEN_ONLY`; or
- `RING_LEVEL_ENDPOINT_NONZERO_ON_NODE1_PROPER_OPEN_PENDING_NILPOTENCE_RADICAL`.

Every timeout, source/member/hash/parser/reducer/transform/count/plant/resource
disagreement is `NO_VERDICT`.  The second marker is not a geometric survivor
without later nilpotence/radical analysis.  Neither marker may be promoted to a
closed-node or whole-component claim.

## Frozen inherited inputs

The immutable TRIPLE02 R3 terminal archive is

`cases/ggv_8_28_upper_endpoint_quotient_nf_endpoint_complements_r3_20260828/custody/terminals/ggv_endpoint_complement_triple02_r3_20260828T175423Z_i0f089.terminal.tar.gz`

with SHA-256
`e5bdd2b25cde0dccd8f1e6b8f0e1fcc815d409426308afb49c45df86af53a1e1`.
The preparer rejects any duplicate or unsafe archive member and requires a
unique exact path and hash for every consumed member.  Material member hashes
include:

| exact archive member | SHA-256 |
|---|---|
| `output/INPUT_MANIFEST.json` | `0025f3eafe43b06d8d4cdd7b99870a52b7da331e8985baf7dd3de09a6e48257e` |
| `output/node_001/NODE_INPUT.json` | `b8bf5ec7f09f53211b00d5aafcee0d5b1b45c997c254437a3edb0f647376f68f` |
| `output/node_001/reduce.sing` | `28385a7044bf6b3a2d79c42738860039b687f32f13d5a43c0e67c50b247af609` |
| `output/node_001/NODE_001_REDUCE_PIVOTS.tsv` | `2edaa006dae3f63e90973d40de5c7cdda00437570283429c69b353023645e7fc` |
| `output/node_001/NODE_001_REDUCE_RESIDUAL.tsv` | `f24a4a23aa99d2864c468cce73d7a3affbb7f7baa7344af29a86060dad1a0bfb` |
| `output/node_001/NODE_001_SIZE_6_WITNESS.tsv` | `fa0428864a2e15b65b89c0fee2c8f91a557571b0c2c7502f1c9230a6f88d8632` |
| `output/node_001/rank_size_6.support.json` | `bb8f7ee0faabbdb8e40be15bd4d6b3df319acbb5d6d6df7643954a65503d8c51` |
| `output/node_001/saturation.sing` | `196cb1205291c8cae0e047adf2a759cfa798fbda5a78ceefd9c18b7ec7a5f670` |
| `output/node_001/saturation.stdout.txt` | `4ee63095327d80572764eb3783d0e77c7a6cf385f47cf93c1cafcea843b29418` |
| `output/node_001/NODE_001_OPEN_SAT_STANDARD_BASIS.txt` | `e2d240e369e516ad8e3fb3ac221061abe465a241ce91ceeef96a4e7c0947493c` |

The archived timeout remains `TIMEOUT_NO_VERDICT`.  Its completed saturation
prefix and OPEN_SB are provenance and a candidate only; they are not treated as
a self-contained proof of saturation equality.

The frozen R5 recursor source is SHA-256
`d679a4d7fb4bf2619fbb3ff47ea6f386ba1c75fdc477276cabf3bec8a2b2cd90`.
The exact inherited rank-six archive is SHA-256
`b947a2d3e81525902a93500026020c2cf9580f070767757389daadb9a6bbe3b2`;
its certificate member is SHA-256
`cc6dc1c4edbceeecf31c17b96ab9a0ac9744720100e7bf3f20ba41c4e4cd99d2`.

## Self-contained saturation certificate

Let `B` be the ideal generated by the archived OPEN_SB bytes.  Production must
certify both containments, not inherit the old `sat(I,Delta)` result:

1. Verify `I subset B`; freshly compute `B:Delta^infinity`; and verify exact
   two-way standard-basis equality with `B`.  This gives
   `I:Delta^infinity subset B`.
2. For each of the two literal generators `b` of `B`, search exponents
   `0,...,64`, record every exact `NF_I(Delta^n b)`, and require one zero normal
   form.  This gives `B subset I:Delta^infinity`.

The bound 64 is only a resource bound for finding positive reverse witnesses.
Exhaustion returns `REVERSE_CONTAINMENT_SEARCH_EXHAUSTED_NO_VERDICT`; it is not
a negative theorem.  The production proper route performs zero pure
`NF_I(Delta^n)` searches, never recomputes `sat(I,Delta)`, and never starts the
closed successor.

## Full c4-preserving endpoint contract

The builder must reproduce the archived `reduce.sing` byte-for-byte before it
can emit production.  The production ring declaration is exactly
`ring ambient=0,(q0,q2,c4,c6),dp;`; the archived reduction script has 30 `c4`
tokens.  `c4` remains present through the original 106-by-105 matrix, full right
transform, base change, lifted kernel, 106-row replay, plants, and endpoint.
It is never moved into a coefficient field.

Production must then:

1. eliminate with the node reducer and compare all 95 pivot records and all 110
   residual entries byte-for-byte with the archive;
2. track the full 105-by-105 right transform `C`, normal-form all 11,025 entries
   and all 110 residual entries against the certified open reducer (11,135
   reductions), record every base-changed `C` entry, and never repivot;
3. replay the banked 6-by-6 minor at rows `1,2,4,7,9,11` and columns
   `1,2,3,5,6,7`, require exact equality and nonzero normal form, and bind the
   inherited rank-six upper-bound certificate;
4. build four denominator-cleared adjugate vectors, check 44 residual-row
   identities, record 420 lifted-kernel entries, and check 424 original-row
   identities;
5. pull back all ten symmetric coefficients of
   `E=x14*x72+x1*x97`, including every cross term.

All counts are positive literal gates.  Content/gcd/factor/radical/nilpotence
normalization is forbidden.

## Controls and fail-closed parser

Before production, AWS must pass:

- the hostile invalid-list-slot diagnostic and an expected-diagnostic parser;
- a clean Singular control containing the proper `(x*y),x,(y)` route with zero
  pure-power reductions and reverse exponent 1;
- the empty `(x^3),x` route with exponent 3, four reductions, and the three
  preceding nonmemberships;
- the strict overideal `(y,x-1)` adversary, which passes weak
  proper/stable/inclusion predicates but fails reverse containment;
- the reducer adversary `q2 mod (q2)=0` and a known nonzero normal form;
- a bordered `0+Delta` plant; and two endpoint shifts `e+1,e+2`, requiring both
  affine replays and at least one nonzero shift.  The fixture explicitly checks
  the innocent case `e=-1`, where `e+1=0` but `e+2!=0`.

The light Python gate must reject mutations to the archive hash, exact member
path, ring/order, Delta, one `c4` matrix term, node/open repivot choice, each of
the five concrete right-transform `C` anchors (identity initialization, pivot
column swap, elimination update, open-basis base change, and recorded entry
value), each of nine operation-count gates, one endpoint cross
coefficient, the archived residual, and both semantic plants.  The terminal
parser rejects any Singular diagnostic regardless of return code.

The no-CAS custody suite must also reject a detached stage child, an incorrect
recorded worker PGID or launcher start time, a nonempty PGID, exact-`JOB_TAG`,
or literal `cgroup.procs` census, a malformed or missing cgroup census, a hung
launcher beyond the bounded reap interval, a nonzero late worker exit after a
mathematical classifier candidate exists, a missing scope firewall, an
observed swap violation followed by a clean final snapshot, a whole-job
timeout followed by worker return zero, a terminal-manifest generator or
replay failure, an unexpected archive member, a valid outer archive with an
invalid embedded manifest, and any late archive failure.

## AWS custody and resource contract

- Fresh immutable job namespace and source archive; source is made read-only.
- Linux with `/sys/class/dmi/id/sys_vendor == Amazon EC2`, IMDSv2 instance and
  hostname binding, and an IMDS-visible `CodexJob=$JOB_TAG` instance tag.
- One pinned core.  At most 1 TiB physical RAM; 256 GiB process virtual-memory
  limit; 64 GiB file limit.
- Zero configured swap before, throughout ten-second telemetry, and after.
- Tiny hostile and adapter controls: 60 seconds each.  Production: 1,200
  seconds.  Whole supervisor: 1,800 seconds with TERM/KILL and an exact
  no-orphan audit.
- Singular normalized-version SHA-256 (random seed normalized):
  `f16fa01f3d3bdb83ac08dc7dbc25ebd84ba147bf3433779a0f5c695cee9ad9ec`.
- `elim.lib` SHA-256:
  `cb39b70a6cff9f1bf6b6397b9de3a597f6bb9d91dad468243bed9bfb2552fcd1`.
- Exact preregistration, source-manifest, source-archive, generated-script, and
  all terminal artifact hashes must match before parsing.  The source-archive
  hash is frozen externally in `SOURCE_ARCHIVE.sha256` to avoid self-reference.

The supervisor first probes a delegated user `systemd` scope.  If usable, the
entire worker and every Singular stage run in one transient scope with a 256
GiB `MemoryMax`, a one-core `taskset`, a task limit, recorded `ControlGroup`,
verified worker membership, TERM/KILL on every exit, and an empty
`cgroup.procs` gate obtained by reading and strictly parsing the pseudo-file's
PID contents (never its metadata size).  A collected scope is accepted only
under the explicit recorded inactive/collected route after exact launcher
reap.  Only when no usable user manager exists may it use the
fallback: `setsid --wait` establishes one worker session, the worker's live
PID/PGID/SID/start time are read from `/proc` and must satisfy
`PID=PGID=SID`, and every stage child must inherit that exact PGID/SID.
`run_singular_stage.py` is forbidden to create a nested session.  Both modes
also kill and census exact same-uid processes carrying the inherited
`JOB_TAG`, with PID start-time revalidation before signals.

The launcher PID and `/proc` start time are recorded immediately.  After any
TERM/KILL, the supervisor polls that exact identity for at most ten seconds;
only absence or a same-start-time zombie permits the shell builtin `wait`.
Identity drift or failure to become reapable is a sticky terminal fault.

The worker never publishes a terminal.  It may atomically write only a
classifier candidate after its artifact manifest, final zero-swap check, and
scope-firewall marker pass.  The supervisor requires worker return code zero,
final zero swap, sticky `swap_violation=0`, sticky `whole_timeout=0`, no
containment-preflight, launcher-reap, or final-systemd fault, the exact
worker/artifact and scope markers, and empty PGID, job-tag, and (when used)
systemd-scope censuses.  Terminal-manifest generation status is checked; the
manifest's exact regular-file census and every hash are independently replayed;
the temporary terminal archive is extracted into a fresh private directory,
rejects unsafe/nonregular/extra members, and replays its embedded manifest.
Only after all archive replay statuses are clean may the archive be atomically
installed.  The supervisor then refreshes the post-archive time/swap latches;
those latches and every earlier gate must be clean before the public
`TERMINAL.marker` can be atomically renamed as the final successful action.
That archive contains
the candidate and gate evidence but no promoted mathematical terminal.  Any
missing or late-failing gate publishes only
`CUSTODY_OR_ADAPTER_FAILURE_NO_VERDICT`.

The source-manifest-covered `runtime_expectations.env` pins the preregistration,
generated script, normalized Singular version, and `elim.lib` hashes.  The
separately reviewed `aws_launch_preflight.sh`, which is outside the source
archive to avoid hash self-reference, pins and verifies the literal source
archive before safe extraction and `exec` of the supervisor.  Its own bytes and
the archive sidecar are frozen in `LAUNCH_MANIFEST.sha256`.

An infrastructure, tag, version/library, zero-swap, orphan, nonzero worker
return, containment, archive, cap, or custody
failure is `NO_VERDICT`.  No nearby branch or closed-node job may be launched by
this packet.
