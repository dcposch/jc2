# Q8 p127 breadth order-8 contact producer

This AWS-only producer lifts 80 distinct audited good fibres of the candidate
degree-190 plane curve through order 8.  The deterministic set is
`1..83` excluding `25,39,56`; `w=25` is omitted because a separately frozen
order-64 lift already exists.

Each lane independently requires:

- the pinned 123-fibre lex-shape audit and its fixed-fibre custody entry;
- all original shape-source remainders and candidate specializations audited;
- monic squarefree degree-190 base eliminant (`H_v` a unit);
- the six-by-six source Jacobian a unit modulo the eliminant;
- the ratio and localization equations at the base and through order 8;
- all six source rows through order 8; and
- vector-space dimension `190*8=1520` with `final_fail=0`.

The intended intersection-theoretic use is additive across distinct plane
fibres.  This producer alone makes no component, characteristic-zero,
grouping, no-merger, or trajectory claim.
