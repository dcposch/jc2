# Immutable freeze notice

Frozen 2026-08-25 after two independent AWS computations agreed on the exact
origin-augmented affine mixed volume `658`, the full replay returned raw torus
value `519`, and both standard controls returned `1`.

The immutable mathematical scope is the numerical sparse bound and the
conditional contact-to-component criterion in the companion report.  This
freeze does not claim that the live breadth lifts have completed, that an
`H`-supported source component has yet been forced, or any characteristic-zero
or trajectory consequence.

Verify custody from this directory with:

```text
sha256sum -c manifest.sha256
python3 verify.py
```

Companion report at freeze:

```text
xmodel/max12-912-order3-nu-q8-sparse-contact-component-lemma-20260825.md
SHA256 78fb3e1b08556498d79ed180f9b64e243f88dd80780936bc47e15b3749b5ce77
```
