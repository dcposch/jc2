# Registration — `(9,12)` order-three `nu!=0` full parity exclusion

Date: `2026-08-24`  
Status: `PRODUCER-EXACT / HOSTILE DIFFERENT-MODEL REVIEW REQUIRED`

## Claim registered

No actual `nu!=0` order-three Keller trajectory lies on
`q=x0=x2=x4=k=0`. The `x5=0`, `A=0`, and `p=0` boundaries are exact and
exhaustive; on `p*x5*A!=0`, the quotient `v=A/(p*x5)` lands on a genus-five
cyclic curve and is constant, forcing `r8` constant against its terminal row.

## Scope firewall

This is a full parity-locus theorem, not a proof that every loaded solution
has parity. Non-parity components, Taylor boundaries, the order-one core, all
`(9,12)`, maximum twelve, a counterexample, and JC2 remain open.
