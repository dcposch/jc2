# Exact endpoint-on-kernel component packet

This case freezes the first exact component portfolio for the homogeneous
endpoint quadratic

```text
E = x14*x72 + x1*x97
```

on the set-theoretic rank-drop components `c8=0`, `q1=0`, and `P=0` of the
frozen 106-by-105 receiver matrix.  `PREREGISTRATION.md` and `SOURCE.sha256`
are the immutable run registration; `RESULT.md` records the strict verdicts
and their scope.

The complete terminal AWS namespaces are preserved verbatim in `custody/`.
Their SHA-256 digests are pinned in `CUSTODY.sha256`.  Each archive contains
the exact right-kernel certificate(s), every diagonal and cross coefficient
of the quadratic pullback, source and output manifests, timing/resource
records, zero-swap preflight, process-group telemetry, and an empty final
process census.

The `c8` and `q1` results are symbolic generic-chart results.  The `P` lane is
only a six-point exact screen and remains `NO_VERDICT`.  Pairwise/triple
intersections and all rank-jump substrata remain pending.
