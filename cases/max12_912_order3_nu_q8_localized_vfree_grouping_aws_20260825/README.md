# Q8 v-free localized component grouping (AWS-only)

This exact finite-field race removes the unused free boundary variable `v`
before `minAssGTZ`.  It computes minimal primes in the original eight-variable
localized quotient, then maps those primes to a nine-variable ring only for
the Q8 boundary intersection.  This preserves the algebra of the prior
localized route while avoiding an unnecessary polynomial extension during
primary decomposition.

The output is modular support only.  Characteristic-zero absolute component
grouping remains the promotion gate.  Run only on AWS under the 12-hour /
256-GiB guard; no result is claimed until harvest and review.
