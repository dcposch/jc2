# V76 fixed-A3 review-surface repair

This nonmutating supplement is the review/promotion surface for
`../td6_c1_c2_c3_q2_fixed_a3_complete_union_v76_aws_20260825/` after the
V57 and V75 hostile reviews.  No V76 producer byte changes.

The exact constructible cover remains

`Spec Q[C,V,U] = D(U*H*B3) union V(U) union V(H) union V(B3)`,

with `H=C-3U^2`.  Its leaves are now charged as:

- repaired V57 original-source identity on `D(U*H*B3)`;
- reviewed V70 whole `U=0` three-piece union;
- reviewed V71 whole `H=0` union;
- repaired V75 whole raw `B3=0` six-leaf union.

The four V76 deletion controls are interpreted constructibly: deleting the
generic leaf leaves a generic point of `D(UHB3)`; deleting a divisor leaf
leaves a generic point of that divisor outside the other two.  They do not
assert anything beyond this set cover.

Promotion remains gated on a clean different-model review of this repaired
dependency surface.  The conclusion, if confirmed, is only emptiness of the
fixed source-typed `(C,V,U,beta)` A3 q2-beta family.  It is not a transverse
neighborhood, full TD6, SP-2, landing, or JC2 theorem.
