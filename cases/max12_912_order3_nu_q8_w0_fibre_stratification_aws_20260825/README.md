# Selected `(9,12)` localized six-row `w=0` fibre stratification

This package implements the frozen preregistration.  It is a boundary-fibre
classifier, not a trajectory theorem.  It neither mutates nor broadens the
reviewed selected corrected-Q8 positive-genus exclusion.

All Singular and substantive Python execution is AWS-only.  Local activity is
limited to editing, hashing, shipping, and short status inspection.

The completed evidence matrix is harvested under `aws_box02_v1/` and
`aws_box03_v1/`.  It contains 34 fail-closed Singular runs: exact
characteristic-zero `std`/block results, an independent characteristic-zero
`slimgb`/`dp` replay, and controls in characteristics 127 and 32003.  The
AWS-only parser `aggregate.py` checks the complete expected matrix and writes
`aws_aggregate_box02_v1/aggregate.json`.

The exact conclusion is only about the affine source chart

```text
w=0,  x5*(x3-2*x5) != 0.
```

That fibre is the reduced degree-eight corrected-Q8 scheme.  The source
boundary `x5*(x3-2*x5)=0`, projective escape of the six coefficient
coordinates, and the separate `p=0` chart are not classified here.
