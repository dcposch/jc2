# Registration

Registered claim: the exact characteristic-zero localized affine
`p=1`, `k=mu=0`, `w=0` six-row fibre with
`x5*(x3-2*x5)` inverted is scheme-theoretically
`Spec(Q[v]/Q8)`, with `Q8` irreducible and squarefree, `r6` a unit, and the
full relative `8 x 8` Jacobian a unit.

The complete preregistered cover was run over `Q`, `F_127`, and `F_32003`,
with independent characteristic-zero `std`/block and `slimgb`/`dp` engines.
The AWS-only aggregate reports 34/34 PASS.

Scope exclusions remain charged: `x5=0`, `x3-2*x5=0`, their overlap,
projective coefficient-source escape, `p=0`, terminal `r8`, both Taylor
families, rationality/trajectory existence, all `(9,12)`, maximum degree
twelve, and JC2.

Report:

```text
xmodel/max12-912-order3-nu-q8-w0-localized-fibre-classification-20260825.md
```
