# TD6 V89H2 `q14` plus high-tail triangularity preregistration

Date: 2026-08-26

Status: producer diagnostic; no result claimed before dual AWS output.

## Exact question

Set `q2,...,q13` to zero and retain the ten coordinates

```text
q14,q16,...,q24
```

over the untruncated exact coefficient ring; `q15` remains the separately
licensed target-shear coordinate and is absent. Rebuild the literal V87
transport, all 38 raw FIRST sources, and genuine raw P12.

Using exactly the reviewed q-zero FIRST pivot columns, form

```text
B(q)=A0^(-1) A(q)=I+N(q).
```

First require the directed support graph of `N` to be acyclic. If it is,
construct and verify the finite polynomial two-sided inverse of `B`, normalize
FIRST by combinations of the original literal sources, and reduce literal
P12. A q-independent reviewed V85 unit remainder excludes this entire
ten-coordinate tail. A positive-q remainder is the exact first
integrability obstruction. A directed cycle obstructs only this triangular
proof and carries no nonintegrability claim.

## Fail-closed gates

1. Consume frozen V87 source bytes and reproduce every q-zero V85 source
   digest.
2. Retain both literal source paths for each of the ten selected q jets;
   specialization of the other twelve licensed jets is explicit.
3. Invert only `A0` over `E3`; any q-dependent inverse is a closed failure.
4. Verify both sides of the matrix inverse, normalized FIRST against the
   original 38 source maps, literal P12 replay, and independent omission of
   one active FIRST source and P12.
5. Emit the complete support graph, topological order or first cycle, exact
   remainder, original-source multiplier inventory, and exact result record.
6. Require denominator radical support to lie in the registered
   `U,H,B3` open; never invert `F` or any q expression.
7. Require byte-identical mathematical output on two distinct AWS hosts.

## Consequence and scope

A collapse would move the already provisional V89H1 tail exclusion one step
lower: all ordered unit-q strata with earliest exponent at least 14 would be
empty on this retained fixed three-center source slice over `D(U H B3)`.
It would leave only `q2,...,q13` as possible earliest transverse units.

This does not cover those twelve lower jets, omitted correction/orbit/pole,
moving-center, dead-stretch, deck/torsion, other boundary moduli, total Rees,
whole fixed A3, TD6, SP-2, or JC2.
