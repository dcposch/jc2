The prior model call timed out after you had hash-checked and loaded every
frozen D8--D15 input. Continue this SAME Fable review from that context.

Prioritize and finish the PRIMARY D8--D15 hostile audit only. Do not inspect
or discuss the later D16--D22 tail or any new campaign work. Do not enter,
list, search, read, build, status, or modify `jc2-lean`. Preserve all frozen
producer bytes.

Avoid another long unbroken planning call: proceed to tool use and create the
independent self-contained standard-library checker requested in the original
prompt now. It must reconstruct the fractional recurrence itself (no import
from the producer), cover all ten modes, verify the charged D8--D13 formulas,
the A|D versus exact-D=0 repair, the D11/D12 rootwise forcing, the separate
c2=0 companion, and the literal raw D0..D15 survivor/windows/forced c14 with
sensitivity mutations. Run it, fix it until deterministic, then create the
requested report, RESULT/README, SOURCE/EVIDENCE manifests, recheck frozen
hashes, and return the explicit PASS/REPAIR/FAIL verdict with paths/hashes.

Keep field/radical conclusions separate from the one literal point and from
scheme/universal endpoint claims. If you discover any defect, name it
immediately and quantify its impact. Complete the artifacts rather than
repeating a narrative plan.
