# K00 literal grade-four gate over the provisional rank-zero plane

You are Opus 5, an equal-standing independent primary researcher in the
plane Jacobian conjecture campaign. Work from repository root
`/Users/dc/code/math/jc2` on frozen Git basis
`92ebe92ad5986a47f01af9ed901260595dfed869`.

Write exactly one repository file:

`xmodel/k00-grade4-rank0-plane-primary-opus5-92e-20260829.md`

Do not edit anything else. Never read, list, stat, grep, build, or touch
`jc2-lean`. No web, network, commit, push, canonical edits, or external
messages. Exact desk algebra is allowed in fresh `/tmp` scratch only, with
at most 60 seconds CPU and 1 GiB address space per process. Any warning,
signal, cap hit, or swap is `RESOURCE_CAP_NO_VERDICT`; if the exact target is
heavier, stop and preregister the smallest AWS packet rather than running it
locally.

## Provisional dependency and source custody

The different-model review of the following theorem is running in parallel,
so treat it as a provisional branch specification, not as a premise you have
verified:

`xmodel/k00-grade3-rankle1-primary-grok46-92e-20260829.md`

It reports that the grade-three compatible rank-zero locus is the reduced
two-plane

```text
(d0_1,d1_1,d2_1,d3_1,d4_1,d5_1)=(2s,t/8,s,t,s,2t),
```

on which `A=0`, `c3=0`, and all six grade-two variables
`u=(d0_2,...,d5_2)` remain free. Your target is conditional on this literal
plane; do not claim it is exhaustive unless you independently prove the
needed upstream statements.

Reconstruct the seven literal `Lambda_grade=4` rows directly from the exact
source labels and terms in

`cases/max12_812_order2_u2_62_k00_grade7_fitting_atlas_v26_20260827/aws_box02_r2_held_compile/output/ATLAS_EXACT_POLYNOMIALS.json`.

Use the V20/V26 source metadata and literal labels as needed. Do not trust a
status string, a previous `/tmp` transcript, or name matching in place of a
declared ring map. Record every source hash actually used.

## Exact question

After substituting the displayed plane, determine exactly the geometric
solution locus of all seven literal grade-four equations over an algebraic
closure, with variables

```text
s,t;
d0_2,...,d5_2;
d0_3,...,d5_3;
k10_0,
```

and with the normalized source open `k10_0 != 0` retained by the explicit
Rabinowitsch localizer `z*k10_0-1` (check the sign). First determine from the
literal rows which variables really occur and whether the newest
`d*_3` block is affine/linear or disappears on the plane.

At minimum decide, with an exact certificate or a typed open state:

1. Is the localized grade-four ideal unit or proper?
2. If proper, what are its dimension, radical/components if desk-accessible,
   and a rational or exact algebraic witness if one can be certified?
3. Does grade four constrain the formerly free `u` variables, the plane
   parameters `(s,t)`, or `k10_0`? Give the smallest exact elimination or
   Fitting description; do not confuse projection closure with a lift.
4. If a nonempty stratum survives, what is the smallest literal grade-five
   successor, retaining every variable/open condition at the first grade
   where it occurs? If empty, state only the conditional branch kill.

## Mandatory controls

- reconstruct source strings independently from `exact_terms` and verify
  the seven `(Lambda_grade,row)=(4,1..7)` hashes;
- declare coefficient field, generator order, substitutions, and every ring
  map;
- compare ordinary and optimized bases if a basis is computed;
- use `ncols`, never `size`, for positional loops;
- unit/proper control ideals and at least these mutations: one plane
  coefficient, one literal grade-four coefficient, and the `-1` in the
  localizer;
- explicitly test that all grade-two and grade-three equations vanish under
  the claimed upstream specialization, but label this an independent check,
  not a promotion of the upstream theorem;
- distinguish ideal equality, radical equality, set-theoretic consequence,
  and a rational witness.

## Output

Begin with one binary disposition such as
`PASS_K00_G4_RANK0_PLANE_EMPTY`, `PASS_..._PROPER`, or a precise
`RESOURCE_CAP_NO_VERDICT`/`OPEN`. Include exact scripts/commands, engine and
version, caps, wall/RSS/swap information, stdout hashes, sufficient
certificate data, mutation outcomes, blockers and the smallest successor.
State all nonclaims: no later-grade lift, arc, source reachability,
counterexample, order-two exclusion, or JC2 conclusion.

End with a `charge_basis` fenced block only if you assert a new numerical
exit price (normally absent here), then a seal giving the body definition,
body byte count, body SHA-256, and frozen basis. The lane runner records the
full-file hash out of band. Fail closed.
