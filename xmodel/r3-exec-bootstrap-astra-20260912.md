# Same-process closed-child bootstrap

Verdict: IMPLEMENTED STATIC / UNEXECUTED / NOT RUNTIME-QUALIFIED. One new
metadata bootstrap and its contract are complete for focused actual-code
FIRST review. No enabled specification, registration, authority, worker,
native qualification or preflight result is supplied. No frozen source changed.

First action was 2026-09-12 15:53:53 UTC. Original publication reserve16:09 /
hard16:12 UTC was retained. Six owned targets were absent before authoring;
the transaction began15:56:35. Code/contract/PINS WHOLE readback and five
unchanged input postpins completed before16:09; publication uses the reserve.

## Exact interface finding

Fresh WHOLE dispatcher inspection confirms that the scientific authority has
no holder/PID field and that outer_argv is the fixed Python/dispatcher/path
vector. All nine authority hashes are acyclic: authority bytes bind the
registration PATH, static inputs and, for valid/dummy, observed leaf identity;
they never bind the registration SHA. The bootstrap reconstructs those exact
canonical bytes in memory and emits no authority files. The resulting nine
full CAPRUN/setpriv/Python vectors preserve the old command order, profiles,
wrong-tag checks and refuse-hash supplied-zero exception. FULL_13 and typed
future hash slots are refused.

The one process verifies a separately approved immutable input specification,
exact code/native/source pins, boot/namespaces/outer membership and empty
topology; creates nine direct empty leaves once; binds their device/inode;
freezes registration and separate PREEXEC-only binding receipt with exclusive
creation/fsync/readback; then execve replaces itself with the same pinned
interpreter into the unchanged dispatcher. It never forks a dispatcher,
keeps a holder, resets a timer, changes membership, retries or deletes.
The existing strict dummy and populated-based completion guard remain intact.

## Scope and review duties

Complete external qualification is still REQUIRED INPUT, not evidence:
ROOT approval and source/runtime/bootstrap FIRSTs; actual interpreter/native
closure and literal refusal compatibility; host/root namespaces, cgroupfs
and ACL support; source/wrapper/stdio/tmpfs/durable layout; exclusive ROOT
no-writer/no-entrant/no-migration trust; effective outer quota hierarchy;
already-armed original supervisor and retirement; finite inherited descriptor
closure. Metadata hashes alone cannot establish those facts. Native hashes
are repeatedly checked and current map membership remains strict; these
bounded observations are non-atomic under the explicit no-concurrent-writer
contract, not an adversarial-ROOT proof.

No aggregate CPU or real-time guarantee is inferred from sampling. CPU
origin stays zero, outer elapsed time includes bootstrap, fixed deadlines
are never now-plus-budget. Bootstrap120seconds and kernel-read64KiB caps are
UNMEASURED fail-closed restrictions. Hashing/blocking I/O must be covered by
the independent outer watchdog. Unsupported observations, stale identity,
preexisting output/leaf, wrong mode/pin, expired clock or refused exec stop
without fallback. Partial creation and pre-exec failure require ROOT recovery
of original streams/spec/leaves; PREEXEC_BINDING is never a preflight success.

OPEN quantity: whether the actual installed metadata path can complete
binding and the unchanged nine controls under all fixed limits. Cheapest
discriminator is ONE separately ROOT-authorized, AWS-only nine-phase batch
after actual-code FIRST and launcher/native qualification, with the specified
stale-identity/pin/freshness/mode/clock/exec negative controls. No observations
or success forecasts exist; no science phase is proposed. The contract
enumerates these prerequisites and exact schema/byte/status limits.

## Custody, read scope and collision check

Exactly five inputs were freshly pinned then read WHOLE in required order:
COORDINATION806lines, TASK, authority133lines, dispatcher744lines, contract60lines.
Long reads used unclipped ranges. Selected dispatcher sections were reread
for the final command comparison. No runner, probe, mutator, scientific
engine, coefficient, other task or linked source body was read. Only inert
text/hash/presence tools and the existing administrative finalizer ran; no
syntax/import/AST/test/dummy or authored-program execution occurred.

Own-only collision check: one new bootstrap/contract, no framework/holder/
service/installer/controller; original runtime and prior attempt artifacts
remain immutable. All authored edits used apply_patch. The final report and
manifest are verified through the unchanged transaction; custody records all
five input hashes and all five owned report/code/contract/PINS/manifest hashes,
excluding custody's self-hash. This packet has no scientific consequence or
execution authority.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `4853`.
- Body SHA-256:
  `89d00272679a9b6df518dfaef318273929abac9ec7d470407e56770c38fb5f23`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
