# TD6 q2-beta N13 exceptional-curve gate — AWS exact replay

## Verdict

**PRODUCER-EXACT PASS; exact cross-package all-beta closure of three curves;
hostile review pending.**

On each exact function field for `H=P3=0`, the `B3` fibre
`t^2-4t+2=0`, and the `B3` fibre `t=1/2`, the full staged replay has ranks
`3470/3602`, `38/132`, `38/94`, and `25/56`, and gives the same exact
original-row compatibility

```text
N13=(k/25)*beta.
```

The coefficient `k` is a previously hostile-confirmed unit.  Therefore any
survivor on one of these curve fields has `beta=0`; the frozen fixed-beta
genuine-P12 curve certificates then exclude it.  Every new denominator is a
power of the curve parameter, and parameter zero is the origin, covered for
all beta by the separately frozen raw `U=0` theorem.  Thus all three whole
set-theoretic exceptional curves are empty in this fixed source scope.

The immutable package is
`cases/td6_c1_c2_c3_q2_n13_curve_quotient_aws_20260825/`.  Archive SHA256:
`a36475f22d64dc025bcda19867f8c603c90329f2c4a5ae9281404d4570017151`.
Stdout SHA256 values are `9a8d76fc...` (`p3`), `3980cc27...` (`b3tq`),
and `a26447df...` (`b3half`).

This does not yet close the generic all-beta `H` or `B3` divisors and makes
no fourth-modulus, whole-TD6, SP-2, landing, or JC2 claim.
