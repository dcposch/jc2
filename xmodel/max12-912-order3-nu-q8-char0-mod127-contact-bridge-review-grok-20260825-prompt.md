# Hostile different-model review request: Q8 cross-characteristic full-contact bridge

Review the immutable producer package

```text
xmodel/max12-912-order3-nu-q8-char0-mod127-contact-bridge-20260825.md
  SHA256 39ae622919f5eba41e4e1a18d6777749d38c34a1be4a10569f65295815a11490

cases/max12_912_order3_nu_q8_char0_mod127_contact_bridge_aws_20260825/
  MANIFEST SHA256 d7abdade469ecb509ac90b9433a78e2af2c89882b190ecbd7b6f7f96d06a1a2e
  FREEZE SHA256   e9744c64074f132791d37006ac81d6cb33fada09167543d1a49860ec4f49e7d6
```

Read the report, every manifest-charged byte, the repaired no-merger erratum,
and the pinned reviewed Q8 formal/global quotient, normalization, primitivity,
and infinity artifacts.  Do not edit producer or frozen bytes.  Return a
hostile report at

```text
xmodel/max12-912-order3-nu-q8-char0-mod127-contact-bridge-review-grok-20260825.md
```

with `CONFIRMED`, `REPAIRABLE`, or `REFUTED`, the smallest failing identity or
missing hypothesis, exact consumed hashes, and execution disclosure.

Charge every point below.

1. Verify that all six sparse rows over `Q` have only 127-unit denominators,
   reduce coefficientwise to the source used by the independently frozen
   full-contact case, and that the redundant `v,inv` equations are exactly as
   advertised.
2. Attack the localization order.  The arithmetic source must invert
   `x5*(x3-2*x5)` but not `w`; explain why its generic `w!=0` open is the
   reviewed punctured quotient and why this is the correct contact closure.
3. Verify that monic normalization of `Q8` over `Z_(127)` is finite etale and
   that a common finite unramified splitting DVR exists.  Check that this
   really supplies all eight integral characteristic-zero roots with the
   advertised distinct reductions.
4. Independently check every contact formula, the normal `3 x 3` solve,
   every 127-denominator unit, all source residuals, and coordinatewise
   reduction to the frozen mod-127 section.  Ensure no Q12/wrong-`x1` slice
   enters.
5. Recompute or independently audit the full relative `8 x 8` determinant,
   its reduction `[101,42,36,116,8,110,107,14]`, gcd one with `Q8bar`, and
   norm `88 mod 127`.  Attack row/column/sign and presentation mismatches.
6. Audit the arithmetic formal-implicit-function conclusion.  State precisely
   why the completed local source is `R[[w]]`, with fibres `K[[w]]` and
   `k[[w]]`, and why the reviewed characteristic-zero selected branch is its
   generic branch.  Distinguish the arithmetic uniformizer 127 from the
   coefficient parameter extension `t^2=w`.
7. Audit the source-scheme identity needed by the primitivity and infinity
   theorems.  Do not infer it from the plane projection `H`; verify it from
   the pinned divided quotient rows and graph/localizer presentation.
8. Attack scope.  The package must not claim mod-127 degree one, all-eight
   component grouping, characteristic-zero no-merger without that separate
   input, Taylor/trajectory realization, maximum twelve, or JC2.

The producer V1 missing-dependency failure is a negative custody control only;
review V2 mathematics.  If executing, follow the standing AWS-only policy for
substantive computation.
