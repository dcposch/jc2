# 09:48Z full-spectrum blind owner report — TD6 owner

Frozen cutoff: `2026-08-25T09:51:47Z`.

## Blind-contract audit

- I verified and read the sealed packet
  `xmodel/ideation-20260825T0948Z-state.md`, SHA-256
  `73cb3cc174d2f0d1cc3bd4af0150a0e83cc05d705372a624d159bffe5f96265b`.
- I then read every current overlay and all 46 numbered rows of
  `APPROACHES.md`.  I did not read any other `20260825T0948Z` submission.
- V50/V53/V54b/V55/V56 remained on AWS throughout.  No statement below is a
  promotion, and the positive-genus theorem is treated as provisional pending
  hostile review.

## Executive reranking

The positive-genus certificate is the first event in this campaign that turns
the explicit selected-Q8 component into a plausible short proof obstruction,
rather than merely a better local fibre.  The immediate target is no longer
to compute more points of `H`: it is to compose six already named interfaces
without widening any of them:

1. geometrically integral `H/F_127`;
2. `g(normalization(H))>0`;
3. the corrected `H`-supported rational contact;
4. the arithmetic `R[[w]]` attachment;
5. a proper flat/semistable specialization statement identifying `H` as a
   reduced component of the horizontal trajectory closure; and
6. the primitive alternatives plus infinity passport.

The new mathematical opportunity is that step 5 may need **less** than the
earlier unique-component/no-merger lemma.  In a proper semistable family, the
generic arithmetic genus equals the sum of the special-component geometric
genera plus the first Betti number of the dual graph.  A single reduced
positive-genus special component therefore cannot occur in a degeneration of
a rational generic curve, even if extra vertical rational components meet it.
This gives a concrete relative-dualizing/stable-reduction obstruction and may
bypass uniqueness at the contact.  What must still be proved is horizontal
component membership and multiplicity/reducedness; a local point attachment
alone is not enough.

The AS update moves the displayed 513/519 points back from “survivors” to
pointwise deaths once the chronological `H6,J6` and degree-five rows are
restored.  That is valuable evidence for restoration curvature, but the live
object is now the full accepted-fibre Q5/H6 formula under two solvers.  TD6
remains a structural third lane: duplicated V50 has crossed all 54 previous
rows, while the current rows and exact denominator boundary remain open.

## Compact disposition vector over all 46 avenues

Legend: `C` continue as a live mathematical client; `R` redesign/narrow to the
named client; `H` hold as reserve/tool; `S` stop at current evidence.

```text
 1 H   2 C   3 R   4 H   5 H   6 R   7 R   8 H   9 H  10 S
11 S  12 H  13 H  14 H  15 H  16 H  17 S  18 S  19 C  20 H
21 R  22 S  23 S  24 S  25 C  26 R  27 H  28 C  29 R  30 H
31 C  32 R  33 R  34 H  35 S  36 C  37 H  38 H  39 R  40 S
41 S  42 S  43 H  44 S  45 H  46 C
```

The non-obvious assignments are:

- Rows `2,25,28,31,39` are now one proof cluster.  Q8 supplies a named
  integral component and positive genus; monodromy supplies the contact
  alternatives; a proper model/Rees calculation supplies horizontal
  membership; relative dualizing cohomology supplies the concrete genus
  class that row 39 previously lacked.
- Rows `6,7` remain wrong as global starting points but are newly licensed on
  the named `H` component: compute its place/normalization data, not an
  imaginary universal `A(F)`.
- Row 33 is `R`, not `C`: untwisted action residues stay costume, but varying
  source adjoints/restoration curvature are a real client-specific class.
- Rows `19,21,36` are one counterexample cluster.  Structured SAT is useful
  only with chronological source closure, canonical carries, fixed collision,
  and a proved state transition.
- Row 46 is `C` only as a certificate layer after source stabilization, never
  as a discovery theorem or finite-universe substitute.

All other `H/S` rows retain the prior reasons: no finite reduction, category
error, a refuted implication, or no named invariant capable of beating the
three live exact gates.

## Principal bottlenecks, reranked

### Proof side

1. **Q8 horizontal attachment and genus specialization.**  Prove that the
   reviewed `H` occurs, reduced and with controlled multiplicity, in a proper
   flat model of the same horizontal characteristic-zero trajectory component
   selected by the `R[[w]]` branch.  Then apply stable-reduction/arithmetic-
   genus monotonicity.
2. **Complete maximum-twelve routing.**  Even a successful selected-Q8
   exclusion must be composed with the exact primitive/contact alternatives
   and every other maximum-twelve load/core branch; no selected leaf is the
   whole bounded theorem.
3. **Global landing and a cofinal/absolute ceiling.**  A maximum-twelve result
   is not JC2 until arbitrary Keller pairs land in a covered degree/book
   universe.  `G2-PSC`, `G2-BD`, full Sigray landing, and a ceiling remain
   distinct debts.

TD6 denominator closure is now below these global proof bottlenecks, though
it remains the best independent boundary-family laboratory.

### Counterexample/disproof side

1. **The full AS accepted-fibre chronological successor.**  Decide the frozen
   Q5/H6 formula, not more displayed points.
2. **A sufficient finite state and recurrent transition.**  Carries, phase,
   support, collision, and restoration history must determine successors;
   rank/count signatures alone do not.
3. **Uniform polynomial complexity and characteristic-zero algebraization.**
   An all-depth restricted-analytic lift with growing support is still not a
   polynomial Keller counterexample.

## New mechanism and cross-connection

### New mechanism: a relative-dualizing genus barrier

Let a hypothetical rational characteristic-zero trajectory component be
spread over a DVR and replaced, after finite base change, by a proper
semistable model.  If the reviewed arithmetic bridge and an associated-prime
calculation show that the normalization of `H` is a reduced component of the
special fibre, then

```text
p_a(generic) = sum_v g(C_v) + b_1(dual graph) >= g(H) > 0,
```

contradicting rationality of the generic source component.  Equivalently, a
nonzero section of the dualizing sheaf on `H` cannot come from a genus-zero
generic fibre.  This is a concrete, client-specific row-28/39 class, not a
cohomological slogan.  It is robust to extra rational vertical components and
therefore potentially avoids the false general no-merger wording.

### New cross-connection: TD6 divisor atlases as Q8 specialization machinery

The exact tool developed for TD6—source-provenanced localization, original-
row syzygies, Fitting ideals, and recursive rebuild of every denominator
divisor—should be applied to the mixed-characteristic Q8 closure.  Its target
is not a Jacobian compatibility unit but the Fitting/associated-prime data for
flatness, multiplicity one, and horizontal membership of `H`.  The same
source-DAG restoration rules used in AS prevent a contact or full-source row
from disappearing during specialization.  Thus the three lanes share one
proof-carrying component-provenance compiler even though their mathematics
remains distinct.

## Strongest attacks

**Strongest proof attack:** certify `H` as a reduced positive-genus component
of the semistable special fibre of the selected horizontal trajectory closure,
then use the genus ledger to exclude the rational generic trajectory.  Audit
the primitive/infinity composition immediately in parallel; do not wait for
another normalization algorithm.

**Strongest counterexample/falsification attack:** run Boolector and Z3 on the
entire source-frozen accepted-fibre Q5/H6 formula.  On SAT, replay the model
literally and compile the next chronological source layer.  On proof-carrying
UNSAT, extract a symbolic left-null/Fitting obstruction over the whole fibre.
Solver disagreement or unproved UNSAT is a software outcome, not mathematics.

## Software acceleration / decisive experiment

Build one **mixed-characteristic component-provenance compiler** with:

- immutable original rows over the DVR and their mod-127 reductions;
- saturation against localization/chart factors;
- associated primes and Fitting ideals for flatness and multiplicity;
- normalization/conductor and relative-dualizing data;
- a local-to-generic branch map at each reviewed contact;
- exact dependency DAGs, omission controls, and automatic descendant taint.

The decisive first run is only the selected `H` prime.  It should answer:
does the horizontal prime selected by the `R[[w]]` branch specialize with
coefficient one to `H`, and does the semistable genus ledger contain the
positive `H` summand?  This is more valuable than another finite-field fibre
census or higher Hensel order.

## Detailed idea cards (three only)

### Card 1 — Q8 semistable genus composition

- **Dependencies:** hostile-confirmed integrality of `H`; independently
  reviewed positive-genus count; corrected contact bound; exact seven-
  coordinate source membership; arithmetic `R[[w]]` bridge; a proper flat
  closure over the DVR; primitive alternatives and infinity passport.
- **Cheapest discriminator:** at one reviewed rational contact, compute the
  mixed-characteristic local/associated prime selected by the horizontal
  branch and its order along `H`.  Test reduced multiplicity one and whether
  `H` is contained in that horizontal closure.  No global normalization is
  needed before this local generic-point test.
- **Outcomes:** `H` horizontal and reduced permits the semistable genus
  inequality; multiplicity greater than one requires base change and a
  conductor calculation; a different horizontal prime means the current
  bridge does not compose; a purely vertical `H` stops the proof attack.
- **Stop condition:** stop further Q8 point/Hensel expansion if horizontal
  membership fails.  If it passes, concentrate on the proper-model and
  primitive-alternative proof until the chain is reviewed end to end.
- **Expected information gain:** very high.  It either converts the new genus
  theorem into a trajectory exclusion or identifies the single missing
  attachment theorem precisely.
- **Forbidden inference:** `g(H)>0` alone does not exclude a characteristic-
  zero trajectory, prove maximum twelve, landing, a ceiling, or JC2.

### Card 2 — AS full accepted-fibre restoration obstruction

- **Dependencies:** frozen accepted predecessor fibre; exact Q5/H6 source
  formula and all 69 retained higher rows; canonical integer carries; fixed
  nonzero collision; literal nested-source replay; two independent solvers
  and proof/model custody.
- **Cheapest discriminator:** the already-running Boolector/Z3 whole-fibre
  decision.  For SAT, replay every row and carry.  For UNSAT, require a proof
  or independently derive the sparse cokernel/Fitting obstruction.
- **Outcomes:** SAT gives the first honest chronological successor at this
  gate and licenses exactly one next-layer compilation; proof-carrying UNSAT
  kills this entire accepted fibre and should be generalized over its rank
  stratum; disagreement or timeout triggers formula partitioning, not a
  mathematical verdict.
- **Stop condition:** stop point sampling permanently.  Stop this AS branch
  on whole-fibre UNSAT; continue only a source-complete SAT transition with
  preserved collision.
- **Expected information gain:** high and binary at bounded scope; lower
  endgame probability than Q8 because recurrence and algebraization remain.
- **Forbidden inference:** finite-depth SAT is not recurrence, an all-Witt
  lift, bounded support, a characteristic-zero polynomial pair, or a
  counterexample.

### Card 3 — TD6 source-polynomial adjoint, then one `q3` test

- **Dependencies:** duplicated V50 54-row PASS; all 40 current-row replays or
  exact disjoint V56 union; V53 dependency-closed N13/P12 ancestry; genuine
  quadratic and omission controls; actual coefficient/termwise denominators;
  raw `U,H,B3` strata; explicit saturation/Bezout rather than gcd.
- **Cheapest discriminator:** finish the existing jobs.  If the combined
  source identity has only allowed support, independently replay it and then
  add the next licensed matrix-changing term `gamma*t^3` to
  `q=t+beta*t^2+gamma*t^3+t^25`, retaining its direct `q'`, transport, and
  varying-echelon contributions.
- **Outcomes:** a denominator-free/covered `-k/50` identity supports a
  universal telescoping adjoint; a residual divisor becomes a mandatory raw
  stratum; a compatible component is the frozen survivor; an identity whose
  unit varies with `gamma` is sensitivity evidence only.
- **Stop condition:** stop one-by-one modulus enumeration if the `q3` test
  breaks universality or if source closure needs an ever-growing unrelated
  divisor atlas.  Characterize the first survivor instead.
- **Expected information gain:** medium-high structurally, but below the Q8
  composition because its current scope remains a selected normalized TD6
  section.
- **Forbidden inference:** no quotient-field unit, reverse pivot product,
  multivariate gcd, fixed-A3 theorem, or one higher-jet derivative proves full
  TD6, SP-2, landing, a ceiling, or JC2.

## Continue / redesign / stop and allocation

- **Q8 — CONTINUE, sharply concentrate.**  Move from genus computation to
  horizontal-prime/proper-model composition and end-to-end hostile review.
  Pause redundant point counts and high-order Hensel expansion unless they
  answer attachment.
- **AS — REDESIGN around whole fibres, continue the live formula.**  The two
  point deaths forbid survivor extrapolation and further point sampling.
  Keep exact whole-fibre solvers and restoration/Fitting extraction.
- **TD6 — CONTINUE through the current source/denominator gate; then decide.**
  Preserve all shards and both duplicates until exact union/custody.  Do not
  expand moduli unless the polynomial identity survives.
- **Global landing/ceiling reserve — CONTINUE at a fixed small share.**  The
  Q8 result raises the value of complete maximum-twelve routing, but does not
  reduce `G2-PSC`, Sigray landing, `G2-BD`, or a ceiling.

Immediate reasoning/producer allocation: **55% Q8, 25% AS, 15% TD6, 5%
landing/ceiling reserve**.  Revert Q8 capacity quickly if the horizontal-prime
test fails; if it passes, keep the majority on composing and reviewing that
single proof chain.
