# Finite generic verifier and installed-API compatibility code

2026-09-09. DISABLED / UNEXECUTED STATIC SOFTWARE, sole provisional child of the frozen exact-linear implementation. First action22:00:08UTC, targets absent; controlling cap22:18:08UTC, earlier than22:20. No mathematical subprocess, syntax/AST check, import, compile, test, fixture, matrix or actual source data was executed/read. No worker or network activity.

## 1. Delivered scope

Prepared compatibility.py, a generic-only adaptation of the pinned dispatch caller, a disabled registration, a fixed generic.plan.json and a finite validation plan. checker.py, evidence.py, algebra.py, execution_gate.py and probe.py are byte-identical copies. The parent implementation is PROVISIONAL while its FIRST static gate is active; no verdict is assumed or read. An adverse parent review quarantines this sole dependent child. The already accepted generic17m mathematics is retained only at its exact coefficient-ring/degree scope; no new source theorem is asserted.

The harness supplies the explicit missing generic verifier/API tests from the parent plan. It imports neither solver.py nor any actual univariate artifact/receipt, never calls solver.candidate or evidence.verified, and never issues or carries ROOT source acceptance. Its fixtures are separately labeled GENERIC-VERIFIER-TEST/v1, lack producer/input-evidence/ROOT-acceptance fields, and cannot pass the parent's actual-source metadata interface. The final generic receipt explicitly says NOT-ACTUAL-SOURCE.

## 2. Complete finite call inventory

One future successful caller batch has eight CAPRUN child dispatches: five authority refusals, valid sentinel, descendant-RSS/TERM/KILL dummy and ONE mathematical compatibility child. The latter has exactly twelve IN-PROCESS checker.verify calls: five positive identities/separators and seven exact-reason negative controls. It also has exactly two FLINT fmpq_mat constructions of shape2x5 and two rref calls, with23 explicit Python-level fmpq constructions. These are source-level finite counts, NOT observed runs. No217x1638 or217x1856 actual decision matrix is built. Internal native/overloaded operation counts are unspecified.

The separate tiny API matrices use M=((1,2),(2,4)) and augmented order[M|b|I2]. For b=(3,6), subtracting twice row1 from row2 and normalizing the identity pivot gives RREF rows(1,2,3,0,1/2),(0,0,0,1,-1/2), pivots0,3. For b=(3,7), the new row is(0,0,1,-2,1), so elimination gives(1,2,0,7,-3),(0,0,1,-2,1), pivots0,2. Both ranks are2. These fixed hand calculations test the target-column criterion on consistent and separating tiny inputs, not the actual solver. Standalone scalar formatting checks cover0,K,K/2 for K=9007199254740993. The installed constructor, entry assignment/indexing, tuple return, canonical rational-string behavior and module/version are all future empirical checks, not presumed compatible.

## 3. Exact generic fixtures and independent checks

All fixtures retain the fixed nine ids, every dense coefficient slot, the literal complete p(v), row envelopes and guard q=r*h0. The imported checker is unchanged: its independent dense Fraction parser/reduction checks the full217 coordinates or every1638 dual column. Toy coefficients are rational constants inside B; these finite tests do not exhaust nonconstant B coefficient or product-component behavior.

Five positives, seven paired negatives are precisely:

1. UNIT: f1=1, others0,q=r=h0=1, h1=1 passes. Replacing h1 by2 fails the full identity.
2. All-zero-row SEPARATOR: all fi=0,q=r=h0=1, lambda extracts T^0v^0; it passes. Twice lambda preserves every zero column but fails lambda*b=1.
3. Nonzero-row SEPARATOR: f1=T,others0,q=r=h0=1, same lambda passes. Adding the T^1v^0 functional fails the FIRST column with reason dual column annihilation i=0 j=0 ell=0.
4. HIGH-WINDOW/PRECISION: f9=K*T^5,others0,q=T^5,r=T,h0=T^4, h9=T^20/K passes. The positive file is serialized, reread, checked to retain literal K and1/K strings, then passed through the unchanged checker's nested parser and complete identity. This is designed positive parser/identity evidence, not merely harness JSON equality. Adding T^25 to h9 introduces K*T^30 and fails the full high window. Replacing the denominator K by the canonical string for int(float(K)) preserves rational typing but fails the identity; replacing it by a float fails rational-string typing instead. The runtime precondition checks K>2^53 and int(float(K))=K-1, so the rounding control is genuine, not a small-number imitation.
5. ZERO GUARD: all fi=0,q=r=0,h0=1 with zero UNIT witness passes. A purported separator equal to the constant functional annihilates all columns but has target value0 and fails normalization.

Each planned fixture is written exclusively, labeled generic-only, hashed before checking, reread and compared, then rehashed. Negatives must differ from their paired positive object and raise the exact ValueError reason. No fixture exists yet. A failure stops the finite run; a final PASS receipt cannot be emitted on a partial inventory. All twelve calls are in-process verifier calls, not production checker.main evidence, an actual source certificate, or an ideal verdict for the campaign source.

## 4. Authorization separation and caller delta

The new external registration schema is F10-GENERIC-COMPATIBILITY-DISPATCH/v1, with generic_only=true. Its shipped instance is disabled and all worker/cwd/deadline/profile/package fields are null. The caller and harness reject source acceptance fields altogether. The exact pinned generic.plan.json serves as the unchanged helper's argv[2] input and generic admissibility digest. It is a scope specification, not an acceptance, selected point or source artifact.

The harness first invokes unchanged authorize, then verifies generic scope/plan/source pins and ROOT's installed package metadata. Only afterward does it import FLINT, checker, Fraction or the frozen modulus helper and allocate fixtures. It uses only evidence.producer_environment for metadata; evidence.verified is unused. ROOT still owns installed version/module/native closure discovery. Missing/incompatible package behavior fails closed; there is no installer, fallback, retry or alternative engine.

The caller's literal diff is retained. run changes only the generic plan/admissibility and scope payload; the physical checks, full CAPRUN/child vectors, source pre/post hashes, no new inner PGID, exclusive immutable authority, no concurrent writer,16MiB inherited file cap,15-second admission, stored absolute/aggregate cutoffs, post-return deadline test and typed dummy identity/RSS/TERM/KILL cleanup are retained. Main uses the new minimal generic vector and sole mathematical child, not the parent's actual-source solver sequence. CAPRUN sampling/cleanup remains bounded supervision, not a strict real-time guarantee.

The harness argv is compatibility.py AUTHORITY GENERIC_PLAN OUTPUT: sys.argv length4, full Python -I -B vector7. Staged source vector excludes solver.py, actual univariate/normalized inputs, actual receipts and source acceptance. No parent source file or helper was edited.

## 5. Prospective limits and remaining uncertainty

Only as UNMEASURED initial recommendations: probe5wall/3CPU/2GiB; dummy5wall/3CPU/32MiB; the one compatibility child180wall/170CPU/2GiB;16MiB files; aggregate240wall. All actual caps, worker, exclusive cwd, UTC cutoffs and outer stop duty must be selected and issued by ROOT later. No time/RSS/bytes/installed-API result is inferred from these proposals. Any cap or unexpected result is STOP/NONDECISION without escalation.

The sole API document consumed is the already pinned root note of https://python-flint.readthedocs.io/en/latest/fmpq_mat.html . No new web call or package inspection occurred. The parent code remains provisional. Passing a future generic batch would establish only this finite test inventory and installed API compatibility; it would not run or decide the actual source matrix, bless source provenance, cover all possible coefficients, or authorize the subsequent actual solver.

## 6. Documentary custody and completion

Exactly11 permitted inputs were pinned before use: parent report/plan, eight parent code files, and ROOT API note. WHOLE report/plan/note reads were made this turn; the eight same-byte WHOLE code reads from the preceding task were reused after fresh matching pins. No original13-input provenance expansion, new gate/peer/ledger/body, actual artifact/receipt, protected tree, AWS/SSH/proc or external agent was accessed. All edits used apply_patch in the new box/private report partial; only documentary metadata and existing transaction tooling ran.

Own new sources/plan/docs are read WHOLE as text, copied files cmp-matched and literal caller pins checked. These are static documentary checks, not executed predicates. One own-only raised-OPEN/quantity/cheapest-test and collision check precedes marker/close/finalize/expected verification. Custody binds every current input and owned final file except itself. All writers IDLE at final handoff before22:18:08UTC.

## OPEN(S) RAISED

One explicit runtime-validation obligation remains: the finite generic/API batch is prepared but entirely unexecuted. Quantity: one separately registered batch with seven preflight children plus one harness, twelve verifier calls and two tiny rref calls. Cheapest next step is a static delta review, conditional on the parent software gate, followed only by ROOT-assigned environment pins/caps/authority. No new campaign OPEN identifier, exit-price declaration, performance guarantee or runtime readiness claim.

## COLLISIONS

status: EMPTY

- NONE. Own targets absent before begin; own-only check, no corpus scan.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `9784`.
- Body SHA-256:
  `d03012681b3538716412d55837b0f6a4010f30610fd9eb6d678199da972e9815`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
