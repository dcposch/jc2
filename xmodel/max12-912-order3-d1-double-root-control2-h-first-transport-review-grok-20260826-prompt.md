You are the hostile independent exact-algebra/tropical referee for the first
transverse-cusp transport of an explicit D1 witness in the plane
Jacobian-conjecture campaign.  This is a text/source/custody review only.  On
the local Mac do not run Singular, Sage, msolve, gfan, Lean, Python algebra,
or any solver.  You may inspect text/JSON, recompute SHA-256, use
cmp/diff/sed/jq, and do hand rational arithmetic.  If substantive exact
computation is required, fail closed and request an AWS replay.

Review the complete frozen package

```text
cases/max12_912_order3_d1_double_root_control2_h_first_transport_v4_20260826/
```

Required anchors:

```text
6ddb1343c7b30d0e4e6daf7e5f13ea364205e1a776c60534d02da26a7c529f14  RESULT.md
0ff05368ab7a5751617db6a6b9a31e67545439ed5fb688cb15238ef879637b68  FREEZE.sha256
93dd031f5d523085d166d7382490f05652261af1ccd281a51aeb88994baeb425  each h_transport.json
```

The `h=0` prerequisite is the reviewed q2 lift:

```text
2a2ff048c299a8878ea28211b72f0d0ad98f27216ae5c3462eb3bc076083ac7d
  xmodel/max12-912-order3-d1-double-root-control2-q2-syzygy-lift-review-grok-20260826.md
```

Charges:

1. Recompute every frozen hash.  Audit the two preregistered AWS hosts/tags,
   PIDs/caps, pre-GO custody, source closure, opposite traversal orders,
   return codes, empty stderr, RSS/swap, literal certificate equality, and
   all reported canonical hashes.
2. Trace the exact source construction with `p=-3,c=2+h`, fixed
   `a=1,k=nu=0,mu=2/3`, full Q/R, all eight charged rows, and corrected
   multipliers `F1+q2/12,F2-q2/9`.  Confirm the specialization at `h=0` is
   literally the reviewed 37-term `W'` with SHA
   `ddb451c4d591f312a27b34ba517235a684c53f2884040624abe704ac243ab33b`,
   and charge the 98/61 term counts and correction SHA.
3. Audit every recorded weight difference on
   `alpha=15/2,beta=5+u,delta=5+v` and the claimed uniform eta threshold.
   Prove or refute the exact **if and only if**: the unchanged corrected
   witness has unique least term `la^20` for every `u,v>0,T>0` iff
   `eta>=15/2`.  Charge all 98 terms, not just the displayed extremals.
4. Inspect the eight displayed h-linear extremals and their coefficients.
   Verify that at `eta=15/2` their margins are respectively `v` or `u`, so
   equality at the closed corner becomes strict throughout the open
   quadrant.  Conversely, for every `0<eta<15/2`, verify that sufficiently
   small positive u or v makes an actual present term fall below target.
   Check the reported counts of 50 potentially bad correction terms and 28
   closed-corner threshold monomials.
5. Audit the ordinary row-homogeneity checksum and the displayed unit-axis
   covariance only as a checksum.  Do not promote it to a moving-axis
   theorem without all target/load transformations being independently
   proved.
6. Enforce scope: `OLD_CORRECTED_WITNESS_DOES_NOT_TRANSPORT_UNIFORMLY` below
   the threshold is a negative result about this witness, not a surviving
   branch or no-lift theorem.  No low-eta, corrected h-witness, moving-load,
   formal-arc, whole-fan, D1, or JC2 conclusion is licensed.

Write exactly one review to

```text
xmodel/max12-912-order3-d1-double-root-control2-h-first-transport-review-grok-20260826.md
```

Do not edit producer files, top-level files, or another report.  End with
exactly one of `H_FIRST_TRANSPORT_CONFIRMED`, `H_FIRST_TRANSPORT_REPAIRED`, or
`H_FIRST_TRANSPORT_REJECTED`.
