# Hostile review — unloaded K00 "normal cone" quartic (ideation §1)

Reviewer: Fable 5, different-model hostile lane.
Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`.
Target: Section 1 of `xmodel/ideation-20260829T2254Z-opus5.md` and its exact
dependents (§1.5, §1.8, §4.1–4.2, Cards A/B insofar as they consume §1).
Lifecycle of this report: `HOSTILE_REVIEW / NO PROMOTION BY ITSELF`.

## 0. Custody

All recomputed by me before use; all match the producer's declarations.

```text
d8e7328479232094b1a77ec8560d85e0a70e4288b18e49d506602bd746d6aa36  producer full file
2b9e2f08f4e58252d984944a34e9d78fa5b596bfe1a350aadc073ed39d393b30  producer body (34609 bytes)
5b0a77e6d16df30de2df344862fc4316aedcc8aeabf1ec81e23d7881ce67ee7a  prelude_Q.sing (the seven literal rows)
96d88f49fd8e2dab1b05d18ac7912d7c1e3374fc474dcfafce29a08558edb62b  radical producer (body 41d6460a...)
e3e84ac9a9aba58fcff731c0b75a279b8f7d62ce252e91253a708234bebcab08  integration note, full-file hash as cited
64eaafe3fe5aaa9238a338f2da045e302d9a239ac9c29312e39d5c545893d42c  allfaces audit at basis (source of the frozen e=3,m=1 fixture)
```

Working tree at the basis for every file consumed. I read no Opus scratch,
script, or log; every object below was rebuilt from the literal prelude rows
in Singular 4.4.1 (arm64-Darwin) under fresh scripts. Every promotion-bearing
run below has zero `?`-error lines (two early scripts hit Singular 4.4
reserved-name/syntax quirks; both were discarded and rerun clean, and one
intermediate GB transcribed out of a partially errored run was re-derived and
re-verified two-sided inside a clean run before use, §3.3).

Budget ledger: every individual promotion-bearing run finished in ≤ 9 s CPU
except (i) one optional refinement (annihilator locus, §2.3) at 86 s, over
the 60 s guideline and reported as such, and (ii) one abandoned `minAssGTZ`
of the saturated family, killed at 300 s and **superseded** by a cheap slice
argument (§4.2). Total productive CPU ≈ 105 s, RSS far under 1 GiB. No AWS
launch is made or required (§9).

## 1. Attack 1 — coordinate change and containments: CONFIRM

With `f0=d0-2d4-d4^2`, `f1=8d1-(1+d4)d3`, `f2=d2-d4-16d3^2`, `f5=d5-2d3`
(byte-matching the sealed radical producer), the maps

```text
phi: d0->e0+2S+S^2, d1->(e1+(1+S)T)/8, d2->e2+S+16T^2, d3->T, d4->S, d5->e5+2T
psi: e0->f0, e1->f1, e2->f2, e5->f5, S->d4, T->d3
```

were machine-verified mutually inverse in both directions (`phi(f_a)=e_a`
and `psi(phi(d_k))=d_k`, all components). So `(f0,f1,f2,f5,d4,d3)` is a
polynomial coordinate system, exactly as claimed.

All **seven** rows, individually: e-degree-0 part `=0`, e-degree-1 part
`=0`, e-degree-2 part `!=0`. Hence `I subset J^2` and each row lies outside
`J^3`, so `I not subset J^3`. The d-side degree-0 and degree-1 jets of every
row also vanish, and the campaign's seven leading quadrics equal
`Q_i(lin_e(d);0,0)` verbatim (all seven compared, exact). The identities
`f0-4f2 = B(q)-d4^2+64d3^2` and `f5+2f1 = A(q)-2d3d4` hold exactly with the
campaign's `A(q)=16d1-4d3+d5`, `B(q)=d0-4d2+2d4`.

Additional exact fact (new, small): the kernel of the linearised normal
projection `lin_e` is precisely the tangent plane `ell(s,t)`, so `lin_e`
identifies `Q^6/ell` with the normal `e`-space.

## 2. Attack 2 — the three objects: the central refutation

### 2.1 The seven-tuple `Q` (CONFIRM)

The images of the rows in `J^2/J^3 ~= Sym^2(J/J^2)` (identification valid:
`J` is generated by part of a coordinate system, a regular sequence) are
seven quadrics `Q_i in Q[S,T][e0,e1,e2,e5]`, recomputed independently. The
`O_D`-span of the `Q_i` is generator-independent; this object — the **second
normal jet** of the rows along `D` — is well defined, exactly as §1.2 says.

### 2.2 The full initial ideal `in_J(I)`: strictly bigger — REFUTE the identification

I computed `in_J(I)` by the Rees degeneration: `G_i = t^{-2} r_i(t e)`
(division exactness checked), saturation by `t` (quotient chain, stable
after 2 steps), then `t=0`. Output: 114 generators, all e-homogeneous;
`(Q) subset in_J(I)` holds, but **100 generators lie outside `(Q)`** (41
cubic, 38 quartic, 21 quintic in `e`, in the computed generating set). The
seven rows are **not** a filtered standard basis.

Much stronger, and decisive:

```text
(e0,e1,e2,e5)^5 subset in_J(I),   sharp at 5 (34 of 35 degree-4 e-monomials
                                   lie outside I+J^5; gr_4 != 0).
```

Verified **twice**, once through the degeneration and once with no t-machinery
at all: every one of the 56 degree-5 e-monomials reduces to zero against
`std` of the row ideal in split coordinates (0.1 s), while
`e0^4, e1^4, e2^4, e5^4` all have nonzero normal form. Transporting through
the automorphism `psi`:

**New sharp theorem (reviewer-found).** `J^5 subset I subset J^2`, both
containments sharp (`f_a^4 not in I` for every `a`; `I not subset J^3`).

Consequences:

1. `sqrt(I)=J` gets a one-line independent desk reproof (`I subset J` and
   `J^5 subset I`), making §1.7's contingency planning moot and replacing
   the AWS-scale quintic-identity route for the radical *statement* (the 28
   multiplier certificates remain the only explicit `f_a^5` witnesses).
2. The proposed `J^17 subset I` corollary (kept open by the integration
   note) is superseded by the sharp exponent 5.
3. `gr_J(Q[d]/I)` has its irrelevant ideal nilpotent, so
   **`Proj(gr_J(Q[d]/I))` is the empty scheme.** The projectivised normal
   cone of `V(I)` along `D` is empty over *every* `(S_0,T_0)` — off `Delta`
   and on it.

Therefore the theorem's second sentence, "Equivalently, the projectivised
normal cone of `V(I)` along its reduced support `D` is empty over exactly
the complement of the plane quartic `Delta=0`", is **REFUTED**, not merely
unproven: the emptiness locus of the true projectivised normal cone is all
of `A^2`, not the complement of `Delta`. The producer computed the right
seven-quadric object and misnamed it; nothing they computed distinguishes
`(Q)` from `in_J(I)`, and the distinction is fatal to the "equivalently".

### 2.3 Where the two objects differ (refinement)

Over `Q(S,T)` the two ideals coincide: `in_J(I)` has the same generic
Hilbert data (dim 0, vdim 16, h-vector `(1,4,6,4,1)`), so with
`(Q) subset in_J(I)` they are equal after localization. The failure module
`in_J(I)/(Q)` is torsion; for the first extra (cubic) generator `g` I
computed the annihilator exactly:

```text
((Q):g) cap Q[S,T] = (Delta)      [86 s CPU, over guideline, reported]
```

So `Delta*g in (Q)`, `g not in (Q)`: the standard-basis failure is supported
**precisely on the quartic**, which is why the seven-quadric locus and the
true cone agree off `Delta` and disagree fatally on it.

## 3. Attack 3 — the quartic and the iff: CONFIRM_WITH_CORRECTIONS

### 3.1 The elimination (CONFIRM, exact)

Saturation of `(Q_1..Q_7)` by `(e0,e1,e2,e5)` stabilizes after 5 quotient
steps; the elimination ideal is principal and equals `(Delta)` exactly, both
containments as ideals (Singular prints the sign-flipped generator
`432T^4-S^3-72ST^2-S^2-64T^2`). Elimination *without* saturation is the zero
ideal — correct, since the zero section makes the affine projection
surjective; the saturation is what removes it.

### 3.2 Why it is an iff, over every field (the gap the producer left)

The producer asserted the iff with no properness or specialization argument.
The statement is nonetheless **true**, and I certify both directions in a
specialization-safe way that needs no citation:

- **Nonzero cone point ⇒ `Delta=0`.** Explicit membership certificate:
  `Delta * m in (Q_1,...,Q_7)` over `Q[S,T,e]` for **all 56** monomials `m`
  of e-degree 5, verified by reduction; the exponent 5 is minimal
  (membership first appears at quotient step 5). These are polynomial
  identities, hence valid over every `Q`-algebra: a common zero `nu` with
  `Delta(S_0,T_0) != 0` forces `nu_a^5=0` for all `a`, i.e. `nu=0`.
- **`Delta=0` ⇒ nonzero cone point over `Kbar`.** Explicit witness section

```text
nu(l) = ( -192(l^3+24l),  l^4-1728,  32(l^3+54l),  864 ),
S(l)=(l^4+24l^2-432)/432,  T(l)=(l^3+36l)/432,
Q_i(nu(l); S(l),T(l)) = 0 identically in Q[l], all seven i,
```

  machine-verified. The fourth entry is the constant `864`, so `nu(l_0)!=0`
  for every `l_0` in every field of characteristic zero. The projective
  parametrization `[S^:T^:Z^]` is base-point-free (`(l0,l1)^5` inside the
  coordinate ideal, verified), satisfies the homogenized `Delta`-identity,
  and is birational onto the projective quartic (the fibre ideal in
  `Q[l,m]` decomposes as the diagonal plus the single zero-dimensional pair
  identifying the two node preimages `l=+-6i`). A projective morphism from
  `P^1` onto the irreducible quartic is surjective on `Kbar`-points, and
  only `l=infinity` maps to the point at infinity, so every affine
  `Kbar`-point of `Delta` is `(S(l_0),T(l_0))` for an affine `l_0`, and
  `nu(l_0)` is its witness.

  (The classical projective-extension theorem plus flatness of field
  extensions gives the same conclusion; the certificates make it
  self-contained.)

- **Independent method** (attack-8 requirement): four affine-chart
  eliminations `e_a=1`. Each chart elimination is principal, contained in
  `(Delta)`, and `Delta` lies in the radical of their intersection — so each
  chart closure is exactly `V(Delta)`, an algorithm-independent crosscheck.

- **Point controls** (all match): `Delta`-values and fibre dimensions at
  `(0,0)`:2, `(-1,0)`:1, `(4,1)`:1, `(1,0),(2,1),(3,-2),(-1,1),(5,3)`:0,
  the two extra rational curve points `(-407/432,37/432)` and
  `(-20/27,5/27)`:1, the cusp `(-4/3, w/9)` over `Q(w)/(w^2+3)`:1, and the
  off-curve `Q(i)`-point `(0,i/8)`:0.

### 3.3 Structure of the generic fibre (new sharpening)

In a clean self-contained run: the saturated fibre over the generic point of
the curve has dim 1 and multiplicity exactly 4, and is sandwiched two-sided
between the line

```text
L(l): 27e2=(l^3+54l)e5,  864e1=(l^4-1728)e5,  972e0=2592l*e1-108l^2*e2+l^5*e5
```

and its fourth powers (`sat subset L`, `L-gens^4 subset sat`). So the
reduced degenerate cone at a generic `Delta`-point is a **single line,
rational over the function field of the curve**, carried with scheme
multiplicity 4 by the seven-quadric system.

### 3.4 Corrections to §1.3

The iff itself: true, now certified (the filed proof was incomplete — no
specialization or properness step). The clause naming the normal cone:
refuted (§2.2). The maximum safe statement is in §8.

## 4. Attack 4 — generic Artinian data: CONFIRM (strengthened)

Over `Q(S,T)`: dim 0, vdim 16, kbase degrees give h-vector `(1,4,6,4,1)`,
socle degree 4 — all as filed. Sharpening: `interred` shows the seven
quadrics span **exactly** a 4-dimensional space of quadrics; four spanning
ones generate the same ideal (two-sided reduction) and have dim 0, hence
form an h.s.o.p. of the Cohen–Macaulay ring `Q(S,T)[e]`, hence an honest
**regular sequence of four quadrics generating the ideal**, forcing the
`(1+t)^4` Hilbert series. So "has the length of a regular sequence of four
quadrics" is correct and undersells: the regular sequence exists inside the
span (the seven generators themselves are of course not one). Field and
closure language in the theorem is correct as written. Bonus: the *true*
`gr` generic fibre has the same invariants (§2.3), so these numbers survive
the §2 retyping.

## 5. Attack 5 — the curve `Delta`: CONFIRM (every item)

- Irreducible over `Q`, exponent 1; geometrically irreducible via the
  parametrization (image of `P^1`).
- Singular scheme: dim 0, vdim 5, minimal primes exactly `(S,T)` and
  `(3S+4, 27T^2+1)` (two-way reduction against the literal claims).
- Node `(0,0)`: milnor = tjurina = 1, tangent cone `S^2+64T^2`
  `=(S+8iT)(S-8iT)` — ordinary node, only `Q`-rational singular point.
- Cusps at `(-4/3, +-sqrt(-3)/9)`: milnor = tjurina = 2 (A2), conjugate over
  `Q(sqrt(-3))`; Tjurina lengths `1+2+2=5` reconcile the vdim.
- Geometric genus 0 (`normal.lib`), and the bookkeeping `p_a=3` minus
  `delta = 1+1+1` is valid because the projective closure is smooth at its
  unique infinite point `[1:0:0]` (linear part `Z`), where the line at
  infinity has contact order exactly 4 (four-fold tangent), and there are no
  other points at infinity (`-432T^4`).
- Parametrization: identity `Delta(S(l),T(l))=0` exact; `l=0 -> (-1,0)`;
  `l=+-6i ->` node (both, conjugate over `Q(i)`, and these are the only
  non-diagonal identifications, §3.2); `l=+-2sqrt(-3) ->` the cusps
  (verified as `l=2w`, `w^2=-3`, giving `(-4/3, w/9)`).
- Branches at the node: substituting `S=+-8iT-4T^2+-4iT^3` leaves residual
  of order **9**, so the displayed three terms are exact and the producer's
  `O(T^4)` is conservative — the branches match their truncations to
  `O(T^8)`.
- Pointwise cone dimensions: all sampled values confirmed (§3.2), **and
  upgraded to a complete classification** (§4.2 below / attack 5's
  "pointwise cone dimensions" item): dimension 2 exactly at the node, 1 at
  every other point of `Delta`, 0 elsewhere, over every characteristic-zero
  field. No affine/projective or arithmetic/geometric-genus misuse found;
  the producer's cone dimensions are affine dimensions in `A^4` and are used
  consistently.

Uniqueness proof for the dim-2 point (new, cheap, replaces the timed-out
decomposition): if a fibre has affine dimension ≥ 2 its projectivization
has dimension ≥ 1 and meets every hyperplane; for the two hyperplanes
`e0-7e1+3e2+2e5` and `5e0+e1-11e2+e5` the sliced-and-saturated eliminations
intersect in the reduced single point `(0,0)` (vdim 1). With the witness
giving dimension ≥ 1 everywhere on `Delta`, the classification is complete.

## 6. Attack 6 — campaign interfaces: CONFIRM_WITH_CORRECTIONS

Confirmed exactly, from my own `Q_i` (never from Opus output):

- `ell(s,t)=(2s,t/8,s,t,s,2t)` is the tangent plane (jets of `D(S,T)`), is
  the `u=v=0` locus of the campaign cone `x(a,b,u,v)`
  (`ell(s,t)=x(t/8,s,0,0)`), and `A(ell)=B(ell)=0`.
- `lin_e(x)=(4u,-v,u,2v) in P0`; tangential part `(d4,d3)(x)=(b-u,8a+v)`;
  `A(x)=B(x)=0` identically; `{A=B=0}` is the `lin_e`-preimage of `P0`.
- At the node the seven quadrics vanish **identically** on
  `P0={e0=4e2, e5=-2e1}` (rowwise), and `P0` is the exact reduced support
  (one minimal prime, two-way match).
- Polar 7x4 matrix at `nu_x`: rows 4 and 6 identically zero; row 1 equals
  `(-3/1024 v, 3/512 u, 3/256 v, 3/1024 u)`; every row kills `P0` (descent
  to `A^4/P0` valid); 16 nonzero 2x2 minors, each a rational multiple of
  `u^2+64v^2`, and the minors **ideal** equals `(u^2+64v^2)` exactly.
- The full published alpha/beta table (all seven rows of the sealed r=2
  rank-fan producer, including `alpha_2=(3/256)v, beta_2=(3/16384)u` and
  rows 5,7) is re-derived: `grad Q_i(nu_x) . lin_e(q) = alpha_i A(q) +
  beta_i B(q)` holds identically for `i=1..7`.

**Correction (wording, binding).** The trap paragraph's literal sentence
"the normal part is `(f0,f1,f2,f5)=(4u,-v,u,2v)`" is false as stated:
machine expansion gives `f0(x)=4u-(b-u)^2`, `f1(x)=-v+(u-b)(v+8a)`,
`f2(x)=u-16(v+8a)^2`, and only `f5(x)=2v` is exact. The true statement —
used correctly elsewhere in §1.5 — is that the **linearised** normal part
`lin_e(x)` is `(4u,-v,u,2v)`.

**The "64 coincidence": producer's caution CONFIRMED, its "coincidence"
reading superseded.** A coordinate mutation `v -> w/2` turns the fan into
`u^2+16w^2` while the campaign-pinned tangent cone stays `S^2+64T^2`, so
there is no basis-invariant numerical identity, and refusing to identify
the two loci was right. But the deeper link is a theorem, not a
coincidence: the unique generic degenerate line (§3.3) extends to a
morphism `P^1 -> P^3` (its witness never vanishes), and its values at the
node preimages are

```text
[nu(+-6i)] = [(+-13824i, -432, +-3456i, 864)] in P0,  (u,v)=(+-3456i, 432),
u^2+64v^2 = 0,  u = +-8i v,
```

sign-paired with the branch slopes `dS/dT = +-8i` at `l=+-6i` (both
machine-verified). So the flat limit of the degenerate line along either
`Delta`-branch is exactly the rank-one ray of the campaign fan with the
matching sign. Distinct objects, canonically incident. Flag/place/series
discipline is maintained: this identifies a **limit incidence**, not the
loci.

## 7. Attack 7 (optional) — the `e=3,m=1` client, executed on frozen data

The fixture is basis-frozen (allfaces audit `64eaafe3...`, eq. 4.4):
`S=8i*tau-4*tau^2`, `T=tau`, `d=D(S,T)+tau^3 w+tau^4 v+tau^5 z` with
`w=(16i,0,0,1,-8i,4)`, so the tangential trajectory is
`d4(tau)=8i*tau-4*tau^2-8i*tau^3`, `d3(tau)=tau+tau^3`. Card A's
discriminator, computed exactly over `Q(i)`:

```text
Delta(d4(tau),d3(tau)) = 320*tau^4 + 2560i*tau^5 - 3904*tau^6 - 2496i*tau^7
                         - 2112*tau^8 - 64i*tau^9 - 1728*tau^10 - 432*tau^12
ord_tau = 4.
```

With `m=1` the generic (off-tangent-cone) contact would be 2 and true
branch contact would be ≥ 8 (§5): the outcome is the producer's
pre-typed **"contact strictly between"** case. In branch coordinates the
fixture reads `d4 = 8i*d3 - 4*d3^2 - 16i*d3^3 + O(d3^4)`: the §1.8(c)
series is **refuted at the `d3^3` coefficient** (`-16i` observed vs `+4i`
predicted; deviation `-20i*tau^3`). The weak mechanism survives with new
support: the fixture's tangent direction is the branch direction, its
contact exceeds the generic value, and — new — its leading normal deviation
`lin_e(w)=(32i,-1,8i,2)` lies in `P0` **exactly on the fan ray `u=+8iv`**,
the same ray my limit theorem pairs with its branch. Per the prompt this
cannot promote the cone, and no post-cutoff G9 artifact was consumed.

## 8. Attack 8 — mutations and independent methods

- **M1 (quadric-level elimination mutation, completes the control the
  producer's §1.6 could not finish):** `Q4 -> Q4 + (1/1024)S^2 e5^2` gives a
  principal elimination of degree **16**, not equal to and not divisible by
  `Delta` (8.6 s). The pipeline is sensitive; `Delta` is a property of the
  rows. Note the producer's own row-level mutation `r4+(1/1024)d4^2d5^2`
  destroys `I subset J^2` (constant part `S^2T^2/256` on `D`), so their
  timed-out "mutated saturate-and-eliminate" was chasing a type-invalid
  object; the honest control lives at the quadric level and passes.
- **M3 (replication of the producer's pointwise control):** at `(-1,0)` the
  mutated pointwise system drops dim 1 -> 0; at `(1,0)` it stays 0. Their
  §1.6 claims replicate exactly.
- **M2 (transcription guard):** `r1 -> r1+(1/512)d5` is detected instantly
  at the `I subset J^2` gate (constant `T/256`, linear `e5/512`).
- **M4 (drop-row):** removing `r7` leaves the elimination equal to
  `(Delta)` — row 7 is redundant for the leading locus. Caution filed:
  `Delta`-invariance is not row-custody; single-row damage need not move
  the quartic.
- Independent exact methods used: the t-free `(e)^5 subset I` verification
  (§2.2), the 4-chart elimination (§3.2), the witness section (§3.2), and
  the hyperplane-slice classification (§5) — none shares an algorithmic
  path with the producer's single saturate-and-eliminate.

## 9. Itemized verdicts

| # | Producer claim (§) | Verdict |
|---|---|---|
| 1 | Triangular coordinate change; `I subset J^2`; `I not subset J^3` (§1.1–1.2, all seven rows) | CONFIRM |
| 2 | Seven quadratic initial forms well defined in `J^2/J^3=Sym^2` (§1.2) | CONFIRM |
| 3 | Generic `dim 0, vdim 16, socle 4, h=(1,4,6,4,1)`; four-quadric regular-sequence length (§1.3) | CONFIRM (strengthened: an actual generating regular sequence of four quadrics exists over `Q(S,T)`) |
| 4 | Principal elimination `= (Delta)`; iff for the seven quadrics over every char-0 field (§1.3, sentence 1) | CONFIRM_WITH_CORRECTIONS (true; filed without the needed specialization/properness step; now certified both directions, §3.2) |
| 5 | "Equivalently, the projectivised normal cone of `V(I)` along `D` is empty over exactly the complement of `Delta=0`" (§1.3, sentence 2) | **REFUTE** — `(e)^5 subset in_J(I)`, `Proj(gr)` is empty over every point; the seven rows are not a filtered standard basis (100 extra initial forms; failure module annihilated exactly by `Delta`) |
| 6 | `Delta` curve package: irreducibility, sing scheme, node/cusps, genus 0, parametrization, infinity (§1.4) | CONFIRM (branches exact to `O(T^8)`, better than claimed) |
| 7 | Pointwise cone dimensions 2/1/0 (§1.4) | CONFIRM, upgraded to a complete all-fields classification with the node as unique dim-2 point |
| 8 | `P0` support and identical vanishing (§1.4) | CONFIRM |
| 9 | Interface identities: `ell`, `A/B`, four-plane preimage, polar rows, alpha/beta table, minors `=(u^2+64v^2)` (§1.5) | CONFIRM |
| 10 | Trap sentence "(f0,f1,f2,f5)=(4u,-v,u,2v)" (§1.5) | CONFIRM_WITH_CORRECTIONS — literally false; must read `lin_e(x)` |
| 11 | "Shared 64 is a coefficient coincidence unless someone proves otherwise" (§1.5) | CONFIRM_WITH_CORRECTIONS — non-identification stands; the "otherwise" is now proved: flat-limit incidence of the degenerate line with the sign-matched fan rays (§6) |
| 12 | §1.6 controls and reported budget failure | CONFIRM (replicated; the missing elimination-level mutation control supplied at the correct object level) |
| 13 | §1.7 scope/firewall | CONFIRM (contingency on the radical review is now moot: independent reproof of `sqrt(I)=J`) |
| 14 | §1.8(a) recentering bound; §1.8(b) node explanation | CONFIRM after retyping "leading normal analysis" to the second-normal-jet system; (b)'s uniqueness now proved, not sampled |
| 15 | §1.8(c) branch-contact series prediction | REFUTE (strong form) on the frozen fixture: contact order 4, first failure at expansion order 3; weak nodal-tangent mechanism supported, with the fixture's normal deviation exactly on the matching fan ray (§7) |
| 16 | §4.2 sentence "`Delta` is the locus over `D` where the fibre of `Proj(gr_J(Q[d]/I))` is nonempty" | **REFUTE** (that locus is empty); the honest Rees-grade invariant this round actually yields is `J^5 subset I subset J^2` |

## 10. Binding corrections

1. Strike "projectivised normal cone of `V(I)`" everywhere it is claimed to
   have `Delta` as its nonemptiness locus (§1.3 sentence 2, §4.1 name,
   §4.2 dictionary). The licensed object is the **seven-quadric second
   normal jet**; the true `Proj(gr)` is empty (`J^5 subset I`).
   `NORMAL-CONE-ATLAS` should be renamed (e.g. `NORMAL-JET-ATLAS`).
2. §1.3's iff must carry a specialization argument; the certificates of
   §3.2 (membership exponent 5; witness `nu(l)` with constant entry 864;
   parametrization surjectivity) are now on file and make it
   field-uniform.
3. §1.5 trap paragraph: replace the literal `(f0,...,f5)(x)` claim by
   `lin_e(x)`.
4. §1.8(c)'s displayed series is falsified on the frozen fixture; any
   consumer must use the corrected fixture expansion
   `d4=8i d3-4d3^2-16i d3^3+O(d3^4)` and the measured contact order 4.
5. Card A's "high/low/between" outcome is decided: **between**, at order 3.
   Card B step (i) as posed is nearly decided by the frozen audit's own
   leading restrictions (degrees 1–3 are below `deg Delta = 4`, so
   divisibility fails for every block with a nonzero low-degree component);
   its informative residue is step (ii).

## 11. Maximum theorem safe for promotion

Exactly this, and no more (all objects literal, all certificates on file):

**Theorem (K00-NORMAL-JET-QUARTIC, repaired).** Let `Q_1..Q_7` be the images
of the seven frozen unloaded rows in `J^2/J^3 ~= Sym^2(J/J^2)`, written in
the split coordinates of §1. Set
`Delta = S^3+S^2+72ST^2+64T^2-432T^4`.

1. For every characteristic-zero field `K` and `(S_0,T_0) in K^2` there is a
   nonzero `nu in Kbar^4` with `Q_i(nu;S_0,T_0)=0` for all `i` **iff**
   `Delta(S_0,T_0)=0`. (Certificates: `Delta*(e)^5 subset (Q)`;
   witness `nu(l)` with constant fourth entry; birational base-point-free
   parametrization of the quartic.)
2. The common-zero cone has dimension 2 exactly at `(0,0)` (where its
   reduced support is the plane `P0`), dimension 1 at every other point of
   `Delta` (reduced fibre a single line, generically carried with
   multiplicity 4 and rational over the function field), and is the origin
   elsewhere.
3. `Delta` is an irreducible rational quartic with one node at the origin
   (tangent cone `S^2+64T^2`), two `Q(sqrt(-3))`-conjugate cusps, a smooth
   four-fold tangent at infinity, and the stated parametrization; the nodal
   branches agree with `S=+-8iT-4T^2+-4iT^3` to `O(T^8)`.
4. The flat limit of the degenerate line at the node along the branch of
   slope `+-8i` is the campaign fan ray `u=+-8iv` in `P0`.
5. Separately: `J^5 subset I subset J^2`, both sharp; hence `sqrt(I)=J`,
   `Proj(gr_J(Q[d]/I))` is empty, and the seven rows are not a filtered
   standard basis, with `in_J(I)/(Q)` torsion annihilated by `Delta` at its
   first extra generator.

Items 1–3 confirm and complete the producer's §1.3–§1.4 at the corrected
type; items 4–5 are reviewer-found. §1.8(a)/(b) survive verbatim under the
retyping; §1.8(c) survives only in weak form.

## 12. Next cheapest discriminators

1. **Fixture contact census (desk, minutes each).** Compute
   `ord_tau Delta(d4,d3)` for the other basis-frozen survivors (the
   `e=2,m=1` prefix cells). If contact 4 recurs, the "between" outcome is
   structural and the deviation order 3 should be matched against the K10
   arrival grade (the audit's "K10 first reaches this fixture at G9").
2. **Card B step (ii) for `A10` only (desk).** Step (i) is already
   negative on degree grounds from the frozen audit rows; (ii) — whether
   the common zero locus of `A10|_D` meets `Delta=0` beyond the origin —
   is a small resultant computation and is the surviving content of the
   card. (Noted in passing: `A6_2|_D` is proportional to `S^2-64T^2`, the
   sign-flipped node tangent cone; untyped, not consumed.)
3. **Multiplicity-4 mechanism (desk).** Decide whether the generic
   multiplicity 4 of the degenerate line equals the local intersection
   multiplicity of `Delta` with a generic pencil through the fibre, i.e.
   whether the scheme structure remembers the quartic's degree.

## 13. AWS

No AWS packet is required: every verdict above is decided at desk scale.
The only computation abandoned for budget (full `minAssGTZ` of the
saturated seven-quadric family, killed at 300 s) is **superseded** by the
slice classification of §5; if the coordinator nevertheless wants the
component census, the job is: r6b, Singular 4.4.1, input `Q_1..Q_7` as
serialized here, quotient-chain saturation by `(e)`, `minAssGTZ`, expected
well under one node-hour — it can change no verdict in this report.

## 14. Nonclaims and guardrail compliance

No exit-price assertion is made, so no `charge_basis` line appears. Nothing
here concerns the loaded source, which does not set `I=0`; nothing is an
arc, germ, occurrence, attainment, map, counterexample, or JC2 result. Set
equality is not scheme equality — this review's own findings (the
multiplicity-4 line, the nilpotent normal cone) are the live illustration.
The tangential quartic and the normal fan remain distinct objects; §6
proves an incidence between them, not an identity. Agreement with the
producer was never treated as proof: every confirmed number above was
recomputed from the literal prelude rows, and the two refuted claims were
refuted by construction of explicit certificates.

<!-- BODY-END -->
