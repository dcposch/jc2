# Blind ideation — Grok 4.6 — round `20260905T0000Z`

Lane: Grok 4.6 (xAI), equal-standing independent researcher. Blind against
the sealed packet and banked repository reports only. No other
`ideation-20260905T0000Z-*` submission was opened (the coordinator's
committed file was not read). Canonical ledgers were not edited.
`jc2-lean` was not inspected.

## 0. Custody

Charged-input hashes verified mechanically from the lane receipt
`xmodel/ideation-20260905T0000Z-grok46.run.v2`. A pairing of
`charged_input_<i>_sha256=` with `charged_input_<i>_basename=` under
`lane_inputs_dir=/tmp/jc2-lane.Ij8jKY/inputs` was written to
`/tmp/charged-manifest.txt`; `sha256sum -c` returned **4/4 OK**. Direct
`sha256sum` of the four frozen files agrees with the prompt block.
No digit was retyped.

```text
fc2a0cb7de5708a0211c43328da8ef17e46b6212cd01443371e4eea66556a5c9  ideation-20260905T0000Z-packet.md
ee1411f975f662a29424bb7e3397af0a390f0bdf1503d1c6efaae979a24f3053  ideation-20260904T1200Z-synthesis.md
e47fd16cfcc91bc7bfdac4ba1b5f46152db8235e6609dca6e29a5966549c38f5  FALLACY-v2.md
c66b70e460a609ff5210fd16015fc7b2ff18dda26f68597a0d817c5b9aeb19d5  COORDINATION.md
```

Packet frozen at the 00:00Z full-spectrum contract. Lane receipt
`basis=2358a71e8c4c034ac2447aec6af88fb882842e6f`. Binding live state:
`notes.md` EVENT 2026-09-04T23:42Z (`g9966-n1-batch2-sol56` sealed;
N1 tally 3 dead / 5 open) together with AUDIT deltas 17(ppppp)–(dddddd).
Newest formal `LIVE STATE` block is still 2026-09-04T08:22Z; the
EVENT chain through 23:42Z is the binding overlay. `FALLACY-v2` in
force. No new exit-price ⇒ no `charge_basis`. Desk CAS only
(Python/stdlib, `pdftotext` of Moh via page N = PDF page N−139,
existing `box/k16brcr-20260903` tails and `box/g9966n1-20260903`
JSON; one core; ≪ 4 GB; no AWS).

Firewall: flag ≠ place ≠ cover series; `REPRESENTATIVE` is not
`FULL_ACTUAL_EXIT`; a floor is not attainment; a relaxation being
empty *is* a nonexistence proof. Descended 4-tuples are not original
skeletons. Prime marks on descended data are labels. Jacobian exponent
on a Prop 6.3 chart is `ℓ = v_s − u_s − 1` (p.197), never
`n' − M_2' − 2` (ERRATUM[17(cccccc)-S6-EXPONENT], 17(dddddd)).

Running-lane protocol: `xmodel/g9966-n1-batch3-opus5-20260903.run.v2`
was read first. It has `initial_status=RUNNING`,
`start_utc=2026-09-04T23:43:29Z`, and **no** `final_status`. The
companion `.md` was not opened. No other in-progress `.md` was opened.
Same-round ideation submissions were not opened.

Read: the packet; the 12:00Z synthesis; COORDINATION.md; FALLACY-v2;
APPROACHES overlays; AUDIT 17(ppppp)–(dddddd); notes.md 08:22Z–23:42Z;
PROGRESS 2026-09-03 digest (no 2026-09-04 entry yet); named DONE
reports (`k16-hsop`, `k16-rank-criterion`, `k16-nzd-gamma`,
`k16-brcr-closedform`, `g9966-n1-skeletons`, `g9966-n1-batch2`,
`k4ray-K89`, `h1-nonres-census`, `sys-guided-gb`); Moh §§6 and
Appendix II (PDF pages 55–71 = journal 194–210); enumerator JSON
and t=3 explicit tail. Drivers under `box/*-20260903/` used as data,
not edited. The running lane's box `g9966n1b3-20260903/` was not
harvested.

---

## 1. Direct answer: the coordinator's headlines are right, and
two of the four questions are mistyped

The packet's state paragraph is accurate:

- D = 108 is closed at skeleton level (split 17(ddddd) + no-split K=8
  17(bbbbbb)).
- The (99,66) *skeleton* verdict is complete for configurations A, B, C
  of the banked row S8 (case (A) qualification discharged).
- N1 is a finite kill list: 8 printed-(1)–(13) assignments, 3 dead
  (S8, S6 with corrected `ℓ=8`, S5), 5 open (S1–S4, S7).
- K16 is one atom: `T_{t,2t−1}` a nonzerodivisor on the Eagon–Northcott
  curve Γ, equivalently `√(I_2(N)+(W_r)) = m` factorwise. Four
  Gröbner / degree / resultant / coprime-leader routes are theorems of
  impossibility, not unfinished work.
- The per-row method (top pin + exact lower-band parametrization +
  `guided_gb`) is validated and shipped.

The four questions do not all inherit that accuracy.

**Q1 is typed one atom too strong.** The JC2-relevant all-t target on
the ray is still (8.1): `τ_t ∈ √I_{t,+}`. (V0) — dim of the *tail*
is 0, equivalently Q-nonvanishing on Γ — implies (8.1), and the
campaign has spent four deep lanes proving (V0) uniformly. The t=2,
y=1/5 fibre is the existence proof that (8.1) can hold while (V0)
fails: the leftover b3-axis is z-type and lies in `V(τ_2)`, so the
inhomogeneous `T_{t,0} = −c + τ_t` becomes the unit `−c` on that
component. A uniform proof of (V0) *must* fail at that fibre
(`a_0 = 0`). A uniform proof of (8.1) need not. The packet lists
“a valuation on Γ, a specialization, an intersection-theory count, a
deformation” as the menu. Intersection theory on the *projectivized*
EN scheme (which is 0-dimensional of weighted length `d_Γ(t)`) is
the right instrument for (V0). The deformation that actually matches
the t=2 witness is “every associated prime of `I_{t,+}` lies in
`V(τ_t)`”, not another cone Gröbner.

**Q2's uniform-in-V alternative is the wrong type.** The 8 assignments
are not 8 copies of one chart. S5 and S6 are `u_s=1` Prop 6.3
descents (both dead, seconds). S1–S4, S7 are `u_s≥2` and each has a
strict-below split window *and* a no-split complement (17(dddddd)
triage). A single obstruction parameterized by V would have to be
simultaneously a descent-chart unit and a joint two-point unit. That
object is not in Moh, not in Xu, and not in the ladder. Killing the
5 is the right (99,66) route. Among them, S2/S3/S7 share
`(u_s,v_s)=(4,7)` and S4 shares `(u_s,v_s)=(3,8)` with the already
dead S8 — those are the only uniformities worth naming, and they are
chart-families, not a V-formula.

**(1)–(13) completeness is citable as necessity and not as Moh's
table.** Printed p.200–201 derives the numerical restrictions. Printed
p.202 then says a computer program “produces only the following
exceptions” and lists *one* (99,66) row, V=(8,8). The frozen
enumerator, implementing the printed predicates, produces eight. This
is not new at (99,66): `box/moh_skeleton_full.py` CONTROL 5 already
measures that after (1)–(13), M_2 is *not* pinned at (75,50) (Moh:
only 55). The printed list is not the whole of Moh's program. The
honest degree-wide theorem is emptiness of the 8 printed-admissible
skeletons, not a citation of the table.

**Q3 is priced below.** The augmented-Schur-row theorem is the same
category error as disc and NONRES (17(ooooo), 17(ttttt), 17(lllll)):
skeleton data do not determine the killing inhomogeneous row. The
efficient path is the shipped per-row method, compiled, not a new
Fitting identity.

**Q4 over-reads the (99,66) method as a proof architecture.**
Enumerate-and-kill is a finite-per-degree *procedure*. It does not
close the two-point stratum, even with K16 and the routing maps. The
single most valuable next object is the EN-resultant of Q on Γ (or
the weaker τ-on-Q-zero statement for (8.1)), because that is the only
infinite two-point family and the 5 skeletons are already in flight.

---
## 2. Q1 — stop proving (V0) by Gröbner; compute the EN-resultant of Q, and prove (8.1) by τ-support

### 2.1 Typed claims

**C1.1 (PROVED, harvested; this session independently checks the integers).**
For every integer t ≥ 2,

```text
L_t = 2 · binom(3t+1, t−1)
d_Γ(t) = [ binom(3t+1, t−1) − binom(2t, t−1) ] / (t+1)
L_t = 2 · binom(2t, t−1) + (2t+2) · d_Γ(t)     (LENGTH-SPLIT)
```

This session: the identity holds at t = 2..10, with
`(L_t, d_Γ) = (14, 1), (90, 15/2), (572, 46), (3640, 805/3),
(23256, 1548), (149226, 35805/4), (961400, 52140), (6216210, 3064347/10),
(40320150, 1817465)`. CI-SERIES and EN-CURVE are theorems. LENGTH-SPLIT
does not force Q-nonvanishing (17(uuuuu)): it describes the length *after*
dim 0.

**C1.2 (PROVED, harvested).** The packet's three all-t routes remain
impossible:

- NO-COPRIME-LEADERS (17(ppppp), 17(rrrrr) DOMINANT-LEADER): in *every*
  monomial order there is a dominant variable `x_*` such that every
  full-support form of weight divisible by `wt(x_*)` has leading monomial
  a pure power of `x_*`. Every `B_r, C_r` has full support. Coprime
  leaders cannot occur.
- DEGREE-BLIND (17(ppppp)): t=2, y=1/5 vs y=2/5 have identical weights
  and predicted length 14, opposite dimensions 1 and 0.
- Weighted Fröberg / degree-only Bézout: the same witness.

The coefficient recursion (17(wwwww)) is a t-indexed triangular path
sum, not a rational function of (t, r). It does not evaluate Q on Γ.

**C1.3 (PROVED, harvested; this session checks the t=3 axis).**
Projectivized Γ is 0-dimensional of weighted length `d_Γ(t)`. Affine Γ
is a 1-dimensional cone. “Q ≠ 0 on Γ” means Q does not vanish at any
weighted-projective point, equivalently does not vanish on any punctured
line of the cone. At t=3 the q_{2,0}-axis is a rank-1 component
(B_1 = C_1 = W_1 = 0, a denominator-zero of the W_1 chart, not a
(V0)-failure). Restricting the charged exact tail
`box/k16brcr-20260903/explicit_tail_t3_exact.txt` to `b_4 = 0`:

```text
B_1 = C_1 = 0,   B_2 = λ q_2^3,   C_2 = μ q_2^5,
Q_3 |_{axis} = (a_0 μ^2 − b_0 λ μ + c_0 λ^2)/λ^2 · q_2^4,
N_{A_3}(a_0 μ^2 − b_0 λ μ + c_0 λ^2) ≠ 0
  = −4205… / 1544…   (this session; both factors of H_3).
```

So Q_3 is a unit times `q_2^4` on that component, agreeing with
17(wwwww). The atom is not “W_1 is a unit”.

**C1.4 (NEW, the (V0) idea).** Let `R_t := P_t / I_2(N)`. EN-CURVE
says: *if* grade `I_2(N) = t−2` (maximal), then `R_t` is Cohen–Macaulay
of Krull dimension 1 and `Proj^{wt}(R_t)` is 0-dimensional of length
`d_Γ(t)`. Multiplication by Q_t (or by any W_r, since
`W_r(p) = B_r(p)^2 Q_t(p,β)` on rank 1) is an endomorphism of this
0-dimensional algebra after inverting the irrelevant ideal. Its
determinant — the Porter–Thomas / Eagon–Northcott resultant
`Res_{EN}(Q_t; Γ) ∈ A_t` — is the product of Q over the weighted
points, counted with multiplicity. (V0) for all t ≥ 3 is exactly

```text
Res_{EN}(Q_t; Γ)  is a unit of A_t, factorwise.
```

This is an element of a *quadratic* étale algebra over Q, not a
pointwise nonvanishing at `~ 3^{3t}` points. The b4-axis leading
matrix `(λ_B(t,r), λ_C(t,r))` of formula (3.23) in 17(wwwww) is the
specialization that computes the leading term of this resultant; the
q-space restriction (t=3: the axis above) is the other chart. Neither
is a Gröbner basis of the tail.

**C1.5 (NEW, the (8.1) idea; strictly weaker; JC2-sufficient).**
Write `T_{t,0} = −c + τ_t` with `τ_t` homogeneous of weight `4t+1`
(Fable sign; 17(uuuuu) §4). Then

```text
(8.1)  ⇔  V(I_{t,+}) ⊂ V(τ_t)  ⇔  every associated prime of I_{t,+}
         lies in V(τ_t).
```

On any such component the inhomogeneous generator becomes the unit
`−c`, so (8.1) ⇒ (T). The t=2, y=1/5 fibre is the model: JPLUS dim 1,
`T0HOM_IN_RADICAL` power 1, z-type Jacobian `J = −c z` (17(kkkkk) /
17(uuuuu) §4.3). Rabinowitsch `I + (1 − z τ) = (1)` is the *wrong*
uniform instrument: it is not a positive-weight cone, and the
properness lemma does not promote it (model failure `R=Z_{(p)},
J=(x−py), τ=x`). The right uniform statement is the support inclusion,
not a modular unit in an auxiliary ring.

**C1.6 (scope).** Fixed-t (T) stands t = 1..7. Clause (i) / B-HSOP
through t=8. Clause (ii) exact t=3,4, promoted t=5,6. t=8 top-tail and
b4=1 z-chart are INCONCLUSIVE_TIMEOUT, not counterexamples. Z-EMPTY is
JC2-irrelevant (z-type pairs put the Jacobian through the simple
place). Demote (V0) from flagship to sufficient criterion; promote
(8.1) back to the all-t target, with the EN-resultant as the clean
enough-for-(V0) certificate.

### 2.2 Cheapest tests

1. **t=3 EN-resultant (desk, < 5 min).** `P_3 = A_3[b_4, q_{2,0}]`,
   weights (1,2). `I_2(N)` is the single 2×2 determinant. Compute
   `Res_{q_2}(det N, W_1)` (and the b4=0 chart separately, C1.3).
   Gate: the resultant, as an element of `A_3[b_4]`, is a unit times
   a power of `b_4`; its `A_3`-norm is a nonzero rational. Negative
   control: the t=2, y=1/5 fibre, where N=0 and the resultant is 0.
   Instrument: sympy or Singular on the charged exact tail; no `sat()`
   wrapping; ring map declared (`yy` minpoly `H_3`, generators
   `b4, q2_0`). Bound: 2 variables, weight-15 det.

2. **t=4 EN-resultant (< 10 min if t=3 is a unit).** Three minors in
   three variables. Same gate in `A_4`. If both are units, the all-t
   claim is the closed form of `Res_{EN}` in terms of `a_0`, the
   leading Wronskian of `(λ_B, λ_C)`, and the q-space resultant.

3. **(8.1) support, not Rabinowitsch.** At t=3, `V(I_{3,+})` is
   already {0}, so the inclusion is vacuous. The first nontrivial
   test is a *deformation*: set `a_0 ↦ ε a_0` in the t=3 tail and
   track whether components that appear at `ε=0` lie in `V(τ)`.
   Bound: one 2-variable family. Do not run t=8 cones.

### 2.3 Answer

A valuation on Γ that is not the EN-resultant is a restatement (the
valuation of Q along each line is finite iff Q is not identically
zero on that line). A t-specialization `P_t → P_{t−1}` does not
exist as a ring map (`H_t ≠ H_{t−1}`, variable count grows). The
usable specialization is *coordinate restriction*
`q_{3,0}=⋯=q_{t−1,0}=0`, which at t=3 is C1.3. Intersection theory
is C1.4. Deformation is C1.5, and it is the (8.1) idea, not the
(V0) idea. Do not launch another tail Gröbner. Do not chase a
rational closed form of `b_r, c_r`.

---

## 3. Q2 — kill the 5; do not hunt a V-formula; cite (1)–(13) as
necessity, not as Moh's table

### 3.1 Typed claims

**C2.1 (PROVED, harvested).** Exactly eight (1)–(13)-admissible
assignments at (n,m)=(99,66), all with `s=3`, `d=(99,33,11,1)`,
`M_1=−66`, `M_3=97`. Sound tally after 17(dddddd):

| ID | M_2 | (V_2,V_3) | (u_s,v_s) | ℓ = v_s−u_s−1 | status |
|---|---:|---|---|---:|---|
| S1 | −22 | (1,9) | (2,9) | 6 | OPEN, 2271 coeff after two pins |
| S2 | 22 | (1,7) | (4,7) | 2 | OPEN, 3441 |
| S3 | 22 | (5,7) | (4,7) | 2 | OPEN, 3441 |
| S4 | 22 | (1,8) | (3,8) | 4 | OPEN, 2973 |
| S5 | 22 | (1,10) | (1,10) | 8 | DEAD, UNIT_IDEAL_CHAR0 |
| S6 | 55 | (2,10) | (1,10) | 8 | DEAD, corrected ℓ=8 |
| S7 | 77 | (8,7) | (4,7) | 2 | OPEN, 3441 |
| S8 | 77 | (8,8) | (3,8) | 4 | DEAD, all three configs |

The JSON `descended_unsplit_row.k` in `skeletons.json` is the *wrong*
helper `n'−M_2'−2` (S1: 20 vs 6; S5: 5 vs 8). Only S8 agrees. Do not
consume that field.

**C2.2 (the uniform-in-V proposal, REFUTED as a type).** A single
obstruction parameterized by V would have to kill:

- `u_s=1` monomial-Jacobian order charts (S5, S6: 58–60 unknowns,
  seconds), and
- `u_s≥2` joint two-point charts with a split window *and* a no-split
  complement (S1–S4, S7; face ODEs already run; joint systems unbuilt).

Those are different geometries. Disc and NONRES already failed as
skeleton-only kills. The ladder law `9(99−3n−11i)` is
(99,66)-specific via `N = u_s d_2` (17(ttttt)). There is no V-formula
in the sources that becomes a unit in every chart.

**C2.3 (the only real uniformities, bounded).**

- S4 and S8 share `(u_s,v_s)=(3,8)` and the same split window
  `1 < δ < 8/3` with surviving faces δ=2 [2,1] and δ=5/2 [1^3] — the
  *same* N2 classification as the banked skeleton, different
  `(M_2, V_2)`. The banked joint chart does **not** transfer (S4
  descends unsplit to `(27,18; M_2'=6, V_2'=1, ℓ=4)`, not
  `(27,18; 21; 8; k=4)`). The face ODEs transfer; the outer D1/D2
  ranks and pole rows do not.
- S2, S3, S7 share `(u_s,v_s)=(4,7)` and the same surviving faces
  (δ=3/2 [2,2] and [2,1,1]; δ=5/3 [1^4]). S2 and S3 even share M_2=22
  and differ only by V_2 ∈ {1,5}. This is the one place a *chart
  family* (one emitter, three V_2 plugs) is cheaper than three
  independent joints.

**C2.4 ((1)–(13) completeness).** Two different claims:

1. *Necessity.* The numbered restrictions on p.200–201 (tower of
   major discs, minor-disc alternative, Galois/automorphism bounds
   (10)–(13) at r=2, `3 ≤ s ≤ 5`, `d_s ≥ 4`) are derived in §§5–6.
   Citable as necessary numerical conditions on a two-point
   `M_s = n−2` pair, with the campaign's known repairs (N5 den(δ)≤u_s
   17(rrr)/(sss); N2 reconstructed as LEMMA[N2] 17(sssss), Xu's
   printed (i) being false as stated).
2. *Exhaustiveness of the p.202 table.* “The Computer program
   produces only the following exceptions.” At (99,66) the table
   prints one row, V=(8,8). The printed predicates admit eight. At
   (75,50) Moh claims the table pins M_2=55 and V_3=4; the enumerator
   after (1)–(13) does *not* pin M_2 (`moh_skeleton_full.py`
   CONTROL 5, the file's own words: “the printed list (1)–(13) is NOT
   the whole of Moh's program”). So the table is a computer-program
   assertion with unpublished extra filters. It is not a theorem, and
   citing it as N1-closure is the hole 17(hhhhh) named.

Killing the 8 printed-admissible skeletons *is* the degree-wide
(99,66) theorem, conditional on (1) the necessity of (1)–(13) as
printed (citable) and (2) chart necessity for each skeleton (the
17(hhhhh) dossier, N3/N4 discharged, N2 discharged, N1 = this list).
It is not a citation of Appendix II's table.

**C2.5 (S1 is not a small S8).** S1 has M_2=−22. Unsplit descent
gives `(18,12; M_2'=−4, V_2'=1, ℓ=6)`. Negative M_2' is not a
licensed Theorem-1.2 4-tuple; it is either unrouted or a reason S1
fails a missing positivity in Moh's unpublished filter. The live
S1 duty is the split joint chart (2271 coefficients, smallest of
the five), not the unsplit pin.

### 3.2 Cheapest tests

1. **S1 split joint, first among the 5.** 2271 coefficients after two
   corner pins vs 2973/3441. Face already exact: δ=4, p=z(z−c),
   q=p^{28}(C+7∫p^3), residual 0. Gate: `guided_gb` UNIT_IDEAL_CHAR0
   over Q on the joint (or INCONCLUSIVE_TIMEOUT with a measured
   residual variable count, not a blank wall). Bound: one chart.
   The running lane owns this; do not duplicate.

2. **S1 versus unpublished positivity (30 min, no CAS).** Source-read
   p.200–202 and Prop 5.6 against M_2=−22. If a printed inequality
   already excludes S1, N1 drops to 4 open and the enumerator has a
   bug. If not, S1 is a genuine extra and Moh's table is strictly
   stronger than (1)–(13). Quantity: one integer M_2, one predicate.

3. **S4 face-to-joint delta against S8.** Same split window as the
   dead skeleton. The cheapest comparison is: which outer rows of the
   S8 joint used V_2=8 or M_2=77 essentially? If none, S4 is an S8
   rerun with different centre data. If the pivot closed form
   `9(99−3n−11i)` used N=u_s d_2, it will fail for S4 the way it
   failed at D=108 (17(ttttt)). Bound: a row-by-row support diff,
   not a new Gröbner.

### 3.3 Answer

Killing the 5 is the right route for the published open case of Moh's
≤100 theorem. A uniform argument across all 8 V-assignments does not
exist at the type level. (1)–(13) as printed necessity is citable;
Moh's table is not. The enumerator-versus-table gap *is* N1, and it
was already measured at (75,50) before this week's (99,66) work.

---

## 4. Q3 — compile the per-row method; do not write a Schur theorem

### 4.1 Typed claims

**C3.1 (cohort).** The (H1) deliverable is the sharpened u_s≥2 census
at D≤200: **296 rows / 166 groups** (17(ttttt); upstream POLY/ODE
u_s≥2 is 310/177; raw printed-(1)–(13) u_s≥2 is 6,117 rows / 4,012
groups — do not sweep the raw list). NONRES is true on 124/166 groups
and kills 0/274 (skeleton, δ) pairs by itself. Disc kills only the
D=108 [1,1]→[2] collapse.

**C3.2 (method, validated).** Top pin + exact lower-band
parametrization → ~36 ring variables → `guided_gb` with three good
primes *and* an exact-Q standard basis (inhomogeneous `μμ_inv−1`
blocks modular promotion). Clients: K=7 in 8.2 s; K=8, K=9 both
internal splits UNIT_IDEAL_CHAR0 (17(aaaaaa)/(bbbbbb)). Composite arm
B_2 constant ⇒ h|J ⇒ composite is already a uniform theorem
(17(vvvvv)). The LEVEL-4 pin `β_b = μ · y^{K−3}(y−x)` is *k=4-ray
specific* (Jacobian X^4, deg(g^2−f^3−λf)=K+6). It does not transfer
to general k.

**C3.3 (the three options, priced).**

| path | what it needs | cost | information |
|---|---|---|---|
| Batched `guided_gb` sweep of 166 | a skeleton→chart compiler with `ℓ`-gate; AWS | days, premature | kills, timeouts, or a SURVIVES |
| Uniform pin+parametrization theorem | a k-independent LEVEL-4 identity | a structural lane, likely saturates as the degree tower did | would make each row K=7-cheap *if it exists* |
| Augmented Schur/Fitting row | an inhomogeneous row determined by skeleton data | already refuted (17(lllll), disc, NONRES) | none |

The uniform pin theorem is the k=4 degree tower in new clothes: the
tower saturated at c=3 with residual width ~4K/3 growing in K
(17(zzzzz)). Do not relaunch it at general k. The Schur row is the
disc mistake in Fitting language: on the distinct-root locus the
interpolation block is *solvable*. Inconsistency is an extra
inhomogeneous row the skeleton does not supply.

**C3.4 (efficient path).** Library-ize pin + lower-band with an
`ℓ`-gate (systems upgrade); finish the 5 joints (running — they
*are* the cost-discovery experiment; S1 at 2271 is cheapest); sweep
leftover u_s=1 descents first among open (H2) rows. Do not write a
Schur theorem. Do not sweep 4,012 raw groups. A 166-group AWS sweep
before S1 returns is a budget on an unmeasured wall (17(yyyyy): ~55
unreduced parameters; the lower-band cut to 36 is what moved it).
After S1–S4, S7 are typed, the 166 re-price as
`N_small · (S1-minutes) + N_large · (S2-minutes)`.

### 4.2 Cheapest tests

1. Compiler regression: S5 [2] exact unit in < 5 s, S6 < 1 s, K=7
   < 10 s, S8-unsplit skipped (already banked). Fail closed if any
   chart is emitted with `ℓ ≠ v_s−u_s−1`.
2. One next u_s=2 joint other than the running (99,66) five, only
   after S1's residual variable count is printed. Not 166.

### 4.3 Answer

Batched `guided_gb` *after* the compiler and after the 5 teach the
cost. Not a uniform pin theorem. Not a Schur theorem. The census is
the (H1) deliverable; the method is the per-row kill; the missing
piece is the emitter, not the algebra.

---

## 5. Q4 — enumerate-and-kill is a procedure, not a two-point proof;
the next object is the EN-resultant

### 5.1 Where the degree-wide sentence is right

At a *fixed* (n,m), if (1)–(13) as printed are necessary and the
enumerator is complete for those predicates, then a Keller pair of
those degrees produces one of finitely many skeletons, and killing
each skeleton in every configuration (split faces in the strict
window, plus no-split complement, with `ℓ` correct) is a degree-wide
theorem. That is exactly the (99,66) task. It generalises *as a
procedure* to any other fixed degree whose printed-admissible list is
short.

### 5.2 Where it is not a proof of the two-point stratum

The procedure is not uniform in n (K=16 is infinite; (75,50) already
has enumerator-vs-table extras, so N1 recurs at every degree).
Routing maps are unproved (OPEN[ROUTING-MAPS]): a skeleton kill is a
chart kill until 17(hhhhh)-style necessity is assembled, and that
dossier is S8-specific. Inside two-point, U-NEGATIVE (~20 rows,
V_2'>d_2'; S1's M_2'=−4 is in this spirit), s'>2
(OPEN[DESCENT-STATE-S3]), and unsplit u_s≥2 failing
`δ*_{s−1} ≥ v_s/u_s` are unrouted. One-place is a different program
(Moh p.194; the packet's PROGRAM line already says this).

So (99,66) degree-wide + K16 + routing maps would prove the
*printed-(1)–(13), two-point, M_s=n−2, routed, U-positive, s'=2*
stratum, not the two-point stratum, and not plane JC2. The 12:00Z
synthesis already corrected the 00:00Z over-read; Q4 re-opens it.

### 5.3 The single most valuable next object

Not the 5 skeletons (already launched). Not a 166-group sweep
(unpriced). Not t=8 cones (exhausted as a method). Not Moh's
10-coefficient chart of case (A) (the configuration is dead by the
36-variable chart; the 10-coefficient reconstruction is paper-grade
debt, not a kill). Not the routing maps (unproved interface, not the
frontier obstruction).

The object is **`Res_{EN}(Q_t; Γ) ∈ A_t`**, or, if that stalls, the
support inclusion `V(I_{t,+}) ⊂ V(τ_t)`. It is the only infinite
two-point family, it is reduced to one scalar in a quadratic algebra,
and the t=2 fibre tells you which of the two statements JC2 actually
needs.

---
## 6. Disposition vector (APPROACHES rows, changes only)

The 12:00Z overlay stands: (H1)∧(H2) is a program along Moh's two-point
`M_s=n−2` line, not plane JC2. Case (A) is no longer a live kill
(17(bbbbbb)); N1 is. K16 is no longer “three routes to try”; it is
one resultant.

| # | change | reason |
|--:|---|---|
| 1 | **raise** | (T) now has closed k=4-ray census members (K=7,8,9) and a uniform composite arm, plus the K16 tail in EN-resultant form. Row-1 certificates kill cofinal families *and* leftover u_s=1 descents. |
| 2 | **unchanged as screen** | The tree generates targets. U-NEGATIVE and s'>2 remain coefficient-level leaks, not a reason to reopen the sieve as a proof. |
| 5 | **unchanged retype** | Prop 6.3 is not Jung–van der Kulk. The D=108 no-split map is closed as a substitution; emptiness is now a theorem at skeleton level, not OPEN[ORDINARY-SUPPORT]. Do not shear. |
| 6 | **lower as a live (A) kill; keep as paper reconstruction** | Case (A) is dead by the 36-variable pin+band chart, not by Moh's 10-coefficient Appendix II chart. The 10-coefficient reconstruction remains paper-grade debt (necessity of *that* chart vs the over-approx). Do not relaunch it as a kill lane. |
| 32 | **raise as the (H1) instrument** | Q* / high-h banding plus `guided_gb` plus lower-band parametrization is the per-row method. It is not a general order-chart theorem. |
| 36 | **retarget** | CE search: a Q=0 point of Γ at some t≥8 (typed (V0)-failure, check (8.1) before advertising a pair), or a SURVIVES of an S1–S4/S7 joint (typed `REPRESENTATIVE` of a degree-(99,66) pair, not a Keller pair until algebraized through Ω). Not original (25,15). Not case (A) 10-coeff. |

Unchanged: 3, 4, 7–31, 33–35, 37–46. STOP as proof routes: disc / NONRES / augmented Schur as (H1) theorems; fixed-t cone at t≥8; coprime-leader Fröberg; degree-only Bézout; k=4 degree-bound tower; 166-group sweep as a *theorem*; Moh p.202 table as N1-closure. RAISE: EN-resultant; `ℓ`-gate compiler; S2/S3/S7 chart family.

---

## 7. Reranked bottlenecks

**Proof, in order.**

1. `Res_{EN}(Q_t; Γ) ∈ A_t^×` for t≥3, or the weaker support inclusion
   for (8.1). Bounded: t=3 is two variables. Closes the only infinite
   two-point family, or produces a typed (V0)-failure to test (8.1) on.
2. The 5 remaining (99,66) skeletons, S1 first (2271). Closes Moh's
   published ≤100 gap *as a printed-(1)–(13) theorem*. Already in
   flight.
3. Chart necessity for S1–S7 (the 17(hhhhh) dossier was S8-specific).
   Bounded: which row families of the S8 joint used V=(8,8) essentially.
4. Compiler + `ℓ`-gate, then leftover u_s=1 descents. Bounded: S5/S6
   regressions.
5. One U-NEGATIVE 4-tuple, source-read of Thm 1.2, no analogy.
   Quantity: 20 rows in the 14:41Z partial.
6. Routing maps. Unproved interface; not the frontier obstruction;
   do not launch before (1)–(2).

**Disproof, in order.**

1. A point of Γ with Q=0 at t≥3 (t=3,4 exact nonvanishing is a
   control: a finding there is a chart error, not a CE). At t≥8 it is
   a (V0)-failure; (8.1) may still hold. Typed `REPRESENTATIVE` of a
   ray member only after algebraizing through the K=16 descent.
2. SURVIVES of S1 joint. M_2=−22 is the most table-divergent extra;
   a nonempty joint is either a missing equation or the first honest
   (99,66) extra-skeleton candidate. `REPRESENTATIVE`, not a pair.
3. A leftover u_s=1 descent whose pin+band residual is nonunit after
   `ℓ` correction.

PIN-NOT-CEILING, `C_FULL_TREE`, and Xu's principal floor stay demoted
as proof programs.

---

## 8. New avenue and new connection

**New avenue: the Eagon–Northcott resultant of the top quadratic.**
Replace “nonvanishing at d_Γ(t) weighted points” by the single scalar
`Res_{EN}(Q_t; Γ) ∈ A_t`, computed from the EN complex (or, at t=3,
from `Res(det N, W_1)` in two variables) and identified with an
explicit unit built from `a_0` (norm known, unique zero t=2) and the
leading-coefficient Wronskian `(λ_B, λ_C)` of 17(wwwww) (3.23). This
is intersection theory on a 0-dimensional weighted projective scheme,
not a Gröbner basis of the tail, not a rational formula for `b_r`,
and not Rabinowitsch. Discriminator: Card I.

**New connection: Moh's unpublished extra filter is the same *class*
of error as the S6 exponent slip.** Printed Prop 6.3(3) says
`ℓ = v_s−u_s−1`; a helper used `n'−M_2'−2`; they agree only on S8;
17(cccccc) shipped a void certificate; 17(dddddd) caught it. Printed
(1)–(13) admit 8 assignments at (99,66); p.202's computer program
prints 1; they agree only on S8. In both cases a *computer-program
assertion* was treated as the printed theorem. The degree-wide (99,66)
task is to empty the printed-admissible list, just as the S6 task was
to rerun at the printed exponent. The `ℓ`-gate below is the software
shadow of this connection.

A second connection, already half-said at 12:00Z and now sharper:
Tschirnhausen on case (A) (depress f, remainder bounds, 10 coefficients)
is the same move as TAIL-SPLIT on the K16 cone (depress the b3-quadratic,
G_r linear). The factor 2 in L_t is that degree. The t=2, y=1/5 failure
of (V0) is the Tschirnhausen step losing its leading term (`a_0=0`).
Case (A) is now dead by a *different* presentation of the same
depression (pin + lower band). The EN-resultant is Tschirnhausen one
step further: after depression, the eliminant on the residual curve.

---

## 9. Strongest proof attack, strongest counterexample attack

**Proof.** Compute `Res_{EN}(Q_3; Γ) ∈ A_3` from the charged exact
2×2 matrix `(C_r, B_r)`, declare the ring
`(0,yy),(b4,q2_0),wp(1,2)` with `minpoly=H_3`, and show the resultant
is a unit times a power of the irrelevant ideal. Then identify the
t=3 value with `a_0^{d} · Wronskian(λ_B, λ_C) · (q-space unit)` and
promote the identification to all t by the EN complex, using only the
proved pivots of 17(wwwww) and the unit-norm of `a_0` for t≥3.
Mechanism: intersection theory on a 0-dim weighted scheme, not dim 0
of the tail. Fail-closed: if the t=3 resultant vanishes, the banked
exact (V0) at t=3 is contradicted and the lane stops on a chart/sign
error. Empty of zeros ⇒ (V0) at t=3 is one scalar, and the all-t
claim has a name.

**Counterexample.** A nonzero point of `Σ_t ∩ V(Q_t)` at some t≥8
(t=3,4 are controls). Typed as a (V0)-failure. Then test (8.1): if
`τ_t` vanishes there, (T) may still hold (z-type, as at t=2 y=1/5);
if `τ_t` does not vanish, the ray member is a candidate pair and
must be algebraized through Prop 6.3 before anyone says Keller.
Second: SURVIVES of the S1 joint. Do not advertise emptiness of
original (25,15), or a joint SURVIVES at S8 split (both branches
are [1]), or case (A) 10-coeff (dead by another chart).

---

## 10. Software acceleration / decisive experiment

**Experiment.** t=3 EN-resultant from the charged exact tail, over
`A_3`, no Gröbner of the tail.

- Instrument: `box/k16brcr-20260903/explicit_tail_t3_exact.txt`
  (B_1, B_2, C_1, C_2, a_0, b_0, c_0 already extracted); sympy
  resultant in `q2_0` of `(B_1 C_2 − B_2 C_1, W_1)`, then reduce
  the `b4`-polynomial modulo `H_3`; separately the b4=0 chart
  (this session: Q-numerator norm ≠ 0).
- Controls: (i) t=2 y=1/5, N=0, resultant 0; (ii) t=2 y=2/5,
  dim 0, resultant a unit; (iii) image check `a_0 = −575/1232 yy + 5/44`
  against 17(rrrrr); (iv) no `sat()`; ring map declared.
- Outcomes: nonzero `A_3`-norm → Card I proceeds to t=4 and to the
  closed-form identification. Zero → STOP, the t=3 (V0) certificate
  and this resultant disagree (chart). Timeout on 2 variables → the
  parser is the bottleneck, not the algebra.
- Bound: < 5 min, ≪ 4 GB, one core. This session already did the
  b4=0 chart; the det/W_1 resultant is the remaining 3 minutes.

Do not ship a 1800 s tail `std` at t=8 as this experiment.

---

## 11. Campaign-systems check

**`UPGRADE` — mechanical Prop 6.3(3) exponent gate, `box/lib/ell_gate.py`.**

Rotate: reproducibility / claim-propagation. Independent of K16
mathematics. The last systems upgrade (`guided_gb`, 17(xxxxx), 14:37Z)
unblocked compute; it did not block the class of error that voided
the 17(cccccc) S6 certificate.

Evidence, this round: (i) 17(cccccc) used `ℓ = n'−M_2'−2 = 2` for S6;
Moh p.197 Prop 6.3(3) gives `ℓ = v_s−u_s−1 = 8`; the two formulae
agree on S8 and on the k=4 ray and *disagree on S1–S7*; (ii) the
enumerator JSON still stores the wrong `k` in
`descended_unsplit_row`; (iii) 17(dddddd) caught it by a source read,
not by a gate; (iv) the next five joints will emit descent charts for
the no-split complements — the same slip is one helper-call away.

Smallest test: a 40-line function

```text
ell_source(v_s, u_s) = v_s - u_s - 1
ell_helper(n1, M2p)  = n1 - M2p - 2
gate(skel) -> AGREE | REFUSE
```

on the eight (99,66) rows. Expected: AGREE only on S8; REFUSE on
S1–S7. Bind REFUSE to “do not emit a descent chart”. Replay S5/S6
certificates' Jacobian target line (`J − c x^8`) as the positive
control. Implementation: one function next to `moh_skeleton_full.py`,
called from every order-basis / joint emitter before Singular.
No change to live lanes except a coordinator note that batch3 must
not consume `descended_unsplit_row.k` from the JSON.

No `NO_CHANGE`: a void exact-Q unit was banked for six hours. That is
a claim-propagation failure, not a solver failure.

---
## 12. Idea cards (three)

### Card I — EN-resultant of Q on Γ at t=3, then 4

**Dependencies.** FITT, EN-CURVE, TAIL-SPLIT, ELIMINANT identity
`W_r = B_r^2 T_{t,2t−1} − G_r(…)`, charged exact tails
`box/k16brcr-20260903/explicit_tail_t{3,4}_exact.txt`, unit-norm of
`a_0` for t≥3, DEGREE-BLIND (do not reverse length), FALLACY-v2 ring
map. Does *not* depend on t≥8 cones, on Rabinowitsch, on a rational
formula for `b_r`, or on the running N1 lane.

**Cheapest discriminator.** At t=3, two variables, compute
`Res_{q_2}(det N, W_1)` over `A_3` and the b4=0 chart (this session:
Q-numerator norm ≠ 0). Bound: < 5 min. Then t=4, three minors, < 10 min.

**Outcomes.**

- Both resultants are units of `A_t` (norms nonzero rationals),
  controls pass → (V0) at t=3,4 is one scalar each; the all-t claim
  is `Res_{EN}(Q_t; Γ) = unit(a_0, λ_B, λ_C)`. First lane continues
  to the identification, not to t=8.
- Resultant 0 at t=3 → STOP. Banked exact (V0) and this computation
  disagree; chart/sign/ring-map error. Do not proceed to t=4.
- Resultant a non-unit non-zero (vanishes on one factor of `H_t`) →
  factorwise failure; (V0) fails on that Galois conjugate. Test (8.1)
  on that factor (Card II). Typed OPEN, not a Keller pair.
- Timeout on 2 variables → parser/coefficient-size, not algebra;
  switch to a 16-bit prime and CRT, still desk-scale.

**Stop.** 90 min, or at a t=3 zero, or at the first undeclared ring map.

**Information gain.** The atom of K16, as an element of A_t rather
than as d_Γ(t) pointwise checks. Highest EV in the round for a *new*
lane.

### Card II — (8.1) as support inclusion, modelled on t=2 y=1/5

**Dependencies.** Lemma CONE, 17(uuuuu) §4 (Rabinowitsch does not
promote), 17(kkkkk) linear-form Jacobian, t=2 exact fibres, Card I's
t=3 resultant (if it is a unit, Card II is vacuous at t=3 and becomes
a deformation). Does not depend on (V0) for all t.

**Cheapest discriminator.** Deform `a_0 ↦ ε a_0` in the t=3 tail
(two variables). At ε=1, (V0) holds. At ε=0, the t=2 y=1/5 mechanism
should appear: a positive-dimensional component contained in `V(τ)`.
Gate: for every ε, every component of `V(I_{3,+}(ε))` lies in
`V(τ(ε))`, including ε=0. Bound: one 2-variable family, < 10 min
per fibre at ε ∈ {0, 1, 1/2}. Instrument: exact over `Q(ε)` or three
rational fibres. No Rabinowitsch auxiliary z.

**Outcomes.**

- Inclusion holds on the family → (8.1) has a deformation proof at
  t=3, and the t=2 fibre is the special point, not an anomaly. The
  all-t (8.1) claim is this inclusion, not dim 0.
- Inclusion fails at some ε ≠ 0 → (8.1) fails in the deformation;
  STOP and do not promote a t-generic (8.1) argument.
- Inclusion fails only at ε=0 in a way that does *not* match t=2
  y=1/5 (τ not vanishing on the leftover component) → the model is
  wrong; typed OPEN[8.1-DEFORMATION-MISMATCH].
- Vacuous because Card I already gives (V0) at t=3 and the
  deformation is not flat → Card II waits on a t where (V0) is
  unknown (t=8), which is *not* this card's cheap test.

**Stop.** 90 min, or at the first fibre where τ does not vanish on a
positive-dimensional component except the documented t=2 y=1/5 type.

**Information gain.** Separates (8.1) from (V0) as mechanisms. If
Card I stalls uniformly, this is the JC2-sufficient fallback the
12:00Z synthesis already named and then under-ran.

### Card III — printed (1)–(13) versus Moh's table, S1 first

**Dependencies.** Moh p.200–202 (PDF 61–63); Prop 5.6 remark on (11);
frozen `moh_skeleton_full.py` CONTROL 4–5; `skeletons.json` eight
rows; 17(cccccc)/(dddddd). Does not depend on joint charts, on
guided_gb, or on K16.

**Cheapest discriminator.** Source-check S1 (M_2=−22, V=(1,9), u_s=2)
against every numbered restriction on p.200–201 and against
Prop 5.6. Quantity: 7 extra skeletons at (99,66) relative to the
table; at least one missing predicate, or none. Bound: 30–60 min
read plus the enumerator's existing `full_ok` flags. No Singular.
If a printed inequality already excludes S1, the enumerator has a
bug and N1 drops. If not, Moh's table is strictly stronger than
(1)–(13) and the degree-wide theorem is the 8, not the 1.

**Outcomes.**

- A printed predicate excludes S1 (and possibly others with M_2<0) →
  N1 shrinks; fix the enumerator; the running lane's S1 joint is
  method-control, not a (99,66) duty.
- No printed predicate excludes any of the 7 → N1 is the 5 remaining
  kills, and citing p.202's table as completeness is a GAP. Card III
  closes the citation question of Q2 without waiting on Gröbner.
- Prop 5.6 “(11) cannot always happen” is the extra filter and is
  *not* implemented as “at least one level admits (10)”
  (`any10` in the enumerator) in the way Moh's program used it →
  typed OPEN[N1-PROP56-FILTER]; cheapest repair is to match the
  remark to the 7 extras, not to invent a positivity.

**Stop.** 60 min, or at the first unsourced extra inequality (do not
fill by cap: if the extra filter is not in the paper, say so).

**Information gain.** Whether the published ≤100 gap is 5 joints or
a smaller list, and whether Moh p.202 is citable. Independent of the
running lane's compute.

---

## 13. Continue / redesign / stop

### Named running lane (receipt only)

Tag `g9966-n1-batch3-opus5-20260903`. Receipt
`xmodel/g9966-n1-batch3-opus5-20260903.run.v2`:
`initial_status=RUNNING`, `start_utc=2026-09-04T23:43:29Z`,
`adapter=opus`, `basis=2c7f9b263c7d0ebf15b7675e2fd2723f573bad94`,
charged_inputs=15, **no `final_status`**. The `.md` was not opened.
Prompt names the task: building and killing the last five (99,66)
skeletons S1–S4, S7.

**Recommendation: CONTINUE.** The task is the right (99,66) duty
(Q2). The method is the shipped per-row kill plus joint-chart
construction (Q3). The lane is < 1 hour old at packet freeze. Two
non-informative attempts have not occurred. Do not redesign on an
unsealed report. Do not open the `.md`.

Receipt-only notes, not instructions to a running process: S1 is the
cheapest joint (2271 vs 3441); S2/S3/S7 are one chart family; do not
consume `skeletons.json`'s `descended_unsplit_row.k` (wrong exponent
on S1–S7). If the lane later stalls on an unreduced 50-parameter
blob, *then* redesign toward the 36-variable lower-band cut, not
toward a V-formula.

### Other major lanes

- K16 structural: four deep lanes SHARPENED without closing. STOP
  further tail Gröbner / t=8 cones / rational-`b_r` hunts. REPLACE
  with Card I (and Card II if Card I's resultant is a unit at t=3,4
  but the identification stalls).
- k=4 ray census members: STOP (K=7,8,9 dead; uniform all-K not
  proved and not needed for the census).
- (H1) NONRES / disc / Schur: STOP as theorems. Census remains the
  deliverable, behind the compiler and behind the 5.
- N2 / N3 / N4: STOP (discharged).

### Single first lane

**`k16-en-resultant-t3t4`** (Card I). Grok or Sol, desk-scale, 90 min
cap, charged tails from `box/k16brcr-20260903/`, no AWS, no t=8.
Owner: not Opus (Opus is on the 5). Independent of the running lane.
If Card I returns units at t=3,4, the follow-on is the identification
with `a_0` and `(λ_B, λ_C)`, still desk-scale, not a 166-sweep.

Do not launch a second N1 lane while batch3 is RUNNING.

---

## OPENS RAISED

**OPEN[K16-EN-RESULTANT].** Decide whether the Eagon–Northcott /
Porter–Thomas resultant of `Q_t` against Γ is a unit of `A_t` for
every integer t ≥ 3, factorwise. Quantity: bound the `A_t`-norm of
that resultant (a nonzero rational at each t, or a polynomial in t
of degree at most the EN length `d_Γ(t)`). Cheapest test: Card I,
t=3, two variables, `Res(det N, W_1)` over `A_3`, < 5 min; negative
control t=2 y=1/5 (resultant 0). This sharpens the standing
K16-Q-nonvanishing-on-Gamma atom; it does not close it.

**OPEN[8.1-TAU-SUPPORT].** Decide whether `V(I_{t,+}) ⊂ V(τ_t)` for
all t ≥ 2 (the JC2-sufficient (8.1), strictly weaker than (V0)).
Quantity: bound the number of associated primes of `I_{t,+}` that
meet `D(τ_t)` — the claim is that this number is 0. Cheapest test:
Card II, t=3 deformation `a_0 ↦ ε a_0`, three fibres ε ∈ {0, 1/2, 1},
< 10 min each; t=2 y=1/5 is the documented zero-dimensional-failure /
inclusion-success control. Do not use Rabinowitsch as the gate.

**OPEN[N1-PRINTED-VS-TABLE].** Decide which extra predicate, if any,
in Moh's unpublished p.202 program kills the 7 printed-(1)–(13)
assignments at (99,66) that the table omits. Quantity: at most 7
extra skeletons; at least one missing inequality, or none. Cheapest
test: Card III, source-read of S1 (M_2=−22) against p.200–201 and
Prop 5.6, 30–60 min, no CAS. Do not fill a gap by cap: if the extra
filter is not in the paper, the answer is “none, the table is
stricter than (1)–(13)”.

**OPEN[S1-UNSPLIT-M2-NEGATIVE]** (new, bounded). Decide whether the
S1 no-split complement is a licensed Prop 6.3 descent at all, given
`M_2' = −4`. Quantity: at most one 4-tuple
`(18,12; M_2'=−4, V_2'=1, ℓ=6)` — either licensed or unrouted.
Cheapest test: compare Prop 6.3 hypotheses (p.197) to this tuple;
if `M_2' < 0` violates a sourced inequality, the unsplit duty is
unrouted (fallback, not a cap) and S1 is split-joint only. Bound:
one page of source plus the batch2 triage already stating the
tuple.

Carried names (not new raises): Moh-census-N1 (now the 5-kill list,
owned by the running lane); K16-Q-nonvanishing-on-Gamma (sharpened
above); U-negative-chart; routing-maps; descent-state-s3; 8.1-vs-V0
(Card II is its mechanism).

Not raised: cofinal emptiness of the screen; a rational formula for
`b_r`; a uniform V-obstruction; a 166-group theorem; an
augmented-Schur row; Z-EMPTY; (V0) for all t as a separate flagship
(that is the EN-resultant raise above); case (A) 10-coefficient kill;
original (25,15) as a CE.

---

## 14. Desk CAS

Stdlib only plus one file parse; one core; no AWS; no `sat()`.
Drivers under `box/*-20260903/` read, not edited.

- **LENGTH-SPLIT.** For t=2..10 the identity
  `L_t = 2 binom(2t,t−1) + (2t+2) d_Γ(t)` holds with the values in
  C1.1. Matches CI-SERIES and every banked measurement.
- **t=3 q2-axis Q.** From `explicit_tail_t3_exact.txt`, on `b4=0`:
  `B_1=C_1=0`, `B_2=λ q_2^3`, `C_2=μ q_2^5`, and the numerator
  `a_0 μ^2 − b_0 λ μ + c_0 λ^2` has `A_3`-norm
  `-4205350652595633634366095066070556640625 / 15442446100106097604517871435761663802459211464135251843068505088 ≠ 0`.
  Both factors of `H_3=588y^2−336y+44`. Confirms 17(wwwww): Q is a
  unit times `q_2^4` on that component. Ring: `A_3 = Q[y]/(H_3)`,
  generators as in the file, no name-only identification.
- **S1–S8 exponent mismatch.** `ℓ_source = v_s−u_s−1` vs JSON `k`:
  S1 6 vs 20, S2 2 vs 26, S3 2 vs 26, S4 4 vs 19, S5 8 vs 5,
  S6 8 vs 2, S7 2 vs 6, S8 4 vs 4. AGREE only on S8. This is the
  `ell_gate` expected output.
- **Moh p.202.** `pdftotext` PDF page 63 (journal 202): the computer
  program “produces only the following exceptions”; the (99,66) row
  is M_2=77, V_3=8, V_2=8. PDF page 61–62: the search conditions
  (1)–(13). PDF page 55 (journal 194): restriction to `M_s=n−2` and
  two points at infinity. PDF page 58 (journal 197): Prop 6.3(3)
  Jacobian exponent `v_s−u_s−1`.
- **Census sizes** (from 17(ttttt), not re-enumerated): 296 rows /
  166 groups sharpened u_s≥2; 6,117 / 4,012 raw.

No `charge_basis`. No ledger edit.

---

## 15. What I am not claiming

- (V0) for all t. That is OPEN[K16-EN-RESULTANT].
- That CI-SERIES or LENGTH-SPLIT implies dim 0. DEGREE-BLIND forbids it.
- That the EN-resultant has been computed here except on the t=3
  q2-axis chart.
- That (8.1) is proved uniformly. Card II is a test, not a proof.
- That N1 dies in this submission. Five joints unbuilt; `.md` of the
  running lane unread.
- That Moh p.202's table is false as a *program* output. It is not
  citable as the printed (1)–(13) list. The extra filter may exist
  in an unpublished program.
- A uniform MINOR-EMPTY / Schur / V-formula theorem.
- That enumerate-and-kill proves the two-point stratum or plane JC2.
- That case (A)'s 10-coefficient chart was the kill (the 36-variable
  chart was).
- That S1 unsplit is a Keller-pair candidate (M_2'=−4 is unrouted
  until Card III / OPEN[S1-UNSPLIT-M2-NEGATIVE]).
- A new exit price.
- Any identification of flag, place, and cover series; of S4 with S8;
  of `n'−M_2'−2` with `v_s−u_s−1`; of `REPRESENTATIVE` with a pair;
  of `V(W_1)=0` with Criterion RANK; of (V0) with (8.1).


## COLLISIONS

`python3 ops/open_collision.py xmodel/ideation-20260905T0000Z-grok46.md`
returned `status: CANDIDATES` (review candidates, never closures).
Same-round `ideation-20260905T0000Z-*` submissions were excluded by
the corpus guard; they were not opened. The named N1 batch3 lane is
still `initial_status=RUNNING` with no `final_status` and no `.md`;
it is not in the corpus.

- `OPEN[K16-EN-RESULTANT]`: NONE. New.
- `OPEN[8.1-TAU-SUPPORT]`: lexical hits on August max12 reviews
  (`support Q=…`, torus fibres, `tau` as a slope parameter). Those
  lines are not the K16 homogeneous `τ_t` or the inclusion
  `V(I_{t,+}) ⊂ V(τ_t)`. Not a closure.
- `OPEN[N1-PRINTED-VS-TABLE]`: NONE. New. (The enumerator CONTROL 5
  at (75,50) is the *origin* of this question, not an answer at
  (99,66).)
- `OPEN[S1-UNSPLIT-M2-NEGATIVE]`: NONE. New. Batch2 states the
  tuple; it does not decide Prop 6.3 license.

## Seal

The skeleton contained no seal marker. No canonical ledger edited.
`jc2-lean` not inspected. No other `ideation-20260905T0000Z-*`
submission read. The named running lane was receipt-only; `.md`
not opened. No `charge_basis`. Desk CAS only.
<!-- BODY-END -->
