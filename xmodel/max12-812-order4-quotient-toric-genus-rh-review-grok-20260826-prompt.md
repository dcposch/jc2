# Unique-output hostile review: order-four quotient toric genus/RH theorem

Review the conditional theorem

```text
xmodel/max12-812-order4-quotient-toric-genus-rh-theorem-20260826.md
SHA-256 7f107c5e422177aac4387bd42e23e4c833592106406a36a01fe89a4ccd555e41
```

Read the two charged corrected-V2 coefficient artifacts named in its Section
0 and recompute every named hash.  Do not use a live AWS endpoint, a modular
plane relation, or any existing verdict token as evidence.

Independently and adversarially check:

1. that the hypotheses of the exact plane gate are explicit and strong
   enough for every conclusion, especially irreducibility, birationality,
   faithful residual `mu_16`, and passage from an affine chart to the smooth
   complete normalizations;
2. the invariant fields for the full residual `mu_16` and the source deck
   `mu_4`, including `w=s/t`, `q=s^2/t^3`, `y=t^2`, `v=t^8`, all extension
   degrees, and the distinction between the two quotients;
3. all three Newton polygons, areas, boundary lengths, interior-point genera,
   and whether the stated nondegeneracy hypothesis really transports under
   the two monomial substitutions;
4. every finite and infinite valuation in both boundary tables, the number
   of places above it, all inertia groups, the ramification totals `24`,
   `40`, `136`, and all three Riemann--Hurwitz identities;
5. the proper-extension argument from the source base `P^1`, nonconstancy via
   `r_7^4`/`r_7^16`, and the exact firewall separating this conditional hand
   theorem from the live plane/nondegeneracy computation and from Keller
   existence.

Try to break the result at omitted boundary points, non-split unramified
places, a kernel in the group action, a mismatch between weighted-projective
and ordinary plane closures, reducibility after `v=y^4`, or an invalid use of
affine genus.  State the smallest failing identity or missing hypothesis if
one exists.

This is source reading and hand review only: no CAS, solver, substantive exact
Python, Sage, Singular, msolve, or Lean locally.

Write exactly one file and no other file:

```text
xmodel/max12-812-order4-quotient-toric-genus-rh-review-grok-20260826.md
```

Classify every issue as fatal, repairable, or cosmetic, state the strongest
surviving theorem and exact scope, and end with exactly one overall verdict
token: `CONFIRMED`, `REPAIR`, or `REJECTED`.
