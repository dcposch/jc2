cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
Done at 01:51:04Z, twelve minutes before the 02:03 reserve. Both owned files exist, the marker is unique and terminal, and writers are idle.

**Outcome.** The one-place full-rank instrument (authority.py, wire.py, finder.py, checker.py plus CONTRACT/CONTROLS) passes a static FIRST review with no REFUTED item and no blocking correction. The only GAP is external readiness: liveness, aggregate storage and phase profiles are unmeasured and nothing has been executed by anyone.

**Verdicts written to xmodel/f10-source-cone-r3-rank-code-gate-fable5-20260911.md**

- **A CONFIRMED.** Both programs rebuild the literal monic septic independently (list convolution versus index loops; I re-expanded the middle factor by hand), pin the interface labels and weights byte-for-byte against produce.py and check.py, and apply p-unit denominator checks to all seven coordinates of every term, including zero coordinates, for all 25 slots, the five extras and c, c_inverse.
- **B CONFIRMED.** Trial division is exact on both sides; the Rabin-style test (gcd for all i up to f/2 plus the final Frobenius identity) is necessary and sufficient for degrees 1 to 7, and I traced the degenerate branches (splitting phi, f=1, phi=X).
- **C CONFIRMED.** Rows and columns are the same lexicographic universe in both programs. I recounted 1453 by hand from the p_4 table. The forward elimination invariant makes the k>=j restriction exact, and the minor is rebuilt from source data, not the reduced array.
- **D CONFIRMED.** The checker's row-minus-multiplier reconstruction and full 88,209-position product check are sound without trusting anything from the finder. A finder ordering mismatch could only cause a false negative.
- **E CONFIRMED.** Strict decimal wire, no JSON numbers anywhere, and qualification binding by byte custody rather than a rerun. One precision note: support is checked term by term, not whole-first.
- **F CONFIRMED (static).** Import order, the single typed argv hole at index 11, input/output separation and receipt semantics are as contracted. External ROOT obligations are named without asserting a runtime defect.
- **G CONFIRMED (SPEC/NOT_RUN).** Own proof that the +1 mutation produces the nonzero first column of M and reaches exactly the identity-failure path.
- **H CONFIRMED static; liveness GAP.** Format arithmetic shows any admissible certificate is under 8.3 MB, but wall/CPU feasibility is unmeasured and no cap change is recommended.

**Custody.** All 15 pins matched before any body was read and again after the final write. Every byte count matches custody.json, and the three sealed body hashes of the charged reports reproduce. The hash rows in the custody file are generated sha256sum output.

**Non-blocking labeling notes recorded:** a p dividing a P7 denominator surfaces as NONINTEGRAL_BASELINE rather than INVALID_PLACE; receipt overflow is STOP rather than INTERNAL_CAP; the checker receipt carries the count 88209, not a per-position list.
DIVERT path=xmodel/f10-source-cone-r3-rank-code-gate-fable5-20260911.md status=CLEAN body_bytes=21454 body_sha256=b30023258e2fd241359ce9cb5fd9764b7a079aee656c447da46256980b2b8eb9
