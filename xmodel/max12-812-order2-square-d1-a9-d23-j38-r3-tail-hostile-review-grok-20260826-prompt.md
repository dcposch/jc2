You are an independent hostile algebraic-geometry referee.  Review only the
frozen D1 `a=9,d=2,3` grade-38 producer below.  Do not edit any producer,
prompt, ledger, audit, or existing artifact.  Write exactly one report:

`xmodel/max12-812-order2-square-d1-a9-d23-j38-r3-tail-hostile-review-grok-20260826.md`

End with exactly `CONFIRMED` or `REJECTED`.

Producer directory:

`cases/max12_812_order2_square_owner_d1_d23_a8a9_j38_r3_tail_20260826/`

Primary custody:

```text
RESULT_A9.md SHA256
65ecc9cb8c4a51fb699e8ff86fdd4d5cb4b7dce744f77a77d2f0c4bc346c01db

PRODUCER_FREEZE_A9.sha256 SHA256
3430b677791372e50b3fc0bc3ad2f504f234832c1f0f1f38f6a891dec64affd1

EVIDENCE_A9.sha256 SHA256
0e953f19aef02184f0dc68e80b0d61deca5c53b5292d7b057744dfd848c20d53

source FREEZE.sha256 SHA256
4a36e6ff0b97800559bbef9b1366ab1e995f2e1deef341b1d009b2d4f7595dc6

source archive SHA256 presented on all six AWS lanes
01cca2d13adfd6685dd628b89f7eace7344b5c778b828d29563f8b8279040f53
```

Claimed scope is exactly

```text
ord(A)=9, ord(C)=11, ord(R)>=9;  d=2
ord(A)=9, ord(C)=12, ord(R)>=9;  d=3
```

after the named square/D1 source gates on `D(p*k0*J)`, through absolute
grade 38.  Audit adversarially and independently:

1. Rehash the producer/source/evidence manifests, all six retrieved lanes,
   and their nested compiled manifests.  Exact `Q` is the theorem endpoint;
   `F_65519,F_65521` are controls.  Check AWS host/tags, rc, validators,
   zero swap, output hashes, and that no Singular `qring` semantics affect
   the computation.
2. Independently enumerate every primitive from all four frozen binomial
   source summands through grade 38 for `(a,c,r)=(9,11,9)` and `(9,12,9)`.
   Confirm exactly thirteen families per cell, pole ceiling three, first
   pole-four grades 42 and 43, padded-cutoff stability, and mechanically
   derived jet maxima.  Look for an omitted higher normal, moving-`p`, load,
   connection, or target jet.
3. Inspect the complete literal source construction and independently check
   the analytic monomial inventory, all-seven-row coefficientwise bridge
   modulo `sigma^39`, every source-jet sentinel, and every row-2/4/6/7
   target through grade 38.  Producer PASS strings are not evidence by
   themselves.
4. Reconstruct the universal pole-three odd functional, including all signs
   and the moving polynomial `p(sigma)`:

   `Phi7+(p/4)Phi5+(3p^2/32)Phi3+(5p^3/128)Phi1`.

   Verify that the complete source contribution is zero modulo `sigma^39`,
   that rows 1,3,5 are target-free, and that the full-equation combination
   is exactly `-sigma^38*J/4` modulo `sigma^39`.
5. Check division by `sigma^38`, extraction of the grade-38 coefficient,
   and the scheme-theoretic unit ideal after adjoining `iJ*J-1`.  Audit the
   exact localization: `J` is source-required/nonzero on the named chart;
   `p,k0` come from upstream gates.  Do not silently claim a theorem on
   `J=0`.
6. Verify the homogeneous `eta` argument for every `ord(R)>=9`: `eta` and
   both leading `R` coefficients must remain uninverted, raising `R` must
   only delay source families, and the pole ceiling/target cancellation
   must remain valid.  Check exact `A,C` language separately.
7. Keep the neighboring `(a,d)=(8,3)` cell completely outside the verdict.
   Its prior 128-GiB failures were resource-only and its high-memory replay
   is separate.  Also enforce every equality-face, other-face, load,
   `p=0`, `k0=0`, global/terminal, order-two, maximum-twelve, and JC2
   firewall.

State the earliest missing source family or smallest failing identity if
any.  Do not use the low-a support miner or neighboring promotions as proof.
