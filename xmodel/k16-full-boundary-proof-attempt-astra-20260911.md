# K16 full marked boundary: bounded all-degree proof attempt

Owner /root/contact_collision_geometry. Actual first action 2026-09-11 17:17:23 UTC. Original publication reserve 17:43 UTC / HARD 17:46 UTC, unchanged. MANUAL / UNREVIEWED.

Exactly four inputs are charged: ROOT TASK and the three named completed reports. All were pinned before body reads. An initial combined tool display clipped; all report bodies were then recovered by separate bounded WHOLE reads before mathematical use. No clipped output is counted as a whole read. No linked file, coefficient artifact, external source or live ROOT work was read.

The target remains the ENTIRE normalized equation with B=eta=1, W(0)=-1, W'(0)=1, U=x^3A^2, deg A=m-2, deg W=2m-1 and the moving leading ratio p=1/(4(2d+1)), 3d^2=m, for every m>=4 and both embeddings. The leading coefficient of A is not fixed by convenience.

## 1. Verdict and exact target

**NO_NEW_MECHANISM.** This attempt proves neither the entire boundary exclusion nor a target polynomial counterexample. The useful bounded output is an all-orders LOCAL control against a proposed multiplicity-only exclusion. It is not a new global implication for marked polynomial solutions.

Work over an algebraically closed field k of characteristic zero. Write

    E = 2xWW' - W^2 +(3U/2-1)W -3U^2/16+3U/4+x.

The unresolved assertion is nonexistence of polynomial A,W satisfying E=0 and EVERY condition in the opening paragraph, for every m>=4. The accepted inputs already supply coprimality of A,W, the rational pole/marking lemma, the norm model and its still-open differential linkage, and consistent leading balance. None is rebranded here as an exclusion. In particular a rationality theorem for arbitrary formal germs is not available.

## 2. Nonzero W-zeros: exact local compatibility

Suppose x=alpha!=0, W(alpha)=0 and U(alpha)=gamma. The value equation is

    alpha = 3gamma(gamma-4)/16.

Thus gamma is neither 0 nor 4. Regarding E as a polynomial in U, with a prescribed local W and its derivative,

    E_U(alpha) = (3/8)(2-gamma).

For EVERY gamma outside {0,2,4} and EVERY prescribed formal W in h k[[h]], h=x-alpha, there exists a unique U in k[[h]] with constant gamma and E=0. Proof: once lower coefficients of U have been found, the coefficient of h^n is linear in its next coefficient, with the same nonzero multiplier (3/8)(2-gamma). All other terms are already determined by the fixed W,W'. This solves successively at every order, not merely through a selected jet.

Since x and U are units in this local ring, U/x^3 is a unit. Choose either square root of gamma/alpha^3 in k. The square-root coefficient recursion has nonzero multiplier twice that chosen root, giving A in k[[h]] with U=x^3A^2. Hence the SQUARE condition causes no local obstruction on this chart, including multiplicities of W.

In particular take the exact constants

    alpha=-9/16, gamma=1, W=h^ell, any integer ell>=1.

Here E(alpha)=0 and E_U(alpha)=3/8. The construction gives a formal full-equation solution with a W-zero of multiplicity EXACTLY ell and A(alpha)!=0. For ell>=2 a direct derivative check gives U'(alpha)=-8/3 and A'(alpha)/A(alpha)=4/3, consistent with the recursion.

This control is LOCAL ONLY. Its formal A is not claimed polynomial or rational. There is no evaluation of that formal germ at x=0, no origin marking, no degree or leading-ratio assertion, and no simultaneous prescription at every finite zero. It therefore does NOT disprove a hypothetical GLOBAL squarefreeness theorem for marked polynomial solutions; it disproves only an inference from the local equation and local square condition alone.

The omitted gamma=2 point is the KNOWN exception alpha=-3/4. Differentiating the full E at a W-zero gives

    2alpha(W')^2 +(3gamma/2-1)W'
       +(3/8)(2-gamma)U' +1=0.

At gamma=2 this becomes 3(W')^2-4W'-2=0. Thus W' cannot vanish and the zero is simple. This agrees with, and is not an advance beyond, the charged boundary-product report. It also explicitly shows why the above unit recursion does not cover that point.

## 3. First remaining gap; stopping decision

The local control has no mechanism for assembling those germs into one polynomial pair, much less the pair with the prescribed origin, degree and moving leader. Conversely it shows why a purely local multiplicity contradiction cannot supply that assembly obstruction. A simultaneous GLOBAL argument using all finite zeros and infinity would still have to prove an additional compatibility failure. No such implication was obtained here.

The attempted differential/divisibility route returned the already-known norm linkage or constraints at individual zeros. Those are not counted as new mechanisms. A proposed descent would have to construct a rational solution or prove rationality before the accepted rational pole lemma could force polynomiality; existence of the formal germ above supplies neither step. Counting conditions, choosing only one leading embedding, setting lambda=1, treating complex coefficients as positive, or discarding the exceptional fibre would not repair this gap.

QUANTITY: the full all-m exclusion/existence assertion in section 1 remains OPEN, with no new bound on m. CHEAPEST TEST in this attempt was manual exact structural reasoning followed by the all-orders local attack above. It detects a failed local inference, not the truth of the target. No finite-degree scan, coefficient artifact, mathematical subprocess or measured scientific runtime exists; feasibility of a complete proof remains UNMEASURED. No further test, descendant or execution is selected.

## 4. Read scope, collisions and custody

The four charged objects and exact pins are recorded in owned PINS.json. The three reports were fresh-WHOLE, not summary reuse. Their initial combined display was clipped; separate bounded reads recovered every line before mathematical use, completed before the observed 17:20:29 UTC clock. Subsequent rereads used only those same charged objects. Historical checker commands appearing inside an input were not executed and their linked files were not read.

Own-target collision checks initially found the final report, its manifest, PINS.json and custody.json absent; the existing ROOT TASK was preserved. The only mathematical claim authored here is the limited local control, manually checked in section 2. No literature/novelty certification, accepted theorem promotion, actual source zero or JC2 conclusion is asserted. Existing norm, square and ramification mechanisms remain at their input scopes. ROOT works independently; no live ROOT result is a proof premise.

The private partial is read WHOLE and the four input pins rechecked before the unique body marker is appended. Publication uses the ordinary lease at the frozen basis above, expected-manifest verification and a sealed WHOLE readback. Final custody binds every charged input and the owned report/manifest/PINS, excluding its own hash. Completion is reported only after all owned writes finish.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7041`.
- Body SHA-256:
  `e4117d5347302393b5e6436908fd571486f57eeb4466d3601a751d4770cce9e6`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
