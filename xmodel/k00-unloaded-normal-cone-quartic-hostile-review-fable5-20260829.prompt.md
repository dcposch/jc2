# Hostile reconstruction — unloaded K00 quadratic cone and tangential quartic

Identity: Fable 5, different-model hostile reviewer.  
Frozen campaign basis: `0f7ee003be45ee40d51d4048897cdacf63821172`.

Read completely and then independently attack exactly this sealed producer:

```text
xmodel/ideation-20260829T2254Z-opus5.md
full SHA-256 d8e7328479232094b1a77ec8560d85e0a70e4288b18e49d506602bd746d6aa36
body 34609 bytes
body SHA-256 2b9e2f08f4e58252d984944a34e9d78fa5b596bfe1a350aadc073ed39d393b30
basis 0f7ee003be45ee40d51d4048897cdacf63821172
```

Review only its Section 1 theorem and the exact consequences that depend on
it. You may read the frozen unloaded prelude and the already sealed radical
producer/review/integration for source definitions, but do not use any Opus
scratch computation as an oracle. Rebuild independently from literal rows.

Mandatory attacks:

1. Verify the triangular polynomial coordinate change
   `(d0,...,d5) <-> (e0,e1,e2,e5,S,T)`, `I subset J^2`, and
   `I not subset J^3`. Check all seven literal rows, not selected rows.
2. Recompute the seven quadratic `J`-initial forms `Q_i`. Distinguish three
   objects rigorously:
   - the ideal generated by these seven initial forms;
   - the full `J`-adic initial ideal `in_J(I)`, which may contain initial
     forms created by cancellations among generators;
   - the actual normal cone `Spec(gr_J(Q[d]/I))` and its projectivisation.
   A claim about the true normal cone is licensed only if you prove the
   seven rows form an appropriate filtered standard basis or independently
   compute the full initial ideal. Otherwise repair every theorem to the
   **seven-quadric leading system** only.
3. Independently saturate the correct homogeneous ideal by the irrelevant
   normal ideal and eliminate the normal variables. Verify or refute

```text
Delta=S^3+S^2+72*S*T^2+64*T^2-432*T^4.
```

   Explain why the elimination gives an `iff` fibre statement rather than
   only closure of a projection; explicitly use projectivity/properness or
   give a specialization-safe certificate. Test positive and negative
   parameter points.
4. Verify generic dimension/length/h-vector/socle claims and whether
   "regular sequence of four quadrics" follows. Check field and
   algebraic-closure language.
5. Verify irreducibility, singular scheme, node/cusps, genus, parametrisation,
   pointwise cone dimensions, the plane `P_0`, and the two local branches.
   Correct any misuse of affine versus projective dimension or arithmetic
   versus geometric genus.
6. Rebuild the claimed exact interfaces with the campaign objects:
   `ell(s,t)`, `A(q)=B(q)=0`, the polar rank matrix, and the normal rank-fan
   ideal `(u^2+64v^2)`. Keep the tangential `(S,T)` quartic distinct from the
   normal `(u,v)` fan and try a coordinate mutation that would expose an
   accidental match.
7. Test the proposed `e=3,m=1` tangential-contact client against the frozen
   fixture if cheap, but treat the post-cutoff G9 closure as outside the
   producer snapshot. This test is optional and cannot promote the cone.
8. Include at least one generator mutation and one independent exact method.
   Desk work must remain under 60 CPU seconds and 1 GiB when predictable;
   if the full filtered-standard-basis or mutated elimination is uncertain or
   heavier, stop and return a precise AWS packet instead of running it locally.

Give itemized `CONFIRM / CONFIRM_WITH_CORRECTIONS / REFUTE / OPEN` verdicts,
the exact maximum theorem safe for promotion, every binding correction, and
the next cheapest discriminator. Agreement is not proof. Equality of sets is
not equality of schemes; a finite cone or jet is not an arc, germ, occurrence,
map, counterexample, or JC2 result.

No web, canonical edits, AWS launch, commit, push, or `jc2-lean` access.
Write exactly one report and no other repository file:

`xmodel/k00-unloaded-normal-cone-quartic-hostile-review-fable5-20260829.md`

End the body with one standalone `<!-- BODY-END -->`; the coordinator will
stamp the immutable seal.
