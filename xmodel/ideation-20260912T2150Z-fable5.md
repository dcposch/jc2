# FULL2150 blind whole-campaign scan — Fable 5.1 (independent)

Lane: ideation-20260912T2150Z-fable5. Blind; ROOT/Astra reports not read.
Inputs: INPUTS.sha256 (b9056f8b380bab15eab954c47bad0e604f83c6cd60eae9e8a158adb37bdede8c),
all seven entries verified OK before reading (sha256sum -c, exit 0); basis
0d39df3c9fd69c939a8420c54d03228b9077777d. Read WHOLE: COMMON, APPROACHES
snapshot, COORDINATION snapshot, PREVIOUS-SYNTHESIS, QUALIFICATION-CLOSEOUT,
EXTERNAL-INTAKE; MASTER46.history lines 257-310. No linked bodies, corpus,
network, interpreter, CAS or scratch. Manual mathematics only. No exit-price
assertion is made, so no charge_basis line (ABSENT expected). JC2 unresolved;
nothing below is a theorem, promotion or execution authority.

## 1. Dispositions over all 46 avenues

Vector (U = unchanged, retaining each avenue's recorded open/closed/refuted
scope and the 0920 allocation modifiers; only two reasoned changes):

1 U (next-r/degree/runtime ladder stays lowered; r3 rows/297 rank UNRUN and
no qualified worker). 2 U (backbone; G2-PSC transport, landing/cofinality
and td ceiling still missing). 3 U. 4 U. 5 U. 6 U. 7 U but see section 3:
A(F) now enters the conductor identity. 8 U. 9 U. 10 U. 11 U (refuted
ladders). 12 U. 13 U. 14 U. 15 U. 16 U (lowered, no named invariant). 17 U.
18 U (closed). 19 U. 20 U. 21 U. 22 U. 23 U. 24 U. 25 U (genus consumer
stopped; residue-A genus 18 stands). 26 U. 27 U.
**28 LOWER** (undo the 0920 temporary raise): NORMAL-NUMERICAL-LOG-1 is now
PROMOTED/CONDITIONAL and the source-line bound r_L>=2d+deg h locates the LC
failure at the original infinity line. The check the raise paid for is done;
no attached candidate contraction matrix exists, so nothing remains to spend
on 28 until a client appears. 29 U (lowered; GMM reformulation is
JC2-equivalent, slice supplies no surjectivity). 30 U.
**31 keep RAISED**: the radial finite graph j_H (RADIAL-CANONICAL-FINITE-
GRAPH-1) and exact conductor I=P_H(f,g,H)R (CANONICAL-CONDUCTOR-EULER-1) make
"B normal iff invertible" the sharpest one-hypothesis reformulation in the
record; the pinching control fixes exactly what does NOT suffice. 32 U (three-
generator presentation; source-unit GAP) but merged with 31 in section 3.
**33 RAISE** (from COSTUME to revived-on-clients): GRAPH-COLLISION-RESIDUE-1
and GRAPH-ASYMPTOTIC-RESIDUE-1 are precisely the "client-specific twisted
residue class" that the 33 stop demanded, and they exclude ALL polynomial
pairs on their graphs without degree bounds. The stop on untwisted divisorial
residues stands; the raise is for residues of source primitives of
target-polynomial forms. 34 U. 35 U. 36 U. 37 U. 38 U. 39 U. 40 U. 41 U
(falsified principle). 42 U. 43 U. 44 U (refuted-as-proof; repaired
membership theorem only). 45 U. 46 U (not a route).

28 and 33 are allocation/framing changes only; 31 retains an existing
raise; no avenue is reopened.

## 2. Bottleneck ranking (proof, disproof)

Proof (by expected information per hour, not probability):
1. **Normality/unit-conductor arrow** (31/32): for an actual Keller frame,
   what constant-Jacobian input forces B=R. The pinching control refutes
   deducing it from finite + unramified + free dualizing + Kahler lift of
   alpha. Section 3 reduces the arrow to two named factors.
2. **Source-specific selection** (BASS first-order selection with tested
   normalization; TRACE A-linear section implies properness). Both are
   "the source must be used" arrows with no producer.
3. **Uniform landing/cofinality** (2, G2-PSC/G2-BD): all-degree, no farm.
4. **r3 positive test** (F10): conditional; blocked on qualification, and
   even success would not resolve JC2 (scalar unitness is only necessary
   for the graded comparison, REG separate).

Disproof endpoints unchanged from 0920 and unreached: (1) one COMPLETE
regular scalar pair on S/T, all Laurent blocks; (2) properness of a COMPLETE
unretired characteristic-zero source ideal with faithful reconstruction; (3)
an actual bounded-support integral AS109 lift (y-degree >= 12 forced). No
input reaches any. Note that endpoint (1) and proof bottleneck 1 share the
source-unit gap (section 3), so one result would move both sides.

## 3. Mechanism verdict and cross-avenue connection

**NO_NEW_MECHANISM.** Evidence: every candidate I could form from the seven
inputs is either a consumer of an existing promoted theorem (conductor,
graph residues, trace image) or a relabelling of a recorded gap (source
units, REG, landing). The best use of capacity is the existing unresolved
normality arrow, sharpened by the connection below, and the already selected
Q(nu)[X] Bezout identity once execution is qualified.

**New cross-avenue connection (31 x 32 x 7), manual, unpromoted, novelty
unclaimed pending checksum.** Let B=j_H(A^2) be the radial finite graph,
P(u,v,w)=0 its irreducible equation, td the geometric degree of F=(f,g).
Off A(F) the fibre of F is reduced with td points (etale), so
P(F(p),w)=a(F(p))·prod_i (w-H(p_i)) up to a constant, where a(u,v) is the
w-leading coefficient. Differentiating at w=H(p):

    P_H(f,g,H)(p) = a(F(p)) · prod_{p'≠p, F(p')=F(p)} (H(p)-H(p')).

So the accepted conductor generator factors on the full-fibre locus as
(leading coefficient of B pulled back by F) times (off-scheme norm of
ΔH=H(p)-H(p')). Two consequences, each hand-checkable:
(i) a is nonconstant and A(F) ⊆ V(a): j_H finite forces H→∞ on escaping
fibre points, so the w-degree drops over A(F). Thus for a counterexample
the conductor's zero locus is the collision curve {ΔH=0 on the off scheme}
pushed to the plane, PLUS whatever of ord(a∘F) along F^{-1}(A(F)) is not
cancelled by the poles of the norm. This identifies the 32 source-unit GAP
(units on off components) and the 31 normality GAP as one gap with an
explicit second term supported on the Jelonek set (7).
(ii) On a swap-stable off component ΔH is odd under (p,p')↦(p',p), so it
cannot be a unit there; unit conductor already needs every off component
swap-free, which is the property of the known pseudo-plane collision L.
Boundary: this uses I=P_H(f,g,H)R as stated in APP §8 and reducedness of
fibres off A(F); it is a factorization, not an exclusion, and says nothing
about which factor fails for a hypothetical source.

## 4. Strongest proof attack and strongest falsification/construction attack

**Proof attack.** Use the factorization of section 3 as the target of the
already promoted Euler/opposite-values lemma: on a generic double pair the
canonical one-form gives opposite E(c) values, and E(c)=E(a∘F)+E(N(ΔH)) on
the full-fibre locus. The lemma already excludes conductors of shape
(h_d - a); the attack is to derive the conductor's shape from (a, ΔH)
rather than guessing it, and to test whether Euler stability of the norm
factor is forced by J(f,g)=1 through the Liouville potential (C(f,g,S)=
C(x,y), dS=x dy-f dg). Type: conditional theorem at best; source necessity
if it only constrains shapes. This is NOT proving normality directly; for
td>=2 the accepted theorem already says B is non-normal, so the only route
is a contradiction from the constrained shape.

**Falsification/construction attack.** S/T remains the endpoint: a regular
scalar pair on T yields a polynomial counterexample on an A^2 chart. The
sharpest recorded restriction is that any regular S pair keeps the swap-free
collision L in its pullback's off square; by section 3(ii) that pair's
radial graph has conductor zero locus containing the L-collision curve,
consistent with non-normality, so no cheap contradiction there. The
construction should therefore be attempted on the full Laurent-block
extension identity, not jets, exactly as APP §7 states; I supply no new
family and select no farm.

## 5. Scientific acceleration / decisive cheap test

Manual, zero compute: verify the factorization P_H(f,g,H)=a(F)·N(ΔH) on
HYPERSURFACE-RADIAL-PINCHING-CONTROL-1 (B=C[z^2,zc,(1+3z)y+9], relation
V^2=U(UW-3V-1)^2, conductor cR). Compute a and the two-point norm by hand
(the normalization is degree-2 birational, so N(ΔH) is one factor) and check
which factor carries the zero of c. Under an hour; bank only after a
different-model FIRST. Success makes the decomposition a usable instrument;
failure means the stated conductor identity was misread and stops card 1.

## 6. Campaign-systems check

Rotation topic: operator/AWS scheduling. **UPGRADE (smallest test).** The
closeout records that metadata routines took seconds while coordination
consumed the 21:20 window; no algebra ran. Proposed: one prewritten,
statically reviewed runbook per worker attempt, with an absolute-UTC phase
table (launch, collector, dummy, science, retirement) whose durations sum to
the retirement window minus the 120-second handoff, an explicit numeric-exit
capture line per phase, and an abort rule at each phase boundary. Smallest
test: a different-model static read of the runbook confirming the sum fits
and every phase names its exit-capture and TERM/KILL observation before any
allocation. No worker, cap change or retry is implied; this is the
closeout's own recommendation made checkable.

## 7. Idea cards (at most three)

**Card 1 — Conductor factorization (31/32/7).** Dependencies: APP §8
CANONICAL-CONDUCTOR-EULER-1 and RADIAL-CANONICAL-FINITE-GRAPH-1 as stated;
etaleness off A(F). Quantity: whether P_H(f,g,H)=a(F)·N(ΔH) holds on the
full-fibre locus and, on the pinching control, which factor vanishes.
Cheapest discriminator: the hand check of section 5 (under one hour).
Outcomes: CONFIRMED → the normality arrow is typed as two named factors and
the source-unit lane and normality lane are merged; REFUTED → drop the
connection, no other consequence; GAP (identity holds only generically) →
record the codimension-one exception set. Stop: one derivation and one
control. Information value: high per hour, because it merges two open lanes
or kills a wrong reading in under an hour; it proves no exclusion.

**Card 2 — Finite graphs relocate the residue mechanism (33/31).**
Dependencies: GRAPH-ASYMPTOTIC-RESIDUE-1's mechanism (residue at a curve with
finite target at infinity). Observation: on a FINITE graph no source curve
has finite target at infinity, so that mechanism is structurally empty; any
residue obstruction must live on the conductor curve V(I). Quantity: whether
the Kahler lift of radial alpha on an actual Keller frame has a residue
along V(I). Cheapest discriminator: the pinching control already has a
global lift with nonunit conductor, so the residue there is zero; this is
the negative control, showing the constant-Jacobian input must enter.
Outcome: either a residue formula in terms of (a, ΔH) or NO_RELOCATION. Stop
after one manual attempt. Value: moderate; mainly prevents a residue farm on
finite graphs.

No third card: the r3 scalar test is already selected, and I have no
honest new construction card.

## 8. Continue / redesign / stop

- F10 r3 / mixed-scalar qualification: STOP allocation until the runbook of
  section 6 is statically accepted; REDESIGN the operator procedure; then
  CONTINUE the single selected Bezout identity, no retry or cap increase.
- Normal-numerical-log / genus restriction (28): CONTINUE as banked, no farm;
  allocation lowered (section 1).
- Radial graph / conductor (31): CONTINUE via card 1 only; no degree, gauge
  or control farm.
- Graph residues (33): CONTINUE only toward card 2; no same-place farm.
- TRACE / BASS: HOLD until a source-specific selection producer exists.
- S/T construction: CONTINUE merged invariant/unrestricted search on the
  full block identity; STOP jets and moment discriminators.
- Block descent d1=2 / non-Galois d2>=4 / primitive: CONTINUE as the
  all-degree proof front; no BD-D2/D3 revival.
- External sweep: boundary-incident follow-up is ROOT's; no blind action.

## 9. Collisions and readback

No new canonical OPEN identifier raised; quantities and cheapest tests are on
each card. Novelty of section 3 is unclaimed and needs the canonical history
checksum (collision-surface-units and canonical-conductor reports are the
first places to look). Readback, word count and postpins precede the marker.

<!-- BODY-END -->
