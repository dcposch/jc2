# Closed-child nine-phase authority templates

First action 2026-09-11 08:41:28.154721347 UTC. Original reserve08:50 / hard08:53 UTC unchanged. Owner /root/model_productivity; basis0d39df3c9fd69c939a8420c54d03228b9077777d.

Metadata-only disabled preparation. No registration, active authority, source contract, code, setup, launcher, worker or scientific execution is created. Only the nine prospective preflight objects are in scope.

## Completed disabled templates

AUTHORITIES.template.json contains exactly the ordered nine keys refuse-status, refuse-caps, refuse-inventory, refuse-source, refuse-hash, valid, startup-produce, startup-check, dummy. It contains no produce, check-positive, mutate or check-negative object. The separate valid.policy.template.json and dummy.policy.template.json are complete equal copies of their corresponding policy objects, not extra phases.

All paths use jc2-closedchild-preflight9-20260911a, and the source job remains f10-source-cone-r3-necessary-rows-v1. The new ROOT source-contract digest and complete source_pins vector are copied from the pinned disabled registration; no referenced body is traversed. Source files and place metadata are unchanged supplied fields, not independently calculated science.

The seven actual-entry refusal algorithms remain literal: refuse-status has DISABLED; refuse-caps changes only source CPU to599; refuse-inventory removes check.py only from files; refuse-source zeroes only files[produce.py], leaving the genuine source_pins vector; refuse-hash retains the normal object but its command deliberately supplies64 zeroes instead of its digest. Startup objects retain the genuine job in metadata, while commands use WRONG-JOB; startup-check alone has check mode and the registered zero input digest. No other authority mutation occurs.

Valid and dummy each have the exact ten policy fields generated by dispatch.authorize, including closed_scope={path,device,inode}. Their paths are the respective direct leaf paths from closed_child_guard.leaves; device and inode remain null. Profiles remain [5,3,8589934592] and [5,3,33554432], respectively. The seven source-authority objects deliberately do not gain a closed_scope field: their exact eleven-key source API is unchanged; the CAPRUN command supplies their closed-scope binding.

## Complete documentary equality checks

Administrative JSON comparisons against a separately assembled transcription of authorize() checked every field of all nine objects, not selected keys. All nine were equal. A separate full ordered command-vector transcription from run() matched every registered vector: the seven actual-entry vectors each have62 arguments; valid and dummy each have64. This includes all closed child/outer paths and identity placeholders, physical placeholders, caps, output paths, setpriv fields, -I -S -B outer flags, -E -s -S -B child flags and both explicit wrong-job/zero-digest controls. No dispatcher method was imported or executed.

Complete JSON readback of the owned bundle equals the prepared nine-object mapping; separate policy readbacks equal the bundle entries. Source-contract source_files equals the registration's complete five-file mapping. The parent remains enabled=false, exclusive_no_concurrent_writer=false, root_no_migration=false; typed_slot_policy is empty and phase_policy is null. Its allowed_phases, command keys and leaf keys are exactly the nine preflights. No active authority is issued by a REGISTERED string inside an incomplete template bundle.

ROOT must complete and authenticate the real physical/native/leaf metadata, then serialize each individual finalized object with dispatch.emit's recursive sorted keys, compact separators and trailing newline. The eight command authority/policy digest placeholders must be replaced by hashes of those exact emitted bytes; refuse-hash's supplied zero digest stays zero. Template-file hashes are custody only and must not be mistaken for finalized per-phase authority hashes. In particular the old valid/dummy policy hashes are obsolete: the new closed_scope leaf identity changes their bytes. No self-hash field or registration cycle is introduced; policies bind the literal ROOT registration path, not its hash.

## Explicit unformed facts and remaining duty

Python/native manifest digests remain JC2_*_PLACEHOLDER strings in source authorities. Host, instance, boot, PID namespace, admission/mathematical/task/worker clocks, native inventory, durable device and immutable outer/child identities remain unformed in the unchanged parent draft. Both policy device/inode values remain null; no device or inode was guessed, coerced to text or observed. Actual ROOT no-migration/no-entrant ownership, initial emptiness, root cgroup namespace/mount, native closure and kernel quiet are future prerequisites, not template evidence. Source/runtime files, limits and algorithms were not modified.

QUANTITY: will one newly authenticated PREFLIGHT_ONLY_9 observation satisfy all seven exact entry refusals, the valid metadata sentinel, and the strict descendant RSS/TERM/KILL plus closed-scope placement/quiet checks? Unknown. CHEAPEST TEST: ROOT's final whole-metadata/pin/argv reconciliation after authentic identity completion; only separately explicit ROOT release can permit the nine controls. Planning wall10 minutes UNMEASURED for that documentary reconciliation, not a runtime forecast or cap change. Failure is STOP/NONDECISION, never authority to retry or start science. No further foundation review or new canonical OPEN is requested.

## Actual read and publication scope

Six inputs were freshly hashed before bodies. Registration808 lines, source contract181, dispatcher744 and both one-line old policies were read human-WHOLE in complete ranges. The old43888-byte AUTHORITIES bundle was completely ingested as JSON; all phase keysets and relevant mutation/binding metadata plus full old valid/dummy objects were displayed and examined. This is MACHINE_WHOLE plus targeted human reading, not a claim of human-WHOLE of that old bundle. No linked files, code imports, mathematical execution, process control, network, worker, fixtures or payloads were accessed. Owned objects received complete readback, metadata comparisons and whole documentary review.

Own targets were absent before begin. Own-only OPEN/collision checks, all six unchanged input postpins, marker-last completion, expected artifact transaction verification and sealed whole read precede custody-first handoff. All writers are IDLE at terminal; this preparation grants no execution or follow-on authority.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6618`.
- Body SHA-256:
  `d79d796bbe1b365bce4cdf4d30ee9ea4c258e6ef12722c5a8ae408126096d3f4`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
