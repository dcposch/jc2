# Hostile review: terminal logarithmic-derivative moments and complete `U=3`

Act as an independent hostile mathematical reviewer.  Read the immutable
target and both charged parent artifacts named in it.  Accept only the exact
statements of the charged, already-reviewed extremal-abc theorem; do not use
the target status, coordinator prose, or any verdict token as evidence for
the new corollary.

Target:

```text
8e1535aac96785ecf72df6ed22cdd239031d89d82e84fbe0d2078ce6b786c5dc
  xmodel/max12-812-terminal-logderivative-pte-u3-classification-20260826.md
```

Audit from scratch:

1. Starting from `W=kappa*A*B/(rad(A)rad(B))`, rederive `T'/T=kappa/P`,
   all partial-fraction residues, the moment equations through degree `U-2`,
   the degree-`U-1` value of `kappa`, and `P'(c_i)=kappa/n_i`.  Attack every
   sign and infinity convention.
2. Audit the converse.  Check that the moment data force equal monic degrees,
   a constant common-denominator numerator, exact degree
   `deg(A-B)=D-U+1`, and squarefreeness of `A-B`.  Look for a missing
   noncollision, nonzero-moment, scalar, degree, or separability hypothesis.
3. Classify `U=3` directly up to affine source transformations.  Verify both
   normal forms, uniqueness, the pole locations `b/(a+b)` and `-b/c`, and
   whether permutations within each sign class account for every duplicate.
4. Independently enumerate exact order two and exact order four profiles.
   Check the claimed totals `3` and `10`, every `D`, every radicand
   multiplicity (including exponent-zero uncharged zeros), and every gcd
   exclusion.  Name the smallest wrong row if any.
5. Challenge the claim that fixed `U` yields a finite exact frontend: state
   precisely what is finite (profiles and algebraic solution scheme) and do
   not infer reducedness or a finite set of geometric points without proof.
6. Enforce the firewall: this is the terminal differential equation only,
   not another Faber tail, Taylor polynomiality, a Keller pair, a whole leaf,
   `(8,12)`, a bound on all `U`, or JC2.

Use source reading and hand derivation only.  Run no CAS, solver, substantive
Python, Lean, or other heavy local computation.  SHA hashing is allowed.
Write exactly one report and edit nothing else:

```text
xmodel/max12-812-terminal-logderivative-pte-u3-review-grok-20260826.md
```

Include model identity, exact target SHA, verdict `CONFIRMED`, `REPAIR`, or
`REFUTED`, the smallest failing identity or missing hypothesis, a complete
independent derivation, the corrected strongest statement if needed, and an
exact scope firewall.
