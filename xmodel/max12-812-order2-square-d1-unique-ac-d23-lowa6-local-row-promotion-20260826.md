# Promotion: eleven low-`a` D1 unique-`AC` `d=2,3` tails

Date: 2026-08-26

Status: **PROMOTED EXACT THEOREM AT THE NARROW SCOPE BELOW.**

## Frozen authority

Producer freeze:

```text
4d69b97d8c6e24baadc76aba69d3d37158a3aa466cf7e44bc3111008c5a07fd3
  cases/max12_812_order2_square_owner_d1_unique_ac_d23_lowa6_local_row_20260826/PRODUCER_FREEZE.sha256
```

Producer result:

```text
04324859fac936e4bca2e114e259d7bcf444491c21d905219a9490deb3dab012
  cases/max12_812_order2_square_owner_d1_unique_ac_d23_lowa6_local_row_20260826/RESULT.md
```

Independent hostile review:

```text
841f0d6ccbb596fa4ace3f08760f039eb288030efa91075112e915d1320592a3
  xmodel/max12-812-order2-square-d1-unique-ac-d23-lowa6-local-row-hostile-review-grok-20260826.md
```

The different-model review ends `CONFIRMED`, rehashes the producer custody,
independently rederives the seven-row/local-root obstruction, and confirms
the timing and firewall.

## Promoted statement

After the frozen square/D1 source gates, in characteristic zero on
`D(p*k0)`, the seven literal Faber equations have no point in exactly the
following strict unique-`AC` contact tails:

```text
(a,d,r_floor) =
  (1,2,2),
  (2,2,3), (2,3,3),
  (3,2,3), (3,3,4),
  (4,2,4), (4,3,4),
  (5,2,5), (5,3,5),
  (6,2,6), (6,3,6),
```

where `a=ord(A)`, `ord(C)=a+d` is exact, and
`ord(R)>=r_floor`.  No leading coefficient of `R` is inverted, so each
listed row is a closed `R`-order tail.

The obstruction is the exact moving-root functional

```text
Phi4+lambda*(Phi3+(p/4)*Phi1)=N(lambda),
lambda^2=-p/2,
```

applied after the first `AC/L` row allocates the exact `A,C` factors to
opposite roots.  Complete source custody leaves one local double-pole
primitive at the terminal grade, `(3/8)C^2/L^2`, and gives the unit
coefficient `(3/2)lambda^2 gamma^2`.  All lower loads, moving-`p`
corrections, seven literal rows, and licensed targets through the respective
terminal grades are included.  Emptiness descends from the finite etale
root cover over `D(p)`.

## Routed cell and firewall

The cell

```text
(a,d,r_floor)=(1,3,2)
```

is not promoted: the independently reconstructed
`-(3/8)R*A^2/L^2` second correction is nonzero and routes that cell to its
own pole-three/local-collision client.  This is not evidence of survival.

This promotion does not include the `a=7` load tie, either `a=8,9`
load-first chamber, an equality face, a positive-order leading load, the
grade-32/34 row-4 target shadows, `p=0`, `k0=0`, another D1 face, a
terminal/global chart, the whole square component, order two, `(8,12)`,
maximum twelve, or JC2.  Nothing here may be extrapolated through those
timing exceptions.
