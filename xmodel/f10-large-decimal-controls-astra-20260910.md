# Separate large-decimal and fresh-checker regression: static source packet

Status: NEW UNREVIEWED STATIC helper. No mathematical execution, generated
fixture/coefficient/candidate, AST/import/compile/syntax check, authority,
worker action, source acceptance, review launch or dependent task.

First action 2026-09-10T04:52:54.809838289Z, own targets absent. Controlling
stop 05:08:54.809838289Z (first+16 minutes, earlier than05:10); final
two-minute reserve from05:06:54.809838289Z. Basis
0d39df3c9fd69c939a8420c54d03228b9077777d is provenance only.

## 1. Deliverable and the repaired test gap

One new large_decimal_controls.py implements three separately authorized
modes: old, new, fresh-check. EXECUTION-INTERFACE.md specifies literal CLI,
existing authority fields, pinned siblings, distinct outputs and root's
future registration duties. No dispatcher, supervisor, acceptance schema,
old scientific module, caller, plan or existing12-case inventory changes.

The accepted repair gate E correctly identified the previous prospective
specification's ingestion omission. Passing giant decimal wires through
input_wire/parse_input before the selected API would hit the old limit in
the test wrapper and fail to distinguish the repaired API. This helper
instead constructs N=10**7421+1 and all Fraction coefficients with integer
arithmetic after authority/pin admission and before any decimal conversion.
It checks4300 immediately before the selected API, with no priming call.

Old mode permits a helper setter ONLY after the actual expected old
decimal-limit ValueError, to serialize/read back the preserved input. New
mode never calls a helper setter: the repaired API must be first. Fresh
mode is a separate exec, imports neither solver nor FLINT, checks4300
immediately before checker.verify and relies only on that entry setter.
The old and new modes are not combined in one interpreter, even though the
gate suggested that as another possibility: the controlling card requires
three separate children and this packet follows it.

## 2. Input and authority perimeter

The card itself and its11 named objects are12 local charged pins, not11
total. All matched current full hashes before bodies or explicitly permitted
same-byte WHOLE reuse. input-pins.json records every path/hash/byte count and
read scope. The new repair gate, card, prospective spec, algebra and gate
source were read WHOLE fresh. Other byte-identical sources and own prior
report use their previous WHOLE reads, explicitly recorded after current
pin checks. No provenance, corpus, ledger, network, live lane or unrelated
body was opened. The entire newly authored source and interface were read
as text; no Python parser or interpreter executed them.

The helper validates MODE/argc/Linux/cwd and hashes the unchanged gate
before importing it. Existing authorize verifies AWS identity, exact child
and CAPRUN parent argv, operation and all declared file hashes. The helper
then checks evidence before import, re-reads authority under its returned
SHA, requires synthetic_only true / exact mode / no source acceptance,
checks the seven fixed siblings, helper/interpreter/input/native pins,
and rechecks all registered hashes before mathematics. Root owns native
closure completeness; the helper does not discover it. It requires exact
inherited16MiB RLIMIT_FSIZE and16MiB read/write bounds. It never raises caps.

All authority fields stay in existing F10-R1-REGISTERED/v1. Root must add
the explicit marker/mode/file-size fields described in the interface and
provide the ordinary python_flint metadata and immutable file vector.
No valid authority or actual descriptor JSON is created here. Metadata
INPUT for old/new is a fixed small descriptor, not coefficient wires;
fresh INPUT at sys.argv[2] is the separately pinned new-mode bundle.

The helper has no capability to start workers or supervise processes. Its
finite per-file checks do not bound serialization's peak allocation or
native computation: registered CPU/RSS/wall/aggregate bounds and unchanged
CAPRUN remain necessary. Root alone selects those caps, observes the dummy
control/actual process identities, enforces original clocks and stops the
worker. An unexpected scientific-stage failure gets an exclusive bounded
STOP/GAP record and re-raises; pre-admission failures may have only CAPRUN
stderr. No broad exception is counted as a passing old test.

## 3. Exact synthetic identity and full checks

Use the pinned literal raw modulus algebra.modulus(), with its monic
normalization for the generic API. Let f1=T^2, f2=1+N*T+T^3, the other
seven generators zero, q=1. These are scalar coefficients in the whole
rank-seven algebra, not a selected component or actual-source row stream.
The minimum pivot is the monic degree2 f1; no coefficient split is needed.
In its remainder module f2=1+N*T and T*f2=T, so the full two-coordinate
multiplier map is triangular. With the retained column ordering the unique
short multiplier for the nonzero nonpivot generator is h2=1-N*T; raw reconstruction gives
h1=N^2-T+N*T^2. The identity is

    (N^2-T+N*T^2)*T^2 + (1-N*T)*(1+N*T+T^3) = 1.

Thus the projected module has14 rational rows,112 multiplier columns,
one RREF and one full rank-seven degree2 leaf. New mode requires those
actual trace fields, but does not use trace as the mathematical verifier.
It calls the unchanged independent DenseFixture verifier on the full
fixture modulus for all217 coordinates. It then parses/checks ALL1638
candidate entries against the literal expected vector: h1 slots0,7,14
are N^2,-1,N; h2 slots182,189 are1,-N; every other slot is zero.
That fixes the actual giant-coefficient path rather than accepting a
different short certificate or merely a status string.

No value N, N^2, modulus or fixture was evaluated or emitted in this task.
Those statements are hand derivation and future source behavior only.

Old mode requires an actual ValueError containing the4300-digit limit and
integer-string-conversion diagnostic. It records full traceback and frame
sites without assuming native construction versus RREF was reached.
Only after that exception and the still4300 check does it set zero for
custody. Any other result, wrong error, changed policy or unexpected
success becomes STOP/GAP, not success. A recorded earlier constructor
failure must not later be described as observed RREF extraction.

Both old/new preserve an arithmetic deep copy before the selected API,
check it unchanged afterward, then serialize it only after the API outcome.
Exclusive read-back is parsed and compared exactly to the original
arithmetic object. Root must compare the two fixture hashes/bytes across
the separate children; a shared input descriptor alone is not that check.

## 4. Fresh checker and meaningful mutation

New mode exports the synthetic ACTUAL-schema {input,certificate} bundle:
literal nonmonic modulus, all nine named dense envelopes
[3,3,2,4,3,3,2,2,5], q=1 in six slots, r=1 in two, h0=1 in five.
Each scalar nonzero coefficient uses the canonical seven-exponent wire;
zero coefficients are empty lists. No binding/execution/source acceptance
is attached to its candidate. The wrapper labels every result synthetic.

Fresh-check uses evidence.load only for JSON structure; the giant numbers
remain strings. Before checker.verify it requires a giant quoted h1
coefficient, full1638 slots, a pristine copy,4300, and absence of solver,
oldsolver and FLINT modules. It then requires the exact independent full
UNIT result and policy0, plus unchanged input. Only afterward does it
convert the old h1 constant and require it equals the literal N^2.

It changes exactly that one rational by+1, checks the arithmetic delta,
restores that slot in a comparison copy to prove all other parsed fields
are unchanged, freezes/re-reads the mutation and calls checker.verify.
The expected failure is exactly ValueError with message
`full 217-coordinate sum h_i f_i equals q^5`: the mathematical residual is
T^2. A decimal-limit error, malformed-wire rejection, missing slot or other
exception is not accepted as this negative control. The full original
INPUT remains hash-stable, and the mutated output is separately retained.

This is an authorized INTERNAL synthetic verify test, never checker.main
or evidence.verified with a fabricated ROOT acceptance. A successful toy
does not prove any actual source ideal unit or source point. The existing
actual semantic-controls program still has its original positive/no-rerun
policy; this distinct required fresh synthetic positive is not inserted
into that program or the old12-case harness.

## 5. Static checks, gaps and stop

Source-text checks establish call ordering, no pre-API setter/decimal
ingestion, all expected witness indices, dense schema padding, separate
mode imports and exact negative reason. Runtime output existence, old
failure site, native behavior, all217 equality, unchanged1638 entries and
fresh-process policy remain UNEXECUTED. There is no claimed syntax/import
check, sampled run, coefficient construction, future timing or performance.

All own output paths are exclusive and bounded, with exact read-back and
hash/size receipts. Current input and own-output pins are in terminal
custody; custody excludes itself. The report is read WHOLE, own-only
raised-OPEN/collision checked before its unique completion marker, then
closed/finalized/verified against its expected manifest.

New canonical OPEN IDs: zero. The smallest remaining discriminator is
the first static review of this exact helper, followed only under new
root registration by the three actual separate children and cross-child
fixture hash comparison. No review, execution, cap selection, promotion
or follow-on is authorized by this packet. All writers IDLE at handoff.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `9799`.
- Body SHA-256:
  `01d1064403a43eb73ff58dbfe60da7f70efd693ee289dcd8136c0eb4fe43ff00`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
