# Repair commission: D43 exact sparse rows v4

You are exact Fable 5 at maximum reasoning.  You produced v3; Opus 5 has now
returned a different-model `REPAIR_REQUIRED`.  Build a **fresh immutable v4
packet**; never edit or reseal v3 in place.

Work only in `/Users/dc/code/math/jc2/`.  Read these exact inputs:

- `cases/d43_exact_sparse_rows_v3_20260828/`;
- `xmodel/d43-exact-sparse-rows-v3-repair-fable5-20260828.md`, SHA-256
  `5759cfe81e21e7e4d3967b96152eaed5e789ed0498b039804b17c30853616b75`;
- `xmodel/d43-exact-sparse-rows-v3-hostile-review-opus5-20260829.md`,
  SHA-256 `6e01e5d7e7852d094559ab57cd7a256c41390fca99e38a6c55fbea8f12209bd9`,
  report-body seal
  `1b90552abddb14bf5946a2e85f0a5ac26dae05d3049ef578db507c766862bd82`;
- the v2 source files needed only to close Opus's explicitly unadjudicated
  v2-to-v3 mathematical-function diff.

Do not run repository-wide status, inventory, search, or build commands.  Do
not access, list, search, build, status, or control `jc2-lean`.  No web, AWS,
Singular, msolve, Sage, or heavy CAS locally.  Bounded Python tests and inert
temporary directories are allowed.  Do not edit canonical ledgers, commit,
or push.

## Output boundary

Create only:

- `cases/d43_exact_sparse_rows_v4_20260829/`;
- `xmodel/d43-exact-sparse-rows-v4-repair-fable5-20260829.md`.

## Mandatory repair set

1. The terminal authority must recompute or bind **every** payload-bearing
   candidate field, not shape-check and copy it.  At minimum bind the lease,
   source/manifest/preflight/pilot/merge/semantic digests to their exact named
   on-disk artifacts, validate the final candidate schema and claim boundary,
   and type/contract-check the exact inventory as non-bool integers
   `29+155=184` with the pinned bands and degree.  Record which fields are
   recomputed and which are receipts; no unexplained copied digest.
2. Resolve the archive path named by the sidecar relative to the sidecar via
   safe-relative logic, require a regular file, and rehash it before any
   positive terminal.  Bind that exact archive digest into the terminal.
3. Refuse every unmanifested member, including directory members.  Put exact
   full-member-set equality at every authority-bearing archive replay.
4. Use a real process-group census parameter in both systemd and fallback
   modes.  Represent an inapplicable cgroup gate as `NOT_APPLICABLE`, never as
   a copied `true`; add gate provenance naming the actual evidence source.
5. Make launcher post-mortem failure authority-bearing.  A positive candidate
   plus nonempty post-mortem census must exit nonzero and emit a separate
   immutable fault object; the evidence must be preserved and linked from the
   terminal/archive contract without overwriting the original terminal.
6. Close the v2-to-v3/v4 byte-diff of every shared mathematical function.
   Distinguish intentional repairs from byte-identical functions and prove no
   mathematical drift beyond the reviewed changes.
7. Preserve the confirmed mathematics: literal collapsed-D21 structural
   equality with digest receipts only, HW-before-multiplication, independent
   f/g construction, one 19-shard/184-row emission, `solve=false`, zero
   production asserts, and optimized-Python fail-closed behavior.

## Required negative controls

Add production-path tests for: each wrong candidate digest value; wrong lease
digest; impossible/string/bool inventory; dangling and drifted archive
sidecar; extra directory and nested directory chain; vacuous pgid input;
copied cgroup gate; positive terminal plus failing launcher post-mortem;
candidate mutation after producer exit; and a semantic receipt changed while
retaining valid hex shape.  Each must refuse through its named gate under
ordinary and optimized Python as applicable.  Retain all old fixtures.

Rebuild deterministically out of tree twice, seal the new packet, and leave
it `LAUNCH_NOT_AUTHORIZED`.  State exact files/hashes, test counts, v2-diff
result, remaining live-Linux debt, and one verdict:
`SOURCE_V4_READY_FOR_DIFFERENT_MODEL_REVIEW`, `REPAIR_INCOMPLETE`, or
`BLOCKED_WITH_EXACT_REASON`.  End with a report-body self-hash.
