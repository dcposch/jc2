# f10-source-cone-direct-place-gate-fable5-20260911 — Fable 5.1 designated FIRST of the direct finite-place source attachment (manual only)

status: UNSEALED (adapter seals; no Seal, no charge_basis authored). First action 2026-09-11T01:38:22Z; both owned targets ABSENT at that instant. Original reserve 01:57:00Z / HARD 02:00:00Z, never reset. Basis 0d39df3c9fd69c939a8420c54d03228b9077777d. Exactly twelve charged inputs in /tmp/jc2-lane.MYmLxd/inputs, unique basenames; all twelve sha256 pins matched the charged list at 01:38:22Z BEFORE any body was read (rows in box/f10-source-cone-direct-place-gate-fable5-20260911/input_custody.md). Every input read FRESH_WHOLE through EOF in one parallel unclipped read per file (reports through their Seal or standalone marker; produce.py 301 lines, check.py 541 lines, check_arithmetic.py 228 lines, arithmetic.py 301 lines, custody.json 129 lines); no clipping occurred, no recovery needed. Static text, manual algebra, sha256 and metadata only; zero CAS/subprocess/import/AST/syntax/compile/test; no coefficient, fixture, candidate, place, provenance, linked, ledger, live or peer body. Accepted imports at stated scope only: complete source-cone17z (A=(B[X]/I)[g^-1]=0 iff the complete normalized r=3 source is empty), the actual ENTIRE degree-seven FIELD B at fixed r3/tau=3/10, conditional rank19z, and source20z STATIC. No coefficient, baseline, place, rank, control or source outcome exists; no operation count, runtime dominance or hour-scale feasibility is imported. Rootcard corrections retained: sourceFIRST's "degree 8" means eight coefficients of a degree-seven P; rankFIRST local x-integrality needs b integral; p-divisible column clearing need not zero its result; lc(S) prime exclusions are conservative. The field-place/all-25 contract is unchanged; no quotient-ring or used-only relaxation is adopted.

## Verdict summary

A CONFIRMED, B CONFIRMED, C CONFIRMED (no smuggled premise), D CONFIRMED (construction/assertion boundary explicit), E CONFIRMED, F CONFIRMED as design with GAP = readiness only, G CONFIRMED. REFUTED: none. Decision: the section-4 statement is a SOUND NEW CONDITIONAL THEOREM (alternative place-admissible completions give 25 rows in ker(B[X]->A) with g' congruent to g, so a nonzero 297-minor of their weight-30 map forces A=0 and, by cone17z, the complete normalized r=3 source empty), distinct from rank19z because it needs neither canonical Bézout representatives nor canonical matrix rank. It is worth ONE bounded implementation review; it does not authorize a run, prime, cap or baseline change.

## A. Local-ring typed-expression evaluation and inverse induction

**CONFIRMED.** Write B=Q[V]/P with P the exact monic rescaling of the septic; O0=Z[1/N][V]/P is free over Z[1/N] on 1,V,..,V^6 because P is monic, so O0 injects into O0⊗Q=B (freeness, not irreducibility, gives the embedding; the candidate's "irreducibility makes O0 a subring" is a harmless imprecision, since irreducibility is what makes B a field). A genuine place pi:O0->k=F_p[V]/phi needs p prime, p not dividing N, phi an irreducible divisor of P mod p; none is selected. Induction: if O is a subring of B carrying pi, and a in O has pi(a)!=0, then a!=0 in B (pi(0)=0), so a^-1 exists in the FIELD B, O[a^-1] is again a subring of B, and pi extends uniquely by a^-1 -> pi(a)^-1, which is well defined because pi(a) is a unit of the field k. Divisions by listed integers with nonzero residue are the same step. Addition, multiplication, fixed-index coefficient extraction in S/theta, differentiation, truncation, the finite binomial `series`, `integrate` (division by e+1), `upper`'s fixed pivots 10j-3(17-h), and the Euler pivots j-3i are polynomial operations with fixed rational scalars, so they commute with coefficientwise pi provided every such denominator is a p-unit; the candidate's item 1 lists exactly these rational pivots. Determinants are integer polynomials in entries, so pi(det M)=det(pi M); nonzero residue of a 297-minor whose entries lie in the enlarged O proves the minor nonzero in O and hence in B. Adjugate inversion of a 3x3 map is covered when det has nonzero residue, and the k-side product check authenticates the numerical inverse only. Fieldness enters at exactly three points: (i) nonzero scalar => unit, which is what lets a single nonzero-residue column coordinate supply a generic completion; (ii) nonzero 297-minor over B => the 297 columns span B^297, which fails over a product base where det can be a nonzero nonunit (the accepted Q x Q control); (iii) in a product base, inverting a scalar that vanishes on one factor discards that factor, so alternative rows would only be congruent on the surviving component. Nothing in the section promotes a modular equality: only NONVANISHING transfers from k to B, and the congruences of C are characteristic-zero theorems proved in A, not observed residues. The candidate says all of this; no modular zero becomes a generic identity.

## B. arithmetic.fi versus nonunique completion; Bézout countercontrol

**CONFIRMED.** arithmetic.py 80-90 (`fi`) runs xgcd(P7,a), demands gcd exactly [1], reduces the cofactor mod P7 and checks the product; the inverse of a unit is unique, and any two Bézout cofactors of a unit differ by a multiple of P7, so the reduced output is canonical whatever the Euclidean path. arithmetic.py 253-261 (`completion`) is two-stage: (g,_,v)=xgcd(P7,c1), (h,u,w)=xgcd(g,c0), l1=u*v, l0=w; the pair satisfies l1 c1+l0 c0=1 but is one representative among all pairs differing by chi*(c0,-c1). It is called only at h=8,9,10 (produce.py 138-139); gap 7 uses the fixed Lambda functional (131-137) and gaps 1-6 the unique adjugate inverse. check.py 379-382 recomputes the pair with its own `bezout` and compares the REPRESENTATIVES (`independent Bezout representatives`), not only line 370's product identity, exactly as the candidate states. Hand replay of the countercontrol with the charged code: B'=Q[w]/(w^2-2), c1=1, c0=w. First stage xgcd(P,[1]) returns g=[1], v=[1]. Second stage xgcd([1],[0,1]): q=[] and rem=[1], then q=[0,1] and rem=[], ending with a=[1], r=[1], t=[], so u=[1], w=[] and (l1,l0)=(1,0). At the place w->3 in F7 (3^2=9=2, so w-3 divides w^2-2 mod 7) the same two calls read xgcd(w-3,1) then xgcd([1],[3]): q=[1/3], rem=[], a=[3], r=[], t=[-1/3], normalized to u=[], w=[1/3], so (l1,l0)=(0,1/3)=(0,5). Both satisfy l1 c1+l0 c0=1, and the residue of the canonical pair (1,0) differs from the modular pair (0,5). So unrestricted modular Euclid does not preserve the canonical representative, and a modular replay compared by check.py 382 would be a false negative. This is a hand example over a different quadratic B', not a selected place, generated fixture or prime recommendation. The delta formula: with lambda*c=lambda'*c=1 put delta=lambda'-lambda, so delta1 c1+delta0 c0=0, and chi=delta1 lambda0-delta0 lambda1. Then chi c0=delta1 lambda0 c0-delta0 lambda1 c0=delta1(1-lambda1 c1)-delta0 lambda1 c0=delta1-lambda1(delta1 c1+delta0 c0)=delta1, and -chi c1=delta0 by the symmetric computation, so delta=chi(c0,-c1) in any commutative ring. Hence x_lambda'-x_lambda=-delta*b=chi(c1 b0-c0 b1)=chi*psi: the freedom is exactly a multiple of the retained compatibility. CONFIRMED.

## C. Source attachment in A: premises available or smuggled

**CONFIRMED; no smuggled premise found.** Let sigma:B[X,z]->B[X] be z->zeta=-c^-1 Hq and Phi the composite to A=(B[X]/I)[g^-1], applied coefficientwise to B[X,z][S,theta]. The recipe from gap 8 onward is a circuit of ring operations, fixed rational scalars, coefficient extraction, `install`, the Euler recurrence, `substitute_z` and B-scalar completions; every step except the completions is identical on both sides and commutes with Phi (A). Gaps 1-7 are unchanged: the six maps use the unique adjugate inverse, gap 7 uses the fixed Lambda pair, and the columns c_h at h=8,9,10 come from `upper(h, const(1), ...)` (produce.py 128) and depend only on C, D, h, so they are the same B-scalars on both sides. Base step h=8: the canonical residual pair after substitution is (-lambda0 psi_8, lambda1 psi_8) by the pure identity b-(lambda*b)c=(-lambda0 psi, lambda1 psi) (uses only lambda*c=1), and psi_8(X,zeta)=K8 lies in I, so in A both Phi(b1)+Phi(x8)c1=0 and Phi(b0)+Phi(x8)c0=0 hold. For any lambda' with lambda'*c=1 in B, Phi(x8')=-lambda'*Phi(b)=Phi(x8)(lambda'*c)=Phi(x8); equivalently x8'-x8=chi psi_8 maps to chi K8=0. Inductive step: A_8', B_8' agree with A_8, B_8 under Phi, so the gap-9 forcing (bilinear in earlier bands), the affine solve `upper` (fixed nonzero pivots), the base pair and psi_9'=psi_9+m psi_8 agree under Phi; K9'=sigma(psi_9') lies in I+(K8)=I, so K9' is in ker Phi and the gap-8 argument repeats; the same at h=10 gives Phi(E')=Phi(E). Then Ahat', E' and hence upperDelta' agree under Phi, the Euler recurrence agrees slot by slot from j=4 down (each bb'[j] is a fixed rational multiple of a polynomial in aa' and higher bb'), so Bhat', J', adjusted', every P1_i', P0_i', the mixed row and P0_0' agree under Phi. Every premise used is accepted or definitional: I generated by the 25 canonical rows (cone17z); Psi7=Hq+cz with c a unit (critical import, unchanged); the canonical lambda_h*c_h=1 (late composition, also produce.py 140); B a field (for generic inverses). The candidate's phrase "the two equations b+cx=0 hold for the canonical source" is precisely their vanishing in A, not an ambient identity, and that is all the proof uses. Comparison is in A, so nilpotents of B[X]/I are irrelevant. Nothing is asserted about ambient identities of the alternative pipeline; see D.

## D. Forcing, resonances, gauges, projections, both inverses, all 25, guard

**CONFIRMED, with the construction/assertion boundary made explicit.** produce.py contains two kinds of nodes. Construction nodes (forcing sums 68-72, `upper` 52-63, matrix solve 84-92, completions 138-141, `install` 164-175, Euler recurrence 181-196, coefficient slots 216-227, graph substitution 249-258) define polynomials; assertion nodes (`equal` at 39-42, 96-101, 140, 145-146, 153-154, 204, 221, 232, 246-248, 251, 263, the `raise` at 58, 62, 171, 193-194, 225, 245 and every `envelope`) only stop the run. For the ALTERNATIVE pipeline the candidate correctly claims Phi-agreement of every construction node and nothing about the assertions. Concretely: the theta^>=2 residual of `upper` (62), the two Euler resonant slots (0,S^0) and (3,S^1) (193), the 81 upper Jacobian slots j>=2 (221), the negative-q coefficients of both inverse charts (245) and the band/Euler equality (204) are, on the alternative side, polynomials in ker Phi that need not vanish in B[X]; a static acceptance of the canonical code does not make them ambient identities, and the candidate does not use them. Its rule "retain the resonance equation, set only the gauge to zero" is the right construction: the gauge assignment is a fixed operation that commutes with Phi, and the retained equation is a further element of ker Phi. Congruences survive coefficient extraction (Phi is coefficientwise on S,theta), resonance projection (a fixed slot assignment), and graph substitution (sigma is a B[X]-algebra map and zeta is unchanged because Psi7 is built before any completion). Source necessity suffices because the theorem needs only F'_j in ker(B[X]->A) and g' congruent to g; it never needs F'_j in I, the mate to exist polynomially, or the inverse charts to be polynomial for the alternative objects. The candidate keeps both inverse-polynomiality conditions and the resonance/support conditions as hypotheses on the CANONICAL construction, where they are accepted, and refuses to replace them by residue tests: correct. All 25 slots and the guard are retained with the same weights (8,9,10; 19..11; 22..11; 27; g' weight 30) because every completion is a weight-zero B-scalar and every other operation is graded. One caution the candidate states and I endorse: a modular run of the UNCHANGED code would abort at 58/62/193/221/245 whenever such a ker-Phi polynomial has nonzero residue, so the alternative circuit must be authored with those nodes demoted to retained rows or diagnostics; this is a readiness item, not a mathematical gap.

## E. g'=g unit, rows vanish in A, weight-30 full rank forces A=0

**CONFIRMED.** Hq is the z^0 coefficient of the unchanged Psi7; ell'=sigma(P0_0') and Phi(P0_0')=Phi(P0_0) by C, so ell' is congruent to ell in A and g'=Hq ell' is congruent to g, the inverted element, hence a unit of A. Each F'_j is in ker Phi by C. Suppose the weight-30 multiplication map of the F'_j (same 1453 column labels (j,M), wt M=30-wt F'_j) has a nonzero 297-minor over B. Since B is a field this is surjectivity onto B[X]_30 (297 = number of weight-30 monomials in weights 1,2,3,4, as counted in the accepted rank gate), so the weight-30 element g' equals sum a_j F'_j with homogeneous a_j. Applying Phi: 0 = Phi(g') = Phi(g), a unit, so 1=0 and A=0; cone17z then gives the complete normalized r=3 source empty. This uses equalities in A and the unimodular-row identity only; no radical, reducedness, rational point, Nakayama or reverse chain. It does NOT give I_30=B[X]_30 or a minor of the canonical 297x1453 matrix: F'_j lies in I:g^infinity, not necessarily in I. The candidate's own scope statement is exactly this, and its rule "do not feed alternative column IDs to the unchanged canonical checker" is required, since check.py binds rows to its own recomputation. No g integrality is needed: only the minor is reduced, g' is used over B.

## F. Smallest extra authentication versus renamed Q expansion

**CONFIRMED as design; GAP = readiness only.** The proposed object is a typed circuit evaluated in k with (1) exact P, rational constants and the place, plus a finite inverse list with nonzero residues; (2) index-by-index attachment of the recipe (ten bands, gap-8 U^2 theta^3 and -U^2 theta^5, gap-10 ell column, full mate, 108/45 inventories, both inverse conditions as canonical hypotheses, 25 slots, guard); (3) the alternative-completion policy or, for canonical rows, a small characteristic-zero trace of the three pairs (c_h are B-scalars from C, D only, so that trace is seven-coordinate rational arithmetic on three columns, not a coefficient baseline); (4) independent reconstruction of the 25 reduced generators and the accepted minor/inverse check. This genuinely avoids Q expansion: the circuit runs in k throughout, and the theorem (C, E) supplies the characteristic-zero meaning of its output without any high-degree Q identity being checked; a "symbolic trace" that re-proves the original identities over Q would not, as the candidate says. Own non-blocking refinement: any k-unimodular pair lambda_k lifts to a B-completion, because a lift l in O has l*c=1+m with pi(m)=0, so l*c is a unit of the local ring and lambda'=l/(l*c) is a B-completion with residue lambda_k; hence the "select a nonzero-residue coordinate" policy is sufficient but not necessary, and a k-Euclid completion is also admissible under the theorem. Readiness: no such evaluator, place, denominator inventory or trace exists in the packet; the pinned code cannot be flagged into one (Fraction wire, check.py 382 literal comparison, aborting assertion nodes per D). No existing code is reusable as a direct modular flag: CONFIRMED.

## G. Changed-object and bad-place controls; unmeasured performance

**CONFIRMED.** Z_(p) control: p and 0 share a residue but differ; a trace that promotes an observed residue equality to a rewrite rule can turn a genuinely zero row into a unit row, and a full modular inverse check on the resulting matrix certifies invertibility of a matrix that is not the reduction of the source, so it cannot repair a missing attachment. The correct discipline is the candidate's: residue equalities are diagnostics, only nonvanishing transfers, and the source attachment is the characteristic-zero theorem of C. Bad place: a genuine nonzero minor can reduce to zero (p dividing a cleared factor, or an unlucky phi), so rank deficiency, refusal or STOP at a doubly-vanishing column residue is a NONDECISION giving no nonmembership, source point or source-zero result. Modular equality is never a characteristic-zero identity here. Performance: nothing is measured, no baseline, place, coefficient height or wall exists, and the candidate claims only a structural reduction of what must be rationally materialized. No source outcome of any kind exists.

## Decision, OPEN quantity, cheapest instrument, numeric planning wall

Decision for ROOT: sound conditional theorem, worth a bounded implementation; not deferred by any exact gap. The only gap is readiness, and it is exact: (a) a k-arithmetic circuit implementing the recipe with the assertion nodes of D demoted (retained rows or diagnostics), the completion policy of section 4 (or the lift refinement of F), the rational-pivot denominator list, and a place with p prime to every listed denominator and phi dividing P mod p; (b) an independent k-side reconstruction of the 25 reduced generators bound to the same circuit contract, then the accepted 297-column inverse witness with its full product check; (c) a separately written manual review of that contract against produce.py index by index before anything runs. The current rational-baseline deployment stays unchanged; the alternative rows must never be fed to check.py.

OPEN quantity (no new canonical identifier): whether a fixed, independently auditable alternative-completion circuit at one genuine place has all scalar inverses place-admissible (twelve named witnesses, six determinants, the three chosen column coordinates, every rational pivot) and an actual nonzero 297-minor of its weight-30 map; equivalently whether A=0 is certified through this presentation. Unknown; no coefficient, rank, place or timing exists.

Cheapest instrument: this FIRST for the theorem (done); next, a 20-minute manual contract review of the authored k-circuit against produce.py, then one single-place, single-trial evaluation with the 88,209-entry inverse check and the +1 witness mutation. Refusal, abort at a doubly-vanishing column residue, timeout or rank deficiency is PARTIAL/NONDECISION; no prime, r, cap or place farm.

Numeric planning wall (own estimate, UNMEASURED, for ROOT's 20-minute planning proposal only, not a runtime assertion and not an accepted import): the modular circuit performs the same dict-sparse products as the pinned producer with one k-coefficient per term instead of seven growing Fractions; on a word-size prime with a degree-one phi that is of order 10^7-10^9 modular operations, plus about 1.3x10^8 field operations for a 297x1453 elimination and 2.6x10^7 for the inverse check. In pure Python that is minutes to roughly an hour; I propose registering a 3600-second wall ceiling for the single trial and measuring before any tighter policy. The wall of the authoring step (a k-arithmetic module mirroring the arithmetic.py interface plus an independent k checker) is a bounded engineering task of order a few hours plus review, not a mathematical cost.

## COLLISIONS

status: EMPTY. Own-only check: this lane wrote exactly this report and box/f10-source-cone-direct-place-gate-fable5-20260911/input_custody.md, both absent at 01:38:22Z; apply_patch only, in bounded writes each under 1500 words (skeleton, A-C, D-G, summary/decision/collisions, custody), then one structural repair rewrite of this file at 01:50Z with unchanged text because the first three section patches had been appended after the last heading instead of under their headings, then the marker; no Seal, no charge_basis, no Write/Edit, no redirection into owned files, no generated text, no input edit, no transaction, no peer, live, shared, protected, provenance or ledger file opened. Zero scientific subprocess of any size; the only executed tools were date, ls, wc, sha256sum and apply_patch. Post-read pins of all 12 inputs verified OK by sha256sum -c against the custody table at 01:49:12Z. Own final WHOLE read of both owned files follows the repair and precedes the marker; the marker line is the only standalone marker and nothing follows it. All writers idle after the marker write.

Own final WHOLE read of both owned files completed 01:51:30Z after the structural repair, before the 01:57:00Z reserve; all twelve post-read pins re-verified OK by sha256sum -c at 01:51:14Z, identical to the charged list; the custody file was unchanged since its whole read at 01:49Z. This completion line and the marker are the last bounded apply_patch write; all writers are idle after it.

<!-- BODY-END -->
