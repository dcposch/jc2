# Research lane: TRIPLE-COVER-CLOSE — the two lemmas that kill the row

You are a bounded primary research lane. The charged r2 report proved:
IF the (6,4) triple cover admits a global power basis (monogenicity)
AND Davenport-Stothers-type cancellation is excluded (forcing
`deg a <= 2`, `deg b <= 3`), THEN no bounded-degree depressed cubic
carries the row data — a uniform KILL of the (6,4) row (hence, with
the provisional Theorem FOLD shear, (8,4) too). Prove the two lemmas:

charged_input=xmodel/pi1s4-64-triple-cover-r2-sol56-20260831.md
charged_input=xmodel/pi1s4-64-fixed-tuple-hostile-review-sol56-20260831.md
charged_input=xmodel/block-descent-a1-na-rowsweep-coordinator-integration-fable5-20260831.md

Your inputs are frozen read-only copies in `{{LANE_INPUTS}}`;
verify these SHA-256 hashes first and stop on mismatch:

```text
c99ffc7f64562ac08e81aa7e927dc04e8c0765182d99db9fdc9f10351772cf9f  {{LANE_INPUTS}}/pi1s4-64-triple-cover-r2-sol56-20260831.md
01ddd1fb65e7bb66cfcf36634682b64fb717384337d1a3b0ba30f4425576faa0  {{LANE_INPUTS}}/pi1s4-64-fixed-tuple-hostile-review-sol56-20260831.md
126c2d2941dcd5b3f95584d0d2cc371dea270ed30494f773abafd4f9e5c20286  {{LANE_INPUTS}}/block-descent-a1-na-rowsweep-coordinator-integration-fable5-20260831.md
```

1. **GLOBAL-MONOGENICITY.** The Miranda triple cover of `A^2 = Spec
   C[x,y]` attached to the `S_3` representation (connected, finite
   flat, reduced discriminant the sextic `D`): its Tschirnhausen
   rank-2 module is free (Quillen-Suslin). Determine whether a global
   power basis exists — i.e. whether the binary-cubic datum
   `(a,b,c,d)` (Miranda Thm 3.6, four coefficients on the free rank-2
   bundle) can be brought to depressed one-variable form `z^3+Az+B`
   by a global change of basis. The obstruction is the binary cubic
   having a global linear factor / the cover being generated by one
   element: analyze via (i) the structure of binary cubics over the
   UFD C[x,y] (a binary cubic form in (u,v) with coefficients in
   C[x,y]: when does GL_2(C[x,y]) reduce it to depressed form? —
   relate to the cubic's discriminant D=b^2c^2-3a^2d^2+4a^3c+4bd^3-
   6abcd = unit * f and its Hessian; over a UFD with all projectives
   free, work the classification), and (ii) if a genuine obstruction
   exists, exhibit it or type the exact residual class of
   non-monogenic covers and re-run the r2 §4/§6 contradictions on
   Miranda's four-coefficient form DIRECTLY (the infinity and node
   analyses may not need monogenicity at all — the r2 report ran
   them on the depressed form; determine whether the four-coefficient
   discriminant admits the same infinity-germ exhaustion; this
   bypass, if it works, makes lemma 1 unnecessary).
2. **CANCELLATION.** Acquire and hash the polynomial Davenport
   inequality literature (Davenport 1965 'On f^3(t)-g^2(t)';
   Stothers 1981; Mason's theorem / Mason-Stothers abc for
   polynomials; Zannier where relevant): for coprime nonconstant
   `a, b in C[x,y]` restricted to a generic line (reduce to one
   variable), `deg(4a^3+27b^2) = 6` with `deg a = m > 2` requires
   cancellation that Mason-Stothers bounds — derive the exact
   consequence: either `deg a <= 2, deg b <= 3`, or the
   Davenport-extremal families (Chebyshev-like) occur — classify
   which extremal families are consistent with the reduced sextic
   discriminant being the ROW's curve (irreducible, three nodes,
   the (6,4,3) infinity type — the r2 report's §4 germs exhaust
   the depressed case; extend to the extremal families). Mind the
   two-variable-to-one-variable reduction: cancellation could be
   line-dependent; make the specialization argument airtight
   (generic line + degree semicontinuity).
3. Assemble: uniform KILL of the (6,4) row (state the theorem with
   every hypothesis), or the exact typed residual.

Consume promoted items and the charged r2's completed sections
(§3-§6 conditional contradictions) at their typed scopes; the
Miranda source must be re-fetched and hashed (the review streamed
it: SHA-256 0bfbaaf77c3c795189d5645466dd3d3ee32b872f5c962309751142d65535f875).

Desk-scale exact reasoning only; you may fetch and hash primary
literature (record exact sources); never run any CAS or computation of
uncertain duration on this machine. Do not edit canonical ledgers, any
charged file, or inspect `jc2-lean`. 6 hours hard budget.

Write one report and no other file:

```text
xmodel/pi1s4-64-triple-cover-close-sol56-20260831.md
```

Create the report file with a skeleton of section headers as your very
first action — the skeleton must NOT contain the `<!-- BODY-END -->`
marker. Append each completed section as you finish it, as a separate
bounded write (aim under 1,500 words per write). Only after the final
section is on disk, append the standalone `<!-- BODY-END -->` line.
If the budget runs short, finish the current section, type the rest
OPEN in one short paragraph each, then seal. Keep it under roughly
6,000 words. Do not include a `charge_basis` declaration.
