# Hostile different-model review — AS F-only D7 post-D10 D9/D8 F3 gate

Work in `/Users/dc/code/math/jc2`.  This is an independent hostile review of
a frozen producer package.  Read in full:

- `xmodel/as-fonly-d7-postd10-d98-f3-20260824.md`;
- every file named by
  `cases/as_fonly_d7_postd10_d98_f3_20260824/MANIFEST.sha256`;
- `xmodel/as-fonly-d7-degree10-pointwise-20260824.md` and its hostile review;
- the corrected divided-carry predecessor and hostile review named there.

Charged hashes are:

```text
producer report  ff85614982fb26a7d8aba2859c7d1be6e3274ff8170114f23b247b5172fd0fc9
MANIFEST         1efda261bcd531fd4352c8b3f27ee2cafd77b7bf36dd31a9379d9083733a8f2b
FREEZE           f03fc06930c12398f714d5af60ed93374ffa80226bf1504d9429747cbdd6fa60
```

Verify hashes and registered replays, but treat those only as regression.
Independently reconstruct and attack every load-bearing claim:

1. Re-expand the integer Jacobian and check the source/divided-carry
   orientation through equations (1)--(3), including the four derivative-
   visible degree-six Frobenius digits, the two invisible digits, and the
   asserted absence of an omitted degree-eight `K/3` term on both licensed
   D10 survivor branches.
2. On the vertical branch, independently regenerate the four degree-nine
   rows and verify the displayed global polynomial section with the affine
   sign convention, including all rank-changing bases.
3. Independently derive the six constant pivots, the triangular coordinate
   change `(p,q,r,t)->(P,Q,R,T)`, the exact `7 x 5` degree-eight matrix, and
   its inhomogeneous column from the original integer expansion.  Check that
   no localization, radicalization, or already-chosen digit is silently
   changed.
4. Prove or refute the operator presentation
   `(X,Y)|->(-AX+BY,-h^3 X-AY)`, determinant
   `Delta=A^2+h^3 B`, and the complete geometric rank-locus statement:
   only ranks `0,2,3,5`, with the exact loci claimed in (8).  Use independent
   minors/radicals or an equivalent proof, not producer PASS strings.
5. Write an independent literal-F3 enumerator with a different implementation
   or ordering.  Reproduce or refute all eight vertical rank-pair counts,
   total `177147`, compatible total `50939`, fibre histogram, and rank-stream
   SHA-256 after matching the producer's documented serialization.
6. Independently reconstruct the `g != 0` endpoint: the three D10 pivots,
   the two D9 equations forcing `f_c=f_d=0`, the exact `13 x 14` affine
   system, all five rank-pair counts, compatible total `954/4374`, structural
   histogram, and documented stream hash.  Check the use of localization at
   `g` and whether either nonzero F3 value is mishandled.
7. Enforce the refusal boundary.  Literal-F3 compatibility is not
   algebraic-closure compatibility, a fixed predecessor may not change old
   Frobenius digits, and nothing here advances the next divided carry,
   exhausts the `a` endpoint/full D7, supplies an all-depth lift, produces a
   characteristic-zero counterexample, or resolves JC2.

Use exact arithmetic and scratch files only outside tracked paths.  Do not
edit any producer, case, canonical, prompt/log/run, coordination, ladder, or
predecessor file.  Do not launch AWS.  Write exactly:

`xmodel/as-fonly-d7-postd10-d98-f3-review-grok-20260824.md`

Give one overall verdict and per-item verdicts from `CONFIRMED`, `GAP`, or
`REFUTED`; report checked hashes, smallest failing identity or missing
hypothesis, exact promotion language, and exact quarantine language.

