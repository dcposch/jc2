You are the hostile different-model reviewer for a proposed narrow Gate-T
coverage composition. Work in `/Users/dc/code/math/jc2`.

Producer:
`xmodel/max12-812-order2-gate-t-drho-unique-ac-ledger-a2d2-composition-sol-20260827.md`
SHA-256:
`1d086a79b0d4e7a6a9905391dc13acf4571d766292c58c7b2f25b1c673141457`

Replay:
`xmodel/max12-812-order2-gate-t-drho-unique-ac-ledger-a2d2-composition-replay-20260827.py`
SHA-256:
`0c124179041b1a3cc115e24d1e5bce4f7ca9edcbc0e3224c302dd20d85b8867e`

Upstream hostile review (consume its correction, not the overclaimed original
sentence):
`xmodel/max12-812-order2-gate-t-kummer-row-bridge-hostile-review-opus5-20260827.md`
SHA-256:
`d62b3f22bcad7de1bacecfa13c455f90bd654b578dd23cc5d2439054993f76f3`

Review by independent derivation. The upstream review found that the old
source map annihilates the stage-zero `J1+J2` root coordinates. This producer
claims to avoid that error by using the deeper shifted pairs. Attack these
items separately:

1. Recompute the strict unique-`AC` ledger, its `s_min`, `G`, and `T` formulas,
   all lifecycle routes, and whether `(a,c,r_floor)=(2,4,3)` really is the
   first cell after `(2,3,2)` in both registered well-orders. Check the
   apparent earlier `a=1` cells are scoped honestly.
2. Check the precise total and B22 rings, `p0=-2*rho^2`, every localization
   factor, both saturations, and the direction of the induced scheme map.
   Do not infer endpoint emptiness from a reversed ring map.
3. Independently derive the shifted finite-jet map for
   `ord(A)=2,ord(C)=4,ord(R)>=3` through terminal grade 18. Verify every index
   and factor in `(aaa*,az*)`, `(ec*,ez*)`, `(rs*,cs*)`, all three loads and
   targets, and prove that every omitted later/source/connection jet is
   unable to contribute. Explicitly distinguish these shifted pairs from the
   annihilated stage-zero pairs.
4. Audit V33 custody: it builds unspecialized general-`rho` rows before a
   later `rho=0` face, but serializes only the face. Decide exactly what this
   does and does not certify. Reconstruct at least the needed grades from the
   frozen source if feasible; do not relabel face bytes as general-`rho`.
5. Independently inspect the B22 endpoint, its four polar primitives, moving
   Hensel root through `ell2`, both allocations, target/load freedom, and the
   terminal functional. Recompute the claimed residue
   `(3/2)*rho^2*cv^2` and verify it is a unit on exactly the stated open.
6. Attack the desk replay's evidentiary value. It mostly checks pins,
   sentinels, ledger arithmetic, and hand-entered Hensel identities; determine
   which central source/endpoint equalities it actually replays and supply an
   independent verifier or explicit documentary derivation for anything it
   does not.
7. Enforce every scope firewall. One contact exclusion must not become the
   whole unique-`AC` fan, generic-square cover, a Rees/terminal-receiver
   theorem, ramified-fibre result, G2-PSC, G2-BD, Gate T, order two, maximum
   twelve, or JC2.

Return separate `CONFIRMED`, `REFUTED`, or `GAP` verdicts for the ledger,
ring/map/saturation, finite-jet completeness, grade-18 custody, B22 endpoint
composition, replay adequacy, and strategic scope. If the theorem is too
strong, state the strongest exact surviving contact theorem and the smallest
repair. Say whether any part is eligible for `AUDIT.md` promotion.

Write the complete report to exactly
`xmodel/max12-812-order2-gate-t-drho-unique-ac-ledger-a2d2-composition-hostile-review-opus5-20260827.md`.
Touch no other campaign artifact. Do not enter, read, build, status-inspect,
or modify `jc2-lean`. Desk-scale exact checks only; no heavy local CAS, no
AWS launch, no web sweep, and no canonical-ledger edits.
