# Independent direct-row residue checker

COMPLETE STATIC CANDIDATE / UNREADY FOR EXECUTION. Owner /root/contact_collision_geometry.
First2026-09-11 03:11:57 UTC. Own report/manifest/box absent.
Original reserve03:39UTC/HARD03:42UTC, never reset.
Basis0d39df3c9fd69c939a8420c54d03228b9077777d.

Exactly12 current-pinned inputs freshly WHOLE-read03:11–03:13 UTC, no reuse
or clipping. Only implementation files check_arithmetic.py/check.py authored; no authority.py or
producer science read/import. Shared metadata API is a prospective dependency
awaiting combined FIRST, not a supplied implementation or execution authority.

No scientific execution/import/AST/syntax/compile/test/CAS/dummy, payload body,
network/AWS/SSH/process/Git/agents/protected/shared work. All tests NOT_RUN.

## Implementation and independence

The two files are a complete STATIC implementation candidate, UNREADY for
execution pending the combined source FIRST and separate ROOT runtime authority.
No authority.py is provided in this box. The only shared campaign-code import is the
agreed metadata-only authorize('check'), returning exactly artifact_path,
contract_sha256, source_pins, place. Its implementation is supplied independently
by the producer owner; this packet neither reads nor accepts those live bytes.
ROOT clarified that the five installed scientific source_pins keys are their
actual absolute installed paths and agree with files[basename]; other reference
keys/vector remain ROOT-frozen. The checker compares the entire returned vector
exactly, without guessing that set or allowing candidate-owned metadata.

The charged old checker/arithmetic serve as accepted literal and independence
references, not as an unchanged executable backend. The new checker does NOT
extract formal forcing bands from the Euler mate, compare characteristic-zero
Euclidean column representatives, parse a rational baseline, or share producer
polynomial arithmetic. All its scalar arithmetic is its own direct reduction
in the authenticated finite field, after its own exact prime/irreducibility/
literal-P7-divisibility verification. It never selects a place.

| Contract operation | Independent implementation |
|---|---|
| Leading series | Coefficient recurrence from c f'=alpha c' f, not binomial expansion |
| Completed matrices | Six signed determinant permutations, adjugate, both inverse products |
| Upper operator/forcing | Fixed coefficient-pair sums, not derivative-expression solver |
| Euler mate | Fresh descending coefficient-pair Jacobian assembly, not producer Q_j text |
| Critical graph | Coefficient Horner substitution, not per-term zeta powers |

Literal tau=3/10 and dL/K/gamma construct the exact degree7 monic P7, constant
coefficient first. The parser binds phi/P7 order and every scalar coordinate.
There are125 distinct ordered denominator records:25 D0,42 D1,58 D2; repeated
numerical values retain distinct names. Every denominator residue and inverse
product is checked. Fifteen scalar inverse nodes retain their actual inputs,
both products and fixed labels: leading/middle/determinants/critical plus the
three independently selected late coordinates. Selection is c1 first, otherwise
c0, otherwise STOP; the unchanged gap7 Lambda is not replaced by that rule.

## Whole circuit retained

Ten formal bands are reconstructed and kept before installation. Full mixed
forcing uses fixed ak0..2/bl0..4 loops, including the gap8 fixed theta3 mate;
early particulars and middle rho correction remain. Critical y=z-U*d0 uses
d0=[theta2]A3; fixed/target terms at gaps7,8,10 and ell's literal variation are
checked. The two low column equations and full/upper residuals stay separate.

Installation uses the fixed(h,j) table, recording its two discarded positions.
The separate Euler sequence retains both prescribed resonance diagnostics and
sets only their entire fixed gauge coefficients to zero. All108 Jacobian
positions,45 lows,63 uppers and22 low-band differences are formed, with zero
and duplicate positions retained. Dformal/Vformal keep their typed Laurent-S
definitions; both full Laurent-q inverse maps and BOTH pole subrecords remain.
Poly carries an explicit coordinate-kind tag: band/raw/laurent_S/inverse/scale/
graph; only named signed axes are permitted. No parameter polynomial is inverted
as a field scalar, and no Laurent auxiliary feeds the formal recurrence.

All150 labelled diagnostics are explicitly indexed and reordered:111 required
canonical-anomaly zeros and39 alternative records, the latter allowed nonzero.
Both pole subrecords are separate, not mistaken for full-polynomial zero tests.
The complete scale expressions and all six comparison groups are computed.
The21/24 low scale arrays are DIFFERENCES, exactly as ROOT's adopted pins require;
their raw source coefficients are not forced to vanish. The guarded top product
uses the explicit original leading coefficients and s^8, never s=1 or z=1.
Every required internal tripwire must pass before CHECKED; agreement alone is
insufficient. Conservative anomaly refusal makes no characteristic-zero claim.

All25 graph rows, five extra polynomials and two scalars are independently
reconstructed, weighted and compared. Psi7=Hq+c*z and its actual inverse are
checked, as are g=Hq*ell and zeta*ell=-c_inverse*g. The mixed row is constructed
as z*P1_0-U*P0_0 BEFORE graph substitution. No diagnostic becomes a new generator.
Source attachment remains conditional through canonical LOCALIZED A at accepted
24z/25z scopes; these are not asserted canonical-rank rows or unsaturated ideals.

## Wire, exact rejection and refusal ceilings

Canonical ASCII decimal-string JSON is round-tripped against its OWN received
bytes only. All graph fields are parsed with strict keys, dense residue arrays,
numerical lexicographic exponents, exact declared weights and no zero sparse
terms. Field/P7/phi data must match ROOT and the literal reconstruction. No
candidate trace, diagnostic, completion, contract or source vector is an oracle.
The ordinary-interpreter guard rejects -O/-OO before scientific imports.

The specified UNEXECUTED control changes only T by adding X1^27 with canonical
coefficient cancellation/insertion and fresh metadata binding. Under a genuine
passing rows object and resource slack, its input-only round-trip and all internal
construction checks are unchanged. Slots0..23 and the extra graph fields still
match; the separate T comparison raises exactly
`CHECK FAILED: direct-source graph slot T` plus one newline on stderr, exit2,
empty stdout. The handler reauthenticates unchanged ROOT metadata and emits no
traceback for that semantic mismatch. No control file or mutator was authored.
An earlier parser/hash/place/resource/metadata refusal does not earn this credit.

Prospective positive stdout is exactly CHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME
plus one newline, exit0. It requires the checker's own full partition and every
displayed comparison, followed by ROOT metadata reauthentication. Refusal stays
STOP/NONDECISION; no rank, generic identity, rational-baseline qualification or
source outcome is minted by this line.

Deterministic refusal ceilings:2<=p<2^31; degree1..7; exact trial division through
sqrt(p), bounded-degree Frobenius/gcd irreducibility;128MiB candidate bytes;
20000 terms per serialized polynomial/400000 aggregate/100000 per internal
polynomial; absolute exponent256 narrowed by type and exact source/graph support.
Both rational field divisions and selected scalar inversions are checked, never
replaced after failure. Recursion, memory, malformed wire and arithmetic errors
cannot yield CHECKED. The agreed metadata limits900wall/600CPU/8GiB/128MiB are
prospective API fields, not supervision supplied here or a measured fit claim.

## Read scope, changed-object control and remaining OPEN

All12 charged inputs were pre-pinned; the card and11 source/design files were
freshly WHOLE-read03:11–03:13 UTC. The original checker was read1–180/181–370/
371–540 through EOF, arithmetic1–227 and authority1–105 through EOF. All design
documents, adopted precedence pins and the dense50-line FIRST were read fully
through EOF/Seal, without clipping. READSCOPE.json records the exact vector.
No old read was substituted, no live producer source was read, and no borrowed
mathematical helper is imported. The only shared dependency is the new four-value
metadata API, explicitly awaiting combined FIRST.

Changed-object control: equality of graph fields cannot certify an invented
trace because Eval_C uses no candidate intermediate. Conversely, omitting trace
serialization does not authorize dropping the150 internal diagnostics, and
scale-low differences being zero does not say all25 source rows are zero.
Changing a late completion need not change every projected residue; faithfully
following the prescribed independent choice is still mandatory.

Existing OPEN quantity: whether both independent implementations of the complete
accepted circuit at one ROOT-admissible place agree and pass all required
tripwires. Unknown; no new canonical OPEN or actual place supplied. Cheapest next
instrument is the single combined different-model STATIC source FIRST against
the frozen12-input obligations and separately completed producer/helper, planning
wall15minutes UNMEASURED. The original30minute authoring figure was a planning
envelope only; actual03:39/03:42 stops govern. No review, run or follow-on was
launched or authorized by this report.

Own-only collision: report/manifest/box absent at first action; only the two
checker source files, owned readscope/report/transaction/custody are written.
All authored writes apply_patch in bounded sections. Existing inputs and peer
files remain unchanged. Own final WHOLE, all12 postpins, OPEN/collision check,
marker-LAST and exact expected transaction verification precede terminal custody.

Final owned source WHOLE review completed03:30–03:31 UTC: arithmetic through
EOF and checker1–180/181–360/361–EOF; report and readscope through EOF. All12
current input postpins match. Own OPEN/collision check complete, no clipping
or scientific validation. No implementation writer or descendant is running;
only bounded documentary publication and terminal custody remain.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `10283`.
- Body SHA-256:
  `ef47fa2161f1c5257210fa396c6c300181b6d474b79546b3a7fa8f211a8d0761`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
