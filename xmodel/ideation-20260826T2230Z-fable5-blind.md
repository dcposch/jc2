All required reading is complete — COORDINATION.md, all of APPROACHES.md (46-row inventory plus every overlay), the AUDIT.md trust-boundary head, the full 2026-08-26 PROGRESS.md section, the 17:58Z LIVE STATE plus 18:29Z/18:50Z events, ladder/REDUCTION.md, both cited xmodel reports, and the 22:30Z freeze packet — plus spot-checks of the newest frozen case artifacts (T-rs/T-cs landing charts, H17/H18 affine cells, TD6 v89h10, D1 a8d3). Here is my independent submission.

---

# Independent full-spectrum JC2 ideation submission — Fable lane, round 20260826T2230Z

**Basis:** freeze packet `xmodel/ideation-20260826T2230Z-model-eval-freeze.md` (HEAD `418e413`, dirty-file SHA set as listed there). I inspected no sibling submission. Evidence-tier language below follows the campaign convention: *promoted* / *provisional* / *producer-tier* / *normalized-chart* / *literal-source* / *global* are kept separate throughout.

## 0. One framing correction I believe the campaign should adopt

The freeze types all maximum-12 work as "method control under the classical total-degree cutoff." That is exactly right for the **counterexample side and for every fixed bounded envelope**: GGV/Heitmann (`gcd(deg_tot P, deg_tot Q) >= 16`) closes those. But the classical cutoff does **not** close the partial-`y` `(8,12)`/`(9,12)` families at unbounded total/coefficient-`x` degree, and the promoted ledger already contains "every Keller pair with maximum actual partial `y`-degree ≤ 11 is an automorphism" (dual-confirmed, 2026-08-24 19:32Z) plus the genuine unbounded-`U` elimination of the entire `(8,12)` exact-order-four leaf (`0ac7da1d…`/`4ac21da8…`/`d0b2e9bc…`). Consequently the current normalized `(8,12)` order-two pile is not merely machinery hardening: **it is conversion-eligible**. If the literal landing program (corrected `pTot` source, six-chart obligation table `50db2706…`, T-rs prefix certificate, T-cs, receiver, glue) closes, and the remaining order-two/order-one fans compose with it, the pile converts into progress toward a real frontier theorem — partial-`y` ≤ 12 — which no bounded-envelope replay can ever be. This raises the correct priority of the landing lane above "protected global reserve" status and simultaneously means the pile's realized value is *contingent*: without T-cs/receiver/glue it stays a normalized-chart archive. Nothing in this observation licenses inferring a source theorem from any normalized chart; it only re-prices the conversion step. (Partial-`y` ≤ 12 would still not touch the REDUCTION.md walls — off-axis coverage, post-jump `M≥2`, composite single-pole, no `td` ceiling — and is not JC2.)

## 1. Disposition vector (all 46 avenues)

| # | Avenue | Disposition | Reason (changes only, others one-line) |
|--:|---|---|---|
| 1 | GGV corner farm | unchanged | Necessary-condition mill; no new event. |
| 2 | Sigray sheet ladder (incl. max-12 program, landing) | unchanged rank, **internal redesign** | Still the backbone; but see lane recommendations — serial affine equality cells inside it should stop after H18 review (§7), and the Sigray-frame coverage debts (REDUCTION.md CRITICAL 4–6) remain unowned while proxies run. |
| 3 | Strip ODEs / residue functional | unchanged | Scope-limited theorem; no client. |
| 4 | Formal-germ D-series | unchanged | Quarantined/parked correctly. |
| 5 | JvdK descent | unchanged | Cusp leading forms still block; control experiment low value. |
| 6 | Abhyankar–Moh | unchanged | Category error documented. |
| 7 | Jelonek A(F) | unchanged | `JUMP-ONLY/TYPE-FAIL` stop stands. |
| 8 | Formal-inverse combinatorics | unchanged | No uniform truncation bound. |
| 9 | Lee–Li E | unchanged | No finite reduction. |
| 10 | HC4 bridge | unchanged | `NO LEVERAGE` stop stands. |
| 11 | Mathieu/GMC ladder | unchanged | Refuted. |
| 12 | Face isolation mod p | unchanged | Low. |
| 13 | Dixmier DC(2) | unchanged | Deg-4 black hole. |
| 14 | End(A₁) audit | unchanged | Defensive recon only. |
| 15 | Commuting PDOs | unchanged | Dictionary incomplete. |
| 16 | D-module/holonomic index | unchanged | Still no named invariant; my new mechanism (§4) is adjacent but distinct and cheaper. |
| 17 | BCW cubic stabilization | unchanged | Category error. |
| 18 | Graded/GIT | unchanged | Closed (Shaska). |
| 19 | Char-p + Witt lifting | **raise (reopen the bounded discriminator)** | The named next discriminators — quantitative conductor growth and the polar-divisor/`A_infinity` comparison (2026-08-24 14:03Z overlay) — were never run; the disproof side currently holds **zero** capacity (§2). Bounded, cheap, and the only live wall on that side. Card 3. |
| 20 | p-curvature formalism | unchanged | Bridge consumes false statements. |
| 21 | p-adic Hensel/model theory | **raise (jointly with 19)** | `A_infinity = 0` is one of the two missing global keys of the AS109 reduction and is exactly what the conductor comparison measures; same client as 19, not a separate lane. |
| 22 | Siegel integral points | unchanged | Wrong direction. |
| 23 | Analytic inverse | unchanged | Fatou–Bieberbach. |
| 24 | Real/Pinchuk | unchanged | Orthogonal. |
| 25 | Monodromy passports | unchanged | The DZ weighted-tree frontend is this technology succeeding *in the terminal frame*; the coupled two-cover CSP on residue-A remains untried but is not the cheapest ceiling client (see 26). |
| 26 | Primitive-monodromy td bound | **raise** | The cofinal-`td` ceiling is a terminal proof wall that has received effectively no registered work for days; Stage-1 (GAP/Magma passport enumeration for td = 6..9 with the Orevkov (48,64) negative control built in) is a weekend-scale AWS job, was the consensus untried pick of all three surveys, and is the only cheap experiment aimed at that wall. Expected outcome is discipline rather than a kill; either way it prices the wall. |
| 27 | Links at infinity | unchanged | Template survives; moduli explosion. |
| 28 | Log/BMY | unchanged | NEEDS-DATA. |
| 29 | LND/Hamiltonian | unchanged | Scoped stop. |
| 30 | ML invariant | unchanged | Doesn't see embedding. |
| 31 | ZMT/Rees integrality | **raise (as mechanism donor, not as the slogan)** | The landing program *is* Rees-algebra boundary technology; S16's "Rees valuations of one complete boundary book" is effectively what the six-chart obligation table does. The Rees-valuation frame is the natural language for the prefix-ceiling meta-theorem (§5, connection (b)) and should be credited/consulted, not left marked "No direct attempt." |
| 32 | Collision ideal | unchanged | Accelerator only. |
| 33 | Symplectic residues | unchanged | `COSTUME` stop. |
| 34 | Tangent sweep/pole removal | unchanged | No 2D absorber. |
| 35 | Dim-3 descent | unchanged | Structural silence. |
| 36 | Guided CE search | unchanged | The honest version lives inside 19. |
| 37 | Finite-field census | unchanged | Won't lift. |
| 38 | Tropical | unchanged | Newton-named work suffices. |
| 39 | Cohomological cluster | unchanged | Still no 10-line global class. My §4 proposal is *local duality of the campaign's own complexes*, a different mechanism; I deliberately do not claim it revives row 39. |
| 40 | Free-associative | unchanged | Correction problem is the problem. |
| 41 | Naive scaling | unchanged | Falsified principle. |
| 42 | Markus–Yamabe | unchanged | Wrong arrow. |
| 43 | Ritt | unchanged | Collapses to GGV. |
| 44 | Moskowicz | unchanged | Closed as proof input. |
| 45 | Differential Galois | unchanged | Tautological. |
| 46 | Lean/formal | unchanged | Asynchronous, off critical path. |

Net: four raises (19, 21, 26, 31), no lowers, no full reopens beyond 19's bounded discriminator, forty-two unchanged.

## 2. Reranked principal bottlenecks

**Proof side, in order:**
1. **Literal landing conversion** — T-cs chart, closed receiver `V(rs,cs,c0,c1,a0,a1)`, base-change/glue, six-chart cover. Not because it is a template, but because it converts the promoted normalized pile (§0). T-rs is one chart at one grade-12 prefix on `D(k)` (confirmed per freeze item 5); five charts, overlaps, and the receiver remain.
2. **Affine-Faber ladder termination** — the H15→16→17→18 equality climb has no well-founded ranking; the root synthesis correctly demands one. Cost per cell grows: the gap between the load wall (~`14H/5`) and the intrinsic cubic (grade `3H`) grows like `H/5`, so serial cells get strictly more expensive and finitely many cells can never close the regime. Card 1.
3. **Cofinal type/topological-degree ceiling** — untouched for days; KJN is type-relative and needs a bounded `(α,β)` menu that nothing supplies. Avenue 26 Stage-1 is its only cheap probe.
4. **Sigray-frame coverage debts** — REDUCTION.md CRITICAL 4–6 (typed total landing map, off-axis `0/0/2691 OPEN`, post-jump `M≥2`, composite single-pole survivors at every composite d). These are the actual JC2 walls; they are currently serviced only indirectly via the landing-template bet, and that bet's transferability (Rees charts of one explicit collision ideal → decorated Eggers–Wall book landing) is looser than the prose suggests and should be re-audited once T-cs closes.
5. **G2-PSC** — owed only by a hybrid architecture; the pure-Sigray bypass remains the cheaper architecture and nothing this round changed that.

**Disproof side, in order:**
1. **Bounded-state/all-depth wall, quantified** — the residue-ball theorem makes "complete fixed-support all-depth lift ⇒ complex counterexample" the whole game; the escape theorem forces `y`-degree ≥ 12 and growing support; the named quantitative discriminators are unrun. Card 3.
2. **Unbounded-`U` lower-tail obstruction via a tree-local identity** — the websweep's own proposal (sign/leaf-removal/cut identity over weighted trees) is the only mechanism on the table that could close all `U` at once; it is currently a "conjecture generator" without an owner.
3. **Algebraization of any surviving normalized family** — currently moot: no normalized family survives its reviewed gates, which is itself the strongest current evidence that the disproof side must leave the bounded envelopes (consistent with 1).

## 3. Post-17:40Z delta, as I read it (typing check)

All per the freeze: H15/a3 fan, H16/q6/a4 equality, H17 open-interval kills, and H18/q6/a6 (grade 54) are **promoted/confirmed normalized-chart** results; the H17 `q=7/a=3` formal Kummer lift is promoted *formal*, and the source-descent congruence `3 | α·H` blocks its rational-source reading — I verified the H18/q7/a4 artifact itself declares "cannot be cited as rational-source accessibility." D1 is promoted through the eleven low-`a` blocks, `a=7`, `a=8,d=2`, both `a=9` tails, and the exceptional pole-three `(1,3,2)` tail; `a=8,d=3` is a genuine memory wall (triple-host rc=14 at 128 GiB), and the attempted universal odd functional there is **refuted** — do not reuse. TD6 has the promoted q14 cokernel functional, its q13 polynomial extension, H4 total-`F`, the all-22-q unitriangular flag and 94-variable P12 normal form; H14 **refutes** the P12-only global q-radical exclusion; the P13 normal form and the `E3` coordinate-12 unit `(3500000000/9)·(U/V)` are **producer-tier only** pending H15/H18/H19 audits. Landing: T-rs grade-12 prefix ρ-unit is a confirmed one-chart/one-prefix certificate; T-cs v18 is preregistered but has no result yet (I checked the case directory: PREREGISTRATION and FREEZE only).

## 4. Genuinely new avenue/mechanism: intrinsic residue/duality formulation of the tail functionals, with a base-change covariance test

**Claim of novelty, carefully scoped.** The root synthesis already names "the splitting-independent cokernel class of the reduced P12 remainder" as the right TD6 invariant. What no file proposes is the cross-lane identification and its landing consequence: the decisive object in *every* current lane is a one-dimensional cokernel/unit functional of a finite graded predecessor complex — TD6's q14 class `N/[V³(V²−4U³)²]`, D1's terminal `−J/4` odd functional, the affine intrinsic cubic `−E·λ³M³/16` at grade `3H`, the cusp unit `E_(6,12)`. **Mechanism:** conjecture that each is the Grothendieck residue pairing of the source connection against the dualizing module of its predecessor complex — i.e., values of one intrinsic residue map, coordinate-free by local duality. This is not row 39's global-class slogan (no global vanishing theorem is claimed); it is local duality applied to the campaign's own finite complexes.

**Why it matters:** if true, the functionals are *covariant under chart base change with an explicit dualizing twist*. The six-chart atlas then needs one covariance lemma plus one per-chart prefix computation instead of six full independent replays, and every future receiver inherits its obstruction class functorially — precisely the missing structure the obligation table currently simulates by hand. If false, that is equally load-bearing: the functionals are chart artifacts, cross-chart reuse is illegal, and the landing program must know that *before* it glues. Card 2 operationalizes this; it is also my "decisive experiment."

## 5. New cross-avenue connections

**(a) D1 generating-transform → affine-Faber ladder termination (new composition).** D1's reviewed universal Laurent-to-Faber generating functional (promoted `0263315e…`/`dbdd2b80…`, with its per-application source-ceiling caveat) turned serial pole-order cells into one closed-form family whose coefficients are truncations of `(1−(p/2)T)^{±1/2}`. The affine ladder is structurally parallel: promoted instances place the intrinsic obstruction at grade `3H` in both closed cases (H15: `[s^45](E·H3+H5) = −E·λ³M³/16`; H18: `H54 = −2m³p² + (5/4)κ·a4³·p⁷`), with load interference entering at affine-linear grades in `H`. No file proposes seeking the `H`-parametric generating functional; the root synthesis asks only for "a well-founded ranking or an explicit transition cycle." Card 1.

**(b) Chartwise prefix ceilings ↔ `G2-BD` (new analogy, honestly labeled speculative).** The obligation table observes that a finite decisive-grade prefix certifies an individual chart (T-rs closed at grade 12). A *uniform* prefix-ceiling theorem — an Artin–Rees-type bound on decisive depth computed from the Rees ideal generators, in the row-31 valuation language — would (i) turn the atlas into six certified-ceiling finite jobs and (ii) be the first worked prototype of a bounded-delay statement of exactly the `G2-BD` flavor: finite determinacy of a valuative gate with an effective constant. I flag this as a design target for the landing owner, not an idea card: it should be attempted only after T-cs returns, on real data from two charts rather than one.

**(c) For completeness of the checksum:** the DZ-tree ↔ terminal frontend connection and "labeled DZ permutations as a G2-PSC sheet bridge" are KNOWN (websweep `bb1aa29f…`; 02:07Z synthesis), and the bridge reading is a SCOPE-CONFLICT if stated as landing — no transport theorem exists from the Faber terminal frame to Sigray tree data. I do not advance it.

## 6. Strongest attacks

**Strongest proof attack.** Finish the first literal atlas instance: T-cs chart certificate, the closed terminal receiver, and the T-rs/T-cs overlap glue, under the reviewed fail-closed compiler discipline (`51c294e0…`), with Card 2's covariance test run in parallel on frozen data. Rationale: it is the unique step that converts promoted normalized-chart algebra into a literal-source theorem class (§0), it exercises the exact machinery every later landing obligation needs, and its failure modes (a missing source term, a non-covariant functional) are each individually informative. It is explicitly *not* a JC2 attack by itself — even a complete fixed-book atlas leaves `G2-PSC` (if invoked), full landing beyond this chart family, and the cofinal ceiling.

**Strongest counterexample/falsification attack.** Card 3: the quantitative conductor-growth / `A_infinity` comparison on the confirmed all-Witt growing-support seed. The residue-ball theorem says any complete fixed-support all-depth lift already transfers to a complex counterexample; the escape theorem says such a lift must leave every bounded envelope. The only honest way forward on the disproof side is to *measure* the growth the escape theorem forces and test whether any polynomial-compatible grammar survives. Everything else on that side is either closed (bounded-`y` no-gos) or ungrammared (`NO-FROZEN-GRAMMAR`).

## 7. Software acceleration (one)

**Port the row-streaming layout to the D1 `a=8,d=3` producer, and let that port be the mandated D1 half of the shared-IR roundtrip.** The landing lane's streaming V2 processes one total/frozen row at a time at ~0.5 GiB where the simultaneous layout used tens of GiB (18:50Z event). The single promoted-composition blocker in D1 is exactly a memory wall (rc=14 at 128 GiB, three hosts, zero swap). The root synthesis authorizes a shared sparse IR only after "one D1 and one affine example roundtrip exactly" — choosing `a=8,d=3` as the D1 example discharges that policy requirement and unblocks the strict `d=2,3` composition in one move. If the 512/384-GiB relaunches also fail, this port is the mandatory redesign under the two-non-informative-attempts rule; build it now, not after.

## 8. Idea cards (three)

### Card 1 — Kill the ladder, not the cells: `H`-uniform mixed-cone functional
- **Target obstruction:** non-termination of the affine-Faber equality-cell climb (`H=16,17,18,…`).
- **Mechanism:** on the congruence-sieved sublattice (the `3|α·H` source-descent congruence already excludes slope-four rational-source readings when `3∤H`; freeze item 3), seek an `H`-parametric generating functional whose grade-`3H` coefficient reproduces the promoted intrinsic cubics and whose earlier coefficients absorb the legal load freedom (`d6`-type), in analogy with D1's `(1−(p/2)T)^{±1/2}` truncation theorem.
- **Dependencies:** promoted homogeneous cone (`9c537ad4…`/`bde78801…`); promoted universal odd-pole generating functional (`0263315e…`/`dbdd2b80…`) including its caveat that each application owes a source ceiling and target placement; promoted H15 fan (`da35a8c2…`/`ce4fa70f…`); frozen H16/H17/H18 support JSON; the congruence erratum.
- **Cheapest discriminator (no new elimination):** parse the already-emitted H16/H17/H18 supports and test whether obstruction grades and load-arrival grades are affine-linear in `H` at fixed slope data, and whether the `3H` functional's shape is stable across the two promoted instances. Hours of parsing on frozen artifacts.
- **Outcome interpretation:** *aligned* → compile the symbolic-`H` candidate and preregister an a-priori prediction of the next `3|H` cell's functional (H=21); a confirmed prediction is strong evidence for a uniform theorem and the ladder collapses to one review. *Misaligned* → the regime genuinely changes shape with `H`; serial cells past H18 are then provably non-informative per unit cost, and the correct successor is the root synthesis's transition-cycle search — either way the ladder stops being an open-ended climb. I explicitly do **not** infer termination from the finiteness of observed transition types; the card's entire point is to replace that temptation with a testable uniform object.
- **Stop condition:** two non-informative symbolic attempts, or one failed a-priori prediction.
- **Expected information gain:** high — decides whether one owner-lane closes by theorem or must be redesigned; bounds future spend on a family whose per-cell cost grows linearly in `H`.

### Card 2 — Covariance-or-artifact: transport one promoted functional through the literal T-rs base change
- **Target obstruction:** the six-chart atlas currently requires independent full replays per chart; and the landing program has no theorem that its receiver obstructions are chart-independent.
- **Mechanism:** §4's residue/duality conjecture, tested at its cheapest point: pull one already-promoted receiver functional (the cusp unit `E_(6,12) = −(21/1024)d³u²`, or D1's `−J/4` class) through the literal T-rs base-change map and compare with the chart-side recomputation up to the predicted dualizing/Jacobian twist.
- **Dependencies:** confirmed T-rs grade-12 prefix certificate (freeze item 5; case `…t_rs_rho_unit_v16`); promoted cusp certificate (`fadb2794…`/`d1a0d83e…`/`cf03801e…`); promoted q14 splitting-independence (`49b9dcba…`/`99f7124e…`/`7d1709d0…`) as the intra-chart precedent; obligation table `50db2706…`. All inputs frozen; one small exact-Q AWS job.
- **Outcome interpretation:** *covariant* → atlas cost collapses (one covariance lemma + per-chart prefixes), and the landing program gains a functorial invariant to carry into every later receiver; this is also the first structural (not computational) fact about the landing interface. *Not covariant* → the functionals are chart-local artifacts; cross-chart reuse is illegal and every chart owes its own obstruction derivation — a scope firewall worth erecting *before* the first glue, at the cost of one job. Both outcomes are consumed; neither promotes anything by itself and the result must not be read as a landing theorem.
- **Stop condition:** one clean pass/fail; no speculative theory-building without the pass.
- **Expected information gain:** highest per dollar in this round; doubles as my decisive experiment.

### Card 3 — Reopen falsification: conductor growth vs `A_infinity`, bounded
- **Target obstruction:** the disproof wall (bounded all-depth state + algebraization) has zero live capacity; its two named discriminators were never run.
- **Mechanism:** on the dual-confirmed all-Witt growing-support Artin–Schreier seed, compute the canonical-gauge truncation degree/support size `d(k)` for Witt levels `k = 1..8`; extract the exact polar-conductor recurrence; attempt a growth lower bound; compare the limiting exterior polar divisor against the `A_infinity = 0` requirement of the AS109 tube split.
- **Dependencies:** AUDIT entries "UNRESTRICTED-SUPPORT ALL-WITT ARTIN–SCHREIER CONTROL (2026-08-24)" and "AS109 GENERIC DEGREE, `A_infinity`, AND DECK-DESCENT REDUCTION"; the dual-confirmed polar-divisor no-cancellation theorem; the 14:03Z overlay naming these as "the next discriminators." No new grammar search is licensed or needed.
- **Cheapest discriminator:** the first five `d(k)` values — pure exact arithmetic, hours.
- **Outcome interpretation:** *provable super-linear growth in every gauge* → a quantitative no-polynomial-lift theorem for this seed class and a template for attacking the bounded-state wall generally (proof-side value from a disproof lane). *Observed plateau or structured sub-growth* → measures the exact support grammar a polynomial lift must exploit, repairing `NO-FROZEN-GRAMMAR` with data instead of guessed caps and restarting the honest counterexample search at `y`-degree ≥ 12 where the escape theorem says it must live.
- **Stop condition:** hard cap at level 8 or two consecutive non-informative levels; the standing prohibition on indefinite digit-chasing stays in force.
- **Expected information gain:** restores the falsification reserve COORDINATION requires, at trivial cost; every outcome updates the only live disproof wall.

## 9. Lane recommendations

- **Root / landing (T-rs → T-cs → receiver/glue):** **continue, raised priority** (§0, §6). Add Card 2 in parallel; attempt connection (b)'s prefix-ceiling theorem only after two charts exist.
- **D1 owner:** **continue to completion, then stop serial expansion.** Finish `a=8,d=3` via the 512/384-GiB relaunch with the streaming port (§7) prepared as the mandatory fallback; land the strict `d=2,3` composition review; do not open new contact families beyond it.
- **Affine-Faber owner:** **redesign after the H18 review.** Run Card 1's frozen-support discriminator before any H≥19 registration; serial equality cells continue only if the discriminator fails *and* a transition-cycle argument is registered in their place.
- **TD6 owner:** **continue to the binary gate.** The E3 coordinate-12 unit is the sharpest pending discriminator; obtain the H15/H18 reviews and the H19 raw original-FIRST/source membership audit. No new coordinate-pivot searches (per root synthesis). Reassess the whole lane at that verdict.
- **Terminal / weighted-tree (order-one and tree pipeline):** **continue at low intensity, but assign an owner to the `U`-uniform tree-local obstruction identity** — it is the only registered mechanism addressing unbounded `U` and is currently ownerless.
- **AS / falsification:** **reopen bounded** (Card 3).
- **Formalization:** **continue asynchronous**, unchanged, `jc2-lean` untouched by campaign writes.
- **New fixed-envelope max-12 registrations:** **stop by default** unless each names its conversion client or control role at registration, per the frontier gate; the pile is large enough that marginal normalized cells without a landing consumer are negative-value (review load, custody surface).

## 10. History checksum table

| Proposal | Label | Basis in the record |
|---|---|---|
| `H`-uniform generating functional for the mixed affine regime (Card 1) | **NEW** (composition of known components) | Components promoted separately: universal functional (AUDIT 17:06Z, `0263315e…`/`dbdd2b80…`), homogeneous cone (`9c537ad4…`/`bde78801…`), `3\|α·H` congruence (freeze §3), H18 receiver (case `…h18_q6_a6_g54_direct_unit_v1`). No file proposes the cross-lane transfer; the root synthesis asks only for "well-founded ranking or transition cycle." |
| Residue/duality covariance functor + T-rs transport test (Card 2, §4) | **NEW** (mechanism); adjacent sentence exists | Root synthesis §2 names the "splitting-independent cokernel class" for TD6 only; no cross-lane identification, no base-change covariance test anywhere in `xmodel/` or AUDIT. Distinct from row 39's global-class program. |
| Conductor growth / `A_infinity` comparison (Card 3) | **KNOWN — unrun** | Named as "the next discriminators," APPROACHES overlay 2026-08-24 14:03Z; AUDIT all-Witt control entry (2026-08-24). No case directory or report executes them. |
| Streaming-row port to `a=8,d=3` (§7) | **KNOWN instrument, NEW client designation** | Instrument: 18:50Z event (V2 streaming). Policy slot: root synthesis §2 ("one D1 and one affine example roundtrip"). My contribution is selecting the binding memory wall as that D1 example. |
| Primitive-group Stage-1 census (§1 row 26) | **KNOWN — unexecuted** | APPROACHES row 26 and historical shortlist item 3 (spec in `xmodel/sol-lateral3.md` lineage); no execution artifact exists. |
| Prefix-ceiling meta-theorem ↔ `G2-BD` prototype (§5b) | **NEW** (design target, flagged speculative) | Obligation-table observation (17:58Z LIVE STATE: finite decisive-grade prefixes) exists; no uniform-ceiling theorem or `G2-BD` analogy appears anywhere. |
| DZ trees as G2-PSC sheet bridge | **KNOWN + SCOPE-CONFLICT** — not advanced | 02:07Z synthesis mention; no Faber→Sigray transport theorem exists, so any landing reading conflicts with REDUCTION.md §7.1. |
| `U`-uniform tree-local obstruction identity (§9) | **KNOWN — ownerless** | Websweep `bb1aa29f…` §3 proposes it as a conjecture generator; I propose only an ownership/priority change. |
| Conversion-eligibility reframing of max-12 (§0) | **NEW** (framing, not a claim) | Reconciles the freeze's method-control typing with the promoted ≤11 theorem (2026-08-24 19:32Z) and the unbounded-`U` order-four elimination (`0ac7da1d…`); changes priority, promotes nothing. |

## 11. Marginal bets

Three things I believe another strong researcher is least likely to produce this round:

1. **The grade-`3H` alignment as a falsifiable termination object.** Both closed ladder instances place the intrinsic obstruction at exactly grade `3H` (H15: `s^45`; H18: `s^54`), the load wall sits at `~14H/5`, and the inter-wall gap grows like `H/5` — so serial cells are a strictly worsening bet, and the *cheap* next move is a frozen-support affinity test plus an a-priori prediction at H=21, not another elimination. Most scans will propose "run H19" or a generic ranking search; the specific two-point structural alignment and its discriminator are the bet.

2. **Landing as converter, not template.** The realized value of ~30 promotions of normalized `(8,12)` algebra is contingent on T-cs/receiver/glue: with them, the pile advances a genuine unbounded-total partial-`y` ≤ 12 program; without them it is an archive. Pricing the landing lane by that contingency — rather than as one of four co-equal owners or a "10% protected global" line — is a portfolio decision I expect no other lane to derive from the ≤11 theorem plus the freeze's method-control typing.

3. **The covariance-or-artifact dichotomy is decision-complete either way.** Each lane treats its unit functional as lane-local, so nobody is incentivized to test whether the functionals transport; yet a single small job on frozen data either collapses the atlas cost by a factor of ~six or erects a scope firewall that prevents the landing program's most tempting future error (reusing a receiver obstruction across charts). Bets 1 and 3 share a theme I'd offer the coordinator as a heuristic: this campaign's cheapest remaining information is now in *relations between already-frozen artifacts*, not in the next elimination.

---

**Global scope firewall (restated):** nothing above is a mathematical claim. All maximum-12 computations remain method controls for counterexample purposes under the classical cutoff; promoted normalized-chart results are not source theorems; `G2-PSC` (if invoked), any invoked `G2-BD`, literal landing beyond the first chart family, Sigray-frame coverage (off-axis, post-jump, composite single-pole), a cofinal type/topological-degree ceiling, and JC2 itself remain open.
