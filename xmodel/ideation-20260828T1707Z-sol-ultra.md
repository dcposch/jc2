# Blind whole-portfolio JC2 report — Sol Ultra — `20260828T1707Z`

## Executive verdict

No proof or counterexample is obtained.  The two new Sigray promotions are
real progress but do not close `td=6`.  Literal composition of MFE with the
concrete two-pole residue A gives **NO HIT**:

```text
P1,P2 entries:             selected exit cost 0+0
first-step merge G_m:      selected exit cost 0
one common suffix step:    selected exit cost 2
terminal x-side witness:   psi=2
MFE:                       2 <= 6-1-2 = 3
slack:                     1
```

The merge's extra q-only resonant orbit is part of the reduced q-pattern,
but it is not thereby a selected positive exit-orbit with a distinct cv
witness.  MFE prices actual selected exits, not polynomial factors.  Thus it
cannot soundly be assigned an extra unit without a new implication

```text
q-only resonant factor at a V2 merge
  => a positive off-pole direction
  => a distinct critical-value flag H with positive actual cluster weight.
```

No reviewed result supplies the first arrow.  Even a proved unit merge
charge would merely turn the display into `3<=3`; it would kill boundary
classes, not residue A.  A kill needs either charge at least two beyond the
recorded suffix cost, a strict MFE in this anatomy, or a separate
realizability/coefficient obstruction.  Equality, slack transport and
MP8-style no-refinement remain unavailable.

The best next proof attack is therefore a **merge-different/coefficient
compatibility lemma** targeted exactly at the survivor, not a larger tree
census.  The best falsification attack is a **two-edge Puiseux jet lift** of
the same survivor through the first coefficient at which the q-only orbit
couples to both incoming parents, followed immediately by algebraization and
collision/nonproperness tests if it survives.

## 1. Compact disposition vector, avenues 1--46

The reference point is the current canonical live overlay, not the original
2026-08-21 scores.

**Changes.**

- `1 raise` — the reducer-safe ranks plus localized kernels turn arbitrary-Q
  GGV from undirected elimination into a finite rank-chart decision tree.
  This is a raise only for the raw-window quadratic endpoint/Fitting design;
  equivalent Groebner variants remain stopped.
- `2 raise` — source Sections 5--9 are review-closed at repaired consumer
  scope, all-`M=1` row-1 roots are dead, and MFE is now a genuine multipole
  inequality.  Pure Sigray remains the principal proof backbone, although
  landing, `RPMC(C)`, type control and cofinality are untouched.
- `4 raise` — residue A supplies a much more specific certified-germ target:
  a two-parent merged pattern with a rigid quadratic coefficient field.
  The raise is narrow: germ-to-polynomial algebraization and an actual
  collision/nonproper end are still missing.
- `7 raise` — unique contact-tree attachment now gives a sound interface
  from pole-tree exits to distinct actual cv flags.  This makes a targeted
  asymptotic-component construction meaningful; it does not construct
  `A(F)` or prove properness.
- `27 raise` — the survivor is now a single explicit splice/contact template
  with immediate two-pole merge and a common suffix, rather than a loose
  topology census.  Topology alone still cannot certify algebraic
  realizability.
- `28 raise` — only for a local different/conductor calculation on that
  fixed merge.  Generic BMY/Euler recombinations remain low-value.
- `31 raise` — Rees/different data can now be attached to explicit selected
  exit valuations and the concrete merged local algebra.  No global
  integrality inference is licensed.
- `36 raise` — residue A is a sharply typed sparse counterexample seed, and
  K00/full-`P6` remains an independent high-value falsifier.  Random Keller
  search is not raised.
- `46 raise` — proof-object checking is now on the critical path because
  endpoint rank atlases and typed tree ownership have many scope-sensitive
  branches.  This is a rigor/software raise, not a standalone proof route.
- `16 lower` — the generic HENS-CT composition backend timed out before
  telescoping.  Resume only after a structural adapter has a named client.
- `25 lower` — the already-negative passport census and absence of coupled
  two-coordinate provenance make another generic monodromy run inferior to
  the survivor-specific coefficient test.
- `26 lower` — no primitive-group mechanism controls the current explicit
  `td=6` survivor; the `A_d/S_d` wall remains.
- `33 lower` — ordinary action residues are automatic, and MFE already
  captures the sound actual-weight information presently available.  Any
  revival needs a genuinely twisted merge class.
- `44 lower` — the claimed no-prime-td proof remains refuted as proof input;
  `td=6` is composite and the repaired membership theorem does not engage
  the live survivor.

**Unchanged (none omitted).**

```text
 3 unchanged   5 unchanged   6 unchanged   8 unchanged   9 unchanged
10 unchanged  11 unchanged  12 unchanged  13 unchanged  14 unchanged
15 unchanged  17 unchanged  18 unchanged  19 unchanged  20 unchanged
21 unchanged  22 unchanged  23 unchanged  24 unchanged  29 unchanged
30 unchanged  32 unchanged  34 unchanged  35 unchanged  37 unchanged
38 unchanged  39 unchanged  40 unchanged  41 unchanged  42 unchanged
43 unchanged  45 unchanged
```

For clarity, the two hybrid obligations remain distinct: `G2-PSC` is
unchanged and open; `G2-BD` is unchanged and open.  Nothing in the MFE proof
transports a GGV packet into the Sigray tree, and nothing in the GGV rank
certificates bounds residue-A delay.

## 2. Bottleneck reranking

### Principal proof bottlenecks

1. **Full-configuration landing with provenance.**  A total theorem must
   accept every normalized exact-pair forest, including off-axis,
   mixed-`mu`, root/SF1 and post-jump `M>=2` states, and emit either a
   complete record or an explicit open constructor.  Current finite books
   are not this theorem.
2. **Cofinal control: `RPMC(C)` plus independently sourced type/`td` or
   complexity ceiling.**  Killing residue A or all of `td=6` would still
   leave the unbounded campaign.
3. **Coefficient/Puiseux realizability of decorated trees.**  Arithmetic
   `Q`-records and actual-weight budgets are necessary conditions.  The
   missing bridge is simultaneous realization of the pattern polynomials,
   h-family, both incoming edge equations and a global polynomial Keller
   pair.  The concrete residue makes this bottleneck testable before the
   first two are solved.

Source trust is no longer a top-three bottleneck at the repaired §§5--9
scope.  Statement 6.2 remains a printed GAP, but current consumers establish
its missing positive-domain premise.

### Principal disproof/counterexample bottlenecks

1. **Lift one source-open necessary-condition seed to a characteristic-zero
   formal object.**  The leading candidate is residue A's simultaneous
   two-edge Puiseux system; K00/full-`P6` plus one prolongation is the best
   independent bounded seed.
2. **Algebraize and globalize.**  A formal germ must become polynomials and
   must exhibit an actual collision or nonproper end.  Neither modular
   nonemptiness, a local ODE solution, nor an all-Witt restricted-analytic
   limit crosses this wall.

## 3. Exact answers to the seven requested interfaces

### MFE and residue A

The pole arrivals have `lambda=0`.  The immediate merge has two simple
incoming pole orbits plus one extra q-only orbit, but no selected positive
exit is certified there, so its actual selected-exit price is zero.  The
common suffix has one `mu=2`, IIa `k=1` selected exit of cost two.  The
terminal has `psi=2`.  Hence MFE is exactly `2<=3`: it neither kills the
cell nor sharpens its terminal menu.  It confirms that the old per-pole
budget would have double-counted the shared suffix.

A sound refinement may price the q-only orbit only by proving that it creates
a new direction outside the rootward pole union and that Statement 7.3
assigns a distinct cv flag.  A factor-count or a claimed equality is
insufficient.  One unit still leaves equality; the useful target is a
**merge excess theorem** giving total actual weight at least two, or one
unit plus an independently proved strictness criterion.

### Root identity

For every genuine contact-zero lower root edge, (R1) gives
`X_root=mu(1-w)>0`, so `w<1` without assuming `mu=1`.  What does not follow
for mixed `mu` is the all-simple top-degree evaluation
`X_root=r/(r+l)`.  The first exact missing implication is:

```text
mixed incoming multiplicities and q-only root factors
  => an explicit formula (or a sharp interval) for X_root
     compatible with each parent's reachable W-alphabet.
```

One needs the full leading coefficient of Proposition 8.1(iv), including
the multiplicity-weighted incoming factors and q-only factors.  Divisibility
alone permits root `M=1`; off-axis entries also lack a complete root landing
menu.  Therefore (R1) currently excludes no broader mixed/off-axis class.
The formula `w=l/(r+l)` is used nowhere outside all-`mu=1`.

### Global composition

**NO HIT as a global bound.**  Contact attachment plus actual cluster
weights yields MFE, an upper budget for already selected exits.  It does not
lower-bound the number or weight of exits in terms of pole count, conductor,
`td`, or depth.  Terminal orbit laws, Euler/genus and present pole-mass
identities still recombine without a positive global excess.

The literal new theorem interface worth testing is:

> **Merge-different interface (conjectural).**  Let `B=C[f,g]` inside the
> normalization of the completed local branch algebra at a `V2` merge.  If
> two pole arrivals and `l` q-only resonant factors occur at that merge, the
> relative different/conductor quotient has length at least `l+1`; its
> non-pole summands inject into pairwise-distinct actual cv flags attached at
> or above the merge.

For residue A, `l=1` would supply the missing excess two and contradict the
slack-one MFE.  This is genuinely stronger than the repository's conductor,
Euler or printed `(22)` attempts: it is local to the finite extension at the
merge, counts a module length rather than transporting `kappa`, and includes
an explicit injection into actual flags.  Neither clause is currently
proved.

### Concrete survivor

The cheapest exact test with the highest kill probability is the
**simultaneous two-parent coefficient reach test**, before full
algebraization.  Work over `Q(sqrt(3))`, retain both choices
`a1/a2=2+-sqrt(3)`, instantiate the two simple incoming p-orbits, the
q-only resonant orbit and the first suffix step, and impose:

1. both per-edge Proposition 9.3 equations with one common child pattern;
2. the h-family identities and cyclic characters;
3. the Jacobian coefficient through the first order where the resonant
   q-orbit interacts with both parent corrections;
4. conjugation/swap symmetry only as a check, never as an assumed equality.

Eliminate newest coefficients incrementally and retain literal provenance.
An ideal `(1)` or incompatible parent ratios kills A.  A reduced point lifts
to the next two Puiseux orders.  A smooth positive-dimensional component is
the counterexample seed and moves immediately to toric gluing,
algebraization, and collision/nonproperness.  This is cheaper and more
decisive than a generic merged-pattern census, a new passport run, or direct
random polynomial search.

### GGV recursion

Use a **module/Fitting atlas with deterministic pivot charts**, not an
unstructured minor atlas and not a single saturation.  On each current
stratum:

1. keep the pinned ambient standard basis and replay every matrix entry to
   normal form;
2. select a lexicographically fixed nonzero rank minor `Delta` and construct
   adjugate right kernels on `D(Delta)`;
3. pull back all coefficients of `x14*x72+x1*x97` to that kernel;
4. certify either an identically zero quadratic or exhibit a nonzero-NF
   coefficient/witness;
5. recurse on the complement `Delta=0` using the next Fitting ideal, never a
   dense-open inference;
6. at each leaf prove that the accumulated chart opens cover the relevant
   rank stratum, or report `NO_VERDICT_UNCOVERED`.

AWS fanout: six existing branches (`P`, `C8P02`, `Q1P02`, `Q1P03`,
`TRIPLE02`, `TRIPLE03`) in parallel, one 16-vCPU/128-GiB zero-swap worker per
branch initially.  Split only after an exact nonzero minor produces
mathematically distinct complement children; cap at 12 workers before
review.  Proof-object gates are frozen input hashes, ambient-NF replay,
nonzero-minor witness, exhaustive next-minor vanishing where rank is claimed,
kernel identity `M*N=0`, literal quadratic pullback, cover identity/radical
certificate, zero swap, and independent replay.  `TRIPLE02/03` dense-open
results remain provisional until their complements close.

### Proof versus disproof

The under-resourced proof route is the local **different/proximity module at
the explicit merge**, joining Avenues 2, 28 and 31.  It can beat the source
backbone short-term because it targets the only concrete `td=6` survivor,
but it cannot replace landing or cofinality.  The under-resourced disproof
route is residue-A two-edge jet realization, joining Avenues 4, 27 and 36.
It is superior to broader sparse search because every discrete datum and the
first irrational coefficient ratio are already pinned.

### Software acceleration

Build a **typed Forest-to-Equations compiler** with two backends: selected-
exit ownership and coefficient reach.  The IR must distinguish pole
arrivals, rootward continuations, selected positive exits, q-only pattern
factors, merge vertices, shared suffix ownership, root/nonroot signatures,
actual witness `kappa`, and every theorem dependency.  It emits (i) an MFE
certificate with a deduplicated witness table and (ii) the simultaneous
Puiseux coefficient ideal with provenance per equation.

Decisive gate: compile residue A and reproduce exactly costs `0,0,2`,
`psi=2`, one shared suffix owner, and no merge witness; then mutate the
attachment of either incoming pole, duplicate the suffix, or relabel the
q-only factor as an exit and require rejection.  On the algebra backend,
reconstruct the reduced degrees `(6,10)` and coefficient-ratio polynomial
`z^2-4z+1`, then verify every emitted edge equation by exact substitution.

## 4. Major-lane decisions

| Lane | Decision | Reason |
|---|---|---|
| pure Sigray/exact-pair | continue | Principal proof backbone; source spine now substantially safer. Target landing plus the residue-A merge interface. |
| hybrid `G2-PSC` | redesign | No packet-to-tree fidelity theorem. Build a typed translator or keep GGV and Sigray logically forked. |
| arbitrary-Q/rank-drop GGV | continue | Continue only the reducer-safe module/Fitting atlas and literal quadratic endpoint. |
| lambda-nonzero/raw-window GGV | redesign | Stop isolated face-order increments; seek recurrence/row-ideal invariance or complete raw-window incidence. |
| LF40 | stop | Existing runs cap at row zero; no new structural discriminator justifies more compute. |
| K00/order-two/TD6 | continue | Protect K00/full-`P6`; add the residue-A two-edge coefficient test as a separate exact falsifier. |
| D43 | redesign | Use only with a named reduced raw endpoint/jet client and provenance-preserving equations; no blind deeper march. |
| Artin--Schreier/Witt | redesign | Finite lifts and growing-support tower are known; target uniform polynomial support/degree or exterior-conductor obstruction, otherwise stop more level-by-level lifts. |
| HENS-CT | redesign | Replace generic multivariate composition with a structural algebraic-series adapter; no rerun of the timed-out path. |
| external intelligence | continue | Low cost and independent; next scheduled broad sweep remains 2026-08-29 05:24Z. Treat manuscripts only after primary-text audit. |

`G2-BD` is not folded into the hybrid row: it remains an open residue-A
bounded-delay/carrier obligation even if `G2-PSC` were solved.

## 5. Detailed idea cards (three only)

### Card A — Residue-A simultaneous coefficient reach

- **Dependencies/licensed assumptions:** promoted pattern/edge formulas at
  their repaired scopes; the displayed residue-A `Q` records; M-PAT only as
  a provisional equation generator, not a theorem; arithmetic over
  `Q(sqrt(3))`; no realizability assumption.
- **Cheapest discriminator:** eliminate through the first mixed resonant
  coefficient for both conjugate ratios, with exact provenance and direct
  substitution replay.
- **Outcomes:** unit ideal/incompatible ratio kills residue A; isolated
  singular points demand two more orders; a smooth component becomes the
  best formal counterexample seed; an adapter ambiguity returns no verdict.
- **Stop/rollback:** stop on any unproved pattern equation, loss of an
  incoming orbit, denominator branch not covered, or failure to reproduce
  the recorded degrees and ratio polynomial.
- **Cost:** 1--2 researcher-days for the typed derivation, then 2--6 AWS
  hours per branch; one hostile mathematical review.
- **Expected information gain:** very high; roughly 0.45 probability of a
  local kill, 0.35 of a sharply smaller obstruction, 0.20 of a serious seed.

### Card B — Merge different to actual flags

- **Dependencies/licensed assumptions:** contact-quotient attachment, MFE,
  finite local normalization at the specified merge, standard different and
  conductor identities.  Do not assume finite global `C[f,g]`-module,
  printed `(22)`, fixed `kappa`, or equality.
- **Cheapest discriminator:** compute the completed local algebra and
  Kähler-different/Fitting length for the exact two-simple-plus-one-q-only
  reduced pattern; test whether length two survives the Jacobian relation
  and whether its generators map to distinct off-pole tangent directions.
- **Outcomes:** length/injection theorem kills A via MFE; length one explains
  the slack and stops this mechanism; zero/noncanonical length falsifies the
  proposed interface; an unassigned length isolates the precise attachment
  lemma still needed.
- **Stop/rollback:** abandon on dependence on a global finiteness assumption
  equivalent to JC2 or if the length is just the old Euler/conductor ledger
  under new notation.
- **Cost:** 2--4 expert-days, negligible compute, two reviews (local algebra
  and Sigray attachment).
- **Expected information gain:** high even on failure because it decides
  whether the q-only orbit can ever be priced soundly.

### Card C — Complete reducer-safe endpoint atlas

- **Dependencies/licensed assumptions:** six certified quotient ranks,
  frozen `106x105` matrix, pinned ambient standard bases and the quadratic
  endpoint.  No radical/component conclusion from rank alone.
- **Cheapest discriminator:** finish `P`, `C8P02`, `Q1P02`, `Q1P03`,
  `TRIPLE02`, `TRIPLE03` pivot charts and their first complements, prioritizing
  any chart where the pulled-back quadratic is nonzero.
- **Outcomes:** a nonzero quadratic gives an endpoint survivor; a certified
  finite cover with zero pullback kills the whole rank locus; rank jumps
  spawn smaller exact children; uncovered complements remain explicit.
- **Stop/rollback:** stop a branch on NF mismatch, common-factor cancellation,
  swap, missing chart-cover proof, or resource cap.  Never promote dense-open
  death to component death.
- **Cost:** initial six-way 16-vCPU/128-GiB fanout, 12-worker review cap,
  estimated 6--24 wall-clock hours plus independent replay.
- **Expected information gain:** high finite discriminator; lower direct JC2
  probability than Cards A/B but the cleanest complete computational lane.

## 6. Likely-missed insight

The survivor's irrational ratio has norm one:
`(2+sqrt(3))(2-sqrt(3))=1`.  The two values are exchanged both by Galois
conjugation and by inversion.  If exchanging the two incoming pole branches
also inverts the coefficient ratio, then an **unordered symmetric merge over
the base field** may force the ratio to be fixed by inversion, hence
`r=+-1`, contradicting `r=2+-sqrt(3)`.  Conversely, if the branches carry
distinct labelled higher-order data, the symmetry is broken and there is no
obstruction.

Cheapest test: derive the exact action of parent swap on the first two
coefficient slots in Proposition 9.3/H1, without imposing symmetry.  Check
whether the merged child and h-family are intrinsically unordered or retain
a label.  This is a few-page exact calculation over `Q(sqrt(3))`; it can
kill A immediately, or definitively prevent misuse of Galois symmetry.

## 7. Epistemic ledger and disclosure

### Promoted facts used

- Repository basis HEAD
  `418e413593120d19e15e6546eb50c985f4b1f038` and every packet-listed file
  hash matched before the corresponding source was inspected.
- Repaired Propositions 5.4, 6.7--6.8, Lemma 6.1, and the campaign
  every-fibre replacement for printed Proposition 5.8 are accepted only at
  the packet's stated review-closed scope.
- Actual-weight `(C7.1*)`, corrected nonroot Proposition 8.4,
  `counterexample=>td>=6`, (R1), the all-`mu=1` formula (R2), and MFE are
  used only at their promoted scopes.
- Residue A's displayed `Q` records, selected suffix cost two and `psi=2`
  are necessary-condition data, not a realizable forest.
- The six GGV residual/total ranks are rank certificates only.

### Provisional inputs

- M-PAT merged-pattern equations, complete mixed/off-axis/root landing,
  coefficient/h-family realizability, and all endpoint results not supplied
  with complement recursion remain provisional or open.
- The proposed merge-different interface, symmetry/inversion obstruction,
  and typed compiler design are new proposals in this report, not evidence.

### Conjectures

- A q-only resonant merge factor may create a local different of length two
  whose non-pole part injects into actual cv flags.
- Parent-swap invariance may be incompatible with the ratio
  `2+-sqrt(3)`; this depends on whether the two incoming branches are
  intrinsically labelled by higher data.

### Failed/closed approaches respected

- MFE composition gives `NO HIT`; a unit merge charge is still insufficient.
- Printed `(22)`, literal `delta_a`, `(22-cl)`, fixed/cross-fibre `kappa`,
  equality/slack and MP8 no-refinement were not revived.
- The l-free root phase-3 census, root-inclusive Proposition 8.4, generic
  monodromy passports, ordinary action residues, Matysiak manuscripts,
  blind higher face order, and quotient operations without ambient NF replay
  remain failed, withdrawn or stopped.
- Dense-open endpoint death is not component death.

### Hidden assumptions explicitly rejected

- No universal book landing, root census, `RPMC(C)`, type bound, cofinal
  `td`/complexity ceiling, GGV-to-Sigray transport, global finiteness of
  `C[x,y]` over `C[f,g]`, Puiseux algebraization, or coefficient
  realizability is assumed.
- `w=l/(r+l)` is not extended beyond all-`mu=1`.
- A q-only factor is not identified with a selected exit or cv flag.
- `G2-PSC` and `G2-BD` are separate.

### Checks run and failed attempts

- Ran SHA-256 verification of the sealed packet: matched
  `1e40e7106e2a3c16b6a6fe92e6d2387228fb1260ae5633e686b456e9f9d95f1c`.
- Ran `git rev-parse HEAD`: matched the required basis.
- Ran SHA-256 checks on all 17 custody-listed source artifacts: every hash
  matched exactly.
- Performed read-only text inspection, line counts and targeted literal
  searches for avenue rows, live 2026-08-28 state, residue A, selected exits,
  q-only factors, suffix cost and `psi`.
- No mathematical command failed.  `shasum` emitted a harmless locale
  fallback warning.  Some direct display calls hit the tool's output-size
  truncation; the relevant canonical current sections and explicit residue
  interfaces were then read by bounded ranges/searches.  No inference is
  based on an unseen peer report.
- No Singular, msolve, Sage, Lean, heavy algebra, AWS access, job control or
  network access was used.

### Files read

```text
xmodel/ideation-20260828T1707Z-packet.md
APPROACHES.md
AUDIT.md
COORDINATION.md
PROGRESS.md
notes.md
ladder/REDUCTION.md
ladder/SIGRAY-AUDIT.md
ladder/SHEET6-2POLE.md
ladder/SHEET6-MULTIPOLE.md
ladder/SHEET6-DEPTH.md
xmodel/ideation-20260828T1149Z-synthesis.md
xmodel/websweep-20260828T0524Z.md
xmodel/sigray-sections7-9-coordinator-integration-sol-ultra-20260828.md
xmodel/sigray-sheet6-depth-root-meet-coordinator-integration-20260828.md
xmodel/sigray-multipole-selected-orbit-attachment-coordinator-integration-20260828.md
xmodel/sigray-multipole-selected-orbit-attachment-hostile-fable5-20260828.md
xmodel/sigray-rootaware-engine-cap-repair-hostile-rereview-gpt5-20260828.md
```

No file beginning `xmodel/ideation-20260828T1707Z-` other than the packet and
this assigned output was read.  No assigned prompt file was provided or
accessed.  No peer `1707Z` response was listed, searched, statted or read.
The nested `jc2-lean` tree was not entered, listed, searched, read, built,
modified, status-checked or controlled.

### Model, contamination and scope

Model identity available to this session: **Sol Ultra**, operating as the
OpenAI Codex agent based on GPT-5; no more specific build identifier was
exposed.  I used no subagents and read no peer response.  There was no
accidental contamination.  The only filesystem write was this report.

This report is an independent strategy analysis under the sealed packet.  A
model verdict is not mathematical evidence.  New theorem interfaces and
probability estimates are research prioritization only and require ordinary
producer, hostile-review and coordinator-promotion gates.
