# Hostile review: ADE-decorated quadratic boundary/different threat map

You are GPT-5.5 xhigh, an independent hostile algebraic-geometric reviewer
for the plane Jacobian-conjecture campaign. Work in
`/Users/dc/code/math/jc2` on frozen git basis
`986427df23c30375b6edfd659ba8a6c6139434ab`.

Read and review this sealed exact provisional producer in full:

```text
ab10ea83f443c93bdb87194ad0e42fe2c442984ba821c3ddf8ddf5cc3f796133
  xmodel/bd-a2-ade-decorated-boundary-different-threat-map-sol56-20260830.md
  body 19667 / 5a1985f48f6b61d4f12b288942b3bbb753b45f3e18a6e31b85cbc077ccd8e2ef
  manifest 9404a4f13e60cf4f40ec45b12a3b5cff81b5c431fdfd96a49e604d5f40e717c8
```

Its frozen authorship basis is `dca72076aa1615b0b1286fd4428a1acac7b65963`;
its producer commit is the review basis above. Read every charged input named
in Section 0 and enforce its exact scope.

Independently reconstruct and attack:

1. Rebuild the signed-coordinate-graph classification of connected root
   subsystems of `D9`. Check whether only `A1,...,A8` and `D4,...,D9` can
   occur, and whether `A9,E6,E7,E8` are genuinely excluded for embedded root
   subsystems rather than only for a chosen simple-root presentation.
2. Derive the inverse-Cartan and congruence formulas for all `A_r` and `D_r`.
   Audit signs, node numbering, integrality, positivity, and the equivalence
   between `m>0`, `n=Cm>=0`, and the stated local Cartier data. Test the
   scalable rays and every advertised one-germ survivor.
3. Verify each total-transform identity producing the target-line cap
   `sum n_i<=8`, the conditional `B`-fibre cap `sum n_i<=4`, and the
   fixed-infinity/no-common-carrier cap. Identify every properness,
   transversality, carrier, and nonnegativity hypothesis; reject any cap used
   outside its proved regime.
4. Audit the distinction between numerical support or multiplicity of `n`
   and distinct physical boundary germs. Check the forest-cycle connector
   criterion when a single tangent/high-contact germ or a branch through an
   ADE point contributes several numerical units.
5. Determine whether the claimed finite successor is actually finite after
   all congruences, caps, physical branch partitions, simultaneous `D9`
   embeddings, and effectivity conditions are imposed. Give the smallest
   explicit enumeration schema that is complete up to the relevant Weyl and
   incidence symmetries.
6. Search for a cheaper theorem that kills any remaining ADE type using only
   the charged data. Conversely, exhibit a precise formal survivor whenever
   the producer claims no exclusion. Keep formal numerical data distinct from
   effective divisors, analytic germs, finite maps, or polynomial maps.
7. State the maximum theorem safe to promote and the exact unresolved strata,
   especially common-carrier and nonfinite cases. Keep this packet separate
   from the independently active moving-double-section/conductor lane, basis
   coverage, counterexamples, and JC2 itself.

Return an itemized verdict (`CONFIRMED`, `CONFIRM_WITH_CORRECTIONS`, `GAP`, or
`REFUTED`), precise repairs, and the cheapest decisive successors. Hard cap:
8,000 tokens and 28,000 UTF-8 bytes. Do not inspect, list, search, stat, build,
modify, or control `jc2-lean`; the process sandbox also enforces this. Do not
run local heavy CAS or Singular. Do not edit inputs, canonical files, scripts,
or dependencies. Write exactly one report:

```text
xmodel/bd-a2-ade-decorated-boundary-different-threat-map-hostile-review-gpt55-20260830.md
```

End with one standalone `<!-- BODY-END -->` line and no seal block.
