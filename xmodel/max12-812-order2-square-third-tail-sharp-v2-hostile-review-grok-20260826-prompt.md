You are the independent hostile referee for one narrow exact necessary theorem
in the `jc2` plane-Jacobian campaign.  Work text-only.  Do not run Singular,
Sage, msolve, Lean, the package compiler, AWS jobs, or any other substantive
algebra program.  You may read files, recompute SHA-256 hashes, validate the
already-emitted manifests, inspect frozen records, and check the displayed
small polynomial identities by hand.  Do not edit any existing file.

Primary producer targets, with required byte hashes:

```text
5a966b1d4dfde21e51bbaa6120a7cf118b05e68cfc0878aa2d1c345c6a0f47a0
  cases/max12_812_order2_square_third_tail_sharp_v2_20260826/RESULT.md
96cb2824fb5c901e7485ba725f5d92e8f116b32a8a9dde7fd6c59cb901be013e
  cases/max12_812_order2_square_third_tail_sharp_v2_20260826/FREEZE.sha256
6ae5fe22481ac4ec085006d91919b35b9ff5b85abd05517b502850fdc07bb26b
  cases/max12_812_order2_square_third_tail_sharp_v2_20260826/RESULTS.sha256
b93d402434f3e76fa277567ca87af881da685826c58b2ffa75e06a4e975e09eb
  cases/max12_812_order2_square_third_tail_sharp_v2_20260826/compile_square_third_tail_sharp_v2.py
```

Read the V2 `RESULT.md`, `REGISTRATION.md`, compiler, both manifests, and every
evidence file named by `RESULTS.sha256` in full.  Validate both manifests.
Read the complete V1 package
`cases/max12_812_order2_square_third_tail_sharp_20260826/` only to audit the
claimed V1-to-V2 extraction repair and the separation of its four failed
deployment endpoints from V2.  Read the predecessor theorem
`xmodel/max12-812-order2-square-third-tail-divisibility-theorem-20260826.md`
to identify exactly what is sharpened.  The successor design is not a
mathematical premise and its fan claims are outside this review.

Attack the following issues independently, identifying the smallest broken
implication or missing hypothesis if any.

1. Custody and execution.  Recompute the four primary hashes and validate
   `FREEZE.sha256` and `RESULTS.sha256`.  Charge source hashes, generated
   Singular hashes, configured fields, tags, return codes, validators,
   stderr, stdout, and extractor self-controls.  Decide whether the two Q
   configurations and two F_65521 controls are four frozen endpoints.  Treat
   F_65521 only as a software control, never as a characteristic-zero proof.
   Verify that V1 really has no mathematical endpoint and that V2 changes
   only coefficient extraction, with no hidden source-formula change.
2. Complete-source bridge.  Starting from the source substitutions
   `K=L^2+Lambda*R`, `N=L*M+Lambda*S`, `k10=Lambda*kappa`, and the full charged
   rows, inspect rather than trust the seven emitted identities.  Verify exact
   divisibility by `Lambda^3`, the orientation of Laurent extraction, and the
   lower-unitriangular Faber transform.  Check that all seven divided rows
   equal the first seven coefficients of
   `[P/(16*L^3)]_-`, with
   `T=2*L*R+M`, `B=R^2+S`, and
   `P=T*(12*B*L^2-T^2)`.  Attack off-by-one coefficient extraction and any
   lower-grade mixing.
3. Completeness at this grade.  Check from the charged source, not a weight
   slogan, that integral `k10=Lambda*kappa`, `k6`, `k2`, targets, and the
   displayed tangent `p=Lambda*p1` have no negative contribution to this
   divided grade.  Verify that arbitrary linear `R,S` remain present.  Decide
   whether seven Laurent rows suffice by comparing the proper numerator and
   denominator degrees after division, including possible polynomial parts.
4. Generic square consequence.  Verify `P == -M^3 (mod L)`.  On fixed
   `p!=0`, check squarefreeness of `L=z^2+p/2`, the characteristic-zero and
   degree hypotheses, and the inference `L|M^3`, `deg M<=1` implies `M=0`.
   Check the same claim for an integral tangent centered at `p=0` without
   silently replacing the full Lambda-dependent denominator by its boundary.
5. Quadruple-root sharpening.  With `L=z^2` and
   `M=alpha*z+beta`, independently verify `[z^0]P=-beta^3`; after beta=0,
   verify `[z^3]P=-alpha^3` for arbitrary linear `R,S`.  Check carefully that
   `z^6|P`, not merely `z^2|P`, follows from the seven vanished source rows.
   Decide whether this proves `alpha=beta=0` at the necessary third-tail gate.
6. Exact scope.  The strongest allowed conclusion is only: for fixed `p` or
   the displayed integral Lambda tangent, and for `k10` of integral
   Lambda-order at least one, the complete third-tail necessary condition
   forces `M=0`; hence the named nonzero zero-load square/discriminant first
   directions and their `p=0` limit fail this gate.  Explicitly exclude
   fractional or ramified positive-order slopes of `p` or `k10`, every
   post-`M=0` cusp/odd face, Newton-fan exhaustiveness, terminal or Taylor
   conditions, existence or exclusion of a strict arc, the whole square
   component, order two, `(8,12)`, maximum twelve, and JC2.

Output exactly one new report and no other file:

`xmodel/max12-812-order2-square-third-tail-sharp-v2-hostile-review-grok-20260826.md`

Include a clause-by-clause verdict table, independent hand derivation of the
generic and p-zero consequences, custody findings, strongest surviving
theorem, exact scope, and sharpest nonclaim.  If the endpoint survives
unchanged, end with exactly
`ORDER2_SQUARE_THIRD_TAIL_SHARP_V2_CONFIRMED`; otherwise end with exactly one
of `ORDER2_SQUARE_THIRD_TAIL_SHARP_V2_REPAIRED` or
`ORDER2_SQUARE_THIRD_TAIL_SHARP_V2_QUARANTINED`, and state the smallest repair.
