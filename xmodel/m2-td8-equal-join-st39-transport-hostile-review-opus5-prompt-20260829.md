# Opus 5 hostile review — Fable td=8 Statement-3.9 transport

Producer: Fable 5. Work independently from `/Users/dc/code/math/jc2` and
write exactly:

`xmodel/m2-td8-equal-join-st39-coefficient-transport-primary-hostile-review-opus5-20260829.md`

Read the complete target
`xmodel/m2-td8-equal-join-st39-coefficient-transport-primary-fable5-20260829.md`
(full SHA-256
`910d3216ad7476b743eb920e3d68026f05efc8ffe1a409c476fb90ce277f6ad4`,
body `3b7851eaaee3f35c23de8d201eb22ad664ab2dba1614d6d4fc04cba562461171`),
its packet `cases/m2_td8_equal_join_st39_transport_fable5_20260829/`, the
reviewed route/local-ODE inputs, and every printed statement used by the
target. Re-read the displayed source pages yourself. Treat every extraction,
notation identification, tower stage, and theorem as an allegation. Do not
read active exact-lambda/cv-census output or later model reports.

Audit independently, with these priority attacks:

1. Verify the full-pattern dictionary: reduced versus full `p/q` degrees,
   `i_V`, `M*_V`, `mu_V`, tower depth, edge orientation, and every use of
   Statements 3.9, 3.17, 3.18 and 8.3. Check that the same symbols are not
   silently assigned different meanings at a pole, chain vertex, merge, and
   trunk.
2. Re-derive the claimed exact upgrade (E) of Proposition 4.6. Track every
   leading coefficient, `kappa`, chart normalization, Jacobian constant,
   sign, and possible vanished/higher-order term. Decide whether the printed
   `ominus` may really be replaced by one globally pinned product on both
   branches.
3. Prove or refute the interior-face binomial lemma and its telescope.
   Statement 3.9(ii) transports one coefficient only; check whether the
   q-side top, enslaved tower element, and resonant coefficient are identified
   correctly and whether satellite roots or a tower-depth change can feed the
   route rather than an escape.
4. Attack the drop-vertex lemma. Does `dq_W>1` force a strict tower drop?
   Re-derive `l_m/k_m-(mu_W-1)=dq_W/D_W`, every proposed `(k_j,l_j)`,
   `mu_G=93/14`, `k_G=79`, the trunk polynomial `k_tr(t)`, gcd claims, and all
   integrality/parity assertions. The stage-1 `k_1=1` interpretation is
   explicitly ambiguous; determine whether alternatives preserve the verdict
   or invalidate the computation.
5. Reconstruct the cross-vertex monomial system without using the producer
   code. Verify the local constants `R_H,R_G,R_tr`, pole family, leading-
   coefficient factors, merge-orbit attachments, available independent
   gauges, and triangular solvability. Seek a hidden closed cycle or shared
   `s_j/c_0` constraint.
6. Re-derive the twin law `(A1/A2)^2=omega^-15`. Check the exponent, which
   orbit/root ratio supplies `omega^3`, the condition `omega^nu=-1`, deck
   coherence, square-root availability under the actual coefficient field,
   and whether solving it over `C*` is enough for a common polynomial source.
7. Check all three Statement-3.9 clauses, not only coefficient clause (ii),
   along the full displayed tree. Distinguish formal top-pattern transport
   from construction of compatible subtop jets and from source landing. If
   the report's explicit system chooses data not licensed by the printed
   statements, narrow the verdict.
8. Replay ordinary/optimized suites, independently reconstruct exact samples
   at `t=0,1` and a symbolic generic `t`, and add hostile mutations in `/tmp`.
   Tests restating target formulas do not validate source semantics.

Return `PASS_AT_ST39_FORMAL_SCOPE`, `PASS_WITH_REPAIR`, `REPAIR_REQUIRED`, or
`FAIL`. Give a clause-level scorecard separately for the exact product law,
drop-stage law, twin law, and existence of a compatible formal coefficient
system. Preserve any valid narrow residue, name the smallest repair/source
datum, include commands/counts and full/body hashes, and state the maximum
safe consequence. Even a pass proves no exact lambda, subtop-jet recursion,
source landing, polynomial realization, panel exclusion, degree bound, or
JC2 result. No AWS is authorized.

Hard boundaries: no web, AWS, commit, push, canonical edits, heavy CAS, or
local long/high-memory process. Never access/list/search/build/status or
control `jc2-lean`; never run global `git status` or workspace-wide searches.
Modify only the requested review report; use `/tmp` for scratch.
