# Exact rational linear decision code — static preparation only

2026-09-09. DISABLED / UNEXECUTED / INTERNAL-UNREVIEWED CODE. First action21:25:24UTC; controlling cap21:50:24UTC, earlier than21:52. Targets absent before writing. This implementation consumes accepted17m, not the pending univariate runtime evidence. No actual coefficient artifact, receipt, matrix, rank, certificate or point was read or constructed. No mathematical subprocess of any size, syntax check, import, compile, test, CAS or worker action occurred.

## 1. Outcome and exact scope

Prepared solver.py, independent checker.py, metadata evidence.py, a narrow dispatch_batch.py adaptation and finite semantic_controls.py. Frozen algebra.py, execution_gate.py and probe.py were copied unchanged. VALIDATION.md specifies the complete future framing, exact acceptance payload, finite controls and outstanding compatibility obligations. caller.diff preserves the complete literal delta; the registration template is false with null worker, paths, profiles, clocks, installed engine and acceptance.

This is a candidate-and-proof checker for the ALREADY ACCEPTED generic17m contract: B=Q[v]/p(v), the entire finite-etale rank-seven algebra; nine polynomials fi and guard q, each degree<=5. The zero localized quotient is equivalent to sum hi*fi=q^5 with all deg hi<=25. Otherwise an exact rational separator exists. The implementation neither proves this theorem again nor promotes the current source artifact/runtime. Future actual source use requires ROOT's independently issued univariate acceptance. Nothing here is an ideal decision, a point, source closure, speedup or dispatch permission.

## 2. Complete system and independent certificate semantics

Rows are (k,a),0<=k<=30,0<=a<=6; columns are (i,j,l),0<=i<=8,0<=j<=25,0<=l<=6. All217 rows and1638 columns remain, including target-zero high rows, zero input rows, redundant generators and componentwise drops. Input order and dense windows are exactly the accepted nine indexed univariate slots. Both r and h0 remain in the guard; the checker additionally verifies q=r*h0 directly from their dense wires.

The producer uses the unchanged sparse Ring only for parsing, products and reduction by the literal complete p. It never calls its B inverse, pair-Bezout, gcd or factor methods. It creates exactly one augmented rational matrix [M|b|I217], dimensions217 by1856, and calls FLINT rref once. All entries are exact numerator/denominator rationals, not floating approximations or a modular conclusion. No second engine, alternative order, scalar specialization, parameter farm or source expansion exists.

Manual extraction proof: row operations on the augmented matrix are invertible and encoded by the final identity block. A pivot in target column1638 yields a row lambda with lambda*M=0,lambda*b=1. If the target column is not a pivot, choosing every free M variable zero and the M-pivot variables equal to their reduced target entries solves Mc=b; identity-block pivot rows have M=b=0. The augmented rank is217 because I217 is present. Incorrect installed behavior or extraction remains merely a failed candidate: the checker does not trust that rank, the solver status, flags, pivots, augmented matrix or engine arithmetic.

The new checker imports neither solver, FLINT nor algebra.py. It independently parses every B coefficient and reconstructs the literal factored p with dense Fraction arithmetic. Reduction subtracts descending leading terms against the monic rational modulus, a separate path from the producer's sparse grouping/division. There is no division by a possibly nonunit B element. For UNIT it reconstructs all nine degree<=25 multiplier polynomials from1638 rational pairs, multiplies by the nine input polynomials and checks the full217-coordinate identity. For SEPARATOR it reconstructs and checks EVERY1638 column, then lambda*b=1 across all217 target coordinates. It receives no precomputed M. The complete exact identity overrides all solver flags.

UNIT therefore carries the literal guarded cofactor identity1=xi^5*sum(hi*fi)-(xi*q-1)*sum((xi*q)^j,j=0..4). A separator has only17m's generic nonzero-quotient interpretation under the imported finite-etale hypothesis; it is not a character, rational point or explicit algebraic point. No separate primal/dual relation is inferred from a header alone.

## 3. Exact wires, source binding and future acceptance

All mathematical rational payloads are canonical string pairs. The independent parser enforces reduced fractions, positive denominators, canonical zero and strictly sorted sparse B basis terms with no other variable exponent. Exact row ids/windows, modulus, guard factors, dimensions and ordering are checked. Missing/extra witness dimensions, floats and noncanonical rational spellings fail. Duplicate JSON keys and nonfinite values fail at the shared metadata boundary, before mathematical parsing. No expression eval or JSON float roundtrip is used.

Both mathematical main entrypoints call unchanged authorize BEFORE any Fraction/backend/FLINT import or matrix allocation. evidence.py then requires a separately pinned ROOT-UNIVARIATE-ACCEPTANCE.json exactly equal to the authority's univariate_acceptance payload. The template is null; no acceptance is minted here. Root's lifecycle-only21:32 update was acknowledged but no gate body, actual artifact or new receipt was consumed.

The REQUIRED future ROOT acceptance contract is:

- schema: F10-UNIVARIATE-ACCEPTANCE/v1;
- status: ROOT-ACCEPTED-GENUINE-UNIVARIATE-CHECKER-RECEIPT;
- univariate_sha256 and receipt_sha256, matching the two actual pinned inputs;
- adapter_sha256=11ebf4307e7cff9b7909020dcdf69cda32f689236453763ce9433fff66fd7b67;
- checker_sha256=6547063b8cf2970ce0aa1e47e5a49db31c2fd4b1e97d87690c954487500a61f0;
- backend_sha256=7d565299faba1a8921fdb36f345af2cae49254fa59730e0e3c7a1f98e587d9dc;
- input_evidence_sha256=d262f3d498323b4606c51828a647b7334e8885095b099acbff41d17dd6db88b9;
- input_evidence: exactly normalized_sha256, normalized_receipt_sha256, normal_generator_sha256, normal_checker_sha256, original_source_sha256, matching artifact and genuine receipt. The last three literal versions are the accepted constants in evidence.py.

The acceptance file's SHA is separately supplied as univariate_acceptance_sha256 in ROOT's fresh dispatch registration. The incoming full receipt must have genuine nine-row status, artifact SHA, all nine row slots, both guard factors and bounded actual degree fields. New evidence binds accepted prior code versions and the original-source provenance without staging conflicting old adapter/checker basenames. A correctly spelled receipt is never treated as execution provenance absent ROOT acceptance after independent custody review. This remains cooperative ROOT authority, not cryptographic authentication against same-user malicious writers.

Certificate binding includes the complete input SHA, prior fullreceipt SHA, acceptance SHA and exact evidence. The genuine checker rebinds them and the candidate producer hash. It independently checks the mathematical payload; claimed candidate status is not a proof premise. No incoming acceptance or receipt is modified.

## 4. Narrow caller and finite designed controls

The caller's run machinery is retained; only per-operation univariate acceptance and installed-FLINT metadata replace the old normalized payload. The main delta selects the new source vector, pinned acceptance, declared installed package files, fresh outputs and sequence. Physical identity, exact full CAPRUN/child vectors, source pre/post hashes, immutable exclusive authority, no concurrent writer, no new inner PGID, inherited16MiB cap,15-second admission margin, fixed absolute/aggregate cutoffs and post-return check remain. CAPRUN's sampling/cleanup does not promise strict real-time resource limits. No supervisor or installer was added.

Sequence: the same five ordinary authority refusals, genuine valid sentinel and descendant RSS/TERM/KILL dummy; then one solver.main, one genuine checker.main and one finite in-process corruption harness. Complete registered Python -I -B vector lengths are9,10,11; sys.argv lengths6,7,8. The incoming univariate.full.receipt.json is distinct from the new decision.receipt.json. All outputs are exclusive; cap/failure/late return gives STOP/NONDECISION, never an automatic retry. Fixed caps/worker/clocks remain entirely ROOT decisions, presently null.

Code for seven common control mutations is authored: dimension216, omitted witness pair, float numerator, noncanonical numerator, altered literal modulus constant, altered indexed row id and missing h0 guard factor. When separately authorized and run, the harness requires each JSON object actually to change, writes it exclusively, rereads and hashes it before an IN-PROCESS checker.verify call; this is not production-entrypoint evidence. No fixture currently exists. The eighth case is branch-specific: scale a separator by2 to fail only lambda*b=1; or add1 to the constant multiplier of a genuinely nonzero input row to fail the full identity. If every input row is zero on a UNIT candidate, that case is explicitly NOT-APPLICABLE, not fabricated coverage. No positive verification is rerun by the harness.

VALIDATION.md supplies separate exact generic positive/negative cases, including both certificate branches, zero guard and a high-T-window/greater-than2^53 rational example. These are mathematical test specifications ONLY. No generic toy wire, verifier-test entrypoint or separate generic registration was implemented or executed. They are not hidden automatic steps of the actual-source caller and cannot be used to fabricate source acceptance. This missing runtime test harness is an explicit remaining task, not an unmentioned completeness claim.

## 5. Documentary API and implementation uncertainties

The sole new API input is ROOT's pinned note of the official python-flint0.9.0 matrix documentation: https://python-flint.readthedocs.io/en/latest/fmpq_mat.html . It licenses constructor/indexing/fmpq construction and rref returning(matrix,rank), not installed availability. No web call was made here. Actual module/version/native closure and performance are unknown. ROOT must supply exact version, absolute module path/hash and a separately verified native-file closure in future file pins. The code refuses missing/incompatible imports and verifies module path/hash/version after import. It contains no installation, package search or fallback.

FLINT rational string spelling is guarded but not experimentally checked: only integer or numerator/denominator forms are accepted, canonicalized by Fraction and independently reparsed. Unsupported installed behavior gives no certificate/decision. A different-model static review and separately authorized finite compatibility/generic tests are needed. No syntax, import, AST, compile, arithmetic, matrix construction or coefficient calculation was performed in this task. Matrix dimensions are theorem constants, not measured performance. All actual coefficient bit lengths, matrix density, certificate bytes, wall/CPU/RSS and success remain unmeasured.

## 6. Read scope, static checks and completion

Exactly13 pinned allowed inputs were read WHOLE after hashes matched; truncated combined output was resolved by separate full reads. input-pins.json and READ-SCOPE.md give the exact paths and limits. No actual coefficient artifact or receipt, current proof/runtime gate, corpus, ledger, protected tree or provenance follower was read. Existing publication tooling is used only for metadata. No agents, network, AWS, SSH, process inspection, worker, installation or external publication occurred. All edits are confined to the new box and private report partial; accepted old files remain unchanged.

The new sources and docs are own-WHOLE read as text before completion. The three copied files are checked byte-identical; the source hash literals are reconciled with current hashes. These are documentary/static checks, NOT executed predicate tests or runtime evidence. The report uses begin/private partial/own whole/open/collision/marker/close/finalize/expected verify. Custody binds all13 current inputs and all owned final artifacts excluding itself. All writers IDLE at terminal handoff before21:50:24UTC.

## OPEN(S) RAISED

One bounded implementation-validation obligation remains: syntax/installed FLINT API plus both positive and negative generic verifier branches, including high-window and precision checks. Quantity: one separately root-registered finite compatibility/generic-test harness (not supplied here) followed, only if authorized, by the single real candidate/check/control batch. Cheapest next action is one different-model static delta review of these sources and exact certificate semantics; then ROOT may assign the finite tests, native pins and caps. No unknown resultant, ideal rank or new mathematical premise is declared solved. No new campaign OPEN identifier or exit-price assertion is authored.

## COLLISIONS

status: EMPTY

- NONE: only own targets checked, absent before begin; no corpus scan.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `13174`.
- Body SHA-256:
  `aaeb3a15403028ac7e1cff238b76fcae61856bd2b54d7a045d97dd7850aeb7a0`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
