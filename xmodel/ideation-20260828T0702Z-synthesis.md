# Whole-campaign ideation synthesis — `20260828T0702Z`

Date closed: 2026-08-28 08:55Z UTC.

Status: **CLOSED — four blind submissions received; post-snapshot news
adjudicated separately below.**  No proof or counterexample to JC2 is
claimed.

## 1. Custody and independence

The frozen packet is
`xmodel/ideation-20260828T0702Z-packet.md` (SHA-256 `b0c9110f...`).  Four
blind whole-portfolio submissions arrived before cross-reading:

| lane | artifact | SHA-256 |
|---|---|---|
| Sol Ultra | `xmodel/ideation-20260828T0702Z-sol-ultra.md` | `3eb422e4...` |
| Fable 5 | `xmodel/ideation-20260828T0702Z-fable5.md` | `c3d6c88e...` |
| Opus 5 | `xmodel/ideation-20260828T0702Z-opus5.md` | `9339a398...` |
| Grok46 | `xmodel/ideation-20260828T0702Z-grok46.md` | `8181ce8c...` |

The packet was not amended after launch.  Several decisive events arrived
after the snapshot and are therefore an explicit adjudication overlay, not
retroactive blind input:

- the D7--D9 theorem was independently reviewed and strengthened to a
  q1-free field theorem (`1874b9db...`);
- Fable's proposed single-root transport was built by Sol and then repaired
  and confirmed by an independent Opus5 clean-room reconstruction
  (`21064740...`);
- a same-model deep active-`c2` reconstruction found one real valuation error
  and a clean exact `D=0` successor (`da189b7f...`), with different-model
  Fable review in flight;
- one exact literal deep point passes the homogeneous rows through D21 and
  dies only at the affine D22 target (`a4bfbb14...`); its AWS exact-Q tangent
  system is inconsistent with a 19-row dual, but the dual is exactly
  normalized evaluation at `(X,t)=(0,8)`, where both weighted coordinate
  gradients of the base vanish.  Thus the first-order obstruction is an
  automatic rank-zero phenomenon, not evidence that the point is isolated;
- Fable audited both new Matysiak papers and refuted their load-bearing
  arguments (`c5655b43...`); they cause no rerank.

## 2. Executive synthesis

The round found one decisive compression and the campaign has already
realized it: **at a single simple root of `A` where `V0` is a unit, the full
local D7--D22 ladder contradicts the endpoint target.**  Consequently, for
squarefree `A`, every `gcd(A,V0) != A` endpoint stratum is field-empty.  The
proper-divisor `W` atlas proposed by Sol and Grok is therefore no longer on
the endpoint critical path.  It remains correct explanatory residue algebra,
but further charting there is dominated by the stronger unit-root theorem.

The unique surviving upper branch-P endpoint target is now

```text
A | V0.
```

This deep locus is not a cosmetic last case.  Its Newton scaling changes,
born rational modes remain live, a literal point survives every homogeneous
row through D22, and the failed translation shear cannot be used as a raw
gauge.  The next discovery work must attack the deep locus directly and keep
`c2=0` and `c2!=0` separate until a proved interface joins them.  The exact
tangent computation should be followed by a second-order/Kuranishi test, not
advertised as local rigidity.

The proof-wide ranking is less changed than the local GGV ranking.  Even a
complete deep-locus exclusion would still need the global landing/coverage
theorem and a cofinal complexity ceiling before implying JC2.  `G2-PSC`
(packet/sheet compatibility) and `G2-BD` (bounded delay after residue-A) are
distinct debts.  No round proposal discharges either, `RPMC(C)`, L3/L5, the
exact-pair landing interface, or the cofinal `td` wall.

## 3. Cross-pollination and history/priority checksum

| proposal or connection | classification after checksum | decision |
|---|---|---|
| Fable `SRT-ONE-ROOT` | **NEW, NOW REVIEWED WITH REPAIR.** Opus's independent root-local Newton model supplied the missing general-prefix D7--D15 bridge and the 432-term endpoint check. | Promote the repaired unit-root theorem; retire the producer shortcut. |
| Opus root-local `LQ` scaling | **NEW MECHANISM, PARTLY REALIZED.** Its `eps^2` unit-root specialization is now load-bearing evidence for SRT. | Reuse the scaling language for the deep `A|V0` Newton model; do not infer the unproved higher resultant ladder. |
| Sol/Grok post-D9 `W` support/product atlas | **NEW LOCAL DESCRIPTION, SUBSUMED FOR ENDPOINTS WITH `C!=A`.** | Stop proper-divisor endpoint fanout. Preserve the product law as a diagnostic and possible scheme-thickness input only. |
| Fable/Grok active-`C=A` card; Sol active-`c2` block | **CONVERGENT NEW TARGET.** Post-round exact work reached D13 on the clean `D=0,c2!=0` successor and produced a D0--D21 literal point. One producer inference `A|D =>` regularity was false. Its exact tangent obstruction is automatic at a rank-zero evaluation point and does not show isolation. | Highest-priority local proof/falsification lane, under background different-model review; move next to second order or a universal endpoint invariant. |
| Sol target-density straightening flow | **NEW IDENTITY, SCOPE-CONFLICT AS A RAW GAUGE.** Exact determinant equivariance does not preserve frozen Newton windows and creates a bound G22 receiver. | Do not launch a gauge reduction. Reconsider only after a proved solution-locus descent theorem. |
| `T_A`/de Rham/cokernel connection from Fable/Grok | **NEW CONNECTION, NOT YET A THEOREM.** The operator echo is real but its windows differ, and SRT removes the proper-divisor client. | Keep as a light tool for any deep-locus first-order image test; no standalone AWS lane. |
| Opus HENS-CT client for root-local coefficient sequences | **NEW CLIENT, SOFTWARE-GATED.** | Finish the rank-one control certificate and mutations first; then test one named deep-locus sequence only if the control passes. |
| Opus `A(F)`-to-KEF meridian bridge | **NEW CONNECTION, BLOCKED ON TYPED DATA.** History already records that `A(F)` has never been constructed from a complete boundary book and that target/source meridians differ. | Preserve as a diversification card; start only from one completed TD6 boundary book and fail closed on any unpinned tail. |
| Fable/Grok request to raise primitive-monodromy stage 1 | **KNOWN / DUPLICATE / CONTRADICTED BY HISTORY.** `xmodel/sol-monodromy-td.md` (`b8144631...`) already executed the exact `td=6..9` census: every degree survives and symmetric/alternating groups hit the predicted wall. | Do not rerun or raise Avenue 26 without new geometric control of finite inertia/support or an exclusion of `A_d/S_d`. |
| parameterized raw compiler | **KNOWN NEED, PARTLY BYPASSED LOCALLY.** SRT is symbolic in a squarefree `A` at unit roots, but global landing and the deep locus still require parameter/window custody. | Continue as landing infrastructure, not as a substitute for the deep theorem. |
| Matysiak condition `5''_s` papers | **REFUTED-ON-AUDIT.** Circular injectivity/UFD steps, an assumed-JC unramified step, and a false Jordan reduction. | Archive as negative intelligence; no proof lane or compute. |

Shared wording was not counted as independent evidence.  In particular, the
three-way enthusiasm for primitive monodromy was stale-packet correlation,
not a vote against the already banked negative census.

## 4. Updated avenue and bottleneck ranking

### Proof side

1. **Global landing/coverage and cofinal ceiling (Avenue 2).**  Still the
   largest JC2 wall: exact-pair two-chart construction, L3/L5, `RPMC(C)`,
   `G2-PSC`, `G2-BD`, and a cofinal `td`/complexity ceiling remain open.
2. **Deep GGV upper endpoint `A|V0` (Avenue 1).**  Now the sole local
   branch-P endpoint locus and the best short-horizon information target.
3. **Source-faithful parameter/window transport.**  Needed both to connect
   raw exclusions to Keller pairs and to prevent fixed-support results from
   being overread.

### Disproof side

1. **A source-open, rank-exact K00/full-`P6` point plus prolongation.**  The
   strongest finite non-GGV falsifier; any successor must use the reviewed
   rank/Fitting compression rather than rerun a capped presentation.
2. **A deep-locus family surviving endpoint and algebraization.**  The known
   literal point is only a homogeneous raw seed and already fails affine
   D22.  The exact tangent test is the cheapest current discriminator between
   isolated rigidity and a deformable survivor component.
3. **Source-complete Artin--Schreier / D43 bounded-support algebraization.** 
   Finite-depth or growing-support Witt survival remains non-counterexample
   evidence.

### Avenue calls

- **Continue/raise internally:** Avenue 1, but only on `A|V0`; no more
  proper-divisor or fixed-`V0=1` endpoint fanout.
- **Continue as the proof-wide backbone:** Avenue 2 and its separately named
  `G2-PSC`/`G2-BD` obligations.
- **Continue narrowly:** Avenue 16 after HENS-CT control; K00, D43, LF40,
  TD6/order-two, and Artin--Schreier under existing source/custody gates.
- **Do not raise:** Avenue 26 primitive monodromy without genuinely new
  finite-inertia/support geometry; its generic group census is already
  negative.
- **Keep closed/stopped:** fixed `V0=1` tails, cutoff-3 reruns, five-mode
  cutoff-two, naive translation gauge, naive van-Dobben-to-plane descent,
  and the Matysiak proof claims.

## 5. Ranked launches and stop rules

1. **Bank, then supersede the interpretation of, the exact-Q tangent
   certificate.**  The frozen result is exact (`rank=291`, 19-row dual), but
   its coefficients `8^(n-22)` are evaluation of the linearized determinant
   at `(0,8)`.  At the base, `F_X=G_X=tF_t-8F=12G-tG_t=0`, so this dual
   annihilates arbitrary perturbations automatically.  Do not infer scheme
   isolation.  The honest successor is the quadratic/Kuranishi equation on
   the 17-dimensional tangent kernel, or a conceptual critical-point-motion
   analysis.
2. **Complete different-model review of the deep D8--D13 repair and literal
   tail.**  New descendants may proceed provisionally; promotion waits for
   review.  Any load-bearing failure rolls back only its descendants.
3. **Attack `A|V0` directly in two sublanes.**  One handles `c2!=0`, starting
   with the exact `D=0` branch and then the omitted `A|D` valuation branch;
   the other handles `c2=0` and its rootwise `H=0 or D=0` split.  Stop broad
   elimination at the first exact low-dimensional invariant or compensation
   stratum and redesign around it.
4. **Finish HENS-CT control and mutations.**  Only a full control certificate
   licenses one deep-locus client.  Reducer disagreement, failed typing, or
   mutation survival makes the backend non-evidence for that client.
5. **Keep a global lane live.**  Resume the smallest typed landing/coverage
   obligation rather than allowing local GGV momentum to consume the whole
   portfolio.  The `A(F)`/KEF connection is a backup only after one complete
   boundary book exists.
6. **Preserve independent falsifiers.**  Continue existing D43 and LF40 jobs;
   launch a compressed K00 successor only when its exact rank-open target and
   fail-closed stop condition are frozen.  Idle AWS is preferable to a stale
   duplicate.

Heavy algebra stays on AWS with registered process custody, zero swap, and
predeclared caps.  Local work is limited to light exact standard-library
checks and synthesis.  No task enters or interferes with `jc2-lean`.

## 6. Round conclusion

The round paid off unusually quickly: Fable supplied the decisive
single-root compression; Sol instantiated it; Opus independently found the
same Newton scale and then repaired and validated the full bridge.  This
collapses all nondeep divisor strata and turns a many-chart general-`V0`
problem into one sharply named locus.  The campaign should exploit that
compression without mistaking it for JC2: the local next target is
`A|V0`, while the global next target remains landing/coverage plus a cofinal
ceiling.

Next quiet full-round deadline: 2026-08-28 20:55Z;
any reviewed deep-locus theorem, exact tangent verdict, HENS-CT control
verdict, external JC2 claim, or rank-changing global result fires an earlier
micro/full round under `COORDINATION.md`.
