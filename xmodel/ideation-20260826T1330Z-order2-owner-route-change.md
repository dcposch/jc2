# Blind whole-portfolio delta after affine-Faber/TD6 route changes

Date: 2026-08-26 13:30Z

Status: **INDEPENDENT IDEATION; NOT A THEOREM OR QUEUE AUTHORITY.**

## Inputs and firewall

I reread the canonical 46-row inventory and current top-level state, without
reading sibling ideation packets.  The two route-changing inputs were:

1. the normalized affine-Faber parity/implicit-function theorem, SHA-256
   `fe1193abe8c45c590f7653d96998a7b053e8de0e09a751cd8fb686d2cbf72fc1`,
   with dual-AWS data result SHA-256
   `a9cb98448d0ff6bdd13120966c870cfc3be95c54bb3a7d67ac80e975e1fd3cc2`;
2. TD6 V85TF1 total-`F` producer result/evidence/freeze SHAs
   `f5af4a1c2e5259000ff266807206b3f4c32576ba9143073239464c71b925e3ce`,
   `c92cddadb33d3fb8778696685cc893ba948e779ed259ed30e67532f65b1df443`,
   and
   `500a64f9296767d531528fd94f3626f9308b65d428df4e9369f1ba8a89194c4e`.

The affine result is still a normalized-coefficient hand theorem with its
hostile review and literal total-Rees consumption gate pending.  V85 is a
producer identity on one normalized total-`F` slice, not a total TD6 chart.
Everything below is therefore a proposed connection or falsifier, not a
promotion of either input.

I found no route-changing consequence for the dead stronger-conjecture
ladder, real/holomorphic inversion, naive scaling, Markus--Yamabe,
free-associative lift, unrestricted finite-field census, or formalization as
a discovery route.  The meaningful deltas concern the boundary/formal-germ
core (rows 1--4), terminal/monodromy/Ritt (25--27,43), Rees
integrality/collision (31--32), exact symmetry (18), and guided/tropical
software (36,38).

## Ranked new connections and cheapest separators

### 1. Replace most of the square contact fan by one adjugate-to-total-Rees certificate

On

```text
delta = D*(5*D+2*s)*(5*D-2*s) != 0,
```

the three odd Faber rows have an invertible Jacobian in all three odd octic
coordinates, parity supplies the zero odd section, and hence `J=0` in the
completed normalized coefficient ring.  V85 shows the exact computational
shape needed to turn a special/normalized identity into a valuative total
statement: retain the literal source, reuse frozen multipliers, divide the
total residual by the released base parameter, and reject every denominator
outside the registered open.

The high-value experiment is therefore not another box of contact orders.
It is a source-typed total-Rees compiler which first builds an explicit
`delta`-localized adjugate/Fitting certificate for the odd block and then
performs V85-style exact residual division in every omitted Rees/base
direction.  A pass would eliminate all finite contact orders over the
generic affine open at once, leaving only `D=0`, `k10=0`, and the two
resonances.  This is the strongest bridge between rows 2--4 and row 31 now
visible.

**Cheapest falsifier:** release one omitted moving-center/Rees coordinate,
recompute the three odd rows literally, and demand both the same determinant
factor and coefficientwise divisibility of the lifted certificate residual
by that coordinate, with no new denominator.  One extra odd column, torsion
class, or outside denominator kills the proposed shortcut immediately.

### 2. Irreducible-source locking converts two resonances into two global branches

Suppose the total-Rees import in item 1 is valid.  A genuine source curve is
irreducible.  If every `J != 0` point lies in

```text
(15*Delta-32*beta)*(5*Delta-32*beta)=0,
```

then its coefficient-map image cannot alternate between the factors: in the
source function field, one factor vanishes identically.  Thus the residual
generic-square problem is not an unbounded valuation fan but two global
load-ratio branches, plus the separately routed `Delta=0` and `k10=0`
boundaries.

This composes naturally with the terminal-power/Belyi and weighted-tree
frontends.  On each locked branch, pull the fixed load ratio through the
terminal `[6,2]` divisor/moment equations and both finite Taylor maps.  The
terminal map remains nonconstant, so any resulting constant/pure-power
identity is a genuine global obstruction rather than a pointwise tangent
calculation.  This links rows 2--4 to the coupled passport problem in row 25
without enumerating all local cones.

**Cheapest falsifier:** in the literal source domain, verify the displayed
product identity after `J`-saturation and prove the relevant component is a
domain (or work componentwise).  Then compile the `A` and `K` factors as two
separate source clients.  A third irreducible factor or a source component
contained in the normalization/chart-loss boundary defeats the lock.

### 3. The resonance branches may be low-critical-value/Pell strata, not arbitrary singular loci

The affine polynomial part is

```text
A(T)=T^5-((s+5D)/4)T^3+(D*(2s+5D)/16)T.
```

On the `A` resonance `s=-5D/2`, it factors as
`T^3*(T^2-5D/8)`; on the `K` resonance `s=5D/2`, its coefficients are
equally rigid.  The Jacobian rank drops exactly where these Padé data acquire
extra critical multiplicity.  This suggests treating the two survivors as
polynomial-Pell/critical-value strata and applying Ritt/dessin methods, rather
than continuing only by local Taylor expansion.  The existing Chebyshev/Pell
control lies off the two resonances and correctly has `J=0`; it is a useful
positive control, not a source survivor.

**Cheapest falsifier:** over exact Q on AWS, compute the critical-value
resultants of the two displayed one-parameter polynomials and compare their
ramification partitions with the reviewed terminal `[6,2]` passport.  If a
partition is incompatible, that entire resonance dies by a short hand
theorem.  If compatible, solve the corresponding polynomial Pell equation
and feed only its finite parameter families to Taylor, rather than a general
coefficient scheme.  This is a concrete revival of rows 25 and 43, not a
generic monodromy slogan.

### 4. Turn V85 into a multibase Cech cube before computing deeper TD6 bands

V85 proves that the fixed-`F` certificate propagates in one honest total
direction on `D(U*H*B3)`, with all 132 transport-free section coordinates
retained and no inversion of `F`.  The unresolved debt is now breadth of the
base, not depth of the same band: q, dead stretch, correction, orbit/pole,
centering, and boundary moduli are frozen.

Build a small Cech cube.  Each edge releases one missing base variable and
must emit a V85-style residual-division identity with an allowed-denominator
list.  Each two-face checks that the two release orders give the same
localized ideal/certificate.  Only after pairwise commutation passes should
one attempt a multivariable total chart.  This targets the source/coverage
gap in rows 1--4 and the valuative Rees formulation of row 31 much more
directly than another high-order CURRENT/K2 computation.

**Cheapest falsifier:** free the algebraically simplest non-`F` base
coordinate, then one two-parameter square with `F`.  Any outside denominator,
missing source label, or order-dependent saturation is an immediate stop.

### 5. Generalize the parity proof to a finite-deck character-block compiler

The mechanism is representation-theoretic: at a deck-fixed coefficient
locus, rows split into characters, the terminal `J` row is in a nontrivial
character, and an invertible same-character Jacobian block forces that
character coordinates, hence `J`, to vanish.  This is not the failed
`G_m`-graded-map route in row 18; it uses the finite Kummer deck action on a
source-licensed local coefficient space.

Implement one compiler which, for each remaining exact order, emits the deck
character table, same-character Jacobian minors, determinant factorization,
and exceptional strata.  Order four is already eliminated, but order three
and the residual order-two branches provide immediate controls.  A finite
resonance list in order three would connect the D1 local work to the same
global terminal/passport locking as item 2.

**Cheapest falsifier:** run the character-block determinant only on one
remaining order-three coefficient client over exact Q, with the full source
rows and no saturation.  If the expected block is rectangular or
identically singular, do not globalize the pattern.

### 6. Make typed polynomial emission a campaign invariant

The quarantined `a=9` V2 census showed that a mathematically correct source
can be corrupted merely by substituting an unparenthesized load series into
a weight-one monomial.  This threatens every avenue using generated Faber,
Rees, carry, or transport rows, not just D1.

Use one expression-tree intermediate representation and generate Singular,
Sage/Python, and human-readable forms from the same nodes.  Mandatory static
checks should include load distributivity, weight, deck character, target
parity, first/last possible grade, and byte-level round-trip equality.  Add
an automatic V85-style residual-division/allowed-factor checker and an
adjugate-minor emitter.  Existing scripts remain immutable controls.

**Cheapest falsifier:** re-emit the corrected `a=9` V3 and V85 sources and
require byte-normalized equality of every weighted load term and every
source inventory hash.  This is software acceleration, not mathematical
evidence.

## Allocation delta

Until the two new inputs are reviewed/consumed, I would allocate the next
bounded wave as follows:

- **35%**: item 1, the literal total-Rees parity/adjugate import;
- **20%**: items 2--3, the two resonance branches plus terminal/Taylor/Pell
  classification;
- **25%**: item 4, TD6 multibase Cech coverage;
- **10%**: item 5, the order-three character-block falsifier;
- **10%**: protected global landing/ceiling and fixed-support disproof work,
  with Lean continuing only on reviewed stable statements.

The immediate proof-discriminating separator is item 1.  If it fails on one
released Rees coordinate, return to the correction fan with the exact new
odd/torsion module exposed.  If it passes, stop broad generic-square fan
enumeration and spend the saved capacity on the two locked resonances and
TD6 chart coverage.
