# Opus 5 hostile review — Fable Q+E5 two-pole merge-local theorem

Producer: Fable 5. Work independently from `/Users/dc/code/math/jc2` and
write exactly:

`xmodel/m2-caseiii-two-pole-e5-local-index-r2-hostile-review-opus5-20260829.md`

Read the complete target
`xmodel/m2-caseiii-two-pole-e5-local-index-repair-fable5-20260829.md`, packet
`cases/m2_caseiii_two_pole_e5_local_index_r2_20260829/`, Sol R1 and Fable's
review of it, the promoted H5a/Q+E5 sources, current BOOK-OFFAXIS/DEPTH/
MULTIPOLE clauses, and only the directly relevant portions of your earlier
M2 reports. Treat every assertion as an allegation; do not read post-cutoff
ideation submissions or unrelated active outputs.

Re-derive and attack:

1. exact scope H1--H4 and pattern identities `D=nu*A-delta`, `M|D`;
2. Lemma A and every equality condition, including widening counterexamples;
3. the M-graded bound, equality iff conditions, and the claim that exactly 12
   promoted cells attain it;
4. the Q+E5 elimination
   `mu0*nu_G*w_U=delta*kbar+mu*w` in all three multiplicity regimes;
5. the claim that incoming `nu_U` is genuinely free, irrelevant to the cell,
   and should be represented by a state/congruence menu rather than bounded;
6. the new infinite pattern fibre for `mu>=2` at fixed `(kbar,nu_G)` and its
   consequence that the engine must retain an existence solve;
7. the migration plan's scope guard, fallbacks, gates, and whether it can
   safely remove the legacy cap only on the exact two-pole leaf scope.

Replay ordinary and `-O` suites and build clean-room exact sweeps/mutations in
`/tmp`, including an independent reconstruction of all 17 promoted cells and
the `10^20+4` incoming-index control. Return
`PASS_AT_QE5_TWO_POLE_SCOPE`, `REPAIR_REQUIRED`, or `FAIL`. Separate theorem
promotion from engine-migration authorization; a mathematical PASS does not
authorize editing `book_offaxis.py` unless every source-level migration gate
is independently certified. Include full/body hashes, commands, counts, and
the narrowest maximum consequence.

Hard boundaries: no web, AWS, commit, push, canonical edits, heavy CAS, or
local long/high-memory process. Never access/list/search/build/status or
control `jc2-lean`; never run global `git status` or workspace-wide searches.
Modify only the requested review; use `/tmp` for scratch.
