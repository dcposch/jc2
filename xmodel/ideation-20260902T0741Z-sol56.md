# Blind ideation submission — round 20260902T0741Z (sol56)

## 0. Custody, scope, and typing

The frozen input hashed to
`2646ac692c292c30597f9c830fd145c85e020207d803927825b8a162086055cf`,
exactly as charged. I read no `ideation-20260902T0741Z-*` sibling report, log,
or prompt. The target report is the only current-round file I wrote. Sources
below are `[P]` = the packet, `[M]` = `xmodel/mprime-alln-h2-opus5-20260902.md`,
`[CC]` = `xmodel/companion-curve-alln-opus5-20260902.md`, and `[HF]` =
`xmodel/horn-flagship-opus5-20260902.md`. Two lanes landed after the freeze:
`[AU]` = `xmodel/a2-ubound-opus5-20260902.md` and `[CG]` =
`xmodel/cusp-a-n8-gate-opus5-20260902.md`; both remain unreviewed here.

All mechanisms proposed here are `IDEA / UNREVIEWED` until their stated
discriminator runs. `REPRESENTATIVE`, curve realization, a cover of a curve,
and an actual Keller map are kept distinct. I make no new exit-price assertion.

## 1. Executive thesis and reframing

The packet's “one A2 theorem” framing is already obsolete. `[AU]` finds a
load-bearing missing `-2qE1` in `EQ2_even`, refutes RAY-2, and un-derives the wall;
in corrected Chamber II it makes only each fixed `(e,c)` slice finite outside
named branches, while `c` remains unbounded `[AU:45-102]`. Separately, `[CG]`
claims two all-degree closures of case (A) `[CG:43-75]`. Both are high-value
but unreviewed. `H2` still means exactly that `A_F` is irreducible
`[M:26-30]`; its complement is reducible `A_F`, and no bounded-`N`
architecture can finish B2/B3/B1 plus that complement.

I reframe the frontier around two questions. First, is A2's `U` geometric
complexity or merely the length of a unimodular/continued-fraction
presentation? Second, can any survivor traverse a declared state arrow—cell to
analytic pullback, curve representation to global cover/source, or companion
curve to jointly realized reducible `A_F`? Standalone companion data are
attained by curves and still imply no map `[CC:499-525]`.

Put the cofinal ceiling and state arrows ahead of more bounded cages. On the
positive side, solve a corrected A2 deviation branch or realize an actual
`N>=6` curve-cover package. The rigid `N=4` B3 representation datum is a
regression fixture; only its Keller realization is known impossible.

## 2. Disposition vectors

### 2.1 Changes among the 46 `APPROACHES.md` rows

Only changed rows are listed; the other 33 are unchanged.

| Row | Disposition | Reason |
|---:|---|---|
| 2, sheet/boundary trees | **raise** | Now an all-`N` H2 profile theorem, not only a degree ladder; its cofinal limit is explicit `[M:413-476]`. |
| 5, JvdK descent | **retype** | A2 gives a matrix-level `GL_2(C[Z])` client; reopen only after its column operation maps legally to source variables. |
| 6, Abhyankar–Moh | **retype** | Sharp for `MULT-2`, but explicit companions attain the remaining standalone data `[CC:434-471]`: classifier, not endgame. |
| 7, `A(F)` | **raise** | `A_F` now organizes the H2/reducible split, B3 topology, and source-realization gap. |
| 25, fibre monodromy | **raise** | The curve-to-ZvK pipeline is validated and B3 is pinned; decisions remain curve-level `[P:135-141]`. |
| 26, primitive monodromy | **retype** | Keep prime/block screens for B2/B3/reducible profiles; a reviewed case-(A) closure would remove that client. |
| 27, links/plumbing | **raise** | B3 push-off data determine components, genus, and ends, though not emptiness `[HF:315-374]`. |
| 28, log surfaces/BMY | **retype** | Retire reducible Euler species; keep declared completions and intersection-lattice sign `[P:65-70]`. |
| 29, Hamiltonian derivations | **retype** | The live client is the corrected deviation system and its exceptional `c` branches, not the retired wall cells `[AU:72-102]`. |
| 30, affine surfaces | **retype** | Test `(Y,B_Y)` containing `U~A^2`; `Y` itself is not the recognition target `[P:70-76]`. |
| 31, ZMT/integrality | **retype** | Construct the open immersion and typed boundary; global finiteness would erase the required boundary. |
| 36, guided CE search | **retype** | Use source-derived A2 or profile-directed `N>=6` families with state certificates, not random supports. |
| 39, cohomology | **raise** | NO-PUSHFORWARD still blocks local transfer, but the landed global peripheral/meridian ranks claim an all-`N` case-(A) kill `[CG:60-75]`. |

### 2.2 Gaps (a)–(l)

| Gap | Disposition | Reason |
|---|---|---|
| (a) A2 `U` bound | **retype** | O0/O1 are resolved; audit the P0, then pin free `c` and treat the named degenerate branches `[AU:805-886]`. |
| (b) B3, 4–16 | **retype** | Use closed `N=4` as a regression to find a uniform invariant, then apply it from `N=6` `[HF:263-374]`. |
| (c) cusp-(A) N8 | **lower** | `[CG]` claims two independent all-`N` closures; review, harvest, and remove the gap if confirmed. |
| (d) B2 | **retype** | Start at B2-N6; beta floors are triage. Seek a singular-branch or `deg A_F` bound `[M:673-684]`. |
| (e) B1/ceiling | **retype** | `R<=1` would kill B1, but a separate cofinal invariant must cover every profile and reducible `A_F` `[M:659-670]`. |
| (f) companion | **retype** | Standalone existence is positive; the gap is joint common-infinity/source-open realization `[CC:499-525]`. |
| (g) N>=6 census | **retype** | Search B2/B3/reducible profiles separately; prime 7 is only a cheap scout and every curve needs preflight. |
| (h) A2 construction | **retype** | Use the corrected free-`c` deviation system; retired wall-cell points and formal jets are not `F`. |
| (i) B3 N4 construction | **lower** | Known-closed degree: regression only, never a CE target. |
| (j) AS109 | **unchanged** | Keep one declared exact `Z_109` finite-support problem, separate from growing-support controls. |
| (k) A2–B3 bridge | **retype** | The packet contradicts itself on object identity `[P:126-129,179-181]`; require an explicit object map. |
| (l) nontermination | **lower** | The old ladder used a flawed equation; corrected deviation bounds replace its positive-signal reading `[AU:805-840]`. |

## 3. Reranked bottlenecks

**Proof, in order.**

1. **Audit the two landed lane resets.** The A2 P0 changes the computation
   basis; the case-(A) theorem would remove an all-degree H2 profile.
2. **Pin A2's free `c`, then decide `c=-2e` and the full `k=2e` stratum.** A
   fixed-parameter finite box is not an unconditional `U` bound.
3. **Find a B3 invariant that consumes its forced singular preimages.** The N4
   regression already shows why the case-(A) units proof cannot transfer.
4. **B2 singular-branch geometry and B1.** Beta floors remain triage.
5. **Reducible `A_F` joint realization/ceiling.** Standalone companions do not
   cover this branch.

**Disproof, in order.**

1. **One typed arrow beyond `REPRESENTATIVE` at an open degree:** full A2
   pullback or realized `N>=6` curve plus complete cover.
2. **A2 finite termination**, not merely an all-order formal solution.
3. **Preflighted `N=6` actual-curve census**; a tuple zero is not row coverage.
4. **Joint reducible realization** with common infinity and source-open boundary;
   the standalone nodal companion already attains its necessary data.
5. **Exact finite-support `Z_109` lift**; growing-support analytic limits do not
   threaten JC2.

## 4. New mechanism and cross-connection

### `A2-SL2/DEV-RESULTANT` — a genuinely new mechanism

On a simultaneous-top cell with `U>=3e+2`, the structural degrees and E0 give

```text
n = U-e,       sigma = (U+e)/2,
deg q = U,     deg s = sigma,
deg r' = n-1,  deg p' = 3n/2-1,
2(q r' - p' s) = kappa != 0.
```

Here every prime means `partial_Z` by the source convention
`[xmodel/cell-32-spec-sol56-20260901.md:120]`; longer formulas spell the
derivative out to avoid ambiguity.

Thus

```text
             [ q   p' ]
M(Z) =       [ s   r' ]       has det M = kappa/2 in C*.
```

This is not an analogy: after a constant rescaling, `M` is an element of
`SL_2(C[Z])`. On this `U>=3e+2` branch, the two degree differences are
the same nonnegative integer

```text
t := deg(p')-deg(q) = deg(r')-deg(s) = (U-3e-2)/2.
```

Moreover

```text
r'/s - p'/q = kappa/(2qs).
```

The right side is strictly proper at infinity, so the polynomial parts of
`p'/q` and `r'/s` coincide. Call that common quotient `H(Z)`, `deg H=t`, and
write

```text
p' = Hq + ptilde,       r' = Hs + rtilde.
```

Then `q rtilde-ptilde s=kappa/2`; equivalently the elementary column operation
`C_2 <- C_2-H C_1` preserves the unit determinant and strictly reduces the
second column. Repeating the Euclidean algorithm gives a continued-
fraction/elementary-matrix word for `M`. `E0` alone permits arbitrary word
length; any Mason–Stothers bound must therefore consume other equations.

Before choosing a complement, the directly rederived E0/O0/O1 give a sharper
compatibility test. Put

```text
W = q partial_Z(s) - partial_Z(q) s,
T = -(C1 partial_Z(E1)-partial_Z(C1) E1)/2
    -3 D1 partial_Z(s)/4 + partial_Z(D1) s/4 - ZW/2.
```

Then E0, O0, and O1 are exactly the following three affine rows in the two
source variables `(r',p')`:

```text
[q,  -s ] [r',p']^t = kappa/2,
[C1,-E1] [r',p']^t = -W/2,
[C2,-E2] [r',p']^t = T.
```

Their augmented determinant must vanish, giving the unreviewed exact identity

```text
D := (s C1-q E1)T + (W/2)(s C2-q E2)
     + (kappa/2)(E1 C2-C1 E2) = 0.                 (DET-EO)
```

Here `E2` is the coefficient polynomial `E2(Z)=b Z eta^2`, not the equation
called E2 in SPEC; call the latter `EQ2_even`. `DET-EO` uses only E0/O0/O1,
which `[AU]` rederived directly `[AU:67-70]`, and does not consume flawed
`EQ2_even`. The tempting specialization to the old E1 wall and RAY-2 is
illegal: corrected `EQ2_even` contains `-2qE1`, the wall is underived,
and RAY-2 is refuted `[AU:805-816]`.

The decisive new move in corrected Chamber II is a **projection to the free
`c`-line**. Substitute `[AU]`'s deviations

```text
X=4b eta^2 r-s^2,        Y=4b^2 eta q-3a s^2
```

into the corrected `s`-free EQ1–EQ3. Away from `c=-2e`, `[AU]` proves
`deg X,deg Y<=4e-1`, independent of `U` `[AU:72-102]`. For fixed `e`, let
`I_e` be their coefficient ideal in the explicitly ordered rational ring

```text
Q[eta_0..eta_(e-1), G_0..G_(2e-1), X_0..X_(4e-1),
  Y_0..Y_(4e-1), kappa, tt, c],
a=b=eta_e=1 (S1/S2), c5=0 (licensed C32 shear), G_(2e)=c,
kappa*c*(c+2e)*tt-1=0,
```

using a block elimination order with every variable but `c` in the first
block. Compute `J_e=I_e intersect Q[c]`. If `1 in I_e`, the declared main
branch is empty. If `J_e` contains a nonconstant polynomial, `c` has finitely
many candidates; in branch (A), for `3-e<=k<=4e-1`, `k!=2e`, outside the named
exceptional values, `[AU]`'s `R(e,c,sigma,k)` then leaves
finitely many `sigma`. The entire vanished-factor stratum `k=2e`—including
`deg X=deg Y=2e`, not covered by the named unequal-degree branch—is a separate
`OPEN[A2-K2E]`; `(e,k)=(1,0),(1,1),(2,0)` go directly to full EQ4/O1s. If
`J_e=0`, the `s`-free subsystem does not pin `c`; feed its
locus to the `s`-dependent stage. `DET-EO` is a checksum there, not an extra row.

Treat `c=0` as Chamber III; split `c=-2e`, `c=-(2e+1)`, Wall A
`3c+6e+2=0`, `OPEN[A2-K2E]`, and Chamber I. Polynomial reconstruction uses the
three exact divisions in Section 6. A fixed `(e,c)` box is not global A2
finiteness, and none of this supplies an actual map.

This cross-connects Avenue 29's deviations with Avenue 5's elementary descent.
Factoring `M` is not a legal source/target automorphism; that map remains
`OPEN`, so it cannot make `U` gauge.

### Safe analytic/group bridge

Do not identify A2 and B3 by their “horn” label. First require
`OPEN[A2-TO-B3-OBJECT-MAP]` to output `A_F` and `E=F^{-1}(A_F)` from a full
analytic solution. Only then compare

```text
(#components(E), genus(normalization(E)), #places_infinity(E),
 local branch types, cover cycle types).
```

At `N=4`, B3 pins this signature `[HF:315-349]`, while the explicit A2 chart is
degree eight under its rank hypothesis: they are not comparable. At matching
degree/profile, mismatch kills only the routed cell and agreement only licenses
construction. Without the object map, return `OPEN`.

## 5. Strongest proof attack

### Harvest and audit `CUSP-A-VOID`

The strongest landed attack is `[CG]`'s `CUSP-A-VOID` (`PROVED-HERE /
UNREVIEWED`). In case (A), its first chain is short enough to audit before any
more census work:

1. the cusp of `A_F` has no preimage, so local étaleness makes
   `E=F^{-1}(A_F)` smooth and `F|E:E->A_F\{c}=C*` étale;
2. `chi(E)=1` forces some connected component to be `A^1`;
3. a morphism `A^1->C*` is constant because the only units of `C[t]` are
   constants, contradicting dominance.

This claims case (A) empty for every `N>=2`, independently of the measured N8
ledger `[CG:43-75]`. Its separate peripheral-rank/meridian-span chain kills the
same profile again. Promote only after checking the no-cusp-preimage lemma,
the `chi(E)=1` hypotheses, component dominance, and that both chains use the
global case-(A) group rather than a forbidden local pushforward. In Chain II
use only the one-way fact that each component-basis meridian occurs among fixed
lifts; do not biject components with fixed sheets, and allow repetitions.

Do not transfer this proof to B3. The exact N4 regression forces a cusp preimage
`y_0`, makes `E` singular there, and gives `chi_c(E)=1-4k`, not `1`
`[HF:315-349]`. Thus both load-bearing steps fail before any units argument.
This is a useful negative cross-connection: it scopes the all-degree case-(A)
closure and says a B3 proof must consume its singular preimages rather than
delete them. `Gamma` should reject any proposed invariant that silently imports
smoothness or `chi(E)=1`; a safe replacement remains typed `OPEN`.

## 6. Strongest counterexample / falsification attack

Run `CORRECTED-DEVBOX-SHOOTER`. Rebuild all seven E/O equations from the source
Even/Odd identities, including `-2qE1`; keep `c` free and impose no retired
wall. In Chamber II branch (A), with `c!=-2e`, use
`deg X,deg Y<=4e-1` to solve the fixed-width box. A nonempty DEVBOX is only a
locus. Adjoin `s` and, pointwise in `C[Z]`, require zero remainders for

```text
eta^2 | (s^2+X),       eta | (3a s^2+Y),
s | (q partial_Z(r)-kappa/2).
```

Set `r=(s^2+X)/(4b eta^2)`, `q=(3a s^2+Y)/(4b^2 eta)`; the last quotient is
`partial_Z(p)`, which must integrate to the declared `p` in characteristic
zero. Check all three in the declared coefficient ring, branching on vanished
leaders and zero. Only then label `SUBSYSTEM_POINT`. Search exceptional `c`,
full `OPEN[A2-K2E]`, the three low-`k` strata, and Chambers I/III separately. This outranks
an undifferentiated `S_6` search because a landed full solution has an explicit
route toward a polynomial pair, whereas a permutation tuple does not.

The state ladder is mandatory:

```text
SUBSYSTEM_POINT
  -> FULL_EO_POINT in the declared quotient R
  -> ADMISSIBLE_PULLBACK (all opens, boundary/general-fibre data,
                          and [K:C(f,g)]=4 checked)
  -> POLYNOMIAL_KELLER_PAIR under the explicit source chart
  -> COUNTEREXAMPLE only after noninvertibility/geometric degree is certified.
```

The explicit chart
`A=x^2, U=x+x^3y, Z=2y+x^2y^2` sends any landed full quotient solution to a
polynomial pair with constant Jacobian. Conditional on the separately checked
rank-four condition, its geometric degree is `2*4=8`; thus a terminating
admissible solution would be counterexample-level rather than a representation
(`xmodel/theta-reopen-explicit-pullback-sol56-20260831.md:412-449`). None of
those later arrows may be inferred from a residual cell.

Outcomes are deliberately symmetric. A unit ideal with positive and negative
controls is a proof lead for that declared branch. Rank deficiency supplies
only a candidate kernel: require augmented-rank compatibility at every level,
all exceptional branches, and both terminal/bottom boundary conditions before
calling it a recurrence. A terminating orbit is checked directly in `R`, then
through the source chart. A formal or nonterminating orbit is only formal;
vanished leaders and missing ring maps return typed `OPEN`.

In parallel but second priority, build the `N=6` census around actual curves.
The `N=4` B3 Keller realization is impossible; its representation/curve data
are regression controls, not a counterexample target. Their value is to force
any proposed group-to-geometry bridge to find the missing obstruction before
that bridge is trusted at `N=6`.

## 7. Software acceleration and decisive experiment

### `CORRECTED-DEVBOX`

Retire the old wall-cell generator: its `EQ2_even` omitted `-2qE1`, so its outputs are
not free-`c` oracles `[AU:734-747]`. Build a source-first deviation
engine instead:

1. regenerate all seven coefficient identities from Even/Odd; emit the
   coefficient field, ordered generators, and source-to-residual images, and
   exact-diff every image;
2. substitute `X,Y`, clear only declared powers of `eta`, and verify the
   corrected `s`-free EQ1–EQ3 identities coefficientwise;
3. in Chamber II, for each fixed integer `e` and `c!=-2e`, use the branch-(A)
   caps `deg X,deg Y<=4e-1`; specialize `sigma` with its parity/domain
   map rather than pretending varying-size systems form one `Q(e,U,j)` ring;
4. run fraction-free linear algebra/resultants, branching on every pivot and
   named exceptional `c`; generate separate Chamber-III and Chamber-I jobs
   using their respective `4e-1` and `2g-1` caps; use two engines with explicit
   Rabinowitsch variables, never a `sat()` wrapper;
5. return `UNIT_IDEAL`, `DEVBOX_LOCUS`, or typed
   `OPEN[branch/ring map]`, plus all specialization images.

Smallest useful test: make old `EQ2_even` fail by exactly `-2qE1`; retain wall-imposed
`(1,3)` EMPTY only as a stale-wall canary; require corrected free-`c` decisions
for `(1,3),(1,5),(1,7)` from scratch; and preserve a synthetic
shifted-generator positive control, labeled as synthetic rather than a
full-system point. Rank loss is not a recurrence
until augmented ranks, exceptional branches, and both boundary conditions pass.
Stop the stale box01 extension and relaunch only corrected jobs.

## 8. Detailed idea cards

### Card 1 — `DEV-PROJECTION / WALL-PIN`

**Dependencies:** reviewed A2 P0, corrected Chamber-II s-free EQ1–EQ3,
branch-(A) deviation bounds, legal opens, and explicit `c` strata. **Cheapest discriminator:** compute
`J_1=I_1 intersect Q[c]` in the declared DEVBOX ring. **Outcomes:** unit ideal
kills that main branch; nonconstant `J_1` gives finitely many `c` candidates;
`J_1=0` sends the returned locus to EQ4/O1s. None is an actual map. **Stop:**
after the generic/exceptional split—no retired wall or monotone-cell work.
**Information gain:** **extreme**, because wall pinning, true freedom, and
emptiness separate in one calculation.

### Card 2 — `GAMMA-B3-REGRESSION -> N6`

The banked curve `Gamma:y^2=x^3(x-1)^2`, parametrized by
`(t^2,t^3(t^2-1))`, has exactly a `(2,3)` cusp and one transverse node
(`xmodel/block-descent-a1-quartic-cycle1-pseudoplane-companion-threat-map-sol56-20260830.md:355-387`): an exact curve-level B3 `k=1` control.
**Dependencies:** complete projection census, native ZvK both orientations,
all `S_4` tuples, and an invariant permitting the forced cusp preimage.
**Cheapest discriminator:** make it reproduce `y_0`, `chi_c(E)=1-4k`, and the
predicted genus/ends on `Gamma`. **Outcomes:** failure rejects the invariant;
agreement licenses an `N=6` formulation but proves no emptiness; group data
alone leave `OPEN`. **Stop:** one audited regression, then generalize or
archive—no more degree-four curves.
**Information gain:** **high mechanism value, no direct closure**.

### Card 3 — `PRIME-7-SCOUT / PROFILE-FIRST-CENSUS`

**Dependencies:** declared `N=7` B2/B3 profiles, faithful encodings, and explicit
degree/transitivity/transposition checks. **Cheapest discriminator:** fix a
meridian up to conjugacy and enumerate with Schreier–Sims. Prime transitivity
gives primitivity; only an actual transposition pins `S_7`. **Outcomes:** zero
is a profile screen absent coverage; survivors rank candidate curve families
and can simplify composite `N=6`; other cycle types retain their real
subgroups. **Stop:** after representation classification, or immediately if
the `N=6` queue is not reduced. **Information gain:** **medium-high at desk
cost**, never an `N=6` substitute.

## 9. Current-lane decisions

| Current lane | Call | Immediate instruction |
|---|---|---|
| `a2-ubound-opus5-20260902` | **harvest P0, review, then continue redesigned** | Reproduce `-2qE1`; reissue the system, then pursue WALL-REDERIVE, full K2E, DEVBOX, and the order ladder. Do not revive RAY-2. |
| `cusp-a-n8-gate-opus5-20260902` | **harvest/review, then stop** | Audit both all-`N` case-(A) chains and their hypotheses. If confirmed, merge the closure and archive the N8 subgroup queue; do not spend more on Alexander minors. |
| `web-sweep-20260902-grok46` | **harvest, then stop** | Record only primary-source results with a direct live client. Queue post-freeze finds for their own gate; do not extend a threat-free broad sweep. |
| box01 A2 cells | **stop stale jobs; reissue corrected only** | `(1,3)/(1,5)/(1,7)` lack corrected free-`c` decisions. Preserve wall logs as canaries, not verdicts; rerun only after corrected preflight. |
| Box03 `869` | **stop at the existing cap** | Harvest a completed result, but do not relaunch or add fallback compute. It is confirmatory inside accepted `N=4` closure and nothing at the frontier depends on it. |

## 10. Campaign-systems check

### UPGRADE — make encoding faithfulness a hard launch gate

The packet calls PREFLIGHT the tracked upgrade `[P:135-145]`; promote it from
queued hygiene to a prerequisite for every `N>=6` realization job. This is not
ceremonial: all six earlier nodal ideals used unfaithful raw/sliced coefficient
conditions, including two apparent two-engine EMPTY results
(`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:189-215`).

**Smallest useful test.** Before solver launch, the generator must (i) emit the
reduced approximate root beside the ideal in the declared coefficient ring and
order; (ii) exact-diff each encoded coefficient condition; (iii) make the old
`(8,6,19)` false-positive canary fail the corrected `86A` cell; (iv) make the
explicit four-node `(9,6,2)` curve pass corrected `96B`; and (v) fail closed on
any ring, map, open-factor, or generator-order mismatch. The corrected ideals
and semantic controls already exist
`[xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511]`.

Use two orthogonal output fields, not the draft's lossy single enum:

```text
artifact = NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT |
           FULL_EO_POINT | CURVE | REPRESENTATION | FULL_COVER |
           POLYNOMIAL_PAIR
attainment = NECESSARY | REALIZED | ADMISSIBLE_PULLBACK |
             ACTUAL_MAP | COUNTEREXAMPLE_CERTIFIED
```

A checked transition table must reject “A2 cell -> B3 kill”, “local torsion ->
global torsion”, and `FULL_EO_POINT -> ACTUAL_MAP` without every Section 6
arrow. Add old-`EQ2_even`/missing-`-2qE1` as a source-fidelity canary. This
small type check protects both analytic and curve workflows.

## 11. Allocation, falsifiers, and close

Allocate frontier effort in this order: (1) independent review and ledger merge
of the A2 P0 and all-`N` case-(A) closure; (2) corrected DEV-PROJECTION and its
exceptional branches; (3) one `Gamma` test of a singular-preimage B3 invariant;
(4) a preflighted B2/B3/reducible `N=6` census, with prime seven only if it
shrinks that queue; (5) joint companion/source-open completion.

The portfolio changes direction on clear falsifiers. If independent source
expansion does not reproduce `-2qE1`, quarantine `[AU]`; if it does, retire all
old wall verdicts. If either case-(A) proof loses a source hypothesis, reopen
only that chain. If the `c`-eliminant vanishes, study the dependency instead of
calling `U` bounded. If a B3 invariant misses `Gamma`'s singular preimage or
Euler ledger, archive it. If curve preflight fails, launch no solver.

Exact finite windows cannot resolve JC2. Next work must expose a cofinal
invariant or advance a survivor through a declared state arrow.

<!-- BODY-END -->
