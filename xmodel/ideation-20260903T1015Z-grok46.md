# Blind ideation — Grok 4.6 — round `20260903T1015Z`

Lane: Grok 4.6 (xAI), equal-standing independent researcher. Blind against
the sealed packet and banked repository reports only. No other
`ideation-20260903T1015Z-*` submission was opened. No in-progress report
of `global-interpolation-sol56-20260902`,
`reducible-branch-review-grok46-20260903`, or
`websweep-20260903T1010Z-grok46` was opened. No canonical ledger edited.
`jc2-lean` not inspected.

## 0. Custody

Packet hash verified, byte-exact against the frozen copy:

```text
99c940d1167acfc7796436457a1d87bdd0a1a1a4c673c7bb2e0d3436e3d086b7  /tmp/jc2-lane.CigVHc/inputs/ideation-20260903T1015Z-packet.md
```

Packet frozen 2026-09-03T10:08:28Z on basis `f5aa1cafd1d848b1b3ccb6b3370972b737f98628`.
Binding live state: `notes.md` 08:33Z handoff and 10:02Z acknowledgment
(the packet's "09:59Z acknowledgment" is that 10:02Z block). Freshest
banked mathematics: AUDIT integration #17 and deltas (a)–(h);
`xmodel/census-rebase-opus5-20260902.md`;
`xmodel/time-function-endgame-review-sol56-20260902.md`;
`xmodel/ideation-20260902T1608Z-synthesis.md`.

Moh 1983 page images **are present on this machine**. File
`refs/moh1983_jram340_configurations_of_roots.pdf`, sha256
`6c8847a8d8374f7d7725c7e2ede2895a2c30034af6a7f28c511a471c41aa6a51`
(matches the packet and the census-rebase source). I read the rendered
pages for Def 5.1 and Prop 5.3 (paper 179–185 = PDF 40–46), Props 6.1–6.4
and the printed (1)–(13) plus p.202 table (paper 198–202 = PDF 59–63),
and Appendix II (paper 207–211 = PDF 68–72), using the OCR
`box/depth-drivers-20260902/moh.txt` only as an index. Claims that
depend on a display formula are from the page image. Claims I could not
read off the image are typed SOURCE-UNVERIFIED.

Firewall: flag ≠ place ≠ cover series; `REPRESENTATIVE` is not
`FULL_ACTUAL_EXIT`; a floor is not attainment. Integers kept apart:
campaign geometric degree `N`; Moh `n = deg_y g = D`; `m = deg_y f`;
`K = gcd(m,n)`; exponents `d,e`; Moh gcd-chain `d_i`; `V_i` of one
major tower. No new exit-price ⇒ no `charge_basis`. Desk CAS: Python 3
stdlib on `box/moh_skeleton_full.py` only, one core, < 1 s, ≪ 4 GB.
No AWS. `FALLACY-v2` in force.

Read: the packet; APPROACHES.md 46-row table plus 2026-08-31 overlays;
AUDIT.md #15–#17(h); PROGRESS.md 2026-09-02; notes.md 08:33Z–10:02Z;
census-rebase; time-function-endgame review (producer only for the
interpolation identity, not as promoted global framing); COORDINATION.md
contract; `FALLACY-v2.md`; `ops/open_collision.py`. Synthesis
`ideation-20260902T1608Z-synthesis.md` as banked. No other
`20260903T1015Z` submission.

## 1. Direct answer: the coordinator is right that N is O(1), and wrong about the all-degree program

The packet asks Q1–Q4 and invites a challenge. The binding sentence in
the 08:33Z handoff is: the all-degree program is realisability of the
(1)–(13)-admissible skeletons with integral pinned `N ≥ 6`. That
sentence is the wrong program. Three corrections, then the replacement.

### 1.1 Where the reading is right

The boundary computes `N` exactly, and the value is `O(1)`. That is
promoted (JAC-FIBRE, FRONTIER-EXACT, D1-PIN, D1-STAR, Phi-lemma,
EXACT-N, PIN-NOT-CEILING, ORTHO-DEFECT). The one-directionality of
integrations #14–#16 was UPPER-ONLY[CONTACT] on a free joint tree; the
Jacobian does not allow the joint tree to be free. The bottom star is a
dessin (BOTTOM-ODE ⇔ STAR-ABC), always realisable at leading order
(STAR-REALISABILITY, zero kills). NO-RESIDUE is a coefficient condition
on `g` alone. The day's enumerator was a superset of Moh's printed
(1)–(13); the true (1)–(13) space is 1,692 V-assignments / 1,189 groups
at `48 ≤ D ≤ 120`, 670 alive at `N ≥ 6`, and **no `D > 100` empties**.
`(1)–(13)` as printed is not Moh's whole program (OPEN[MOH-PROGRAM],
652 excess rows at `n ≤ 100`). All of that stands. I am not re-litigating
a promoted identity.

### 1.2 Where the reading is wrong

**Wrong 1 — Q1 conflates two different Moh acts.** The p.202 table is
the claimed *output of a computer program* ("produces only the following
exceptions which satisfy the necessary numerical restrictions").
Appendix II is a *hand endgame on those six rows*: Prop 6.4 / 6.3
degree-reduction when `u_s = 1`, approximate-root truncation to 7–17
coefficients, then case-by-case polynomial algebra. I read Appendix II
from the page images. It kills the six; it is not what cut 658 rows to
6. Uniformising Appendix II is a different (and secondary) ask from
recovering the program. The rebase lane's phrase "his extra eliminations
are exactly the endgame the campaign needs in uniform form" mixes them.
Call the split **SPLIT-MOH**: PROGRAM (658 → 6, unknown, numerical) ≠
APPENDIX-II (6 → 0, known, case-by-case algebra). SOURCE: Moh p.202
l.1–3 and p.207–211.

**Wrong 2 — skeleton realisability of an infinite census is not an
all-degree proof program.** PIN-NOT-CEILING is the theorem that the
boundary will never give `D ≤ C(N)`. The (1)–(13)+integrality census
does not empty for `D > 100` (14,016 groups at `D ≤ 200`, first `s = 6`
at `n = 192`). A proof that consumes only skeleton arithmetic — `M_i`,
`V_i`, `δ_i`, `q`, integrality of `N` — can close JC2 only if it empties
every large `D`. The measured census says that (1)–(13)+pinned-`N` does
not. Therefore: either Moh's missing PROGRAM-filter empties (then *that*
is the proof, and it is not "(1)–(13) realisability"), or a uniform
theorem must use a datum **not in the skeleton**. Realising or killing
row after row is Moh's n ≤ 100 posture, not an all-degree proof.

**Wrong 3 — the D = 105 trio is not a flagship.** Killing it buys
`D_min ≥ 108` only. `D = 108` still has 76 (1)–(13) groups with
`N ≥ 6`. A uniform theorem buys the conjecture. The trio is a
method-client for a global interpolation engine, and a negative
control for any proposed PROGRAM-filter (it must not kill Moh's six,
and it may or may not kill the trio). It is not worth an Opus
frontier seat as a realisation target.

### 1.3 What the all-degree program should be

`[f,g] = c` along the pencil `g = c_2` is the statement that
`d/dx f(x, τ_i) = c / g_y(x, τ_i)` on every simple branch `τ_i` of a
generic fibre (endgame review §2.1, confirmed). Since `g − c_2` is
monic of degree `n` and `deg_y f = m < n`, `f` is the unique
degree-`< n` Lagrange interpolant of those `n` abelian integrals, and
that interpolant must be a polynomial in `x`. LOCAL-KELLER at one disc
is only necessary (GAP as a global statement; resonance set and
order-2 recurrence refuted). NO-RESIDUE is the coefficient form of
"the Gelfand–Leray form has vanishing residue on every branch."

That is the all-degree object:

> The pair is invertible (after automorphism) if and only if the
> unique degree-`< n` interpolant of `∫ dx/g_y` along the roots of
> `g − c_2` is a polynomial of `y`-degree `m`. Equivalently: a
> polynomial section of the Gauss–Manin sheaf of the pencil, of
> `y`-degree strictly less than the covering degree, exists.

The skeleton is the Newton–Puiseux *type* of that pencil at infinity.
`N = Σ (1 − δ^0)^+` is the polar mass of the same integral, pinned
`O(1)` independently of `D`. Bounded polar mass plus unbounded covering
degree is the tension. It is not resolved by enumerating types.

The program, in order of expected information per hour:

1. **Recover PROGRAM, not Appendix II** (Q1). A desk filter on the 658
   rows, fail-closed on the six printed rows. Trichotomy: empties every
   large `D` (proof path); leaves an infinite family (that family *is*
   the CE list); fails to separate (the missing restriction is not
   skeleton-arithmetic, and the campaign stops hunting it in (1)–(13)
   variants).
2. **A non-skeleton theorem.** Two live mechanisms, not yet tried in
   exact form: (A) the interpolant-polynomiality identity as a
   Picard–Fuchs / D-module statement on the pencil, with a cofinal
   invariant that grows with `D` while `N` stays `O(1)`; (B) the
   Hurwitz dimension of the *admissible cover* of the Moh disc tree
   with a Davenport–Stothers passport at every bottom vertex
   (DESSIN-TOWER, §8). Either can empty uniformly. Neither is "realise
   the next skeleton."
3. **A counterexample-side lift** of one surviving type to high Puiseux
   order, only after (1) has named the type as PROGRAM-surviving, and
   only as a Newton engine, not as a flagship existence proof.

Moh, at n ≤ 100, did (1) by an unrecovered program and (3) by
Appendix II. He did not claim an all-degree theorem. Copying that
shape at D = 105, 108, … is a year of 14,016 groups.

## 2. Q1 — Moh's missing elimination, in uniform form

**Discriminator, as charged:** kill 652 of 658 (1)–(13) rows at
`n ≤ 100`, in particular `M_2 ∈ {5, 10, 40, 60}` at (75, 50), and keep
the six printed rows. Then name the all-degree form.

### 2.1 What I ran (desk)

`box/moh_skeleton_full.py`, Moh's own space (`Kmin = 2`, `n ≤ 100`),
CONTROL 4's 658 rows reproduced. At (75, 50) the nine (1)–(13) rows
are exactly:

```text
M2  V2  q      A1  any10  UNI-weight
 5  20  10/11   3  False  (all-(11) shadow)
10   1  12/17   1  True
40   1   1/3    2  True
40   2   2/3    1  True
40  11   8/9    3  False
55   2   2/5    3  True   ← printed (δ1 = 2/3, ERRATUM 1/3)
55   3   3/5    2  True   ← printed
60   9   9/11   2  False
60  20  10/11   3  False
```

All nine have `u_s = 1` and `V_3 = 4`. NOT-ALL-(11) kills four of nine
and keeps both printed rows plus `M_2 ∈ {10, 40}`; it is necessary for
Prop 5.6's numerical shadow and **not** the missing filter (rebase:
1,189 → 969, empties nothing). Prop 4.6 / 5.3 degree of `q(π)`,
`deg q = V_r (n − M_{r−1}) / d_r` (paper 182, page image): integer and
`> 1` on all 658 rows and on all six printed; kills 0. Lemma 5.3's
`d_s > V_s > d_s/2` is already the `r = s` window of (7) together with
the code's `V_s < d_s`. `u_s = 1` keeps 509 of 658 and kills the printed
(99, 66). Confirmed: every stricter reading of (8)–(13) the rebase
tried kills printed rows; the cheap Prop 4.6 degree test is automatic.

### 2.2 What the page images add (and do not add)

Def 5.1(4) (paper 179): at each major disc the conditions of Prop 4.6
hold with `v = V_{i+1}(d_i / d_{i+1})`. Prop 5.3 *defines* `δ_{r−1}` as
a minimum of actual root distances and *proves* it equals the Def 5.1(3)
formula. The campaign uses the formula as a definition. That is licensed
for a necessary numerical screen and unlicensed as existence of `p(π)`.
Prop 4.6 at `r ≥ 2` further requires a polynomial `q` of degree
`V_r (n − M_{r−1}) / d_r > 1`, squarefree, containing every root of `p`.
That is a Davenport–Stothers-type condition at **every** level, not only
at the bottom (STAR-ABC is `r = 1` only). I did not, in < 10 min, turn
that existence into a numerical predicate that separates the 658. It
remains the cheapest untried probe (Card I).

Appendix II, from the page images: first three classes have `u_s = 1`;
Prop 6.3 divides by `d_s` to the p.207 table `(16, 12)`, `(21, 14)`,
`(15, 10)` (outside the search space). Approximate-root truncation
then Tschirnhausen drops coefficients to 7–17, then a contradiction on
`h` of a minor-disc-forced shape. (99, 66) has two minor-disc
distributions, 10–11 variables. Last sentence: no CE of degree ≤ 100.
A finite computation, not a theorem. SOURCE: paper 207–211.

[A-M.1] and [M.3] are cited in Moh's bibliography (paper 212). I did
not open those papers (not in `refs/` under those filenames). A
semigroup condition on `{M_i}` beyond the λ/μ integrality the rebase
already found automatic is SOURCE-UNVERIFIED as the 658 → 6 filter.

### 2.3 All-degree form, stated

I do not know the PROGRAM-filter. OPEN[MOH-PROGRAM] stands, quantity
unchanged: 652 excess rows, 59 excess `(n, m)` classes, at `n ≤ 100`.
I will not fill it by analogy.

The trichotomy, with the split:

- If PROGRAM is a theorem on the characteristic sequence / the
  existence of `p(π)` / a complete Galois partition of `deg p` that
  empties every large `D`, that theorem **is** the proof and the
  census is a test, not a hunt. All-degree form: the predicate, once
  written, is degree-free (Moh's `s ≤ 5` is not: it fails at `n = 192`).
- If PROGRAM is an arithmetic sieve that leaves an infinite family,
  that family is the CE-side target list. Stop enumerating the
  complement. The interpolant engine of Q4 runs only on the family.
- If PROGRAM is genuinely case-by-case (or is a 1983 encoding we cannot
  recover from the printed text), the campaign needs a **different**
  uniform mechanism — interpolant polynomiality or DESSIN-TOWER — and
  should stop treating "Moh's missing filter" as the all-degree plan.
  Appendix II is already known to be case-by-case; that does not decide
  PROGRAM.

Cheapest test on the census: Card I. Each outcome is in §12.
Interpretation of a fail-to-separate: the missing restriction is not
a predicate of `(n, m, M_*, V_*)`. Stop. That is information.

## 3. Q2 — the D = 105 trio: realise or kill

The three groups, all `m = 70`, `K = 35`, `(d, e) = (2, 3)`, `V_2 = 1`,
`a_1 = 3`, `b_1 = 2`, STAR-SUM count `(e − d)V_2 − 1 = 0`, `A_1 = 2`,
recomputed from `Skel` this session:

```text
M=[28,103] Vs=5  u=25  q=1/2   δ1=7/12   A2=18  UNI N = 6..12 (k even, 12..24)
M=[28,103] Vs=6  u=30  q=9/13  δ1=11/26  A2=13  UNI N = 9 (k=13; 18 is ≥ 17)
M=[40,103] Vs=4  u=28  q=9/17  δ1=19/34  A2=17  UNI N = 9 (k=17)
```

`D_1` holds 3 roots of `g`; `D_2` holds 75 / 90 / 84; `D_3` holds all
105. Order 0 is a Weierstrass star `(p_f, p_g)` of degrees (2, 3),
Mason-extremal of degree 2, rigid after affine automorphisms of `π`.
STAR-REALISABILITY already says this never kills. Local order-1/2 at
one disc is the refuted flagship (wrong resonance set `μ · {0, 1, 2, 3}`,
wrong order-2 recurrence). Do not rerun it.

### 3.1 Cheapest exact discriminator, per group

1. **NO-RESIDUE on `g`**, coefficient-level, promoted, independent of
   `f`. One linear condition per Galois orbit of branches. First cheap
   screen; a kill here is a theorem about `g` of this Newton type, not
   a pair.
2. **Global order-1 linear algebra** (Q4's smallest engine), not local.
   Unknowns: the first Puiseux jet of each Galois orbit of discs (order
   `A_2 ∈ {18, 13, 17}` already collapses the 105 roots to a handful of
   orbits at the `D_2` layer, plus the bottom star, plus the complementary
   minor-disc jets counted by NONPROPER-COUNT `= e(K − Σ V_2)`).
   Conditions: the `n − m = 35` degree-killing relations on the
   interpolant, plus NO-RESIDUE, plus `g(x, τ_i) = c_2` at this order.
   I expect order 0 to be 0-dimensional after Aut (cannot kill). I
   expect the first *possible* counting kill at **global order 1**, and
   I would not bet on it: unknowns and conditions are both `O(K) = 35`.
   Order 2 is the first order at which a counting kill is likely if
   order 1 is square. Typed, not attained: OPEN[T105-COUNT] below.
3. Construction, if one believes realisability: `(2, 3)` with one
   bottom-major disc of 3 `g`-roots, glued to a minor block of
   NONPROPER-COUNT `e(K − k V_2)` branches (69 / 66 / 54 at the UNI
   `k = 12, 13, 17` that realise `N = 6, 9, 9`). The dessin is the
   DS pair of degrees `(2, 3)` at `V_2 = 1` (Mason `deg C = 2`;
   STAR-REALISABILITY; Weierstrass shape). Gluing is DESSIN-TOWER on
   a three-`P^1` chain. I do not believe this is realisable; I also
   do not believe killing it is the proof.

### 3.2 What a kill buys, and the seat

A kill of the trio buys `D_min ≥ 108` only (`D = 108` still has 76
groups). A uniform theorem buys JC2. The trio is **not** worth a
flagship seat. It is a method-client: develop the Q4 engine on Moh's
Prop 6.3 reductions `(16, 12)` and `(15, 10)` (Appendix II already
kills them; vacuous-pass control) and then point it at the trio.
Queued GLOBAL-COUPLING should be that client. If the engine kills
the trio by counting at order 1 uniformly in `(2, 3, V_2 = 1)`,
promote the counting as a lemma and run it on every such row.

## 4. Q3 — skeleton realisability is the wrong all-degree frame

Fresh eyes, as asked. The boundary computes `N`; nothing on the
boundary bounds `D`. That is PIN-NOT-CEILING, promoted. The remaining
object is not "an infinite census to realise." It is a polynomiality
statement for one abelian integral on an unbounded-degree pencil whose
polar mass is bounded.

### 4.1 Verdict on (a)–(f)

**(a) RAISE — this is the all-degree frame.** `f = Lagrange(∫ dx/g_y)`
is one meromorphic connection on the pencil `g = c_2` (Gauss–Manin of
the Gelfand–Leray form). LOCAL-KELLER is that connection in the bottom
chart; globally one needs a polynomial section of degree `m`. Cofinal
invariant to check, not assume: irregularity (Poincaré rank) at the
places at infinity, as a function of the g-tree. Polar mass `N` is
already `O(1)`; irregularity can still grow with Newton slopes. If it
is `≥ c(n − m)` while a degree-`m` polynomial section forces
`≤ C(N)`, that is the proof. This is not the 2026-08-23 inverse-Jacobian
connection (p-curvature identically 0; étale map, not the pencil). Do
not fill by "genus grows": MF-EXACT already spent Riemann–Hurwitz to
pin `N`. Bound the irregularity or return typed OPEN.

**(b) RAISE — DESSIN-TOWER, the new avenue.** STAR-ABC is one leaf.
The k bottom discs are a Galois orbit of Davenport–Stothers pairs glued
through the Puiseux tower: an admissible cover of the Moh disc graph
with a prescribed leaf type. Expected dimension is a function of
`(d, e, V_*, s, k)`, desk-computable. Negative: unrealisable.
Zero: rigid (isolated CE if any). Positive and growing in `D`: CE-side
moduli. Not queued DISC-COUPLING (local, dead target, refuted
resonances). Not row 25's 169-passport CSP. Bound: the integer
dimension per skeleton.

**(c) LOWER as a proof; keep as a machine.** Interpolant mod `p` is a
Q4 backend. It is not an all-degree bound (inverse-Jacobian p-curvature
already 0; Cartier-of-NO-RESIDUE is a reformulation). Pencil p-curvature
is (a) in char p. Do not resurrect row 20 as a JC2 proof.

**(d) RAISE as a Q1 candidate, not as the all-degree proof.** Appendix II
uses approximate roots. Rebase found printed AM `λ_j, μ_j` automatic, so
naive semigroup inequalities are not PROGRAM. Bresinsky / approximate-
root Wronskian untried as a numerical filter. SOURCE-UNVERIFIED as the
658 → 6 cut: [A-M.1] and [M.3] were not opened. Card I is the
AM-adjacent test that *is* on the pages I read (Prop 4.6 partition).

**(e) RAISE as the strongest CE attack, after PROGRAM names the type.**
A Newton lift to high order at one PROGRAM-surviving skeleton is the
first positive signal above `D = 100`. A box01 cluster is not a map.
Do not lift a row PROGRAM would kill, or the dead
`(105, 70, (−70, −63, 103), V = (1, 4, 1))` skeleton.

**(f) RAISE, and answer: (1)–(13)+integrality never empties.** Measured
through `D ≤ 200` (14,016 groups; `s ≤ 5` already fails). Quantity:
number of (1)–(13)+`N ∈ Z, N ≥ 6` groups as a function of `D`. A uniform
theorem must use a non-skeleton datum unless PROGRAM empties. If
interpolation-as-counting also admits infinitely many types, the proof
is (a) or (b), not a census.

### 4.2 The coordinator's frame, one line

Q3 asks whether skeleton realisability is the right all-degree frame.
No. It is Moh's n ≤ 100 frame, correctly used by Moh as a finite
search, incorrectly promoted by the handoff to an infinite hunt.
The right frame is interpolant polynomiality (a), with DESSIN-TOWER (b)
as the combinatorial shadow of the same identity, and PROGRAM (Q1) as
a possible arithmetic collapse that would make the hunt finite after
all.

## 5. Q4 — software: the all-degree filter that is a machine

The (1)–(13) census runs in seconds to `D = 200`. What is missing is
not a faster enumerator. It is an order-by-order exact linear-algebra
engine over Puiseux data with a **counting bound typed per skeleton**.

**Smallest version worth building this week** (desk, then AWS only if
the matrix at D = 105 order 1 exceeds the 10 min / 4 GB gate):

- **Input.** One skeleton `(n, m, M_*, V_*)` plus a branch-packet
  `k` with `N = k V_2 q ∈ Z, N ≥ 6`. Preflight must return 0.
- **Order 0.** BOTTOM-ODE / STAR-ABC. Already exists
  (`box/tfe-drivers-20260902/bottomode.py`). Must report rank/coker,
  not `pass`.
- **Order 1, GLOBAL.** Unknowns: first jets of the Galois orbits of
  discs (major and the NONPROPER-COUNT minor block). Conditions:
  `g(x, τ_i) = c_2` at this order; NO-RESIDUE; the `n − m` leading
  `y`-coefficients of the Lagrange interpolant vanish. Field `Q` after
  rigidifying the bottom star. Output the 4-tuple
  `(n_unknowns, n_conditions, rank, coker)` and refuse to print
  "survives" if the matrix is `0 × 0` or if the degree-killing block
  was not assembled.
- **Gates.** (G1) planted automorphism `(y, x + y^k)` for `k = 5, 7`
  survives to every implemented order (positive control). (G2) a
  (1)–(13)-illegal skeleton, or Moh's Prop 6.3 reductions `(16, 12)`
  and `(15, 10)` which Appendix II already kills, die at order 0 or 1
  (negative control that catches a vacuous pass). (G3) dropping the
  degree-killing block must strictly increase coker or free variables;
  if the truncated system still "kills," the kill is a bug. (G4)
  `python -O` replay with the assert-scan of COORDINATION.md, or label
  ordinary-only.
- **Stop.** One of: a typed counting kill at order 1 on the D = 105
  trio (promote as a `(2, 3, V_2 = 1)` lemma); a free jet that is only
  `REPRESENTATIVE` (not a polynomial, not a CE); wall-clock 10 min at
  desk / the AWS cap if moved; or G1/G2/G3 fail (engine wrong, stop).

Without the typed 4-tuple this is another local-Keller. The Sol lane
is writing the conditions; this engine is the machine. First
mathematical seat remains Card I (§13): if PROGRAM empties, the
client list is empty.

## 6. Disposition vectors

### 6.1 APPROACHES.md 46-row table — changes only

The 2026-08-31 overlays (rank-four irreducibility, one-cusp horn,
colouring ladder) are stale relative to #17: `N ≤ 5` is closed
unconditionally, the exact machinery is H2-free, reducibility lives in
the zero block. I am not re-scoring those overlays; the 46-row inventory
is the contract object.

| # | change | reason |
|--:|---|---|
| 1 | lower | GGV corner farm is not the all-degree object. `N` is pinned `O(1)`; no next-pair cutoff is a proof. Retain as a certificate store. |
| 2 | raise, retype | The Moh/Sigray tree *is* the object. The stuck "no cofinal td ceiling" is now a theorem (PIN-NOT-CEILING). The live remainder is interpolant polynomiality and DESSIN-TOWER, not "exclude td = 6, 7, 8." |
| 3 | raise, retype | BOTTOM-ODE is the r = 1 strip ODE, promoted. Residual is the global interpolant, not a depth-three Ore extension of the old strip. |
| 4 | lower | Formal-germ D-series is not the all-degree program. A Newton lift of a PROGRAM-surviving skeleton is a CE-side *use* of the instrument, not a D25 comprehensive. |
| 6 | raise | AM was "tried on the wrong object." The object is now the characteristic data of `(f, g)` at infinity. Appendix II *is* approximate roots. PROGRAM may live here. Still not AM-on-fibres. |
| 7 | raise | `N = deg A(F)` is an exact skeleton function, `O(1)`. Residual: the non-proper block (NONPROPER-COUNT), which is where reducibility lives. Constructing `A(F)` is still the compactification problem; the degree is no longer unpinned. |
| 16 | raise | Q3(a): D-module / holonomic index of the pencil `g = c_2`, not a slogan against a Keller endomorphism. Fable-unique row, now with a named invariant (irregularity of `∫ dx/g_y`). |
| 20 | reopen, retyped | Inverse-Jacobian p-curvature is already zero (Sol, 2026-08-23; synthesis correction). The *pencil's* Picard–Fuchs operator mod `p`, and the interpolant over `F_p`, are a different object. File as Q4 backend and as (a) in char p, not as Tsuchimoto ⇒ JC2. |
| 25 | raise | STAR-ABC is a dessin. DESSIN-TOWER is the missing global passport. Row 25's 169-passport CSP ignored the second coordinate; this does not. |
| 45 | raise | Differential Galois of the inverse PDE was tautological. Differential Galois of the *pencil* Picard–Fuchs operator is Q3(a). Kovacic is the wrong algorithm; the operator is the right object. |

Unchanged: 5 (cusp leading forms still block shears); 8–15, 17–19,
21–24, 26–44, 46. Row 26 remains the non-proper residual, not pinned
`N`. Row 33 stays COSTUME. Row 36 is not the CE search; (e) is.

### 6.2 Q3 (a)–(f) and queued fronts

| item | change | reason |
|---|---|---|
| Q3(a) | raise | §4.1; the all-degree frame. |
| Q3(b) | raise | DESSIN-TOWER; §8. |
| Q3(c) | lower as proof; keep as machine | §4.1. |
| Q3(d) | raise as Q1 candidate | Appendix II uses it; naive AM quantities automatic; Prop 4.6 existence untried as a predicate. |
| Q3(e) | raise as CE attack | After PROGRAM names the type. |
| Q3(f) | raise, answered yes for (1)–(13)+integrality | 14,016 groups at `D ≤ 200`; proof must use a non-skeleton datum unless PROGRAM empties. |
| global-interpolation-sol56 | continue | The exact global conditions are the all-degree object. Relaunch already done at 10:02Z. Do not point it at a flagship realisation of the trio. |
| reducible-branch-review | continue | Delta (g) is H2-free and load-bearing; different-model gate is due. Unreviewed as of 10:02Z. |
| websweep-20260903T1010Z | continue | 24 h sweep overdue since 20260831T0454Z. |
| census-rebase second-reader | continue, **unblock** | Hold "BLOCKED on Moh PDF" is stale: the file is on this clone at the census-rebase hash. Charge pp.200–202 + Appendix II for OPEN[MOH-PROGRAM], not a re-transcription of (8)–(13) (already corroborated). |
| branch-orbits v2 | continue, on (1)–(13) | Is (UNI) a theorem; orbit-aware knapsack. After PROGRAM if Card I returns fast. |
| DISC-COUPLING / GLOBAL-COUPLING | **redesign** | Dead target, refuted resonances. Relaunch as a DESSIN-TOWER dimension + global-order-1 client on a PROGRAM-surviving type, not as "realise D = 105." |
| Box01 cluster census | stop as a JC2 instrument | Auxiliary; 425 cells missing; a numerical cluster is not a map. Harvest is done fail-closed. Billing hold is a systems item, not a math lane. |
| Box03 | stop | Already stopped. |

## 7. Reranked bottlenecks

**Proof, in order.** (1) PROGRAM as a degree-free predicate, or a typed
fail-to-separate; quantity 652 rows. (2) A cofinal non-skeleton invariant
(pencil irregularity, or DESSIN-TOWER dimension), bounded in `N` or shown
negative. (3) Global interpolation as a counting lemma, once PROGRAM has
not emptied. (4) Reducible residual in the non-proper block (delta (g),
under review) — not pinned `N`.

**Disproof, in order.** (1) Newton lift of one PROGRAM-surviving skeleton
(Q3(e)); a germ is `REPRESENTATIVE`, not a map. (2) A DESSIN-TOWER
component of dimension `≥ 0` that lifts to a polynomial pair. (3) Do not
start from box01 clusters, the dead D = 105 STAR row, or Moh's six
(Appendix II already kills them; they are method controls).

PIN-NOT-CEILING demotes every "bound `D` from the boundary" bottleneck.
`N ≤ 5` is closed. The 08-31 rank-four / one-cusp bottlenecks are not live.

## 8. New avenue and new cross-connection

**New avenue: DESSIN-TOWER.** Admissible covers of the Moh major-disc
graph, STAR-ABC type at each bottom vertex, Prop 4.6 type at each
internal vertex. Expected dimension is an integer in
`(d, e, V_*, s, k)`, desk-computable. Not STAR-ABC (one leaf), not
DISC-COUPLING (local, wrong resonances), not row 25's uncoupled
passports. Appendix II, as geometry, is a hand computation of this
space at four types after Prop 6.3. All-degree form: the dimension
formula. Negative at every `N ≥ 6` type is a proof; a positive-dimension
family is the CE list.

**New cross-connection.** STAR-ABC (row 25) ⊗ Moh tree (row 2) ⊗
approximate roots (row 6) ⊗ pencil Gauss–Manin (rows 16, 45) ⊗
ORTHO-DEFECT. The interpolant is a polynomial section of the
Gauss–Manin sheaf of the dessin-tower family. ORTHO-DEFECT
`2de N = Σ (e m_ν − d m'_ν)^2` with `N = O(1)` is the same pairing at
infinitely-near points (almost-proportional multiplicities). That is
the infinitesimal form of Galois-conjugate Belyi maps through the
tower; negative DESSIN-TOWER dimension is the global form. Not claimed
as a theorem: it is the theorem-interface pass, unrun in exact form.

## 9. Strongest proof attack, strongest counterexample attack

**Proof.** Write the DESSIN-TOWER expected-dimension formula and evaluate
it on (i) Moh's six (must be `≥ 0` before Appendix II's further
truncation, or the formula is wrong), (ii) the 652 excess rows (the
PROGRAM discriminator: if dimension `< 0` on the 652 and `≥ 0` on the
six, PROGRAM *was* this formula, and the all-degree form is in hand),
(iii) the D = 105 trio and a slice of `D ≤ 200`. If (ii) separates,
stop hunting PROGRAM in (8)–(13) variants and spend the seat on proving
the dimension formula. If (ii) does not separate but (iii) is uniformly
negative for `N ≥ 6`, that is the proof and PROGRAM is irrelevant. If
the formula is nonnegative infinitely often, the proof is Q3(a)
irregularity, not combinatorics.

**Counterexample.** After PROGRAM (or after a fail-to-separate of
PROGRAM, on a (1)–(13)+`N ≥ 6` type with DESSIN-TOWER dimension `≥ 0`),
Newton-lift the interpolant system to high Puiseux order at one type —
the smallest is a D = 105 trio member or a D = 108 row with small `a_1`.
A numerical solution to large order is the first positive signal above
`D = 100`. It is `REPRESENTATIVE`, not `FULL_ACTUAL`. Algebraization is
Avenue 4, charged only after the germ exists. Do not advertise a box01
cluster as this signal.

## 10. Software acceleration / decisive experiment

Card I's census predicate is the decisive experiment of this round:
stdlib, fail-closed on CONTROL 0–7 and the six printed rows,
discriminator the (75, 50) `M_2 ∈ {5, 10, 40, 60}` rows. CONTROL 4
was 0.25 s here. The Q4 engine is the week's instrument, not the first
experiment. Vacuous-pass control: a predicate that kills a printed row
is rejected (`A_j = den(δ_j)` already taught this). Positive: the nine
(75, 50) rows of §2.1 must print before any kill-count.

## 11. Campaign-systems check

**`UPGRADE` — Moh-PDF presence gate, and un-stale the 10:02Z hold.**

Rotate: state freshness and claim/review propagation. Independent of
the Q4 engine.

The 10:02Z hold blocks the census-rebase second-reader and branch-orbits
v2 on a missing Moh PDF. On this box the file is present at sha256
`6c8847a8…` (census-rebase source). The hold is stale.

Smallest test (already PASS this session):

```text
test -f refs/moh1983_jram340_configurations_of_roots.pdf \
  && sha256sum refs/moh1983_jram340_configurations_of_roots.pdf
```

Expect `6c8847a8d8374f7d7725c7e2ede2895a2c30034af6a7f28c511a471c41aa6a51`.
Put that check in the second-reader charged inputs and **launch**. A
clause in `box/preflight.py` failing closed on a missing/mismatched Moh
PDF stops the next laptop→cloud drift.

Not the upgrade: do not git-add `refs/`; do not re-harden `N ≤ 5`; do
not invent AWS credentials (fleet hold stays human). Box01 idle-billing
is that gate. `lane.sh` Linux port is already `NO_CHANGE`
(`launcher-linux-smoke-fg-grok46-20260903` PASS); do not retest
bubblewrap.

## 12. Idea cards (three)

Not a D = 105 realisation flagship, not a boundary ceiling, not
Tsuchimoto-as-proof, not another (8)–(13) transcription, not local
Keller with the refuted resonance set.

### Card I — PROGRAM discriminator (Prop 4.6 partition + minor complement)

**Dependencies.** Census-rebase §1 and §5 (printed (8)–(13), CONTROL 4's
658 rows, the six printed rows); Def 5.1(4) and Prop 5.3 / 4.6 from the
page images (paper 179–183); Prop 6.1 minor-disc multiplicity bound
(paper 190–193); `box/moh_skeleton_full.py` CONTROLs 0–7. Do not consume
in-progress global-interpolation. Do not consume Appendix II as if it
were PROGRAM.

**Discriminator.** Extend `moh_skeleton_full.py` with one predicate
`prop46_partition(S)`: at each level `j = s−1, …, 2`, with
`Q = V_{j+1} d_j / d_{j+1}` and `A_j, △_j, □_j` as in (9), decide
whether there exists a partition of `Q` into parts, one of which is
`V_j`, the others either major-window parts satisfying (10)∨(11) or
minor parts `≤ d_j / (n − M_j)` (Prop 6.1), with a≠0 parts in Galois
orbits of size `A_j`. Fail-closed: refuse to print kill-counts unless
CONTROL 0–7 still pass and all six printed rows satisfy the predicate.
Then run n ≤ 100 and report (kept printed, killed excess, (75, 50)
`M_2` set remaining). Desk, stdlib, < 10 s.

**Outcomes.** Separates 658 → 6, keeps the six: PROGRAM was this
predicate (drop `s ≤ 5` above `D = 120`); run `D ≤ 200`; empty ⇒ proof
path. Keeps the six, kills (75, 50) excess but not 652: a proper
strengthening, not PROGRAM; iterate, do not claim closure. Kills a
printed row: encoding wrong; stop. Kills 0: PROGRAM is not a partition
of `deg p`; stop (8)–(13) descendants; Card II next.

**Stop.** One of those four, or 10 min. No Groebner, no D = 105 job.

**Gain.** Q1 decided, or a typed fail-to-separate.

### Card II — DESSIN-TOWER expected dimension

**Dependencies.** STAR-ABC; Def 5.1 dual graph; Prop 4.6 internal
passports; Card I if it separated, else (1)–(13)+`N ≥ 6`. Genus-0
admissible-cover dimension (`r − 3` per component, minus Aut, plus
nodes). Not the 169-passport CSP; not DISC-COUPLING's order-2 driver.

**Discriminator.** Integer `expdim(S, k)`. Evaluate on the six (must
be `≥ 0` or the formula is wrong), the 652 excess, the D = 105 trio,
a `D ≤ 120` slice. Desk python.

**Outcomes.** Wrong on the six: stop, OPEN[DT-DIM]. Separates 658 → 6:
PROGRAM is this combinatorics; prove the formula; `expdim < 0` at all
`N ≥ 6` is the proof. Uniformly negative at `N ≥ 6` even if it misses
p.202: the proof. Nonnegative infinitely often: combinatorics cannot
finish; proof is Q3(a); those types are the CE list.

**Stop.** Formula fails the six, or a uniform sign through `D ≤ 120`.
No realisation job.

**Gain.** PROGRAM, a proof, a named CE list, or a typed no.

### Card III — global order-1 engine, method-control first

**Dependencies.** Endgame review's repair of LOCAL-KELLER (necessary,
not global); NO-RESIDUE promoted; STAR-ABC; Q4 gates G1–G4 of §5;
`box/preflight.py`. Client list: Prop 6.3 reductions `(16, 12)` and
`(15, 10)` first (Appendix II kills them), then one D = 105 trio member
only if Card I has not already emptied the type.

**Discriminator.** The 4-tuple `(n_unknowns, n_conditions, rank, coker)`
at global order 1. Gates G1–G4. Desk if the Galois-reduced matrix is
< 10^3; AWS only if not, with a registered tag.

**Outcomes.** G1/G2/G3 fail: engine wrong, stop. `(16, 12)` survives
order 1: engine weaker than Appendix II, not yet a bug, go to order 2
or stop as a week-one instrument. D = 105 dies by counting at order 1
with G1–G4 green: promote as a `(2, 3, V_2 = 1)` lemma, run on every
such row, do not call it a proof of JC2. Free jet: `REPRESENTATIVE`
only; do not claim a CE; Card II's dimension should have been `≥ 0`.

**Stop.** A typed 4-tuple on `(16, 12)` and one D = 105 group, or
G-failure, or the 10 min desk cap. No local-Keller rerun.

**Gain.** The instrument Q4 asked for, with a vacuous-pass control,
pointed at the right finite set.

## 13. Continue / redesign / stop, and the first lane

| lane | action |
|---|---|
| `global-interpolation-sol56-20260902` | **continue** (already relaunched). Exact global conditions. Do not retarget as a D = 105 realisation flagship. Feed Card III. |
| `reducible-branch-review-grok46-20260903` | **continue**. Delta (g) different-model gate. |
| `websweep-20260903T1010Z-grok46` | **continue**. Overdue. |
| census-rebase second-reader (queued) | **continue, unblock**. PDF is here. Charge OPEN[MOH-PROGRAM] + Appendix II, not (8)–(13) again. |
| branch-orbits v2 (queued) | **continue** on (1)–(13), after Card I if it returns in an hour. |
| DISC-COUPLING / GLOBAL-COUPLING (queued) | **redesign** as Card II + Card III client, not a trio flagship. |
| Box01 | **stop** as mathematics; harvest is fail-closed. Billing is a human gate. |
| Box03 | **stop** (already). |

**The single first lane I would launch with a frontier seat is Card I
(PROGRAM discriminator).** Desk-scale; discriminator is the charged 652
rows; every outcome reroutes the all-degree program before a seat is
spent on D = 105. Does not wait on Sol prose, AWS, or a Hurwitz formula.
Card II is the second seat if Card I fails to separate. Card III is the
week's software. The trio is a client, not a flagship.

## OPENS RAISED

OPEN[MOH-PROGRAM]  bounded quantity: 652 excess (1)–(13) rows and 59
   excess `(n, m)` classes at `n ≤ 100` relative to Moh's p.202 table
   of 6 rows in 4 classes. Carried from census-rebase; not closed here.
   This report splits it from Appendix II (6 → 0) and does not fill
   the 658 → 6 gap by cap. Decide whether a degree-free predicate on
   `(n, m, M_*, V_*)` realises the cut, or that no such predicate
   exists in (8)–(13) descendants.

OPEN[DT-DIM]  bounded quantity: the integer expected dimension of the
   admissible-cover space of a Moh disc-tree with STAR-ABC passports at
   the bottom vertices, as a function of `(d, e, V_*, s, k)`, at every
   (1)–(13)+`N ≥ 6` skeleton. Negative / zero / positive is the
   proof / rigid-CE / family trichotomy of Card II. Must be `≥ 0` on
   Moh's six or the formula is wrong.

OPEN[T105-COUNT]  bounded quantity: the 4-tuple
   `(n_unknowns, n_conditions, rank, coker)` of the *global*
   interpolant linear system at order 1 (and at order 2 if order 1 is
   square) on each of the three D = 105 groups, UNI packets
   `k ∈ {12, …, 24}`, `{13}`, `{17}` respectively. A counting kill is
   `coker = 0` and `rank = n_unknowns` with G1–G4 green; a free jet is
   `REPRESENTATIVE` only. Does not bound `D`.

OPEN[PF-IRREG]  bounded quantity: the irregularity (Poincaré rank) of
   the Picard–Fuchs / Gauss–Manin connection of the Gelfand–Leray form
   `dx / g_y` on the pencil `g = c_2`, as a function of the g-tree, at
   a degree-minimal Jacobian pair. To be bounded above in terms of `N`
   (or shown `≥ c(n − m)` for large `D`). Not the inverse-Jacobian
   connection (already p-curvature 0). Not MF-EXACT's Euler
   characteristic (already consumed to pin `N`).

OPEN[PROP-5.6-SHADOW]  bounded quantity: 220 groups (1,189 → 969) as
   measured by census-rebase; whether "all levels take (11)" is
   equivalent to `σ_1 = π t^{δ_1}` including the `r = 2` step that
   (12)/(13) does not expose. Carried; not the 658 → 6 filter
   (this session: kills 4 of 9 at (75, 50), keeps both printed).

## Desk CAS and Moh pages

This session ran `box/moh_skeleton_full.py` as a library (no
`--` census dump to D = 200). Wall-clock 0.25 s for n ≤ 100 (1)–(13);
< 1 s total. Peak memory: python stdlib. Reproduced: 658 rows / 63
classes; nine (75, 50) rows as in §2.1; D = 105 trio `(q, A_1, A_2,
root-counts, UNI N)` as in §3; Prop 4.6 `deg q > 1` kills 0 of 658;
`V_s ≥ d_s` kills 0; `u_s = 1` keeps 509 including 5 of 6 printed.
Moh PDF sha256 `6c8847a8…` verified present; pages 179–185, 198–202,
207–211 read as images. No `charge_basis`. No ledger edit.

## Carried opens (not raised here)

STAR-ABC, NO-RESIDUE, TF-0, TF-DESSIN existence beyond (2, 3),
DELTA75 (closed as erratum), UPPER-ONLY[CONTACT],
OPEN[COMPANION-R0-REALISATION] (non-proper block). STAR-REALISABILITY
is closed (zero kills). LOCAL-KELLER as a global statement stays GAP.
The dead D = 105 STAR row is not a target.

## COLLISIONS

`python3 ops/open_collision.py` on this report: `status: CANDIDATES`
(expected; hits are review candidates, never closures). Banked hits
only, recorded here. Same-round `ideation-20260903T1015Z-*` files and
in-progress lane reports that the scanner also listed were **not
opened** and are not used.

- `OPEN[MOH-PROGRAM]`: AUDIT.md:15504 (bounded 652 excess),
  notes.md:20667, census-rebase §5 (652 rows / 59 classes). Carried,
  not closed; this report splits PROGRAM from Appendix II.
- `OPEN[PROP-5.6-SHADOW]`: census-rebase (220 groups). Carried; this
  session's (75, 50) count (kills 4 of 9, keeps both printed) agrees
  it is not the 658 → 6 filter.
- `OPEN[DT-DIM]`, `OPEN[T105-COUNT]`, `OPEN[PF-IRREG]`: NONE in the
  banked corpus. New.

<!-- BODY-END -->
