# Whole-campaign ideation synthesis — `20260828T1149Z`

Date closed: 2026-08-28 12:24Z UTC.

Status: **CLOSED — all four blind submissions received and hash-verified.**
This document adjudicates strategy; it does not claim a proof or
counterexample to JC2.  New mathematical claims found by one lane remain
provisional until the ordinary different-model promotion gate is met.

## 1. Custody and independence

The immutable packet is
`xmodel/ideation-20260828T1149Z-packet.md`, SHA-256
`31094db14e41506e121b037bd001e077d6bd80d6b4f38ae49a071116e52fef94`.
The four submissions were produced before cross-reading:

| lane | artifact | SHA-256 |
|---|---|---|
| Sol Ultra | `xmodel/ideation-20260828T1149Z-sol-ultra.md` | `3a90101d4da5cdfd13fbe97d749711f758c81d2222aed71c6ee7ff34a024905e` |
| Fable 5 | `xmodel/ideation-20260828T1149Z-fable5.md` | `0cc8db02a74cad72b516013fc5324db7624d4e3061d6808316ba2850d352a4fa` |
| Grok46 | `xmodel/ideation-20260828T1149Z-grok46.md` | `44994692e56f2ab93bae8d9c3d3cf2bad4e01eb5b4bc91a7b2a1d74a15370d` |
| Opus 5 | `xmodel/ideation-20260828T1149Z-opus5.md` | `882485d623e9fc21ba0d52c1eea1f86c2902ff1e4e64d5543f86398a0afbac12` |

Opus disclosed that peer filenames appeared after its mathematics was
drafted, but it did not read their contents.  Its more important independence
caveat is mathematical: its new corner theorem consumes Facts A/B from an
earlier Opus review, so the theorem still needs a different-model hostile
review.

Post-packet operational events are an overlay, not blind input:

- the four short arbitrary-`Q` AWS solver variants all capped with strict
  `NO_VERDICT`; all four large r6 hosts were stopped with zero swap;
- the corrected row-dual/denominator-strata design was frozen but not run
  before the round closed;
- Proposition 5.1's forced-puncture producer and checker completed
  provisionally; Opus hostile review remains in flight;
- unit-`S` relative orders four and five completed provisionally and replayed
  exactly; Fable review remains in flight;
- the two Matysiak papers were audited by Fable, refuted at their first
  load-bearing implications, and archived as negative intelligence.

## 2. Executive synthesis

The proof-wide frontier moved decisively toward the pure intrinsic/Sigray
route.  The exact-pair L3/L4/L5 theorem supplies genuine normalized boundary
places and exact orbit/order conversion; the complete Proposition 4.2 repair
supplies a total positive-tree tower including the unique `(1,0,c)` shift.
The next wall is no longer construction of an individual place.  It is the
universal, provenance-preserving passage from all such places and ancestors
to a complete finite book, followed by `RPMC(C)`, type control and a cofinal
complexity ceiling.

There is, however, cheap source debt immediately in front of that wall.  The
four reports differ on its ordering, but after checking actual dependencies
the campaign should execute:

1. bank the already-sketched Proposition 5.4 `q`-half repair;
2. sweep every consumer of Statement 6.2's missing
   `H in V_a cap T_a^+` hypothesis;
3. audit Proposition 6.8 and the exact §§7--9 statements actually consumed
   by promoted books, especially Proposition 7.3, Proposition 7.5,
   Notation 7.1 and Proposition 8.4;
4. carry an explicit auxiliary-`h` `kappa` rider through every use of
   Statement 3.9.

These are source-trust tasks, not substitutes for landing.  Once they are
banked, landing is the first genuinely open architectural implication.

On the finite GGV side, the round is also decisive about method.  Stop
equivalent Groebner variants and stop increasing isolated face order.  The
next exact object is the rank/Fitting stratification of the frozen
`106 x 105` receiver matrix, with the **quadratic** endpoint pulled back to
the kernel on every stratum.  In parallel, preserve K00/full-`P6` as the
strongest independent bounded falsifier.  More local exclusions remain
valuable, but no GGV result currently discharges a pure-tree hypothesis.

No report found a new global degree bound.  Riemann--Hurwitz, Euler/genus,
terminal orbit data and the pole-mass identity recombine to known identities
unless a genuinely new conductor/capacity inequality is supplied.

## 3. Four-way consensus and adjudication

### 3.1 Strong consensus

All four lanes agree on the following decisions, after translating their
different labels:

- **Raise the pure Sigray/exact-pair backbone.**  Its first large open wall is
  universal landing/coverage, not the now-promoted local place constructor.
- **Repair source trust before leaning harder on later books.**  Proposition
  5.4, Statement 6.2 and unaudited later source statements have much higher
  information per hour than another large elimination.
- **No global-bound hit.**  Exact terminal/orbit data improve typing and
  enumeration, but do not themselves bound genus, conductor, `td`, type or
  the number of entries.
- **Redesign the arbitrary-`Q` face around exact linear algebra.**  Use
  cofactors, row duals, kernels and Fitting/denominator strata, not another
  equivalent ideal presentation.
- **Stop blind face-order marching.**  Orders four/five suggest a recurrence
  or row-ideal redundancy theorem; the next result should be an all-order
  invariance result or a complete raw-window/endpoint test.
- **Keep K00 protected.**  A source-open, rank-exact full-`P6` point plus one
  prolongation remains the best bounded counterexample seed; a selected
  chart or capped solve is not evidence.
- **Build a typed exact-pair/source compiler.**  It should emit proof objects,
  retain ancestor and unused-branch provenance, use cross-multiplied
  zero-safe relations, and serve as a source-fuzz regression harness.
- **Do not transfer GGV conclusions into the pure route.**  `G2-PSC` and
  `G2-BD` remain distinct scoped obligations; fixed-face endpoint theorems
  have no fibre/place/`kappa` map into Sigray data.

### 3.2 Necessary corrections to individual reports

1. **The endpoint is quadratic, not linear.**  Sol's proposed identity
   `w(theta)^T M(theta)=E` is ill-typed.  Here
   `E=x14*x72+x1*x97`; after computing `N=ker M`, one must decide the
   quadratic `E(Nt)`.  A nonzero value over `C` can be scaled to `E=1`; an
   identically zero pullback makes that stratum endpoint-dead.
2. **A normalized left-kernel vector is only a pivot-chart object.**  Opus's
   suggestion to primitive-clear a rational vector can delete a common
   cofactor factor, hence an actual rank-drop component.  Freeze the 106 raw
   signed maximal cofactors with one calibrated omitted-row determinant; do
   not cancel their common factor.  Recurse on pivot-denominator/Fitting
   strata.  Fable's and Grok's stratified versions, and the independent
   backend design, have the required fail-closed geometry.
3. **K00 is not Avenue 19.**  Grok's phrase “Avenue 19/K00” is a numbering
   error.  Canonical Avenue 19 is the characteristic-`p`/Witt lane; K00 is a
   bounded order-two/guided-search client, closest to Avenue 36.
4. **Coupled inertia is not yet emitted by L5.**  Fable's proposed coupled
   two-coordinate CSP is promising but premature.  L5 gives places and deck
   data for a normalized fibre of one coordinate.  It does not yet prove a
   coupled two-fibration passport, finite-end/component provenance, or the
   all-boundary Proposition 5.1 semantics.  Retain this as a gated pilot,
   not a full reopening of Avenue 25.
5. **“Zero audit coverage” needs a narrower reading.**  Opus correctly finds
   that the 72-item census stops at Proposition 6.2 and that Proposition 8.4
   is exposed.  But Notations 8.1 and 9.1 were directly inspected in the
   Proposition 4.2 review and are definitions, not the first expensive proof
   targets.  The verified-but-unbanked Proposition 5.4 repair has both lower
   cost and immediate promoted consumers; it goes first, while the later
   audit starts in parallel as capacity permits.

## 4. New mathematical items from the round

### 4.1 `CORNER-M1` — exact but not yet independently promoted

At a repaired Proposition 4.2 constant corner, put

```text
a = deg p_F,
b = deg p_(h_m,F) = (mu_F-1)a+1.
```

Because `M_F^* | a` and the corrected definition includes the terminal
degree,

```text
M_F = gcd(M_F^*, b) = 1.
```

This is a clean local consequence of promoted formulas.  It does not bound a
conductor (`<n,n+1>` is the standard scalable warning), does not enter pole
mass because corners are nearrow while pole paths are searrow, and does not
by itself create a global charge.

### 4.2 Opus Theorem C — promising, provisional, and apparently strengthen-able

Opus gives an ancestor induction excluding a nonzero constant first leading
part `g_F^+` at every `F in T_a^+`.  Its key exact identity is

```text
delta_0(F*c) - delta_0(F) = 1 - mu_f - mu_g.
```

At a constant child, `mu_g=0` and `mu_f>=1`, so the parent tower is still
nonempty; Fact A propagates the same first corner to the root, contradicting
Fact B.  The proof uses Statement 3.9 only for the fixed `f,g`, so a common
initial `kappa` suffices.

This theorem is **PROVISIONAL** because its Facts A/B dependency was authored
by Opus and its fresh induction has not yet had different-model hostile
review.  Review must attack: the `alpha_0=0`/delta identity, the implication
`delta_0(parent)>0` needed for Fact A, finite ancestor propagation, and the
choice of a common `kappa`.

There is also an unclaimed strengthening to test.  Target translation
`g -> g-b` preserves the Keller determinant, noninvertibility and the
Lemma 2.1 Newton normalization.  If Theorem C applies uniformly to every
translated pair, it excludes a constant `(g-b)_F^+` for **every** `b`, not
only for `b=0`; this would remove Opus's residual
`{m_F=0,d_{g,F}<0,pi(F)>1}` sliver and restore condition (7) completely on
`T_a^+`.  This strengthening must be reviewed rather than silently banked.

### 4.3 Statement 3.9 `kappa` rider — direct defect, repair expected cheap

Opus gives an exact counterexample at `kappa=1` showing that Statement 3.9's
conclusion fails when `kappa` is suitable for the fibre but not for the
auxiliary polynomial `h`.  Proposition 3.1's pole-order condition is
`h`-dependent.  Thus every consumer with a derived tower member must either
fix one common multiple for the finite family of `f-a`, `g` and all `h_j`,
or prove compatibility under enlargement.  No promoted conclusion is yet
retracted; the rider must be inserted and independently checked.

### 4.4 Proposition 5.1 repair — provisional and bypassable on the pole spine

The post-cutoff producer gives the unique finite-puncture shift
`b_P=g(P)` and replaces the threshold by the zero of
`rho=d_(f-a)+d_(g-b_P)+v-1`.  It also finds that `m_F` is undefined at
`d_(f-a)=0`; the global statement should therefore use the `rho`/leading-
Jacobian predicate and define `m` only on the appropriate positive or
negative side.  Its checker passes 54,854 exact cases; hostile review is
still running.  Pole-only consumers use `b=0` and do not wait for this
repair.  Finite-end, `A(F)` and coupled-passport work must wait for it.

### 4.5 Unit-`S` orders four/five — provisional stopping evidence

Exact producer reports and coordinator replays find cancellation through
relative orders four and five.  The different-model review is still in
flight, and the order-five checker wording overstates explicit coverage of
regular `c10`/`f5` rows.  Even if confirmed, the right conclusion is not
“compute order six”: test finite-mode recurrence/row-ideal invariance, or
compile the complete raw determinant/window incidence.

## 5. Updated bottlenecks and avenue calls

### Proof bottlenecks

1. **Universal landing and coverage.**  Define a total, fail-closed map from
   every normalized exact-pair forest/configuration to a complete finite
   record, preserving branches, entries, contexts and all ancestor data.
2. **`RPMC(C)`, type control and a cofinal ceiling.**  A type-relative kill at
   finitely many `td` values is not a proof of JC2; no current theorem bounds
   the selected type or `td` absolutely/cofinally.
3. **Source-trust/decorated-tree integration.**  Proposition 5.4, Statement
   6.2, Proposition 6.8 and the consumed §§7--9 block are the cheapest
   movable obligations feeding item 1.

### Disproof bottlenecks

1. **A source-open, rank-exact K00/full-`P6` point plus prolongation.**
2. **Characteristic-zero algebraization of a bounded-support certified
   germ, with an actual collision/nonproper end.**  L5 helps certify a germ;
   it does not supply polynomial algebraization.
3. **A deep GGV survivor component beyond the automatic first-order
   obstruction.**  The honest test is the Kuranishi quadratic on the known
   17-dimensional tangent kernel, not another interpretation of the rank-zero
   evaluation dual.

### Avenue disposition changes

- **Avenue 2:** raise and make the principal proof backbone.
- **Avenue 1:** narrow/redesign to rank/Fitting kernel strata and the
  all-order redundancy/raw-window tests; stop equivalent GBs and face-order
  increments.
- **Avenue 4:** raise narrowly for the remaining certified-germ to polynomial
  algebraization arrow.
- **Avenues 6/7/31:** retain as newly better-typed interface pilots after
  all-boundary/source review; do not claim a bound or reopen at scale yet.
- **Avenue 16:** lower execution priority pending a non-composition HENS-CT
  backend and a named client.
- **Avenue 25:** gated pilot only; no generic monodromy rerun.
- **Avenue 26:** unchanged/down; the exact `td=6..9` generic group census
  already hit the `A_d/S_d` wall.
- **Avenue 36/K00 clients:** raise/protect as the strongest finite falsifier.
- All stopped/refuted avenues and the Matysiak claims remain stopped.  Avenue
  19 remains the characteristic-`p`/Witt lane.

## 6. Ranked execution plan

Work proceeds speculatively without waiting for unrelated reviews, within the
campaign's ordinary fanout limits:

1. **`SIGRAY-54-R1`:** extract a standalone, source-faithful proof of the
   Proposition 5.4 `q`-half using deck characters and the bracket equation;
   enqueue immediate different-model hostile review.
2. **`QQ-COFACTOR-STRATA-R1`:** on AWS, freeze the exact matrix and a
   calibrated 105-row block; compute all 106 raw signed cofactors with no
   common-factor cancellation; verify `ell^T M=0`; recurse on denominator /
   Fitting strata; pull the quadratic endpoint back to each kernel.  Stop
   fail-closed at the first custody, calibration, swap or cap failure.
3. **`SIGRAY-CONSUMERS-R1`:** mechanically classify every promoted consumer
   of Statement 6.2 as having the repaired domain or not, then audit
   Proposition 6.8 and the named §§7--9 consumers.  Proposition 8.4 is the
   highest-exposure later proposition.
4. **`CORNER-C-R1`:** different-model hostile review of Theorem C,
   `CORNER-M1`, the translated-pair strengthening and the Statement 3.9
   `kappa` rider.  Promote only the maximal scope that survives.
5. **`K00-COMPRESSED-R1`:** after its exact preregistration is frozen, run
   BASE4 and LOW-KILL on separate AWS capacity, require replay against the
   undeduplicated equations, and route any point immediately to full `P6`
   plus one prolongation.
6. **`EPTC-R1`:** implement the exact-pair certified tree/source-fuzz compiler
   with the mandatory `y-x^2` coverage failure and a tame automorphism as
   negative/positive controls.  It may support later coupled-passport or
   certified-terminal searches only after the source semantics are banked.
7. **`KURANISHI-R1`:** compute the second-order obstruction on the known
   17-dimensional deep-locus tangent kernel on AWS if capacity remains; the
   first-order dual is automatic and must not be counted again.

Heavy algebra runs only on AWS, on isolated registered jobs with zero swap.
Local execution remains limited to source inspection and light exact
standard-library checks.  No campaign task enters, reads, builds, modifies or
controls `jc2-lean`.

## 7. Model/collaboration evaluation

Treat Fable 5 and Opus 5 as equal co-researchers with Sol Ultra and Grok46;
this round confirms that all four add nonredundant value:

- **Sol Ultra** supplied the clean dependency architecture, `CORNER-M1`, the
  semantic compiler and K00 prioritisation, but made the endpoint
  linear-vs-quadratic typing error caught during synthesis.
- **Fable 5** most accurately priced the immediate Proposition 5.4/Statement
  6.2 source package and supplied the best stratified kernel design; its
  coupled-inertia claim needs more provenance than L5 currently gives.
- **Grok46** independently converged on cofactors/Fitting strata and the
  source-fanout triad, but misnumbered K00 as Avenue 19.
- **Opus 5** produced the round's most original mathematical content:
  Theorem C and the auxiliary-`h` `kappa` defect, plus the all-order
  redundancy idea.  Its cofactor primitive-clearing shortcut is unsafe and
  its theorem still needs different-model review.

The best operating pattern remains independent whole-portfolio ideation,
then coordinator adjudication rather than voting.  Convergence determines
priority; disagreements and individual errors determine review tests.

## 8. Round conclusion

The round did not solve JC2, but it materially compresses both sides of the
campaign.  The proof route now has a precise frontier—source-trust integration
followed by universal landing—while the finite computational route has a
precise replacement for expensive, duplicative Groebner runs.  The highest
short-horizon mathematical payoff is Proposition 5.4 plus the corner/`kappa`
review; the highest computational payoff is the raw-cofactor/Fitting
stratification, with K00 retained as the independent falsifier.

The next quiet full ideation round is due no later than twelve hours after
this close, and earlier on any promoted corner theorem, Proposition 5.4
failure, rank-stratum survivor/closure, K00 full-`P6` event, or credible
external JC2 claim.
