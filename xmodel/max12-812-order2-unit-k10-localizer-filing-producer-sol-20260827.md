# Producer — `k`, `k0`, and `k10` are one coefficient; `k=0` is a sibling family

Date: 2026-08-27

Status: **EXACT SOURCE-FILING CORRECTION, AWAITING DIFFERENT-MODEL REVIEW.**

## Exact custody chain

The controlling Gate-T obligation table, SHA-256 `50db2706...`, defines its
target twice as the

```text
post-M=0, unit-k10 collision family
```

and later as the post-`M=0`, `D(k10)` collision Gate T.  Its firewall lists
`k10=0` as a different uncovered load ray, outside that target.

The literal actual-total V17 exporter, SHA-256 `25af4029...`, constructs

```python
k10 = dense_series(base, ["k", "k1", "k2c"]
                         + [f"k10_{i}" for i in range(3, 11)])
```

so, in mathematical notation,

```text
k10(sigma)=k+k1*sigma+k2c*sigma^2+... .
```

Over the coefficient ring used by the formal source, this series is a unit
if and only if its constant coefficient `k` is a unit/nonzero at the
field-valued scope.  Thus the promotion's `k`, the older fixed-special-fibre
notation `k0`, and the source series `k10` name one coefficient at three
levels:

```text
k = k0 = constant term of k10.
```

The independent Fable5 calculus review, SHA-256 `db160c13...`, already
identified this naming drift and warned that listing both `V(k)` and
`V(k10)` double counts the same boundary.  The independently reviewed
exact-square source, SHA-256 `30acda98...`, describes `k10=0` as a
**different two-load problem**, not a silently lost affine family.

## Correct filing

Within the named post-`M=0`, unit-`k10` Gate-T family:

```text
D(k) = D(k0) = the registered source base.
```

Therefore the genuine factor `k` in the promoted `T-rs` certificate
(`52b59e8d...`) and in the expected direct `T-cs` certificate is still a
real localizer algebraically, but it creates no additional on-family
closed complement.  Its `V(k)` is outside this family's registration.

Globally, no work disappears: `k10=0` remains a mandatory sibling
load-timing family in the order-two fan, with its own square/discriminant/
zero-normal supports and source/landing obligations.  It must be routed and
proved there.  It must not be counted both as `V(k)` below each unit-`k10`
Rees chart and again as the `k10=0` family.

The exact horizontal prefix section at `cs=1,k=0` in producer
`max12-812-order2-p0-total-rees-prefix-zero-sections-producer-sol-20260827.md`
is consistent with this filing: the unit-`k10` source rows were exported on
their closure, but the boundary point belongs to the sibling family and
cannot be killed by the charged unit-family prefix.

## Ledger consequence if reviewed

- Preserve every existing `D(k)` theorem literally.
- In the live Gate-T obligation count, call `D(k)` intrinsic to the selected
  unit-`k10` family; do not list `V(k)` as an on-family residual.
- Keep one separate global item named `k10=0 sibling load-timing fan`.
- Treat `k`, `k0`, and `k10` as aliases only with the level distinction above;
  never list `V(k)` and `V(k10)` as two strata.

## Firewall

This is a scope and deduplication correction.  It does not close the
`k10=0` sibling, the remaining `T-cs` direct total identity, either `J2`
chart, the receiver, the source/landing fan, order two, maximum twelve, or
JC2.  It does not turn `k` into an exceptional coordinate or permit dropping
it from a polynomial identity; it says only that its localization is already
part of the registered family being proved.
