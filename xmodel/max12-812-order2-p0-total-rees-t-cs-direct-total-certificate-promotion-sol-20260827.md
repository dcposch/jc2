# Promotion: direct actual-total certificate on the ordered `T-cs` chart

Date: 2026-08-27

Status: **PROMOTED EXACT POLYNOMIAL MEMBERSHIP THEOREM.**

## Evidence

```text
7af66e58a7262ca05bf140919ed3d6ff4e0357bdb6db3378d494907ff33baa8f
  xmodel/max12-812-order2-p0-total-rees-t-cs-direct-total-certificate-opus5-20260827.md

f16e1114b24e88dfa60d0e687881817f73055361d450547f40ddc9cd5374a844
  xmodel/max12-812-order2-p0-total-rees-t-cs-direct-total-certificate-hostile-review-grok-20260827.md
```

The different-model review independently reparsed all 22 frozen source rows,
reconstructed the honest/substituted presentation bridge, expanded the
grade-10 branch, re-ran the branch-`(b)` Laurent rewrite with cofactor
accumulation, checked denominator clearing, and expanded the final
composition.  It did not trust the producer's scratch scripts, hashes of
intermediate Laurent objects, or V18/V19 engine output.

## Theorem

In the ordinary polynomial ring over `Q` containing the frozen exact-Q rows

```text
Tg10_1,...,Tg12_7,Tg14_5,
```

let `K` be generated by those rows and the honest ordered-chart equations

```text
rs-cs*qrs,  c0-cs*qc0,  c1-cs*qc1,  qrs.
```

Then

```text
cs^447 * k^164  in  K.                                      (T-cs)
```

This is the staged certificate type

```text
exceptional power = cs^447,
genuine localizer = k^164,
rho factor        = 1 = 1+rho*0,
ordered equations = the four equations displayed above.
```

Neither `cs`, `k`, nor `rho` is inverted in the polynomial identity.  The
exponents are sufficient and are not asserted minimal.

The reviewed generating data consist of two exact pieces.  The grade-10
rows give `cs^6*k^2*rho^6` in the honest ideal.  After setting `rho=0`, a
Laurent calculation inverting only `cs` and `k`, followed by explicit
denominator clearing, gives

```text
cs^147*k^54 + rho^2*H
```

in the total substituted ideal.  The polynomial identity

```text
cs^6*k^2*A^3
 = cs^6*k^2*(A+B)*(A^2-A*B+B^2) - H^3*(cs^6*k^2*rho^6),
A=cs^147*k^54,  B=rho^2*H,
```

has left side `cs^447*k^164`.  A telescoping polynomial expansion returns
the result to the honest chart equations without division.  The reviewer
verified every generating identity and denominator bound; printing the
million-term fully composed cofactors is unnecessary for membership because
they are determined polynomial combinations of the checked pieces.

## Consequence and routing

After Rees saturation by the exceptional coordinate `cs` and localization
at the genuine factor `k`, `(T-cs)` makes the ordered `T-cs` chart ring zero.
This closes the **whole registered ordered `T-cs` total-family stratum** on
`D(k)`, not merely its `rho=0` fibre.  In the separately reviewed unit-`k10`
Gate-T filing, `D(k)` is intrinsic to the named family.  Thus there is no
additional on-family `V(k)` residual, although the one global off-family
load-timing sibling remains open.

Together with the promoted `T-rs`, `T-c0`, and ordered `T-c1` theorems, this
completes the four ordered `J1=(rs,cs,c0,c1)` chart strata for the named
unit-load family.  It does not eliminate arcs on `V(J1)`; those route to the
two `J2=(a0,a1)` stages and the separate terminal receiver.

V19 is no longer needed to establish existence of a typed `T-cs`
certificate.  Its already-running capped jobs may still provide smaller
exponents or an independent machine artifact, but no successor is licensed
without new value.

## Scope firewall

This theorem inherits the frozen V9/V17 literal-source provenance and does
not enlarge their source universe.  It does not close the off-family
`k10=0` load-timing fan, either `J2` chart, the terminal receiver, the
generic deck/square comparison, full landing/coverage, arbitrary standard
pairs, `G2-PSC`, any invoked `G2-BD`, order two, maximum twelve, or JC2.

