# K=16 ray (T) at t=6: heuristic and Laurent/Euler terminal systems

Date: 2026-09-03. Lane: `k16-t6-grok46-v2-20260903`.
Status: SKELETON (jobs not yet launched; this file has no `BODY-END`).

## Verdict (pending)

Typed after saturation returns. Allowed values for (T) at t=6: `PROVED-HERE` /
`MEASURED-MODULAR` / `OPEN`. No exit-price is asserted, so no `charge_basis=`
line is due.

## 0. Custody

MEASURED. Receipt `xmodel/k16-t6-grok46-v2-20260903.run.v2` was parsed with
`awk` on `charged_input_<i>_basename=` / `charged_input_<i>_sha256=` (indices
1..18). Manifest written to `/tmp/k16t6-v2-inputs.manifest.from-receipt`.
`sha256sum -c` returned **OK for all 18/18** frozen files under
`/tmp/jc2-lane.OiWvSv/inputs`. Digits were not retyped.

Frozen basis from the receipt: `7bf1eac0e9e7bf6d739dd1ec83042390892255ac`.

Drivers and new artifacts live in `box/k16t6-20260903/`. This is the only new
`xmodel` report. No ledger, `jc2-lean`, `ideation-*`, or named in-progress
lane report (`k16-terminal-proof-*`, `k16-t5-replay`, `g9966-*`,
`emitter-native`) is read or edited.

Charged sources used: `t_order_system.py`
(`e115d576e850523f8796008d1b9d7899382cbac918ac2e74419207d9e7e7dc28`),
`triangular_preprocess.py`
(`f2defb76d36172c541431b2fff04b34770438eef81167210fec404344fefbf93`),
`k16t56_pipeline.py`
(`96d3b8eaefcab0eefee079aa89addc1e48aa8eabf24d6aa4e7948f4564eeba53`),
`terminal_laurent_model.py`
(`f900ca3a6e5537e9605a98a5135a4b7dc9eb78c2a2cdde0aeff972b348db9c29`),
`laurent_pivot_formulas.py`
(`2765a94a1f955fb6844ef7886ac4a9f70135fee56c4c44e39c466b3325883bb7`),
and the charged t=6 heuristic QHy emitter
(`d840675b403e7e97c650294a73b9080f568150aab0a485fcbf6079122a1f69cd`).

FALLACY-v2 applies. Previous v2 attempt started Singular jobs, adapter-exited
0 with this report missing; leftover stamps in the box have START and no END.

## 1. Plan (this skeleton)

1. Copy charged drivers into the box. Patch only `LANE_INPUTS` (this lane's
   frozen copies) and drop the t=4-only `t4_order_system.py` hash check, which
   is not among the 18 charged files. Point Laurent `OUT` at this box.
2. **Chart.** Regenerate t=6 with `--stop-after units`: 63 unknowns / 93
   equations, Phi (−1, 6/19), 22 Q-pivots, primitive `H_6=507y^2-273y+35`,
   three A_t-units. Cap 1500 s.
3. **Heuristic order.** Reuse the terminal 12×6 whose charged QHy file matches
   `box/k16t56-20260903/t6_heuristic_exact_QHy_std.sing` byte-for-byte, with
   run log `t6_heuristic.run.log` recording HASH_OK of the two charged Python
   generators, 32 A-pivots, residual names `(q7_0,q8_1,…,q12_1)`, bands 0..11,
   degrees 25..14. Do not re-run the 3162 s A-elim.
4. **Laurent/Euler order.** No `terminal_laurent_t6.json` exists on disk.
   Run charged `terminal_laurent_model.py 6` with `OUT` in this box. Cap 3000 s.
   If it finishes, emit the same saturation family as the heuristic.
5. **Saturate.** Three modular primes 32003/32009/32027 (coefficients reduced
   in Python before `std`), then exact `nfmodStd` over `A_6=Q[y]/(H_6)`.
   Caps: 1200 s modular each, 3600 s nfmodStd, `setcores(4)` on the exact job.
   Wrapper controls and the actual-pair classifier as in the charged emitters.
6. Print the heuristic terminal in band order; compare with t=2..5
   (`2t×t`, degrees `4t+1..2t+2`). Controls. Typed verdict.

Long jobs launch after this skeleton is on disk, with explicit `timeout`
caps and `.stamp`/`.exit` files. The turn does not end until every job
started here has finished or hit its cap.
