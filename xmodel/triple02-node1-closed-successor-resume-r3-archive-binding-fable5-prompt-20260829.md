# Fable 5 repair — TRIPLE02 closed-successor R3 archive binding

You produced R2. Work from `/Users/dc/code/math/jc2` and write a fresh,
immutable R3 packet plus exactly this report:

`xmodel/triple02-node1-closed-successor-resume-r3-archive-binding-fable5-20260829.md`

Target packet:
`cases/ggv_8_28_upper_endpoint_triple02_closed_successor_resume_r3_20260829/`

Read R2, your R2 report, and the complete different-model hostile review
`xmodel/triple02-node1-closed-successor-resume-r2-hostile-review-opus5-20260829.md`.
Treat R2 as immutable. Copy to the new packet and repair the blocking O-B1
finding: `derive_late_decision` currently authenticates a mutable fresh
extraction rather than the frozen archive bytes. At decision time, bind the
fresh root to the charged archive hash and re-run the complete manifest and
exact-membership verification before reading any classifier input or creating
the decision record. A same-UID edit, deletion, replacement, symlink, or
archive/fresh-root mix-and-match must fail closed.

Also close directly adjacent low-risk custody findings from the review where
doing so cannot alter the mathematics: exact rather than divisibility-only
node-SB census if source semantics warrant it; bind runner-log content and the
previously uncharged launch manifest; strengthen systemd read-back to all five
charged properties; place latches under the appropriate authenticated
custody. If any is intentionally deferred, make the remaining rehearsal
obligation explicit and fail closed at the pilot boundary.

Preserve the R2 mathematical builders byte-for-byte unless an identified
custody repair makes that impossible. Regenerate all derived artifacts,
manifests and decisions from the new source; add the review's working forgery
as a mandatory negative control. Replay ordinary and `-O` selfchecks, driver
dry run, classifier mutations, archive/marker attacks, death/restart paths,
and exact census/rank controls.

The report verdict may be only
`SOURCE_READY_FOR_DIFFERENT_MODEL_REVIEW` or `REPAIR_INCOMPLETE`; R3 itself
does not authorize even a disposable rehearsal. Include full/body hashes,
exact commands/counts, and an R2-to-R3 file/function diff inventory.

Hard boundaries: no AWS, live Singular, commit, push, canonical edits, heavy
CAS, or local long/high-memory process. Never access/list/search/build/status
or control `jc2-lean`; never run global `git status` or workspace-wide
searches. Modify only the requested R3 report and packet; use `/tmp` for
scratch.
