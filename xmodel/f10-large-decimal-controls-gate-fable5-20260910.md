# f10 large-decimal controls gate (Fable 5.1, 2026-09-10) — FIRST hostile STATIC gate

Status: UNSEALED body, no charge_basis. First action 05:19:55 UTC (own targets verified absent, then all 13 input hashes taken BEFORE any body read; copy in box/f10-large-decimal-controls-gate-fable5-20260910/input-hashes-verified-0519Z.txt). Controlling stop 05:33:55 UTC (first+14 min, earlier than 05:35:00), final two minutes reserved from 05:31:55, never reset.
Charged inputs: exactly 13 immutable snapshots in /tmp/jc2-lane.CEHxQW/inputs; all 13 SHA-256 matched the charge list exactly. Method: cat/sed/diff/sha256sum and hand arithmetic only. ZERO Python/CAS/import/AST/compile/syntax/test/fixture execution, no coefficient artifact, no network, git, proc, live-file or other-lane read.
Premises, not re-hardened: accepted 17zu narrow repair (solver.py/checker.py setters, oldsolver.py = accepted old version 961e22e6); algebra.modulus() rank-seven squarefree acceptance. The oldsolver/solver diff was regenerated here: exactly two hunks, one comment plus `sys.set_int_max_str_digits(0)` inside generic_candidate (new line 16) and inside main before candidate (new line 330); nothing else differs.

## A. Admission, modes, authority, pins, bounds — CONFIRMED

- CLI and modes: large_decimal_controls.py:99-101 requires exactly AUTHORITY INPUT MODE OUTPUT and MODE in {old,new,fresh-check}; Linux at 102; flat cwd at 104; the gate file is hashed against FROZEN before `from execution_gate import authorize` (105-108); authorize is called with operation `check` only for fresh-check, else `build` (109). Because authorize's check branch requires `sys.argv[2]` to be hash-registered (execution_gate.py:66-67), the fresh bundle is necessarily INPUT at argv[2], as the interface states.
- Authority: bounded 64 KiB re-read (114) compared to the SHA returned by authorize (115); `univariate_acceptance` None, `synthetic_only` True, `large_decimal_mode` == MODE (117-118); `file_size_limit_bytes` == 16777216 and RLIMIT_FSIZE soft=hard=16 MiB inherited, never set (119-120); registered interpreter (121). evidence.py is hashed before its import (110). evidence.verified is never called, so no fabricated acceptance path exists.
- Pins: all seven FROZEN siblings must be regular non-symlink files whose registered digest equals FROZEN equals the live digest (123-126); helper self-pin and interpreter pin (127-128); INPUT pin required in every mode (138); every registered file re-digested before science (140-141) and after (270-273), authority and INPUT re-digested after (272). producer_environment (139) is metadata only; `import flint` occurs only in old/new (201), never in fresh-check, and 163-164 asserts solver/oldsolver/flint absent from sys.modules there.
- Outputs: the six paths O, .fixture, .certificate, .bundle, .mutation, .failure must be absent by lexists (132-134), must not alias any registered file, INPUT or authority (135); freeze() writes with `xb`, refuses more than 16 MiB before writing, chmod 0444, bounded re-read, byte and parsed-object equality (46-55). Written sets match the interface: old fixture+O, new certificate+bundle+fixture+O, fresh mutation+O.
- Failure path: the outer handler (281-289) writes O.failure.json with class, reason, traceback and retained artifacts, then re-raises; every name it uses (custody, input_sha, artifacts, mode) is bound before the try at 146. Pre-admission failures have no helper file, as the interface says.
- Cooperative custody only: nothing here authenticates ROOT; native-closure completeness, dummy control and registration remain declared ROOT duties (interface 17-27, 37-43).

## B. Arithmetic-first fixture, old/new discrimination — CONFIRMED

- No decimal path before the selected API: primitive_fixture (58-71) builds N = 10**7421+1 and all coefficients with int/Fraction arithmetic and algebra.modulus(); the only str() calls in the helper before the API are on file paths. The 4300 check sits at 212, after `import flint`, primitive_fixture and the selected solver import, so any import-time policy change would fail closed. Module level of solver.py, oldsolver.py, algebra.py and generic_controls.py contains no setter; the only helper setter is line 228, inside the old branch after the observed failure.
- Old branch (213-231): accepts only `ValueError` whose message contains both `Exceeds the limit (4300 digits)` and `integer string conversion`, requires policy still 4300 afterwards, records extract_tb frames without asserting a site; success or any other error raises (226) and becomes STOP-GAP via the handler. Only then (228) is zero set, and the summary labels that timing. Hand trace of oldsolver on this fixture: fmpq entries are built from integer arguments (solver 179-180), so the first decimal conversion on the path is `int(pieces[0])` inside rational() on the RREF entry -N (solver 217-220), predicted, not observed; the helper correctly records rather than assumes it.
- New branch (233-259): relies solely on generic_candidate's own setter (checked == 0 at 234). Hand derivation: project f1=T^2 (degree 2, leading coefficient 1, unit, so no split, one leaf, pivot 0), f2 = 1+N T+T^3; rows = 7*2 = 14, columns = 8*14 = 112, one rref. Mod T^2, f2 ≡ 1+N T and T f2 ≡ T, so h2 = 1 − N T; target − h2 f2 = N^2 T^2 − T^3 + N T^4, divided by T^2 gives h1 = N^2 − T + N T^2 with zero remainder; CRT idempotent is 1. Identity: (N^2 − T + N T^2) T^2 + (1 − N T)(1 + N T + T^3) = 1. Witness index (i*26+j)*7+ell gives slots 0, 7, 14 = N^2, −1, N and 182, 189 = 1, −N, all others zero, exactly the helper's expected vector (252-255). DenseFixture.verify's UNIT return dict equals the literal at 249-250.
- Common fixture: pristine deepcopy before the API (207), equality after (260), first input_wire only after the outcome (262), exclusive freeze and parse_input read-back compared to the arithmetic tuple (263-266); parse_input's envelopes (1..6 slots, 7 coordinates, degree-seven p) hold for this fixture. Cross-child hash/byte comparison is correctly left to ROOT (interface 73-74).

## C. Actual-schema synthetic bundle and fresh checker — CONFIRMED

- actual_bundle (74-95): nonmonic raw modulus wire; nine ids and bounds [3,3,2,4,3,3,2,2,5] identical to checker.py:89-90; each row padded dense to degree_bound+1 with `[]` for zero coefficients (bparse of [] is bzero); nonzero scalars use the canonical seven-exponent triple with sorted unique k, which is what bparse requires (65-79). Guard keys exactly {variable, equation, q, factors}; q = 1 in six slots, r = 1 in two, h0 = 1 in five, so tmul(r,h0) equals q and q^5 has 26 slots. The certificate carries none of the forbidden acceptance keys and is CANDIDATE-ONLY. actual_bundle runs only after the new API set policy 0, so its str() of N is legal.
- Fresh-check (147-196): evidence.load keeps quoted wires as strings; before checker.verify the helper asserts the bundle shape, a >4300-character quoted h1 numerator with denominator '1', 1638 slots, no solver/oldsolver/flint module, and policy 4300 (165). It then requires policy 0 (167), the exact 217/9/25 UNIT result, and an unchanged bundle (168). Only afterwards does it convert h1's constant, require it equal (10**7421+1)**2, add exactly 1, and prove by a restored copy that no other parsed field changed (173-183). The mutated bundle is frozen exclusively, re-read, and must fail checker.verify with exactly `full 217-coordinate sum h_i f_i equals q^5` (checker.py:135): N^2+1 is a canonical spelling, all earlier need() checks pass, and the residual is +T^2 in total[2]. Acceptance (194) or any other exception is STOP-GAP. No checker.main or evidence.verified call exists.

## D. Interface sufficiency and scope — CONFIRMED with one small interface omission

- Unchanged scope: generic.plan.json is the charged ce193898 bytes with its eight positives/four negatives; the helper never reads it, never calls generic_candidate on a small fixture first, and does not touch callers, caps or schemas. Serialization allocation is explicitly left to registered CPU/RSS/wall/aggregate caps (interface 37-43, 71). Every status string is labelled synthetic_only with source_acceptance None (274-275).
- Smallest necessary interface defect (documentation, not code): the helper requires INPUT to be an absolute path whose parent is the package root and not a symlink (large_decimal_controls.py:130, 38), but EXECUTION-INTERFACE.md states this only for OUTPUT ("root-local", line 60) and describes old/new INPUT merely as an externally pinned descriptor (44-49). A ROOT placing the descriptor outside the flat package gets the pre-science refusal `flat absolute pinned INPUT`, fail-closed but wasted. Remedy: one sentence in the interface, "INPUT must be a regular non-symlink absolute path inside the package root, registered in file_sha256 in every mode." No code change is needed.
- No code defect found. Everything at runtime remains UNEXECUTED: old failure site, native behaviour, the 217 and 1638 equalities, fresh-process policy, and output existence.

## Verdict

A CONFIRMED. B CONFIRMED (math and call order by hand; runtime site predicted only). C CONFIRMED. D CONFIRMED with the INPUT-location omission above as the only remedy. No REFUTED item. GAPs are exactly the declared UNEXECUTED runtime facts and ROOT's registration duties; none is a code defect.

## Collision scan (own / own-only)

New canonical OPEN IDs raised here: zero. Writes: this report and the box directory only, both absent at 05:19:55. Own-only scan at the final write: no raised-OPEN heading, no charge_basis line, no Seal section.

<!-- BODY-END -->
