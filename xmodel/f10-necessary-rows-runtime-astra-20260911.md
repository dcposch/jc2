# Necessary-row runtime identity port

STATIC / DISABLED / UNEXECUTED. First action2026-09-11 05:54:47UTC; owned box/report/transaction absent and six charged pins matched before reads or explicitly reused WHOLE text. Original publication reserve06:07/HARD06:10UTC remains fixed.

Scope is only the two specified literal substitutions in NEW dispatch.py, probe.py and mutate.py copies. No old source, scientific algorithm, authority layout, cap, supervisor, command order or exact direct-source failure predicate may change. A combined source/runtime DELTA FIRST and fresh ROOT registration precede any execution.

## Delivered identity port

The three owned Python copies are complete. Each whole new file was text-compared to its pinned original after exactly two global literal substitutions: `direct-rows` to `necessary-rows`, and `DIRECT_ROWS` to `NECESSARY_ROWS`. All three comparisons passed. Saved unified diffs were independently regenerated and matched literally. No other text change was made; all frozen originals retain their supplied hashes.

The new job is `f10-source-cone-r3-necessary-rows-v1`. Namespaced runtime, policy, wire, phase and mutation schemas and positive/status strings receive only the specified replacements. Generic `direct`, singular `direct-row`, and `direct-source` text intentionally remain. In particular the T rejection still requires normal exit 2, empty stdout, and exactly `CHECK FAILED: direct-source graph slot T\n` on stderr.

Owned source pins:

- `dispatch.py`: `073e22dbcfa3a11a2afaf6fd9c845ca6ad1d0038ccd3fddc22db21e9f5beeb24`.
- `probe.py`: `5f1211e1a77b5ce2bb4cc496deabc533e3c41cdea88ea750149dac886cc09c69`.
- `mutate.py`: `12eac7bbe5566ac0372c51b36617409e0501595024d948d6086a767f90d84eec`.

The matching `.diff` files and `PINS.json` are in `box/f10-necessary-rows-runtime-astra-20260911/`. No scientific module, new supervisor, registration, policy instance, or coefficient artifact was supplied.

## Preserved contract and scope

All old algorithms, thirteen-command order, caps/profiles, deadlines/admission checks, source five-file API, ROOT-CAPRUN authority layout, CAPRUN/native-closure schemas, exact flags, source/physical/process checks, freeze/readback rules and quiet/durable custody remain byte-identical outside those literal substitutions. This port does not repair or strengthen any inherited instrument property. Optional inverse-mutation hardening remains OFF: restoring the saved slot certifies serialization and outside-slot fidelity, not an independently implemented arithmetic inverse test.

The adopted interface and its design FIRST license necessary-row scope, not an equality of the old and new overall acceptance domains. Alternative Laurent-q inverse/pole arrays are NOT_COMPUTED in the new scientific contract. Their source owners' changes are not implemented or inspected here. Retained scientific inverses and the conditional source attachment are not re-proved by a runtime rename. This lane produces no source point, source-zero, rank, admissible-place, or JC2 result.

ROOT must bind the genuinely new authority/job/source/contract pins and exact prospective preflight policy. No historical positive receipt, acceptance, worker fact or enabled registration is supplied or reused. The combined source/runtime DELTA FIRST must precede any separately authorized execution.

## Reading, checks and remaining work

The adopted interface, its publication manifest, and the design FIRST were read FRESH_WHOLE. The three old Python inputs use explicitly declared same-agent prior-WHOLE reuse after current pins; their complete current text was also retrieved for literal copying. All three new sources and all three diffs were read WHOLE. `PINS.json` records all six exact inputs and modes. No live producer/checker, provenance, coefficient body, fixture, network, worker or protected input was accessed.

Only source-text substitution, diff/readback and documentary hashes were checked. ZERO imports, parsing/AST, syntax/compile tests, scientific subprocesses or runtime controls were performed. The source is STATIC / UNEXECUTED and carries no execution authority.

OPEN quantity: one combined source/runtime DELTA FIRST, then one separately ROOT-registered validation if ROOT chooses to authorize it. Cheapest next check is the literal three-file diff plus final source/authority/schema/expected-byte bindings in that combined review. Runtime cost and behavior remain unmeasured for these new copies. The requested static identity port is complete; runtime readiness is not established.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `4587`.
- Body SHA-256:
  `3b13443b108d86c09ed15416eaf85a147305ef4cbffd320e2ff879c9ae3d8285`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
