# Hostile different-model review: whole-order-three terminal Belyi classification

Write the review to
`xmodel/max12-912-order3-global-terminal-belyi-classification-review-grok-20260825.md`.
Do not edit any producer, case, canonical, coordination, prompt, run, or log
file.  Read the target and every named parent in full before deciding.

Target:

- `xmodel/max12-912-order3-global-terminal-belyi-classification-audit-20260825.md`
- required target SHA-256:
  `28f33b993726b4bd7441a79a8f4208983dbbd0c5e4113d839a78af1b8cc7b6c6`

Independent same-model scope cross-check (context, not evidence):

- `xmodel/max12-912-order3-universal-terminal-belyi-scope-audit-as-20260825.md`
- SHA-256:
  `4518ee123fe422e6ee287ea43e865943059455dfcaa6a7f863717d3626e20117`

Charged reviewed parents and exact SHAs are listed in the target.  In
particular read the Kummer preflight and review, universal Faber producer and
review, selected terminal classifier and review, and its nondegeneracy
erratum.

Audit adversarially:

1. Does the reviewed whole order-three Faber normalization really give
   `sigma(r8)=zeta^2*r8` and hence `r8=u^2 R`, `R in C(x)`, before any
   selected-Q8, `k=mu=0`, or `nu!=0` specialization?  Trace the exact weight
   convention and constant field.
2. Starting from the original terminal row `9*r8'=j/u`, independently check
   `T=r8^3 in C(x)`, `T'=(j/3)hR^2`, and
   `h=(j^3/27)T^2/(T')^3`; test all zero, constant, ramified, and scalar
   edge cases.
3. Re-derive the Wronskian polynomiality criterion for reduced `T=A/B`, and
   determine whether any step of the selected terminal classifier secretly
   uses Q8 coordinates, `nu`, a selected coefficient component, parity, or a
   contact theorem after the terminal identity is available.
4. Check that `h in C[x]`, `3|deg h`, and `h` noncube are licensed on every
   actual nontrivial-Kummer residual `(9,12)` trajectory, and that they exclude
   both unequal-degree strata.  Recheck the balanced passport,
   `r+s=e+1`, `deg h=3(e+1)`, and Riemann--Hurwitz saturation.
5. Search the repository for an earlier branch-wide promotion.  Distinguish
   a new composition/scope theorem from duplicate algebra.
6. Attack the firewall: the cyclic `D=1,2` terminal controls, lower
   `(k,mu,nu)` fibre, original terminal converse, both Taylor families,
   removed boundaries, order-one/B8 leaves, maximum twelve, and JC2 must all
   remain unclaimed.

Return one of `CONFIRMED`, `CONFIRMED_WITH_REPAIRS`, `INCONCLUSIVE`, or
`REFUTED`; identify the smallest false identity or missing hypothesis.  Give
a precise promotable sentence and exact residual obligations.  This is a
mathematical/provenance audit; do not run local CAS, solvers, or substantial
Python.  Lightweight hashing/search is permitted.
