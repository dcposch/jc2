# Hostile source/certificate review — explicit control-2 `la^20` witness

Act as a hostile commutative-algebra/tropical-degeneration and CAS-custody
referee. This is a text-only review on the local Mac: do not run Python,
Singular, Sage, msolve, Lean, Gfan, a solver, or substantive symbolic
computation. Lightweight reads, searches, hashes, and hand arithmetic are
allowed. State explicitly what you did and did not run.

Audit the complete package

```text
cases/max12_912_order3_d1_double_root_control2_la20_syzygy_v2_20260826/
```

whose witness-freeze file has SHA-256

```text
04cd09886ed78e1af48a6387ee2c67996856d2b6f59791963450e7e4380e32a3
```

Read every root source/custody/result file and every harvested file under
`aws_r6d_LPDP/`. Also read:

```text
cases/max12_912_order3_d1_double_root_control2_la20_syzygy_20260826/V1_SOFTWARE_CONTROL.md
cases/max12_912_order3_d1_double_root_control2_la20_dp_cone_20260826/V1_SOFTWARE_CONTROL.md
cases/max12_912_order3_d1_double_root_control2_rees_corrected_A_20260826/RESULT.md
cases/max12_912_order3_d1_double_root_control2_rees_corrected_A_20260826/PROMOTION.md
xmodel/max12-912-order3-d1-double-root-control2-la20-witness-cone-formulation-20260826.md
```

Charge every item below and fail closed on ambiguity.

1. **Source custody and repair.** Verify the frozen V2 compiler pins and
   reconstructs exact quarantined V1 source SHA
   `2d22f9867aceb250d8df6002fc4a541fffd225dfb6f8b18d3bbce2aab0f75737`,
   then changes only its one tag and the four reserved-identifier tokens
   `GCD` to `CDSTD`. Verify the emitted V2 source SHA
   `e9b2c1594460cc8a6053654f0fcd810522044c6b1ef89d6e92ec2fb0038fd894`.
   Confirm that V1 has no mathematical verdict and that no V1 printed
   saturation exponent is cited as evidence.

2. **Saturation and lift semantics.** Audit the toy orientation control, the
   construction `I=(E1,...,E8,LT)`, `C=I:<s>^infinity` via
   `sat_with_exp`, and two-way equality with direct `sat(I,<s>)`. Check what
   the returned exponent `N` licenses. Audit the `lift(H,TARGET)` orientation
   for `H=(C,s)`, selection of nonzero `C` coefficients, the lift of
   `s^N*USED` back to all nine original generators, and the final literal
   zero-polynomial check. Decide whether the printed identity really proves

   ```text
   s^96*la^20-s^97*TAIL=sum FINAL[i]*I[i].
   ```

   Do not assume that `96` is minimal; explain why minimality is irrelevant
   after `s=1`.

3. **Exact dehomogenization.** Recompute by hand from the printed `TAIL` that
   substituting `s=1` into `la^20-s*TAIL` gives exactly the eight-term `W` in
   `WITNESS_RESULT.md`. Check the primitive integral scaling by `972`, every
   sign, and every coefficient. Verify that this places `W` in the
   dehomogenized ideal generated by all nine charged equations, not merely in
   a saturation or localization with an uncharged denominator.

4. **Weight and cone arithmetic.** In order
   `(la,tau,rho,q1,q0,r2,r1,r0)`, independently calculate every term weight at
   `(4,1,1,22,22,30,30,30)` and audit the claimed list
   `81,80,88,82,82,82,82,82`. Derive all seven strict halfspaces in
   `CONE_PREMISES_PROVISIONAL.md`, including the `T>0` inequality from
   `la^20*tau`, and check that `rho` is absent. Check the stated margins.

5. **Initial-ideal theorem.** Decide whether `W in J` plus a unique
   least-weight term `la^20` implies `la^20 in in_w(J)` under the campaign's
   least-weight convention, and whether invertibility of `la` on the stated
   eight-coordinate torus makes its localization empty. Explicitly
   distinguish this finite **witness obstruction region** from a full
   Gröbner cone or a complete Newton fan.

6. **Exceptional faces and coefficients.** Audit the formulation's claim
   that equality faces generally have a nonmonomial witness and require a
   separate full face-initial-ideal torus saturation. Audit the parametric
   warning: denominators and the target coefficient must be charged, while
   vanishing of a non-target coefficient only shrinks support. Confirm that
   this computed certificate is only at a fixed rational source and proves no
   moving-parameter neighborhood by itself.

7. **AWS endpoint.** Verify registered tag/PID/caps, source closure, compiler
   and Singular rc zero, empty compiler/CAS stderr, empty stdout diagnostics,
   exact PASS markers, result hashes, timing/max RSS/no-swap statement, and
   absence of `FAIL_` or parser diagnostics. Treat the still-running
   global-`dp` cone job and V3 minimum-exponent race as pending corroboration,
   not evidence for this review and not prerequisites for the exact LPDP
   witness identity.

8. **Scope firewall.** The strongest permitted conclusion is an explicit
   dehomogenized witness, hence the fixed-weight obstruction and the seven
   strict witness halfspaces, for exact
   `a=1,h=q2=k=nu=0,mu=2/3` and the frozen nine-generator support. It is not a
   moving-axis/load, nonzero-`q2`, other-support, all-face, full-fan,
   whole-double-root, D1, or JC2 theorem.

Write exactly one report and make no other repository edits:

```text
xmodel/max12-912-order3-d1-double-root-control2-la20-witness-review-grok-20260826.md
```

End with one token on its own line:
`LA20_WITNESS_CONFIRMED`, `LA20_WITNESS_REPAIRED`, or
`LA20_WITNESS_REJECTED`.
