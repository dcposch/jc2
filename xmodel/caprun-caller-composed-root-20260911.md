# Actual caller through single-call historical staging

ROOT, September11 2026. OFFLINE DOCUMENTARY TEST; caller FIRST still live,
no operational acceptance. Basis0d39df3c9fd69c939a8420c54d03228b9077777d.
Original reserve18:42/HARD18:45UTC; selected18:39.

## Observed result

The real supplied-input caller generated its HISTORICAL_TEST packet; the
same functions.exec call applied that packet to a fresh private directory
and independently SHA256-read all14 files. Actual compound elapsed365ms,
78889candidate bytes. Every suffix/hash equals the earlier historical
EXERCISE; this includes complete SUMMARY.json. No installation, actual host
observation, holder, release, input-shell or scientific execution occurred.

Retained documentary directory:
`box/caprun-stage-exercise-20260911.pf3eoQkl/`.
NEVER install or execute these historical files. Candidate status remains
CANDIDATE_NOT_INSTALLED_NOT_RELEASED. No file was overwritten or deleted.
The original mktemp directory is private; ROOT introduced no concurrent writer.

Owned `box/caprun-caller-composed-root-20260911/RECIPE.js` records the exact
orchestrator body. It reuses the prior tested staging flow with the real six-
argument caller, supplied historical pin/observation/decision files, explicit
HISTORICAL_TEST context, and an exact14-name output check. No general runner,
observer or serializer was added. The bound helper/source inputs are unchanged.

An initial pre-execution equality check found exactly one extra final LF
introduced when writing the recipe. That check stopped before creating a
candidate directory, running the caller or applying a patch. ROOT compared
the entire readback against the intended recipe plus that LF and dispatched
the exact file body inside an async closure. No source correction or failed
candidate directory was retried. The measured365ms covers the subsequent
mktemp/caller/parse/application/hash flow only, not prior preparation or review.

## Evidence and interpretation

Before execution all25 producer input/owned pins matched the completed caller
custody. ROOT had read the caller/test/contract/fixtures/report WHOLE and
replayed its six documentary methods, including standalone CLI, in the prior
intake. This test adds actual compound staging with that caller, not another
claim of full startup. The exact recipe was read WHOLE before dispatch.
`EXERCISE.json` records all14 hashes, the elapsed time and comparison scope.
Its historical identities/prefix-in-content remain documentary, never current
authority. Final input/output posthashes and record WHOLE are required before
closing this packet; neither passed hashes nor enabled fixture flags authorize
an installer or a FIFO write.

This removes the specific untested caller-to-staging boundary that contributed
to batch-c's ASCII failure. It does not show that ROOT can finish all fresh
physical/ACL/native-list/namespace/liveness observations, authenticated complete
readbacks, installation and separate release within120seconds. The accepted
runtime and scientific caps remain unchanged. Batch-b/c stay CLOSED; no new
actual batch or worker follows automatically. Fable's ongoing FIRST is on
the caller itself; this later ROOT exercise is not silently added to its inputs.

QUANTITY: all14 historical candidate byte strings across the actual one-call
boundary. CHEAPEST TEST completed: this one local stdlib documentary CLI and
apply_patch/readback,365ms observed. No optimized-mode or full deployment claim,
new canonical OPEN, mathematical result, extra review or scientific computation.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `3581`.
- Body SHA-256:
  `4280b45f5a2b90901569f1b57bc3b56eb83abd0b7f786effa8a2ef3ab1a69ff8`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
