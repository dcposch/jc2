# Research lane: THETA-STAGED — kill or pin the one-cusp horn

You are a bounded primary research lane executing the round's
top-ranked composite. Do not edit canonical ledgers, any charged file,
or inspect `jc2-lean`.

charged_input=xmodel/block-descent-a1-one-cusp-wild-valuation-structure-sol56-20260831.md
charged_input=xmodel/block-descent-a1-one-cusp-fibre-connectedness-sol56-20260831.md
charged_input=xmodel/block-descent-a1-fibre-connectedness-hostile-review-grok46-20260831.md
charged_input=xmodel/ideation-20260831T1033Z-synthesis.md

Your inputs are frozen read-only copies in `{{LANE_INPUTS}}`;
verify these SHA-256 hashes first and stop on mismatch:

```text
2710e90fdcbf1c2c9f61d6a606cf1e6553de5de65e4899367005433bc4f4e5f5  {{LANE_INPUTS}}/block-descent-a1-one-cusp-wild-valuation-structure-sol56-20260831.md
7a60ff245fc351a99a23815908909327dc6a9644f849a4d3b289079c98488474  {{LANE_INPUTS}}/block-descent-a1-one-cusp-fibre-connectedness-sol56-20260831.md
e87d0af3fb433edc5056ff651ce8623aae840bfb962d7d08eba6588b970123ca  {{LANE_INPUTS}}/block-descent-a1-fibre-connectedness-hostile-review-grok46-20260831.md
2793104bcf746eab690ca75a2aaf2ef105b64e7be0c1826eb1fa9ed51ba992ef  {{LANE_INPUTS}}/ideation-20260831T1033Z-synthesis.md
```

The partition table and its consistency clause are promoted; the
missing object is geometric. Staged protocol, fail closed at every
stage:

- Stage 0 (typing): declare the charged coordinate and ring map on
  `R=C[A,U,Z]/(U^2-A-A^2Z)`, the admissible/minimal SNC model class to
  which the promoted table applies, the unique horizontal vertex, and
  either a canonical-model clause or a transformation law proving
  `Theta_h=d_h-r_h-2` is presentation-independent. Any failure: report
  typed OPEN and stop.
- Stage 1 (prefilter): determine whether an independently licensed
  global puncture count `n` and parity data separate the four partition
  rows without a resolution. One page. If decisive, done.
- Stage 2 (model): construct ONE marked SNC compactification of
  `Spec R`, matched to the promoted completion identities
  (1.3)/(1.12)-(1.14); derive the divisor/intersection table; compute
  `d_h` and `r_h` two independent ways; then the single subtraction.

Interpretation: negative or odd `Theta_h` kills the charged one-cusp
configuration outright. Nonnegative even selects exactly one partition
row — never attainment. Record the Orevkov-remainder species as a side
observation WITHOUT identifying it with `Theta_h` (that bridge is
unproved analogy). No pole identity before the vertex class is typed;
do not iterate compactifications to force a vertex. Six hours hard
budget.

Computation rules: desk-scale exact reasoning only; you may fetch and
hash primary literature (record exact sources). Never run Singular,
msolve, any CAS, or any computation of uncertain duration or memory on
this machine.

Write one report and no other file:

```text
xmodel/round1033-theta-staged-sol56-20260831.md
```

Create the report file with a skeleton of section headers as your very
first action and append each completed section as you finish it. Keep
it under roughly 6,000 words. End its body with a single standalone
`<!-- BODY-END -->` line and write absolutely nothing after that line.
Include a `charge_basis` declaration only if you assert a genuinely new
exit price with a direct mathematical-source citation; otherwise omit
it entirely.
