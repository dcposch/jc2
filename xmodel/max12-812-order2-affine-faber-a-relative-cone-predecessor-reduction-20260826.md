# Delayed affine-Faber `A`: relative-cone predecessor reduction

Date: 2026-08-26

Status: **EXACT NAVIGATION INTERPRETATION OF PROMOTED FIXED-RAY THEOREM.**

## Custody and scope

This note reconciles the raw 371-term support calculation with the reviewed
valuative composition.  It introduces no larger theorem.

```text
e0a64e55ea21c41ee06e635740f7b8af375ce64855638cc44109afb8b384492a
  cases/max12_812_order2_affine_faber_a_full_k_multisupport_20260826/RESULT.md
c16d31a8089dca365014a710e40d33a6e5a0b4924053439ead03f584a4e45e7b
  cases/max12_812_order2_affine_faber_a_full_k_relative_cone_v2_20260826/RESULT.md
f7606a759e5cf6f9a52432baef1af2fb170ce04b65cbd573a96a0f17bbdc0619
  cases/max12_812_order2_affine_faber_a_full_k_relative_cone_v2_20260826/EVIDENCE.sha256
743742bac469af09ccb3b46b6366657fa62c4161e6b61a11361f63cbc5f2d0c8
  cases/max12_812_order2_affine_faber_a_full_k_relative_cone_v2_20260826/FREEZE.sha256
fef0524a91c239b9086df8377e0b1270430b2fb164915eb8286bf52f9508cf32
  xmodel/max12-812-order2-affine-faber-a-delayed-load-valuative-composition-theorem-20260826.md
a6d434b5342a32bcefc74b78fd4526eaf1a6556a73ddcc22b970c9958d248f18
  xmodel/max12-812-order2-affine-faber-a-delayed-load-valuative-composition-hostile-review-grok-20260826.md
0c4d242775572faef3578c42721c9d6cad483c3073dd9b98cffec5119783291c
  xmodel/max12-812-order2-affine-faber-a-delayed-load-valuative-composition-promotion-20260826.md
```

The only covered load ray is

```text
k10=Lambda^12*K10, k6=Lambda^8*K6, k2=Lambda^4*K2,
Lambda=sigma^3,
```

inside the weighted repeated-`A` formal neighbourhood on
`D(p*m*K10)`, with `v(a)>=5`, normal order 15, and
`q=min(v(U),v(V))>0` rational after ramification.

## Relative cone table

Let `I_<w` be the initial ideal generated by all complete source-row
coefficients of weight strictly below `w`.  The raw functional
`K=E*H3+H5` has 371 distinct coefficient groups.  Relative to the complete
predecessor ideals, its apparent walls reduce as follows.

| Kernel order | First controlling weight | Relative conclusion |
|---|---:|---|
| `0<q<6` | `30+2q<42` | The complete unloaded quadratic row face is empty on the two residue charts `D(x)` and `D(y)`. |
| `q=5` | `40` | The same row face is already empty; the four-term raw-`K` equality at weight 45 is unreachable modulo `I_<45`. |
| `q=6` | `42`, then `45` | Loads may tie the row face at 42, but every survivor is killed by the exact direct unit `[sigma^45]K=-E*lambda^3*M^3/16`. |
| `q>6` | at most `45` | The same direct unit kills the arc, independently of later quadratic, load, center, tangent, and complement terms. |
| `q=infinity` | `45` | The direct unit remains nonzero. |

Thus the raw-`K` direct-unit inequality `q>5` is stronger than needed only
after predecessor reduction.  The raw equality polynomial at `q=5`,

```text
lambda^2*(-3*M^2*X^2*a/8 + 3*E*Y^2*a/2
          -3*Y^2*X/16 + 3*M^2*R0*X/16),
```

is a valid support sentinel, not a new source component: it occurs five
weights after the empty grade-40 row face.  Likewise the schedule wall
`30+2q=45` at `q=15/2` never needs its broad timed-out emitter, because the
center-complete direct unit already controls every `q>=6`.

## Coefficient factors and unexplored slopes

The conclusion uses the registered units `E mod sigma=p`,
`M mod sigma=m`, and `lambda`, hence fails closed off `D(p*m)`.  The raw
load support contains, for each of `K2,K6,K10`, Pareto forms

```text
v(Ki)+v(a),  v(Ki)+v(lambda),
```

and the target forms `v(mu2)+v(a)` and `v(mu4)+v(a)`.  On other load
slopes one of these monomials can beat or tie `3*v(lambda)`.  No raw Newton
hull can remove it: one must first impose the appropriate affine load graph
and predecessor initial ideal, then recompute the row functional.  The
present fixed-ray reduction supplies a positive control for that procedure,
while the old center-truncated q6 package remains the mandatory negative
control.

## Explicit nonclaims

This note does not prove a raw-source-to-chart overlap, a total-Rees atlas,
coverage of `q=0` or `0<v(a)<5`, another load slope, any factor-degenerate
open (`p*m*K10=0`), terminal/Taylor closure, order two, maximum twelve, or
JC2.  It does not promote the 371-term support table itself as a Newton-fan
certificate.
