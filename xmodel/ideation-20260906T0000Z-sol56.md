# Blind ideation sol56 — non-truncation, descent breakers, and the K16 4-jet

## Custody and executive answer

I mechanically joined the twelve numbered `charged_input_<i>_basename` and
`charged_input_<i>_sha256` fields in the receipt with `awk` and piped the resulting
manifest to `sha256sum -c`. All twelve frozen files returned `OK`. I read no other
`ideation-*` submission, made no ledger edit, and did not use `jc2-lean`.

My strongest answers are:

1. Replace the raw Jacobian tail (depths 163/178) by attained characteristic
   equations. The \(T_2\) chain moves the Jacobian scalar \(j\) to defect **20/25**;
   the complete \(T_2+T_3\) chain forces \(j\) constant and deletes its chart rows.
   On a licensed family-C child, the corresponding \(d\)-weighted defect is **zero**.
2. Test the source-side exact differential, which Moh descent does not preserve.
   Its residue has weight 0; more strongly, the canonical \(F\)-differential forces
   each pole leader of \(G\) nonzero at weight \(m_P\). Exact two-place data then face
   a genus/divisor identity.
3. For K16 let \(u=4\eta+3bl_2\), \(r=B\eta\). Seek the bounded certificates
   \(u^3\in J_t+(r)\), \(r^2\in J_t+(u)\); with
   \(u^2+4T_2\in J_t\), they prove the required radical equality. Eliminate the
   \(L\)-tail first through its unit Toeplitz block.

One custody discrepancy remains. The prose says **38** family-C ES leaves
(packet.md:14-17; residual65-structure-fable5-20260905.md:120-125), but both the
declared counts and arrays in the charged roster sum mechanically to **36** on 20
rows. I price the 36 rostered leaves and record
\(\mathrm{OPEN[C\!-\!LEAF\!-\!CUSTODY\!-\!38\!-\!VS\!-\!36]}\); I do not invent two.
## The decision problem, typed once

Let (A=k[\mathbf a]) be a declared coefficient ring and (I\subset A) the
**unlocalized** ideal of necessary rows. If a chart requires nonzero coordinates
(z_1,\ldots,z_r), put (z=\prod z_i). Then

\[
V(I)\cap D(z)=\varnothing
\iff z\in\sqrt I
\iff z^N\in I\text{ for some }N
\iff I+(1-Uz)=(1).
\]

This is the precise meaning of “the distinguished coordinate is nilpotent.” A proper
localized ideal is only a **necessary-chart survivor**, never a Keller pair. Failure to
find (N) is OPEN, not nonnilpotence. If saturation is used, the ideal component,
ring, and positive/negative controls must be printed; an opaque `sat()` return is not
evidence (`FALLACY-v2.md:14-21`).

For the parent non-truncation charts the coordinates are: the Jacobian scalar (j),
the attained leader (\lambda), and the branch separation. The printed leader weights
are 143 for ((99,66)) and 153 for D108
(`char-degree-instrument-astra-r2-20260905.md:81-98,140-149`); the raw (j)-rows are
at 163 and 178. In the cover grading, the ((99,66),\delta=2) root separation
(\rho) has weight 4. For the (\delta=5/2) and D108 quadratic faces a root
separation has weight 5, while the polynomial chart coordinate (c=\beta^2) has
weight 10; this last weight is inferred directly from the printed homogeneous faces
\(\pi(\pi^2-c)\) and ((\pi-\mu)^2-c), not from a matching variable name
(`char-degree-instrument-astra-r2-20260905.md:147-151,174-181`).

## Q1 — a non-truncation instrument that is smaller than the augmented chart

### Proposal Q1.A — \(T_2\) characteristic-Jacobian defect

Let \(H=T_2^\psi(F,G)\). Proposition 2.2 supplies its **attained** degree and
nonzero scalar leader, while Proposition 3.1 supplies the complete target family
(char-degree-instrument-astra-r2-20260905.md:15-40,53-79):

\[
H=G^3-F^2+aG^2+bFG+cF+dG+e,\qquad
J(F,H)=(3G^2+2aG+bF+d)\,J(F,G).                              \tag{CJ}
\]

Writing \(j=J(F,G)\), the first layer carrying this distinguished coordinate is

\[
w(j)=(n+D_2-2)-2m=
\begin{cases}20&(99,66),\\25&(108,72).\end{cases}
\]

These are exactly \(n_2D_2-D_3\), not the raw constant-Jacobian depths 163/178
(char-degree-instrument-astra-r2-20260905.md:81-106). This does not contradict the
cone-vertex gate: that theorem concerns the old truncated bands; the attained
\(H\)-rows are new non-truncation data
(cone-vertex-gate-opus5-20260905.md:240-273,371-382).

For a completed monomial-J child, grade
\(x^by^a\) by \(w_d=a+b/d\). If \(J(P,Q)=j x^\ell\), the characteristic degree is
\(D\), and Proposition 3.2 gives derivative weight \(D+M\), then

\[
w_d(j)=n-M-1-\frac{\ell+1}{d}.                              \tag{CJC}
\]

All 20 rostered family-C rows satisfy \(d(n-M-1)=\ell+1\), so their \(j\)-coordinate
would occur at weight **0**, rather than old defects \(34,\ldots,207/2\). This is
conditional: the children are prefix-only, and Moh's remarks extend the face ODE but
do not by themselves extend Proposition 2.2 attainment to \(J=jx^\ell\)
(residual65-structure-fable5-20260905.md:120-125,290-302). Until the terminal label
and weighted attainment are proved, (CJC) is OPEN.

**Instrument and price.** On each of three coverage-valid parent charts, monic-pivot
the high \(H\)-coefficients and retain only variables reaching defect 20/25. Emit
(CJ), subtract every complete leading target, and test
\(z=j\lambda\rho\) or \(j\lambda c\) by \(z^N\in I\), \(N=1,2,3\), first over two
good primes and then by exact-\(\mathbb Q\) multiplier substitution. Require fewer
than 100 active variables before launch. Cost: 3 person-hours; three 32-core,
128-GiB jobs capped at 3 hours (288 core-hours), below 20 GiB remote scratch each
and 5 MiB returned. The old 2,500--2,931-generator attempts timed out
(char-degree-instrument-astra-r2-20260905.md:189-211).

**Decisive refuter.** An exact proper localized defect ideal with a raw point, or
proved quotient, on which \(z\ne0\) refutes this kill. Rank deficit, timeout, or
failure for \(N\le3\) is only OPEN.
### Proposal Q1.A2 — impose \(T_3\) and remove the Jacobian chart

This is the safer parent-level compressor. Theorem A attains
\(D_3=145,227\) with nonzero leaders \(\lambda_3\), and the charged arithmetic has
\(M_3=97,106=n-2\)
(char-degree-instrument-astra-r2-20260905.md:42-51,70-79). Moh Proposition 3.2,
printed pp.159-160, gives for \(B_3=(\partial T_3^\psi/\partial G)(F,G)\)

\[
\deg B_3=D_3+M_3=242,\ 333.
\]

But \(\deg J(F,T_3^\psi(F,G))\le n+D_3-2=242,\ 333\), while the chain rule says
\(J(F,T_3^\psi)=B_3J(F,G)\). Hence a nonzero \(J(F,G)\) has degree zero.
Theorem B excludes \(J\equiv0\) once the actual source faces and attained \(T_2\)
are present (char-degree-instrument-astra-r2-20260905.md:108-134). Therefore every
point of a **compatible canonical \(T_2+T_3\) chart** already has nonzero constant
Jacobian: no raw Jacobian rows and no \(j\)-localizer are needed.

The distinguished coordinate is \(\lambda_3\), the scalar leader at actual degree
**145/227**, reached from \(T_2^{\,n_2}\) at recursive defect
**\(n_2D_2-D_3=20/25\)**. Build \(T_3\) by the monic approximate-root recursion,
retain every permitted target coefficient, subtract the whole leading target, and use
\(1-Z_3\lambda_3\). A merely existential polynomial of degree 145/227 is insufficient:
the instrument must print the triangular map to the canonical \(T_2,T_3\).

**Price.** Two person-hours for the recursion/map audit; then three sparse
defect-20/25 coefficient systems, 16 cores, 64 GiB, two-hour cap each, under 10 GiB
remote scratch. **Refuter:** failure of the canonical triangular map, or an exact
proper compatible chart on which \(\lambda_3\ne0\) but the Proposition-3.2 derivative
degree drops, refutes the compressor. A timeout is OPEN.

### Proposal Q1.B — solve the exact face ODE in root space

**Mathematical statement.** At a typed first-separation leaf, let (p(X)) and
(q(X)) be the two actual face polynomials, (M=\deg p), (N=\deg q). The necessary
equation is

\[
M p q'-N p'q=\kappa p,\qquad \kappa\ne0,                 \tag{FO}
\]

with (q) squarefree, every root of (p) a root of (q), and the leaf's printed
multiplicity/orbit pattern. Moh proves exactly these conclusions in Proposition A.3,
printed p.205; the all-level license is the p.171 sentence sending conclusions (2)-(5)
to A.3/A.4 (`census-coverage-gate-opus5-20260905.md:222-244`). This is stronger than
the census test (M\ne Nv_i): after writing
(p=\prod(X-a_i)^{v_i}), (FO) requires the full polynomial

\[
M q'-Nq\sum_i\frac{v_i}{X-a_i}=\kappa,
\]

not just its evaluations ((M-Nv_i)q'(a_i)=\kappa).

**Instrument.** Use the roster's fixed partition and Galois-orbit form, normalize two
root parameters by the allowed affine action, and solve the coefficient ideal of (FO)
with localizers for (\kappa), the face discriminant, and the first-separation
coefficient (s_\rho). The distinguished (s_\rho) has rational contact weight
(\rho=P/Q), or integer weight (P) after the declared cover (t=u^Q). A unit kills
that typed leaf without any receiver chart. A proper ideal merely says that this
necessary face survives.

The frozen roster contains 36 leaves but only 20 templates after grouping by
((u_s,\rho,Q,\lambda)); exact leaf JSON has 35 distinct records, with only R023/R024
identical. Batch by exact record first, then cache isomorphic normalized systems. The
same construction should be made for the three named ((99,66))/D108 split faces
using their audited actual targets, not their historical over-restricted faces
(`joint-chart-degenerate-fable5-20260905.md:256-298`).

**Price.** 2 person-hours to write and audit one root-space generator; 35 exact
systems, at most 12 root/scale variables each, two good primes plus exact-Q replay:
8 core-hours, 8 GiB RAM cap, under 100 MiB temporary output, under 2 MiB retained.

**Decisive refuter.** One exact localized point satisfying the *full* (FO), all orbit
and distinctness equations, and (s_\rho\kappa\ne0) refutes an ODE kill for that leaf.
Conversely a unit is charged only to that typed first-separation set; no physical place,
cover series, or sibling flag may be counted with it.

### Proposal Q1.C — the canonical pole leader and two-place genus test

**Mathematical statement.** Work over characteristic zero on the geometrically
connected generic fiber \(C:F=t\), after base change to \(\overline{k(t)}\), and let
\(\bar C\) be its smooth completion. Geometric connectedness is essential; for a
Keller \(F\) it follows from the standard closed-polynomial reduction: if
\(F=H(Q)\), then \(J(F,G)=H'(Q)J(Q,G)\in k^\times\) forces \(\deg H=1\).
On \(C\),

\[
\omega_F=\frac{dx}{F_y}=-\frac{dy}{F_x},\qquad dG=-j\omega_F .
\]

At a physical boundary place with minimal uniformizer \(u\), write
\(\omega_F=(A_Pu^{\kappa_P}+\cdots)du\), \(A_P\ne0\). Exactness gives a complete
local alternative:

\[
\begin{array}{c|c}
\kappa_P=-1 & \text{impossible (a Laurent derivative has zero residue)},\\
\kappa_P\le-2 & m_P=-\kappa_P-1,\quad
 [G]_{u^{-m_P}}=-jA_P/(\kappa_P+1)\ne0,\\
\kappa_P\ge0 & e_P=\operatorname{ord}_P(G-G(P))=\kappa_P+1.
\end{array}                                                    \tag{PD}
\]

Thus the distinguished coordinate is the Laurent pole leader
\(g_{P,-m_P}\), at integer weight **\(m_P=-\kappa_P-1\)**, not the face
separation \(c\) or \(\rho\). If
\(x=\alpha u^{-r}+\cdots\) and \(F_y=b_Pu^s+\cdots\), then
\(\kappa_P=-r-1-s\), \(A_P=-r\alpha/b_P\), and (PD) prints the leader
\(jr\alpha/[b_P(\kappa_P+1)]\). It is forced nonzero once the complete physical
\(F\)-tree fixes \(r,\alpha,s,b_P\).

There is also an exact global check. If every boundary place is a pole, then
\(G:C\to\mathbb A^1\) is finite étale. For degree \(d\) and \(r_\infty\) places
over infinity, Riemann--Hurwitz gives
\(2g-2=-d-r_\infty\), hence \(g=0,d=r_\infty=1\); the generic map has degree one,
so the birational Keller theorem finishes. More generally,

\[
2g-2=\sum_{Q\in D_0}(e_Q-1)-\sum_{P\in D_\infty}(m_P+1).       \tag{GD}
\]

With exactly two physical places, both poles are impossible. With one pole and one
finite place, \(e\le d=m\) and (GD) gives \(2g=e-m\), hence \(g=0,e=m\).
Any certified two-place tree with \(g>0\) or \(e\ne m\) is therefore impossible.

**Printed input and instrument.** Moh Proposition 4.5 gives at most two projective
directions, not two normalization places (PDF printed pp.169,172;
char-degree-instrument-astra-r2-20260905.md:70-79). Proposition 4.6 supplies face
polynomials/root containment (own-data-gate-opus5-20260905.md:238-253), and reduced
denominators give orbit sizes (census-coverage-gate-opus5-20260905.md:90-101).
Build POLE-DIVISOR-v1 from the *complete* physical Puiseux tree. For monic \(F\),
\(F_y(y_i)=\prod_{j\ne i}(y_i-y_j)\), so exact contacts and their leaders compute
\(s\) and \(b_P\); use minimal uniformizers, then check (PD), branch semigroups,
pairwise intersections, genus, and (GD). A localizer
\(1-Zg_{P,-m_P}\) decides the named nonnilpotence claim without \(G\)-jet or
characteristic charts.

**Price and refuter.** Once exact trees exist: 2 person-hours, below one CPU-hour,
100 MiB RAM, and 1 MiB retained. Acquiring exact leaves is the real price and may
return to the 199--4,889-variable nilpotence charts
(residual65-structure-fable5-20260905.md:43-48). A third physical place refutes only
the two-place shortcut; \(\kappa\ge0\) refutes only all-poles. An exact complete tree
with \(\kappa=-1\), or violating (GD), kills the row. An unresolved multiple-root
cluster is OPEN. In particular, local powers 54/126/64 are not pole orders, and no
top direction or cover flag is counted as a place.
## Q2 — conditions that genuinely break Prop. 6.3 invariance

### Proposal Q2.A — the parent Gelfand-Leray residue vector

**Mathematical statement.** On the generic smooth fiber (C:g=T), define

\[
\omega_g=\frac{dx}{g_y}=-\frac{dy}{g_x}.
\]

Since (dg=0) on (C) and (J(f,g)=j\ne0), one has
(df=j\omega_g). Hence (\omega_g) is exact and

\[
\operatorname{Res}_P(\omega_g)=0
\quad\text{for every physical }P\in\bar C\setminus C.       \tag{GL}
\]

This is uniform in all 65 source rows and uses the original Keller equation, not a
numerical shadow of it. It is **not descent-invariant**. Proposition 6.3 produces
(J(P,Q)=j'\gamma^\ell), so only
(\gamma^\ell\omega_Q=(j')^{-1}dP) is exact
(`residual65-structure-fable5-20260905.md:136-147,161-180`). For the explicit sanity
check (Q=\gamma\pi, P=\gamma^{\ell+1}/(\ell+1)), one has
(J(P,Q)=\gamma^\ell), while
(\omega_Q=d\gamma/\gamma) has residues (+1,-1); multiplication by
\(\gamma^\ell\) makes it exact. Thus this is a real logical break, not a hoped-for
failure of transported arithmetic.

**Instrument and coordinate.** At each separately certified infinity place expand
(dx/g_y) only through its (t^{-1}dt) coefficient. The distinguished coordinate is
the residue (R_P), of scaling weight **0**. Add (R_P=0) to the source face ideal;
if the existing face equations force (R_P\ne0), the row is killed. Recenter the
generic fiber symbolically: Moh printed p.194 warns that replacing (g) by (g+c)
changes order-zero face data, so an at-level coefficient cannot be copied between
fibers or confused with strict-below parting.

**Price.** Pilot R009/R050 plus the two ((99,66)) faces: 2 person-hours, 4 core-hours,
8 GiB, under 20 MiB. Then 41 coarse-family templates: 6 person-hours, 24 core-hours.
**Refuter:** an exact face point with all (GL) residues zero survives this condition;
a residue depending on an unproved identification of a flag with a place is discarded.

### Proposal Q2.B — exact contact at the two Xu-tight rows

R009 and R050 are the only margin-zero rows; their data and children are printed in
`roster.jsonl:9,50`, and the isolation of these two is
`residual65-structure-fable5-20260905.md:244-259`. Both have (u_s=1,v_s=3).
Moh Proposition 6.4 supplies only the floor \(\delta^*\ge3\), exactly the floor consumed
by the current Xu inequality (`census-coverage-gate-opus5-20260905.md:280-294`).

Equality must hold in **every** floor used by the Xu sandwich, so there are two
separate contact tests. For a simple level-2 minor packet the printed lower bound is
\(\rho_0=\delta_2+[d_2/(n-M_2)](1-\delta_2)\)
(census-coverage-gate-opus5-20260905.md:275-279). Hence:

* R009 has \(\rho_0=5/16+(64/44)(11/16)=21/16\). On \(t=s^{16}\), let
  \(\kappa_{009}=[s^{21}](\tau_1-\tau_2)\).
* R050 has \(\rho_0=1/4+(28/12)(3/4)=2\). On \(t=s^4\), let
  \(\kappa_{050}=[s^8](\tau_1-\tau_2)\).

Nonzero \(\kappa\) is equality; \(\kappa=0\) is a strict improvement and kills that
equality pattern. Independently, the principal-minor floor is 3; its equality
coordinate is the vector of \(t^3\) branch differences. The relevant integer cover
weights are therefore **21**, **8**, and **3**. Enumerate every full source-tree
pattern attaining all other bounds—do not multiply coordinates from alternative
patterns—and on each test the mandatory equality coordinates by explicit inverse
equations. The face arithmetic to reproduce first is \(P,Q,A=(48,33,16)\) for R009
and \((21,9,4)\) for R050.

**Price.** Two simple-root/Hensel systems through those weights: 4-6 person-hours,
12 core-hours, 16 GiB, under 30 MiB; abort above 96 active variables. **Refuter:** one
fully licensed equality pattern with a proper exact localized jet ideal. That permits
the floor on a necessary chart, not attainment by a polynomial pair.

### Proposal Q2.C — child source-support consistency, but only after completion

For a complete child, the non-invariant equation is the monomial-J coefficient itself:

\[
J(P,Q)=j x^\ell,\qquad
j\notin\sqrt{I_{\rm child}},\qquad
w(j)=n'+m'-2-\ell
\]

in the ordinary top-degree-defect grading (the physical monomial \(x^\ell\) itself has
weight \(\ell\)). The 65 defect values range from 21 to 102; R009 has 77 and R050
has 60, while both physical \(j x^\ell\) terms have \(\ell=1\). This is the receiver problem recorded as
199-4,889 unknowns (`residual65-structure-fable5-20260905.md:43-48,244-265`). Apply
Q1.A to replace these weights by characteristic defect, zero on the 20 completed
family-C shapes. For the current prefixes, however, the terminal (M'_{s'+1}) is not
known and the route is not licensed. Killing ES leaves and fixing the terminal pair are
prerequisites, not consequences (`residual65-structure-fable5-20260905.md:246-253`).

## Q3 — the K16 uniform quadratic

Put

\[
u=4\eta+3b l_2,\qquad r=B\eta,\qquad
T_2=3b^2w_3+18Bw_2-10\eta^2.
\]

The printed exact identity is (u^2+4T_2+24E_2=0); on (V(J_t)), (T_2=0)
iff (u=0). The weights are (w(u)=2t, w(T_2)=4t, w(r)=4t+1)
(`k16-tacnode-fable5-20260905.md:183-203,224-245`). The same source proves that the
second Hensel branch is an irreducible cubic algebraic function, that the infinity
branches are compatible, and that termination of the contact chain supplies no
relation on (r) (`k16-tacnode-fable5-20260905.md:109-142,147-181,254-281`). I do not
reuse any of those three failed mechanisms.

### Proposal Q3.A — two bounded syzygies after Toeplitz tail elimination

**Mathematical statement to prove.** Uniformly for terminal (t\ge3), in the declared
characteristic-zero ring,

\[
u^3\in J_t+(r),\qquad r^2\in J_t+(u).                       \tag{S}
\]

Then the elementary, Lean-checkable ideal lemma

\[
u^2+4T_2\in J_t, (S)
\Longrightarrow
\sqrt{J_t+(T_2)}=\sqrt{J_t+(u)}=\sqrt{J_t+(r)}
\]

proves exactly “(T_2) vanishes on every terminal datum iff (R).” The exponents are
evidence-driven but still conjectural: the frozen (t=3,4,5) calculations show the
minimum power of (r) modulo (J+(u)) is 1,2,2 and that of (u) modulo (J+(r))
is 3,3,3; the (r^2) memberships at 4 and 5 are labelled weight-vacuous, so they are
not a theorem (`k16-tacnode-fable5-20260905.md:234-245`).

Before Macaulay search, eliminate the (L)-jets without a chart. If
(D=\partial_YQ_P(x,L)) and (d_j=[x^j]D), the exact relation

\[
\frac{\partial F_k}{\partial l_m}=-d_{k-m},\qquad
d_{3N}=\frac{3}{4y^3}(1-4p)\ne0
\]

is printed at `k16-tacnode-fable5-20260905.md:85-96`. Therefore the block with
columns (l_2,\ldots,l_{N-1}) and rows
(F_{3N+2},\ldots,F_{4N-1}) is triangular with determinant
((-d_{3N})^{N-2}), after ordering from the tail. Audit that the terminal presentation
really contains these rows and that projection agrees with the Theorem-H ideal; then
search (S) in the projected P-jet ideal. If (l_N) is temporarily made variable, add
its normalization (l_N=1/y); otherwise do not count it as a column.

**Instrument.** Sparse weighted Macaulay membership at weights (6t) for (u^3)
and (8t+2) for (r^2), two split primes, rational reconstruction, then exact
multiplier verification. At (t=8) the target spaces have respectively 23,009 and
127,372 weighted monomials. Report the Hilbert function first so a socle-forced
membership is never advertised as structure.

**Price.** Toeplitz/image audit: 30 minutes, one workstation, under 8 GiB. The (t=8)
discovery: two 32-core, 128-GiB jobs, 3 hours each (192 core-hours; roughly six
instance-hours), under 30 GiB remote scratch and under 5 MiB returned. This is
consistent with the charged six-variable (t=8) baseline of 35.54 GiB and 7,752 s
(`k16-t8-gate-astra-20260905.md:288-310`). Allow 3 person-hours for exact replay and
writing the all-(t) recurrence.

**Decisive refuter.** A nonzero modular normal form at a good split prime refutes the
bounded certificate (S) at that (t); an exact point of (V(J_t)) with (u=0,r\ne0)
or (r=0,u\ne0) refutes the desired equivalence itself. A modular membership alone
does not prove characteristic zero.

### Proposal Q3.B — three counterexample charts as fast falsifiers

At fixed (t), decide

\[
K_f=J_t+(u,1-Zr),\quad
K_{r,B}=J_t+(B,1-Zu),\quad
K_{r,\eta}=J_t+(\eta,1-Zu).
\]

All three must be unit ideals. These use explicit Rabinowitsch equations, not an opaque
saturation. On (b\ne0), use the weighted action once to set (b=1), and on (u=0)
eliminate (l_2=-4\eta/(3b)). The (b=0) boundary is separate: there (u=4\eta)
and (T_2\equiv-4\eta^2\pmod J); only
(J_t+(b,B,1-Z\eta)) remains as a reverse counterexample chart. The tacnode proof
explicitly makes no claim on (b=0) (`k16-tacnode-fable5-20260905.md:205-207`).

**Price.** Four parallel 64-GiB lanes, 3-hour cap each (12 instance-hours), hashes and
small bases only returned. **Refuter:** any nonunit chart must yield an elimination
polynomial or exact/algebraic point and pass a perturbed-row negative control. Even all
fixed-(t) units remain evidence for, not proof of, the uniform statement.

## Ranked next 12 hours

1. **Lane T3/CJ20-25 — canonical characteristic compressor.** Hours 0-2: print and
   raw-check the canonical \(T_2,T_3\) triangular map and all target subtractions.
   If that map closes, use Proposition 3.2 and Theorem B and omit \(j\). Otherwise
   emit the defect-20/25 \(T_2\) fallback; proceed only below 100 active variables.
   Hours 2-8: two-prime tests, starting \((99,66),\delta=2\); hours 8-12: exact-Q
   replay or OPEN. One 32-core/128-GiB worker at a time; return below 5 MiB.
2. **Lane FO+GL — rostered split faces and the second place.** Hours 0-2: reconcile
   38-vs-36 and generate the 35 exact root-space ODE systems. Hours 2-5: solve (FO)
   at two primes and replay exact units/points. In parallel, compute contact
   coordinates of weights 21/8/3 and the residues for R009/R050. Hours 5-12: expand
   only to families whose physical-place audit closes. Resources: 8 cores/16 GiB;
   retained output below 5 MiB.
3. **Lane K16-S — Toeplitz projection and bounded syzygies.** First 30 minutes:
   verify row coverage, triangular determinant, and the map to Theorem-H at (t=3,4,5).
   Then run the (t=8) weight-(6t) and weight-(8t+2) Macaulay searches at two
   split primes, with one counterexample chart in parallel. Resources: two
   32-core/128-GiB workers for at most 3 hours each, followed by exact multiplier
   checking. Stop as soon as a good-prime nonmembership refutes either bounded syzygy.

## Fallacy-v2 disposition

Every proposed unit is attached to a declared localized ideal; every proper point is
only a necessary-chart point. The 36 leaves are typed first-separation records and are
not counted as physical places. R009/R050 use a strict improvement over a floor and do
not assert the next contact value. Family-C child rows remain conditional until the ES
alternative is removed and a terminal child label is proved. Parent, child, cover, and
physical-place coordinates have separate names. No new exit price is asserted in this
ideation report, so no `charge_basis=` line is licensed. Unproved generalized
attainment, missing place reconstruction, failed rational reconstruction, and all
timeouts remain typed OPEN.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `24460`.
- Body SHA-256:
  `9f760760ec3cc503033a79acc61a4cd3b0a8cf045f277e367553337a2cfc14c6`.
- Frozen basis: `de78ca950def644a22c1bf5b6c877be8d043d0de`.
