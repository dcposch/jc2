cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
The gate is complete. Report written to `xmodel/k16-boundary-product-gate-fable5-20260906.md` (11,826 bytes, one standalone BODY-END marker, divert status CLEAN, no charge-basis line).

**Verdict: all four claims CONFIRMED at their stated scope. No REFUTED item. The product target stays OPEN and nothing is promoted.**

- **Custody.** All six receipt hashes verified mechanically against the charged-inputs list and the run receipt. The producer report hash matches the prompt. The producer checker replays with 11 PASS in normal mode and is unmodified. Under `-O` its bare asserts are erased and it still prints PASS, so its replay is not optimization-safe. My 69 reviewer controls use explicit exceptions, verified to fire under `-O`, and live in `box/k16-boundary-product-gate-fable5-20260906/`.

- **Claim 1 (pole lemma).** Every valuation cell replayed with actual Laurent leaders. Uniqueness is strict everywhere because `2w+1` is odd and `4a` is even. The origin coefficient `B(eta − W'(0) − 3c²/4)` was confirmed with a general jet, independent of the regular part of A. Both negative controls are genuine whole-equation solutions. Characteristic zero is used once, for the derivative leader.

- **Claim 2 (normalization).** Scaling identity verified with actual generic polynomials at m = 4, 5. Converse cover reconstructed explicitly with the sign choice; all constants nonzero for integer m ≥ 4 on both factors.

- **Claim 3 (quadratic model).** Both algebra maps well defined and mutually inverse with an actual degree-2 A. Ramification identities confirmed in both directions, and the ramified root is realised by an actual A, so it cannot be dropped.

- **Claim 4 (norm + DF).** Necessity, converse, constants, multiplicities and the prime-label convention all check. The root-swap control is correct at its limited scope. One scope remark: the leading ratio is forced by the top of `WJ = R` up to the d-sign, so it is automatic in the union over both factors.

- **History checksum.** The pole lemma is new relative to the three named reports and answers Xempty §7.2's explicit request. The quadratic model is the even-part reduction of Xempty's quartic b=0 map; only the sigma-norm structure is new. Neither is an obstruction.

- **Reviewer meta-fact.** (DF) is sigma-anti-invariant, so the t-model carries exactly the x-line residual plus divisibility and adds no independent equation. No follow-on discriminator is proposed. This is saved as a memory to keep later lanes from re-deriving or over-promoting the model.
DIVERT path=xmodel/k16-boundary-product-gate-fable5-20260906.md status=CLEAN body_bytes=11826 body_sha256=d40a0aefde728d22f0b4e38aaef908dd2c59408b3e0fee1631ca17b35d5e1130
