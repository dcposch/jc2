# Promotion: actual-total T-rs chart rho-unit lemma

Date: 2026-08-26

Status: **PROMOTED AT THE NARROW CHART SCOPE BELOW.**

## Theorem

On the actual total-Rees `T-rs` chart

```text
cs=rs*qcs,  c0=rs*qc0,  c1=rs*qc1,
```

let `E` be generated by the four hash-pinned exact-Q V9 rows

```text
P1   = Tg10_1/rs,
P2   = Tg10_2/rs,
P3   = Tg10_3/rs,
P126 = Tg12_6/rs^2,
```

and let `J=E:rs^infinity`.  On `D(k)`, put

```text
V = 8*rho^2*qcs^2+3,
U = 1+32*rho^2*qcs^2*V.
```

The exact identity

```text
35*rs^2*k*U
 = 32768*P126 + 4096*P2 + 8192*qcs*P3
   + 12288*rho^2*qcs*P1
```

puts `k*U` in `J`.  Therefore `U` is zero in the localized quotient on
`D(k)`, and

```text
rho^(-1) = -32*rho*qcs^2*(8*rho^2*qcs^2+3).
```

Equivalently, `J+(rho)` contains `k` and
`J+(rho,1-v*k)` is the unit ideal.

## Evidence and review

- producer report SHA-256:
  `3e1dd8fe17efe7c124406dd63ce4f6ad89c1382c2d97019ec375a2696d39c954`;
- exact-Q result SHA-256:
  `f30365d06e64bd94fbea17c9b1d5a0ab4ee364d883c55bbeb71fb863d342a905`;
- independent F65521 encoding-control result SHA-256:
  `8a9962dfe4e610e186c3d493faece07fc1db53260bfff256c5f32308988edbe3`;
- common exact certificate artifact SHA-256:
  `890aede0abfef6fae6a574e44fa0883415cd8943b2b6d81905f65ff6374d9e28`;
- hostile-review SHA-256:
  `b32de58cbfab4d4ee23068829054f6aa255e2c2b6afa03f9cb1021a0386c65e6`,
  verdict `CONFIRMED`.

The reviewer independently expanded the identities over Q, checked the
`rs`-valuations `(1,1,1,2)`, inspected saturation/localization encodings,
rehash-checked both harvests, and treated the finite-field lane only as a
software control.

## Provenance and scope firewall

The review hash-checks and algebraically inspects the four V9 row files but
does not independently re-extract them from the complete literal total
emitter.  Promotion therefore treats their frozen total-source provenance as
an explicit upstream dependency.  Because the four rows are a subset of the
complete grade-12 prefix, their rho-unit conclusion persists after adjoining
the remaining correctly sourced prefix rows.

This closes only the actual-total `T-rs` chart on `D(k)`.  It does not close
`T-cs`, `c0`, `c1`, `a0`, `a1`, the closed receiver, any overlap/gluing
obligation, `k=0`, global Gate T, order two, maximum twelve, or JC2.
