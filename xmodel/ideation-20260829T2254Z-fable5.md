# Blind whole-portfolio ideation — Fable 5 — round `20260829T2254Z`

Researcher: Fable 5 (equal standing, blind).
Frozen pushed basis: `0f7ee003be45ee40d51d4048897cdacf63821172` (verified
`git rev-parse HEAD`).
State packet `xmodel/ideation-20260829T2254Z-state.md` verified, full SHA-256
`d1cc6a649b120253675c1694a2afc4b0c929393cd11f66c99f2635b49185236a`.
All seven charged files recomputed and matched:
`APPROACHES.md 91e9790d…`, `AUDIT.md 4a96be4e…`, `COORDINATION.md f947045b…`,
`PROGRESS.md b8e57d44…`, `ladder/REDUCTION.md 7f901db6…`, `notes.md 87c3821a…`,
`xmodel/ideation-20260829T1808Z-synthesis.md 76f068f8…`.
Charged producer `xmodel/k00-ram-e2m1-allfaces-uniformity-audit-sol56-20260829.md`
recomputed `64eaafe3fe5aaa9238a338f2da045e302d9a239ac9c29312e39d5c545893d42c`
before use. No peer submission, prompt, log, or run record from this round was
read. No web. No `jc2-lean` access. All local checks in this report are greps
and file reads plus hash recomputation; no CAS was launched.

Typing discipline used throughout: a finite jet is not a formal arc; a formal
arc is not an algebraic germ, a map, occurrence, attainment, or JC2; equality
of sets is not equality of schemes; `G2-PSC` and `G2-BD` are distinct; B and S
are distinct. No exit price is asserted anywhere in this report, so no
`charge_basis` line appears.

---

## 1. Disposition over all 46 numbered avenues

Baseline is the `1808Z` synthesis (no rerank) plus the four post-cutoff
overlays through 22:44Z. My disposition: **1 raise, 45 unchanged, 0 lower,
0 reopen.**

| Disposition | Avenues | Reason class |
|---|---|---|
| **RAISE** | **4** (formal-germ certification + algebraization) | See below. |
| unchanged (tried core; principal) | 1, 2, 3, 36 | Advanced in content this round but not in rank: 2 stays the principal proof route (QCS retype sharpens its bridge without supplying it); 36 stays the protected falsifier, now narrowed to `R4-00` + ramified atlas with the first honest survivor fixture. Narrowing and sharpening are not reranks. |
| unchanged (partial/live secondary) | 7, 9, 14, 19, 21, 25, 26, 27, 28, 29, 31, 32 | No new evidence this round touches their stuck points. 26 remains the consensus untried td-ceiling pick; nothing this round executed it. |
| unchanged (low/no advocate) | 5, 6, 8, 10, 12, 13, 15, 16, 17, 20, 22, 23, 24, 30, 33, 34, 35, 37, 38, 39, 40, 42, 43, 45, 46 | No delta. For 33 I attempted a reopen and it self-refuted (§3.3); the 0719Z stop stands verbatim. |
| unchanged (closed/refuted) | 11, 18, 41, 44 | Already closed; nothing reopens them. |

**Why raise Avenue 4, and only Avenue 4.** Two facts changed together this
round. (a) For the first time the campaign holds an exact, sealed,
full-source surviving jet: the `e=3,m=1` rank-one fixture over `Q(i)`
(`xmodel/k00-ram-e2m1-allfaces-uniformity-audit-sol56-20260829.md:255`,
`S=8i*tau-4*tau^2, T=tau`, survives G0–G8, K10 first arrives G9, and the
`-4 -> -3` mutation control breaks G8). Every previous K00 cell died, so
Avenue 4's algebraization half never had a live client on this route.
(b) The campaign already banked the effective pointwise decision tool it
needs: Tougeron's implicit function theorem with the `2e+1` constant
(`ladder/DEPTH-STAB.md:51`, toy demonstration
`cases/depthstab_check.py:89-140`). The old reserve condition — "Greenberg
remains reserve until a complete integral finite-support scheme and effective
bound exist" (PROGRESS.md, 2026-08-25 overlay; APPROACHES.md:4252) — was
stated for the char-p AS boxes; the K00 ramified cell **is** a frozen
finite-support literal system over an equicharacteristic-zero DVR, so the
pointwise form's hypotheses are now available. This is a raise of execution
priority for Avenue 4's algebraization sub-lane, not a claim that any arc,
germ, or map exists. Card 1 (§8) is the instantiation.

---

## 2. Reranked global gaps

### Proof-side gaps (rank order)

1. **P1 — Global actual selector / coverage.** Still no reviewed arrow from
   an arbitrary minimal Keller counterexample to a bounded
   td/type/entry/U1 client, no all-td coverage theorem, and no actual-map
   source packet for the conditional td12 laboratory. Unchanged rank 1; the
   `QCS-GLUE-OR-KILL` token-ceiling non-verdict changes operations (§7), not
   mathematics.
2. **P2a — QCS strictness at collisions (`STRICT-COLLIDE-POLY`).** The
   reviewed margin `Xi = E_gen-(s-1) = d-s-sum_i b_i` is exact; atypicality
   forces a collision but not positive excess; the type-(2,3) surrogate
   attains collision with zero excess and its natural ansatz forcibly
   contains `r^2 = r(0)^2 + 3s^2/2`, not a square in `C(s)`
   (`xmodel/weight-at-atypical-analytic-surrogate-coordinator-integration-sol56-20260829.md:95-111`).
   Polynomial/rational origin is exactly the unconsumed hypothesis. Card 2.
3. **P2b — the count `#atypical >= s-1`.** Named as needed in three files
   (`APPROACHES.md:135-136`,
   `xmodel/pcb-generic-collision-surplus-coordinator-integration-sol56-20260829.md:173-174`,
   the Opus hostile review) and, per my grep sweep, **owned by no card and
   attached to no mechanism anywhere in the repository**. I am promoting
   this from a footnote to a ranked gap with a proposed mechanism (Card 3).
   Note P2a and P2b are jointly still weaker than QCS's consumers want:
   P2a gives `E_gen >= #atypical`, P2b gives the count.
4. **P3 — `G2-PSC` transport (hybrid) or a pure-Sigray source theorem.**
   Unchanged.
5. **P4 — absolute/cofinal td ceiling.** Unchanged; Avenue 26 remains the
   cheapest untried bypass and would shrink P1 from "select" to "cover a
   finite list".
6. **P5 — conditional td12 four-lane detour exclusion.** Parked behind named
   source objects; correctly so.

### Counterexample/falsification-side gaps (rank order)

1. **C1 — `R4-00` completion.** Rank-zero is triply reproduced empty at
   grade 18; R1+/R1-/R2 grade-19 unit packets are `PRODUCER_UNREVIEWED`
   under active Opus review. Cost to the round: zero new launches. The
   integer atlas is not promoted closed until that review lands.
2. **C2 — deciding the first honest resonance (`e=3,m=1`).** G9 is the
   first undecided equation. The new content of this report: run the
   Tougeron layer **first**, so the carry has a named stopping grade
   `2e+1` instead of open-ended grade chasing (Card 1).
3. **C3 — `(e,m)` uniformity.** The all-faces `e=2,m=1` corollary is under
   my own (Fable) hostile review in the background; the refuted
   coefficient-blind all-`e` induction means uniformity must route through
   the load calendar. The master resonance-calendar theorem (§5) is the
   structural attack; the state packet's `R subset J^2` plus load degrees
   K10-cubic/K6-quadratic/K2-linear is exactly the input it needs.
4. **C4 — source completeness / receiver / reachability.** Even a fully
   closed atlas (integer + ramified) covers one normalized support and
   declared unit opens; no theorem yet says a hypothetical counterexample
   must land a section here. This gap has no owner. It deserves one owner
   at specification intensity only (typed interface first, no compute).
5. **C5 — other supports, `C6=0`, other load faces/cones.** Unchanged.
6. **C6 — algebraization/realization chain.** Formal arc -> algebraic germ
   (Artin) -> polynomial/finite Taylor realization -> Keller/collision
   replay. Meaningful only after C2 produces an arc; typed now so a positive
   C2 outcome has a licensed successor instead of an improvised one.

---

## 3. New mechanism, new connection, and one self-refuted reopen

### 3.1 Genuinely new mechanism (for P2b): `ATYP-COUNT` via asymptotic critical values

**Claim of novelty, checked.** Greps for `Rabier`, `asymptotic critical`,
`Parusinski`, `Kurdyka` across the repository return only D-module
irregularity contexts (Malgrange index in the R7R1 audits) and one
Siersma–Tibăr constant-degree mention in `AUDIT.md:10249`; nothing attaches
asymptotic-critical-value (ACV) theory to the `#atypical >= s-1` gap, and
that gap itself has no mechanism on file. Novelty: **NEW** as a mechanism;
the target statement is KNOWN (named, unowned).

**Mechanism.** For a primitive polynomial `f`, the atypical values are
contained in critical values plus asymptotic critical values, and ACVs are
detected by the Rabier/Malgrange condition at infinity along boundary
branches. In the Sigray frame the fixed fibre carries `s` pole vertices with
entry data `(a_i, b_i, nu_i)`. The proposed theorem shape: **for a Keller
pair, each pole vertex beyond the first forces at least one ACV event that
is separated from the others by the boundary tree, unless a quotient
collision merges them — and merges are exactly what QCS charges.** That is,
prove `#atypical >= s-1` directly from boundary-tree separation of ACVs,
with `J=1` entering through the promoted local identities
(`E_P = Delta_P/(alpha beta) = sum_{F in S_P} a_F b_F/nu_F` with the
attachment set `S_P` load-bearing — the 1940Z lesson that pole vertices are
not proper roots must be respected from the start).

**Why this is not Avenue 39 in costume.** Avenue 39's complaint is "no named
class"; ACV theory is not a cohomology class, it is a quantitative
metric/valuative condition at infinity with a finite computable witness set
per pair, and it consumes the same boundary-tree objects the campaign
already builds. It belongs inside Avenue 2's QCS ledger.

**Falsifier-first protocol.** Before any proof attempt, sweep non-Keller
controls: primitive polynomials with `s >= 2` pole vertices and few atypical
values (Broughton-type `x(xy-1)` and its relatives, plus the campaign's tame
products). If `#atypical < s-1` occurs without `J=1`, the sweep localizes
exactly where the Keller identity must enter; if it occurs **with** a
Jacobian-one-compatible local package, the target statement is dead and QCS
must route through the boundary-complex/suspension formulation instead.
Either outcome is information. Detailed card: §8 Card 3.

### 3.2 New connection between existing avenues (36 <-> 4): the Tougeron decision layer on K00

**Claim of novelty, checked.** Tougeron `2e+1` is banked in
`ladder/DEPTH-STAB.md:51` and demonstrated in `cases/depthstab_check.py`
(Avenue 4, D-series context). A generic "attempt Artin approximation" line
appears in `xmodel/ideation-20260827T1104Z-k00_closure_incidence.md:256`
(Card C, speculative, never executed), and Opus proposed
Greenberg/quantitative-Artin for **char-p AS boxes**
(`xmodel/ideation-20260826T2350Z-opus5.md:494`,
`…0827T0145Z-opus5.md:188`). No file computes a Jacobian contact order along
any K00 jet or names a stopping grade for any K00 cell. Novelty:
**KNOWN mechanism, NEW instantiation** — and the instantiation only became
possible this round, because before the `e=3,m=1` fixture there was no
surviving exact jet to decide.

**Content.** Each normalized K00 cell is a finite system of polynomial
series-equations (seven rows + load structure + targets) in finitely many
series unknowns over `K[[tau]]`; its solutions are sections, its truncations
are the campaign's jets. Tougeron/Newton: if a jet solves the system mod
`tau^{2e+1}`, where `e` is the `tau`-order of the appropriate Jacobian
minors along the jet, the jet extends to a **formal arc**. Consequences:

- The disclaimers "a finite jet is not an arc" become quantitative in both
  directions: emptiness at any grade kills arcs (already used), and survival
  past `2e+1` **produces** an arc (never yet usable — now testable).
- The grade-19 unit cofactors in the R1+/R1-/R2 packets are naturally typed
  as the leading terms of the Tougeron obstruction series of an
  overdetermined split; the decision layer unifies the unit-kill phenomenon
  and the lifting phenomenon as the two signs of one object.
- The unloaded radical theorem makes `e` computable in closed form along
  graph-supported jets: with `sqrt(r1..r7) = J` and `R subset J^2` in
  surface normal coordinates, the unloaded Jacobian block lies in `J`, so
  its order along the graph `D(S,T)` is explicit, and only the load terms
  (K10 cubic, K6 quadratic, K2 linear) correct it. Note the direction of
  the firewall: this consumes the producer's exact identities, which are
  under my own hostile review; Card 1 therefore carries
  `PROVISIONAL-CONSUMES-96d88f49` and must fall closed if that review
  fails.

Overdetermination is real (seven-plus rows, roughly six series unknowns), so
the card runs a typed three-branch protocol, not a naive minor formula:
(i) compute `r = rank_{K((tau))} DF` along the fixture and the order `e_r`
of its `r x r` minors; (ii) if the residual rows vanish along the lifted
family to all computed orders, certify a syzygy over the localized ring and
apply Tougeron to a minimal subsystem; (iii) otherwise the reduced
obstruction series is the resonance object itself — its first unit
coefficient is the kill grade, generalizing row-six units. Detailed card:
§8 Card 1.

### 3.3 Attempted reopen of Avenue 33, self-refuted (recorded to prevent re-derivation)

I attempted to convert the promoted polynomial action primitive
(`P dQ - x dy = dS`) into a `STRICT-COLLIDE-POLY` instrument: for polynomial
origin all periods of `omega = f dg - x dy` vanish, so if the surrogate's
zero-excess collision required a nonzero period over its Morse/tube cycle,
polynomial origin would force strict excess. This fails structurally:
`omega` is closed by the Jacobian identity itself, the surrogate's chart is
simply connected (its square root `R = sqrt(1+3s^2/2)` is analytic at
`s=0`, branch `R(0)=1`), and the Morse cycle bounds its thimble — so every
relevant period vanishes for the surrogate too, exactly the "J=1 in
costume" outcome the 0719Z feasibility stop recorded
(`xmodel/ideation-20260824T0719Z-feasibility.md:62`). Avenue 33 stays
closed; the rationality constraint on the **pair data itself** (already
isolated by the reviewed forced-branch identity) is the only surviving
polynomial-origin instrument, and Card 2 uses that directly.

---

## 4. Selector and QCS portfolio (prove / falsify / bypass)

**Target A: global selector (P1).**

- *Prove:* no new arrow is visible from this round's material; honest
  assessment is that P1 stays a specification-and-audit lane. Keep one
  owner on the actual-map selector interface with the `U1*(R)` no-go as the
  hard constraint (any proposed selector must fail on those formal
  configurations for a stated, checkable reason).
- *Falsify:* extend the `U1*(R)` family technique to whatever next
  interface a selector proposal names; that family is the standing
  falsifier factory.
- *Bypass 1 (preferred):* Avenue 26 td-ceiling experiment, unchanged spec
  (primitive-group enumeration with the Orevkov `(48,64)` td=9 negative
  control built in). It attacks P4, which converts P1 from selection to
  finite coverage. Cheap, GAP/Magma-class, still untried after two rounds
  of consensus.
- *Bypass 2:* pure-Sigray minimization (drop GGV as input), which owes its
  own source theorem but no `G2-PSC`; keep as the architectural fallback,
  no launch.
- *Operational:* the `QCS-GLUE-OR-KILL` retry must run under the sectioned
  output contract of §7; a second unbounded attempt would be malpractice.

**Target B: QCS strictness + count (P2a, P2b).**

- *Prove (P2a):* Card 2 — enlarge the reviewed forced-branch computation
  from the special ansatz `f=t^2+a(s), g=t^3+t r(s)` to the full type-(2,3)
  local normal form with `b(s)t^2` and `c(s)` terms, and decide whether the
  Jacobian identity still pins a non-square datum at every zero-excess
  collision. The integration names this the cheapest successor; the
  enlarged-ansatz sweep is the first concrete execution spec.
- *Falsify (P2a):* the same computation read in the other direction — any
  rational-origin zero-excess solution of the enlarged system is a
  source-compatible falsifier germ and stops `STRICT-COLLIDE-POLY`
  permanently.
- *Prove (P2b):* Card 3 (`ATYP-COUNT`).
- *Falsify (P2b):* Card 3's stage-A control sweep, run **before** the proof
  attempt.
- *Bypass (P2):* the boundary-complex/suspension formulation remains live
  but gated on one common labelled surface/boundary packet; the degree-six
  PALF comparison is dead pre-Kirby (A4 genus-two germ vs genus-one
  passport), and the A2-filling category is empty since a Keller `f` is a
  submersion. No unrestricted mapping-class or Kirby search is licensed;
  the gate is the packet, not compute.

---

## 5. Strongest next K00 attack (explicit comparison)

Comparing the seven named options:

1. **`R4-00` closure.** Highest certainty, zero marginal launch: rank-zero
   is triply reproduced; the three unit packets are in Opus review; base
   jobs on Box02/Box03 already separate base properness from the Jdet
   kill. Nothing to add. Do not treat the integer atlas as closed until
   review lands.
2. **All-order lifting (Tougeron layer).** Cheapest new information in the
   whole K00 program: desk-scale, minutes, and it retypes everything
   downstream. **Run first.** (Card 1 stage A.)
3. **`e=3,m=1,G9` carry.** The producer already specs the successor
   (`…allfaces…:344`, five-dimensional G8 lift fiber, fixture as mandatory
   old-pass control, `alpha=0` forbidden). But run it **after** stage A: if
   `2e+1 <= 8` the fixture already extends (the carry's outcome is known
   before it runs, and the honest question becomes arc typing and C6/C4);
   if `2e+1 > 8`, the carry inherits a stop condition (grind to `2e+1`,
   not open-ended) and every grade until then is a genuine test.
4. **General ramification/load calendars.** The master resonance-calendar
   theorem: linearize the loaded prolongation along the unloaded graph on
   the normal module (rank-2 by the radical theorem), diagonalize the
   constant part, and prove the resonance set is the arithmetic progression
   determined by `(e, m)` and load degrees (3,2,1), reducing each cell to
   finitely many explicit unit constants (the `5/65536` and
   `(5kappa/3)^9/2^19` phenomena become values of one function). This is
   the overlay's named "graded induction" target; the normal-module
   operator form is the new mechanism detail. Medium cost; start after
   stage A because stage A's Jacobian computation **is** the first column
   of this operator.
5. **Source completeness / reachability (C4).** Prerequisite for meaning,
   not for progress; one owner writes the typed interface (what exactly a
   closure-first arc on this support asserts about a hypothetical pair, and
   what Gate-T/receiver must supply). No compute.
6. **Algebraization (C6).** Typed successor only; consumes a positive
   stage-A outcome via Artin approximation and finite Taylor realization;
   pre-registered, not launched.
7. **Other supports/faces.** Hold; the current frontier is not starved.

**Verdict:** stage A of Card 1 now; all-faces review (mine) and Opus R1/R2
review continue in background; G9 carry launches with its stop condition
inherited from stage A; master-calendar build follows; C4 gets a
specification owner.

---

## 6. Mathematical-software idea (decisive experiment)

**`tougeron_check` — a reusable exact Jacobian-contact auditor.**
Input: a frozen cell system (rows as serialized polynomials over
`Q` or `Q(i)`), a stored jet (the fixture bytes from the sealed all-faces
packet), and a declared unknown list. Output, exact and desk-scale
(well under the 60 s / 1 GiB cap for the fixture: a ~7 x 12 matrix of
series through `tau^{~12}`):

1. `rank_{K((tau))} DF` along the jet, with the pivot certificate;
2. `e_r` = order of the `r x r` minor gcd along the jet;
3. the typed verdict: `LIFTS_TO_FORMAL_ARC` (jet length `>= 2e_r+1` and
   residual rows certified syzygous), `TARGET_GRADE=2e_r+1` (jet shorter:
   names the exact stopping grade for the carry), or
   `OBSTRUCTION_SERIES(first unit at grade g)` (the resonance object).

Regression gate: reproduce `cases/depthstab_check.py`'s toy
(`x0=t` solves mod `t^3`, `e=1`) byte-exactly, plus one planted negative
control (perturb the fixture's `-4` to `-3`, which the sealed packet proves
breaks G8 — the tool must not report a lift). This single tool serves the
`e=3` fixture, retro-types the R1/R2 unit cofactors, and becomes the first
column of the master-calendar operator. It is the round's decisive
experiment: every one of its three outcomes changes the K00 queue.

---

## 7. Campaign-systems check: UPGRADE

**Card `SECTIONED-OUTPUT-CONTRACT/v1`.** Evidence of need: the long Fable
`QCS-GLUE-OR-KILL` attempt died at the 64,000-output-token ceiling with **no
report at all** — an operational non-verdict that cost a full proof-side
lane, and the state packet itself demands "a sharply bounded output" for any
retry. Current runners deliver one monolithic body sealed at the end, so a
ceiling hit destroys all value.

**Upgrade.** Long-form deliverables are pre-registered as an ordered list of
sections with per-section token budgets; the runner writes each section to
disk as it completes, each with its own `<!-- SECTION-END k -->` marker and
byte count, and a final assembly step seals the whole body. A ceiling or
crash then yields a typed `TRUNCATED_AFTER_SECTION_k` receipt plus all
completed sections instead of nothing.

**Smallest measurable acceptance test.** One synthetic three-section task
run under a deliberately small cap chosen to die inside section 3: assert
(a) sections 1 and 2 exist on disk, parse, and carry correct byte counts;
(b) the receipt names section 3 as the truncation point; (c) an identical
run without the cap produces a byte-identical prefix for sections 1–2 and a
sealed full body. Pass/fail is mechanical.

Secondary observation, `NO_CHANGE`: the Grok HTTP 402 path already has the
correct typed handling in the state packet (`UNAVAILABLE_NO_MODEL_WORK`,
preserved failed receipt, no round blockage); a pre-launch balance probe
would duplicate that machinery for at most one saved round-trip. Evidence:
this round lost no verdict to the 402 itself, only the lane's model work,
which a probe would not restore.

---

## 8. Research cards (three) and ranked launch portfolio

### Card 1 — `K00-ARC-DECISION/v1`: Tougeron layer on the ramified fixture

- **Novelty:** mechanism KNOWN (`ladder/DEPTH-STAB.md:51`,
  `cases/depthstab_check.py`); K00 instantiation NEW (grep-verified: no
  Jacobian contact order computed along any K00 jet anywhere; the 0827
  Card C mention was generic and unexecuted). Connection 36<->4.
- **Dependencies:** promoted G7 theorem and sealed all-faces packet
  `64eaafe3…` (hash recomputed) for the fixture bytes; `R subset J^2` and
  radical identities from producer `96d88f49…` — **provisional consume**,
  fails closed if my active hostile review of that producer fails; no AWS.
- **Exact experiment:** stage A: run `tougeron_check` (§6) on the
  `e=3,m=1` fixture; emit `(r, e_r, verdict)`. Stage B (conditional): if
  `TARGET_GRADE > 8`, hand the stop condition to the G9 carry; if
  `LIFTS`, produce the sealed formal-arc certificate (Newton iteration
  transcript to a declared grade plus the syzygy certificate) and type it
  strictly: *formal arc on one normalized ramified cell; not an algebraic
  germ, not occurrence, not attainment, not a map, not JC2*.
- **Cheapest discriminator:** stage A itself (minutes, exact `Q(i)`).
- **Outcome meanings:** `LIFTS` = first attained formal source point of
  the ramified atlas; C6 successor arms (Artin + finite Taylor
  realization), and the counterexample side gets its first genuine
  positive-type object. `TARGET_GRADE=g` = the resonance decision is a
  bounded computation, not a grind; the carry inherits grade `g`.
  `OBSTRUCTION_SERIES` with a unit = the fixture's branch dies at a named
  grade; the all-`e` picture tightens without induction.
- **Stop condition:** stage A is a single bounded computation; stage B
  stops at the named grade or the certificate. No open-ended grades.
- **Expected information gain:** highest per CPU-second available this
  round; every branch reorders the K00 queue.
- **Compute class:** desk (<60 s, <1 GiB); no preregistration needed.
- **Ownership:** producer = any non-Sol model with a free lane (Opus after
  its R1/R2 review, else Fable after the all-faces review closes);
  falsifier = the mutation controls are built into the tool's regression
  gate; hostile review = different model, as always. **Provisional start
  licensed now** (uses only sealed inputs + new code).

### Card 2 — `STRICT-COLLIDE-POLY/v2`: enlarged-ansatz rationality obstruction

- **Novelty:** target KNOWN (named cheapest successor in the surrogate
  integration); the enlarged-ansatz execution spec is NEW; the avenue-33
  period instrument is recorded self-refuted (§3.3) so nobody re-derives
  it.
- **Dependencies:** promoted surrogate integration (basis `31777ce9…`
  lineage) and the QCS margin identities; nothing unreviewed.
- **Exact experiment:** in the boundary chart of the promoted control,
  take the full type-(2,3) interface ansatz
  `f = t^2 + a(s)`, `g = t^3 + b(s) t^2 + r(s) t + c(s)` with the same
  residual `(z^2, z^3+z)` normalization, impose `df^dg = s ds^dt` exactly,
  and eliminate to the constraint variety on `(a,b,r,c)`. Decide: does
  every zero-excess solution force a datum outside `C(s)` (the reviewed
  special case forces `r^2 = 1 + 3s^2/2`), or do `b,c` open a rational
  escape?
- **Cheapest discriminator:** the elimination is a small ODE/resultant
  system in one variable `s`; desk symbolic.
- **Outcome meanings:** *forced non-square in the enlarged class* = the
  first genuine polynomial-origin strictness theorem at this interface
  type; `E_gen >= #atypical` becomes consumable there, and the remaining
  QCS load moves wholly onto P2b. *Rational escape found* = an explicit
  source-compatible falsifier germ; `STRICT-COLLIDE-POLY` dies and the
  boundary-complex route becomes the only QCS bridge — a decisive negative.
  *Underdetermined* = the interface class needs a second normalization
  input; typed `OPEN`, no cap-filling.
- **Stop condition:** one elimination plus at most two normal-form
  branches; no family sweeps beyond the declared ansatz.
- **Expected information gain:** high; this is the named shortest live
  proof-side bridge, and both signs are decisive.
- **Compute class:** desk; AWS only if the elimination swells, in which
  case preregister (not launched from this report).
- **Ownership:** prove = one model; falsify = a different model reads the
  same variety for rational points first (the falsifier direction is
  cheaper and must not be owned by the prover); bypass owner = holds the
  boundary-complex gate (packet spec, no search).

### Card 3 — `ATYP-COUNT/v1`: `#atypical >= s-1` via asymptotic critical values

- **Novelty:** NEW mechanism (grep-verified §3.1) for a KNOWN, unowned gap
  (P2b).
- **Dependencies:** promoted Section-7 quotient identities and the pole
  vertex/proper-root attachment discipline (`S_P` load-bearing); standard
  ACV theory (Rabier/Malgrange condition, Parusiński–Tibăr) — external
  classical inputs to be cited precisely at proof time, no web needed for
  stage A.
- **Exact experiment (stage A, falsifier-first):** compute `s` (pole
  vertices of the fixed Sigray fibre) and the full atypical set for a
  small battery of explicit **non-Keller** primitive polynomials with
  `s >= 2` (Broughton `x(xy-1)` family, tame products, one GGV-corner
  toy). Test `#atypical >= s-1` on each. Stage B (only if stage A shows
  the inequality fails without `J=1` but no Jacobian-one-compatible
  violation appears): draft the boundary-tree separation lemma — distinct
  pole vertices force distinct ACV events unless a quotient collision
  merges them — with `J=1` entering through the promoted `E_P` identity.
- **Cheapest discriminator:** stage A is finite arithmetic on known
  examples; hours of desk work, no CAS risk.
- **Outcome meanings:** *inequality fails on a Jacobian-one-compatible
  local package* = P2b is dead as stated; QCS must be re-decomposed (this
  would be a major negative worth a round of its own). *Fails only without
  `J=1`* = the proof knows exactly which hypothesis carries the load;
  stage B proceeds. *Holds everywhere tested* = weak positive evidence
  and a sharpened conjecture with named witnesses.
- **Stop condition:** stage A is a fixed battery (<= 8 pairs); stage B
  stops at a complete lemma statement with typed gaps — no cap-filling by
  analogy, per FALLACY-v2.
- **Expected information gain:** medium-high; it is the only mechanism on
  file for P2b, and its falsifier is nearly free.
- **Compute class:** desk.
- **Ownership:** prove = one model; falsify = stage A can be run by a
  second model independently (it is pure computation); bypass = the
  boundary-complex owner (same as Card 2's bypass — if the suspension
  route lands, P2a/P2b both become moot).

### Ranked launch portfolio

1. **Card 1 stage A** (`tougeron_check` + fixture) — desk, immediate,
   provisional start licensed; reorders everything downstream.
2. **Systems `SECTIONED-OUTPUT-CONTRACT/v1`** (§7) — small, mechanical
   acceptance test; unblocks the licensed `QCS-GLUE-OR-KILL` retry.
3. **Card 2** (enlarged-ansatz sweep) — desk symbolic; the named shortest
   proof-side bridge, both outcomes decisive.
4. **Card 3 stage A** (control battery) — desk; nearly-free falsifier for
   the only unowned QCS half.
5. **Master resonance-calendar build** (§5 item 4) — after Card 1 stage A
   supplies the operator's first column; preregister AWS only on swell.
6. **`e=3,m=1,G9` carry** — launches with the stop condition inherited
   from Card 1 stage A (`TARGET_GRADE`), not open-ended.
7. **Continue, no new launch:** Opus R1/R2 review (C1), my all-faces and
   unloaded-radical reviews (C3), C4 interface specification (one owner,
   prose only), Avenue 26 td-ceiling experiment as the standing selector
   bypass when a lane frees.

Provisional descendants that may start without waiting for review: portfolio
items 1–4 (each consumes only promoted results, sealed hashed inputs, or new
code; item 1's radical-identity dependence is declared and fails closed).
Promotion of anything produced still requires different-model hostile
review and binding integration.

---

## 9. Boundary and custody statement

Written files: this report only (plus no `/tmp` scratch was ultimately
needed). No canonical file edited; no commit or push; no AWS or heavy CAS
launched; no web access; `jc2-lean` untouched; no peer material from this
round read. All quoted hashes were recomputed in this session before use.
Nothing in this report asserts an arc, a germ, occurrence, attainment, a
map, a counterexample, a new exit price, or JC2.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `30831`.
- Body SHA-256:
  `fe68a4dbe7e845ab24079f01be92d466703f6335a66277a34808eee32a70da27`.
- Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`.
