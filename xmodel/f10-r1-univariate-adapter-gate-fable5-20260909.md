# Gate: exact nine-row univariate adapter/checker (FIRST static gate, hostile manual read)

gate_of=xmodel/f10-r1-univariate-adapter-astra-20260909.md
producer_transaction=564a594588c00ce9756545aa01868c0cfcdb324b0b2db7e905e51e46c6d6d181
status=COMPLETE; verdict A CONFIRMED, B CONFIRMED (one named U-slot identification gap), C CONFIRMED (symbolic contract), D CONFIRMED (two un-charged constants named); no REFUTED; no GAP blocking; no new OPEN

## 0. Custody and scope

First action 19:41:28 UTC (lane directory created 19:41). Controlling stop is the earlier of 20:01:00 UTC and launch+20 min, i.e. 20:01:00 UTC, never reset. All sixteen charged inputs were hashed in the charged order at 19:42 over /tmp/jc2-lane.TQrMD7/inputs and every digest matched the ordered list before any read; the sha256sum output is reproduced unedited in box/f10-r1-univariate-adapter-gate-fable5-20260909/custody.txt. The own report was absent at 19:46 before the skeleton was written. Producer artifact.json (transaction 564a5945) records body_bytes 8961, body SHA 5f2dd9f0..., closed 2026-09-09T19:33:30Z, frozen basis 0d39df3c; the charged statements that all producer writers were IDLE at 19:34:22 and that root custody 2cb6b4c9 preceded this gate are taken as charged, not verified.

Read WHOLE: producer report (78 lines), adapter.py (90), checker.py (145), input_evidence.py (52), algebra.py (170), execution_gate.py (72), DISABLED.json, VALIDATION.md, READ-SCOPE.md, artifact.json, and the normalized backend producer report bc041570 (226 lines, for the interface). Read by keyword grep only at the matched lines cited below: the univariate cover gate 72a020db, the backend gate 0f653f53, the composition d91b3e38, its gate dc4f601e and the slice producer 6367aecf. Nothing else was opened: no provenance follower, ledger, peer, corpus, live file, coefficient artifact or protected path. ZERO mathematical subprocess, CAS, script, import, compile, test or arithmetic of any size; every check below is a manual read of the charged text. No web, network, AWS, SSH, proc or agent access.

Accepted and consumed, not rerun: theorem 17j (composition) and 17l (c-unit univariate cover, gate 72a020db), the exact arithmetic algebra.py 7d565299 and helper execution_gate.py cbfe55ff (byte-identical to the backend copies by the charged hashes), and the root 17n QUALIFIED STATIC acceptance of the frozen backend with its five qualifications. This gate accepts nothing about execution; all code is DISABLED/UNEXECUTED.

Verdict summary: A CONFIRMED, B CONFIRMED with one named identification gap (U slot), C CONFIRMED as a symbolic contract, D CONFIRMED with two un-charged constants named. No REFUTED item. No dependent runtime exists.

## A. Faithful mapping of the entire normalized source

- Seven coefficients, full B. adapter.py:26-28 fixes e3=(3,0,0),(1,1,0),(0,0,1) and e4=(4,0,0),(2,1,0),(0,2,0),(1,0,1), exactly the exhaustive weight-3/4 lists of cover gate line 16; :27 rejects any other monomial shape, and extract (:21-25) keeps the whole v-exponent as a B element with x,y,z,s,S,t cleared. read() (:16-19) requires s,S,t exponents zero and inherits the width-7, v-degree<7 and canonical-rational checks of algebra.py:129-140. No factor of p is chosen: the modulus is the literal eight-coefficient list of algebra.modulus() (:15). CONFIRMED.
- gamma. C=a+H*theta+F*theta^2+theta^3 (:34), with H=a enforced at :32 and leading order [F,H,a,b] (:30) matching backend report line 66 (F=v/w, H=a=1/w, b=1/f5). es (:35-37) is the inverse series of C^2: es[0]=a^-2 using only the supplied proved ainv (checked a*ainv=1 at :33), and es[i]=-a^-2*sum_{j=1..min(i,6)} [theta^j]C^2 * es[i-j], the exact recurrence sum_{j=0..i} c_j e_{i-j}=0 for a degree-6 C^2; e_0..e_6 are exact, no truncation. integ (:39-42) is the term-wise antiderivative of (lambda1-lambda0*theta)*C^-2 through theta^7: coefficient of theta^{i-1} in the integrand is l1*e_{i-1}-l0*e_{i-2}, divided by i, with the i=1 case handled by the conditional at :41 (the expression is the second argument of r.sub, so precedence is right). [theta^7](C^2*integ) needs c_0..c_6 and integ_1..integ_7, all present; scaled by 21/2 (:43). This is literally gamma=(21/2)[theta^7]C^2 Int((-lambda0*theta+lambda1)/C^2) of cover gate line 40 and producer lines 22-23, with the sign lambda1-lambda0*theta as required. The only divisions are by ainv and by the rationals 1..7 and 2. CONFIRMED.
- Actual c*gamma=1. :44 checks cc*ci==1 in the entire B before any use. Consequence worth stating: because the inverse of a unit is unique, any route error in :34-43 (including a swapped lambda3 order, which is an interface constant of the un-charged generator) either fails loudly here or yields the same correct element; the artifact can never carry a wrong c_inverse. CONFIRMED.
- r=-(a+bT)/c. :45 rp=-ci*(a+b*T); b is never inverted. T is axis 0 and theta axis 5 (:13), so the univariate variable reuses the x slot after x=1 is imposed by specialize (:46-52: x^i dropped, y^j->T^j, z^k->rp^k). CONFIRMED.
- Nine indexed rows and windows. :53-57 require nine G slots, G[7]=Phi3, G[8]=Phi4 and the weights 7,6,5,8,7,6,5,3,4 (composition order, cover gate line 52). Rows :65-67: seven specialized low G, P4=d+e*T+f*T^2+g*r (:62, also checked equal to specialize(Phi4) at :63), P11=r*h1-ubar*h0 (:64, sign as cover gate line 52), ids and bounds 3,3,2,4,3,3,2,2,5 exactly as cover gate line 54. wire (:68-70) emits every slot 0..bound including zeros and rejects any T-degree above the bound or any leftover y,z,s,S,t exponent; zero, duplicate and degree-dropped rows stay indexed. h0,h1,ubar are specialized from H0,H1,maps['4'][0] after weight checks 9,8,2 (:58-60) and wired with bounds 4,4,1; r with 1; guard q=r*h0 with 5, both factors named (:75). Phi3 is checked to specialize to exact zero (:61) and recorded as the consumed slot (:76). No count of nonzero rows, finiteness, rank or ideal statement is emitted (:77). CONFIRMED.
- Bounds 3,3,2,4,3,3,2,2,5 and h0,h1<=4, U<=1, r<=1, q<=5 are the proved envelopes of cover gate line 54 (degree j+k<=floor(w/2) with r affine); the weight requires at :57,:59 make a false failure impossible. CONFIRMED.

## B. Independent checker

- No adapter import (checker.py:1-13); shared frozen algebra.py acknowledged (:2). Strict parser :21-35: list-of-triples, seven nonnegative int exponents, v<7, s=0, kind 'B' forces all six geometric exponents zero, kind 'parameter' forces s,S,t zero, canonical sorted-unique keys, string rationals with gcd 1 and nonzero numerator; nofloat (:15-19) rejects every float anywhere in both inputs before parsing. CONFIRMED.
- Phi reassembly. :55-61 rebuild Phi3, Phi4 from the seven reported coefficients and require equality with the parsed normalized Phi3/Phi4; independence is uniqueness of coefficients, not the adapter extractor. c_inverse: :62 requires c_inverse*c==1. r: :67-68 require r0=-ci*a, r1=-ci*b (no b division). CONFIRMED.
- Dense binomial substitution. specialize (:78-89) expands each normalized monomial x^i y^j z^k v^m as sum_h C(k,h) r0^{k-h} r1^h T^{j+h}, a different route from the producer's sparse composition; :85 enforces j+h<=bound, which the G weight checks (:94,:96) make satisfiable. dense (:63-65) requires exactly bound+1 slots, so an omitted zero slot is rejected. Rows :108-117: literal id list (no dedup), fixed degree_bound per row, seven low G, P4 from the reassembled Phi4, P11 from the verified r,h1,ubar,h0 via the dense product times (:71-74). h0,h1,ubar (:102-105) from H0,H1,U; Phi3 consumed (:106-107); guard set, variable, equation string, both factors and q=r*h0 (:118-120). CONFIRMED.
- Source-bound projections. :98-101 bind G[0..6] to the t^h S^i projections (1,1),(1,2),(1,3),(0,1)..(0,4) of the normalized Jhat and H0,H1 to (0,0),(1,0); with S=axis 4, t=axis 5 this matches ids E1/S1..E0/S4 and the weights 9-(h+i). Named units: L, R, w via 112*L*w=R, the f5 literal (:44-48), all four products (:50), leading F=v*winv, H=a=winv, b=f5inv (:51-52), literal complete modulus equal on both artifacts (:38-39). CONFIRMED.
- Zips: every zip in both files is preceded by an exact length require (adapter :53 for :57 and :74; checker :91 for :94, :109 for :111), or pairs literals of equal length. No truncation. Expected sides: every univariate object is rederived from the normalized artifact; the only literal expected sides are the accepted L, R, f5 and the shared COVER dict (see C). CONFIRMED.
- Identification gap (named, not a refutation): U is read at maps['4'][0] by both adapter (:58) and checker (:95) and is bound only by its weight 2 (:96). V0 and K2 also have weight 2 (backend gate line 97), and the layout of maps['4'] belongs to the un-charged backend generator, so a wrong slot would pass both files silently and corrupt P11 only. Smallest repair: bind U in checker.py to the normalized Pi wire as minus the tau^2, S^0 coefficient (backend report line 76: Pihat=z*tau-U*tau^2+S*tau^3), or bind it to the original u receipt. This is a static one-line addition; no code was modified here.

## C. Fourth pair-Bezout representatives and the cover contract

- Producer section 4 restates backend report lines 104-116: the pair-Bezout fourth left inverse differs from the Gram inverse by d=t*(Bcol,-Acol), so E'-E=-t*Phi4, Phi3/Phi4 are formed before that substitution and unchanged, and every later G/H representative differs by a Phi4 multiple. After (x,y,z)=(1,T,r(T)) a Phi4 multiple becomes a P4 multiple, so the nine-row ideal is the same; h0 may change by a P4 multiple, so q=r*h0 is the same element of the quotient and the localization is the same. This is a consequence of the accepted 17n backend statement, consumed not re-derived. CONFIRMED.
- Actual wire use: adapter.py:54,:58 read the supplied G, H0, H1, U and never a Gram literal; checker.py:90-105 bind those same wires to the supplied Jhat projections. No polynomial is asserted byte-identical to the Gram representatives (producer line 46). CONFIRMED.
- Cover contract. COVER (input_evidence.py:42-52) states base B[T,xi]/(rows,xi*q-1), q=r*h0, X^3=h0^2/r^7, y=X^2*T, z=X^3*r, s=h0/r^3, target H0=s^7, H1=U*s^5, z=s^2 unsquared, rank-three parameter cover, not source-map degree. These are exactly the maps of cover gate lines 58-64. The checker's check data['cover']==COVER (:121) compares one shared module constant with its own copy; it is a literal-identity test carrying no mathematical content, and the producer and VALIDATION group 8 correctly describe it as literal. The mathematics is the accepted 17l contract, and no physical-source power (degree 112/196) is expanded anywhere in the charged code. CONFIRMED as a symbolic contract, with the literal/mathematical distinction stated here.

## D. Authority, pins, receipt scope, validation plan

- Order. adapter.main (:79-88) and checker.main (:130-143) call the unchanged authorize() first, then verified_input, and only inside adapt()/verify() import algebra; execution_gate is the accepted helper. Mathematical imports occur after authority. CONFIRMED.
- Pins checked by input_evidence.verified_input (:13-40): root normalized_acceptance dict with status ROOT-ACCEPTED-GENUINE-CHECKER-RECEIPT, backend checker/generator hashes equal to the constants, normalized and receipt paths pinned in file_sha256 with matching digests, acceptance binding the same two digests, algebra.py pinned at 7d565299 (equals the charged copy), evidence helper self-pinned, normalized source_sha256=168bdfd3 (confirmed by backend gate line 14 as exact.json) and implementation_sha256=generator, receipt status, artifact and source binding, and the five scope fields 20/9/2/10/8. authorize (execution_gate.py:62-70) additionally rehashes every pinned file, requires the script, helper and runner pins, and for 'check' the argv[2] pin. checker.main pins the univariate artifact (:136-137) and binds implementation_sha256 to the pinned adapter.py (:138-139) and input_evidence to its own recomputation (:140). Both outputs open with mode 'x' (fresh). argv counts 5 and 6 match VALIDATION.md's two entrypoints. CONFIRMED.
- Un-charged constants (named, not refuted): NORMAL_GENERATOR d444d022 and NORMAL_CHECKER 091bbf35, the receipt status string and the five receipt field names appear in no charged input; they name the frozen 17n backend code, which was not charged here. A mismatch refuses before mathematics, so the failure direction is safe. DISABLED.json leaves normalized_acceptance null and enabled false, so authorize refuses at :19. A typed receipt is never accepted as execution evidence; the root-owned field is the documented cooperative trust boundary, with no cryptographic authentication claimed.
- Validation plan. VALIDATION.md's positive pair plus eight groups is coherent, but early-path refusals dominate: group 1 stops in verified_input, group 2 (modulus) at checker.py:39 by list inequality, group 7 (float) at :19 before any parsing, group 8 at :121 by literal identity. The minimal subset that reaches NEW semantics is: the genuine positive pair; group 4 (alter one of a..g, reaching :60-61, and alter c_inverse, reaching :62); group 5 (alter one row slot, reaching :117, and alter the r linear slot, reaching :68); group 6 (drop h0 from factors or alter one q slot, reaching :119-120); group 3 (omit a trailing zero slot, reaching :64). Every mutated univariate fixture must keep its implementation_sha256 and input_evidence fields and receive a fresh file_sha256 pin, or :137-140 refuse for stale binding rather than the intended reason. A named unit witness change (group 2 second half) reaches :46-50 and is semantic. CONFIRMED with this subset.

## E. Verdict, defects, smallest repairs

Verdict: the NEW adapter/checker pair is a faithful static implementation of the accepted 17l nine-row univariate interface over the entire B, with the c inverse computed by the accepted finite formula and verified by identity, all nine indexed rows and every dense slot retained, both guard factors kept, and an independent binomial read-back bound to the normalized Jhat. Static acceptance only: it permits ROOT to consider a separately registered validation; no execution, runtime readiness, artifact, degree, count or ideal decision follows.

Defects (none blocking, no code modified):
- B-gap: U identification at maps['4'][0] is bound only by weight. Smallest repair: one checker line binding U to minus the tau^2 S^0 coefficient of the normalized Pi wire.
- D-doc: the 'check' authority must also pin adapter.py (checker.py:139), which VALIDATION.md does not list. Smallest repair: one sentence in VALIDATION.md.
- Shared f5 literal now has a third copy (checker.py:48); the 17n qualification applies unchanged.

The 17n qualifications (unused c2 tail, full residual rows, gauges, shared f5, build-hash custody) and the located h3/h4 literals are consumed input qualifications, not repair tasks of this gate.

## OPEN(S) RAISED

None. The U-slot gap is a bounded one-line static repair inside the existing pending joint static review, with wall under one minute and no new identifier; the two un-charged constants resolve at the future registration. Own-only scan of this report found no exit-price assertion, so no charge_basis line is authored.

## COLLISIONS

status: EMPTY

- NONE. The own report and box were absent at 19:46 UTC before the skeleton write; only own files were written.

## Own read scope and completion

All sixteen ordered hashes matched at 19:42 before reading. Producer, the three new code files, both frozen copies, DISABLED.json, VALIDATION.md, READ-SCOPE.md, artifact.json and the backend producer report were read WHOLE; the other five reports by grep-matched lines only. Only date, ls, mkdir of the owned box, sha256sum, cat, grep, head, wc and the editor ran; no shell or Python file-write trick, no compile, import, test, arithmetic, CAS, coefficient or matrix creation, web, network, AWS, SSH, proc or agent. Skeleton without marker first; bounded writes; own WHOLE, raised-OPEN and collision checks precede the marker. No Seal and no charge_basis are authored.

<!-- BODY-END -->
