# Fable5.1 hostile gate: strict separated F10 complement

Fable5.1, September9,2026, invitation 10:14:47Z. Independent gate over fields of
characteristic zero. All four charged inputs were read WHOLE from the lane inputs
after checking the four pins in charged order (all matched; the separated proof's
body hash 4de52d41... over its first 19793 bytes also matched its artifact JSON).
Every arrow below was rederived by hand; ZERO mathematical subprocesses of any size.
No source theorem, source review, peer body, log, receipt, provenance path, protected
tree, fetch or AWS was used. The accepted 16l finite reference and the terminal
coalesced Fable gate supply the hypotheses; the separated proof is NEW and was
judged on its own arrows only.

Inputs (full-file SHA-256, charged order):
- f10-separated-cubic-consumer-astra-20260909.md a81364a115d5ec07b7229dd5283044ba10b0f9b0dfc40a4c97f38f8baf827eaa
- its .artifact.json 336c89ee56f297815e61a56cfa2ae6085dd7816efe2ba48f742b73b46cb07474
- f10-coalesced-resonance-reduction-coordinator-20260909.md d225258c605617037d4d74b2cdce9430ce1fc8ac49e81d01dc1b125d76ad0ee1
- f10-coalesced-resonance-gate-fable5-20260909.md 221a5d584c5a412c9b1ac8b51f6bb936e616108b40391a0cb0a50759465f38ff

## Summary verdicts

| Item | Verdict | First precise failure |
|---|---|---|
| A parent interface, budget (1), list (2), full earlier B, division, N' | CONFIRMED | none; one convention wording note |
| B every simple minimum: inverse, lattice, 4q+5 denominator | CONFIRMED | none |
| C one-plus-double: lambda in {1,2}, strict (9), Morse, both congruences, indices, Y^(16h+17) | CONFIRMED | none |
| D separated Morse: lambda=1, kappa in {1,2,3}, two-sheet balance, (13) | CONFIRMED | none |
| E entire B after regrouping, target 4T-kappa/2, strict margin (14) | CONFIRMED | none; one hypothesis wording note |
| F exhaustiveness and scope | CONFIRMED | none; scope exactly as stated |

The theorem gated: under the literal 16l hypotheses, a minimizing triple factor with
lambda_split<r (strict first separation) is contradictory for every q>=0. A pass
narrows the literal receiver to the accepted coalesced resonance; it excludes neither
F10, actual 112/196, any source family nor JC2, and I claim none of those.

## A. Parent interface, budget, first split, full earlier B, division, target N'

Interface. m=3q+4, n=5q+7, T=m+n-1=8q+10, N=7(m+n)-5=56q+72=7T+2: CONFIRMED
(7T+2=56q+70+2). 3n=15q+21=5m+1, gcd(m,n)=1 from 5m-3n=-1. The reference A_s=f_s(R)+F,
F!=0, j=ord_s F, deg F_j=7m-j, the budget j<=7L-Delta with Delta in {1,2/3,1/3}>0,
L=m-d/3>1 and d<=3m-4 are the accepted 16l statements, used as hypotheses only.
Bound (1). The minimizing triple line has ratio d/3; minimality gives the other triple
line multiplicity >=d and the simple line b multiplicity >=d/3, hence >=ceil(d/3) by
integrality; so 7m-j=deg F_j>=2d+ceil(d/3): CONFIRMED, ties included, no divisibility
of F_j by K assumed.
Chart hypotheses. R=zeta^3+alpha zeta+beta with alpha,beta in sE[[s]], F=sum c_l zeta^l,
integral orders >=j, ord c_d=j, ord c_l>j for l<d, W=B_s-R^n coefficientwise positive:
these are exactly the terminal gate's Section B statements (zeta-multiplicity d
preserved, no negative orders), CONFIRMED as hypotheses. r<=ord c_d/(3m-d)=j/(3L)<7/3.
List (2). lambda=min(ord alpha/2, ord beta/3) with ord alpha, ord beta integers >=1,
so lambda lies in (1/2)Z or (1/3)Z, is >=1/3, and lambda<r<7/3 leaves exactly
1/3,1/2,2/3,1,4/3,3/2,5/3,2: CONFIRMED. Infinite orders allowed, at least one finite.
First split. zeta=s^lambda Y, Rbar(0)=S=Y^3+A(X)Y+B(X)!=Y^3 (lambda is attained), wt Y=
h=7/3-lambda>0, wt Rbar=D=7-3lambda=3h. (4): lambda<r gives ord c_l+l lambda>3m lambda
for l<=d; for l>d, ord c_l>=j and j>(3m-d)lambda give the same; the c_d Y^d term sits
at j+d lambda and no other index feeds Y^d, so 3m lambda<mu<=j+d lambda and deg phi<=d:
CONFIRMED. Scalar terms a_i s^(7(m-i))R^i are later by (m-i)(7-3lambda)=(m-i)D>0, so
the A initial is exactly S^m, monic of degree 3m with no Y^(3m-1) term.
Full earlier B before division. B_s-zeta^(3n)=(R^n-zeta^(3n))+W has all coefficients
of positive order (alpha,beta,W positive), and R^n lives at orders >=3n lambda, so an
initial at nu<3n lambda comes from W alone, with top degree v, v lambda<nu, and top
coefficient h(X) of weight 7n-nu-v(7/3-lambda)>7n-7nu/(3lambda)>0. First bracket order
in (Y,X) is 3m lambda+nu against target N+lambda (one zeta-derivative: [ ]_(zeta,X)
=s^(-lambda)[ ]_(Y,X)); the target's leading term -c s^N X^(5/3) is Y-free and the
+cX^(-2/3)zeta term is later by lambda. Above the target: contradiction; at or below:
the Y^(3m+v-1) coefficient 3m h' vanishes (P_X=mS^(m-1)(A'Y+B') has degree <=3m-2), so
X h'=wt(h)h forces h=0. CONFIRMED, no G>=2j premise, scalar kernels retained.
Division. At 3n lambda Q is S^n plus lower degree, monic. Equality of (3(m+n)-1)lambda
with N needs lambda=(7m-1)/(3m)=7/3-1/(3m): 3lambda=7-1/m and 2lambda=14/3-2/(3m) are
non-integral (3m|2m-2 is impossible for m>=2), and this value exceeds 2 anyway; so the
bracket is later (contradiction) or earlier (commutes). Weighted Euler for P,Q plus
P_Y Q_X=P_X Q_Y gives nQP_Y=mPQ_Y, (Q^m/P^n)_Y=0, Q^m=P^n=S^(mn), unique factorization
and monicity give Q=S^n. Hence Bbar=s^(-3n lambda)B_s has Bbar-Rbar^n coefficientwise
strictly positive, orders in (1/e0)Z: (5) CONFIRMED, division legitimate.
Target N'. Order N+lambda-3(m+n)lambda=N-(3T+2)lambda=7T+2-(3T+2)lambda=DT+2-2lambda:
(6) CONFIRMED; DT>=10>2lambda-2 so N'>0; leading coefficient -cX^(5/3) on both lines
epsilon=+-1 since a^2=X^2 there; all later corrections are Y-multiples. (7):
J=mu-3m lambda<=j-3L lambda<=DL-Delta, and a minimizing root of phi relative to S has
delta'<=deg phi/3<=d/3, so J/(m-delta')<=J/L<D. CONFIRMED.
Wording note (non-load-bearing): "greater than N" in Section 3 compares
(3(m+n)-1)lambda with N in the (zeta,X) convention, equivalently 3(m+n)lambda with
N+lambda in (Y,X); the displayed equality value is the correct one.

## B. Every simple minimum after splitting

t=ord_root(phi) at a minimizing simple root satisfies t<=deg phi/3<=(3m-4)/3, so
t<=m-2 (three simple roots, or a simple root tying/beating a double root): CONFIRMED.
Inverse. S'(y0)!=0, so z=Rbar inverts by Hensel to Y=y0+z/S'(y0)+... over the field
containing y0 with coefficient orders in (1/e0)Z, no new ramification: CONFIRMED.
Fbar in z has orders >=J, order J at z^t, greater below t (multiplicity preserved by an
invertible transverse change at s=0). eta=min_(l<=t) ord Fbar_l/(m-l)<=J/(m-t)<D; for
t<l<m, (m-l)eta<(m-t)eta<=J<=ord Fbar_l, and l>=m terms sit at >=J+l eta>m eta; scalar
terms a_i s^((m-i)D)z^i are later by (m-i)(D-eta)>0. So P=Z^m+U, U!=0, deg U<=t<=m-2:
CONFIRMED. Bbar-z^n keeps positive orders after the substitution.
Target. [ ]_(Y,X)=z_Y[ ]_(z,X) with z_Y=S'(y0)+O(s^eta) at the balance, so the target
is at N' in (z,X), N'+eta in (Z,X), Z-free leading coefficient -cX^(5/3)/S'(y0);
returning shifts are later by eta. Earlier B initials die by (3) with weight
nD-nu-v(D-eta)>0 (Bbar has weight nD): CONFIRMED. At n eta the bracket (m+n)eta versus
N'+eta is later (contradiction), earlier (commutes, P=(Z+c)^m, c=0, U=0) or equal,
which is eta=N'/T=D+(2-2lambda)/T: (8) CONFIRMED.
lambda<=1: eta_*>=D contradicts eta<D. lambda>1: e0=3,2,3,1 and e0 D=9,5,6,1 are
integral; e0(2lambda-2)=2,2,4,2; T=2(4q+5); so e0 eta_* = integer - k/(4q+5) with
k=1,1,2,1, all coprime to the odd 4q+5: reduced denominator 4q+5>3q+4=m. The balance
is attained at some l0<=t with ord Fbar_(l0)=(m-l0)eta in (1/e0)Z, requiring
(4q+5)|(m-l0) with 0<m-l0<=m<4q+5: impossible. CONFIRMED. This covers the three-simple
case and every tie with the simple root in the one-plus-double case, including t=0.

## C. One-plus-double boundary and the second coalesced Morse case

lambda in {1,2}. A depressed cubic Y^3+AY+B with a double root has 4A^3+27B^2=0, and
A=0 or B=0 would force a triple root (excluded, S!=Y^3); so ord alpha=2lambda and
ord beta=3lambda are both attained integers, lambda is integral, and (2) leaves 1,2:
CONFIRMED. S=(Y-a)^2(Y+2a)=Y^3-3a^2Y+2a^3, a!=0.
Strict double minimum. Any tie with the simple root is Section 4/item B; so e/2 <
ord_(Y+2a)(phi), i.e. ord_(Y+2a)(phi)>=t+1 for e=2t and e=2t+1 alike, giving
deg phi>=3t+1 (even), 3t+2 (odd): (9) CONFIRMED and earned; with deg phi<=d<=3m-4
this gives t<=m-2 and e<=2m-3.
Morse chart. Rbar_Y=S'(Y)+O(s) with S'(a)=0, S''(a)=6a!=0, so Hensel gives Y_c in
K[[s]]; xi=Rbar(Y_c) has positive integral order (S(a)=0); Rbar-xi=(Y-Y_c)^2 u with
u(0)=3a, a unit, and omega=(Y-Y_c)u^(1/2) gives Rbar=xi+omega^2 over K(sqrt(3a)) with
integral s-orders (lambda integral): CONFIRMED, wt omega=D/2, wt xi=D. Fbar orders
>=J with first multiplicity e at J; Wbar positive; rho=min_(l<=e) ord Fbar_l/(2m-l)
<=J/(2m-e)=(1/2)J/(m-e/2)<D/2 by (7) with delta'=e/2. CONFIRMED.
Coalesced case ord xi>=2rho. omega=s^rho Om: Rbar^m gives (Om^2+b)^m, F gives U!=0 of
degree <=e<=2m-3 (l>e later since (2m-l)rho<(2m-e)rho<=J), scalars later by
(m-i)(D-2rho)>0; no Om^(2m-1) term. Bbar-omega^(2n)=((xi+omega^2)^n-omega^(2n))+Wbar
is coefficientwise positive, so (3) with unit transverse weight D/2 and weight
nD-nu-v(D/2-rho)>D(n-nu/(2rho))>0 kills earlier initials in all three target cases
(P_X has degree <=2m-2, the top bracket coefficient is 2m h'); the target
-cX^(5/3)/omega_Y has its omega-dependence later by rho. At 2n rho: later is a
contradiction, commuting gives P=W^m with W monic quadratic, W!=Om^2+b forces
deg U>=2(m-1)>2m-3 (the m-term cofactor sum is monic of degree 2(m-1) in char 0),
W=Om^2+b forces U=0; so (2(m+n)-1)rho=N', rho=N'/(2T+1): (10) CONFIRMED.
lambda=1: D=4, N'=4T, gcd(4T,2T+1)=gcd(2,2T+1)=1, denominator 2T+1=16q+21>2m=6q+8;
2rho=4-4/(2T+1) non-integral so ord xi>2rho, b=0; an A correction at index l<2m needs
(2m-l)rho integral, i.e. (2T+1)|(2m-l) with 0<2m-l<=2m<2T+1: impossible, U=0.
CONFIRMED.
lambda=2: D=1, N'=T-2, gcd(T-2,2T+1)=gcd(T-2,5) in {1,5}; 2rho=2(T-2)/(2T+1) lies in
(0,1), so b=0 in both subcases. gcd 1: (2T+1)|(2m-l) impossible as above. gcd 5:
T=8q+10=3q mod 5, so 3q=2 mod 5, q=4 mod 5, q=5h+4, m=15h+16, n=25h+27, T=40h+42,
rho=40(h+1)/(5(16h+17))=8(h+1)/(16h+17), reduced since gcd(h+1,16h+17)=1 and 16h+17
is odd: CONFIRMED. A indices: (16h+17)|(2m-l), 2m=30h+32, so l in {14h+15, 2m}
(32h+34>2m): CONFIRMED complete. B indices: (16h+17)|(2n-l), 2n=50h+54, so l in
{2h+3, 18h+20, 34h+37, 2n} (64h+68>2n): CONFIRMED complete; the xi^k omega^(2(n-k))
terms of Rbar^n are later since ord xi>2rho. Hence P=Om^(14h+15)P1, Q=Om^(2h+3)Q1 and
both P_Om Q_X and P_X Q_Om are divisible by Om^(16h+17), while at (10) the bracket
must equal the nonzero Om-free target coefficient: contradiction. Vanishing of an
allowed coefficient only raises the divisibility. CONFIRMED. No ODE from 16l is used.

## D. Second separated Morse regime

kappa=ord xi<2rho. lambda=2: kappa>=1>2rho since 2rho<D=1, impossible; so lambda=1,
D=4, N'=4T, kappa in {1,2,3} (kappa<2rho<4, integral): CONFIRMED.
Regrouping (11). omega^2=z-xi gives Fbar=U(z)+-a(1-z/xi)^(1/2)V(z), a^2=-xi, with
U_k=sum_(h>=k) c_(2h) binom(h,k)(-xi)^(h-k), V_k likewise from c_(2h+1); coefficient
orders >=J, s-adically convergent since kappa>0. e=2t: U_t=c_(2t)+(terms with xi
factors, order >=J+kappa) has order J and U_k, k<t, has order >J; e=2t+1: V_t has order
J. CONFIRMED. The square-root series has coefficients of order -k kappa and is
expanded only after z=s^eta Z with eta>kappa, where each k>=1 term is later than its
k=0 term by k(eta-kappa)>0: CONFIRMED.
Global balance. q_l=min(ord U_l, kappa/2+ord V_l); a cancellation of leading terms on
one sign doubles the other, so q_l is realized on a sheet. From kappa<2rho:
ord c_l+l kappa/2>m kappa for l<=e, and for l>e via ord c_l>=J>(2m-e)kappa/2; each
U_k, V_k contribution is such a term shifted by -k kappa, so q_l+l kappa>m kappa and
eta=min_(l<m) q_l/(m-l)>kappa: (12) CONFIRMED. eta<=J/(m-t) (even, q_t<=ord U_t=J) and
eta<=(J+kappa/2)/(m-t) (odd): CONFIRMED. For t<l<m: q_l>=J, and in the even case
(m-l)eta<(m-t)eta<=J, in the odd case (m-l)eta<=(m-t)eta-eta<=J+kappa/2-eta<J since
eta>kappa; so P_+-=Z^m+U_+-, deg U_+-<=t<=m-2, not both zero: CONFIRMED. Scalars are
later by (m-i)(4-eta)>0 once eta<4, which (13) supplies.
Original budget (13). lambda=1 makes (4) read J<=j+d-3m and (1) gives
J<=4m-d-ceil(d/3), decreasing in d; d>=deg phi>=3t+1 gives
J<=4m-(3t+1)-(t+1)=4(m-t)-2, and d>=3t+2 gives 4(m-t)-3: CONFIRMED from the original
factor count and (9), no new normal-form assumption. Hence eta<=4-2/(m-t) (even),
eta<=4-(3-kappa/2)/(m-t) (odd), both <4. CONFIRMED.

## E. Entire B after regrouping, target 4T-kappa/2, strict margin (14)

Positivity. Wbar=sum w_l omega^l has integral orders >=1, so its regrouped Ue_k,
Vo_k have orders >=1 and kappa/2+ord Vo_k>=3/2: CONFIRMED. The sheet series in z is
NOT coefficientwise positive before the balance (the (1-z/xi)^(1/2) tail has orders
-k kappa); the proof correctly never uses it there. At eta>kappa a k>=1 cross term of
index h has order [kappa/2+ord Vo_h+h eta]+k(eta-kappa), strictly above the k=0 term
+-a Vo_h z^h, which survives on at least one sign; so the GLOBAL earliest B order nu
is a k=0 order, and at nu only k=0 terms contribute on both sheets: CONFIRMED. On a
sheet where the top coefficient h_+- (degree v, v eta<nu by positivity of the k=0
orders) is nonzero, it is weight-homogeneous of weight 4n-nu-v(4-eta)>0, the A initial
is monic with deg U_+-<=m-2, and the Z^(m+v-1) bracket coefficient m h_+-' must vanish
in the commuting and target-equality cases, while the too-late case contradicts the
sheet's own target: CONFIRMED (the sheet evaluation is a differential ring map,
omega_z=1/(2omega) on the sheet, so the bracket identity restricts sheetwise).
Consequently every k=0 term has order >=n eta, and every cross term is then later than
n eta by k(eta-kappa); the B initial at n eta on each sheet is Z^n plus k=0 terms of
degree <n, monic: CONFIRMED.
Target. [ ]_(Z,X)=omega_Z[ ]_(omega,X) with omega_Z=s^eta/(2omega) of order
eta-kappa/2 and Z-free leading coefficient +-s^(eta-kappa/2)/(2a_1(0)); the
(omega,X) target is -cs^(N')X^(5/3)/omega_Y with omega-dependence later by kappa/2;
so each sheet's target is at 4T-kappa/2+eta, Z-free, nonzero, opposite signs on the
two sheets (not used): CONFIRMED.
Margin (14). Even: T eta<=4T-2T/(m-t) and 2T/(m-t)>2>3/2>=kappa/2 since T>m>=m-t.
Odd: T eta<=4T-(3-kappa/2)T/(m-t) and (3-kappa/2)T/(m-t)>3-kappa/2>=3/2>=kappa/2,
strict at kappa=3 because T/(m-t)>1. So (m+n)eta<4T-kappa/2+eta on both sheets:
CONFIRMED. The monic initials then commute sheetwise: nQP_Z=mPQ_Z, Q^m=P^n,
P=(Z+c)^m, no Z^(m-1) term gives c=0 and U_+=U_-=0, contradicting the global balance:
CONFIRMED. No ord G>=2j premise; every W term and every scalar a_i term is carried.
Wording note (non-load-bearing): Section 2's hypothesis "every coefficient of B-z^n
has positive s order" is satisfied literally in items A-C and only after the balance
(k=0 orders) in item E; Section 6 says this explicitly.

## F. Exhaustiveness, fields, controls, scope

lambda ranges over exactly (2). S!=Y^3 is depressed, so it has three simple roots or
one simple and one double root (a triple root needs A=B=0). A minimizing root of phi
exists (phi!=0); if it is simple, or tied with a simple root, item B applies for every
lambda in (2); otherwise the double root is strict, lambda in {1,2}, and the Morse
regimes ord xi>=2rho (item C, xi=0 included) and ord xi<2rho (items D-E) are
complementary. No discriminant boundary is skipped: CONFIRMED.
Fields. Splitting adjoins roots of S and denominators e0<=3; Hensel inverses keep the
lattice; the Morse chart adjoins sqrt(3a) and keeps integral orders; the second
separated step adjoins sqrt(-xi_0) and at most s^(1/2), where no lattice argument is
used. Weight homogeneity and X-Euler with kbar constants persist in finite algebraic
extensions. Localization at generic X only tests an existing identity: CONFIRMED.
Controls 1-6 are changed-hypothesis checks of the cited steps (pure cube, one-sheet
cancellation, an arbitrary positive-order B term, the tie phi=S^t, a rescaled
valuation variable, the lambda=2 B indices); they are not source points and I
rederived each as stated. CONFIRMED as controls.
Scope. With accepted 16l this narrows the literal receiver (q>=0, tops K^m,K^n,
bracket 2c a^3, minimizing triple factor, 16l reference) to the coalesced resonance
(d=2m, j=7q+9, identity (B) of the coordinator report). It does not exclude F10,
actual 112/196, any source family, or JC2; it proves no properness, no unit
certificate and no realisation. No smaller verified portion needs reporting: no gap
was found in A-F.

## Perimeter and own checks

Read: the four charged inputs only, whole. Written: this file and
box/f10-separated-cubic-gate-fable5-20260909/ (README and input hashes) only. No
OPEN token is raised here and none is consumed or closed; no seal or charge_basis
line is authored. Own whole-body read and the own-only raised-OPEN/collision check
were done before the marker was appended.

<!-- BODY-END -->
