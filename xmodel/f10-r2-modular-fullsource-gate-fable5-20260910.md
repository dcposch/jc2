# FIRST Fable 5.1 gate: conditional full-source modular unit lift through the finite local model

status=UNSEALED (no Seal, no charge_basis authored; adapter seals)
lane=f10-r2-modular-fullsource-gate-fable5-20260910
gate_model=claude-fable-5-1
first_action_utc=2026-09-10T15:07:10Z (hash command); stop = earlier of 15:25:10Z (first+18 min) and 15:26:00Z USER TERM (KILL 15:26:05Z); final-3-min reserve from 15:22:10Z; never reset
mode=STATIC; manual math/text/hash only; ZERO subprocess/import/AST/syntax/compile/test/CAS of any size; tools date, ls, wc, sha256sum, grep, apply_patch and bounded file reads only
owned=xmodel/f10-r2-modular-fullsource-gate-fable5-20260910.md, box/f10-r2-modular-fullsource-gate-fable5-20260910/ (both absent at 15:07:10Z; unsealed skeleton first, then body)
charged_input=xmodel/f10-r2-modular-fullsource-route-astra-20260910.md
charged_input=xmodel/f10-r2-modular-properness-route-astra-20260910.md
charged_input=xmodel/f10-r2-modular-properness-gate-fable5-20260910.md
charged_input=xmodel/f10-r2-cyclic-core-astra-20260910.md
charged_input=xmodel/f10-r2-cyclic-core-gate-fable5-20260910.md
charged_input=box/f10-r2-highest-bezout-code-astra-20260910/CONTRACT.md

## 0. Custody, read scope, disclosure

All 6 snapshots in /tmp/jc2-lane.1nAcsS/inputs (distinct basenames) were hashed by sha256sum at 15:07:10Z BEFORE any body read; all 6 digests equal the charged pins byte for byte (rows generated from the sha256sum output into box/input_custody.md; this body carries no hex digest). Both owned targets were absent at 15:07:10Z. All 6 were then read WHOLE in parallel single bounded reads: fullsource route (393 lines, ends at its Seal, read 1-393 in one pass), properness route (114, at Seal), properness gate (78, at marker), cyclic core (229, at Seal), cyclic gate (66, at marker), CONTRACT.md (70, no marker by design). No read was clipped, so no recovery read was needed. No baseline coefficient body, old mod-89 partial, provenance, ledger, corpus, Git, network, process, other lane, live file, ROOT worker file or active highest-code author file was accessed; nothing was executed, imported, parsed or compiled.

Disclosure: this is a different-model FIRST review of the Astra report. Two of the six inputs, the properness gate and the cyclic gate, were authored by Fable 5.1 in earlier sessions, as were the accepted highest and infinity gates they cite. They are consumed at their exact accepted scope and not re-proved: the cyclic core gives the unconditional generic identity A = E[s]/(s^3 - t0) with all fifteen rows and the complete nineteen-row cover; dimension 35 and seven per class are CONDITIONAL on highest regularity; the modular Route P and Route F theorems are accepted as of 17zzv without any actual highest proof. No actual coefficient, integral place, special basis, rank, certificate or source outcome exists in these inputs, and none is asserted here.

## 1. Verdicts

| item | verdict |
|---|---|
| A local model, special highest emptiness, componentwise Nakayama, R_O finite, generic regularity (report sections 2-3) | CONFIRMED; no circular or infinite-module Nakayama; hypotheses exact |
| B full modular unit identity lifts to Q_O = 0, Q_B = 0, guarded source zero (section 4, first half) | CONFIRMED; refinement: integrality is owed on f5,f6,f7 and only on the rows the certificate uses |
| C optional free basis, exact 35 special and generic, seven per class in every characteristic (section 4, second half) | CONFIRMED; no uncorrected imported step |
| D fifteen classes, 7x105 special matrix, (2) chain, target 1 versus t0^7 (section 5) | CONFIRMED |
| E two changed countercontrols (sections 6-7): literal weights, classes, rings, points | CONFIRMED; both controls replayed exactly |
| F minimal prospective packet, INCONCLUSIVE semantics, representation benefit (section 8) | CONFIRMED CONDITIONAL; no execution or allocation |

Smallest blocking correction: none. REFUTED: nothing. One refinement in B weakens a hypothesis and changes no verdict.

## A. Local model and componentwise finiteness (sections 2-3)

CONFIRMED. Own replay. O local domain, maximal ideal m, residue field k, fraction field the accepted FIELD B; only O -> k and O subset B are used, and no map from B to k is ever invoked. S_O = O[Y1,Y2,Y3] with weights 1,2,3; f_h = Psihat_h(Y,1), H_h = Psihat_h(Y,0), so every monomial of f_h has Y-weight h - 5d with d the z-degree, and f_h - H_h has all terms of weight strictly below h, each drop a positive multiple of five. Hypothesis: Proj k[Y]/(Hbar5,Hbar6,Hbar7) empty over kbar. Weighted Nullstellensatz over kbar gives rad(Hbar) = (Y) in kbar[Y]; since (Hbar)kbar[Y] meets k[Y] in (Hbar)k[Y] by faithful flatness, (Hbar) is (Y)-primary in k[Y]. k[Y] is Cohen-Macaulay in every characteristic, so the homogeneous system of parameters Hbar5,Hbar6,Hbar7 is a regular sequence (regular at the irrelevant ideal, hence globally by the graded argument); in particular no Hbar_h is zero. Hilbert series replayed: (1 - t^6)/((1 - t^2)(1 - t^3)) = (1 + t^3)/(1 - t^2) = (1 - t + t^2)/(1 - t), so the series is S5 S7 (1 - t + t^2), degree 4 + 6 + 2 = 12, value 5 x 7 x 1 = 35, an integer identity independent of the characteristic. M = S_O/(H5,H6,H7): each M_n is a quotient of the free O-module on the finitely many weight-n monomials, hence finitely generated; right-exactness gives M_n tensor k = (k[Y]/(Hbar))_n = 0 for n > 12; finite-module Nakayama over a local ring (no Noetherian hypothesis) gives M_n = 0 for each n > 12 separately, so nothing is applied to an unproved finite total module. For a monomial b of weight n > 12, b lies in (H)_n, so b = sum a_h H_h with a_h homogeneous of weight n - h over O; in R_O this is b = -sum a_h (f_h - H_h), a combination of monomials of weight below n; induction on n spans R_O over O by the monomials of weight at most 12, so R_O and its quotient Q_O are finite O-modules. Generic regularity: M tensor B = B[Y]/(H) is then finite-dimensional, so (H) is (Y)-primary over B and H5,H6,H7 form a regular sequence over the field B; this is independent of the properness parent. The parent gate item A affine-finite step should be read with f ranging over the three degree-6 generators of the subring S' (which cover the ordinary plane), not an arbitrary higher-degree f; the present section 3 argument does not use that Proj construction at all, so nothing propagates. Nilpotents are retained (module vanishing, not point counting), zero highest forms are excluded by the hypothesis, degree drops in f_h only shrink the reduction terms, domain is needed only for B and for item C, and no Noetherian hypothesis enters.

## B. Full modular unit identity lifts (section 4, identity (1))

CONFIRMED with one refinement. Q_O = R_O/J_O with J_O generated by the fifteen named rows g_1..g_15 in S_O, g_15 the actual mixed row L1 - U0 L0 as one polynomial. Right-exactness: Q_O tensor k = k[Y]/(fbar,gbar). Identity (1), 1 = sum a_h fbar_h + sum b_j gbar_j in k[Y], says exactly Q_O/mQ_O = 0. Q_O is finite over O by A, so Nakayama gives Q_O = 0 including every nilpotent and torsion element; Q_B = Q_O tensor B = B[Y]/(f,g) = 0 because the integral representatives have the authenticated generic images. Then C_B = Q_B[L0^-1] = 0, E_B = 0, and by the unconditional accepted core identity A = E_B[s]/(s^3 - t0), free of rank three, the complete nineteen-row source algebra with s (hence the guard omega = W t5 s^8) inverted is zero. No rational cofactor lift, flatness, basis or reducedness is used: (1) is read only in k[Y]. Sufficiency direction only: nothing sets s or L0 to one, and both low rows stay represented through the mixed row and the cover relation w^3 = L0, as the cyclic gate item F confirmed. Refinement: if some rows have b_j = 0 in (1), replace J_O by the ideal of the used rows; the quotient is still finite, still reduces to zero, so it is zero, and Q_B is a further quotient. Hence integrality is owed on f5,f6,f7 and on the used rows only; the report requirement that all f and g be integral is a safe sufficient statement, and its remark that L0 need not be reduced separately is a special case. The requirement is strictly stronger than the twenty z-free highest slots of the properness route because H_h is a component of f_h. A certificate proving only rad(fbar,gbar) contains (Y) leaves Q_k nonzero and local, so it is not (1); the report says so. The report asserts no actual source exclusion, correctly.

## C. Optional free basis (section 4, second half)

CONFIRMED. Over any field F with (Hbar) regular, the accepted filtered argument (homogenize with t of weight one; (t,F5,F6,F7) regular; graded permutability makes t a nonzerodivisor modulo (F5,F6,F7); t-saturation identifies (F) with the homogenized ideal; set t = 0) gives gr R_F = F[Y]/(H) and dim R_F = 35 exactly; it uses only Cohen-Macaulayness of F[Y] and permutability of homogeneous regular sequences over a field, valid in every characteristic including five. The Z/5-grading is the monomial-weight decomposition of the polynomial ring, the ideal is generated by class-homogeneous elements, the weight filtration respects the classes, and the series modulo t^5 - 1 is 7 S5, so each class has dimension seven with no roots of unity or averaging. Lift: 35 verified special basis monomials, seven per class, define phi: O^35 -> R_O; coker phi is finite (quotient of R_O) and reduces to zero, so Nakayama makes phi surjective; tensoring with the flat O-module B gives a surjection B^35 -> R_B between 35-dimensional spaces, an isomorphism, so ker(phi) tensor B = ker(phi_B) = 0 by exactness of localization; ker(phi) sits in the torsion-free O^35 and every element is killed by a nonzero element of the domain O, so ker(phi) = 0. No finite generation of the kernel, no DVR and no rational basis test is used. Since phi respects classes, each (R_O)_j is free of rank seven on its lifts. Imported steps: the filtered argument is the cyclic core section 2 as confirmed by the cyclic gate item B (no defect found there); the Proj properness step of the parent gate is not used; the transfer late-operator correction touches nothing consumed here. No uncorrected step is imported.

## D. Cyclic classes, the 7x105 special matrix and the chain (2) (section 5)

CONFIRMED. Classes replayed from the accepted weights: K1_i has weight 14 - i, i = 1..6, classes 3,2,1,0,4,3; K0_i has weight 16 - i, i = 1..8, classes 0,4,3,2,1,0,4,3; L0 = K0_0 weight 16 class 1, L1 = K1_0 weight 14 class 4, U0 weight 3 class 3, so the mixed row L1 - U0 L0 is homogeneous of class 4 = 3 + 1; fifteen rows, exactly the cyclic gate item A list. The f_h and every g_j are Z/5-homogeneous, so the grading exists over O, k and B directly and base change commutes with each component and with the homogeneous quotient. For c in R and g of class d, (c g)_0 = c_(-d) g, so (J_k)_0 = sum_j (R_k)_(-d_j) gbar_j, the degree-zero part of the ideal generated by the fifteen rows; no products of generators are needed. With verified special cyclic bases this is the column space of a 7x105 matrix over k, each column a product reduced modulo the actual fbar_h (R_k is the actual quotient, not the associated graded) and read in the basis of (R_k)_0; zero and dependent columns are allowed. Chain (2): rank 7 iff (J_k)_0 = (R_k)_0 (dimension seven) iff 1 in (J_k)_0 (1 has class 0, and 1 in an ideal means the ideal is everything, so its degree-zero part is everything) iff Q_k = 0. The third step is legitimate exactly because (J_k)_0 is the degree-zero part of an ideal. Hence the single target 1 suffices, while t0^7 in (J_k)_0 only makes t0 nilpotent in (Q_k)_0, which kills the special localization C_k and nothing more; section 6 shows that does not lift. A nonzero 7x7 minor of correctly reconstructed columns certifies rank 7; a claimed numerical rank or a guessed basis does not. Sizes are over k; a residue extension multiplies scalar storage by its degree, as stated. No actual rank is computed and the report explicitly forbids replacing the named source slots by a truncated receiver: every used polynomial must map to its actual named row, consistent with the refinement in B.

## E. The two changed countercontrols (sections 6-7)

CONFIRMED, both replayed literally. Section 6: O a DVR with uniformizer pi, weights x:1, y:2, z:3. Highest triple x^5 (weight 5), y^3 + z^2 (6 = 6), y^2 z (4 + 3 = 7). Over any field: x = 0; y^2 z = 0 gives y = 0 or z = 0; y = 0 forces z^2 = 0, z = 0 forces y^3 = 0; only the origin, so special highest regularity holds geometrically in every characteristic. f5 = x^5 - pi has drop 5 and class 0, f6 = H6, f7 = H7, so R is Z/5-graded, and by A and C it is free of rank 35 over O with seven per class. L0 = x has class 1 as required; t0 = x^5 = pi; J = (0) in all fifteen slots. Modulo pi, t0 = 0 so t0^7 = 0 in (Q_k)_0 and L0^35 = x^35 lies in (J_k)_0 = 0 after reduction (x^5 = 0 in R_k): the special localized quotient is zero. Generically x^5 = pi is a unit, so x is a unit, R_B[x^-1] = R_B is nonzero (free of rank 35; over Bbar the point y = z = 0, x a fifth root of pi). R[x^-1] inverts pi, so if it were a finite O-module, pi M = M and Nakayama would force M = 0, contradicting its generic fibre; the localization escapes the finite-model argument, exactly as the report says. Note the full test (1) correctly FAILS here: Q_k = R_k is 35-dimensional, consistent with Q_B nonzero. Second control x^5 in place of x^5 - pi: same regular highest triple, Q_k = R_k nonzero so (1) fails, yet x is nilpotent in R_B so C_B = 0 and the guarded source is zero; the full test is sufficient, not complete. Section 7: H5 = x^5, H6 = pi y^3 + z^2, H7 = y^2 z (weights 5, 6, 7); f5 = x^5 - 1 (class 0, drop 5), f6 = pi y^3 + z^2 - x (classes 6, 6, 1, all 1 mod 5, drop 5), f7 = y^2 z; J = (z), and z has weight 3, class 3, which is an available slot (K1_1, K1_6, K0_3 or K0_8); L0 = x class 1. Generic highest triple: x = 0, then y^2 z = 0 and pi y^3 + z^2 = 0 with pi a unit force y = z = 0, regular. Special: pi = 0 gives (x^5, z^2, y^2 z), so [0:1:0] survives and Route P fails. Chart x = 1, u = y, v = z: H5 = 1 so P = 1, Q = 0; H6 = pi u^3 + v^2 so B3 = pi u^3, B1 = 0, B0 = 1; H7 = u^2 v so C3 = 0, C2 = u^2, C1 = 0; F = Q^2 B3 - P Q B1 + B0 P^2 = 1, G = 0, a constant unit, so Route F holds literally over O; generic X1 = 0 is empty by the computation above even though the Y2Y3 coefficient is zero. Full special quotient: z = 0 makes f6 = -x, so x = 0, and f5 = -1 is a unit, so Q_k = 0 and (1) holds. Generic: Q_B = B[x,y]/(x^5 - 1, pi y^3 - x) has the point x = 1, y^3 = pi^-1 over Bbar, nonzero, and stays nonzero after inverting x, which is already a unit. Q_O = O[x,y]/(x^5 - 1, pi y^3 - x) makes x and then pi units, so it is not finite over O: the missing hypothesis is precisely special highest finiteness, and Route F plus generic regularity cannot supply it. Both objects are changed algebraic controls, not source or Keller points, as the report states.

## F. Minimal prospective packet, semantics and representation (section 8)

CONFIRMED CONDITIONAL. The packet is exactly what the theorems consume: (i) one authenticated local model O with fraction field the accepted field B and specified residue field k (any maximal ideal of the monic order suffices for the algebra; a simple root is a convenience); (ii) all three f_h and the used named rows authenticated in S_O with supports and weights checked before projection and the mixed row taken as one polynomial, with any nonunit rescaling declared as a new model whose own special fibre is certified; (iii) a WHOLE special highest projective emptiness certificate, for which the same-model Route P form is sufficient: cbar Fbar + dbar Gbar = 1 in k[u] through the denominator-free identities (covering Q = 0, zero rows, constants and drops) PLUS same-model nonzero residues of the Y2Y3, Y2^3 and Y3^2 coefficients on Y1 = 0 (own replay: 2a + 3b = 5, 6, 7 give only Y2Y3; Y2^3, Y3^2; Y2^2Y3, and the first two forms kill both coordinates over kbar); (iv) the full modular unit identity (1) with any nonzero constant inverted in k, or the verified special basis, the fifteen product blocks and a full-span certificate (2). Success gives Q_O = 0 and the accepted generic guarded source zero. Failure of any item is INCONCLUSIVE: section 6 second control has guarded source zero with (1) failing, and the empty-chart control P = 1, Q = 0, H6 = H7 = 0 gives F = G = 0 with an empty chart. No source conclusion follows from highest regularity alone, modular nilpotence or L0^35 membership never lifts, and the existing highest-chart instrument does not check this larger packet. Representation assessment without runtime or height claims: the certificate objects are finite and shape-bounded (35 basis monomials, a 7x105 matrix over k or bounded cofactors in (1), each residue-field scalar versus seven rational coordinates), the reduction of every product terminates by weight descent in steps of five, and no rational basis or rational membership witness is ever displayed; the dominant cost is the model, integrality and provenance audit, exactly as the report says. If pursued, this candidate belongs in one code/input/runtime/result gate rather than further review lanes; nothing is executed, registered or allocated here, and the actual place, coefficients, basis, rank and certificate remain UNKNOWN.

## OPEN(S) RAISED

None. No new canonical OPEN. The retained unknowns are the producer's own: the authenticated model, integrality of f and the used rows, the whole special highest certificate, and whether 1 lies in the reduced eighteen-row ideal, equivalently rank 7 of the verified cyclic matrix. No charge_basis, no follow-on authority, no new global theorem.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly xmodel/f10-r2-modular-fullsource-gate-fable5-20260910.md and box/f10-r2-modular-fullsource-gate-fable5-20260910/input_custody.md, both absent at 15:07:10Z; no shared, frozen, input, ROOT, worker, highest-code or old file was touched; no corpus scan, no other lane, no network, no scientific subprocess, no Git.

## Completion

Bound disclosure: the first body append (sections 0-1, A-C) was about 1450 words; the second append (D-F, OPEN(S), COLLISIONS, Completion) totalled 1682 words, above the 1500-word per-write bound, although every individual section is below it (largest: E at 638 words). This overage is recorded here, not concealed; no content was removed to hide it.

Authoring attestation: every byte of both owned files was written by apply_patch (skeleton and custody 15:12:22Z, sections 0-1 and A-C 15:13:18Z, sections D-F, OPEN(S), COLLISIONS and this block at about 15:15Z); no Write/Edit tool, no shell redirection, no helper script, no source edit, no execution, no network, no Git. Honest patch record: the custody file first received a stray leading plus sign on its first hash row from the patch prefix; it was corrected by an apply_patch update at 15:13:18Z and the six rows were rechecked against a fresh sha256sum of the snapshot directory. A first attempt to append sections D-F at 15:14:13Z failed on context matching and changed nothing (file still 45 lines); the append was re-anchored on the exact last line and applied. The body carries no 64-hex digest. Own WHOLE reread of this report (header, sections 0-1, A-F, OPEN(S) RAISED none, COLLISIONS EMPTY) was done before the marker was appended; the marker was confirmed absent before that append. No Seal and no charge_basis are authored; the adapter seals. Stop 15:25:10Z, never reset. The marker below is the only standalone marker in this file and nothing follows it. All writers idle at the marker.

<!-- BODY-END -->
