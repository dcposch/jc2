# Whole-portfolio ideation — coordinator packet

Date: 2026-08-26 01:45Z

Status: **INDEPENDENT RESEARCH PACKET; STRATEGY INPUT, NOT EVIDENCE**

## Blindness and scope

The coordinator rescanned the canonical 46 avenues, their current gaps, and
all campaign news through 01:42Z.  The ideas below were developed before the
first sibling ideation packet was read; this file was frozen immediately
after that packet arrived, so the provenance records the timing honestly.
No mathematical claim is promoted here.

## Ranked cards

### 1. Finish the order-four deck quotient by a divisor certificate

The exact residual plane relation has Newton polygon
`conv{(0,0),(8,3),(1,3),(0,2)}`, hence arithmetic genus seven.  Four ordinary
torus nodes and the exact top cusp of semigroup `(2,7)` account for all seven
delta units.  More importantly, its normalized `v` divisor is provisionally

```text
div(v)=8*Z-P_0-P_A-6*P_B.
```

Thus the deck quotient `Y: y^4=v` has ramification contribution
`3+3+(4-gcd(4,6))=8`, so Riemann--Hurwitz gives `g(Y)=1`.  The proof-grade
gate is not more exploratory genus computation: certify the exact source
saturation/component, dominance or birational projection to `(a5,a6)`, the
`a6=0` complement, all four divisor places, irreducibility/connectedness of
the Kummer cover, and nonconstancy.  Then a nonconstant
`P^1_x -> Y` is impossible and the whole order-four `mu4!=0` leaf dies for
all `U`.  Run premise closure and hostile review concurrently on AWS.

Stop condition: a frozen exact quotient/divisor/RH theorem plus independent
review, or a precise failed premise identifying the surviving component.

### 2. Use labeled Davenport--Zannier permutations as the missing sheet bridge

The new weighted-tree frontend is not only an enumerator.  Lu--Song's
full-labeled permutation construction carries individual sheets and branch
cycles.  That is exactly the kind of provenance absent from `G2-PSC`, the
GGV-packet-to-Sigray decorated-tree transport obligation.  Prototype a
functor on the already finite terminal cases: attach the GGV corner/packet
labels to the DZ permutation triple, pass through the plane tree, and compare
the induced cyclic orders, local degrees, and distinguished sheets with the
known td6 Sigray terminal books.

Stop condition: all finite td6 cases commute under one explicit label map, or
one exact mismatch proves the proposed bridge cannot be the missing global
functor.  This is a low-compute proof-design lane, not a claim that terminal
trees already encode the complete boundary book.

### 3. Compile lower tails as a tree-local leaf-pruning recurrence

The signed-moment identity is the barycentric formula
`n_i=kappa/P'(c_i)`.  Deleting a terminal leaf changes `P`, `A/B`, and their
Laurent expansions by one explicit rank-one factor.  Derive the induced
recurrence for the first unresolved Faber/Grunsky tail, and test whether its
tree sum has a fixed sign, a nonzero extremal-edge term, or a telescoping cut
formula.  In parallel express the same obstruction through the Schwarzian of
the Belyi map, whose double-pole principal parts are passport-local.  This is
the best route from finite terminal enumeration to a uniform bound in `U`.

Stop condition: an exact recurrence reproduced on every `U=3` client and the
order-four all-weight-three star family, followed by either a uniform
nonvanishing proof or a concrete surviving family that redirects the search.
All enumerative checks run as count-certified AWS shards.

### 4. Replace hand D1 fans by the normalized Rees/tropical object

The two missed correction rays show that raw leading-weight comparisons are
not the correct state space.  Compute the tropicalization or normalized Rees
algebra of the full finite correction ideal, retaining each intermediate
rational layer as a graph variable.  The known tied ray and both correction
rays are mandatory positive controls; the reviewed `la^20` monomial is a
mandatory empty-cone control.  This connects the live D1 problem to the
otherwise dormant Rees-valuations/integrality avenue and can certify all
valuation cones at once.

Stop condition: an exact finite fan whose torus initial ideals cover the
charged valuation region, with every cone either monomial-empty or supplied
with a formal-lift successor.  Modular fan discovery is permitted only as a
guide; characteristic-zero certificates remain required.

### 5. Share one cotangent/Kuranishi engine between TD6 and the AS lift lane

TD6's 22 live q sensitivities plus eleven dead-stretch sensitivities and the
AS fixed-support Witt problem are both deformation problems over a frozen
source DAG.  Build one proof-carrying expression DAG that emits the tangent
map, Fitting ideals of its cokernel, and the quadratic Kuranishi obstruction,
with automatic source-identity reconstruction.  This amortizes the expensive
software work and directly addresses the D1 lesson that hand-refactored rows
can silently acquire constants.

Stop condition: the TD6 V80 table and one already reviewed AS fibre are both
reproduced from the same abstract engine; then run the first joint nonlinear
Fitting/Kuranishi gate rather than extending serial coordinate probes.

### 6. Reinterpret the shifted Davenport--Stothers curve as an elliptic-surface section

The loaded order-four identity

```text
g(z)^2=f(z)^3-2f(z)+V_5(z)
```

makes `(f,g)` a high section of the elliptic surface
`Y^2=X^3-2X+V_5(z)`.  The surface is in the rational-elliptic range, where
Shioda height lattices and singular-fibre contributions can turn a large
coefficient system into a finite section classification.  Audit whether the
already reconstructed one-dimensional coefficient curve is a known
Mordell--Weil/height stratum and whether the terminal coefficient `r7` is a
height or specialization coordinate.  This is secondary to the nearly
closed genus-one quotient, but it may cover the exceptional chart or explain
the rational normalization structurally.

Stop condition: a ten-line height inequality/classification that reproduces
the exact coefficient curve, or a source-level mismatch that parks the lane.

## Allocation until synthesis

- 45% order-four quotient premise closure and adversarial review;
- 20% D1 corrected dual encoding plus full tropical/Rees coverage;
- 20% TD6 V80 reconciliation and joint deformation gate;
- 10% DZ labeled-sheet bridge / uniform tree recurrence;
- 5% AS and protected global clients, with no registered job interrupted.

Reviews run asynchronously.  Every substantive calculation is AWS-only.
