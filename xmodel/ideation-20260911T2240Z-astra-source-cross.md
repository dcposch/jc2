# FULL2240 cross — Astra source

First action 2026-09-11 23:01:28 UTC; original reserve23:16/HARD23:19.
Strategy only. All17 current input pins matched before consumption.
The original ten inputs reuse this agent's same-byte WHOLE blind reads; the
own blind reuses its complete sealed read at22:53:28. CROSS-PINS, contract,
ROOT/geometry/Fable blinds and postblind history are fresh WHOLE. No peer
cross, linked provenance, new external source or execution was consumed.

## 1. What the merged implication actually says

Write A=C[p,q], R=C[x,y], n=[FracR:FracA], N=R/A,
M=Tr(R), K0=ker(Tr:R→M), B=C[e_p,e_q], E=FracB.
The three contributions merge, rather than vote, into

    0 → K0 → N → M/A=⊕L_i → 0.

The injection sends k to [k]; Tr(a)=na gives K0∩A=0. If Tr(r)∈A,
subtract Tr(r)/n from r. This proves exactness without a trace section.
Trace equivariance and flat E-localization preserve the sequence. At the
named BASS-SELECTION holonomic/Mellin tier its ranks are finite and additive;
neither finite rank nor IC simplicity supplies a stable E-line.

There is NO nontrivial-source kerTr=0 branch. Localizing R at A\{0} gives
the finite field extension L: the resulting finitely generated algebraic
K-algebra is a field. For n>1 its trace kernel has dimension n−1; clear an
A-denominator to obtain nonzero k∈K0. Since K0 injects into the stipulated
B-torsion-free N, its finite Mellin rank is at least1. Conversely K0=0
forces n=1; the accepted R∩FracA=A then gives R=A and M/A=0. Fable's
kerTr=0 plus nonzero rank-one boundary summand is therefore an EMPTY actual
Keller branch, not an economical selection shortcut.

The boundary comparison needs its exact conditional conventions. From
H²(M)=0 and right exactness, H²(M/A)=0. Under the named finite-normalization
IC/de Rham comparison H²(L_i)=H¹_dR(D̃_i), this forces each connected smooth
affine normalization to be A¹ (2g+punctures−1=0). That is conditional
bookkeeping, not a newly verified primary import. The same codimension-one
shift gives

    ρ(L_i)=−χ(D̃_i∩{pq≠0}),

not an undetermined sign. If no component lies in the axes, this is n_i−1,
where n_i counts DISTINCT normalization points over their union. Any target
translation must change B and ALL fibre terms consistently. Components in
an axis require the Euler formula, not a fictitious finite n_i.

The first unsupported selection arrow is that some ACTUAL quotient has a
δ-stable line; n_i=2 would suffice but is not established. Given a specified
line δℓ=aℓ, geometry correctly isolates the NEXT obstruction: for a lift v,
b=δv−av lies in (K0)_E, and a lifted line exists iff
(δ−a)w=−b has a solution there. This is an additive semilinear cokernel,
not an E-linear rank test. A line inside K0 is a different possibility.
Even a successful lift leaves polynomial-G/resonant normalization open;
the previously solved finite-prefix lifting does not remove either gap.

## 2. Cheapest discriminator: expose the Euler content, then STOP

The merged strict inequality is ρ(N)≥1+Σρ(L_i) for n>1. There is a useful
source-specific rewriting, under the same comparisons and constructible
Euler additivity, which tests rather than assumes its novelty. Here χ is
compactly supported Euler characteristic.
Set U={pq≠0}, D_U=D∩U, ν:⨿D̃_i→D, and

    δ(y)=n−#F⁻¹(y),       b(y)=#ν⁻¹(y).

Here b counts normalization branches, not defect multiplicities. Off D,
the actual finite étale covering has n points. Since χ(U)=0, finite-fibre
Euler integration gives χ(F⁻¹U)=−∫_(D_U)δ dχ. The source formula for ρ(N)
is exactly χ(F⁻¹U); normalization gives Σρ(L_i)=−∫_(D_U)b dχ. Hence

    ρ(K0)=∫_(D_U)(b−δ)dχ ≥1       (n>1).

This identifies the budget as a difference of the standard fibre-defect
and normalization Euler counts. The positivity input is source-kernel
nonvanishing plus B-torsion-freeness, NOT positivity of Euler integration.
Pointwise nonnegative defects do not determine the sign; singular strata
and omitted points must remain. The packet supplies no bound making this
integral ≤0, nor an actual defect function to evaluate. Thus it has NOT
proved an inequality independent of the standard fibre Euler constraints.
Claiming either a contradiction or automatic redundancy would overreach.

The cheapest manual discriminator is this exact elimination of the axis
formula, followed by asking for a source-only restriction on b−δ. The
elimination is displayed; the required restriction is absent. Outcome:
NO_GAIN toward exclusion/selection from the supplied numerical data; STOP
as a new allocated lane. If a concrete source restriction later appears,
one ≤15-minute UNMEASURED stratification check can test the integral before
any gate. No generic-curve Gröbner experiment, guessed sign, or module farm
can supply that restriction. This is not a new canonical OPEN.

## 3. Construction and resource disagreement

The support warning survives an explicit degree check. For
T={t²−1=x²Z}, E_T=(2xt,2t²−1,Z) has generic degree2: a generic target t'
has two square-root lifts t, and x=x'/(2t). The accepted pullback scales
the volume by2 and preserves every Laurent weight. If (H,G) were a scalar
pair of generic degree d, then

    (H∘E_T^k, 2^(−k)G∘E_T^k)

would have the SAME prescribed support pair and degree2^k d. Regularity,
dominance and generic finiteness follow from the nonzero bracket; degrees
multiply in the function-field tower. This is conditional on existence,
not a constructed pair. It defeats a support-only degree ceiling as neutral
preprocessing: such a ceiling would itself exclude that support's pairs.
It does not identify function spaces, ordinary degrees or target orbits,
nor rule out a genuine full-support exclusion. Allocation stays merged S/T;
no new H, mate or runnable construction test appears.

The strongest proof endpoint ON PAPER remains authenticated fixed-r3 rows
followed by the whole297 minor; it is finite and scoped, not an all-JC2
theorem. Trace has wider reach but no closing source inequality. Complete
characteristic-zero source properness/point reconstruction and the scalar
pair are the strongest honest construction endpoints; their rankings
reflect different missing objects, not measured ease. Necessary-row
properness, modular points and a zero minor do not establish either.

Reject Fable's immediate single-shot allocation: changing the launch
description does not qualify the missing end-to-end release. Accepted
mathematics need not be re-reviewed; actual rows, place/inverse conditions,
independent checking and terminal custody still must exist. Hold the SAME
experiment, all25 slots/guard and297 rows, without a new cap or worker.
The known296 alternative needs its full exact-B/containment hypotheses,
not a residue-support guess. No mathematical software run is selected.

## 4. Survivors and review boundary

Survive as proposed compositions: exact kernel sequence, nontrivial-degree
kernel positivity, the conditional boundary-rank/defect identity, and the
precise additive line-lifting obstruction. If ROOT elects to bank them,
ONE different-model FIRST should check the IC shift, finite-fibre Euler
attachment and source/nonzero-kernel argument together. This cross does
not promote them or request a broad re-review of accepted foundations.
The support-degree implication can be checked in that same narrow intake;
it does not fund a construction lane.

ROOT's postblind pairing control fires its own duplicate stop: even the
full additive traceless-pairing image can be S[1/u]+S[1/v] with no mixed
pole. It is not a polynomial source, but excludes the proposed purely
local inference. Never replace the actual IC quotient by full mixed
localization. No peer supplies an extra actual source object NOW.

Separate systems decision: merge all curation suggestions into the already
ROOT-owned documentary cleanup; no new lane, controller, scientific
preprocessor or utilization target. Retain all46 scopes, exact old history,
the sweep boundary/late-publication qualifications and source-vs-necessary
distinctions. This report changes no frozen file. All costs above are
planning estimates, not observed execution. Whole-read/postpin/collision
and quantity/cheapest-test checks precede sealing; no unresolved publication
placeholder or further work is authorized.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `8408`.
- Body SHA-256:
  `e6964b4653666559d83847821d656c7738cfd0f8d0096eb35374ede6be9c5664`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
