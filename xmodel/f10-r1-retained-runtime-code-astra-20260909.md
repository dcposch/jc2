# F10 r1 retained-row runtime code preparation

Status: DISABLED / INTERNAL-UNREVIEWED new caller and finite harness. The
transformer, checker, authority helper and probe are byte-identical accepted
copies. No code was executed, imported, compiled or tested; no retained data
was emitted and no worker was assigned. This report does not decide an ideal.

First action17:14:37 UTC, September9; controlling cap17:34:37 UTC. Both new
targets were absent before transaction begin. The eight exact allowed input
hashes matched before their WHOLE static reads and remained unchanged at the
recorded postcheck. Their paths, hashes and read scopes are in input-pins.json
and READ-SCOPE.md. Accepted17d remains a static-review premise only.

## Completed preparation

The new owned directory contains:

- unchanged transformer.py, checker.py, execution_gate.py and probe.py;
- dispatch_batch.py, a narrow adaptation of the previously reviewed v2 caller;
- semantic_controls.py, twelve finite changed-object semantic checks;
- caller.diff, the literal old/new caller diff;
- dispatch.registration.DISABLED.json, which specifies no worker or executable
  cap and cannot authorize execution;
- VALIDATION.md and read/custody metadata.

The intended finite batch is five actual authority refusals, a valid sentinel,
the existing descendant-RSS/TERM-ignoring/parent-exit probe, one genuine
transform, one genuine production checker read-back, and one capped semantic
harness. There is no mode farm, optimized-mode matrix, solver, factorization,
prime, alternative representation or new supervisor.

The caller retains the unchanged helper/CAPRUN exact-argv interface, inherited
16MiB per-file limit before CAPRUN exec, no new caller PGID, recorded namespace
and leader start identity, typed MATCH/SENT15/MATCH/SENT9 dummy sequence,
sampled group RSS, cleanup/reap/no-owned-live-group check, input pre/post pins,
fifteen-second admission margin and original stored post-return cutoff. Future
root registration replaces stale historical worker/deadline constants; it must
provide an exact host/interpreter/input vector, all cap profiles, mathematical
and task absolute UTC deadlines and aggregate wall cap. No default new budget
is supplied. Any failure is STOP/NONDECISION without retry or escalation.

Root must separately verify ownership and arm its independent exact-worker
stop timer before dispatch. Cooperative exclusive immutable per-operation
authority, with no concurrent writer, is the explicit operating condition.
The helper reread race is not claimed eliminated against a malicious same-user
writer. No optional helper source-rehash patch is introduced. Sampled RSS and
cleanup limits are not strict real-time guarantees.

## Evidence deliberately kept separate

The unchanged checker.main positive run is the only production-entrypoint
full read-back. Its future receipt binds actual artifact and code hashes and
checks every original slot/retained row, all eight bracket bands, complete
forcing, ten inverse-pole slots, gauges/envelopes and top/guard restoration.
None of those future runtime outcomes is claimed now.

The twelve negatives call unchanged checker.verify in-process after a valid
authority and a separately hash-bound genuine production receipt. Each fixture
is exclusively written, reread, independently hashed and expected to fail with
an exact exception class/reason. This avoids a stale-hash rejection being
misreported as a semantic test, but it is not evidence of twelve production
entrypoint invocations.

The controls target dropped retained row, L inverse, scale, modulus, signed-s
loss, forbidden lower exponent, missing pole inventory, missing leading-a
restoration, changed B5, float contamination, a wrong canonical coefficient
above2^53 and its distinct rounded canonical integer string. The omission of
restoration a expects KeyError, not an invented custom checker message.

The precision pair checks a real rounding precondition and two negative row
substitution outcomes. Harness JSON exact-string retention is not a positive
execution of the checker's nested rational parser and does not establish
successful unchanged large-coefficient payload semantics. No production API is
modified to expose that parser, and no assertion is made about large
coefficients in the as-yet-unemitted retained artifact.

Likewise, altered A/B would fail original coefficient binding before a later
bracket or inverse-pole equation. This packet claims no such late negative
coverage. Pole-slot omission tests inventory. The single genuine positive
read-back is the proposed direct evidence for complete bracket/pole equations.

## Validation still required; no measured payoff

The future genuine checker receipt already measures original/retained row
terms, artifact bytes, degree ranges and coefficient bit lengths. CAPRUN
telemetry supplies real time/resource observations. No statistics or algebra
subprocess was run here. Twelve verifier calls in one harness may cost more
than the single positive run; cap feasibility and costs remain unknown.

There are three explicit pending validation quantities, not claimed results:
(1) one independent static delta review of caller/harness/registration;
(2) seven fresh same-caller preflight operations under root-chosen authority;
(3) three registered mathematical children (transform, full read-back, finite
controls). The cheapest authorized next step is the static delta review; no
worker dispatch follows automatically. If a future cap prevents completion,
retain the partial evidence and stop, not a larger automatic run.

No additional mathematical source gap is raised, and no new OPEN identifier
is introduced. The outstanding questions are explicitly the three engineering
validation quantities above. All accepted originals remain untouched; no
shared/canonical edits, agents, network, process inspection or AWS actions
occurred. The new code has no runtime acceptance or speedup claim.

Publication is through begin/private-partial/whole-own-read/own-open-check/
close/finalize/expected-verify; final custody binds the exact frozen output
and transaction. All writers are idle at terminal handoff.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6191`.
- Body SHA-256:
  `9f9ae25026aeb47b0eb94987d67ecb4ab61ef62305559916d00ced243a54211b`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
