# Whole-portfolio synthesis — round `20260827T0635Z`

Date sealed: 2026-08-27 06:35Z  
Date closed: 2026-08-27 07:50Z

Status: **coordinator navigation and allocation decision.** Mathematical
claims retain the evidence tier and lifecycle state of the cited artifacts.
Nothing in this synthesis proves or disproves JC2, Gate T, order two, or the
maximum-12 reduction.

## Custody and equal-standing roster

The packet and four blind submissions were frozen before any lane saw another
submission:

```text
packet    8e140384a0603a588c03dfd65a764b8be5ab26f3301838de09b5b2d1c39131be
Sol       05400316ac7ba488faf93822a6a8dac8b5fde0ab8be4e0fcce0132565e179764
Fable 5   d1f607000ba9992e2778066ce95364205df48db15e3d27fbe71f3933953615a5
Grok      2c0fa61ede365a4c9ef6f4252704c1dc18825818a6c35e12b4c9de763a1f1761
Opus 5    0fbb452456a34b24b28b35a9fd0f99fc6ac86f81edab7a704369aeba0b7881da
```

Fable 5 and Opus 5 received the same packet, maximum-effort setting, shell
boundary, deadline, and blindness rule as the other direct-model lanes. Their
ideas are deduplicated by mathematical mechanism after collection; model name
is neither a vote nor a source of review immunity.

Cross-pollination and independent exact replay:

```text
Fable 5 cross-review             163f0aee46efbb127ef07eafa9a68e28f0defc8df3f8d4c1e2d770670c4cda2d
Opus 5 cross-review              7db079e57e5d9a9e6e96192835b4733297f5d643f177ff05888eb4cc1be43486
Sol L43 hostile replay           b9cc1e8ea56a3e2ca3b394ae9c9faea74934f58ea561ddafd363be493b78fdfd
Sol radical-cascade replay       ede6b287bab76c00ac8d5b8e99e630a2006fcb7f252db0fa865764ad9dddb133
```

## What the round established

### 1. Ordered `T-a1`: structure before larger elimination

Three independent mechanisms now fit together at the **raw, ordinary,
`rho=0`, rows-through-grade-19** scope.

- Fable 5 proved the fixed-weight restriction: multiplication by a homogeneous
  ideal generator cannot lower sigma weight. Consequently the raw row ideal
  forever excludes the nine mixed monomial shapes
  `(1,0)` through `(1,3)`, `(2,0)` through `(2,2)`, and `(3,0)` through
  `(3,1)`; pure `k^j`, `j<=4`, are also excluded by the independently
  verified support theorem. The `a1^4` conclusion is only through grade 19,
  and every statement here is in the `rho=0` fixed-weight ring.
- Sol's exact first-occurrence calculation shows symbol rank exactly two on
  `D(a1)` at grades 14--19, not three. Rows 1 and 2 split the two newcomer
  directions; the ideal identity `e0=0` removes the apparent third symbol;
  rows 3--7 form the persistent compatibility block. Thus adding newcomer
  variables cannot by itself repair a failed compatibility row.
- Opus 5 found an exact radical/field-point cascade. Sol and Fable 5 replayed
  it independently line by line. It is a structural split of the raw
  `rho=0` solution set, not an ideal-membership certificate and not an
  unspecialized Rees-chart theorem.

The post-snapshot V38 producer checked all sixteen mixed targets
`a1^i k^j` with `i>=1`, `5i+4j<=25`: every target is an exact-Q nonmember of
the ordinary raw ideal generated through grade 19, with dual finite-field
compiler/validator agreement and controls. Weights 17--19 are final by the
restriction lemma; weights 20--25 remain only relative to rows through grade
19. V39 separately finds `Tg19_7` outside the ideal generated by the other 69
frozen rows at weight 19, so it is a genuinely new compatibility condition.
Both claims remain provisional pending the already-running different-model
hostile review.

After the blind cutoff, the cascade itself closed both raw branches. A2 dies
at grade 16 by the exact two-row identity

```text
Tg16_6-(3/32)rs1*Tg13_2=(21/1024)a1^2 rs1^2,
```

and A1 has an ordinary six-row branch certificate `a1^8`. Combined with the
reviewed radical cover, this leaves no characteristic-zero field point on
`D(a1)` in the frozen raw `rho=0` prefix through grade 19. Producer/replay
SHA-256 values are `5d4c42ff...` / `f4ec7293...`; Opus5 hostile review is
running. This is the largest mathematical delta after the snapshot, but it
still does not commute saturation with `rho=0` or close the total chart.

### 2. Terminal receiver: raw support is understood; honest incidence is not

Opus 5's support hypergraph computation is exact and has two independent
replays. Among 4,997 monomial supports there are 88 inclusion-minimal supports
(11 singletons, 54 pairs, 23 triples). Eleven forced singleton vertices and
eleven pairwise-disjoint residual supports prove a minimum hitting-set size of
22; the exhibited set attains it. Hence the maximum coordinate-linear
zero-section for the raw `rho=0` system through grade 19 has dimension 43.
The displayed `L43` has `k` free and `a1=0`.

This is **not** an irreducible component, an all-depth receiver, or a point of
the honest collision/Rees incidence. The proposed generic-`L43` receiver test
is ill-typed until a collision-to-`[6,2]` pullback is actually constructed.
Raw receiver row exports are therefore stopped.

The literal `K00` section is killed on `D(sigma*Jdet)` by the exact terminal
equation `Phi7`; that does not make the geometric blowup fibre empty, because
saturation before base change can retain transverse directions. The correct
object is closure first:

```text
K = (Phi) : Lambda^infinity : Jdet^infinity
B = K + (Lambda) + M_K00
H_K00 = B : (C6*k10*Jdet)^infinity.
```

Restriction-first evaluation is a mandatory negative control, not a substitute
for this incidence. The `rho=0` tip and `C6=0` companion stay separate.

### 3. Duals are separators, not quotient identities

Both cross-reviewers found a genuine mathematical error in Grok's proposed
rewrite of the five-term V37 obstruction. A functional `lambda` annihilating
the weight-19 ideal slice and satisfying `lambda(phi19)!=0` proves
`phi19` is **not** in that slice; it cannot be rewritten as an element of the
ideal. A nonzero evaluation on a future generator merely invalidates that
separator. It does not imply membership or predict a certificate type.

The corrected transport rule survives: to extend a separator after adjoining
homogeneous rows, enumerate and test every complementary-weight multiple.
Bare-generator evaluation is fail-open. General-`rho` transport additionally
requires a new argument because `rho` has weight zero.

### 4. Cross-avenue composition and software acceleration

The strongest new software idea is one fail-closed gate shared by landing
lanes rather than another family-specific solver. Before an expensive span or
Groebner launch it should perform:

1. divisibility and vacuous-nonmembership checks;
2. row/ring/chart/source provenance and specialization-direction checks;
3. support-hypergraph and coordinate-section census;
4. complete complementary-weight dual transport;
5. exact replay of positive and deliberately corrupted controls.

The theorem-interface composition pass also found a major AS109 tightening
after the sealed snapshot: composing the reviewed AS-TRI and Hensel
noninjectivity results with the source-audited 2018 partial-`y` automorphy
theorem appears to exclude `deg_y Q=2,3,4,5`. If the producer and
different-model review survive, the first possible target moves from two to
six, with `6 | deg_x(q_6)`, `deg_y P>=12`, `3 | deg_y P`, and the stated
common-core divisibility conditions. The prior `n=2`/`n=3` compute plan is
then stopped as a history duplicate rather than launched.

## Deduplicated model contributions

- **Sol:** exact symbol/compatibility decomposition; the closure-first `K00`
  discriminator; complete V38/V39 compiler/validator wave; integration and
  explicit ring-residence firewalls.
- **Fable 5:** the fixed-weight shape floor and its all-depth consequence at
  the raw homogeneous scope; Kummer-aware trivial-locus classifier; strongest
  hostile replay of all V37 dual products; exact independent confirmation of
  Opus's support and cascade results.
- **Grok:** a useful `Proj` versus affine-`V(I)` warning that independently
  anticipated the `K00` correction, plus the surviving core of a dual-
  transport engine. Its displayed primal rewrite, V34 negative control, and
  newcomer-emptying outcome are withdrawn.
- **Opus 5:** the exact `L43` support theorem; the ordered-`a1` radical
  cascade; a support-incidence preflight that would have prevented wasted
  computation in this campaign; and systematic registration of the
  `rho=0`-to-total lifting obligation. These are material nonduplicates of
  Fable's work and have now been independently replayed.

## Ranked executable cards

### Card I — `T-A1-STRUCTURE-FIRST`

Finish the V42 and V39 hostile reviews. The raw `rho=0` solution set has now
died, so compute the smallest global radical/ideal certificate and separately
attack the two missing conversions: specialization versus chart saturation,
and `rho=0` versus a literal total identity of the form
`a1^N(1+rho W)`. The first speculative successor has reduced the latter to
fixed-weight module membership over `Q[t]_(t)`, `t=rho^2`, beginning at
`N=6`; it runs on AWS rather than treating raw-field-point emptiness as the
final theorem.

### Card II — `RECEIVER-CLOSURE-FIRST`

Compile and run `H_K00` in the order displayed above, with the restriction-
first unit as a mandatory negative control. Success gives an honest generic
receiver discriminator; nonunit/proper output yields a concrete transverse
component to parametrize; timeout or compiler failure records the smallest
missing map and forces a redesign. Do not substitute `L43` or more raw rows.

### Card III — `AS109-DEGREE-SIX + PREFLIGHT`

Hostile-review the composed partial-`y` theorem first. If confirmed, cancel
all `n=2`/`n=3` searches and target only the constrained `n=6` arithmetic
Newton corner. In parallel, install the cheap support/dual/ring gate as a
reusable front end for future landing experiments. Neither task blocks TD6.

## Launch, continue, hold, stop

**Launch/harvest now:** A2-first cascade continuation and its remaining A1
branch; V38/V39 hostile review; closure-first `H_K00`; different-model review
of the AS109 degree-six floor as soon as the producer banks; the reusable
preflight gates.

**Continue in background:** TD6 H19R2 to its registered caps; source/landing
coverage and deck/square work; the unchanged 24-hour web-sweep clock; the
independent user-owned `jc2-lean` formalization lane.

**Hold:** grade-20 export until the cascade/V39 verdict determines whether it
can produce final rather than prefix-relative information; generic `L43`
receiver evaluation until the pullback exists; the row-32/secant proposal;
full `3P-E31` without the arbitrary-standard-pair bridge.

**Stop:** raw receiver exports; V32/V34 orbit prolongation; all `T-a0`
successors; the false dual-as-primal rewrite and its broken V34-point control;
serial affine-Faber height increments; paid AS109 rigid-leaf search without a
flat model; and, conditional on review of the new composition, AS109 `n=2`
and `n=3` arithmetic searches.

## Standing-model decision

**Fable 5 is an equal standing whole-portfolio co-researcher. Opus 5 remains
in the same standing set.** Opus clears the user's marginal-capability test
decisively on this round's campaign evidence: `L43`, the radical cascade, and
the preflight instrument are exact, mutually independent additions that were
absent from Fable's blind report, and Fable subsequently confirmed them by
fresh replay. Opus also identified and withdrew its own overreach from raw
`L43` to an honest receiver and its speculative row-32 bridge. Retention is
based on correctness-adjusted nonduplicate information gain, not benchmark
reputation or report volume.

## Post-snapshot queue and clocks

Post-snapshot results are recorded above as deltas and do not retroactively
alter the blind submissions. V38/V39 are with Fable5, V42 is with Opus5, and
the AS109 floor is with Grok; none blocks the already-launched reversible
successors. Closure-first `H_K00` is live in exact Q and two finite-field AWS
lanes. V42, the AS109 degree-six composition, and a post-close finite-flat
deck/square reduction materially change the ranked state, so they trigger a
follow-on significant-news packet rather than mutating this sealed round.
Absent that trigger, the quiet deadline would be `2026-08-27T19:50Z`. The
broad web-sweep deadline remains `2026-08-28T00:00Z`.
