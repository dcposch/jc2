# K16 ramification gate: the delimited NO-GAIN statement is CONFIRMED; the polynomial target stays OPEN

2026-09-06. Gate lane `k16-ramification-gate-fable5-20260906`, reviewer Fable 5.1, producer Astra. Basis `0d39df3c9fd69c939a8420c54d03228b9077777d`. Gated report `xmodel/k16-square-ramification-obstruction-astra-20260906.md`, body SHA-256 `ee7a62c7811ea866c4569161db21f813e76fac26c4d69203e90210af462d0cd4` (10574 bytes), full-file SHA-256 `8f3b2be660bf6c8d918d6980596506ee111ae1f5cc2d67f5a32db2ac52eabb29`.

**Verdict.** The delimited NO-GAIN statement is CONFIRMED: the exact ramification budget (RB)/(RB-WJ)/(GAP) is satisfiable at every target degree, the complementary-factor term `r(J)-h` cannot be dropped, and the differential identity forces no selected simple root into the critical divisor, at ordinary places or at the fixed place `x=-3/4`. No REFUTED item. No GAP that changes a conclusion; two scope notes are recorded in §7. This gates a delimited failure of one mechanism only, not an all-m exclusion and not a universal failure of rational-map methods. The polynomial `B*eta!=0` nonexistence target remains OPEN. No exit-price assertion is made here, so the receipt for one is expected to be absent.

## 1. Custody and replay

The ten files in `/tmp/jc2-lane.Kcvsrq/inputs` are byte-identical (`cmp`) to their repository copies. Producer seal replayed: exactly one standalone `<!-- BODY-END -->` line (line 233); body through it is 10574 bytes with SHA-256 `ee7a62c7...0d4`, agreeing with the seal footer and with `xmodel/k16-square-ramification-obstruction-astra-20260906.md.artifact.json` (schema `jc2.artifact-finalize/v1`, lease `6c062dda7284bb563f6d5c6899387501`, closed `2026-09-06T13:37:53Z`, mode 0444, full-file SHA-256 as in the header). `box/k16-square-ramification-20260906/sources.sha256` has the custody hash `9b45d370...53e6`; `sha256sum -c` from the repository root reports all six terminal sources OK. `check.py` hashes to `537d63f8...9be6` as custody records.

Checker runs (each under the script's own 512 MiB / 25 s CPU limits):

| command | result | wall / peak RSS |
|---|---|---|
| `python3 check.py` | 11 PASS, `ALL_EXACT_CONTROLS_PASS`, exit 0 | 0.03 s / 13 MB |
| `python3 -O check.py` | 11 PASS, `ALL_EXACT_CONTROLS_PASS`, exit 0 | 0.13 s / 17 MB |
| `python3 check.py --mutate` | exit 1, `CHECK_FAILED: mutated-D-prime-must-fail` | 0.11 s / 31 MB |
| `python3 -O check.py --mutate` | exit 1, same message | 0.54 s / 39 MB |

AST observation frozen from the checker source: 0 `Assert` nodes, 0 `__debug__` references, 12 `require` calls (eleven controls plus the mutation control), failures raised as `RuntimeError`. Optimized-mode verification is therefore genuine; the only textual `assert` is inside a comment. Scope of the checker: one ordinary place `alpha=28/3`, one root `D0=3/4`, one slope `w=1`, order 10, plus scalar fixed-place identities. It is a sign/normalization detector, as the producer says; the evidence weighed below is the degree-uniform argument.

Reviewer artifacts (owned directory `box/k16-ramification-gate-20260906/`, exact `fractions` arithmetic and two hand-written quadratic fields, no CAS): `gate_check.py` (SHA-256 `f7a5290e2fe899cbbbb20bb51b856270d0677643f889fd8e3160f574954de326`, 107 PASS, 0 FAIL, 0.08 s, 13 MB) and `gate_fixed_place.py` (19 PASS, 0.05 s, 12 MB). Outputs are retained as `.out`. No fleet, AWS, solver, shared ledger, tool, launcher, validator or FALLACY edit, external post, live successor lane, or `jc2-lean` access.

## 2. Review item (1): finite-algebra norm map, degrees, multiplicities, `x=-3/4` — CONFIRMED

Re-derived independently. In `k[x]/(R)`, `D` and `x` are units (`R=D(xD/3-1)-1`, `R(0)=-1`), `tau=xD/3` satisfies `tau-1=1/D`, hence `f(tau)=3(D+1)/D^2=x` and `S(tau)=xZ^2/(4tau)=1`, so `Q(tau)=0`. Conversely `R(f(t))=D(f)(tS-1)-1=Q(t)*(1+tD(f(t)))` and `1+tD(f(t))=-Q(1-t)`, so `R(f(t))=-Q(t)Q(1-t)`: the "norm" is the norm from `k[t]` to `k[f]=k[x]` under the deck involution `t->1-t`. The two composites are the identity; the maps are mutually inverse algebra isomorphisms, so every local length, hence every root multiplicity, transfers, including at `t=1/2`. The root accounting `S=(t-1)D(f(t))`, orders `2s, 2s+1` at `t=0,1`, `2a, 2a` at the two preimages of a nonzero root `!= -3/4`, `4a` at the sole preimage of `-3/4`, sums to `4n+1`; `r(S)=2r-delta` follows. Both R and Q have degree N.

Changed-object checks (all PASS): five rational instances at n=3,4 including `A=(x+3/4)^2` (`delta=1`, repeated root at the fixed place, `r(S)=3`), `A=(x+3/4)(x-2)`, `A=x(x-1)(x+3/4)` (`s=2`), and `A=(x-1)^2`; on each, `S=(27/4)t^2(t-1)^3A(f)^2`, `r(S)=2r-delta`, `e(Q)=e(R)`, and `R(f(t))=-Q(t)Q(1-t)` hold exactly, and the unmerged mutant `r(S)=2r` fails whenever `delta=1`. Over `Q(sqrt(-6))` with `A=-(128/225)sqrt(-6)(x^2+1)` (so `Z(-3/4)^2=-8/3`): `R(-3/4)=0`, `R'(-3/4)=4/3`, `Q(1/2)=0`, `Q'(1/2)=-2`, both roots of order exactly one, `e(Q)=e(R)`; the double zero of `R(f(t))` at `1/2` splits one-and-one between `Q` and `Q(1-t)`, and `W=x+3/4` gives `W(f(t))` a double zero while `Q` stays simple. So no fixed-place double counting exists. The D-quadratic at `alpha=-3/4` is `-(1/4)(D0+2)^2`: the unique root is why the place is fixed.

## 3. Review item (2): `e(R)<=2r-delta-1` and the complementary factor — CONFIRMED

`gcd(S,S')` and `gcd(Q,Q')` are coprime because `S-Q=1`, their product divides `S'=Q'` of degree `N-1`, and `deg gcd(H,H')=deg H-r(H)` in characteristic zero, so `e(S)+e(Q)<=N-1`, i.e. `e(Q)<=r(S)-1=2r-delta-1<=2n-1`. This is Mason–Stothers for `S-Q=1` in self-contained form. `r(R)=r(W)+r(J)-h` is inclusion–exclusion on root sets; subtracting from `deg R=2q-1` gives (RB-WJ) and (GAP) exactly, with repeated and common factors included. The complementary term is essential: at `r=n, delta=0` the right side of (GAP) is `q+1` while `r(W)<=q`, so the bound demands only `r(J)-h>=1`, one J-root outside W, which `e(R)=0` satisfies. Verified on every instance: `e(S)+e(Q)<=N-1` and `e(Q)=deg gcd(Q,C)`. Scope note (a): for `r=n, delta=0` the (RB) ceiling `2n-1` equals the trivial ceiling `deg C=2n-1`, so in the generic case (RB) adds nothing to the critical-divisor count; the producer never claims otherwise, and this strengthens, not weakens, the no-gain reading. No omitted repeated-root contribution was found: multiplicity of any Q-root is `1+ord_t C`, and at the fixed place `Q'(1/2)=D0=-2` regardless of `D'`, so a fixed-place R-root is always simple.

## 4. Review item (3): critical divisor, contact, formal recurrence — CONFIRMED

`S'=(27/4)t(t-1)^2A(f)C` with `C=(5t-2)A(f)+6t(t-1)(2t-1)A'(f)`, `deg C=2n-1`, leading coefficient `(5+4(n-1))lc(A)3^(n-1)=N*lc(A)*3^(n-1)`: re-derived and verified on all instances. At a Q-root `t, t-1, A(f(t))` are units, so `Q'=C/[t(t-1)A(f)]` and the excess is consumed exactly by the critical divisor. (DC): `R'(alpha)=D0^2/3+(2t-1)D'(alpha)` and `R'=wJ(alpha)` at a W-root; `J(alpha)=2w+(6t-1)/alpha` since `2/(t-1)-1/alpha=(6t-1)/alpha`. The relation `Q'(t)=3(t-1)R'(alpha)` is a polynomial identity in `D'` at every Q-root (`Q'=D0+3(t-1)(2t-1)D'`, `(t-1)D0=1`), so it holds at `t=1/2` with both sides equal to `D0`; nothing is cancelled. (NC) follows. Verified by an independent truncated-series solve at `alpha=28/3` for BOTH roots `D0=3/4 (t=7/3)` and `D0=-3/7 (t=-4/3)` and two slopes `w=1,-2`, with an arbitrary W germ: (DC), `R'=wJ`, (NC); the mutants with `+D0^2/3`, with the complementary `(t-1)` factor dropped, and with the conjugate preimage `1-t` all fail.

Recurrence (LF) to all orders: the order-j coefficient of `F(D)` is `(2t-1)D_j` plus terms in `D_0..D_(j-1)` and `W`, uniformly in `j` (the `D_j` coefficient comes only from `(2alpha/3)D_0D_j` and `-(2W_0+1)D_j` with `W_0=0`), so the producer's proof is a genuine all-orders statement; the pivot vanishes only at `t=1/2`, and both D0-roots of an `alpha!=-3/4` avoid it because `f(1/2)=-3/4`. Square root and `A=Z/x` need only `D0,alpha!=0`. Fixed place: with `D=-2` constant, `R=(4/3)(x+3/4)` exactly; the slope condition `3w^2-4w-2=0` and the order-k pivot `2(k+1)w-8/3` were verified over `Q(sqrt 10)` as the exact linear coefficient at orders 2–7 for both slopes, with `WJ=R` solved to order 7. Scope note (b): the fixed-place germ is a W-recursion at constant D, narrower than the ordinary-place statement (D is not solved for an arbitrary W germ there); the producer calls it "an explicit formal control", which is accurate. Neither germ is polynomial or carries the origin/infinity markings, exactly as stated. No false local implication was found.

## 5. Review item (4): shape-only controls — CONFIRMED as stated

Squarefreeness of `Q_lambda` for all but finitely many `lambda` is correct (a multiple root needs `S_1'(t)=0` and `lambda^2S_1(t)=1`, a finite set of critical values), and the isomorphism transports it to `R_lambda`. The controls carry only degrees, `W(0)=-1`, `J(0)=1`, and the root counts; the producer lists the missing global markings explicitly (`W'(0)=1`, the leading ratio, J's differential definition) and glues nothing. One further missing marking, verified here: the target's order-1 coefficient of `WJ-R` is `1-3c_2`, so a true solution has `W''(0)/2=1/3`; the shape controls do not carry even this second jet. No statement in the producer's §4 exceeds its scope; nothing there is a counterexample or the excluded `B=0` monomial family.

## 6. Normalization `B=eta=1` with the leading coefficient moving — CONFIRMED

The producer's E is the boundary-product form `E=2xWW'-W^2+((3/2)U-B)W-(3/16)U^2+(3/4)BU+B*eta*x` at `B=eta=1`, matching the checker's literal E and, with `t=m-1`, the ueta (line 103), f3abel (lines 71–74, `omega=1/(4y^2(2d+1))`) and Xempty (line 49) parameters. The scaling lemma (boundary-product §2, its gate §3) fixes `B=eta=1` while `lambda=lc A` stays free; the producer keeps it free. The ratio is re-derived from the top coefficient of `WJ=R`: `lc J=N*lcW+(3/2)lcA^2`, `lc R=(3/16)lcA^4`, so `u=lcW/lcA^2` satisfies `16(4m-3)u^2+24u-3=0`, whose two roots are exactly `u=1/(4(2d+1))` for `d=+-sqrt(m/3)` (identity `(4m-3)+6(2d+1)-3(2d+1)^2=4(m-3d^2)`, verified at unrelated `(m,d)` pairs), root product `-3/(16(4m-3))`. Both roots survive; at `m=3s^2` both are rational (m=12: `1/20` and `-1/12`), and `lc(W)lc(J)=lc(R)` was checked with an arbitrary moving `lc A`. Every unread dependency used here is typed: the scaling lemma and (UT) top balance are consumed from the boundary-product report and Series Prop 3.1 as previously gated; nothing else from the six sources enters the proof.

## 7. Verdicts and surviving scope

- CONFIRMED: item (1) norm map, degrees, multiplicities including `x=-3/4`; item (2) (RB), (RB-WJ), (GAP) with the complementary factor; item (3) (CP), (DC), (NC) at every Q-root including the fixed place, the (LF) all-orders recurrence, the fixed-place W-recursion; item (4) shape-only controls with their listed missing markings; the `B=eta=1` normalization with both `3d^2=m` roots retained and `lc A` moving; custody, seal, and optimized-mode verification.
- REFUTED: none.
- GAP: none affecting the conclusion; scope notes (a) and (b) above are recorded, and neither touches satisfiability, which is all the no-gain needs.

Surviving scope of the NO-GAIN: the polynomial-abc/Mason–Stothers count on `S-Q=1` in `t` (equivalently on `R=WJ` on the x-line), the critical-divisor degree count against the W-divisor, and local formal contact at ordinary and fixed places. Not covered: any all-m exclusion, any universal failure of rational-map or ramification methods, and the global filter (DF) of the boundary-product report, which stays the typed open obligation. The target `B*eta!=0` polynomial nonexistence remains OPEN. No formal-local control is a global point; no shape-only control satisfies the full differential filter (each fails `W'(0)=1`, `W''(0)/2=1/3`, the leading ratio, and J's definition). Fallacy audit: floors were not read as attainment, the ring maps were declared with image checks, the fixed place was handled without cancelling `2t-1`, and no gap was filled by cap or analogy.

## 8. Follow-up

One candidate, distinct from the failed `t`-side count only if it produces a new divisor bound: on the x-line, `F(D)=0` is quadratic in `D` with discriminant `Delta=1+(4/3)x+(8/3)(W+W^2+xWW')`, a polynomial in `W` alone of degree `2q`; a polynomial solution needs `Delta=P^2` and `1+2W+eps*P=(x^3/2)A^2`, which is where the two `d`-signs enter. Because `P | Delta'`, this puts the square condition on `W` (so `r(Delta)<=q`) instead of on `A`. The cheapest test is a minutes-scale exact jet check at `m=4` of whether the three origin markings plus `Delta` being a square already conflict at low x-order; if it merely reproduces (RB), it should be closed as the same no-gain, and no further item in this family is proposed.

<!-- BODY-END -->
