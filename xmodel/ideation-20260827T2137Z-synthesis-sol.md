# Significant-news whole-portfolio ideation synthesis — `20260827T2137Z`

Packet sealed: 2026-08-27 21:37Z  
Coordinator synthesis: 2026-08-27 22:40Z  
Coordinator: Sol Ultra  
Status: **FULL-SPECTRUM ROUND CLOSED; ASYNCHRONOUS REVIEW DEBT EXPLICIT.**

This round allocates research and evaluates model capability.  It does not
prove or disprove JC2.  In particular, a finite face fixture, a target-space
dimension, a necessary passport filter, or a rank stratum is not a global
Keller theorem.

## 0. Custody, blindness, and review topology

Sol, Fable5, Opus5, and Grok46 received the same sealed 46-avenue packet and
submitted before seeing any peer report.  All four submissions passed their
custody hashes:

```text
packet   a492f5eeeb5786f9061bf3730a077b6e256e8386c5dcb42c978bbda5519aac80
Sol      2112a0ad5fbc4fcc5d1a26efd725c01500186f6299b5b591e2849eb0a865ac32
Fable5   edd383ad33838268cc7e700f2aa913dab73eb6744a60133a131a69eb7ef476bb
Opus5    49dd042a15670f082ea4335610150eee3022fc7a37282df200e2ae3a052ea0a0
Grok46   bfd18a275165e1740f883222c1a6e0f8b43cb5e2737d18b8eea01b9c2615d57e
delta    0e09faeb69480b7594864199fbd061291af490cf8e514d1795784fbf00ad9bee
```

The post-seal truth delta was not retrofitted into any blind report.  The
following hostile reviews are complete:

```text
Opus ideas / Sol       0ccdc259358267fc805c2af477bc6b0f48620dbd2e76c016cdb85caee1ba3cba
Fable ideas / Sol      d7e810f75ca2158fa8839a60e2005a85e655c5ad9309dda35963f2daee14ea3b
Fable report / Opus    8c539104f61a2fb88acd618d2fb7e86d324ed6d18ca56217be71b6649803ba93
Opus report / Fable    b3f3886c7783ce22e060865206a700f0aab356caf6f708f75f355e99b684517a
Grok ideas / Sol       c72091731b9de04d928c858bd26589a7bd551ac4ce593243539af6c76d2d9b89
Sol ideas / Sol        19ca246614dadcae99f739352c55908989ac0dd3d78cf03efbc6676c01133a99
post-seal exact / Sol  8bad7ca04ae9a6f45efde305b208309f528173820250d20dab67d00bc32d73a4
Fable T3 / Sol         5beb555075c662e76f8b86062efdf89b2c3a4dd0eb80ffd52fbf3c38bf130d55
```

Fable5's independent review of the Opus5 report was still running at close,
so the round correctly did not block on it: Opus's load-bearing torsor theorem
already had a different-model hostile review, and Opus had independently
reviewed Fable's strongest new mathematics.  The late report then sealed at
`b3f3886c...`.  It independently confirms the torsor theorem and the positive
Opus capability verdict.  Its new linear-vacuity claim is separated as a
post-close provisional event in section 9 and is not silently folded into the
sealed-round evidence.
The Opus cross-review header names `ops/adapters/claude.sh`; the actual pinned
Opus run used `ops/adapters/opus.sh`.  This is a provenance typo, not a model
or mathematical ambiguity.

## 1. Round verdict

The round changes neither the JC2 verdict nor the three global bottlenecks:

1. a universal/cofinal landing or total-degree theorem;
2. a typed exact-pair, two-chart, all-root route from local face data to the
   global residual forest, including truncation and leaf/place fidelity; and
3. a decided genuine survivor endpoint or a source-compatible counterexample
   lift.

It does add one promoted structural theorem, one promoted lower local theorem,
and one promising effective-compression direction:

- the four periodic upper gate receivers are the character summands of one
  geometric `mu4` torsor;
- the lower endpoint ODE has a uniform two-root local analysis forcing
  `gamma == 3 (mod 4)`; and
- the algebraic quarter-root series admits a reviewed closed all-row
  coefficient formula, making a P-recursive gate engine a concrete next
  target.  Raw coefficients are P-recursive; gate coordinates and an
  effective finite-decision bound remain open.

## 2. Opus5's torsor theorem — promoted core, extrapolations rejected

Write

```text
H = product_i (X-a_i)^(e_i),       U=A1-Z(H),
V_H = {p^4=H} -> U,                c=gcd(4,e_1,...,e_r).
```

Over characteristic zero after adjoining `mu4`, `V_H -> U` is a finite
etale, possibly disconnected `mu4` torsor, and

```text
H1_dR(V_H) = direct_sum_(j=0)^3 H1_dR(U,nabla_j),
b1(V_H)    = 4(r-1)+c,
dim H1_dR(U,nabla_m) = r-1+k_m.
```

Here `k_m=1` exactly when `4 | e_i m` for every `i`.  Thus any four
consecutive character receivers total `b1(V_H)`, and the frozen twelve-row
totals are exactly `42/51/12/87` for P/Q/the one-root control/the squarefree
degree-eight control.

The theorem is a receiver-space statement, not a row-independence theorem.
The infinite tower maps infinitely many nonlinear coefficient functions into
periodic copies of four spaces.  It does not make realized codimension equal
to target dimension, produce a global linear solver, or finitely decide the
tower.  The only hard full-124-`F` conclusions from the height count are the
not-before bounds P58 and Q52.  Collapse at either row is not proved.

Opus's further claim that free `G` slots prevent the `F` tower from deciding
the endpoint is false: the inhomogeneous `D22=1` target couples the two sides,
and the frozen `G22` window is empty.  Its proposed linear-time `GATE-MARCH`
also fails after the last new `F_n` block: later rows are nonlinear conditions
on old variables, with no fresh triangular handle.  The post-close E0 review
sharpens this: the licensed new-slot map already vanishes at every `k_m=1`
row from 28 onward, so a receiver-dimension-rate march fails well before the
window ends.

## 3. Fable5's compression direction — exact core, missing uniform theorem

Fable identified the exact gauge relation

```text
T_m(HY)=H T_(m+4)(Y),
T_m(Y)=4H Y' + (m-12)H'Y.
```

It induces `coker(T_(m+4)) -> coker(T_m)` by multiplication by `H` and is an
isomorphism after localizing at `H`.  This is the correct mechanism for the
localized upper gate receivers.  It does not identify polynomial cokernels,
frozen polygon windows, or the upper endpoint problem; the inverse uses
`H^-1` and can leave every finite window.

The proposed coherent four-sector packaging is a valuable new direction but
was not constructed in the blind report.  A natural candidate follows from
four-section extraction of the known algebraic series `Q=P^2`:

```text
Gamma_a(X,z) = sum_(k>=0) H^k q_(a+4k)(X) z^k.
```

Opus's post-seal cross-review supplied the sharper all-row formula, and an
independent exact replay confirms it.  Let `phi(t)=F(t)^(1/8)` be the
pre-reversion root, let `t=s phi(t)`, put `P(s)=phi(t(s))`, and write
`Q(s)=P(s)^2=sum q_n s^n`.  Lagrange inversion gives

```text
q_n = (2/(n+2)) [t^n] phi(t)^(n+2)
    = (2/(n+2)) [t^n] F^((n+2)/8).
```

This explains the character and triangular leading term uniformly and fixes
the apparent `q3` mismatch: the exponent varies with `n`.  Since

```text
P^8 = sum_(n=0)^14 F_n s^n P^n,
```

`Q` is algebraic and its raw coefficient sequence is P-recursive.  That
recurrence has `X`-dependent coefficients, so applying a de Rham quotient or
residue projection does not automatically preserve it.  A creative-
telescoping/finite-reduction theorem and then constructible control of
singular recurrence indices are still required.  Thus no pointwise or
uniform effective `N0` is yet proved.  Fable's Krull argument is a
non-sequitur for effectiveness; Noetherianity gives only abstract finite
ideal generation, and rationality of the proposed residue pairings was not
established.

## 4. Lower face — one real Fable theorem and a scope split

For the exact endpoint ODE

```text
4K g' - 3K' g = 4K,       K=xi (xi-rho)^gamma,
```

Fable's direct local calculation is independently confirmed.  It forces
`g(0)=g(rho)=0`; writing `g=(xi-rho)h` gives

```text
h(rho)=4/(4-3gamma).
```

The leading-degree alternatives are `deg g=1` or
`deg g=3 deg(K)/4`.  The degree-one alternative is a mirage because a linear
polynomial cannot have both forced roots.  Hence polynomial solvability
requires `gamma == 3 (mod 4)`.  This is uniform in `gamma`, strengthening the
earlier finite-range necessity, but it proves neither sufficiency nor a GGV
family exclusion.

Fable's `29 versus 36` LF40 prediction is rejected.  Localized receiver
dimensions are generator ceilings, not independent equations, and the 36
counted `G` slots are not free after the earlier determinant rows.  Sol's
post-seal transformed-series calculation instead gives the provisional
F-side relation

```text
w_(n+17)' = (3-n) q_n / 24,
```

so `n=3` (raw label 20) is resonant.  Opus separately computes the polynomial
compiler cokernel as seven per nonresonant row.  These are different objects,
not competing verdicts: the first is a localized necessary gate and the
second is the literal windowed compiler.  Their exact interface remains
review debt.  No obstruction count currently predicts whether LF40 is unit
or proper; the two exact AWS engines should decide or cap without model bias.

## 5. Other proposal dispositions

- **Grok `FACE-NC`: stop.**  Nearby cycles of the zero-locus family live on
  `Z(H)`, not on `U`, and the displayed first-order family is
  `H(1+tV)`, locally constant as a divisor.  Moreover distinct `mu4`
  characters split semisimply, so rows 23 and 24 cannot acquire the proposed
  equivariant nonsplit extension.  A genuine higher-rank client would need an
  explicitly typed connection or Gauss--Manin object.
- **Grok `FACE-CHAR`: stop pending a base map.**  The face local system and a
  Keller passport live over different bases.  No loop map or character
  factorization is supplied, and a `-1` eigenline does not imply an odd total
  permutation.
- **Sol `EXIT-DIFF`: refile only as an intrinsic local-energy conjecture.**
  The proposed module is untyped and even its optimistic sum is bounded by a
  quantity already linear in unbounded genus and `td`.  It is not an
  `RPMC(C)` or degree-ceiling bridge.
- **Sol `BI-FACE-SRC`: keep as a provenance/separation audit.**  The common
  442-slot raw lattice and chart regradings type-check, but the current frozen
  upper branch-P and lower LF40 normalizations already collide at the linear
  coefficient `f_15_52`: upper forces zero while lower forces
  `-14 a rho`, with `a rho` inverted.  A corresponding `G` collision also
  appears.  Independent same-model replay confirms that the localized graph
  fibre product is the zero ring, with current upper-manifest custody repaired
  to `67c40382...`.  Different-model promotion review is still required.  The
  maximum claim is only that these two literal normalized fixtures are not
  faces of one shared raw point; arbitrary branch-P and FACEPIN families may
  have coordinate or normalizer freedom absent from this graph.
- **G2 corridor: delete the stale route.**  VGG Corollary 7.4 contains native
  same-edge information already subsumed by 7.1/7.2, not the missing physical
  chart.  Minimal VGG re-selection is chart-rigid.  Retain L1 face-power and
  L2 multiplicity custody; L3 truncation, L4 exact-pair two-chart coverage,
  and L5 deck/leaf-place fidelity remain open.  Under its own first clause,
  `EXIT-RPMC(C)` is equivalent to `RPMC(C)`.
- **Finite ends: retain a necessary filter only.**  Chau's finite-end identity
  is source-confirmed and its two-coordinate coupling is real.  The marked
  component congruence prunes the clean residue-A census `169 -> 48`, but all
  48 survive.  It is not a monodromy bound or a family kill.

## 6. Model evaluation and permanent roster decision

**Opus5 passes the admission test and is permanently added to the automatic
equal-standing full-round roster alongside Fable5, Sol, and Grok46.**  The
decision is based on blinded same-input mathematical output after
deduplication, not benchmark reputation or model identity.

| Researcher | Distinctive value this round | Hostile-review correction |
|---|---|---|
| Fable5 | coherent fixed-receiver packaging; uniform lower local theorem; strong prefix/custody discipline | no effective uniform `N0`; localization cannot replace polynomial/windowed problems; LF40 budget inference and stale G2 corridor rejected |
| Opus5 | torsor/cohomology theorem; source-level G2 correction; all-row Lagrange formula in cross-review | target dimensions do not imply independent cuts, global linear march, collapse, or a free-`G` no-go |
| Sol | global scope integration; finite-end and G2 source audits; lower resonance and bi-face separation candidates | `EXIT-DIFF` fails as a degree bridge; bi-face and lower interface remain provisional |
| Grok46 | aggressive coupled-client and passport proposals; useful q1-compatible allocation idea | both new face promotions fail category/base typing |

Fable5 and Opus5 are materially complementary.  Fable is especially strong
at compiler/source custody, local algebra, and finding lifecycle or scope
leaks.  Opus adds nonduplicate global geometric reframing and closed-form
derivation.  Both generate overclaims; neither receives evidentiary credit
without the same hostile review imposed on every other lane.

## 7. Portfolio and execution decisions

The principal avenue changes are:

| Avenue | Decision | Reason |
|---:|---|---|
| 1 | continue exact endpoints; modestly raise tower compression | upper, LF40, and K00 are genuine finite decision objects; algebraic/P-recursive compression is the best new route to the infinite gate tail |
| 2 | lower VGG-corridor branch; retain exact-pair backbone | L3--L5, landing, `RPMC(C)`, and the cofinal ceiling remain the proof bottleneck |
| 3 / 16 | raise rank-one algebraic packaging; stop `FACE-NC` | torsor/index structure is exact; no typed coupled rank-two client yet exists |
| 7 | modest raise | finite-end identity and marked profiles are exact but currently only filters |
| 19 | continue | source-open, nonzero, full-`P6`, rank-exact K00 remains the strongest finite counterexample lane |
| 25 | hold | no face-loop-to-passport map is typed |
| 26 | unchanged high priority | no new mechanism bounds the cofinal total degree |
| 36 | continue only on compressed survivor schemes | ambient random or fewnomial search remains stopped |

All heavy algebra remains on AWS.  At close, five independent exact upper
lanes on Box03, LF40 R1 on r6b, its `std/dp` cross-check on Box02, and the K00
full-`P6` rank-exact lane on r6a were healthy with zero swap and no verdict.
The exact 466-generator upper prefix quotient was additionally launched on
spare Box02 capacity after exact-Q cofactor replay proved equality with the
full 513-generator ideal.  Its first launcher attempt failed closed before
`RUN_STARTED` because GNU awk rejected a variable name; the one-token repair
was re-frozen and the actual lane began at 22:30:48Z.  Proper subsets remain
silent; only a replayed raw unit certificate or a full witness is evidence.

## 8. Immediate queue and clocks

1. Promote the reviewed all-row raw coefficient formula at its notation-
   guarded scope; build and independently review the missing gate-coordinate
   recurrence.  Send the bi-face contradiction to a different model before
   lifecycle promotion.
2. Propagate the now-confirmed post-close E0 linear-realization correction and
   begin the required follow-on full round without pausing unrelated research.
3. Let the AWS endpoints decide or cap.  If any exact lane returns, launch
   output review before dependent expansion.
4. Build the fixed-sector P-recursive recurrence and ask for an explicit
   finite prefix bound on one frozen P or Q stratum before attempting uniform
   family language.
5. Keep the proof-side integrator on L3--L5 / `RPMC(C)` / total-degree
   interfaces, not on another renaming of the same corridor.

This full round closes at 2026-08-27 22:40Z.  The next 12-hour backstop is
2026-08-28 10:40Z; any terminal AWS verdict, load-bearing review reversal, or
credible external result triggers an earlier round.  The last broad web sweep
is `xmodel/websweep-20260827T1348Z.md`; its quiet deadline remains
2026-08-28 13:48Z.

## 9. Post-close event — Fable linear-vacuity theorem under review

Fable5's late cross-review independently re-proves the torsor theorem and
judges Opus's unique capability real, complementary, and sufficient for
admission.  It also proposes a new exact theorem not present in either blind
submission.  In the licensed exact-`p`-power presentation, the `F_n`-linear
term is

```text
(F_n/4) H^((n+2-j)/4-2) p^j,       j=m mod 4.
```

Whenever `k_m=1` and `m>=28`, the polynomial gauge makes this linear term
class-exact.  Different-model exact reconstruction confirms Fable's P/Q rank
tables: `26/35` through row 34 and total frozen degree-one ranks `27/37`, so
uniform licensed stable-range equality stops at row 27.  Every later cut must
come from nonlinear carry, for which no generic nonvacuity or independence
theorem is known.  This is an additive scope correction to the old
operational gate schedule, not a refutation of the torsor/index theorem, the
abstract height ceilings, or any live rows-1--22 endpoint computation.

The confirming review is
`xmodel/ideation-20260827T2137Z-fable-linear-vacuity-hostile-review-sol-ultra.md`
(`5beb5550...`).  It also rejects Fable's proposed raw `D28` `0 versus 4`
experiment: the prompt had the wrong affine target, symbolic `F6` makes the
elimination bilinear rather than a rational linear solve, no compatible lower
target point is supplied, and raw compatibility would test polynomial
descent/window support rather than the class presentation.  The correction is
now canonical and triggers a follow-on full round; unrelated AWS work
continues.
