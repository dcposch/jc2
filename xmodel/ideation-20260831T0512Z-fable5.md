# Fable 5 independent significant-news ideation — 2026-08-31T05:12Z

Author: Fable 5, equal-standing independent whole-portfolio researcher  
Packet: `daceadc1d5c22ec752b651f8be53860502ad64020134091c8a765aabf6bdb991`
(`xmodel/ideation-20260831T0512Z-significant-news-packet.md`), hash reproduced
before reading.  
Git basis: `e66542021690fcc7a1100482716804b2d8f174f7`.

Custody: all seventeen charged SHA-256 values in the packet were reproduced
byte-exactly before any charged file was read (the four ledgers, the eight
branch-front artifacts, the four quartic-horn artifacts, the degree-six
RESULT, the web sweep, and the post-ledger corrigendum). The corrigendum
`be4c287f...` is charged: the cable artifact's reviewer hash `72cf5510...`
is misbound and the intended GPT review is sealed `440c83af...`; the
spectator artifact's predecessor hash is `60ba8f433f4e...`. No sibling model
report, log, `.run`/`.run.v2` receipt, or excluded workspace was inspected.
No repository file other than this report is written. Small exact in-memory
checks (Fraction arithmetic; polynomial determinant evaluations) were run;
no CAS, AWS, or web access was used.

Evidence-tier tags used throughout: **[PROMOTED]** (in `AUDIT.md` after
different-model review), **[PROVISIONAL]** (sealed producer-exact,
review-gated), **[NEW-DERIVATION]** (derived in this report from charged
interfaces; conjectural until produced and hostile-reviewed),
**[CONJECTURE]** (stated without derivation), **[COMPUTATIONAL]** (verified
by my in-memory exact replay only).

No exit-price assertion is made in this report, so no `charge_basis` line is
declared (per `FALLACY-v2.md`, absence is the correct state).

---

## 1. Compact state reconstruction

The canonical program: for a hypothetical noninvertible Keller map, normalize
`C[F,G]` in `C(x,y)` to get `A2 --j--> Y --pi--> A2` with `j` an open
immersion and `pi` finite flat of degree `d=[C(x,y):C(F,G)]`. **[PROMOTED]**:
some connected component of the reduced branch `B` has nontrivial `pi1`
(all-degree); `d != 3`; `d != 2` follows one line earlier (a `C2`-Galois
cover has no unramified sheet over its branch, contradicting the generic
companion sheet that `p_D(F,G)` supplies — this is the same fixed-block
contradiction as `BD-GAL`, specialized to the whole map; I flag it because
no ledger line states `d != 2` explicitly, and one should).

**[PROMOTED]** rank four: `b1(B)>=1`, at least one `(2,2)` fibre, `G=S4`,
`n22>=h-k+1`, `m>=2h-1`; minimal row `h=k=1, beta=n22=1, n4=0`, with only
`m=|T31| in {0,1}` surviving the Euler arithmetic (threat map
`cf157e17...`, uncharged in this round's packet — see §3.4).
**[PROMOTED]** (per the overlay and the 05:19Z audit entry): for an
irreducible one-place `A1`-normalized branch with transitive meridional
transposition `S4`, `Delta_aff(B)>=3`; and the `Pic(D)[3]` = Fox/localization
kernel identification. **[PROVISIONAL]**: the genus-three cable census
(`T(3,4)` sole survivor after the `b1=1` filter), the complete `m=0`
obstruction (`Delta_aff>=4` in the no-cusp row), the `(4,3)` normal-form
classification with its four `b1=1` strata, and the spectator control
(nonzero affine sign-Fox class, injective two-cusp restriction,
`Pic(Dsp)[3]=0`).

Quartic one-cusp horn: **[PROMOTED gate]** function-pair reduction
(`mu=d1`, saturated Orevkov–Chau budget, immersive self-identified companion
`C0`); **[PROVISIONAL]** inverse-Kummer successor (residual class is the
inverse class; collision = monogenic index locus); the surviving global gate
is the index-four subfield `K=C(F1,F2)` of `M=C(X,S)` with
`F_i in O(U)` (single-valuation intersection), logarithmic Jacobian
`J_(X,S)(F1,F2)=c/(mu X)`, and the saturated `(3,1)+(2,2)` boundary census.
The degree-six invariant cell is empty with two parameter-independent
Jacobian coefficients (`[x^2 y]J=8`, `[x^4 y^2]J=4`) **[EXACT, AWS
cross-checked]**. Zero-cusp double plane: connected etale `C3` torsor forces
`0 != alpha in H^1_et(D,Z/3)^-`; nodal control excluded; one-cusp row
ramifies at the cusp.

External: no characteristic-zero resolution; Strinz 08-30 note is prose-tier
cross-pollination only. Operations: 336 vCPUs all-in, 144 running, four
running `r6*` idle; Box01 busy on GB21; launchd-backed lane wrapper now
survives coordinator loss; reviews asynchronous.

---

## 2. Avenue-inventory reassessment (obligation 1)

The 46-row survey predates the branch-descent pivot. Corrections to record
at the next synthesis (dispositions relative to the 2026-08-21 table):

- **Row 2 (Sigray sheet ladder) — retype, not "active proof backbone."**
  The campaign's live backbone is now the canonical-normalization branch
  program, which is a composite of rows 26 (blocks/primitivity), 31
  (ZMT/integrality), 30 (affine-surface classification), 27 (links at
  infinity), 25 (passports), and 7 (`A(F)`/Chau). Recommend registering it
  as a first-class row ("47: canonical branch topology") so dispositions
  stop being smeared across six rows. Sigray/GGV machinery: dormant,
  control-tier.
- **Row 27 (links at infinity / splice) — raise.** Formerly "template
  survives"; now load-bearing: Neumann–Rudolph goodness, minimal Seifert
  genus, and Schubert cabling are the engine of the delta theorems.
- **Row 25 (Hurwitz passports) — raise, with the coupled instantiation.**
  The old objection "Riemann existence is generous" is obsolete in this
  program: the `S4` meridian-transposition coloring census is a *coupled*
  passport computation that returns exact zeros (`T(2,7)`, winding-three
  cables) and kills rows. Sol's dormant 7/10 "coupled two-coordinate CSP"
  is exactly what the census became.
- **Row 39 (cohomological cluster) — reopen narrowly.** Grok's "refuse to
  start until a 10-line class is written" is now answered: the 10-line class
  exists and is `H^1_et(D,Z/3)^-` with the local-extension condition. The
  reopened scope is only the double-plane/Fox instantiation, not slogans.
- **Rows 29/33 (LND / symplectic residues) — reopen narrowly, twisted.**
  The untwisted action-residue gate returned `COSTUME` because on `A2`
  everything is exact. The pseudo-plane `U` has `pi1(U)=Z/mu` and
  `K_U=0`: the logarithmic Jacobian equation is a genuinely *twisted*
  client (finite-monodromy local systems), which is precisely the revival
  condition the `COSTUME` verdict named. See §5.2.
- **Row 7 (`A(F)`) — mark absorbed/vindicated.** Chau's parametrization is
  now consumed at every step; the once-"unpinned" place data of `A(F)`
  components is resolved (normalization `A1`, one place) inside the program.
- **Row 32 (collision ideal) — dormant connection now has a client.** The
  three-generator saturation-free presentation `I+(det A)` applies verbatim
  to the invariant-cell Keller systems of §6 (off-diagonal of `H` on the
  cyclic cover); use it as the equation generator there.
- **Rows 4/36 (K00 / guided search), 19–21 (char-p/p-adic) — hold dormant
  with named reentry points** (§8): K00 `e2m2,h10=2` and `(4,1)`-G11 faces;
  AS109 correction-degree-twelve frontier. No relaunch this round.
- **Rows 5, 8–18, 22–24, 34–46 — unchanged** (dead, low, or
  control-tier as recorded).

Routes genuinely independent of the rank-four front, in priority order:
(i) the all-degree quantitative delta floor (§8.1 — attacks every degree
simultaneously); (ii) the reducible-branch/source-forest ladder (`h>=2`
rows, untouched by all one-place machinery); (iii) counterexample-facing
programs (K00 faces, invariant-cell search-as-disproof, char-p degree-12);
(iv) the dormant Sigray/GGV control stack.

---

## 3. The surviving `m=1, Delta_aff=3, T(3,4)` row (obligation 3)

This is the round's highest-value target: closing it closes the entire
irreducible charged genus-three row. I propose **two independent kill
routes**. Route A is new in this report and is purely Fox/normal-form; Route
B is the overlay's ramification-divisor conjecture, which I audited
per-stratum and found sound with one pleasant simplification and a short
list of interfaces to verify. Both must be produced as sealed artifacts and
hostile-reviewed; nothing here is promoted.

Shared frame **[from charged artifacts]**: `B` irreducible, normalization
`A1`, `Delta_aff=3`, `b1=1`, infinity knot `T(3,4)`, delta-sequence `(4,3)`,
normal form `X=t^3+a t`, `Y=t^4+b t^2+c t`; coincidence cubic
`G(s)=s^3+(2a-b)s-c` with diagonal locus `D(s)=-3s^2-4a`; the complete
`b1=1` strata (t34 packet (3.6)):

```text
I.   contact-three two-branch point               (no unibranch sing.)
II.  cusp at a conductor endpoint + contact two   (no second sing. point)
III. ordinary node + two ordinary A2 cusps        (the spectator orbit)
IV.  ordinary node + one (2,5) unibranch point.
```

Row data **[charged]**: `n22=1` with the `(2,2)` fibre exactly over the
unique two-point normalization fibre; `n4=0`; `m=1` means exactly one
`(3,1)` value, whose local decomposition group is `S3` fixing the fourth
sheet.

### 3.1 Route A [NEW-DERIVATION]: local-determinant + stratum-rigidity + Fox restriction

Step A1 (*where can the `(3,1)` value sit?*). A `(3,1)` fibre cannot sit
over a smooth point of `B` (one meridian generates the local group, image
`C2`, partition `(2,1,1)`), and cannot sit over the two-point-fibre value
(that value carries the charged `(2,2)`). So the `(3,1)` point `z` is a
*unibranch singular* point of `B`.

Step A2 (*local 3-colorability*). The rank-3 local factor is a connected
3-sheeted cover of the local ball branched at the unibranch germ, with
meridians mapping to transpositions and transitive local image `S3`. That
is exactly a nontrivial Fox 3-coloring of the local link of `B` at `z`,
hence requires `3 | det(local link)`. Within the delta budget the unibranch
options are the `A2` cusp (link `T(2,3)`, `det=3`) and the `(2,5)` point
(link `T(2,5)`, `det=5`). **The `(3,1)` point must be an ordinary `A2`
cusp.** In particular stratum IV cannot carry `m=1` (its only unibranch
point has determinant five), and strata I/II have no admissible unibranch
point at all: in II the cusp sits at the conductor endpoint, where the
charged packet already places the `(2,2)` fibre, and the two branch factors
exhaust rank four (`e_a+e_b<=4` forces `(2,2)`), leaving no second singular
point for a `(3,1)`. So `m=1` forces **stratum III**.

Step A3 (*stratum III is one affine orbit*). With `a=-3q^2/4`, `b=-q^2/2`,
`c=0`, the reparametrization `t -> (q/2)t` and the affine target scalings
`X -> (q/2)^3 X`, `Y -> (q/2)^4 Y` carry every `q != 0` member to `q=2`,
i.e. to the finalized spectator `(t^3-3t, t^4-2t^2)`. (Checked exactly; the
scalings send `(a,b,c)` to `(-3,-2,0)`.) So the spectator computation
covers the entire stratum.

Step A4 (*Fox over-determination*). The `S4` representation composed with
`S4 -> S3` is a sign cocycle `z in H^1(A2-B, L_sign)` (charged dictionary,
`60ba8f43...` §3). At the `(3,1)` cusp the local image is `S3`, so the
local restriction of `z` is nonzero; at the other cusp the fibre is
`(2,1,1)` (`m=1`, `n4=0`), local image `C2`, so the local restriction is
zero; and `z != 0` globally (a coboundary would make the whole image
conjugate to `C2`, which cannot contain the local `S3`). But the charged
spectator computation gives `dim H^1(A2-Bsp, L_sign)=1` with the two-cusp
restriction map `lambda -> (lambda,lambda)`. A class restricting to
`(nonzero, zero)` does not exist. **Contradiction; the `m=1` row is
empty.**

Why Route A *sees `S3`* rather than recycling the invalid `C2` duplicate:
it never argues at the `(3,1)` point at all. It *prices* the `S3`: a local
`S3` is exactly a nonzero local restriction of the one-dimensional global
sign class, and the diagonal restriction lock `(lambda,lambda)` makes
"`S3` at one cusp, `C2` at the other" over-determined. Overlapping
transpositions at the cusp are fully permitted; they are what the nonzero
local restriction means.

Dependencies to charge in the producer: t34 classification `2b3cc0b7...`
(strata completeness — the depressed-cubic root patterns are exhaustive,
"not merely a list"); spectator `a25d4b2f...` + corrigendum `be4c287f...`
(the `F3 -> F3+F3` diagonal); Fox localization `60ba8f43...` (dictionary
(3.1)–(3.2)); quartic integration `5d7df7ce...` (row data); the threat map
`cf157e17...` for `m<=1` (see §3.4). New lemmas to write: the
`(3,1) => local 3-coloring => 3 | det` step (elementary, but must be stated
with the local decomposition group, not divisorial inertia); the stratum-II
rank-exhaustion step; the affine-orbit rigidity of stratum III.

Weakest failure mode: the local-image dictionary at the *second* cusp — if
a `(2,1,1)` fibre over a unibranch point could have local image larger than
`C2` (it cannot: the rank-2 factor's group is `C2` and rank-1 factors are
fixed by the promoted henselian argument, but the producer must say this
via the fibre algebra, not via the curve). First falsification test: apply
Route A verbatim to the spectator's own finite-free quartic (which exists,
with *both* cusps `(3,1)`-like, `m=2`): Route A must *not* kill it — and it
does not, because `m=2` demands `(nonzero,nonzero)=(lambda,lambda)`, which
is realizable; this is exactly the spectator's observed `S4` cover. That
the constraint pattern distinguishes `m=1` from `m=2` on the same curve is
a sharp built-in control.

### 3.2 Route B [audit of the overlay conjecture]: full ramification divisor of the cubic projection

The overlay's proposal survives my per-stratum audit, with these findings:

1. **The `a=0` (double critical point) case is vacuous inside `b1=1`.**
   Every `b1=1` stratum forces `a != 0` (I: stated; II: `a=-3r^2, r!=0`;
   III/IV: `a=-3q^2/4, q!=0`). So the "double critical point at the unique
   `T31` value" limb of the overlay never occurs in this row; the audit
   should record it as unreachable rather than proved-and-used.
2. **Stratum III:** `X'=3t^2-3q^2/4` has roots exactly `+-q/2`, which are
   exactly the two cusps (checked: `Y'(+-q/2)=0`). The `(3,1)` cusp absorbs
   one critical point; the *other* critical point is the second cusp, which
   is `(2,1,1)/C2` (`m=1`, `n4=0`), so its two coalescing vertical meridians
   have the same image — a duplicate — and three transpositions with a
   duplicate span at most two edges and are intransitive. The Hurwitz-tail
   subgroup invariance (Nielsen moves) transports this to the based frame.
   Coincidence checks (verified exactly): the conductor `X`-value is `0`
   (endpoints `+-sqrt(3)q/2` under odd `X`), the cusp values are
   `-+q^3/4`; all three special `X`-values are distinct, so the local disk
   frames are clean.
3. **Stratum IV:** critical points `+-q/2`; `q/2` is the `(2,5)` point;
   `-q/2` is immersive (`Y'(-q/2) != 0`), is not a conductor endpoint
   (endpoint product would need `3q^2/4 = 13q^2/4`), and has a singleton
   fibre, hence a smooth image point with a vertical tangent: `(2,1,1)/C2`
   duplicate again. (Under Route A stratum IV is already dead at Step A2;
   Route B kills it independently, which is worth keeping as redundancy.)
4. **Strata I/II force `m=0`** exactly as in Step A2, so Route B need not
   treat them.

Route B *sees `S3`* because the `S3` point is structurally forced to *be*
one of the two `X'`-roots (both derivatives vanish at a cusp), consuming
one critical point; the kill is executed at the other critical point, whose
row-typed local group is abelian (`C2`, or the node's `Z^2` if a critical
point were a conductor endpoint — excluded in III/IV by the checks above
but needed in the general lemma). No duplicate claim is ever made at the
`(3,1)` point.

Interfaces still to verify before sealing Route B (the overlay's demand):
the fibre-length inequalities excluding critical conductor endpoints
(same as the `m=0` packet §3, unchanged); the Hurwitz-invariance
application when the special `X`-value's disk contains one special point
only (guaranteed by the distinct-values check); and the statement that
vertical-tangent smooth points have local `C2` (one meridian class).

### 3.3 Third and fourth options (lower priority)

A compactification/intersection route (resolve the quartic cover over a
compactified `T(3,4)` boundary and contradict intersection numbers) and a
polynomial-normal-form route on the cover side both look strictly more
expensive than A/B and are not developed here. If A and B both fail review
at the same interface, the failure will name the right third route.

### 3.4 Producer obligations

One sealed artifact `M1-T34-CLOSURE` containing both routes, charging the
five artifacts above **plus the threat map** `cf157e17...` (the `m<=1`
arithmetic is the only reason "closing `m in {0,1}` closes the row," and it
is not in this round's charged set — a promotion that omits it would have a
scope hole). Replay: exhaust the `6^3` transposition triples per case,
verify the stratum arithmetic on an exact grid, and mutate
(`--mutate-allow-nonzero-second-cusp`, `--mutate-skip-det-gate`). Then a
different-model hostile review bundled with the four provisional
genus-three inputs (§11).

---

## 4. Genus-four successor for the `m=0` row, and the uniform ladder (obligation 4)

### 4.1 What is provable symbolically before any enumeration

**Census [COMPUTATIONAL, verified here].** Conductor-eight one-place
delta-sequences (AG–S formula, `h<=2`):

```text
h=1: (9,2) -> T(2,9);   (5,3) -> T(3,5);
h=2: (6,4,5) -> C_(2,5)(trefoil);   (9,6,2) -> C_(3,2)(trefoil).
```

Genus-four prime iterated cables not on this list — `C_(4,+/-1)(trefoil)`
and `C_(2,+/-1)(genus-two cables)` (the first depth-three towers) — have no
conductor-eight delta-sequence, so the semigroup gate kills them before any
topology, exactly as `(9,6,1)` died at genus three. This asymmetry (knot
census strictly larger than polynomial census) grows with genus and is the
first place the successor packet should generalize: a freeness lemma "no
delta-sequence realizes winding-one-after-nontrivial-tower or
`C_(p,1)`-type final steps" would prune every future genus at once.

**Determinant gate [COMPUTATIONAL].** `det T(2,9)=9`, `det T(3,5)=1`,
`det C_(2,5)(tref)=5`, `det C_(3,2)(tref)=9`. So `T(3,5)` and
`C_(2,5)(tref)` die by `3 | det` (double-branched-cover lemma, promoted
interface).

**Meridional-rank gate [NEW-DERIVATION, easy].** `T(2,9)` is two-bridge, so
its group is generated by two meridians; two transpositions generate an
intransitive subgroup of `S4`. Dead. (This gate is degree-uniform:
`T(2,n)` dies for every `n` and every `S_d`, `d>=4`.)

**Sole survivor of the desk gates: `(9,6,2) = C_(3,+/-2)(trefoil or
mirror)`, `det 9`.** Next steps in order:

1. **Coloring census** (desk, replay-pattern of the frozen cable script,
   braid `X_3^(+-3)(s1 s2)^(q-...)` on six strands with exact Burau
   control): count transitive meridian-transposition `S4` colorings. The
   winding-three genus-three rows all returned zero; if `C_(3,2)` also
   returns zero, **the entire irreducible `m=0` genus-four layer closes by
   desk computation** and the packet should immediately test the
   conjecture "no winding-`>=3` cable of a *nontrivial* companion admits a
   transitive meridian-transposition `S4` coloring" (`T(3,4)=C_(3,4)(unknot)`
   shows the nontrivial-companion hypothesis is essential).
2. If colorings exist: **normal form + self-pair scheme** for `(9,6,2)`
   (degrees 9 and 6; approximate-root reduction as in the `(6,4,3)` case)
   and the `b1=1` filter. The `(6,4,3)` kill came from a forced
   at-least-two-self-pairs cubic; the analogous statement for `(9,6,2)` is
   the first genuinely new algebra, and its failure would itself be a
   useful control.
3. **Boundary/affine Fox gates**: the local restrictions (§3 machinery) on
   whatever singularity strata survive `b1=1`; the delta-four unibranch
   menu is `(2,7)` (`det 7`, dead as a `T31` site), `(2,5)+...`, `(3,4)`
   (`det 3`, live), and `A2` (`det 3`, live) — so the `m=1` analog at genus
   four is *not* automatically rigid and will need the general Fox script
   of §7 idea 8.
4. **Quartic-lift gate** last, as the packet's screening order prescribes.

### 4.2 The uniform ladder [NEW-DERIVATION; the strategic point]

The genus-three `m=0` proof used only: a degree-three normalization
coordinate, `b1=1` with the `(2,2)` over the unique conductor pair, and
`m=n4=0`. None of this is genus-specific. For *every* row with
delta-sequence `(q,3)` (i.e. `r1=3`, infinity knot `T(3,q)`):

- `X` has degree 3, `X'` degree 2, so there are at most two critical
  points, and the cubic-fibre length arithmetic excluding conductor
  endpoints is verbatim the same;
- every unibranch singular point is a critical point of `X`; a vertical
  tangent at a smooth point is also `(2,1,1)/C2`;
- hence in any `m=0, n4=0, b1=1` row, some critical point of `X` is a
  `C2` site with coalescing meridians — duplicate — dead, *uniformly in
  `q`*.

Since `T(3,q)` (`q` even) passes the determinant gate at every genus
(`det T(3,even)=3` — e.g. `T(3,8)` at genus seven), this uniform lemma
**U1** is what prevents the ladder from being an infinite grind. Together
with **U2** (winding-two final cables force `b1>=2`, generalizing the
`(6,4,3)` self-pair cubic), **U3** (the winding-`>=3` coloring conjecture
above), the rank gate (`T(2,n)`), and the determinant gate, the plausible
endgame is: *the irreducible one-place `m=0`, `b1=1` row is empty at every
delta*, proved by five uniform lemmas plus the finite `T(3,4)`-analog
residue that each genus's `m>=1` variant leaves. The genus-four packet
should be written explicitly as the first instance of U1–U3, not as a
one-off.

Smallest deliverable: the `C_(3,2)(trefoil)` coloring census plus the U1
write-up (both desk-scale, one lane).

---

## 5. Reattacking the one-cusp global index-four / log-Jacobian gate (obligation 5)

The gate **[PROMOTED/PROVISIONAL mix, §1]**: exclude
`K=C(F1,F2) subset M=C(X,S)` of index four with `F_i` in the
single-valuation intersection `O(U)`, `J_(X,S)(F1,F2)=c/(mu X)`, `M=K(X)`,
and the saturated `(3,1)+(2,2)` census. Two attacks using four of the six
permitted toolkits, with controls.

### 5.1 Companion resultant + valuation/discriminant ledger [NEW-DERIVATION]

Field theory gives the object directly: the minimal polynomial
`P(T;F1,F2)` of `X` over `K`, computable as
`Res_S(F1(X,S)-u, F2(X,S)-v)` up to declared normalization. Three exact
facts couple it to the promoted geometry:

1. `div_U(X)=mu*Phi` (from `O(U-Phi)=C[X,X^(-1),S]` and `nu(X)=mu`), so
   the norm `N_(M/K)(X)=+-P(0)` has divisor `mu*C0` plus boundary terms —
   the reviewed inverse-Kummer identity `(t-a)r=(c0 o pi)^mu` is exactly
   this statement, so the frame is consistent with what is already sealed.
2. `disc(P) = ind^2 * Delta_(S/A)` where `A=C[F1,F2]`, `S` its integral
   closure in `M`, `ind` the monogenic-index divisor of `A[X]` in `S`, and
   `Delta_(S/A)` the intrinsic quartic trace discriminant. In the charged
   row the generic branch behavior is `(2,1,1)` along the irreducible `B`,
   so `Delta_(S/A)=q_B * (square)` (tame, one transposition sheet); this is
   the rank-four sibling of the promoted `DISC8-INDEX` typing discipline —
   index/Fitting is not conductor, source different is not target
   discriminant.
3. The reviewed collision locus (`u^mu-v^mu=0` locally) is *inside* `ind`.

The attack: compute `deg disc(P)` twice. Once from below, via the
Newton polygons of `P` at the places of `K` at infinity — which the
*saturated* Orevkov–Chau budget pins: saturation means no boundary
contribution is free, so any nontrivial `ind` at infinity must consume a
unit the budget does not have. Once from above, via `q_B`-degree plus the
even square. A strict mismatch closes the horn. This executes the overlay's
"attack whether that index necessarily consumes an infinity-budget unit"
with a named computable object.

Controls against overclaiming (mandatory): (i) the producer's all-`mu`
hyperelliptic firewall family must *survive* the ledger (it realizes the
Euler/orbit identities without being Keller — if the ledger kills it, the
ledger is wrong); (ii) the split-DVR model `(u^mu,1,2,3)` must survive
locally (no local valuation argument can close the horn — promoted); (iii)
Formanek's primitive-element theorem retains a recorded source-access gap
(official PDF 403); any step consuming primitivity of `t` must carry that
flag until the primary text is pinned.

Weakest failure mode: the boundary Newton-polygon computation may only
bound `deg disc(P)` in one direction, reproducing "floor, not attainment"
(FALLACY: floor/attainment). First falsification test: run the whole ledger
on the explicit invariant pair `(u,v)=(x w^b, w^c z)` of the single-pole
model — a non-Keller pair with known bracket (6.5) — and check the ledger
correctly *fails to close* on it for the right reason (nonconstant
Jacobian), not by a sign accident.

### 5.2 Log-symplectic torsion + `w`-graded Poisson leading form [NEW-DERIVATION]

`U` is a `Q`-homology plane with `pi1(U)=Z/mu` and trivial log canonical
class; `omega=dx dy` descends to the nowhere-zero form
`(dX dS)/(mu X)`. Untwisted action residues are automatically exact
(`H^1_dR(U)=H^2_dR(U)=0`) — the old Avenue-33 `COSTUME` — but the finite
`Z/mu` fundamental group supplies *twisted* invariants: the eigenspaces of
the cyclic cover `Z -> U` under characters of `Z/mu`. In the explicit
single-pole model the invariant-pair bracket is exactly

```text
{x^a w^b z^c-type pairs}:  {u,v} = w^(b+c-1)[(c+br+mu)w^mu-(c+br)],
```

a two-term `w`-polynomial whose coefficients differ by `mu`. Conjecture
(**leading-form lemma**): for any `H1,H2` in the full invariant ring, the
top `w`-semi-invariant weight part of `{H1,H2}` is determined by the top
parts of `H1,H2` and is a nonzero non-constant semi-invariant unless the
top parts are algebraically dependent; descending induction then forces
`{H1,H2}` nonconstant for every pair satisfying the field condition
`[M:K]=4`. Evidence: the degree-six cell's two parameter-*independent*
Jacobian coefficients (`8` and `4`) are exactly what such a structural
identity would produce; the number `mu` appearing in the bracket gap
matches `J=2=mu` there.

Weakest failure mode: cancellation between distinct `w`-weights via the
hypersurface relation `x^r z=w^mu-1` (the packet's own warning that naive
weight inequalities miss residue cancellations). First falsification test:
compute the bracket's `w`-leading term for all *pairs of basis invariants*
at `(mu,r)=(2,2),(3,2)` (finite, desk) and check the claimed determination
by top parts; a single counterexample retires the lemma cheaply. Primary
interfaces: the invariant-ring artifact `0e2e09c8...` (its (5.5)–(6.5)),
Kummer-cover eigenspace decomposition, and — for the torsion-residue
variant — the standard finite-cover comparison of twisted de Rham with
group cohomology of `Z/mu`.

These two attacks are complementary: §5.1 is global-arithmetic
(discriminant degrees), §5.2 is local-structural (graded bracket); they
cannot fail at the same interface.

---

## 6. The degree-eight invariant-ring cell (obligation 6)

**Decision: YES — run it, but as a deliberately small pattern-mining job,
and commission the structural lemma in parallel as the real product.**
Rationale: 20 parameters, 57 coefficient equations is minutes-to-hours of
Groebner work, far below the threshold where "AWS or theorem" is a real
tradeoff; and either outcome (emptiness, a parameter-independent
coefficient, or a survivor) directly informs the §5.2 lemma. The exploratory
finding "no parameter-independent coefficient at degree eight" makes the
job *more* informative, not less: if the cell is empty *without* a single
forced coefficient, the mechanism is interaction, not a universal identity,
and the leading-form lemma must be formulated accordingly.

Specification (per obligation 6):

- **Cells:** `(mu,r) = (2,2)` degree-8 (the named 13-basis/20-parameter/57-
  equation cell), then degree-10 same `(mu,r)`, then `(3,2)` degree-9
  (first nontrivial cell there); stop the grid after these three unless a
  pattern demands more.
- **Coefficient rings:** exact `Q` primary; cross-checks over `F_32003` and
  `F_65521` (the RESULT.md convention), plus one fresh prime `F_31991` to
  avoid prime-specific coincidences.
- **Term orders / engines:** `dp` with Singular `std` and `slimgb` as two
  independent lanes (engine diversity as in RESULT.md); msolve only with an
  exact rational certificate (the known first-prime `[1]`-header trap).
- **System:** the 57 (resp. analogous) coefficient equations of
  `J(H1,H2)-const`, target constant `mu`; **saturation: none needed** — the
  claim is `1 in I`, an ideal-membership/unit test, so no `sat()` wrapping
  risk arises; if a survivor appears, its component analysis moves to a
  fresh reviewed packet before any saturation is attempted.
- **Mutation:** target-`mu+1` constant (the RESULT.md `target 3` pattern)
  must return non-unit or differently-structured output; a planted
  sign-flipped equation must flip the verdict.
- **Shard/schedule:** one shard per (cell, engine, field) — ≤ 15 lanes;
  two stopped `r6*` nodes started for this, the four idle running `r6*`
  nodes used first (they are already billed); wall clock cap 4 h/shard,
  fail-closed off AWS, registered job tag, remote hostname in custody,
  `EVIDENCE.sha256` before retrieval.
- **Stopping rule:** all shards terminal or 24 h, whichever first; a
  survivor variety immediately quarantines the lane (potential
  counterexample — exact independent verification before any claim).
- **Exact ledger-changing outputs:** (a) "normalized pullback cells
  `(2,2)<=10` and `(3,2)<=9` empty" (extends the bounded-ansatz closure);
  (b) the complete list of parameter-independent Jacobian coefficients per
  cell (feeds §5.2; this is the prize); (c) or a quarantined survivor.

The "higher-value exact computation" the packet asks me to weigh is the
§5.2 basis-pair bracket table — I propose doing *both*, because the AWS job
is cheap and the bracket table is a desk lane; they answer different
questions about the same object.

---

## 7. Idea cards (obligation 2: ten ideas; cards 2, 3, 4, 5 are new or cross-avenue; 7, 8 are cross-avenue instantiations)

Format per card: implication / hypotheses / weakest failure / first test /
primary interface / EV-cost score (0–10) / smallest deliverable.

**1. M1-T34-CLOSURE (Routes A+B, §3).** Implication: the irreducible
charged genus-three rank-four row is empty, completing `Delta_aff>=4` for
the whole irreducible minimal-cycle packet. Hypotheses: charged row data;
t34 strata; spectator Fox computation; threat-map `m<=1`. Weakest failure:
the `(2,1,1)`-implies-local-`C2` dictionary at singular unibranch points.
First test: the spectator `m=2` non-kill control (§3.1). Interfaces: Fox
localization `60ba8f43...`; t34 `2b3cc0b7...`; spectator `a25d4b2f...` +
corrigendum. Score: **9** (row-closing, desk-scale, two independent
routes). Deliverable: one sealed producer with replay + mutations, review
bundled with its provisional inputs.

**2. QUADRATIC-DELTA all-degree floor [NEW].** Implication: for every
degree `d>=3`, an irreducible one-place `A1`-normalized branch with
transitive meridian-transposition `S_d` monodromy has
`Delta_aff >= (d-1)(d-2)/2`, sharp at `T(d-1,d)` (checks: `d=3` gives 1,
trefoil; `d=4` gives 3, `T(3,4)` — both match promoted results).
Mechanism: transitivity needs `>= d-1` distinct transpositions, so the
infinity knot needs meridional rank `>= d-1`; `mr <= bridge`; for
*algebraic* iterated cables the delta-sequence growth inequalities force
genus at least quadratic in bridge (single torus step:
`g=(p-1)(q-1)/2 >= (b-1)b/2` since `q>=p+1`; cable steps need the
characteristic-pair growth `q_(i+1) > p_(i+1) q_i ...` — this is where the
proof lives, in AG–S arithmetic, not raw knot theory). Hypotheses:
one-place; irreducible; simple (transposition) inertia. Weakest failure:
the cable-step induction may need the meridional-rank-vs-bridge equality
(open MRC territory) if `mr < b` behaves badly — the derivation avoids MRC
in the stated direction (`mr >= d-1` is forced, and only `g >= f(b)`,
`b >= mr` are used), but the tower induction must be checked against
`|q|`-small algebraic cables. First test: verify no conductor-`<=8`
delta-sequence knot admits a transitive `S5` transposition coloring
(predicted: none, since all have bridge `<= 3`); one afternoon with the
frozen coloring engine. Interfaces: AG–S delta-sequences; Schubert genus;
the promoted meridian surjection. Score: **8** (attacks *every* degree at
once; independent of rank-four; modest cost). Deliverable: statement +
proof for pure torus rows (`h=1`), with the tower case as a flagged lemma.

**3. LOG-TORSION SYMPLECTIC GATE (§5.2) [NEW].** Implication: the one-cusp
horn dies because the twisted (`Z/mu`) bracket structure forbids constant
Jacobians in the invariant ring. Score: **7**. (Fields as in §5.2;
deliverable: the basis-pair bracket table at `(2,2),(3,2)`.)

**4. W-GRADED POISSON LEADING-FORM LEMMA (§5.2) [NEW].** The structural
version of card 3; separated because its failure mode (hypersurface-relation
cancellation) is independent of the torsion framing. Score: **7**.
Deliverable: proof or exact counterexample of top-part determination on the
finite basis-pair table.

**5. QUARTIC DISC/INDEX RESULTANT LEDGER (§5.1) [NEW].** Implication:
saturated boundary budget + monogenic index forces `deg disc(P)` overflow;
horn closed. Score: **6** (higher risk: floor-vs-attainment). Deliverable:
the exact ledger on the hyperelliptic firewall family (the mandatory
non-kill control) plus the general degree bookkeeping.

**6. GENUS-4 SUCCESSOR + UNIFORM LADDER U1–U3 (§4).** Implication: the
irreducible `m=0` ladder closes uniformly, not genus-by-genus. Score: **8**.
Deliverable: `C_(3,2)(trefoil)` coloring census + U1 write-up.

**7. REDUCIBLE-BRANCH FOREST LADDER [cross-avenue].** Implication: extend
the delta/Fox machinery to `h>=2` rank-four rows (source a forest of
`A1`s), where infinity is a *link*: the correct gates are the
sign-specialized multivariable Fox matrix (the localization packet's own
warning) and the promoted `m>=2h-1` component bound. Hypotheses: charged
forest interfaces. Weakest failure: no analog of Neumann–Rudolph goodness
for the multi-component boundary without new source work. First test:
classify the `h=2, m=3` minimal configuration's possible boundary links and
compute one sign-Fox matrix. Interfaces: quartic integration §6 identity
(6.1); Neumann splice calculus. Score: **5** (open-ended but untouched —
this is where the row lattice is thinnest). Deliverable: the `h=2` boundary
threat map.

**8. GENERAL FOX-RESTRICTION SCRIPT [cross-avenue, instrumentation-as-
mathematics].** Implication: for any parametrized branch `(p(t),q(t))`,
mechanically compute `H^1(A2-B,L_sign)` and all singular-link restrictions
(the (8.1) screening object), making the genus-four and `m>=1` searches
linear algebra over `F3`. Hypotheses: based braid factorization computable
from the parametrization (the spectator's `SL2(Z)`+exponent-sum technique
generalizes). Weakest failure: basepoint/ordering conventions silently
flipping a restriction (the spectator packet shows the discipline needed).
First test: reproduce the spectator's `(lambda,lambda)` and the infinite
false-positive family's zero kernel from §6 of the localization packet.
Interfaces: `60ba8f43...`. Score: **7** (multiplies every branch-side
lane). Deliverable: script + two reproduced controls, then run on all
genus-four `b1=1` strata.

**9. STRINZ CROSSWALK (§9).** Score: **4** (bounded; prose-tier input).
Deliverable: one typed adapter-question memo, no compute.

**10. K00/CHAR-P KEEP-WARM (§8.2).** Score: **3** now (counterexample-side
insurance; nothing in this round raises it). Deliverable: an explicit
dormancy note naming reentry conditions, so the next round does not re-pay
the survey cost.

---

## 8. Higher-degree proof routes and counterexample-facing programs (obligation 7)

Excluding rank four would *not* prove JC2, and the report treats it only as
the minimal live degree of the canonical route. The degree-`d` frontier:

### 8.1 Proof side

- The all-degree branch-topology theorem [PROMOTED] plus card 2's
  quantitative floor is the right shape of generalization: at degree `d`
  the irreducible one-place branch needs delta `~ d^2/2`, i.e. enormous
  singular budgets, while `n22`-type identifications multiply. The
  medium-term conjecture worth naming now: *for every `d`, the irreducible
  one-place `b1=1` analog rows are empty*, by U1–U3-style uniform lemmas
  with `S_d` in place of `S4` (the rank gate and determinant gate are
  already degree-uniform; U1's cubic-projection argument is specific to
  `r1=3` rows but its duplicate mechanism generalizes to the minimal-`r1`
  coordinate whenever `r1 < d`... this last step is genuinely open and is
  the correct successor after the genus-four packet).
- At `(2,2)`-rich degrees, the resolvent/double-plane tower generalizes:
  `S_d`-covers have sign double planes and `A_(d)`-resolvent gates; the
  `Z/3` theory used here becomes the odd part of `H^1_et(D, ...)` for
  larger simple quotients only at `d=4` (where `V4` is normal) — record
  explicitly that the resolvent trick is quartic-specific, so nobody
  silently recycles it at `d=5`, where `S5` has no proper normal subgroup
  except `A5`.

### 8.2 Counterexample side (unchanged priorities, named reentry points)

- **K00:** cheapest literal faces remain `e=2,m=2,h10=2` and the untouched
  `(4,1)` unit-ray at G11. Reentry condition: an idle reasoning slot with
  no branch-front task, or a Strinz-crosswalk hit that maps onto a K00
  band. Not this round.
- **Char-p/Witt:** the AS109 frontier stands at correction `y`-degree
  twelve; uniform-complexity falsification remains the only admissible
  target (no unrestricted finite-level lifts). Reentry: any external
  artifact-backed char-p claim, or the degree-twelve budget shrinking.
- **Invariant-cell search (§6)** doubles as the *live* bounded
  counterexample program: a survivor there is a Keller counterexample
  candidate by construction, with quarantine rules already specified.
- **Classical frontier gate** discipline: any new bounded search must
  restate actual total degrees (GGV/Heitmann `gcd>=16` closure) before
  compute; the `(75,125)` Strinz shapes pass that gate, our small invariant
  cells are `method-control`-tier relative to it.

---

## 9. Safe questions from the Strinz 08-30 note (obligation 8)

Prose-tier only; nothing promotable, no formula imports, no broad CAS:

1. **Adapter question:** does the campaign's literal band/recurrence
   machinery (K00 569-tail calendars) contain the missing
   source-to-reduced adapter that would make their Row-40
   `(15,25)`-homogenization argument apply to the *source* model? A typed
   yes/no with the exact mismatch is the deliverable.
2. **Staircase isomorphism:** is their five-periodic K5 band staircase,
   with `q20` defined modulo `span(rbar^4)`, isomorphic to one of our
   reviewed possible-arrival calendars? If yes, our promoted
   non-recurrence results transfer as *questions* (never as theorems) about
   their surviving tied `d2` patterns.
3. **Independent reconstruction targets:** their `q18` leading-pole kill of
   `(m,k,ell)=(2,1,0)` at K7 and the positive `q19` quadratic-normal-form
   component are the two claims cheap enough to reconstruct on our engines
   from scratch; treat a mismatch as information about *their* note, not
   ours.
4. Their finite K5–K7 recompile recommendation is *their* program; we do
   not adopt it as a lane.

---

## 10. Campaign and software improvements (obligation 9; ≥4, each bounded)

1. **Theorem-interface cards + scope linter.** Machine-readable cards
   (object, hypotheses, ring, bounds, conclusion, firewalls) emitted at
   promotion time, with a deterministic linter that flags consumers whose
   charged hypotheses mismatch (the `D4`-row deletion, the
   rational-vs-morphic forest repair, and this packet's own `m=0`-vs-`m=1`
   local-group distinction are all mechanically expressible). Smallest
   test: encode the ~15 block-descent artifacts and require the linter to
   rediscover those three known scope events; no new authority, cards link
   to `AUDIT.md`.
2. **Adversarial-control registry harness.** A frozen registry of known
   false-positive objects (nodal control, spectator, `Gm`-parametrized
   positive control, split-DVR model, hyperelliptic firewall family) with a
   harness that runs every new gate/replay against all registered controls
   and fails when a control passes a gate that should reject it (or is
   killed by a ledger that should pass it). This mechanizes the "sharp
   controls against overclaiming" pattern that currently relies on each
   producer remembering. Smallest test: wire the five named objects to the
   §3/§5 producers of this round.
3. **AWS packet manifest + idle reaper.** One manifest schema binding
   source hashes, engine+version, term order, prime, shard id, mutation
   spec, and stop rule, verified remotely before launch and hashed into
   `EVIDENCE.sha256` (RESULT.md already does 80% of this by hand); plus a
   coordinator tick action that stops idle running nodes within standing
   authority — the round starts with four idle running `r6*` nodes, which
   is exactly the leak this closes. Smallest test: re-express the
   degree-six RESULT lanes in the schema and run the reaper dry.
4. **ROWVIEW: deterministic row-state compiler (improves the next ideation
   round itself).** A ROUNDVIEW/v1-style, hash-bound, fail-closed compiler
   that renders the branch-row lattice (per row: charged hypotheses,
   lifecycle, open gates, named controls, blocking reviews) from
   `AUDIT.md` anchors. This round required hand-reconstruction from ~30k
   ledger lines; a compiled two-page row table in the packet would cut
   ideation context cost several-fold and reduce stale-premise risk.
   Fail-closed on missing/duplicate anchors; output is a prompt slice, not
   an authority.
5. **Orphaned-lane watchdog** (small): `lane_detach` now survives
   coordinator loss; add a tick-time sweep that detects completed-but-
   unharvested sidecars and queues receipt-first harvest, so the accidental-
   termination failure mode cannot silently strand finished model work.

---

## 11. Ranked next-wave allocation (obligation 10)

Reviews are background; provisional consumption is labeled per task.
Ranked by expected information per unit time:

1. **Sol (coordinator + integrative producer): `M1-T34-CLOSURE`** (§3,
   Routes A+B in one sealed artifact, charging the threat map). May consume
   the t34 classification, spectator, and cable census **provisionally
   (labeled)**; promotion blocked until item 2 clears. This is the
   highest-fanout artifact of the wave.
2. **Fable (this model, fresh lane): bundled hostile review** of the four
   provisional genus-three inputs — cable census `03b16c2d...`, `m=0`
   complete obstruction `bcfc47b0...`, t34 classification `2b3cc0b7...`,
   spectator `a25d4b2f...` (+ corrigendum) — as one frozen packet (all are
   Sol-produced, so Fable review satisfies the different-model rule; the
   packet overlay explicitly demands an independent review of the `m=0`
   result). Six-hour target; `M1` promotion consumes this.
3. **Opus: finish/replace the interrupted one-node topology audit** (its
   partial report is not evidence), then hostile-review `M1-T34-CLOSURE`
   when frozen. Between the two, develop card 2 (QUADRATIC-DELTA): the
   torus-row case is self-contained and Opus's prior topology audits make
   it the natural owner.
4. **GPT reviewer lane (receipt-first): genus-four successor packet** (§4):
   `C_(3,2)(trefoil)` coloring census + U1 uniform lemma + census/gates
   write-up. May consume `Delta_aff>=4` **provisionally (labeled)**; its
   own outputs are exact/desk and independently replayable.
5. **Internal agent A (desk): §5.2 basis-pair bracket table** at
   `(2,2),(3,2)` and the leading-form lemma attempt; consumes only the
   sealed invariant-ring identities (producer-exact; label provisional).
6. **AWS (2 lanes now on the idle running `r6*` nodes, 2 more from stopped
   stock if shards queue): degree-8/10 invariant-cell job** per the §6
   spec, with manifest+mutations; quarantine-on-survivor. Also: stop the
   remaining idle nodes (improvement 3) once shards conclude. Box01
   continues GB21 untouched.
7. **Grok (402-risk; if available): Strinz crosswalk memo** (§9) and an
   independent replay of the spectator Fox computation (cheap, and it
   doubles review coverage of the most-consumed control). If Grok is
   unavailable, fold the crosswalk into internal agent B and record
   `UNAVAILABLE_NO_MODEL_WORK`.
8. **Internal agent B (systems, 48-h window): improvement 3 (manifest +
   reaper), then improvement 4 (ROWVIEW)** — one at a time, measured, with
   the required regression gates; neither blocks mathematics.
9. **Deferred, with reentry conditions recorded:** §5.1 disc/index ledger
   (after the firewall-family control script exists); reducible-branch
   threat map (card 7) when a slot frees; K00/char-p reentry per §8.2; no
   Sigray/GGV relaunch.

Success criteria for the wave: (a) `M1-T34-CLOSURE` sealed and its input
bundle reviewed — if both clear, the irreducible charged genus-three
rank-four row is closed and the next full round pivots to genus four and
the reducible ladder; (b) the genus-four desk gates either close the
`m=0` layer or return the exact surviving object; (c) the invariant-cell
job returns its coefficient pattern for the leading-form lemma. Any
refutation inside (a) is a critical trigger for an immediate full round.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `45925`.
- Body SHA-256:
  `8b66f247276089978ec9621f9897b7f01f3c5e41a04ca05c9644ead0bd256800`.
- Frozen basis: `e40079bf3699311a3955486ee300197e1248d374`.
