# Grok 4.6 primary research — td=12 U1 family's first trunk consumer

Work independently from `/Users/dc/code/math/jc2` and write exactly:

`xmodel/m2-td12-u1-trunk-consumer-primary-grok46-20260829.md`

This is primary mathematics, not review. Use only the repaired, promotion-safe
residue in
`xmodel/m2-equal-join-semilinear-primary-research-hostile-review-grok46-20260829.md`,
the underlying Opus report where the review explicitly confirms a clause, the
current source text for Proposition 8.1(iv), Statement 3.9 and the P0/P1
budget rules, the passed finite-chain R2 theorem/review, and directly relevant
canonical corrections. Do not read the active td=8 transport, exact-lambda,
U2-review, ideation, or post-target model outputs.

Target the source-pinned td=12, m=3, `[2,2,2]`, type `(2,3)` U1 family:

- poles `(a,b,nu)=(1,2,3)^3`, incoming `(mu,w)=(2,3/2)^3`;
- merge family `(r,mu,eps,w)=(3,2,0,3/2)`, admissible merge parameter
  `n >= 5`, `n mod 6 in {1,5}`, with
  `Rad=t^3-A`, `A != 0`, `w_tr=9/2`, `M=2`, merge lambda floor zero;
- proposed dirty trunk step
  `(l,eps,k,lex,nu)=(2,0,1,0,25)`,
  `(dp,dq,E,kbar,X)=(75,51,27,17,25)`, successor `(w,M)=(2/3,3)`,
  recorded lambda lower bound 8, followed by the P1 terminal with `j=1`,
  `psi=2`, and shared budget ceiling 9.

Attack the first unresolved downstream consumer, in this order:

1. Verify the row, merge progression and dirty-step arithmetic directly from
   the current source and repaired clauses. Keep the merge parameter distinct
   from the trunk's printed `nu=25` and from every local polynomial exponent.
2. Derive the *exact* Proposition 8.1(iv) equation for that dirty trunk cell,
   including its one non-chain orbit, all zero-root factors, the nonzero RHS,
   and any eta/deck normalization. Solve it over characteristic zero without
   imposing a degree cap. Use residues/partial fractions or coefficient
   recurrences to decide `TRUNK_T1_DEAD`, `TRUNK_T1_SURVIVES`, or whether one
   named source coefficient/jet is missing.
3. If it survives locally, determine the complete solution shape and all
   coefficient ratios. Test compatibility with the merge output
   `Rad=t^3-A` at every level that the printed Statement 3.9 actually relates.
   Separate independent gauges from a genuine shared coefficient. Do not
   promote local survival to gluing or realizability.
4. Re-derive the exact lambda of the dirty step if the printed/source data
   determine it; otherwise prove the narrowest floor/ceiling and identify the
   first missing subtop or extra-branch jet. Decide whether slack one is
   genuinely available, forced to zero, or consumed.
5. If the displayed dirty step dies, search the complete cap-free reduced P0
   menu from `(9/2,2)` at remaining budget at most nine for another trunk
   path, using the reviewed exact divisor/congruence law rather than legacy
   phantom edges. Prove completeness of any finite menu used. If it survives,
   still check whether another consumer varies with the merge AP parameter.

Use exact hand algebra and short standard-library Python only; no broad
uncapped local enumeration or heavy CAS. Put any small certificate packet in
`cases/m2_td12_u1_trunk_consumer_grok46_20260829/`. Include ordinary and `-O`
tests, cap-token rejection, full/body hashes, exact commands/counts, and one
lead verdict: `FAMILY_KILLED_AT_TRUNK`, `FAMILY_SURVIVES_FIRST_TRUNK`, or
`PARTIAL`. State the maximum safe consequence and the single best next lemma.
No result here proves landing, existence of a Keller pair, a panel exclusion,
a td ceiling, or JC2.

Hard boundaries: no web, AWS, commit, push, canonical edits, heavy CAS, or
local long/high-memory process. Never access/list/search/build/status or
control `jc2-lean`; never run global `git status` or workspace-wide searches.
Modify only the requested report and uniquely named packet; use `/tmp` for
scratch.
