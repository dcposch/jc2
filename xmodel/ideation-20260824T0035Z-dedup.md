# Deduplicated ideation map — round `20260824T0035Z-2386780`

- **Deduplicator/session:** OpenAI Codex, Bacon lane (`/root/compute_audit`),
  after blind collection was sealed
- **Inputs:** `root`, `bacon`, `averroes`, `nash`, and `grok2` submissions plus
  the sealed packet
- **Snapshot:** base `2386780cfe436da2d9c406d2f53f4baf30d10af9`, cutoff
  `2026-08-24T00:35:00Z`
- **State:** **FROZEN DEDUP** at `2026-08-24T00:56:49Z`

This document maps proposals, not claims.  A “vote” means that a blind report
allocated an idea card or an explicit major-lane call to the object; it is not
mathematical evidence.  Four submissions are separate Codex sessions and one
is Grok, so even blind convergence is model-family-correlated rather than five
statistically independent expert judgments.

## 1. Exact card fingerprint map

Aliases are `R` = root, `B` = bacon, `A` = Averroes, `N` = Nash, and `G` =
grok2.  The four fingerprint fields are deliberately kept separate: similar
targets with different mathematical objects are not duplicates.

| ID | Target | Mechanism | Mathematical object | Cheapest decisive test |
|---|---|---|---|---|
| **R-A** | Contradict nonproperness, or obtain a cap-free global index, directly from a hypothetical Keller map. | Extend `dP,dQ` as a rank-two log/meromorphic frame on a canonical compactification and localize its elementary-divisor/`c2`/different defect. | Zariski-Main finite normalization, resolved boundary, logarithmic cotangent frame. | Derive the one-blow-up formula; run triangular and tame-automorphism controls; evaluate one complete residue-A tree. |
| **R-B** | Decide whether finite D-windows form a compatible smooth/dominant tower or become obstructed. | Compute the relative cotangent obstruction and Fitting ideal for `W_(n+2) -> W_n`, then identify any applicable Greenberg/Artin function. | Source-defined transition schemes over the promoted D25 `A^14` cells. | Generate only the D27 source increment over a generic D25 cell and compute the relative Jacobian/Fitting ranks at two primes. |
| **R-C** | Replay external bound-125 certificates and determine whether any exact cofactor repairs an internal cCa Q-gap. | Canonical rational certificate IR plus explicit coordinate substitutions and exact `1=sum h_i f_i` replay. | Sparse polynomial ideals, localizations, variable maps, and Bézout/Gröbner certificates. | Pin commits, run each advertised short verifier, parse one terminal certificate, and attempt a sparse coordinate isomorphism. |
| **B-A** | Decide whether the fixed B=84 D43 carrier has a standard-smooth common-integral point and hence a characteristic-zero coefficient point. | Re-emit all parked/graph equations over the radical ring, retain syzygies, and search away from the fixed origin in the full 14-free cell. | The common 218-row D43 scheme, its local rings, and its Hensel lift. | Different-model review of `D43-NF-FID`, followed by a minimal integral parked/reducer emission required to reproduce both prime hashes. |
| **B-B** | Build a coordinate-invariant boundary passport that can both checksum GGV/Sigray transport and charge pole-tree events against finite capacity. | Pull back the derivative matrix, saturate its cokernel, and track generic Smith exponents plus residual Fitting lengths under blow-up. | Torsion sheaf on a log resolution and its decorated boundary dual graph. | Extract profiles on tame maps and the Hénon tower before/after one coordinate change and one blow-up; reject a mere `sum=N` identity. |
| **B-C** | Recover exact Q-unit certificates for reopened internal ideals and classify external terminal-certificate lineage. | Learn sparse Nullstellensatz support modulo primes, solve Macaulay systems, CRT/rationally reconstruct cofactors, and canonicalize external maps/hashes. | Rational syzygies, Macaulay matrices, and normalized terminal ideals. | Run the two external bounded verifiers and a cCa2 support-learning pilot; do not start with cCa6. |
| **A-A** | Obtain an intrinsic pure-Sigray source and a sublinear type estimate sufficient to turn KJN into an absolute ceiling. | Attach both branch cycles, valuations, contact, and gradient-cokernel labels to a canonical boundary passport; seek `alpha*beta <= A*td^theta`, `theta<1`. | Normalized projective graph, `A(F)`, `Q_F`, decorated pole tree, and its type parameters. | Build the first-event dictionary and test it on triangular/Hénon/rooftop controls, then enumerate retained labels on a known tree and formal escape families. |
| **A-B** | Prove `D43-CH0`, then and only then test a compatible characteristic-zero pro-system and algebraization. | Integral provenance compiler, full-cell local smoothness, followed by typed compatible lifts through increasing depths. | Common radical-ring 218-row scheme and the inverse system of D-windows. | Hostile `D43-NF-FID` replay, integral 34-row/D23 re-emission, all-218 `p^2` evaluation, and one non-origin point if needed. |
| **A-C** | Determine correctness and true independence of SuperMind and Ziwei Guo bound-125 claims. | Machine-readable lineage DAG separating exact equivalence, coordinate equivalence, and social/derivational provenance. | Pinned repositories, field polynomials, polygon systems, terminal identities, and coordinate maps. | Run both advertised bounded suites, exact-check identities, then canonicalize the Belyi quintic and terminal systems against known lineages. |
| **N-A** | Prove finiteness directly by showing field traces of powers of `x,y` have no poles and lie in `C[f,g]`. | Sum Newton–Puiseux principal parts over inverse branches; use residue/monodromy cancellation and Newton identities to obtain integrality. | Finite field extension `C(f,g) subset C(x,y)`, trace functions, and the asymptotic divisor. | Implement trace summation on tame Keller controls, a non-Keller generically finite control, and the best filed residue-A data. |
| **N-B** | Decide whether D-series prolongation is finite-type, obstructed, or carries a persistent positive-dimensional symbol. | Treat bands as a discrete differential-algebraic system; compute tangent kernels, obstruction cokernels, and one nonlinear lift per free direction. | Prolongation maps `X_(D+2) -> X_D`, block Jacobians, and symbol modules. | Sample non-origin points at both primes, compute exact sparse symbol ranks/minors, then replay one actual next-band lift. |
| **N-C** | Decide whether the recorded GGV packet is information-complete for a decorated Sigray tree, and identify the minimal missing augmentation if not. | Translate both sides into MacLane/key-polynomial, semigroup, residue-degree, ramification, contact, and dual-graph fingerprints; search for collisions. | Coordinate-free valuation fingerprint of the GGV packet and candidate Sigray entries. | On `(72,108)`, extract invariant fields, enumerate compatible tree fingerprints, and stop at the first mismatch, collision, or unique covariant match. |
| **G-A** | Reconstruct a GGV-free Sigray source, or kill the idea that pure-boundary/symplectic data contains more than `J=1`. | Compare primitives of the symplectic one-forms with the Fitting/Smith filtration of the gradient cokernel. | Homogenized derivative cokernel `Q`, polar divisors of primitives, and residue-A leading data. | One-afternoon exact comparison on elementary/tame maps, residue-A leading terms, the class-kill decoy, and one Hénon control. |
| **G-B** | Find a non-origin `Z_p` D43 point and polynomially bounded tails, or certify that this B=84 carrier is only modular/formal. | Integral re-emission plus multi-point local-normal-form sampling, run in parallel with Padé/support-growth tests on tails. | D43 `A^14` parked cell, local ideals, and generating functions of inverse-limit tails. | Review fidelity, reproduce the integral reductions, test at least 16 non-origin points through band 42, and compare tail growth at banked depths. |
| **G-C** | Determine whether bound-125 claims share one degree-21 Belyi core and whether that method transports to residue-A. | Exact replay/provenance diff plus a method-level comparison of the degree-21 five-cover layer with the degree-4 residue-A A4 Belyi map. | Suzuki quintic and covers, external terminal certificates, and residue-A `beta(u)`. | Replay both new repositories, hash/diff all core objects, then test whether residue-A is a pullback/factor or merely a distinct candidate for the same passport-to-certificate method. |

## 2. Overlap clusters and preserved vote counts

### Cluster I — D-series structure, not another depth (`R-B`, `B-A`, `A-B`, `N-B`, `G-B`)

- **Direct card votes: 5/5.** Every report makes the D-series its strongest or
  near-strongest disproof/diagnostic card; every report rejects another blind
  fixed-B depth increase and keeps B=168 held.
- **Integral/Hensel subcluster: 3 direct votes** (`B-A`, `A-B`, `G-B`), with
  `N-B` treating the common integral model as a needed upgrade and `R-B`
  deliberately starting earlier at D25.
- **Transition/symbol subcluster: 2 direct votes** (`R-B`, `N-B`), with
  `A-B` explicitly continuing to a pro-system only after `D43-CH0` and `G-B`
  adding polynomiality/support growth.
- **Real sequencing disagreement:** root would define D25→D27 before spending
  on an isolated D43 lift; bacon/Averroes/grok2 place the common-integral D43
  gate first; Nash runs symbol and fidelity work concurrently.  This must not
  be reported as five votes for one exact next command.
- **Deduplicated content:** the common program is a staged compiler:
  source-faithful transitions, common integrality, non-origin smoothness,
  prolongation symbol, compatible lifts, and finally polynomiality/global
  nonautomorphism.  Passing any earlier stage does not imply a later one.

### Cluster II — intrinsic boundary data as source or obstruction (`R-A`, `B-B`, `A-A`, `G-A`; `N-A` adjacent)

- **Direct passport/coframe/Fitting votes: 4/5.** The four cards use the same
  exact pure-boundary determinant support but propose different upgrades:
  localized log-`c2` (`R-A`), blow-up capacity (`B-B`), intrinsic passport plus
  sublinear type (`A-A`), and a Fitting/symplectic source-costume test (`G-A`).
- **Adjacent global trace vote: 1** (`N-A`). Nash uses the same compactified
  boundary and inverse-branch data but targets field-trace regularity, not a
  Sigray source or a local capacity.
- **True common core:** start from an actual Keller map and retain directional,
  valuation, and branch-pairing data lost by scalar pole-mass ledgers.
- **Not a common theorem:** a source functor, a transport checksum, a localized
  Chern contradiction, a sublinear type theorem, a jump-capacity inequality,
  and trace regularity are six different claims.  The control computation can
  be shared; the proof obligations cannot.

### Cluster III — external certificate replay and lineage (`R-C`, `B-C`, `A-C`, `G-C`; Nash major-lane call)

- **Direct card votes: 4/5; explicit immediate-action endorsement: 5/5.**
  Nash used its third card for `G2-PSC` but separately called the external
  replay immediate.
- **Exact overlap:** all four cards pin repositories, run advertised bounded
  verifiers, exact-check terminal identities, and distinguish correctness from
  lineage/independence.
- **Extensions that must survive dedup:** root proposes a reusable certificate
  IR; bacon adds sparse rational-cofactor recovery for cCa/R1; Averroes makes
  equivalence and social lineage separate DAG edges; grok2 adds the degree-21
  Belyi/residue-A method-portability question.
- **Perimeter:** even unanimous clean replay settles evidence and priority at
  the conditional `(72,108)` frontier; it does not prove `G2-PSC`, an all-
  degree ceiling, or JC2.

### Cluster IV — GGV/Sigray architecture (`N-C`, with `B-B` adjacent and `R-A`/`A-A`/`G-A` choosing bypass)

- **Direct information-completeness test: 1 vote** (`N-C`). Bacon's torsion
  passport can act as an augmentation/checksum, but its main card does not
  claim that the recorded GGV packet is sufficient.
- **Architecture-level convergence: 5/5.** Every report rejects the current
  hybrid arrow as established; four prioritize or permit a pure intrinsic
  source, while Nash proposes a cheap collision test before abandoning the
  bridge.
- **Important non-overlap:** a collision refutes transport from the *recorded
  packet*, not every richer `G2-PSC` theorem using the original Keller pair;
  an intrinsic pure-Sigray source bypasses rather than proves `G2-PSC`.

### Cluster V — direct trace regularity (`N-A`)

- **Direct votes: 1/5.** This is the only card whose target is the finite-
  extension trace criterion `Tr(x^m),Tr(y^m) in C[f,g]`, hence a direct
  finiteness proof with no books or degree ceiling.
- **Why it is not absorbed into Cluster II:** the trace is a global symmetric
  sum over inverse branches. A local Fitting or log-frame passport is useful
  only if it retains the branch pairing needed to calculate the sum.
- **Why it remains serious:** its conditional deduction to integrality and
  automorphism is explicit, and the proposed falsifier is cheap.

## 3. Apparent agreement resting on shared assumptions

1. **Pure-boundary correlation.** Four boundary cards were all prompted by the
   same newly exact identity `det(A)=jZ^N`. Their convergence is not four pieces
   of evidence that the cokernel contains a positive or cofinal invariant; all
   assume it remembers more than total determinant support. Hénon, tame, and
   class-kill controls must be run before allocating theorem effort.
2. **D-series correlation.** All D cards consume the same D25/D43 lineage.
   `D43-NF-FID` is still producer-checked/provisional, and a different-model
   review can simultaneously lower three integral-first proposals. Root's
   source-level D25 transition is less exposed to that one artifact but still
   assumes the finite D-system is a faithful truncation of one meaningful
   inverse problem.
3. **Algebraization assumption.** Every positive D outcome remains far from a
   counterexample. Smooth modular/integral points, compatible formal lifts,
   convergence/algebraicity, global polynomial gluing, constant Jacobian at
   all orders, and nonautomorphism are separate gates. The shared word
   “algebraization” must not hide them.
4. **Pure-Sigray target assumption.** Four reports prefer bypassing GGV, but
   that preference assumes a corrected Sigray object can be constructed
   intrinsically and that its later landing/coverage statements are complete.
   Avoiding `G2-PSC` does not discharge source, coverage, or type provenance.
5. **External-intake correlation.** Four cards respond to the same web-sweep
   discovery and msolve erratum. Their vote count establishes urgency, not the
   correctness or independence of any artifact. It also assumes the published
   bundles expose dependency-complete exact identities and coordinate maps.
6. **Negative-control correlation.** Hénon and the class-kill family appear in
   several cards because the packet named them. Passing these controls is
   necessary but does not establish polynomial-origin sufficiency.
7. **Held-compute pseudo-vote.** All five reject B=168 now partly because the
   packet explicitly withholds authorization. This is correct policy, not an
   independent strategic experiment result.
8. **Blind-model correlation.** Root, bacon, Averroes, and Nash are independent
   blind sessions of the same model family. Count their distinct mechanisms,
   but do not translate 4:1 ideation agreement into a probability or evidence
   tier.

## 4. Genuinely novel cross-report connections

### X1 — torsion passport as the missing input to TRACE-REG

Combine `N-A` with `R-A`/`B-B`/`A-A`/`G-A`: require the intrinsic boundary
passport to retain monodromy pairing of inverse branches, then ask whether its
local Fitting/log-frame residues compute the principal part of
`Tr(h dx wedge dy)`.  This gives the passport a direct global-properness target
and gives TRACE-REG a finite local data schema.  A collision of passports with
different trace poles would decisively show that the local object is too weak.

### X2 — one D compiler, three successive invariants

Combine `R-B` and `N-B` with `B-A`/`A-B`/`G-B`: the common integral emitter
should also emit typed projections and relative cotangent matrices.  The same
content-addressed source then certifies, in order, model fidelity, relative
obstruction/symbol, non-origin smoothness, compatible lifting, and tail
polynomiality.  This removes the present false choice between “test D25→D27”
and “lift D43”: the cheap source/projection schema and fidelity review can run
first, and expensive local algebra is gated on both.

### X3 — sublinear type is enough; bounded type is not the only target

Combine the blow-up capacity of `B-B` with `A-A`'s observation that KJN needs
only `alpha*beta <= A*td^theta` for some `theta<1`.  The torsion passport need
not enumerate a finite type menu: a sublinear growth charge, coupled to the
type-relative KJN inequality, would already force an absolute ceiling.  Hénon
is the immediate test that the charge is restricted to the correct minimal/
residue-A scope.

### X4 — certificate lineage first, Belyi portability second

Combine `R-C`/`B-C`/`A-C` with `G-C`: first decide exact correctness and
lineage of the degree-21 certificates; only then extract a method-level
passport-to-number-field certificate template and test it on the distinct
degree-4 residue-A map.  This prevents a shared certificate lineage from being
misreported as independent support while preserving the genuinely new
possibility that its method, rather than its polynomial, transfers.

### X5 — a falsifiable augmentation for `G2-PSC`

Feed the intrinsic Rees/Fitting boundary labels from `B-B`/`A-A` into `N-C`'s
valuation fingerprint.  If two nonisomorphic Sigray trees still share the
augmented fingerprint, the bridge is information-deficient even after the
natural polynomial-origin data are added.  If the augmentation yields a
unique covariant match, it produces a sharply typed candidate lemma; it is
still not a proof of global selection or coverage.

## 5. Ranked consolidated candidates

Ranking is an execution portfolio: information per bounded cost first, while
retaining one direct proof and one disproof route.  It is not a ranking of
truth probabilities.

### 1. Exact external certificate and lineage intake

- **Support:** 4 direct cards; 5 explicit immediate-action endorsements.
- **Consolidated action:** pin SuperMind and Ziwei Guo, run advertised bounded
  suites in clean environments, exact-check identities, canonicalize fields/
  polygons/coordinate maps, and build the lineage DAG/IR against Suzuki, Roy,
  Helali, and campaign artifacts. Then decide whether any exact cofactor can be
  reused on cCa2; keep cCa6 behind a size forecast.
- **Why first:** hours to two days can change public correctness, priority,
  internal certificate strategy, and whether “independent lines” is accurate.
- **Hard stop/perimeter:** fail closed on a missing manifest or false identity;
  do not replace exact replay with modular votes, and do not infer beyond the
  GGV-Horruitiner bridge-conditional degree frontier.

### 2. Staged D-series provenance, symbol, smoothness, and polynomiality gate

- **Support:** 5 direct cards, with a real sequencing split preserved above.
- **Consolidated action:** review `D43-NF-FID`; define source-faithful typed
  transitions; emit a common integral scheme whose reductions match both
  primes; compute relative symbols and obstruction modules at non-origin
  points; demand all-218 `p^2`, flatness/local generation, and compatible
  lifts; only then test Padé/support stabilization and global nonautomorphism.
- **Why second:** this is the strongest bounded counterexample/falsification
  program and can also show that deeper-window nonemptiness is structurally
  uninformative.
- **Hard stop/perimeter:** no B=168, no D75, no monolithic `std`/F4, and no
  inference from a timeout, tangent dimension, or characteristic-zero finite
  jet to a germ or polynomial Keller pair.

### 3. Boundary invariant control bench and intrinsic source test

- **Support:** 4 direct boundary cards; one adjacent trace card.
- **Consolidated action:** implement one reusable exact bench computing the
  log-coframe elementary divisors, gradient-cokernel Fitting/Smith data,
  symplectic primitive poles, Rees valuations, and their change under one
  blow-up. Run it on elementary/tame automorphisms, Hénon, class-kill, a solved
  GGV chart, and residue-A leading data.
- **Why third:** a one- or two-day negative result can stop several correlated
  proof slogans; a positive coordinate-stable result can supply a pure source
  object shared by candidates 4–6.
- **Hard stop/perimeter:** if all outputs reduce to determinant order or scalar
  pole mass, lower the whole cluster; passing controls yields a proposed
  invariant, not source completeness or a bound.

### 4. TRACE-REG via branch-paired boundary residues

- **Support:** 1 direct card, strengthened by cross-connection X1.
- **Consolidated action:** prove or falsify cancellation of principal parts of
  the first Newton sums of inverse branches, using the candidate boundary
  passport only if it retains branch pairing. Keep the exact conditional
  implication `trace regularity => integrality => finite etale => automorphism`
  separate from the conjectural cancellation.
- **Why fourth:** it is the most direct global JC2 mechanism in the set and
  bypasses GGV, Sigray coverage, and degree ceilings, but it has only one blind
  proposer and a high risk that filed local data omit the required global sum.
- **Hard stop/perimeter:** stop at an exact noncancelling control with the same
  inputs, or at a proof that the retained passport does not determine trace
  poles.

### 5. Sublinear type / blow-up capacity from the intrinsic passport

- **Support:** 2 direct mechanisms (`B-B`, `A-A`), with `R-A` and `G-A`
  supplying adjacent local objects.
- **Consolidated action:** after candidate 3 passes, seek a nonnegative jump
  charge and the weaker sufficient growth theorem
  `alpha*beta <= A*td^theta`, `theta<1`, rather than demanding a finite type
  menu. Couple it explicitly to the type-relative KJN implication.
- **Why fifth:** it attacks the cofinal degree wall, but only after source,
  invariance, and non-tautology are established.
- **Hard stop/perimeter:** an invariant whose budget grows linearly with type,
  or which Hénon violates outside an unjustified restriction, is not an
  absolute bound; retain it only as a typed checksum.

### 6. `PSC-FINGERPRINT` route-killing test

- **Support:** 1 direct card, with 4 reports agreeing that the current hybrid
  arrow is absent and one boundary card offering an augmentation.
- **Consolidated action:** extract the recorded `(72,108)` GGV valuation
  fingerprint, enumerate compatible decorated Sigray fingerprints, and search
  for mismatch/collision/unique covariant match; repeat once with intrinsic
  Rees/Fitting labels if that augmentation is available.
- **Why sixth:** it can cheaply kill an ill-posed bridge or state a precise
  candidate `G2-PSC` lemma, but a positive unique match still leaves global
  selection, proof, landing, and coverage.
- **Hard stop/perimeter:** stop at the first exact collision or unique typed
  match; do not turn a combinatorial match into transport by rhetoric.

## Frozen synthesis

The strongest convergence is procedural and structural: verify external exact
evidence now, replace D-depth accumulation by a provenance/transition/
smoothness/algebraicity compiler, and test whether the pure-boundary module
contains information beyond `J=1` before building theory on it.  The genuinely
new minority ideas—TRACE-REG and the GGV/Sigray fingerprint collision test—are
kept separate because deduplicating them into the boundary consensus would
erase exactly the independent alternatives the blind round was designed to
surface.
