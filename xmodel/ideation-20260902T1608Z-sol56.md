# Blind ideation submission — round 20260902T1608Z — Sol 5.6 ultra

Charged packet SHA-256: `ab66bd6b87dc035740325c309349ec8d52a5963b6b1a0a6e0e53a34f74715af3` (verified before reading). Frozen campaign basis: `7f7c0306768d`. Scope: blind full-spectrum response to `OPEN[UPPER-TO-FLOOR]`; banked reports only; no `jc2-lean`; no canonical-ledger edits.

## 1. Direct verdict

The coordinator is right about the record and wrong if the reading is promoted one quantifier further.

The right statement is: **every boundary instrument currently built, including Moh's published skeleton, is one-directional.** `N-CEILING` gives `N <= U`; extra structure below `D_1` adds contact and lowers `N`; the filter `N <= 16` therefore cannot reject a skeleton (`xmodel/integration16-coordinator-fable51-20260902.md:38-54`; `xmodel/n-on-the-tree-review-grok46-20260902.md:143-153`). The 418 rows are necessary H2 realization targets, not maps, and “pinned to 4” consumes the H2 floor `N >= 4` (`xmodel/n-on-the-tree-review-grok46-20260902.md:191-218,334-345`). No ceiling has been proved.

The overstatement is: **the lane has not proved that the boundary route itself can never give a floor.** The interval

```text
0 <= N|D_1 <= a_1(-lambda_f(delta_1))
```

is the range allowed by Moh's skeleton and abstract ultrametric completion. Its lower endpoint is not attained by an actual Keller pair. The review says explicitly that the full Jacobian may constrain the subtree and that Moh does not (`xmodel/n-on-the-tree-review-grok46-20260902.md:137-151`). Integration #16 itself reserves exactly this exception (`xmodel/integration16-coordinator-fable51-20260902.md:109-115`). Thus “as built” is correct; “no boundary argument” is not licensed. Candidate (f) must be stated as a no-go for arguments factoring through Moh (1)–(13) plus the major skeleton, not for arguments using new Jacobian coefficients below `D_1`.

There is also a plain factual error in candidate (c). APPROACHES row 20 was run as a standalone formalism lane on 2026-08-23. The canonical inverse-Jacobian connection has identically zero p-curvature, while Tsuchimoto's non-tautological object starts with a Weyl-algebra endomorphism and is not defined on an arbitrary commutative Keller map (`xmodel/sol-pcurvature.md:3-30,423-426,501-528`). That branch is `KNOWN-HOLLOW/retire`, not “never run.” Finite-field realization remains a different, one-way falsification tool.

My allocation answer is consequently direct. Give `D_1` one bounded, exact pilot. If it does not expose a positive Jacobian deficiency at the first post-`delta_1` levels, stop treating a universal `D <= C(N)` as the primary proof target. Move the proof flagship to the Moh endgame—uniform elimination of finite-depth, unbounded-size skeletons—and run the exact same `D_1` machinery backwards on the counterexample side. Keep a small global-invariant probe, but do not fund five speculative ceiling languages at once. The depth count is finite; the coefficients and sizes are not. That is an elimination/realization problem, not evidence that another scalar ledger is waiting to be found.

## 2. A new exact cross-connection: the `D_1` self-different

The missing joint `f`/`g` subtree can be reduced to a one-colour subtree before any large computation. Work in Moh's monic gauge, put `t=x^{-1}`, take generic `c_2`, and let `tau_i` be the Puiseux roots of `g-c_2`. Set

```text
h_i(x) = f(x,tau_i(x)).
```

Implicit differentiation and `[f,g]=1` give the exact branch identity

```text
d h_i/dx = f_x - f_y g_x/g_y = 1/g_y(x,tau_i).                 (SD1)
```

If `h_i` has pole order `p_i=-ord_t h_i>0`, then differentiation changes the `t`-order from `-p_i` to `1-p_i`, hence

```text
p_i = 1 + ord_t g_y(x,tau_i).
```

If `h_i` is finite, its first nonconstant algebraic term has positive rational `t`-order, so `1+ord_t g_y<0`; equality zero would integrate to a logarithm and cannot occur in the Puiseux field. Because `g` is monic,

```text
g_y(x,tau_i) = product_{j != i}(tau_i-tau_j).
```

Combining this with CONTACT-DEFICIENCY yields the exact, review-needed formula

```text
N = sum_i max(0, 1 + ord_t g_y(x,tau_i))
  = sum_i max(0, 1 + sum_{j != i} ord_t(tau_i-tau_j)).          (SD2)
```

The first consequence is already stronger than a change of coordinates on the same free datum. Let `B` be a bottom-major disc of radius `delta_B=delta_1`, and let `tau_i` be a shifted `g-c_2` root following a root `rho_i` in `B`. The shifted roots preserve every contact visible at radius `delta_B`, so

```text
ord_t g_y(tau_i)
  = sum_{j != i} ord_t(tau_i-tau_j)
 >= lambda_g(delta_B)-delta_B.                              (SD3)
```

The subtraction removes the self-root's clipped contribution `delta_B`. Equations `(SD2)` and `(SD3)` give a lower bound per root. RADIUS-ORDER and `M_1=-m`, with `m=Kd`, `n=Ke`, simplify it to

```text
p_i >= 1+lambda_g(delta_B)-delta_B
     = d(1-delta_B)/(d+e) =: c_B > 0.                       (SD4)
```

But FRONTIER-N and monotonicity of `lambda_f` give the opposite inequality

```text
p_i <= -lambda_f(delta_B)
     = d(1-delta_B)/(d+e) = c_B.                            (SD5)
```

Therefore equality is forced. This is the proposed **THEOREM D1-SELF-DIFFERENT** (elementary derivation here; not yet independently reviewed): every shifted root in a bottom-major disc contributes exactly `c_B`, while DETECTOR-NULL makes every maximal minor disc contribute zero. Consequently

```text
N = sum_{bottom-major B} q_B c_B,
q_B = e V_2(B),
c_B = d(1-delta_{1,B})/(d+e),
sum_B q_B <= u e.                                           (SD6)
```

Different bottom branches may have different lower `V` data and different radii; no common `c_B` is assumed. Individual weights may be rational and integrality is required only after complete Puiseux/Galois orbit packets are summed. The safe computational relaxation is therefore a rational knapsack over **all** allowed branch types, permitting even arbitrary repetitions and unused major-root mass. A failure to make an integer `N` in this over-approximation is a kill; a hit is only a candidate until Moh's orbit restrictions (8)–(11) and actual root-tree attachment are imposed.

This directly revises the opening verdict. Moh's major skeleton alone is upper-only, but the full Jacobian condition below `D_1` supplies a matching self-different floor term by term. The supposedly arbitrary finer contact cannot change the weight. The remaining datum is a finite branch-partition problem, not a free joint subtree. That is a concrete answer to `OPEN[UPPER-TO-FLOOR]`, though a cofinal `N >= phi(D)` still requires solving `(SD6)` uniformly.

This also connects two lanes that the present ranking keeps separate. For a fixed candidate `g`, finding `f` is the linear Hamiltonian equation `[f,g]=1`; the banked kappa class vanishes exactly when a polynomial mate exists (`xmodel/hamiltonian-kappa-gate-20260824.md:49-80`). The kappa module has no universal receiver and remains a bad global ceiling strategy (`xmodel/hamiltonian-kappa-gate-20260824.md:180-198`), but on one typed `D_1` skeleton it is an exact obstruction/certificate engine. Candidate (d) should therefore be lowered as a global flow proposal and reused as the local solver inside candidate (a).

The first cheap gate is now the exact `(SD6)` partition, followed by the no-log condition in `(SD1)`: every branchwise Laurent expansion of `dx/g_y` must have zero residue before it can be the differential of an algebraic `h_i`. Passing both is not a Keller witness; failing either is an exact rejection in its stated scope. This is the strongest proof attack in this submission.

## 3. Disposition vectors

Codes are `U` unchanged, `R` raise, `L` lower, and `RT` retype. This is a full 46-entry vector; reasons follow only for changes, as charged.

```text
 1 RT   2 RT   3 U    4 U    5 RT   6 U    7 RT   8 U    9 U   10 U
11 U   12 U   13 U   14 U   15 U   16 U   17 U   18 U   19 U   20 RT
21 U   22 U   23 U   24 U   25 RT  26 R   27 RT  28 L   29 U   30 U
31 U   32 U   33 U   34 U   35 U   36 R   37 U   38 U   39 U   40 U
41 U   42 U   43 U   44 U   45 U   46 U
```

| Row | Change | Reason |
|---:|---|---|
| 1, GGV corner farm | `RT` | Finite-depth/unbounded-size Moh endgame and typed realization program, not a cofinal degree farm. “Always a next pair” is false as a count; pair size and the bottom subtree remain free (`xmodel/integration15-coordinator-fable51-20260902.md:28-36`). |
| 2, sheet/Eggers–Wall/boundary trees | `RT` | Classifier, `N`-upper-bound, and realization instrument. It pins windows but is not presently a `D`-ceiling route (`xmodel/integration16-coordinator-fable51-20260902.md:38-63,100-105`). |
| 5, Jung–van der Kulk/Aut | `RT` | Now substantive infrastructure: orbit-minimality gives a degree-preserving affine source change into the subrectangular gauge. It still supplies no degree-lowering elementary automorphism (`xmodel/integration15-coordinator-fable51-20260902.md:11-19`). |
| 7, `A_F`/nonproperness | `RT` | The source curve `E` and its additive/log-Chern package have now been tried; those invariants scale and do not cross. Retain nonadditive representation data and realization of `A_F`, not another Euler/BMY count (`xmodel/integration10-coordinator-fable51-20260902.md:12-40`). |
| 20, p-curvature | `RT` | Tried and retired as an independent commutative Keller obstruction, contrary to the packet. The automatic connection has zero p-curvature; the useful Tsuchimoto direction has the wrong source (`xmodel/sol-pcurvature.md:14-30,522-528`). |
| 25, fibre monodromy/passports | `RT` | Scalar passports, total `H_1`, and vanishing-cycle rank are now exact Riemann–Hurwitz/Euler identities. Only coupled two-coordinate or isotypic representation data remain live (`xmodel/keller-pencil-genus-opus5-20260902.md:105-140,375-380`). |
| 26, primitive monodromy/Galois | `R` narrowly | Test character-valued Chevalley–Weil multiplicities on the N=4 H2 cover. Do not repeat total-H1 or total-genus counts. A single `S_4` virtual-character computation is the launch gate. |
| 27, links/splice/plumbing | `RT` | Use as an attainment/realisability filter on pinned skeletons, not as a scalar ceiling. Topology can reject a proposed completion even though the major skeleton alone only upper-bounds `N`. |
| 28, log surfaces/BMY | `L` | BMY on `E` and on its Galois closure is proved vacuous by scaling; Noether, genus, and total vanishing-cycle ledgers reproduce identities (`xmodel/integration10-coordinator-fable51-20260902.md:16-25`; `xmodel/integration13-coordinator-fable51-20260902.md:22-32,47-63`). |
| 36, guided counterexample search | `R` narrowly | For the first time it has a finite structural target: branch-robust H2 skeleton groups pinned to `N=4`. Replace generic SAT/fewnomial search by skeleton-conditioned Laurent realization, starting at D=105. |

All other rows stay `U`. In particular rows 29 and 33 are not globally reopened: Hamiltonian exactness has no universal receiver and untwisted symplectic residues are automatic. Their exact identities may be used inside the fixed `D_1` client without changing their portfolio status.

### Candidates (a)–(f)

| Candidate | Disposition and rank | Reason |
|---|---|---|
| (a) Jacobian below `D_1` | `R`, rank 1, **answered structurally** | `(SD1)`–`(SD6)` turn the reviewed interval into exact branch weights and a finite partition problem. Audit branch-type exhaustiveness, independently review the proof, then run the full 418-row filter. |
| (f) boundary one-directionality theorem | `RT`, rank 2 | The schema-limited no-go remains true for Moh's published skeleton alone, but a categorical `UPPER-ONLY` theorem is now challenged: the Jacobian self-different supplies the missing lower inequality termwise. Redesign (f) as a hostile review of D1-SELF-DIFFERENT, not a presumption of failure. |
| (b) global invariant | `RT`, rank 3 | Lower additive `E` invariants and total pencil `H_1`; raise only isotypic Galois `H_1`. Abstract étale gonality is rejected below as a renaming without a `D_min`-stable lower bound. |
| (e) analytic growth | `U` low, rank 4 | It has the right desired sign but no named inequality or receiver. Require an exact toy inequality that survives automorphisms in `D_min` gauge and a non-Keller polynomial-submersion control before launch. |
| (d) Hamiltonian flows | `L`, rank 5 | Completeness/LND is essentially the conjecture and the kappa class is an exact reformulation. Reuse the linear mate obstruction locally under (a); do not launch a global dynamics lane. |
| (c) characteristic p | `RT/L`, rank 6 | Stop p-curvature as proof engine; it already ran. Keep finite-field/Witt computations only as falsifiers or lifting diagnostics for one typed integral realization system. Positive modular points never constitute a characteristic-zero map. |

### Deferred fronts

1. **Moh endgame: raise to co-flagship.** Bound the first campaign slice as the number of realizable groups at `D=105`, an integer in `[0,264]`; do not call an all-D enumeration finite merely because depth is log-bounded.
2. **Pinned-N=4 realization: raise.** The first slice is the Boolean realization of the exact D=105 group in Section 7. This is the best disproof front.
3. **General reducible branch: raise modestly and keep distinct from the already-closed reducible rank-four proper-block branch.** H2 cannot by itself resolve unconditional JC2; integration #16 explicitly leaves the reducible branch untouched (`xmodel/integration16-coordinator-fable51-20260902.md:7-10,121-123`).
4. **A2: unchanged deferred.** It is an untyped map-germ with two repaired P0s, not the B3 horn. No event in this packet repairs the typing arrow.
5. **B3 degree-5/6 census: lower/stop as a window route.** Exact rows without a general-N assembly theorem gave no legal pruning and were demoted (`xmodel/integration14-coordinator-fable51-20260902.md:64-71`). Preserve the data; launch no wider census.

## 4. Reranked bottlenecks

### Proof bottlenecks

1. **Independent proof and typing audit of D1-SELF-DIFFERENT.** Check the shifted-root association, the clipped self term in `(SD3)`, every monicity/genericity hypothesis, and the claim that the enumerator's full-`V` rows exhaust one arbitrary bottom-major branch at fixed `(M_i,d_i,V_s)`. This is now ahead of every other proof task.
2. **Exact branch-packet filter.** Replace `U_rob=max U` by the attainable rational set in `(SD6)`, first permissively and then with Moh (8)–(11), Puiseux conjugacy, and all radius alternatives. The bounded first output is the surviving subset of the 418 H2 pin-4 groups in `[101,200]`.
3. **Uniform Moh endgame on survivors.** Apply the no-log residue, kappa/mate recurrence, and coefficient compatibility. The depth count is already bounded; the theorem must eliminate arbitrary sizes or show a repeatable realization pattern.
4. **General reducible branch.** Keep it parallel because a closure under H2 does not settle unconditional JC2. Do not confuse this with rank-four proper-block reducibility, already closed in the current APPROACHES overlay (`APPROACHES.md:10-22`).
5. **Nonadditive global backup.** Character-valued Galois `H_1` only. Total Euler, genus, BMY, and vanishing-cycle ranks rank below it because they have already collapsed to identities.

### Disproof bottlenecks

1. **Realize one complete D1 branch packet, not a skeleton maximum.** A branch-robust group and a rational-knapsack hit are necessary data only. Puiseux conjugacy, attachment, and Moh's remaining conditions must all hold.
2. **Polynomial mate.** Construct a monic degree-105 `g` with the chosen shifted-root tree and solve the linear equation `[f,g]=1` in degree at most 42. A formal Laurent solution is not a polynomial pair.
3. **Geometric degree and global profile.** Certify `deg_x Res_y(f-c_1,g-c_2)=4` in the declared monic quotient; then compute the nonproperness set and verify the H2/B3 typing. Correct top forms and `J=1` do not imply any of these.
4. **Actual counterexample state.** Only after characteristic zero, all coefficients, global degree, nonproperness, and noninvertibility are proved may a survivor leave `FORMAL/NECESSARY` state. Modular or all-Witt growth is diagnostic, never algebraization.

## 5. Decisive desk experiment and strongest attacks

I ran two read-only queries on the reviewed `box/moh_skeleton_N.py` enumerator; no repository file was written. The first counted branch-robust groups with `4 <= U_rob < 5` at the MOH-SHARP-2 degrees:

```text
D                         105   108   112   117   120
old H2 groups pinned N=4   14    40     0     0     5
```

The second implemented `(SD6)` as a deliberately permissive rational knapsack. For each group it collected every enumerated lower-`V` branch type, assigned resource `V_2` and weight

```text
w(V) = e V_2 [-lambda_f(delta_1(V))],
```

allowed arbitrary repetitions and unused resource subject only to `sum V_2 <= u`, and asked whether the exact rational sum can equal 4. This drops orbit multiplicities, Moh (8)–(11), and mutual attachment constraints, so it over-approximates actual packets. A hit proves nothing; a no-hit is safe conditional on the branch-type exhaustiveness audit.

```text
D                         105   108   120   total
old pin-4 groups            14    40     5      59
permissive SD6 survivors     3    16     2      21
rejected                     11    24     3      38
```

Wall time was 25.5 seconds after a sub-second extraction; memory stayed desk-scale. This is a qualitative change: the first lower-side instrument rejects 38 of the 59 first-window targets even after deliberately enlarging the branch-packet space. It also kills the visually tight D=105 example `M_2=-35`; its single-tower endpoint `360/83` must not be treated as attained, but no multiset of any allowed branch weights for that group sums to 4.

The strongest proof attack is now: independently review `(SD1)`–`(SD6)`, add a fail-closed exact-weight mode, rerun all 418 targets, and then impose genuine orbit packets on the survivors. If the 21 permissive hits all disappear, the pinned-N=4 realization list closes without a coefficient Gröbner system. If some remain, their exact packets are the only rational D1 configurations the coefficient endgame needs to see.

The strongest counterexample attack changes accordingly. Use the surviving D=105 group

```text
(D,m,K;d,e) = (105,42,21;2,5)
M = (56,103),       d-chain = 105 > 21 > 7 > 1,
V_s = 4,            (u,v) = (12,9),
```

with the permissive packet of four bottom-major discs having `V_2=3`. For each disc,

```text
delta_1=23/30,  lambda_g=-1/6,  lambda_f=-1/15,
q=eV_2=15,      w=q(-lambda_f)=1;
```

four weights sum to `N=4` and consume `sum V_2=12=u`. This is cleaner than trying to force 4 below an unattained `360/83` endpoint. It is still only a permissive survivor: first require that four copies form complete Puiseux orbits and satisfy Moh (8)–(11). If they do, construct `g`, use `(SD1)` to integrate its branch values, and solve the global kappa/mate equation. If they do not, discard it without solver work and take the next exact packet.

The branch-count output also answers the packet's concrete choice: start at D=105, not D=120. D=105 minimizes both coordinate degrees and has a depth-three chain. D=120 has fewer old pin groups but larger degrees and root packets. D=112 and 117 have no old pin-4 group at all.

## 6. Idea cards (three only)

### Card 1 — D1-SELF-DIFFERENT and exact-weight filter (`NEW`)

**Mechanism.** Use the Jacobian ODE `(SD1)` to express `N` through the shifted `g`-root different, prove the matching inequalities `(SD3)`–`(SD5)`, and replace `N <= U_rob` by the exact branch-weight equation `(SD6)`. This is the requested lower-to-floor mechanism: it injects the Jacobian below `D_1`, where Moh stops, rather than adding another boundary ledger.

**Dependencies.** Monic `deg=deg_y` gauge; characteristic zero; generic squarefree `g-c_2`; FRONTIER-N; RADIUS-ORDER with `M_1=-m`; DETECTOR-NULL; the fact that shifted roots preserve the clipped tree down to `delta_1`; disjoint bottom-major discs; exhaustive one-branch lower-`V` rows. No H2 is used in the identity; H2 supplies `N>=4` in the first filter.

**Cheapest discriminator.** A different-model hand proof on one bottom disc plus three controls: `(y,x+y^k)` gives `k` contributions `1/k` and total `N=1`; the D=105 `M_2=-35` group has no permissive weight-4 packet; the D=105 `M_2=56` group has the explicit four-weight hit. Then implement the 59-row regression and audit all vanished-leader/radius branches.

**Interpretation of outcomes.** If the proof and controls pass, the coordinator's categorical one-directionality reading is false and the campaign owns a two-sided finite filter. If `(SD3)` fails, identify the first shifted-root contact that can become smaller than its clipped unshifted contact; that is the exact flaw and Card 1 stops. If the formula is correct but all 418 rows retain a packet, it still removes joint-subtree enumeration and hands exact inputs to the Moh endgame. If the full filter empties the 418, that closes those necessary N=4 skeleton targets, not N=4 itself unless coverage of all admissible degrees is separately proved.

**Stop condition.** Stop promotion on any failure of monicity, root association, branch-type exhaustiveness, or orbit accounting. Stop scaling the DP once exact orbit constraints leave a stable large family with no uniform obstruction; pass those finite packets to coefficient elimination.

**Expected information gain.** Very high. A 26-second permissive prototype already changed 59 candidates to 21 and supplies a theorem-level explanation rather than a larger census.

### Card 2 — D=105 four-unit packet realization (`NEW APPLICATION`)

**Mechanism.** Work backwards from the simplest permissive survivor: `(M_2,V_s)=(56,4)` with four `V_2=3` bottom branches, each exact weight one. Construct a Galois-stable shifted-root tree for `g-c_2`; reconstruct branch values of `f` by integrating `dx/g_y`; then solve the global linear mate equation in the finite coefficient box `deg f<=42`, `deg g<=105`. The same computation is adversarial: failure yields a missing Moh-endgame condition; success yields the sharpest counterexample seed.

**Dependencies.** Card 1; complete orbit packets; Moh (8)–(11); subrectangular leading form `H=x^{12}y^9` after affine normalization; exact coefficient field and monic generator order; resultant degree in the declared quotient; characteristic-zero coefficients. H2 is used only to select `N=4`, not to infer a map.

**Cheapest discriminator.** Before emitting full coefficients, test whether four copies of the `V_2=3` branch type form legal Puiseux conjugacy orbits and attach inside the 60-root major class. Next run the branchwise residue/no-log gate on `dx/g_y`. Both are rational arithmetic. Only then build the sparse Jacobian linear system.

**Interpretation of outcomes.** Orbit or residue failure kills this packet exactly and promotes the failed condition into Moh's search. A finite linear inconsistency kills the typed coefficient family. An all-order formal solution is a formal survivor only. A polynomial pair with `J=1` must still pass resultant `N=4`, nonproperness, H2 profile, and noninvertibility; failure at any arrow is diagnostic, not a counterexample. A full pass is a genuine JC2 counterexample candidate and triggers independent exact replay immediately.

**Stop condition.** Stop after the first exact obstruction and generalize it before trying another support. Stop the disproof claim at `FORMAL` if algebraization or global degree is absent. Do not widen to D=108/120 until every D=105 permissive packet is classified.

**Expected information gain.** High and symmetric: either a new uniform elimination rule or the first coefficient-level seed tied to geometric degree rather than merely to a top form.

### Card 3 — isotypic Galois-closure homology (`NEW RECEIVER, LOW-COST GATE`)

**Mechanism.** Candidate (b) should not ask for total `H_1`; that is already Riemann–Hurwitz. On the generic target line, pass to the Galois closure of the degree-N cover and compute `H_1` as a rational `G`-module. Local inertia contributes induced characters, so nonnegative integral multiplicities of each irreducible representation are stricter data than the scalar Euler characteristic. At N=4 with H2 meridian type `1^2 2`, a transitive group generated by the finite transpositions is `S_4`; the first gate is therefore finite and explicit.

**Dependencies.** A typed branch-cycle tuple including infinity, not just cycle counts; connected generic line cover; tame characteristic zero; exact distinction between source `E`, line cover, and Galois closure. E-BMY scaling and total pencil homology are negative controls, not inputs to a claimed inequality.

**Cheapest discriminator.** For each existing N=4 passport row, compute the Chevalley–Weil/Lefschetz virtual character from finite transposition inertia and the infinity permutation; decompose it into the five rational irreducibles of `S_4`. Check nonnegative integral multiplicities. Run a genuine four-sheet cover as a positive control and the scalar dimension against MF-EXACT.

**Interpretation of outcomes.** A negative/fractional multiplicity kills the branch-cycle row globally without a boundary-size ceiling. If every virtual character is effective and depends only on scalar RH data, candidate (b) collapses to the known identity and stops. If only some isotypic component constrains the infinity permutation, couple that component—not total `H_1`—to the D1 packet.

**Stop condition.** One complete N=4 pass with every row effective and no information beyond scalar RH. Do not build a surface Galois closure or invoke BMY after that negative result.

**Expected information gain.** Medium. The gate is cheap and genuinely nonadditive, but Card 1 is much closer to the live obstruction.

## 7. Software acceleration

Add a fail-closed `--exact-n-weight 4 --jsonl` mode to `box/moh_skeleton_N.py`; do not create a second enumerator. The existing program already uses exact `Fraction` arithmetic and distinguishes the published-tower bound from the branch-robust maximum (`box/moh_skeleton_N.py:27-28,401-407`). The new mode should make the following join explicit:

```text
Moh group
  -> all one-bottom-branch full-V rows
  -> exact (V_2, delta_1, lambda_f, q=eV_2, w=-q lambda_f)
  -> permissive rational DP under sum V_2 <= u
  -> orbit-packet DP under Moh (8)--(11)
  -> no-log residue gate
  -> sparse polynomial-mate system
```

Each JSONL record should contain `D,m,K,d,e,M,d_chain,V_s,u,v,U_rob`, the complete typed branch list, the attainable rational `N` set, and status in the chain

```text
SKELETON -> WEIGHT_HIT -> ORBIT_HIT -> RESIDUE_HIT -> MATE_HIT.
```

The program must fail closed if two full-`V` rows map to the same displayed branch key with different radii, if a rational weight has not been normalized, or if the rows have not been certified exhaustive for the fixed group. It must never label `WEIGHT_HIT` as `REALIZABLE`. The negative control is the D=105, `M_2=-35` group (no weight-4 hit); the positive arithmetic control is the D=105, `M_2=56` four-unit multiset; the map-level control is `(f,g)=(y,x+y^k)`, whose `k` branch contributions sum to one. Regression counts before orbit constraints are exactly

```text
old groups:       (14,40,0,0,5) at D=(105,108,112,117,120)
weight survivors: ( 3,16,0,0,2).
```

This is the decisive software acceleration because it changes the unit of work from a maximum attached to a skeleton to a finite exact packet that the coefficient solver can consume. Its bounded full run is the 418 reviewed pin-4 groups for `101 <= D <= 200`; only after that regression passes should it be extended to the larger census. Do not raise the search cap merely to collect more maxima.

## 8. Current-lane calls

| Current lane | Call | Concrete instruction |
|---|---|---|
| `d1-subtree-opus5` | **REDESIGN / continue** | Put D1-SELF-DIFFERENT first. Independently try to break `(SD3)` and the bottom/minor exhaustion; if it survives, replace generic joint-tree enumeration by exact branch packets, orbit constraints, and the residue gate. Stop the old brute-force formulation once the reduction is certified. |
| `n-on-the-tree-review-grok46` | **STOP complete** | The review delivered the typed upper-only reading and its explicit full-Jacobian exception. Reopen only as hostile review of `(SD1)`--`(SD6)`, under a new charge. |
| `box01` cluster census | **REDESIGN after the paid run** | Preserve and archive the current 975-cell output, but do not spend on larger state caps. The successor is a join: exact D1 packets in, boundary-cluster realizability out. No packet means no cell search. |
| `Box03` | **STOP** | It is already stopped; nothing here repairs its launch condition. |
| `A2` | **STOP / remain deferred** | Its object is still profile-untypeable. Reopen only after a map-to-germ typing theorem, not after another capped elimination. |
| `(B3)` census | **STOP / remain deferred** | The finite degree-5/6 rows have no general-N assembly theorem. Preserve the rows and use them only to test a proposed assembly mechanism. |

Start one parallel, tightly scoped **general reducible-branch audit** once a seat is free. Its first output is not another census: it must say whether the existing rank-four proper-block closure covers any part of the general reducible residual and name the first uncovered branching type. This prevents an H2-only success from being reported as unconditional progress.

## 9. Campaign-systems check

**UPGRADE[BLIND-ALLOWLIST] — smallest useful test.** Blind custody should be enforced by a positive manifest, not a negative filename pattern. During this submission a delegated discovery command used exclusions that did not normalize a leading `./`; it exposed a few startup/charge lines from current-round prompt/log artifacts. It did **not** expose another submission body, and none of those lines is used here, but the custody state is properly `BLIND-CUSTODY-DEGRADED`, not silently clean. Every mathematical assertion and citation in this report was independently reconstructed from the charged packet and named banked reports.

The smallest regression is a temporary corpus containing four canaries:

```text
xmodel/ideation-20260902T1608Z-canary.md
./xmodel/ideation-20260902T1608Z-canary.log
xmodel/current-lane/canary.txt
xmodel/banked-control.md
```

Run the lane reader once from the repository root and once through a `./`-prefixed path. It passes only if the first three are denied before open and the banked control is readable. The production wrapper should resolve real paths, reject symlinks and every current-round basename/suffix except the single hash-pinned charged packet, then allow only a frozen manifest of banked inputs. The same manifest must be passed to the collision scanner: its default `xmodel/*.md` expansion would otherwise ingest submissions that arrived after this blind lane started. This upgrade is independent of the mathematical software in Section 7.

There is a second, nonblocking preflight diagnostic: the packet says it was frozen at `16:08:46Z` but cites integration #16 with a later-looking report timestamp. The content hash controls this lane scientifically, so I do not infer mutation; future packet construction should nevertheless reject nonmonotone displayed times or record that the timestamp is an import time rather than an event time.

## OPENs raised or sharpened — bounded quantities

- `OPEN[MOH-SHIFTED-DIFFERENT-AUDIT]` — Bound the review indicator `R_SD` by `0 <= R_SD <= 1`, where `R_SD=1` exactly when `(SD1)`--`(SD6)` pass all stated hypotheses and the enumerated full-`V` rows exhaust every one-branch local type. Also report the pilot survivor count `S_59`, bounded by `0 <= S_59 <= 59`; the permissive computation gives 21 before review and orbit constraints.

- `OPEN[MOH-EXACT-BRANCH-PACKETS]` — Bound the exact-packet survivor count `S_418` by `0 <= S_418 <= 418` after full Puiseux-orbit and Moh (8)--(11) constraints, and separately record the permissive count. This is a finite H2 target list, not a statement about all degrees.

- `OPEN[D105-FOUR-UNIT-REALIZATION]` — Bound the realization indicator `R_105` by `0 <= R_105 <= 1`: `R_105=1` only if the specified `(M_2,V_s)=(56,4)` four-unit packet has legal conjugacy orbits, zero branch residues, a characteristic-zero polynomial mate with degrees at most `(42,105)`, resultant degree four, the H2 profile, and noninvertibility.

- `OPEN[UPPER-TO-FLOOR]` — Bound `L(D)=inf N(F)` from below over noninvertible degree-minimal Keller pairs of maximum degree `D` (with `L(D)=+infinity` if none exist), and decide the indicator `B_phi` with `0 <= B_phi <= 1` for existence of an unbounded function `phi(D)` satisfying `N(F) >= phi(D)`. D1-SELF-DIFFERENT supplies the exact finite-skeleton formula `(SD6)`; it does not yet prove `B_phi=1`. In the active H2 slice the only relevant finite values are integers `4 <= N <= 16`.

## 11. Guardrail audit, priority, and stopping rule

The symbols are separated as follows: campaign geometric degree is `N`; Moh's polynomial degree is `D` (called `n` inside the reviewed script); a bottom-major **disc** is not a physical place, a dicritical flag, or a cover series; and `tau_i` denotes a generic-fibre Puiseux root, not an exit. No new exit price or first-separation charge is asserted, so no `charge_basis` declaration is applicable. The derivation uses pole orders only for roots with `p_i>0`, treats finite branches through the `max(0,...)` in `(SD2)`, and excludes the logarithmic zero-order case inside the Puiseux field.

The experiment uses exact rational arithmetic only: no `sat()`, quotient-ring remainder, derivative-labelled prime, or `M`-descent inference occurs. Future coefficient work must declare `Q[x,y] -> K((t^(1/r)))`, generator order, coefficient field, and every image check; the resultant must be normalized in the declared monic quotient and branched on vanished leaders. A packet hit is not attainment, a Laurent mate is not a polynomial mate, and a representative tree is not a full actual first-separation set.

Priority for the next bounded cycle is therefore:

1. hostile proof review of D1-SELF-DIFFERENT and branch-list exhaustiveness;
2. the 418-row exact-weight/orbit run;
3. residue and polynomial-mate elimination on the surviving D=105 packets;
4. in parallel, the small general-reducible audit and the one-pass `S_4` isotypic test.

If `(SD3)` fails, publish the smallest failed contact configuration and retire Card 1; the campaign then pivots immediately to Moh-endgame elimination plus realization, not a new scalar ceiling. If it passes but the exact filter leaves a cofinal family, stop expanding degree windows and attack the repeatable family symbolically. If it passes and the first windows empty, extend only far enough to identify a uniform descent or periodic obstruction. The campaign should judge success by a proved elimination/realization rule, not by the number of rows enumerated.

## COLLISIONS

status: CANDIDATES

- `OPEN[MOH-SHIFTED-DIFFERENT-AUDIT]` (report:310): NONE

- `OPEN[MOH-EXACT-BRANCH-PACKETS]` (report:312): NONE

- `OPEN[D105-FOUR-UNIT-REALIZATION]` (report:314): NONE

### OPEN[UPPER-TO-FLOOR]

- `AUDIT.md:15024` — degree-N Keller maps); NO-CEILING[LATTICE-LEDGER]; MERIDIAN-FLOOR+
- `xmodel/meridian-floor-sharpen-opus5-20260902.prompt.md:4` — MERIDIAN-FLOOR: for a noninvertible Keller map of geometric degree N
- `xmodel/meridian-floor-sharpen-review-gpt55-20260902.prompt.md:5` — floor is an IDENTITY: for the degree-N cover C_L = F^{-1}(L) -> L over a
- `xmodel/n-on-the-tree-review-grok46-20260902.md:153` — **FILTER-INVERSION (CH:431–438).** A Moh skeleton determines an **upper** bound on `N` and no lower bound. `"N<=16"` is compatible with every skeleton: even those with `U>16` may have `N` as small as 2 if the `D_1` sub-tree adds enough c...
- `xmodel/n-on-the-tree-review-grok46-20260902.md:234` — **CAN.** Write `N` as a contact deficiency on the tree of `f g` (FRONTIER-N), for dominant maps in the monic `deg=deg_y` gauge. Evaluate an **upper** bound `U(skeleton)` from Moh's major-tower data alone, after DETECTOR-NULL sets every m...
- `xmodel/n-on-the-tree-review-grok46-20260902.md:236` — **CANNOT.** Prove any ceiling `D<=C(N)` (the skeleton bounds `N` above; a `D`-ceiling at fixed `N` needs a floor on `N`). Compute `N` exactly from a skeleton (`D_1` sub-tree is free in Moh's recursion and can drive the contribution from ...
- `xmodel/n-on-the-tree-review-grok46-20260902.md:268` — Carried: `OPEN[MOH-ENDGAME]`, `OPEN[NONPROPER-DEGREE]`, `OPEN[SUBRECT-ORBIT-BRIDGE]` (INT15 CLOSED YES, not reopened), `OPEN[ANTICANON-DEFECT]`, `OPEN[SAT-MASS]`, `OPEN[DELTA-AFF-VS-N]`. **Retyped, not promoted here:** `OPEN[NU-BOUND-AT-...
- `xmodel/n-on-the-tree-review-grok46-20260902.md:274` — Flag/place/series: Moh `n=D` never mixed with campaign `n=deg Abar_F`. `d_i`, exponent `d` of `H^d`, and GGV `d_j` kept apart. Major tower / minor discs / `D_1` sub-tree carry different theorems (Def 5.1(1); Prop 6.1; nothing). The Sn/pr...

Collision adjudication: the three new finite questions have no lexical candidate. The `UPPER-TO-FLOOR` hits are the known meridian identity, ledger no-ceiling result, and N-on-tree upper-only review; none imposes the shifted-root self-different. In particular, the review's quoted `CANNOT` applies to the Moh skeleton with the `D_1` subtree free and explicitly motivates the new full-Jacobian audit. These candidates establish provenance; they do not close the sharpened OPEN.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `37875`.
- Body SHA-256:
  `2a13ef95cedb341bb8fc4b6f187d667ac052fac21a9c2538a424fc7e30629844`.
- Frozen basis: `7f7c0306768dd6393e9a8dd161d594df9f14c8d0`.
