# Fullunit offline deployment preparation

OFFLINE / DISABLED / UNEXECUTED. First action2026-09-10 17:29:35UTC; reserve17:45/hard17:48 never reset. Owned targets absent and all seven exact input pins matched before bodies. No actual worker/registration/clocks selected, baseline/candidate read/hash/parse, scientific subprocess/import/AST/syntax/compile/test, network/AWS/SSH/process/Git/corpus/protected/shared action.

## Delivered metadata

Four deployment templates, ROOT-FINALIZATION-CHECKLIST, PINS and READ-SCOPE. Registration schema/job is fullunit; enabled remains false. The complete thirteen-vector sequence references the accepted dispatcher/mutator and the original five/fullunit three/wrapper three layout. No science/probe files are copied or changed; their path/hash references are inherited or taken from the charged dispatcher. Native collector is byte-identical to its charged template. Setup is a narrow path/role/hash migration, not a new scientific instrument.

Twenty unique install references include deferred ROOT/native/registration files. The old historical status-fix gate deployment record is omitted; the current accepted fullunit runtime gate replaces the old runtime gate reference. Probe's inherited highest status-fix source reference is deliberately preserved because the new fullunit source box has no probe copy. Baseline remains only a future literal reference and was never accessed. The separately secured highest result is not an input or premise.

## Contract preserved

Exactly nine accepted preflights then fullunit-produce/positive/mutate/negative. Four typed ROOT_REGISTRATION_SHA256 holes occur only after the matching flag in the final four vectors; there is no candidateSHA hole. Unknown candidate digest is obtained only from the authentic root-frozen positive receipt before mutation. Metadata vector lengths51(probe controls/dummy),46(two startup),54(each fullunit entry),55(mutation) are string counts, not runtime observations.

Joint600wall/500CPU/2GiB/16MiB, profiles control5/3,dummy5/3,fullunit120/99,mutate30/24 and all original admission/cutoff/quiet/current-source/native/fsync/custody checks remain. Outer660seconds/5secondstop/control-group/2GiB/Tasks32 are unchanged. Producer normal2 with INCONCLUSIVE_NO_CERTIFICATE and no candidate ends at10calls; it is not a bad-prime or properness diagnosis. The13-call success requires genuine fullunit positive receipt plus exact unchanged-checker198/188/181 negative rejection and remains FULLUNIT_CHECKED_NEGATIVE_CONFIRMED/scienceNONE. No lift/source-zero/JC2 conclusion or extra computation is included.

## Finalization ordering and remaining authority

ROOT can finish worker-independent metadata review before allocation. Actual EC2/host/boot/ns/device and native hashes can only be observed AFTER separately authorized allocation. All must then be finalized, pinned and authenticated BEFORE any science. The checklist makes this distinction explicit; no impossible before-allocation physical attestation is claimed.

All unresolved physical/native/ROOT/UTC fields use JC2_*_PLACEHOLDER; setup's existing refusal guards remain. Python3.12 paths/UID65534/GID65534 are inherited fixed profile requirements, not claims about an allocated worker. ROOT must verify them and stop on mismatch rather than silently retarget. It must resolve per-control authority hashes, preserve four allowed self-hash holes, independently verify all complete expanded vectors, root0444 ancestry/source inventories, fresh paths/mount/durable target and original deadline fit. The sealed templates are not enabled copies or an execution invitation.

Every main unit, cgroup, TERM/KILL timer, backup-stop/harvest/cancel reference must derive from the SAME JC2_BATCH_SUFFIX_PLACEHOLDER binding. The T1620/T1720 historical typo is not copied as an actual suffix. ROOT owns the independent worker backup and final cleanup; all timer identities must be checked literally against the one main unit before launch and harvest.

## Verification and scope

All seven inputs read WHOLE or explicitly same-byte prior-WHOLE reused after current pins. All four owned templates and documents read whole; clipped combined metadata output was recovered for the final four vectors. Documentary checks confirm enabledfalse, exact labels/profiles/token positions, unique install destinations/basenames, aligned install/registration/setup pins and byte-identical native collector. No template or science source is executed or syntax-checked; metadata parsing/hashing is not runtime validation. Actual costs, sufficient place, witness existence, live observations and semantic outcomes remain unknown.

No new science gate, code change, solver, prime/cut/cap farm, worker allocation or follow-on is authorized. Pending ROOT finalization/explicit execution release is the single remaining deployment obligation; cheapest next check is ROOT's metadata preflight on finalized copies after its chosen allocation, before science. No new canonical OPEN. Own WHOLE/OPEN/collision/current-pin checks precede marker and transaction seal; custody FIRST/all writers IDLE at terminal handoff.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5165`.
- Body SHA-256:
  `078ea1b5f74194007bfd04780d51b08069f361910079cea70a753d3aefc795af`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
