# Grok 4.6 cross-pollination and adversarial synthesis — round `20260827T0935Z`

Author: Grok 4.6, equal-standing JC2 co-researcher
Date: 2026-08-27
Status: **CROSS-REVIEW AND MERGE. NO JC2 PROOF OR COUNTEREXAMPLE. NO
PROMOTION. NO AWS JOB LAUNCHED. NO CAMPAIGN ARTIFACT EDITED EXCEPT THIS
FILE.**

This lane was started because new Claude/Fable/Opus calls hit an account
cap.  That resource event is not evidence about the mathematical quality of
any of the four blind submissions.

---

## 0. Custody and what was actually executed

All thirteen required SHA-256 values were recomputed locally and match the
prompt exactly:

```text
e871e3d71a8777ca50f7c796bf7547023a8c2ca8e2930c6a0981ddcebd6ae3db  xmodel/ideation-20260827T0935Z-packet.md
98a494a561f39cfef770eec11c07eca3e17e4e9fac61a1a3d143f614d991b90c  xmodel/ideation-20260827T0935Z-sol.md
098b81d19cba6f278e93a809e60a3a4892eea7979758ffe6b0bb2997163b98c0  xmodel/ideation-20260827T0935Z-fable5.md
265cfa731dd0e1614966c5eec3367d0dc38a959a2b43a848256c64c4be0b6217  xmodel/ideation-20260827T0935Z-grok.md
3295402670b65b0a61d56aa9d4f117084b282d649eb72a2afd7df6fe3195782e  xmodel/ideation-20260827T0935Z-opus5.md
bc539ba91600b95e5400e96635e8973ef6cb6454b9641c0c68d09a99aef0690f  xmodel/max12-812-order2-p0-total-rees-j2-a1-two-fibre-generic-eliminant-hostile-review-grok-20260827.md
7f0c3aea3ef9cf299b2fac00f62627d765c92e0b721efbe88248a69390a92bcd  xmodel/max12-812-order2-k00-d8-hostile-review-opus5-20260827.md
039590c8524e98e0fce3f6ada46fdf338270ece56c3aea553644c83911b7f7d7  cases/max12_812_order2_u2_62_k00_colon_local_v14r1_20260827/RESULT.md
c2efa158bbc494b0d89d556c747e229cca069133fc7b3f30e5cbba3b19e5617c  cases/max12_812_order2_gate_t_uniform_contact_naturality_v46_20260827/RESULT.md
a1f18d28e2f2edf556f91a77f6e80fbbe210e3cc801b0d54ec9b972dcbb906e2  xmodel/max12-812-order2-gate-t-uniform-contact-shift-naturality-interface-review-repair-v2-sol-20260827.md
2354703a559a5da2b4f2032560a5740b2cda1f3160f5c6598dcdbe1c4c1432be  xmodel/max12-812-order2-gate-t-strict-uac-three-row-syzygy-interface-sol-20260827.md
be7bb44171b4a41ef0c0685476e8104e760335f12f51a37b06420273b1456b0d  xmodel/ideation-20260827T0935Z-jelonek-primary-check-sol.md
```

Theorem baseline for every `T-a1` sentence below is the two-fibre hostile
review SHA `bc539ba9…` (§7 equivalences, §8 safe compute plan).  Post-snapshot
context in the prompt is treated as unreviewed delta, not as a promotion.
V45 `RESULT.md` was read because the prompt requires integrating that
producer; it was not on the SHA list and is used only as producer-tier
scope.

No CAS, no Groebner engine, no AWS, no network, no `jc2-lean` contact, no
canonical-ledger edit.  The converter and the eliminant-predicate arguments
in §§2–3 are hand algebra in the polynomial ring.

---

## 1. Executive verdict

Four independent co-discoveries survive, and three advertised decision
procedures collapse to two already-running jobs plus one review.

1. **The frozen ordered `T-a1` chart is decided by the full 59-row
   generic fibre over `Q(rho)`.**  Fable and Opus both found this.  The
   equivalences are true after the graded-unit repair.  Finite `rho=c`
   fibres are heuristic only.  The special-fibre half is promoted V42, not
   the unpinned 17-row basis.  The N-ladder cannot prove existence and is
   not on the critical path while the generic run is live.
2. **Opus’s claimed eliminant third outcome is algebraically empty once
   the dehom special control `I+(t)=(1)` holds.**  A nonzero `t`-divisible
   element of `I ∩ Q[t]` then yields one with nonzero constant.  The current
   nonzero-constant predicate is already equivalent to `E ≠ 0` on that
   hypothesis.  The software needs an inconsistency sentinel, not a new
   success branch.  Opus’s proposed `chart-closes-by-escape` token is
   dangerous if the special control is not actually passing on the same
   ideal.
3. **The constructive converter holds.**  From `a1^M − B = t H` with
   `B ∈ J` and `v a1^D ∈ J`, `v = t^s q`, one has `q a1^(D+Ms) ∈ J`.
   Replay obligations are listed in §3.  The `a1^104` candidate is not an
   input until that replay exists.
4. **K00 local membership is a producer, not a card.**  D8 is independently
   PASS.  V14R1 is the colon that Sol/Fable/Opus all proposed, producer-PASS,
   review-pending.  D9+ stays stopped.  The live mixed object is the class
   of `b1 − phi1(q0)` in `(A/I)/im(Syz(phi0) → A/I)` before honest source
   restriction.  Polynomial syzygy evaluations are complete for forcing and
   incomplete for non-forcing.
5. **Serial `ACT-TOT-G22/G24/…` leave the mathematical unique-`AC`
   critical path** after the V46 schema/verifier survives its own
   different-model review and each endpoint linker checks compiler semantics.
   They do not license coverage, ramified `rho=0`, equality faces, or Gate T.
   The three-row lemma is complementary, not a replacement of V46.  V45 is
   one contact.
6. **Opus adds significant capability beyond Fable**, but not on the
   two-fibre theorem itself.  Fable’s selected-row one-sidedness is the
   statement to keep.  Opus’s 17-row negative branch is the most dangerous
   error in the four blinds and is discarded.

Nothing here proves or disproves JC2, Gate T, order two, maximum twelve,
`G2-PSC`, `G2-BD`, a cofinal `td` bound, a terminal receiver, or an AS109
lift.

---

## 2. Adjudication of Fable and Opus on the two-fibre theorem

### 2.1 Shared claim, repaired

Both assert, for the frozen homogeneous ordered-`a1` total ideal `J`,

```text
K+(rho)=(1)
  <=>  a1 ∈ √(J S')
  <=>  a1 ∈ √(J Q(rho)[X])  and  a1 ∈ √(J0)
  <=>  J|_{a1=1}=(1) over Q(rho)  and  J0|_{a1=1}=(1) over Q,
```

with `S' = T^{-1}S`, `T = {u ∈ Q[rho] : u(0) ≠ 0}`, `K = J : a1^∞`,
`J0 = J|_{rho=0}`.

**Verdict: CONFIRMED as equivalences.**  Proof of dehomogenization is the
graded-unit lemma in the two-fibre review §3.2, not Fable’s `G_m` +
Nullstellensatz-over-`Q(rho)` sketch and not Opus’s component-and-cone
sketch.  Both sketches fail over `Q` and `Q(rho)` for the same two reasons:
`z ↦ z^5` is not surjective, and the affine Nullstellensatz is not valid
over those fields.  The conclusion is nevertheless true over any field.

Opus’s Theorem A (2) adds the navigation sentence that, given `a1 ∈ √J0`,
the criterion is equivalent to `E ≠ 0` where `E = K ∩ Q[rho]`.  That is
the same theorem: special unit plus generic unit forbids `E ⊆ (rho)`.  No
exponent `N` appears in the *decision*.  An exponent still appears in the
typed certificate and in the `v(0)=0` converter.

Special-fibre half: pin promoted V42 (`5d4c42ff…` / `a4f6b931…`) with the
`ez9` spectator transport.  A 17-row `rho=0, a1=1` unit, *if* the named
rows are the V42 cascade rows, is algebraically sound for the conjunct and
is not the load-bearing pin.

### 2.2 Finite `rho=c` fibres — both heuristic only

| Claim | Source | Verdict |
|---|---|---|
| Modular `(p, c≠0)` census is a theorem in either direction | Fable F1 stage 1 | **REFUTED** as a theorem; legal as a resource probe |
| Exact-`Q` unit at one rational `c` is decisive-grade generic evidence | Fable F1 stage 2 | **REFUTED** (two-fibre review: `I=(x, rho-1)` is generic nonunit and unit at `c=2`) |
| Random `t0` empty fibre is overwhelming evidence of generic emptiness | Opus §6.1 | **HEURISTIC**.  Empty closed fibres of a generically nonempty family are a proper closed set.  A random empty fibre is the expected behaviour of an exceptional empty fibre, not a proof |
| Random `t0` nonempty fibre indicates `E=0` | Opus §6.1 | **HEURISTIC**, directionally the better of the two, still not a theorem.  Must confirm over `Q(t)` |
| Several independent nonunit fibres ⇒ skip the `Q(rho)` run and hunt a survivor | Fable F1 stop | **NAVIGATION only**.  Must not be reported as `K+(rho) ≠ (1)` |

Keep Fable’s stages 3–4 and the two-fibre review’s safe plan.  Demote every
finite fibre to a software probe.  Do not skip the full 59-row `Q(rho)` run
on consistent modular nonunits.

### 2.3 Selected-row one-sidedness — keep Fable, discard Opus’s negative

Fable: a selected-row generic unit with extracted `U(0) ≠ 0` is a positive
certificate for the full ideal; a selected-row nonunit proves nothing.

Opus Card A both-outcomes: 17-row nonunit ⇒ “horizontal obstruction, no
certificate at any `N`”, and “the whole total-`T-a1` certificate programme
is dead.”

**Fable is correct.  Opus’s negative is false** (two-fibre overstrong O4).
The eight rows that vanish at `rho=0` are invisible to every 17-row and
every `J0` computation and can still kill the generic fibre.  `Tg19_2`’s
`(3/8)rho^2 ez9` pivot exists only on the generic fibre.  A 17-row nonunit
is an invitation to escalate to all 59 rows, not a chart-negative.

Opus’s own §8.9 records this asymmetry and then Card A’s negative branch
ignores it.  That is the round’s most load-bearing writing error.

### 2.4 Opus’s eliminant-predicate “bug”

Opus §6.2: the compiler sets `unitfactor` only from an eliminant generator
with nonzero constant term, and therefore conflates

```text
E' = 0                 generic fibre nonempty, chart does not close
E' ≠ 0 but E' ⊆ (t)    generic fibre empty, chart closes by Theorem A
```

into one token `no-unit-eliminant`.

**Ask, as charged.**  Suppose the dehom special control is `I+(t)=(1)`.
Then `t` is a unit in `R/I` for `R = Q[t,Y]`, `Y = X \ {a1}`.  Let
`E = I ∩ Q[t]`.

**Lemma.**  If `I+(t)=(1)` and `0 ≠ p ∈ E` with `p = t^s q` and `q(0) ≠ 0`,
then `q ∈ E`.

*Proof.*  `1 = i + t h` for some `i ∈ I` and `h ∈ R`.  Raise to the `s`-th
power: `1 ≡ t^s h^s (mod I)`.  Multiply by `q`: `q ≡ t^s q · h^s = p h^s
(mod I)`.  `p ∈ I` so `p h^s ∈ I`, hence `q ∈ I`.  Also `q ∈ Q[t]`, so
`q ∈ E`.  ∎

**Corollary.**  If `I+(t)=(1)` and `E ≠ 0`, then `E` contains an element
with nonzero constant term, i.e. `E ⊄ (t)`.  A reduced Gröbner basis of the
principal ideal `E ⊂ Q[t]` is generated by a polynomial not divisible by
`t`.

The branch `E ≠ 0` but `E ⊆ (t)` is therefore **algebraically empty** on
the hypothesis the special control uses.  Given that control, the current
nonzero-constant predicate is already equivalent to `E ≠ 0`.  It does not
need a new mathematical success token.  It needs an **inconsistency
sentinel**: if the special control passed on the *same* ideal `I` and the
engine emits a nonzero `t`-divisible candidate, the engine failed to return
a reduced generator of `E` (or the special control is false).  Report
`ELIMINANT_INCONSISTENT`, content-reduce or recompute.  Do not report
`chart-closes-by-escape`.  Do not report `no-unit-eliminant`.

Without the special control, `E ≠ 0` and `E ⊆ (t)` is possible and is
exactly overstrong O2 (`J = (rho)`): generic unit, special nonunit,
`K+(rho) ≠ (1)`.  Treating a `t`-divisible eliminant as success in that
case would be a chart-closing bug.  Opus’s proposed fix (4) is vacuous when
the special control holds and dangerous when it does not.

Scope: the compiler’s `t=0` control is a 17-row control.  V42 is the full
`J0` special-fibre theorem.  Do not mix them.  For the full 59-row generic
run the special conjunct is V42; the promotion object remains an explicit
`a1^N U(rho^2) ∈ J` with `U(0)=1`.

**Predicate disposition:** current nonzero-constant test **KEEP** as the
promotion test.  Add the sentinel.  Do not implement Opus’s third success
branch.

### 2.5 Other Fable/Opus two-fibre defects, compressed

- Fable’s converter paragraph (“rehomogenizes directly to `a1^D u` with
  `u(0) ≠ 0`”) is false as a general algorithm (two-fibre §5.3 toy
  `J=(rho^2, a1^2)`).  Repair is §3 below.
- Fable’s “first honest surviving landing geometry” oversells a
  prefix-relative frozen negative.  Not all-depth, not Gate T, not a
  landing-side counterexample.
- Fable’s origin-funnel (§3.3) is true as support geometry over an
  algebraic closure: every homogeneous saturated-chart survivor meets the
  terminal origin.  The receiver stays load-bearing under either generic
  outcome.  Grok’s blind submission said the same in different language
  (reduced special fibre on `D(a1)` is empty, points live at `a1=0`).
- Opus’s geometric proof of Theorem A (2) is the same Nullstellensatz
  defect as Fable’s sketch `(iii)`.  Conclusion kept, proof replaced.
- Opus’s “the exponent is irrelevant” is true of the decision and false of
  the typed certificate / `v(0)=0` converter.
- Both toys (Fable’s odd `x(x−rho y)`, Opus’s even `f − t x`) are correct
  positive controls on the lemma.

---

## 3. Constructive split and the total converter

**Claim, as charged.**  In `S = Q[t,X]` (or `Q[rho,X]` with `t = rho^2`),
`J ⊂ S` an ideal, suppose

```text
a1^M − B = t H,     B ∈ J,
v a1^D ∈ J,         v = t^s q ∈ Q[t],   q(0) ≠ 0.
```

**Derive:** `q a1^(D+Ms) ∈ J`.

*Proof.*  `a1^M − t H = B ∈ J`, so `a1^M ≡ t H (mod J)`.  Then
`(a1^M)^s − (t H)^s` is divisible by `a1^M − t H`, hence lies in `J`.  So

```text
q a1^(D+Ms)
  = q a1^D (a1^M)^s
  ≡ q a1^D (t H)^s
  = (t^s q) a1^D H^s
  = (v a1^D) H^s
  ∈ J.
```

No nonzerodivisor hypothesis on `t` is used.  If `J` is homogeneous, `t`
has weight 0, and `q` has weight 0, the output is homogeneous of weight
`5(D+Ms)`.  Normalize `q/q(0)` and, if the reviewed typed form is required,
even-average in `rho`.  This *is* a cofactor in `T`.

This replaces the two-fibre review’s unspecified syzygy-colon at §8.4
whenever an explicit special identity of the displayed shape exists.  It
does not replace the generic-fibre decision: it converts a generic Bézout
with `v(0)=0` into a typed `U(0)=1` identity after an effective `M` is
known.

**What must be replayed, and what does not yet exist.**

1. **Special identity in the total ring, not only in `J0`.**  A certificate
   `a1^M ∈ J0` with polynomial multipliers `m_i` lifts automatically:
   `a1^M = ∑ m_i (f_i|_{t=0})` and `f_i = f_i|_{t=0} + t δ_i` give
   `a1^M − ∑ m_i f_i = −t ∑ m_i δ_i`, so `B = ∑ m_i f_i ∈ J` and
   `H = −∑ m_i δ_i`.  This lift **fails** if the `m_i` themselves were
   computed after setting `t=0` in a way that does not produce polynomials
   over `Q[t]` (typical Groebner/Nullstellensatz multipliers at `t=0`).
   The constructive tree’s candidate `a1^104 ∈ J0` is therefore not an
   input to the converter until replay exhibits either polynomial `m_i` or
   an explicit total identity `a1^104 − t H ∈ J`.  No claim until that
   replay.  Do not treat `104` as `N_{J0}`.
2. **Generic identity** `v a1^D ∈ J` as an explicit combination of the
   literal total rows, with `v` factored as `t^s q`, `q(0) ≠ 0`.
3. **The binomial identity** coefficientwise, as a polynomial, not as a
   Gröbner reduction.
4. **Unrestricted weight-`5(D+Ms)` replay** against every complementary
   homogeneous product, V43-style, including a one-coefficient mutation
   control.  A seeded or tree-produced identity that fails unrestricted
   replay is a failed seed.
5. **Even-average and `U(0)=1` normalization** if promoting to the reviewed
   typed form.
6. **V42 existence is not a substitute for (1).**  It makes the converter
   non-constructive.  Do not wait for an effective `M` to *run* the generic
   decision; wait for it only if the generic Bézout lands in the `v(0)=0`
   branch.

If `s=0` the construction is idle: the generic Bézout is already in `T`.

---

## 4. N6 / N7 / high-memory versus the generic decision

Wall-clock, not token volume.  The only two-sided decision is the full
59-row `Q(rho)` Gröbner run, which the prompt says is launching.  Selected-row
jobs remain one-sided.  Everything else is a floor-raiser, a converter
input, or a custody audit.

**Already answered, stop as discovery.**

- V43 `a1^6 ∉ J0`: stop rebuilding weight 30.  Review may finish as
  lifecycle.  Descendants stay `PROVISIONAL` until that review.
- The N=7 *fixed seed* `Lambda_7(a1·m)=Lambda_6(m)`: 924 residual equations
  and no correction unknown.  That seed is refuted.  This is not N=7
  nonmembership, and it is not a license to launch an unseeded weight-35
  dual while the generic run is live.

**Critical path.**

| Job | Role | Stop |
|---|---|---|
| Full 59-row `Q(rho)` GB, all nonzero general-rho rows including the eight rho-only rows and `Tg19_2`, linear `ez9` pre-elimination legal | Two-sided frozen-chart decision | **Unit** (GB meets `Q(rho)^*`): go to converter (§3); stop all N-duals, stop high-mem multiplier extraction, keep selected-row only as independent certificate replay.  **Complete nonunit**: `K+(rho) ≠ (1)` for the frozen grade-through-19 chart; stop all N-duals and multiplier extraction; unhold grade-20+ as navigation only; survivor parametrization is a new producer, not Gate T.  **Timeout/memory/incomplete**: `NO VERDICT`, not a negative; keep the rows below |
| Selected-row generic unit with extracted `U(0) ≠ 0` | One-sided positive, can finish first | Same as full unit.  A selected nonunit is discarded |
| Constructive special-fibre tree (`a1^104` candidate) | Converter input `M`, not a decision | Continue until replay or a registered cap.  Success supplies `M` for §3.  Failure does not reopen N-duals by itself |
| Finite-field specialization of homogeneous `J0` testing `a1^N` (Grok Card A) | Floor-raiser only; nonmembership specializes | Run **only if** the generic run is `NO VERDICT`.  Specialized membership is not `N_{J0}`.  Cap at the constructive `M` if pinned, else 16.  Negative control: specialized `a1^6` remains a nonmember |
| Unseeded N=7 dual | Floor-raiser, expensive | **HOLD**.  Launch only if generic is `NO VERDICT`, the 104-tree has not supplied `M`, and specialization has not skipped N=7.  Unrestricted replay mandatory.  Do not retry the dead seed |
| 192-GiB / 1-TiB tracked multiplier extraction | Failed memory strategy | **STOP** as critical path.  One cheap `lift` on the smallest sufficient row subset is optional calibration, not a decision |
| Per-`N` DVR ladder as existence proof | Constitutionally one-sided | **STOP** as a decision procedure.  Floor-raising fallback only under generic `NO VERDICT` |

Asymmetric finality stands: a frozen unit persists under later rows of the
same presentation; a frozen nonunit is prefix-relative.

Do not serialize the generic run behind N=7, behind the 104-tree, or behind
V43’s result review.  Review runs in the background.  If V43 is later
REFUTED the floor drops to V38’s `N ≥ 6`; that does not change the generic
decision.

Resource: one large-RAM AWS box for the 59-row `Q(rho)` run; selected-row
and the 104-tree continue on already-live instances; specialization, if
needed, is flint-only on a small instance.  Do not split the 512-vCPU quota
between an unseeded N=7 dual and the generic run.

---

## 5. K00: D8, V14R1, mixed quotient, syzygy completeness

### 5.1 D8 is a theorem at its stated scope

Opus5 hostile review SHA `7f0c3aea…` is **PASS**.  Independently:

```text
r7 ∈ (r1,…,r6) + m^9     in Q[d0,…,d5], at C6=1,
```

with rank `2547 = 2547` over exact `Q` and a 492-entry lift replaying all
2996 equations.  Refused inferences in that review stand: not polynomial
membership (V8), not local or formal membership, not a finite obstruction
in either direction, no loads, no `Lambda`, no receiver, no JC2.

D8 is now a corollary of V14R1 *if* V14R1 survives review (Krull: local
membership implies every finite order).  Keep D8 as a jet cross-check of
the colon multiplier, not as a reason to run D9.

### 5.2 V14R1 is the colon card, producer-only

Exact producer statement: `r7 ∈ I R_m` with

```text
h = 63*d4 + 20,    h(0) = 20 ≠ 0,
h r7 = ∑_{i=1}^6 u_i r_i,
```

fresh-process residual `0`.  Compatible with reviewed global nonmembership.
Scope firewall in the `RESULT.md` is correct: unloaded normalized `C6=1`
coefficient slice only.

Sol Card 1 / Fable F2 / Opus Card B asked for exactly this computation.
**Do not relaunch a colon.**  The remaining job is different-model hostile
review of V14R1, including:

- completeness of the claimed 87-generator syzygy module, not a truncated
  degree window;
- equality of the seventh-coordinate projection with `(I:r7)`;
- jet agreement of the `h`-normalized multipliers with the promoted
  degree-7 273-entry lift and the D8 492-entry lift;
- independence from V14’s custody-failed matrix writer.

If review PASSes: local membership is a theorem; the `(d)`-adic ladder is
infinite; D9+ stays stopped; “first filtered obstruction degree” does not
exist and is removed from the obligation list.  If review FAILs: the colon
question reopens; D9 stays stopped anyway (two non-informative filtered
degrees plus a producer that must not be laundered).

Grok’s blind Card B local-order repair is **superseded**.  V11/V13 stay
stopped as software.  One transform-replay attempt is not worth a slot
once a global colon producer exists.

### 5.3 Mixed first-order class, representation invariance

The live mixed target, before honest source restriction, is the class

```text
[b1 − phi1(q0)]  ∈  (A/I) / im(Syz(phi0) → A/I).
```

This is the first-order obstruction module of a presentation: `q0` is a
0-jet solution, `phi0` the order-0 map, `phi1` its linearization, `b1` the
order-1 inhomogeneous term, and `Syz(phi0)` the first-order automorphisms
of the order-0 equations.  The class vanishes iff `q0` lifts to first
order up to syzygy.

**Representation invariance, as a class in that coker.**

- Changing generators of `I` does not change `A/I`.
- Replacing `phi0` by `phi0 ◦ U` for `U` invertible, or adding a redundant
  generator with a splitting, changes `Syz` by a quasi-isomorphism and
  leaves the image in `A/I` stable.
- Replacing `q0` by `q0 + s` for a syzygy subtracts `phi1(s)` from the
  numerator; if `phi1(s)` lands in the image, the class is unchanged.

The coker itself *does* depend on whether `I` and `phi0` are the honest
equations.  Compilation, Rees, and chart generators can kill classes that
the owner source does not, and can manufacture classes the owner source
does not see.  That is why the prompt’s “before honest source restriction”
firewall is load-bearing.  Vanishing on this mixed quotient is necessary
for a compiled first-order lift and is not sufficient for an honest
Faber-source germ.  Do not infer `H_K00`, Taylor realization, closure,
receiver, or JC2 from vanishing or from nonvanishing.

### 5.4 Localization completeness of polynomial syzygy evaluations

Let `N = Syz(phi0)` as a polynomial module, `N_m` after localizing at the
K00 maximal ideal.

- A finite generating set of `N` generates `N_m`.  An incomplete
  generating set (truncated degree, missing generator, biased harness)
  under-generates `N` and **over-estimates** the coker.
- Consequently: **polynomial forcing is one-sided complete.**  If a
  polynomial syzygy forces `M2(0)`, `M4(0)`, or `M6(0)`, the same identity
  holds locally.  Extra localized syzygies can only force more.
- **Polynomial non-forcing is incomplete.**  A nonzero polynomial class
  may die after inverting a unit at `m`.  Local vanishing can require a
  colon element that a truncated Syz missed.

V14R1’s unloaded colon is the completeness argument for `r7`: full Syz,
seventh coordinates equal the colon, a unit-at-`m` denominator is
exhibited.  The mixed calculation must meet the same standard.

The preliminary full 87-syzygy evaluation that “suggests `M2(0), M4(0),
M6(0)` are all forced” used a harness biased *against* the `M6`-forced
outcome.  That bias, if it discarded forcing syzygies, would make forcing
harder to see, so a forcing verdict that survived the bias is directional
evidence — and is still **not a theorem**.  V16R1 is the replay.  Treat
the triple forcing as provisional.  If V16R1 confirms it, Grok Card B’s
attainability test becomes “does the owner source realize these *forced*
leading values”, not a free search over jets.  If V16R1 refutes `M6`
forcing, the mixed class is still the object; do not revert to D9.

---

## 6. Uniform V46, three-row contacts, V45

### 6.1 What is already a formula theorem

The uniform contact-shift naturality identity survived Fable5 hostile
review after Sol’s V2 repair: coefficient extraction is the displayed
jet-reindexing map `R_tot[[sigma]] → R_D1[[sigma]]`, not bare sigma-adic
continuity; cancellation only raises valuation, so a raw census is safe
and over-retaining.  Source identity over any `Z[1/2]`-algebra, no unique-`AC`
inequality and no `rho` localization.  Shifted-root maps separately use
`D(rho)`.

V46 is a producer-PASS of the schema/verifier (`PASS-UNIFORM-CONTACT-NATURALITY-V46-V0-V4`),
pending its own different-model implementation review.  Ten endpoint
families are promoted in the schema; `(a,d,r)=(8,3,8)` is
`confirmed_but_unpromoted`; the nearby artifact named “promotion” is
`PROMOTED ROUTE FALSIFICATION ONLY`.  The eleven-family union is
conditional.

Opus §6.3’s missing-primitive guard is a real interface defect the formula
review did not close: formula (3.2) can *under*predict a ceiling if a
primitive is missing from the licensed inventory.  Add the two-sided check
`max^raw ≥ max^licensed` plus a registered cancellation certificate for
any gap, as a V46-reviewer attack, not as a new AWS lane.

### 6.2 Serial exporters

**Mathematical critical path:** `ACT-TOT-G22`, `ACT-TOT-G24`, and later
per-grade unique-`AC` exporters may leave it after

1. V46 schema/verifier survives different-model review, and
2. each concrete linker has mechanically checked that endpoint’s compiler
   semantics and provenance.

Until both, the exporters are workflow artifacts that may finish in the
background; they are not proof obligations and they are not a reason to
block the generic `T-a1` or K00 mixed jobs.

`(2,5,≥3)` V44R1 / V45 is a chamber endpoint and a control on the
interface, not a template for `G26`.  V45 producer: for
`ord(A)=2, ord(C)=5, ord(R)≥3`, `G=17`, `T=20`, the verifier reports the
finite-jet identity and a provisional contact exclusion on `D(rho*k)` by
the reviewed `B23` endpoint.  Status is exact-Q compiler/validator PASS,
contact exclusion pending different-model review.  One contact only.

### 6.3 Three-row syzygy interface

The algebraic lemma is elementary and correct as a radical statement: on
`D(rho) ∩ (D(A0c) ∪ D(A1))`, `g1=g2=g4=0` forces `C0c=C1=0`, contradicting
exact nonzero leading `C` given exact nonzero leading `A`.  No root
allocation, Hensel, deck split, or leading-`k10` unit is used.

It can replace twelve low-band endpoint proofs only after a frozen
row-level linker and different-model review certify purity conditions 1–4
for each manifest.  Low-band walls are mathematical: `RA^2/L^2` at
`r ≤ 2d−2`, `A^3/L^3` at `a ≤ 2d−5`, `k6` contamination of `Phi1,Phi2` at
`a ≥ 7`, target walls at `G ≥ 28` and `T_C2 ≥ 32`.  Current `(2,5,≥3)` is
not wholly a three-row contact: `r ≥ 5` is, `r=3,4` still need `B23` (or a
certificate that retains `RA^2`).

Complementary to V46, not competing.  Do not delay V46 review on the
three-row linker, or conversely.

### 6.4 What remains after a successful linker freeze

| Object | Status after V46+linker |
|---|---|
| Strict unique-`AC` on `D(rho)` serial grade custody | Off the mathematical critical path |
| Eleven-family union | Still conditional on `(8,3,8)` |
| Coverage outside strict unique-`AC` | Unchanged hole: unique-`RC`/`R3`, `k=0`, positive-order leading loads, unlisted contacts |
| Ramified `rho=0` | Untouched.  Hensel dies.  This is Cards 1–2, not a unique-`AC` contact |
| Equality faces | Untouched |
| Six staged Rees charts | Untouched |
| Terminal / Taylor receiver | Untouched; load-bearing under either `T-a1` outcome |
| `G2-PSC`, `G2-BD`, cofinal `td` | Untouched |
| Gate T, order two, maximum twelve, JC2 | Not licensed |

Grok’s blind Card C mutation list (stage-zero pairs, factors `1` for `4`
or `2`, drop `2*sigma*ell1`, `C^2` ceiling, analytic-only `(8,3,8)`)
remains the right V46-reviewer checklist, together with Opus’s
missing-primitive guard.

---

## 7. Orthogonal ideas and history checksum

Apparent novelty is labelled only after a checksum against `APPROACHES.md`,
the 08:57Z overlay, `RECON.md`’s Jelonek line, and the `20260827T0635Z`
round.

### 7.1 Jelonek (Opus §4.1; Sol primary-source check)

Primary source: Jelonek, arXiv:2607.20597, Lemma 2.1 and Theorem 2.2.
`A(n,d)` is Zariski closed in `X(n,d)`.  Every irreducible component `Y`
is either contained in `A(n,d)` or has `Y \ A(n,d)` dense in `Y`.

**Keep:** if a bounded-degree component contains one counterexample,
counterexamples are generic *inside that component*.  Avenue 36’s recorded
“thin locus” slogan is not justified as a search-calculus premise.

**Withdraw:** “every component containing an automorphism is generically
automorphic.”  Finding an automorphism does not force the first branch of
the dichotomy.  Compatible with naive scaling (avenue 41): an orbit can
limit to a linear automorphism while generic points are nonautomorphisms.
Opus built that purity bridge and refuted it; the Sol check confirms the
refutation.  Do not raise random sampling on this theorem.  Do not raise
avenue 7 as an `A(F)` construction programme.  A component-decomposition
experiment is useful only where `K_D` is already tractable and the output
has a lift/ceiling reading.  No current `T-a1`, K00, AS109, Gate T, or JC2
claim changes.

**History:** Jelonek 2607.20597 has been in `jc72108/RECON.md` since
2026-07-22 as “generic elements of suitable components are counterexamples
for `n ≥ 3`, `d ≥ 6`.”  Avenue 7 has always been Jelonek `A(F)`.  The
density-in-component reading is a correct inference, not a new theorem.
The purity bridge is **KNOWN-FALSE** (avenue 41, dim-3 CE).  Reopen 18/41
only as lemma-retrieval, not as routes.

### 7.2 AS109 `n=6` face isolation (Fable F3) vs gauge-conductor (Sol Card 3)

Avenue 12 is historically stuck on “no integer `m` to send to infinity.”
Wilson’s GMC(2) face isolation is the named technique.  The floor-six
composition already collapsed the live corner to `deg_y Q = 6` with the
recorded divisibilities.  Fable’s move — top two `y`-degree bands, 109-adic
Kummer-carry, hunt a face congruence — is a **new executable card on an
old row**, not a new avenue.  Sol’s gauge-conductor (quotient the new polar
coefficient by every bounded polynomial Keller gauge, seek a stable class)
is the right *successor* if two bands return no constraint, and is also
not a new commutative-algebra theorem.  Degrees 2–5 and prime-target
searches stay stopped.

Do not lower avenue 19 on the critical path (Opus).  The packet’s
anti-collapse instruction is explicit.  Cap it, relabel it as
negative-information, and fund Card 4.

### 7.3 Bernstein–Sato / D-module probe (Fable raise of 16)

Avenue 16 has been Fable-unique and empty (“no concrete invariant named”)
since the master table.  Naming the `b`-function of the pencil at atypical
values is a concretization of an existing row, not a new avenue.  The
honest failure mode Fable records — identically trivial for all `J=1`
pairs — is likely.  **HOLD** as a one-afternoon `dmod.lib` desk test if a
reasoning slot is idle.  Do not raise avenue 16 before that test runs.  Do
not spend AWS algebra.

### 7.4 Primitive-group census (Fable raise of 26)

**KNOWN, specified, unexecuted.**  `APPROACHES.md` already records Grok’s
weekend GAP/Magma script, ramification supported at `≤ N` places, types
for `td=6,7,8,9`, Orevkov `(48,64)` td=9 negative control, then Sol’s
coupled CSP only if the control disciplines the idea.  Resource-orthogonal:
no AWS algebra, no landing-path slot.  Launch as a sidecar under Card 4’s
desk budget, not as a raise of the row’s promise.  Grok’s blind
“unchanged, protected from collapse” is the right rank.

### 7.5 Other checksums

| Proposal | Status |
|---|---|
| Two-fibre / generic fibre as the `T-a1` decision | **NEW this round** as an executable decision (Fable+Opus independently).  Eliminant *instrument* already existed |
| K00 global colon | Textbook; **NEW as a campaign card** this round; **already executed** as V14R1 |
| Inverse-system persistence (Sol) | Software; overlaps Grok Card A seeding.  This round’s N=7 seed is dead |
| Origin-funnel / reduced fibre = terminal | **NEW identification** this round (Fable + Grok independently) |
| Duals push only along specializations that keep the test monomial alive | **NEW firewall** (Grok); do not re-propose V43 as a K00 bound |
| Unique-`AC` disjoint from ramified `rho=0` | **NEW firewall** (Grok); Hensel dies at `rho=0` |
| Missing-primitive underprediction guard | **NEW** (Opus §6.3) |
| Ladder-hygiene process gate | Extension of the 0635Z shared gate, not new math |
| Incremental sparse dual engine across T-a1 and K00 | Software; useful if generic stalls; not a theorem |
| Section-first honest-equation calculus, dual-as-primal rewrite, generic `L43` receiver, “export more tail to kill CS0/Z00” | **KNOWN**, not re-proposed |

---

## 8. Unique correctness-adjusted contributions

Scored after discarding refuted subclaims.  The two-fibre *review* is this
model’s review lane, not the blind Grok submission; both are counted under
Grok.

### Sol

Unique and correct: the colon as the K00 decision (now a producer); the
AS109 gauge-conductor as the successor if face isolation returns zero; the
persistence-engine as a shared software object if the generic run stalls;
the Jelonek primary-source check that keeps density and kills purity;
portfolio allocation that refuses to collapse into total-rho/K00.

Miss: did not find the two-fibre decision, and left the N-ladder on the
critical path.  Card 2’s N=7 seed is now refuted as a seed.  Avenue-4
reopen trigger is still conditional on mixed reachability, correctly.

### Fable

Unique and correct: the two-fibre lemma as an executable both-outcome
decision; selected-row one-sidedness stated accurately; origin-funnel;
AS109 `n=6` face isolation; Lee–Li retarget of the V43 compiler as an
idle-capacity item; primitive-group as a resource-orthogonal filler;
explicit stop rule that selected-row nonunit proves nothing.

Miss / repaired: dehomogenization sketch; Card F1 stage 2; converter
claiming automatic `U(0) ≠ 0`; 17-row as load-bearing pin; “first honest
surviving landing geometry”; raising 16 and 26 before the cheap tests run.

### Grok (blind submission plus two-fibre review)

Unique and correct: the graded-unit repair and the safe compute plan
(theorem baseline); specialization as a *skip* of exact duals, never as an
exponent certificate; 17-row custody fail-closed against `J0`; unrestricted
replay as load-bearing on any seed; dual-transport firewall; unique-`AC`
versus ramified-fibre disjointness; attainability of leading `M2,M4,M6(0)`
before another unloaded Macaulay increment; Card C mutation controls;
adversarial audit of own Card A.

Miss: the blind submission treated the generic eliminant as parallel to
the N-sandwich rather than as the replacement decision (Fable and Opus
both saw this).  Local-order repair is now superseded by the colon
producer.  Not raising 19 is defensible; the packet still wants the
orthogonal lane funded.

### Opus, and whether it adds significant capability beyond Fable

**Yes, with a split inventory.**

On the two-fibre theorem Opus is an independent co-discovery, not a
strictly stronger theorem.  Fable’s selected-row one-sidedness is the
statement to merge.  Opus’s 17-row negative would have wrongly killed the
`T-a1` programme; that subclaim is discarded and is worse than Fable
stage 2.

Capability Fable did not supply:

1. **Eliminant-predicate software analysis**, even though the advertised
   third mathematical outcome is empty.  The inconsistency sentinel is
   the surviving deliverable.
2. **Missing-primitive underprediction guard** on the naturality ceiling
   (landing-path, cheap, should be a V46-reviewer attack).
3. **Jelonek search-calculus reframing** plus a recorded self-refutation
   of the `G_m`-purity bridge.  That self-refutation is the useful part
   and is now confirmed by the primary-source check.
4. **Fourteen-lane `rho=0` census** of registered `T-a1` cases, and the
   V43 denominator factorization showing `65519` and `65521` are lucky
   for the certificate denominator.  Custody, not a theorem.
5. **Ladder-hygiene as a portfolio process** (extension of the 0635Z
   gate).  Decision-relevant for T-a1 and K00; AS109 stays a capped
   negative-information ladder.

Refuted or overstrong, discarded: 17-row negative as chart-negative;
`E ⊆ (t)` as a live success under the special control; geometric proof
of Theorem A over `Q`; lowering 19 on the critical path; raising 7/36
into a search; Monte-Carlo unit as near-proof; “the exponent is not part
of the question” as a reason to stop the 104-tree before a `v(0)=0`
Bézout exists.

Net: keep Opus as a second proof of two-fibre, as the author of two
software guards that Fable did not write, and as the Jelonek/41 hygiene.
Do not keep Opus as the owner of selected-row semantics or of the
eliminant success token.

---

## 9. Claim matrix

| Claim | Fable | Opus | Sol | Grok blind | This review | Disposition |
|---|---|---|---|---|---|---|
| Two-fibre equivalences | stated, bad dehom proof | stated as Thm A, bad geometric proof | not found | not the replacement decision | CONFIRMED after graded-unit | **MERGE as theorem baseline** |
| Finite `rho=c` unit ⇒ generic unit | stage 2 yes | Monte-Carlo weaker | — | fibres heuristic | **REFUTED** | heuristic probes only |
| 17-row generic nonunit ⇒ no certificate | correctly one-sided | Card A negative **false** | — | 17-row is a ceiling only if in `J0` | O4 | **keep Fable** |
| `E ≠ 0` but `E ⊆ (t)` is a live success given `I+(t)=(1)` | not claimed | claimed | — | converter `v(0)=0` is a different object | **REFUTED** as live math | inconsistency sentinel |
| Generic Bézout ⇒ `U(0)=1` | claimed | claimed | — | review refuted | **REFUTED**; converter §3 | replay required |
| N-ladder decides existence | demoted | redesign/stop | still critical | sandwich, not a decision | cannot | **STOP as decision** |
| K00 colon decides local membership | F2 | Card B | Card 1 | wanted local-order repair | producer V14R1 | **REVIEW, do not relaunch** |
| D8 ⇒ local membership | no | no | no | no | no; D8 PASS | **PASS at D8 scope** |
| D9 as discovery | re-scope after colon | hold | stop | redesign | stop | **STOP** |
| Serial G22+ off unique-`AC` critical path | after review | after review | after review | Card C | after V46 impl. review + linker | **HOLD exporters** |
| Linker ⇒ Gate T / coverage / `rho=0` | firewall mostly | firewall | firewall | firewall | firewall | **REFUSED** |
| Jelonek ⇒ random CE search | — | raise 36 | check: no | — | density only | **NO LAUNCH** |
| `G_m`-purity of components | — | built then refuted | check confirms refutation | — | **KNOWN-FALSE** | lemma-only 41 |
| AS109 n=6 face isolation | F3 | lower 19 | gauge-conductor | continue bounded | fund orthogonal | **CARD 4** |
| M2/M4/M6(0) all forced | — | — | — | attainability test | provisional; V16R1 | **do not promote** |

---

## 10. Unique-contribution matrix

| Deliverable | Unique owner after correction | Merge into |
|---|---|---|
| Two-fibre decision procedure | Fable (statement) + Opus (independent co-discovery) + Grok review (proof repair, safe plan) | Card 1 |
| Selected-row one-sidedness | Fable | Card 1 |
| Inconsistency sentinel on `t`-divisible eliminant | Opus, corrected | Card 1 |
| Constructive `q a1^(D+Ms)` converter | charged here; Grok review named the gap | Card 1 |
| Specialization skip of exact `N` | Grok | Card 1 fallback |
| Dual unrestricted-replay hygiene | Grok | Card 1 |
| K00 colon as the local decision | Sol / Fable / Opus (three-way) | Card 2 review of V14R1 |
| D8 independent PASS | Opus review | Card 2 cross-check |
| Mixed coker + source-restriction firewall | packet + Grok Card B, updated | Card 2 |
| Polynomial syzygy forcing one-sided | this review | Card 2 |
| Unique-`AC` formula identity | producer + Fable formula review + Sol V2 | Card 3 |
| Missing-primitive ceiling guard | Opus | Card 3 reviewer attack |
| Three-row radical lemma + twelve-contact list | Sol post-snapshot | Card 3 complementary |
| UAC disjoint from `rho=0` | Grok | Card 3 firewall |
| AS109 n=6 face isolation | Fable | Card 4 |
| AS109 gauge-conductor successor | Sol | Card 4 stop/successor |
| Jelonek density, purity withdrawn | Opus + Sol check | no card |
| Primitive-group sidecar | historically Grok/Sol, Fable revived | Card 4 desk |
| `b`-function naming | Fable | HOLD, not a card |
| Origin-funnel / terminal reduced fibre | Fable + Grok | Card 1 both-outcome note |

---

## 11. Four executable merged cards

### Card 1 — `TA1-GENERIC-DECISION`

- **Decide.**  `J|_{a1=1} = (1)` in `Q(rho)[X \ {a1}]` for the *full* frozen
  59-row general-`rho` system (eight rho-only rows and `Tg19_2` included).
  Combined with V42 this is `K+(rho)=(1)` for the frozen grade-through-19
  ordered `T-a1` chart.
- **Dependencies.**  Two-fibre review SHA `bc539ba9…`; promoted criterion
  `98003865…`; V42 `5d4c42ff…` / `a4f6b931…`; Fable5 spectator transport of
  `ez9`.  Independent of V43 as a result, of the 17-row GB, and of `a1^104`.
- **Already live.**  Full 59-row `Q(rho)` job launching; selected-row jobs
  continue one-sided; constructive special-fibre tree replaying.
- **Both outcomes.**
  - **Unit** (GB meets `Q(rho)^*`): do not promote.  Lift, project to
    weights `0 mod 5`, rehomogenize to `v a1^D ∈ J`.  If `v(0) ≠ 0`,
    even-average, normalize, unrestricted weight-`5D` replay.  If `v(0)=0`,
    apply §3 once an effective `M` with polynomial lift exists.  Frozen
    chart special fibre empty, monotone in later rows of the same
    presentation.  Stop N-duals and high-mem extraction.
  - **Complete nonunit:** frozen `K+(rho) ≠ (1)`.  Prefix-relative.
    Unhold grade-20+ as navigation.  Parametrize the `a1=1` slice as a
    new producer centered at the origin only as support geometry.  Not
    all-depth, not Gate T, not a landing-side counterexample, not JC2.
    Stop N-duals.
  - **NO VERDICT:** fall back to selected-row (still one-sided), the
    104-tree, then specialization skips, then — only then — an unseeded
    dual at the first open `N`.
- **Controls.**  Design toy `J=(f−t x)` must return nonempty; low-grade
  subset nonunit; `rho=0` specialization of the stage-3 input consistent
  with V42; one-coefficient row mutation flips the unit; finite `rho=c`
  fibres are not theorems.
- **Resources.**  One large-RAM AWS instance for the 59-row run; do not
  co-schedule an unseeded N=7 dual on the same quota.  Selected-row and
  104-tree keep their current instances.
- **Stop.**  Complete GB in either direction, or a selected-row unit with
  `U(0) ≠ 0` and unrestricted replay.  Crossing a registered RAM/time cap
  without a complete GB is `NO VERDICT`.  Do not retry the dead N=7 seed.
  Do not treat `a1^104` as a claim before replay.
- **Inconsistency sentinel.**  If a dehom special control on the *same*
  ideal passed and the eliminant candidate is nonzero and `t`-divisible:
  `ELIMINANT_INCONSISTENT`, content-reduce, do not close the chart.

### Card 2 — `K00-MIXED-COKERNEL`

- **Decide.**  (Review) V14R1 local membership.  (Producer, already
  queued) the class of `b1 − phi1(q0)` in `(A/I)/im(Syz(phi0) → A/I)`
  before honest source restriction, and whether `M2(0), M4(0), M6(0)` are
  polynomially forced (V16R1).
- **Dependencies.**  Promoted V8/V9 `6c4ebd6d…`; D8 PASS `7f0c3aea…`;
  V14R1 producer `039590c8…` (not yet a theorem); load-normal stencil as
  input only; owner source with connection jets (naive `p=−2*rho^2`
  family remains illegal as a replacement emitter).
- **Do not launch.**  A new colon, D9+, or a local-order transform repair.
- **Both outcomes, V14R1 review.**
  - **PASS:** local membership is a theorem; `(d)`-adic ladder is
    infinite; remove “first filtered obstruction degree”; D8 becomes a
    jet cross-check; mixed coker remains the object.  No receiver, no
    closure, no JC2.
  - **FAIL:** colon question reopens as software/custody, not as D9.
    Mixed coker still runs.
- **Both outcomes, mixed coker / V16R1.**
  - Class zero *and* leading `M_i(0)` forced, before source restriction:
    first-order compiled lift exists; next is honest source restriction
    and `H_K00`.  Not a germ, not a counterexample.
  - Class nonzero polynomially: necessary obstruction at this
    presentation; still not local-complete unless the Syz generating set
    is.  Record exceptional power / localizer / rho factor.
  - `M6(0)` not forced after a complete Syz: Grok Card B attainability
    remains a search; do not read this as unloaded costume without the
    source test.
- **Localization rule.**  Polynomial forcing ⇒ local forcing.  Polynomial
  non-forcing ⇏ local non-forcing.  Require the 87-generator completeness
  claim to be replayed, not assumed.
- **Resources.**  Review is desk-scale.  V16R1 already AWS.  No new heavy
  matrix.
- **Stop.**  V14R1 review lands, or two hours of mixed-coker disagreement
  with V8.  Do not fund D9 as discovery.  Do not infer receiver/closure/JC2.

### Card 3 — `UAC-LINKER-FREEZE`

- **Decide.**  Whether the V46 schema/verifier implements the reviewed
  formula theorem, whether the three-row linker may replace twelve
  low-band endpoints, and whether V45’s `(2,5,≥3)` contact exclusion
  survives review.
- **Dependencies.**  Formula theorem `1cfa10b4…` + Fable5 review + Sol V2
  `a1f18d28…`; V46 producer `c2efa158…`; three-row interface
  `2354703a…`; V45 producer (not SHA-listed; contact-only); eleven D1
  endpoints; Kummer finite-flat kept, stage-zero composition dead.
- **Cheapest discriminator.**  Different-model implementation reviews,
  desk-scale, no `Tg22` emitter.  Mandatory attacks: Sol V2 map, Opus
  missing-primitive guard, Grok Card C mutations, three-row purity walls,
  `(8,3,8)` filename trap, V45 factorization/index/factor/localizer/root
  orientations.
- **Both outcomes.**
  - V46 review PASS and per-endpoint linker checks: serial
    `ACT-TOT-G22/G24/…` leave the mathematical unique-`AC` critical path.
    Remaining unique-`AC` work is endpoint manifests plus chamber review.
    Still no ramified, equality-face, coverage, or Gate-T claim.
  - V46 review FAIL: exporters stay as they were; V44R1/V45 continue as
    chamber work; do not compose.
  - Three-row PASS on a listed contact: that contact may drop the
    moving-root endpoint linker.  `(2,5,r=3,4)` still need `B23`.
  - V45 review PASS: one contact excluded on `D(rho*k)` at the stated
    gates; not a cover.
- **Resources.**  Review slots, not AWS algebra.  G20/V44R1 may finish as
  background.
- **Stop.**  One implementation-review reversal, or two chambers plus
  mutations.  Do not emit `Tg22` as a proof obligation.  Do not apply
  shifted-root maps at `rho=0`.

### Card 4 — `AS109-N6-FACE` (orthogonal, capped)

- **Decide.**  Whether the top two `y`-degree bands of the floor-six
  corner produce a 109-adic face congruence incompatible with the
  recorded common-core divisibilities.
- **Dependencies.**  Promoted floor-six composition `3d6d09fd…` /
  `bc136621…`; reviewed residue-ball Hensel noninjectivity.  No
  provisional landing input.
- **Cheapest discriminator.**  Desk-scale band extraction.  No AWS until
  a candidate congruence needs an exhaustive residue check.
- **Both outcomes.**
  - New constraint: floor rises or the corner shrinks; may terminate the
    corner.  Do not reopen degrees 2–5.
  - No constraint after two bands: stop the face hunt; the surviving
    leading-form data is a seed for Sol’s gauge-conductor (bounded
    polynomial Keller gauges at W2/W3), not for unstructured support
    search.  Null result banked.
- **Sidecar, same card, no extra reasoning slot.**  Primitive-group
  GAP/Magma census as historically specified, Orevkov `(48,64)` control
  mandatory.  Does not raise avenue 26.
- **Not in this card.**  `b`-function probe (HOLD, idle desk only).
  Jelonek component enumeration (no launch).  Random CE search (no).
- **Resources.**  Zero AWS algebra.
- **Stop.**  Two bands with no constraint, or one new constraint fully
  recorded.  Do not descend to lower bands without a new mechanism.
  Gauge-conductor: stop if two Witt levels show no stable class and no
  bounded gauge.

---

## 12. Launch / continue / hold / merge / stop

| Lane | Decision | Note |
|---|---|---|
| Full 59-row generic `Q(rho)` GB | **CONTINUE** (already launching) | Card 1, two-sided |
| Selected-row total-rho eliminant | **CONTINUE** | one-sided positive only |
| Constructive special-fibre tree / `a1^104` | **CONTINUE** | converter `M`; no claim until replay |
| V43 N=6 dual as discovery | **STOP** | answered; review as lifecycle |
| N=7 fixed seed | **STOP** | seed refuted; not N=7 nonmembership |
| Unseeded N=7 / weight-35 dual | **HOLD** | only under Card 1 `NO VERDICT` |
| Finite-field `J0` specialization skip | **HOLD** | Card 1 fallback, not parallel by default |
| 192-GiB / 1-TiB multiplier extraction | **STOP** as critical path | optional cheap `lift` only |
| Per-`N` ladder as existence proof | **STOP** | constitutionally one-sided |
| V14R1 colon | **CONTINUE review; do not relaunch** | Card 2 |
| D8 | **MERGE as PASS** | jet cross-check of V14R1 |
| D9+ unloaded Macaulay | **STOP** | two non-informative degrees + colon producer |
| K00 V11/V13 local-order | **STOP** | superseded by colon |
| Mixed `Lambda^19` / first-order coker / V16R1 | **CONTINUE** | Card 2; M-forcing provisional |
| Closure-first `H_K00` | **CONTINUE** | load-bearing under both Card 1 outcomes |
| V46 schema/verifier review | **CONTINUE** | Card 3; exporters wait on it |
| Serial `ACT-TOT-G22/G24/…` | **HOLD / leave critical path** | after V46 review + linker |
| Three-row interface review | **CONTINUE** | complementary to V46 |
| V45 `(2,5,≥3)` review | **CONTINUE** | one contact; not a template |
| `(2,3,≥2)`, `(2,4,≥3)` reviews | **CONTINUE** | background |
| Stage-zero Kummer composition | **STOP** | REFUTED |
| Ramified `rho=0` as a unique-`AC` contact | **STOP** | Cards 1–2 |
| Equality faces / unique-`RC`/`R3` / `k=0` | **CONTINUE narrowly** | not licensed by the linker |
| TD6 H19R2 | **CONTINUE** | to registered caps |
| Serial TD6 `q` / affine-Faber `H` | **STOP** | prior-round stop |
| AS109 n=6 face isolation | **LAUNCH desk** | Card 4 |
| AS109 n=2..5 / prime-target / rigid leaf | **STOP** | history-closed |
| Primitive-group census | **LAUNCH sidecar** | Card 4 desk; Orevkov control |
| `b`-function / avenue 16 | **HOLD** | idle afternoon only |
| Jelonek component search / avenue 36 raise | **STOP as a launch** | density reading only |
| Prime-ray full `3P-E31` | **HOLD** | firewall |
| Raw receiver exports / generic `L43` / dual-as-primal | **STOP** | unchanged |
| Web sweep | **CONTINUE** | clock `2026-08-28T00:00Z` |
| `jc2-lean` | **untouched** | not entered, read, built, or inspected |

Nonblocking parallelism: Card 1 generic run, Card 2 V14R1 review + V16R1,
Card 3 implementation reviews, and Card 4 desk work have no data
dependency.  Do not wait for Claude/Fable/Opus account recovery to start
any of them.  Heavy algebra stays on AWS; this report launches nothing.

---

## 13. Non-claims

Nothing here proves or disproves JC2, Gate T, order two, maximum twelve,
`G2-PSC`, `G2-BD`, a cofinal degree/type ceiling, source/landing coverage
outside strict unique-`AC`, the ramified `rho=0` deck/square fibre, the
terminal or Taylor receiver, or an AS109 polynomial lift.  V43, V14R1,
V16R1, V45, V46, and the three-row linker are not promoted by this file.
The two-fibre lemma decides the frozen grade-through-19 ordered `T-a1`
chart and nothing else.  Local K00 membership, even if V14R1 survives
review, is the unloaded `C6=1` coefficient slice.

---

## 14. File / tool / edit disclosure

**Files read in full, hashes verified first:** the twelve prompt-listed
inputs plus the sealed packet (thirteen SHA-256 values in §0).

**File read in part because the prompt requires integrating V45 producer
evidence:**
`cases/max12_812_order2_gate_t_drho_a2d3_composition_v45_20260827/RESULT.md`
(not on the SHA list; used only as producer-tier scope).  Directory
listings under `xmodel/` and `cases/` to locate that file and to confirm
V46/three-row/V45 review logs exist and are not consumed as verdicts.

**Files grepped, not read in full:** `APPROACHES.md` (rows 7, 12, 16, 26,
36, 41, historical shortlist, primitive-group experiment); `notes.md` and
`PROGRESS.md` hit-context for Jelonek; `jc72108/RECON.md` Jelonek line for
the history checksum; prior ideation filenames for novelty checks.  No
canonical ledger was used as a promotion authority.

**Tools:** `shasum -a 256`, `ls`, `wc`, `Read`, `Grep`.  No Python CAS, no
Singular, no FLINT, no Groebner engine, no desk replay script.

**Edits:** this file,
`xmodel/ideation-20260827T0935Z-grok-crossreview.md`,
is the only campaign artifact written or modified.

**Boundaries:** no network, no browsing, no AWS access or job launch, no
heavy local CAS, no edits to any other campaign artifact, and no contact
of any kind with `jc2-lean` (not read, entered, built, status-inspected,
edited, staged, or cleaned).
