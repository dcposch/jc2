# D=108 δ=3 — does the D2 centre vanish, and is the weight floor the right floor?

Opus 5 · 2026-09-05 · lane `d108-center-opus5-20260905`
(re-routed from Astra after two backend capacity failures)

**Disposition: `D108-DELTA3-DEAD` is REFUTED at stage 0.**

**The centre half of the repair SUCCEEDS. Moh's own p.201 (8) bookkeeping proves
the D2 generic point of this row admits exactly ONE centre term, `a_0` (an
integer-`t` term, constant in `y`); it is absorbed by the constant translation
`y ↦ y + a_0`; that translation is NOT free in the frozen chart (it is spent
pinning the minor jet to `jet0 = 0`), so I paid the gauge and re-ran with `jet0`
free — the incidence ideal is still a unit, with an exact-Q cofactor certificate
that specialises at `jet0 = 0` to the identity `17(ddddd)` published.**

**But auditing the floor's OFFSET forced an audit of its SLOPE, and the slope is
wrong. The frozen engine's D2 generic point is `t = s^4, z = π s^6` (weight
`4r+6q`, face weight 42). Moh Def 5.1(3) on this row gives `δ_2 = 1/4`, hence
`z`-radius `1+δ_2 = 5/4`, hence `t = s^4, z = π s^5`, weight `4r+5q`, face weight
`35 = ord_s K3(σ_{D2})`. On the chart the source actually licenses, the twelve
nonzero incidence rows become twelve `Q*` pivots on twelve distinct `h3`
coordinates and the residual is EMPTY: the block imposes nothing beyond `c ≠ 0`.
I exhibit an explicit rational point that satisfies every incidence row, obeys
`c ≠ 0`, and obeys `D_2`-minimality. The sibling `(99,66)` engine passes the
same radius test exactly, so this is a D=108-specific transplant error, not a
family-wide convention.**

No ledger was edited; no `jc2-lean`; no `ideation-*`. No new exit-price
assertion is made, so no FALLACY-v2 `charge_basis=` line applies.

## 0. Custody

The manifest was built mechanically from `xmodel/d108-center-opus5-20260905.run.v2`
by joining its numbered `_basename` and `_sha256` fields with `awk` and prefixing
`lane_inputs_dir`; no digest was retyped. `sha256sum -c` returned `OK` for all six
frozen files in `/tmp/jc2-lane.nD5MyA/inputs`. Retained as
`box/d108-center-20260905/frozen-inputs.opus5.sha256` and `.opus5.check.log`
(named to avoid overwriting the partial Astra manifest already in that
directory). No content mismatch.

The partial Astra artifacts in `box/d108-center-20260905/` were read but NOT
consumed as results: every number below comes from
`box/d108-center-20260905/work/center_incidence.py`, which I wrote independently
and which imports only `qstar_reduce` from the frozen charged engine. Where my
results and Astra's overlap they agree; where they differ in framing I say so
(§5). Repo files outside the frozen set were read read-only at basis `c78e4f98`.
Only `box/d108-center-20260905/` and this report were written.

## 1. The centre lemma (task 1)

Moh Def 5.1(4), printed p.179 (`box/g108gate-20260903/moh_p179_196.txt:89`):
`σ_i = Σ w_j t^j + π t^{δ_i}` is the unique general point of `D_i`. Def 5.1(3)
at the same place calls `δ_i` the **logarithmic radius** of `D_i`, and Def 5.1(1)
fixes the root counts: `T_j` has `(μ_j/d_{i+1})·V_{i+1}` roots in `D_i`.

The engine's coordinates are `x = t^{-1}`, `w = t y = y/x`, `z = w − 1`, and
`K3 = t^9 h3(t^{-1}, w/t)` (`band_engine.py:222` conventions block). So for two
roots in `D_i`, `ord_t(z_1 − z_2) = 1 + ord_t(y_1 − y_2) = 1 + δ_i`, i.e. the
`z`-radius of `D_i` is `1 + δ_i`.

**Which exponents can occur in the centre.** Moh p.201 (8) — quoted in the
frozen skeleton header — computes the radii through the increment denominators:
`L_j = lcm{den δ_s, …, den δ_{j+1}}` and `A_j = den(L_j δ_j)`. `L_j` is exactly
the ramification already present when the tower reaches level `j`, so the centre
of `σ_j`, being the common initial part of the `D_j` roots at exponents strictly
below `δ_j`, has its exponents in `(1/L_j)Z`. For this row
(`Skel(108,72,[81,106],{2:7,3:7})`):

```text
delta = {1: 3/8, 2: 1/4, 3: -1}      L = {1: 4, 2: 1}      A = {1: 2, 2: 4}
```

`L_2 = 1`. **The D2 centre exponents are integers.** In `z` the admissible range
is `0 < j < 1 + δ_2 = 5/4`, and the only integer there is `j = 1`; `j = 0` is
already excluded because the `D_3` centre `w = 1` is fixed by the degree-9 face
`z^7(1+z)^2 = w^2(w−1)^7`. Hence

> **Lemma C (D2 centre).** The D2 generic point of the D=108 row is
> `z = a_1 t + π t^{1+δ_2}` with a single free centre coordinate `a_1`. In `y`
> this is `σ_2 = x + a_1 + π t^{δ_2}`: `a_1` is a CONSTANT in `y`.

The same computation at level 1 gives `L_1 = 4`, so `σ_1 = x + a_1 + c t^{δ_2} +
Π t^{δ_1}` — one further centre coordinate at exactly the `D_2` radius, which is
the term the engine's `D1` line already carries. The gate's structural guess
("the only Def 5.1(4) center term below order 6 in `s` is an integer-`t` term
that is a constant in `y`") is therefore correct as stated, and now proved from
Moh's own (8) rather than asserted.

## 2. The gauge, and the gauge ledger (task 2a)

`z ↦ z + a_1 t` ⟺ `w ↦ w + a_1 t` ⟺ `y ↦ y + a_1`. The group element is

```text
  φ(x,y) = (x, y + a_1),   det = 1,  x fixed,  J unchanged,
  y-degrees unchanged, every leading form unchanged (a constant translation
  does not change a top-degree form), ord_t(root) >= -1 preserved.
```

The full gauge group available here is `(x,y) ↦ (x, λy + μx + s_0)` — degree-`≤1`
translations only, because `d = −δ_s = 1` (`17(ssss)` radii `(3/8,1/4,−1)`;
a higher-degree translation would push a root past `ord_t = −1`). It is
3-dimensional, and FALLACY-v2 requires every normalization to name its element:

| normalization in the frozen stage-0 chart | element | status |
|---|---|---|
| `major_coordinate z = w−1` (D3 cluster leading coefficient `= 1`) | scaling `y ↦ λy` | SPENT |
| face `z^7(1+z)^2 = w^2(w−1)^7` (the other cluster at `w = 0`) | shear `y ↦ y + μx` | SPENT |
| `minor_series w = jet1 t^2 + jet2 t^3 + π t^4` — no `t^1` term, i.e. the minor branch has no `y`-constant | constant `y ↦ y + s_0` | **SPENT** |
| `p = π^2 − c` (no linear term in `π`) | reparametrisation of the child generic point, NOT in the above group | spent, and hostile-tested by the gate (`b` free ⇒ still dies on `b²+4c ≠ 0`) |
| `local_power ≤ 8` truncation | none — drops ROWS | safe |

So the answer to the gate's ledger question is **no**: the constant translation
is not free at D=108 δ=3. It is exactly as consumed as `"b0":"fixed_zero"` is at
`(99,66)` δ=5/2 — the consumption is just recorded implicitly, in the shape of
the minor series, rather than in a `centre` dict.

The minor generic point is itself a Def 5.1(4) point at its own place: for
`δ' = 3` its admissible centre exponents are the integers `0,1,2`, of which the
chart carries `jet1` (`j=1`) and `jet2` (`j=2`) free and pins `j = 0`. So the
whole gauge question is one coordinate wide on each side, and spending the
translation on the major centre releases exactly one coordinate on the minor:

> **Repair R.** Use `y ↦ y + a_1` to set the D2 centre to zero (so the weight
> floor is stated in the coordinate it is proved in), and free the minor
> constant: `w = jet0·t + jet1·t^2 + jet2·t^3 + π·t^4`.

This is a strictly weaker hypothesis than the frozen chart's, and it is a
FALLACY-v2-clean move: it frees an UNKNOWN rather than dropping one.

## 3. Repair R measured, at the engine's own radius (task 3)

`box/d108-center-20260905/work/center_incidence.py` re-emits `h3_template` and
`minor_incidence_rows` with two knobs — `ord_s(z)` and `jet0` — and reduces with
the frozen `qstar_reduce`. Every case was then re-run independently in Singular
over `Q` (`work/singular-crosscheck.out`), because a `sympy` `[1]` alone is a
single-engine result.

```text
case                              weight   cutoff jet0  coords piv resid  sympy  Singular dim / reduce(1)
A engine, frozen                  4r+6q      43   pinned    7    7    5    UNIT      -1 / 0
B engine, weight-42 control       4r+6q      42   pinned    9    9    2    UNIT      -1 / 0
C engine, floor removed (17(zzzzzz))4r+6q   none   pinned   45   12    0    NONUNIT    1 / 1
D engine + Repair R               4r+6q      43   FREE      7    7    5    UNIT      -1 / 0
E engine + Repair R, 42 control   4r+6q      42   FREE      9    9    2    UNIT      -1 / 0
F Moh Def 5.1(3) radius           4r+5q      35   pinned   14   12    0    NONUNIT    1 / 1
G Moh radius, face fixed          4r+5q      36   pinned   13   12    0    NONUNIT    1 / 1
H Moh radius + Repair R           4r+5q      35   FREE     14   12    0    NONUNIT    1 / 1
```

A, B and C reproduce the frozen gate baseline, the frozen `7 → 9` control, and
the `17(zzzzzz)` §2.2 measurement exactly (same coordinate counts, same pivot
counts, same residual counts).

**D is the result the repair was commissioned to produce, and it is positive.**
With `jet0` free the residual rows acquire `jet0` but the ideal is still the unit
ideal, with the exact-Q cofactor identity extracted by `lift` in Singular
(`work/D-certificate.out`, `CHECK = 1`):

```text
1 = Zc*(-5*jet0^3*jet1 + 9/2*jet0*jet1^2 + 7*jet0^2*jet2 - 9/2*jet1*jet2) * r71
    - Zc * r80
    - Zc*(1/2*jet1^2 + 1/2*jet0*jet2) * r81
    - (Zc*c - 1)
```

At `jet0 = 0` this is term-for-term the identity published in the charged gate
(`g108-delta3-kill-gate-gpt55-20260903.md`, Stage-0 Replay). So **mode (ii) as
the h-support gate raised it — the missing Def 5.1(4) centre — does NOT by
itself break `17(ddddd)`.** Had the floor's slope been right, this report would
have read `RE-CERTIFIED (with the centre lemma)`.

## 4. The floor's slope is wrong (the finding that decides the verdict)

Auditing the offset of `4r+6q ≥ 43` requires knowing what `6` is. It is
`ord_s(z)` at the D2 generic point. Five independent checks, all in
`work/radius-ledger.json`:

**(i) Def 5.1(3) directly.** `δ_2 = 1/4` ⇒ `z`-radius `= 1 + δ_2 = 5/4`. With
`den(5/4) = 4`, the uniformiser is `s = t^{1/4}` and `z = π s^5`. The engine uses
`s^6`, i.e. `δ_2 = 1/2`.

**(ii) The order of `K3` at that point.** By Def 5.1(1), `V_3 = 7` of the nine
roots of `K3` lie in `D_2`; `π` is transcendental so each contributes exactly
`δ_2`, and the two roots at `w = 0` contribute `δ_3 = −1` each:

```text
  ord_t K3(sigma_D2) = 9 + 7*delta_2 + 2*delta_3 = 9 + 7/4 - 2 = 35/4
  ord_s K3          = 4 * 35/4 = 35      (the engine's face weight is 42)
```

35 is exactly the face weight `5·7` under `4r+5q`; 42 is exactly `6·7`. This is
an EXACT value, not a floor, so the two conventions cannot both hold.

**(iii) The sibling engine passes the identical test.** `(99,66)` is the row
`Skel(99,66,[77,97],{2:8,3:8})`, `delta = (4/9, 1/3, −1)`. Its engine uses
`t = s^3, w = 1 + π s^4` (weight `3r+4q`) at D2 and `t = e^9, w = 1 + e^{12} +
Π e^{13}` at D1 (`g9966-outer-bridge-grok46-20260903.md:137,145`). Source values:
`1 + δ_2 = 4/3 → s^4` ✓, `1 + δ_1 = 13/9 → e^{13}` with centre at `e^{12} = t^{4/3}` ✓,
face weight `4·8 = 32 = ord_s K3` ✓. Three for three. So the convention is right
in the family and wrong on this row.

**(iv) The transplant is visible.** The D=108 D1 line is
`s = e^2, t = e^8, z = e^{12}(1 + Π e)` — the sibling's `e^{12}`/`e^{13}` kept
verbatim while only `t = e^9` was changed to `e^8`. Def 5.1(3) for D=108 gives
`e^{10}` (centre, `= s^5`) and `e^{11}` (free part). Both D=108 exponents are one
power of `s = t^{1/4}` too deep, at both levels, by the same amount.

**(v) Corroboration from `V_2`.** The `D_1` packet inside `D_2` contributes a
root of multiplicity `V_2 = 7` to the `K2` D2-face (degree 28 in `π`). Under
`4r+5q` the face sites are `q ≡ 0 (mod 4)` — a degree-7 polynomial in `π^4`, so
multiplicity 7 is available. Under `4r+6q` the sites are `q ≡ 0 (mod 2)` and the
engine's own recorded face `pi^12*(pi^2-1)^8` carries multiplicity **8 ≠ V_2**.

## 5. The refutation

At the source radius the lower box splits `31 below / 1 equal / 13 above` (the
engine's `36 / 2 / 7`), and the chart gains seven coordinates:

```text
  engine (4,6) cut 43:  Hc_1_7 Hc_1_8 Hc_2_6 Hc_2_7 Hc_3_6 Hc_4_5 Hc_5_4
  Moh    (4,5) cut 35:  + Hc_3_5 Hc_4_4 Hc_5_3 Hc_6_3 Hc_7_2 Hc_8_1 Hc_9_0
```

and all twelve nonzero incidence rows become `Q*` pivots on twelve distinct
coordinates (`work/F_moh_cut35_jet0pinned.json`). The residual is empty; the
block is vacuous. Explicit rational point (`work/witness-minimal.json`), all
thirteen rows verified to vanish by direct substitution:

```text
  jet1 = jet2 = c = 1,  Hc_5_4 = Hc_9_0 = 0
  Hc_2_7 = 2   Hc_3_6 = -2  Hc_4_4 = -1  Hc_4_5 = -1
  Hc_5_3 = 2   Hc_6_3 = -1  Hc_7_2 = 4   Hc_8_1 = -2   (rest 0)
```

It clears every sourced side condition, not just the rows:

* `c = 1 ≠ 0` — the only localization the gate's FALLACY-v2 check declares.
* `ord_s K3(σ_{D2}) = 35` exactly — it sits ON the proved floor, not above it.
* D2 face `= π^3(π^4 + 2)`, which has five distinct roots, so `D_2` really is a
  minimal disc in the sense of Prop 5.2 (a pure `π^7` face would not be). The
  minimality condition is `Hc_5_3 ≠ 0`, and the pivot gives
  `Hc_5_3 = 2·jet1·jet2`, so it removes only `jet1·jet2 = 0` — this witness is
  outside that locus.
* The face is a polynomial in `π^4` times `π^3`, i.e. it has the Galois shape
  forced by `A_2 = 4`.

**This is a rational point of the NECESSARY common-`h3` incidence block only. It
is not a Jacobian pair and no such claim is made.** What it refutes is the unit,
and the unit is the whole certificate: `17(ddddd)` states "the kill is
independent of the outer accounting (the unit ideal is generated by the
incidence rows alone)". With the incidence block vacuous there is nothing left
in that certificate.

Note the difference from `17(zzzzzz)` §2.2. That probe removed the floor
entirely (45 free coordinates) and was correctly described as "not the repair".
Here the floor is KEPT — it is simply the floor the source proves — and the kill
still fails. Astra's partial `physical36`/`physical35` runs reached the same
`(4,5)` weight; I did not consume them, and I differ from its `minimal_t8z`
witness, which has `jet1 = jet2 = 0` and therefore `Hc_5_3 = 0`, violating
`D_2`-minimality. The witness above is chosen to survive that objection.

## 6. `(99,66)` δ=2 and δ=5/2 (task 4)

Centre theory only; a separate lane replays the elimination.

**Radius: SOUND.** Shown in §4(iii). `3r+4q` is exactly `1 + δ_2 = 4/3` on
`Skel(99,66,[77,97],{2:8,3:8})`, and `cutoff 33` is `face weight 32` made
strict, matching `ord_s K3 = 32`. The D1 line matches `δ_1 = 4/9`. The D2 blocks
of `17(pppp)`/`17(tttt)` do NOT inherit the D=108 defect.

**Centre: the same one coordinate.** `L_2 = lcm{den δ_3} = 1` here too, and
`1 + δ_2 = 4/3` contains the single integer `1`. So the D2 generic point is
`z = a_1 t + π t^{4/3}` and `a_1` is again a constant in `y`, absorbed by
`y ↦ y + a_1`.

**Gauge ledger: worse than D=108 on one branch.** Both minor series are pinned
constant-free — δ=2 uses `w = u t^2 + ζ t^3` and δ=5/2 uses `t = τ^2,
w = u τ^4 + v τ^6 + π τ^7`, i.e. `y = u t + v t^2 + π t^{5/2}` — so each spends
the constant translation once. On δ=5/2 the branch map then spends it a **second**
time: `h3_branch_map("delta52")` pins all 21 coordinates including
`Hc_11_0 = 0`, declared `"b0":"fixed_zero"`, and `Hc_11_0` is exactly the
`x^0 y^0` coefficient of `h3` (`r = 11, degree = 0 ⇒ a = 11−r−b = 0`). Two
independent normalizations, one group parameter. On δ=2, `Hc_11_0` is left free
(`free = [Hc_7_4, Hc_10_1, Hc_11_0, rho, u]`), so only the single spend applies.

**Verdict for `(99,66)`: CONDITIONAL on Lemma C plus the gauge payment**, i.e.
the D2 blocks are sound once the analogue of Repair R is applied. The minimal
enlargement is small and named:

```text
delta=2   : free the minor constant  -> y = jet0 + u t + zeta t^2 ; replay stages 0-4
delta=5/2 : free the minor constant  -> y = jet0 + u t + v t^2 + pi t^{5/2}
            AND release exactly one of {jet0 pin, Hc_11_0 = 0} -- not both
            ; replay stages 0-8
```

`17(zzzzzz)` §2.3 typed these UNDECIDABLE-FROM-RECORD "leaning REGRESSED". This
lane moves the centre half to a decided, cheap obligation and clears the radius
half outright; the two kill rows (`stage4_J_d159_k35 = 6264`,
`stage8_G_local16_coord0 = 64`) remain untested, as scoped.

## 7. What this does to the ledger claims

Stated as consequences only; no ledger edit is made here.

| claim | status after this lane |
|---|---|
| `17(ddddd)` `CONFIRMED[D108-DELTA3-DEAD]` | **REFUTED at stage 0.** The unit is an artifact of a D2 radius one `s`-power too deep. Not "conditional": there is an exact rational point of the necessary block. |
| `17(bbbbbb)` D=108 CLOSED at skeleton level | the split arm loses its kill entirely (this lane); the no-split arm was already CONDITIONAL on `deg beta = b_min` (`17(zzzzzz)` §2.1). D=108 is OPEN on both arms. |
| `17(zzzzzz)` §2.2 "REGRESSED, CONDITIONAL on the D2 centre being zero" | the stated condition is now PROVED-AND-PAID (§1–3) and is not what fails; the failure is the radius. The §2.2 measurement itself replays exactly. |
| `17(ddddd)` "unit ideal generated by the incidence rows alone" | still true as a statement about the frozen chart, and is precisely why nothing survives the correction. |
| `17(pppp)` / `17(tttt)` D2 blocks | radius SOUND; CONDITIONAL on Lemma C + one gauge payment (§6), with the δ=5/2 double-spend named. |
| `17(tttt)` `NO-KELLER-PAIR-WITH-THE-(99,66)-SKELETON` | unchanged by this lane; its D=108 dependency is not in scope here. |

The one D=108 statement that is *strengthened*: the frozen `local_power ≤ 8`
truncation and the `7 → 9` weight-42 control were both correctly typed as safe,
and Repair R shows the centre objection is answerable. The defect is a single
transplanted exponent.

## 8. Adversarial notes and negative controls

* **I demanded the group element for every normalization, including my own.**
  Repair R's element is `(x,y) ↦ (x, y + a_1)`, det 1, `J` unchanged. I did not
  use it twice: having spent it on the major centre I freed `jet0` rather than
  keeping the minor pin.
* **Two engines, exact `Q` both.** Every UNIT and NONUNIT above was computed in
  `sympy` over `QQ` and re-run in Singular over `Q`; `dim` and `reduce(1,std)`
  agree on all eight cases. No modular result is consumed as a theorem. The
  unit cases additionally carry a written cofactor identity checked to `1`.
* **Positive control on my re-emitter.** A/B/C reproduce the frozen gate, the
  frozen 42-control, and the `17(zzzzzz)` §2.2 probe with identical coordinate,
  pivot and residual counts before any new case was run.
* **Negative control on the refutation.** The witness is checked by direct
  substitution into all thirteen raw rows (not via the pivots), and separately
  against `c ≠ 0`, `D_2`-minimality, the exact order `ord_s = 35`, and the
  Galois shape of the face. A point that failed any of these would not be a
  refutation.
* **Floor/attainment.** `ord_s K3(σ_{D2}) = 35` is an equality, derived from
  Def 5.1(1) root counts plus transcendence of `π`, not a lower bound promoted
  to an equality. The corresponding support statement `4r+5q ≥ 35` is a floor,
  and I used it only as a floor (the witness sits on it).
* **Dropping rows vs dropping unknowns.** Everything I removed from the frozen
  setup is an unknown being FREED (seven `h3` coordinates, one `jet`); no row
  was dropped anywhere.
* **What I did not do.** I did not re-run the outer accounting (`8514 + 4 − 607
  − rank`), which the gate itself declines to promote; it cannot rescue a
  vacuous incidence block anyway. I did not re-run the `(99,66)` staged
  elimination. I did not attempt to decide whether the D=108 δ=3 branch is alive
  — refuting a kill is not a survival proof.
* **Flag/place/series.** The major D2 disc, the minor δ=3 place, and the
  child `δ' = 4` continuation are kept separate throughout; the centre
  coordinate `a_1` is a major-place object and `jet0` a minor-place object, and
  the single translation relates them rather than identifying them.

## 9. Artifacts

```text
box/d108-center-20260905/frozen-inputs.opus5.sha256        manifest built by awk from the receipt
box/d108-center-20260905/frozen-inputs.opus5.check.log     sha256sum -c, 6/6 OK
box/d108-center-20260905/work/center_incidence.py          independent re-emitter (radius x centre knobs)
box/d108-center-20260905/work/{A..H}_*.json                eight cases: chart, raw rows, pivots, residual, GB
box/d108-center-20260905/work/{A..H}_*.sing                Singular exact-Q scripts
box/d108-center-20260905/work/singular-crosscheck.out      dim / reduce(1) for all eight
box/d108-center-20260905/work/D-certificate.sing/.out      lift() cofactors for Repair R, CHECK = 1
box/d108-center-20260905/work/radius_ledger.py             Def 5.1(3) radii vs engine substitutions
box/d108-center-20260905/work/radius-ledger.json           D=108 MATCH false, (99,66) MATCH true
box/d108-center-20260905/work/witness-minimal.json         the refuting rational point, all rows verified
```

**VERDICT: `D108-DELTA3-DEAD` REFUTED.** The centre lemma holds and the centre
repair is clean (`RE-CERTIFIED` on that axis alone), but the D2 weight floor is
stated at `ord_s(z) = 6` where Moh Def 5.1(3) gives `5`; on the corrected chart
the stage-0 common-`h3` incidence block is vacuous and carries an explicit
rational point. `(99,66)` δ=2 / δ=5/2 are radius-SOUND and CONDITIONAL on
Lemma C plus one named gauge payment.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `21665`.
- Body SHA-256:
  `9c3b3615d8d18e9a694dd172db778c0a76bf7dd99589f10108515bd36276a2d3`.
- Frozen basis: `c78e4f98abc6284cfac320e2a0f92f01d5a3f070`.
