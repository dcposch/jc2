# Promotion: tied D1 `(a,c,r)=(3,3,2)` primary-`C2` cell

Date: 2026-08-26

Status: **PROMOTED AFTER INDEPENDENT HOSTILE REVIEW CONFIRMED.**

## Promoted statement

After the frozen generic-square and D1 source gates, in characteristic zero
the complete seven literal Faber equations are scheme-theoretically empty on
exactly

```text
D(p*k0), ord(A)=3, ord(C)=3, ord(R)=2.
```

At the decisive absolute grade 16 the complete polar source is exactly

```text
(3/4)A*C/L, (3/8)C^2/L^2,
(5/16)k0*R^3/L, (5/8)k0*R*C/L.
```

The `C2` family is the unique pole-two term.  After clearing `L^2`, the
Laurent/Faber numerator is the canonical remainder modulo `L^2` of the
fully cleared numerator; the nonzero discarded ordinary quotient is retained
and checked exactly.  On the finite etale root cover `p=-2*lambda^2`, the two
root functionals reduce to

```text
(3/8) C(+lambda)^2, (3/8) C(-lambda)^2.
```

The coefficient charts `D(c1)` and `D(c0)` exhaust nonzero linear `C`.
Both localized ideals contain `1` before radicals, and omitting `C2` makes
both root terminals identically zero.  Emptiness therefore descends from the
root cover to the base chart on `D(p)`.

The first polar grade is also the source ceiling.  There is no in-window
normal or connection jet, delayed polar `k6` or `k2` load, or target; the
earliest target is grade 28.  All seven literal source rows, their analytic
bridge, the pole-two recurrences, the ordinary quotient, both root signs,
and both localized coefficient charts were independently reconstructed by
the hostile reviewer.

## Immutable custody

```text
producer RESULT
b0ae59bfe59705c95114eebebcf639a0a27832710ddbe89a1bf1646e69e40c9b

producer freeze
41fdc7c3e3abd7b16159467484f2f4610337709d13dc7a0dbdbe4277a3407cc5

producer evidence
2bff9bc7358a5fd05a9a6dfa3efd11629ba24381359eb4c94dec134ae419299e

source freeze
30673eead795eb94dfc1f1b46fe39327bfe58ed3d5acac773d717ec5421488e8

hostile review
5bf14ac8b8560cf6e7da348e6797dd8edaa053a764e1e3b6f4596a2adfbbb2db
```

The producer ran exact `Q` plus independent `F_65519,F_65521` controls on
AWS, all rc 0 and zero swap.  Every generated program used an ordinary
polynomial `ring`, never a `qring`; the Singular 4.3.2 quotient-ring
normalization hazard is therefore inapplicable.  The hostile reviewer
rehashed all custody, independently enumerated the four-source census and
all seven grade-16 rows, reconstructed the Faber bridge and pole-two
remainder, audited both localized charts, and returned `CONFIRMED`.

## Firewall

This promotion contains only the displayed exact contact.  It does not
promote the wider primary-`C2` family, `ord(C)=8` or higher, another tied
face, positive-order leading load, `p=0`, `k0=0`, the exact-square zero
section, an excluded terminal/global chart, fan exhaustiveness, order two,
maximum twelve, or JC2.
