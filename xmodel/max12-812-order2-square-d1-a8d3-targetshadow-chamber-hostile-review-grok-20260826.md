# Hostile review — D1 `a=8,d=3` target-shadow chamber split

| Field | Value |
|---|---|
| Target | `cases/max12_812_order2_square_owner_d1_a8d3_targetshadow_chamber_split_20260826/` |
| Overall verdict | **CONFIRMED** |
| Smallest failing identity | none |
| Smallest hash defect | none |
| Smallest firewall defect | none |
| Reviewer / model | Grok 4.6 (xAI). Independent hostile rederivation. Producer `PASS` tokens, validator strings, and finite-field agreement are not authority |
| Method | SHA-256 of every charged pin, nested freeze/evidence path, and all three retrieved AWS trees; independent four-atom binomial census of the 17 polar families at `(A,C,R)=(8,11,8)`; byte comparison of parent versus compiled odd rows; exact truncated `sigma`-series expansion over `Q` of the four literal odd `SourcePhi` rows; hand membership of both chart ideals. No Singular re-execution of the AWS script, Sage, msolve, Lean, or AWS replay |
| Repo | `/Users/dc/code/math/jc2` |
| Git HEAD | `418e413593120d19e15e6546eb50c985f4b1f038` |
| Date | 2026-08-26 |

Independently recomputed SHA-256 of the seven charged primary pins match. Every path named in `PRODUCER_FREEZE.sha256` (6/6), `FREEZE.sha256` (11/11), and `EVIDENCE.sha256` (48/48) rehashes to the printed digest. Nested `compiled.sha256` on all three retrieved lanes rehashes (2/2 each). Exact `Q` is the characteristic-zero endpoint; `F_65519` and `F_65521` are separate compiled inputs and separate engine transcripts of the same source archive. The frozen no-verdict a8d3 analytic-bridge failure is not contrary evidence: the present producer reads the canonical literal odd rows, not that bridge. No file other than this review was written. No producer, ledger, prompt, source, or other file was edited.

This confirms only emptiness of the named cell `ord(A)=8`, `ord(C)=11`, `ord(R)>=8` on `D(p*k0*J)` after the cited source gates. It is not a D1, square-component, order-two, maximum-twelve, or JC2 theorem.

---

## Verdict

**CONFIRMED.**

After the frozen square/D1 source gates, in characteristic zero on `D(p*k0*J)`, the complete seven-equation system has no point in the strict contact tail

```text
ord(A)=8, ord(C)=11, ord(R)>=8.
```

The four literal odd Faber rows of the frozen complete grade-38 parent already form an empty subsystem on that chart, so no condition on the three even equations is used. Independently expanding those four rows as polynomials in `sigma` yields the exact identities

```text
Phi1 = 0  mod sigma^28,
[sigma^28] Phi1 = (3/4) k60 c1,
```

and, with moving connection `p(sigma)=p+2 sigma ell1+...+2 sigma^{10} ell10`,

```text
S = Phi7 + (p(sigma)/4) Phi5
         + (3 p(sigma)^2/32) Phi3
         + (5 p(sigma)^3/128) Phi1,
S = 0  mod sigma^38,
[sigma^38] S = -(3/32) p eta k60 c1 b0.
```

Every sign and coefficient matches the producer, including the isolated inverse-root `k6RC` column `e5=-3/16`, `e7=-3p/64` and the vanishing of every charged `c0` mixed term. The inventory's family-16 analytic/literal coefficients `(-3/8,+3p/32)` are the falsified fixed-`z` model; they are not the rows that were expanded.

The exact-contact leading-`C` locus for the numerator `c1 z+c0` is the scheme-theoretic cover

```text
D(c1)  union  (V(c1) intersect D(c0)).
```

On `D(c1)`, row one forces `k60=0` because `3/4` and `c1` are units. On `V(c1)`, the residual vanishes identically. Neither branch inverts `b0`, `eta`, or a leading coefficient of `R`. In the full odd equations the same combination differs from its source by exactly `-sigma^{38} J/4`, with no earlier odd target. Adjoining the relevant `C` inverse and inverses for `p,k0,J` therefore yields the unit ideal on each chart before radicals.

The identity is polynomial in `eta`. Substituting `eta=sigma^s`, `s>=0`, covers the closed tail `ord(R)>=8`. Extra nonnegative powers of the uniformizer only delay `R`-bearing families.

A later Keller-source composition may treat `J` as a unit and rewrite the common open as `D(p*k0)`. This producer inverts `J` and does not prove that composition.

No claim is made about `J=0`, `p=0`, `k0=0`, an equality or another leading-face chamber, positive-order load, another D1 region, the whole square component, order two, `(8,12)`, maximum twelve, or JC2.

**CONFIRMED**

---

## Verdict table

| Issue | Charge | Finding |
|---|---|---|
| 1. Rehash freeze, evidence, nested AWS, including three retrieved trees | seven primary pins; every named path | **holds**; `PRODUCER_FREEZE` 6/6, `FREEZE` 11/11, `EVIDENCE` 48/48, three nested `compiled.sha256` 2/2 |
| 2. Exact `Q` vs two primes | distinct registered AWS executions of one archive; freeze, rc, validator, diagnostic, swap | **holds**; exact `Q` is characteristic 0; primes are software controls |
| 3. Singular 4.3.2 qring hazard | `qring`, or `==0`/`subst`/`diff` without canonical reduction | **does not occur** in the compiled producer; ordinary `ring`, every `==0` is `reduce(...)==0`, every `subst` follows a reduce-checked quotient, no `diff` |
| 4. Parent inventory, 17 families, odd subsystem, missing source/target | baseline `(8,11,8)`, grade 38, 17 primitives, jets, byte-for-byte odd import | **holds**; first pole four at grade 41; only odd target is `-sigma^{38} J/4` |
| 5. Grade-28 identity and moving `S` | exact coefficients, signs, divisibility/quotients; not the fixed-`z` bridge | **holds** by independent `Q`-series expansion of the four literal odd rows |
| 6. Exact-`C` cover, `D(c1)` forces `k60=0`, closed branch kills residual, no `b0`/`eta`/`R`-leading division | scheme-theoretic cover and both branches | **holds** |
| 7. Full odd combination, unit ideals before radicals | difference `-sigma^{38} J/4`; both ordinary-ring unit ideals | **holds** by string identity of `FullPhi` versus `SourcePhi` and elementary membership |
| 8. Closed `ord(R)>=8`, role of `eta`, theorem on `D(p*k0*J)`, no global composition | homogeneous `eta=sigma^s`; Keller remark | **holds**; this producer does not prove the common-open composition |

---

## 1. Custody

Independently recomputed SHA-256 of the seven charged primary pins:

| Artifact | SHA-256 | Role |
|---|---|---|
| `RESULT.md` | `0f63f33ced93c1109e3146172e46b33e3fee28e174b081d1a1e426150168ba23` | producer report |
| `FREEZE.sha256` | `76ebe3a4d8c8dfad196cf72c96f2221cef540c600a6ab9b80225744163c39f8a` | source/parent freeze |
| `EVIDENCE.sha256` | `24d74a8441b11fd6105a4db1cbb3dd390fdc253f5de0fc9ea4ec624c000d95c0` | retrieved AWS evidence |
| `AWS_LAUNCH_METADATA.md` | `12a08b746472ac9ee3b047156fa0c7cee5137dd676e0bbb977583b2561243da4` | launch/resource record |
| `PREREGISTRATION.md` | `5e7b42e25024e218cc589c0813827ef4835dacb8a33b1f7457d9ffc57a734170` | preregistered split |
| `compile_chamber_split.py` | `431a204151ded8ef492658751855edb1b76dfec6ab31fb87677e8bb991c82982` | AWS-only compiler |
| `PRODUCER_FREEZE.sha256` | `4c9b9e441d666b91d8255af8aca3978eeccd8263775b33184a1e3334f0fd51b2` | producer pin list (self-hash; not a row of itself) |

`PRODUCER_FREEZE.sha256` pins the first six of those artifacts and does not pin itself. Every row of `FREEZE.sha256` and `EVIDENCE.sha256` rehashes. The review prompt rehashes to `c2e3a7f180ce05c605129d1561bd56496ad9f4b7114a57f8ab05bf6859ef20d7` and is not mathematical evidence.

Parent pins named by `FREEZE.sha256`, all matching:

| Artifact | SHA-256 |
|---|---|
| parent `square_d1_a8d3_j38_r3_q.sing` | `77c38a24304af28e2a80c495abda486a38b17f1085d7be3f6dd0ece84815c6d6` |
| parent `square_d1_a8d3_j38_r3_p65519.sing` | `72b01773d67d32db1b78668229bdc05db2b2c2c3041e75e386b8dee22444ce08` |
| parent `square_d1_a8d3_j38_r3_p65521.sing` | `8575b38bf2bd106d6a03b0a04325f61ce3a02bf692ce98211cb6febc82d3ce9f` |
| parent `source_inventory.json` | `de195a2a005c32a53c3802dc1ee5f8429bb94a7337543d4f1419be5ad4c14cf6` |
| `RESULT_A8D3_G38_ROUTE_FAILED.md` | `3a3e8ec967dafc26d05788930de1ddaad19a77f9d2cc2c5e93bacd5632b1304e` |
| isolated `k6RC` `RESULT.md` | `64f731aa4a518b467d2b60489643178c2e6f0c069b7f9c0a568ae685f9378ba2` |
| rank-jump `PRODUCER_FREEZE.sha256` | `a28e20033d4e0d5cd8fd3303a371d802147db65875cace445904a48f9f4fc83e` |

The three parent program bodies are byte-identical except the opening `ring R=<char>,(...),dp;` line. All three parent files have `qring` count zero.

### AWS executions, archive `a6110b910ba4de57de9744b846f3bbbcdc41709a2d18bcf53522c3f145fbb6a5`

All three retrieved `archive.sha256` files name that digest. All three on-host `FREEZE.sha256` checks print eleven `OK` lines and no mismatch. Compiler stderr is empty on every lane. Outer stderr/stdout are empty. Mathematical stdout is byte-identical across characteristics, SHA-256 `389467ccb94d61c0c68e9615e4d21d68f7318a04b9b50c9d81ff8b475f1dc03c`. Engine `rc=0`, `Swaps: 0`, validator `PASS_D1_A8D3_TARGETSHADOW_CHAMBER_EMPTY`, no `// **`, no `? `, no `error occurred`. Nested `compiled.sha256` matches each lane's `result.json` and `.sing`.

| field | exact `Q` | `F_65519` control | `F_65521` control |
|---|---|---|---|
| host | Box02 / `ip-172-30-0-186` | Box03 / `ip-172-30-0-249` | r6d / `ip-172-30-0-45` |
| tag | `max12_812_order2_square_d1_a8d3_targetshadow_q_box02_20260826T2130Z` | `..._p65519_box03_20260826T2130Z` | `..._p65521_r6d_20260826T2130Z` |
| launcher PID | `335211` | `275814` | `348397` |
| start UTC | `2026-08-26T21:15:09Z` | same | same |
| wall | `8:36.60` | `6:44.53` | `6:45.47` |
| peak RSS KiB | `26628184` | `21419004` | `21415716` |
| engine rc / swaps | `0` / `0` | `0` / `0` | `0` / `0` |
| compiled characteristic | `0` | `65519` | `65521` |

Exact `Q` is the characteristic-zero proof. The two primes are independent host/software controls. In both fields `4` and `32` remain units, so the displayed rational coefficients of the identities stay nonzero; that is a control, not a second derivation.

---

## 2. Singular qring audit

Each compiled producer program is 48 lines. Line 1 is an ordinary polynomial ring

```text
ring R=<char>,(z,t,sigma,p,ell1,...,J,iJ,ip,ik0,ic1,ic0),dp;
```

obtained from the parent ring by adjoining `ip,ik0,ic1,ic0`. Counts on all three compiled files: `qring` 0, `diff(` 0, `reduce(` 13, `==0` 13, `subst` 3. Every `==0` is of the form `reduce(poly, std-ideal)==0`. Each `subst` is `subst(quotient, sigma, 0)` and is gated by a prior `reduce(original, std(sigma^N))==0` together with the polynomial identity `reduce(sigma^N*quotient-original, std(0))==0`. That is canonical reduction on an ordinary polynomial ring. No conclusion uses a quotient-ring assignment.

The frozen parent does contain `diff`/`subst` in the failed PredPhi analytic bridge and in jet-maximum sentinels. Those lines are not imported. The present compiler copies only the four `poly D1A8D3J38R3_SourcePhi{1,3,5,7}=...;` definitions, which are unique, semicolon-terminated, and byte-identical to the parent in all three characteristics.

---

## 3. Parent inventory, families, and odd subsystem

Independent four-atom expansion of the four source summands at baseline `(A,C,R)=(8,11,8)` through grade 38, padded by `+1` and `+2` in the product bounds, yields exactly the frozen 17 primitives, including coefficients, poles, and `fixed_sigma`. Maximum pole through grade 38 is 3. Extending the same census to grade 60 places the first pole-four wall at grade 41 (`k6 A C`, coefficient `-3/16`). The next polar families sit at grade 39 (`A^3` and `R A C`, both pole 3) and do not enter grade 38. Derived mechanical maxima

```text
A:9, C:10, R:8, k10:8, k6:10, k2:8, p:10
```

match the frozen inventory; licensed source jets are 100, licensed target jets are 22. High-jet grade-38 sentinels are `a1_9`, `c1_10`, `b1_8`, `k0_8`, `k60_10`, `k20_8`, `ell10`. Status is `COMPLETE-D1-A8D3-THROUGH-GRADE38`. Homogeneous tail marker is `R=8+s via eta=sigma^s, s>=0`.

The only odd target in the pinned inventory is

```text
{row: 7, grade: 38, variable: J, coefficient: -1/4}.
```

Rows 1, 3, 5 are target-free. Even targets `mu20_*`, `mu4_*`, `mu6_*` cannot enter the odd combination.

Parent `FullPhi1`, `FullPhi3`, `FullPhi5` are characterwise identical to the corresponding `SourcePhi` rows. Parent `FullPhi7` is exactly `(SourcePhi7)-sigma^38*(J/4)`. That is the claimed full-versus-source difference, read from the compiled parent bytes, not from a `PASS` string.

Why emptiness of the odd subsystem is enough: a point of the seven-equation system is a point of every subset. If `{Phi1, Phi3, Phi5, Phi7}` is already empty on the named chart, the three even equations are idle. The argument is coefficientwise in the jets (the actual first-normal system), not the auxiliary hypersurface `Phi_i=0` in the ring that still treats `sigma` as a base coordinate. The producer extracts those coefficients by reduce-checked division in `sigma`.

A missing polar source that could alter an odd row through grade 38 would have to be either a pole-four family below grade 41, a grade-`<=38` primitive omitted by the padded census, or an inverse-root motion term not present in the canonical tails. The census is pad-stable and matches the frozen list. Inverse-root motion is already in the literal `SourcePhi` rows: those rows are the tail-library conversion of the exact coefficient substitution `K=L^2+sigma^2 R`, `D=L A+C` at fixed Faber coordinate, and the independent series expansion below includes every mixed correction those tails actually carry through grade 38. The only surviving source monomial in `S` is `k60 c1 b0 eta p`. No omitted source or target through grade 38 was found.

---

## 4. Grade-28 identity and moving `S`

The four odd parent definitions were parsed as polynomials over `Q` and expanded as series in `sigma` truncated after degree 38, without consuming AWS `PASS` lines.

| row | expanded terms | first `sigma`-degree | `[sigma^28]` |
|---|---|---|---|
| 1 | 907 | 28 | `(3/4) k60 c1` exactly |
| 3 | 2977 | 28 | (not needed) |
| 5 | 5088 | 28 | (not needed) |
| 7 | 6503 | 28 | (not needed) |

Row 1 has no term of `sigma`-degree less than 28, so `Phi1` is divisible by `sigma^{28}` in the polynomial ring. The quotient is the shifted series; its constant term is `[sigma^{28}] Phi1`. That constant term is exactly `(3/4) k60 c1`, with no jet, `p`, `eta`, `b0`, or `b1` companion. This is the claimed divisibility/quotient pair.

Forming

```text
S = Phi7 + (p(sigma)/4) Phi5 + (3 p(sigma)^2/32) Phi3 + (5 p(sigma)^3/128) Phi1
```

from those expansions collapses fifteen thousand odd monomials to a single term:

```text
S has no term of sigma-degree < 38,
[sigma^38] S = -(3/32) p eta k60 c1 b0.
```

The identity is exact, including sign. In particular `S` vanishes identically upon setting `k60=0` and upon setting `c1=0`.

The mixed `k6RC` column read from the same expansion at grade 38 is the isolated inverse-root column, not the inventory family-16 column:

| coefficient | canonical `SourcePhi` (this expansion) | isolated diagnostic | inventory family 16 (fixed-`z`) |
|---|---|---|---|
| `e5[k60 c1 b0 eta]` | `-3/16` | `-3/16` | `-3/8` |
| `e7[k60 c1 b0 eta p]` | `-3/64` | `-3/64` | `+3/32` |
| `e5[k60 c0 b1 eta]` | `0` | `0` | `-3/8` |
| `e7[k60 c0 b1 eta p]` | `0` | `0` | `+3/32` |
| `Phi1`, `Phi3` mixed | `0` | `0` | `0` |

The constant-`p` part of `S` on that column is `e7+(p/4) e5 = -3p/64 + (p/4)(-3/16) = -3p/32`, which is the displayed residual. Family 15 `k6 A^2` is annihilated by the same combination: `S[38]` contains no `a0 a1` monomial. That is the rank-jump distinction — the generating-function coefficients annihilate every true binomial family, including family 15, and leave the motion-corrected `k6RC` column. The frozen no-verdict analytic bridge used the uncorrected family-16 coefficients, for which `S` would have vanished; those coefficients are not the literal rows.

Row-1 and row-3 term counts match the inventory. Rows 5 and 7 are short by one term each (`5088` vs `5089`, `6503` vs `6504`) because the inventory still lists the false `b1 c0` family-16 monomials. That discrepancy is evidence that the compiler imported canonical tails rather than the failed bridge, not a defect in the producer.

---

## 5. Exact-`C` cover, both branches, no hidden division

Under the named numerator convention the leading ordinary part of `C` is `c1 z+c0`, with `c1,c0` the constant terms of the two coefficient jets. Exact contact `ord(C)=11` is `(c1,c0)\ne(0,0)`. The set

```text
D(c1)  union  (V(c1) intersect D(c0))
```

is exactly that complement of the origin in the leading-`C` plane. The printed line `A8D3_SHADOW_EXACT_C_CHART_COVER=1` is a hardcoded sentinel, not a Singular membership test; the cover itself is elementary and is used as such.

On `D(c1)` the grade-28 equation is `(3/4) k60 c1=0`. In characteristic zero, `3/4` is a unit and `c1` is inverted, so `k60=0` scheme-theoretically. Then `[sigma^{38}] S=0` as a polynomial identity.

On `V(c1) intersect D(c0)`, the factor `c1` kills the residual directly. The closed chart adjoins `ic0 c0-1` only to stay on exact contact; the unit-ideal calculation below would still succeed on the larger set `V(c1)`, which this producer correctly does not claim.

The compiled chart ideals invert `c1` or `c0`, `p`, `k0`, and `J`. They do not invert `b0`, `b1`, `eta`, or any other leading coefficient of `R`. The residual is linear in `eta` and in `b0`; vanishing of either would only make emptiness easier, and neither is used as a denominator.

---

## 6. Full odd combination and unit ideals

Because `FullPhi{1,3,5}=SourcePhi{1,3,5}` and `FullPhi7=SourcePhi7-sigma^{38} J/4`, the same odd combination of the full rows is

```text
S_full = S_source - sigma^{38} J/4.
```

There is no earlier odd target. On either exact-`C` branch `S_source` vanishes through grade 38, so the terminal coefficient is `-J/4`.

The two ordinary-ring ideals, written in the jet ring after taking the displayed coefficients, are

```text
OpenUnit   = ( (3/4) k60 c1,
               -(3/32) p eta k60 c1 b0 - J/4,
               ic1 c1 - 1, ip p - 1, ik0 k0 - 1, iJ J - 1 )
ClosedUnit = ( c1,
               -(3/32) p eta k60 c1 b0 - J/4,
               ic0 c0 - 1, ip p - 1, ik0 k0 - 1, iJ J - 1 ).
```

On the open chart, `(3/4) k60 c1` and `ic1 c1-1` put `k60` in the ideal; the second generator then reduces to `-J/4`; with `iJ J-1` one has `1`. On the closed chart `c1=0` reduces the second generator to `-J/4` immediately, and again `1` is in the ideal. Both memberships are before radicals. The generators `ip p-1` and `ik0 k0-1` are the named open, not ingredients of the residual. In particular `k0` does not appear in the odd identities; it is inherited from the upstream gates.

These proxy generators are consequences of the odd jet equations (row-one leading coefficient, and the grade-38 coefficient of the odd combination). A point of the odd subsystem on either chart would therefore force `1=0`. Emptiness is an exact ideal-membership certificate, not a point sample.

---

## 7. Homogeneous `ord(R)>=8` coverage, `eta`, and theorem scope

`R` is written `sigma^8 eta (b1(sigma)+t b0(sigma))`. The residual is polynomial in `eta`. Substituting `eta=sigma^s` for `s>=0` realises every closed tail `ord(R)>=8`. Extra nonnegative powers of `sigma` only push `R`-bearing families to later grade. Neither `eta` nor `b1,b0` is inverted, so there is no hidden restriction to `ord(R)=8`. Orders of `A` and `C` remain exact.

The compiled scope string is `A8_D3_C11_R_GE_8_ON_D_P_K0_J_ONLY`. The unit ideals invert `J`. On a Keller source `J` is a unit, and a later composition may rewrite the common open as `D(p*k0)`. That composition is not a theorem of this producer: the present certificate is emptiness on `D(p*k0*J)`, and nothing is claimed on `V(J)`, `V(p)`, or `V(k0)`.

---

## 8. Firewall

Confirmed statement, and nothing further:

After the frozen square/D1 source gates, in characteristic zero, the cell `ord(A)=8`, `ord(C)=11`, `ord(R)>=8` is empty on `D(p*k0*J)`.

Not confirmed, and not to be composed from this file:

- `J=0`, `p=0`, `k0=0`
- an equality face or another leading-face chamber
- positive-order load
- another D1 region, including `a=7` and `a=9`
- the whole square component
- order two, `(8,12)`, maximum twelve, or JC2
- the global/common-open Keller composition that would drop the `J` factor

The earlier broad a8d3 analytic bridge remains a frozen no-verdict route failure. It used a PredPhi/`diff`/`subst` reconstruction whose family-16 column is not the canonical tails. The present split reads those tails directly. That failure is not contrary evidence.

**CONFIRMED**
