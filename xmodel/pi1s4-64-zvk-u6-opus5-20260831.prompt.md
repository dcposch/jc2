# Research lane: ZVK-U6 — the direct computation on the fold union

You are the flagship computation lane on
OPEN[PI1S4-(6,4)-FOLD-ZVK-U6] (charged FOLD-REDUCTION §4.3(b)): the
Zariski-van Kampen computation for the two-component union
`U_6 = D' ∪ D'^-` (two 3-nodal coprime (4,3) quartics, hyperflexes at
a common infinity point, cross-locus over {u=0} with total contact
3), deciding whether `π₁(C² − U_6)` admits a surjection onto S_4 with
all meridians transpositions (the two components' meridians may map
to different transposition classes — track both) satisfying the
disjointness constraints pulled back from the fold (charged §3: the
S_4 rep on D restricts to U_6 with the specific local conditions the
charged report derives — consume them at their typed scope,
PROVISIONAL).

charged_input=xmodel/pi1s4-64-fold-reduction-opus5-20260831.md
charged_input=xmodel/pi1s4-64-fixed-tuple-opus5-20260831.md
charged_input=xmodel/block-descent-a1-na-rowsweep-coordinator-integration-fable5-20260831.md

Your inputs are frozen read-only copies in `{{LANE_INPUTS}}`;
verify these SHA-256 hashes first and stop on mismatch:

```text
8529de8ec1b11450a95481ffdf19f6e5116129aeef2ea2c27579b4e4fcc93a74  {{LANE_INPUTS}}/pi1s4-64-fold-reduction-opus5-20260831.md
a60887501137251f50f1ce7bf8ddc8921b1f22e20a4aebced47ce35699a0fad8  {{LANE_INPUTS}}/pi1s4-64-fixed-tuple-opus5-20260831.md
126c2d2941dcd5b3f95584d0d2cc371dea270ed30494f773abafd4f9e5c20286  {{LANE_INPUTS}}/block-descent-a1-na-rowsweep-coordinator-integration-fable5-20260831.md
```

The charged report types the entry data completely (§4.3(b)): 6
strands (4+... derive: each quartic contributes 4 strands to a
vertical line? the union has degree 8 — re-derive the strand count
from the charged setup; the report says 6 strands, 11 discriminant
values, local braid types, forced spanning-tree structure of the
four identification relations — re-derive ALL of it from the §2
geometry rather than trusting the numbers). Then:

1. Write the braid monodromy factorization constraints: each
   component alone has abelian complement (promoted), so all
   relations within a component collapse; the cross-relations (nodes
   of D' ∩ D'^- over {u=0} with contact μ_τ summing to 3, plus the
   infinity identifications) carry everything.
2. Impose the S_4 conditions and the fold-pullback constraints;
   solve the resulting finite system (it is a finite check over
   subsets of the 6 transpositions of S_4 with conjugation actions —
   enumerate exhaustively, show the enumeration is complete).
3. Verdict: NO tuple exists (row KILLED via the fold restriction —
   state the theorem and its dependency chain: which provisional
   items it consumes), a tuple EXISTS (the fold route is exhausted;
   report the tuple), or OPEN at a named computation with the exact
   finite system written out for a successor/AWS job.

Desk-scale exact reasoning only; you may fetch and hash primary
literature (record exact sources); never run any CAS or computation of
uncertain duration on this machine. Do not edit canonical ledgers, any
charged file, or inspect `jc2-lean`. 6 hours hard budget.

Write one report and no other file:

```text
xmodel/pi1s4-64-zvk-u6-opus5-20260831.md
```

Create the report file with a skeleton of section headers as your very
first action — the skeleton must NOT contain the `<!-- BODY-END -->`
marker. Append each completed section as you finish it, as a separate
bounded write (aim under 1,500 words per write). Only after the final
section is on disk, append the standalone `<!-- BODY-END -->` line.
If the budget runs short, finish the current section, type the rest
OPEN in one short paragraph each, then seal. Keep it under roughly
6,000 words. Do not include a `charge_basis` declaration.
