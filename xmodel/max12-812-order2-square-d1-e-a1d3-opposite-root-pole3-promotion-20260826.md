# Promotion: exceptional D1 `E=(a,d)=(1,3)` closed `R` tail

Date: 2026-08-26

Status: **PROMOTED AFTER INDEPENDENT HOSTILE REVIEW CONFIRMED.**

## Promoted theorem

On the frozen generic square/D1 chart `D(p*k0)`, after the reviewed
square/D1 and unique-first-`AC` gates, the entire routed exact-contact block

```text
ord(A)=1, ord(C)=4, ord(R)>=2
```

is empty.

The complete seven-row source through grade 18 has eight polar primitives
and a unique pole-three family

```text
-(1/16)A^3/L^3.
```

The exact `L^3` recurrence and both Laurent-to-Faber root identities give,
after the reviewed opposite-root allocation of the nonzero leading `A,C`
forms, the deck-conjugate terminal units

```text
+(1/2)au^3*lambda^3,  -(1/2)au^3*lambda^3.
```

Every pole-at-most-two family is multiplied by `L` in the `L^3` numerator
and vanishes at the nominated root.  This includes both `C^2` and `RA^2`,
all higher normal corrections through grade 18, and the exact second
correction to `RA^2`.  Rows `1,3,5,6` are target-free there.  The unit ideals
invert only the named root/exact-contact/load coefficients
`lambda,au,cv,k0`; no leading `R` coefficient is inverted.  Hence the
argument covers the full closed tail `ord(R)>=2`, not only exact order two.

## Immutable custody

```text
producer RESULT SHA256:
f2b947ddb2b251069666bb8287800127253ca3f118f5fb22587b3b0032bba1f5

producer freeze-manifest SHA256:
65a40079b7b52964a775d01ef651f24ee04dcce05650147ed11327ace5913237

producer evidence-manifest SHA256:
31050d988c6246a48610847a7e7498bc55922a8ea0c9a4e87a1db7a30808e382

hostile review SHA256:
984783eab8342d2523a6e1d7365785f5d37746d0393fc30cfad206bb5658b774
```

The producer ran exact `Q` plus independent `F_65519` and `F_65521`
software controls on AWS.  Characteristic zero, not good reduction, is the
promoted endpoint.  The hostile reviewer independently rehashed the custody,
re-enumerated the eight primitives, checked the `L^3` numerator/recurrence,
both root sheets and signs, the localization, target timing, and the closed
`R` tail, and returned `CONFIRMED` with no failing identity or missing source
monomial.

## Firewall

This promotion closes only the routed `E` block on `D(p*k0)`.  It does not
cover an equality face, a positive-order leading load, `p=0`, `k0=0`, another
D1 face, a terminal/global chart, the square component, order two, `(8,12)`,
maximum twelve, or JC2.
