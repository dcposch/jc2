# Contact Gram runtime port

STATIC SOURCE ONLY — complete proposed source package, UNREVIEWED and UNEXECUTED. First action2026-09-10T20:20:34.706868764Z; original reserve20:43 UTC / hard stop20:46 UTC retained. No worker, payload, actual authority, scientific import or test was created.

## Outcome

Prepared dispatch.py, independent mutate.py, byte-identical probe.py, CONTRACT.md, READ-SCOPE.md and PINS.json in the owned box. The dispatcher ports the accepted middle-real infrastructure to job f10-contact-gram-certificate-20260910 and the exact corrected contact authority. It freezes exact installed role hashes, not merely a matching basename elsewhere, and supplies corrected python_flags/full_argv/science_paths before prospective launch. ROOT's independently authenticated source/native/physical registration remains mandatory.

The contact producer/checker source was provisional at invitation. ROOT reported its FIRST gate A–F confirmed without source correction at20:29. This lifecycle-only notice was recorded without reading the optional new gate or changing the13-input vector. This new runtime/mutator has no independent review or execution result.

## Exact state machine

The inherited nine preflights remain: five specific metadata refusals, valid sentinel, two actual WRONG-JOB science CLI refusals, and the same-PGID no-CAS orphan dummy with actual RSS-triggered identity-matched TERM/KILL/reap checks. The unchanged probe SHA is d0df7ba6d9e0e082bed92321dd390fe91bc4bf8f32ce68cdf9c8a9180b4e39f4.

One producer is admitted after preflights. Normal0 candidate and normal2 complete INCONCLUSIVE artifact are distinguished by exact stdout hash/size, empty stderr and bounded existing output; a partial/error artifact stops. One independent checker receives the root-frozen output. A complete checked-INCONCLUSIVE receipt ends honestly after11 calls without control coverage. A positive receipt alone enables one mutator and two negative checkers:14 calls on full success.

Positive status is exactly PRESCRIBED_CONTACT_EXCLUDED_BY_CERTIFICATE. The checker receipt retains prescribed triangle t<1, no full-window equivalence/source/JC2, and explicit absence of independent Gram determinant equality checking. Producer sign status is never promoted. New runtime terminal status CONTACT_CERTIFICATE_CHECKED_NEGATIVES_CONFIRMED still carries science NONE pending separate review and root adjudication.

## Meaningful controls, not observations

The mutator authorizes through the unchanged corrected metadata authority, then checks fixed code, its own source pin, positive receipt and current input hash before Fraction/payload parsing. It independently checks full canonical wire schema/bounds without producer/checker arithmetic imports. It changes exactly Uphi+1 and the existing Bernstein[0][0]+1 in separate clones. Complete canonical-byte inverse restoration is checked for each; three SHA values must differ.

Uphi constant insertion/replacement/deletion is canonical and leaves every other semantic field unchanged. Its identity residual changes by the nonzero fixed phi. Bernstein[0][0] changes expansion by the nonzero basis polynomial (1-t)^(52-j)(1-v)^52. These are manual expected predicates, not computed fixtures.

Each unchanged negative checker must return NORMAL_EXIT2, empty stdout, no receipt, and respectively EXACT stderr:
`INCONCLUSIVE: full contact cofactor identity\n`
or
`INCONCLUSIVE: full Bernstein expansion\n`.
A stale hash, malformed canonical rational, authority failure, resource cap, receipt, traceback or different message is STOP, not a passed control. Overflow of either fixed change means unavailable/STOP; no alternate mutation or extra control is introduced.

## Budgets and deployment boundary

Profiles are frozen at producer400wall/359softCPU, positive checker90/89, mutator10/9, negatives30/29 each. Nominal phase wall560 and hard-child CPU520 leave40wall/30CPU under original joint600/550, all UNMEASURED. Separate preflight180wall and original2GiB/16MiB remain. ROOT must enforce aggregate CPU including dispatcher/descendants externally; inherited reaped-child CPU accounting and sampled group RSS are not a hard total-resource theorem. No runtime feasibility or speed claim is made.

The actual dedicated16MiB tmpfs bounds baseline, two mutants, logs and metadata together. Mutator checks triple size plus available bytes before exclusive0444/fsync/readback writes. Future metadata can still fill the reserve; that is STOP, never a cap increase. Every consumer gets a fresh root-frozen hash and authority; quiet-before-body, exact parent/child identities, namespace, input/native pre/post pins, deadlines and durable custody remain inherited. Directory freeze fsync is explicit; ROOT owns final parent-directory/archive/stop obligations.

No disabled deployment scaffold was necessary. CONTRACT specifies all future fields/vector formulas and leaves physical/native/deadline values to ROOT. There is no actual registration or worker authority.

## Read scope, own controls and limits

All13 supplied hashes matched before bodies; twelve fresh WHOLE reads and explicit same-byte prior WHOLE reuse of CAPRUN4435279d are recorded. No other scientific body or gate was read. No candidate/baseline/fixture coefficients were read, generated, parsed or hashed locally. No import/AST/syntax/compile/test/CAS or mathematical subprocess was run.

Manual whole source readback recovered clipping and caught an append-context placement error, repaired before seal. A relative-path slip initially placed only new dispatch/probe under /home/ubuntu/box; apply_patch moved them to the intended owned repository directory and the agent-created empty directory was removed. No preexisting file was touched. This operational error and recovery are explicit in READ-SCOPE, not hidden as test success.

This report creates zero canonical OPEN IDs. One residual validation quantity remains: independent static correctness of this exact frozen runtime/mutator interface; cheapest next test is its separately authorized FIRST review, followed only if ROOT separately registers execution. Actual CPU, wire size, coefficient limits and whether a positive certificate exists remain unknown. No follow-on is authorized.

The owned report/source WHOLE read, own-only unfinished-marker/collision inspection, final current13-pin comparison and artifact expected-manifest verification precede terminal custody.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6465`.
- Body SHA-256:
  `4206b37997a8c2786510417bf715d0252ae3625e5f5457f285ba7942575c4012`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
