# Grok 4.6 primary research — exact lambda on the td=8 equal-join route

Work independently from `/Users/dc/code/math/jc2` and write exactly:

`xmodel/m2-td8-equal-join-exact-lambda-primary-grok46-20260829.md`

This is primary mathematics, not review. The reviewed formal route is
`xmodel/m2-td8-equal-join-route-family-sol56-20260829.md` with review
`xmodel/m2-td8-equal-join-route-family-hostile-review-grok46-20260829.md`.
Your own local Prop. 8.1(iv) report may be used only as established local
pattern context. Do not read active Opus/Fable/Sol outputs produced after
those reports.

The recorded lower-bound budget is exactly `2+2+2=6`, meeting the filter
ceiling. Determine whether the true Sigray lambda contribution is exactly 2
or strictly larger at each of the three priced steps:

- each of the two identical incoming `(21,15)` A-steps, with the rigid local
  pattern charged in the route;
- the `(85,35)` trunk step, including every dependence on the affine
  parameter `t` through the edge labels/handshake.

Read and pin the printed definition of lambda and every theorem used to
obtain AF2/the recorded floor, including the relevant preceding definitions,
hypotheses, conventions, and errata in canonical campaign files. Reconstruct
the actual expansions or valuation/intersection data required by the
definition. Do not infer equality merely because the lower bound is 2.

Seek, in order:

1. a uniform proof that any one charged step has `lambda>=3`, which kills
   the entire affine route;
2. an exact computation `lambda=2` for each step, with all equality
   conditions and compatibility data exposed;
3. if neither is derivable from present information, a minimal typed formula
   for the missing coefficient/jet datum and a finite symbolic discriminator
   that can be handed to the transport lane.

Keep distinct: a local pattern, a compatible route, an actual source
landing, and a realizable Keller map. Check whether the two incoming copies
are genuinely independent or whether their shared merge imposes a coupled
lambda correction. Also test whether the affine parameter changes the exact
valuation despite the fixed reduced successor.

Use exact hand algebra and short standard-library Python only; provide a
small source/test packet if it materially certifies a finite identity. No
heavy CAS or speculative AWS. State one of `ROUTE_KILLED_BY_STRICT_LAMBDA`,
`EXACT_LAMBDA_TWO_PROVED`, or `PARTIAL`, with the narrowest maximum
consequence and a precise next lemma. Include full/body hashes and exact
replay commands/counts.

Hard boundaries: no web, AWS, commit, push, canonical edits, local
long/high-memory process, Singular, msolve, Sage, or PARI. Never
access/list/search/build/status or control `jc2-lean`; never run global
`git status` or workspace-wide searches. Modify only the requested report
and a uniquely named packet under `cases/` if needed; use `/tmp` for scratch.
