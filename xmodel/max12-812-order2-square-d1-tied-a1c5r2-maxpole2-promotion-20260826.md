# Promotion: tied D1 `(a,c,r)=(1,5,2)` maximal-pole cell

Date: 2026-08-26

Status: **PROMOTED AFTER INDEPENDENT HOSTILE REVIEW CONFIRMED.**

## Promoted statement

After the frozen generic-square and D1 source gates, in characteristic zero
the complete seven literal Faber equations are empty on exactly

```text
D(p*k0), ord(A)=1, ord(C)=5, ord(R)=2.
```

At the decisive absolute grade 16 the complete polar source is exactly

```text
(3/4)A*C/L, (5/32)k0*A^2/L, (5/16)k0*R^3/L,
-(3/8)R*A^2/L^2.
```

The last family is the unique pole-two term.  The reconstructed negative
tail is the canonical remainder modulo `L^2` of the fully cleared numerator;
the discarded ordinary quotient is retained exactly.  The first root Faber
pair allocates the nonzero linear `A,R` forms.  Both same-root charts are
unit ideals.  On each opposite-root survivor, exact division by `L` gives
the deck terminals

```text
+(5/2)k0*lambda^3*bu^3, -(5/2)k0*lambda^3*bu^3,
```

and derivative-row units `5*k0*lambda^4*bu^3`.  At the nominated `A` root,
`AC`, `A2`, and divided `RA2` vanish; the nonzero `R3` terminal cannot
cancel.  The four root-value charts are exhaustive, the unit ideals are
before radicals, and emptiness descends from the finite etale root cover on
`D(p)`.

The quarantined V1 literal equality failure was only an overstrong
Laurent-versus-ordinary normalization sentinel.  It is not a mathematical
defect in the promoted V2 statement.

## Immutable custody

```text
producer RESULT
f09b44f18011e592f8366a380d1bba723a223d4c424fbf9cbfa951a8de87d994

producer freeze
7813393ee756923f73754fd6c3e1ba8e153594ffa7ccf3963926e765761adede

producer evidence
d4802e87047438882c5f825980d7bf622436636e4a94c8c8429ad46a826a8be1

hostile review
7b83e24c00e1b5e2c76f41590806596c586fa067102882677e740b7978983e6b
```

The producer ran exact `Q` plus independent `F_65519,F_65521` controls on
AWS, all rc 0 and zero swap.  The hostile reviewer rehashed every custody
row, independently enumerated the source, reconstructed both pole stages
and both deck terminals, audited the four charts, and returned `CONFIRMED`.

## Firewall

This promotion contains only the displayed exact contact.  It does not
cover `ord(C)>=6` (a separate live review), another primary/tied face,
positive-order leading load, `p=0`, `k0=0`, the exact-square zero section,
an excluded terminal/global chart, fan exhaustiveness, order two,
maximum twelve, or JC2.
