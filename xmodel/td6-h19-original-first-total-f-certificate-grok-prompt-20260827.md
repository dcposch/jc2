Act as an independent JC2 co-researcher.  Your only repository write is

`xmodel/td6-h19-original-first-total-f-certificate-grok-20260827.md`.

Read in full the promoted H18 theorem, the repaired H19R1 producer result and
hostile review, and every source/compiler/manifest they charge:

- locate H18 via SHA prefix `4120d6b6` in `xmodel/`;
- `cases/td6_c1_c2_c3_allq_mod_f_unitriangular_functional_v89h10_aws_20260826/P13_RAW_COORDINATE12_AUDIT_R1_RESULT.md`;
- `xmodel/td6-v89h19r1-raw-p13-coordinate12-hostile-review-grok-v2-20260826.md`;
- the H15/H18/H19R1 case sources and exact FIRST data named there.

The direct raw-P13 shortcut is dead.  Target the real missing composition:
track literal CURRENT degree-13 coordinate 12 through the complete original-
FIRST affine solve (all 38 rows, all 132 transport variables, all 22 q
coordinates), recover explicit multipliers for the reviewed H18 unit
`(3500000000/9)*(U/V)`, and then clear the independent total relation `F`
without inverting unregistered factors.  Preserve the reviewed target-shear
status of q15, the normalization `F=0,D(U*H*B3)`, and the identities
`B3=V^4`, `U*H=V^2-4U^3` only at their licensed scopes.

Try exact structural cancellation first: identify which original-FIRST
multipliers convert H19R1's two raw `1/U` terms into H18's `U/V` unit, and
whether the genuine omission fixture changes a cancellation cofactor or the
endpoint.  You may use light exact reads and sparse arithmetic locally, but
no Groebner basis, Singular, large matrix solve, or heavy/uncertain local CAS.
Do not mutate AWS, use the web, or access `jc2-lean`.  If a heavy solve is
required, produce the smallest AWS-ready fail-closed design, reusing existing
frozen matrix/factorizations and emitting exact original-FIRST and total-F
multipliers rather than another quotient normal form.

Return exactly one verdict:

- `EXPLICIT_CERTIFICATE` with all multipliers and direct expansion checks;
- `EXACT_OBSTRUCTION` identifying a genuine inconsistency in H18 composition;
- `BOUNDED_AWS_DESIGN` with scripts/inputs/outputs/negative controls/stop
  rule sufficiently exact for immediate coordinator implementation; or
- `NO_VERDICT` naming the first missing datum.

Separate raw support, FIRST cancellation, `F=0` reduction, denominator
clearing, and source/landing composition.  Do not withdraw H18 merely because
H19 raw differs.  No whole TD6, SP-2, or JC2 claim without the complete
composition and existing coverage obligations.
