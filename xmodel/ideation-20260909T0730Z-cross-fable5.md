# 0730 equal cross — Fable 5.1: two new proofs survive a hostile read; the family closes except at one triple-root stratum

Tag `ideation-20260909T0730Z-cross-fable5`. Frozen basis `0d39df3c9fd69c939a8420c54d03228b9077777d` (provenance only). Actual root invitation 08:31 UTC; lane start 08:31:44 UTC; common ABSOLUTE completion 08:47 UTC. The original soft target 08:27:37 was MISSED before this seat was invited and is not reset. JC2 remains UNRESOLVED. This is a cross-review strategy report: nothing here is a promotion, a gate verdict, a launch order, or a new accepted premise. ZERO mathematical subprocesses of any size: every argument is factored prose on the frozen bytes.

## 0. Custody, scope, disclosures

- All 20 charged objects were read WHOLE from `/tmp/jc2-lane.rmq2vA/inputs`: PINS.json (19 content entries, own hash in outer custody), the four blinds (root, Astra, Sol, own), both new proofs and their transactions, the nine unchanged science/history snapshots, poststate, and the contract. Labeled excerpts are treated as the entire charged objects. Own metadata ledger: `box/ideation-20260909T0730Z-cross-fable5/read-ledger.sha256` (sha256sum output only).
- No live gate, peer cross, log, receipt, checker, protected tree, AWS/SSH, CAS, script, or public resource was touched. Both new proofs are treated as UNREVIEWED science; their active gates are uncharged and were not read.
- Inherited-knowledge disclosure: this seat's persistent memory holds earlier-lane notes on this campaign; none is imported as a premise. Same-model caveat: the own blind is one of the four inputs; agreement with it is not weighted.
- Status at seal: COMPLETE on every contract item within the 15-minute window; depth is limited accordingly, and every mathematical statement beyond the packet is labeled UNVERIFIED.

## 1. Deduplicated view of the four blinds

All four blinds converge on one strategic move and it has already been executed post-blind: replace fixed exponents by a parameter-uniform local theorem (root Card 1, own Card 1 item iv, Astra Card 1, Sol Card 1 all ask for this) and independently check that the F9 source arrow is uniform in the family parameter (root Card 2, own Card 1 items i–ii, Astra's "first genuine barrier"). The two new proofs are exactly those two arrows. Consequences for the blind cards:

- **Superseded by the new local proof:** own Section 3 heuristic (K^(2m−n) | F_j²) and Astra's ratio-window lemma (H | F_j² for n=2m−1). The new proof never uses an order-2j divisibility lever, an ordG bound, a quotient q, or the remainder rho; it uses whole B and a global minimizing line. Both instruments are now unnecessary for the exclusion and should be retained only as history. Own Card 2 (2m−n census) loses its purpose and is STOPPED.
- **Absorbed:** Astra's m=4 control F_13 = K²·b. It lies inside the new proof's scope (delta = 2 = m−2, L = 2, j = 13 = 7L−1 with Delta = 1, so the budget (5) holds with equality and the margins (7)/(8) are strict). It is a useful boundary control for the gate, not an obstruction.
- **Still live:** the fixed-degree ROW SELECTION for each family parameter (own Card 1 item i, with its F10 j=0 caveat) — this is the first precise gap for actual-degree closure, treated in Section 3 and Card 1 below.
- **Deferred, three origins (Astra Card 2, root Card 3, 0445):** the finite D<=35 sieve naming F16(0) 99/165 and F22(1) 96/160. Not selected ahead of Card 1; see Card 3.
- **Systems:** own probe UPGRADE and Sol's release-receipt UPGRADE target the same failure class; deduplicated in Section 7 to one proposal.

## 2. Hostile test of the new root local proof (all coprime exponents)

Tested steps, in the order the proof's own stop boundary requests. No invalidating gap was found in this read; each item below was checked by independent derivation on the frozen text.

- **Finite construction (3)–(4).** At orders 1..7 the image of R_(7−i) under multiplication by mK^(m−1) is exactly the K^(m−1)-divisible subspace of that homogeneous degree, so a complement element is not K^(m−1)-divisible; at i=7 the image is the scalar line kK^(m−1), removing the T^(m−1) term. Orders above 7 have degree below 7(m−1). Scalar K^i components can only occur at orders 7(m−i) and are removed there. F != 0 because f_1'(R_1) has degree 7(m−1) > 3. Correct; no polynomial m-th root is assumed.
- **Budget (5).** delta < m−1 because delta >= m−1 would give K^(m−1) | F_j. Integral delta = t: K^t | F_j and non-scalarity give deg >= 7t+1. Half-integral: the three simple lines round up, giving degree >= 7delta + 3/2. Correct.
- **Margins (7)–(8).** With L <= m and n >= m+1, (m+n−1/2)/L > 2, hence N − (m+n−1/2)j/L > 1/2 and N − (m+n−1)j/L >= 2 + (m+n−1)Delta/L. Correct and genuinely uniform in (m,n).
- **Euler lemmas.** The Z^(M+t−1) coefficient of [P,Q] is M q_t' because P_X has Z-degree <= M−1 for monic P; the Euler identity then gives (Lambda − th)q_t = 0. For the second lemma, X[P,Q] = eh(nP_Z Q − mPQ_Z) is a direct substitution of the two Euler identities; Q^m = P^n and coprimality give W^m, W^n; the Y^(2m−1), Y^(2m−2) coefficients pin W = Y²+b0 provided deg U <= 2m−3, which is exactly why the proof needs delta < m−1 at a double line. Correct.
- **Simple line (Section 4).** Terms l > d are later by (m−d+l)eta > m·eta; the scalar terms are later by (m−i)(7−eta); B's whole remainder has coefficient orders >= 1, so an earlier initial has t·eta <= nu−1 and Euler factor 7n − 7nu/eta + (7/eta − 1) > 0; at n·eta the initial is monic of degree n. The chain rule [A~,B~]_(z,X) = y_z[A,B]_(y,X) with y_z a unit places the target at N + eta. Correct.
- **Double, coalesced (Section 5).** Transverse quadratic coefficient −2epsilon X^5 (K_yy/2 at y = epsilon X) is a unit; r <= j/(2L) < 7/2; (13) follows from (7). deg U <= d <= 2m−3 is exactly the e=2 lemma's threshold. Correct.
- **Double, separated (Section 6).** (15) holds for l <= d from ord c_l >= (2m−l)r > (2m−l)kappa/2 and for l > d from j >= (2m−d)r; q_l > (m−l)kappa follows summand-wise; the odd anchor gives eta <= (j+kappa/2)/(L+1/2) < j/L exactly because kappa < 2r <= j/L. y_z = y_zeta/(2zeta) has order −kappa/2, so the target order N − kappa/2 + eta is right, and (20) is strict in both parities. At any bounded order only finitely many bases and shifts contribute, so initials are polynomials. The two-sheet tuple (u+v, u−v) cannot vanish on both sheets. Correct.

**Points the independent gate should still pin (not gaps found):** (i) Euler-homogeneity of sheet coefficients after adjoining rho = sqrt(−xi) and s^(1/2), i.e., that Q_± satisfies the Euler identity with the same h; (ii) that the s-orders of all bases and shifts lie in one lattice so "initial" is well defined; (iii) the m=2 instance (delta in {0,1/2}, L in {2,3/2}), which the proof covers by the stated L bounds. None of these looks false to this seat.

**Scope hostility.** The theorem is a receiver theorem for the SPECIFIC K of degree 7 with line profile (1,1,1,2,2); it does not transfer to other rows' K (F22's corner (8,24) and length-2 chain give a different receiver). It excludes families only through a separately proved source arrow.

## 3. Hostile test of the new Astra source interface (all family parameters)

Every m-dependence named by root Card 2 is handled explicitly: the vertical Euler ODE 21m·a0'f − a0f' = f forces deg a0 = 1 and 14mA = 1 (a degree r >= 2 has leading coefficient proportional to 21mr − 7m at degree 7m+r−1); the top Jacobian coefficient 21n f'h − 21m f h' = 0 transports the SAME r0; the cubic Euler equation is literally 16b's after multiplying E by m; the bracket weight at the first opposite edge is (7(m+n)−1)(3rho+sigma) + 2rho > 0 for every m+n; the exponent map (I,J) = (3j−i, j) sends the four support inequalities to I,J >= 0, 2I+J <= 7e, I−J <= 2e; the composite determinant is g for every e; and the normalization kappa = lambda/mu, mu³ = a·lambda gives a' = 1, b' = 3/2, lambda' = 1 with lower-face scalar r(0)kappa³/mu^6 = −3/2 and c = c0 kappa² mu/(alpha beta mu^(7(m+n))). All checked by hand on the frozen text; no error found.

**First precise gap for FAMILY closure (a scope boundary the proof itself states):** the theorem's hypothesis 2 (F9 chain data with A1 = (11/7,2), i.e., a nonzero DOUBLE selected root) is supplied for q=1 only, by 16a's fixed-degree filter at D=28. For general q the source proof does not, and does not claim to, select the row. The three printed rows with the same corner (7,21) are F9, F10 (A1 = (13/7,3), a TRIPLE selected root) and F11 (k=2). Ratio (q+2):(2q+3) is incompatible with F11 in both orientations, but coincides with F10 swapped exactly when q = (j+2)/(j+1), i.e., j = 0, q = 2 (m,n) = (4,7), actual 112/196 — IF the primary family index admits j = 0 (the 0445 synthesis already uses "F16, q=0", so this must be assumed until the primary range is read). UNVERIFIED manual filter, both orientations, all 24 rows at D=28 (only F1 also admits 28 = 4M+12N, and its ratio (2j+3):(3j+4) is incompatible). The imports (GGHV Thm 2.20, Cor 7.4 with q_E = 7, Guccione–Guccione Cor 1.6/Prop 2.1) are consumed at literal statement scope as in 16a; the proof's Section 4 claim that Cor 1.6/Prop 2.1 do not assume m=3, n=5 is an import assertion the gate must read.

**Composition (not accepted, stated for planning only).** If both proofs pass their gates, the composite excludes every Keller pair whose ML-normalized standard representative is ordinary rectangular with F9(q) chain data, for EVERY q >= 0. With the manual D=28 filter above this would give: no Keller pair of actual degrees (28(q+2), 28(2q+3)) for every q != 2, and at q = 2 the single surviving stratum F10(0) with triple selected root r = (z−a)³. That is an infinite actual-degree exclusion at the imported-theorem tier, not maximum-degree coverage: for D = 7M+21N > 28 other rows (e.g., F2 at D=35) share the degree pair, and F2/F22 carry the same (m,n) formulas with different corners and receivers.

## 4. Sol's cubic marked-sheet pull-push against BINDING BD-GAL

Answers to the contract's questions, at the generic point of one irreducible branch divisor D = V(p), with g2^*D = 2R_D + U_D.

- **Is g1^*U_D = div(p∘F) automatic?** Yes. F = g2∘g1 is étale, so dg2 is invertible at every point of g1(A²); hence g1(A²) misses the whole non-étale locus R of g2, g1^*R_D = 0, and pulling back the principal divisor div_Y(p) = 2R_D + U_D gives div(p∘F) = g1^*U_D. It carries no information beyond étaleness.
- **Is the norm merely p^d1?** Yes. p ∈ C(f,g) ⊂ K, so N_(C(x,y)/K)(p∘F) = p^d1 and its divisor is d1(2R_D + U_D). The same-function pull-push is tautological.
- **Individual irreducible factors, all boundary terms.** Write p∘F = prod pi_i^(a_i) in the UFD C[x,y]. Zariski Main gives the finite degree-d1 map ĝ1: X̃ = Spec(B̃) → Y, B̃ the integral closure of B_K in C(x,y), with A² ⊂ X̃ open and boundary ∂ = X̃ − A² of pure codimension one (complement of an affine open in a normal affine surface), curves E_k. Each pi_i is a polynomial on A² but generally NOT in B̃, so it has poles along some E_k. Then div_Y N(pi_i) = f_i U_D + sum_k ord_(E_k)(pi_i)·ĝ1_*(E_k), with sum_i a_i f_i = d1. The ONLY new content is the boundary term. Its components ĝ1(E_k) are exactly the components of Jelonek's asymptotic set A(g1) of the first leg, and Z = Y − g1(A²) ⊂ A(g1) (over a point outside A(g1) the fibre has d1 distinct source points). Two cases: (a) A(g1) = Z: every boundary class lies in the BINDING injected lattice, the relation collapses to the exact identity N(pi_i) = const·p^(f_i) for every irreducible factor — a constraint, NOT a contradiction (it is consistent with p^d1 and with the fixed-sheet accounting); (b) A(g1) ⊋ Z: some ĝ1_*(E_k) is a class of a curve meeting g1(A²), outside the lattice, and no relation in Cl(Y) is controlled. In neither case does [U_D] become torsion by divisor bookkeeping. **Verdict: the mechanism is refuted as a standalone divisor-class obstruction; its residue is the row-7 statement "A(g1) = Z" (g1 proper onto its image), which is not supplied by BD-GAL.** This is a question, not a new lemma.

## 5. New cross-connection, routes, discriminator

- **Genuinely new cross-connection (two).** (i) Rows 26/30/31 ↔ 7: the boundary corrections in any first-leg pull-push are the asymptotic set A(g1) ⊇ Z of the étale leg; A(F) = g2(A(g1)) since g2 is finite. The cubic block question is therefore not orthogonal to nonproperness; it inherits row 7's unconstructed A(F). (ii) Row 1 ↔ the retired triple-root consumers (15s–15v): the all-q F9 closure meets, at q = 2, the F10(0) triple-root stratum, the same shape of problem the campaign already solved once at a different degree. No new global mechanism: **NO_NEW_MECHANISM for a complete JC2 proof or counterexample.**
- **Strongest proof route.** Independent gates of both new proofs (already active, uncharged), then Card 1: the all-q D=28 selection with the F10(0) stratum typed, then a triple-root receiver for q = 2 only if that stratum is admissible.
- **Strongest falsification route.** For the local proof, the stated controls are the right attack surface; the cheapest additional one is the m = 2, n = 3 instance (delta in {0,1/2}), since it stresses L = 3/2 and deg U = 0 or 1; no counter-object emerged in this read. For the source proof, the falsifier is an import misread: Cor 1.6/Prop 2.1 or Cor 7.4 quietly assuming a bounded exponent. The counterexample side gains nothing in this packet; rows 19/21 remain the only constructive tower and are untouched.
- **One decisive mathematical discriminator.** Read the primary family-index range and decide, both orientations, which printed rows admit D = 28 at ratio (q+2):(2q+3) for all q. Outcome A (j >= 1 only, or F10(0) inadmissible): the composite closes the entire actual-degree column 28(q+2)/28(2q+3). Outcome B (F10(0) admissible): the column closes except a single triple-root stratum at 112/196, which becomes the next exact source interface. Either outcome is decided by documentary reading, zero computation, and either changes the next synthesis.

## 6. Full 46 vector

Unchanged-default relative to the frozen current scopes, with justified changes only:

- **1 RAISE (scoped).** All four blinds; the two new proofs are precisely parameter-uniform source-family work. Not a degree farm.
- **29 RAISE (scoped).** Root, Astra, Sol; the new local proof is whole-B/one-reference Euler rigidity on a monomial-J receiver. The empty constant-J closed-H program stays stopped.
- **36 LOWER.** All four blinds; no complete unretired CE ideal exists (I_F9 is a unit; F16/F22 have no contracts).
- **31 UNCHANGED** at its 0445 scoped raise: the reverse client is finished; Sol's raise and root/Astra's lower cancel to the default.
- **5, 4, 26 UNCHANGED.** Own scoped raise of 5 withdrawn (16e's descent is consumed); Sol's lower of 4 and raise of 26 are not earned by this packet (Section 4 removes the cubic discriminator).
- All other rows UNCHANGED; **none REOPENED.** Global landing, off-axis, G2-PSC, G2-BD, cofinal type/degree, K16 all-m product and b=0 boundary retain their exact gaps.

## 7. Cards (three) and one systems decision

### Card 1 — all-q D=28 selection and the q=2 triple-root stratum (documentary; NEW reading)

- **Object/scope:** hypothetical characteristic-zero Keller pairs of actual degrees (28(q+2), 28(2q+3)), q >= 0; source ring K[u,v]; receiver k[g,p] weights (2,1). Source arrow: 15x normalizer → GGV 5.20 → GGHV Section 5 table at D = 28 → F9(q) or F10(0) → new source proof → new local proof.
- **Dependencies/overlap:** both new proofs passing their independent gates (uncharged here); accepted 15x/16a imports; own blind Card 1 item i and Section 5 caveat; root Card 2.
- **Test (20–30 minutes prose):** read the primary family-index range; filter all 24 rows, both orientations, at D = 28 and ratio (q+2):(2q+3) for symbolic q; record the row set per q.
- **Outcomes:** only F9(q) for all q → the composite is an infinite actual-degree exclusion at imported tier, record it and STOP the column; F10(0) at q=2 admissible → type the triple-root source (r = (z−a)³, A1 = (13/7,3)) as the ONE next exact interface, no builder; any other row surviving → record the exact row and stop.
- **Stop:** no q >= 2 receiver or source construction, no ideal, no solver, no AWS; stop after the filter. Continue only into a prose source-typing task for the surviving stratum.

### Card 2 — cubic sandwich: A(g1) = Z discriminator (REDESIGN of Sol Card 2; NEW question)

- **Object/scope:** A = C[f,g], normal finite-flat rank-three B_K ⊂ C[x,y], Y = Spec B_K, étale quasi-finite g1, ZMT compactification X̃ = Spec B̃, boundary curves E_k, asymptotic set A(g1).
- **Dependencies/overlap:** BINDING BD-GAL and class-lattice injection; Sol Card 2 (superseded by Section 4); row 7.
- **Test (60–90 minutes prose):** decide whether every boundary curve E_k maps into Z, equivalently whether g1 is proper onto its open image. Positive control: finite étale first leg (trivially A(g1) = ∅). Negative control: an open immersion deleting a curve (A(g1) = Z, exact identity holds, no contradiction), showing that even case (a) yields only the norm identity N(pi_i) = const·p^(f_i).
- **Outcomes:** A(g1) ⊋ Z provable in general → the divisor-class route is dead at cubic degree, STOP; A(g1) = Z provable → record the norm identity as a typed constraint and ask for the second, independent step that turns it into a contradiction before any further cubic work; undecidable in the window → INCONCLUSIVE, no launch.
- **Stop:** one branch divisor, one compactification; no classification of cubic algebras, no revival of d2 = 2, no existence claim.

### Card 3 — finite D<=35 sieve as a source-typing gate only (KNOWN, three origins)

- **Object/scope:** the 24 printed rows, both orientations, D <= 35, ratio 3:5 and the (j+2):(2j+3) rows; conditional on the printed range/completeness imports.
- **Dependencies/overlap:** Astra Card 2, root Card 3, 0445 deferral; not needed for Card 1.
- **Test (20 minutes documentary):** confirm F16(0) 99/165 and F22(1) 96/160 as the only unretired 3:5 rows at this horizon; record that F22 shares F9's (m,n) formulas but has corner (8,24), a length-2 chain and therefore a DIFFERENT receiver, so neither new proof transfers by relabeling.
- **Outcomes:** confirmed → two named necessary interfaces, no builder; missed row → extend the list; import gap → GAP. **Stop** at the list; subordinate to Card 1; no F16/F22 farm.

### Systems: ONE UPGRADE — pre-charge seat availability probe (deduplicated with Sol's receipt)

Own blind and Sol's Card 3 target the same class: seat availability and launch-window drift. Selected: a minimal non-mathematical model call before any receipt opens, failing closed within 60 seconds with a typed `SEAT_UNAVAILABLE`, its result recorded as the "seat-credit availability" field Sol's release receipt asks for. Justification from the record: the 16d/16e Fable credit failures were detected only at collection and each cost a fallback gate; this cross missed two launch windows (08:18, 08:23) for coordinator-side reasons the probe does not address and no proposal here claims to. Test: one deliberately invalid credential run and one normal run, expecting two typed outcomes and no report skeleton on failure; the existing 60-second model-child confirmation still runs after a passed probe. If maintainers judge the adapter path frozen, record NO_CHANGE with that reason. No scheduler, daemon, extra ledger, or clock change; disclosed as originating in this seat's own blind.

## 8. Continue / redesign / stop

- **Both new proofs:** CONTINUE only through their active independent gates; no composite is accepted here; retain each independently proved portion on a GAP.
- **F9 84/140, D108, 75/125, degree 99, six F2 receivers:** STOP at accepted scopes.
- **Next fixed-degree hop:** REDESIGN into Card 1; no 112/196 or F16/F22 builder before the selection is typed.
- **Cubic block:** REDESIGN into Card 2; the original pull-push is not launched.
- **K16, Witt/109, Sigray/boundary:** CONTINUE unchanged at their stated gaps; no new discriminator this round.
- **Compute/fleet:** STOP; no unretired client. Broad sweep on its own clock.

No Seal and no `charge_basis` line are authored (no new exit price is asserted). No follow-on authority is claimed. All writers of this seat are IDLE at seal.

## OPEN(S) RAISED

None. The A(g1) = Z question and the q=2 stratum are stated inside their cards; no new ledger identifier is registered.

## COLLISIONS

status: EMPTY

- NONE — this report raises no `OPEN[...]` identifier; own-only check, no corpus or protected-tree scan was performed or claimed.

<!-- BODY-END -->
