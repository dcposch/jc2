# Significant-news whole-portfolio state packet — `20260829T2254Z`

Snapshot UTC: `2026-08-29T22:54Z`  
Clean pushed basis: `0f7ee003be45ee40d51d4048897cdacf63821172`  
Objective: prove the plane Jacobian conjecture in characteristic zero or
construct and verify an explicit polynomial counterexample. A necessary
condition, abstract configuration, formal-family survivor, finite-jet kill,
or plausible proof outline is not victory.

## 0. Custody, authority, and blind boundary

Every invited researcher receives this same packet and works blind. Before
using a charged file, recompute its hash. The exact 46-avenue inventory,
scores, tried/untried state, and overlay history are in `APPROACHES.md`; read
the master table and enough history to test novelty, not only the newest
overlay. The detailed proof dependency arrows are in `ladder/REDUCTION.md`.

```text
91e9790dd409103e7f77ebf7ad5fed3e2edf2180a91d3f35b40e62bb816e1d0c
  APPROACHES.md
4a96be4e1cd43ee805a0e34cade96bd5c321c5bbeef78568d7b35e96e3f0f62a
  AUDIT.md
f947045b4d747efe89a8dadc37ab46e13985ce6099e3bbd40dbd93e3b2489083
  COORDINATION.md
b8e57d444f17028ed806bfb0f8831dcbcd0501abacb3b0375a80e3e55c42cff2
  PROGRESS.md
7f901db6c8e6fbc80c85581a331c247d5c219e23bf286136f77fbc0182ebbe1b
  ladder/REDUCTION.md
87c3821ad161bdd00477f5397138e4e3cd8bf3af7f1c965fb81e9a4aab67d13c
  notes.md
76f068f879e658a5588669da329bb6773fcd1c9abc6bb9bda70f150fabd7ae82
  xmodel/ideation-20260829T1808Z-synthesis.md
```

The newest canonical overlay and this packet supersede historical queue or
"active" language. `APPROACHES.md` is authority, not a generated dashboard.
The previous full blind/cross-pollinated synthesis is the charged `1808Z`
artifact above; use it for novelty history, but reassess every avenue.

This round is triggered by non-echo promoted and provisional K00 results plus
a proof-side scope correction. No web sweep is licensed: the broad sweep is
not due before `2026-08-30T03:40Z` absent new external news.

## 1. Significant-news delta

### 1.1 Proof side: collision is exact, strictness and selection are not

The reviewed generic quotient-collision margin is

```text
Xi = E_gen-(s-1) = d-s-sum_i b_i.
```

An Euler jump equals collision capacity minus weight excess, so atypicality
forces a quotient collision but not positive excess. A reviewed type-`(2,3)`
analytic Keller surrogate attains the collision with zero analytic-profile
excess at one interface. The proposed labelled degree-six PALF comparison
also fails before Kirby calculus: its equality germ has an `A4` genus-two
Milnor fibre, while the separately proposed passport has genus one. Bare
positivity, atypicality, or matching Euler counts therefore cannot supply
QCS. The shortest live bridge is actual polynomial/rational origin forcing
strict quotient weight, or a common labelled boundary/source complex with
the required incidence and suspension maps.

The global selector remains upstream: there is still no reviewed arrow from
an arbitrary minimal Keller counterexample to a bounded td/type/entry/U1
client, no all-td coverage theorem, and no actual-map source packet for the
conditional td12 laboratory. The long Fable `QCS-GLUE-OR-KILL` attempt hit a
64,000-output-token ceiling with no report and is an operational non-verdict,
not evidence for or against QCS. A retry must have a sharply bounded output.

### 1.2 Literal integer K00 atlas: one residual cell

Since the prior round, different-model reviews and binding integrations have
promoted exact exclusions for normalized V20R2 valuations three and five and
for the valuation-four `R4-02` child. Together with the prior valuation
results, `R4-00` is the sole literal normalized `K[[Lambda]]` residual. This
is a finite-jet source atlas, not occurrence, source completeness, an arc, a
map, or JC2.

Within `R4-00`, independent Fable, Opus, and Sol reconstructions agree that
the next-rank-zero fan has three grade-14 rays and each is killed sharply by
a raw row-six unit at grade 18. Sealed exact AWS packets for next ranks R1+,
R1-, and R2 give second-process replayed unit cofactors at grade 19. Their
central identity is

```text
Jdet_0 = -(s1/64)G1,14 -(s/64)G1,15
          -(5/256)G1,19 -(3/32)G3,19
          -(1/2)G5,19 - 4G7,19.
```

The R1/R2 packets are `PRODUCER_UNREVIEWED`; Opus hostile review is active.
Descendants may use them provisionally within speculative limits, but the
integer atlas is not promoted closed.

### 1.3 First ramified atlas and the first honest resonance

The normalized generic-K00 `e=2,m=1,B11111` cell is promoted empty at G7:

```text
V(G0,...,G7) intersect D(k10[0])
                intersect (union_i D(d_i[1])) = empty.
```

The binding corrections are the grade-two center
`mu=(s^2,st/8,16t^2,0,0,0)`, the centered variable `w=d[2]-mu`, the K10
row-two coefficient `5/65536`, and row-six typed as source sensitivity rather
than a post-G3 negative control.

A new exact corollary, still review-gated, shows that at the same `e=2,m=1`
and declared C6/K10/Jdet unit opens, each of
`k6,k2,mu2,mu4,mu6` may have arbitrary positive order or vanish identically:
all such boundary load-order faces share the empty G0--G7 prefix. Its sealed
producer is

```text
64eaafe3fe5aaa9238a338f2da045e302d9a239ac9c29312e39d5c545893d42c
  xmodel/k00-ram-e2m1-allfaces-uniformity-audit-sol56-20260829.md
```

Do not extrapolate in ramification index. The same replay refutes the naive
coefficient-blind all-`e`, `m=1` recentering induction by an exact `e=3,m=1`
rank-one fixture over `Q(i)` which solves the full literal source through G8.
The second surface coefficient is essential. G9 is undecided. The fixture is
a finite jet, not an arc, an attained source point, a map, or a counterexample.

### 1.4 Exact unloaded geometry: powerful but mixed-source gated

AWS fifth-power identities and a direct independent replay prove globally
over `Q`

```text
sqrt(r1,...,r7)
 = (d0-2d4-d4^2,
    8d1-(1+d4)d3,
    d2-d4-16d3^2,
    d5-2d3).
```

Thus an unloaded solution over a field or reduced Q-algebra lies on

```text
D(S,T)=(2S+S^2,(1+S)T/8,S+16T^2,T,S,2T).
```

All four generators have explicit fifth-power certificates in the original
seven-row ideal, and the frozen producer is

```text
96d88f49fd8e2dab1b05d18ac7912d7c1e3374fc474dcfafce29a08558edb62b
  xmodel/k00-unloaded-surface-global-radical-sol56-20260829.md
```

The producer remains under Fable hostile review. Even after review, mixed
loaded equations do not set the unloaded ideal to zero. Graph recentering is
licensed only after an unloaded coefficient block is independently isolated.
The exact normal-coordinate containment `R subset J^2` and surface load
degrees K10 cubic / K6 quadratic / K2 linear explain the resonance calendar;
they do not replace it.

### 1.5 Resource and process state

All heavy or uncertain CAS is remote. Box02 owns R2 base diagnostics and
Box03 owns R1+/R1-; the exact grade-19 packets are frozen. Fable reviews the
unloaded radical and all-`e=2,m=1` corollary and separately builds an R4
packet. Opus reviews the R1/R2 unit packets. These background jobs do not
serialize ideation or cheap descendants.

Grok returned HTTP 402 (usage balance exhausted) before the all-face review
began and is unavailable at the snapshot. Record its blind ideation lane as
`UNAVAILABLE_NO_MODEL_WORK` if the same condition recurs; do not block round
closure. Fable and Opus remain equal-standing researchers. The user-owned
formalization is independent and must never be inspected or controlled.

The latest machinery repair narrowly unignores frozen manifest-bound R4
outputs and exempts immutable remote stdout/stderr/meta receipts from
whitespace normalization. Every round still owes both a mathematical-software
idea and a separate campaign-systems upgrade/no-change check. Prefer measured
improvements to context freshness, deduplication, model/AWS utilization,
adapter reliability, custody, and report-to-ingestion latency.

## 2. Required independent whole-portfolio analysis

Think about the entire proof, disproof, mathematical-software, and campaign-
systems portfolio. Do not optimize only the avenue you previously owned.
Victory may come from a new avenue, an untried connection, a bypass of the
current architecture, or a decisive falsification of a protected ansatz.

Your report must include:

1. a compact `unchanged/raise/lower/reopen` disposition over all 46 numbered
   avenues, explaining every change;
2. a reranked list of global proof gaps and counterexample/falsification gaps;
3. at least one genuinely new avenue or mechanism and one new connection
   between existing avenues, each checked against repository history;
4. the strongest prove/falsify/bypass portfolio for the global selector and
   the polynomial-origin QCS strictness or boundary-complex bridge;
5. the strongest next K00 attack, explicitly comparing R4-00 closure, the
   `e=3,m=1,G9` carry, general ramification/load calendars, source
   completeness, all-order lifting, reachability, and algebraization;
6. one mathematical-software acceleration or decisive experiment;
7. a separate campaign-systems `UPGRADE` card with the smallest measurable
   acceptance test, or `NO_CHANGE` with evidence;
8. at most three detailed research cards and one ranked launch portfolio.

Each card must state novelty (`NEW`, `KNOWN`, `DUPLICATE`, or
`SCOPE-CONFLICT`), dependencies, exact theorem or experiment, cheapest
discriminator, meanings of every outcome, stop condition, expected
information gain, compute class, and parallel prove/falsify/bypass ownership.
Identify provisional descendants that may start without waiting for review;
promotion still requires different-model hostile review.

Search history before claiming novelty. Preserve arrow directions and
quantifiers. Keep `G2-PSC` and `G2-BD` disambiguated. Keep B and S separate.
Formal data are not maps; a floor or survivor is not attainment; equality of
sets is not equality of schemes. A finite jet is not a formal arc, algebraic
germ, polynomial map, counterexample, or JC2 result.

## 3. Tool and output boundary

No web or external messages. Never read, list, search, stat, build, modify,
or otherwise touch `jc2-lean`. Desk-scale exact algebra is allowed only under
60 CPU seconds and 1 GiB RSS per process. Heavy or uncertain CAS is AWS-only
and must be preregistered rather than launched during this blind report.

Do not edit canonical files, commit, push, or read another blind submission,
prompt, log, or run record from this round. Write only the assigned report
plus scratch under `/tmp`. No new exit price is expected; absent an actual
new typed price assertion, omit any exit-price declaration. End with a
standalone `<!-- BODY-END -->`; do not add a seal. Fail closed.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `10764`.
- Body SHA-256:
  `7a5441f090607accbbc80c38c6a703d06d8986cb8dbbe0721e099e756c52dfe5`.
- Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`.
