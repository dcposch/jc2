# f10-r2-semantic-ready-result-gate-fable5-20260910 — FIRST Fable 5.1 review of the actual semantic 6/10 result and the coefficient-packet promotion gate

status: UNSEALED (adapter seals; no Seal, no charge_basis authored)
lane: f10-r2-semantic-ready-result-gate-fable5-20260910
reviewer: Fable 5.1 (claude-fable-5-1), FIRST different-model review of the ROOT/Astra execution report
started: first model action 2026-09-10 16:49:33 UTC (lane runtime began before it); deadline min(ROOT unit launch + 18 min, 17:10:00 UTC), bounded here as no later than 17:07:33 UTC; reserve from 17:04:30 UTC; never reset
mode: documentary intake; manual text/hash reading only; zero subprocess/import/AST/syntax/compile/test/CAS; artifact.json, mutated6.json, mutated10.json HASHONLY plus byte count (never opened)
writes: apply_patch only; owned targets xmodel/f10-r2-semantic-ready-result-gate-fable5-20260910.md and box/f10-r2-semantic-ready-result-gate-fable5-20260910/input_custody.md

## 0. Custody, read scope, disclosure

All 24 snapshots under /tmp/jc2-lane.oGDQXV/inputs were hashed with sha256sum and counted with wc -c at 16:49:46 UTC BEFORE any body read; all 24 digests equal the charged list byte for byte. The rows live in box/f10-r2-semantic-ready-result-gate-fable5-20260910/input_custody.md and were generated by piping sha256sum and wc -c output into apply_patch; no digest in this body is typed from memory, every one is copied from that output or from the charged text files. Both owned targets were absent at 16:49:33 UTC; the skeleton was created at 16:53:25 UTC by apply_patch. Read WHOLE at 16:50-16:52 UTC, each read ending at the file's last line with no clipping: the ROOT report (to its Seal block), the parent actual-result gate (to its marker), the semantic code gate (to its marker), CUSTODY.json (28308 bytes, one line, read through fold, recovered whole), ROOT-REGISTRATION.json, mutate.receipt.json, semantic-binding.json, check6.stderr, check10.stderr, and the twelve mutate/check6/check10 pre, post, telemetry and python-live records. HASHONLY plus byte count, never opened, parsed or executed: artifact.json (dac655a7…, 1652675 bytes), mutated6.json (b70af8a2…, 1652232 bytes), mutated10.json (f89081b7…, 1652675 bytes). No provenance link, corpus, Git, network, AWS, SSH, agent, other lane, live, shared or protected file was touched; no input was edited; no Python helper, subprocess, import, AST, syntax, compile, test or CAS ran.

Disclosure of prior same-model involvement: the parent actual-result gate (3bce5121…) and the semantic code gate (469fd1c7…) were authored by this same model, Fable 5.1, in earlier sessions with no continuity into this one. They are consumed as accepted imports at their stated scope: the parent gate's positive-baseline acceptance and its withholding of promotion, and the code gate's acceptance of the unchanged mutator, dispatcher and its correction of the control-10 rejection line (376, whole-polynomial comparison, no array-length change). Every mutator, dispatcher and check.py line number cited below is taken from those gates; no code file is charged here. The ROOT report is a different author (ROOT/Astra). This is one FIRST different-model review of the ROOT evidence, not a rerun licence and not a duplicate source review.

## 1. Verdict table

| item | verdict |
|---|---|
| A binding of the three actual files and the baseline | CONFIRMED; frozen 0444/0555 modes, runner PIDs and the guard at mutator line 207 are ROOT attestation or accepted-code inference, not charged bytes |
| B registered argv, live identity, custody order | CONFIRMED for the three charged pre records literally; the other nine pre records are ROOT attestation; native manifest is fresh-derivation attestation consistent with all pins |
| C two rejections, both mathematical | CONFIRMED; control 6 at check.py 329/216, control 10 at check.py 376 then check_arithmetic.py 208; the Psi path satisfies the named risk as corrected by the code gate; the K1/K0 branch at 383 was not exercised |
| D promotion | CONFIRMED: the specific semantic-negative-control gap is discharged for this baseline; smallest genuine blocking gap: none |
| E scope | CONFIRMED as stated: discriminatingly checked r2 reconstruction coefficients under the accepted 19-row/32-residual/80-slot interface only |

## A. Binding of the three actual files and the positive baseline

Local identity of the three actual files. My sha256sum of mutated6.json is b70af8a2…6eca5 at 1652232 bytes; of mutated10.json f89081b7…86a0ab at 1652675 bytes; of mutate.receipt.json 4f851182…37990 at 1665 bytes. The receipt's outputs record exactly those two digests and byte counts (control 6: 1652232, control 10: 1652675). CUSTODY.files lists frozen/mutated6.json, frozen/mutated10.json and frozen/mutate.receipt.json with the same three digests and byte counts, and semantic-binding.json at 1515 bytes with my local digest f634a181…. check6.pre and check6.post both carry input_sha256 b70af8a2…; check10.pre and check10.post both carry f89081b7…; the binding's two outcome records and CUSTODY.result.outcomes carry the same two input digests, and CUSTODY.result.receipt_sha256 and the binding's receipt_sha256 equal 4f851182…. So the bytes the two checkers read before and after their runs are the bytes charged here, and the receipt that describes them is the frozen receipt CUSTODY pins.

Unique changed inputs. The three digests dac655a7…, b70af8a2… and f89081b7… are pairwise distinct, so each fixture differs from the baseline and from the other fixture (the code gate's lines 259-261 require this inside the mutator; here it is visible in the digests themselves). Byte counts are consistent with the two edits as the receipt states them: control 6 replaces a three-term subtree by the empty list and is 443 bytes shorter; control 10 replaces one rational string by another of the same length and keeps 1652675 bytes.

Positive baseline. The receipt's input record is path metadata/artifact.json, 1652675 bytes, dac655a7…; my local artifact.json is dac655a7… at 1652675 bytes; ROOT-REGISTRATION pins that path to dac655a7…; mutate.pre and mutate.post carry input_sha256 dac655a7…; CUSTODY.runs[mutate].input_sha256, CUSTODY.result.baseline_sha256 and the binding's baseline_sha256 are dac655a7…. This is the identical digest the parent gate accepted as the positively checked packet (its check.pre/post input and its CUSTODY entry). The baseline body was never read here; the digest proves identity, not content.

Whole-inverse restoration and canonical wire. The receipt has canonical_wire true, inverse_edit_restores_whole_input_bytes true and checker_run false with status TWO_FIXTURES_WRITTEN_UNCHECKED. Under the accepted mutator these flags restate `need` assertions at lines 202 (baseline byte-canonical), 213 (control 6 inverse) and 257 (control 10 inverse), so the mathematical meaning of "restores whole input bytes" comes from the accepted code and the mutator's exit 0, not from the booleans alone. Control 6 record: mutation "bands[6].B_var := []", gap "7", old_terms 3, old_subtree_sha256 b606787b…; that is the three-term gap-7 mate deleted to the canonical empty sparse encoding, and the mutator's exit 0 implies its line-207 guard (unit vartheta-squared term present) passed, an accepted-code inference. Control 10 record: family Psi, row 0, term 0, basis 5, exponents ["0","0","0","1","0","0"], fallback null, term_deleted false, old "636435903631458235552625/373375762584", new "636435903631831611315209/373375762584". Both strings are in the canonical numerator/denominator form with the same positive denominator and equal numerator length; the +1 relation is the accepted mutator's line 233 (`wire(rational(old) + 1)`), which I do not recompute and treat as receipt metadata, exactly as charged. fallback null and term_deleted false mean no fallback constant was added and no term deleted, so this is an in-place coordinate change of an existing nonzero Psi coordinate.

Exact source, ROOT, native, authority, job-tag identities. The receipt's sources map pins check.py e6bc7cca…, check_arithmetic.py e5dd4bcb… and mutate_controls.py a896a809…, equal to the registration pins, to the mutator's own --self-sha256 argv value, to CUSTODY.result.mutator_sha256 and to the binding. The five science pins (authority.py 2a274ac4…, arithmetic.py fe9ab1ab…, produce.py eeb3fe3b…, check_arithmetic.py e5dd4bcb…, check.py e6bc7cca…) are identical in the registration, in all twelve CUSTODY run records, in CUSTODY.result and the binding, in all three pre and post records, and equal the set the parent gate bound to the positive run. The receipt's root_registration_sha256 0d0b0063…, my local digest of ROOT-REGISTRATION.json, mutate.pre root_sha256, the expanded argv slot, CUSTODY.root_sha256, CUSTODY.result.root_registration_sha256 and the binding agree. Native acbf656e… is the native_sha256 of all twelve runs and of all three pre/post pairs and the registration's native-manifest pin. The authority digest 75327bcf… is pre, supplied and post for mutate, check6 and check10 and is the CUSTODY authority/mutate.json, check6.json, check10.json entry (806 bytes each). Job tags: the mutator runs under f10-r2-semantic-controls6-10-v1 (registration jobtag, argv, receipt job_tag); both checkers run under f10-r2-reconstruction-v1, the same registered checker tag as the positive run. ROOT-attestation items in this section: root 0444 fixtures inside a 0555 frozen directory (no charged JSON carries modes), the 274 local pins and fsync, and the dispatcher's own receipt parsing; the packet is consistent with them.

## B. Registered commands, live identity, custody order

Literal argv match. I compared element by element the argv of mutate.pre, check6.pre and check10.pre against ROOT-REGISTRATION.commands. The mutate vector is identical except for the single slot after `--root-registration-sha256`, where the registered token ROOT_REGISTRATION_SHA256 is replaced by 0d0b0063…, the digest of the registration itself; the token appears nowhere else and in no other command. The check6 and check10 vectors are identical to their registered lists with no token. Each vector is CAPRUN (python3.12 -I -S -B run_capped.py) with the registered profile, --rss-sample-seconds 0.05, --term-grace-seconds 1, the three stream/telemetry paths, --cwd science, then setpriv --reuid 65534 --regid 65534 --clear-groups --no-new-privs, then the child. Child flags: the checkers use python3.12 -E -s -S -B check.py with --registered-job f10-r2-reconstruction-v1, --authorization authority/check6.json or check10.json, --authorization-sha256 75327bcf… and --input frozen/mutated6.json or frozen/mutated10.json; the isolated mutator uses python3.12 -I -S -B mutate_controls.py with the nineteen-element vector the code gate describes (job tag, root pin, self pin, checker, checker-arithmetic, input, output6, output10, receipt). Telemetry argv_count 31 for mutate and 22 for each checker equals the length of the vector after CAPRUN's own arguments (eight setpriv elements plus 23 or 14 child elements, counted). The python_argv field of each pre record equals the argv in the matching python-live record.

Live identity. mutate.python-live: pid 8492 = pgid 8492, start_ticks 42449, uid and gid quadruples 65534, NoNewPrivs 1, CapEff, CapPrm and CapInh all zero, boot 17eb5153…, pid namespace pid:[4026531836], cgroup 0::/system.slice/jc2-f10-r2-controls-ready-20260910T1620.service, Max cpu time 9/10, Max file size 15728640, core 0/0, mapped files exactly the interpreter, loader, libc, libexpat, libm and libz. check6.python-live: 8504 = 8504, ticks 42580, cpu 169/170; check10.python-live: 8579 = 8579, ticks 43049, cpu 169/170; otherwise the same identity fields. Each telemetry start_identity carries the same boot and the same start_ticks (42449, 42580, 43049) and the same pid = pgid. The parent recorded in each pre is pid 8424 = pgid 8424 with root uid, full CapEff and NoNewPrivs 0, the same boot, namespace and cgroup, matching the registration's boot_id, pid_namespace, cgroup_path, uid/gid 65534 and the ROOT report's SYSTEM Main PID 8424. Runner PIDs 8491/8503/8578 live in the uncharged runner-live records and are ROOT attestation.

CUSTODY order and flags. CUSTODY.runs holds exactly twelve records in the registered order refuse-status, refuse-caps, refuse-inventory, refuse-source, refuse-hash, valid, startup-produce, startup-check, dummy, mutate, check6, check10, each label once, timestamps 16:23:23.716 to 16:23:47.156 UTC, every record with runner_live_observed true, actual_python_live_observed true and owned_group_quiet true, resource null everywhere except dummy (rss, RESOURCE_CAP), four distinct refusal authority digests for refuse-status, refuse-caps, refuse-inventory and refuse-source, and 75327bcf… for the other eight records (refuse-hash reuses the valid authority with an all-zero registered CLI pin, as the parent gate describes). CUSTODY.files lists 128 entries: nine per run (pre, post, python-live, runner-live, runner stdout/stderr, child stdout/stderr, telemetry), outer.stdout and outer.stderr at 0 bytes, semantic-binding.json, two writer payloads, three frozen files and twelve authority files; counted by hand. The post record utc of mutate, check6 and check10 equals the utc of the matching CUSTODY run record, and each post's telemetry_sha256 equals the run record's and my local telemetry digest (f80a9612…, 5ac9ce90…, 09f85d71…). ROOT's statement that all twelve pre argv matched the registration is verified here for three and is attestation for the other nine, whose pre bodies are uncharged and are not described here. Native acbf656e… (1226 files, 91 directories, 31 aliases per ROOT) is a fresh-derivation attestation consistent with every pin; it is not an atomic mapped-image proof, and it differs from the positive run's native digest quoted by the parent gate (1e08bbdd…), which is a different worker's closure and is not charged here. Limits: registration LIMITS 600 wall, 500 CPU, 2147483648 memory, 16777216 aggregate; profiles control [5,3,2 GiB], dummy [5,3,32 MiB], mutate [10,9,2 GiB], check [210,169,2 GiB]; the telemetry caps of the three charged runs equal their profiles; nothing was raised or reset.

## C. The two rejections

Exit shape. mutate.telemetry: NORMAL_EXIT, child_exit_code 0, child_returncode 0, child_signal null, runner_exit_code 0, resource null, error null, kill_sent and term_sent false, stdout 0 bytes and stderr 0 bytes (both the empty-input digest e3b0c442…), wall 0.514077927 s, max group RSS 33918976, 16:23:30.700817 to 16:23:31.214887 UTC; so the mutator's 10 s/9 s profile, unmeasured in the code gate, is now measured at about half a second. check6.telemetry: NORMAL_EXIT, child 1/1, signal null, runner_exit_code 1, resource null, error null, stdout 0 bytes, stderr 652 bytes with digest ce039446…, wall 4.057132479 s, RSS 26853376, 16:23:32.016268 to 16:23:36.073396. check10.telemetry: NORMAL_EXIT, child 1/1, signal null, runner 1, resource null, error null, stdout 0 bytes, stderr 647 bytes with digest 93a19334…, wall 10.234767187 s, RSS 30916608, 16:23:36.699753 to 16:23:46.934511. My local digests and byte counts of check6.stderr and check10.stderr equal those telemetry fields and the binding's stderr_sha256 values. Both stdout files are empty: no CHECKED string was printed. The ROOT table reproduces these figures exactly.

Control 6 stderr, read whole (652 bytes). Four frames: check.py line 416 in module, `main()`; line 412 in main, `print(check_file(path))` with a caret line; line 329 in check_file, `col = residual_pair(sub(oper(h, avar, vvar), tvar))` with a caret line; line 216 in residual_pair, `raise ValueError('upper band residual')`; final line `ValueError: upper band residual`. This is the branch the parent gate predicted for zeroing the gap-7 mate and the branch the code gate traced through the unchanged check.py: with B_var empty, the h=7 operator pair reduces to the surviving upper target and residual_pair rejects it. Line 329 is inside the band loop after the earlier gaps, after authority (authority.py refusals), after the parse hooks and bounds (check.py 21-63, 81), so this is a rejection by the band arithmetic, not by the parser, the authority pins, an early exit, a cap or a signal.

Control 10 stderr, read whole (647 bytes). Frames: 416 `main()`; 412 `print(check_file(path))` with a caret line; 376 in check_file, `eq([poly(v) for v in array(doc['rows']['Psi'], 3)], psis, 'all three Psi slots')` with no caret line, which is legitimate CPython formatting when the failing expression spans the whole statement; check_arithmetic.py line 208 in require, `raise ValueError('CHECK FAILED: ' + label)`; final line `ValueError: CHECK FAILED: all three Psi slots`. Line 376 is reached only after every band check and the Jacobian block (code gate D: rows are first parsed at 376-378), and psis is the checker's own reconstruction, so the failure is the whole-polynomial comparison of the packet's Psi rows against the reconstructed Psi: a mathematical rejection at the deliverable layer, not a parser, authority, early or cap failure. The parent gate had predicted 383 with 376 "only if an array length changed"; the code gate corrected that: 376 compares whole polynomials, so an in-place change to any Psi coordinate fails there with no length change. The observed path is exactly that corrected prediction, and the accepted mutator's selection landing in Psi row 0 (family order Psi, K1, K0; first strictly larger absolute value) is why 376 rather than 383 fired.

Does the Psi path satisfy the named risk? The parent gate's named risk was that the deliverable rows are tied to the reconstruction only by the literal comparison, together with the vacuity probe for a zero-row packet. Control 10 shows that a one-unit change in an actual nonzero deliverable coordinate is rejected by that comparison under the same check.py that accepted the baseline, and fallback null shows the mutator found a nonzero retained coordinate, so the all-empty-rows degeneracy is excluded for the selected row. What it does not show: the K1/K0 comparison branch at 383 was never exercised, and nothing here censuses which of the other retained rows are nonzero (section E). Neither was required by the parent gate's design, which accepted either branch.

Consistency with the three pre/post pairs and custody: each pre and post pair carries the same input digest, authorization 75327bcf…, native acbf656e…, root 0d0b0063… and the five science pins; each post's status NORMAL_EXIT and telemetry digest equal the CUSTODY run record; the binding's outcome reasons are the two final traceback lines verbatim, with stdout_bytes 0. Joint 24.337646631999974 s wall and 25.085507 s cgroup CPU (binding and CUSTODY.result agree) cover all twelve calls plus dispatcher overhead and are far inside 600/500; the span from the ROOT-attested unit start 16:23:22.80 to the check10 post 16:23:47.156 is consistent with that wall figure. These are measurements, not billing, tokens or credits.

## D. Promotion decision

The gap as the parent gate stated it: the only checker behaviours ever observed were one accept and two WRONG-JOB refusals that exit before the arithmetic import, so the accept of dac655a7… could not be distinguished from a vacuous accept, and promotion of the packet as discriminatingly checked coefficients was withheld pending a semantic rejection of a changed object. The evidence now charged: the same check.py (e6bc7cca…, with the same arithmetic and check_arithmetic pins) under the accepted dispatcher family, invoked with the same child flags and the same registered checker tag, rejected two objects that differ from the baseline by one verified semantic edit each, one at the band-arithmetic layer (329/216) and one at the deliverable-row comparison (376/208), while the parent-accepted run of the same code on the baseline printed the CHECKED string. Same code, three inputs, opposite verdicts on the two changed ones: that is the discrimination the gap asked for. The edits are tied to the baseline by the accepted mutator's whole-byte inverse restoration and by the receipt's baseline digest, and the rejected bytes are tied to the checker calls by the pre and post input digests. Both selected controls actually ran; both are typed NORMAL_EXIT with child exit 1, no cap, no signal, no error, empty stdout.

Lifecycle change justified: the packet dac655a7… moves from CHECKED_BUT_NOT_PROMOTED to PROMOTED as discriminatingly checked r2 reconstruction coefficients under the accepted source contract, citable as "formed by produce.py eeb3fe3b…, accepted by check.py e6bc7cca…, and discriminated by the same check.py on controls 6 and 10 (b70af8a2…, f89081b7…) under the semantic-controls registration 0d0b0063…". No requirement is added for the thirteen remaining controls or for a positive producer/checker rerun: no new doubt about the checker or the packet arose in this review, and the positive acceptance is an accepted import, not repeated here. No new computation was run and no broad audit was made.

Smallest genuine blocking gap: none. The items that remain are limits, not blocks, and none licenses hardening: (1) the K1/K0 comparison branch at 383 is unobserved, but the parent gate's control-10 design accepted either branch; (2) frozen modes, the nine uncharged pre bodies, runner PIDs, the fresh native derivation, the 274 local pins, fsync, termination and timers are ROOT attestations, each consistent with the charged bytes and none load-bearing for the discrimination inference, which rests on the pinned code and the pre/post input digests; (3) the interpreter build identity across the positive run and this run is not established by charged bytes (this registration pins python3.12 a92f0f95…, the positive run's registration is uncharged), but the checker is stdlib-only pure Python whose verdict is fixed by language semantics, so this does not touch the inference.

## E. Scope of what is promoted

What promotion means. The packet dac655a7… is a set of r2 reconstruction coefficients that the unchanged checker accepts under its static contract and rejects when a gap-7 mate is removed or when its largest retained Psi coordinate is shifted by one, under the accepted 19-row, 32-residual-slot, 80-Jacobian-slot interface. That is the whole claim.

What it does not mean. No actual integrality at 89, no highest-regularity statement, no unit, no rank, no 35-element basis, no source point, no source zero or nonzero, no all-r extension, no degree-frontier exclusion and no JC2 conclusion; the verdict strings, CUSTODY.result.science_outcome NONE and the binding say so literally, and neither control run touches any of those. The captured selection (Psi row 0, term 0, basis 5 nonzero) shows one nonzero retained coordinate; it does not census the other rows, so the interface's empty-list degeneracy is excluded only for that row. Controls 6 and 10 address the two named checker-discrimination risks (band layer, deliverable layer); they do not establish universal bug-freedom of check.py, and the thirteen designed controls that did not run remain designed, not observed. ROOT-attestation limits and the honest measurements are as itemized in A-D: joint 24.337646631999974 s wall and 25.085507 s cgroup CPU are the batch's measured cost, not billing, tokens or credits.

## F. OPEN(S) RAISED, COLLISIONS, WHOLE check, attestation

OPEN(S) RAISED: none. No new canonical OPEN; the queued next-tier actual highest/full-unit tests remain uncompleted work, not new claims.

COLLISIONS: status EMPTY. Own-only check: this lane wrote exactly xmodel/f10-r2-semantic-ready-result-gate-fable5-20260910.md and box/f10-r2-semantic-ready-result-gate-fable5-20260910/input_custody.md, both absent at 16:49:33 UTC; no corpus scan; no other file touched.

WHOLE check: 21 objects read whole as itemized in section 0, three HASHONLY; this body was reread by full cat before the marker and no placeholder remains. Authoring: every byte of both owned files was written by apply_patch (custody rows piped from sha256sum and wc -c output into apply_patch's stdin); no Write/Edit tool, no shell redirection into any file, no helper, no source or input edit, no Seal, no charge_basis, no execution, import, AST, syntax, compile, test, CAS, network, AWS, SSH, Git, agents, corpus, shared, protected, other-lane or live file. Timeline: hashes 16:49:46, absence 16:49:33, skeleton 16:53:25, whole reads 16:50-16:52, sections 0-A 16:54, B-C 16:55, D-F and this attestation before the 17:04:30 reserve, deadline bounded at 17:07:33 UTC, never reset. The standalone marker below is the only such line in this file and nothing follows it.

<!-- BODY-END -->
