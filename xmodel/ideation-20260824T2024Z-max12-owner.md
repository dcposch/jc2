# Blind whole-portfolio ideation — maximum-12 owner lane

Cutoff: `2026-08-24T20:24:00Z`  
Charged basis: `2e6104a417cfe15a93a901aa0a9129094a2ae11b`  
Status: **independent blind submission / not degraded**.  I read no sibling
`T2024Z` submission and saw no sibling snippet.  Repository searches used the
exact root-relative exclusion

```text
--glob '!xmodel/ideation-20260824T2024Z-*'
```

This report is sealed to the packet cutoff.  In particular, it does not
consume any later freeze or review outcome for the provisional parity descent,
TD6 `c1` line, corrected AS rows, or any other post-cutoff event.

## 0. Executive decision

No proof or counterexample to JC2 exists at this cutoff.  The reviewed
maximum-eleven theorem is unchanged.  The strongest new *confirmed* local fact
is the full-absorption kill on the `(9,12)` order-three
`k=mu=0, nu!=0` leaf; the strongest new *provisional* fact is the genus-five
parity descent.  Neither closes the loaded fibre, a maximum-twelve cell, or a
global reduction.

The fastest next proof discriminator is not another coefficient rectangle.
The quadratic Wronskian already gives the complete ramification budget of

```text
beta = g^3/f^4.
```

After the two three-point passports are removed, three codimension-one
remainders are genuine four-point Hurwitz problems.  Their reduced Nielsen
classes are finite sets carrying a braid action, so each component has an
exact compactified Hurwitz-curve genus.  The fourth critical value is unique
and Kummer-invariant on each of these strata, hence lies in `C(x)`.  If every
relevant Hurwitz component has positive genus, the `x`-trajectory is constant;
the branch then returns to an isotrivial scaling/twist calculation and the
terminal row.  Genus zero instead produces an explicit rational family to
reconstruct.  This is Card 1, the strongest new proof attack and software
experiment.

The new cross-avenue mechanism is therefore:

```text
exact coefficient fibre
  -> labelled critical-value collision passport
  -> reduced braid orbit / Hurwitz curve
  -> rational moduli map from P1_x
  -> genus constancy or explicit genus-zero parameter
  -> terminal ODE and original Taylor boundaries.
```

It joins the exact passport lane (Avenue 25), the function-field genus
rigidity that succeeded in the cube trajectory (a narrowly useful piece of
Avenue 22), and source-honest coefficient reconstruction.  It is materially
stronger than a bare Riemann-existence census, which only proves that a cover
exists.

A second new transfer takes the projectivized-direction discipline of the
maximum-12 Kuranishi work to TD6 without claiming the spaces are analogous.
Once the provisional `c1` line is reviewed, parameterize every common-center
triple by a line through the licensed base center and eliminate the line
parameter with every stagewise pivot stratum retained.  The projection to the
direction plane either proves a finite center cover, returns a proper exact
exceptional locus, or falsifies the attempted widening immediately.  This is
Card 2.

The strongest counterexample attack remains the already-known, corrected AS
accepted-digit recursion on the original nonreduced predecessor scheme.  It
is Card 3 only so that its exact decision contract is visible; no novelty is
claimed for it.  The divided integer carry and embedded component are
load-bearing.  Raw survivor counts, a radical replacement, or an opaque
`UNSAT` are not evidence.

## 1. Cutoff evidence and scope firewall

| Input at cutoff | Lifecycle/evidence used here | Exact consequence permitted | Forbidden overread |
|---|---|---|---|
| maximum actual partial `y`-degree `<=11` | different-model confirmed theorem | maximum twelve has primitive cells `(8,12)` and `(9,12)` | no total-degree or JC2 theorem |
| order-three `k=mu=nu=0` DZ20 face | different-model confirmed | that one nontrivial-Kummer trajectory is empty | no loaded or order-one exclusion |
| complete `B` ramification absorbed by `W=0` | different-model confirmed, review SHA `6d34908cbd2ead9769e4090fcab903ec5d5b7bd9db485db1d681708c5d36946e` | passports `(18,2,2,1^14)` and `(18,3,1^15)` have no trajectory | no one-collision, no-collision, double-off-`W`, or equal-non-1 kill |
| parity genus-five descent | producer/coordinator exact but explicitly provisional in the packet | high-priority mechanism candidate and regression target only | no theorem, no whole loaded-fibre claim |
| TD6 complete licensed `c1` line | producer exact but not yet review-promoted | Card 2 may be designed conditionally | no center block, neighborhood, SP-2, or TD6 class kill |
| corrected AS post-D10 rows | source comparison/freeze/review incomplete | continue source-honest compiler preparation | old counts are retracted; no survivor or emptiness theorem |
| Makar-Limanov shape/mate intake | conditional primary-source statements | provenance-bearing row checker is licensed | no GGV/Sigray/max-partial bridge or mate-existence oracle |

Automatic Keller coprimality/squarefreeness is retained as a trajectory
firewall.  Projected common-factor components are elimination artifacts unless
an exact source calculation says otherwise.

## 2. All-46 disposition

`U` means unchanged from the charged canonical table.  A scoped raise changes
only the named client or instrument, not the global promise of the avenue.

| # | Disposition | Cutoff reason |
|---:|---|---|
| 1 | U | Maximum eleven is closed, but maximum twelve and the all-degree GGV farm remain unbounded in `x`/total degree. |
| 2 | U | A provisional center line and one loaded partial-degree face do not repair `G2-PSC`, `G2-BD`, full landing, off-axis coverage, or a `td` ceiling. |
| 3 | **raise, scoped instrument** | The exact Wronskian/terminal rows now feed a finite critical-value and genus calculation on named maximum-12 strata.  This is not an extension of the general strip theorem. |
| 4 | U | Neither a four-point Hurwitz family nor finite AS digits supplies an inverse system, typed source germ, or algebraization theorem. |
| 5 | U | Scaling/isotriviality of one cover is not a Jung--van der Kulk degree drop for a Keller pair. |
| 6 | U | No asymptotic component has been proved one-place. |
| 7 | U | The spectral critical values are coefficient-fibre data, not a construction of `A(F)` or a cross-fibre correspondence. |
| 8 | U | No uniform formal-inverse truncation bound is obtained. |
| 9 | U | No finite reduction of Conjecture E is obtained. |
| 10 | U | The exact `NO LEVERAGE` HC4 gate is unaffected. |
| 11 | U | The refuted stronger-conjecture ladder remains closed. |
| 12 | U | Kummer congruences and branch divisors are client-specific, not a general multinomial face-isolation theorem. |
| 13 | U | No new DC(2) normal form or degree-four noncommutative client appears. |
| 14 | U | No candidate nonsurjective `End(A_1)` endomorphism appears; the unrun T2 audit remains defensive reconstruction. |
| 15 | U | “Spectral norm” here supplies no commuting-PDO dictionary. |
| 16 | U | No holonomic index or D-module receiver is named. |
| 17 | U | The order-three character does not recognize a plane-origin subclass after cubic stabilization. |
| 18 | U | Kummer weights organize one exact branch but do not make a hypothetical counterexample globally graded. |
| 19 | U / **redesign active compiler** | The old post-D10 counts are retracted; only the corrected integer-source, nonreduced next-carry recursion remains licensed.  This changes execution, not the avenue's global rank. |
| 20 | U | Cartier arithmetic does not restore the false p-curvature/JC bridge. |
| 21 | U | Maximum-eleven raises the conditional AS degree floor, but Hensel still does not kill `A_infinity` or descend a rational deck action. |
| 22 | **reopen, technique only** | The provisional parity cover and the confirmed earlier elliptic/cusp exclusions show that a coefficient trajectory can be killed by the genus of an explicit fixed curve.  Card 1 tests the same technique on exact Hurwitz curves; no Siegel theorem for unknown fibres is claimed. |
| 23 | U | No metric completeness, properness, or holomorphic-inverse input appears. |
| 24 | U | Pinchuk's real maps remain unrelated to the quarantined quasi-polynomial source. |
| 25 | **raise, exact client** | The remaining codimension-one loaded strata have three explicit four-point passports derived from the exact quadratic Wronskian.  Unlike the old generous passport census, they carry a rational trajectory and a computable reduced braid action. |
| 26 | U | A degree-36 Nielsen computation does not bound primitive monodromy or topological degree globally. |
| 27 | U | No Sigray splice/tree realizability theorem follows from the Hurwitz calculation. |
| 28 | U | No log-Chern sign or completed boundary divisor is supplied. |
| 29 | U | A one-variable Wronskian is not a universal receiver for the Hamiltonian cokernel. |
| 30 | U | No embedding-sensitive affine-surface invariant is obtained. |
| 31 | U | Superelliptic integrality inside a coefficient quotient does not prove `C[x,y]` integral over `C[P,Q]`. |
| 32 | U | Neither card makes the global off-diagonal ideal a unit; injectivity remains the endpoint. |
| 33 | U | The spectral differential is a client identity, not a nonautomatic global action residue. |
| 34 | U | No rational pole-removal theorem is obtained. |
| 35 | U | No higher-dimensional counterexample descends to the plane. |
| 36 | U | Generic sparse/SAT search remains low.  Card 1 is a finite source-derived Hurwitz compiler, not a reopening of random coefficient search. |
| 37 | U | Finite-field extras still do not predict liftability. |
| 38 | U | Kummer weights and collision strata do not supply a saturation-safe tropical obstruction. |
| 39 | U | Genus constancy and Cartier carries do not give a universal cohomological vanishing theorem. |
| 40 | U | Constant commutative Jacobian still does not imply a free-associative lift. |
| 41 | U | The higher-dimensional deformation countercontrol remains decisive. |
| 42 | U | A Hurwitz cover is not a Hurwitz-stable vector field. |
| 43 | U | Common-power/Faber structure gives no new Ritt decomposition theorem. |
| 44 | U | The no-prime proof remains unusable; the present cover degree is composite. |
| 45 | U | The terminal ODE is explicit already; differential Galois language adds no inverse theorem. |
| 46 | **raise, infrastructure only** | Reduced-Nielsen braid actions, Hurwitz-curve genus certificates, projective direction atlases, and integral carry quotients admit a common exact manifest/replay format.  Certification still cannot manufacture coverage. |

The four changed rows are `3,22,25,46`.  Row 19 is an execution redesign
forced by the packet's correction, not a promise change.

## 3. Reranked bottlenecks and attacks

### 3.1 Proof side

1. **Fastest bounded theorem target — loaded `(9,12)` order-three fibre.**
   Classify the one-collision, double-`B`-off-`W`, and equal-non-1
   four-point strata by Card 1; then attack the genuine five-point/no-collision
   open with the character quotient.  Preserve both Taylor families.
2. **Highest-centrality finite proof target — TD6 moduli coverage.**  A
   reviewed empty `c1` line would still leave two center directions,
   dead stretch, boundary, F1, pole, other terminal classes, and global
   landing.  Card 2 is a fail-closed test of whether the center block admits a
   finite exact projective cover.
3. **Remaining maximum-12 branches.**  Nonzero `k,mu`, the polynomial core,
   order one, every `(8,12)` Kummer order, and all original boundaries remain
   charged.  No result on the selected order-three leaf orders these away.
4. **Global wall.**  Even a maximum-twelve theorem leaves the pure-Sigray
   full-landing/cofinality problem and, for a hybrid proof, `G2-PSC`; a
   residue-A route separately owes `G2-BD`.  More book cells do not repair
   those obligations.

### 3.2 Counterexample/disproof side

1. **Corrected AS accepted-digit recursion.**  Work componentwise over the
   original nonreduced scheme, derive the divided integer numerator before
   reduction, and reconstruct every survivor.  This remains the strongest
   actual counterexample attack because an all-depth fixed-degree polynomial
   branch could enter the characteristic-zero/Hensel bridge.
2. **Positive maximum-12 trajectories.**  Any genus-zero Hurwitz component or
   quotient component that survives terminal and Taylor reconstruction is a
   high-priority candidate, not an inconvenience.  It must be replayed in the
   original coefficient equations and tested for polynomial provenance.
3. **Formal/q-polynomial routes.**  Pinchuk Theorem 3.4 remains source-gapped,
   and D-series transitions remain untyped.  They are controls, not current
   construction roots.

**Strongest new proof attack:** Card 1.  
**Strongest actual counterexample attack:** Card 3 (known lane, corrected
execution).  
**Strongest new falsification attack:** Card 2; a nonzero projective
exceptional locus immediately falsifies extrapolation from the `c1` line.  
**Decisive software experiment:** the Card-1 reduced-Nielsen/braid/genus
compiler, with exact passport and boundary-cusp certificates.

## 4. Detailed cards

### Card 1 — `MAX12-4PT-HURWITZ-GENUS`

**History label:** `NEW` mechanism at this exact client.  Fixed three-point
Hurwitz finiteness and generic passport existence are old.  The repository
contains no reduced braid-orbit genus computation for these four-point
strata.

**Target.**  Close, or explicitly parameterize, the three codimension-one
remainders of the confirmed full-absorption theorem on the reviewed
order-three `k=mu=0,nu!=0` landing.

The labelled branch cycles are:

| stratum | over `0` | over `infinity` | over `1` | over fourth value `t` |
|---|---|---|---|---|
| exactly one simple `B` root in `W` | `3^12` | `4^9` | `(18,2,1^16)` | `(2,1^34)` |
| double `B` root off `W` | `3^12` | `4^9` | `(18,1^18)` | `(3,1^33)` |
| two distinct `B` roots, equal non-1 critical value | `3^12` | `4^9` | `(18,1^18)` | `(2,2,1^32)` |

Each row has total defect `70=2*36-2`.  Thus there is no unrecorded branch
value on that stratum.  In each row the fourth value is uniquely
distinguished from `0,1,infinity`; Kummer/Galois covariance therefore fixes
it, so `t in C(x)`.  This invariant-field assertion is a mandatory first
type-check, not an assumption to hide.

**Dependencies.**  The confirmed Faber landing; `k` differential-constant;
the exact quadratic Wronskian and full-absorption review; actual Keller
coprimality/squarefreeness; the order-three character action; both original
Taylor-boundary families; and exact characteristic-zero braid arithmetic.
Do not import a unitree list or the zero-load DZ20 stabilizer conclusion.

**Cheapest discriminator.**  For each row:

1. enumerate simultaneous-conjugacy classes of transitive permutation
   quadruples in the stated conjugacy classes;
2. quotient by the reduced four-strand braid action;
3. compute the compactified coarse Hurwitz-curve genus from the exact braid
   permutations and record cusp widths/stabilizers;
4. verify the product, transitivity, defect, orbit size, genus, and boundary
   degeneration by an independent replay.

No coefficient elimination is needed before those four integers are known.

**Outcome interpretation.**

- **No Nielsen class:** that stratum is empty; independently check the class
  enumeration because the coefficient equations suggested it.
- **Every component has genus `>0`:** the rational map
  `P1_x -> H_component` is constant.  Classify the resulting isotrivial
  scaling/twist, impose `9*r8'=j/u`, and reconstruct Taylor boundaries.
- **A genus-zero component exists:** compute a rational universal family and
  pull the Faber invariants/terminal one-form back to its parameter.  A
  surviving rational trajectory becomes the first reconstruction target.
- **Several braid components/stabilizers:** keep them disjoint; compute the
  descent lattice per component.  Do not average genera or reuse the DZ20
  modulus.
- **A boundary cusp is the only rational image:** identify whether it is the
  already killed full-absorption passport, the parity boundary, or a new
  Taylor stratum before claiming closure.
- **The fourth value is not in `C(x)`:** `TYPE-FAIL`; repair the character
  quotient before using genus.

**Stop condition.**  Stop this card before the generic five-point stratum.
Stop a branch only after every braid component is constant-and-terminally
killed or explicitly parameterized and reconstructed.  If the Nielsen set is
larger than the coefficient fibre by more than an order of magnitude, first
add the monic/depressed support constraints rather than brute-force `S_36`.

**Expected information gain / cost:** `9/10`, low-to-moderate exact cost.  A
positive-genus result converts a moving coefficient problem into finite twist
arithmetic; genus zero gives an explicit survivor rather than an opaque GB.

### Card 2 — `TD6-CENTER-DIRECTION-ATLAS`

**History label:** `NEW` global-cover discriminator for this moduli block.
The stagewise TD6 factor atlas and maximum-12 Kuranishi direction analysis are
known separately; no report parameterizes every center by a projective line
through the base and certifies the projection of all compatible lines.

**Target.**  Conditional on hostile confirmation of the complete licensed
`c1` line, decide whether that one-dimensional exclusion widens to the full
three-parameter common-center block inside the same fixed boundary/F1/pole/
dead-stretch specialization.

Put the licensed base center at `c0=(1,1,1)` and write

```text
(c1,c2,c3) = c0 + T*(a,b,c),       [a:b:c] in P2.
```

Every affine center lies on such a line.  For each transport/first/current
pivot stratum, perform the already required stagewise parameterization over
`E[a,b,c,T]`, clear only proved denominators, and eliminate `T` plus the
linear transport variables.  The resulting homogeneous projection ideal on
`P2` is the exact exceptional-direction locus.

**Dependencies.**  A confirmed/frozen `c1`-line theorem; confirmed
3,602-column center typing and full-cokernel tangent; original stagewise
transport rows; exact treatment of `C=0,3` and the quadratic `J=0` stratum;
and fixed values of every noncenter modulus.  The base is a nonsolution, so no
tangent-space-at-a-solution inference is allowed.

**Cheapest discriminator.**  Before a universal projection, run the same raw
rebuild on the `c2` coordinate line, `c3` coordinate line, and the diagonal
direction `(1,1,1)`.  Record quotient/component width and the degrees of the
first two independent compatibility numerators.  Proceed only if all three
fit the same bounded stagewise schema.  This is a falsifier of feasibility,
not interpolation evidence.

**Outcome interpretation.**

- **Projective projection ideal is the unit ideal:** the entire common-center
  block is empty at the fixed remaining moduli; this is still not SP-2.
- **A finite set of directions remains:** rebuild those exact lines, including
  all denominator roots; each is a finite successor.
- **A curve of directions remains:** normalize it and pull back the line
  compatibility system; a positive-genus normalization may permit the same
  rational-map constancy test as Card 1, while genus zero gives a new explicit
  center family.
- **The projection ideal is zero/full `P2`:** the `c1` result does not widen;
  stop and return to the direct multi-center atlas.
- **A pivot denominator vanishes on an uncovered component:** coverage
  `GAP`; rebuild that raw fibre rather than saturating it away.
- **A center produces a compatible finite-band point:** reconstruct all
  original rows and preserve it as a TD6 falsification candidate.

**Stop condition.**  Stop after the three-line preflight if two directions
require incompatible source schemas or if the projected elimination is wider
than the already budgeted direct `(c1,c3)` atlas.  Never infer global center
coverage from samples or from the injective tangent at the base nonsolution.

**Expected information gain / cost:** `7.5/10`, moderate exact cost.  The
first probe cheaply distinguishes a genuinely reusable line theorem from a
special-axis result and, on success, supplies a fail-closed finite cover.

### Card 3 — `AS-D8-PRIMARY-BOCKSTEIN`

**History label:** `KNOWN-COMPOSITE / continue existing owner`, not a new
mechanism.  It is included because it is the strongest counterexample attack
and the packet correction changes its exact source contract.

**Target.**  On the surviving post-D10 vertical and endpoint strata, derive
the first degree-eight divided-carry obstruction as a section over the
original nonreduced predecessor scheme, including the embedded primary
component and all six Frobenius directions.

**Dependencies.**  Confirmed D10 pointwise gate; independently matched
integer-source generator with both `L/3` and the later single-cross
`[K_Frob/3]`; frozen corrected D9 section; exact predecessor ideal and its
nonreduced cover.  The provisional literal-`F3` census is not a dependency.

**Cheapest discriminator.**  At one generic vertical point, each endpoint,
and one point supported on the embedded primary component:

1. reconstruct the exact integer numerator at the charged modulus;
2. prove divisibility on the predecessor scheme;
3. reduce the quotient modulo three;
4. compute the augmented rank/cokernel class against every accepted next
   digit; and
5. compare generated rows with an independent determinant expansion.

Only after these four controls agree should the calculation be lifted from
points to the component coordinate rings.

**Outcome interpretation.**

- **Unit/augmented-rank obstruction on every associated piece:** the corrected
  successor is empty at this row; freeze exact row certificates.
- **The class vanishes with a global section:** advance that component and
  compute the next integer quotient; no characteristic-zero conclusion.
- **The class is torsion on a proper support:** split exactly by its
  annihilator/Fitting ideal and retain the embedded piece.
- **Reduced supports die but the embedded primary survives:** the nonreduced
  structure is load-bearing; do not promote radical emptiness.
- **The two source compilers disagree:** quarantine all counts and stop before
  any solver run.
- **Only an opaque solver verdict is obtained:** no result; change to the
  affine/cokernel representation.

**Stop condition.**  First failed integer-divisibility/source-row comparison,
or two opaque attempts on the same representation.  Do not widen prime,
degree, or AWS capacity before one component carries a replayable quotient
certificate.

**Expected information gain / cost:** `8/10`, moderate cost already owned by
the active AS lane.  It can either remove the first honest surviving
components or expose the precise next-carry support of a bounded polynomial
candidate.

## 5. Major-lane decisions

| Major lane | Decision | Exact action / stop rule |
|---|---|---|
| maximum-eleven composition | **stop production** | Retain as immutable regression and route input only. |
| zero-load order-three DZ20 | **stop branch** | Confirmed empty; reuse only passport/stabilizer controls. |
| full `B`-absorption loaded boundary | **stop branch** | Confirmed empty at exact scope; retain passports as Card-1 boundary cusps. |
| provisional parity genus-five descent | **continue freeze/review** | Do not consume as theorem or mutate its bytes; use it as a control for the quotient compiler. |
| remaining `k=mu=0,nu!=0` order-three fibre | **redesign / continue with Card 1** | Four-point Hurwitz genus first; character quotient for genuine five-point/no-collision second; preserve Taylor boundaries. |
| other order-three loads and polynomial core | **continue, separately stratified** | No import from `k=mu=0`; use approximate-cubic/invariant coordinates and exact rank boundaries. |
| `(9,12)` order-one and every `(8,12)` route | **defer but preserve manifests** | Do not call them cheaper/dead; keep order-two `(8,12)` leaf and both Taylor families charged. |
| TD6 `c1` line | **continue lifecycle, no fanout before review** | If confirmed, run Card-2 three-line preflight; if gapped, retain only reviewed strata. |
| TD6 remaining center/dead-stretch/F1/pole atlas | **continue exceptional-first** | Stagewise raw rebuilds only; no one-shot affine Smith shortcut or SP-2 promotion. |
| corrected AS post-D10 recursion | **continue Card 3 with existing owner** | Source comparison/freeze/review first; original nonreduced ideal and integer quotient mandatory. |
| old AS census / omitted-cross generator | **stop/quarantine** | Retracted counts are controls only and must not seed successors. |
| D-series / integral D43 / D75 | **stop at source gate** | No typed full source transition; more depth is unlicensed. |
| Makar-Limanov Newton/mate intake | **continue only typed checker/package** | The proposed GGV-frame bridge test already exists in the source audit; do not duplicate it or filter campaign cells heuristically. |
| Pinchuk quasi-polynomial global reduction | **stop globally** | Termination/category gap remains; retain local Theorem 4.1 identity only. |
| pure Sigray/GGV architecture | **continue coverage theory, stop new cells** | Work on full landing/coverage and scoped `G2-PSC`/`G2-BD`, not another bounded book expansion. |
| ambient GB / generic sparse search / AWS expansion | **stop** | Current gates are exact finite quotient, braid, resultant, and carry certificates; opaque runs add no evidence. |
| hostile reviews and source comparisons | **continue nonblocking** | Promotion remains review-gated; no report byte is edited. |

Speed-optimized allocation: Card 1 first on one-collision and double-`B`
passports; existing AS Card 3 in parallel; finish TD6 lifecycle and only then
run Card 2's three-line preflight.  Reserve the generic five-point maximum-12
stratum until the four-point genus calculation says whether Hurwitz geometry
is actually discriminating.

## 6. History, priority, and blindness checksum

The six canonical cutoff hashes match the packet exactly:

```text
a069522025da7bef75cf0112dce8857b643f84ae7106cbd6756e959ee5afc965  APPROACHES.md
b2b29c6bb666d5bc50616921db858e3daefa75d54d48471f6b71e7e187928c92  AUDIT.md
9c29aae163268097978596ec448e6bac429044d81fa92bff3374e3852588bfc7  PROGRESS.md
813ccd6a15b8f00333b8ba66a4febff25d542660d86af723cad96124380d558d  notes.md
2bed8117d2137bd9f93d558d7b9aca98a02a21c5b42688fc181e99d683f537cd  COORDINATION.md
f0fca49811349483c437d674ab819065eb09500330516d161978868c71bf6371  ladder/REDUCTION.md
```

I read the packet, all 46 avenue rows, all of `AUDIT.md`, all of
`PROGRESS.md`, the newest `19:32Z LIVE STATE`, all of `COORDINATION.md`, all
of `ladder/REDUCTION.md`, my prior `T1820Z` owner report, and the closed
`T1820Z` synthesis.  I also searched repository reports and git history for
the target/mechanism synonyms below without opening any current-round sibling.

Deduplication and priority results:

- **Card 1 is `NEW` at this client.**  Three-point Hurwitz finiteness,
  DZ20 scaling descent, generic Nielsen existence, and the statement “the
  remainder is four-point” are banked.  No prior artifact computes the
  reduced braid orbits, compactified Hurwitz-curve genera, cusp widths, or the
  resulting `P1_x` moduli-map obstruction for the three displayed passports.
- **Card 2 is `NEW` as a coverage test.**  TD6 already has center tangents,
  a stagewise `c1` atlas, and a `(c1,c3)` dependency census.  Maximum-12 has
  weighted Kuranishi/projective directions.  No prior report covers all
  affine center triples by projective lines through the base and computes the
  exact exceptional-direction projection.  It must not reuse the invalid
  one-shot affine Smith shortcut.
- **Card 3 is `KNOWN-COMPOSITE`.**  `AS-CARTIER-DAG`, bounded
  Greenberg--Cartier lookahead, augmented Fitting, and componentwise integer
  quotients are already in the history.  The packet's source correction
  changes the charged generator, not the novelty label.
- The tempting Makar-Limanov/source-shear identification is **duplicate and
  type-unsafe**: the primary-source audit already proposes a bridge-or-stop
  test and explicitly says its `D` is not yet the sheared total-degree gcd,
  a GGV packet invariant, Sigray `td`, or partial degree.
- A generic monodromy/passport census is **known insufficient**; Card 1 uses
  the moduli curve and rational trajectory, not Riemann existence alone.
- A generic sparse coefficient search, a new AS cap grammar, a one-shot TD6
  Smith system, direct D12 carry, Pinchuk reverse polynomialization, ordinary
  action residues, projective connectedness, and untyped D-series depth are
  stopped or source-gapped in history and are not relabelled as new.

No current-round sibling report was read, named, listed, or exposed.  The own
target did not exist before this write.  This submission is therefore
**not degraded**.

## 7. Scoped conclusion

This scan contributes two falsifiable mechanisms, not two theorems.  The
four-point braid/genus gate can turn three moving loaded strata into either
finite isotrivial twist problems or explicit rational survivor families.  The
TD6 direction atlas can decide cheaply whether the provisional `c1` theorem
has any finite-cover leverage beyond its axis.  The existing corrected AS
recursion remains the best counterexample-side investment.  None changes the
global truth status: maximum twelve, TD6/SP-2, a characteristic-zero AS lift,
and JC2 all remain open at the packet cutoff.
