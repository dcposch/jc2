# TD6 V73K review repair

Status: **load-bearing identity hostile-review confirmed; custody repair
frozen; clean report-path rerun requested.**

The different-model hostile review returned `CONFIRMED_WITH_REPAIRS` and
confirmed the exact V73K theorem on
`V=0,C=-5U^2,D(U)` for every beta in the fixed source-typed A3 q2-beta
section.  In particular, it confirmed source typing, reconstruction of all
twelve original previous rows, retention of the direct `q'` term in the
positive theorem run, the beta-independent U-supported residual and exact
inverse, the lift to thirteen original first rows, the full original-row
convolution, all theorem-run negative controls, the U-only denominator
ledger, and dual-host mathematical/artifact agreement.

The required repair is provenance-only.  The frozen
`evidence/control/box03-qprime` bytes in the original package came from the
predecessor six-odd-row/per-edge producer, not from V73K.  The frozen V73K
runner cannot reproduce that mode because its direct-q-prime and twelve-row
assertions are unconditional.  The predecessor output is now explicitly
classified as support-only evidence; it is not a V73K pass gate, theorem
premise, independent theorem run, or custody proof.  No return code or
launch metadata survived for that predecessor directory, and none is
inferred.

No positive identity artifact changed.  The exact theorem scope remains
only `V=0,C=-5U^2,D(U)` for all beta in the fixed section.  This does not
consume `U=0` and implies no whole-B3, whole-A3, TD6, SP-2, landing, or JC2
claim.

Custody:

- original package MANIFEST SHA-256:
  `1282f908eb5e09543e1f5e6f97e0fce5fdac09873fb12ba1f9ff856d9b01b50f`;
- original package FREEZE SHA-256:
  `d50d8844d930f56b66d72c64d50ca213895e140c5a1aa96456f8bdd61092851e`;
- original producer report SHA-256:
  `716717abe5ba5bd7e5cf7ac58a2ac87ebfb06d6f4b25d58a0152a216796de49b`;
- preserved review adapter log SHA-256:
  `ee85b05c39d94a68406a5d4892a3b7875c915a0a0326d5198858c9880b6ad262`;
- predecessor q-prime stdout/stderr SHA-256:
  `c698dca9cf042dfae331e502422644b7e38aed17b29ae91e74acbbaec8c5fa46` /
  `12994f401a70692743ddc7a5830b0b244516e64857bced5c09d5b65159b563e8`.

The original adapter returned lane rc4 only because its prompt omitted an
explicit report destination.  The V2 prompt charges exactly one new output
file and no other edit.
