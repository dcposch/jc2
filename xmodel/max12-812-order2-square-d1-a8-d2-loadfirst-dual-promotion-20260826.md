# Promotion: D1 `a=8,d=2` load-first tail

Date: 2026-08-26

Status: **PROMOTED EXACT THEOREM AT THE NARROW SCOPE BELOW.**

## Frozen authority

```text
9a85f15ea741255ecda04fc787467393d1661a08cd8ed501549d4cacf32d91fd
  cases/max12_812_order2_square_owner_d1_a8_d2_loadfirst_dual_functional_20260826/PRODUCER_FREEZE.sha256
919f6e1a296e084ffdf4d569fcf8c184bcf5b2cf263f47b2882bd03f50539021
  cases/max12_812_order2_square_owner_d1_a8_d2_loadfirst_dual_functional_20260826/RESULT.md
0558ea1e668245b983e217ef329a6ccf0baf5a8f5769f669f43a245d24990e21
  xmodel/max12-812-order2-square-d1-a8-d2-loadfirst-dual-hostile-review-grok-20260826.md
```

The independent Grok 4.6 hostile review rehashes the frozen producer and
all retrieved AWS evidence, independently re-enumerates the complete source,
rederives the load split, multiplication rank, pole-two reconstruction,
moving-root pairings, unit ideals, and finite-etale descent, and ends
`CONFIRMED`.  It finds no mathematical or scope defect.  Its custody note
that the ephemeral launch archive itself was not retrieved does not affect
the pinned source and on-host freeze checks.  Its nonblocking observation
that generated `GH1_exact`/`Psi_pm_exact` flags are not endpoint factors is
closed independently by the verified source valuation and coefficient
identities.

## Promoted statement

After the frozen square/D1 source gates, in characteristic zero on
`D(p*k0)`, the seven literal Faber equations have no point in the strict
closed contact tail

```text
ord(A)=8, ord(C)=10, ord(R)>=8.   (d=2)
```

Write `k6=k60+sigma*k60_1+...`.  The proof is the exhaustive
scheme-theoretic split `D(k60) union V(k60)`.

On `D(k60)`, the unique grade-27 primitive is `(3/4)k60*C/L`, one grade
before the row-2 target; its first two rows generate the unit ideal on both
exact-`C` charts.  On `V(k60)`, grade 28 begins with
`(3/4)C(A+k60_1)/L`.  The multiplication determinant
`c0^2+(p/2)c1^2` closes the rank-two chamber, while the rank-one face is
retained.

The complete source through grade 30 has pole ceiling two and sole pole-two
column `(3/8)C^2/L^2`.  On the finite etale root cover the two target-free
functionals

```text
Phi4 +/- lambda*(Phi3+(p/4)*Phi1),  lambda^2=-p/2,
```

annihilate all pole-one columns and give terminal squares
`(3/8)(+/-c1*lambda+c0)^2`.  Those squares generate the unit ideal on both
exact-`C` charts before radicals, including the rank-one chamber.  No
leading coefficient of `R` is inverted, so this proves the full closed
`ord(R)>=8` tail and descends to the unsplit `D(p*k0)` chart.

## Firewall

This promotion contains exactly the displayed `a=8,d=2` cell.  It does not
include `a=8,d=3`, `a=9`, a row-2 or row-4 target-shadow chamber, an equality
face, a positive-order leading `k10`, `p=0`, `k0=0`, another D1 face, a
terminal or global chart, the square component, order two, `(8,12)`, maximum
twelve, or JC2.  No timing or emptiness claim may be extrapolated to any of
those cells.
