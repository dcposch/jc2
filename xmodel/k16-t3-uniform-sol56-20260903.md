# K16 ray theorem (T): `t=3`, `t=4`, and the uniform-coefficient attack

**Lane:** `k16-t3-uniform-sol56-20260903`  
**Date:** 2026-09-03  
**Verdict:** theorem (T) is proved here at `t=3`; `t=4` has three modular unit
bases but remains open in characteristic zero. The proposed fixed-band uniform
obstruction is disproved by an exact subsystem point, and no uniform unit
certificate is presently in hand.  
**Scope:** theorem (T) on the K=16 ray at fixed `t=3` and `t=4`, followed by a
generic-`t` coefficient-family audit. No ledger edit; no `jc2-lean`; no prohibited
in-progress report read. Drivers and measurements are confined to
`box/k16t3-20260903/`.

Claim labels are literal. **MEASURED** records reproducible program output;
**MEASURED-MODULAR** is evidence over named finite fields and is not a
characteristic-zero proof; **DERIVED** is a displayed mathematical consequence;
**PROVED-HERE** is an exact certificate or checked proof. `OPEN[...]` is not filled
by analogy or by finitely many fixed-`t` computations.

## 0. Custody and fail-closed gate

**MEASURED.** The readable lane receipt
`xmodel/k16-t3-uniform-sol56-20260903.run.v2` was parsed with `awk`. Its
`charged_input_<i>_sha256` and `charged_input_<i>_basename` fields were paired,
without retyping a hash, under `/tmp/jc2-lane.NKyBDU/inputs/`; the resulting
manifest was passed to `sha256sum -c`. All **10/10** files returned `OK`. The
content-mismatch stop did not fire.

**MEASURED (scope).** Only the ten frozen inputs and new artifacts named in this
report were read. Neither prohibited in-progress lane report was opened. There was
no ledger edit, no `jc2-lean` access, and no `ideation-*` file access. All driver
writes are under `box/k16t3-20260903/`; this report is the only `xmodel` write.

## 1. The family and the generalized order chart

### 1.1 Radii and denominator

**DERIVED + MEASURED.** Put

```text
n=12t+4, m=8t+4, M2=12t+1, V2=3,
e=n/4=3t+1, q=m/4=2t+1.
```

The frozen symbolic derivation simplifies Moh's Definition-5.1 values to

```text
delta2^51=-1/2, delta1^51=t/[2(3t+1)],
(delta2',delta1')=(-1,t/(3t+1)).
```

It asserts these identities before specialization. Thus `t=3` gives
`(-1,3/10)`, as requested, and `t=4` gives `(-1,4/13)`. Theorem 1.2 gives both
coefficient families the common order bound

```text
ord C_i >= -i/e = -i/(3t+1).
```

### 1.2 Spaces, generator, and gauges

**DERIVED.** With

```text
z=pi-gamma,
B=pi*z+b1*pi+b2,
A=pi*B+b3,
h=pi*A+b4,
```

the intersection of the major- and minor-disc bounds gives

| deficit `i` | coefficient space `S_i(t)` |
|---:|---|
| `1 <= i <= t` | `<1>` |
| `t < i <= 2t` | `<1,A>` |
| `2t < i <= 3t` | `<1,A,B>` |
| `i=3t+1` | `<1,gamma,A,B,z>` |

The generated chart is

```text
P=h^e + sum_(i=1)^e alpha_i h^(e-i),  alpha_i in S_i(t),
Q=h^q + sum_(j=2)^q beta_j  h^(q-j),  beta_j  in S_j(t).
```

Here `beta_1=0` is the same approximate-root/Tschirnhausen normalization as in
the charged generator. The nonzero second top-line coefficient is normalized to
`-1`, as in that generator; `c!=0` is saturated explicitly.

**PROVED-HERE (three safe target gauges).** Apply, in order,

```text
Q <- Q-const(beta_q),
P <- P-alpha_t Q,
P <- P-const(alpha_e).
```

The middle index is `e-q=t`: `alpha_t h^(e-t)=alpha_t h^q` aligns with the
leading term of `Q`. Thus the charged `alpha_2=0` gauge generalizes to
`alpha_t=0`, not to a fixed `alpha_2=0`. The scalar `alpha_t` induces changes
`alpha_(t+j) <- alpha_(t+j)-alpha_t beta_j`; monotonicity
`S_j(t) subset S_(t+j)(t)` keeps every changed coefficient in its declared
space. The two endpoint operations are scalar target translations. The composite
target map is invertible triangular with determinant one, so it preserves the
Jacobian, degrees, monicity, and both-disc filtration. No parameter-dependent
quantity is inverted.

**DERIVED (counts).** Before gauging there are

```text
alpha: 6t+5, beta: 3t+2, b1..b4 and c: 5,
total chart unknowns including c: 9t+12.
```

The gauges delete three scalar coordinates, giving `9t+9` unknowns: `36` at
`t=3` and `45` at `t=4` (ungauged: `39` and `48`).

**MEASURED (same generator).** The generalized driver specialized at `t=2`
has exactly the same ordered parameter list, `P`, `Q`, 37 tagged coefficient
equations, equation order, and Sympy expressions as the frozen
`t2_order_system.py`. Deterministic numerical-parameter reconstruction of the
full h-adic Jacobian is `True` at `t=2,3,4`.

### 1.3 Exact banded identity

**PROVED-HERE.** Set `alpha_0=beta_0=1`, `beta_1=0`. Before monic
`h`-division, the raw coefficient at h-level `k` is

```text
R_k = sum_(e+q-i-j=k) J(beta_j,alpha_i)
    + sum_(e+q-i-j-1=k)
        [(e-i) alpha_i J(beta_j,h)
         +(q-j) beta_j J(h,alpha_i)].
```

Starting with `C_0=0`, the driver performs the recurrence

```text
F_k=R_k+C_k = h*C_(k+1)+N_k,  deg_pi N_k<4,
coeff(N_0-c*gamma)=0, coeff(N_k)=0 for k>0.
```

Division is Euclidean division in `pi` by the monic quartic `h`. Hence it is
exact, divides by no parameter leader, and retains every zero-leader branch.

## 2. Fixed `t=3`: `(40,28;37;3;J=c gamma)`

**MEASURED (chart).** Here `(e,q)=(10,7)` and the verified radii are
`(-1,3/10)`. The three gauges reduce 39 ungauged unknowns to 36 including
`c`. Expansion gives 51 coefficient generators at levels `0,...,13`, with
degree histogram

```text
degree 1: 1, degree 2: 6, degree 3: 23, degree 4: 21,
generator text: 11,590 bytes.
```

The exact level histogram is `7,6,6,6,5,5,5,2,2,2,2,1,1,1`. The generated
chart reconstructs the uncollected h-adic Jacobian identically.

**MEASURED-MODULAR (not a proof).** Singular 4.3.2, one effective core,
degree-reverse-lexicographic order, and explicit `sat(I,<c>)` component
extraction gave the following reduced bases. All stderr files are empty and
all actual-pair, ring, extracted-component, empty-wrapper, and nonempty-wrapper
controls pass.

| field | input bytes | SHA-256 | wall / max RSS | reduced basis |
|---|---:|---|---:|---|
| `GF(32003)` | 13,465 | `7414711be8a87bec7f5a9ea47943c7181199fb2912840ce633b0bf96ba5652c3` | 62.36 s / 61,608 KiB | `[1]` |
| `GF(32009)` | 13,465 | `2ca5e0a18f9140bbea15a5e925c76cbacf2b6e35939ee73befc583381624b5dd` | 65.84 s / 61,752 KiB | `[1]` |
| `GF(32027)` | 13,465 | `59d99caa2f34e17d086666f7d25dd6b6ebbcaa5326a98f54c2918c79bafaea63` | 61.88 s / 61,840 KiB | `[1]` |

The primes were mechanically checked prime. These three unit ideals do not by
themselves lift to characteristic zero.

**MEASURED (bounded exact-Q attempts).** Direct exact `sat` followed by `std`
reached the required 1,800 s cap without a basis: 1,800.12 s, 1,952,704 KiB,
exit 124, with all controls complete. A direct Rabinowitsch `slimgb` run was
stopped after 1,515.54 s with no basis and is not used. The exact
constant-pivot quotient has 13 pivots, 37 residual coefficient equations, 23
chart unknowns including `c`, and one `T`; its `slimgb` program is 57,766 bytes
with SHA-256
`6a0b8c9760e8be340b48d374b42f5be033e880ac3bc9915a08758f45cc8df435`.

Both exact quotient attempts also reached the cap without a basis: `std` used
1,800.02 s / 328,336 KiB and `slimgb` used 1,800.04 s / 613,492 KiB (exit
124). These timeouts are diagnostics only.

**PROVED-HERE (exact normalized certificate).** Write `x=q4_1` and `y=q7_1`
in the constant-pivot quotient. Exact source-row identities give

```text
343*c = 10*x*y*(2*x^2-21*y),
11*x^4-84*x^2*y+147*y^2 = 0.
```

All 37 residual rows are homogeneous for a mechanically solved positive
grading, with `wt(x)=13`, `wt(y)=26`, and `wt(c)=65`. On `c!=0`, the first
identity forces `x!=0`; over the algebraic closure choose `lambda` with
`lambda^13*x=1` and apply the checked weighted action. This makes `x=1` and
puts `y` in the exact quadratic field

```text
K=Q[y]/(147*y^2-84*y+11),
c=10*y*(2-21*y)/343.
```

The discriminant is `588`, a nonsquare. Moreover `H(0)=11` and
`H(2/21)=13/3`, so `y`, `2-21y`, and hence `c` are units in `K`. Thus this
slice captures both conjugate `y`-fibres and loses no algebraic-closure point
of the saturated locus.

After exact duplicate removal there are 29 non-minimal-polynomial rows in 20
auxiliary unknowns. Seventeen successive rows are affine-linear in distinct
variables with coefficients in `K*`; the emitter verifies each displayed
inverse and each substitution modulo `H`. Six further rows reduce to zero.
These quotient-ring isomorphisms leave six generators in `(b3,b4,a2_0)`,
coming from bands `h=0,...,5`.
Singular exact `std` returns the reduced basis `[1]` in 0.04 s / 12,892 KiB;
an independent exact `slimgb` run returns `[1]` in 0.57 s / 13,968 KiB. Both
exit 0 with empty stderr and all actual-pair, unit, ring, empty-wrapper, and
nonempty-wrapper controls passing. The respective inputs have SHA-256
`fc2c504604c51e33e7c63e32bde4a952d0c6741b99fcf5e9c334c63eefb4796b`
and `7f92d0cadce7bf892b6615030c8d28eb972496d459018c1cec63ac2fa9a112dd`.
The 13-entry input/output certificate manifest
`preprocessed/SHA256SUMS.t3-normalized-results` verifies mechanically; its
SHA-256 is
`7b75911088875d3d8434e7580a30f5d529eb7cddb7a6fd473ff75824539aafde`.

Therefore the normalized locus is empty over the algebraic closure, hence the
original `c`-saturated order-chart ideal is the unit ideal. Since the chart is
a necessary superset, theorem (T) for `(40,28;37;3)` is **PROVED-HERE**.

## 3. Fixed `t=4`: `(52,36;49;3;J=c gamma)`

**MEASURED (chart).** Here `(e,q)=(13,9)`, the radii are `(-1,4/13)`, and the
gauges reduce 48 unknowns to 45 including `c`. There are 65 coefficient
generators at levels `0,...,17`, with degree histogram

```text
degree 1: 1, degree 2: 6, degree 3: 30, degree 4: 28,
generator text: 18,221 bytes.
```

The generalized and independently generated dedicated `t=4` charts agree in
ordered parameters, `h,A,B,z,P,Q`, tags, row order, and every equation.

**MEASURED-MODULAR (not a proof).** Ordinary `std` did not finish in 1,200 s
at any of the three primes. With the identical ideals in Rabinowitsch form,
`slimgb` completed as follows; all type/ring and positive/negative controls
pass and stderr is empty.

| field | input bytes | SHA-256 | wall / max RSS | reduced basis |
|---|---:|---|---:|---|
| `GF(32003)` | 19,986 | `6b77fa946a743a115cf89cb815bce9b854d4439dba3e10c9da2a6c481d299f23` | 133.77 s / 1,213,808 KiB | `[1]` |
| `GF(65521)` | 19,986 | `2a352f9613b6ea92511e5a5f42672d1bf3af149c148f0095993e181b23022da5` | 227.64 s / 507,856 KiB | `[1]` |
| `GF(1000003)` | 19,994 | `b3b5e5ccc8972ee67d1e14665c8ebf326435c13f0ea49bc223b489f1e6ccda0d` | 241.91 s / 507,792 KiB | `[1]` |

The three characteristics are prime and avoid every pivot, substitution,
row-clearing, and control denominator. They remain modular evidence only.

**MEASURED (bounded exact Q).** Direct exact Rabinowitsch `slimgb` passed its
controls but reached the 1,800 s cap without a basis: 1,800.04 s, 609,904 KiB,
exit 124. Its 19,974-byte input has SHA-256
`0b096ff42b83d7507c078961a637c64725d4dc83609b058841035908254e8ec6`.
The exact quotient input has 16 constant pivots, 48 residual coefficient
equations, 29 chart unknowns including `c`, and one `T`; its 186,790-byte
`slimgb` program has SHA-256
`54abfe8f94f98d2d2baa3d46b8504e2fe793868c22eea6f1d7d894b96c34d045`.

The reduced exact `slimgb` run likewise reached the cap without a basis:
1,800.05 s / 829,468 KiB, exit 124, after all controls passed.

**PROVED-HERE (normalization equivalence; not yet a unit certificate).** In the
48-row quotient put `u=q5_1`. A unique positive grading was recovered from
3,650 monomial-difference constraints (rank 28, nullity one), with
`wt(u)=17`, `wt(q9_1)=34`, and `wt(c)=85`. Two exact rows are

```text
35*u^4-270*u^2*q9_1+486*q9_1^2 = 0,
-2187*c+130*u^3*q9_1-1404*u*q9_1^2 = 0.
```

Thus `c!=0` forces `u!=0`, and weighted scaling normalizes `u=1`. Writing
`v=q9_1` gives `H4=486*v^2-270*v+35`, whose discriminant
`4860=18^2*15` is nonsquare. Exact gcd checks make
`c=v*(130-1404*v)/2187` a unit in `Q[v]/(H4)`. Checked substitution,
quadratic reduction, zero-row deletion, and duplicate removal yield 38
generators in 26 variables over this exact field. The emitted 151,168-byte
input has SHA-256
`848c2c6aa4215b9e15d87a3dd9b8903a7d1b395e2960284f4232458bf1b84bd7`.

No exact standard basis for this normalized input completed inside the
remaining lane bound; it was stopped after 774.00 s / 990,404 KiB (exit 1),
with controls passed and no `MAIN_DONE`. Consequently it supplies a
proof-preserving reduction, but not a unit certificate.

## 4. Generic `t`: banded equations and coefficient dependence

### 4.1 What is and is not `t`-dependent

**DERIVED.** In deficit indexing, the only numerical coefficients involving `t`
are `e-i=3t+1-i` and `q-j=2t+1-j`. Every generated numerical coefficient is
therefore in `Z[t]`, of degree at most one. The Jacobians of the coefficient
spaces, `z,B,A,h`, the monic carry, the target `c*gamma`, and the saturation
equation are `t`-independent. The changing index ranges and the four regimes of
`S_i(t)` are a separate combinatorial dependence. In particular, after the
piecewise spaces have been declared, the order-chart ideal contains no
`1/(3t+1)` or other rational function of `t`; no denominator saturation is
needed merely to state this family.

**DERIVED (reverse indexing).** Rename tail coefficients by h-power,

```text
p_s=alpha_(e-s), r_u=beta_(q-u).
```

Then a raw tail band has the literally `t`-independent form

```text
R_k^tail = sum_(u+s=k) J(r_u,p_s)
         + sum_(u+s=k+1)
             [s p_s J(r_u,h)+u r_u J(h,p_s)].
```

A final level `H` uses tail coefficients only through power `H+1`; their spaces
have stabilized when `t>=H+1`. After the displayed canonical renaming, all
generators through that level are then identical, not merely rational
specializations.

**MEASURED.** At `t=2,3,4` the equation counts are respectively `37,51,65`,
matching `14t+9`; their exact level histograms match

```text
h=0:                     7
h=1,...,t:               6 each
h=t+1,...,2t:            5 each
h=2t+1,...,3t+1:         2 each
h=3t+2,...,4t+1:         1 each.
```

The scripts, rather than extrapolation from three values, remain the authority at
each fixed `t`.

### 4.2 High-side recurrence

**DERIVED + MEASURED.** Let `a_1,b_1` be the `A` coordinates of
`alpha_(t+1),beta_(t+1)`. The sole highest equation, at `h^(4t+1)`, is

```text
(2t+1)*a_1-(3t+1)*b_1=0.
```

The next equation, with the next `A` coordinates `a_2,b_2` and scalar
`u_1=alpha_1`, is

```text
(2t+1)*a_2-(3t+1)*b_2-3t*u_1*b_1
-b4*((2t+1)*a_1-(3t+1)*b_1)=0.
```

At `t=2,3,4` the leading equations are `5a_1-7b_1`, `7a_1-10b_1`, and
`9a_1-13b_1`. Over `Q(t)`, `2t+1` and `3t+1` are safe pivots for positive
integral `t`; this organizes the high coefficients recursively. It does not
produce a unit, because the number of middle bands between this recurrence and
the Jacobian tail grows with `t`.

### 4.3 A uniform constant-pivot spine

**PROVED-HERE.** There is nevertheless a proof-preserving uniform reduction.
The identities

```text
J(h,A)=pi*h-b4*pi,
J(h,B)=2h-b3*pi-2b4
```

show, in descending bands, a constant pivot `q=2t+1` on the `A` coordinate of
each `alpha_i`, `t+1<=i<=e`, and a constant pivot `2q` on its `B` coordinate
for `2t+1<=i<=e`. Pairing with a nonleading coefficient of `Q` only enters a
lower band, so the elimination is triangular. After those coordinates are
removed, the `pi^3` row in band `h^(q-1)` has coefficients `(-4q,3q)` on the
`(gamma,z)` coordinates of `alpha_e`; eliminating `gamma` leaves coefficient
`-3q/4` on `z` in the `gamma*pi^2` row. Thus, in characteristic zero, there
are exactly

```text
(e-t)+(e-2t)+2 = 3t+4
```

field-constant pivots, occupying bands `4t+1` down through `2t`. The same map
is valid modulo primes not dividing its recorded pivot and clearing scalars.

At each step an equation has the form `a*x+b` with `a in Q*` and `x` absent
from `b`. Substitution `x=-b/a` gives an explicit quotient-ring isomorphism,
not a projection. The composed map is checked by exact expansion on every
original generator; each cleared emitted row is checked to be a nonzero
rational multiple of its image. Since the map fixes `c`, it extends to the
Rabinowitsch presentation with `T*c-1`. Consequently the original and reduced
`c!=0` ideals are unit ideals simultaneously.

**MEASURED.** The finite reductions are:

| `t` | original equations / chart unknowns | pivots | residual equations / chart unknowns | pivot bands |
|---:|---:|---:|---:|---|
| 2 | 37 / 27 | 10 | 26 / 17 | 9 through 4 |
| 3 | 51 / 36 | 13 | 37 / 23 | 13 through 6 |
| 4 | 65 / 45 | 16 | 48 / 29 | 17 through 8 |

Counts include `c` but not `T`. A fresh exact `t=2` reduced run gives `[1]` in
0.48 s with 13,916 KiB maximum RSS, independently validating the transformation
against the frozen certificate. The surviving sizes grow as `6t+5` chart
unknowns and, in these three generated cases, `11t+4` equations. The pivot
spine is uniform; the latter three-point residual-count pattern and a closed
formula observed for two terminal images are not used as generic theorems.

## 5. Low-band test and first nonuniform band

### 5.1 Exact symbolic consistency witness

**PROVED-HERE.** The requested two-/three-band symbolic obstruction does not
occur. Set

```text
b1=b2=b3=b4=0,
h=pi^3(pi-gamma), B=pi(pi-gamma), z=pi-gamma,
Q=h^q+B,
P=h^e+gamma+2z=h^e+2pi-gamma,
c=-1,
```

and set every other chart coordinate to zero. This point obeys all three gauges
and all coefficient-space declarations. Exact differentiation gives

```text
J(B,2pi-gamma)=-gamma,
J(h,2pi-gamma)=pi^2(2pi-3gamma),
J(B,h)=-2h,

J(Q,P)=-gamma
       +q*pi^2(2pi-3gamma)*h^(q-1)
       -2e*h^e.
```

Thus the `h=0` target equation and every level `1,...,q-2=2t-1` hold. The
only residual levels are `h=2t` and `h=3t+1`. For any fixed low cutoff `H`,
the same point satisfies all levels `0,...,H` once `2t-1>=H`. It also satisfies
`h=0` together with any fixed number of highest levels for sufficiently large
`t`, because its defects remain at those two moving middle levels. Therefore
neither the bottom two/three bands nor a fixed two/three-high-band subsystem can
have a uniform unit certificate.

**CONTROL / SCOPE.** This is a point of a proper subsystem, not a pair with
`J=-gamma`, not a representative of the four-tuple, and not an attainment
claim. Its purpose is solely to prove consistency of the proposed fixed-band
test.

### 5.2 First changing bands and certificate support

**MEASURED.** Under canonical tail renaming, the `t=2` and `t=3` systems agree
at `h=0,1`; their first difference is `h=2`, where the later chart has the new
boundary coordinates `p_(3),B` and `r_(3),A`. More generally, the systems at
`t` and `t+1` first differ at low level `H=t` for `t>=2`. From the high side, offsets
`L=0,...,t-1` from `4t+1` each have one equation and offset `L=t` first has
two, so `t=2` versus `t=3` first differs at high offset `L=2` as well.

**MEASURED LIMIT / OPEN[CERTIFICATE-SUPPORT].** The charged `t=2` artifact
contains the ordered generators and reduced basis `[1]`, but no Singular
protocol, lift matrix, or representation of `1` in the input generators. A
reduced basis `[1]` alone does not identify which original band generators its
reductions used. Hence the literal first generator used by the `t=2` or `t=3`
unit representation is not claimed. What is proved here is the first system
divergence (`h=2`/`L=2`), the first nonzero affine-`t` high generator
(`h=4t+1`), and the first level missed by the exact low-band witness (`h=2t`).

### 5.3 The `pi^-1` tail

**OPEN[DESCENT-ANCHOR].** This order chart is polynomial and deliberately omits
the separate reciprocal-Laurent tail equations. The charged report identifies
the required tuple coefficient and gives a constant-pivot series-reversion
recipe, but does not supply the map into this growing family. Importing tail
cancellations printed for a different Appendix-II row would be analogy. Thus the
banded identity supplies a uniform local recurrence, while neither it nor a
proved `pi^-1`-tail bridge currently supplies a uniform unit certificate.

## 6. Controls, proof scope, and FALLACY-v2 audit

**MEASURED (controls).** Each accepted fixed-`t` run checks its declared main
ring. Full-chart runs check the extracted saturation component; normalized
field runs check that the image of `c` is a unit. The positive empty control
`<c>:<c>^infinity` reduces `1` to zero; the negative nonempty control
`<c-1>:<c>^infinity` does not. Equivalent Rabinowitsch controls use
`<c,Tc-1>` and `<c-1,Tc-1>` where emitted. The exact actual-pair control
`(pi,pi-gamma^2/2)` has `J=gamma` and reciprocal anchor degrees `(2,2)`, but
its pi-degrees are `(1,1)`, so the K16 four-tuple classifier fails.

**PROVED-HERE (logical wrapper).** The order chart omits additional Laurent
conditions and is therefore a necessary superset of the requested locus. A
characteristic-zero unit ideal after saturating by `c` proves the fixed-`t`
target empty a fortiori. A nonunit or modular-only result would instead be only a
chart/counting result and would not construct a target pair.

**FALLACY-v2 audit.** No cv flag is identified with a place or cover series; no
exit set or exit price is asserted. The actual-pair control is not promoted to a
four-tuple representative. Monic h-division handles raw zero remainders without
discarding a vanished-leader branch. The ring, generator order, coefficient
field, saturation component, and controls are declared. Fixed-`t` certificates
are not promoted to theorem (T) uniformly. No new exit-price assertion is made,
so no `charge_basis=...` line is due.

## 7. Typed verdict and reproduction

The requested verdict is:

1. **`t=3`: PROVED-HERE.** The exact normalized quadratic-field basis is
   `[1]`; therefore `(40,28;37;3;J=c gamma)` is saturated-empty.
2. **`t=4`: MEASURED-MODULAR / OPEN in characteristic zero.** The corrected
   65-generator, 45-chart-unknown ideals have reduced basis `[1]` over each of
   `GF(32003)`, `GF(65521)`, and `GF(1000003)`. The bounded exact runs produced
   no basis, so theorem (T) at `(52,36;49;3)` is not claimed.
3. **Uniform in `t`: PARTIAL, not proved.** The family and the
   quotient-preserving constant-pivot recurrence are proved uniformly through
   bands `h^(4t+1)` down to `h^(2t)`. The exact witness proves every fixed
   collection of low or extreme-high bands consistent for large `t`, and no
   uniform unit certificate or proved Laurent-tail bridge is in hand.
   Accordingly **OPEN[T-UNIFORM-COEFFICIENT]** remains open.

Principal reproduction entry points are
`t3/t_order_system.py`, `t4/t4_order_system.py`,
`preprocessed/triangular_preprocess.py`, and
`preprocessed/t3_normalized_slice.py`, all below
`box/k16t3-20260903/`. Full commands are recorded in
`t3/RUN_COMMANDS.txt` and `t4/T4_HANDOFF.md`. The prepared-input manifests,
the 13-entry `t=3` result manifest, and all completed-run stderr/control gates
were rechecked before sealing this report.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `23011`.
- Body SHA-256:
  `3ec8e14915c224474701f484da44d8933b57004872151c748db94a7a5ebf19f9`.
- Frozen basis: `57aec76ad26db5f05dd8095852084b1e9f18c7ba`.
