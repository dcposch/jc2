# 0040 cross review — Fable 5.1 (adversarial synthesis + exact Moh-map composition)

2026-09-09, lane `/tmp/jc2-lane.uOfFHK`, frozen basis `0d39df3c9fd69c939a8420c54d03228b9077777d`, target 01:39:08 UTC. All ten charged inputs were read whole before writing; their sha256 values match `charged-inputs.list` byte for byte (pins in `box/ideation-20260909T0040Z-cross-fable5/input-pins.txt`). No live proof/gate, Volta body, current cross report, code, log, receipt, jc2-lean, agent, AWS/SSH/web/CAS or mathematical subprocess was used. Everything below is scalar/factored desk prose. Nothing here is promoted; root decides.

## 1. Astra Card 1: the two explicit Moh maps (exact composition review)

**Source object (named dependencies retained).** The child of the actual parent (125,75; M=(−75,105,123); d=(125,25,5,1); V=(2,4)) under the u_s=1 Prop 6.3 descent, after the accepted gate's centering: P (deg 25), Q (deg 15) in K[g,p], K algebraically closed of characteristic zero, tops H_old^5 / H_old^3, [P,Q]=c_old·g², c_old≠0, with H_old=p²S and S one of (p−g)³, (p−g)²(p−ag) with a²−a−1=0, or p³−g³. Dependencies not re-proved: child-data transport, monomial-J use of Def 5.1/Prop 5.3, top-face licence, the accepted positive-face Euler screen (report 07e53340, gate eade8ec8). Bracket convention is immaterial for landing in 15q because 15q allows any nonzero c.

**Map 1 (squarefree, [1,1,1]) — CONFIRMED.** A(g,p)=Q(−g,p), B(g,p)=P(−g,p). Top: H_old(−g,p)=p²(p³+g³)=H_Q exactly, so A_15=H_Q³, B_25=H_Q⁵; total degrees are invariant under g↦−g. Bracket: A_g=−Q_g(−g,p), A_p=Q_p(−g,p), likewise for B, hence [A,B](g,p)=(P_gQ_p−P_pQ_g)(−g,p)=c_old(−g)²=c_old g². The two sign flips (target swap, reflection determinant) cancel exactly as Astra states.

**Map 2 (double/single, [2,1]) — CONFIRMED.** With a²−a−1=0: 1/a=a−1=:t, ρ:=t²=2−a, and ρ²−3ρ+1=(5−3a)−(6−3a)+1=0, t=1−ρ. Substituting p↦−ap: (−ap)²(−ap−g)²(−ap−ag)=a²p²·(ap+g)²·(−a)(p+g)=−a⁵p²(p+g)(p+tg)², using (ap+g)²=a²(p+tg)². So H_old(g,−ap)=h0·H_ρ with h0=(−a)⁵, the double factor lands on M=p+tg and the simple factor on L=p+g, matching 15q's labelling. A=h0^{−3}Q(g,−ap), B=h0^{−5}P(g,−ap) have monic tops H_ρ³/H_ρ⁵ and degrees 15/25 (invertible linear substitution, a≠0). Bracket: h0^{−8}·(−[P,Q])∘φ·det φ = a^{−40}·(−c_old g²)·(−a)=c_old a^{−39}g²≠0. The conjugate a↦1−a sends ρ=2−a to 1+a, the other root; t≠1 because a=2 is not a root, t≠0 because ρ≠1. Both embeddings are covered.

**Two-partition composition with 15q — CONFIRMED at exact scope.** 15q is a field theorem with arbitrary lower parts and arbitrary nonzero c, so no coefficient dictionary is needed; the maps are literal instances of hypothesis (S). Consequence: for THIS named parent's u_s=1 child, partitions [1,1,1] and [2,1] cannot occur; only H*=p²(p−g)³ remains. Not established: attainment of any partition, the M2=90 sibling, arbitrary (75,125) pairs, other u_s=1 rows, all D125, JC2. Distinction from the old F2 dictionary: the old attempt needed coefficient-level identification with the six polygon ideals; here only tops, degrees and the bracket form enter. GAP items: none found. The scope is exactly what Astra wrote.

## 2. Triple/cubic direction (H*=p²L³, L=p−g)

**Typing.** The [3] stratum is an exactly typed receiver (Volta intake B): ordinary A15/B25 over Kbar, tops H*³/H*⁵, [A,B]=c g² (name swap only negates c). The WF centralizer step ports: on H*=h0, T=1/p, u=g/p give T⁵=(1−u)³/h0, valuation 3/5 at u=1 forces total ramification of index 5, hence geometric irreducibility of the generic fibre; the rest of WF §2 uses only that. Nothing about the local charts ports by resemblance.

**Independent cross-derivation of the unsealed late-T hint (UNREVIEWED, not a premise; Volta's body not read).** Lemma: R_s of combined degree 5 with R_0=H and polynomial centralizer K[H]; T a homogeneous polynomial of combined degree N in (s,g,p); if ord_s[R,T]>N then [R,T]=0. Proof sketch: the lowest coefficient T_l (l≤N) satisfies [H,T_l]=0, so T_l=κH^{(N−l)/5}; subtract κs^lR^{(N−l)/5}, which leaves the bracket unchanged and raises the order; a nonzero degree-N form has s-order ≤N, so the process ends at T∈K[s][R]. Application: T of (3) has N=35. For H*, H*|F_j² gives pL²|F_j, so j≤12; at j=12, F_12=λpL², ord G≥24 gives ord[F,G]≥36, hence ord[R,T]≥36>35 and [F,G]=c s³⁶g² exactly. Identity (4) at order 24 (35−24=11 not divisible by 5) forces G_24=(5/9)F_12²/H*=(5/9)λ²L. Then [pL²,L]=[p,L]L²=L², so [F_12,G_24]=(5/9)λ³L²≠c g². This reproduces the hint's three displayed quantities and kills j12 for H*, conditional on the mechanical H* ports of (2), (5), (6) and ord G≥2j.

**Consistency control against 15q (zero cost).** The same argument at golden j12 gives G_24=(5/9)λ²L and [pLM,L]=L((t−2)p−tg). Restricted to p=0 this is −(5/9)λ³t g²; restricted to M=0 (p=−tg) it is (5/9)λ³t(t−1)²g². These are exactly 15q's two chart values c=−5λ³t/9 and c=5λ³t(t−1)²/9, so the two-chart disagreement is the non-proportionality of one global degree-2 form to g². This ties the new lemma to the accepted proof rather than replacing it; if root accepts the lemma, golden j12 acquires a second, chart-free derivation. If the lemma is refuted, 15q is untouched.

**Budget arithmetic (Card 2) — CONFIRMED.** With u=ord_p F_j, v=ord_L F_j: u≥1, v≥2, u+v≤15−j, not(u≥4 and v≥6). p-chart margin 15j/(6−u)<36 (u≤3), L-chart coalesced margin 23j/(9−v)<36. Case sweep: u≥4 always passes through v; u=1 fails only at j12 (v=2); u=2 fails only at j11 (v=2, 23·11/7>36); u=3 fails only at j8, v=4 (40 and 184/5). The list {j8 p³L⁴, j11 p²L², j12 pL²} is exactly right. Note the p-chart coalesced lemma of 15q does not port for u≥4 (deg U≤5 reaches the Y⁵/Y⁴ comparison), so those cases must go through L, as Card 2 says. A one-step late-T extension does not kill j11: the order-33 equation [H*,X]=(10/9)λ³p³L² is solved by X=−(5/27)λ³p², so j11 needs the moving orders 34–36.

**ONE next exact missing lemma after the late-T task:** the L-chart cubic normal-form lemma with its regime trichotomy: existence of ζ over a finite Puiseux extension with R_s=ζ³+a_sζ+b_s (inflection-point shift kills ζ², cube root of the unit), explicit orders of a_s,b_s, the first-contact statement for F (analogue of WF §5), and the split into coalesced (ord a_s≥2r, ord b_s≥3r) versus 1+2 versus 1+1+1. Card 2's coalesced lemma is stated conditionally on exactly this, and j8/j11 cannot even be typed without it. Its Euler/UFD core (n<ν/r, 25−ν−nh>25−5ν/(3r)>0, P⁵=Q³, W=Y³+aY+b forcing U=0) checks in prose.

## 3. Fable blind: general-e census and the e=3 toy (self-attack)

- **Census arithmetic** rechecked: d1 3j=8e−4 needs k≥1 (a double factor exists), which the blind left implicit; without it (e,k)=(2,0) appears vacuously. With k≥1 the resonant set is {(5,2,d1,12),(3,1,d3,4),(3,1,d1,7,q0=8)} as stated; d2 has no solution for any e (2e≡1 mod 15 gives j>2e−1).
- **Calibration, not falsifier.** With 15q promoted, a point of the (9,15) toy ideal can say nothing about e=5: the blind's "single-chart WF argument is suspect" inference is heuristic, not logical, and §2 above shows the golden j12 leaf also has a global derivation, so "resonance realizable with one chart" is the wrong question. The size/performance claim (191 slots, 276 rows, msolve envelope) is unverified and irrelevant to any consumer.
- **Launch verdict: NO.** No unretired client has a (9,15) pair with top p²(p+g) and J=c g²; explicit NO HIT. Withdraw OPEN[E3-TOY-POINT]; keep OPEN[WF-RESONANCE-CENSUS] only as a corrected desk fact with the k≥1 clause, no test.

## 4. Sol Card 1: logarithmic cusp-residue

**Tautology at every s-order; STOP (DUPLICATE/COSTUME by Sol's own rule).** Φ=B_s³/A_s⁵ is a rational function with Φ|_{s=0}=1, so log Φ=Σ_{k≥1}s^kψ_k with ψ_k rational in (g,p) (poles on H=0), and the s^k coefficient of dlogΦ is the exact form dψ_k, whose residues vanish identically at every pole. For fixed s≠0 the residues are the integers 3ordB−5ordA and their sum on any line is the degree of a principal divisor, zero. The wedge identities dA∧Ω=3A[A,B]dg∧dp are the Jacobian condition rewritten. Moving-root and coefficientwise bookkeeping both carry no information; no pole ledger is needed. Sol Card 2's script is prohibited and its endpoint is accepted in 15q; §2 supplies the omitted-term worry a chart-free answer, so no duplicate instrumentation.

## 5. Priorities, all-degree limits, one systems choice

Proof: (1) review the late-T lemma and the H* ports after Volta is terminal, with the golden restriction control above; (2) root's decision on Card 1 (two partitions retired for the named parent); (3) the L-chart normal-form lemma, then j11, j8, then the separated regimes; (4) source scope beyond this parent (M2=90 sibling, arbitrary 75/125, u_s=1 rows) and K16 all-m stay open and are untouched by any of this. Disproof: no new candidate; common4 h is retired by 15q; the only typed unretired finite object is the [3] receiver, whose old 12-variable and 141/598 charts are non-exhaustive; McKay–Wang closes only constant-J two-root tops and is a classical screen. All-degree limitation: even a complete H* exclusion closes one named parent, not D125.

Systems: at most ONE — the 2 KB delta index in packet.md, measured only from existing receipts, whole reading unchanged. Not the linter: its only demonstrated catch (duplicated row) is syntactic, while the actual failure mode (stale row-26 revival, syntactically complete) is semantic and neither tool can detect it.

## 6. What changed after blind, gap, bounded next test

Changed: 15q promoted, so my cards A/B lose their target and Card 1 now composes with a theorem; six clients existentially unit; Volta's classical screen; the triple stratum is the frontier with an active late-T derivation. Exact remaining gap: H* with j≤11, all L-chart regimes, and the j8/j11 endpoints. Bounded next test (≤30 min prose, no compute): verify the late-T lemma's finite-kernel induction at every order ≤35, the H* transfer of ord G≥2j, G_24=(5/9)λ²L, [pL²,L]=L², and that the golden specialisation reproduces both 15q chart scalars.

Scope limits: composition verdicts are conditional on the named Moh dependencies; §2's lemma and control are my desk derivations, unreviewed; no manifest hash was checked against canonical files, only the lane copies; no exit price is asserted.

<!-- BODY-END -->
