# Hostile review: degree-at-least-three adjoint-defect gate

You are Opus 5 at maximum reasoning, the genuinely different-model hostile
reviewer for the plane Jacobian-conjecture campaign. Work in
`/Users/dc/code/math/jc2` on the exact charged files below. The producer is
explicitly provisional: independently reconstruct it and search for the
strongest counterexample to every inference. Do not infer correctness from a
seal, manifest, canonical ledger, prior Sol reasoning, or agreement with any
other report.

Charge these exact bytes:

```text
4eb8fc8ba526441c609448602a122601330ed99db9264d91261ea4cfb810a113
  xmodel/bd-a2-d3plus-pg-defect-sol56-20260830.md
f11ef6340961d41fdca88c84ded7f825c0868864d291cdf7abfe13cadbabd68c
  xmodel/bd-a2-d3plus-pg-defect-sol56-20260830.md.artifact.json
ba69b33fba97215ac3e4b2481b06917baf004e884508a15aef136575a9440778
  xmodel/block-descent-structure-coordinator-integration-sol56-20260830.md
6a8558e42a67f1ec5c8bcf12321b6e6805b30a570eba17862c6e7e24cd631b08
  xmodel/bd-a2-rational-forest-coordinator-integration-sol56-20260830.md
17f41e706bcae7556cd99e019263e7fad254201fe51f9a24ed158b2be68560c6
  xmodel/bd-a2-normal-singular-quadratic-incidence-coordinator-integration-sol56-20260830.md
34b7c50638509e50c308766db4d21051824740c64afb1188bff88b248d27f1a3
  xmodel/bd-a2-one-attachment-disc-max-hostile-review-gpt55-20260830.md
```

The last two files are analogy/scope checks only; do not import a quadratic
conclusion into degree at least three. Audit every item below with literal
arguments.

1. For an integral normal Cartier hypersurface
   `X ~ dA+3B` in `P2 x P1`, `d>=3`, independently compute the hypersurface
   cohomology and adjunction. Verify every vanishing and the factor of two in
   `h^2(O_X)=(d-1)(d-2)`. Check that no hidden flatness, Cohen--Macaulay, or
   smoothness assumption is being used.
2. Given only a dominant rational map `A2 --> X`, test the passage through a
   resolution `r:Y->X`: does one really obtain a dominant rational
   `P2 --> Y`, and is every smooth projective unirational complex surface
   rational in the exact strength needed? Check `H^1(O_Y)=H^2(O_Y)=0`.
3. Reconstruct the Leray sequence for `r_*O_Y=O_X`. Check support and
   finiteness of `R^1r_*O_Y`, absence of a relevant `R^2`, and whether the
   claimed isomorphism with `H^2(O_X)` and the pointwise length sum are
   canonical and resolution-independent.
4. Audit the trace/Grauert--Riemenschneider inclusion
   `r_*omega_Y subset omega_X`. Verify that it is an ideal times the
   invertible dualizing sheaf, that its quotient is actually
   zero-dimensional, and that GR vanishing applies to this projective
   resolution of a normal Gorenstein surface. Pay special attention to the
   direction of the inclusion and to possible torsion or non-saturated
   rank-one phenomena.
5. Derive the cohomology of
   `0 -> r_*omega_Y -> omega_X -> Q_GR -> 0` without skipping endpoint
   groups. Decide whether it really gives
   `H^0(omega_X) ~= H^0(Q_GR)` and hence
   `length Z_GR=(d-1)(d-2)`. Independently verify the asserted pointwise local
   duality with `R^1r_*O_Y`, or state the exact correction if only equality of
   total lengths is justified.
6. At `d=3`, verify that `omega_X=q^*O_P1(1)`, that the full two-dimensional
   canonical system is exactly pulled back from `P1`, and that its evaluation
   on `Z_GR` is an isomorphism. Check surjectivity/dominance of `q:X->P1` and
   exclude a hidden vertical hypersurface case.
7. Let `T` be the scheme-theoretic image of `q|Z_GR`. Attack the length
   argument carefully: whether a nonzero section of `O(1)` vanishing on a
   length-at-most-one image must vanish on `Z_GR`, whether
   `length(T)<=length(Z_GR)`, and whether equality forces
   `O_T ~= q_*O_Z`. Decide whether the final statement is really an
   isomorphism `Z_GR ~= T` as schemes.
8. Check the claimed exhaustive length-two alternatives, including embedded
   length-two schemes at a singular point of `X`, two reduced points over one
   base value, and the meaning of nontrivial base-parameter action. Preserve
   the distinction among support points, branches, places, ramification
   primes, sheets, and defect modules.
9. Independently test the topology-only negative control
   `x^3+y^4+z^5=0`: compute its geometric genus with the correct strict/weak
   Newton inequality and verify that a good resolution really has a rational
   tree. If this example is wrong, replace it or delete the control; do not
   let a bad control contaminate the theorem.
10. Give separate verdicts for the numerical cohomological defect, the GR
    adjoint scheme, the `d=3` horizontal-image theorem, the negative control,
    and the proposed successor. State the maximum safe theorem and the exact
    dependencies/firewalls. No result here supplies a block, a finite
    presentation, basis invariance, occurrence, a map, or JC2.

Use targeted primary literature only where needed to verify a named theorem;
cite it precisely and distinguish your own inference. Do not run local heavy
CAS or Singular. Do not inspect, list, search, stat, build, modify, or control
`jc2-lean`. Do not read any sibling ideation or q=6 report, any other active
external-model prompt/log/report/run receipt, or any uncharged canonical
update. Do not edit inputs, Git state, or canonical artifacts.

Return itemized `CONFIRMED`, `CONFIRM_WITH_CORRECTIONS`, `GAP`, or `REFUTED`,
with exact repairs and a maximum-safe theorem. This task makes no new
exit-price assertion: emit no `charge_basis={...}` line; receipt status
`ABSENT` is expected. Hard cap 16,000 tokens / 60,000 UTF-8 bytes. Write
exactly one report:

```text
xmodel/bd-a2-d3plus-pg-defect-hostile-review-opus5-20260830.md
```

End with one standalone `<!-- BODY-END -->` line and no seal block.
