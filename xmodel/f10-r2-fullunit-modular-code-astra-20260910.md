# r2 fixed89/root0 positive full-unit instrument

STATIC / UNREVIEWED / UNEXECUTED. First action16:04:00 UTC on2026-09-10; reserve16:24, hard stop16:27. All ten named pins matched before bodies and owned targets were absent. Baseline remains a future literal pin only and was not accessed. No runtime, worker, registration or source outcome authority.

## Delivered code and exact scope

The owned package has new isolated entry.py, produce.py and check.py, a precise CONTRACT, disabled plan, input pins/read scope and literal entry diff. Original arithmetic.py/check_arithmetic.py remain separately pinned and unchanged. Shared entry contains metadata/JSON/file handling only; its only change from the frozen highest entry is job/receipt/refusal naming. New tag f10-r2-fullunit-modular89-v1 cannot authorize itself: ROOT's authenticated pre-exec registration and exact isolated sibling/source/native/argv/cap staging remain external prerequisites. No active registration or runtime caller is included.

Both implementations independently reconstruct literal monic P7 at tau2/7 through their separate rational arithmetic, verify fixed prime89, root0 and nonzero derivative, and require every dense coordinate denominator to be an89-unit before projection. They authenticate all20 used original polynomials: Psi5/6/7, K1_0..6, K0_0..8 and U, with weights5/6/7,14-i,16-i and3 and no S/vartheta support. All original six-coordinate supports and all seven coefficient coordinates remain checked, including positive-z terms and coordinates killed by root0. Conservative integrality includes even unused zero-cofactor slots.

At z=1 the generator order is exactly Psi5/6/7; K1_1..6; K0_1..8; K1_0-U*K0_0. All15 additional slots remain, including zero/dependent cases. The last polynomial is rebuilt from authenticated U,K1_0,K0_0, not mislabeled as K1_0 alone. Its two constituent weights14 and19 give a weight bound19 and class4 modulo5; no false integer homogeneity is assumed. No scale, scalar rescaling, quotient basis, field-factor selection, source guard normalization or highest-regularity assumption enters this special identity.

## Bounded positive search and sound acceptance

The producer searches one literal finite class0 cut: rows are all monomials of weight<=30,class0; the three f blocks use all opposite-class multipliers of weight<=30-h, and each additional g block all opposite-class multipliers of weight<=12. Expected247 rows and424+330=754 columns are checked by the future code, not claimed as measured actual matrix output. A mismatch is INCONCLUSIVE.

No product is truncated. f products have weight<=30. The other products have crude bound19+12=31 and class0, hence integer weight<=30; every actual key is also checked against the row inventory. Deterministic finite-field forward Gaussian elimination and back substitution choose free coordinates zero. The producer reconstructs all18 cofactor polynomials and separately checks their literal product sum equals1 before publishing a candidate. An inconsistent cut, bad place, denominator or bound is INCONCLUSIVE; no rank-based nonunit/properness claim or larger cut follows. No completeness theorem is read or used.

The independent checker loads only original check_arithmetic.py, reconstructs the whole source rows and mixed generator afresh, and consumes no matrix/pivot/reduced-row/rank data. Every indexed cofactor slot, exponent, residue, degree and class is checked, including empty slots. It accumulates the full identity in a separate dense31x16x11 box, first rejects any product above weight30 or wrong class, then compares EVERY position, including absent/zero/outside-simplex positions, with the constant1 target. Producer and checker share no mathematical helper.

The successful status is exactly FULL_SPECIAL_UNIT_CHECKED_NO_SOURCE_OUTCOME. It certifies the literal special polynomial identity only. Accepted conditional17zzw additionally requires an actual independently checked WHOLE special highest-projective certificate for the same local model and prerequisite source/data acceptance to deduce generic/source zero. This package has no such actual certificate or promotion. Highest regularity is not needed for the correctness of a positive literal identity; it is needed for the separate finite-model lift. Source failure, point, nonregularity, all-r or JC2 conclusions are absent.

## Wire, caps and failures

Canonical ASCII JSON uses rational/residue/exponent strings, duplicate/numeric-token rejection, sorted compact bytes plus newline, strict all18 label order and zero=[]. Cofactors have caps25/24/23 then twelve for each of15 g slots; no nonzero-class constant insertion. Matrix bounds and loops are fixed; no prime/root/order/representation fallback. Full certificate cap2MiB, each receipt32768B, future baseline15MiB reader with literal1652675-byte/hash match. Existing outer600wall/500CPU/2GiB/16MiB ceiling is prospective only; ROOT must assign child profiles/deadlines. No runtime or coefficient size/cost is measured.

Metadata boundary runs before scientific imports/parsing. Source and baseline bytes are rechecked; output creation is exclusive/no-follow with fsync/readback/0444, certificate is reread before checker receipt. ROOT pinning before entry import is indispensable; self-supplied hash strings and DMI are bindings, not authentication. Source races, incomplete output or missing receipt cannot constitute success. Candidate-stage ValueError returns INCONCLUSIVE_NO_CERTIFICATE; checker sufficient-place failures return INCONCLUSIVE_FIXED_PLACE; false wire/identity rejects. No fail path proves properness.

## Meaningful negative design, not implemented or executed

After positive checking, select a specialized nonzero generator with a nonzero cofactor, then change one EXISTING nonzero cofactor term by+1 mod89, deleting it only if it becomes0. A positive sum1 guarantees such a pair. The class/degree stays unchanged, including wrap88-to0. The identity difference is one monomial times a nonzero generator, nonzero in F89[Y1,Y2,Y3]. It must reach the final literal-identity error, not an earlier parser/class/pin check. Record the index/term/deletion and old/new whole-byte hashes; reverse the edit and require exact restoration of ALL original certificate bytes. No constant is inserted into a nonzero-class cofactor. No mutator, fixture, authority or runtime is authored.

## Readback and remaining obligations

All ten exact inputs were read whole, with clipped combined reads recovered explicitly in READ-SCOPE. All three owned sources were read whole without running them. Literal source naming, mixed-row construction, positive Gauss extraction, full independent identity, wire/caps and negative-control proof were manually reviewed. No syntax/import/AST/compile/test, mathematical subprocess/enumeration, baseline access, network or worker action occurred. This is producer static code, not independent acceptance.

One FIRST source review is pending. Fixed-place data validity, whether this cut yields a positive witness, runtime and the separate highest certificate remain unknown; the cheapest next check is a single ROOT-selected registered candidate/check after review, with the designed genuine corruption only after success. No new campaign OPEN ID or follow-on authority. Own whole/raised-OPEN/collision and current-pin checks precede the marker, then transaction/custody. All writers IDLE at terminal handoff.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7469`.
- Body SHA-256:
  `a37236a6d4e874efa3f285eb82edfe8f991d91b5d5be2dd0a94e9ca8b5ff2bd0`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
