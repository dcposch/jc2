# Hostile review mandate: `BD-FIX3` affine-linear residual control

You are Fable 5 acting as a different-model hostile mathematical reviewer in
the plane Jacobian-conjecture campaign. Work autonomously in
`/Users/dc/code/math/jc2` on frozen git basis
`0d7544ebd5cb12def6bac892646010301098be3c`.

Review in full:

```text
67e1305ba8dc452af6e120b37e24c102856ac5236d9140ffae9a0446c28261bc
  xmodel/bd-fix3-affine-linear-residual-control-producer-sol56-20260830.md
  body 16015 bytes / 0a82cca57e2a3d58fec256256c581f87483dae3301459a6b202cb057e6f1902b
```

Binding/provisional dependencies to distinguish carefully are:

```text
xmodel/block-descent-structure-coordinator-integration-sol56-20260830.md
xmodel/block-descent-d2-galois-obstruction-producer-sol56-20260830.md
xmodel/block-descent-d2-galois-obstruction-hostile-review-opus5-20260830.md
xmodel/bd-fix3-targeted-source-lead-sol56-20260830.md
```

The producer claims a reduction, not closure: an affine-linear Miranda cubic
block is forced to `W!=0`, `S_X=empty`, and irreducible ramification support.
It then constructs a smooth explicit cubic cover satisfying all known
cover-side filters, but with no `A2` first leg; hence the exact remaining gate
is redundant normalization-boundary sheets for an actual everywhere-etale
quasi-finite map from `A2`.

Independently reconstruct and adversarially audit all of the following.

1. Verify every charged hash, basis, seal, and dependency/lifecycle statement.
2. Re-derive the Miranda multiplication/binary-cubic dictionary used here,
   and the claim that cubic monogenicity is equivalent to `Phi(r,s)`
   representing a unit. Check conventions and any hidden primitivity or
   Gorenstein hypothesis.
3. Audit the affine-linear partition: common coefficient zero; the incidence
   resolution and `Cl(Y)=Z/3`; the source-unit contradiction; absence of
   common coefficient zero; `S_X=empty`; why `W=0` is excluded; and the
   affine-line torsor under `O_P1(-3)` with `Cl(Y)=Z` and constant units.
4. Attack the reducible-ramification exclusion via localization. Check whether
   all ramification components have nonzero relevant classes and whether the
   rank argument really forces a nonconstant unit on `U=Y\R`.
5. Audit the irreducible residual: solve the ramification equations over and
   at `Crit(rho)`; justify `R=P1\Crit(rho)` as a scheme/support; check
   fixed-sheet input, reducedness, adjunction/Hurwitz, `[R]` being a generator,
   `O(U)^*/C^*=0`, `Cl(U)=0`, `H_c^1(R)!=0`, and `chi_c(U)>=2`.
6. Recompute the explicit control
   `uX^3+X^2Y+XY^2+vY^3=0`: finite flatness, smoothness/integrality, charts,
   ramification parametrization, transition divisor, Picard/class group,
   units, Euler characteristic, branch/fixed third sheet, and the exceptional
   triple-root point. Look specifically for a singularity, missed branch
   component, discriminant multiplicity, or false `R=G_m` assertion.
7. Independently check the provisional Opus strengthenings on the control:
   injection `Z[R]->Cl(Y)`, nonprincipal different/nonfree relative dual,
   and the Delone-Faddeev no-unit norm claim. Do not promote a reviewer-derived
   result merely because the control is consistent with it.
8. Audit Lemma 4.1. Does `Cl(U)=0` really force the image of a dominant etale
   quasi-finite `A2->U` to miss only finitely many points? Check normality,
   effectiveness/principality of a missed divisor, regularity of the rational
   function, and injectivity of pullback.
9. Check the Euler argument showing the first leg cannot be finite over its
   image, and verify the redundant-sheet countercontrol is actually etale,
   quasi-finite, surjective, and nonfinite with the stated source.
10. Audit every general-cubic corollary in section 6. In particular distinguish
    ramification support from discriminant divisor, justify or refute
    `g2^*D=2R_D+T_D`, squarefree divisorial discriminant, `S3`, and the claim
    about the quadratic discriminant field.
11. Try to break the proposed `BD-A2-FIRSTLEG` successor or sharpen it to the
    cheapest exact prove/falsify/bypass gate. Do not infer that `g1` equals its
    image or is finite; boundary components may map onto curves covered by
    other sheets.
12. Return an itemized verdict (`CONFIRMED`, `CONFIRM_WITH_CORRECTIONS`,
    `GAP`, or `REFUTED`) and the maximum exact statement safe to promote.

Do not inspect, list, search, stat, build, modify, or control `jc2-lean` in
any way. Do not run local heavy CAS or local Singular. Desk algebra and
seconds-scale exact controls are allowed. Do not edit canonical files,
scripts, dependencies, or the producer. Treat the MathOverflow post as a
lead rather than authority. Write exactly one report:

```text
xmodel/bd-fix3-affine-linear-residual-control-hostile-review-fable5-20260830.md
```

End the report with one standalone `<!-- BODY-END -->` line and no seal
block. Formal data are not maps; finite fibres are not attainment; branch,
ramification, discriminant, image, and nonproperness loci must remain
distinct.
