# F10 r1 retained-row transformer and independent checker — disabled preparation

2026-09-09, nonemptiness_certificate. INTERNAL-UNREVIEWED CODE, UNEXECUTED. First action16:38:21 UTC; controlling cap16:58:21 UTC. No compilation, import, mathematical subprocess, transformed artifact, runtime result or decision has occurred. The task prepares one representation and a validation plan; it authorizes neither dispatch nor solving.

## Outcome and exact scope

New owned code consists of transformer.py, checker.py and a byte-identical copy of the accepted execution_gate.py. authority.DISABLED.json has enabled=false and no host, caps or argv. VALIDATION.md supplies the finite future control plan. Existing mathematical/source files are unchanged. There is no new caller, supervisor, CAPRUN change, field-factor selection or solver frontend.

The accepted17b input supplies the exact leading algebra and formal source substitution, including the two identically-zero top rows, guard and17 retained rows. Accepted16r supplies the complete source/Euler/pole identities. Their mathematics is not re-proved or promoted by writing this code. The implementation still needs independent static review and a separately authorized capped runtime validation.

The source is ONLY exact.json SHA168bdfd3a791f24a6631de66ae4e71c3f209ee9eb0d21fc466f9aa802f5ad576, with its original12-variable/20-slot definition. This task read that artifact only at schema/variable/key/row-shape scope, not its coefficient bodies. Hashing the complete bytes is custody, not a WHOLE mathematical read. Exact seven input paths/hashes/read scopes are in input-pins.json; all allowed proof and code inputs were read WHOLE after hashes matched. No provenance, pending peer or other corpus was followed.

## One chosen wire and exact substitution

The coefficient ring is

    (Q[v]/p)[s,s^-1,u,ell,d0,v0,v1,k1,k2,k3].

Sparse wires use the fixed12-axis order u,ell,d0,v0,v1,k1,k2,k3,s,S,t,v. S,t are bookkeeping polynomial coordinates, not two extra free parameters. Each nonzero term is a sorted exponent vector plus canonical numerator/positive-denominator STRINGS. The s exponent is signed; all other exponents are nonnegative, with v reduced to0..6. Zeros are empty wires and remain explicitly mapped. This keeps negative Laurent powers instead of normalizing s or silently clearing them.

The transformer constructs p from the accepted factored E,L,R formula over Q[v]. It computes only the named inverses L^-1,w^-1,f5^-1 by Euclidean identities in Q[v], requiring constant nonzero gcd and multiplying the result back to1 modulo p. It never divides by an arbitrary coefficient of the finite product algebra. The finite-partition sum defines f5; the checker uses its independently written five-term rational formula.

The original parameter substitution is literal:

    d1=v*w^-1*s^-1, v2=w^-1*s^-2, k4=w^-1*s^-3,
    omega=w*f5*s^8,

with all eight lower parameters unchanged. Every original A/B coefficient, coefficient slot including zeros, forcing, residual, restoration coefficient and row is substituted from the frozen input. All20 IDs remain in the output; E1/S8,E0/S9 and GUARD/omega*a*b-1 must be zero and are marked IDENTICALLY-ZERO. The17 remaining IDs are separately listed but not assumed nonzero or solved.

No original source powers of degrees112/196 are constructed. The only inverse-pole products in the checker have the existing low-index bound i<j<=5.

## Independent checker and exact failures

The checker imports neither the transformer nor the old builder/checker. The producer groups coefficients by all exponents except v and uses dense Q[v] division. The checker performs flat rational-monomial multiplication and monomial-by-monomial division by its independently built fixed monic modulus. Producer binary powers and checker repeated multiplication are separate. They share only the unchanged metadata authorization helper, not algebra or serialization functions.

The checker independently verifies:

- Exact unreduced p, canonical basis and all named unit-definition/inverse identities. A changed modulus cannot define the checker’s arithmetic.
- All12 parameter mappings, including signed s powers and omega; both artifacts’ source bindings and the producer code hash through future authority.
- Every original coefficient/zero slot/forcing/residual/row under an independent substitution, not merely row counts.
- A's full cubic boundary parametrization; all A/B coefficient envelopes and k0,beta,gamma gauges.
- The complete determinant in S,t, all eight t-bands, target Delta from its factored Pi definition, all forcing terms by removing the diagonal term from the independent determinant, and every low residual coefficient.
- Every one of the ten inverse-pole slots by direct S=p*z^3-z^2+u*z,t=z^-1 substitution, including unexpected-pole rejection. No generic-u or sampled-boundary check is used.
- Actual leading a,b restoration: a*(w*s^3)=1, b*(f5*s^5)=1, and omega*a*b=1. Thus leading invertibility is checked rather than inferred from names or field irreducibility.

The residual polynomials themselves are generally nonzero elements of the free lower-parameter ring. A future verifier PASS means equality of representations and correct source conditions, NOT that those17 equations vanish or their ideal is proper/unit. The source endpoint remains the accepted conditional112/196 statement only after all equations are solved with the stated guards.

Fractions use Python arbitrary-precision integers; rational coefficient wires require canonical strings, and floats anywhere are rejected. There is no binary64 roundtrip or evaluation of textual polynomial expressions. Python’s installed decimal-integer conversion limit is not changed: if a future output exceeds it, refusal is an honest NONDECISION, not evidence of precision correctness. Runtime precision and malformed-wire checks remain outstanding.

## Disabled validation and cost disclosure

The unchanged helper retains exact physical host, registered full parent/child argv, cwd and input-hash checks. No future worker/caps/deadline is selected here. Any root-authorized run must first use fresh same-caller refusal/physical/current-pin/exclusive-output/cleanup controls for these new child paths, and inherit the existing bounded CAPRUN/file-limit supervision. This packet cannot reuse an old enabled registration merely because its helper hash matches.

VALIDATION.md specifies changed-object checks for a dropped row/constant, wrong inverse, wrong scale, wrong modulus, valid versus corrupted negative Laurent powers, omitted pole slot/genuine pole, >2^53 precision and omitted leading slot. These are DESIGNED ONLY, not generated or run; a separately reviewed finite control harness is still needed if the root chooses to automate them. No old PASS is relabeled as a new-code PASS.

The future checker receipt reports per-row original/retained terms, original parameter degree, retained lower-parameter degree and signed s range, v degree, numerator/denominator bit lengths, and artifact bytes. These are typed comparisons, not directly interchangeable total-degree notions. Wall/CPU/RSS remain supervisor telemetry. No transformed counts, size estimates, speed forecast, factorization or solver experiment has been performed.

## Static review, remaining OPEN and completion

The producer statically read the new full transformer/checker, unchanged helper and documents. Hash equality establishes the helper copy’s literal identity only. All new source is uncompiled/unimported/untested; even syntax and API execution compatibility remain runtime qualifications. No assert-dependent semantic checks are used, but optimized-mode evidence is NOT claimed.

OPEN quantities: transformed artifacts emitted=0; new-code runtime/mutation modes passed=0; actual transformed row sizes/degrees/bits and wall/CPU/RSS remain unmeasured. Cheapest next test is one separately reviewed, registered capped transformation plus this independent full-condition read-back and the finite changed-object controls—not a solve or a branch farm. If code review or any cap/check fails, retain bytes and stop NONDECISION without automatic escalation.

Own WHOLE read and own raised-OPEN check precede the marker. Source/code/report/transaction current pins and terminal custody accompany the handoff. All writers IDLE; no follow-on execution authority.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `8433`.
- Body SHA-256:
  `7dcec64adf8c3126fd2eac984c37ed89d929b94bf0bb8322e6caa307e26449d6`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
