# Contact-Gram deployment metadata preparation

Status: COMPLETE OFFLINE METADATA PREPARATION / DISABLED / UNEXECUTED.

First action: 2026-09-10 20:50:12 UTC. Fixed publication reserve: 21:12 UTC; hard stop: 21:15 UTC. This packet neither registers nor launches a worker. No scientific or authored-script execution is authorized or performed.

## Scope

Prepared a matching registration template, reference-only installation inventory,
setup template, native-collector reference, and ROOT finalization checklist.
All physical, native, clock and external-supervisor facts remain unresolved
placeholders. Original source bytes are untouched. There is no scientific,
runtime, installation, worker or allocation result.

## Deliverables and exact interface

The owned box contains ROOT-REGISTRATION.template.json (disabled
f10-contact-gram-runtime/v1), INSTALL-REFERENCES.template.json,
setup.template.sh, NATIVE-COLLECTOR-REFERENCE.md,
ROOT-FINALIZATION-CHECKLIST.md, PINS.json and READ-SCOPE.md. The native
collector is a pinned reference only, not a copied or rewritten source.
No producer, checker, authority, probe, mutator, dispatcher or CAPRUN copy
was created. The setup is deliberately setup-only, NOT a launcher: unresolved
placeholders cause refusal and there is no systemd-run or new supervisor.

The registration has all 14 literal commands in the dispatcher's exact order:
five authority refusals, valid, startup-producer, startup-checker, dummy,
produce, check, mutate, negative-uphi, negative-bernstein. Source flags are
-E -s -S -B; outer dispatcher/CAPRUN flags are -I -S -B, followed by unchanged
setpriv and literal science CLI arguments. Wrong-job startup-checker retains
its deliberately absent input name. Both mutation inputs are the frozen paths.
I compared every serialized vector against the frozen run/execute text,
including profiles, authority/output/stream names and the positive receipt.
This is manual metadata/source comparison, not runtime or syntax testing.

All 15 proposed staged basenames are distinct: seven code references, three
science-statement references and five future ROOT/native metadata records.
Nine executable/source roles and three science paths plus the native manifest
are pinned in the disabled registration. ROOT registration is not self-pinned;
the external finalized install inventory can pin its bytes. No future
certificate/receipt/mutant SHA is invented or embedded as a registration hole.

Source and native metadata stay outside the one physical 16MiB tmpfs.
Root0755 ancestry/root0444 files and science UID/GID0700 writer permissions
are explicit. The generated original certificate, both variants, ALL streams
and metadata share that mount. Durable replicas use a separately absent EBS
target, with a required future JSON INTEGER durable_device. One batch suffix
is shared by source/work/durable paths, main cgroup/unit and every timer,
harvest and cancellation reference.

The dispatcher consumes FILES-ONLY f10-native-closure/v1: 1–1500 entries,
at most 256KiB. ROOT separately authenticates collector directory/alias/
absence/python_path/cache observations. Neither R2 six-field enforcement nor
its registration-token schema is imported. Historical middle setup values
are documentary pattern only and were not reused.

## Lifecycle and retained outcomes

At invitation runtime was provisional and this was its sole conditional
child. ROOT later reported FIRST intake complete at 20:55 UTC: A–D/F confirmed,
E fail-closed confirmed, with UNMEASURED liveness risk because negative
30/29 may repeat much of positive 90/89 work. This is a lifecycle instruction,
not a new documentary input; the live/terminal gate body was not read.
The exact frozen profiles remain unchanged. Any timeout is STOP, not a
negative-control pass; no speculative rebalance is implemented.

A complete normal-2 producer artifact remains eligible for the SAME checker.
Checked sign-inconclusive outcome ends after 11 calls without mutations;
the positive path can finish all 14 calls only through both semantic
rejections. Wrapper science remains NONE, with no full-window equivalence,
source or JC2 conclusion. Source/runtime theory interiors were not re-reviewed.

## Raised OPEN items and cheapest next checks

Two groups remain deliberately unresolved; the templates are not dispatchable.

1. One offline launch blocker: ROOT must finalize and verify external full-unit
   550 CPU / 600 wall / 2GiB enforcement including dispatcher and unaccounted
   descendants, its start boundary and terminal/custody margin. Existing
   per-child accounting is not asserted to prove that bound. Cheapest next
   check: ROOT's direct review of its separately frozen OS supervision plan
   against the entire owned cgroup. No new guard is implemented here and no
   allocation occurs while the offline blocker remains.
2. One future observed-registration group: physical/Owner/boot/namespace,
   canonical executable/native pins, selected UID/GID, integer EBS device,
   original offset-explicit UTC values and exact installed-source/output
   absence facts. Cheapest check is separately authorized ROOT metadata
   observation AFTER allocation, then literal field/vector/hash validation
   BEFORE immutable enabled registration or any science. No such observations
   or authority are invented by this packet.

ROOT's later possible OS-primitives plan is not implemented or treated as
an observed guarantee here. All costs, native completeness and liveness remain
unmeasured. No optional hardening, worker action or extra science is included.

## Reading, publication and handoff

All nine exact inputs were current-hashed before fresh WHOLE text reads.
PINS/READ-SCOPE identify each; no provenance was followed. Installation-only
scientific references were neither reread nor freshly hashed. No coefficient,
baseline, candidate or fixture body/hash was accessed; no authored script,
import, syntax/AST/compile/test, mathematical subprocess or network was run.

Own registration, inventory, setup and documentation were read WHOLE before
seal; the two OPEN groups are retained rather than hidden. Target absence,
all current input hashes, owned hashes and expected transaction verification
are recorded in terminal custody. The final marker is added only after own
review; close/finalize/verify use basis
0d39df3c9fd69c939a8420c54d03228b9077777d. Exact seal/idle times are in custody.
No follow-on or execution authority is requested or inferred.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6499`.
- Body SHA-256:
  `5a9bf68e3a2c4eee0ca806423d7fb3c0045c316e12f2369440be0423e7926ea8`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
