You are Grok 4.6 acting as an independent hostile mathematical reviewer in the
Plane Jacobian Conjecture campaign. Work only in /Users/dc/code/math/jc2.
Never enter, enumerate, search, read, build, status, modify, or control any
jc2-lean path. Use local files only; no web, AWS, or heavy computation.

This is a fresh stable-basis rerun after an earlier custody-invalid attempt.
The Git basis must remain exactly

  c3598b92598c1596e6c6331f4c877619b432a115

Review this producer from first principles:

  xmodel/qe5-delta0-state-slot-map-r1-sol56-20260829.md

The producer has full-file SHA-256
`bccf157880bf868d6c204f300a62a2e96a2ee5ce4d1d2be94789079110155cb8`
and claims body bytes/hash
`18948` / `b27e7436eab58c75fbf5949626214c50942ff9ec6316b65a6069e765e7cecd91`.
Recompute both before relying on it.

## Frozen charged-source manifest

At launch, every mathematical source charged by the producer has the exact
full-file SHA-256 below. Verify every one at the start and again immediately
before writing your verdict. If any current hash differs from this manifest,
stop and write `INPUT_MUTATED / NO_PROMOTION`; do not salvage a verdict.

```text
6bea12ccd5c710d9ddd1c694ad7b65632ccedf75af0d93013486388fdb4f2c39  ladder/REDUCTION.md
40104334b5e21d6495f9857a6c13a2877ad0e31a67529c5f23243e7170cfaaaa  ladder/BOOK-OFFAXIS.md
ad9ced6c0420902f8cf63edf3636bed8633be511e925f208d24a24c35d71036d  ladder/SHEET6-DEPTH.md
59a2fa489f7f999b24aabc4c707254b7e54f8db02d69690226754e3224f9bdda  ladder/SHEET6-III.md
93adb7acedf4cf0fd56ffc68e4571649a04eab12e1271431449a79465a964bcb  ladder/SHEET6-MULTIPOLE.md
dd09069baeeaaa38644963571f929077af016cbbac664aaea9f3f50bee8f6d90  xmodel/sol-h5a.md
3b8bd5c9e2a1f4132cff4353e0e7e0fd9b4426cbeea0b0c13d092161018f5d2f  xmodel/grok-h5a-review.md
885e5cc261449890e23d54cb024f19f339424f6138938d2396c2e655244c7092  xmodel/m2-caseiii-two-pole-e5-local-index-r3-repair-fable5-20260829.md
9660ffe9302061c9daf7bc31c8eda57fb9412315283c868d8d072655d2033570  xmodel/m2-caseiii-two-pole-e5-local-index-r3-hostile-review-opus5-20260829.md
d996f57b8281d821753d773d88ebc5ee5d957fe2f6ffab5e0fe84e306842b872  ladder/TOWER-UNIFORM.md
c22e3a1f977fef94022f34378232fcc78148fa506501f71ec6a57313d6042ebc  cases/book_offaxis.py
```

The producer's own source table records older hashes for `REDUCTION.md` and
`BOOK-OFFAXIS.md`. That is known historical source drift, not a mutation during
this launch. Read the current files and inspect the exact diff from
`eaad172e59742ef8cfd055eace9bc6d3b2da8463` to the frozen basis for those two
paths. Decide explicitly whether the added U2/full-exit-floor overlays alter
any Q+E5 load-bearing premise, citation, or conclusion. Do not silently replace
the producer table; report the stale provenance and its mathematical impact.

## Required hostile reconstruction

Read every cited local source and the charged passages directly. Do not trust
the producer's algebra, citations, controls, or prior desk diagnostic.

1. Reconstruct the type contract. Decide exactly which fixed equal-state slots
   are needed for a local R2.2 pattern-existence verdict, which additional
   slots are merely the route-facing fail-closed envelope, and whether calling
   Section 3.1's record "minimal" is literally correct. Keep target `nu_G`
   distinct from incoming `nu_U`, and pattern existence distinct from ODE,
   Puiseux-edge, source, polynomial, route, or Keller realization.

2. For fixed positive `M_G`, forced target `nu_G`, and integer menu value
   `K=kbar_G`, prove or refute uniqueness of
   `X_G`, reduced `X_G/K=a/b`, `(dp,dq)`, `D`, `s`, `Sm`, `mmax`, and `kmin`.
   Identify every positivity, integrality, divisibility, gcd, sign, case, and
   provenance assumption. State clearly that `(k,lex,mults)` need not be
   unique even if a canonical witness can be chosen.

3. Re-derive Proposition 4.1 without copying it. Check each implication:
   `D=nu_G*(mu*s-Sm)`, strict NE gives `A>=s>=1`,
   `dq<=1+nu_G+D<=1+2D`, `gcd(M_G,nu_G)=1`,
   `M_G*nu_G | D`, `dq/D<=2+1/(M_G*nu_G)`, and
   `K<=mu*w*(2+1/(M_G*nu_G))`. Test whether it is a safe cap, a sharp cap,
   or an attainment claim, and whether rational `w` makes the integer menu
   finite and exactly enumerable without a numerical cutoff.

4. Prove or refute Proposition 5.1 as an iff statement for the R2.2 factor
   grammar. Treat `Sm=0` and `Sm>0` separately. Check congruences, exact
   handshake, S, R, strict NE, `dq<dp`,
   `mmax=floor((dp-1)/dq)`, `k<=s`, q-only orbit count, and the claim that a
   positive bounded partition of `Sm` exists iff
   `ceil(Sm/mmax)<=s`. Search for edge cases such as `s=0`, `Sm<0`,
   `mmax=0`, repeated roots, or an unaccounted q orbit. If useful, perform a
   bounded exact-rational exhaustive check, but it is only a bug finder, not
   the proof.

5. Inspect `cases/book_offaxis.py` directly. Verify whether `cell_check`
   searches an unbound `nu` including `nu=1`, omits N1
   `gcd(kbar_G,nu_G)=1`, and accepts the Section 6.2 profile only through
   `nu=3` while forced `nu_G=2` fails. Recompute the positive boundary
   fixture, the wrong-index leakage fixture, and the typed case-I
   fractional-`kbar` negative control. The case-I fixture must route to
   `NOT_APPLICABLE/FALLBACK`, never become a case-III death.

6. Inspect the R3 producer and Opus review directly. Decide whether `M_U` is
   genuinely unbound only in migration pseudocode or in a theorem. Check N1
   at every menu member, distinguish target index from arrival index, and
   decide whether the proposed exact fixed-index consumer has enough arrival
   and interface data to assert only pattern existence or anything stronger.

7. Decide the software consequence precisely. Does a mathematical pass license
   (a) writing a new standalone exact-rational certificate/checker,
   (b) changing any canonical verdict, or (c) migrating/replacing/bypassing
   legacy `cell_check` or changing `NUCAP=500`? Name every missing test,
   interface, arrival/source certificate, or separate software review before
   any migration. Fail closed on ambiguity.

## Output contract

Return exactly one primary verdict: `PASS`, `PASS_WITH_REPAIR`, or `FAIL`.
For every defect, give exact replacement wording or the smallest formula/type
repair and state whether any mathematical or downstream conclusion changes.
Include:

- invocation/model disclosure (`grok-4.6`) and stable Git basis;
- a complete pre/post source hash table for the manifest above;
- target full-file and body-seal verification;
- exact source citations with current line numbers;
- an independent arithmetic replay of all three controls;
- explicit arrival/interface and realization exclusions;
- the exact maximum safe consequence;
- a software-migration disposition;
- review risks and residual open obligations.

Do not include a `charge_basis=` line: this report makes no exit-charge claim.
Do not edit any canonical ledger, source, engine, prompt, run record, log, or
any file except your report. Write the report exactly to:

  xmodel/qe5-delta0-fixed-index-hostile-rereview-grok46-c359-20260829.md

Seal the report: define the body as all bytes before the literal `## Seal`
heading, and include its body byte count and SHA-256. Do not commit or push.
