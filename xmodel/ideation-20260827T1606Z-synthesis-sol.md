# Matched whole-portfolio model evaluation — `20260827T1606Z`

Packet sealed: 2026-08-27 16:06Z  
Coordinator synthesis: 2026-08-27 16:48Z  
Coordinator: Sol

Status: **FOCUSED BLIND MODEL EVALUATION AND CAMPAIGN ALLOCATION.**  This
two-model round does not reset the full-spectrum ideation clock.  Evidence
retains the tier and scope of its producer/review artifact.  Nothing here
proves or disproves JC2, a GGV family, K00 closure incidence, order two,
maximum twelve, either `G2` obligation, or global coverage.

## 0. Custody and blindness

Both Anthropic models received the same packet, contract, maximum-effort
setting, shell/tool boundary, and no-peer rule:

```text
packet  1306771f11885a846bc3b57c8ff7d0d40f0f2cfe5abc9a45e0477c8b9e7cbb60
Fable5  bb9c54afd086db383dcab3519f556f65ae5abb134ba3555f57c7a6cdbf045257
Opus5   9f9d3d053933560c6e555ce357025254d9237d989b4ba78f205dbb870939b4fa
```

The packet pins repository HEAD
`418e413593120d19e15e6546eb50c985f4b1f038` plus seven basis and six delta
hashes.  Both submissions reproduce them.

Fable5 is clean-blind.  It noticed by mtime that two files postdated the
seal, did not read them, and disclosed that fact.  Opus5 did not read Fable's
report, but did read the post-seal structural D35 addendum solely to identify
and withdraw a duplicate D35-cancellation candidate.  Mark that atom
`BLINDNESS-DEGRADED`; it receives no novelty credit.  The lower-face
`nu=17` mechanism does not depend on the addendum and remains independently
derived.

This is a matched whole-portfolio evaluation, not a clock-resetting full
round: Sol, Grok, and specialist research lanes did not submit blind
whole-list reports.  The last full round remains `20260827T1349Z`,
synthesized at 15:18Z.

## 1. Post-seal lifecycle corrections

The packet deliberately contained several unresolved or recently changed
claims.  Their later reviews control campaign use, irrespective of what either
blind report inferred.

### 1.1 Promoted after different-model review

Opus5's combined cross-audit (`d9e65315...`) independently confirms:

1. **D5G**, exactly as the raw determinant compiler and global-`H`-multiple
   custody theorem on the artificial fixture
   `R0_ARTIFICIAL_CUSP_CONTROL`.  It is not a genuine-family face or lower-
   row theorem.
2. **R6**, as complete parameter compression of R5's necessary rational
   endpoint survivors.  The monic degree-`h` survivor locus has codimension
   exactly `floor(h/2)`; at degree eight only `b=0,2` survive, both
   four-dimensional.
3. **K00 V24R6R1**, as exact emptiness of the selected leading-base/F10
   `D(W)` and normalized `D(k10_0W)` charts.  The exact syzygy shows that
   `W` already vanishes on the base scheme; it kills the chosen chart, not
   that scheme or the other minor charts.

Fable5's different-model review (`7ab758fa...`) independently confirms the
corrected R7R1 characteristic/de Rham theorem.  The normalized exact target
is attainable over `L=K(X)(p)`, `p^4=H`, exactly when all `q_n dX` are exact;
a finite target through `t^(N-1)` licenses exactly `n+22<N`.  The review
finds one additive correction: the D3 determinant ceiling is 34, because
`D35` cancels identically.

### 1.2 Provisional descendants

D5G35 (`f0fe0f5d...`, freeze `1668795f...`) compiles
`D0,...,D34` and freezes the unsolved 734-generator target

```text
D0=...=D21=0,  D22=1,  D23=...=D34=0,
```

with `D35=0` automatic.  Grok review `ba7162fe...` independently confirms
the complete compiler and conditional R7R1 cutoff interface.  The target
gate itself remains unsolved.

K00 V26R1F (`86742147...`, source freeze `3d04cc18...`, evidence
`f012cc1a...`) provisionally finds `I6(A)=0` and `B+I5(A)` proper of exact
affine dimension three in the six leading variables.  The V24R6R1 rollback
hold is released, but V26R1F needs its own different-model review and says
nothing yet about the full atlas or later grades.

### 1.3 Hidden-prefix correction

Different-model audit `5735ee90...` of Grok producer `43a949b2...` retains
the D3 windows, high-weight linear maps, mode schedules, endpoint branches,
negative control, and stated rank computations.  It withdraws the claimed
universal weight-15-first search.  On the producer's generic `Q`
(`delta=1`) and generic `P` (`delta=2`) strata,

```text
D1=0  =>  G1=(3/2)H F1,
D2=0  =>  H | F1^2  <=>  AB | F1      (H=A^2B).
```

Thus compatibility is already nonlinear at weight two on those strata, and
the unconditioned 127-parameter sampler, its finite-field trials, and stop
rule are invalid.  The `delta=4,8` perfect-square strata have weight-one
homogeneous modes, so the displayed implication is not proved there; they
need separate conditioned compilers.  Further retained corrections are:
for `4|delta` the first nonpolynomial scheduled mode is weight 13; shared
roots imply only `gcd(A,B)|v`; scaling preserves support, while translation
can leave the frozen windows.

This hidden test is a useful discriminator between the two submissions.
Fable repeatedly treats the high-weight system as the next incidence and
overstates how completely the tower eliminates the raw face.  Its local
`q1/q2` computations can remain necessary filters, but only after
intersection with the appropriate conditioned prefix scheme.  Opus instead
identifies the entire lower-prefix ideal as the main blocker and proposes a
sequential row compiler, so its lead survives the correction procedurally.

## 2. Deduplicated atomic evaluation

The preregistered 100-point rubric is: soundness/scope 25, novelty 25,
mathematical substance 15, JC2 leverage 15, decisiveness 15, and reusable
complementarity 5.  Unaudited theorem atoms are capped at 18/25 for
soundness.  Scores rank experiments; they do not promote claims.

| ID | Deduplicated atom | Score | Correctness-adjusted status |
|---|---|---:|---|
| O1 `NU17` | Regrade the known `(4,-1)` lower face; same determinant operator and D3 slot census, target `-tau^17`; sequential rows 1–17 | 88 | pre-review score; narrow core confirmed, full card rejected |
| O2 `FACE-EXP` | Face-normal endpoint differential `K^(-w(1,1)/m)dξ` with upper/lower instantiations | 80 | new at direct-edge scope; mixed rows not covered |
| F1 `Q1/Q2-GATE` | Higher R7R1 exactness gates on the upper survivor branches; explicit rank/residue tests | 72 | branch-P `q1` atom confirmed; broad decoupling refuted |
| O3 `FACEPIN` | Audit which face data are family-pinned rather than artificial/free | 71 | actionable scope audit, not a theorem |
| O4 `GAMMA-FILTER` | Lower-face multiplicity pruning via `K^(-3/4)dξ` | 68 | overclaimed; hold until mixed prefix is compiled |
| F2 `RECIP22` | Proposed reciprocity bridge from lower solvability to endpoint | 66 | genuinely new but conjectural; do not launch |
| F3 `CORNER-COVER` | General face-indexed characteristic tower | 55 | merge with O2; Fable adds all-row emphasis |
| O5 `FITMEM` | Infer a unique nonempty K00 minor chart from minor nonmembership | 42 | scope-invalid; stop |

The decisive novelty distinction is narrow but real.  The `(4,-1)` face and
its `B=x(z-1)^7` leading data already occur in campaign history.  Opus's new
content is the lower chart `x=tau^-4 xi`, `y=tau`, the exact target exponent
17, the regraded windows on the same raw lattice, the face-indexed endpoint
mechanism, and the proposed two-grading compiler checksum.  Fable's new
content is not the existence of R7R1 but the concrete higher-row operators
on the surviving upper-face strata.

### 2.1 Post-score hostile adjudication of O1

Grok review `72f10ad7...` confirms the lower chart identity, recurrence,
windows/census, `G17` handle and upper tail, the unmixed ODE, and necessity
of `gamma=3 mod 4` for the two-root ODE shape.  It refuses promotion of
`NU17` as scored.  The native control's upper face is `X^16-1`, not the
monomial inferred from `B^2`; raw-lattice and family pinning are open; and
28 `G` slots survive above the target, so determinant rows continue through
40.  The general endpoint iff and multiplicity sufficiency also remain
gapped.  Thus 88 records the information value of the submitted atom before
review, not the strength of the promoted theorem.  The correctness-adjusted
campaign value is the new grading plus a repaired full-tail compiler design.

### 2.2 Post-score hostile adjudication of F1

Opus5 review `46736edc...` (body `432645eb...`) confirms the branch-P `q1`
atom exactly: for monic squarefree quartic `A`, the integrable numerators are
the image of `Q -> 2AQ'-3A'Q`, an injective rank-13 operator into the
16-dimensional artificial D3 window.  It confirms the rank fixture, trace
descent, and the fixed `q2` residue `-3/64`.  The surrounding strategy is
refuted: lower rows remain load-bearing and G-completion has even-weight
kernel freedom through weight 12.  Several useful sharpenings first appear
in the review—branch-Q codimension four, a weight-three divisibility gate,
transversality, and `F2` surjectivity for `q2`—so those remain provisional
until another model reviews them.

## 3. Mathematical findings and limits

### 3.1 Fable5: precise upper-face gates

On the generic perfect-square branch `H=A^2` with squarefree quartic `A`,
Fable derives, and Opus5 independently confirms, the `q1` numerator map

```text
Q  |->  2A Q' - 3A'Q,       deg Q <= 12.
```

It has rank 13 in the 16-dimensional raw numerator space, hence three
linear exactness conditions.  The fixture `A=X^4-1` gives rank 13 exactly.
Its `q2` nonvacuity control `F1=0,F2=1` has residue `-3/64` at `X=1`.
These are compact, falsifiable gates and the strongest Fable contribution.

Limits: Fable did not compute the genus-three `Q` branch; special multiplicity
strata are untreated; and the gates are not the first raw compatibility
conditions.  Early rows and remaining `G` parameters stay nonlinear, so
the statement that no Groebner-style elimination is needed is withdrawn.
No `G2-PSC` transport follows from proposing the tower classes as packet
fields.

### 3.2 Opus5: the second grading

With `x=tau^-4 xi`, `y=tau`, `ftil=tau^8 f`, and `gtil=tau^12 g`, Opus
derives

```text
12 ftil_xi gtil - 8 ftil gtil_xi
  - tau(ftil_xi gtil_tau-ftil_tau gtil_xi) = -tau^17 J(f,g).
```

Its regraded windows recount the D3 lattice as `141+301=442`; `F_17` is
empty and `G_17` has degrees 0 through 7, so the endpoint retains a linear
handle absent from the upper weight-22 row.  Hostile review adds the
load-bearing tail: `G_18,...,G_24` contains 28 more slots and determinant
rows can continue through 40.  A complete positive fixture must therefore
satisfy all rows, not merely the prefix through 17.

Limits: the native control is `f=B^2-x-y^8`, whose upper face is
`X^16-1`; the report's monomial claim accidentally used `B^2` alone.  The
direct-edge endpoint calculation does not by itself govern the
full mixed recurrence.  The claimed complete lower-face classification was
supported by finite exact enumeration plus a proof sketch, not a complete
proof over all shifted/reducible strata.  The farm-wide
`gamma=3 (mod 4)` filter is therefore held.  The report sometimes treats
chain-pinning as established before its own FACEPIN audit, and its proposed
K00 minor shortcut makes the false inferences `M notin B => D(M) nonempty`
and “one live chart => unique live chart.”

## 4. Roster decision

**Retain both as standing equal co-researchers and send both every future
full packet blind.**  Equal standing means equal access, submission
contract, post-deduplication consideration, and review standards; it does
not mean equal scores or interchangeable assignments.

Fable5 is strongest in this round at conservative scope custody, exact local
differential calculations, and extracting small theorem-shaped gates.
Opus5 supplies materially stronger lateral reframing: it identifies a second
grading, connects face geometry to the characteristic transform, and designs
two-sided falsification checks.  Their best atoms are complementary after
deduplication.

Opus's roster admission does not depend on the broader `NU17` atom, which
hostile review has now narrowed substantially.  Its independently confirmed
V21 finite-truncation proof and superelliptic
endpoint mechanism already satisfy the campaign admission rule by changing
research and review decisions.  The present round is strong additional
evidence of unique capability over Fable5, not a replacement contest.

## 5. Allocation and launches

Do not increase the total GGV allocation merely because the narrow lower-face
core survived hostile review.  Within the existing GGV share allocate
approximately:

1. **12 points:** the review-confirmed three-object FACEPIN read, then a
   fresh lower-face compiler through `Dtil_0,...,Dtil_40`, with target
   `Dtil_17=-1`, every other row zero, and mutations that catch prefix and
   tail omissions.
2. **9 points:** the upper-face lower cascade at weights three through six,
   conditioned sequentially and stratified by multiplicity.
3. **7 points:** freeze the confirmed branch-P `q1` certificate and test its
   intersection with the relevant prefix strata; do not fund `q2` as an
   `F1`-cutting instrument.

Continue the chart-free K00 atlas and long exact AWS cross-checks
independently.  Do not launch Opus `FITMEM`, the farm-wide gamma filter,
Fable reciprocity, or a general Risch/Differential-Galois program yet.
Review remains asynchronous: safe work on the compiler skeleton may proceed
under rollback tags while the theorem reviews run in the background.

## 6. Stop conditions and next checksum

- The lower chart identity, windows, and target exponent passed independent
  replay; consume only that promoted scope.
- Freeze FACEPIN first, then compile rows sequentially through 40 and stop at
  the first inconsistent row or at a replayable all-row fixture.  A row-17
  prefix is not a complete object.
- Fable's branch-P rank/residue calculations passed.  Do not call them
  raw-jet obstructions until the conditioned prefix intersection is explicit.
- Recheck every claimed face/family transfer against the artificial-fixture
  boundary before expensive computation.

The next clock-resetting full round is due by 2026-08-28 03:18Z unless
significant news starts one sooner.  The next broad web sweep is due by
2026-08-28 13:48Z.
