cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
The gate is complete. Verdict: A CONFIRMED, B CONFIRMED, C CONFIRMED, D CONFIRMED with two declared GAPs. No item was REFUTED.

**What I did**
- Hashed all 20 charged snapshots at 06:50:29 UTC before reading any body. Every SHA-256 matched the charge list. Own targets were confirmed absent before the only writes.
- Read WHOLE the two ROOT reports, the static gate, helper, interface, addendum, three result summaries, both telemetry files and both fixtures. Read the bundle, certificate, mutation, authorities, observations and manifest by exact-field excerpt only, using jq for string length, regex and key listing. No integer was converted and no scientific subprocess ran.

**Key findings**
- **A.** The old ValueError is recorded at oldsolver rational line 218, called from generic_candidate line 243 during RREF solution extraction, with the helper setting zero only afterwards. The new run entered at 4300 and finished at 0 through the API setter. Old and new fixture files are byte-identical by my own cmp, and helper, interpreter, FLINT and descriptor pins are unchanged across both authorities.
- **B.** I re-derived h1 = N² − T + NT², h2 = 1 − NT, all others zero, and the sum equals 1. The certificate column order gives slots 0, 7, 14, 182, 189, and the bundle witness has exactly those five nonzero entries. The giant numerators match the decimal patterns of N², N and −N by regex. The certificate file equals the bundle certificate.
- **C.** The fresh child took the exact new bundle as argv[2], hash-registered as one of two extra pins. Deleting witness slot 0 makes mutation and bundle identical. Slot 0 in the mutation matches the pattern of N² + 1. The hand residual is T², so the exact identity message is the correct failure, not a parse error.
- **D.** Argv, caps, FSIZE, first-dispatch clock and cutoff, same-call PID/start-tick captures, seven controls, 80-file copy before stop and backup cancellation are all in the retained bytes. Recorded limitations are honestly labelled. Two GAPs remain declared: native closure completeness is a ROOT declaration, and TERM/KILL was exercised on a control unit only.

**Outputs**
- Report: `xmodel/f10-large-decimal-runtime-gate-fable5-20260910.md`, unsealed, with a standalone BODY-END as the last line and no raised OPEN, charge_basis or Seal.
- Box: `box/f10-large-decimal-runtime-gate-fable5-20260910/input-hashes-verified-0650Z.txt`, re-verified against the inputs.

The synthetic regression is sufficient support for a future, separately registered genuine-source retry under unchanged limits. This gate grants no launch and makes no source, unit, exclusion or JC2 claim. Finished at 06:56 UTC, inside the 07:04:29 stop.
DIVERT path=xmodel/f10-large-decimal-runtime-gate-fable5-20260910.md status=CLEAN body_bytes=11231 body_sha256=c8e9ffa41a806c13651baaf51f20c13cccbd112bb96360b63e0e28331a50202c
