# Blind whole-portfolio ideation synthesis — `20260824T1820Z`

- Packet cutoff: `2026-08-24T18:20:00Z`
- Synthesis close: `2026-08-24T19:03Z`
- Status: **COMPLETE / DEGRADED INDEPENDENCE**, six of six reports landed;
  the AS-owner and TD6-owner reports are useful submissions but do not count
  as blind votes on the accidentally disclosed root snippet
- Packet SHA-256:
  `12d50a31630f8dfb2664ce8953e371832457cd9faa7f0c13480a32e1e0c4379e`
- Root / Claude / Grok / AS-owner / cube-owner / TD6-owner report SHA-256:
  `551203ee45370c7fc3a6ef2c8bb788de06823a35d5c5374d59d57e660d1b8d83` /
  `4aa41026b7ec76c66f02688299eb1efc6964f4071d7479aeac9aae8d142679ef` /
  `a6f1e651b57b07a859b8168da8f721c1c21e9721b9727fcabae7535ac2b4897e` /
  `8ed1b09a46f787266d5772c315bc7806b7b2c24634940bec8bd0bf00a062a319` /
  `04c08b4b56c5d27214a9005094fa24da57328bd37668da3f413d33ad2147b0db` /
  `a3d56f67a3eea45db761ba72a41c2f424fc10e1509e50630c5515cacdb9866e2`

This synthesis deduplicates only after collection.  Blind convergence is an
allocation signal, not mathematical evidence.  Post-cutoff facts are recorded
separately and retain their own review status.

## 1. Portfolio verdict

Four uncontaminated scans independently selected the same new critical-path
client: the undeformed coprime `(9,12)` order-three fibre should be attacked
through its spectral norm, sharp polynomial-abc equality, and exact
degree-36 Belyi passport before any ambient primary decomposition.  All six
reports selected the same three-root portfolio after scope is ignored:

1. **maximum-12 spectral/Faber proof root:** finite DZ/Hurwitz orbit descent,
   then terminal and original Taylor-boundary reconstruction;
2. **AS/Witt disproof root:** a source-honest integer-quotient compiler and
   pointwise augmented-Fitting classification of the degree-ten successor;
3. **TD6 proof backup:** a source-typed stagewise `c1` factor-stratum atlas,
   using polynomial row-module certificates only after the parent open has
   been parameterized and rebuilding every genuine exceptional fibre.

The newly found Pinchuk 2021 paper is a bounded fourth audit, not a fourth
coefficient campaign.  Its elementary generalized DZ identity is already a
local maximum-12 client.  Its broad quasi-polynomial reduction changes the
global architecture only if its equivalence direction, termination, and
polynomial recovery survive a line audit.

The proof-side wall outside these finite gates is still universal landing or
cofinality: maximum-12 closure would extend a bounded partial-`y` theorem, not
settle JC2.  The disproof-side wall is still algebraization: neither a
finite-depth Witt survivor nor a rational-power q-map is a polynomial Keller
counterexample.

## 2. Deduplicated card matrix

| Fingerprint | Independent blind support | History label | Exact target | Decision |
|---|---:|---|---|---|
| `MAX12-SPECTRAL-DZ-ORBIT` | 4/4 uncontaminated; 6/6 total | `NEW-COMPOSITE`; prior DZ watch becomes an exact coefficient-derived client | Classify the coprime `k=mu=nu=0` equality fibre with passport `(3^12)|(4^9)|(20,1^16)`, descend every affine/scaling orbit including stabilizers, then impose the terminal and original coefficient boundaries | **Launch/continue at highest priority**; raw global Groebner is only a cross-check |
| `PINCHUK-QRED-SOURCE-GATE` | 3 uncontaminated plus 2 degraded | `NEW SOURCE`, audited after collection | Type every arrow of Theorem 3.4 and decide whether it is a polynomial reduction, a one-way q-polynomial necessary condition, or has a gap | **GAP / quarantine globally**; retain only the independently checked Theorem 4.1 identity |
| `AS-D10-GHOST/FITTING` | 3 exact-card reports; broad support from all | `KNOWN` lane, `NEW` source-quotient hardening | Decide pointwise solvability of the eight-by-nine degree-ten affine digit system over the original nonreduced corrected ideal | **Continue concurrently**; global module nonmembership is navigation only |
| `TD6-C1-SMITH` | Claude plus degraded TD6-owner; all keep lane active | `SCOPE-CONFLICT` after cross-pollination | A one-shot localized unit row-syzygy was proposed for all later rows | **Redesign stagewise**: later substitution is genuinely nonlinear in the 132 transport variables; retain the typed first-band parametrization and recursively rebuild earlier pivot strata |
| `D12-DIRECT-CARRY` | 1/6 | `SCOPE-CONFLICT` as written | Use degree twelve as the first characteristic-zero-compatible AS cap | **Redesign/defer**: no typed D12 predecessor or accepted-digit chain was supplied; build it only after the D7 compiler is proof-carrying |
| `PINCHUK-POLYGON/RESIDUE-A-DICTIONARY` | 1/6 | `NEW FALSIFIER`, numerical resemblance only | Compare Pinchuk's final polygon `conv{(-1,0),(35,6),(42,7),(0,0)}` with the residue-A branch-entry ledger in one typed dictionary | **Half-day reserve**; an exact mismatch is useful, resemblance proves nothing |

The AS-owner's spectral normalization/conductor card and every
passport/dessin card merge into the first fingerprint.  The discriminant

```text
Disc_g(g^3-f^4-3*k*f^2*g-k^3*f^2)
  = -27*f^4*(f^2-k^3)^2
```

gives that card a mandatory `k!=0` conductor split, not a separate avenue.
Likewise “ghost compiler”, “source quotient”, “integer carry compiler”, and
“augmented Fitting” are stages of one AS proof-carrying transition, while
“pivot logger”, “Smith gate”, and “polynomial syzygy” are one TD6 coverage
mechanism.

## 3. Exact new connection and cheapest proof attack

On the named undeformed fibre,

```text
W=g^3-f^4,
f*W'-4*f'*W=g^2*(3*f*g'-4*f'*g).
```

If `f,g` are coprime of degrees `9,12` and `deg W=16`, the last factor is a
nonzero scalar.  Thus `beta=g^3/f^4` is a degree-36 three-point cover with

```text
0:        3^12,
infinity: 4^9,
1:        20,1^16.
```

The ramification defects `24+27+19=70=2*36-2` leave no fourth branch value.
There are finitely many transitive permutation triples, hence finitely many
source-PGL2 orbits.  The unique index-20 point fixes source infinity and the
depressed normalization removes translation.  What remains is an isotrivial
scaling trajectory, possibly twisted by the finite deck group.

This is the round's strongest proof attack because it turns one singular
positive-dimensional coefficient fibre into a finite cover/descent problem.
It is also a falsifier: any surviving normalized orbit must be reconstructed
through the original Keller coefficients and Taylor polynomiality, and would
be the strongest current near-counterexample rather than something to hide.

The exact stop boundary is coprimality.  Common-factor strata, nonzero
invariant loads, `k!=0` components disjoint from the common cubic, the
polynomial-core leaf, and `(8,12)` remain separate mandatory branches.

## 4. Post-collection cross-pollination and adversarial findings

### 4.1 Stabilizer-aware orbit descent

The naive modulus-20 scaling claim is not safe until dessin automorphisms are
handled.  A post-collection root derivation gives a stronger finite audit.
The deck group fixes the unique index-20 point, is cyclic affine, and its
order `e` divides both `36` and `20`; hence `e in {1,2,4}`.  If `lambda` is a
scaling parameter and `eta=lambda^e`, then `eta` descends to the cubic Kummer
field.  Its character gives the three exact possibilities

```text
(e,N=20/e,a,S) = (1,20,2,14), (2,10,1,4), (4,5,2,4),
eta=u^a*v, v in C(x),
r8=C*eta^(-N)=u^2*R,
R=C*h^(-S)*v^(-N).
```

Combining this with `9*h*R'+6*h'*R=j`, a finite zero of `h` of multiplicity
`m` must have `m=3 mod N`.  The apparent `m=3` resonance cancels the leading
coefficient and therefore cannot equal nonzero `j`; all finite multiplicities
are strictly greater than three.  Since `v` has no divisor away from the
zeros of `h`, the degree at infinity is either noncancelling, which forces one
root and makes `h` a cube because `3|deg h`, or cancelling, which forces
`deg h=3*(number of roots)`, contradicting every multiplicity being greater
than three.  If the descent proof survives independent attack, the coprime
undeformed nontrivial-Kummer fibre is empty without enumerating templates or
Taylor coefficients.  This is **PRODUCER-EXACT POST-CUTOFF** at report SHA
`4a5ec8b08aab7a9ba5d7c22593efeb67621fea720128dd7051972f52cc9191e5`;
different-model hostile review is active.

### 4.2 Direct D12 proposal

Cross-pollination with the AS owner returned `REDESIGN`, not rejection of
degree twelve.  D12 is the first cap not ruled out as a characteristic-zero
degree by the maximum-eleven theorem, but there is no typed “D12 carry” until
the full D12 first-digit locus and every accepted lower quotient are built.
The cap-seven predecessor cannot be reused because it omits D8--D12 modes.
Finish D7 degree ten as the compiler/control; only then launch a separate,
manifest-tracked D12 depth schedule.

### 4.3 TD6 affine-certificate falsifier

Cross-pollination falsified the proposed one-shot affine certificate before
it was frozen as mathematics.  Direct source compilation of previous/current
rows on the 132 transport variables produces genuine monomials of degree at
least two.  The staged first-band parameterization is therefore load-bearing:
a localized affine row-module certificate can cover current-pivot roots only
on the earlier first-plus-previous open, not every earlier pivot divisor.
Those divisors require recursive exact specialization/reparameterization or a
genuinely nonlinear Nullstellensatz/Fitting certificate.  The two raw-special
jobs descended from the invalid affine assumption were stopped.  This is a
software/provenance correction, not a reversal of the generic `c1` result or
of the independently typed `c3` dual run.  The frozen blind submission was
restored byte-for-byte; the separate custody erratum has SHA-256
`da2ef83320ca21cc3ea48b47455de813b56afb05670fa00ee237275a1f268ac0`.
On the valid generic first-plus-previous open, the literal `X0,t^12` row has
zero coefficients and a nonzero constant residual, so all current-stage pivot
roots are already pruned.  The remaining finite tree consists only of
previous/pole pivot roots rebuilt from the first-stage chart, first-stage
pivot roots rebuilt from transport, and raw transport fibres `c1=0,3`.

## 5. Review and lifecycle news after the packet

- The maximum-12 Kummer preflight is different-model **CONFIRMED**.  A
  shell-bearing coordinator reran all four frozen hash checks and reproduced
  payload SHA-256
  `eb1e72d69193cded40438981907f62972725945c39a18e34891dd8f5b3645bf1`.
  Promotion must say “monic-core divisor class order”; at pair level the
  `H=0`, coefficient-`x`-degree-at-most-one corner of `(9,12)` is already
  closed by the frozen `L=2`, gcd-six route.
- The corrected AS divided-carry erratum is different-model **CONFIRMED**.
  The exact first residual is `L/3+K+C_x+D_y`; the corrected 40-variable,
  30-row ideal has dimension 18, is nonradical, and has the exact cover
  `I=Q0 intersect Q1 intersect E`.  The former 29-row completeness and
  two-piece cover remain quarantined.
- The cube owner has a producer-exact formal-trapping result on
  `k!=0,mu=nu=0`: every reduced formal fibre arc meeting the common cubic
  stays common, including the discriminant cubic.  Nilpotent thickening and
  disjoint components are not excluded.
- The TD6 generic `c1` pivot open is producer-empty.  The independently typed
  staged `(c1,c3)` dual is still computing; the proposed dependency-complete
  affine certificate was source-invalid and is replaced by the stagewise
  erratum above.  No whole-line claim exists yet.
- The Pinchuk source audit closed at report SHA-256
  `bbc8c1df4993301babed4fff5717f89bcae7fd849448e1c221884ab345ca9774`:
  Theorem 4.1 is confirmed under matched leading coefficients, Theorem 3.4
  has a published termination gap and is not a global JC2 reduction, and the
  Pakovich--Zvonkin citation classifies unitrees rather than all equality
  pairs.  The self-contained finite-three-point-cover argument remains the
  maximum-12 route; no literature list is imported.  The TD6 tangent
  reconstruction remains live in the background.

## 6. Software and allocation

One exact stratified-algebra backend should serve three typed front ends:

1. spectral norm/passport/deck-group/descent certificates for maximum 12;
2. integer numerator, proved divisibility, ghost rows, augmented ranks, and
   nonreduced incidence for AS/Witt;
3. polynomial Hermite/Smith or row-syzygy certificates, invariant factors,
   and raw exceptional rebuilds for TD6.

Shared serialization is useful; shared mathematical inference is not.  A
generic rank, module remainder, reduced cover, or modular image never implies
pointwise or whole-scheme emptiness.

Run concurrently:

1. cube owner: independently verify and freeze the stabilizer-aware
   spectral-orbit valuation exclusion; then split common factors and nonzero
   loads;
2. AS owner: classify augmented ranks `4` through `0` over the original
   corrected nonreduced ideal and reconstruct every survivor;
3. TD6 owner: finish the independently typed `c1/c3` dual, then build a
   stagewise source-valid exceptional tree; use affine syzygies only on opens
   where the prior parameterization proves linearity, with nonlinear
   Nullstellensatz/Fitting or raw rebuilds elsewhere;
4. coordinator: lifecycle promotion, synthesis, source audit intake, broad
   sweep, and independent review launch for the spectral theorem.

Keep AWS expansion stopped.  The active costs are exact symbolic cores and
certificate design; redundant ambient Groebner jobs have been terminated.
Keep stopped: untyped D12, generic exponent rectangles, p=109 brute force,
unbounded q-polynomial census, new Sigray cells, ordinary action residues,
and new unrelated TD6 pencils.

No result in this round proves or disproves JC2.
