# Production task: corrected LANDING-LEDGER LL-1 packet

You are Fable 5 at maximum reasoning.  Repair your landing-ledger design at
its first exact discriminator and implement the corrected `td=6,m=2` LL-1
packet.  Treat the hostile review as an independent mathematical correction,
not as a request to defend the old Section 5.4.

Read in full:

- `/Users/dc/code/math/jc2/xmodel/landing-ledger-primary-research-fable5-20260829.md`
  (full SHA-256
  `26bdd150a5a4bace23069b65f3234de6080fc7fb083f46a92773ec276a6fc689`,
  body `83fe23123f35275e551ef8d1704da1181dea9c346a4915cd0e15c6db82998700`);
- `/Users/dc/code/math/jc2/xmodel/landing-ledger-primary-research-fable5-hostile-review-grok46-20260829.md`
  (full SHA-256
  `62d8f55ee6a34ccd960121e9e64498fbb3ee3a92a8a4681fec4290fc9c7bc5da`,
  body `88c1d66f09314e8717234b8617da84cd78a7fe95da90b31a120721272d678e79`);
- only the exact campaign sources cited by those two reports for the LL-1
  laws, especially `MULTIPOLE.md`, `BOOK-OFFAXIS.md`, `DEPTH.md`, and
  `REDUCTION.md`.  Resolve their actual repository paths without entering
  `jc2-lean`.

Do not access, list, search, build, status, or control `jc2-lean`.  Do not run
`git status` or another workspace-wide command.  No web, AWS, local Singular,
msolve, Sage, PARI, or other heavy CAS; no canonical edits; no commit/push.
Bounded exact-rational Python and source tests are allowed.

Write only:

- fresh packet directory
  `/Users/dc/code/math/jc2/cases/landing_ledger_ll1_r2_20260829/`;
- producer report
  `/Users/dc/code/math/jc2/xmodel/landing-ledger-ll1-r2-repair-fable5-20260829.md`.

Mandatory mathematical repairs and implementation gates:

1. At `nu=1`, do not treat `eps_q` as an independent contribution to
   `dq`.  Either normalize it to a root-location flag or identify the old
   `(l,eps_q=1)` record with family I at `L=l+1` before classification.
   Retain the forced eta slot at `nu>=2`.
2. Replace the old Section 5.4 and the two-cell machine-only row by the exact
   family-I split: odd `L` has `M=1` and is MP2-dead; even `L` is D9-dead for
   every `L`, including `s(0)=0`.  Reconstruct the one-line degree
   identification and D9 residue/log obstruction from the licensed source;
   do not invent or invoke a td-7 zero-chain extension.
3. Implement the complete corrected LL-1 hand-run.  It must reproduce the
   unique header/entry, `W={2}`, root death, ZCH unreachability, the unique
   admitted IIa `(2,3,1)->Q(6,12,3,2,5)`, first P0 successor, and both
   terminal budgets.  Derive `M` from the child shape and use one coherent
   `w_cert` token, with the admitted child marked `W-CLOSED-FORM`.
4. Correct A2: `UNCOVERED` may be empty.  Fail if any generated candidate is
   neither classified nor emitted `UNCOVERED`; fail if an `UNCOVERED` row is
   already killed by a cited promoted theorem.  Every genuine open row, if
   any, must carry its exact missing hypothesis, smallest instance, and
   blocked consumers.  Preserve the orthogonality of coverage and
   alive/dead status.
5. Add hostile mutations for both duplicate parametrizations and require
   `(dp,dq)=(2,8)` / old `(l=5,eps_q=1)` / family-I `L=6`, including
   `s(0)=0`, to classify identically as D9-dead.  Retain the useful old
   mutation battery; add wrong cached `M`, inconsistent `w_cert`, a theorem-
   killed open row, an unclassified candidate, and a genuine synthetic cap
   that must become `UNCOVERED` rather than rejected.
6. Keep the scope firewall explicit.  This packet tests the corrected LL-1
   quotient and hand-run at `td=6,m=2`; it is not a proof that the candidate
   grammar equals all geometric configurations, does not bound depth, and
   proves no source landing, ceiling, Keller, or JC2 statement.  State the
   exact remaining LL-2/`td=7` obligations rather than claiming totality.
7. Run ordinary and optimized (`python -O`) tests.  Emit deterministic JSON
   fixtures/results, an exact inventory and manifest, and a fail-closed
   validator that derives rather than trusts the summary.  Give exact test
   counts and hashes.

End the report with a body self-hash and report the full packet/report hashes.
Status may be `SOURCE_READY_FOR_DIFFERENT_MODEL_REVIEW`; do not authorize AWS
or canonical promotion.
