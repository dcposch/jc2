# Critical-remainder hostile-review packet: prepared, not launched

2026-09-07. **PREPARATION COMPLETE; NO mathematical replay or promotion.**
A fresh immutable14-input packet is ready for one independent Fable review
of the claimed finite pure-center restriction ord(alpha)>=j.

## Custody before preparation

The named producer report, transaction and custody hashes match root's
charge. Transaction verification passed. All17 current entries in producer
custody were rehashed without drift before preparation. The complete
producer proof/checker/helper and complete14c/f/g/v source proofs plus15b
producer/gate were read; mathematical scripts were not executed.

Thirteen byte-identical snapshots have unique basenames; the fourteenth
charged input is their source/path/hash manifest. Snapshot and original
hashes were rechecked; the prompt's exact charged-path set, uniqueness and
literal staging template were checked. No frozen source or canonical file
changed. Metadata-only checks corrected a quoting typo and an irrelevant
list-order comparison; neither affected any source, proof or control.

## Review design

The prompt requests six separate verdicts: exact source attachment;
filtered coefficientwise division and first-remainder regularity;
moving-coordinate parity and Puiseux evaluation; the exact zero-fiber
identity and universal cross-term valuation bounds; target sign/primitive
and affine pole; and precise limitations. It explicitly requires a>0
from the pure center so critical substitution is topologically defined,
w(V)=15, deg(P)<=13, w(Q)<=2 and deg(Q)<=8, and equal-order parity
noncancellation. Infinite q,v,d valuations are separated from geometric
escape to infinity. No exclusion for a>=j or alpha=0, no degeneration
existence, and no guarded-ideal or JC2 conclusion is allowed.

The reviewer must independently derive the argument and run bounded tiny
actual changed-object controls normally/-O; producer valuation samples are
not proof. The whole15b gate is retained, including its toy-evidence
corrections. Centralizer/classification trust remains imported through14f.
Historical Astra2200 square-class discussion is explicitly omitted because
the producer declares it history, not a premise. Root's independent replay
is not used as a substitute for the different-model derivation.

The checker imports a hard-coded relative helper. Instructions preserve
both exact code files under their original relative paths in a NEW
repository-shaped scratch root; frozen inputs/outputs stay untouched.
No actual R³/R⁵, A15/B25 or full-source arithmetic is authorized. Each
script is limited to30wall/25CPU/512MiB with -I -B; review budget<=20minutes.
A standalone BODY-END and no authored seal/charge_basis are required of
the future review report. No lane, price, or authority was fabricated.

## Frozen handoff

Owned directory: `box/d125-pure-critical-remainder-gate-prep-20260907/`.

- Prompt `review.prompt.md` SHA256: `215fc90568bfe1c4c21189959612b0ad4e241e7ae2d07375e221988c2e299ccd`.
- Input manifest `input-pins.json` SHA256: `b2117971e5946e00fe94c6d06b5cc31e9be5379af4f7d2ffc5cc1f9bb2f691b3`.
- `preparation-check.json` records14 exact charged paths, unique basenames,
  all current source/snapshot matches, and no mathematical replay/launch.

All prepared files are frozen read-only at handoff. Root retains acceptance,
narrowing and launch. All writers and tool children terminal. STOP/IDLE.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `3452`.
- Body SHA-256:
  `179e667beb21388ff52bb3d3f8cb0486cac6c1cbc0603504ebd70b0adc813980`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
