# K00 V20R2 valuation five: coordinator integration of the reviewed closure

Coordinator: Sol 5.6  
Date: 2026-08-29 UTC  
Frozen campaign basis: `0375694aa21fd8db3b465ef2098ca539106a1582`  
Lifecycle: `PROMOTED EXACT FINITE-JET EXCLUSION / SOURCE-WIDE INFERENCE FORBIDDEN`

## Binding verdict

Promote the exact valuation-five exclusion below, with repairs R1--R4 folded
into this integration rather than silently changing the sealed producer. On
the normalized V20R2 source

```text
C6=1,
k10[0]=kappa!=0,
k6[0]=k2[0]=mu2[0]=mu4[0]=mu6[0]=0,
Jdet[0]!=0,
d=Lambda^5 x+Lambda^6 y+...,
x!=0,
```

there is no field-valued characteristic-zero jet satisfying all seven
coefficient rows through grade 19. Leading `DQ(x)` ranks two and one die at
grade 14; leading rank zero dies at grade 19. Thus no same-source formal
`K[[Lambda]]` section has exact valuation five.

This closes the integer valuation-five lane only. It does not identify the
closure of the normalized source inside a larger ambient functor: a DVR arc
may pull back `Lambda` with ramification index greater than one. It also says
nothing about `C6=0`, delayed or fractional load orders, another support, an
actual polynomial map, a counterexample, or JC2.

## Evidence

The Opus 5 producer is
`xmodel/k00-r5-jetfan-v2-repair-opus5-20260829.md`, full SHA-256
`91dcf1731590ca2bdae53804c911c01b2a4ba1ebfc58aa6e182f402c84292d84`,
body SHA-256
`a13c02edb9e79161cb6f43c4ec05e670e65ad94c6ae83ef1447d58018b68ff21`.
Its embedded exact replay has SHA-256
`e7c6cab141c31f2732481bd8a19eeea91ac19786e8bfce81e5e4691e97f9d83a`
and passes ordinarily and under `python3 -O`.

The independent Sol grade-19 and custody audit is
`xmodel/k00-r5-jetfan-v2-repair-g19-custody-audit-sol56-20260829.md`,
full SHA-256
`bcb5f8dc1559b704d039b89b91a153976decab1ae2db83f68ae25ce6c497d62d`,
body SHA-256
`ad1da4e3970ce2f1d4256fea08007aa751c2b1a918a96ced11e4634be1450c23`.

Fable 5's hostile review is
`xmodel/k00-r5-jetfan-v2-repair-hostile-review-fable5-20260829.md`, full
SHA-256
`aa2ad0afe89e870b626acf3d056d64076c12d9c04d6cbad205c430c19220d349`,
body SHA-256
`0d249cbd704c6c40ff594c46a3bdffe392521e5f32c458364fc03e4fa75a4f17`.
It independently reconstructs the source with a separate exact engine and a
truncation-free numeric path. All ten attacks are confirmed and its verdict
is `PASS_WITH_REPAIRS`; every required repair is display-level.

## Exact rank fan and terminal certificate

Put

```text
A(q)=16q1-4q3+q5,
B(q)=q0-4q2+2q4,
Delta=u^2+64v^2.
```

The reduced leading cone is the four-plane `A(x)=B(x)=0`. The rank-two,
rank-one, and rank-zero strata of `DQ(x)` are respectively `Delta!=0`,
`Delta=0!=(u,v)`, and `u=v=0` after algebraic closure. Exact grade-14
cokernel identities exclude rank two. For both Gaussian rank-one signs, the
grade-13 equation gives the exhaustive split `tau*f=0`; exact grade-14
identities exclude both branches. Descent from algebraic closure gives the
field-valued characteristic-zero statement.

On rank zero, `x=ell(s,t)=(2s,t/8,s,t,s,2t)`. The minimal residual object
`K00-R5-R0-RESIDUAL/v2` is `Z x A^58`, with 78 live coordinates and
canonical serialization SHA-256
`02b4cb9f90dfb72eca05608386578de57b6d4db93ef94c1ed60cb807e95f9e6e`.
Its corrected rank-zero dependency ranges are
`d[6..13]`, `k10[0..6]`, `k6[1..7]`, `k2[1..4]`,
`mu2[1..5]`, `mu4[1..3]`, `mu6[1]`, and `Jdet[0]`.
The exact V14R1 contraction and grade-12 cone equations give the polynomial
ideal certificate

```text
5*Jdet[0] belongs to the ideal generated by the 140 restricted rows.
```

Consequently the rank-zero residual is empty over every `Q`-algebra in which
`Jdet[0]` is a unit. This last statement is scheme-theoretic and needs no
radical, reducedness, or algebraic-closure argument. Without the declared
open it yields only `Jdet[0]=0`, not emptiness.

## Repairs folded into promotion

1. Replace the producer's displayed `M4(x)` vector by
   `(5/8192)(-32uv,u^2-64v^2,4uv,0,uv/4,0,uv/32)`. The replay and all
   downstream computations use the true vector.
2. For the grade-19 `k10[6]` coefficient, rows 3, 5, and 7 are proportional
   to row 1; row 2 is instead
   `(5/32768)s B(d6)-(5/512)t A(d6)`. All lie in the span of
   `A(d6),B(d6)`; no proof step uses the incorrect word “proportionally.”
3. The replay pins ten mathematical inputs. The three rejected/audit reports
   were checked separately by the producer and reviewers, not by that pin
   table. Likewise, the replay computes but does not print or gate the two
   optional rank-two cokernel values; Fable independently verifies them.
4. The producer's valuation-six and valuation-at-least-seven remarks are
   contextual heuristics, not claims of this report. Those valuations were
   already proved and promoted independently; the review's suggested
   successor is redundant and is not relaunched.

The producer's false “no existing artifact was read” sentence is replaced by:
only the named mathematical inputs and contextual reports were read; no
canonical ledger or formalization path was accessed and no existing artifact
was modified. Its replay docstring's “four mutations” is read as five, which
is what the code and report implement.

The optional extra grade-13 row-vanishing replay check would improve drift
protection but is not a mathematical prerequisite: Fable verifies both the
row vanishing and the grade-14 cokernel's independence from the chosen image
solve.

## Scope

This is a finite-jet and same-source formal-section exclusion on one exact
normalized support. Formal data are not a map. The result proves neither
closure incidence in ramified DVRs, source exhaustiveness, existence or
nonexistence of a Keller counterexample, nor JC2.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5716`.
- Body SHA-256:
  `97d61bdc38564ba5c30312d3221c72f5210df42512bd980a2771b15d59346ff5`.
- Frozen basis: `0375694aa21fd8db3b465ef2098ca539106a1582`.
