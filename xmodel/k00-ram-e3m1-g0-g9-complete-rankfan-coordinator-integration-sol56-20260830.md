# Coordinator integration: complete generic-K00 `e=3,m=1` fan through G9

Coordinator: Sol 5.6 Ultra  
Date: 2026-08-30 UTC  
Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`  
Lifecycle: **BINDING PROMOTION / EXACT FIELD-VALUED G9 EXCLUSION / IN-CELL FORMAL-ARC EXCLUSION**

## 0. Custody and verdict

The sealed producer is

```text
22e9752761f3bb4ee94b4dccf18cb72ff49bd0d92453ad4cf39eb401cc45f84a  full
2e3086f577cc4e0b228cfe55df92413ca749e0b3230ddb71208735b49809bc8e  body, 13038 bytes
  xmodel/k00-ram-e3m1-g0-g9-complete-rankfan-sol56-20260829.md
bcd4004e671091c7d1ffc265affbd160b1d2b2a712f62dd97b698150dfc9fcba
  xmodel/k00-ram-e3m1-g0-g9-complete-rankfan-replay-sol56-20260829.py
```

Opus 5 independently rebuilt the 569-tail source and returned
`CONFIRM_WITH_CORRECTIONS`:

```text
0f7c18eb06dc2451fd4db668de7682a5d59f1c734509402b2537790b0408db87
  hostile review, 32569 bytes through BODY-END
e6338464c8dbe442deeed116c493af7550cb212a3dc22c1377ef695240ac4afe
  lane log
281d51a7401c232b4bd6b5e969b1076adb011bfd3e21f307162f03bf823072b7
  prompt
```

The lane ended `DONE`, exit zero, on the frozen basis. Ordinary and optimized
producer replays agree; Opus's separate sparse engine reproduces every
displayed rank fan, cokernel form, terminal, and fixture, and supplies real
tail mutations. No mathematical item is refuted.

## 1. Promoted maximum theorem

Let `K` be an algebraically closed field of characteristic zero. Use the
frozen 569-tail K00 source and charged compiler coordinates, so `C6=1`, with

```text
Lambda=tau^3,                 ord_tau(d)=1,
k10[0] != 0,
k6,k2,mu2,mu4,mu6 in tau*K[[tau]] or identically zero.
```

Then

```text
V(G0,...,G9) intersect D(k10[0])
             intersect (union_i D(d_i[1])) = empty.             (1.1)
```

The registered `Jdet[0]` unit remains part of the named source cell but is
not used by (1.1); its sector cannot reach G9. Every positive-or-infinite
boundary order of K6, K2, and the targets has literally the same G0--G9
system. Thus the exact normalized cell has no compatible formal arc: any
such arc would truncate to a forbidden G9 point. This remains only an
in-cell formal-source conclusion, not reachability, convergence,
algebraization, attainment, or a polynomial map.

Through G8 the source is purely unloaded. The only load term through G9 is
`k10[0]` times the grade-nine cubic; K10's apparent quadratic arrival cancels
on the leading surface.

## 2. Reviewed exhaustive tree

The promoted G0--G3 incidence gives the unique leading plane
`d[1]=ell(s,t)`, `(s,t)!=(0,0)`. It transfers from the earlier `e=2` packet
because no load can reach G2 or G3 for any `e>=1`.

After exact surface recentering, every fresh first-normal coefficient lies on
the same reduced four-plane. The polar rank is two off
`Delta=p^2+64q^2`, one on the two nonzero Gaussian rays, and zero at
`p=q=0`. These three strata exhaust the reduced support.

- At first normal order two, rank two dies at G5, rank one at G6, and rank
  zero recenters.
- At first normal order three, rank two dies at G7. Each rank-one sign has a
  genuine G8 component, but the literal source gives
  `G9_6=epsilon*i*q^3/32`; both die on `D(q)`. Rank zero recenters.
- At first normal order four, rank two dies at G9; rank one has terminal
  `-epsilon*5*i*k10[0]*t^3/16`; rank zero reduces to the two incompatible
  surface cubics. The same cubics kill every higher normal order and the
  exact-surface branch `N=0`.

The banked fixture still has all 63 coefficients G0--G8 equal to zero and
`G9_6=i/32`, so it was a genuine survivor prefix and its first failure is now
located exactly.

## 3. Binding corrections

1. The producer source list mislabels `d453b956...` as the leading-plane
   integration. The actual charged carrier is `0eb501b4...` section 2; Opus
   also re-derived the plane independently.
2. In the producer replay, `DELTA_64`, `W2_192`, and `K10_OPEN` are
   check-side assertions, not source mutations; the W2 comparison alone has
   an unreachable failure branch. Keep the old fixture and propagated
   surface perturbation as genuine controls, and consume Opus's five actual
   tail mutations for source sensitivity.
3. State the ambient open as `d[1]!=0`; `(s,t)` are coordinates only after
   applying the leading-plane theorem.
4. Include `N=0` explicitly beside `ord(N)>4`; both meet the same G9 load
   obstruction.
5. Only `k10[0]` can occur through G9. The producer honestly retained
   `k10[1..3]`, but their absence is an exact cancellation statement.
6. There are seven row slots but only six nonzero leading quadrics; row six
   is identically zero in that leading system.
7. The G9 point-set exclusion does exclude formal arcs inside this exact
   cell. It says nothing about another cell or source incidence.

The reviewer also verifies a possible later extension from `k10[0]!=0` to
`k10!=0`, with the same terminal shifted to G`9+ord(k10)`. That is a separate
unpromoted theorem because its truncation window changes.

## 4. Scope and next discriminator

Nothing here covers `k10=0`, the `C6=0` tip, `e=3,m=2`, another support,
positive characteristic, nonreduced scheme structure of the G8 component,
source completeness, reachability, a map, a counterexample, or JC2.

The cheapest missing residue at this ramification index is `e=3,m=2`. Its
G4 leading system is the same normal quadric fan, but K10 first reaches G10;
the G9 terminal which closes `m=1` is unavailable. It is therefore a genuine
new transition problem rather than a replay of this theorem.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5551`.
- Body SHA-256:
  `3c08e46be1ea079bf5882c400d5256fc0ed00a57076bfab019a0ca386aed430e`.
- Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`.
