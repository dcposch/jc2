You are the independent hostile referee for the correction-aware
generic-square high-contact `A`-prolongation calculation in the `jc2`
repository.  Work text-only.  Do not run Singular, Sage, msolve, Lean, the
package compilers, or any substantive algebra program.  You may read files,
recompute SHA-256 hashes, inspect already-emitted records, and check the
displayed finite identities by hand.  Do not edit any existing file.

Primary targets, with required byte hashes:

```text
36b8abd0383d901261a4b410c94a0ffb759e3dea37263852af86778e77d5589d
  xmodel/max12-812-order2-square-a-prolongation-design-20260826.md
13b1189088853c9781bec87dba16978f1342c818b34ff07bd8025c5ea24275d4
  cases/max12_812_order2_square_a_prolongation_20260826/RESULTS.md
36dfff6b0cd7234c4e4e32a0a6bb82496fbdf543ebdfc4aa649cda519e6b8e51
  cases/max12_812_order2_square_a_prolongation_20260826/FREEZE.sha256
a00eeaa475eac943d67ad8fa9d954762572f2657fc791ab2ec0dc9688efdedfa
  cases/max12_812_order2_square_a_prolongation_20260826/RESULTS.sha256
eba4640a9c7da51e4297e7e0b3c236f73f2eb2f70770de0634e1d0fea652d13f
  cases/max12_812_order2_square_a_prolongation_owner_v2_20260826/RESULT.md
6dbf5b6118aac14c38facdd977e221b0e4648a9e875fe03867172c646e322f64
  cases/max12_812_order2_square_a_prolongation_owner_v2_20260826/FREEZE.sha256
3f3d729ed046431e92a6cbd1f5e373526525f39422a495fe9d66def8476a6e80
  cases/max12_812_order2_square_a_prolongation_owner_v2_20260826/RESULTS.sha256
```

Read both result reports and the design in full.  Verify both packages'
manifests and every evidence file they name.  Also read completely:

```text
xmodel/max12-812-order2-generic-square-halfweight-kuranishi-design-20260826.md
cases/max12_812_order2_square_halfweight_kuranishi_20260826/RESULTS.md
xmodel/max12-812-order2-square-halfweight-kuranishi-hostile-review-grok-20260826.md
xmodel/max12-812-order2-square-third-tail-divisibility-theorem-20260826.md
xmodel/max12-812-order2-u2-62-oneparameter-rees-reduction-20260826.md
```

Attack the following issues independently and identify the smallest broken
implication or missing hypothesis if any:

1. Custody.  Recompute all seven primary hashes, validate both packages'
   manifests, and verify that the root exact-Q producer, root good-prime
   control, owner-v2 exact-Q certificate, and owner-v2 good-prime control are
   four distinct registered executions.  Charge compiler and input hashes,
   tags, hosts, characteristics, return codes, validators, stdout hashes,
   resource records, and the quarantined root first attempt.  A passing
   sentinel or manifest is not itself mathematics.  Confirm that no failed
   first-attempt endpoint is used.
2. Scaling and source substitutions.  Starting only from
   `L=z^2+p/2`, `K=L^2+Lambda*R`, `N=L*M+Lambda*S`, and
   `f=K^2+Lambda*N`, substitute the registered high-contact scaling and
   independently check every ordinary coefficient in both compilers.  In
   particular audit the factor two between
   `E0=(e1*z+e0)/2` and the cancellation family
   `E0=-(5*k0/24)A0`.  Decide whether the corrected control is exact and
   whether the first attempt was correctly rejected.
3. Complete analytic receivers.  Independently expand `f^(3/2)` and the
   charged `k10*f^(5/4)` through grades fourteen and fifteen with
   `A=A0+sigma*A1`, `R=sigma^2(B0+sigma B1)`,
   `C=sigma^4(E0+sigma E1)`, and `k10=k0+sigma*k1`.
   Check every coefficient and sign in `h14,h15`.  Attack contributions from
   corrections to `K`, from all binomial powers, from lower grades, and from
   `k6,k2`, finite targets, terminal data, and Taylor loads.  Distinguish
   terms proved polynomial from terms merely omitted by valuation.
4. Source/Faber bridge.  Inspect the complete emitted seven source rows and
   the owner-v2 lower-unitriangular transform.  Check exact sigma
   divisibility, quotient extraction, forbidden-variable assertions, the
   relation `q^2+(p/2)v^2=1`, the unit diagonal, and coefficientwise identity
   of all seven grade-fourteen and grade-fifteen rows with their Laurent
   receivers.  Decide whether seven rows really suffice by comparing the
   cleared numerator degree with `deg L^3`; do not accept a row-count slogan.
5. Characteristic-zero separator.  Clear `16 L^3` in `h15`.  Verify that
   every term but `-A0^3` is divisible by `L`, and that source-row vanishing
   implies `L^3` divides the cleared numerator.  Then check carefully that
   on `D(p)` squarefreeness of the degree-two `L` and `deg A0<=1` imply
   `A0=0`, including the case where `L` splits.  Determine whether any
   grade-fourteen root allocation or tangent coefficient can evade this.
6. Frozen-`p` scope and moving-`p` audit.  Both frozen packages keep `p`
   constant in `sigma`; do not silently call them all-tangent calculations.
   Independently put `L(sigma)=L+sigma*ell` and differentiate the
   grade-fourteen receiver.  Check whether the additional cleared
   grade-fifteen numerator is exactly
   `-12*ell*L*A0*E0 + 12*ell*B0*A0^2
   -(5/2)*ell*k0*L*A0^2`, whether its reduction modulo `L` is
   `12*ell*B0*A0^2`, and whether the grade-fourteen equation really gives
   `L | B0*A0^2`.  Decide what survives now as a fixed-`p` theorem and what
   must await an exact moving-`p` source replay.  Do not promote the hand
   addendum as source-certified evidence.
7. Producer radical.  Inspect the raw exact-Q grades and the precise
   sequential saturations by `p` and `k0`.  Decide whether the displayed
   radical, membership of `a0,a1`, and non-membership of `1` are reported
   faithfully; whether the saturation matches localization on `D(p*k0)`;
   and whether radicalization discards scheme structure in a way that would
   invalidate the pointwise exclusion.  Compare it to, but do not conflate
   it with, the independent separator.
8. Good-prime and independence firewalls.  Treat `F_65521` only as a
   software control.  Decide exactly which parts are independently
   reconstructed and which are merely same-source dual-host custody.
9. Scope.  Attack every stronger inference.  This is only the fixed-`p` cone
   `ord_sigma(R)>=2`, `ord_sigma(C)>=4` on `D(p*k0)` after one reviewed
   half-weight ray.  It does not establish normalized-fan exhaustiveness,
   route lower valuations/root allocations, handle `p=0`, close the square
   branch, close order two or `(8,12)`, prove maximum twelve, or prove JC2.

Output exactly one new report and no other file:

`xmodel/max12-812-order2-square-a-prolongation-hostile-review-grok-20260826.md`

Include a verdict table, custody findings, independent derivation of both
receivers and the modulo-`L` separator, producer-radical audit, strongest
surviving theorem, exact scope, and sharpest non-claim.  If the result
survives unchanged, end with exactly
`ORDER2_SQUARE_A_PROLONGATION_CONFIRMED`; otherwise end with exactly one of
`ORDER2_SQUARE_A_PROLONGATION_REPAIRED` or
`ORDER2_SQUARE_A_PROLONGATION_QUARANTINED` and state the smallest repair.
