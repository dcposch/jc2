# Fable 5 hostile review — Grok U2 `nu=1`, unbounded-`lex` reduction

Producer: Grok 4.6. Work independently from `/Users/dc/code/math/jc2` and
write exactly:

`xmodel/m2-u2-nu1-unbounded-lex-primary-hostile-review-fable5-20260829.md`

Read the complete target
`xmodel/m2-u2-nu1-unbounded-lex-primary-grok46-20260829.md`, its packet
`cases/m2_u2_nu1_unbounded_lex_grok46_20260829/`, the promoted/repaired U1
semilinear result and hostile review, the finite reduced-chain R2 report and
review, and every canonical ladder clause directly cited by the target.
Verify the target's full hash
`9c20947be6d2ac6aad19176fae66f0b4acfb6ec6bce257a2169d8d266c667613`
and body hash
`49fdda2b84e4a405f6432239546826a943326a5357f10efb26eaa8b2a149a1b6`.
Treat every source reading and theorem as an allegation. Do not read other
active post-target model outputs.

Audit independently, with special priority:

1. Re-derive the U2 normal form from the actual merge equations. Check that
   `nu=1` and unbounded `lex` really force `eps=k=0`, permit eta absorption,
   and give `p=Rad^mu`, `q=Rad*S`, with the stated degree and nonvanishing
   conditions. Look for omitted zero/free roots, orbit collisions, unequal
   invariants, and hidden characteristic or normalization assumptions.
2. Re-derive
   `r Rad S' - L Rad' S = C' != 0` and
   `C=mu C'/(r+L)`. Classify its polynomial solutions rather than trusting
   the packet search. In particular attack the claims for `r=2` parity and
   `r>=3`, `Rad=t^r-A`, `L == 1 mod r`; distinguish examples from an iff.
3. Verify every transport formula:
   `dp=r*mu`, `dq=r+L`, `E=mu*L`,
   `M=gcd(r*mu,r+L)`, `kbar=w(r+L)/L`,
   `w_tr=w(r+L-1)/L`, and `lambda_G=0`. Check integrality, primitive
   normalization, edge direction, and whether any parameter suppressed here
   is consumed later.
4. Attack the central finiteness theorem at fixed `(td, entry)` and budget.
   Give a complete quantifier-safe proof or a counterfamily. In particular
   inspect dirty multi-step paths, induction over their number/order, possible
   dependence of the allegedly bounded divisibility integer on `L`, `s`, or
   prior vertices, zero-cost trunks, cycles/revisits, changing radicals,
   family-record collisions, and the passage from a finite P1 grid to a bound
   on `L`. A finite capped search is not evidence for this theorem.
5. Independently check the stated td=7 and td=8 controls against the current
   source: row data, costs, ceilings, direct arrivals, post-A state, and the
   claimed unique `L=1` exact-fit cell. State whether each is theorem-level,
   pattern/superset-level, or only diagnostic.
6. Replay both ordinary and optimized suites and add clean-room exact checks
   and hostile mutations in `/tmp`. Reject cap tokens, hardcoded expected
   rows, or tests that merely restate formulas.

Return exactly one lead verdict:
`PASS_AT_RECORDED_PATTERN_SCOPE`, `PASS_WITH_REPAIR`, `REPAIR_REQUIRED`, or
`FAIL`. Separate (a) local ODE algebra, (b) transport, (c) fixed-terminal
finiteness, and (d) low-td controls, so a failure in one does not erase valid
narrow residues. State the maximum safe consequence, exact repair if needed,
next research target, commands/counts, and full/body hashes. No verdict here
authorizes landing, realization, Statement-3.9 gluing, a global td bound, or
any JC2 consequence. A pass authorizes canonical promotion only at the
explicit reviewed scope and licenses no AWS.

Hard boundaries: no web, AWS, commit, push, canonical edits, heavy CAS, or
local long/high-memory process. Never access/list/search/build/status or
control `jc2-lean`; never run global `git status` or workspace-wide searches.
Modify only the requested report; use `/tmp` for scratch.
