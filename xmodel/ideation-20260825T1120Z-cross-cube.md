# `20260825T1120Z` adversarial cross-pollination — cube/max-12 owner

Date: 2026-08-25  
Inputs: the four sealed `1120Z` owner reports, followed only after collection
closed by the registered Q8, AS-`p=2`, and TD6 post-cutoff endpoints.  Consensus
below is allocation evidence, not proof evidence.

## Deduplicated fingerprints

| fingerprint | supporters | common content | genuinely different edge |
|---|---|---|---|
| `Q8-W0-HORIZONTAL-COVER` | root, cube, AS, TD6 | Replace more interior computation by an exhaustive source-derived finite/projective/valuative cover of the complement of the corrected-Q8 chart; retain terminal and Taylor data. | Root/cube emphasize the finite `x5=0` jet and a terminally decorated Newton/Rees fan. AS emphasizes normalization of the closure over `Q[w]`. TD6 emphasizes a proof-DAG chart union and source-complete denominator custody. |
| `AS-COMPLETE-CARTIER` | all four | Compute the first omitted Q4 Cartier coordinate on the whole source-complete Q5 predecessor fibre, split its Fitting strata, and restore all remaining rows only on survivors. | Root/cube rank the live global Q5 gate above seed proliferation. AS/TD6 also use a small `p=2` seed as an orthogonal support-threshold falsifier. |
| `SOURCE-DAG-FITTING-BROKER` | all four | One content-addressed compiler should emit original-row leaves, denominator opens, saturation/union certificates, rank/Fitting strata, and engine-independent replays for Q8, AS, and TD6. | TD6 is the immediate software client; Q8 supplies a closure/saturation stress test; AS supplies SAT/UNSAT proof-certificate clients. |
| `TD6-CURRENT-THEN-Q3` | root, cube, TD6; AS agrees on priority order | Finish the current/previous source lift and denominator union before adding the next matrix-changing q-jet. | TD6 proposes the exact `gamma*t^3` dual-adjoint test; the other reports mainly enforce the no-premature-union firewall. |
| `MONODROMY/ADJUNCTION-AFTER-LANDING` | root, cube, AS | Use grouping, normalization, genus, or adjunction only after a new source-certified boundary component exists. | This is a sequencing rule, not a new standalone proof mechanism. |

The row-32 disagreement is semantic rather than mathematical. Root/cube
“raise” automatic collision because the residue-ball theorem upgrades a
complete fixed-support tower to a counterexample. AS/TD6 “lower” explicit
collision variables because that theorem removes them from the AS critical
path. The correct allocation is: raise the theorem's campaign value, remove
collision-search computation from the live AS restoration gate.

## Strongest shared hidden assumption

All three consensus mechanisms can accidentally identify a specialized or
localized object with the horizontal source component that an actual
trajectory uses. The exact failure modes are the same:

1. a component of `I|_(w=0)` can be purely vertical and need not lift over
   `D(w)`;
2. homogenizing or saturating only after setting `w=0` can create or erase
   coefficient-infinity strata;
3. a generic rank/cokernel calculation does not cover the zero set of its
   pivot, and a denominator list does not prove the listed opens cover the
   original source;
4. a finite-depth SAT section need not belong to one fixed finite scheme at
   every later depth.

This is not hypothetical. The post-cutoff raw Q8 fibre has a finite
`x5=0,A=x3!=0` line, while the generic tangent calculation gives
`rank(J_y)=rank([J_y|F_w])=5`: first-order transverse arcs are compatible,
not killed. Yet neither fact proves that this line lies in the horizontal
closure of the selected open. The source-honest split is therefore mandatory:

```text
A = closure of selected open:      I : (w*x5*A)^infinity,
B = w-horizontal boundary leaves: (I + (x5*A)) : w^infinity,
A = x3 - 2*x5,
```

followed by relative homogenization, saturation by the projective irrelevant
ideal and by `w`, and only then specialization. Any proposed Q8 landing
atlas, TD6 denominator union, or AS compactness chain that lacks the analogous
source-before-specialization step remains conditional.

## Best combination

Run one shared **horizontal-closure/Fitting proof DAG**, but keep three
mathematical foregrounds distinct:

1. **Q8:** compute the two horizontal closures above; on every surviving
   finite or projective chart, solve the completed local branch far enough to
   obtain the first nonzero `r6`, `r8`, and two true-centre Taylor defects.
   Rebuild every special-`a` factor separately. This is the most immediate
   proof-side discriminator.
2. **AS:** compute the full-Q5 Q4 Cartier class and Fitting strata; feed only
   actual zero strata to SMT/restoration. Collision variables stay deleted.
3. **TD6:** complete V56b/V54D source/denominator coverage with the same DAG
   format before testing the next q-jet.

The broker should regard `(source ideal, declared open, base parameter,
closure order)` as part of the content hash. This prevents a specialized
fibre, quotient-field identity, or reverse-chart heuristic from being reused
as a source-global certificate.

## Cheapest separating experiment

The best three-way separator is a small, source-closed endpoint in each lane,
not another broad search:

- **Q8:** over `Q(a)`, compute `(I+(x5)):w^infinity` and the selected-open
  closure `I:(w*x5*A)^infinity`; if the `x5=0` line survives horizontally,
  calculate the first omitted-row/Kuranishi coefficient and the induced
  orders of `r6,r8` and the Taylor defects, including every root of the rank-5
  minor. Empty horizontal closure favors the global valuative-cover card;
  a source-valid formal branch favors terminal/Taylor reconstruction.
- **AS:** decide whether the global Q4 Cartier coordinate is a unit on the
  whole Q5 predecessor algebra. A unit immediately kills that support; a
  zero stratum outranks further Q8 interior computation on the disproof side.
- **TD6:** finish the exact current-row union and V54D denominator mirror. A
  source identity raises the q3 recurrence card; any uncovered factor sends
  effort to that one raw stratum.

This experiment separates allocations because each outcome is binary and
source-complete. Generic tangent survival, point samples, and one-sided
quotient reductions do not qualify.

## Post-cutoff evidence and rank changes

### Q8

Producer-internal clean mirrors agree on the raw specialized boundary:

- `x5=0,A!=0` is exactly
  `x1=x3=a`, `d2=d4=0`, `c=1/a`, with
  `81*r6+4*a^3=0` and `r8=0`;
- `A=0,x5!=0` is empty;
- `x5=x3=0` is an unloaded affine three-space and its loaded split is empty;
- over `Q(a)`, `rank(J_y)=rank([J_y|F_w])=5`, so a transverse first-order
  deformation exists.

Custody is under
`cases/max12_912_order3_nu_q8_w0_escape_boundary_aws_20260825/`; clean
generator SHA `17dd99143f6887698f15e1834eac26a1622055519c7b0e7ef4800d6ebb18fa6d`,
runner SHA `85eb9d223978731ed7f017b12622534412985ac8fba9578421dfe685e53666ac`,
clean prereg manifest SHA
`e423e9bb9e9a70b9211817ca96c85b41d3ea63317cde130a5ff058bd4f280b3f`.
This raises the **horizontal saturation plus higher Kuranishi** subproblem and
lowers any expectation of an immediate tangent kill. Raw projective jobs are
controls only until the `w`-horizontal saturation is added. No selected-leaf
or max-12 theorem follows.

### AS `p=2`

The equal-cap depth-three pilot is proof-checked UNSAT at cap 2 and directly
replayed SAT at cap 3, where the preregistered identity gauge is available.
The DRAT verification stdout SHA is
`dbcabf023392cab7ae44d744009904dc62a52a9da7ff7a380f7f03029a095397`;
the cap-3 direct replay JSON SHA is
`7ec5b1fc65b399f79da93f902271d3f4ba452d387c416e3a79bb60a3c85df4ec`.
This proves a sharp support threshold for this bounded pilot, not an all-depth
branch. It slightly raises small-seed diagnostics but does not displace the
global Q5 Cartier gate.

### TD6

Post-cutoff V56b rows `0,13,34,36--39` are exact PASS/source-liftable with no
obstruction. The V54D diagnostic reports six nonunit denominators supported
only on `U,H`, but its independent mirror is pending and
`reverse_chart_coverage_inference=false`. Thus software/custody risk falls;
there is no theorem-level or allocation rank change while the current-row
union remains incomplete.

## Allocation verdict

1. Keep Q8 horizontal closure plus completed-local terminal/Taylor jets first
   on the proof side; explicitly demote raw specialized/projective fibres.
2. Keep the source-global AS Cartier/Fitting gate first on the disproof side;
   use the `p=2` cap threshold only as a compiler/support control.
3. Keep TD6 third until its exact current-row/denominator union closes.
4. Fund the shared source-DAG broker because it attacks the strongest common
   hidden assumption, but do not count consensus on the broker as evidence
   for any mathematical implication.
