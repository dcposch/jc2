# Blind whole-portfolio ideation — Fable 5 — round `20260830T1015Z`

Date: 2026-08-30 UTC  
Researcher: Fable 5 (equal-standing whole-portfolio lane)  
Frozen basis: `4decd33d5caf8553bcbaa199b93a900ecabeee18`  
Lifecycle: **BLIND IDEATION REPORT / NO NEW EXIT PRICE ASSERTED / receipt
`charge_basis_status=ABSENT` expected**

## 0. Custody, blind boundary, and compute ledger

Recomputed before reading:

```text
ce5b9564a8f5cab7eea146e0408bc32950ff8a168a678f340d682fba4e621c58
  xmodel/ideation-20260830T1015Z-state.md            (assigned packet)
90df4ac07e8b58fceeff3dd9c378cfd784b593e030c34bbaa76c31ee9ce39fe9  APPROACHES.md
ab1865c8b4b1022ec3dfb206ada07d2b698bb4a713437aa5e1d6a6e0420398e1  AUDIT.md
1c0c20fa4798f429cf00325fd19aa8605f0050a32f28e2885dedfb14a03510cc  COORDINATION.md
046dbe4d52ab5ed14e10b761e5a60d62c1bac26c2ef0ed97c10599a5146182fa  PROGRESS.md
7f901db6c8e6fbc80c85581a331c247d5c219e23bf286136f77fbc0182ebbe1b  ladder/REDUCTION.md
2dc015cf32e1b057f3848451fc37fd0acbec2a4e5b2784731ba2b02f9c3cbb86  notes.md
0db1bd7d6b8162780340532c75901a7721e6bd621f00981ee915b1114d2e06e1
  xmodel/ideation-20260829T2254Z-synthesis.md        (novelty baseline)
bb55f9a53274217d424eb42e3eda7730bf75efdfeeb64a48b0c6752a707f1f38
  xmodel/websweep-20260830T0340Z.md
6a7dc7bec108d867fe9215a85fdc09c2d2790db73d36c4123e96f7419f9511f3
  xmodel/bd-a2-singular-f5-unbalanced-local-polar-sol56-20260830.md
5a31a2ab48746828f08cc0b7e2d349b2c2dc773a2faa59fa23a51cb667bbaecc
  xmodel/bd-a2-q8-f5-global-lattice-elimination-b5-b6-u5-u6-sol56-20260830.md
f942e743dc206d958f31892e838d7517fe23eacbc863b56c9441b60152bc0cb4
  xmodel/bd-a2-normal-f5-decorated-carrier-effectivity-reduction-sol56-20260830.md
94f5bc5ce509809b951f5189bb6d3f863c09133dfda77522965d791a7419ee2e
  xmodel/bd-a2-normal-f5-contracted-carrier-compatibility-corrigendum-sol56-20260830.md
db3fbb623f8f7f1843506daafc4374dfd87d3a5365773bfd2859cb17f1015e77
  xmodel/bd-a2-singular-f5-local-polar-and-contracted-multiplicity-sol56-20260830.md
```

The last three match the hashes charged inside the two frozen producers and
their coordinator integration. I read no sibling submission, prompt, log, or
run receipt from this round, including every artifact of the active GPT-5.5
q=8 hostile review. No canonical file or Git state was touched; no web access;
no `jc2-lean` contact. Total local compute: hashing plus two standard-library
Python integer enumerations in `/tmp` (5.7 s and 9 s, trivially inside the
60 s / 1 GiB desk bound; no Singular, no CAS). Those scratch runs are
unsealed corroboration only; every use below is in the elimination-safe
direction (zero survivors of a deliberately relaxed candidate set).

## 1. Disposition over all 46 avenues

Baseline: the `2254Z` synthesis (no avenue changed global tier there) plus
all overlays through 08:33Z. Changes here and the reason for each:

| # | disposition | reason |
|--:|---|---|
| 7 | **raise** | Two live clients now exist where the table said "not cheaper than rebuilding the compactification": the promoted ACS complement-cover theorem is infrastructure, and Section 3.2 below gives a genuinely new, desk-scale, unconditional structure target for `A(F)` (non-abelian `pi_1`, non-nodal floor). |
| 26 | **raise (execution)** | The funnel's q=8 table is provisionally empty and Section 5 arms an exact q=6 discriminator with zero-survivor scratch evidence in every singleton allocation cell. This is a client raise, not a claim that primitivity moved. |
| 28 | **reopen/raise** | Log-BMY was stuck at `NEEDS-DATA` ("B/x-resolution tails unpinned", 2026-08-12 battery). The two q=6 cells now have completely pinned boundary graphs, `rho`, `k`, `e(U)=12-r-c`, and `K^2`; the named blocker is gone for this one client (Section 3.3). |
| 30 | **raise (execution)** | Same funnel client as 26; the forced non-`A^2` sandwich surface is where a q=6 survivor, if any, would have to live. |
| 31 | **raise (execution)** | The block normalization/ZMT frame carries the funnel; the Kummer-unit mechanism of Section 3.2 is also naturally an avenue-31 statement. |
| 2 | unchanged | Still principal; the 17:58Z selector reset stands; nothing in this round supplies a td/type/entry selector. |
| 4, 36 | unchanged tier, **rebalanced inside** | Shift capacity from further literal rows to transition-image and source-completeness (Section 6); row extension keeps only cheap filler status. |
| 19, 21 | unchanged | Degree-twelve frontier and Hensel typing unchanged by this round. |
| 1, 3, 5, 6, 8, 9, 10, 12, 13, 14, 15, 16, 17, 20, 22, 23, 24, 25, 27, 29, 32, 33, 34, 35, 37, 38, 39, 40, 42, 43, 45, 46 | unchanged | No new evidence touches them; tried/stuck fields and concordant-low scores stand as tabled. |
| 11, 18, 41 | unchanged (dead/closed) | Refuted or closed; nothing reopens them. |
| 44 | unchanged (closed as proof) | Retain only the repaired membership theorem and the general prime-degree question; that remnant is consumed by Section 3.2, which does not resurrect the refuted headline. |

No avenue is lowered this round; no numbered avenue changes global tier.

## 2. Reranked gap lists

Global proof gaps, sharpest first:

1. **Presentation/selector arrows for the fixed-quadratic funnel** — basis
   existence (a hypothetical block need not admit any quadratic trace-zero
   presentation; the basis lane "cannot prove quadratic-basis existence"
   yet) and block-index selection (prime td admits no proper block at all,
   so the funnel is vacuous there; primitivity open). These become the
   binding constraint the moment q=6 closes; the funnel is absent from
   `ladder/REDUCTION.md` precisely because these arrows are unregistered.
2. **QCS** — unchanged content; the pair-square stalk gate and
   `STRICT-COLLIDE-POLY` remain the two live handles.
3. **`G2-PSC` transport** — the S:8 GGV-chain-to-Eggers–Wall functor is
   still the only named prove-lane.
4. **Bounded boundary-adaptation theorem** — without it the second
   `A1`-ruling classes cannot be imposed on the raw `D9` marking, blocking
   the strongest known refinement of the funnel machinery (08:33Z overlay).
5. **`G2-BD`** — via `A-SCALE(B_A) => UCD-A-min => G2-BD`; only residue-A
   routes owe it.

Counterexample/falsification gaps, sharpest first:

1. **Source completeness** — still unproved and now the cheapest place a
   single falsifier would collapse an entire framing (Section 6).
2. **Transition-image/occurrence** — the reviewed `e=2,m=2,h10=1` cell is
   the first fully closed dataset on which a scheme-image theorem can be
   computed rather than conjectured.
3. **Compatible all-order lifting** — blocked behind 2.
4. **Reachability** — blocked behind 1–2.
5. **Algebraization** — last; requires a typed compatible object first.
   The char-p degree-twelve frontier (avenue 19) remains the independent
   falsification-side alternative.

## 3. New avenue, new mechanism, new connection (history-checked)

### 3.1 Novelty audit method

Searched `APPROACHES.md` (master table + every overlay), `notes.md`,
`AUDIT.md`, `ladder/REDUCTION.md`, and the `2254Z` synthesis for: prior q=6
global lattice work (none exists — the q=8 packet's own firewall lists q=6
as not eliminated; only carrier *counts* exist in the corrigendum); prior
`Fulton`, `Deligne`, `nodal`, abelian-monodromy, or cyclic-monodromy
squeezes (none; the ACS cover and a stopped `ACS-CEIL` deficit-Euler
experiment exist, which my Section 3.2 does **not** reuse); prior BMY-on-F5
work (none; BMY appears only in the 2026-08-12 residue-A battery, left open
on unpinned tails).

### 3.2 Genuinely new mechanism: `AF-PI1-NODAL-SQUEEZE`

Composition of three promoted/banked items with two classical external
theorems; every arrow keeps its direction.

Charged campaign inputs: (i) ACS (promoted): a Keller map restricts to a
connected degree-`td` finite étale cover of `A^2 - F^{-1}(A(F))` over
`W = A^2 - A(F)`, and `A(F)` of a counterexample is reducible or singular;
(ii) the block-descent review corrections (BD-GAL memory): the degree-two
block died because rank two forces a complete intersection whose different
is principal, against "R supports no nonzero principal effective divisor";
(iii) the same review's prior-art flag that the `d1=1` Galois case is
likely classical (Campbell/Razar/Wright) — treated below as a source-audit
gate, not as new.

Target theorem T1: *for any plane Keller counterexample,
`pi_1(A^2 - A(F))` is non-abelian.* Sketch: if abelian, the ACS monodromy
is an abelian transitive subgroup of `S_td`, hence regular, hence
`C(x,y)/C(P,Q)` is abelian Galois; over `C` every cyclic layer is Kummer,
`C(P,Q)(g^{1/n})` with `g` an `n`-power-free polynomial representative.
Let `Y` be the normalization of the target `A^2` in the layer. Every
component of `div_Y(g)` lies over `{g=0}` with `n`-power-free multiplicity,
hence is ramified, hence lies in the ramification support, which the étale
open `A^2` misses pointwise. So `g` and `1/g` both restrict to regular
functions on `A^2`, forcing `g` constant, the layer trivial, and `td=1` —
contradiction. (This is the promoted S1 unit argument run at `d1=1`, where
it is *easier*: the first leg is an open immersion.)

Corollary T2 (the new geometric floor): by Fulton–Deligne, the complement
of a nodal projective plane curve has abelian fundamental group; Nori-type
refinements weaken nodality to numerical/local conditions. Contrapositive
of T1: *the projective arrangement `closure(A(F)) + L_infinity` violates
every such abelianness criterion.* Since each `A(F)` component is a
polynomially parametrized curve and therefore has one place at infinity,
the expected locus of failure is a controlled worse-than-node singularity
or `L_infinity`-tangency — an unconditional sharpening of the promoted
"reducible or singular", and a cheap necessary-condition filter for any
future `A(F)` candidate built from books (avenue 7's stuck point).

Named risks, stated up front: (a) the abelian-implies-automorphism corner
may be exactly classical Campbell — the flagged source audit must run
before any external novelty claim; the geometric corollary T2 through
Fulton–Deligne/Nori appears nowhere in the repo either way; (b) the Kummer
argument uses the *normalization's* ramification, not a suborder's
different, so the order-versus-maximal-order trap of the degree-`>=3`
block case does not arise, but review must recheck that; (c) Nori-type
hypotheses may never hold for one-place-at-infinity arrangements — in that
case the outcome is a documented no-go closing the abelian shortcut, still
cheap. This is card 2.

### 3.3 Genuinely new connection: log-BMY on the pinned q=6 cells

Avenue 28 × the funnel. BMY/log-Chern was `NEEDS-DATA` since 2026-08-12
because resolution tails were unpinned. The two q=6 survivors now have
*fully pinned* data: boundary dual graphs (Section 5), `rho`, `k`,
`K_Xtilde^2 = -1`, `e(Xtilde)=13`, the promoted `bar-kappa` control from
`BD-A2-FIRSTLEG`, and the exact completion-invariant `e(U)=12-r-c` with its
per-fibre partition refinement (each degenerate second-ruling fibre
contributes its own Euler defect, so `12-r-c` must decompose as an actual
fibre ledger, not just satisfy an inequality). A conceptual obstruction
here would answer the state packet's request for a non-enumerative q=6
kill. This is deliberately kept a sub-lane of card 1, not a third card:
if the lattice gate closes q=6 first, this becomes a redundancy check;
if a lattice cell survives, this is the next filter on that exact cell.

New-within-known: the corrected q=6 gate itself (Section 5) — the
`b_j = 2a_j + sigma_j` section-attachment correction, the `sum nu_Z <= 1`
carrier collapse, root confinement to `I_6`, and the `B_3` second fibre
component `M` — is new exact content inside the packet-named q=6 avenue.

## 4. Selector / QCS / `G2-PSC` / `G2-BD` portfolio

**Global selector.** Prove: (i) the intrinsic degree ceiling
`d_min >= ceil(deg Delta / 4)` (DISC8-INDEX, promoted) is the only
basis-independent presentation control in hand — the prove-lane is a
bounded-adaptation plus basis-existence pair on the block side, not a new
td selector; (ii) the prime/composite dichotomy should be made explicit in
`REDUCTION.md`: prime td forces primitive monodromy (no proper blocks), so
the entire quadratic funnel can only ever serve composite-td clients, and
any claimed global selector must say which branch it feeds. Falsify: extend
the `U1*(R)` no-go family toward a meta-theorem that *no* selector can be
built from bounded reduced local data alone — each new no-go instance
cheaply prunes selector proposals. Bypass: finish the funnel interior
(q=6, Section 5) so that all remaining pressure is provably on the two
presentation arrows, and bank `AF-PI1-NODAL-SQUEEZE` structure that any
future selector may consume. 

**QCS.** Prove: the pair-square stalk gate at one actual strict-henselian
conductor stalk (`PAIR-SQUARE-QCS` N2+N3 bar inertia-built `B`; the square
itself is still untested). Falsify: `STRICT-COLLIDE-POLY` via one
source-compatible polynomial-origin germ with zero profile excess. Bypass:
the pole-incidence/suspension boundary-complex formulation stays typed
`OPEN`; do not fill it by analogy.

**`G2-PSC`.** Prove: the S:8 transport functor (GGV chain to Eggers–Wall)
on one complete td6 class. Falsify: one exact fidelity-loss control — a
configuration whose GGV restriction and Sigray decoration provably
disagree. Bypass: pure-Sigray architecture (documented; costs all GGV
inputs).

**`G2-BD`.** Prove: the registered chain `A-SCALE(B_A) => UCD-A-min =>
G2-BD`. Falsify: a bounded-delay countermodel inside residue-A moduli.
Bypass: route around residue-A entirely (Sigray-only or funnel routes owe
no `G2-BD`).

## 5. The exact q=6 attack, and precisely why the q=8 proof does not transfer

### 5.1 Marked-fibre geometry (derived here, to be re-proved by the producer)

Both q=6 rows admit the relabelled `S`-adapted marking: fibre blowups
`P_1,P_2,P_3`, `l=3`, `T=S_0+3F-sum_(i=4)^9 P_i`, `S=S_0`, `S.T=1`.

`U_3/A_3`: fibre `F_0=2L+E_1+2E_2+E_3` with
`E_1=F-P_1-P_2`, `E_2=P_2-P_3`, `E_3=P_1-P_2`, `L=P_3`; star with centre
`E_2`; both sections and the smooth degree-4 polar germ meet `E_1` at the
same physical point (`Z=1` in the local packet), the cusp germ passes the
node `E_1 cap E_2`. With `m=(2,2,1)=h`, the exceptional polar part is
exactly `Z_H=2F-P_1-P_2-2P_3`.

`B_3/A_2`: Cartan integrality forces all three branch contacts onto one
end component (`a=3e_1`, `h=(2,1)`), and fibre consistency then *requires a
second strict fibre component* `M`: `F_0=L+E_1+E_2+M`, all multiplicity
one, with `L=P_l`, `E_1=F-P_l-P_a`, `E_2=P_a-P_m`, `M=P_m`, chain
`L—E_1—E_2—M`. Generic (`tau!=0`, `Delta=9-8tau!=0`) polar: three germs of
`A`-degrees `4,2,2` at distinct smooth `E_1` points, `m=(2,1)=h`,
`M_0=Z_H=2F-2P_l-P_a-P_m`.

In both rows the strict ramification total is

```text
C^(0)=r^*R_X-Z_H=4S_0+9F-sum c_iP_i,
c=(1,1,0,2,2,2,2,2,2),   sum c=14,   sum c^2=26,
C^(0).S=C^(0).T=1,       C^(0).L=0   (and C^(0).M=1 in B_3).
```

### 5.2 Why the q=8 mechanism fails verbatim

1. **H-attachment.** At q=8 all strict primes were disjoint from
   `S_0,L,T` (`C^(0).S_0=0`), giving `b_j=2a_j`. At q=6,
   `C^(0).S=C^(0).T=1`: exactly one strict prime meets `S` and one meets
   `T` — through the partially resolved tangency point on `E_1`. The
   disjointness step is *false*, and `b_j=2a_j+sigma_j` with
   `sigma_j=C_j.S in {0,1}`, `sum sigma_j=1` (likewise a `tau_j` unit
   constraining the `I_6` row sums).
2. **Horizontality.** The q=8 argument "`a_j>0` because the marked fibre
   contains only `L` and its root tree" breaks in `B_3/A_2`: the fibre
   contains `M`, a strict non-exceptional vertical component that *can* in
   principle carry different mass. The unpromoted `tau=0` modulus cell's
   "residual block at the opposite endpoint" sits exactly at the `M`
   attachment; its exact simple representative even shows a vertical prime
   of Cartier coefficient two (outside the coefficient-one subcell).
   `U_3/A_3` has no `M`; horizontality survives there.
3. **Local input.** The `B_3/A_2` special moduli (`Delta=0` splitting;
   `tau=0` residual block) are explicitly not promoted; and at a shared
   attachment site the forest theorem does **not** force two strict primes
   to be disjoint (both can attach through one inserted SNC curve without
   a cycle), so the gate must carry a shared-site exemption graph.
4. **Budget.** Euler slack is 3 units (`rho+k=5<=8`), not 1. But the
   lattice itself collapses the allocation set: every q=6 carrier class
   contains both `O`-pair indices (corrigendum), and `c=1` there, so
   `sum nu_Z <= 1` — at most one contracted carrier, Cartier coefficient
   one, sharper than both the Euler cap and the generic-DVR `nu<=4`.
   Affine roots are confined to `I_6` index pairs (an `O`-pair root has
   the class of `E_3` resp. `E_2` and is impossible), with `mu<=2`.

### 5.3 Corrected gate and scratch outcome

With `sigma,tau` as above, the pairwise identity becomes

```text
C_j.C_k=2a_ja_k+a_j sigma_k+a_k sigma_j-sum_i x_(ji)x_(ki),
sum_i x_(ji)=a_j+2b_j-d_j,     sum_(i in I_6)x_(ji)=a_j+b_j-tau_j,
```

and the forest gate is `C_j.C_k=0` for pairs at distinct sites. Closed
form: the baseline bound is `sum_(j<k)C_j.C_k >= 2 sum_(j<k)a_ja_k +
(4-a_(j*)) - 6 > 0` in both rows (using `Q>=sum c` and the fibre-side
constraints `a_1<=2`, `a_2>=2` in `U_3`), so both baselines die; the
`mu=2` root cells are borderline (bound exactly 0) and need the
per-coordinate refinement, exactly as `B_6`'s coefficient-two case did at
q=8.

Unsealed scratch enumeration (this report, standard library, ~15 s total,
relaxed constraints, elimination-safe): zero pairwise-disjoint survivors in
*every* singleton cell tested — `U_3` baseline (191 linear / 51
adjunction-valid / 0), `B_3` generic baseline (4440/780/0), all 20 one-`Z`
cells in each row (0), all oriented `mu=1` and `mu=2` `I_6` roots (0
survivors, including the borderline cells), the `B_3` `Delta=0`
irreducible `4+4` cell (0), the `Delta=0` tangent-pair cell *even with the
shared-site pair exempted from the zero gate* (0), and every alternative
assignment of the `S/T` units to other primes (0). Not yet enumerated:
multi-root combinations (`r_aff<=3`), root+`Z` combinations, affine
`A_2/A_3` trees with their coefficient vectors, and the `tau=0`
vertical-`M` subcell, which needs new local input first. A sealed producer
must re-derive the constraint set from the geometry rather than trust this
report. This is card 1; stake: with the q=8 review and this closure, the
*entire* normal-plus-nonnormal reduced finite-near-infinity fixed-quadratic
stratum would be empty, moving all funnel pressure onto the presentation
arrows (Section 4) plus nonreduced/basepoint/degree-drop strata.

Non-quadratic alternatives, per the packet's requirement: the strongest is
the QCS pair-square stalk gate (Section 4); the cheapest is
`AF-PI1-NODAL-SQUEEZE` (Section 3.2), which touches no quadratic
presentation at all.

## 6. K00: strongest next attack

Explicit comparison, information gain per unit cost:

1. **Transition-image theorem on the closed cell (best).** The reviewed
   `e=2,m=2,h10=1` closure through G13 is the first complete finite
   dataset; compute the scheme-theoretic image of one truncation map
   (G11→G13) from the 569 frozen tails. Empty or low-dimensional stable
   image quantifies compatible-tower pressure for *every* future cell;
   a nonempty stable image redirects the program toward lifting. Either
   outcome is structural; more rows cannot substitute for it. Desk to
   AWS-light exact linear algebra.
2. **Source-completeness falsifier (close second, cheapest global kill).**
   Enumerate the declared partition (C6=0 tip, K10/Jdet boundary faces,
   other supports, ramified `(e,m)` outside the closed calendars) and hunt
   one DVR-section shape provably outside every registered calendar. A hit
   collapses K00-as-atlas at a stroke; a completed covering enumeration is
   the first genuine completeness evidence. Three closed calendars are
   currently *zero* evidence of completeness.
3. **All-order lifting / reachability / algebraization**: correctly gated
   behind 1–2; building them now would type objects that may not exist.
4. **More rows (worst as a primary).** `e=3,m=2` G13–G21 and the reopened
   `e=2,m=1,h10=1` G8 face are cheap background fillers only; each closes
   one face of an atlas whose completeness is unproved. Do not let the
   compiler's convenience set the agenda.

## 7. Mathematical-software acceleration

`FOREST-LATTICE-KERNEL/v1`: factor the q=8 replay and the Section 5 gate
into one parametrized standard-library kernel: input = marking state
(`q`, `I`-sets, fibre classes), local vector table, budget caps
(`rho+k<=8`, `sum nu_Z`, root confinement), attachment units
(`sigma/tau`), and a shared-site exemption graph; output = closed-form
bound per cell plus exhaustive orbit enumeration plus deterministic JSON
digest, zero-`Assert` AST, identical tri-mode output, forged-survivor
negative control. Regression gate: byte-reproduce the q=8 digest
`dcfbe164178ea93e85c608526edb588ea96aaf6d6488c3865660c708b79f3da7` and the
zeros of Section 5.3. Decisive experiment: the full q=6 allocation closure
(all multi-root/`Z` combinations) — projected well under a minute of desk
time. The same kernel then serves nonreduced-infinity and higher-block
strata, and replaces per-packet ad hoc replays.

## 8. Campaign-systems `UPGRADE` card

`PROVIDER-CAPACITY-PREFLIGHT/v1`. Evidence of need: repeated full launch
choreographies (immutable prompt freeze, custody, receipts) consumed by
HTTP 402/spend-ceiling refusals before any model work — Grok twice, Fable
and Opus at the 05:32Z review attempt, and again per state packet §1.5.
Change: a capped no-op capacity probe per provider before prompt freeze;
on refusal, the lane is recorded `UNAVAILABLE_NO_MODEL_WORK` without
creating a prompt artifact or consuming the packet's launch slot.
Smallest measurable acceptance test: mocked 402/429/200 endpoints —
(i) 402/429 produce a typed `UNAVAILABLE_NO_MODEL_WORK` receipt, zero new
prompt artifacts, nonzero exit; (ii) 200 proceeds byte-identical to the
current path (hash-compared artifacts); (iii) identical behavior in
ordinary, `-O`, and `-OO` (zero-`Assert` gate per the 1.6 lesson); plus
one live capped probe per configured provider logged once. Probe token
cost must be capped and recorded. If the maintainers judge the probe
unimplementable without billing risk, the fallback is `NO_CHANGE` with
this card retained as evidence of the recurring waste pattern.

## 9. Research cards and ranked launch portfolio

### Card 1 — `BD-A2-Q6-F5-GLOBAL-CLOSURE/v1`

- **Novelty:** NEW (avenue named by the packet; the corrected gate,
  `sum nu_Z<=1` collapse, `M`-component horizontality failure, and all
  zero-survivor evidence are new; no prior q=6 global analysis exists).
- **Dependencies:** promoted forest/Euler/carrier theorems + corrigendum;
  provisional unbalanced local table (only its `U_3` row) — chargeable
  exactly as the q=8 packet charged its (0.2), i.e. as a stated exact
  hypothesis pending review; NEW local completion required for `B_3`
  special moduli (`Delta=0` splitting and Cartier data; `tau=0` residual
  block and vertical-`M` multiplicity).
- **Theorem:** neither `B_3/A_2` nor `U_3/A_3` has a global lattice
  survivor in the relaxed nine-blowup lattice, in any Euler-allowed
  allocation, under the corrected `sigma/tau` gate with shared-site
  exemptions.
- **Cheapest discriminator:** already run for every singleton cell (all
  zero); the open discriminator is the multi-root/`Z`-combination
  enumeration plus the `B_3`-special local table.
- **Outcomes:** all cells zero ⟹ (conditional on the q=8 review) the
  singular-F5 cell and with it the entire reduced finite fixed-quadratic
  stratum is empty; a surviving cell ⟹ the campaign's sharpest concrete
  realization target, handed to the BMY-pinned sub-lane (Section 3.3);
  `tau=0` local work reveals a genuine vertical different ⟹ a new typed
  subcell, not a defeat.
- **Stop condition:** sealed producer + different-model review, or one
  surviving cell frozen with exact coordinates.
- **Info gain:** highest available on the proof side this round.
- **Compute class:** desk (<60 s); kernel of Section 7.
- **Ownership:** prove = producer lane; falsify = `B_3` modulus-special
  local analysis (it is where a survivor would hide); bypass = BMY-pinned
  sub-lane. Provisional descendants may start now; promotion waits on
  review.

### Card 2 — `AF-PI1-NODAL-SQUEEZE/v1`

- **Novelty:** NEW as a composition (Section 3.1 audit); the `d1=1`
  Galois corner carries the standing prior-art flag (likely Campbell) and
  a mandatory source audit before any external novelty claim.
- **Dependencies:** promoted ACS theorem; BD-GAL review corrections;
  classical Fulton–Deligne/Nori statements (external, to be source-pinned
  at the next licensed sweep).
- **Theorem (T1):** `pi_1(A^2-A(F))` of a Keller counterexample is
  non-abelian; **(T2)** hence `closure(A(F))+L_infinity` fails every
  nodal/Nori abelianness criterion — an unconditional singularity/tangency
  floor on `A(F)`.
- **Cheapest discriminator:** desk-verify the Kummer-unit one-liner and
  the abelian⟹regular⟹Galois steps; then map exactly which
  abelianness criteria can apply to one-place-at-infinity arrangements.
- **Outcomes:** proved ⟹ banked structure + a necessary-condition filter
  for any future `A(F)`/book candidate + permanent closure of the abelian
  shortcut; a gap found ⟹ the exact location where Kummer costume
  survives étale-ness, feeding the avenue-44 remnant; prior art ⟹ cite
  and keep T2's geometric refinement if it survives.
- **Stop condition:** sealed desk note + different-model review; no CAS.
- **Info gain:** moderate-high at very low cost; unconditional (no
  presentation, no funnel scope).
- **Compute class:** zero beyond desk prose.
- **Ownership:** prove = desk producer; falsify = search for an abelian
  nodal arrangement compatible with polynomial-curve infinity behavior;
  bypass = none needed.

### Card 3 — `K00-IMAGE-VS-COMPLETENESS/v1`

- **Novelty:** KNOWN objects, new discriminator framing (the packet asks
  for exactly this comparison; no image computation has ever been run).
- **Dependencies:** reviewed `e=2,m=2,h10=1` G13 closure and its 569
  frozen tails; the comparison-functor machinery; loaded-source firewall.
- **Experiment:** (A) scheme-theoretic image of the G11→G13 truncation map
  on the closed cell; (B) enumerate the source-completeness partition and
  attempt one uncovered-face falsifier.
- **Cheapest discriminator:** (A) is exact finite linear algebra on frozen
  data; (B)'s first step is a finite list comparison.
- **Outcomes:** as in Section 6; every branch is structural; none is a
  map, arc, attainment, or JC2 claim.
- **Stop condition:** image computed and sealed, and partition either
  falsified or completely enumerated.
- **Info gain:** highest on the counterexample side.
- **Compute class:** desk; AWS-light only if the image algebra exceeds the
  desk bound (preregister in that case).
- **Ownership:** prove = image lane; falsify = completeness-falsifier
  lane; bypass = cheap G8/G13+ row fillers only as idle background.

### Ranked launch portfolio

1. Card 1 producer + its `B_3` special-modulus local subtask (desk;
   review after the q=8 verdict returns).
2. Card 3(A) transition image on the closed cell (desk).
3. Card 2 desk note (cheap, unconditional, independent).
4. Section 7 kernel (folded into card 1's replay engineering).
5. Section 8 systems preflight (build + acceptance test).
6. Idle background only: card 3(B) enumeration, `e2m1 h10=1` G8 filler,
   BMY-pinned exploratory note.

All lifecycle boundaries repeated: nothing here asserts effectivity,
occurrence, realization, an arc, a map, a counterexample, or a JC2
conclusion; scratch enumerations are unsealed and elimination-direction
only; provisional inputs stay provisional until different-model review.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `28233`.
- Body SHA-256:
  `3f9db83fe78f1f46c7b7cbff3edee9f26547c7a96abec2e2b6d9052e104af5c7`.
- Frozen basis: `dcd6cf9ccce35e7deee956245ca46d2dfd00d5d2`.
