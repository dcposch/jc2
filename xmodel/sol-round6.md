# Instrument Round 6: the family D25 decision

## Five-line executive summary

1. **DEFECT RULING:** $E_{29}\neq0$ forces the repaired candidate to use 30 inputs and 30 measured outputs with no free stream; withdraw `e=295` for that object, while retaining CYCLIC-30 as an all-depth gate.
2. **STOP RULING:** the old 1,080 minor certificates do not transfer, but an exact all-30-stream replay through $t^{40}$ gives \(\ell^+\ge31>11\) at all 36 sampled `a00pp` operators, so the substantive D23 stop survives in corrected form.
3. **D25 COMPILER:** one exact master emission can cover all 36 atlas fibers over their split finite étale coefficient algebra; it shares Row 24, one rank-four Schur transform, and one monomial skeleton, but it must retain 36 coefficient components and componentwise verdicts.
4. **DECISION:** family-wide D25 EMPTY unconditionally kills every scoped mod $p$ formal germ; D25 NONEMPTY only deepens survival, while a promoted corrected \(e^+\le12\) would be the decisive germ-track signal.
5. **POSTERIOR (CONJECTURAL):** I assign 6% to all 36 D25 fibers being EMPTY, 22% to a mixed outcome, 72% to all 36 remaining NONEMPTY, and only about 5% conditional probability of finding a corrected `E_CANDIDATE^+ <= 12` on a D25 survivor.

## 1. Adjudication of the two Round-5 defects

### 1.1 $E_{29}$: exact refutation and the repaired object

**FACT.** The assertion `eta29 == 0` in Round 5 is false. The first term of $E_{29}$ occurs at $t^{36}$, and the derivative of this coefficient is supported on the six residue-four input coefficients at absolute tail level 48. In characteristic zero,

\[
 \partial_{\mathrm{tf1}_{48}}
 [t^{36}\eta^{29}]\mathcal E
 =-\frac{147880611647231905314470776689}{1024}\ne0.
\]

Its value happens to vanish at every banked witness; that does not permit deletion of its derivative row. The other eta components $E_{30},\ldots,E_{33}$ vanish through the computed band 41, and the remaining mod-6 grading checks pass there.

Put $u=t^6$. The corrected source and target are

\[
 X=\bigoplus_{q=1}^{30}t^{r_q}k[[u]],
 \qquad
 Y^+=\bigoplus_{a=0}^{29}t^{s_a}k[[u]].
\]

The 30 source streams are the six residues of each of `tf1,tf2,tg1,tg2` and the three even residues of each of `tg01,tg02`. Their shifts remain

\[
 r_\rho=6+\rho\quad(\rho\ne4),\qquad r_4=16,
 \qquad \sum_qr_q=288.
\]

For $0\le a\le28$, retain the Round-5 shifts

\[
 s_a=6+2((a+1)\bmod3),\qquad s_{28}=16;
\]

add $s_{29}=36$. Thus \(\sum_{a=0}^{29}s_a=276\). Under **REQUIRED GATE CYCLIC-30**, the repaired candidate is the single square Ore operator

\[
 L_s^+=D(H_0,\ldots,H_{29})_s
 \in\operatorname{Mat}_{30}
   \bigl(k[[u]]\langle\Theta\rangle\bigr),
 \qquad \Theta=u\frac d{du}.
\]

There is no candidate free stream, no family of 30 minors, and no minimization over a free-stream index. The completed equation ideal must likewise be corrected to

\[
 \widehat I_E^+
 =\overline{\bigl([u^j]H_a:0\le a\le29,\ j\ge0\bigr)}.
\]

For a causal $30N\times30N$ truncation $M^+(N)$, define

\[
 \delta^+(N)=30N-\operatorname{rank}M^+(N),
\]

and define the delayed inverse loss by

\[
 \ell^+=\min\{\ell\ge0:
 F^{n+\ell}Y^+\subseteq L_s^+(F^nX)\text{ for every }n\ge0\}.
\]

Only after an exact Ore normal form proves stabilization to a finite defect $d^+$ may one record the index diagnostic

\[
 (e^+)^{\mathrm{idx}}
 =6d^++\sum_as_a-\sum_qr_q
 =6d^+-12.                                      \tag{1}
\]

Equation (1) is not itself a DEPTH $e$. Promotion requires $(e^+)^{\mathrm{idx}}\ge0$, $0\le\ell^+\le(e^+)^{\mathrm{idx}}$, the direct global-$t$ weighted-pivot checksum, a causal delayed parametrix realizing that loss, the completed-ideal/source-generation bridge, the quadratic remainder estimate, and **CONJECTURE E-HENSEL** (or an admissible nonlinear coordinate change eliminating the Euler terms).

**REQUIRED ALL-DEPTH GATE — CYCLIC-30.** Prove that $H_0,\ldots,H_{29}$ are all the output streams and that every higher eta component vanishes, including the $t^{42+}$ x-side corrections. The current computation is only a regression through band 41. In particular, $N\ge2$ for $H_{29}$ reaches $t^{42}$, so a corrected stabilized $d^+$ cannot be recovered from the present pure-y truncation.

### 1.2 Ruling on `e=295` and the stop rule

**RULING — `e=295` IS WITHDRAWN FOR THE CORRECTED OPERATOR.** The number 295 was

\[
 6\cdot56+7-48
\]

for the old winning `tf1/r1` (29\times29) minor, whose omitted input shift was $r_c=7$. It is neither the value nor a bound for $L_s^+$. The artifact `delta_plus_eta29` is also not a corrected square truncation: it is a $(29N+1)\times29N$ matrix that still omits the selected input. Strictly, no promoted DEPTH $e$ existed even before this correction. Preserve `295` only under a name such as `OLD_29MINOR_E_INDEX`; do not use the derived target $D=591$.

**RULING — THE SUBSTANTIVE STOP SURVIVES, WITH A NEW CERTIFICATE.** A Round-6 read-only replay assembled all 30 inputs and all 30 outputs through global band 40 at the 36 points of 8.S7. The weighted window has 174 output coordinates and 170 input coordinates. Its rank is 117 at 27 points and 115 at nine. In every case, adjoining the unit target supported on $(H_{29},u^0)$, of global level 36, raises the rank by one, so that target is outside the image.

Since $F^6X=X$, the inclusion defining \(\ell^+\) fails at $n=6$ for every \(\ell\le30\). Hence

\[
 \boxed{\ell^+\ge31>11}
\]

at all 36 sampled operators. Causality makes this a certified lower bound: inputs above band 40 cannot repair a missing target at band 36, and the $t^{42}$ x-side caveat is irrelevant to this finite-window obstruction. This projection-level stop remains valid even if CYCLIC-30 later discovers further output streams.

Replace the statement “all 1,080 blocks stop” by:

> **CERTIFIED MOD-$p$, FINITE-WINDOW FACT:** all 36 corrected full operators stop above the D23 threshold.

These 36 operators are **six witnesses times three deep draws times two primes on the single `a00pp` radical fiber**. They are not the 36 fibers of the atlas. No family-wide $e$ statement follows.

### 1.3 DEEPMAP: exact label correction

**FACT / BANKED MEASUREMENT.** The emission-to-true-stream involution is

```text
tf1_49 <-> tg1_49       tf2_49 <-> tg2_49
tf1_54 <-> tg02_54      tf2_54 <-> tg01_54
tg1_54 <-> tg2_54
```

Equivalently, the D23 reconstruction registry must read

```text
x74=tg02_54  x75=tg01_54  x76=tg2_54   x77=tg2_49
x78=tg1_54   x79=tg1_49   x80=tf2_54   x81=tf2_49
x82=tf1_54   x83=tf1_49
```

The levels and $W_1/W_2$-side assignments were already correct. Only this relabeling makes all band-$\le22$ witness rows vanish and gives the exact 86/86 derivative-row match. `valuation_e2.py` and `d23_ecandidates.json` already use the corrected involution, so their matrices are not discarded; all D25 reconstruction code must now make the true names canonical and treat the old names as an import-only convention.

The $E_{29}$ defect changes the later derivative object. It does not change the primal Row-24 equations. DEEPMAP does affect D25 because the full D23 reconstruction DAG feeds those equations.

## 2. Final family-level D25 emission specification

### 2.1 Can one system cover all 36 fibers?

**YES AS ONE EXACT FAMILY OBJECT; NO AS ONE REUSED SPECIALIZATION.** Let

\[
 B_{36}=\mathbf F_{105337}[\omega_1,\omega_2,\sigma_1,\sigma_2]/
 (\omega_1^3-1,\omega_2^3-1,\sigma_1^2-1,\sigma_2^2-1).
\]

At this split prime and the fixed $r_3$ embedding used by the atlas,

\[
 B_{36}\simeq\prod_{\lambda\in\Lambda}\mathbf F_{105337},
 \qquad
 \Lambda=(\mathbf Z/3)^2\times\{\pm1\}^2.
\]

Use

\[
 A_1=A_{1,0}\omega_1,\qquad
 A_2=A_{2,0}\omega_2,\qquad
 HW_i/W_i=h_{32}\sigma_i.
\]

This ring is the exact disjoint union of the 36 atlas fibers. It permits one source registry, one Row-24 emission, one Schur transform, one reconstruction topology, and one component-vector normal-form compiler.

All D25 algebra is performed in the $W$-chart ring

\[
 \mathcal R_{36}=B_{36}[z,a]/
 (\mathit{uW1}\,W_1-1,\ \mathit{uW2}\,W_2-1),
\]

where $z$ includes `W1,W2,uW1,uW2`; equivalently, localize at $W_1W_2$. Thus $W_i$ and $HW_i=h_{32}\sigma_iW_i$ are units. “Unit” below always means a unit in this chart ring on the indicated idempotent components, not merely a nonzero polynomial.

The atlas coefficient laws become

\[
 g_3=g_3(\omega_2\omega_1^{-1}),\qquad
 g_2=g_2(\omega_2,\sigma_2),\qquad
 g_1=g_1(\omega_1,\omega_2,\sigma_1),
\]

giving respectively 3, 6, and 18 coefficient classes. This is useful compression, but it is not a reduction to 3, 6, or 18 solver cases: the joint $g_1,g_2$ key determines all four fiber labels and is injective on the 36 fibers. Keep all three $g_i$; the atlas shows that omitting any one changes the ideal even though the three together cut codimension two.

**CONJECTURE D25-TRANSPORT.** There may be a character-diagonal coordinate change transporting every complete D25 fiber to `a00pp`. The class laws and support identity suggest it, but do not prove it. No solver deduplication may use this conjecture.

### 2.2 The family $G_{23}$ cache

Let $e_\lambda$ be the 36 primitive idempotents of $B_{36}$, and align the 509 monic reduced bases using the common monomial order and the atlas's element-for-element support identity. Define

\[
 \widetilde G_k=\sum_{\lambda}e_\lambda G_{k,\lambda},
 \qquad 1\le k\le509.
\]

Every component has the same leading monomial and leading coefficient one, so \(\widetilde G=(\widetilde G_1,\ldots,\widetilde G_{509})\) is a monic componentwise Gröbner basis over $B_{36}$. Store each polynomial as one support skeleton plus a 36-entry coefficient vector. One leading-monomial index can execute all components in parallel; whenever an intermediate coefficient is a zero divisor, split the reduction trace by its idempotent support mask.

This construction does **not** license copying `a00pp` coefficients into the other components. `d23_atlas_p105337.json` contains hashes, statistics, and the $g$-rows, but not the other 35 full basis coefficient arrays. Production must ingest and hash the actual 36 cached bases. Do not assume that the remaining 506 basis polynomials obey the smaller 3/6/18 class partitions.

### 2.3 Build only the full-live pristine Row 24

Use `VDEG_CAP=25`, call the source generator through absolute level 56, freeze the B-side, and retain `PIN42`. Keep **every** D23 lift coordinate and dormant coordinate live. Extract only the nine $k=24$ components of the original series equation. Assert:

```text
Row 23 == 0
eta support(Row 24) == {2,5,8,11,14,17,20,23,26}
frontier ==
  tf1_51, tf1_56, tf2_51, tf2_56,
  tg1_51, tg1_56, tg2_51, tg2_56, tg01_56, tg02_56
```

Reproduce the banked 90-entry first-occurrence digest

```text
ab5ee038b118ba6d0b6303c6b2b55b110dc9872c203e1c6b2d60044458e69d29
```

before consuming the nonlinear payload. `directionb_strike.build_frozen`, called by `sol_algkill.py::check_row24`, is a symbol test only; it must never provide $b_{24}$. Compare the ten frontier derivatives of the full-live row to the frozen symbol explicitly.

### 2.4 One universal Schur transform gives five residuals

Write the nine pristine rows as

\[
 F_{24}=A_{24}y+b_{24},
\]

where $y$ is the ordered ten-variable frontier above. The banked rank/pivot test and a new exact Round-6 read-only derivation give

\[
 A_{24}=C_{24}\operatorname{diag}(
 A_1W_1,A_1^2,A_2W_2,A_2^2,
 A_1HW_1,A_1^2,A_2HW_2,A_2^2,A_1^2,A_2^2).       \tag{2}
\]

Here $C_{24}$ is a constant $9\times10$ matrix over $K_3=\mathbf Q(\sqrt3)$, has rank four, and has pivot columns 0,1,2,3. All ten diagonal factors are units on every atlas component of the $W_1W_2\ne0$ chart. Consequently one constant rank-five left kernel $L_{24}$ works on all 36 fibers:

\[
 L_{24}C_{24}=0,
 \qquad
 \exists y\ F_{24}=0
 \iff c_{24}:=L_{24}b_{24}=0.                    \tag{3}
\]

Equation (3) gives exactly five family compatibility residuals, together with a saved solve map for four frontier variables and a six-dimensional frontier kernel.

The explicit factorization and $L_{24}$ are **ROUND-6 EXACT DERIVATIONS, NOT YET BANKED**. Production must re-certify them. The current deterministic, serialization-specific read-only hashes are

```text
C24_90_SHA256 d54a9af631b9e629873240aa2ad0509df7e16608053139c09893c33b9a201801
L24_SHA256  fc1e9b10224f690f97886d075837133e30622c34e9d5318b59d6a0a488443bd4
```

The production manifest must state the coefficient serialization and deterministic RREF rule. It must also reduce $C_{24}$, its row-operation matrix $U_{24}$, and $L_{24}$ at each working prime. Read-only checks give rank four with pivot columns 0,1,2,3 at $p=105337,105673,200257$, but these checks are not yet banked.

For extension of one fully fixed D23 point, the four level-51 values are fixed and the six new level-56 columns have rank two, leaving seven compatibilities. That is not the family existential problem. The family compiler must leave level 51 live and use the five-residual projection.

### 2.5 Reconstruction and coefficientwise normal form

Let $z$ be the 22 det23 base variables and $a$ the unresolved lift variables. Replay the D23 manifest over the chart ring $\mathcal R_{36}$:

1. Register the 21 band-free coordinates
   `x18..x23, x26..x31, x33..x36, x38..x41, x48`, together with `x16,x24,x69`.
2. Leave `x32,x37` free and solve `x17,x25` using the certified unit $2\times2$ minor.
3. Backsolve the 22 band pivots in reverse at-use order.
4. In the Row-22 deep block, choose six kernel coordinates and solve four. Certify the chosen minor as a unit in all 36 components; otherwise split by idempotent masks. Use the corrected x74--x83 map from Section 1.3.
5. Register `uf30`, the four level-53 tails, and the four possible ordinary level-55 tails. The four level-51 tails belong to the Row-24 frontier and all six level-56 tails are new frontier variables. Prune only after an exact occurrence census.

Substitute this DAG only into the five rows $c_{24}=L_{24}b_{24}$, never into all nine pristine rows. Expand each residual as

\[
 c_\nu(z,a)=\sum_\alpha h_{\nu,\alpha}(z)a^\alpha
\]

and compute

\[
 R_\nu(z,a)=
 \sum_\alpha
 \operatorname{NF}_{\widetilde G_{23}}
       (h_{\nu,\alpha})(z)a^\alpha.              \tag{4}
\]

The $a$'s remain polynomial variables; never turn them into an invertible coefficient field. Save exact membership traces

\[
 c_\nu-R_\nu=\sum_{k=1}^{509}q_{\nu k}\widetilde G_k. \tag{5}
\]

Strip content only if it is a unit of $\mathcal R_{36}$, i.e. a $B_{36}^{\times}$-valued coefficient times a Laurent monomial in $W_1,W_2$. A coefficient vanishing on any component is a zero divisor and requires an idempotent split and component mask. If another affine block appears after (4), Schur it only across components on which its pivot minor is a chart unit; retain every other rank chart.

The sparse family quotient is

\[
 J_{25}^{B}=I_{23}^{B}+\langle R_1,\ldots,R_s\rangle,
 \qquad 0\le s\le5.                              \tag{6}
\]

Its relative shape is $(22+r)$ variables and $(29+s)$ equations: the 26-row D21 core, all three $g_i$, and the nonzero D25 residuals. Keep five family rows when a residual is supported on only some idempotent components. A rank-complete pre-Schur quotient has $(32+r)$ variables and 38 equations. The full control is the 72-row pristine D23 fiber plus all nine Row-24 rows: 81 equations and 92--101 variables. If the family algebra is flattened to an ordinary field system, add the four selector variables and their four defining equations.

Use the 509-element $G_{23}$ bases only as normal-form engines. Do not prepend their 405,524 terms to an ordinary F4 input; solve the sparse 26-plus-3-plus-residual presentation.

### 2.6 Output contract and launch gates

Emit from one source of truth:

- a family manifest with all 36 evaluations, radical/chart data, variable order, source hashes, and the corrected deep-label map;
- the aligned coefficient-vector $\widetilde G_{23}$ and all 36 basis hashes;
- raw pristine Row 24, $C_{24},L_{24}$, pivot solve and kernel maps;
- the reconstruction DAG and complete dormant-variable occurrence census;
- five reduced residuals, membership traces (5), and component-nonzero masks;
- the sparse family quotient, the 81-row control, and either 36 solver-ready specializations or one ordinary selector-variable union system;
- a 36-bit EMPTY/NONEMPTY mask, dimensions, and reconstructed witnesses over $\mathbf F_p$, finite extensions, or symbolic coordinate algebras as required.

The mandatory gates are:

1. The selector algebra has 36 distinct evaluations; its idempotents are orthogonal, sum to one, and pass evaluation/interpolation round trips against every atlas label.
2. Every specialization of the master D21 rows, all three $g_i$, and all 509 $\widetilde G_k$ is exactly its cached fiber artifact; common support, leading monomial, monicity, and order are rechecked.
3. Prove both ideal inclusions between the sparse 26-core-plus-three-$g$ presentation and the 509-element $G_{23}$ basis on every component. Normal-form reduction of the sparse generators supplies only one direction; retain reverse membership/provenance certificates too.
4. The 3/6/18 class laws hold exactly. No class representative is substituted for an unproved transport.
5. Row 23, eta support, raw digest, full-live frontier equality, factorization (2), and all modular reductions pass. Verify $U_{24}\in GL_9$, $\operatorname{rank}L_{24}=5$, that $L_{24}$ is the complete left kernel, the four pivot columns are chart units, and every Schur round trip succeeds.
6. Carry a family-level D23 projection/backsolve certificate: rank two for the level-44 block, left-kernel span by all three $g_i$, all 22 unit pivots, and the Row-22 rank-four solve. Point replays alone do not support an EMPTY theorem.
7. Every reconstruction pivot is a family unit or has a complete idempotent rank split. No removed variable remains in (4), and every identity (5) replays componentwise.
8. Quotient points reconstruct through every dormant coordinate and satisfy all 81 pristine rows. Replay $\tau$, every $h$-sign branch, $W$-saturation, `PIN42`, and the `+42` mutation negative control.
9. Reproduce the family support at $p=105673$ before a structural cross-prime claim. A cross-prime D25 verdict requires the complete 36-component compilation and solve there, not merely matching support.

Preflight one residual. Warn at 100,000 intermediate terms or degree 16; stop explicit expansion at 250,000 terms and retain the DAG/hybrid form. Launch an F4 pilot only if the surviving lift count is small (target $r\le5$) and the sparse input remains below roughly 50,000 terms. These are engineering stops, not mathematical verdicts.

## 3. The exact D25 discrimination logic

Let

\[
 V_{25}=\bigsqcup_{\lambda\in\Lambda}V_{25,\lambda}
\]

denote the variety defined by the exact family object, with quotient/backsolve equivalence and every chart guard certified.

### 3.1 D25 EMPTY

If $J_{25}^{B}=(1_B)$, equivalently every one of the 36 specializations is `[1]`, then

\[
 V_{25}=\varnothing.
\]

Every formal germ has a depth-25 truncation. Therefore:

> **THEOREM AT THE STATED SCOPE:** family-wide D25 EMPTY proves that every D23 survivor was a finite-depth mirage and that no mod $p$ formal germ exists on the residue-A, B-frozen, no-log, `PIN42`, $W_1W_2\ne0$ chart at that prime and the atlas's fixed $r_3$ embedding.

This kill direction uses neither valuation $e$, CYCLIC-30, nor E-HENSEL. One empty component kills only that component. A union-level NONEMPTY result does not show that all components survive, which is why the 36-bit mask is mandatory.

D25 EMPTY is the point at which the residue-A kill campaign **resumes**, not the point at which a characteristic-zero theorem may already be announced. Repeat the exhaustive family calculation at a second good prime, then seek an exact $\mathbf Q(\sqrt3)$ identity with source-row provenance and discharge the conjugate $r_3$ embedding, other charts, no-log/`PIN42` coverage, and B-unfreezing. Modular emptiness alone is a screening-tier result.

### 3.2 D25 NONEMPTY

D25 NONEMPTY proves only that at least one point of $V_{25}$ projects to the D23 survivor locus. It deepens finite-window survival; it does not produce an inverse-limit point, a formal germ, a characteristic-zero germ, an algebraic branch, or a polynomial Keller pair.

For every surviving component, record the image and fiber dimensions of

\[
 \pi_{25,23}:V_{25,\lambda}\longrightarrow V_{23,\lambda}.
\]

A severe image contraction is useful mirage evidence, while a positive-dimensional image shared across the coefficient classes is germ-track evidence. Neither is a theorem about infinite depth.

### 3.3 The corrected $e$-test at D25

The D25 threshold is

\[
 \left\lfloor\frac{25-1}{2}\right\rfloor=12.
\]

Recompute the exact low-band part of $L_s^+$ at genuine reconstructed D25 points using the true deep labels. A finite D25 point does **not** determine its $t^{42+}$ x-side completion. Before following $H_{29}$ beyond its first coefficient, do one of three things: use a sufficiently long explicitly compatible lift that supplies every coefficient consumed; prove a finite-range invariance lemma; or retain and quantify over the unspecified completion variables. Never silently zero-fill them.

Track compatible points

\[
 s_{25}\leftarrow s_{27}\leftarrow s_{29}\leftarrow\cdots,
\]

not unrelated random witnesses or unrelated zero completions. Only an all-depth compatible chain, or a proved finite-determinacy result, supplies an all-depth operator.

| corrected D25 behavior | exact conclusion |
|---|---|
| A **promoted** $e^+(s)\le12$, with CYCLIC-30, an exact delayed parametrix, the completed-ideal bridge, and E-HENSEL banked | A mod $p$ formal germ exists: decisive germ-track. |
| A stable, internally consistent `E_CANDIDATE^+ <= 12` with measured \(\ell^+\le12\) | Strong germ-track evidence; no theorem. |
| Finite promoted $e^+(s)>12$ | D25 is too shallow; target $D=2e^+(s)+1$. |
| Certified \(\ell^+(s)>12\) | This point cannot supply a D25 Hensel certificate; it may still extend singularly. |
| Drifting/unbounded \(\delta^+(N)\), resonance, or $e^+=\infty$ | Mirage-like or singular-track behavior; not a kill. |
| Some later exact window is EMPTY | Definitive finite-depth mirage. |

Thus a small promoted $e^+$ distinguishes germ-track as a theorem; no large-$e$ observation distinguishes mirage as a theorem. The empirical germ-track signature is a bounded, stabilized defect and delay along one compatible chain, eventually with $D\ge2e^++1$. The empirical mirage signature is continued image contraction plus growing defect/loss, followed ultimately by EMPTY. A genuinely singular formal germ can imitate the latter until very deep order.

A locus-wide claim that no D25 point has $e^+\le12$ requires symbolic stratification of every D25 component. Witness sampling cannot prove it. If the present \(\ell^+\ge31\) obstruction persists at a D25 point, then any finite-$e$ target for that point is at least D63; this is not itself evidence of nonextension.

## 4. Updated probabilities

These are subjective epistemic estimates, not certificate grades. The 6/22/72 fiber-outcome partition is conditional on the exact family object and a completed modular verdict; it describes the mathematical outcome, not the probability that the compiler finishes. The small-candidate and loss rows are additionally conditional on D25 NONEMPTY and witness extraction. The outcomes are correlated; the 36 components must not be modeled as independent trials.

| event | Round-6 estimate | reason for update |
|---|---:|---|
| Honest family Row-24 structural census and certified quotient | **85%** (80--92%) | The support-aligned family ring and universal rank-four Schur step remove the main design ambiguity; full-live expansion and the missing 35 coefficient arrays remain engineering risk. |
| All 36 D25 fibers EMPTY | **6%** (5--10%) | D23 leaves an 11-dimensional locus on every fiber and Row 24 contributes at most five existential residuals; a kill needs a structured unit ideal, not an equation count. |
| Mixed EMPTY/NONEMPTY D25 mask | **22%** (15--30%) | Coefficients genuinely vary and the combined class key distinguishes all fibers, despite shared support. |
| All 36 D25 fibers NONEMPTY | **72%** (60--80%) | Family-wide codimension-two survival and common support make continued correlated survival the modal outcome. |
| At least one D25 survivor | **94%** | Complement of family-wide EMPTY. |
| Corrected `E_CANDIDATE^+ <= 12` somewhere | **5%** (3--10%) | Every corrected D23 sample already has \(\ell^+\ge31\); this extrapolates from 36 `a00pp` operators, not from all atlas fibers, and D25 may select a better-conditioned sublocus. |
| Initial D25 samples still have corrected loss (>12) | **90%** (85--95%) | The same \(\ell^+\ge31\) finite-window obstruction is uniform across all sampled points and both tested primes on `a00pp`; the all-depth loss itself is undetermined. |
| Fully promoted D25 formal-germ certificate in this round | **2%** (1--4%) | A small value would still need CYCLIC-30, an Ore parametrix, the completed-ideal bridge, and E-HENSEL. |

**CONJECTURE / LOW CONFIDENCE.** Conditional on D25 NONEMPTY but no candidate at or below 12, I currently split the route-wide outcomes approximately 60% that every D25 survivor dies at some later finite depth versus 40% that at least one scoped mod $p$ formal germ exists, with uncertainty of at least 20 percentage points in either direction. The latter germ could have finite (e^+>12) and simply be too deep for D25 certification; it need not be infinitely singular. Stabilized finite $e^+$ would move probability sharply toward germ-track; continued growth of \(\delta^+\), \(\ell^+\), and projection codimension would move it toward mirage. The probability that this local modular route already yields a polynomial characteristic-zero counterexample remains below 1%.

## 5. Final launch order

1. Ingest the 36 full $G_{23}$ coefficient arrays and bank the $B_{36}$ manifest; do not substitute `a00pp` coefficients.
2. Re-certify the full-live Row-24 frontier against the banked digest and bank (2), $C_{24}$, $L_{24}$, and their hashes.
3. Build the five Schur residuals once, replay the corrected reconstruction DAG, and reduce coefficientwise through \(\widetilde G_{23}\).
4. Specialize and solve the sparse family quotient, returning a 36-bit verdict mask and reconstructed 81-row witnesses for every NONEMPTY component.
5. On EMPTY, replicate the full family at a second good prime and start the exact characteristic-zero kill certificate. On NONEMPTY, compute projection dimensions and the corrected 30-output $e$-object on compatible D25 lifts.
