# Closed-scope holder and administrative admission — static templates

Owner /root/model_productivity. First action 2026-09-11 09:19:53.809415048 UTC; fixed reserve09:34 and hard09:37 UTC. Basis0d39df3c9fd69c939a8420c54d03228b9077777d. STATIC/UNEXECUTED; no worker, registration, authority or scientific result.

Only holder.template.py, outer-admission.template.sh, README.md, PINS.json and custody.json are prepared; frozen sources and registration remain untouched.

## Delivered boundary

The disabled Python3.12 holder and outer admission shell implement the launch-order separation, not a dispatcher release. Holder is an administrative file outside exact science/wrapper inventories; its root0700 FIFO channel is outside batch-output inventory. The shell arms the original absolute TERM/KILL duties and checks the separately armed ROOT worker-stop duty before admitting only this holder. It captures holder identity/kernel controls within60 seconds and returns ADMITTED_NOT_RELEASED. No final-registration serializer, installer, authority, leaf creation, token sender, source code change or runtime phase is supplied.

Holder accepts only a fixed73-byte RELEASE/token/newline frame followed by FIFO writer EOF. Nonblocking open, bounded poll/read, prefix checks and an absolute UTC plus120-second monotonic ceiling cover missing writer, partial token, extra bytes, writer remaining open and expired deadline. Duplicate bytes before EOF fail. A later separate writer cannot be excluded by a FIFO observation; the exclusive trusted-ROOT one-writer/no-restart commitment is explicit. No input specifies commands or environment. ROOT chooses the token and actual deadline strictly before the unchanged admission cutoff; both remain placeholders here.

The fixed waiting sentinel follows the absent-registration check and FIFO-open barrier. This lets ROOT distinguish a merely started process from the intended admission wait point before later finalization. A current PID/sentinel is still not atomic liveness evidence. ROOT must recheck actual process/start/namespace/cgroup/leaf identity immediately before its separately authorized release; existing stale-positive false-STOP risk remains accepted.

The no-child holder closes admission/inherited descriptors, installs null stdin, retains regular outer streams, resets signals/mask, checks cutoffs and replaces itself with the exact registered seven-element outer_argv through os.execve. Environment is exactly PATH=/usr/bin:/bin, LANG=C, LC_ALL=C, TZ=UTC. PID/start/cgroup and original unit lifetime/timers persist. CPU origin remains0; holder work is not subtracted. Existing outer properties and source/batch limits are unchanged. No full13 escalation or retry occurs.

## Static checks and honest qualification

All five inputs were prehashed. Four received fresh human-WHOLE reads; the large registration received complete JSON ingestion plus human context/selected complete fields. Clipped dispatcher511-780 output was recovered in fresh511-640 and641-EOF reads. No linked source body was followed. The literal old/new outer diff is retained in README; it includes all template changes, including basename transition, split-stage input placeholders, admission-only main command, sentinel/capture checks and STOP trap. No actual commands were run beyond allowed documentary text/hash/publication tools.

The holder OUTER and ENV are literal matches to charged registration metadata by manual comparison. Source inventories stay exact five/three; no admin/FIFO role is added. All actual host/boot/namespace/time/token/hash values remain unformed and guards disabled. Prospective layout strings use the mandated fixed base but attest no installed object. Registration itself is untouched with null device/inode fields and false enabled/exclusivity/no-migration flags.

Specific qualification gaps remain Linux FIFO event/EOF behavior, installed CPython signal/fd/exec behavior, systemd accounting/identity, shell metadata capture and actual native/namespace topology. These templates are source-reviewed-by-author only, not tested or accepted at runtime. External ROOT authenticates the final registration hash, source/native/physical fields and nine empty leaves, freezes it once0444 with fsync/readback, binds its one-time release record, and retains all original absolute stops. Holder's regular0444/path check is not an independent hash authenticator. Old split setup/final installer remains an explicit next ROOT duty.

QUANTITY: one same-PID holder admission-to-exec transition with the required closed-tree/identity and fixed-time preconditions. CHEAPEST TEST: focused review of these two exact administrative files and ROOT's completed physical metadata before any separately authorized qualification. Numeric planning wall10 minutes UNMEASURED for review;120 seconds is a ceiling to fit inside unchanged deadlines, not a runtime forecast or extension. No new foundation gate, mathematical review or execution is requested.

Own full text/JSON reads, literal diff comparison, input postpins and own-only collision/raised-OPEN checks precede the last completion marker. Sealed whole read and expected-manifest verification precede custody-first handoff. This static task becomes COMPLETED/ALL WRITERS IDLE at terminal; no follow-on authority.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5330`.
- Body SHA-256:
  `1516391426d7ac1901e17698340afbe5e53cf7744b90c641f8d4ee4b8b1c6e99`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
