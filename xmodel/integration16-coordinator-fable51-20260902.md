# INTEGRATION #16 — coordinator (Fable 5.1), 2026-09-02 ~17:15Z

Basis 709ccb5be7b80b4fb314f6fe1fa1f6eb19a29307. Binds the N-ON-THE-TREE flagship
(`n-on-the-tree-opus5-20260902.md`, 44223b32) and its different-model
hostile review (`n-on-the-tree-review-grok46-20260902.md`, 481d71bf),
and revises integration #15 §D. Scope throughout: Keller,
noninvertible, degree-minimal, Moh's gauge (deg = deg_y, monic in y,
two points at infinity, NU-TWO). Case (A) EMPTY and untouched; A2
untouched; no Z(G) = 1; H2 only where the campaign's N-window
[4, 16] is quoted.

## A. What is promoted (reviewed, with scopes)

1. **CONTACT-DEFICIENCY** reconfirmed with monicity load-bearing
   (negative control: off the monic gauge the resultant identity
   fails, 6 ≠ 2 and 12 ≠ 3). Scope repair to the depth lane's §5.1:
   same-slope orders are ≥ 0 only under Moh Lemma 6.1 / Prop 6.1;
   the budget form "sum same-slope ≥ 180 − N" is NOT promoted off
   that normalisation.
2. **THEOREM FRONTIER-N.** For every dominant F with both
   coordinates monic in y of y-degree equal to degree, generic
   (c_1, c_2): N = Σ_i (−λ_f^{(i)}(δ⁰_i))⁺ over the roots ρ_i of g,
   where δ⁰_i is the level at which λ_g^{(i)} vanishes. The c-shift is
   absorbed: N lives on the tree of f·g alone. 7/7 rows exact; 3
   recomputed by the reviewer.
3. **THEOREM RADIUS-ORDER** (NU-TWO scope, M_s = n − 2):
   λ_g(δ_r) = −n ∏_{j>r} P_j/Q_j = −n(1 − δ_r)/(n − M_r). Moh's
   logarithmic radius (Def 5.1(3), p.179, read from the page image)
   and the order of g at the general point of the disc are one datum.
   242,099 V-skeletons, 0 failures, reproduced.
4. **LEMMA DETECTOR-NULL.** By Moh Prop 6.1(2) (p.191) and Theorem
   (5) (p.200), every root of a minor disc with ord g < 0 is a
   distribution detector, so minor discs contribute EXACTLY ZERO to
   N. This inverts the depth lane's location of the missing input:
   it is not the minor-disc distribution but the sub-tree of the LAST
   major disc D_1, where Moh's minor criterion degenerates (r = 1:
   #roots ≤ n/(n + m) < 1; there is no M_0) and his recursion ends.
5. **THEOREM N-CEILING.** N ≤ U(skeleton) = u·d·(−λ_g(δ_1)) =
   u·d·e·(1 − δ_1)/(d + e), with u the multiplicity of the major
   linear factor of the top form and deg P = Kd, deg Q = Ke = D.
   λ_g(δ_1) ≥ 0 is impossible at a counterexample (it would give
   N = 0; Ax–Grothendieck gives N ≥ 2). The cover of the bottom-major
   discs by a_{s−1} = ue is crude; Moh's search (8)–(11) can only
   lower U, so every kill count below is a floor.
6. **NO LOWER BOUND from the skeleton; FILTER-INVERSION.** Unknown
   structure below δ_1 can only ADD contact, which LOWERS N; the
   skeleton cannot forbid the proportional configuration that drives
   N|_{D_1} to 0. Hence the filter "N ≤ 16" is compatible with every
   skeleton and rejects nothing; the operative test is U ≥ N_min.
7. **HARMONIC-BOUND** (degree-minimal, Moh's gauge, NU-TWO, Lemma
   6.1, 2 ≤ d < e): 1/N > 1/deg P + 1/deg Q, hence N < D·d/(d+e) < D/2.
   NOT for every noninvertible Keller pair (false of automorphisms;
   "unconditional" in the producer meant "no H2"). In the campaign's
   degree-minimal scope the gauge is available by the orbit bridge.
8. **MEASURED, confirmed against the hashed log, a D ≤ 60 rerun, and
   independent window arithmetic:** D ≤ 400: 3,874,261 branch-robust
   groups; U < 2 never (min U = 792/329); U < 4 on 1,147 (0.030%); no
   degree emptied. D ∈ [101, 200]: 19,455 of 116,385 groups get a
   window strictly inside [4, 16]; 418 are pinned to N = 4. At the
   MOH-SHARP-2 degrees {105,108,112,117,120} max U_rob = 224/5, so
   N ≤ 44 there (measured corollary, tightest at D = 120). Moh's four
   survivors carry U_tower = 9, 6, 10.5, 12, 8, 16 and sit inside
   N ≤ 16; (64,48) confirmed independently.

## B. Corrections to the record

- Integration #15 §D's sentence "its outcome can prove a window" is
  WITHDRAWN (AUDIT delta 15(a), now reviewed): the filter conditional
  was never satisfiable. OPEN[N-ON-THE-TREE] is CLOSED as posed
  (instrument exists; filter inverts); residue OPEN[D1-SUBTREE].
- "Provably U > 2 at all D" is DEMOTED to measured (D ≤ 400): the
  producer's 12/5 asymptotic is a lower estimate of a supremum, not a
  theorem. OPEN[U-GT-2-ALL-D] (bounded: min U_rob at D > 400).
- Moh 1983 "corrections": C5 (top-form exponent n/d_s) is an OCR
  restoration, confirmed against p.194; C6 is likewise an OCR
  restoration (p.200 already prints #roots ≤ n/(n − M_r)), NOT a
  Moh error; the n = 75 row is legible on p.202 (δ_1 = 1/2 [1/3]) and
  OPEN[DELTA75-BRACKET] remains open (printed 1/3 vs computed 2/3 at
  V_2 = 2; a δ ↔ 1 − δ swap on the alternative V is the candidate).
- RAMIFICATION-FLOOR's identification of proper places with
  bottom-major discs, and the S·n non-proper places with minor
  frontier balls, are NOT promoted (typing claims, unchecked).

## C. The reading (direct)

Every instrument built on 2026-09-02 — the cusp/homology cage, the
source curve E, the boundary package at general N, the meridian
floor, the polar ledger, the pencil genus, the satellite mass, the
Puiseux depth, and now the contact deficiency on Moh's tree — bounds
in one direction: D (or n, T, Ψ) from BELOW in N, or N from ABOVE
in D. A ceiling D ≤ C(N), the one statement that would cross Moh's
floor and finish the H2 residual, needs N bounded from BELOW by D.
No instrument in the record supplies that, and this lane shows why
the boundary tree cannot: its free datum (the sub-tree of D_1) only
ever lowers N. The campaign-level question is therefore
**OPEN[UPPER-TO-FLOOR]** (bounded: the integer N ∈ [2, 16] at a
degree-minimal counterexample): name a mechanism that bounds N below
by D, or prove that none exists on the boundary and redirect.

What the instrument CAN do is also new and concrete: it attaches an
N-window to every Moh skeleton, and pins 418 skeletons at
D ∈ [101, 200] to N = 4 exactly. Those are the first concrete
realisation targets the counterexample side has ever had — a
degree-minimal counterexample with N = 4 at an admissible degree must
have one of a finite, listed set of skeletons, each with U = 4.

## D. Direction

1. **OPEN[D1-SUBTREE] is the only untried place** for a floor from
   the boundary: impose [f, g] = 1 below δ_1, where no published
   argument (Moh's recursion needs r ≥ 2) has looked. Lane
   `d1-subtree-opus5-20260902` running. Either outcome is decisive
   for the boundary route: a Jacobian-forced minimum makes the filter
   two-sided; compatibility with N|_{D_1} → 0 is THEOREM UPPER-ONLY
   and ends the ceiling program on the boundary.
2. **A full ideation round on OPEN[UPPER-TO-FLOOR]** with six
   candidate mechanisms (below D_1; a global non-boundary invariant
   such as Chevalley–Weil or the étale structure of E = F⁻¹(A_F);
   characteristic p / p-curvature; the Hamiltonian flows; growth
   inequalities; a one-directionality theorem), and the direct
   question whether the ceiling is the right target at all versus
   MOH-ENDGAME, the reducible branch, or realising a pinned N = 4
   skeleton at D ∈ {105,108,112,117,120}.
3. **The counterexample side gets a list:** extract the pinned N = 4
   skeletons at the admissible degrees from box/moh_skeleton_N.py
   and hand them to a realisation lane with the census machinery.
4. OPEN[MOH-14] is cheap and closes 1,147 groups either way; deferred
   to a quiet seat.

## E. Opens (bounded quantities)

OPEN[UPPER-TO-FLOOR] (campaign-level; N ∈ [2,16]); OPEN[D1-SUBTREE]
(N|_{D_1} ∈ [0, a_1(−λ_f(δ_1))]); OPEN[U-GT-2-ALL-D] (min U_rob at
D > 400); OPEN[DELTA75-BRACKET] (one printed entry); OPEN[MOH-14]
(1,147 groups). Carried: MOH-ENDGAME, NONPROPER-DEGREE,
ANTICANON-DEFECT, SAT-MASS, DELTA-AFF-VS-N. Retyped as a floor, not
promoted: NU-BOUND-AT-A-PLACE.

## F. Typed block

```text
INTEGRATION  #16 (Fable 5.1), basis 709ccb5be7b8
PROMOTED     FRONTIER-N; RADIUS-ORDER (M_s = n−2); DETECTOR-NULL;
             N-CEILING (upper bound only); FILTER-INVERSION;
             HARMONIC-BOUND (degree-minimal, Moh gauge, NU-TWO);
             N <= 44 at the MOH-SHARP-2 degrees (measured);
             the D<=400 / [101,200] counts; survivor U-values; (64,48).
WITHDRAWN    #15 §D "can prove a window"; "provably U>2"; C6 as a
             Moh error; "n=75 illegible".
NOT CLAIMED  any D-ceiling; N = U; a skeleton lower bound on N;
             realisability of any skeleton; emptying of any degree.
READING      the boundary route is one-directional as built; the
             campaign question is OPEN[UPPER-TO-FLOOR]; the only untried
             boundary datum is the sub-tree of D_1 (lane running).
NEXT         full round on UPPER-TO-FLOOR (freeze on this basis);
             D1-SUBTREE; pinned-N=4 skeleton list for realisation.
```

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `8943`.
- Body SHA-256:
  `b2123a74fb4cb265e2c1f11c3e5af10199c4af9934d362bee8a26a58a7c9eaf1`.
- Frozen basis: `709ccb5be7b80b4fb314f6fe1fa1f6eb19a29307`.
