# One-shot r3 administrative binding sequence

Status: INTERNAL / SOURCE-ONLY / UNEXECUTED. No host qualification, approval,
worker, enabled seed or execution is supplied. First action 2026-09-12
18:37:27 UTC; original publication reserve18:51 and hard18:54 UTC.

This owns one new administrative service/source and preserves the accepted
launcher/bootstrap/runtime. The final source and contract below will state
implemented checks separately from external qualification obligations.

## Delivered implementation

bind_once.py supplies the concrete finite sequence: validate one externally
approved immutable pre-epoch seed and installed references; verify actual
administrative service membership in the constrained parent slice; consume
the one-shot with an exclusive record; start the preinstalled watchdog ONCE;
observe its paired original epoch; derive only enumerated late slots from
ROOT-fixed offsets; freeze spec, then spec-pinning card, then card-pinning
outer unit; reload/read back; write a separate commitment; start the accepted
outer service ONCE and record actual returned status. No holder remains in
the outer cgroup, and no existing campaign source is imported or modified.

The new source SHA is e9bf742cd4348dcd7ba08609653ed619ae4379ee4784b26aaf81e3e8ecb80a81.
CONTRACT.md specifies the complete seed field contract and service/input
ordering; service.template has an unresolved SHA and supplies no authority.
VALIDATION.md contains ten manual negative traces and the host checklist.
PINS.json records six named source/metadata inputs plus the unchanged
administrative finalizer instrument. All source is unexecuted, including
syntax/AST/import/compile/test; there were no fixtures, scientific payloads,
network, worker, native collection or service operations.

## Exact limitations and remaining qualification

The inherited approval strings are supplied by ROOT in a SHA-bound seed;
this code cannot create approval by validating a spelling. Qualification
references authenticate prior record bytes but do not prove their substance.
Actual outer/leaf identities remain unformed until unchanged bootstrap.
The code requires actual observations of parent resources; none were made
on a worker in this task. Future outer resources are
expected values checked by unchanged launcher/bootstrap, not pre-observed
facts about an absent service. Metadata directories remain outside native
exact inventories, and generated inputs cannot participate in self-pin cycles.

Complete outer-unit byte equality is implemented. Effective loaded properties
are only partly compared; ExecStart serialization and remaining mapping are
explicit prior installed-host qualifications. The fixed property rendering
'47min 55s' is an unobserved compatibility requirement, not a claimed host fact.
The administrative unit's loaded timeout/dependency behavior, stop propagation,
native closure, original accounting history and independent worker retirement
likewise remain external. Native manifests are already installed/whole-byte
qualified before this sequence; this source does not implement their collector
or assembler again. PID1/SSH daemon CPU is not task-owned CPU.

The 115+5 watchdog remains armed through the entire attempt; all post-epoch
task-owned administration inherits the task slice. No total120-second fit or
hard-realtime guarantee is claimed. A failed/unknown systemctl operation or
incomplete output remains STOP, with no reset, cancellation, retry or repair.
Exit0 records only one returned start, never successful preflights or science.

## Own review, collision and next test

Complete owned source/docs were read back as text, including the two final
immutable-seed rechecks. All inherited rendering fields were compared manually
against the accepted launcher; the sole deliberate substitution is its actual
installed launcher path for that source's __file__. No execution was used.
Own-only collision check: new target paths were absent before authoring; no
other report/ledger was read for collision matching. This is the selected
missing integration step, not a new foundation review or mathematical claim.

OPEN[ACTUAL_HOST_QUALIFICATION]: QUANTITY: decide whether one genuinely
qualified installed host satisfies the finite checklist and whether the exact
one-shot plus nine phases terminates under the unchanged original cutoff.
CHEAPEST TEST: one different-model actual-code FIRST, then separately ROOT-
registered AWS qualification and ONE PREFLIGHT_ONLY_9 attempt; <=120seconds
after epoch is an UNMEASURED stop window, not an expected runtime. No passing
seed, real host, approval, qualification, loaded behavior or execution clearance
is supplied by this code packet. No other implementation lane is requested.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `4764`.
- Body SHA-256:
  `ffcbb87b9b4b056e728603e35a7c932d7e79c3512ca54dfd6c72ef019fcb5ad0`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
