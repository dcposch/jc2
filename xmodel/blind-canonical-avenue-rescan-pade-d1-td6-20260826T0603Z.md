# Blind canonical-avenue rescan after Padé, D1 V10/V11, and TD6 Stage A

Date: 2026-08-26 06:03 UTC

Status: **INDEPENDENT ALLOCATION / FALSIFIER PACKET; NO MATHEMATICAL
PROMOTION.**

## Blindness and evidence boundary

This scan read the complete canonical 46-row inventory in `APPROACHES.md`,
the current `AUDIT.md` / `PROGRESS.md` summaries, and the primary promoted or
producer artifacts named below.  It did not open or use any sibling ideation,
brainstorm, or allocation packet.

The significant new inputs are:

```text
2c917b9f... / 73c30502...   order-two nonzero-load Padé theorem/review
827e76bd... / 27275f3d...   order-two exact first divisibility jet/review
a497ce3e...                 D1 V10 two-chart hostile review, CONFIRMED
a4f6e864...                 D1 V10 promotion
466a0665...                 D1 V11 moving-load freeze (review live)
b47b613f...                 TD6 Stage-A 24-kernel hostile review
440b3fd2...                 TD6 V83 selected rank-38 open producer
8f3a6f4c...                 TD6 V84 harness failure, no K2 verdict
```

The Padé result makes the exact order-two first-normal reduced support only
`Lsq union D` after the licensed opens.  D1 V10 exhausts every noncentral
weighted triple-root direction at fixed loads, while V11 producer evidence
makes the same witness uniform in all constant-field loads and retires the
central cone vertex projectively; the latter remains review-gated.  TD6 has
a reviewed 24-dimensional previous/pole linear kernel, but no quadratic
table because the first K2 attempt failed in scalar-to-symbolic source
dispatch.

## Whole-46 disposition

Every canonical row was reconsidered.  The compact disposition below is a
scheduling result, not a new theorem.

| Disposition | Canonical rows | Current use |
|---|---|---|
| Advance as live mathematics | 2, 4, 19, 21, 25, 31, 38, 43 | TD6/source trees; exact arc/component recursion; protected AS/p-adic disproof; terminal passports; local Rees/normal-cone coverage; named tropical/Fitting charts; composite-factor test on the square branch |
| Advance asynchronously as rigor/software | 46 | Freeze/review/certificate interfaces only; never a discovery premise |
| Use only as a client-specific donor | 1, 3, 5, 6, 7, 8, 9, 26, 27, 28, 29, 32, 33, 34, 36 | Source provenance, ODEs, tame-coordinate checks, one-place/asymptotic diagnostics, recurrences, branch cycles, boundary topology, sparse syzygies, collision controls, and bounded negative controls after a live component is named |
| Hold or retire absent a new named invariant | 10--18, 20, 22--24, 30, 35, 37, 39--42, 44--45 | Dead implication ladders, untyped cohomology/analytic slogans, generic finite-field search, stabilization/descent tourism, refuted proof inputs, and tautological inverse ODEs |

This preserves the canonical global firewall: no local component calculation
supplies full landing/coverage, `G2-PSC`, every route-specific `G2-BD`, or an
absolute/cofinal topological-degree ceiling.  On the counterexample side, no
finite Witt stage supplies a characteristic-zero polynomial algebraization
with collision.

## New connection 1: one normal-cone/arc-space closure lemma for Padé and D1

The order-two matched charts and the D1 weighted charts are instances of the
same deformation-to-the-normal-cone problem.  A first-normal support component
is smooth on a principal open; arcs whose component equations have valuation
at least `ord(Lambda)` are represented by the matched chart; apparent slower
or nonpositive rescalings either hit the component's Fitting/rank-drop locus
or were graded at the wrong first nonzero coefficient.  D1's projective cone
vertex is the clean model of the latter: the zero leading vector is not a
point of `Proj` and must be regraded.

This connects canonical rows 4, 31, and 38 in a named, saturation-aware way.
It is not generic “tropical geometry”: the object is the Rees algebra of the
explicit component ideal, and the certificate is its conormal/Fitting
stratification.

Cheapest falsifier:

1. On `Lsq`, compute the Jacobian/conormal rank of
   `(c,4r-p^2,2n1-pn3,2n0-pn2)` against the seven exact first-normal rows on
   the declared source open.
2. On `D`, do the same in `(a,d,lambda)` away from `d=a^2`.
3. On `Lsq intersect D`, compute the first nonzero Fitting ideal instead of
   assigning the intersection by convention.

A unit maximal minor proves matched-valuation exhaustiveness on that open.  A
nonunit returns the exact weighted-chart locus.  This small dual-AWS rank/
Fitting job should precede any broad next-jet saturation.

## New connection 2: the square Padé component is a targeted Ritt/Hensel client

On `Lsq`, write

```text
K=S^2,       S=z^2+s,
N=S*(alpha*z+beta).
```

Then the first-normal rational expression collapses exactly:

```text
(3/8)*N^2/K + k10*K^(5/2)
  = (3/8)*(alpha*z+beta)^2 + k10*S^5,
```

which is polynomial.  Thus the complete first-normal tail vanishes for a
structural composite-factor reason, not seven accidental cancellations.
Canonical row 43 (Ritt/composite coordinates) is therefore newly useful as a
local client even though it remains weak globally.  The Hensel question is
whether the common `S` factor persists, splits, or is forced away at the next
source/Taylor grade.

Cheapest falsifier: before a full component Kuranishi run, substitute the
exact `Lsq` parameterization into the terminal `[6,2]` row and both first
Taylor-boundary leading forms.  Test the complete graph on the base and the
first transverse grade.  If any gives a unit after the declared opens, the
whole square branch dies without computing its general next jet.  Otherwise
emit the first coefficient not divisible by `S` and use that single
factor-splitting obstruction to design the next chart.

## New connection 3: passports should be receivers before they are endgames

Both order-two components are rationally parameterized at first contact, and
the order-one terminal theorem already shows how a complete divisor/branch
graph rejects a coefficient component before genus.  Canonical row 25 should
be used here as an early receiver: push the exact `[6,2]` terminal function
onto `Lsq` and `D`, normalize the complete graph including chart complements,
and compare its divisor/branch data with the licensed two-value/three-value
profiles.  This is much cheaper than generic invariant-quotient saturation
and respects the warning that a plane projection is not a normalization.

Cheapest falsifier: compute one exact terminal rational function on each
generic component and its complete zero/pole divisor.  Multiple pole support,
an extra branch value, or a wrong source-map degree kills that component.
Survival merely licenses the next divided jet and Taylor rows.

## New connection 4: transplant the D1 filtered-syzygy method to TD6 current

D1 stopped being a sequence of opaque saturations when the obstruction was
treated as a filtered row-module class: sparse corrections preserved the
distinguished unit term while cancelling entire lower grades.  TD6 current
has the analogous shape.  The previous/pole base is consistent with
24-dimensional kernel, whereas the current base has the reviewed unit
obstruction.  Its derivative on that kernel is an adjoint-sensitivity map,
not a tangent space.

Instead of interpreting current kernel vectors geometrically, solve the
sparse row-module lifting problem: can the current unit identity be corrected
over the 24 source axes so that its constant term remains a unit?  This is a
client-specific use of canonical rows 2, 3, 4, and 29.

Cheapest falsifier: finish only the exact first current adjoint table
`S_cur:Q_prev->C_cur`.  If the unit class is stationary modulo the row module,
attempt one sparse source-lifted correction.  If it varies, record the exact
coefficient ideal and do not call its kernel a family.  Either endpoint is
more informative than isolated coordinate probes.

## New connection 5: `R38` is a symbolic-matroid basis question first

V83 proves one selected FIRST minor factors through `R38`; it does not prove
rank drop on `R38=0`.  This is a determinantal matroid/basis-exchange problem,
not yet a new geometric stratum.  Treating every factor of one pivot product
as a raw fibre would over-shard the source atlas.

Cheapest falsifier:

- choose one exact point on `R38=0` with `U*H*B3 != 0` and compute the packed
  FIRST rank;
- if rank 38, extract one alternative 38-minor nonzero there and build a
  two-minor Bezout/cover certificate modulo `R38`;
- if rank drops, compute the exact rank and first nonzero Fitting ideal on the
  quotient.

This is the smallest honest decision on the V83 debt and a reusable
determinantal-cover pattern for canonical rows 2, 31, and 32.

## New connection 6: one typed Artin-algebra engine serves all three fronts

The V84 failure is a software boundary with mathematical consequences: an
old scalar constructor silently assumed `Fraction`, while the licensed source
RHS lives over `E[C,V,U]`.  Padé next jets, D1 load characters, and TD6 K2 all
need functorial evaluation over truncated Artin algebras with a nontrivial
coefficient ring.

The shared engine should expose only ring operations and construct

```text
R[eps_1,...,eps_n] / (monomials of degree > d),  d=1 or 2,
```

with exact substitution, inversion of declared units, symmetric quadratic
tables, polarization, source provenance, and canonical serialization.  It
must never coerce a coefficient through `fractions.Fraction`.

Cheapest software acceptance test: rerun the failed four-axis TD6 K2 pilot
with symbolic centers, plus synthetic multiplication, inverse, diagonal,
mixed-polarization, and scalar-specialization controls.  Require byte-equal
forward/reverse dual-host output before launching the 300-pair block cover.

## Allocation delta

Relative to the current `50% order two/order one / 15% D1 / 30% TD6 / 5%
protected global-formal` split:

| Lane | Now, while V11 review is live | After V11 confirmation/composition |
|---|---:|---:|
| Order two / order one | 50% | 55% |
| D1 | 10% | 5% |
| TD6 | 35% | 35% |
| Protected global / AS / formal | 5% | 5% |

Within the 55% maximum-twelve lane, use roughly 40 points on the two exact
order-two components and 15 on the order-one divisor/graph screen.  D1 gets
no new broad algebra job while its V11 review is live.  If V11 confirms the
constant-load and projective-retirement composition, its remaining 5% is for
the exact source-scope audit: decide whether “formal-series loads” are genuine
source data or merely families of already-covered constant-load points, and
name any truly distinct source chart before compute.

Within TD6, spend the first increment on the typed source-RHS repair and a
four-axis K2 pilot; spend the second on the `R38` alternate-minor falsifier;
launch the 300-pair K2 cover only after both pass.  Preserve the 5% global/AS
reserve: none of Padé, D1, or TD6 repairs landing/coverage, a cofinal ceiling,
or characteristic-zero algebraization of the AS tower.

## Immediate bounded queue

1. **Order-two receiver screen:** `Lsq` and `D` terminal/Taylor leading
   forms, complete graph and chart complements; dual AWS, 8 GiB / 1800 s.
2. **Order-two coverage screen:** conormal/Fitting ranks on `Lsq`, `D`, and
   their intersection; dual AWS, 8 GiB / 1800 s.
3. **TD6 typed K2 repair:** four axes with full symbolic centers and mandatory
   polarization controls; dual AWS, 8 GiB / 1800 s.
4. **TD6 `R38` discriminator:** one exact quotient point, rank, then one
   alternate minor or first Fitting ideal; dual AWS, 4 GiB / 900 s.
5. **D1:** review and composition only.  No new elimination unless the review
   identifies an actual source stratum not already represented by constant
   loads and correct projective grading.

Stop rules are strict.  A nonunit is a stratum, not a source; a terminal
passport is necessary, not a lift; a selected minor is not a rank locus; a
quadratic table is not a nonlinear family; and no local source cone is JC2.
