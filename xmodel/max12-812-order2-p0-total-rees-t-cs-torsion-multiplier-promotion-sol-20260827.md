# Promotion: branch-(b)-free existence of a direct T-cs monomial

Date: 2026-08-27

Lifecycle: **PROMOTED EXISTENCE THEOREM; NON-EFFECTIVE EXPONENT.**

Cross-pollination producer/reviews:

```text
Opus5 synthesis  c16cca5ca7aa72d7bc8eb83bc2ba2d9982bae59e251c35c1fb3ae472c095e65a
Grok review       b0fb7edaf262f0db64bef2de1a0ad1657251deab355d963855934c0284b6285c
Fable5 review     1481d3cadc30ee024c7d4e07e746a6e15ce000a52358e589a1fdf5add3e755c1
```

## Theorem

Let `K` be the honest ordered T-cs ideal generated by the 22 frozen source
rows and the ordered chart equations.  The promoted special-fibre theorem on
`D(cs*k)` implies, by Rabinowitsch clearing, that for some unspecified
positive integer `m`,

```text
cs^m*k^m is in K+(rho).
```

Every generator of `K` is even in `rho`.  Taking the even part of a cleared
identity therefore strengthens this to

```text
cs^m*k^m is in K+(rho^2).
```

The independently reviewed grade-10 identity gives

```text
cs^6*k^2*rho^6 is in K.
```

For any ideal `J`, if `f=c*rho^2 mod J` and `g*rho^6` is in `J`, then
`g*f^3` is in `J`.  Applying this elementary torsion-multiplier yields

```text
cs^(3m+6)*k^(3m+2) is in K.
```

Thus a direct honest polynomial monomial with rho factor `1` (`W=0`) exists
without the branch-(b) certificate used to obtain the explicit exponents
`(447,164)`.

## Exact interpretation

This argument uses only the valid cleared containment in `K+(rho)` and never
uses the withdrawn converse asserting a factor `1+rho*W`.  Localization at
`cs*k` is paid for by the displayed left-hand powers.  The honest/substituted
bridge applies because the left-hand monomials lie in the substituted ring.

The theorem is non-effective: Nullstellensatz clearing supplies neither `m`
nor cofactors.  It is therefore robustness evidence and a second proof of
existence, not a replacement campaign certificate.  The already promoted
explicit identity `cs^447*k^164 in K` remains the replayable certificate and
the source of its known exponents.

No J2, receiver, off-family, coverage, order-two, maximum-twelve, or JC2
conclusion follows.
