# Independent `BD-FIX3` gate: affine-linear Miranda triple covers

You are Grok acting as an independent mathematical researcher in the plane
Jacobian-conjecture campaign.  Work autonomously in
`/Users/dc/code/math/jc2` on frozen git basis
`0d7544ebd5cb12def6bac892646010301098be3c`.

Read and charge:

```text
xmodel/block-descent-structure-coordinator-integration-sol56-20260830.md
xmodel/block-descent-d2-galois-obstruction-producer-sol56-20260830.md
xmodel/bd-fix3-targeted-source-lead-sol56-20260830.md
```

The second file is a review-gated producer, not yet canonical.  Independently
verify every portion you consume.  The targeted public sources are Rick
Miranda's 1985 paper
`https://www.math.colostate.edu/~miranda/preprints/TripleCoversInAG.pdf`
and the unanswered 2026-07-24 MathOverflow proof sketch
`https://mathoverflow.net/questions/513574`.

Goal: decide the sharpest exact theorem available for a proper intermediate
Keller block cover of quotient degree `d2=3`, first in the affine-linear
Miranda subfamily and then, only if justified, in general.

Required work:

1. Reconstruct the trace split and Miranda multiplication table over
   `A=C[u,v]`, including the binary cubic and discriminant/ramification loci.
2. Independently audit the MathOverflow case partition for affine-linear
   `a,b,c,d`.  Do not cite its proof as established.  Check normality,
   integrality, incidence identifications, class groups, unit groups,
   ramification components, and every compact-support claim.
3. Consume the Keller sandwich constraints only after verifying them:
   `g1:A2->Y` is dominant quasi-finite etale and open; its image misses the
   non-etale locus `R`; pullback on regular functions is injective;
   `C[x,y]^*=C^*`; every branch component of `g2` has an unramified generic
   sheet; hence cubic divisorial inertia is `(2)(1)`, the cubic extension is
   non-Galois, `Y` is nonfactorial, and `B/A` is nonmonogenic.
4. Prove explicitly that a nonconstant unit on `U=Y\R` contradicts the
   source chart even though `g1` need not be an open immersion or have
   degree one and its image need not equal `U`.
5. Focus adversarially on the apparent residual
   `W!=0, S_X=empty, R irreducible`, where the public sketch obtains only
   `H_c^1(R,Z)!=0`.  Decide whether fixed-sheet transposition inertia,
   `Cl(Y)`, the divisor relation `g2^*D=2R+E`, canonical/different data, or
   the actual etale `A2` first leg forces a contradiction.  Do not convert
   boundary topology into a unit or map obstruction without a theorem.
6. If the residual survives, construct the strongest faithful algebraic or
   topological control and state precisely which Keller input it lacks.
7. Separate outcomes: affine-linear cubic closure; residual-only theorem;
   general cubic closure; or `STOPPED_OPEN`.  None implies primitivity or
   JC2 unless every missing global step is actually proved.

Use desk mathematics and seconds-scale exact controls only.  No local heavy
CAS and no local Singular; route any genuinely heavy finite job to AWS only
after preregistration.  Do not inspect, list, search, stat, build, modify, or
control `jc2-lean` in any way.  Do not edit canonical files, producer files,
or scripts.  Write exactly one report:

```text
xmodel/bd-fix3-affine-linear-miranda-gate-grok46-20260830.md
```

End it with one standalone `<!-- BODY-END -->` line and no seal block.  Give
full hashes, itemized verdicts, maximum safe scope, and the cheapest exact
successor.
