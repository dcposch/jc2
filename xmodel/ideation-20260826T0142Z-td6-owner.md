# Independent whole-portfolio delta scan — TD6 owner (01:42Z)

Date: 2026-08-26T01:42Z

Blind contract: I reread the complete 46-row `APPROACHES.md` inventory and
the current canonical overlays before reading any sibling submission for
this round.  The live inputs were the confirmed terminal moment/Davenport--
Zannier interface, the order-four four-node/repeated-face geometry, the D1
correction fan and newly isolated A-source identity defect, reconciled TD6
V78, and the frozen C1 Lean build.  These are research cards, not theorem
claims.

## Ranked cards

### 1. Normalize the order-four quotient first; compute the Kummer cover from its divisor

**Connection.**  The exact residual plane has Newton arithmetic genus seven,
while the provisional singularity budget is four ordinary torus nodes plus
one boundary singularity of delta three.  That points to a rational
normalization, so the old positive-genus argument on `X` disappears but the
deck quotient `Y: y^4=v` need not be rational.  Kummer Riemann--Hurwitz on
the *normalized divisor of `v`* is therefore the decisive invariant, and is
cheaper and safer than normalizing the substituted plane `P(q,y^4)`.

**First exact experiment.**  Certify irreducibility/birationality and a
complete toric singularity cover; prove the delta sum is exactly seven;
parameterize `X~=P1`; pull back and factor `div(v)` including every boundary
place; then compute `g(Y)` and the degree of `rho`.  The newly reported
candidate divisor `(+8,-1,-1,-6)` gives `g(Y)=1`, which would exclude every
source map `P1_x -> Y` once projection/dominance and the `a6`-chart cover are
source-certified.

**Falsifier/stop.**  An unaccounted component, chart loss, non-birational
plane projection, or a divisor with genus zero stops the genus claim.  In
the genus-zero case, immediately use the already finite terminal degree
divisibility/passport sieve instead of launching another global elimination.

### 2. Replace TD6 per-coordinate solves by one universal adjoint Laurent symbol

**Connection.**  V78's sharp `q14` cutoff and the dead-stretch formula
`j*binom(j-1,k) r^(m-5+12k)` are coefficient extractions from one filtered
Laurent operator.  For the frozen transport equation `A x=b`, every
first-order obstruction is

```text
ell (b'_theta - A'_theta x0),        ell in ker(A^T).
```

Thus a single source-replayed left-cokernel kernel should produce all 22
licensed `q` sensitivities and all eleven dead-stretch sensitivities by
convolution; solving 33 separate derivative systems is unnecessary.

**First exact experiment.**  Emit the adjoint as a Laurent generating
polynomial with original-row ancestry and denominator ledger.  Require it to
reconstruct byte-for-byte the common V78B/V78C 22-row table and both-host
V80B per-slot tables.  On success, form one joint 33-column conormal/Fitting
matrix over the center ring and stratify it constructibly.

**Falsifier/stop.**  Any mismatch identifies a missing varying multiplier,
lambda-prime term, source shift, or denominator and blocks the compressed
compiler.  Zero coefficients remain first-order invisibility only; no
nonlinear neighborhood or full-TD6 inference is licensed.

### 3. Turn terminal weighted trees into a leaf-pruning lower-tail recurrence

**Connection.**  The moment theorem gives barycentric weights
`1/P'(c_i)=n_i/kappa`; hence every rational expression in the terminal nodes
can be reduced canonically in `L[x]/(P)` or evaluated nodewise.  This is a
direct algebraic interface between Davenport--Zannier trees and the first
unresolved Faber tail.  A leaf of the weighted tree corresponds to a simple
node whose barycentric contribution can be separated exactly.

**First exact experiment.**  Use the constructive tree census to compute the
first unresolved lower-tail obstruction for all thirteen `U=3` classes and
the complete `U=4` census in the barycentric basis.  Search for an identity
of the form `R(tree)=R(tree\leaf)+local(edge,weights)`; verify it as a
polynomial identity, not by samples.  If found, induct on leaves and keep
the two Taylor boundaries as separate terminal cases.

**Falsifier/stop.**  Two trees with identical proposed leaf-local data but
different exact residuals kill that locality ansatz.  Retain the same tree
generator as a complete dynamic-programming backend; do not mistake a
terminal census for lower-tail or unbounded-`U` closure.

### 4. Tropicalize the D1 *graph of intermediate quotients*, not the expanded endpoint

**Connection.**  Both missed D1 rays came from higher coefficients of an
earlier rational layer.  Introduce graph variables for `QR/K`, `R^2/K^2`,
`Q^3/K^2`, and subsequent source layers, with their cleared defining
relations, then tropicalize the localized/Rees graph ideal.  Tropical
modification retains cancellation choices that a hand comparison of raw
weights necessarily loses.

**First exact experiment.**  On the double-root local ring, build the
smallest graph through the first three target rows.  The two known correction
rays and the tied `alpha=2 beta` ray are mandatory positive controls.  Only
after all three appear, enumerate projected cones and run exact initial-ideal
tests on each support mask.

**Falsifier/stop.**  If either known correction ray is absent, the graph is
not source-complete.  If projection produces an uncontrolled positive-
dimensional fan, stop global fan enumeration and attack its minimal prime
directly by Newton--Puiseux lifting.

### 5. Exploit fixed-passport Hurwitz rigidity as a landing constraint

**Connection.**  For fixed `U` and signed multiplicity profile, the terminal
map is a three-branch-point cover and has only finitely many affine classes.
Consequently an irreducible positive-dimensional coefficient component can
vary its terminal map only through source/target affine gauge.  Once those
gauges are fixed by the normalized source, the terminal Hurwitz map should
be constant.  This can convert the new weighted-tree frontend into a local
landing theorem rather than only a census.

**First exact experiment.**  On the loaded order-four coefficient component,
write two gauge-invariant cross-ratios/branch invariants of the terminal map
as functions in its exact function field.  Prove they are constant by the
finite Hurwitz scheme, then test whether the coefficient component forces
either invariant to vary.  A nonzero differential would exclude that
component without compiling every lower row.

**Falsifier/stop.**  If terminal invariants are constant and the component
still varies entirely in lower-tail data, record that precise fibre and do
not infer emptiness.  This is fixed-passport landing only, not a global
packet/sheet-compatibility theorem.

### 6. Make every algebra compiler proof-carrying from a single expression DAG

**Connection.**  The D1 discrepancy is now an exact nonzero constant in one
hand-expanded A-source identity, not a `sat` API failure.  TD6's caught hash,
slot, and stale-state defects point to the same avoidable risk: independently
hand-written expanded and localized formulas drift.

**First exact experiment.**  Generate expanded, localized, derivative, and
omission-control rows from one immutable expression DAG.  Require two
evaluators (exact characteristic zero and several deterministic good-prime
specializations), bidirectional reduction of every named identity, and a
normalized row hash before any expensive elimination.  Retrofit first to
the D1 A/B pair and the TD6 joint adjoint compiler.

**Falsifier/stop.**  Any evaluator disagreement quarantines the source before
CAS.  This is an accelerator and trust-boundary improvement, not independent
mathematical evidence.

## Allocation delta

For the next short cycle: 30% order-four normalization/Kummer divisor,
25% correction-complete D1 graph tropicalization, 25% TD6 joint adjoint plus
dead-stretch block, 15% weighted-tree lower-tail recurrence, and 5% global
landing/formal/certificate reserve.  Lean remains asynchronous: its C1 build
removes proof-drift risk but does not justify shifting discovery capacity
away from the four live mathematical gates above.

No card closes a full passport, TD6, the global landing/cofinality wall, or
JC2 without its stated source, coverage, and review gates.
