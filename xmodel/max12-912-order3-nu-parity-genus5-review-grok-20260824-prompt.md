# Hostile review — `(9,12)` order-three full parity genus-five exclusion

Act as a hostile different-model mathematical reviewer in
`/Users/dc/code/math/jc2`. Read in full:

- `xmodel/max12-912-order3-nu-parity-genus5-exclusion-20260824.md`;
- every file in `cases/max12_912_order3_nu_parity_genus5_20260824/`;
- the frozen parity `x5=0` predecessor and the reviewed maximum-twelve
  preflight/Faber inputs named by the report; and
- the confirmed DZ20 hostile review only for its actual-Keller
  constant-field/coprimality facts.

Charged hashes:

```text
report   f37376c96ab7e3c6ed7aa554dd04e2c72c9b2f5092ba6df03f524ea44765515d
manifest 07bde6525d798777afee038560a77cb0b02e908808ecd0a2abb857ebf0151010
freeze   5bc9e303bc9d9e9fb16956d7bdc2afade902ba5a51238e5925f9e1cf8dfb8537
```

Reconstruct independently:

1. exhaustion of `x5=0`, `A=0`, `p=0`, and `p*x5*A!=0`, including the
   distinction between an element being zero and having zeros at places;
2. the exact `v=A/(p*x5)` solve for `x1,x5,r6,r8`, every denominator, and
   the `p=0` relation for `r8^9`;
3. the Kummer character claim `v in C(x)`, `p=u^2P`, and why `p^9` and
   `nu` are cubes in `C(x)`;
4. removal of cube factors giving `Y^3=A5(v)/(3v^2-2)`;
5. squarefreeness/disjointness over the algebraically closed constant field,
   the seven finite branch points, the unbranched infinity of order `-3`,
   and genus `5`;
6. the no-nonconstant-map step from `P1_x`, including extension across
   poles; and
7. why constant `v` forces constant `p,r8` and contradicts the terminal row.

Attack hidden singularities/reducibility of the cubic cover, constant-field
extensions, a missed `D=0` or `A2=0` component, cancellation at infinity,
and any improper promotion from parity to the full loaded fibre. Replay PASS
strings are regression only; do independent algebra or a separate scratch
implementation.

Write exactly
`xmodel/max12-912-order3-nu-parity-genus5-review-grok-20260824.md`.
Do not edit producer, case, canonical, coordination, or other review files.
End with exactly one verdict: `CONFIRMED`, `GAP`, or `REFUTED`, and state the
smallest failing identity or missing hypothesis if applicable.
