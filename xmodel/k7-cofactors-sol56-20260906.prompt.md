# CERTIFICATE lane (make the five K=7 β-strata closures theorem-tier): the K=7 gate (frozen) promoted b = 9..13 (ten cover charts) on the strength of msolve-over-Q [1] from two builds plus explicit Singular cofactor identities Σ a_i g_i = 1 for THREE charts; the k8-b11-q1 report (frozen) re-applies the campaign's msolve erratum: a lone msolve [1] under a characteristic-0 header can be a first-prime unit printed before rational reconstruction — a SIGNAL, not a certificate. TASK: for each of the SEVEN K=7 charts without a cofactor identity (identify them from box/k7-strata-gate-20260905/ and box/k4ray-strata-solve-20260905/; integral denominator-cleared generators, both covers q0/q1 for b = 9..13): (1) compute an exact rational cofactor identity 1 = Σ a_i g_i with Singular over Q (`std` then `lift`, or `liftstd`; the charts are 44–51 variables — use the weighted block order that finished before; watchdog 40 min per chart; if `lift` is too slow, an alternative exact certificate: a Gröbner basis over Q computed by Singular with `option(redSB)` equal to {1}, recorded with the full basis computation log — state which certificate type each chart got), (2) VERIFY each identity by direct multiplication in the original ring (independent script, exact rationals), (3) record custody (generator SHA, order, command, wall, identity SHA); (4) VERDICT: 10/10 Q-certified (⇒ the K=7 promotion stands unconditionally) / list of charts still signal-only. A fleet worker is allowed (`bash ops/fleet/fleet.sh launch 1 r7i.8xlarge`; TERMINATE before sealing). FALLACY-v2 (a modular [1] is a signal; the certificate is the rational identity or a fully logged exact-Q basis). DISK DISCIPLINE: ~3 GB free — keep identities on the worker if > 10 MB and record hashes. ≤ 200 min; no ledger edits; no jc2-lean; no ideation-* input. Drivers to box/k7-cofactors-20260906/.
Report: xmodel/k7-cofactors-sol56-20260906.md
Seal (<!-- BODY-END -->); 6-14KB; 200 min.
charged_input=xmodel/k7-strata-gate-sol56-20260905.md
charged_input=xmodel/k8-b11-q1-sol56-20260905.md
charged_input=xmodel/k4ray-strata-solve-opus5-20260905.md
charged_input=ops/fleet/fleet.sh
charged_input=FALLACY-v2.md

Your inputs are frozen read-only copies in {{LANE_INPUTS}};
verify these SHA-256 hashes first, MECHANICALLY: the receipt `xmodel/k7-cofactors-sol56-20260906.run.v2` carries `charged_input_<i>_sha256=`/`_basename=` lines; build the manifest with awk and run `sha256sum -c`; stop only on a real content mismatch:

```text
78edece02efa7223d9130aa334416339f69c10de6d3cf4c6b7873581b791e1f8  {{LANE_INPUTS}}/k7-strata-gate-sol56-20260905.md
c16199138f4e93d3babdde1372e0000fb02ef831e1dd2b9e143d486bb0a6b6ac  {{LANE_INPUTS}}/k8-b11-q1-sol56-20260905.md
2af3e012dd4157669308f3855a22582967e2fffdc25cad3bf825b1050f25be4c  {{LANE_INPUTS}}/k4ray-strata-solve-opus5-20260905.md
a9da94d341a691942e9d6a6a6d90d97008b2d7baa1cadb2592481cf6b9c11c4c  {{LANE_INPUTS}}/fleet.sh
e47fd16cfcc91bc7bfdac4ba1b5f46152db8235e6609dca6e29a5966549c38f5  {{LANE_INPUTS}}/FALLACY-v2.md
```
