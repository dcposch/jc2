# FIRST Fable 5.1 static gate — four actual-certificate semantic controls

lane: f10-middle-real-controls-gate-fable5-20260910
reviewer: Fable 5.1 (claude-fable-5-1), independent FIRST static review of new code
started: 2026-09-10T11:12:49Z
deadline: min(first+18min = 11:30:49Z, 11:33:00Z) = 11:30:49Z; three-minute reserve from 11:27:49Z
status: UNSEALED (adapter seals; no Seal section and no charge_basis authored here; the standalone BODY-END marker is the last line)
scope: STATIC only. Zero subprocess/import/compile/test/fixture/CAS. Manual text/math/hash.
owns: this file and box/f10-middle-real-controls-gate-fable5-20260910/ only.

## 0. Custody

Target report and box were ABSENT at 11:12:49Z before any write.
15 immutable snapshots in /tmp/jc2-lane.hKWtnH/inputs hashed WHOLE before any body read; all 15 match the charged list
(pins in box/f10-middle-real-controls-gate-fable5-20260910/INPUT-PINS.sha256).

## 1. Read record

WHOLE (text, after the hash match): controls report 3fcd1e88 (49 lines), mutator.py 7969c503 (132 lines), dispatch.py c805a4d3 (521 lines; the whole-file display clipped at 2 KB and was recovered by three bounded reads 35..200, 201..380, 381..521 after the 1..34 preview), CONTRACT.md 8b76bfbe, DISABLED-REGISTRATION.json b95caf02, READ-SCOPE.md 30409b83, authority.py 45871331 (133 lines), checker.py 9369f0f3 (313 lines), probe.py d0df7ba6, run_capped.py 4435279d (1198 lines, two bounded reads; caller interface only, not re-reviewed), runtime gate 0562b82a, code gate d3b8c092, VALIDATION.md aadc2c26, check.payload 1a9bb0f5 (680 bytes, metadata). EXCERPT ONLY as charged: producer.py 44859af3 hashed WHOLE, body lines 1..12 and 246..273 (273 lines total). No provenance link followed, no file outside the 15 opened, nothing executed, imported, compiled or tested. The 854264-byte candidate is NOT an input and nothing below infers coefficient correctness from its SHA or from the positive receipt.

Mechanical text checks only: the dispatcher SCIENCE dict (lines 27-35) is entry-for-entry identical to authority.py lines 17-26 (diff of the normalised name:hash pairs, 8 entries); the REQUIRED/probe/CAPRUN/receipt literals in dispatch.py equal the charged pins of producer.py, checker.py, authority.py, the code gate, the runtime gate, probe.py, run_capped.py and check.payload (each literal occurs exactly once); check.payload's `code` map is exactly the three charged source hashes.

## 2. Verdicts A–G

### A. Mutator semantics — CONFIRMED

`change_constant` (mutator.py 39-53) scans for the exponent vector `['0','0','0']`, refuses duplicates, and: absent constant → inserts `[['0','0','0'],['1','1']]` at index 0, the unique canonical slot because the checker requires strictly increasing exponent tuples and (0,0,0) is the minimum; present constant c → `plus_one` gives the reduced Fraction c+1 as `[str(num),str(den)]`; c = −1 → the result `['0','1']` deletes the term, which the checker's zero-term rejection (checker.py 178) makes mandatory. In every case U' = U+1 and R' = R+1 exactly as polynomials, integrality preserved (U, R are integral by the accepted envelope). j: literal `'4'`→`'5'` (line 100). Bernstein: `coefficients[1][1]` → reduced c+1 (line 102), zero kept as a literal dense slot `['0','1']`, which `rational()` in the checker accepts. Wire validity: `str(int)` produces exactly the checker's regexes (signed numerator, unsigned positive denominator, no leading zeros); the mutator's 12000-bit cap (line 35) bounds decimals to ≤3613 digits, below the 4096-character limit, so the +1 can never trip the length gate; `sys.set_int_max_str_digits(4096)` is armed inside `authorize` before the first `int()`. No float, no rounding: Fraction only, imported after authorization. No incidental field change: each fixture is a deepcopy of the parsed original, `changed!=data` is required (103), and `restored==data` after undoing only the prescribed subtree (110-115) proves nothing else moved; `authority.emit` re-serialises with sorted keys, which is semantically neutral for the checker's dict parse and preserves list order. Guaranteed semantic effect on THIS baseline (a checker-accepted CANDIDATE by the pinned receipt with the same checker hash): U+1 changes the cofactor identity by S, whose V^7 coefficient is 245·(−120t)·144 = −4233600·t ≠ 0 from the checker's own `reconstruct` (S = 2K² − (7/6)βKD + 245γD²); R+1 ≠ R; the recomputed edge count is 4 (receipt j='4' under the identical checker), so 5 ≠ 4; the tensor-Bernstein basis is linearly independent so one changed coefficient changes the expansion by C(36,1)C(21,1)·t(1−t)^35·s(1−s)^20 ≠ 0. Pin, schema or parse failures produce different stderr strings and are STOP in the dispatcher (below), never a pass. Cosmetic: `IndexError` is not in the mutator's `__main__` handler, but every subscript is guarded by an earlier shape check on this baseline.

### B. Checker branches reached — CONFIRMED (static specification only)

All four fixtures pass every gate before their target identically to the baseline: key set, science/code (unchanged files), variables/bounds, status CANDIDATE, polynomial inventory and envelopes (a constant term has total degree 0 and is integral; sortedness holds at index 0). Order in `verify`: (218) quotient identity, unchanged; (220-223) `any(e[2] or e[3])` on U'S+Wcof·T = R+S → `positive V cancellation` for U, fires before (224); (224-225) R' ≠ identity → `full cofactor identity`; term caps cannot fire because every product has τ+α ≤ 40, V ≤ 11, at most 10332 monomials < 20000. j: pullback and the synthetic loop are baseline-identical (count 4), then (238-240) 5 ≠ 4 → `wrong maximal excluded-edge factor`, before the Bernstein degree test at 254. Bernstein: j passes, endpoint/epsilon/target unchanged, degree_t '36' = 40−4, 37×22 rectangle, `rational` accepts the mutated pair, `expansion != target` (267-268) → `full Bernstein expansion`, before the row-0 test at 269. Each exception is caught by the inner handler (304-306): stderr exactly `'INCONCLUSIVE: '+reason+'\n'` (one print), stdout empty (print only on success), `return 2`, no `emit` so the receipt is ABSENT; CAPRUN reports NORMAL_EXIT with child_returncode 2. The dispatcher's four literals (CONTROLS, lines 22-23) are byte-equal to checker.py lines 223, 225, 240, 268. Unexecuted.

### C. `authorize('mutate', …)` fits the unchanged authority — CONFIRMED

mutator.py 66 calls `authority.authorize('mutate', a.job, a.registration, outputs, a.input)` before the Fraction import (69), before any read of the baseline (84) and before any coefficient string is touched. Against authority.py: tag = science JOB (dispatcher passes `--job JOB`, 427); registration written by root as 0444 with `operation:'mutate'` (dispatch 292, 252); exclusive True; DMI/instance/hostname from the ROOT card; `cwd` = source (CAPRUN `--cwd`); `argv` = the exact script vector the dispatcher composes at 281-285 (sys.argv excludes interpreter flags); `executable` = canonical python; SCIENCE identical (mechanically checked); caps = JOINT = the literal dict of authority 88-89; deadline '+00:00' hence tz-aware; pins = the ROOT pins plus the original (already pinned, 218) — the required list authority/producer/checker/python/input is covered because every role file is required in pins (203-204) and the input is the original; every pin is re-hashed by the nonroot child; outputs = five exact absent paths in the same order (receipt, mutated-U/R/j/bernstein) on both sides (dispatch 282-286, mutator 65), inside the fresh writer dir. The mutator then re-verifies the receipt via the permit's pin (73) and byte hash (76), the original's size and hash (74, 85), receipt schema/status/input/code/j/degrees (80-83) and the baseline's schema/status/science/code/j (87-89). `postcheck` precedes each of the five `emit` calls (105, 118); emit is 'xb' exclusive with ceilings LIMIT−4096 and 8192 (dispatcher reads the receipt with the same 8192). No positive producer or checker run exists in the dispatcher: `startup-producer`/`startup-checker` are wrong-job only and fail at authority line 63 before `regular(input_path)` (line 100), so the absent `frozen/startup-absent.input` is never touched — no math. No self-authorised science: the only arithmetic is three `+1`s.

### D. Dispatcher — CONFIRMED

Exactly 14 labels (137-140) and `execute` issues them in order: five refusals, valid, two startups, dummy (nine no-math preflights, own 180 s window), then `mutate`, then `check-U/R/j/bernstein`. Freeze: after the mutator's CAPRUN is terminal, the cgroup is quiet and pins revalidated (347-349), the receipt is read (429), the four inventory entries are bound to name/path/reason/hash/bytes (439-444), every file is renamed inside the tmpfs to the absent frozen target, chowned root, chmod 0444 and re-hashed (446-447), and only after all four moves is the frozen dir set 0555 (449) and the first checker admitted (451-452). Each check gets its own root 0444 authority pinning the frozen input (250-251), the pre record carries `frozen_input_sha256` (309), post-run drift is rechecked (351, 456). Role binding: producer/checker/authority paths are tied to the accepted hashes and basenames (206-208), probe to its literal (209), CAPRUN to its literal (205), every REQUIRED basename to exactly one pin (211-213), the mutator receipt's `mutator_sha256` to the registered mutator pin (435) and its `code` to the accepted three (430-434); the positive receipt's `code` map is compared directly (225). This closes the prior runtime gate's A-GAP (role-path binding, receipt code). No live read: stdout/stderr/telemetry are consumed only after group quiet and the terminal stream-hash binding (371-373); the fixtures are hashed only after the mutator run returned. Source TOCTOU is closed by ownership (root 0444 in a 0555 root dir, science uid cannot write), the same residual the accepted code gate recorded (P1), not new. The old STATIC runtime gate's prerequisites carry over unchanged: o+rx ancestry and o+r on every pinned file (the child hashes ALL pins, now including the 854264-byte original and the receipt), source and native manifest outside the tmpfs, dispatcher stdout on the tmpfs, canonical escape-free paths, pure cgroup v2, integer `durable_device`, minimal env, native closure. New: the original and receipt must be root-owned 0444 with root-owned no-0o022 ancestry outside the mount (214-217) and pinned (218-220).

### E. CAPRUN and budgets — CONFIRMED

The command at 298-302 uses only options present in `parser()` (run_capped.py 143-177); the child vector is hashed exactly as `canonical_argv_hash` (361 vs 184-188); every telemetry key consumed exists (713-780 and terminal updates); `caps.wall_seconds` is float 10.0/45.0/5.0 and compares equal to the int profile. Dummy semantics are the unchanged accepted trace (32 MiB cap, orphan 64 MiB, TERM ignored, KILL, reap → RESOURCE_CAP/rss, exit 125). Joint window: `math_begin`/`math_cpu_start` are set once at the mutator's admission (269-273), never reset; mutate 10/9 (hard 10) and four checks 45/49 (hard 50) give 210 hard child CPU ≤ 330 and 190 s of walls ≤ 360 with the 15 s admission margin at each of the five admissions; preflights keep the separate 180 s window (276, 359) with 5/3 profiles. RSS is CAPRUN's 0.05 s sampled group RSS: a ceiling with bounded overshoot, not a completion guarantee; cap hits are typed STOP/NONDECISION. Tmpfs: the four fixtures (≈4×854 KB), five authorities, streams, telemetry, pre/post/live records and payloads all live on the ≤16 MiB mount (revalidated after every run); the original and receipt are outside it (216-217). The prior D-GAP (ps CPU charged to RUSAGE_CHILDREN) is now benign: ≤ about 4000 ps calls over ≤200 s against 120 s of headroom. Liveness note, not a defect: the mutator hashes every pin six times (authorize + five postchecks) inside 10 s wall / 9 s CPU; expected well under 2 s but unmeasured; a miss is STOP.

### F. Durable custody — CONFIRMED

`durable_copy` runs on success and on every caught STOP (507-515), requires group quiet and now<task_end, copies each regular non-symlink file 'xb' with fsync and hash comparison, caps the inventory at 200 (≈14 runs × ≤10 files + 3 payloads + 4 fixtures + 2 outer streams ≈ 150), writes CUSTODY.json under the 64 KiB `write_json` bound (≈25 KB estimated), fsyncs every directory bottom-up and now the durable parent (495, the prior F-GAP), and writes nothing to stdout after a successful copy. Any failure prints the single typed `STOP_CUSTODY_INCOMPLETE` line and exits 2; no deletion/unmount/worker stop. False success is impossible: exit 0 requires `FOUR_SEMANTIC_REJECTIONS_CONFIRMED`, and a cap, signal, changed reason, stray stdout or present receipt raises before that status is formed. ROOT's external stop (no signal handler in the dispatcher) and terminal capture of an uncopied mount remain. Baseline in the durable copy: NOT needed. The original is an immutable ROOT input outside the mount whose hash is fixed in the root registration (hashed into CUSTODY.json as `root_registration_sha256`), in every copied pre.json `pins`, in the mutator receipt and in the result; the four fixtures themselves are copied from frozen/. A fresh sha256 of the retained original at recovery time reproduces custody. The outer stdout location check is still unenforced (ROOT duty, unchanged).

### G. Blocking defect versus ROOT prerequisite — no blocking defect

Hand-read every statement of mutator.py and the changed dispatcher paths for literal stdlib APIs, ordering and bounds: none found that would break at runtime. Smallest optional repair (not required): add `IndexError` to the mutator's `__main__` handler. Everything else is a satisfiable ROOT setup prerequisite (D above). Old science and CAPRUN remain accepted unchanged; this gate accepts the new mutator/dispatcher STATICALLY only.

| item | verdict |
|---|---|
| A exact U+1/R+1/j→5/B[1][1]+1, canonical wire, no rounding, no side change, guaranteed effect | CONFIRMED |
| B four unchanged branches, exit 2, empty stdout, absent receipt, exact stderr, ordering | CONFIRMED (static) |
| C authorize('mutate') before Fraction/parse, pins/outputs/argv/caps, no new science, no rerun | CONFIRMED |
| D 14 commands, 9 preflights + mutate + 4 checks, freeze-all-before-first-check, bindings, no live read | CONFIRMED |
| E CAPRUN API/telemetry/dummy, 360/330/2 GiB/16 MiB incl. mutator, 10/9 + 4×45/49 (210 hard), 180 preflight, never reset | CONFIRMED |
| F durable copy, ≤200, 64 KiB, fsync incl. parent, no post-copy stdout, typed STOP; baseline not needed | CONFIRMED |
| G blocking defect | none; one optional handler line |

REFUTED: none. GAP: none new; the accepted residuals (P1 read-bytes digest, o+rx provisioning, outer-stdout location) stand as ROOT prerequisites.

## 3. Remaining ROOT obligations if STATIC passes

1. Register an enabled `f10-middle-real-controls/v1` file (root 0444) with: instance/hostname/uid/gid/ns/cgroup; source_root holding producer/checker/authority/probe/dispatcher/mutator as root-owned 0444 siblings in 0755 ancestry; python/setpriv/caprun; native manifest (pinned); `pins` covering every role file, the eight science reports, code-astra c3111895, code gate d3b8c092, runtime gate 0562b82a, the native manifest, the original (854264 bytes, ORIGINAL_SHA 21915179) and the positive receipt 1a9bb0f5; the reviewed bytes mutator 7969c503 and dispatcher c805a4d3 as the registered role pins; `outer_argv`; all 14 command vectors generated exactly as `run()` composes them; caps/profiles literal; preflight 180; '+00:00' deadlines leaving custody time.
2. Stage the original and receipt root-owned 0444, world-readable, outside the tmpfs; fresh ≤16 MiB tmpfs with writer (uid-owned 0700), frozen and authority (root, 0755) empty; absent durable dir on ext4/xfs with integer st_dev; pure cgroup v2 unit; minimal env-i startup.
3. Native/fresh-worker status and quota: NOT verifiable here (no AWS/SSH/network in this lane); the prior positive run's worker state is unknown to this gate and must be re-established by ROOT.
4. Then the ACTUAL nine preflights and four controls are owed; only their terminal custody can turn the static statements above into observed ones, and success means only `FOUR_SEMANTIC_REJECTIONS_CONFIRMED`. No runtime authority, certificate theorem, source result, all-r closure or JC2 claim is made here.

## 4. Own checks

OPEN(S) RAISED: none. No new canonical OPEN ID; the producer's two task-local pending quantities stand: (1) this FIRST whole-source static gate of the new mutator/dispatcher bytes, now delivered with no blocking defect and the ROOT prerequisites in section 3; (2) the actual nine-preflight/four-control batch, ROOT-registered separately and not authorized here.

COLLISIONS: EMPTY. Own-only check: this lane wrote exactly xmodel/f10-middle-real-controls-gate-fable5-20260910.md and box/f10-middle-real-controls-gate-fable5-20260910/INPUT-PINS.sha256, both absent at 11:12:49Z; apply_patch only; no Seal, no charge_basis, no helper/source/charged-input change, no fixture, no subprocess of any scientific kind, no registration, no execution, no network/AWS/SSH/Git write. An attempted temp file for the hex audit inside the read-only lane inputs directory failed and created nothing; the audit was redone with process substitution.

## Completion

Authoring attestation: skeleton 11:13 UTC, sections 1-3 and the pins file 11:20:54 UTC, this section afterwards, all by apply_patch; no Write/Edit tool, no shell redirection to any file. Own WHOLE read of this report (74 lines before this section) at 11:21:04 UTC with a standalone-marker-absence check (zero `<!-- BODY-END -->` lines in both owned files before this write; the status line names the marker in prose only) and a 64-hex-token audit at 11:21:19 UTC: the report carries zero full tokens (abbreviated forms only), the pins file carries exactly the 15 charged tokens, zero foreign tokens; `sha256sum -c` of the pins file against the live inputs returned zero non-OK lines. Read scope: 14 inputs WHOLE, producer.py hash-WHOLE body-EXCERPT as charged; the clipped dispatch.py display recovered by bounded reads. Verdict for a reader of this section alone: A-F CONFIRMED (B as static specification), G no blocking defect, REFUTED none, no new GAP; acceptance is STATIC ONLY of the new mutator/dispatcher bytes 7969c503 and c805a4d3; old science and CAPRUN stay accepted unchanged; the baseline candidate body was not read and no certificate, source, all-r or JC2 claim is made; the actual nine preflights and four controls remain owed under a fresh ROOT registration. Raised-OPEN check (none) and collision check (EMPTY) precede the marker and the 11:27:49 reserve. The marker below is the only standalone marker in this file and nothing follows it. All writers idle at the marker.

<!-- BODY-END -->
