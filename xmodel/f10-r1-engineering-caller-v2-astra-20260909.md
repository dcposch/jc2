# F10 r1 engineering caller v2 — disabled preparation

Status: **PREPARATION ONLY / UNEXECUTED / NO DISPATCH AUTHORITY.** This packet
contains new corrected caller and precision copies, exact textual diffs and a
disabled registration plan. All old bytes are preserved. No worker was queried,
restarted or contacted; no mathematical or predicate subprocess ran.

Actual start 2026-09-09 13:06:11 UTC. The earlier of start+12 minutes and the
parent's fixed13:20 UTC cap controls this preparation:13:18:11 UTC. Prospective
future execution times are NOT an invitation or an extension of this cap.
Basis:0d39df3c9fd69c939a8420c54d03228b9077777d.

## 1. Inputs and read scope

The terminal Fable caller gate `xmodel/f10-r1-caller-stop-gate-fable5-20260909.md`
SHA946e07d0f33507d6a4a5322fb6055ea802e91577a3ca2890b75e5e4dc46b30c5
was pinned BEFORE a WHOLE read. The original caller da3d8d50… and precision
130a4496… were pinned before WHOLE reads. The unchanged probe02913a1c…,
builder2dcba70d…, checkere2970c71…, helpercbfe55ff… and CAPRUN4435279d…
were rehashed. Their accepted16u/static interfaces are reused; no foundation
or mathematical source proof is re-reviewed. The checker’s full determinant,
row and guard comparison code, the WHOLE execution helper and CAPRUN’s literal
POST_KILL_OBSERVE_SECONDS=1.0 were statically inspected. The original whole
checker/builder and CAPRUN-interface read scopes remain as previously recorded.
No live proof/report/log, new theorem or remote state was consumed.

Fable confirmed the old STOP arose from treating SENT records as though they
were MATCH records. It also required observed identity and signal-type checks,
fixed helper pins, and a corrected precision control. Its old metadata replay
is not a new-v2 runtime test. Its statement that no constrained artifact
coefficient exceeds2^53 is NOT consumed: no artifact exists to establish that.

## 2. Exact caller delta

Only `dispatch_batch.py` and `precision.py` receive new code copies. Their
literal old/new diffs are adjacent `.diff` files. The unchanged original probe
is referenced by its root-fixed hash, not rewritten.

The dummy requires precisely four typed records: before-term/MATCH,
before-term-send/SENT/15, before-kill/MATCH, before-kill-send/SENT/9. Each MATCH
must additionally identify the same observed PID, PGID and start identity as
the top-level registered leader. Each SENT signal must satisfy `type(...) is
int`; float15.0 cannot pass by Python equality. RESOURCE_CAP status is now
required alongside rc125/resourceRSS. Existing namespace, same-PGID, aggregate
RSS, cleanup, reaping and live-process checks remain unchanged.

Literal probe and corrected-precision hash checks run at main-entry before
control dispatch. Root must externally verify the new caller hash; no impossible
self-referential hash constant is embedded. Every per-operation authority still
records and postchecks the actual caller/helper/input hashes and complete argv.

Future job/cwd is `f10-r1-exact-artifact-validation-v2-20260909`; only the SAME
retained instance i-08d2a40f272ee9fa2, hostname ip-172-30-0-72 and pinned
interpreter are allowed. The prospective mathematical deadline is13:35 UTC.
The 360-second aggregate budget, one-child sequencing, builder60wall/50CPU/
2GiB, checker30wall/25CPU/2GiB, inherited16MiB file cap, exclusive outputs and
no-retry behavior are unchanged. Root’s prospective13:40 task stop is external
and grants no mathematical time beyond13:35.

Admission now requires `now+wall+15 < hard`, where hard is the stored original
minimum of absolute deadline and first_math+360. Immediately after CAPRUN
returns, the caller records the return time; after custody/cleanup preservation
it rejects `returned >= hard` as NONDECISION before accepting a result. The
stored hard cutoff and return time are included in the dispatch record. No
fresh cutoff is manufactured at return and no cap is silently enlarged.

The15-second margin is NOT a strict real-time guarantee. CAPRUN has sampled
lifecycle/RSS observations, cleanup/reap observations and bounded ps calls;
arbitrary scheduler/kernel delay can exceed an admission estimate. The explicit
post-return rejection prevents a late result being reported as a batch PASS,
but cannot make a delayed process have stopped earlier. CAPRUN is unchanged;
there is no new supervisor or process-group scheme.

## 3. Corrected precision scope

The helper retains a genuine synthetic integer of magnitude above2^53 and requires its
float-rounded integer to differ while remaining above2^53 in magnitude. It
verifies the unmodified complete artifact and then checks that the actual guard
constant wire is `[[0]*12,"-1","1"]`.

Two fresh changed payloads are then checked separately. A FLOAT numerator must
fail with `floating point anywhere in artifact`. A rounded integer disguised
as a canonical decimal STRING must fail specifically with `full guard row`.
The second is intentionally valid rational syntax: its failure must be the
exact identity comparison, not a parser type/canonicality rejection. Any other
reason or unexpected success raises. The receipt pins the actual artifact bytes
and labels both rejection paths independently.

Generic synthetic JSON roundtrip wording was removed. These controls do not
prove positive fidelity for all large artifact coefficients or establish their
sizes before an artifact is built. No builder, checker or exact rational parser
was changed. All precision and metadata test outcomes remain PENDING.

## 4. Disabled plan, verification boundary and handoff

`REGISTRATION.disabled.json` has enabled=false and dispatch_authorized=false;
it records the new caller hash for root preflight, exact helper hashes, fixed
host, caps and prospective deadlines. It is not an execution authority file
consumed by the caller. Root must independently review this terminal packet,
verify physical/current pins and absent new remote paths, and explicitly invite
any later dispatch. No old directory or output is reusable by implication.

`PENDING-CONTROLS.md` requires metadata negatives for missing/reordered/forged
events, false observed identities, float signals, changed helper bytes,
insufficient admission room and post-deadline returns. No such replay ran here.
All seven ordinary full-condition modes and the corrected precision test remain
pending under a future registered worker. No optimized evidence, emitted ideal,
point, properness/unit result or solver permission is claimed.

Text changes used apply_patch, literal diff inspection and metadata hashing
only. New code/text and the report receive a WHOLE static read; own OPEN and
collision checks precede the marker and begin/close/finalize/expected-verify
publication. All old source/report hashes are rechecked at terminal custody.

OPEN(S) RAISED: None.

COLLISIONS: EMPTY at own-artifact scope; no broad search, old-byte edit, remote
action, mathematical execution or follow-on authority.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6984`.
- Body SHA-256:
  `afc13d9e39a6a2fe5156d6f8e839682a566be6257e13c0df5ec5bba03483fbc9`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
