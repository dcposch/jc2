You are the hostile different-model reviewer for one narrow plane-Jacobian
campaign result.  Work from the exact files below and independently recompute
the load-bearing identities.  Do not broaden the claim.

Producer report:
`xmodel/as-max12-tame-right-composed-residue-seeds-producer-20260825.md`

Frozen case:
`cases/as_frontier_conjugate_seeds_20260825/`

Audit all of the following:

1. distinguish right-composition `A∘B` from group conjugation;
2. verify both displayed inverses and `det J(B)=1` over `Z`;
3. verify the exact `(9,12)` and oriented `(8,12)` partial-`y` and total
   degree pairs, including constant unit leading `y` coefficients;
4. verify `det J(A∘B)=1-3u^2` over `Z`, and that the target swap/sign has
   determinant one and preserves the Jacobian determinant;
5. recompute the complete `F_3^2` fibre census and noninjectivity;
6. verify source/output hashes, rc files, environment records, two-host
   agreement, and scope markers;
7. actively search for any hidden inference from a finite-field seed to a
   mod-9 lift, Q8/TD6 chart landing, characteristic-zero map, counterexample,
   maximum-12 theorem, or JC2 conclusion.

Return a concise report with verdict `CONFIRMED`, `CONFIRMED WITH FIXES`, or
`REJECTED`; list every actual mathematical error separately from naming,
custody, or exposition defects.  If confirmed, state the strongest exact
claim and the sharpest non-claim.
