# D1 control-2 q-linear transverse-cusp lift promotion

Date: 2026-08-26

Status: **PROMOTED EXACT FIXED-CHART WEIGHT THEOREM.**

The dual-AWS producer

```text
7d3973e84c37d158a1f88796fdd5747e32d13e7fabd071ece80f6ea5a4c7942d
  cases/max12_912_order3_d1_double_root_control2_hq_linear_filtered_lift_v8_20260826/RESULT.md
```

with freeze SHA

```text
85a6ddb3b72fce2b2cce6399ba69315bd869ad9eae76461de6963a446ed648ff
  cases/max12_912_order3_d1_double_root_control2_hq_linear_filtered_lift_v8_20260826/FREEZE.sha256
```

has hostile independent review

```text
2c3dbbd50089892197dd5ad5c0fb90776b5c2d2dea4ac2752f3b1a3218cc7d4c
  xmodel/max12-912-order3-d1-double-root-control2-hq-linear-filtered-lift-review-grok-20260826.md
```

with verdict `HQ_LINEAR_FILTERED_LIFT_CONFIRMED` and no load-bearing repair.

## Promoted statement

In the pinned ordinary-tail eight-row module at
`a=1,p=-3,c=2+h,k=nu=0,mu=2/3`, with full Q/R support and all reviewed
q2/V6/V7 corrections, the further exact correction

```text
F1 -> F1 - h*q1/36,
F2 -> F2 - h*q0/72
```

produces the 48-term full polynomial identity of SHA `e549fd68...`.  The
complete h-linear Q-R, `R^2/Q^3`, and `Q^2R` projected union vanishes.  With
`alpha=15/2`, `beta=5+u`, `delta=5+v`, and `eta=wt(h)/L`, its unique least
term is `Lambda^20` uniformly over every `u,v,T>0` if and only if
`eta>=0`.  There is no negative eta-zero corner margin; all 30 non-target
equality-face terms have positive `u` or `v` slope, and
`Lambda^20*tau` is strict for `T>0`.

Thus every positive h valuation is closed in this fixed chart by one finite
identity.  No h-adic convergence or multiplier-support-completeness claim is
needed for that explicit weight theorem.  Negative eta is outside the
certified witness cone, not a branch survivor.

## Firewall

This is a fixed axis/cusp/load and fixed charged-cell theorem.  It does not
assert moving-axis or moving-load covariance, omitted normal-direction or
fan coverage, a formal neighborhood, the whole double-root locus, D1, or
JC2.  The 24-column RREF is complete only for its stated projection; the
stronger eta conclusion comes from checking every term of the exact output.
