cli=2.1.228 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai
The cross-review is complete and written to `xmodel/ideation-20260827T0935Z-fable5-crossreview.md`. All fourteen source files verified against their SHA-256 pins before use.

**Headline mathematical results:**

1. **The two-fibre criterion is true and now proved three ways.** I gave a complete proof of the four-way equivalence (saturation criterion ⟺ certificate form ⟺ DVR radical ⟺ generic+special fibre radicals ⟺ both dehomogenized unit ideals), with every hypothesis named. Homogeneity is load-bearing in exactly two of the three steps, and one counterexample, `J = (rho·a1 − 1)`, breaks both at once; the DVR prime-stratification step is unconditional. My blind §3.1 and Opus's Theorem A are the same theorem found independently; the already-proved special-fibre unit is precisely one conjunct, which is what makes the generic-fibre run decisive in both directions.

2. **Two source-verified findings that change the launch plan.** Opus's §6.2 predicate defect is real (byte-verified), but the lane writes `ELIMINANT_SIZE` and `eliminant_ideal.txt` *before* the mislabeled quit — so completed and in-flight runs can be re-adjudicated for free, no relaunch. And the seventeen `SELECTED` rows are literal frozen `Tg` rows, discharging Grok's not-in-J0 custody hazard; the positive branch is sound for the full chart.

3. **The exponent ladder is decision-irrelevant**; I gave the explicit certificate converter (`N = D + 2k·N0` for a t-divisible eliminant), which settles the Fable/Opus disagreement about the 192-GiB lift: needed only for explicitness in one sub-case, and then via cheaper bounded per-leaf solves through the formalized cascade lemmas.

4. **K00**: global nonmembership and V14R1's provisional local membership coexist because the failure module is supported inside `{d4 = −20/63}`, missing the origin. The full syzygy module *does* yield a representation-invariant first deformation obstruction for the mixed problem — I formalized the class and its invariance proof, consuming V14R1's already-harvested 87-generator syzygy module.

5. **Claim matrix**: 30 rows; one claim marked outright **wrong** (Grok's single-prime specialization skip rule — Opus's own denominator factorization is the concrete refutation), four scope corrections against my own blind submission (converter completeness, origin-funnel proof gap since repaired, jet cross-validation overstatement, stage-2 semantics).

**Decisions**: launch M1 (generic-fibre decision, telemetry re-adjudication first), M2 (V14R1 freeze/review + obstruction class), M3 (UAC schema/verifier freeze); hold the seeded N=7 dual as fallback-only and the Jelonek reframe; stop the multiplier-extraction exponent hunt, K00 D9+ as discovery, V11/V13 repair, and serial G22/G24+ exporters (conditional on the M3 freeze). Opus verdict: yes, significant correctness-adjusted unique capability — complementary forensic profile; recommend pairing rather than serializing the two of us on load-bearing reviews.

Full disclosure is in §14 of the report: hand algebra only, no AWS, no web, no CAS, `jc2-lean` untouched, and exactly one repository file written.
