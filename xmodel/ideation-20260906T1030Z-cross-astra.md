# Adversarial exchange — 20260906T1030Z — Astra

**Verdict: REDESIGN. NO_NEW_MECHANISM for an unrestricted JC2 obstruction.** The strongest surviving computational mechanism is source-respecting constant-pivot elimination of the finite C block. It merits a circuit test, not a solver-performance promotion. The K16 rational-to-polynomial lemma is a substantive boundary result, but supplies no polynomial nonexistence theorem. JC2, full-family properness, source-to-stage8 necessity, and all-degree exclusion remain OPEN.

Custody: work began 2026-09-06 11:52:51Z. Read the complete common packet, collection, all five blind submissions, all six charged follow-on reports, and FALLACY-v2 from `/tmp/jc2-lane.mprOtd/inputs`. Recomputed all **14/14 charged-index SHA-256 hashes**, the five collection submission full hashes, four recorded submission body hashes, and all eight available internal footer seals. Historical common-input and artifact hashes inside reports were treated as metadata; their uncharged files were not reopened. Internal parallel desk attacks used only these frozen inputs and do not constitute a different-model gate. No live CROSS/report/log, AWS, CAS solve, jc2-lean, shared-ledger/adapter edit, or external publication was used. Only this report was written in the repository.

**Blind submissions judged on their own proofs.** Coordinator, Astra, and Sol describe the same filtered Hamiltonian mechanism. Astra supplies the strongest blind version: the scalar symbol

`T_r(Y^i W^j)=2(24i−9j)Y^(i+17)W^(j+47)`

has resonances at degrees 0,11,22,33; the actual degree-33 source relation removes that resonance with pivot 1. Retaining every source-image, residual and inverse row gives an abstract 189-direction graph elimination over Q-algebras. Its missing instance maps and cost cannot be supplied by counting agreement among authors. [Blind argument](/tmp/jc2-lane.mprOtd/inputs/ideation-20260906T1030Z-astra.md:95).

The strongest refutation is sharper than a rank caveat: **Opus's polynomial universal positive-cokernel unit cannot exist as stated.** At D=b=0, its forcing vector Φ, the positive part of `((3D+a+bh)/2)J(h,D)`, is zero. Thus every polynomial `ℓΦ` vanishes there and cannot be a unit of the full polynomial base ring. C=0 satisfies all positive equations; the inverse row rejects it. Rational ℓ could only give a localized statement with an unpaid complement. This falsifies the proposed global positive-row certificate, not the complete physical ideal. [Proposed target](/tmp/jc2-lane.mprOtd/inputs/ideation-20260906T1030Z-opus5.md:396).

Opus also identifies a positive-row kernel with `J(P,G)=0`, although it permits `J(P,G)=constant`. Even a valid uniform rank floor 188 plus two rank-191 samples would not imply rank191 everywhere, global uniqueness, or a low-degree eliminated residual. Its “milliseconds over Q” oracle is unmeasured. Fable's exact rank-drop/decomposability converse lacks membership of the primitive generator in the actual C-space, and its enlarged block additionally contains h. These blind proofs remain defective after the later theorem. [Opus](/tmp/jc2-lane.mprOtd/inputs/ideation-20260906T1030Z-opus5.md:159), [Fable](/tmp/jc2-lane.mprOtd/inputs/ideation-20260906T1030Z-fable5.md:180).

Same-source T2/T3 inclusion, after the explicit h/inverse graph maps, proves neither strict containment nor computational dominance. A larger locus can be harder to exclude. Fable's source grading also lacks an action: the post-cutoff packet identifies fixed h3(4,5)=−8/3 at positive defect 4, while h has fixed X coefficient 1. Its inverse-row fallback survives; J0=1 and six affine gauge slices require actual source-preserving maps.

**Post-cutoff facts, separately charged.** The complete physical ideal is reviewed/replayed: 600 variables, 1629 rows, 11,299,180 terms, genuine `Zj*J0−1`. Exact-Q slimgb parsed everything but timed out at 600.959 s / 33.29 GiB without a basis. Modular msolve crashed at 26.817 s / 10.30 GiB before F4 evidence. The allocation count overflow is proved: 6,779,508,000 entries wrap to 2,484,540,704; crash causation remains inference. Neither outcome decides properness. No retry is authorized.

The 24-pivot expanded prefix has 575 variables but only 668 positive rows after 300 s, already 9,967,234 terms and a degree 5 row versus the complete original's degree 4. Partiality and fill-in are exact; contention prevents a clean timing comparison. This defeats automatic expansion, not every DAG design. The old 2 TB T2/T3 build is operator-stopped; retain partials and protected-worker custody. [Terminal packet](/tmp/jc2-lane.mprOtd/inputs/cross-packet.md:16).

K16's promoted boundary lemma needs B≠0, W(0)=−B and the exact first jet W′(0)=η; dropping either relevant marking/nonvanishing condition admits pole controls. Its norm model preserves multiplicities and the differential equation, including ramification. Anti-invariance `DF=(2t−1)G0(f(t))` supplies no independent obstruction; root-swapping refutes arbitrary-divisor sufficiency, not existence of a compatible divisor. No specific new all-degree polynomial obstruction survives this exchange. [Boundary gate](/tmp/jc2-lane.mprOtd/inputs/k16-boundary-product-gate-fable5-20260906.md:17).

**Independent attack on the post-cutoff transverse theorem.** Accept its mathematical argument conditionally on the literal source/support assertions, at PRODUCER-CHECKED scope. With `X=x,W=y−x`, det=1, work over a characteristic-zero field. The reported restrictions are h(X,0)=X+c, G(X,0) monic quadratic, degG=66, Gtop=H⁶, H=(X+W)³W⁸, and degC≤35 with W-degree≤32. The unrestricted h-lift must preserve the fixed X term and top; this is a source/code obligation, not a name match.

For nonconstant P∈V_C with J(P,G)=0, the charged common-polynomial theorem gives G=φ(R), P=ψ(R). Restriction to W=0 forces degφ=1 or 2. The first forces degP≥66. The second forces degR=33 and ψ linear; `αRtop²=H⁶` forces a nonzero W³³ coefficient, forbidden in P. Thus the **full** C-kernel consists of constants. Both source hypotheses matter: dropping the transverse restriction allows G=H⁶,P=H; dropping C support allows R=H³+X,G=R²,P=R. [Proof and controls](/tmp/jc2-lane.mprOtd/inputs/linear-c-transverse-rank-astra-20260906.md:72).

This gives a concrete post-cutoff counterexample to Fable's converse: D=b=0 makes G=h² composite, while the full C-rank remains 191 because h has the forbidden W³³ term. The enlarged (C,a) block does have the commuting direction Δa=2,ΔC=0 there.

Positive rows require a separate argument. The control J(X,X²+W)=1 distinguishes the maps without purporting to inhabit this source. At an actual Keller pair F,G, if P=ΔC+(Δa/2)h and J(P,G)=κ, then Q=P−(κ/j)F commutes with G. Keller forces G primitive and its centralizer k[G]. If κ≠0, degQ=99, impossible in k[G] of degree 66; hence κ=0. Degree and W³³ support then force Δa=0 and ΔC constant. This is conditional uniqueness, not existence or uniform enlarged-block rank.

Nonreduced base changes need more than fiberwise language. Delete the constant C column. Over the polynomial Q-base, rank 191 at every geometric point makes this matrix's maximal-minor ideal unit. A Bezout combination of minors and adjugates gives a polynomial left inverse, which survives arbitrary base change. It supplies no chosen constant 191 minor or cheap inverse. Conversely, the highest-face divisibility parametrization is only reduced: `D34=εX⁹W²⁵,C35=0` over Q[ε]/(ε²) satisfies its square equation without the claimed divisibility. Do not substitute that parametrization for the original scheme.

The **190 positive-pivot lemma also survives**. Restriction V_C→k[X] has image span{1,X}, so V₀={P:P(X,0)=0} has dimension 190. Order a rational source basis by increasing minimum W-order r≥1 and decreasing leading X-degree i within each r. For `P=W^r p_r(X)+…`, the lowest Jacobian coefficient is `−r p_r(X)(2X+t)`. Row `(i+1,r−1)` has diagonal −2r; later columns cannot reach it. All rows have positive degree. This triangular constant determinant preserves nonreduced Q-algebras. It remains an uninstantiated source-basis construction, improves 189 pivots by only one, and proves no final 191st positive pivot or runtime gain. A dedicated different-model source/code gate remains necessary. [Lemma](/tmp/jc2-lane.mprOtd/inputs/linear-c-transverse-rank-astra-20260906.md:174).

**Bottlenecks and composition.** Proof ranking: (1) all-degree coverage plus a uniform polynomial-realization obstruction; (2) source-to-stage8 necessity for fixed-family exclusions; (3) for every actual m≥4 and each factor of Q[d]/(3d²−m), exclude bBη≠0, plus separately b=0,Bη≠0; (4) fixed-chart decisions. The 24,063→90→64 census is only n≤200; K16(T) stops at t≤8. Sol's D(g)/V(g) atlas discipline supplies no missing map or terminating uniform induction. The post-cutoff degree estimate already returns 145/227; neither proposal earns a new obstruction label. [Degree interface](/tmp/jc2-lane.mprOtd/inputs/degree-estimate-interface-astra-20260906.md:24).

CE ranking: (1) produce a genuine candidate or characteristic-zero properness certificate for a complete physical family; (2) make its complete decision/certificate computation affordable; (3) exact all-row witness verification. A nonzero finite Q-algebra witness with all graph and inverse identities supplies a field quotient and Keller pair of exact degrees 99,66; degree nondivisibility rules out automorphy. A modular point, numerical residual or subset nonunit does not suffice. A unit excludes only this family pending necessity.

Concrete composition: use Coordinator/Astra/Sol's scalar graph elimination with Sol's explicit nonzero-J constraint, then Fable's calibrated variable projection to search the remaining compatibility equations. Preserve every residual and the inverse; use rank-revealing linear solves and retain rank-drop cases. Adopt W-order only after source/code checking; use Opus's rank samples as telemetry, never global elimination licenses.

**Cheapest chosen experiment: one W-filtration circuit instantiation, no solve.** Assuming the pinned source and full-row labels, allocate at most 15 minutes implementation/review and one 60-second / 1 GiB emission attempt. These are proposed budgets, not measured runtimes. Emit the rational 190-column transformation, selected rows, and triangular DAG in abstract G coefficients; check source reconstruction, forbidden entries, every −2r diagonal, and a deterministic known-target roundtrip `J(C*,G)`. Mutating a diagonal or omitting a residual/inverse label must fail. Retain complete residual labels without expanding source substitutions.

Record nodes, edges, coefficient degree, wall/RSS and verification cost. Existing 189 construction/replay measured 0.335 s / 1.538 s and its abstract graph 42,320 terms; those are comparison data, not a forecast. Any identity failure or cap stops the attempt. A compact verified graph raises priority only for source instantiation/evaluation; it does not license solving. Excessive graph size lowers this schedule's priority. `OPEN[W-CIRCUIT-COST]` remains until measured. This deterministic test outranks a parser repair requiring wide-count review and large ingestion, uncalibrated numerical starts, and K16 reformulations with no new obstruction.

Numerical admission controls remain required before any search: choose one deterministic rational source s* with J0(s*)≠0, set Zj*=1/J0(s*), and use its exact, generally nonconstant coefficient target J*. This is an artificial known-target control, not a Keller witness. Check evaluator/projection identities at s* exactly, then at most three fixed perturbations×five damped steps at 53 and 106 bits under a 60-second cap. Record actual costs; “seconds per iteration,” worker-hours and 30% recovery are unmeasured. Test the exact zero-J specialization, where the inverse row is −1 for every finite Zj. Also test `(u,uv−1)` along `(ε,1/ε)`: residual tends to zero while coordinates diverge. Log unscaled all-row residual, J0,Zj and coordinate norms. Failed recovery diagnoses the instrument or conditioning, not infeasibility; failed algebraic reconstruction supplies neither exact zero coordinates nor a modular seed.

| Major direction | Disposition |
|---|---|
| .63 subset / .73 K7 solves | CONTINUE existing custody/caps; harvest without extension. |
| Complete physical-J solving | REDESIGN; no raw retry or unsafe msolve reuse. |
| Old 2 TB T2/T3 build | STOP further build escalation; retain terminal partials and worker custody. |
| C elimination | CONTINUE bounded circuit/source gate; STOP automatic dense expansion and receiver ports. |
| Numerical search | REDESIGN to admission controls; no 1000-start campaign yet. |
| Source atlas / 44 receivers / split clients | REDESIGN around an actual missing map or uniform invariant; no repeated bookkeeping lane. |
| K16 | CONTINUE queued bounded full round; require an obstruction beyond norm/residue reformulation; no finite-m extrapolation. |
| Exact finite-algebra/Hensel acceptance | CONTINUE contract; candidate-triggered verification only. |
| R005 depth, R050/e28, relative rank, unrestricted Ni, repeated degree estimate | STOP new follow-ons absent changed hypotheses or measured benefit. |
| Literature sweep | CONTINUE existing clock; no duplicate sweep now. |

**Systems: NO_UPGRADE.** The cleanup already fulfills the 48-hour trial. An audit-heading index addresses a documented parser miss, but a stale or incomplete index can create false confidence; complete reading worked here. Durable catch-up lacks an established scheduler dependency, and a contract checker cannot catch this round's algebraic errors. Spending the next 15 minutes on the transverse source/code gate has higher mathematical value. No systems implementation is proposed in this exchange.

No new exit-price assertion is made; no charge-basis line applies. Completed 2026-09-06 12:02Z, approximately 10 minutes desk-only, before both limits.

<!-- BODY-END -->
