# FIRST review — supplied-input candidate caller (Fable 5.1)

Verdict: ACCEPTABLE AS AN OFFLINE, SUPPLIED-INPUT CANDIDATE EMITTER under its stated trust boundary. All five charged claims CONFIRMED; no contract break; delimitations below. No worker, batch-b/c retry, holder change, cap raise, candidate installation, release, 120-second or mathematical claim is authorized or implied. Only the new caller was reviewed; the two helpers keep their earlier FIRST acceptance and were not re-reviewed.

First action 2026-09-11 18:30:19 UTC: clock, absent-target check (report path and box directory absent, apply_patch present). Original reserve 18:48:00, HARD 18:51:00, never reset. All 23 inputs matched their charged hashes (`sha256sum -c`, 23 OK, directory count 23) before any read; all 23 were then read WHOLE through EOF (the three large JSON files in two overlapping unclipped halves each). No linked, original, sibling, corpus or peer path was opened; the test_latebind.py, historical_exercise.py and RESULT references in PINS.json/TASK were not followed. Postpins and own hashes: `box/caprun-candidate-caller-first-fable5-20260911/input_custody.md`.

## Replay and attack harness

Exact command (ordinary interpreter 3.12.3, no -O):

```
python3 -I -S -B box/caprun-candidate-caller-first-fable5-20260911/review_controls.py /tmp/jc2-lane.kgQvAj/inputs
```

Harness SHA-256 `1f74c77a16f6300e8ca8d33c3003302a2855943b50d760ff22627673cc531e3d`; deterministic stdout SHA-256 `5f58337e0d57390ec9f3c895de950c2d477c81f3ee99951a6b5c17f1fb70fa93` (no timings printed); exit 0; 170 expectations, 0 failed: PREPIN 23, static S01–S12, load L01–L05, replay R01, positive P01–P13, negative N01–N37 (43 expectations), separators F-series 48, ASCII-directive A01–A02, POSTPIN 23.

Relocated scope, stated honestly: the harness loads the charged `test_caller.py` from the snapshot (its `caller.py` is adjacent), then sets `HISTORY`, `FIXTURES`, `EXERCISE_PATH` and `C.DEPENDENCY_PATHS` to the flat snapshot in memory before `setUpClass`; every expected digest is untouched (L02, L03). Five supplied methods ran in-process through unittest, 5/5 ok (R01). `test_real_standalone_cli` was deliberately NOT invoked, since in the flat snapshot it would infer the original sibling helper paths; my harness launches no subprocess at all. ROOT's independent replay of the real standalone CLI remains the only standalone evidence; nothing here is labelled as a new standalone run. My own controls call `main(argv)` with `builtins.open` substituted per path, so the caller's real bounded reader runs on every case, and every open is recorded (P10: all read-binary, all under the snapshot).

## Verdicts per claim

**1. Six literal inputs, explicit context, bounded read-only I/O, hashes before either import — CONFIRMED.** Imports are exactly hashlib, importlib.util, json, pathlib.Path, sys (S01); no execution, write or network token (S02); the single `open` is read-binary and bounded to 131,072 bytes with the excess byte probed (S03; P13 accepts exactly 131,072, N07/N08 refuse 131,073 for metadata and bundle end-to-end). Code order is argument count, context literal, both helper hashes in one loop, then both imports in a second loop (S07); five, seven, lowercase and empty contexts refuse (N01–N04). Tampered latebind, tampered stage_patch, both tampered, missing helper, and a binding with an extra or missing key all refuse with `spec_from_file_location` never called (N32–N37). The prefix is unpacked and handed only to `make_patch`, never opened (S08). Symlinks, concurrent replacement and the supplied expected pins are ROOT's authentication, as the contract states.

**2. Strict metadata, bind, SUMMARY context, eight separators, make_patch, packet context; fourteen commitments and candidate-only status survive — CONFIRMED.** The pins are parsed by `latebind.load`; observation and decision are strictly loaded for the context check and again inside `bind`; the order in `packet` is exactly the claimed chain. Historical replay: exit 0, empty stderr, fourteen expected hashes equal the frozen EXERCISE map, LF readback reproduces all fourteen byte strings and 78,889 bytes, the four historical finals are byte-identical, and SUMMARY carries HISTORICAL_TEST, `installation_or_release_authorized: false` and thirteen commitments that pair with the reconstructed bytes (P01–P07). Metadata strictness: list, float, duplicate key, NaN, trailing garbage, CR, invalid UTF-8, empty, uppercase digest, altered digest, extra key, decision/observation context mismatch, stale observation hash, disabled flag, altered freeze token and pins/decision divergence all refuse with empty stdout (N10–N27). Separator hygiene is placed between `bind` and `make_patch` (F-series, claim 5). A separator inside observation metadata cannot reach candidate text because every free field is equality- or regex-bound by the binder (N28).

**3. One fully constructed ASCII packet; failures leave no stdout; no atomicity claim — CONFIRMED.** The packet is `ensure_ascii`, `allow_nan=False`, compact, one trailing LF, size-checked before the single `sys.stdout.write` (S10, P02). The historical em dash appears in the packet only as the six-character ASCII JSON escape (backslash, u, 2, 0, 1, 4) and is raw UTF-8 in the reconstructed card (P08), so the old ASCII failure is avoided. Every negative in this review (all N and F cases, 75 refusals) produced empty stdout and the exact stderr line. A failing output device raises through `main` rather than returning 0 or REFUSED (P11), matching the contract's "not represented as successful complete emission". Note: the 8 MiB packet envelope is unreachable under the parents' 524,288-byte candidate bound (worst-case escaping stays under about 1.6 MiB); vacuous but harmless, like the binder's aggregate bound.

**4. No writes, application, observation, installation, release or execution; main guard is the only tail — CONFIRMED.** Static reading of actual imports and output paths: the only writes are the stdout packet and the stderr refusal line (S04); the only execution is `exec_module` of the two hash-pinned helpers, and a hash mismatch prevents any import attempt (N32–N35); the main guard is the final statement and the only `__main__` reference (S05); no historical identity literal lives in the caller (S09), and its only 64-hex literals are the two charged helper pins (S06). The supplied tests' single subprocess target is the adjacent caller under -I -S -B and the tests never write (S11, S12). Inputs unchanged after everything (POSTPIN 23/23).

**5. Historical evidence is documentary only; eight nonvacuous negatives; ROOT duties retained — CONFIRMED with delimitations.** For each of the eight code points and for both text carriers (card and installer, 16 cases) my replay refreshes the prepared pins and the decision, runs the accepted `bind` and `make_patch` successfully with the separator present, then shows the new caller raising exactly `forbidden universal-newline separator` and `main` refusing with empty stdout (F000B–F2029). Old-pass/new-pass is also shown: ASCII directive text (End Patch, Add/Delete/Update File, Move to, hunk header, bare +/-/space lines) appended to the card survives as prefixed data with exactly fourteen Add File lines and one End Patch line (A01, A02). The 0.255 s figure is a unittest duration; I measured nothing, and no holder, timer, clock, /proc, native-list, ACL or 120-second fact was touched. Delimitation A: the supplied `test_missing_argument_and_file` patches `read_bounded` globally, so its missing-file case fires at the first read, which is the latebind hash read; it is a missing-dependency control, not a missing-input control. N05/N06 cover a missing pins file and a missing bundle file with helpers present. Delimitation B (inherited from the binder FIRST): ROOT_ATTESTED_CANDIDATE is transport acceptance only; the thirteen documents are byte-identical to the HISTORICAL_TEST run and only SUMMARY differs (P12), so custody must keep candidates with their SUMMARY. ROOT retains native-list role authentication, complete prepared-text and lowercase-placeholder duties, physical/ACL/liveness facts, fresh private exclusive absent prefix with no overwrite, and any explicit later release.

## Omitted controls, defects, duties

No contract break found. Producer omissions now covered: seven arguments, lowercase/empty context, missing input with helpers present, exact 131,072/131,073 boundaries for metadata and bundle, bundle hash mismatch, stale observation hash, freeze token, pins/decision divergence, uppercase digest, list/empty/invalid-UTF-8/trailing-garbage/CR metadata, prefix edges, tampered and missing helpers with import assertions, binding key drift, separators in the installer carrier, every ASCII directive form, output-device failure, transport-only ROOT_ATTESTED delimitation, packet determinism. Tested scope: ordinary `python3 -I -S -B` 3.12.3, no -O; stdlib only; no candidate bytes written; no subprocess, input shell, JS, scientific, runtime, native or dummy execution; no network, AWS, SSH, process control or /proc, /sys, /run, /opt inspection; no frozen source repaired; inputs unchanged. Raised canonical OPEN items: 0. No charge_basis, no exit claim, no Seal.

Own-only collision check: the report path and box directory were absent at 18:30 UTC before my first write at 18:37; only this report, `input_custody.md` and `review_controls.py` were authored, all via apply_patch; no shared ledger, input or protected file was edited. Own full read through EOF and fresh 23/23 postpins completed before the marker. All writers idle at the end of this turn.

<!-- BODY-END -->
