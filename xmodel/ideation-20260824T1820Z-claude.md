# Blind whole-portfolio ideation — round `20260824T1820Z` — Claude/Fable

- Date: 2026-08-24, frozen against packet cutoff `2026-08-24T18:20:00Z`
- Basis: `7832fb73ac887041968f9cec2dc6cba7aa0d0bcf`
- Contract: blind; no other `ideation-20260824T1820Z-*` file was read except the
  packet and my own prompt. No canonical/producer/case/source file was edited.
  No web or shell tools were used.

## 0. Input verification

Read in full: the packet; `APPROACHES.md` (all 46 avenues);
`AUDIT.md` (2,889 lines); `PROGRESS.md`; `COORDINATION.md`;
`ladder/REDUCTION.md`; newest `LIVE STATE` (`notes.md` 17:54Z);
`xmodel/max12-partial-y-kummer-preflight-20260824.md`;
`xmodel/max12-partial-y-shared-faber-probe-20260824.md`;
`xmodel/as-fonly-first-residual-divided-carry-deep-d7-erratum-20260824.md`
plus its `FREEZE.txt`; `xmodel/td6-centering-tangent-gate-20260824.md`;
`cases/max12_912_order3_fibre_20260824/order3_fibre.py` (header/pins);
`xmodel/pinchuk-quasipolynomial-reduction-source-audit-grok-20260824-prompt.md`
(a post-cutoff reviewer prompt, not an ideation submission).

Hash caveat: this lane has no shell, so SHA-256 values were not recomputed.
Consistency was checked by cross-reference instead: the erratum `FREEZE.txt`
carries `report_sha256 cbcd8851…` matching the packet; `order3_fibre.py` pins
the preflight report `30cb45cc…` and Faber probe `d303bf76…` matching the
packet; the TD6 tangent gate pins the reviewed adjoint/whole-`B` artifacts by
the hashes recorded in `AUDIT.md`. No inconsistency found.

Pinchuk primary text: **not accessible**. `refs/` contains only
`jc86.pdf, do.pdf, sigray_i.pdf, sigray_full.pdf, zoladek2008_official.pdf,
chau1999_apm71_full.pdf`; no repo snapshot of IM9017 exists. Everything below
about Pinchuk therefore uses only the packet's transcription, at
`SOURCE-FOUND / CLAIMS-UNREVIEWED`, with the exact missing checks listed in §8.

## 1. Reviewed vs provisional ledger used below

Reviewed (promoted, different-model): the maximum-11 partial-`y` theorem and
the `(6,9),3|H` exclusion; all AS map-only controls (triangular `D=7` terminal
point; balanced cyclotomic family; layer-7/6 associated scheme with three
minimal plus one embedded primary component); TD6 `q_B` pencil emptiness,
jet-orbit adjoint, and centering-escape stops; the AS109 Hensel
degree/`A_infinity` ledger, wild-symplectic and polar-conductor gates; the
secant idempotent; the Moskowicz refutation-as-proof.

Provisional / producer-only (do not overread): the max-12 Kummer preflight and
shared Faber probe (frozen, hostile review running); every `(9,12)`
order-three fact in the packet — the lower system `r1..r8`, the terminal ODE
`9hR'+6h'R=j` with the `-3C` control, the four Kuranishi quadrics and their
projective solution, and especially the **spectral polynomial
`W=g^3-f^4-3k f^2 g-k^3 f^2`** (explicitly coordinator-derived,
independent-check-required); the corrected AS 30-row divided-carry ideal (GB
269, dim 18, nonradical, three-piece cover — review active); the TD6
common-centering tangent (independent 3,602-column reconstruction running) and
the **generic `c1`-line exclusion** (producer-level; nine nonzero generic
compatibilities, gcd 1, one constant residual — exceptional pivot strata of
degree up to 16 are open, so this is not line or family coverage).

Quarantined: the 29-row AS deep-branch gate (missing divided carry
`(U_x+V_y-x^2)/3`; bytes preserved).

## 2. Disposition vector — all 46 avenues

Baseline: the 1633Z synthesis and current APPROACHES overlay. `U` = unchanged.

| # | Avenue | Disposition | Reason (only where changed) |
|--:|---|---|---|
| 1 | GGV corner farm | U | Wall unchanged. Note (no rank change): if Pinchuk Thm 3.4 survives audit, its q-category could finally type GGV's Laurent endpoints (`[P,Q]=x^k`) — see §5 connection C1. SPECULATION until audit. |
| 2 | Sheet ladder / Sigray | U | TD6 progress (generic `c1` open empty) is inside one normalized section; `G2-PSC`, coverage, and td-ceiling walls unchanged. |
| 3 | Strip ODEs | U | |
| 4 | Formal-germ D-series | U | Held; stationarity/typed-source stops stand. |
| 5 | JvdK descent | U | |
| 6 | Abhyankar–Moh | U | |
| 7 | A(F) / Jelonek | U | |
| 8 | Formal inverse | U | |
| 9 | Conjecture E | U | |
| 10 | HC4 bridge | U | Stopped (`NO LEVERAGE`). |
| 11 | Mathieu/GMC ladder | U | Dead. |
| 12 | Face isolation | U | |
| 13 | DC(2) | U | |
| 14 | End(A_1)/Zheglov | U | |
| 15 | Commuting PDOs | U | |
| 16 | D-module/holonomic | U | |
| 17 | BCW cubic | U | |
| 18 | Graded/GIT | U | Closed. |
| 19 | Char-p + Witt lifting | **raise** | The divided-carry erratum restores source-honesty to the only active disproof recursion; the corrected nonreduced 30-row ideal plus the reviewed embedded-component structure give the first structured acceptance scheme; the cyclotomic family calibrates what finite-depth survival is worth. This is now the sharpest falsification lane. |
| 20 | p-curvature / reduction-mod-p formalism | **raise (instrument tier only)** | Two consecutive carry omissions (AS109 erratum; AS 29-row quarantine) are both ghost-coordinate/Witt-addition bookkeeping errors. The Witt/de-Rham–Witt formalism is the right *language and compiler* for the AS recursion (§5 C3, §6). Not a proof route; no PC(2)/JC(4) consumption. |
| 21 | p-adic Hensel / AS109 | U | Bookkeeping note: the reviewed max-11 theorem raises the conditional correction floor to `y`-degree ≥ 12 in one coordinate, with maximum 12 reducing (conditionally, by the same integral-target-operation pattern as the reviewed ≤9 corollary) toward `(8,12)/(9,12)`. Constraint sharpens; mechanism unchanged. |
| 22 | Siegel/heights | U | |
| 23 | Analytic/metric | U | |
| 24 | Real JC / Pinchuk map | U | Explicit guard: S. I. Pinchuk's 2021 q-polynomial paper is a different topic from the real Pinchuk counterexample; no change to this avenue. |
| 25 | Monodromy passports / dessins | **raise** | Prior rounds (1358Z, 1633Z) already flagged the DS/dessin bypass for `(8,12)/(9,12)` as a watch item; that is KNOWN. New this round: Pinchuk Thm 4.1 plus its cited Pakovich–Zvonkin 2014 "complete classification of minimizing pairs" makes the `k=mu=nu=0` boundary a *named, potentially consumable finite input* rather than an analogy — pending audit item 5 (what PZ actually classifies at exponents `(4,3)`). First time avenue 25 has a concrete downstream client with a primary source attached. |
| 26 | Primitive-monodromy td bound | U | Still untried; the avenue-25 client is passport-level, not group-bound-level. |
| 27 | Splice/links | U | |
| 28 | Log/BMY | U | |
| 29 | LND/Hamiltonian | U | Stopped after scoped theorem. |
| 30 | ML invariant | U | |
| 31 | ZMT/integrality | U | Ranks 2–5 known-closed; degree ≥ 6 open; no new client. |
| 32 | Collision ideal | U | Accelerator banked; unit-ideal endpoint is still JC2. |
| 33 | Action residues | U | Stopped (`COSTUME`). |
| 34 | Tangent sweep | U | |
| 35 | Dim-3 descent | U | |
| 36 | Guided search | U | Stopped. |
| 37 | Finite-field census | U | |
| 38 | Tropical | U | |
| 39 | Cohomological cluster | U | The 1633Z AS-only reopen (the `x^2y^2` Cartier/de-Rham cokernel class) stands; this round's carry erratum supports, but does not further change, that scoped reopen. |
| 40 | Free-associative | U | |
| 41 | Naive scaling | U | |
| 42 | Markus–Yamabe | U | |
| 43 | Ritt | U | Watch note only: if the Pinchuk audit reveals a composition-flavored q-reduction, revisit. SPECULATION. |
| 44 | Moskowicz | U | Closed as proof input; repaired `xy`-membership theorem retained. |
| 45 | Differential Galois | U | |
| 46 | Lean/formal | U | |

No avenue is lowered or newly reopened this round. The two raises are
evidence-driven: 19 by the reviewed-and-corrected carry scheme, 25 by a newly
discovered primary source naming the classification the campaign had only
conjectured.

## 3. Reranked principal bottlenecks

Proof side:

1. **Uniform coverage/landing** (`G2-PSC` + full-configuration books +
   composite single-pole; REDUCTION CRITICAL 3–7). Still the wall. Nothing in
   this round touches it directly; the only new object that could is Pinchuk
   Thm 3.4, *if* it survives audit as a genuine normalization of every
   noninvertible Keller map into a typed q-category (SPECULATION).
2. **A uniform-in-`d` partial-`y` mechanism.** The max-11 theorem is real but
   per-cell; each maximum is a new bespoke closure. The DZ/extremal structure
   (§5) is the first candidate for a mechanism that is uniform in the
   signature `(r,s)`: the extremal stratum is *always* a Mason–Stothers
   equality locus, and the nontrivial-Kummer branches always force
   `delta=0`. If `(9,12)` and `(8,12)` close by one template, the ladder may
   admit an induction rather than a bash. This is the highest-information
   proof question the campaign can actually move this week.
3. **Type menu / absolute td ceiling** (KJN needs bounded provenanced types).
   Unchanged.
4. **TD6/SP-2 family coverage**: exceptional `c1` strata, centering families,
   dead-stretch/F1/pole moduli, then the other seven terminal classes.

Disproof side:

1. **Exact fixed-degree AS tower with correct carries** (map-only, `p=3`).
   The corrected 30-row ideal is the live object; the cyclotomic family
   proves finite-depth survival is cheap, so the target is an all-depth
   mechanism: either a terminal theorem (Cartier/embedded-component
   obstruction firing at bounded depth for every residue at fixed degree) or
   a survivor tower with support growing slower than the cyclotomic
   calibration — the genuine counterexample signal.
2. **AS109 coupled section**: `CLOSED-SUPPORT + UNIT-L` with `y`-degree ≥ 12
   in one correction (post-max-11 floor). Grammar stop stands; held until a
   gauge section or groupoid rule exists.
3. **Algebraization of formal carrier families** (D43/A-SCALE). Held at the
   stage-2 integral-model gate; unchanged.
4. **q-map counterexample category** (SPECULATION, audit-gated): if Thm 3.4
   is sound, the counterexample search could be run inside the reduced
   q-category, where the leading pair already has constant Jacobian, and only
   the return-to-polynomial step obstructs. Cheap to assess once the audit
   lands; do not build ahead of it.

## 4. Answers to the campaign questions

**Q1 (DZ equality at `(4,3),d=3`).** Yes at the stratification level,
conditionally at the rigidity level. Mason–Stothers gives, for coprime monic
`f,g` of `z`-degrees `9,12`, `deg_z(g^3-f^4) >= 3(4*3-4-3)+1 = 16`; the packet's
producer-exact fact says the `k=mu=nu=0` stratum sits exactly at 16. Equality
cases of this bound are rigid objects (ramification concentrated to the
Mason–Stothers minimum), finitely many at fixed degrees up to the obvious
equivalences, and — per Pinchuk's own citation — classified by Pakovich–Zvonkin
2014. The finite-list claim is therefore *plausible and now source-named*, but
licensed only after audit item 5 verifies what PZ classifies (all complex
equality pairs vs trees/unitrees only) and after the campaign checks that the
list survives the two charged Taylor boundaries and the affine/scaling
trajectory constraints over `k(x)` (the coefficients here are functions of
`x`, not constants — the audit prompt's "over `C(x)`" question is exactly the
right one; a fibrewise-constant list plus the terminal ODE is the honest
consumption route). For `k!=0` and nonzero `mu,nu` there *is* a comparable
spectral statement: `deg_z W` drops through `21 -> 18 -> 16` exactly as the
loads `mu, nu, R` vanish, so `W` is a stratifying invariant everywhere, and on
each stratum the identity `J(f,W)=3(g^2-k f^2)J(f,g)` exports the Keller
constant into the `(f,W)` pair for valuation arguments of the type that closed
the `(6,9)` elliptic sheet. Card 1 makes this exact. All of this is downstream
of the unpromoted `W` identities and must start with their independent check.

**Q2 (Pinchuk Thm 3.4).** Undecidable here — primary text absent (§8). From
the packet transcription alone, the three live hypotheses are: (a) *genuinely
new reduction client* — a normalization of every noninvertible Keller map into
a reduced q-map whose leading weighted pair is already constant-Jacobian would
be a typed home the GGV §4 endpoints never had (REDUCTION CRITICAL 3's
ill-typing is exactly the disease this could treat); (b) *rename* — leading
forms of Keller pairs being proportional powers with constant-Jacobian-like
leading brackets is classical Newton-polygon material (Abhyankar/GGV/Sigray
leading-pair facts), and "quasi-elementary source changes" may just be the
standard toric/Laurent moves; (c) *scope gap* — a 12-page paper proving a
six-case reduction with "the rest is similar" and rational-power field changes
has the classic termination and category-closure hazards. The cheapest
decisive source audit is already launched (the Grok lane; its seven items are
well-chosen). §8 lists the four checks I would add. Exact connections: to
`G2-PSC` only in mode (a); to partial-`y` coverage via Thm 4.1 (real,
elementary, and independently checkable now — the lower-bound direction
follows from Mason–Stothers/Wronskian algebra sketched in §8 and needs no
paper); to counterexample construction in mode (a) via the q-category search
(SPECULATION).

**Q3 (cheapest exhaustive `(9,12)` split).** Order the strata by certificate
cost, not by geometry: (0) independently verify the four `W` identities
(hours, symbolic; mandatory before anything consumes them); (1)
`k=mu=nu=0`: extremal stratum — finite rigid list route (Card 1), plus the
two Taylor boundaries; (2) `nu!=0` (`deg_z W=18`) and (3) `mu!=0`
(`deg_z W=21`): norm-typing plus the terminal ODE `9hR'+6h'R=j` with the
`s=C0(x-a)^m`-type pole dichotomy inherited from the reviewed `(6,9)`
Faber–Laurent landing (methods reusable, coefficients not); (4) `k!=0`,
squarefree `K`: already rigid to first order by the producer Kuranishi
quadrics — remaining: order-one core and invariant loads; (5) `disc(K)=0` and
`k=0` handoffs last (these are where `(6,9)` needed the most bespoke work).
The spectral invariant *does* bypass full primary decomposition for the
stratification itself: the five strata are cut by explicit constructible
conditions (`k`, `disc K`, `mu`, `nu`, `R`), and exhaustiveness is a
disjunction check, not a GB decomposition. What it does not bypass is the
pointwise transversality/cokernel work inside each stratum (the quadrics) —
that is where the fibre compiler stays.

**Q4 (highest-information successor; simpler engines).** Ranking: (i) the
`(9,12)` `k=mu=nu=0` stratum via Card 1 — cheapest decisive step on the proof
backbone; (ii) the AS degree-ten componentwise successor on the corrected
nonreduced `I` (Card 2) — the disproof backbone, now source-honest; (iii) TD6
exceptional `c1` strata (Card 3) — necessary coverage, lower novelty. Engine
simplification without losing nonreduced/exceptional structure: for AS, the
ghost-coordinate carry compiler (§6) — it computes over `Z` once, so divided
carries are structural, and it works on `I` directly (the three-piece cover
stays a localization aid, per the erratum's own warning); for TD6, a
pivot-tree stratification logger that *enumerates* pivot-factor vanishing loci
instead of sampling them, emitting one certificate per stratum. Both preserve
exactly the structures the packet warns about.

**Q5 (what is still missing).** (a) A uniform-in-`d` partial-`y` mechanism
(§3 P2). (b) An all-depth AS terminal theorem at fixed degree — everything
so far is pointwise-in-residue or cap-relative. (c) A typed
q-category/`G2-PSC` client (audit-gated). (d) Software: the carry compiler and
the stratification logger. (e) Intelligence: a frozen snapshot of
Pakovich–Zvonkin `arXiv:1306.4141` and the Pinchuk PDF — both absent from the
repo and both now load-bearing-adjacent.

## 5. New mechanism and new cross-avenue connections

**M1 — Norm-form spectral typing (NEW; history-checked).** Generalize the
coordinator's `W`: for a Kummer branch of order `e` over `u^d=h`, package the
deviation of `g` from the common-power section as a *norm form* — for
`(9,12)`, `W=Norm_{t^3=f}(g-f t-k t^2)` — and use (i) its `z`-degree and
leading Laurent data as the stratification invariant (bypassing primary
decomposition, Q3), (ii) its Jacobian transfer `J(f,W)=3(g^2-k f^2)J(f,g)` to
export the Keller constant into derived pairs for valuation arguments, and
(iii) its extremal stratum, where `deg W` hits the Mason–Stothers bound, as
the entry point for the finite rigid list. For `(8,12)` the analogous object
is the order-two/order-four norm of `g` against `f^{3/2}`-sections with
signature `(3,2)`, `d=4`, bound `4(6-3-2)+1=5` — the classical
Davenport/Shabat-tree case, which is *better* classified than `(4,3)`.
History check: `W` itself is this round's coordinator derivation (packet);
"resolvent" hits in history are the stopped quartic-resolvent cards (a stale
degree-4 target, a different object); no prior report uses norm-degree typing
as a decomposition bypass. Dependency: the unpromoted `W` identities.

**C1 — The q-polynomial category as a common home (NEW).** Pinchuk Thm 4.1's
third condition says the DZ-extremal pairs are exactly the constant-Jacobian
locus of quasi-polynomial maps `(x^{-k}p(x^m y), x^{-r}q(x^m y))`. The
campaign already owns two other families of "constant-Jacobian objects outside
`Aut C[x,y]`": GGV §4's Laurent endpoints (`[P,Q]=x^k`; REDUCTION CRITICAL 3
calls them ill-typed) and Sigray chart leading pairs. All three live in the
same q-category. If Thm 3.4 survives audit, it is a candidate *typed
normalization target* adjacent to `G2-PSC` — the first object that could
receive both GGV and Sigray data without the current category mismatch.
SPECULATION until the audit returns; no lane should build on it now.

**C2 — Avenue 25 ⇄ maximum-12 extremal stratum (KNOWN, strengthened).**
Identified in rounds 1358Z/1633Z as a watch item; what is new this round is a
primary source (Pinchuk citing PZ 2014) that may convert it from analogy to
consumable finite input. Labeled KNOWN-strengthened, not new.

**C3 — Avenue 19 ⇄ avenue 20: carries are ghost coordinates (NEW as an
operational connection).** Both carry errata (AS109's uncarried second digit;
the AS 29-row omission) are instances of one mistake: computing digit `n`
rows from mod-`p^n` data instead of from the integer/Witt expansion. In Witt
language the divided carries *are* the ghost-component corrections, and the
depth-six `x^2y^2` obstruction is a Cartier class (1633Z already reopened
avenue 39 AS-only on this). The new step is operational: implement the
recursion in ghost coordinates once, and carry omissions become impossible by
construction (§6). Builds on, and is distinct from, the KNOWN Cartier-class
observation.

## 6. Software accelerator (one) and decisive experiment (one)

**Accelerator — integer ghost/divided-carry compiler.** One exact engine that,
given `P=P_0+p U_1+p^2 U_2+...`, computes `det J(P,Q)-1` over `Z` symbolically
once and *derives* every depth-`n` residual row by exact division with all
lower-order carries retained — never composing mod-`p^n` shortcuts. Interface:
emits per-depth typed rows plus the divisibility certificate demanded by
commit `7832fb7`'s provenance gate (the gate is policy; this is the tool that
makes compliance structural). Negative controls are already banked: it must
*fail* the quarantined 29-row gate (recovering `u5_3+v5_2`), reproduce erratum
equation (7) (`-9x^4+9x^2y^2-9x^4y^2`), and reproduce the cyclotomic family's
`1-3^{2m}x^{4m}` determinants. Reuse: both AS lanes and any future Witt/`p`-adic
successor. This is the single highest-leverage instrument this round because
it retires a bug class that has now fired twice on the disproof backbone.

**Decisive experiment — spectral verification plus extremal-list arithmetic.**
Step 0 of Card 1: independently verify the four `W` identities and the
`21/18/16` degree law (symbolic, no paper needed), then derive the exact
ramification-passport arithmetic for equality pairs at `(4,3),d=3` (small
integer Riemann–Hurwitz/CSP, reusing avenue-25 machinery). This is decisive in
both directions: an empty or finite passport list converts the `k=mu=nu=0`
stratum into a checkable candidate sweep; a failure of the `W` identities
kills the coordinator's spectral connection before anything consumes it.

## 7. Idea cards (three)

### Card 1 — `SPECTRAL-DZ-912`: close the `(9,12)` extremal stratum by rigid-list arithmetic

- **Claim to test.** On the order-three `(9,12)` branch with `k=mu=nu=0`, the
  pair `(f,g)` sits on the Mason–Stothers equality locus
  `deg_z(g^3-f^4)=16`; the equality locus at fixed signature is rigid/finite
  fibrewise; imposing the two charged Taylor boundaries (preflight eq. 2.5)
  and the terminal descent `r8=u^2R`, `9hR'+6h'R=j` on each candidate empties
  the stratum — or exhibits an explicit surviving trajectory.
- **Dependencies.** Unpromoted: the `W` identities (verify first — step 0);
  the frozen preflight/Faber probe (review running; consume only their exact
  producer facts). External: PZ 2014 classification scope — *only* for the
  shortcut; the fallback is a self-contained passport enumeration by
  Riemann–Hurwitz plus transitivity (avenue-25 code pattern), which needs no
  external source. Reviewed inputs: max-11 theorem, `(6,9)` method library.
- **Cheapest discriminator.** Step 0 (hours): symbolic re-derivation of
  `W=Norm_{t^3=f}(g-ft-kt^2)`, the `T`-expansion, `J(f,W)=3(g^2-kf^2)J(f,g)`,
  and the degree law. Step 1 (a day): exact passport arithmetic for the
  equality case — count candidate ramification types over the `x`-generic
  fibre; the count is a small integer.
- **Outcomes.** (i) `W` fails → spectral connection dead; the fibre compiler
  continues unaffected; large negative-information gain, cheap. (ii) Passport
  count 0 → stratum empty over `k(x)`-generic points; remaining work is the
  degenerate `x`-fibres — major step toward `(9,12)`. (iii) Finite count > 0 →
  run each candidate through the Taylor boundaries and terminal ODE; every
  candidate dies → stratum closed; any survivor → an explicit
  near-counterexample trajectory, the strongest disproof lead the partial-`y`
  program has produced — escalate immediately. (iv) Arithmetic
  underdetermined (function-field coefficients defeat fibrewise rigidity) →
  stop, record the exact failure mode; this bounds what PZ can ever license.
- **Stop condition.** Step 0 failure; or passports enumerated and every
  candidate decided; or two non-informative attempts at the function-field
  lift (COORDINATION redesign rule).
- **Information gain.** High either way: it decides whether the maximum-12
  frontier closes by structure (uniformity candidate, §3 P2) or by bash, and
  it is the direct test of packet Q1.

### Card 2 — `AS-GHOST-D10`: degree-ten successor on the corrected nonreduced ideal, ghost-compiled

- **Claim to test.** The corrected 40-variable/30-row ideal `I` (dim 18,
  nonradical) admits a componentwise degree-ten attachment (carry degrees ten
  through seven, then accepted second digits) whose obstruction either
  terminates every residue at bounded depth or exposes a residue family whose
  survival depth beats the cyclotomic calibration (`D'<=4m` at degree
  `2m+1`) — the first genuine tower signal.
- **Dependencies.** The erratum producer (review active — provisional; work
  is cheap/reversible and stays within the speculative budget); the reviewed
  associated-top structure (promoted); the ghost compiler of §6 (build
  first — it is also the erratum's independent verifier). Hard rule from the
  erratum: successors start from `I`, never from a radical or from the
  `Q0/Q1/E` pieces alone.
- **Cheapest discriminator.** Build the ghost compiler and replay the three
  banked negative controls (§6). Then attach only the degree-ten carry rows
  to `I` and compute the dimension drop componentwise against the three-piece
  cover used as localization guide.
- **Outcomes.** (i) Dimension drops to the point that the embedded/Cartier
  obstruction forces same-cap terminality for all residues → a bounded-depth
  terminal theorem at `D=7` — the first all-residue statement in the lane.
  (ii) A free factor persists with support growing sublinearly vs the
  cyclotomic family → candidate tower; escalate to all-depth analysis.
  (iii) Growth matches the cyclotomic pattern → the lane is measuring known
  false positives; redesign toward the direct fixed-map-degree tower already
  licensed by the promoted category correction.
- **Stop condition.** Compiler controls fail (then fix the tool, not the
  math); or the componentwise attachment exceeds one six-hour lane without a
  dimension verdict (Gröbner blowup → move to the fleet-safe decomposed
  route); or hostile review of the erratum returns `GAP/REFUTED` (quarantine
  descendants mechanically).
- **Information gain.** High on the disproof backbone: converts a
  twice-corrected bookkeeping lane into a structurally safe recursion and
  produces either the first terminal theorem or the first honest tower
  signal.

### Card 3 — `TD6-C1-STRATA`: exceptional-stratum certificates for the `c1` line, then `c3`

- **Claim to test.** The generic `c1`-pencil emptiness (producer-level)
  extends to the whole line: every exceptional pivot stratum (pivot factors
  of degree ≤ 16 over the degree-18 field `E`) is either empty or
  reconstructs to a certified survivor.
- **Dependencies.** The frozen tangent gate and whole-`B` machinery
  (reviewed); the producer `c1` pencil result (provisional); exact FLINT
  tower arithmetic (running). No new mathematics — this is coverage debt.
- **Cheapest discriminator.** Enumerate the pivot-factor vanishing loci from
  the recorded pivot numerators/denominators (the tangent gate already
  mandates recording them); for each stratum, rebuild the staged elimination
  with that pivot forced to zero (exact rebuild, no sampling), emitting a
  per-stratum denominator-cleared certificate. The pivot-tree logger (§4 Q4)
  makes this a loop, not bespoke work.
- **Outcomes.** (i) All strata empty → the `c1` line is fully excluded — the
  first whole-line kill in the common-centering program; the matrix-changing
  successor moves to `c3`/two-parameter families with the same logger.
  (ii) A stratum survives → an explicit exceptional centering candidate;
  test it against the next centered row before any family claim (the
  reviewed pattern: survivors die at the following band or become real
  leads). (iii) A pivot factor fails to clear denominators → the licensed
  fallback is the exact rebuild at that specialization; if that also stalls,
  record the stratum `OPEN` — fail closed, never interpolate.
- **Stop condition.** All strata certified; or two strata exceed the
  six-hour cap (then the engine, not the math, is the bottleneck — redesign
  the logger); or the independent 3,602-column reconstruction returns a
  discrepancy (immediate quarantine of the pencil result and this card).
- **Information gain.** Moderate but necessary: converts a generic-open
  statement into line coverage, and produces the reusable stratification
  logger for every later `E[B]/E(C)` pencil (dead-stretch, F1, pole).

## 8. Pinchuk audit status — what is checkable here, and the exact missing checks

Checkable now, without the paper: (a) the degree arithmetic — for
`(k,r)=(4,3), d=3`: `d(kr-k-r)+1=16`, matching the packet's spectral boundary;
(b) the lower-bound direction of Thm 4.1 via Wronskian/Mason–Stothers algebra:
`W(p^r,q^k)=p^{r-1}q^{k-1}(r p'q-k p q')`, and if `r p'q-k p q'=c` is a
nonzero constant then `W(D,q^k)=c\,p^{r-1}q^{k-1}` with `D=p^r-q^k`, whose
degree bookkeeping forces `deg D >= d(kr-k-r)+1` (I re-derived this
inequality direction; the full three-way equivalence, its coprimality/leading
hypotheses, and behavior over the differential field `C(x)` remain unverified
here and are the launched audit's item 4).

Missing checks (exact): fetch and freeze the English full text
(`https://www.mathnet.ru/php/getFT.phtml?jrnid=im&option_lang=eng&paperid=9017&what=fullteng`),
hash it into `refs/`, and snapshot Pakovich–Zvonkin `arXiv:1306.4141`. The
launched Grok audit's seven items cover definitions/fidelity, Thm 1.3
provenance, Thm 3.4 termination/cases, Thm 4.1, PZ scope, the max-12 client,
and impact mapping. I would add four checks not explicit there:
1. **td preservation**: whether q-equivalence preserves generic fibre
   cardinality/field-extension degree in both directions — load-bearing for
   any `G2-PSC`-adjacent use, stronger than invertibility preservation.
2. **Quantifier form**: whether Thm 3.4 normalizes *every* noninvertible
   Keller map or selects *some* representative (the REDUCTION CRITICAL-2
   failure pattern).
3. **Recovery direction**: whether the reduced q-map retains enough data to
   reconstruct the polynomial map, or is a one-way projection like GGV §4
   (in which case it is a necessary-condition mill, not a reduction client).
4. **Translation fidelity**: IM9017's English text is a translation; any
   load-bearing sentence should be checked against the Russian original if a
   discrepancy is suspected.

## 9. Strongest attacks

**Proof:** Card 1's chain — verify `W`, stratify by spectral degree, close the
extremal stratum by rigid-list arithmetic plus Taylor boundaries, close
`nu!=0`/`mu!=0` by the norm-typed terminal-ODE valuation method, finish
`k!=0` from the Kuranishi quadrics (order-one core, invariant loads,
`disc(K)=0`, `k=0` handoffs) — then port the template to `(8,12)` (signature
`(3,2)`, bound 5, classical tree classification) and ask the uniformity
question (§3 P2). This is the only currently visible route to a *mechanism*
rather than a bash.

**Counterexample/falsification:** Card 2's tower test at `p=3` fixed degree
with structural carries, calibrated against the cyclotomic false-positive
family; secondary: keep the AS109 coupled-section question (`y>=12`,
`CLOSED-SUPPORT+UNIT-L`) parked until a gauge section exists; tertiary
(SPECULATION, audit-gated): the q-category counterexample search of §3 D4.
Also genuinely falsifying: Card 1 outcome (iii) — a surviving extremal
trajectory would be the sharpest counterexample lead the partial-`y` program
can produce.

## 10. Lane recommendations (continue / redesign / stop)

| Lane | Recommendation |
|---|---|
| Max-12 `(9,12)` order-three fibre/Kuranishi (internal root 1) | **continue**, with Card 1 step 0 (independent `W` verification) inserted before any consumption of the spectral facts — the packet itself demands it. |
| Max-12 preflight + shared Faber hostile reviews | **continue** (background, nonblocking). |
| Corrected AS 30-row successor (internal root 2) | **continue; redesign the acceptance engine** to the ghost/divided-carry compiler before degree-ten rows are banked (Card 2). Keep the 29-row package quarantined; never substitute the radical or the cover pieces for `I`. |
| AS erratum hostile review | **continue**; on `GAP/REFUTED`, quarantine Card 2 descendants mechanically. |
| TD6 `c1` pencil + exceptional strata, `c3` dual thickening (internal root 3) | **continue** into Card 3; fail closed on any denominator that will not clear. |
| TD6 3,602-column independent reconstruction | **continue** (it gates the pencil result's promotion). |
| Pinchuk source audit (launched) | **continue**; single owner stands — no duplicate lane; adopt §8's four added checks; snapshot the PDF and PZ 1306.4141 into `refs/`. |
| Broad web sweep #9 (due 21:25Z) | **continue (mandatory)**, adding targeted queries: Pakovich–Zvonkin DZ-pair classification and successors; quasi-polynomial Jacobian literature citing IM9017; Mason–Stothers equality over function fields. Absence from prior sweeps is not novelty evidence. |
| AWS expansion | **keep stopped** — every current gate is symbolic/certificate-bound, agreeing with the packet. |
| B=168, deeper fixed-depth D-work, new book cells, raw passports, primitive-group censuses without support bounds, generic sparse search | **keep stopped/held** — nothing this round reopens them. |
| Ideation/sweep cadence | **continue** per COORDINATION clocks. |

## 11. History/priority checksum record

Searches run over `*.md` (plus targeted globs) before any novelty claim:

- `Pinchuk` → 18 files; all pre-cutoff hits are the *real* Pinchuk map
  (avenue 24) or this round's packet/prompts; the 2021 q-polynomial paper is
  absent from campaign history at cutoff, as the packet states. `refs/`
  contains no copy (checked by glob); a post-cutoff audit-prompt exists.
- `Zannier|Zvonkin|Shabat|dessin|Mason|Stothers` → the DS/dessin bypass for
  `(8,12)/(9,12)` was pre-identified in `ideation-20260824T1358Z-claude.md`
  and `ideation-20260824T1633Z-grok.md` (labeled watch-only) → my C2 is
  **KNOWN-strengthened**, not new; the norm-typing mechanism M1 and the
  q-category connection C1 do not appear anywhere in history → **NEW**.
- `de Rham|Witt vector|ghost|Verschiebung|resolvent|norm form` → avenue-39
  AS-only reopen (Cartier class) is prior (1633Z cube-owner) → my C3 is the
  operational extension, labeled accordingly; "resolvent" history is the
  stopped quartic-resolvent card family (stale degree-4 target, different
  object) → no conflict; no prior ghost-coordinate compiler proposal →
  accelerator is **NEW** as an instrument (the *policy* it implements is
  commit `7832fb7`, acknowledged).
- Known-degree/closed-case ledger reconciled: first open mapping degree six
  (Domrina/Żołądek); partial-`y` closed through 11 (reviewed); `(8,12)/(9,12)`
  open; AS109 `y`-floor 12 (conditional corollary, flagged as my inference);
  `(72,108)` conditional-external; td-7 panel closed at filed perimeter;
  td-11/13 open. No card above targets a closed case.

## 12. Honest limits of this submission

The spectral/DZ program (Card 1) rests on coordinator-derived, unpromoted
identities and an unaudited external classification claim; every step is
gated accordingly. The q-category reading of Pinchuk 3.4 is SPECULATION and is
nowhere load-bearing above. The `c1` generic exclusion is producer-level and
is treated as sensitivity information, not coverage. Nothing in this report
proves or disproves JC2, and no result here should be promoted without the
different-model review required by COORDINATION.
