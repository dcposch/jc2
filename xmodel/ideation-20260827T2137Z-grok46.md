# Blind whole-portfolio ideation — `20260827T2137Z` — Grok46

Sealed packet: `xmodel/ideation-20260827T2137Z-packet.md`  
Packet SHA-256: `a492f5eeeb5786f9061bf3730a077b6e256e8386c5dcb42c978bbda5519aac80` (verified)  
Ideator: Grok 4.6 (xAI), designation **Grok46**  
Status: **CLEAN-BLIND SUBMISSION.**  This is an independent whole-portfolio scan, not a review of a peer report.

No algebraic result, unit certificate, family exclusion, counterexample, or JC2 verdict is claimed.  A finite fixture is not a family landing; a selected chart is not a fan cover; a finite jet is not an arc; a proper superlocus is not a witness; a producer report is not promoted evidence; a model name is not a vote.

---

## 0. Frozen basis, scope, and safety

Git basis `418e413593120d19e15e6546eb50c985f4b1f038` and all eight packet-listed file hashes matched before any scientific use.  All seven listed evidence hashes also matched.

Read: complete 46-row inventory and 21:30Z overlay in `APPROACHES.md`; newest correction blocks in `AUDIT.md` and `PROGRESS.md`; 21:17Z live-state and 21:30Z Box03 snapshot in `notes.md`; 18:08Z synthesis as the mandatory history/dedup baseline; the packet-named evidence files; `xmodel/websweep-20260827T1348Z.md`.  Individual 18:08Z ideator reports were not used as a novelty source.

Safety: no AWS launch or disturbance; no canonical-ledger edit; no `jc2-lean` entry, listing, read, build, or modification as a research act.  One accidental workspace-wide grep hit paths under `jc2-lean/` and the possibly post-seal file `xmodel/g2-c74-place-exit-rpmc-hostile-review-opus5-20260827.md`.  Both are firewalled: nothing from either is used as evidence, and `C74-PLACE` / `EXIT-RPMC(C)` remain unpromoted as the packet requires.  Subsequent searches excluded `jc2-lean/**` and `xmodel/ideation-20260827T2137Z-*`.  No peer 2137Z submission was read.

Desk-scale exact checks only; no heavy local algebra.

---

## 1. Disposition vector for avenues `1..46`

Baseline is the canonical 46-row table as already overwritten by the 21:30Z overlay and the 18:08Z synthesis.  `unchanged` means that ranking and scope stay.  Reasons are given only for non-unchanged rows.

| # | Disposition | Reason if not unchanged |
|---:|---|---|
| 1 | unchanged | Genuine survivor-`H` upper endpoint and family-pinned LF40 remain the right GGV spend.  The LF40 R1 serializer repair is compiler custody, not a new mathematical avenue.  The live `A=X^4-1`, `V=1` fixture is a known q1-negative independence discriminator, not a q1-compatible survivor. |
| 2 | raise | The transpose discriminator is now decisive: signed conjugation is a source theorem for a conjugated pair, not the other chart of one fixed pair.  Concentrate on the pure-Sigray intrinsic two-chart forest plus the narrower same-pair VGG 7.3/7.4 corridor.  Stop spending on a wholesale anti-standard copy of GGV5 Theorem 2.20 as fixed-pair coverage.  `G2-PSC` remains a hybrid-only debt; it is not the first pure-Sigray theorem. |
| 3 | raise | The first honest coupled/extension object is now namable: the nearby-cycles exact sequence of the GGV univariate face unfolding (Card A).  That is a depth-three / extension client, not another isolated rank-one row.  Lower-face ODE `(4,-1)` and upper rank-one gates stay as graded pieces, not as a substitute for the extension. |
| 4 | unchanged | Fixed-`H` saturated germ remains an exact finite client; global algebraization remains open.  Do not identify a proper `lambda`-saturation with a Keller map. |
| 5 | unchanged | Cusp leading forms still block triangular shears; amalgam rigidity does not bite on non-automorphisms. |
| 6 | unchanged | One-place theorem still applies to unpinned `A(F)` components, not to multi-place fibres. |
| 7 | raise | Only as a quantitative finite-end client.  The already-proved pole/finite-end Riemann--Hurwitz ledger (`sol-pc.md` Prop. 2.2--2.3) says a counterexample's finite-valued ends carry total local degree at least `td`.  That is an `A(F)` ramification lower bound, not a construction of `A(F)`.  Do not revive dual-pencil vanishing-cycle divisors that already returned `TYPE-FAIL`/`JUMP-ONLY`. |
| 8 | unchanged | No uniform truncation bound. |
| 9 | unchanged | Still an implication ladder with a worse generating function. |
| 10 | unchanged | Quintic module already `NO LEVERAGE`; no later observable is named. |
| 11 | unchanged | Dead as a route. |
| 12 | unchanged | No integer to send to infinity. |
| 13 | unchanged | Strictly stronger than JC2; no Schur form in `A_2`. |
| 14 | unchanged | Defensive recon only. |
| 15 | unchanged | Dictionary incomplete. |
| 16 | raise | Rank-one de Rham index stays the concrete invariant.  **Close** the irregularity-escape for polynomial clients: R2 source-pins that `P,W` generate regular-holonomic modules and algebraic six operations preserve regularity, including nonproper algebraic direct image.  A global rational exponential twist is sufficient, not necessary, but a polynomial Keller pair cannot manufacture irregularity by algebraic operations.  **Close** naive filtered rank-bundling.  **Raise** the first exhibited-filtration client: nearby cycles of the face unfolding (Card A), which is exactly the short exact sequence R1 now requires. |
| 17 | unchanged | Category error; the cubic class contains genuine higher-dimensional counterexamples. |
| 18 | unchanged | Graded plane Keller maps are automorphisms. |
| 19 | unchanged | K00 rank filtration remains the strongest finite falsification lane.  The live full-`P6` rank-exact successor is the right object; the origin is a trivial rational point and must not be re-targeted. |
| 20 | unchanged | Known bridge consumed false PC(2)/JC(4) statements. |
| 21 | unchanged | Hensel does not kill `A_infinity` or force a degree congruence. |
| 22 | unchanged | Siegel classifies the wrong direction. |
| 23 | unchanged | Holomorphic analog is false. |
| 24 | unchanged | Pinchuk `J` is nonconstant. |
| 25 | raise | Only a cheap face-loop character constraint (Card B), not a new full branch-cycle CSP.  Rank-one `nabla_m` monodromy around roots of `H` constrains the cyclic quotients of those conjugacy classes in a passport; it does not couple the two coordinates or replace Riemann existence. |
| 26 | unchanged | Still the cheapest untried attack on a cofinal `td` ceiling; Orevkov `(48,64)` remains the required negative control.  No proved ramification-support bound currently connects a small empty range to JC2. |
| 27 | unchanged | Admissible diagram is not algebraic realizability. |
| 28 | unchanged | Naive BMY blocked; Euler/genus ledger passes identically. |
| 29 | unchanged | Quotient is an exact reformulation without a universal receiver. |
| 30 | unchanged | ML invariant does not see the embedding. |
| 31 | unchanged | Finiteness is the missing global assertion. |
| 32 | unchanged | Three-generator presentation is still injectivity. |
| 33 | unchanged | Untwisted global exactness stays closed.  Twisted face-character bookkeeping remains absorbed into avenues 1 and 16; Card B is that bookkeeping, not a revival of ordinary action residues. |
| 34 | unchanged | No spare coordinate to absorb a pole. |
| 35 | unchanged | Known mechanisms are structurally 3-dimensional. |
| 36 | unchanged | Search only on theorem-compressed survivor schemes and nonzero/source-open K00 charts.  Ambient random sampling stays stopped. |
| 37 | unchanged | Char-`p` extras will not lift. |
| 38 | unchanged | Prevariety overapproximates. |
| 39 | unchanged | No 10-line class attached to `J=1`. |
| 40 | unchanged | Commutative det-1 does not give free invertibility. |
| 41 | unchanged | Principle falsified by dimension-3 counterexamples. |
| 42 | unchanged | Arrow points the wrong way. |
| 43 | unchanged | Collapses to GGV after Aut-reduction. |
| 44 | unchanged | Closed as proof input; retain only the repaired membership theorem. |
| 45 | unchanged | Inverse PDE is tautological. |
| 46 | unchanged | Formalization cannot manufacture the finite universe.  The user-owned `jc2-lean` tree remains out of campaign scope. |

---

## 2. Reranked bottlenecks and falsification attacks

### Three highest-value proof bottlenecks

1. **Universal / cofinal degree or landing theorem.**  After the transpose discriminator, this is not repaired by a second GGV source run.  The exact pair already names the intrinsic two-chart forest.  What remains is Sigray decoration trust (including the filed Proposition 4.2 tower gap), complete landing, `G2-BD` where invoked, and a type-relative bound `RPMC(C)`.  VGG 7.3/7.4 corridors contribute **zero** to mismatch energy; total degree is supported at exits.  `EXIT-RPMC(C)` is therefore not a cheaper theorem than `RPMC(C)`: once corridor centers are identified, bounding exit amplitudes *is* `RPMC(C)`.

2. **Typed same-pair anti-side interface, not `G2-PSC` as the first spend.**  `G2-PSC` is still the hybrid GGV-packet-to-tree map.  Pure Sigray bypasses it.  The honest same-pair remainder is: retain every residual root of Corollary 7.4 faces, prove translations are actual Puiseux truncations (`H-TRUNC`), and identify leaves with proximity centers without duplication.  That interface is unpromoted.  Live `CornerData` still has no coefficient pair, so it cannot feed the interface.

3. **A decided affine endpoint on a genuine q-compatible survivor family.**  Rank-one de Rham capacity already says rows 23--34 cannot empty the determinant-anchored sub-tower.  The exact identity `E=t^{22}` licenses an infinite `q` tower with no proved bound.  The live Box03 fixture omits `D23/G22/q1` and is q1-negative at `V=1`.  A prefix verdict there is a discriminator, not a family landing.

### Two strongest falsification / counterexample attacks

1. **K00 nonzero/projective, source-open, rank-exact, full-`P6`, prolongable point.**  Promoted facts: every geometric point of the dimension-three base has rank at most two; `I2` cuts to dimension two; radicals are distinct; a rank-exactly-two stratum is nonempty over `\overline{\mathbb Q}`.  The origin is trivial.  The live r6a job is the right object.  No explicit point, jet, arc, or receiver follows from the stratum theorem.

2. **Family-pinned LF40 through row 40 on the reviewed R1 ideal.**  Compiler custody is now different-model confirmed (`1+f_0_1*g_1_0-f_1_0*g_0_1` at row 17; 740 generators; seven target mutations rejected; staging immutability repaired live).  A characteristic-zero unit would exclude only the last `(72,108)` GGV case conditional on the primary reduction and raise that conditional bound from 108 to 125.  It would not prove JC2.  No row-1 terminal basis exists at the seal.

---

## 3. New mechanism and new connection

History search (excluding `jc2-lean/**` and `xmodel/ideation-20260827T2137Z-*`) was run before these labels.

### 3.1 NEW mechanism — `FACE-NC`: nearby-cycles SES of the GGV face unfolding

**Target obstruction.**  R1 now source-pins `h^1(E)\le h^1(A)+h^1(B)` only for an *actual* short exact sequence of connections.  Keeping two series together does not exhibit that filtration; an irreducible rank-two connection is outside R1.  Polynomial clients cannot escape through irregularity (R2).  The campaign therefore has a rank-one index law and no honest higher-rank client.

**Mechanism.**  The GGV edge is a univariate unfolding `F(X,t)=F_0(X)+t F_1+\cdots` with face polynomial `H` (on branch P, `H=A^2`).  The promoted connection

```text
nabla_m = d + (m/4) dH/H    on    U = A^1 \ Z(H)
```

is the rank-one eigenline of the face-loop monodromy of this unfolding, with character `H^{-m/4}`.  The nearby-cycles exact sequence of the degeneration from a generic small-`t` fibre of `F(\,\cdot\,,t)` to the special fibre `H=0` is an actual short exact sequence of (regular) constructible sheaves / connections on `U`.  Consecutive weight pieces are the named row connections; the extension class is a rank-two client to which R1 applies.

**Decisive test.**  For the live squarefree branch-P data `A=X^4-1`, `H=A^2`, `m=23`:

- `e_i=2`, so `k_{23}=1` iff `4\mid 2\cdot 23`.  This fails, hence `k_{23}=0` and `dim H^1(U,nabla_{23})=r-1=3`.
- Independently, the q1 operator `T_A(Q)=2A Q'-3A'Q` on `deg Q\le 12` has rank 13 in the 16-dimensional `F1` window, cokernel dimension 3.
- Residue of `(23/4)\,dH/H` at each simple root of `A` is `23/2`; local monodromy is `exp(-2\pi i\cdot 23/2)=-1`.  Four finite meridians multiply to `+1`; infinity monodromy is trivial, consistent with `deg H=8`.

These three numbers matching is the first discriminator: `coker T_A \cong H^1(nabla_{23})` as vector spaces with matching local monodromy characters.  The second discriminator is whether the nearby-cycles sequence of the actual unfolding `F=H+t F_1+\cdots` (not an abstract pair of row connections) is nonsplit for some consecutive `(m,m')`.  A split sequence is R1-vacuous; a nonsplit sequence is the first licensed rank-two client.

**History labels.**

| Claim | Label | Closest hits |
|---|---|---|
| `nabla_m` rank-one index law | `KNOWN` | Promoted Opus5/Fable5 gate law; R5 mode schedule `k_m` |
| Dual-pencil vanishing-cycle divisor of `aP+bQ` at infinity | `KNOWN` / `SCOPE-CONFLICT` | 2026-08-24 Avenue 16/25 reopen; later `TYPE-FAIL` / `JUMP-ONLY`.  Do not revive that divisor. |
| Sabbah/Hamiltonian vanishing cycles of `G_f^{(0)}` at atypical values | `KNOWN` | Hamiltonian-`kappa` gate; different object |
| Nearby-cycles SES of the *univariate GGV unfolding* as R1's exhibited filtration; `nabla_m` as a character of vanishing homology | **`NEW`** | No campaign document identifies `nabla_m` with nearby cycles of `F(X,t)`, or exhibits the SES R1 demands |

### 3.2 NEW connection — `FACE-CHAR`: face-root monodromy characters cut Avenue 25 passports

**Connection.**  Avenue 16's `nabla_m` is a rank-one local system on `A^1\setminus Z(H)`.  Avenue 25's passports are permutation representations of degree `td` on the sheets of `\widehat g`.  These cannot be identified (different rank, different base).  They *do* share the conjugacy classes of loops around roots of the face polynomial, once a Newton face of the actual pair is identified with `H`.  For those classes only, the permutation's cyclic quotient must carry the character `exp(-2\pi i\cdot (m e_i)/4)`.  On the live P fixture at `m=23`, every face-root loop acts by `-1` on that eigenline, so those permutation classes cannot be in the kernel of the sign character.

**History labels.**

| Claim | Label | Closest hits |
|---|---|---|
| Untwisted passports are too generous | `KNOWN` | Avenue 25 original stuck-point; all 169 residue-A passports admit transitive identity tuples |
| Opus5 passport-rigidity unification with de Rham | `KNOWN` / unpromoted heuristic | 18:08Z synthesis: "passport-rigidity consequences remain proposed research" |
| Dual-pencil CSP coupling all linear projections | `KNOWN` / stopped | 2026-08-24 Avenue 25 raise; not this object |
| Face-root loop characters of `nabla_m` as a *local* constraint on permutation types | **`NEW`** | Adjacent to the unpromoted rigidity heuristic, but the decisive object is a named conjugacy class, not a global CSP |

This is also the required new connection among existing avenues: **16 ↔ 25**, with 1 supplying the face polynomial `H` and 2 supplying (only after an exact pair) the identification of `H` with an actual Newton face.

---

## 4. Required connection tests and principled separations

Objects tested: intrinsic two-chart forest; `C74-PLACE`/`EXIT-RPMC`; finite-end Riemann--Hurwitz identity; upper de Rham gates; affine endpoint; lower LF40; K00 rank filtration; branch-cycle CSP; global landing/receiver obligations.

| From | To | Composition | Separation |
|---|---|---|---|
| Intrinsic two-chart forest | `C74-PLACE` | Invalid on `CornerData` | The forest is constructed from an exact normalized pair and a fibre.  VGG 7.3/7.4 need valuation-ratio hypotheses and eligible faces of that same pair.  Live `CornerData` has no coefficient field.  Feeding a frozen polygon record to `C74-PLACE` is a type error. |
| `C74-PLACE` | `EXIT-RPMC(C)` | Valid only after proximity identification | `C74-PLACE` is a place/center bijection.  `EXIT-RPMC` is a numerical bound on mismatch at exits.  Once centers are identified and every nonzero mismatch is assigned to a unique first exit, bounding those amplitudes *is* `RPMC(C)`.  The corridor length is not a degree bound: common-power faces contribute zero to `(1/2)\sum(R_p/\alpha-S_p/\beta)^2`. |
| Finite-end RH identity | Pole-only landing | Invalid as a coverage claim; valid as a numerical identity | The identity is `KNOWN`: `sol-pc.md` Prop. 2.3, restated in the bypass note as `\sum_{S\in N} e_S = td + b_1(C)-1`.  A pole-only contradiction *can* prove JC2 without modeling finite ends if it independently empties every pole configuration (pole mass is tautologically `td`).  A "full boundary coverage" theorem cannot discard the finite-end sector.  `C74-PLACE` corridors are not that sector. |
| Finite-end RH | `EXIT-RPMC` | Conditional numerical coincidence, not a map | Bypass-note (4.5) equates pole mass, mismatch energy, and finite-end ramification (normalized).  That makes them three coordinates of one number, not three composable theorems.  Bounding one bounds `td`; it does not identify exits with finite ends. |
| Upper de Rham gates | Affine endpoint (live Box03) | Invalid | Gates begin at `q1` / row 23.  Every live upper decision input omits `D23/G22/q1`.  Desk check: `V=1` gives `F1=H=A^2`, which lies outside `im T_A` at every tested degree bound `deg Q\le 4,\ldots,24`; the reduced-cascade handle `V=A'R+2AR'` never hits `V=1` in polynomials.  This q1-negativity is `KNOWN` (18:08Z independence discriminator).  A `D0..D22` unit kills a negative control; a proper prefix would show endpoint independence of `q1`, not a survivor. |
| Affine endpoint | LF40 | Invalid | Different face, different grading, different rigidity.  Upper `D22=-L_{22}(g_{22})` is rigid; lower `G_{17}` has eight free slots.  No typed row map.  A unit in one is not a unit in the other. |
| LF40 | K00 rank filtration | Invalid | GGV family-pinned jet versus D-series order-two maximum-twelve collision.  Already the campaign type `NOT_TYPED_COMPOSABLE` for analogous transports. |
| K00 | Branch-cycle CSP | Invalid | Fitting rank of a grade-seven matrix is not a permutation of sheets of `\widehat g`. |
| Branch-cycle CSP | Global landing / `G2-BD` | Invalid | Passports do not supply pole-path landing or a carrier-denominator bound.  Card B constrains only face-root conjugacy classes. |
| Two-chart forest | Upper de Rham | Invalid as identification; Card A is the proposed bridge | Forest lives on boundary places of `f=a`.  `nabla_m` lives on `A^1\setminus Z(H)`.  Different base, different rank.  Nearby cycles of the *face unfolding* are a third object, not a rewrite of the pair's Eggers--Wall forest. |
| Anything above | K00 receiver / mixed `Lambda^{19}` reachability | Invalid | Closed `J1` charts and the K00 origin-trivial threefold are a separate deformation-theoretic receiver obligation.  GGV face instruments do not map into it by shared variable names. |

**Single sharpest invalid composition.**  Treating signed-transpose equivariance plus VGG `psi_2` as the missing same-pair infinity chart.  The two printed pair-sign conditions are mutually exclusive for every finite-support pair in every `L^{(l)}`; at the live corner the rejected sign is `28-8=20>0`.  `psi_2` sends `x=\infty` to `x'=0` and changes the compactification.  That composition is now a reviewed theorem of *failure*.

---

## 5. Strongest attacks to launch now

### Strongest proof attack

**Object, not slogan:** for one exact normalized pair (an automorphism negative control first, then the campaign's explicit non-Keller `8_28` realization if a coefficient-complete pair is available), compute the intrinsic two-chart Newton--Puiseux forest *and* the Corollary 7.4 residual roots on the opposite interval, and test whether successive translations equal the actual branch truncations at those roots.

This is the cheapest `C74-PLACE` discriminator that does not wait on AWS and does not consume `CornerData`.  It attacks bottleneck 2 at the exact missing arrow (`H-TRUNC` + leaf/place custody).  Do not launch a fourteen-clause anti-standard rewrite of Theorem 2.20.  Do not treat a PASS as `RPMC(C)` or a degree ceiling.

### Strongest counterexample / falsification attack

**Object:** a nonzero point of the rank-exactly-two stratum of the six-variable K00 base that additionally satisfies the omitted full-`P6` equations.

The live r6a job is already this object; do not replace it.  Do not launch a parallel "random rational point on the threefold" search: the origin already is one.  If r6a caps with no verdict, the cheapest successor is an explicit geometric point of `J_2=B+I_5` off the origin (hyperplane section to dimension zero, then a number-field point), *then* a full-`P6` incidence test — never a modular properness flag promoted as a witness.

LF40 R1 is the second falsification object and should continue, not be restarted or widened.

---

## 6. One correctness-preserving software / AWS acceleration

**Do not launch the held 466-generator prefix quotient while any of the five Box03 decision lanes is alive.**  At 21:30Z those lanes used about 112/495 GiB with 383 GiB free and caps at about 01:13--03:04Z; the authoritative `lp/std` lane alone held 59 GiB RSS against a 280 GiB cap.  The packet already preregisters 466 only after current lanes fail *and* Box03 retains about 150 GiB headroom.  A sixth GB of the same q1-negative fixture is not an acceleration.

**Proposal: idle cofactor-certificate service, not a new solver.**

- **Exact inputs.**  Upper: the frozen raw generators of the live branch-P fixture, plus the already-computed 697 cofactor entries / 994 monomials for the 47 omitted prefix generators of the 466 quotient (`8e25502c...` / `afd9565d...` / `c66b1396...`).  Lower: the reviewed R1 ideal whose row 17 is `1+f_0_1*g_1_0-f_1_0*g_0_1` (freeze `70af5634...`, program `435c350f...`).  K00: the twice-reviewed full-`P6` source freeze `f149a179...`.  Do not reserialize any of these.
- **Parallel decomposition.**  Already in force: five upper orders/engines on Box03; LF40 `dp/slimgb` on r6b and `std/dp` on Box02 (41 engine-call changes, same ideal); K00 one core on r6a.  Add **zero** solver cores.  The new artifact is a replay harness that sits idle until a unit or a claimed proper basis appears.
- **Certificate / replay format.**  On a reported unit: (i) the engine's expression of `1` on its working generators; (ii) exact-`Q` cofactors back to the frozen raw generators, not to an intermediate basis; (iii) for a 466-quotient unit only, composition with the precomputed 47-generator cofactors to a raw-unit certificate; (iv) independent replay of that linear combination by a second engine (upper: a different order; lower: slimgb vs `std`).  On a reported proper basis: a rational or number-field witness with every raw equation evaluated, not a `dim` status flag.
- **Resource estimate.**  Zero additional GiB and zero additional cores while the seven live Singular processes run.  Replay of a unit is desk-scale or one short AWS core after harvest.  466 remains a fallback: one core, two hours, at most 128 GiB VM, only after a fresh Box03 audit shows zero swap and 150 GiB headroom.
- **Evidence firewall.**  Modular and row-RREF lanes stay guidance.  Positive-tail row-22 units already harvested are not family evidence and must not trigger 466.  Engine-A unit without raw cofactor replay is not a verdict.  LF40 slimgb and `std` must agree on the same frozen generators; disagreement is an engine bug, not a mathematical fork.  Producer reports are not promoted evidence.
- **Stop rule.**  First exact unit with replayed raw cofactors, or first properness with a literal witness of all raw equations, or resource cap.  Then harvest and fold; do not spawn a representation-equivalent sixth upper lane.  If the current `V=1` fixture units, the *next* upper object is a point of the 5-dimensional q1 handle `V=A'R+2AR'` (`deg R\le 4`), not 466 generators of the same negative control.

---

## 7. Idea cards (three)

### Card A — `FACE-NC` exhibited filtration

- **Target gap.**  R1 has no licensed rank-two client because no short exact sequence of named row connections has been exhibited; polynomial irregularity is closed.
- **Dependencies.**  Promoted `nabla_m` gate law; R7R1 row operators; R7R2 scope split; the univariate GGV unfolding (not a global Keller pair).
- **Cheapest discriminator.**  For `A=X^4-1`, `H=A^2`, compare `dim H^1(nabla_{23})` with `dim coker T_A` (already both 3; see §3.1 and §10), then compute whether the nearby-cycles sequence of `F=H+t F_1` is nonsplit for `(m,m')=(23,24)` at the reduced cascade `F_1=HV`.  Desk-scale linear algebra on the unfolding, not AWS.
- **PASS.**  A nonsplit SES of the two named connections, constructed from the unfolding.  R1 then applies to that object only.
- **FAIL.**  The sequence splits, or the vanishing-cycle sheaf is not an extension of the two row connections.  Then R1 stays vacuous and irregularity stays closed: Avenue 16 has no higher-rank polynomial client.
- **Stop.**  After one squarefree P fixture and one squarefree Q fixture.  Do not iterate over further `m`.
- **Rollback subtree.**  Only this card and the Avenue 16 "coupled client" raise.  Rank-one index law and R2 regularity closure are untouched.
- **Information gain.**  Either the first licensed rank-two object, or a proof that the face unfolding does not supply one — which closes the last natural R1 candidate.

### Card B — `FACE-CHAR` face-root passport cut

- **Target gap.**  Avenue 25 passports are too generous; the unpromoted "passport-rigidity" heuristic has no named conjugacy class.
- **Dependencies.**  Card A's character computation (local monodromy of `nabla_m`); an identification of `H`-roots with actual face-root loops, which for a GGV *fixture* is tautological and for a global pair requires the exact-pair forest, not `G2-PSC`.
- **Cheapest discriminator.**  On the live P fixture, every face-root meridian acts by `-1` on `nabla_{23}`.  Enumerate the 169 residue-A identity tuples (already known to exist) and test whether any permutation assigned to those meridians lies in the kernel of the sign character.  If the GGV face is not a residue-A face, run the same sign test on the `8_28` four-root deck orbit `(z^4-alpha)^{21}` instead: four roots, one orbit, character `-1`.
- **PASS.**  A forbidden cycle type on a named conjugacy class.  That kills those passports only.
- **FAIL.**  All existing tuples already satisfy the sign constraint.  Then Card B is costume: the character is too weak to cut Riemann existence.
- **Stop.**  After the sign character on one named orbit.  Do not build a two-coordinate CSP here.
- **Rollback subtree.**  Only the Avenue 25 cheap raise.  Avenue 16 rank-one law stays.
- **Information gain.**  Either the first passport cut that uses the promoted de Rham law, or a documented negative control that the character does not constrain identity tuples.

### Card C — q1-compatible upper successor (allocation, not a new theorem)

- **Target gap.**  Five live exact-Q lanes decide a fixture already outside `im T_A`.  Their information value is the independence discriminator (`D22=1` without `q1`), which is `KNOWN`.  They cannot land a genuine survivor-`H` endpoint.
- **Dependencies.**  Promoted reduced cascade `F1=HV`, `F2=(V^2+HZ)/4`; promoted q1 handle `V=A'R+2AR'`, `deg R\le 4`; live Box03 harvest.
- **Cheapest discriminator.**  Wait for the current lanes (do not disturb them).  If they return a unit, the discriminator has fired in the expected direction; next input is one explicit handle point, e.g. `R=1`, `V=A'` (check `A'` is nonzero squarefree-coprime as required), rows 1--23 now including `q1`.  If they return proper, the discriminator has fired in the interesting direction (`D22=1` independent of `q1`); still the next *family* object is a handle point, not 466 generators of `V=1`.
- **PASS.**  A compiled exact target on a handle point, with `q1` imposed, whose raw generators replay.
- **FAIL.**  Handle-point target is unit through row 23.  Then this particular 5-dimensional slice dies; remaining q1-compatible `R` of degree `\le 4` is a 4-dimensional residual, not a farm.
- **Stop.**  After one handle point and, if unit, one second point with `deg R=4`.  Do not saturate the five-parameter family on AWS.
- **Rollback subtree.**  Only the next upper fixture choice.  Current live lanes, LF40, and K00 stay.
- **Information gain.**  Re-anchors upper spend on a q1-compatible object, the same class of lifecycle correction that emptied the artificial `H=X^8-1` gate.

---

## 8. Major-lane `continue` / `redesign` / `stop`

| Lane | Verdict | Note |
|---|---|---|
| K00 | **continue** | Live full-`P6` rank-exact job on r6a is the right object.  Do not restart legacy R2/R5.  Do not insert big-ring covectors `l0,v1,v2` into the six-variable `BASE4` ring.  Six-variable R4 remains held. |
| GGV upper | **continue** current five Box03 lanes as the independence discriminator; **redesign** the *next* fixture to a q1-handle point (Card C).  **Stop** any plan to launch the 466-generator quotient against `V=1` except as the packet's stated fallback. |
| GGV lower | **continue** | R1 compiler is reviewed; r6b `dp/slimgb` and Box02 `std/dp` are the right pair.  Add no third engine.  A future unit still needs independent output review and excludes only conditional `(72,108)`. |
| Coverage / landing | **redesign** | Pure-Sigray forest + unpromoted `C74-PLACE` on an *exact pair* is the coverage architecture.  Hybrid `G2-PSC` remains a debt, not the next theorem.  Wholesale anti-standard Theorem 2.20 is **stop**.  Universal landing, `G2-BD`, and `RPMC(C)` remain open and are not implied by chart-naming. |
| TD6 / AS | **continue** | Independent of the three AWS fronts.  Do not steal Box03/r6a/r6b/Box02 cores.  No new td6/AS109 news in the sealed delta. |
| Formal algebraization | **continue** | Only the fixed-`H` saturated finite-scheme client (avenue 4) and the K00 mixed-deformation receiver.  A finite jet is not an arc. |
| Sparse counterexample search | **continue** | Theorem-compressed residuals only (avenue 36).  Ambient sampling stays **stop**. |
| External-claim audit | **continue** | Matysiak SSRN 7229458 remains `ACTIONABLE-UNAUDITED` / primary text blocked.  Do not rerank internal exact lanes on an abstract.  Next broad sweep is due 2026-08-28 13:48Z absent earlier news. |

---

## 9. Insight a differently trained researcher is likely to miss

The instinct after a transpose PASS is to copy the complete chain to the other chart.  The instinct after a rank-one de Rham law is to bundle two rows or to hunt irregularity.  Both instincts are now source-false.

What is easy to miss is a **sign computation on the mismatch form, not on the pair**.  VGG Proposition 7.3 propagates `v_w(P)/m = v_w(Q)/n` through the opposite interval.  On the common resolution that is `R_p/\alpha = S_p/\beta`, hence **zero** contribution to

```text
td / (alpha beta) = (1/2) sum_p (R_p/alpha - S_p/beta)^2.
```

So every additional common-power face, and every extra clause of an anti-standard complete chain, is invisible to total degree.  Degree is supported only at exits from proportionality.  Once that is seen, `EXIT-RPMC(C)` cannot be a cheaper successor to `RPMC(C)`: it *is* `RPMC(C)` after the centers are named.

The matching desk-scale check is elementary and does not need AWS.  For live `8_28`, Corollary 7.4 gives `ell(P)=alpha R^{12}` and `ell(Q)=beta R^8` on the certified interval, hence `v(P)/12 = v(Q)/8` whenever the common primitive is used.  The ratio `12/8=3/2` is exactly `m/n` for `(m,n)=(3,2)` after the `q=4` scaling, which is the zero-mismatch condition.  This is a three-line identity, not a forest theorem.

The same arithmetic explains Card A: `nabla_m` sees the *graded* face, which is the associated-graded of the unfolding, not the other infinity chart of the pair.  Copying theorems across that identification is the same error as copying Theorem 2.20 across the pair-sign wall.

---

## 10. Conjectural leaps, failed attempts, assumptions, checks

### Conjectural leaps (not theorems)

- `FACE-NC`: that nearby cycles of the univariate GGV unfolding *are* an extension of two named `nabla_m` row connections.  The dimension/character match at `m=23` is evidence for the eigenline identification, not for the SES.
- `FACE-CHAR`: that GGV face-root meridians inject into the permutation representation of an actual pair.  For a GGV fixture this is tautological; for a global pair it needs the exact-pair forest.
- `C74-PLACE` and `EXIT-RPMC(C)` are packet-provisional and under live Opus5 review.  Used only as reversible design; not restated as theorems.  Independently, `EXIT-RPMC` is not cheaper than `RPMC` (§4, §9).
- Bypass-note triple identity (pole mass = mismatch energy = normalized finite-end ramification) assumes the common-resolution identification of pole formulas with point-basis data.  The finite-end RH half is already proved in `sol-pc.md`.

### Failed attempts in this scan

- Treating `V=1` q1-negativity as a new lifecycle bomb.  History shows it is the 18:08Z independence discriminator.  Relabelled `KNOWN`.  Still used as Card C allocation, not as a theorem.
- Treating the bypass-note Riemann--Hurwitz identity as new.  It is `sol-pc.md` Proposition 2.3.  Relabelled `KNOWN`.
- Searching for an irregular polynomial-client escape after R2.  None exists inside algebraic six operations.  Closed rather than redesigned.
- A wholesale anti-standard Theorem 2.20 as same-pair coverage.  Reviewed FAIL at the pair-sign wall.

### Assumptions

- Frozen hashes are the entire scientific basis; post-seal reviews and terminal computations are out of scope.
- Heavy algebra remains AWS-only; seven live Singular processes must not be disturbed.
- Model identity is not evidence.

### Checks actually performed

1. SHA-256 of the sealed packet: match.
2. Git `HEAD` and eight frozen common-basis hashes: match.
3. Seven packet-listed evidence hashes: match.
4. Exact linear algebra over `\mathbb Q`: operator `L(R)=A'R+2A R'` on `A=X^4-1` never hits `V=1` for `deg R\le 0,\ldots,11`; injective at each window.
5. Exact linear algebra: `T_A(Q)=2A Q'-3A'Q` never contains `F1=A^2` for `deg Q\le 4,8,12,16,20,24`; on the D3 window `deg Q\le 12` the map is rank 13, cokernel 3.
6. Arithmetic: `k_{23}=0` for `e_i=2`, `dim H^1=3`, matching `coker T_A`; residues `23/2`; face-root monodromy `-1`; four meridians and infinity consistent.
7. Identity check: `T_A(A^2 R)=A^2 L(R)`, recovering the reduced-cascade q1 handle and the degree bound `deg R\le 4` from `deg(A^2 R)\le 12`.
8. Comparison of bypass-note (4.3) with `sol-pc.md` (2.13): same identity, `K_\infty = td + 2g_C + s - 2`.
9. Targeted history search for `C74-PLACE`, `EXIT-RPMC`, finite-end RH, irregular input / Airy, `nabla_m`, nearby/vanishing cycles, `V=1` / `A=X^4-1` q1 handles, Gauss-Manin face identification.  Pattern `ideation-20260827T2137Z` excluded from scientific reads; only this prompt and the packet were used from that family.
10. No AWS, no `jc2-lean` consumption, no peer 2137Z submission read.

### Accidental contamination (firewalled)

- Workspace-wide grep briefly listed `jc2-lean/gcd3-69-cube/...` paths.  Those files were not read as sources; no lemma from them is used.
- The same grep listed `xmodel/g2-c74-place-exit-rpmc-hostile-review-opus5-20260827.md`, which the packet classifies as live-at-seal / post-seal debt.  The file was not used.  `C74-PLACE` and `EXIT-RPMC(C)` are treated as unpromoted.

---

## End matter

- **Report path:** `xmodel/ideation-20260827T2137Z-grok46.md`
- **Exact model identity:** Grok 4.6, released by xAI; campaign designation Grok46.  Equal-standing whole-portfolio ideator with Sol, Fable5, and Opus5 for this round.  Model identity is not a vote.
- **Checks run:** packet and frozen-basis hashes; seven evidence hashes; desk-scale exact-`Q` linear algebra on `T_A` and `L` for `A=X^4-1`; residue/monodromy arithmetic for `nabla_{23}`; RH-identity comparison to `sol-pc.md`; targeted history searches with `jc2-lean` and peer-2137Z exclusions.
- **Blindness disclosure:** The sealed packet, the frozen common basis, packet-named evidence, the 18:08Z synthesis, and the 13:48Z websweep were read.  No other `ideation-20260827T2137Z-*` file except this prompt and the packet was used.  No peer submission for this round was read.  Two accidental grep hits (`jc2-lean/**`, a possibly post-seal C74 review path) are disclosed above and were not used as evidence.  This report is an independent whole-portfolio scan, not a review of another model's submission.
