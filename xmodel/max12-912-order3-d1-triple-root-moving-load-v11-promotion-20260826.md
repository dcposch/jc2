# Promotion: D1 triple-root moving-load discriminator V11

Date: 2026-08-26

Status: **PROMOTED EXACT CONSTANT-FIELD THEOREM IN THE FROZEN ORDINARY
SOURCE; NO GLOBAL D1 OR JC2 VERDICT.**

## Frozen producer and hostile review

```text
c7d42e2d38fc8ca3f283c768287a87f7e52dba0678aa41412f6be5b9d62db925
  cases/max12_912_order3_d1_triple_root_moving_load_v11_20260826/RESULT.md
466a0665f8c6a69a318a91ccd585517978767da9bf2b0f8b28ad098d1906d13d
  cases/max12_912_order3_d1_triple_root_moving_load_v11_20260826/FREEZE.sha256
c170e8cb2343b20bb70a8d3051b64dba72c482789e3fd8e154d049c1496604ba
  cases/max12_912_order3_d1_triple_root_moving_load_v11_20260826/SOURCE_CLOSURE.sha256
fe00bdbb2d310dbd618b8b3334544ba983c8f7bd108961a28441e568c45e39b9
  xmodel/max12-912-order3-d1-triple-root-moving-load-v11-review-grok-20260826.md
```

The independent text-only review returned
`MOVING_LOAD_V11_CONFIRMED`.  It recomputed all immutable hashes, checked
both 35-file remote manifests, audited all 62 exponent records and all 14
load-sensitive records, compared the 48 load-free records dictionary-wise
with V10, and charged the central-vertex and rescaled-Lambda conclusions.
It found no failing identity and required no source or certificate repair.

## Promoted constant-load statement

In the pinned ordinary-tail eight-row module with charged source SHA

```text
67343b569db54f61f4a9e56e4262c0bf4fa781323a1287addbaf4fff93844623,
```

use the literal reviewed V8 multipliers and put

```text
kbar = Lambda^6*k,
row 3 target = Lambda^15*mu,
row 6 target = Lambda^18*nu,
row 8 target = Lambda^20*(1+tau),
```

where `k,mu,nu` are arbitrary elements of the characteristic-zero constant
field.  Their axis characters are zero.  All eight transported full-load
row covariances hold under

```text
p=-3*a^2,  c=2*a^3+h.
```

The resulting cleared combination has 62 terms and canonical SHA

```text
c0f1552c06650e321db954ab99788324f59e52dea94fd8e7dfbdfbb3931aeab6.
```

Its specialization `(k,mu,nu)=(0,2/3,0)` is dictionary-equal to the
reviewed 48-term V9 witness of SHA

```text
53cf4b656c4415324a74862ec108b902ac8eade22733b2cb3a58cd3e447491e0.
```

The 14-term load residual has SHA

```text
dbd78a81294d473aa043af6e0b0c87ce8050ae3e906a20d07395143d4371d75f.
```

Its parameter counts are `k:13, mu:0, nu:1`.  The absence of `mu` is the
exact identity that the literal row-3 multiplier is zero.  Every load term
has strictly positive closed-corner margin: the minimum is 1, the unique
`nu` term has margin 3, and the `k` margins are `1`, `7/2`, or `6`.  Thus
the old equality face is coefficientwise unchanged for every constant-field
load value.  Its 30 non-target terms still have a positive `u` or `v` slope,
while the distinguished terms remain coefficient-one
`a^3*Lambda^20` and `a^3*Lambda^20*tau`.  The full histogram is

```text
0:32, 1:10, 5/2:11, 3:1, 7/2:2, 5:5, 6:1,
```

and the obstruction list is empty.  The promoted endpoint is therefore

```text
EXISTING_WITNESS_FULL_CONSTANT_LOAD_UNIFORM.
```

## Central vertex and Lambda chart

The reviewed exceptional-support theorem of SHA

```text
a274c8d8e0883ce7606f31f69803a293afb15ad48fe9d401191cac1cdbe1daee
```

identifies the reduced strict coefficient-infinity support with the
common-cubic graph, projectively `P(2,3)`.  V11 verifies that all eight graph
coefficients vanish at `(p,c)=(0,0)`.  This is the affine cone vertex and
the irrelevant point of `Proj`, not a strict coefficient-infinity leading
centre.  This retires the central point only as a projective leading
direction; it does not exclude an arc that must be regraded at a finer
coefficient order.

Every nonzero discriminant-zero projective point has

```text
p=-3*a^2,  c=2*a^3,  a!=0.
```

After the `P(2,3)` normalization, `a` is a unit.  Hence a positive
radial/Rees `Lambda` has `v(Lambda/a)=v(Lambda)>0`.  Nominal
`v(Lambda/a)<=0` is not another live point of this strict projective chart:
it is a finite/`Lambda`-leading order choice, or the selected leading vector
is zero and must be regraded.

## Dual custody

| Host | Registered tag | PID | Order | JSON SHA | Elapsed / RSS / swaps |
|---|---|---:|---|---|---|
| Box03 `98.80.65.144` | `max12_912_order3_d1_triple_root_moving_load_v11_20260826T054500Z_box03_forward` | 168130 | forward | `bbd712444d8b6b3392ed0c2209f99e04f9b8d740b0e7e508a63c3eabdbc93be0` | 6.46 s / 22,340 KiB / 0 |
| r6d `100.26.198.153` | `max12_912_order3_d1_triple_root_moving_load_v11_20260826T054500Z_r6d_reverse` | 235150 | reverse | `0c6aca31fa67b11ad939f015e0786d9e6a72cf1527051a8e437f1bcc23379bcf` | 6.40 s / 22,480 KiB / 0 |

Both workers returned rc 0 with empty stderr, one compiler PASS, `DONE` and
no `FAILED`.  Deleting only `tag` and `order` gives identical certificates
of SHA

```text
a4f7c47b7f57e6dea9cecd89a74d4b7b4ac80a59abad431dfe3a64260b142477.
```

There is no V11 PID-capture erratum and no AppleDouble sidecar.  The remote
manifest SHAs are `ea47f554...` on Box03 and `bb0ac5f6...` on r6d.

## Scope firewall

The cyclic-D1 source used here declares `k,mu,nu` in the constant field, so
the theorem covers every actual load value in this frozen source, including
all zero and special load strata.  Loads that themselves vary as formal or
Puiseux series are a broader deformation problem, not an uncovered value of
the declared D1 constants; no theorem for such moving-series loads is
promoted.

The result is confined to the charged ordinary-tail source, the registered
V8 weight cone, and its strict associated-graded coefficient-infinity
chart.  A regraded higher-order leading problem, a `Lambda`-leading
filtration, another source chart or normal direction, embedded thickness,
global landing/accessibility, D1 as a whole, and JC2 are not claimed.
