# Blind whole-portfolio ideation — Sol 5.6 — 2026-09-06T10:30Z

Status: **ON TIME; JC2 UNRESOLVED.** Frozen basis
`0d39df3c9fd69c939a8420c54d03228b9077777d`, cutoff 10:28Z. I read the
packet, current strategy, archived avenue table 1--46, 10:26Z live state,
relevant current audit entries, every charged recent report, the charged
preflight, and the guardrail. I did not read the coordinator submission, any
other report from this round, any prohibited live report/log, the Sol2TB
report, the local K16 boundary report, or `jc2-lean`; I used no external
premise, AWS, or heavy CAS.

Receipt verification was mechanical, from
`/tmp/jc2-lane.W31QlQ/charged-inputs.list`: 18 manifest rows, 18 prompt
`charged_input` rows, and exactly 18 staged regular files; all 18 SHA-256
checks pass, with no missing, extra, duplicate, case-fold collision, schema,
or order mismatch. The six ledger/archive hashes agree with `packet.md:35-41`.
The packet full SHA is `e42bd85368024cc5f907efd20ef3bf9f8e6e3a04af1f85a6b5e1f11f453904b1`;
its seal independently passes at 11,656 body bytes, body SHA
`db7e289dffdd38e7f8d54ff158e67998bbdfef6ca6be7df142bda5ea9d17c3d6`.

## Executive disposition

**Portfolio recommendation: REDESIGN.** Preserve the all-degree boundary-tree
programme and the honest counterexample route, but move new effort from raw
Gröbner depth to two interfaces: a branch-complete source-to-chart atlas on the
proof side, and a filtered Hamiltonian-cokernel projection of the factored-J
family on the disproof side. Continue already detached, custody-bound solves
only to their existing caps and harvest them exactly. Stop fresh R005 pivot
depth, R050 residue expansion, generic HC4/rank work, and any duplicate broad
sweep. This is not a recommendation to stop the campaign or to infer anything
from a timeout.

The finite chain `24,063 -> 90 -> 64` is a theorem about necessary data only at
`n <= 200`: 44 `u_s=1` and 20 split-class configurations, not pairs and not an
all-degree universe (`APPROACHES.md:10-21`; `AUDIT.md:18055-18065`). Neither a
finite census nor the fixed `(99,66)`/D108 clients can discharge the universal
quantifier.

## Disposition vector, avenues 1--46

Legend: `U=unchanged`, `R=raise`, `L=lower`, `O=reopen narrowly`, relative to
the frozen current programme state, not the archive's historical scores or
closure labels. The archive supplies the avenue IDs only.

```text
01 U  02 R  03 U  04 L  05 U  06 U  07 U  08 U  09 U  10 L  11 U  12 U
13 U  14 U  15 U  16 U  17 U  18 U  19 U  20 U  21 U  22 U  23 U  24 U
25 U  26 U  27 U  28 U  29 R  30 U  31 U  32 U  33 L  34 U  35 U  36 R
37 U  38 U  39 U  40 U  41 U  42 U  43 U  44 U  45 U  46 U
```

Reasons for every change:

- **2 R:** the actual-stabilizer necessity theorem and recovered all-degree
  characteristic criterion make the decorated boundary/characteristic tower
  the only current proof scaffold with an honest universal schema. The raise
  is conditional on explicit source maps; it is not a claim that the 64 rows
  cover all degrees.
- **4 L:** exact finite lifting remains usable only with fixed support and
  exact omitted-row containment with unit multipliers. The primitive
  `(x,x+p^L)` control defeats arbitrary finite-depth survival
  (`small-certificate-gate-fable5-20260906.md:95-140`). No germ-to-polynomial
  bridge has improved.
- **10 L:** the full-primary Ni audit leaves the pairing at `a=0` with
  `deg Q<=2`; for arbitrary `Q`, its extra adjugate equation is an identity and
  the surviving condition is the original Jacobian equation
  (`ni-hc4-interface-astra-20260906.md:82-158`).
- **29 R:** the factored identity turns the new 192-parameter `C` sector into a
  finite-support image problem for the Hamiltonian derivation `D_G`; this is a
  concrete, falsifiable client rather than a universal LND slogan.
- **33 L:** R050's four residue rows are already consequences of full `J`, mix
  ordinary degrees 1--250, show no early-pruning gain, and the exact-fibre
  primitive control does not globalize (`r050-parent-residue-gate-fable5-20260906.md:118-168`).
- **36 R:** complete direct-ideal properness now has a promoted one-way path to
  an actual characteristic-zero counterexample, and the complete physical-J
  family is constructible at 600 variables/1,629 rows. Properness is still
  unknown and a unit would exclude only that family.

## Bottleneck reranking

**Proof side, highest first.** (P1) A uniform, branch-complete map from every
minimal characteristic-zero source pair to a tested necessary receiver,
followed by a structural unit argument on every receiver. The finite 64 are a
regression population only. (P2) For the 44 `u_s=1` clients, replace raw
receiver Gröbner work by a structural theorem: even R005 is unresolved at 307
unknowns and degree 55 (`AUDIT.md:18081-18084`). (P3) For the 20 `u_s>=2`
clients and fixed `(99,66)`/D108 branches, supply the actual-root/source map and
decide the complete nontruncated system; numerical floors do not license the
missing radius. (P4) As a bounded prototype, prove the exact weak K16 product
target `b*B_m*eta_m=0` for every actual `m>=4` and every factor, including the
separate `b=0`, `B_m*eta_m!=0` boundary. This is lower globally because K16
alone is not an all-degree coverage theorem.

**Disproof side, highest first.** (D1) Decide properness of the complete
physical-J family, preferably after the linear-`C` projection in Card 2. A
single exact characteristic-zero field/finite-algebra point is a counterexample;
no source-necessity converse is needed (`full-ideal-counterexample-gate-fable5-20260906.md:40-55`).
(D2) Decide properness of the complete direct core only after repairing and
rebinding the builder; the 466-row subset's nonunit/timeout has no force.
(D3) Search for a nonzero finite Q-algebra witness or a Hensel lift with exact
local containment for every omitted row. A smooth mod-p subsystem alone is
inadmissible.

## Detailed card 1 — strongest proof/falsification attack: constructible-complete source atlas

**Tag: KNOWN algebraic device + NEW connection (provisional history), avenues
2 + 32 + 31.** Replace a single generic gauged chart by a finite constructible
cover. Whenever a normalization divides by a polynomial `g`, register both
`D(g)` with `zg-1` and its complement `V(g)`; recurse until every actual source
point has a branch. On each branch declare a homomorphism from the receiver
ring, in its stated generator order and coefficient field, to the source
coordinate ring/localization and check the image of every receiver generator.
This imports avenue 32's saturation discipline into avenue 2's source routing,
with avenue 31's exact ring-map requirement. It is not `sat()` around a generic
component: the zero complement is compulsory.

**Dependencies.** The promoted actual-centre stabilizer and minimality/two-point
coverage; the printed scalar characteristic recurrence with actual **total**
degrees; the fixed source theorem for each datum; and an explicit gauge action.
The current reverse gate itself lists the unpaid items: stage-8 necessity,
`beta=1`/`jet0=0`, the Prop. 4.6 T3 face, and canonical-resultant compatibility
(`full-ideal-counterexample-gate-fable5-20260906.md:51-53`). No name match is a
map. This card is pointwise over algebraically closed characteristic zero;
nilpotents are irrelevant to source-point coverage, while any later ideal
certificate must retain its own ring.

**Raised OPEN.** `OPEN[UNIFORM-RECEIVER-COVER]`: exact quantity = whether, for
every normalized minimal characteristic-zero Keller pair and every finite
admissible characteristic sequence, the constructible branches above contain
at least one branch with a proved source-to-receiver homomorphism. Cheapest
test = a 45-minute desk falsifier on the frozen `(99,66), delta=2` datum: make
the four-arrow table, list every inverted polynomial, add every zero
complement, and symbolically image-check every row family without running a
solver. Passing this one client is not the universal theorem; failure is a
decisive counterexample to the present routing template.

**Outcome interpretation.** All branches covered and all images zero: the
complete direct ideal becomes a legitimate exclusion receiver for this fixed
datum only; extend by a symbolic induction on tower length, never by finite
sampling. A zero complement survives: emit that branch as an additional chart
or leave it `UNASSIGNED`; a unit on the generic chart cannot kill it. A row
image fails: keep the direct ideal solely as a one-way counterexample family.
Missing route-to-state data: stop and return typed OPEN, not a guessed map.

**Gate/time price.** One source specialist, <=45 minutes desk work for the
fixed-client falsifier; different-model source/page gate before any exclusion
claim. No CAS or fleet is needed for this test.

**Stop condition and information gain.** Stop after the first unfillable
source arrow or after the fixed datum passes; do not expand to 44 receivers
until a uniform induction statement is written. Information gain is high:
it decides whether current large ideals have any proof-side value and exposes
exactly which complement is missing, without waiting for a Gröbner result.

## Detailed card 2 — new mechanism and strongest disproof attack: filtered Hamiltonian cokernel

**Tag: NEW mechanism (provisional history), connection avenues 29 + 36.** In
the factored physical family, put

```text
A=(3D+a+b*h)/2,       G=h^2-b*h/3+D.
```

Then the charged identity contracts further, exactly, to

```text
J(F,G) = A*J(h,D) + J(C,G).
```

Thus, after fixing the non-`C` source coordinates, the counterexample problem
is not initially a 600-variable nonlinear Gröbner problem: it asks whether the
prescribed polynomial `j-A*J(h,D)` lies in the image of the Hamiltonian
operator `D_G:C |-> J(C,G)` on the **specific 192-dimensional source support**,
with `j!=0`. This is the finite-support cokernel version of avenue 29's
derivation obstruction, now used as an avenue 36 construction test.

There is an exact constant-symbol reduction. Set `p=y`, `q=y-x`; then
`J(p,q)=1` and the fixed top is

```text
G_66=p^18*q^48=(p^3*q^8)^6.
```

For a degree-`d` monomial,

```text
J(p^i*q^(d-i),G_66)
  = 6*(11*i-3*d)*p^(i+17)*q^(d-i+47).
```

In characteristic zero the symbol kernel through `deg C<=35` is exactly
`span{1,r,r^2,r^3}`, `r=p^3*q^8`; every other monomial has a nonzero scalar
pivot. Since the charged preflight establishes 192 linearly injected `C`
parameters, with identity coefficient slots and disjointness from `h,D`
(`preflight.py:24-32`), an exact occurrence/module check can determine whether
at least 188 directions eliminate by degree-descending scalar pivots, leaving
at most four resonant modes. This is a theorem about the graded symbol, not yet
about the complete source module: occurrence in `a,b`, lower-degree coupling,
all rows, and closure after substitution must be checked mechanically.

**Ring and dependencies.** After the pending different-model factored-J gate,
let `B=Q[z]` contain every non-`C` source variable and the 160 monic `h` lifts.
Adjoin the 192 `C` coordinates and a scalar `j`. All 1,469 Jacobian coefficient
conditions have the form

```text
M(z)*(c_1,...,c_192,j)^T = -r(z),
```

where the `j` column is minus the constant-monomial unit vector; retain the
open `D(j)` by `Zj*j-1`. The actual matrix is `1469 x 193`, with a 194th
augmented column. Scalar-Q pivots preserve the quotient scheme, including
nilpotents. Any later nonconstant pivot must be split into its `D(g)` and
`V(g)` branches; generic rank alone is never an exclusion. The family omits
T2/T3/localizer variables by design, so a unit here excludes only this frozen
family, while any characteristic-zero point is already a counterexample
(`factored-jacobian-pilot-astra-20260906.md:91-125`).

**Raised OPEN.** `OPEN[LINEAR-C-COKERNEL]`: exact quantities = (i) the rank of
the actual 192-direction `C` module under the filtered symbol of `D_G`, (ii)
the number and literal term count of the resulting compatibility rows after
all scalar pivots, and (iii) whether any exact-Q base specialization admits a
solution with `j!=0`. Cheapest test = <=30 minutes: occurrence check for
`a,b`; exact extraction of the filtered sparse matrix; scalar-pivot closure;
three deterministic small-rational rank/consistency evaluations plus modular
telemetry only as a cross-check. No stock msolve: its signed-32-bit offset is
known to overflow on the 11.3M-term input
(`factored-jacobian-pilot-astra-20260906.md:255-257`).

**Outcome interpretation.** An exact rational base assignment with a
consistent solution and `j!=0`, followed by independent evaluation of all
1,469 rows and reconstruction of `F,G`, is an actual rational counterexample.
A mod-p solution is only a signal; lifting requires the full finite-algebra or
exact-local-containment contract, so `(x,x+p^L)` remains a blocking negative
control. Generic inconsistency gives explicit cokernel equations confining the
base locus, not emptiness; every rank-drop complement remains. If the only
solutions at a tested base have `j=0`, that base is rejected, not the family.
An exact unit after exhaustive complementary branches excludes only this
physical family.

**Controls.** The highest-J face is already an identity
(`preflight.py:37-45`), so it must reduce to zero and cannot be advertised as
the first obstruction. The frozen `rho=1`, other-parameters-zero source has
all positive J rows zero but `Zj*J0-1=-1`; the projected system must reject it.
Add a small affine `J=1` positive operator control and a deliberately vanished
pivot with both branches. These controls prevent top-face, missing-constant,
and generic-only false wins.

**Gate/time price.** One exact sparse-emitter lane, <=30 minutes for the
discriminator; then one different-model algebra/map review, <=30 minutes.
Only if projected terms are at least threefold below 11,299,180 and the first
nonautomatic compatibility band is explicit should a separately authorized
solver pilot be considered.

**Stop condition and information gain.** If the occurrence audit verifies that
all 192 source directions enter only through `C`, stop if their actual module
rank is below 188; otherwise replace 188 by the proved symbol-rank floor and
reassess. Also stop if scalar elimination fails closure, projected fill exceeds
about 3.8M terms, or no nonautomatic compatibility row appears before that wall.
In the last case, stop elimination and try one exact reconstruction rather than
launching a solver. Do not repair failure with a generic saturation. A pass
either exposes a tiny resonant construction space or a concrete base
obstruction; both sharply separate counterexample-friendly geometry from
another representation-only speedup.

Contract crosswalk: Card 1 is the strongest proof-side attack and a fixed-client
falsifier; Card 2 is the genuinely new mechanism, strongest disproof attack,
and decisive exact-software experiment (the filtered sparse emitter). Card 2's
use of avenue 29's Hamiltonian obstruction as an avenue 36 construction and
elimination method is the required new connection. Card 3 is the separate
systems upgrade.

## Major-work decisions

| Work | Decision | Exact boundary |
|---|---|---|
| Protected full2TB complete-direct build/solve, `.63` subset solve, `.73` K7 `b9 q0` | **CONTINUE** | Existing custody and caps only; verify the complete input and exact-Q output at harvest. A timeout/nonunit subset is no verdict. Do not launch a duplicate. |
| Live independent factored-J gate | **CONTINUE** | Review only. No solve or representation promotion before its terminal replay; this report does not consume its live result. |
| Factored-J after gate | **REDESIGN** | Run Card 2 before a raw Gröbner solver. A point is disproof; a unit has frozen-family scope only. |
| Direct T2/T3 proof route | **REDESIGN** | First repair/rebind the full stream and run Card 1's source-map falsifier. Keep `t3eq`; it occurs in `T3_strict` (`AUDIT.md:18111-18117`). |
| Class-A receivers | **REDESIGN** | Seek a uniform structural receiver invariant. Do not port R005 pivots to 44 clients. |
| K16 | **CONTINUE one existing bounded specialist; REDESIGN the next question** | Target only `b*B_m*eta_m`, factorwise for every actual `m>=4`, plus the separate `b=0`, `B_m*eta_m!=0` boundary. Do not seek a fixed radical exponent, a leading-balance cutoff, or infer all `m` from `m=4,5`. |
| R005 deeper substitution | **STOP** | The minimum is at 11 pivots; 54 pivots reach 17.85M terms with no basis. Reopen only if an incremental implementation reverses term growth in a controlled one-solver comparison. |
| R050 residue/e=28 expansion | **STOP** | The e=28 residue is the negative of the e=4 residue; no new condition or measured pruning. Add the four rows only if a complete R050 source chart is built for another reason. |
| Relative-rank and unrestricted Ni/HC4 lanes | **STOP** | Rank 2 is automatic on the genuine locus; arbitrary-Q pairing is the original JC2 equation. |
| External sweep | **CONTINUE ON CLOCK, STOP NOW** | Do not repeat before the Sep7 09:23Z covered-channel backstop; preserve the older X/Bluesky/Zulip gap clock. |

## OPEN register and novelty checksum

- `OPEN[UNIFORM-RECEIVER-COVER]`: exact quantity = universal constructible
  coverage, with a checked source-to-receiver map on at least one branch for
  every normalized minimal pair and admissible sequence. Cheapest test = the
  45-minute `(99,66), delta=2` branch-table falsifier in Card 1. **NEW
  connection / KNOWN ingredients**, provisional until the final post-collection
  history checksum.
- `OPEN[LINEAR-C-COKERNEL]`: exact quantities = actual 192-direction module
  rank, surviving compatibility-row count and term count, and existence of an
  exact-Q base solution with `j!=0`. Cheapest test = Card 2's <=30-minute
  occurrence/extraction/scalar-closure discriminator. **NEW mechanism**,
  provisional; it is not the old renamed full-J ideal, because its discriminator
  is the four-dimensional graded Hamiltonian kernel and exact scalar-pivot
  quotient.
- `OPEN[FACTORED-J-PROPERNESS]`: exact quantity = whether the complete frozen
  physical-J ideal over Q is proper, equivalently whether it has a geometric
  characteristic-zero point. Cheapest test = Card 2; acceptance requires exact
  evaluation of every original row in a nonzero finite Q-algebra. **KNOWN
  target / NEW proposed reduction.**
- `OPEN[K16-PRODUCT]`: exact quantity = for every actual `m>=4` and every field
  factor of `Q[d]/(3d^2-m)`, whether
  `V(K_m) intersect D(b*B_m*eta_m)` is empty, together with nonexistence of
  `(A,W)` satisfying `(B-UF)+(B-JL)` on `b=0`, `B*eta!=0`. Cheapest test now =
  harvest and exact-check the already-running bounded product/boundary
  specialist, requiring no duplicate computation. If it does not decide, do
  not repeat the exhausted full `m=6` basis attempt; require a new bounded
  mechanism before further compute. **KNOWN**, and deliberately weaker than `eta_m=0`
  (`k16-ueta-classification-astra-20260906.md:290-319`).

Items rejected by checksum: generic Hensel/compactness without containment is
**DUPLICATE/REFUTED BY CONTROL**; R050 residue as a global mate is
**SCOPE-CONFLICT**; relative rank and arbitrary-Q Ni pairing are
**SCOPE-CONFLICT**; fixed K16 powers, leading balance, lambda-only localization,
and more transported descent are **DUPLICATE CLOSED MECHANISMS**. Incremental
R005 regeneration is **KNOWN software**, not a new mathematical route.

## Detailed card 3 — systems UPGRADE (separate from mathematical software)

**Tag: UPGRADE proposal; not implemented in this lane.** Add an explicit-path
round-report contract checker. It may read only the one report path and its
charged receipt--never scan `xmodel/`--and should verify: exactly one terminal
standalone BODY-END; IDs 1--46 each occur once in the disposition vector; no
more than three detailed cards; each typed OPEN declaration has a nearby exact quantity
and cheapest test; and the charged-input manifest is complete and hash-clean.
This targets ingestion omissions without touching claim truth.

**Dependencies and cheapest discriminator.** No adapter change is needed for
the prototype. After blind collection, invoke a read-only parser separately on
each of the four collected report paths, pairing each invocation with the same
explicit packet/receipt paths, <=20 minutes total; then compare its findings
with manual ingestion. It must never discover inputs by directory scan.

**All outcomes.** Zero false negatives and at most one explainable false
positive: gate a separately reviewed implementation. A missed malformed OPEN,
wrong avenue count, or accidental read outside the explicit paths: reject the
upgrade. Merely restating the existing hash check with no contract defect
caught: record `NO_CHANGE` and delete the proposal.

**Stop condition, price, information gain.** Stop after five fixtures or one
outside-path access; budget one 20-minute systems lane and no research block.
Expected gain is lower report-to-ingestion latency and fewer scope omissions,
not mathematical progress. No shared tool, adapter, or ledger is edited by
this report.

## Scope and fallacy audit

All theorem-bearing statements above are characteristic zero. Modular work is
telemetry only. Card 1 distinguishes source-to-receiver necessity from the
reverse point-to-polynomial disproof map and covers every localization by its
closed complement. Card 2 uses scalar-unit eliminations in the declared Q-ring;
it preserves nilpotents, while generic rank samples prove nothing. Exact total
degree is never inferred from a floor, and no finite result is extrapolated to
all degree. The 600-variable family is not asserted equivalent to T2/T3 or
necessary for a source pair. No pole identity, raw remainder, derivative/prime
ambiguity, or carrier/attainment substitution is used. No new exit-price
assertion is made, so no `charge_basis=` line is licensed.

<!-- BODY-END -->
