# Promotion: fixed-`p=0` residual `A` chart, `C` contact one

Date: 2026-08-26

Status: **PROMOTED EXACT SOURCE-TYPED CONTACT-RAISING THEOREM AT THE NAMED
FIXED COLLISION CONE.**

## Frozen chain

```text
162a279615931f93bd15e3ea176e46c30669449cae9e35dd41305d174559fd23
  cases/max12_812_order2_p0_a_lowcontact_c1_20260826/RESULT.md

958dd6b3336d024f3b575420413ffa26b0b7811a6972a3e955c9ea2804810de1
  cases/max12_812_order2_p0_a_lowcontact_c1_20260826/RESULTS.sha256

c0b77f7effb05ec973ce367477246d6f42ca035099553a0732392064eb4059d4
  cases/max12_812_order2_p0_a_lowcontact_c1_20260826/FREEZE.sha256

da5501e953d94e10609ce102dc0de7e2b04ede88b188d6d88ac3b03d82606de9
  xmodel/max12-812-order2-p0-a-lowcontact-c1-hostile-review-grok-20260826.md
```

The different-model hostile review ended `CONFIRMED`.  It independently
reconstructed the exact-Q rows, audited the Faber indexing and correction
census, checked both raw chart deductions, verified independence from every
retained first `R` correction, and confirmed that the generic V12 and
unbounded fan are not consumed.

## Promoted theorem

In the complete seven-row ordinary source, after the sharp third-tail gate,
work on the fixed collision fibre `p=0`.  Set the leading `R,C` coordinates
and every moving-`p` jet to zero, retain an arbitrary first `R` correction,
and write

```text
A0=a1*z+a0,
C=sigma*(e1*z+e0)/2+O(sigma^2).
```

The complete absolute grade-eleven rows are exactly

```text
g11_2=(3/8)*a0*e0,
g11_1=(3/8)*(a1*e0+a0*e1).                         (1)
```

On `D(a0)`, (1) is raw unit-triangular and forces `e0=e1=0`.  On
`V(a0) intersect D(a1)`, the second row forces `e0=0`; after only that raw
equation, the complete grade-twelve row is

```text
g12_2=(3/32)*e1^2.                                  (2)
```

Thus `D(e1)` is empty and every DVR arc in either leading-`A` chart raises
`C` contact beyond one.  Equations (1)--(2) are symbolically independent of
the arbitrary retained first `R` correction, so the statement covers every
`ord_sigma(R)>=1`.  The conclusion is arcwise/contact-raising; it does not
replace `e1^2=0` by `e1=0` as a scheme-theoretic identity.

The source emitter retains all seven tails, all available corrections,
`k10,k6,k2`, and all targets.  Exact source grading proves that the later
loads and targets do not enter grades eleven or twelve.  Exact Q is the
characteristic-zero theorem lane; `F_65521` is an independent software
control.

## Firewall and successor

This theorem is restricted to fixed `p=0`, leading `A!=0`, `C` contact one,
and `R` contact at least one.  It does not cover `C` contacts two or three,
`R` contact zero, the mixed `R`-contact-one tangent sheets, the high-contact
cone except through its separate reviewed theorem, a total unspecialized-`p`
Rees map, positive-valuation moving `p`, the all-zero Pell/Chebyshev
receiver, `k10=0`, the whole square branch, order two, `(8,12)`, maximum
twelve, or JC2.

The immediate exact successor is the finite correction-aware shard list in
`xmodel/max12-812-order2-p0-cech-lowcontact-pell-total-rees-successor-design-20260826.md`,
with moving constant-`A` corrections routed before the permanent tangent
module is used.
