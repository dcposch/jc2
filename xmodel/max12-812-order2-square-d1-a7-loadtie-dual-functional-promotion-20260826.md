# Promotion: D1 `a=7`, `d=2,3` load-tie tails

Date: 2026-08-26

Status: **PROMOTED EXACT THEOREM AT THE NARROW SCOPE BELOW.**

## Frozen authority

```text
bb662b3d25cd12dec712dc2763e1efd6dd09b1dd084babce65bcb9effb0daac0
  cases/max12_812_order2_square_owner_d1_a7_loadtie_dual_functional_20260826/PRODUCER_FREEZE.sha256
69048f6fd2f3ddf78e8328840d0400fe29d523c5258fbd5c154b211559c433aa
  cases/max12_812_order2_square_owner_d1_a7_loadtie_dual_functional_20260826/RESULT.md
1ae7f6d95b7d707b3fe54b07a59fbd103435d13b4a0a764b6495215785d26530
  xmodel/max12-812-order2-square-d1-a7-loadtie-dual-functional-hostile-review-grok-20260826.md
```

The independent Grok 4.6 hostile review rehashes every producer/source/
evidence row, independently enumerates the complete primitive inventories,
rederives the rank jump and both root functionals, and ends `CONFIRMED`.
It finds no mathematical or scope defect.  Its nonblocking observation that
the generated `Psi_pm_exact` quotient flag is not multiplied into the final
endpoint does not weaken the theorem: the independently verified source
valuation is at least `G`, and every charged coefficient identity passes.

## Promoted statement

After the frozen square/D1 source gates, in characteristic zero on
`D(p*k0)`, the seven literal Faber equations have no point in either strict
closed contact tail

```text
ord(A)=7, ord(C)=9,  ord(R)>=7,   (d=2),
ord(A)=7, ord(C)=10, ord(R)>=7.   (d=3)
```

At the first wall, the complete tied column is
`(3/4)C(A+k60)/L`.  Its multiplication matrix has determinant
`Delta_C=c0^2+(p/2)c1^2`; the off-determinant chamber forces
`A+k60=0`, while the rank-one face is retained.  The complete source through
the terminal grades 28 and 30 has pole ceiling two and sole pole-two column
`(3/8)C^2/L^2`.  On the finite etale root cover, the two functionals

```text
Phi4 +/- lambda*(Phi3+(p/4)*Phi1),  lambda^2=-p/2,
```

annihilate all pole-one columns and give the terminal square pair
`(3/8)(+/-c1*lambda+c0)^2`.  Those squares generate the unit ideal on both
exact `C` charts before radicals, including the rank-one chamber.  Rows
1,3,4 are target-free through both terminal grades, and leading `R` is not
inverted, so emptiness covers the full closed `ord(R)>=7` tails and descends
to the unsplit chart.

## Firewall

This promotion contains exactly the two cells displayed above.  It does not
include `a=8,9`, a row-2 or row-4 target-shadow chamber, an equality face, a
positive-order leading `k10`, `p=0`, `k0=0`, another D1 face, a terminal or
global chart, the square component, order two, `(8,12)`, maximum twelve, or
JC2.  No timing or emptiness claim may be extrapolated to `a=8,9`.
