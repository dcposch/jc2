# Four-model ideation synthesis — round `20260829T0250Z` — Fable 5

Coordinator-synthesis lane. Producer: Fable 5 (exact `claude-fable-5`),
written `2026-08-29T03:23Z`. Inputs: the sealed state packet and the four
blind lane reports of this round, in full, and nothing else. The excluded
sibling `xmodel/ideation-20260829T0250Z-grok46.md` was not opened. No
canonical file was edited; no commit, push, AWS action, web access, or heavy
local computation; the user-owned formalization tree was not touched in any
mode. `COORDINATION.md` was consulted only to fix promotion/coordinator-block
nomenclature (its own §"Promotion and adversarial review" and the ideation
round protocol). Bare `G2` is not used below.

## 0. Custody — verified inputs and preserved evidence tiers

### 0.1 Hashes verified this session (all byte-exact)

Six canonical roots: recomputed, all six match the packet's charged values
exactly (`COORDINATION.md 99a4d735…`, `APPROACHES.md 2e0e8b9b…`,
`AUDIT.md c611a519…`, `PROGRESS.md 5c7efc87…`, `notes.md 1e8e4646…`,
`ladder/REDUCTION.md 0a88db0d…`).

Round inputs, full-file and body seals:

```text
3707e151584f73969e4c09827bea3bc739fe82f59c192553605a47d276d6e6b6  state-packet (full)
4c17371550d628f6fa33fe29b6d2fd93a5b1c8b180cf45402bdb5df4b62b9c20  fable5 (full)
5b90f45745f00dfc1b01c9838c82a1b396b6cdae305c789f2214658e0a6a00db  fable5 (body, first 30827 bytes) VERIFIED
a5aa5376a88ac1b80df34099cb24123258655eab683994ce152bef86e40693e4  opus5 (full)
82d84021f34fc88f0075de81528ef27476e68afce93e7e8196449912b89fa760  opus5 (body, first 53533 bytes) VERIFIED
9aebaab2eca09f80ea69a26404a73b845ac49d685a58b0d0908358fea89e8a00  sol56 (full)
ecbcd993f04a448e33315e90aa904dfa380dc42d448306be75458c8d1ec2d9af  sol56 (body) VERIFIED
2854e41be313fc3f0f1ac4826140663d98ebf75a616f1e3c21a8bb03337d2a42  grok46-rerun (full)
eea80419ae3ad1db2ed34cc5ea766abc4c90009bb196f0a6d9a601b76dccb8b3  grok46-rerun (body, first 23965 bytes) VERIFIED
```

The sol56 report prints its body hash without a byte count; the unique
matching prefix was determined constructively at 26984 bytes (through the
blank line after its end-marker) and verified. All four reports attest blind
production and independently attest the same packet hash.

The two provisional evidence roots
(`m2-td8-prop81iv-parametric-primary-grok46-20260829.md` `2d9fa6fa…` and
`m2-equal-join-semilinear-primary-research-opus5-20260829.md` `7ed65bc2…`,
body `bacf0d6b…`) are **outside this synthesis's read set** and were not
re-opened; their hashes are attested identically by all four lane reports.
That is quadruple cross-attestation, recorded as such, not as an independent
verification by this lane.

### 0.2 Evidence tiers — the distinction every claim below carries

- **PROMOTED / independently reviewed** (packet authority): cap-free reduced
  P0 chain theorem (two different-model reviews); K0 degree-432 field with
  the finite content ledger and ARITH-SPREAD only (two-frame nonemptiness
  REFUTED); ACS connected finite-etale cover + reducible-or-singular
  rigidity (Euler ceiling REFUTED, inertia untyped); LL-1 `td=6,m=2`
  mathematics at LL1 scope (provenance R3 active); Sol's `td=8,m=2`
  equal-join route at recorded-book scope — formal-cell infinitude, one
  reduced successor, recorded lambda **floors** meeting budget at equality;
  Fable's refutation of the incoming-index theorem (only the two-pole
  merge-local core survives; `NUCAP=500` stays).
- **PROVISIONAL, under active hostile review**: Grok's parametric local
  Prop. 8.1(iv) theorem (Fable review active); Opus's semilinear U1/U2
  report, Theorems A–E and the `td=12,m=3,[2,2,2]` witness (Grok review
  active). Opus Theorem F is **known-defective** inside the latter (cites
  the refuted incoming-index theorem) and is consumed by nothing below.
- **CONDITIONAL**: anything derived from the provisional tier (orbit
  fusion, the transport equation, `UNIFORM-TREE`, Sol's U2 direct-terminal
  lemma). Falls with its foundation; every launched card carries that stop
  condition.
- **BLIND OPINION**: the four lane reports themselves. Convergence between
  them is a scheduling signal, never promotion; a shared premise from the
  same packet is correlated input, not four votes.

Standing scope rule inherited from all four reports and enforced on every
item below: **no Keller map, germ, characteristic-zero point, degree
ceiling, landing statement, or JC2 inference is drawn from any formal cell
or family**. Alive ≠ existent; formal cell ≠ germ ≠ polynomial.

## 1. The double-count firewall (adjudicated first, adversarially)

**One law, two derivations — never two results.** Grok's parametric solve of
printed Prop. 8.1(iv) on the reviewed `td=8` affine family is the
`(r,mu,eps)=(2,3,0)` instance of Opus Theorem E: `sigma=0` (opposite orbit
bases) *is* `Rad(T)=T^2-A`. All four lanes assert or accept this
identification (Opus §3.1 "one fact with two derivations"; Grok §3 as an
explicit `NEW` identification; Fable §1.3 "agree in shape"; Sol treats it as
one transport target). The synthesis therefore charges the campaign with
exactly **one provisional law — the U1 merge rigidity law** — with two
derivations under two separate active reviews. Consequences:

1. A pass of *one* review promotes the law only at that derivation's scope
   (`td=8` local-pattern scope for the parametric solve; general
   `(r,mu,eps)` for Theorem E). The agreement between them is a consistency
   datum with a shared input (the same printed Prop. 8.1(iv)), not
   independent confirmation.
2. The identification itself gets a mechanical audit: `ORB-CENSUS` pass 0
   (§4, action A5) is Grok's §5.1 coefficient-wise match of T1-EQJOIN at
   `(2,3,0)` against the `td=8` solver, with the frame-`rho` mutation
   control. After it runs, no ledger can double-count the law even by
   accident.
3. **Composition hazard, carried forward verbatim**: the frame ratio
   `rho=2/3` and the ODE ratio `rho=6nu/(2nu+1)` are different objects; any
   successor composing T1-GEN with the parametric solve must carry Grok's
   Prop. 8.1 exponent pin `i=14` (St. 3.17(i)); the `i=7` handshake quotient
   corrupts full-degree and `D` bookkeeping (Opus §9 flag, Grok's report).
4. **Floors are not costs.** The slack-0 (`td=8`) and slack-1 (`td=12`)
   budget figures use recorded lambda *floors*, promoted at recorded-book
   scope. Nobody may cite them as exact costs (Grok: "do not upgrade
   recorded floors by proclamation"); action A1 exists to price them.

A second, smaller double-count found and resolved: Fable's §6 claims its
entry emitter would "settle the one factual unknown" of the `td=12` witness
(global type "derived, not read"), while the sealed packet and Opus §9 state
the exact current entry census already confirms the global `(2,3)` row. The
packet is authority: the row is confirmed; the emitter's value is downgraded
to fail-closed regression re-emission, which is still wanted (A6).

## 2. Consensus/disagreement matrix — all changed avenue dispositions

Legend: `U` unchanged, `R` raise, `L` lower, `O` reopen; `n` = narrow;
`U*` = unchanged tier with recorded internal re-weighting. 29 of 46 avenues
were left unchanged by all four lanes
(5, 7, 8, 10–15, 17, 19–24, 29, 32–34, 36, 37, 40–46); the 17 rows moved by
at least one lane:

| # | Avenue | F | O | S | G | Reading and resolution |
|--:|---|:-:|:-:|:-:|:-:|---|
| 1 | GGV / degree farm | U | U | U | R | G-only relative raise (finite discriminators gain value against a non-Noetherian full-cell book). Record as a note; no allocation change — the closed successor stays custody-gated (TRIPLE02 R3). |
| 2 | Sigray sheet ladder | R | U* | R | R | **Consensus in substance (4/4)**: principal avenue; internal allocation moves from degree bookkeeping to the coefficient tier; endpoint object re-typed to labelled family records + finite residue (§3-M8). Opus's `U*` is presentational (already principal). |
| 3 | Vertex-gap / strip ODEs | U | R | R | R | **3 raise**; F folds the identical content into avenue 2. Adopt: raised *as mechanism supply for the merge frontier*; its own endpoint scope stays limited. No substantive dissent. |
| 4 | Germ certification / algebraization | Rn | Rn | U | U | **2–2 split, resolved conditionally.** F/O raises are already conditional on transport compatibility; S ("cell ≠ germ") and G (algebraization is the disproof wall, not raised) hold. Adopt: **no pre-arming**; avenue 4 arms iff A2 returns compatible. |
| 6 | AM one-place | U | U | U | Rn | G-only typing raise (AM is the right test on `A(F)` components, which remain unconstructed). Record; no launch. |
| 9 | Lee–Li | U | U | U | L | G-only lower; restates a low tier. Adopt. |
| 16 | D-module / holonomic | U | U | U | L | Convergent: G's lower restates the standing post-HENS-CT lowered tier. Adopt. |
| 18 | Graded / equivariant | On | On | U | U | **2 reopen, identically firewalled** (finite-cyclic `Z/r` receiver; not a CE hunt; no Shaska contradiction; the local→global bridge does not exist and is the first thing to type). Adopt at typing scope, zero standing allocation. |
| 25 | Monodromy / dessins | U | L | U | L | Two lowers (O withdraws own prior raise; TDIC `NOT-TYPED`), none raises. Adopt lower. |
| 26 | Primitive-monodromy td | U | L | U | U | O self-withdrawal; the new `Z/r` block hypothesis is carried *inside avenue 2*, and explicitly does not unstop the census (no support bound). Adopt. |
| 27 | Links at infinity | U | L | U | U | O self-withdrawal of the coupled bet. Adopt. |
| 28 | Log surfaces / BMY | U | L | L | U | Two lowers on the signed Euler identity; two independent negative accountings now on record. Adopt lower; `ACS-CEIL` stays stopped. |
| 30 | Affine classification / ML | U | L | U | U | O self-withdrawal. Adopt. |
| 31 | Integrality / ZMT | U | U | O | Rn | S/G both point at the ACS cover as the nearest-typed object avenue 31 ever had (normalization/conductor over a necessarily reducible-or-singular `A(F)`); F/O note the row already sits at a raised narrow setting. Adopt: typing raise recorded, launch gated on component-labelled data that do not exist (same debt as rows 25/39). |
| 35 | Dim-3 descent | U | U | U | L | G-only; van Dobben already killed the naive route. Adopt restatement. |
| 38 | Tropical | U | U | L | U | S withdraws its own prior rank/lasso raise; direct semilinear/divisibility arithmetic dominates. Adopt. |
| 39 | Cohomological cluster | U | U | O | U | S-only reopen (`ACS-FOX` names the first concrete class). Bank the mechanism (§3-M7); the reopen itself is gated on the identical missing component-labelled packet, so no row change until avenue 7's construction debt is paid. |

**Unanimous negative space (as load-bearing as the raises):** none of the
four raises 25–28/30 (last round's coupled topology-at-infinity bet is
closed, by its own proposer); none proposes any numerical `kbar`-cap
campaign — F (`NUCAP` stays), O (Theorem A, if it survives, says no cap can
exist on U1), S ("a larger `kbar`/`nu` cap is not a theorem"; "never
enumerate to a cap"), G ("do not seek a numerical `kbar` bound").
**Confirmed: the four reports provide no new valid reason for a `kbar`-cap
campaign, and none is launched.**

## 3. Idea merge — one fingerprint per idea, labels explicit

Fingerprinting by target obstruction × mechanism × object × decisive test
(protocol step 4). Merged items M1–M10; unique mechanisms preserved inside
each merge.

- **M1 — U1 merge rigidity law** (`Rad(T)=T^r-A` / `sigma=0` / opposite
  orbits). One provisional law, two derivations (§1). Opus's orbit-fusion
  reading (nonzero roots = one `mu_{r*nu}`-orbit) and the gauge-free
  transport equation `e_j({c_e^nu})=0` are **NEW** mechanisms on it; Fable's
  ratio form `(c_e/c_f)^{nu_G}=zeta_r^{e-f}` is the same equation at `r=2`,
  independently derived — merged, counted once, the convergence noted as
  consistency only. Status: PROVISIONAL.
- **M2 — `TD8-TRANSPORT-GATE`** (merge of Fable `RATIO-TORSION`, Opus
  `SIGMA-ZERO`, Sol `U1-CHAR-TOR`, Grok cyclic-base gluing — four workups of
  one experiment: can the two `td=8` arrival invariants be opposite?).
  Target **KNOWN** (both provisional roots name transport as next);
  merged mechanism **NEW**. Unique components preserved: (F) the
  gauge-typing pre-gate — common `C*` vs `(C*)^2`, decidable from printed
  St. 3.9 + promoted L3–L5 alone, so it runs before any provisional theorem
  is consumed — plus the K0 character-certificate torsion oracle; (O) the
  executable single-equation form with the **identical-pole kill branch**
  (`c_1^nu = c_2^nu` forced ⇒ family dead in one line) and
  valuation/reality handles; (S) the Smith-normal-form monomial decision,
  the derive-then-classify exponential-recurrence fallback (no `t`-recurrence
  is ever *assumed* across family members — Sol correction 3 is a standing
  rider), original-identity replay, and the six-outcome taxonomy including
  `SCOPE-CONFLICT`-as-outcome; (G) the `ominus`-constant-level St. 3.9
  formulation, the `t=0`-cell-plus-uniform-identity economy, and the
  firewall that this is **not** `SUBST-TRANSPORT`/TEMPLATE E4–E6
  (residue-A object — `SCOPE-CONFLICT` if identified). Promoted `td=6`
  OBSTRUCTION O (`2±sqrt(3)` non-torsion) is the planted control (F, O).
- **M3 — `U2-CLOSE`** (merge of Fable `U2-HEIGHT`, Opus `U2-DESCENT`, Sol
  `U2-MOBIUS-TAIL`, Grok log-residue). Target **KNOWN** (Opus's own prior
  §10 item 1; packet-named regime); **four genuinely distinct NEW
  mechanisms, all preserved** because they attack different faces:
  (S) Möbius/direct-terminal elimination — a convergent nonconstant integer
  sequence is eventually constant, so infinite U2 survivors must use a
  downstream P0 step [conditional on the U2 algebra, which F notes is
  rederivable from R2.1 case I without consuming the provisional report];
  (F) the budget-linked denominator law `den(w_t) | M_t <= M_entry*2^B`
  with the adversarial bounded-price `den`-shrinking search; (O) descent-cost
  growth in `num(w_tr)` with the `w_t<1`-terminal vs `w_tr>1`-child
  observation; (G) the D9-style logarithmic residue polynomial in `l` —
  the only mechanism fully independent of the ODE review. Calibration
  instance for all: the recorded `td=7` P4 closure.
- **M4 — `UNIFORM-TREE`** (Opus §3.2 conjecture ∪ Fable §4.4
  `U1-SYMMETRY`). One conjecture: all-U1 configurations have conjugate pole
  subtrees, `td=m*Lambda`, hence composite `td`; Fable's pole-direct
  special case and avenue-18 descent-receiver framing preserved; Opus's
  honest gap (equal arrival does not by itself force equal `Lambda_i`;
  deviation is priced in lambda) and census discriminator preserved.
  **NEW**, with **two SCOPE-CONFLICT firewalls carried verbatim**: it is
  not Moskowicz's refuted "no prime td" (conditional, all-U1-only, silent
  on `m=1`), and the `Z/r` block does not unstop the primitive-group
  census. Status: CONDITIONAL on M1.
- **M5 — instrument suite** (Opus `ORB-CENSUS` ∪ Grok §5 three-identity
  desk packet ∪ Fable `EQJOIN-FAMILY/v1`+entry emitter ∪ Sol
  `FAMILY-CONSUMER/v1`). **NEW as instruments.** Pass 0 = the §1
  double-count audit (G §5.1). Fusion-index pass with planted controls
  (`td=8` → `2nu`; `td=12` → `3nu`; `td=6` `lex=1` cell → `nu` and
  OBSTRUCTION O reproduced; perturbed `e_1 != 0` rejected). Conjugacy
  census = M4's discriminator. Record schema: union of Opus hash/period
  rules and Sol's coefficient-character + consumer-list fields; entry
  emitter with Fable's fail-closed G-a–G-e gates (role: regression
  re-emission per §1's second finding). All refuse cap tokens and any
  incoming-index dependency (S/G firewall).
- **M6 — trunk-vs-rigidity** (Grok card 3). **NEW connection, unique,
  unmerged**: the `(85,35)` trunk ODE as the first *post-merge* coefficient
  consumer of `b=-a`; complementary to M2 (upstream), can kill the family
  without transport ever being typed.
- **M7 — ACS follow-ons, banked not launched** (Fable §4.3 ∪ Sol card 3).
  Two distinct **NEW** statements sharing one blocked prerequisite —
  avenue 7's component-labelled horizontal finite-end packet, which does
  not exist: (F) unique image-carrying boundary divisor ⇒ `A(F)`
  irreducible hence **singular** (paper-level corollary of the promoted
  rigidity; uses only the component count, not inertia — not `TDIC-SET`);
  (S) `ACS-FOX` Shapiro identity
  `#comp(F^{-1}(A(F))) - #comp(A(F)) = dim H^1(Y,V) >= 0` plus the
  cover-first disproof direction (reverses the books→`A(F)` flow). Both
  banked with the same gate; Sol itself concedes current data do not meet
  the dependencies. `KNOWN`-blocked prerequisite; no lane.
- **M8 — endpoint re-typing** (Fable `FINREC-BOOK` ∪ Grok's three-layer
  decomposition ∪ Sol's four interface objects ∪ Opus's family-keyed LL-2
  demand). **Consensus (4/4)**: the off-axis endpoint is a finite set of
  labelled semilinear family records + finite exceptional tables + exactly
  two leaks (U2; case III pending Q+E5 R2), not a finite cell book.
  Fable's record-set finiteness lemma (induction on merge depth) is the one
  concrete **NEW** proof obligation; Opus's cell-collision example
  (`(2,10,0,9)` vs `(2,9,4,5)` at `nu=2`) is the **NEW** justification for
  family-keyed, minimal-period-normalized hashing. Feeds the queued
  REDUCTION CRITICAL 4/5 re-wording (§8).
- **M9 — withdrawn/refuted this round, recorded so nobody re-derives**:
  Opus's index-doubling/`nu`-primitivity kill of U1 (checked false —
  `nu_F` is a Puiseux characteristic, not a maximal index); Opus's prior
  25/26/27/28/30 coupled raise (withdrawn by proposer); Sol's prior generic
  rank/lasso/tropical abstraction (withdrawn, **DUPLICATE** of its own
  prior); "numerical `kbar` cap as completeness" (**KNOWN false**,
  unanimous); Euler-characteristic ceiling accounting (**DUPLICATE — two
  independent negatives**, do not repeat).
- **M10 — D43 read-only-emitter redesign** (Opus §7). **NEW** custody
  proposal: strip positive terminal authority entirely; emit rows + content
  hash, adjudicate offline. Adopted as the standing **contingency** if the
  current v4 lifecycle fails custody again; not a replacement for the
  running review.

**SCOPE-CONFLICT ledger (union of all four, binding on successors):**
Theorem F as written; identifying local `nu`-orbits with global degree-`td`
sheet monodromy (or inferring `nu<=td`); identifying local root torsion
with target-component inertia; treating a U1 cell/family as a germ or map;
treating any finite D43 point as landing or algebraization; using the ACS
cover without source/target component labels; running `SUBST-TRANSPORT` as
the U1 gluing attack; drifting `UNIFORM-TREE` toward the unrestricted
prime-`td` claim; reading the `Z/r` block as a census support bound.

## 4. The next six mathematical actions (ranked by expected information gain ÷ dependency risk)

Type key: [P] proof-side, [C] counterexample-side, [I] infrastructure.

1. **A1 `TD8-EXACT-LAMBDA` [P]** — price the three floor-2 steps of the
   promoted `td=8` route (two `(21,15)` A-steps — the solved L1 family-C
   shape, so P2 pricing should be mechanical — and the `(85,35)` trunk);
   then the `td=12` trunk (slack 1). *Dependencies: promoted material only —
   no provisional theorem consumed.* One strict excess kills the first
   promoted infinite family outright at St. 9.4 equality; all-exact-2 yields
   the campaign's first exact-budget route and arms A2's compatible branch.
   Ranked first because it is the only top-tier kill gate with zero
   dependency risk; all four lanes rank it top-3.
2. **A2 `TD8-TRANSPORT-GATE` [P+C — the dual-use experiment]** — merged
   card M2. Step 0 (gauge typing) consumes printed + promoted material only
   and runs now; the torsion decision fires the moment the parametric
   review lands. Outcomes redirect the whole portfolio: identical-pole kill
   (strongest proof advance on the board) / non-torsion ratio (family dies
   at the coefficient tier except finitely many `t`) / matching torsion
   (strongest disproof seed ever held — arms avenue 4) / untyped transport
   (named construction debt into the landing schema). Highest single-
   experiment information (O "maximal", S "10/10", G "highest per
   desk-day"); ranked second only because its full form waits on the active
   review.
3. **A3 `U2-CLOSE` [P, with a sharp C branch]** — merged card M3, layered:
   rung 1 Sol's direct-terminal lemma on independently rederived U2 algebra;
   rung 2 the one-step renormalizer classification carrying F's denominator
   law and O's cost-growth; cross-check G's log-residue (ODE-independent).
   Closes the only provably infinite reduced regime, or emits the campaign's
   sharpest counterfamily (which would falsify any finite merge-book
   reading). Essentially review-independent; desk-scale.
4. **A4 `TRUNK-CONSUMER` [P]** — M6, Grok card 3: the `(85,35)` trunk ODE
   versus `b=-a`, one parametric identity in `(t,a)`. Cheap, parallel,
   conditional on M1; an incompatibility kills the `td=8` family at the
   next vertex with no transport typing needed.
5. **A5 `ORB-CENSUS` + `UNIFORM-TREE` adjudication + Theorem F repair
   [I→P]** — M5 passes 0–2 with planted controls (pass 0 permanently
   retires the §1 double-count), then promote-or-delete `UNIFORM-TREE` by
   the conjugacy census (empty ⇒ proof target and the first coefficient-tier
   degree constraint; cheap equalization ⇒ delete loudly; expensive ⇒ the
   priced quantitative form is the theorem). Theorem F is narrowed/re-derived
   from the retained Q+E5 merge-local core before any consumer reads it.
6. **A6 record schema + LL-2 design [I]** — M5's schema
   (`EQJOIN-FAMILY/v1` ∪ `FAMILY-CONSUMER/v1` fields, fail-closed entry
   emitter as regression replay) and the family-record-keyed LL-2 design
   (Opus's collision example is the requirement; LL-2 itself stays blocked
   on LL-1 R3 — the unanimous schedule). Feeds CRITICAL 4's re-typed
   landing codomain and every future family-level promotion.

**Counterexample-side standing queue (not separate actions):** A2's
matching-torsion branch arms U1-seeded germ work (avenue 4) on the smallest
member first; A3's infinite branch would be the sharper falsifier; D43
remains background custody with **no solve authorized**; K00's protected
client sub-lane remains the strongest *actual* bounded char-0 seed (Sol) and
is not displaced by anything here. **Explicitly deferred:** any `kbar`-cap
campaign (§2), ACS sub-experiments (M7 banked), char-p/Witt beyond reserve,
monodromy census, avenue-1 re-allocation.

## 5. Peer-majority allocation

Reviews gate promotion, not provisional downstream research
(`COORDINATION.md` promotion rule 5; every card's stop condition handles a
refuted foundation fail-closed). Assignments follow the majority of the four
reports' own recommendations; vote basis noted per row.

| Model | Primary lane | Background / review duties |
|---|---|---|
| **Fable 5** | **A2** — finish its active hostile review of the parametric report first (it is the gate's foundation and the round's critical path: G §4 "do not schedule gluing descendants until that review returns"); run gauge-typing step 0 in parallel from printed+promoted material; then execute the merged torsion decision. Q+E5 case-III R2 stays in-lane (`NUCAP=500` until done). *(Votes: F card-2 self-assignment; O card-B stop conditions; G §4.)* | TRIPLE02 R3 (its build); default hostile reviewer for Sol's A3 outputs. |
| **Opus 5** | **A5 + A6** — `ORB-CENSUS` passes 0–2, `UNIFORM-TREE` adjudication, Theorem F repair (its own erratum), record schema + LL-2 design. *(Votes: O's own instruments; F §6 emitter and S §5 compiler converge on the same schema.)* | K0 R3 **strictly last** of all custody items (F yield order; O's own "unpriced tax"); D43 v4 hostile review per that lane's lifecycle; default reviewer for Grok's A1 outputs. |
| **Grok 4.6** | **A1 + A4** — exact lambda on the three `td=8` priced steps, then the `td=12` trunk; the trunk-vs-rigidity identity from the same desk packet. *(Votes: F §8 assigns lambda to Grok explicitly; G's own §5/§7; S wants the lane run independently in parallel.)* | Continue its active hostile review of the Opus semilinear report — non-blocking for A3/A5, which consume the semilinear algebra under provisional labels; default reviewer for Opus's pass-2 census. |
| **Sol 5.6** | **A3** — `U2-CLOSE` rungs 1–2 on independently rederived U2 algebra, coordinating Grok's §5.2 residue instrument as the cross-check. *(Votes: S's own 10/10 card with the only drafted first rung; O card C names sol56's skeleton as the thing being extended; F card 1 calibrates on Sol's recorded `td=7` closure.)* | LL-1 R3 provenance repair (source-pin/parity only, then freeze); LL-2 family-record client design with Opus (A6); default hostile reviewer for Fable's A2 output. |

Reviewer/producer separation holds: no model reviews its own production
(Sol reviews Fable's gate output rather than Grok, whose provisional theorem
the gate consumes). If either active review returns `REFUTED`, the dependent
cards (A2 full form, A4, A5 adjudication) roll back by their stop conditions
while A1 and A3 continue unaffected — the round's information flow was
deliberately shaped so the two cheapest kill gates carry no provisional
dependency.

## 6. AWS versus desk-scale

**All of A1–A6 are desk-scale by construction and by unanimous design:**
exact `int`/`Fraction`/sparse-polynomial stdlib arithmetic, no CAS, no cap
tokens, hours not machines. No AWS action is requested or licensed by this
synthesis. AWS remains reserved for: (i) the custody-lifecycle rehearsals
already in flight (D43's disposable live-Linux rehearsal at its passed
scope; TRIPLE02 only after R3 and a source pass), per their own lanes, and
(ii) any future heavy sweep — a multi-`td` transport solve, a larger U2
elimination — **only after a sealed, independently source-reviewed packet**,
per the standing rule. Last verified headroom 160 vCPUs; the 16-vCPU
user formalization instance is untouched and stays untouched; no new
machines are needed for anything launched here.

## 7. Not yet canonical — claims that must stay out of the canonical files

1. The U1 merge rigidity law (M1) in any form — `Rad=T^r-A`, `sigma=0`,
   orbit fusion, the transport equation — until at least one of the two
   active reviews passes; then only at that derivation's scope, and always
   as one law (§1).
2. `UNIFORM-TREE` and every corollary (composite `td`, prime-`td` off-axis
   emptiness) — conjecture tier, census-gated, one drift away from refuted
   Moskowicz; keep it out even as "likely".
3. Sol's U2 direct-terminal lemma and all M3 rung-2 machinery — conditional
   on rederived U2 algebra.
4. Opus Theorem F in its current form — defective; nothing may consume it
   pre-repair.
5. The `td=12` witness as anything more than a provisional formal route
   with floors (R4 superset semantics, `alive != existent`, trunk-step ODE
   unsolved).
6. Any local-to-global monodromy identification (`nu <= td`, torsion ↔
   inertia) — `SCOPE-CONFLICT` tier (§3 ledger).
7. Fable's §4.3 ACS corollary and Sol's `ACS-FOX` identity — bankable
   *statements* resting on promoted facts, but single-lane and unreviewed;
   hold for one different-model check before any canonical note (cheap:
   both are one-paragraph verifications).
8. Exact-lambda figures: until A1 lands, canonical files must continue to
   say *floors*, never costs.
9. Nothing from any of the four blind reports enters `AUDIT.md` on the
   strength of this synthesis; ideation output is opinion until its named
   discriminator runs and is hostilely reviewed.

## 8. Coordinator block — promotion-ready

```text
ROUND 20260829T0250Z — FOUR-MODEL SYNTHESIS (Fable 5) — READY TO BANK
BASIS  commit 418e413; packet 3707e151; six roots verified byte-exact;
       four blind lanes verified full+body (fable5 4c173715/5b90f457,
       opus5 a5aa5376/82d84021, sol56 9aebaab2/ecbcd993,
       grok46-rerun 2854e41b/eea80419); tainted sibling grok46.md excluded
       and unopened; provisional roots 2d9fa6fa / 7ed65bc2 not re-opened
       (quadruple cross-attested only).
VERDICT round clean; four blind lanes convergent on one frontier (merge
       coefficient tier), with the disagreements adjudicated in §2/§3.
PROMOTIONS none. Both new roots stay PROVISIONAL under their active
       reviews (Fable←Grok parametric; Grok←Opus semilinear). ONE-LAW RULE:
       Theorem E ≡ local Prop. 8.1(iv) solve — one provisional law, two
       derivations, never two results; ORB-CENSUS pass 0 mechanizes this.
       Composition pin: i=14 (St 3.17(i)); frame rho=2/3 vs ODE
       rho=6nu/(2nu+1) never mixed. Floors are not exact costs.
LAUNCH (desk-only, reversible, reviewers non-blocking)
       A1 TD8-EXACT-LAMBDA         Grok   [P]   no provisional deps
       A2 TD8-TRANSPORT-GATE       Fable  [P+C] step 0 now; full gate
                                                after parametric review
       A3 U2-CLOSE                 Sol    [P/C] rederive U2 algebra first
       A4 TRUNK-CONSUMER           Grok   [P]   conditional on M1
       A5 ORB-CENSUS+UNIFORM-TREE  Opus   [I→P] incl. Theorem F repair
       A6 RECORD-SCHEMA+LL2-DESIGN Opus+Sol [I] LL-2 blocked on LL-1 R3
       Default hostile reviewers: A1→Opus, A2→Sol, A3→Fable, A4→Sol,
       A5 pass-2→Grok (after its semilinear review closes).
BACKGROUND custody, strictly off the mathematical path, yield order:
       D43 v4 lifecycle > TRIPLE02 R3 (Fable) > LL-1 R3 (Sol) > K0 R3
       (Opus, strictly last). Adopt M10 (read-only emitter, no positive
       terminal authority) as standing contingency if D43 custody fails
       again. No D43 solve authorized.
HOLDS  no kbar-cap campaign (no new valid reason; unanimous); NUCAP=500
       until Q+E5 case-III R2; avenue 4 arms only on A2-compatible;
       ACS sub-experiments (ACS-CEIL, TDIC) stay stopped; M7 statements
       banked, no lane; G2-PSC zero active spending (de facto pure-Sigray;
       terminology firewall retained; reinstated only if a hybrid argument
       is actually built); G2-BD dormant until residue A; char-p/Witt at
       reserve; ambient/guided CE search closed; K00 client protected.
QUEUED CANONICAL EDITS (coordinator-only, next overlay window):
       APPROACHES per §2 resolutions (2,3 notes; 4 conditional-arm; 18
       narrow reopen note; 25-28/30 coupled-bet closure; 31 typing note);
       REDUCTION CRITICAL 4/5 codomain re-typing per M8 (labelled family
       records + finite residue + two named leaks); SCOPE-CONFLICT ledger
       of §3 appended to the avenue-2 notes.
AWS    none for A1-A6; sealed source review precedes any AWS mathematics;
       16-vCPU user instance untouched; 160 vCPU headroom unchanged.
CLOCKS broad web sweep due 2026-08-29T05:24Z (outside this lane); both
       active hostile reviews are high-fanout — six-hour latency target;
       two consecutive M2-only rounds without ceiling/landing movement
       trigger the standing zero-base reset (F §9.4, adopted).
SEALS  this synthesis: body and full SHA-256 in the appendix below.
```

*Report body ends. The seal below covers everything above this line.*

report_body_sha256 = 1bc47e35b193e2b75cc9499532c6589c02f3b4fc64d232a2ae47213ced88e770
(sha256 of the first 31502 bytes of this file, i.e. up to and including the
line "*Report body ends. The seal below covers everything above this line.*")

Full-report SHA-256 is the hash of the complete file bytes, including this
appendix. Compute with:

```
python3 -c "import hashlib,pathlib; print(hashlib.sha256(pathlib.Path('xmodel/ideation-20260829T0250Z-synthesis-fable5.md').read_bytes()).hexdigest())"
```

Sealed packet verified this session:
`3707e151584f73969e4c09827bea3bc739fe82f59c192553605a47d276d6e6b6`.
