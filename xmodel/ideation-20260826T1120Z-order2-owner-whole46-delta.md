# Fresh-eyes whole-portfolio delta — 2026-08-26T11:20Z

Status: **independent strategy delta after rereading all 46 canonical avenues;
not a theorem, not a shared-ledger update, and not a reset of the twelve-hour
backstop.**

## Evidence boundary

This scan incorporates only the post-08:45Z changes relevant to ranking:

- reviewed generic-square contact gates `c=1,2` and vertical `c=3,r>=2`;
- dual-AWS producer PASS, still under hostile review, for the normalized
  `r=1` receiver and symbolic unique-`AC` subcone
  `a>=2,c=a+1,r>=a` on `D(p*k0)`;
- reviewed elimination of the `p=0` odd sheet
  `V(rs) intersect D(cs*k0)`;
- the still-provisional `p=0,D(rs)` cusp grade-twelve unit candidate;
- TD6 V82QST2's ten fixed-A3 `F=0` base incompatibilities, whose global
  certificate denominator has thirteen unregistered factors, and the live
  V82QST3 genuine pre-PREV P12 lift intended to adjudicate that denominator;
- the elementary empty-special-fibre implication in
  `cross-empty-special-fibre-valuative-propagation-20260826.md`, SHA
  `22d32f1840f47da09660fd8f6b37966562b6532e8bad12544953a7cbb6185c85`.

The V12 order-two result, cusp unit, and TD6 F client are provisional.  In
particular, even a V82QST3 PASS would close only a fixed-A3 `F=0` open; it is
not yet an identity in a total family with independent base `F`.

Rows 1--46 were reread.  Only avenues 1, 2, 4, 25, 31, 38, and 46 acquire a
new connection or allocation consequence below.  The evidence gives no new
reason to revive avenues 3, 5--24, 26--30, 32--37, or 39--45; their existing
stuck points and protected controls are unchanged.

## Ranked genuinely new bridges and cheapest falsifiers

### 1. Replace infinitely many positive-valuation jets by one exact special-fibre certificate

**Connection:** avenues 2/4 (sheet/Rees germs) and 31 (integrality/Zariski
Main).  The useful content of the new finite-type lemma is not merely a
shortcut: it identifies the exact algebraic object the local campaign should
produce.  For a single raw localized family `B` over a base `t`, an exact
certificate `B/tB=0` makes `t` a unit and excludes every integral or ramified
positive-order arc at once.  The same compiler can serve two live fronts:

- square: `t=p`, after an exact Cech cover of the `p=0` odd, cusp, and
  `R=C=0`/higher-contact receivers;
- TD6: `t=F`, after lifting the fixed-A3 genuine-P12 certificate to a total
  source family in which `F` remains independent and is never inverted.

This is the highest-value compression because it can retire whole valuation
towers, not just one face.  It is also fail-fast: current normalized clients
do not yet meet its hypotheses.

**Cheapest falsifier:** finish the exact-source `p=0,D(rs)` cusp row, then run
the missing `p=0,R=C=0,A!=0` receiver and one irrelevant-ideal/Cech coverage
check in the same unspecialized `Q[p]` source chart.  Any surviving zero
receiver, omitted overlap, or denominator containing `p` refutes propagation
at once.  In TD6, V82QST3 is the immediate falsifier: one outside denominator
factor, failed replay, or omission-control failure stops even the fixed-A3
precursor.

### 2. Globalize the square fan as a coherent obstruction/Fitting problem

**Connection:** avenues 4 and 38, with tropical geometry used only as an
exact finite face selector, not as proof.  The `c=1,2,3`, high-contact,
`r=1`, and symbolic `d=1` calculations are local trivializations of one
proper-fraction obstruction map over the two-root cover of
`L=z^2+p/2`.  Their repeated pattern—root allocation followed by a residue
of strictly higher pole order—suggests a coherent cokernel whose Fitting
support is precisely the exceptional fan skeleton.

Instead of compiling another rectangular box of valuations, construct one
fraction-free residue matrix over the root-splitting base, retain the moving
connection, and compute a small maximal minor.  Descent under the deck
involution would turn the current list of hand faces into a finite support
theorem and leave only named exceptional charts.

**Cheapest falsifier:** on the symbolic unique-`AC` simple block, emit the
residue matrix before imposing a root orientation and factor one maximal
minor.  If it acquires an unregistered factor or vanishes on a generic cell,
the sheaf compression fails and the existing finite `(d,s)` atlas remains the
correct strategy.  This is cheaper and more discriminating than a broad
radical or monolithic saturation.

### 3. Turn terminal Belyi data into a component-killing differential, not a post-hoc screen

**Connection:** avenues 4 and 25 (formal components plus
dessins/Hurwitz).  Every genuine surviving coefficient/Rees component carries
the nonconstant terminal function `r7` with an exact divisor/log-derivative
passport.  The order-four leaf showed that a quotient differential can kill
a whole source component after local coefficient algebra.  The order-one
pure-power criterion and order-two weighted-tree passports suggest the same
division of labor here: local tails construct a candidate component; a
universal `dr7` or `dlog(T)` Fitting condition tests whether its normalization
can support the required two-branch map.

This is stronger than checking a generic plane-section genus and safer than
assuming a local Rees sheet already contains the finite Taylor data.

**Cheapest falsifier:** for the `p=0` cusp if it survives, or for the lone
unique-`AC` exception, compute the divisor and critical values of the actual
source-typed `r7` on the normalized component.  One extra critical value or
wrong zero/pole multiplicity kills that component.  A rational component
with the exact passport is a useful counter-control and is then sent to both
finite Taylor receivers.

### 4. Use support-minimal source certificates as the shared square/TD6 software primitive

**Connection:** avenues 2 and 4 at the software layer.  V82QST2 is a sharp
negative control: ten exact staged incompatibilities are hidden behind an
over-broad global LCM with thirteen unregistered factors.  The correct object
is a support-minimal syzygy from the genuine raw row against the earliest
licensed pivot stage.  This is exactly the discipline that kept the square
row bridges honest after the unlocalized `c=2` failure.

Build one fraction-free certificate minimizer which records per-row
denominators, clears only the selected syzygy, and returns the radical of its
actual denominator plus an omission control.  Reuse it for TD6 P12, square
fan residues, and D1 weighted charts.

**Cheapest falsifier:** V82QST3 itself.  Compare its denominator support with
the V82QST2 LCM.  If the same thirteen factors remain intrinsically, the
software optimization is cosmetic; if only the registered factors remain,
make this the mandatory producer contract elsewhere.

### 5. Attach the terminal weighted tree as a checksum for the missing GGV-to-Sigray transport

**Connection:** avenues 1, 2, and 25.  Full `G2-PSC` transport remains too
large to assume, but the reviewed terminal extremal-abc/Davenport--Zannier
map supplies a much smaller invariant: the signed moment data and weighted
bicolored tree of the terminal partial-y client.  A valid packet-to-book
functor must preserve this invariant.  This gives a bounded first theorem
between the local maximum-twelve campaign and the global sheet ladder,
without pretending it proves full landing or coverage.

**Cheapest falsifier:** take one already source-typed promoted `(6,9)` or
`(8,12)` client for which both packet and Sigray descriptions exist, compute
the terminal signed moments/tree independently on both sides, and compare
the labeled ramification/character data.  A mismatch identifies the first
precise failure of the proposed transport; a match defines the minimal datum
future `G2-PSC` work must preserve.

### 6. Formalize only the two reusable logical kernels

**Connection:** avenue 46 as rigor support, not discovery.  The finite-type
unit lemma and the descent of a root-split residue obstruction are short,
stable targets.  Formalizing them would prevent repeated scope leakage
(`specialized chart` versus `total family`, and `one root orientation` versus
deck descent) while leaving source-row discovery on AWS.

**Cheapest falsifier:** a paper proof audit should first expose every
localization and base-change hypothesis.  If the statement cannot be written
without importing an unproved cover/exhaustiveness axiom, do not formalize the
campaign-specific application; formalize only the abstract lemma.

## Reprioritization

Keep the two-front core, but move effort from already-shrinking individual
jet families to exact family/coverage objects:

- **35%** order-two square: finish V12 review, the symbolic unique-`AC`
  simple block and lone exception, while giving roughly half of this share to
  the `p=0` Cech/total-family certificate;
- **25%** TD6 CURRENT: first V82QST3, then a total `F` family only if the
  fixed-A3 certificate is divisor-safe;
- **15%** global landing/ceiling and the terminal-tree checksum for
  `G2-PSC` (raised now, before a possible local maximum-twelve closure makes
  this the sole bottleneck);
- **15%** bounded D1/order-one/remaining maximum-twelve coverage, with
  terminal divisor screens attached early rather than after component
  classification;
- **10%** protected fixed-support counterexample/Witt work.

The highest-value next separator is therefore not another broad saturation:
it is the three-part square special-fibre package—cusp, `R=C=0` receiver, and
exact Cech coverage in one unspecialized `Q[p]` chart.  In parallel, the
smallest TD6 separator is the already-live genuine-P12 denominator support.
Formal review and modular controls remain asynchronous; none should block the
next producer.

No proof or counterexample to JC2 is claimed.
