# 0730 Astra cross — a uniform local mechanism, not a universal source

September9,2026. Frozen basis `0d39df3c9fd69c939a8420c54d03228b9077777d`. COMPLETE cross at its documentary/manual scope; original soft08:27:37 was missed. The first actual invitation was08:31, with absolute08:47 and maximum15minutes, neither extended here. JC2 remains unresolved.

## Decision and deduplication

The new canonical-A / WHOLE-B proof is the highest-information candidate. Its normalized multiplicity minimum supplies the strict degree loss missing from a naive extrapolation of16f. I found no gap in the charged local argument at this cross's manual scope. The all-parameter source proof likewise has an explicit rectangular-F9 antecedent and a usable completion of its next-edge justification below. BOTH remain UNREVIEWED here; no external gate result was accessed or inferred.

This combines root's contact-minimum redesign with the concern shared by Astra, Fable and Sol that finite degree hopping cannot close the problem. It supersedes, conditionally, the narrower Astra/Fable `n=2m-1` reference-window proposal and Sol's `F_j=KC`-based capacity ledger: the new proof assumes neither `ord G>=2j` nor `K|F_j^2`. Do not launch three parallel versions of that test. Fable's sign-of-`2m-n` census is not a boundary of the new whole-B method. Failure of one old margin also does NOT prove all uniform methods nonconvergent.

Sol's marked-sheet idea genuinely uses the right first leg, but the proposed total-function pullback/norm supplies no new constraint. The exact cancellation below stops that version without excluding all possible factor-specific methods. The finite admission sieve is the already deferred0445 proposal, not a new mechanism or immediately executable classifier. Fable's correlated-import ledger repeats the existing explicit import perimeter; no concrete doubt earns a foundation farm.

Full46 disposition relative to current frozen scopes: retain Astra's `1 RAISE` for source-attached uniform work, `29 RAISE` only for this monomial-J mechanism, `31 LOWER` only for the completed F9 reverse task, `36 LOWER` for absent unretired complete search object; all other IDs UNCHANGED, none reopened. Sol's26 raise is not earned by the total norm, and its4 lowering has no new algebraization discriminator. Fable's5 observation is a correct use of already accepted target descent, not new progress on nondividing sources. Global31, G2-PSC/G2-BD, off-axis/full landing, K16 and cofinal degree/type obligations remain unchanged.

## Card1 — finish the two independent uniform arrows, then compose only their intersection

**Object/field/ring.** Root's candidate concerns ordinary `A,B in k[a,b]`, characteristic zero, degrees `7m,7n`, tops `K^m,K^n`, coprime `2<=m<n`, `K=(b^2-a^2)^2 b(b^2-3a^2/2)`, and bracket `2c*a^3`, `c!=0`. The separate source candidate starts with an ORDINARY attained rectangle and an ACTUAL standard F9 chain, not a bare Laurent definition or a numerical degree label. These are post-blind producer claims, not accepted16f restatements.

**Hostile local check.** The finite A normalization removes images of `mK^(m-1)` at orders1..7 and scalar K-powers at their homogeneous orders. The first residual is neither divisible by `K^(m-1)` nor scalar; if none remains, `f'(R)` of degree `7(m-1)>3` divides the nonzero Jacobian, impossible. No centralizer theorem or B-reference enters this step.

For `delta=min ord_L(F_j)/e_L`, let `L=m-delta`. When delta is integral, divisibility by `K^delta` and the removed scalar case give `j<=7L-1`. When half-integral, the THREE simple factors each round upward, giving `j<=7L-3/2`. This is a real source-derived strict defect, not a raw-support or generic-rank estimate. In particular the old `K^2*b` contact does not evade the new minimizing-factor analysis.

At a minimizing simple line the inverse `z=R_s` gives `eta<=j/L<7`; at a double line the Morse balance has `r<=j/(2L)<7/2`. All lower scalar A terms are later. The exact degree thresholds for the correction are respectively `m-2` and `2m-3`, so common linear/quadratic powers cannot absorb it. The latter threshold cannot be replaced by `2m-2`: a shifted common quadratic is the explicit surviving control outside the hypothesis.

The important change is `W=B_s-R_s^n`, whose COMPLETE chart coefficients have positive s-order. At its first initial of order nu and degree t, `t*eta<=nu-1` (or `t*r<=nu-1`). Therefore its highest-coefficient Euler factor is strictly positive below the monic n/2n order. This excludes earlier B initials without any assertion about G or the derivative remainder rho. The accepted15z control is not a counterexample to that different argument.

In the separated chart, `U(z)` and `V(z)` are formed BEFORE choosing an initial. Their sums converge because `ord xi=kappa>0`; the transverse leading even/odd coefficient anchors one base at order j. A positive square-root shift gains `eta-kappa>0` relative to its own base. A global minimum exists, and `(u+v,u-v)` cannot vanish on both sheets. Crucially, the proof first rules out ALL earlier whole-B bases; only then can it rule out their returning shifts at the monic order. Both monic A initials are present even when one correction vanishes. The complete chain rule gives target `N-kappa/2+eta`, `N=7(m+n)-5`, rather than silently dropping the inverse derivative.

With `Delta=1` or `3/2`, the common strict margin is

`N-(m+n-1/2)j/L >= -3/2+(m+n-1/2)Delta/L > 1/2`.

It covers the smallest m, a half-integral minimum, zero higher coefficients and finite/infinite coalesced orders. At the terminal initial, Euler plus UFD uses coprimality; it is not the false assertion that arbitrary commuting monic polynomials must be equal powers of a linear form. This cross confirms the argument's internal fit, not a new gate verdict or extension to other K/targets/nilpotent rings.

**Source audit and one precise completion.** In the new source proof, the common vertical shift, original polynomial Euler ratio `b=3a/2`, denominator7, complete positive interval, integral linear face-root, both integer cuts, determinant g and diagonal/output scalings are parameter-independent as written. Their named primary interiors remain imports at16a/b's scope; this cross did not fetch or re-prove those interiors.

Section5's sentence "there is a next lower edge because the polynomial is nonmonomial" should be read with the following actual support reason. The unchanged `(-2,7)` face retains BOTH `C_e=(21e,7e)` and the nonzero point `(0,e)`. The Laurent shear strictly lowers this weight in every changed term, so cannot cancel either face coefficient. The corner is still the unique coordinatewise maximum. Hence the finite support has a point with lower y-coordinate, and the next lower supporting edge at that corner really exists with a finite positive slope. The removed slope2 face leaves a strictly larger slope; the repeated Cor7.4/integer-lattice argument rules out every rational slope in `(2,3)`. Thus `i<=3j` is justified, not inferred from nonmonomiality in an arbitrary Laurent polygon. This is an explicit completion from the charged hypotheses, not an added source assumption.

**Source boundary.** Even if both proofs pass, the conclusion concerns every ordinary rectangular actual-F9 representative in the stated all-q family. It does not yet say every pair of numerical degrees `28(q+2),28(2q+3)` enters F9. Fable's same-degree selection needs a new complete filter. For example its own warning about F10 index0 matters: the printed formula is `5j+7,3j+4`, so at0 its swapped ratio is4:7 at the SAME corner sum28. The allowed index range and exclusions must be checked before discarding it. No all-degree source coverage, maximum bound, new complete ideal or receiver-to-source lift is obtained from a matching ratio.

**Test/outcomes/stop.** Highest priority is the already-earned independent verification of these TWO terminal proofs, with separate source/local/composition verdicts. No duplicate review lane is proposed here. If both pass, bank exactly their intersection and reassess the remaining source classes; if source fails, retain only the abstract local result; if local fails, retain the necessary source map; a timeout is INCONCLUSIVE. Stop at the first unsupported arrow, without a4/7 solve, sign census, linter implementation or serial parameter farm. This is the decisive mathematical discriminator; a proof-specific changed-contact or target satisfying every claimed antecedent would be an informative falsifier, not a CE unless it also reconstructed a full constant-J source.

## Card2 — cubic marked-sheet norm: total-function version is NO-GAIN

**Object/dependencies.** Use exactly BINDING BD-GAL: `A^2 --g1--> Y --g2--> A^2`, first leg etale quasi-finite, second normal finite flat of degree3, function-field first-leg degree d1. For a branched prime `D=V(p)`, tame degree accounting and an unramified sheet give `div_Y(g2^*p)=2R_D+U_D`. This is not a claim that U_D is Cartier or attained as a new source.

Since g1 misses the ramified component, the Weil-divisor pullback through its etale image already gives

`div_A2(p composed with F)=g1^*U_D`.

This is automatic and adds no relation beyond the existing principal function. Put `f=g2^*p in K(Y)`. Its field norm along `K(A^2)/K(Y)` is EXACTLY `f^d1`, because f belongs to the base field. Its divisor is `d1(2R_D+U_D)`, not `d1 U_D`. A pushforward that omits the missing R_D contribution is not the norm divisor.

More explicitly, in the finite normalization `pi:Z->Y` containing the open first-leg source, a polynomial source factor h_i is only a rational function on Z. Its full norm divisor includes the pushforward of its source zeros AND every boundary valuation `v_E(h_i)[k(E):k(pi(E))] pi(E)`. These coefficients can be signed; regularity on the open A2 does not control them. If `p composed with F=unit*product h_i^(r_i)`, multiplying all norms restores exactly `f^d1`. The omitted boundary terms then restore the missing ramified contribution. The class-lattice injection is entirely compatible with this: `[U_D]=-2[R_D]` is already known, and neither class is thereby torsion.

**Genuinely new condition required.** An individual factor could help only if the actual first-leg geometry supplies an additional bound, vanishing, divisibility or independent relation for its ENTIRE boundary-valuation vector, strong enough that elimination yields a nonzero boundary-only principal divisor. No such condition is in Sol's card or this packet. The finite-etale and open-immersion controls do not supply it for this nonproper source.

A literal boundary control is `U={x!=0} subset Y=A^2`, with f=x and the identity function-field extension. Its divisor on U is zero, while its norm on Y is x and has the missing line as divisor. This is NOT the required A2 first-leg source and does not refute BD-GAL; it isolates the invalid operation of omitting boundary valuations from a norm.

**Decision/outcomes/stop.** STOP the total-function norm discriminator as a new mechanism; it is the same principal divisor written twice. REDESIGN only upon a specific source-derived individual-factor valuation condition, not a generic cubic-algebra or compactification census. If a future full relation survives all boundary terms, it merits one review; if it reduces to the displayed norm identity, NO-GAIN; absent source control is GAP, not cubic-block exclusion. This leaves d1=2, non-Galois d2>=3 and primitivity open. No current factor-specific successor is authorized.

## Proof, CE and systems priorities

The new connection is the contrast between two valuation transfers: canonical contact normalization creates a strict defect of1 or3/2 that powers Euler rigidity, whereas the total norm conserves the original principal divisor and creates no defect. That distinguishes a potentially scalable mechanism from a reformulation without pretending either supplies global source coverage. NO_NEW_MECHANISM for a complete JC2 proof or CE is established by this cross.

Proof priority: Card1's exact family composition first; global landing/K16 retain their importance but no new discriminator here; the SAME deferred finite `3:5,D<=35` admission check is a conditional fallback only if it selects a genuinely different source. It must not start F16/F22 or other degree farms automatically. Fable's ratio-census/software linter and Sol's capacity linter are unnecessary if the uniform proof passes, and unjustified if its hypotheses fail.

Genuine CE priority remains a complete guarded unretired polynomial source with every row and nonzero guard, or an exact finite-polynomial characteristic-zero AS109 lift. Neither is supplied here. Growing-support Witt towers, boundary germs, nilpotent contacts, modular points and necessary receivers remain falsification evidence only. Stop every retired F9/F2/Moh/99/D108 solve; no AWS, coefficient emission or bound increase is earned.

**Separate systems: NO_CHANGE.** Fable's proposed availability call cannot guarantee credits for the subsequent long task and introduces a new external charge; Sol's release receipt largely duplicates the existing verified vector/root seal/actual-invitation metadata. The observed missed windows justify applying that existing workflow promptly, not another wrapper or model-productivity inference. Restore availability from the actual user notice, preserve shorter late-seat intervals and equal standing, and never activate a prospective deadline retrospectively. No new scheduler, collector, probe or proof farm.

## Evidence and publication perimeter

All20 frozen charged objects were read WHOLE, including all four blinds, both new proofs/transactions, the full46 history, current science/excerpts and contract/poststate/PINS. Clipped displays were repaired by overlapping explicit rereads; byte-identical whole reads from the earlier blind were also retained. Excerpts are not claims to omitted originals. All19 content hashes and invitation/PINS were checked before bodies and are rechecked at publication. No current gate/cross/report/log/receipt, primary original or mutable canonical replacement was accessed. The cross consumes the new proofs only as UNREVIEWED candidates. No mathematical subprocess of any size, web search, CAS, old checker, source/power expansion, AWS/SSH, agent, protected/shared/public write or external communication occurred. Only metadata reads and this own publication were executed.

No new campaign identifier, promotion, clock reset or follow-on authority is asserted. Custody records the precise20-object vector, read scope, transaction and all terminal owned pins. All writers will be IDLE before handoff.

## OPEN(S) RAISED

None.

## COLLISIONS

status: EMPTY

- NONE — own-report raised-OPEN check only; no corpus scan.
<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `14857`.
- Body SHA-256:
  `514b90b9a52c3c32fc8fbc85883db3e396f793b907b8af30c8e6e6b2fa8cfffa`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
