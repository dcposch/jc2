# TD6 q2-beta B3-divisor closure — AWS exact replay

## Verdict

**PRODUCER-EXACT whole-`B3=0` closure; promotion held on the independent V43
canonical generic gate; hostile review pending.**

On the exact birational chart of `B3=0`, independent r6d and Box03 V44 runs
give byte-identical canonical stdout (SHA256
`a33dc711c1b8b7d215912aa8c86c43eac8b9be43cc30e1c219583bf952c937ee`).
The exact genuine-P12 plus staged-N13 original-row identity has constant unit
residual `-k/50`.  Its complete denominator radical is precisely
`tau(tau-2)w(2tau-1)(tau^2-4tau+2)`; no source scaling or gauge normalization
is used.

The chart audit routes `tau=0`, parameter infinity, and the only missed
affine base point `(-5,0)` to `V=0`, where
`B3=4U^2(C+U^2)(C+5U^2)`.  The frozen all-beta rational-line package closes
the two non-`U` components, and the frozen all-beta `U=0` theorem closes the
remaining component and origin.  The frozen exact N13 exceptional-curve
package, with its fixed-beta P12 dependencies, closes the finite factors
`tau=1/2` and `tau^2-4tau+2=0` for every beta.  The specialization `tau=2`
is the origin.  Thus this constructible cover closes the whole set-theoretic
`B3=0` divisor for every beta in the fixed source-typed A3 section.

V39 is retained only as a custody-negative control.  Its exact algebra,
base P12, ranks, denominators, and residual were unaffected, but four SHA
fields (first-pivot, beta-tail, N13 value, N13 multiplier) included process
addresses through legacy `repr(E3)`.  V44 replaces them with exact-coordinate
serialization and agrees byte-for-byte across hosts.

The immutable package is
`cases/td6_c1_c2_c3_q2_beta_b3_divisor_aws_20260825/`.  Campaign promotion is
deliberately held until V43 independently closes the same portability gate
on the generic open.  No fourth-modulus, whole-TD6, SP-2, landing, or JC2
claim is made here.
