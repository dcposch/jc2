# Grok 4.6 primary research — close or exhibit U2 (`nu_G=1`, unbounded lex)

Work independently from `/Users/dc/code/math/jc2` and write exactly:

`xmodel/m2-u2-nu1-unbounded-lex-primary-grok46-20260829.md`

Use the Opus semilinear report and your completed hostile review
`xmodel/m2-equal-join-semilinear-primary-research-hostile-review-grok46-20260829.md`
at their reviewed scopes, plus current BOOK-OFFAXIS P0--P5, MULTIPOLE
MP6/D9, the reviewed LL-1 `nu=1` normalization, printed Prop. 8.1(iv), and
only directly relevant canonical clauses. Do not read active ideation,
transport, or Q+E5-review outputs.

Target the sole currently proved-infinite reduced merge-state regime:
Prop. 9.3 case I at `nu_G=1`, `kbar in Q`, `r>=2` equal `(mu,w)` nonzero
arrivals, and unbounded q-only extra count `lex`. Do not assume the legacy eta
slot is independent: normalize `nu=1` exactly as LL-1/MP6 requires before
classifying degrees, roots, `M`, prices, or ODEs.

Derive the most general legal U2 shape, allowing every free 0-root/non-chain
variant that can remain unbounded. Then attack in this order:

1. Solve the printed Prop. 8.1(iv) equation uniformly in `lex`, using the
   general T1-GEN identity. Determine whether the extra polynomial `S` exists
   in arbitrarily high degree, whether a D9-style logarithmic residue kills a
   parity class, or whether only finitely many degrees survive. Keep the
   nonzero RHS gate explicit.
2. For surviving shapes, derive exact `M`, successor `w`, and lambda floors;
   compose with the cap-free reduced P0 chain theorem and P1
   `j=M(1-w_t) in N*`/psi budget. Decide at fixed `(td,entry)` whether
   infinitely many `lex` can reach a legal terminal.
3. Reproduce the td=7 `(2,2t)` tail and its first-step-inversion budget death
   as a mandatory negative control. Search the smallest td=8--14 source row
   for either a second uniform death or an explicit infinite budget-fitting
   formal counterfamily.
4. Distinguish pattern-tier survival, Statement-3.9 gluing, landing, and an
   actual Keller map. If a global theorem needs a missing source datum, name
   the smallest one and emit a finite typed discriminator.

Use exact hand algebra and short standard-library Python only; no broad
uncapped local enumeration. Provide a small packet if it certifies a uniform
identity. Return `U2_REDUCED_FINITE`, `U2_INFINITE_FORMAL_COUNTERFAMILY`, or
`PARTIAL`, with the narrowest theorem, all outcome meanings, stop condition,
full/body hashes, and exact replay commands/counts.

Hard boundaries: no web, AWS, commit, push, canonical edits, heavy CAS, or
local long/high-memory process. Never access/list/search/build/status or
control `jc2-lean`; never run global `git status` or workspace-wide searches.
Modify only the requested report and a uniquely named packet under `cases/`
if needed; use `/tmp` for scratch.
