# FIRST runtime-evidence gate: minimum-pivot generic validation (Fable 5.1)

Status: FIRST different-model runtime-evidence review of the charged ROOT
execution report e5765acb and its 14 charged evidence files. DOCUMENTARY
ONLY: manual text/hash/diff plus bounded stdlib JSON metadata comparison.
No CAS, import, AST, compile, fixture/candidate body parsing, arithmetic,
network, AWS, SSH, /proc, agent, Git, ledger or live-file access. First
action 03:46:08 UTC; controlling stop 04:00:00 UTC; reserve from 03:58.
Accepted premises: 17zh harness c369dc11, 17zk automatic caller, 17zp
literal caller e62e5dc4 and solver 961e22e6. Unchanged scientific code is
not re-reviewed. No Seal and no charge_basis line are authored here.

## 1. Verdict

- A CONFIRMED. The charged report reconciles against ROOT registration,
  caller interface, preflight/batch receipts, both telemetry files, live
  principals, terminal record and stop record on every checkable field.
  All 18 principals (Main 1952, 8 CAPRUN callers, 8 principal children,
  descendant 2246) were captured in the same launch with literal argv,
  pid/ppid/pgid, raw stat, start ticks and one PID namespace. Both races
  are recorded honestly (section 3). Unit accounting is `[not set]`.
- B CONFIRMED. Receipt: 8 positives in the stated order and branches,
  4 ValueError EXPECTED-REFUSAL negatives with the stated reasons, schema
  F10-GENERIC-CONTROLS/v1, engine/harness/solver/authority/host binding
  exact, 6524 bytes. All 20 sidecar hashes and sizes in batch.PASS match
  the 100-path remote-owned manifest. Dummy sequence, generic telemetry
  and the non-reap status of null cleanup are as the report states.
- C CONFIRMED WITH QUALIFICATION. The 112-vector, literal caps, deadlines,
  aggregate 240, pins and refusal contract are verified from the charged
  registration and caller interface. ROOT's per-record `VERIFIED` bindings
  rest on uncharged per-operation files; I did not re-verify those bytes.
- D No blocking defect. Qualified GENERIC runtime acceptance stands:
  synthetic-only, no actual-source, timing or height claim. A fresh,
  separately ROOT-registered actual attempt with actual caller ae3df9a9
  is ELIGIBLE for selection. This is not automatic execution, acceptance,
  or permission to reuse old paths, clocks or raise caps. A synthetic
  success does not discharge the actual 217/1638/source equations.

## 2. Custody and scope

All 15 snapshots hashed before reading; every digest matched the task
list. The generated pin file is box/f10-minimum-pivot-generic-runtime-gate-
fable5-20260910/input-pins.sha256. Own report and box were absent before
writing. Read WHOLE: charged report, 17zp gate, ROOT-REGISTRATION, receipt,
both telemetry, reconciliation, preflight, batch, manifest, live-principals,
terminal record, stop record. Registration: stdlib JSON metadata read of
every top-level key and the 111-entry pin vector (paths and counts; native
digests not individually re-listed). Caller e62e5dc4: lines 228-300 read
whole plus grep hits over the entire 336-line file for argv, pins, caps,
deadlines, control order and receipt binding; lines outside those regions
rest on the accepted 17zk/17zp premises. Nothing was executed or parsed
beyond JSON metadata; fixture and candidate bodies were never opened.

## 3. A: chronology and identity reconciliation

Charged evidence supports this order, every step strictly increasing:
unit launch 03:38:16.891 (invocation e7e7daf4, Main 1952, argv exactly
the registered `python3 -I -B dispatch_batch.py ca6efd85...`); controls
prelaunch-checked and started 03:38:17.080 through 03:38:18.927 in the
registered order disabled/wronghost/argv/cap/missing-input/valid/dummy;
preflight.PASS 03:38:19.436; generic prelaunch 03:38:19.469, caller start
03:38:19.503, child 03:38:19.556 to 03:38:20.440, caller return
03:38:20.456; batch.PASS 03:38:20.534; unit Main 0/inactive/dead
03:38:20.593; terminal quiet marker 03:39:04.525; 112 `OK` rehash lines
and the 100-path manifest closed 03:39:04.564; instances all `stopped`
03:40:21.280; backup timer inactive 03:40:21.289 (8.7 ms after);
reconciliation 03:41:52.111. Hard cutoffs: controls 1789012020 =
03:47:00 (the registered mathematical deadline), generic
first_math + 240 = 03:42:19.436; every record returned before its cutoff
with the 15 s admission margin.

Identity: caller_stat in batch records and live-principals agree on
pid/ppid/pgid/sid and start ticks for all 8 callers (19199, 19229, 19260,
19290, 19319, 19349, 19380, 19438). Generic child 2330 start ticks 19443
and dummy leader 2232 start ticks 19385 match their telemetry
start_identity; descendant 2246 has ppid 1 and pgid 2232 at
03:38:19.057. Nine original groups: 1952 plus the eight child leaders.
All 18 principal lines carry pid:[4026531836]. CAPRUN argv caps match the
registration profiles literally (probes 5/3/2147483648, dummy
5/3/33554432, generic 180/170/2147483648); telemetry argv_count 6
(generic) and 8 (dummy) match the observed argv.

Races. (i) Final cgroup: live-principals ends with
`read error: 0: No such device` followed by MainPID=0/ExecMainStatus=0/
empty ControlGroup/inactive/dead at 03:38:20.593. This is a late read of
a vanished cgroup after all principals had been captured; no principal
was lost and orchestration did not abort. Recorded as the report states.
(ii) rg-missing prestage (exit 127, plain-grep recovery 03:37:08): this
is attested ONLY by the charged report text. The ROOT preflight/staging
transcript, the timer-ACTIVE-before-start ordering (03:34:58/03:35:00),
the strict-SSH pin check, the 03:37:43 prelaunch 112-pin verification,
the 03:39:05.242 pull match, the 03:39:24 stop request and the 03:39:58
stopped read are not among the 15 charged files. They are consistent with
ROOT-REGISTRATION's prescriptions and with the charged stop record, and
nothing charged contradicts them, but I did not independently verify
them. Unit accounting (MemoryPeak/CPUUsageNSec `[not set]`) is confirmed
unavailable in both the live and terminal records.

## 4. B: receipt, batch and telemetry

Receipt (6524 bytes, SHA bb6f0a07): positives allzero-q0 UNIT, allzero-q1
SEPARATOR, constant-degree25 UNIT, raw-two-T UNIT, nonreduced-remainder
UNIT, lost-component SEPARATOR, varying-pivot UNIT, all-generators UNIT;
UNIT checks record 217 coordinates / 9 multipliers / degree bound 25,
SEPARATOR 1638 columns / 217 target coordinates; every status
VERIFIED-SYNTHETIC-ONLY. Negatives scaled-separator, raw-unit-plus-one,
short-witness, nonreduced-coefficient-modulus: all ValueError,
EXPECTED-REFUSAL, reasons verbatim as the report. Binding: engine module
2e5f8f17 / 0.9.0 equals the registration python_flint block; harness
c369dc11 and solver 961e22e6 equal the manifest digests of
generic_controls.py and solver.py; authority f824e45a equals the manifest
generic.authority.json; hostname/instance/job_tag equal the registration.
Status ALL-12-GENERIC-CONTROLS-VERIFIED-NO-SOURCE-ACCEPTANCE, scope
synthetic rank-seven only, exactly the strings the caller requires.

Batch.PASS: 8 records; the first 7 are byte-equal to preflight.PASS's
records; per record authority, prelaunch and telemetry digests match the
manifest; all `remaining_group_live` empty; status
QUOTIENT-GENERIC-ENGINEERING-PASS-NOT-ACTUAL-SOURCE; plan digest ce193898
matches the manifest; no batch.STOP.json exists in the manifest. All 20
receipt path/sha pins are present in the batch artifacts and in the
manifest with the observed byte counts. Reconciliation agrees with batch
on all 8 records (authority, prelaunch, caller pid, return code; input
count 112, with 111 on missing-input by design).

Telemetry. Generic 2330: NORMAL_EXIT, child 0, runner 0, resource null,
wall 0.88459243 s, group RSS 44179456 B, stdout 593 B (SHA matches the
manifest generic.stdout), stderr 0 B; termination cleanup_complete null,
leader_reaped false, no signals sent, empty live/zombie lists. That block
is not reap evidence; quiet rests on the separate 03:39:04 terminal
record. Dummy 2232: RESOURCE_CAP/rss, runner 125, child 0, RSS 80052224 B
above the 32 MiB cap, MATCH before-term with ticks 19385, SENT 15, MATCH
before-kill, SENT 9, cleanup_complete and leader_reaped true, zombie
[2232] before reap, live []. The 0.8846 s / 44.18 MB figures are
synthetic-only; no genuine-source timing, height or improvement follows.

## 5. C: registration and caller contract

Registration ca6efd85: schema F10-QUOTIENT-GENERIC-DISPATCH/v1, enabled,
generic_only, exclusive writer, deadlines 03:47:00/03:51:00 UTC,
aggregate 240, file cap 16777216, profiles exactly probe 5/3/2 GiB,
dummy 5/3/32 MiB, generic 180/170/2 GiB, python_flint 0.9.0 / 2e5f8f17,
and a 111-entry file_sha256 vector: 10 task files, /usr/bin/python3 and
100 installed/native files (69 flint/types, 3 python_flint.libs, 6 system
libraries, dist-info METADATA and the rest of the flint tree). With the
registration itself that is the fixed 112. No acceptance key is present.
Caller interface (lines 201-266): sole argv is the 64-hex registration
digest checked against the file; refuses on schema/enabled/generic_only,
acceptance keys, cwd, host (and `math-hq`), non-UTC deadlines, aggregate
not 240, file cap, non-literal profiles, altered dummy cap; `known` pins
nine task files including solver 961e22e6 and plan ce193898; the supplied
vector must equal base plus package exactly; every supplied digest is
recomputed; fixed_files adds the registration as the 112th; fixed_barrier
runs before the plan is read; all outputs must be absent. Control order
at lines 268-274 equals the observed record order, and the generic step
requires receipt schema/status/scope/execution/engine/harness/solver, 8/4
inventory, 20 literal sidecar bindings, authority barrier, receipt
re-digest and return before the stored hard cutoff. Each derivative
authority pins 112 (111 on missing-input intentionally). The charged
reconciliation records all 8 authority/prelaunch/dispatch checks as
VERIFIED; those per-operation files are uncharged and remain ROOT's
observation plus the accepted automatic-caller premise, not my re-read.

## 6. D: gaps, defect search and eligibility

Smallest actual blocking defect: none found. Gaps, none blocking:
- GAP[UNCHARGED-TRANSCRIPTS]: AWS/SSH/prestage/pull/stop-request
  chronology and the rg exit-127 record are report-attested only.
- GAP[UNCHARGED-PER-OPERATION]: authority, prelaunch, dispatch and
  caller stdout files for the 8 operations are hash-listed in the
  manifest but not charged; their contents are ROOT-reconciled only.
- GAP[CALLER-WHOLE]: caller lines outside 228-300 and the grep hits are
  covered by accepted premises, not a fresh whole read.
- Telemetry argv_sha256 serialization is unknown here and was not
  recomputed; the literal argv was compared instead.
Acceptance granted: qualified GENERIC runtime acceptance of ROOT
execution e5765acb, synthetic-only. Not granted: any source certificate,
full source point, JC2, speed or height claim, or any conclusion about
the actual caller. Genuine-source readiness: eligible for a fresh,
separately ROOT-registered actual attempt with actual caller ae3df9a9,
under new registration, paths and clocks, unchanged caps. No automatic
execution, no cap raise, no repeated generic run, no helper hardening.

<!-- BODY-END -->
