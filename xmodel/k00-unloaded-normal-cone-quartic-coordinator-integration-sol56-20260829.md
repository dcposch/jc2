# Binding integration: unloaded K00 normal jet, quartic, and true normal cone

Coordinator: Sol 5.6 Ultra  
Review closure: 2026-08-30 UTC  
Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`  
Lifecycle: **BINDING PROMOTION / EXACT SECOND NORMAL JET / SHARP NILPOTENCE / LOADED-SOURCE FIREWALL**

## 0. Custody and immutable review seal

Campaign session 20056 closed with `exit_code=0`, `final_status=DONE`, and no
AWS launch. Its frozen artifacts are

```text
75944fa9a0390f5e734250cabf00d6fec3afb05286cbd0b3b1cfb317bcc0fded
  xmodel/k00-unloaded-normal-cone-quartic-hostile-review-fable5-20260829.prompt.md
4ce99f6e387dc91f1e0ed57328f845ede9f0aad7365057287e47fcad4c6f0fab
  xmodel/k00-unloaded-normal-cone-quartic-hostile-review-fable5-20260829.run.v2
05f07a642323de4f2f2700748862545ba6b7399e7bc2e697232e2be09abb5c86
  xmodel/k00-unloaded-normal-cone-quartic-hostile-review-fable5-20260829.log
74813e3307ee0514038f271b9714f5f0fac82ed63b2bdbbc1b7727ff9a740cee
  xmodel/k00-unloaded-normal-cone-quartic-hostile-review-fable5-20260829.md
```

The run manifest's prompt, log, and report hashes match the current bytes.
The review has one standalone `<!-- BODY-END -->` at EOF, so its body and
full file coincide: 27558 bytes, SHA-256 `74813e...40cee`. This paragraph is
the coordinator's immutable seal of that unmodified lane report.

The reviewed producer correction is

```text
79a69a4519b47b2c93fd72311687d1dc4733938b3cd077d785504e57a2dc4f9f  full
b115f76e36dfeda68d0c08eac1eae981b4feb4f85a8d713f8a9655bc501f4dab  body, 5183 bytes
  xmodel/k00-unloaded-projective-normal-cone-correction-sol56-20260829.md
```

Fable's verdict is `CONFIRM_WITH_CORRECTIONS`: the correction's empty-Proj
conclusion is confirmed and sharply strengthened; the Opus identification
of the quartic with the true projectivized normal cone is refuted.

## 1. Promoted sharp normal-cone theorem

Let

```text
A=Q[d0,...,d5],  I=(r1,...,r7),
J=(f0,f1,f2,f5), R=A/I, K=J/I,
```

where the `r_i` are the seven frozen unloaded rows and

```text
f0=d0-2*d4-d4^2,       f1=8*d1-(1+d4)*d3,
f2=d2-d4-16*d3^2,      f5=d5-2*d3.
```

Then, as literal ideal containments in `A`,

```text
J^5 subset I subset J^2.                              (1.1)
```

Both bounds are sharp: `J^4` is not contained in `I` (indeed each displayed
`f_a^4` is outside `I`), and `I` is not contained in `J^3`. Consequently

```text
sqrt(I)=J,       K^5=0,       K^4!=0.                 (1.2)
```

Thus the actual normal cone of the reduced support `D=Spec(A/J)` in
`X=Spec(A/I)` is

```text
C_D X=Spec(gr_K(R)),       Proj(gr_K(R))=empty.       (1.3)
```

The relative Proj is the empty scheme over every point of `D` and remains
empty after arbitrary base change. The affine cone in (1.3) need not be the
zero scheme: it retains nilpotent positive-degree structure supported on its
zero section. The earlier correction's `K^17=0` pigeonhole argument was
correct but nonsharp; (1.2) supersedes it. Likewise, for any centered
valuation for which the displayed ideal values are finite,

```text
2*v(J) <= v(I) <= 5*v(J),                             (1.4)
```

sharpening the earlier upper constant 17 without determining integral
closures or Rees valuations.

## 2. Promoted quartic theorem at its correct type

The change

```text
(d0,...,d5) <-> (e0,e1,e2,e5,S,T)
```

used by the review is a polynomial coordinate change and identifies `J`
with `(e0,e1,e2,e5)`. Let `Q_1,...,Q_7` be the images of the seven rows in
`J^2/J^3`, viewed as quadrics in the four normal variables over `Q[S,T]`,
and set

```text
Delta=S^3+S^2+72*S*T^2+64*T^2-432*T^4.              (2.1)
```

For every characteristic-zero field `k` and `(S0,T0) in k^2`, there is a
nonzero `nu in kbar^4` satisfying all seven `Q_i(nu;S0,T0)=0` if and only if
`Delta(S0,T0)=0`. This is a theorem about the **seven-quadric second normal
jet**, not the actual normal cone. The reviewed certificates are the
saturated elimination `(Delta)`, all 56 inclusions
`Delta*(degree-five normal monomial) in (Q_1,...,Q_7)`, and a base-point-free
parametrized witness whose fourth normal coordinate is the constant 864.

Geometrically, the common-zero affine cone has dimension two exactly at the
node `(0,0)`, dimension one at every other geometric point of `Delta`, and
is only the origin off `Delta`. Over `Q(S,T)` its Artinian fibre has length
16, Hilbert vector `(1,4,6,4,1)`, and is generated by a regular sequence of
four quadrics selected from the seven row generators. Along the
generic point of `Delta`, the reduced positive-dimensional fibre is one
rational line, carried with multiplicity four by the seven-quadric scheme.

The curve `Delta=0` is an irreducible rational plane quartic with an ordinary
node at `(0,0)`, two conjugate `A2` cusps over `Q(sqrt(-3))`, and one smooth
point at infinity where the line at infinity has contact order four. Its two
nodal branches have slopes `+8i` and `-8i`. The flat limits of the generic
degenerate normal line along those branches are respectively the campaign
fan rays `u=+8iv` and `u=-8iv`. This is an incidence between distinct
objects, not an identification of their parameter spaces.

## 3. Why the two objects differ

The seven rows are not a `J`-filtered standard basis. The independently
computed full initial ideal satisfies

```text
(Q_1,...,Q_7) proper-subset in_J(I),
(e0,e1,e2,e5)^5 subset in_J(I).                     (3.1)
```

One computed homogeneous generating set for `in_J(I)` contains 100 elements
outside `(Q_1,...,Q_7)`. The two ideals agree after passage to `Q(S,T)`, so
their difference is base torsion. For the first extra cubic `g`, the review
locates that torsion exactly on the quartic by the base annihilator

```text
((Q_1,...,Q_7):g) intersect Q[S,T] = (Delta).        (3.2)
```

This explains why the quadratic system degenerates precisely on `Delta`
while the true projectivized normal cone remains empty there. No occurrence
of “normal-cone quartic” may be consumed without this distinction.

## 4. Binding corrections and campaign interfaces

1. Replace every use of `NORMAL-CONE-ATLAS` for the quartic by
   `SECOND-NORMAL-JET-ATLAS` or `QUADRATIC-RESONANCE-DISCRIMINANT`.
2. On the campaign cone `x(a,b,u,v)`, only the linearized normal coordinate
   is `(4u,-v,u,2v)`. The literal tuple `(f0,f1,f2,f5)(x)` contains higher
   tangential terms and must not be replaced by that linear tuple.
3. The frozen `e=3,m=1` fixture has
   `ord_tau Delta(d4(tau),d3(tau))=4`. Its branch expansion is
   `d4=8i*d3-4*d3^2-16i*d3^3+O(d3^4)`, so the prior `+4i*d3^3` prediction is
   false. Only the weaker nodal-tangency mechanism survives.
4. The matching coefficient 64 in the tangential node and transverse fan is
   coordinate-dependent. The promoted statement is the sign-matched
   flat-limit incidence above, not equality of the loci.

The unloaded ideal `I` is not set to zero by the loaded source
`R+K10*A10+K6*A6+K2*A2-targets=0`. Nothing here licenses loaded recentering,
coefficientwise cancellation, an arc, reachability, a map, a counterexample,
or JC2.

## 5. Remaining obligations

- The review contains exact recomputation outcomes but does not ship its
  transient Singular scripts, the 114-generator initial ideal, or portable
  reduction certificates. Archive a deterministic replay before relying on
  the counts, multiplicity-four refinement, or annihilator calculation in a
  later machine pipeline. This is a reproducibility obligation, not an open
  mathematical verdict in the reviewed packet.
- Card B's surviving question is the exact intersection of
  `A10|_D=0` with `Delta=0` beyond the origin. It is not decided here.
- A contact census for the frozen `e=2,m=1` survivors may test whether the
  measured order four is structural. It cannot change (1.1)--(1.3).
- The full affine normal-cone scheme, its minimal presentation, integral
  closures, and Rees valuations remain undescribed. Characteristic-positive
  fibres are outside the quartic theorem's scope.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7992`.
- Body SHA-256:
  `d026eb793bf1a79711e3d485ac3d634033d66c8897a7e589b31eca984337a074`.
- Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`.
