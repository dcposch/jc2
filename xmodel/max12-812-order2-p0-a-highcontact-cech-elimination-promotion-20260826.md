# `(8,12)` order two: fixed-`p=0` high-contact `A`-chart elimination

Date: 2026-08-26

Status: **PROMOTED EXACT NECESSARY THEOREM ON THE TWO NAMED HIGH-CONTACT
OPENS; NOT A COMPLETE COLLISION ČECH COVER.**

## Promoted theorem

After the sharp third-tail gate has forced `M=0`, specialize the complete
seven-row source to `p=0` before any localization by `p`.  On the residual
high-contact cone

```text
Lambda=sigma^2,
R=sigma^2*(B0+sigma*B1+...),
C=sigma^4*(E0+sigma*E1+...),
A=A0+sigma*A1+...,
k10=k0+sigma*k1+...,
```

with `k0 != 0`, write

```text
A0=a1*z+a0,       B0=bs0*z+br0/4,       E0=(e1*z+e0)/2.
```

On `D(a0)`, the sixth complete raw row at absolute sigma-grade fifteen is

```text
g15[6]=-(1/16)*a0^3,
```

and is a unit.  On `V(a0) intersect D(a1)`, two complete raw grade-fourteen
rows are

```text
g14[2]=-(3/32)*a1^2*br0,
g14[1]= (3/8)*a1*(e0-a1*bs0).
```

They force `br0=0` and `e0=a1*bs0` by raw triangular pivots.  After only
those two equations, the third grade-fifteen row is

```text
g15[3]=-(1/16)*a1^3,
```

a unit on `D(a1)`.  Hence both leading-`A` opens of this fixed-`p=0`
high-contact cone are empty.

## Custody and review

The producer result, evidence manifest, and source freeze have SHA-256

```text
0db373023a1431e3d8e266faa9b52f90a886627fa77aae1c6738f860b86853dd
bf9d044082ad94e5a46d7b4b8c32638bd6381cfef7964409aa91d06bd805799c
9b162c2dff50a6ca51d0eb0e7bb352379a5e0cd5f33acb7ce9c020ab7128b8a5
```

respectively.  Exact Q on Box03 and an independent `F_65521` control on
r6d both passed with zero swap.  Every frozen tail, correction, lower load,
and target remained in the emitter; source census and termwise valuation
show `k6,k2` and targets cannot enter grades fourteen/fifteen at `p=0`.

The independent hostile review is **CONFIRMED**, SHA-256
`1c1981db8919fb53953b93e8831d223b46c08a02b000e291cfbba9637f66b64d`.
It rederived all fourteen source/Faber rows and the four displayed identities
over Q, checked the raw pivots and dual-lane custody, and confirmed that no
generic V12/unbounded-`d=1` or eight-form fan claim was consumed.  The failed
Claude launch is deployment-negative quota custody only.

## Exact firewall

This theorem requires `ord_sigma(R)>=2` and `ord_sigma(C)>=4` in the stated
secondary high-contact scaling.  It does not cover the lower collision
Newton cones `ord_sigma(R)=1` or `ord_sigma(C)=1,2,3`, and therefore does
not yet establish emptiness of the full `D_+(a0)` or `D_+(a1)` charts in an
iterated total Rees blowup.  It also does not prove total unspecialized-`p`
glue, positive-valuation moving-`p` exclusion, the all-zero exact-square/
all-load receiver, `k0=0`, terminal/Taylor closure, the whole square branch,
order two, maximum twelve, or JC2.

The next task is a complete fixed-`p=0` low-contact fan for the two `A` opens,
formed from the full source with `R` contact one and `C` contacts one through
three, followed by exact base-change maps from the total Kummer/Rees family.
