# Hostile review prompt: degree-one influence on three displayed AS Q3 fibres

Review the frozen producer and case as an adversarial algebra/source audit.  Do not trust the prose summary, cached ranks, or producer assertions.  Do not edit producer bytes.

Primary report:

`xmodel/as-fonly-d7-q3-q2q1-degree1-influence-producer-20260825.md`

Case:

`cases/as_fonly_d7_q3_q2q1_degree1_influence_20260825/`

Required output:

`xmodel/as-fonly-d7-q3-q2q1-degree1-influence-review-grok-20260825.md`

## Charges

1. Pin and inspect the consumed parent `solve_two_level.py` at SHA-256 `cec50f9fd9bf9ce3311e5687658473e9c4d7577b4997ac96b1e1f3b3932d2fcd` and its reviewed Q3 provenance.  Check orientation, coefficient indexing, integer determinant construction, and the meaning of the three pinned model files.
2. Verify that the successor adds exactly all homogeneous degree-one forms `(W1,Z1)` at order 27 and `(H1,J1)` at order 81, without overwriting or dropping the degree-three/degree-two and Q3-fibre coordinates.
3. Check the 91-slot inventory independently: every coefficient from total degree 0 through 12, including degree zero and all 63 slots of degrees 7 through 12.  Check exact divisibility before every `/27` and `/81` reduction.
4. Check the chronology: the complete `/27` active system is solved first, all 81 accepted active assignments are enumerated exactly, and the `/81` systems retain every inactive coordinate.  Audit the complete-design affinity premise rather than assuming it.
5. Reproduce the three rank pairs, the 243-branch count, the row-8 coordinate `[x^2y]`, its zero coefficient row, constants `2,2,1`, and zero survivor count.
6. Audit the negative control.  Confirm from the literal matrices that added degree-one columns occur at `/27` and `/81`, alter non-row8 equations/ranks, yet never alter row 8.  An implementation that merely appends unused variables must fail review.
7. Check the constant-output claim: their derivatives are identically zero, so exclusion from the variable list is algebraic rather than a normalization assumption.
8. Audit completeness of the **degree-at-most-three derivative-effect inventory** on each displayed fibre: the reviewed degree-three/degree-two blocks, the new degree-one blocks, and derivative-zero constants.  Look for any missing nonhomogeneous, cross-order, source, reparametrization, or carry direction that can affect a `/27` or `/81` row.  If completeness is not source-licensed, stop the theorem at the explicitly displayed blocks.
9. Read `xmodel/as-fonly-linear-normalization-lemma-20260825.md` and audit its composition with the global normalized predecessor chart.  Distinguish sharply between (a) normalizing a genuine complete `Z_3` determinant-one map somewhere in the full normalized scheme and (b) preserving or exhausting one incomplete pinned predecessor fibre.  The lemma alone must not turn the three points into global coverage.
10. State scope exactly.  The maximum promotable claim without an additional chart-composition theorem is exclusion of the displayed degree-three/degree-two/degree-one digit families over these three pinned predecessor fibres.  Refuse whole structural-base, global predecessor-scheme, `79 -> 76`, all-depth, counterexample, and JC2 claims.

If you run substantive replay, obey the campaign AWS-only policy and record host, immutable job path, source hashes, commands, return codes, stdout/stderr hashes, and resource use.  A no-Bash mathematical/source review is also acceptable if its limitations are explicit.

Return `CONFIRMED`, `CONFIRMED_WITH_REPAIRS`, or `REJECTED`, with every issue classified as mathematical, source-typing, software, custody, or wording/scope.
