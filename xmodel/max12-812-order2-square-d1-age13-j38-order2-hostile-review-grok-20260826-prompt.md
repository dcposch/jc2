# Hostile review charge: D1 `a>=13` grade-38 order-two recurrence

You are the independent hostile reviewer. Work read-only except for the one
required report. Do not trust producer PASS markers, validator strings, or
finite-field agreement as mathematical authority. Rehash custody and
rederive the claim from the frozen seven-row source.

Repository: `/Users/dc/code/math/jc2`.

Required report path (write exactly this file and no other file):

```text
xmodel/max12-812-order2-square-d1-age13-j38-order2-hostile-review-grok-20260826.md
```

Primary charged artifacts:

```text
09ca321436f4262b680801b74dc365cdff632c7630b65da50476547e5229139c
  cases/max12_812_order2_square_owner_d1_age13_j38_odd_recurrence_20260826/RESULT.md
0a65d8b2e792fc7e68e9a7aa17995a51b2eb38f09fe19845b77cbeb343757491
  cases/max12_812_order2_square_owner_d1_age13_j38_odd_recurrence_20260826/EVIDENCE.sha256
956090162a137a4afe3f4544f8d41c5622d38ec5627f1669f58a219c45a42358
  cases/max12_812_order2_square_owner_d1_age13_j38_odd_recurrence_20260826/FREEZE.sha256
246f6279b7801d21896936531c706dea3d22f42fa2c702f11eb6ec74e011bff5
  cases/max12_812_order2_square_owner_d1_age13_j38_odd_recurrence_20260826/compile_age13_j38.py
```

Charge all of the following.

1. Rehash every entry in `FREEZE.sha256` and `EVIDENCE.sha256`; distinguish
   exact Q (the theorem endpoint) from the independently compiled
   `F_65521` software control. Check engine rc, diagnostics, inputs, and zero
   swap.
2. Independently reconstruct all seven literal source rows from the frozen
   tails and D1 substitutions. Verify the parenthesized `k10,k6,k2` loads,
   moving `p(sigma)`, all required normal jets, and sparse targets through
   absolute grade 38. Find the smallest omitted monomial if there is one.
3. Audit the complete baseline `a=r=13,c=14` source inventory through grade
   38: `k6*C/L` at 31, `k2*R/L` at 35, `A*C/L` at 37, and
   `C^2/L^2`, `k10*R*C/L`, `k2*A/L^2` at 38. Confirm that the first
   triple-pole `k2*C/L^3` begins at 39 and every other pole-order-three or
   higher family begins no earlier.
4. Rederive, not merely replay, the moving pole-order-at-most-two odd-row
   syzygy

   ```text
   Phi7-(p(sigma)/4)Phi5-(p(sigma)^2/32)Phi3
       -(p(sigma)^3/128)Phi1 = 0 mod sigma^39
   ```

   for the source-only rows. Verify the signs and all coefficients.
5. Verify that the full row-seven target changes the relation by exactly
   `-sigma^38*J/4`, while the even targets cannot enter this odd relation.
   Check that adjoining `iJ*J-1` gives the unit ideal before radicals and
   that `J` is the nonzero Keller constant on the named `D(J)` chart, not a
   dispensable source variable.
6. Audit homogeneous coverage. Explain why
   `A=sigma^13 theta Abar`, `C=sigma^14 theta Cbar`,
   `R=sigma^13 theta eta Rbar`, followed by
   `theta=sigma^n,eta=sigma^s`, covers every `a>=13,r>=a` after common
   ramification and cannot move an omitted wall earlier. Test for a hidden
   assumption that theta or eta is a unit.
7. Keep the separately compiled `a=12` calculation sharply scoped. Check
   that the order-two recurrence is broken by the grade-38 leading
   `k2*C/L^3` term and that this is only a negative control for the proof
   method, not evidence that `a=12` survives.
8. Check the Chebyshev/Pell positive control and the precise firewall. No
   claim beyond the registered D1 source cone on `D(J)` is allowed.

Classify every issue as a mathematical defect, custody/software defect, or
scope wording defect. State the smallest failing identity and smallest
missing source monomial if any. End with exactly one verdict word on its own
line: `CONFIRMED`, `REFUTED`, or `INCONCLUSIVE`.
