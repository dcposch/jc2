# FULL0925 Astra adversarial cross — no vacant-slot successors

September13,2026. Actual first09:57:14UTC; original reserve10:10/HARD10:13.
MANUAL scope/decision review, UNPROMOTED. The three terminal blinds and
POSTBLIND were authenticated before reading and read WHOLE. Exact personal
WHOLE APP eaa26e23 and COORD77ec0b reads are expressly reused with fresh
hashes. One combined12k-token response clipped in transit; separate complete
Fable and POSTBLIND reads recovered every affected byte. No live peer cross,
extra linked source, network, scientific execution, AWS, shared edit or agent.
The unchanged pinned administrative finalizer is the sole Python execution.

## 1. Object-level merge

| Merged object and missing implication | Classification; decisive test disposition |
|---|---|
| ROOT arithmetic bridge: small geometric degree in characteristic p implying invertibility | KNOWN OPEN in supplied POSTBLIND. The proposed source-scope test has already returned no applicable theorem; STOP, not a characteristic/prime census. |
| Fable quadratic deck, fixed-point absence and rational linearization | KNOWN/DUPLICATE by POSTBLIND's precise historical attachment. Neither the normal form nor polynomial primitive removes affine poles or supplies a plane target. |
| Fable invariant ring of the displayed S involution | KNOWN/DUPLICATE. Exact two-chart descent answers the proposed experiment below; no CAS allocation. |
| Fable minimal Möbius family | DUPLICATE in the sole-pole/order<=2 regime after the necessary area test; broader poles are outside that conclusion, not selected successors. |
| Fable rational-pencil/target-line genus comparison | SCOPE-CONFLICT. These are different families of curves; there is no asserted degree or divisor comparison connecting their genera. |
| Astra marked source-line action and ROOT Hamiltonian deformation | KNOWN action/deformation mechanisms. The displayed control passes the marking; infinitesimal shear generation supplies no bounded polynomial integration. |
| Astra relative tangent slice | KNOWN graph-PDE fingerprint for graph slices; otherwise GAP, not a new construction. Needs an actual coordinate, constant restricted bracket and an actual collision. Absolute slices add no candidate. |

No merged card supplies a NEW global mechanism. Classification uses objects
and missing arrows, not agreement among model names. ROOT's history claims
are documentary attachments supplied for this cross, not fresh primary
source verifications by me.

## 2. S invariant ring: CONFIRMED

Write T={t^2-1=x^2 Z} and sigma(x,t,Z)=(-x,-t,Z). The displayed map

    i_-(x,y)=(x,x^2 y-1,x^2 y^2-2y)

identifies A2 with U=T minus {x=0,t=1}. Its inverse is
y=(t+1)/x^2 where x!=0 and y=Z/(t-1) where t!=1; these formulas agree
on the overlap and cover U. The second chart sigma(U) removes the OTHER
line {x=0,t=-1}. The two omitted lines are disjoint, hence U and sigma(U)
cover all T, not merely a codimension-two complement.

The induced rational involution on the first chart is exactly
tau(x,y)=(-x,2/x^2-y). A polynomial P fixed as a rational function by tau
is regular on U and, by invariance, on sigma(U); therefore it is globally
regular on T and sigma-invariant. Conversely every invariant global function
pulls back to an invariant polynomial. This proves the potentially delicate
extension direction without assuming that tau is a regular A2 automorphism.

Because the equation is monic quadratic in t, each global T-function has a
unique form a(x,Z)+t b(x,Z). Invariance makes a even and b odd in x.
The invariant ring is generated by x^2, xt, Z, with relation

    (xt)^2=x^2+x^4 Z.

Pullback gives exactly C[x^2,x^3 y-x,x^2 y^2-2y], the stated S ring.
Thus the strict-inclusion alternative proposed as a one-hour experiment is
REFUTED. This is equality of unrestricted rings; it does not identify
ordinary-degree cutoffs or every ansatz under changes of coordinates. It
does not construct a scalar pair or exclude unrestricted S pairs. ROOT and
I use the same model: this check is NOT a different-model promotion gate.

## 3. Minimal Möbius scope: CONFIRMED, not a general classification

For an ACTUAL constant-nonzero-Jacobian pair F with F composed with tau=F,
the rational chain rule gives J(tau)=1. For tau=(-x,mu_x(y)), this is
-partial_y mu=1. In characteristic zero, mu=-y+h(x), h in C(x). A genuine
y-dependent fractional-linear denominator is therefore incompatible with
the necessary Jacobian condition. The involution identity then gives
h(-x)=h(x).

Assume additionally that the ONLY possible finite pole of h is x=0 and
its order is at most2. Evenness forces h=2k(x^2)+c/x^2. If c=0, tau is
regular at (0,k(0)) and fixes that point. A nontrivial deck transformation
of an actual étale map cannot have such a regular fixed point: the local
inverse of F makes its germ the identity, hence its rational map the
identity. Thus c!=0. The polynomial source coordinate

    v=(2/c)(y-k(x^2))

has constant nonzero Jacobian and conjugates tau to (-x,2/x^2-v).
This is precisely the old unrestricted S candidate after a polynomial
source automorphism and harmless constant bracket rescaling. Geometric
mapping degree is preserved; ordinary displayed degrees need not be.

More finite poles, higher pole order and general rational-coordinate
linearizations are NOT classified. In particular a Cremona coordinate a
is not automatically a polynomial source coordinate x. No general d1=2
closure, no classification of pseudo-planes and no next pole-order lane
follow. Fable's second minimal-family experiment has no changed candidate
space in this regime, but its broader unexamined regimes remain open.

## 4. Factorization, degree conventions and other surviving gaps

Fable's useful factorization observation is sound with qualifications.
For an actual Keller F and intermediate K, the normalization B of C[f,g]
in K lies in C[x,y]: its elements are integral over the normal source ring.
Thus p:A2->Y=Spec B exists. It is unramified since relative differentials
are a quotient of those for F. At closed points, the residue fields are C;
unramifiedness gives the claimed surjection on completions. Their normal
two-dimensional local domains have equal dimension, so the kernel is zero.
Consequently p is étale, its image S' is open and smooth, and the second
leg is étale on S'. This does NOT make p finite or an everywhere two-sheeted
topological covering. It does not identify S' with S, prove S' affine or
establish the additional properties of a classified pseudo-plane.

Here d1=[C(x,y):K]=2 and d2=[K:C(f,g)] are generic field degrees,
N=2d2. Retain the accepted second-leg d2=2/3 and Galois stops; a fixed-S
scalar pair consequently requires the stated surviving d2>=4 regime.
The quadratic first extension being Galois is NOT the full Keller
extension or the second extension being Galois.

There is one additional GAP in calling this the same object as a
swap-stable rational off-diagonal component. A deck involution gives such
a component whose two source projections are BIRATIONAL. Rationality and
swap stability alone do not imply those degree-one projections. Without
that condition, the component is a correspondence, not an automorphism
of C(x,y); its involution does not automatically produce an index-two
intermediate subfield there. No existing collision GAP is closed.

The rational deck-pencil cannot contradict the promoted genus of general
target-line pullbacks: even a rational surface carries pencils of positive
genus, and the two divisor classes have not been identified. Nor does a
nonproperness-hypersurface normalization from the tangent construction
become the full-field normalization of an arbitrary plane Keller map.

My marked-action calculation remains non-discriminating: the same-a control
gives Delta b da=4 dx/x^2=d(-4/x), and the primitive is a difference of
the global polynomial -2xy. The marked coordinate does not repair the
missing ACTUAL PLANE TARGET. ROOT's infinitesimal Hamiltonian spanning is
likewise not finite-degree integration; no orbit-openness inference follows.

For my absolute W-slice screen, the kernel deduction is valid GIVEN the
stated geometrically connected smooth generic fibre and surjectivity of
(C,D): constants are its base function field, and a denominator divisor
would persist under the surjective smooth map. The literal fibre formula
is not freshly reconstructed from an extra source in this cross. Retain
only the conditional manual screen, not a new promoted family closure.
W(h)=c modulo(h) still needs an exhibited source-coordinate completion and
two distinct equal-output points on h=0. Neither is supplied. Graphs give
the old PDE; global slices pull back target coordinates already excluded
at the accepted tangent scope. No independent construction test survives.

ROOT's arithmetic source check is reported KNOWN OPEN; nothing supplied
permits dropping FINITE/GALOIS or replacing geometric degree by total degree.
It closes the proposed scope inquiry, not the arithmetic conjecture.

## 5. Allocation decision and all46 preservation

Next proof test: **NO_TEST**. Next genuinely different disproof test:
**NO_TEST**. The exact remaining endpoints are actual polynomial-plane-target
normality/properness, uniform all-degree source landing, and a complete
regular S/T pair or genuine complete-source point. Restating an endpoint
does not create a cheaper discriminator. The ring and minimal-family
questions are already answered; the genus proposal compares wrong objects;
relative slices have no new candidate. Do not refill the slots with scalar
exceptions, another pole order or a repeated control.

All46 mathematical scopes stay unchanged. Temporary dispositions remain:
LOWER1's scalar/preflight queue; retain6/7's narrow informational value from
the accepted tangent theorem without a new launch; LOWER16/29's unchanged
operator/flow queues and34/35's defeated construction descendants;
UNCHANGED2--5,8--15,17--28,30--33,36--46. In particular reject the proposed
allocation RAISE26/32 until a genuinely changed test exists, and do not
redesign4/33 into an automatic involution family.20's bounded source inquiry
is finished. No accepted degree frontier, cubic block, receiver, source
case, genus scope or protected-repository boundary is reopened.

## 6. One small procedural upgrade, no software change

Use bounded returned-output chunks, with the cap applying to the ENTIRE
tool response, not merely each constituent command. Evidence is direct:
my first four-file response clipped despite per-command output limits;
separate Fable and POSTBLIND calls returned both complete files without
clipping. Fable independently reported two recovered large-input reads.
Trial the existing sed/read mechanism with roughly4000-token chunks and
record zero unrecovered intervals; do not claim a measured speedup.

This changes an instruction, not the packet format: no duplicate half-file
snapshots, builder, controller or ledger. It does not excuse final-byte
readback after the last mathematical qualification. No third worker or
automatic preflight repair is authorized. Primary mathematical research
continues only through a separately selected changed idea; absence of a
selected test here is not absence of resources or proof that JC2 is blocked.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `11324`.
- Body SHA-256:
  `832d7a4a2f66c7b24b2d457e4deac133337a21ffcff035c9d786a078b4b9e63c`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
