# FIRST Fable 5.1 gate: conditional finite-eliminant/Nakayama bridge for the r2 highest forms

status=UNSEALED (no Seal, no charge_basis authored; adapter seals)
lane=f10-r2-modular-properness-gate-fable5-20260910
gate_model=claude-fable-5-1
first_action_utc=2026-09-10T14:43:34Z (hash command); stop = earlier of 15:01:34Z (first+18 min) and 15:03:00Z USER TERM (KILL 15:03:05Z); final-3-min reserve from 14:58:34Z; never reset
mode=STATIC; manual math/text/hash only; ZERO subprocess/import/AST/syntax/compile/test/CAS; tools date, ls, wc, sha256sum, cat, grep, apply_patch only
owned=xmodel/f10-r2-modular-properness-gate-fable5-20260910.md, box/f10-r2-modular-properness-gate-fable5-20260910/ (both absent at 14:43:34Z; unsealed skeleton first, then body)
charged_input=xmodel/f10-r2-modular-properness-route-astra-20260910.md
charged_input=box/f10-r2-highest-bezout-code-astra-20260910/CONTRACT.md
charged_input=xmodel/f10-r2-highest-bezout-code-gate-fable5-20260910.md
charged_input=box/f10-r2-reconstruction-code-astra-20260910/produce.py
charged_input=box/f10-r2-reconstruction-code-astra-20260910/arithmetic.py
charged_input=xmodel/f10-r2-infinity-forcing-final-gate-fable5-20260910.md
charged_input=xmodel/f10-r2-cyclic-core-astra-20260910.md
charged_input=xmodel/f10-r2-cyclic-core-gate-fable5-20260910.md

## 0. Custody, read scope, disclosure

All 8 snapshots in /tmp/jc2-lane.Ptcv3b/inputs (distinct basenames) were hashed by sha256sum at 14:43:34Z BEFORE any body read; all 8 digests equal the charged pins byte for byte (rows copied from the sha256sum output into box/input_custody.md; this body carries no hex digest). Both owned targets were absent at 14:43:34Z. All 8 were then read WHOLE in parallel single cat -n reads: Astra report (113 lines, ends at its Seal), CONTRACT.md (69), highest gate (67, at marker), infinity final gate (66, at marker), cyclic core (228, at Seal), cyclic gate (65, at marker), produce.py (285), arithmetic.py (277). No read was clipped, so no recovery read was needed. The baseline packet was not opened, hashed or parsed; its pin is a future literal only. The two older manual mod-89 partials are not inputs and none of their residues is used. No provenance, ledger, corpus, Git, network, process, other lane or live file was accessed; nothing was executed, imported, parsed or compiled. produce.py and arithmetic.py were read only to audit inversion/shape obligations, never run.

Disclosure: this is a different-model FIRST review of Astra's report. Three accepted imports (highest gate, infinity final gate, cyclic core gate) were authored by Fable 5.1 in earlier sessions; they are consumed at their exact stated scope and not re-proved. All eight reads were fresh in this session; no same-byte WHOLE reuse.

Accepted imports consumed: infinity gate gives characteristic-zero X1=0 origin-only over B; cyclic gate imports P7 irreducible over Q, B a degree-7 FIELD, and gives CONDITIONAL dim 35 / seven per class from the parameter-sequence hypothesis; highest gate gives the shapes, F/G degree<=5 and the denominator-free whole-chart identities. All actual coefficients, primes, roots, leaders and cofactors remain UNKNOWN here.

## 1. Verdicts

| item | verdict |
|---|---|
| A proper specialization lemma (report section 2); pi*u-1 contrast | CONFIRMED; no missing morphism/finite/local assumption |
| B finite-eliminant/Nakayama Route F (section 5) | CONFIRMED; lemma sound with nilpotents, no hidden integral-v or special-infinity hypothesis |
| C exact deg<=5, nonzero reduced u^5 leader, [u^5]F formula; Route P alternative | CONFIRMED for F; the analogous G formula has one extra term (refinement, not a defect) |
| D one finite place of the FIELD B; etale DVR presentation; product control | CONFIRMED; DVR/simple root is convenient, not necessary (refinement) |
| E wire integrality vs localized equality; producer inversion obligations | CONFIRMED; integrality is owed on the 20 z-free slots, support validation on all 24 (refinement) |
| F smallest honest certificate; INCONCLUSIVE semantics; scope | CONFIRMED CONDITIONAL GO; next discriminator = (f5,g5) residues with the corrected g5 |

Smallest blocking correction: none. REFUTED: nothing. Three refinements are recorded in C, D, E; none changes a verdict.

## A. Proper specialization lemma (report section 2)

CONFIRMED. Own replay. S=A[X1,X2,X3] with weights 1,2,3 and S'=A[X1^6,X2^3,X3^2]: S' is a polynomial ring on three generators of degree 6, so Proj S' = P2_A after regrading. S is finite over S' (each X_i is integral: X1^6, X2^3, X3^2 lie in S'), and the irrelevant ideal S'_+ S has radical S_+, so Proj S -> Proj S' is everywhere defined; on D_+(f), f in S' homogeneous, S'_f = S'_(f)[f,f^-1] because S' is generated in the degree of f's powers, hence the degree-0 part S_(f) of the finite graded S'_f-module S_f is finite over S'_(f). So Proj S -> P2_A is finite, hence proper, and Z = Proj S/(H5,H6,H7) is closed in Proj S, hence proper over Spec A. No flatness, reducedness or leader preservation enters. Fibre step: Z_k empty iff Z_{kbar} empty (faithfully flat field extension). Z -> Spec A proper is closed; if Z_B is nonempty the image contains the generic point, so its closure Spec A, so the closed point, so Z_k nonempty. Equivalently V(I) nonempty in a local Spec A forces I subset m. Purely topological, so nilpotents are retained. Over B: Proj(B[X]/(H)) empty iff rad(H) = (X1,X2,X3) (weighted Nullstellensatz over a field) iff the only geometric common zero is the origin, and this is stable under every field extension of B, including every complex embedding. That is exactly the cyclic core's parameter-sequence hypothesis (core :31-34, gate B). pi*u-1 contrast replayed: A[u]/(pi u-1)=A[1/pi]=B, special fibre k[u]/(0u-1)=0; closure pi X-Y in P1_A has generic point [1:pi], special point [1:0]; on Y!=0 the special fibre is empty and the generic Y=0 locus is empty, yet [1:pi] lies in the generic chart. So generic-infinity emptiness plus special-affine emptiness never composes without properness of the chart object. Missing assumptions: none; the only hypotheses used are A local (closed-point step), H_h in A[X] (the integrality obligation of section 4) and positive weights (Proj construction).

## B. Route F: finite eliminant plus Nakayama (report section 5)

CONFIRMED. Own proof. Let F,G in A[u]. Case 1: one of them is a unit of A, then (F,G)=A[u] trivially. Case 2: F has actual degree d>=1 with unit leading coefficient. Dividing by the unit, A[u]/(F) is free over A on 1,u,...,u^(d-1) (monic division algorithm over any ring), so M=A[u]/(F,G) is a quotient of it, finitely generated over A. Right-exactness of tensor gives M/mM = M tensor_A k = k[u]/(Fbar,Gbar), which is zero by cbar Fbar + dbar Gbar = 1. Nakayama for finitely generated modules over a local ring (no Noetherian hypothesis needed) gives M=0, i.e. (F,G)=A[u]. Nothing about nilpotents, torsion or points is used; the identity is a statement about cofactors in k[u], and cbar,dbar need not lift. Field/localization maps: only A -> k (reduction) and A subset B (inclusion); the eliminant identity is then read in A[u] subset B[u] subset B[u,v]. Generic step: the highest gate's identities F=Q^2H6-H5[QB1+B0(Qv-P)] and G=Q^2H7-H5[QC2+C1(Qv-P)] hold over any commutative ring (own expansion in the highest gate, section A), so 1 in (F,G)B[u] gives 1 in (H5,H6,H7)B[u,v]: the characteristic-zero chart X1=1 is empty over Bbar, weighted scaling moves every X1!=0 zero there, and the accepted characteristic-zero X1=0 endpoint leaves only the origin. Both facts are generic; no special-fibre infinity, integrality of v, unit B0/C1 or reduction of any H coefficient is needed. The proper object is Spec M -> Spec A, finite; Astra's phrasing is exact. Attack pi*u-1: F=pi u-1, G=0 gives Fbar=-1 and cbar=-1 passes the modular identity while (F) is proper; the actual degree is 1 with nonunit leader pi, so the hypothesis fails, and Case 1 fails since F is not constant. Every attempted counterexample must have all generators with nonunit leaders, which the hypothesis forbids; the lemma is therefore sharp on exactly the stated hypothesis. Degree cases: if [u^5]F lies in m but is nonzero, F has actual degree 5 with a nonunit leader and finiteness fails, so a lower REDUCED degree is worthless without an exact proof that the higher coefficients vanish in B, as the report says. If F=0 and G has positive actual degree, no modular identity can hold (Gbar has positive degree), consistent. Zero F with unit constant G is Case 1. Minimality: only F,G in A[u] is required; the H coefficients may stay in B provided F,G are the exact elements of B[u] defined by the highest gate's formulas (see E for the equal-ideal relaxation).

## C. Sufficient actual leader and the two separate routes (report sections 5, 7)

CONFIRMED with one refinement. Degree bound replayed from the shapes (deg P<=2, Q<=1, B3<=3, B1<=1, B0<=0, C3<=3, C2<=2, C1<=0): Q^2B3<=5, PQB1<=4, B0P^2<=4, so deg F<=5 exactly, and only Q^2B3 reaches degree 5, giving [u^5]F=([u]Q)^2[u^3]B3 as the report states. If F in A[u] and the residue of [u^5]F is nonzero in k, then that coefficient is a unit of the local ring A and F has actual degree 5 with unit leader: sufficient for B. Refinement for G: Q^2C3<=5 AND PQC2<=2+1+2=5, so [u^5]G=([u]Q)^2[u^3]C3-[u^2]P[u]Q[u^2]C2. The report only displays the F formula and never claims the G one, but a checker must not transplant it: the g5 residue needs both terms. A lower reduced degree is not enough (B). Route P is a genuinely separate sufficient route: reduce a full integral model, verify cbar Fbar+dbar Gbar=1 in k[u] (the identities hold over k, so the special affine chart including Q=0 is empty), and independently verify the same-model residues of alpha,beta,gamma nonzero; own replay of the X1=0 weight-forced shapes: 2a+3b=5 gives only X2X3, =6 gives X2^3 and X3^2, =7 gives only X2^2X3, and alpha X2X3=0 followed by beta X2^3+gamma X3^2=0 kills the remaining coordinate over kbar, delta unused. Then section 2 gives Z empty and the generic parameter sequence. Correct that the accepted characteristic-zero nonvanishing of alpha,beta,gamma cannot be pulled back to k (a nonzero element of B can lie in m), and correct that Route F needs no special boundary at all. No mixing of fibres occurs in either route.

## D. One finite place of the FIELD B (report section 3)

CONFIRMED with one refinement. With P7 monic and p-integral, O=Z_(p)[Z]/(P7) is free of rank 7 over Z_(p), torsion-free, with O tensor Q = B a field, so O is a domain with fraction field B. Own replay of the etale claim: for beta a simple root of P7bar in F_p, (O/pO) localized at (Z-beta) is F_p[Z]/(Z-beta)=F_p by CRT, so at n=(p,Z-beta~) the maximal ideal of O_n is pO_n: O_n is a one-dimensional regular local ring, a DVR with uniformizer p, residue F_p and fraction field B. Valid despite the nonmonic septic: produce.py:26-32 divides the literal septic by its leading coefficient and the packet/certificate modulus is that monic P7 (CONTRACT :36, :59); the ideal (P7) and hence B are unchanged by the rescaling, but only the MONIC representative makes O finite over Z_(p), so the literal monic P7 must be the certified object, as the report requires. Shape fact from the literal formulas only (not a coefficient assertion, still to be verified against the packet P7): the only non-integer constant is tau=2/7 and the leading coefficient -245*120*144*tau is the integer -1209600=-(2^8)(3^3)(5^2)7, so the monic P7 has denominators supported on {2,3,5,7}; p-integrality is automatic at any other p. Refinement: sections 2 and 5 need only a LOCAL DOMAIN A with fraction field B, and any localization O_n at any maximal ideal is one, simple root or not; the simple-root/DVR condition is convenient (it makes "locally integral" mean "in A" after normalization and makes k=F_p) but is not a hypothesis of either lemma. The report's caution that repeated-root/bad-order cases need extra proof is therefore stricter than necessary, not wrong. No map B -> F_p exists (characteristic), reduction is O_n -> F_p, and emptiness over B is emptiness under every complex embedding (A). Product control replayed: B=QxQ, e=(1,0), forms e(X1^5+X2X3), e(X2^3+X3^2), eX1^7: first component X1^7=0, then X2X3=0, then the remaining coordinate dies, origin only; second component all forms vanish, so a place of the first factor proves nothing about the whole. Degree 7 alone cannot exclude this; the FIELD premise is the accepted import. No statement about the prime 89 or any prime is derived here.

## E. Wire integrality versus localized equality (report section 4)

CONFIRMED with one refinement. The theta example replayed: theta^2=1+p, p odd, Z^2-1-p splits mod p with simple roots +-1, so Z[theta] is etale at p and n=(p,theta-1) is a DVR point; (theta-1)/p = 1/(theta+1) in Q(theta), theta+1 is congruent to 2, a unit at n, so the element lies in Z[theta]_n although its dense coordinates (-1/p,1/p) have p-denominators; at the conjugate place theta+1 is congruent to 0 and it is not integral. So "dense denominators prime to p" is sufficient and not necessary for membership in A, a replacement must be proved EQUAL in B and its denominator proved a unit of A, and a nonunit rescaling gives a different model whose own special fibre must be certified. All correct. Relaxation, valid by B: Route F may use any lambda F, mu G with lambda,mu in B^x, since they generate the same B[u]-ideal; equality up to a declared B^x-scalar is enough, literal equality is not required. Scope refinement: the model H_h=Psihat_h(X,0) is defined by the z-free terms only, so integrality is owed on at most 20 B-coefficients (140 rational coordinates) plus P7, while support/weight rejection (no S/vartheta, exact weight) must read all <=24 slots (168 coordinates) before discarding positive z-degree; own count of weight-h monomials in (X1,X2,X3,z) with weights (1,2,3,5): z-free 5/7/8, with z 1/1/2, totals 6/8/10=24. For Route F the minimal integrality set is the <=12 coefficients of F,G (84 coordinates) if F,G are first computed exactly over B. Producer inversion audit (no output read): field inversions dL (produce.py:34), W (:36), t5 (:41), H7 (:43), matrix pivots gap1-4 (:91, arithmetic.py:218), completions at h=6,7 (:147, arithmetic.py:240-248) are B-elements of unknown valuation and each needs a unit check at the place; the purely rational divisions are 1/lead (:32), -1/210 (:35), series binomials with exponents 12/7, 15/7, -2 (:39,:42,:139; arithmetic.py:193), integrate 1/(e5+1)<=1/7 (:200), 1/2 (:144), 4/15 and -15/4 (:96-98), 1/10 (:160), upper pivots 7j-3(12-h) with |pivot|<=33 (:61-64), and Euler pivots j-3e[4] with |pivot|<=48 (:202-205). So every rational pivot is an integer of magnitude at most 48 or a fixed small fraction: automatically a unit at any p above that bound, whereas the field inversions are never automatic. A mod-p replay of the recurrence proves equality with the exact B-forms only if every field inversion is certified an A-unit from the exact packet 'units' pairs (:262) and every intermediate stays in A; without that it proves nothing, as the report says. Cofactor bounds replayed: c,d of degree<=5 are <=12 residue-field scalars, the identity checks 11 u-powers; the exact instrument's 84 rational coordinates and 77 positions are 12*7 and 11*7. These are certificate shape bounds, not speed or height claims.

## F. Smallest honest certificate, scope, decision

CONFIRMED, CONDITIONAL GO for the design; nothing is executed or authorized. Minimal Route F certificate: baseline hash/bytes and tier; literal monic P7 plus the FIELD import; p with a primality certificate; beta with P7bar(beta)=0 and P7bar'(beta)!=0 (or a verified residue extension), or, by D, any maximal ideal of the monic order with its residue field; integrality evidence for the 20 z-free coefficients or for F,G directly, with any B^x-rescaling declared; independent re-extraction of the whole three rows with the S/vartheta/weight rejections before projection; independent F,G; the reduced (f5,g5) with the corrected g5, or an exact characteristic-zero proof of vanishing above the reduced degree; cbar,dbar and the 11-position identity in k[u]. Route P replaces the leader item by same-model alpha,beta,gamma residues. Failure of any item (nonempty special fibre, nonunit denominator, both leaders zero mod p without exact vanishing, failed cofactor) is INCONCLUSIVE: the empty-chart control P=1,Q=0,H6=H7=0 gives F=G=0 with H5=1, so certificate failure is never a source point or a non-regularity. Success licenses exactly V(H5,H6,H7)={0} over Bbar, hence the cyclic gate's conditional gr R equality, dimension 35 and seven per class, and nothing more: no basis, no 7x105 membership, no source point/unit, no all-r, no JC2. Next cheapest discriminator: at the first place passing the integrality audit, the pair of residues (f5,g5); one nonzero settles the finite-model obstacle and only the modular Euclid remains; both zero sends the work to exact higher-coefficient vanishing or to Route P. The exact rational-B Bezout instrument remains the accepted fallback. No worker, prime search, registration or execution is authorized by this gate.

## OPEN(S) RAISED

None. No new canonical OPEN. The retained unknowns are the producer's own: the actual place, integrality audit, (f5,g5), the modular cofactors, and the original highest regularity question. No charge_basis, no follow-on authority, no new global theorem.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly xmodel/f10-r2-modular-properness-gate-fable5-20260910.md and box/f10-r2-modular-properness-gate-fable5-20260910/input_custody.md, both absent at 14:43:34Z; no shared, frozen, input or old file was touched; no corpus scan, no other lane, no network, no scientific subprocess, no Git.

## Completion

Authoring attestation: every byte of both owned files was written by apply_patch (skeleton and custody 14:49:21Z, sections 0-1/A-B 14:50:07Z, sections C-F/OPENS/COLLISIONS 14:51:52Z); no Write/Edit tool, no shell redirection, no helper, no source edit, no execution, no network, no Git. Honest patch record: the 14:51:52Z append was anchored on a blank context line and landed at the top of the file; it was removed by an apply_patch delete at 14:53:31Z and re-appended verbatim after section B at 14:54:25Z, with the heading order, line count 71 and word count 2679 verified equal to the intended body; no input or shared file was involved. Own WHOLE reread of this report (header, sections 0-1, A-F, OPEN(S) RAISED none, COLLISIONS EMPTY) was done at 14:54:25Z before this block; the eight custody rows in box/input_custody.md were rechecked against a fresh sha256sum of the snapshot directory (each row found exactly once); the body was confirmed to carry no 64-hex digest and the marker was confirmed absent before it was appended. All work finished before the 14:58:34Z reserve; stop 15:01:34Z, never reset. No Seal and no charge_basis are authored; the adapter seals. The marker below is the only standalone marker in this file and nothing follows it. All writers idle at the marker.

<!-- BODY-END -->
