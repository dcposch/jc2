# Binding integration: normal F5 carrier/effectivity reduction

Date: 2026-08-30 UTC  
Coordinator: Sol 5.6 Ultra (`/root`)  
Frozen basis: `b491ba9951471a67b9e2edad930eb95002e56c04`  
Lifecycle: **BINDING COORDINATOR INTEGRATION**

## 0. Decision

Promote the corrected normal-F5 carrier packet, with every repair required by
the hostile review.  In the charged normal proper-cubic-block scope, an
`S`-adapted contraction gives an exact nine-blowup marking over `F_2`; the
singular F5 point has a finite list of necessary marked fibre/ADE tags; the
binding Euler theorem removes the `A8` tag; and the remaining divisor classes,
carrier compatibility graphs, degree equations, residual caps, and genus
identity form a finite necessary numerical/combinatorial threat state.

The promoted singular list contains **eight**, not nine, tags.  No tag or
compatible carrier pair is asserted effective.  The singular local different
and the coefficients of contracted ramification carriers remain open.

One review correction changes a mathematical formula.  A target line through
the image of a contracted carrier necessarily contains that carrier in its
pullback.  The raw target cap on `n` is therefore invalid there.  The binding
formula first removes every contracted ramification carrier over that target
point and bounds only the residual vector `n-v`.

## 1. Frozen evidence

This integration binds the producer together with its mandatory corrigendum:

```text
f942e743dc206d958f31892e838d7517fe23eacbc863b56c9441b60152bc0cb4
  xmodel/bd-a2-normal-f5-decorated-carrier-effectivity-reduction-sol56-20260830.md
  body e4823537bbaa3bb6142790456c11814d9be32c8b3f4c93f2563a2a13eb2a641d

94f5bc5ce509809b951f5189bb6d3f863c09133dfda77522965d791a7419ee2e
  xmodel/bd-a2-normal-f5-contracted-carrier-compatibility-corrigendum-sol56-20260830.md
  body 488da252288cd9cc14ea797529d49b2d9db3c0a74bcab4308965ac07205182b6
```

and the immutable hostile review:

```text
2d4740a7cc9c67a9d9865526ca3a462de6ab6200e3cf316bcac52036433766b6
  xmodel/bd-a2-normal-f5-decorated-carrier-effectivity-reduction-hostile-review-gpt55-20260830.md
  body/raw receipt 93fd7e936fc2f14add9d893130d37e64dc20e3fc7eb66cb741221d0da75fe63a
  receipt e63ef2ddc761ebc89b0c358f4bd6bf269b79eddae88f784d8f579ed810403ee1
  log 1c774899f64fea4152310321594829c14f598f52ab4de6bc38cc30761436e1b1
  verdict CONFIRM_WITH_CORRECTIONS
```

The review independently reconstructed the F5 degrees and adjunction, fibre
saturation, crepant delta calculation, contraction to `F_2`, all ruled
classes, the physical marked local table, the corrected carrier graphs,
ramification coefficients, residual identities, class-group injection,
support genus, finiteness, and the Euler intersection.  Its receipt reproduces
the prompt, adapter, launcher, sandbox, charge-basis validator, fallacy file,
raw report, and log hashes before the coordinator seal.

The binding upstream integrations are the singular-F5 bridge `f385387d...`,
the global D9/fibre ledger `8b1404cc...`, the A1-ruling/Euler theorem
`f092a715...`, and the smooth local-different theorem `8fcd071f...`.

## 2. Exact adapted marking

On the minimal resolution write

```text
A^2=3,       A.B=2,       B^2=0,       K=-A+B,
H=L+S+T=F5=(0,1)+(1,1)+(1,1).
```

Each F5 component has `A`-degree one, and adjunction gives

```text
L^2=-1,       S^2=T^2=-2.
```

Choose either section branch as `S`.  Contract fibre components disjoint from
`S`; the relative-minimal endpoint is exactly `F_2`, with `S` its negative
section.  Since `K_Xtilde^2=-1` and `K_F2^2=8`, precisely nine contractions
occur.  Reversing them, with infinitely-near centres allowed, gives the
orthogonal total-transform basis

```text
S=S_0,       B=F,
A=2S_0+5F-sum_(i=1)^9 E_i.                              (2.1)
```

If `p_0` is smooth, its original fibre is `B_1` and, after relabelling,

```text
L=F-E_l,
T=S_0+4F-sum_(i!=l)E_i.                                (2.2)
```

If `p_0` is Du Val, `L=E_l`.  Target-line saturation forces each boundary
branch to attach transversely at a smooth exceptional point with one unit.
The actual scheme-fibre equation then gives `L.S=L.T=0`.  Crepancy and the
F5 delta invariant give

```text
q=h^t a=6  =>  S.T=1;
q=h^t a=8  =>  S.T=0.                                  (2.3)
```

Writing `h_S` for the retained weight-one fibre component's exceptional
coefficient yields

```text
q=6: h_S=2,
     T=S_0+3F-sum_(i in I_6)E_i,       |I_6|=6,
     Z_H=2F-2E_l-E_j-E_k;

q=8: h_S=3,
     T=S_0+2F-sum_(i in I_4)E_i,       |I_4|=4,
     Z_H=3F-2E_l-E_j-E_k-E_u-E_v.     (2.4)
```

These are total-transform class equations.  Binary centre subsets do not
erase chronological proximity.  For `q=6`, the three unit branch attachments
occupy two physical smooth points: one for `L`, and one shared by `S,T`.  For
`q=8`, they occupy three physical smooth points.  This replaces the
producer's erroneous phrase “three prescribed smooth attachment points.”

## 3. Local marked tags and Euler intersection

Before the Euler filter, the physical fibre diagrams and local Cartan rows
give exactly the nine necessary singular tags

```text
q=6: B_3/A_2, U_3/A_3;
q=8: B_5/A_4, B_6/A_5, B_7/A_6, B_8/A_7, B_9/A_8,
     U_5/D_5, U_6/D_6.                                  (3.1)
```

The balanced `B_4/A_3` row dies because the singleton contact is internal;
the marked `U_3/A_3` row survives because `L` meets the central vertex and
the sections share a spin point.  The `U_4/D_4` row has distinct spin
contacts and hence `S.T=0`, contradicting its required `q=6` value.  Repeated
units on one exceptional component are distinct physical contacts, not a
tangent or a doubled germ.

The reduced-infinity adjunction energy is unconditional:

```text
sum_trees (h-1)^t C(h-1)<=4,
F5 tree contribution=(h-1)^t C(h-1)=q-4.               (3.2)
```

No ADE point lies on `H` away from `p_0`, because every such point would make
the crepant delta contribution positive while the F5 curve is smooth there.

Now let `r_0` be the ADE rank at `p_0`, `r_aff` the total affine ADE rank, and
`k` the number of distinct reduced nonexceptional ramification primes.  The
binding Euler theorem gives

```text
r_0+r_aff+k<=8,
new A1-ruling base P1 => r_0+r_aff+k<=7.                (3.3)
```

Consequently `B_9/A_8` is impossible, and the binding singular list is

```text
B_3/A_2, U_3/A_3,
B_5/A_4, B_6/A_5, B_7/A_6, B_8/A_7,
U_5/D_5, U_6/D_6.                                      (3.4)
```

The residual table is:

| state | `r_0` | general `r_aff+k` | new-ruling `P1` base |
|---|---:|---:|---:|
| smooth `B_1` | 0 | `<=8`, `k>=2` | `<=7` |
| `B_3/A_2` | 2 | `<=6` | `<=5` |
| `U_3/A_3` | 3 | `<=5` | `<=4` |
| `B_5/A_4` | 4 | `<=4` | `<=3` |
| `B_6/A_5`, `U_5/D_5` | 5 | `<=3` | `<=2` |
| `B_7/A_6`, `U_6/D_6` | 6 | `<=2` | `<=1` |
| `B_8/A_7` | 7 | exactly `1` | impossible |

In the `A7` row, `(r_aff,k)=(0,1)`; the sole prime is noncontracted, has
`A`-degree eight, the new ruling has base `A1`, and every new-ruling fibre is
irreducible in reduced support.  These statements concern the adapted new
`A1` ruling, not the original conic fibre class `B`.

## 4. Contracted carrier classes

Every nonexceptional prime contracted by `pi` to an affine target point has

```text
A.Z=0,       B.Z=1,       Z^2=-3,
Z_J=S_0+2F-sum_(j in J)E_j,       |J|=5.               (4.1)
```

Proximity and effectivity remain separate.  In the three marked states the
raw numerical compatibility graphs are exactly

| state | candidates | compatible pairs | clique number |
|---|---:|---:|---:|
| smooth | 70 | 560 | 2 |
| `q=6` | 20 | 10 | 2 |
| `q=8` | 24 | 36 | 2 |

The original unrestricted set-theoretic argument is retracted.  The maximum
two follows only from the state-specific markings:

* smooth candidates all contain `l`, so three pairwise-compatible five-sets
  would have union of size at least ten;
* `q=6` candidates contain one fixed outside pair, leaving pairwise-disjoint
  three-subsets of a six-set;
* `q=8` compatibility forces complementary two-subsets of `I_4`, so three
  cannot coexist.

Thus an effective configuration has **at most** two contracted carriers.
The raw graph having edges does not prove that even one compatible pair is
simultaneously effective.

## 5. Ramification and the corrected residual caps

The total ramification divisor has exact class

```text
r^*R_X=2A+B=4S_0+11F-2sum_iE_i.                         (5.1)
```

Separate noncontracted primes `C_j`, contracted primes `Z_k`, and the actual
exceptional cycle `M`.  Fixed-sheet degree-three inertia makes every
noncontracted divisorial coefficient one.  It does not control a prime
contracted to codimension two; write its coefficient `nu_k>=1`.  Then

```text
sum_j C_j+sum_k nu_k Z_k+M
    =4S_0+11F-2sum_iE_i,
sum_j A.C_j=8,
sum_j B.C_j+sum_k nu_k=4.                               (5.2)
```

In particular every `nu_k<=4`.  Distinct `C_j` have distinct divisorial
target images and are generically birational to them.  At a singular F5
point only a positive partition of eight among the `A.C_j` is known.  At a
smooth F5 point the reviewed local different gives two distinct global
primes of exact `A`-degrees three and five.

### 5.1 Actual source fibre

For an actual scheme fibre

```text
F_t=Gamma+sum_i beta_iE_i,
```

let `V_t` be the actual Cartier-weighted sum of every nonexceptional fibre
component common with strict ramification, put `v_i=V_t.E_i`, and set
`R_o=R_str-V_t`.  Then

```text
4=Gamma.R_o+beta^t(n-v),       beta^t(n-v)<=4.          (5.3)
```

Every scheme weight `beta` remains.  The cap belongs to one actual fibre and
is unavailable on raw `n` before common carriers are removed.

At the F5 fibre, with `ell_i=L.E_i`, the analogous infinity identity is

```text
2=Gamma.(S+T)+beta^t(a-ell).                            (5.4)
```

### 5.2 One target image

Fix a target point `q` and define

```text
V_q=sum_(pi(Z_k)=q) nu_k Z_k,
v_i^(p)=V_q.E_i,
R_(o,q)=R_str-V_q.
```

A generic target line through `q` contains every `Z_k` over `q` in its
pullback, so generic choice cannot remove the common curves.  It can avoid
all divisorial ramification images and all other contracted target points.
If `G_q` is the complete nonexceptional part of the resolved pullback and
`ell^(p)` its positive exceptional valuation vector, total-pullback
orthogonality cancels the common-carrier self terms and gives

```text
8=G_q.R_(o,q)
  +sum_(p over q) (ell^(p))^t(n^(p)-v^(p)),             (5.5)

sum_(p over q) sum_i(n_i^(p)-v_i^(p))<=8.              (5.6)
```

The raw cap `sum n_i<=8` is binding only in the subcell `V_q=0`.  This
residual target cap replaces the producer's false assertion that a generic
line through `pi(Z_k)` can exclude `Z_k`.

### 5.3 Class group and support genus

For `Y=X-H`, dominance and localization give `O(Y)^*=C^*` and inject the free
group on all reduced nonexceptional ramification primes into `Cl(Y)`.  In
particular their reduced classes are independent; the coefficients `nu_k`
do not enter that statement.

Let

```text
D_R=Supp(r^*R_X),
W_R=r^*R_X-D_R
   =sum_k(nu_k-1)Z_k+sum_alpha(m_alpha-1)E_alpha.
```

Connected support and adjunction give the exact identity

```text
p_a(D_R)=9+(W_R^2-3sum_k(nu_k-1))/2>=0.                (5.7)
```

Only when every `nu_k=1` is `W_R` purely exceptional, yielding the
conditional energy bound

```text
sum_trees (m-1)^t C(m-1)<=18.                          (5.8)
```

## 6. Finiteness and next exact gate

The necessary discrete state is finite without the invalid raw target cap.
There are nine blowup coordinates and finitely many proximity graphs and
marked D9 root subsystems.  Equation (5.2) bounds horizontal degrees, the
number of primes, and every `nu_k`; its `F` coefficient bounds each effective
fibre coefficient.  The adjunction equation bounds centre multiplicities.
Once strict carrier and root classes are selected, `n=R_str.E` is determined
and `m=C^-1n` is unique.  Equivalently, (5.6) bounds the residual vector while
the finite carrier list bounds `v`.

This is finiteness of numerical classes, target-label partitions, proximity
graphs, and physical contact partitions.  It is not finiteness of target
coordinates, blowup locations, analytic germs, or moduli.

The cheapest exact successor has two local parts:

1. factor the completed local different for each surviving singular F5 tag,
   producing its strict primes, coefficients, exceptional vector, physical
   contacts, and global `A`-degree partition of eight;
2. at the generic DVR of every candidate `Z_J`, compute
   `nu_J=ord_(Z_J) Fitt^0 Omega_(X/P2)` from the two target parameters and
   their leading tangential terms.

Only after those values are known should the finite proximity/effectivity and
connector filters be run.  No heavy enumeration is currently licensed.

## 7. Firewall

The eight singular tags and the smooth row are necessary states, not
effective configurations, moduli, analytic incidences, finite cubic algebras,
maps, counterexamples, or a JC2 result.  A subset class is not a curve; a
zero intersection is not disjoint realizability; a branch attachment is not
automatically a distinct physical place; and a finite numerical state is not
an occurrence theorem.  Contracted coefficients remain uncontrolled beyond
`1<=nu_k<=4`, and the singular local different remains unfactored.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `13359`.
- Body SHA-256:
  `a946988a77a558786a1226d9d7f05c179c7662f33685bf1acab211ce95e884e0`.
- Frozen basis: `b491ba9951471a67b9e2edad930eb95002e56c04`.
