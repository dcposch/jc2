You are the hostile different-model reviewer for the producer-checked D1
result `FREE-TAIL-SIGNAL`. Work in `/Users/dc/code/math/jc2` and adjudicate
only its exact one-band modular scope.

Read:

- `xmodel/round1-dtransition-20260824.md`
- `cases/round1_dtransition/transition_symbol.py`
- `cases/round1_dtransition/samples.json`
- the source files and promoted D25 artifacts named by the report/data.

Primary clauses to attack separately:

1. The unreduced source recurrence defines a typed projection `X27 -> X25`
   in the stated residue-A chart, adding exactly ten band-26 rows and ten
   first-occurrence coordinates and changing no lower row.
2. The implementation has no dependency on D43 or `D43-NF-FID` and the four
   sampled points really satisfy the cited D25/source gates.
3. At each registered prime, the witness block has
   `rank(A)=rank([A|b])=4`, right- and left-kernel dimensions six, and all six
   displayed affine kernel lifts replay in the unreduced recurrence.
4. At each registered prime, the deterministic interior sample has
   `rank(A)=4`, `rank([A|b])=5`, hence is outside the one-band projection image.
5. These pointwise results justify only the scoped verdict: compatible
   six-dimensional one-band fibers exist over the two witnesses while other
   D25 points are cut by compatibility. They do not prove a component,
   dominance, persistence, inverse-limit survival, a formal germ, or a
   characteristic-zero object.

Audit source/provenance and mathematical semantics rather than trusting the
report. Rerun `--selftest` and `--run` into a fresh `mktemp -d`; demand a
byte-identical JSON. Independently inspect the new-coordinate registry and
derive or directly replay at least one witness kernel vector and one interior
cokernel obstruction without trusting stored verdict strings. Check for stale
samples, accidental reduction/NF imports, incorrect first-occurrence logic,
rank versus affine-solvability confusion, gauge directions, and field/fiber
mismatch. A shared source mistranscription is a distinct possible gap.

Return exactly `CONFIRMED`, `CONFIRMED WITH GAPS`, `GAPS`, or `REFUTED`, with
the smallest failing witness if applicable and the honest evidence tier. Do
not launch the proposed full-cell compatibility child or edit source/data.

Write only `xmodel/review-dtransition-grok.md`. Do not edit shared ledgers.
Record model/CLI/version, basis, UTC, commands, hashes, independent checks,
scope, and final verdict near the top.
