# Sol Ultra blind whole-portfolio ideation — `20260828T1149Z`

Date sealed: 2026-08-28 UTC  
Researcher: Sol Ultra, root coordinator  
Input cutoff: packet freeze at 2026-08-28 11:49Z  
Verdict: **no proof or counterexample to JC2; one new exact local lemma and
two high-information execution redesigns.**

## 0. Executive judgment

The significant news changes the *first* obstruction on the pure Sigray
route.  Intrinsic place construction is no longer the first gap: for a
Sigray-normalized fibre, exact two-chart terminals and their deck/place law
are available.  Proposition 4.2 is also no longer the first source gap.  The
first exact unproved implication is now a **source-faithful semantic
decoration theorem**, centered on the unaudited Propositions 6.3--6.8:

```text
exact boundary places and ancestor residuals
  -> complete typed pole/finite tags, towers, M and Q at every used vertex
  -> source-certified marked-event landing object.
```

Proposition 5.1 at finite nonzero punctures is not on the shortest pole-only
proof spine.  Pole mass, the pole characteristic paths, MP5--MP6 and the
type-relative `RPMC(C)` client use `g(P)=infinity` and can bypass it.  It is
nevertheless mandatory for an all-boundary compiler, for finite-end/
`A(F)` coupling, and for any claim that the compiled object is source-
complete rather than merely pole-complete.

The strongest immediate proof attack is therefore a proof/refutation/bypass
audit of Propositions 6.3--6.8, beginning with 6.7--6.8, implemented against
the exact-pair forest rather than against prose records.  The strongest
counterexample attack remains a nonzero, source-open, rank-exact K00 point
which is compatible with full `P6` and admits at least one honest
prolongation.  Neither should wait for unrelated review rounds once its own
inputs are provisionally sound.

One likely-missed consequence of the Proposition 4.2 repair is exact and
cheap:

> **Primitive-corner lemma.**  At every repaired constant corner,
> `M_F=1`.

Indeed, if `a=deg p_F`, then `M_F^*` divides `a`, while the terminal member
has degree `b=(mu-1)a+1`; hence
`M_F=gcd(M_F^*,b)=1`.  This creates a new Avenue 2--7--25 connection between
the positive tower and finite ends.  It does **not** yet yield a conductor,
delta, Riemann--Hurwitz, semigroup, mass, or degree bound: `M=1` is a gcd
statement, and numerical semigroups such as `<n,n+1>` have gcd one and
unbounded conductor.  The missing bridge is a typed theorem identifying
which finite ends pass through these corners and how their marked inertia
orders enter the later critical-value charges.

On the GGV side, four/five isolated face orders are increasingly likely to
be determinant-row repackaging.  The quadratic-`Q` generic rank theorem
should not be followed by another equivalent Gröbner timeout.  Extract the
generic row-dual certificate, factor its denominator, and recurse only on
the denominator strata.  This directly attacks the incidence
`M(theta)z=0, E(z)=1` and avoids constructing the full maximal-minor ideal.

## 1. Custody, blindness, and scope

I verified the packet SHA-256
`31094db14e41506e121b037bd001e077d6bd80d6b4f38ae49a071116e52fef94`
and every charged hash.  All values matched exactly, including:

```text
APPROACHES.md                         f5b16a545a1f...
AUDIT.md                              92220e1fee14...
COORDINATION.md                       3fc3c9d9e024...
PROGRESS.md                           bd48c82ada1a...
notes.md                              eba9b04f1c85...
ladder/REDUCTION.md                   df6f019c7ca2...
ladder/SIGRAY-AUDIT.md                6d1c34d1484f...
prior 0702Z synthesis                 0c0a94505684...
exact-pair producer                   841c848eb98a...
exact-pair Opus review                5d219c53cc80...
exact-pair independent checker        26e24ec9667b...
Proposition 4.2 producer              10bc55d53f9c...
Proposition 4.2 Opus review           47f2b608f47b...
Proposition 4.2 checker               8396a44c5f81...
quadratic-Q generic-rank report       0775a496a0b2...
quadratic-Q checker                   83ab6c75e60b...
unit-S first correction/review        c3f03f37... / 79076a9c...
unit-S order-four/order-five reports  874bcd98... / aa88f190...
web sweep                             e03a5bec4278...
downloaded-paper audit                c5655b43144f...
```

I read no peer file beginning
`xmodel/ideation-20260828T1149Z-{fable5,opus5,grok46}` and did not inspect a
peer process's output.  No accidental peer contamination occurred.  I did
not touch AWS, `jc2-lean`, Singular, msolve, Sage, or Lean.  I ran only
read-only shell inspection, hash checks, and `pdftotext` to inspect the
primary Sigray pages containing Propositions 6.6--6.8.  I wrote only this
file.  Post-cutoff worker events are excluded from the mathematical scan.

Files substantively read or re-read for this report were the sealed packet
and prompt; the current overlays and master avenue table in `APPROACHES.md`;
the current day in `PROGRESS.md`; the newest state in `notes.md`; current
correction/source and `RPMC(C)` sections of `AUDIT.md`; all current theorem
and gap sections of `ladder/REDUCTION.md`; `ladder/SIGRAY-AUDIT.md`; the
prior `0702Z` synthesis; the charged exact-pair, Proposition 4.2,
quadratic-`Q`, and unit-`S` reports; and the previously promoted finite-end
report/review.  Primary-source inspection was limited to the charged Sigray
pages.  No claim below treats a model verdict or a computation without an
exact certificate as mathematics.

## 2. Dependency order on the shortest pure-Sigray route

The present chain should be separated into six layers.  Combining them under
the word "landing" hides where the first failure actually occurs.

### 2.1 Layer P0 — intrinsic source and places: available

For a normalized Lemma 2.1 rectangle/NE-corner fibre, the exact pair gives
all-root strict truncations, both infinity charts, and one certified terminal
deck orbit per normalized boundary place.  The terminal record is
`(h,chart,kappa,F)` with node-computable `deg p_(h,F)=1`; orbit size is the
reduced denominator `e_S`, stabilizer size is `kappa/e_S`, and cover orders
rescale by `kappa/e_S`.

This is the pure route.  It deliberately does not consume a VGG/GGV source
leaf.  Therefore `G2-PSC` is bypassed, not solved.  Hybrid `H-TRUNC`, source
coverage and terminal certification stay open.

### 2.2 Layer P1 — semantic decoration/source audit: first unproved arrow

The exact forest contains enough coefficient data to *compute* direct
orders and residual polynomials.  What is not yet proved as one theorem is
that the resulting ancestor record agrees everywhere with every Sigray
decoration later consumed:

```text
pi, kappa, nu, d_(h,F), p_(h,F), T+/T-/nearrow/searrow,
the complete approximate-root tower, M_F, Q(F), merge provenance,
same-branch pole ancestry, and all root-orbit transports.
```

The systematic audit ends at Proposition 6.2.  Propositions 6.3--6.8 are
outside it, while Proposition 6.8 is load-bearing in MP5--MP6.  Hence the
first exact unproved implication is not "a place exists" or "the deck orbit
is faithful"; it is:

> **SEM-DECOR6.**  The exact-pair forest, with all ancestor residuals,
> canonically emits the full typed decoration and satisfies the corrected
> source relations through Proposition 6.8 on every pole ancestor path.

This target is narrower than a landing theorem and broader than auditing one
printed formula.

### 2.3 Layer P2 — local marked-event landing: conditional theorem, not a
full compiler

The promoted MP/depth perimeter gives conditional landing of an all-`b=1`
configuration at its marked first jump/root event.  It still lacks a single
typed configuration-to-record map with fail-closed provenance.  This is the
next arrow after SEM-DECOR6, not part of SEM-DECOR6 itself.

### 2.4 Layer P3 — full-configuration coverage: open

Even granting P2, downstream all-`mu>=2` merges, post-jump `M>=2` chains,
later merges, composite single-pole states and general off-axis entries are
not exhaustively covered.  This is the universal landing/coverage wall.
`G2-BD` is separate: it is a bounded carrier/delay obligation only in an
architecture that has already reached the relevant residue-A state.  It is
not `G2-PSC`, SEM-DECOR6, or full coverage.

### 2.5 Layer P4 — type-relative degree control: open

`DIR(C) <=> PC(C) <=> RPMC(C) => KJN(C)` remains a conditional
type-relative route.  A finite `B`-independent `C` would give
`td <= C alpha beta` at a licensed fixed type.  Exact-pair terminals do not
prove the polar-capacity inequality.

### 2.6 Layer P5 — absolute/cofinal control: open

Even type-relative KJN does not cap `alpha beta` or supply a cofinal type
menu.  The Henon tower blocks an unrestricted carrier bound.  An absolute
contradiction needs either a selected-counterexample type theorem or another
cofinal mechanism.  This is logically later than P1--P3 but probably harder
than all of them.

### 2.7 Proposition 5.1: critical-path verdict

**Bypassable on the shortest pole-only spine.**  Pole paths have
`g(P)=infinity`, use the corrected positive tower with `h0=g`, and lie in
`T_a^searrow`.  The printed finite-nonzero-puncture shift does not enter the
pole mass, the pole-entry pin, Proposition 6.8's same-branch-pole client, or
the formal statement of `RPMC(C)`.

**Not bypassable for full semantics.**  An all-boundary compiler, Chau
finite-end identities, marked inertia, `A(F)`, or any assertion that every
finite asymptotic end is represented needs the finite value `b_P=g(P)` in
the tower.  Thus Proposition 5.1 should be repaired in parallel, but it
should not block the 6.3--6.8 pole audit or a pole-only descendant.

## 3. Theorem-interface composition pass

### 3.1 Exact new consequence: primitive constant corners

Let a repaired Proposition 4.2 tower at `F in T_a^+` have a constant corner.
Put

```text
a = deg p_F >= 1,
b = deg p_(h_m,F) = (mu_F-1)a+1.
```

By definition `M_F^*` is the gcd of a list containing `a`, so
`M_F^* | a`.  The fully repaired definition includes the terminal degree:

```text
M_F = gcd(M_F^*, b).
```

Therefore `b == 1 (mod M_F^*)` and

```text
M_F = 1.                                               (CORNER-M1)
```

This uses no unreviewed source statement beyond the promoted terminal-degree
formula and the definition of `M_F`.  It is local to a constant corner in
`T_a^nearrow`; it does not alter the already corner-free
`T_a^searrow` pole paths.

### 3.2 Composition with exact terminals, Riemann--Hurwitz and finite ends

The tempting composite is:

```text
exact terminal place S
 -> finite value b_S
 -> unique nearrow constant corner F(S)
 -> M_(F(S))=1
 -> primitive marked inertia/semigroup
 -> global finite-end bound.
```

Only the middle local implication `constant corner -> M=1` is currently
proved.  The first bridge is precisely where finite-puncture/source semantics
matter.  More importantly, no reviewed theorem identifies `M_F` with the
marked inertia length `e_S`, a conductor, a delta invariant, or the size of a
Chau component block.  Chau gives

```text
sum_(finite ends S) e_S = td + b1(C_a) - 1,
```

which is an equality with uncontrolled right-hand side, not an upper bound.
The exact orbit law computes `e_S`, but `M_F=1` alone does not bound it.

**Literal verdict: NO HIT on a global bound.**  `CORNER-M1` is a real new
interface and a cheap discriminator, not a proof of a conductor/delta,
semigroup, proximity, mass, or topological-degree inequality.  The scalable
semigroup control `<n,n+1>` shows why gcd one by itself cannot cap conductor.

### 3.3 Possible critical-value connection

Primary-source Proposition 6.8 starts in the pole-side `T_a^searrow`, so it
cannot transport a nearrow constant corner to a pole.  The nearby Section 7
critical-value statements do begin on the nearrow side.  This suggests the
correct test is not "manufacture a pole from CORNER-M1" but:

> Does every constant corner feed a finite critical-value vertex whose
> Section 7/9 charge contains `M_F`, and does `M_F=1` force a missing unit in
> the global charge budget?

No such implication is currently licensed.  A source trace can decide it in
hours and should precede any computation.

### 3.4 Direct intrinsic route and the hybrid firewall

The intrinsic exact-pair object can in principle drive a source-faithful
landing compiler without `G2-PSC`: it has the actual normalized polynomial,
both charts, all truncations and the deck action.  The compiler must retain
all ancestor residuals and every unused branch, not just terminal places.
This is a direct route to P1--P3.

There is no analogous direct proof of `RPMC(C)`: exact places make the terms
of the rootwise energy/polar-excess sum computable, but supply no upper
capacity inequality.  Conversely, none of the reviewed fixed-face GGV
endpoint facts discharges SEM-DECOR6, landing, `RPMC(C)`, or cofinality.
Their source/category and scope are different.  **NO HIT** from a reviewed
GGV endpoint statement into a pure-tree hypothesis at this cutoff.

### 3.5 Highest-fanout source target

Rank the listed source work as follows:

1. **Propositions 6.3--6.8 as one typed cluster**, beginning with 6.7--6.8.
   This is the first unreviewed cluster and Proposition 6.8 is already
   load-bearing in MP5--MP6.
2. **Proposition 5.1**, in parallel, for all-boundary/finite-end completeness;
   do not let it block pole-only work.
3. **Sections 7--9**, but only after P1 emits typed inputs; otherwise the
   audit cannot distinguish a source failure from an import failure.
4. Candidate Proposition 5.4 repair and Statement 6.2 are lower priority
   because current audits already supply their operative repaired forms.

Cheapest triad for 6.3--6.8:

- **prove:** rederive the same-branch continuation and termination directly
  from exact place orders/residual roots, with zero orders allowed and every
  ratio cross-multiplied;
- **refute:** mutation-test all inequality boundaries, equality cases,
  degree-one endpoints and axis vertices against finite exact jets;
- **bypass:** define the next pole on an exact place as the first continuation
  parameter where the computed order of `g` becomes negative, and prove that
  this intrinsic construction supplies exactly the MP5--MP6 fact needed,
  without importing the printed Proposition 6.8 proof.

Stop the cluster if the bypass cannot reproduce the exact branch/orbit
provenance used by MP5--MP6; a merely existential pole is insufficient.

## 4. Compact disposition vector, avenues 1--46

The baseline is the sealed current overlay/prior synthesis.  `Raise` and
`lower` are marginal changes from that baseline, not absolute scores.

| # | disposition | reason / current scope |
|---:|---|---|
| 1 | **raise** | Generic quadratic-`Q` exclusion compresses the live face to an exact rank-drop locus.  Raise only the stratified-incidence/raw-window sublane; stop equivalent Gröbner variants and blind face-order increments. |
| 2 | **raise** | L3/L4/L5-exact and Proposition 4.2 are now available.  This exposes SEM-DECOR6 as a smaller first theorem and yields `CORNER-M1`.  `G2-PSC`, `G2-BD`, landing, `RPMC(C)` and cofinality remain separate. |
| 3 | unchanged | Strip/ODE structure remains useful, but no new reviewed global client; isolated unit-S face orders appear redundant with determinant rows. |
| 4 | unchanged | Formal survival still needs characteristic-zero, bounded-support algebraization and a polynomial pair.  Exact terminals improve diagnostics, not the implication. |
| 5 | unchanged | No new degree-dropping automorphism or polydegree invariant. |
| 6 | unchanged | One-place methods still apply naturally to asymptotic components, whose full data are not constructed. |
| 7 | **raise** | `CORNER-M1` supplies a new finite-end/tree interface to test, and exact terminals now enumerate ends.  Raise only the typed bridge; Chau's identity itself remains non-bounding. |
| 8 | unchanged | No uniform inverse-truncation theorem. |
| 9 | unchanged | No finite reduction of Conjecture E. |
| 10 | unchanged | Existing exact JC2-sector probe remains `NO LEVERAGE`. |
| 11 | unchanged | Remains stopped/refuted as a route. |
| 12 | unchanged | No independent good-prime/face-isolation mechanism beyond existing identities. |
| 13 | unchanged | DC2 remains strictly stronger and computationally opaque beyond small slices. |
| 14 | unchanged | Defensive Zheglov audit only; no endomorphism candidate. |
| 15 | unchanged | No completed spectral/PDO dictionary. |
| 16 | **lower** | The mathematical fixed-client D-finiteness remains, but the charged control stalls before CT in generic composition.  Lower near-term execution until a structural backend bypass exists. |
| 17 | unchanged | Stabilization still raises dimension and lacks a plane-origin classifier; the Matysiak dependency adds nothing. |
| 18 | unchanged | Naive symmetric-power descent is closed; only the narrowly typed one-vertex boundary discriminator remains. |
| 19 | unchanged | Degree-12 bounded-support frontier remains the honest AS target; Witt depth with growing support is non-evidence. |
| 20 | unchanged | No standalone p-curvature implication. |
| 21 | unchanged | Hensel sheets persist but do not kill `A_infinity` or algebraize. |
| 22 | unchanged | Heights still face an unbounded unknown family. |
| 23 | unchanged | Analytic analogues remain too broad/false. |
| 24 | unchanged | Pinchuk data do not approach constant complex Jacobian. |
| 25 | **raise** | Exact terminals plus `CORNER-M1` can in principle supply fully marked finite-end inertia, including `e=1`; raise only the typed coupled CSP/interface, not ordinary passports. |
| 26 | unchanged | The executed `td=6..9` census already reaches the `A_d/S_d` wall; no rerun without new finite-inertia geometry. |
| 27 | unchanged | Topology without algebraic/source realization remains permissive. |
| 28 | unchanged | Keep one source-tracked full-boundary/one-vertex test; no naive BMY or symmetric-power descent. |
| 29 | unchanged | Hamiltonian/LND reformulations still lack a universal receiver. |
| 30 | unchanged | No embedding-sensitive affine-surface invariant. |
| 31 | **raise** | Exact place completeness and computable pole/finite orders turn finiteness into a concrete exclusion of finite-valued boundary places.  This is a better typed Rees/ZMT client, though still not a proof. |
| 32 | unchanged | Three-generator collision presentation is useful only on a bounded/source-derived family. |
| 33 | unchanged | Untwisted residues remain automatic; no new twisted class. |
| 34 | unchanged | No licensed two-dimensional pole-removal mechanism. |
| 35 | unchanged | Known higher-dimensional mechanisms still do not descend to the plane. |
| 36 | **raise** | K00's source-open rank-exact/full-`P6` incidence is the strongest independent finite counterexample discriminator.  Raise the structured kernel-chart successor, not generic sparse search. |
| 37 | unchanged | Finite-field extras do not certify characteristic-zero liftability. |
| 38 | unchanged | Tropicalization still loses saturation; use only inside the determinantal stratifier if helpful. |
| 39 | unchanged | No ten-line Keller-forced cohomology class. |
| 40 | unchanged | Commutative determinant one still does not imply free invertibility. |
| 41 | unchanged | Remains stopped; scaling is refuted by higher-dimensional controls. |
| 42 | unchanged | Spectral implication still points the wrong way. |
| 43 | unchanged | Composite-coordinate reduction returns to known Newton constraints. |
| 44 | unchanged | Closed as proof input; repaired membership theorem only. |
| 45 | unchanged | No non-tautological differential-Galois client. |
| 46 | unchanged | Continue as certification and dependency checking, not as an independent discovery proof. |

No avenue is reopened.  In particular, `G2-PSC` is not merged with
`G2-BD`: the pure route bypasses the former, while the latter remains a
separate residue-A carrier obligation if that architecture is used.

## 5. Reranked bottlenecks

There are two useful orderings: dependency order and expected difficulty.
The first missing implication is SEM-DECOR6, but the likely deepest wall is
cofinality.

### 5.1 Principal proof bottlenecks

1. **Absolute/cofinal degree control.**  Prove a `B`-independent rootwise
   capacity bound such as `RPMC(C)` *and* control the selected type, or find a
   different cofinal invariant.  Exact local books at finitely many `td`
   cannot replace this.
2. **Universal full-configuration landing/coverage.**  Cover composite
   single-pole, general off-axis, and post-jump `M>=2` continuations with a
   total provenance-preserving map.  `G2-BD` may be one scoped subproblem,
   not the whole wall.
3. **SEM-DECOR6/source trust.**  This is now the first arrow and the cheapest
   of the three: audit or intrinsically replace Propositions 6.3--6.8, then
   connect the exact-pair forest to the corrected later source calculus.

The local GGV endpoint is a high-information bounded theorem program, but it
does not outrank these global proof bottlenecks because even total branch-P
emptiness would not close landing or cofinality.

### 5.2 Principal disproof/counterexample bottlenecks

1. **Source-open finite incidence plus prolongation.**  Produce a nonzero
   rank-exact K00 point satisfying full `P6`, then lift it at least one grade
   with stable tangent dimension.  A selected chart, modular point, special
   fibre or unprolonged Artin point is insufficient.
2. **Characteristic-zero bounded-support algebraization and collision.**  A
   D43/AS/GGV formal survivor must become one fixed polynomial Keller pair,
   retain a certified collision/nonproper end, and be nonautomorphic.  More
   depth with growing degree/support is not progress on this bottleneck.

## 6. Strongest attacks to run next

### 6.1 Proof attack: `SEM-DECOR6-R1`

Rebuild Propositions 6.3--6.8 from the exact-pair forest, starting with
6.7--6.8.  The deliverable is not prose agreement.  It is a theorem/interface
which, for every pole ancestor path, emits:

```text
(place id, chart, orbit, pi, kappa, nu, all raw/place orders,
 residual roots and multiplicities, tower members including zero orders,
 M, Q, next-place/pole provenance)
```

and proves the exact MP5--MP6 same-branch-pole statement.  Run proof and
bypass constructions in parallel; send a hostile review to a different
model as soon as either is provisionally coherent.  Do not wait for the
finite-puncture Proposition 5.1 lane.

### 6.2 Counterexample attack: `K00-P6-KERNEL-ARC-R1`

Use the promoted chart-free rank-drop locus, not another selected minor.
Construct kernel charts from exact rank factorization, impose the nonzero/
projective source-open condition and all full-`P6` equations, and search for
an exact rational point.  For every candidate require:

1. replay against the original, non-deduplicated equations;
2. exact rank, nonzero and source-open witnesses;
3. full-`P6` compatibility;
4. one honest next-grade lift and tangent-rank census; and
5. a different-model review before promotion.

An inconsistency certificate would be a real bounded exclusion.  A point
without item 4 is navigation only.  A point with all five is the strongest
current seed toward a counterexample, still not a counterexample until the
all-depth/algebraization/collision obligations close.

## 7. Software acceleration and decisive experiments

### 7.1 Quadratic-`Q` stratified dual certificate

Let `M(theta)` be the exact `106 x 105` matrix over
`Q(q0,q1,q2,c4,c6,c8)`, and let `E` be the origin-endpoint linear functional.
The target incidence is

```text
M(theta) z = 0,       E(z) = 1.                         (QINC)
```

Generic rank 105 means a rational row-dual certificate for `E` exists on a
nonempty open set.  Extract it from the reviewed RREF:

```text
w(theta)^T M(theta) = E,
```

clear denominators, factor the common denominator `Delta(theta)`, and recurse
only on `Delta=0`.  On each factor, either produce another localized dual
certificate or an exact kernel point satisfying `E=1`.  Intersect surviving
factors rather than compute all 106 maximal minors at once.  This is a
constructible-rank stratification/proof object, not a probabilistic rank
sample.

AWS plan: distribute independent denominator factors and modular
reconstructions across 16--32 one-core workers, each under a two-hour inner
cap and a conservative memory guard; use at least three independent large
primes for discovery, then require exact rational reconstruction and literal
symbolic verification over `Q`.  A second backend verifies every final
identity.  Stop if two recursion levels produce neither a verified new
certificate nor an exact survivor, or if reconstructed degrees exceed the
predeclared bound; do not answer that failure by adding equivalent term
orders.

Possible outcomes are materially distinct:

- all strata certified: universal exclusion of the fixed quadratic-`Q` face;
- exact parameter/kernel point: route that point to the omitted raw windows,
  even tails and four-root equations;
- codimension-two unresolved remainder: a sharply smaller exact target,
  still progress unlike a generic timeout.

### 7.2 Lambda-nonzero branch

If different-model review confirms orders four/five, stop incrementing the
isolated face series.  Attempt a general row-ideal redundancy theorem for
face residues; in parallel compile the first complete raw determinant/window
incidence on the unit-`S` branch.  The decisive output is an exact raw
endpoint certificate or exact raw survivor, not another regular face
coefficient.  Keep all born modes and lower floors; omissions such as
`c10`/`F5` must be explicit zero-load theorems.

### 7.3 Source-audit compiler

Represent every source statement with typed domains (`T+`, `T-`, nearrow,
searrow, pole, axes), signed orders including zero, and cross-multiplied
relations rather than divisions.  Compile exact-pair nodes into this schema
and emit small proof objects plus mutation tests.  The Proposition 4.2
`l=0` omission and its downstream `0/0` sites become regression fixtures.
This should be shared with the independent Lean formalization only through
stable exported statements; it must not enter or modify `jc2-lean`.

## 8. Detailed idea cards (three only)

### Card 1 — `CORNER-M1 -> FINITE-END-CHARGE`

- **Dependencies/licensed assumptions:** promoted Proposition 4.2 repair;
  definition of `M_F`; exact-pair terminals; primary Section 7/9 statements
  only after individually checked.  Do not assume Proposition 5.1 or identify
  `M_F` with inertia.
- **Cheapest discriminator:** trace one arbitrary constant corner through
  Statements 7.1--7.3 and the exact charge formula; either derive a literal
  `M=1` contribution to a global budget or construct a formally admissible
  scalable tree with `M=1` and unbounded inertia/conductor.
- **Outcomes:** (A) a new primitive finite-end restriction; (B) a precise
  missing bridge, likely Proposition 5.1/all-boundary semantics; (C) a
  scalable control killing the global-bound hope while retaining the local
  lemma.
- **Stop/rollback:** stop after the first well-typed scalable control or if
  `M` never occurs in the finite-end charge.  Retain `CORNER-M1`; roll back
  only the proposed global connection.
- **Cost/EIG:** 3--6 desk hours plus one hostile review; no AWS.  Very high
  EIG per hour because it can either open or cheaply close a new global
  connection.

### Card 2 — `SEM-DECOR6-R1`

- **Dependencies/licensed assumptions:** normalized exact pair,
  L3/L4/L5-exact, audited source through Proposition 6.2, corrected
  Proposition 4.2 and ordinary Newton--Puiseux facts.  No hybrid GGV input.
- **Cheapest discriminator:** independently derive Propositions 6.7--6.8 on
  exact place paths, including degree-one, equality, zero-order, axis and
  deck-orbit mutations; compare only the exact output theorem used by
  MP5--MP6.
- **Outcomes:** (A) proof/repair promotes the first missing arrow; (B) exact
  counterexample invalidates a load-bearing MP claim; (C) intrinsic
  first-negative-order bypass replaces Proposition 6.8 at exactly the needed
  scope.
- **Stop/rollback:** fail closed if the emitted pole lacks the same branch or
  orbit provenance.  Do not expand to Sections 7--9 until this cluster has a
  stable typed output.
- **Cost/EIG:** 1 Sol researcher-day, parallel Fable/Opus hostile review,
  light exact software only.  Highest proof-side EIG.

### Card 3 — `K00-P6-KERNEL-ARC-R1`

- **Dependencies/licensed assumptions:** promoted chart-free K00 rank-drop
  theorem and reviewed full-`P6` equations; no inference from the capped
  selected-chart solve.
- **Cheapest discriminator:** exact kernel-chart incidence with a Rabinowitsch
  nonzero/source-open witness, followed immediately by one prolongation and
  tangent rank.
- **Outcomes:** (A) exact unit/inconsistency certificate excludes the current
  locus; (B) exact point but obstructed next grade localizes the first
  obstruction; (C) exact point with stable lift becomes the strongest
  bounded-support counterexample seed.
- **Stop/rollback:** after two structurally distinct kernel charts or one
  exact first obstruction, redesign around the certificate; do not enumerate
  1,640 selected minors or infer all-depth survival.
- **Cost/EIG:** 16--64 AWS vCPU for 2--6 hours, under 1 TiB per machine, plus
  independent exact replay.  Highest counterexample-side EIG.

## 9. Major-lane allocation

| lane | call | execution rule |
|---|---|---|
| pure Sigray / intrinsic exact-pair | **continue, raise** | Run `CORNER-M1` trace and SEM-DECOR6 immediately; provisional descendants may proceed while review runs. |
| hybrid `G2-PSC` | **redesign / deprioritize** | Preserve typed source work, but do not make the pure proof wait for VGG `H-TRUNC` or missing second-chart coverage.  A hybrid claim still owes `G2-PSC`. |
| `G2-BD` | **continue only in scoped residue-A architecture** | Never conflate it with `G2-PSC`, landing, or cofinal type control. |
| arbitrary-`Q` / rank-drop GGV | **continue, redesign** | Stratified dual-certificate recursion on `(QINC)`; no equivalent term-order fanout. |
| lambda-nonzero / raw-window GGV | **continue, redesign** | Finish different-model order4/5 review, then raw windows or a general redundancy theorem; stop blind face-order marching. |
| fixed `V0=1`, fixed tails, all-odd-zero/q-only | **stop** | Reviewed empty or non-excluding at stated scopes. |
| LF40 | **continue existing exact distinct engine only** | Harvest exact output; no duplicate reducer/order unless a mathematically new stratum is named. |
| K00 | **redesign and continue** | Kernel-chart/full-`P6`/arc successor; stop selected-minor brute force. |
| order-two / TD6 | **continue protected** | They remain independent bounded exact proof/falsification lanes; preserve source and review gates. |
| D43 | **continue protected** | Seek characteristic-zero bounded-support/source-complete output; modular or finite-depth survival remains navigation. |
| Artin--Schreier | **continue narrowly** | Work at the degree-12 bounded-support frontier or prove a support/conductor theorem; stop mere deeper growing-support Witt lifts. |
| HENS-CT | **redesign** | Do not rerun generic `annihilator_of_composition`; require a structural direct-recursion/diagonal/residue backend on the charged control first. |
| external intelligence | **continue** | Broad sweep at the scheduled cadence and on significant news; bounded primary-text audits of claims.  Matysiak is closed as unsound evidence. |

## 10. Epistemic ledger

### Proved/promoted and licensed here

- Intrinsic L3/L4/L5-exact for normalized Lemma 2.1 fibres, with exact orbit,
  stabilizer and order-rescaling law.
- Complete repaired Proposition 4.2, including the unique constant shift,
  terminal degree, `M_F` slot, nearrow location, Facts A/B and downstream
  cross-multiplied repairs.
- Generic rank 105 of the fixed quadratic-`Q` `106 x 105` matrix and generic
  odd-fibre/origin-endpoint exclusion, at its exact fixed-face scope.
- Unit-`S` first correction and regularity through relative order three.
- `CORNER-M1`, by the two-line gcd derivation in Section 3.1, conditional
  only on those promoted definitions/formulas.
- Chau's finite-end mass identity and marked component divisibility at their
  previously reviewed scopes.

### Provisional inputs

- Unit-`S` order-four/order-five cancellations, pending different-model
  dependency review.
- Any post-freeze source repair or AWS result; none is consumed here.
- All proposed SEM-DECOR6, finite-end-corner and stratified dual-certificate
  descendants.

### Open conjectures / missing implications

- SEM-DECOR6 and the systematic audit/replacement of Propositions 6.3--6.8.
- Proposition 5.1 at finite nonzero punctures for all-boundary semantics.
- Typed local landing compiler and universal full-configuration coverage.
- General off-axis and post-jump `M>=2` closure.
- `G2-PSC` in any hybrid architecture; `G2-BD` in the scoped residue-A
  architecture.
- `RPMC(C)`/polar capacity, selected type control and absolute cofinality.
- Universal quadratic-`Q` face exclusion, lambda-nonzero raw endpoint,
  K00 full-`P6` point/arc, and characteristic-zero algebraization/collision.

### Failed/stopped approaches that remain failed

- Generic receiver-rate gate march, q-only exclusion, fixed `V0=1` tails,
  all-odd-zero raw endpoint, naive translation gauge, naive plane
  symmetric-power descent, T-RH-only primitive monodromy, unrestricted
  carrier bound, generic HENS composition backend, Matysiak proof claims,
  Mathieu/GMC/Poisson route and naive scaling.
- A timeout, absent solver marker, modular sample or model verdict is no
  mathematical evidence.

### Hidden assumptions explicitly rejected

- General-reduced-curve two-chart coverage: false; normalized-fibre
  hypothesis is load-bearing.
- Hybrid source leaf equals exact intrinsic terminal: unproved/ill-typed.
- `M_F=1` equals inertia one or bounded conductor: unproved and generally
  implausible without further Keller structure.
- `G2-PSC = G2-BD`: false conflation.
- Local marked-event landing equals full downstream coverage: false.
- A fixed-`td` book or fixed branch-P endpoint theorem supplies a cofinal
  ceiling: false.
- Generic rank 105 excludes the rank-drop variety: false.
- Regular face orders imply raw endpoint regularity/emptiness: false.
- A bounded formal or modular survivor is a characteristic-zero polynomial
  counterexample: false.

### Checks, failed attempts and contamination

All charged hashes matched.  Primary Propositions 6.6--6.8 were inspected
directly.  The `CORNER-M1` calculation was checked symbolically by divisibility:
every divisor of `a` is coprime to `(mu-1)a+1`.  No heavy computation was
run.  No peer response was read.  No failed mathematical proof is concealed:
the attempted Riemann--Hurwitz/conductor composition ends at **NO HIT**
because the needed `M_F`--inertia/conductor bridge does not exist in the
reviewed perimeter.

## 11. Final priority order

1. Seal and hostile-review `CORNER-M1`, then run its bounded Section 7/9
   source trace; stop quickly on a scalable control.
2. Execute SEM-DECOR6 proof/refutation/bypass in parallel, starting at
   Propositions 6.7--6.8; repair Proposition 5.1 independently in the
   background.
3. Replace arbitrary-`Q` Gröbner fanout by denominator-stratified dual
   certificates on `(QINC)`; route any exact survivor to raw windows.
4. Run K00's kernel-chart/full-`P6`/first-arc discriminator as the protected
   counterexample lane.
5. Keep LF40, TD6/order-two, D43 and AS as independent nonduplicate lanes,
   and keep the scheduled external sweep.

This allocation exploits the new exact place theorem immediately, keeps
review asynchronous, and concentrates AWS on constructible survivor strata
rather than equivalent presentations.
