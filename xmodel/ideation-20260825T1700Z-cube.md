# Blind whole-portfolio ideation — cube/max-12 owner — `20260825T1700Z`

Status: **sealed strategy analysis; no mathematical promotion**  
Cutoff packet: `xmodel/ideation-20260825T1700Z-packet.md`, SHA-256
`3d85fbc6be7de3ced218f9aa0adb1174e8f525a8f73fd267c21f6bd6d0f92dbc`.

I read the complete 46-row `APPROACHES.md` inventory at the packet-pinned
SHA `6895a17d...`, the packet-pinned `AUDIT.md` and `PROGRESS.md`, the prior
sealed synthesis at SHA `0a96577f...`, and my own post-cutoff B8/B9 evidence.
I did not read any other `1700Z` submission.  The working `notes.md` had
changed after the packet cutoff, so I did not use its later bytes as blind
input.  Dual-AWS executions and multiple reports derived from one source are
replication/custody, not independent mathematical votes.

## Executive delta

The most important new deduction is elementary but load-bearing.  In a
fixed-total-degree-12 exact map with partial-`y` degrees `(9,12)`, the
coefficient `b_12(x)` of `y^12` is a nonzero **constant**.  The reviewed
partial-`y` routing gives

```text
a_9=h^3,                  b_12=h^4.
```

Hence `h` is constant.  After the harmless finite scalar extension already
licensed by the Kummer routing, its class is trivial: every fixed-D12 B9
candidate lies on the **order-one polynomial-core leaf**, never on the
selected order-three Q8 leaf.  The same argument gives order one for fixed-D12
B8 from `a_8=h^2,b_12=h^3`; its order-two and order-four leaves are likewise
irrelevant to the residue seed.  This concerns the partial-`y` Kummer class.
It does not remove the separate total-homogeneous common cubic/quartic
constraint.

Thus the new B8/B9 families strengthen the proof/disproof pincer but sever the
proposed selected-Q8 interface more decisively than a generic bad-reduction
warning.  Seed-facing proof work should move to the fixed-cap order-one core;
selected order-three Q8 remains valuable only for the broader unbounded-total
maximum-12 theorem.

## 1. Disposition vector over all 46 avenues

Legend: `U` unchanged, `R+` raise, `L-` lower, `O` reopen.  This is a research
allocation vector, not theorem status.

```text
 1 U   2 R+  3 U   4 R+  5 R+  6 U   7 U   8 U   9 U  10 U
11 U  12 U  13 U  14 U  15 U  16 U  17 U  18 U  19 R+ 20 U
21 R+ 22 U  23 U  24 U  25 U  26 U  27 U  28 U  29 R+ 30 U
31 R+ 32 U  33 U  34 U  35 U  36 R+ 37 U  38 U  39 U  40 U
41 U  42 U  43 U  44 U  45 U  46 U
```

Reasons for every change:

- **2 (`R+`)**: the maximum-12 proof interface now has a much smaller
  seed-facing leaf: fixed-D12 B8/B9 are order-one cores.  This does not repair
  `G2-PSC`, `G2-BD`, coverage, or the cofinal sheet ceiling.
- **4 (`R+`)**: the complete B8 `A^27/A^95` and B9
  `A^109/A^183` results make a finite-type dilatation/formal-smoothness test
  concrete.  More undifferentiated depth is still low value.
- **5 (`R+`)**: the exact proportional top-face target shear is the first
  genuine degree-descent operation on the live B9 family.  This is not a
  general Jung--van der Kulk descent theorem.
- **19,21,36 (`R+`)**: two complete structured frontier families now survive
  their first nonlinear gates, and an exact integral lift would already have
  a residue-ball collision.  The all-depth fixed-support existence question,
  not collision bookkeeping, is the disproof wall.
- **29 (`R+`, machinery only)**: cotangent/Fitting data on a Neron
  dilatation can distinguish a smooth all-depth component from another
  singular carry centre.  This is not a revived universal Hamiltonian/LND
  proof.
- **31 (`R+`)**: integral dilatation, `3`-saturation, and horizontal closure
  are the exact bridges missing between congruence families and an integral
  characteristic-zero object.  Zariski Main by itself remains tautological.

There is no new reason to reopen HC4, Mathieu/GMC, DC2, higher-dimensional
descent, real/analytic, generic cohomological, finite-field-census, or naive
scaling routes.  Rows 25/26 remain useful inside general Q8/TD6 proof work,
but the fixed-D12 Kummer collapse does not raise them for the B8/B9 seeds.

## 2. Reranked bottlenecks

### Proof side

1. **Fixed-cap polynomial-core maximum 12.**  Classify the order-one
   `(9,12)` common cubic first, then the order-one `(8,12)` common quartic.
   This is the only maximum-12 proof branch that can directly kill the live
   fixed-D12 residue seeds.
2. **General maximum-12 coverage.**  Selected order-three Q8 has a reviewed
   contact exclusion but still owes the full horizontal/projective cover,
   nonselected loads, Taylor/terminal provenance, and the order-one core.
   The B8 order-two/order-four branches remain part of the general theorem
   even though they cannot contain the fixed-D12 seed.
3. **TD6 source cover.**  Rebuild raw `C=U=0` after the `C^2` localization
   correction and review V70 before any whole-`H=0`/A3 composition.
4. **Campaign-wide wall.**  Full source landing/coverage and an absolute or
   cofinal complexity ceiling remain untouched.  No finite maximum-12 cell
   theorem alone proves JC2.

### Disproof/falsification side

1. **All-depth fixed-support existence.**  Convert the complete B8/B9
   liftable centres into finite-type integral dilatations and test formal
   smoothness; do not infer recurrence from large affine point counts.
2. **Necessary leading-face incidence on the complete family.**  The one
   displayed B9 representative fails proportionality, but only the full
   `A^183` family matters.  Intersect it scheme-theoretically before taking
   another point branch.
3. **Algebraization is now conditional rather than mysterious.**  A genuine
   `Z_3` coefficient point already is a polynomial map of fixed support; the
   reviewed residue-ball theorem supplies noninjectivity, and the finitely
   generated characteristic-zero field embeds into `C`.  What is missing is
   the point, not a separate collision search.

## 3. New mechanism and cross-avenue connection

The genuinely new mechanism is a **Neron-dilatation Kuranishi tower**.  Let
`X/Z_3` be the exact fixed-support determinant-one coefficient scheme.  A
complete liftable locus in `X(F_3)` defines the dilatation

```text
X' = Spec(A[J/3]) with 3-torsion removed,
```

where `J` cuts out that locus.  The special fibre of `X'`, rather than one
chosen digit string, is the next exact state.  A relative-Jacobian/Fitting
unit at one rational point gives a Hensel section through every depth; a unit
ideal kills the whole registered family; a singular special fibre gives the
next smaller centre.  B8 is unusually cheap because its first nonlinear
centre is exactly the reduced linear space `(s22,s26)=0` after the registered
coordinate reduction.

The new cross-avenue connection is

```text
fixed total cap (19/21/36)
  -> constant top partial-y coefficient
  -> trivial Kummer class (2)
  -> binary common-core orbit / target degree descent (5)
  -> integral dilatation and horizontal closure (4/29/31).
```

This is not the prior generic statement that B9 sits near `p=0`, nor the
selected-Q8 bad-reduction normal cone.  It proves that the fixed-D12 seed
cannot enter any nontrivial Kummer leaf even after the licensed scalar
extension.

## 4. Three ranked cards

### Card 1 — `CAP1-CUBIC`: fixed-cap Kummer collapse and cubic-orbit proof gate

**Role:** strongest proof attack.

**Dependencies.**  An exact characteristic-zero fixed-D12 `(9,12)` pair;
unit `y^12` coefficient; the reviewed common-power identity
`a_9=h^3,b_12=h^4`; the standard finite scalar extension.  For the next
stage, use only denominator-free original determinant rows and the classical
top-homogeneous common cubic `K` with `P_9=aK^3,Q_12=bK^4`.

**Cheapest discriminator.**  First freeze the one-line constant-`h`
certificate for B9 and B8.  Then, over an algebraically closed
characteristic-zero field, split the binary cubic into its three root
multiplicity orbits

```text
L^3,              L^2 M,              L M N,
```

normalize each by `PGL_2`, and compile only the first two lower homogeneous
Jacobian bands with all coefficients and original source rows retained.
Binary cubics have no cross-ratio modulus, so this is a finite three-stratum
gate; the quartic B8 analogue is deliberately deferred because it has a
cross-ratio.

**Outcome interpretation.**  A source-replayed unit kills that cubic
stratum for fixed-D12 B9.  A low-dimensional cokernel identifies the exact
next Pfaffian/Taylor client.  Positive-dimensional survival merely says the
first two bands are insufficient; it does not produce a map or a Kummer
order-three branch.

**Stop condition.**  Stop raw expansion after two bands if every stratum
retains the expected common-core dimension and no new invariant/cokernel of
bounded width appears; route survivors to sparse lower-row elimination
rather than a universal Groebner basis.

**Information gain / cost.**  Very high / low-to-medium.  A hand theorem
first removes the wrong Kummer branches, and the cubic orbit set is finite.

### Card 2 — `DILATE-95/183`: finite-type singular-Hensel discriminator

**Role:** strongest counterexample/falsification attack; genuinely new
mechanism.

**Dependencies.**  The exact source-hashed B8 `A^27/A^95` and B9
`A^109/A^183` complete-family packages; all determinant rows; the reviewed
residue collision theorem.  For B8, consume the V2 ideal equality
`(five generators)=(s22,s26)`, not the quarantined V1 false PASS.

**Cheapest discriminator.**  Construct the first `3`-saturated dilatation of
the exact integral coefficient scheme along the complete liftable centre.
Compute the relative cotangent/Jacobian Fitting ideals on its special fibre,
first over the generic point and then at registered rational points.  Replay
all original determinant rows after undoing the dilatation coordinates.

**Outcome interpretation.**

- a relative-smooth `F_3` point gives a compatible `Z_3` section by Hensel;
  fixed support plus the residue-ball collision then gives a genuine
  characteristic-zero counterexample after finite-field-of-definition
  descent;
- a unit special fibre kills the whole registered predecessor family, not
  the residue seed globally;
- a nonempty singular fibre supplies the exact next centre and obstruction
  module, without choosing a digit branch.

**Stop condition.**  After two exact dilatations, stop if neither relative
corank nor centre dimension decreases and no finite presentation of the next
centre emerges.  Do not substitute additional selected points for the
missing smoothness/emptiness theorem.

**Information gain / cost.**  Maximal / medium.  Every endpoint is
theorem-level for the registered family and separates all-depth survival,
family death, and genuine singular continuation.

### Card 3 — `FACE-SHEAR`: complete-family proportionality incidence

**Role:** fastest necessary-condition sieve and bridge between the broad and
normalized B9 boxes.

**Dependencies.**  The complete broad B9 `A^183` family over its one pinned
mod-243 parent; the unit `y^12` coefficient of `Q_12`; the exact original
determinant scheme.  Do not consume the failed displayed representative as a
family verdict.

**Cheapest discriminator.**  Add one scalar `c`, solve it from the unit
`y^12` coefficient, and impose every coefficient of

```text
P_12-c Q_12=0
```

on the universal family.  Eliminate the 147 carry-linear spectators first,
then solve the residual active ideal with exact replay.  Apply the integral
target shear familywise and retain the total-degree `9/10/11` strata.  The
degree-10/11 strata are all-depth no-counterexample strata, but finite-level
death is non-effective; only the degree-nine stratum feeds the normalized
common-cubic compiler.

**Outcome interpretation.**  Empty incidence kills this current broad B9
parent as an all-depth source.  A reduced nonempty incidence produces the
exact sheared family to pass to Card 1 or Card 2.  A large nonreduced
incidence is useful only through its Fitting/dilatation centre.

**Stop condition.**  Stop pointwise leading-face tests immediately.  If the
spectator quotient is zero, record the exact affine bundle and move to the
dilatation; if it is nonzero, accept only a complete ideal/family result.

**Information gain / cost.**  Very high / low-to-medium: only thirteen top
homogeneous coefficients and one scalar are added before the already-known
spectator quotient.

## 5. Software acceleration

Extend the existing source-hashed carry compiler with two exact operations:

1. `dilate(J,p)`: construct `A[J/p]`, saturate by `p`, emit the special fibre,
   relative Fitting ideals, and an undo-coordinate original-row replay;
2. `incidence_then_schur`: impose a small invariant incidence, quotient its
   coefficient vectors by the registered spectator image, and emit both the
   reduced ideal and a lift back to every original row.

The runner must classify false PASS, point death, family death, chart miss,
and smooth all-depth section separately.  Exact source hashes, V1 negative
controls, coefficient-cap/actual-degree audits, and independent `std` versus
`slimgb` or a second matrix generator remain mandatory.  AWS-only execution
continues; dual hosts are replay evidence, not separate votes.

## 6. Continue / redesign / stop

| Major lane | Recommendation | Exact reason |
|---|---|---|
| Broad B9 D12 | **Continue, redesign** | Intersect the complete family with proportional leading faces, then dilate; stop selected-point depth. |
| Normalized B9 `(9,12)` box | **Continue** | It is the direct fixed-cap order-one/common-cubic counterexample box; impose the cubic locus familywise. |
| B8 D12 | **Continue** | The solved `A^27/A^95` gate is the cheapest first dilatation and independent structural asymmetry test. |
| Selected order-three Q8 | **Continue narrowly** | It remains part of general maximum 12 and has near-terminal source-valid work, but **stop** treating it as the B8/B9 seed interface. |
| Fixed-cap order-one max12 | **Raise/start** | It is the only proof lane that can directly meet either fixed-D12 seed.  Start with B9's finite cubic orbit set. |
| TD6 | **Continue at reduced urgency** | Finish raw `C=U=0` and the corrected source cover; stop all B8/B9 cross-landing claims. |
| Global landing/ceiling | **Continue 5% reserve** | The proof-wide wall is unchanged; no local family justifies a large speculative expansion. |
| Closed historical lanes | **Stop/hold** | No new receiver, source map, or bounded universe changes their prior negative gates. |

Speed allocation for the next bounded window:

```text
AS/D12 complete-family + dilatation                 40%
maximum-12 proof                                    35%
  of which fixed-cap order-one cubic/quartic        25%
  and selected nontrivial-Kummer Q8                 10%
TD6 corrected source cover                          20%
global landing/cofinality reserve                    5%
```

Near-terminal source-valid jobs need not be preempted to hit percentages.
Raise AS above 40 only for a smooth dilatation, exact recurring finite-type
state, or all-depth section—not another finite digit.  Restore more Q8 share
only for a complete general max-12 cover, not a selected-leaf refinement.

## 7. Refusal scope

The B8 and B9 affine families are finite-depth schemes.  The fixed-cap
Kummer collapse does not exclude the order-one core, prove any map exists,
or prove all maximum-12 maps automorphic.  The leading shear is conditional
on an exact proportional top face.  No card proves an inverse limit,
characteristic-zero Keller map or collision, full Q8/TD6 cover, global
landing, complexity ceiling, counterexample, or JC2.

