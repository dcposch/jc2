# Direct finite-place source reconstruction — manual interface audit

NEW, UNREVIEWED MANUAL CLIENT THEOREM. PROVED_CONDITIONAL for a specifically source-attached expression reconstruction; NOT acceptance of a direct modular replay of the current program. No implementation, place selection, coefficient payload, rank or source result. First action 2026-09-11T01:17:38.388122315Z; targets absent. Original reserve01:33/HARD01:36UTC unchanged. Exactly eleven inputs freshly read WHOLE after their hash barrier; clipped aggregate reads were recovered separately. Accepted imports: cone17z, source20z at STATIC scope, actual-field identification and conditional rank19z. No runtime or coefficient correctness is inferred from those static acceptances.

## 1. Outcome and the actual obstruction

A rational coefficient baseline is not logically necessary. What is necessary is a proof that the residue computation evaluates particular characteristic-zero source expressions. A typed expression reconstruction can supply this without expanding their rational coefficients. It must distinguish construction from validation: passing an equality check modulo a place never proves the corresponding characteristic-zero equality.

There are two materially different proposed clients. Freezing the existing canonical formulas requires preserving their three late Bézout choices. Alternatively, one may choose a place-admissible column completion and prove that its formulas still map every complete source to zero. Section4 proves the latter necessity statement, including nilpotents; it does NOT assert byte equality with the current canonical artifact. Either is a NEW reviewed source/instrument attachment, not a permission to change the selected baseline pipeline.

The concrete code distinction is load-bearing. `arithmetic.fi` has a unique field inverse; its internal Euclidean branch may change harmlessly. `arithmetic.completion`, called at gaps8,9,10, returns a NONUNIQUE Bézout pair. `check.py` explicitly recomputes that pair and compares representatives, not only their product identity. Naïve evaluation at a single reduced factor may therefore produce different subsequent polynomials even if every modular check passes.

## 2. Exact local-ring expression theorem

Write B=Q[V]/P for the accepted actual degree-seven FIELD. Choose O0=Z[1/N,V]/P, with P monic over Z[1/N]; irreducibility over Q makes O0 a subring of B. A genuine place is a homomorphism pi:O0->k=F_p[V]/phi, with p prime, p not dividing N, and phi an authenticated irreducible divisor of P modulo p. No prime or factor is selected here.

Consider a finite, typed polynomial-expression circuit with fixed variables X1..X4,z,S,theta. Constants are the literal rational/leading expressions in the charged producer. Each division must be either by a listed integer with nonzero residue at the place or by a B-SCALAR expression a whose already-defined residue is nonzero. Inductively enlarge O by a inverse; pi extends uniquely. Since O embeds in B, nonzero residue proves a is nonzero in B. This induction, not a modular assertion that an arbitrary alleged inverse is correct in B, defines the generic inverse. A separately checked residue product authenticates its numerical evaluation. No parameter-dependent inversion is admitted.

Addition, multiplication, fixed coefficient extraction, differentiation, finite power/series operations and fixed-index linear operators commute with pi. Sparse removal of actual zero coefficients does not alter this fact. It does NOT permit omission of a prescribed equation or conversion of a modular zero into a generic identity. Matrix inversion by determinant/adjugate is covered when the determinant residue is nonzero; both products remain required in an independent evaluation check. The chosen denominator list can be represented by expression identities rather than expanded seven-coordinate rational polynomials.

Consequently a checked residue minor of these authenticated generic columns is the reduction of that minor in B. Nonzero residue gives nonzero determinant in B. This is the accepted specialization argument; the new obligation is authenticating the expressions as source generators. Fieldness is essential: in a product base, localizing a nonzero element may discard another characteristic-zero component. Without the accepted actual-field theorem one would need whole-base units or a component-complete cover. Nothing here selects a characteristic-zero embedding.

## 3. Concrete branch audit and a reusable countercontrol

The producer's relevant chain is: literal P7/W/C/D and contacts; six completed3x3 maps with full mixed forcing; critical gap7 with its fixed Lambda functional; late gaps8,9,10; full Euler mate; full Jacobian and all108 slots/45 low slots; both inverse charts; critical z graph; ALL25 rows and Hq*ell guard. The scalar divisions are `fi`, matrix determinants, the rational band/Euler pivots, and scalar column completions. There is no licensed division by a parameter polynomial. The critical coefficient inverse is tied to the actual gap7 expression, not candidate-owned data.

Constant-only checks (leading ODE/contact, matrix/column identities) are distinguishable from high-parameter checks: `upper`'s omitted-upper-row rejection; early/middle full residual; Euler resonance; Euler/band equality; upper Jacobian zeros; inverse-polynomiality; graph shape and row/guard readbacks. The latter may be zero on the universal source quotient without a modular pass establishing ambient polynomial zero. They cannot simply be relabeled generically checked.

Bézout countercontrol, unrelated to any proposed r3 place: take B'=Q[w]/(w²-2), c1=1,c0=w, and the place w->3 in F7. The charged two-stage algorithm first gives gcd(P,c1)=1. Over Q[w], its second Euclidean call on (1,w) yields the completion (1,0). Over the linear residue presentation the call is (1,3), yielding (0,1/3). Both satisfy lambda1*c1+lambda0*c0=1, but they are different. This is a hand example, not a generated fixture or a prime recommendation. It refutes canonical-representative preservation by unrestricted modular Euclid.

For two completions lambda and lambda' of the same unimodular column c=(c1,c0), put b=(b1,b0), psi=c1*b0-c0*b1 and x_lambda=-lambda*b. Set delta=lambda'-lambda and chi=delta1*lambda0-delta0*lambda1. Direct multiplication using lambda*c=1 gives delta=chi*(c0,-c1), hence x_lambda'-x_lambda=chi*psi. Thus differences are multiples of the RETAINED compatibility, not automatically zero polynomials. This proves the precise freedom and explains the current checker's false-negative rejection of alternative representatives.

## 4. Source attachment without asserting a generic successful run

Here is a sufficient NEW client contract and its proof. Let I be the accepted canonical complete homogeneous25-row ideal in B[X1..X4], and let A=(B[X]/I)[g^-1] be its accepted complete source algebra, with g=Hq*ell. Use this LOCALIZED source algebra, not an unproved equality of unsaturated ideals. Interpret the source recipe as finite algebraic expressions, not as a claim that every executable assertion terminates successfully. Preserve all input equations, full forcing, the fixed critical functional, all25 output positions, weights and guard. At gaps8,9,10 one may select a column coordinate with nonzero residue and use its generic B inverse as completion. At least one residue coordinate MUST be nonzero for this policy; otherwise STOP. No generic nonzero test is inferred merely from the other coordinate's vanishing residue.

Compare the new expressions with the canonical ones in A, NOT only at its field points. The first six maps and gap7 agree: their inverses are unique, and the critical functional is unchanged. In A the retained critical compatibility is zero and z equals the canonical zeta. At gap8 the two equations b+c*x=0 hold for the canonical source. A unimodular completion forces x=-lambda*b in any commutative ring; therefore the new value agrees with the canonical value in A. Equivalently section3 expresses the difference as a multiple of the retained psi. Inductively the full earlier forcing agrees in A, so the same argument works at gaps9 and10. All reconstructed source bands therefore agree in A.

The full mate's nonresonant Euler coefficient operator is a fixed rational linear division operator, with the prescribed resonant gauges zero. Apply it to both congruent reconstructed A/target expressions: their B expressions agree in A. At an Euler resonance the source equation imposes the forcing coefficient zero; a modular disappearance of that coefficient is NOT an ambient identity proof. Formally retain this equation when interpreting source necessity, and set only the specified mate gauge to zero. The installed support projections and auxiliary source divisions are likewise justified on the accepted source, not by a residue-only support test. Both original inverse-polynomiality conditions remain source hypotheses; neither is replaced by modular evidence.

Thus full bracket formation, every low coefficient, the mixed low row, and critical substitution all agree with their canonical classes in A. Each of the new25 rows F'_j maps to zero in A. Hq is unchanged and ell' equals ell in A, hence g'=Hq*ell' equals the UNIT g in A. This proves membership in the kernel of B[X]->A; it does NOT claim membership in unsaturated I. The argument is scheme-theoretic: it uses equalities in A and unimodular row identities, with no radical, reducedness, rational point or reverse chain assumption. All completions are weight-zero B scalars, so the prescribed homogeneous weights remain valid.

Now suppose an independently authenticated direct evaluation of these F'_j has a nonzero297minor of its full weight30 multiplication map. Section2 gives rank297 over B. Their degree30 multiples span B[X]_30, so in particular g' is their B[X]-linear combination. Its image in A is therefore zero; that image is also the unit g. Hence A=0, with the accepted complete-source consequence. The certified minor is a characteristic-zero minor of this ALTERNATIVE necessary25-row presentation. It is NOT thereby a certified minor of the literal canonical297-by1453 matrix, nor does this argument establish I_30=B[X]_30. If that literal canonical conclusion is required, preserve its canonical completion trace instead. Do not feed alternative column IDs to the unchanged canonical checker. No g integrality is needed for the full-rank implication.

This proof authenticates necessity, not arbitrary ambient polynomial identities or successful execution of the rational producer. It keeps all25 rows and source conditions; the alternative formulas may be a weaker necessary presentation away from the original source. That suffices for this positive full-rank implication, not for properness or a source point. The accepted source equivalence supplies the canonical equations in A, including the full mate and inverse conditions; the proof does not promote modular assertions to them. Static program hashes alone cannot replace that import. It remains necessary to review the proposed expression contract's implementation of this exact interpretation before applying the theorem.

## 5. Smallest additional authentication and remaining GAP

No complete rational source coefficient expansion is required by the preceding proof. The additional object is a fixed expression/branch contract, with independently verified residue evaluation, containing:

1. The exact monic P, rational constants and genuine place; a finite list of scalar inverse expressions and nonzero residues, including all rational pivots that the fixed index ranges can use. The twelve named unit witnesses, matrix inverses and late completion choices must bind their actual expression inputs. No candidate may supply a free leading coefficient or critical c.
2. A typed index-by-index attachment of the fixed recipe to the charged formulas: all ten bands/full forcing, fixed gap8 U² term and target, gap10 ell term, full mate,108/45 inventories, BOTH inverse conditions,25 graph slots and original guard. Construction and assertion nodes are separate. A residue-only assertion is diagnostic, never a generic rewrite rule.
3. For an alternative completion, the section4 congruence interpretation; for exact canonical output instead, a SMALL characteristic-zero leading-constant/Euclid trace fixing the three pairs. Canonical equality cannot be substituted for the weaker congruence or vice versa. Every executed data-dependent degree branch must either be this authenticated constant trace or implement an operation proved to commute with specialization.
4. Independent reconstruction of all25 reduced generators and full column indexing, followed by the accepted complete inverse/minor check. Matrix-source binding, not a rank header, is authoritative.

The avoided object is the large rational parameter-polynomial baseline. The replacements are bounded leading-constant calculations and a one-time static source/operation proof, followed by finite-place arithmetic. That is a structural reduction in what must be rationally materialized, NOT a measured time advantage: no branch trace, finite-place evaluator, denominator inventory, place or certificate has been constructed here. A generic symbolic trace that merely promises to check all original high-degree equalities over Q would NOT achieve this bypass. The section4 source-congruence proof is specifically what avoids that promise.

Current readiness GAP: neither a reviewed implementation of this new expression contract nor its independent evaluator exists in the charged packet. The unchanged code remains a rational-baseline producer/checker, and its `Fraction` arithmetic/Euclid/comparison wire cannot be reinterpreted as this contract by a runtime flag. The canonical pipeline is untouched. If ROOT requires literal canonical-row identity rather than the proved necessary-row containment, the small canonical leading trace and corresponding branch contract remain additional prerequisites.

Changed-object control for equality authentication: in Z_(p), the elements p and0 have the same residue but differ generically. A trace that uses the observed equality p=0 to replace a genuinely zero row by1 (for example after multiplying the proposed row identity by p) can manufacture a modular unit matrix unrelated to the source. A full modular inverse check cannot repair this missing source attachment. This is a logical countercontrol, not an actual r3 defect or fixture. Conversely a bad place can kill a genuine nonzero minor; failure gives no nonmembership, source point or source-zero result.

## 6. Scope, OPEN and custody review

Classification: NEW CONDITIONAL source-expression attachment; known determinant specialization RECOVERED; blind modular replay as canonical authentication REFUTED by the completion/equality controls. No actual r3 rank, valid place, source exclusion, numerical-degree coverage, all-r theorem or JC2 conclusion. No code, prime, cap, run or follow-on is authorized. No current runtime or other-lane input was read.

OPEN quantity: whether a fixed independently auditable direct expression contract can be completed with all scalar inverses place-admissible and an actual nonzero297minor. Cheapest next check is a FIRST manual review of sections3–5 and the finite assertion/branch boundary, BEFORE any prospective implementation decision. Unmeasured cost; no numerical runtime or coefficient-height claim. Missing runtime evidence is not repaired by this theorem. No new canonical OPEN identifier or ledger edit.

Own-only collision check: targets were absent; only this report/transaction and its tagged box are authored. All eleven charged bodies are FRESH_WHOLE; sourceFIRST and rankFIRST were reread separately through their last marker after initial aggregate clipping. Rootcard corrections are retained, including degree7 rather than the gate's degree8 wording and the qualified denominator/integrality statements. All authored text uses apply_patch. Final own WHOLE, postpins, unique marker and expected transaction verification are documentary publication steps only. Scientific subprocess count ZERO; no coefficient payload or actual matrix was read or formed.

Own report/PINS/READ-SCOPE WHOLE review completed01:30:47UTC; all eleven current originals matched at01:30:33UTC. The explicit rational-pivot residue qualification was then reread as a one-line clarification. Publication follows, with no further mathematical work.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `16586`.
- Body SHA-256:
  `e1d267cacdfdcad1a5401fb3719c8790a2e64c0a2a766b6d4ca9ea60cfdb2d37`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
