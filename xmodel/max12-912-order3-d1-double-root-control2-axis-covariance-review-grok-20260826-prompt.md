# Hostile review charge: D1 V8 unit-axis covariance V9

Act as a hostile algebraic-geometry/commutative-algebra/source-custody
referee.  This is a text-only review: do not run local Singular, Sage,
msolve, Lean, algebra compilers, or substantive Python.  Read every file in

```text
cases/max12_912_order3_d1_double_root_control2_axis_covariance_v9_20260826/
```

and every source named by its closure.  Read the promoted V8 theorem and its
review.  Pin:

```text
V9 RESULT  a061aea2bab3abcd9a8e88a3e86f18de233f46515cd2965bafa79ff1516642e2
V9 FREEZE  003788e80c359fa715d799f3fe66a35929b105c772d139865e87f123c1a9bda2
V8 review  2c3dbbd50089892197dd5ad5c0fb90776b5c2d2dea4ac2752f3b1a3218cc7d4c
V8 promotion b503f6a70c39c256ba27f27823d5788bf5787badc507efa3079433064eb142e3
```

Charge every point:

1. Full freeze/source/result/pre-GO/AWS/cap/diagnostic custody and dual
   traversal agreement; verify canonical certificate SHA `1375bd72...`.
2. Literal ancestry to the reviewed V8 multipliers and charged rows.  Audit
   the ordinary source homogeneity `12+i`, the coefficient scaling
   orientation, and all eight exact row identities.  In particular charge
   row-3 `mu*Lambda^15`, row-6 zero `nu`, row-8
   `Lambda^20(1+tau)`, fixed `k,mu,nu`, and `rho/tau` characters.
3. Verify from every multiplier monomial that the least common negative axis
   character is exactly `-3`, hence clearing power three is both sufficient
   and minimal for the literal V8 combination.  Check the powers
   `a^(11-i)` and `a^23`, not merely dimensional plausibility.
4. Audit the full 48-term coefficientwise identity, universal SHA
   `53cf4b65...`, exact `a=1` specialization SHA `e549fd68...`, and
   coefficient-one targets `a^3*Lambda^20` and
   `a^3*Lambda^20*tau`.  Distinguish a full polynomial identity from a
   congruence or finite h truncation.
5. Verify `p=-3a^2,c=2a^3+h`,
   `Delta=-27h(4a^3+h)`, and Jacobian determinant `-6a`.  Charge why, on an
   arc centered at h=0 with a a unit, `v(Delta)=v(h)`.
6. Audit the mathematical transport: on `D(a)`, multiplication by `a^3`
   and the coordinate automorphism preserve the monomial-initial obstruction;
   after the etale a-choice, faithful-flat descent really transfers
   nonexistence/unit-initial-ideal conclusions.  State the exact normalized
   valuation variables and do not smuggle in positive axis valuation.
7. Preserve the firewall: fixed loads/source and the promoted V8 cell only;
   no triple-root `a=0`, moving loads, other normal directions, whole fan,
   D1, or JC2 conclusion.

Produce exactly one review file at

```text
xmodel/max12-912-order3-d1-double-root-control2-axis-covariance-review-grok-20260826.md
```

Include overall verdict, smallest failing identity, smallest missing
hypothesis for a stronger theorem, evidence tier, exact promotion text if
confirmed, and explicit firewall.  End with exactly one token:
`UNIT_AXIS_COVARIANCE_CONFIRMED`, `REPAIR_REQUIRED`, or `INCONCLUSIVE`.
