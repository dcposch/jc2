# Complete late-bound CAPRUN orchestration recipe — Sol fallback (2026-09-12)

- Tag: `caprun-complete-lateflow-sol-20260912`
- Basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`
- Lifecycle: `INTERNAL-UNREVIEWED`, disabled/unbound preparation only.
- Scope: concrete orchestration recipe and custody; no worker, batch, science, AWS, or runtime selected or launched.

## Status

Draft opened under transactional artifact lease. Required diagnosis bytes were hash-pinned before content read.

## Outcome

Prepared a concrete inert orchestration packet, not another authority serializer and not a launch-ready batch. It preserves the accepted dispatcher/caller/binder/installer trust model. The nine authority candidates are explicitly `EXPECTED_RUNTIME_OUTPUTS_DO_NOT_PREINSTALL`; the unchanged dispatcher remains their sole O_EXCL live writer. Only registration and card cross the final installation boundary.

The executable-shaped sequence is fully written before any future holder: [RECIPE.js](../box/caprun-complete-lateflow-sol-20260912/RECIPE.js) plus frozen constants, observation, explicit attestation, and separately invoked release templates. It is disabled/unbound and requires a different-model review of a later fully bound descendant. No batch or worker is selected.

## Concrete phase split

1. `FROZEN-CONSTANTS.template.json` freezes host/transport/batch paths, original clocks, prepared inputs, exact nine phases, exact fourteen candidates, five staging names, and the do-not-preinstall disposition. `armed:false`, unresolved tokens, and `UNREVIEWED_UNBOUND` fail closed.
2. First `RECIPE.js` invocation authenticates the preholder manifest, coordinator timer receipt, and exact native-list bytes; invokes bound descendants of historical `outer-admission.sh` and `prepare-leaves.sh`; invokes the bound observation script; add-publishes only fresh observation JSON; reports `AWAITING_EXPLICIT_ROOT_ATTESTATION`; and terminates.
3. ROOT separately invokes the bound `attest.template.sh` with literal `ROOT_EXPLICITLY_ATTESTS_OBSERVED_LIVE_STATE_NOT_RELEASE`. It no-clobber writes the exact caller decision schema, pins the observation hash and four prepared hashes, and contains no release operation. `RECIPE.js` cannot invoke this helper.
4. Second `RECIPE.js` invocation authenticates the observation and decision, invokes the unchanged `python3 -I -S -B .../caller.py` with its six supplied inputs and `ROOT_ATTESTED_CANDIDATE`, applies the accepted patch at a fresh allowed prefix, and requires exact complete readback of all fourteen paths/hashes.
5. The recipe requires five remote targets absent, then stages only registration, card, three-entry manifest, generated unchanged installer, and exact `native.sha256` list bytes under strict-host-key transport. It performs remote hash readback. Nine authority references and SUMMARY stay local/documentary.
6. The generated unchanged final installer rechecks manifest, literal hashes, PID/start/invocation/namespaces, exact leaf inventory/identities/domain/emptiness, registration `.pins`, native-list bytes, original stops, then installs registration/card only, fsyncs, compares and returns `INSTALLED_NOT_RELEASED`.
7. ROOT later makes a separate release decision and separately invokes the bound `release.template.sh`. That script rechecks original absolute holder/admission stops, elapsed monotonic time from the original `/proc` start tick against 120 seconds, PID/start/invocation, registration hash/mode and FIFO identity; it writes exactly `RELEASE <fixed-token>\n` and closes the sole writer. It is absent from `RECIPE.js`.

All later recipe boundaries recheck the original admission stop. Nothing rebases at admission, attestation, installation, or release. The holder's existing same-PID exec and its own min(original start+120 seconds, absolute cutoff) remain unchanged. Dispatcher per-phase original admission checks remain unchanged. Caps, profiles, predicates, phase order, sources, and `PREFLIGHT_ONLY_9` scope remain unchanged; this is not FULL13 or science evidence.

## Predicates and STOPs

The observation source checks ROOT, exact holder/unit identity, invocation, command line, cgroup membership, no child, outer domain/no subtree, elapsed original holder age, original absolute stops, empty `authority`/`frozen`/`writer` children, and all nine leaf path/device/inode/domain/no-subtree/no-process/no-descendant/unpopulated facts. It deliberately does not rerun historical preholder whole-tmpfs-empty after legitimate outer stdout/stderr exist.

Every mutation boundary is absent-target/no-clobber. Every command failure is terminal evidence with no retry, replacement worker, second holder, cap increase, cleanup, or automatic release. No model-composed command is needed during a live interval: a future selected batch must bind, hash, and review all five sources and its frozen data before admission.

GAP[BOUND-PACKET-REVIEW]: no actual batch, worker, host, clocks, paths, coordinator receipt, native bytes, holder token, or bound descendant is selected. Quantity: one complete bound administrative packet and five code sources. Cheapest test: different-model source review plus full-byte substitution/pin comparison, estimated 20 minutes; no live execution.

GAP[LIVE-LATENCY]: full observation through separate attestation, caller/readback, staging, install and separate release has not been measured. It must fit the remaining original 120-second monotonic interval and both original absolute stops. Historical 365 ms applies only to caller/patch/fourteen-file readback. Quantity: end-to-end remaining milliseconds. Cheapest test: one separately authorized no-science exact live attempt under unchanged caps, under 120 seconds; not authorized here.

## Custody and read scope

Primary pins: diagnosis `8e3a8ea16497b0a3e27801dd012ea0baa6c397977f128ab1cdef6a1e4c75d74d`; diagnosis PINS `a7f298a8452e2d793b707d9905a03ec1d48ea755684e5709694fc212842b0d9c`; dispatcher `52b3d916f4fc3941bb98c8b4f7698ab940680d09ee4e065de1f167857e3fd436`; caller `73596b115c73db9aac6751c08d26b2a90cbddccfaa1d6a8615ada65a8076df90`; late binder `ff2beb48b68b55dc5035709a9114bc7202dd38cef488d20b8163d0c55e2366fe`; stage patch `f7c61db67a9982a99f1febaed6dae0b6e3595199ffccad70647b66556b907066`; final installer template `b0aac1034ba6985dac9c23e75b4f258336ea238449088880f7ed1b632152ad99`; accepted composition `ab055f37a28cf7d7bb247951e33a26d3243d54ed4be56bd0f726d471d008515e`.

`PINS.json` records all pre-read sources, authored artifact hashes, and exact read scopes; `POSTPINS.sha256` freezes critical post-read bytes. Own five code/data sources and self-controls were read WHOLE after final source edits. Dispatcher reliance is selected-source-through-pinned-diagnosis, not a new whole runtime review. No local syntax, AST, import, test, dummy, caller, installer, runtime, CAS, SSH, AWS, worker, holder, FIFO, or science execution occurred.

Prepared between first action 03:48 UTC and publication reserve 04:04 UTC; hard stop remains 04:07 UTC. The earlier Astra capacity error supplied no artifact. This fallback changes no shared ledger.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7133`.
- Body SHA-256:
  `a24f74175ed10e344649df98fbe3bd3ecd6241b5eac9e878c8e9c7073fc39a26`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
