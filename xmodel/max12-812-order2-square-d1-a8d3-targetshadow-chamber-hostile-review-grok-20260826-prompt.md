# Hostile review charge: D1 `a=8,d=3` target-shadow chamber split

You are an independent hostile mathematical reviewer.  Work read-only in
`/Users/dc/code/math/jc2`.  Review the frozen producer

```text
cases/max12_812_order2_square_owner_d1_a8d3_targetshadow_chamber_split_20260826/
```

and write your report only to

```text
xmodel/max12-812-order2-square-d1-a8d3-targetshadow-chamber-hostile-review-grok-20260826.md
```

Return exactly one top-level verdict: `CONFIRMED`, `REFUTED`, or
`INCONCLUSIVE`.  This is a narrow producer review, not permission to promote a
larger D1/global statement.

## Mandatory custody checks

1. Recompute the SHA-256 of `RESULT.md`, `FREEZE.sha256`,
   `EVIDENCE.sha256`, `AWS_LAUNCH_METADATA.md`, `PREREGISTRATION.md`,
   `compile_chamber_split.py`, and `PRODUCER_FREEZE.sha256`.  Rehash every
   path named by each manifest, including all three retrieved AWS trees.
2. Establish that exact Q and `F_65519`,`F_65521` are distinct registered AWS
   executions of the same source archive, with correct on-host freeze checks,
   rc zero, terminal validators, no diagnostic, and zero swap.  Exact Q alone
   is the characteristic-zero endpoint; the primes are controls.
3. Audit the Singular 4.3.2 quotient-ring hazard: state whether any conclusion
   uses `qring` assignment, `==0`, `subst`, or `diff` without explicit
   canonical reduction.  The claimed producer uses an ordinary polynomial
   `ring`; verify that from the compiled bytes.

## Mandatory mathematical checks

4. Independently inspect the pinned parent source inventory and all three
   literal parent programs.  Verify the baseline `(A,C,R)=(8,11,8)`, grade-38
   ceiling, all 17 licensed primitive families, all source/load/moving-`p` and
   target jets through grade 38, and that the compiler imports the four odd
   literal rows byte-for-byte.  Explain why emptiness of this odd subsystem is
   enough for the complete seven-equation system; flag any missing source or
   target that can alter an odd row through grade 38.
5. Recompute, without trusting PASS strings, the exact grade-28 identity
   `[sigma^28]Phi1=(3/4)k60*c1`, including divisibility/quotient semantics.
   Recompute from the literal odd rows the moving relation

   ```text
   S=Phi7+(p(sigma)/4)Phi5+(3p(sigma)^2/32)Phi3
          +(5p(sigma)^3/128)Phi1,
   S=0 mod sigma^38,
   [sigma^38]S=-(3/32)p*eta*k60*c1*b0.
   ```

   Check every sign and coefficient.  Do not import the falsified fixed-`z`
   analytic bridge; distinguish that route failure from the canonical literal
   source rows after inverse-root motion.
6. Verify that `D(c1) union (V(c1) intersect D(c0))` is exactly the nonzero
   leading-`C` locus for the named numerator convention.  On `D(c1)`, check
   scheme-theoretically that row one forces `k60=0`; on the closed branch,
   check that `c1=0` kills the residual.  Confirm that no division by `b0`,
   `eta`, or a leading coefficient of `R` occurs.
7. Verify from the pinned target inventory and literal definitions that the
   full odd combination differs from the source by exactly
   `-sigma^38*J/4`, with no earlier odd target.  Recompute both ordinary-ring
   unit ideals on the stated localizations and decide whether they prove
   emptiness before radicals.
8. Audit the homogeneous closed `ord(R)>=8` coverage, the role of `eta`, and
   the exact theorem scope on `D(p*k0*J)`.  Check the statement that a later
   Keller-source composition may regard `J` as a unit, but that this producer
   itself does not prove the global/common-open composition.

## Firewall

Do not extrapolate to `J=0`, `p=0`, `k0=0`, another contact/equality or leading
face, positive-order load, another D1 region, the full square component,
order two, maximum twelve, or JC2.  The earlier broad a8d3 analytic bridge is
a frozen no-verdict route failure, not contrary evidence if the literal-row
split is correct.  If anything is wrong, identify the smallest failing byte,
identity, branch, or localization and state the narrow surviving scope.
