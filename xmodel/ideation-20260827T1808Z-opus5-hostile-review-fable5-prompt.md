# Hostile different-model review: Opus5 `DR-INDEX` / capacity claims

You are Fable 5, acting as a hostile mathematical reviewer.  Review the
producer report

`xmodel/ideation-20260827T1808Z-opus5.md`

whose frozen SHA-256 is
`a9b2eb9023ac78733dcbf100e9b584e0445a894edd4098026a073f8ec87e5cff`.
The common packet is
`xmodel/ideation-20260827T1808Z-packet.md` (SHA-256
`e388864250cdaa7bd23d5eb0babe8929bf0c02d80758ae91d708202ea9e2f6cc`).

Write exactly one report to

`xmodel/ideation-20260827T1808Z-opus5-hostile-review-fable5.md`

and do not edit any canonical ledger.  Do not enter or inspect `jc2-lean`.
Use desk-scale exact arithmetic only; any heavy algebra would be AWS-only, but
none should be needed here.

Audit every load-bearing claim, with special attention to:

1. Whether the promoted row-23/24 operators really identify with
   `H^1_dR(A^1-Z(H), d+(m/4)dH/H)` and whether
   `dim H^1 = r-1+k_m` has the stated signs, hypotheses, and algebraic versus
   analytic meaning.
2. Whether `k_m` is exactly the promoted rational-mode schedule, including
   multiplicities, infinity, signs, and the choice of twist representative.
3. Whether the extrapolation to determinant rows 25 and above is licensed.
   Independently derive or compute the cheapest actual row-25/26 discriminator
   from frozen campaign formulas if possible.  Distinguish a theorem about an
   abstract operator family from a theorem about the campaign rows.
4. Whether the stated stable range and the numerical capacity table follow
   from the frozen coefficient windows.  Check the claimed lower bounds
   `>=78`, `>=66`, `>=114`; identify double-counting or nonlinear-coupling
   caveats.
5. Whether the assertions about rigidity, rank two, irregularity, and the
   proposed reopening/reranking of avenues 16/25/33 are mathematically sound,
   or merely heuristic.
6. Novelty/history scope, and whether any part duplicates promoted q1/q2
   codimension results.

Give atom-by-atom verdicts from `CONFIRMED`, `CONFIRMED WITH REPAIR`,
`PROVISIONAL`, `REFUTED`, or `SCOPE-CONFLICT`.  State exactly what may be
promoted now and what must remain a proposed discriminator.  Include exact
checks, counterexamples, and the shortest corrected theorem statement.  End
with output path, SHA-256, exact model identity, checks run, and scope
firewall.
