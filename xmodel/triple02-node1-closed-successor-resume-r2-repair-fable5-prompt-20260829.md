# Production task: TRIPLE02 node-1 closed-successor resume R2 custody repair

You are Fable 5 at maximum reasoning.  Produce a fresh R2 packet that keeps
the R1 mathematical recursion fixed and repairs the hostile source review's
custody blockers.  Do not overwrite or patch R1.

Read in full:

- `/Users/dc/code/math/jc2/xmodel/triple02-node1-closed-successor-resume-r1-fable5-20260829.md`;
- `/Users/dc/code/math/jc2/xmodel/triple02-node1-closed-successor-resume-r1-hostile-review-sol56-20260829.md`
  (full SHA-256 `ad25f49675f26128d220a5954c8e7f72580060b929e42868eeefaefd75cfafb2`,
  body hash `019ac48bef4ff4999e76d17dde7a4428c48accb1c8d35b26702e0283df898caf`);
- the packet
  `/Users/dc/code/math/jc2/cases/ggv_8_28_upper_endpoint_triple02_closed_successor_resume_r1_20260829/`;
- only the seven exact frozen dependencies named by that packet/review.

Do not access, list, search, build, status, or control `jc2-lean`.  Do not run
`git status` or another workspace-wide command.  No web, AWS, local Singular,
msolve, Sage, PARI, or other heavy CAS; no canonical edits; no commit/push.
Bounded Python/source/archive negative controls are allowed.

Write only a fresh packet directory
`/Users/dc/code/math/jc2/cases/ggv_8_28_upper_endpoint_triple02_closed_successor_resume_r2_20260829/`
and producer report
`/Users/dc/code/math/jc2/xmodel/triple02-node1-closed-successor-resume-r2-repair-fable5-20260829.md`.

Mandatory repairs:

1. C1: eliminate the unbound live candidate and post-archive TOCTOU.  Create
   one content-addressed decision record that independently binds the exact
   job tag/nonempty nonce, source archive, lease/worker identities, production
   and classifier summary/verdict hashes, complete artifact manifest/archive,
   and candidate.  Late authority must freshly extract/replay the archive,
   derive/compare the same candidate, and bind the public marker to the
   archive and decision digests.  Never reread an unbound live candidate.
2. C2: bind every dynamic stage to this exact job and regenerate it for
   classification.  Result records must hash the required identity and bind
   nonce/job tag, lease, supervisor/worker/child identity and starttimes,
   uid/PGID/SID/cgroup, caps/argv, exact Singular binary digest, source hash,
   script/stdout/stderr.  The classifier must regenerate and byte-compare
   every reduce/rank/saturation/chart script and exact required artifact set;
   copied/missing identities and fabricated one-line stages must refuse.
   Likewise derive each bounded no-verdict from the exact driver state and
   reason, not an allowlisted string.
3. C3: success is eligible only in an externally owned systemd unit/scope
   with independent `RuntimeMaxSec`, `KillMode=control-group`, zero swap,
   aggregate memory/task limits, and supervisor-death containment.  Make the
   PGID fallback `NO_VERDICT`-only or remove it.  Supply a precise disposable
   Linux rehearsal plan; do not claim the live behavior has run.
4. Require a nonempty schema-checked nonce.  Make direct-supervisor entry
   acquire/verify the same atomic lease or fail closed.  Use no-replace atomic
   terminal publication.  Make archive verification exact over both regular
   files and directories; add an empty-directory negative fixture.  Bind the
   exact external launcher hash to its own launch manifest/preflight.
5. Correct the stale R3 and proper-open archive member-count prose
   (454=391+63 and 574=490+84).  Preserve the source-confirmed mathematics:
   node-2 seed `(g1,g2,Delta_node1)`, bound 6, no node-1 open reentry,
   structural rank bound 6, repaired two-containment saturation theorem,
   complete rank-0..6 charts, exact endpoint coefficients/plants, and the
   narrow allowlist.  Do not broaden to radical/geometric/whole-component or
   JC2 claims.
6. Run ordinary and optimized bounded selfchecks plus hostile fixtures for
   candidate swaps before/after archive installation, copied prior-job stage
   trees, missing/wrong identities, wrong scripts/caps/binary/source/nonce,
   regular and empty-directory extras, terminal collision, and every final
   timeout/swap/containment/archive fault.  No fixture may claim to test live
   process behavior unless it actually does; that remains AWS rehearsal debt.
7. Freshly seal all sources/dependencies and make status
   `SOURCE_READY_AWS_NOT_AUTHORIZED`.  The only next action licensed by the
   report is a different-model hostile source review; no AWS launch.

End the report with a body self-hash and give exact packet/archive/report
hashes, test counts, and a C1--C3/other-findings repair crosswalk.
