# Bootstrap launcher and original-clock interface

Verdict: CONCRETE SOURCE IMPLEMENTED / STATIC UNEXECUTED / QUALIFICATION
OUTSTANDING. One launcher.py supplies the service-entry checks and same-PID
exec into the unchanged repaired bootstrap. There is no installer, holder,
FIFO, controller family, registration generator or source alteration. No
unit, approved card/spec, host observation or runtime result is emitted now.

First action2026-09-12 17:01:49 UTC; fixed reserve17:20 / hard17:23 retained.
The initial six target checks were absent; the subsequently chosen single
launcher.py name was also explicitly absent before creation. The Python
entry replaces a shell launcher to avoid ad hoc JSON parsing, shell-token
expansion and a second helper. It is source text only, never imported,
compiled, parsed, syntax-checked or executed during this task.

## Acyclic concrete interface

An observed PREARMED monotonic timer supplies the original task epoch.
outer_started_utc means that earlier task/watchdog epoch, NOT a later
service-start claim. The launcher compares the actual timer's paired
ActiveEnterTimestamp and monotonic property with the approved card; its
receipt separately records actual ExecMainStartTimestamp, monotonic service
activation, MainPID and InvocationID. There is no predicted actual timestamp.

ROOT first prepares/qualifies static files and the ordinary task slice and
watchdog units. After arming the watchdog, ROOT records the actual epoch,
freezes the bootstrap spec, then freezes a card containing that spec SHA.
The outer unit contains the card SHA in its literal ExecStart. Neither spec
nor card contains the future outer-unit SHA. ROOT retains the final exact
card/spec/unit readback as a SEPARATE launch commitment. Review/qualification
hashes concern independently completed method/installed qualification, not
a recursively hashed future deployment. The generated registration remains
the repaired bootstrap's responsibility; its old acyclic interface is unchanged.

The source verifies literal ROOT0444 unit texts, no drop-ins, actual armed
timer and unfired guard, actual active/running service and exact process,
host/boot/namespaces, original null and stream identities, pinned tools/spec/
bootstrap, and the fixed ancestor resource/accounting attachment. It issues
only three read-only systemctl property calls, waits for them, closes extra
descriptors and execs the exact pinned bootstrap argv/environment. Ordinary
systemd plus env -i removes injected variables without a surviving parent.
It does not start/install a service, manufacture approval or reset timers.

## Chosen monotonic cutoff and limitations

The separate bootstrap120-second monotonic check plus UTC deadline is NOT,
alone, a proof of epoch-relative120-second binding under a later realtime
rollback. ROOT supplied a scope-preserving simplification during this task:
PREFLIGHT_ONLY_9 can instead have the ENTIRE experiment stopped by one
prearmed original-epoch watchdog. This source freezes exactly that choice:
OnActiveSec115, then the existing outer five-second TERM/KILL stop budget.
No binding-specific monitor/cancellation is introduced. Entry requires both
clocks younger than100seconds. Work that does not fit is STOP, never a
preflight success. Declared source/phase maxima stay unchanged; the additional
strict experimental limit is not a runtime forecast or cap increase.

RuntimeMax2875 plus the service's already-active monotonic start and stop5
also preserves a conservative sub3000 whole-task envelope, but the115+5
watchdog is the selected earlier cutoff. Scheduling/I/O/timer behavior is
still an OS qualification, not a hard-realtime theorem. The original
absolute admission/math/task/retirement fields remain fixed and checked.
All freeze/install/handoff time consumes the earlier epoch. Worker-side
administrative resource costs must be charged to the fresh exclusive task slice;
the watchdog stops both that slice and the outer unit, while itself staying
outside the stopped slice. ROOT must qualify propagation and each administrative
member's termination budget; outer.cpu.stat alone
cannot contain work done before the outer existed. The entry checks the
literal ancestor identity/resources/counter, not that historical coverage.

Outstanding required qualification: exact installed native closure/tool
behavior; systemd property rendering, effective loaded unit correspondence
after reload, no cached/override drift; source/output/stdio/cgroupfs/ACL/null/
namespace observations; original timer and independent retirement behavior;
fresh ancestor coverage and all elapsed costs; snapshot sizes and duration.
No checksum or approval string supplies those facts by itself. ROOT must
complete the external final loaded-unit/clock readback before starting;
matching unit-file bytes in this entry does not prove cached properties.
No worker allocation is licensed by this packet.

## Controls, read scope and closeout

Two explicit unexecuted semantic controls are in CONTRACT. Rehash/reapprove
a card/spec with a false epoch while retaining the actual timer: paired
property comparison rejects. A genuine epoch at age100seconds fails entry
even while its timer still waits. Wrong approved spec SHA fails before any
systemctl call or bootstrap exec. Failures emit only LAUNCH_STOP/exit2 in
the original streams; no leaves/authority/registration are made by this
launcher. LAUNCH_ENTRY_CHECKED_NOT_BOOTSTRAP_RESULT is never a positive batch.

OPEN quantity: whether the concretely bound launcher/native path and nine
unchanged controls complete within the chosen original120-second whole
experiment. Cheapest test: focused actual-code FIRST, then ONE separately
ROOT-registered AWS launcher/native observation with exact terminal custody;
120seconds is an UNMEASURED planning cutoff, not expected completion time.
No retry, profile farm, source outcome or further execution is authorized.

Exactly five inputs were freshly pinned and read WHOLE, COORDINATION first.
A clipped aggregate COORDINATION output was recovered in an unclipped
221–660 reread. Repaired bootstrap, contract and terminal intake were all
fresh WHOLE; no old holder, linked source, peer/native recipe or external
body was read. Root's suggested stricter cutoff is disclosed above, not
treated as observed evidence. Own source/contract/PINS/report undergo WHOLE
readback and the five source pins are rechecked before sealing. Collision
check is own-only: this is one service-entry implementation, not another
review of the closed-child runner or a replacement orchestration framework.
All authored bytes used apply_patch and the unchanged administrative
begin/close/finalize/expected-verify transaction. Final custody binds every
input and owned artifact and excludes its own self-hash.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6815`.
- Body SHA-256:
  `5f7d8655a98255583413000731618564ed68b4462bfb62c21c4ad59bdc32e660`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
