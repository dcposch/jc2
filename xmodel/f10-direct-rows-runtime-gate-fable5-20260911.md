# f10-direct-rows-runtime-gate-fable5-20260911 — FIRST Fable 5.1 static gate on the disabled direct-rows source runtime (dispatcher, ROOT-policy probe, independent T mutator, disabled registration)

status: UNSEALED (adapter seals; no Seal, charge_basis or transaction authored). STATIC ONLY: manual text and hash reading; zero subprocess, import, AST, syntax, compile, test, CAS or dummy execution of any size; no coefficient, baseline, fixture, place or candidate body exists or was generated. This is NOT a runtime observation.
opened: 2026-09-11T03:47:26.629931185Z (true first action: date, absence check of both owned targets, listing of the input directory). Both owned targets were ABSENT at that instant: xmodel/f10-direct-rows-runtime-gate-fable5-20260911.md and box/f10-direct-rows-runtime-gate-fable5-20260911/ (directory absent).
reserve: ORIGINAL publication reserve 2026-09-11 04:13 UTC / HARD 04:16 UTC, never reset. Runtime 2400 does not extend it. USER TERM/KILL+5 s stays armed.
basis: 0d39df3c9fd69c939a8420c54d03228b9077777d (HEAD of the session snapshot; no git command run).
writes: apply_patch only, each under 1500 words; owned targets are exactly this file and box/f10-direct-rows-runtime-gate-fable5-20260911/input_custody.md.

## 0. Custody — 13 charged inputs, pinned 03:48:46 UTC BEFORE any body read

All 13 sha256sum values at /tmp/jc2-lane.FaRhLi/inputs equal the charged list verbatim (13 unique basenames; `sha256sum -c` printed 13 OK, rc=0). The full hash table, line and byte counts and post-read pins are in box/f10-direct-rows-runtime-gate-fable5-20260911/input_custody.md. Every file was read FRESH_WHOLE through EOF in this task in one parallel read each (dispatch.py 661 lines, run_capped.py 1198 lines, mutate.py 260, probe.py 186, authority.py 133, DISPATCH.diff 306, the four documents and the JSON template). No read clipped; no prior-WHOLE attestation reused; no linked, provenance, live or uncharged source read (the five scientific files other than the frozen authority.py, the old accepted dispatcher, PINS.json, READ-SCOPE.md and any live peer output were NOT read).

Scope imported from the two charged FIRST reports only at stated scope: the old source runtime gate (05775a6f…) for the accepted CAPRUN reading, its telemetry/argv-hash facts and the inherited ESRCH false-STOP; its speculative cost, reconstruction-dominance and likely-timeout claims are NOT imported. The Astra runtime report (ee0f1b5b…) is the author's own description and is checked against the code, never trusted. authority.py (5629db5d…) is the frozen, NOT yet accepted metadata helper; only its interface with this runtime is inspected.

## 1. Verdicts A–F

Line numbers refer to dispatch.py unless a file is named. Every statement is a hand reading of the charged text.

### A. Metadata/entry composition and genuine refusals — CONFIRMED (acyclic scheme; one named ROOT derivation obligation still open)

Composition: 24 fixes the five basenames; 156-158 take the five final hashes from the registration as 64-hex; 188-193 require source_pins to be a 5..64 absolute-key vector in which each installed absolute path `science_dir/name` carries exactly the source_files hash, the same rule authority.py:107-109 enforces from the child side (`sp.get(join(base,name)) == files[name]`); 224-225 and 311-313 bind the installed bytes to those hashes at startup and every revalidate; 194 binds contract_sha256 by shape, 195-200 bind place by string shape only (p/degree bounded decimals, phi and P7_mod_p lists of 2..8 decimals), exactly the shape authority.py:110-116 checks and no arithmetic. Science authorities 376-383 carry exactly the eleven keys authority.py:79 demands (job_tag/status/authority/mode/artifact/files/runtime/limits/contract_sha256/source_pins/place); runtime carries the interpreter pin and the simple inventory path and hash (authority.py:117-127). Entry: 414 runs the ACTUAL installed `science_dir/produce.py` (or check.py) by absolute canonical path with the eight-token tail of 404-405 (`--registered-job TAG --authorization AUTH --authorization-sha256 HASH --output|--input PATH`), so sys.argv has nine elements and argv[0] equals `join(realpath(dirname(authority.py)), produce.py)` as authority.py:25-33 requires; flags are `-E -s -S -B` (415-416), which leaves the science dir as sys.path[0] for the child's own `import authority` and lets authority.py:36 strip it before stdlib imports. Refusal branches, traced in authority.py's own order (12 optimize, 14 mode, 16 platform, 18-24 DMI, 25-29 argv, 30-33 entry, 34-35 inventory, 70-76 byte pin, 79-80 keys, 81-82 registration, 83-87 mode/path, 88-90 producer state, 94-95 limits/inventory, 98-100 per-file pins in FILES order, 101-116 contract/source_pins/place, 117-127 runtime): refuse-hash (402 supplies 64 zeros for a correct file) dies at 73-74 `authority/inventory byte pin` before any key is read; refuse-status (384) dies at 81-82 `no ROOT registration`; refuse-caps (385, cpu_seconds '599') and refuse-inventory (386, check.py key deleted) BOTH die at 94-95 `registered limits/source inventory`; refuse-source (387, produce.py zeroed) dies at 98-100 `source pin: produce.py` only after authority.py and arithmetic.py digests pass; startup-produce/startup-check (403, WRONG-JOB) die at 25-29 with SystemExit `REFUSED: registered CLI contract` before any file read. What the child then writes to stderr and which exit code it returns is decided by the final produce.py/check.py handling of a ValueError or SystemExit from authorize(), which is not an input here; the dispatcher therefore takes the seven (exit_code, stdout='', stderr) triples from a ROOT preflight policy (207-220) pinned in r['pins'], bound to the final source_files (212), with exit 1..125, empty stdout, ASCII stderr of 1..8192 bytes ending in newline, and compares bytes exactly (515-516, 526-527) with NORMAL_EXIT, no signal, absent payload. No wildcard, substring or generic nonzero acceptance exists in the code. Acyclicity: the preflight file binds source hashes, not the registration hash; every registered command vector is literal except the three typed slots (428-439), and the probe/refusal authority digests are canonical dumps (97) of registered data that contain a ROOT path, never a ROOT hash, so ROOT can precompute them without a self-cycle. Header hashes do not authenticate ROOT: trust is root-owned 0444 registration, exclusive writer, host/instance/boot/namespace/cgroup binding (168-177) and full native closure (226-232, 309-329). Blessing an arbitrary wrong failure: the code cannot tell an intended branch from a registered traceback of an unrelated bug, so this is exactly the remaining ROOT obligation, stated here without pretending it is filled: derive each of the seven triples from the final source TEXT (which line of authority.py is reached, how the entry converts it, which exit code), not from an observed run; record that refuse-caps and refuse-inventory share one branch and therefore identical bytes, so their independence lies in the mutation, not the observation; register the seven triples in an immutable preflight file whose source_files equal the final five hashes. If the final entries let a ValueError propagate, the registered stderr would be a layout-dependent traceback (absolute science path, line numbers); that is admissible by shape but is a source-FIRST matter to prefer a one-line refusal. The old hard-coded science hashes and the old traceback matcher are gone (DISPATCH.diff 16-22, 269-285); the template's UNFORMED placeholders cannot pass 154-163.

### B. Separate ROOT-policy helper (probe.py) — CONFIRMED (no always-refuse contradiction found; two unmeasured resource obligations)

probe.py imports only stdlib (2-10) and never imports authority or arithmetic; its CLI (141) is the eleven-token vector the dispatcher builds at 411-412, and 144 reconstructs `actual` as `[realpath(sys.executable), -E, -s, -S, -B] + sys.argv`, which equals the dispatcher's `py` (415-416) because the interpreter role is canonical (181, 183) and `--cwd science_dir` (424) does not enter sys.path for a script run by absolute path. root_policy 65-137 repeats the immutable ROOT barriers as the science user: 0444 root file with root-owned traversable ancestry (39-49), enabled schema/tag/exclusivity (68-69), FIRST clearance string (70), four ordered UTC stops (71-73), caps/environment equal to the live environment (74; the dispatcher launches CAPRUN with env=ENV at 453 and setpriv does not alter it), science UID/GID (75), EC2 vendor/asset tag/hostname/boot/namespace (76-80), owned cgroup with memory.max (81-84), own-file pin under r['files']['probe'] (85-86), interpreter pin and no-site ordinary startup (87-89), then the registered command's suffix equals `actual` and equals /proc/self/cmdline (90-96) with NO ROOT hash slot for the probe role (91-95 only fire for role mutator), isolated five/three-file inventories (97-100), full native closure with every pin and native file rehashed (101-107), four-way source/reference/pin agreement (108-110), directory/alias/absent/sys.path/own-maps closure (111-130), simple subset (131-135) and ROOT drift (136). main 146-157 then binds the policy file to `authority_dir/<mode>.json`, its digest to argv, the output to `writer_dir/<mode>.payload` absent, and the nine-key expected record (151-153) equals the dispatcher's 370-372 object field by field (schema, root_registration_path as a path string, job_tag, label, artifact, science_outcome NONE, source_files, LIMITS, profile from r['profiles'] which 164-165 pins to PROFILES). The separate policy digest is deterministic and contains no ROOT hash; the valid event is ROOT_POLICY_ONLY_SENTINEL (166), consumed at 519-521 with UID/GID identity and empty streams, and is nowhere treated as source authorization. Dummy 167-182 is the original scenario: parent_started, fork, child ignores TERM and handshakes, parent_exit_pending then _exit, orphan waits for reparenting, touches 64 MiB, orphan_allocated_64MiB, pause; the shared file offset is serialized by the handshake. CAPRUN (run_capped.py 860-913, 475-518, 431-454, 368-390) keeps the zombie leader as the identity anchor, samples exact-PGID RSS at 0.05 s, sends TERM 15 with 1 s grace then KILL 9 after a MATCH on pid/pgid/start identity, reaps once; 530-543 require RESOURCE_CAP/rss, both sends, leader reaped, cleanup complete, empty live list, exactly one MATCH and one SENT per stage, the four-event order, parent/orphan identity equalities, pid == leader, all pgids == leader, RSS > 32 MiB. setpriv execs the interpreter in place, so the leader pid is the probe's pid. Composition of -E/-s/-S/-B with path/cwd: sys.path[0] is the wrapper dir, allowed by 123-124; -S gives no_site == 1 (89). Contradiction search under a correct registration: none found; two obligations are unmeasured, not contradictions: (i) the probe rehashes the whole native closure (≤3000 files) inside the 5 s wall / 3 s CPU control profile, and (ii) the dummy's barrier must stay under the 32 MiB group RSS cap before the fork, or CAPRUN would kill it early with the wrong event sequence (a STOP, never a false pass). Only two files may lie outside the science UID's read: none, since registration, manifests and policy are root 0444 in root 0755 ancestry.

### C. Independent finite-coordinate mutator (mutate.py) — CONFIRMED, with one GAP on the inverse-restoration test

Authentication before any coefficient byte: stdlib-only imports (2-10); seven-token CLI (141) equal to 407-409; `actual` with -I -S -B (142) equal to 415-416; root_policy for role mutator with the single ROOT_REGISTRATION_SHA256 slot rewritten to the live hash (91-95) and argv[4] equal to it (144); phase policy recomputed and required equal to r['phase_policy'] (145-150) with positive_record at argv[6]; the positive record must be root 0444, have exactly the eight keys the dispatcher emits at 554-560, status POSITIVE_CHECKED_NO_SOURCE_OUTCOME, the live ROOT hash, the same policy and source_files, and checker_stdout_sha256 equal to the SHA-256 of the literal `CHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME\n` (151-156); baseline path/SHA/bytes bound (157-159); the check-positive authority reconstructed as the exact eleven-key object (163-169) equal to the dispatcher's 376-383 emission for that label (mode check, artifact = frozen baseline and its hash, files, runtime with the interpreter pin and simple-inventory path/hash, LIMITS, contract, source_pins, place) WITHOUT importing authority.py or calling authorize; writer 0700 UID/GID (170-172). No checker argv is faked and no science module is imported. Wire: no-number parse with duplicate-key refusal (185), byte round-trip `wire(doc) == raw` with the producer's canonical form (181-186), exactly six top keys with schema f10-r3-direct-rows/v1, job_tag, contract, source_pins and place equal to ROOT's (186-189); place p in 2..2^31-1, f in 1..7, phi length f+1, P7 length 8, leading 1 LAST per ROOT-ADOPTED-WIRE-PINS item 1, residues in 0..p-1 (193-197); graph keys exactly Hq,c,c_inverse,zeta,slots,ell,g,U with 25 slots (198-200); c and c_inverse dense length f (201-204); the 30 polynomials with weights [8,9,10]+[19..11]+[22..11]+[27] and Hq 7, zeta 7, ell 23, g 30, U 4 (205-206), matching the wire proposal 94-97; typed `{kind:graph, terms}` with ≤20000 terms, aggregate ≤400000, four exponents ≤256, weight sum with variable weights 1..4, strictly increasing NUMERICAL tuples, nonzero dense vectors (208-218). Edit (219-234): only slots[24] at exponent (27,0,0,0), coordinate 0 += 1 mod the authenticated p; absent term inserted as `1,0,…,0` in numeric order only if both term caps have a spare (226, closing the old 400000 edge); all-zero vector deletes the term; canonical decimal re-encoding; joint `len(changed)+len(raw) ≤ 125829120` and `changed != raw` (236); output and receipt O_EXCL with fsync and readback hashes (241-251); receipt exactly as 567-573 demand, FORMED_UNCHECKED, checker_run false. GAP (non-blocking, fail-closed): the "inverse restoration" at 237-238 restores a deep copy of the original slot (220) and requires `wire(doc) == raw`. That proves the edit touched nothing outside slot 24 and that serialization is byte-faithful, and with canonical dumps it also proves `changed` differs from `raw` only inside slot 24. It does NOT exercise the arithmetic inverse: an edit at the wrong coordinate, wrong exponent or wrong modulus would pass this test and still yield a receipt saying inverse_restored_whole_bytes true with the registered mutation string; the negative checker would still report slot T, so such an error would not be caught anywhere. By static reading the edit is the registered one (223, 230), so no defect exists in the text; the test is simply weaker than its name. Smallest correction, mutate.py only, dispatcher unchanged: after computing `changed`, re-parse it, apply coordinate 0 -= 1 mod p at (27,0,0,0) with the symmetric delete/insert, and require the result to wire to `raw` before restoring. The mutator does not re-prove primality, irreducibility or source correctness; those come from the genuine positive check, as scoped.

### D. Full phase/semantic composition — CONFIRMED

LABELS 32-34 are the thirteen names; 267 requires the registered command keys to equal them; execute() 510-593 runs them in that order and 586 requires the ledger to equal LABELS; 393 forbids any second launch of a label, so there is no retry. The positive record 554-560 is emitted only after run() returned through group_quiet (484), revalidate and size budget (485), post-launch pin drift on authority and input (486), joint caps (487), CAPRUN telemetry binding of argv hash, count, runner exit, cwd, caps, leader identity and stream path/SHA/bytes (488-497), the required live runner and python captures (503-504, no retry), NORMAL_EXIT/0 (551) and the exact stdout literal with empty stderr (552-553). One fixture: the writer inventory must be exactly valid.payload, dummy.payload, mixed-row.json, mutation.json (577-578); refusal and startup payloads never exist (515, 526); fixture and receipt are moved and frozen root 0444 (579-580, 595-607) and the frozen directory becomes 0555 before the single check-negative (581); that run gets a fresh input hash and hence a fresh authority digest in its typed slot (400-402, 428-437), and 576 requires the fixture hash to differ from the baseline. Negative predicate 583-585: NORMAL_EXIT with resource None and child_returncode 2, child_signal None, child_exit_code 2, EMPTY stdout, stderr bytes exactly `CHECK FAILED: direct-source graph slot T\n`; a traceback, parser refusal, resource stop (CAPRUN types WALL_TIMEOUT, RESOURCE_CAP/cpu on SIGXCPU, RESOURCE_CAP/rss, SIGNAL, RUNNER_FAILURE at run_capped.py 888-938) or any other label fails closed. The literals FORMED_UNCHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME (546) and CHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME (552) equal the wire proposal 112-114 and the adopted pins item 7. The dispatcher never parses a packet; the mutator validates wire grammar only; no coefficient check or rank theorem exists in either. Terminal status 587-591 is DIRECT_ROWS_AND_T_REJECTION_CHECKED_NO_SOURCE_OUTCOME with science_outcome NONE, other controls NOT_RUN_SPEC, and main 657 returns 0 only for it.

### E. Caps and custody preserved without a new supervisor — CONFIRMED

LIMITS 25-26 (900/600/8 GiB/128 MiB), BATCH_LIMITS 27-28 (3000/2100), PROFILES 29-31 (control 5/3, dummy 5/3/32 MiB, produce and check 900/599, mutate 30/20), TOTAL/PACKET/META 36 and budget() 287-289 (elapsed+reserve < 3000, now+reserve < mathematical stop, sampled cgroup CPU < 2050 with 50 s nominal cleanup, explicitly not a kernel aggregate) are untouched by DISPATCH.diff; kernel enforcement remains per-child RLIMIT_CPU soft/hard+1 in CAPRUN (639-659), the outer cgroup memory.max at init and every revalidate (177, 298), RLIMIT_FSIZE 120 MiB and core 0 in every child (448-450), and tmpfs <= 128 MiB (332-333). size_budget 351-366 keeps the four packet paths under 120 MiB jointly and everything else under 8 MiB. Exact command comparison and typed slots (425-440), CAPRUN runner and python live capture with UID/GID/namespace/capability/maps checks (456-474) and the mandatory capture for the four science phases (503-504), group quiet before telemetry/stream/packet reads (484), immutable move/freeze (595-607) and byte-identical fsynced durable copies under the original cutoffs (609-636) are all outside the diff. DISPATCH.diff review: the 19 hunks change only the docstring, JOB, the SCIENCE constant to registration-derived hashes, schema/review_clearance/source_pins/contract/place/preflight bindings, the preflight path in the outside-tmpfs list (a strengthening), the mutation string, the root-policy authority branch and three new authority keys, the probe argv, the refusal loops (now the actual producer entry under ROOT-supplied exact bytes instead of hard-coded ValueError strings), the sentinel name, the two literals, the receipt schema, the exact exit-2 stderr in place of the removed traceback matcher, and the custody/result names. Two guarantee changes are intended and disclosed: refusal bytes moved from code into a pinned ROOT file (A), and the negative predicate became strictly tighter. No cap, retry, supervisor, worker control or ESRCH hardening entered; identity() 143-144 still catches only FileNotFoundError, so the inherited transient-process false STOP is retained as charged. No physical, native, worker, cgroup or clock evidence exists.

### F. Readiness and next boundary — CONDITIONAL ACCEPT (static), blockers named

This disabled port can be accepted statically, conditional on (1) the combined source FIRST of the five files, (2) ROOT's source-specific derivation of the seven refusal triples as named in A, and (3) a fresh ROOT registration. Precise blockers versus honest unformed fields: blockers are only (1) and (2), because the code cannot form them itself. Unformed deployment fields, correctly left UNREGISTERED/UNFORMED in DISABLED-REGISTRATION.json (which covers every key the dispatcher, probe and mutator read, with commands and typed_slot_policy already literal), are instance/host/boot/namespace/cgroup/CPU origin, UID/GID, six role files and pins, both native manifests, science/wrapper/output/durable paths and device, outer argv, four stops, thirteen command vectors with precomputed policy digests, source_files, source_pins, contract, place, review_clearance and preflight_policy. Resource unknowns, not defects: full-closure rehashing inside the 5 s/3 s profiles for probe and inside refuse-source for the producer entry; the mutator's 30/20 profile must cover its own closure rehash plus one hooked json.loads, per-term regex validation of up to 400000 terms and two canonical dumps of a wire that must stay under about 60 MiB for the joint cap. Optional style: none required; the C correction is optional and non-blocking. The eventual registered thirteen-call observation already contains its dummy, positive and controls; no extra smoke farm or unchanged source-positive replay is requested. ROOT should finish the worker-independent preparation (derivation, registration, manifests, digests) before any allocation. This FIRST is not execution, not a baseline, not a source exclusion, not a rank result, not an all-r proof and not a JC2 solution; the parked rational rank runtime is not an input and is not qualified by this wire.

## 2. Verdict table

| item | verdict |
|---|---|
| A eleven-key authorities, absolute source_pins, actual entry and nine-argv, five refusal branches traced, seven triples ROOT_UNFORMED | CONFIRMED; obligation: derive triples from final source text, caps/inventory share one branch |
| B ROOT-policy probe: acyclic policy digest, full barriers, exact suffix, sentinel not authorization, dummy four-event TERM/KILL scenario | CONFIRMED; unmeasured 5 s/3 s closure hashing and 32 MiB pre-fork RSS |
| C mutator: positive-record authentication, reconstructed check-positive authority, six-key wire, 30 polys + 2 scalars, exact T edit | CONFIRMED; GAP: copy-restore is not an arithmetic inverse test (optional fix in mutate.py only) |
| D thirteen phases, positive record ordering, one fixture, exact exit-2 negative, literals, terminal status | CONFIRMED |
| E caps/custody unchanged, diff intent-only, inherited false STOP retained | CONFIRMED |
| F readiness | CONDITIONAL ACCEPT on source FIRST + refusal derivation + registration |

## 3. OPENS RAISED

No new canonical identifier. QUANTITY: registered refusal triples derived today = 0 of 7, and observed phases = 0 of 13; whether the final five-file source, under this port, reaches the two positive literals and the exact slot-T exit 2 inside the unchanged 900/599 per-phase and 3000/2100 joint caps is UNKNOWN. CHEAPEST TEST: the combined source FIRST on the frozen five files, then ROOT's text-derived seven triples, then ONE registered thirteen-call observation; no toy, no replay. NUMERIC PLANNING WALL (UNMEASURED, static): child allocation 2775 s < 3000 s joint and hard child CPU 1857 s < 2100 s, both static sums; no speed or timeout expectation is imported from the old batch, whose 900 s wall hit had no baseline. This review is static reading and writing from 03:47 UTC to the final timestamp in section 5, inside the 22-25 minute planning figure; no CAS or process was run.

## 4. COLLISIONS

status: EMPTY — own-only inspection: both owned targets absent at 03:47:26.629Z; this lane wrote exactly xmodel/f10-direct-rows-runtime-gate-fable5-20260911.md and box/f10-direct-rows-runtime-gate-fable5-20260911/input_custody.md (plus the box directory) by apply_patch; no other file, input, source, CAPRUN, frozen report, protected or shared object touched; no corpus scan; no scientific or runtime source modified.

## 5. Completion attestation

Timeline (UTC, 2026-09-11): 03:47:26.629 first action and target absence; 03:48:46 all 13 pre-read pins equal to the charged list (13 OK); 03:48-03:54 all 13 bodies FRESH_WHOLE to EOF by parallel whole reads; 03:55:15 skeleton (no marker) and custody file by apply_patch; 03:56:33 verdicts A-C; 03:57:57.871 all 13 post-read pins unchanged (13 OK) and every 64-hex token in both owned files matched against live sha256sum output (0 unmatched); 03:59:02 verdicts D-F, table, OPENS RAISED, COLLISIONS and the custody post-pin note; 03:59:15 own WHOLE read of both owned files and a second token cross-check (0 unmatched, no marker present); then the custody-order correction and this section with the marker last, inside the original 04:13 reserve, never reset. Disclosures: one apply_patch write (verdicts A-C) measured about 1740 words by wc, above the 1500-word bound; one D-F attempt failed on a fragment anchor and wrote nothing; the custody post-pin block was first inserted after the title and then moved by a further patch. Report size by wc -w is above 3000 because hashes, table syntax and line-number tokens count as words; the substantive prose is inside the 2200-3000 target. Tools: date, ls, mkdir (box directory only), sha256sum, wc, cat, tail, grep, awk, sort, comm, printf, sed on a shell variable, apply_patch; no Write/Edit, no redirection into any owned or other file, no generated text, no artifact transaction, no input edit, no git command. Zero mathematical subprocess/import/AST/syntax/compile/test/CAS of any size including any no-math dummy; zero coefficient/candidate/baseline/fixture/place body; zero network/AWS/SSH/process control/agents/protected/shared/live/provenance/extra sources. No scientific or runtime source was modified. No Seal and no charge_basis authored; the adapter seals. Final own-file hashes are reported in the terminal response. All writers idle after the marker below, which is the only standalone marker in this file and is followed by nothing.

<!-- BODY-END -->
