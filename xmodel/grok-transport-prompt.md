Hostile review of TRANSPORT.md + cases/transport_check.py in
/Users/dc/code/math/jc72108 (full access, python3 — run the checker).
Under review: Sol's partial repair of REDUCTION gap G2 — a transport
theorem for the GGV-SELECTED polynomial standard pair (cusp peak
reduction; lexicographic minimality; signed-axis transport), with
honestly stated scope limits (does NOT decorate Eggers-Wall trees, does
NOT cover post-Laurent [P,Q]=x^j objects, does NOT repair book coverage
or the td bound). Attacks: (1) the cusp-peak-reduction mechanism — replay
the main lemma exactly; (2) the machine gate: what does transport_check.py
actually verify vs what the prose claims (gap between checked and
claimed = the classic hazard); (3) the SCOPE statement: is the claimed
repair genuinely a repair of part of G2 as REDUCTION.md defines it, or
does the "GGV-selected" restriction reduce it to G1's existential
selection restated (check against REDUCTION.md T-chain: which T-link
does this actually prove); (4) the residue-A + td-7 instance
verifications; (5) anything FALSE (not just unproven). Deliverable:
write xmodel/grok-transport-review.md, VERDICT first line, findings
ranked. No other repo modifications. No git.
