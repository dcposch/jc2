# Blind ideation 20260903T1200Z — Sol 5.6 ultra

**Packet custody.** The sealed packet at
`/tmp/jc2-lane.QuKMx3/inputs/ideation-20260903T1200Z-packet.md` was read first.
Its SHA-256 was independently verified as
`c90fa6876b069478f658845be6efa2bd056b2e340af7284104af4d042e6c4f95`.
I did not read another `ideation-20260903T1200Z-*` submission, any unfinished
current-lane report, or `jc2-lean`, and I did not edit a canonical ledger.

## 0. Verdict and direct answer

**The all-degree program is now a coefficient obstruction program indexed by
the whole major tree.**  The boundary has done its job: it computes the
integer (N), and the morning's work makes especially clear that it does not
bound (D).  The whole-major-tree theorem has also done its job: it compiles
a selected Moh path into the finite set of admissible sibling multiplicity
trees.  Neither layer asserts that the node polynomials are simultaneous
initial forms of one pair (f,g).  That simultaneity, followed by global
polynomiality, is the remaining freedom and the only credible main proof
front.

Concretely, the program should be:

1. use `C_FULL_TREE_ODE` (and exact orbit packing, not `(UNI)`) as a **target
   generator**, not as a hoped-for degree ceiling;
2. attach to each surviving full tree one common coefficient scheme, with
   edge sharing, Galois action, the local ODEs, the Jacobian equations, and
   the global `DEG/POLY/NO-RESIDUE` equations;
3. eliminate or lift that scheme order by order, using componentwise
   Fitting/saturation rather than equation counts; and
4. where (u_s=1), descend once to the smaller monomial-Jacobian scheme and
   run the same coefficient obstruction there.  The descent is compression,
   not a substitute for coefficient consistency.

The coordinator's framing needs one material correction.  The packet calls
theorem (T) a height-(2), four-integer problem.  That is true for Moh's
printed (s=3) controls and for the now-dead (D=105) descendants, but not
for the all-degree frontier.  Among the stored passport-screened (D=108)
survivors are

```text
(n,m)=(108,72), M=(24,78,106), V=(1,1,5) or (1,4,5), d_s=6, u_s=1.
```

The licensed degree/Jacobian part of one Prop. 6.3 step gives

```text
(n',m')=(18,12), J=c gamma^3, M'=(4,13), and s'=3.
```

The natural (V'_i=V_i) map used by the harness gives candidate values
(V'=(1,1)) or ((1,4)); its source evidence was only the p.207 matches, so
this higher-depth state is itself a gate.  Under that map the descendants
have the same four-tuple ((18,12,M'_2,V'_2)=(18,12,4,1)) but different
(V'_3).  Throwing away the
height-(3) datum repeats, after descent, the same selected-path information
loss that the whole-tree theorem just repaired.  Moreover the proved formula
(delta'_i=(k+1)delta_i^{5.1}) was stated and checked on five (s'=2)
rows/ten rationals (`xmodel/descent-radii-grok46-20260903.md:235-266,
321-338`).  Applying it to (s'>2) is plausible, and `AUDIT.md:16073-16075`
does so, but the cited report does not prove that extrapolation.  The safe
all-degree statement is therefore **(T<=4)**: nonexistence of the relevant
monomial-Jacobian pairs with the *entire inherited tree* of height at most
four, together with the (u_s>1) alternative.  Height two is its terminal
client, not yet its universal form.

The reviewed tree remains the decisive advance (`AUDIT.md:15960-16015`), but
the next theorem must preserve its data: generate it, descend only when
licensed, and attack the shared coefficients.

## 1. Q1–Q4 disposition vector

The vector below is relative to the 1015Z synthesis and this packet.  `RAISE`
means allocate proof attention, not promote a claim.

| Question / candidate | Disposition | Reason |
|---|---|---|
| Q1: prove JC2 by eventual emptiness of `C_FULL_TREE(_ODE)` | **LOWER** | The screen is a powerful necessary target generator, but it has many survivors through (D=200).  No observed thinning is a theorem, and (N=O(1)) supplies no ceiling. |
| Q1: search (D\le400) for a screened cofinal family | **RAISE as falsification/target selection** | One closed-form survivor family would permanently stop the census-as-proof framing and give coefficient systems with repeated shape.  Failure through 400 is only data. |
| Q1: uniform (j=2) forced-zero theorem | **RAISE, retyped statewise** | There is an exact divisor parameterization when the DP `danger` bit is still set; Idea Card 3 states it.  It is not a row-level scalar predicate once a nonzero ancestor has cleared `danger`. |
| Q1: recover Moh's exact 1983 six-row program | **LOWER** | The historical artifact can settle provenance, not JC2.  The mathematical residual after the sourced tree is simultaneous coefficients; no desk test can recover the CDC listing (`AUDIT.md:15992-15997`). |
| Q1: edgewise full-tree recentering | **UNCHANGED / secondary** | Potentially useful, but it changes Prop. 5.4's quantifier and still leaves excess rows.  Do not consume it before a proof. |
| Q2: independent existence of every node's (p_v,q_v) | **RAISE, but insufficient** | The A.3 equation has an exact discriminant-saturated weighted-configuration form.  Nodewise nonemptiness must be separated from adjacent-node compatibility. |
| Q2: simultaneous sibling coefficients | **RAISE to the principal bottleneck** | Encode it as a fibre product of the node schemes over actual edge coordinate/jet maps, then add global polynomiality.  This is the missing universal coefficient object. |
| Q2: theorem (T) is identical to sibling coefficients | **RETYPE** | Descent is one receiver of the coefficient problem.  It is one-way, applies directly only on (u_s=1), and the first (D=108) descendants retain height (3). |
| Q2: raw tree-decorated moment engine | **REDESIGN** | Raw moments have zero cokernel/MDS behaviour and the next-coefficient terms do not stratify by level.  Use contracted tree messages as the global equations inside the coefficient scheme, not a free-standing rank oracle. |
| Q2: cheapest (D=108) discriminator | **RAISE** | First solve node schemes, then the first genuine edge fibre; in parallel attack the two ((18,12;\gamma^3)) descendants with the ramification-absorption block.  A bare 36-row moment count is not decisive. |
| Q3: four-integer, height-(2) theorem (T) | **LOWER as universal statement; KEEP as terminal lemma** | It omits (M'_3,V'_3) on the first screened frontier.  Replace it by (T<=4), preserving the inherited tree. |
| Q3: existing `[P,Q]=x^k` certificate machinery | **RAISE as backend** | It now has a precise input class.  Do not restart blind degree farming; compile Φ-supported, tree-inherited systems into the existing exact/saturation stack. |
| Q3: uniform scalar obstruction in ((n',m',M'_2,V'_2)) | **LOWER** | Two (D=108) descendants share those four integers and differ at (V'_3).  Search for a graded coefficient obstruction, not another scalar gate. |
| Q3: Appendix-II compiler | **RAISE** | The corrected ((15,10)) computation proves that approximate-root support plus exact saturation is a real kill mechanism.  Generalize its algebra, not its printed case split. |
| Q3: Kummer/character decomposition with (r=k+1) | **REOPEN / NEW** | It turns a monomial Jacobian into a constant Jacobian over a cyclic extension and block-grades the coefficient equations; Idea Card 2. |
| Q3: repeated Prop. 6.3 descent to height (2) | **LOWER pending proof** | The descended pair is no longer Keller.  The current transformation cannot simply reuse the constant-J formula; the naive second pass on the (D=108) row even emits exponent (-1), a warning, not a theorem. |
| Q4: another boundary/topological ceiling | **STOP** | The promoted exact-(N), PS-growth, and Hurwitz-dimension results price this route negatively. |
| Q4: exact local Hurwitz passports | **ONE CHEAP AUDIT, then likely stop** | Normalized passports through degree (8) showed no new failure.  A full actual-node test can either add a filter or prove this layer automatic; it cannot replace coefficient gluing. |
| Q4: coefficient obstruction as a tree complex | **RAISE / NEW FORM** | The local weighted schemes, edge equalizers, Kummer blocks, and global remainder equations form one finite exact object.  That is the uniform form the campaign lacked. |

Queued-front vector:

| Queued front | Disposition |
|---|---|
| Tree-decorated moment engine | **REDESIGN** as a tree contraction/equalizer inside the sibling coefficient scheme.  Require an actual full orbit/tree packet before emission. |
| p.202 residue through the sibling system | **CONTINUE after the first engine control**, as a 49-row/32-group regression batch, not as the first frontier lane. |
| box01 restart for heavy Gröbner | **STOP/HOLD** until a typed exact ideal survives sparse modular linear pages and exceeds the desk cap.  No fleet spend for an unknown-count system. |

## 2. The 46-row disposition vector

The vocabulary is the campaign vocabulary.  Reasons are supplied only for
changes; `UNCHANGED` preserves the row's current live/parked/dead typing and
does not revive it.

```text
 1 RAISE       2 RAISE       3 RAISE       4 UNCHANGED   5 UNCHANGED
 6 UNCHANGED   7 UNCHANGED   8 UNCHANGED   9 UNCHANGED  10 UNCHANGED
11 UNCHANGED  12 UNCHANGED  13 UNCHANGED  14 UNCHANGED  15 UNCHANGED
16 LOWER      17 UNCHANGED  18 UNCHANGED  19 UNCHANGED  20 UNCHANGED
21 UNCHANGED  22 UNCHANGED  23 UNCHANGED  24 UNCHANGED  25 LOWER
26 RAISE      27 UNCHANGED  28 UNCHANGED  29 UNCHANGED  30 UNCHANGED
31 UNCHANGED  32 UNCHANGED  33 UNCHANGED  34 UNCHANGED  35 UNCHANGED
36 RAISE      37 UNCHANGED  38 UNCHANGED  39 UNCHANGED  40 UNCHANGED
41 UNCHANGED  42 UNCHANGED  43 UNCHANGED  44 UNCHANGED  45 LOWER
46 UNCHANGED
```

Reasons for the eight changes:

- **1 — RAISE.**  `[P,Q]=x^k` is no longer merely the receiver of a vague
  degree farm.  It is the exact monomial-Jacobian coefficient class produced
  by Prop. 6.3, now with degrees and the p.207 radii mechanically checked.
  The raise is for (T<=4), Kummer grading, and inherited-tree clients, not for
  another blind cutoff farm (`AUDIT.md:16051-16080`).
- **2 — RAISE.**  This is the morning's load-bearing theorem: every
  above-threshold sibling extends, and the full-tree DP kills (D=105,117).
  It is now the front end of every proof and disproof coefficient target.
- **3 — RAISE.**  Prop. A.3 is no longer a one-path strip check.  Its
  (P-Qu\ne0) constraint applies to every (p)-root and cuts 60 to 58; more
  importantly, its root equations admit the exact barycentric formulation in
  Idea Card 1.
- **16 — LOWER.**  The D-module/Picard--Fuchs promotion from 1015Z did not
  produce a ceiling.  PS-3 is generically attained, raw moments have zero
  cokernel in the fixed-ray tests, and exact remainder coefficients do not
  carry a level dictionary.  Keep the language only as the dual bookkeeping
  of the coefficient complex.
- **25 — LOWER.**  The Hurwitz expected dimension is
  (max(k-2,0)\ge0) everywhere.  Exact passports remain a cheap local
  prefilter and the 19 rigid cases remain targets, but dessin combinatorics is
  not the all-degree endgame.
- **26 — RAISE, retyped.**  Not as a primitive-monodromy ceiling.  The exact
  local cyclic action, fixed root, nonzero (A_j)-orbits, and orbit
  partitions are indispensable state in `C_FULL_TREE` and in the coefficient
  scheme.  Do not infer global primitivity from them.
- **36 — RAISE.**  Structured exact search finally has a legitimate target:
  a screened full tree at (D=108), or its tree-inherited
  ((18,12;\gamma^3)) descendant.  This is not random sparse support search;
  a rational solution of the full original Keller system would be a
  counterexample, while a formal or descended solution would not.
- **45 — LOWER.**  The same reprice as row 16.  A generic inverse ODE is
  tautological; the useful ODE is already the exact node A.3 equation and its
  coefficient consequences in rows 1/3/29.

Rows 4, 6, and 29 retain only their typed roles: decorated tails,
approximate roots, and coefficient operators.  `UNCHANGED` does not revive a
closed row; row 46 remains verification-only.

## 3. Reranked bottlenecks and attacks

### Proof bottlenecks, in order

1. **Whole-tree soundness:** the running Opus review is the abort gate for
   the universal quantifier, orbit partition, and `danger` recursion.
2. **Edge coefficient compatibility:** locally existing node polynomials do
   not imply one pair; parent coefficients and chart changes must induce all
   children simultaneously.
3. **Correct descent state:** the first live target has (s'=3); p.207's ten
   checks neither supply its radii nor license repeated descent.
4. **Uniform coefficient obstruction:** a (D=108) kill proves a method, not
   JC2; the target is an obstruction for every height-at-most-five full tree.
5. **The (u_s>1) branch:** one of eight stored (D=108) passport rows has
   (u_s=2); compile p.209 rather than applying Prop. 6.3.
6. **Screened cofinality:** useful for targets, below coefficients in proof
   value; a finite negative scan is not eventual emptiness.

The lost CDC listing, edgewise recentering, and a stronger semigroup scalar
rank below these.  They can simplify a target list; none presently attacks
the existence of one pair.

### Disproof bottlenecks, in order

1. **One exact (D=108) full-tree coefficient point;** local stars, formal
   prefixes, and descendants are not enough.
2. **One inverse-descent edge;** Prop. 6.3 is one-way, so a descendant
   `SURVIVES` verdict is only an obstruction failure.
3. **Characteristic-zero completion;** modular smoothness guides search but
   needs rational reconstruction and exact substitution.
4. **A repeating screened tree shape;** it is a cofinal laboratory, not a
   coefficient realization.

### Strongest proof attack

Attack the (D=108\to(18,12;\gamma^3)) pair by **ramification
absorption**, then lift the resulting obstruction into the tree coefficient
complex.  Monomial-Jacobian pairs without inherited data are plentiful: for
integers (d\ge r\ge1) and (a\ne0),

\[
 Q_0=\pi^d+a\gamma^r,\qquad
 P_0=\pi^{d+1}+{d+1\over d}a\gamma^r\pi
\]

satisfy, by direct differentiation,

\[
 J(P_0,Q_0)=-{r(d+1)\over d}a^2\gamma^{2r-1}.
\]

For (d=r=2), this is

\[
 Q_0=\pi^2+a\gamma^2,\qquad
 P_0=\pi^3+\tfrac32a\gamma^2\pi,\qquad
 J=-3a^2\gamma^3,
\]

which has exactly the primitive (3:2,k=3) signature of the selected
((18,12)) descendant.  Inflating (pi) to a monic approximate root (h)
of (pi)-degree six gives

\[
 J(P_0(\gamma,h),Q_0(\gamma,h))=-3a^2\gamma^3 h_\pi.
\]

The unwanted ramification factor (h_\pi) is unavoidable under pure
composition.  All possible existence is therefore concentrated in the
allowed lower approximate-root corrections.  Compute the top two (h)-adic
Jacobian blocks and prove that the inherited tree/radius supports cannot
cancel the nonconstant part of (h_\pi).  This is a precise initial-form
lemma with a proof-shaped generalization; it does not confuse the empty pure
composition slice with the full pair.

### Strongest counterexample attack

Run the same fork in the opposite direction: solve for the **smallest exact
correction support that does absorb (h_\pi)** for either of the two
((18,12;\gamma^3)) inherited trees.  If a component survives, require a
smooth point after discriminant/leader/(c) saturation, extend one tree page,
then solve the inverse Prop. 6.3 edge equations.  Only after the complete
((108,72)) polynomial pair passes (J\in k^*), all degree leaders, and the
full tree/global polynomiality conditions is it counterexample evidence.
The degree pair (108,72) cannot be the degree pair of a plane polynomial
automorphism, so an exact original Keller point would be decisive; the
descendant alone would not be.

## 4. Idea cards (three maximum)

### Card 1 — the exact sibling scheme: barycentric nodes, edge fibre product

**Mechanism.**  At a full-tree node (v), write (P=P_v=\deg p_v),
(Q=Q_v=\deg q_v), let (a_1,\ldots,a_Q) be the distinct roots of the
squarefree (q_v), and let (u_i=\operatorname{mult}_{a_i}p_v), including
(u_i=0) at unused (q)-roots.  Define (w_i=P-Qu_i).  Prop. A.3 is

\[
Pp_vq_v'-Qq_vp_v'=c_vp_v,\qquad c_v\ne0.
\]

At (a_i), the reviewed local calculation gives

\[
w_iq_v'(a_i)=c_v.                                      \tag{4.1}
\]

The standard barycentric identities
(sum_i a_i^j/q_v'(a_i)=0) for (0\le j\le Q-2) therefore turn A.3 into
the weighted Vandermonde equations

\[
\sum_{i=1}^{Q}w_i a_i^j=0\quad(0\le j\le Q-2),\qquad
\sum_iw_i a_i^{Q-1}=c_v                              \tag{4.2}
\]

when (q_v) is monic.  Conversely, after saturating by
(Delta(q_v)c_v) and imposing (w_i\ne0), (4.2) makes the vector
((w_i)) proportional to ((1/q_v'(a_i))).  The polynomial

\[
E=Pq_v'-Qq_vp_v'/p_v
\]

then has degree at most (Q-1) and takes the same value (c_v) at all
(Q) roots of (q_v), so (E=c_v) and A.3 follows.  Thus this is an exact
node scheme, not an equation count or a passport analogy.  The local source
identity is checked in `xmodel/whole-tree-review-grok46-20260903.md:456-492`.

The uniform answer to `OPEN[SIBLING-COEFFICIENTS]` is **not** the product of
these node schemes.  It is their fibre product over the edge transition
maps, with the global polynomiality equations appended.  The dictionary is:

| Whole-tree object | Coefficient datum |
|---|---|
| node (v) | one actual local factor polynomial pair (p_v,q_v), its root-position scheme (4.2), degrees (P_v,Q_v), radius and centre |
| factor/child edge (v\to w) | a declared zero/nonzero factor label, multiplicity, coordinate substitution (\pi_v=C_{vw}(t)+t^{\delta_w-\delta_v}\pi_w), and the induced initial/jet map |
| sibling packet | all factors in the orbit partition, including unused (q)-roots; not merely the selected (V)-flag |
| leaf | a truncated root series (\tau_i), not automatically a physical place until the whole edge system is realized |
| Galois orbit | the actual permutation action, stabilizer character, shared coefficients, and one integration constant per complete branch orbit |
| ancestor | the shared outer units in (D_i=\prod_{j\ne i}(\tau_i-\tau_j)); descendants do not receive independent copies |
| root of the tree | the invariant sums giving `DEG`, `POLY`, and `NO-RESIDUE`; distinct orbit contributions may cancel only here |

This dictionary is forced by the exact tree factorizations and local/global
separation in `xmodel/global-interpolation-sol56-20260902.md:240-392` and
its minimal decorated input at lines 500-514.  It explicitly separates a
cv flag, a factor root, a branch series, and any eventual physical place.

**Dependencies.**  The Opus whole-tree review must pass; the screen must
export every orbit partition, not one representative witness; edge chart
maps and coefficient sharing must be supplied by the sibling lane; rings,
generator order, discriminants, and every saturation factor must be explicit.

**Bounded quantity and cheapest discriminator.**  Start with exactly the two
stored (D=108) variants at
`box/mohprog-drivers-20260903/tree-independent-passport-D48-120.json:1214`
and `:1263`.  Solve each node scheme separately, then form only the first
nontrivial edge fibre.  There are at most 108 slots per node and at most
(4\cdot108=432) across a height-four target before orbit quotient.  Emit the
weighted equations over (mathbf Q), reduce by cyclic orbits, fix two affine
root coordinates, and use a discriminant Rabinowitsch variable.  Hard cap:
nine minutes and 4 GB.

**Outcomes.**

- Node scheme empty: a new exact local screen; replay on Moh's six before
  any promotion.
- All nodes nonempty but the first edge fibre empty: the first genuine
  sibling-coefficient obstruction and the right pattern for an all-tree
  theorem.
- Edge fibre nonempty and smooth: a high-value counterexample seed; attach
  one global remainder page, without calling it an actual pair.
- Only positive-dimensional node schemes: not failure.  Compute the edge
  elimination ideal rather than subtracting dimensions.

**Stop condition.**  Stop immediately if the exporter cannot state the
edge substitutions or supplies only a representative path.  Also stop after
nodewise nonemptiness if edge compatibility has not been typed; do not spend
on a product of unrelated local solutions.

**Expected information gain.**  Very high.  It decides whether the first
missing constraint is already local root position, is genuinely cross-node,
or lies only in global polynomiality, and it gives a reusable exact object
for every later tree.

### Card 2 — Kummer/Fourier blocks and ramification absorption for (T<=4)

**Mechanism and new cross-connection.**  For a monomial-Jacobian descendant
(J(P,Q)=c\gamma^k), put (r=k+1) and (X=\gamma^r/r).  Then

\[
dP\wedge dQ=c\,dX\wedge d\pi.
\]

This is a constant-Jacobian equation over the cyclic Kummer extension

\[
k[X,\pi]\subset k[\gamma,\pi],\qquad \gamma^r=rX,
\]

not an ordinary Keller pair over (k[X,\pi]).  Decompose the free module by
(mu_r)-character,

\[
P=\sum_{a=0}^{r-1}\gamma^aP_a(X,\pi),\qquad
Q=\sum_{b=0}^{r-1}\gamma^bQ_b(X,\pi).
\]

After multiplying by (rX) and reducing (gamma^{a+b}) with
(gamma^r=rX), every Jacobian coefficient belongs to a declared character
and Newton-weight block.  The invariant block supplies (c); all other
character blocks vanish.  The factor (r=k+1) in the p.207 radius rule is
exactly the valuation pullback under this Kummer map.  That connects descent
radii, row 1's monomial bracket, Galois characters, and Appendix-II support
in one algebraic mechanism; it is not another numerical coincidence.

Use the primitive pair in section 3 as a positive control.  For the live
((18,12;\gamma^3)) target, (r=4) and a degree-six approximate root
introduces the explicit (h_\pi) factor.  The character blocks identify the
minimal correction modes capable of cancelling it.

**Dependencies.**  First close `OPEN[DESCENT-STATE-S3]` for the two (D=108)
variants.  Preserve their full (V'_3) data.  Compile allowed monomials from
proved radii only.  Use the corrected Appendix-II saturation discipline.

**Bounded quantity and cheapest discriminator.**  The first run has two
targets and only the top two (h)-adic/Newton pages.  As a calibration, the
already typed G2 system has exactly 14 `h`-coefficients, 27
`beta`-coefficients, and `c`: 42 unknowns
(`xmodel/descent-radii-grok46-20260903.md:383-401`).  Do not launch its full
Gröbner basis.  Emit equations once, bucket them by (r) characters and
weight, row-reduce sparse linear pages over three good primes, and certify
only a decisive minor over (mathbf Q).  If the (D=108) two-page system
survives, saturate only the reduced correction ideal at (c), degree
leaders, and discriminants.

**Outcomes.**

- Every allowed correction mode is forced out: a type-specific theorem (T)
  and a plausible induction on Newton weight.
- A smallest nonzero mode survives: the best exact correction slice for the
  counterexample attack; solve that slice, not an arbitrary support.
- Character blocks immediately recouple densely: the grading is still a
  correct implementation check but not an accelerator.
- A full descendant solution: it refutes the type-specific (T) lemma only;
  it does not lift itself or produce a JC2 counterexample.

**Stop condition.**  Stop after two pages if no variable or equation reduction
is achieved, or at the desk resource cap.  A modular survivor is never
reported as `SURVIVES` over (mathbf Q).

**Expected information gain.**  Highest on the live frontier.  It tests a
specific obstruction and its exact opposite on the first non-dead degree,
while calibrating the uniform monomial-Jacobian receiver.

### Card 3 — turn the (j=2) forced-zero pattern into a divisor theorem

**Mechanism.**  For an (s=3) row put

\[
X=n-M_2,\quad K=d_2,\quad r=d_3,\quad v=V_3.
\]

Here (M_3=n-2), (r\mid K,X),
(P=vK/r), (Q=vX/r), and Def. 5.1(3) gives

\[
\delta_2=1-{X(2v-r)\over vX-r}
={X(r-v)-r\over vX-r}.
\]

Let (B=vX-r) and
(G=\gcd(B,X(r-v)-r)=\gcd(B,r(X-2))).  The reduced denominator is
(A_2=B/G), while Euclid gives

\[
rB-vr(X-2)=r(2v-r),\qquad G\mid r(2v-r).              \tag{4.3}
\]

If (X>K), then the major threshold (h=K/X<1).  At (s=3) the
(j=2) parent is still `danger`.  A positive canonical zero residue
(b_0=P\bmod A_2) is at least one, hence exceeds (h), forces a major
zero sibling, and reaches Prop. 5.6.  Survival therefore requires
(A_2\mid P), and consequently

\[
vX-r\mid vK(2v-r).                                    \tag{4.4}
\]

For fixed ((K,r,v)), (X=(d+r)/v) comes from a divisor
(d\mid vK(2v-r)) with (d\equiv-r\pmod v).  This is an exact finite
parameterization, not a ceiling and not sufficiency.

For (s>3), fix the entire higher DP state and set

\[
C=\prod_{j=3}^{s}(V_j(n-M_j)-d_j),\quad
E=(n-M_s-1)\prod_{j=4}^{s}(V_j(n-M_{j-1})-d_j),
\]

with (L=L_2).  Then (L\delta_2=U(X)/W(X)), where

\[
U=L[(EV_3-C)X-Ed_3],\qquad W=E(V_3X-d_3),
\]

and the content-correct statement is

\[
\gcd(U(X),W(X))\mid |LEd_3C|.                         \tag{4.5}
\]

On a still-dangerous (j=2) state with (X>K), the same reasoning makes
(W(X)) divide a fixed statewise carrier after primitive reduction.  A
nonzero ancestor can clear `danger`, so (4.5) must be a DP-state rule, never
a scalar row filter.

**Dependencies.**  The promoted `danger` semantics and full higher
(V)-tuple; exact reduced denominators; no edgewise recenter conjecture.

**Bounded quantity and cheapest discriminator.**  Verify (4.4) on every
(s=3) member among the 2,824 `C_FULL_TREE_ODE` assignments through
(D\le200), then emit the primitive carrier divided by (N) on every
still-dangerous (j=2) state through (D\le400).  This is integer
arithmetic in the running screen, expected seconds and under 1 GB.

**Outcomes.**

- Carrier/(N) is uniformly bounded and divisor choices stabilize: promote
  a precise all-degree lemma target coupling the pin to (4.5).
- It grows on a screened parametric family: stop the skeleton-ceiling route
  and bank that family as the coefficient/counterexample target list.
- The equivalence fails on a control: repair denominator/content or danger
  propagation; no mathematical inference.

**Stop condition.**  One growing exact family, or any need to discard the
full DP state, stops the ceiling claim.  No extrapolation from (D\le400).

**Expected information gain.**  High at negligible cost.  It either exposes
the arithmetic theorem behind the repeated (b_{\min}>h) kills or turns
screened cofinality into a concrete family problem.

## 5. Decisive experiment and software acceleration

**Launch `d108-1812-ramification-block`; it gets the first frontier seat.**

The lane has a fail-closed four-stage contract:

1. **Scope gate (ten minutes maximum).**  Re-derive the Prop. 6.3 state map
   for the two (s=4\to s'=3) rows, including which (V_i) survive and the
   three descended radii.  Compare the result with the tempting
   ((k+1)\)Def.5.1 extrapolation, whose predicted triples are
   ((-1,1/2,3/2)) for (V'_3=1) and
   ((-1,-2/3,2/3)) for (V'_3=4).  These numbers are **predictions**, not
   promoted radii.  If the map is not derived, stop; do not emit supports.
2. **Support and character compiler.**  Generate the permitted approximate-
   root corrections for ((18,12;\gamma^3)), decompose them modulo
   (r=4), and emit the top two (h)-adic Jacobian pages.  The primitive
   ((3,2;\gamma^3)) pair must pass, while pure degree-six (h)-inflation
   must print the factor (h_\pi).
3. **Sparse exact decision.**  Row-reduce the character/weight blocks over
   three good primes.  A claimed obstruction requires one rationally
   certified decisive minor or a symbolic identity.  A survivor advances to
   the reduced exact ideal; a rank count alone has no verdict.
4. **Controlled saturation only if needed.**  Declare the coefficient field,
   generator order, degree leaders, discriminants, and (c\ne0) component;
   run positive and negative Rabinowitsch controls.  Stop at nine minutes or
   4 GB and print `COUNTING-BOUND/TIMEOUT`, never `EMPTY` by wrapper.

This either gives the first live coefficient obstruction or isolates its
smallest defeating mode.  Joint character/Newton-weight emission replaces a
dense Gröbner call by sparse pages.  Later tree contraction of global moments
must match brute interpolation on two controls; it is no new rank theorem.

## 6. OPEN register

This is the complete OPEN ledger for this submission.  For an all-degree
statement, “bounded quantity” means the bounded structural parameter and the
finite first discriminator; it does not pretend that degree is bounded.

| OPEN | Bounded quantity | Cheapest safe test and stop |
|---|---|---|
| `OPEN[SCREENED-COFINALITY]` | Current proxy: all exact tree/ODE/orbit states through (D\le400); tree height is at most five. | Finish the running enumerator, canonicalize tree shapes, and ask for a repeated parametric shape.  A survivor family answers cofinality positively; absence through 400 is `MEASURED` only. |
| `OPEN[J2-DIVISOR-CEILING]` | All (s=3) rows among 2,824 ODE-screened assignments through (D\le200), then all still-dangerous (j=2) states through 400. | Verify (4.4), emit primitive carrier/(N), and stop on one exact growing family.  Seconds, below 1 GB. |
| `OPEN[SIBLING-COEFFICIENTS]` / node-fibre form | First bound: two explicit (D=108) trees and one edge; at most 108 slots per node, 432 across height four before orbit quotient. | Solve (4.2) nodewise, then one edge fibre with discriminant and leader saturation.  Stop if edge maps/full partitions are absent.  Nine minutes/4 GB. |
| `OPEN[DESCENT-STATE-S3]` | Exactly two (s=4\to3) rows, two (V'_3) values, and six candidate radii; the new scope beyond p.207 is one tree level. | Symbolically redo the Prop. 6.3 substitution and the generalized Lemma 5.1/Def. 5.1 recurrence; one numerical root-cluster check is only a control.  Stop if the (V)-map is inferred only by name matching. |
| `OPEN[T<=4]` | Unbounded degree, but inherited tower height (s'\le4); first client is the two height-3 ((18,12;\gamma^3)) rows. | Decide the two top correction pages.  A uniform character/weight lemma raises the theorem; a surviving exact correction support refutes that lemma, not all of (T<=4). |
| `OPEN[KUMMER-CHARACTER-RIGIDITY]` | (r=k+1) character blocks; calibration G2 has (r=5) and 42 variables, live (D=108) has (r=4) and two top pages. | Sparse block row reduction over three primes plus one rational certificate.  Stop after two pages if there is no reduction. |
| `OPEN[PRIMITIVE-LIFT-1812]` | Two (D=108) (V)-variants; one degree-six approximate root; initially two (h)-adic blocks. | Test whether legal corrections cancel (h_\pi); saturate only a reduced ideal.  `EMPTY` is a type lemma; nonempty gives a lift seed. |
| `OPEN[MINOR-DICHOTOMY]` | At the first frontier, one stored (D=108) row has (u_s=2): ((108,72,M=(81,106),V=(7,7))). | Compile both p.209 alternatives for this one row with declared chart and polynomial supports.  Stop rather than substitute the (u_s=1) transform. |
| `OPEN[DESCENT-LIFT]` | First bound: one surviving ((18,12;\gamma^3)) component and one parent transition back to either (D=108) tree. | Solve the inverse approximate-root/edge equations and verify by forward substitution.  A descended point without this edge is not counted as an original survivor. |
| `OPEN[ODE-PASSPORT-SUFFICIENCY]` | 631 normalized abstract profiles were exhaustively checked through reduced degree 8 with zero failures (counts (2,5,13,32,75,162,342)); next bound is degree 12 plus every actual node through (D\le120). | Use cycle-type DP/connection coefficients and transitivity.  A reachable failure sharpens the screen; no failures plus a constructive transportation-tree proof retires passports as a killer. |
| `OPEN[PASSPORT-SHARPNESS]` | The present external inequality removes three rows after ODE (58 to 55). | Derive the exact quotient cover and replay those three profiles with the full normalized cycle data.  Until then keep all passport counts explicitly `EXTERNAL`. |
| `OPEN[FULL-TREE-RECENTER]` | At (n\le100), at most 35 rows/25 groups/four classes beyond the passport screen; 55 becomes 20 if imposed. | Prove the chart translation at one nonselected integral-radius sibling, or exhibit why the globally selected-disc hypothesis fails there.  No census agreement promotes the quantifier. |
| `OPEN[MOH-PROGRAM-ARTIFACT]` | 49 excess rows/32 groups at (n\le100) after the strongest tree/passport screen. | Acquire the CDC 6500 listing/source or an independently preserved output.  There is no honest desk-CAS substitute; stop allocating proof seats to inference from the six printed rows. |

The p.207 `OPEN[DESCENT-RADII]` is **closed only at its reviewed ten-rational,
height-2 scope**.  The higher-height question is deliberately renamed
`OPEN[DESCENT-STATE-S3]`.  `M_2>m`, PS residue growth as a ceiling, the two
fixed-(N) rays, and raw moment cokernel are not OPENs here; they have
negative or superseded dispositions.

## 7. Current and queued lane decisions

| Lane | Decision | Exact handoff |
|---|---|---|
| `whole-tree-review-opus5` | **CONTINUE to seal, then STOP** | It is the load-bearing second gate.  A refutation aborts/reseals the round; confirmation ends the review task.  I inspected only its receipt, not its unfinished report. |
| `screened-census-grok46` | **CONTINUE** | Deliver exact `C_FULL_TREE_ODE` × nested-orbit states through (D\le400), canonical tree-shape fingerprints, all partitions per survivor, and the divisor-carrier columns.  Stop at the promised bound; do not infer cofinal emptiness. |
| `sibling-coefficients-sol56` | **CONTINUE, highest existing lane** | Require separate verdicts for nodewise A.3 existence and cross-node transition compatibility on the charged ((90,60)) and (D=108) rows.  Do not accept independent node polynomials as simultaneity.  I did not open its unfinished report. |
| `descent-radii-grok46` | **STOP as completed** | Its ten p.207 rationals and G2/G3 calibration are banked.  Any (s'=3) work is a new, explicitly scoped lane, not silent continuation of the closed OPEN. |
| `a2six-ray-moment-sol56` | **STOP as a proof lane** | It completed and confirmed the interface wall; the ray is dead under `C_FULL_TREE`.  Retain its custody refusals, matrices, and controls for regression only. |
| queued tree-decorated moment engine | **REDESIGN** | Build root-to-leaf tree contraction inside the node/edge coefficient scheme.  Launch only after a real (D=108) edge packet exists. |
| queued p.202 residue through sibling system | **CONTINUE, second batch** | Use the 49 excess rows/32 groups as fail-closed regression after one live (D=108) scheme is correct.  It is not the first seat. |
| queued box01 restart | **STOP/HOLD** | Restart only for a declared residual ideal that passes desk linear pages, includes controls, and demonstrably exceeds local memory/time.  The missing fleet key is not currently blocking mathematics. |

The receipt also shows a post-freeze `appendix2-compiler-grok46` process with
no final status.  I did not open a report.  **CONTINUE to its cap** as
calibration infrastructure; do not let it displace the live (D=108)
frontier.

**Single first lane:** `d108-1812-ramification-block` — recover the
(s'=3) state for the two ((24,78,106)) variants and decide their top two
(h)-adic/Kummer-character blocks.  If only one frontier seat exists, it
goes here.

## 8. Campaign-systems check

**UPGRADE — promotion scope-diff for quantifiers and index ranges.**  This is
a claim/review-propagation control, independent of the mathematical software.

Fresh evidence is delta 17(t).  The producer states Φ for descended
(s'=2) data and validates five such rows; the (V_i'=V_i) map in the
descent producer was `SOURCE-INFERRED` from the p.207 matches
(`xmodel/m2-descent-opus5-20260903.md:382-384`).  The promoted ledger then
says conditions (8)--(13) can be evaluated on “any descended pair”
(`AUDIT.md:16073-16075`).  The first (D=108) client is (s'=3), so that
scope expansion changes an active mathematical decision.

Add three fields to every promoted claim atom:

```text
producer_scope = {quantifiers, index/depth range, hypotheses}
reviewed_scope = {quantifiers, index/depth range, hypotheses}
promoted_scope = {quantifiers, index/depth range, hypotheses}
```

Promotion fails closed unless `promoted_scope` is a subset of both prior
scopes or the integration supplies a separately cited derivation for the
difference.  Matching formula text is not a scope check.

**Smallest useful test:** audit exactly three clauses of delta 17(t)—the
(V)-map, Φ's depth, and `DESCENT-CLOSURE`—against the two producer typed
blocks and the single (s=4\to s'=3) (D=108) row.  This is a manual/regex
fixture under ten minutes.  It must flag “(s'=2\to any)” while accepting the
ten-rational p.207 promotion.  If it does not, redesign the fields before
automation.

No AWS scheduling change is justified.  The proposed first experiment is
desk-scale, and box01 should remain stopped until a typed residual ideal earns
the cost.

## 9. Fallacy-v2 and claim typing

```text
REVIEWED INPUT
  local WHOLE-MAJOR-TREE obligation; C_FULL_TREE as a derived necessary
  filter; D=105 and D=117 screened empty; PS/trace identities and their
  negative ceiling reading; p.207 height-2 descent radii and Appendix-II
  corrected control.

PROVED-HERE
  the primitive monomial-Jacobian family in section 3;
  the discriminant-saturated weighted-Vandermonde equivalence for one A.3
  node in Card 1;
  the s=3 j=2 divisor implication (4.4), conditional exactly on danger=true
  and X>K;
  the Kummer change X=gamma^(k+1)/(k+1) and its character decomposition.

MEASURED / FINITE EXTRACTION
  the two stored D=108 full-tree witnesses and their first descent arithmetic;
  seven u_s=1 versus one u_s=2 among the eight stored passport survivors;
  the 631 normalized abstract passport checks through reduced degree 8.
  These are not cofinality or geometric-realisation claims.

OPEN / NOT CLAIMED
  whole-tree sufficiency; screened cofinal emptiness or nonemptiness;
  general Phi at s'>2; repeated descent for a monomial-Jacobian pair;
  theorem (T<=4); any D=108 coefficient point; any lift of a descendant;
  passport sharpness/sufficiency; edgewise recentering; recovery of the CDC
  program; any new lower bound on D_min beyond the promoted screened statement.
```

Guardrail checks:

- **Flag/place/series:** the node dictionary keeps the selected (V)-flag,
  a root of (p_v), a truncated branch series, and a physical place distinct.
  A stored witness is never called a full actual branch packet.
- **Carrier/attainment and floor/exactness:** screen survival is necessary
  arithmetic only.  A node solution, a descendant, a modular point, and a
  complete Keller pair are four different verdict levels.  No lower bound or
  capacity is promoted as attainment.
- **Pole/interior:** no pole identity is applied outside the proper-branch
  hypotheses of the banked global interpolation framework.
- **Saturation:** every proposed empty verdict names the ring component,
  discriminant, degree leaders, (c\ne0), and positive/negative controls.
- **Raw remainders/ring maps:** remainder equations are formed in the declared
  monic quotient.  The Kummer map is explicitly
  (k[X,\pi]\hookrightarrow k[\gamma,\pi]), (X\mapsto\gamma^{k+1}/(k+1));
  equal variable names are not used as an identification.
- **Merge/descent:** no use of Prop. 6.3 or a second descent is inferred from
  a change in (M).  The (u_s=1) hypothesis and the inherited state are
  checked at each licensed step; the (u_s>1) row is sent to the minor
  dichotomy.
- **Target indices:** original, descended, and search indices stay distinct.

No new exit-price assertion is made, so the special `charge_basis` declaration
is neither required nor supplied.

<!-- BODY-END -->
