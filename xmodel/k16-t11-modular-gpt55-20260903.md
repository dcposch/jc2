# K16 terminal modular falsification lane at t=7,8,11

**Lane.** `k16-t11-modular-gpt55-20260903`  
**Output box.** `box/k16t11-20260903/`  
**Charged inputs.** verified from `xmodel/k16-t11-modular-gpt55-20260903.run.v2` against `/tmp/jc2-lane.bJNqSD/inputs` by generated `sha256sum -c` manifest.  
**Verdict.** **OPEN / INCONCLUSIVE for (8.1) at t=7,8,11.** No modular nonunit and no refutation point was found, but the requested full ideal or complete two-chart unit test did not finish for any of the new `t` values in the bounded lane.

## Input and recurrence discipline

The receipt-derived manifest is `box/k16t11-20260903/input_manifest.sha256`; `sha256sum -c` returned `OK` for all 17 frozen charged inputs.  The first attempted manifest write landed inside the read-only lane input directory and failed before content verification; the successful manifest was generated mechanically from the receipt into the output box.

The lane-local drivers are copied from authenticated charged artifacts and then narrowly patched for this task:

- `terminal_array_recurrence.py`: copied from charged artifact summary hash `8a935838ac01731110efd5f36652cae3d66bd00ffa23ae4450cdadaa41438001`.
- `singular_terminal_driver.py`: copied from charged artifact summary hash `0cc821ff59adc955d2c6c33a572766b01569d578f1034f343c4f3661cebcf8ed`, then patched to add direct `full`, `b4zero`, `b4one`, `nfmodstd`, variable-order, and `noredSB` bounded modes.
- `top_tail_fast_recurrence.py`: copied from the frozen charged input hash `b6b1db68b89685d7d98828299d2128c47381b361e94c5b2c7ba049d8907429a3`, then patched only for algorithm/variable-order retries and `noredSB`.

The implemented recurrence is the proved coefficient-array recurrence, not a `sigma_t` expansion.  The source formulas are the charged `U_m,C_m` arrays (2.2), the `B,S` recurrences (2.3)--(2.4), the remainder arrays (2.5)--(2.7), the high-weight solve (2.8)--(2.12), and the final `X`-coefficient transform (2.13), cited at `/tmp/jc2-lane.bJNqSD/inputs/k16-terminal-proof-sol56-20260903.md:68` through `:226`.  The good-prime/pivot exclusions use the denominator list and resultants at the same file, lines `265` through `360`.

## Controls

`python3 box/k16t11-20260903/terminal_array_recurrence.py --t 2 3 4 5 6 --prime 1009 --points 3 --records box/k16spine-20260903`

Output: `box/k16t11-20260903/array_recurrence_t2_t6.out`.

Result: all ten `(t,fibre)` cases for `t=2..6` reported `PASS_RECORD`, followed by `{"checks": 10, "status": "ALL_PASS"}`.  This is the requested control that the recurrence regenerates the charged terminal records at `t=2..6`.

Additional formula-only controls for the requested larger values:

- `array_recurrence_t7_t8_t11_p1009.out`: `t=7,8,11`, both fibres, five deterministic points per fibre, `ALL_PASS`.
- `array_recurrence_t7_p47.out`: `t=7`, both fibres, `ALL_PASS`.
- `array_recurrence_t8_p59.out`: `t=8`, both fibres, `ALL_PASS`.
- `array_recurrence_t11_p53.out`: `t=11`, both fibres, `ALL_PASS`.

These formula-only controls check pivots, high-band killing, and terminal evaluations at points; they are not ideal certificates.

## Good primes and fibres

`good_prime_audit.json` mechanically records roots of `H_t mod p` and nonzero closed pivots (2.10)--(2.12).  Every listed prime below has two distinct finite-field fibres and no zero recurrence pivot in the tested fibre list.

- `t=7`: primes `47,1009,1031,1033`.
- `t=8`: primes `59,1009,1031,1033`.
- `t=11`: primes `53,1009,1031,1033`.

For the split target at `t=11`, `p=1009` gives roots `y=439,746`; these correspond to `d=+2,-2` respectively by `d=2qy-(t+1)` with `q=23`.  Exact split fibres are `y=7/23` for `d=+2` and `y=5/23` for `d=-2`, matching the charged split statement.

## Modular ideal measurements

The relevant two-chart assembly is the charged standard cover: if `(I_t,b4)=[1]` and the dehomogenized `b4=1` chart is `[1]`, then `I_t=[1]`; see `/tmp/jc2-lane.bJNqSD/inputs/k16-terminal-proof-sol56-20260903.md:657` through `:664`, and the explicit test split in `/tmp/jc2-lane.bJNqSD/inputs/k16-middle-spine-sol56-20260903.md:687` through `:710`.

### t=7

Completed modular `b4=0` chart measurements:

```text
p=1009 branch 0: B4ZERO_FULL_UNIT, basis [1], elapsed 4.54s
p=1009 branch 1: B4ZERO_FULL_UNIT, basis [1], elapsed 5.02s
p=1031 branch 0: B4ZERO_FULL_UNIT, basis [1], elapsed 3.52s
p=1031 branch 1: B4ZERO_FULL_UNIT, basis [1], elapsed 4.10s
p=1033 branch 0: B4ZERO_FULL_UNIT, basis [1], elapsed 4.42s
p=1033 branch 1: B4ZERO_FULL_UNIT, basis [1], elapsed 4.89s
```

The direct full ideal at `p=1009, branch=0` timed out at 300s.  The `b4=1` side also timed out at the same fibre under these variants: generic top chart, full `b4=1` slice using all rows, authenticated fast top-tail, fast top-tail with reverse variable order, fast top-tail with `slimgb`, fast top-tail with `groebner`, and fast top-tail at small good prime `p=47`.

**Verdict for t=7.** `OPEN[MODULAR-T7]`.  The `b4=0` chart is measured `[1]` on three good primes and both fibres, but the `b4=1` chart and direct full ideal were not measured as `[1]`.  No `NOT [1]` point was produced.

### t=8

Completed modular `b4=0` chart measurements:

```text
p=1009 branch 0: B4ZERO_FULL_UNIT, basis [1], elapsed 250.25s
p=1009 branch 1: B4ZERO_FULL_UNIT, basis [1], elapsed 186.10s
```

Timed out modular `b4=0` chart measurements:

```text
p=1031 branch 0: INCONCLUSIVE_TIMEOUT at 300s
p=1031 branch 1: INCONCLUSIVE_TIMEOUT at 300s
p=1033 branch 0: INCONCLUSIVE_TIMEOUT at 300s
p=1033 branch 1: INCONCLUSIVE_TIMEOUT at 300s
p=59 branch 0:  INCONCLUSIVE_TIMEOUT at 300s
```

No `b4=1` or direct full `t=8` unit basis finished in this lane; after `t=7` top/full timeouts and the `t=8` zero-chart cost, those jobs were not promoted to exact or claimed.

**Verdict for t=8.** `OPEN[MODULAR-T8]`.  One good prime closes the `b4=0` chart on both fibres; the requested three-prime/full or complete two-chart test did not finish.  No `NOT [1]` point was produced.

### t=11

The full-`b4` recurrence-only constructions at `p=1009` timed out before emitting all expanded row degree/term counts:

```text
p=1009 branch 0 (d=+2): INCONCLUSIVE_TIMEOUT at 300s
p=1009 branch 1 (d=-2): INCONCLUSIVE_TIMEOUT at 300s
```

The specialized `b4=0` chart at the same split fibres also hit the 300s bound:

```text
p=1009 branch 0 (d=+2): INCONCLUSIVE_TIMEOUT at 300s
p=1009 branch 1 (d=-2): INCONCLUSIVE_TIMEOUT at 300s
```

The later `p=1031,p=1033` `t=11` zero-chart sources were emitted but the queue was interrupted after earlier timeouts to preserve the lane bound.  The finite-field array recurrence checks still passed at `p=1009` and `p=53`, both fibres, five points per fibre.

**Verdict for t=11.** `OPEN[MODULAR-T11-SPLIT]`.  The recurrence/pivot fibre controls passed, including the split fibres `d=+2,-2`, but no modular standard basis `[1]` for the full ideal or for a complete two-chart cover was measured.  No `NOT [1]` point was produced.

## Exact status

No new characteristic-zero `nfmodStd` promotion is made.  Singular 4.3.2 has `nfmodStd` in `nfmodstd.lib`, and the lane-local driver supports it, but exact runs were not launched after the modular full/top tests failed to close inside the bounded pass.  Any exact `[1]` statement for `t=7,8,11` remains open here.

## FALLACY-v2 audit

- No split zero divisor is inverted: split `t=11` is treated fibrewise, with `d=+2` and `d=-2`.
- Modular point/formula checks are not promoted to ideal certificates.
- A `b4=0` chart unit is not promoted to the full ideal without a completed `b4=1` chart.
- Timeouts are labelled `INCONCLUSIVE_TIMEOUT`, not `NONUNIT`.
- No exit-price assertion is introduced, so no `charge_basis` line is applicable.

## Artifacts

Primary machine summaries:

- `box/k16t11-20260903/status_summary.json`, sha256 `c7c5312cd2aaf956b0ac632699347264ccb354bf0fa699f7106ee96a29bfeb6b`.
- `box/k16t11-20260903/good_prime_audit.json`, sha256 `74eae7946c07b8429a9ba9eaf6acb61c7755718a961485c206555eb62a6bdb5d`.
- `box/k16t11-20260903/artifacts.sha256`, replayed by `artifacts_check.out`.

No ledger files, `jc2-lean`, or `ideation-*` files were edited.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `8185`.
- Body SHA-256:
  `f4ab5423f43fb89d53f2ebba5445b8bb2477f1e423551e1d54d6f933ad862e1d`.
- Frozen basis: `4b1322fefe85698f8dd710a9108a66caebbef14a`.
