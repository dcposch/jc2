# Administrative placement source repair — Sol

- First action: `2026-09-12T07:01:31.010120164Z`.
- Basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
- State: disabled, unbound, unexecuted, pending different-model FIRST.

## Status

**IMPLEMENTED, DISABLED/UNBOUND, UNREVIEWED.** Two minimal administrative SOURCE derivatives and their exact diffs are published. They are not runtime-qualified or launch authority and require a different-model FIRST after concrete binding.

## Deliverables and source boundary

- `setup-pre-admission.template.sh` preserves the retained science/runtime/native/holder installation and absence guards, while parameterizing obsolete physical paths. It adds only the external administrative placement, two exact five-row manifests, and one-FIFO census/identity observation.
- `preholder-security.template.sh` preserves the recursive science-base `0755` directory / `0444` regular-file / no-symlink predicates and the retained ancestry, tmpfs, namespace, cgroup-absence, package, channel, and FIFO guards. It adds exact qualification of the external tree and the frozen FIFO identity.
- `BINDING-AND-NEGATIVE-CONTROLS.md` defines the binding chain and failure cases. The two `.diff` files are byte-exact `diff -u` outputs against the charged retained sources; their hashes equal freshly generated diff-stream hashes.

No helper, FLOW, holder, dispatcher, caller, binder, stage, installer, science source, cap, phase, or authority serializer was edited. No authority candidate is preinstalled.

## Exact administrative placement

The setup derivative accepts only a direct child of `/opt` for the admin tree, requires it absent and disjoint from the science base in both ancestry directions, qualifies `/` and `/opt`, creates the tree root:root `0755`, and explicitly installs exactly:

`outer-admission.sh`, `observe-and-leaves.sh`, `receive.sh`, `release-check.sh`, and `fifo-write.sh` as root:root `0555` regular non-symlink files with no extra entry or subtree.

It does not treat the five charged unbound-template hashes as installed hashes. ROOT must bind each source's frozen early placeholders, hash the resulting bytes, and use those same five hashes in the exact stage manifest, exact absolute-path installed manifest, corrected eight-role remote manifest, frozen configuration, and security derivative. The setup checks both five-row texts, their manifest hashes, all stage bytes, all installed bytes, paths, census, ownership, and modes. Holder and installed/native data roles remain `0444` inside the unchanged base; source exclusion and the existing stage/native checks are unchanged.

## FIFO and authority boundary

Setup creates one root:root `0600` non-symlink FIFO below the root:root `0700` channel, rejects any extra child/subtree, and emits its device/inode. Before holder start, ROOT must freeze those positive observed values into the security derivative. Security rechecks exact path/type/mode/census/device/inode.

This does not silently claim later mechanical identity coverage. The unchanged observer, release check, and FIFO writer check path/type/mode, not the frozen FIFO device/inode. Therefore the accepted exclusive ROOT/no-concurrent-writer/no-replacement custody must cover security completion through separately authorized release. Adding repeated identity comparisons would require separately selected helper SOURCE edits and remains an explicit GAP.

## Manual controls and disposition

Manual static inspection confirms both files begin fail-closed: any unresolved `JC2_*_PLACEHOLDER` or wrong distinct authorization phrase exits before mutation. Setup performs no systemd/holder/worker/release action; security is read-only qualification. Negative controls cover unsafe or overlapping ancestry, existing/symlink targets, extra/missing/nonregular files, wrong modes/owners/hashes/manifest rows, stale template hashes, existing registration/cgroup, and FIFO census/type/identity drift. No live value, worker, batch, clock, enabling flag, or release token was selected.

All 17 inputs were hash-pinned before their charged reads and unchanged at post-pin. The five immutable program templates and ROOT intake received fresh whole reads; the prior ten exact-current whole-read scopes and completed placement report were reused. Both new sources and the contract received whole readback. No source was run; no shell/helper syntax check, import, AST, compilation, test, mock, fixture, network, SSH, AWS, science, or protected-tree action occurred.

Disposition: **SOURCE REPAIR COMPLETE AS INERT BYTES; NOT READY until exact physical early binding, manifest construction, different-model FIRST, and ROOT intake.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `4699`.
- Body SHA-256:
  `a3fd0678690812bbe667c86089d497136a8da8eeec4a71f1dccb58b05c37aa70`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
