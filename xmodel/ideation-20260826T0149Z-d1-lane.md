# Blind 46-avenue scan — D1 lane contribution

Date: `2026-08-26T01:49:22Z`

Blind 46-avenue scan (news-integrated), ranked new connections/experiments:

1) **Order4 Kummer-orbifold kill (very high).** Package the provisional divisor `div(v)=8Z-P0-PA-6PB` on `X^nu=P1` into the degree-4 root cover `Y:y^4=v`; RH gives g(Y)=1. Prove source-component dominance/nonconstancy and that the actual a6 supplies the lift `P1_x→Y`; then no nonconstant map P1→genus1, excluding the entire mu4!=0 leaf for every U. Smallest exact gate: certify all boundary valuations + no omitted a6=0/source component + constant-map alternatives. DZ/passport data can independently check the four branch-cycle residues mod4, but terminal trees must not replace lower-tail/source landing.

2) **Certificate-guided D1 Gröbner-cone continuation (high).** From the now dual-confirmed la^20 initial-ideal element, extract an exact lifted syzygy/preimage in the Rees ideal. Its term-weight inequalities give a rigorously certified Gröbner cone around `(4,1,1;22;30)`; then turn moving axis/q2/load coefficients on symbolically. The la20 coefficient becomes a parameter polynomial: kill the principal open where nonzero and recurse only on its exceptional variety. This connects tropical row38 to Rees/integrality row31 and can cover fan cells without brute global saturation.

3) **DZ-tree electrical/leaf-removal lower-tail invariant (high-medium).** For count-certified terminal weighted trees, compute the first unresolved lower-tail scalar for all small U and regress it against cut/path sums (weighted Wiener/electrical energy). Moment identities already express terminal coefficients as signed power sums; a tree cut formula with fixed sign/nonvanishing could induct under leaf removal and close unbounded U. Experiment: Lu–Song census→exact Belyi realization→first lower-tail value→candidate invariant, with terminal/lower-tail firewall.

4) **Coupled monodromy block obstruction (medium-high).** A nontrivial coefficient quotient (order4 rho/v, or D1 toric parameter) forces a block system on the terminal DZ permutation triple. Instead of testing 169 unconstrained passports, test whether prescribed weighted-tree monodromy admits the required imprimitivity/quotient ramification. This may eliminate whole terminal classes before tail CAS; exact permutation CSP is cheap and proof-discriminating.

5) **TD6 source-gauge chain homotopy (medium).** Treat V78C’s q2–q14 active / q16–q24 P12-zero split as a Spencer/Koszul question: explicitly solve whether each dead-stretch column is source reparametrization plus terms beyond a proven determinacy bound. If a uniform chain homotopy exists, remove 11 slots before the nonlinear Fitting gate; if not, the first failed homotopy row names the next obstruction. D1’s prevariety-vs-full-ideal lesson warns not to infer this from P12 zeros alone.

6) **Proof-carrying CAS as speed multiplier (medium software value).** Require every unit/monomial endpoint to emit a `lift`/syzygy certificate and a second tiny verifier (or AWS Lean `MvPolynomial` identity). This would have caught factored E8 before multi-hour races and turns reviews into linear certificate checking. Apply first to D1 la20, then order4 divisor identities and TD6 minors.

I found no stronger new action in the remaining low-ranked routes: AS/Witt still lacks a fixed-support/cofinal-degree bridge; C1 Lean improves trust but not discovery; symplectic/HC4/naive scaling remain stopped.

