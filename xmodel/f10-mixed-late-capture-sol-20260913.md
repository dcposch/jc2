# f10 mixed late capture and science-unit source packet

Status: **INTERNAL / UNREVIEWED / UNEXECUTED / UNBOUND**. ROOT different-model FIRST is mandatory before any use.

First action: `2026-09-13T00:20:20.394704716Z`. All nine input pins matched before body use; seven newly relevant bodies were read WHOLE and the unchanged policy/finalizer WHOLE reads were reused after exact repinning.

## Delivered source

- `dispatch.py` SHA-256 `079f9ad7b92e64af5c5fc6a5e17de14ffa5245627ee5c8a4ce0ef2a018a2fa5d` is the accepted dispatcher plus one ten-line insertion. The insertion occurs only after registration, host/clock, complete source pins, canonical layout, exact empty sibling census, qualification/native manifest, and library checks. It flushes Python stdout/stderr, creates both runner files with `O_CREAT|O_EXCL|O_NOFOLLOW`, forces 0600, `dup2`s them onto fds 1/2, and closes temporary descriptors.
- `science-unit.template.sh` SHA-256 `3bee708f82e06e77426ddd7bca9953ef84858b058d88f78c5bf25672a6e579eb` is an inert literal outer sequence with only placeholders for fresh facts and authority.
- `DELTA.md` SHA-256 `d3c40887b9a216909ec0e8e2f93e424e9d4d0b0978d3872dbb9be5583b1b3bb3` records the exact boundary and limitations.

## Load-bearing ordering

The dispatcher still demands both mount siblings empty at line 92. The new capture is after that predicate and all remaining initial data/library validation, yet before construction of phase authority, the dummy subprocess, or any one of the five following scientific/control runs. Thus no scientific child can precede capture. Early rejection remains on the unit journal. Once both `dup2` calls succeed, dispatcher diagnostics and CAPRUN supervisor status inherit the two root-owned files inside the same bounded tmpfs; CAPRUN continues to open each child stdout/stderr/telemetry separately under the existing per-phase names.

An existing runner filename causes `O_EXCL` refusal; no freshness check is relaxed and no existing byte is truncated. Root DAC override is required to traverse/write the child-owned 0700 output directory, but the scientific setpriv child remains subject to the prior positive-UID, NNP and zero-effective-capability predicates.

## Literal unit boundary

The unit template creates only one 256 MiB tmpfs and its initially empty admin/output siblings. It retains journal startup, stores timer evidence in shell memory until after admission so admin remains empty, uses no HOME assignment, and binds TMP/cache paths to output. It arms absolute TERM at the frozen deadline minus five seconds and KILL at the original deadline before invoking the exact `python -E -s -S -B dispatch --registration ... --sha256 ...` root CLI via `systemd-run --wait`.

The service preserves CPUQuota 80%/100 ms, MemoryMax and AS 32 GiB, swap 0, TasksMax 64, LimitCPU 3300, FSIZE 256 MiB, RuntimeMax 3535 seconds plus TimeoutStop 5 seconds, KillMode control-group, no delegation, empty ambient capabilities, and only DAC/KILL/SETUID/SETGID in the root bounding set. A late launch gains no new absolute time: the prelaunch and terminal comparisons use frozen admission/deadline strings, while the independent absolute timers dominate the relative service limits.

After `--wait`, the sequence records its numeric return, bounded unit/journal diagnostics, demands empty/no-child then absent service cgroup, cancels timers, rechecks the original deadline, produces exact census/hash files, and requires exit zero. ROOT must still authenticate live unit/cgroup values (including burst zero), timer relations, installed bytes and all placeholders; fsync every file and parent; WHOLE-read/hash and durably transfer evidence; and retire the exact unit/worker. A partial capture, early refusal, late terminal proof, output beyond bounds, or any predicate failure is STOP and cannot be retried.

No caps, six-phase ordering, controls, mathematics, authority schema, worker, or execution were changed or selected.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `3984`.
- Body SHA-256:
  `3b2aa82b128ad2e4b92616afa9e3ac0d98e9aba7070054b386754533885cca76`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
