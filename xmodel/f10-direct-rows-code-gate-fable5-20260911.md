# FIRST Fable5.1: f10 direct-row source pair (producer + checker + authority) code gate

lane=f10-direct-rows-code-gate-fable5-20260911
author=Fable 5.1 (claude-fable-5-1)
skeleton_written_utc=2026-09-11T03:39Z
target_absence_checked_utc=2026-09-11T03:38:38Z (both owned targets absent before first write)
pins_verified_utc=2026-09-11T03:39:20Z (16/16 OK, see input_custody.md)
reserve_utc=2026-09-11T04:02Z hard_utc=2026-09-11T04:05Z
status=COMPLETE (verdicts A-E CONFIRMED, F BLOCKED by one correction; adapter seals)

## 0. Scope and precedence

Scope is ONE combined static FIRST of the five newly authored files (authority.py, arithmetic.py, produce.py by the producer owner; check_arithmetic.py, check.py by the checker owner) plus the new four-value metadata API. Precedence applied: ROOT-ADOPTED-WIRE-PINS > ROOT-ADOPTED-CORRECTIONS > rows proposal > original OPERATIONS/ASSERTIONS/WIRE; the earlier rows-wire FIRST is used only for accepted scope, and its two universal overclaims (all low arrays zero implies all 25 rows zero; every completion change survives every projected residue) are rejected. Accepted 23z/19z, the corrected circuit 24z and the rows projection 25z are imports and are not re-reviewed. Canonical tripwires are conservative STOPs, not new characteristic-zero identities. Nothing here is a source point, source exclusion, canonical rank, unsaturated-ideal equality, all-r result or JC2 conclusion. This FIRST cannot license execution: the separate ROOT runtime (true authorization, physical identity, caps, quiet, durable custody) is under independent authoring and is out of scope.

Method: hash and static text tools only (sha256sum, cat, sed, awk, wc, ls, date, apply_patch); manual mathematics; zero scientific subprocess/import/AST/compile/test/CAS/dummy; no code written or modified; no fixture, prime, payload or baseline generated. Every input was pinned before any body was read and re-pinned after (input_custody.md). check.py exceeded the display limit on the first read and was recovered in three ranges 1-180 / 181-360 / 361-529 through EOF; CONTRACT.md did not display on the first attempt and was read whole on the second. No clipped aggregate is claimed WHOLE.

Verdict summary: A CONFIRMED; B CONFIRMED; C CONFIRMED; D CONFIRMED; E CONFIRMED; F BLOCKED by exactly one readiness correction (arithmetic.py power() over-expansion, lines 236-238), otherwise READY for ROOT registration only after the separate runtime FIRST passes. No GAP verdict; no new canonical OPEN ID.

## A. Metadata/interlock composition

**CONFIRMED.** authority.py lines 1-133 contain no polynomial or finite-field code: only os/sys at module top, hashlib/json imported inside authorize (line 40-41), all stdlib. Order of checks supplied by source: optimize refusal FIRST (line 12) before any other work; mode in {produce, check} (14); Linux and EC2 DMI vendor (16-24); exactly nine argv with fixed positions and the mode-specific --output/--input at argv[7] (25-29); actual installed entry path equals dirname(realpath(authority.py))/produce.py or check.py compared with sys.argv[0] (30-33), so passing mode alone cannot turn the checker entry into a producer; exact five-file inventory of the installed directory (34-35); sibling lookup removed from sys.path during authorization (36) and restored only at the end (131), so no scientific sibling can be imported during auth; giant-decimal guard (37-39). Metadata is read as at most 65537 bytes, size-capped at 64 KiB and SHA-pinned BEFORE json.loads (70-76), with duplicate-key refusal and JSON number/float/constant refusal (59-68). Exactly the eleven keys are required by set equality (79), so no extra field can pass; job_tag/status/authority literal (81); artifact has exactly path and sha256 and path equals abspath(argv[8]) (83-87); produce requires sha256 UNFORMED and lexists false (88-90), so the target is unformed and never overwritten; check requires size at most 128 MiB and a byte digest equal to the registered pin before check.py parses (91-93); limits equal the fixed LIMITS mapping and files has exactly the five basenames (94-95); argv[8] absolute and realpath-canonical (96-97); every installed file digest equals files[basename] (98-100), including authority.py itself; contract_sha256 pin format (101); source_pins is a nonempty string-keyed mapping whose values are all lowercase 64-hex, and for each of the five files the key dirname(realpath)/basename must be present with value equal to files[basename] (102-109), which is exactly the ROOT-approved 03:15 clarification; the remaining vector is not invented or filled (only format-checked), and check.py line 49 later requires the whole returned vector to equal the artifact's source_pins exactly. Place is shape-checked only (110-116): four keys, p/degree strings, phi/P7_mod_p lists of strings; no primality, factor, reduction or leading-coefficient arithmetic. runtime keys, interpreter digest and pinned native subset (117-127). Return is exactly the four keys with copied dicts (132-133). Both callers are faithful: produce.py imports only authorize at module top (line 2), calls authorize('produce') first (356) and imports arithmetic only inside build after authorization (6); check.py imports only authorize at top (2), refuses optimize itself (509), calls authorize('check') (511) and imports check_arithmetic only afterwards (512, 8). Neither caller parses a candidate body before authorization. Self-hashed JSON is not treated as provenance: the code comment (128-130) and both contracts say so, and the digest of the metadata is compared to argv[6], which is itself a launcher-supplied value; genuine registration is the ROOT launcher's task.

Launcher boundaries (trusted, not supplied by source) that a correct install must satisfy or authorize refuses: (1) the five-file inventory check (line 34) refuses if a `__pycache__` directory exists; CPython writes authority's bytecode at import time, before authorize runs, unless the launcher uses -B / PYTHONDONTWRITEBYTECODE or a read-only directory. CONTRACT.md line 7 and 18 delegate exactly this (no-bytecode, read-only layout). This is the only refusal that is unavoidable for a correctly installed registered directory under a naive launch; it is a launcher obligation, not a source defect, and the smallest optional in-source hardening would be setting sys.dont_write_bytecode before line 2 of both entries (not required). (2) sys.argv[0] must be the canonical realpath of the entry (no symlinked directory) and argv[8] must be canonical. (3) The pinned interpreter must be 3.11+ for set_int_max_str_digits. (4) DMI vendor literally Amazon EC2. Two optional semantics notes: in check.py the terminal reauthentication on the success path raises Mismatch on drift, which prints `CHECK FAILED: terminal ROOT metadata drift` (exit 2) instead of a STOP label; it is a distinct string and cannot be confused with the T predicate. A metadata drift detected inside the Mismatch handler (line 520) propagates as an uncaught ValueError (exit 1, traceback), which is a refusal with no semantic credit. Neither is blocking. The terminal re-digest of the input (authority line 92 on the second call) closes the read-then-parse window for the success path.

## B. Independent finite-field engine

**CONFIRMED.** Literal P7: produce.py 11-24 and check.py 56-68 each build dL, K, gamma from tau=3/10 as ascending lists, S7 = 2K^2 - 140 tau (1+tau-6V) K dL + 245 gamma dL^2 with (1+tau-6V) written ascending as [1+tau, -6], check 8 coefficients (degrees 6, 6, 7 give length 8) and leading coefficient exactly -245*120*144*tau, then divide by that leading value. Both compare place P7_mod_p CONSTANT-FIRST with leading 1 LAST to the reduced literal monic (arithmetic.py 112-115; check.py 108-110, 71) and require phi | P7 (116-117; 110). Primes: producer trial division from 2 through floor(sqrt(p)) (105-109); checker 2 then odd d through sqrt (101-105); both bound 2 <= p < 2^31 and degree 1..7 through canonical decimal strings (producer integer(): ASCII, decimal, no leading zero, at most 10 digits; checker regex `0|[1-9][0-9]{0,9}`). Irreducibility: producer checks gcd(phi, V^(p^i) - V) = 1 for all i <= f/2 by its own Euclid on trimmed lists plus V^(p^f) = V (118-128); checker checks V^(p^f) = V and gcd(phi, V^(p^(f/l)) - V) = 1 for each prime l | f in {2,3,5,7} (112-116), i.e. Ben-Or versus Rabin, both exact and equivalent for f <= 7; the f=1 edge reduces V to a constant in both and passes trivially. Field: producer field() reduces by its own rem() (54-70) and pads dense to DEGREE; checker F() (120-126) does direct monic reduction from the top coefficient down; both fm/fmul reduce products; inverses are producer fpow(a, p^f - 2) with two-sided product check (132-141) versus checker extended Euclid over F_p[V] with unit check and two-sided product (134-143). Series: producer finite binomial series with per-step truncation at theta^n (270-277), denominators j = 1..7 and 10; checker coefficient recurrence k f_k = sum_{i<=3} (alpha i - (k - i)) c_i f_{k-i} (257-268), denominators 10k. Integration 1/(e+1), e+1 in 1..8 (280-284; 271-278). Exactness: every rational constant is a Fraction (Q(17,10), Q(-2), Q(1,2), Q(1,21), Q(24,11), Q(6,11), 1/ratio, -1/pivot, 1/(j-3i)) and every field operation is integer modular; I found no float, no `/` on ints, and no `**` with float anywhere in the four scientific files. Denominator registry: 25 D0 + 42 D1 + 58 D2 = 125 labels in the fixed order, built literally by both (produce.py 26-36 with an order-and-completeness check at 337-338; check.py 99-112 with count and distinctness), D1 never zero over Z for the stated ranges, D2 zero exactly at the two excluded resonances; repeated values keep distinct labels and duplicate labels are refused (arithmetic 149; check 112). Fifteen named inverse nodes dL, W, t5, H7_5, H7_6, det1..6, critical_c, pivot8..10 with exact set checks (produce 334-336; check 487-493); every field inversion goes through fi/unit with a label and both products; ground-field inverses of registered denominators are the only other inversions. Typed support: six kinds with signed axes exactly laurent_S:S, inverse:q, scale:s and S; band S-free, laurent_S theta-free, graph X-only; exponent cap 256 and 100000-term cap in both (arithmetic 167-179; check_arithmetic 159-170). Zero is the empty dict in both; coefficient/derivative/shift/truncate/scalar/substitute exist in both with the checker's scalar() and graph_substitute() rejecting parameter support and non-band input; the checker's Horner substitution (281-286) differs from the producer's per-term powers (327-333). Producer Poly equality is kind-blind (dict equality), checker equality includes kind; this is a strictness difference, not an error. One engine defect, classified as the F blocker: arithmetic.py power() (233-239) squares `a` AFTER consuming the last bit, so power(x, 17) also forms x^32 and power(x, 9..15) forms x^16. Harmless on small bases, but see F for the inverse-chart call site.

## C. Full source reconstruction

**CONFIRMED** against the literal OPERATIONS recipe, each file independently. Leading stage: c = 1 + theta + V theta^2 + W theta^3 with W = -K/(210 dL), t = trunc<=7 c^(17/10), t5 = [theta^5]t, C = c/W, D = trunc<=5(t)/t5, H7_h = [theta^7] trunc<=7 c^((27-h)/10) (produce 44-55; check 122-128); leading ODE 10CD' - 17C'D = -theta^7 and contacts 6,7 (56-59; 129-130); a=C0, b=D0, F=C2, H=C1. Ten formal bands: full forcing W_h = sum_{i=1}^{h-1} ((10-i) A_i B'_{h-i} - (17-h+i) A'_i B_{h-i}) from the recurrence sequences Ab, Bb only (produce 86-90 as polynomial products; check 206-212 as fixed coefficient pairs ak 0..2, bl 0..4), computed before installation and never fed from Euler in either file. Gaps 1..6: part = U(h, 0, -W_h, 4); basis A_j = theta^j, target -2 theta^6 only at (4,2); columns (b1, b0, 10a[theta^0]B_j - 17b[theta^0]A_j); matrix = transposed columns; determinant by six signed permutations plus adjugate with BOTH products checked (arithmetic 296-320; check 170-197); xyz = M^-1(-base1, -base0, rho) with rho = X_h at h<=4 and 0 at 5,6; at 5,6 the inverse-entry identity inv[2][2] = ratio_h H7_h is checked, rho = -xyz2/(ratio_h H7_h), xyz recomputed; A_h = sum xyz_j theta^j, B_h = part + sum xyz_j B_j; early residual O + forcing - target with the modified gap-4 target -2 xyz2 theta^6; early rho; middle U2/V4; U = -xyz2 at h=4 (produce 91-125; check 214-246). Gap 7: d0 = [theta^2]A_3, y = z - U d0 (no shifted D index), A_part = y theta, target = -2 z theta^5, fixed 0, A_var 1, target_var 0; particular U(7, A_part, target - W_7, 2), variation U(7, 1, 0, 2); unchanged Lambda: invC2 = W^2 trunc<=6 c^-2, lambda_R = F[theta^6](C^2 integral(R invC2)) - (1/2)[theta^5](same), R = theta then 1, integration constant 0 (produce 127-131, 148-155; check 249-255, 265-270). Gap 8: A_part 0, target -U^2 theta^5, fixed U^2 theta^3 retained as a mate, A_var 1 (134-136; 257). Gap 9 zero data, A_var 1. Gap 10: A_var 0, target_var -theta^4; E = value; literal ell column theta^2 + (6/11)F theta + (1/21)(13H - (24/11)F^2) checked (171-176; 284-287). Late selection c1-first, otherwise c0, otherwise STOP_BAD_COLUMN_PLACE, inverse labelled pivot_h, selection recorded (157-160; 272-277); lambda1 c1 + lambda0 c0 = 1, value = -(lambda1 base1 + lambda0 base0), Psi_h = c1 base0 - c0 base1, A_h = A_part + value A_var, B_h = part + value B_var, full residual retained with both low-pair identities and the separate upper defect (161-170; 278-292). Installation is the fixed table (A0: 0..3, A1..6: 0..2, A7: 0..1, A8,9: 0, A10 empty) sending (h,j) to S^(10-h-3j) theta^j and recording install:5:2 and install:6:2 as discards even when zero (184-196; 300-312); the formal Ab/Bb arrays are not mutated. Dformal = (a2+U)/S, Vformal = (a1 - z + U Dformal)/S as typed laurent_S shifts, Kpar = a0, negative-S subrecords recorded (200-205; 316-320). Pi = z theta - U theta^2 + S theta^3 and Delta = -(E theta Pi + theta Pi^2) with no factor dropped (206-207; 321-322). Euler: B^E_5 = S^2, 6..8 = 0; for j = 4..0 the producer forms the literal Q_j with S-derivatives (211-217) while the checker assembles [theta^(j+2)] of the Jacobian pair minus the unknown leading term (331-337), which I expanded by hand and it equals Q_j term by term; every i in 0..17-3j is visited, q_00 and q_31 recorded and the mate coefficient left 0, all other [S^i]B^E_j = q_ji/(j-3i) (219-226; 340-346). Bhat = sum_{j<=5} B^E_j theta^j; Euler_band[h] for h = 0..10; J = Ahat_S Bhat_theta - Ahat_theta Bhat_S (producer derivatives 234; checker coefficient pairs 364-369); R = J - Delta; 108 positions, 45 low (P1 21, P0 24), 63 upper with the 41/22 split by 23-i-3j <= 7 (246-258; 372-388); 22 readbacks against 0 for h<7 and (-lambda0 Psi_h, lambda1 Psi_h) for h = 7..10. Both inverse charts S = P q^3 - z q^2 + U q, theta = q^-1 with bounds -3..30 and -5..51 and separate pole records (265-277; 391-404). SCALE: z = s^2, theta = s t through the exponent map s-exp = 2 z-exp + theta-exp + offset with offsets -3, -5, -1, -2, -3, -1, -3 (282-291; 410-420); origPi, origUpper, origJ, origResidual = origJ - origUpper - 1 - origu t; guard = W t5 s^8 with orig_a = [S^10 t^0]origA, orig_b = [S^17 t^0]origB; A_inverse, target_inverse (s^-7 Delta), bracket_scale (s^-7 J), low1 as 21 DIFFERENCES [S^i t^1]origResidual - (s^-6 P1_i(s^2) - delta_i0 origu), low0 as 24 DIFFERENCES with -1 at i=0, guard_product = guard orig_a orig_b - 1 (292-316; 421-450). This matches ROOT-ADOPTED-WIRE-PINS items 2 and 3 exactly. The original leading top product W t5 Wi t5i = 1 is retained (280; 407). No operation is skipped because it is not serialized.

## D. Mandatory partition and graph

**CONFIRMED.** Inventory recounted by hand from both files: upper 32 (h1..6 part + basis 0..2 = 24; h7..10 part + variation = 8), early 12, middle 4, install 2, Euler-resonance 2, Euler-band 11, upper-J 63, low-band 22, auxiliary 2 = 150. Canonical flags: 24 + upper:7 pair 2 + 12 + 4 + 2 + Euler-band h<=7 8 + upper-J 41 (per j = 2..7: 8, 8, 8, 8, 6, 3) + low-band h<=7 16 + 2 = 111; alternative 6 + 2 + 3 + 22 + 6 = 39. produce.py asserts 150/111/39 (330-331) and STOPs on any nonzero canonical record (341-342) and on any nonzero scale comparison including every low1/low0 entry (343-345); check.py builds the literal ordered label list, asserts 150 and set equality of names, 111/39 (458-475), STOPs on canonical anomalies (476-477) and on all six scale groups (451-455). Both keep the two full inverse polynomials and two pole records outside the 150 and never require poles to vanish. The partition runs after every stipulated internal expression is computed and BEFORE the producer serializes or the checker compares the graph. False-test audit: I traced every unconditional `need`/`equal` that is not a ROOT-listed tripwire and found each is structural or a construction tautology (checker A10 = 0 since A_var = 0 at gap 10; [S^0]Kpar = 0 from A10 = 0; [theta^3]Ahat = S from [theta^3]C = W Wi = 1; Euler shear/translation gauges are the two zeroed coefficients; the gap-7 "unshifted D coefficient" check at 252-255 reduces to [theta^j](-2 z theta^5) = 0 for j <= 4; the mate-support bound 4/3/2 and the installation index bound follow from the solve degrees; inverse q bounds follow from i - j >= -j and 3i - j <= 3(10-3j) - j). The remaining hard equalities (leading ODE, contacts, inv[2][2] identity, lambda1 c1 + lambda0 c0 = 1 at gap 7, Euler-band h<=7, ell column) are the imported leading identities that ASSERTIONS classifies A, implemented identically in both files; no new ambient identity is demanded from a modular pass. Critical graph: Hq = [z^0]Psi7, c = [z^1]Psi7 as a scalar node, Psi7 = Hq + c z checked, zeta = -Hq c^-1, Psi7(zeta) = 0 checked, T built as z P1_0 - U P0_0 BEFORE substitution, ell = P0_0(zeta), g = Hq ell, U retyped, zeta ell = -c^-1 g checked (produce 318-328; check 479-486). Exactly 25 slots in the order K8, K9, K10, A1_1..9, A0_1..12, T with weights 8, 9, 10, 19..11, 22..11, 27; Hq, zeta 7; ell 23; g 30; U 4; zero and duplicate slots are emitted as empty term lists and compared individually. The checker parses the candidate graph into parsed_graph and uses nothing from it in construction (line 93 comment and code order); no candidate intermediate feeds Eval_C.

## E. Strict wire and semantic control

**CONFIRMED.** Producer serialization: six top keys, eight graph keys, 30 polynomials and two dense scalars, json.dumps with ensure_ascii, sort_keys, compact separators, one trailing newline, ASCII-encoded, 128 MiB cap (produce 347-363); wire_p refuses residual z/S/theta, negative exponents, zero coefficients and wrong weight, sorts by integer exponent tuple (numerical lexicographic) and emits four decimal exponent strings with a dense length-f residue list (arithmetic 336-349); 20000/400000 term caps. Checker parse: read at most 128 MiB + 1 and refuse over-cap or empty, ASCII decode, json.loads with duplicate-key and number/float/constant refusal, INPUT-only round-trip against its own received bytes (check 33-40), exactly six top keys, literal schema/job, source_pins a string mapping with all values and contract_sha256 matching `[0-9a-f]{64}`, exact equality of contract, whole source vector and place with the ROOT context (46-50), place keys and canonical decimal integers, phi f+1 and P7 8 with leading 1 enforced in initialize, graph keys, per-polynomial kind/terms keys, term caps, exact weight per term, dense nonzero coefficients in 0..p-1, strictly increasing numerical tuples (74-92). There is no whole-object comparison with the reconstruction anywhere; the only object-level comparison is the input-only round trip. Prescribed control traced by hand: after ROOT rebinds the fresh artifact SHA, authorize passes; the mutated T is still canonical because (27,0,0,0) is the unique weight-27 tuple with first entry 27 and therefore the last term; parse passes; every internal stage is candidate-independent so construction and partition are unchanged; comparisons proceed c, c_inverse, Hq, zeta, ell, g, U (496-499) then slots in order (500-502); slots 0..23 agree and slot 24 raises Mismatch('direct-source graph slot T'); main catches Mismatch first, reauthenticates unchanged metadata, prints `CHECK FAILED: direct-source graph slot T` to stderr and exits via SystemExit(2): normal exit 2, empty stdout, no traceback (518-522). No earlier comparison involves T (g and the guard identity use Hq, ell, zeta only). Parser, hash, authority, place, cap and Stop failures go to `STOP_NONDECISION: ...` with exit 2 (523-525) or to authority's REFUSED SystemExit, earning no semantic credit. Positive lines: producer prints FORMED_UNCHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME only after the full partition and the fsync/readback/no-overwrite hard-link publication (364-378); checker prints CHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME only after all internal checks, every field comparison and terminal reauthentication (514-517). No fixture, positive or negative test exists; this is static reachability only.

## F. Readiness

**BLOCKED by exactly one correction; otherwise READY for ROOT registration only after the separate runtime FIRST passes.** BLOCKING (correctness of reachability under the fixed prospective ceilings, no measured claim): arithmetic.py power(), lines 236-238, executes `a,n=mul(a,a),n//2` after the final bit, so the inverse-chart loop at produce.py 273, `power(chartS, ex[5])`, forms chartS^32 for every Bhat term with S-exponent 16 or 17 and chartS^16 for exponents 8..15. The S^17 theta^0 term of Bhat is exactly the leading scalar t5i, which is nonzero on every run that can reach success (Euler-band:0 is canonical and forces it to equal [theta^0]D = t5i, and 'leading b' at 279 checks it), so chartS^32 is always attempted. chartS = P q^3 - z q^2 + U q with U the weight-4 polynomial in X1..X4 (at most five monomials); chartS^n has sum over a+b+c=n of N(4c) distinct monomials, where N(w) counts weight-w monomials in weights 1,2,3,4 (about w^3/144). For n = 32 that sum is of order 8x10^5 when U has its generic support (four or five monomials), which exceeds the 100000 internal-term cap inside mul (arithmetic 224) and STOPs the producer with STOP_INTERNAL_TERM_CAP on every such place; with two or three monomials the cap may not fire, but the discarded squaring of two ~10^4-term polynomials is still the dominant cost of the whole run. The result is never wrong, only unreachable. Smallest correction, one line: in power() halve n first and square only if n is still nonzero (e.g. `n//=2` then `if n: a=mul(a,a)`); the same pattern in fpow() (line 95) is harmless for field elements and may be left. After that correction chartS^16 (~4x10^4 terms) and chartS^17 stay under the cap; no cap raise is requested or licensed. The checker's power() (check_arithmetic 221-225) is linear and never exceeds chartS^17, so it has no cap defect. OPTIONAL, not blocking and not licensed as a rewrite: both files recompute chartS^i per source term (produce 273; check 398); a per-exponent cache would be a pure performance change. Other optional items: producer Poly equality is kind-blind; checker labels a terminal metadata drift as CHECK FAILED; producer main() has no catch-all so a Stop is an uncaught ValueError traceback (contract-permitted, no success line). Style: check.py's gap-7 tautology check (252-255) is dead weight. No fixture, prime, place, rank, matrix or runtime exists; the old rational predecessor's 900-wall timeout says nothing about this pair's speed, and I make no time, memory or size measurement. Ceilings 900 wall / 600 CPU / 8 GiB / 128 MiB, 100000 internal, 20000 wire, 400000 aggregate, exponent 256 are refusal ceilings in both files and never truncate. Static review is not execution; no source, rank or JC2 outcome follows from CONFIRMED verdicts.

## G. Remaining quantity, cheapest next gate, collision check

No new canonical OPEN ID. REMAINING QUANTITY (unchanged): whether the two independent implementations of the complete corrected circuit, at one ROOT-supplied place with all 125 rational and 15 scalar residues nonzero and all 111 canonical plus six scale tripwires zero, agree on the 30 polynomials and two scalars; unknown, nothing computed. CHEAPEST AUTHORIZED NEXT GATE: (1) the producer owner applies the one-line power() correction to arithmetic.py and ROOT re-pins that file (a bounded static re-read of the diff and the changed pin, minutes, UNMEASURED); (2) the separate ROOT runtime FIRST (launcher with -B or read-only layout, canonical argv, interpreter and native pins, caps, custody); (3) only then one single-place producer run, one independent checker run, and the T+X1^27 control with fresh rebinding. This FIRST's own planning wall was 18-22 minutes UNMEASURED against the original 04:02/04:05 controls; no runtime forecast is made.

COLLISIONS: EMPTY. Own-only: this lane wrote exactly xmodel/f10-direct-rows-code-gate-fable5-20260911.md and box/f10-direct-rows-code-gate-fable5-20260911/input_custody.md, both absent at 03:38:38Z; apply_patch only, bounded writes under 1500 words (skeleton; 0/A/B; C/D/E; F/G; custody; closing+marker); no Seal, charge_basis, transaction, Write/Edit, redirection into owned files, generated mathematics or code, input edit, network, AWS, SSH, process control, git, agents, peer, live, shared, protected, provenance or ledger access. Tools: date, ls, sha256sum, wc, cat, sed, awk, grep, apply_patch. All 16 pins verified before any body (03:39:20Z) and recomputed after all reads (03:54:03Z, 16/16 OK).

Closeout: own final WHOLE read of both owned files completed 03:55:07Z with no `(pending)` placeholder and no earlier marker; wc reports 3891 words for this report, of which a large share are line citations and numerals, so the substantive body modestly exceeds the 2200-3000 target and was not trimmed against the 04:02Z reserve. This paragraph and the marker below are the final bounded apply_patch write, before the original 04:02Z reserve; all writers are IDLE after it and no follow-on authority is issued. Verdicts: A CONFIRMED, B CONFIRMED, C CONFIRMED, D CONFIRMED, E CONFIRMED, F BLOCKED (one-line arithmetic.py power() correction, lines 236-238), no GAP.

<!-- BODY-END -->
