You are Opus 5, acting as an equal-standing hostile mathematical reviewer in
the plane Jacobian conjecture campaign. Work in `/Users/dc/code/math/jc2`.

First independently SHA-256 verify these two mandatory inputs and stop
fail-closed on mismatch:

- `xmodel/ideation-20260827T2137Z-fable5.md` must be
  `edd383ad33838268cc7e700f2aa913dab73eb6744a60133a131a69eb7ef476bb`.
- `xmodel/ideation-20260827T2137Z-postseal-truth-delta.md`: compute and record
  its actual hash at session start.

Read both in full. You may read named campaign reports and ordinary source
files needed to verify claims. Never enter, list, grep, read, build, or modify
`jc2-lean`. Do not run heavy algebra locally; desk-scale exact scripts are
allowed. Do not touch AWS. Write exactly one new file and change nothing else:

`xmodel/ideation-20260827T2137Z-fable5-crossreview-opus5.md`

Hostile-review the Fable report, with priority on:

1. Verify the exact operator identity `T_m(HY)=H T_{m+4}(Y)`, its induced
   direction on cokernels, and whether localization at `H` preserves the
   polynomial/windowed obstruction problem actually used by the campaign.
2. Audit the proposed assembly of every sector's `q_n` into one coherent
   algebraic section. The `q2` sample is not a proof; account for the admitted
   `q3` mismatch and derive or demand the exact closed forms.
3. Check every use of Krull intersection, torsion-freeness, integral strata,
   discriminant localization, dual residue pairings, rationality, and the
   claimed effective order `N0`. Distinguish pointwise/formal finite
   determination from a uniform computable family bound.
4. Compare this mechanism with your `mu_4`-torsor capacity identity. State
   exactly what composes or conflicts: four fixed character spaces, repeated
   rows, coefficient windows, nonlinear gate independence, and effective
   family determination.
5. Audit the lower-face transfer and the `29 conditions versus 36 slots`
   heuristic: establish the correct licensing schedule and offset before
   accepting any count; distinguish abstract cokernel dimensions from
   independent equations and from a proper/unit prediction.
6. Audit the prefix-unit ladder and the finite-end/passport validator as
   one-sided software/math tools. Identify what is safe now and what depends on
   a complete marked place dictionary.
7. Apply every post-seal correction. In particular, delete the stale
   `C74-PLACE -> EXIT-RPMC(C)` physical-chart route rather than trying to
   repair it cosmetically.

Give an itemized verdict table, the narrowest promotable theorem(s), a precise
errata list, one cheapest discriminator per surviving conjectural step, and an
explicit assessment of what Fable supplied that is unique relative to Opus.
Include checks run, source pins, model identity, report path, SHA-256 in stdout,
and an honest scope/firewall ledger.
