cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
The gate report is complete and sealed at the body level with the standalone marker as its last line at 10:17:14 UTC, inside the 10:19:35 reserve. All writers are idle; nothing further will be written or run.

**Outcome: no blocking static code defect in dispatch.py or probe.py. Acceptance is STATIC ONLY.** REFUTED: none. All seven claims are CONFIRMED, four with non-blocking GAPs.

**What was done**
- Verified both owned targets absent, then hashed all 15 snapshots against the charged pins before any body read. All 15 matched.
- Read 13 inputs whole, including all 471 dispatcher lines, 64 probe lines, authority.py, and the full 1198-line CAPRUN source. producer.py and checker.py were hashed whole and read only at the charged excerpt ranges. The clipped dispatcher display was recovered by three bounded reads.
- Wrote the skeleton, custody file, sections A–G, verdicts, OPEN, COLLISIONS and Completion by apply_patch only. Hash rows were generated from live sha256sum output. Own whole read, marker check and hex-token audit passed.

**Key findings by claim**
- **A.** Registration, full cmdline comparison, EC2/host/namespace/cgroup, root/nonroot via setpriv, and every accepted hash are enforced. GAP: the accepted science hashes bind to a basename match, not the role path, and the checker receipt's code dict is never compared. One-line fixes named.
- **B.** All five refusal strings trace literally through the unchanged authorize path. Both wrong-job CLIs fail at the job tag before any stat. Nine preflights precede mathematics. GAP: the dispatcher checks only for absent write bits, never for o+rx. A 0700 "root-only" authority or source ancestry would pass every check and then STOP at the first probe with a permission error.
- **C.** Every telemetry field the dispatcher reads exists in CAPRUN's dict with matching construction. The dummy TERM-then-KILL, zombie-leader anchor, and cgroup-quiet-before-readback all hold.
- **D.** The 360/330/2 GiB/16 MiB envelope, one-time monotonic start, and 279+49 soft with +1 hard are correct. GAP (liveness): RUSAGE_CHILDREN charges thousands of ps invocations against the 330 ceiling, so a producer near its cap yields a NONDECISION, not a false positive.
- **E.** Tmpfs capacity, O_EXCL writes, rename-freeze-hash, CANDIDATE gating and receipt binding are all confirmed.
- **F.** Durable copy and typed STOP_CUSTODY_INCOMPLETE confirmed. GAPs: the durable parent directory is never fsynced, and the outer stdout location is unverified.
- **G.** No API, syntax-shape or bounds defect found by hand reading. Path regex and cgroup format constraints are listed as ROOT prerequisites.

**What remains owed** is entirely ROOT-side: the prerequisites listed in the Verdicts section and the actual nine-preflight regression. This gate makes no certificate, nonresonance, source, F10 or JC2 claim.
DIVERT path=xmodel/f10-middle-real-runtime-gate-fable5-20260910.md status=CLEAN body_bytes=21867 body_sha256=0562b82afa2b6f6da1e2c488d261fc65f22f705629c12446bbfeaf85bdccbef2
