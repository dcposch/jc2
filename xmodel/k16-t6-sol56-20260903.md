# K = 16 theorem lane at t = 6

Date: 2026-09-03  
Lane: computation, theorem (T)  
Instance: `(76,52; 73; 3; J=cγ)`  
Status: **COMPLETE — MEASURED-MODULAR**

## Verdict

**(T) at t = 6: MEASURED-MODULAR.** All three audited prime fibres have unit terminal ideal. Neither bounded exact `nfmodStd` run returned a basis, so this report does not promote the modular observation to `PROVED-HERE`; the exact question remains open in this lane.

No all-`t` statement is attempted. No exit-price assertion is made, so no `charge_basis=` line is due.

## 1. Custody, scope, and operation

The lane receipt `xmodel/k16-t6-sol56-20260903.run.v2` was parsed mechanically with `awk`: its 18 `_sha256`/`_basename` pairs generated `/tmp/k16t6-receipt-manifest.sha256`. `sha256sum -c` returned **OK for 18/18** frozen files under `/tmp/jc2-lane.Gp5PbG/inputs`; no digest was transcribed into the check command.

The completed heuristic t = 6 artifacts were reused only after `(cd box/k16t6-20260903 && sha256sum -c SHA256SUMS.reused)` returned **OK for 10/10**. In particular, the copied producer log, audit, Q-pivot TSV, A-pivot TSV, terminal TSV, and charged exact-QHy job equal their originating `box/k16t56-20260903/` files byte for byte. Their producer used the frozen chart/preprocessor hashes `e115d576…e7dc28` and `f2defb76…fefbf93`; the terminal TSV hash is `afd1102a…3853`. The old six capped attempts were not reused as results: their stamps had only `START`, no `.exit`, and their outputs stopped at `MAIN_START` or were empty.

All new or repaired drivers and outputs are confined to `box/k16t6-20260903/`. No ledger, `jc2-lean`, `ideation-*`, or named in-progress lane report was read or edited. Long jobs were explicit `timeout` children of foreground shells. The three modular jobs were one-threaded and joined by the same waiting shell. The primary exact phase used three workers; a one-core isomorphic-field pilot was deliberately stopped, stamped `rc=1`, and excluded. The final Laurent exact attempt used four workers. GNU-time peak RSS for multiworker jobs below is a per-process/non-aggregate maximum.

## 2. Chart, Q-spine, and normalizer

The frozen generator constructs the gauged necessary superset with `e=3t+1`, `q=2t+1`, radii `(-1,t/e)`, and the safe target gauges `alpha_t=0`, `const(beta_q)=0`, `const(alpha_e)=0`. At `t=6` it gives

```text
tuple=(76,52;73,3), (e,q)=(19,13), Phi=(-1,6/19),
unknowns including c=63, equations=93.
```

The fresh frozen-driver regeneration returned `FRESH_CHART_PASS`, including all displayed counts and deterministic numeric-parameter h-adic reconstruction; wall time was 3:04.59 and peak RSS 459,848 KiB. The provenance-checked completed elimination gives 22 rational Q-pivots, 70 residual rows, and 41 residual unknowns including `c`. The exact ordered spine is

```text
a7_1,...,a12_1                         coefficient 13;
a13_2,a13_1,...,a19_2,a19_1            coefficients 26,13;
a19_0,a19_3                            coefficients -52,-39/4.
```

The 22 names are distinct; their bands run from 25 down to 12. `t6_Q_pivots.tsv` records every source index, tag, band, name, and coefficient, and a field-by-field checker validates it against this formula.

With `x=q7_1`, `y=q13_1`, the positive grading is

```text
wt(x)=25, wt(y)=50, wt(c)=125.
```

The unique normalizer rows, after `x=1`, are

```text
raw H = 2028y^2-1092y+140 = 4 H_6,
H_6   = 507y^2-273y+35,
cbar  = 19y(7-78y)/2197.
```

`disc(H_6)=3549=13^2·21`, so `H_6` is irreducible and `A_6=Q[y]/(H_6) ≅ Q(√21)`. Exact resultant/inverse checks in the raw-H convention give

```text
Res(H,y)=140,          y^(-1)=39/5-507y/35;
Res(H,7-78y)=354900,  (7-78y)^(-1)=78y/175-1/5;
rem_H(cbar)=-665(13y-2)/28561,
Res(H,rem_H(cbar))=8844500/4826809,
cbar^(-1)=1113879y/3325-85683/665.
```

Each product reduces to 1 modulo `H`. Thus `cbar` is a unit. The homogeneous `c!=0` locus forces `x!=0`, and the positive grading makes `x=1` a covering normalization; no component is discarded by a zero-divisor inversion.

## 3. The two triangular normalisations

### 3.1 Short-remainder heuristic

The provenance-checked run used the charged short-remainder/rare-variable score. After 12 Q-associate duplicates were removed, it had 57 rows in 38 auxiliaries. It made **32 affine A-pivots** (`5t+2`), checking at every step that the coefficient depends only on `y`, its resultant with `H` is nonzero, its displayed inverse multiplies to 1 modulo `H`, and substitution kills the pivot row. Every resultant is nonzero. A-elimination took 168.906 s; the complete producer took 3162.538 s, including 935.118 s for Q-elimination and the exact slice conversion.

It terminates in 12 equations in

```text
(q7_0,q8_1,q9_1,q10_1,q11_1,q12_1).
```

In ascending canonical band order the tags are all `(0,1)`, bands are `0,...,11`, degrees are `25,...,14`, and term counts are

```text
556, 484, 417, 358, 305, 259, 218, 183, 152, 126, 103, 84.
```

The full primitive `Q[y]` representatives and their degree-`<2` `A_6` sections are `t6_heuristic_terminal.tsv`; the 32-pivot certificate is `t6_heuristic_A_pivots.tsv`.

### 3.2 Proved Laurent/Euler spine

This is the charged split-safe order, not an inference from finite samples. Put `s=h-b4` and

```text
U=h^13 + sum_(j=2)^12 qj_0 h^(13-j),
C=h^5  + sum_(j=1)^5 Cj h^(5-j),       a=sC,
g1=19/13,
g2=19y/13+57/169,
g3=19(78y-7)/2197,                     c=-yg3.
```

The coefficient recurrences are

```text
B'=(g3/(2y))(5C+3sC'),
y(D+2sD')=3g3U'+aB-saB'+2sa'B+g3 b3(C+5a'/2).
```

Here the source's `a/s` has been replaced by the polynomial identity `C`, since `a=sC`; no localization at `s` is used.

The `r^1` divisibility condition solves `b1` with unit coefficient `yg3=-c`; the gauge `[h^12]X'=0` solves the integration constant `B0` with coefficient `13/y`. With

```text
V=sa-yb3,  Y=sD-b3B-g3b2,  R=VX'-U'Y-yg3,
```

the descending high coefficients of `R` eliminate

```text
24:C1, 23:C2, 22:C3, 21:C4, 20:C5,
19:q6_0, 18:q7_0, ..., 13:q12_0, 12:b2.
```

The charged generic forms place every high pivot in `Q(t)[y]/(H_t)`. After clearing the displayed finite rational denominators, their recurrences have integral `Z[t,y]` representatives. With `d=2(2t+1)y-(t+1)`, the visible primitive linear classes are

```text
L_C=A_C d+B_C,
A_C=9j^2t+18j^2-54jt^2-81jt-26j+72t^3+144t^2+88t+16,
B_C=-9j^2t-10j^2+24jt^2+33jt+10j-12t^3-20t^2-8t;
L_Q=A_Q d+B_Q,
A_Q=12t^2+16t+4-j(3t+4),       B_Q=2(t+1)(j-t);
L_2=3(2t+1)d+(t+1).
```

Their norms are

```text
Res(H_t,L_C)=-4(2t+1)^2(3t+2)^3 F_C(t,j),
Res(H_t,L_Q)=-4(2t+1)^2(t+1)(3t+2)^2 F_Q(t,j),
Res(H_t,L_2)=-12(2t+1)^2(t+1)(3t+2)(4t+1),
```

with `F_C(t,t-n)` a coefficientwise-positive quartic for `n>=1` and `F_Q(t,2t+1-n)=n^2+4nt+6n+4t^2-3 >= 12`. Hence all reconstruction and high pivots are units, including on split fibres. At `t=6`, `t6_laurent_pivot_specialization.json` records all 13 specialized high coefficients and nonzero resultants.

The exact sparse implementation of these same recurrences completed at `t=6` in 11.16 s (10.98 user s; 46,588 KiB), with internal recurrence, inverse, annihilation, support, band, and degree assertions. Its `t=2,3,4,5` runs matched every field and terminal coefficient of the hash-banked charged records. At `t=6`, `laurent_pivot_formulas.py` printed `FIXED_RECORD_ASSOCIATES_PASS= terminal_laurent_t6.json`. The original general-SymPy attempt was interrupted after 41 minutes with empty output and is excluded, rather than mislabeled a timeout or a result.

### 3.3 Map/equality discipline

Each order is triangular and quotient-preserving; every inversion occurs only after reduction modulo the declared `H`. Each is separately obtained from the same normalized chart by recorded quotient-ring isomorphisms. The heuristic residual coordinates and Laurent residual coordinates differ. No t = 6 rowwise coordinate-map artifact exists, so this report does **not** assert a direct identity between the two terminal coordinate lists by matching band labels or names.

## 4. Terminal system in canonical band order

For the proved spine, after all substitutions `sigma_6`, define exactly

```text
T_(6,k) = [h^k] sigma_6(R),       k=0,1,...,11,
```

in canonical generator order

```text
A_6[b3,b4,q2_0,q3_0,q4_0,q5_0].
```

This coefficient definition plus the displayed Euler recurrences is the compact exact print of the 12 x 6 system; clearing the finite displayed denominator product gives integral representatives. The complete expanded primitive degree-`<2` representatives are printed in canonical band order in `t6_laurent_terminal.tsv` (1,347,642 bytes, SHA-256 `47164418…11313b`). The compact row certificate is:

| band | degree | terms | primitive-row SHA-256 prefix |
|---:|---:|---:|---|
| 0 | 25 | 556 | `e524848c623a` |
| 1 | 24 | 484 | `bba151c482e6` |
| 2 | 23 | 417 | `a987e3f79351` |
| 3 | 22 | 358 | `4a93e4765897` |
| 4 | 21 | 305 | `95ea795ecd93` |
| 5 | 20 | 259 | `06b22a5154f5` |
| 6 | 19 | 218 | `39cecbc3d173` |
| 7 | 18 | 183 | `981c3c61f860` |
| 8 | 17 | 152 | `4ea239103e9d` |
| 9 | 16 | 126 | `6e98e59dfb47` |
| 10 | 15 | 103 | `288079cbd0c7` |
| 11 | 14 | 84 | `27640d7d5eb6` |

The induction-data comparison is:

| t | exact terminal rows x variables | bands | degrees |
|---:|---:|---|---|
| 2 | 4 x 2 | 0..3 | 9..6 |
| 3 | 6 x 3 | 0..5 | 13..8 |
| 4 | 8 x 4 | 0..7 | 17..10 |
| 5 | 10 x 5 | 0..9 | 21..12 |
| 6 | 12 x 6 | 0..11 | 25..14 |

Thus `2t x t`, bands `0..2t-1`, and degrees `4t+1..2t+2` are exact finite data through every completed record. They are induction data, not an all-`t` proof of terminal unitality.

## 5. Direct terminal ideal test and controls

The heuristic terminal ideal returned reduced basis `[1]` at all three audited primes, with `MAIN_DONE`, ideal/ring extraction, wrapper controls, `MAIN_SATURATED_EMPTY`, and exit 0:

| field | result | wall | user s | max RSS KiB |
|---|---|---:|---:|---:|
| `GF(32003)` | `[1]` | 16:21.78 | 659.40 | 535,992 |
| `GF(32009)` | `[1]` | 16:04.06 | 651.68 | 535,968 |
| `GF(32027)` | `[1]` | 16:27.50 | 653.90 | 535,672 |
| exact `A_6`, heuristic | no basis, `rc=124` | 1:15:01 | 2712.98 | 3,365,044* |
| exact `A_6`, Laurent | no basis, `rc=124` | 13:00.56 | 514.78 | 1,475,948* |

`*` GNU-time per-process/non-aggregate peak. The exact logs end at `MAIN_START`; neither contains `MAIN_EXACT_UNIT`, `MAIN_DONE`, or a basis. They are inconclusive caps, not negative algebraic results.

No terminal `sat()` call is hidden here. Since `cbar` is already proved a unit and eliminated, the drivers directly test the terminal ideal. Modular rings contain raw `H=4H_6` as a generator and the exact ring uses `minpoly=H`; because 4 is a unit in `Q` and in all three odd characteristics, these define the same quotient as primitive `H_6`. Two primes split `H_6` and one leaves it irreducible, and all three avoid the leading coefficient/discriminant and every Q/A/unit denominator or resultant.

The Rabinowitsch EMPTY/NONEMPTY controls use `<u,Wu-1>` and `<u-1,Wu-1>`, respectively; each asserts `typeof(...)==ideal`, the current basering, and the expected reduction of 1. The repaired actual-pair classifier has no Singular errors and checks `J(pi,pi-gamma^2/2)=gamma` and weighted pi-degrees `(1,1)`, hence it is not a `(76,52)` chart point. The chart generator's h-adic reconstruction is the independent positive identity control.

## 6. FALLACY-v2 audit

The chart remains a necessary superset: only an exact unit ideal after the proved `c!=0` normalization promotes (T). Modular unit ideals alone are typed `MEASURED-MODULAR`. Raw `H=4H_6` is distinguished from its content-reduced primitive associate. Every remainder is reduced in the declared degree-`<2` quotient; zero is handled before affine pivoting. Generator orders, coefficient rings, and maps are explicit. Prime marks in the Laurent formulas mean `d/dh`; no label is silently differentiated. No flag/place/series identification, exit-set charge, carrier/attainment claim, pole identity, floor equality, merge descent, or target/arrival index occurs in this lane.

## 7. Reproduction artifacts

`RESULTS.tsv` records completed and capped jobs. `SHA256SUMS.final` hashes the curated drivers, terminal records, outputs, stamps, and exit codes and verifies with `sha256sum -c`. Principal records are `t6_heuristic_terminal.tsv`, `t6_heuristic_A_pivots.tsv`, `terminal_laurent_t6.json`, `t6_laurent_terminal.tsv`, `t6_laurent_terminal_summary.json`, the three `t6_modrun_*` triples, and both exact-run triples.

`input_map_validation.out` proves mechanically that the local Laurent constructor differs from frozen hash `f900ca3a…` only by its output directory. It also proves that the exact heuristic driver's 12 rows are byte-identical to rows 2–13 of frozen `t6_heuristic_exact_QHy_std.sing`, its minpoly is byte-identical to row 1, and the run driver differs from its origin only by `setcores(1)` to `setcores(3)`. `fresh_chart_verify.out`, `q_h_verify.out`, and `actual_pair_classifier_fixed.out` bank the remaining controls.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `13067`.
- Body SHA-256:
  `3df3b9559a3c66c4055eec477f3cf8b40563a19588151cbb63a5f101a8c610f0`.
- Frozen basis: `bc097f5aa70096b263e584c2971c5a0791fab7fa`.
