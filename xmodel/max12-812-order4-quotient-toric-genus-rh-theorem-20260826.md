# `(8,12)` order-four quotient geometry: exact invariant fields and toric RH gate

Date: 2026-08-26

Status: **CONDITIONAL PRODUCER THEOREM; EXACT PLANE/NONDEGENERACY GATES LIVE ON
AWS.  NO ORDER-FOUR ELIMINATION CLAIM YET.**

## 0. Scope and charged source

Let `C_4` be an irreducible one-dimensional component of the corrected,
hostile-confirmed normalized coefficient fibre

```text
(r_1,r_2,r_3,r_4-1,r_5,r_6):r_7^infinity,       r_7!=0.
```

The charged source is the V2 coefficient client (SHA-256
`c1aa88b40104205f327240dd33a4e674983577ce6d9dfafc82b8547959312621`)
and its CONFIRMED delta review (SHA-256
`252bbd07d29084952453b45546cff7e0d8ddda7a72576311d1f982ee01a66200`).

This note proves the quotient, completion, genus, and Riemann--Hurwitz
consequences of one explicit exact plane gate.  It does **not** assert that
the live AWS computation has passed that gate.  In particular, neither a
modular relation nor the genus of the affine `r_4=1` slice is consumed as a
source obstruction.

## 1. Exact plane gate

Put

```text
s=a_5,       t=a_6,       q=s^2/t^3,       v=t^8.       (1.1)
```

Assume the following are certified over the characteristic-zero constant
field, enlarged to contain `mu_16`:

1. the normalization `Ctilde` of the complete closure of `C_4` has
   function field `K=L(s,t)`;
2. the unique irreducible relation of `s,t` is

   ```text
   F(s,t)=P(s^2/t^3,t^8)=0;                            (1.2)
   ```

3. `P(q,v)` is irreducible and has Newton polygon

   ```text
   Delta_X=conv{(0,0),(0,2),(1,3),(8,3)};              (1.3)
   ```

4. `P` is nondegenerate with respect to every face of `Delta_X`, including
   the two-dimensional face.

The equality in (1.2) is an equality of Laurent polynomials; multiplying by
a monomial is harmless.  The gate must be checked from the corrected V2
ideal saturated by `r_7`.  Birationality in item 1 cannot be inferred merely
from a plane elimination relation.

## 2. The complete curve and both finite quotients

The residual normalization group acts by

```text
lambda(s,t)=(lambda^3 s,lambda^2 t),       lambda in mu_16.  (2.1)
```

Its subgroup `mu_4` is exactly the source Kummer deck group.  Define

```text
w=s/t,       y=t^2.                                      (2.2)
```

Then

```text
w^16=q^8v,       t=w^2/q,       s=w^3/q,       y^4=v.   (2.3)
```

The character of `w` in (2.1) is primitive.  Hence the action is faithful,
the generic orbit has sixteen points, and

```text
K^mu_16=L(q,v),
K^mu_4 =L(q,y),       y^4=v.                            (2.4)
```

Equivalently, the exponent lattices generated by `(2,-3),(0,8)` and
`(2,-3),(0,2)` have indices `16` and `4`.  Formula (2.3) proves both
containments and the exact extension degrees; no invariant-ring generation
claim is needed.

Let

```text
Xtilde=Ctilde/mu_16,       Ytilde=Ctilde/mu_4.          (2.5)
```

These are smooth complete quotient curves with the fields in (2.4).  Thus
`Xtilde` is the normalization of the ordinary projective closure of
`P(q,v)=0`, while `Ytilde` is the normalization of the ordinary projective
closure of `P(q,y^4)=0`.  The latter statement includes every point with
`t=0`, every pole of `q` or `y`, and every point at infinity.  It is not the
same object as the affine `r_4=1` slice.

## 3. Exact genera from the Newton polygons

Nondegeneracy and the toric genus theorem identify the geometric genus with
the number of interior lattice points.

For `Xtilde`, `Delta_X` has area `23/2` and boundary length

```text
2+1+7+1=11.
```

Pick's theorem gives

```text
g(Xtilde)=23/2-11/2+1=7.                              (3.1)
```

Replacing `v` by `y^4` scales the second exponent coordinate by four.  The
deck quotient polygon is

```text
Delta_Y=conv{(0,0),(0,8),(1,12),(8,12)}.
```

It has area `46` and boundary length `8+1+7+4=20`, hence

```text
g(Ytilde)=46-20/2+1=37.                               (3.2)
```

Finally the monomial map `(s,t)->(q,v)` has lattice index sixteen.  The
polygon of `F` is

```text
Delta_C=conv{(0,0),(0,16),(2,21),(16,0)},
```

with area `184` and boundary length `16+1+7+16=40`.  Therefore

```text
g(Ctilde)=184-40/2+1=165.                             (3.3)
```

Equation (3.3) is a consistency check, not by itself a source obstruction.
The relevant source target is at least `Ytilde`, because the source base is
the deck quotient.

## 4. All boundary places and the residual `mu_4` cover

The four edges of `Delta_X` give all zeros and poles of `q` and `v` on
`Xtilde`.  Nondegeneracy makes the face roots distinct.  The complete table
is

| edge | number of places | `ord(q)` | `ord(v)` |
|---|---:|---:|---:|
| `[(0,0),(0,2)]` | 2 | 1 | 0 |
| `[(0,2),(1,3)]` | 1 | 1 | -1 |
| `[(1,3),(8,3)]` | 7 | 0 | -1 |
| `[(8,3),(0,0)]` | 1 | -3 | 8 |

The zero and pole degrees separately balance: `deg(q)_0=deg(q)_infinity=3`
and `deg(v)_0=deg(v)_infinity=8`.

For `Ytilde->Xtilde`, the Kummer equation is `y^4=v`.  A valuation `n`
has `gcd(4,n)` points above it, ramification index
`4/gcd(4,n)`, and total local ramification contribution
`4-gcd(4,n)`.  Thus the one upper-left place and the seven top places are
totally ramified; the two left places and the lower-diagonal place are
unramified.  The total ramification is

```text
R_(Y/X)=1*3+7*3=24.                                  (4.1)
```

Riemann--Hurwitz gives

```text
2g(Ytilde)-2=4(2g(Xtilde)-2)+24=4*12+24=72,
```

which reproduces `g(Ytilde)=37`, including infinity.

## 5. The deck `mu_4` cover and the full `mu_16` check

On `Ytilde`, the Kummer equation for `Ctilde->Ytilde` is

```text
w^4=q^2y.                                             (5.1)
```

The lifted boundary table is

| source edge on `Xtilde` | places on `Ytilde` | `ord(q)` | `ord(y)` | `ord(q^2y)` |
|---|---:|---:|---:|---:|
| left | 8 | 1 | 0 | 2 |
| upper-left | 1 | 4 | -1 | 7 |
| top | 7 | 0 | -1 | -1 |
| lower diagonal | 4 | -3 | 2 | -4 |

Consequently the eight left places have inertia two, the upper-left and
seven top places have inertia four, and the four lower-diagonal places are
unramified.  The total ramification is

```text
R_(C/Y)=8*(4-gcd(4,2))+1*(4-gcd(4,7))
        +7*(4-gcd(4,1))=16+3+21=40.                  (5.2)
```

Riemann--Hurwitz gives

```text
2g(Ctilde)-2=4(2g(Ytilde)-2)+40=4*72+40=328,
```

which reproduces `g(Ctilde)=165`.

Directly over `Xtilde`, (2.3) is `w^16=q^8v`.  The four rows of the first
table give valuations `8,7,-1,-16`.  Hence

```text
R_(C/X)=2*(16-gcd(16,8))
        +1*(16-gcd(16,7))
        +7*(16-gcd(16,1))
        +1*(16-gcd(16,16))
        =16+15+105=136.                              (5.3)
```

Again

```text
2g(Ctilde)-2=16(2g(Xtilde)-2)+136=16*12+136=328.
```

The tower identity `40+4*24=136` independently checks every ramification
contribution.

## 6. Source consequence and passport firewall

An order-four source Kummer curve maps equivariantly to the relevant
component of `C_4`.  Its quotient base `P^1_x` therefore has a rational map
to `Ytilde`; properness of `Ytilde` extends this map across every pole.  The
function `r_7^4` is deck invariant and pulls back, up to a nonzero constant,
to the nonconstant terminal map.  Thus the extended map is nonconstant.

Accordingly, if the exact gates of Section 1 pass, (3.2) contradicts the
existence of the source.  In fact the composition to `Xtilde` is already
nonconstant because `r_7^16` pulls back to the fourth power of the terminal
map, and (3.1) gives the same contradiction.  This would eliminate the
entire `mu_4!=0` coefficient leaf for every `U`, not merely `U=2`.

Until the exact plane, birationality, irreducibility, and nondegeneracy gates
pass, no such elimination is claimed.  If any gate fails, one must instead
normalize the exact deck invariant graph and compute the degree of
`rho=r_7^4`.  For `U=2`, the confirmed passports have degrees `D=1` and
`D=3`, so `deg(rho:Ytilde->P^1)` must divide `1` or `3`.  That is a
necessary terminal sieve only; passing it does not provide Taylor
polynomiality or a Keller pair.
