# One-dicritical defect/connectivity: bounded source test

Owner /root/contact_collision_geometry. MANUAL / UNREVIEWED. First action 2026-09-11 18:04:31 UTC; original publication reserve 18:22 UTC / HARD 18:25 UTC, unchanged. Frozen basis 0d39df3c9fd69c939a8420c54d03228b9077777d.

The target is the full one-dicritical (e,d)=(2,1) stratum of a stipulated actual Keller map, uniformly in geometric degree N, not coordinate degree and not arbitrary JC2. The proposed inequality N<=2+sum K_p is a question, not a premise. Exactly ROOT TASK and its three pinned reports are charged; review repairs govern the meridian-floor report. No linked sources or live ROOT result is a proof premise.

## 1. Verdict and source tier

**SOURCE-GAP.** No proof or counterexample to the proposed inequality is obtained. The exact source-attached output is a conditional transport lemma at a K=0 ORDINARY NODE. It invalidates fixed global sheet-support labels, not every possible connectivity-versus-defect argument. No claim that every surviving curve has such a node is made.

Use the source geometry at its charged tier, not as a fresh foundation review. For the stipulated Keller map F, let D be its irreducible nonproperness curve and let U be the affine source A2. On the charged smooth compactification X, the unique affine-image dicritical l has l'=l intersect Phi^{-1}(A2) isomorphic to A1. Its map to D factors through the normalization eta:D~ -> D. Since its tangential degree is one, l' -> D~ is an isomorphism; D~ is A1. The normal index along l' is two. Over the target complement V=A2 minus D, the actual source U minus F^{-1}(D) is an N-sheet finite etale cover.

The charged fibre and Euler identities specialize to

    W=2, a=N-2, N-a_p=2r_p+K_p,
    sum_p K_p=N-3.

At a normalization place t over p, write its local escaping degree as mu_t=2+k_t. Here k_t>=0 and K_p is the sum of these k_t over the places above p, because the degree-one dicritical map has local degree one everywhere. These are the actual fibre-law charges, not newly assigned costs.

If the proposed N<=2+sum K_p were proved from this source, it would give N<=N-1 and exclude this stratum. Neither that contradiction nor transitivity itself proves the missing inequality.

The MFS review repairs are retained: meridional rank gives a LOWER bound through mrk(G)<=deg D; its scope is not an impossibility theorem for all future geometric methods. No centerless assertion is used. Local orbit/component identifications use the stipulated small-ball covering hypothesis. Floors and price tables assert no realizability. The S-known and W-only floors are not confused; no table or coordinate-degree estimate enters the lemma below.

## 2. Actual conditional node-transport lemma

Suppose p is an ordinary node of D with K_p=0. Let t_1,t_2 be its two distinct normalization places, viewed on l'. Choose a sufficiently small target ball B_p. In the charged ordinary-node local picture, the two positive branch meridians act on the N sheets as transpositions tau_1,tau_2 with disjoint supports P_1,P_2. This is the actual small-ball escaping picture, not an inference from abstract group order: there are four escaping sheets, two at each normalization place, and N-4 affine sheets. In particular this conditional case requires N>=4.

There is a based loop gamma in V whose actual monodromy g satisfies

    g(P_1)=P_2, and therefore g tau_1 g^{-1}=tau_2.

Here is the source construction, including its forbidden divisors.

1. Remove from l' the preimages of Sing D, its intersections with every OTHER component of X minus U, and its intersections with every other component of Phi^{-1}(D). There are finitely many such points: distinct algebraic curves have only finitely many intersections on this surface, and only finitely many divisor components occur. Infinity is not in l' to begin with. Call the remaining connected punctured affine line l^o. The normal-index-two local form is available there, and eta identifies its image with a subset of D_smooth.

2. Pick t_i' in l^o arbitrarily close to t_i, with images inside B_p. Join t_1' to t_2' by a compact embedded arc c in l^o. A complex affine line minus finitely many points is path connected; the arc can avoid every removed point. Its image under eta is an arc in D_smooth. Thus this step uses the SINGLE actual dicritical and its degree-one normalization map, not merely conjugacy in S_N.

3. Along c choose a thin normal neighborhood disjoint from all the other forbidden divisors. Such a neighborhood exists by compactness of the arc. In local source/target transverse coordinates the map has the degree-two form (s,z) -> (s,z^2), after multiplication by nonvanishing analytic units. Push the target arc slightly in a continuous nonzero normal direction. The resulting path delta lies in V and has two tracked lifts near c. Both lifts lie in U minus F^{-1}(D): the only removed source divisor in the chosen neighborhood is l' itself, and the normal coordinate never vanishes. No contracted tail, other boundary component, strict transform of F^{-1}(D), or point at infinity is crossed. This is a topological/analytic path construction, not a claimed global algebraic section or coordinate chart.

4. Let y_i be delta's endpoints and y a reference point of B_p minus D. Choose basing paths beta_i from y to y_i INSIDE B_p minus D, matching the two local escaping components. Then beta_1 followed by delta followed by beta_2^{-1} is gamma. Transport first identifies P_1 with the two tracked lifts, next carries those lifts to the second neighborhood, and finally identifies them with P_2. With the convention g(P_1)=P_2, conjugation gives the displayed equation. Different choices can change g, but not the existence of such transport.

The node costs K_p=0, and the arc itself meets only the smooth normal-index-two locus. Thus this is a genuine conditional source path carrying an inertia pair to a DISJOINT pair without traversing a positive-defect point. It is not a constructed Keller map or a realization of the proposed forbidden stratum.

## 3. The first missing implication

The elementary counting step WOULD be valid under the following extra global transport hypothesis. Start with one two-sheet support. Attach each charged local escaping set of size 2+k_t at most once, each time overlapping the already accounted set in at least two labels. Then each attachment adds at most k_t labels, so the final set has size at most 2+sum_t k_t. To conclude the desired inequality, one must also prove that this final set contains the supports of ALL relevant meridian conjugates, including those obtained by normalization return paths, global self-identifications and infinity. Transitivity would then force the set to contain all N sheets.

Neither the sizes 2+k_t nor the identity sum k_t=N-3 supplies that global hypothesis. They give cardinalities of LOCAL escaping fibres, not simultaneously based identifications or a generating statement about their transports. MFS's local orbit estimate and its lower bound on meridional rank point in the other direction and do not provide it.

The node lemma pinpoints a first invalid shortcut: if one declares the generic escaping pair globally fixed merely because l' -> D~ has degree one, the constructed g sends that pair to a disjoint one. In a growth procedure that has accounted for P_1 but not P_2, this transport introduces new labels with no local charge at the node. This does NOT prove that every possible global accounting fails: P_2 might have been accounted for elsewhere, and g's homotopy class might involve positive-defect events even though the chosen path avoids them. A successful proof would have to establish precisely that global charging or generation statement, not assume it.

Two source-topology cautions are essential. First, the normalized curve being A1 does not remove the self-identification t_1~t_2 in D; the regular locus used for transport is also punctured. Second, simple connectivity of U=A2 cannot trivialize this monodromy. The lifted gamma goes from one sheet to another and is not a loop in U. Even when a power has a closed lift, a contraction in U may cross F^{-1}(D), whereas the covering is over its complement. Thus the actual affine source open has been retained throughout the construction, not replaced by an abstract permutation representation, but its global geometry has not supplied the desired extra hypothesis.

This is distinct from the already-known S_N/point-stabilizer controls. No new finite group example, curve passport or surface is manufactured here. The source-conditional path, and the precise limitation on using it, are the bounded contribution. If the surviving curve has no K=0 ordinary node, this lemma makes no assertion about its other singularities; the missing global theorem is still not supplied by these inputs.

## 4. Scope and closeout

QUANTITY: decide N<=2+sum K_p uniformly in geometric degree on the stated actual-source stratum. Verdict SOURCE-GAP: neither this inequality nor its negation is established. No stratum exclusion, source realization, coordinate-degree bound or JC2 conclusion follows.

CHEAPEST TEST performed: a manual check of the local-to-global support step using the actual dicritical and forbidden divisors. A future proof would have to control the based global transports relative to the charged local sets; this is a mathematical missing implication, not a request for a group farm or another floor table. Scientific runtime/feasibility is UNMEASURED. No further task, gate, computation or new canonical OPEN identifier is selected.

All four inputs were pinned before their own bodies. The three reports were pinned together before any report read and read FRESH WHOLE, including seals, in bounded ranges with no clipping. The MFS review was read first. All fresh input reading completed by the observed 18:05:30 UTC clock. Earlier linked foundations remain at the explicit charged tier; no linked file, /tmp driver, historical code, live review, external source or protected tree was read.

Own-target collision checks found final report, manifest, PINS and custody absent; ROOT TASK remains unchanged. Final private partial/PINS WHOLE readback, input postpins and OPEN/QUANTITY/scope/collision checks precede the unique body marker. Normal close/finalize/expected verification and sealed WHOLE readback follow. Final custody binds all inputs and owned report/manifest/PINS, excluding itself. All authored files are confined to the specified lease/report and own documentary box; no scientific execution or shared edits occurred.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `10635`.
- Body SHA-256:
  `a1decd2852fc6b619ca09964699e721d0c1925362ee9aa0da4d0f4e5ed6b0cb9`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
