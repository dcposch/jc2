# Hostile review V2 — first claimed complete elimination of the `(8,12)` exact-order-four coefficient-infinity leaf

| Field | Value |
|---|---|
| Target | `xmodel/max12-812-order4-source-to-lemniscatic-elimination-20260826.md` |
| Required target SHA-256 | `0ac7da1db8eb37c638f4d8014ffc4e62741c808212dccd5c6e8cbb1d4602d49b` |
| Recomputed target SHA-256 | `0ac7da1db8eb37c638f4d8014ffc4e62741c808212dccd5c6e8cbb1d4602d49b` |
| Overall verdict | **CONFIRMED** |
| Smallest failing identity | none |
| Smallest missing hypothesis | none that breaks a numbered claim of the target |
| Reviewer / model | Grok 4.6 (xAI). Independent hostile evidence review of the frozen named bytes. Different model family from the producer. No producer status line, no `PASS` token, and no charged `CONFIRMED` string is evidence of a lemma |
| Method | SHA-256 of the target and of every charged V2/V3/saturation/predecessor artifact; source reading of compilers, runners, emitted Singular, stdout/meta/validation; hand algebra of the Groebner criterion, prime lift, nonverticality, descent characters, and the genus-one pullback. No local Singular, Sage, msolve, Lean, or substantive exact Python |
| Repo | `/Users/dc/code/math/jc2` |
| Git HEAD | `574e873d4753b57dbaeb0fb83e320fbef3694724` |
| Date | 2026-08-26 |

Independently recomputed SHA-256 of the target is `0ac7da1db8eb37c638f4d8014ffc4e62741c808212dccd5c6e8cbb1d4602d49b`, matching the required pin. The cancelled previous review tag, its obsolete target SHA, and its log/prompt/run were not opened and are not evidence. V1 seeded endpoints were not opened. Producer verdict language was not used as a theorem.

---

## Verdict

The composition is correct as a necessary-source elimination of the complete exact-order-four coefficient-infinity leaf of `(8,12)`, for every source degree `U`. Independently: the reviewed order-four high row is exactly the pair of strata `mu_4=0` and `mu_4!=0` with no omitted lower Faber loads; weighted normalization to `r_4=1` loses no geometric `mu_4!=0` source; `8 dR_7/dx=j/u!=0` makes `r_7` nonconstant; the 71-element seed is the frozen `modSat(Itail,ideal(r7))` basis copied byte-for-byte into `JQ`, not a guessed component and not a mere containment `Itail subset (JQ)`; both V3 AWS hosts consumed identical source archive and identical emitted input, each with `rc=0` in `.meta` and `engine_rc=0` in validation, and with no `<tag>.rc` file; V3’s two-sided equality `std(lead(GredRaw))=std(lead(std(GredRaw)))` is exactly the standard-basis criterion for the raw reduction, and repairs the V2 near-pass; the monic model over `Z_(32003)` is a free standard-monomial module whose special fibre is a domain, so the generic fibre is a domain even in positive dimension, while `dim J=1` is charged separately by `SEEDED_Q_DIM=1`; `J` prime and `a6 notin J` give `J:a6^infinity=J` formally, independently of the V2 `a6` control; the one-dimensional special `(a5,a6)` contraction plus primitive content forbids both `q` and `v` constant; exact membership of the cleared `P(q,v)` together with geometric integrality of the residual plane makes every geometric coefficient component dominate that plane and map nonconstantly, after `y=a6^2`, to the irreducible normalization of `P(q,y^4)=0`; a genuine source has dense image in one such component, with `a6`-chart coverage; the deck characters make `q` and `y` invariant, so the map descends to `P1_x` and cannot exist. The complementary confirmed Shioda--Hall theorem kills `mu_4=0`. Order two, order one, emptiness of all `(8,12)`, maximum twelve, and JC2 are untouched.

**CONFIRMED**

---

## Hashes and charged evidence

Recomputed SHA-256. Every value below was hashed from the named path in this review. Manifest lines that recompute are marked “matches freeze/RESULTS”.

### Target and named predecessors

| Artifact | SHA-256 | Role |
|---|---|---|
| `xmodel/max12-812-order4-source-to-lemniscatic-elimination-20260826.md` | `0ac7da1db8eb37c638f4d8014ffc4e62741c808212dccd5c6e8cbb1d4602d49b` | target; matches the required pin |
| `xmodel/max12-812-order4-mu4-nonzero-coefficient-curve-client-v2-20260825.md` | `c1aa88b40104205f327240dd33a4e674983577ce6d9dfafc82b8547959312621` | source-typed V2 client |
| `xmodel/max12-812-order4-mu4-nonzero-coefficient-curve-review-grok-v2-20260825.md` | `252bbd07d29084952453b45546cff7e0d8ddda7a72576311d1f982ee01a66200` | named client review; unused as a lemma |
| `xmodel/max12-812-order24-coefficient-infinity-source-audit-20260825.md` | `092dfb6d7de3e57ae153dcf3365fa2884ce289710b6c80c3193a2bc7f7d7853e` | exact order-four load list `(2.5)`, deck `(4.1)` |
| `xmodel/max12-812-terminal-exact-differential-divisor-theorem-20260825.md` | `1cd824c9553a800ddf390bc972a29656e1286699406b1ca46bbc94bdfe18084b` | `8 r_7'=j/u` |
| `xmodel/max12-812-terminal-exact-differential-divisor-review-grok-20260825.md` | `db67f16dbda759b8481fcbf32491fe77c37483bb5fe70830eb42e95541767251` | named terminal review; unused as a lemma |
| `xmodel/max12-812-order4-source-projection-prime-lift-criterion-20260826.md` | `30bdf1100b21ad5cbb4f8cfcb495b47b9da61b17cb6199354d1e5b54ff85100c` | weakest gate and monic good-reduction lemma |
| `xmodel/max12-812-order4-residual-cusp-kummer-genus1-gate-20260826.md` | `cd6c37c145e325a48dbf382453bde4c727b98a576897d10a1f9b3752ebdd1756` | residual plane, `g(Y)=1` |
| `xmodel/max12-812-order4-residual-cusp-kummer-genus1-review-grok-20260826.md` | `59fb338533ff47a25d00893684caa3076bb7bb7bc2a43ab788cabbc0eb932c6a` | named genus review; unused as a lemma |
| `xmodel/max12-812-order4-residual-cusp-kummer-genus1-tangent-erratum-20260826.md` | `26a083fcfa805a52fdb295ceaaf94ae7497047d8545a2323a48a688ac39e2300` | nonmutating tangent wording |
| `xmodel/max12-812-order4-lemniscatic-target-corollary-20260826.md` | `97ace425a5fc0dc03aa12edb2e09f29e3143566b0939d3ce833a3872ee32a07f` | `Z_E^2=X_E^3-16X_E`, `j=1728`, differential `(8)` |
| `xmodel/max12-812-order4-lemniscatic-target-corollary-review-grok-20260826.md` | `09684e0be2784758b97f7e82e19ae9c8a33c7375d637417740d8f21b7d686425` | named lemniscatic review; unused as a lemma |
| `xmodel/max12-812-order4-mu4zero-davenport-stothers-elimination-20260825.md` | `a5d40fd81838118735c4c2ad379bbac0a1e7d70a66a4a419eff078e8099d7b03` | complementary `mu_4=0` theorem |
| `xmodel/max12-812-order4-mu4zero-davenport-stothers-review-grok-20260825.md` | `a40827f99f63292d8cceb95cc491493bec1a618424f0fd8043236279d5aa8904` | named Shioda--Hall review; unused as a lemma |

### Exact saturation source

| Artifact | SHA-256 | Role |
|---|---|---|
| `.../coefficient_curve_v2_modsatQ.sing` | `d2fedf6c9b2b09787c7e45a00f1626ac10f7d4c68c44030102758021a55c9496` | six-generator `Itail`, `modSat(Itail,ideal(r7))` |
| `.../max12_812_order4_mu4_nonzero_curve_v2_modsatQ_..._box02.stdout` | `5e780dcf82d2a3ac17c3ae7957c819c8b7b8a1a1de6231f88e4f808a2b348c45` | unique 71-line basis between `SAT_BASIS_BEGIN/END` |
| that stdout’s extracted basis (71 lines, 38268 bytes) | `98b5dbd2e1736c089e5e079c6b5889c8c6c0a27cec31cfaff7069cc64864159e` | exact bytes copied into every `JQ` |
| `.../modsatQ_..._box02.stderr` | `704a2d8379bd0c1ab5bdbdd7d5cfb0ce77fe7f4e779c81a24461ccf3ab8366ff` | timing wrapper; not a Singular diagnostic |
| `.../modsatQ_..._box02.meta` | `6b2f7fec3424b6f5f1d924b6024235845c27e286393acb8dc9e51549dbd7ed4a` | `rc=0`; stdout SHA matches |
| `cases/max12_812_order4_mu4_nonzero_curve_20260825/FREEZE_V2.sha256` | `d4a5c584a792d38d74c09b607da4777ba83715d0d092c590b99f15083b35e31b` | V2 curve freeze |

### V2 seeded projection

| Artifact | SHA-256 | Role |
|---|---|---|
| `.../v2_20260826/FREEZE.sha256` | `ff7381f6ac2b78445a3ed9debab6a1116568984defe98a9d0ce77f414308fcbf` | V2 freeze list |
| `.../v2_20260826/RESULTS.sha256` | `1c1c937c8ec58c13cdfcabef4d168490d60b00341622c5e8e650fb906b7fc47a` | V2 results list |
| `.../compile_seeded_projection_v2.py` | `7ede3c5a34bf4d2a1592dedaa268307bd464c8dd5b22936f684999aefbb1a5bd` | pins both full SHAs; copies 71 lines into `JQ` |
| `.../run_seeded_projection_aws_v2.sh` | `a95b0b3c9502fbde2fda065c3f457d7a9870a4597a542bfce985ae163f6bbbfa` | V2 validator |
| `.../launch_remote_v2.sh` | `073800b47dd643366e7b6db16f60f704c8f7a38de4da7d57eccc275f1ddf122b` | V2 launcher |
| `.../v2_20260826/REGISTRATION.md` | `4f4ac063bb89390b85060b750e371979980e0ddb587ad22aca5d78a66b7e1d7e` | V2 registration |
| `ops/aws_exact_lane.sh` | `ebe06a100ae8f6bcdbdc580be89954d4cbc560c97cd75014153d61292415959b` | evidence-grade wrapper; writes `rc` into `.meta` |
| `.../aws_box03/compiled/seeded_projection_v2.sing` | `28721c66a0838487dc9490f1a33cd4b3d95324ffe80aaef9dcedabed3c10645a` | V2 prime input; `JQ` bytes equal the extracted basis |
| `.../aws_r6d/compiled/seeded_projection_v2.sing` | `e914485304a962554404c43e569fbd0f20776ff70d9d710ce22bc60ff65331af` | V2 `a6` input; same `JQ` bytes, different suffix |
| `.../seeded_prime_v2_..._box03.stdout` | `cc74e37bc889eb1ecc16db8737ba91e142c764e6b8a6744e9306b2cc282e279d` | V2 prime near-pass; five `GQ is no standard basis` lines |
| `.../seeded_prime_v2_..._box03.meta` | `e8b57120fadc7389128dc3ad3f323fa5cdddc734bdef63bcd7812ece5fe1dda1` | `rc=0` |
| `.../seeded_prime_v2_..._box03.validation` | `3183c29e4100279c84670877bf583e56b97442a4aa3c6249f8c018dd6e298bfe` | `engine_rc=0`, `validator=PASS` |
| `.../seeded_a6_v2_..._r6d.stdout` | `cd97e2c95cf8e32d498367179b94600da08839c601f3e57329379eed6987d884` | V2 `a6` control; six `GQ is no standard basis` lines |
| `.../seeded_a6_v2_..._r6d.meta` | `40b00680dfa885fa55f666044c59fada376b65d6f89375ca36e34758d9b4024c` | `rc=0` |
| `.../seeded_a6_v2_..._r6d.validation` | `815b1a58bd171887f3ea3646e412cbb8332de32663143a653c6cf149c9095acd` | `engine_rc=0`, `validator=PASS` |

### V3 raw-reduction control

| Artifact | SHA-256 | Role |
|---|---|---|
| `.../v3_rawgb_20260826/FREEZE.sha256` | `07c7582663a7f6569aaf0f8101f0b32ca631f711df33be3a03572d08996debe6` | V3 freeze list |
| `.../v3_rawgb_20260826/RESULTS.sha256` | `ee921a6f3d63d1963a99ef564847d34f1975acd6f21604eb105d5136fafcb64b` | V3 results list |
| `.../compile_seeded_projection_v3_rawgb.py` | `a10133c1bc958a166a23388e71ea5e4b0044d30dce4c55e37bd7c03b0ea2baf7` | pins V2 input `28721c66…`; unique marker replacements |
| `.../run_seeded_projection_aws_v3_rawgb.sh` | `a50ad0b626b60d85ec7f939c29c481cbd8dc7bd45d7e243d12c6aa7898a9eccc` | requires unique PASS sentinels, `engine_rc=0`, zero `GQ` warnings |
| `.../launch_remote_v3_rawgb.sh` | `9cbe26de810a55a5788ad4f341b72bd9c3b6a6d039114456549539da2fa530e2` | recompiles V2 then V3 on the host |
| `.../v3_rawgb_20260826/REGISTRATION.md` | `995d32b566b0b15b68cacbbb6e87751e83b9d626874eea810bfdb2194e47d67f` | V3 registration |
| both hosts `source.tgz` | `22337e0323a95ebe262a9f21d0c48aadfc1560d8a333b7a7db5a02e253dc8d8c` | identical frozen source archive |
| both hosts `compiled/seeded_projection_v2.sing` | `28721c66a0838487dc9490f1a33cd4b3d95324ffe80aaef9dcedabed3c10645a` | identical V2 prime input |
| both hosts `compiled/seeded_projection_v3_rawgb.sing` | `97fc8dbec766dea32afa0c1857447341b81fc7ce87a0bec72764b055510e05a7` | identical V3 input; `JQ` bytes equal the extracted basis |
| both hosts `compiler_v3.stdout` | `746c886217e409914ebf8af9196d4d99ad60a7845509f2ae4c1da599f4ad5868` | records those two input/output SHAs |
| Box03 prime stdout | `9d59776d2682d83ce873583ecae07c24059483fdc06d3f43dd542b169182cc34` | all charged sentinels `=1`/`PASS`; zero `GQ` warnings; zero `? `; zero `=FAIL` |
| r6d prime stdout | `77027e08c71352afa848ec1f3966d197919c1c623b8d727999516e327bc6b805` | same non-warning lines as Box03 |
| both hosts validation | `4eae0cd3a340921c88891e12040f966cb65e7f0883121a1bb45cd77a15a61c0f` | `engine_rc=0`, `gq_not_standard_basis_warning_count=0`, `validator=PASS` |
| Box03 `.meta` | `5cf277a7d6d75805ac534c3e843fccba8d5d8a5285912d6edbc90f32db9e1262` | `rc=0`; stdout SHA `9d59776d…` |
| r6d `.meta` | `63fcb3983e818327f1b170e978b43092b064a7877b5c6ffa3d602548596ff049` | `rc=0`; stdout SHA `77027e08…` |
| both hosts `SINGULAR.version` | `fa1d8f3cf059880c82abec7dcad2f8390f4331a4e5c6b74ac2de322fcffbcc4a` | Singular 4.3.2 |

### Plane membership

| Artifact | SHA-256 | Role |
|---|---|---|
| `.../run/exact_candidate.stdout` | `9b0e08c2d826286364f07f6a6fba9560e23ca8ea4fcba15cbbcf373152badbe3` | `EXACT_CANDIDATE_MEMBER=1`, `EXACT_RESIDUAL_FACTOR_COUNT=1` |

V2 `FREEZE.sha256` and V3 `FREEZE.sha256` recompute for every listed path. V2 `RESULTS.sha256` and V3 `RESULTS.sha256` recompute for every listed path that this review opened. No V1 artifact is a parent of a numbered claim.

---

## Attack 1 — exact source and exhaustion

**CONFIRMED.**

The source audit `(2.5)` is the complete order-four load after the character filter and the three target gauges:

```text
e=4: (r_1,...,r_7)=(0,0,0,mu_4,0,0,R_7),
e=2: (r_1,...,r_7)=(0,mu_2,0,mu_4,0,mu_6,R_7).
```

Exact order four is `e=4`. There is no surviving lower Faber constant: `(2.6)` is `H_F(w)=w^12`. The `mu_4=0` and `mu_4!=0` branches of that row are exhaustive for this leaf. Order-two loads are a different `e` and are not consumed.

The Faber weight is `wt(r_ell)=12+ell`, so `wt(r_4)=16`. After a finite constant extension, `lambda^16 mu_4=1` represents every geometric `mu_4!=0` point on `r_4=1`. This is a weighted `G_m` scaling of the depressed coefficients; it loses no geometric source and does not claim arithmetic descent.

The terminal identity is `8 r_7'=j/u` with `j in L^*`. If `R_7` were constant on `C` its derivative would vanish, contradicting `j/u!=0`. Hence `r_7` is nonzero and nonconstant as a function on any genuine source. The coefficient-curve client records the same identity as `(0.2)` and the triangular comparison of `Itail` with the degree-`(11..6)` coefficients of `V=g^2-f^3+2f`. The frozen saturation source reconstructs both presentations, requires both containments and `reduce(v5+2*r7,Gtail)==0`, and only then saturates. Its stdout prints

```text
TAIL_TO_COEF=1
COEF_TO_TAIL=1
LEAD_RELATION=1
SOURCE_EQUIVALENCE=PASS
```

uniquely, with no `=FAIL`. No `(9,12)` formula is present in the source or in the V2/V3 compilers.

The complementary `mu_4=0` theorem is the unique Hall/Shioda order-four Davenport--Stothers normal form plus the divisor-19 contradiction, for every `U>=1`. Its independent review pin recomputes. This composition uses that theorem only as the complementary stratum. It does not import the extra named order-two `[6,2]` kill.

---

## Attack 2 — saturation-seed equality, not containment

**CONFIRMED. The custody chain is closed. `JQ` is not a guessed component.**

The frozen source constructs the intended six-generator tail

```text
ideal Itail=r1,r2,r3,r4-1,r5,r6;
```

checks the independent shifted presentation `Icoef=v11,...,v6`, and then executes, verbatim,

```text
list SS=modSat(Itail,ideal(r7));
ideal J=modStd(SS[1]);
```

It prints `SOURCE_EQUIVALENCE=PASS`, `SAT_IS_UNIT=0`, `SAT_DIM=1`, `SAT_BASIS_SIZE=71`, and the complete basis between unique `SAT_BASIS_BEGIN/END` delimiters. Those four sentinel strings and both delimiters occur once. There are 71 nonempty one-line generators; every line except the last ends with `,`; the last does not. No `?` diagnostic and no `=FAIL` occur in that stdout. The `.meta` records `rc=0` and the stdout SHA `5e780dcf…`.

The V2 compiler SHA `7ede3c5a…` pins both full source and stdout SHAs and refuses any other value. It refuses missing or nonunique copies of `SOURCE_EQUIVALENCE=PASS`, `SAT_IS_UNIT=0`, `SAT_DIM=1`, `SAT_BASIS_SIZE=71`, `SAT_BASIS_BEGIN`, and `SAT_BASIS_END`. It requires exactly 71 nonempty one-line basis elements with the comma punctuation above. It splices those exact bytes into

```text
ideal JQ={basis};
```

Comparison of the extracted 38268-byte basis with `JQ` in every V2 prime, V2 `a6`, and V3 compiled input is byte identity (common SHA `98b5dbd2…`). The emitted prefix is the source text before `ideal Gtail=modStd(Itail);`, so the same `Itail` is present and `modSat` is not re-run; the seed is the frozen printed saturation, not a later rewrite.

Scalar normalization divides each `std(JQ)` generator by its leading coefficient after a nonzero-lead preflight. Nonzero scalar multiplication does not change the generated `Q`-ideal. V2/V3 check both directions:

```text
SEEDED_Q_RAW_TO_NORM=1
SEEDED_Q_NORM_TO_RAW=1
```

together with `SEEDED_Q_SELF_GB=1` (`JQ` reduces to zero against the normalized basis) and `SEEDED_Q_MONIC=1`. `SOURCE_TO_SEEDED_Q=1` is only `Itail subset (GQ)` and is not the equality argument. Equality `JQ = sat(Itail,r7)` is the byte-copy of the unique printed saturation basis. That basis is `modStd` of the full saturation, not a `minAssGTZ` factor, so `JQ` is not a guessed component of a reducible saturation. The later prime-lift then proves that this saturation is itself prime of dimension one.

---

## Attack 3 — endpoint custody, warnings, return codes

**CONFIRMED. V1 unused. V2 prime is only a near-pass. V3 repairs the charged warning and the missing raw-GB certificate.**

V1 is named only as an immutable non-decisive predecessor. No V1 hash is a premise. V2 prime input `28721c66…` is consumed by V3 as frozen source text, not as a theorem that the raw reduction is a Groebner basis. V2 stdout contains five lines

```text
// ** GQ is no standard basis
```

and does not print `QRED_RAW_IS_GB`. That is exactly the near-pass described in the target: elementwise scalar normalization clears Singular’s `isSB` flag, and the prime lane compares `std(GredRaw)` rather than `GredRaw` itself to the native special fibre.

Both V3 hosts consumed source archive `22337e03…` and emitted V3 input `97fc8dbe…`. Compiler output on both hosts records those pins. After replacing the host/job path, the two V3 stdouts are identical. Their 37 non-warning lines coincide, including every charged sentinel in target §2. The only differing lines are 221 copies of Singular’s `redefining seed_lc` warning, which embed the job path; those are not `?` diagnostics, not `=FAIL`, and not `GQ is no standard basis`.

Return-code custody is as claimed. No `<tag>.rc` exists in either V3 run directory or in the relevant V2 run directories. The wrapper `ops/aws_exact_lane.sh` writes the actual process status into each `.meta` as `rc=0`. The outer validator writes `engine_rc=0` and `validator=PASS`. Box03 used 192672 KiB RSS in 1:58.94 with zero swaps and exit status 0; r6d used 192084 KiB in 1:57.30 with zero swaps and exit status 0.

V3’s Q-side repair is the unique insertion, after scalar normalization,

```text
attrib(GQ,"isSB",1);
print("SEEDED_Q_SCALAR_NORMALIZATION_SB_MARK=1");
```

Nonzero scalar multiplication preserves a standard basis, so the mark is justified. The V3 validator requires that sentinel uniquely, requires `QRED_RAW_GB_CERTIFICATE=PASS` uniquely, and fails if any `GQ is no standard basis` line is present. Both hosts record `gq_not_standard_basis_warning_count=0`. That repairs the V2 Q-basis warning. It does not by itself prove the raw positive-characteristic list is a Groebner basis; that is Attack 4.

The V2 `a6` lane prints `A6_SAT_J_TO_J6=1` and `A6_SAT_J6_TO_J=1` with `SEEDED_A6_CERTIFICATE=PASS`. Its stdout contains six `GQ is no standard basis` lines. The proof below does not reduce against that unflagged scaled basis. V3 proves `J` prime and `a6 notin J`, which gives the same saturation equality formally. The V2 lane is an independent control.

---

## Attack 4 — raw good reduction

**CONFIRMED. Equality of the generated leading ideals, together with equality of the generated polynomial ideals, is exactly the Groebner criterion for `GredRaw`. The later reduction sentinel is a noncircular membership check, not the Groebner proof.**

V3, after `ideal Gred=std(GredRaw);`, computes

```text
ideal LrawCandidate=std(lead(GredRaw));
ideal LstdCandidate=std(lead(Gred));
```

and requires both reductions to vanish. By construction of `std`, `(GredRaw)=(Gred)` as polynomial ideals. For a global monomial order — here `dp` on both the characteristic-zero ring and `ring P=32003,(a0,...,a6),dp` — a finite list `G` is a Groebner basis of `I=(G)` if and only if `<LT(G)>=LT(I)`. The right-hand side equals `<LT(std(G))>`. Therefore

```text
<LT(GredRaw)> = <LT(Gred)>
```

is exactly the criterion that `GredRaw` is a Groebner basis. Standardizing the two monomial ideals and comparing them two ways is a correct test of that equality. Only after it passes does V3 set `attrib(GredCertified,"isSB",1)` on a copy of `GredRaw` and require `reduce(Gred,GredCertified)` to vanish. That last check is membership of a generating set of the same ideal; a false zero cannot be manufactured by the `isSB` flag, because a reported zero is an explicit combination. It is not used as the Groebner proof, so it is not circular. `lift_ok` includes both `qred_raw_is_gb` and `qred_marked_reduces_std` before the native comparisons.

Coefficientwise `imap` from the monic characteristic-zero basis succeeded with no `?` line. Every displayed denominator in the 71-element basis was checked against `32003`; none is divisible by `32003`. Monic leading terms therefore survive. Ring maps and term orders agree (`dp` on the same seven variables). Native ideal and initial-ideal comparisons are two-sided:

```text
QRED_TO_NATIVE=1
NATIVE_TO_QRED=1
QRED_LM_TO_NATIVE=1
NATIVE_LM_TO_QRED=1
```

and the raw sizes are `QRED_RAW_SIZE=71`, `QRED_STD_SIZE=71`. The native special fibre is itself obtained by `sat(Ip,ideal(r7p))` in characteristic `32003`, not by trusting the reduction blindly.

---

## Attack 5 — prime lift and dimension

**CONFIRMED, including in positive dimension, with `dim J=1` charged separately.**

`minAssGTZ(Jp)` returns one prime, and two-sided reduction against `std` of that prime vanishes. An ideal equal to a prime is prime. The special-fibre quotient is therefore a domain.

A monic Groebner basis over `V=Z_(32003)` gives unique remainders in `V[a]`. The standard monomials are a free `V`-basis of `A=V[a]/(G)`, so `A` is torsion-free. If two nonzero generic-fibre normal forms had product zero, dividing out the minimum `32003`-adic contents would produce two nonzero special-fibre classes with product zero, contradicting the special domain. This argument never uses relative dimension zero. Hence

```text
A_Q = Q[a0,...,a6]/J
```

is a domain. The sentinel `SEEDED_Q_DIM=1` is an independent exact-`Q` Krull-dimension computation, not an inference from the special fibre or from a one-dimensional `(a5,a6)` image.

`SEEDED_Q_A6_NONZERO=1` is `reduce(a6,GQ)!=0`, so `a6 notin J`. For a prime ideal this is equivalent to

```text
J:a6^infinity = J.
```

Constant extension `Q -> C` is flat, so multiplication by `a6` remains injective on `A_Q tensor C`. No geometric coefficient component is contained in `a6=0`. Isolated points of the chart complement are restored by completing the rational map. This is not inferred from the V2 `a6` control.

---

## Attack 6 — nonverticality

**CONFIRMED, and kept separate from `dim J=1`.**

The special-fibre contraction `eliminate(Jp,a0*a1*a2*a3*a4)` is one nonzero polynomial, irreducible over `F_32003` (`PLANE_SIZE=1`, `PLANE_FACTOR_COUNT=1`). The special `(s,t)=(a5,a6)` image is one-dimensional. The monic model is flat.

If both `s` and `t` were algebraic over `Q` in `A_Q`, primitive univariate relations would clear into `V[s]` and `V[t]`. Torsion-freeness lets one divide out the exact `p`-content. The reductions cannot be the zero polynomial, and cannot be a nonzero constant unless the special fibre is the zero ring, which it is not (`P_SAT_UNIT=0`). They would therefore make the special `(s,t)` image zero-dimensional, contradiction. Hence at least one of `s,t` is nonconstant on the generic fibre.

If both `q=s^2/t^3` and `v=t^8` were constant, then `t` would be algebraic over the constants and `s` would be algebraic over the constants. That is the previous case. The undefined chart `t=0` is excluded by `a6 notin J`. Nilpotents are excluded by the domain. Therefore `(q,v)`, and likewise `(q,y)` with `y=t^2`, is nonconstant. This does not use `dim J=1` except insofar as the ambient ring is already known not to be zero.

---

## Attack 7 — componentwise projection

**CONFIRMED.**

The plane certificate of SHA `9b0e08c2…` reconstructs the same polynomials `r1,...,r7` as the saturation source (the `poly r1=`, `poly r7=`, and `poly v11=` lines are byte-identical) and the same `Itail=r1,r2,r3,r4-1,r5,r6`, saturates by `r7`, and prints `EXACT_CANDIDATE_MEMBER=1` for the cleared substitution

```text
FCAND = (the numerator of P(a5^2/a6^3, a6^8)).
```

The Newton support of `P` makes that numerator already a polynomial: the most negative `a6`-exponent after substitution is zero. In `Frac(A_Q)`, where `a6!=0`, this is `P(q,v)=0`. `EXACT_RESIDUAL_FACTOR_COUNT=1` is `Q`-irreducibility of `P`. Geometric integrality of the residual plane, and genus one of the normalization of `P(q,y^4)=0`, are the already confirmed residual theorem and lemniscatic corollary. The `EXACT_NODE_BOUNDARY_CERTIFICATE=FAIL` line in that stdout is the known face-squarefree failure; it is not used here, and the genus review did not use it as a node proof.

`Q(P=0)` and `Frac(A_Q)` both have transcendence degree one, and `(q,v)` is nonconstant, so the inclusion of function fields is finite. After base change to `C`, characteristic zero implies that a domain of finite type is geometrically reduced, so `Frac(A_Q) tensor_Q C` is a product of fields. Geometric integrality of `P` keeps `C(P=0)` a field. Every factor is finite over that field, hence every geometric coefficient component dominates the residual plane. None is vertical.

Adjoining the actual function `y=a6^2` lands in `P(q,y^4)=0`. If `y` were constant on a factor then `v=y^4` would be constant, and irreducibility of `P` in both variables would force `q` algebraic, contradicting nonconstancy of `(q,v)`. The map of every geometric component to the geometrically irreducible normalization `Y` is therefore nonconstant. This is a function-field statement, not an inference from quotient irreducibility alone. Denominators of `q` are powers of `a6`, already inverted. There is no quotient/source mismatch: the coefficient scheme is the image in `a`-space, not `C/mu_4`.

---

## Attack 8 — actual source component

**CONFIRMED.**

A genuine source supplies coefficient functions `a_i in L(C)`. The identity `8 dR_7/dx=j/u!=0` makes `r_7`, a fixed polynomial in those coefficients, nonconstant on `C`. The image is therefore one-dimensional. It lies in `V(Itail)` and off `V(r_7)`, hence in `V(J)`. Each geometric component of `V(J)` is already one-dimensional, so the image closure of the irreducible source is a full geometric component, not a proper closed curve inside one. Attack 5 supplies `a6`-chart coverage of every such component.

---

## Attack 9 — descent and the genus-one contradiction

**CONFIRMED.**

Source audit `(4.1)` is the exact deck action on the ordinary coefficients: the generator sends `t |-> zeta t` and `z |-> zeta z`, and

```text
a_i = t^[-i]_e A_i(q)
```

with `A_i` invariant. For `e=4` this is `a_i |-> zeta^{-i} a_i`. Then

```text
q = a5^2/a6^3  |->  zeta^{-10}/zeta^{-18} q = zeta^8 q = q,
y = a6^2       |->  zeta^{-12} y = y.
```

The rational map `C --> Y` is `mu_4`-invariant, so it descends to the quotient `C/<deck mu_4> = P1_x`. The quotient map is finite, so nonconstancy is preserved. A rational map from a smooth complete curve to a projective curve extends across every pole.

The confirmed residual theorem gives that the complete normalization of `Y: P(q,y^4)=0` is a geometrically irreducible genus-one curve. The confirmed lemniscatic corollary identifies it with `Z_E^2=X_E^3-16 X_E`, `j=1728`, and exhibits the nonzero regular differential `dX_E/Z_E = -d xi/W`. The tangent erratum changes only a displayed cubic coefficient and none of the divisor, the genus, or this differential.

A nonconstant characteristic-zero morphism `P1 -> Y` is separable, so pullback of that differential is a nonzero regular differential on `P1`. But `H^0(P1, Omega^1)=0`. This is the contradiction.

---

## Attack 10 — scope

**CONFIRMED as written, and not enlarged.**

`mu_4=0` plus `mu_4!=0` exhaust exact order four for all `U`. The `mu_4!=0` obstruction is independent of `U` (coefficient-infinity plus a map from `P1_x`). The `mu_4=0` theorem is already stated for every `U>=1`. Nothing in the composition is an order-two theorem, an order-one theorem, emptiness of all `(8,12)`, a proof of maximum twelve, a counterexample, or JC2. Computational `PASS` tokens were not used as lemmas; the code that prints them was read.

---

## Strongest exact theorem that survives

Let `L` be a characteristic-zero constant field, enlarged harmlessly for geometric questions, and let `C` be the smooth projective normalization of an exact-order-four Kummer source `u^4=h(x)` with `C/<deck mu_4>=P1_x`. Let `f in L(C)[z]` be monic depressed of degree eight and let `g=F_12(f)`. Suppose the ordinary Faber tails are

```text
(r_1,...,r_7)=(0,0,0,mu_4,0,0,R_7)
```

with either `mu_4=0` or `mu_4!=0`, and with `8 dR_7/dx=j/u!=0`. Then no such source exists, for every `U`.

In particular, after weighted normalization `r_4=1` on the nonzero-load stratum, the frozen 71-element saturation

```text
J = ((r_1,r_2,r_3,r_4-1,r_5,r_6):r_7^infinity)
    subset Q[a_0,...,a_6]
```

is a prime ideal of dimension one, `a_6 notin J`, every geometric component of `V(J)` maps nonconstantly to the geometrically irreducible genus-one normalization of `P(q,y^4)=0` with `q=a_5^2/a_6^3` and `y=a_6^2`, and that map descends to `P1_x`, which is impossible. The `mu_4=0` stratum is empty by the unique Hall/Shioda order-four Davenport--Stothers triple and the divisor-19 contradiction.

This is a necessary-source theorem on the exact-order-four coefficient-infinity leaf of `(8,12)`. It does not address order two, order one, the rest of `(8,12)`, maximum twelve, or JC2.

---

## Return-code and warning ledger

| Endpoint | `.meta rc` | `engine_rc` | `<tag>.rc` | `? ` | `=FAIL` | `GQ is no standard basis` | other `// **` |
|---|---|---|---|---|---|---|---|
| saturation `modsatQ` box02 | 0 | (none; wrapper `rc=0`) | absent | 0 | 0 | 0 | 0 in stdout |
| V2 prime box03 | 0 | 0 | absent | 0 | 0 | 5 | `redefining seed_lc` |
| V2 `a6` r6d | 0 | 0 | absent | 0 | 0 | 6 | `redefining seed_lc` |
| V3 prime box03 | 0 | 0 | absent | 0 | 0 | 0 | 221 `redefining seed_lc` (loop variable) |
| V3 prime r6d | 0 | 0 | absent | 0 | 0 | 0 | 221 `redefining seed_lc`, path-only vs Box03 |

The V3 validator’s zero-warning check is specifically the `GQ is no standard basis` string. The remaining `redefining seed_lc` lines are Singular’s redeclaration of a loop variable; they are not a failed Groebner test and they do not differ between hosts except by job path.

---

## Findings, classified

**Fatal.** None.

**Repairable, non-blocking.**

1. Plane membership is a parallel `modSat` of the same `Itail`, not a `reduce(FCAND,GQ)` printed by V3. The `r1`, `r7`, and `v11` polynomials are byte-identical to the saturation source, the saturation specification is the same, and `P` vanishes on the unique dim-1 prime once equality of the two saturations is granted by that specification. A printed reduction of `FCAND` against the frozen 71-element `GQ` would close this identification inside the V3 stdout. It is not a guessed-component gap and does not break `P(q,v)=0` in `Frac(A_Q)`.

2. V3 still compares `Gred=std(GredRaw)` to the native special fibre rather than `GredCertified`. After Attack 4 this is the same pair of ideals and the same initial ideals. Harmless.

**Cosmetic.**

3. Two hundred twenty-one `redefining seed_lc` warnings per V3 host, from `number seed_lc=...` inside a `for` loop. Harmless, path-only across hosts.

4. The sentinel name `SEEDED_Q_SELF_GB` is `reduce(JQ,GQ)==0`, i.e. containment, not a Groebner self-test. The actual Groebner property on the `Q`-side is `GQraw=std(JQ)` plus two-sided scalar-normalization equality.

5. V2 `a6` finishes in under one recorded second. For a prime already known not to contain `a6` that is the expected cost of `sat`, not a skipped job.

---

## Smallest failing identity

None. The identities

```text
JQ = printed 71-element sat(Itail, r7),
<LT(GredRaw)> = <LT(std(GredRaw))>,
J prime of dimension 1 over Q, a6 notin J,
P(q,v)=0 in Frac(Q[a]/J),
q = a5^2/a6^3 and y=a6^2  are mu_4-invariant,
```

all hold of the frozen bytes and the hand algebra above. No source-component coverage, saturation-custody, or semantic endpoint gap remains.

CONFIRMED
