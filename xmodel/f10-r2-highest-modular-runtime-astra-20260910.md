# Highest modular runtime and one cofactor mutation

SOURCE ONLY / PROVISIONAL / UNEXECUTED. First action15:37:20.195126591UTC; fixed stop15:59:00UTC, reserve15:56:00UTC. Nine mandatory preflights and exactly four new phases are prospective only. Parent FIRST code gate was enqueued before this one dependent generation; its live body is not read. No worker, registration, actual baseline or certificate is accessed.

## Result

The owned package supplies dispatch.py, unchanged probe.py, independent mutate.py, CONTRACT.md, a disabled registration template, READ-SCOPE.md and14-input PINS.json. All code is UNEXECUTED; no syntax/import/AST/compile/test, mathematical subprocess or coefficient-body access occurred. This is a proposed runtime instrument, not runtime evidence or authority. ROOT's later lifecycle notice reports the unchanged three highest source files static-accepted; no extra gate file was read and this new package remains unreviewed.

The accepted caller's ROOT/EC2/boot/ns/cgroup/UID/clear-environment checks, exact SIXFIELD native inventory and actual mapped paths, unchanged CAPRUN, quiet boundaries and durable custody are retained. A separate exact three-sibling highest directory is added beside the original exact five-file science directory and new exact three-file wrapper. No original science source is changed. Nine preflights remain, including samePGID orphan RSS/TERM/KILL identity evidence. Then exactly highest-produce, highest-positive, mutate, highest-negative:13 calls total. There is no baseline positive producer/checker rerun.

## Meaningful mutation and failure

The mutator independently rebuilds literalP7, checks complete Psi rows, all seven coordinates/dense89-unit denominators/support/weights, fixed root and special boundary, then extracts F,G with original arithmetic.py only. It never imports the highest producer/checker. From cF+dG=1 it changes constantc by1mod89 if F is nonzero, otherwise constantd with G nonzero. The identity changes by that nonzero polynomial, hence at least one of eleven positions fails. Wrap88to0, empty arrays and canonical trimming are handled. Restoring the exact old array in the whole certificate must restore original canonical bytes.

The nineteen-entry mutation CLI uses the fixed root-frozen positive receipt path instead of an unknowable future candidate-SHA argv value. Exact receipt/root/source/baseline bindings and the candidate digest are checked before mathematical import. This resolves prospective argv binding while leaving ROOT_REGISTRATION_SHA256 as the sole typed expansion. ROOT authentication and immutable freeze remain mandatory; a matching digest alone grants no authority.

The caller only parses bounded metadata, never coefficient JSON. Candidate and producer receipt freeze before independent positive checking; positive receipt freezes before mutation; changed fixture and mutation receipt freeze before negative checking. Success requires the exact positive HIGHEST_SPECIAL_PROJECTIVE_CHECKED_NO_SOURCE_OUTCOME status, followed by NORMAL_EXIT1/no signal/empty stdout/no receipt and exactly check.py frames168,158,151 and final ValueError: CHECK FAILED: fixed-place full modular Bezout identity. One bounded constrained caret line per frame is allowed, not assumed absent. Extra/chained/parser/resource/native failures do not count.

## Caps and stop semantics

Joint600wall/500CPU/2GiB/16MiB remains unchanged. Nine preflights each wall5; three highest phases each120wall/99softCPU; mutation30wall/24softCPU. Allocated child wall435 and hardCPU361 leave overhead; these are finite allocations, not measured performance. Certificates are each at most2MiB, metadata at most1MiB. Caller admission/cgroupCPU/cleanup/durable boundaries remain. No retry, alternate prime, exact fallback or cap increase is implemented.

Producer INCONCLUSIVE_FIXED_PLACE normal2/no candidate ends after10 calls without any subsequent check/mutation. All unexpected outcomes stop nondecision. Only complete13-call success emits HIGHEST_CHECKED_NEGATIVE_CONFIRMED, always scienceNONE. There is no claim of actual regularity, basis, cyclic dimension, source zero/point or JC2. ROOT still owes a separately authenticated reviewed registration, exact13 argv, actual native closure including loaded ld.so.cache, live identity captures, group quiet, durable custody and independent intake.

## Evidence and remaining question

All14 current hashes matched before bodies. Eight prior exact-byte WHOLE reads were reused and six fresh WHOLE reads completed; READ-SCOPE records clipping and recovery. The full edited caller was read back and compared as text, not parsed or executed. Own mutator and documentary contracts were manually reread. No baseline/certificate bytes were read or generated locally. All old frozen inputs remain unchanged.

Raised canonical OPEN quantity: zero. The smallest remaining technical question is whether the new code/CLI/freeze composition passes its first independent static review; the cheapest next test is that documentary audit, not an unregistered run. Feasibility at89/0 and actual runtime remain unknown. No follow-on authority is requested or inferred.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5193`.
- Body SHA-256:
  `8737d3454fdf83a2075ff8b7b1743f8881dd26ff30222746f14a083dc836600a`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
