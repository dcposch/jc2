# One mixed-scalar resultant observer: static implementation

Owner /root/execution_reliability; ROOT collector. Actual first UTC
2026-09-13T07:31:08.966333443Z. Original reserve07:44UTC/HARD07:47UTC retained.
Implemented and personally STATIC-WHOLE-REVIEWED; NOT syntax-tested,
import-tested, runtime-tested or mathematically qualified. No AWS operation,
scientific/interpreter/AST/dummy execution, external-model call or new agent.
Only the unchanged administrative publication finalizer ran on HQ.

## Scope and frozen inputs

TASK f4114bcd514ea44c405d3e79c8f0d7780a9373d418e57a1d4f1e116873e6b749
was SHA-before-WHOLE read. All named input pins are reproduced in this task
box's PINS.sha256 and rechecked after implementation. Frozen APPROACHES was
read whole, using bounded follow-up reads where an initial display clipped.
Frozen COORDINATION517fca6f... uses TASK's expressly allowed exact-byte personal
previous-WHOLE reuse: the old whole policy plus the allocation-only delta had
already been personally read, and this exact current hash had been pinned.
Both named scientific reports and the unchanged job.sh/JOB.md were whole read.
The accepted rank-seven, unit-denominator, support/degree, nonzero-resultant and
exact cube-factor premises are imported only within TASK's stated scope; stale
historical producer headers do not change that accepted scope.

Only observer.py, DEPLOYMENT.md, job.DISABLED.json, PINS.sha256 and custody.json
in box/f10-mixed-resultant-observation-astra-20260913, plus this transactional
report and its finalizer metadata, are authored. No shared source/ledger,
previous sealed packet, legacy dirty fleet.sh/dispatch, protected tree/mirror,
user settings or other workload was touched. No existing output was overwritten.

## Implemented delta

The sole payload uses exact Python/Sympy polynomial arithmetic. It reconstructs
trunc_7(phi^s) by the literal multinomial falling-factorial formula and phi'/phi
by the coefficient recurrence for inverse phi, where phi=1+u+Xu^2+Yu^3.
The two truncations use exponents2t-1 and4-t. Their coefficient14 product
reconstructs B. Literal U,V,K give P0=3V^2+(t-2)((6X+t-3)VU+KU^2), then
termwise fifth-power clearance gives G=U^5 B(t,X,V/U).

One direct resultant Res_X(P0,G) over QQ[t] is requested. There is no content
extraction/reconstruction branch, gcdex, cofactor or engine retry. Exact R and
generic degree_X G=m are retained for possible later independent verification.
The literal divisor (3t-5)^34 is divided exactly, with full Q and remainder DATA
saved BEFORE factorization. Raw rational factors are saved BEFORE the complete
factor-product reconstruction. Only after that gate are linear roots mapped
by r=(2-t0)/(3t0-5) and integer r>=2 candidates emitted. All mathematical
coefficients, exponents, multiplicities and candidates are exact strings.
No floating discovery, executable certificate or assert-dependent gate occurs.

AWS DMI/exact-instance/HQ/tag/cgroup/credential/cwd/environment guards precede
the Sympy import and all output creation. The payload relies explicitly on the
unchanged runner's physical and effective resource/capability/descendant
checks. It creates unique mode directories and fixed exclusive checkpoint
names. Each complete JSON DATA file is bounded at7MiB, fsynced, published by a
no-overwrite hard link and directory-fsynced. Incomplete publication retains
the partial. Original exception tracebacks are printed first to durable phase
stderr; an error JSON is best effort. Runner logs, receipts and all partials
are retained on failure; absent output is never a successful empty result.

## Proposed bounded worker gate, not authority

DEPLOYMENT.md binds the future procedure to the tested immutable runner and
root-owned0444 source/manifest ancestry. job.DISABLED.json is intentionally
unrunnable: disabled schema, enabled=false, null bindings and UNBOUND arguments.
ROOT must create and pin a separately authorized real manifest after independent
execution review; the current packet does not allocate or launch anything.

The proposed budget is60seconds separately bounded worker dependency/negative
preflight plus ONE840second service containing synthetic smoke and observation;
total active<=900seconds, memory16GiB, CPU200%, Tasks32, per-file8MiB and the
existing5+5second runner cleanup/receipt grace. ROOT separately owns exact-ID
retirement, retained EBS, raw collection/hash-copy verification and any failed
collection recovery. No second mathematical attempt is selected.

Worker preflight must observe Python/version/hash, Sympy/package provenance and
hash inventory, actual exact ground/native backend identity and isolated import
availability. No package installation is assumed or authorized. Negative guard
controls require exact causal HQ/instance/cgroup refusals before scientific
import. The in-service synthetic smoke requires r=2 from (7t-12)(t^2+1), and
rejects a deliberately corrupted factor unit by the exact product-mismatch
cause. Neither smoke nor the old runner pilot validates the scientific
resultant. All proposed checks remain UNEXECUTED.

## Mathematical trust and remaining gap

Every eventual DATA document is OBSERVATION/UNREVIEWED. A factor-product check
does not independently prove factor irreducibility or the resultant identity.
Timeout during factoring may leave a useful complete Q checkpoint but no
candidate verdict. ROOT's prospective separate integer specialization/Bareiss
verification can use retained m and R; it is not implemented, authorized or
claimed here. No independent certificate, speedup measurement, scalar-unit
proof, source-zero, all-F10 or JC2 conclusion has been produced. This deliberately
selected observation could remove only the named finite-exception prerequisite
of the actual critical-coefficient comparison.

The source/template/contract are frozen for different-model review. This is a
completed STATIC implementation slice, not a successful scientific run. Final
custody records actual all-writers-idle UTC and postpin verification separately.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6061`.
- Body SHA-256:
  `f30784769104b74a519783107ba778b51e8eb4c37dbfb5f35daf60f2540f1cb7`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
