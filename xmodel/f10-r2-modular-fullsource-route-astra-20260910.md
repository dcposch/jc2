# r2: modular whole-source exclusion through a finite local model

NEW CONDITIONAL MATHEMATICAL RESULT, UNREVIEWED. First action 2026-09-10 14:49:49 UTC; fixed hard
cap 15:11:00 UTC, publication reserve 15:08:00 UTC, never reset. Exact
owned report, transaction and box were absent before the first read.
Basis: 0d39df3c9fd69c939a8420c54d03228b9077777d.

Exactly four ROOT-named files were hash-pinned before bodies. The properness
producer was UNREVIEWED at invitation; this is the authorized single
speculative child and consumes none of its assertions without the proof
audit below. The accepted cyclic producer
and FIRST gate are explicitly reused from their previously completed WHOLE
reads after matching their unchanged current hashes. The properness producer
and highest CONTRACT were freshly read WHOLE. Prior authorship is disclosed.
No old mod89 partial, actual coefficient packet, code or extra source is used.

## 1. Verdict and the exact sufficient condition

YES, conditionally: an authenticated EMPTY WHOLE special highest-projective
fibre permits a finite-field FULL unlocalized-source unit-ideal test. If that
second test succeeds, the generic guarded source is zero by a finite-module
argument. No rational basis, rational multiplication table or characteristic-
zero ideal-membership cofactors need be computed for this implication.
Characteristic-zero source provenance and coefficient-integrality checks are
still necessary; this is not permission to omit the original coefficient data.

The two positive certificates are logically different:

1. A complete special highest-projective emptiness certificate establishes
   finiteness of the INTEGRAL, unlocalized compatibility algebra.
2. A modular identity for 1 in its WHOLE eighteen-row ideal establishes
   zero special quotient. Nakayama then gives zero integral and generic
   quotient, and the accepted source cover/core gives zero guarded source.

The proposed 35-lift basis refinement and seven-dimensional cyclic block
refinement are also correct under these hypotheses. A modular membership
test for t0^7=L0^35 is NOT a substitute for step 2, even with special
highest regularity, length 35 and seven per cyclic class. Section 6 gives
a counterexample satisfying all those algebraic hypotheses.

Route F in the charged properness report supplies generic highest
regularity, not this special highest premise. It cannot alone justify the
new finite local model or lift a zero special full quotient. Section 7 gives
a weight-compatible counterexample where its eliminant is already F=1.

No actual place, coefficient-integrality audit, highest certificate, basis,
rank or modular full-unit certificate has been checked here. Therefore no
ACTUAL r2 source exclusion, point, regularity or dimension is asserted.

## 2. Precise rings and source interface

Use O for the coefficient ring to distinguish it from the source algebra.
Assume O is a local domain with maximal ideal m, residue FIELD k and fraction
field the accepted WHOLE B=Q[Z]/(P7). Fieldness is the accepted import in the
charged cyclic producer/gate; it is not inferred from degree seven. A
certified finite place of this field is sufficient. There is no ring map
from the entire characteristic-zero B to k; reduction is O -> k.

Let S_O=O[Y1,Y2,Y3], with weights 1,2,3. The exact actual polynomials

    f_h=Psihat_h(Y,1), H_h=Psihat_h(Y,0), h=5,6,7,

must have authenticated integral representatives in S_O, with H_h their
weight-h parts. Thus f_h-H_h has strictly smaller weight; for the actual
source every drop is a positive multiple of five. Write

    R_O=S_O/(f5,f6,f7).

The fifteen named additional polynomials g_j must likewise belong to S_O
and have the correct generic images from the accepted cyclic interface:

    K1_i(Y,1), i=1,...,6;
    K0_i(Y,1), i=1,...,8;
    L1-U0*L0, where L0=K0_0(Y,1), L1=K1_0(Y,1).

Let J_O be their ideal in R_O and Q_O=R_O/J_O. All fifteen slots are
retained, including zero or dependent rows; g15 is the actual MIXED row,
not L1 alone. Subscript k or B denotes the indicated scalar extension.
The coefficient-integrality hypothesis must cover f and g, not just H.
For this sufficient unlocalized-zero route, integrality of the mixed row
itself is enough: one need not separately reduce L0 if it is not used by
the modular certificate. Any modular t0 test additionally requires a
verified integral L0 and its correct fifth power.

The accepted generic cyclic-core identities are

    C_B=Q_B[L0^-1],
    E_B=(Q_B)_0[t0^-1], t0=L0^5,
    source_A_B = E_B[s]/(s^3-t0).

The last ring is free of rank three over E_B, and the accepted common
cover supplies the complete original nineteen-row source maps. In
particular Q_B=0 implies C_B=E_B=source_A_B=0. This sufficient implication
does not set s or L0 equal to one and deletes no low row. The two original
low rows remain represented by the mixed row and the accepted cover/core
relation. The original guard and full source reconstruction are consumed
only through that accepted exact interface.

## 3. Special highest regularity makes the local algebra finite

Assume the WHOLE special weighted projective scheme

    Proj k[Y1,Y2,Y3]/(Hbar5,Hbar6,Hbar7)

is empty, geometrically, not merely lacking k-rational points. Since these
forms have positive weights, their homogeneous ideal is (Y1,Y2,Y3)-primary.
The polynomial ring over k is Cohen--Macaulay; its three parameters form a
regular sequence. This works over every residue characteristic. In
particular none of the three reduced forms is zero. The Hilbert series is

    (1-t^5)(1-t^6)(1-t^7)/((1-t)(1-t^2)(1-t^3))
      = S5(t)*S7(t)*(1-t+t^2), S_n=1+...+t^(n-1).

It is a polynomial of degree 12 and value 35 at t=1. These are integer
Hilbert-series statements, not division by 5, 6 or 7 in k. Bad primes for
the ACTUAL source formula may still be excluded by integrality; the abstract
algebraic argument itself requires no such characteristic restriction.

Let M=S_O/(H5,H6,H7), with its ordinary weighted grading. Each M_n is a
finite O-module because only finitely many monomials have weight n. Tensor
and passage to that homogeneous quotient give

    M_n/m M_n = (k[Y]/(Hbar5,Hbar6,Hbar7))_n.

For every n>12 the right side is zero, so the finite-module Nakayama lemma
gives M_n=0. There is no application of Nakayama to an unproved finite total
module: it is applied separately to the already finite components. Thus
M is generated over O by the finitely many monomials of weight at most 12.

Now take any monomial b of weight n>12. The vanishing of M_n gives an
EXACT identity over O

    b=sum_h a_h H_h, with a_h homogeneous of weight n-h.

In R_O this becomes b=-sum_h a_h(f_h-H_h), a combination of monomials
of strictly smaller weight. Induction reduces all monomials to weight at
most 12. Hence R_O is finite over O, and so is Q_O. In the actual cyclic
situation each step lowers the weight by a multiple of five and preserves
the cyclic class. No actual reduction coefficients were computed here.

This also proves generic highest regularity without relying on the
parent's properness proof: M tensor B is finite, so the generic
homogeneous ideal is (Y1,Y2,Y3)-primary, and its three generators are a
regular sequence over the field B. Properness gives the same implication,
but is not an additional hidden hypothesis of this componentwise proof.

## 4. A full modular unit identity lifts zero; optional free basis

Suppose the independently checked modular certificate is

    1=sum_(h=5)^7 a_h fbar_h + sum_(j=1)^15 b_j gbar_j
       in k[Y1,Y2,Y3].                            (1)

This says Q_O/m Q_O=0. Section 3 has proved Q_O finite over the local
ring O. Nakayama therefore gives Q_O=0, including all nilpotents and
torsion. Tensoring with B and using section 2 proves zero generic guarded
source. No flatness, reducedness or rational coefficient lift of (1) is
needed for this inference. A modular certificate that only proves the
RADICAL contains all variables, or only emptiness after L0 inversion, is
not (1). A constant nonzero polynomial in the modular ideal is acceptable
after its checked field inverse; zero rows cause no problem.

The optional basis statement is stronger and also valid. Over either field
k or B, a regular highest sequence gives

    gr R = field[Y]/(H5,H6,H7), dim R=35.

This is the accepted filtered argument: homogenize the f_h with an extra
variable t of weight one; regularity of (t,F5,F6,F7), graded permutability,
and t-saturation identify the entire highest ideal with (H5,H6,H7).
Polynomial-ring Cohen--Macaulayness and this saturation proof work in every
characteristic, including those dividing five; no derivative or averaging
over roots of unity occurs. Thus both special and generic dimensions are
EXACTLY 35, not a generic upper bound.

Choose 35 monomials whose classes form a verified basis of the ACTUAL
R_k, with seven in each cyclic class. Their lifts give a map O^35 -> R_O.
Its cokernel is finite and reduces to zero; Nakayama makes it surjective.
After tensoring with B it is an isomorphism, because both dimensions are
35. Every vector in the kernel is therefore killed by a nonzero element
of the domain O. The kernel lies in the torsion-free module O^35, so it
is zero. Those same monomial lifts are an O-basis. No finite-generation
assumption on the kernel, nor an actual characteristic-zero basis test,
is used in this argument. A merely proposed or labelled monomial list is
not the required verified special basis.

## 5. Cyclic zero-quotient certificate: seven rows, not one nilpotence target

The actual f_h and all g_j are homogeneous modulo five. This gives direct
cyclic gradings over O, k and B; it does not use diagonalization or division
by the group order. Base change commutes with the direct components and
the homogeneous quotient. The three source cyclic classes are

    deg L0=1, deg U0=3, deg L1=4;

the mixed row has class 4. The classes of the first six g_j are
(3,2,1,0,4,3), of the next eight (0,4,3,2,1,0,4,3), and of the last 4.
The Hilbert series modulo t^5-1 is 7*(1+t+t^2+t^3+t^4). Consequently
each (R_k)_j has dimension seven; section 4 also gives seven free lifts
over O in each class.

For a row g_j of class d_j, projection of c*g_j to degree zero retains
exactly c_-d_j*g_j. Therefore

    (J_k)_0=sum_j (R_k)_(-d_j)*gbar_j.

With verified special cyclic bases this is the column space of a canonical
7-by-105 matrix over k: seven actual products for EACH of fifteen named
rows. Products and reductions must use the actual fbar_h, not only Hbar_h.
Zero or dependent slots are allowed. Over a residue extension these sizes
are over k; scalar expansion into its prime field is a separate encoding.

The sufficient and equivalent full-quotient conditions are

    rank_k(matrix)=7
      iff (J_k)_0=(R_k)_0
      iff 1 belongs to that column space
      iff Q_k=0.                                (2)

The third equivalence is legitimate because the space is the degree-zero
part of an IDEAL and 1 has degree zero. Thus an identity for the special
target 1 is enough; a full-rank minor is one convenient alternative proof.
An arbitrary single target, notably t0^7, has no such property. Checking a
nonzero 7-by-7 minor of correctly reconstructed columns certifies rank 7;
checking only a claimed numerical rank, or columns formed with a guessed
basis, does not. Positive certificate multipliers may be zero for unused
rows, but every used polynomial must map to its actual named source slot.
This does not authorize replacing the complete source ideal by an
unrelated truncated receiver.

Equation (1) is the smaller mathematical contract if a direct modular unit
identity is available: it needs no basis or full matrix at all. The matrix
form (2) is a finite way to seek or certify the same stronger sufficient
outcome. This report neither implements nor runs either instrument.

## 6. Sharp negative: modular nilpotence does not lift even with all finite bounds

Let O be a DVR with uniformizer pi. In this CHANGED weighted presentation
write x,y,z for variables of weights 1,2,3, and set

    R=O[x,y,z]/(x^5-pi, y^3+z^2, y^2*z),
    L0=x, t0=L0^5=pi, J=(0), with all fifteen slots explicitly zero.

The highest triple (x^5,y^3+z^2,y^2*z) has only the origin over every
field: x=0, then y^2*z=0, and y^3+z^2=0 kills the other coordinate.
Thus it satisfies special highest regularity. The preceding proof gives
R free of rank 35 over O and rank seven in every cyclic component.

Nevertheless, modulo pi, t0=0 and L0^35=0 belongs to (J_k)_0. The special
localized quotient is zero. Generically x^5=pi is a unit, so x is a unit
and R_B[x^-1]=R_B is NONZERO: over an algebraic closure take y=z=0 and
any fifth root of pi for x. The localization R[x^-1] inverts pi and cannot
be a nonzero finite O-module; if it were finite, pi*M=M and Nakayama
would force M=0. It is not zero, as its generic fibre shows.

This refutes lifting modular t0-nilpotence or modular L0^35 membership even
after proving the full special highest premise, dimension 35 and the
seven-dimensional blocks. It retains cyclic degree one for L0, nilpotents,
and the correct three weights. It is NOT an actual r2 source point or a
substitution into the fixed source formulas.

Conversely replace x^5-pi by x^5. The same regular highest triple gives a
nonzero unlocalized special quotient, while x is nilpotent even generically,
so localization at x is zero. The proposed full-unit test then fails even
though the guarded quotient is zero. It is a stronger SUFFICIENT test,
not a complete decision method for the original localization problem.

## 7. Sharp negative: Route F plus generic regularity is not enough

Again take a DVR O with uniformizer pi and variable weights 1,2,3. Set

    H5=x^5, H6=pi*y^3+z^2, H7=y^2*z,
    f5=x^5-1, f6=pi*y^3+z^2-x, f7=y^2*z,
    J=(z), placing z in one class-3 slot and zeros in the other fourteen;
    L0=x.

The generic highest triple is regular: x=0, y^2*z=0 and pi*y^3+z^2=0
force y=z=0 because pi is a generic unit. On the chart x=1, P=1,Q=0,
B0=1,C1=0, so the charged denominator-free eliminants are F=1,G=0.
Route F succeeds, even as a literal integral unit identity. Generic infinity
is also empty. But the SPECIAL highest boundary contains [0:1:0], so Route P
has not succeeded.

The full special quotient with J is zero: z=0 makes f6=-x, while f5=x^5-1
then contradicts x=0. The generic quotient is nonzero: z=0,x=1 and
y^3=pi^-1 give a geometric point. It remains nonzero after L0 inversion.
Algebraically Q=O[x,y]/(x^5-1,pi*y^3-x) makes x and then pi units, so Q
cannot be finite over O. This is exactly the missing hypothesis in an
attempt to apply Nakayama from generic regularity alone.

All three f_h have the required top weights and cyclic classes, and the
chosen additional row has a legitimate cyclic class. They are a changed
algebraic control, not reconstructed actual source coefficients. The
counterexample is to the proposed inference without special finiteness,
not to the accepted source maps or to Route F's genuine generic conclusion.

## 8. Minimal prospective certificate and exact remaining obligations

The following is a sufficient proof packet, not an execution specification:

- Authenticate a single integral local model O with fraction field the
  accepted FIELD B and specified residue field k. A prime/root or residue
  extension, every required denominator, and every equality identifying the
  model with the frozen generic polynomials must be verified. Direct dense
  coordinate reduction and a locally integral replacement are different
  contracts; the latter needs exact equality in B and unit denominators.
- Authenticate ALL three actual f_h and all fifteen g_j in that model,
  checking supports, weights and the actual mixed low row before projection.
  Highest-row integrality alone does not certify lower-row integrality.
  Multiplying a row by a nonunit is not a harmless special-fibre change;
  any altered integral presentation needs its own exact generic identity
  and a certificate for its literal new special fibre.
- Certify the WHOLE special highest projective fibre empty. One sufficient
  form from the charged CONTRACT is cF+dG=1 in k[u], verified by all
  coefficients through its denominator-free identities, PLUS the same-model
  special boundary coefficients alpha,beta,gamma nonzero. The latter are
  the coefficients of Y2Y3 in H5, Y2^3 and Y3^2 in H6 on Y1=0. The
  generic boundary theorem cannot replace these special checks. Constants,
  Q=0, zero slots and degree drops remain covered by the identities.
- Supply and independently verify the full modular unit identity (1), or
  the verified actual special basis/products and the full-span certificate
  (2). A nonzero constant must have its inverse checked. Modular nilpotence
  or membership of L0^35 alone is explicitly insufficient.

The algebraic theorem then supplies the finite local lift and accepted
generic source-zero implication. It computes no rational basis or rational
membership witness, although it proves they exist where needed. There is
no measured runtime or speedup claim; denominator/provenance work may still
be substantial. The existing highest-chart instrument alone does not check
this larger packet and its no-source-outcome receipt is not upgraded here.

Precise cheapest success discriminator AFTER the complete highest/model
prerequisites is whether 1 belongs to the whole modular eighteen-row ideal;
in the verified cyclic matrix formulation it is rank 7, not membership of
a nilpotence target. Its value, the place, all actual coefficients and
integrality, the special basis and matrix rank are UNKNOWN. Failure is
inconclusive for the guarded source by section 6.

## 9. Read scope and publication boundary

Exactly four inputs are charged: the modular-properness producer, cyclic
producer, FIRST cyclic gate and highest-bezout CONTRACT named by ROOT.
All four hashes matched before bodies. The first and last were read WHOLE
freshly; the two unchanged cyclic reports reuse their previously completed
WHOLE reads, explicitly recorded above. No current review, old partial,
coefficient body, linked provenance, corpus or baseline was consulted.

The load-bearing local finiteness, basis lift, source composition and
countercontrols are proved here. ROOT subsequently attested that the
properness parent's FIRST gate was collected and it was conditionally
promoted as 17zzv; this is lifecycle context only. No new gate body was
read, no extra proof premise was imported, and the four charged inputs
remain unchanged. ROOT's mathematical suggestion was a proposal to audit.
The accepted cyclic core identities are consumed at their exact scope;
their dimension bounds remain conditional until an actual highest
certificate exists. No changed control is claimed to describe the actual
source.

All authoring is apply_patch in the exact owned report/box and the existing
documentary transaction. No scientific subprocess of any size, code,
implementation, runtime, network, process inspection or other lane was used.
No child, gate, registration, source computation or follow-on is launched.
Own WHOLE/OPEN/collision review, current pins and terminal custody precede
the final handshake. The fixed deadline is not reset by completion time.

## OPEN(S) RAISED

None new. The exact assigned quantity, after an authenticated full-special
highest/model certificate, is whether 1 lies in the whole reduced
eighteen-row ideal, equivalently rank seven of the verified cyclic matrix.
Actual model and certificate data are unknown. No execution, review,
research lane or follow-on authorization.

## COLLISIONS

status: EMPTY. Exact own destinations absent; own-only check, no corpus scan.

Own WHOLE body review and all four repeated input pins completed after
15:00:45 UTC. The lifecycle wording above was then corrected and reread.
All mathematical work is complete; only documentary sealing and custody
remain, before the original fixed cap.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `20016`.
- Body SHA-256:
  `e7b5cabc181315f2f9c3ea5a7245fa981466b2b9c6ba924efbcfd470266fd875`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
