# Independent hostile F10 all-parameter SOURCE gate (Fable 5.1)

tag=f10-all-parameters-source-gate-fable5-20260909
frozen_basis=0d39df3c9fd69c939a8420c54d03228b9077777d (provenance only)
reviewer=Claude Fable 5.1 (claude-fable-5-1)
launch=root actual invitation 09:14 UTC (prep superseded); lane clock opened 09:14:27 UTC; hard stop = earlier of 09:45 UTC and 09:39 UTC, i.e. 09:39 UTC
subprocesses=ZERO mathematical subprocesses. Only date, ls, wc, cat/sed, grep, sha256sum, mkdir and this report write were run. All algebra below is hand-derived factored prose.

## 0. Custody and read scope

All ten charged files under /tmp/jc2-lane.Hn7c5G/inputs were read WHOLE (the GGHV excerpt in three sed windows covering lines 1-439). Their sha256 values are recorded in the box PINS file. The producer body hash 5b2bb27a... and file hash cc6edcb7... match the charged transaction; the accepted F9 review hashes f33820d2..., matching the producer's own pin. The excerpt headers charge these literal intervals: GGV 1401.1784v3 lines 100-405, 680-751, 1870-2113; GGHV 1708.07936v1 lines 30-100, 155-205, 640-820, 1184-1240, 1350-1415; lower paper 1605.09430v2 lines 1-307. GGV lines 2114-2182 (Theorem 7.6) are NOT in my packet; the producer does not consume 7.6 (it derives the Euler endpoint directly), so nothing below relies on it. No PINS source path, no peer report, no live F2/F9/F10 gate, no old checker and no original follow-through was opened. The producer is treated as PROVISIONAL / UNREVIEWED; the accepted F9 review was used only to identify imported theorem scope.

Imported at statement trust where the primaries cite them and my packet omits the interior: [8, Prop 2.1(2)] inside Prop 2.1(2a); [8, Lemma 2.2] inside Theorem 2.6; Remarks 2.10, 3.1, 3.3, Prop 3.7, Cor 3.6 inside the printed Prop 7.1 proof; the "Mimic" interiors of Prop 7.3 and Cor 7.4; GGHV Prop 2.5, Remarks 2.14, 2.22, [1, Props 5.13-5.18] inside Theorem 2.20's proof; GGHV (3.17)/(3.18) (the table orientation convention, cited but not printed in the packet).

## 1. Target A: actual source and arithmetic orientation. CONFIRMED.

Printed row (GGHV table, lines 1350-1415): F10, A0=(7,21), A0'=(1,0), A1=(13 wr 7,3), k=1, m=5j+7, n=3j+4. The parameterization at lines 1184-1240 runs over j in N0 (formula (3.20)), so q=0 (m=7, n=4) is included. 3(5q+7)-5(3q+4)=1, so m,n are coprime and m>n>1 for every q>=0. The table caption says only the (3.17) cases are printed and the (3.18) cases arise by swapping m and n. The producer fixes the printed orientation as an explicit hypothesis and makes no swap; the swapped family is simply outside the stated theorem. F11 shares the chain but has k=2 and m=j+2, n=3j+5; the producer distinguishes it and uses neither k nor the family formula anywhere except through coprimality, so the label is a scope restriction, not a premise. No table-to-existence inference is made: the theorem is conditional on a hypothetical pair.

Face and common root, rederived. With A0'=(1/m) st_{7,-2}(P0) and A0=(1/m) en_{7,-2}(P0) as chain data, v_{7,-2}(P0)=7m and the face is the segment from (m,0) to (7m,21m); likewise 7n for Q0. Expected bracket weight 7m+7n-5>0 exceeds the constant bracket's weight 0, so Prop 1.13 gives commuting faces. Prop 2.1(2b) with tau=7m, mu=7n, gcd(m,n)=1 gives ordinary R (last clause, P0,Q0 in L) with l(P0)=alpha R^m, l(Q0)=beta R^n. Ordinary monomials of weight 7 under (7,-2) are u, u^3v^7, u^5v^14, u^7v^21, so R=u r(z), z=u^2v^7, deg r<=3; st/en additivity (Remark 1.9(4),(5)) forces st(R)=(1,0) and en(R)=(7,21), hence r cubic with r(0)!=0, monic after a scalar in k. Both endpoints are chain hypotheses, not derived from bare standardness.

Triple classification, rederived. Theorem 2.20(8), formula (2.5), with l0=1, k0=m: A1=(1,0)+(m_lambda/m)(2/7,1). A1=(13/7,3) forces m_lambda=3m and checks 1+6/7=13/7. p0(z)=alpha r(z^7)^m in z=u^{2/7}v. For lambda!=0 the root lambda of z^7-a0 (a0=lambda^7) is simple in characteristic zero, so the multiplicity of lambda in r(z^7) equals the multiplicity of a0 in r. Hence 3m=m·mult, mult=3, and r=(z-a0)^3 with a0!=0; a0=-(quadratic coefficient)/3 lies in k. Q0 carries the same root with multiplicity 3n. No F9 double/simple profile is reused.

## 2. Target B: common centering. CONFIRMED.

Theorem 2.6 at (0,1): (0,1) in V_{>0}, v_{0,1}(P0)=21m>0, constant bracket, clause (1) gives ordinary F of (0,1)-degree 1, so F=v h(u). Hand bracket: [v h, f v^{21m}]=v^{21m}(21m h' f - h f'), giving 21m h'f - hf' = f. Cases: deg h=d>=2 puts a nonzero coefficient alpha H_d(21md-7m) at degree d+7m-1>7m against degree 7m on the right; d=0 leaves degree 7m-1 on the left; h=0 is impossible. So h=A0 u+B0, top coefficients give 14m A0=1, and (u+r0) f'=7m f with r0=B0/A0 in k, whence f=alpha(u+r0)^{7m}. The top (0,1)-component of the constant bracket, v^{21m+21n-1}(21n f' hQ - 21m f hQ'), vanishes, so hQ'/hQ=7n/(u+r0) and hQ=beta(u+r0)^{7n}, the SAME shift. The substitution u->u-r0 sends u^i v^j to (u-r0)^i v^j, never raising u-exponents, so both rectangles, both corners and the corner coefficients are preserved and every changed term has strictly lower (7,-2)-weight; the whole (7,-2)-face is literally unchanged, hence a0 is unchanged. Purity: a term u^{7e}v^j needs 49e-2j<=7e, so j>=21e, and the rectangle gives j=21e; the shift creates no new u^{7e} terms. Field scope: r0 is a coefficient ratio in k, no extension.

## 3. Target C: genuine triple Euler polynomial and denominator 7. CONFIRMED.

Theorem 2.6 at (7,-2): 7-2=5>0, v=7m>0, clause (1) gives ordinary E of weight 5 with [E, alpha R^m]=alpha R^m, hence [mE,R]=R in the domain L. Weight-5 ordinary monomials solve 7i-2j=5, i.e. u^{1+2s}v^{1+7s}; negative j is impossible for i>=0, so even in the Laurent ring the form is uv f0(z) with f0 a polynomial. With z_u=2z/u, z_v=7z/v I recomputed [uv f0, u r]=u(5z r' f0 - r f0 - 7z r f0'), so (1) is exact. For r=(z-a0)^3, dividing by r gives (2). Multiplying (2) by (z-a0): (14z+a0)f0 - 7z(z-a0)f0' = z-a0; the degree-(d+1) coefficient is 7c_d(2-d), so deg f0<=2. Solving the general quadratic c2 z^2+c1 z+c0 directly, without presupposing the pole argument: a0 c0=-a0, 14c0+8a0c1=1, 7c1+15a0c2=0, giving c0=-1, c1=15/(8a0), c2=-7/(8a0^2), which is exactly (3), and f0(a0)=0 follows. All divisions are by a0, 7, 8, 15 in characteristic zero. The producer's ansatz route gives (7B+8a0A)z+a0B, which I reproduce. So E has support (1,1),(3,8),(5,15); by Remark 1.8, en_{7,-2}(E)=(5,15)=(5/7)(7,21), consistent with Theorem 2.6(2),(3) (st(E)=(1,1), en(E) aligned with (7m,21m)).

Swap (u,v)=(y,x): brackets negate, so -E_swapped is Theorem 2.6's element for the swapped pair; its start under (-2,7) is (15,5)=(5/7)(21,7), so p/q=5/7 and q=7. Uniqueness: if two homogeneous weight-5 solutions differ by Z!=0 then [Z,R0]=0 with R0=y(x^7y^2-a0)^3 of weight 7, and Prop 2.1(2a) with (m',n')=(5,7) gives Z^7 = const·R0^5; ord_y R0=1 gives 7 ord_y Z=5, impossible in k[x^{±1}][y]. So the element in Cor 7.4 is the constructed one, not an arbitrary solution, before and after the Laurent cut (the d0-face is unchanged by psi). Endpoint (5,15) confirmed.

## 4. Target D: whole positive interval and integer gap. CONFIRMED.

After shift and swap, C_e=(21e,7e) is the unique max-x, unique max-y and unique max-total point of each member, so the v_{1,1} and v_{0,1} ratios are m/n, exactly the mirrored ratios Cor 7.4 uses. The next edge at C_e going clockwise from d0=(-2,7) has normal strictly past (1,0) and before (0,-1), so rho>0>sigma; taking the nearest in the union of the two polygons, both members have the C_e monomial strictly between d0 and d and both faces contain C_e at d. Some member has an axis term x^k, k>=1 (else both x-derivatives vanish on y=0 and the constant bracket is 0), so its d-value 7e(3rho+sigma) is positive, hence 3rho+sigma>0 for both. Sector: d x d0 = 7rho+2sigma > rho > 0 since sigma>-3rho, so the arc from d to d0 is under a half-circle and 3rho'+sigma'>0 on all of it; positivity holds for every direction in [d,d0] as Prop 7.3's minimum requires. Expected bracket weight (4) equals (7(m+n)-1)(3rho+sigma)+2rho>0, so the d-faces commute; Prop 2.1(2b) with positive proportional weights makes both powers of one R, so nonmonomiality of either forces d in Dir(P) and Dir(Q). Every Cor 7.4 hypothesis is literal: l=1, constant bracket, ratios m/n, coprime m,n>1, d0 in V_{>=0} and Dir(P) with value 7e>0, (1/m)st=(21,7) in Z x N, 7<21, st(F)=(5/7)(21,7). Conclusion l_d(P)=R1^{7m}. Minimum x-exponent is additive, so R1 is ordinary; y-degree 7m of the face gives y-degree 1; homogeneity allows one term per y-level; nonmonomial gives R1=a1x^3y+b1x^{i0}, and 3rho+sigma=rho i0 with gcd 1 and rho>0 forces rho=1, sigma=i0-3=-k, k integer: derived, not assumed. 0<k<3 from sign and positivity; lower Cor 1.6 (stated for any Jacobian pair in L, no minimality) forbids the slope-1 edge k=1; the lower Prop 2.1 remark on 0<k<1 is redundant but correctly applied (there en_d(P)=C_e). So k=2 exactly. The same lambda for Q follows from the common R at d and unique factorization; lambda=-b1/a1 is recovered in k from the second face coefficient.

Next edge and rational slopes. psi(y)=y+lambda x^{-2} maps (i,j) to (i-2t,j-t), preserving i-2j and lowering the d0-weight by 3t, so the corners stay unique maxima, the d0-face with its endpoint (0,e) persists, and both (1,-2)-faces collapse to their corner monomials because psi is graded for (1,-2). The axis term x^k persists and is not collinear with (0,e),(21e,7e), so the polygon is two-dimensional and the next clockwise edge at C_e exists with rho'>0>sigma', kappa>2. For 2<kappa<3 the same positivity, sector and (4) arguments hold in L^(1), Cor 7.4 applies with the persisting q=7, and the y-linear root a'x^3y+b'x^{i0} with i0 in Z forces rho'=1 and integral kappa, contradiction. The boundary kappa=3 has d'-value 0 and is not passed through any corollary; kappa>=3 alone yields i-21e<=kappa(j-7e)<=3(j-7e), i.e. i<=3j, for both members (the non-edge member has the C_e monomial there).

## 5. Target E: full-support map and both attained faces. CONFIRMED.

Pre-psi bounds: i,j>=0, -2i+7j<=7e, so i-2j>=3i/7-2e>=-2e with equality only at (0,e). Post-psi: j>=0, i<=3j, -2i+7j<=7e, i-2j>=-2e, all four checked above including terms with negative x-exponent (they still satisfy the d0 and i-2j bounds). x=g^{-1}, y=g^3p sends (i,j) to (3j-i,j); the four inequalities become I,J>=0, 2I+J<=7e, I-J<=2e, so the entire outputs are ordinary with support in e·Delta, not merely their top forms. Composite (6): u=g^3p+lambda g^2-r0, v=g^{-1}; u_p=g^3, v_g=-g^{-2}, v_p=0, determinant +g; product of factor determinants 1·(-1)·1·(-g)=g agrees; bracket c0·g. The map is graded from (-2,7) to (2,1), so the upper face is alpha Hbar^m with Hbar=g^3p(p^2/g-a0)^3=p(p^2-a0g)^3. The (1,-1)-face collects exactly the terms with i-2j=-2e, which are the psi-images of the single term (0,e) with coefficient alpha r(0)^e=alpha(-a0^3)^e; no other source term contributes and no cancellation is possible, giving (7): alpha(-a0^3)^e g^{2e}(gp+lambda)^e with all e+1 binomial terms nonzero. Attained vertices: (0,7e) from C_e with coefficient alpha; (3e,e) from (0,e), t=0; (2e,0) with coefficient proportional to lambda^e!=0. I+J<=7e-I gives the unique total leader at (0,7e); I<=3e from adding the two bounds, attained only at (3e,e). Plus sign: with y-lambda x^{-2} the face keeps (7e,0), violating i<=3j and giving g^{-7e}; the producer's control is right. No source coefficient is removed; the origin is only optionally added by a constant.

## 6. Target F: normalization, finite fields and cover. CONFIRMED.

mu^3=a0 lambda (both nonzero, so mu!=0), kappa0=lambda/mu. Hbar(kappa0 g, mu p)=mu^7 p(p^2-(a0 kappa0/mu^2)g)^3 and a0kappa0/mu^2=a0lambda/mu^3=1. Lower face: kappa0^{2e}(kappa0 mu)^e g^{2e}(gp+lambda/(kappa0 mu))^e with lambda/(kappa0 mu)=1; scalar (-a0^3)^e kappa0^{3e} mu^e/mu^{7e}=(-a0^3 lambda^3/mu^9)^e=(-1)^e. Dividing A by alpha mu^{7m} and B by beta mu^{7n} normalizes BOTH faces of BOTH outputs simultaneously and makes the total leaders p^{7m}, p^{7n}. Bracket: c=c0 kappa0^2 mu/(alpha beta mu^{7(m+n)}), nonzero, not set to 1, e=m and n both carried. Only mu may need an extension: r0, a0, lambda, alpha, beta, c0 are coefficient ratios in k, so k'=k(mu) is finite; Cor 7.4's own root extraction (gamma^{qm}=gamma2 in the 7.2 template) affects only the shape argument, and the used scalars are recovered in k. Cover (g,p)=(a^2,b): weight 2I+J becomes total degree, top K=b(b^2-a^2)^3 with one simple and two triple lines, Jacobian of the cover 2a, bracket 2c·a^3, orientation retained; output swap would negate c and exchange m,n, and none is made. The inverse of (6) is rational with determinant +v, so no receiver-to-source lift, point, exclusion, degree selection, maximality or JC2 conclusion follows, exactly as the producer disclaims.

## 7. Verdicts, strongest conclusion and read scope

A CONFIRMED, B CONFIRMED, C CONFIRMED, D CONFIRMED, E CONFIRMED, F CONFIRMED, each at the imported-statement tier named in Section 0 (Theorem 2.6, Prop 1.13, Prop 2.1, Cor 7.4 with printed "Mimic" interior, Theorem 2.20(8), lower Cor 1.6). No local gap found; no concrete unsupported hypothesis found. Strongest exact conditional conclusion: for every q>=0, m=5q+7, n=3q+4, every ordinary characteristic-zero pair with nonzero constant bracket, supports in [0,7e]x[0,21e] with attained corners, and actual (7,-2) starting face from m(1,0) to m(7,21) whose selected child is (13/7,3), yields over a finite extension ordinary A,B with (2,1)-faces H^m,H^n, H=p(p^2-g)^3, (1,-1)-faces (-1)^e g^{2e}(gp+1)^e, supports in e·Delta with e(2,0),e(3,1),e(0,7) attained, degrees 7e, g-degrees 3e, bracket c·g with c!=0, and cover tops K^e with bracket 2c·a^3. Scope notes, not gaps: the family formula enters only through coprimality, so the printed orientation and k=1 are labels of the hypothesis; the (3.18) swapped orientation is not claimed; GGHV (3.17)/(3.18) and GGV Theorem 7.6 are outside the packet and unused. No seal, no charge_basis, no exit price, no promotion; all writers IDLE after this write.

## OPEN(S) RAISED

None.

## COLLISIONS

status: EMPTY

- NONE — own-only check, no corpus scan.

<!-- BODY-END -->
