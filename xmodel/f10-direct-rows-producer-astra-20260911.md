# Independent direct-row producer — STATIC / UNEXECUTED

First action 2026-09-11 03:11:41 UTC. Original publication reserve 03:36 / hard 03:39 UTC unchanged. Owner /root/model_productivity; basis 0d39df3c9fd69c939a8420c54d03228b9077777d. Scope is the accepted corrected direct circuit and ROWS-ONLY projection, not rational baseline reconstruction or canonical ideal equality.

This report will distinguish completed source from any exact remaining gap. No scientific execution, import, syntax/AST/compile/test, payload, place selection, network, worker or peer-source access is permitted or performed.

## Outcome

COMPLETE STATIC PRODUCER CANDIDATE, UNEXECUTED and awaiting combined independent source review. Owned scientific files are authority.py, arithmetic.py, produce.py. The checker author owns a separate arithmetic/checker implementation and has not supplied any body to this lane. There is no runtime, rank source, matrix, actual place, coefficient artifact, fixture, registration or source outcome here.

The implementation retains the complete corrected operation graph while serializing only the six-key ROWS-ONLY artifact. This is not the earlier rational reconstruction object and cannot qualify as its baseline. Accepted24z/25z provide conditional source/projection scopes; a successful later residue evaluation would not prove a generic polynomial identity or an equality of unsaturated ideals. No positive execution or test is claimed.

## Producer and independent interface

The metadata-only helper returns exactly artifact_path,contract_sha256,source_pins,place under the fixed eleven-key ROOT record and old nine-argv convention. It checks the actual installed entry against produce/check mode, refuses optimized interpreters before scientific imports, authenticates the five installed source files and supplied interpreter/native subset, and enforces the fixed prospective limit record. ROOT's approved clarification uses each installed absolute source path as a source_pins key matching files[basename]; other references remain ROOT-defined. No field or polynomial function resides in authority.py. The native subset and byte hash checks are not provenance, native closure, cgroup supervision or self-issued authority.

Producer arithmetic separately verifies the one supplied prime, constant-first monic residue factor of degree1..7 and actual literal P7_mod_p at tau3/10. It uses exact finite-field arithmetic, deterministic labeled scalar inverses and explicit typed sparse polynomial records. The fixed D0/D1/D2 registry checks every named denominator even when a later correction happens to vanish. All15 actual scalar inverse inputs/products are retained. No favorable factor or prime is chosen, including no hardcoded proposed-place value.

Formal A/B bands are recursively constructed as a sequence distinct from the installed A/Euler B sequence. All mixed forcing, modified gap4 target, gap7 y=z-U*d0 and unchanged Lambda, gap8 fixed U²theta³ mate, gap10 variation and c1-first late selection are literal. Six completed matrices use determinant/adjugate inverses with both products checked. Installation is fixed by the table, retains (5,2),(6,2) discarded coefficients and does not rewrite the formal bands. Dformal/Vformal use typed Laurent-S shifts, never coefficient-field S inversions or replacement forcing.

The separate Euler sequence visits every j/i index, recording both resonances while fixing their entire polynomial coefficients to zero. Full J and both Delta terms produce all108 positions,45 low entries and63 upper entries. All22 readbacks are computed against the correct zero/compatibility values. Both full inverse charts and both pole subrecords are computed. No low-row or inverse-polynomiality hypothesis is silently imposed beyond the adopted partition.

All150 labeled diagnostics exist, with111 required canonical-anomaly zeros and39 allowed alternative records. Both pole subrecords may be nonzero. The six SCALE comparison groups are explicit constructions, including21/24 low DIFFERENCES rather than raw low source rows. Original leading guard operands carry the correct s^-3/s^-5 factors; no s=1 or z=1 specialization occurs. The partition is applied after complete internal construction and before output. The25 graph slots, five extra polynomials and two scalars use the exact labels, weights and source-localized guard identity.

## Source and wire controls

The only serialized object is schema f10-r3-direct-rows/v1, job f10-source-cone-r3-direct-rows-v1, ROOT-bound contract/source/place and graph. Canonical ASCII JSON has sorted keys, compact separators, one newline, decimal-string residues/exponents, dense length-f field elements, numerical lexicographic sparse order, exact homogeneous supports and retained zero/duplicate slots. There are no producer diagnostics or matrix claims for the checker to trust.

The producer publishes a complete fresh file by fsynced temporary/readback and no-overwrite hard link, then emits only FORMED_UNCHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME. A resource, anomaly, wrong place, denominator, inverse, type or publication failure is STOP/NONDECISION; any leftover file is unaccepted. The prospective fixed900wall/600CPU/8GiB/128MiB limit fields do not themselves enforce runtime caps. ROOT still owns frozen startup/native/physical/cgroup/input-postpin/quiet/durable evidence.

Exactly one source semantic control remains SPEC/NOT_RUN: T+X1^27, all other fields preserved, canonical inverse byte restoration and fresh ROOT binding. Under a genuine positive artifact the independent checker must reach exact stderr CHECK FAILED: direct-source graph slot T plus newline, normal exit2 and empty stdout. Parser/hash/resource/authority failures earn no semantic credit. This lane neither executed nor implemented the peer checker or a mutator; reachability and independence remain combined-FIRST review duties, not presumed test results.

## Read scope, limitations and handoff

All12 inputs were freshly pinned and read WHOLE; no prior-turn reuse. Aggregate display clipping was recovered through individual whole rereads of affected ASSERTIONS/WIRE/rows-proposal text and a separate full gate reread. READ-SCOPE and PINS record exact paths, hashes and117520 input bytes. Only already charged source text was used for transcription. All new source and documentation were manually read whole; no syntax/import/AST/compile/test occurred. Metadata publication tooling alone executed.

The accepted gate's two overclaims are not imported: zero low arrays alone do not force all25 rows zero, and a changed completion need not survive projection at every residue. The source attachment stays in the canonical localized algebra. A future authenticated rank client needs its own new binding and separate review; no canonical297-minor assertion is made.

No new canonical OPEN identifier. The existing quantity is whether faithful independent evaluations of this entire corrected circuit agree at one ROOT-provided admissible place while every required anomaly check passes. Cheapest next instrument: the combined different-model static review of these frozen files and the separate checker. Thirty-minute authoring is unmeasured planning, not a performance forecast; original03:36 reserve/03:39 hard controls. Runtime, actual-place feasibility, coefficient sizes, anomaly outcomes and speed are UNKNOWN. No follow-on or execution authority is issued.

Own-only collision scope was checked before begin and before sealing; no corpus search. Final own WHOLE review, exact input postpins, no new raised OPEN, marker-last publication and expected transaction verification precede custody-first collection. All writers are IDLE at terminal.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7756`.
- Body SHA-256:
  `e69e48b9c0597c675c60294339bd0b2eb367ca46b10de0f88dbd21ddd420e9e8`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
