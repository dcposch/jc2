# Blind whole-portfolio ideation — Grok 4.6 — round `20260902T0741Z`

Lane: Grok 4.6 (xAI), equal-standing independent researcher.
Produced blind against the sealed packet and banked repository
reports only. No other `20260902T0741Z` lane prompt, log, run
record, or report was opened. No canonical edit, commit, push,
AWS action, or CAS. Bare `G2` is not used.

## 0. Custody

Packet hash verified, byte-exact against the frozen copy:

```text
2646ac692c292c30597f9c830fd145c85e020207d803927825b8a162086055cf  /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.i7XCtI/inputs/ideation-20260902T0741Z-packet.md
```

Packet basis `8939320b`. Deltas in the packet are the charged
facts. The 2026-09-02 web sweep is **not** consumed (packet:
queued, not in this freeze).

Firewall: flag ≠ place ≠ cover series; `REPRESENTATIVE` is not
`FULL_ACTUAL_EXIT`; dicritical `l`, `A_F`, `E=F^{-1}(A_F)`,
and Galois-sheet stay distinct; four-box `a,b,sigma,e_j`
stay distinct; no safe replacement ⇒ typed `OPEN`; no new
exit-price ⇒ no `charge_basis`.

Read: the packet; `APPROACHES.md` 46-row table plus 2026-08-31
overlays; `AUDIT.md` top; `PROGRESS.md` through 2026-08-31;
MPRIME-ALLN-H2 + review; HORN-FLAGSHIP + review; RAY-KILL;
CASE-A-SWEEP; HOMCOVER-TRANSFER; COMPANION-CURVE-ALLN;
SOURCE-GATE-962; integration 7. No other `20260902T0741Z`
submission. No recycled `20260902T0022Z` cards.

## 1. The packet's gap framing is wrong

The list (a)–(l) is a **flat residual inventory**, not a
dependency graph. Three items are category errors. Fresh
ranking starts from THEOREM PROFILE and from delta 9, not from
twelve co-equal gaps.

### 1.1 (k) is already answered: different objects

Delta 9 adopted: the A2 residual and the (B3) cage are
**separate instruments on different objects**. Gap (k) then
asks for a bridge on “the same geometric object.” False.

- A2 is a unibranch **germ of the map** at infinity, A-degree
  2: `(eta, s, q, r, G)` in `C[Z]`, wall `g=2e`. No cusp of
  `A_F` enters (RAY-KILL §5: do not quote RAY-* at a (B3)
  cusp).
- (B3) is a **representation of `pi_1(C^2 \ A_F)`** when `A_F`
  has a unibranch cusp and a multibranch point. The A2 wall
  does not appear.

A ray-kill is a statement about a Jacobian germ. A (B3) kill
is a statement about `rho`. Feeding one to the other requires
a realized `F`, which neither residual has. **Retype (k) as
WRONG-OBJECT / closed.** Closing `OPEN[A2-U-BOUND]` does not
close (B3), and (B3) has **no empty window in `N` at any `N`**
(HORN-FLAGSHIP, review CONFIRMED). The “one theorem away”
slogan in (l) counts theorems on the wrong object.

### 1.2 (l) and (h) conflate two infinities

1. **Jet order.** Four linear conditions on five new
   coefficients per order, non-terminating. Formal residual;
   Avenue 4 (D-series) in costume. Nonempty jets ⇏ polynomial.
2. **Polynomial `U`.** Surviving `U` run `3e+2, 3e+4, ...`
   without bound. Each cell `(e,U)` is finite and
   overdetermined (excess `~ 5U/2+5e`). This is
   `OPEN[A2-U-BOUND]`.

Gap (l) asks whether jet non-termination is a positive-side
family. **No.** That is the campaign’s oldest CE trap. Cells
`(1,3)`, `(1,5)` EMPTY at two-engine Q, `(1,7)` MODULAR,
`(2,6)` RAY-EDGE: evidence *against* a polynomial family, at
the certainty the packet records. Gap (h) licenses
construction only after a polynomial section, or after the
theorem lane returns “no bound” **and** a section. **Lower
(h). Close (l) as WRONG-INFERENCE.** Keep (a), staffed.

`OPEN[A2-O0-O1]` is a cheaper lever on (a): two of seven
equations absent from the reviewed record. Independent on the
wall ⇒ six-on-five, formal residual can die without a
`U`-bound. That is live L1. Do not duplicate; do not confuse
with (h).

### 1.3 (e) asks the wrong all-degree question

H2 is **already mixed**. THEOREM PROFILE is all-degree as a
classification; several rows are all-degree as emptiness.

| profile | all-degree content | bounded remainder |
|---|---|---|
| (0) | EMPTY every `N` (SMOOTH-KILL) | none |
| (A) | EMPTY `4<=N<=7`; LZ to a torus-knot group every `N` | cells `N>=8` |
| (B1) | EMPTY `N<=16`; **dies every `N` if `R<=1`** | `N>=17` only if `R>=2` |
| (B2) | EMPTY `N<=4`; `beta` forced `5–16` | `OPEN[DEG-AF-VS-N]` (badly typed) |
| (B3) | **no empty window in `N`** | the horn; A2 is not this |

Complement of H2: reducible branch, Euler/`chi_c` retired
(COLLAPSE-N, COMPANION-EXISTS, BURNSIDE-CHI); live handle
`OPEN[COMPANION-R0-REALISATION]` plus SOURCE-OPEN-U Mode 2.

Named all-degree instruments **not** staffed: `R<=1` for (B1);
CUSP-A-KAPPA for (A); CUSP-KILL **replacement on `E`** for
(B3). Bounded-`N` is right only for (B2) and A2 cells.
**Retype (e).** Stop the monotone-invariant hunt.

### 1.4 (d): `OPEN[DEG-AF-VS-N]` is false as a raw bound

MPRIME wants `n=deg A_F <= f(N)` to bound `beta`. Raw, no:
`psi in Aut(C^2)` on the **target** gives `A_{psi circ F} =
psi(A_F)`, same `N`, still Keller, `deg psi(A_F)` unbounded.
PROFILE class is Aut-invariant; degree is not.

Retype: **min-deg of `A_F` in the target-Aut orbit**. Avenue 5
as TRANSPORT on the target, not Jung descent on `F`. Degree 3
is empty for (B2) (nodal cubic is (B1); cuspidal is (A)).
First cell: degree-4 polynomial curve, singular branch at a
multibranch point, no unibranch singularity.

### 1.5 (b)/(i) is a false dichotomy

The (B3) N=4 cage is rigid-**not-empty** (B3-N4 CONFIRMED):
`rho(G)=S_4`, Puiseux `{2,3}`, dicritical `(1,2)`. Killing at
representation already failed. Building a map from a
representation is SOURCE-OPEN-U (Mode 1 = `(M')` by SG-1), and
every explicit N=4 **curve** is dead. The skipped object is
**`E`**:

- `k_odd=0`: `j=2`, both components rational, `n_1+n_2=3+4k`.
- `k_odd>=1`: `j=1`, genus `k_odd-1`,
  `n_infty=3+4k-2k_odd`, unique affine singularity `y_0`
  analytically a `(p,q)` cusp (`F` étale), `a_q=0`.

Ordinary nodes: `t=1` odd, so `k_odd=k>=1`. Minimal `k=1`:
**`E` rational, five places at infinity, unique affine `A2`,
no other affine singularity.** CUSP-KILL wanted `E cong C`.
It fails because of ends (and genus for `k_odd>=3`). The
replacement is an embedding theorem for this numerical type,
not a construction of `F`. **Retype (b). Lower (i).**

### 1.6 What the list should have been

Proof, H2: (1) CUSP-KILL replacement on `E` at (B3) N=4 `k=1`
(Card I); (2) `OPEN[A2-U-BOUND]`/`OPEN[A2-O0-O1]` on the
map-germ, staffed; (3) case (A) `N>=8`, staffed, CUSP-A-KAPPA
the all-degree reorganisation; (4) (B2) after target-Aut
min-deg (Card II); (5) (B1) via `R<=1`, not `N>=17` census.

Proof, reducible: R0 + SOURCE-OPEN-U Mode 2; JOB-A is the
unrun discriminator. Euler stays retired.

CE: prime `N=7` **curve-first** against PROFILE `(B2)∪(B3)`,
not `S_6`, not the A2 ray, not the (B3) N=4 representation
(Card III). Char-p/Witt orthogonal.

## 2. Disposition vector

Legend: `U` unchanged, `R` raise, `L` lower, `T` retype.
Reasons only for changes. Unlisted 46-rows are `U`.

### 2.A Gaps (a)–(l)

| gap | d | reason |
|---|:-:|---|
| (a) A2-U-BOUND | U | Correct theorem, staffed. O0/O1 cheap lever; cell EMPTYs are evidence, not the theorem. |
| (b) (B3) cage 4–16 | T | Not “build or kill” at representation. Replacement is an embedding theorem for `E` at N=4 `k=1`. |
| (c) HOMCOVER-CUSP-A-N8 | U | Staffed. Gift: order-24 cell is the unique transitive `S_4`-action on 8 letters (cube vertices, stab. `C_3`). |
| (d) (B2) beta-forced | T | `OPEN[DEG-AF-VS-N]` false raw (target Aut). Min-deg in the Aut-orbit; deg 3 empty, deg 4 first cell. |
| (e) (B1) / ceiling | T | `R<=1` kills (B1) every `N`; CUSP-A-KAPPA reorganises (A); (B3) has no `N`-window. Stop monotone hunt. |
| (f) COMPANION-R0 | U | Correct reducible residual. JOB-A unrun. Do not revive `chi_c`. |
| (g) N>=6 census | T | Prime `N=7` curve-first against `(B2)∪(B3)`, not `S_6`. At `N=7`, (A) and (B1) empty. |
| (h) infinite-`U` as CE | L | No polynomial section. Jets are formal. Licensed only if theorem lane returns “no bound” **and** a section. |
| (i) (B3) N=4 construction | L | Representation is not a map. Every explicit N=4 curve is dead. |
| (j) Char-p / Witt | L | Orthogonal, deg-12. Not this bottleneck. |
| (k) A2–(B3) bridge | T | WRONG-OBJECT. Delta 9. Closed as a target. |
| (l) ladder as CE signal | T | WRONG-INFERENCE. Four-on-five is jets. Close. |

### 2.B Packet-named avenues

| Avenue | d | reason |
|---|:-:|---|
| Path-1 MPRIME-ALLN-H2 | U | Promoted. Consume PROFILE; no “H2 complete” paraphrase. |
| Path-2 COMPANION-CURVE-ALLN | U | Closed negative as obstruction. Harvest JOB-A / R0. |
| A2 cell window | R | Evidence for (a). Certainty tags required (§8). |
| SOURCE-OPEN-U | U | Mode 1 = `(M')` (SG-1). Live only Mode 2 / reducible. |
| CUSP-A-KAPPA | R | All-degree reorganisation of (A). Live lane owns it. |
| N=4 explicit curves | L | All dead. Remaining work confirmatory or instrument. |
| Degree-monotone invariant | L | No candidate; `R<=1` replaces it for (B1). |

### 2.C 46-row inventory

Raise 5, 6, 7, 26, 27, 36; lower 1, 4, 19. Reopen none.
Row 16 `U`: no 10-line holonomic class. Row 25 `U`:
SIROCCO+ZvK is an instrument. Row 31 `U`: SOURCE-OPEN-U
Mode 2 already charged.

| # | d | reason |
|--:|:-:|---|
| 1 | L | N<=5 closed. GGV corners do not see PROFILE or `E`. |
| 4 | L | Warning: A2 jets are the D-series pattern. Negative control on (h), not a route. |
| 5 | R | Target-Aut **normalisation of `A_F`**, not Jung descent. Retype of `OPEN[DEG-AF-VS-N]`; shared with reducible degree-cap. |
| 6 | R | AMS/LZ on **`E`**. Multi-place (`n_infty>=5`) is the CUSP-KILL failure and the new avenue. Historical wrong object was fibres. |
| 7 | R | `A_F` is the PROFILE object. Min-deg after target Aut is Card II. |
| 19 | L | Orthogonal this round. |
| 26 | R | Prime `N` forces primitivity. CE design is `N=7` first, not a td-ceiling. |
| 27 | R | Links at infinity on **`E`**, not residue-A, not `A_F`. |
| 36 | R | Guided search only as curve-first PROFILE census at `N=7`. |

Negative space: no reducible rank-four trees; no colouring
conductors; no Euler revival on RED-N; no Moskowicz-as-proof;
no HC4; no Card C without `OPEN[CARD-C-SOURCE-NORMAL-FORM]`;
no desk CAS.

CASE-A-SWEEP is unreviewed: N=8 pair list and cheap-gate zeros
are `MEASURED` until a different-model gate. Promotion of a
new (A) gate must not rest on the sweep’s unreviewed family
collapse.

## 3. Reranked bottlenecks

Proof, then disproof, by expected information per desk-day.
Floors are not attainments. Running flagships are consumed.

1. **CUSP-KILL replacement on `E` at (B3)-N=4, ordinary-node
   `k=1`.** Cage rigid-not-empty, no `N`-window; A2 does not
   speak to it; N=4 curves are dead, `E` was never the census
   object. B3-N4 pins the type (CONFIRMED). Card I.
2. **`OPEN[A2-U-BOUND]` / `OPEN[A2-O0-O1]`**, staffed.
   Map-germ horn. A `U`-bound finite-izes per-`e`; it does
   not bound `e` and does not kill (B3). Consume O0/O1 before
   another cell.
3. **Case (A) at `N>=8`.** Staffed. CUSP-A-KAPPA is the
   all-degree reorganisation. Order-24 cell has a unique
   group name (§4). Survivors stay `REPRESENTATIVE`.
4. **(B2) after target-Aut min-deg.** Card II.
5. **Reducible: JOB-A + R0.** Companion exists. Euler
   retired. SG-1 forbids counting Mode-2 Euler as extra
   evidence. SOURCE-OPEN-U Mode 2 is the architectural gap.
6. **(B1) via `R<=1`.** One valuation statement, every `N`.
   Later: N=17 is not the CE frontier.

Disproof: (i) prime-`N=7` curve-first (Card III), not `S_6`;
(ii) A2 section only if the theorem lane says no bound;
(iii) Witt orthogonal; (iv) do not interpolate `(P,Q)` from
a representation. Proof-side dominates until a
PROFILE-compatible polynomial curve at `N>=6` exists.

Path-1 and Path-2 landed. What they left: `E`, the A2 wall,
the N=8 torus-knot cells, and a degree-cap.

## 4. New avenue

**Punctured-rational embeddings of `E` as the CUSP-KILL
replacement, starting at (B3) N=4 ordinary-node `k=1`.**

Not Path-1/2, not A2, not HOMCOVER-CUSP-A-N8, not a
representation rerun, not a 0022Z card.

CUSP-KILL (promoted, conditional on LZ and Campbell): `j=1`
⇒ `E` homeomorphic to `C` ⇒ LZ ⇒ Campbell. At (B3) N=4 with
`k_odd>=1`, B3-N4 gives genus `k_odd-1` and `n_infty>=5`.
The mechanism fails because of **ends and genus**.
Replacement: constrain `E subset C^2` as a plane curve of
the pinned numerical type.

Ordinary nodes ⇒ `k_odd=k`. Minimal `k=1`:

```text
E subset C^2  rational (g=0),  n_infty=5,
unique affine singularity y_0 analytically A2,
no other affine singularity,  chi_c(E)=-3,
j=1, a=2, dicritical (s,mu)=(1,2), rho(G)=S_4,
F etale so (E,y_0) cong (A_F, p_0).
```

`REPRESENTATIVE` covering geometry, not a realized map. The
N=4 census killed candidates for `A_F`, not for `E`. Three
gates, never merged, never filling a gap by cap.

**(G1) Componentwise CUSP-KILL, `k_odd=0` arm.** Even
contacts: `j=2`, `n_1+n_2=3+4k`. For `k=1`, seven ends;
splits `(1,6)`, `(2,5)`, `(3,4)`. Only `(1,6)` has a
component homeomorphic to `C`; LZ applies to **that**
component. Other splits: `OPEN[B3-N4-END-SPLIT]`. Ordinary-node
(B3) skips this arm.

**(G2) Link of `E` at infinity vs dicritical `(1,2)`.** Five
places ⇒ 5-component algebraic link. Avenue 27 was tried on
residue-A and on `A_F`; never on the **preimage** `E`.
Places of `E` at infinity are `bar E cap l` on the unique
affine-image dicritical (`s=1`, `mu=2`). Incompatibility
kills; compatibility is not a map. If the splice needs
`deg E`: `OPEN[E-INFINITY-SPLICE-DEGREE]` and stop G2. Never
invent `deg E = N * deg A_F` (`a=2` is `deg(E -> A_F)`;
`deg F` is not `N`).

**(G3) Embedding of a rational 5-punctured plane curve with
unique affine `A2`.** One-place is LZ. Two-place is
Kaliman/`C*` (adjacent, not identical). Five-place:
rational `(R_1(t), R_2(t))` with five poles and one finite
`(t^2, t^3)` point, `delta_aff=1`. Empty ⇒ cell dead as a
plane curve. A parametrization is still not `F`.

Not a bridge to A2. Not HOM-COVER transfer. Not “build the
N=4 representation.” `Loc_{p_0}`, `G`, `G_{p,q}`, `E` stay
apart; `iota` not crossed in the NO-PUSHFORWARD direction.

Gift to the staffed (A) lane, not this avenue. N=8 trefoil
cell: `|rho(G)|=24`, transitive on 8 letters, point-stab.
order 3. Subgroups of order 3 in `S_4` are conjugate, so
**one** conjugacy class of transitive `S_4`-actions on 8
letters: cube rotations on vertices. Identify `Y` with that
cover before Alexander polynomials.

## 5. New cross-connection

**`OPEN[DEG-AF-VS-N]` (H2, (B2)) and the companion degree-cap
(RED-N) are the same target-Aut problem; the CUSP-KILL
replacement is Avenue 6 and Avenue 27 applied to `E`, not to
`A_F`.**

- Row 5: `Aut(C^2)` on the **target**. Minimise `deg A_F`
  (H2) or `sum deg D_i` (RED-N). TRANSPORT, not Jung descent.
  The cap `sum deg D_i <= max(deg P, deg Q)` is MPRIME’s
  wanted currency; `deg P` stays free until Aut-normalised.
- Row 6: AMS/LZ on `E` when `n_infty=1`; multi-place when
  `n_infty>=5` is why CUSP-KILL fails. Historical AM ran on
  fibres.
- Row 27: splice of `E` at infinity, fed by dicritical
  `(s,mu)` from PROFILE/N4-PIN, not residue-A passports.

Negative connection: **A2 jets are Avenue 4.** Treat (h)/(l)
as the D-series failure mode. Cell EMPTYs are a finite-depth
empty window: evidence, not a germ.

Refuse to reopen: SG-1, razor (b) Mode 1 **is** `(M')`.
Four-box Euler / HOM-COVER rank / DET-LINF stay Mode-2 tools.
Do not restack them on H2 (B3). Mixing `E` with `A_F` or a
cover series is the flag/place collision FALLACY-v2 forbids.

## 6. Strongest proof attack and strongest counterexample attack

### Proof

**Desk embedding/splice constraints on `E` at (B3) N=4,
ordinary-node `k=1`, in parallel with staffed A2-U-BOUND
(not instead of it).**

A2-U-BOUND is the map-germ flagship; closing it does not
close (B3). (B3) has no empty `N`-window; N=4 is pinned
(B3-N4 CONFIRMED). Attack = Card I, G1–G3. Parallel, do not
steal: O0/O1 (live L1); CUSP-A-KAPPA (live); JOB-A
(reducible). Do not rerun N=4 (B3) representations: Prop
3.1–3.2 already pin `S_4` and `{2,3}`. If G2 returns `OPEN`
on degree, stop and retype; do not farm parametrizations.

### Counterexample

**Curve-first PROFILE census at prime `N=7`, then
SIROCCO+ZvK+`S_7` only on a surviving polynomial curve.**

Not `S_6`. Not the A2 ray. Not the N=4 (B3) representation.
Not Witt. Not Card C.

At `N=7`: (A) empty, (B1) empty, residual `(B2)` `beta>=2`
or `(B3)`. Prime `N` forces primitivity. Do **not** export
N=4 transposition meridians: type `OPEN[N7-MERIDIAN-TYPE]`
until written from PROFILE at `N=7`. Degree 3 is only (A)
or (B1). Degree 4 is the first cell (same list as Card II,
now at covering degree 7). Empty at 4: increment once, then
`OPEN[N7-CURVE-DEGREE]`. A curve is not a map; PREFLIGHT
mandatory. A representation survivor is `REPRESENTATIVE`;
Mode 1 Euler is `(M')` (SG-1), not a new razor.

## 7. Software acceleration or decisive experiment

**Fail-closed `E`-numerical-type checker**, desk Python, no
Groebner, no SIROCCO, no Sage `__main__`.

Input: PROFILE cell `(N, profile, a, W, k, k_odd, (s,mu),
(p,q) or OPEN, j)`. Output: `(g(E), n_infty, j,
delta_aff(E), chi_c(E), CUSP-KILL-fires |
componentwise-candidate | splice-OPEN | type-OK, named
compactification or OPEN)`.

Fail-closed: `k_odd=0` ⇒ `j=2`, `n_1+n_2=3+4k`, no split
picked; `k_odd>=1` ⇒ `j=1`, `g=k_odd-1`,
`n_infty=3+4k-2k_odd`, CUSP-KILL must **not** report `cong
C`; ordinary nodes ⇒ `k_odd=k`; `deg E` is `OPEN` unless a
sourced formula is supplied; `E` is a source curve, not a
dicritical or a sheet; radical not `squarefree_part`.

Positive control: case (A), `k=0`, `j=1`, `n_infty=1` must
report CUSP-KILL-fires. Negative: B3-N4 `k=1` ordinary node
must report `g=0, n_infty=5, j=1` and must not fire
CUSP-KILL. Second negative: N=8 trefoil cell is (A); feeding
it here must `OPEN` on profile mismatch. First (B3) N=4
`k=1` run **is** the §6 G1/G2 bookkeeping.

## 8. Campaign-systems check

**`UPGRADE` — certainty tags on the A2 cell ledger.** Rotate:
claim/review propagation. PREFLIGHT stays the tracked
0022Z upgrade; do not replace it.

The packet says cells are “being emptied by computation”
and then distinguishes two-engine Q, MODULAR, RAY-EDGE, and
RUNNING. Those are different claims. Promotion of EMPTY
requires Q plus a second engine (RAY-KILL spec). A MODULAR
unit ideal is evidence only.

Smallest test: four-word status on every cell already run;
refuse promotion from MODULAR.

```text
Q-TWO-ENGINE | Q-ONE | MODULAR | RUNNING | BOUNDARY-THEOREM
(1,3)  Q-TWO-ENGINE
(1,5)  Q-TWO-ENGINE
(1,7)  MODULAR          (not promotion-grade)
(2,6)  BOUNDARY-THEOREM (RAY-EDGE)
(1,9)  RUNNING
```

Positive control: ledger uses those words for `(1,3)` and
`(1,5)`. Negative: `(1,7)` not promotable. Implementation:
a 20-line table in the cell-window runner plus one harvest
assertion. No solver change.

Not the upgrade: do not halt box01; do not promote
CASE-A-SWEEP without a gate; do not change
seal-at-completion; do not retire M2.

## 9. Idea cards (at most three)

Not Path-1/2, A2-U-BOUND, CUSP-A-N8-GATE, an `S_6` farm, a
0022Z card, or an A2–(B3) bridge.

### Card I — `E`-embedding, (B3) N=4 `k=1`

**Dependencies.** B3-N4 CONFIRMED (`a=2`, `W=2`, dicritical
`(1,2)`, `rho(G)=S_4`, Puiseux `{2,3}`, genus `k_odd-1`,
`n_infty=3+4k-2k_odd`, unique affine `y_0`). Ordinary nodes
⇒ `k_odd=k`. LZ/Campbell only one-place. SCOPE[B3-QH] for
any local-centre claim; do not consume `Z(G)=1`. Not A2, not
(A), not Mode-1 Euler.

**Discriminator.** Desk, in order. G1: `k_odd=0` split; only
`(1,6)` is componentwise-CUSP-KILL; else
`OPEN[B3-N4-END-SPLIT]`; ordinary-node `k=1` skips G1. G2:
5-component link vs multiplicity-2 dicritical; if degree is
required, `OPEN[E-INFINITY-SPLICE-DEGREE]` and stop G2. G3:
rational parametrization, five poles, one finite `(t^2,t^3)`,
`delta_aff=1`. §7 checker is bookkeeping. No CAS for G1/G2.

**Outcomes / stop / information.** G1 fires: that component
dies by promoted CUSP-KILL; the other remains, retype. G2
mismatch: the cell dies at covering geometry; no map. G2
compatible: not a map; G3. G3 empty: not a plane curve. G3
nonempty: a curve `E`, still `REPRESENTATIVE` for `F`;
SOURCE-OPEN-U next; do not interpolate `(P,Q)`. Untyped
compactification: `OPEN`. Stop at one kill, one
parametrization, or a named OPEN on degree/split. Do not
increment `k` (`k>=2` has genus). Gain: first mechanism that
sees (B3) after the representation cage saturated, or a
typed reason the replacement also fails.

### Card II — target-Aut min-deg of `A_F`, (B2) first

**Dependencies.** PROFILE (B2): no unibranch singular point,
`beta>=2` at `5<=N<=10`, `beta>=1` at `11<=N<=16`, Lemma A.
NODAL-ALL-N (nodal cubic is (B1)). Target Aut acts on `A_F`.
`OPEN[DEG-AF-VS-N]` is the false raw bound. Not (B3), not A2.

**Discriminator.** Polynomial curves of degree 3 and 4, one
place at infinity, all singularities multibranch, at least
one singular branch. Deg 3 must be empty for (B2) (control:
nodal=(B1), cuspidal=(A)). Deg 4 is the first live cell.
Sources: AMS semigroup, Lemma EMB-AUTO, rational-quartic
lists. No Groebner.

**Outcomes / stop / information.** Deg-3 nonempty for (B2):
`OPEN` (bug). Deg-3 and deg-4 empty: min-deg `>=5`; stop;
retype `OPEN[B2-MINDEG-GE-5]` (delta budget then lower-bounds
`beta` against `a-1`). Deg-4 nonempty: finite Aut-normal
list; representation later, only at `N` PROFILE allows.
Untyped “degree 4”: `OPEN` on the semigroup. Do not export
onto (B3) or RED-N.

### Card III — prime `N=7` curve-first

**Dependencies.** N<=5 closed at packet calibration.
CUSP-A-EMPTY at N=7. NODAL-ALL-N. PROFILE at N=7: `(B2)`
`beta>=2` or `(B3)`. Prime ⇒ primitive. PREFLIGHT.
SIROCCO+ZvK validated. Card II’s deg-4 list is the first
input. `OPEN[N7-MERIDIAN-TYPE]`: do not copy N=4
transpositions.

**Discriminator.** (1) Write meridian type from PROFILE at
N=7, or `OPEN`. (2) Degree-4 polynomial curves from Cards
I–II matching `(B2)` or `(B3)`. (3) SIROCCO+ZvK+transitive
subgroups of `S_7` containing that meridian, PREFLIGHT on.
No `S_6`. No map interpolation.

**Outcomes / stop / information.** No deg-4 curve:
`OPEN[N7-CURVE-DEGREE]`, stop; no `S_7` farm. Curve exists,
all reps die: that curve is rep-dead; increment is not this
card. Curve exists, a rep survives: `REPRESENTATIVE`; Mode 1
Euler is `(M')`. Untyped meridian: `OPEN[N7-MERIDIAN-TYPE]`,
stop before GAP. Gain: first CE-side object past N=5 that
PROFILE does not already kill, or a typed reason the frontier
is not an `S_N` census.

## 10. Lane verdicts: continue / redesign / stop

| Lane | Verdict | Ground |
|---|---|---|
| a2-ubound-opus5 | **continue** | Map-germ flagship. L1 (O0/O1) before L5. Do not consume box01 EMPTYs as theorems. |
| cusp-a-n8-gate-opus5 | **continue** | Case (A) flagship. Gift: unique `S_4` cube-action. Prove CUSP-A-KAPPA if cheaper than a new N=8 homology gate. Survivors `REPRESENTATIVE`. |
| web-sweep-20260902-grok46 | **continue** | Finds queued, not in this freeze. |
| box01 A2 cells | **continue**, protocol **redesign** | Evidence for (a). §8 status words mandatory. `(1,7)` MODULAR not promotion-grade. Finite window never closes `OPEN[A2-U-BOUND]`. |
| Box03 869 | **continue** low | Confirmatory on a dead N=4 type. Stop if no theorem-relevant bit by the next micro-round. |
| CASE-A-SWEEP | **review**, then freeze | Unreviewed. Gate the N=8 pair list before any promotion that consumes family collapse. |
| PREFLIGHT | **continue** | Do not replace. §8 is a second, smaller upgrade. |
| SOURCE-OPEN-U Mode 1 | **stop** as a razor | SG-1: it is `(M')`. Mode 2 / R0 remains. |
| Euler/`chi_c` on RED-N | **stop** | Retired. |
| A2–(B3) bridge | **stop** | Wrong object. |
| infinite-`U` construction | **stop** until L5 returns a section | Jets are not a family. |
| S_6-first census | **redesign** as Card III | Prime N=7 curve-first. |
| Degree-monotone hunt | **stop** | `R<=1` is the (B1) instrument. |
| Colouring conductors | **stop** | Saturated at 28. |
| Reducible rank-four trees | **stop** | `m=1`. |
| Card C direct F | **stop** | Dead at step 0. |
| K00 algebraization | **continue** inherited | Orthogonal. Sharp filtration only. |
| Witt / AS109 | **continue** low | Orthogonal. Not staffed. |
| qqideal + msolve 0.10.1 | **continue** | Default. PREFLIGHT + §8 tags. |
| Heavy CAS policy | **continue** | Faithfulness and certainty required. This ideation ran no CAS. |

If A2-U-BOUND returns uniform empty: (B3) is **untouched**;
shift proof budget onto Card I, not onto more cells. If it
returns “no bound” plus a polynomial section: CE-level for
the map-germ horn only; not a Keller map; not (B3). If
cusp-a-n8 kills the order-24 cell and `(3,4D)`: case (A) at
N=8 empty, CUSP-A-KAPPA becomes the all-N program. If Card I
kills N=4 ordinary-node `k=1`: (B3) starts at `k>=2` or
even-contact `k_odd=0`.

N<=5 stays closed. Formalization non-blocking.

<!-- BODY-END -->
