# f10 mixed native cat correction: different-model static FIRST

Status: **CONDITIONALLY STATIC CONFIRMED**. No execution, worker, instance binding, actual native fit, or scientific authority follows.

First action: `2026-09-12T22:56:00.844176100Z`. All four charged hashes matched before body use. I read both new files WHOLE and reused my immediately preceding WHOLE reads of the old collector and corrected commands only after fresh exact-pin checks. The immutable coordination snapshot was likewise reused; no mutable canonical coordination state was read.

## Exact delta and closure of the reported omission

The complete old/new collector diff is one changed line in the fixed `jc2_path` vector: it appends exactly `/usr/bin/cat` immediately before `; do`. No other byte, recursion rule, path, placeholder, file/directory bound, or metadata cap changes.

The charged corrected commands invoke external `cat` exactly for the boot-ID read:

```text
test "$(cat /proc/sys/kernel/random/boot_id)" = "$TASK_BOOT"
```

Adding `/usr/bin/cat` to the collector's root vector directly closes the precise omission reported by the preceding FIRST. The unchanged visitor will require it to resolve as a regular file, record its hash/stat and alias when applicable, and recursively collect its ELF dependencies under the existing limits. This conclusion is confined to that previously identified missing executable; it is not a fresh whole-procedure, inspector, assembler, or algebra review.

## Qualification boundary

The source remains disabled by `JC2_INSTANCE_PLACEHOLDER`. ROOT must still bind fresh physical facts and verify that actual collector output includes the requested root or alias, remains within every unchanged count/byte/resource bound, and satisfies the separately reviewed assembler/inspector and runtime predicates. This static delta does not establish actual native fit, closure completeness on a future host, no escape, worker qualification, or permission to execute.

Verdict: **CONDITIONALLY STATIC CONFIRMED** for the one-line `/usr/bin/cat` correction. No blocking source mismatch exists within this four-file delta.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `2158`.
- Body SHA-256:
  `23b376dce3e71f5aea09e1a2660286bc3cb8be1ac10e7ef6794dac3ccae00c6a`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
