# Significant-news blind delta — AS owner — 2026-08-25 15:50Z

## Blind contract and exact new input

I reread the canonical 46-row inventory and current gaps without reading any
other `1550Z` submission.  This is an ideation/routing note, not a promotion.
The triggering objects were independently replayed on Box02 and r6d from the
same source (script SHA-256
`1d69ef0aed8c522bc487c5dd937d9d03201f7a98772f4b7dfe19878b0ee11192`);
the two stdout files are byte-identical (SHA-256
`cab00eea8599b35e507550ca35731203bf212b7331247c0ed40166d3504ebd44`).

Over `F_3`, put

```text
A(s,t) = (s-s^3,t),
B_{m,k}(x,y) = (u,v) = (x+y^m, y+u^k).
```

Each `B_{m,k}` is the composition of two triangular automorphisms, with
inverse `y=v-u^k`, `x=u-y^m` and determinant one.  The exact controls give:

- `(m,k)=(3,4)`: `G9=A o B_{3,4}` has total and partial-`y` degrees
  `(9,12)`, leading `y` coefficients `(-1,1)`, and Jacobian one over `F_3`;
- `(m,k)=(4,2)`: raw `A o B_{4,2}` has degrees `(12,8)`; the determinant-one
  target swap/sign has degrees `(8,12)` and leading coefficients `(1,1)`;
- each reduced map has exactly three image values on `F_3^2`, each with a
  three-point fibre; and
- for the displayed integral lifts of the formulas,
  `det J(A_Z o B)=1-3u^2`, not one.

Thus these are exact noninjective residue seeds with unit max-12 faces.  They
are not lifts modulo nine, all-depth branches, complex counterexamples, or
maximum-12 exclusions.  The two examples share one fingerprint—tame source
conjugation of the same AS map—and therefore count as one idea, not two
independent votes.

## Whole-46 checksum

The event changes the ranking of avenues `19`, `21`, and `36`, and gives a
new regression/interface for `2`, `4`, `31`, and `46`.  It does not revive
any closed route.

- **Raised/direct:** `19` (Witt lifting), `21` (adic collision transfer),
  `36` (structured counterexample search), and `4` only at the finite
  fixed-support coefficient-scheme layer.
- **Proof-side interface, not evidence:** `2`, `7`, `25`, `26`, `28`, and
  `31`.  A max-12 normal form or landing claim over `Z_(3)` should accept or
  explicitly exclude these unit-face controls, but a characteristic-three
  seed does not land a characteristic-zero boundary component.
- **Tool donors:** `1`, `3`, `8`, `9`, `29`, `32`, `33`, `38`, and `46`.
  In particular, source conjugacy, Fitting/Kuranishi reduction, the reviewed
  residue-ball theorem, and proof-producing coefficient compilers are useful;
  none supplies a global conclusion alone.
- **No rank change:** `5`, `6`, `10`, `13`--`18`, `20`, `22`--`24`, `27`,
  `30`, `34`, `35`, `37`, `39`--`45`.  The prior objections and stop rules
  remain.  Avenue `11`/`12` and the other explicitly refuted/category-mistyped
  programs stay closed.

The global walls are unchanged: proof lanes still owe complete landing plus a
cofinal complexity wall; the disproof lane still owes one complete
fixed-support all-depth determinant-one point and the reviewed collision
composition.

## Card 1 — transported-support D12 lift gate

**Connection:** `19+21+36`, with `4+31+46` as algebra/certification tooling.

This is not a generic 182-variable search.  Precomposition by `B^{-1}` gives
an exact equivalence

```text
F == G (mod 3), support(F) in V_{<=12}
  <=>
H=F o B^{-1} == A (mod 3),
support(H) in B^{-1*} V_{<=12}.
```

The transported space has the same 91-dimensional basis per coordinate,
although it is lacunary and high-degree in the AS coordinates.  Determinant
one and the three residue-ball collisions are preserved.  This lets the
source-first AS compiler consume a finite warped basis rather than invent a
new solver architecture, while explaining why the maximum-`y`-eleven escape
theorem does not apply after conjugation.

The cheapest complete gate keeps all 182 output coefficients (including
zero/spectator columns rather than relying on normalization) and all 276
possible determinant monomials of total degree at most 22.  For
`F=G_Z+3U`, the exact first-digit equation is

```text
-u^2 + L_{G_Z}(U) = 0 mod 3.
```

It is one affine `F_3` system.  If nonempty, the complete second digit for
`F=G_Z+3U+9V` is the quadratic Kuranishi equation

```text
(-u^2 + L_{G_Z}(U))/3 + det J(U) + L_G(V) = 0 mod 3,
```

on the entire first-digit fibre, with literal integer replay modulo 27.  Use
the raw `(9,12)` and oriented `(8,12)` seeds as two structural controls, not
as independent statistical samples.

Positive controls are source-derived: the cotangent truncation
`(s-s^3,t(1+3s^2)) o B` survives modulo nine at total cap 18 for `G9` and cap
16 for `G8`; adding `9s^4` gives a modulo-27 control at cap 24.  Therefore a
D12 failure is a genuine cap-growth datum, not a compiler success by
vacuity.

**Outcome/stop:** a replayed second-digit survivor launches the fixed-support
tower, with Kuranishi/Fitting and lasso/rational reconstruction in parallel.
First- or second-digit emptiness closes only that transported D12 support.
No number of finite survivors is a counterexample, and no empty D12 gate is a
maximum-12 proof theorem.

## Card 2 — use the seeds as a max-12 normalization regression

**Connection:** `2+19+28+31`, with the live Q8 proper-model work as client.

Both seeds sit exactly at primitive max-12 degree profiles and have unit
leading `y` faces.  They are therefore unusually sharp negative controls for
any proposed frontier argument that silently obtains an obstruction merely
from a divisible/nonunit leading face or from dividing by three.  Substitute
their literal coefficient points into the Q8 leading-face/contact equations
and record exactly which source open, saturation, or characteristic-zero
hypothesis rejects them.  If a source-honest chart accepts a seed, compute its
proper-model special fibre and the induced component/genus map; this tests the
landing machinery, not JC2.

**Cheapest discriminator:** an exact coefficient-chart membership and
denominator audit, followed only when typed by the first proper-model fibre.
A rejected point is useful only if the rejecting hypothesis is named.  An
accepted positive-genus component does not obstruct the finite-field map and
does not prove a characteristic-zero landing.

## Card 3 — p=109 cannot borrow the unit-face trick integrally at max 12

**Connection:** `19+21` with the `(6,9)`/max-12 proof frontier.

There is a clean valuation obstruction to the tempting weighted-normalization
bridge.  In a normalized coordinate with unit linear term, write

```text
q(y)=y+...+c y^m,       v_pi(c)>0.
```

Under `y=pi^a Y` and the target rescaling that restores the unit linear term,
the leading coefficient has valuation

```text
v_pi(c) + (m-1)a.
```

Every integral scaling has `a>=0`, so it cannot make the leading coefficient
a unit.  A ramified scaling that does so has `a<0`; it is not an integral
automorphism of the source lattice and does not preserve the Hensel residue
balls/collision theorem.  Moreover, conjugating the `p=109` AS term by an
integral nonlinear shear `x -> x+y^r` creates a unit `y^(109r)` face, never a
max-12 face.  Thus the new p=3 construction does not license importing the
Q8/TD6 monic/depressed normalizations into the p=109 lift problem.

**Cheapest discriminator:** formalize the displayed scaling lemma for the
actual normalized linear core and test all allowed integral affine/weighted
changes.  Stop immediately if coordinate mixing supplies a genuinely
integral unit-face chart; otherwise keep p=109 and max-12 as separate lanes.

## Post-mod729 AS priority and allocation

The new D7 modulo-729 survivors are important but typed: exact third
Bockstein systems leave affine families of dimensions 61 and 52 at three
reviewed parents.  At modulus 2187, `729 det J(T)` survives, so the next gate
is a quadratic carry plus affine fresh digit on the full nonreduced family,
not another global linear module.  This remains the best mechanism-discovery
client, while the D12 first digit is the cheaper counterexample falsifier.

For the immediate event window I recommend **Q8 35 / AS 40 / TD6 20 /
global 5**, with AS split roughly 15 points to the complete D12 first/second
digit gate and 25 to the D7 mod2187 Kuranishi/Fitting mechanism.  Revert to
the sealed `40/30/25/5` allocation if both D12 seeds die by depth two without
a reusable invariant.  Raise AS further only for a complete D12 survivor
with a regular Hensel minor, a periodic carry state, or an exact rational
reconstruction.  A source-honest Q8 landing or a denominator-free TD6 cover
still outranks prolonged digit chasing.

No claim in this note proves or disproves JC2.
