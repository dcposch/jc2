# f10-middle-real-code-gate-fable5-20260910 — FIRST Fable5.1 STATIC code gate: uniform real-domain exact certificate source

status: UNSEALED (adapter seals; no Seal section and no charge_basis authored here; the standalone BODY-END marker is the last line)
lane: f10-middle-real-code-gate-fable5-20260910
first_action_utc: 2026-09-10T09:16:50Z (sha256sum before any body)
stop_utc: 2026-09-10T09:30:50Z (first+14 min, earlier than 09:33:00Z); reserve 09:28:50Z; never reset
mode: manual math/text/hash only; ZERO subprocess/CAS/Python/import/AST/compile/syntax/test/fixture/coefficient generation; every statement below is a hand reconstruction from the 18 charged snapshots
owned: xmodel/f10-middle-real-code-gate-fable5-20260910.md, box/f10-middle-real-code-gate-fable5-20260910/input_custody.md (both absent at 09:17:21Z); all writes apply_patch only

## 0. Charged inputs and read attestation

All 18 ordered immutable snapshots in /tmp/jc2-lane.xY4CxV/inputs were hashed BEFORE any body was read; every digest equals the charged list (the full sha256sum output is reproduced in the owned input_custody.md; the 64-hex tokens below are copied from that live output). Producer report c3111895…7785e9 (38 lines), artifact b99276f6…27c2448, producer.py 44859af3…ee0dd2 (273 lines), checker.py 9369f0f3…8043c (313 lines), authority.py 45871331…59d73d (133 lines), VALIDATION aadc2c26…60bbaa, DISABLED-REGISTRATION d48472ed…c87a145, OWN-REVIEW 182d6376…b48a48, READ-SCOPE 0d0b2fc8…f2e48, ROOT-CARD a2e65875…3035, and the eight accepted/gate premises 3f79cabe…347ea, f04c174e…c21f, 8cf54b2f…f012, dc43ef1c…7ad, 82f11ed7…4e6b6, 8a80bcc8…8491, 890e5c9c…5e5, cff18a06…7c62 were all read WHOLE as text after the match. Two whole-file reads overflowed the tool display; the persisted tool-result copies were re-read whole by bounded reads, so nothing was clipped. No provenance was followed and no file outside the 18 was opened. Accepted 17zzi (real-domain producer + its FIRST gate) is taken as the certificate CONTRACT only, not as nonresonance; other new r2/all-r sources are not inputs. The producer artifact JSON's body-hash/lifecycle fields are taken as stated (metadata; nothing here depends on them).

## A. Producer S/T versus checker E_X/Q_X reconstruction: CONFIRMED

Producer `sources()` (three variables t=tau, a=alpha, v=V; no W anywhere): nu=2−t; D=12V²−12(1−t)V+(1+t)(2−3t); K=−840V³+840(1−t²)V²+42(1+t)(2+t)(4t−3)V+2(2−3t)(1+t)(2+t)(3+t); gamma=t[(1+t)(2+t)(3+t)−30(1+t)(2+t)V+180(1+t)V²−120V³]; sigma=a+nu, pi=a·nu, A=sigma−7, B2=sigma²−pi−12sigma+47, B3=sigma³−2sigma·pi−18(sigma²−pi)+119sigma−342, P=840V³+420AV²+42B2V+B3; S=2K²−140t(1+t−6V)KD+245gammaD²; T=DP−(12V+A)K. Own check against the accepted displays: 17zzb (3) at u=2−t gives (u−3)(4−3u)=(1+t)(2−3t), 12(1−u)=−12(1−t); K's four terms with u−3=−(1+t), u−4=−(2+t), u−5=−(3+t), 3u−4=2−3t, 5−4u=4t−3 reproduce exactly the four producer terms (sign of the constant: −2(2−3t)·(−1)³(1+t)(2+t)(3+t)=+2(…)); gamma equals 17zzb (8); A/B2/B3/P/ell equal 17zz (6); S equals 17zzd (2); T=DP−ellK. All signs and constants CONFIRMED.

Checker `family()` reproduces the literal seven-term E_X and eight-term Q_X of 17zz (5) coefficient by coefficient (1,30,180,120,120,720,360 and 1,42,420,840,210,2520,2520,2520) in a separate four-variable algebra; `reconstruct()` forms Q_nu−7E_nu, REQUIRES W-degree ≤1 (independent W² cancellation), reads K as the W⁰ part and D as the W¹ part over 210, beta/gamma as the W¹/W⁰ parts of E_nu, and S=2K²−(7/6)·beta·K·D+245gammaD²; since beta=120t(1+t−6V), (7/6)·120=140, identical to the producer's S. Z comes from monic synthetic division of Q_alpha−Q_nu by (alpha−nu) along the alpha axis with root nu (alpha-free, so the root test passes), remainder required zero and W-degree ≤1; T=D·Z_W0+K·Z_W1·(−1/210)=DP−ellK. The synthetic loop (leading coefficient times x^(k−1), subtract term·(x−root)) is standard and exact. Degree guards deg_V S≤7, deg_V T=5, lc_V T=10080 match 17zzd/septic-gate. The producer's own guard compares the V⁵ coefficient of T with the constant 10080 as a dict. No hidden W: producer objects are 3-exponent; the checker's S, T are formed from W-axis-zeroed coefficients, and the identity check rejects any V or W exponent. CONFIRMED.

## B. Three long divisions, integerized 9×9, subset-DP signs, cofactor conversion: CONFIRMED

Division: for power 2,1,0 the producer takes q=rem_V^(power+5)/10080, subtracts q·V^power·T; three steps leave deg_V rem≤4 (checked), rem·10080³ integral (checked), matching 17zzd (denominators c0, c0², c0³). Matrix: columns V^i·(c0³Frem), i=0..4, and V^j·T, j=0..3; entry [row][col] is the V^row coefficient, rows 0..8, zero slots retained as empty polynomials (padding is automatic, degree drop of Frem is harmless). DP: states[mask] at level k is the determinant of matrix rows 1..k on the ordered columns of mask, computed by Laplace expansion along its last row (matrix row k, 0-based row k−1 of the submatrix) with sign (−1)^((k−1)+position) and minor states[mask without col] from level k−1 (rows 1..k−1): correct. At level 8, co[j]=(−1)^j·det(rows 1..8, columns ≠ j) is exactly the cofactor of entry (0,j); Σ_j matrix[0][j]·co[j] is the row-0 Laplace expansion of det B, and Σ_j B[k][j]·co[j]=0 for k≥1 (alien cofactors), so Σ_j column_j(V)·co[j]=det B=R as polynomials: (c0³Frem)·Atilde+T·Btilde=R with Atilde=Σ_{i<5}co[i]V^i, Btilde=Σ_{j<4}co[5+j]V^j. With U=c0³·Atilde and Frem=S−Qdiv·T: R=U·Frem+Btilde·T=U·S+(Btilde−U·Qdiv)·T, i.e. Wcof=Btilde−U·Qdiv, exactly as coded. Scaling five columns by c0³ multiplies det by c0^15 and the T-cofactors by c0^15, the Frem-cofactors by c0^12, so U=c0^15·A and Wcof=c0^15(B−A·Qdiv) of the accepted contract; integrality of R,U,Wcof is re-checked by the producer and re-required by the checker. Zero determinant raises 'zero determinant' (INCONCLUSIVE, exit 2); no header, engine or sample replaces the identity. CONFIRMED.

## C. Rectangle, maximal (1−t)^j, Bernstein conversion, checker's independent route, all 22 edge entries: CONFIRMED

Producer `rectangle()`: tau→(6+t)/21, alpha→(45−3t+7ts)/21 via cached powers, envelope k=0, i+j≤40, j≤21 enforced, scaled by 21^40 (integral since i+j≤40). `remove_edge()`: (1−t) divides iff poly(1,s)≡0 (`at_one`); ascending division a_i=a_(i−1)+b_i with the top residual b_deg+a_(deg−1)=0 checked (the code comment "a_i=b_i−b_(i−1)" states the inverse relation, code is right; cosmetic). Loop stops at the first non-divisible quotient, so j is maximal; j≥1 required; endpoint=Rhat(0,0), epsilon its sign; G=epsilon·reduced; degree 40−j. Bernstein: b_(i,jj)=Σ_{k≤i,l≤jj} c_(k,l)·C(i,k)/C(deg,k)·C(jj,l)/C(21,l) is the exact monomial→tensor-Bernstein conversion at fixed bidegree (40−j,21) (own check on t at degree 1 and on constants). Checker: nested Horner pullback (integrality and (40,21) envelope enforced), descending synthetic division by (t−1) with quotient negated (own check on 1−t²: quotient −t−1, reduced t+1), count must equal declared j with 1≤j≤40, endpoint zero → INCONCLUSIVE before epsilon is read, epsilon∈{±1} with epsilon·endpoint>0, reduced envelope ≤(40−j,21), dense (A+1)×22 rectangle, expansion Σ b·C(A,i)t^i(1−t)^(A−i)·C(21,k)s^k(1−s)^(21−k) compared to epsilon·reduced coefficient by coefficient, then all 22 row-0 entries =|endpoint| (a true identity since Rhat(0,s) is s-free), then any negative → INCONCLUSIVE, then corners. Statuses: zero R, zero endpoint, negative Bernstein, nonpositive corner (unreachable after the row-0 equality, harmless) are INCONCLUSIVE with receipt and exit 2; t=0, s=0, s=1 are never removed. CONFIRMED.

## D. Wire, schema, caps, error paths: CONFIRMED

Producer emits only strings: sorted [[exp-strings],[num,den]] terms of reduced Fractions, j/epsilon/degree strings, dense Bernstein rows, science/code/bounds dicts; json.dumps with sort_keys and allow_nan=False. Checker: strict key set and schema string, science and code pins equal to its own authority values, variables/bounds literal, status∈{CANDIDATE,INCONCLUSIVE}, exact polynomial inventory, per-name envelopes Qdiv(3,3,2) Frem(8,8,4) R(40,21,0) U(32,32,4) Wcof(35,35,6) (all consistent with 17zzd (3): U_i≤32−i, Wcof_j≤35−j, deg_alpha R=21), strictly increasing exponent tuples (duplicates rejected), canonical decimal regex with the 4096-char limit before int(), positive reduced denominators, 12000-bit cap, integrality for R/U/Wcof, term cap 20000, duplicate JSON keys rejected by the pairs hook, floats/NaN/Infinity rejected, and every JSON integer rejected in wire mode; size checked from stat before reading; receipt ceiling min(4 KiB, 16 MiB−artifact). set_int_max_str_digits(4096) is armed inside authorize before any read. No eval/pickle/assert anywhere (grep of the three sources). CERTIFIED_NONRESONANCE is returned only at the end of the full verify path; all exceptions print INCONCLUSIVE and exit 2 without a receipt; the dict returns are INCONCLUSIVE with receipt and exit 2. Early rejections are parser/identity, and VALIDATION labels classes 4, 6, 7, 11 correctly as not sign-branch coverage. CONFIRMED.

## E. authority.py entry gate: CONFIRMED as metadata-only, with residuals

Order verified: int-digit guard, literal JOB tag and Linux, registration absolute/regular/non-symlink/uid 0/mode exactly 0444/≤64 KiB, strict JSON (ints allowed there for caps), enabled/jobtag/operation/exclusive flags, DMI vendor 'Amazon EC2' and board_asset_tag i-hex, instance/hostname, cwd and full sys.argv, realpath interpreter, SCIENCE dict, exact caps dict, tz-aware unexpired deadline, pins dict covering the three sources (resolved folder of authority.py), the interpreter and the checker input, every pin re-hashed, outputs list equality and absence (dangling symlinks caught) — all before `from fractions import Fraction`/`math` and before any polynomial (P/checked/const refuse while F/RAT is None). postcheck re-hashes registration and pins and re-tests the deadline before emit; emit is 'xb' exclusive. Residuals, exactly as the producer states: no supervisor, rlimit, cgroup, timeout, CPU/RSS or stdout/stderr budget; the SCIENCE comparison is dict-to-dict and hashes no file, so shipped reports are proved only if ROOT lists them in pins; the 'exclusive writer' flag is declarative, 'xb' is the only mechanical exclusivity. The disabled template (enabled false, operation/caps/pins null) fails at 'disabled or mismatched registration' and, in the owned box, already at the root/0444 test: it cannot authorize. CONFIRMED.

## F. Startup contract: no code fix REQUIRED, one contract line MUST be pinned and tested

Both CLIs `import authority` at module load. Without -P/-I the interpreter prepends the script's directory, so the sibling module resolves. Under `python -I` (which implies -E, -s and, from 3.11, -P) or `-P`, the script directory is NOT on sys.path and the import fails with ModuleNotFoundError before any handler: uncaught traceback, exit 1, no artifact — fail-closed but a wasted registered attempt. This is not a certificate-semantics defect; it is an entry-contract item: ROOT's wrapper must pin the exact interpreter flag set (recommended -E -s -B, no -I/-P) together with argv, and the five-refusal regression must show the first refusal is 'wrong job or platform' (proof that the import path works). Optional hardening (two lines, not required): insert the resolved script directory into sys.path before the import. Two further contract items: the guard `sys.set_int_max_str_digits` exists on 3.11+ and on the 3.10.7/3.9.14/3.8.14 backport releases, but `fromisoformat` accepts a trailing 'Z' only on 3.11+, so the registration deadline must be written with an explicit '+00:00' unless 3.11+ is pinned; registration pin keys must be the resolved absolute path strings, since the code compares `str(folder/name)` of the resolved folder. Ordering: producer runs authorize → build → postcheck → emit; checker runs authorize (input pinned) → stat/size → read → verify → receipt-hash by re-reading the input → postcheck → emit. Residual TOCTOU: the verified bytes are read once, while the pin check (before) and the receipt digest/postcheck (after) re-read the path; a swap-and-restore between those reads would be invisible. Under ROOT's stated immutable root-owned input this is closed; the smallest code precision (P1) is to compute the receipt digest from the verified `raw` bytes and compare it to the pinned value. Late writes: nothing is written after emit; a partial artifact on write failure is rejected by the checker's parser.

## G. Twelve changed-object classes, discriminating branch actually reached

1 U+1: identity gains S, whose V⁷ term trips 'positive V cancellation' (line 222) before the R comparison — one class, not two. 2 R+1: V-free, so 'full cofactor identity'. 3 j±1: recomputed count ≠ declared → 'wrong maximal excluded-edge factor' (j=0 hits the same message). 4 deleted t=0 entry: row length ≠22 → 'complete Bernstein rectangle required' (schema, not sign). 5 interior +1: basis independence → 'full Bernstein expansion'. 6 negative replacement: expansion mismatch unless the genuine candidate is itself negative there; the negative-return branch needs a real identity-consistent candidate. 7 zero-constant R with cofactors unchanged: 'full cofactor identity'. 8 duplicate key: pairs hook. 9 duplicate term: 'duplicate or unsorted polynomial term'. 10 oversize: 'aggregate wire size limit' after authorize (its own pin). 11 >4096-char numerator: 'canonical decimal string required'; JSON int: 'JSON integer forbidden in wire'. 12 wrong bounds/exponent: 'variables or declared bounds'/'polynomial degree envelope'. All twelve as VALIDATION states; no positive fixture is invented anywhere; no executable harness exists; the 360/330/2 GiB/16 MiB figures remain unmeasured and confer no authority. One unmeasured static risk, not a defect: the DP performs on the order of 10⁷–10⁸ Fraction operations on an all-integer matrix (Fraction arithmetic over integer values is gcd-bound), so the CPU cap could be approached; VALIDATION already classifies any cap hit as NONDECISION.

## Verdicts, smallest defect, static acceptance scope

| item | verdict |
|---|---|
| A literal S/T from accepted D/K/gamma/P; independent E/Q, W² cancellation, monic alpha division; signs/constants; no hidden W | CONFIRMED |
| B three divisions by 10080, c0³ integerization, padded 9×9, DP sign (−1)^(k−1+pos) and (−1)^j, R=US+WcofT, c0^15 conversion, zero/drop cases | CONFIRMED |
| C rectangle, maximal (1−t)^j≥1, epsilon, Bernstein conversion vs tensor expansion, 22 edge entries, retained edges, INCONCLUSIVE statuses | CONFIRMED |
| D strict schema, string wire, canonical rationals, caps before allocation, float/int rejection, CERTIFIED only on full success | CONFIRMED |
| E metadata-only authority ordering and refusals; not a supervisor; SCIENCE dict is not a file proof; template cannot authorize | CONFIRMED |
| F import-at-load under -I/-P; deadline 'Z'; resolved pin keys; TOCTOU residual | GAP (contract), no required code fix |
| G twelve classes reach the stated branches; no fixture; caps unmeasured | CONFIRMED |

REFUTED: none. Smallest blocking defect: none for the certificate semantics; the code may proceed to a future bounded validation ONLY under a ROOT entry contract that pins full interpreter flags without -I/-P (or applies the optional sys.path insertion), writes an offset-explicit deadline, uses resolved absolute pin keys for the three sources, the interpreter and the input, guarantees the immutable input (or applies P1), and runs the five-refusal/valid/descendant-cleanup regression before any scientific run. Cosmetic: inverted division comment; unreachable 'zero endpoint' relabel after the row-0 test. This gate mints no runtime compatibility, measures nothing, authorizes no execution or registration, and makes no nonresonance, source or JC2 claim; a future certificate success would give only the accepted 17zzi scope.

## OPEN(S) RAISED

None. No new canonical OPEN ID; the producer's two task-local pending quantities stand: (1) this static review, now delivered with the F contract items; (2) one separately registered capped formation/check batch, not authorized here.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly xmodel/f10-middle-real-code-gate-fable5-20260910.md and box/f10-middle-real-code-gate-fable5-20260910/input_custody.md, both absent at first action; apply_patch only; no Seal, no charge_basis, no source edit, no fixture, no subprocess, no rerun of any accepted report.

## Completion

Authoring attestation: both owned files were written by apply_patch only (custody file and report body 09:26:04 UTC, this section afterwards); no Write/Edit tool, no heredoc redirection to any file, no helper inspection. Own WHOLE read of this report (66 lines before this section) and of the custody file completed 09:26:19 UTC with a marker-absence check (zero markers in both files) and a 64-hex-token audit of both owned files against the live sha256sum output of the 18 inputs (zero foreign tokens; the 18 full tokens live in the custody file, the report carries only abbreviated forms). Raised-OPEN check (none) and collision check (EMPTY) precede the marker and the 09:28:50 reserve. No follow-on, computation, registration, seal or charge_basis is authored. The marker below is the only standalone marker in this file and nothing follows it. All writers idle at the marker.

<!-- BODY-END -->
