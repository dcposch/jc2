# F10 r1 retained-row code: bounded hostile static gate over the degree-seven algebra

2026-09-09, Fable 5.1 (claude-fable-5-1). STATIC REVIEW ONLY. Launch 16:56:42 UTC; controlling deadline 17:16:42 UTC (launch+20 min, earlier than 17:20:00 UTC). No compilation, import, execution, mathematical subprocess, CAS, script, test or arithmetic replay of any size occurred. Runtime tests = 0, transformed artifacts = 0, control harness supplied = 0. Nothing here is a runtime acceptance or an execution authorization.

## Custody and read scope

All eleven charged inputs were hashed in /tmp/jc2-lane.0jIs3l/inputs with sha256sum in the charged order and every digest matched the ordered list in the charge (11/11) before any input was read. Inputs 1–10 were read WHOLE with cat -n. Input 11 (exact.json, 88,634 bytes) was read at schema/variable/key/row-shape scope with grep/head text extraction only, no JSON parser, no coefficient body read; whole-file hashing of input 11 is custody, not a WHOLE mathematical read. No provenance, pending peer, corpus, process census, network, AWS, SSH or agent was followed or used. Inputs 8/9 are treated as accepted 17b proof/gate at their algebra and full-source interface scope and input 10 as accepted 16r; none is re-proved here. Owned outputs: this file and box/f10-r1-retained-row-static-gate-fable5-20260909/ only; no charged code was modified.

## A. Representation faithfulness: STATIC CONFIRMED (no defect found)

Ring and axes. transformer.py:12 declares the width-12 axis order u,ell,d0,v0,v1,k1,k2,k3,s,S,t,v and checker.py:19 the identical list; both bind the original order u,ell,d0,d1,v0,v1,v2,k1,k2,k3,k4,omega (transformer.py:13, checker.py:20) and refuse unless the artifact's own "variables" list equals it (transformer.py:31, checker.py:39). Input 11's declared variables list is exactly that (grep, schema scope). Width-13 wires carry S at index 12 and both codes map that axis to atom(9)=S (transformer.py:153, checker.py:160). s (axis 8) is the only signed axis (checker.py:127); v is reduced to 0..6 by both reductions.

Modulus. transformer.py:63-67 builds p = 24R^2 + (280-1344v)LR + 49EL^2 from ascending dense lists E=[39,-360,960,-512], L=[25,-144,192], R=[-195,2016,-6720,7168]; checker.py:70-74 builds the same from independent sparse atoms. Both literal polynomials agree with accepted input 8 (7) and input 9 section B (E, L, R signs and constants checked term by term by hand). transformer.py:68 forces exact degree 7 and checker.py:76 forces the exact v^7 lead; checker.py:136 demands equality of the UNREDUCED modulus dict, and its own arithmetic never uses the artifact modulus (checker.py:78-94 build lower_modulus from its own p). A changed modulus therefore cannot define the checker's arithmetic. CONFIRMED.

Inversions. transformer.py:74-82 is an extended Euclid in Q[v] with invariant t_i*a = r_i (mod monic); it refuses unless the terminal remainder is a nonzero constant (line 79: unit in the product algebra, not a field assumption) and re-multiplies to 1 (line 81). Only L (84), w (86) and f5 (101) are inverted; `divide` divides only by leading coefficients in Q (line 59) and `monic` by the rational lead (69). No coefficient of the finite algebra is divided, no factor is selected, no denominator chart is dropped. w = R*Linv/112 (85) matches input 8 (8). CONFIRMED.

f5. transformer.py:88-100 enumerates (i,j,l) with i+2j+3l=5, l<=1, j<=2: (5,0,0),(3,1,0),(1,2,0),(2,0,1),(0,1,1), coefficient (7/4)_(i+j+l)/(i!j!l!) with v^j w^l; fall[0..5] holds (7/4)_0..(7/4)_5 (lines 88-90), enough for (5,0,0). Hand values: -945/1024/120 = -63/8192; 105/256/6 = 35/512; -21/64/2 = -21/128 (twice); 21/16. checker.py:148-150 hard-codes exactly these five terms, so the producer's loop and the checker's literal are independent and agree; checker.py:151 compares the artifact f5 to the literal. CONFIRMED.

Mapping. transformer.py:148-153: d1 -> v*w^-1*s^-1, v2 -> w^-1*s^-2, k4 -> w^-1*s^-3, omega -> w*f5*s^8, eight lower parameters and S unchanged; checker.py:156-160 rebuilds the same from its own v, artifact winv (verified by product at 143) and its own f5. Signs and scale match input 8 (4) and the guard omega = w*s^8*f5 of input 8 section 4 / input 9 section C. No s=1 normalisation; Laurent powers are kept as signed exponents, never cleared (transformer.py:182 emits sorted signed exponent vectors; checker.py:127 accepts negatives on axis 8 only). checker.py:153 additionally tests 6144w^2+(640-3072v)w+E = 0 in B, which is the E6 identity of input 8 (5) and pins w to the correct root class of the eliminant, not merely to a solution of 112Lw=R. CONFIRMED.

Rationals. Both parsers demand str numerators/positive str denominators that round-trip through int and are reduced (transformer.py:166, checker.py:29); no_float rejects any float anywhere (transformer.py:24-30, checker.py:32-38, JSON floats and NaN/Infinity both parse as float). Output uses Fraction numerator/denominator strings (transformer.py:182,185) with allow_nan=False. No binary64 path exists. Python's int/str conversion limit is not raised (correctly documented as NONDECISION, not proof of precision). CONFIRMED.

## B. Slot survival and independent read-back: STATIC CONFIRMED (no defect found)

Inventory. transformer.py:187-192 pins the literal 20 IDs (E1/S0..S8, E0/S0..S9, GUARD/omega*a*b-1) against the original, marks exactly E1/S8, E0/S9 and GUARD IDENTICALLY-ZERO, refuses unless those three substitute to the empty wire, and lists the other 17 as RETAINED without asserting nonzeroness. checker.py:235-238 repeats the 20-ID equality on BOTH artifacts and the 17-ID retained list; 246-247 checks each status; 253-254 checks the three zeros as polynomials. Input 11's rows carry exactly those 20 IDs in that order (grep). Zero coefficient slots: transformer.py:199 substitutes every slot wire including empty ones (input 11 encodes zero as [] per its coefficient_format string); checker.py:210-213 demands envelope+1 slots per coefficient, equality of each slot to the S-projection of the substituted coefficient, and equality to the independent substitution of the original slot. CONFIRMED.

Independent substitution. checker.py:165-177 is its own substitute() over its own mapping (156-160), flat raw_mul plus long reduction (60-97), repeated multiplication for powers (99-104). It is applied to A and B (208), every slot (213), every forcing (233), both residuals (243), every row (249) and the three restoration wires (282). It does not import the transformer and shares no arithmetic. CONFIRMED.

Full reconstruction, not inferred from IDs. checker.py:195-198 rebuilds A from the accepted boundary form f=Sd-u, h=1-ud+Sv, k with k(0)=0 (input 10 (3)) and compares to the substituted original A (201-202); 203 pins B5=S^2; 209 enforces the source envelopes deg<=4-i, 7-i and t-free coefficients; 214-215 checks the three gauges as actual coefficients. 216-221 form the full Jacobian A_S B_t - A_t B_S against Delta from the factored Pi (input 10 section 2); 222-224 bound t<=7 and force the six upper bands t^2..t^7 to vanish, 239-243 extract the t^1/t^0 bands with S-envelopes 8/9 (6r+2, 7r+2) and compare them to both the artifact residuals and the substituted original residuals; 250-252 compare each retained row to the band coefficient at its S_degree, so a permuted S_degree label would be caught. 229-233 recompute the five forcings by removing the diagonal E_j B_j = jB_j - 3SB_j' from the independent determinant and compare to both artifact and original. That is all eight t-bands and all 52 bracket slots (227). By hand I checked the t^0 band sign: [A,B]_0 = k'B_1 - hB_0', Delta_0 = 1, so residual = J - Delta matches input 10 (12). CONFIRMED.

Ten inverse-pole slots. I enumerated the envelope-possible poles under S=pz^3-z^2+uz, t=z^-1 by hand: for A (t-degree j<=3, S-degree i<j) the negative z-orders are -3,-2,-1 with p^0 only; for B (j<=5) they are -5,-4,-3,-2,-1 with p^0 and additionally -2 and -1 with p^1 (from S z^-5, S z^-4, S^2 z^-5). That is exactly the list at checker.py:256 and in input 11's pole_slots. 258-269 substitute directly (no generic u, no sampled boundary), keep the negative-z part term by term, which commutes with normal() because normal() touches only the v axis; 271 rejects any pole outside the list; 272-273 require each listed slot to vanish. Reusing axes 9/10 as p/z is safe because 209 already forced t-exponent 0 on every coefficient and 267 zeroes the S exponent before multiplying. CONFIRMED.

Leading and guard restoration. checker.py:274-279 test atop = a, atop*(w s^3) = 1, btop*(f5 s^5) = 1 and omega*atop*btop = 1 as polynomial identities in B[s,s^-1]; 280-282 bind the saved a/b/guard wires to those values and to the substituted originals. These are tests, not inferences: if the accepted identity b = 1/(s^5 f5) failed in the algebra, line 278 refuses. Coefficient slice identified honestly: input 11's restoration b wire has five width-13 terms (v2k4 736/819, d1v2^2 -4022/23205, d1^2k4 -2944/23205, d1^3v2 4384/69615, d1^5 -32/4641), all of T-weight 5; by hand from input 8 (2) the d1^5 coefficient is -10*16/(1105*21) = -32/4641 and the v2k4 coefficient is 20/273+52/63 = 736/819, so the frozen source's b is D5 of the accepted recurrence. The guard wire is -1 + omega*k4*b with the same five coefficients.

Blind spots examined and closed. s/S/t/v: the checker names the generator v and the polynomial vp; s is axis 8 and S axis 9 throughout; no code path confuses them. Silent zip truncation: every zip is preceded by an explicit length or ID equality (mapping 161, A/B 199, slots 206-210, rows 237, measures use the same pinned rows); zip(data["mapping"], mapping) deliberately stops at 12 of the 13 mapping entries. Parser holes: read() demands strict sorted order, width 12, int exponents, Laurent only on s, v<7, canonical nonzero strings; dense() rejects trailing zeros; read_old() demands nonnegative width-12/13 exponents and no explicit zeros (input 11 has none, grep). Shared blind spot: the mapping list is hand-typed identically in both files; it is pinned by the variables-list demand, by the A parametrization built from role symbols and compared to the substituted original A (which forces d1,v2,k4 to be the S-coefficients of d,v,k), by the E6 identity and by the restoration identities. A wrong role assignment in input 8 (4) itself would not be caught, but (4) is accepted (input 9 section C).

## C. Entrypoint authority: STATIC CONFIRMED with one minor GAP (double read of the authority file)

Order. transformer.py:209-212 and checker.py:299-302 check argv length then call authorize() before any hashlib/json import and before any mathematical import; fractions/math are imported only inside transform()/verify() (transformer.py:17-18, checker.py:16-17). Top-level imports are sys, pathlib and the helper (hashlib, json, os, socket, sys). CONFIRMED.

Helper reuse. execution_gate.py is used unmodified at its accepted limitations: enabled must be true (line 19), operation label build/check (23), EC2 DMI host/instance (25-30), -I -B flags (31), literal child argv (33-36), full parent argv equal to the exact CAPRUN runner argv with string caps (37-57), cwd (56), executable pins for script, helper and runner (62-65), the check-side ORIGINAL pin (66-67), and a hash pass over every registered file (68-70). Known accepted limitations, not re-hardened here: the helper hashes itself only after it has been imported; caps are compared as literal strings, not bounded numerically; /proc parent cmdline is a snapshot; admissibility_sha256 is presence-only. I did not compare this copy against the accepted helper's bytes because that file is not a charged input; byte identity rests on input 1's statement.

Pins. Source: constant SOURCE_SHA (transformer.py:11, checker.py:12) equals input 11's charged hash 168bdfd3...; the transformer requires the registration pin to equal it AND rehashes the bytes (216-221); the checker does the same and additionally requires the RETAINED hash registered and matching (307-311) and the producer source pin to equal the artifact's implementation_sha256 (313-315). Output creation is exclusive via open(...,"x") in both (225, 321). authority.DISABLED.json has enabled=false, no host, empty argv, empty caps and empty pins, so authorize() raises "no active registered authority" at line 19-20 before any host or file check. Zero dispatched authority. CONFIRMED.

GAP-C1 (minor, hardening only, not mathematical). Both mains read the authority file twice: once inside authorize() and again at transformer.py:215 / checker.py:305. The second read is unpinned, so a writer with access to the authority path between the two reads could supply different file_sha256 entries for the second read. On the build side the effect is nil (the constant SOURCE_SHA is re-enforced at 217-221). On the check side the second read supplies the expected RETAINED hash (310) and the producer pin (314); the registered-file hash loop in authorize() would not see a swapped table. Smallest exact repair, confined to the two mains: rehash the bytes read at 215/305 and require equality with custody["authority_sha256"] before using spec. Two lines per file; no helper or CAPRUN change. Affected claim: "both source/artifact/producer pins bound" holds against artifact tampering but not against a concurrent authority swap. Requires root-level write access to the authority path, so it is not a blocker for a root-supervised single run.

Registration remains outstanding. Nothing here registers a caller, host, cap, control or physical path; no old run authorizes these child paths (helper hash equality proves only literal identity of the helper).

## D. Typed statistics, mutation plan, minimal harness: STATIC CONFIRMED with GAP-D1 (two designed controls cannot reach their target check)

Statistics. checker.py:284-294 report per row: original term count and total degree over the 12 original variables (omega, d1, v2, k4 at degree 1), original max rational bit length, retained term count, max degree over the eight lower parameters only, min/max signed s exponent, max v degree, max retained bit length. The names keep the notions distinct; nothing compares them as one degree, and no speedup, size forecast or solver claim appears in code or documents. Fewer variables is not asserted to help. CONFIRMED.

Mutation reachability (order of checks: schema 39-41, modulus 136, unit identities 141-153, mapping 163, A form 202, coefficient substitution 208, envelopes 209-213, gauges 215, bands 222-224, slot map 227, forcing 233, IDs 237-238, residuals 242-243, rows 249-254, poles 271-273, restoration 276-282). After a fresh registered hash:
- WRONG-MODULUS reaches "wrong modulus" (136) before any arithmetic uses the artifact. WRONG-INVERSE reaches "L/w/f5 inverse identity" (141/143/152). WRONG-SCALE reaches "retained scale/parameter mapping" (163) or, if propagated, "full A boundary parameterization" (202). DROP-ROW reaches "all20 original slots" (237); its constant-term variant reaches "full low bracket" (242) before the original comparison. PRECISION: a float is rejected by type at 38; a >2^53 canonical integer is parsed exactly and then fails "original row substitution" (249). LAURENT: negatives on u or S fail the domain demand (127); a clamped genuine negative fails 249 or the sort demand (128). OMIT-POLE entry removal reaches "all ten pole slots" (257). OMIT-LEADING via B5 reaches "leading B coefficient" (203). All semantic. CONFIRMED.
- GAP-D1. The "genuine inverse pole" control and any negative control of the full-bracket band checks cannot be reached by mutating the retained artifact: A/B are compared to the substituted frozen original at 202/208 first, and the original is pinned by the constant SOURCE_SHA, so a payload that passes 208 but carries a pole would require an inconsistent original. Those checks are consistency tests of the frozen source, valuable but only positively exercisable. Also OMIT-LEADING by removing the saved a or b slot raises KeyError at 281 (uncaught, no demand string). VALIDATION.md should say so rather than promise a pole-test rejection.

Minimal missing harness (requirements only, nothing written here): (1) a per-control authority generator that registers fresh hashes of transformer.py, checker.py, execution_gate.py, the CAPRUN runner, the original and the mutated retained payload, one child at a time; (2) a payload mutator that edits JSON text only, no arithmetic; (3) an expected-reason table keyed on the exact demand strings above, asserting the reason and the nonzero exit, with uncaught exceptions counted as refusals; (4) a positive control equal to the genuine artifact receipt; (5) CAPRUN telemetry capture; (6) an explicit statement that the pole and band negatives are out of reach without a control ORIGINAL and a checker variant. No generic infrastructure beyond that.

## E. Qualified readiness: READY for ONE bounded transformation plus full read-back test, not a solve

Verdict. No mathematical or correctness blocker was found in A-E. Every identity the code relies on (top rows zero, guard zero, b*f5*s^5 = 1, E6 = 0, unit products) is tested at run time, not assumed, so a failure surfaces as a named refusal. Readiness is qualified by: C registration outstanding; optional GAP-C1 repair; GAP-D1 wording in VALIDATION.md; caps chosen by root.

Hidden behind UNEXECUTED, none mathematical: (a) the frozen original's sign conventions (rows = J - Delta, forcing = Q_j of input 10 (5)) are verified only at run time; a mismatch is NONDECISION, not a code bug; (b) checker cost: substitute() recomputes powers per term by repeated multiplication and the full Jacobian of the substituted A,B is a raw product of dictionaries with thousands of terms times the seven-element v basis, so the checker is likely the slower child; my estimate, explicitly an estimate with no measurement, is under 15 minutes wall and under 2 GB RSS for both children; (c) Python's int/str limit and any KeyError/IndexError are refusals without demand strings; (d) the retained artifact records the modulus and the unit s but no explicit rows p(v)=0 or s*s'-1; any downstream consumer must adjoin both, which is a consumer contract, not a defect of this packet; (e) syntax has not been compiled; I found no syntax or API error by reading, including Fraction/int mixing, dict/tuple widths and the "x" open modes.

Smallest exact repairs. GAP-C1: rehash the second authority read against custody["authority_sha256"] in both mains (two lines each). GAP-D1: edit VALIDATION.md OMIT-POLE/OMIT-LEADING wording; no code change. No silent edit was made and no new representation is proposed.

## Custody table (generated from sha256sum output, charged order)

| # | input | sha256 | match |
|---|-------|--------|-------|
| 1 | f10-r1-retained-row-code-astra-20260909.md | 32145434ada72a415c454a1bfe3b96e38ff6380338d88746a174b348f10d2279 | yes |
| 2 | f10-r1-retained-row-code-astra-20260909.md.artifact.json | 7b17d4a4c13a86c803d5b0779b2dabaeaa7a22fa926a5421a0690610b722da0e | yes |
| 3 | transformer.py | ff5d08c8b8f0ec30b071893cc9cf99db3a3a56a30bb54d653a19c0a8dd5df869 | yes |
| 4 | checker.py | 7d80b261df626fa84ab72d7ff06e3c37e1ba6f6e98e40d6e7d2142d2397aacda | yes |
| 5 | execution_gate.py | cbfe55ff11503cee094dc49e54b656d209806aa37ee30cc281902902152049c6 | yes |
| 6 | authority.DISABLED.json | a3c9f2c94e5591c40ed6d65ede39eb539951a802773dd9ecb3b17153fb8f5498 | yes |
| 7 | VALIDATION.md | 5840e4ceced654c4a2b66ae45251e6977d52e6c3c035233778910323e675906e | yes |
| 8 | f10-r1-leading-coefficient-compression-astra-20260909.md | ead39705189f7faf338bab7937488e96c3ddf296c9ba0eed328ea63442619fb7 | yes |
| 9 | f10-r1-leading-univariate-gate-fable5-20260909.md | 1708746e6d54d745b7a33df54dd339b197df7ec33b1a67c79877ab2429e6946e | yes |
| 10 | f10-whole-mate-euler-elimination-astra-20260909.md | a5ab487c4619742b6252e2503488eea4d6e927b58fd2821207cc85bcba7cf8d5 | yes |
| 11 | exact.json (schema scope only) | 168bdfd3a791f24a6631de66ae4e71c3f209ee9eb0d21fc466f9aa802f5ad576 | yes |

The same eleven lines are in box/f10-r1-retained-row-static-gate-fable5-20260909/input-custody.sha256, written by redirecting sha256sum output. Input 2's artifact.json records body 8433 bytes, body sha 7dcec64a..., full sha equal to row 1; consistent with input 1's seal. Frozen basis of inputs 1, 8, 10 is 0d39df3c9fd69c939a8420c54d03228b9077777d, equal to the HEAD I recorded.

## GAPs, limitations, OPEN

Summary verdicts: A CONFIRMED; B CONFIRMED; C CONFIRMED with GAP-C1 (minor, hardening); D CONFIRMED with GAP-D1 (two designed controls unreachable, one uncaught KeyError); E READY for one bounded transformation plus read-back, qualified as stated. No runtime test pass, emitted row, ideal unit/properness, point, exclusion or JC2 claim is made. No Seal and no charge_basis are authored.

GAPs left open: (i) byte identity of execution_gate.py with the accepted helper was not verified by me (not a charged input); (ii) no compilation, so syntax is unverified beyond reading; (iii) runtime sign conventions of the frozen original are unverified until the checker runs; (iv) wall/RSS are estimates only; (v) the retained rows' term counts, degrees and bit lengths remain unmeasured; (vi) own-only collision check, not an exhaustive novelty scan.

Limitations of this read: inputs 1-10 whole with cat -n; input 11 by grep/head at schema, key, variable, wire-width and pole/ID/slot inventory scope plus the restoration b/guard wires (eleven coefficients) as an honestly identified slice; no coefficient bodies of rows, A, B, slots, forcing or residuals were read. Tools: sha256sum, ls, date, wc, grep, head, awk/cut/sort/uniq on text, mkdir, git rev-parse HEAD, and the Write/Edit tools for this report. No CAS, script, import, compile, test, arithmetic replay, process census, network, AWS, SSH, agent or corpus.

## OPEN(S) RAISED

- OPEN[F10-R1-RETAINED-CODE-RUNTIME-READBACK] QUANTITY: checker verdict on the genuine transformed artifact, value <= 1 PASS-REPRESENTATION receipt or exactly one named refusal string, together with the per-row retained term counts, lower degree, signed s range, v degree and bit lengths of the receipt. CHEAPEST TEST: one root-registered CAPRUN transformer run followed by one registered checker run on the same host, after the C registration and optionally the GAP-C1 repair. WALL: <= 15 minutes for both children, an estimate with no measurement.

## COLLISIONS

status: EMPTY

- NONE — own-only raised-OPEN check (this report and its box only); no corpus scan.

## Own whole-read and completion

Own whole-read at 17:08:17 UTC with cat -n: sections custody/read scope, A, B, C, D, E, custody table, GAPs/limitations, OPEN(S) RAISED (one entry with quantity, relation token, cheapest test, estimated wall), COLLISIONS. All eleven 64-hex tokens in this report were cross-checked against the owned input-custody.sha256 by comm in both directions (11/11, none unmatched) and the 40-hex basis equals HEAD. No placeholder remains. No Seal, no charge_basis, no marker before this line. Completed before the 17:16:42 UTC deadline; all writers idle; no execution authority is created.

<!-- BODY-END -->
