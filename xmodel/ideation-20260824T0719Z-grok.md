# Blind full-spectrum ideation — Grok 4.6 — `20260824T0719Z-c17bd25`

Status: **FROZEN BLIND SUBMISSION**. Independent of every other
`xmodel/ideation-20260824T0719Z-*.md` file. No shared ledger, packet, code,
or artifact was edited. No work is launched. No claim is promoted.

- Lane: xAI/Grok ideator
- Packet: `xmodel/ideation-20260824T0719Z-packet.md`
- Clean basis named by the packet: `c17bd2542b40f3178ec619ae4a73501550555336`
- State cutoff: `2026-08-24T07:19:08Z`
- Read scope: exactly the packet's required inputs, including all 46 rows of
  `APPROACHES.md`

Evidence labels used below:

- **FACT** — theorem, replayed identity, or dual-confirmed scoped result
- **CONDITIONAL** — exact deduction from a named extra hypothesis
- **HEURISTIC** — plausibility, ranking, or analogy, not a proof
- **TEST** — proposed experiment or paper gate, not yet run

---

## 0. Hash verification

Every required SHA-256 was recomputed on disk before any mathematical
judgment. All 18 listed digests matched the packet table.

| Input | Packet SHA-256 | Result |
|---|---|---|
| `COORDINATION.md` | `ad6388abed8cf38f9a3688525dc976b4dd249e0ef1ff072856baf0a0591942b3` | match |
| `APPROACHES.md` | `5677947851abee833c56c520ba1ba14795d0aaa6844678efa47fff537fe2e044` | match |
| `AUDIT.md` | `7300920f91bbd0e2aca9544ee46e32fcf5f67e2a4c9587c28d0fe01f6796c31b` | match |
| `PROGRESS.md` | `861920ac07500e96ef9f2a8f3f0b5a54f777c39c94e22cfbe53faf85ac37e057` | match |
| `notes.md` | `bcefda385088b59242dab495b70994dd903db42f370c72ea1a604cf27e6d7c49` | match |
| `xmodel/ideation-20260824T0453Z-synthesis.md` | `76d9b7685771f65138fcb3c3dba1cfdd1ab104a9a5b21a7e3262c971f96ff790` | match |
| `xmodel/ideation-20260824T0453Z-dedup.md` | `d100616a66115ab0fa1dedc49bfbee910b8d70a0b915268c8e974a197a11747b` | match |
| `xmodel/ideation-20260824T0453Z-packet-erratum.md` | `7dfd30f0f9b7fb95bdc6c61598b69bc33a418b4281f6198df1a6066dcf39d47f` | match |
| `xmodel/exact-coframe-gate-20260824.md` | `8aad8b60777fb7d52fb037df961f1204f255ee36f1775a3f8c772d1739691571` | match |
| `xmodel/exact-coframe-gate-20260824-erratum.md` | `6b71a5bdae1af77ab49a4adae0a9041e1fd1193f5de84ecec65a9b5225b8894e` | match |
| `xmodel/exact-coframe-gate-review-grok-20260824.md` | `f3ad02d9e104692b5e066a4926c10f92c369445a6dd47eea4e6dba22a226caea` | match |
| `xmodel/exact-coframe-background-review-grok-20260824.md` | `1fa8b5cc89d35163ad0aa43e1eb0a2fc3f7f1f6a28cbcf2085af45a0dca2e553` | match |
| `xmodel/completion-pair-gate-20260824.md` | `d5027984be4ae4dbe4d0b5f95161d6d57d71ed9b3c086dd6fce2f03190e6c1bc` | match |
| `xmodel/completion-pair-rank2-review-grok-20260824.md` | `ab540f10a2431fd2a59c88cea983cebbd8883e5777e74d7f0a415eff0b7fc952` | match |
| `xmodel/weighted-d-source-gate-20260824.md` | `04047377c778ababf07e847f5a72d39787dd452e284bc01d83538b5131f7d08a` | match |
| `xmodel/weighted-d-source-review-grok-20260824.md` | `266e30b1c12f7cbc9a60a86ad1b450d5ba23d9db27f307a22be04f01c99da07e` | match |
| `xmodel/k3-local-preflight-20260824.md` | `579bac61491fa4d33a2bc0d454be83f7207fd44595e88bc6240190f5f022416e` | match |
| `cases/k3_local_preflight_20260824/replay_expected.py` | `7a3a1252e0d1e8ef8de9b3065fea3063e0df131dabcfa565c0240504f2d43001` | match |

No other `ideation-20260824T0719Z-*.md` file was opened. The 04:53Z
packet-erratum was read only as a required input; it records a previous-round
provenance typo and is not a mathematical delta for this snapshot.

---

## 1. Disposition vector, avenues 1–46

Legend: `U` unchanged, `R` raise, `L` lower, `O` reopen. Reasons are required
for every non-`U` entry. Overlay scores in `APPROACHES.md` are historical;
this vector is a post-delta ranking vote, not a promotion.

| ID | Call | Reason if not `U` |
|--:|:---:|---|
| 1 | U | GGV farm still has no degree/td ceiling. Park/Wright constraints may shrink supports later; that is a test, not a present raise. |
| 2 | U | Hybrid still owes `G2-PSC`; all versions still owe landing/coverage and a cofinal type bound. Four stopped gates did not repair those. |
| 3 | U | Strip/ODE theorem remains scope-limited; Żołądek priority unchanged. |
| 4 | L | **FACT:** weighted-D gate is `NO-TYPED-SOURCE/NO-QUOTIENT`; D-state is `NO-TYPED-STATIONARITY`; P4P1 is `ORIGIN-ONLY`. Two consecutive non-informative full-source representations. No band 28, Hankel, quotient, or deeper D is licensed. |
| 5 | R | Tameness is now a load-bearing *obstruction* (Wright + Jung–van der Kulk: det-1 automorphism Jacobians lie in `E_2`), not only a descent tool. Complementary to the closed one-shear family. This is not a licence for low-degree coframe search. |
| 6 | O | Previously tried on the wrong object (multi-place residue-A fibers). **FACT:** the Broughton polynomial `P=x+x^2 y` is unimodular, not a coordinate, and admits no complementary `Q` with `[P,Q]=1`. That is the correct one-polynomial object for coordinate recognition. |
| 7 | U | `A(F)` remains unconstructed. Identifying it with a ZMT discriminant is a restatement until a principal relation appears. |
| 8 | U | Formal-inverse combinatorics still lack a truncation bound. |
| 9 | U | Conjecture E remains an unbounded implication ladder. |
| 10 | L | Stronger-conjecture adjacent to the Zhao massacre. Quintic module is a cheap *reserve source-replay*, not a critical-path root. Unbounded HC4 expansion stays held. |
| 11 | U | Dead. |
| 12 | U | No integer to send to infinity. |
| 13 | U | Strictly stronger than JC2; deg-4 black hole. |
| 14 | U | No candidate non-surjective `A_1` endomorphism. |
| 15 | U | Dictionary incomplete. |
| 16 | U | MICRO-DEFECT still lacks a Keller-specific index that survives automorphisms and the 3-dimensional CE control. The thick-line Fitting identity is an input, not a positivity theorem. |
| 17 | U | Plane-origin subclass still unrecognized; genuine CEs exist in the cubic-linear class in higher dimension. |
| 18 | U | Shaska closed graded plane Keller maps as a CE hunt. |
| 19 | L | Unrestricted finite Witt is stopped for this seed. **FACT:** compatible lifts exist at every level with linearly growing support and a nonpolynomial limit. K3 is `COMPILER-READY/HEAVY` and Moh-negative as a CE hunt. Bounded-support/mixed-cancellation is a redesign, not a raise of the current representation. |
| 20 | U | Known bridge consumed false PC(2)/JC(4). |
| 21 | U | Transfer still needs a uniform complexity bound. |
| 22 | U | Wrong direction for Siegel; no effective smallest-CE bound. |
| 23 | U | Holomorphic analog false; Fatou–Bieberbach. |
| 24 | U | Pinchuk `J` nonconstant; SOS orthogonal to complex emptiness. |
| 25 | L | Raw passports already `COSTUME`; 169 residue-A passports all admit transitive tuples. Compactification-side Hurwitz data remain too generous without a second-coordinate/Jacobian pairing that is not a restatement of finiteness. |
| 26 | U | Still no proved ramification-support bound. Orevkov's `N≠2,3` is not a cofinal `td` bound. Enumeration without a bound stays forbidden. |
| 27 | U | Admissible diagrams are not algebraic realizability. |
| 28 | U | Euler/genus ledger still the predicted non-result; BMY needs data. |
| 29 | R | Hamiltonian slice `X_P(Q)=1` is now an effective obstruction (weight recurrence), not a slogan. Local nilpotence with a slice remains close to JvdK; the *absence* of any slice for explicit unimodular noncoordinates is the new handle. |
| 30 | U | ML invariant does not see the embedding. |
| 31 | R | **FACT:** rank two is a known Galois/quadratic theorem. **FACT:** Orevkov Theorem 1.1 also excludes generic mapping/function-field degree three. The live finite-normalization frontier is degree `≥ 4`. This is not a rank-three descendant. |
| 32 | U | Saturated collision ideal remains the injectivity axiom in costume unless a new Fitting handle appears. |
| 33 | L | Polar action-residue charge is `COSTUME` as a raw passport. The nominee's `dt/t` class modulo a PIN ideal still compactifies an exact 1-form whose stable outputs were already J=1 consequences. Algebraic primitives remain usable as a *different* object (Card A), but the polar program as specified should not be a root. |
| 34 | U | No spare coordinate to absorb a pole in dimension two. |
| 35 | U | Known 3-dimensional mechanisms remain structurally 3-dimensional. |
| 36 | U | Generic sparse search stays held. A Park-constrained support test is a later software diagnostic, not a reopen of the hunt. |
| 37 | U | Char-`p` extras need not lift. |
| 38 | U | Prevariety overapproximates. |
| 39 | L | The requested 10-line class is unstable nonmembership in `E_2`. **FACT:** Wright already makes closed det-1 + non-`E_2` equivalent to a Keller nonautomorphism. Exact coframes refine this row into prior art, not a new cohomological engine. |
| 40 | U | Commutative det-1 does not give free invertibility. |
| 41 | U | Falsified by dimension-3 CEs. |
| 42 | U | Arrow points the wrong way. |
| 43 | U | Collapses to GGV after Aut-reduction; the submitted mixed-Ritt uniqueness claim was false without a gauge. |
| 44 | U | Unvetted prime-`td` claim. Orevkov already kills the primes 2 and 3 as mapping degrees. Do not spend a root; a lemma-1 check on Orevkov's `td=9` remains cheap coordination work. |
| 45 | U | Inverse PDE is tautological; univariate reduction reproduces Żołądek A.7. |
| 46 | U | Formalization is not a discovery route. |

Changed IDs: raise `5, 29, 31`; lower `4, 10, 19, 25, 33, 39`; reopen `6`.
All other IDs: unchanged.

---

## 2. Reranked bottlenecks after the exact deltas

These are campaign bottlenecks, not JC2 theorems. Rank is by whether a
resolution would change the global proof/disproof architecture.

1. **A global polynomial-origin receiver that is not a low-rank restatement of finiteness.**
   The ZMT triple `(X,D,j)` remains the canonical object. **FACT:** rank two
   is closed and classical. **FACT:** generic mapping degrees two and three
   are impossible. Low-rank completion schemas are therefore exhausted
   through three. A rank-independent principal boundary relation, or a
   different global object, is still owed. `G2-PSC` is a separate hybrid
   transport gap and must not be renamed as this item.

2. **Wright's exact-coframe dichotomy is JC2, and one complete non-elementary
   row is empty.**
   **FACT:** for a full det-1 Jacobian, `J(P,Q) ∈ E_2(C[x,y])` if and only if
   `(P,Q)` is an automorphism (Wright weak Jacobian + Jung–van der Kulk;
   primary 1978 body still unread, restatement frozen). **FACT:** every det-1
   completion of the Broughton row is `L(h)C_B`, and `(1+2xy)h_y - x^2 h_x = 6y`
   has no polynomial solution. So JC2 is equivalent to: every closed matrix
   in `SL_2(C[x,y])` is elementary. The remaining mathematical gap is not
   "find a new reformulation"; it is a theorem that *other* non-elementary
   classes contain no closed matrix, or an explicit closed matrix of legal
   degree. Low-degree double-orbit search is an instrument/negative control
   (Moh/GGV).

3. **First live mapping degree is four.**
   After Campbell–Razar–Wright–Bass–Connell–Wright (Galois) and Orevkov
   Theorem 1.1 (`N ≠ 2,3`), the first untouched function-field degree is 4.
   This is a sharper proof bottleneck than "some rank `≥ 3`". It is not a
   licence to reopen rank three.

4. **No absolute/cofinal type or complexity ceiling.**
   **FACT:** unrestricted UCD/K2C is false (Hénon tower). Type-relative
   `DIR ⇔ PC ⇔ RPMC ⇒ KJN` remains single-model and type-relative.
   `A-SCALE ⇒ UCD-A-min ⇒ G2-BD` is a different, residue-A-only chain.
   Fixed-type or bounded-degree kills do not resolve JC2 without an
   exhaustive bridge.

5. **`G2-PSC` versus pure-Sigray landing.**
   Hybrid GGV→Sigray still owes packet/sheet fidelity. Pure Sigray may
   bypass `G2-PSC` but then cannot import GGV restrictions and still owes
   source, coverage, and type control. Residue-A separately owes `G2-BD`.
   Bare `G2` remains forbidden.

6. **Typed full-polynomial D-source.**
   **FACT:** no named `S_full` point and no maps `Dα_1,Dβ_1,Dα_2,Dβ_2`.
   Ambient `(C,R)` formulae at levels 42/84 are acceptance tests, not a
   quotient. Resurrection is a producer that reconstructs those coefficients
   from `φ_f,φ_g`, not another sidecar.

7. **Uniform polynomial complexity for Witt descent.**
   **FACT:** the Artin–Schreier seed lifts through every finite Witt level
   with growing support, and the inverse limit is restricted-analytic, not
   polynomial. The missing criterion is bounded support/degree, or a
   different seed/invariant. Degree three as a characteristic-zero CE is
   independently excluded by Moh/GGV; the K3 compiler is too singular to
   even decide verticality.

8. **Exact characteristic-zero certificates where modular traces are still
   invoked.**
   Standing msolve unit-basis and parser hazards. Not the JC2 critical path,
   but any internal chart proof that still cites first-prime `[1]` is not a
   Q-certificate.

What dropped in rank: exact-coframe *search* as a CE root; rank-two/three
normalization as a live schema; weighted-D continuation; raw passports;
unrestricted Witt levels; polar action-residues as a capacity budget.

---

## 3. Idea cards (three)

Collectively these supply: a new mechanism (Card A), a new cross-avenue
connection (Cards A and B), the strongest proof attack (Card B), the
strongest falsification attack (Card C), and a software/decisive experiment
(Card A, with a cheap Park diagnostic named in §8).

None of the three is the polar `ACTION-RESIDUE-INDEPENDENCE` nominee or the
`HC4-QUINTIC-MODULE` nominee.

### Card A — `HAMILTONIAN-WEIGHT-SLICE`

**Target claim (exact, scoped).** Let
`X_P = P_x ∂_y - P_y ∂_x`. Then `[P,Q]=1` if and only if `X_P(Q)=1`.
For a unimodular noncoordinate `P`, nonexistence of a polynomial slice is a
scoped non-Keller theorem for that first component. The first complete
example is already in hand:

- **FACT:** `P = x + x^2 y` has unimodular gradient, reducible zero fibre
  (hence not a coordinate), and no `Q` with `[P,Q]=1`, because every det-1
  completion of `dP` is `L(h)C_B` and `X_P`-conjugate closedness is the
  nonterminating recurrence `c_n = (-1)^n(n+3)`.

The proposed claim is *not* JC2. It is: there is an effective weight-filtered
obstruction that decides `1 ∈ im(X_P)` for unimodular polynomials whose
Hamiltonian is quasi-homogeneous of weight `+1` with no weight leak, and this
obstruction applies to at least one provenanced Newton family beyond
Broughton.

**Avenue IDs.** Primary 6 (reopened), 29 (raised), 5 (raised). Connections to
1 (GGV first polynomials), 32 (existence of `Q` rather than a collision),
39 (the closed coframe is `dP` plus a slice).

**Novelty.** New mechanism extracted from this round's scoped negative, not
present in the 04:53Z card graph. Previous exact-coframe work searched for a
closed matrix in a Cohn orbit. This card treats the Hamiltonian cokernel of a
*fixed unimodular* `P` as the object, and the weight recurrence as a proof
engine rather than a one-family accident.

**Evidence used.** Exact-coframe gate and different-model review
(`CONFIRMED`); Wright/Jung–van der Kulk dichotomy; Park leading-term
certificate for `C` and `C_B`; Broughton noncoordinate control; Moh/GGV
degree ceiling as a stop against advertising low-degree slices as CEs.

**Dependencies / evidence tiers.**

- **FACT:** Poincaré lemma in characteristic zero; unimodular row ⇔ some
  complementary `SL_2` second row; completeness of `L(h)M_0` for a fixed
  first row.
- **FACT:** the Broughton/Cohn weight calculation.
- **CONDITIONAL:** if `X_P` is strictly of weight `w` with no leak, then a
  target of weight `w-1` can be hit only by the matching weight summand of
  `Q`, and a nonterminating recurrence with nonzero terminal monomial proves
  `1 ∉ im(X_P)`.
- **HEURISTIC:** GGV leftover leading forms or residue-A approximate roots
  admit such a weight.
- **Not assumed:** that the test on all unimodular `P` is cheaper than JC2
  (it is not; that would be a restatement).

**Cheapest discriminator (TEST).**

1. Paper: write `X_P` and the complementary-row curl equation for the two
   standard one-place unimodular noncoordinates of degree three,
   `x + x^n y` at `n=2` (done) and `n=3`, plus one non-quasi-homogeneous
   deformation `x + x^2 y + y^k` at the smallest `k` that preserves
   unimodularity. Stop if the deformation leaks weight and the recurrence
   method has no replacement.
2. Software, stdlib sparse polynomials as in the frozen exact-coframe
   replay: given `P`, compute a particular complementary row from the
   unimodular identity, form `κ = curl(second row)`, and attempt the
   weight-filtered linear solve `X_P(h) = -κ`. Emit one of
   `NO-WEIGHT`, `TERMINAL-OBSTRUCTION`, `SLICE-FOUND`, `INCONCLUSIVE-DEGREE`.
3. Negative controls: `P=x` and `P=x+y^2` must return `SLICE-FOUND`.
   Broughton must return `TERMINAL-OBSTRUCTION`. A random coordinate
   `Φ_1` of a tame automorphism must return `SLICE-FOUND`.

**Outcomes.**

- `SLICE-FOUND` for a unimodular noncoordinate of degree `< 108`, not
  `(72,108)`: treat as a pipeline/Moh-GGV contradiction until the pair is
  shown not Keller. Do not advertise a CE.
- `SLICE-FOUND` at legal degree with independently certified non-`E_2`
  Jacobian: freeze as a characteristic-zero CE candidate (falsification
  jackpot; then Card A stops and review is enqueued).
- `TERMINAL-OBSTRUCTION` on a provenanced GGV/residue-A leading polynomial,
  with a proved no-leak lemma for that Newton weight: scoped family kill,
  independent of book cells.
- `NO-WEIGHT` on the first provenanced family: the mechanism does not
  transfer; stop this representation for that family.

**Cost / time.** Paper half-day for the `n=3` and one deformation; software
gate one to three hours if it reuses the frozen sparse ring. No fleet.

**Stop condition.** Weight leak without a replacement filtration; a slice
that merely restates "coordinates have slices"; any support/degree widening
into generic sparse search; any claim that emptiness of `im(X_P)` for all
unimodular noncoordinates has been proved by this gate.

**Expected information gain.** High per hour. Either the method is
Broughton-specific (then stop and keep the scoped theorem), or it kills a
Newton family, or it produces the first honest complementary-polynomial
search with a fail-closed recurrence rather than a degree guess.

**Resurrection trigger.** A producer that supplies a weight (or filtration)
with a no-leak proof for a named GGV polygon or a named residue-A
approximate root. Not another left shear of `C_B`.

---

### Card B — `MAPPING-DEGREE-FOUR`

**Target claim (paper-first).** No characteristic-zero plane Keller map has
function-field degree four; or else an exact separator naming the
Galois-theoretic or compactification step that fails at four while succeeding
at two and three.

**Avenue IDs.** Primary 31 (raised). Cross-avenue connection: 31 × Orevkov's
compactification/`π_1` method (25/26 adjacent) × the ZMT different/class-group
perimeter already confirmed at rank two. This is the new connection forced
by the deltas: degrees 2 and 3 are gone, so the first unknown mapping degree
is a single finite number, not an open rank ladder.

**Novelty.** Not a new reformulation of JC2. New as a *campaign attack*: the
04:53Z completion-pair root stopped at rank two, and Orevkov's degree-three
exclusion was not in that packet. Rank three is not licensed. Degree four is
untouched and is the smallest remaining non-Galois window (`S_4`, `A_4`,
`D_4` closures; the Galois groups of order 4 are already covered by the
classical Galois case).

**Evidence used.** Completion-pair gate and review (`RANK2-NOGO` `CONFIRMED`,
known); Bass–Connell–Wright Theorem 2.1 Galois case; Orevkov Theorem 1.1 as
frozen in the rank-two review; Vitushkin rational (non-polynomial) degree-two
example as the polynomial-hypothesis control; TRACE-REG as a *different*,
unbounded finiteness equivalent, not to be rebuilt.

**Dependencies / evidence tiers.**

- **FACT:** `[L:K]=2` is impossible for plane Keller maps over `C`.
- **FACT:** generic `[L:K]=3` is impossible (Orevkov). Do not confuse this
  with total polynomial degree three (Wang).
- **FACT:** if `L/K` is Galois then invertibility holds (BCW).
- **CONDITIONAL:** if every non-Galois quartic Keller extension produced a
  degree-three Keller map, or a Galois Keller map, Orevkov+BCW would kill
  degree four. No such reduction is claimed here.
- **HEURISTIC:** Orevkov's compactification/`π_1` obstruction may have a
  degree-four analogue whose failure mode is nameable in one paper pass.

**Cheapest discriminator (TEST).** Paper only, no enumeration, no rank-three
schema.

1. Freeze primary Orevkov 1986/87 and the Galois sources already cited in
   the rank-two review. Extract the exact step that uses `N=3` (permutation
   action on three sheets, braid/π₁ generation, or a trigonal canonical
   class).
2. Replay that step on a hypothetical monogenic quartic
   `R = A[z]/(z^4 + a z^3 + b z^2 + c z + d)` with the confirmed perimeter
   (`R` finite free, `R^*=C^*`, ramification supported on `D`, no principal
   boundary divisor).
3. Return exactly one of: `EXTENDS` (a written degree-four no-go at the
   same standard as rank two), `FAILS-AT` (named step, with a countermodel
   in the formal ZMT category), `GALOIS-REDUCTION` (an exact functor from
   non-Galois quartic Keller to a Galois or cubic Keller object), or
   `RESTATEMENT` (the argument needs finiteness/étaleness of `B/A`).

**Outcomes.**

- `EXTENDS`: strongest currently available proof increment; still not JC2;
  stop this card and enqueue different-model review; do not open degree 5
  in the same generation.
- `GALOIS-REDUCTION` that is exact: reduce to BCW; same review path.
- `FAILS-AT` with a formal (not polynomial) countermodel: degree four stays
  open; do not launch a coefficient search.
- `RESTATEMENT`: stop. This is the registered failure mode of completion
  schemas.

**Cost / time.** One paper-first lane, target a few hours to one day. No
solver, no fleet, no rank-four enumeration.

**Stop condition.** First exact separator, restatement, or any attempt to
open rank three, one-boundary families, or a primitive-group census without
a ramification bound. Do not treat a quartic resolvent as Keller without an
étaleness proof.

**Expected information gain.** Highest among proof attacks in this snapshot.
The campaign just spent a root rederiving the quadratic case. Degree four is
the first place a completion-pair argument could be campaign-new.

**Resurrection trigger.** A published theorem already excluding mapping
degree four (then this card is prior-art intake, not a root); or an exact
non-Galois quartic reduction that does not rest on the conclusion.

---

### Card C — `TATE-HORIZONTAL-MIXED`

**Target claim (falsification / no-lift).** For the promoted odd-prime seed
`(x-x^p, y)` and its exact all-Witt tower
`F_n = (x-x^p,\; y \sum_{j=0}^{n-1} (p x^{p-1})^j)`,
the factor `(1-p x^{p-1})^{-1}` has a horizontal divisor in the Tate algebra
that cannot be cancelled by any polynomial mixed correction of bounded
bidegree in a frozen nilpotent gauge. Consequently this seed has no
uniformly bounded-degree characteristic-zero polynomial lift.

**Avenue IDs.** Primary 19 (lowered as currently executed; this is the
redesign). Connections to 21 (p-adic algebraicity) and 5 (triangular
shears as the restricted slice). Not a K3 continuation and not another
`W_n` level.

**Novelty.** The all-Witt control is promoted and confirmed; it shows that
*some* compatible tower exists with growing support. The new object is the
horizontal divisor of a Tate unit modulo polynomial units, together with a
gauge-fixed mixed cancellation test. This is the UNIT-INFINITY mechanism
from the previous collection, repaired by the uniqueness counterexample
recorded in the 04:53Z dedup (`P = x-x^3+3x^2` over `Z/9` is another
restricted lift).

**Evidence used.** Promoted `W2-SURVIVOR` over `Z/9`; promoted all-Witt
closed form and nonpolynomial inverse limit; K3 preflight
(`COMPILER-READY/HEAVY`, rank 6, kernel 10, first obstruction killed by
`B=x^2 y`); Moh/GGV as the reason degree three cannot be a characteristic-zero
CE even if the generic fibre were nonzero.

**Dependencies / evidence tiers.**

- **FACT:** the displayed `F_n` has determinant `1 - p^n x^{n(p-1)}`, hence
  det 1 mod `p^n`, and the marked collision persists.
- **FACT:** support and degree of `Q_n` grow linearly; the inverse limit is
  not a polynomial endomorphism of `A^2`.
- **FACT:** uniqueness of the restricted lift is false unless `P` is frozen
  or a nilpotent triangular gauge is quotiented.
- **CONDITIONAL:** if every polynomial mixed correction that preserves det 1
  mod `p^n` and the marked collision must introduce a new monomial of
  `x`-degree `≥ c n` or `y`-degree `≥ 1` with coefficient not divisible by
  arbitrarily high `p`-powers, then no bounded-degree polynomial limit
  exists for this seed.
- **HEURISTIC:** the horizontal pole of `(1-p x^{p-1})^{-1}` along `x^p = 1/p`
  (Tate) is the algebraicity obstruction; mixed `y`-terms cannot cancel a
  purely horizontal divisor without creating a compensating pole.

**Cheapest discriminator (TEST).**

1. Paper: freeze the gauge. Restrict to
   `P = x - x^p + p A(x,y)`, `Q = y + p B(x,y)` with `A,B` of total degree
   `≤ p`, and quotient by the triangular nilpotent gauge that produced the
   known extra `Z/9` lift. Do not claim uniqueness outside this quotient.
2. Prove the restricted `P_y=0` slice cannot cancel the horizontal divisor
   (exact triangular no-go). This is the negative control, not the theorem.
3. One mixed ansatz only: lowest bidegree `A` with a `y`-term, same degree
   cap, exact determinant-1 condition modulo `p^2` then `p^3`. Emit
   `NO-CANCELLATION`, `NEW-MONOMIAL-FORCED`, or `BOUNDED-MIXED-SURVIVOR`.
4. Do not run the degree-three local Gröbner compiler. K3 already failed
   Stage-B. Do not invert 3 and claim a characteristic-zero point: Moh/GGV
   forbid advertising it as a CE, and the preflight is too singular to
   certify verticality.

**Outcomes.**

- `BOUNDED-MIXED-SURVIVOR` over `Z/p^2` with no new monomial: reopen a
  *bounded* Witt child in a later generation, still not a characteristic-zero
  inference.
- `NEW-MONOMIAL-FORCED` with an exact support increment: this is the uniform
  complexity certificate for the seed; stop further unrestricted lifts.
- `NO-CANCELLATION` on the frozen mixed ansatz: scoped negative for that
  ansatz only; do not upgrade to all mixed polynomials.
- Any attempt to read a `Z/p^n` survivor as a complex polynomial map: reject.

**Cost / time.** Paper plus a small exact arithmetic probe over `Z/9` and
`Z/27` for `p=3`. Hours, not a fleet job.

**Stop condition.** Gauge not frozen; uniqueness claim without the
nilpotent counterexample quotient; a `W_3` enumerative search; any
K3-unchanged Gröbner; any modular-to-`Q` inference; support expansion past
the frozen bidegree.

**Expected information gain.** Decisive for this seed's CE door, which is
the only presently explicit odd-prime collision. It cannot prove JC2. It can
close the last honest "maybe this tower algebraizes" reading of a promoted
control.

**Resurrection trigger.** A different seed whose first Witt obstruction
vanishes *and* whose support is already bounded independently of level; or a
proved cancellation mechanism that is Aut-covariant, not ansatz-specific.

---

## 4. Nominee adjudication

The delta triage nominated avenue 33 `ACTION-RESIDUE-INDEPENDENCE` as next
bounded root and avenue 10 `HC4-QUINTIC-MODULE` as orthogonal reserve. Those
are candidates, not assignments.

### 4.1 `ACTION-RESIDUE-INDEPENDENCE` — do not launch as specified

**FACT:** `P dQ - x dy` is closed for every Keller pair, including any CE,
by `dP ∧ dQ = dx ∧ dy`. Poincaré supplies a polynomial primitive `S_F` in
characteristic zero. Polar residues of `S_F` therefore exist for automorphisms
and for hypothetical CEs.

**FACT:** the raw-boundary probe returned `COSTUME` for untwisted
action-residue charge and for raw primitive pole orders. Tame and Hénon
automorphisms make those orders arbitrarily large; orbit minimization
returns identity data. The stable Keller-suite outputs were log-determinant
volume orders and zero residues of exact meromorphic differentials —
consequences of `J=1`.

The nominee tries to repair this by taking the first `dt/t` class modulo a
same-order Jacobian/no-log/PIN ideal on a minimal `(2,3)` jet. That is the
right *shape* of a repair (quotient out costume), but it still compactifies
an exact 1-form whose residues were already the costume-stable output. TRACE
and `NORM-MOMENT-SEP` independently showed that contact packets do not fix
quadratic moments; adding a polar class of `S_F` without a covariance and
minimality theorem repeats that failure mode.

**TEST that would be permissible as a coordination-owned negative control,
not a root:** compute `S_F` for the COSTUME suite (identity, `T_n`,
`T_4^{-1}∘T_4`, Hénon `r=0,1`) and one residue-A leading pair *without
going to infinity* — i.e. treat `S_F` as a polynomial and test whether any
Aut-invariant extracted from it survives Hénon degree growth. Stop at the
first covariance failure. This is Card A's symplectic neighbour, not the
polar nominee.

**Verdict:** `lower` avenue 33 as specified. Do not make it Root 1.

### 4.2 `HC4-QUINTIC-MODULE` — reserve source-replay only, not a root

Meng–Yang: HC4 ⇒ JC2 by doubling. Ni: the Hessian conjecture is known for
degree `≤ 4`. The first open homogeneous layer is degree 5. Sol scored this
9/10; this lane's standing objection is the prove-something-stronger
failure mode, now stronger after Long 2026 killed Mathieu/PC(2)/GMC(`n≥3`).
HC4's survival is survivorship until a primary-source freeze says otherwise.

**FACT:** Shpilrain–Yu Proposition 2.4's printed strengthening was false.
That is a warning about importing "furthermore" clauses from implication
papers. HC4⇒JC2 must be replayed from primary text before any module is
built.

**CONDITIONAL:** even a correct implication does not make HC4 easier than
JC2. A quintic obstruction that does not vanish would falsify HC4 at degree
5 and say nothing about JC2. A vanishing first term is weak evidence.

**Verdict:** do not launch as a mathematical root. A bounded primary-source
replay of the implication and of Ni's quartic, owned by coordination, is
allowed by the packet's "may be proposed, not pre-authorized" language. If
the implication is false as printed, stop. Unbounded HC4 expansion remains
held.

### 4.3 Better alternatives

1. **Hamiltonian weight slice (Card A)** — uses the actual new theorem of
   the round, produces a fail-closed engine, and has both a proof reading
   (family kill) and a CE reading (legal-degree slice of a noncoordinate).
2. **Mapping degree four (Card B)** — the proof frontier created by Wright
   being prior art and Orevkov closing `N=3`.
3. **Tate-horizontal mixed cancellation (Card C)** — the only remaining
   honest door on the promoted odd-prime seed, redesigned rather than
   deepened.

These three occupy the proof, new-mechanism, and falsification slots without
selecting either nominee as lead.

---

## 5. Lane calls: continue / redesign / stop

| Lane | Call | Scope of the call |
|---|---|---|
| D-series | **stop** current representations | No typed `T_src`. Do not continue band 28, weighted quotient, Hankel, two-driver, integral D43, or deeper D. Resurrection: a producer that reconstructs `α_m,β_m` from `φ_f,φ_g` through factor level two. |
| GGV/Sigray | **continue paper architecture only** | Hybrid still owes `G2-PSC`; residue-A still owes `G2-BD`; no new book cells; no DIR/A-SCALE census. Books remain book-relative. |
| Boundary / action / TRACE | **redesign** | Raw passports and polar action-residues are costume. TRACE-REG is an honest unbounded finiteness equivalent, not an engine from the current packet. Algebraic Hamiltonian slices (Card A) replace polar residues. |
| Local-bound / DIR / KJN / A-SCALE | **stop expansion** | Type-relative reductions may be banked as single-model partials. They do not yield a cofinal ceiling. Hénon already killed unrestricted UCD. |
| Witt | **redesign, do not continue** | Unrestricted next level: stop. All-Witt growing-support control: keep as a theorem about this seed. Card C is the bounded-complexity redesign. K3 unchanged: stop. |
| Exact-coframe | **stop search; keep the scoped theorem** | Wright is prior art for the dichotomy. The Broughton/Cohn fixed row is empty. Do not run low-degree `E_2 C E_2` as a CE hunt (Moh/GGV). Do not write `SL_2/E_2` as a group. A later double-orbit paper lemma at *legal* degree is a different, expensive gate, not this generation. |
| Finite-normalization | **stop ranks 2–3; paper-only degree 4** | Rank 2 confirmed and known. Rank 3 not licensed and already excluded as mapping degree by Orevkov. Card B is the degree-4 paper gate, one generation, restatement-stopped. |
| External-artifact / SuperMind–Guo | **stop as a proof count** | Promoted at stated artifact tiers only. Not independent of the GGV-Horruitiner bridge. No unconditional degree-125 theorem. Primary Wright 1978 PDF remains unread; freeze it as literature, not as a research root. |
| box01 `build_tails43.py` | **do not stop before a checkpoint** | Operational, not mathematical. Accept orbit-work loss if killed now. |

---

## 6. Hidden assumptions, hostile self-attack, invalidating event

### Shared hidden assumptions

1. The ZMT open immersion and the Wright `E_2` dichotomy are the right
   global objects. **FACT:** they are equivalent to JC2 on the plane. That
   does not make their boundary or closed-matrix loci accessible.
2. Polynomial origin leaves a filtration (weight, Newton, mapping degree, or
   Tate degree) that can be computed before the missing global theorem.
   `NORM-MOMENT-SEP` is evidence that local algebra/different/contact data
   do not contain the polynomial information. Card A can fail the same way
   if the Hamiltonian has no weight.
3. Low-degree automorphisms plus one non-Keller control are hostile enough.
   They catch tautologies; they do not prove plane-specific vanishing.
4. Moh/GGV degree bounds are campaign-baseline. A low-degree closed coframe
   or a degree-three characteristic-zero collision would be treated as a
   contradiction of those theorems, not as JC2. That policy is only as
   strong as the GGV-Horruitiner bridge.
5. Blind convergence in the previous round is not evidence. Four of five
   completed 04:53Z scans shared a model family; this round must not treat
   the nominees as confirmed by vote.

### Hostile attack on the leading proposal (Card A)

The Hamiltonian-weight slice is the most likely of the three cards to be a
**restatement or a one-example method**.

- If the class of `P` is "all unimodular polynomials", then
  `1 ∈ im(X_P) ⇒ P` is a Keller first component, and JC2 says `P` is then a
  coordinate. Proving `1 ∉ im(X_P)` for every unimodular noncoordinate *is*
  JC2. The card is worthless unless it stays inside a proper subclass with
  a uniform no-leak weight.
- The Broughton operator is strictly weight `+1` for `w(x^i y^j)=i-j`.
  **HEURISTIC warning:** a lower-degree tail leaks weight. The terminal
  monomial obstruction need not survive perturbation. That leak is the
  D-series algebraization problem in Hamiltonian language.
- Unimodular noncoordinates of degree three are below Moh. Their slice
  emptiness is consistent with JC2 and does not stress the conjecture.
- A rational slice exists in `C(x,y)` as soon as `X_P` is nonzero (orbit
  method); the frozen rational control `h_{\mathrm{rat}} = y^2(3+2xy)/(1+xy)^2`
  is exactly that phenomenon. The polynomial problem is algebraization of a
  rational/formal slice — the same shape as Witt's nonpolynomial limit.
- Park's leading-term test does not, by itself, produce a weight.

If the first deformation `x+x^2 y + y^k` returns `NO-WEIGHT` and the first
GGV leading form is not quasi-homogeneous for `X_P`, Card A should be
killed rather than generalized.

Card B's corresponding attack: Orevkov's `N=3` step may use trigonal
geometry that is false for quartics, in which case the card returns
`FAILS-AT` immediately and must not spawn an enumerative quartic search.
Card C's attack: mixed cancellation might be ansatz-specific, or the
horizontal divisor might be gauge-artefactual once `P` is allowed to vary
in the nilpotent directions already exhibited over `Z/9`.

### Event that would invalidate this ranking

Any one of:

- Primary Wright 1978 text reversing the weak Jacobian direction, or showing
  an extra hypothesis that a hypothetical CE could evade. (Then exact
  coframes drop from "JC2 reformulation" to "one-way CE certificate" and
  search, at legal degree, rises.)
- A mapping-degree-four theorem already in the literature, or a
  characteristic-zero Keller map of function-field degree 4. (Card B dies or
  becomes intake.)
- A polynomial slice for a unimodular noncoordinate of legal degree.
  (Falsification jackpot; this ranking's proof-first tilt is wrong.)
- A typed full-polynomial D-source producer reconstructing `α,β` from
  `φ_f,φ_g`. (Avenue 4 resurrects and may outrank Hamiltonian weights.)
- Refutation of Moh/GGV at the reduction-bridge layer, producing a genuine
  leftover family below degree 108. (Low-degree coframe and K3 change from
  negative controls to CE hunts.)
- A covariant, non-costume boundary invariant that distinguishes Hénon from
  residue-A. (Avenue 33 polar would rise; this report's lower call on 33
  would be wrong.)

---

## 7. Four-root portfolio

Respects: holds (B=168, D75, band 28 / deeper D, new book cells, DIR/A-SCALE
census, cCa6 F4, unbounded HC4, generic sparse, integral D43, public
communication, msolve disclosure); background review (exact-coframe search
is a negative control at cheap degree; do not write `SL_2/E_2`; Wright
unread from 1978 PDF); one-generation speculative limit (no descendants
until a claim is provisional and different-model review is enqueued); at
most one new dependency generation; box01 not killed before a checkpoint.

No root below is launched by this file. This is a proposed allocation for
the coordinator after collection.

| Root | Share | Object | Immediate gate | Stop |
|---|---:|---|---|---|
| **H — Hamiltonian-weight slice** | 35% | Card A. Fixed unimodular `P`, operator `X_P`, weight-filtered slice equation. | Negative controls + `n=3` + one deformation. | `NO-WEIGHT`; restatement as JC2; low-degree `SLICE-FOUND` treated as a CE; generic sparse widening. |
| **N — mapping degree 4** | 30% | Card B. Paper-first Orevkov-step replay on a monogenic quartic ZMT algebra. | Primary-source freeze of Orevkov 1986/87 and BCW Galois. | `RESTATEMENT`; any rank-3 schema; enumerative quartic covers; primitive-group census without a bound. |
| **W — Tate-horizontal mixed** | 20% | Card C. Gauge-fixed mixed cancellation of the Tate unit's horizontal divisor. | Freeze gauge; triangular no-go; one mixed ansatz over `Z/9` then `Z/27`. | Unrestricted `W_n`; K3 unchanged; uniqueness without gauge; modular-to-`Q` reading. |
| **C — coordination / review / cheap reserves** | 15% | Preserve holds and claim DAG. Optional HC4 primary-source replay (implication + Ni quartic only). Park leading-form classifier as software on already-banked automorphisms and Cohn, not a search. Checkpoint/audit box01. Web sweep #9 by its deadline. Enqueue different-model review at the first provisional. | First decisive H/N/W gate. | Silent promotion; second speculative generation; widening a reserve into a root. |

Replacement order if a root stops: (1) Park-Newton diagnostic on GGV leftover
supports, still not a sparse hunt; (2) MICRO-DEFECT paper gate *only after*
a Keller-specific index term is written that vanishes on Hénon and is
nonzero on a documented 3-dimensional CE, or is honestly plane-specific;
(3) HC4 source-replay if the implication survives primary freeze. Not
replacements: weighted D, rank 3, low-degree coframe search, raw passports,
new book cells, unrestricted Witt.

Portfolio shape versus the nominees: avenue 33 polar is not a root; avenue
10 is at most a C-owned source-replay. The three mathematical roots are a
new Hamiltonian mechanism, the first live mapping degree, and a redesigned
bounded Witt obstruction.

---

## 8. Software acceleration (named, not a fourth card)

A Park leading-form classifier is the cheapest instrument tying Cards A–B
to avenue 1 without generic search.

**FACT (CONDITIONAL on `det J=1` and no cancellation in the leading
determinant, which is forced because `det J` is the nonzero constant 1):**
the leading homogeneous matrix `LF(J)` is singular. For non-linear maps it
has rank 1.

**FACT (Park):** if a matrix is elementary and `LF` has rank 1, one leading
row is a monomial multiple of the other. Cohn fails this test.

**FACT (Wright + Jung–van der Kulk):** a det-1 automorphism has elementary
Jacobian, hence Park-compliant leading rows.

**TEST:** on the COSTUME automorphism suite plus Cohn/`C_B`, emit
`PARK-PASS` or `PARK-FAIL`. Expected: automorphisms pass, Cohn fails. Then,
as a diagnostic only, evaluate the *support* of leftover GGV leading
Jacobians (no coefficient search) for forced Park type. A forced `PARK-FAIL`
support is a coframe-type family; closedness is then Card A. A forced
`PARK-PASS` support looks like Aut leading data and is not a Cohn-type CE
shape.

Do not treat Park-pass as an automorphy certificate (the test is necessary,
not sufficient). Do not run this as a sparse CE hunt.

---

## 9. What this report does not do

It does not prove or disprove JC2. It does not promote `RANK2-NOGO`, the
Broughton one-shear theorem, Wright, or Orevkov beyond their already
reviewed scopes. It does not launch H, N, or W. It does not edit
`AUDIT.md`, `APPROACHES.md`, `PROGRESS.md`, `COORDINATION.md`, `notes.md`,
code, or the sealed packet. It does not communicate externally.

Frozen.
