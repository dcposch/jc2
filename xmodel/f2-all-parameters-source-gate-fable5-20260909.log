cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
Gate complete at 09:20:47 UTC, well inside the 09:30 stop. All writers are idle; nothing runs in the background.

**Result: CONFIRMED on all six targets, overall CONFIRMED as a conditional necessity arrow** at the ordinary rectangular actual-F2 scope, with the named imports retained at their tier. No REFUTED item and no GAP in the necessary direction was found.

**Outputs**
- Report: `xmodel/f2-all-parameters-source-gate-fable5-20260909.md`, SHA-256 `f5b684b1…e403adae6537e`, ends with the standalone BODY-END marker. No seal and no charge_basis were authored.
- Own box: `box/f2-all-parameters-source-gate-fable5-20260909/` holds the eleven input pins with sizes, the output hash, and the read perimeter. Input hashes match the charged list byte for byte.

**What I re-derived by hand, independently of the producer**
- The (0,1) Euler ODE. It forces a linear Euler coefficient with 15mA=1, then (u+r0)f'=5m f, and the same shift for Q from the vanishing top-row bracket.
- The (5,-1) bracket. Both partial derivatives expanded give equation (1) with right-hand side r, so the accepted chain's 1/3 is the m=3 specialization only. Degree count gives deg f=3, f(0)=-1, endpoint (4,16), denominator 5 for every m.
- Both quartic cases by full coefficient comparison. The (2,1,1) residual is a quadratic with three zeros, so the whole ODE holds. In the (2,2) case the cubic term cancels identically and the three remaining coefficients force rho²-3rho+1=0.
- The opposite-edge chain. The common edge follows from the bracket gap plus common powers, positivity on the whole arc from the half-plane argument, integer slopes from the y-degree-1 root, k=1 excluded by the polynomial-stage Corollary 1.6, uniqueness of the Euler element after cuts from ord_y, and i≤4j from the supporting line with slope at least 4 even when only one polygon has that edge.
- The x⁻⁵ cut, T4 exponent map, both H forms, the composite map determinant a·g², and the general scaling exponent 4-5(m+n).

**Observations worth root's attention, none rising to a gap**
- Corollary 5.7 is indispensable. The rectangle alone does not fix Q's (5,-1) value; the producer names this import correctly.
- The standard-pair condition on the lower right corner comes from the (5,-1) face, not from the rectangle. A0'=(1,0) is load-bearing for r(0)≠0.
- Corollary 7.4's proof extracts a qm-th root of a scalar. Over a non-closed field the scalar form of the face power is what is actually used, and nothing downstream changes.
- The producer's claim that only coprime m,n>1 are used is accurate. The relation n=2m-1 never enters the transport.

No promotion, follow-on, or commit was made.
DIVERT path=xmodel/f2-all-parameters-source-gate-fable5-20260909.md status=CLEAN body_bytes=13757 body_sha256=f5b684b133bc277583409f036506d683e200c3edeed616379e1e403adae6537e
