You are Opus5 implementing a bounded exact-algebra AWS producer.  Work only
in the existing case directory

`cases/td6_c1_c2_c3_allq_mod_f_unitriangular_functional_v89h10_aws_20260826/`

and write only these new files:

1. `PREREGISTRATION_P13_COORDINATE12_ORIGINAL_FIRST_TOTAL_F.md`
2. `replay_v89h19r2_p13_coordinate12_original_first_total_f.py`
3. `run_v89h19r2_p13_coordinate12_original_first_total_f.sh`
4. `SOURCE_P13_COORDINATE12_ORIGINAL_FIRST_TOTAL_F.sha256`
5. `launch_p13_coordinate12_original_first_total_f_host.sh`

Read in full first:

- `xmodel/td6-h19-original-first-total-f-certificate-grok-20260827.md`
  (the controlling design; implement Stages 4–5 and every control);
- the H15, H18, H19R1 clients/runners/preregistrations/manifests in this case;
- `ops/run_td6_coordinate12_h19.sh`, but do not edit it;
- every imported V85/V87/H6/H11/H12 module the design names.

Implement the exact design, not a new quotient checksum.  Required core:

- rebuild the frozen 38-row original FIRST system and literal 2,757-term
  P13;
- replay the pinned H11/H15 flag, inverse, affine map, and full NF;
- compute the 38 original-row inverse columns with the existing triangular
  `solve_original` path;
- polarize degree-<=2 P13 to explicit original-FIRST multipliers and expand
  them back to the full E3 identity;
- run the H19R1 genuine source-addend omission and a FIRST-row omission;
- clear all F=0 denominators, lift with the generic retained objects, divide
  the exact residual by total `F`, and emit the denominator-cleared total-F
  multipliers with the P13/F omissions;
- enforce the exact denominator, q15, environment, hash, telemetry, and
  nonclaim contracts in the design.

Favor reuse/import of frozen functions over copied algebra.  Do not edit any
existing/frozen artifact.  The launch helper may support only explicit hosts
`r6a` (`3.91.104.135`) and `box01` (`54.175.21.169`), key
`/Users/dc/.ssh/claude-cli.pem`, tag prefix
`td6_v89h19r2_p13_c12_first_total_`, 786432-MiB virtual cap, one thread,
and 43200-second wall cap.  It must package the complete pinned closure,
verify the archive SHA remotely, and launch detached under a fresh job root.

No AWS mutation or launch in this task.  No heavy local execution; only
`py_compile`, shell syntax, hashing, import/static inspection, and genuinely
tiny unit probes.  Do not access `jc2-lean`, use the web, modify ledgers, or
commit.  Do not weaken an assertion merely to make a test pass.

At completion report:

- every new-file SHA;
- static/syntax checks run;
- exact algorithmic deviations from Grok’s design, if any;
- anything that prevents immediate root audit and dual-AWS launch.
