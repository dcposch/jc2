# D43 exact sparse-row emitter v3: independent repair assignment

You are Fable 5 acting as an equal-standing producer.  Starting from the
immutable v2 packet, build a new v3 packet that repairs every launch blocker
in the hostile review.  This is production engineering in service of an exact
mathematical experiment: preserve the v2 mathematics byte-for-byte wherever a
change is not forced, and fail closed everywhere.

Do not access `jc2-lean` in any way.  Do not launch AWS, Singular, msolve, or
any heavy/local CAS computation.  Bounded source checks and no-CAS hostile
fixtures are authorized.  Do not modify v1 or v2.  Write only inside a fresh
`cases/d43_exact_sparse_rows_v3_20260828/` packet plus one producer report in
`xmodel/`.

## Controlling inputs

- Hostile review:
  `xmodel/d43-exact-sparse-rows-v2-hostile-review-gpt56-20260828.md`, SHA-256
  `4f6f6c90e0526ab07591e2ec6a97f475e477b534cb6991085764cd693a99020d`.
- Immutable v2 packet: `cases/d43_exact_sparse_rows_v2_20260828/`.
- The source-review PASS on the separate TRIPLE02 R5 packet may be used as a
  pattern for fail-closed terminal publication and custody, but do not copy
  assumptions that are false for this two-side D43 job:
  `xmodel/triple02-proper-open-resume-r5-hostile-review-fable5-20260828.md`.

## Required v3 repairs

Implement and independently exercise all eight items in the hostile review:

1. literal structural equality for the canonical D21 rows and literal
   inequality/refusal of each HW1, HW2, and +42 mutation; hashes remain only
   receipts;
2. no production-path authorization, algebra, semantic, receipt, or custody
   `assert`; optimized Python must reject every forged object identically;
3. one fresh atomic job namespace plus a nonblocking kernel-backed exclusive
   lease acquired before preflight/output mutation; bind a strong run nonce,
   launcher identity, and lease identity into all receipts;
4. one whole-job containment boundary for supervisor, both side workers,
   emitter, monitor, and descendants; traps cancel/reap the peer on first
   failure and all terminal paths prove no PGID/JOB_TAG/cgroup orphans;
5. aggregate memory/pids/swap limits with `memory.swap.max=0` where cgroup v2
   is used, sticky continuous host/cgroup resource monitoring, monitor-death,
   OOM, timeout, peak, and final-empty receipts;
6. exactly one authoritative terminal object, published by atomic rename as
   the last successful action only after every payload/custody/archive gate;
   no positive `VERDICT`, `COMPLETE`, receipt, or marker may exist earlier;
7. deterministic immutable source and terminal archives, safe-member census,
   embedded manifest exact-set/hash replay in a fresh private extraction, and
   outer SHA replay before positive publication;
8. bounded hostile tests covering duplicate launch, stale directory,
   supervisor death, peer failure, surviving orphan, timeout, OOM/resource,
   transient swap, monitor death, every late-finalization boundary, archive
   corruption/unsafe member, and `python -O`.

The wrapper must retain the v2 conditional flow: f and g are independently
constructed, same-host/different-PID/overlap is proved, and one process emits
all 19 shards plus the 184-row merge only after band-20 equality.  It remains
an emitter only (`solve=false`).  A successful future AWS run can claim only
the finite support-specialized raw-J row packet, not a point, E/unit/template,
all-depth compatibility, Keller map, or JC2.

Prefer a source archive plus a separately hash-pinned external launcher to
avoid self-reference.  Require Linux/Amazon EC2, exact instance/hostname/tag,
one pinned core per heavy side, no more than 1 TiB physical RAM, explicit
whole-job caps, zero swap, and a fresh independent hostile review plus
coordinator GO before AWS.  If systemd user scope is unavailable, a fallback
is acceptable only if it has an exact process-group/session identity,
job-tag census, bounded reap, and equivalent fail-closed terminal inputs.

Run only the bounded tests needed to demonstrate the repair.  Seal every
source/report input with hashes, preserve the exact mathematical claim
boundary, and leave the status `V3_REPAIRED_LAUNCH_NOT_AUTHORIZED`.  Produce a
review request with explicit charged roots and hostile targets.

Write the producer report only to:

`xmodel/d43-exact-sparse-rows-v3-repair-fable5-20260828.md`

End with the packet/report hashes, tests run, residual limitations, and the
exact statement that no AWS launch is authorized.
