# Fable 5 hostile source review — D43 K0 field certificate R2

You are the independent hostile reviewer. The R2 producer was Opus 5. Work
from `/Users/dc/code/math/jc2` and write exactly one durable report:

`xmodel/d43-k0-field-certificate-r2-hostile-review-fable5-20260829.md`

Read in full before judging:

- `cases/d43_k0_field_certificate_r2_20260829/REVIEW_REQUEST.md`
- `cases/d43_k0_field_certificate_r2_20260829/README.md`
- `xmodel/d43-k0-field-certificate-r2-repair-opus5-20260829.md`
- the complete R2 packet and every frozen input named by its seal as needed;
- the R1 hostile review and R1 rehearsal terminal records when relevant.

Treat every producer claim as an allegation. Execute all bounded, non-CAS
preflight and mutation checks in the review request under normal Python and
`-O`; construct independent hostile fixtures and new mutations. Audit the
mathematics gate by gate, especially rank-432 freeness/independence, theorem
separation, the unreduced Galois action, load-bearing literals, GP syntax and
semantics by source reading, authorization/custody, and claim discipline.
Do not infer that a green battery proves soundness. If `gp` is absent, do not
install it and do not pretend the GP file ran; make the parser/execution gap
explicit.

Return one exact verdict at the top: `PASS`, `PASS WITH REPAIR`, or `FAIL`.
Only literal `PASS` can later enter the authorization record. State exactly
what that verdict licenses; it must not itself authorize AWS. Include exact
full-file hashes for all charged inputs, an independently reproducible body
hash with a body-end marker, commands/counts, concrete counterfixtures, and
the residual-risk/claim boundary.

Hard boundaries: no AWS, web, commit, push, canonical-ledger edit, heavy CAS,
or local long/memory-heavy process. Never access/list/search/build/status or
control `jc2-lean`; never run global `git status` or workspace-wide searches.
You may modify only the requested report; use `/tmp` for bounded scratch.

