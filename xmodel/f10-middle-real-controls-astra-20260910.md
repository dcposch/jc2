# F10 middle real certificate: four semantic-control sources

Status: COMPLETE SOURCE PREPARATION / DISABLED / UNEXECUTED. This is producer-authored code requiring its FIRST independent static gate. It is not runtime evidence, certificate acceptance, a source theorem or launch authority.

First action: 2026-09-10 10:42:10 UTC. Fixed controlling cap: 11:04:00 UTC; publication reserve: 11:01:00. Original targets were absent. Transaction basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.

## Deliverable

The own box `box/f10-middle-real-controls-astra-20260910/` contains a minimal new mutator.py and a narrowly adapted dispatch.py, four unchanged accepted science/probe copies, a literal dispatcher diff, disabled registration and complete contract. There are exactly nine retained preflights, one future authorized mutator, and four genuine unchanged checker.main calls. No original producer formation or positive checker rerun is included.

The baseline is fixed by SHA256 `219151792c8ffbbaab2e745fa60bd72e9c05c705fcc2ded992bcd97badf05470`, 854264 bytes. Its coefficient body was NOT read, copied, computed or emitted here. Only the pinned 680-byte terminal receipt was read as metadata: it binds j=4, degrees36/21 and the accepted science code. Its status is not promoted by this task.

The four changed objects are U+1, R+1, j4-to5, and interior Bernstein coefficient[1][1]+1. Every mutation starts from the same original, preserves all other fields, uses canonical rational strings, must actually differ, is exclusively written and reread/hash-bound. The mutator calls unchanged authority.authorize before Fraction import or coefficient parsing. A bounded metadata receipt names all four exact changed hashes and intended error predicates.

The ROOT dispatcher freezes all four changed copies as root-owned readonly inputs before the first negative check. It requires NORMAL_EXIT/2, empty stdout, absent checker receipt, and respectively these exact stderr lines:

- `INCONCLUSIVE: positive V cancellation`
- `INCONCLUSIVE: full cofactor identity`
- `INCONCLUSIVE: wrong maximal excluded-edge factor`
- `INCONCLUSIVE: full Bernstein expansion`

Each has one final newline. Any cap, signal, early binding failure, changed reason, unexpected output or stale fixture hash is STOP/NONDECISION, not semantic coverage. Successful controls would mean only `FOUR_SEMANTIC_REJECTIONS_CONFIRMED`.

## Retained safeguards and precise changes

producer.py, checker.py, authority.py and probe.py are byte-identical to their accepted inputs. CAPRUN stays external and unchanged at `4435279df8f987c667ebc2fae8e4bd987916032561cb3faf1e07a57789d196c2`. No helper or supervisor is replaced. DISPATCH.diff records every alteration of the parent dispatcher. Besides the new fixed sequence/baseline bindings, it directly binds each scientific role path to its accepted hash and fsyncs the durable directory's parent, addressing the prior gate's documentary qualifications without changing science.

The outer controls job/schema is distinct from the unchanged accepted authority's science JOB. Every authority remains ROOT-owned, immutable and hash-checked BEFORE launch, with full registered argv, source/native manifest, exact input and outputs. Startup flags stay `-I -S -B` externally and `-E -s -S -B` for science/probes. The inherited nonroot identity, same-PGID RSS/TERM/KILL dummy, live identity capture, cgroup quiet, pre/post file pins, file limit, strict errors and no-retry handling remain.

The science window begins before the mutator and never resets: joint360wall/330CPU/2GiB/16MiB. Proposed fixed subprofiles are mutator10wall/9softCPU and each checker45wall/49softCPU; their hard CPU allowances total210, not a cap increase. Nine preflights retain their separate180wall window. All durations are prospective limits, not measured performance. Original absolute ROOT deadlines and15-second admission/post-return checks control every child. Conservative wrapper/ps accounting and sampled cleanup can stop the batch; strict real-time guarantees are not claimed.

Dedicated16MiB tmpfs covers all four fixtures plus all working outputs. The original and old receipt remain readonly input storage outside it. Terminal-only EBS replicas/manifests and file/directory/parent fsync preserve available bytes before dispatcher exit, including STOP where exact groups are quiet. ROOT must supply truthful physical/native closure, readable immutable files/ancestry, env-i startup, cgroup/mount/UID setup, absent durable destination, absolute clocks and backup stop. This package supplies none of that authority; the registration is disabled with null fields.

## Evidence, limits and next checks

PINS.json contains exactly the ROOT card plus its ten inputs. All were checked before body access; READ-SCOPE.md distinguishes new WHOLE reads from same-byte prior WHOLE reuse. New source and own documentation received manual whole-text review. Metadata-only hashing, literal textual diff generation and the publication transaction were the only executed tools. ZERO scientific subprocesses of any size, imports, AST/syntax/compile/tests, fixtures, network/worker actions or coefficient reads occurred.

Two unresolved quantities, with no new canonical OPEN ID: zero independent reviews of these new bytes, addressed most cheaply by one FIRST whole-source gate; and zero actual four-control executions, addressed only afterward by a separately ROOT-registered finite batch. No optional control classes, positive repeat, measured speed, mathematical conclusion, runtime readiness or follow-on is claimed. Original and shared files are unchanged. All writers will be IDLE before terminal custody handoff.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5709`.
- Body SHA-256:
  `cc199266526d44ae9452a5af37434450f23a5944a0fe9bcb3453ca6398121d50`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
