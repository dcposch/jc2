# `(8,12)` order two: `p=0` cusp grade-12 unit elimination

Date: 2026-08-26

Status: **PROMOTED EXACT NECESSARY THEOREM AT THE NAMED CUSP OPEN.**

## Promoted theorem

After the reviewed sharp third-tail gate has forced `M=0`, consider the
quadruple-root collision chart

```text
p=0,       d=rs,       d*k0 != 0
```

in the source-typed `(8,12)`, exact-order-two, terminal `[6,2]` client.  The
complete raw grade-ten equations reduce on this open, without a radical, to

```text
c0=0,
96*c1^2+5*k0*d^3=0,
96*a0*c1+15*k0*cs*d^2=0.
```

With the raw Laurent coordinate `u=c1/d`, these equations are equivalently

```text
c1=d*u,       a0=3*cs*u,       5*k0*d+96*u^2=0,
d*u != 0.
```

Before any grade-eleven pivot, radical, saturation, or projected-sheet
equation is used, the sixth complete source row at absolute sigma-grade
twelve is exactly

```text
E_(6,12)=-(21/1024)*d^3*u^2.
```

Row six has no target at this grade.  The displayed coefficient is therefore
a raw unit on `D(d*u)=D(rs*k0)`.  Consequently the post-`M=0`, unit-load
`p=0` cusp open `D(rs*k0)` admits no prolongation through grade twelve.  No
later terminal or finite-Taylor equation can restore this open.

## Source custody and independent review

The frozen complete-source producer has:

```text
RESULT.md      fadb2794e395bfbc037aac4cd68b88eb1304ce8e58883d391f847e8a09aa4526
RESULTS.sha256 9008a545ec71f0f67719c0f37c2c58ac0d3e8e9e7a4de7b20a9b2c8661c766d7
FREEZE.sha256  0fc1d6f4a52d54df98d5c650611421a2e82657038840be54a6f60f95641145a2
```

Exact Q on Box03 and an independent `F_65521` control on r6d both passed.
The exact-Q lane carries the characteristic-zero statement; the modular lane
is a compiler/control check.  Both retained the complete seven-tail source,
the first and second moving-center/correction jets, `k10,k6,k2`, and every
target at its frozen weight.  An independent focused reconstruction of all
120 sixth-row monomials agrees with the complete collector; the two
load-bearing pole-six contributions sum to `-21/1024`.

The hostile review is **CONFIRMED** at SHA-256
`d1a0d83ef24f2ef6db8cfe790326e716d51243ded34ea3a1d2cd79ca6fc8d8b0`.
It independently checked the raw cusp reduction, Laurent isomorphism,
absolute grade, Faber connection, correction/load/target completeness, both
unit pivots, the unreduced sixth row, the focused source replay, and the
scope firewall.

## Firewall and successor

This promotion eliminates only the post-`M=0`, `p=0`, unit-`k0` cusp open
`D(rs*k0)`.  It is disjoint from the separately promoted odd open
`V(rs) intersect D(cs*k0)`.  It does not prove that those opens and the
residual `C/A` charts form one normalized-Rees/Čech cover; it does not exclude
positive-valuation moving-`p` arcs, the all-zero higher-contact receiver,
`k0=0`, other infinity supports or terminal profiles, the whole square
branch, all order two, all `(8,12)`, maximum twelve, or JC2.

The immediate successor is exact source-certified coverage of the residual
`R=C=0` region, followed by one total unspecialized-`p` Kummer/Rees glue
certificate.  The generic V12/unbounded-`d=1` and eight-form fan artifacts
remain quarantined navigation and are not inputs to this promotion.
