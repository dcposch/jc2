# Fable 5 hostile review — td=8 Prop. 8.1(iv) route certificate

Producer: Sol 5.6. Work independently from `/Users/dc/code/math/jc2` and
write exactly:

`xmodel/m2-td8-equal-join-prop81iv-route-hostile-review-fable5-20260829.md`

Read the full target
`xmodel/m2-td8-equal-join-prop81iv-route-sol56-20260829.md`, packet
`cases/m2_td8_equal_join_prop81iv_r1_20260829/`, the reviewed D2 route
producer/review, printed Notation/Proposition 8.1 in
`refs/sigray_full.pdf` pp. 39--41, and the directly relevant promoted ladder
derivations. Do not read any active Opus/Grok parametric-consumer output.

Treat the target as an allegation. Verify first that the charged full frame
really identifies `delta=X` and `1-u=kbar`; an error there invalidates every
displayed identity. Re-derive from scratch:

1. the affine merge identity for general `nu=4+3t`;
2. the two-orbit rigidity condition `A+B=0`, including nonzero RHS and all
   root/eta/searrow/multiplicity conditions;
3. both fixed `(21,15)` consumers and the `(85,35)` trunk, including the
   claimed forced root ratios;
4. degree/gcd/frame matches and whether any printed condition outside the
   ODE makes one of these patterns inadmissible;
5. the exact distinction between independent local solutions and one
   compatible global coefficient system.

Replay both test modes and build independent exact mutations/fixtures. Seek
especially a missing power of `i`, a full-vs-reduced-pattern mismatch, an
illicit coordinate gauge, collision of the two nu-orbits, or a nonconstant
RHS hidden by cancellation. Return `PASS_AT_LOCAL_PATTERN_SCOPE`,
`REPAIR_REQUIRED`, or `FAIL`, with the narrowest maximum consequence. Include
full/body hashes and exact commands/counts. A PASS authorizes only canonical
promotion of the local formal theorem, not AWS, landing, realizability, exact
lambda, a degree ceiling, or JC2.

Hard boundaries: no web, AWS, commit, push, canonical edits, heavy CAS, or
local long/high-memory process. Never access/list/search/build/status or
control `jc2-lean`; never run global `git status` or workspace-wide searches.
Modify only the requested report; use `/tmp` for scratch.
