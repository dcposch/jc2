# f10-r1 quotient generic caller gate: Fable 5.1 static delta review (2026-09-10)

lane=f10-r1-quotient-generic-caller-gate-fable5-20260910
reviewer=Fable 5.1 (claude-fable-5-1), independent of Astra and ROOT
launch=2026-09-10 01:52:03 UTC; hard_stop=02:09:00 UTC (earlier of launch+18m=02:10:03 and 02:09:00); final 3 min reserve
mode=static delta gate; read-only; zero subprocess of any kind (no Python/AST/compile/syntax/test/CAS); documentary apply_patch writes only
owned=xmodel/f10-r1-quotient-generic-caller-gate-fable5-20260910.md and box/f10-r1-quotient-generic-caller-gate-fable5-20260910/ only
authority=NONE. No Seal and no charge_basis are authored here. This review confers no runtime authority on the new caller.

## 0. Custody (thirteen ordered inputs)
All thirteen snapshots in /tmp/jc2-lane.CtB9ni/inputs were verified by sha256sum in the assignment order at 01:52 UTC, BEFORE any body read: 13 of 13 MATCH. Rows below are copied from the sha256sum output.

| # | charged input | sha256 | lines |
|---|---|---|---|
| 1 | xmodel/f10-r1-quotient-generic-caller-astra-20260910.md | 969d3f4699ff31edf0e286f95b9e6d063fb7329d177db68e501636ee56754e84 | 52 |
| 2 | box/f10-r1-quotient-generic-caller-astra-20260910/dispatch_batch.py | 54c55be3db27207a2c22072699a19d80c2be6a882e5de7e237fd2bd6f57f1ed0 | 336 |
| 3 | box/f10-r1-quotient-generic-caller-astra-20260910/caller.diff | 49ca33133cd2f537774b431cfb08b5192ffcd1a43575c92704b4ec34d5e25dc8 | 287 |
| 4 | box/f10-r1-quotient-generic-caller-astra-20260910/generic.plan.json | ce193898e3114f1db018b86e6ebe0b2ea4fdac5f65296dee76245f94da0b964a | 91 |
| 5 | box/f10-r1-quotient-generic-caller-astra-20260910/dispatch.registration.DISABLED.json | 8a6d6ab4923f4b553de59d31713249275ea84852b7aba96c2b86f3031283f965 | 18 |
| 6 | box/f10-r1-quotient-generic-caller-astra-20260910/STATIC-NOTES.md | 9c18108593ba98f04838f5697e501b2afc36119ca2cbca449ae75fda06ee9c32 | 28 |
| 7 | box/f10-r1-quotient-generic-caller-prep-20260910/ROOT-CARD.md | e56d1b76e05e36239b0339da2bc57209a825dfb2b0320a21f740ae2239cd508b | 117 |
| 8 | box/f10-r1-quotient-generic-caller-gate-prep-20260910/accepted-dispatch_batch.py | e1780f99c939ac37e0c149f1d22fb1d7741ad76c8ff183d3d3cc0f4da3e11b72 | 223 |
| 9 | box/f10-r1-quotient-remainder-generic-controls-astra-20260910/generic_controls.py | c369dc11b4e9562b050333225133aedf9cb63a04f3309424fdfab769465d363a | 425 |
| 10 | box/f10-r1-quotient-remainder-code-astra-20260910/execution_gate.py | cbfe55ff11503cee094dc49e54b656d209806aa37ee30cc281902902152049c6 | 72 |
| 11 | box/f10-r1-exact-linear-decision-code-astra-20260909/probe.py | 02913a1caf8cb5ebe2ec7c404ede0247a1954baee3751af8b2645496f6b6e1a7 | 21 |
| 12 | xmodel/f10-r1-quotient-remainder-static-gate-fable5-20260910.md | a797cbed9ad4483ec453599fc8154e4d5ce2dd64b06d0796a79a0ad69655cbef | 97 |
| 13 | xmodel/f10-r1-linear-compatibility-gate-fable5-20260909.md | d9bf289989d2631f0ab39657b4a3f0c8f7389e21ded388e68e366cd13d7fdc31 | 102 |

Read scope: all thirteen read WHOLE by `cat -n`, one file per call, after the hash match. Every displayed output ended at the file's last `wc -l` line, so no clipping occurred and nothing had to be recovered. Nothing else was read: no provenance link, corpus, Git, shared/protected/live file, receipt, log, other process or agent, and no broad xmodel listing. Both own targets were absent at 01:52:03 UTC before the skeleton write.

Operations used: date, ls, sha256sum, wc, cat -n, diff -u, grep, comm, and apply_patch for every authored write. ZERO Python, compile, import, AST, syntax, test, fixture, CAS, network, AWS, SSH, process or agent activity. The one text `diff -u` of two charged files is a byte comparison, not code execution.

## 1. Scope, premises, and what is NOT reviewed
- Reviewed: the narrow delta only (caller.diff, new dispatch_batch.py, generic.plan.json, the disabled template, STATIC-NOTES) and the literal harness/authority/probe interface it binds to (generic_controls.py, execution_gate.py, probe.py).
- Accepted premises, not re-reviewed: the 17zh kernel/harness mathematics and its static acceptance (input 12), the 17zb/zc old-caller workflow and its runtime qualifications (input 13), CAPRUN 4435279d unchanged, and all supervisor semantics. solver.py, checker.py, evidence.py, algebra.py and run_capped.py are NOT inputs; their hashes are cited only from ROOT-CARD L28-34 and the harness FROZEN block (generic_controls.py L16-19).
- Not premises: any actual-source coefficient, receipt, acceptance, old executor output or live peer. The two operationally stopped attempts are ROOT-CARD context only (L3-6), neither mathematics nor a reusable registration.
- The literal diff body equals a fresh `diff -u` of input 8 against input 2 (label lines excepted), so the old and new sources charged here are exactly the two sides of the charged diff. The old caller is the accepted starting implementation, not permission to reuse its registration, plan or payload.
- Nothing here is a syntax, import, API, timing, pass, source-point, ideal-decision or JC2 claim. The new caller stays UNEXECUTED and confers no runtime authority.

## A. External ROOT registration
CONFIRMED.

- Argv-before-parse: dispatch_batch.py L201-203 require exactly one argument, 64 lowercase hex characters, before anything is read; L204-205 require a regular, non-symlink `dispatch.registration.json` whose digest equals that argument, before L206 parses it with the duplicate-key/non-finite refusing `read_json` (L42-51).
- Fresh schema and generic-only: L207-209 require schema `F10-QUOTIENT-GENERIC-DISPATCH/v1`, `enabled is True`, `generic_only is True`; L210-211 forbid `univariate_acceptance` and `normalized_acceptance` in the outer registration. The template (input 5) carries the new schema with `enabled: false` and all sixteen keys the caller reads, every runtime selection null, `file_sha256` empty.
- -I/-B, DMI, host, cwd, exclusive writer: L212 flags; L213 `Path.cwd()==ROOT==registration['cwd']`; L215-217 EC2 vendor, board asset tag equal to instance_id, hostname equal to registration and not math-hq; L218 exclusive-writer contract. Unchanged from the old caller L148-154.
- Fixed caps/deadlines/aggregate: L219-223 two explicit UTC deadlines, DEADLINE is their minimum; L224-225 aggregate must be the integer 240 (old: any positive int); L226 file cap 16777216; L227-231 the three profiles must EQUAL the literal probe 5/3/2 GiB, dummy 5/3/32 MiB, generic 180/170/2 GiB dictionaries (old L163 checked only the set of names); L232-235 field/type checks retained.
- Exact vector and ROOT-supplied self pin: `known` (L236-245) holds nine literal pins. Four equal charged inputs by my own hashing (generic_controls c369dc11, generic.plan ce193898, execution_gate cbfe55ff, probe 02913a1c); five equal ROOT-CARD rows (solver 58cdc472, checker 122842e5, evidence cbe5a9e0, algebra 7d565299, run_capped 4435279d), and the four FROZEN constants in the harness agree. `base` (L252) adds `dispatch_batch.py` and `/usr/bin/python3` with NO literal: their digests come only from registration `file_sha256` and are verified on disk at L260-261. L253 disjointness of task/interpreter pins from the native closure, L254 every native pin an absolute resolved path (strict), L255 exact set equality. No fixed number of native files is asserted; ROOT owns that future vector.
- Interpreter symlink: `/usr/bin/python3` is in `base`, not the resolved-path native set, and `digest`/`is_file` (L62-63, L35) follow symlinks, so an OS symlink with the registered content digest is accepted; the harness L395 compares resolved interpreters, consistent.
- Disabled template fails before derivative authority or STOP writes: `armed` becomes True only at L267 after every startup pin, plan read and absence check; the STOP file is written only `if armed` (L333-335); authorities are written only inside `run`. With the template, refusal occurs at L207-209. Consequence, by design per ROOT-CARD L104 and STATIC-NOTES L5: any pre-arm failure leaves no STOP file, only the caller's nonzero exit and traceback.
- Coherence: ROOT is `Path(__file__).resolve().parent` (L10); all output, authority and pin paths are `ROOT/name`; the child spec's `cwd`/`runner_path`/file names are the same strings the gate rebuilds (execution_gate.py L44-57), as in the old caller. The prospective CLI `/usr/bin/python3 -I -B <flat>/dispatch_batch.py <ROOT-SHA>` uses only stdlib imports, so `-I` is safe.
- Documentary nits, non-blocking: L202 accepts lowercase hex only (digest output is lowercase, so consistent); `job_tag` truthiness is not checked at startup, so an empty tag would make every probe fail at execution_gate.py L21-22 with a NON-matching reason and STOP, never PASS.

## B. Prospective inside-run barrier
CONFIRMED for the retained CAPRUN contract; cooperative, not adversarial, custody.

- Absence including dangling symlinks: `absent` (L28-29) uses `os.path.lexists`. L107-109 check nine per-operation names (authority, stdout, stderr, telemetry, caller.stdout, caller.stderr, dispatch, prelaunch, sentinel); L110 additionally checks the receipt plus all twenty sidecars for the mathematical operation (`generic_outputs`, L53-59, built from the pinned plan's 8 and 4 names, 8x2+4=20 sidecars). Startup L265-266 checks preflight/batch PASS/STOP and the same 21.
- Authority frozen BEFORE Popen: L124 exclusive `open('x')` write, L125 chmod 0444, L126 digest, L128 `fixed_barrier(auth, sha)` which checks EVERY original `fixed_files` entry (module-level map, L262) as a file with its digest, and the authority as regular, non-symlink, mode 0444, same digest (L31-40). The missing-input control deletes the plan key only from the copied child map `files` (L106, L123), never from `fixed_files`, so the original plan pin is still checked at L128 and L141.
- Frozen prelaunch record: L129-135 exclusive write, chmod 0444, digest; it stores authority digest, timestamp, full fixed map, both argv vectors, `hard_cutoff_epoch`, `first_math_epoch`. L153 requires it unchanged after return; L157-159 bind it into the dispatch record.
- Same checks plus wall+15 immediately before the sole Popen: inside the `with` (L136-143) the child-written outputs (stdout, stderr, telemetry, sentinel) and, if mathematical, the 21 generic outputs are rechecked with lexists, `fixed_barrier` is repeated, and `time.time()+wall+15 < hard` is repeated; `started` is taken after these. The caller-owned names created between the two barriers (authority, prelaunch, caller.stdout/stderr) were made with `open('x')`, and `.dispatch.json` is later created exclusively (L163), so a concurrent creation fails closed rather than overwriting.
- Retained CAPRUN and child vectors: `parent` (L100-105) and `child` (L97) are byte-for-byte the old L53-61 lists; `Popen(parent, stdin=DEVNULL, stdout=out, stderr=err, cwd=ROOT, preexec_fn=filelimit, start_new_session=False)` (L144-145) is unchanged; `filelimit` (L72-73) sets RLIMIT_FSIZE 16 MiB inherited before exec; no new PGID (L3, L145). Namespace/stat capture (L127, L146-147), exact-PGID `quiet` sweep after wait (L75-83, L151), and post-pins (L152-153) retained or strengthened (old L99-100 rechecked only the child map; new rechecks the original map plus authority mode).
- Stored cutoffs, never renewed: `first_math` is set once (L92-93); `hard=min(DEADLINE, first_math+AGGREGATE)` (L94) is stored in the prelaunch and dispatch records; L164 refuses a return at or after it as NONDECISION; L324 refuses late final validation before batch.PASS.
- Exceptional paths: every `require` raises RuntimeError; inside `main` any exception after `armed` writes batch.STOP exclusively and re-raises (L330-336). Inherited, not new: if L146 or L147 raises after Popen, the CAPRUN process is not waited (same as old L93-94), and `quiet` only swallows FileNotFound/ProcessLookup/Permission errors. Both fail closed (no PASS), but they leave the supervisor to the outer SYSTEM unit ROOT must register.
- Explicitly NOT protection: chmod 0444 plus hashing is a cooperative barrier (comment L125, STATIC-NOTES L16); a malicious same-user writer or a replaced file between the last barrier and exec is not excluded. `caller_stat` is CAPRUN's /proc stat (L147), not an independent snapshot of the mathematical child (STATIC-NOTES L28 says so). No new supervisor was added, correctly.

## C. Seven controls
CONFIRMED against the literal gate and probe.

- Exactly seven, in the old order, then preflight.PASS (L268-273): five `gate-<defect>` probes with trailing `[plan, sentinel, 'plain']`, `gate-valid`, `dummy-descendant`; all operation `check` (default L85), profiles `probe`/`dummy`. Then exactly one generic build (D).
- Each deliberate refusal (L165-169): rc nonzero AND telemetry `NORMAL_EXIT`, sentinel absent by lexists, and the exact expected substring from `REFUSALS` (L20-26) in the child's stderr. All five strings equal execution_gate.py literals (L20, L30, L43, L57, L67; grep count 1 each). Hand trace of the gate order for each defect: disabled stops at L19-20; wronghost passes L19-22 and stops at L29-30 because hostname is `NOT-THE-REGISTERED-HOST`; argv passes the child-argv check L33-36 and stops at L41-43 because `parent_argv` carries `EXTRA` while the real CAPRUN cmdline does not; cap passes L41-43 (parent_argv unchanged) and stops at L56-57 because `required` is rebuilt with `rss_bytes='1'`; missing-input passes L58-65 (script, gate, runner pins present) and stops at L66-67 because the resolved plan path was deleted from the child map. An unrelated failure produces a different message and fails the substring check, so it cannot count as PASS (STATIC-NOTES item 3 verified).
- gate-valid: probe.py L6 authorizes, L7 writes `POSTAUTHORIZE\n` by exclusive text open; the caller requires exact bytes `b'POSTAUTHORIZE\n'` (L271, old used `read_text()`), rc 0 and `NORMAL_EXIT` (L196).
- dummy-descendant: new L171 requires the same exact sentinel bytes; retained L172-194 require rc 125, `RESOURCE_CAP`/`rss`, TERM sent, KILL sent, cleanup complete, leader reaped, measured group RSS above the 32 MiB profile (probe.py L13 allocates 64 MiB in a SIGTERM-ignoring child, L12), two stdout JSON lines with the caller's namespace and the recorded PGID, the exact MATCH/SENT 15/MATCH/SENT 9 sequence, MATCH identity equal to the registered leader pid/pgid/start identity, integer signals. Old and new bodies of this block are identical in the diff. The RESOURCE_CAP/leader-reap semantics of CAPRUN are accepted constants, not re-reviewed.
- No new runner, dummy or test is required or authorized by this review.

## D. Single AUTHORITY OUTPUT build
CONFIRMED, with one documentary GAP on the `engine` equality.

- Exactly one build after the seven controls: L274-275 `run('generic','generic_controls.py',[ROOT/'generic.receipt.json'],'generic',operation='build',mathematical=True)`. Child argv is `python3 -I -B <ROOT>/generic_controls.py <authority> <receipt>`: `len(sys.argv)==3` (harness L378), no plan argument (ROOT-CARD L59-64, plan.json `child_cli`). Authority spec carries `operation:'build'` (L111), `generic_only:True`, `univariate_acceptance:None` (L116), matching harness L385 `authorize(...,'build')` and L392-393; the gate skips its `check`-only input rule at L66 for build.
- Plan names/branches/predicates match the literal harness: the eight positive names and four negative names in generic.plan.json equal `CASES`/`NEGATIVES` (harness L21-27) in order (grep extraction, both lists identical); expected branches UNIT/SEPARATOR/UNIT/UNIT/UNIT/SEPARATOR/UNIT/UNIT equal `fixture_table` L261-268, which the harness itself enforces at L301; the four negative reasons equal the `reject` calls at L333, L339-340, L345, L355. The plan holds no coefficients or source data (91 lines: scope, names, CLI, two source hashes equal to `known`, proposed caps).
- Nine summary keys: harness L366-372 writes exactly schema, status, scope, execution, engine, harness_sha256, solver_sha256, positives, negatives; caller L280-281 requires that exact set. Status, scope and schema strings are byte-equal in both files (grep count 1 each). `execution` equals the gate's four-key custody dict (execution_gate.py L71-72), which the caller reconstructs from JOB, registered hostname, INSTANCE and its own authority digest (L286-287); hostname and instance were already forced equal at L215-217. `harness_sha256` is `digest(__file__)`, `solver_sha256` is the sibling solver digest (L370-371); the caller binds them to `known` (L289-290).
- Positives/negatives: caller L292-293 requires lists of length 8 and 4 (harness L356 asserts the same). Per record: exact key sets (L305 vs harness L304-305; L317 vs harness L317-318), ordered id equality with the plan, status literals, `check == expected` where expected is the UNIT dict {217, 9, 25} or SEPARATOR dict {1638, 217} exactly as returned at harness L199-200 and L226-227, with `type(...) is int` guards against booleans; negatives require `ValueError` and the exact reason.
- Twenty sidecars: `bound_artifact` (L295-303) requires each pin to be exactly `{'path','sha256'}` with the literal expected path (harness `freeze` L283 returns exactly that), a regular non-symlink file, and a matching digest of the bytes; it records the observed byte count. The harness embeds hashes, NOT sizes, and the caller never claims an embedded-size comparison (comment L301-302, STATIC-NOTES L16). No fixture or certificate JSON is parsed and no arithmetic occurs in the caller. Count checks are documentary bindings of the harness's own 217/1638 assertions, not a replay.
- Post-pins and STOP: L322 `fixed_barrier` over every original native/source/registration pin plus the frozen build authority; L323 receipt digest unchanged after readback; L324 final validation before the stored cutoff; L325-328 batch.PASS with receipt digest and stat size. Any malformed, partial or late result raises, writes STOP and preserves partials; there is no cleanup or retry anywhere in the file.
- GAP-D1 (documentary, fail-closed): L288 requires `receipt['engine'] == registration['python_flint']`. The harness sets `engine = producer_environment(spec)` (L412, L369), a function of evidence.py, which is not a charged input. If it returns the spec's three-key dict verbatim the equality holds; if it adds or derives fields, the batch STOPs with a false negative, never a false PASS. Smallest repair, for a later producer if it ever fires: compare the three keys individually rather than the whole dict.

## E. Literal diff and scope
CONFIRMED.

- Literal diff: `diff -u accepted-dispatch_batch.py dispatch_batch.py` reproduces caller.diff exactly after its two label lines (`--- accepted-dispatch_batch.py`, `+++ proposed-dispatch_batch.py`), 151 added and 44 removed lines. All nine 64-hex tokens in the new caller are accounted for by charged inputs 4, 9, 10, 11 and ROOT-CARD rows; no other hash literal appears.
- Not changed: no scientific module, supervisor, engine, source schema (`F10-R1-REGISTERED/v1` retained, L111), CAPRUN vector or cap value. The box contains only the caller, diff, plan, template and notes; none of the six scientific modules, probe or CAPRUN is copied or mutated. The `compatibility.py` payload, its stale plan pin 1fe704aa and its 5/7 tally are removed, not carried.
- Behavioural deltas, each accounted for above: external registration SHA argv (A); exact profile/aggregate literals (A); removal of the old trailing-input auto-pin loop (old L63-70), now redundant because the plan is a literal pin and the receipt/sentinel must be absent, so the child map equals the original fixed map exactly; lexists absence and prelaunch/prospective barrier (B); strict JSON reader for registration, plan, telemetry and receipt; refusal substrings and byte-exact sentinels (C); `univariate_acceptance: None` in child authorities; build-operation receipt contract (D); STOP only when armed (A).
- Inherited limitations, localized: the unwaited-CAPRUN edge after Popen (B); cooperative same-user custody (B); no independent live-child capture (B); plan fields other than the two case lists are hash-frozen but not cross-checked, as in the prior gate's nit; no syntax/import check of any file exists by mandate, and a syntax error fails closed at launch.
- Consequence: one fresh finite generic batch MAY be PREPARED on this exact packet after ROOT's own registration. Runtime, migration evidence and an independent review of that receipt remain required before any actual-source use. No assurance farm or optional instrument is proposed.

## 2. Verdict table
| item | verdict | smallest actual defect / repair |
|---|---|---|
| A external ROOT registration | CONFIRMED | none blocking; nit: job_tag truthiness unchecked at startup (fails closed with wrong reason) |
| B prospective inside-run barrier | CONFIRMED | none new; inherited unwaited-CAPRUN edge after a failed namespace/stat read, fail-closed |
| C seven controls | CONFIRMED | none |
| D single build and receipt contract | CONFIRMED | GAP-D1: engine dict equality rests on uncharged evidence.producer_environment; fail-closed; repair = per-key compare if it ever fires |
| E literal diff and scope | CONFIRMED | none |

No REFUTED item. No repair, hardening or code was authored. No Seal and no charge_basis line exist in this report.

## 3. OPEN items and collisions (own-only extraction)
OPEN(S) RAISED: none. Quantity: new OPEN count = 0. GAP-D1 is a documentary, fail-closed gap on an uncharged module, not a mathematical OPEN, mechanism or research ID. The remaining obligations (ROOT registration of this exact packet with real host/cwd/clocks/native closure, the separate SYSTEM unit and stop timer, same-launch live capture, and an independent intake of the resulting receipt) are already declared by ROOT-CARD L88-91 and L114-117 and are registration matters, not new OPENs. No exit-price assertion, charge_basis line, Seal section or closing hash section is authored.

COLLISIONS: status EMPTY. Own-only check: both own targets were absent at 01:52:03 UTC before the skeleton write; the box inventory is `input-pins.sha256` alone (13 rows, written by apply_patch, no shell scratch write); no corpus scan and no broad xmodel listing.

Own whole-read at 01:59 UTC by `cat -n`: 106 lines, header, sections 0, 1, A, B, C, D, E, 2, 3. Before this section the only placeholder was this section's own; grep found no completion marker, no Seal heading and no charge_basis line. The 13 distinct 64-hex tokens in this report equal, by comm in both directions, the 13 rows of the owned manifest and a fresh sha256sum of the thirteen inputs. The 8-hex prefixes in sections A and E are pin literals read from the caller, the file cap, and dates, not custody rows. Verdict unchanged after the reread: A, B, C, D, E CONFIRMED at the static delta tier only; nothing executed; no registration, worker, acceptance or JC2 conclusion created. Completed before the 02:09:00 UTC stop; all owned writers idle after the marker line below.

<!-- BODY-END -->
