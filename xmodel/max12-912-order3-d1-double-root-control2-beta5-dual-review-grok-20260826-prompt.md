You are the hostile independent commutative-algebra/tropical referee for one
exact equality-face certificate in the plane Jacobian-conjecture campaign.
This is a text/source/custody review only.  On the local Mac do not run
Singular, Sage, msolve, gfan, Lean, Python algebra, or any solver.  You may
read files, check SHA-256, use cmp/diff/sed, and do hand algebra.  If a new
substantive computation is needed, fail closed and request an AWS replay.

Review the complete frozen package

```text
cases/max12_912_order3_d1_double_root_control2_beta5_face_v1_20260826/
```

Required dual freeze SHA-256:

```text
c8c541b2a098b396b32bd3e905189ca54f2dd11c44ecc7b74c0cf98ed5570a34
  DUAL_FREEZE.sha256
```

Required provisional report SHA-256:

```text
c42f8d470de0692f7f7eead4887702a91e70c0505453cea6843469303b837503
  DUAL_RESULT_PROVISIONAL.md
```

Also consume, but independently audit rather than inherit, the source and
scope explanations in:

```text
xmodel/max12-912-order3-d1-double-root-control2-slope-uniform-obstruction-20260826.md
xmodel/max12-912-order3-d1-control2-slope-uniform-review-grok-20260826.md
cases/max12_912_order3_d1_double_root_control2_rees_20260826/PREREGISTRATION.md
```

Charges:

1. Recompute every freeze hash.  Audit AWS host/tag, source-hash gates,
   compile/solve separation, caps, rc, empty compiler/CAS stderr and stdout
   diagnostics, timing/RSS/swap, and exact input/output SHAs for A and B.
2. Trace the compiler from the pinned charged ordinary-tail source.  Verify
   that A is expanded direct saturation with global `dp`, B is expanded
   double-inverse elimination with `lp,dp`, and that both use the same nine
   exact finite generators, fixed load/support, target terms, Rees map, and
   weight `(4,1,1,20,20,30,30,30)`.  Search for the historical E8 compact
   rendering error and confirm this package does not consume it.
3. Audit contraction and special-fibre semantics in both encodings.  Explain
   why different contraction generator counts (420 versus 319) are allowed.
   Compare all 41 displayed special-fibre basis polynomials: confirm or refute
   the claim that they are literally identical up to the position of `s`.
4. Verify that `la^20` genuinely occurs in both full specialized initial
   ideals, not merely in submitted-generator initials or a dehomogenized
   artifact.  Audit the exact meaning of `DISPLAYED_RESIDUE_SURVIVES=0` and
   the independent torus saturation `GHT[1]=1`.
5. Recompute the beta=5 face of the reviewed witness: `la^20`, `q1*q0^3`,
   and the five `qrr` terms tie at weight `20L`; `la^20*tau` is higher for
   `T>0`.  Determine whether the computed initial ideal is independent of the
   positive split `L=3T+H` (the only explicit tau occurs in the higher row-8
   target and rho only in the homogeneous relation), or whether the run
   licenses only the sampled split `(L,T,H)=(4,1,1)`.
6. State the strongest exact theorem and its firewall.  It must not become a
   moving-axis, later-q2, other-support/load, whole-cone/fan, whole-double-root,
   D1, or JC2 claim.  Distinguish closure of this single beta=5 face from the
   already-reviewed strict beta>5 witness region.

Write exactly one review to

```text
xmodel/max12-912-order3-d1-double-root-control2-beta5-dual-review-grok-20260826.md
```

Do not edit any producer file, top-level file, or other report.  Include
recomputed hashes, the exact promotion text (or smallest repair), and end
with exactly one of `BETA5_DUAL_CONFIRMED`, `BETA5_DUAL_REPAIRED`, or
`BETA5_DUAL_REJECTED`.
