# Independent J2 direct-certificate design after V22R1 promotion

Work in `/Users/dc/code/math/jc2` as an independent hostile algebra researcher.
Do not edit canonical files, case inputs, or `jc2-lean`.  Your sole write is

`xmodel/max12-812-order2-p0-total-rees-j2-certificate-design-grok-20260827.md`.

V22R1 is now promoted with repairs; read its producer, hostile review,
promotion, and case erratum.  Independently inspect the literal V23R1 chart
polynomial bytes under
`cases/max12_812_order2_p0_total_rees_j2_typed_census_v23_20260827/output_r1/`.
Do not trust V23 prose or JSON without checking the bytes.  Respect its V1
control erratum.

Find the fastest exact route to a correctly typed direct total-source
certificate on either or both stage-two charts over
`J1=(rs,cs,c0,c1)`:

- `T-a0`: impose J1 and `a1=a0*qa1`; only `a0` is exceptional and may be
  absorbed by saturation.
- ordered `T-a1`: impose J1 and `a0=0`; only `a1` is exceptional and may be
  absorbed by saturation.

An acceptable direct identity has a power of the exceptional coordinate,
possibly a genuine `k` power if explicitly retained, and a polynomial
rho-unit factor `1+rho*W`.  Never invert rho, qa1, a jet, or an exceptional
coordinate inside the identity.  Distinguish ideal from radical membership
and point killing from chart emptiness.

Concentrate on a route complementary to the simultaneous Fable5 design:
derive eliminants by hand from the grade-11/12 core and the sparse grade-15
row 6, identify determinant/unit factors, and design a *small* frozen AWS
experiment rather than an unstructured Groebner basis.  Check whether

`a0*(e1+qa1*e0)` and `a0*(e0+qa1*rho^2*e1)`

lead to the unit `1-qa1^2*rho^2`, and whether analogous ordered-a1 torsion
identities can cancel every nuisance in a grade-15 cubic.  Search all grades
10--15, not only the named sparse rows.

Deliver either explicit cofactors with an exact residual-zero replay, a
rigorous obstruction, or a hash-ready AWS specification including ring,
generators, exact target family, grading/degree bounds, controls, resource
cap, and stop rule.  Rank independent variants that can run concurrently on
Box02.  You may perform only light read-only desk checks under one minute and
1 GiB; do not launch AWS or heavy local algebra.  State a strict scope
firewall.
