# Fixed-r3 reconstruction and one mixed-row control: static runtime

Status: COMPLETE STATIC CANDIDATE / UNREVIEWED / UNEXECUTED.
Owner /root/contact_collision_geometry. First action2026-09-11 00:45:13 UTC;
exact report, manifest and box absent. Original reserve01:13/HARD01:16 UTC.
Basis0d39df3c9fd69c939a8420c54d03228b9077777d. No runtime authority.

All18 inputs matched before fresh WHOLE reads. No prior-read reuse. The
runtime gate's clipped middle was recovered in bounded lines36–65.

## Outcome

Disabled metadata dispatcher, no-math probe, isolated mathematical mutator,
registration contract and exact future13-command scope. No scientific or dummy
source was executed, imported, parsed as AST, compiled, syntax-checked or tested.
No coefficient, baseline or fixture exists in this publication.

The sole provisional source dependency was discharged by ROOT's later lifecycle
attestation of its static FIRST A–H clearance. That review was not read and is
not an added input; feasibility and every runtime observation remain unknown.
No worker, instance inventory, enabled registration or performance claim exists.

## Exact changed interface

The new dispatcher adapts the charged reviewed r2 control/custody architecture,
not its old source tag, error strings, authorities, caps or outcomes. CAPRUN
remains byte-identical at its charged hash. Six-field full native/Python closure,
EC2/boot/namespace/cgroup checks, mapped-file checks, exact root0444 files,
literal argv and UID/GID/cap capture, fresh phase barriers, group-quiet before
terminal reads, move/freeze and byte-identical fsynced durable custody remain.
Science lives in an exact five-file directory; the separate wrapper has exactly
dispatch.py/probe.py/mutate.py. No optional supervisor migration or hardening.

Actual r3 authority binds mode and artifact path/SHA, with UNFORMED only for
producer. Its small source-native inventory is a distinct <=64KiB job_tag/files
subset of the ROOT-authenticated full closure, including the interpreter.
Every subset entry equals the full pin; it is never represented as sufficient
startup closure alone. Actual native inventories remain ROOT's future input.

A necessary registration obstruction was found and resolved by an explicit
ROOT static-design amendment: baseline and fixture SHAs do not exist when a
single pre-run registration is frozen, so their derived authority SHAs cannot
be literal constants in that same pre-run command inventory. Only the two
checker commands have a unique typed AUTHORIZATION_SHA256 slot at the unique
--authorization-sha256 flag. Only mutate has the separately approved unique
ROOT_REGISTRATION_SHA256 slot. Duplicate/missing/misplaced/other-phase tokens
are rejected; every other command byte stays literal and expanded argv is
recorded. No general expansion or future finder-selected policy is admitted.

Mutator consumes a root0444 positive-phase record at ROOT's exact registered
path. ROOT dispatcher emits it only after independently observed positive
checker success, quiet and current frozen pins. It binds baseline path/SHA/size,
ROOT SHA, source pins, literal policy and actual positive authority. Mutator
rechecks ROOT/host/source and that record, then invokes actual authorize('check')
on the same baseline before any scientific parse/import. It adds X1^27 to
graph.slots[24], preserving all other document values and seven-coordinate
exact rational representation. Canonical whole bytes must be restored by the
inverse slot edit. Mutator's receipt remains FORMED_UNCHECKED, never PASS.

The fresh negative checker must reach exactly `all25 literal graph rows T`.
Acceptance matches the entire actual four-frame traceback at check.py540/536/
520 and check_arithmetic.py227, exact final ValueError, normal exit1, no resource
or signal, empty stdout. Only bounded native caret lines may vary. Parser/hash/
source/pivot/resource failure or another mathematical row cannot pass the
selected control. The five other semantic controls remain NOT_RUN/SPEC.

## Finite envelope and changed-object checks

Exactly nine no-math preflights precede one producer, one positive checker,
one separate mutator and one negative checker. Source LIMITS remain per-call
900wall/600CPU/8GiB/128MiB. NEW combined planning envelope is3000wall/2100CPU/
8GiB exclusive cgroup/128MiB tmpfs: three science profiles900/599softCPU,
mutator30/20, nine preflights5/3. Hard child CPU is1857 and wall allocation2775,
leaving243CPU/225wall for caller/runner/cleanup. These are static allocation
sums, NOT measurements. Source report's producer/checker-only1800wall proposal
is not silently reused as the thirteen-command budget.

ROOT registers the original cgroup CPU origin before launch, so startup CPU
is included; sampled2050 cutoff leaves nominal50CPU cleanup reserve but is
not a hard aggregate CPU quota. All custody steps remain subject to3000/2100
and original UTC stops. Slow native scans, missed required live identity,
wire/term/decimal exhaustion, combined packet overflow or copy overrun means
NONDECISION, not semantic failure. Baseline plus one fixture share120MiB;
all logs/metadata share8MiB. No128MiB-per-packet doubling is licensed.

Manual changed-object checks: an unchanged fixture fails hash distinctness;
malformed token placement fails before launch; altered ROOT/source/native pins
fail fresh barriers; wrong r2 refusal strings fail exact r3 predicates; a
parser refusal fails the whole negative traceback; positive success alone
cannot yield final thirteen-command success. These are source-text checks,
not executed tests. Inherited ESRCH identity race may still cause false STOP;
it was not expanded into optional supervisor hardening.

## OPEN quantity, cheapest instrument and custody

No new canonical mathematical OPEN. Pending quantity is whether this new static
dispatcher/probe/mutator and narrow dynamic registration policy implement the
contract correctly. Cheapest instrument is one bounded FIRST whole-source
review, prospective1200wall seconds, unmeasured and not launched here. Only
ROOT can subsequently authorize the single specified <=3000wall batch, after
actual fixed inventory/registration/stops; neither this report nor the source
FIRST clearance supplies runtime authority. Runtime readiness is UNKNOWN.

Own code/documentation were WHOLE-read and changed ranges replayed as text.
All18 postpins and all owned hashes are in terminal custody, with the expected
transaction SHA. All writes were apply_patch plus the existing documentary
artifact transaction. No science/dummy execution, syntax/AST/import/compile
check, coefficient/fixture generation/read, network, AWS, process inspection,
Git, corpus, protected/shared edit, live gate read, agent or follow-on occurred.

COLLISIONS: EMPTY in exact own-target inspection. Targets were initially absent;
source/CAPRUN/frozen reports are unchanged. Publication remains within the
original reserve01:13/HARD01:16, never reset. No source, rank, point, unit,
fieldness, degree or JC2 outcome follows from this static candidate.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7078`.
- Body SHA-256:
  `2dc7bbc85a1a2ab8013c8d0c245ae4916a08745022866e46811803ae602bc76e`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
