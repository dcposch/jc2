# Hostile review: D3 Halphen CFS state machine

You are GPT-5.5, the genuinely different-model hostile reviewer for the plane
Jacobian-conjecture campaign. Work in `/Users/dc/code/math/jc2` on the exact
charged files below. The producer is provisional: reconstruct the mathematics
from the hypotheses and actively seek counterexamples before agreeing. Do not
infer correctness from seals, manifests, prior agreement, or campaign status.

Charge these exact bytes:

```text
be42d2cf894d92928978935381028dbf5ae7ba1d3598a3474d7c1460a7b6dee9
  xmodel/bd-a2-d3-halphen-cfs-state-machine-sol56-20260830.md
148ddb5e532b28a23361866f671984bd22a1b395a836484043f6f82c6001c1bf
  xmodel/bd-a2-d3-halphen-cfs-state-machine-sol56-20260830.md.artifact.json
2570d44ec3c31b4b559299099be196dd52b13b7ba1cfe4539043f96fc823ee9c
  ops/d3_halphen_cfs_state_machine_replay.py
7e73b7a6ebd1c4f6529c8c4502e150de1a8b4e4664d03323bfc8631b03ab08c5
  xmodel/bd-a2-d3-triple-line-first-jet-invariant-gate-sol56-20260830.md
a389b0a700a96f1ac68f3123f19fc7fa502e31b305e0973f164e50c5d6c585bd
  xmodel/bd-a2-d3-triple-line-first-jet-invariant-gate-sol56-20260830.md.artifact.json
95f2f9fc72d6560749aef090ce91dbd0ef3398ed705bcede69258425ad4ca9de
  xmodel/bd-a2-d3-hodge-level-divisor-coordinator-integration-sol56-20260830.md
```

Audit the proposed state-machine theorem item by item.

1. Check the theorem interface: exact plane CFS level two, Halphen minimal
   floor one, and strict-Henselian insolubility. Verify precisely which
   promoted input supplies each fact and whether CFS applies over the complex
   DVR without a hidden solubility or completeness assumption.
2. Start from the raw base-degree-three form, not a truncated formal gauge.
   Check that the provisional first-jet gate really permits the two raw
   branches `H=y^2z,q2=0` and `H=y^3` while retaining `a,ell,m` and all
   determined higher coefficients correctly.
3. Recompute `L_x`, `L_y`, the central form
   `z^2(q2*x+C2*z)`, and the exact `L_z` three-cycle. Decide whether CFS
   Theorem 4.3 truly rules out this cycle because level `2>1`, including the
   four-iteration statement and the possibility of another singular locus or
   integral move.
4. Check the level drop on the fixed path and the use of CFS Lemma 5.8.
   Reprove the full double-root elimination and look for a missed scalar cube,
   residue-field issue, or nonminimal level-one loophole.
5. Recompute the triple-root cube equations
   `q1=U=0,R=3s^2,C3=s^3`. Verify the conclusion that only `Q=0` and the
   `Q=y^2` type survive among the stabilizer orbits.
6. Independently expand both critical flags. Check `b,c,d`, both first cube
   equations, the cancellation of the intermediate `theta*Y*z^2` term, and
   the exact `kappa,eta` formulas. Confirm that the final model is critical
   exactly on `kappa=0,eta!=0` and that these conditions force `s!=0`.
7. Audit the constant normalizations. Distinguish a plane stabilizer from a
   local base-parameter rescaling; say exactly when `m=3,ell=0,q0 in {0,1}`
   is legitimate and when a globally frozen coordinate requires retaining
   the open `q0!=0` shard.
8. Attack the positive control
   `F=(x+t*z)^3+t*y^3+t^3*x^2*z`. Verify exact level two, the critical
   lowered form, generic smoothness, and local normality. Then, as a separate
   successor analysis, homogenize it to
   `(S*x+T*z)^3+T*S^2*y^3+T^3*x^2*z` and report any cheap exact facts about
   global normality, singularities, invariants, fibre types, rationality, and
   compatibility with the promoted four-row surface theorem. Do not conflate
   a global surface with a polynomial map.
9. Read and rerun the replay in ordinary, `-O`, and `-OO` modes. Check the
   AST-assert guard, mutation, hashes, and whether every software-backed claim
   is derived rather than circularly hard-coded.
10. Price the result correctly: this is a necessary local constructible gate,
    not global attainment, an arc, a dominant `A2` map, a counterexample, or
    JC2. Identify the cheapest decisive global obstruction if the theorem
    survives.

Use primary or authoritative sources for any theorem you do not derive.
Return itemized `CONFIRMED`, `CONFIRM_WITH_CORRECTIONS`, `GAP`, or `REFUTED`,
then a maximum-safe theorem, exact dependencies, and the cheapest decisive
successor. This review makes no exit-price assertion: emit no
`charge_basis={...}` line; receipt status `ABSENT` is expected. Do not run
heavy local CAS. Do not inspect, list, search, stat, build, modify, or control
`jc2-lean`. Do not read any q6 report or any sibling external-model prompt,
log, report, or receipt. Do not edit inputs, Git state, or canonical
artifacts.

Write exactly one report:

```text
xmodel/bd-a2-d3-halphen-cfs-state-machine-hostile-review-gpt55-20260830.md
```

End with one standalone `<!-- BODY-END -->` line and no seal block.
