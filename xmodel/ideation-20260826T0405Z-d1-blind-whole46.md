# Blind whole-46 significant-news delta — D1 owner

Date: 2026-08-26T04:05Z

Independence: I rescanned the canonical 46-row inventory and current primary
campaign ledger after the confirmed exact-order-four elimination, the
confirmed fixed-chart D1 full-q2 closure, and the TD6 V82S2 first-gate result.
I did not read another researcher's current-round delta or synthesis.

## Rank changes

1. **Raise rows 25/27/28 (covers, boundary trees, adjunction) to the best
   non-D1 proof-discriminating client.**  Order four is the first case where
   a source-certified rational component is killed by a positive-genus cyclic
   target, while the zero-load stratum is killed by divisor arithmetic.  This
   is now a reusable two-stratum architecture, not a topology slogan.
2. **Raise rows 31/38, with row 4 as a controlled client.**  The explicit D1
   q2 and h corrections show that Rees/tropical continuation can be organized
   as a filtered row-module lift.  The decisive global object is the
   obstruction/cokernel module, not more isolated weight points.  Row 4 stays
   firewalled from algebraization until a finite torsion or determinacy
   theorem is produced.
3. **Keep row 2 first overall but change its immediate computation.**  The
   terminal tree frontend should feed cyclic-target genus/divisor filters and
   symmetry-reduced lower-tail systems.  More unlabeled terminal enumeration
   alone is now lower value.
4. **Raise row 46 only as proof infrastructure.**  Formalize the reusable
   valued-arc, cyclic-cover genus, and monic-good-reduction implications behind
   discovery; do not spend the discovery budget formalizing additional fixed
   CAS rows.
5. **Lower completed or exhausted clients:** retire exact order four; stop
   isolated TD6 first-order axes now that all 24 kernel directions pass; stop
   additional fixed D1 weights inside the reviewed strict cone.  Rows
   10/11/23/24/33 remain stopped, and rows 13--17/20/22/29--30/34--35/39--45
   receive no new positive evidence from this news.

## New connections and cheapest falsifiers

### 1. Order-two cyclic receiver, stratified by load

Apply the now-proved order-four architecture to the five exact-order-two
`U=4` terminal classes.  For each terminal weighted tree, derive the first
source-certified residual function `v_T` and split the load-zero and
load-nonzero strata.  In the loaded stratum compute the normalization of

```text
Y_T: y^2=v_T
```

directly from `div(v_T) mod 2`.  Positive genus plus a componentwise
nonconstant map from a rational source component gives the same impossible
source-to-target composition used at order four.  In the zero-load stratum,
test the analogous divisor/torsion relation instead of forcing the genus
argument.

**Cheapest falsifier:** take one rational and one quadratic order-two `U=4`
class; derive only the first residual target, divisor parity, source
nonconstancy, and the `a_top=0` complement.  If both targets normalize to
`P1` or the projection is vertical, demote this architecture before running
all five classes.  This is my single best next experiment outside D1.

### 2. Discriminant-localized certificate as finite D1 determinacy

For `K=z^3+pz+c`, the squarefree theorem works after inverting the cubic
discriminant `Delta`, whereas the remaining point is the double-root cusp.
The q2/h syzygy ladder looks like the Taylor expansion of that localized
certificate.  In the frozen row-module quotient `M`, ask whether the target
class satisfies

```text
Delta^N [Lambda^20]=0
```

for an explicit minimal `N`, or more generally compute the Fitting/annihilator
of its cyclic submodule.  A cleared-denominator identity would replace an
unbounded list of h-valuations by a finite certificate; failure identifies
the exact non-torsion obstruction rather than another timeout.  The unit-axis
covariance reduces `(p,c)` motion to the one true transverse discriminant
normal before this calculation.

**Cheapest falsifier:** on the already frozen q2-corrected chart, construct
the h-linear and h-quadratic row-module maps and test whether the reviewed
squarefree certificate's first two pole coefficients satisfy the same
recurrence as the explicit corrections.  A mismatch kills the proposed
localization interpretation; agreement justifies computing the exact
annihilator on AWS.

### 3. One filtered obstruction complex for D1 and TD6

The D1 correction equations and the TD6 rank-nine/kernel-24 result are two
instances of the same deformation complex:

```text
multiplier corrections --d--> row variations --pi--> obstruction cokernel.
```

At filtration degree `n`, the known lower corrections produce `C_n`; a lift
exists exactly when `pi(C_n)=0`.  Exact RREF gives either a correction or a
left-cokernel functional.  Build this once with source provenance and use it
for mixed D1 normals and for the quadratic Kuranishi map on the full TD6
24-space.  This turns serial coordinate tests into sparse block linear
algebra and makes an obstruction proof-generating by construction.

**Cheapest falsifier:** compute the polarized second obstruction on a basis
of the D1 `(q2,h)` plane and on a small spanning set of the TD6 kernel over
two good primes.  If its support is dense or order-dependent, do not build a
universal sparse operator; if stable, reconstruct the exact rational blocks
and then evaluate all kernel pairs.

### 4. Tree characters as a lower-tail block diagonalizer

The Davenport--Zannier permutation/tree model supplies more than a terminal
count: it gives a finite automorphism group and labeled edge/vertex
permutations for each passport.  The lower-tail Taylor/Rees differential is
equivariant under those automorphisms.  Decompose it into rational character
blocks before elimination.  This can pair Galois-conjugate terminal classes,
explain repeated ranks across the 49 `U=4` shards, and preserve labels needed
for a future `G2-PSC` sheet-transport functor.

**Cheapest falsifier:** for the five order-two and five rational order-four
`U=4` classes, compute tree automorphisms and the first tail Jacobian only;
verify equivariance and compare full rank with the sum of block ranks.  If
the source normalization destroys equivariance, retain labels for provenance
but abandon the speed claim.

### 5. Cyclic quotients as positive-genus receivers beyond order four

Whenever a boundary passport has a cyclic sheet quotient, compute its Kummer
eigensheaf degrees and Riemann--Hurwitz genus before full tail elimination.
A positive-genus quotient is enough: one does not need the normalization of
the whole source leaf.  This potentially links the sheet-number ladder,
monodromy passports, and log-adjunction clients, and may provide early kills
for higher terminal books or TD6 components after landing is source-certified.

**Cheapest falsifier:** enumerate cyclic quotients of the eight TD6 terminal
passport classes and compute genus from charged branch multiplicities.  Stop
immediately if every quotient has genus zero or if the source map is not
licensed by the current landing data.

### 6. Formalize the implication schemas, not the instances

The order-four proof and D1 cone use three short reusable schemas: monic
good-reduction/prime lift, no nonconstant rational-to-positive-genus map, and
the valued-arc consequence of a unique monomial initial term.  A small Lean
library for these schemas would make future order-two/D1 promotions depend
only on explicit source certificates and would expose missing source-to-
landing hypotheses early.

**Cheapest falsifier:** state the three lemmas with every geometric premise
explicit and check whether the current order-four and D1 promotions instantiate
them without adding an unproved source axiom.  If not, the missing premise is
the deliverable; no further instance formalization should start.

## Allocation delta

- Outside D1, start the two-class order-two cyclic-receiver falsifier first.
- In D1, continue the h syzygy ladder while racing the discriminant-torsion
  formulation; stop fixed-point cone sampling in the already covered open
  cell.
- In TD6, compute the quadratic obstruction on the entire 24-kernel, not more
  first-order axes.
- Use DZ labels and characters upstream of lower-tail shards, but preserve the
  terminal-versus-lower-tail and source-versus-landing firewalls.

None of these ideas supplies an absolute/cofinal td ceiling, universal
landing, `G2-PSC`, full D1, full TD6, maximum twelve, or JC2 by itself.
