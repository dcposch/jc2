cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
Publication is complete at 14:08:14 UTC, inside the reserve. Final state: the report carries exactly one standalone marker with nothing after it, no hex digest in its body, and the custody file's twelve rows each match a fresh sha256sum once.

**Outcome: all six items CONFIRMED, no blocking correction, one liveness-only GAP.**

- **Custody.** All 12 inputs hashed at 13:58:58Z and matched the expected pins before any body read. Every file was read whole and unclipped. Both owned paths were absent, the skeleton went in first with no marker, and every write used apply_patch.
- **A CONFIRMED.** Both scripts check S/vartheta absence and the full weight on every term before dropping z. I rederived the shapes (2,1), (3,1,0), (3,2,0), the degree-5 bounds, and expanded both cofactor identities by hand. A checked cF+dG=1 puts 1 in the chart ideal over any commutative ring, including Q identically zero. Zero, constant, degree-drop and nonunit cases all end as INCONCLUSIVE, never a common-zero claim.
- **B CONFIRMED.** The producer re-forms the literal monic P7, validates the interface, runs a bounded Euclid whose invariant I replayed, and reads back the identity before serialising. The checker imports only the pinned check_arithmetic, rebuilds F and G itself, and tests all 77 positions. The hypothesised wire_q leak is not present: the 4096-character bound and the 4300-digit guard both raise ValueError inside the candidate stage, so the only outcome is candidate rejection, and the checker would reject an over-long integer anyway. No edit.
- **C CONFIRMED** as bindings. The -E -s -S -B vector keeps the sibling entry import; the contract states every ROOT duty rather than hiding it.
- **D CONFIRMED.** No exception or partial-output path yields a success receipt. Success needs the full identity, then a post-read equality, then the receipt.
- **E CONFIRMED** as design. The +1 mutation changes the identity by exactly F or G and fails at check.py:129, with the fresh-receipt caveat noted.
- **F CONFIRMED** conditional. Checked identity plus the accepted X1=0 endpoint and weighted scaling gives the parameter-sequence hypothesis, unlocking the 35-dimensional graded core without performing the membership test.

**GAP (liveness only).** The baseline key names the scripts consume come from the accepted reconstruction schema, charged here only as a gate report, not as code. A mismatch fails closed before any output.

**Next step** stated in the report: ROOT registration of the isolated three-file directory, one producer run, one checker run, then the single mutation control. Baseline promotion still owes controls 6 and 10 separately.
DIVERT path=xmodel/f10-r2-highest-bezout-code-gate-fable5-20260910.md status=CLEAN body_bytes=14115 body_sha256=ec7b0ed1767f5a015e3344dba220219bfef98f7afb4a8bb166ca386cea2e0230
