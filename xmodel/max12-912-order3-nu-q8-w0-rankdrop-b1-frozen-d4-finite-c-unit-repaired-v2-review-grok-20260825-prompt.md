# Fresh hostile review request — repaired frozen-`d4` finite-`c` landing unit

Act as a hostile algebraic-geometry and source-fidelity referee.  Work
read-only; do not edit repository files.  Do not run Bash, local CAS, solver,
network, or long Python.  Read/hash files and reason by hand.  The frozen AWS
Box02 endpoint is the accepted computation; no missing mirror may be silently
invented.

Read in full:

- `xmodel/max12-912-order3-nu-q8-w0-rankdrop-b1-frozen-d4-finite-c-unit-repaired-v2-20260825.md`;
- `cases/max12_912_order3_nu_q8_w0_rankdrop_b1_loaded_saturation_aws_20260825/PARAM_SCOPE_ERRATUM.md`;
- every `PARAM_*` and `aws_box02_param_q_std_dp_v1/*` file in that case;
- the pinned quotient compiler/order-three parent;
- the reviewed generic-`c`, slope-two, and raw ramified-audit reports together
  with both immutable ramified-audit errata.

Charge explicitly:

1. Verify the six source rows, map `d4=1,d2=2+u`, polynomial coefficient
   ring `Q[...,c]`, selected load `w*x5*(x3-2*x5)`, and principal saturation.
2. Decide whether `Landing=GC+(w,u,x1,x3,x5)=(1)` excludes every finite
   `c` at exactly that centre without a hidden `c` denominator.
3. Refuse `GC=(1)`, global selected-open emptiness, moving `d4`, or arbitrary
   ramification.  Check that the repaired V2 text no longer makes those
   inferences and correctly identifies `GL` under `C_PARAMETER_BASIS`.
4. Audit the Box02 rc, diagnostics, hashes, source regeneration chain, timing,
   output, one-engine/no-cofactor/no-Box02-version-capture trust tier.  Treat
   independent Box03/`modStd` endpoints as pending custody, not present proof.
5. Enforce the full firewall: moving `d4`, full rank-drop/full `Hsrc`, all
   projective infinity, terminal/Taylor realization, trajectories, all
   `(9,12)`, maximum twelve, and JC2 remain open.

End with `CONFIRMED`, `CONFIRMED_WITH_REPAIRS`, or `NOT_CONFIRMED`, list every
repair, and name the smallest exact geometric successor.
