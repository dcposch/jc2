# 14:40Z post-collection adversarial cross-scan — TD6 owner

Status: cross-pollination only; no mathematical promotion.  I read the four
sealed `1440Z` submissions after collection closed.  AWS producers continued
independently.

## Immediate correction to my blind note

My blind note described the reported AS signal too loosely as `omega` being
"uniformly zero" and then entertained source-module membership.  The exact
news is different: every one of the 11,881 nonempty current Q9 fibres is
**zero-partial**, and on each fibre the three `omega` values occur uniformly
(one third of the completions have `omega=0`).  Thus `omega` is fibrewise a
surjective affine coordinate at this projected stage, not an identically zero
section.  The next gate is the intersection of the `omega=0` slice with all
downstream Q8--Q3 restoration/terminal conditions.  An ideal-membership test
`omega in I_Q9` is therefore the wrong primary experiment unless it is used
as a negative control expected to fail.

This correction also changes the common trichotomy: TD6 currently has a unit
obstruction on a named open; Q8 has localized unit ideals with boundary debt;
AS has a **surjective partial coordinate**, not a proved zero obstruction.

## 1. AS `Z/9` module audit

The AS owner's proposed compression has a sound conditional core.  For a
*fixed* predecessor map and a fresh correction `27U+81V`, bilinearity gives

```text
det J(F+27U+81V) = det J(F) + 27 L_F(U+3V) mod 243,
```

because every fresh--fresh quadratic term is divisible by `27^2=729`; after
division by 243 it is still zero modulo 3.  This licenses a linear congruence
module over `Z/9` at a fixed predecessor point, with literal integer replay.

The hidden implication to reject is that this is automatically one linear
module over an entire Q3 affine fibre.  When the Q3-kernel coordinates and
the fresh digits vary jointly, the coefficient operator `L_F` varies with
the predecessor and contains Q3-coordinate--fresh-digit cross terms.  Those
mixed terms need not vanish at the relevant divided order.  The reviewed fact
that inherited Q4-kernel columns vanish in the earlier Q3 gate does not prove
that these later mixed columns vanish.

Cheapest separating experiment: compile the complete 91-row integer map on
the origin, both multiples of every Q3-kernel basis vector, both multiples of
every fresh `Z/9` basis vector, and every mixed Q3/fresh pair.  Accept a global
`Z/9` module only if all mixed second differences vanish exactly at their
declared moduli.  Otherwise rank-stratify the Q3 base and use a linear module
only fibrewise.  In either case retain degrees 0--7 and all restoration rows;
then solve `omega=0` jointly with the downstream block.  Do not spend more on
standalone `omega` counts.

Sol's Greenberg/Artin-function suggestion becomes useful only after that
complete fixed-support integral coefficient scheme is defined.  Nonempty
unrelated truncated charts or changing gauges/supports are not its jet tower.
For one fixed finite variable set, nested nonempty congruence loci already
have a compactness route to a `Z_3` point; Greenberg would add a finite-depth
decision bound.  An empirical stabilization depth is not such a bound, and
the final algebraic/field-transfer and collision open conditions must remain
explicit.

## 2. Q8 finite/infinity constructible-cover audit

All four submissions correctly converge on a coefficient-compactification
problem, but a generic `Q(c)` unit plus an infinity calculation is not yet a
cover.  A proof-carrying cover must contain, from the same six-row source:

1. a cleared original-generator cofactor and its exact finite denominator
   factors in `Q[c]`;
2. direct specializations/saturations at every finite factor, while retaining
   moving `d4`, earlier coefficient drift, and the selected-open incidence;
3. homogenize-before-specialize `c=infinity` with irrelevant-ideal and source-
   horizontal saturation; and
4. explicit overlap/contraction checks showing the finite and infinite charts
   are the same projective source closure.

The reviewed finite cylinder/local-germ exclusion and ordinary tangent result
are strong leaves, but they do not remove ramified weighted arcs through the
doubled rank-drop line.  The exceptional-divisor/Rees ladder proposed by cube,
AS, and Sol is the strongest independent convergence of this round.  Its
negative control is specialization before saturation, which can manufacture
or lose vertical components.  Positive genus is consumed only after a source
component actually lands and dominates the reviewed proper curve.

## 3. TD6 five-piece H cover and global impact

The exact topology is small.  On `D(U)`, with `T=V^2/U^3`,

```text
P3norm = T^2 - 32T + 128,
QHnorm = T^2 + 8T - 64,
((5T-136)QHnorm - (5T+64)P3norm)/512 = 1.
```

Hence `P3=0` and `QH=0` are disjoint on `D(U)`; neither is thereby empty.
The honest H atlas is exactly:

- V64 on `H=0,D(U*V*P3*QH)`;
- the direct `V=H=0,D(U)` source-unit edge;
- `H=P3=0,D(U)`;
- `H=QH=0,D(U)`; and
- the direct `H=U=0` theorem (provided by the already frozen whole-`U=0`
  source result).

The first and fifth pieces are frozen; the duplicated V-edge producer is
terminal and being packaged; P3/QH source DAGs are live.  No Cech language is
needed to hide missing leaves: each leaf must have an original-row identity,
and the displayed Bezout identity supplies the set-theoretic disjointness
check.  Closing all five would prove only the whole `H=0` divisor inside the
fixed source-typed A3/q2-beta section.  It would not cover `H!=0`, the full
center ring, the separate B3 debt, other q/p-boundary, dead-stretch, pole, or
F1 moduli, and it would not imply TD6, SP-2, landing, or JC2.  Thus it is a
high-information bounded closure, not a global-wall event.

## 4. Sol's genus/asymptotic-set bridge

The proposed bridge is valuable but conditional: if an exact source map shows
that the reviewed positive-genus curve is the normalization (or is dominated
by the normalization) of an actual irreducible component of the nonproperness
set, rational/uniruled parametrization constraints can contradict it.  What
cannot be imported is positive genus of a selected residual factor before
component identity, dominance, separability, and proper-model compatibility
are proved.  This idea therefore raises the value of honest Q8 landing; it
does not bypass landing or create a cofinal td bound.

The cofinal-genus proposal has a clean stop rule: find one infinite family of
source-valid terminal packets with genus zero or one.  Until that falsifier is
run on a parametrized family rather than a bounded census, allocate it as the
global 5% reserve, not a proof-critical lane.

## Verdict and allocation change

- Strongest genuine convergence: one source-hashed exceptional-divisor /
  constructible-cover compiler can serve Q8 and TD6, while its dual
  rank-stratified transition compiler serves AS.
- Strongest hidden invalid implication: the AS `Z/9` map is linear globally
  over the Q3 fibre.  It is certainly linear only after fixing the predecessor
  unless mixed Q3/fresh terms are exactly shown to vanish.
- Cheapest discriminators: mixed Q3/fresh second-difference audit for AS;
  direct finite-denominator cofactor plus saturated infinity overlap for Q8;
  finish the two live P3/QH leaves for TD6.
- Allocation: Q8 40%, AS 30%, TD6 25%, global/Greenberg/genus 5%.  If the AS
  mixed audit passes globally, move five points from TD6 to AS for the complete
  `Z/9` block; if it fails, keep fibrewise shards and do not expand enumeration.

No post-collection idea changes the global wall: proof still needs complete
landing and a cofinal/absolute complexity bound; disproof still needs one
complete fixed-support all-depth polynomial map and the reviewed collision /
field-transfer composition.
