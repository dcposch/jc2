# F10 r1 disabled nine-row univariate adapter

Status: CODE PREPARATION ONLY; UNEXECUTED. Mathematical premises17j/17l are accepted at their exact frozen scopes. The normalized backend implementation remains PROVISIONAL STATIC CODE ONLY, and this is its single authorized speculative software child. It does not promote either implementation or authorize execution.

First action: 2026-09-09 19:20:00 UTC. Controlling cap: 19:38:00 UTC. New report/box absence was checked before transaction begin. Basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`. Eleven exact inputs and read/reuse qualifications are recorded in `input-pins.json` and `READ-SCOPE.md` in the matching box. No provenance-only path was followed. No live gate or other science was consumed.

## 1. Delivered files and boundary

The new box contains `adapter.py`, independent-path `checker.py`, metadata binding `input_evidence.py`, byte-identical frozen copies of `algebra.py` and `execution_gate.py`, `DISABLED.json`, and a finite `VALIDATION.md`. There is no caller modification, new supervisor, fixture generator or solver. No normalized or univariate artifact was constructed. No import, compilation, test, arithmetic subprocess, coefficient construction, web, AWS/SSH, process audit, agent or shared-file edit occurred.

The copied arithmetic handles the ENTIRE coefficient algebra B=Q[v]/p(v), not a selected factor or presumed field. Coefficients use canonical numerator/denominator strings and the fixed seven-element v basis. Existing authority must succeed before mathematical imports. The new template is disabled, has no worker/caps/argv and cannot dispatch anything.

## 2. Literal adapter and complete slots

The input is the complete normalized homogeneous presentation, including its full A/B, Jhat, maps, G, H0/H1 and unit witnesses. Its bytes are preserved through an immutable SHA binding, not rewritten or truncated to leading faces. The adapter extracts all seven coefficients from

    Phi3=a*x^3+b*x*y+c*z,
    Phi4=d*x^4+e*x^2*y+f*y^2+g*x*z.

The letters a,b,c here are compatibility coefficients, distinct from the normalized leading cubic's constant a0. The code uses the accepted17l inverse formula for c. With C(theta)=theta^3+F*theta^2+H*theta+a0, normalized H=a0, and the actual supplied third-band left inverse (lambda1,lambda0), it constructs

    gamma=(21/2)[theta^7] C(theta)^2
             integral((-lambda0*theta+lambda1)/C(theta)^2)dtheta.

Only the proved a0 inverse is used in this finite formal coefficient recipe. Crucially the code verifies c*gamma=1 in the full algebra. It neither divides by b nor performs a gcd on source rows. It then sets r(T)=-gamma*(a+b*T), substitutes (x,y,z)=(1,T,r(T)), and retains all fixed rows in this exact order:

    E1/S1, E1/S2, E1/S3,
    E0/S1, E0/S2, E0/S3, E0/S4, P4, P11.

Their degree envelopes are respectively 3,3,2,4,3,3,2,2,5. P4=d+e*T+f*T^2+g*r. With h_i=H_i(1,T,r) and Ubar=U(1,T,r), P11=r*h1-Ubar*h0. Phi3 is recorded as a separately consumed zero slot, not silently lost. Zero, duplicate and degree-dropped rows remain indexed; each dense row includes every coefficient slot through its bound. The additional bounds are deg(r)<=1, deg(h0),deg(h1)<=4, deg(Ubar)<=1 and deg(q)<=5.

The exact guard is xi*q(T)-1 with q=r*h0. Both factors are retained, without assuming either nonzero before localization. No nonzero-row, finiteness, root-count, rank, point or ideal conclusion is emitted.

## 3. Independent read-back and source-evidence prerequisite

The checker does not import the adapter. It reassembles Phi3/Phi4 from the seven reported coefficients, checks the literal modulus and named leading unit identities, and verifies the actual c inverse product. It performs substitution by dense-T binomial expansion of each original normalized monomial, whereas the producer uses sparse polynomial composition. It reconstructs every one of the nine rows, h0,h1,Ubar,r and the guard, with fixed slot/degree/weight checks. The seven low rows and H0/H1 are also rebound to the actual supplied whole Jhat coefficient projections. A strict independent wire parser rejects floats, non-string rational coefficients, noncanonical rationals, negative exponents and altered basis/axes.

Both paths deliberately share the unchanged frozen B arithmetic; they are NOT independent coefficient-algebra implementations. Most importantly, these local checks do not independently re-prove the normalized full source. Before either adapter entrypoint may run, `input_evidence.py` requires a root-owned registration binding the exact normalized artifact, its genuine full-checker receipt, the frozen normalized generator/checker versions and original source SHA. The receipt must cover all20 original slots, nine G, two H, ten inverse-pole slots and eight bracket bands. Its expected status is the full normalized representation PASS, not an ideal decision.

A correctly spelled JSON receipt alone is NOT proof of execution. The root-owned `ROOT-ACCEPTED-GENUINE-CHECKER-RECEIPT` field may only be supplied after actual registered full-checker execution and terminal custody review. It is null here. This is an explicit existing provenance trust boundary, not a cryptographic authentication claim. The adapter cannot bless an unverified input. If the provisional backend is refuted, this child is quarantined; changing its hashes to another backend requires a separate version/review.

## 4. Equivalent fourth presentation, not identical Gram wires

The frozen backend uses a literal verified pair-Bezout fourth left inverse rather than the original Gram choice. Both left inverses act on the same unimodular two-row ell column. Their difference annihilates that column and is a multiple of its syzygy; consequently the reconstructed ell differs by a multiple of retained Phi4. Phi3/Phi4 are formed before that substitution and unchanged, while subsequent G/H representatives can differ by Phi4 multiples. After the univariate substitution these differences are P4 multiples.

Thus the complete homogeneous quotient and its localized guard are the same at the accepted17j/17l scope. The implementation uses its ACTUAL G/H wires, including those differences. It does not falsely identify all polynomials with the original Gram representatives, discard Phi4, select a generic factor, or add a gauge. The actual c inverse product and complete normalized-source prerequisite remain mandatory.

## 5. Reconstruction contract and limitations

The artifact records, and the independent checker checks literally, the accepted symbolic reconstruction

    Runi=B[T,xi]/(all nine rows, xi*r*h0-1),
    X^3=h0^2/r^7,
    y=X^2*T, z=X^3*r, s=h0/r^3,
    H0=s^7, H1=U*s^5, z=s^2.

This is the accepted finite-etale rank-three PARAMETER cover. It is not a source-map degree claim. The checker uses the theorem's symbolic contract rather than expanding degree112/196 source powers. Reconstruction uses the bound complete normalized A/B and all four maps, not only their leading pieces. The future representation result remains conditional on genuine input acceptance and does not establish properness or a source point.

## 6. Pending review/validation, no runtime-readiness claim

All new code is uncompiled and unexecuted. Static reading completed; no syntax/runtime compatibility or performance claim follows. No coefficient bytes, degrees, term counts, timing or memory have been measured for an adapter artifact. The only future measurements encoded are actual degrees, basis-term counts and artifact bytes after a separately authorized run.

`VALIDATION.md` specifies one genuine producer/checker positive plus eight finite mutation groups: prerequisite binding; modulus/unit witnesses; dropped/reordered/zero slots; seven coefficients/c inverse; substituted rows/h/r; both guard factors; degree/exponent/precision wires; and reconstruction contract. Each mutated fixture must receive fresh hashes to distinguish semantic rejection from stale-hash refusal. Rejected large strings alone are not positive nested-parser evidence. No fixtures or tests were generated here.

Outstanding quantities: one joint static adapter/checker review; one genuine normalized full-source accepted artifact/receipt; one separately registered adapter/checker validation batch. Cheapest next test is the static delta review against the exact11 input pins, not execution. Runtime authority, caps and scheduling remain entirely with root. No new framework, coefficient-field splitting, solver, decision certificate or automatic follow-on is authorized.

Own raised-OPEN scan: no new canonical OPEN identifier or new mathematical gap is claimed. The three explicitly pending review/execution items above are software/provenance prerequisites, not theorem promotion. The private substantive body and own files were read before closure; input/copy/owned hashes and transaction are frozen in terminal custody. All writers stop at publication.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `8961`.
- Body SHA-256:
  `5f2dd9f02e34d571cf7df6ecea6ed2ef2a567be801ae3fcc0ef6371622f491b0`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
