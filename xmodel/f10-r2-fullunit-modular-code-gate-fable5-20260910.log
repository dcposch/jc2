cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
Review complete and published before the reserve. The report ends with the standalone body marker, appended last at 16:53:18 UTC after the own whole reread, the current-pin recheck, and the collision check.

**Verdicts (all six CONFIRMED, no REFUTED, no blocking fix):**

- **A entry boundary.** Exact 17-argv/8-flag vector, the distinct job tag, canonical symlink-free distinct paths, hard-coded arithmetic digests that equal the packet pins, and the baseline pin all run before any scientific import. The schema checks match what baseline-produce.py emits, so the canonical-baseline equality cannot fail spuriously.
- **B place and source.** Both modes rebuild the literal P7 at tau 2/7 from the same three-term septic with different polynomial helpers. Primality of 89, the simple root at 0, and 89-unit denominators on all seven coordinates of every term of all twenty polynomials are enforced before the z=1 projection. Mixed row bound 19 and class 4 hold.
- **C producer.** I hand-verified the documentary constants with the partition count p3(n) = round((n+3)²/12): rows 247, f-block columns 424, g-block columns 330. The forward elimination invariant makes line 174 exactly a consistency test, and the producer re-multiplies the cofactors to check the literal sum equals 1.
- **D checker.** No matrix, pivot or rank is consumed. The 18 generators are rebuilt independently, every cofactor slot is shape-checked, and the identity is compared at every position of a dense 31x16x11 box.
- **E semantics.** Producer ValueError gives INCONCLUSIVE_NO_CERTIFICATE with exit 2, checker Inconclusive gives INCONCLUSIVE_FIXED_PLACE with exit 2, and malformed or false-identity paths exit 1 with no receipt.
- **F control design.** A plus-one change to an existing term of a used cofactor passes every check before the final identity and must fail there. I wrote out the expected three-frame traceback (lines 198, 188, 181) and advised binding return code, frame lines and the final line rather than a stderr digest.

**Non-blocking notes recorded in the report:** a baseline missing a top-level key would raise KeyError rather than ValueError and STOP with no receipt, which is correct given the byte-exact pin; ROOT's frozen sibling directory must contain no `__pycache__`.

**Disclosures:** the prior fullsource gate among the inputs was Fable 5.1 authored. The first report write measured 1628 words, above the 1500-word bound, because the eleven charged_input lines and the verdict table count as words. This is stated in the Completion block.

**Deliverables:** `xmodel/f10-r2-fullunit-modular-code-gate-fable5-20260910.md` (78 lines, no hex digest in the body, no Seal, no charge_basis) and `box/f10-r2-fullunit-modular-code-gate-fable5-20260910/input_custody.md` with the 11 hash rows generated from sha256sum output. All bytes were written via apply_patch. Nothing was executed, imported or compiled.
DIVERT path=xmodel/f10-r2-fullunit-modular-code-gate-fable5-20260910.md status=CLEAN body_bytes=19618 body_sha256=22fea3eeeea5a9877eb2749384294f070b174889b42a210804665a0fa617a918
