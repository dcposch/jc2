# D28 actual-degree column — independent Fable gate PREP

September9,2026. Owner /root/nonemptiness_certificate. Basis0d39df3c9fd69c939a8420c54d03228b9077777d. PREP ONLY: no invitation, review, mathematical verdict, promotion or follow-on authority.

Prepared exactly one future gate, tag `d28-uniform-actual-degree-gate-fable5-20260909`, output `xmodel/d28-uniform-actual-degree-gate-fable5-20260909.md`. Its target was absent at preflight. Root alone launches after its own whole prompt/current-pin/vector review. The prospective deadline09:57UTC applies ONLY if root actually invites by09:32UTC, with at most25minutes from actual invitation and whichever bound is earlier. No active review clock exists. Prep itself remains bounded by09:29UTC.

The new proof remains PROVISIONAL. The gate separates its all-q!=2 actual-degree exclusion from the necessary q2 F10(0) alternative at112/196. No F10 existence/exclusion, maximum196, new coefficient ideal, certificate, source computation or JC2 assertion is charged.

## Frozen packet

`box/d28-uniform-actual-degree-gate-20260909/root-invite.prompt.md` contains the literal repeated `charged_input=` declarations and literal `{{LANE_INPUTS}}`. PINS lists11 data objects; PINS itself is the12th charged object. All basenames are unique.

Nine WHOLE byte-identical copies: new D28 proof, transaction, READ-SCOPE, input-pins and custody; accepted15x general producer and different-model gate; complete frozen accepted15x and adjudicated16h entries. Two exact documentary primary bundles: GGV718–790 and1431–1461; GGHV640–820,910–979,1180–1255,1350–1425. These contain the complete required definition/standardization, complete-chain/admissibility, nonnegative parameter instructions and BOTH Section5 tables. Partial Proposition4.6 context is explicitly labelled, and no whole-paper or uncharged F18–F21 proof claim is made.

Every original source hash and snapshot hash/byte count, source URL and literal interval hash appears in PINS. Whole primary source hashes are provenance; the reviewer is instructed to read only charged lane copies. No mutable whole canonical file, pending F2/F10 map/gate, current synthesis or live report is added. Accepted15x's generalized interface and16h's exact composed stratum emptiness are premises; local F9 proof interiors are not re-reviewed. The accepted producer/gate's other conclusions are context, not a new prerequisite.

## Six review groups

1. Given-pair, minimum-free normalization; exact inverse-degree identity, arbitrary q and fields.
2. Complete M35 table at a+b<=28, all congruence exclusions and both orientations.
3. Independent manual ratio/end-point filter, including the actual swapped F10 exception.
4. Full ordinary rectangles under literal constant translation; actual typeII.b starting chain; standardness after output swap.
5. Exact16h quantifiers and antecedents, without row-to-realization inference or exceptional-source exclusion.
6. Meaningful manual changed-object/numerical-hypothesis controls, field transfer and precise limits.

Separate CONFIRMED/GAP/REFUTED verdicts and actual proof reasoning are requested, not an old-verdict vote. The prompt demands manual controls only and explicitly forbids ANY mathematical subprocess of any size, arithmetic toy, old checker, CAS, high powers, source expansion, solver, AWS/SSH, agents, protected/shared/public access or live-peer reads. No charge_basis or authored trailing seal is requested; basis is provenance only. The external wrapper's own receipt conventions remain unchanged.

## Documentary validation and read scope

Preflight uses the ACTUAL `sed -n 's/^charged_input=//p'` parser found at ops/lane.sh line258, not an indexed-key regex. It verifies the exact ordered12-object vector, unique basenames, current source/snapshot pins, nine byte-identical whole copies, all SIX intervals using real sed against the pinned primaries and exact bundle bytes, the literal placeholder, and future report absence. PASS is frozen in preflight.json. No launcher or new generic preparer/collector was created.

This is metadata preparation only; it changes none of the sealed scientific proofs. Their full read scopes were completed by the prior source task and are preserved; this prep does not pretend to perform a second mathematical review. The author reads the whole new prep and prompt and performs the own-only raised-OPEN check before the final completion marker. Existing begin/close/finalize/verify transaction used; all current owned/input pins are in custody. Every writer will be IDLE before metadata-first handoff.

## OPEN(S) RAISED

None.

## COLLISIONS

status: EMPTY

- NONE — own-only check; future gate target absent; no corpus scan.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `4751`.
- Body SHA-256:
  `897bf16df55757634287d09f9264f5c7f341eb77af505147ccc599582432ebd5`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
