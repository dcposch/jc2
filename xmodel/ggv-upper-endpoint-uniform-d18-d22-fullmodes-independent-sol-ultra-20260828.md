# Independent uniform full-mode endpoint audit: D18--D22

Date: 2026-08-28  
Method: an independent sparse Laurent recurrence and an independent dense
ordinary-`Q[X]` replay, both using only Python standard-library
`fractions.Fraction` arithmetic

## Verdict

**PASS on the frozen 303-variable/513-generator upper branch-P fixture.**

Starting from the hash-pinned and replayed D16/D17 packet, the exact
nine-mode characteristic cascade has the predicted alternating endpoint
shape:

```text
D18: c18=0 and R18=A^2*h18
D19: R19=A^2*h19
D20: c20=0 and R20=A^2*h20
D21: R21=A^2*h21.
```

After these four relations, the full weight-22 coefficient generated by all
nine admitted modes has no pole worse than `A^-2`.  Since there is no raw
`G22` slot,

```text
D22_raw = -L22(g22),
L22(R)   = -40*A^3*A'*R - 8*A^4*R'.
```

Therefore `D22_raw` is divisible by `A=X^4-1`.  It cannot equal the target
scalar `1`.  Thus the frozen uniform upper branch-P endpoint fixture has no
field point.

This is a charged fixed-fixture verdict, not a claim that every remaining
JC2 branch has already been transported into the fixture.

## 1. Independent coordinates

The square prefix is reconstructed rather than imported:

```text
S=A^2+t/2+(Z/8)t^2+(V/16)t^3+(R/2)t^4
       +(Q/2)t^5+(T/2)t^6+(Y/2)t^7.
```

Writing `F=S^2+sum(Bj*t^j, j=8..14)` gives the independently checked
defects

```text
B8  = F8  - Y/2 - T*Z/8 - Q*V/16 - R^2/4,
B9  = F9  - Q*R/2 - T*V/16 - Y*Z/8,
B10 = F10 - Q^2/4 - R*T/2 - V*Y/16,
B11 = F11 - Q*T/2 - R*Y/2,
B12 = F12 - Q*Y/2 - T^2/4,
B13 = F13 - T*Y/2,
B14 = F14 - Y^2/4.
```

Put `a=3*B8+2*c8`.  The frozen starting relations are replayed as

```text
(3/8)*B8^2+(1/2)*c8*B8+c16=A^2*M,
a*B9-2*M=4*A^2*N.
```

No gauge, square root, carrier, or sign branch is selected.

## 2. D18 and the `c18` birth

The complete polar part is

```text
g18_polar = c18/A^3 + R18/A^2,

R18 = a*(B10/4-B9*Z/16) + 3*B9^2/8 - N/2.
```

The actual raw `G18` is a polynomial, so

```text
c18 + A*R18 is in (A^3).
```

Reducing modulo the nonconstant polynomial `A` gives `c18=0`; division by
`A` then gives `R18 in (A^2)`.  Define the quotient `h18` by
`R18=A^2*h18`.

The `c18/A^3` direction is the weight-18 indicial kernel.  It is killed by
the polynomial raw window, not by pretending that the same-row differential
operator sees its scalar coefficient.

The successor causal check is explicit: at D19 the predecessor-mixed and
same-row pieces are respectively

```text
+9*c18*A'/A^2  and  -9*c18*A'/A^2.
```

They cancel.  D19 does not independently kill `c18`.

## 3. D19 successor

After the exact D18 relation,

```text
g19_polar = R19/A^2,

R19 = a*(B11/4-B10*Z/8+B9*(Z^2-V)/32)
      +3*B9*B10/4-3*B9^2*Z/16-h18/2.
```

Hence polynomiality gives `R19=A^2*h19`.  A formal mutation which retains
only one factor of `A` leaves an actual `A^-1` pole.

## 4. D20 and the `c20` birth

Define

```text
U20 = B12/4-3*B11*Z/16+B10*(5*Z^2/64-V/16)
      +B9*(-R/4+5*V*Z/128-5*Z^3/256),

V20 = -3*B9^2*V/32+15*B9^2*Z^2/128
      -9*B9*B10*Z/16+3*B9*B11/4+3*B10^2/8,

R20 = a*U20+V20-h19/2.
```

The complete polar part is

```text
g20_polar = c20/A^4 + R20/A^2.
```

Because the literal `G20` window is polynomial,

```text
c20 + A^2*R20 is in (A^4).
```

Reduction modulo `A` gives `c20=0`, and then `R20=A^2*h20`.
At D21 the linked predecessor-mixed and same-row pieces are respectively

```text
+12*c20*A'/A^3  and  -12*c20*A'/A^3.
```

They likewise cancel; the kill is caused only by D20 polynomiality.

## 5. D21 successor

Define

```text
U21 = B13/4-B12*Z/4+B11*(9*Z^2/64-3*V/32)
      +B10*(-R/2+3*V*Z/32-7*Z^3/128)
      +B9*(-Q/4+3*R*Z/8+3*V^2/256
            -21*V*Z^2/512+7*Z^4/512),

V21 = B9^2*(-3*R/4+9*V*Z/64-21*Z^3/256)
      +B9*B10*(-9*V/32+27*Z^2/64)
      -3*B9*B11*Z/4+3*B9*B12/4
      -3*B10^2*Z/8+3*B10*B11/4,

R21 = a*U21+V21-h20/2.
```

The recurrence gives `g21_polar=R21/A^2`, hence
`R21=A^2*h21`.

## 6. Endpoint transfer and sign

Substituting all four relations into the exact coefficient generated at
weight 22 leaves 56 polar monomials, every one with exponent exactly
`A^-2`; their canonical encoding has SHA-256

```text
f9e7b6d7e6a1ac8b6cdd5292c8195514f7f0222430d9707942ebfa482cae2929.
```

Regular terms are retained; the complete exponent support is exactly
`{-2,0,2,4,6,8}`.  Applying the exact endpoint operator raises
every `A` exponent to at least `1`, including coefficient-derivative terms.
Thus

```text
D22_raw=-L22(g22) in (A).
```

The raw source has no `G22` window.  Its constant D22 generator is exactly
`D22_raw-1` (zero-based generator index 495, SHA-256
`43d957a5f8c04d732cfcaf25d7602ae7a39cf1ba3e71a6e3731d338bf1cc5216`).
The exact square fixture has `D22_raw=0` and therefore evaluates that folded
generator to `-1`.

The independent sign diagnostic agrees: for

```text
N=X^5/40-X/8,
```

one has `8*N'=A`; a synthetic `g22=N/A^5` would give
`L22(g22)=-1` and `D22_raw=+1`.  It is not an admitted raw slot and has pole
order five, which the audited nine-mode continuation cannot produce.
Reversing its sign produces target `-1` and fails.

A formally appended newborn homogeneous term `c22/A^5` does not rescue the
target: it is annihilated by `L22`, so it changes neither `D22_raw` nor the
contradiction.

## 7. Literal window enforcement

The authoritative raw source was re-read at SHA-256
`ead2fa404a741c5bc55934de6a1651d417701422f4b5311e3de7e3dccca0e5a0`.
The exact windows and independent literal maps are:

| row | literal G window | slots | map rank/nullity |
|---|---:|---:|---:|
| D18 | degrees 2--6 | 5 | 5 / 0 |
| D19 | degrees 3--5 | 3 | 3 / 0 |
| D20 | degrees 3--4 | 2 | 2 / 0 |
| D21 | degree 3 | 1 | 1 / 0 |

After each pole cancellation, the **full** characteristic coefficient must
equal its literal polynomial window.  The quotient symbols `h18..h21` are
not silently identified with raw G slots: regular pieces and every absent
low/high coefficient remain live constraints.  The endpoint argument is
stronger than needed in this respect—it uses only polynomiality—so it covers
every solution of those stricter windows.

Each of the eleven window basis columns was replayed against an independently
implemented ordinary-polynomial determinant recurrence and all 513
serialized generators.

## 8. Mutation controls

Four literal first-failure families exercise the four rows.  Each was also
replayed at scalars `-1` and `2`, with the expected quadratic scaling.

| row | construction | first residual |
|---|---|---|
| D18 | `B9=X`, omit the quadratic Laurent continuation | `6X-24X^5+18X^9` |
| D19 | `B9=A*X`, insert `G18=3X^2/8`, omit successor | `-3X+15X^5-12X^9` |
| D20 | `B10=X`, omit the quadratic Laurent continuation | `6X-36X^5+30X^9` |
| D21 | `B10=A*X^2`, insert `G20=3X^4/8`, omit successor | `-6X^3+27X^7-21X^11` |

Every prior row is exactly zero in each family, and every one of the 513 raw
generators matches the independent determinant replay.  Separate formal
mutations show that replacing either D18 or D20's required `A^2` lift by
only `A` leaves an `A^-1` pole.  Omitting either negative mode misses its
independent birth pole.

## 9. Dependence on `A=X^4-1`

The new D18--D22 Laurent and endpoint-divisibility steps do **not** evaluate
the roots of `A` and do not use its coefficients.  In characteristic zero,
they use only that `A` is nonconstant (so it cannot divide a nonzero scalar)
and is a nonunit (so an element of `(A)` cannot equal `1`).  They do not use
squarefreeness.

The fixed-`A` gate is the surrounding fixture: the literal degree windows,
slot maps, row hashes, normalized square prefix, and the charged D16/D17
starting packet were compiled for `A=X^4-1`.  This audit therefore does not
broaden the verdict to an uncompiled universal family.

## Scope firewall

- The conclusion is endpoint emptiness for this frozen uniform branch-P
  fixture, not by itself a proof of JC2.
- No transport from cutoff-three or from other endpoint branches is asserted.
- All nine admitted modes through `c20` are carried.  Modes are killed only
  at their audited literal polynomial windows.
- All G18--G21 lower and upper window equations remain live.
- No gauge, root, sign sheet, carrier, or unit is selected.
- No CAS, AWS computation, or Lean state was used.
