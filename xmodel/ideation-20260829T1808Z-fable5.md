# Blind whole-portfolio ideation — Fable 5 — round `20260829T1808Z`

Date: 2026-08-29 UTC  
Researcher: Fable 5, equal-standing whole-portfolio lane  
Frozen basis: `e930fa90b8ee9d86220a2cff72fba94e04b20baa`  
Lifecycle: `BLIND_IDEATION_REPORT / NO_PROMOTION / FAIL_CLOSED`

## 0. Custody and boundary compliance

Recomputed this session and matched against the state packet:
`xmodel/ideation-20260829T1808Z-state.md` full-file SHA-256
`f58ee9b5...` and body seal (9040 bytes, `d48e33c5...`); `APPROACHES.md`
`6f5006f4...`; `AUDIT.md` `bff17fd8...`; `COORDINATION.md` `f947045b...`;
`PROGRESS.md` `460675f2...`; `ladder/REDUCTION.md` `2adfb3b6...`; and the
four binding integrations `4c2fd1de...`, `2fcf8b9d...`, `6c2ca262...`,
`1e3e50e7...` (full hashes in the Seal preamble below). `git rev-parse
HEAD` equals the frozen basis.

Also read at the basis (uncharged, hash-tracked by git): the master
46-row table and full overlay history of `APPROACHES.md`;
`ladder/REDUCTION.md` T1–T10 and the 17:58Z frontier overlay;
`xmodel/ideation-20260829T1517Z-synthesis.md`; the newest `PROGRESS.md`
day; my prior-round attack
`xmodel/td12-occurrence-coverage-attack-fable5-93d-20260829.md`;
`ladder/SHEET6-CLASSICAL.md` §§1–3; `ladder/AM-CHECK.md` §3; and the
PCB-EXCESS definition and audit-program lines of
`xmodel/u1-star-pcb-independent-audit-sol56-93d-20260829.md`.

No other `ideation-20260829T1808Z-*` file, peer submission, prompt, log,
or run record was read. No web, no external message, no AWS action, no
commit/push, no canonical edit. `jc2-lean` was not read, listed,
searched, stat'ed, built, or touched; all repository searches this
session went through ripgrep, which consumes the repo-root `.ignore`
fence (verified: `rg --files` returns zero `jc2-lean/` paths against
1003 with `--no-ignore`). Desk computation stayed under seconds of CPU.
Files written: this report only (plus `/tmp` scratch).

No new typed exit price is asserted anywhere in this report, so no
`charge_basis` line and no exit-price declaration appear.

## 1. Disposition over the 46 numbered avenues

One raise; no lower; no reopen; forty-five unchanged.

| # | Avenue | Disposition |
|--:|---|---|
| 1 | GGV corner farm | unchanged |
| 2 | Sheet ladder (Sigray frame) | unchanged — principal, with the 17:58Z re-aim (global selector vs conditional td12 lab) |
| 3 | Vertex-gap / strip ODE | unchanged — mechanism supply, still no actual client |
| 4 | Formal-germ certification | unchanged — open, unarmed |
| 5 | JvdK descent | unchanged |
| 6 | Abhyankar–Moh / one-place | **raise (mechanism-supply tier only)** — see below |
| 7 | A(F) asymptotic variety | unchanged |
| 8 | Formal-inverse combinatorics | unchanged |
| 9 | Lee–Li Conjecture E | unchanged |
| 10 | HC4 bridge | unchanged (`NO LEVERAGE` stands) |
| 11 | Zhao/Mathieu ladder | unchanged (refuted) |
| 12 | Face isolation / p-adic multinomials | unchanged |
| 13 | DC(2) | unchanged |
| 14 | End(A_1) / Zheglov audit | unchanged (T2 replay still named-and-not-done) |
| 15 | Spectral surfaces | unchanged |
| 16 | D-module / holonomic index | unchanged (the 0827 b-function proposal remains contingent on its unrun one-afternoon test; nothing new this round) |
| 17 | BCW cubic stabilization | unchanged |
| 18 | Graded / GIT | unchanged (closed as CE hunt) |
| 19 | Char-p / Witt lifting | unchanged (degree-twelve frontier) |
| 20 | p-curvature formalism | unchanged |
| 21 | p-adic / Hensel | unchanged |
| 22 | Integral points / heights | unchanged |
| 23 | Analytic global inverse | unchanged |
| 24 | Real JC / Pinchuk | unchanged |
| 25 | Passports / dessins | unchanged (coupled two-coordinate CSP remains the untried half; Card B overlaps it in spirit, not in object) |
| 26 | Primitive-monodromy td bound | unchanged |
| 27 | Links at infinity / splice | unchanged |
| 28 | Log surfaces / BMY | unchanged (the chi-level genus balance is known to close identically — `SHEET6-CLASSICAL.md` §1c — so no raise) |
| 29 | LND / Hamiltonian frames | unchanged |
| 30 | ML invariant / classification | unchanged |
| 31 | Integrality / ZMT / Rees | unchanged |
| 32 | Collision ideal / injectivity | unchanged |
| 33 | Symplectic primitives | unchanged (`COSTUME` stands) |
| 34 | 2D tangent sweep / pole removal | unchanged |
| 35 | Dim-3 descent | unchanged |
| 36 | Guided CE search / K00 | unchanged — protected falsification owner; the valuation-2–5 program (Card A) proceeds inside it |
| 37 | Finite-field census | unchanged |
| 38 | Tropical | unchanged |
| 39 | Cohomological cluster | unchanged |
| 40 | Free-associative lift | unchanged |
| 41 | Scaling deformation | unchanged (dead) |
| 42 | Markus–Yamabe | unchanged |
| 43 | Ritt decomposition | unchanged |
| 44 | Moskowicz | unchanged (closed as proof input) |
| 45 | Differential Galois | unchanged |
| 46 | Lean / AI certification | unchanged |

**The one change, avenue 6, explained.** The campaign's own record
contains a banked, Keller-specific, per-place instrument with proven
kill power at fiber tier: the valuation form of `J(f,g)=1` along a
smooth fibre, `AM-2` of `ladder/SHEET6-CLASSICAL.md` §2 — at each place
at infinity, `ord_t(f_y) = ord_t(dx/dt) - ord_t(dg/dt)`, which coupled
the *entire* pinned residue-A contact table to the pole order of `g`
with zero slack, killed the `pi_G=4` x-side branch, and pinned the
B-/x-side contact windows. `ladder/AM-CHECK.md` §3.3 records this
instrument as "exhausted" — but only at the residue-A fiber tier. I
verified by search that no `ord_t`/per-place valuation identity appears
anywhere in `ladder/BOOK-TD12.md` or `ladder/BOOK-OFFAXIS.md`, and that
none of the sextet/detour checks named by the 17:58Z integrations (P0,
divisor, resolvent, N1, R1.0, strict-NE, MP2, terminal, T1) is
place-global. AM-2 is therefore an untapped candidate for exactly the
actual-source invariant the sextet integration asks for. This raises
avenue 6 as mechanism supply to avenue 2's conditional lab and selector
attack (Card B); it is not a standalone endgame claim, and the
one-place semigroup half of avenue 6 stays where it was (A(F) tier).

## 2. Reranked gap lists

Global selection (proof side), ranked by expected leverage:

1. **PCB-EXCESS status** (`sum_i(I_i - wt_i(a)) >= s(a)-1`): undecided,
   with maximal consequence either way — §4 shows it is exactly a
   per-value weight-concentration cap and that its truth would kill the
   entire U1 sector including B25/S17 and every `U1*(R)`.
2. **An interface-external realizability invariant.** `U1*(R)` proves
   the reduced merge/trunk/budget interface can never select or bound
   `td`; any selector must consume actual-source geometry the interface
   does not carry (AM-2 is my candidate class).
3. **Full-configuration landing/coverage** (T9/T10 holes: off-axis
   completeness, post-jump mixed, `M>=2` suffixes, composite
   single-pole).
4. **Absolute/cofinal td ceiling** (CRITICAL 7) — unchanged, hardest.
5. **`G2-PSC`** — owed only by hybrid architectures; the pure-Sigray
   lane continues to bypass it.

Conditional td12 laboratory (separate, after assuming the occurrence):

1. `TD12-U1-FAMILY-AWARE-DETOUR-EXCLUSION/v2` — source-typing
   A7/C5/D13/E11 plus W1–W3 and pure-epsilon descendants (Card B gives
   the discriminator; §4's EXCESS ladder gives the budget-side pincer).
2. `OCCURRENCE-B25/S17` — a named landing, downstream of 1.
3. Route-separated source values / gauge serialization (B and S
   separate) — downstream of 2.

Counterexample/falsification side:

1. **K00 V20R2 valuations 2–5** — the concrete finite frontier; first
   the requested independent review of the `REVIEW_DERIVED_UNREVIEWED`
   nonzero-`y` valuation-two strata claims, then the rank-one stratum
   `u=+-8iv`, then the `v=3,4,5` windows (Card A).
2. **Char-p degree-twelve frontier** (avenue 19) — unchanged rank.
3. **Formal-germ D-series** (avenue 4) — open, unarmed, behind K00.

## 3. Required novelty: one new mechanism, one new connection

**New mechanism — the graded EXCESS ladder with an atypical-value
prove-route (§4).** Novelty check: the PCB-EXCESS inequality and the
"injection of `s-1` pole relations into positive excess events" program
are on file (Sol audit, one day old); the *graded* ladder
(`EXCESS-1` versus `EXCESS-(s-1)`), its equivalent anti-concentration
reading `sum_i wt_i(a) <= td - s(a)` for every `a`, the exact
consequence arithmetic on the printed sextet/`U1*(R)` records, and the
atypical-value existence route to the first rung appear nowhere I
searched (`PCB`, `concentrat`, `atypical` sweeps across `xmodel/`,
`ladder/`, `AUDIT.md`). The prior atypical-value uses in the record
(2026-08-24 dual-pencil divisor, later `TYPE-FAIL/JUMP-ONLY`; the 0827
b-function proposal) concern different objects and are not consumed.
Marked `NEW` (mechanism), with the audit's injection program cited as
the `KNOWN` neighbor.

**New connection — AM-2 (avenue 6) into the td12 conditional lab and
the `U1*(R)` selector attack (avenue 2).** Checked as described in §1:
the instrument is banked and place-global; the td12/U1 record checks
are vertex- or step-local; no book file applies any per-place valuation
identity. Marked `NEW` (connection). Two adjacent knowns, checked and
*not* duplicated: the chi-level genus/Euler balance closes identically
(`SHEET6-CLASSICAL.md` §1c — I do not propose rerunning it), and the
residue-A AM-2 numbers themselves (banked; used only as positive-control
methodology).

## 4. Global selector: strongest prove / falsify / bypass attack

Setup, from the promoted Section-7 equality `td - 1 = sum_i I_i` with
`I_i = integral_(U_i) w_i dchi_c`:

```text
PCB-EXCESS  <=>  sum_i wt_i(a) <= td - s(a)   for the fibre value a,
```

i.e. an *anti-concentration cap*: no single finite value may carry
cluster weight exceeding `td - s`. Grading it by the excess `e`:

```text
EXCESS-e:  sum_i (I_i - wt_i(a)) >= e        (PCB-EXCESS is e = s-1).
```

**Derived consequence arithmetic (same-model, unreviewed; under the
printed budget semantics in which every promoted ceiling is rooted at
total weight `td-1`, so `EXCESS-e` lowers every printed ceiling by
`e`).** At `s=3`, `td=12`:

- `EXCESS-2` (= full PCB): the one-step `k`-cells of `U1*(R)` have
  floor `3R-1` against adjusted budget `(4R-k-2)-(R-1) = 3R-k-1`, i.e.
  slack `-k < 0` for every `k>=1` and every odd `R`. All of B25
  (`R=3,k=1`), S17 (`R=3,k=2`), the D13/E11 continuations (floors 9–10
  against `10-2=8`), and the A7/C5 tails (9 against 8) die. PCB true
  would therefore empty the entire U1 equality sector at every `td`,
  not merely tighten it — consistent with, and sharper than, the
  integration's "would kill the abstract family" caution.
- `EXCESS-1` (much weaker): S17's one-step cell dies (`8 > 8-1`); the
  three zero-slack D13/E11 witnesses `(539,99)@49`, `(893,95)@47`,
  `(154,22)@7` die in the promoted full-exit floor regime
  (`10 > 10-1`); B25, the A7/C5 tails, and the AF2-only readings
  survive at exactly zero slack. So even the weakest nontrivial rung
  of the ladder collapses most of the new detour surface back toward
  the old quartet — at typical-fibre scope (rider below).

**Prove lane (strongest).** Target `EXCESS-1` at typical fibre values
first, via:

```text
ATYPICAL-EXISTENCE. If (f,g) is a Keller counterexample, f has at
least one atypical value.
```

Proof skeleton from literature-standard pieces: `J=1` makes both
coordinates primitive (`f=h∘u`, `deg h>=2` forces the nonunit `h'(u)`
to divide 1) and every fibre smooth; if `f` had no atypical value it
would be a locally trivial fibration over `C`, so
`chi(fibre)=chi(C^2)=1`, and the connected smooth generic fibre has
`2g + #places = 1`, i.e. genus 0 and one place; AMS plus
A^1-fibration triviality (Kambayashi–Miyanishi class input) make `f` a
coordinate; then `J(x,g)=g_y=1` gives `g=y+c(x)`, an automorphism —
contradiction. The genuinely new bridge to price is
`WEIGHT-AT-ATYPICAL`: an atypical value carries at least one unit of
Section-7 weight on some `U_i`. `SOL-PROP58-REVIEW.md` line 61 already
identifies atypical fibres as "purely an at-infinity phenomenon...
exactly the terms of `Delta_a^pole`", which is the right starting
point. Together these give `EXCESS-1` at every typical `a`, hence the
S17/D13/E11 kills above at typical-fibre records. Riders, honestly:
(i) the weight-jump/atypicality link must be extracted from the exact
promoted Section-7 statement, not assumed; (ii) each consumed record's
fibre must be typed typical or the argument returns `OPEN` — a
retreat-to-the-atypical-fibre outcome is itself a strong narrowing.

**Falsify lane (structurally weak — say so).** There are no actual
counterexamples to test, automorphisms fail the normalization frame,
and the audit's `P^1` control (`g=t+1/t`) already shows generic curve
theory cannot decide PCB. The only honest falsification data are
pinned semi-actual models: compute the concentration
`sum_i wt_i(a)` against `td-s` on the frozen residue-A template
(`SHEET6-CLASSICAL` data, `td=6`), and on any future actual-source
packet. A violation there would kill PCB-for-templates only, and would
be reported at exactly that scope.

**Bypass lane.** Do not route the selector through PCB at all: use
place-global AM-2 feasibility (Card B) directly on `U1*(R)`, which
consumes `J=1` per place rather than the Euler budget, and can in
principle produce an `R`-growing obstruction (pairwise star contacts
scale like `R^2` against degree-squared genus room) that no constant
excess supplies.

## 5. TD12 detours: strongest actual-source discriminator

The invariant invisible to the reduced witnesses is the **per-place
AM-2 ledger of the full contact completion**. A reduced trunk row
`(nu,dp,dq,E,kbar,X)` plus a continuation witness constrains, but does
not verify, the ultrametric contact table its actual realization would
need; AM-2 turns `J=1` into one exact linear identity *per place at
infinity*, coupling every pairwise contact sum, the leading-coefficient
chart term, and the pinned entry data `(a,b,nu)=(1,2,3)`, `Lambda=4` at
each pole. The residue-A precedent shows this system is zero-slack and
genuinely global — precisely what none of the sextet's vertex-local
checks (T1 included) sees. Fail-closed test: build the constraint
system over exact rationals with every undetermined contact as a typed
unknown subject to ultrametric and admissibility laws; infeasibility
kills the family at actual-source tier; feasibility returns `OPEN`
(non-exclusion), never occurrence. Dual-tree crosscheck (`ord g` along
each pole branch by the product formula over the `g`-side series, as at
residue-A §2a) doubles every equation without new hypotheses. Card B
below; smallest first client is E11's `(154,22)@7` witness (smallest
`nu`), with B25 as mandatory positive control.

## 6. K00 valuations 2–5: strongest attack and cheapest discriminator

Exact scope: the normalized V20R2 mixed source truncated modulo
`Lambda^20`, `C6=1`, the five frozen boundary zeros, `Jdet[0]!=0`,
`k10[0]!=0`; possible transverse valuations 2, 3, 4, 5 only; nothing
here is an arc, germ, map, or attainment.

1. **First (mandated) step:** independent different-model
   reconstruction of the `REVIEW_DERIVED_UNREVIEWED` nonzero-`y`
   valuation-two claims — the grade-eight rank-two-stratum kill, the
   old-plane invariance of the zero-odd calculation, the over-`Q`
   closure of the old-plane lane, and the isolation of the rank-one
   stratum `u=+-8iv, v!=0` as smallest possible survivor. I am
   model-eligible (the deriver was Opus). Cheapest honest
   discriminator: exact desk replay over `Q` of the grade-six system
   `Q_i(y)=0` on `A(y)=B(y)=0`, the rank stratification of `DQ(y)`,
   and the grade-eight system on the rank-two stratum, with the
   review's concrete `kappa=5` compatible 7-jet as positive control
   and a mutated quadric coefficient as negative control. Well under
   the 60 s / 1 GiB desk cap; no CAS.
2. **Cheap structural lemma (`LEADING-CONE-UNIVERSALITY`), new but
   small:** for `v in {3,4,5}` the leading vector `d[v]` satisfies the
   same seven quadrics at grade `2v` (`2v < 2+3v`, so no `k10` load
   interferes, unlike `v=2` where grade `8 = 2+3·2` mixes in
   `kappa L_i`), hence lies on the same plane `V(A,B)` with the same
   rank-`<=2` stratification. One stratified geometry then serves all
   remaining valuations; the per-`v` differences are only the load
   grades (`k10` at `2+3v`; `k6` at `6+2v`; `k2` at `10+2v`; target at
   19). Corollary worth preregistering, not asserting: `v=5` has the
   shortest window (grades 10–19) and is likely the cheapest next
   closure after `v=2`.
3. **Stop rule:** if the desk replay refutes any reviewer-derived
   claim, quarantine that claim and its would-be successors and report
   the exact failing identity; do not substitute a scheme/Groebner run
   (AWS-only, and currently unjustified).

## 7. Mathematical-software item: one decisive desk experiment

`cases/place_ledger_td12.py` (spec; not written this session): exact
`int`/`Fraction` builder for the AM-2 feasibility system of §5. Inputs:
a sextet row or witness record plus typed unknown contact variables;
outputs: `INFEASIBLE` (kill at actual-source tier), `FEASIBLE(model)`
(OPEN, with one witness contact table), or `UNDERDETERMINED(list)`
(fail-closed: exactly which tree data the record does not pin).
Decisive first run: E11 `(154,22)@7` and positive control B25; then
`U1*(R)` symmetric stars at `R=3` (must reproduce the B25/S17 fits) and
`R=5,7` as the selector probe. Every branch of outcomes is informative:
infeasible at a detour witness = first actual-source detour kill;
feasible everywhere = AM-2 is absorbable and the interface no-go
extends to it (also worth banking); underdetermined = the exact missing
source-typing list for `DETOUR-EXCLUSION/v2`, which is the theorem's
required input schema anyway.

## 8. Campaign-systems card: `UPGRADE — ops/seal.py`

- **Problem evidenced in the record:** body-seal custody failures are a
  real, recurring class — the S-bridge Opus producer seal was malformed
  (mathematics `PASS_WITH_MATERIAL_REPAIRS`, custody quarantined), and
  every producer/reviewer/coordinator currently recomputes
  `BODY-END`-bounded byte counts and hashes with ad-hoc inline code
  (I did again this session). This is time added to every sealed
  replayable certificate and a standing malformation risk.
- **Checked-before-proposing:** no seal tool exists (`ops/` listing;
  `BODY-END` absent from `ops/*`); the previously obvious systems gap —
  fencing `jc2-lean` from search — is *already closed* by the repo-root
  `.ignore` (verified 0 vs 1003 files), so I am not proposing that.
- **Proposal (coordinator-applied; I cannot write outside this report):**
  `ops/seal.py stamp <file>` computes and appends the canonical seal
  block for the unique `<!-- BODY-END -->` boundary; `ops/seal.py
  verify <file...>` recomputes and exits nonzero on any mismatch or on
  a missing/duplicated marker.
- **Smallest measurable acceptance test:** `verify` exits 0 on the four
  17:58Z binding integrations and the state packet (five known-good
  seals), exits nonzero on a one-byte-mutated `/tmp` copy (control) and
  on a file with two `BODY-END` markers (malformation control); `stamp`
  then `verify` round-trips on a fresh scratch file. Runtime under one
  second; no dependencies beyond the standard library.

## 9. Research cards (three)

### Card A — `K00-V2-STRATA-REPLAY` + `LEADING-CONE-UNIVERSALITY`

- **Novelty:** replay is the state packet's mandated next step (not
  claimed novel); the universality lemma is `NEW` but small.
- **Dependencies:** frozen 569 tails, `compile_contracted_source_v20r2.py`
  (hashes in integration `1e3e50e7...`), the two promoted valuation
  theorems, the Opus review's derived claims as attack targets.
- **Exact deliverable:** verdicts `CONFIRMED/REFUTED/GAP` per derived
  claim (rank-two grade-eight kill; old-plane invariance; over-`Q`
  closure; rank-one `u=+-8iv` isolation), plus a proof of the
  grade-`2v` leading-cone lemma for `v=3,4,5`.
- **Cheapest discriminator:** the §6 desk replay with its positive
  (`kappa=5` 7-jet) and negative (mutated quadric) controls.
- **Outcome meanings:** `CONFIRMED` arms a reviewed rank-one successor
  and shrinks valuation two to one stratum; `REFUTED` rolls back the
  derived claims before anything consumes them (cheap now, expensive
  later); `GAP` types exactly the missing identity.
- **Stop condition:** any need for uncertain CAS or >60 s CPU — halt,
  report, preregister for AWS instead of running.
- **Expected information gain:** high per cost; it gates the entire
  remaining K00 frontier.
- **Compute class:** desk. **Ownership:** prove = this card's runner
  (non-Opus); falsify = the mutation controls inside the same run;
  bypass = none needed (finite question).
- **Provisional descendant that may start pre-review:** drafting the
  `v=5` window packet (shortest window) as text only, no computation.

### Card B — `AM2-PLACE-LEDGER` at td12 and on `U1*(R)`

- **Novelty:** `NEW` connection (§3); instrument itself banked
  (`KNOWN`, residue-A).
- **Dependencies:** promoted entry laws (T7), the sextet integration
  rows and witnesses, `SHEET6-CLASSICAL` §2 methodology as template;
  the product-formula and chart conventions must be restated at td12
  scope (the `-bbox`/stacked-fraction and chart caveats of the record
  apply).
- **Exact deliverable:** the AM-2 feasibility theorem-or-verdict per
  family: for each of A7/C5/D13/E11 (witnesses included), B25/S17
  (controls), and `U1*(R)` `R=3,5,7`: `INFEASIBLE` / `FEASIBLE` /
  `UNDERDETERMINED(list)`, fail-closed per §7.
- **Cheapest discriminator:** the `(154,22)@7` E11 run.
- **Outcome meanings:** infeasible detour = first actual-source
  exclusion input to `DETOUR-EXCLUSION/v2`; infeasible `U1*(R)` at
  large `R` = first interface-external selector-relevant theorem
  (pole-inflation kill); feasible everywhere = AM-2 absorbability, a
  reviewed extension of the no-go (bankable); underdetermined = the
  exact source-typing schema `v2` must demand.
- **Stop condition:** if the constraint derivation needs any
  tree-completion law not in the promoted record, return
  `SOURCE_TRANSPORT_OPEN` for that family and continue with the rest.
- **Expected information gain:** highest of the three; every outcome
  feeds a named canonical target.
- **Compute class:** desk (exact rationals; parametric elimination for
  large `R` capped — if it exceeds the cap, preregister for AWS).
- **Ownership:** prove (build system, run) = producer lane; falsify =
  different-model review must re-derive the per-place identity and
  attack the chart conventions (the known trap); bypass = §4's EXCESS
  ladder attacks the same families on the budget side independently.
- **Provisional descendant:** after the B25/S17 controls pass, the full
  sextet + W1–W3 sweep may start pre-review.

### Card C — `EXCESS-LADDER` / `ATYPICAL-EXISTENCE`

- **Novelty:** `NEW` mechanism (§3), with the Sol audit's injection
  program as the cited `KNOWN` neighbor; `DUPLICATE`-risk on the "PCB
  kills the abstract family" fragment, which the integration states
  qualitatively — the exact per-cell arithmetic and the graded ladder
  are the additions.
- **Dependencies:** the promoted Section-7 equality; the printed
  sextet/`U1*(R)` floors, budgets, and ceilings; for the prove lane,
  external published inputs to be pinned exactly (AMS; A^1-fibration
  triviality; fibration/atypical-set basics), plus the new
  `WEIGHT-AT-ATYPICAL` bridge extracted from the promoted Section-7
  statement.
- **Exact deliverable:** (i) a reviewed note fixing the budget
  semantics and certifying the §4 consequence table (what exactly dies
  under `EXCESS-1` and `EXCESS-(s-1)`); (ii) the
  `ATYPICAL-EXISTENCE => EXCESS-1(typical a)` theorem or its typed
  `OPEN` at the weight-bridge; (iii) the residue-A concentration
  computation as the falsify-lane control.
- **Cheapest discriminator:** the residue-A concentration check
  (frozen data, desk); then the weight-bridge attempt.
- **Outcome meanings:** `EXCESS-1` proved at typical fibres kills S17's
  one-step cell and the three zero-slack D13/E11 witnesses at
  typical-fibre scope, and forces any surviving conditional occurrence
  onto the atypical fibre — a major narrowing either way; the bridge
  failing cleanly types exactly what Section-7 weight does not see;
  the residue-A check either supports the cap or bounds PCB's scope.
- **Stop condition:** if `WEIGHT-AT-ATYPICAL` requires identifying
  flags with places or series — firewall — return `OPEN` immediately.
- **Expected information gain:** high; it is the cheapest precise
  engagement with the round's top-ranked global gap.
- **Compute class:** desk / proof work only.
- **Ownership:** prove = producer lane on the bridge; falsify =
  residue-A control plus a hostile review hunting for a
  one-atypical-value model that concentrates weight; bypass = Card B.
- **Provisional descendant:** none before the budget-semantics note is
  reviewed (it underwrites the arithmetic).

## 10. Ranked launch portfolio

1. **Card A** (K00 replay + universality) — mandated gate, cheapest,
   desk; unblocks the whole counterexample frontier.
2. **Card B** (AM-2 place ledger) — highest information gain; feeds
   both the detour exclusion and the selector attack.
3. **Card C** (EXCESS ladder) — budget-side pincer on the same
   families; starts with two desk checks.
4. **Systems:** `ops/seal.py` per §8 (coordinator-applied; acceptance
   test included).
5. Reviews remain background per protocol; the only promotion-blocking
   dependency is that Card C's consequence table must not be consumed
   by any lane before its budget-semantics note passes different-model
   review.

## 11. Stops, nonclaims, firewall

Consistent with the ladder: do not rerun the chi-level genus balance on
book records (known identical closure); do not relaunch the grade-seven
K00 Fitting packet (rejected), `j=42`, S `j=13` standalone, or any
grade-six valuation-one K00 client (dead seed); do not treat any
feasibility outcome of Card B as occurrence or any floor fit as
attainment; do not assume PCB or any EXCESS rung anywhere (Card C
proves or types them).

Nothing in this report asserts an occurrence, attainment, `PairRef`,
source value, degree ceiling, polynomial map, counterexample, or JC2
conclusion. All witnesses discussed are formal reduced records; floors
are floors; a finite jet is not an arc, germ, or map. B and S remain
separate; flags, places, and series remain distinct objects; every
proposed test above fails closed. No exit price is asserted and no
`charge_basis` line is therefore due.

## Charged-input hashes (recomputed this session)

```text
f58ee9b50d640860fa36f34f12f39bcbb0f8fb852e2ace90be8e62323e09ccfb
  xmodel/ideation-20260829T1808Z-state.md
d48e33c594731b4e6f0761c21c28460a15e15a7a9ea80801692b2fdc1a62529b
  its 9040-byte body through BODY-END
6f5006f46e158bce2211a7d408e206ceef0e54d8b0781d1fa92905d57f2cabcf
  APPROACHES.md
bff17fd8acb0f71fbf1ddc0b5f9d1ca30886222e03ea1f9309a92ac5cff707a0
  AUDIT.md
f947045b4d747efe89a8dadc37ab46e13985ce6099e3bbd40dbd93e3b2489083
  COORDINATION.md
460675f20fccbf774067f067d57b09f93a4d8e811fc0f7b7e899957a9a0ea0f0
  PROGRESS.md
2adfb3b6c25552654e3bc6fbac3b54e6d3ace56a861125cde201317455450c57
  ladder/REDUCTION.md
4c2fd1de827361f06bcbfdd465e0cdb6f03c63203c9943f89af60b8aaaa7637d
  xmodel/u1-star-interface-no-go-coordinator-integration-sol56-93d-20260829.md
2fcf8b9d9d0f7a28c9425bf73d30ba893015c69074afef57d2425309826ace50
  xmodel/td12-u1-first-nonneutral-sextet-coordinator-integration-sol56-93d-20260829.md
6c2ca262a32488ea964d648a21f9a5e008f47f270529d9d24af3021b73ddf6f3
  xmodel/k00-valuation6-hand-obstruction-coordinator-integration-sol56-93d-20260829.md
1e3e50e7b06f6d1f5d3e24bacc93af38b3ba3c17477ecab992ad376bb5e19e7d
  xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md
```

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `28151`.
- Body SHA-256:
  `1e530d1f69b8300b297e7c690b96a007b6ad85e58b62536ab017292d7800f1ee`.
- Frozen basis: `e930fa90b8ee9d86220a2cff72fba94e04b20baa`.
