# Conditional one-plus-double: immutable hostile-gate preparation

September9,2026. **PREP ONLY / NO LAUNCH / NO MATHEMATICAL PROMOTION.** The future external tag/report are `triple-one-plus-double-gate-fable5-20260909` and `xmodel/triple-one-plus-double-gate-fable5-20260909.md`. Root alone sets an absolute deadline on invitation; the suggested review duration is25 minutes.

The owned packet is `box/triple-one-plus-double-gate-20260909/`. Ten immutable snapshots contain proof7709b0da..., its four provenance files, accepted cubic producer/gate1620f9ef.../cc5f4ca6..., accepted late-contact producer/gateb5413087.../c9a399a8..., and contact coverf3ab28a6... strictly as an EXPLICIT CONDITIONAL TABLE. The source producer transaction and all six owned/four input pins were verified before copying. Current source/snapshot pins are rechecked at publication. No contact-cover gate or root three-simple report was read.

The whole accepted cubic gate was read at its exact current hash in this prep task; all other whole frozen proof/report bodies were read in the immediately preceding tasks and retained unchanged. No mathematical subprocess or parent replay was performed. Snapshot-tier distinctions are explicit; producer verdicts are not independent proof evidence for the new theorem.

After preparation began, root supplied a dated nonloadbearing prose correction to producer §7. `root-qualification.md` freezes it separately: rebuilt G contributes degree<=1, while `(5/3)z²F` contributes cubic/quadratic terms in the full monic quintic. The reviewer must retain and independently verify that full initial; producer bytes are untouched. `PINS.json` preserves the initial ten-snapshot preparation record. `FINAL-PINS.json` is the AUTHORITATIVE charged manifest, adding the separately frozen qualification. The twelve charged inputs are this final manifest, the qualification, and the ten snapshots.

The six hostile targets cover first scaling/order/degree bounds; Bhat and G15 vanishing BEFORE division; the weight2 homogeneous centralizer and every rebuilt scalar kernel; S|phi²/simple-root removal/exact double table; the second Morse coalesced argument; and the second separated argument with field extensions, both full G signs, corrected full B initial and strict13/12.5 target margins. A separate scope paragraph asks ONLY for the conditional coalesced/three-simple/one-plus-double classification, not proof of the three-simple or contact-cover branches and not a whole-source conclusion.

The prompt forbids ALL mathematical subprocesses of any size, old code/replays, high-power/full-pair/source computation, CAS, AWS/SSH/network, agents and shared/protected/live-peer access. Manual changed-object controls and complete independent prose proofs are required. Exact repeated `charged_input=` declarations, unique basenames, literal `{{LANE_INPUTS}}`, no exit-price declaration and BODY-END/no authored seal are enforced. The preflight uses the actual `ops/lane.sh` sed extraction and compares the twelve-element vector exactly; it does not accept indexed declaration variants.

Owned metadata-only scripts perform hashing, exclusive snapshots and begin/close/finalize/verify; they are included in custody with all input/qualification/manifest/prompt artifacts. `preflight.json` records the actual vector; terminal custody records current hashes and all writers idle. No launch or descendant follows automatically. Root must whole-read/recheck this packet before inviting the reviewer.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `3522`.
- Body SHA-256:
  `cb746d7fa701344567037fa1dad2d3bc0bdee59ccf6eacd790df9587004d054c`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
