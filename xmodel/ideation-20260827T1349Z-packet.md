# Significant-news full-spectrum ideation packet — `20260827T1349Z`

Sealed: 2026-08-27 13:49Z  
Coordinator: Sol / root  
Triggers: provisional exact total ordered-`a1` certificate plus newly found
external full-JC2 claim requiring primary-text audit

Status: **SEALED BLIND SNAPSHOT.**  Never mutate this packet.  Events after
the cutoff are queued.  A C5 review that merely confirms the complete
provisional identity and scope below is a coalesced lifecycle event; a
refutation or scope change aborts this round and requires a reseal.

## Frozen common basis

- Git basis: `418e413593120d19e15e6546eb50c985f4b1f038`; the shared
  worktree contains concurrent uncommitted campaign evidence.  Read only
  except for your one assigned ideation report.
- `APPROACHES.md`: `1112706cd6182d23e360a6e76e5555fcd54264c6470155157f3f7ce42366482b`.
- `AUDIT.md`: `fca661f1c1c041b2a8648d2b6e5bf5f4834d3e44004bada23e5182ca79b08fbc`.
- `PROGRESS.md`: `7ba7ef761d9ee3022eb861daab798d1980efa4fcc881d1675b6ddc0d1c4db40e`.
- `COORDINATION.md`: `039bcdabd9959066b74141262acf109bd9ece326580e56c81f24493c8336c76a`.
- `notes.md`, including the 13:49Z `LIVE STATE`:
  `d2ace1aeb635711daa6f4eaca77644ee65bf5861157509f8c1dff83e93db8105`.
- Prior full synthesis:
  `xmodel/ideation-20260827T1104Z-synthesis-sol.md`,
  `df8c920f019f1d99053af6d81f4eee52ad88c23d2c8e7bc513782ea06efe0eba`.
- Broad web sweep: `xmodel/websweep-20260827T1348Z.md`,
  `a8a1ae4f888a73a7db7ea9d639088c8cdb3c480600e67104955bdd4b535ac5c8`.

Read the complete numbered avenue inventory and current gaps, not only the
new ordered-chart result or your current specialty.  Historical reports are
evidence only at their stated scope; the frozen canonical hashes above and
the exact delta below control this round.

## 1. Ordered `T-a1`: a provisional pure total certificate

Two separately reviewed inputs are now exact in the same literal raw
grade-through-19 source:

```text
a1^104 in J0=J_total|_(t=0),
5*t^6*a1^4 in J_total,          t=rho^2.
```

V43C5 V2 has provisionally replayed their explicit converter in the ordinary
polynomial ring `Q[t,X19_total]` and obtained

```text
a1^628 = sum_j M_j(t,X) R_j(t,X).
```

The identity uses 25 of the 59 nonzero literal total rows, has 950 expression
nodes, contains no localization or division by `t`, and remains a certificate
under `t -> rho^2`.  Its algebra is

```text
x=a1^104,  y=tH,
Phi=sum_(k=0)^5 x^(5-k)y^k,
a1^4*((x-y)*Phi+y^6)=a1^4*x^6=a1^628.
```

The corrected corpus has 70 named slots, 59 nonzero rows and eleven exact
zero slots; every source hash and nine registered mutations replay.  AWS:
239.10 seconds, one core, 153,180 KiB RSS, zero swap.

- freeze: `dab07efa6090152cb8e427992e871c2300c7131d2478c6645bffe9fc200046cd`;
- proof circuit: `f8426bcf6bb4acbe4a2c12e1897588a7b8ce92bc02da84b7fc02d64d64c145e4`;
- result JSON: `215a64b27e3ea43fd3238158a5e07e686d3de4c7d12ff5bde0e691f916938299`.

A separate Grok hostile review is live.  Treat the certificate as a named
provisional premise with a rollback subtree.  Even a PASS closes only the
frozen literal total raw ordered-`a1` chart.  It does not supply a terminal
receiver map, normalized K00 reachability, Gate T, source/landing coverage,
order two, maximum twelve, or JC2.

## 2. Common K00 mixed source: first closed-stratum descendants

- V20R2 is the reviewed honest `Lambda<=19` common source/compiler: 140
  equations on 169 columns, five boundary zeros and 164 free columns, with
  scalar contraction before weighted specialization.
- V21 gives three reviewed *separate* locally nonzero K10/K6/K2 classes; they
  are not one mixed class.
- V22's first honest valuation-one stratum has grade-six equation
  `kappa*F10=0`, `kappa!=0`; `F10` is not in the six-quadratic initial ideal.
  This excludes `D(F10)` but leaves a proper homogeneous affine
  dimension-three closed prefix.
- V23 finds the grade-seven newest-variable block has generic rank five.
  V24 provisionally contracts the complete two-dimensional left cokernel to
  exact compatibility polynomials `C6=C7=0`, conditional on the prior literal
  grades and the normalized chart.  Opus5 hostile review is live.
- A separate `F_65521` localization test found the prior-plus-`F10` ideal is
  the unit ideal, so V24's two modular zero normal forms were vacuous.  This
  does not refute the exact conditional Cramer theorem and cannot prove
  characteristic-zero emptiness.  An exact-Q properness/nonemptiness test is
  the critical next gate and is running on AWS.

Ask whether this should continue as successive constructible strata, be
replaced by a representation-independent Fitting/derived obstruction, or be
connected to terminal receiver incidence in a new way.  Do not infer arc
exclusion from a finite compatible jet or from a vacuous modular normal form.

## 3. GGV/global transport: local carrier completeness, global provenance gap

- The first fibre-tagged one-chain `8_28` control is exact but deliberately
  non-Keller.  A ledger-invisible mutation changes a pole order and total
  mass, proving coarse-ledger insufficiency only for arbitrary polynomial
  controls.
- R1 exactly separates raw `2S/3S` rows from local cusp coefficients.  Raw
  `F_14` has only `X^2`, raw `G_21` only `X^3`, and raw `G_22` is empty;
  local `U_14=X` is not a direct raw polynomial row.  In two named polynomial
  ansatzes, `E_22=M(Y)=4HY'+6H'Y`, `H=X^8-1`, and degree excludes `M(Y)=1`.
- Reviewed R2 closes the higher-`u` loophole rootwise.  After formal local
  Morse normalization with unrestricted `Q`, `Gamma-1`, and `u^4+` sidecars,
  the only weight-22 constant carriers are factorwise
  `6V_8U_14` and `(9/2)U_11^2`.  Different etale factors may choose different
  branches.
- The decisive gap remains global: serialize raw polynomial provenance
  through cleanup/deck gluing and control the global `H`-multiple.  R3
  provisionally gives a rational-mode/pole exclusion for an artificial
  squarefree replacement edge only; its Grok review is live.  It does not
  exclude the original `8_28` family.

Keep `G2-PSC` (packet-to-decorated-tree transport/fidelity) distinct from
`G2-BD` (architecture-specific boundary-divisor input).  A local cusp
compiler or certificate-sized pullback does not by itself discharge either.

## 4. Contact, ramified, and global coverage state

V47R1 and V48 are reviewed bounded compilers for exactly twelve baseline plus
four raised strict unique-`AC` contacts and eight registered negatives.  They
compress allocation and chamber bookkeeping but add no endpoint and prove no
fan cover.  Equality faces, positive loads, ramified `rho=0` deck/square
fibres, terminal receiver incidence, off-family source/landing, and cofinal
degree/type control remain open.

The AS109 low-Witt theorem cuts one `v_109(beta)=1` content stratum at target
degree six but does not empty that target.  TD6, formal-germ algebraization,
guided sparse counterexample search, monodromy/boundary, and all other avenue
families remain eligible for reranking.  Do not let recent V43/K00 density
collapse the portfolio.

## 5. New external intelligence

The broad sweep found Lukasz Matysiak, *The Jacobian Conjecture in Dimension
Two and the Role of the Condition 5''s*, SSRN 7229458 (19 pages, posted
2026-08-10).  Its abstract explicitly claims a complete JC2 proof in two
forms, relying on an endomorphic square-free-factorization condition `5''s`
developed in predecessor SSRN 7229358 (34 pages).

SSRN's Cloudflare challenge blocks both PDFs from the campaign environment,
and no lawful mirror was located.  Classification is
`ACTIONABLE-UNAUDITED / BLOCKED-BY-PRIMARY-TEXT-ACCESS`, not “external
progress” or “JC2 solved.”  The author's 2024 paper also advertised proofs,
which lowers prior credibility but is not a mathematical refutation.

Every report must address both possibilities: the fastest lawful
dependency-first audit if primary text is acquired, and whether the abstract
suggests a genuinely useful square-free/image-subring connection that can be
stress-tested independently without assuming its theorem.  Watch for
circular injectivity/properness, factorial closure, image-UFD/normality, and
dimension-count assumptions.

## 6. Capacity and lifecycle

- Heavy algebra runs only on AWS.  Regional quota is 512 vCPUs; additional
  machines up to 1 TiB RAM may be started when information gain warrants it.
  Desk-scale exact scripts and source review may run locally.  Never run
  heavy Singular locally.
- Provisional results may drive safe descendants while hostile review runs in
  parallel.  Every descendant names its premise and rollback subtree.
- Do not enter, read, build, status-inspect, or modify `jc2-lean`; it is a
  user-owned parallel formalization and does not conflict with this campaign.
- Sol, Fable5, Opus5, Grok, and all three active research agents are core
  whole-portfolio ideators for this round.  Fable5 and Opus5 have equal
  standing and receive this identical packet independently; neither may read
  the other's report.  Model identity supplies neither a bonus nor a vote.

## Required blind submission

Without reading any other `ideation-20260827T1349Z-*` report, submit:

1. a compact disposition vector over **every numbered avenue** in
   `APPROACHES.md`: `unchanged`, `raise`, `lower`, or `reopen`, with reasons
   for every change;
2. reranked proof and counterexample bottlenecks based on the complete state;
3. at least one genuinely new avenue or mechanism and at least one new
   cross-avenue connection, with a repository/history check rather than a
   novelty assertion from memory;
4. the strongest proof attack and strongest falsification/counterexample
   attack;
5. one software or AWS acceleration and the exact correctness firewall;
6. at most three detailed idea cards, each naming dependencies, cheapest
   discriminator, interpretation of PASS and FAIL, stop condition, rollback
   subtree, and expected information gain;
7. `continue`, `redesign`, or `stop` for every current major lane;
8. explicit analysis of the provisional total-certificate PASS/reversal
   branches and the Matysiak acquisition/audit branches.

Keep the submission decision-oriented.  The coordinator will deduplicate by
target obstruction, mechanism, object, and decisive test, then run the
canonical history checksum before cross-pollination or launch.
