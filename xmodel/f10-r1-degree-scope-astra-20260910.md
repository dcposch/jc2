# F10 r1 certificate: reconciliation of actual-degree scope

2026-09-10. Status: UNREVIEWED conditional interface composition; no new proof of an imported premise and no runtime work. Basis 0d39df3c9fd69c939a8420c54d03228b9077777d is publication provenance only.

First action: 11:59:31.531182272 UTC. Controlling stop: 12:17:31.531182272 UTC, the earlier of first+18 minutes and 12:19. Publication reserve begins 12:14:31.531182272. Exact owned targets were absent before the lease. Only the two charged objects were hashed before their whole reads.

## Decision

YES, at the named imported scopes. The charged accepted propositions already imply that no characteristic-zero Keller pair has actual total degrees 112 and 196, in either order. Together with 16m, they exclude the entire numerical family

    (deg P, deg Q) = (28(q+2), 28(2q+3)), q >= 0,

and its output-swapped version. This is KNOWN CONDITIONAL COMPOSITION / DUPLICATE DERIVATION: 16r explicitly announced precisely this consequence of an r1 unit. The present task supplies the missing documentary reconciliation with the now-accepted zero-ring endpoint, not a new mathematical mechanism or fresh acceptance of external reductions. This scope reconciliation itself remains UNREVIEWED pending ROOT's separate first gate.

## 1. Quantifiers and named premises

Fix the accepted normalization, published reduction/completeness, and internal transport propositions in the two charged objects. The conclusion is

    For every characteristic-zero field K and P,Q in K[X,Y],
    [P,Q] in K* implies (deg P,deg Q) != (112,196),(196,112).

Degrees here are ACTUAL ordinary total degrees, not receiver, weighted, generic, or table-only degrees. Let q_col denote the numerical-column index, j_F10 the printed F10 index, and r=j_F10+1 the increasing-exponent compact parameter. These are different indices.

The necessary-source premise in 16m is universal over actual pairs, not existence inferred from a row. For q_col=2 it selects an ordinary rectangular representative of printed F10(0), after output swap; hence j_F10=0 and r=1. Its explicit composition paragraph supplies 16k's actual-source antecedents. The 16p composition paragraph then supplies the entire specified F10 stratum's compression antecedents. Neither step is an extrapolation from equal numerical degrees.

## 2. Arrow audit from an arbitrary given pair

Assume a pair P,Q as above exists, exchanging the two outputs if necessary.

1. **Move the given finite data to C.** Its finitely many coefficients generate a finitely generated characteristic-zero field over Q, which embeds in C. Injectivity preserves nonzero coefficients, actual degrees, and the nonzero constant Jacobian. No normalizer over the original K is required. This is also the field-transfer route expressly retained in 16m.

2. **Apply the ACTUAL-pair selection of 16m.** At q_col=2, the accepted normalization and complete-chain interface provide an affine-normalized ordinary rectangular representative, with printed F10(0) profile (7,4), corner (7,21), opposite corner (1,0), next point (13/7,3), direction (7,-2), type II.b and k=1. The degree identity forces inverse-coordinate degrees M=N=1. Output swap is licensed by transferred face multiplicities and horizontal standardness, not solely by a ratio match; it changes only the sign of a nonzero Jacobian scalar. The increasing receiver exponents are (4,7).

3. **Use the ENTIRE necessary compression, 16p.** Its stated 16k-to-16l/16n-to-16o composition covers this ordinary source. Actual polynomiality forces u=-h in its notation. With S=R-v0 and t=z^-1, the whole outputs have t-degrees 3/5, nonzero leading coefficients C0*S and D0*S^2, and C0*D0=c!=0. Coefficient bounds are deg_S A_i<=4-i and deg_S B_i<=7-i, with the monic base degrees 4/7 attained before scalar normalization. The exact target is c*Delta, including its -c*S^2*t^7 term, not a constant-Jacobian target in this chart. The accepted two-ring intersection and retained inverse-pole condition cover the whole boundary. This is not a receiver-only or leading-form point; no backward printed-chain theorem is needed for this forward contradiction.

4. **Pass to the complete guarded Euler presentation, 16r.** All 16p necessary sources map to L_1. The permitted scalar normalizations, output translations and B->B+beta*A+gamma gauges normalize A_3=S, B_5=S^2 and k(0)=[S]B_3=B_0(0)=0. Their former monic coefficients become a,b, retained by omega*a*b=1, not set to 1. Both full lower identities E1,E0 remain; the upper recurrence and inverse-pole consequences are accepted polynomial identities on every stratum, including u=0. Thus the given source yields a C-point of the complete L_1, not a projected subset of equations. This step uses the explicitly stated necessary direction, not just the sufficient counterexample endpoint.

5. **Normalize leading coefficients with the scale retained, 17b.** The leading ODE at every guarded L_1 point forces H!=0. Therefore s=H/a is a unit. The displayed substitutions identify its leading data with the FULL algebra B=Q[v]/(p), retaining s and omega=w*s^8*f5. The proved units L,w,f5 lose no branch. The existence equivalence is stated in both directions over every characteristic-zero field, preserving inverse-pole conditions and EVERY remaining 17 row. No preselected factor of B restricts this construction, and no extra same-field root choice is required.

6. **Compose all four bands, 17j and its whole gate.** The triangular quotient maps retain complete mixed forcing, two compatibility equations, all remaining lower rows, gauges and guards. In particular the corrected third forcing uses D_i, not D_(i+1). The full point maps to the eleven-slot quotient over B[s,s^-1]. Normalized reconstruction has z=s^2 and exact unsquared targets H0=s^7, H1=U*s^5. Eliminating s is an all-algebra equivalence using s=H0/z^3; z and H0 are forced units, not additional generic exclusions. The optional two-coordinate slice has only algebraically closed-field nonemptiness equivalence (or at most cubic field extensions), which is sufficient here but is not needed as a same-field ring isomorphism.

7. **Use the accepted complete zero-ring endpoint, 17zze.** Its certificate proves the entire R_uni zero. The accepted 17l/17x transport identifies the COMPLETE normalized 17j guarded ring as the monic cubic cover of that ring; hence that complete ring is zero. All components and full-slot/inverse-pole data within these maps are retained. A point from step 6 would give a unital map from this zero ring to C, impossible.

This is the contradiction. There is no first missing source arrow among the accepted statements as charged.

## 3. The apparent conflicting caution

17zze's final caution says its reviewed endpoint is not, by itself, a proof about every 112/196 map; it also expressly says the external source-admissibility chain was not newly re-proved. That appropriately limits the scope of that isolated certificate review. It cannot erase the separately accepted necessary arrows in 16m/16p/16r/17b/17j when those propositions are now explicitly consumed.

The resulting statement is conditional on those imports. It is not an assertion that the certificate alone re-audited them. If “not every map of degrees112/196” is instead read as a blanket campaign-wide prohibition even AFTER composing those accepted premises, it conflicts with 16r's explicit “unit at r=1 would close ...112/196 ... via16m” and should be qualified, not turned into an invented mathematical gap.

Neither coordinate-invariance of r nor transporting the computed cofactor back to arbitrary original coordinates is required. One normalized representative suffices to contradict its necessary existence. The accepted certificate is in the normalized ring. There is no newly computed original-source cofactor; field-point exclusion and an explicit transported certificate are different deliverables. If desired, finite guarded presentations and Nullstellensatz recover existential ideal-unit statements, not new explicit cofactors.

## 4. Entire-column consequence and perimeter

For q_col!=2, 16m already excludes all actual pairs in the displayed numerical family. Section 2 excludes its sole remaining q_col=2 exception. Therefore the whole D28 NUMERICAL COLUMN is conditionally empty.

Inherited trust is exactly 16m's Division Lemma/ML ordinary normalization through GENERAL15x; GGV5.20 and cited interiors; GGHV2.20 and cited interiors; Prop3.2 actual MN membership; Def3.3/(3.20); whole Section5 M=35 completeness in both orientations; accepted16h/16g; and the internal source, normalization, quotient and certificate transports quoted above. The present read does not fresh-verify any primary proof or expand any imported scope.

No conclusion follows about every pair whose degrees are multiples of 28, arbitrary degree pairs of gcd28, every degree up to196, a maximal-degree196 bound, all-r F10, global source coverage, or JC2. The column is exactly the displayed parametrized family. An arbitrary bare Laurent standard pair or an unrealized table entry is not substituted for an actual ordinary source.

## 5. Changed-premise controls and stop

- Remove 16m's actual normalization/selection proposition: normalized r1 zero alone does NOT exclude every same-degree pair. The missing implication would then be “given pair -> actual F10(0) representative.” This is absent only in that deliberately weakened premise set.
- Replace the COMPLETE 17zze endpoint by a selected leading-factor or generic-chart unit: a point in an omitted component need not map to it. The accepted endpoint is explicitly whole-algebra, with z,H0 units forced by the retained scale/target.
- Keep only the leading ODE: it has solutions and yields no exclusion. The full lower rows and inverse-pole conditions in steps 3–7 are indispensable.
- Omit an explicit original-source cofactor while retaining all accepted existence arrows: the contradiction still goes through. Demanding that cofactor as an additional necessary premise is the failed stronger reading.

No new open mathematical quantity is raised. The only outstanding action is ROOT's separate first review of this narrow quantifier/scope composition before any promoted wording; no old proof farm, scientific replay, new source claim, or follow-on is authorized.

## Read scope

The complete 71-line ACCEPTED-INTERFACES.md and complete 93-line weighted-composition gate were freshly read after both required hashes matched. The former consists of six labeled literal AUDIT excerpts, not the whole ledger or underlying proofs. No linked originals, provenance bodies, code, certificate, current ledger, peer, or external source was inspected. Earlier campaign involvement is disclosed and supplies no additional premise.

## OPENS RAISED

None. Existing imported propositions are conditional premises, not newly opened proof-audit tasks.

## COLLISIONS

EMPTY. Exact report, transaction and owned box were absent at first action. The no-overwrite lease owns this publication. No corpus scan was performed.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `11125`.
- Body SHA-256:
  `9838c8a9ff1d6236294c041eabe7dc0ce7c8ebac2be081770fc6e02d29ba97a2`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
