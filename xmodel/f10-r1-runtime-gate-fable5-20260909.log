cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
The gate is complete and closed before the 13:50:36 UTC stop. Nothing further is needed.

**Verdict: RUNTIME PASS at the permitted scope.** The report is at `xmodel/f10-r1-runtime-gate-fable5-20260909.md`, ending with the standalone body-end line, no Seal and no charge basis. Support files are in `box/f10-r1-runtime-gate-fable5-20260909/` with their own custody hashes.

What was recomputed, not assented to:

- **Custody.** All 143 charged snapshots hashed first; every one matches the charged vector.
- **A. Binding.** All 16 batch records equal their dispatch files in the frozen caller's order. Authority and telemetry SHAs, stdout/stderr paths, byte counts and SHAs all bind to snapshot bytes. Argv digests recomputed from recorded child argv with CAPRUN's declared encoding match 16 of 16. Parent argv equals the exact required runner form. All nine source pins plus the interpreter pin match the caller's hard-coded values. The five refusal authorities carry exactly their intended defects, and no mathematical authority is missing a pin. All nine mathematical records share one budget clock and one cutoff, and every return precedes it. Final audit PGIDs equal the 16 telemetry PGIDs with nothing live. Poststate is stopped with volume retained.
- **B. Controls.** Five refusals exit 1 with the matching gate line and no sentinel. Valid gate wrote its sentinel. The descendant test shows the TERM-ignoring child in the leader's group, RSS cap exceeded, exit 125, and the exact four-event identity sequence with consistent PID, PGID, start identity and namespace.
- **C. Checker.** Every mode's argv, receipt authority and artifact SHA bind to the same execution. Full and u-zero report 8 bracket, 10 inverse-pole and 20 ideal slots. Each negative mode rejects with its intended string. The artifact has the fixed 12-variable order, 20 rows, string rationals and no float token.
- **D. Precision.** Ran on this artifact, guard precondition held, float payload and rounded canonical string each rejected on the intended path. Scope stated as two mutated guard payloads only.

Two boundaries are stated in the report: the interpreter pin is root-established and not in the snapshot, and null cleanup fields on normal exits are expected and distinct from the tested dummy cleanup. No OPENs raised, no collisions.
DIVERT path=xmodel/f10-r1-runtime-gate-fable5-20260909.md status=CLEAN body_bytes=11684 body_sha256=5547777e3f5c114e7de5bba830269f4d307131cc5cf67c61a406e1aca9f272ec
