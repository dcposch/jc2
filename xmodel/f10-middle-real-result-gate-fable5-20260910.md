# f10-middle-real-result-gate-fable5-20260910 — FIRST Fable5.1 result gate: actual real-triangle certificate, four semantic controls, uniform middle-unit composition

status: UNSEALED (adapter seals; no Seal section and no charge_basis authored here; the standalone BODY-END marker is the last line)
lane: f10-middle-real-result-gate-fable5-20260910
first_action_utc: 2026-09-10T12:25:43Z (sha256sum of all 22 inputs before any body)
stop_utc: 2026-09-10T12:43:43Z (first+18 min, earlier than 12:46:00Z); reserve 12:40:43Z; never reset
mode: manual reasoning/text/hash only; ZERO mathematical subprocess/import/AST/syntax/test/CAS of any size; no network/AWS/SSH/process/Git/agents/corpus/shared/protected/other-lane/live-file read; no provenance traversal; no input edit
owned: xmodel/f10-middle-real-result-gate-fable5-20260910.md and box/f10-middle-real-result-gate-fable5-20260910/input_custody.md (both absent at 12:25:43Z); all writes apply_patch only

## 0. Custody, read record, disclosure

All 22 snapshots in /tmp/jc2-lane.CtJFkX/inputs were hashed before any body was read; all 22 equal the charged list (live output reproduced in the owned input_custody.md; abbreviated tokens below). 21 objects were read WHOLE as text after the match: ten markdown reports (domain astra 3f79cabe / gate f04c174e, resonance astra 8cf54b2f / gate dc43ef1c, full-boundary astra 82f11ed7 / gate 8a80bcc8, univariate astra 890e5c9c, septic gate cff18a06, code gate d3b8c092, controls gate 4ba018dbb, positive ROOT report b84a4c41, controls ROOT report 8aa0e760), POSITIVE-CUSTODY.json f4d62113, CUSTODY.json 790fa2c9, check.payload 1a9bb0f5, check.telemetry.json 46a9d4bf, mutate.payload beb732db and the four stderr files. certificate.json 21915179 (854264 bytes) is HASH ONLY: never opened, no coefficient read, parsed or computed. Four whole-file displays overflowed the tool window and were re-read WHOLE from the persisted copies; nothing was clipped. No file outside the 22 was opened.

Disclosure. The code gate 17zzl, the controls gate 17zzq and the four mathematical gates (domain, full-boundary, resonance, septic) were authored by Fable 5.1, the same model as this lane; they are accepted premises here and are not re-reviewed. Independence in this gate is relative to the Astra producers and the ROOT executor, and this is the FIRST review of the runtime artifacts (receipt, telemetry, custody, mutator receipt, stderr). Same-byte reuse: POSITIVE-CUSTODY.json is an exact byte copy (SHA f4d62113) of the secured first-positive CUSTODY.json, renamed only to avoid a basename collision; nothing else in the inputs or in this report is a byte reuse. Earlier "actual unknown / no theorem yet" statements in the accepted pre-runtime reports are read as time-scoped, not as contradicting premises.

## A. Executed independent checker, binding of receipt and telemetry: CONFIRMED

The accepted contract 17zzi (domain astra + gate) and the accepted code gate 17zzl establish, as premises at their scope, that checker 9369f0f3 reconstructs E_X/Q_X from the seven- and eight-term displays, forms S and T without the producer's determinant, requires the identity U·S+Wcof·T=R coefficient by coefficient with every V^k (k>=1) coefficient zero ("positive V cancellation"), forbids parameter denominators (integer constants only), performs the exact rectangle pullback, recounts the maximal (1-t)^j and compares it to the declared j, and compares the full tensor-Bernstein expansion and every sign, then equates all 22 collapsed-row entries to |Rhat(0,0)|. No uncharged code was opened here; that premise is consumed, not re-derived.

Binding, from the receipts themselves. check.payload: schema f10-middle-real-check/v1, status CERTIFIED_NONRESONANCE, input_sha256 21915179 (equals the hashed certificate), code map exactly {authority 45871331, checker 9369f0f3, producer 44859af3} (equals the ROOT pins and the mutator receipt's map), registration_sha256 16ff2f07 (equals authority/check.authority.json in POSITIVE-CUSTODY), j=4, degree_t=36=40-4, degree_s=21, bernstein_entries 814=37x22, retained edges t=0, s=0, s=1, scope "accepted17zzi real triangle only". check.telemetry.json: schema CAPRUN/v1, NORMAL_EXIT, child_returncode 0, pid/pgid 8955, start_identity boot 1dd41ed9 / start_ticks 93103, cwd /opt/jc2-real-20260910T1022, caps 45 wall / 49 CPU / 2 GiB, 10:27:44.483772 to 10:27:48.005444 (3.5217 s), stderr 0 bytes, stdout 23 bytes 57dcf10c: every one of these equals the POSITIVE-CUSTODY entries (check.telemetry.json 46a9d4bf, check.stdout 57dcf10c, check.stderr e3b0c442, runs[check] child_returncode 0) and the ROOT report's section 4. The checker emits its receipt only at the end of the full verify path (17zzl D), so a receipt with status CERTIFIED_NONRESONANCE plus child exit 0 and empty stderr is the executed positive outcome, not a partial.

Reliance stated exactly. This gate did not and cannot re-verify a single coefficient: the body is hash-only by charge. The claim "the identity and the Bernstein positivity hold for the bytes 21915179" rests on (i) the reviewed checker code 17zzl, (ii) its actual execution on the ROOT-attested worker under the pinned authority with unchanged sources (ROOT report sections 2-3, native manifest 134e1732 and 1217-file closure), and (iii) the receipt/telemetry/custody bindings above. The j=4 / 36x21 / 814-entry witness is a finite exact object checked in full by that run, not a parameter sample. Nothing here infers correctness from the SHA alone.

## B. Four actual semantic controls: CONFIRMED

Mutator semantics are the accepted 17zzq premise: U constant +1, R constant +1, literal j '4' to '5', interior Bernstein [1][1] +1, each on a deepcopy with changed!=data and restored==data, wire-canonical, no other field touched. mutate.payload: schema f10-middle-real-mutator/v1, status FOUR_CHANGED_FIXTURES, mutator_sha256 7969c503 (the bytes reviewed by 17zzq), original 21915179, positive_receipt 1a9bb0f5, code map identical to check.payload, four fixtures each 854264 bytes, changed true, readback EXACT, with expected_error strings and hashes U a2c6e9e5, R 05d40df9, j 3a8c06fb, bernstein a18b33e2.

CUSTODY.json 790fa2c9 result: status FOUR_SEMANTIC_REJECTIONS_CONFIRMED, positive_science_reruns 0, original_input 21915179, positive_baseline_receipt 1a9bb0f5, mutator_receipt beb732db (equals the snapshot and the writer/mutate.payload entry); four checks with normal_exit 2, receipt_absent true, fixture hashes equal to the mutator receipt, stderr hashes 5d627b6b / a4974bfa / 0ba3a181 / ddcb3719 equal to the four charged stderr snapshots, telemetry hashes equal to the runs entries. The four literal stderr bodies are exactly "INCONCLUSIVE: positive V cancellation", "INCONCLUSIVE: full cofactor identity", "INCONCLUSIVE: wrong maximal excluded-edge factor", "INCONCLUSIVE: full Bernstein expansion", each plus one newline (38, 37, 49, 39 bytes; own character count agrees). Those are the checker's identity, cofactor, edge-count and expansion branches (checker lines 223/225/240/268 per 17zzq B), not stale-pin, schema, parse or resource refusals (those print different strings and the dispatcher types them STOP). All four stdout files are 0 bytes and no check-* receipt exists in the CUSTODY inventory. The frozen inventory lists the four mutated fixtures under frozen/ and the controls ROOT report records the move/0444/0555 freeze of all four before the first check; the CUSTODY run order is nine preflights, mutate, then check-U/R/j/bernstein.

Differential meaning. The unchanged checker accepted the baseline (batch 1) and rejected each fixture at its intended branch (batch 2); since the fixtures keep the original byte length, the hash difference plus the differential verdict is what proves the mutation was applied. Exactly four semantic classes are claimed; the 14 runs of the new CUSTODY are 9 authority/startup/dummy controls (five refusals exit 2, valid exit 0, two wrong-job startups exit 2, dummy RESOURCE_CAP/rss) plus mutate and the four checks, not fourteen positive proofs. Hardware attestation (instance i-08d2a40f272ee9fa2, fresh boot 5ef9c85d, pid namespace 4026531836) is ROOT's explicit statement; no physical verification is possible in this document-only gate and none is claimed.

## C. Whole real triangle, retained edges, strict positivity, complex common roots: CONFIRMED

Cover. tau=(6+t)/21 maps t in [0,1) onto [2/7,1/3); 7tau-2=t/3, so alpha=3-3tau+(7tau-2)s=(45-3t+7ts)/21 covers [3-3tau,1+4tau] as s runs over [0,1] (at t=0 the interval is the single point alpha=15/7). Only t=1 (tau=1/3) is excluded, where R vanishes identically (D^2 | S, D | T at tau=1/3); the removed factor (1-t)^4 is positive for t<1, and t=0, s=0, s=1 are retained (receipt retained_edges).

Positivity. With G=epsilon·Rhat/(1-t)^4 of bidegree (36,21), the checker verified all 814 coefficients b_(i,jj)>=0 and the entire i=0 row equal to the positive endpoint |Rhat(0,0)|. Since every tensor-Bernstein basis element is >=0 on [0,1]^2 and the i=0 row contributes (1-t)^36·|Rhat(0,0)|·sum_jj B_jj^21(s)=(1-t)^36·|Rhat(0,0)|>0 for t<1 (partition of unity in s), G>0 strictly at every t in [0,1), s in [0,1]. Hence Rhat, and so R(tau,alpha), is nonzero on the whole triangle and has the constant sign epsilon there; R itself need not be positive (epsilon may be -1), exactly as the charge states. The identity R=U·S+Wcof·T lives in Z[tau,alpha,V] with no parameter denominator inverted (only the integer unit 10080 in the division, and 21^40 in the pullback), so it specializes at every point of the triangle: a common complex root V0 of S(tau;·) and T(tau,alpha;·) would give R(tau,alpha)=0, contradiction. Therefore no common complex root, not merely no real one. No unverified denominator, inversion, subdivision or prime sample enters.

## D. Uniform middle-unit composition for every actual (r,h): CONFIRMED

Lattice inside the triangle. For integer r>=2 and r+2<=h<=2r: tau=r/(3r+1) is 2/7 at r=2, increasing, <1/3; alpha=(8r+3-h)/(3r+1) equals (6r+3)/(3r+1)=3-3tau at h=2r and (7r+1)/(3r+1)=1+4tau at h=r+2, decreasing in h, so every linked pair lies in the closed triangle, including the collapsed point (2,4). This holds for every residue of r modulo 7 (and modulo anything): the certificate is a statement on the real triangle and never sees residue families, so r = 4,5,6 mod 7 are covered exactly as the rest.

Inverse construction, whole ring. Fix an actual (r,h). Accepted 17zzd/septic: A_nu=Q[V,W]/(E_nu,Q_nu)=Q[V]/(S) with D a unit (Bezout, only f,e inverted), the W·t5 guard automatic so B_nu=A_nu, and T=D·Z in A_nu. Specialize the certified identity at the rational point (tau,alpha): in Q[V]/(S), Wcof(tau,alpha,V)·T(tau,alpha,V)=R(tau,alpha), a nonzero rational by C. So T^{-1}=Wcof/R, Z^{-1}=D·T^{-1}=D·Wcof/R, both explicit elements of Q[V]/(S). Accepted 17zz (8): H7_h=s_scale^7·(alpha)_3·(alpha-nu)·Z/5040 in L=B_nu[s_scale,s_scale^{-1}], with s_scale, (alpha)_3, (alpha-nu), 5040 units, so H7_h^{-1}=5040·s_scale^{-7}·D·Wcof/((alpha)_3(alpha-nu)R). This is a ring identity valid on every component and with nilpotents (in fact A_nu is etale of rank 7, but that is not needed); no irreducibility of S or T is used. The rectangle coordinate s of the certificate is a parameter of alpha and is not the Laurent source scale s_scale; the two symbols never interact. Conclusion at the accepted 17zz/17zzd/17s interface: H7_h is a unit of L for every actual r>=2 and every h in r+2..2r, uniformly, closing the middle unit question raised in 17s/17zz (the sole GAP left by the resonance, full-boundary, univariate and domain reports).

## E. Outcome perimeter: CONFIRMED

What is licensed: wherever the accepted 17s interface writes the middle coefficient row as H7_h·Y_h+J_h=0, Y_h=-J_h/H7_h is now an exact division by a unit of L for every actual (r,h). What is not: earlier-row compatibility, actual mixed forcing, later rows, inverse-pole and source guards, the whole C_r or C_2 source ideals, any r2 source emptiness, any bound on r, any original polynomial map, any positive counterexample, any full uniform source claim, and JC2 itself remain exactly where they were. The certificate is sufficient only: a failure would have been INCONCLUSIVE, and here the positive witness plus the four controls are what is claimed. The other eight corruption classes of VALIDATION remain SPECIFICATION; no automatic new gate farm, rerun, subdivision, prime sweep, larger cap or engine is authorized or requested by this gate.

## F. Data, caps, custody scopes reconciled: CONFIRMED

| quantity | value | source agreement |
|---|---:|---|
| positive math window | 19.040856795 s | POSITIVE-CUSTODY result, ROOT section 4 |
| producer + checker walls | 15.0020 + 3.5217 s | telemetry / ROOT; sum below the window (admission gaps) |
| controls math window | 8.433348330 s | CUSTODY result, controls ROOT |
| mutator + four checks | 0.2852 + 0.6722 + 0.6706 + 1.6120 + 3.7642 s | controls ROOT table; sum below the window |
| positive runs / files | 11 / 116 | own count of POSITIVE-CUSTODY (9 preflights + produce + check; 101 stream/record + 3 payload + 1 frozen + 11 authority) |
| controls runs / files | 14 / 149 | own count of CUSTODY (126 + 2 + 3 + 4 frozen + 14 authority) |
| remote-local hash matches | 233 + 23 (positive), 325 (controls) | ROOT sections 5 and closure |

The CPU figures (20.4933 / 7.6970 s conservative RUSAGE_CHILDREN including CAPRUN/ps; dispatcher 2.6648 / 3.2306 s) are labelled as not payload-only and not billing; no quote, billed duration, credits or inference cost is inferred anywhere and none is here. Terminal order: both ROOT reports check the correct SYSTEM unit, MainPID/PGID and cgroup absent (10:33:11 and 10:33:54; 12:15:38.595) BEFORE consuming custody, and both disclose that the transient unit's default Result=success is not outer-exit evidence; child exits are preserved in per-run telemetry. Timers: original USER TERM/KILL and AWS stops were armed before launch and never reset in both batches. The two batches ran on different boots (1dd41ed9, then 5ef9c85d) with the same instance and native closure, as disclosed. Pin relations checked here and found exact: receipt code map = mutator code map = ROOT source pins; receipt registration = positive check authority; telemetry = custody = runs entry; four stderr = four custody digests = four literal branches; four fixture hashes = mutator receipt = frozen inventory; baseline and receipt hashes identical across both custodies; root_registration 2f86ce99 / 8a2b4602 equal to the two ROOT reports. No whole-component gap and no pin relation error was found. The acceptance is of actual evidence with disclosed trust in the accepted code gates and ROOT custody; no rerun, computation or hardware check was fabricated to fill the parts this limited review does not inspect.

## Verdicts, theorem wording at supported tiers, GAP

| item | verdict |
|---|---|
| A executed reviewed checker; receipt/telemetry bind candidate, sources, authority, exit 0; finite exact witness; reliance disclosed | CONFIRMED |
| B four valid-authority semantic rejections at the intended branches; exit 2, empty stdout, absent receipts; frozen before first check; 9+1+4 runs; no positive rerun | CONFIRMED |
| C whole triangle covered, only t=1 excluded, edges retained; G>0 strictly; R nonzero of constant sign; no common complex root | CONFIRMED |
| D every actual (r,h) in the triangle; T^{-1}=Wcof/R, Z^{-1}=D·Wcof/R, H7_h^{-1} by unit prefactors; whole ring; all r including 4,5,6 mod 7 | CONFIRMED |
| E licenses Y_h=-J_h/H7_h at the 17s interface only; nothing about forcing, later rows, guards, sources, r bound, map, counterexample, JC2 | CONFIRMED |
| F windows, counts, custody, terminal order, timers, pins reconciled; no billing inferred; trust disclosed | CONFIRMED |

REFUTED: none. GAP: none new for the certificate and controls at their scope.

Tier 1 (accepted 17zzi contract + executed reviewed checker 9369f0f3 on bytes 21915179 under ROOT custody f4d62113): for every real (tau,alpha) with 2/7<=tau<1/3 and 3-3tau<=alpha<=1+4tau, R(tau,alpha) is nonzero with constant sign epsilon, and the septic S(tau;V) and quintic T(tau,alpha;V) have no common complex root.

Tier 2 (Tier 1 + accepted 17zz, 17zzb, 17zzd, septic gate): for every integer r>=2 and every h with r+2<=h<=2r, Z is a unit of B_nu=Q[V]/(S) with inverse D·Wcof(tau,alpha,V)/R(tau,alpha), and the middle coefficient H7_h is a unit of L=B_nu[s_scale^{+-1}] with inverse 5040·s_scale^{-7}·D·Wcof/((alpha)_3(alpha-nu)R).

Tier 3 (consequence at the accepted 17s interface): Y_h=-J_h/H7_h is an exact division for every actual (r,h). No statement beyond this tier is made.

Precision items, none changing a verdict: (P1) the equal 854264-byte size of all four fixtures is expected (+1 without a digit-count change, j one character) and is not a "no-op" signal, since the differential verdict and the hashes prove the change; (P2) the producer's 15.00 s wall and the checker's 3.52 s wall are far below their 300 s and 45 s caps, so the 17zzl static CPU-risk note on the producer's Fraction DP is discharged by observation; (P3) reduced-ness of A_nu is available (septic gate) but unused in D, so the composition is robust to any future re-scoping of that etale result; (P4) the abbreviated token "4ba018dbb" in section 0 is a nine-character prefix of the controls gate hash, correct but one character longer than the other abbreviations.

## OPEN(S) RAISED

None. No new canonical OPEN ID. The middle H7-unit quantity of 17s/17zz is closed at Tier 2 above; the remaining full-source quantities (earlier compatibility, mixed forcing, later rows, guards, C_r/C_2, JC2) are unchanged and are not re-raised here.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly xmodel/f10-middle-real-result-gate-fable5-20260910.md and box/f10-middle-real-result-gate-fable5-20260910/input_custody.md, both absent at 12:25:43Z; apply_patch only; no Write/Edit tool, no shell redirection to any file, no helper, source, fixture, input or charged-file change, no Seal, no charge_basis, no subprocess of any mathematical kind, no rerun, registration, network, AWS, SSH, Git or agent action.

## Completion

Authoring attestation: the custody file (12:27:48 UTC) and the report body (12:29:58 UTC) were written by apply_patch only; this section and the two precision edits above are the final apply_patch. No Write/Edit tool, no heredoc or shell redirection to any file, no helper. Own WHOLE read of this report and of the custody file completed 12:30:13 UTC with a marker-absence check (zero standalone markers in both owned files before this write), a 64-hex-token audit (the report carries zero full tokens; the custody file carries exactly the 22 charged tokens and zero foreign ones, `uniq -u` against live sha256sum empty) and an abbreviated-prefix audit (every 8-hex prefix in the report is either a charged input hash or a literal token occurring in a charged body: source pins, fixture hashes, boot identities, registrations, check.stdout and the empty-file digest; the certificate body was excluded from that grep). Verdict for a reader of this section alone: A-F CONFIRMED, REFUTED none, no new GAP; the actual certificate 21915179 and the four semantic controls are accepted at Tier 1, the uniform middle H7-unit composition at Tier 2, the Y_h division licence at Tier 3, with trust placed explicitly in the accepted code/controls gates and ROOT custody and no coefficient recomputed here. No follow-on, computation, rerun, registration, seal or charge_basis is authored. Raised-OPEN check (none) and collision check (EMPTY) precede the marker and the 12:40:43 reserve. The marker below is the only standalone marker in this file and nothing follows it. All writers idle at the marker.

<!-- BODY-END -->
