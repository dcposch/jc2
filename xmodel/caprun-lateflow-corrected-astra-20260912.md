# CAPRUN corrected late-flow orchestration

STATIC_CORRECTIONS_IMPLEMENTED / UNEXECUTED / INTERNAL-UNREVIEWED. Disabled and unbound; no worker, science or installation. First action2026-09-12 05:22:01UTC, owned targets absent. Original reserve05:40/HARD05:43 unchanged. All original files remain unchanged.

## Result and exact scope
The new versioned tail supplies four separately selected ROOT entries: ADMIT, ATTEST, INSTALL, RELEASE. Six source/config derivatives, two host-separated manifest templates and six literal diffs accompany the contracts. Existing kernel/caller/binder/stage/installer/dispatcher algorithms remain imports at their frozen scope; no scientific source was read or changed. No shell-helper/code execution, syntax, AST, import, compile, tests, dummy, AWS, SSH, network, payload or protected-tree access occurred.

ROOT's six identified defects are corrected in source: a distinct selection token avoids the composer's own placeholder trap; fourteen local and eight remote roles are checked on their actual hosts, with remote installed/native closure readback; record publication checks absent targets and a stable private parent under an explicit exclusive-writer contract; observation identity must equal admission DATA; an immutable INSTALL commitment and later receipt bind RELEASE's expected registration and all staged/current digests; release repeats missing swap/burst/argv/exe/root-namespace/migration/leaf/type/subtree/descendant predicates. Transfer now retains pipefail. All nine authority files remain expected outputs, never preinstalled.

Historical admission batch and three deadline literals are placeholders in a new derivative. CONTRACT gives the complete constants-only preholder rebinding inventory for the still-unchanged historical holder/installer and four prepared files. This is not an instruction to run old dated instances.

## Retained handles and trust
Reentry inspects retained phase state before config-key reuse or a new seedStop. A known live handle can only be collected using its exact original argv and stop; then the phase STOPs. No automatic replay or downstream continuation is supported. Unknown launch, publication interruption and completed/failed phase remain stopped. This intentionally does not claim full resumability.

apply_patch is not O_EXCL. Absence/parent/readback/freeze evidence plus trusted ROOT exclusive ownership is the actual publication contract. Simultaneous malicious ROOT mutation is outside scope. Namespace/path/proc checks are non-atomic and depend on the retained no-migration/no-concurrent-writer discipline. Installed/native manifests remain ROOT-qualified actual-host inputs, not inferred facts.

## Validation and remaining quantities
STATIC-CONTROLS contains ten manual negative traces, not observed tests. The cheapest next check is ONE focused different-model FIRST of full source/diffs plus exact ROOT binding, not foundational re-review. OPEN1: no bound preholder packet or worker metadata has been formed; exact prerequisite inventory is in CONTRACT. OPEN2: added SSH/hash work fitting the unchanged holder120s window is UNMEASURED; only a separately authorized nine-only observation could settle liveness after review. No cap increase, retry or FULL13 follows. No scientific/source/rank/JC2 consequence is claimed.

PINS records28 current-pinned inputs:21 fresh WHOLE, six hash-only references, one same-agent exact-current-byte COORDINATION WHOLE reuse. No live peer report or old mathematical result is a premise. Administrative artifact_finalize workflow inspection is publication-only. READ-SCOPE identifies modes. Own source/contract/diff/report text readback and postpin/collision checks precede the final marker; custody binds all owned outputs and expected manifest. All measurements are metadata, not runtime performance.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `3855`.
- Body SHA-256:
  `ca8ddbcda4f20977d74df0591468c5fcae3f3dc2038fbbefe8a1179d9edc9c2e`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
