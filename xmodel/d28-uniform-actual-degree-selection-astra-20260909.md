# The D28 actual-degree column: exact source selection

September9,2026. Author /root/nonemptiness_certificate. Basis `0d39df3c9fd69c939a8420c54d03228b9077777d`. **PROVED HERE by manual composition at the named accepted/imported tier; new result PROVISIONAL pending review.** This is source selection, not an F10 exclusion. ZERO mathematical subprocesses of any size.

## 1. Statement and result

Fix an integer q>=0. Suppose ordinary polynomials P,Q over a characteristic-zero field have nonzero constant Jacobian and actual total degrees

`28(q+2), 28(2q+3)`.

Under the accepted GENERAL given-pair normalization of15x, the literal GGV standardization and GGHV complete-table imports, and accepted16h's empty ordinary F9 stratum, this is impossible unless q=2. The remaining numerical possibility is actual degrees112,196. Over C, every hypothetical pair at this exception has, after affine source normalization, nonzero output scalings if needed, and swapping the OUTPUTS, an ordinary rectangular standard F10(0) representative with

`(m,n)=(7,4), A0=(7,21), A0'=(1,0), A1=(13/7,3), k=1`,

starting direction(7,-2) and typeII.b. Its supports lie in `[0,7e] x [0,21e]` and attain `(7e,21e)`, for e=7,4 in this printed F10 orientation. Its actual degrees are196,112 in that order. This is a necessary alternative, not source attainment or an assertion that the exceptional stratum is nonempty.

Before using16h, the exact exhaustive alternatives are: the ordinary rectangular F9(q) source, or q=2 and this F10(0) source. In either alternative the inverse normalizer has coordinate degrees M=N=1. No same-numerical-degrees identification is assumed; these data are derived below.

The nonexistence conclusion for q!=2 transfers to every characteristic-zero field. The positive representative assertion is stated over C, or after the usual finite algebraic coefficient-witness transfer; no selected normalizer over the original field is asserted. No F10 receiver theorem, pending F2 theorem, maximum196 bound, arbitrary D28-ratio result, computed unit certificate, source ideal, or JC2 conclusion is used or proved.

## 2. Exact accepted normalization, without global minimality

Put m0=q+2 and n0=2q+3. Then n0=2m0-1, m0>=2 and gcd(m0,n0)=1. In particular neither actual degree divides the other. Work over C first. Such a Keller pair would be nonautomorphic by the classical plane-automorphism degree-divisibility theorem, an explicit import of accepted15x. Its GENERAL Section3, not its later specialization to75/125, applies.

Here is the imported interface and the manual substitution needed for this column. A polynomial SOURCE automorphism of the given pair produces Pbar with a genuine simultaneous coordinate-degree corner(r,s), 0<r<s. The leading-bracket argument for every positive weight produces a proportional rectangle for Qbar, with corner(t,u). Let U,V be inverse source coordinates of positive ordinary degrees M,N. The coordinatewise corner is the UNIQUE support point maximizing Mi+Nj, so no leading cancellation is possible even if U_M and V_N are dependent. Hence

`deg P=Mr+Ns`, `deg Q=Mt+Nu`.

Their reduced ratio is still m0:n0. Coprimality gives integral a,b with 0<a<b and

`(r,s)=m0(a,b)`, `(t,u)=n0(a,b)`,
`28=M*a+N*b >= a+b`.                                           (1)

This is not a claim that arbitrary source automorphisms preserve degree ratios. It is the consequence of two proportional actual rectangles plus the positive inverse degree functional. Initially a+b may be smaller than28 and the normalizer may be nonlinear. Neither possibility is silently removed.

GGV1401.1784v3 Definition4.3 is literally satisfied in the ordinary ring C[x,y]: both total- and horizontal-degree ratios are m0/n0, the bracket is a nonzero constant, and `v_(1,-1)(en_(1,0)Pbar)=m0(a-b)<0`. Proposition5.20 applies to EACH such pair. Its successor-direction sentence is an additional conditional conclusion, not an antecedent. Its displayed proof chooses either the identity or `x->x, y->y+lambda` with lambda constant. Therefore it preserves the ENTIRE ordinary rectangles and their attained corner coefficients, not just the stated total degrees and endpoint. The resulting standard pair has actual total degrees `m0(a+b), n0(a+b)` and normalized horizontal endpoint A0=(a,b).

No global minimum B, no smaller counterexample, no nonlinear target degree reduction, no Laurent child, and no assumption that the original pair was already standard enters this arrow. ML2025's normalization and the classical Division Lemma are consumed at the accepted15x import perimeter; their primary interiors were not freshly re-proved here.

## 3. Why the finite published table covers this entire infinite column

GGHV1708.07936v1 Theorem2.20 applies to EACH ordinary standard(m,n) pair. Items(6),(12) and Section2.4 give an actual complete admissible chain with A0=(a,b). Equation(1) gives a+b<=28, hence the published Section5 enumeration at a+b<=35 covers it. The bound is on the NORMALIZED corner, not on max(deg P,deg Q). Thus an arbitrarily large q does not require a new numerical degree census. The separate paragraph using global minimality to bound B is not consumed.

The printed Section5 header explicitly obtains the other degree orientation by swapping m,n. Definition3.3 and(3.20) parameterize each displayed family by j in N0; q denotes the actual-degree column parameter and is not preidentified with j. Both output orientations are checked by comparing the smaller reduced degree with the larger.

The WHOLE two tables contain exactly the following rows with a+b<=28. This table retains each printed row before any ratio exclusion:

| family | A0 | printed(m,n), j>=0 | first exact disposition |
|---|---|---|---|
| F1 | (4,12) | (2j+3,3j+4) | ratio test below |
| F2 | (5,20) | (j+2,2j+3) | 5 divides M*a+N*b, not28 |
| F3 | (5,20) | (4j+3,3j+2) | same divisibility |
| F4 | (5,20) | (2j+3,12j+16) | same divisibility |
| F5 | (5,20) | (7j+9,4j+5) | same divisibility |
| F6 | (5,20) | (3j+4,8j+10) | same divisibility |
| F7 | (6,15) | (j+2,4j+7) | 3 divides M*a+N*b, not28 |
| F8 | (6,15) | (2j+3,5j+7) | same divisibility |
| F9 | (7,21) | (j+2,2j+3) | possible, j=q |
| F10 | (7,21) | (5j+7,3j+4) | possible only j=0,q=2 |
| F11 | (7,21) | (j+2,3j+5) | ratio test below |
| F18 | (6,18) | (j+2,4j+7) | 3 divides M*a+N*b, not28 |
| F19 | (6,18) | (2j+3,5j+7) | same divisibility |

There is no omitted small row in the second table. The excluded-by-size rows are F12 and F22–F24 at sum32; F13 and F20–F21 at sum30; F14–F17 at sum33. These exhaust F1–F24. The paper's separate exclusion of F18–F21 is unnecessary; F18/F19 were retained and disposed of directly by(1). Neither a fixed-ratio F2 exclusion nor any new F10 map is needed.

Divisibility in(1) is orientation-invariant. Swapping polynomial outputs preserves the normalized A0 for the corresponding reduced m,n, because the two corners are proportional. Swapping the source axes merely interchanges the two positive inverse degree coefficients; it cannot turn a multiple of5 or3 into28. The standard convention already chooses a<b.

## 4. Exact ratio filter, all endpoints and both orientations

The column's smaller-to-larger ratio is

`r(q)=(q+2)/(2q+3)`, with `1/2 < r(q) <= 2/3`,

where equality on the right occurs only at q=0. The left inequality is always strict for finite q.

**F1.** Its printed first degree is smaller. For every j>=0,

`(2j+3)/(3j+4) - 2/3 = 1/(3(3j+4)) > 0`.

Hence no orientation matches the column. This handles the remaining corner(4,12), whose divisibility alone would NOT suffice: equation(1) there is M+3N=7 and has positive solutions. No unjustified affineness was used to discard it.

**F11.** Its printed first degree is smaller, and

`(j+2)/(3j+5) < 1/2`

because `2j+4<3j+5`. Thus it never matches, including j=0. The limit1/2 of the actual-degree column is not an attained column entry.

**F9.** Its printed orientation is already smaller/larger. Equality of its ratio and r(q) gives

`(q+2)(2j+3)=(j+2)(2q+3)`, hence `j=q`.

The pairs are coprime by their consecutive linear relation, so this is also equality of the actual reduced exponent pair, not a hidden common scaling.

**F10.** Its printed first degree is larger since `(5j+7)-(3j+4)=2j+3>0`. Therefore comparison MUST use the SWAPPED smaller ratio `(3j+4)/(5j+7)`. Equality with r(q) gives

`(q+2)(5j+7)=(2q+3)(3j+4)`
`q(j+1)=j+2`, equivalently `(q-1)(j+1)=1`.                    (2)

For nonnegative integral q,j the only solution is q=2,j=0. In particular q=0 and q=1 fail, and allowing a rational parameter in(2) would give spurious values. At j=0 the printed pair is(7,4), exactly the swapped column pair(4,7). The actual degrees are112,196. This explicit survivor of the FILTER is why a conclusion excluding the entire column would be unsupported here.

All arithmetic in this section was performed manually as polynomial identities/inequalities. No arithmetic script, numerical test or symbolic subprocess was run.

## 5. Attaching the actual starting profile, then using16h

In both remaining rows A0=(7,21). Equation(1) becomes

`28=7M+21N`, or `M+3N=4`.

Since M,N are positive integers, M=N=1. Thus the original normalizing automorphism is AFFINE, and the further Proposition5.20 constant translation is affine too. The actual standard degrees are still `28m0,28n0`; their full ordinary supports are the rectangles `[0,7e]x[0,21e]`, with the joint corners attained. This supplies the rectangle premise absent from bare Laurent standardness. No inverse degree can be zero, since inverse coordinates of a polynomial automorphism are nonconstant.

The table also supplies A0'=(1,0). Its direction is(7,-2), since the difference(6,21) is perpendicular to that primitive direction. The selected child is(11/7,2) in F9 or(13/7,3) in F10. Since A1 differs from A0', item(7) of Theorem2.20 rules out typeII.a at index0; item(6) leaves typeII.b. These length-one chains therefore have l0=1 and l1=lcm(7,1)=7>1 by item(8). In item(13) the greatest index with li=1 is0. Consequently these are the ACTUAL typeII.b starting triples of the standard pair, not later transformed corners or bare labels. Item(8) gives selected root multiplicity2m in F9 or3m in F10; its fractional-coordinate formula is part of the actual chain, not an assertion that its fractional child is the ordinary source.

For F9 the parameter equality j=q and original output order give exactly the antecedent of accepted16h:

ordinary rectangular standard pair, degrees28(q+2),28(2q+3), attained corners, A0=(7,21), A0'=(1,0), direction(7,-2), typeII.b, selected A1=(11/7,2), k=1, and nonzero constant Jacobian.

Accepted16h says this entire specified ordinary F9 stratum is empty, by the reviewed necessary map and the independently accepted receiver exclusion16g. We consume THAT composed emptiness statement, not a false implication from a necessary table row to source existence. The source selection just proved supplies its exact antecedent for the F9 branch, so that branch is impossible.

For q=2 the F9 branch remains impossible, but the other selected row is F10(0). Swapping the two standard outputs changes(4,7) to the printed(7,4), negates the nonzero bracket, and preserves polynomiality, rectangles and proportional endpoint conditions. Standardness also survives: write the horizontal leaders as x^(ma)f(y) and x^(na)h(y). Their expected bracket x-degree (m+n)a-1 is positive, so `m f h'-n f'h=0`, whence `h^m=constant*f^n`. Thus ord_y(h)=(n/m)ord_y(f), and the horizontal START endpoints are proportional too; the strict standardness inequality for P transfers to Q. This is a manual polynomial-domain calculation, not a premise about all arbitrary output changes. Alternatively apply the same standardization with the swapped outputs from the start. The paper's explicit swapped-orientation rule then gives exactly the exceptional source stated in Section1. Its selected multiplicity is21 for the first, degree196 polynomial and12 for its mate. No local triple-root exclusion, no newly frozen F10 map, and no conclusion about this exceptional source's existence are invoked.

## 6. Fields, trust and meaningful failure controls

For an arbitrary characteristic-zero field K, the finite coefficient field of an alleged pair embeds into C; actual degrees and the nonzero bracket survive. The complex contradiction for q!=2 therefore excludes such pairs over K. For the exception, the safe positive statement is a complex representative of that embedded pair. If a representative over an algebraic extension is desired, the finitely many coefficients of the affine normalizer, translations, scalings and inverse, along with their nonvanishing guards and finite support/chain equations, provide the usual finite-witness algebraic-closure transfer. No specific original-field normalizer or source point is claimed. Nonexistence does not require this optional positive transfer.

Manual changed-object controls:

- Dropping the inverse identity and keeping only a+b<=28 leaves F2's corner(5,20) available. This is a changed numerical premise, not a Keller example: sum25 fits but `5M+20N=28` cannot hold. The arithmetic identity, not just the bound, eliminates the wrong family.
- Dropping the ratio condition leaves positive solutions of F1's inverse equation, for example M=4,N=1. F1 is eliminated by its STRICT ratio inequality, not by an invented divisibility obstruction.
- Omitting the swapped F10 orientation would wrongly remove the actual table pair(7,4) at j=0. Equation(2) retains the necessary q=2 exception. No table realization is inferred from this arithmetic check.
- Allowing Laurent inverse coordinate degrees or a zero-degree coordinate invalidates the strict positive functional used in(1). Polynomial source automorphisms exclude these cases; no Laurent-standard normalization is substituted.
- Without the ordinary rectangle, a preserved endpoint alone need not give the assumed total leader or degree. Proposition5.20's literal constant translation and the preceding given-pair normalizer supply the missing support condition here.

**Verdict:** the proposed D28 column filter succeeds at its named imported/accepted theorem tier, with exactly the stated possible F10(0) exception. This is a new PROVISIONAL source-selection composition, not a new proof of the normalization foundations or table enumeration. It imports15x GENERAL Section3 and its different-model review, GGV Definition4.3/Proposition5.20, GGHV Theorem2.20/admissibility/published complete Section5 formulas, and the precise16h ordinary-stratum emptiness. It does not consume15x's special F2 receiver exclusion,15y's closed-H consequence, a pending F2/F10 source result, the current synthesis, or any live peer/gate/report/log/receipt. No existence, computation license, general degree ceiling, all-column closure including q=2, or JC2 assertion follows.

## 7. Read scope and custody

The exact15x proof and gate were found by the narrow named AUDIT lookup and the parent's confirmed filenames; the guessed nonexistent normalization filename was never opened. Their current full hashes were checked before WHOLE reads. The initially clipped multi-output display was repaired by complete separate reads before any WHOLE claim. The accepted15x and16h canonical entries are frozen as complete exact nonblank-body intervals in the own box, excluding only their inter-entry blank separator. Their full source hash and literal line ranges are in input-pins.json; no mutable whole AUDIT is a final charged premise.

- `xmodel/f2-fixed-degree-composition-coordinator-20260909.md`, SHA `aeeddd6a501d59529120a49377ecf42aa22314d880c4304f65cc6dd56091d318`: WHOLE; GENERAL Section3 is the normalization premise. Other conclusions are read context, not consumed.
- `xmodel/f2-fixed-degree-composition-gate-fable5-20260909.md`, SHA `2d6f386942d1f55ad5292e0ae4122549cd43e8804cc2d9664953b81db0d554d4`: WHOLE; exact accepted normalization/import scope. Its already banked M,N typo is not used.
- GGV primary retained `1401.1784v3.txt`, SHA `e3694dde3f83c2ab6ed8d957fc6b53472e6a6dd55486af1dff345eade39e37b1`: literal lines718–790 and1431–1461, including whole Definition4.3 and Proposition5.20 statement/proof. Proposition4.6 statement/partial proof was read as orientation context, not claimed wholly re-proved; the needed standardness-under-swap step is shown directly above. Nearby minimality statements are explicitly not premises. https://arxiv.org/pdf/1401.1784v3 .
- GGHV primary retained `1708.07936v1.txt`, SHA `49df06d11bbc4556a9b09771cdd60ca40a75b7542d327bbb622e09434e6a467e`: literal lines640–820 whole Theorem2.20/proof;910–979 admissibility statement/derivation;1180–1255 family definition and N0 formulas;1350–1425 whole two Section5 tables with the header's BOTH-orientation instruction. The separate F18–F21 proof is not needed or newly claimed wholly read. https://arxiv.org/pdf/1708.07936v1 .
- Frozen15x/16h entries: WHOLE at their declared accepted statement/import scope. No accepted-source checker or local receiver proof interior was re-run or newly assumed at a stronger tier.

ZERO mathematical subprocesses, numerical toy checks, source/high-power expansions, CAS, solvers, scripts for arithmetic, AWS/SSH, agents, protected access, shared/public writes, or new generic driver. Only documentary reads, hashes, metadata extraction and own publication via apply_patch/existing transaction tools. Whole own body and own-only OPEN check precede the canonical BODY-END marker. No follow-on authorized; all writers IDLE at handoff.

## OPEN(S) RAISED

None.

## COLLISIONS

status: EMPTY

- NONE — own-only extraction, no corpus scan.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `17742`.
- Body SHA-256:
  `3acf28848f1fac6c17e9d0afe63bc3fe204a2de3864acf90fb1c2cd1f30d47e1`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
