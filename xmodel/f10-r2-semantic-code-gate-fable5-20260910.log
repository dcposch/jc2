cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
The gate is published and sealed at 13:42:58 UTC, nine minutes before the deadline, with the standalone marker as the last line.

**Verdict: CONFIRMED on all six items A–F, no blocking correction found.** Report at `xmodel/f10-r2-semantic-code-gate-fable5-20260910.md` (2,927 words, sections under 1,500 words each) plus a generated custody file in the owned box. All 12 input pins matched before reading, and re-hashed unchanged after the body.

Key findings, each traced to source lines in the report:

- **A.** Control 6 empties only `bands[6].B_var` after a non-vacuous theta-squared unit guard that check.py:355-357 forces on every accepted packet. Control 10 starts from a fresh reload, picks the first strictly-largest absolute coordinate in Psi/K1/K0 order, deletes a term only when all seven coordinates vanish, and the inverse edit must reproduce the original bytes whole. The mutator's validator counts the same 103 polynomials as the checker, so a validated fixture cannot trip the checker's parser.
- **B.** Platform, DMI, exact 19-entry argv and 64-hex pins precede hashlib; json and Fraction are imported only after source pins and the collision check. The ROOT SHA is recorded, never read, so it is a binding only.
- **C.** Science map and CAPRUN pin unchanged in the diff. New outside-tmpfs requirement covers roles, registration, manifest and native files. The token expansion touches exactly one slot after a unique flag and the whole list must equal the freshly built vector, with no shell or environment expansion path.
- **D.** I traced both fixtures through the unchanged check.py by hand. Control 6 reaches line 329 and raises at 216 because `oper(7, {}, {})` leaves the bare T^4 target. Control 10 fails at 376 for Psi or 383 for K rows, then check_arithmetic.py:208. Both match the dispatcher's literal frames after whitespace stripping. Freeze to root 0444 under 0555 precedes both checkers and is rechecked before Popen and after group quiet.
- **E.** Twelve labels, 475 wall and 386 hard CPU, joint envelope and 15 MiB joint fixture cap confirmed. Dummy and durable copy are byte-identical in the diff. The ESRCH race is retained unchanged.
- **F.** Only `TWO_SEMANTIC_REJECTIONS_CONFIRMED` with `science_outcome NONE` can be emitted, and exit 0 requires it.

Two non-blocking notes. The mutator's 9-second CPU profile is unmeasured against three full validations of the 1.65 MB baseline; an overrun is a typed STOP, not an accept. The outcome records carry the frozen input hash but not its path string.

What remains is ROOT's: an enabled registration with the single token in the mutate vector, installation of the pinned mutator and baseline, and one observed twelve-child batch. Nothing was executed, and no observation is claimed.
DIVERT path=xmodel/f10-r2-semantic-code-gate-fable5-20260910.md status=CLEAN body_bytes=20387 body_sha256=469fd1c701a2af3149ff7401c7592a4fb313ad7ee64b922105d86c27700eb09b
