# Blind whole-46 significant-news scan — order-two owner

Snapshot UTC: 2026-08-26 06:15Z  
Basis commit: `418e413593120d19e15e6546eb50c985f4b1f038`  
Status: **BLIND SUBMISSION; NOT SYNTHESIZED; NO SIBLING IDEATION PACKET READ.**

## 0. Frozen news packet

This scan consumed the canonical 46-row `APPROACHES.md`, current
`PROGRESS.md`, and the following post-checkpoint evidence before looking at
any sibling submission:

- exact order four is eliminated for every `U`;
- the nonzero-`k10` Padé theorem/review (`2c917b9f...` / `73c30502...`)
  makes the saturated order-two first-normal reduced support exactly the
  square/load component and the zero-load discriminant component;
- the square matched third tail forces `L|M^3`, hence `M=0` for squarefree
  `L=z^2+s`, with a separate `s=0` degeneration;
- the discriminant transverse Jacobian has rank three, not five.  The
  ordinary matched chart is exactly empty, but a two-dimensional
  half-weight kernel remains.  The frozen formula/certificate source is
  `ddbc7580...`; exact-Q and dual-prime source/analytic comparisons are live;
- D1 V10 is reviewed positive and the V11 constant-load extension is
  producer-positive on dual AWS, with weighted triple-root scope only;
- TD6 has a reviewed 24-dimensional linear kernel; the repaired quadratic
  Kuranishi computation, not more single axes, is the live gate;
- order-one has a reviewed component rejector: its descended `r7` must be a
  pure-power/two-branch-value map with degree `d` satisfying
  `d*n=U-1`.  The first fixed-load curve probes timed out before producing a
  component.

The Padé review is complete.  Review debt remains on the new discriminant
rank/half-weight formula, square third-tail calculation, D1 V11, and any
endpoint reported by the live AWS clients.  All heavy work is AWS-only.

## 1. Compact disposition vector over all 46 avenues

```text
 1 raise       2 raise       3 unchanged   4 raise       5 unchanged
 6 reopen      7 unchanged   8 unchanged   9 unchanged  10 lower
11 lower      12 unchanged  13 unchanged  14 unchanged  15 unchanged
16 unchanged  17 lower      18 lower      19 unchanged  20 unchanged
21 unchanged  22 unchanged  23 unchanged  24 unchanged  25 reopen
26 unchanged  27 unchanged  28 unchanged  29 unchanged  30 unchanged
31 unchanged  32 unchanged  33 unchanged  34 unchanged  35 unchanged
36 raise      37 unchanged  38 reopen     39 unchanged  40 unchanged
41 lower      42 unchanged  43 reopen     44 lower      45 unchanged
46 unchanged
```

Reasons for every change:

- **1 raise:** D1 has moved from an isolated cone witness to reviewed smooth
  discriminant transport and dual-AWS weighted triple-root/load controls.
  Coverage, rather than construction of an obstruction, is now the gap.
- **2 raise:** exact order four is gone; exact order two is reduced to two
  explicit first-normal components; TD6 has reached its first genuine
  quadratic Kuranishi gate.  Both are sharper finite sheet-tree clients.
- **4 raise:** the order-two discriminant supplies an explicit singular
  formal germ with rank-three linearization and a two-variable quadratic
  obstruction.  This is simultaneously a proof gate and the cheapest honest
  counterexample-side lift attempt.
- **6 reopen (narrowly):** the order-one coefficient normalization is forced
  rational and carries a two-branch pure-power function `r7`.  A one-place
  recognition test is now source-typed on an actual coefficient component,
  unlike the earlier wrong-object use of Abhyankar--Moh.  Reclose immediately
  if the complete `r7` divisor has more than one finite place.
- **10 lower:** the exact JC2-sector probe already returned no named receiver;
  the new component recursion offers strictly cheaper discriminators.
- **11 lower:** the route remains refuted; no new receiver appeared.
- **17 lower:** high-dimensional cubic stabilization does not preserve the
  newly decisive plane multiple-root/terminal structures.
- **18 lower:** the graded plane case is closed and orbit limits still lose
  nonproperness.
- **25 reopen:** the reviewed `r7` theorem converts the order-one problem to
  an exact two-branch-value/pure-power cover on each dominated component.
  This is a stronger, coupled use of passports than the permissive 169-cover
  residue-A census.
- **36 raise:** a nonzero half-weight discriminant Kuranishi point would be a
  source-typed formal counterexample seed, not a random-map search.  It has a
  finite exact next-grade falsifier.
- **38 reopen (as a technique, not a standalone theorem):** D1's reviewed
  strict weight cones and the order-two half-weight normal cone now give named
  saturated Rees clients.  Certified fan traversal can cover exceptional
  faces without the old prevariety/saturation mistake.
- **41 lower:** the rank-three example is another explicit warning that naive
  linear scaling misses singular normal directions.
- **43 reopen (narrowly):** Padé forces the loaded first-normal quartic to be
  a square, and the next tail forces divisibility by its quadratic root.
  Persistence or failure of this composite factor through one more exact
  grade is now a concrete Ritt-type test rather than a global slogan.
- **44 lower:** the repaired membership theorem still has no contact with the
  exact component gates; prime covering degree is not the live order-two
  invariant.

All other avenues are unchanged because the news neither supplies their
missing global receiver/coverage theorem nor invalidates a banked negative
control.

## 2. Bottleneck reranking and lane decisions

Proof bottlenecks, in order:

1. classify the discriminant half-weight Kuranishi zero locus and recurse on
   its triple-root/square-intersection exceptional divisors;
2. close the square/load component by its pure-load/degenerate next jet and
   immediately impose terminal `[6,2]` plus both Taylor receivers;
3. apply the confirmed `r7` divisor/critical-value test to a redesigned
   order-one component construction;
4. finish D1 weighted triple-root coverage and TD6's repaired quadratic map;
5. preserve the global landing/coverage and cofinal-ceiling work, which none
   of the local advances replaces.

The strongest proof attack is componentwise valuation induction on order two:
use the exact radical decomposition, compute the normal cone/Kuranishi map on
each component, and split only its factored rank/zero divisors.  The strongest
counterexample attack is to take any nonzero half-weight triple-root point as
a formal seed and attempt one further exact divided jet before investing in
algebraization.

Current-lane recommendations:

- order-two square/discriminant recursion: **continue**, but never return to
  the timed-out monolithic saturation;
- order-one fixed-load curve: **redesign** around the `r7` two-branch graph
  before Gröbner saturation;
- D1 weighted triple root: **continue** through coverage/factor divisors;
- TD6: **continue** only the repaired K2/Spencer object; stop serial axes;
- exact order four: **stop** production; retain only controls;
- generic invariant-quotient reconnaissance: **stop** until a component-
  coverage theorem supplies a specific exceptional-load client.

## 3. Three idea cards

### Card A — shared proof-carrying normal-cone compiler (`NEW`)

**Connection:** avenues 2 + 4 + 38, and the live order-two/TD6 gates.

Build one source-pinned engine that takes an exact family, its first ideal,
and a component parametrization; computes the transverse Jacobian and kernel;
derives the Newton weights forced by the first nonzero Kuranishi terms; emits
all weighted Rees charts with zero-section and saturation firewalls; and
returns exact initial maps plus factor divisors.  The order-two rank-three
calculation and TD6's 24-kernel are two test fixtures for the same interface.

Dependencies: exact source rows, a reviewed component parametrization, and a
proof that tangent coordinates are invertible on the declared open.

Cheapest discriminator: reproduce the frozen order-two `rho^6` source ideal
from the analytic quadratic map and factor its zero locus.  If equality
fails, stop and repair the coordinate compiler.  If the initial ideal is
unit, exclude that valuation chart.  If nonunit, emit only factor-localized
successors.  Stop after two noninformative monolithic GB attempts; never
project scaled variables.  Expected information gain: very high, because it
separates genuine Puiseux strata from artifacts and transfers directly to
TD6.

### Card B — two-branch `r7` receiver before order-one genus (`NEW`)

**Connection:** avenues 6 + 25 + 43 with the order-one terminal theorem.

Instead of saturating six fixed-load tail equations blindly, graph `r7` and
its derivative, construct a small plane projection that retains all boundary
places, and impose that the normalized `r7` map has exactly two branch values.
Equivalently, after a Möbius normalization test whether its critical divisor
is supported over `{0,infinity}` and whether the map is a pure power of degree
dividing `U-1`.  Only surviving components deserve genus or Taylor work.

Dependencies: actual source-component coverage, complete projective graph,
and the degree firewall `d*n=U-1`.

Cheapest discriminator: on two small modular fixed-load tuples, eliminate the
graph-critical ideal without saturating the whole coefficient ideal.  More
than two critical values is a safe rejector; two values produces a rational
parametrization target; chart loss or unknown component coverage is no
verdict.  Stop a tuple at 20 minutes/16 GiB.  Expected information gain: high
because the previous jobs spent 40 minutes only reaching `std(I)`.

### Card C — multiple-root collision recursion (`NEW CONNECTION`)

**Connection:** avenue 1's D1 triple-root frontier with avenues 2/4's
order-two discriminant half-weight kernel.

The order-two half-weight equations show analytically that the zero-load
branch away from the triple-root divisor is the first cheap target, while any
survivor is forced toward a collision of the double root with a root of `Q`.
D1 independently concentrates on weighted triple-root charts.  Do not
transfer formulas between `(9,12)` and `(8,12)`; transfer the local-algebra
interface: root-centered coordinates, discriminant factors, weighted normal
cones, and factor-by-factor coverage certificates.

Dependencies: exact factorization of the order-two Kuranishi support and the
reviewed D1 V10/V11 scope.

Cheapest discriminator: compare the two clients' weight semigroups and
coordinate Jacobians, then run the order-two triple-root `e=0` next grade
with terminal `[6,2]` data retained.  A unit kills the only nonsquare
half-weight escape; a nonunit becomes the strongest formal-disproof seed;
different weights mean software reuse only.  Stop before any cross-degree
identity is assumed.  Expected information gain: maximal on the current
order-two branch.

## 4. Allocation delta

Until the half-weight endpoint lands: `45%` order-two component recursion,
`20%` D1 weighted coverage, `25%` TD6 K2, `5%` redesigned order one, `5%`
global/formal backstop.  If the generic discriminant half-weight gate is
empty and only the triple-root divisor survives, shift ten points from
generic order two to a dual-host triple-root next grade plus terminal/Taylor
receiver.  If it has a positive-dimensional off-triple-root component,
immediately allocate one producer and one adversary to one-more-grade lifting;
do not bury it under genus heuristics.
