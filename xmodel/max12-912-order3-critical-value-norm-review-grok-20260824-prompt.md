# Hostile review — maximum-12 unordered critical-value norm

Act as a hostile different-model mathematical reviewer in
`/Users/dc/code/math/jc2`. Read in full:

- `xmodel/max12-912-order3-critical-value-norm-20260824.md`;
- every file in `cases/max12_912_order3_critical_value_norm_20260824/`;
- the exact order-three parent compiler; and
- the reviewed full-absorption and parity producer/review pairs pinned by the
  case.

Charged hashes:

```text
report    7339478798e00894c7b975c60aca821ec7ff2da383ffe5165a93cfa361bb6cb6
manifest  73b55970e600fe65664a089a03642291fbbbd5e6d9480515517011f1f2745e0a
freeze    258fea426d71f524de180b84d6633f0e163f6d837f665f094ab484c22b5283d6
```

Independently reconstruct and attack:

1. the legality and exact covariance of normalizing the nonzero constant
   load `nu` to one; state any needed constant-field/scaling hypothesis;
2. `p=a7/3`, `B=54*(z^2-s)`, and `s=-(a7+10*rho)/9` on the actual seven-row
   fibre with `r8=rho`;
3. the source-derived `f,g` and their even/odd quadratic remainders, including
   all charged hashes;
4. the norm coefficients and the identity
   `disc_T Norm(G-TF)=4*s*(G0*F1-G1*F0)^2`;
5. direct equality, up to the declared leading/content factor, with
   `Res_z(B,g^3-T*f^4)` on several exact symbolic or rational controls;
6. disjointness and exhaustiveness of the four leaves after localizing at
   `s*Norm(F)`, plus the separate double-root and `B`-meets-`f` strata; and
7. the parity positive control and strict non-exclusion scope.

Attack hidden use of the fibre equations, loss of a resultant leading factor,
the distinction between `Norm(f)` and `Norm(f^4)`, one-root versus two-root
absorption, equal values equal to `0`, `1`, or infinity, and whether the
actual-Keller coprimality firewall is being applied too early.  Run the replay
and independently compare the pair norm with a direct resultant after exact
rational specializations satisfying and not satisfying the fibre rows.

Write exactly
`xmodel/max12-912-order3-critical-value-norm-review-grok-20260824.md`.
Do not edit producer, case, canonical, coordination, or other review files.
End with exactly one verdict: `CONFIRMED`, `GAP`, or `REFUTED`, and identify
the smallest failing identity or missing hypothesis if applicable.
