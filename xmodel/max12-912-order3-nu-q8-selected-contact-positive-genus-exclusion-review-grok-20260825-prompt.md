Act as an independent hostile algebraic-geometry referee.  The producing
agents are GPT-family; you are the different-model reviewer.  Read in full:

- `xmodel/max12-912-order3-nu-q8-selected-contact-positive-genus-exclusion-provisional-20260825.md`;
- `xmodel/max12-912-order3-nu-q8-positive-genus-specialization-bypass-20260825.md`;
- `xmodel/max12-912-order3-nu-q8-positive-genus-specialization-bypass-review-codex-20260825.md`;
- every parent report/review named in the provisional composition's table,
  especially the common-source arithmetic bridge, mod-127 source-component
  theorem, corrected rational-contact theorem, infinity/primitivity review,
  and the positive-genus producer.  Treat the positive-genus point-count
  theorem as an explicit conditional input unless its separate Grok review
  already exists when you read.

Audit rather than restate the exact selected-contact trajectory exclusion.

1. Check that every characteristic-zero and mod-127 contact is a point of the
   same full eight-equation arithmetic source, not merely the same plane
   projection, and that the reviewed completion really is the whole-source
   domain `R[[w]]`.
2. Attack the local closure argument.  If `Y` is the selected generic
   component and `Z` its closure, determine whether its local ideal must be
   zero in the 127-torsion-free `R[[w]]`; whether the special branch is
   uniquely `k[[w]]`; and whether the reviewed H-supported source component
   through the existential winning contact is necessarily this same branch.
   Test vertical-tail, component-merger, embedded-component, nonreduced, and
   plane-incidence countermodels.
3. Verify exact source dominance onto geometrically integral `H` and the use
   of separability/Riemann--Hurwitz to get positive genus upstairs.  State
   whether separability is needed for the later Luroth obstruction.
4. Audit the proper/ruled-residue step.  From a nonconstant map
   `P1_K -> Ytilde`, extend the valuation of the special component to
   `K(P1)`.  Prove or refute that the residue field is rational after finite
   base change and that its finite subfield `k(C)` must be rational by
   Luroth.  Check all finiteness, properization, normalization, residue-field,
   and characteristic-p hypotheses.  Compare with semistable reduction but
   do not rely on arithmetic-genus equality without its hypotheses.
5. Check the primitive all-eight/eight-singletons propagation and the exact
   infinity theorem.  Make sure the conclusion covers only the selected
   `k=mu=0,nu!=0` corrected-Q8 contact leaf and does not silently become all
   `(9,12)`, max-12, or JC2.
6. Identify the smallest repair for every gap.  Give one final verdict:
   `CONFIRMED`, `CONFIRMED_WITH_REPAIRS`, or `BLOCKED`.

Do not use Bash, CAS, network access, or write/edit any file; disclose that
limitation.  Return a self-contained review on stdout, at most 12,000 words.
End with exactly one verdict token on its own line.
