# Blind whole-portfolio ideation — Grok 4.6 — round `20260829T1517Z`

Lane: Grok 4.6 (xAI), equal-standing independent researcher.
Produced blind against the sealed packet only. No other
`ideation-20260829T1517Z-*` file, peer prompt, log, run record, or
post-cutoff report was opened. No web, AWS action, canonical edit,
commit, push, or heavy CAS. The separately owned `jc2-lean` tree was
not entered, enumerated, listed, searched, read, status-checked,
built, or modified. Bare `G2` is not used.

## 0. Custody

Model identity: Grok 4.6, xAI.
Clean basis (verified `git rev-parse HEAD`):
`40c1ab3448209e3d87173feb947a733f6fe54f7f`.

Required packet hashes (recomputed this session, byte-exact):

```text
648acf8ba6d8cd2b498d7a0825fc0e239044a4270e0a928009811550806d3449
  xmodel/ideation-20260829T1517Z-state.md
72284172888e3c8ed38d9a619ac195c5060bd0b7dd5abf1b728634e0aa4cfdad
  xmodel/roundview-20260829T1517Z-40c1ab34.md
```

Authoritative roots named by the packet (recomputed, all match):

```text
3f35e3a8a5d204a8c24d96821077117e6efe263a4e2765da7a527d0c13686877  AUDIT.md
d7e833c890c6fdc11a646c5feb5125665b9c298e7fb93a6a706e1afdc63102bc  COORDINATION.md
d45f1db6117f67e21065c29e0ff65ff7c1f8304f3ecc37fa796c5cd4b93b4e65  PROGRESS.md
805152bf2e68edad1a5d55b9fff7092e7a19f40171406050c9d9b50e994de608
  xmodel/ideation-20260829T1224Z-synthesis.md
f781077840a8405b4ef100b718bc6eeef17a9d07948aaadda2eb2d51aecf27ab  APPROACHES.md
```

Additional files read as history/checksum, not as a second queue
(recomputed):

```text
319212fbf03feb1dfe7ce1f4255a021b5e7931815af5f46a1f890027ddf9797a  notes.md
e47fd16cfcc91bc7bfdac4ba1b5f46152db8235e6609dca6e29a5966549c38f5  FALLACY-v2.md
29270ff6192fcee2eecb4ba68578010b2b3f0c5519feb71dc67baa7f68bb784b  ladder/REDUCTION.md
b2b85ef6e4c2204ae3318089204c08ae077bf091f0165da0531f9d55e35b4d26  ops/FLEET.md
```

Read: the complete state packet and the complete ROUNDVIEW/v1 slice
(preamble, newest overlay, 46-row master table, older-overlay index);
current promotion/correction blocks at the top of `AUDIT.md`; the
complete `2026-08-29` entry of `PROGRESS.md`; `COORDINATION.md` roles,
gates, and the 14:54Z/15:07Z allocation-and-systems refresh; newest
event blocks in `notes.md` through `2026-08-29 15:07Z`; `G2-PSC`/`G2-BD`
and T9–T10 in `ladder/REDUCTION.md`; `FALLACY-v2.md`; the prior-round
synthesis named by the packet; and only those producer/review/integration
reports whose hashes the packet or the live canonicals already name.
ROUNDVIEW/v1 is context, never a queue. Historical “active” tags are
timestamped history.

Firewall (applied below). A cv flag is not a physical place and is not a
cover series. `REPRESENTATIVE` is not `FULL_ACTUAL_EXIT`. The latter
aliases `FULL_ACTUAL_FIRST_SEPARATION` and is a complete-carrier lower
floor, never attainment. Do not identify one labelled route-kill with
all U2, all one-P0, or all td12. Do not identify a finite formal jet with
a polynomial map, a `PairRef`, or a Keller pair. Do not infer occurrence
from `ALIVE`. Merge-free is not Statement 8.5. Target `nu_G` is not
incoming `nu_U`. Keep B and S routes separate; the two S evaluations
share one parent family and are not independent generic coefficients.
If no safe replacement exists, the report returns typed `OPEN`. No new
exit price is asserted, so `charge_basis` is absent.

This lane does not recycle its `20260829T1224Z` cards
`S17-BEYOND-FLOOR`, `INNER-U2-TWO-POLE-FULL-ACTUAL`, or
`DIRTY-V2A-EXIT-TYPE`. The first was stopped as a homogeneous-window
descendant; the second lacked a named remaining two-pole inner frame
and a localization lemma; the third would double-count a priced P0/MFE
step. It does not recycle `TWIN-ORDER` as a route-kill. The finite-s
integration already audited the td12 `m=3` U1 family as **not** a
finite-pole consumer; that identification is `SCOPE-CONFLICT`.

## 1. Disposition vector (avenues 1–46)

Legend: `U` unchanged, `R` raise, `L` lower, `O` reopen. Reasons only
for changes. Homonymy does not move a row. Lifecycle tags in reasons
are `promoted`, `provisional`, `formal`, `book-relative`, or
`conjectural`. News that sharpens a bottleneck without changing
endgame-P or numbered rank is recorded in §2, not as a raise.

| # | d | reason if changed |
|--:|:-:|---|
| 1 | U | |
| 2 | U | Remains the principal proof avenue. The B-bridge core is now `promoted` at named-actual-occurrence scope and does not kill; occurrence, `PairRef`, cap, and transgression stay missing. That is a bottleneck move inside the avenue, not a rank change. Homogeneous-window descendants stay stopped. |
| 3 | U | Homonymy firewall: the promoted B landing operator `L_j` is a source-jet ODE on a named occurrence, not a Newton-face strip residue `R_{k,d2}`. Do not raise Żołądek A.7/Ore. Card 3 records a functor, not a row raise. |
| 4 | U | Still no `PairRef` and no polynomial completion. The K00 grade-four survivor is a finite jet, not a germ-to-polynomial algebraization client. Do not pre-arm DEPTH-STAB on it. |
| 5 | U | The G4 shear `u |-> u-mu` is an automorphism of coefficient space, not a triangular shear of a Keller pair. Homonymy with JvdK is `SCOPE-CONFLICT`. |
| 6 | U | |
| 7 | U | Finite-s is a flag-carrier theorem on one fibre. It does not construct `A(F)`. |
| 8 | U | |
| 9 | U | |
| 10 | U | |
| 11 | U | |
| 12 | U | |
| 13 | U | |
| 14 | U | |
| 15 | U | |
| 16 | U | HENS-CT timeout stands; `L_j` is not a holonomic-index client. |
| 17 | U | |
| 18 | U | The rank-zero plane is not a `G_m` symmetry of a map. Shaska closure stands. |
| 19 | U | Method transfer in Card 2 is not a char-p/Witt revival and does not mix K00 jets with Artin–Schreier lifts. |
| 20 | U | |
| 21 | U | |
| 22 | U | |
| 23 | U | |
| 24 | U | |
| 25 | U | Finite-s does not license a monodromy census. Stay down. |
| 26 | U | td12 is composite; killing one U2 label does not license a primitive-group bound. Stay down. |
| 27 | U | Finite-s is banked with no `s>=3` source-typed P0/P2 client. Do not reopen splice diagrams. |
| 28 | U | |
| 29 | U | |
| 30 | U | |
| 31 | U | |
| 32 | U | `OFF-DIM2` still has no complete-map client. A jet is not that client. |
| 33 | U | Untwisted residues remain `COSTUME`. Hermite content is not a new twisted class until a transgression exists. |
| 34 | U | |
| 35 | U | |
| 36 | U | Remains the protected bounded characteristic-zero seed. Grade three is now the reduced plane; grade four is a `provisional` proper nonreduced survivor. That sharpens the falsification bottleneck to literal grade five; it does not raise the numbered promise, and a finite jet is not a counterexample. |
| 37 | U | |
| 38 | U | Direct identity `(CF)` dominates tropicalization of the same generators. |
| 39 | U | No 10-line class. |
| 40 | U | |
| 41 | U | |
| 42 | U | |
| 43 | U | |
| 44 | U | Closed as proof input. |
| 45 | U | Inverse PDE remains tautological; `L_j` is not Kovacic. |
| 46 | U | Formalization is not a route; `jc2-lean` stays outside the campaign boundary. |

No numbered avenue is raised, lowered, or reopened. Unanimous negative
space this round: no numerical `kbar`/`nu` cap; no `j=42` descendant
without source values; no revival of rank-three through rank-five K00
charts; no Galois conjugacy of the two S evaluations; no PairRef from a
formal envelope; no finite-s reprice of B25/S17; no `TWIN-ORDER` closure
law; no exit-price declaration.

## 2. Reranked bottlenecks

Proof-side, by expected information per desk-day, then by endgame
necessity. Endgame-P of a checkable JC2 proof remains modest:
family-kills are necessary book work, not a cofinal ceiling
(`REDUCTION.md` Critical 7 / T9–T10).

1. **Named actual B25 or S17 occurrence from a hypothetical minimal
   counterexample.** The B-bridge core is `promoted` and functorial from
   the pair **plus** a named occurrence and finite Puiseux prefix, not
   from the pair alone. Its landing row reproduces the reviewed T1
   condition `8B=9A` and supplies no route kill. Absence of an occurrence
   theorem is exactly `OCCURRENCE-B25` (and the sibling analogue). T9(c)
   still has no completeness certificate for any `b>=2` entry; T10 is a
   desired statement. This is now strictly upstream of all coefficient
   arithmetic.
2. **Serialized route-separated `PairRef` / completion / source values
   `P_k` for `k>=1`.** Still missing. The formal cascade is a
   `FORMAL_JET_ENVELOPE`: B24 is transparent for even `i>=16` and S16
   for even `i>=12`. Homogeneous-window descendants stay stopped.
3. **Original-to-terminal tower transgression, or a genuinely nonabsorbed
   distant inhomogeneous constraint.** The endpoint Hermite class
   (`3i-2` slots on B, `4i-3` on S) is an original-pair invariant. The
   terminal T1 quotient is a different object. They are compatible and
   not identified. Sol’s `j=17` vacuity / `j=42` frontier is
   `provisional`; `j=42` lies outside B24 and is unusable without source
   values. Do not launch it. Card 3 attacks the transgression *shape*
   without inventing jets.
4. **Remaining U2 / one-P0 coverage after the labelled-ray kill.** The
   corrected `NESTED-U2-P0-U2-RAY/v1` is dead at actual td12 landing,
   `REPRESENTATIVE` scope. Direct adjacent U2-to-U2 edges are finite at
   promoted actual-edge scope. The repaired one-P0 family is still an
   infinite semilinear union (`t=5 mod 6`, `K=1 mod 6`). Absolute-index
   / product-of-`nu` coverage remains `OPEN`. Finite-s does not close it:
   there is still no source-typed `s>=3` P0/P2 client, and the td12
   `m=3` U1 family is audited as not that client.
5. **LL-1 remaining seven rows as floors only.** Promoted two-pole plus
   finite-s carriers; inventory `13 -> 7`; every price
   `LOWER_FLOOR_ONLY`. `ALIVE` is a conservative superset, not
   occurrence. Bank equality profiles until a decoration/occurrence
   client exists.
6. **Independently sourced type menu + cofinal `td` ceiling.** Unchanged
   endgame wall.
7. **`RPMC(C)`**, still type-relative. **`G2-PSC`** owed only by a hybrid
   architecture; pure Sigray bypasses it; zero active spending.
   **`G2-BD`** dormant until residue A.

Counterexample / falsification side:

1. **Literal grade five on the grade-four survivor** — strongest bounded
   characteristic-zero seed now on the board. Must retain both the
   radical-level generators `(RA,RB)` and the scheme-level generators
   `Q_r(u-mu)`, keep `k10_0 != 0` via `z k - 1`, and introduce every
   variable at its first occurrence (`d*_3` and `k10_0` return; `d*_4`
   and `k10_1` disappear on the plane, as a G4 support census, not as a
   G5 theorem). This is not a polynomial map.
2. **Different-model replay of the G4 primary** — review debt, not a
   mathematical blocker to *preparing* the two-variant G5 packet. Do not
   promote G4, and do not launch G5 before this checkpoint is sealed.
3. **S17 parent polynomial completion**, then B25, if a `PairRef`
   appears. Still not a Keller pair. The S bridge has mathematical
   `PASS_WITH_MATERIAL_REPAIRS` and custody `FAIL_PRODUCER_BODY_SEAL`;
   `FLOOR-COORD` is provisionally a WLOG gauge via `x |-> x+c`, not a
   fixed-coordinate theorem and not a value-transport map.
4. **Remaining U2 infinite** after coverage work. Infinite reduced leak
   of Critical 5; not a map.
5. **Char-p / Witt (Avenue 19)** stays reserve. First live bounded
   partial-degree frontier remains maximum twelve; no all-Witt-to-
   polynomial inference.

What this news does *not* rerank: landing totality, `RPMC(C)`, type/
cofinal control, `G2-PSC`, `G2-BD`, ACS construction debt, LL-1 as an
occurrence engine. The portfolio moves to occurrence/transgression on
Avenue 2 and to literal G5 on Avenue 36, not onto a new global bridge.

## 3. New mechanism, new connection, composition pass

### 3.1 New mechanism — `K00-POLARIZATION-SHEAR` (NEW)

On the reviewed rank-zero plane

```text
Pi: (d0_1,...,d5_1) = (2s, t/8, s, t, s, 2t),
```

Opus’s G4 primary (producer evidence, not promoted) gives the
generatorwise identity

```text
phi(Lambda_(4,r)) = Q_r(u - mu(s,t)),
mu = (s^2, s t/8, 16 t^2, 0, 0, 0),
```

with `mu` the unique solution of the 72 linear equations matching
linear-in-`u` parts against the polarization of the six nonzero
grade-two quadrics, and with
`sigma: (s,t,u) |-> (s,t, u-mu)` a triangular polynomial automorphism
of `A^8`. Residual gauge: `mu` is well-defined only modulo the tangent
plane `L = T_Pi`, and `(CF)` is invariant under `mu |-> mu + L` and
breaks off `L`.

Mechanism, not a G4 restatement: treat this as a **polarization-shear
functor on even Lambda grades**. At each later even grade `2m`, form
the linear-in-the-current-even-jet parts, polarize the same `Q_r`
against them, and ask whether a unique polynomial `mu_(2m)` exists
modulo the accumulated gauge such that

```text
phi(Lambda_(2m,r)) = Q_r(w - mu_(2m))
```

holds generatorwise on `Pi` (or on the G4 locus). Consequences if the
functor continues:

- even grades after G2 are translated copies of the same grade-two
  variety and cannot kill by themselves;
- only odd grades can impose new constraints on previously dropped
  blocks (`d*_3` and `k10_0` at G5);
- G6 is then a scheduling skip, not a Groebner job.

If the functor stops at G4, G6 is a second even-grade obstruction and
G5 is not the unique next kill window. The G4 identity is `KNOWN`
producer evidence. The functor, the uniqueness-modulo-`L` claim at
later grades, and the G6 discriminator are **NEW**. Close synonyms
checked: G4 §1/§8.4 successor text (G5 support census only; no G6
shear); V26 compiler affine-in-newest at grade seven (a different
block); JvdK shears (wrong object); DEPTH-STAB (see §3.2). Card 1.

### 3.2 New connection — associated-scheme thickening protocol
consumes K00-G5 (NEW)

Avenue 19’s associated-scheme work (char-3 AS D7, not a K00 object)
already isolated a load-bearing distinction: field-valued points of a
gate see only the reduced support, while Fitting ranks, accepted-digit
recursions, and any successor that consumes the original nonreduced
primary see the thickening. G4 reports `I4` strictly inside
`sqrt(I4)`, with `RA^3, RB^3, RA*RB in I4` and `RA, RA^2, RB, RB^2`
not in `I4`, nilpotency index exactly three, multiplicity 8 on both
`I4` and its radical.

Connection, not an identification of maps: the G5 scheme-input variant
is the K00 instance of that protocol. Run Fitting/Jacobian ranks of
the G5 rows, affine in the returning `(d*_3, k10_0)` block if the
compiler identity holds, on `T/I4` and on `T/sqrt(I4)` separately. If
the ranks differ, the thickening is load-bearing at this gate and the
radical input is not a substitute. If they agree, radical input covers
field-valued points of *this* gate, and the scheme input is retained
only as custody for a later Fitting successor. Do not transport a
char-p collision, a Witt digit, or an Artin–Schreier conductor.

Second, weaker connection (recorded, not a card): the even-grade shear
is a DEPTH-STAB *pattern* on a bounded max12 order-2 jet. Arm Avenue 4
only if every atlas grade survives **and** a polynomial completion
exists. A nonempty finite jet is not that client. `SCOPE-CONFLICT` to
treat G4’s `A^12 x G_m` as an algebraized germ.

### 3.3 Theorem-interface composition

| Theorem (lifecycle) | Object | Consumes / discharged by | Result |
|---|---|---|---|
| Complete G3 incidence on `V(B)` (`promoted`) | reduced plane `Pi x A^6_u` | `I5,I4,I3(A) subset B`; `sqrt(R1)=J0`; rank-two fourth-power obstruction | **HIT** as G4/G5 branch spec. **NO HIT** with full-`P6`, source, arc, Keller pair, JC2. Stop rank 3–5 charts. |
| G4 `(CF)` + nonreduced `I4` (`provisional`) | `Q_r(u-mu)` and `sqrt(I4)=(RA,RB,zk-1)` | plane substitution `phi`; localizer `zk-1` | **HIT** with Card 1 (G6 shear) and Card 2 (G5 two-input). **NO HIT** with a branch kill, a polynomial map, or promotion. |
| B-bridge C1–C3 + repaired C4 (`promoted`, named-occurrence scope) | landing ODE and `L_j` | type `(2,3)` B state; greedy subtraction to `s*` | **HIT** as T1 solvability, not a kill. **NO HIT** with `OCCURRENCE-B25`, `CAP-B25`, `PairRef`, `j=42`. **HIT** with Card 3 as the missing original-chart counterpart. |
| Finite-s full-actual carrier (`promoted`) | `a(P)=max_j min(v_j,O(P,P_j))` | one fibre/component; physical flags; finite pole-path union | **NO HIT** with B25/S17 source polynomials, `m=3,4` books, nested-U2 representative kill, td11 skeleton. Bank. |
| Endpoint Hermite class (`provisional`) | `K_(s*) ≡ kappa_F mod H_i` | original-pair cross-sum; `H_i=gcd(P_0,P_0')` | **NO HIT** with terminal T1. **HIT** with Card 3. Not a route kill. |
| S-bridge math (`provisional`); custody fail | source shear, Newton floor, `x |-> x+c` | named S17 occurrence | **NO HIT** with value transport. `FLOOR-COORD` is WLOG gauge after different-model review and reseal. |
| Formal cascade (`promoted` with repair) | binomial response through `s<=r` | type `(2,3)`, `r=3i/2` | **NO HIT** with B24/S16 kill. Stop homogeneous descendants. |
| Labelled one-P0 U2 td12 kill (`promoted`, `REPRESENTATIVE`) | actual landing of one label | source-mass, cap-free P0, selected-exit/MFE | **NO HIT** with other U2/one-P0, all td12, finite-s reprice. |
| `TWIN-ORDER` non-torsion (`OPEN`) | `(4+3i)/5` | Gaussian valuation | **NO HIT** with a holonomy/closure law. Do not recycle. |

Honest global reading: the new theorems remain Avenue-2 internal on the
proof side and Avenue-36 internal on the falsification side. The only
cross-avenue composition that currently types is the thickening
protocol in §3.2. A speculative “minimal CE must be B25 or S17” lemma
would bypass occurrence; it is **not typed** (T9(c)/T10). Record as
`OPEN`, do not launch.

## 4. Strongest attacks

**Proof.** Stop every homogeneous-window and `j=17` residue descendant.
Do not launch `j=42` against absent `P_k`. Do not spend the proof slot
on LL-1 equality profiles, on Q+E5 engine edits, on `G2-PSC` hybrid
transport, on ACS-CEIL, on `TWIN-ORDER`, or on treating finite-s as a
B25/S17 consumer. Run Card 3 (Hermite transgression shape, no invented
jets) as the cheapest object-level attack on the promoted B-bridge’s
named missing functor. In parallel, keep occurrence as typed `OPEN`:
the honest remaining coverage work is the infinite one-P0 semilinear
union and T9(c), not a uniform constructor. Background: different-model
S-bridge reconstruction after reseal, and different-model review of
the B25 `j=17` vacuity. Sibling packets stay route-separated.

**Counterexample / falsification.** The honest surviving bounded seed
is the G4 rank-zero-plane locus, provisionally irreducible
`A^12 x G_m` at the reduced level and nonreduced of index three at the
scheme level, with `k10_0 != 0`. Strongest attack: Card 1 first (G6
polarization-shear identity, desk), then Card 2 (literal G5, both
inputs). If G5 is the unit ideal on either input, this seed dies as a
char-0 jet and the bounded CE hunt moves off K00. If it remains
proper, report dimension/nilpotency and stop; a surviving jet is not a
polynomial map and is not JC2. Explicitly reject: relaunching rank 3–5
charts; treating the G4 witness points as a Keller pair; SAT search;
Witt lifting of this jet; using G4’s `codim 2` as an exit price.

## 5. Mathematical-software acceleration / decisive experiment

**Object.** Polarization-shear identity at Lambda grade six on `Pi`,
and, if that identity is untyped as a remainder, the same test in the
declared G4 quotient.

**Placement.** Desk first. Python 3 stdlib `json` + `fractions.Fraction`
against the frozen atlas

```text
d7ec6d18d58265421ff315fa00e38cd32992ac12f7d8166cf6c436e23bf06501
  cases/max12_812_order2_u2_62_k00_grade7_fitting_atlas_v26_20260827/aws_box02_r2_held_compile/output/ATLAS_EXACT_POLYNOMIALS.json
```

and the G4 closed form `(CF)`. No Singular, no cap token, no AWS for
the support/polarization step. Resource bound: 60 CPU seconds and 1 GiB
RSS. If a normal form in `T/I4` or `T/sqrt(I4)` is required, do **not**
run it locally: preregister an AWS packet with the same atlas hash,
the plane map `phi`, `mu`, both G4 input ideals, positive/negative
controls below, wall 600 s, RSS 16 GiB, stop at first non-identity or
unit, ordinary/optimized replay.

**Controls.**

- Positive: replay `phi(Lambda_(4,r)) = Q_r(u-mu)` generatorwise from
  `exact_terms` (must remain identically zero; six nonzero rows plus
  row 6 `= 0`).
- Negative: mutate `16 t^2` to `15 t^2` in `mu`; at least one G4 row
  must fail (G4 already records six mismatches).
- Non-`PairRef` / non-map control: label every output
  `FORMAL_JET_ENVELOPE`, never `PAIRREF`, never `POLYNOMIAL_COMPLETION`.
- Ring map: declare `phi: S -> T` exactly as G4 §1 (generator order
  `d0_1..d5_1, d*_2, d*_3, k10_0`; coefficient field `Q`; image checks
  on the plane). Matching names prove nothing.

**If the linear/polarization step is inconclusive** (G6 rows involve
variables whose first occurrence is not yet in the G4 ring, or the
quotient remainder is needed): freeze the AWS packet above; do not
occupy fleet quota speculatively. Current AWS headroom is irrelevant
until that packet exists. This is the Card 1 discriminator, not a G5
Groebner job and not a full-`P6` chart.

## 6. Campaign-systems card — `UPGRADE`

Axis: process lifecycle / unreaped-child polling, the live systems
incident of this checkpoint. Mathematical scheduling penalty: none, if
the packet is Singular-wrapper-plus-fixtures only.

The coordinator’s next-maintenance target is a repo-owned
wait-authoritative helper **and** migration of nine bare `kill -0`
loops. That is larger than the incident. The four hung desk Singular
invocations printed expected stdout and then stalled on open
stdin / missing `quit;` / `kill -0` watchdog. D43 `run_capped` already
uses `timeout`+`prlimit` but still polls with `kill -0` in
`poll_stage`; do not mutate D43 AWS scripts in this packet.

Smaller bounded packet: `ops/run_capped_wait.py` (Singular-capable
desk wrapper), no call-site migration.

Contract:

- stdin is `DEVNULL` unless `--stdin-file` is explicit;
- generated Singular scripts end with `quit;`;
- `Popen.wait(timeout=wall)` is authoritative (no `kill -0`);
- on timeout: `killpg` TERM, bounded grace, `killpg` KILL, one reap;
- record PID, PGID, start identity, wall/CPU/RSS;
- CPU/RSS via `resource.setrlimit` on macOS desk and a documented
  Linux `prlimit` path; do not require Linux for the fixture;
- forbid broad name matching, unscoped `pgrep`/`pkill`, unresolved
  globs.

Smallest measurable test (ordinary and `-O`, byte-identical logs):

1. `Singular --version` with closed stdin exits 0 in `< 2 s`
   (the Fable incident).
2. Script `1+1;` without `quit;` under the wrapper with `wall=2`
   is TERM/KILL’d, reaped once, nonzero; no orphan PGID.
3. Script `1+1; quit;` with closed stdin exits 0.
4. A grep fixture on the helper source fails if `kill -0` appears.

Positive control: fixture 3. Negative control: fixture 2, and a
planted `while kill -0` watchdog that the grep fixture must catch.
Do not migrate `ops/lane.sh` or the seven upper-endpoint supervisors
until this helper is reviewed. Do not churn `FALLACY-v2.md`,
`ROUNDVIEW/v1`, or charge-basis validation. Next systems checkpoint
remains `2026-08-31T09:30Z`. `ROUNDVIEW/v1` was the previous trial;
this incident is new evidence against `NO_CHANGE`.

## 7. Idea cards (three)

### Card 1 — `K00-POLARIZATION-SHEAR`
[`NEW` functor; G4 identity `KNOWN` as producer evidence]

**Fingerprint.**
`K00-Pi|even-Lambda-grade|polarize-Q2-against-linear-even-jet|unique-mu-mod-L|G6-identity-or-OPEN`

**Target.** Whether even Lambda grades on `Pi` after G4 are translated
copies of `V(Q2)` or a new obstruction. Not a G5 Groebner, not
full-`P6`, not a polynomial map.

**Dependencies.** Promoted: complete G3 incidence, the plane `Pi` as
branch spec. Provisional: G4 `(CF)`, uniqueness of `mu`, residual
gauge `L`, nonreduced `I4`. Excluded: treating `(CF)` as promoted;
G5 unit/proper; source reachability; order-two exclusion.

**Cheapest discriminator.** The desk experiment of §5: extract
`Lambda_(6,r)` from frozen `exact_terms`, apply the declared `phi`
extended by first-occurrence generators, polarize `Q_r` against
linear parts, test generatorwise identity or return `OPEN` on typing.
No CAS for the support/polarization step.

**Outcomes.**

- Generatorwise translated-`Q` identity at G6, unique `mu_6` modulo
  `L`: even-grade functor continues; skip G6 Groebner; G5/G7 are the
  only remaining atlas kill windows on this plane. Still not a map.
- Identically zero on the G4 locus, stronger than translated-`Q`:
  even grades after G4 are automatic on this branch. Same scheduling
  consequence; record which ideal (`I4` vs `sqrt(I4)`) was used.
- New generators, not in `(Q_r(w-mu_6))`: functor stops; G6 is a
  second even-grade obstruction; do not skip it; still no branch kill
  until that ideal is shown unit or proper.
- Untyped ring map, vanished leaders, or zero polynomial mishandled:
  `OPEN`. Do not fill by cap, by G5 analogy, or by the grade-seven
  newest-block affine identity.

**Stop.** One exact G6 identity/non-identity/`OPEN`. No G7 unless G6
is automatic and G5 is already running as Card 2.

**Information.** Binary on whether G5 is the unique next kill window
for this seed, without a 13-dimensional Groebner.

### Card 2 — `K00-G5-THICKENING-FITTING`
[`KNOWN` target `K00-G5-RANK0-PLANE`; `NEW` method-transfer]

**Fingerprint.**
`K00-G5-RANK0-PLANE|two-input-I4-vs-sqrtI4|Fitting-rank-of-returning-d3-k10_0|associated-scheme-thickening-protocol|k10_0-localizer`

**Target.** Literal grade five over the G4 survivor, both inputs.
Decide unit/proper, dimension, and whether the returning `d*_3` and
`k10_0` are forced. Not later grades, not a source point, not a
counterexample.

**Dependencies.** Promoted: G3 plane as branch spec. Provisional: G4
`(CF)`, `I4` vs `sqrt(I4)`, G5 support census (`d*_4` and `k10_1`
disappear; `d*_3` and `k10_0` survive). Method: associated-scheme
“retain the nonreduced primary for Fitting” (Avenue 19 protocol, not
object). Excluded: rank 3–5 charts; radical-only as a silent
substitute; launching before this checkpoint is sealed.

**Cheapest discriminator.** After Card 1’s support step, test whether
the seven G5 rows, after `phi5` and dropping the vanished `d*_4`,
`k10_1` blocks, are affine in `(v,k)=(d*_3, k10_0)`. If yes, Fitting
ranks of that affine map on `T/I4` and on `T/sqrt(I4)` are a linear
algebra job over those two rings (desk if the specialized matrices
are small; AWS if Groebner of `I4` is re-derived). If not affine,
preregister the two-variant G5 Groebner as AWS-only. Declare the ring
map, generator order, coefficient field `Q`, and image checks.

**Outcomes.**

- Unit on either input: this plane-branch dies at G5 on that input.
  Report which input. Do not transport the kill to full-`P6`, to a
  polynomial family, or to JC2.
- Proper, ranks equal on `I4` and `sqrt(I4)`: field-valued points of
  this gate see only the reduced locus; retain scheme input as custody
  for a later Fitting successor; record dimension/nilpotency.
- Proper, ranks differ: thickening is load-bearing; radical input is
  not a substitute; continue on the scheme.
- `k10_0` forced to zero against the localizer: the open condition
  dies; that is a branch kill of the normalized chart, still not JC2.
- Untyped saturation, unnamed remainder ring, or vanished leaders:
  `OPEN`. Fail closed.

**Stop.** Unit/proper plus the Fitting-rank comparison, or `OPEN` on
typing. No G6 Groebner unless Card 1 says the even-grade functor
stopped.

**Information.** The protected CE seed either dies at its first
returning-block grade or becomes a documented proper scheme of known
dimension. Either outcome is a campaign decision; neither is a
polynomial counterexample.

### Card 3 — `TD12-HERMITE-TRANSGRESSION-SHAPE`
[`NEW` shape lemma; Hermite class and T1 `KNOWN` as distinct objects]

**Fingerprint.**
`td12-B-or-S-separated|original-Hermite-class|terminal-T1-landing|transgression-degree-support-shape|no-invented-P_k`

**Target.** The missing functor from the original-pair Hermite class
to the terminal T1 solution, as a degree/support/overdetermination
statement, without emitting source values. B and S stay separate;
run B first (promoted landing operator). Not a `j=42` expansion, not
a PairRef emitter, not an occurrence theorem.

**Dependencies.** Promoted: B-bridge C1–C3, repaired `L_j`, T1
solvability `8B=9A` with unique on-weight
`T = gamma q p^(-i)`. Provisional: original Hermite congruence
`K_(s*) ≡ kappa_F mod H_i`, `deg H_i = 3i-2` on B and `4i-3` on S;
`j=17` vacuity. Excluded: identifying `K_(s*)` with Fable’s reduced
deviation top `T`; identifying Hermite slots with cv flags; using
`P_k` for `k>=1`; sibling objects on the B card.

**Cheapest discriminator.** Desk identities only. Write the
transgression as a polynomial map from original jets
`(P_0, G_0, finite original tail)` to the reduced trunk `(p,q)` that
must (i) fix the named occurrence’s leading `P_0 = lambda_f p^i` and
(ii) send `K_(s*)` to the T1 solution `T`. Compare the number of
independent Hermite conditions (`3i-2` on B) with the dimension of
the T1 solution space (one on-weight unknown `gamma`, with `8B=9A`
already imposed). If the ring map cannot even be declared, return
`OPEN`. No CAS.

**Outcomes.**

- Shape overdetermined after T1 is imposed, with an exact independence
  statement for the Hermite slots that uses only `P_0` and the
  promoted landing operator: a candidate `CAP-B25` *shape*, still not
  a cap, because an unbounded original tail can absorb conditions.
  Record the missing support bound as `OPEN`; do not fill it by
  `td=12`.
- Shape underdetermined: transgression, if it exists, is not forced
  by Hermite+T1; stop this card; occurrence and `PairRef` remain the
  bottlenecks.
- Dictionary untyped (original cross-sum vs reduced `T` live in
  different charts; no declared map): `OPEN`. Do not analogize via
  matching root names `A,B`.
- Independent of transgression, a genuine nonabsorbed constraint is
  found that uses only promoted operators and `P_0`: that is the
  distant inhomogeneous object the packet asked for; still no
  occurrence theorem.

**Stop.** One shape verdict `overdetermined` / `underdetermined` /
`OPEN`. No `j=42` expansion. No S objects in the B run.

**Information.** Turns “transgression missing” into a typed
over/under count, or fails closed. Does not manufacture occurrence.

## 8. Live-lane `continue / redesign / stop`

- **K00 rank-three through rank-five charts / localizers / selected
  G3 rank-one or rank-two charts:** `stop`. The promoted incidence is
  the reduced plane. Preserve historical `RESOURCE_CAP_NO_VERDICT`.
- **K00 G4 primary:** `continue` as background different-model
  replay. Not promoted. Not a blocker to *preparing* G5. Different-
  model reconstruction is review debt.
- **K00-G5-RANK0-PLANE:** `continue` after this checkpoint is sealed,
  both scheme-input and radical-input variants, Card 1 first if it
  is truly desk-scale. Do not launch in this blind report. Heavy or
  uncertain CAS is AWS-only.
- **TD12 B global source bridge:** `continue` at occurrence /
  transgression (Card 3). `stop` homogeneous-window descendants.
  `stop` `TD12-B25-RESROW/v1` at `j=17` pending independent review.
  Do not launch `j=42`. Do not emit values without a `PairRef`.
- **TD12 S global source bridge:** `redesign` custody (reseal the
  producer body) then `continue` different-model mathematical
  review. Treat `FLOOR-COORD` as a provisional WLOG gauge, not a
  research fork and not a frozen-coordinate theorem. Route
  separation stands. No value transport without serialized maps.
- **`TWIN-ORDER`:** `stop`. Exact non-torsion, no closure law. Do not
  recycle the same symmetry.
- **Finite-s carrier:** `stop` new descendants. Bank until a
  source-typed `s>=3` P0/P2 *physical-flag* client is named. The
  td12 `m=3` U1 family is not that client.
- **LL-1 / equality profiles:** `stop` further inventory work.
  Floors only; no occurrence.
- **Labelled `NESTED-U2-P0-U2-RAY/v1`:** `stop`. Dead at
  `REPRESENTATIVE` actual td12 landing. Do not reprice it with
  finite-s.
- **Remaining one-P0 / other U2 labels:** `continue` as coverage /
  landing work (absolute-index bound, T9(c)), not as INNER-U2 or
  DIRTY-V2A recycles.
- **Process lifecycle:** `continue` the smaller `UPGRADE` in §6.
  Do not migrate nine `kill -0` sites before the wrapper is
  reviewed.
- **`ROUNDVIEW/v1`:** `stop` further machinery this round; it just
  shipped as the previous trial. Non-authority firewall stands.

## 9. History-checksum labels

| Item | Label |
|---|---|
| `K00-POLARIZATION-SHEAR` | `NEW` functor; G4 `(CF)` `KNOWN` producer evidence |
| `K00-G5-THICKENING-FITTING` | `KNOWN` successor target; associated-scheme Fitting transfer `NEW` |
| `TD12-HERMITE-TRANSGRESSION-SHAPE` | `NEW` shape lemma; Hermite vs T1 identification `KNOWN` false |
| finite-s × td12 U1 / B25 / S17 as consumer | `SCOPE-CONFLICT` (finite-s integration §2) |
| `S17-BEYOND-FLOOR` | `DUPLICATE` / stopped; not reused |
| `INNER-U2-TWO-POLE-FULL-ACTUAL` | `DUPLICATE` / unarmed; not reused |
| `DIRTY-V2A-EXIT-TYPE` | `DUPLICATE` of a priced P0/MFE step; not reused |
| `TWIN-ORDER` as route-kill | `KNOWN` `OPEN`; recycle forbidden |
| `OFF-DIM2` on a jet | `SCOPE-CONFLICT` |
| `LIPSHITZ-CODOMAIN` | `KNOWN` refuted; not reused |
| `MAX12-U2CELL` | `DUPLICATE`; not reused |
| JvdK shear = G4 `u-mu` | `SCOPE-CONFLICT` |
| finite jet = polynomial map / `PairRef` | `KNOWN` false |
| `FLOOR-COORD` as fixed-coordinate theorem | `KNOWN` false (provisional WLOG gauge only) |
| `j=42` descendant | `KNOWN` blocked (no source values) |
| rank 3–5 K00 charts | `KNOWN` stop |
| wait-authoritative Singular wrapper | `NEW` smaller systems packet; 9-site migration `KNOWN` as later work |
| artifact-kind enum from `1224Z` | `KNOWN` systems; not the incident of this round |

No proof or counterexample to JC2 follows from anything in this
report. No new exit price is asserted.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `34124`.
- Body SHA-256: `f809f40262f55702f43d680faf019d9f371db5ad124672c756294b5475eb3545`.
- Frozen basis: `40c1ab3448209e3d87173feb947a733f6fe54f7f`.
