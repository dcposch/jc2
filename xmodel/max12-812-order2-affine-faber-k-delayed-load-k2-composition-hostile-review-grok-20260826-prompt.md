# Hostile review: delayed-load affine-Faber `K` face to discriminant K2

Work in `/Users/dc/code/math/jc2`.  Adversarially review the immutable
producer theorem

```text
6178089bb11efebb304b2eb24dca6eb654bde060396fbf4f4456f515cae28989
  xmodel/max12-812-order2-affine-faber-k-delayed-load-k2-composition-exclusion-20260826.md
```

Write exactly one report to

```text
xmodel/max12-812-order2-affine-faber-k-delayed-load-k2-composition-hostile-review-grok-20260826.md
```

Edit no other file and do not touch `jc2-lean`.  Use no verdict token as
evidence.  Rehash every charged theorem/result you actually consume.  Exact-Q
identities may be evidence; finite-field lanes are software controls only.

The main question is whether the producer really proves that every formal or
Puiseux successor of the exceptional delayed-load `K` face on `D(D)` is empty.
Attack, rather than assume, all of the following:

1. Re-derive the UFD split for
   `Q0=z^4+p*z^2+r` and
   `N1=y*z^3+(2*D*x+(p/2)*y)*z`.  On `r=0`, check the identification with the
   discriminant point `(a,b,e,m)=(0,0,p,p*x)` and the units actually licensed.
2. Check the exact V3 lower-unitriangular source/analytic row bridge and the
   repaired K2 statements
   `(I_K2:kappa^infinity)=1` and
   `((I_K2+(kappa)):e^infinity)=1` in `R_m`.  Verify that the repeated-root
   point is in the `e`-unit chart, not the routed `b=e=0` square intersection.
3. Scrutinize the producer's normalized-blowup lemma.  Starting from an
   arbitrary ramified source arc, decide whether tangent absorption into
   `(a,d,m)`, the three complementary linear pivots, and the two-dimensional
   half-weight kernel genuinely exhaust every relative valuation.  Search for
   a root-splitting or fractional-order correction that is neither killed by
   an earlier linear initial form nor represented in the K2 weighted chart.
   Check the coordinate faces and the asserted intrinsic `-m^3` prevention of
   an all-zero escape.
4. Check source scaling and load timing.  Determine independently whether the
   delayed loads can contribute at the relevant K2 initial grade; if
   `kappa=0` is asserted, verify it rather than inheriting it.  Also decide
   whether the first K2 colon already handles a possible tied nonzero load.
5. Treat the dual sigma-through-15 moving-double-root unit computation only as
   corroboration.  It does not itself cover root splitting.  Decide whether
   the abstract K2 composition supplies exactly that missing exhaustiveness.

If the argument is incomplete, give the smallest explicit missing hypothesis,
valuation face, or countermodel and the strongest narrower theorem that
survives.  Keep the scope firewall: this is only the exceptional delayed-load
`K` face on `D(D)`, not `A`, `D=0`, other source slopes, order two, maximum
twelve, or JC2.

End with exactly one standalone token: `CONFIRMED`, `REPAIR`, or `REFUTED`.
