# F10 r1 engineering batch: preflight STOP, no mathematical execution

Verdict: **STOP / NONDECISION.** The five changed-authority refusal controls and
valid-authority control ran. The descendant dummy demonstrated RSS enforcement,
identity-validated TERM/KILL and complete cleanup, but a newly written caller
predicate incorrectly rejected legitimate SENT event records. The batch stopped
before builder/checker dispatch. No mathematical child, exact ideal artifact,
runtime representation verification, point or ideal decision exists.

The original caller and all remote evidence are frozen, not patched or retried.
`CALLER-FIX-PROPOSAL.md` describes the exact proposed correction for independent
review. The exclusively owned worker has been STOPPED, not terminated; its EBS
and local evidence copies remain recoverable.

## 1. Authority, scope and documentary reads

Actual local start: 2026-09-09 12:40:27 UTC. Root's absolute task stop remains
12:58 UTC, mathematical deadline 12:53 UTC and at most 360 aggregate wall seconds
from the first mathematical child, whichever earlier. The mathematical clock
never started (`first_math_epoch: null`). No caps or deadline were increased.

The WHOLE terminal static gate
`xmodel/f10-r1-complete-builder-gate-fable5-20260909.md`, SHA
481d2a2edb12855cbdfa11f1cc1d0d15d7933b039896612827a683c3d70a09d7,
was hash-verified BEFORE its WHOLE read. Its A–E verdicts are STATIC only.
The accepted 16q/16r and frozen builder report/plan are reused at their exact
prior read scopes, without a new mathematical theorem or optimization premise.
Root's REGISTRATION.md was read WHOLE and preserved as NOT DISPATCHABLE; root
explicitly authorized the new owned per-operation registrations separately.
The frontier JSON hash f9d1fddc… was verified before its WHOLE read. Its actual
112/196 declaration is NOT_CLOSED_BY_THIS_GATE, not proof of openness.

`ops/FLEET.md`, `ops/fleet/fleet.sh` and its README were read WHOLE; CAPRUN
identity, process-start and telemetry code was statically inspected at the
relevant exact intervals. Frozen CAPRUN SHA4435279d… and its accepted test file
SHA96775b17… were verified; the old tests were NOT rerun. No legacy timeout
wrapper, CAS, allocation, solver, prime or source-power expansion was used.

Lookup-scope incident: the guessed `ops/fleet.sh` was absent. An overbroad
`rg --files /home/ubuntu` filtered to `/fleet.sh` returned three fleet filenames.
No protected body was returned/read, but the lookup scope was too broad. It was
stopped immediately, disclosed to root, and all later accesses used exact
`ops/fleet/` or owned paths. No claim of exhaustive protected-tree nonenumeration
is made for that command. No shared/protected file was modified.

## 2. Exact worker and caller

The only controlled instance was i-08d2a40f272ee9fa2, c7i.4xlarge, private IP
172.30.0.72, Owner=f10-r1-exact-artifact-validation-20260909, EBS
vol-0574e0fa5aed1f5e3. Exact AWS describe confirmed running before use. Remote
DMI gave Amazon EC2 and that instance ID; hostname was ip-172-30-0-72.
The remote interpreter SHA was a92f0f95e883390c7256b2e441484aac06b1002dbe1d924141a77c8d82f96223.
The exclusive directory `/home/ubuntu/f10-r1-exact-artifact-validation-20260909`
was absent before creation. Existing fleet key/SSH/rsync transport was used.

Frozen builder/checker/helper/CAPRUN pins were checked before transport and
again on the worker. Every enabled operation authority includes exact physical
host, job, cwd, source/helper/CAPRUN/interpreter/admissibility/frontier hashes,
full child argv and full parent CAPRUN argv, caps and exclusive output paths.
Checker-type probes also pin their input. Original sources were not edited.

New `dispatch_batch.py` is a single-job metadata caller, not a supervisor. It
sets inherited RLIMIT_FSIZE=16MiB in the pre-exec hook BEFORE the exact CAPRUN
exec; it creates no additional PGID. CAPRUN alone creates and records its
child's session/PGID. One child was dispatched at a time. Mathematical stages
were configured for builder60wall/50CPU/2GiB sampled RSS and checker30wall/25CPU/
2GiB, with strict admission against the original deadline and aggregate budget.
Those stages never ran. No output-size stress test is claimed.

## 3. Observed controls and exact stop

Between 12:44:21.056319 and 12:44:22.230182 UTC, the actual disabled, wrong-host,
changed-full-argv, changed-cap and missing-input-pin registrations each produced
an ordinary nonzero child exit, with no postauthorize sentinel. The valid
registration produced POSTAUTHORIZE and ordinary exit0. Their exact authorities,
stdout/stderr, caller logs, dispatch records and CAPRUN telemetry are preserved.
This is actual changed-object refusal evidence, not a mock Boolean predicate.

The descendant dummy ran under wall5/CPU3/RSS33554432 bytes. Its leader PID/PGID
2628 exited0 while child2630 ignored TERM and retained a 64MiB allocation.
Both reported PID namespace pid:[4026531836] and PGID2628. The caller itself
verified the CAPRUN process's PID namespace before waiting. CAPRUN observed
77955072 bytes aggregate RSS, exited125/RESOURCE_CAP (the intentional dummy
outcome), and recorded 0.337713239 seconds wall time. Its leader exit remained0;
the resource classification came from the live descendant, not leader failure.

CAPRUN's before-term and before-kill entries each recorded MATCH for PID/PGID
2628 and boot/start identity
`boot=e4cc45da-5390-408f-8da8-f6c385846edc;start_ticks=39218`.
The accompanying before-term-send and before-kill-send entries correctly
recorded SENT with signals15 and9. `cleanup_complete`, `leader_reaped`,
`term_sent` and `kill_sent` were all true, with no live group before reap.

My new caller then required ALL entries of identity_checks to have result MATCH.
That was an overstrict event-type predicate: the two legitimate SENT entries
caused `RuntimeError: TERM/KILL identity not matched`. It is a caller defect,
not a frozen CAPRUN/source/builder defect. `batch.STOP.json` records the failure
at 12:44:22.648882 UTC, `first_math_epoch: null`, and all seven completed
nonmathematical dispatch records. No `preflight.PASS.json`, build authority,
`exact.json`, mathematical receipt, or precision result was created.

The required response to a caller mismatch was STOP, so there was no inline fix,
new cap, retry or mathematical dispatch. The proposed four-event typed-sequence
predicate is isolated in a non-dispatchable prose file; it is not accepted or
executed. All seven ordinary checker modes and the genuine >2^53 attack remain
unrun. The precision helper source is preserved but was never imported/run.

## 4. Terminal recovery, pins and limitations

After all dispatches ended, the exact remote directory was copied locally under
`remote/`. A separate read-only process audit checked all seven recorded PGIDs
(2599,2604,2609,2614,2619,2624,2628); every live list was empty and exact.json was
absent. This audit is preserved and hashed before the stop operation. The exact
owned instance was then stopped; a subsequent exact-instance describe confirmed
STOPPED, the same Owner and retained EBS. No instance was terminated and no
volume/data was deleted; the backup stop timer may remain idempotent.

The local metadata audit verifies every authority/source/input and telemetry
digest against preserved bytes. Custody distinguishes source inputs, new caller
files and pulled runtime evidence. Runtime telemetry timestamps/numerical
measurements are preserved as native raw JSON, not reinterpreted as exact
mathematical coefficients. No ideal rows/terms/degrees/runtime/RSS are reported,
because no ideal was built. The dummy RSS/wall observations above are only
engineering measurements.

All authored report/proposal/helper source receives a WHOLE read; own OPEN and
collision checks precede the body marker and existing transactional publication.
Original frozen files, root draft, shared ledgers and mathematical packet are
unchanged. This producer-run result is not a promotion gate. New dispatch would
require separate root review/authority and fresh exclusive outputs.

OPEN(S) RAISED: None.

COLLISIONS: EMPTY at owned-artifact scope. The stopped caller/predicate failure
and overbroad filename lookup are explicitly retained; no corpus cleanup.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `8297`.
- Body SHA-256:
  `6f14f0671b6437dd50c8b8cf43ae699a157771c2f056cf23437ca7fc3ff79801`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
