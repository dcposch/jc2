# Necessary-row independent checker: exact static delta

STATIC / UNEXECUTED / pending combined DELTA FIRST. No runtime or source outcome.

Actual first action 2026-09-11 05:53:59 UTC. Own report, manifest and box absent; original reserve06:04/HARD06:07 retained without reset.

## Selected change

The adopted ROOT interface selects a NEW necessary-row service, not the old complete-internal-circuit service. This owner copies only the frozen independent checker. Exact changes are deletion of predecessor lines390–404 and three schema/job/success literals; every other predecessor byte is preserved. The unchanged independent arithmetic is not copied or edited. Alternative Laurent-q inverse/pole coefficients are NOT_COMPUTED; no dummy records or added wire keys are used.

## Read scope and publication

Current COORDINATION is same-agent exact-pin prior-WHOLE reuse from the immediately preceding inverse-redesign task. Adopted interface, its documentary manifest and designated FIRST are freshly WHOLE. The predecessor checker, eligible for same-byte prior-WHOLE reuse, is additionally freshly read WHOLE this turn, including parser, removed branch, retained leading/scale/graph comparisons and terminal success/error sites. Exact pins and read intervals will accompany custody. No other scientific input or live peer source is read.

## Exact source delta and retained predicates

New source: box/f10-necessary-rows-checker-astra-20260911/check.py, SHA256 `1d2d5b5408bfbf13b96f09453b1f14dfe884d261e702843bc31e5a043ffa53d1`. The generated check.diff has exactly three hunks: original42–43 now require `f10-r3-necessary-rows/v1` and `f10-source-cone-r3-necessary-rows-v1`; original390–404 are deleted; original504 now returns `CHECKED_NECESSARY_ROWS_25_NO_SOURCE_OUTCOME`. Original529 lines become514. The original docstring, comments outside that branch, imports and all remaining bytes stay literal. Text copying and exact-diff inspection are not interpreter/syntax validation.

The deleted block includes only its Laurent-q comment, symbols/chart expression, two lists, coefficient-expansion loop/envelope check and two list-length comparisons. No replacement lists, zeros, descriptors, wire fields or inverse evaluator are introduced. Leading a/b and guarded-product checks immediately following it remain. This is not the unselected streaming design, nor unchanged full-circuit acceptance: alternative Laurent-q inverse and pole coefficients are NOT_COMPUTED.

Every other source predicate stays byte-identical: strict six-key top/eight-key graph parsing,30 polynomials/two scalars, exact source/contract/place binding and terminal reauthentication; literal leading/septic/place arithmetic;125 denominators and15 named scalar inverses; completed matrix identities; all forcing, fixed installation and separate Euler construction/gauges;108/45/63 coefficient positions and22 low-band readbacks;150 diagnostics with111 canonical checks and39 alternative records; both Laurent-S auxiliary checks;13 SCALE fields and six comparison groups including A_inverse and every21/24 low difference; critical graph/guard identities; all25 individually named rows, weights, ell,g,U and mixed T. Independent arithmetic implementations remain independent; no producer-science import is added.

The unchanged row loop still constructs `direct-source graph slot ` plus each label, and main still prints `CHECK FAILED: ` plus that mismatch to stderr, then exits2. Thus the adopted T predicate text remains `CHECK FAILED: direct-source graph slot T` followed by newline. This is a static unchanged-path statement, not an observed rejection. All other direct-source/error/nondecision strings and byte/resource caps remain literal.

The checker still imports metadata-only `authorize` and independent `check_arithmetic` at their existing sites. This owner does not author/copy authority or arithmetic. Future installation must bind unchanged check_arithmetic SHA256 `9d2aac637dbdeaa192cc3fe531e83694f9853b6573556ad5d49362f754b1163e` and the separately authored new-tag authority in the new files/source vector. That arithmetic pin is ROOT's installation requirement, not a fresh body read or new execution here. No live producer/authority source is inspected, and no old registration/receipt is valid merely because retained mathematics is unchanged.

## Theorem scope, OPEN and handoff

The accepted adopted-interface/FIRST scopes are imported, not re-reviewed:23z attaches the same25 necessary rows to the kernel of the complete canonical LOCALIZED source algebra with guard mapping to its unit;19z supplies the source-bound genuine-place rank-to-field exclusion implication. The removed numerical alternative inverse/pole branch is not a premise. Canonical inverse-polynomiality remains a SOURCE hypothesis, not an observation by this instrument. No source point, unsaturated-ideal equality, rank, source-zero, all-r or JC2 result is supplied.

Readiness is STATIC CANDIDATE ONLY, pending the combined different-model source/runtime DELTA FIRST and new ROOT bindings. OPEN quantity: whether the independently registered necessary-row producer/checker complete with identical30+2 positions and reject the freshly rebound T mutation at its exact predicate. Cheapest next discriminator: combined static diff/authority/runtime review; subsequent computation requires separate ROOT authorization. Runtime, support beyond retained caps, speedup and cap sufficiency are UNMEASURED. No scientific subprocess, import, AST, syntax, compile, test, CAS, dummy, coefficient/fixture body, network/AWS/SSH, worker, delegation, shared edit or protected access occurred.

Own-only collision scope: target report/manifest/box were absent at first action; only this owned checker copy, minimal diff, PINS and report transaction/custody are authored. No canonical OPEN identifier or corpus-wide collision search is claimed. One attempted documentary patch had a nonmatching context and made no change; the correct bounded append followed. This did not affect source bytes or clocks. Final own WHOLE, input/owned postpins and marker-last sealing remain documentary completion duties.

Closeout05:56:55 UTC: own check.py, check.diff, PINS and report received untruncated WHOLE readback; all five current input pins and three owned pins matched. Own final report/manifest destinations remain absent, COLLISIONS NONE. The exact diff has no additional hunk; no source writer remains active. This closeout and unique final body marker end report authorship. Only transaction verification, sealed readback and terminal custody follow, before original06:04 reserve/06:07 hard stop.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6669`.
- Body SHA-256:
  `3acc337bf5e0cb573ed759c0ec75b9595ed7361df946ce948396961ebb238d18`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
