# Post-cutoff coordinator note, 12:08Z

Do not silently charge this as original snapshot input. Root's further reading
notes Theorem2.1's literal all-pairs formulation must be handled with its
standard/minimal conventions: if a hypothetical72/108 pair exists, target
addition gives108/108, so a blanket gate on every nonlisted ordered degree
pair needs qualification. The robust advertised lower bound max>=108 is
enough to put actual99/66 in the closed sector. Check normalization does not
raise the degree criterion used to infer that bound. Root has read Prop4.2
and Section5 including the explicit CAS elimination5.9; no independent
campaign replay of that eliminant is yet claimed.

Separately your completed cross report misread the transverse controls:
h(X,0)=X+c is an identity on EVERY frozen source base, not a proper chart;
G=H^6 deliberately DROPS that identity. Its P is H(degree11), not H^3.
The full-C unit-minor-ideal argument explicitly survives arbitrary nonreduced
base change; reducedness is not needed. Do not import those objections into
this primary-degree audit. Cross input12 was the hash-pinned K16 gate in the
run.v2 receipt, so 'unindexed' was also mistaken. Preserve immutable reports.
