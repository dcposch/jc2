# Hostile text-only review — ordinary D1 V8-cone composition

| Field | Value |
|---|---|
| Claim under review | Exact relative closure of genuine strict coefficient-infinity directions inside the frozen ordinary isotrivial source and registered V8 weight cone, by set-theoretic/projective composition of the reviewed squarefree order-20 theorem, V8/V9 unit-axis obstruction, V10 weighted two-chart cover, and V11 constant-load/vertex/rescaled-Lambda theorems |
| Target | `xmodel/max12-912-order3-d1-ordinary-v8-cone-composition-theorem-20260826.md` |
| Target SHA-256 | `97161bce10367142b027a1108db75b02e3eb3194f9b74583201267c657a14294` |
| Overall verdict | **ORDINARY_V8_CONE_COMPOSITION_CONFIRMED**. Every named input hash recomputes. The V10 review-prompt squarefree-SHA erratum is recorded accurately and did not enter a source closure, compiler, certificate, or theorem. V8 remains the fixed-chart unique-least-term identity; V9 transports it on `D(a)` at fixed loads; V10 is the weighted two-chart cover of noncentral directions; V11 restores constant-field loads and retires the vertex / nominal `v(Λ/a)≤0` remainder. No parent is silently expanded to a full fan or to series loads. “Genuine direction” is the product of a nonzero `Proj P(2,3)` leading vector with a positive radial/Rees `Λ` inside the charged ordinary source and the registered open V8 cell; it is not defined as “whatever was already excluded.” The reduced-support partition `Δ≠0` / nonzero `Δ=0` / affine vertex is exhaustive for reduced projective centres. Both axes are squarefree. The disc-zero locus is one `P(2,3)` point `(-3:2)`. Chart interfaces `h=a^3 s`, `h=t^3,a=tb`, walls `s=0,-4`, opposite-axis `(a,h)↦(-a,h+4a^3)`, and positive rescaled `Λ` match the reviewed V10 cover; no valuation ray, sign, or equality wall of that cover is missing from the composition. V11’s 62/14/48-term theorem makes the V8/V10 combination uniform in every constant `k,μ,ν`; the V10 h-chart squarefree open was already uniform. `(p,c)=(0,0)` is the irrelevant `Proj` point, not an excluded live arc of this leading problem. Nominal `v(Λ/a)≤0` is a wrong chart/order or a zero leading vector requiring regrading. The licensed conclusion is exactly the relative statement: if a cyclic-D1 client has already landed in this charged ordinary source and this registered V8 cone, with constant-field loads, no genuine strict coefficient-infinity direction remains. Global landing, source-chart coverage, rational-section/Taylor access, series loads, D1 as a whole, and JC2 are declined |
| Smallest failing identity | none in the target, any named hash pin, the displayed disc-zero parametrization, uniqueness of `(-3:2)`, the axis-squarefree discriminants, the V10 chart-overlap and opposite-axis identities, or the logical interface among the promoted V8–V11 scopes |
| Smallest missing hypothesis for a stronger theorem | a landing/accessibility theorem placing every D1 client in this ordinary source and this V8 cone; a theorem for formal/Puiseux-series loads; a classification of `η<0` or of the closed walls `u=0` or `v=0`; a regraded higher-order leading problem at the cone vertex; embedded thickness; D1; or JC2 |
| Evidence tier | SHA-256 of the target and every named anchor; source reading of every listed promotion, review, V11 `RESULT.md` / `FREEZE.sha256`, and the charged reconstruction header; hand expansion of `Δ=-4p^3-27c^2` on `p=-3a^2,c=2a^3`, on both axes, on the a-chart residual `p=-3,c=2+s`, on the h-chart residual `p=-3b^2,c=2b^3+1`, of `s=b^{-3}=-4` on `4b^3+1=0`, and of the opposite-axis preservation of `(p,c)`. No Singular, Sage, msolve, Lean, V8–V11 compiler, charged reconstruct, or other substantive algebra program |
| Reviewer / model | Grok 4.6 (xAI). Independent hostile composition referee |
| Repo | `/Users/dc/code/math/jc2` |
| Git HEAD | `418e413593120d19e15e6546eb50c985f4b1f038` |
| Host | Darwin. No parent compiler or reconstruction was run |

## What was and was not run

No local substantive computation was run. In particular this review did **not** execute any V8–V11 compiler, `independent_reconstruct.py`, Singular, Sage, msolve, Lean, or any other solver, and it did not re-multiply an eight-row combination. Hashes were recomputed with SHA-256 of file bytes. Displayed polynomial identities in two variables were expanded by hand. Parent tropical counts (48/62/14-term histograms, closed-corner margins, face slopes) were taken from the already-reviewed records, not recomputed from certificates.

The composition theorem is uncommitted. Every named parent file is the live byte object whose hash is quoted below.

---

## Verdict

**CONFIRMED**

The target is an exact relative no-direction theorem inside one frozen ordinary source and one registered open weight cell. It is the set-theoretic partition of the reviewed reduced exceptional divisor `P(2,3)` together with the already-promoted chart and load theorems on each piece. No new algebra is claimed, and none is needed. No identity failed. No parent scope is inflated.

---

## Strongest exact claim that survives

Work over a characteristic-zero finite constant extension `L`, in the ordinary isotrivial eight-tail source charged by

```text
67343b569db54f61f4a9e56e4262c0bf4fa781323a1287addbaf4fff93844623
  independent_reconstruct.py,
```

with the literal reviewed V8 multipliers, the ordinary constant-field loads

```text
kbar = Λ^6 k,     r_3 = Λ^{15} μ,     r_6 = Λ^{18} ν,
r_8 = Λ^{20}(1+τ),
```

`k,μ,ν ∈ L` arbitrary, and the registered open V8 cell

```text
α = 15/2,   β = 5+u,   δ = 5+v,
u>0, v>0, T = wt(τ)>0, η ≥ 0
```

after the licensed V9 hat-normalization and the licensed V10 weighted-chart changes. A genuine coefficient-infinity direction in this theorem is a nonzero projective leading coefficient vector of the reviewed associated-graded divisor, together with a positive radial/Rees `Λ`, inside that source and cell.

No such direction exists. Equivalently: if a cyclic-D1 client has already been placed in this exact ordinary-tail chart, has constant-field loads as declared by the source, and lands in this registered V8 cone, then it has no genuine strict coefficient-infinity leading direction.

The exhaustive reduced-projective partition is:

| Leading situation | Disposition, as licensed by the pinned parents |
|---|---|
| Common cubic, `Δ = -4p^3-27c^2 ≠ 0` | Reviewed squarefree order-20 theorem. Already uniform in every constant `k,μ,ν` and every strict positive slope. Both axes `p=0,c≠0` and `c=0,p≠0` lie here. |
| `Δ=0`, nonzero projective centre, unit axis | `p=-3a^2`, `c=2a^3+h`. V9 transports the V8 identity étale-locally on `D(a)`. Unique least term `Λ^{20}` on the registered open cell. V11 restores constant loads: the 14 load monomials have closed-corner margin at least 1, so they cannot cancel the old face. |
| Noncentral weighted degeneration toward the triple-root vertex | V10 with `wt(a)=1`, `wt(h)=3`. a-chart `h=a^3 s` scans the full combination; residual walls `s=0,-4`. h-chart `h=t^3`, `a=tb`; residual squarefree open `4b^3+1≠0` is the order-20 theorem, already load-uniform; `4b^3+1=0` is the overlap `s=-4`, identified with the opposite double-root wall by `a'=-a`, `h'=h+4a^3`. V11 makes the combination part of this cover uniform in constant loads. Positive rescaled `Λ` is required. |
| `a=h=0` at the selected leading order | All eight graph coefficients vanish. Affine cone vertex of the reviewed support theorem; irrelevant point of `Proj P(2,3)`; not a projective leading direction. |
| Nominal `v(Λ/a)≤0` at a nonzero disc-zero point | After `P(2,3)` normalization `a` is a unit, so positive radial `Λ` gives `v(Λ/a)=v(Λ)>0`. The nominal remainder is a finite/`Λ`-leading order or a zero leading vector that must be regraded. Not a live point of this strict chart. |

---

## Exact meaning of the relative closure

The closure is **conditional on already being in the charged ordinary source and the registered V8 cone**. It is a statement about reduced projective leading centres of the associated-graded eight-tail divisor, paired with a positive radial/Rees filtration that satisfies the V8 inequalities, with `k,μ,ν` constant in `L`.

It is **not** a proof that every D1 client enters this chart or this cone. It does not produce a rational constant-field section, a Taylor landing, or global source-chart coverage. It does not classify embedded or nilpotent thickness. It does not treat loads that are themselves series. It does not exclude a regraded leading form whose actual coefficient vector is not this `P(2,3)` form. It is not a closure of D1, of maximum twelve, or of JC2.

---

## Sharpest non-claim

Do not read the target as: D1 has no coefficient-infinity arc; every cyclic-D1 client lands in this ordinary chart; the V8 cell is the only normal cone; `η<0` or `u=0` or `v=0` is obstructed; `(p,c)=(0,0)` cannot appear as a finer-order leading form after regrading; formal-series loads are covered; `I_inf` is radical; a rational section exists; or JC2.

---

## Sources actually used

Read in full, hashes recomputed and matched before any parent conclusion was trusted:

```text
97161bce10367142b027a1108db75b02e3eb3194f9b74583201267c657a14294
  xmodel/max12-912-order3-d1-ordinary-v8-cone-composition-theorem-20260826.md
a274c8d8e0883ce7606f31f69803a293afb15ad48fe9d401191cac1cdbe1daee
  xmodel/max12-912-order3-d1-weighted-infinity-exceptional-support-review-grok-20260825.md
1b6e629affdc5a73995e0ab2d7f051ad772d92f75bb61e67362782fcb8f5b4ac
  xmodel/max12-912-order3-d1-squarefree-common-cubic-order20-obstruction-20260825.md
6883ef76710176ae9e091249224730e3ff181fa76c66c767b53b26c770c101d5
  xmodel/max12-912-order3-d1-squarefree-common-cubic-order20-review-grok-20260825.md
b503f6a70c39c256ba27f27823d5788bf5787badc507efa3079433064eb142e3
  xmodel/max12-912-order3-d1-double-root-control2-hq-linear-filtered-lift-promotion-20260826.md
2c3dbbd50089892197dd5ad5c0fb90776b5c2d2dea4ac2752f3b1a3218cc7d4c
  xmodel/max12-912-order3-d1-double-root-control2-hq-linear-filtered-lift-review-grok-20260826.md
b8a1122709bca351e396f7a2985cc65e79138c6079ec52d56cfb10fc93125df2
  xmodel/max12-912-order3-d1-double-root-control2-axis-covariance-promotion-20260826.md
f81c5b054c2ff331176bd39005ad8f826b92efbfd5e16b1f7f20ae7dcbab7226
  xmodel/max12-912-order3-d1-double-root-control2-axis-covariance-review-grok-20260826.md
a4f6e8646f1f0bd198c0473f1588d3f21bfb6c167a23db48e173bcd83a7d3158
  xmodel/max12-912-order3-d1-triple-root-weighted-two-chart-v10-promotion-20260826.md
a497ce3e63569bedfaa3ba2c547e90bc86ade50e6db9d2895693332a67a8ad1e
  xmodel/max12-912-order3-d1-triple-root-weighted-two-chart-v10-review-grok-20260826.md
c7d42e2d38fc8ca3f283c768287a87f7e52dba0678aa41412f6be5b9d62db925
  cases/max12_912_order3_d1_triple_root_moving_load_v11_20260826/RESULT.md
466a0665f8c6a69a318a91ccd585517978767da9bf2b0f8b28ad098d1906d13d
  cases/max12_912_order3_d1_triple_root_moving_load_v11_20260826/FREEZE.sha256
fe00bdbb2d310dbd618b8b3334544ba983c8f7bd108961a28441e568c45e39b9
  xmodel/max12-912-order3-d1-triple-root-moving-load-v11-review-grok-20260826.md
abe8720ecdce3eb33f3c9f98a7016161fc7aad09286fae5073f06ace37524fac
  xmodel/max12-912-order3-d1-triple-root-moving-load-v11-promotion-20260826.md
67343b569db54f61f4a9e56e4262c0bf4fa781323a1287addbaf4fff93844623
  cases/max12_912_order3_d1_passport_full_fibre_taylor_v2_20260825/independent_reconstruct.py
```

Quoted inner V11 polynomials, checked as strings against the reviewed V11 theorem rather than recomputed:

```text
c0f1552c06650e321db954ab99788324f59e52dea94fd8e7dfbdfbb3931aeab6   62-term full-load witness
dbd78a81294d473aa043af6e0b0c87ce8050ae3e906a20d07395143d4371d75f   14-term load residual
53cf4b656c4415324a74862ec108b902ac8eade22733b2cb3a58cd3e447491e0   48-term V9/fixed specialization
```

The V10 review-prompt transcription

```text
1b6e629affdc5a41c5abb7e06d63c8dba026bcc46df0a6f21bfed724589189c5
```

occurs in `xmodel/max12-912-order3-d1-triple-root-weighted-two-chart-v10-review-grok-20260826-prompt.md`, is quoted as an erratum by the V10 promotion and the V10 review, and occurs in no `SOURCE_CLOSURE`, compiler constant, AWS certificate, or theorem file.

---

## Charge 1 — Custody and parent-scope quotation

**CONFIRMED. Every required hash matches. Parent scopes are quoted at their promoted strength. No fixed chart is replaced by a full fan, and no constant load is replaced by a moving series.**

Target hash `97161bce…` matches the required pin. All fourteen named anchors match the pins in the charge prompt and in the target’s frozen-input table. The charged reconstruction `67343b56…` is named in the target body with the same hash; it is the ordinary-source pin of every parent.

Squarefree SHA erratum. The live/frozen theorem is `1b6e629affdc5a7399…`. The V10 review prompt alone printed `1b6e629affdc5a41c5…`. The target states that this was a prompt-only transcription error, explicitly recorded by the V10 promotion, and that it never entered a source closure, compiler, certificate, or theorem. That is accurate: the wrong digest appears in the V10 review prompt, is quoted as an erratum by the V10 promotion and V10 review, and the live theorem, V10 `SOURCE_CLOSURE`, V10 compiler constant `SQUAREFREE_SHA`, both V10 certificates, the squarefree review target, and the squarefree review prompt all carry `1b6e629affdc5a7399…`.

Promoted scopes, as quoted:

| Parent | Promoted scope | Target quotation | Creep? |
|---|---|---|---|
| V8 | Fixed chart `a=1`, `p=-3`, `c=2+h`, loads `k=ν=0`, `μ=2/3`; unique least `Λ^{20}` on `u,v,T>0` iff `η≥0`; 24-column RREF complete only for the stated projection; the weight theorem is about the explicit 48-term polynomial | Unique least `Λ^{20}` throughout the registered open cell; combination used as an obstruction, not as module-support completeness | No. The fan is V10’s row, not V8’s. |
| V9 | Unit-axis covariance on `D(a)` at those same fixed loads; `v(a)=0`; positive axis valuation excluded from licensed scope, not ruled out | Transports the V8 identity étale-locally on `D(a)` | No. `v(a)>0` is the next row (V10). |
| V10 | Weighted blow-up `wt(a)=1`, `wt(h)=3`; noncentral directions; positive rescaled `Λ`; fixed loads; central vertex, nonpositive rescaled `Λ`, and moving loads charged separately | Two charts `h=a^3 s` and `h=t^3,a=tb`; residual squarefree open to order-20; overlap `s=-4`; V11 for loads and vertex | No. |
| V11 | Constant-field `k,μ,ν`; 62-term combination; vertex retired as `Proj`-irrelevant; `v(Λ/a)≤0` classified as chart/order; series loads declined | Restores constant loads; vertex is the irrelevant `Proj` point; nominal `v(Λ/a)≤0` is regrading/wrong order; series loads remain outside | No. |

The target’s phrase “after the licensed coordinate normalizations and weighted-chart changes” refers exactly to V9 hats (`wt(a)=0`) and V10 charts (`wt(a)=1`, `wt(h)=3`, residual `Λ`). Those are the promoted changes of the same ordinary module, not a new source.

V8’s combination remains an obstruction rather than a completeness theorem: vanishing of all eight rows implies vanishing of any combination, and unique least term a unit times `Λ^{20}` forbids that vanishing. The target does not claim that the 24 V8 columns exhaust the eight-row module.

---

## Charge 2 — Meaning of a genuine direction

**CONFIRMED. The definition is coherent and not tautological. No live point of the stated relative problem is discarded by wording.**

The target defines a genuine coefficient-infinity direction as all four of:

1. a nonzero projective leading coefficient vector on the reviewed associated-graded divisor,
2. a positive radial/Rees `Λ`,
3. the exact charged ordinary source `67343b56…`,
4. the exact registered V8 inequalities `α=15/2`, `β=5+u`, `δ=5+v`, `u>0`, `v>0`, `T>0`, `η≥0`.

Explicitly excluded from the relative problem, not from mathematics: the zero affine-cone vector; a filtration in which `Λ` is not radial-positive; a leading form that appears only after regrading.

This is the product of the reduced `P(2,3)` exceptional divisor with the open V8 weight cell, inside one source. The subsequent theorem is that this product is empty of solutions of the eight loaded equations. Emptiness is proved by the partition, not by defining the set to be empty.

Directions that are outside the definition, and why that is not improper discarding:

| Candidate | Status |
|---|---|
| `η<0` | V8 unique-least fails for this witness (displayed `h`-positive face terms fall below target). The cell is a next-lift problem, not a surviving projective centre. The target keeps `η≥0` as a hypothesis. |
| Closed walls `u=0` or `v=0` | Face monomials can tie at the closed corner. The registered cell is the open quadrant, matching V8. |
| `T=0` | `Λ^{20}` and `Λ^{20}τ` would share weight. Squarefree and V8 both require `v(τ)>0` for the unit leading coefficient of row 8. Coefficient-infinity in this chart has `T>0`. |
| Embedded / nilpotent thickness | Not a reduced projective leading vector. The support theorem is reduced support only; the target says so. |
| Formal-series loads | Not values of the cyclic-D1 constants `k,μ,ν∈L`. V11 firewall. |
| Regraded vertex | The chosen associated-graded vector is zero; a finer-order leading form is a different problem. Named as a non-claim. |
| Other passports / non-ordinary sources | Different charged module. |

No point that is simultaneously a nonzero `Proj` centre, a positive radial `Λ`, this source, and this open V8 cell is defined away.

---

## Charge 3 — Projective partition

**CONFIRMED. Squarefree `Δ≠0`, nonzero `Δ=0`, and the affine vertex exhaust the reduced points. Both axes are squarefree. The disc-zero locus is one `P(2,3)` point. Reduced support is enough for a no-direction theorem; it is not a thickness theorem.**

The reviewed exceptional-support theorem, SHA `a274c8d8…`, verdict CONFIRMED, identifies the reduced affine support of `I_inf` with the graph of `C=z^3+p z+c`, `f=C^3`, `g=C^4`. In the weighted projective space of the `B_i` this is `P(2,3)`, including both axes and the discriminant-zero locus. The affine origin is a point of the cone and is not a projective coefficient-infinity centre. Embedded `m`-primary thickness is not classified.

A nonzero point of that graph is squarefree or has `Δ=0`. There is no third reduced projective class.

Disc-zero parametrization, characteristic zero. A depressed cubic has a multiple root at `a` iff `p=-3a^2` and `c=2a^3`:

```text
(z-a)^2(z+2a) = z^3 - 3a^2 z + 2a^3,
-4p^3 - 27c^2 = 108 a^6 - 108 a^6 = 0.
```

Affine origin iff `a=0`. Every nonzero disc-zero point has `a≠0`. The `P(2,3)` action `(p,c)↦(λ^2 p, λ^3 c)` with `λ=1/a` sends it to `(-3:2)`. The representative `(-3:-2)` is the same point (`λ=-1` flips the odd-weight coordinate). So the remaining locus is one point of `P(2,3)`.

Axes, characteristic zero:

```text
p=0, c≠0:   Δ = -27 c^2 ≠ 0,
c=0, p≠0:   Δ = -4 p^3 ≠ 0.
```

Both lie in the squarefree open and are covered by the order-20 theorem. They are not extra disc-zero centres.

Reduced support versus thickness. A coefficient-infinity *direction* is a point of `Proj` of the associated-graded cone. Extra embedded structure at the irrelevant ideal, or non-reduced structure along the graph, does not create a new reduced projective centre. The target claims no additional reduced projective leading direction, and expressly declines a global thickness or lifting theorem. That is the correct strength. Using reduced support for a no-direction conclusion is licensed; inferring reducedness of `I_inf` would not be, and is not inferred.

There is no fifth reduced projective leading direction to test.

---

## Charge 4 — Chart composition V8 / V9 / V10

**CONFIRMED. The interface is exact. No chart overlap, valuation ray, sign, or equality wall of the claimed cover is missing.**

Licensed chain:

- V8: unique least `Λ^{20}` at `a=1`, `p=-3`, `c=2+h`, fixed loads, on `u,v,T>0` iff `η≥0`. All 30 non-target equality-face terms have a positive `u` or `v` slope; `Λ^{20}τ` is strict for `T>0`.
- V9: eight row identities `E_i(a,h,Q,R,Λ,ρ,τ)=a^{12+i} E_i(hats)` at `p=-3a^2`, `c=2a^3+h`; clearing power exactly 3; identity `sum_i a^{11-i} F_i(hat) E_i = a^{23} W_{V8}(hat)` of SHA `53cf4b65…`; Jacobian `-6a`; `Δ=-27h(4a^3+h)`; transport on `D(a)` with `wt(a)=0`. Positive axis valuation is outside V9, not ruled out.
- V10: blow-up of `(a,h)=(0,0)` with `wt(a)=1`, `wt(h)=3`, under `ell=wt(Λ)-wt(a)>0`.

a-chart `h=a^3 s`. Residual cubic `p=-3`, `c=2+s`. Discriminant, by hand:

```text
-4(-3)^3 - 27(s+2)^2 = 108 - 27s^2 - 108s - 108 = -27s(s+4).
```

Walls `s=0` and `s=-4`. Axis coefficients of the 48-term (and, after V11, 62-term) combination vanish, so this is the cone `wt(h)≥3 wt(a)`, not one numerical `h` grade. On `s=0` the residual is the V8 double-root axis; `η` need not lift, but every non-target face term has positive `u` or `v`.

h-chart `h=t^3`, `a=tb`. Residual cubic `z^3-3b^2 z+(2b^3+1)`. Discriminant, by hand:

```text
-4(-3b^2)^3 - 27(2b^3+1)^2 = 108 b^6 - 108 b^6 - 108 b^3 - 27
  = -27(4b^3+1).
```

Ordinary tails homogeneous of weight `12+i`, including targets 15, 18, 20, convert the physical equations into residual ordinary loaded equations. Positive rescaled `Λ` makes the residual a coefficient-infinity arc. On `4b^3+1≠0` the residual centre is squarefree, so the reviewed order-20 theorem applies. That invocation uses ordinary homogeneity and the squarefree hypotheses (`v(τ)>0`, constant loads, same charged source), not the V8 combination.

Overlap. On `b≠0`, `s=h/a^3=b^{-3}`. The locus `4b^3+1=0` forces `b^3=-1/4` and `s=-4`. Opposite-axis map, by hand:

```text
a' = -a,   h' = h+4a^3,
p' = -3a'^2 = -3a^2,
c' = 2(-a)^3 + h + 4a^3 = 2a^3 + h.
```

At `s=-4` one has `h' = 0`: the already-covered opposite double-root wall.

Coverage of noncentral directions, rank-one valuation, as already charged by V10:

- `v(h)≥3 v(a)`, `a≢0`: a-chart, including walls `v(s)=0` and interior `v(s)>0`.
- `v(h)<3 v(a)`, `h≢0`: h-chart interior.
- `h≡0`, `a≢0`: `s=0`, a-chart wall.
- `a≡0`, `h≢0`: `b=0`, residual `z^3+1`, squarefree, order-20.
- `a≡h≡0`: central vertex, not a projective centre (Charge 6).

If `b` poles then `v(s)>0` (a-chart interior). If `s` poles then `v(b)>0` (h-chart interior). There is no ray with both infinite. Sign of `a` is the opposite-axis identification, not a missing chart. The étale `a`-choice on the smooth discriminant is V9’s faithfully flat descent; a quadratic scalar extension does not create a new centre.

Equality walls of the *weight cell* (`u=0`, `v=0`, `T=0`) are not chart walls of `(a,h)`. They are outside the registered open V8 cell, matching Charge 2. The chart walls `s=0,-4` are present.

V10’s `ell>0` is the residual of `Λ/a` (a-chart) or `Λ/t` (h-chart). On a unit-axis disc-zero centre, V9 has `v(a)=0`, so this is ordinary positive `Λ`. On the degeneration toward the vertex it is the V10 hypothesis, quoted by the target.

---

## Charge 5 — Loads

**CONFIRMED. V11 makes the V8/V10 combination uniform in every constant-field `k,μ,ν`, including zero and special values. The V10 h-chart squarefree theorem is already uniform at this scope. Series loads stay outside.**

Squarefree order-20, SHA `1b6e629a…`, is uniform in the constants `k,μ,ν` including every zero-load stratum, and never divides by a load. The V10 h-chart open `4b^3+1≠0` is an invocation of that theorem. It does not need V11. The target’s first table row states this, and V11 Charge 6 records that V11 does not replay the h-chart squarefree argument.

V8/V9/V10 a-chart used the fixed loads `k=ν=0`, `μ=2/3`. That was the only load firewall in the partition. V11 restores

```text
kbar = Λ^6 k,   row 3 = Λ^{15} μ,   row 6 = Λ^{18} ν,
row 8 = Λ^{20}(1+τ),
```

with load characters zero, and fail-closes all eight full-load row covariances at `p=-3a^2`, `c=2a^3+h`. The 62-term combination has SHA `c0f1552c…`. Specializing `(k,μ,ν)=(0,2/3,0)` is dictionary recovery of the frozen 48-term polynomial of SHA `53cf4b65…`, not a nearby normalization. The 14 load-sensitive terms have SHA `dbd78a81…` and counts `k:13`, `μ:0`, `ν:1`. Vanishing of `μ` is the identity `F_3≡0` in the literal V8 multipliers. Every load term has strictly positive closed-corner margin (minimum 1; unique `ν` term margin 3; `k` margins 1, 7/2, or 6). Axis coefficients still vanish on all 62 terms. The 30-term non-target face, the coefficient-one target `a^3 Λ^{20}`, and the coefficient-one split `a^3 Λ^{20}τ` are unchanged.

Constants do not change valuations. Therefore no constant-field value of `(k,μ,ν)` — zero, generic, or special — can cancel a face monomial or manufacture a below-target term. Cancellation among load monomials can only delete above-face terms. Two slope-zero `k` terms sit at margin 1, not on the face. This is uniformity of the *existing* witness, which is exactly what the relative obstruction needs: vanishing of the eight rows still implies vanishing of the combination, and the combination still has unique least term a unit times `Λ^{20}` on the registered cell.

The cyclic-D1 source declares `k,μ,ν` as elements of `L`. Formal or Puiseux-series motion of the loads would change the associated-graded rows. No parent covers that. The target keeps it outside. The sentence “V11 makes the cover uniform in all constant loads” is the a-chart/combination part of V10 together with the already-uniform h-chart squarefree open; it is not a claim that V11 re-proves squarefree.

---

## Charge 6 — Vertex and rescaled `Λ`

**CONFIRMED. `(p,c)=(0,0)` is projectively irrelevant for this leading problem, not an excluded live arc. Nominal `v(Λ/a)≤0` is a wrong chart/order or a zero leading vector. All regraded and `Λ`-leading non-claims are preserved.**

V11 checks that all eight graph coefficients vanish at `(p,c)=(0,0)`. Combined with the reviewed support theorem, this is the affine cone vertex `C=z^3`, `f=z^9`, `g=z^{12}`. Three distinct origin-objects, already in the support review: the affine origin, a point of the cone; an origin-supported embedded component, unclassified; the irrelevant point of `Proj`, which is not a point of the weighted projective exceptional divisor. A coefficient-infinity leading centre is a *projective* point. The vertex is not one.

This does **not** exclude an arc whose associated-graded vector in this weighting is zero and which must be regraded at the next actual coefficient order. The target says so: “Such a regrading can be necessary when the selected leading vector is the cone vertex,” and lists a regraded higher-order leading form and a `Λ`-leading filtration as genuinely outside the source cone.

Nonzero disc-zero projective points have `a≠0`. After `P(2,3)` normalization, `a` is a unit, so a positive radial/Rees `Λ` retains `v(Λ/a)=v(Λ)>0`. A cell with `v(Λ/a)≤0` is therefore not a coefficient-infinity arc whose *leading* cubic is a genuine nonzero disc-zero point of this chart. The two exhaustive alternatives are exactly the target’s: after normalization, `v(Λ)≤0` (finite/`Λ`-leading, wrong order for this filtration); or the leading vector is the vertex and must be regraded. Neither is a missed live point of the stated strict chart.

V10’s vanishing of physical `a` while approaching the vertex is a different leading (residual) cubic, already partitioned into the a-chart and h-chart rows. It is not this remainder.

---

## Charge 7 — Relative versus global D1

**CONFIRMED. The target licenses exactly the conditional relative statement, and declines global landing, source-chart coverage, rational-section/Taylor access, series-load motion, D1 as a whole, and JC2.**

Licensed, as written:

> If a cyclic-D1 client has already been placed in this exact ordinary-tail chart, has constant-field loads as declared by the source, and lands in the registered V8 weight cone, then it has no genuine coefficient-infinity leading direction.

Declined, as written: every D1 client lands in this ordinary source chart or in this normal cone; a rational constant-field section; Taylor landing/accessibility; global source-chart coverage; formal- or Puiseux-series load motion; a regraded higher-order leading form; a `Λ`-leading filtration; another passport or ordinary/nonordinary source; embedded thickness as a lifting theorem; D1 as a whole; maximum twelve; JC2.

The squarefree global-support corollary (`H≠(1)` implies reduced projective support is the double-root point) is **not** promoted as a D1 or no-section theorem. The composition uses the local partition of reduced support inside the already-assumed ordinary source and V8 cone. That is the weaker, correctly scoped reading.

Status line of the target matches the verdict: exact relative closure of genuine coefficient-infinity directions in the frozen ordinary source/V8 cone; not a global D1 or JC2 theorem.

---

## Attacks recorded and rejected

1. **V8 unique-least silently treated as a full double-root fan.** The fan is V10’s row. V8 is quoted at `a=1`; V9 moves it onto `D(a)`; V10 covers `v(a)>0`.
2. **V9 “positive axis valuation is excluded” misread as a mathematical vanishing.** The target puts that cell in V10. Matches V9’s “excluded from licensed scope.”
3. **V11 series-load language (“moving-load”) smuggled into the composition.** The target treats `k,μ,ν` as constants in `L` and names series motion as a broader DVR-deformation problem.
4. **“Genuine direction” defined as the already-obstructed set.** The definition is geometric (nonzero `Proj` vector, positive `Λ`, source, V8 cell). Emptiness is the theorem.
5. **`η<0` or `u=0`/`v=0` discarded by tautology.** They are outside the registered open cell, matching V8. They are not projective centres.
6. **A fifth projective class on `P(2,3)`.** Axes are squarefree. Disc-zero is one point. Vertex is not a `Proj` point.
7. **Reduced support too weak because of unclassified thickness.** Thickness does not create a new reduced projective direction. The target does not claim reducedness.
8. **Missing ray with both `s` and `b` infinite, or a missing sign of `a`.** V10 coverage: one poles iff the other vanishes. Opposite-axis map identifies `s=-4` with `h'=0`.
9. **V11 required for h-chart squarefree uniformity.** Squarefree is already uniform. V11 closes the combination/a-chart load firewall only.
10. **Special constant loads cancel the equality face.** Load monomials are strictly above the face; constants do not change weights; `μ` never appears.
11. **Vertex retired as an excluded arc.** It is the irrelevant `Proj` point. Regrading remains open and is named.
12. **`v(Λ/a)≤0` is a missed disc-zero centre.** After normalization `a` is a unit. Remainder is wrong order or zero leading vector.
13. **Relative theorem read as global D1 or as the squarefree `H≠(1)` corollary.** The target’s Yes/No paragraph blocks both readings.
14. **Wrong squarefree SHA inherited from the V10 review prompt.** The target pins the live digest `1b6e629affdc5a7399…` and records the prompt erratum.

## Nits, not defects

1. Table row 1 quotes squarefree at its native slope-uniform strength, which is stronger than the ambient V8 cone. The licensed composition remains inside that cone.
2. “V11 makes the cover uniform” is slightly coarse for the h-chart squarefree open, already uniform by order-20. The same row already sends that open to order-20, and the following paragraph identifies V11 as closing the *only load firewall* (the combination).
3. `independent_reconstruct.py` is hashed in the scope section rather than in the frozen-input table. The pin is present and correct.
4. The target does not restate V8’s converse (`η<0` fails for this witness). It includes `η≥0` as a hypothesis, which is the licensed cell.

No source repair is required.

ORDINARY_V8_CONE_COMPOSITION_CONFIRMED
