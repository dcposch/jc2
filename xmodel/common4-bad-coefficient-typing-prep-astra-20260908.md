# Common4 centered bad coefficient: exact-source typing packet

2026-09-08. PREP ONLY, no launch or coefficient decision. Future tag
`common4-bad-coefficient-typing-sol56-20260908`; immutable prompt
`box/common4-bad-coefficient-typing-20260908/sol.prompt.md`.

## Result

A local text-only coefficient schema is available. The packet does not
need the remote full row stream to ask Sol to type the proposed h.
The unchanged `baseline.py` defines the exact common4 support, complete
faces/fixed zeros, labels and variable order; unchanged `exporter.py`
defines all full J/lift rows, source-map encoding and guard obligations.
Neither was imported or executed. Both were read completely as text.

Ten uniquely named charged snapshots include the two requested whole
mathematical sources, whole sufficient lift contract, those two exact
code files, the actual compact common4/Q header/manifest, a scope note,
and only the relevant historical and accepted canonical excerpts.
`PINS.json` records every original/snapshot SHA256 and byte count, with
section provenance for excerpts. It also retains provenance-only pins
for the terminal preflight/exporter/pilot reports consulted during typing.
No extra source or accepted checker is silently charged for execution.

The original stream is known at historical path
`/home/ubuntu/d125-small-source-construction-pilot-20260906/common_4-rational/`
on retained EBS of root-owned i-0da0cebfc97c9fd54. Its JSONL SHA is
276ee6f4c23df9577cbeb832f06e84ae1c06dcf29b960e067d08083d3a0db13e;
the Singular SHA is
0fde2d1d3cfcc3659f31431bbc2dca8ea506a870c6309e73217f4cd12b68ab7c.
These are retained provenance, not fresh remote observations. The worker
remains out of scope and no full stream was read or copied locally.

## Exact distinctions imposed on the task

The accepted complete baseline has371 variables and816 literal slots,
including all105 inverse-polynomiality rows, fixed-assignment identities
and guards. Its common4 normalization has already set mu=1. The prompt
requires reconciling the general mu formula for the centered coefficient
with that actual normalized source; no extra mu, c=1, h=1 or lambda guard.
The original coefficient ring and lift map are preserved: the translation
defines h, not an automatically valid new centered source presentation.

The existing name `z` inverts c; any later localization uses a fresh
`z_h`. If its coefficient map is licensed, the proposed object retains
all816 rows and adds z_h*h-1 (372 names817 slots). This is a typed proposal,
not an emitted equation system, a nonzero class, a membership decision,
properness or an existence result. Sol must distinguish fixed-face zero,
free-coefficient polynomial and redundancy modulo the full source ideal.
No missing semantic dependency has been inferred merely because the sole
complete stream is remote. A specific map gap, if found, must stop work.

The accepted lift theorem supplies the conditional properness endpoint
over an algebraic closure, with no rational-point requirement. One unit
localization would exclude only this h-open, not every bad coefficient
or the common4 source. Receiver-only/modular evidence and the old J=0
face control cannot establish the full-source endpoint.

## Dispatch and custody

Read-only wrapper inspection resolves the adapter ambiguity: the argument
is `sol`, not `sol56`. `ops/lane.sh` constructs adapters/ARG.sh;
`ops/adapters/sol.sh` exists/executable and defaults to gpt-5.6-sol, while
`ops/adapters/sol56.sh` is absent. No wrapper, adapter or environment was
changed. The future tag may still end in sol56 as requested. Root alone
will preflight and launch separately.

The metadata preflight uses the actual literal charged-input sed parser,
checks ten unique basenames, exact output instruction, literal placeholder,
all source/snapshot pins and transaction integrity. It does not launch a
lane or evaluate mathematical functions. Date-stamped AUDIT13l/m/n/s
excerpts supersede historical PENDING headers without editing them.

Read perimeter: whole requested receiver/interface, whole lift/preflight/
exporter/pilot prose, whole baseline/exporter static code, exact compact
common4 header/manifest, named canonical entries and history229–270.
Searches were confined to these named source tags and metadata directory;
no live peer body/log/receipt, protected project, remote state, CAS, high
power, source reconstruction, mathematical replay or new agent was used.
No mathematical outcome for h is asserted by this preparation. All files
are immutable after transaction/custody publication; writers and metadata
children terminal at handoff. STOP/IDLE.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `4656`.
- Body SHA-256:
  `c441d24077d7a321459bddfa57d81c2e716749e19b566f209f59467210998b18`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
