# Hostile review — D1 `a>=10`, `C>=a+1` source-ceiling corollary (Grok V2)

| Field | Value |
|---|---|
| Target | `xmodel/max12-812-order2-square-d1-age10-cge-a1-source-ceiling-corollary-20260826.md` |
| Parent producer | `cases/max12_812_order2_square_owner_d1_age10_j38_odd_recurrence3_20260826/` |
| Overall verdict | **CONFIRMED_CONDITIONAL** |
| Smallest failed substitution | none among `n,m,s>=0` |
| First missing jet | none; at `m=10` the grade-38 `k6*C/L` coefficient `C_10` is present, and at `m=11` the earliest `C`-dependent family is grade 39 |
| First omitted fan point | none among unique-`AC` points with `a>=10`; what remains is the finite `a<=9` threshold range |
| Condition | specialization is exact as a closed source-ceiling of the frozen grade-38 identity, but final promotion remains conditional on independent confirmation of the parent producer review. This review does not import, mutate, or certify that parent review |
| Reviewer / model | Grok 4.6 (xAI). Independent hostile rederivation. Producer PASS markers, validator strings, finite-field agreement, and the failed Claude/Fable log are not authority |
| Method | SHA-256 of the charged corollary pins, both parent manifests, nested compiled manifests, and nested freeze ancestry used below; independent four-summand binomial primitive census through grade 80; mechanical `C_10` ceiling table; generating-function pole-three identity; unique-`AC` integral census. No Singular re-execution, Sage, msolve, Lean, or package compiler |
| Repo | `/Users/dc/code/math/jc2` |
| Git HEAD | `418e413593120d19e15e6546eb50c985f4b1f038` |
| Date | 2026-08-26 |

Independently recomputed SHA-256 of the two frozen corollary pins match the charged bytes. All seven rows of the corollary manifest, both parent manifests, both nested `compiled.sha256` files, and every nested freeze row used below rehash. The first Claude/Fable delivery wrote no report and has no verdict; it is preserved unchanged and is not used as mathematical evidence. No file other than this review was written.

---

## Verdict

**CONFIRMED_CONDITIONAL.**

The frozen corollary is an exact polynomial specialization of the compiled grade-38 source identity, not a density argument from an exact-contact open and not a new landing theorem. The exact compiled source is a polynomial in every `Abar,Cbar,Rbar,theta,eta` jet. The only localization used for the endpoint is `iJ*J-1`. Substituting

```text
theta = sigma^n,   eta = sigma^s,   Cbar = sigma^m*Chat,
n,m,s >= 0
```

commutes with the moving `p(sigma)` Faber connection, with reduction modulo `sigma^39`, and with the complete target rows. It produces the closed source range

```text
a >= 10,   C-contact c >= a+1,   R-contact r >= a
```

with no displayed leading coefficient inverted or constrained. The finite `C_10` jet is tight and gapless for `0<=m<=10`; at `m=11` the earliest `C`-dependent family is grade 39 and vanishes modulo `sigma^39`. Raising `C` delays every family containing `e_C` copies of `C` by exactly `m*e_C`, leaves pole order and targets unchanged, and sends the first pole-four family `k6*A*C/L^4` from grade 43 to `43+m`. After the specialization the odd-row combination still equals `-sigma^38*J/4`, even targets remain absent, and adjoining `iJ*J-1` is the unit ideal before radicals.

The same closed cone contains every unique-`AC` point

```text
d = c-a in {1,2,3},   s = r-a >= 0,   a+3*s > d
```

with `a>=10`, including all three `d=2` classes and both `d=3` classes. No high-`a` unique-`AC` cell is omitted.

This confirms only the `C>=a+1`, `r>=a` specialization after the same upstream gates. It is not a confirmation of the parent producer, another D1 fan face, a positive-load theorem, a `p=0`/`k0=0` landing, a whole-square / order-two / max-12 theorem, or JC2. Promotion of the corollary remains conditional on the separately live parent producer review.

**CONFIRMED_CONDITIONAL**

---

## Verdict table

| Issue | Charge | Finding |
|---|---|---|
| 0. Two frozen corollary pins | hashes | both match the charged bytes |
| 0. Seven manifest rows | corollary `.sha256` | 7/7 match |
| 0. Parent manifests | `PRODUCER_FREEZE`, `FREEZE`, `EVIDENCE` | 4/4, 13/13, 30/30 |
| 0. Nested compiled / ancestry | both lanes; `a>=13` freeze; high-contact freeze | 3/3 + 3/3; 8/8; 9/9 |
| 1. Polynomial source; only `iJ*J-1` | compiled ring, substitutions, inverses | **holds**; no `A,C,R,theta,eta` leading-coefficient inversion or Bezout/exact-contact equation |
| 2. Specialization (2) commutes | `p(sigma)`, `mod sigma^39`, complete targets; closed vs density | **holds**; conclusion is a closed source-ceiling |
| 3. Finite `C_10` ceiling | each `0<=m<=10`; `m=10` grade-38 `k6*C/L`; `m=11` grade 39 | **holds**; no missing `Chat` jet |
| 4. Primitive movement | `e_C` shift, pole order, targets, `C`-free ceiling, pole-four at `43+m` | **holds**; first omitted lower-grade family under raising: none |
| 5. Odd-row functional after spec | even targets absent; survivor `-sigma^38*J/4`; unit ideal before radicals | **holds** |
| 6. Hostile `m=-5` control | pole-four at grade 38; reject silent extension | **holds as a negative control**; wall is already crossed at `m=-1` |
| 7. Unique-`AC` consequence | all `d=2` and `d=3` classes for every `a>=10` | **holds**; first high-`a` omitted point: none |
| 8. Firewall | only `C>=a+1,r>=a` after the same gates; conditional on parent | **holds** |

---

## 0. Custody

Recomputed SHA-256 of the two charged corollary pins:

| Artifact | SHA-256 | Role |
|---|---|---|
| `xmodel/max12-812-order2-square-d1-age10-cge-a1-source-ceiling-corollary-20260826.md` | `d0ef64c39189df96654df50726fd7b65a958e0eaea67ec2204456b7bc9bf3087` | frozen corollary |
| `xmodel/max12-812-order2-square-d1-age10-cge-a1-source-ceiling-corollary-20260826.sha256` | `7cdf4a707204bcd7fbbbea6e6b21aab687afef11b2a8aad7e33a864b01fac560` | corollary manifest |

The charged review prompt (obeyed with the single delivery-path substitution to this file) rehashes to `295b1386968ee28c3b31095b3a1aa8d1a670e0d1af183e84744fda07bfe079c1`.

All seven corollary-manifest rows match:

| Path | SHA-256 |
|---|---|
| corollary `.md` | `d0ef64c39189df96654df50726fd7b65a958e0eaea67ec2204456b7bc9bf3087` |
| parent `RESULT.md` | `2ccfdfe9c3deb4d32594d2f5e1237684023d5b75438a02229776436c38456883` |
| parent `EVIDENCE.sha256` | `25793913c683a2b6ae914a4ab9b0450cb61c82a8c17b92325ca5292a8183cdef` |
| parent `FREEZE.sha256` | `d5bf43b80b0565b06407b85e6e333381a4015cccbd80a90e6c819fe33a02fa03` |
| parent `AWS_LAUNCH_METADATA.md` | `9720c604276f5f293a3ad91d67beff049c1948f6e4f686ab34335d47262220fc` |
| parent `PRODUCER_FREEZE.sha256` | `60b2e1d52156f0648f7f39739f06c139d7d274c28fe4147eaba8d7213dfc2f0b` |
| parent `compile_age10_j38_r3.py` | `9f0184b1007103478381c474ce6e4c1802ed8a53ab23dee79e7dd77518e0168e` |

`PRODUCER_FREEZE.sha256` is 4/4. Parent `FREEZE.sha256` is 13/13, including the `a>=13` compiler/registration/freeze/result/evidence pins and the high-contact compiler/registration/freeze pins. Parent `EVIDENCE.sha256` is 30/30. Nested `compiled.sha256` is 3/3 on each AWS lane, including the byte-identical inventory

```text
884922fede59bc3540a61aa089a9ed92f65235b269330d63b9195afa69019297
  source_inventory.json
```

on both exact `Q` and `F_65521`. Nested `a>=13` `FREEZE.sha256` is 8/8 and nested high-contact `FREEZE.sha256` is 9/9.

A passing manifest is not a theorem. Engine sentinels, the Chebyshev/Pell heartbeat, and the failed Claude usage-limit log are not used below. Vanishing of the parent polynomial identity (1) is the parent theorem and is not re-proved here; this review decides the specialization.

The parent producer review is a separate lifecycle object. This report does not read, mutate, or import it.

---

## 1. Compiled source is polynomial; only localization is `iJ*J-1`

The exact compiled characteristic-zero script is a polynomial ring

```text
ring R=0,(z,t,sigma,p,ell1..ell10,theta,eta,
          a1..a1_7,a0..a0_7,
          c1..c1_10,c0..c0_10,
          b1..b1_6,b0..b0_6,
          k0..k0_6,k60..k60_10,k20..k20_6,
          mu20..mu20_10,mu4..mu4_6,mu6..mu6_2,
          J,iJ), dp;
```

There is no inverted generator `ia1`, `ic1`, `ib1`, `itheta`, or `ieta`. The only extra inverse-looking variable is `iJ`. The seven source polynomials `R3_SourcePhi1..7` are assigned in this ring. The substitutions are polynomial:

```text
A = sigma^10 * theta * ((a1+...+sigma^7 a1_7) + t (a0+...+sigma^7 a0_7)),
C = sigma^11 * theta * ((c1+...+sigma^10 c1_10) + t (c0+...+sigma^10 c0_10)),
R = sigma^10 * theta * eta * ((b1+...+sigma^6 b1_6) + t (b0+...+sigma^6 b0_6)),
p(sigma) = p + 2 sigma ell1 + ... + 2 sigma^{10} ell10.
```

No displayed leading coefficient of `A`, `C`, `R`, `theta`, or `eta` is required to be a unit. The compiled file contains no `/theta`, `/eta`, `/a1`, `/c1`, `/b1`, `/a0`, `/c0`, `/b0`, no Bezout equation, and no exact-contact constraint `a1*ia1-1` or analogue.

The only series inversion is the truncated geometric inverse of the moving connection `R3_sp = p(sigma)/2`,

```text
R3_Invq = sum_{0<=k<=4} (-1)^k binom(q+k-1,k) (R3_sp)^k t^{2k},
```

which is a polynomial in `(p,ell1..ell10,t)`. The independent Laurent emitter `R3_H` begins

```text
(3/4)*sigma^17*(k60+...+sigma^{10} k60_10)
  *(sigma^11*theta*((c1+...+sigma^{10} c1_10)+t*(c0+...)))
  *t^2*R3_Inv1 + ...
```

which is polynomial in every listed jet.

The grade-38 relation is the polynomial

```text
R3_source_rel
  = SourcePhi7 + (p(sigma)/4) SourcePhi5
               + (3 p(sigma)^2/32) SourcePhi3
               + (5 p(sigma)^3/128) SourcePhi1,
R3_rel
  = FullPhi7 + (p(sigma)/4) FullPhi5
             + (3 p(sigma)^2/32) FullPhi3
             + (5 p(sigma)^3/128) FullPhi1.
```

The endpoint localization is exactly

```text
ideal R3_unit = std(ideal(subst(R3_q38, sigma, 0), iJ*J-1)).
```

That is the only inversion used for emptiness on `D(J)`. The parent identity being the zero polynomial in these jets is the parent theorem; polynomiality of the compiled relation in every `Abar,Cbar,Rbar,theta,eta` jet is visible from the ring and the substitutions, independently of any engine PASS.

---

## 2. Specialization commutes and is a closed source-ceiling

Write the parent identity, as a polynomial in the compiled jets, as

```text
Phi7 + (p(sigma)/4) Phi5 + (3 p(sigma)^2/32) Phi3 + (5 p(sigma)^3/128) Phi1
  = -sigma^38 * J/4     mod sigma^39.                         (1)
```

The three substitutions

```text
theta = sigma^n,   eta = sigma^s,   Cbar = sigma^m * Chat,    n,m,s >= 0     (2)
```

are ring maps of the polynomial ring in §1. They do not touch `p(sigma)`. Faber-transform coefficients are polynomials in `p(sigma)` with even row-steps, so (2) commutes with the moving connection and with the lower-unitriangular odd-row Faber matrix. Reduction modulo `sigma^39` is quotienting by an ideal preserved by (2) for every `n>=0`. Target rows are independent of `A,C,R,theta,eta`:

```text
row 2:  -sigma^{28} (mu20 + ... + sigma^{10} mu20_10),
row 4:  -sigma^{32} (mu4  + ... + sigma^6  mu4_6),
row 6:  -sigma^{36} (mu6  + ... + sigma^2  mu6_2),
row 7:  -sigma^{38} (J/4).
```

Compiled `FullPhi2` ends with the displayed `mu20` jet and `FullPhi7` ends with `-sigma^38*(J/4)`. Specialization does not mix rows and does not alter those subtractions.

The resulting source conditions are the closed divisibilities

```text
A in sigma^{10+n}     k[[sigma]][z]_{<=1},
C in sigma^{11+n+m}   k[[sigma]][z]_{<=1},
R in sigma^{10+n+s}   k[[sigma]][z]_{<=1},
```

i.e. `a>=10`, `c>=a+1`, `r>=a` after renaming `a=10+n`. This is evaluation of a polynomial identity at a closed face, not Zariski density of the exact-contact open `ord(A)=a`, `ord(C)=a+1`, `ord(R)=r`. No leading coefficient of `Chat`, `Abar`, or `Rbar` is inverted.

---

## 3. Finite `C_10` ceiling

Independent four-summand binomial expansion at baseline `A=10,C=11,R=10` recovers exactly the eleven compiled primitives through grade 38. The unique earliest `C`-dependent family is `k6*C/L` at grade 28, coefficient `3/4`, pole 1, remaining budget `38-28=10`. That is the mechanical origin of `C_10`. The compiled inventory contains both `c1_10` and `c0_10` among analytic monomials, and the literal Faber source at grade 38 still contains the family-0 terms

```text
(3/4) c1_10 k60          on row 1,
(3/4) c0_10 k60          on row 2,
```

together with the three odd-row connection corrections of `c1_10 k60 p^*`. There is no `c1_11` in the ring.

For each shift `m`, family `k6*C/L` moves to grade `28+m` and the remaining `Chat` budget is `10-m`. Parent coefficients `C_m,...,C_10` relabel as `Chat_0,...,Chat_{10-m}`. The table is exact:

| `m` | `k6*C/L` grade | remaining | max available `Chat` index | max needed | grade-38 `k6*C/L` possible | vanishes `mod sigma^39` |
|---:|---:|---:|---:|---:|---|---|
| 0 | 28 | 10 | 10 | 10 | yes | no |
| 1 | 29 | 9 | 9 | 9 | yes | no |
| 2 | 30 | 8 | 8 | 8 | yes | no |
| 3 | 31 | 7 | 7 | 7 | yes | no |
| 4 | 32 | 6 | 6 | 6 | yes | no |
| 5 | 33 | 5 | 5 | 5 | yes | no |
| 6 | 34 | 4 | 4 | 4 | yes | no |
| 7 | 35 | 3 | 3 | 3 | yes | no |
| 8 | 36 | 2 | 2 | 2 | yes | no |
| 9 | 37 | 1 | 1 | 1 | yes | no |
| 10 | 38 | 0 | 0 | 0 | yes, leading `Chat_0 = C_10` | no |
| 11 | 39 | -1 | — | — | no | yes |
| 12 | 40 | -2 | — | — | no | yes |

At `m=10` the possible grade-38 `k6*C/L` coefficient is still present: it is the compiled `C_10` jet already displayed. At `m=11` the family is grade 39, hence zero modulo `sigma^39`. Every other `C`-dependent primitive through grade 38 starts later, so its shifted grade is at least `31+11=42` or, for `e_C=2`, `32+22=54`. No omitted `Chat_j` with `j>=0` can enter the truncated identity.

The same comparison for every `C`-dependent family through grade 38 and every `0<=m<=10` finds no missing `Chat` jet: if `e_C>=1` then the needed index is `38 - (base + m e_C)`, which is at most the available index `10-m`.

---

## 4. Primitive movement, `C`-free ceiling, pole-four

The four atoms of `X` in `f=L^4(1+X)` are

```text
2R/L^2,   R^2/L^4,   A/L^3,   C/L^4
```

with binomial summands unloaded / `k10` / `k6` / `k2`. A monomial with `e_C` copies of the `C` atom and no other change has grade shifted by exactly `m e_C` under `C |-> sigma^m C`, and the same pole order, because pole is computed from atom denominators and load `alpha`, neither of which involves the contact of `C`. `C`-free monomials are invariant. Inverse-connection factors and Faber transforms are polynomials in `p(sigma)` and do not create new `C` families or move targets.

The seven `C`-dependent primitives through grade 38, together with the first pole-four family, move as follows.

| family | `e_C` | pole | base | `m=0` | `m=10` | `m=11` | `m=-5` |
|---|---:|---:|---:|---:|---:|---:|---:|
| `k6*C/L` | 1 | 1 | 28 | 28 | 38 | 39 | 23 |
| `A*C/L` | 1 | 1 | 31 | 31 | 41 | 42 | 26 |
| `k10*R*C/L` | 1 | 1 | 32 | 32 | 42 | 43 | 27 |
| `C^2/L^2` | 2 | 2 | 32 | 32 | 52 | 54 | 22 |
| `k10*A*C/L^2` | 1 | 2 | 35 | 35 | 45 | 46 | 30 |
| `k10*C^2/L^3` | 2 | 3 | 36 | 36 | 56 | 58 | 26 |
| `k2*C/L^3` | 1 | 3 | 36 | 36 | 46 | 47 | 31 |
| `k6*A*C/L^4` | 1 | 4 | 43 | 43 | 53 | 54 | 38 |

Raising `C` (`m>=0`) therefore cannot reveal a previously excluded lower-grade family, cannot change a pole order, and cannot move a target. The four `C`-free primitives through grade 38 are

```text
k2*R/L       grade 32, pole 1,
k10*A^2/L    grade 34, pole 1,
k2*A/L^2     grade 35, pole 2,
k6*R^2/L     grade 36, pole 1.
```

Their maximum pole is two, inside the parent's pole-three ceiling. Expanding the same binomial census through grade 80, the first pole-order-four family is uniquely `k6*A*C/L^4` at grade 43, coefficient `-3/16`. (Degree-two `k6` term on the `A` and `C` atoms: `binom(3/4,2)*2 = -3/16`, grade `12+5+5+10+11=43`.) After (2) it sits at grade `43+m>=43`. Consequently every source term through grade 38 still has pole order at most three, and the order-three Laurent-to-ordinary functional of §5 still applies.

Product ranges `range(6)` versus `range(8)` in the atom multi-index produce identical primitives through grade 60, so the census is not an artificial truncation.

---

## 5. Odd-row functional, even targets, unit ideal

Put `s=p/2` and `Phi(x)=sum_m Phi_{2m+1} x^m`. The binomial series is

```text
(1 - s x)^{-1/2}
  = 1 + (p/4) x + (3 p^2/32) x^2 + (5 p^3/128) x^3 + O(x^4).
```

A pole-`q` basis element `x^e/(1+s x)^q` with `0<=e<q` becomes `x^e (1-s x)^{q-e-1/2}` under the frozen Faber substitution. Multiplication by a further `(1-s x)^{-1/2}` yields `x^e (1-s x)^{q-e-1}`, a polynomial of degree `q-1`. For pole ceiling three this degree is at most two, so the `x^3` moment against `(1-s x)^{-1/2}` vanishes. That moment is exactly

```text
Phi7 + (p/4) Phi5 + (3 p^2/32) Phi3 + (5 p^3/128) Phi1.
```

The identity is formal in the moving series `p(sigma)` and uses the same `p(sigma)` as the compiled Faber transform. After (2) the pole ceiling through grade 38 remains three (§4), so the identity still kills every non-target source term modulo `sigma^39`.

Even targets live only on even rows 2, 4, 6. Faber coefficients vanish unless `row - source_row` is even and nonnegative, so the odd combination never sees `mu2`, `mu4`, or `mu6`, before or after (2). The sole odd target is `-sigma^38*(J/4)` on row seven, and the coefficient of `Phi7` in the combination is 1. The full combination is therefore exactly `-sigma^38*J/4` modulo `sigma^39`, independently of every `A,C,R,theta,eta` jet. Adjoining `iJ*J-1` gives

```text
(-J/4, iJ*J - 1)  =  (1)
```

before radicals, because `(-4 iJ)*(-J/4) = iJ*J = 1`. That is scheme-theoretic emptiness on `D(J)`, not a radical membership.

---

## 6. Hostile boundary control `m=-5`

The claim is only `m>=0`. Lowering `C` is a negative control against silent extension across the source-ceiling wall.

At shift `m=-5` the displayed pole-four family `k6*A*C/L^4` reaches grade `43-5=38`, coefficient `-3/16`. An independent re-expansion at baseline `C=6` confirms that family at grade 38, and also shows that pole-order `>=4` terms already exist earlier at this illegal contact (`C^3` at grade 33, `k6 C^2` and `R C^2` at grade 34). The charged pole-four wall is therefore real, and is not the first failure.

The first illegal lowering is already `m=-1`: `k6*C/L` moves from grade 28 to grade 27, which is a new lower-grade `C` family whose leading jet is not among the compiled `Cbar` coefficients (those begin at order 11). The parent `C_10` ceiling would also be insufficient (remaining budget 11). The corollary correctly refuses every `c < a+1`. This review rejects any silent extension across that wall.

---

## 7. Unique-`AC` consequence

The normalized integral unit-load unique-`AC` domain is the charged set

```text
d = c-a in {1,2,3},   s = r-a >= 0,   a + 3 s > d.
```

The closed source ceiling is `a>=10`, `c>=a+1`, `r>=a`. Every unique-`AC` point with `a>=10` satisfies `c=a+d >= a+1` and `r=a+s >= a`. The strict inequality `a+3s>d` is automatic once `a>=10`, since `d<=3`. Direct census of all `a>=10`, `d in {1,2,3}`, `s>=0` finds no point of the unique-`AC` domain outside the cone.

In particular, for every `a>=10` the corollary contains

```text
d=2, s=0:    (a, a+2, a),
d=2, s=1:    (a, a+2, a+1),
d=2, s>=2:   (a, a+2, a+s),
d=3, s=0:    (a, a+3, a),
d=3, s>=1:   (a, a+3, a+s).
```

The first high-`a` omitted unique-`AC` point does not exist. What remains of those families is the finite threshold range `a<=9` (with the still-parametric `s` tails), which is outside the claim. The first omitted unique-`AC` cell at all is at `a=9`, for example `(a,c,r)=(9,11,9)` in the `d=2,s=0` class; it is not a failure of the corollary.

The `d=1` slice `c=a+1` is the parent's exact-contact cone, included here as the `m=0` face. The corollary's genuine new coverage is the raised-`C` unique-`AC` cells `d=2,3`.

---

## 8. Firewall

This review confirms only the specialization

```text
C-contact c >= a+1,   R-contact r >= a,   a >= 10
```

of the frozen grade-38 source identity, after the same registered upstream square/D1 gates, and only conditionally on independent confirmation of the parent producer. It does not:

- confirm or promote the parent `a>=10,c=a+1,r>=a` theorem;
- decide any other D1 fan face (unique `RC`, tied first face, exception `(a,c,r)=(1,4,2)`, `a<=9`);
- claim a positive-order leading `k10` theorem;
- land `p=0` or `k0=0`;
- close zero/infinity, exact-square-zero, terminal/Taylor, or global charts;
- close D1, the whole square component, order two, `(8,12)`, maximum twelve, or JC2.

The Chebyshev/Pell identity in the parent compiler is a nonempty engine heartbeat and is not an input to the specialization.

---

## Failed first delivery

The Claude/Fable attempt at

```text
xmodel/max12-812-order2-square-d1-age10-cge-a1-source-ceiling-corollary-hostile-review-claude-20260826.md
```

does not exist. Its log records only an API usage-limit error and contains no mathematical work and no verdict. That attempt is preserved unchanged and is not evidence for or against the corollary.

---

CONFIRMED_CONDITIONAL
