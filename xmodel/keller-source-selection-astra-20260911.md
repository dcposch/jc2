# Actual-source first-order selection: bounded attempt

Owner: /root/contact_collision_geometry (Astra).
Basis: 0d39df3c9fd69c939a8420c54d03228b9077777d.
Actual first action: 2026-09-11 22:24:12 UTC. Original reserve22:42/HARD22:45 unchanged.

Verdict: NO_NEW_SOURCE_MECHANISM. The actual-source slice identities below obstruct the attempted minimal-degree descent; they do not refute existence of a line or a Keller map. Exactly TASK and its pinned completed FIRST report were read fresh WHOLE. Finite E-dimension is stipulated independently of the live rank review. No other report, external source, computation, worker, or descendant is used.

## 1. Minimal relation and genuine source operations

Use the stipulated actual inclusion A=C[p,q]⊂R=C[x,y], J(p,q)∈C*, N=R/A, B=C[e_p,e_q], E=Frac(B), δ=p∂q, and V=E⊗_B N. The Ore rule is δb=σ(b)δ with σ(e_p)=e_p−1 and σ(e_q)=e_q+1. Bass gives B-torsion-freeness, so N↪V. Assume N≠0. Finite E-dimension supplies a relation for every nonzero m∈N. Choose m with smallest positive δ-degree s among all nonzero source classes; normalize its minimal relation over E as

    Pm=0,    P=δ^s+Σ_(j<s) a_j(e_p,e_q)δ^j,    a_j∈E.

Its cyclic space has dimension s. Clearing left denominators gives a U-relation on N. The accepted finite-homogeneous-prefix argument allows an exact relation on a representative in R after a polynomial subtraction. This lifts the CLEARED U-operator, not an assertion that rational Euler operators act on R itself. No lifting gap is imported from the earlier, uncharged rank report.

Now use genuine lifted derivatives and their slices. Each of p,q,∂p,∂q is an invertible semilinear map on V: ∂p p=e_p+1, p∂p=e_p, and similarly for q. These Euler factors are nonzero elements of E. Their shifts of (e_p,e_q) are respectively (−1,0),(0,−1),(+1,0),(0,+1). From δ=p∂q and [∂p,p]=[∂q,q]=1, for every j≥0,

    δ^j p=pδ^j,                 δ^j∂q=∂qδ^j,
    δ^j∂p=((e_p−j+1)/(e_p+1)) ∂pδ^j,
    δ^j q=((e_q+j)/e_q) qδ^j.

For example ∂pδ^j=(e_p+1)p^(j−1)∂q^j and δ^j∂p=(e_p−j+1)p^(j−1)∂q^j for j≥1. Every displayed rational multiplier is nonzero. Therefore for D equal to ANY of these four source operations, the spans of δ^j(Dm) are obtained from D applied to the spans of δ^jm by nonzero scalar rescaling. Semilinear invertibility preserves dimension:

    dim_E E[δ]Dm = dim_E E[δ]m = s.

Thus none of these actual slice/derivative operations can reduce minimal cyclic degree. This is stronger than merely observing that differentiating an equation creates extra terms. Linear combinations or nonlinear source products are not covered by this equality.

## 2. Attempted product descent and its first missing implication

Multiplication by p gives a literal lower-degree operator

    C_p := p⁻¹Pp−P
         = Σ_(j<s)(a_j(e_p+1,e_q)−a_j(e_p,e_q))δ^j.

But the exact source identity is

    C_p m = p⁻¹P(pm),

NOT C_p m=0. Minimality would force C_p=0 if this last value vanished. It does not imply P(pm)=0: pm has a shifted degree-s minimal annihilator, not necessarily the SAME P. The first unproved implication of this descent is therefore same-kernel stability under p for the chosen minimal relation. The following full polynomial-source control only tests that implication without minimality: on the identity Keller source, P₀=δ²+e_p and f=q satisfy P₀f=0, but P₀(pf)=pq≠0, and p⁻¹[P₀,p]f=q. Here [f]=0 in N, so this is not a counterexample to the actual nonzero-N statement.

The seemingly favorable q-commutator does not even lower degree after correct localization. The raw formula [δ^s,q]=s pδ^(s−1) contains p outside E[δ;σ]. Since p/q=δe_q⁻¹, conversion back restores the lost degree. Precisely,

    q⁻¹Pq = Σ_(j≤s) a_j(e_p,e_q+1)
                         ((e_q+j+1)/(e_q+1))δ^j,   a_s=1.

Consequently q⁻¹[P,q] has degree s and nonzero leading coefficient s/(e_q+1). Treating p or q as ordinary E-coefficients gives a false drop.

Commutative multiplication in R also cannot be applied directly to source classes. A is not an ideal of R when N≠0: [1]=0 but, for h∈R\A, [h·1]≠0. In particular [f]² and arbitrary h[f] are not well-defined on N. On exact representatives, for a cleared operator Q=Σ b_j(e_p,e_q)δ^j with Qf=0,

    Q(hf)=Σ_j b_j[δ^j,h]f+Σ_j[b_j,h]δ^j f,
    [δ^j,h]=Σ_(i=1)^j binom(j,i)(δ^i h)δ^(j−i).

The coefficients δ^i h lie in R, not generally in B or E, and the second sum need not lower δ-degree. Neither minimality nor the prefix subtraction makes this expression vanish. This is the exact multiplication obstruction, not a claim that nonlinear source structure can never help.

A separate factorization check is sound but does not repair it: R∩C(p,q)=A. Indeed write a/b∈R with a,b∈A coprime. If an irreducible π divides b, π(p,q) is a nonconstant nonunit of the polynomial UFD R, so choose a height-one prime P above a factor. Étale flatness and going-down give ht(P∩A)≤1; since it contains π, P∩A=(π). But a=b(a/b)∈P, contradicting coprimality. Thus b is constant. This uses actual polynomial units/factorization; it fails, for example, for (p,q)=(x,xy), where J=x is not a unit and y=q/p∉A. However E is a field of EULER OPERATORS, not C(p,q). An E-line in V or its rational coefficient ratios are not rational source functions. The intersection result cannot turn P into a first-order operator.

## 3. Verdict and exact scope

NO_NEW_SOURCE_MECHANISM. The bounded attack supplies exact failures of the proposed degree-lowering moves, not a proof or refutation of the actual-source line statement. In particular the polynomial control above does not satisfy nonzero N or minimality. No arbitrary Ore-module example is substituted for a source example.

The missing vanishing for this particular route is P(pm)=0 for an appropriate globally minimal source pair (P,m), or another source identity making C_p m=0 with C_p≠0. The displayed identities establish neither. If C_p vanishes as an operator instead, that alone supplies no smaller annihilator. The source factorization argument only excludes rational source functions outside A, and says nothing about a rational Euler-operator line. These are the first precise failed implications, rather than a restatement of JC2 as a theorem.

A δ-stable E-line would give some first-order relation after clearing denominators, but not automatically the selected polynomial/resonant form e_p−r+δG(e_p,e_q), r≥0. The charged accepted FIRST report establishes injectivity of that selected family on actual N at its explicit Bass/analytic tier. It supplies neither line selection nor the polynomial-G normalization. For a selected U-relation, homogeneous-prefix lifting IS already available, as stipulated and checked in the charged gate; no extra representative-lifting hypothesis is left open here. There is consequently no new composition to source zero, properness, or JC2.

OPEN quantity: the source-specific vanishing/lower relation just identified; even success must still provide an E-line and the separate resonant normalization. Cheapest test: a manual identity using actual polynomial-source multiplication that both stays in E[δ;σ] and annihilates the chosen class, rather than the nonzero commutator values above; planning wall 900 seconds UNMEASURED, no follow-on authorization. No finite-degree experiment, new operator family, group-action integration, or slice-surjectivity assertion is proposed.

Read/collision scope: exactly two current pinned inputs, each fresh WHOLE; completed gate used only at TASK's accepted scope, not its obsolete opening-skeleton prose. No live report or source-history search, so no novelty certification. Owner-only final/manifest/box checks; no shared or protected access. All work was manual reasoning, inert text/hash/date, apply_patch, and the existing documentary transaction. No scientific process, primary lookup, coefficient artifact, worker, or child. Current pins, own whole readbacks, transaction status and terminal times are in the owned PINS/custody.

Preseal completion: final substantive partial and PINS read WHOLE; both input hashes unchanged and own final/manifest destinations absent at observed 2026-09-11 22:32:28 UTC. No placeholder or premature marker; the sole OPEN has its exact quantity and cheapest test above. Scientific work is now closed, with no line or resonant selection proved. Only transaction/readback/custody remain under the original clock.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `8577`.
- Body SHA-256:
  `0fd4ba189aac84b7006bad74a3746b1a2e07f6130791451b732a8021f6fe3ddc`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
