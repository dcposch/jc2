# Promotion: D1 `a=8,d=3` moving-connection rank jump

Date: 2026-08-26

Status: **PROMOTED ROUTE FALSIFICATION ONLY.**

## Frozen authority

```text
a28e20033d4e0d5cd8fd3303a371d802147db65875cace445904a48f9f4fc83e
  cases/max12_812_order2_square_owner_d1_a8d3_connection_rankjump_20260826/PRODUCER_FREEZE.sha256
2c422f36cecec94e870c1bab4767d3dc61b689f100fc583ad3509a2a176f90bc
  cases/max12_812_order2_square_owner_d1_a8d3_connection_rankjump_20260826/RESULT.md
75145f27deb4cec1ba99401551a9cd8fd68a140603ae621b583ff2a3efd3de6b
  xmodel/max12-812-order2-square-d1-a8d3-moving-connection-rankjump-hostile-review-grok-v2-20260826.md
```

The independent hostile reviewer rehashed every producer and diagnostic pin,
checked exact Q plus both fresh prime controls, independently derived the
inverse-root correction, extracted the `k6A^2` family from the complete
inventory, and returned `CONFIRMED`.  The first Claude review attempt ended
before analysis because of a provider limit and is not evidence.

## Promoted negative statement

For the independent source monomials `k60*a0*a1` and `k60*b0*c1`, the
corrected literal odd-row columns on rows `(Phi5,Phi7)` are

```text
k6*A^2 : (-3/16, +3*p/64),
k6*R*C : (-3/16, -3*p/64).
```

Hence a normalized functional

```text
Phi7 + alpha*p*Phi5 + beta*p^2*Phi3 + gamma*p^3*Phi1
```

would require both `alpha=+1/4` and `alpha=-1/4`.  The two-column determinant
is `9*p/512`, a unit on `D(p)`, and

```text
e7 = (32/(3*p))*col(k6*A^2) - (32/(3*p))*col(k6*R*C).
```

Thus no single coefficientwise universal odd functional annihilates the
complete moving-connection source block in this chamber on `D(p)`.

The corrected connection identity used here is

```text
q_RC + (partial_z q_C)*z_R = -(3/16)*R*C'/(z*L^2).
```

It proves the canonical literal `k6RC` tails and falsifies only the earlier
fixed-`z` analytic bridge.  No Singular `qring` semantics enter.

## Firewall

This is a promoted impossibility for one proof route, not an existence or
emptiness statement for the `a=8,d=3` cell.  It does not assert that either
monomial is freely variable on the earlier source scheme, construct a point,
or decide any other D1 chamber, the square component, maximum twelve, or JC2.
The separate target-shadow chamber producer/review is the cell-level route.
