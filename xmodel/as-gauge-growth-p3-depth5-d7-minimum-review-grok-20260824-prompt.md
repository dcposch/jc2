# Hostile different-model review — AS `p=3`, depth-five exact cap-seven minimum

You are the independent different-model reviewer. Work in
`/Users/dc/code/math/jc2` at committed basis
`c327bdc8d02472feba42573760325099f34b8cdf`, with the named frozen producer
artifacts uncommitted on top. Read in full:

- `xmodel/as-gauge-growth-p3-depth5-d7-minimum-20260824.md`;
- every payload under
  `cases/as_gauge_growth_p3_depth5_d7_minimum_20260824/`;
- `xmodel/as-gauge-growth-p3-depth4-gate-20260824.md` and its hostile review;
- the reviewed completed-gauge and bounded-polar reports defining this system.

Frozen hashes:

- report (both copies):
  `618a954ea683fd8444dafc4511c9aed708ea88459c524d747b815d48c6618f65`;
- Python replay:
  `f7a877784fee1bcf2a1cdb49873d959001ea4a08a7639127612fdefd8f13d295`;
- Singular replay:
  `7e0caabd513f907199489ae7c1181356e1584eede9da65a7b40e7dc96cc6beb1`;
- manifest:
  `42d3917672e1eb24ea390cbb24b57d40d5e1a933300ed4f2d3047910ea8bfefc`;
- freeze record:
  `fac6c139ffdcb373d983cd74bc96a0293378d2ccffdc66d4e1c5fa0884222b83`;
- canonical replay payload:
  `6e161a0cde8387b5853dc6756ef13e44c60785f166bcdd8658a086023090924b`.

Verify every hash and rerun both registered engines from the case directory.
Those are regression evidence only. Then reconstruct the claims with a third
exact sparse-polynomial implementation or a full independent derivation:

1. Reconstruct the identity-branch system modulo 243:
   `P=A-A^3`, `Q=B*S5(A)`,
   `S5=1+3A^2+9A^4+27A^6+81A^8`, with determinant-one polynomial gauge
   `(A,B)` and simultaneous total-degree simplex cap seven on `A,B,P,Q`.
   Attack orientation, carry, rectangle/simplex, and map/gauge-cap mistakes.
2. Transcribe the eight displayed base-3 digit polynomials `b,c,d,e,f,g,h`
   independently, build the integer representatives `A,B`, and recompute
   `P,Q` coefficientwise modulo 243 without importing producer support tables.
   Verify all four exact total degrees are seven and the special fibre is
   `(x-x^3,y)`.
3. Recompute directly, not merely by chain rule,
   `det J(A,B)=det J(P,Q)=1 mod 243`; also verify
   `(1-3A^2)S5(A)=1 mod 243`, `B=Q(1-3A^2)`, and every cap cancellation.
4. Verify membership in `B_(3,5)(7,7)`. Then audit the lower bound carefully:
   reduction modulo 81 of any depth-five cap-at-most-six point would be a
   depth-four cap-at-most-six point in exactly the already reviewed system.
   Confirm that the cited depth-four exhaustive result really excludes that
   set. Decide whether this proves the exact minimum `D_min(3,5)=7`.
5. Audit the consequence for the previously proposed law
   `(n-1)(p-1)+1`: the reviewed minima at depths two through four are
   `3,5,7`, while this point gives depth five minimum seven, so the law is
   falsified rather than merely unproved.
6. Attack every inference to depth six, liftability of this residue class,
   an inverse-limit/polynomial/Tate lift, `A_infinity`, deck descent, or JC2.
   The producer licenses none of them. Do not import any newer unreviewed
   depth-six Cartier calculation into the minimum verdict.

Use exact arithmetic. A producer replay plus prose comparison is not
independent evidence. Put scratch work outside tracked paths. Do not edit any
producer, case, canonical, notes, prompt, log, run, or erratum file, and do not
launch AWS. Write exactly one report:

`xmodel/as-gauge-growth-p3-depth5-d7-minimum-review-grok-20260824.md`

Give `CONFIRMED`, `REFUTED`, or `GAP` overall and per numbered claim. Include
hashes, independent derivations, the smallest failing identity if any,
precise promotion scope, and quarantine language at both ends.
