# Hostile review — Q8 leaf-4 quotient and terminal descent

Act as a hostile different-model mathematical reviewer in
`/Users/dc/code/math/jc2`. Read in full:

- `xmodel/max12-912-order3-nu-q8-leaf4-descent-jet-20260824.md`;
- every file in `cases/max12_912_order3_nu_q8_leaf4_descent_jet_20260824/`;
- only the frozen parents pinned by that manifest.

Charged hashes:

```text
report    b835aa0370b69e508ed776e1c9e0008f1186c751e537b63506619a7c43e95738
manifest  0b9b3f7176e337db4d4068b06d96bca60279889561dc654748a4bcfbe8e058d6
freeze    700ddb29d01de5075de3f5a4f7cc2df00f8b35aa6a59803e778ee3c87edb7de2
```

Run the replay, but independently reconstruct and attack:

1. Kummer descent and weight zero of `theta=a0^2/p^9`, `pi=p^9`,
   `q=r8/p^10`, and `S=r8^9=pi^10*q^9`;
2. the negative control proving `p^9*R6(v)=nu` is parity-only and fails by a
   unit times `t^2` on the second branch;
3. the root-free formulas for `tau` and `Delta`, including denominators and
   every unit claim in their `theta` jets at all eight Q8 contacts;
4. whether `Delta` is genuinely an etale coordinate on the involution
   quotient and whether `dS/dDelta` is a unit;
5. provenance and algebra of `9*r8'=j/u`, `r8=u^2R`, `u^3=h`, and the
   necessary identity `h^3*(S')^9=j^9*S^8`;
6. all places where a formal coefficient-fibre identity might be confused
   with a trajectory, converse, algebraization, Taylor constraint, or global
   quotient relation.

Specifically search for any illicit off-parity use of `r6=p^9R6(v)`, any
chosen-root ambiguity, hidden vanishing denominator, missing ninth-root
descent, or overclaim from local etaleness. State the smallest false identity
or missing hypothesis if found.

Write exactly
`xmodel/max12-912-order3-nu-q8-leaf4-descent-jet-review-grok-20260824.md`.
Do not edit producer, case, canonical, coordination, prompt, log, run, or any
other review file. End with exactly one verdict: `CONFIRMED`, `GAP`, or
`REFUTED`.
