# Event-triggered whole-portfolio scan — fresh-connection lane

Date: 2026-08-24T11:03Z  
Charged bank: `51aa1cc210b20d6c57c5bb0ba3b4a6dfa5fc8f54`  
Status: **FROZEN IDEATION / THREE BOUNDED RECOMMENDATIONS / NO LAUNCH**

## 0. Executive verdict

The new evidence does not justify a third backbone.  It sharpens the two
existing ones and, for the first time, gives three finite objects on which
previously vague avenues can be tested without pretending to solve their
global typing problems.

Ranked by chance-adjusted information per local cost:

| Rank | Card | Bounded first experiment | Local information / cost |
|---:|---|---|---|
| 1 | `TD6-TRANSPORT-COVARIANT` | Compute the moduli-only elimination ideal of the **next paired band** on the exact codimension-one survivor, using transport syzygies before solving its 56 free coefficients | Very high / 6--12 agent-hours |
| 2 | `SEXTIC-46-ONE-PLACE` | Normalize every branch of the exact `(4,6)` boundary cover at its unique weighted infinity point and compare the value semigroup of both boundary functions with the pulled-back constant-row form | High / 10--20 agent-hours |
| 3 | `AS109-TROPICAL-GROUPOID` | Decide whether the sextic `(4,6)/(5,6)` AS109 negative-face data admit a finite gauge-invariant symbolic motif grammar; do not enumerate coefficients unless finiteness is proved | High stop value, medium success chance / 8--16 agent-hours |

Cards 1 and 2 are proof/disproof-frontier calculations on exact named
objects.  Card 3 is enabling software with a theorem-valued specification
gate.  They can run independently, but this report launches none of them.

## 1. Evidence consumed and trust firewall

The full 46-row `APPROACHES.md` inventory, `PROGRESS.md`, the newest
`notes.md` live state, and the latest full-round synthesis were reread.  The
only mathematical deltas admitted into this scan are:

1. the different-model-confirmed characteristic-zero theorem that both
   coordinate `y`-degrees at most five imply automorphy, together with the
   still-provisional sextic frontier `(4,6)` and `(5,6)`;
2. the reviewed conditional AS109 degree bound `d>=109` and the provisional
   exact identification of `d-109` with total negative-valued tropical
   multiplicity;
3. the confirmed TD6 first-band/nonuniform-next-row results and the
   provisional uniform formula

   ```text
   [s^-1 t^13]J = (6/5)(5E2-2E1^2),
   ```

   whose source-open complex zero locus supports an exact 56-dimensional
   paired-next-row family;
4. the reviewed secant projector theorem and its reviewed projective and
   AS109 `COSTUME` children.

During this scan a further provisional `(4,6)` child landed.  It reduces the
full coefficient equations and both polynomial boundary equations to an
explicit curve/one-form/boundary-cover object with one weighted point at
infinity.  It is used provisionally and nonblockingly in Card 2; no review
verdict is presumed.

No literature-priority claim, web search, AWS result, exact lift, terminal
TD6 realization, or JC2 implication is inferred.

## 2. All-46 disposition

Every row was reconsidered.  The compact disposition below accounts for all
46; “raise” means only that the new evidence supplies a named client, not
that the top-level approach has become a theorem.

* **Raised/redesigned by a concrete client:** `2,8,19,21,29,31,38,45,46`.
  These are respectively TD6 realization, moment/formal-inverse machinery,
  AS lifting, Hensel degree, algebraic foliations, vertical finiteness,
  tropical initial ideals, differential algebra, and certificate software.
* **Adjacent, but not an independent vote:**
  `3,5,6,7,16,25,26,28,32,34,36,43,44`.  Their useful pieces are absorbed
  below.  In particular, secants remain an accelerator, prime-degree claims
  remain unvetted, and topology/monodromy still need a source-derived global
  object.
* **No rank-changing delta:**
  `1,4,9,10,11,12,13,14,15,17,18,20,22,23,24,27,30,33,35,37,39,40,41,42`.
  Existing stops and holds remain.  This scan does not reopen HC4, weighted
  D, unrestricted Witt levels, generic sparse search, ordinary action
  residues, or new book cells.

The apparent revival of tropical and one-place methods is deliberately
client-specific.  It does not erase their previous global objections.

## 3. Card 1 — `TD6-TRANSPORT-COVARIANT`

### New connection

Let the roots of the reduced F1 orbit polynomial be the multiset

```text
rho=(C,C,U,V),       e1=sum rho_i,       p2=sum rho_i^2.
```

The new obstruction is

```text
5E2-2E1^2 = (e1^2-5p2)/2.                              (3.1)
```

Thus the first parameter-free obstruction is a quadratic moment relation of
the F1 orbit multiset.  More importantly, it arose as a constant row after
eliminating thousands of global transport equations.  This suggests a
general object that the current append-a-band workflow does not expose:

```text
the module of transport-covariant left syzygies of the Jacobian rows.
```

This connects the TD6 global transport compiler to rows 8/46
(coefficient/moment identities and exact software), without claiming a
formal-inverse theorem.

### Exact first experiment

Work only with the normalized SP-2 chart-pattern family already certified on
the source-open locus

```text
Q=5E2-2E1^2=0,
C U V (C-U)(C-V)(U-V) != 0,
L^8 A^3=9.                                               (3.2)
```

Use the exact affine transport parameterization, quotient by the 36 x-side
and two pole-side next rows, and retain its 56 free directions.  For exactly
one further paired Jacobian band:

1. compile the residual polynomial map in the 56 directions over the
   function field of (3.2);
2. compute its transport-syzygy/left-null module before attempting a full
   solve;
3. eliminate the free directions to the symmetric moduli ring;
4. saturate by the source-open product in (3.2); and
5. express every surviving moduli-only equation in `e1,e2,e3,e4` or power
   sums, so coordinate artifacts are visible.

The finite output is one of:

* `NORMALIZED-SP2-KILL`: the saturated ideal with `Q` is the unit ideal;
* `NEW-COVARIANT`: a second nonzero moduli invariant remains, with its exact
  intersection with `Q`;
* `ADJUSTABLE`: the elimination ideal is zero and an exact solution/dimension
  certificate shows that the whole band can be absorbed by the 56 directions.

### Why this is not the existing next-row instruction

The existing report says to impose the following band.  This card changes
the computational observable: find all parameter-free transport covariants
first, rather than specialize the moduli or launch a large nonlinear solve.
Equation (3.1) is the control that this engine must rediscover from the
previous band.

### Stop and scope

Stop after one paired band.  A local kill concerns only the retained
x-boundary normalization and one SP-2 chart pattern.  `ADJUSTABLE` licenses
neither another numerical sample nor AWS.  None of the outcomes supplies
landing/coverage, `G2-PSC`, `G2-BD`, another terminal class, or a TD6 theorem.

## 4. Card 2 — `SEXTIC-46-ONE-PLACE`

### New connection

The provisional `(4,6)` discriminator has produced the object that the old
Abhyankar--Moh/one-place and log-surface suggestions previously lacked:

```text
an explicit coefficient curve C_(L;k1,k2),
an explicit polynomial one-form eta,
two exact polynomial boundary functions,
and one weighted projective infinity point [1:0:0:0].
```

The old category error was to apply one-place theory to multi-place Keller
fibres.  This is instead a genuinely one-point-at-infinity **coefficient
curve/boundary cover**.  It may have several normalization branches over that
point, so “one point” is not yet “one place”; computing those places is the
gate.  This connects rows 6, 28, 29, 34, and 45 on a finite exact client.

### Exact first experiment

Use only the four frozen equations for `(r,q,B,U)`, weights `(1,2,3,4)`, the
last-row form `eta`, and both boundary functions.  Separately for `L=0` and
`L!=0`:

1. compute the complete local normalization/Puiseux branches above
   `[1:0:0:0]`, including exceptional parameter strata rather than one
   generic numerical slice;
2. compute the value semigroup of `r,q,B,U` on every branch;
3. pull back both boundary functions and `eta` to the normalization;
4. impose regularity of both polynomial boundary values and the exact order
   condition coming from `h*eta(X')=jbar!=0`; and
5. reject a branch only by an explicit uncancelled negative valuation or a
   nonzero-constant-row order contradiction.

For `L=0`, the plane model

```text
Phi(A)^2=X^2 R(A,X)
```

allows normalization/conductor calculations before returning to the full
boundary cover.  The known nonlinear rational component is a mandatory
control; an engine that reports only the two closed line components fails.

The finite outputs are:

* `CLOSE-46`: every normalization branch fails one exact order condition;
* `MINIMAL-POLE-SURVIVOR`: a complete Puiseux branch with all pulled-back
  valuations and parameter conditions;
* `PARAMETER-STRATA-EXPLODE`: no finite exhaustive parameter stratification
  was proved, which is a specification stop rather than permission to sample.

### Chance-adjusted value

This is narrower and more exact than a generic Darboux-polynomial search on
the `(5,6)` Pfaffian surface.  Closing `(4,6)` removes one of only two sextic
patterns; a survivor supplies the smallest algebraic pole normal form that
any later theorem must consume.  The `(5,6)` rational-leaf/Darboux gate should
remain the nonblocking successor, not run as a substitute for an incomplete
normalization here.

### Stop and scope

No low-degree Darboux nonmembership is promoted to a theorem.  No conclusion
about `(5,6)`, `y`-degree seven, arbitrary AS109 support, or existence of an
exact lift follows.

## 5. Card 3 — `AS109-TROPICAL-GROUPOID`

### New connection

The old AS109 support compiler failed before enumeration because literal
monomial supports are infinite and exact triangular gauges change the next
nonlinear support.  The new `A_infinity` ledger supplies a semantic quantity
that is insensitive to how many literal slots happened to be displayed:

```text
dim A_infinity = sum of exact multiplicities at negative source weights.
                                                               (5.1)
```

The sextic floor simultaneously limits the first unresolved coordinate
profiles to `(4,6)` and `(5,6)`.  Each coordinate has at most seven vertical
coefficient rows, even though their `x`-exponents remain unbounded.  This
suggests a symbolic **initial-face motif groupoid**, not another slot cap:

```text
vertices = y-row incidences, symbolic x-exponents, coefficient valuations,
           and exact initial-form residue classes;
arrows    = exact triangular gauges transported through the first nonlinear
           successor;
output    = negative-weight initial systems and their multiplicities.
```

This is the first specific client that answers the old objection “tropical
prevariety without a Newton name.”  It combines rows 19, 21, 31, 38, and 46.

### Exact specification gate

Do **not** start a coefficient search.  For `(4,6)` and `(5,6)` only, prove or
refute the following finite-grammar claim:

> Every common negative-valued initial system of a hypothetical AS109 lift is
> represented, through the first nonlinear determinant successor, by one of
> finitely many symbolic incidence motifs modulo the transported triangular
> gauge groupoid.

The proof must handle all three valuation regimes
`w_x<0`, `w_y<0`, and one zero coordinate.  In particular, `w_x=0,w_y<0`
can see roots of an entire top coefficient polynomial and cannot be silently
replaced by an extremal exponent.  Coefficient cancellations, carries, and
initial-ideal saturation are part of a motif; bare Newton polygons are not.

Exact controls are:

* the unbounded triangular-gauge family that killed literal-slot finiteness;
* the non-Keller maps `x-x^109+109x^N`, whose residual tropical rank is
  exactly `N-109`; and
* tame determinant-one maps, which must not acquire spurious negative
  multiplicity.

The outputs are:

* `FINITE-MOTIF-GROUPOID`, with a proof of coverage and a finite manifest;
* `INFINITE-MOTIFS`, with an explicit pairwise-inequivalent parametric family;
* `TYPE-FAIL`, if initial data do not retain enough coefficient/gauge
  information to transport the determinant successor.

Only the first output licenses a later finite coefficient compiler.  Even
then, (5.1) is a degree/infinity constraint, not existence of a lift or a
contradiction.

### Stop and scope

No exponent rectangle, monomial-count widening, sampled congruence class,
generic sparse search, or AWS is permitted.  Proving `A_infinity=0` for one
motif gives `d=109`; it does not by itself contradict JC2.  Rational descent
of the formal 109-cycle remains a separate gate.

## 6. Deduplication and rejected near-misses

1. **`td=6` does not imply `y`-degree at most five.**  A primitive source
   linear form can generate the degree-six function-field extension, but
   primitive-element degree is not polynomial degree in the complementary
   source coordinate.  No bridge to the new bounded-`y` theorem is licensed.
2. **Real negativity is irrelevant.**  The quadratic
   `5E2-2E1^2` is negative definite on the real source-open locus, but a
   complex Keller counterexample need not have a real specialization.  The
   exact complex survivor prevents a real-positivity promotion.
3. **Do not count the two missing AS bridges as one.**  `A_infinity=0` gives
   `d=109`; an off-diagonal rational factor gives a deck map.  Divisibility
   and the Galois contradiction require proving that the descended action
   contains the full 109-cycle.  Trace and the secant idempotent do not do so.
4. **Secant/projective connectedness stays stopped.**  The current TD6 finite
   band is not an exact Keller pair, so feeding it to the projector does not
   create the missing saturated boundary cycle.  Naive homogenization remains
   `COSTUME`.
5. **No numerical deepening off the survivor.**  TD6 samples with
   `5E2-2E1^2!=0` are already generically dead.  More such samples add zero
   information.
6. **No prime-degree shortcut.**  Degree `109` is conditional on eliminating
   `A_infinity`; the campaign's all-prime claim remains unvetted, and the
   reviewed single-pole theorem is not an all-configuration theorem.
7. **No generic Darboux census.**  The sextic cards use frozen equations,
   boundary functions, and valuation orders.  Absence of an invariant curve
   below an arbitrary degree cap would not close the Pfaffian foliation.

## 7. Recommended sequencing, without launch

Card 1 has the best expected information per hour and remains the direct
proof-backbone bet.  Card 2 is the cleanest exact disproof-frontier gate after
the newly landed `(4,6)` reduction.  Card 3 should occupy an enabling-software
slot only if its finiteness proof is treated as the deliverable; a motif
enumerator written before that proof would repeat the stopped grammar error.

Reviews of the moduli formula, `A_infinity` ledger, and sextic children can
continue in the background.  No card here needs to block on them for
reversible derivation work, but no provisional parent may be promoted or
used publicly before its hostile review lands.

No statement in this report proves or disproves JC2.
