# Primary research: equal-arrival merge families and the M2 quotient

You are Opus 5 at maximum reasoning. Attack the remaining M2 obstruction:
equal-`(mu,w)` nonzero-arrival merges where `kbar` is affine and unbounded.
The goal is not to force a false numerical bound; it is to prove the
strongest exact semilinear quotient and then attack the first parametric
equation that still consumes the unbounded local cell.

Read in full:

- `/Users/dc/code/math/jc2/xmodel/m2-budget-quotient-primary-research-opus5-20260829.md`
  (your prior report; full SHA-256
  `ffb83ec1fcbfbbb18573a030e80e22c20a7d78bb92a5e7a6a5ce4d7b9b96e7af`,
  body `b161421ccb04a62307b87be4a60f130ded64fe53228bd600c960f2c502632283`);
- `/Users/dc/code/math/jc2/xmodel/m2-budget-quotient-falsifier-grok46-20260829.md`;
- `/Users/dc/code/math/jc2/xmodel/m2-finite-reduced-chain-skeleton-sol56-20260829.md`
  (body `8587625b2f12e486f9a3a3773ee5f4dcac7f853edcaddf69221dbab9ea7b1bcb`);
- `/Users/dc/code/math/jc2/xmodel/m2-caseiii-two-pole-incoming-index-bound-sol56-20260829.md`
  (full `406e78d220b62910e6296866db998bd0e7d898b2e6a618fe0aa70ffbeb73fd46`,
  body `8a16cad9cc3211f90e86b1cc31a3fe2991f3817138ae9da4f6dbc56ddf7b5c43`);
- the exact current `ladder/BOOK-OFFAXIS.md`, `ladder/SHEET6-DEPTH.md`,
  `ladder/SHEET6-MULTIPOLE.md`, and `REDUCTION.md` clauses those reports cite.

Do not access, list, search, build, status, or control `jc2-lean`. Do not run
`git status` or another workspace-wide command. No web, AWS, local Singular,
msolve, Sage, PARI, or heavy CAS; no canonical edits; no commit/push.
Bounded exact-rational/symbolic Python is allowed.

Write only:

- optional fresh packet
  `/Users/dc/code/math/jc2/cases/m2_equal_join_semilinear_opus5_20260829/`;
- report
  `/Users/dc/code/math/jc2/xmodel/m2-equal-join-semilinear-primary-research-opus5-20260829.md`.

Research charges:

1. Repair the quantifiers in your prior `C_e=0` theorem. Distinguish
   `C_e=0` for one nonbinding edge from the simultaneous degeneracy that
   makes a legal unbounded family. Classify exactly every merge-pattern
   regime in which the merge-local `nu_G` or `kbar` is unbounded after all
   searrow, NE, root, gcd, and handshake constraints.
2. Derive the general equal-arrival formulas, not just examples. For `r`
   nonzero arrivals all `(mu,w)`, determine every allowed free-0-root
   variant and give exact formulas for `dp,dq,E,kbar,X,M` and trunk child
   `(w_tr,M)`. Prove the admissible `nu_G` set is a finite union of explicit
   arithmetic progressions and identify all exceptional values. Check
   whether the downstream reduced state is constant/periodic even though
   `kbar` is affine.
3. Decide whether a numerical `kbar` bound is actually necessary for the
   *combinatorial* full-configuration ledger. If a semilinear family record
   suffices, specify a unique normalized record and equality/hash rules so
   two parametrizations cannot duplicate a D9-style family. State exactly
   which later consumers still need the full parameter.
4. Attack the first such consumer rather than stopping at architecture. Use
   the promoted td-7 equal-join family (and the td-6 D9 family as a negative
   control) to reconstruct the exact T1/Prop 8.1(iv)/log equation as a
   function of the progression parameter. Seek a uniform contradiction, a
   finite exceptional-value theorem, or an explicit infinite alive family.
   If source data are insufficient, identify the smallest exact missing row
   and write a fail-closed symbolic-emitter specification for it.
5. Reconcile the result with the finite reduced P0-chain theorem and the
   separate incoming-`nu_H` bound. Be precise that your earlier
   `nu<=mu0*num(w0)` proof binds merge-local `nu_G`, not automatically the
   incoming case-III index. Preserve any correct algebra after repairing the
   notation.
6. If you write code, make it cap-free, mutation-tested, normal/`-O`
   replayable, and emit exact parametric identities rather than sampled
   evidence. Include D9 duplicate-normalization and unequal-mu false-family
   controls.
7. End with a ranked next action and an honest verdict: theorem proved,
   counterfamily found, or exact named gap. No landing, ceiling, Keller, or
   JC2 inference without a full typed chain.

Seal the report body and give exact hashes for every produced artifact.
