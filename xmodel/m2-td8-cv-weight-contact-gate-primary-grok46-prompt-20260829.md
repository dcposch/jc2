# Grok 4.6 primary research — td=8 closed cv/contact gate

Work independently from `/Users/dc/code/math/jc2` and write exactly:

`xmodel/m2-td8-cv-weight-contact-gate-primary-grok46-20260829.md`

This is a provisional downstream primary lane; it deliberately does not wait
for review of its input. Read the complete Opus report
`xmodel/m2-td8-first-extra-jet-exact-lambda-primary-opus5-20260829.md`
only as an allegation, then re-read all printed source clauses used here:
Propositions 5.6, 5.7, 5.8, 7.1, 7.3, 7.5 and their definitions, plus the
current promoted corrections and the reviewed td=8 source row. Do not read
Fable's active exact-separation review, Fable's active coefficient transport,
the active td=12 trunk lane, or any later model output.

Target the exact equality configuration that Opus claims is necessary for the
reviewed td=8 equal-join route to fit its shared budget:

```text
td=8; two Lambda=4 poles;
Delta_A1=Delta_A2=Delta_trunk=2; lambda_(0,y)=0; delta_a=0;
T_{a,cv} consists exactly of weights {2,2,2,1};
each A extra-direction branch has deg(p_H)=2 at its cv vertex.
```

Do not assume this configuration is legal merely because its weights sum to
`td-1`. Attack it through two independent finite source interfaces:

1. **Quadratic cv recursion.** Specialize Proposition 7.1 at each putative
   `deg(p_H)=2` vertex. Enumerate/prove all legal integer recursion data
   `(k_j,l_j,s_j)` and every degree/integrality/divisibility condition forced
   by `deg(r_j)=(l_j/k_j)*2`, the relevant `Lambda(P)`, and the actual
   characteristic denominator. Decide whether a quadratic cv vertex of
   weight two can occur on either A branch.
2. **Closed global census.** With `delta_a=0` and exactly four cv vertices of
   weights `2,2,2,1`, use Propositions 5.6/5.8 and the two `Lambda=4` poles to
   enumerate every compatible tuple
   `(Lambda(P),kappa_F,pi(F),degree/multiplicity data)`. Include the mandatory
   `(0,x)` weight-one vertex and distinguish the two A vertices and trunk.
   Check all sum, gcd, orbit, place, and distinctness constraints. Seek a
   contradiction that kills the route uniformly in its affine parameter.
3. Reconcile the two interfaces. If they survive separately, determine
   whether they can be realized simultaneously at the *formal source-data*
   tier. If the printed source lacks one datum, name the smallest exact datum
   and emit a finite typed discriminator rather than guessing it.
4. Independently verify the premise that budget equality implies this exact
   four-vertex configuration. If an omitted nonnegative term or a source
   indexing ambiguity changes the census, return that correction instead.

Use hand algebra and a short exact standard-library packet under
`cases/m2_td8_cv_weight_contact_gate_grok46_20260829/` if useful. No broad
enumeration caps: derive every finite range from the source. Replay ordinary
and `-O`, reject cap tokens, and include exact counts, full/body hashes, and a
clause-level scope table.

Return one lead verdict:
`ROUTE_KILLED_BY_CV_GATE`, `CV_CONFIGURATION_FORMALLY_SURVIVES`, or `PARTIAL`.
Even a kill is conditional on the reviewed td=8 route and the exact-separation
premise until that premise passes different-model review. Survival proves no
gluing, landing, realizability, counterexample, degree ceiling, or JC2 result.
No AWS is authorized.

Hard boundaries: no web, AWS, commit, push, canonical edits, heavy CAS, or
local long/high-memory process. Never access/list/search/build/status or
control `jc2-lean`; never run global `git status` or workspace-wide searches.
Modify only the requested report and uniquely named packet; use `/tmp` for
scratch.
