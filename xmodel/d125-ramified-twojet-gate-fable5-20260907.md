# Gate: generic k=s² finite formal arcs at pure-power centres (Fable 5.1, 2026-09-07)

Bounded 15-minute hostile gate of `xmodel/d125-ramified-twojet-discriminator-astra-20260907.md` (frozen SHA `1440132691…`, whole text read) with its `check.py` and `witness.json` (`37387ae5…`), read from `/tmp/jc2-lane.KxsftI/inputs` only, together with the 14c parity normalization, the 14f boundary classification and its gate, the 14g low-row saturation and its gate, the preflight/`client.py` and `FALLACY-v2.md` as literal interfaces. Root's checker read, ten-run replay (2.768 s) and transaction root `b9884818…` are taken as given. 14c, 14f (field classification and centralizer interface) and 14g (low-row saturation) are PROMOTED and consumed, not re-reviewed; the producer's "still PROVISIONAL" wording for the low-row lemma is stale, not a hypothesis gap. The producer's checker imports a helper from an unavailable path and was NOT run; all controls below are my own stdlib code in `box/d125-ramified-twojet-gate-fable5-20260907/` (`gate_controls.py` SHA `973c1242…`, `run.py`, `witness.json` `3ede3e4e…`, `replay.json`, `input_pins.txt`). No R³, R⁵, degree-15/25 pair, 803-row ideal, CAS, AWS, SSH, solver, peer, ledger or adapter was touched; no frozen file was modified.

## Verdict table

| item | verdict |
|---|---|
| 1. K[[s]] hypothesis, k=s², t≠−3, pure powers up to the whole β shear, finite coefficients, target −5s⁶g²/9 so orders 1–3 vanish; U₁ odd, deg≤13, w≤1; U₂ leader g²p | **CONFIRMED** |
| 2. Orders 1/2 with f(R)=aR+bR³ retained; U₁=RC, R=pT squarefree, T irreducible over Kbar for every t≠−3; φ(R)(u,0)=3u so φ(C) ordinary; a≠0 ⇒ C scalar mod R | **CONFIRMED** |
| 3. a=0 branch: whole s·b/3 shear, complete V₂ kernel, exact E₃ with 5/27·RC³; C scalar mod T; D∈span{p²,p⁴,gp³}; φ(T) valuation 1 and both negative rows ⇒ D∝p²(p²+gp−1); U₁=cR+dR²R_t' | **CONFIRMED** |
| 4. a01=0 ⇒ c=0 ⇒ x₁=0 against x₁²=−3(t+3)³≠0; no automorphism; scope is genuine k=s² arcs and explicitly saturated three-jets over K[s]/(s⁴), not raw nilpotent jets; K[[s]] only | **CONFIRMED** (truncation convention pinned in §4) |
| 5. Controls, replay scope, pure-power coverage for t≠−3 after 14f/14g, whole shears preserve every row used | **CONFIRMED** (one gauge-slice note, §5); typed gaps uncharged |

## 1. Hypothesis and first-face bounds (item 1)

The arc is A=R³+ΣsʳUᵣ, B=R⁵+ΣsʳVᵣ over K[[s]], K of characteristic zero, with every coefficient finite at s=0 and the generic point in the guarded source over K((s)). In the 14c chart the moving coefficients are A_(2,1)=k=s², B_(8,5)=5k/3=(5/3)s², B_(1,0)=5k²/9=(5/9)s⁴ and the Jacobian scalar c₀k³=−(5/9)s⁶, so [A,B] vanishes through s⁵ and only s¹–s³ are used. Own enumeration: the odd slots of total degree ≤15 with weight 5i−7j=3 are exactly (2,1),(9,6), i.e. the fixed inner face, and the odd slots of degree ≤13 with weight 1 or 2 are exactly (3,2). Hence every Uᵣ is odd with deg≤13 (degree 15 is the fixed H³, 14 is even), U₁ has weight ≤1, and U₂ carries the forced leader g²p of weight 3. Vᵣ has deg≤23 and its g and g⁸p⁵ slots are pinned to orders 4 and 2. The whole shear B→B−βA reduces a classified t≠−3 centre to pure powers (§5). CONFIRMED.

## 2. Orders one and two (item 2)

Order s¹ is [R,3R²V₁−5R⁴U₁]=0, so 3R²V₁−5R⁴U₁=F(R)∈K[R] by the imported centralizer K[R] (Arzhantsev–Petravchuk, same trust as promoted 14f; R is closed since deg 5 is prime and H is not a fifth power). R²|F(R) forces F's constant and linear coefficients to vanish, F is odd, and deg F(R)≤33 leaves F=R²(aR+bR³): the producer's f(R)=aR+bR³ with 3V₁−5R²U₁=f(R) is exact, an R⁵ term being excluded by degree. Replaying [U₁,V₁] with 3V₁=5R²U₁+f(R) gives (10/3)RU₁[U₁,R]+(1/3)f'(R)[U₁,R], hence E₂=3R²V₂−5R⁴U₂−(5/3)RU₁²−(1/3)f'(R)U₁ with f'=a+3bR²; own control checks this bracket identity on a tiny toy R=g+p²+gp with a=5, b=−2 (my first toy dropped the factor 5 and failed, a useful negative). E₂=h₂(R) with h₂(0)=0. Reduction mod R gives aU₁≡0, so a≠0 ⇒ R|U₁; for a=0 dividing by R and evaluating at the origin gives R|U₁². T=R/p is even, T mod p=−(t+3)≠0, and as a cubic in g over Kbar((p)) its constant coefficient p³+tp−(t+3)/p has valuation −1 while g³ and (t+3)g have valuations 3n and n: no root of any integer valuation exists, the cubic is irreducible over Kbar((p))⊇Kbar(p), and primitivity plus Gauss give irreducibility in Kbar[g,p]. Both uses of t≠−3 are explicit. So R=pT is squarefree and U₁=RC with C even, deg C≤8, w(C)≤w(U₁)−1≤0. Own degree-five lift: φ(R) is ordinary for symbolic t and its v⁰ part is exactly 3u, so ord_vφ(R)=0 and ordinariness of φ(U₁), which is the s¹ coefficient of the linear A-negative rows, gives ord_vφ(C)≥0. For a≠0, E₂/R mod R gives aC≡scalar. CONFIRMED.

## 3. Order three and the tangent restriction (item 3)

The shear B→B−s(b/3)A is a K[[s]]-shear by a multiple of A: it preserves the Jacobian, the linear polynomiality rows, B's top (15<25), B's weight-5 slots (A has weight ≤3), origin and parity, and makes V₁=(5/3)R³C. E₂=h₂(R) with R²|h₂(R) and deg≤33 gives h₂=ℓR³+mR⁵ exactly, so V₂=(5/3)R²U₂+(5/9)RC²+(ℓ/3)R+(m/3)R³ with no omitted kernel term. Hand replay of [R³,V₃]+[U₁,V₂]+[U₂,V₁]+[U₃,R⁵]: the R³[C,U₂] terms cancel, the R²C[R,U₂] terms combine to −(10/3), and [RC,RC²]=RC²[R,C] produces (5/9)RC²[R,C]=(5/27)R[R,C³]. So E₃=3R²V₃−5R⁴U₃−(10/3)R²CU₂+(5/27)RC³−(ℓ/3)RC−mR³C is exact; own toy control confirms it and the 5/9 mutation fails there first. E₃/R mod R gives (5/27)C³−(ℓ/3)C≡scalar mod R, hence mod T; the cubic has nonzero leading coefficient and splits over Kbar, and Kbar[g,p]/(T) is a domain, so C≡c mod T. D=(C−c)/T is even, deg≤4, w≤−8; own enumeration gives exactly p²,p⁴,gp³. Own lifts: the negative parts are v⁻², v⁻⁴+4v⁻², −v⁻⁴−3v⁻², with no u and no v⁻¹ or v⁻³ row, so the matrix is [[0,1,−1],[1,4,−3]]. φ(T) has valuation 1 with leader −3uv, so φ(TD) ordinary needs only ord_vφ(D)≥−1, which still kills both rows: kernel (−1,1,1), D∝p²(p²+gp−1), and T·p²(p²+gp−1)=R·R_t' with R_t'=p(p²+gp−1)=∂_tR_t checked as polynomials. The omitted-row mutation (0,1,1) leaves a v⁻² pole and fails. Since p²|D, C(g,0)=c∈K and then d∈K. CONFIRMED.

## 4. Contradiction, truncation convention and scope (item 4)

Own degree-≤3 truncated products (never R³ itself): R³ has no part below degree 3, [gp²]R³=0 and [p³]R³=−(t+3)³ (coefficients −27,−27,−9,−1 in t). U₁=cR+dR²R_t' has [p]U₁=−c(t+3), [gp²]U₁=c(t+3), and d enters first at p³; an added independent gp² term fails this control. The promoted 14g certificates k²a01, k³h1, k⁴h2 ∈ I_low hold in the domain K[[s]] with k=s²≠0, so a01=0 and x²=3ky exactly. Order 1 of a01=0 gives c=0, hence x₁=0; order 2 of x²=3ky with x₀=0 gives x₁²=3y₀=−3(t+3)³, nonzero for every t≠−3 and zero exactly at t=−3 (own evaluation). Contradiction. No automorphism of the plane and no tangent removal is used anywhere; the only moves are K[[s]]-multiples of A added to B. Truncation convention, pinned: the proof consumes the s³ Jacobian coefficient (U₃,V₃), the s¹ A-negative rows, the s¹ coefficient of a01 and the s² coefficient of x²−3ky, so it also kills three-jets over K[s]/(s⁴) on which those three saturated equations are imposed explicitly; K[s]/(s³) is not enough. It says nothing about raw jets: over K[s]/(s⁴) the raw rows are −(5/9)k²a01 etc. with k²=s⁴=0, so a01 is unconstrained, and the producer's countercontrol [sp,5s⁴g/9]≡0 mod s⁵ with A_p=s≠0 (own replay, and the cancel-k mutation fails) is a toy illustration only. The statement is for K[[s]] with k=s² literally; a general DVR arc would need completion, a coefficient field and a unit-square adjustment of k, none of which is claimed. No claim at t=−3, higher ramification, coefficients escaping to infinity, degeneration of a guarded point, guarded-source emptiness or JC2. CONFIRMED.

## 5. Controls, coverage and shears (item 5)

Own runner: ten capped runs (five modes × normal/−O), positive witnesses byte-identical, zero Assert nodes, 0.75 s wall, each mutation recorded by its first failing gate: cubic 5/9 → order-3 identity; omitted v⁻² row → φ(D) valuation; independent gp² tangent → the p/gp² relation; nilpotent-k cancellation → its own gate. The producer's five mutation branches are reachable by the same ordering; its witness values (slots, matrix, kernel, 3u, −3uv, 5/27, tangent coefficients ∓7(t+3)) agree with mine. Coverage: 14g's interface a01=−α(t+3), e=−γ(t+3) with a01=e=0 in the domain gives α=γ=0 for every t≠−3, so every classified centre with t≠−3 is R³, R⁵+βR³ and the constant whole shear reaches pure powers: the result covers all t≠−3 centres, as stated. Shears B→B−λA, λ∈K[[s]]: Jacobian bilinear with [A,A]=0; polynomiality rows linear in coefficients and satisfied by A; B's fixed top and weight-5 faces untouched (15<25, w(A)≤3<5); origin, parity and the A⊂B polygon (14f) preserved. Note, uncharged: the optional [p¹⁵]B=0 gauge slice of 14f §3 moves by λ·[p¹⁵]R³=λ, but the proof never uses that slice, so nothing depends on it.

Typed gaps, uncharged: **GAP-TRANSACTION** (root `b9884818…` and the report SHA are not among the frozen inputs, taken from root); **GAP-HISTORY** (the bounded novelty check is unverifiable from the frozen set); **GAP-HELPER** (the producer's checker depends on a helper at an unavailable path, replaced here by own controls). No exit-price claim is made, so no charge basis is declared. **STOP:** review only; five verdicts above, no promotion authority beyond them.

<!-- BODY-END -->
