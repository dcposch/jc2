# Ideation round 20260905T1200Z — Fable 5 blind submission

- Lane: `ideation-20260905T1200Z-fable5`; basis `da15d260` (receipt `xmodel/ideation-20260905T1200Z-fable5.run.v2`).
- Inputs: the four charged files under `/tmp/jc2-lane.1lX6Y2/inputs`; manifest generated by `awk` from the
  receipt's `charged_input_<i>_sha256=` / `_basename=` lines and checked with `sha256sum -c`: 4/4 `OK`
  (packet, 0200Z synthesis, FALLACY-v2, COORDINATION). No digest retyped.
- Also read (banked, sealed): AUDIT 17(jjjjjj)–(ssssss); notes.md LIVE STATE 02:51Z and EVENTs through 09:28Z;
  `moh14-fullorder-grok46`, `moh14-fix-solve-opus5`, `moh-sprime3-compiler-grok46`, `k16-exact-square-fable5`,
  `prop54-minimality-opus5`, `receiver-atlas-k1-astra` (all 2026-09-05); `k16-rank-criterion-fable5` and
  `k16-nzd-gamma-gpt55` (2026-09-03/04) for the Γ atom; `ideation-20260905T0200Z-astra.md` §4–5 (routing);
  my own 0200Z submission; Moh pp.200–202 (PDF 61–63); Xu 2016 §8 (text layer). NOT read: any
  `ideation-20260905T1200Z-*` file (including the coordinator's blind submission); the running lane
  `moh14-msolve-sol56` (receipt only: `initial_status=RUNNING`, no `final_status`).
- Desk drivers: `box/ideation1200-fable5-20260905/` (< 10 min, foreground, mod p on the frozen rank-lane rows).
- Sections are appended in bounded writes; the seal line is appended last.

## 0. Headline — direct answers, and where the packet's framing is wrong

**Q1 (K16).** The atom is a GENERICITY statement, not an identity: the tail is t weighted forms in t
weighted variables whose expected common-zero locus in weighted projective space has dimension −1, and
(V0)-tail says the specific K16 forms are not on the discriminant. Two new measured facts change its shape
(§1.1, desk, mod p on the frozen rank-lane rows, exact at t=3): (a) the Eagon–Northcott curve Γ_t is
REDUCED on the chart b4 ≠ 0 at t = 3, 4, 5 — its d_Γ(t) weighted points are simple points, so "one scalar per
point" is now "one scalar per SIMPLE point" and multiplicities carry no structure; (b) at t = 3 the seven
points of Γ_3 ∩ {b4 ≠ 0} form ONE irreducible Galois orbit over A_3 (the degree-7 univariate polynomial is
irreducible over Q(√3)), with Frobenius cycle type (2,5) at p = 32003. If (b) persists (t = 4 exact pending
at seal; t = 5 has cycle type (3,38,54,170), consistent), then clause (ii) is ALL-OR-NOTHING at each t: the
top row, being defined over A_t, vanishes at every point of the orbit or at none, so (V0)-tail at fixed t is
ONE nonvanishing at ONE simple point (a modular evaluation at a good-reduction point, no number-field std),
and the all-t problem splits into (I) irreducibility of the determinantal curve Γ_t over A_t for all t and
(II) one uniform point with T_top ≠ 0. That is the genuinely new mechanism of this round (Card A); cheapest
decisive test: the exact factorization of the Γ_4 point polynomial over Q(√15) (46 points; running at seal)
and the t = 5 one on a worker (265 points, minutes).

**Q2 (Moh ≤ 100).** Yes with four typed conditions: an msolve UNIT on the six class unions is the ≤ 100
finish only if (i) it is an exact rational certificate, not msolve's first-prime `[1]` printed under a
char-0 header (COORDINATION §Replayability); (ii) union-dominance is used in the proved direction only
(union empty ⇒ every fibre empty); (iii) the chart's NECESSITY at s′ ≥ 3 is sourced — today Φ_eff at s′ ≥ 3
is a derivation validated only at s′ = 2 against Moh p.207 (17(nnnnnn)); a UNIT on a chart that is not a
necessary over-approximation kills nothing; (iv) the two Xu Cor 5.3 deaths of the 14 are cited as such.
Paper-grade statement and dependency list in §2.1. If msolve stalls on the K′ = 4, ℓ = 3 fibres (333/378
unknowns), the structural route is not a faster engine: it is the exhaustive DISC-ASSIGNMENT case split of
the u′ outer roots (each case a sourced tighter chart; their union is the B_safe chart) run per V-stem with
band-wise pivot elimination at emission (§2.2, Card B).

**Q3 (coverage).** On Moh's line coverage IS a finite check per degree, and it is already mechanised
(census → screen → split-window dichotomy → chart); the Sigray first-separation / route-to-state
interfaces (`ladder/REDUCTION.md:1122–1187`) belong to avenue 2's tree architecture and are NOT consumed by
a Moh-line proof — the packet's "routing maps" conflates the two. What is NOT finite is the kill: the census
is cofinally nonempty, so the all-degree statement is a uniform theorem over the descended lattice, and K16
is its prototype — the packet's "coverage, not K16" inverts the dependency. The binding SUB-GAP is the s′ ≥ 3
NECESSITY theorem (Card C): without it the finite ≤ 100 kill is not paper-grade and no degree is
"covered"; with it, per-degree coverage is mechanical and the only all-degree gap is the uniform (T).

**Q4 (leverage).** None of the four. The single highest-value fleet deployment is the u_s = 1 (T)-batch
that is already running, EXTENDED to the eight u_s = 1 rows of D = 108 (same builder, same type) so that the
first honest degree-wide statement above 100 closes with the ≤ 100 finish; second, the Γ_t factorizations
of Card A (minutes per t on a worker). The 1359-class u_s ≥ 2 census is finite and buys no theorem; K16
t = 8..11 verification adds no uniform information; the coverage proof is desk work (§4).

**Corrections to the packet.** (1) "The Moh ≤ 100 residual is a solver wall" is true of the class UNIONS;
the V-stems and the disc-assignment split were never run. (2) "Class logic is conjunctive" is right, but the
running lane should report per fibre — a union UNIT is a kill, a union TIMEOUT is not a class verdict.
(3) The K16 residual is not "needs a new idea" in the abstract: it is a Galois-orbit question with a
one-point certificate, testable today.

## 1. Q1 — K16: the atom retyped, two new measured facts, the mechanism, the cheapest test

### 1.1 What was measured here (desk, < 5 min each, drivers `box/ideation1200-fable5-20260905/gamma*.sing`)

Setting (charged, 17(rrrrr)): tail rows `T_{t,2t−1−r} = a_r b3² + b_r b3 + c_r` (r = 0..t−1), `a_0` a unit,
`G_r = a_0 T_{2t−1−r} − a_r T_{2t−1} = B_r b3 + C_r`, `N = ((C_r, B_r))_{r=1..t−1}`, `Γ_t = V(I_2(N)) ⊂ Spec P_t`
(P_t = A_t[b4, q_2, …, q_{t−1}], weights 1..t−1). Clause (ii) of RANK = `T_{t,2t−1}(p, β(p)) ≠ 0` at every
point p of Γ_t with `β = −C_r/B_r`. I reused the frozen `rank_t{t}_mod_p*_b0.sing` prefixes (rows, splits,
ring P, unchanged bytes up to `ideal BC=Bs+Cs;`) and appended: `I2 + (b4−1)`, its `vdim`, its `radical`, the
`vdim` of the radical, `finduni` factor degrees, and the `b4 = 0` slice.

| t | p (root of H_t) | vdim(Γ_t ∩ {b4=1}) | reduced points | b4 = 0 part | d_Γ(t) check | finduni factor degrees mod p (ordinary) |
|---|---|---:|---:|---|---|---|
| 3 | 32003 | 7 | **7** | one orbifold point (q2-axis, weight 2 → 1/2) | 7 + 1/2 = 15/2 ✓ | q2: 2, 5 |
| 4 | 32029 | 46 | **46** | none (slice dim 0) | 46 ✓ | q2: 1, 3, 7, 11, 24 |
| 5 | 32009 | 265 | **265** | 6 reduced points at b4=0, q2=1 (→ 3) + a q3-axis point (→ 1/3) | 265 + 3 + 1/3 = 805/3 ✓ | q2: 3, 38, 54, 170 |
| 6 | 32003 | 1548 | (radical running at seal) | none (vdim = d_Γ) | 1548 ✓ | — |

So `d_Γ(t)` of 17(rrrrr) counts DISTINCT geometric points (with the orbifold convention on the boundary):
Γ_t is generically reduced. The "multiplicities 2, 8, 20 / 5, 15, 35, 55, 120" of 17(rrrrr) §5.4 were
weighted `mult()` values of F_p-primes (Frobenius orbits × weights), not scheme multiplicities.

**Exact at t = 3** (`gammax_t3.sing`, ring `(0,yy)`, `minpoly = H_3`): the radical of `I_2(N) + (b4−1)` has
`vdim = 7` and its `finduni` polynomial in q2 (ordinary degree 7) is IRREDUCIBLE over A_3 = Q(√3). Hence
Γ_3 ∩ {b4 ≠ 0} is a single closed point of degree 7 over A_3 — one Galois orbit — and the mod-p cycle type
(2,5) is a Frobenius element of its Galois group. (t = 4 exact: degree 46 over Q(√15), `gammax_t4.sing`,
running at seal; the mod-p cycle types (1,3,7,11,24) at one root and (6,13,27) at the other are both
compatible with transitivity.)

### 1.2 The retyped atom: a discriminant-avoidance (genericity) statement

Eliminating b3 from the tail is classical: t quadratics `T_r(b3)` with coefficient matrix
`M_t(p) = ((c_r, b_r, a_r))_r ∈ Mat_{t×3}` have a common root iff `rank M_t(p) ≤ 2` AND the kernel vector is a
Veronese point `(1, β, β²)`. With `a_0` a unit the rank condition is exactly `rank N ≤ 1` (Γ_t), and the
Veronese condition on Γ_t is exactly `W_r = a_0 C_r² − b_0 B_r C_r + c_0 B_r² = 0` (desk identity: the kernel of
rows 0 and r is `(−B_r, C_r, (c_0 B_r − b_0 C_r)/a_0)`, and `x_1² = x_0 x_2` is `W_r = 0`). So

> (V0)-tail_t ⟺ the map `M_t : WP(1,…,t−1) → Mat_{t×3}` misses the common-root variety `R_t` (codim t−1).

Dimension count: `(t−2) − (t−1) = −1`, i.e. a GENERIC weighted map of these weights misses `R_t`; (V0)-tail
says K16's specific map is not on the discriminant of this avoidance problem. The one banked failure
(t = 2, y = 1/5) is the degenerate stratum `a_0 = 0` (17(eeeeee)), where `M_2` loses its unit first row. This
typing explains at once why every LOCAL or DEGREE-ONLY route died (17(ppppp), 17(oooooo)): avoiding a
discriminant is an open condition with no local certificate. It also says what a proof must be: a
global reason that a t-indexed specific point stays off a t-indexed hypersurface.

### 1.3 The mechanism: Galois all-or-nothing + one-point certificate (NEW)

**Lemma (all-or-nothing).** Let Z ⊂ Γ_t ∩ {b4 ≠ 0} be a Galois orbit over A_t (a closed point of WP over
A_t) and f ∈ P_t[b3] defined over A_t (here f = T_{t,2t−1} with b3 = β(p) = −C_r/B_r, i.e. f|_Z = W_r/B_r²).
Then f vanishes at every geometric point of Z or at none, because Gal(Ā_t/A_t) acts transitively on Z and
f(σP) = σ(f(P)).

**Consequence.** If Γ_t ∩ {b4 ≠ 0} is ONE orbit (measured t = 3; t = 4 pending), clause (ii) at t reduces to
ONE inequality `T_{t,2t−1}(P, β(P)) ≠ 0` at ONE point P, and — because Γ_t is reduced — a simple F_p-point P̄
of Γ_t mod p at a prime of good reduction lifts (Hensel) to a geometric point P, so `T_top(P̄, β̄) ≠ 0 mod p`
proves `T_top(P, β) ≠ 0` exactly. Together with clause (i) (B-HSOP, charged through t = 8) this is a
fixed-t instrument for (V0)-tail that needs NO standard basis over a number field: one radical mod p (272 s
at t = 5, single core), one irreducibility proof, one evaluation. The boundary orbits (b4 = 0, present at
t = 3 and t = 5) are separate orbits of a smaller Γ-type system and get the same treatment recursively.

**All-t shape.** (I) Prove Γ_t ∩ {b4 ≠ 0} irreducible over A_t for all t. Γ_t is the rank-1 locus of a
2 × (t−1) matrix whose entries are the spine polynomials B_r, C_r; irreducibility of such determinantal
loci is a monodromy statement (avenue 26's vocabulary: the Galois group of the point set), and the
measured cycle types (2,5), (1,3,7,11,24), (3,38,54,170) are the data a Chebotarev-style argument would use.
(II) Exhibit one point per t with T_top ≠ 0 — the natural candidate is a FORMAL point: Astra's formal
(F3) solutions at x = 0 that "never truncate" (17(hhhhhh)) are points of an analytic thickening of the cone;
a formal point of Γ_t with an explicit leading term would give T_top(P) as a closed-form scalar in t, and by
the lemma that single scalar would prove clause (ii) on the whole orbit. I do not have (I) or (II); both are
now well-posed and each is a bounded lane.

**Why this is not the refuted routes.** Coprime-leaders / degree-only / exact-square / resultant were all
attempts to certify the avoidance by an identity in the coefficients; the linked-quartic was a pointwise
solution classification. The lemma uses the FIELD OF DEFINITION of the points, a datum none of them
consumed, and replaces `d_Γ(t) ~ (27/4)^t` nonvanishings by one per orbit.

### 1.4 Cheapest decisive tests (bounded)

- **T1 (irreducibility, running/next).** `gammax_t4.sing` (exact factorization of the degree-46 point
  polynomial over Q(√15)); then t = 5 (degree 265 over Q(√2)) on a c7i worker. Quantity: number of A_t-orbits
  of Γ_t ∩ {b4 ≠ 0}, `k_t`; measured `k_3 = 1`. `k_4 = k_5 = 1` ⇒ Card A is the K16 program; `k_t ≥ 2` ⇒ the
  lemma still applies orbit-wise (k_t points instead of d_Γ(t)), and the orbit sizes become the structure
  to explain.
- **T2 (one-point instrument at t = 8).** Radical of `I_2(N) + (b4−1)` mod p at t = 8 (P_8 has 7 variables,
  52140 points; compare 272 s at t = 5 with 265 points — expect hours, a fleet job, not desk), then evaluate
  `W_1` at one F_p-point. Quantity: `T_top ≠ 0` at one simple point ⇒ clause (ii)_8 modulo irreducibility;
  this is the first t ≥ 8 (V0)-tail evidence not blocked by the 1800 s std wall (17(uuuuu)).
- **T3 (genericity control, 10 min).** Twenty random weighted systems of the tail's weights mod p at
  t = 3, 4, 5: fraction m-primary. Prediction 20/20 (the map avoids R_t generically); a lower fraction
  would mean the weights themselves force common zeros and the typing of §1.2 is wrong.
- **T4 (t = 9 socle test, standing from 17(oooooo)).** Unchanged: `N_9 = 3` predicted; a weight-74
  Macaulay rank mod p on a worker. Falsifies or confirms the LAW, not the atom.

## 2. Q2 — the Moh ≤ 100 finish: what a UNIT proves, the paper-grade statement, the structural fallback

### 2.1 If msolve returns UNIT on the six class unions

**It is the finish iff the following hold, each typed.**

1. **Exact certificate.** msolve 0.10.1 char-0 `-g` can print a first-machine-prime unit basis under a
   char-0 header before rational reconstruction (COORDINATION, Replayability). A `[1]` is trace evidence
   until a rational certificate `1 = Σ a_i f_i` (or an independent exact-Q `std` on the certificate subset,
   `modular_solve.py` Stage B) is replayed. The running lane's "UNIT [1] modular signals on several fibres"
   (packet §1) are Stage A signals, not kills.
2. **Union dominance, proved direction only.** Setting the extra coefficients of the union chart to zero
   carries a fibre solution into the union (the fibre equations are the union equations with those unknowns
   zeroed), so union UNIT ⇒ every fibre UNIT ⇒ class dead. A union TIMEOUT or NON-UNIT says nothing
   (17(rrrrrr)/(ssssss)). Report per fibre; the class verdict is the conjunction.
3. **Necessity of the chart at s′ ≥ 3 (the load-bearing dependency).** The chart is "free leading form +
   every D1 monomial with `wt ≥ j·B_safe`, `B_safe = V′_2 δ′_1 + u′ δ′_{s′}`, Jacobian `c·x^ℓ`, `ℓ = v_s − u_s − 1`,
   descended radii `δ′_i = (ℓ+1)·Def 5.1(3)(n′, M′, d′, V′, s′, i)`". The radii formula Φ_eff is a DERIVATION
   (17(nnnnnn)) validated only at s′ = 2 against Moh's Appendix II p.207; the floor `B_safe` is an argument
   about where the u′ outer roots can sit. Neither is a printed theorem at s′ ≥ 3. A UNIT on a chart that is
   not a necessary over-approximation of the descended pair kills nothing (17(ggggg) shape, FALLACY-v2
   floor/attainment). This is Card C and the binding sub-gap of Q3.
4. **The two Xu deaths.** Rows `(90,60; M=(45,80,88); V=(2,5,4))` and `V=(3,5,4)` die by Xu 2016 Cor 5.3
   (`IM_max = 43/7 < Im_min = 7`), an arXiv (v4) source. For paper grade either cite it as a dependency or kill
   those two rows by the chart route as well (they descend to `(18,12; M′=(9,16); ℓ=2)`, s′ = 3, same builder).

**Paper-grade statement.** *Theorem (Moh 1983, completed).* Let k have characteristic 0 and let
`f, g ∈ k[x,y]` satisfy `J(f,g) ∈ k^×` with `max(deg f, deg g) ≤ 100`. Then `k[f,g] = k[x,y]`.
*Proof shape.* A counterexample of degrees ≤ 100 yields one of minimal total degree, still ≤ 100 (degree
reductions by automorphisms only lower degrees; 17(pppppp) §3). Minimality gives Moh's normalisation (3)
(degrees not simultaneously reducible), which by Prop 5.4 + Lemma 5.3 + `δ_i ≥ −1` forces the two-point
configuration `M_s = n−2` (17(pppppp)); the divisibility case `m | n` is the classical theorem Moh cites;
`f = T_1^ψ` is nonrestrictive (p.200). Its characteristic data lie in the (1)–(13) census (Moh Theorem
p.200 + Prop 6.1; enumerator confirmed 17(cccccc)). The promoted whole-tree screen (17(r)/(hh)/(ll)) plus
Xu Cor 5.3 leaves Moh's six printed rows and twelve excess rows. The printed rows are dead: four by Moh's
Appendix II (replayed, 17(hhh)), the (99,66) row degree-wide by 17(ffffff) (independent of Prop 6.3). The
twelve excess rows are `u_s = 1`, descend by Prop 6.3 with the radius automatic (Prop 6.4, 17(gggggg)) to
monomial-Jacobian pairs with characteristic data Φ_eff, whose full Theorem-1.2 D1 order charts (item 3)
are empty by exact certificate (item 1). Contradiction. ∎ — with the explicit dependency list
{17(pppppp), 17(cccccc), 17(r)/(hh)/(ll), Xu Cor 5.3 (2 rows), 17(hhh), 17(ffffff) ← 17(r)+(hh)+(pppp)+
(tttt)+(bbbbbb), 17(gggggg), Card C, the six certificates}. Scope note the gate should mirror: this is
Moh's induction under his standing hypotheses; the census reproduces his program with 14 rows he did not
print — the theorem does not depend on why.

### 2.2 If msolve stalls on the K′ = 4, ℓ = 3 fibres (M6_13: V1_1 333, V1_3 378 unknowns)

Diagnosis first: those fibres are large not because K′ is large (K′ = 4 is the smallest) but because
`δ′_1 = 9/4, 1/2` and `B_safe = −15/4, −11/2` are far from `B_tight`, so the D1 inventory admits many
high-x monomials that only the WEAKEST disc assignment of the u′ = 3 outer roots licenses. The structural
route is therefore to stop paying for the floor:

- **Disc-assignment case split (Card B).** The u′ outer roots of h lie in `D′_2, …, D′_{s′}`; for an
  assignment `(a_2, …, a_{s′})`, `Σ a_j = u′`, Theorem 1.2's floor is `B(a) = V′_2 δ′_1 + Σ_j a_j δ′_j`, and
  `B_safe = B(0,…,0,u′)` is the weakest. The cases are exhaustive and each case chart is a NECESSARY
  over-approximation for that assignment (the same source step as B_safe, applied with the actual disc of
  each root), so "every case UNIT ⇒ fibre dead" is sound and "union of cases = B_safe chart" is the control.
  Case count `C(u′ + s′ − 2, s′ − 2)`: 10 at (u′,s′) = (3,3) for M6_13, 36 at (7,4) for Mm12; each case chart
  is the size of a K = 8 chart (the 36-variable K89 kills, 17(bbbbbb)) rather than 333–378 unknowns.
- **Per V-stem, not per union** (17(rrrrrr) §7): the union weakened both the inventory and the threshold.
- **Band-wise pivot elimination at emission** (memory: 78–84 % of unknowns carry raw unit pivots; the
  K = 8, 9 kills came from top pin + lower-band parametrisation, not from a faster std). msolve is the right
  engine for the residual after elimination, not for the 385 KB-row expanded ideal.
- **Prefix-certificate discipline.** Stage A on generator prefixes sorted by size, Stage B exact-Q on the
  certificate subset; never a full basis.

Bounded price: 12 V-stems × ≤ 36 cases, each ≤ 10 min on one core after emission = one fleet-hour on 64
cores; emission is the critical path (one lane-day for the case-split emitter, validated by reproducing the
B_safe chart as the union of cases on Mm15 and the K = 8 kill).

## 3. Q3 — coverage: finite per degree, uniform for the kill; the binding sub-gap named

### 3.1 The arrows on Moh's line, each typed

| arrow | statement | status |
|---|---|---|
| R1 | minimal counterexample ⇒ Moh's (3) ⇒ two points at infinity, `M_s = n−2` | PROMOTED 17(pppppp) |
| R2 | two-point pair ⇒ characteristic data in the (1)–(13) census of (n,m) | sourced (Moh Thm p.200, Prop 6.1); enumerator confirmed 17(cccccc) |
| R3 | census row ⇒ screen (partition / Prop 5.6 / POLY-ODE / Xu) | PROMOTED 17(r)/(hh)/(ll); Xu external |
| R4 | survivor ⇒ split window nonempty (joint chart, u_s ≥ 2) XOR empty (Prop 6.3 descent; automatic at u_s = 1) | sourced dichotomy, radius cleared 17(gggggg); den(δ*) ≤ u_s provable (memory prop63-radius-dichotomy) |
| R5 | descended pair ⇒ D1 order chart of the Φ_eff data is a necessary over-approximation | DERIVED at s′ ≥ 3, validated only at s′ = 2 — **the binding sub-gap** |
| R6 | U-NEGATIVE rows (`V′_2 > d′_2`) ⇒ some chart | UNASSIGNED (no chart family treats them) |
| R7 | chart empty ⇒ row dead (certificate) | per row; exact-Q certificates promotable |

R1–R4 and R7 are finite, mechanical checks per degree pair (n,m): the census is finite, the screen is a
replay, the dichotomy is per row. So COVERAGE per degree is a finite check — provided R5 is a theorem and
R6 is either sourced impossible or given a chart. Neither the Sigray first-separation nor route-to-state
interface appears in this table: they are obligations of avenue 2's tree architecture (`ladder/REDUCTION.md`
CRITICAL 4) and a Moh-line proof does not consume them (Astra 0200Z §4.2 said the same; the packet's Q3
should drop the phrase).

### 3.2 Why the all-degree statement is a uniform theorem and where K16 sits

The census is cofinally nonempty (PIN-NOT-CEILING; Sol's unbounded pinned-N family), the u_s = 1 residue
at D ≤ 200 is 1,110 rows with NO compression at the full descended datum (memory us1-rows-dominate), and
u_s = 1 is 78 % of the residue. So the all-degree program is: uniform (T) over the u_s = 1 lattice
`(a, b; K′; ℓ; s′; M′, V′)` plus uniform (H1) over the u_s ≥ 2 lattice. K16 is the line
`(a,b) = (3t+1, 2t+1), K′ = 4, ℓ = 1, s′ = 2` of the first — the prototype of the dominant mechanism. The
packet's "the coverage is the real gap, not K16" has the dependency backwards: coverage is finite once R5
is a theorem; the kill is infinite and K16 is where its uniform form is being learned.

### 3.3 The binding sub-gap, stated as a lemma to prove (Card C)

> **Lemma (s′ ≥ 3 necessity).** Let (f,g) be a two-point Keller pair with characteristic data
> (n; M_1..M_s; V_2..V_s), u_s = 1, and let (P,Q) = (ḡ(σ), T̄_1(σ)) ∈ k[γ,π]² be its Prop 6.3 descent
> (y = γ^{−u_s}, x = (y − e − σ)/b, J = −(u_s/b)γ^ℓ, ℓ = v_s − u_s − 1). Then (P,Q) is monic in π of degrees
> (n′, m′) = (n,m)/d_s, its characteristic tower has depth s′ = s−1 with M′_i = M_i/d_s, V′_i = V_i, radii
> δ′_i = (ℓ+1)·Def 5.1(3)(n′, M′, d′, V′, s′, i) (after the p.174 drop), and for every j the coefficient
> a_j(σ) of its h-adic expansion satisfies ord a_j ≥ j·(V′_2 δ′_1 + u′ δ′_{s′}).

Validation obligations: Appendix II p.207 (5/5, s′ = 2, already done); the two (99,66) S5/S6 descents
(ℓ = 8) whose kills are promoted; the (25,15) order-chart-is-a-sub-system episode (17(ggggg)) as the
negative control for the inventory. This is a source-reading lane (Moh pp.174, 179, 194–199, 207), no CAS.

## 4. Q4 — leverage: none of the four as posed

- **u_s ≥ 2 census (1359 classes).** Finite; buys "JC2 for D ≤ 200" at best, and each class needs the FULL
  joint chart (pole + Jacobian + incidence rows, 2271–3441 coefficients; the batch-3 joint charts did not
  finish, 17(iiiiii)). No uniform information; no theorem. Not now.
- **s′ > 2 residual (the 12 fibres).** Already running; the right first client of the fleet. EXTEND it to
  the eight u_s = 1 rows of D = 108 (`(18,12; ℓ=3)×6`, `(27,18; ℓ=1)×2`, same builder, memory
  us1-rows-dominate): the increment is a degree-wide D = 108 closure, the first honest statement above 100.
- **K16 t = 8..11 verification.** Fixed-t (V0) by std is a 1800 s wall at t = 8 (17(uuuuu)) and proves
  nothing uniform. Replace it by Card A's instrument: radical mod p + one evaluation (T2) — hours, not a
  Gröbner basis — and the exact factorizations of T1 (minutes at t = 4, 5).
- **Coverage proof.** Desk work (Card C), no fleet.

**Challenge to the framing.** "Fast Gröbner at 1000-vCPU scale" is the wrong lever for both fronts: the
Moh charts fail on generator bulk, not solver speed (385 KB rows; band-wise pivots at emission are the
measured fix), and K16 fails on `d_Γ(t) ~ (27/4)^t` points, which no basis computation enumerates. The
fleet's highest-value use is CERTIFICATE MINING: run the (T) charts smallest-first over the u_s = 1
residue at D ≤ 200 sorted by `(K′, ℓ, s′)` and record WHERE each kill happens (which band, which pivot
produces the unit) — the k = 4 ray's uniform lemmas (TOP-BAND, `3 deg β ≥ 2K+1`, `deg(g² − f³ − λf) = K+6`)
were all read off kill certificates, never off verdicts.

## 5. Disposition vector (changes only), bottleneck reranking, contract items

Rows are APPROACHES.md's master union table (1–46) with the 2026-09-03/04 overlays and my 0200Z vector as
the baseline; changes only.

* Row 1 (monomial-Jacobian receiver, `[P,Q] = x^k`): **LOWER from "#1 all-degree object" to "routing
  object"** — 17(mmmmmm): the literal k = 1 receiver is empty but nothing maps into it (K16's forbidden unit
  coefficient `[γπ⁰]P = −g`), 0/1110 discharged; my 0200Z RAISE is withdrawn. The ℓ = 1 fold question
  survives only as a card-level question (0200Z Card A, still unrun).
* Row 2 (Sigray/Eggers–Wall tree, first-separation, route-to-state): **RETYPE** — its interfaces are not
  consumed by a Moh-line proof (§3.1); keep for the tree architecture, drop from the Moh-line gap list.
* Row 26 (primitive monodromy / function-field Galois): **RAISE one notch, RETARGET to K16** — the Galois
  group of the point set Γ_t ∩ {b4 ≠ 0} over A_t (one orbit at t = 3, exact) is now the operative datum of
  the K16 atom (§1.3); avenue 26's tools (Chebotarev cycle types, transitivity) are the ones that apply.
* Row 25 (dessins / passports): **unchanged** (no new client).
* Row 29 (LND / slices): **LOWER back to unchanged baseline** — the 0200Z RAISE rode on the receiver
  unification, which is corrected.
* Row 36 (guided CE search): **RETARGET** to POSDIM + J-test on the ℓ = 1 fibres of the 12 (Mm15, M12_17
  ×3, M9_20 ×2, Mm12) and to a Γ_t point with `T_top = 0` at t ≥ 8 (refutes (V0)_t, not JC2).
* Program rows (overlay 20260904/05): "Moh ≤ 100 residual = a solver wall" → **RETYPE** "solver wall on
  the class UNIONS; V-stems, the disc-assignment case split and band-wise emission untried" (§2.2);
  "K16 needs a genuinely new idea" → **RETYPE** "K16 = discriminant avoidance; Galois all-or-nothing +
  one-point certificate; irreducibility of Γ_t is the new target" (§1.3); "source→receiver coverage is
  the real all-degree gap" → **LOWER/RETYPE** "coverage is finite per degree once the s′ ≥ 3 necessity
  lemma is sourced; the all-degree gap is the uniform kill, K16 its prototype" (§3); "D = 108 CLOSED at
  skeleton level" → **unchanged from 0200Z RETYPE** (eight u_s = 1 rows owed).
* All other rows (3–24, 27–28, 30–35, 37–46): unchanged, no new evidence this round.

**Bottlenecks reranked (proof side).** (P1) the s′ ≥ 3 NECESSITY lemma (Card C) — every ≤ 100 kill and every
"covered degree" stands on it. (P2) the twelve fibre certificates — running; fallback Card B. (P3) K16
via Card A: irreducibility of Γ_t + one-point certificate (this round's new lever). (P4) the eight
u_s = 1 rows of D = 108 (same builder as P2). (P5) U-NEGATIVE (R6): source it impossible or give it a chart.
STOP: any further fixed-t (V0) std at t ≥ 8; any u_s ≥ 2 census sweep before P1; any "receiver
emptiness ⇒ source emptiness" claim without a proved pullback (17(mmmmmm)).

**Disproof side.** (C1) POSDIM + J = nonzero constant on an ℓ = 1 fibre of the 12 (the smallest unvisited
receiver slots below Moh's bound). (C2) a simple point of Γ_t with `T_top = 0` at some t ≥ 8 (kills (V0)_t;
(8.1) would then need the finer τ test). (C3) τ ≠ 0 on the t = 11 rational fibre (d = −2). Nothing else is
a signal; a modular point on a necessary over-approximation is never a counterexample.

**Contract items, named.** Genuinely new avenue/mechanism: the Galois all-or-nothing lemma on the reduced
EN curve Γ_t and its one-point certificate (§1.3). New connection: K16's clause (ii) ↔ avenue 26 (Galois
group of a determinantal point set) ↔ the discriminant-avoidance typing of generic weighted systems
(§1.2); also Moh's floor `B_safe` ↔ an exhaustive disc-assignment case split (§2.2), the same move that
made the split-window survivor list a theorem in 17(iiiiii). Strongest proof attack: Card C then the
certificates (Moh ≤ 100); Card A (K16). Strongest falsification: (C1). Software acceleration / decisive
experiment: the radical-mod-p + one-evaluation instrument for (V0)-tail at t = 8 (T2), replacing the
1800 s std wall.

## 6. Idea cards (three)

### Card A — K16-GAMMA-GALOIS: irreducibility of Γ_t and the one-point certificate (NEW)

*Object.* `Γ_t = V(I_2(N)) ⊂ Spec P_t`, reduced on `b4 ≠ 0` (measured t = 3, 4, 5 mod p; t = 3 exact), one
Galois orbit over A_3 at t = 3 (exact).
*Claim to test.* `k_t := #{A_t-orbits of Γ_t ∩ {b4 ≠ 0}} = 1` for t = 4, 5 (then 6). If so, clause (ii) of RANK
at t is ONE nonvanishing `T_{t,2t−1}(P, β(P)) ≠ 0` at ONE simple point, provable by a modular evaluation at
a good-reduction prime (§1.3), and the all-t atom is (I) irreducibility for all t + (II) one uniform point.
*Cheapest discriminator.* `gammax_t4.sing` / `gammax2_t4.sing` (exact factorization of the degree-46 point
polynomial over Q(√15); running at seal), then t = 5 (degree 265 over Q(√2)) on a c7i worker: ≤ 1 fleet-hour.
*Dependencies.* Frozen rank-lane rows (`box/k16rank-20260903/terminal_t{t}_exact_none.out`), Singular
`radical`/`finduni`/`factorize` over `(0,yy)` with `minpoly`; no new theory.
*Outcomes.* `k_4 = k_5 = 1` ⇒ launch (I) as a monodromy lane (the EN structure of N and the family in t are
the inputs; avenue 26 tools) and (II) as a formal-point lane (Astra's non-truncating (F3) solutions as
candidate points on an analytic thickening; the boundary orbits at b4 = 0 as candidate rational points —
note t = 3 has a rational boundary point, the q2-axis, at which `T_top` is an explicit scalar). `k_t ≥ 2`
with bounded orbit count ⇒ the lemma applies orbit-wise; the orbit sizes become the structure to explain.
`k_t` growing with t ⇒ the lemma still holds but buys only a constant factor; record and stop.
*Stop condition.* 2 lane-hours for T1 at t = 4, 5; one lane-day for (I)/(II) design.
*Expected information gain.* High: it is the first K16 statement whose fixed-t cost does not scale with a
Gröbner basis, and the first that names the field of definition as the datum.

### Card B — MOH-DISC-SPLIT: the disc-assignment case split for the 12 fibres (structural fallback)

*Content.* Replace the single floor `B_safe = V′_2 δ′_1 + u′ δ′_{s′}` by the exhaustive family of floors
`B(a) = V′_2 δ′_1 + Σ_{j=2}^{s′} a_j δ′_j`, `Σ a_j = u′`, one chart per assignment, each a necessary
over-approximation for the pairs whose u′ outer roots sit in the discs `(D′_j)^{a_j}`; run per V-stem;
eliminate band-wise at emission (memory: order-chart-rows-are-dense); Stage A prefix ladders mod p, Stage B
exact-Q on the certificate subset.
*Validation (before any verdict).* (i) the union of the case inventories equals the B_safe inventory
(control: Mm15, 126 unknowns, `coker = 0`); (ii) the K = 8 (24,16;18;7;4) kill reproduces; (iii) a
NON-EMPTY control (`⟨c−1, Tc−1⟩`) stays non-unit; (iv) every case script prints `rows > 0` (memory:
native-builder-transext-oom — an empty rows file is an OOM, not a kill).
*Cheapest discriminator.* Mm12 (77 unknowns, s′ = 4, u′ = 7 → 36 cases) and M2_9 V3_8 (88, s′ = 3, u′ = 3 →
10 cases): if every case is UNIT in ≤ 10 min on one core, the 12 fibres are a one-fleet-hour job; a POSDIM
case is an honest counterexample slot (extract, J-test); a TIMEOUT case is the residual, now typed by disc
assignment.
*Dependencies.* Card C for the necessity of `B(a)` (the same source step as `B_safe`); the fixed builder
(`builder_fix.py`); `staged_band_emitter.py`.
*Stop condition.* One lane-day for the emitter + validation; then the fleet.
*Expected information gain.* Medium-high: converts the compute wall into ≤ 36 K = 8-sized charts per fibre and,
as a by-product, records WHICH assignment carries the last survivor — the datum the uniform (T) needs.

### Card C — MOH-S3-NECESSITY: source the s′ ≥ 3 chart (the binding sub-gap; no CAS)

*Content.* Prove the Lemma of §3.3 from Moh's text: Prop 6.3's explicit substitution (p.197–198) gives
(P,Q) monic in π with π-degrees (n′, m′) and Jacobian `−(u_s/b)γ^ℓ`; the descended π-roots inherit the
tower `D′_2 ⊋ … ⊋ D′_{s′}` with radii Φ_eff (the (ℓ+1) factor from `θ = γ^{u_s}`... to be derived, not
assumed); Theorem 1.2 applied to the descended pair gives `ord a_j ≥ j·B(a)` for the actual disc assignment
a, hence `≥ j·B_safe`. Write it as a paper-grade lemma with the p.174 drop and the `u′ = 0` edge
(`OPEN[DESCENDED-U-PRIME-ZERO]`) handled explicitly.
*Cheapest discriminator.* Reproduce Appendix II p.207 (5/5) from the lemma's statement alone; reproduce the
promoted (99,66) S5/S6 descents (ℓ = 8); exhibit the (25,15) sub-slice (17(ggggg)) as the negative
control (what a non-necessary inventory looks like).
*Dependencies.* Moh pp.174, 179, 194–199, 207 (page images); 17(gggggg) (radius), 17(nnnnnn) (Φ_eff).
*Stop condition.* 3 lane-hours; if a step is unsourced, return the typed gap (which hypothesis, which page)
rather than a cap or analogy.
*Expected information gain.* Decisive for Q2/Q3: with it, every UNIT certificate is a paper-grade kill and
per-degree coverage is mechanical; without it, nothing above s′ = 2 is promotable.

## 7. Lanes — the single first lane; continue/redesign/stop for the running lane (receipt only)

**The single first lane: `k16-gamma-galois-<seat>` (Card A).** Exact factorization of the Γ_t point
polynomials at t = 4 (desk) and t = 5 (worker), the all-or-nothing lemma written out with its Hensel
step, and the one-evaluation instrument run at t = 6 (radical mod p already at vdim 1548; the radical did
not finish in the 540 s desk cap) and t = 8. Seat: Astra or Fable (a Galois-theoretic design lane);
≤ 4 lane-hours. Card C is the second lane (Opus: it owns the Prop 5.4/6.3 source work) and should start in
the same tick because every Moh ≤ 100 certificate depends on it.

**`moh14-msolve-sol56` (running; receipt `initial_status=RUNNING`, no `final_status`): CONTINUE with three
receipts.** (1) Type every char-0 `[1]` from msolve `-g` as first-prime trace until the rational
certificate is reconstructed and re-checked (COORDINATION Replayability; the packet's "UNIT [1] modular
signals" are Stage A). (2) Report per FIBRE, not per class; a union TIMEOUT is not a class verdict, a union
UNIT is (dominance in the proved direction). (3) On stall at the K′ = 4, ℓ = 3 fibres (333/378), do not buy
more solver: hand the fibre to Card B (disc-assignment split, V-stems, band-wise emission). Do not promote
any kill before Card C lands — label them PROVISIONAL[S3-NECESSITY].

## 8. Campaign-systems check — UPGRADE (rotation: adapter/engine reliability)

**UPGRADE: an msolve verdict wrapper that fails closed on first-prime units.** Symptom: COORDINATION
already records that msolve 0.10.1 char-0 `-g` may print a unit basis computed at the first machine prime
under a characteristic-0 header; the running lane is the first campaign consumer of msolve at scale and the
packet already quotes "UNIT [1] modular signals". Without a mechanical gate, a first-prime `[1]` will be
pasted into AUDIT as a kill. Implementation: `box/lib/msolve_verdict.py` — runs msolve, parses the header
and the basis, and prints `MS_VERDICT UNIT_CHAR0` ONLY if a rational reconstruction completed (msolve's
own `-P`/verbosity trace or a re-check of the returned basis over Q by Singular `reduce(1, std(S))` on the
certificate subset), else `MS_VERDICT UNIT_FIRST_PRIME_TRACE`; any other outcome `NONUNIT`/`TIMEOUT`.
Smallest useful test (old-pass/new-fail): the ideal `⟨xy − 1, x − p⟩ ⊂ Q[x,y]` with p the first machine
prime is unit mod p and non-unit over Q (the point `x = p, y = 1/p`); the wrapper must print
`UNIT_FIRST_PRIME_TRACE`, never `UNIT_CHAR0`; the positive control `⟨c, Tc − 1⟩` must print `UNIT_CHAR0`.
Cost: one lane-hour; zero regression risk (a wrapper, no engine change); it is the promotion gate the
packet's §1 already needs.

## 9. OPENs

OPENS RAISED

- `OPEN[K16-GAMMA-IRREDUCIBLE]` — is `Γ_t ∩ {b4 ≠ 0}` a single Galois orbit over A_t for all t ≥ 3?
  QUANTITY: the orbit count `k_t` satisfies `k_3 = 1` (exact); decide whether `k_4 = 1` and `k_5 = 1` by exact
  factorization of the degree-46 and degree-265 point polynomials (≤ 1 fleet-hour), then prove `k_t = 1` for
  all t or exhibit the first t with `k_t ≥ 2`.
- `OPEN[K16-ONE-POINT-CERTIFICATE]` — the all-or-nothing instrument for clause (ii).
  QUANTITY: for each t with `k_t = 1`, clause (ii)_t holds iff `T_{t,2t−1}(P, β(P)) ≠ 0` at one simple point P;
  the fixed-t cost is one radical mod p (272 s at t = 5; bound the t = 8 cost ≤ 1 worker-day) plus one
  evaluation, i.e. at most 1 nonvanishing per orbit instead of `d_Γ(t)`.
- `OPEN[K16-GAMMA-REDUCED-T6]` — reducedness of Γ_6 on b4 ≠ 0.
  QUANTITY: `vdim(Γ_6 ∩ {b4=1}) = 1548 = d_Γ(6)`; decide whether the reduced point count is `= 1548`
  (radical mod 32003; did not finish in the 540 s desk cap) — a smaller count would be the first multiplicity.
- `OPEN[MOH-S3-NECESSITY]` — the s′ ≥ 3 order chart as a sourced necessary over-approximation (Card C).
  QUANTITY: the number of unsourced steps in the chain Prop 6.3 → Φ_eff → Theorem 1.2 floor must be brought
  to `= 0`; today it is `≥ 1` (Φ_eff at s′ ≥ 3 is validated only at s′ = 2, 5/5 rows of p.207).
- `OPEN[MOH-DISC-ASSIGNMENT-SPLIT]` — the exhaustive case split of the u′ outer roots (Card B).
  QUANTITY: case count `= C(u′ + s′ − 2, s′ − 2)` per fibre, at most 36 on the 12 fibres (Mm12, u′ = 7,
  s′ = 4); the union of the case inventories must equal the B_safe inventory (`coker = 0`) before any case
  UNIT counts.

OPENS ANSWERED OR STANDING (not raised here)

- The exact-square OPENs of 17(oooooo) (`K16-SOCLE-LAW`, `K16-TAU-EXPONENT-T9`) stand unchanged; T4 of §1.4
  is their test.
- `MOH-LE100-ORDER-CHART` (17(ssssss)) stands; §2 types what a UNIT on it proves and Card B is its fallback.
- `DESCENDED-U-PRIME-ZERO`, `CHILD-SEARCH-CONDITIONS` (17(nnnnnn)) stand; Card C must handle the first
  explicitly.
- `RECEIVER-ATLAS` / `ROUTING-MAPS` (0200Z synthesis): re-typed in §3 — the Sigray interfaces are not on
  Moh's line; the receiver is a routing object (17(mmmmmm)); the binding sub-gap is `MOH-S3-NECESSITY`.
- Standing from 0200Z and still unrun: the ℓ = 1 fold test at t = 2 (Card A of that round) and the
  degree-wide D = 108 eight rows.

## 10. Collision scan (`ops/open_collision.py --root .`, filtered in the shell of every same-round file and the running lane tag)

Full filtered output: `box/ideation1200-fable5-20260905/collision_scan.filtered.txt` (status CANDIDATES). Dispositions:

- `OPEN[K16-GAMMA-IRREDUCIBLE]`: NONE.
- `OPEN[K16-ONE-POINT-CERTIFICATE]`: lexical hits on the K16 deltas 17(gggg), (iiii), (tttt), (iiiii), (qqqqq)
  FITT/EN-CURVE, (uuuuu), (zzzzz), (nnnnnn), (oooooo), (pppppp), the 0200Z coordinator blind submission and the
  `minor-empty-disc-sol56` prompt — all KNOWN context (the FITT/EN-CURVE statements are the charged inputs of
  §1.1); none states an orbit-wise or Galois all-or-nothing certificate, none closes the OPEN.
- `OPEN[K16-GAMMA-REDUCED-T6]`: NONE.
- `OPEN[MOH-S3-NECESSITY]`: lexical hits on 17(cccccc) (N1 enumeration) and 17(eeeeee) (0000Z synthesis) —
  KNOWN; neither sources Φ_eff at s′ ≥ 3.
- `OPEN[MOH-DISC-ASSIGNMENT-SPLIT]`: NONE.

## 11. Desk artifacts and status at seal

`box/ideation1200-fable5-20260905/`: `gamma_t{3,4,5,6}.sing/.out` (mod p; prefixes byte-identical to the frozen
`rank_t{t}_mod_p*_b0.sing` through `ideal BC=Bs+Cs;`, then `I2`, chart `b4 = 1`, `radical`, `finduni`,
`b4 = 0` slice); `gammax_t3.sing/.out` (exact over `(0,yy)`, `minpoly = H_3`: vdim 7, reduced 7, degree-7
polynomial irreducible over A_3); `gammax_t4.sing` (exact, radical variant: vdim 46 exact, radical not finished
in the 540 s cap); `gammax2_t4.sing` (exact, radical-free: vdim 46 exact, `finduni` + `factorize` over Q(√15)
still running at seal — its `.out` carries the answer to T1 at t = 4 if it lands); `gamma_t6`: vdim 1548 exact
mod p, radical not finished in the cap. Every run was foreground, one core, ≤ 540 s. No ledger, `jc2-lean`,
`ideation-20260905T1200Z-*` file or running-lane report was read or written. No exit-price assertion is
made, so no `charge_basis` line is due (validator status ABSENT expected).

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `41177`.
- Body SHA-256:
  `64bb87d7a6c5b421db6d8e8a05fc5f1d559251a0fd281400321d7011115dfed2`.
- Frozen basis: `da15d2603f0635123d107cb30a5d85a85918d996`.
