# HARVEST LANE (≤ 200 min wall; a waiting/verification job — do not start new solves unless instructed below). The frozen t2t3-direct report was cut off at its sealing step (its PENDING_* placeholders are unfilled and its termination claim is FALSE): on worker 172.30.0.55 (i-0fc259ff68e914869, r7i.16xlarge 512 GB; ssh as ubuntu with the jc2-fleet key; use -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null because the IP's host key was reused) TWO decisive jobs are STILL RUNNING under /home/ubuntu/t2t3-direct-20260906/ on the (99,66) δ=2 literal certificate subset d2-z55 (449 vars, 466 generators, 6,448,959 terms; stream SHA a78c6a46e4b50dec6df51712c9c4b8d3c717b52490488d6819563bc5e18044fe per the report — verify against the presentation file's actual hash): (1) Singular slimgb (pid ~73504; started ~04:45Z; 108 GB RSS at 05:30Z; run_capped 9,000 s wall) and (2) msolve -m 5000 -g 2 -t 64 (pid ~100509; started ~05:09Z; 38 GB; 9,000 s wall), outputs under runs/. TASK: (a) confirm both jobs, their exact start times and caps, and the input SHAs; (b) WAIT for them to finish or hit their caps (poll every 5 min; report progress: F4 degree lines from msolve -v 2, RSS); (c) when each ends, read its output EXACTLY: for msolve, is the output the unit ideal [1] (a MODULAR SIGNAL only, p = 1073741827 — state it so) or a nonunit basis (no force for the full ideal) or a timeout; for Singular exact-Q slimgb, is the fully delimited reduced basis literally {1} (a THEOREM-TIER kill of the (99,66) δ=2 direct chart, since the subset is literal) or nonunit (no force) or timeout; (d) copy ONLY the compact outputs (≤ 2 MB total: result headers, basis size, timing, RSS, the run_capped JSON receipts; NOT the streams) to box/t2t3-harvest-20260906/ on the host; (e) if BOTH jobs end without a result before your budget, leave them running and say so with the remaining wall; if both are finished (or capped), TERMINATE the worker with `sh ops/fleet/fleet.sh term i-0fc259ff68e914869` and confirm the state; (f) VERDICT: typed per job; if Singular gives {1}: state "DEAD-EXACT-Q[(99,66) δ=2 direct attained-T₂+T₃ chart]" with the subset SHA and the exact-Q script SHA (a different-model gate will replay it). FALLACY-v2 (a modular unit is a signal; a subset unit is conclusive for the full ideal; a subset nonunit is nothing). Host writes ≤ 2 MB; `df -h /` before writing. No ledger edits; no jc2-lean; no ideation-* input.
Report: xmodel/t2t3-harvest-sol56-20260906.md
Seal (<!-- BODY-END -->); 4-10KB; 200 min.
charged_input=xmodel/t2t3-direct-sol56-20260906.md
charged_input=box/t2t3-direct-20260906/build_direct.py
charged_input=box/t2t3-direct-20260906/make_sound_prefix.py
charged_input=ops/fleet/fleet.sh
charged_input=FALLACY-v2.md

Your inputs are frozen read-only copies in {{LANE_INPUTS}};
verify these SHA-256 hashes first, MECHANICALLY: the receipt `xmodel/t2t3-harvest-sol56-20260906.run.v2` carries `charged_input_<i>_sha256=`/`_basename=` lines; build the manifest with awk and run `sha256sum -c`; stop only on a real content mismatch:

```text
a0c3c3a0810e1765924ea06a8fdb6b0665feb3884195ea1514f01303eca8817c  {{LANE_INPUTS}}/t2t3-direct-sol56-20260906.md
77ef23b86f655e08a11913cf838a037c82adbb89cd1bc4a7e86c6471d034be3e  {{LANE_INPUTS}}/build_direct.py
92710ff81699850cd2b9fa93363bdda6d16b796db80f13365bd32e13c0174af2  {{LANE_INPUTS}}/make_sound_prefix.py
a9da94d341a691942e9d6a6a6d90d97008b2d7baa1cadb2592481cf6b9c11c4c  {{LANE_INPUTS}}/fleet.sh
e47fd16cfcc91bc7bfdac4ba1b5f46152db8235e6609dca6e29a5966549c38f5  {{LANE_INPUTS}}/FALLACY-v2.md
```
