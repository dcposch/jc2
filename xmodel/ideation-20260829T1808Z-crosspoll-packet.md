# JC2 round `20260829T1808Z` — adversarial cross-pollination packet

Basis: `e930fa90b8ee9d86220a2cff72fba94e04b20baa`  
Lifecycle: all four blind reports sealed; post-blind exact packages sealed;
pre-synthesis  
Purpose: independently break or confirm the full K00 valuation-two exclusion,
adjudicate the proposed global-selector mechanisms, and choose the smallest
nonduplicative mathematical and systems descendants.

## Sealed blind inputs

```text
bfa137c567e366dca26c6d0bc4a8824912cbba03d74df2bee27264000bcfb0cb
  xmodel/ideation-20260829T1808Z-sol56.md
  body 27206 / 190a5e108b767b99cdb0cbf7303bc64d8537bf0f4161324598735f8097308b88
37d1942a2ba6585c6b7d977aa5d20fd994008dbdef250a05c0425b92ac29f6ee
  xmodel/ideation-20260829T1808Z-fable5.md
  body 28151 / 1e530d1f69b8300b297e7c690b96a007b6ad85e58b62536ab017292d7800f1ee
2f65a96ff2ba2fc5d573b90c41aca57cfffb4efbe6c3bbdd2a3ea914079032cc
  xmodel/ideation-20260829T1808Z-opus5.md
  body 47459 / 486b11c0082831f295f0505fee5482d1ab1d955964e793b5a93914eed5ebdc86
190aae86c664627a8668dc219a49d8ffad7f09e4e90bc4559aa531e35288b6d5
  xmodel/ideation-20260829T1808Z-grok46.md
  body 34560 / 9257ea3daecedb627746d1b8da879776c0ccc4480fcfc77f01160a51681cb50a
```

State packet:

```text
f58ee9b50d640860fa36f34f12f39bcbb0f8fb852e2ace90be8e62323e09ccfb
  xmodel/ideation-20260829T1808Z-state.md
  body 9040 / d48e33c594731b4e6f0761c21c28460a15e15a7a9ea80801692b2fdc1a62529b
```

All four blind run records ended `DONE`, exit zero, with
`charge_basis_status=ABSENT` and stable prompt, adapter, validator, and
`FALLACY-v2` inputs.  Read all four reports completely.  Agreement is evidence
about a computation, never proof.

Custody exception: Opus disclosed that one bare recursive search traversed the
fenced `jc2-lean` path before being killed.  It states that no fenced content
was used in any mathematical claim and no fenced object was modified.  Treat
this as a process violation to repair, not evidence for any claim.  Do not
repeat it: no `jc2-lean` access of any kind is permitted in this round.

## Post-blind exact evidence

The following may be consumed now but must not alter blind novelty
attribution.

```text
2ebd1d78391a145c446d2113a1801e342118773c6fd9ce63be183dfd94f15a58
  xmodel/k00-r2-full-rank-fan-provisional-sol56-20260829.md
  body 11680 / c596347cf5e343854b6838f8b7f3241c623de78250676033fc364d07bc23098c
2c918d5b0d165977bdd2b598cf48d90cf3d0dfe0922ce27213dfa01dc255e7c4
  xmodel/k00-r2-full-rank-fan-replay-sol56-20260829.py
66f119e0e46b56f067802baa9118d0080370dc404878f9db667284c2d45d54a0
  xmodel/u1-ham-kum-conn-stop-audit-sol56-20260829.md
  body 5520 / ad2d79d8e220234a315d63dd8e5b9d264084dbc824a3dc804a2588988e7813a9
```

The K00 replay reconstructs 569 frozen source tails with pure stdlib exact
arithmetic, reports four nonzero rank-two minors and all six rank cells empty,
passes mutation controls, and runs in about 2.6 seconds.  It is still
`PROVISIONAL_EXACT_DESK_THEOREM`: a different model must reconstruct, not
merely rerun or trust, its decisive identities.

The U1 audit stops Sol's blind `U1-HAM-KUM-CONN/v1` proposal: the conditional
connection formula is correct, but the reviewed U1 datum is fixed-fibre and
does not supply a varying `D`-stable Kummer algebra; the displayed form is
twisted-exact; and there is no typed map from characters to distinct positive
PCB excess events.  Attack the audit, but do not silently consume the blind
Avenue-16 raise.

## Candidate mechanisms to reconcile

- Sol: `TD12-SEXTET-INDUCED-CHAR/v1`, the now-stopped Hamiltonian--Kummer
  composition, full K00 `r=2` rank fan, `R345-JETFAN/v1`, and
  `INGESTCHECK/v1`.
- Fable: `EXCESS-e` ladder, `ATYPICAL-EXISTENCE`, `AM2-PLACE-LEDGER`, K00
  stratum replay, and a body-seal validator.
- Opus: `SCALE-PIN` / per-root `RPMC(1)`, exact nonproperness/inertia ledger,
  minimal-topological-degree descent, a valuation calendar, and
  `FENCE-DEFAULT/v1`.
- Grok: `K00-V25-FIRST-LOAD-CALENDAR`, an actual-automorphism Section-7/PCB
  typing control, conditional zero-slack detour tests, `K00-F9F11-REPLAY`, and
  a body-seal validator.

## Mandatory mathematical attacks

### 1. Hostile audit of the K00 valuation-two theorem

Independently reconstruct the decisive statements from the frozen source
semantics and exact tails.  Do not accept the report or replay as an oracle.
Verify or break all six cells:

1. the leading-cone parametrization
   `x=(2b+2u,a,b,8a+v,b-u,16a+4v)`, the rowwise factorization of `DQ(x)`, and
   the rank fan `rank 0/1/2` governed by `Delta=u^2+64v^2`;
2. leading rank one: grade-six row 6 is
   `u(192v^2-u^2)/65536` and is a unit on both `u=+-8iv`, `v!=0` branches;
3. leading rank two: grade five forces `A(y)=B(y)=0`; at grade six, check the
   two zero-image rows and the two branches `u=0` and `u^2=192v^2`, including
   the stated incompatible linear equations and the nonzero `S5`/`S7`
   constants;
4. leading rank zero/old plane, next rank two: reconstruct the grade-eight
   `D,F` identities and prove their common zero is absent on the rank-two
   open;
5. old plane, next rank zero: check invariance under arbitrary old-plane `y`
   and the two promoted `y=0` cubics, not just a special slice;
6. old plane, next rank one: on `u=8iv`, verify grade seven, the two
   grade-eight rows forcing `lambda=0`, `s=8it`, and the remaining
   `-(5i/16) kappa t^3`; verify the conjugate branch separately.

Inspect the replay and its mutation controls, but also use at least one
independent hand, symbolic, or differently organized exact check.  Give a
binary `PASS / PASS_WITH_REPAIR / FAIL`, name any repair, and state the maximum
typed theorem.  The intended scope is only: field-valued compatible jets of
exact valuation two, normalized V20R2 source, through grade eight.  Do not turn
it into a formal arc, algebraic germ, polynomial map, counterexample, or JC2
result.  If it passes, decide whether promotion leaves precisely valuations
3,4,5 after composing with the already promoted `r=1` and `r>=6` exclusions.

### 2. Audit the global-selector claims

Hostile-audit Opus `OP-1` through `OP-6` rather than voting on them:

- Check every sign and normalization in `Delta_P=Lambda_P`, including local
  intersection multiplicity and generic-`nu` assumptions.
- Decide whether `RPMC(C)` really becomes
  `a_P b_P/nu_P <= C mu_P/B`, whether this is equivalent to the existing
  per-point lane or only sufficient, and whether `mu_P/B` is genuinely absent
  from the reduced interface.
- Re-derive the nonproperness defect and Riemann--Hurwitz formulas.  Check
  finiteness, connectedness, ramification support, multiplicities, and sums
  over components/values.
- Check the claimed exact meridian cycle type at a generic point of `A(F)`.
  Separate a local cycle calculation from a global determined passport, and
  decide whether Avenues 7, 25, and 26 warrant any raise.
- Check the pure-Sigray minimal-`td` selection: is normalization licensed for
  an arbitrary minimal-`td` counterexample, does it preserve `td`, and what
  source-audit debt replaces GGV minimality?  The block-quotient-to-smaller-
  Keller-map seam must remain explicit.

Classify each as `NEW / KNOWN-EQUIVALENT / DUPLICATE / SCOPE-CONFLICT /
REFUTED / PROVISIONAL`, with the smallest exact theorem worth retaining.

### 3. Audit the other proof-side proposals

- Fable `EXCESS-e`: check the arithmetic that `EXCESS-1` would eliminate S17
  and the zero-slack D13/E11 cases, the precise typical-fibre scope, and whether
  full PCB would eliminate `U1*(R)`.  Do not merge flags, places, or series.
- Fable `ATYPICAL-EXISTENCE`: determine whether a nonproper Keller coordinate
  must have an atypical value in the required sense and, separately, whether
  atypicality can force `EXCESS-1`.  Stop at the missing bridge if present.
- Fable `AM2-PLACE-LEDGER`: decide whether Abhyankar--Moh applied per actual
  component of `A(F)` supplies any new equality before a full boundary contact
  table is known, or is underdetermined/duplicative.
- Sol `TD12-SEXTET-INDUCED-CHAR`: test whether a parent deck character or
  full-index condition on A7/C5/B25/S17/D13/E11 is a genuinely new source
  discriminator, or merely repackages existing character machinery without a
  typed occurrence map.
- Grok's actual-automorphism Section-7/PCB control: decide whether the objects
  are defined at `td=1`; use any failure to sharpen hypotheses, not as a
  counterexample.  Check its valuation calendar against the exact contraction
  budget and the new `r=2` theorem.
- Recheck the U1 Hamiltonian--Kummer STOP audit.  Reopen only if it supplies
  the varying source family, receiver map, and no-duplication assignment named
  in its gate.

### 4. Choose the fastest portfolio after review

New work need not wait for every review.  Assuming provisional results only at
their exact type, select at most three next mathematical launches that jointly
cover proof, falsification, and a bypass.  Give each a falsifiable theorem or
experiment, dependencies, owner/reviewer split, desk/AWS placement, both-
outcome meaning, and stop rule.  Decide among at least:

```text
K00 valuations 3/4/5 (calendar versus JETFAN order),
SCALE-AUDIT / SCALE-PIN,
exact inertia/passport stage 1,
minimal-td block descent,
TD12 inherited-character preflight,
AM2 / EXCESS source bridge.
```

Do not let the conditional td12 laboratory masquerade as a global selector.

## Systems attack

Two uncommitted tools are available for review:

```text
59add10d8b291bb85a32c8f8a155d7ff0a7c4d6dd646fc164f48304b34fd80c1
  ops/seal.py (7438 bytes)
73f7e7bb554d3ba6936eb472539eebce3425eda619e8f118d1cf29f632a6ff49
  ops/test_seal.py (4306 bytes)
```

They implement exact standalone `BODY-END` marker validation, body bytes/hash,
frozen-basis checks, atomic stamping, regular-file/no-follow checks, and six
focused fixtures.  Review marker semantics, TOCTOU resistance, atomicity,
symlink/nonregular refusal, portability, basis parsing, and overlap with
existing validators.  Inline or quoted marker text must not count; exactly one
standalone marker must.

Also adjudicate `FENCE-DEFAULT/v1`.  A wrapper helps only when used; propose the
smallest enforceable default that prevents recursive tools from crossing the
fence without breaking legitimate explicit-path work.  Select exactly one
bounded systems trial for this checkpoint.  Do not modify `ops/lane.sh` while
these lanes are active.

## Required output

1. Recompute and list every charged full/body hash you use.
2. Give `PASS / PASS_WITH_REPAIR / FAIL` for the K00 valuation-two theorem,
   with the exact maximum theorem and source-scope consequence.
3. Give a deduplicated claim table covering every mandatory mathematical
   attack and preserving independent convergence.
4. Recommend at most three mathematical launches plus exactly one bounded
   systems trial, with ownership, placement, both-outcome value, and stops.
5. Give a stop/defer list and one synthesis recommendation under 250 words.

No web sweep, canonical edit, heavy local computation, AWS action, commit,
push, or `jc2-lean` access.  Exact desk work must stay below 60 CPU seconds and
1 GiB when known in advance.  Write only the output path named by the lane
prompt and seal the body if possible.  No new exit price is requested, so
`charge_basis` must remain absent.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `11344`.
- Body SHA-256: `0703f1f37b7dd445c4d8b74cb06e7f1f00b996c8af89d87dac6f2263628e1961`.
- Frozen basis: `e930fa90b8ee9d86220a2cff72fba94e04b20baa`.
