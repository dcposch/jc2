# K00 rank-five / grade-three rank-at-most-one combined hostile review

You are Fable 5, an equal-standing independent adversarial reviewer in the
plane Jacobian conjecture campaign. Work from repository root
`/Users/dc/code/math/jc2` on frozen Git basis
`92ebe92ad5986a47f01af9ed901260595dfed869`.

Write exactly one repository file:

`xmodel/k00-rank5-rankle1-combined-hostile-review-fable5-92e-20260829.md`

Do not edit anything else. Never read, list, stat, grep, build, or touch
`jc2-lean`. No web, network, commit, push, canonical edits, or AWS. Exact desk
algebra is allowed only in fresh `/tmp` scratch with explicit caps of at most
60 seconds CPU and 1 GiB address space per process; stop on warnings, signals,
cap hits, or swap. Do not launch a full-P6 job.

## Charged claims to review

1. `xmodel/k00-rank5-maxclass-next-sol56-20260829.md`
   full SHA-256
   `0a3af03ed8d07db852f833d0dc9ebe3ec151934308f4a139cf7ac72124f9b3cb`.
2. `xmodel/k00-grade3-rankle1-primary-grok46-92e-20260829.md`.
   Its full-file self-seal uses the report's stated zero-field convention;
   independently hash and record the actual bytes as well.

The first report is provisional Sol work; the second is an independent Grok
primary. Neither is a premise. Reconstruct every load-bearing object from the
frozen source artifacts and literal source labels. You may read already
reviewed campaign files only to identify sources or compare history; clearly
label any result you consume instead of replay.

Minimum source perimeter:

- `cases/max12_812_order2_u2_62_k00_grade7_fitting_atlas_v26_20260827/`
  exact atlas/source metadata;
- `cases/max12_812_order2_u2_62_k00_rank_compressed_atlas_v27_20260827/`
  exact prepasses/source labels and the reviewed V27 containments;
- the exact report paths and hashes they declare;
- `FALLACY-v2.md` (also appended by the lane runner).

## Review A: maximal-minor class theorem

Independently rebuild the frozen 7x7 matrix A and leading ideal B, with
declared ring maps and variable order. Audit:

- literal 5x5 minor census: total, zero/nonzero, exact polynomial equality
  classes up to literal equality/sign only, and W/M class counts;
- the lex representative M and its term count;
- membership/certificate claims `W in (q1,q3,q4)` and
  `M in (q1,q2,q4,q6)`; distinguish independently replayed facts from the
  already-reviewed W fact;
- the conclusion `I5(A) subset (q1,...,q6) subset B`, hence `B+I5(A)=B`
  and zero rank-exact-five points/charts on V(B);
- the composition with the reviewed V27 containments
  `I4(A),I3(A) subset B+I5(A)`, and exactly what it licenses;
- controls: ordinary versus optimized bases, localization unit/proper pairs,
  at least one sign/entry mutation, `ncols` versus `size`, exact-Q only.

Give one of `PASS`, `PASS_WITH_REPAIR`, `FAIL`, or `OPEN` for Review A.

## Review B: grade-three rank-at-most-one theorem

Independently rebuild the seven literal grade-three rows and audit:

- `P3=C*u+c3`, `C=A[:,1..6]`, all second derivatives zero, exact variable
  and ring-map custody, and the statement that `k10_*` is absent at grade 3;
- definitions `J2=B+I3(A)`, `J1=J2+I2(A)`,
  `R1=J1+I2(E3)`, `J0=J1+I1(C)`, `R0=J0+(c3)`, `E3=[C|c3]`;
- the engine-dependent labelled-slot/nonzero-generator distinction for
  minors; use `ncols` for loops and explain any 813/1225 discrepancy;
- the exact four linear generators of J0, primality/reduced two-plane,
  parametrization, `A=0`, `c3=0`, and `R0=J0`;
- `sqrt(R1)=J0`, including independent fourth-power or radical certificates
  and a Rabinowitsch unit/proper pair;
- whether these facts really imply: rank-exact-one grade-three compatible
  locus empty, rank-zero compatible locus exactly the displayed two-plane,
  every u works there; and whether any closure/geometric-field qualification
  is missing;
- the smallest valid successor and the requirement not to impose `k10_0`
  before grade 4.

Give one of `PASS`, `PASS_WITH_REPAIR`, `FAIL`, or `OPEN` for Review B.

## Required attacks and output discipline

Actively try wrong ring maps, swapped variable order, sign flips, one matrix
entry mutation, one c3 mutation, zero-slot mishandling, replacing radical
equality by ideal equality, confusing a projection with a lift, and confusing
rank(C) with rank(E3). Record exact commands/scripts, engine/version, caps,
stdout hashes, and enough serialized evidence for replay. If a claimed
certificate is too large, state the smallest sufficient hash/identity rather
than omitting the check.

End with:

- separate binary dispositions for A and B;
- a composition table stating the maximum jointly licensed theorem and all
  nonclaims (no Keller lift, no order-two exclusion, no JC2 conclusion);
- exact blockers/successors, deduplicated against the already reviewed
  chart-free rank-two packet;
- a `charge_basis` fenced block naming every report/source artifact actually
  used and its SHA-256;
- seal: body definition, byte count, body SHA-256, frozen basis. The lane
  runner will record the full-file hash out of band.

Do not trust status strings or prose summaries. Fail closed.
