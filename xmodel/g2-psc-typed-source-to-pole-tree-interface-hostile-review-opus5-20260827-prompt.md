# Hostile review: typed `G2-PSC` source-to-pole-tree interface

You are Opus 5, acting as a different-model hostile mathematical reviewer.
Review the frozen Sol Ultra producer

`xmodel/g2-psc-typed-source-to-pole-tree-interface-sol-ultra-20260827.md`

whose SHA-256 is
`4c3f2236a11f296f7eb194bb26e8cdc8ff0b91f10d8d7151e6314caa62629304`.

Write exactly one report to

`xmodel/g2-psc-typed-source-to-pole-tree-interface-hostile-review-opus5-20260827.md`

and do not edit any canonical ledger or any other campaign artifact.  Do not
enter, list, read, build, status-inspect, or modify `jc2-lean`.  Desk-scale
exact checks and primary-source reads are allowed; run no heavy local algebra
and launch no AWS.

Audit every load-bearing claim, especially:

1. The exact pair/fibre to corrected decorated Sigray pole-tree constructor:
   component ordering, rotation/sign, fibre choice, completions, both infinity
   charts, Puiseux/deck quotients, residual polynomials, and Q/jump/max labels.
2. The claimed exact local map from one coefficient-complete GGV type-II.b
   occurrence plus compatible branch evaluation to one Sigray flag segment.
   Reconstruct the ring diagram and identify any hidden field, root,
   presentation, or convergence assumption.
3. Whether current `CornerData` is genuinely too weak to be a source object,
   and whether the cited reviewed non-Keller `8_28` collision really proves
   ambient nonfunction rather than only a normalization or fibre mismatch.
4. Whether GGV5 Theorem 2.20 supplies only a selected-root, one-sector trace,
   or whether the producer missed a theorem that recovers all roots, the
   second chart, coverage, corrected denominator maxima, or paired residuals.
5. Whether the proposed exact-pair-backed, two-chart, all-root residual forest
   is sufficient and minimal for a composable contract.  Separate a data
   schema from the theorem that every Keller source produces and GGV captures
   it.
6. The three desk checks (family invariants, transport contract, prototype
   local compiler), novelty/history, and every claimed implication for
   `G2-PSC`, `G2-BD`, landing, cofinality, and JC2.

Give atom-by-atom verdicts from `CONFIRMED`, `CONFIRMED WITH REPAIR`,
`PROVISIONAL`, `REFUTED`, or `SCOPE-CONFLICT`.  State the shortest corrected
typed contract, the first genuinely missing theorem, what may be promoted,
and the cheapest next discriminator.  Include exact checks and source
citations without trusting producer prose.  End with output path, SHA-256,
exact model identity, checks run, and a scope firewall.
