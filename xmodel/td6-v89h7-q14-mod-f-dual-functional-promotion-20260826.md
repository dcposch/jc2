# Promotion: TD6 V89H7 minimal q14 dual quotient functional

Date: 2026-08-26

Lifecycle: **PROMOTED / HOSTILE-REVIEW CONFIRMED.**

## Promoted theorem

On the literal normalized three-center source slice, set

```text
q2=...=q13=0,
q15=0 only in the reviewed target-shear coordinate,
q14,q16,...,q24 independent and untruncated,
F=C*U-V^2+U^3=0,
```

and work on `D(U*H*B3)`.  Let `I_FIRST` be the ideal generated by all 38
original packed raw FIRST sources, after the exact two-sided chart
`C=(V^2-U^3)/U`.  Over `K=Frac(Q[V,U])`, the full retained-q FIRST pivot
block has a two-sided polynomial inverse, so reduction by its monic,
distinct-pivot rows is a unique normal form for the quotient by
`I_FIRST`.

The K-linear functional

```text
lambda(g) = E3 scalar coordinate 0 of the coefficient of
            q14 * (empty parameter monomial)
            in NF_FIRST(g mod F)
```

annihilates `I_FIRST` and satisfies

```text
lambda(P12) = N / [V^3*(V^2-4U^3)^2] != 0,
```

where `N` is the explicit nonzero polynomial frozen in the producer result.
The denominator is exact.  On `F=0`, `B3=V^4` and
`U*H=V^2-4U^3`, so every denominator factor is already a unit on the
registered open.

Thus literal P12 has a nonzero q14 cokernel class modulo full original
FIRST+`(F)` in this exact residue scope.  This is splitting-independent: it
does not use the earlier H5 chosen complement.

## Validation and custody

- Producer result:
  `cases/td6_c1_c2_c3_q14_mod_f_dual_functional_v89h7_aws_20260826/RESULT.md`,
  SHA256 `49b9dcba0bb5956dfdbb10293b7bdf3e440a39b127929089c2402ffb06db32cd`.
- Producer freeze SHA256:
  `7115c67a564b1f174a2e7337a1e64566b8ad2a422405c7243dfee9e8de77a6e0`.
- Hostile review:
  `xmodel/td6-v89h7-q14-mod-f-dual-functional-hostile-review-report-20260826.md`,
  SHA256 `99f7124e50db3daded967f42fe7591c06e27cfa05fe681537fed3aab5013ad6b`,
  verdict `CONFIRMED`.
- Dual AWS exact artifacts and stdout are byte-identical.  A fresh-prime
  check at `p=1000033`, `(C,V,U)=(15,4,1)` reproduces the complete 17-record
  class and gives `lambda(P12)=159760 != 0`.

## Scope firewall

This is not `1 in (P12,FIRST,F)`, a source-point exclusion, a low-q unit
chart, a q15 source-modulus theorem, total-Rees/source lifting, whole fixed
A3, TD6, SP-2, or JC2.  Restoring q13 and then the remaining low-q variables
requires separate exact polynomial-extension gates.

