# Fable 5 repair — LANDING-LEDGER LL-1 R3 provenance

You produced LL-1 R2. Work from `/Users/dc/code/math/jc2` and write a fresh,
immutable R3 packet plus exactly this report:

`xmodel/landing-ledger-ll1-r3-provenance-repair-fable5-20260829.md`

Target packet:
`cases/landing_ledger_ll1_r3_20260829/`

Read R2, your R2 report, and the complete Grok review
`xmodel/landing-ledger-ll1-r2-hostile-review-grok46-20260829.md`. Treat R2 as
immutable. The mathematics passed at LL-1 scope. Make only the narrow
provenance/test-quality repair required by that review:

- pin the exact current bytes of `ladder/BOOK-OFFAXIS.md` and
  `ladder/REDUCTION.md`, alongside the two source pins that already match;
- rederive the consumed P0/P1/section-4/T7 clauses against those exact bytes
  and state whether any intervening edit changes a decision (expected: no);
- replace A1's tautological `2*nu+2 == 2*nu+2` check with an honest frozen
  source/fixture parity gate for the charged 26-shape/351-route family claim,
  or explicitly remove that count from the packet's proved scope if the
  underlying engine artifact is unavailable. Never manufacture parity.

Preserve the R2 compiler/validator semantics and reviewed LL-1 decisions.
Regenerate a fresh manifest and all derived output; ordinary and `-O` tests,
recompile-byte-identity, cap-firewall, normalization, empty-UNCOVERED, D9,
and residue-book controls must still pass. Add a source-drift mutation that
fails closed. Inventory every R2-to-R3 byte/function change.

The report verdict may be only `SOURCE_READY_FOR_DIFFERENT_MODEL_REVIEW` or
`REPAIR_INCOMPLETE`; R3 cannot promote itself or authorize AWS. Include
full/body hashes and exact commands/counts.

Hard boundaries: no AWS, commit, push, canonical edits, heavy CAS, or local
long/high-memory process. Never access/list/search/build/status or control
`jc2-lean`; never run global `git status` or workspace-wide searches. Modify
only the requested R3 report and packet; use `/tmp` for scratch.
