# r2 semantic controls 6 and 10 — disabled runtime integration

Status: source candidate, UNEXECUTED. No registration or runtime authority.

First action 2026-09-10 13:12:57 UTC; hard cap 13:30:57, publication reserve
13:27:57. All eleven permitted input hashes matched before reads; owned
report and box were absent. Basis 0d39df3c9fd69c939a8420c54d03228b9077777d.

The deliverable is an owned-copy dispatcher delta, unchanged probe, disabled
registration contract and validation plan. The baseline coefficient artifact
is a future literal pin only, never read or copied in this task. Mutator source
was independently authored. ROOT released its terminal source/contract/report
at13:18; all three matched before WHOLE reads. Final source a896a809... is now
bound literally, still NEW/UNREVIEWED until the combined FIRST gate.

## Complete prepared delta

The owned dispatcher preserves the unchanged five-file science inventory,
authority object/tag, nine old preflights, six-field native closure, physical
identity checks, actual command supervision, caps, quiet barriers and durable
custody. The isolated wrapper adds only the mutator. No accepted scientific
file or original dispatcher is edited. probe.py is byte-identical.

The new tail is one silent unchecked mutator, two full fixtures/receipt moved
and frozen0444 under0555 BEFORE either check, then unchanged check.py twice.
All12 child labels must complete in order. There is no positive producer or
checker rerun. The13 other semantic controls remain NOT_RUN.

Receipt validation is metadata-only: exact schema/keysets/root/source/baseline
bindings, indices, bounded rational strings, actual file hashes/sizes, distinct
changed objects, literal fallback and exact output inventory. Reduced rational
arithmetic, largest-coefficient choice and whole inverse restoration are the
mutator's obligations; metadata booleans do not independently prove them.

Whole pinned checker read confirms four exact traceback frames for each
negative. Control6 reaches216 via329, not the later ell identity. Control10
reaches check_arithmetic208 via376(Psi) or383(K1/K0). Entire ASCII traceback
is matched, with only bounded native caret marker lines, normal exit1, no
signal/resource and empty stdout. Array-length change is not needed for Psi;
a zero coordinate deletes its term only when all seven coordinates vanish.

All code/baseline/native/registration remain outside tmpfs. Both fixtures
share15MiB aggregate packet reserve and all metadata share1MiB, within the
hard16MiB mount; no per-fixture allowance doubles capacity. Budgets remain
600wall/500CPU/2GiB; registered child sums475wall/386hardCPU are allocation
arithmetic, not measured performance. Sampled480 cutoff and no retry persist.
Final success is only TWO_SEMANTIC_REJECTIONS_CONFIRMED, science_outcome NONE.

## Manual changed-object and scope checks

Metadata-only hand checks: a baseline-identical fixture fails distinct hashes;
changing both fixtures to the same bytes fails distinctness; stale source or
receipt path fails exact binding; extra/chain traceback fails full grammar;
one successful negative cannot produce final success; both fixtures freeze
before the first run. These are static reasoning, NOT observed controls.
No source ideal/unit/point/finite-basis/degree or JC2 consequence is delivered.

## Remaining validation and custody

One combined FIRST review must cover this dispatcher delta plus the newly
released mutator. ROOT then owns any immutable registration, exact installed
native/interpreter/path/clock/vector binding and finite12-child execution.
No enabled authority or actual worker is claimed. The old ESRCH false-STOP
race remains intentionally unchanged; no hardening or extra control farm.
No runtime measurements or syntax compatibility tests were performed.

One necessary registration-interface issue was found independently: storing
the final ROOT JSON SHA inside that same JSON's literal mutator command is
self-referential. The candidate adds exactly one typed token substitution
ROOT_REGISTRATION_SHA256, only after the unique mutator ROOT-hash flag;
every other argv byte stays literal and the full expanded argv is compared
and recorded. Other commands reject that token. ROOT was notified before
this source-only candidate change. This explicit delta requires FIRST review;
it is not an accepted change to an enabled registration. No general expansion
or extra command carrier/framework is introduced.

## OPENS RAISED

No new canonical OPEN. Required pending quantities are one combined code
gate, one ROOT registration and two exact semantic negative observations;
the cheapest actual validation is the specified finite batch, not a positive
reconstruction rerun. Any registration-interface gap is stated explicitly.

## COLLISIONS AND AUTHORING

Owned targets were absent and existing inputs preserved. A relative-copy
path mistake briefly placed two same-byte copies under /home/ubuntu/box;
apply_patch removed those copies and recreated them at the correct absolute
owned paths. An empty misplaced directory remains. No other bytes changed;
parent notified. Read-scope records this exception rather than claiming
perfect own-path-only writing. All source remains STATIC/UNEXECUTED.

Own WHOLE text/read-scope/OPEN and exact collision check precede final marker.
All14 source hashes and all owned output hashes are retained in final custody.

ROOT acknowledged the typed single-slot hash substitution as a reasonable
FIRST-review candidate before sealing; no execution authority was added.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5574`.
- Body SHA-256:
  `cd82c7659d81fc081b5b83bbfb198cbc6dcfe3c32fa0733b2b90ab55b540e4c7`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
