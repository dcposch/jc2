# Blind whole-portfolio ideation — cube/max12 owner — `20260825T0427Z`

- Sealed packet: `xmodel/ideation-20260825T0427Z-packet.md`, SHA-256
  `dca3ca592abb19b4f0d7988eb611000e9b53c8e23b32337e4ba37647ea0a839e`.
- Basis: packet cutoff `2026-08-25T04:27Z`; no post-cutoff result is used in
  the ranking below.
- Status: **DEGRADED, NARROW DISCLOSED EXPOSURE**.  During the required
  history checksum I passed an explicitly shell-expanded `xmodel/*.md` list
  to `rg`; its later glob exclusion could not remove one already-expanded
  sibling path.  I saw only lines from the 0427 root submission concerning
  avenues 4/31 and the Q8 moving-Hensel/no-merger card.  I stopped immediately
  and did not inspect another 0427 submission.  Those Q8 dispositions and
  ideas remain listed because they are already forced by the sealed packet,
  but they receive **zero independent-vote and zero novelty credit** here.
  The AS Myhill--Nerode mechanism and the TD6 valuative-escape redesign were
  developed before the exposure and were not in the exposed lines.
- Evidence firewall: the TD6 fixed `A^3` closure and AS unreduced affine
  Kuranishi map remain producer-tier at cutoff; the Q8 candidate plane curve
  is not yet a quotient component.  Nothing below proves or disproves JC2.

## 1. Portfolio judgment

The new TD6 result changes the *kind* of transverse question.  Once the
`beta=0` affine fibre is empty, a solution over `C((beta))` cannot approach it
with all auxiliary coefficients integral: it must acquire a pole.  Thus a
finite dual-number perturbation of the existing unit obstruction is a source
identity probe, not a neighborhood or solvability probe.  The decisive TD6
object is now the finite set of source-honest valuation/initial systems at
projective infinity.

The strongest counterexample-side discriminator remains AS, but the next
object should not be another census or a fitted polynomial.  It should be the
minimal **right congruence** on canonical carry histories: two prefixes are
identified only when every licensed continuation has the same transition
behavior.  A same-summary/different-successor pair immediately falsifies a
proposed state; stabilization gives a genuine finite-state candidate; growth
quantifies the missing conductor.

Q8 remains the fastest bounded theorem client.  The moving-v Hensel/Padé lane
should finish the seven coordinate functions, substitute them into the
original rows, and discharge the six-item no-merger checklist.  Fixed-fibre
agreement, arithmetic irreducibility of the standalone `H`, or a smooth
projection point alone is not component membership and cannot select the
characteristic-zero contact grouping.

Ultimate proof walls remain full landing/coverage and an absolute or cofinal
type/degree bound.  Ultimate disproof walls remain all-depth state
sufficiency, a characteristic-zero polynomial algebraization, collision, and
an actual Keller pair.

## 2. Disposition over all 46 avenues

Compact vector (`U` unchanged, `R+` raise, `RO` reopen narrowly):

```text
 1 U   2 R+  3 U   4 R+  5 U   6 U   7 U   8 U   9 U  10 U
11 U  12 U  13 U  14 U  15 U  16 U  17 U  18 U  19 R+ 20 U
21 R+ 22 U  23 U  24 U  25 R+ 26 R+ 27 U  28 U  29 U  30 U
31 R+ 32 U  33 U  34 U  35 U  36 U  37 U  38 RO 39 U  40 U
41 U  42 U  43 U  44 U  45 U  46 U
```

Reasons for every change:

| Avenue | Change | Exact reason and scope |
|---:|:---:|---|
| 2 | `R+` | The fixed source-typed TD6 three-center section is now producer-exactly empty by an exhaustive affine divisor cover.  This creates a sharp projective-escape client, but does not improve landing, boundary-modulus coverage, `SP-2`, or a degree ceiling. |
| 4 | `R+` | Q8 moving-v Hensel reduces order-eight memory from over 50 GiB to about 48 MiB and supplies a credible formal-to-global graph reconstruction; AS also has a concrete all-depth state question.  Neither is algebraization yet.  **No independent-vote credit due to the disclosed exposure.** |
| 19 | `R+` | Correct source-divided rows, an exact terminal canonical branch, and a large explicit Kuranishi chart make a falsifiable finite-state program substantially sharper.  This raises the experiment, not the probability of a characteristic-zero counterexample. |
| 21 | `R+` | Canonical carry histories now support an exact right-congruence/minimal-state test.  Hensel sheets and finite-depth survival still do not remove `A_infinity`, bound support, or produce a rational deck descent. |
| 25 | `R+` | The Q8 infinity passport has become load-bearing and excludes the all-eight component at registered trajectory scope.  Passports alone remain generous; Taylor data and the second coordinate are still essential. |
| 26 | `R+` | Primitive Galois action turned contact grouping into an exact binary alternative and therefore demonstrated real leverage in one bounded client.  It supplies no global topological-degree bound. |
| 31 | `R+` | Q8 now has a named finite-flat/multiplicity-one no-merger problem, while TD6 has a named Rees/valuative escape problem.  These are concrete integrality clients rather than a generic Zariski-main slogan.  **No independent-vote credit for the Q8 part due to exposure.** |
| 38 | `RO` | Reopen only for the source-derived TD6 `beta` valuation fan: the empty affine special fibre forces any survivor to have a pole, so tropical feasibility followed by exact saturated initial ideals is logically complete for that deformation once a finite support-derived fan is proved.  Generic tropicalization, unsaturated initials, and unnamed Newton searches remain stopped. |

No avenue is newly lowered.  The canonical ledger already prices generic
sparse search, raw passports, cohomology slogans, unrestricted Witt depth,
and untyped formal work at their current low/held levels; the cutoff news
does not justify paying for them again.

## 3. Reranked bottlenecks

### Proof side

1. **Executable:** classify TD6 escape at infinity under the first
   source-proven transverse modulus, rather than infer a neighborhood from
   finite jets of an empty fibre.
2. **Executable:** turn the Q8 candidate plane curve into an actual quotient
   component with all seven coordinates and full contact limits, then apply
   multiplicity-one no-merger and the reviewed infinity theorem.
3. **Structural:** transport/landing with complete boundary provenance and
   an absolute or cofinal complexity bound.  Neither item 1 nor item 2 moves
   this wall by itself.

### Counterexample/falsification side

1. **Executable:** prove or falsify a canonical AS carry right congruence and
   time-homogeneous transition section.
2. **Structural:** bound map/support complexity along an infinite branch; a
   finite recurrent residue graph is still only a formal characteristic-three
   object.
3. **Endgame:** characteristic-zero polynomial algebraization preserving an
   actual collision and determinant one.

The cheapest experiment separating the leading allocations is asymmetric:
run the AS same-signature/different-successor collision test and the TD6
support-only valuation feasibility scan.  Both can fail decisively before a
large exact elimination.  Q8 already has active high-information producers
and should not be displaced by a fresh speculative computation.

## 4. New mechanism and cross-avenue connection

### New mechanism: canonical-carry Myhill--Nerode quotient

For a source-honest canonical AS prefix `a`, let `L(a)` be the set of all
licensed future lower-digit words together with their accept/obstruct and
carry-output labels.  Define

```text
a ~ b  iff  L(a)=L(b).
```

This is the minimal right congruence, not a feature fit.  Partition refinement
starts from the exact integer carry signature and repeatedly splits a block
whenever one licensed next digit lands in different labeled blocks.  It gives
three proof-grade outcomes: a stable finite quotient, a concrete
same-state/different-successor refutation, or certified state growth.  The
history checksum found finite-state, matrix-state, SCC, and carry-signature
proposals, but no prior Myhill--Nerode/right-congruence or `p`-kernel
formulation in the canonical ledgers and prior `.md` reports.  This is the
genuinely new mechanism of this submission.

It connects avenues 19 and 21 to avenue 8: a finite right congruence is the
automata analogue of a uniform formal-inverse truncation rule.  If it
stabilizes, one obtains a finite `3`-kernel-style object on which recurrence
can be stated honestly; if it grows, the separating words give an explicit
carry-conductor lower bound.  Christol/automatic-sequence language may
classify a resulting characteristic-three formal series, but it does **not**
algebraize it over characteristic zero and is not itself a counterexample.

### New sharpening of a known connection: empty fibre means valuative escape

The 20260824T2024Z synthesis already proposed TD6 projective escape/Fitting,
so that headline is `KNOWN-COMPOSITE`, not new.  The closed trivariate atlas
now makes the logical reduction sharper: for a putative `beta`-family,
rescale each auxiliary coefficient by its minimum `beta`-valuation.  The
leading terms solve a source-derived initial ideal on the projective
boundary.  This is a direct connection between avenue 2, avenue 31's Rees
valuations/properness, and a narrowly reopened avenue 38.  It also explains
why a finite dual-number adjoint of the nonzero constant remainder cannot by
itself distinguish nearby existence: `-k/50 + epsilon*a` is still a unit.

## 5. Detailed cards (three only)

### Card A — `TD6-BETA-VALUATIVE-ESCAPE` — strongest proof attack

- **History label:** `KNOWN-COMPOSITE / MATERIAL REDESIGN`.  Projective
  escape was selected in the 20260824T2024Z synthesis; the new cutoff makes
  the valuation-first formulation mandatory and makes finite affine jets
  non-decisive.
- **Dependencies:** producer-exact fixed `A^3_(C,V,U)` closure (review still
  charged), the full source-typed equations with the q-boundary class
  `beta`, every localizer, and a proved finite support-derived set of weight
  cones.  Dead stretch `d6` remains a separate next modulus.
- **Cheapest discriminator:** before any Gröbner basis, solve the min-plus
  valuation constraints of the transport, first-band, and genuine P12 rows,
  fixing `ord(beta)=1` and requiring at least one negative auxiliary
  valuation.  For each feasible cone, form the exact saturated initial ideal
  and test the cheapest source row/unit first.  All substantive work is
  AWS-only.
- **All outcomes:** no feasible cone proves no Laurent-family escape for
  this deformation; feasible cones with unit initial ideals give an exact
  projective-boundary kill; a nonempty initial scheme freezes the smallest
  honest escape chart and refutes any claimed finite-neighborhood inference;
  an infinite/uncontrolled cone set is a type failure showing that the
  chosen source support or grading is incomplete.
- **Stop condition:** stop after the finite cone list is exhausted or at the
  first source-reconstructible survivor.  Do not continue finite dual jets
  after two successive derivatives of a unit obstruction give no new
  boundary information.
- **Expected information gain/cost:** very high/high.  The feasibility pass
  is cheap; only surviving cones consume exact elimination.  It tests the
  only way a generic `beta` solution can coexist with the empty special
  fibre.

### Card B — `AS-CARRY-MYHILL-NERODE` — strongest counterexample/falsifier

- **History label:** `NEW`.  It refines the known finite-state/carry-signature
  program by defining the coarsest *provably sufficient* state rather than
  fitting one.
- **Dependencies:** corrected integer source rows, canonical representative
  convention, the exact `3^13` Q9-chart carry-signature grouping, and an
  exact transition section retaining all spectators and divided carries.
- **Cheapest discriminator:** seek two prefixes with the same proposed
  signature but different labeled one-step successor multisets.  One pair is
  a complete state-sufficiency refutation.  If none appears, perform exact
  partition refinement over the enumerated transition relation and replay
  one representative per final block.
- **All outcomes:** a collision forces the minimal missing carry coordinate;
  a stable acyclic quotient kills this fixed `D=7` branch; a stable reachable
  SCC plus an exact section gives an all-depth characteristic-three formal
  candidate; continuing refinement/growth yields a quantitative conductor
  lower bound and stops the bounded-state counterexample program at this
  cap.  No outcome alone gives a characteristic-zero polynomial pair.
- **Stop condition:** stop immediately on a same-state/different-successor
  witness; otherwise stop after two consecutive depths fail to stabilize the
  partition unless a proved monotone state bound is available.
- **Expected information gain/cost:** very high/medium.  The decisive object
  is much smaller than literal completion enumeration and every negative
  result tells exactly which state datum is missing.

### Card C — `Q8-GRAPH-AND-NO-MERGER` — fastest bounded closure

- **History label:** `KNOWN-LIVE / DEGRADED INDEPENDENCE`.  This follows the
  sealed packet's active pipeline; the narrow sibling exposure removes all
  novelty and independent-vote credit from this card.
- **Dependencies:** candidate monic `H`, its standalone geometric
  integrality, moving-v Hensel coefficients, original eight quotient rows,
  localizer, all eight corrected contact coordinates, full-source Jacobian
  minors, the integral `Z_(127)` model, and the reviewed primitive/infinity
  theorem.
- **Cheapest discriminator:** continue simultaneous common-denominator Padé
  on all `7*190` sequences only while higher orders confirm it; accept only
  after exact substitution modulo `H` in all original rows.  Then compute
  finite limits on the simple `Q8bar` factor and the full local Jacobian at
  every contact.  AWS only.
- **All outcomes:** exact graph plus the six no-merger checks forces the
  all-eight characteristic-zero component and kills registered trajectories
  there; a failed recurrence means only that the denominator/order guess was
  too small; a direct-substitution failure refutes the candidate graph; a
  pole or wrong contact limit preserves the singleton alternative and hands
  a precise boundary to normalization/Taylor work.
- **Stop condition:** no Padé relation is accepted without global
  substitution.  Stop escalating blind order after two failed common-
  denominator models and switch to projective denominator/pole support or a
  direct generic basis.  Stop the whole card at the first failed no-merger
  checklist item and report that exact bridge.
- **Expected information gain/cost:** high/medium-high.  It can close the
  selected Q8 branch but not the other `(9,12)` leaves, `(8,12)`, or the
  global landing wall.

## 6. Software accelerator / decisive experiment

Build one **proof-carrying valuation and state-refinement compiler** rather
than three bespoke samplers:

1. a source polynomial presentation plus a distinguished parameter produces
   its finite feasible valuation fan, saturated initial ideals, denominator
   divisors, and old-pass/new-fail controls (TD6 and later Q8 pole support);
2. an exact integer transition relation produces the minimal right
   congruence, separating words, SCC/ranking certificates, and source-row
   hashes (AS);
3. every AWS job is content-addressed, pins its transitive closure and engine,
   fails on nonempty stderr unless classified, and emits membership/cofactor
   or original-row substitution certificates rather than solver verdicts.

This is an accelerator, not avenue 46 as a proof route.  msolve remains fully
quarantined; pure Singular or independently checked exact arithmetic is the
acceptance surface.

## 7. Continue / redesign / stop

- **Q8 — continue, with a hard acceptance gate.**  Finish moving-v orders,
  common-denominator reconstruction, and exact original-row substitution;
  then discharge all six no-merger items.  Stop interpreting more modular
  fibres or support-nullity boxes as component evidence.  This recommendation
  has zero independent-vote credit because of the disclosed exposure.
- **AS — continue and redesign around right-congruence falsification.**  Run
  the full `3^13` signature census once, but use it to construct/refute a
  canonical transition state.  Stop affine-unreduced polynomial fitting and
  literal lower-completion counts unless they distinguish canonical states.
- **TD6 — continue, but pivot from finite adjoint jets to projective
  escape.**  The q2 dual adjoint is useful only to expose a universal source
  identity or leading valuation; it cannot promote an affine neighborhood of
  an already empty fibre.  Classify q2 Laurent escape first, then use `d6` as
  the next matrix-changing control.  Preserve fixed-A3 scope until hostile
  union review closes.
- **Background — stop generic expansion.**  Keep exact Double-B custody and
  the directional-width landing falsifier bounded.  Do not reopen generic
  sparse search, unrestricted Witt depth, raw tropical prevarieties, new
  book cells, or Q8 rectangles.  Reserve one lane for hostile review and one
  for the ultimate landing/complexity wall rather than placing the full
  portfolio under Q8/AS/TD6 descendants.

## 8. Adversarial scope audit

1. A fixed source-typed TD6 `A^3` is not a neighborhood, TD6, `SP-2`, or a
   landing theorem.  Empty special fibre does not prevent `beta*x-1=0`-type
   escape.
2. A finite AS state graph, even with a cycle, is not an all-depth lift until
   state sufficiency, time homogeneity, and a transition section are proved;
   an all-depth characteristic-three lift is not a characteristic-zero
   polynomial collision.
3. A geometrically integral plane `H mod 127` is not a quotient component and
   cannot decide characteristic-zero grouping.  Full coordinates,
   multiplicity one, local uniqueness, and specialization are load-bearing.
4. Closing selected Q8 does not close all maximum twelve; closing maximum
   twelve would still be a bounded partial-`y` theorem, not a total-degree or
   cofinal JC2 theorem.

## 9. Post-cutoff operational note (not used above)

While this sealed scan was being written, the AWS moving-v order-16 Q8 lane
returned `order=16`, `final_fail=0`; orders 32 and 64 remained live on Box03
and Box02.  This is producer status after the packet cutoff and carries no
ideation-vote or mathematical-promotion weight in this report.
