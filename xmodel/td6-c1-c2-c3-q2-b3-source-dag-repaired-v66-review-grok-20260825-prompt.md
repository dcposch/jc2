# Hostile bounded review: TD6 V66 generic B3 source-DAG chart

Act as an adversarial algebra/source/custody reviewer.  This is a bounded,
read-only audit.  Do not edit producer/canonical files and do not rerun the
substantive replay, CAS, solver, Lean, or heavy Python.  You may hash files,
extract the small source archive privately, inspect source/evidence, and run
the lightweight custody verifier.

Read in full:

- `xmodel/td6-c1-c2-c3-q2-b3-source-dag-repaired-v66-aws-20260825.md`;
- `cases/td6_c1_c2_c3_q2_b3_source_dag_repaired_v66_aws_20260825/README.md`,
  all manifests/freeze/verifier/source archive, both AWS evidence trees, DAG,
  denominator ledger, and controls;
- the V60 ancestry erratum and reviewed V64 report only as needed to check the
  repaired row-ancestry convention.

Verify or refute:

1. The producer uses the exact birational `B3=0` chart for the fixed
   source-typed A3 `(C0,V0,U0)` q2-beta section.  Audit the rename of internal
   chart variables `(C,V)` to `(t,w)`, raw center formulas, inverse formulas,
   raw B3 identity, and chart provenance.  No weighted source scaling.
2. Polynomial beta and direct `q_beta'=1+2 beta t+25 t^24` are retained with
   fixed p-boundary/dead-stretch/F1/pole data.
3. Current N13 row13 traces through exactly previous `('X-1',14)` and
   original first rows.  `('X-1',0)` is only an independent quadratic cache/
   control, never a second N13 edge.  Audit current/previous omission controls.
4. Genuine P12 has the asserted 2,893 raw terms, 28 original first rows, and
   exact direct source lift; its N13 glue gives `-k/50`.  Check signs,
   q-prime/varying-echelon contributions, and that no affine surrogate is
   substituted.
5. The complete source-leaf plus pinned-P12 denominator radical is exactly
   `{t,w,t-2,2t-1,t^2-4t+2}`.  Inspect all ledger leaves and norm factors;
   do not accept a summary print if artifacts disagree.
6. Box02/r6d custody, rc zero, source closure, paired DAG/ledger equality,
   normalized-stream agreement, manifests/freeze, and negative controls.
7. The exact theorem scope is only
   `B3=0,D(t*w*(t-2)*(2t-1)*(t^2-4t+2))` in fixed A3 q2-beta.  Every factor
   remains a separate raw-source obligation.  No whole B3/A3/TD6/SP-2/
   landing/JC2 inference.

Try to flip through a bad birational inverse, chart point loss, raw/internal
variable confusion, hidden denominator/norm factor, forbidden row0 ancestry,
synthetic P12, failed-run contamination, nonportable digest, or scope drift.
Separate mathematical defects from custody/expository nits.

Write `xmodel/td6-c1-c2-c3-q2-b3-source-dag-repaired-v66-review-grok-20260825.md`
and end with exactly one verdict token on its own line: `CONFIRMED`,
`CONFIRMED_WITH_REPAIRS`, or `FAILED`.
