# 0310 adversarial cross — Astra

Status: adversarial CROSS; common deadline 2026-09-09 04:40:00 UTC, unchanged.
Author: /root/model_productivity. Frozen basis: 0d39df3c9fd69c939a8420c54d03228b9077777d.

This cross uses only the equal frozen 21-file packet. New normalization and closed-H conclusions remain provisional. It is not a promotion gate or authority for computation. No mathematical subprocesses are used.

## 1. Outcome and normalization audit FIRST

The source-normalization review is the highest-value pending decision. The two supplied new proofs appear coherent at their stated imported-primary-theorem tier; this cross finds no missing algebraic arrow in their rectangle/inverse-degree argument. I do not promote them or import the unseen joint gate. If their three separate conclusions pass, stop both the remaining ACTUAL75/125 proof search and the search for a constant-J, nondividing-degree, closed-H client of15w. A correct conditional late-contact theorem need not have a nonempty source class.

The critical new argument is stronger and cleaner than my blind's two-factor route. Given ML's polynomial SOURCE normalizer for this very counterexample, the first polynomial has an actual nonzero coordinatewise corner (r,s), with0<r<s. For every strictly positive weight its leader is that single monomial. The excess bracket weight is positive because r,s>=1, r<s and the other polynomial is nonconstant. Therefore the other leading face commutes with x^r y^s. Distinct monomials produce distinct bracket indices, so its exponents lie on the same ray, and only the farthest ray point can maximize any positive weight. Taking weights toward the axes proves that the second polynomial has a proportional coordinatewise corner too. This does not assume the mate's rectangle in advance.

Let U,V be the inverse normalizer's polynomial coordinates, M=deg U,N=deg V, both positive. The rectangle corner uniquely maximizes Mi+Nj. Thus its substituted highest product cannot cancel, even if U_M and V_N are algebraically dependent. The exact original degrees are Mr+Ns and Mt+Nu. Coprimality of the original reduced ratio m:n then gives corners m(a,b),n(a,b) and D=Ma+Nb>=a+b. Both the horizontal and total ratios required by GGV Definition4.3 are now present. The supplied primary audit distinguishes Proposition5.20's ordinary endpoint-preserving statement from its optional successor clause and from the separate minimal-pair corollaries. No hidden GLOBAL minimality remains in this composition.

For75/125 the normalized sum is at most25. The two new proofs agree on the complete printed-table filter, including the otherwise easily omitted F18/F19 and swapped orientation. Only F2,j=1 yields{3,5}; hence(a,b)=(5,20). The stronger equality25=5M+20N actually forces M=N=1. This is a derived consistency check, not an assumed affine-normalizer theorem. The ensuing standard-F2 arrow, ordinary support cone for S(W)(g,p)=W(g,p/g), independent target scalings, and15q top/target exclusion are literal existential arrows. A coefficientwise Moh/F2 bijection or a reverse lift is unnecessary for this proof direction.

My most serious attempted countercontrol is the tied-weight cancellation P=x-y² under U=x+y²,V=y. It really changes the polynomial to x, but lacks the joint nonzero corner, so it does not refute the new inverse-degree lemma. Nonlinear TARGET reduction can alter the ratio, but is not used. A Laurent inverse would lose positivity of M,N, but is not used either. These are manually checked changed objects, not passed executable controls.

The remaining trust boundaries are precisely the ML given-pair source normal form, the indicated GGV statements, GGHV's printed exhaustive finite table, and the already accepted chain/minimal-map/15q implications. This cross read the two complete source audits and the charged GGV endpoint excerpt, not fresh complete copies of ML/GGHV or every cited upstream proof. It cannot replace the separately commissioned different-model primary gate. If the table import is withheld, the conclusion stops at a standard(3,5) pair with a+b<=25; it does not silently select F2. If the ML quantifier or ordinary clause fails, the first source arrow stops there. Neither concern is a discovered counterexample to the supplied statements.

For arbitrary characteristic-zero K, embedding the finitely generated field of the alleged pair's coefficients in C preserves exact degrees and nonzero Jacobian. A complex contradiction is enough; a chosen normalizer over K is not needed. The scalar extension proof for the centralizer uses a finite coefficient basis and remains valid separately. No nilpotent-ring centralizer theorem is inferred.

### M90 and the scope of retirement

The root/Astra normalization cards and the initial part of Sol/Fable's M90 cards are one decision tree, not four projects. If the proposed source arrow passes, every numerical M90 datum REALIZED BY an ordinary actual75/125 Keller pair is excluded by that source theorem, with no need to reconstruct its child. That does not identify the M90 and105 coefficient families or prove a list of necessary numerical data intrinsically inconsistent. A label125, or only partial-pi degrees with unbounded total degree, is not the hypothesis. If the exact retained row has not yet licensed its ACTUAL total degrees, only that small typing question remains; do not build its receiver by analogy.

If the normalizer/table interface fails, a bounded M90 signature extraction becomes useful again. It must retain the child's own radii, polynomial ring, characteristic packet, target scalar/exponent, and ordinary degrees. Fable's conditional odd-count warning cannot be applied after an unlicensed fractional centering: the degree-five factor control pi*(pi²-gamma)² has conjugate nonzero double packets and a zero simple packet. Subtracting sqrt(gamma) from one selected packet changes the ring and destroys the naive remaining-packet Galois count. No assertion that this is M90's actual datum is made. Honest gamma=x² ramification changes c*gamma² to2c*x^5, not c*x². Numerical same-shape hits therefore remain hypotheses to type, not free closures.

### Closed-H screen versus accepted15w

The new source proof obtains H proportional to U_M^a V_N^b. The FIRST assertion of Corollary7.9 gives d=gcd(a,b)>2 for this individual standard pair. Hence H is a proper d-th power geometrically, contradicting the lower-degree commuting root forbidden by ker[H,-]=K[H]. This bypasses both McKay–Wang and my blind's extra upper-envelope lemma. It covers any coprime reduced m,n>1, not just3:5, but remains provisional here.

Accepted15w excludes only late contacts3j>7D in its explicit closed-H constant-J3:5 source. The proposed classical composition excludes that WHOLE source class independently. These statements are consistent: no attained smaller Keller pair is produced, and no counterexample to the abstract finite late-T lemma appears. Confirmation would make both early-contact work within the same closed-H source and Sol's threshold refinement poor source-frontier investments. Proper-power H and monomial-J receivers are outside this emptiness theorem. Neither conclusion resolves JC2 or implies a maximum-degree125 theorem.

## 2. Proper powers: useful reformulation, missing source construction

Fable's reduced-base observation has a correct algebraic component. Under the imported geometric factor bound, a common homogeneous form can be written H=h^e up to scalars, with h a line or a product of two distinct lines with coprime multiplicities. In the two-line case the elementary monomial-bracket calculation gives ker[h,-]=K[h] after the stated field extension. Tops H³,H5 become h^(3e),h^(5e). The centralizer obstruction has been moved to a different exponent problem; it has not been removed from15w by changing a name.

The leap from this reformulation to an AUTOMATIC general canonical-reference/kernel theorem is unsupported. Let A=a_s(R)+F, B=b_s(R)+q_s(R)F+G, where b_s'=a_s' q_s+rho_s in K[s][T], derivatives being in T. The exact product-rule identity is

    [A,B]=[R,T]+[F,G],
    T=a_s'(R)G-rho_s(R)F-(q_s'(R)/2)F².

It is valid with all scalar coefficients retained, but it does not prove ord G>=2j. The omitted term is rho_s(R)F. At3/5 the accepted reference supplies ord rho>=3D>j, so this term starts after2j. With3e/5e the same inequality is not automatic. Fable CardB includes ord G>=2j among its input assertions and then describes it as supplied by K[h]; that is the first missing implication, not an established universal frame.

Here is one exact, UNEQUAL proper-power scalar-reference screen, not a polynomial Keller example. Fix e=3, a primitive two-factor h=x^r y^s with coprime0<r<s and d=r+s. Work in the scalar indeterminate T, without materializing any power of h. Set

    a_s(T)=T^9,
    b_s(T)=T^15+beta*s^(7d)*T^8,
    q_s(T)=(5/3)T^6,
    rho_s(T)=8*beta*s^(7d)*T^7.

These are combined-homogeneous references of degrees9d and15d when T has degree d, and b'=a'q+rho holds term by term. The coefficient beta is an allowed scalar B kernel in this enlarged exponent problem. If beta!=0 and j>7d, rho(R)F starts at7d+j<2j, with coefficient8*beta*h^7*F_j. In particular the proposed late interval3j>23d=(9+15-1)d lies entirely beyond that threshold. The polynomial-domain nonzero assertion is genuine when F_j!=0; whether its Hamiltonian bracket cancels is a further equation, not a license to delete it. Switching the ACTUAL reference coefficient beta to0 removes this term. This is a changed scalar polynomial, not a boolean-predicate mutation, but neither version is asserted to come from a Keller source.

An actual source might force beta=0, force an earlier G, or impose cancellation against other reference coefficients. Any of those could repair a future theorem. The current packet proves none universally. Thus this screen refutes the inference that old3/5 kernel induction applies automatically to arbitrary scalar references; it does NOT refute every proposed proper-power late-contact theorem. It supplies one sharply bounded first test rather than an all-ratio program. No synthetic choice of r,s,d here certifies an open classical source; its numerical degree pair must still pass the full classical/source screen before an actual client is claimed.

Fable's proposed H0=xy Keller control is already excluded, more directly than by the provisional closed-H screen. For P_top=(xy)^k, k>0, at weight(1,1) both endpoints are(k,k), parallel to(1,1). The charged ordinary constant-J GGV Theorem2.6(4) forbids this. A larger common exponent does not change that endpoint. So neither the2/3 nor6/10 examples with this base are open Keller controls. By contrast Sol's H=uv in an ABSTRACT centralizer lemma has no Keller mate hypothesis and is perfectly legitimate there. The two uses must not be conflated.

The physical F2 h=uv4 is an unequal base and can be a historical method control, but the complete F2 source is retired. Passing or failing one coefficient test there cannot license a universal source construction. The new conditional closed-H proof moreover predicts e>2 for the geometric common root of an actual nondividing pair, not the e=1 or2 toy branch. It still supplies no proof of the enlarged canonical order bounds.

Fable's approximate-root connection is suggestive, not a ready Abhyankar–Moh theorem. At a ramified root, even coprime leading powers3/5 produce local degrees6/10 or9/15; their common root is quadratic or cubic. The accepted proofs kill these cases by the SOURCE remainder-degree gap and coefficient comparisons, not by asserting every local common root is linear. Conversely a general common-power initial permits a tower but does not prove the tower's convergence, polynomial termination, or global source compatibility. Stable avenue6 denotes one-place/coordinate recognition in the master table; an approximate-root analogy should not silently rename that historical result or its existing place-data gap.

The arithmetic fact that certain licensed u_s=1 child exponents are coprime is not enough to say their only remaining difficulty is multiplicity. The actual Jacobian exponent, source ring, radii/centering, degree loss, all scalar kernels and global initial selection still matter. Fable's proposed44-row shape census does not discharge them. Its arithmetic would be mathematics, not metadata, and has no authority in this cross. The conditional normalizer decision should precede any such census; no farm is selected.

## 3. Sol's closed-threshold lemma: valid-looking abstract improvement, weak current payoff

I independently agree with the proposed finite lemma at its literal polynomial hypotheses. Split T into combined-homogeneous pieces T^(n), n<=N. Since R has degree D, their brackets have distinct combined degrees D+n-2, so ord_s[R,T]>=N implies the same bound for each piece. For a piece's first order ell<n, ell<N and its bracket coefficient is[H,T_ell]=0. The centralizer and homogeneity give T_ell=a H^i, iD=n-ell. Subtract a*s^ell*R^i; this is polynomial, degree n, and preserves the bracket while raising the order. If ell=n the remaining coefficient is a scalar times s^n and already commutes. There are finitely many orders0 through n. Thus[R,T]=0. Zero pieces and N=0 cause no exception. This is a hand proof, not a promotion or an executable verification.

The sharpness control R=H=uv, T=s^(N-1)u for N>=1 really has nonzero bracket -s^(N-1)u and order N-1; no Keller hypothesis is present. It is not disqualified by the balanced-top theorem above. It tests a changed polynomial at the actual threshold, not merely a predicate.

Composing at N=7D with accepted15w would include3j=7D. At equality m=3D-j=2D/3, positive n<=2m-D=D/3 still gives D/2<m<D, and L=D+2m=7D/3 lies strictly between2D and3D. The direct leading-degree lemma therefore has the same contradiction. The integer improvement occurs only when3 divides D; the old strict result did not already assert it. This is NEW relative to the frozen packet, with no literature-novelty claim.

Nevertheless a separate source-free review/descendant chain is not the next critical path. If the provisional whole closed-H screen passes, this application has no possible source at any j. The abstract equality lemma may be retained as a useful candidate for a future genuinely different T/R interface. No such live interface is identified by Sol's card. Its modest mathematical validity does not restore an all-degree client or contradict15w. Stop after this assessment unless a specific nonempty/unretired theorem interface needs the equality.

## 4. Falsification and all-degree priorities after deduplication

Fable CardC names a real imported Moh extension rather than a nonexistent theorem, but its proposed experiment overstates the conclusion. An arbitrary tame postcomposition of(x,x^ell*y) has monomial Jacobian. It does not automatically have the monicity, characteristic hierarchy, major-packet hypotheses, minor-radius premise, and original-parent provenance of the exact shifted proposition used in the105 attachment. A failure on such a pair refutes the licensed import ONLY AFTER every antecedent is independently checked. A trivial or unqualified packet gives NO TEST, not a rollback of all u_s=1 closures. One passing example gives no theorem. Nor is this now the only cheap route that could change the75/125 verdict: the independent standard-F2 normalizer bypasses that Moh import altogether. I would not launch a generic control hunt while the precise normalizer gate is already deciding the source question. A specific incompatible printed clause, if found in a future scoped audit, remains worth surfacing; no such clause is supplied by this cross.

The strongest proof direction beyond the finished local clients is still a source-complete uniform argument. There are two quite different possibilities: a primitive-root reference theorem that retains all scalar remainders on actual nondividing Keller sources, or a genuinely global landing/coverage/type theorem in the reduction architecture. Neither is delivered here. General source normalization does not provide G2-PSC's decorated-sheet fidelity, G2-BD's post-residue delay bound, off-axis completeness, or a cofinal type/degree ceiling. A table complete through normalized sum35 is finite, and the44+20 census population is the n<=200 window of necessary data, not an all-degree list of polynomial pairs.

A full characteristic-zero ideal decision on a COMPLETE UNRETIRED original-source system remains the strongest direct CE endpoint. Properness certified by an applicable exact Groebner basis plus all original equations is enough by Nullstellensatz; no additional rational point is logically required. An exact all-row point over a nonzero finite Q-algebra is an alternative. A necessary receiver point, modular basis, finite jet, unbounded-support formal lift, projected collision or timeout is not that endpoint. This packet attaches no new sufficient unretired chart. Fable's stronger phrases 'no sufficient client exists' and 'none at degree<=125' should be narrowed to the named available clients, with conditional actual75/125 closure distinguished from arbitrary maximum125.

For independent falsification, Sol correctly identifies an exact point on the K16 b*B_m*eta_m open locus as a falsifier of the proposed uniform product vanishing. It would NOT alone disprove JC2. Both coefficient-field factors and the separate b=0 obligation remain distinct; finite-m exclusion is not uniform. There is no new generator/cost discriminator in this packet, so no K16 search is launched and the old square/residue/ABC reformulations stay stopped. The Witt route's missing fixed-degree polynomiality/A_infinity condition also remains unchanged. These are honest independent endpoints, not auxiliary cards disguising an unauthorized solve.

### Two actionable cards TOTAL

**Card1 — CONTINUE the already-owned source-normalizer decision; merge duplicates.** Classification: NEW composition of KNOWN imported statements and accepted15q; provisional. Dependencies: the two exact new reports aeeddd6a... and3935d763..., their named ML/GGV/GGHV imports, accepted minimal receiver/15q scope. Cheapest discriminator: the existing different-model primary gate, not a second commission; root alone performs its terminal receipt/pin/body intake. No new arithmetic authority is needed. Require separate verdicts on given-pair polynomial normalization, exact75/125-to-F2 composition, and whole closed-H emptiness. If confirmed, retire actual75/125 source work at that tier and stop searching for a15w closed-H client; do not expand to max125 or JC2. If refuted, name the actual failed arrow and preserve independently correct conclusions. If GAP, retain precisely the covered intermediate statement, and only then decide whether the M90 literal source signature is the fallback. Timeout is nondecision. Stop after the verdict and scope integration. This supersedes root/Astra's separate normalization proposals and conditionally removes Sol/Fable's M90 follow-ons; no duplicate receiver construction is earned.

**Card2 — REDESIGN once at the first proper-power derivative remainder, not a general-e tower.** Classification: KNOWN exact wedge identity / NEW enlarged-source obligation; current scalar example is METHOD DISCRIMINATOR ONLY. Dependencies: elementary factor/centralizer typing at its imported field scope, all enlarged A/B scalar kernels, and a future exact original-source contract if one is claimed. Cheapest proposed next task: at most25minutes of pure-prose order bookkeeping for ONE unequal primitive-base,3e/5e interface, beginning with the e=3 coefficient in§2 and including every reference coefficient that could cancel it. Root must authorize this separately; no current launch. The test is whether the actual necessary equations force the early derivative remainder to vanish, or require a corrected G/order statement. A proved vanishing with all source hypotheses becomes a narrowly scoped candidate lemma, not a universal proof; an unavoidable competing coefficient is a redesign result, not a Keller counterexample; missing actual source or no complete order ledger is GAP/NO ATTACHMENT and stops the task. No general theorem, approximate-root recursion, source census or solver follows automatically. If no literal source beyond retired/classically closed classes is available, stop with the scalar-control result rather than paying for an unattached family. Its information gain is locating the first failed induction, not counting rows or generalized exponents.

No third card is earned. In particular Sol's valid-looking threshold refinement, Fable's44-row farm and generic Moh import toy do not outrank these decisions. NO_NEW_MECHANISM for resolving JC2 is the honest overall verdict: the useful new connection is source normalization deciding which Hamiltonian reference problem could be relevant, while proper powers expose a specific missing scalar-order implication.

## 5. Binding history, systems and resource consequences

Stable labels matter. Row1 is the GGV Newton/standard-pair route; row2 is the Sigray/tree/coverage route. Exact source typing draws on both but is not itself a proof of row2's missing universal landing. Row6's one-place recognition theorem is not automatically an approximate-root tower theorem. Row29's local Hamiltonian identities do not establish locally nilpotent derivations. Row26's old successor line is superseded by BD-GAL: d2=[K:C(f,g)]=2 and every proper Galois SECOND leg are closed; d1=[C(x,y):K]=2 is different. A proper power of a leading form does not produce an intermediate function field or an actual Galois block. The remaining non-Galois d2>=3 sandwich needs its genuine A² first-leg embedding, not a cover-only cubic model. None of these historical mechanisms is reopened here.

The original manifest has31 entries plus the manifest itself:32 actual charged files. Its complete retained bytes confirm this. Sol's30/31-directory/32-semantic narrative is wrong and cannot justify a new overlay. The root-release and later equal invitation already separate a frozen mathematical packet from actual launch authority. The supplied poststate reports that all scientific pins ultimately matched; this cross does not invent an independent replay of uncharged command logs.

The distinct root Fable custody lapse is real: the first WHOLE body read preceded completion of current-pin checks after CLEAN was wrongly compared with CLEAN_SEALED. Later successful pins do not retroactively restore the required ordering, and no live body was read. That is an operator sequencing/status-vocabulary defect, not evidence that scientific bytes were missing. Preserve the disclosure and use the existing explicit receipt/check-success/body ordering; no new generic collector, manifest framework or theorem checker is needed. My own common contract/poststate read preceded the local21-file rehash; all peer mathematical bodies were read only after the full match. This is disclosed rather than calling every byte read receipt-first.

**Systems decision: NO_NEW_MACHINERY.** Continue the already measured packet-size reduction as ordinary curation:133733 bytes were removed before invitations, not a proved runtime or comprehension speedup. Fable's proposed lemma sheet could help only with exact hypothesis matching; treating G>=2j as a universal citation would bake this round's central error into the sheet. Accepted proofs already provide the required references, and no measured incremental benefit justifies a new maintained layer now. Sol's overlay rests on a false count. The existing protocol is enough to enforce sequential metadata success before body reads and root-only acceptance. Keep any current compact-input trial within its original scope; do not claim controlled throughput improvement from changed mathematics or word counts.

Continue one exact proof/source owner plus one different-model hostile reviewer, with at most one independently scoped global/CE-interface lane when a concrete question exists. Astra/Fable/Sol supply the useful diversity here; agreement is not a vote and failure of a precise idea is not grounds for permanent model removal. No standing utility seat, extra review cycle, AWS allocation, longer solve or modular retry is selected. Retired inputs/EBS remain preserved under root custody. The last broad cutoff stays September8 22:24:26.958594UTC, with the recorded social/access holes; this cross did no web sweep and resets no clock.

## 6. Read perimeter and completion

Actual task start04:12:03UTC. The exact invitation f4f46be4... and cross-PINS27f50f75... define the perimeter, not original paths cited inside reports. The local before-read manifest records21 current matches: PINS itself plus its20 charged snapshots. All four blind reports were read WHOLE, including their metadata/seals; combined-output truncation was followed by explicit rereads of the omitted Fable/Sol ranges. Both new provisional proofs, accepted15w review and exact audit entry, primary endpoint excerpt, full frozen COORDINATION/APP/master46/reduction/admissibility/M90/B0/0040/BD-GAL context, common contract/poststate and original manifest were read WHOLE at their specified content/metadata scopes. No uncharged mathematical link was followed. All21 pins are rechecked at final custody.

The mathematical results discussed here are manual prose and formal coefficient/degree comparisons. No mathematical subprocess of any size, old checker, scalar/numeric script, actual H/R power expansion, A/B construction, CAS, solver, AWS/SSH, agent launch or public/canonical/protected-project access occurred. The scalar-reference screen uses an indeterminate T and is not an expanded source pair. Only bounded documentary metadata, text reads, own edits, own OPEN extraction and the existing publication transaction ran. There was no new primary retrieval; the exact GGV excerpt was supplied in the packet.

Inherited campaign knowledge and authorship of one blind are disclosed. Neither permits access to newer canonical science. The provisional joint normalizer gate, its receipt/log, and every current peer cross remain unread. Poststate lifecycle15w is accepted; the new source proofs are still provisional regardless of their apparent coherence. The report is an independent cross assessment, not a different-model promotion of my own previous theorem.

COMPLETE at the requested cross scope: two ranked cards, explicit disagreement/conditional source retirement, threshold assessment, historical and manifest corrections, proof/CE endpoints and a reasoned no-machinery decision. No new full46 vector or canonical OPEN is raised. The unique completion marker is appended only after substantive whole-body review and owned extraction. The final transaction and custody supply actual publication/terminal time. All writers stop after sealing; no automatic continuation is authorized.

## OPEN(S) RAISED

None.

## COLLISIONS

status: EMPTY

- NONE — the report contains no explicitly raised `OPEN[...]` entries.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `27192`.
- Body SHA-256:
  `f13cb94def026bb6b3c31840256d96203e72cbf1096c2ae88b420ab529f993d6`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
