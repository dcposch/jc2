cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
Complete at 17:48:23 UTC, before the 17:58:00 UTC stop. The report `xmodel/f10-r1-retained-runtime-gate-fable5-20260909.md` ends with a single standalone marker and nothing after it, and the only other owned artifact is the custody file in `box/f10-r1-retained-runtime-gate-fable5-20260909/`.

**Verdicts**

- **A, caller migration: STATIC CONFIRMED.** The registration vector is exactly eleven files by set equality. The eight named pins are constants, and the self, harness and interpreter pins are root-supplied but hash-verified. DMI, hostname, cwd and -I -B checks are present. The old worker, deadline, hostname and interpreter constants are gone. Both cutoffs and the aggregate cap are stored, with the 15-second admission and the post-return refusal now applied to every operation. Each of the five defects was traced to a distinct helper refusal that fires before any file write. The dummy and PGID blocks are untouched by the diff.
- **B, order and controls: STATIC CONFIRMED.** Transform, then checker.main, then the harness. All twelve mutations were traced to their exact failure line, exception class and string in checker.verify, including the KeyError with the quoted key. The harness imports checker only after authority, feeds the reread fixture straight to verify, which has no artifact-hash logic, so no stale-hash refusal can pose as a semantic test.
- **C, precision pair and wording: STATIC CONFIRMED.** The pair uses two genuinely distinct integers and both are rejected at the row-substitution line. The packet correctly says this is not nested-parser payload evidence and claims no late bracket or pole negative.
- **D, readiness: READY (qualified).** No blocking correction found. One root-registered bounded engineering run may follow a fresh same-caller preflight. This is not a solver or dispatch licence.

**Non-blocking notes for a later packet.** Defect refusals are typed only as nonzero exit, not by stderr string. Sentinel and fixture paths are not in the pre-existence lists, but exclusive opens make a stale file a STOP rather than a false pass. The second unpinned authority read also exists in the harness, the same deliberately default-off pattern.

**GAPs and OPEN.** Syntax is unverified beyond reading. Artifact and fixture sizes, wall and RSS are unmeasured with no forecast. One OPEN names the preflight-plus-batch quantity with wall stated as unknown. Collision check is own-only and empty.

Nothing was committed, no source or harness was edited, and no execution authority or registration was created.
DIVERT path=xmodel/f10-r1-retained-runtime-gate-fable5-20260909.md status=CLEAN body_bytes=22407 body_sha256=f8a34d1c96be944fa28f8d5c5b7d60ee9ad093d7a81634c34b404524b7949435
