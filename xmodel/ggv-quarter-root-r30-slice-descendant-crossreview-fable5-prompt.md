# Fable5 hostile review — row-30 slice descendants through class row 34

You are Fable 5 acting as an independent hostile mathematical reviewer in
`/Users/dc/code/math/jc2`.

The charged producer file is immutable at this exact hash:

```text
e5a2c968e516a40509d136cfd57613c8be84e50041444ce8146916b976bdf24c
  xmodel/ggv-quarter-root-r30-slice-descendant-r0-sol-ultra-20260827.md
```

Read it completely and fail closed if its bytes do not match.  You may read
only the exact dependency files and frozen source JSON explicitly named in
that report.  Do not reread mutable canonical ledgers, inspect or traverse
`jc2-lean`, touch AWS, run heavy local computation, or read unrelated
ideation submissions.  Small exact desk arithmetic and independent scripts
are allowed.

Independently rederive and classify each atom `CONFIRMED`, `REPAIRED`, `GAP`,
or `REFUTED`:

1. the row-31 gauge, quotient basis, equations
   `6b1+b5=0`, `13b2+3b6=0`, `b4=0`, and split surjectivity over the full
   nonreduced parent base;
2. every coefficient in gauged `q_10`, its residue vector and ideal
   `(u^2*v,u^5)`, and containment in `(u*v,u^4)`;
3. the row-33 gauge, both receiver-coordinate maps, affine equations and
   unit-pivot cancellation without localizing at `u`;
4. every coefficient and cancellation in gauged `q_12`, all finite and
   infinity residues, ideal `(v^2,u^3*v,u^6)`, and the claim that it adds
   exactly `v^2` modulo the parent ideal;
5. the resulting full coefficient-ring isomorphism, count of 24 free newest
   slots, length-five base with basis `1,u,u^2,u^3,v`, radical `(u,v)`, and
   square-zero nonzero `v` tangent;
6. every firewall: these are licensed de Rham class equations on one symbolic
   slice, not raw determinant rows, a polynomial-`G` lift, endpoint, branch-P
   family exclusion, Keller pair, or JC2 result.

Do not merely rerun the producer's embedded script: give at least one
independent derivation of the coefficient/residue identities and directly
attack the odd-row quotient typing and scheme contraction.  Include exact
hashes, any small replay, pinned model/CLI identity, and a precise promotion
recommendation.

Write exactly one output and nowhere else:

- `xmodel/ggv-quarter-root-r30-slice-descendant-crossreview-fable5-20260828.md`
