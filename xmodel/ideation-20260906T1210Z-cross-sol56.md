# Adversarial exchange 20260906T1210Z — cross synthesis

## Custody and endpoint verdict

`HASH_AUDIT=PASS`: all nine staged files byte-match their charged repository counterparts; PDF/text are `ac18e80c…80bd`/`f3eca2a5…2368`, and all report hashes match `collection.json`. Collection was terminal at 12:24:00Z. The round remains `EXPEDITED/DEGRADED`: Fable is `QUARANTINED` for its invalid `delta=n/a`, Sol sealed late, and root's already-recorded pre-collection Fable-summary exception is not erased.

**Endpoint verdict: MISTAKEN ENDPOINT, not a missing reduction.** The imported exact definition says `st`/`en` are the first/last points encountered counterclockwise; equivalently, for a nonmonomial face, `(rho,sigma) × (en-st)>0` ([GGV, arXiv:1401.1784v3, Notation 1.6 and Remark 1.7, p.4](https://arxiv.org/pdf/1401.1784v3)). For Proposition 4.2 case 1 (`ggvh…txt:348-352`):

- P: `(-1,1) × ((0,12)-(6,18))=12>0`, hence `st_{-1,1}=(6,18)`.
- Q: `(-1,1) × ((0,18)-(9,27))=18>0`, hence `st_{-1,1}=(9,27)`.

These are exactly Theorem 5.1(2–3) (`ggvh…txt:659-662`). Cases 2 and 3 also meet the endpoints. Opus reversed `st` and `en`; Corollary 5.7 is unnecessary. The note that [5] orders *directions* oppositely does not reverse endpoints. This definition-level check, not a majority vote, resolves the objection.

## Corrected route ranking

1. **99 is externally excluded at theorem trust.** The robust claim is the advertised actual maximum-degree lower bound 108 (`ggvh…txt:63-90`), not an executable filter from Theorem 2.1's arbitrary-pair list. Proposition 4.2 supplies all three polynomial polygons and Theorem 5.1 closes them. Cited predecessors and the full reduction/table chain remain externally trusted, not internally replayed; no majority upgrades that trust. Existing (5.9) replays need not be repeated, and the post-cutoff root/agent certificate remains unreviewed.
2. **The complete actual 99/66 client is closed, independently of necessity.** Its genuine polynomial `F99,G66` and `ZjJ0-1` make any point an actual Keller counterexample. Reverse source-to-chart necessity is irrelevant to that implication. Fable's useful first conditional chain survives only as: if the external chain and required Nullstellensatz/base-field hypotheses are accepted, the *full* ideal is unit. This neither computes an internal unit nor implies the 466-row subset is unit; an independently certified subset unit keeps only its own scope.
3. **Do not classify arbitrary 108–124 pairs.** Sol's observation that Theorem 2.1 literally says “any counterexample” is retained as a literal-text reading. Imported normalization conventions and target addition make its pair list unsafe as an arbitrary-representative classifier. Fable's second conditional chain—normalization plus target preservation would permit a broader overlay—stays conditional until those arrows are proved. The arithmetic census counts do not change.
4. **D108 is the fastest live interface; an all-degree realizability obstruction remains the strongest proof route.** Absolute polynomial-Keller degree results do not apply to `.73` K7 charts with `J=x^k`. K16 is only a prototype; its next useful result must cover every actual `m`, coefficient factor, and the `b=0` boundary.

## One deduplicated card

**D108 source → GGHV `(8,28)` (`KNOWN` ingredients; `NEW` proposed composition; no map proved).** Known: source top `h=(X+W)^8W^28`, `F_top=h^3,G_top=h^2`; GGHV lists `(8,28),(3,2),108` and Proposition 4.3 produces Laurent pairs with bracket `x^2` and two polygons (`ggvh…txt:116-126,492-499`). Dependency: declare field, source and target rings, generator order/images, coordinate normalization, actual degree attainment, and every exceptional stratum.

Cheapest decisive test: a written map table, first checking that the bracket image is exactly `x^2`, then supports and both Proposition 4.3 branches; one deliberately invalid image is the negative control. `PASS` licenses only “known-frontier client,” not existence, properness, smaller support, or a solver launch. `FAIL` kills this composition, not D108. Stop at the first missing arrow or mismatch; no dense export. Strongest falsification is categorical: a literal source top factor need not equal a corner created after Laurent/non-polynomial transformations. This rejects Fable's claims that a visual match proves campaign novelty/smaller supports or that mismatch externally excludes D108.

## Roles, execution, and systems

Observed work and correction cost favor Astra for primary integration/structural synthesis; Sol for bounded exact certificates, with literal theorem readings scope-gated; Opus for narrow hostile source extraction, since its valuable objection prompted the right check but its endpoint reversal makes independent definition review mandatory; Fable only for conditional hostile hypotheses until its unread proof/gate bodies, arbitrary-degree filtering, support/novelty claims, `.73` classification, stopping-sweep proposal, and quarantined workflow are corrected. Coordinator/root remains the arbiter. These are task roles, not votes.

**Software:** retain only a scratch regression specification separating `GGHV_CLOSED_LT108`, the 72/108 exception, theorem out-of-scope, and `GCD_GATE_PASS`; never mutate census counts or adapters here. **Systems: `NO_UPGRADE`.** Use the existing pre-launch literature reconciliation; add no canonical ledger and change no protected-worker custody.

**CONTINUE** `.63` subset and `.73` K7 work under their original distinct caps, scoped receivers, bounded parser repair/W190 validation. **REDESIGN** D108 around the card and K16 only around a genuine all-`m` obstruction. **STOP** 99 frontier solves/retries, dense99 expansion, repeated norm-only K16, and automatic 99-to-108 code/support ports. No JC2 proof, counterexample, new internal exclusion, or exit-price assertion is claimed.

<!-- BODY-END -->
