# Fixed89/root0 fullunit runtime and single-cofactor mutation

STATIC / UNREVIEWED / DISABLED / UNEXECUTED. First action2026-09-10 16:55:58UTC; reserve17:15/hard17:18 never reset. Own targets absent and all eleven exact input pins matched before bodies. No baseline/candidate body, hash or coefficient access; no mathematical subprocess/import/AST/syntax/compile/test, worker/network/proc or registration action.

## Deliverable and retained trust

Owned dispatch.py is a narrow derivative of the accepted highest status-fixed dispatcher; owned mutate.py is an independent fullunit single-cofactor mutator. CONTRACT, exhaustive SOURCE-DELTA.diff, disabled plan, PINS and READ-SCOPE accompany them. Original five science files, fullunit entry/produce/check, probe and CAPRUN remain unchanged external inputs. No copy of an uncharged probe or accepted science source is made. New source needs ONE FIRST runtime/mutator gate; neither accepted fullunit mathematics/code nor prior runtime acceptance automatically promotes it.

Dispatcher deltas are new fullunit job/schema/three sibling hashes/mutator hash, phase/path/profile labels, positive success literal, eighteen-slot selection metadata and the exact new negative traceback. The original nine authority/startup/dummy controls, strict current native/boot/ns/argv/identity/cgroup/cap barriers, prelaunch authority hash, live capture, quiet-before-payload, freezes and durable custody remain. No instrument hardening, native exception, favorable-identity wait, retry or additional experiment is introduced. Non-atomic mapped/process observations retain their accepted limitations.

## Sequence and exact terminal meanings

Nine old preflights precede fullunit-produce, fullunit-positive, mutate, fullunit-negative: thirteen calls only on the full path. No original baseline positive producer/checker rerun. Normal2/resourceNone/empty-stream producer with no candidate and exact INCONCLUSIVE_NO_CERTIFICATE receipt returns after ten calls with precisely that status, scienceNONE, and no later phase. It is not diagnosed as bad prime or properness.

The normal0 candidate is frozen ROOT0444 under0555 and checked by the unchanged fullunit checker. Its exact FULL_SPECIAL_UNIT_CHECKED_NO_SOURCE_OUTCOME receipt binds actual unknown candidateSHA, source path-to-hash map, root/baseline and place before freeze. Mutation reads that immutable receipt and verifies the candidate digest before mathematical import, avoiding a circular prospective SHA in argv. Only ROOT_REGISTRATION_SHA256 is substituted, exactly once in the typed slot of each last-four full vector; all expanded CAPRUN argv must equal ROOT registration before launch.

The negative must be normal1/resourceNone/no signal, no receipt, empty stdout and the exact ordered three-frame unchanged check.py traceback198(module main),188(main verify),181(verify finalraise), ending `ValueError: CHECK FAILED: fixed-place literal full-source unit identity`. Bounded legitimate caret lines alone are optional. Checker sufficient-place normal2, shape/parser/native/resource errors or missing live capture are STOP, never a semantic pass.

Final successful wrapper status FULLUNIT_CHECKED_NEGATIVE_CONFIRMED always retains science_outcome NONE. It records a checked special identity and one meaningful rejection, not source-zero. An actual checked highest certificate, integral/source data acceptance and the separate accepted conditional lift remain prerequisites for any ROOT source conclusion. No full-source or JC2 result is claimed by this preparation.

## Independent mutator and proof of the one edit

Before math: exact19-argument job vector, metadata/self/arithmetic pins, ROOT-frozen positive receipt and actual candidate/baseline bindings. Only original arithmetic.py is imported, never fullunit producer/checker. It independently reconstructs literal monicP7 at tau2/7, fixed prime89 and simple root0. All seven rational coordinates of every term of twenty used polynomials are89-integral before z1/root0; full six-coordinate support and weights are checked. Uweight3, K1_0weight14 and K0_0weight16 yield the actual mixed K1_0-U*K0_0, bound19/class4. All eighteen generators and cofactor slots remain, including zeros.

The original canonical certificate has exact labels/modulus/caps/opposite classes. Its whole polynomial identity is rechecked independently. Since the sum is1, some nonzero specialized generator has a nonempty cofactor. The first such slot and its EXISTING first monomial are selected. Increment residue by1 modulo89; delete that term exactly on88-to0. No exponent, constant insertion, weight/class or slot change is permitted. The identity difference is a monomial times a nonzero polynomial over the domain F89[Y1,Y2,Y3], hence nonzero and within the original checked product box. Thus all early predicates remain satisfied and the unchanged checker must reject its final identity. A separate original-byte reload supplies the exact old term list; inverse restoration must reproduce the entire original canonical certificate bytes.

Mutation receipt binds index/label/exponents/old/new/deletion/length metadata and actual input/output path/SHA/bytes, with checker_run false and whole-byte restoration true. Dispatcher checks bounded metadata and readback only, not coefficient arithmetic. Source/baseline/candidate/positive receipt postchecks, exclusive writes, fsync and0444 remain. Fresh hashes and frozen fixtures prevent stale-payload controls.

## Caps, static review and remaining checks

Joint600wall/500CPU/2GiB/16MiB unchanged. Profiles control5/3, dummy5/3, fullunit120/99 and mutate30/24 (wall/softCPU); dummyRSS32MiB, others2GiB. Thirteen child allowances435wall/361hardCPU are prospective bounds, not measured costs. Original wall+15 admission, sampled480CPU safety cutoff, post-return deadlines,2MiB certificate/32768 receipt/1MiB metadata limits and durableEBS byte custody remain. No actual worker/native/clock/command registration is invented; the template has enabledfalse and null operational fields.

Eleven exact charged texts were read WHOLE or explicitly same-byte prior-WHOLE reused after current pins, as READ-SCOPE details; clipped combined reads were recovered where needed. Both new sources were reviewed whole (dispatcher via original plus exhaustive literal diff), with manual CLI/schema/frame/negative-proof tracing. No tests or fixtures executed. One FIRST code/instrument review is pending, and actual costs, live capture, sufficient place, witness existence and semantic negative remain unknown. Cheapest next check is only a ROOT-selected fresh registered finite batch after that review, not a registration issued here. No new canonical OPEN; no follow-on authority. Own WHOLE/OPEN/collision/current-pin checks precede the marker and transactional custody; all writers IDLE at terminal handoff.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6882`.
- Body SHA-256:
  `b712b514fff08b6f8a29cae98d9ad0bbe7cd71f4968125a05e7203815ec2e240`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
