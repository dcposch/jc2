# Significant-news whole-portfolio synthesis — `20260825T1550Z`

Status: **strategy synthesis, not mathematical promotion**  
Round trigger: exact tame source transforms of the `F_3` Artin--Schreier
collision at the live `(9,12)` and `(8,12)` maximum-12 frontiers  
Computation placement: **AWS-only for every substantive calculation**

## Inputs and independence

Four whole-portfolio scans were sealed before cross-reading:

- Sol: `xmodel/ideation-20260825T1550Z-sol.md`, SHA256
  `b7eb0f671d2102da22d17b3bd3f7d61eb4e75a1e9860bdb9949c301041807141`;
- AS owner: `xmodel/ideation-20260825T1550Z-as.md`, SHA256
  `42a7810a49eb60403e98548f123904a7b0064214358bf4a83bc263038dc6c33a`;
- cube/max-12 owner: `xmodel/ideation-20260825T1550Z-cube.md`, SHA256
  `e5e85f071257d6be9c225c351ba3fc8a84bee6d64a42ac7e87bca9acf793b890`;
- TD6 owner: `xmodel/ideation-20260825T1550Z-td6.md`, SHA256
  `218a9d4c25e02861cc4d4e2c7671cf40cc167d5428057ee6ec1cb12f95f7310e`.

Each scan checks all 46 canonical avenues.  The TD6 owner derived the first
finite-depth B9 lifts during the blind window and isolated them as own-news.
The other reports did not consume those results.  No vote is treated as
independent mathematical evidence: the synthesis follows exact interfaces,
falsifiers, and source coverage.

## Exact news at the synthesis cutoff

### Reviewed residue seeds

Let `A(s,t)=(s-s^3,t)` and

```text
B9=(u=x+y^3, v=y+u^4),
B8=(u=x+y^4, v=y+u^2).
```

Then `G9=A∘B9` has total and partial-`y` degrees `(9,12)`.  The target-
oriented `G8=phi∘A∘B8`, `phi(P,Q)=(Q,-P)`, has degrees `(8,12)`.
Both have unit leading `y` coefficients, determinant one after reduction
modulo three, and exactly three fibres of size three on `F_3^2`.

Grok 4.6 independently recomputed the identities and returned
**CONFIRMED**, with no mathematical error, in
`xmodel/as-max12-tame-right-composed-residue-seeds-review-grok-20260825.md`.
The maps are right-compositions/source-coordinate transforms, not group
conjugates.  The old directory slug containing `conjugate` is nonmathematical
and remains only because renaming would break the freeze.  For `G8`, the
target orientation `phi` must always be stated.

### Producer-exact fixed-D12 survival

For B9, exact dual-AWS packages now give one fixed total-degree-at-most-12
branch through `Z/27` and `Z/243`.  The displayed `Z/243` pair has actual
partial-`y` and total-degree pairs `(9,12)` and passes literal integer
determinant replay.  Both packages await hostile review.

During synthesis, the complete fresh order-243 digit system above that fixed
`Z/243` parent was also solved on AWS with all 182 D12 output columns and all
276 determinant rows.  The nonzero-row matrix has 141 rows, rank 108, and an
affine kernel of dimension 74.  An exact reconstruction passes modulo 729;
its actual partial-`y` pair remains `(9,12)` and its total-degree pair is
`(11,12)`, within the same D12 cap.  The two producer representations are
term-for-term identical: one retains all 276 rows and the other independently
removes 135 literal-zero rows.  The corrected producer package is frozen as
`cases/as_b9_max12_w6_next_digit_aws_20260825/`; hostile review is pending.

The deterministic mod-729 particular does **not** lift to mod 2187 under any
fresh D12 digit: the complete 182-column system is inconsistent in two AWS
replays.  This is point death only.  It does not kill the 74-dimensional
mod-729 affine fibre.  The correct whole-fibre gate has since completed on
Box02 and Box03: its 70 nonzero left-cokernel equations have rank 39 and are
consistent, so a 35-dimensional projection of the 74-dimensional predecessor
fibre lifts.  Together with the 74-dimensional fresh kernel this gives
exactly `3^109` solutions modulo 2187, and a literal fixed-D12 witness passes
all determinant rows.  This is still one fibre over one fixed mod-243 parent,
not the whole earlier mod-243 scheme or an all-depth point; freeze and hostile
review are pending.

The same whole-family linear window has now continued producer-exactly
through modulus `3^10=59049`.  Successive `(rank,new-kernel,prior-projection)`
triples are

```text
3^6:  (108,  74,  0)
3^7:  (147, 109, 35)
3^8:  (162, 129, 55)
3^9:  (164, 147, 73)
3^10: (164, 165, 91).
```

All stages retain 182 D12 columns, all 276 rows, source controls, and literal
witnesses on dual AWS runs.  For `F=F5+243T`, the quadratic term has factor
`243^2=3^10`, so it is zero modulo `3^10`; it first enters the divided carry
for the transition to modulus `3^11=177147`.  That complete 165-dimensional
quadratic gate is the first genuinely nonlinear frontier.  The linear-window
package is being frozen and remains unreviewed at this cutoff.

### Producer-exact B8 control

The complementary oriented `(8,12)` seed now has an explicit point modulo
27.  Its source-complete fixed-frontier envelope has 81 first-coordinate
slots (`total<=12,y<=8`) and 91 second-coordinate slots
(`total<=12,y<=12`).  The full 276-by-172 fresh operator has rank 104 and
nullity 68.  Dual-AWS literal replay preserves actual degree pair `(8,12)`
and determinant one modulo 27.  A prior 182-slot representative is retained
only as a negative control because it overflowed the first coordinate's
`y`-degree.  The corrected package is frozen; hostile review is live.

Finite survival at three, five, or six digits is not a counterexample.  The
reviewed low-`y` theorem already implies that some fixed supports can survive
many digits and nevertheless die later.  The importance here is the exact
maximum-12 interface and the reusable obstruction scheme, not a probabilistic
claim that JC2 is likely false.

### Late TD6 hostile-review correction

The V69/V33 raw-`U=0` review returned **CONFIRMED_WITH_REPAIRS**, with one
load-bearing scope defect: two transport pivots contribute `(-C)^2`, so the
licensed theorem is only `U=0,D(C)`, not the whole `U=0` divisor.  The
intersection `C=U=0` is a separate unrun raw `u-h-zero` stratum.  Therefore
every producer composition claiming a whole fixed-A3/H cover is quarantined
until that stratum is rebuilt from original source rows.  The address-
dependent digest problem remains custody-only; the omitted `C^2` localization
is the mathematical repair.

## Consensus delta over the 46 avenues

All four scans agree on the only material allocation changes:

1. **Raise 19/21/36 sharply:** characteristic-`p` lifting, p-adic collision
   transfer, and structured counterexample search now share a named fixed-D12
   object at the first unproved partial-degree frontier.
2. **Raise 4/29/31/46 as machinery:** finite-type arcs, Cartier/Kuranishi and
   Fitting strata, integral source transport, and proof-carrying compilers are
   the decisive implementation layer.
3. **Raise 2/28/38 narrowly as an interface audit:** max-12/Q8 must identify
   the exact integral monomial-core/bad-reduction boundary.  Shared degree
   numerology is not a landing theorem.
4. **Use 5/43 constructively:** tame right words are search variables for
   moving residue collisions into small unit-leading supports.  They do not
   provide Jung--van der Kulk degree descent for a hypothetical Keller map.
5. **Do not redirect TD6 at B8/B9:** the residue maps have function-field
   degree three, TD6 has topological degree six and unrelated pole/SP-2
   source data, and its normalized p=3 chart is nonintegral.  Only software
   discipline transfers.
6. All closed or low-information routes remain closed/low: no new HC4,
   Mathieu/GMC, DC2, higher-dimensional descent, analytic, real, scaling,
   prime-td, or generic cohomological mechanism appears.

The global proof walls are unchanged: source-honest landing/coverage and a
cofinal complexity ceiling remain open.  The disproof wall is also unchanged:
no compatible all-depth fixed-support point or characteristic-zero map exists
yet.

## The central pincer

The two sides of the campaign now act on the same honest degree data.

### Disproof direction

For one fixed seed and the full D12 coefficient box, nonempty solution sets
modulo every `3^n` yield a compatible `Z_3` point by compactness.  Determinant
one makes each of the three source residue balls map isomorphically onto the
same target ball, so the lift is noninjective.  Transport to characteristic
zero would give a genuine JC2 counterexample.

No explicit collision variables are needed during lifting.  The complete
coefficient scheme, exact truncation maps, and the residue-ball theorem are
the interface.

### Proof direction

A characteristic-zero theorem covering both primitive maximum-12 pairs
would force every fixed-support B8/B9 tree to die at some finite modulus.
The current selected-Q8 certificate does not do this: B9 lies on a
monomial-core/order-one bad-reduction boundary rather than the licensed
generic order-three Kummer open.  The next proof-side task is the
denominator-cleared original coefficient scheme and its horizontal normal
cone along `3=p=0`, not substitution into divided generic formulas.

This pincer is exact but not yet effective in either direction: the proof
side has no complete max-12 theorem, and the disproof side has no all-depth
branch.

## Execution plan for speed

### 1. B9 branch and complete local scheme — highest immediate priority

Run two nonblocking tracks:

- **fast branch:** at every accepted parent, divide the exact determinant
  residual, solve the complete fresh D12 digit system, reconstruct over the
  integers, and advance immediately.  Parallelize rational/periodic digit
  reconstruction and exact substitution.  A finite PASS only advances the
  branch.  The first deterministic mod-729 parent stopped at mod 2187, while
  the complete 74-dimensional family passed; any new point branch must be
  chosen from that licensed liftable 35-dimensional projection.
- **complete family:** retain the full affine kernel at each nonlinear
  transition.  The mod-729 cone above the displayed parent has dimension 74,
  and its complete mod-2187 Bockstein leaves a `3^109` cone.  Continue the
  full affine modules rather than enumerating or selecting one representative.
  Writing `F=F5+243T`, the term `243^2 det J(T)` vanishes through modulus
  `3^10=59049` and first enters the divided carry at modulus
  `3^11=177147`; do not introduce the quadratic term one digit early or late.

If the fast branch fails, do not declare the seed dead; solve the complete
predecessor family.  If a unit obstruction kills that family, backtrack to
the preceding complete family.  An all-depth theorem requires either an
exact p-adic/algebraic component, a singular-Hensel certificate, or a proved
recurring state—not merely a long digit string.

### 2. Paired full D12 compilers

Compile both B9 and B8 from the same source-hashed engine:

- 91 monomials per output, 182 coefficient columns;
- all 276 possible determinant rows before removing literal zeros;
- direct original-coordinate and transported-AS matrices as independent
  controls;
- exact integer carry before every reduction;
- complete affine modules, cokernel bases, quadratic obstruction maps, and
  omission/degree/support controls.

B8 is the independent structural control, not an independent statistical
vote.  A complete early B8 obstruction would expose a real asymmetry; a
survivor now gives the second finite maximum-12 branch.  Continue its whole
degree-compatible family rather than silently widening the first coordinate
past `y`-degree eight.

### 3. Support-pressure invariant

The rational determinant normalization

`(s-s^3, t/(1-3s^2))`

exports successively higher powers of `s`.  The B9 finite-degree corrections
reroute the first exported terms through cross-Jacobian products.  Filter the
transported D12 correction complex by the first monomial outside the support
and compute its class in the cokernel of the fresh-digit operator.  This is
the best route to an effective finite-death theorem if the branch eventually
fails, and the best diagnostic of a hidden recurrence if it does not.

### 4. Q8/max-12 integral boundary

Keep all already-live moving-centre, finite-closure, original-J, and
coefficient-infinity jobs.  In parallel, audit B9 against every
pre-normalized integer row and identify the first denominator/cofactor that
vanishes at `3=p=0`.  Build only the resulting horizontal component or record
`NO HIT`.  Do not spend a large Groebner run on a generic divided chart that
cannot contain the seed.

### 5. D7 mechanism donor and TD6 cover

- Continue the reviewed/frozen D7 mod-729 hostile review and the source-
  complete mod-2187 quadratic carry.  Its nonreduced-family machinery should
  be shared with D12, but D12 receives the next idle capacity.
- Finish the near-terminal TD6 divisor cover and pending hostile reviews.
  The raw `C=U=0` intersection is now explicitly reopened by hostile review;
  rebuild it before any whole-cover composition.  Do not open a B9-to-TD6
  algebra lane absent a source map; the exact verdict is `NO DIRECT HIT`.

## Software architecture

One coordinate-covariant p-adic coefficient compiler should own:

1. integral tame word and inverse, with exact monomial-support transport;
2. original coefficient rows and a separately generated transported copy;
3. exact divided residuals, fresh linear operator, affine kernel, cokernel,
   and higher quadratic carry;
4. full/nonzero-row mappings, canonical serializers, source hashes, host
   custody, actual degree pairs, support caps, and negative controls;
5. branch mode for speed and family/Fitting mode for completeness;
6. automatic stop labels distinguishing point death, family death,
   fixed-support death, chart miss, and theorem-level resolution.

All CAS, exact-Python algebra, SAT, and Lean builds run on AWS.  The Mac is
limited to editing, hashes, SSH/status, and lightweight cloud-review clients.
At the placement audit, no campaign CAS worker was local, swapouts were zero
over the sample, and all seven AWS hosts had zero swap.

## Resource allocation and nonpreemption rule

For the current critical window use:

```text
AS/D12 lifting and obstruction       40%
Q8/max-12 proof and interface        35%
TD6 source-cover completion          20%
global landing/web reserve            5%
```

Within the AS share, target roughly 25 points at B9/B8 D12 and 15 at the D7
mod-2187 mechanism.  Do not kill near-terminal, source-valid Q8 or TD6 jobs
merely to make the accounting exact; apply the allocation to newly freed
cores and replacements.  Stop duplicate point branches once source hashes
show they are the same implementation, while retaining genuinely independent
matrix generation or replay.

Return toward `40 Q8 / 30 AS / 25 TD6 / 5 global` when the complete current
D12 families die, a clean max-12 scope mismatch is proved, or D12 ceases to
produce reusable obstruction information.  Raise D12 above 40 only for an
exact recurring carry state, rational/algebraic reconstruction, singular-
Hensel component, or another theorem-level interface—not for one more digit.

## Review and trigger policy

Producer work continues without waiting for hostile review.  Promotion waits
for a different-model audit of exact statements, hashes, degree/support
claims, and firewalls.

Additional consecutive finite-digit PASSes on the same B9 point are expected
news inside this round and do not each trigger another whole-portfolio scan.
The next immediate significant-news trigger is one of:

- a terminal obstruction for the complete current family;
- a complete B8 result;
- an exact all-depth/recurrence/Hensel certificate or rational Keller pair;
- a source-honest B9-to-max12 horizontal landing or proved chart miss;
- closure of a whole Q8 or TD6 source cover;
- external literature/news changing a canonical avenue.

Absent such news, the next full blind round is due no later than
`2026-08-26T04:15Z`.  The existing web-sweep deadline remains
`2026-08-26T01:26Z`.

## Bottom line

The campaign has not proved or disproved JC2.  It has acquired the cleanest
proof/disproof interface so far: explicit noninjective unit-leading residue
maps exactly at both maximum-12 frontiers, with one B9 fixed-D12 branch now
surviving several exact AWS-replayed digits.  Speed now comes from running a
fast selected branch, the complete nonreduced family, the B8 control, and the
integral max-12 boundary in parallel while reviews remain in the background.
