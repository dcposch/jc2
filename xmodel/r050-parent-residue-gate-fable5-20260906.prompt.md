# Hostile gate: R050 physical-place residue pilot

Report: xmodel/r050-parent-residue-gate-fable5-20260906.md. Budget 60 minutes,
desk proof plus tiny exact checks only, no AWS/heavy CAS. Host writes <=1 MB.
Producer Astra; independent model Fable. No jc2-lean, mutable/live reports,
shared ledgers or launcher/adapter edits. End with standalone <!-- BODY-END -->.

The completed producer report and 110-line check script are frozen below.
Root verified transactional publication and independently read/replayed both.
There is NO R050 kill, no complete source realization, no measured speedup.

Independently adjudicate these exact claims, without accepting the producer
or the older gate's status words as a proof:
1. The licensed R050 final major pattern has physical-place split 4+28,
   not 32 distinct places. Derive the zero-root centre and parameter u,
   including the absence of a t^(19/28) coefficient and alpha != 0.
2. From the actual D1 valuation floor derive H=Av+B+u H1(v)+O(u^2),
   deg H1<=4, A!=0, then on the generic fibre f=T derive
   Res(dx/f_y)=4 H1'((T-B)/A)/A^2. Check sign and all coordinate changes.
3. Audit the explicit 19-term family: total degree and top, both faces,
   exact physical branch, residue eps/[kappa^2(1-a)^2]. Does it prove only
   independence from those three faces, NOT from the whole R050 source?
4. Audit the mate-floor calculation and b H1'=-4 [u^1]J_xy. Separate
   redundancy on the full Keller ideal from possible early-row compression.
5. Verify positive automorphism and h=x+x^2*y negative controls, including
   every affine fibre having a regular primitive but no global polynomial
   mate. Audit the reported off-by-one error in Sol's twisted example;
   retain the older pencil report's explicit globalization caveat.

Source reading: use the completed exact-contact gate for its exact pattern,
then independently read the relevant Xu primary text provided. If an actual
source interface cannot be verified, mark that arrow GAP; do not fill it by
the coefficient family. Give a usable exact theorem at the surviving scope,
and the single cheapest next discriminator if any. NO_NEW_MECHANISM is fine.
Do not build a large receiver or claim that residue vanishing suffices for a
Keller mate. If tiny replay needs SymPy, run bounded locally and preserve
source bytes; store independent notes in box/r050-parent-residue-gate-20260906/.

charged_input=xmodel/r050-parent-residue-pilot-astra-20260906.md
charged_input=box/r050-parent-residue-20260906/check.py
charged_input=xmodel/exact-contact-gate-astra-20260906.md
charged_input=box/exact-contact-gate-20260906/xu.txt
charged_input=xmodel/ideation-20260906T0000Z-sol56.md
charged_input=xmodel/keller-pencil-genus-opus5-20260902.md
charged_input=FALLACY-v2.md

Read the frozen inputs in {{LANE_INPUTS}} and mechanically verify their SHA
manifest from the indexed charged_input fields of your own run.v2 receipt.
