# Blind full-spectrum ideation — cube/max12 owner — 20260825T0859Z

Status: **BLIND / PRODUCER IDEATION ONLY**.  I read the sealed state packet
`xmodel/ideation-20260825T0859Z-state.md` (SHA named there), all 46 canonical
rows and overlays in `APPROACHES.md`, the 08:55Z live-state section, and the
relevant current audit entries.  I did not read another `0859Z` submission.
No statement below is a mathematical promotion.

## 1. Compact whole-portfolio assessment

| Row | Current assessment and cheapest honest next discriminator |
|---:|---|
| 1 | GGV farm is a conditional finite-cell engine, not a landing or ceiling theorem.  Retain only as a source-typed regression suite and client for a future `G2-PSC` proof. |
| 2 | Sigray/Eggers--Wall is still the proof backbone.  TD6's fixed-center atlas is real progress, but arbitrary centers, valuative escape, full landing, and a cofinal sheet bound remain charged. |
| 3 | Strip/residue ODE is an exact narrow theorem.  Extend only when a named depth-three source row or maximum-degree client supplies the Ore/resultant data. |
| 4 | Formal germs are now bifurcated: Q8 uses component/genus specialization; AS uses a finite layered deformation system.  Neither finite depth nor a modular arc algebraizes by itself. |
| 5 | JvdK descent remains a moderate negative-control lane; no cusp-avoidance theorem is visible from the new news. |
| 6 | Abhyankar--Moh was applied to the wrong fibres.  Reopen only after an actual one-place component of `A(F)` is constructed. |
| 7 | `A(F)` remains the correct global object but has no cheap construction.  Keep a small reserve for extracting it from one fully realized TD6/Q8 boundary component. |
| 8 | Formal-inverse trees lack a uniform truncation principle.  No new client arises from the AS survivors. |
| 9 | Lee--Li/Magnus is an unbounded universal quantifier; hold after the existing smallest-cap controls. |
| 10 | HC4's named quintic gate returned `NO LEVERAGE`; do not revive without a new observable. |
| 11 | Mathieu/GMC/Poisson implication ladders are dead for this purpose. |
| 12 | Face-isolation multinomials add no asymptotic parameter beyond the existing face count.  Hold. |
| 13 | DC(2) is stronger and higher-dimensional; its degree-four black hole loses to the live plane gates. |
| 14 | The Zheglov/End(`A_1`) audit is defensive source work, not a current counterexample candidate. |
| 15 | Two-variable commuting-PDO spectral surfaces still lack the dictionary; no bounded experiment outranks current work. |
| 16 | No named holonomic index exists.  Hold until a ten-line invariant and a Keller functor are supplied. |
| 17 | Cubic stabilization loses the plane-origin class and now contains genuine higher-dimensional counterexamples.  Stop. |
| 18 | Weighted-homogeneous plane Keller maps are closed; use symmetry only as a stratum/control. |
| 19 | **Raised sharply by the two deep AS survivors.**  The honest target is now the complete local deformation/obstruction ring of each fixed finite-support source state, not more unstructured digit breadth. |
| 20 | Standalone `p`-curvature still consumes broken higher-dimensional bridges.  Its useful content is already inside row 19. |
| 21 | Hensel/model theory becomes useful only after a finite, source-complete system is smooth/flat and the collision open persists.  This is now an immediate AS client, not a theorem from the current finite-depth SAT states. |
| 22 | Integral-points/height methods remain pointed in the wrong direction and ineffective without a uniform pencil. |
| 23 | Holomorphic and metric global-inverse slogans are refuted by standard analytic phenomena. |
| 24 | Real Pinchuk maps remain a control, not a deformation to constant complex Jacobian. |
| 25 | Passports are useful only after source coupling.  Q8's primitive grouping and infinity theorem are a successful narrow client; raw passport census stays stopped. |
| 26 | Primitive monodromy is effective locally in Q8, but gives no global sheet ceiling.  Reuse only with full contact/source data. |
| 27 | Splice/topological diagrams overadmit algebraically unrealizable configurations.  TD6/Q8 algebraic source checks must remain attached. |
| 28 | Log/BMY needs completed tails and a non-costume divisor.  No new such divisor is supplied by the AS event. |
| 29 | Hamiltonian/LND cokernels are exact reformulations without a universal receiver.  Reuse the adjoint/Fitting machinery, not the global claim. |
| 30 | Makar--Limanov/exotic-surface classification does not see the embedding; low priority. |
| 31 | Integrality/ZMT is still the global finiteness wall.  Q8's proper arithmetic graph and TD6's Rees boundary are the only currently typed clients. |
| 32 | The secant determinant removes saturation syntax but not injectivity.  Use it only inside a bounded AS/Q8 source family with an explicit collision. |
| 33 | Untwisted action residues are exact-form `COSTUME`; no revival from the new states. |
| 34 | Rational pole removal remains a plausible negative-control experiment but is dominated by TD6's already typed valuation boundary. |
| 35 | Known higher-dimensional counterexample mechanisms are structurally non-plane.  Stop tourism. |
| 36 | Guided search is licensed only on a frozen finite source grammar.  The two AS survivors justify local completion around those points, not a fresh generic sparse search. |
| 37 | Finite-field census has value only as fibre/factor evidence with exact lifting firewalls, as in Q8.  It is not a counterexample census. |
| 38 | Tropicalization remains an overapproximation unless saturation and a named Newton datum survive; no current client needs it first. |
| 39 | Cohomological slogans remain untyped.  The usable specialization invariant is ordinary curve genus, already a concrete Q8 client. |
| 40 | Free-associative lifting does not follow from commutative determinant one.  Stop. |
| 41 | Naive scaling deformation is falsified by higher-dimensional counterexamples.  Stop. |
| 42 | Markus--Yamabe hypotheses do not follow from constant Jacobian.  Stop. |
| 43 | Ritt primeness collapses to the existing degree/leading-form reductions.  Hold. |
| 44 | The no-prime-degree proof is refuted; retain only the repaired `xy` membership theorem, which the AS geometry currently evades. |
| 45 | Differential-Galois treatment of the inverse is tautological beyond the known ODE client.  Hold. |
| 46 | Formal verification is valuable only after a finite universe and source theorem exist.  Apply it to small bridge lemmas, never as discovery or axiom laundering. |

## 2. Three highest-information mathematical bets

### Bet A — complete local deformation ring at the two AS survivors

This is the only new event capable of turning quickly into a disproof-side
endgame.  Replace state enumeration by the cotangent/obstruction complex of
the **complete remaining finite source system** at bases 513 and 519.

- Exact dependencies: the hash-pinned two SAT models and literal nested-source
  replay; a chronological compiler containing every still-unencoded lower
  Jacobian/Cartier coefficient; the fixed finite support/degree grammar; and
  a collision coordinate that is a unit on the chosen open.
- Cheapest falsifier: compute the first genuinely unencoded layer's RHS in
  the left cokernel of the full restoration Jacobian at both points.  A
  nonzero pairing kills that point immediately.
- First AWS task: emit, from one source compiler, the two sparse matrices,
  left-null bases, Fitting minors, and first Kuranishi quadratic; reimpose all
  earlier rows after every restoration.  If both pass, add all remaining
  finitely many determinant rows at once and compute flatness/smoothness of
  the resulting local `Z_(3)` scheme.
- Success condition: a source-complete finite system with collision open has
  a formally smooth/flat branch over a Henselian DVR (or an exact generic
  characteristic-zero point).  A finite tuple over its characteristic-zero
  fraction field then gives a finite-support characteristic-zero pair after
  field embedding.
- Stop condition: both points fail the first new cokernel class, the support
  must grow, or the collision becomes zero/nonunit.  Then classify the common
  obstruction before searching more states.
- Forbidden inference: SAT through the current 63+46 rows, a smooth truncated
  scheme, repeated finite lifts, or eventual-looking digits do **not** imply
  an all-depth lift, a polynomial pair, a collision, or a counterexample.

### Bet B — exact positive genus of the selected-Q8 plane component

This remains the fastest proof-side bypass.  The reviewed 37,010 contact
bound and arithmetic contact bridge mean that `g(H)>0` can kill the selected
primitive alternatives without first reconstructing a degree-one source
graph.

- Exact dependencies: reviewed mod-127 `H`-component; corrected contact
  erratum SHA `c470fd...` (old 35,582/4,448 retired); arithmetic contact bridge
  review SHA `571221ad...`; geometric irreducibility/separability of `H`; and
  the conditional branch-value genus lemma SHA `03bc2f9e...` or an exact
  normalization genus.
- Cheapest falsifier: exact normalization returns genus zero, or the radical
  discriminant minus the full singular-value projection has degree at most
  `2*190-2=378`.
- First AWS task: factor `rad Res_v(H,H_v)` and, for each irreducible
  `q(w)`, test in `F_127[w]/(q)[v]` whether
  `gcd(H,H_v,H_w)=1`; independently run normalization/brnoeth.  Sum only
  factors certified nonsingular, handle infinity separately, and demand
  `N>378`.
- Success condition: an exact genus lower bound `g(H)>0`, followed by the
  reviewed arithmetic specialization and primitive one-or-eight grouping,
  excludes a nonconstant `P^1` trajectory on every selected contact
  component.
- Stop condition: genus zero, inseparability, or an unclosed proper-model
  specialization hypothesis.  Resume exact coordinate-graph reconstruction
  rather than infer from Newton interior points or point counts.
- Forbidden inference: positive genus here does not close the other
  `(9,12)` leaves, polynomial core, `(8,12)`, arbitrary maximum 12, or JC2.

### Bet C — TD6 valuation-integral `N13` unit on the complete center cover

The highest-value common proof mechanism is to turn TD6's affine atlas into
a valuative statement.  The candidate `N13=(k/25) beta` is useful only if its
coefficient is a unit in the integral source algebra on every boundary chart.

- Exact dependencies: V50's denominator-free replay of all 54 original rows;
  the independent V52 order; the fixed source-typed three-center scheme; an
  exact normalization/Rees cover including coordinate infinity; and the
  source provenance of `N13`.
- Cheapest falsifier: one exceptional chart where the cleared coefficient has
  a zero/pole, or one original row whose reduction disagrees after the
  quotient-field substitution.
- First AWS task: compute numerator and denominator divisors of the `N13`
  coefficient in each normalized chart, their norms/Fitting ideals, and the
  saturated complement.  If the denominator is a unit and numerator is
  `k/25`, conclude `beta=0` for valuation-ring points and compose only then
  with the reviewed fixed-`A^3` emptiness.
- Success condition: a denominator-free/source-ideal unit certificate on a
  complete raw cover, including every coordinate boundary, closes the
  selected arbitrary-center valuation family.
- Stop condition: any nontrivial boundary divisor or positive-dimensional
  uncovered chart.  Build the explicit Rees fan there; do not add more affine
  points.
- Forbidden inference: a quotient-field unit, generic pivot, or finite affine
  cover does not close pole-valued points, full TD6, SP-2, landing, or JC2.

## 3. Two software/compute accelerators

1. **Restoration-curvature/cotangent compiler.**  A source-hashed compiler
   should accept layered rows, regenerate literal integer carries, reimpose
   all prior rows after each restoration, and output Jacobian ranks,
   left-cokernel witnesses, Kuranishi quadrics, Fitting minors, and exact
   source replays.  It directly serves AS Bet A and audits the same hidden
   assumption in TD6 and Q8 Taylor reconstructions.  Automatic descendant
   taint on a pin/replay mismatch is mandatory.

2. **Finite-algebra curve broker.**  Given fixed-fibre shape bases and a
   candidate eliminant, automate clean-basis parsing, coordinate interpolation
   or Hensel/Padé, full modulo-eliminant substitution, discriminant-factor
   sharding, singular-value projection, normalization/genus, denominator-unit
   checks, and arithmetic-contact manifests.  Every stage is content-addressed,
   AWS-only, holdout-tested, and fail-closed.  This is reusable for Q8, TD6
   exceptional curves, and any finite AS local component.

## 4. History/dedup and adversarial checks

- Bet A is not the old finite-state recurrence card: it uses the new
  source-complete survivors as local points and asks for the next unencoded
  obstruction plus a finite full-system flatness theorem.  It earns no credit
  until the remaining source rows and collision open are literally present.
- Bet B is an accelerated endpoint of the existing selected-Q8 component
  lane, not a new global monodromy theorem.  It consumes the corrected
  37,010 theorem and refuses the superseded constants.
- Bet C is not the quarantined quotient-field `N13` inference: its whole point
  is integral divisor accounting on a complete cover.
- I do not revive action residues, HC4, raw passports, generic sparse search,
  naive projective connectedness, or finite-state periodicity; their recorded
  negative controls still apply.
- Shared hidden assumption to attack: all three bets can accidentally replace
  a full source scheme by a projected/quotient object.  Acceptance therefore
  requires the same literal source rows, open/localizer, and boundary model
  before and after elimination or specialization.

## 5. Reallocation caused by the deep AS survivors

Shift the working allocation from roughly `35% Q8 / 30% AS / 30% TD6 / 5%
global reserve` to **40% AS / 35% Q8 / 20% TD6 / 5% landing-and-ceiling
reserve**.  AS receives the largest share because two independent full-gate
survivors make a local deformation computation materially more informative
than additional broad state search.  Q8 retains nearly equal resources
because its genus/factor and graph gates are already exact, parallel, and
close to binary endpoints.  TD6 stays active but loses breadth until V50/V52
settle denominator provenance.  The global reserve remains nonzero: neither
a Q8 selected-leaf kill nor a TD6 fixed-center theorem supplies `G2-PSC`, full
landing/coverage, or a cofinal complexity bound.
