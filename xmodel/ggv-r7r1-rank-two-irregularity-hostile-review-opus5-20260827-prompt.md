# Opus5 hostile review — R7R2 rank-two / irregularity typing audit

Act as a different-model hostile mathematical referee.  Review exactly

`xmodel/ggv-r7r1-rank-two-irregularity-typing-audit-sol-20260827.md`

whose required SHA-256 is
`bd742ac48bb8768b3ea67089e3e749f43d292b09077e3d178904c71c685acb2c`.
Fail closed on a mismatch.  Its frozen dependencies and hashes are listed in
Section 1 and must be rechecked.

The report challenges your round-`20260827T1808Z` proposal that retaining
`(F,G)` gives a rank-two connection or that a `t`-direction pushforward may
automatically add irregularity.  Re-derive rather than defend the proposal.
Audit:

1. the exact `P,W,s=t/P` conjugacy and scalar equation at fixed `P`;
2. whether a nonlinear scalar equation for two series can legitimately be
   called a rank-two connection, before or after linearization;
3. `M=D_(X,s)/D_(X,s) partial_X`: characteristic variety, absolute versus
   relative holonomicity, and whether Malgrange's irregularity/index formula
   is applicable;
4. whether the arbitrary kernel `C_L[[s]]` really witnesses the missing
   `s`-relation;
5. the generic formal-etale and zero-`s`-slope claim, including the boundary
   at roots of `H` where `p` is not a unit in the polynomial ring;
6. the report's regular-holonomic direct-image paragraph.  Check nonproper
   maps, algebraic versus analytic scope, moving/open divisors, Gauss--Manin
   confluence, and delete or repair any overstatement;
7. whether an honest holonomic object forced by support/descent already exists
   elsewhere in the campaign and invalidates the no-go;
8. the narrowest justified allocation change for avenues 16 and 25.

Use primary D-module sources where necessary.  Try to produce a counterexample
to every broad sentence.  Distinguish `REFUTED`, `CONFIRMED WITH REPAIR`, and
`CONFIRMED`, and state exactly what remains a legitimate irregular/higher-rank
avenue.  No agreement-by-model and no JC2 overread.

Write exactly one new file:

`xmodel/ggv-r7r1-rank-two-irregularity-hostile-review-opus5-20260827.md`

Do not edit canonical ledgers, the producer, frozen artifacts, adapters, or
any other file.  Do not enter, list, grep, read, build, status-inspect, or edit
`jc2-lean`; search only from `/Users/dc/code/math/jc2` with that child
excluded.  No heavy local algebra and no AWS mutation.  Read-only primary-
source web access is allowed.  Record exact hashes, checks, model identity,
scope firewall, and the final output SHA in your response.
