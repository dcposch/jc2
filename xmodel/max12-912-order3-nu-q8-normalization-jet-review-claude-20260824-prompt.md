# Hostile review — corrected Q8 normalization, norm, Taylor, and terminal jet

Act as a hostile different-model mathematical reviewer in
`/Users/dc/code/math/jc2`. Read in full:

- `xmodel/max12-912-order3-nu-q8-normalization-jet-20260824.md`;
- every file in `cases/max12_912_order3_nu_q8_normalization_jet_20260824/`;
- only the predecessor reports/cases pinned by that manifest.

Charged hashes:

```text
report    739fbad475bc10540756c7a0180168185523f6e3cfe368225e03efa1abe62d65
manifest  036b8656ba53dd3b1b212b2bf5bec6d0eed83f8a56023cf3ebf0b689156d8971
freeze    807ad9dacbc477972c3c30b3770d84ca27340b5771ec2aad3efc90d7eb3e5b50
```

Run the replay, then independently reconstruct and attack these exact points:

1. irreducibility of `Q8` over `Q` and every claimed unit inference;
2. the seven-row order-three fibre, the `t=a0` odd parameter, the displayed
   cubic/even jet, and the nonzero quadratic coefficient of `r8`;
3. substitution into the reviewed root-free norm: the node lies in leaf 3,
   while `E/t` is a unit and the punctured branch lies in leaf 4;
4. all localization and scaling assumptions, especially restoring general
   `p0`, keeping `nu` differential-constant, and distinguishing formal fibre
   geometry from an actual trajectory;
5. reconstruction of all 23 original Taylor members at the true center
   `r=A/9`, including the negative control against using `z=0`;
6. provenance of `9*r8'=j/u`, differentiation of the jet, and the finite-place
   valuation contradiction; check whether cancellation, ramification,
   fractional valuations, variation of `p0` or `v0`, or omitted higher terms
   can invalidate it;
7. why the infinity calculation is only a necessary condition and why no
   global algebraization, rationality, or Jacobian-conjecture conclusion is
   claimed.

Do not inherit the quarantined Q12 calculation. Distinguish identities checked
by replay from interpretive deductions. Find the smallest false identity or
missing hypothesis if one exists. Keep the review focused and concise.

Write exactly
`xmodel/max12-912-order3-nu-q8-normalization-jet-review-claude-20260824.md`.
Do not edit producer, case, canonical, coordination, prompt, log, run, or any
other review files. End with exactly one verdict: `CONFIRMED`, `GAP`, or
`REFUTED`.
