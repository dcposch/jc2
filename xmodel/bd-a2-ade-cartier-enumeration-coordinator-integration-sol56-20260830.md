# Binding integration: capped local ADE Cartier enumeration

Date: 2026-08-30 UTC  
Coordinator: Sol 5.6 Ultra  
Frozen integration basis: `2bd4b54166cddb26dad415d6462cc4ff55e76885`  
Lifecycle: **PROMOTED EXACT LOCAL TABLE / GLOBAL GEOMETRY OPEN**

## 0. Verdict and custody

This integration binds the exact producer and reproducible artifacts

```text
03263fe7f7d97356f07291bb30ec38d5b4182a1ffc85fcbf57722d59cf3b50a9
  xmodel/bd-a2-ade-cartier-pair-enumeration-sol56-20260830.md
  body 20023 / 002ce81b56f2d703766db0e5a52696a8537be5cee52c2730d3832eaa6d1f85dc
  manifest 443d4e5236e8e0649878c8e408a1dc92ebb779573a57638c90467fcfda769fb9
d79fd1c484f229a0d564f537e29a31de6b86c97250db7649a6edf2de34ee8a94
  ops/ade_cartan_pair_enumerate.py
bfbd89d61d1181bc0bdb9a243c8d3b85074541194621eb57047af9ecf884e279
  xmodel/bd-a2-ade-cartier-pairs-cap8-sol56-20260830.json.gz
  canonical JSON 3762426 / 11ca074c9a4bc6656960222897d8c674f8486970b7a20cb1975cfcdb818d1d19
```

to the GPT-5.5 xhigh hostile review

```text
f2a35e4e427eb00c928ba7bf13e1ff0225e3d3bccfb1804511101a75039458a3
  xmodel/bd-a2-ade-cartier-pair-enumeration-hostile-review-gpt55-20260830.md
  verdict CONFIRM_WITH_CORRECTIONS.
```

The producer was sealed on basis
`986427df23c30375b6edfd659ba8a6c6139434ab`, published with its artifacts at
`02572552f8f894ede08acb4a020a3af722b03e54`, and reviewed on that publication
basis.  Root read every line of the 500-line producer, regenerated identical
logical and gzip bytes in 1.2 seconds, replayed all 72,695 rational systems,
and hash-checked the external receipt.  The reviewer independently rebuilt
the graph automorphism groups, exact solves, germ DP, and every canonical JSON
record.  No script or data correction is required; the scope corrections
below are binding.

This table charges the promoted ADE threat-map integration
`3846f7e2c861ebf5f399dbc9a729c3f1d35dc28c561891c8883a8a3328a7240f`.

## 1. Exact promoted table

Fix **one connected exceptional component at one Du Val point**, of type

```text
A1,...,A8 or D4,...,D9.                               (1.1)
```

With positive Cartan matrix `C`, enumerate

```text
n in Z_(>=0)^r minus {0},       n in C Z^r,
m=C^(-1)n in Z_(>0)^r.                                  (1.2)
```

Under the proper target-line local cap `sum n_i<=8`, the exact counts are

```text
72,695 nonzero candidate vectors;
56,335 fail Cartier integrality;
16,360 labelled (m,n) pairs;
12,238 orbits under local Dynkin-diagram automorphisms. (1.3)
```

In the separately conditional stratum where the relevant `B`-fibre meets the
different properly, `sum n_i<=4` gives

```text
3,218 candidates; 2,446 nonintegral;
772 labelled pairs; 646 diagram orbits.                 (1.4)
```

The exact `A_r` congruence, `D_r` parity/spin congruences, inverse formulas,
strict positivity, D4 triality, chain/spin automorphisms, cap nesting, and
raw-orbit recovery are those promoted in the charged threat map and are
independently reproduced by the committed standard-library data.

The diagram quotient is unlabelled local bookkeeping.  Dominance proves that
different displayed `n` vectors are in different Weyl orbits, but no Weyl
reflection is asserted to preserve the geometric marking, effectivity, or
other divisor labels.

## 2. Numerical physical-germ partitions

On the smooth minimal ADE resolution, one irreducible strict branch has one
centre on the exceptional SNC divisor.  Its numerical contact vector is
exactly

```text
k e_i at a smooth point of E_i, or
u e_i+v e_j at the node E_i intersect E_j, i--j,       (2.1)
```

with all displayed coefficients positive.  Every single atom in (2.1) is
locally realizable as an irreducible plane branch; this does not assert that a
collection descends to the charged Cartier divisor or occurs in an incidence
equation.

The committed generating-function DP counts unordered multisets of these
numerical atom types.  For an actual strict divisor, its minimum is a lower
bound for the number of analytic local branch germs.  It is not a lower bound
for global carrier components, distinct physical points on the minimal
exceptional divisor, or realized incidence solutions.  Coordinate entries,
germs, physical points, separated final joining paths, and global carriers
remain distinct.

Without carrier labels, every listed `n` has a unit-vertex partition that can
be drawn as separate outside leaves on the ADE tree.  This proves only that
the unlabelled local numerical forest test eliminates none of (1.3) or (1.4),
not that any row is realizable.

## 3. Exact conditional one-carrier cut

Assume additionally, on one common embedded SNC resolution, that:

1. the ADE tree and all shared infinitely-near attachment trunks form one
   connected inner graph;
2. the reduced strict transforms of the actual outside carriers form one
   connected subgraph disjoint from the inner graph before final attachments;
3. distinct analytic branches give distinct final joining edges.

Two branches then give two edges between connected disjoint subgraphs and
hence a cycle.  A rational forest permits exactly one germ.  Filtering the
local table by the one-atom condition leaves exactly

```text
cap 8: 718 labelled / 563 diagram orbits;
cap 4: 198 labelled / 163 diagram orbits.              (3.1)
```

Shared trunks, coincident initial points, or branches through one ADE node do
not invalidate this only because they are resolved inside the common inner
graph before distinct final edges are counted.  Carrier intersections or a
shared nonexceptional component invalidate the disjoint-subgraph hypothesis;
(3.1) must not be used there.  “One germ” may be an edge atom supported on two
adjacent Dynkin vertices.

## 4. Cap typing and exact successor

The target-line cap eight is a local consequence for the chosen proper line
through one singular image; it is not one budget to allocate among unrelated
singular points.  A fixed infinity divisor or one `B`-fibre can instead give a
shared budget among the configurations lying on that actual slice, but only
under its own proved proper-intersection/no-common-component identity.

The promoted counts are therefore a complete exact table only for one
connected local ADE component under the named cap.  They are not counts of:

```text
disconnected exceptional multisets;
simultaneous root-subsystem embeddings in D9(-1);
effective exceptional curves or ruled-surface blowup clusters;
paired infinity vectors (h,a=Ch);
physical carrier labels or shared-slice allocations;
analytic bidegree-(2,3) incidence equations;
finite maps, polynomial maps, counterexamples, or JC2 evidence. (4.1)
```

The smallest global successor is carrier-labelled geometry: enumerate
simultaneous `D9` embeddings and actual vertical conic-fibre configurations;
attach paired infinity data and only the correctly shared slice budgets; test
strict carrier classes for effectivity in the nine-blowup ruled marking; then
apply (3.1) only where its connected-carrier hypotheses have been certified.
No further optimization of the local table is useful.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7075`.
- Body SHA-256:
  `70abb6a03b8577b38d9fc33810585ff7dc732200dd2d53bd7f00d234adbc2c66`.
- Frozen basis: `2bd4b54166cddb26dad415d6462cc4ff55e76885`.
