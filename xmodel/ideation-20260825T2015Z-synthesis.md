# Significant-news synthesis: classical closure, universal terminal passport, and live reallocation

Date: 2026-08-25 20:15Z  
Coordinator: Sol  
Trigger: primary-source classical degree correction plus a reviewed branch-wide
order-three theorem

## Evidence consumed

This synthesis cross-reads the blind/cross-audited mini-round

- `ideation-20260825T1940Z-as-transfer.md`;
- `ideation-20260825T1950Z-cross-as.md`;
- `ideation-20260825T1950Z-cube-classical-closure-v2.md`; and
- `ideation-20260825T1950Z-cube-ft2-faber-dedup-v3.md`.

It also consumes the classical-closure producer/review pair
`def1c2a1...` / `511d8a55...`, the universal Faber producer/review pair
`d303bf76...` / `e2ddc5b5...`, the whole-order-three terminal Belyi
producer/review pair `28f33b99...` / `5da99825...`, and the squarefree
homogeneous recurrence producer/review pair `ff082a2c...` / `a5492704...`.
No unreviewed computational verdict is promoted here.

## Decisive routing corrections

1. Every characteristic-zero envelope with both actual total degrees at most
   12 is already counterexample-closed by the GGV/Heitmann lower bound
   `gcd(deg_total P,deg_total Q)>=16`.  Strict or broad D12 B8/B9 work is a
   method/custody corpus, not a counterexample frontier.  Remaining long
   fixed-D12/fixed-support solver races were stopped without a verdict.
2. Rebuilding the raw `21 x 23` coefficient-function Ore matrix or another
   high Faber row is duplicate work.  The reviewed differential-field Faber
   theorem already integrates every high row with unbounded coefficient-`x`
   degree.  The first new object is the algebraic lower-Laurent fibre together
   with the original Taylor polynomiality boundaries.
3. On every actual nontrivial order-three partial-`y` `(9,12)` trajectory,

   ```text
   T=r8^3 in C(x),       h=(j^3/27)*T^2/(T')^3,
   ```

   and `T` has the reviewed balanced Belyi passport.  This is branch-wide;
   selected Q8, `k=mu=0`, and `nu!=0` are not hypotheses.  It is a necessary
   classification, not an exclusion: the cyclic `D=1,2` terminal rows remain
   positive controls.
4. The strict-D12 squarefree recurrence through homogeneous rows 18--12 is
   independently confirmed at reduced-point/UFD scope.  It is a useful
   triangular-method control, but adds no live partial-`y` scope beyond the
   Faber theorem and must not receive a standalone compute allocation.

## First genuinely live gate

The next gate is **passport-aware lower-fibre/Taylor realization**, not a
high-row or fixed-cap search.  It starts from the frozen order-three compiler

```text
r1=r2=r4=r5=r7=0,       r3=mu,       r6=nu,
9*r8'=j/u,              u^3=h,
```

and retains all constants `(k,mu,nu)`, all eight coefficient functions, the
true center `r=A/9`, and all `10+13` original Taylor members.  Two bounded
fronts may run in parallel:

- **unhandled coefficient front:** begin with the `k!=0` chart, classify its
  source-honest lower-fibre components and the projection to `r8`; do not
  repeat the selected `k=mu=0,nu!=0` Q8 component;
- **passport/pole front:** impose the cyclic `D=1,2` terminal controls first
  and compute, at each zero/pole of `h` and at infinity, the exact Kummer
  valuation/tropical initial systems of the seven lower rows plus both Taylor
  families.  The passport fixes the local orders of `h`, `T`, and `r8`, so
  this is a finite principal-part test rather than a coefficient-`x` cap.

The second front is potentially the faster global discriminator.  For a
finite place with `m=ord(h)` and `z=ord(T)`, use ramification
`e_p=3/gcd(3,m)` and the forced cover valuation

```text
ord_cover(r8)=e_p*z/3.
```

Kummer character congruences for every `a_i` and the true-center variable
must be retained.  Taylor regularity is imposed as nonnegative cover
valuation of the invariant expressions

```text
u^ell*f^(ell)(r)/ell!,       u^ell*g^(ell)(r)/ell!.
```

An initial-ideal infeasibility is useful only with complete cancellation and
residue checks; a tropical prevariety miss is not a theorem.

Mandatory controls are the exact selected-Q8 source on its licensed chart,
the zero-terminal common-cubic family at `j=0`, both cyclic terminal
passports, and a power-core `h=g^3` input that must fail the nontrivial-Kummer
admission gate.  A pass must produce either an exact component/pole-prime
classification, an exact finite-place Taylor contradiction, or a literal
rational survivor with every 23 Taylor member and the original terminal row
replayed.

Stop if the result is only a dimension, sampled-degree agreement, modular
support guess, or another Laurent coefficient.  Stop a finite-state claim if
the minimal state grows with `deg(h)` or discarded coefficient history is
needed.  Heavy execution is AWS-only and preregisters
`ops/frontier_gate.py --purpose frontier --tag <registered-tag>
--partial-y-degrees 9 12 --total-unbounded`.

## Parallel portfolio

Scheduling target for the next cycle:

```text
50%  unbounded order-three lower fibre / Taylor / passport
25%  TD6 source-complete B3-to-A3 cover
15%  global landing, packet compatibility, and cofinality
10%  genuinely unbounded AS/support-growth transfer and certificates
 0%  standalone FT2/high-row or fixed-D12 theorem search
```

The existing Double-B characteristic-zero membership races and Q8
source-horizontal/projective-boundary jobs remain inside the first 50%:
they address live components/landing and are not fixed-D12 searches.  TD6
V73--V75 continues independently.  The long `d25` global trajectory build is
also retained.  Review remains asynchronous; reasonable producer confidence
unblocks unrelated successors.

## Proof-wide wall and firewall

Even a complete order-three `(9,12)` theorem is not JC2 without a proved
landing/cofinal partial-`y` bound or a complete global packet/source cover.
Conversely, finite-support 3-adic death and fixed-D12 recurrence do not imply
the unbounded theorem.  The order-one `(9,12)` leaf, both `(8,12)` nontrivial
leaves, power-core strata, and all global landing obligations remain open.
No proof or counterexample to JC2 is claimed.
