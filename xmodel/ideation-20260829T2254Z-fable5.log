cli=2.1.228 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai
Blind submission complete. The report is at `xmodel/ideation-20260829T2254Z-fable5.md` (30,831 bytes, ends with the standalone `<!-- BODY-END -->`, no seal, no exit-price line). Custody: the state packet hash, HEAD, all seven charged files, and the all-faces producer `64eaafe3…` were all recomputed and matched before use; I read no other file from this round, wrote nothing else in the repo, and launched no CAS.

What the report says, in brief:

**Disposition (46 avenues):** 45 unchanged, one raise — Avenue 4's algebraization sub-lane. The justification is a conjunction that only became true this round: the `e=3,m=1` fixture is the first surviving exact K00 jet in campaign history, and the effective decision tool it needs (Tougeron's `2e+1` criterion) has been banked in DEPTH-STAB since the D-series era but never had a live client.

**Three research cards:**
1. **K00-ARC-DECISION/v1** — compute the Jacobian contact order `e` along the `e=3,m=1` fixture and apply the `2e+1` Newton bound. Grep-verified as never executed on K00 (only generic Artin mentions and char-p box proposals exist). Every outcome is decisive: either the fixture extends to a formal arc (first attained ramified source point, correctly typed as not-a-map), or the G9 carry inherits an exact stopping grade instead of open-ended grinding, or the obstruction series names the kill grade. Desk-scale, provisionally launchable now.
2. **STRICT-COLLIDE-POLY/v2** — enlarge the reviewed forced-branch ansatz with `b(s)t²` and `c(s)` terms and decide whether zero-excess collisions still force non-square data. I also recorded that my attempted avenue-33 reopen (action-form periods as the polynomial-origin instrument) self-refutes — the form is closed and the cycles bound — so the 0719Z stop stands and nobody re-derives it.
3. **ATYP-COUNT/v1** — the genuinely new mechanism: `#atypical ≥ s−1` is named as necessary for QCS in three files but has no owner or route anywhere in the repo; I propose asymptotic-critical-value (Rabier/Parusiński–Tibăr) boundary-tree separation, falsifier-first on non-Keller controls.

**Software + systems:** the `tougeron_check` exact auditor (with the sealed `−4 → −3` mutation as its planted negative control), and a systems UPGRADE card — sectioned deliverables with per-section seals, directly motivated by the QCS-GLUE-OR-KILL 64k-token total loss, with a mechanical three-section acceptance test.

**Portfolio headline:** run the Tougeron computation before the G9 carry, since its result determines whether the carry is a test or a foregone conclusion.
