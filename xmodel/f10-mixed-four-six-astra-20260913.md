# Integral-coordinate mixed-scalar discriminator

MANUAL co-research, not FIRST or promotion. First action 2026-09-13 04:17:21.458234425 UTC; original reserve04:31/HARD04:34 unchanged. Own targets absent; all four mathematical inputs, COORDINATION and unchanged administrative finalizer pinned before reads.

## Quantity

Decide the same mixed B on every leading root in r=4 or6 modulo7, retaining all parameter digits and factorial carries. The one selected discriminator below treats r=6 modulo7; r=4 is not calculated or claimed closed.

## Outcome

For every actual r>=2 in the six infinite classes

    r=6,13,20,27,34,41 modulo49,

the mixed B is a unit on the ENTIRE actual leading algebra. At every root v_7(B)=0, including roots over arbitrary residue-field extensions. Higher parameter digits are unrestricted. The exact remaining carry in this class is r=48 modulo49. There the first deflated test vanishes on a six-root cluster, while the other root is still proved nonzero. This is a precise residual obstruction, not an actual B-zero, all-r theorem or claim about r=4. All new conclusions require independent FIRST.

Use phi=1+u+Xu^2+Yu^3, T_h=trunc_7(phi^h), and

    B=[u^14](phi'/phi)T_(2t-1)T_(4-t),
    t=(5r+2)/(3r+1),  d6=d7=0.

The charged source map and coordinate integrality for EVERY root when r=4,5,6 modulo7 are imported at their accepted scopes. The old middle Z/H7 scalar is not B. No source unit is assumed to have unit residue.

## 1. Exact deflation and its factorial carry

For r=6 modulo7 write t=5+7e with e in Z_7, s=2t-1=2+7(1+2e), b=4-t=6-7(1+e). The universal polynomial quotient

    R_s=(T_s-phi^2)/(s-2)

exists coefficientwise over Q[s,X,Y], since T_2=phi^2 exactly. The dropped product (phi'/phi)*phi^2*T_b has degree at most12. Consequently, BEFORE any source reduction,

    B=(2t-3)J,  J=[u^14](phi'/phi)R_s T_b.             (1)

Coefficients of 7R_s are 7-integral. Among terms of u-degree<=7, a factorial denominator divisible by7 can occur only in the pure u^7 term: all other multinomial lengths are at most6. Monic division by s-2 introduces no new denominators. Its exceptional coefficient at s=2 is

    binom'(2,7)=1/105,  7*binom'(2,7)=1/15=1 mod7.

Thus, for integral X,Y and s=2 modulo7,

    7R_s = u^7 modulo7,
    residue(7J)=[u^7](phi'/phi) residue(T_b).          (2)

This keeps the carry rather than replacing the divided quotient by a characteristic-seven derivative. For h=h0+7h1 with 0<=h0<=6, the finite binomial formula gives

    residue(T_h)=trunc_7(phi^h0)+h1bar*u^7.

Indeed only binom(h,7) has a factorial7 denominator; its residue is h1bar. Every other coefficient is determined by h0. In particular the full second digit of b in (2) is -(1+ebar).

## 2. The deflated residue is one fourth power

All expressions in this section are in the residue field; bars on X,Y,e are suppressed. Write a_n=[u^n]phi^-1 and b_n=[u^n]phi^-2. Frobenius phi^7=1+u^7+X^7u^14+Y^7u^21 gives

    d6=b6=0,  d7=b7+1+e=0,
    [u^8]phi^6=a8+a1=a8-1.

Using (phi^6)'=6(phi'/phi)phi^6 in characteristic7, equation (2) becomes

    residue(7J)=6(a8-1)-(1+e)=-a8-e.

Differentiating phi^-1 instead gives a8=-(b7+2Xb6+3Yb5). Therefore

    residue(7J)=-1-2e+3Yb5.                           (3)

Here are the finite coefficient calculations and their explicit reductions:

    F=Y^2+(2-X)Y+X^3+3X^2+4X=0,
    C=[u^7]phi^5
      =3X^2+6X^3+5Y+4XY+2X^2Y+2Y^2,
    e=-C,
    b5=1+6X+2X^2+2Y+6XY.

The first equation is d6/3. Reducing by F gives

    C=4X^3+4X^2+6X+(1+6X+2X^2)Y,
    Yb5=X^4+X^3+5X^2+6X+(4+3X+X^2)Y.

In 1-2C-3Yb5 the entire Y coefficient cancels. The result factors exactly:

    1-2C-3Yb5
      =1+4X^4+3X^3+5X^2+5X
      =4(X^2+3X+4)^2=4(X-2)^4.

Substituting e=-C into (3) consequently proves

    residue(7J)=3(X-2)^4.                             (4)

Every equality is a polynomial coefficient identity or reduction by an actual source equation. No residue-field division by Y, d5 or X-2 occurred.

## 3. Uniform consequence and exact exceptional cluster

If (4) vanishes in ANY residue-field extension, X=2. Then F forces Y^2=0, hence Y=0 in that field; the displayed C is4, so e=3. Conversely (X,Y,e)=(2,0,3) satisfies both source residual equations and makes (4) zero.

If e!=3, then both (4) and 1+2e are nonzero. Hence v_7(J)=-1, v_7(2t-3)=1, and (1) gives v_7(B)=0 at every leading root. Writing r=6+7n gives

    e=-(9+10n)/(19+21n)=1-2n modulo7.

Thus e=3 exactly when n=6 modulo7, i.e. r=48 modulo49. This proves precisely the six classes stated above, without a bound on any later digit.

For the exceptional carry put t0=3/2 and a=X-1/4. Since 2t-3=(r+1)/(3r+1), v_7(t-t0)=v_7(r+1)>=2, finite but with no assumed upper bound. The charged U,V,K,P formulas specialize exactly to

    U0=3a^2,  V0=a^3,  K0=a^2(a-3/2),
    P0=9a^7-(3/2)a^6.

Their coefficients differ from their t0 values by multiples of7: U,K have 7-integral denominators and V has at most one factorial7 denominator, while t-2 is a unit. Thus

    residue(P)=2(X-2)^6(X-1).

Hensel factor separation between these two coprime factors counts six roots, with multiplicity, in the X=2 cluster and one in X=1. In the first cluster F forces Y=0; the source guard cannot remove it, since rational-algebra unitness does NOT imply nonzero residue. At X=1, F together with e=3 gives Y=2, and (4) is nonzero. That root has v_7(B)=v_7(2t-3)-1, so remains nonzero. The six-root cluster has residue(7J)=0 and therefore needs a genuinely subsequent term; (1) alone supplies no answer there.

No actual characteristic-zero root with B=0 or lift of a B-zero was exhibited. The residual pair is a control for the failure of this discriminator, not an actual source point. Whole-algebra unitness in the six successful integer classes follows from absence of a zero at EVERY algebraic root, hence a rational Bezout inverse under the accepted septic presentation; unitness persists under base change and nilpotents.

## Stopping point and documentary scope

The one exact test is exhausted at (4) on the six-root r=48 cluster. No repeated carry expansion, parameter farm, rzero/rthree/five49 reproof or r=4 calculation is added. The exact unclosed quantity is the first nonzero term of 7J on that cluster for arbitrary v_7(2t-3)>=2; no valuation bound or discarded guard supplies it. A focused manual coefficient/factor check is the cheapest proposed audit, planning5 minutes UNMEASURED, not execution or a new canonical OPEN. This gives no REG, source exclusion, all-F10 or JC2 conclusion.

Four mathematical inputs freshly WHOLE after current pins: remaining-local report (its final seal tail recovered separately), old final-residues report, exact reduction and mixed-scalar FIRST. COORDINATION uses personal exact-byte WHOLE reuse from September12 20:05-20:14UTC, freshly pinned. No live Fable, linked input, other source, corpus, network, worker, agent or protected tree/mirror. Only manual mathematics, inert text/hash/UTC/apply_patch and the pinned administrative finalizer; no scientific execution/import/AST/syntax/test/CAS/helper or shared edits. Own WHOLE, postpins and collision/quantity checks precede marker LAST, expected verification and final custody.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7211`.
- Body SHA-256:
  `3e2b0ef9bdb367874ab3a58b2f71bb159fa48e6b376f94c37b299e33949caa08`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
