You are the hostile independent reviewer of a claimed exact first
normal/divisibility jet for the `(8,12)` order-two one-parameter Rees family.
This is text-only review.  Do not run local Singular, Sage, msolve, Lean, or
substantive exact Python.  Do not enumerate the dirty worktree or print broad
`git status`; inspect only the target and its charged sources.

Target:
`xmodel/max12-812-order2-first-normal-divisibility-jet-theorem-20260826.md`

Required target SHA-256:
`827e76bde5d394ab17f7c05561a1bf254b6fdf8fc3453dd60400d16e80061fdc`

Independently attack:

1. Verify every coefficient in the common-quartic expansion `(1.1)`, and
   prove it is an ambient-coordinate isomorphism on `D(Lambda)`.  Decide
   whether allowing `p,c,r` to remain full variables really absorbs all
   tangent motion and whether the zero normal section has the stated
   higher-contact meaning.
2. From the exact source audit plus erratum, verify the unloaded seven-tail
   map vanishes and has zero full coefficient differential at every
   `K^2`.  Check that this implies polynomial divisibility by `Lambda^2`,
   not merely an order statement on reduced points.  Include the load and
   target exponents and affine-load hypothesis; look for any cross term of
   lower order.
3. Verify that dividing all seven rows by `Lambda^2` preserves the scheme on
   `D(Lambda)`, and state precisely what saturation is still required for
   the strict closure.  Fail any inference from the unsaturated first gate
   to the full Rees boundary.
4. Re-derive the shape of `q_l`: quadratic in the four normal variables plus
   `k10` times the exact load derivative at `K^2`, independent of
   `k6,k2,mu2,mu4,mu6,J`.  Check this is literal product-factor
   independence, not unsafe elimination of a finite quotient.
5. Audit both ideal saturations in `(3.2)`.  Is `(p,c,r)` exactly the
   weighted-projective common-quartic irrelevant locus?  Does saturation by
   `(n0,n1,n2,n3,k10)` isolate nonzero first normal/load direction without
   losing higher-contact arcs or falsely declaring them impossible?  Check
   multi-generator saturation semantics and nilpotents.
6. Check source scope: reduced common-quartic support is enough to cover
   leading points, but not a license to identify its scheme structure with
   the seven-tail fibre.  Ensure no `(9,12)` formula, terminal `U=4` map,
   Taylor condition, or order-two closure is imported.

Identify the smallest failing identity/hypothesis and give the strongest
corrected theorem.  Write only to
`xmodel/max12-812-order2-first-normal-divisibility-jet-review-grok-20260826.md`.
Include the recomputed target/source hashes and end with exactly one of
`CONFIRMED`, `REPAIR`, or `REJECTED`.  Do not edit the target, compiler
packages, source clients, or top-level campaign files.
