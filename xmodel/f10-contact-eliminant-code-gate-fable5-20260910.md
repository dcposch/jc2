# FIRST gate: contact eliminant implementation, producer/checker source (producer f10-contact-eliminant-code-astra-20260910)

2026-09-10. Gate lane fable5. First action 04:24:42 UTC; fixed stop 04:38:42 UTC (first+14 min, earlier than 04:39:00); publication reserve from 04:36:42 UTC; never reset. Own report and box verified ABSENT at 04:24:42 before the skeleton. Charged inputs: the eight ordered SHA256 pins of the lane brief, all eight verified byte-identical in /tmp/jc2-lane.vbQf8n/inputs at 04:24:42 before any body read (c70cd58f producer report 11049 B; 50d1bda0 ROOT-CARD 4485 B; c1ec52ca accepted17zr interface 13710 B; 76d25dfe its FIRST gate 13731 B; 3c4f18e6 produce.sing 3380 B; 2e0dda86 check_certificate.py 6375 B; 71907e70 CONTROLS.md 1791 B; 15c3356d PINS.md 2216 B). All eight read WHOLE via parallel reads; no clipping. The accepted17zr interface and its gate are premises, not re-reviewed. Exactly one additional primary read: the pinned Singular reference.doc (raw bytes SHA256 2a3ad025ff6c7707cf0119bce8a33efcafa6eb400cffce87ae6a5af18283f0ea, 305121 B, retained in the own box), read by named nodes only; raw-file line numbers and the exact scope are in box READ-SCOPE.md. No whole-manual or installed-runtime-version claim. ZERO mathematical subprocesses: no CAS, Python, import, AST, compile, syntax, fixture or test; hash, grep, sed, awk text extraction and hand arithmetic only. No Seal, no charge_basis.

## 0. Verdict table

| Item | Verdict | Smallest actual defect |
|---|---|---|
| A literal ring/ideal/transcription; both quartics; I1..I4 with map, guard and limits retained; no root filtering | CONFIRMED | G1 (GAP, documentary): the checker's b3..b0 are derived from the same c,d,e,f, and the literal b quartic (accepted17zi) is not a charged input, so "independent reconstruction" is arithmetic-level only |
| B Singular algorithm, static source only, API semantics from the pinned manual | CONFIRMED (static) | G2 (GAP): ASCII-links node, procedure/link-parameter syntax and the printed form of numerator/denominator (sign, no "/1") are not in reference.doc at this commit; any deviation is a checker REJECTION, never an acceptance |
| C independent checker parser/Poly induction, CLI bound, digit setting, payload status | CONFIRMED | D1 (completeness): check(raw) relies on main() for set_int_max_str_digits(0); a direct check() call on >=4300-digit tokens under CPython >=3.11 rejects a valid certificate |
| D prospective controls: F+1, H4+1, omitted I4, malformed files; static vs executed | CONFIRMED as static deductions; NONE EXECUTED | D2 (design): CONTROLS.md item 4 omits I4 but keeps z in the ring, so dim>=1 holds because z is free, and the dimension refusal does not witness the V=W=0 X-line |

Overall: the producer source and the checker source are a correct static design for an exact certificate F = sum Hi*Ii with F nonzero in Q[X]; soundness of an accepted certificate rests only on the checker's literal reconstruction and exact arithmetic, both verified here by hand. Nothing has run. No F, cofactor, GB, coefficient artifact, execution, promotion or follow-on authority.

## A. Literal ring/ideal/transcription

Producer (produce.sing lines 5-26) versus the accepted interface section 2: ring 0,(z,V,W,X),dp = Q[z,V,W,X] global dp. a3=30V-14, a2=71-270V+180V^2+120W, a1=-154+780V-900V^2+120V^3-600W+720VW, a0=120-720V+1080V^2-240V^3+720W-1440VW+360W^2: byte-for-byte. c=4-12V, d=-48+234V-240V^2-90W, e=188-1194V+2040V^2-720V^3+(870-1800V)W, f=-240+1800V-3960V^2+2280V^3+(-1800+6120V-2520V^2)W-2160W^2: byte-for-byte. g=d-c*a3, A=c^2*a2-c*e+d*g, B=c^2*a1-c*f+e*g, C=c^2*a0+f*g, I1=cAC-cB^2+dAB-eA^2, I2=dAC-cBC-fA^2, I3=AX^2+BX+C, I4=zcA-1, in that order: byte-for-byte with interface (1). CONFIRMED.

Checker (check_certificate.py lines 65-87): a3..a0 identical to the producer's. b3..b0 hand-subtracted: a3-b3=(30V-14)-(42V-18)=4-12V=c; a2-b2=(71-119)+(-270+504)V+(180-420)V^2+(120-210)W=-48+234V-240V^2-90W=d; a1-b1=(-154+342)+(780-1974)V+(-900+2940)V^2+(120-840)V^3+(-600+1470)W+(720-2520)VW=188-1194V+2040V^2-720V^3+870W-1800VW=e; a0-b0=(120-360)+(-720+2520)V+(1080-5040)V^2+(-240+2520)V^3+(720-2520)W+(-1440+7560)VW-2520V^2W+(360-2520)W^2=-240+1800V-3960V^2+2280V^3-1800W+6120VW-2520V^2W-2160W^2=f. All four coefficient matches exact. g, A, B, C and the four generators are the same expressions, with z*c*A-1 retained. Variable order z,V,W,X (index 0..3) equals the ring order, and the producer's leadexp intvec positions 1..4 are the same order. Both maps (I1,I2 numerators, I3 cover), the guard I4, and the interface's limits are retained; no root filtering is implemented or claimed; no source/JC2 result. A CONFIRMED. G1: the producer report section 4 itself says b was hand-derived from b=a-(cY^3+dY^2+eY+f), so the checker cannot catch a shared transcription error in c,d,e,f; the accepted17zi literal b is outside this lane's inputs. Documentary GAP, not a defect in the identity checked.

## B. Singular algorithm, static, source only

Pinned manual nodes (raw-file lines): liftstd 4537-4548: sm=liftstd(m,T) gives matrix(sm)=matrix(m)*T; the producer's G0=liftstd(I,T) at lines 29-30 keeps G0 and T together. lift 4493-4504: matrix(sm)*U=matrix(m)*T with U a diagonal unit matrix, and "U is always the identity if the basering is a polynomial ring"; contact is a polynomial ring, so L=lift(G0,ideal(F)) at line 52 gives matrix(ideal(F))=matrix(G0)*L and H=T*L is 4x1 with F=sum H[i,1]*I[i]. finduni 2825-2834: returns an ideal contained in the input whose i-th generator is univariate in the i-th ring variable, minimal degree, and REQUIRES a zero-dimensional reduced Groebner basis; the producer calls it only on G=std(G0) under option(redSB) (6112-6116), after reduce(1,G)==0 has excluded the unit ideal (reduce needs a standard basis, 7145-7175; G is one) and dim(G)!=0 has refused positive dimension (dim 1598-1610: Krull dimension when the argument is a standard basis; -1 only for (1), already branched away). X is the fourth ring variable, so uni[4] is the X-eliminant. option(none) 5992-5995 turns off all options; the caveat at 6191-6192 (multBound/degBound-type values are not reset) is harmless in a fresh session. subst 8367-8376: multi-pair, sequential; substituting three zeros sequentially equals the simultaneous substitution, and equality with F holds iff no term contains z,V,W, which is exactly F in Q[X]. char 565-569, quit 10177-10181 (works inside procedures and blocks), numerator 5879-5883, denominator 1466-1470, lead 4289-4298, leadcoef 4334-4340, leadexp 4368-4375 (intvec, length nvars), open 5931-5936 all match the usage. write 9549-9560: for an ASCII link all expressions are converted to strings and separated by a newline; the example at 9579-9581 states a string is written "without the quotes"; ":w" overwrites. The loop at lines 69-76 subtracts lead(value) each pass, so it terminates and emits every term once with exact numerator and denominator numbers, never machine integers or floats. Ring-variable name collisions: none (z,V,W,X versus a0..f,g,A,B,C,T,G0,G,F,L,H,I). The proc is defined at line 64 before its first call at line 89. Output reuse: the only file is contact-eliminant.cert in ":w" mode, and every PRODUCER_FAIL quits before the serializer, so an early failure can leave only a STALE file from a previous run; the producer report section 3 correctly makes fresh-directory custody a ROOT obligation. Load-bearing point: the producer recomputes residual=F-sum H[i,1]*I[i] against the ORIGINAL I at lines 56-59, so the certificate's truth does not depend on liftstd/lift semantics at all. B CONFIRMED statically. G2: reference.doc at this commit does not contain the ASCII-links node, the procedure-definition node (typed link parameter, return();) or the printed format of numerator()/denominator() (denominator sign, absence of "/1"); each is a completeness question only, since any unexpected token makes the checker reject. No engine pass, no syntax pass, no version claim.

## C. Independent checker

Poly (lines 21-62): terms is a dict exponent-4-tuple -> nonzero Fraction, zeros removed at construction; add merges by exponent; neg, sub, rsub, radd, rmul route int operands through coerce, so 30*V, 71-270*V, -c*a3 and c**2*a2 have their ordinary meanings; mul convolves exponent tuples with rational products; pow is repeated multiplication with a nonnegative int guard. By induction over these operations every value in literal_generators is the exact element of Q[z,V,W,X] denoted, with z,V,W,X the unit exponent vectors in that order (line 68). Parser (lines 90-146): ascii decode, whitespace tokens, exact header JC2_CONTACT_CERT_V1 / interface pin / gate pin / ring Q z V W X dp / generators I1..I4, exactly the five blocks F,H1,H2,H3,H4 in order, term = canonical decimal integers (regex plus str(int)==token, so "-0", "+1", "01" reject), denominator>0, numerator!=0, exponents>=0, reduced coefficient, no duplicate monomial, endpoly per block, terminal endcert, no trailing token; an extra block after H4 fails the endcert expectation; empty blocks give zero cofactors. check (lines 149-163): byte bound, F nonzero, every F exponent has z,V,W parts (0,0,0), residual F-sum Hi*Ii over the RECONSTRUCTED generators equals the empty dict; returns sha256 of the raw bytes. Nothing from the certificate is evaluated as code; the header hashes are string equality only, no GB, header flag, producer or emitted I is trusted. CLI (lines 166-181): exactly one argument, read(MAX_BYTES+1) then check refuses len>64 MiB, every Exception (including MemoryError and RecursionError) prints CHECK_FAIL and returns 2, CHECK_OK with status 0 only after the identity. fractions/hashlib/re/sys import at module load; stdlib only. Payload status confirmed: no host, path, authority or resource guard exists; static acceptance authorizes no execution anywhere. C CONFIRMED. D1: sys.set_int_max_str_digits(0) is set only in main() (lines 172-173); a direct check(raw) with numerator tokens of >=4300 digits on CPython >=3.11 raises inside int() or str() at lines 110-111 and is rejected, a false negative, never a false accept. Smallest actual checker defect; completeness only.

## D. Prospective controls

Every item in CONTROLS.md is a static deduction; NONE EXECUTED, and the file, the producer report and PINS say so consistently. Item 2 (F -> F+1): residual becomes 1, nonzero, rejected at line 162; the zero-F edge case F=-1 is rejected earlier at line 155, as item 2 anticipates. Item 3 (H4 -> H4+1): residual becomes -I4 = 1-zcA, nonzero since its value at z=0 is 1; rejected by the identity, not by a digest. Item 4 (omit I4): hand check at V=W=0: c=4, d=-48, e=188, f=-240, g=8; A=1136-752-384=0, B=-2464+960+1504=0, C=1920-1920=0, so I1,I2,I3 vanish for all X and no nonzero F(X) lies in (I1,I2,I3); dim(G)!=0 must refuse. Item 4 correctly separates this from omitting the serialized H4 block, which is a parser failure at the "poly H4" expectation. Item 5 malformed cases each map to a named rejection line (zero denominator 133, negative exponent 133, duplicate 140, truncated 97, extra block/trailing 143-145, F with V 157, changed ring/pin 102-104). Item 6 (F=1 branch, zero cofactors) is deferred to dummy controls and never claimed. D CONFIRMED as static deductions. D2: in the ring Q[z,V,W,X] with I4 omitted, z appears in no generator, so the ideal has dimension at least 1 for that reason alone, and a dimension refusal would fire whether or not the V=W=0 X-line exists; the interface section 7 notes "arbitrary z if z was retained" but CONTROLS.md item 4 does not say to drop z. The control as written is vacuous as a test of the saturation mechanism. Design defect in the control, not in the certificate.

## E. Smallest actual defects

D2 (design, D) is the smallest actual defect: the omitted-I4 dimension control must be stated in Q[V,W,X] (or with z eliminated) to witness the spurious X-line. D1 (completeness, C): check() is not self-contained on the decimal digit limit. G1 (documentary GAP, A): b3..b0 derive from the same c,d,e,f; the literal b source is uncharged. G2 (GAP, B): three manual items are outside reference.doc at this commit. No soundness defect was found: an accepted certificate proves F in I with F nonzero in Q[X], by the checker alone.

## OPENS RAISED

- NONE new. The assigned GAP stays as the producer recorded it, no new canonical ID: QUANTITY the number of executed producer/checker runs, syntax passes, fixtures or certificates is = 0; D1 and D2 are recorded for ROOT, no repair authored here; runtime/cap/host/dummy dispatch registration remains ROOT-owned.

## COLLISIONS

status: EMPTY

- NONE — own-only check: xmodel/f10-contact-eliminant-code-gate-fable5-20260910.md and box/f10-contact-eliminant-code-gate-fable5-20260910/ were ABSENT at 04:24:42 UTC before the skeleton; no corpus, ledger, provenance, Git, live-file, AWS, SSH, process or other-lane scan performed.

Own WHOLE reread and own-only raised-OPEN/collision extraction done before the 04:36:42 publication reserve; the box hash rows in READ-SCOPE.md are generated from sha256sum output and cross-checked against the lane brief pins. Written: this report, box READ-SCOPE.md and the retained reference.doc snapshot, documentary writes only. No mathematical subprocess of any size, code execution, fixture, coefficient artifact, agent, Git/shared/protected work or other-lane read occurred. ROOT retains custody and any further review; this gate gives no promotion, execution or follow-on authority.

<!-- BODY-END -->
