# Blind ideation — Grok 4.6 — round `20260903T1200Z`

Lane: Grok 4.6 (xAI), equal-standing independent researcher. Blind against
the sealed packet and banked repository reports only. No other
`ideation-20260903T1200Z-*` submission was opened. No in-progress report
of a running lane was opened (`.run.v2` checked for `final_status=DONE`
before any `.md`: `descent-radii-grok46`, `a2six-ray-moment-sol56`,
`whole-tree-review-grok46` DONE; `sibling-coefficients-sol56`,
`screened-census-grok46`, `whole-tree-review-opus5`,
`appendix2-compiler-grok46` had no `final_status` and were not opened).
No canonical ledger edited. `jc2-lean` not inspected.

## 0. Custody

Packet hash verified, byte-exact against the frozen copy:

```text
c90fa6876b069478f658845be6efa2bd056b2e340af7284104af4d042e6c4f95  /tmp/jc2-lane.4XZQpm/inputs/ideation-20260903T1200Z-packet.md
```

Packet frozen 2026-09-03T12:00:32Z on basis
`4250a6e47f514c6c76a9bc708fbaa533db657eb8`. Lane `.run.v2` records basis
`0a38ba79d9306b6463de041b9d38ef3cebfa6f19`. Binding live state:
`notes.md` 11:44Z / 11:58Z / 12:00Z EVENT blocks. Freshest banked
mathematics: AUDIT integration #17 deltas (p)–(t);
`xmodel/whole-tree-review-grok46-20260903.md`;
`xmodel/moh-program-review-sol56-20260903.md`;
`xmodel/m2-descent-opus5-20260903.md`;
`xmodel/descent-radii-grok46-20260903.md`;
`xmodel/ps-growth-opus5-20260903.md`;
`xmodel/ideation-20260903T1015Z-synthesis.md`.

Firewall: flag ≠ place ≠ cover series; `REPRESENTATIVE` is not
`FULL_ACTUAL_EXIT`; a floor is not attainment. Integers kept apart:
campaign geometric degree `N`; Moh `n = deg_y g = D`; `m = deg_y f`;
`K = gcd(m,n)`; Moh gcd-chain `d_i`; `V_i` of one major tower. No new
exit-price ⇒ no `charge_basis`. Desk CAS: Python 3 stdlib on
`box/moh_skeleton_full.py`, `box/mohprog-drivers-20260903/full_tree_partition.py`,
`box/mohsieve/descent.py` only; one core; < 3 s; ≪ 4 GB. No AWS.
`FALLACY-v2` in force. `descend_once` `WINDOW-FAIL` is **not** consumed
as a kill (it is the new-top window of `(6)`, which delta 17(q) says
does not descend).

Read: the packet; APPROACHES.md 46-row table plus the 10:45Z overlay;
AUDIT.md #17 (p)–(t); PROGRESS.md 2026-09-03; notes.md 10:40Z–12:00Z;
the banked reports named above; COORDINATION.md contract; `FALLACY-v2.md`.
No other `20260903T1200Z` submission.

---

## 1. Direct answer: the coordinator's three-layer sentence is right as
a stack and wrong as a program

The packet's binding frame is: the boundary computes `N`; the tree
computes the admissible multiplicity structure; the coefficients are
the remaining freedom. Q1–Q4 ask whether the screened census is a
proof, what the coefficient problem is, how to attack theorem `(T)`,
and whether anything sits outside those three layers.

Three corrections, then the replacement.

### 1.1 Where the reading is right

The boundary computes `N` exactly and the value is `O(1)` (PIN-NOT-CEILING,
promoted). The whole-major-tree obligation is PROVED-IN-SOURCE (delta
17(r)): Prop 5.3 is a universal conditional and p.200 Theorem (4) holds
for every subdisc. `C_FULL_TREE` is a derived, fail-closed, uniform-in-`D`
filter that keeps Moh's six, reproduces the exact `(75,50)` residue
`{M_2=55, V_2∈{2,3}}`, and empties `D=105` and `D=117` by a forced
major-zero sibling at `j=2` with the Lemma 5.3 danger flag still set.
Both named fixed-`N` rays die by that mechanism. After the tree there is
no further sourced *scalar* sieve (delta 17(m) stands). PS-GROWTH is
true and is not a ceiling (delta 17(s)). Dessin-tower expected dimension
is `max(k-2,0)≥0` everywhere. Raw moment-parity is MDS. Descent radii
are closed: `δ'=(k+1)·Def 5.1(3)` on the descended data (delta 17(t)).
Theorem `(T)` is the statement that would finish Moh's line for `u_s=1`.
All of that stands. I am not re-litigating a promoted identity.

### 1.2 Where the reading is wrong

**Wrong 1 — "the tree computes the multiplicity structure" conflates
PATH-ARITH with attainment.** `C_FULL_TREE` is a necessary arithmetic
screen on integer skeletons. A pair that exists would have such a tree;
the converse is OPEN[SIBLING-COEFFICIENTS] and is exactly Moh p.143.
Calling the screen "the structure of the pair" is a floor read as
attainment. Type: DERIVED necessary filter, not a theorem that a
Jacobian pair exists or does not.

**Wrong 2 — "coefficients are the remaining freedom" treats the
leftover as bookkeeping.** After the tree the leftover is not free
coefficients of a Keller pair. For `u_s=1` it is emptiness of a
monomial-Jacobian pair of bounded tower height with a 4-tuple of
inherited data — theorem `(T)`, APPROACHES row 1's world, Moh's
Appendix II uniformised. For `u_s>1` it is OPEN[MINOR-DICHOTOMY]. That
is JC2 restated in a better coordinate, not residual freedom.

**Wrong 3 — Q1's emptiness-horn is the wrong bet, and the uniform
`j=2` kill is not an all-degree statement.** The mechanism
"`b_min = P mod A_2 > h` at `j=2` ⇒ forced major zero ⇒ Prop 5.6"
kills every *s=3* skeleton whose least nonnegative residue at the
fixed root is still major while the top-chart danger flag is set. It
killed both named rays because those families have `b_min≥3` and
`h<1`. It does **not** constrain skeletons with `b_min=0`, i.e.
`A_2 | P`, and it does not constrain `s≥4` rows whose danger flag
clears at a nonzero break above `j=2`. The screened census at
`D≤200` has 1,516 groups (tree) / 1,261 (passport) and grows. This
session exhibits an explicit cofinal `C_FULL_TREE` family containing
Moh's printed `(64,48)` as the `k=1` member (Card I). The screen does
not thin to nothing. A proof that consumes only the original tree
cannot finish.

**Wrong 4 — Q4's invitation to name a fourth invariant is the
instinct that already failed three times this morning.** Trace/moments,
dessin dimension, and raw parity were each priced and each returned a
shape constraint, a nonnegative dimension, or an MDS matrix. The
honest remainder is coefficient computation in the descended
coordinate. Asking for a mechanism "outside boundary / tree /
coefficients" is the right *question*; the answer is that descent is
not a fourth invariant of the original pair — it is a finite change of
coordinates after which the tree is four integers and the coefficients
are Appendix II.

### 1.3 What the all-degree program should be now

Moh's line, assembled, with the tree in its actual place:

1. The boundary pins `N = O(1)`. Done.
2. `C_FULL_TREE` is a necessary finite filter on PATH-ARITH skeletons.
   Done, promoted. It empties two rays and two degrees; it does not
   empty the census.
3. The screened space is cofinally nonempty. Exhibit: the
   `(64,48)`-ray of Card I (closed form + the algebra). That list is
   the counterexample-side target. The proof must leave the skeleton.
4. When `u_s = d_s − V_s = 1`, Prop 6.3/6.4 is a **finite reduction**
   of depth `≤ s−1 ≤ 4` to a monomial-Jacobian pair
   `[P,Q] = cγ^{V_s-2}` of π-degrees `(n/d_s, m/d_s)`, not an induction
   and not Jung–van der Kulk (the descended pair is not Keller).
   Radii are now Φ. Conditions (8)–(13) are unblocked on the descended
   data.
5. Theorem `(T)`: no such pair exists, for every 4-tuple
   `(n', m', M_2', V_2')` arising from a screened `u_s=1` skeleton.
   `(T)` + descent + OPEN[MINOR-DICHOTOMY] = JC2 along Moh's line.
6. Uniform form of the coefficient problem: Lemma 2.1 support
   (`g_j ∈ k` for `j < m'−1`, `deg_x g_{m'−1} = k+1`) plus Φ-radii plus
   the approximate-root truncation Moh ran by hand in Appendix II.
   That is OPEN[SIBLING-COEFFICIENTS] *after* descent, not a Gröbner
   basis on the original `(p_j, q_j)` at every node of a `D=108` row.

The census is a target-list generator, not a proof program. The
all-degree theorem is `(T)` on a 1-parameter family that already sits
inside the screened space, then `(T)` in general, then the minor
branch.

---

## 2. Q1 — the screened census is a target list, not a proof

### 2.1 The uniform `j=2` kill, scoped

At `s=3` the only recursive node of `C_FULL_TREE` is `j=2`. An actual
factorisation is `P = b + A_2 Σ u_ℓ` with `b ≡ P (mod A_2)`. Prop 5.6
kills a still-centred major zero leaf. Hence:

- if `b_min = P mod A_2 > h = d_2/(n−M_2)`, every packing carries a
  major zero and the row dies (danger still set from the top chart);
- if `b_min ≤ h`, a packing with no major zero may exist; `b_min=0`
  (i.e. `A_2 | P`) is the clean case.

Banked controls: Moh `(75,50)` and `(64,48)` have `b=0` and survive;
Sol's `L=8a+5` and the geometric `A_2=6` ray die uniformly
(`b_min>h`). Diophantine split on `(P,A_2,h)`, not a degree bound.
`D=108` and §2.2 refute any all-degree reading.

At `s≥4` the danger flag can clear at a nonzero break above `j=2`.
This session: of 60 `C_FULL_TREE` rows at `n≤100` (`K_min=2`), only
6 are `s=3`; 39 are `s=4` and 15 are `s=5`; only 33/60 have `b≤h`.
The other 27 survive by a cleared danger flag. The packet's "uniform
mechanism" is the `s=3` ray-killer, not the screen.

### 2.2 Exhibit: the `(64,48)`-ray (OPEN[K16-RAY], PROVED-HERE)

For every integer `k ≥ 1` put

```text
n = 16(3k+1),    m = 16(2k+1),    M = (n-12, n-2),    V = (V_2, V_3) = (3, 3).
```

Then `gcd(2k+1, 3k+1) = 1` identically, `K=16`, `e=3k+1`, `dd=2k+1`,
`s=3`, `d = (n, 16, 4, 2)`, `u_s = d_3 − V_3 = 1`, and the Def 5.1
windows are *k-independent*:

```text
4/3 < V_2=3 ≤ 12,     2 < V_3=3 ≤ 4.
```

Logarithmic radii: `δ_3 = −1`, `δ_2 = 1/4` (both constant);
`δ_1 = 1 − (3k)/(8k)` wait, measured: `δ_1 ∈ {9/16, 4/7, 23/40, …}`
with `A_1 = 3k+1 = n^*`. Node data at `j=2` are constant:

```text
A_2 = 4,  P = 12,  Q = 9,  h = 4/3,  b_min = 0.
```

Condition (12) is an identity: `A_1 = n^* = 3k+1`, `m^* = 2k+1`,
`V_2=3`, so `A_1 | n^* V_2` and `A_1 | m^* V_2 − 1 = 2(3k+1)`.
The `s=3` tree DP is exactly the `j=2` packing of these constants
plus (12). Moh's printed `(64,48)` is the `k=1` member (`b=0`, one
nonzero orbit `u=3`). ODE (3.7): `P − Q u = 12 − 27 ≠ 0` at the
unique orbit, uniformly.

Machine: PATH-ARITH(1–13) + `C_FULL_TREE` + ODE + passport, all
True, for `k=1..6` (degrees 64, 112, 160, 208, 256, 304), each
`TERMINAL` under Prop 6.3:

```text
(n', m', M_2', V_2', k_jac) = (12k+4, 8k+4, 12k+1, 3, 1).
```

Because the only recursive tree-node is k-independent and (12) holds
for all `k`, `C_FULL_TREE` on this ray is a theorem for every
`k≥1`, not six coincidences. Type: PATH-ARITH + DERIVED tree, **not**
existence of a Jacobian pair. `REPRESENTATIVE` of a skeleton family,
never `FULL_ACTUAL`.

Passport is EXTERNAL (delta 17(r)); it passed for `k=1..6` and is
not claimed as a source theorem.

### 2.3 What Q1 asked, answered

- **Is the screened space cofinally nonempty?** Yes. Card I is a
  closed form; the algebra is the constant `(P,A,Q,h,b)` knapsack
  plus the identity (12). Nested-pack orbit knapsack is not needed
  for this ray: `s=3` packing is exact (delta 17(j)) and is the
  single orbit of size `A_2=4` with multiplicity 3.
- **If a screened family survives for all `D`, exhibit it.** Above.
  Degrees `64, 112, 160, 208, …` — not `D=108`, which is a different
  residue (mostly `s=4`). The first member is Moh's own first printed
  row; Appendix II already kills `k=1` after descent. The live CE
  target is `k≥2`.
- **If the screen thins to nothing, what is the theorem?** It does
  not. The `j=2` mechanism cannot be turned into an all-degree
  statement on (1)–(13) plus the tree: it needs `b_min > h` at a
  still-dangerous `j=2`, and `A_2 | P` is attainable cofinally.
  Beyond (1)–(13) it needs the Galois packing and Prop 5.6, which
  the screen already uses; there is no further arithmetic to extract
  from that implication.

Cheapest test of OPEN[K16-RAY] for a hostile reader: replay
`full_tree_ok` on `k=1` (must match Moh `(64,48)`) and `k=2`
`(112,80; M=(100,110), V=(3,3))`; check the four identities
`δ_2=1/4`, `A_2=4`, `P=12`, `(12)` as polynomials in `k`. Desk,
< 1 s, already done this session.

---

## 3. Q2 — the coefficient level is `(T)`, and the cheapest D=108
discriminator is `descend_once`

### 3.1 Uniform form of OPEN[SIBLING-COEFFICIENTS]

Moh p.143 names the simultaneous existence of the sibling polynomials
`(p_j, q_j)` at every node, over one coefficient field, with matching
leading terms, the ODE at every node, and the Galois action. After
`C_FULL_TREE` the *multiplicities* of those siblings are pinned
(orbit knapsack). The residual is their coefficients.

That residual has a uniform form, and it is not a new Gröbner engine
on the original pair:

- If `u_s=1`, Prop 6.3 *is* the sibling system at the top disc,
  rewritten as a monomial-Jacobian pair in `k[γ,π]`. Lemma 2.1
  (source-read, m2-descent §4.3; Moh p.208–210) forces `g_j ∈ k`
  for `j < m'−1` and `deg_x g_{m'−1} = k+1`. Φ supplies `δ'`.
  Approximate-root truncation plus Tschirnhausen is Appendix II.
  **This is theorem `(T)`.** The dictionary node ↔ datum: the unique
  remaining major disc of the descended `s'=2` tower is Appendix II's
  `(h, β)`; the original `D_j` for `j≥3` have been divided out.
- If `u_s>1`, OPEN[MINOR-DICHOTOMY] (p.209 alternatives). First
  screened client above 100: the `D=108` row
  `(n,m,M,V,d)=(108,72,(81,106),(7,7),(108,36,9,1))`, `u_s=2`,
  passport-alive. Shape-cousin of Moh's `(99,66)`.
- The tree-decorated moment engine is a *rewrite* of the same
  interpolant in the original coordinates. After this morning it is
  the more expensive rewrite: three sealed lanes (d105-rank-gate,
  fixed-n6, a2six) each refused because the first page is not
  determined by the skeleton, and the driver's orbit-custody claim
  is repair debt. Do not build it until `(T)` on the small descended
  list is priced.

### 3.2 Cheapest exact discriminator on the first D=108 survivor

Campaign `K_min=16`, this session (0.24 s):

```text
D=108 PATH-ARITH 206 / TREE 21 / ODE 20 / passport 18
```

Matches the packet (21/13/8 UNI-alive under bare tree; 18/11/8
passport). Descent diagnosis, with the engine's `WINDOW-FAIL`
**split** into new-top vs intermediate:

```text
us>1:              2
s'=2 window-OK:    0
s'=3 intermediate window fail:  0   ← none; not a kill
s'=3 new-top-only fail:        17   ← (6) does not descend; NOT a kill
s'=3 window-OK:                2   ← both M2-LE-M, (18,12; γ^3)
```

The two genuine post-descent `s'=3` window-OK rows, both `M2-LE-M`
(not a kill: `M_2>m` is not a theorem):

```text
(108,72; M=(24,78,106), V=(1,1,5)) → (18,12; M_2'=4, V_2'=1, k_jac=3)
(108,72; M=(48,90,106), V=(1,1,5)) → (18,12; M_2'=8, V_2'=1, k_jac=3)
```

The 17 new-top-only rows remain `s'=3` `(T)` instances of degrees
`(27,18)`, `(18,12)`, `(12,8)` — COUNTING-BOUND as a bloc, not the
first experiment.

**Cheapest discriminator:** `descend_once` (done); Appendix-II on
the two `(18,12; γ^3)` systems; MINOR-DICHOTOMY on
`(108,72, M=(81,106), V=(7,7))`. Not a sibling Gröbner on un-descended
`(90,60)`. For the 19 `u_s=1` rows this *is* `(T)`. The moment engine
is the more expensive rewrite of the same dictionary; after Prop 6.3
the dictionary is four integers.

---

## 4. Q3 — attack `(T)` on a 1-parameter family, not on G2/G3

G2 `(15,10; x^4)` and G3 `(21,14; x^2)` are calibration objects.
`D=105` is empty under the tree; a SATURATED-EMPTY on G2 would prove
the compiler, not a frontier theorem. The live `(T)` list starts at
Moh's printed descents (already killed, with ERRATUM[APPII-GAMMA-B]
and a corrected Case 2) and continues into Card I.

### 4.1 What is known

Monomial-Jacobian pairs *exist* (`(y, x^{k+1}/(k+1))`,
`(y^2+x^{k+1}, y)`); existence without the 4-tuple is not `(T)`.
Lemma 2.1 is a support theorem: `g_j ∈ k` for `j<m'−1` and
`deg_x g_{m'−1}=k+1`, so the Newton polygon has one or two faces,
compatible in shape with `s'=2` but not automatically with the
inherited `M_2'`. Row-1 certificates become kills once Φ cuts to
Appendix-II size; Moh `(16,12; X)` and `(15,10; X^2)` are already
SATURATED-EMPTY (the latter with ERRATUM[APPII-GAMMA-B]). Approximate
roots are the truncation; Φ automates p.210's `δ_1`-shape. Radii and
(8)–(13) are unblocked. The remaining gate is polynomiality of the
truncated expansion.

### 4.2 Uniform obstruction in `(n', m', M_2', V_2')`

On Card I the 4-tuple is rigid:

```text
n' = 12k+4,  m' = 8k+4,  M_2' = 12k+1 = n'−3,  V_2' = 3,  J = c γ.
```

So `k_jac=1`, `V_2=3`, `M_2 = n'−3` for every member. Lemma 2.1 with
`k_jac=1` says the unique x-dependent coefficient of `g` has
`deg_x = 2`. The characteristic datum `M_2 = n'−3` is a near-terminal
slope (Moh's own `(4)` is `M_s = n−2`; here the descended `M_2` sits
one off the end). A Newton-polygon computation that this slope is
incompatible with a single quadratic-in-x coefficient, uniformly in
`k`, would be `(T)` for the ray and would not grow with `D`.

I did not, in desk time, finish that polygon. It is Card III. The
base `k=1` is already empty (Moh + mechanical SATURATED-EMPTY). The
cheapest new instance is `k=2`: `(28,20; X, M_2=25, V_2=3)`. Φ applies;
order conditions will cut below the 172-unknown shape-free count.
This is larger than Moh's 22 but in the same family as `(16,12)`.

### 4.3 What Appendix II generalises to

A compiler: input a screened `u_s=1` row; output the Lemma 2.1 shape
in `(γ,π)` with Φ-radii and the saturation ideal of the truncated
approximate-root expansion. Positive control: Moh `(15,10)` and
`(16,12)` SATURATED-EMPTY. Negative control: an automorphism put in
Moh's gauge must not saturate (the 12 witnesses fail (4)/(6) and are
not in the screened space). The compiler is the uniform form; it is
already launched as `appendix2-compiler-grok46` (in-progress, not
read). Point it at `(28,20)` and at the two `(18,12; γ^3)` from
`D=108`, not at G2/G3.

---

## 5. Q4 — there is no fourth invariant; the proof is the coefficient
computation, and its uniform form is `(T)`

Fresh eyes, as asked. The three layers are three presentations of one
object (a pair). The boundary is the fibre at infinity; the tree is
the Galois packing of that fibre; the coefficients are the pair.
A mechanism "outside all three" would have to see the pair without
seeing `N`, without seeing multiplicities, and without seeing
coefficients. PS-GROWTH saw `(m,n,q_max,e)` and was a shape
constraint. Dessin-tower saw the packing and had nonnegative
dimension. Raw moments saw an MDS matrix. Each was a rewriting of
one of the three layers, priced this morning, negative as a ceiling.

Descent looks like a fourth object and is not. It is a finite
coordinate change that *collapses* the tree to four integers and
*forces* the Jacobian to a monomial, after which Lemma 2.1 collapses
the coefficients to a 1-parameter slice of `g`. That is why Moh
finished by hand. The all-degree form of that hand computation is
`(T)`.

If a seat is spent hunting a new invariant of the original pair, it
will rediscover a shape constraint. Spend it on `(T)` for Card I.

**Single first lane.** `k16-ray-T`: (a) hostile check that the
`(64,48)`-ray is `C_FULL_TREE` for all `k` (the identities of §2.2;
desk); (b) Appendix-II compiler on the `k=2` descended pair
`(28,20; X, M_2=25, V_2=3)` with Φ. This is simultaneously the Q1
exhibit, the Q3 attack, and the first honest CE target above Moh's
table. Not a sibling Gröbner on `(90,60)`. Not a D≤400 emptiness
search (the space does not empty). Not G2/G3.

---

## 6. Disposition vectors

### 6.1 APPROACHES.md 46-row table — changes only

The 10:45Z overlay's "census never empties" was PATH-ARITH+(1)–(13);
after the tree both named rays are dead, but a different family lives.
Not re-scoring the 08-31 rank-four overlays.

| # | change | reason |
|--:|---|---|
| 1 | **raise** | Not a certificate store and not a next-pair farm. Theorem `(T)` *is* row 1: monomial-Jacobian pairs of unbounded degree, bounded height `s'=2`. Card I lands in `[P,Q]=γ` (`k_jac=1`) with 4-tuple linear in `k`. Row-1 certificates are now kill conditions for a cofinal screened family, not for G2/G3. |
| 5 | retype, do not raise as JC2-descent | Prop 6.3 drops degree by `d_s≥4` but the descended pair is **not** Keller and the tower does not regenerate. Not Jung–van der Kulk amalgam. Keep the distinction or the next lane will try shears on a monomial Jacobian. |
| 6 | **raise** | Appendix II *is* approximate roots on the descended one-place curve. Card I is an unbounded family of such curves with `M_2'=n'−3`, `V_2'=3`. AM-on-fibres stays wrong; AM-on-the-descended-pair is the engine of `(T)`. |
| 16 / 45 | **lower as ceiling**; keep as remainder arithmetic | PS-GROWTH / NEXT-COEFF: identities true, not a `D`-bound, coefficients do not stratify by level (delta 17(s)). The `n−m−1` remainder form is licensed for a later tree-decorated engine and is not this round's seat. |
| 20 | unchanged lower | Pencil p-curvature is (a) in char p; inverse-Jacobian p-curvature is already 0. Diagnostic only. |
| 25 | **lower as a finish** | Dessin-tower `expdim = max(k−2,0) ≥ 0` everywhere; 19 rigid candidates in `D≤120` sit at large `V_2`, small `k`, not on Card I (`V_2=3`, `A_1=3k+1` grows). Hurwitz combinatorics cannot finish. Keep as a leaf-type check on `(T)` instances. |
| 29 | keep raise, not first | `D_g f = 1` with Moh's filtration is the ODE at nodes; Lemma 2.1 is the monomial-Jacobian specialisation. Downstream of `(T)`. |
| 36 | **raise, retarget** | CE search: Card I `k=2` pair `(28,20; X)` and the two `D=108` `(18,12; γ^3)` systems. Not the dead trio, not a numerical lift of a PATH-ARITH ray, not box01 clusters. A SURVIVES on `(28,20)` is the first honest positive signal above Moh's table. Typed `REPRESENTATIVE` until algebraized. |
| 43 | mild raise | Ritt / composite structure of a Lemma 2.1 polynomial (`g` almost a polynomial in `y` plus one x-dependent coefficient) is the shape `(T)` attacks. Not a standalone route. |

Unchanged: 2 (the Moh tree *is* `C_FULL_TREE`, now promoted; the leftover is not "exclude td=6,7,8"); 3 (BOTTOM-ODE is (3.7), consumed); 4 (D-series is not the program); 7–15, 17–19, 21–24, 26–28, 30–35, 37–42, 44, 46. Row 33 stays COSTUME. Row 2 is not raised further: the tree is done as a sieve.

### 6.2 Q1–Q4 candidates and queued fronts

| item | change | reason |
|---|---|---|
| Q1 screened-census-as-proof (emptiness horn) | **stop as a proof program** | Card I is cofinal in the screen. Emptiness of `D=105,117` is a ray-kill, not a degree-kill. |
| Q1 `j=2` `b_min>h` as all-degree theorem | **stop** | Needs `s=3` and `b_min>h`; `A_2\|P` is attainable cofinally. Keep as the typed kill of the two named rays. |
| Q1 screened-census as CE-target generator | **raise** | That is its job. Search for other `A_2\|P` families; do not enumerate to 400 hoping for empty. |
| Q2 sibling-coefficients on the original pair | **redesign** | Run on descended `(18,12; γ^3)` and `(28,20; X)`, not on `(90,60)` and an un-descended `D=108` row. |
| Q2 theorem `(T)` | **raise; this is the program** | §1.3, §4. |
| Q2 tree-decorated moment engine | **defer** | Three lanes hit the decoration wall; descent supplies the decoration for `u_s=1`. Repair the custody bug non-blocking. |
| Q3 attack `(T)` | **raise first** | Card I + Lemma 2.1 + Φ. G2/G3 are controls. |
| Q3 literature / Abhyankar | consume, do not reseat | Approximate roots *are* Appendix II; no new paper hunt. |
| Q4 fourth invariant | **answered negative** | §5. |
| queued P202 residue through sibling system | **redesign** | `n≤100` TREE excess is 54 rows of OPEN[MOH-PROGRAM-ARTIFACT]; pass through `descend_once` first. Most `s≥4` rows become `s'=3` `(T)` instances, not original-pair Gröbner. |
| queued box01 restart | **hold** | Needed only if `(28,20)` exceeds desk after Φ. Fleet key is a human gate, not a math reason to wait. |

---

## 7. Reranked bottlenecks

**Proof, in order.**

1. Theorem `(T)` for the Card I family: `J=cγ`, `V_2=3`, `M_2=n'−3`,
   degrees `(12k+4, 8k+4)`. Base `k=1` is Moh `(16,12)`, already empty.
   A k-independent Newton/Lemma-2.1 obstruction is a proof of JC2 along
   this ray; a SATURATED-EMPTY at `k=2` is a method-client that may
   generalise. Bounded: one 4-tuple of linear forms in `k`.
2. `(T)` for general screened `u_s=1` 4-tuples (including the `D=108`
   `(18,12; γ^3)` pair and the `s'=3` leftovers). The compiler is the
   instrument. Quantity: 19 `u_s=1` TREE rows at `D=108`; cofinally,
   every Card I member plus every other `A_2\|P` family the census
   still produces.
3. OPEN[MINOR-DICHOTOMY] for `u_s>1`. First screened client:
   `(108,72, M=(81,106), V=(7,7), u_s=2)`. Moh's `(99,66)` is the
   control. Quantity: 2 rows at `D=108`; 1 at `n=99`.
4. OPEN[MOH-PROGRAM-ARTIFACT] (49 excess rows after passport at
   `n≤100`). Secondary: the CDC listing, or the unproved edgewise
   recentre. Not a frontier seat. The `(75,50, M_2=40, V_2=1)`
   falsifier of scalar gates is already killed by the tree (forced
   zero `b=2`).

**Disproof, in order.**

1. An explicit polynomial pair in the Card I `k=2` shape
   `(28,20; J=cγ, M_2=25, V_2=3)`, or a SURVIVES from the Appendix-II
   compiler on that system. That is the first honest positive above
   Moh's table. Typed `REPRESENTATIVE` until it is a map in `k[x,y]`.
2. The same for either `D=108` `(18,12; γ^3)` pair (`M_2'∈{4,8}`,
   `V_2'=1`). Smaller degrees, `k_jac=3`, `M_2'≤m'` (allowed).
3. Do not start from G2/G3, the dead rays, box01 clusters, a numerical
   interpolant of an undecorated skeleton, or Moh's six (controls).

PIN-NOT-CEILING and `C_FULL_TREE` demote every "bound `D` from the
boundary or from the original tree" bottleneck. The 10:45Z "three live
all-degree mechanisms" are each priced: (A) shape only, (C) dimension
nonnegative, (B) is the one that remains, and it is `(T)`.

---

## 8. New avenue and new cross-connection

**New avenue: the `(64,48)`-ray / OPEN[K16-RAY].** An explicit
unbounded family of `C_FULL_TREE`+ODE skeletons, closed form in one
integer `k≥1`, containing Moh's first printed row, with k-independent
`j=2` knapsack `(P,A,Q,h,b)=(12,4,9,4/3,0)` and identity (12),
descending to the 1-parameter `(T)` family
`(12k+4, 8k+4, 12k+1, 3; J=cγ)`. Not Sol's `L=8a+5` (dead by the
tree). Not the `A_2=6` ray (dead by the tree). Not a UNI `N=6`
laboratory (`A_1=3k+1` grows). The all-degree form is `(T)` on this
4-tuple. Negative: a proof. Positive (a pair at some `k≥2`): a
counterexample. Desk-verified `k=1..6`; algebra for all `k` in §2.2.

**New cross-connection.** `C_FULL_TREE` at `s=3` is a Diophantine
predicate on `(P, A_2, h)`: `P mod A_2 ≤ h` is necessary for
survival, and `A_2 | P` is sufficient for the zero-sibling to be
absent. Descent then sends every `u_s=1` survivor to row 1. The
proof/CE split is the same arithmetic: families with `b_min > h`
die at the tree (the two named rays, `D=105`, `D=117`); families
with `b=0` and the `(64,48)` node-data live and become `(T)`.
STAR-ABC ⊗ Moh tree ⊗ approximate roots ⊗ `[P,Q]=x^k` is no longer
a slogan: on Card I it is a 4-tuple. Not claimed as a theorem that
no other `A_2 | P` family exists; it is the interface the next
census lane should search, not "growth vs `D`".

A near-miss, recorded rather than promoted: `descend_once`
`WINDOW-FAIL` kills 17/19 of the `D=108` `u_s=1` rows *if* one
illegitimately applies the new-top window of condition (6). Delta
17(q) says the `s≥3` half of (6) does not descend. Intermediate
windows failed on **0** TREE rows in `{64,75,84,96,108,112,120,132}`.
OPEN[DESCENT-WINDOW-TOP]: the engine flag is not a kill. Bounded:
those 17 rows. Cheapest test: the top-vs-intermediate split already
run (`s3_inter_fail=0`).

---

## 9. Strongest proof attack, strongest counterexample attack

**Proof.** Prove `(T)` for `J=cγ`, `V_2=3`, `M_2=n'−3`. Mechanism:
Lemma 2.1 forces a unique x-dependent coefficient of degree 2; the
Newton polygon of such a `g` cannot realise the major-disc datum
`M_2=n'−3` with `V_2=3` except possibly at small `n'` (the `k=1`
case, already empty). If the polygon obstruction is k-independent,
Card I dies for all `k` and JC2 holds along this ray; the general
`u_s=1` screen is the same argument with a 4-tuple in place of the
linear forms. Fail-closed gates: Moh `(16,12)` must come out empty
(positive control already in hand); an automorphism witness of
delta 17(q) must not be in the domain (it fails (4)/(6)). Do not
prove `(T)` by enumerating `D`.

**Counterexample.** Construct, or SATURATED-NONEMPTY, a pair of
total/π-degrees `(28,20)` with `J=cγ`, `M_2=25`, `V_2=3`, Φ-radii
the evaluation of Def 5.1(3) on that 4-tuple times `k_jac+1=2`.
If the truncated Appendix-II system is nonempty over `Q`, that is
the first positive above Moh's table. Lift is `REPRESENTATIVE`
until the pair is written in `k[x,y]`. Second target: either
`(18,12; γ^3)` from `D=108`. Do not advertise a numerical
interpolant of an undecorated `D=108` row as this signal.

---

## 10. Software acceleration / decisive experiment

**Experiment.** Run the Appendix-II compiler (Lemma 2.1 shape + Φ
order conditions + saturation of the truncated approximate-root
ideal) on Card I `k=2`: `(28, 20; X, M_2=25, V_2=3)`.

- Instrument: `box/mohsieve/descent.py` (already emits the 4-tuple),
  `box/descentradii-drivers-20260903/phi_delta.py` (Φ), the launched
  appendix2-compiler (do not open its in-progress `.md`). Desk first
  (`< 10 min`, `< 4 GB`); box01 only if Φ does not cut below ~30
  unknowns.
- Controls: Moh `(16,12)` and `(15,10)` SATURATED-EMPTY (already);
  a random Lemma-2.1 pair *without* the 4-tuple must not be fed to
  the same ideal (vacuity).
- Outcomes: SATURATED-EMPTY → method-client for Card III's uniform
  polygon, and a second killed member of the ray. NONEMPTY /
  SURVIVES → CE-side, typed REPRESENTATIVE. COUNTING-BOUND after Φ
  → emit the system and stop, as m2-descent did at 172 unknowns;
  then and only then box01.
- Wall-clock: the `(15,10)` corrected Case 2 closed in seconds
  after saturation. `(28,20)` is the step-up.

Already-run: the K16-ray identities (§2.2) and the WINDOW-FAIL split
(§8). Do not ship `descend_once` `WINDOW-FAIL` as a kill.

---

## 11. Campaign-systems check

**`UPGRADE` — split `descend_once` `WINDOW-FAIL` into `TOP-WINDOW`
vs `INTERMEDIATE-WINDOW`; do not treat the former as a kill.**

Rotate: claim/review propagation and instrument fidelity. Independent
of the Appendix-II compiler.

Delta 17(q) (unreviewed then, promoted in 17(t) on other claims)
already says condition (6)'s `s≥3` half does not descend. The engine
in `box/mohsieve/descent.py` `_windows_ok` nevertheless checks the
new top (`i=s'`) against `V_{s'+1}=d_{s'+1}`. On every TREE row
scanned this session, every `WINDOW-FAIL` was exactly that new top;
intermediate failures were zero. A lane that consumes `WINDOW-FAIL`
as emptiness of `(T)` would fake a proof.

Smallest test (PASS this session, 1.6 s): the top-vs-intermediate
diagnosis on `D∈{64,75,84,96,108,112,120,132}` printed
`s3_inter_fail=0` and `s3_top_only` equal to the engine's
`WINDOW-FAIL` count. Implementation: two flags, no change to
`TERMINAL` / `M2-LE-M` / `US-GT-1`. Regression: Moh's five `u_s=1`
p.207 rows stay `TERMINAL`.

Secondary, non-blocking: the globalinterp.py orbit-custody bug
(fixed-n6, a2six) is repair debt on a deferred engine; patch before
any moment lane is relaunched, not before `(T)`.

---

## 12. Idea cards (three)

### Card I — the `(64,48)`-ray, and `(T)` at `k=2`

- **Dependencies.** Promoted `C_FULL_TREE` (delta 17(r)); Prop 6.3
  descent (delta 17(q,t)); Φ (delta 17(t)); fail-closed enumerator.
  Does not depend on OPEN[MOH-PROGRAM-ARTIFACT], moments, or
  passport sharpness.
- **Cheapest discriminator.** (a) Algebra: `δ_2=1/4`, `A_2=4`,
  `P=12`, `(12)` as polynomials in `k` — done. (b) Compiler on
  `(28,20; X, M_2=25, V_2=3)`.
- **Outcomes.** Empty at `k=2`: the ray is a method-client for a
  uniform Lemma-2.1 obstruction (Card III). Nonempty: CE-side,
  REPRESENTATIVE. Algebra of (a) fails: the family is not
  PATH-ARITH for large `k` (contradicts the identities; stop and
  repair).
- **Stop.** SATURATED-EMPTY or SURVIVES at `k=2`; or the identities
  of §2.2 fail a hostile check; or wall-clock 10 min desk / AWS cap
  on the compiler.
- **Information gain.** Q1 answered with an exhibit; Q3 given its
  first live instance; the CE list named. Highest of the round.

### Card II — `D=108` post-descent residue

- **Dependencies.** Card I's engine, not its family. `descend_once`
  with the window split of §11. Φ. OPEN[MINOR-DICHOTOMY] for the
  one `u_s=2` row.
- **Cheapest discriminator.** Already have the 21-row table. Next:
  Appendix II on `(18,12; γ^3, M_2'∈{4,8}, V_2'=1)`; separately,
  the p.209 alternative on `(108,72, V=(7,7))`.
- **Outcomes.** Both `(18,12)` empty and the minor row empty: `D=108`
  dies as a *pair* target (skeleton residue remains, typed
  PATH-ARITH). Either SURVIVES: first positive above 100, not on
  Card I. COUNTING-BOUND: emit and stop.
- **Stop.** A typed empty/nonempty on the two `(18,12)` systems, or
  desk cap.
- **Information gain.** Q2's "cheapest on the first `D=108`
  survivor" answered without a new sibling engine. Redesigns the
  running sibling-coefficients lane.

### Card III — Lemma 2.1 Newton polygon vs `M_2=n'−3`, `V_2=3`, `k_jac=1`

- **Dependencies.** Card I's 4-tuple; Lemma 2.1; Def 5.1 on the
  descended data; Φ. Does *not* depend on Gröbner.
- **Cheapest discriminator.** Write the Newton polygon of a
  Lemma-2.1 `g` of π-degree `8k+4` with one x-degree-2 coefficient,
  compute the unique candidate `M_2`, compare to `12k+1`. Desk
  algebra, one page. Control: at `k=1` the polygon must be
  incompatible (Moh already empty) or the write-up is wrong.
- **Outcomes.** Incompatible for all `k`: `(T)` on Card I, a
  proof-along-the-ray, no compiler needed for this family.
  Compatible at a finite set of `k`: those `k` are the compiler
  list. Compatible for all `k`: the obstruction is not the polygon
  (fall through to the compiler; this card stops).
- **Stop.** A typed compatibility, or a hole in Lemma 2.1's
  hypotheses on this family (return OPEN, do not fill).
- **Information gain.** Distinguishes "Appendix II forever" from
  "one Newton obstruction". If it hits, it is the all-degree form
  Q3 asked for.

---

## 13. Continue / redesign / stop

**Current (packet).**

| lane | rec | why |
|---|---|---|
| `sibling-coefficients-sol56` | **redesign** | Q2 on a `(90,60)` excess row and an un-descended `D=108` survivor is the expensive original-pair sibling system. Point it at Card II's two `(18,12; γ^3)` and/or Card I `k=2`. If the lane has not yet chosen coefficients, this is a one-line retarget. In-progress `.md` not read. |
| `screened-census-grok46` | **continue, retarget** | D≤400 × nested-pack is fine as a census dump and a search for *other* `A_2\|P` families. Cofinality of the screen is no longer the question (Card I). Do not spend the seat proving emptiness. In-progress `.md` not read. |
| `descent-radii-grok46` | **stop** | DONE. Φ closed. G2/G3 COUNTING-BOUND, and `D=105` is empty under the tree anyway. |
| `a2six-ray-moment-sol56` | **stop** | DONE. Ray dead under the tree; moment page not intrinsic; engine-validation harvested. |
| `whole-tree-review-opus5` | **continue** | Second gate of 17(r). A refutation aborts this round (packet). In-progress `.md` not read. Do not duplicate Grok's source+DP gate. |

**Queued (packet).**

| front | rec | why |
|---|---|---|
| tree-decorated moment engine | **defer** | Decoration wall is real; descent is the cheaper decoration for `u_s=1`. Repair custody first. |
| P202 residue through sibling system | **redesign** | Through `descend_once` (window-split), not original-pair Gröbner. Artifact, not frontier. |
| box01 restart for heavy Gröbner | **hold** | Only if Card I `k=2` remains COUNTING-BOUND after Φ. |

`appendix2-compiler-grok46` (launched ~12:0xZ, no `final_status`,
not read): **continue**, feed `(28,20)` and `(18,12)`.

**Single first lane:** Card I, `k16-ray-T`. If no new seat, the
redesign of `sibling-coefficients-sol56` onto those two systems
*is* that lane.

---

## OPENS RAISED

**OPEN[K16-RAY].** The family of §2.2 is `C_FULL_TREE`+ODE for every
`k≥1`, as PATH-ARITH skeletons, not as pairs. Bounded: the identities
`δ_2=1/4`, `A_2=4`, `P=12`, `Q=9`, `h=4/3`, `(12)`, and the `j=2`
packing; plus machine `k=1..6`. Cheapest test: hostile replay of
those identities and `full_tree_ok` on `k=1,2`. (This session: PASS.)

**OPEN[T-K16].** Theorem `(T)` on the 4-tuple
`(12k+4, 8k+4, 12k+1, 3; J=cγ)`. Bounded: one family, one new
instance `(28,20)` at `k=2`. Cheapest test: Card I(b) / Card III.

**OPEN[D108-RESIDUE].** After TREE+ODE+passport+`descend_once`
(window-split), the live `D=108` pair-targets are two `(18,12; γ^3)`
systems and one `u_s=2` row. Bounded: 3 problems. Cheapest test:
Card II.

**OPEN[DESCENT-WINDOW-TOP].** Engine `WINDOW-FAIL` on descended
`s'≥3` is the new-top window of (6), which does not descend.
Bounded: 17 rows at `D=108`, 80 at `D=120`, 0 intermediate fails on
the scanned slice. Cheapest test: the split already run; then the
§11 patch. Not a kill.

**OPEN[MINOR-DICHOTOMY]** (carried, not new). p.209 for `u_s>1`.
Bounded at the frontier: 1 passport-alive `D=108` row
`(72, M=(81,106), V=(7,7))` plus Moh `(99,66)` as control. Cheapest
test: replay Moh's two minor-disc distributions on the `D=108` row.

**OPEN[MOH-PROGRAM-ARTIFACT]** (carried). 49 excess rows after
passport at `n≤100`; needs the CDC listing. No desk test. The tree
did not close it; neither does Card I (which *contains* a printed
row, not the discriminator of the six).

**OPEN[SIBLING-COEFFICIENTS]** (carried, retyped). Uniform form is
`(T)` after descent, not a new original-pair engine. Bounded: 3
pair-problems at `D=108` (the D108-RESIDUE list).

**OPEN[FULL-TREE-RECENTER]** (carried, not first). Edgewise recentre
is an over-read of Prop 5.4. Bounded: the 55→20 row drop at `n≤100`.
Do not promote.

**OPEN[PASSPORT-SHARPNESS]** (carried, not first). Passport is
EXTERNAL. Bounded: 3 rows in the 58→55 ODE-to-passport drop. Card I
survives the implemented passport for `k=1..6`; do not promote as
source.

Not raised: a cofinal emptiness of `C_FULL_TREE`; a residue floor
for PS-GROWTH; `M_2>m` as a theorem; UNI as a theorem; a fourth
invariant of the original pair.

---

## 15. Desk CAS

Stdlib, one core, no AWS. `n≤100` `K_min=2`: 658 → 60/58/55 in 1.00 s
(matches banked). Campaign `K_min=16` at
`D∈{72,84,96,100,108,112,120,132}`: TREE 2, 6, 35, 3, 21, 8, 94, 6
in 2.2 s; `D=108` 21/20/18 matches the packet; `s3_inter_fail=0`.
Card I identities + `full_tree_ok` for `k=1..6` in 0.2 s. No `sat()`
this session (banked saturations cited with their controls). No
`charge_basis`. No ledger edit. Display-formula citations are to
banked image-first reports (whole-tree, m2-descent, descent-radii);
Card I / window-split / `D=108` residue are not source-display claims.

## 16. What I am not claiming

Card I is PATH-ARITH+tree, not a pair (floor, not attainment).
`D=105,117` empty of skeletons, not of pairs. Engine `WINDOW-FAIL`
and `M_2'≤m'` are not kills. Passport is not Moh. `(T)` is unproved
except the printed Appendix-II SATURATED-EMPTY cases. No new exit
price.

## COLLISIONS

`python3 ops/open_collision.py`: `status: CANDIDATES` (review
candidates, never closures). Same-round `ideation-20260903T1200Z-*`
files and in-progress lane reports the scanner listed were **not
opened** and are not used.

- `OPEN[K16-RAY]` (report:713): NONE
- `OPEN[T-K16]` (report:719): NONE
- `OPEN[D108-RESIDUE]` (report:723): NONE
- `OPEN[DESCENT-WINDOW-TOP]`: lexical noise in old notes / nfz reviews;
  no banked claim that engine `WINDOW-FAIL` is a kill. Stands.
- `OPEN[MINOR-DICHOTOMY]`: AUDIT.md:15940, 15951 and
  `m2-descent-opus5-20260903.md:669` — carried, same name, same `u_s>1`
  / p.209 quantity. Not a closure. First screened client above 100 is
  new (the `D=108` `V=(7,7)` row).
- `OPEN[MOH-PROGRAM-ARTIFACT]`: AUDIT.md:15860 (49 excess). Carried,
  not closed.
- `OPEN[SIBLING-COEFFICIENTS]`: one global-interp-review hit, not a
  closure; retyped here as `(T)` after descent.
- `OPEN[FULL-TREE-RECENTER]`: NONE
- `OPEN[PASSPORT-SHARPNESS]`: AUDIT.md:15992 (EXTERNAL, 3 rows).
  Carried.

---

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `40505`.
- Body SHA-256:
  `1c5a7feb193bc508aa9d17cec54ba8b120fc1df2b66e2140025176d5c327c601`.
- Frozen basis: `4250a6e47f514c6c76a9bc708fbaa533db657eb8`.
