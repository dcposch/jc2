# Mixed next-operator procedure review

Status: **GAP / SOURCE-ONLY / NO LAUNCH AUTHORITY**. First action
`2026-09-12T21:46:17.520895750Z`. The preceding qualification attempt remains
CLOSED; none of its host, boot, cgroup, clock or registration facts were reused.

## Verdict

No complete literal no-observer operator command procedure can satisfy the
requested actual child rlimit and kernel-cgroup verification with the selected
source bytes. I stopped at that exact obstruction and did not invent a runtime
framework, polling race or launch command.

The new probe closes the child identity/readiness gap for PID, PGID,
start_ticks, boot, PID namespace, cgroup path and touched payload. CAPRUN closes
the leader identity, sampled group RSS and identity-matched TERM/KILL/reap
record. Neither records the live child's actual `RLIMIT_AS`/`RLIMIT_FSIZE`, and
CAPRUN records requested rather than child-observed CPU limits. Neither records
the live bytes/identities of cpu.max, cpu.max.burst, memory.max,
memory.swap.max or controller/delegation state.

Those `/proc` child facts cease to exist after reaping, and the transient
cgroup can disappear before terminal sequential inspection. An asynchronous
operator inspection has no guaranteed window: CAPRUN samples a32MiB RSS cap
while the probe deliberately touches64MiB before its marker. Requiring a human
or model to poll faster would be a race, not a finite verified procedure; a
new observer is outside scope.

`box/f10-mixed-next-operator-procedure-sol-20260912/PROCEDURE.md` records all
unqualified successor placeholders, the exact unavailable evidence, the
terminal artifacts that would otherwise be usable, and the smallest possible
future resolution—adding actual rlimit and bounded cgroup-control readings to
the same probe record—without implementing or authorizing it.

All 25-second, CPU80%/100ms/burst0,32GiB memory/AS,swap0,256MiB FSIZE,
separate256MiB tmpfs, one-cgroup/no-escape, bounded-stream, exact-startup and
source-pin requirements remain unchanged and uninstantiated. Existing native
metadata is not promoted to complete qualification. No worker, dummy,
interpreter, import, syntax, AST, CAS, AWS or network action occurred.

Fresh WHOLE reads covered the eight inputs in PINS. Final WHOLE readback covers
PROCEDURE, PINS, report and finalizer manifest; strict input postpins precede
terminal custody.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `2402`.
- Body SHA-256:
  `039394d0bf3b4f606662925bf2bbd8e8ff974f0f49dd8bc0be1984fa5e82fb1c`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
