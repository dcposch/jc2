# Blind whole-portfolio ideation — Grok 4.6 — round `20260902T0022Z`

Lane: Grok 4.6 (xAI), equal-standing independent researcher.
Produced blind against the sealed packet and banked repository
reports only. No other `20260902T0022Z` lane prompt, log, run
record, or report was opened. No canonical edit, commit, push,
AWS action, or CAS. Bare `G2` is not used.

## 0. Custody

Packet hash verified, byte-exact against the frozen copy:

```text
df7df824c39e3b9a2eeefc398f813e9b99aa11268ac98ff6b4c8fc26cd9a6c2c  /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.BZvblo/inputs/ideation-20260902T0022Z-packet.md
```

Packet basis `f6a4591d` plus later ledger appends; the packet's
deltas are the charged facts.

Firewall: flag ≠ place ≠ cover series; `REPRESENTATIVE` is not
`FULL_ACTUAL_EXIT`; dicritical, `A_F` component, and Galois-sheet
stay distinct; four-box `a,b,sigma,e_j` stay distinct; BLOCK-zero
is a floor; `(M')` under `(H2)` is not the block-free form;
packages `(F1)/(F2)/(S1)/(S2)/(S4)` consumed-not-rederived; no
safe replacement ⇒ typed `OPEN`; no exit-price ⇒ no
`charge_basis`.

Read: the packet; `APPROACHES.md` + 46-row table; top of
`AUDIT.md`; `LIVE STATE` through 00:22Z; banked reports supplying
the named deltas (Domrina II, DO-I mu2, REP-96, TB-GERM,
encoding-faithfulness r2, CELL-32, SHEET-GATE, CAGE-N). Other
`20260902T0022Z` submissions were not opened. No recycled
20260831T1033Z cards. No flagship relaunch.

## 1. Disposition vector

Legend: `U` unchanged, `R` raise, `L` lower, `O` reopen. Reasons
only for changes. Homonymy does not move a row.

### 1.A Packet-named avenues

| Avenue | d | reason if changed |
|---|:-:|---|
| N=4 literature (Domrina II + DO-I mu2) | R | SOUND-AFTER-REPAIRS modulo packages; R1/R2 confirmed; FORK-CONSUMED-NOWHERE; mu2 REPLAYED-SOUND with Prop 4.1 and Cor 3.8 load-bearing. Do **not** promote: mu2 Grok review is `PROVISIONAL`; packages unrederived. DET-LINF is a lemma, not JC2. |
| N=4 campaign residual | R | Dead: `(8,6,11)`/`(8,6,7)` at rep (TUBE-2+TB-GERM, `beta_1=8+3kappa`, `kappa` odd); `(8,6,3)` at curve (`I_29` EMPTY, KILL-BINDING). Live: `(8,6,9)` at `c_2(T)`; `(9,6,2)` REALIZED+CABLE-3; `(9,6,4)` nonempty, N-A-boundary. Old suite was unfaithful. |
| REP-96 §7 fork | R | BLOCK-zero is a floor (`OPEN[BMFACT-STRAND-VS-BLOCK]`, `OPEN[BMFACT-BASEPOINT]`). SOURCE-IS-C2 is the largest gap and is not a flagship. MPRIME-COMPANION is Path 2's client. |
| Literature posture `N>=6` | R | N=5 not soundly closed. GGV `gcd>=16`, `!=2p` is total-degree, not geometric degree. Sigray 9.1 unaudited. Raise soundness, not a census-clone. |
| Instruments | R | SIROCCO+ZvK on a real curve; unfaithful ideals and Sage footguns #10/#11 are systematic. PARAM next for 869/964. |
| Flagship MPRIME-ALLN-H2 | U | Endorsed. Do not duplicate. |
| Flagship COMPANION-CURVE-ALLN | U | Endorsed, includes `(9,6,2)`-`D_2`. Do not duplicate `chi_2+sigma_2=1`. |
| A2 horn `OPEN[A2-CELL-32]` | R | T3/T4 closed `s=0` and `deg eta=0` on `G!=0`; T1–T2 rigidify the live section. Successor is the named `3x3` at `e>=1`. Construction stays CE-level. |
| N=5 census | R | Raise as soundness; redesign as Card III, not an N=4-suite copy. |
| Primitive monodromy | R | Prime N ⇒ primitive. Cheap N=5 filter, not a td-ceiling. |
| Degree-monotone invariant | L | Adjacent survivors kill naive monotonicity. No candidate. |

### 1.B 46-row inventory

Legend as above. Only changes listed; every unlisted row is `U`.
Count: raise 2, 7, 16, 25, 26, 27, 31; lower 1; reopen none.
Row 25 is an instrument raise, not a colouring-genus revival.

| # | d | reason |
|--:|:-:|---|
| 1 | L | GGV corners do not decide `(9,6,2)` or Zoladek 4.10. Total-degree firewalls already sit in GGV. |
| 2 | R | DET-LINF gives an unconditional tree-sign; a realized N=4 curve exists. No cofinal td ceiling. |
| 7 | R | `(9,6,2)` is an actual polynomial curve that would be a branched `A_F` component of a Keller map. |
| 16 | R | A2 T1–T5 yield a named `3x3` leader system on `C[Z]`, the first index-shaped client on that ring since LND died. |
| 25 | R | SIROCCO+ZvK ran on a real curve. Riemann existence remains generous (SOURCE-IS-C2). |
| 26 | R | Prime N forces primitivity; cheap at N=5. The `d2=2` sandwich is the all-degree counterpart. |
| 27 | R | CABLE-3 is a family-2 `g=3` inner-braid gate (6 classes / 144 tuples). |
| 31 | R | SHEET-GATE `Y=Spec B` with four boxes is the correct `Y` for SOURCE-IS-C2, not a bare Riemann-existence surface. |

Negative space: no reducible rank-four trees; no colouring
conductors; no BLOCK-zero as a kill; no `(M')` under `(H2)`
against `(9,6,2)`; no R1/R2 name-export; no Moskowicz-as-proof;
no desk CAS.

Packet-flags: mu2 is `PROVISIONAL` until the in-flight Grok
review — "N=4 foundation acceptable" is calibration, not
promotion. `(8,6,9)` `c_2(T)` is a Chern number, not a Groebner
generator. `(9,6,4)` dies only to a sourced YES on
`OPEN[NA-R1-SHARPNESS-IRREDUCIBLE]`; do not relax N-A.

## 2. Reranked bottlenecks

Proof, then disproof, by expected information per desk-day.
Floors are not attainments. Running jobs are consumed.

1. **SOURCE-IS-C2** on any realized residual. Riemann existence
   gives a normal degree-4 cover; a Keller map needs SHEET-GATE
   `U ⊂ Y` with `U ≅ A^2`. Largest gap; neither flagship attacks
   it. Card I.
2. **SAGE-NATIVE BM-factorisation** of `(9,6,2)`. BLOCK-zero is a
   floor. Zero plus reviewed scripts kills this curve at
   representation (not the architecture). Consume; do not relaunch.
3. **`OPEN[NA-R1-SHARPNESS-IRREDUCIBLE]`.** One unit at
   `C^2=2 r_1`, `T=T_x=0` irreducible nodal. YES kills `(9,6,4)`
   and downstairs `(6,4)`/`(8,4)`. Do not relax by analogy. Card II.
4. **`c_2(T)=chi(O_Z)` of `(8,6,9)`.** Local `m=4` is admissible;
   residual is triple-plane geography. Independent of job 869.
5. **A2 `3x3` at `e>=1`**, named in CELL-32-TERMINATION §6.
   Construction stays CE-level.
6. **N=5 soundness.** Zoladek 4.10 gapped; GGV `>=16` is total
   degree; Sigray 9.1 unaudited; H2 already killed the N=5 trivial
   dicritical. Residual: reducible `A_F` plus a literature repair.
   Card III.

Path 1 and Path 2 are staffed; they would eat the companion clause
and the H2 side, and still leave SOURCE-IS-C2.

Disproof: (i) algebraize `q|_U` only after a Chern match — matching
Chern numbers are not a map; (ii) K00 at `J^5 ⊂ I ⊂ J^2`,
orthogonal, do not starve; (iii) 869/964 points are curves, not
maps; (iv) Witt unchanged. Proof-side currently dominates. A YES
on SAGE-NATIVE plus a YES on Card I would flip that overnight.

## 3. New avenue or mechanism

**Euler mismatch of the Riemann-existence cover against the
four-box decomposition** — a numerical test of
`OPEN[REP-96-SOURCE-IS-C2]` that constructs neither `F` nor `D_2`.

Not Path 1, not Path 2, not a BM-factorisation rerun, not the
Aug 31 residual-sheet card.

SHEET-GATE (`round1033-sheet-gate-opus5-20260831.md` §2) builds
`Y=Spec B` with open immersion `j: A^2 -> Y` and finite flat
`q: Y -> A^2` of degree 4, `q∘j=F`. Write `U=j(A^2)`, `B_Y=Y\U`,
`A_F=q(B_Y)`. Four boxes: `a=#F^{-1}(p)` in `(U,e=1)`; `(U,e>1)`
empty by Keller; `b` in `(Y\U,e=1)`; ramified box nonempty by
purity; `d=a+b+sum delta_j e_j`, `sigma=a+b`, hence `a<=d-2`.
`B_Y` is a divisor on `Y`, not a dicritical and not a Puiseux
place of `D`.

REP-96 names a different `Y`: the Riemann-existence cover of a
representation `phi: pi_1(A^2\D)->S_4`, and types SOURCE-IS-C2 as
"`Y ~ C^2`". That is the wrong question. SHEET-GATE `Y` is never
`C^2` (Lemma 2.2: `B_Y` nonempty). The right question: **does the
Riemann-existence cover admit a Zariski-open `U ≅ A^2` whose
complement is a curve mapping onto `D`, with four-box numbers
matching SHEET-GATE?**

Cycle types of the banked SIROCCO braids determine `sigma`, local
`H_p`, and the `e_j`. Cycle type is conjugacy-invariant, so
`OPEN[BMFACT-STRAND-VS-BLOCK]` does not infect this arithmetic.
(`OPEN[BMFACT-BASEPOINT]` still infects labelled transports;
any count that needs a labelled word stays `OPEN`.) Independently,
`chi_c(Y)_RH` is the compact-support Euler of a 4-sheeted cover
branched along a nodal curve of known degree and known local
monodromy (for `(9,6,2)`: transpositions generically, two disjoint
transpositions at each of four nodes). If `U ≅ A^2` and
`Y=U ⊔ B_Y`, then `chi_c(Y)=1+chi_c(B_Y)`.

For `(9,6,2)`, REP-96 already gives `sigma=2`, `a=2` (so `b=0`)
and ramification sum 2, i.e. one ramified dicritical of type
`(delta,e)=(1,2)` over `D_1`. That four-box split is a *floor*,
not a kill. The Euler identity is the extra equation. If
`1+chi_c(B_Y) != chi_c(Y)_RH`, the cover cannot be a Keller
SHEET-GATE `Y`. Two typed modes, never merged:

- Mode `D_1`-only: irreducible `A_F`. Under H2, nodal N=4 already
  dies by `(M')` (REP-96 §7 control). Mismatch is confirmatory.
  Match contradicts promoted H2+(M') and flags a bug in the Euler
  computation — type `OPEN[MODE1-MATCH-VS-H2]`, do not claim a CE.
- Mode `D_1 ∪ D_2`: needs a numerical type for `D_2`. Path 2 is
  the producer (`chi_2+sigma_2=1`, deg `>=2`, Lemma NL). This
  avenue does not construct `D_2`; Mode 2 is `OPEN` until Path 2
  emits a type. What it does unconditionally is Mode 1 plus the
  arithmetic any later `D_2` must satisfy.

Untyped compact RH: `OPEN`, do not fill by cap. Pole identities
on `L` fire only after that vertex class is named. Colouring,
CABLE-3, BM-factorisation, and `(M')` do not compute `chi_c(Y)`.

## 4. New connection between existing avenues

**The missing `(8,6,9)` input `c_2(T)=chi(O_Z)` and the missing
`(9,6,2)` Euler `chi_c(Y)_RH` are the same Chern-number species:
geography of a low-degree `S_3`/`S_4` cover of the plane, branched
along a curve of known weighted degree, with a typed non-Gorenstein
or nodal fibre.**

Rows **#25, #31, #7, #2** plus the live N=4 residuals.

TB-GERM type B (`(8,6,9)`, `beta_1=23`, germ `A_22`, `m=4`) names
`c_2(T)=chi(O_Z)` of a non-cyclic triple plane: `deg Delta-bar=10`,
`(deg S_pi, deg T_pi)=(8,1)`, one non-Gorenstein point
`C[z,w]/(z,w)^2`, 10 `A_1` points (`tb-g2-finish-opus5-20260901.md`
§7.1). That is `S_3` geography; Ciliberto–Miranda
arXiv:2512.07965v1 is the named slot at weighted degree `<=10`,
not a campaign theorem. Card I's `chi_c(Y)_RH` is the same species
on an explicit degree-4 cover. Groups differ; the Chern number
does not. One cycle-type→`chi` pipeline (§6) serves both
residuals. DET-LINF is the sign on whichever tree is named;
export requires retyping Pic generators. Mixing the two covers
identifies a cover series with a place.

## 5. Strongest proof attack and strongest counterexample attack

### Strongest proof attack this week

**Euler/four-box test of SOURCE-IS-C2 on the banked `(9,6,2)`
braids, in parallel with consuming SAGE-NATIVE.**

SAGE-NATIVE is already running; do not relaunch. The new desk
attack is Card I Mode 1 on conjugacy-invariant SIROCCO output
(8 transposition tangencies, one four-square node fibre) and
`q=t^6+8t^2`, `p=t^9+12t^5+24t` (REP-96 §1). Check four-box
`(a,sigma,ram)=(2,2,2)`; compactify as `P^2` with named `L` and
`D_bar` (degree 9, `delta_inf=24`, `beta_1=25`), never identifying
that place with a dicritical; write ramification plus node
correction; if `L` is untyped, `OPEN[RH-LINE-AT-INFINITY]`;
compare `chi_c(Y)_RH` to `1+chi_c(B_Y)`. Parallel: Card II.
Third: A2 `3x3` at `e>=1`.

### Strongest counterexample attack this week

**Algebraize `q|_U` only after a Chern match.** Do not interpolate
`(P,Q)` from the curve (assumes irreducible `A_F`). Do not steal
Path 2. Do not run another Witt level. A SAGE-NATIVE class plus
Mode-2 match licenses one Padé step of two primitive elements of
`B=O(Y)`: declare the ring map; if an ideal appears, extract the
component, assert the ring, positive control
`p^2-q^3-64q=64t^2`, non-branch negative control. Native zero:
return to K00 at `J^5 ⊂ I ⊂ J^2`. PARAM points are curves, not maps.

## 6. Software acceleration or decisive experiment

**Fail-closed cycle-type geography calculator**, desk Python, no
Groebner, no SIROCCO rerun.

Input: either banked `(9,6,2)` SIROCCO JSON plus
`(d,n,delta_aff,delta_inf,beta_1, #nodes)`, or a Miranda type
`(deg Delta-bar, deg S_pi, deg T_pi, m, #A_1, fibre algebra)` as
TB-GERM §7.1 lists for `(8,6,9)`. Output: `(sigma, a, b,
ramification, chi_c(Y)_RH, chi_c(B_Y), match|mismatch|OPEN)` with
the compactification named, or typed `OPEN` (untyped `L`; Mode 2
lacks `D_2`; flag/place/series collision; `sat()` on a non-ideal).

Fail-closed: cycle types only (do not close
`OPEN[BMFACT-STRAND-VS-BLOCK]`); declare the target ring and the
function cutting out `D`; radical not `squarefree_part` (footgun
#10); no Sage `__main__` (footgun #11); positive control
`(9,6,2)` four-box `(a,sigma,ram)=(2,2,2)`; negative control
`(6,4)` at `E=-3` must not match; optional DET-LINF on a *named*
dual graph, never Domrina vertex names on a Sigray/Orevkov tree.

First `(9,6,2)` run is the §5 attack. First `(8,6,9)` run either
computes `c_2(T)` from the type or returns `OPEN` (curve-dependent,
TB-GERM §6). PARAM is the next library release, not this experiment.

## 7. Campaign-systems check

**`UPGRADE`** — encoding-faithfulness preflight as a required
gate of every Groebner / msolve job.

Evidence: all six suite ideals were UNFAITHFUL (raw binomial vs
reduced approximate root); `(9,6,2)` was a false EMPTY. That is
the week's costliest silent-zero, larger than footguns #10/#11.
FALLACY-v2 names remainder degree; nothing yet *refuses* a `.ms`
of raw `p^a-q^b` coefficients. Attach at the qqideal oracle (P1
disagreement = ORACLE-P0). Seal-at-completion is already adopted.

Smallest upgrade. (1) Every nodal-type job emits the reduced
second approximate root beside the `.ms` and diffs it against the
raw binomial; nonempty diff = `UNFAITHFUL`, solver refused.
Positive control: realized `(9,6,2)` passes corrected, fails old.
Negative control: encoding-faithfulness r2 point A
(`Delta=(8,6,19)`) must be rejected. (2) Record remainder degree
in the declared quotient, branch on vanished leaders, handle
zero; no boolean emptiness.

Not the upgrade: do not retire M2 before the oracle reports; do
not block Card I on PARAM; do not change the promotion rule.
Clause 2 is new: no frozen input requires remainder-degree as a
solver refusal.

## 8. Idea cards (at most three)

Not Path 1, Path 2, a colouring census, a SAGE-NATIVE relaunch, or
a recycled 20260831T1033Z card.

### Card I — Euler mismatch of the Riemann-existence cover

**Dependencies.** SHEET-GATE four-box + Lemmas 2.2–2.3; REP-96 §1
and §7 R2; banked SIROCCO census. Not Path 1/2, NA-R1, A2, or
Domrina packages. Mode 2 waits on Path 2's `D_2` type
(`chi_2+sigma_2=1`, deg `>=2`, Lemma NL).

**Discriminator.** Desk Euler vs four-box on banked `(9,6,2)`
cycle types, compactification named (§5). No CAS, no strand labels.

**Outcomes / stop / information.** Four-box fails `(2,2,2)`: `OPEN`
(bug). Mode-1 mismatch: irreducible SOURCE-IS-C2 dies. Mode-1
match: `OPEN[MODE1-MATCH-VS-H2]`, not a map. Untyped `L`:
`OPEN[RH-LINE-AT-INFINITY]`. Mode-2 mismatch kills that companion
type; match licenses the §5 Padé step only. Stop when every
realized residual has Mode 1 and Mode 2 is decided or waiting; stop
immediately if cycle type depends on the SIROCCO basepoint. First
number on the largest geometric gap; an `OPEN` on compactification
is also the missing lemma of `(8,6,9)`'s `c_2(T)`.

### Card II — N-A sharpness at equality, irreducible nodal

**Dependencies.** N-A as promoted (strict `C^2 > 2 r_1+4T+T_x`;
sharpness is the `T_x` coefficient). Fold identity
`C̃^2=d^2-2 delta_inf-M_inf`. Targets: `(9,6,4)` and downstairs
`(6,4)`/`(8,4)`. Does not consume the bitangent-conic
countermodels beyond `T_x != 0`.

**Discriminator.** Desk literature+countermodel: may `C^2 >= 2 r_1`
when `C` is irreducible, nodal, `T=T_x=0`, only non-nodal point
the resolved infinity place? Witness or sourced proof. Do not
relax by analogy. Sources: Nori 3.27, N-A packet, Orevkov 1990.

**Outcomes / stop / information.** YES: those rows die, no Box03.
Do not export onto `(9,6,2)`. Witness that strictness is essential:
OPEN closes negative. Untyped Nori at infinity: `OPEN`. Stop at
one YES, one witness, or a named missing hypothesis. Do not
iterate `OPEN[NA-AGGREGATE-REDUCIBLE]`. Packet already flags this
as lethal to `(9,6,4)`.

### Card III — Sound N=5 without an N=4 suite clone

**Dependencies.** Zoladek 4.10 gap (`I_2 ⊂ (1/m)Γ(f_2)`,
arXiv:1401.1784, custody `8b426751`); GGV total-degree `>=16`,
`!=2p`; Sigray 9.1 (`refs/sigray_full.pdf`); H2-kill of the N=5
trivial dicritical; CAGE-N N=5 instance, NO-DEG-CAP; DET-LINF.
Prime N ⇒ primitive (row 26). No Domrina vertex-name export.

**Discriminator.** (1) Retype DET-LINF at a Zoladek/Sigray
infinity tree (name ambient, Pic, subgraph `L̃∞`); ask whether
`det<=0` is 4.10's inclusion, else `OPEN`. (2) Replay Sigray 9.1
at geometric degree 5 only. No N=5 msolve until one returns.
Free screen: only primitive subgroup of `S_5` with a transposition
is `S_5`.

**Outcomes / stop / information.** DET-LINF ⇒ 4.10: literature
N=5 repairs at that lemma, not JC at degree 5. Orthogonal: `OPEN`
on 4.10, keep Sigray. Sigray SOUND: geometric degree 5 closed;
H2 residual is reducible `A_F` (Path 1). Sigray GAP: census
justified, primitive screen first, faithfulness mandatory. Stop at
first typed closure of degree 5, or GAP in both plus a named first
census profile. No conductors, no `2m<=N-1` by rank-four copy.

## 9. Lane verdicts: continue / redesign / stop

Lanes named in the packet's state deltas and launched flagships.
Older overlay names are treated as superseded when they conflict.

| Lane | Verdict | Ground |
|---|---|---|
| N=4 literature / mu2 review | **continue** review-only | Acceptable foundation; mu2 `PROVISIONAL`. No fourth replay. Packages unrederived. |
| Job 869 `(8,6,9)` curve | **continue** | Type-B realizability. Independent of `c_2(T)`. |
| `(8,6,9)` `c_2(T)` | **redesign** | Card I `S_3` mode / §6 calculator, not Groebner, not unlicensed Ciliberto–Miranda. |
| `(8,6,11)`/`(8,6,7)` | **stop** | Twice-derived rep kills. Bind repairs at integration. |
| `(8,6,3)` | **stop** | Curve KILL-BINDING. |
| SAGE-NATIVE + script review | **continue** | BLOCK-zero is a floor until this lands. |
| SOURCE-IS-C2 as `Y~C^2` | **redesign** | Card I. SHEET-GATE `Y` is never `C^2`. |
| Path 2 COMPANION-CURVE-ALLN | **continue** | Endorsed. Card I Mode 2 consumes its `D_2` type. |
| Path 1 MPRIME-ALLN-H2 | **continue** | Endorsed. Consumes N=5 reducible residue after Card III. |
| Job 964 `(9,6,4)` | **continue** | Race with Card II, not a stop. |
| A2 `OPEN[A2-CELL-32]` | **continue** | Named `3x3` at `e>=1`. No LND/Picard/ML, no pullback. |
| N=5 census clone | **redesign** | Card III. No corrected-ideal jobs until faithfulness + first discriminator. |
| Primitive-as-td-ceiling | **redesign** | Free N=5 screen (transpositions force `S_5`). `d2=2` sandwich stays all-degree. |
| Degree-monotone hunt | **stop** | No candidate. |
| Colouring conductors | **stop** | Saturated at 28. |
| Reducible rank-four trees | **stop** | Closed by `m=1`. |
| K00 algebraization | **continue** inherited | Orthogonal. Sharp filtration only. No desk CAS. |
| Witt / sparse-above-GGV | **continue** low | Not this week's CE. |
| qqideal oracle | **continue** + §7 | Attach the preflight. |
| Heavy CAS policy | **continue** | Quotas unconstrained; faithfulness still required. This ideation ran no CAS. |

If SAGE-NATIVE returns zero and is reviewed: stop the `(9,6,2)`
representation lane, keep Card I as architecture, shift budget
onto Cards II–III, `(8,6,9)` geography, and A2 `3x3`. If a
survivor: raise Card I and the §5 Padé step, still without
duplicating Path 2.

<!-- BODY-END -->
