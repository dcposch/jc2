# Blind whole-portfolio ideation — Grok 4.6 — round `20260902T0022Z`

Lane: Grok 4.6 (xAI), equal-standing independent researcher.
Produced blind against the sealed packet and banked repository
reports only. No other `20260902T0022Z` lane prompt, log, run
record, or report was opened. No canonical edit, commit, push,
AWS action, or CAS. Bare `G2` is not used.

## 0. Custody

Packet hash verified, byte-exact against the frozen copy:

```text
df7df824c39e3b9a2eeefc398f813e9b99aa11268ac98ff6b4c8fc26cd9a6c2c  /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.BZvblo/inputs/ideation-20260902T0022Z-packet.md
```

Packet basis named: `f6a4591d` plus later ledger appends; the packet's
deltas are the charged facts. This lane did not treat later live
bytes as a substitute for the packet.

Firewall (applied below). A cv flag is not a physical place and is
not a cover series. `REPRESENTATIVE` is not `FULL_ACTUAL_EXIT`.
Dicritical component, `A_F` component, and Galois-sheet stay
distinct. Four-box counts `a`, `b`, `sigma`, `e_j` stay distinct.
A BLOCK-reading zero is a floor, not a kill. `(M')` under `(H2)`
is not the block-free form. Structure packages `(F1)/(F2)/(S1)/
(S2)/(S4)` are consumed-not-rederived. If no safe replacement
exists, the report returns typed `OPEN`. No exit-price is asserted,
so no `charge_basis` line.

Read: the complete packet; `APPROACHES.md` overlays plus the 46-row
table; current-state blocks at the top of `AUDIT.md`; newest `LIVE
STATE` in `notes.md` through the packet's 00:22Z launch; and only
those banked reports the packet names or that supply the named
deltas (Domrina II replay/repair, DO-I mu2, REP-96 inner and its
review, TB-GERM review, encoding-faithfulness r2, CELL-32
termination, SHEET-GATE, CAGE-N notes). Other lanes' `20260902T0022Z`
submissions were not opened.

This lane does not recycle its `20260831T1033Z` cards (residual
unramified sheet, δ-sequence dual graph, inertia-weighted Orevkov
export). Those clients are subsumed or rerouted by the new evidence.
It does not restatement-launch the two endorsed flagships
`MPRIME-ALLN-H2` and `COMPANION-CURVE-ALLN`.

## 1. Disposition vector

Two layers, as the packet names avenues and `COORDINATION.md` still
owns the 46-row inventory. Legend: `U` unchanged, `R` raise, `L`
lower, `O` reopen. Reasons only for changes. Homonymy does not move
a row. Lifecycle tags are `promoted`, `provisional`, `formal`,
`book-relative`, or `conjectural`.

### 1.A Packet-named avenues

| Avenue | d | reason if changed |
|---|:-:|---|
| N=4 literature chain (Domrina II + DO-I mu2) | R | Packet: SOUND-AFTER-REPAIRS modulo structure packages; censuses repaired; R1/R2 confirmed; FORK-CONSUMED-NOWHERE; §§5–7 no new gap; mu2 REPLAYED-SOUND with campaign Prop 4.1 and Cor 3.8 load-bearing. Raise because the literature route is now a checked chain rather than a reception slogan. Do **not** promote: mu2 Grok review is in flight (`PROVISIONAL`); packages `(F1)/(F2)/(S1)/(S2)/(S4)` are not rederived. DET-LINF is a reusable lemma, not a closure of JC2. |
| N=4 campaign residual `(8,6,*)`/`(9,6,*)` | R | Three types dead at stated scopes: `(8,6,11)`/`(8,6,7)` DEAD at rep level, twice-derived (TUBE-2 + TB-GERM, `beta_1=8+3kappa`, `kappa` odd, `beta_1 ≡ 2 mod 3`); `(8,6,3)` DEAD at curve level (corrected `I_29` EMPTY, two-engine, KILL-BINDING). Residual is three objects, not six. `(8,6,9)` rep-open at named global `c_2(T)`; `(9,6,2)` REALIZED and survives every promoted gate including CABLE-3; `(9,6,4)` locus nonempty, N-A-boundary. Encoding unfaithfulness was a real kill-reversal; the corrected board is the live one. |
| REP-96 §7 fork | R | The fork is now the campaign's sharpest object. BM-FACTORISATION is finite and running (BLOCK reading zero is a floor, `OPEN[BMFACT-STRAND-VS-BLOCK]`, `OPEN[BMFACT-BASEPOINT]`). SOURCE-IS-C2 is the largest geometric gap and is **not** being attacked by either endorsed flagship. MPRIME-COMPANION is Path 2's client, not a restatement target here. |
| Literature posture (received `N>=6`, Zoladek GGV gap) | R | Packet is right: N=5 is not soundly closed. GGV gcd-of-total-degrees `>=16` and `!=2p` is a *total-degree* firewall, not a geometric-degree theorem; it does not close N=5. Sigray's independent `td>=6` remains unrefereed and unaudited as a substitute. Raise the *soundness* of N=5, not a census-clone. |
| Instruments (msolveio+qqideal, SIROCCO+ZvK, footguns #10/#11) | R | End-to-end braid pipeline on a real curve is new campaign capability. Unfaithful ideals and Sage `squarefree_part`/`__main__` are systematic, not one-off. PARAM extraction is the next lever for `(8,6,9)`/`(9,6,4)` points. |
| Flagship MPRIME-ALLN-H2 | U | Endorsed Path 1; launched. Do not lower, do not duplicate. |
| Flagship COMPANION-CURVE-ALLN | U | Endorsed Path 2; launched, including a `(9,6,2)`-`D_2` job spec. Do not duplicate the collapse `chi_2+sigma_2=1`. |
| One-cusp A2 horn `OPEN[A2-CELL-32]` | R | Two sections closed by degree termination (T3/T4: `s=0` and `deg eta=0` empty on `G!=0`); live section is now one-parameter rigid in `C1` (T1) with two scalar laws on `Xi` (T2). Successor is a named desk `3x3` leader determinant at `e>=1`, not another rigidification. Raise because the horn is now a finite chamber computation rather than an open PDE. Construction remains CE-level; do not flip back to building a pullback. |
| N=5 census | R | Pipeline ready, and N=5 is the first unchecked geometric degree. Raise as a *soundness* target, redesign as a mechanism (Card III), not as a copy of the N=4 suite. |
| Primitive monodromy | R | At prime N the function-field extension cannot factor; monodromy is primitive. That is the cheap N=5 filter, not a td-ceiling slogan. |
| Degree-monotone invariant | L | Adjacent colourable survivors already kill naive monotonicity. No new invariant landed. Do not staff a conductor hunt. Remainder/DET-LINF/geography are algebraic replacements, not this row. |

### 1.B 46-row inventory

Legend as above. Only changes listed; every unlisted row is `U`.
Count: raise 2, 7, 16, 25, 26, 27, 31; lower 1; reopen none.
Row 25 is raised as an *instrument*, not as a colouring-genus route
(that use stays stopped).

| # | d | reason |
|--:|:-:|---|
| 1 | L | GGV corners do not decide `(9,6,2)` or Zoladek 4.10. Total-degree firewalls already sit in GGV. |
| 2 | R | DET-LINF gives an unconditional tree-sign; a realized N=4 curve exists. No cofinal td ceiling. |
| 7 | R | `(9,6,2)` is an actual polynomial curve that would be a branched `A_F` component of a Keller map. |
| 16 | R | A2 T1–T5 yield a named `3x3` leader system on `C[Z]`, the first index-shaped client on that ring since LND died. |
| 25 | R | SIROCCO+ZvK ran on a real curve. Riemann existence remains generous (SOURCE-IS-C2). |
| 26 | R | Prime N forces primitivity; cheap at N=5. The `d2=2` sandwich is the all-degree counterpart. |
| 27 | R | CABLE-3 is a family-2 `g=3` inner-braid gate (6 classes / 144 tuples). |
| 31 | R | SHEET-GATE `Y=Spec B` with four boxes is the correct `Y` for SOURCE-IS-C2, not a bare Riemann-existence surface. |

Unanimous negative space this round: no revival of reducible rank-four trees; no colouring conductors 34/40; no treating BLOCK-zero as a kill; no quoting `(M')` under `(H2)` against `(9,6,2)`; no export of R1/R2 graph names off Domrina's plane N=4 setting without retyping; no Moskowicz-as-proof; no desk CAS.

**Packet-flag.** mu2 is Sol-produced and `PROVISIONAL` until the in-flight Grok review lands. The packet's "N=4 foundation acceptable" is a verification-calibration statement, not a promotion.

**Packet-flag.** `(8,6,9)` "rep-open at `c_2(T)`" is a missing Chern number of a triple plane, not a missing Groebner generator. Do not feed it to msolve.

**Packet-flag.** `(9,6,4)` "would ALSO die to a YES on `OPEN[NA-R1-SHARPNESS-IRREDUCIBLE]`" is a one-unit literature question. A NO leaves the row alive. Do not fill by relaxing N-A.

## 2. Reranked bottlenecks

Ranked by expected information per desk-day toward a proof or a
counterexample of JC2, after the packet's deltas. Floors are not
attainments. Running Box03 jobs are consumed, not re-proposed.

### Principal proof bottlenecks

1. **`OPEN[REP-96-SOURCE-IS-C2]` for any realized N=4 residual curve.**
   Riemann existence from a braid representation supplies a normal
   surface `Y` with a degree-4 map to `A^2`. A Keller map additionally
   needs the SHEET-GATE open `U ⊂ Y` with `U ≅ A^2` and `q|_U` étale of
   Jacobian one. This is the largest single gap on the only realized
   type, and neither endorsed flagship attacks it. Card I.

2. **Convention-free BM-factorisation of the realized `(9,6,2)` curve.**
   BLOCK-reading zero is a floor (`OPEN[BMFACT-STRAND-VS-BLOCK]`,
   `OPEN[BMFACT-BASEPOINT]`). SAGE-NATIVE `6^9` is the cheapest
   remaining discriminator of the representation. If it returns zero
   *and* the scripts survive hostile review, the type dies at
   representation level and bottleneck 1 is moot for this curve (not
   for the architecture). Consume the running job; do not relaunch.

3. **`OPEN[NA-R1-SHARPNESS-IRREDUCIBLE]`.** N-A misses `(9,6,4)` (and
   the leftover `(6,4)`/`(8,4)` downstairs) by one unit at `C^2 = 2 r_1`
   with `T=T_x=0` irreducible nodal. A sourced YES kills those rows
   without Groebner. A sourced witness that the strict form is sharp
   leaves them alive. Card II. Do not relax the inequality by analogy.

4. **Named Chern number `c_2(T)=chi(O_Z)` of the `(8,6,9)` triple
   plane.** Local TB-GERM/`m=4` is admissible at the `A_22` germ; the
   residual is global geography (Ciliberto–Miranda weighted degree 10,
   one non-Gorenstein point, 10 `A_1` points). This is not a curve-job
   substitute; the curve job 869 decides realizability of the nodal
   type, which is independent and already running.

5. **A2 live cell at `e>=1`.** T3–T5 closed `s=0`, `deg eta=0`, and
   `r'=0` up to one wall. The successor is the general-`e` `3x3`
   leader determinant named in CELL-32-TERMINATION §6. Desk-scale.
   Construction of a pullback remains CE-level; inconsistency is the
   proof route.

6. **N=5 soundness, not an N=4-style suite.** Zoladek 4.10 is an
   unsupported lattice inclusion; GGV total-degree `>=16` does not
   close geometric degree 5; Sigray 9.1 is the independent `td>=6`
   candidate and is unrefereed. Under H2 the campaign already killed
   the trivial-dicritical N=5 profile. The residual is reducible `A_F`
   at N=5 plus a literature repair. Card III.

`(M')` all-N under H2 and the companion collapse are endorsed
flagships and are *not* re-ranked as new bottlenecks; they are
already staffed. They would, if they land, eat bottlenecks 1's
companion clause and the H2 side of N=4/N=5, and still leave
SOURCE-IS-C2.

### Principal disproof bottlenecks

1. **Polynomial structure on a four-box `Y` that already has the
   right Chern numbers.** If `(9,6,2)` retains a representation and
   Card I's Euler test matches `U ≅ A^2` geography, the remaining
   CE step is to algebraize `q|_U` as a polynomial Keller map. That
   is harder than K00 Padé, but it is aimed at an *explicit* branch
   curve rather than a formal germ. Floor: matching Chern numbers
   are not a map.

2. **K00 / sharp filtration `J^5 ⊂ I ⊂ J^2` algebraization.**
   Untouched by today's N=4 residual. Still the only disproof lane
   the 2026-08-21 surveys treated as capable of producing an actual
   map without a realized `A_F`. Orthogonal; do not starve.

3. **`(8,6,9)` or `(9,6,4)` point extraction.** If 869/964 return a
   Q-point, that is a curve, not a Keller map. It becomes a CE
   attack only after SOURCE-IS-C2 and a companion. PARAM is the
   instrument, not a search.

4. **Growing-support Witt tower.** Unchanged; not this week's lever.

Disproof information does not presently exceed the proof-side
frontier: we have one realized curve and a running representation
decision. The honest CE claim is that a YES on SAGE-NATIVE plus a
YES on Card I's Euler match would flip the ranking overnight.

## 3. New avenue or mechanism

**Euler mismatch of the Riemann-existence cover against the
four-box decomposition** — a numerical test of
`OPEN[REP-96-SOURCE-IS-C2]` that constructs neither `F` nor `D_2`.

Not Path 1, not Path 2, not a BM-factorisation rerun, not the
Aug 31 residual-sheet card.

SHEET-GATE (`round1033-sheet-gate-opus5-20260831.md` §2) builds
`Y=Spec B`, `B` the integral closure of `C[F_1,F_2]` in `C(x,y)`,
open immersion `j: A^2 -> Y`, finite flat `q: Y -> A^2` of degree
`d=4`, `q∘j=F`. Write `U=j(A^2)`, `B_Y=Y\U`, `A_F=q(B_Y)`. Four
boxes at a generic point of a branch component: `a=#F^{-1}(p)` in
`(U, e=1)` (nonempty); `(U, e>1)` empty by Keller; `b` in
`(Y\U, e=1)`; ramified box `(Y\U, e>=2)` nonempty by purity; and
`d=a+b+sum delta_j e_j`, `sigma=a+b`, hence `a<=d-2`. Flag/place/
series: `B_Y` is a divisor on `Y`, not a dicritical and not a
Puiseux place of `D`.

REP-96 names a different `Y`: the Riemann-existence cover of a
representation `phi: pi_1(A^2\D)->S_4`, and types SOURCE-IS-C2 as
"`Y ~ C^2`". That is the wrong question. SHEET-GATE `Y` is never
`C^2` (Lemma 2.2: `B_Y` nonempty). The right question: **does the
Riemann-existence cover admit a Zariski-open `U ≅ A^2` whose
complement is a curve mapping onto `D`, with four-box numbers
matching SHEET-GATE?**

Cycle types of the banked SIROCCO braids determine `sigma`, local
`H_p`, and the `e_j`. Cycle type is conjugacy-invariant, so
`OPEN[BMFACT-STRAND-VS-BLOCK]` does not infect this arithmetic.
(`OPEN[BMFACT-BASEPOINT]` still infects labelled transports;
any count that needs a labelled word stays `OPEN`.) Independently,
`chi_c(Y)_RH` is the compact-support Euler of a 4-sheeted cover
branched along a nodal curve of known degree and known local
monodromy (for `(9,6,2)`: transpositions generically, two disjoint
transpositions at each of four nodes). If `U ≅ A^2` and
`Y=U ⊔ B_Y`, then `chi_c(Y)=1+chi_c(B_Y)`.

For `(9,6,2)`, REP-96 already gives `sigma=2`, `a=2` (so `b=0`)
and ramification sum 2, i.e. one ramified dicritical of type
`(delta,e)=(1,2)` over `D_1`. That four-box split is a *floor*,
not a kill. The Euler identity is the extra equation. If
`1+chi_c(B_Y) != chi_c(Y)_RH`, the cover cannot be a Keller
SHEET-GATE `Y`. Two typed modes, never merged:

- Mode `D_1`-only: irreducible `A_F`. Under H2, nodal N=4 already
  dies by `(M')` (REP-96 §7 control). Mismatch is confirmatory.
  Match contradicts promoted H2+(M') and flags a bug in the Euler
  computation — type `OPEN[MODE1-MATCH-VS-H2]`, do not claim a CE.
- Mode `D_1 ∪ D_2`: needs a numerical type for `D_2`. Path 2 is
  the producer (`chi_2+sigma_2=1`, deg `>=2`, Lemma NL). This
  avenue does not construct `D_2`; Mode 2 is `OPEN` until Path 2
  emits a type. What it does unconditionally is Mode 1 plus the
  arithmetic any later `D_2` must satisfy.

If the compact RH formula cannot be typed, `OPEN` on the
compactification; do not fill by cap. Pole identities on the line
at infinity fire only after that vertex class is named. Colouring,
CABLE-3, BM-factorisation, and `(M')` do not compute `chi_c(Y)`.
Sources: SHEET-GATE Lemmas 2.2–2.3; REP-96 §1 and §7 R2; DET-LINF
Hodge+Grauert for any compactification tree this avenue names.

## 4. New connection between existing avenues

**The missing `(8,6,9)` input `c_2(T)=chi(O_Z)` and the missing
`(9,6,2)` Euler `chi_c(Y)_RH` are the same Chern-number species:
geography of a low-degree `S_3`/`S_4` cover of the plane, branched
along a curve of known weighted degree, with a typed non-Gorenstein
or nodal fibre.**

Rows involved: **#25** (fibre monodromy / Riemann existence),
**#31** (ZMT / integral closure `Y`), **#7** (`A_F` as object),
**#2** (dicritical tree / now DET-LINF), and the live residual
types of the N=4 campaign chain.

Licensed identification. TB-GERM type B (`(8,6,9)`, `beta_1=23`,
germ `A_22`, `m=4`) names `c_2(T)=chi(O_Z)` as the missing input
of a normal non-cyclic triple plane: `deg Delta-bar=10`,
`(deg S_pi, deg T_pi)=(8,1)`, one non-Gorenstein point
`C[z,w]/(z,w)^2` over `P_inf`, 10 `A_1` points
(`tb-g2-finish-opus5-20260901.md` §7.1). That is `S_3`-cover
geography; Ciliberto–Miranda arXiv:2512.07965v1 is the named
classification slot at weighted degree `<=10`, not a campaign
theorem (S3FR forbade consuming it as a repair). Card I's
`chi_c(Y)_RH` is the same numerical species on an *explicit*
degree-4 cover. Groups differ (`S_3` vs `S_4`); the Chern number
does not.

Consequences: one cycle-type→`chi` pipeline (§6) serves both
residuals and any later `(9,6,4)` curve. DET-LINF is the sign
constraint on whichever compactification tree is named; export
requires retyping ambient surface and Pic generators (matching
vertex names are not a map). Row 25's "Riemann existence is
generous" and row 31's "ZMT exposes the branch divisor" become
one Chern-number question.

Refuses: the two covers are not the same object. Mixing them
identifies a cover series with a place.

## 5. Strongest proof attack and strongest counterexample attack

### Strongest proof attack this week

**Euler/four-box test of SOURCE-IS-C2 on the banked `(9,6,2)`
braids, in parallel with consuming SAGE-NATIVE.**

SAGE-NATIVE is the cheapest representation discriminator and is
already running; do not relaunch. Zero plus reviewed scripts kills
this curve at representation level; SOURCE-IS-C2 remains the
architecture for `(8,6,9)` and `(9,6,4)`.

The strongest *new* desk computation: Card I Mode 1 on conjugacy-
invariant SIROCCO output (8 transposition tangencies, one
four-square node fibre, exponent 16) and the curve
`q=t^6+8t^2`, `p=t^9+12t^5+24t` (REP-96 §1). Steps: (i) check
four-box reproduces `a=2`, `sigma=2`, ramification sum 2, else
`OPEN` (bug); (ii) compactify the target as `P^2` with named `L`
and named `D_bar` (degree 9, `delta_inf=24`, `beta_1=25`) — do not
identify that place with a dicritical; (iii) write ramification
of a simple 4-sheeted cover plus node correction; if `L` is
untyped, `OPEN[RH-LINE-AT-INFINITY]`; (iv) compare `chi_c(Y)_RH`
to `1+chi_c(B_Y)`. Mismatch kills irreducible-`A_F` SOURCE-IS-C2;
match is a floor. Native-zero / Mode-1 mismatch / Mode-1 match /
untyped compactification are the four outcomes of §3.

Second-cheapest parallel: Card II (N-A sharpness). Third: A2
`3x3` at `e>=1`, already specified.

### Strongest counterexample attack this week

**Algebraize `q|_U` only after a Chern match. Do not interpolate
`(P,Q)` from the curve alone (that assumes irreducible `A_F` and
SOURCE-IS-C2). Do not steal Path 2's `D_2`. Do not run another
Witt level.**

If SAGE-NATIVE returns a class *and* Card I Mode 2 matches once
Path 2 emits a `D_2` type, one Padé step of two primitive
elements of `B=O(Y)` is licensed, and only then. Declare the
ring map; matching names prove nothing; if an ideal appears,
extract the component, assert the ring, run positive control
`p^2-q^3-64q=64t^2` and a non-branch negative control. If native
is zero, there is no CE on this curve: return the disproof budget
to K00 at `J^5 ⊂ I ⊂ J^2`, AWS if larger than desk. PARAM points
from 869/964 are curves, not maps.

## 6. Software acceleration or decisive experiment

**Fail-closed cycle-type geography calculator**, desk Python, no
Groebner, no SIROCCO rerun.

Input: either banked `(9,6,2)` SIROCCO JSON plus
`(d,n,delta_aff,delta_inf,beta_1, #nodes)`, or a Miranda type
`(deg Delta-bar, deg S_pi, deg T_pi, m, #A_1, fibre algebra)` as
TB-GERM §7.1 lists for `(8,6,9)`. Output: `(sigma, a, b,
ramification, chi_c(Y)_RH, chi_c(B_Y), match|mismatch|OPEN)` with
the compactification named, or typed `OPEN` (untyped `L`; Mode 2
lacks `D_2`; flag/place/series collision; `sat()` on a non-ideal).

Fail-closed: cycle types only (do not close
`OPEN[BMFACT-STRAND-VS-BLOCK]`); declare the target ring and the
function cutting out `D`; radical not `squarefree_part` (footgun
#10); no Sage `__main__` (footgun #11); positive control
`(9,6,2)` four-box `(a,sigma,ram)=(2,2,2)`; negative control
`(6,4)` at `E=-3` must not match; optional DET-LINF on a *named*
dual graph, never Domrina vertex names on a Sigray/Orevkov tree.

First run on `(9,6,2)` is the §5 proof attack. First run on the
`(8,6,9)` type either computes `c_2(T)` from the type or returns
`OPEN` (TB-GERM §6: curve-dependent), which tells that lane to
wait for a point. PARAM is the next library release, not this
experiment.

## 7. Campaign-systems check

**`UPGRADE`** — encoding-faithfulness preflight as a required
gate of every Groebner / msolve job.

Evidence: all six suite ideals were UNFAITHFUL (raw binomial vs
reduced approximate root); `(9,6,2)` was a false EMPTY. That is
the week's costliest silent-zero, larger than footguns #10/#11.
FALLACY-v2 already names remainder degree and `sat()` wrapping;
nothing yet *refuses* a `.ms` of raw `p^a-q^b` coefficients. The
msolve 0.10.1+qqideal oracle (P1 disagreement = ORACLE-P0) is the
attachment point. Seal-at-completion and no-mid-flight-patches
are already adopted; do not re-test.

Smallest upgrade. (1) Every nodal-type job emits the reduced
second approximate root beside the `.ms` (Tschirnhausen +
even-pole reduction for `(8,6)`; four AM coefficients for
`(9,6)`) and diffs it against the raw binomial; nonempty diff =
`UNFAITHFUL`, solver refused. Positive control: realized
`(9,6,2)` passes corrected, fails old. Negative control:
encoding-faithfulness r2 point A (`Delta=(8,6,19)`) satisfies the
old A equations and must be rejected. (2) Record remainder degree
in the declared quotient, branch on vanished leaders, handle zero;
emit no boolean emptiness (qqideal Certainty owns that).

Not the upgrade: do not retire M2 before the oracle reports; do
not block Card I on PARAM; do not change the promotion rule.
Clause 2 is new even if clause 1 is implicit in "corrected
systems": no frozen input requires remainder-degree as a solver
refusal.

## 8. Idea cards (at most three)

None of these is Path 1, Path 2, a colouring census, or a recycled
20260831T1033Z card. The named SAGE-NATIVE run is consumed, not a
card.

### Card I — Euler mismatch of the Riemann-existence cover

**Dependencies.** SHEET-GATE four-box table and Lemmas 2.2–2.3
(promoted `(M')` family); REP-96 §1 curve data and §7 R2 (reviewed
CABLE-3/class lists; BM-factorisation itself is *not* required for
cycle types); SIROCCO JSON/census already banked (8 tangencies +
node fibre). Does *not* depend on Path 1, Path 2, NA-R1, A2, or
Domrina packages. Mode 2 depends on a numerical type for `D_2`
(`chi_2+sigma_2=1`, deg `>=2`, Lemma NL) *if and when* Path 2
emits one; until then Mode 2 is `OPEN`.

**Cheapest discriminator.** Desk Euler vs four-box on the banked
`(9,6,2)` cycle types, compactification named, as in §5. No CAS.
No strand labels.

**Outcomes.** Four-box fails `(2,2,2)`: `OPEN` (bug). Mode-1
mismatch: irreducible SOURCE-IS-C2 dies (confirmatory of H2+(M')).
Mode-1 match: floor, type `OPEN[MODE1-MATCH-VS-H2]`, not a map.
Untyped `L`: `OPEN[RH-LINE-AT-INFINITY]`. Mode 2 mismatch kills
that companion type; match licenses the §5 Padé step only.

**Stop.** Every realized residual has a Mode-1 verdict; Mode 2
decided or waiting on Path 2. Stop immediately if cycle type
depends on the SIROCCO basepoint.

**Information.** First number on the largest geometric gap. An
`OPEN` on compactification is also the missing lemma of
`(8,6,9)`'s `c_2(T)`.

### Card II — N-A sharpness for irreducible nodal curves at equality

**Dependencies.** Theorem N-A as promoted-with-scope (strict
`C^2 > 2 r_1 + 4T + T_x`; sharpness datum is the `T_x` coefficient,
two bitangent conics). Fold-reduction identity `C̃^2 = d^2 - 2
delta_inf - M_inf` reproducing (M-INF). Residual rows `(9,6,4)`
(delta_aff=6, on the boundary), and downstairs `(6,4)`/`(8,4)` at
deficit 1. Does *not* depend on BM-factorisation, companions, or
Domrina. Does not consume the two promoted N-A countermodels
beyond their stated `T_x != 0` / reducible scope.

**Cheapest discriminator.** Literature-and-countermodel, desk.
Question: may `C^2 >= 2 r_1` replace the strict inequality when
`C` is irreducible, nodal, `T=T_x=0`, and the only non-nodal
point is the resolved place at infinity? Hunt a witness (an
irreducible nodal rational curve with `C^2 = 2 r_1` and
non-abelian kernel) or a sourced proof that none exists. Do not
relax N-A by analogy with the bitangent-conic sharpness. Primary
texts already in custody: Nori 3.27, the N-A packet, Orevkov 1990.

**Outcomes.** Sourced YES: `(9,6,4)` and downstairs `(6,4)`/`(8,4)`
die — highest-leverage N=4 kill that does not wait on Box03. Do
not export onto `(9,6,2)`. Sourced witness that strictness is
essential: OPEN closes negative; rows live. Untyped Nori
hypotheses at infinity: `OPEN`. Do not fill.

**Stop.** One YES, one witness, or a named missing hypothesis. Do
not iterate `OPEN[NA-AGGREGATE-REDUCIBLE]`.

**Information.** The one-unit question the packet already flags as
lethal to `(9,6,4)`. Independent of the flagships.

### Card III — Sound N=5 without an N=4 suite clone

**Dependencies.** Packet literature posture: Zoladek 2008 through
N=5 with GGV Lemma 4.10 gap (`I_2 ⊂ (1/m)Γ(f_2)` unsupported;
arXiv:1401.1784, custody `8b426751`); GGV gcd-of-total-degrees
`>=16`, `!=2p` (total degree, not geometric degree); Sigray thesis
Theorem 9.1 as the independent `td>=6` candidate (unrefereed,
`refs/sigray_full.pdf` in the Domrina-repair hash list); campaign
H2-kill of the N=5 trivial-dicritical profile (promoted); CAGE-N
recovery of N=5 as an instance, NO-DEG-CAP; DET-LINF as an
unconditional tree-sign lemma. Prime N ⇒ primitive monodromy
(row 26). Does *not* depend on `(9,6,2)` or Path 2. Does not
export Domrina R1/R2 vertex names.

**Cheapest discriminator.** Two desk pages, in order.

1. Retype DET-LINF at a Zoladek/Sigray infinity tree: declare the
   ambient compactification, the generator of Pic, and the subgraph
   that plays the role of `L̃∞`. Ask whether `det <= 0` is the
   missing inequality behind Lemma 4.10's lattice inclusion, or is
   a different object. If the ambient is not a projective rational
   surface with tree-of-`P^1`s at infinity, output `OPEN` (pole/
   interior, vertex class).
2. Hostile replay of Sigray 9.1 at geometric degree 5 only, against
   the official PDF, with a one-page dependency graph. Do not
   replay all of Sigray.

Do not launch an msolve suite at N=5 until one of these returns a
typed theorem or a typed gap. Primitive-group filter (the only
primitive subgroup of `S_5` containing a transposition is `S_5`;
`A_5`, `AGL(1,5)`, `D_{10}` contain none) is a free screen on
CAGE-N's N=5 profiles, not a third engine.

**Outcomes.** DET-LINF implies 4.10: literature N=5 repairs at
that lemma, still modulo the rest of Zoladek — not JC at degree 5.
DET-LINF orthogonal to 4.10: `OPEN` on 4.10, keep Sigray. Sigray
9.1 REPLAYED-SOUND: geometric degree 5 closed independently; H2
residual is reducible `A_F` (Path 1's N=5 instance). Sigray GAP:
census justified, primitive screen first, faithfulness preflight
mandatory. Primitive screen may kill individual CAGE-N classes.

**Stop.** First typed closure of geometric degree 5, or typed GAP
in both 4.10 and Sigray 9.1 plus a named first census profile. No
conductors. No export of `2m<=N-1` by copying rank-four brackets.

**Information.** Sound N=5 without a compute campaign, or a
precise missing lemma. Orthogonal to both flagships.

## 9. Lane verdicts: continue / redesign / stop

Lanes named in the packet's state deltas and launched flagships.
Older overlay names are treated as superseded when they conflict.

| Lane | Verdict | Ground |
|---|---|---|
| N=4 literature chain (Domrina II, R1/R2, DET-LINF, DO-I mu2) | **continue** as review-only | Foundation acceptable per packet calibration; mu2 Grok review in flight (`PROVISIONAL`). Do not staff a fourth replay. Packages stay consumed-not-rederived. |
| mu2 hostile review (in flight) | **continue** | Binding for the literature ledger line. This ideation is that seat's sibling, not a substitute. |
| N=4 campaign residual: `(8,6,9)` curve job 869 | **continue** | Realizability of type B. Independent of `c_2(T)`. Oracle window is the certainty layer. |
| `(8,6,9)` `c_2(T)` / triple-plane geography | **redesign** | Redesign as Card I's `S_3` mode / §6 calculator, not as a Groebner generator and not as an unlicensed Ciliberto–Miranda consumption. |
| `(8,6,11)` / `(8,6,7)` | **stop** | Twice-derived rep kills; bind repairs at integration. |
| `(8,6,3)` | **stop** | Curve-level KILL-BINDING; Moh and TB local already negative. |
| `(9,6,2)` SAGE-NATIVE + script review | **continue** | Cheapest representation discriminator; BLOCK-zero is a floor until this lands. |
| SOURCE-IS-C2 as "`Y ~ C^2`" recognition | **redesign** | Redesign as Card I (four-box + Euler of the ZMT surface). The slogan `Y ~ C^2` is false for SHEET-GATE `Y`. |
| MPRIME-COMPANION / Path 2 COMPANION-CURVE-ALLN | **continue** | Endorsed. This lane does not duplicate `chi_2+sigma_2=1` or the `(9,6,2)`-`D_2` job. Card I Mode 2 consumes Path 2's numerical type. |
| Path 1 MPRIME-ALLN-H2 | **continue** | Endorsed. Orthogonal to Cards I–III except as a consumer of N=5 reducible residue after Card III. |
| `(9,6,4)` six-node job 964 | **continue** | Locus nonempty. Parallel Card II may kill it first; that is a race, not a stop. |
| A2 horn `OPEN[A2-CELL-32]` | **continue** | Successor is the named `3x3` at `e>=1`. Do not reopen LND/Picard/ML. Do not construct a pullback. |
| N=5 census as an N=4-suite clone | **redesign** | Redesign as Card III. Pipeline stays warm; do not fire corrected-ideal jobs until faithfulness preflight exists and Card III's first discriminator has returned. |
| Primitive monodromy as a td-ceiling | **redesign** | Redesign as the free N=5 screen inside Card III (transpositions force `S_5`). The `d2=2` sandwich remains the all-degree client of row 26, not this week's flagship. |
| Degree-monotone invariant hunt | **stop** as a lane | No candidate; adjacent survivors kill naive monotonicity. Remainder/DET-LINF/geography replace it. |
| Colouring ladder / more even conductors | **stop** | Saturated at 28. |
| Reducible rank-four trees/splices/`n22` | **stop** | Closed by `m=1`. |
| K00 / formal-germ algebraization | **continue** at inherited priority | Orthogonal disproof. Strictly at the sharp filtration; no desk CAS. |
| Witt / AS109 / sparse-above-GGV | **continue** at low intensity | Untouched. Not this week's CE. |
| Encoding-faithfulness / qqideal oracle | **continue** plus the §7 upgrade | Oracle window is live; attach the preflight. |
| Heavy/uncertain CAS | **continue** the policy | AWS unconstrained by quota; still not a reason to skip the faithfulness gate. Desk work stays desk-scale. This ideation ran no CAS. |

If SAGE-NATIVE returns zero and is reviewed: stop the `(9,6,2)`
representation lane, keep Card I as architecture, and shift proof
budget onto Card II, `(8,6,9)` geography, A2 `3x3`, and Card III.
If it returns a survivor: raise Card I and the §5 algebraization
step immediately, still without duplicating Path 2.
