# Significant-news whole-portfolio state packet — `20260830T1015Z`

Snapshot UTC: `2026-08-30T10:15Z`  
Clean pushed basis: `4decd33d5caf8553bcbaa199b93a900ecabeee18`  
Objective: prove the plane Jacobian conjecture in characteristic zero or
construct and verify an explicit polynomial counterexample. A necessary
condition, abstract configuration, formal-family survivor, finite-jet kill,
or plausible proof outline is not victory.

## 0. Custody, authority, and blind boundary

Every invited researcher receives this same packet and works blind. Before
using a charged file, recompute its hash. The exact 46-avenue inventory,
scores, tried/untried state, and overlay history are in `APPROACHES.md`; read
the master table and enough history to test novelty, not only the newest
overlay. The detailed proof dependency arrows are in `ladder/REDUCTION.md`.

```text
90df4ac07e8b58fceeff3dd9c378cfd784b593e030c34bbaa76c31ee9ce39fe9  APPROACHES.md
ab1865c8b4b1022ec3dfb206ada07d2b698bb4a713437aa5e1d6a6e0420398e1  AUDIT.md
1c0c20fa4798f429cf00325fd19aa8605f0050a32f28e2885dedfb14a03510cc  COORDINATION.md
046dbe4d52ab5ed14e10b761e5a60d62c1bac26c2ef0ed97c10599a5146182fa  PROGRESS.md
7f901db6c8e6fbc80c85581a331c247d5c219e23bf286136f77fbc0182ebbe1b  ladder/REDUCTION.md
2dc015cf32e1b057f3848451fc37fd0acbec2a4e5b2784731ba2b02f9c3cbb86  notes.md
0db1bd7d6b8162780340532c75901a7721e6bd621f00981ee915b1114d2e06e1  xmodel/ideation-20260829T2254Z-synthesis.md
bb55f9a53274217d424eb42e3eda7730bf75efdfeeb64a48b0c6752a707f1f38  xmodel/websweep-20260830T0340Z.md
```

The newest packet delta below supersedes stale queue language in canonical
summaries; promoted claims remain controlled by `AUDIT.md`. The prior blind
and cross-pollinated `2254Z` synthesis is the novelty baseline. No broad web
sweep is due before `2026-08-31T03:40Z` absent external news.

This full round is triggered by a material proof-side funnel: a large fixed-
quadratic normal-singular cell has narrowed to two q=6 local rows, conditional
on the new q=8 review. Do not let that one route monopolize ideation. Scan all
proof, disproof, bypass, and systems avenues.

## 1. Significant-news delta

### 1.1 Fixed quadratic presentations have become a finite geometric funnel

Different-model review now binds all of the following in their literal
proper-block scopes:

- the smooth, irreducible, finite-near-reduced-squarefree-infinity quadratic
  stratum is empty;
- the nonnormal fixed-quadratic presentation stratum is empty;
- a normal singular reduced finite-near-infinity presentation is forced to
  the unique `F5=(0,1)+(1,1)+(1,1)` infinity germ, with all eight local
  intersection units at its triple/tangency point;
- its Du Val rows reduce to a finite balanced/unbalanced table, and the
  completion-invariant `A1`-ruling/Euler theorem gives `rho+k<=8`;
- the balanced local polar theorem kills `B7/A6`, `B8/A7`, and `B9/A8`.

The latest exact producers are

```text
6a7dc7bec108d867fe9215a85fdc09c2d2790db73d36c4123e96f7419f9511f3
  xmodel/bd-a2-singular-f5-unbalanced-local-polar-sol56-20260830.md
5a31a2ab48746828f08cc0b7e2d349b2c2dc773a2faa59fa23a51cb667bbaecc
  xmodel/bd-a2-q8-f5-global-lattice-elimination-b5-b6-u5-u6-sol56-20260830.md
```

The local packet proves provisionally

```text
U3/A3: 4+4,   m=(2,2,1),       n=2e1+e2;
U5/D5: 2+3+3, m=(2,4,5,3,3),   n=e2+e4+e5;
U6/D6: 3+5,   m=(2,4,5,6,3,3), n=e2+e4.
```

The global packet proves provisionally that every q=8 row
`B5/A4,B6/A5,U5/D5,U6/D6` has positive total pair intersection in the
relaxed `F2` nine-blowup lattice, contradicting the rational-forest
pairwise-disjointness gate. It covers baseline, all Euler-allowed contracted
`Z` carriers, and all affine-`A1` roots, including the sole coefficient-two
edge case. Combined with the binding higher balanced deaths, it conditionally
empties the entire singular-F5 q=8 table.

A genuinely different GPT-5.5 xhigh hostile review is active on immutable
basis `4decd33d...`. Treat the result as provisional for cheap reversible
work. Promotion, further fanout, and any general quadratic closure must wait.

If review succeeds, the reduced finite normal-singular F5 cell has exactly
two local rows left:

```text
B3/A2: q=6; generic strict partition 4+2+2, with special modulus cells;
U3/A3: q=6; strict partition 4+4 and the marking above.
```

The highest-value immediate geometric discriminator is therefore a complete
q=6 global lattice/forest/Euler analysis, including all affine ADE and
contracted-carrier allocations. Seek a conceptual obstruction as well as a
finite replay. Also seek a route that bypasses fixed quadratic presentation
or proves an intrinsic quadratic-basis/degree ceiling. A closure of this
funnel is not JC2 unless the presentation/selector arrows are supplied.

### 1.2 The complete-base shortcut was corrected, not proved

Nonvacuous cyclic `A1` spaces over `P1` with first-leg degree pairs `(2,2)`
and `(2,4)` exist, so the former idea that `d1>=2` alone rules out complete
base is false. The reverse-etale *full sandwich* is instead excluded by the
canonical class of the actual first-leg surface. Do not reuse the degree-only
shortcut. A bounded-adaptation theorem and intrinsic presentation control
remain open.

### 1.3 Counterexample/K00 front remains exact but finite-jet only

The current progress ledger includes reviewed closure of the normalized
`e=2,m=2,h10=1` cell through G13 and many literal integer and ramified
calendar cells. Those are field-point/finite-jet exclusions, not arcs, source
coverage, attainment, algebraization, or maps. The global counterexample
gaps remain source completeness, compatible all-order lifting, occurrence,
reachability, and algebraization. Reassess whether further row extension has
higher information gain than an image/transition theorem or a falsification
of source completeness. Do not extrapolate one load calendar to all
ramification indices.

### 1.4 Global proof architecture is still selector-limited

The broader proof route still lacks a reviewed arrow from an arbitrary
minimal Keller counterexample to one bounded boundary/block client. QCS,
`G2-PSC`, and `G2-BD` retain their exact separate scopes; never write bare
`G2`. The fixed-quadratic funnel is a powerful client, not the global
selector. Look for new compositions between intrinsic discriminant/index,
unit/forest topology, valuation/degree reductions, monodromy, and minimal-
counterexample structure. Equally, attack the assumption that a useful
quadratic trace-zero frame exists.

### 1.5 External and resource state

The `20260830T0340Z` web sweep found no external characteristic-zero plane-JC
resolution. It triaged one missed August-25 public frontier note, which makes
no exclusion or map claim. No new Matysiak version or repair exists.

Fable 5 and Opus 5 remain equal-standing whole-portfolio researchers but
their recent calls hit the Anthropic account spend ceiling before model work.
Grok's balance is exhausted. Invite all three; classify a repeated refusal as
`UNAVAILABLE_NO_MODEL_WORK`, never as a mathematical response. GPT-5.5 is
working as the q=8 reviewer and is omitted from blind ideation to preserve
independence. AWS capacity is idle and available up to the user's 512-vCPU
quota; all heavy or uncertain CAS is AWS-only. Current q=6 and lattice tests
are desk-scale unless a registered elimination grows materially.

The user-owned `jc2-lean` formalization is independent. Never inspect, list,
search, stat, build, modify, or control it.

### 1.6 Campaign machinery learned two small but general lessons

Transactional report finalization is active. A completed unbalanced replay
then exposed a distinct evidence flaw: Python `assert` checks vanish under
`-O/-OO`. Its immutable corrigendum installs an explicit-check replay with a
mutation that fails in all modes. Future optimized-mode claims require a
zero-`Assert` AST gate (or proof remaining asserts are nongating) plus an
old-pass/new-fail control in every claimed mode.

Separately, `charge_basis_status=ABSENT` is the correct receipt for a report
that asserts no new exit price. Never add a dummy `charge_basis` marker for
ordinary hashes, geometry, or consumption of a promoted price.

## 2. Required independent whole-portfolio analysis

Think about the entire proof, disproof, mathematical-software, and campaign-
systems portfolio. Do not optimize only the avenue you previously owned.
Victory may come from a new avenue, an untried connection, a bypass of the
current architecture, or a decisive falsification of a protected ansatz.

Your report must include:

1. a compact `unchanged/raise/lower/reopen` disposition over all 46 numbered
   avenues, explaining every change;
2. a reranked list of global proof gaps and counterexample/falsification gaps;
3. at least one genuinely new avenue or mechanism and one new connection
   between existing avenues, each checked against repository history;
4. the strongest prove/falsify/bypass portfolio for the global selector,
   QCS, `G2-PSC`, and `G2-BD`;
5. an exact q=6 attack or a precise explanation of why the q=8 mechanism
   fails there, alongside at least one non-quadratic alternative;
6. the strongest next K00 attack, explicitly comparing more truncation rows
   with source-completeness, transition-image, all-order lifting,
   reachability, and algebraization work;
7. one mathematical-software acceleration or decisive experiment;
8. a separate campaign-systems `UPGRADE` card with the smallest measurable
   acceptance test, or `NO_CHANGE` with evidence;
9. at most three detailed research cards and one ranked launch portfolio.

Each card must state novelty (`NEW`, `KNOWN`, `DUPLICATE`, or
`SCOPE-CONFLICT`), dependencies, exact theorem or experiment, cheapest
discriminator, meanings of every outcome, stop condition, expected
information gain, compute class, and parallel prove/falsify/bypass ownership.
Identify provisional descendants that can start without review; promotion
still requires different-model hostile review.

Search history before claiming novelty. Preserve arrow directions and
quantifiers. Keep B and S separate. Formal data are not maps; a floor or
survivor is not attainment; equality of sets is not equality of schemes. A
finite jet is not a formal arc, algebraic germ, polynomial map,
counterexample, or JC2 result.

## 3. Tool and output boundary

No web or external messages. Never touch `jc2-lean` in any way. Desk-scale
exact algebra is allowed only under 60 CPU seconds and 1 GiB RSS per process.
Heavy or uncertain CAS is AWS-only and must be preregistered rather than
launched during this blind report.

Do not edit canonical files, commit, push, or read another blind submission,
prompt, log, or run record from this round. Write only the assigned report
plus scratch under `/tmp`. This round makes no new exit-price assertion; omit
`charge_basis={...}` and expect `ABSENT`. End with a standalone
`<!-- BODY-END -->`; do not add a seal. Fail closed.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `10989`.
- Body SHA-256:
  `11e02fbb3366bbd42a1f1180f809edb632eafce2972c876fffc8b203109f84e2`.
- Frozen basis: `4decd33d5caf8553bcbaa199b93a900ecabeee18`.
