# Promotion: exact `J1=0` navigation for the frozen 22-row prefix

Date: 2026-08-27

Status: **PROMOTED EXACT PREFIX THEOREM WITH A SCOPE REPAIR.**

## Evidence

```text
f9c8aab86af0f6c0ffd5c247a75b447bacb8723e172eec440806fd843c978e4b
  xmodel/max12-812-order2-p0-total-rees-j1-zero-prefix-producer-sol-20260827.md

bedb8dfe0a451fee8082bbd61f5134f1dc43fd9e20e7b23d485f475bfde31a03
  xmodel/max12-812-order2-p0-total-rees-j1-zero-prefix-hostile-review-grok-20260827.md
```

The reviewer independently parsed the 22 raw polynomial files with exact
rational arithmetic and did not execute or trust the producer replay.

## Theorem

Let `I22` be generated by the exact-Q V9 rows `Tg10_1..Tg12_7` and the V17
row `Tg14_5`.  After setting

```text
J1=(rs,cs,c0,c1)=0,
```

all seven grade-10 rows vanish.  So do `Tg11_4`, `Tg11_6`, and `Tg12_6`.
Exactly twelve rows survive, first at grade 11:

```text
Tg11_1,Tg11_2,Tg11_3,Tg11_5,Tg11_7,
Tg12_1,Tg12_2,Tg12_3,Tg12_4,Tg12_5,Tg12_7,Tg14_5.
```

Every survivor except `Tg12_4` has both `a0`- and `a1`-support, while

```text
Tg12_4|J1 = (3/32)*(e0^2+rho^2*e1^2),
Tg12_2|only e1 retained = (3/32)*e1^2.
```

The two `Q[rho]` maps `A00` and `A10`, sending respectively `a0` or `a1`
to `1` and every other source name except `rho` to `0`, kill all 22 rows.
Therefore

```text
a0 notin radical(I22+J1),   a1 notin radical(I22+J1).
```

The standard `a0` chart and ordered `a1` chart of this prefix are nonempty.
Separately, the origin/`rho`-line, sending `a0=a1=0` and all other names
except `rho` to zero, kills all 22 rows and witnesses nonemptiness of the
terminal receiver of this prefix.

## Scope repair

The producer correctly proved the two unit-point statements, but attributed
terminal nonemptiness to `A00/A10`.  That implication is invalid because
those points do not lie on `V(a0,a1)`.  The terminal conclusion remains true
for these bytes for the independent origin/`rho`-line reason above.  This
promotion supersedes only the faulty attribution, not the census or result.

Consequently, an emptiness search for either standard `J2` chart or the
terminal receiver using only these 22 rows is deciding a false statement.
This does not prohibit studying their nonzero relations.

## Scope firewall

This is a theorem about a frozen exported prefix, not a fully compiled Rees
chart.  It says nothing about grade 13, the other grade-14 rows, higher rows,
unwritten stage-two equations, additional localizers, the full source, Gate
T, order two, maximum twelve, or JC2.  New rows may kill any displayed
section.  The registered successor is therefore source export before any
stage-two emptiness computation.

