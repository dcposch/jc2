# One-place r3 rank runtime and one inverse-entry control

STATIC IMPLEMENTATION CANDIDATE / UNREADY FOR EXECUTION; no run, place or baseline supplied.
Owner /root/contact_collision_geometry. First2026-09-11 02:02:10 UTC;
exact report, manifest and box absent. Original reserve02:28/HARD02:31 UTC,
never reset. Basis0d39df3c9fd69c939a8420c54d03228b9077777d.

All16 inputs freshly pinned before bodies and freshly WHOLE-read. No reuse.
Initial combined gate output clipped; both gates were recovered in full bounded
ranges. No clipped scope counted. Four rank source files and CAPRUN unchanged.

## Scope and outcome

Disabled prospective dispatcher/probe/independent inverse-entry mutator and
ROOT registration contract. No scientific or dummy source execution, import,
AST, syntax, compilation, test, coefficient/candidate/fixture/baseline body,
network/AWS, process inventory, protected/shared work, agents or follow-on.

The owned box supplies dispatch.py, probe.py, mutate.py, CONTRACT.md,
DISABLED-REGISTRATION.json and READSCOPE.json. The disabled template is expressly
not a runnable registration; ROOT must supply a separate complete actual policy.
No source foundation, fieldness, rank, source point, exclusion or JC2 result is
claimed. Accepted rank22z and prior runtime21z are scoped static parents only.

## Specific interface change

Rank authority binds the actual finder/checker script path and fourteen-entry
live argv. Therefore a proxy probe or mutator calling that authorize function
would fail or require forbidden impersonation. ROOT expressly approved this
adaptation: five refusals run the actual finder; two wrong-tag refusals run actual
finder/checker; the valid sentinel and descendant dummy prove ROOT policy only,
not successful rank authorize or mathematical startup. The independently
ROOT-policy-bound mutator never calls rank authorize or imports science.
This is a new contract point for FIRST, not accepted runtime evidence.

Nine preflights precede one finder, one positive checker, one inverse-entry
mutator and one negative checker. Exact early authority stacks and labels are
bound to the pinned source. Actual positive finder/checker receipts are matched
in full. A fresh ROOT positive-phase record binds the frozen certificate, source
authority, positive receipt, prior baseline qualification, place, native and
literal mutation policy. No baseline reconstruction/check rerun is included.

The mutator changes just inverse[0][0][0] by1 modulo ROOT p, checks canonical
whole-byte restoration under the inverse change, and emits a single unchecked
fixture. A fresh checker must exit2 normally with empty stdout, exact stderr
`CHECK FAILED: full inverse identity\n`, and no receipt. Authority/parser/hash/
place/resource failures do not pass the control. For an invertible selected
matrix, its first column is nonzero; adding the first matrix unit to the inverse
changes the product by a nonzero matrix. No field oracle is borrowed to perform
the mutation. No such artifact or execution exists in this packet.

## Boundary, caps and trust

Frozen source authority's ROOT_AUTHORIZATION_SHA256 hole remains unique at
index11. Runtime permits only the two fresh checker authorization-SHA slots and
one mutator ROOT-registration-SHA slot, each at its unique named argument; all
other argv bytes remain literal. Full six-field native closure is independent
of the smaller job_tag/files source inventory. ROOT's previous complete-source
qualification requires genuine accepted execution evidence, not self-attested
JSON. Source and wrapper directories retain separate exact readonly inventories.

Joint600wall/550CPU/8GiB is unchanged and UNMEASURED. Nine preflights have5wall/
3softCPU each; finder180/170, each checker150/130, mutator15/9. These sum540wall
and479 hardCPU including CAPRUN's per-child+1: nominal60wall/71CPU remains for
metadata, cleanup and custody. Cgroup CPU sampling at520 is not a kernel hard
aggregate CPU limit. All original admission/math/task/worker clocks still bind.

Baseline<=128MiB is EBS input, not tmpfs. Certificate+fixture<=32MiB and all
static/runtime metadata<=8MiB give168MiB logical input/output data; transient
durable-copy duplicate<=40MiB gives208MiB, excluding fixed OS/native/code bytes.
Working tmpfs capacity<=128MiB; source receipt<=32768B. No performance fit claim.
CAPRUN is unchanged; ROOT retains terminal cleanup, recovery and worker-stop
responsibility. A missed mandatory live process capture stops without retry.

## OPEN / cheapest test / collision

OPEN quantity: feasibility of the actual frozen one-place297x1453 finder and
two full88209-position identity checks inside the fixed joint envelope, plus
successful physical/native/startup/quiet custody. No value is measured here.
Cheapest permitted next discriminator is FIRST static review of this exact
disabled packet, especially entry adaptation, receipt schema, typed slots and
aggregate budget; planning wall15minutes, not authority or an observed duration.
Only after separate ROOT authorization could the nine5-second preflights precede
the one bounded batch; no command here authorizes those runs or another lane.

Changed-hypothesis control: a source-hash or place refusal on the changed payload
does not establish the full inverse-identity negative. A candidate-authored
qualification or a probe masquerading as checker does not establish authority.

Own-only collision check: report/manifest/box were absent at02:02:10; no existing
or shared bytes overwritten. All authored changes use apply_patch. One large
mechanical dispatcher replacement exceeded the intended small-write convention;
this documentary deviation is disclosed, not counted as execution or a proof.
Final current input pins, all-owned WHOLE review, transaction and custody bind
the actual published bytes. No provider/terminal or execution observation is
invented. Terminal custody is excluded from its own hash list by construction.

Whole current owned source/documentary text reviewed through EOF during
02:17–02:24 UTC; all16 current input postpins match. OPEN and own-only collision
checks completed; no scientific validation was executed. This is a complete
STATIC candidate, still UNREADY pending FIRST and separate actual ROOT policy.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6283`.
- Body SHA-256:
  `a209a78806fb967498bcd82dc1e479308ca2afcd0e4ce0ba2b83475832127e8f`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
