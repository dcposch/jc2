# Opus 5 hostile review — GATE-REC theorem R8

You are Opus 5 acting as an independent hostile mathematical reviewer in the plane Jacobian-conjecture campaign.

Work only in `/Users/dc/code/math/jc2`. Read the following report completely:

- `xmodel/ggv-quarter-root-sector-gate-rec-theorem-r8-sol-ultra-20260827.md`

You may read only the exact campaign files explicitly cited by that report when needed to resolve a claim. Do not glob or traverse the repository; do not inspect `jc2-lean`; do not read any `ideation-20260827T2255Z*` or `ideation-20260827T2259Z*` file; and do not edit any canonical file (`APPROACHES.md`, `AUDIT.md`, `COORDINATION.md`, `PROGRESS.md`, `notes.md`). No heavy computation may run locally.

Hostile-check, with explicit derivations where possible:

1. the sector shift `b -> b+22`, gauge normalization, and distinction between the literal `sum H^k q_(b+4k) z^k` and the actual transported class series;
2. algebraicity/D-finiteness of four-sections and whether reduction-based creative telescoping really proves every fixed-receiver coordinate is D-finite/P-recursive at fixed `H,F`;
3. the quoted order bound `R <= n deg_X(d_Hermite) + dim N_V`, including whether its hypotheses and variables have been transferred correctly;
4. the finite-prefix conclusion and all singular-index caveats;
5. the generic parameter-cell claim and the refusal of a uniform numerical `N0` without specialization control;
6. every claimed scope/firewall, especially that no row independence, nonlinear nonvacuity, descent, endpoint theorem, or JC2 consequence follows.

Classify each major assertion as CONFIRMED, REPAIRED, GAP, or REFUTED. Distinguish theorem from algorithmic recipe and pointwise from family-uniform statements. The producer is Sol Ultra, so your report is a different-model promotion leg only if it is genuinely independent and survives.

Write exactly one output file:

- `xmodel/ggv-quarter-root-sector-gate-rec-theorem-r8-crossreview-opus5-20260827.md`

Include the producer SHA-256, hashes of every additional local file actually read, a reproducible desk-check section, a concise promotion recommendation, and your own output SHA-256 on the final line. Do not write anywhere else.
