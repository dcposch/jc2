# Hostile different-model review — TD6 moduli uniformity

| Field | Value |
|---|---|
| Claim under review | Frozen TD6-MODULI-UNIFORMITY-GATE: the numerical next-row obstruction extends to the centering-independent formula `[s^{-1}t^{13}]J=(6/5)(5E_2-2E_1^2)` on the retained SP-2 x-boundary/F1 pattern; the complex hypersurface `5E_2-2E_1^2=0` is a genuine paired-next-row survivor of affine dimension `56` on a licensed normalized slice, not an SP-2 kill |
| Overall verdict | **CONFIRMED** |
| Smallest failing identity | none |
| Hidden local-to-global step | none: one exact coefficient of one Jacobian row, for one retained chart-pattern normalization, is uniform in common centering and in pole/dead-stretch data that never enter that coefficient; a genuine complex finite-band survivor on a specialized slice is not a Keller pair, a terminal-class realization or kill, a landing theorem, or JC2 |
| Evidence tier | independent exact algebra over `Q` and `C` (registered replay; a second reverse-lex least-index GE; unreduced reconstruction of pole rows (19) as 94-vector identities; hand two-form / F1-line support / first-row divisibility / quadratic-form minors / algebraic-survivor arithmetic); primary-source read of the completed first-band and numerical-next-row hostile reviews and the charged SP-2/F1/r9-M2 ledger text |
| Reviewer / model | Grok 4.6 (xAI). Different model family from the producer |
| Repo | `/Users/dc/code/math/jc2` |
| Git HEAD | `51aa1cc210b20d6c57c5bb0ba3b4a6dfa5fc8f54` (matches the charged basis) |
| Review window (UTC) | 2026-08-24T10:48:00Z – 2026-08-24T11:16:25Z |
| Python | 3.14.6; stdlib `fractions.Fraction` / `math` / `hashlib` only |
| Host | Darwin arm64 |

Producer inputs reread in full before any verdict:

- `xmodel/td6-moduli-uniformity-gate-20260824.md` (SHA-256 `499e95763759fe195ab1eba5fbf97bfb9d255bbf6fb2b090add938d92f1f3a06`)
- `cases/td6_moduli_uniformity_20260824/replay.py` (SHA-256 `55340fa662a20e1777eaa18fae2d26589c11ec7f3f2960f4a0a6efe015e7b6ab`)
- `cases/td6_moduli_uniformity_20260824/FREEZE.sha256` (SHA-256 `8d28ce7bc0ca31b51887457e9f5e33537f0ec86d950a6e0e21c78813a78994af`)

All three frozen hashes match the launch prompt. The freeze file body is exactly the two producer hashes above. The committed basis is exactly `51aa1cc210b20d6c57c5bb0ba3b4a6dfa5fc8f54`. No producer, canonical, ladder, or PDF file was edited. No successor Jacobian band was appended. No AWS work was launched.

Confirmed first-band and numerical-next-row producers/reviews, reread against that basis:

- `xmodel/td6-two-chart-first-band-20260824.md` SHA-256 `cb373892233bddaf1b8fbf7722335ca43ee366b337151c3b244d2604d8168bf2`
- `cases/td6_two_chart_first_band_20260824/replay.py` SHA-256 `c55e213672ebd22cf3e9e8f378ffa857f85c3a54be7ed39e5c956df36ff6e735`
- `cases/td6_two_chart_first_band_20260824/FREEZE.sha256` SHA-256 `995a0ba55a20b9aa59626654614fe54a5f8beb95742a5376ddf006eef3fe3d49`
- `xmodel/td6-two-chart-first-band-review-grok-20260824.md` SHA-256 `265c1103391e2bae889c018bb0637fe486e7292644c0295e4f9965e5085e7a25` (overall **CONFIRMED**; all six subclaims **CONFIRMED**)
- `xmodel/td6-two-chart-next-row-20260824.md` SHA-256 `32124d20ec84ef59d5b116639176b12053f5da6de1a9458dd4a2095d6b1618f0`
- `cases/td6_two_chart_next_row_20260824/replay.py` SHA-256 `0fc299a18a7ace6e5f64f98f38a71775331f226baac2e8ad258dd40e744fa5b8`
- `cases/td6_two_chart_next_row_20260824/FREEZE.sha256` SHA-256 `9ab95652b387f84044651a1a3a01e7a8371e3579e19530f29f34bdc5b7d78f60`
- `xmodel/td6-two-chart-next-row-review-grok-20260824.md` SHA-256 `12834c356c2c26243522b2609753370118ecd2c8ed87907033ca27e926d2ac2f` (overall **CONFIRMED**; all six subclaims **CONFIRMED**)

Cited canonical SP-2 / F1 / r9-M2 material, working-tree hashes on disk at review time:

- `ladder/SHEET6.md` SHA-256 `37f90bc8aff1ad977486cb5f414b88f983d4e7d46d771bd5ef34649085120f4d`
- `ladder/SHEET6-CAMPAIGN.md` SHA-256 `fca34f4cad606c9097cdb87c4e9e51609ab5c73d16a03efd764a7c06cdc4ccbb`
- `ladder/SHEET6-LROOT.md` SHA-256 `d8dc2eed9d386a74c76075fe265abee3ffae5b5c700341aa5134f5592fa0a358` (SP-2 ledger 175, 187–200; remaining two-chart surface 256–282)
- `ladder/SHEET6-LT-REVIEW.md` SHA-256 `82d94e1a43ae9ec315189b4892faafa840027f4f1696dba6a394866331aa22c6` (LR2 re-derivation 66–73)
- `ladder/SHEET6-AF3.md` SHA-256 `555363fde61291a0c689bbf9d789c01f73734e0c403d350d85ae2fd0ddab8099` (§4 entry family, displayed ODE)
- `ladder/SHEET6-AF2.md` SHA-256 `c7f7601d5b72e2512561d12d160ac8e7458ee3954a8c18182b41ef6174053b3c` (IIa pattern `(η^ν−c^ν)^μ Π_k`)
- `ladder/SHEET6-A3L1-REVIEW.md` SHA-256 `6c045f36686e5dd73c6f067597d8a01857094a1add06288b955ef0e06b6aa44b`

`SHEET6.md` and `SHEET6-LROOT.md` differ from the first-band freeze hashes because of uncommitted campaign-status notes recording the numerical next-row death. The combinatorial SP-2/IIa/r9-M2 typing used below is the same ledger text at 187–200.

**Promotion.** Accept as `GENERIC-OBSTRUCTION / COMPLEX-SURVIVOR / STOP` of this retained SP-2 chart-pattern normalization at the first paired next Jacobian rows. Bank formula (1), real negative-definiteness, the source-open complex graph (12), joint rank `3508/3602` with dependent-row identities (17), next-row dimension `56` on the normalized slice, and the explicit algebraic point (21). Promote nothing else.

**Quarantine.** SP-2 is not killed. No td=6 terminal class is killed. JC2 is untouched. Formula (1) does not quantify broader x-boundary polynomials, other F1 patterns, other terminal classes, landing, or exact `J=1`. The 56-space is finite-band nonemptiness, not a Keller pair.

---

## Headline and subclaim table

Write `s=y^{-1}`, `x=c_1 s+c_2 s^2+c_3 s^3+t s^4`, F1 chart `x=q^{-5}`, `y=η q`, and on the survivor slice the pole chart `x=r^{-25}`, `y=r^5+ζ r^{17}`. The reduced case-II(a) F1 polynomial is `R(z)=(z-C)^2(z-U)(z-V)` with `z=η^5`.

| # | Exact subclaim | Verdict | What would have flipped it |
|---|---|---|---|
| 1 | Rectangles `(15,60)/(25,100)`, x-boundary `t^{15}` / `t+t^{25}`, and IIa reduced F1 `R(z)=(z-C)^2(z-U)(z-V)` are the retained licensed SP-2/r9-M2 control; formula (1) quantifies common centering and pole/dead-stretch data that never enter the coefficient, while the 56-space existence certificate specializes center `(1,1,1)`, zero dead stretch, and `C=1` | **CONFIRMED** | a printed LR2 clause forbidding nonzero common centering; IIa reduced pattern required to be a different orbit triple; producer claiming (1) for a general x-direction polynomial or for the other seven td=6 classes; producer treating the 56-space as quantifying general centering |
| 2 | The three extractions (7) are the unique F1-line monomials for those jets; every `c1,c2,c3` contribution lies on a strictly more polar forbidden diagonal; first row forces `[t^0]g1=-E1/5`; then `[s^{-1}t^{13}]J=(6/5)(5E2-2E1^2)`, recovering `-18858/3125` at `C=1,U=2,V=28/25` | **CONFIRMED** | an on-line centering contributor to `[s t^{14}]f`, `[s t^{24}]g`, or `[s^2 t^{13}]f`; a surviving `t^{<14}` term in `f1` after the first row; a sign error in `-14(-3E1)(-E1/5)+2(3E2+3E1^2)`; frozen numerical mismatch |
| 3 | Quadratic (10) is negative definite over `R` (minors `-3,5,-25/4`); only real zero is the excluded origin; the `C=1` graph (12) minus the three finite divisors (14) is a genuine nonempty complex source-open set, licensed by JC2/`C`-coefficients, not a formal zero of one slot | **CONFIRMED** | a positive eigenvalue of (10); a ledger clause forcing real orbit values; (14) exhausting (12) over `C`; the paired next rows being incompatible on (12) |
| 4 | On the normalized slice the first-band matrix is the fixed `Q`-matrix of rank `3508/3602`; symbolic RHS propagation yields exactly the two pole-scale identities (17), both automatic under `L=25(1-S+D)`; no parameter pivot denominator or rank jump; pole ODE leftover `(25/9)L^8 A^3` and normalization `L^8 A^3=9` | **CONFIRMED** | reverse-lex rank ≠ 3508; a third nonzero symbolic dependent row; a pivot lead involving `S,D,L,A`; ODE leftover not `(25/9)L^8 A^3`; (17) failing to force `L=25(1-S+D)` jointly over `C` |
| 5 | All 40 x-slots: degrees `37,38,39` identities, degree `13` the only affine compatibility, homogeneous x rank `36`; pole rows (19) add rank exactly two modulo x when `LA≠0`, with no extra affine condition; dimension `94-36-2=56`. No copied `f`/`g` blocks; no source-open rank drop that changes the conclusion | **CONFIRMED** | a fourth zero-homogeneous x-degree with nonzero constant; x tangent rank ≠ 36; (19) failing as a 94-vector identity; pole rank ≠ 2 at some `LA≠0`; `g` rows writing into `f` indices |
| 6 | Point (21) `10S^2-35S+37=0`, `D=S-22/25`, `L=3`, `A=1/9` is source-typed (`U,V` nonzero, distinct, not `1`), kills the obstruction, and satisfies `L^8 A^3=9`; it certifies finite-band nonemptiness only, not a Keller pair | **CONFIRMED** | `S=22/25` or `S=-9/25` a root of the quadratic; `1-S+D=0`; `3^8(1/9)^3≠9`; producer calling (21) a polynomial automorphism or a terminal realization |
| 7 | Scope is the retained normalized x-boundary/F1 pattern. The formula neither kills SP-2 nor quantifies broader boundary patterns, other terminal classes, landing, or JC2. Smallest valid next band is the paired Jacobian condition on the 56-space, or a global exclusion of (12) | **CONFIRMED** | producer promoting a class kill, a uniform-in-all-boundary-data theorem, exact `J=1`, or further generic samples off (12) as if they still carried information |

All remarks below are non-blocking unless marked otherwise. None changes a rank, a jet, a certificate coefficient, a hash, or a verdict.

---

## Replay

Registered command, rerun unmodified from the charged tree:

```sh
python3 cases/td6_moduli_uniformity_20260824/replay.py
```

Exit code 0. Printed verdict:

```text
TD6-MODULI-UNIFORMITY-GATE: PASS
verdict = GENERIC-OBSTRUCTION / COMPLEX-SURVIVOR
center_dependence = none_in_obstruction
general_formula = (6/5)*(5*E2-2*E1^2)
E1 = 2*C+U+V; E2 = C^2+2*C*(U+V)+U*V
normalized_formula = (6/5)*(-2*S^2+2*S+5*D-3); S=U+V D=U*V
symbolic_transport = rows 6547; rank 3508 / 3602; affine_dimension 94
transport_compatibility = L^3+[25*(S-D-1)]^3 = 0
transport_compatibility = L^5+[25*(S-D-1)]^5 = 0
source_relation = L=25*(1-S+D) (both automatic)
pole_J_leading = L^8*A^3/9; normalization L^8*A^3=9
next_x_on_zero_locus = rank 36 / 94; affine_dimension_after_x 58
next_pole_mod_x = rank 2 when L*A!=0; paired_affine_dimension 56
quotient_basis_pivots = 10,25,38
explicit_survivor = C=1; 10*S^2-35*S+37=0; D=S-22/25; L=3; A=1/9
symbolic_certificate.sha256 = 45cdf5d42496d3807c006cf27387cea4f91b3c3bc18155d7a0812e1d82c130f0
modular_samples = none
SP2_killed = false
JC2_resolved = false
```

Canonical stdout SHA-256, including its final newline, recomputed independently from the live bytes:

`596a3f54276d4b95ec241933a6c7ec1cf1d782e8b7d40376532e172e282da5ea`.

This matches the producer report. Arithmetic is `fractions.Fraction` on sparse monomials in `(S,D,L,A)` and on the fixed rational first-band matrix. No CAS, no floating point, no modular samples, no AWS.

The moduli replay imports the next-row compiler only after asserting SHA-256 `0fc299a18a7ace6e5f64f98f38a71775331f226baac2e8ad258dd40e744fa5b8`, which itself pins the first-band compiler at `c55e213672ebd22cf3e9e8f378ffa857f85c3a54be7ed39e5c956df36ff6e735`.

A second engine, written for this review and not imported from the moduli `main()`, rebuilt the 6547-row system from the hash-pinned next-row compiler and then diverged on purpose: (i) reverse-lexicographic row order in least-index sparse GE; (ii) independent support enumeration of every centering contributor to the three jets in (7); (iii) independent multivariate extraction of `[z^{11}]R^3`, `[z^{19}]R^5`, `[z^{10}]R^3` and the Jacobian arithmetic for (1); (iv) unreduced reconstruction of the two pole-J rows as 94-vector identities (19); (v) pole rank at four values of `(L,A)`, including `A<0`. Insertion-order rank, reverse-lex rank, frozen jets, formula (1), (17), (19), and the dimension count all matched. No assertion failed.

---

## 1. Source typing, rectangles, and which moduli are quantified

SP-2 from `SHEET6-LROOT.md:175,187–200`: chain

```text
F0 = (3,6,5,2,8) → F1 = (15,60,5,4,4)  (μ=2 IIa k=2, n=12) → (0,y),
```

type `(3,5)`, rectangles `(15,60)/(25,100)`, `R=π_G=4`, slack 0, one x-cluster. Reduced F1 pattern `(η^5-c^5)^2 Π_2`. IIa dictionary (`SHEET6-AF2.md:127`) is `p=(η^ν-c^ν)^μ Π_k` with `k≥1`, here `ν=5`, `μ=2`, `k=2`. The three fifth-power orbit values are the chain orbit `C` (squared) and two extra orbits `U,V`, required nonzero and pairwise distinct. Writing `R(z)=(z-C)^2(z-U)(z-V)` and `q^{15}f=R(η^5)^3+O(q)` is exactly that reduced polynomial in `z=η^5`.

LR2 licenses a common unramified x-truncation of length 3, not the numerical values `(c1,c2,c3)`. The parent centering-escape already showed that `T=xy^4-y^3` is legal; the present chart (3) is the same truncation with arbitrary common integral-power coefficients. The producer retains the normalized boundary polynomials (4), and says so.

AF3 §4 supplies the r9/M2 family `p=ζ(ζ^5-A)`, `q0=B(ζ^{10}-(5/3)A ζ^5+(5/9)A^2)` with `AB≠0` and leftover `(25/9)A^3 B`. On the survivor slice the producer transports the F1 Taylor factor `L=25(1-U)(1-V)` into `B=L^5` and normalizes `L^8 A^3=9`. That is a point of the cited one-parameter family, not the whole family, and it is used only for existence, not for formula (1).

What formula (1) actually quantifies, versus what the 56-space specializes:

| datum | status in formula (1) | status in the 56-space certificate |
|---|---|---|
| rectangles `(15,60)/(25,100)`, IIa `μ=2,k=2,ν=5`, r9/M2 shape | retained (forced combinatorics) | same |
| x-boundary `t^{15}`, `t+t^{25}` | retained (selected) | same |
| common centering `(c1,c2,c3)` | quantified: every extra contributor is on a forbidden F1 diagonal, with no division by a centering coefficient | specialized to `(1,1,1)` |
| F1 orbits `C,U,V` | enter (1) through `E1,E2`; vanishing is `Q=0` | specialized to `C=1` and the graph (12) |
| r9 parameter `A`, dead stretch, later local jets | do not enter (1) | zero dead stretch; `A` fixed by (18) |
| pole chart `n=12` | unused | specialized |

The completed first-band and next-row reviews already confirmed that `(c1,c2,c3)=(1,1,1)`, orbits `1,2,28/25`, zero dead stretch, and `A=1/9` are one licensed control, not terminal-forced data. This gate does not pretend otherwise. Complex orbit values are the native coefficient field of JC2; nothing in the IIa/r9 ledger forces `U,V∈R`.

Claim 1 stands.

---

## 2. Line extractions (7), centering independence, and formula (1)

In the F1 chart, `x^i y^j=η^j q^{j-5i}`. Holomorphy below `q^{-15}` (resp. `q^{-25}`) forbids `j-5i<-15` (resp. `<-25`). The leading line is equality.

Centered expansion of `x^i` uses `i-t` factors from `{c1 s, c2 s^2, c3 s^3}` and `t` factors of `t s^4`. Independently of the values of `c1,c2,c3` (including zeros), the possible sources of `[s^k t^d]` are

```text
j = 4d + (sum of {1,2,3} over the extra factors) - k,   i = d + n,   n≥0.
```

For the three target jets the unique on-line monomials are `(i,j)=(14,55)`, `(24,95)`, `(13,50)`. Every extra-factor contributor has `n≥1` and therefore

```text
j-5i = (extra-n) - (pole order) - 4n ≤ - (pole order) - 2n < -(pole order).
```

Those coefficients vanish by F1 holomorphy before any centering value is used. The target monomials themselves use only `(t s^4)^d`, coefficient `1`. This is a coefficientwise proof for arbitrary `c1,c2,c3`.

On the F1 line, `x^{14}y^{55}=η^{55}q^{-15}` is the `z^{11}` term of `R^3`, `x^{24}y^{95}` is the `z^{19}` term of `R^5`, and `x^{13}y^{50}` is the `z^{10}` term of `R^3`. Expanding `R=z^4-E1 z^3+E2 z^2-⋯` independently as a multivariate polynomial in `(C,U,V)` gives

```text
[z^{11}]R^3 = -3 E1,     [z^{19}]R^5 = -5 E1,     [z^{10}]R^3 = 3 E2 + 3 E1^2,
```

which is (7), and recovers `E1=2C+U+V`, `E2=C^2+2C(U+V)+UV` from the same expansion.

The already-imposed first row is `f1(t)(1+25 t^{24})-15 t^{14} g1(t)=0`. Since `gcd(t^{14},1+25 t^{24})=1` in `Q[t]`, this forces `t^{14}∣f1`. The rectangle plus F1 holomorphy forbid `[t^{15}]f1`: the only candidate is `(i,j)=(15,59)` with `j-5i=-16<-15`. Thus `f1=-3 E1 t^{14}` identically, and

```text
g1 = (-E1/5)(1+25 t^{24}) = -E1/5 - 5 E1 t^{24},
```

which is (8) and is consistent with `[t^{24}]g1=-5 E1` from (7).

The next-row two-form was already confirmed: `dx∧dy=s^2 ds∧dt` even with extra centering (the `ds` part of `dx` wedges to 0 against `dy`), so

```text
[s^{-1}]J = f1 g1' - f1' g1 + 2 f2 q' - 2 p' g2,   p=t^{15}, q=t+t^{25}.
```

At `t^{13}`: `p' g2` has degree `≥14`, and `f1 g1'` is supported in degree `37`. The remaining terms are

```text
-14 (-3 E1) (-E1/5) + 2 (3 E2 + 3 E1^2) = (6/5)(5 E2 - 2 E1^2).
```

Characteristic zero is used only to divide by `5`. At the frozen numerical point `C=1,U=2,V=28/25`,

```text
E1=128/25,   E2=237/25,   (6/5)(5 E2-2 E1^2)=-18858/3125,
```

and the three jets reproduce the confirmed next-row values `-384/25`, `-128/125-(128/5)t^{24}`, `66927/625`. Formula (1) is therefore the same one-coefficient certificate, now as a polynomial in the F1 moduli, with no centering denominator.

Claim 2 stands.

---

## 3. Quadratic stratification and the complex open set

Substituting `E1,E2` yields

```text
Q(C,U,V)=5 E2-2 E1^2 = -3 C^2 + 2 C(U+V) + UV - 2 U^2 - 2 V^2.
```

The symmetric matrix is

```text
[[-3, 1, 1], [1, -2, 1/2], [1, 1/2, -2]]
```

with leading principal minors `-3`, `5`, `-25/4`. Hence `Q` is negative definite over `R`, and its only real zero is the origin, which is outside the source-typing open set `C,U,V` nonzero and pairwise distinct. Every real source-typed specialization is obstructed. That cannot be promoted to an SP-2 kill: JC2 is a statement about `C[x,y]`, and complex orbit values keep the same IIa combinatorics.

Normalizing the nonzero chain orbit to `C=1` and writing `S=U+V`, `D=UV` gives (11)–(12). The three source-open conditions (13) pull back to the three quadratics (14), with discriminants `-20`, `-15`, `-80`. Each excludes two complex points of the graph `D=(2S^2-2S+3)/5 ≅ A^1`. Explicit source-typed points exist, e.g. `S=0`, `D=3/5` (`U,V=±i√(3/5)`, `1-S+D=8/5≠0`) and the algebraic point of claim 6. This is a nonempty Zariski-open subset of a genuine hypersurface, not a formal vanishing of one Jacobian slot: claims 4–5 show the rest of the paired next rows are solvable there.

Claim 3 stands.

---

## 4. Symbolic transport, rank `3508/3602`, (17), ODE, normalization

The first-band homogeneous matrix depends on the centering and the three chart supports, not on the F1/pole *values*. With center frozen at `(1,1,1)`, it is the same rational matrix already confirmed at rank `3508` in `3602` variables (nullity `94`, `6547` rows: `1764+4743+40`). Insertion-order least-index GE and reverse-lex least-index GE both give rank `3508`; leftmost free coordinates therefore do not depend on a row-order artefact. Every `f`-transport row writes only into `[0,976)`; every `g`-transport row writes only into `[976,3602)`; the 40 first-Jacobian rows write into both blocks and nowhere else. No local coefficient copy is introduced.

Symbolic RHS propagation through that factorization, with `R^3,R^5` and the r9 patterns in `(S,D,L,A)` on the F1/pole leading lines and with x-boundary `t^{15}`, `t+t^{25}`, produces exactly two nonzero dependent rows, with keys

```text
('f', 'F0', -3, 6)     and     ('g', 'F0', -5, 10).
```

Those are the pole top-scale slots `ζ^6` of `f` and `ζ^{10}` of `g`. Their right-hand sides are exactly (17):

```text
L^3 + [25(S-D-1)]^3 = 0,     L^5 + [25(S-D-1)]^5 = 0.
```

The other `3037` dependent rows have vanishing symbolic RHS. Pivot leads are in `Q`, never zero, maximum denominator `9`, and involve none of `S,D,L,A`. There is no parameter-dependent pivot denominator and no rank jump on this slice: the matrix does not see those parameters.

The F1 Taylor at `η=1+ζ r^{12}` is `P∼-25(S-D-1) ζ^2 r^{24}`, so the transported tops are `[-25(S-D-1)]^{3,5}`. Odd powers produce the plus signs in (17). Jointly over `C`, `ρ^3=ρ^5=-1` for `ρ=L/[25(S-D-1)]` forces `ρ^2=1` and then only `ρ=-1`, i.e. the source relation `L=25(1-S+D)`. Each odd-power identity separately would allow extra roots of unity; both together do not. Under (15) both rows are identities.

Independent dual evaluation of the three jets in (7) through the same factorization reproduces `[z^{11}]R^3`, `[z^{19}]R^5`, and `[z^{10}]R^3` as polynomials in `(S,D)`, so the GE does not silently drop those F1 identifications.

The leading pole ODE is computed directly from (16):

```text
3 p q0' - 5 p' q0 = (25/9) L^8 A^3,
```

with all positive-degree coefficients zero. The chart determinant is `-25 r^{-9}`, so

```text
J_{x,y} = (3 p q0' - 5 p' q0)/25 = L^8 A^3 / 9
```

at leading order, and `[r^0]J=1` is `L^8 A^3=9`. On `L≠0` this always has three nonzero solutions for `A` over `C`. At the rational point `L=3`, `A=1/9` one has `3^8 (1/9)^3=9`.

Claim 4 stands.

---

## 5. Complete next rows, formula (19), dimension `56`

On the 94-space the first transverse x-jets remain frozen (empty parameter dictionaries), now with moduli-dependent constants matching (7)–(8). Consequently the nominally quadratic next x-row is affine-linear, with homogeneous part `2 f2_{\mathrm{hom}} q'-2 p' g2_{\mathrm{hom}}` independent of `(S,D,L,A)`. Independently:

- 40 slots `t^0,…,t^{39}` (the a priori bound already confirmed by the next-row review);
- zero homogeneous directions exactly at degrees `13,37,38,39`;
- homogeneous rank of the remaining 36 rows is `36`;
- quadratic monomials: none;
- symbolic constants at `37,38,39` vanish identically, so those three slots are identities;
- degree `13` has zero homogeneous part and constant equal to (11).

Thus (11) is the only x-next compatibility equation. On (12) the x-system is solvable of affine dimension `94-36=58`.

The homogeneous parts of `[ζ^4]p1`, `[ζ^3]q1`, `[ζ^8]q1` are independent modulo the 36 x-rows, with deterministic quotient pivots `10,25,38`. On the whole first-band space those are in fact the only nonzero `p1,q1` jets (`p1` supported in degree `4`, `q1` in degrees `3,8`, even with `ζ` run out to the rectangle bounds). Unreduced, as identities of 94-vectors, the two pole-J rows are exactly (19):

```text
R3 = -(4/9) L^5 A^2 P - (1/5) L^3 A Q3,
R8 =  (2/3) L^5 A P - (3/5) L^3 Q3 - (4/5) L^3 A Q8.
```

(The naive extra `[ζ^9]p1` term that appears in a term-by-term expansion of the local wedge is the zero functional on this 94-space.) When `LA≠0`, the `Q3` coordinate of `R3` and the `Q8` coordinate of `R8` are nonzero, so the two rows have rank exactly two. Direct rank of the compiled pole-J tangent is `2` at `(L,A)=(3,1/9)`, `(3,1)`, `(2,1/2)`, `(1,1)`, and `(5,-2)`; it drops to `1` at `A=0` (`L≠0`) and to `0` at `L=0`, both excluded by (15) and (18). Combined x-plus-pole tangent rank at the numerical point is `38`, matching the confirmed next-row review.

Because the two pole rows have independent linear parts, they impose no affine compatibility condition: constants merely shift the particular solution. On (12)–(13),(15),(18) the simultaneous system is nonempty of affine dimension `94-36-2=56`.

No copied coefficient block manufactures this. No source-open specialization of `(S,D,L,A)` changes the first-band rank or the x-tangent rank, and the only pole rank drop is the documented `LA=0` locus.

Claim 5 stands.

---

## 6. Explicit algebraic survivor

Set `C=1`, `D=S-22/25`, and take either root of `10S^2-35S+37=0`. Discriminant `1225-1480=-255`, so the point is genuinely complex. Then

```text
L = 25(1-S+D) = 25(3/25) = 3,     A=1/9,     3^8 (1/9)^3 = 9.
```

The linear slice `D=S-22/25` is exactly `L=3`. Substituting it into (11) produces a scalar multiple of `10S^2-35S+37`, so the obstruction vanishes on this quadratic. Source-typing:

- `D=0` would force `S=22/25`, which is not a root (`M(22/25)≠0`);
- `1-S+D=3/25≠0`, so neither extra orbit equals the chain orbit;
- `S^2-4D=S^2-4S+88/25`, and `M-10Δ=5S+9/5`, whose only root `S=-9/25` is not a root of `M`, so `U≠V`.

Point (21) therefore meets every nondegeneracy used in this gate and keeps pole data rational. It is a point of the 56-dimensional affine family of pairs satisfying the first band plus both next Jacobian rows. It is not a polynomial map with `J≡1`, not a realization of SP-2, and not a Keller pair. The producer says so.

Claim 6 stands.

---

## 7. Scope, exclusions, and the smallest remaining band

Formula (1) is uniform in common centering and in every pole/dead-stretch parameter that does not appear in the coefficient, *for the retained normalized x-boundary and the retained IIa F1 pattern*. It does not quantify a more general unsplit degree-15/25 x-polynomial, a different F1 reduced shape, the other seven td=6 terminal classes, opposite-side landing, mapping degree, or exact `J=1`. Repeating generic samples off (12) cannot add information: off (12) the next x-row is impossible for every centering and every pole datum inside this pattern.

The smallest valid successor on this control is therefore forced onto the complex codimension-one locus (12): impose the next paired Jacobian band on the 56-dimensional exact family, or produce a global condition that excludes (12). Before any SP-2 or terminal-class claim, a campaign-level argument must still justify that the retained x-boundary exhausts the licensed x-direction data and must restore every omitted Eggers-tree, other-infinity, mapping-degree, and landing condition.

Claim 7 stands.

---

## Parameter, source-typing, and field caveats (non-blocking)

1. Formula (1) is an implication from first-band existence, not a construction of first-band solutions for arbitrary `(c1,c2,c3)`. Existence of the 56-space is proved on the licensed slice `C=1`, center `(1,1,1)`, zero dead stretch. The obstruction coefficient itself does not divide by a centering coefficient and does not see pole data.
2. `C=1` is a genuine normalization of a nonzero chain orbit: `Q` is homogeneous of degree two, and `Q=0` is equivalent to (12) on `C≠0`.
3. Jointly (17) kills extra cube/fifth roots of unity. Separately, `L^3+[25(S-D-1)]^3=0` would allow two further complex scales; the fifth-power row removes them.
4. The first-band matrix is over `Q`. Its rank is therefore the same over every characteristic-zero extension, including the quadratic field of (21) and `C`. No modular specialization was used, and none is needed.
5. `SHEET6.md` / `SHEET6-LROOT.md` hashes moved relative to the first-band review because of campaign-status notes. Those notes correctly record that the numerical death is not uniform and is not an SP-2 kill; they do not change the IIa/r9 combinatorics.

---

## Hashes

| artifact | SHA-256 |
|---|---|
| moduli-uniformity report | `499e95763759fe195ab1eba5fbf97bfb9d255bbf6fb2b090add938d92f1f3a06` |
| moduli-uniformity replay | `55340fa662a20e1777eaa18fae2d26589c11ec7f3f2960f4a0a6efe015e7b6ab` |
| moduli-uniformity freeze manifest | `8d28ce7bc0ca31b51887457e9f5e33537f0ec86d950a6e0e21c78813a78994af` |
| canonical replay stdout | `596a3f54276d4b95ec241933a6c7ec1cf1d782e8b7d40376532e172e282da5ea` |
| symbolic certificate payload | `45cdf5d42496d3807c006cf27387cea4f91b3c3bc18155d7a0812e1d82c130f0` |
| imported next-row report | `32124d20ec84ef59d5b116639176b12053f5da6de1a9458dd4a2095d6b1618f0` |
| imported next-row replay | `0fc299a18a7ace6e5f64f98f38a71775331f226baac2e8ad258dd40e744fa5b8` |
| imported first-band report | `cb373892233bddaf1b8fbf7722335ca43ee366b337151c3b244d2604d8168bf2` |
| imported first-band replay | `c55e213672ebd22cf3e9e8f378ffa857f85c3a54be7ed39e5c956df36ff6e735` |
| first-band hostile review | `265c1103391e2bae889c018bb0637fe486e7292644c0295e4f9965e5085e7a25` |
| next-row hostile review | `12834c356c2c26243522b2609753370118ecd2c8ed87907033ca27e926d2ac2f` |

**JC2 scope.** This is an exact generic obstruction and an exact complex finite-order survivor for one SP-2 chart-pattern normalization. It neither realizes nor kills a terminal class, supplies no Keller pair, and neither proves nor disproves the plane Jacobian conjecture.
