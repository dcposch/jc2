# Hostile review: V47R1 executable literal three-row contraction

| Field | Value |
|---|---|
| Charged target | additive V47R1 literal three-row contraction repair |
| Reviewer / model | Grok 4.6 (xAI). Independent different-model adversarial desk review |
| Method | SHA-256 of every charged file, both freezes, and every R1 `source_pins` entry before any producer `PASS` line was treated as evidence; unmodified in-process replay of `verify_three_row_linker_r1.py` with no `--output`; a second sparse `Fraction` engine with unshared zero dictionaries implementing V46 `SCHEMA.json` `normative_primitives` and contracting the frozen 569 tails; independent maxima/`active` derivation from the V46 polar inventory; extra-jet pad `+1` on boundary regimes; live emitter mutations (row-1 tail deletion, `c_factor=1`, degree-32 target slots). Producer `PASS-GATE-T-STRICT-UAC-THREE-ROW-LINKER-V47R1-LITERAL-CONTRACTION`, schema status tokens, V47 `CONFIRMED`/`GAP/REPAIR` labels, and frozen `result.json` equality were assertions to attack, not evidence |
| Repo | `/Users/dc/code/math/jc2` |
| Git HEAD | `418e413593120d19e15e6546eb50c985f4b1f038` (charged files are untracked relative to that commit; custody is the assignment SHA-256) |
| Date | 2026-08-27 |

Independently recomputed SHA-256 of the six charged files match the review assignment. Every R1 `source_pins` entry rehashes. Both freeze inner walks match. The R1 verifier was rerun in this session **without** writing into `run_v0_v10_r1/`; the Python object it compared to frozen `result.json` is identical (exit 0, internal `json.loads(frozen) != result` did not fire). A second, non-aliasing engine reproduced the same 16 records and the same `manifest_records_sha256`. No file other than this review was written. No frozen producer artifact, canonical ledger, AWS job, heavy CAS, web sweep, or `jc2-lean` tree was entered or modified. `PYTHONDONTWRITEBYTECODE=1` and `python3 -B`; no `__pycache__` or `.pyc` remains under the R1, V47, or V46 trees.

This review does **not** broaden the three-row radical lemma, does **not** drop `D(p*k10)` / `D(p*k0)` or `D(rho)`, does **not** promote `(a,d,r)=(8,3,8)`, and does **not** conclude Gate T, order two, maximum twelve, either global `G2-PSC`/`G2-BD`, JC2, or a counterexample.

---

## Verdicts

| # | Mandated attack | Verdict |
|---|---|---|
| 1 | Custody and additive boundary | **CONFIRMED** |
| 2 | Exact polynomial engine | **CONFIRMED** |
| 3 | Literal emitter bridge / row semantics | **CONFIRMED** |
| 4 | Finite-jet completeness | **CONFIRMED** |
| 5 | All 48 coefficients | **CONFIRMED** |
| 6 | Raw/D1 normalization | **CONFIRMED** |
| 7 | Mutations | **CONFIRMED** (three producer controls are predicate/post-hoc; independently live-fired) |
| 8 | Promotion / allocation-step replacement | **CONFIRMED** |

**Smallest failing identity:** none of the 48 extracted slot polynomials, none of the sixteen support maxima, none of the extra-jet pad-`+1` probes, none of the eight mutation fires.

**Smallest failing hypothesis:** none. The V47 gap — that a SHA-256 of an endpoint report plus the strings `Phi1=h1`, `Phi2=h2`, `Phi4=h4+(P/2)*h2` is not a coefficient comparison of `Phi1[G]`, `Phi2[G]`, `Phi4[T_C2]` to `(g1,g2,g4)` — is repaired by an executable term-by-term contraction of the frozen 569-tail emitter at generic shifted jets.

**Smallest repair:** none.

**Exact V47+V47R1 scope after this review: eligible to replace the moving-root allocation step for exactly these sixteen contacts**, inheriting each resolved endpoint's localization and theorem type, and nothing else. Manifest IDs:

```text
A2D1, A3D1, A4D1, A5D1, A6D1,
A2D2, A3D2, A4D2, A5D2, A6D2,
A5D3, A6D3,
A1D2_R3, A2D3_R5, A3D3_R5, A4D3_R5
```

Replacement is of the allocation *step* on those already-resolved endpoints. It is not a new endpoint, not a unit-ideal claim, and not a license to drop upstream chart factors.

---

## 0. Custody of the six charged artifacts

Recomputed SHA-256, all matching the assignment:

```text
b895315acc241ca8542611cadefee0d4f29217f1ffecf8fbd3761b513b6aa908
  cases/max12_812_order2_gate_t_strict_uac_three_row_linker_v47r1_20260827/SCHEMA_R1.json
70085a3e2dadbc8d56605724880fa9ba0a2ff5b46258c0d60973d4a754b877d7
  cases/max12_812_order2_gate_t_strict_uac_three_row_linker_v47r1_20260827/verify_three_row_linker_r1.py
6c16224703cf79f35073bc41ac8f5cd5d1b8fa24d756aa2891b80df2870c8270
  cases/max12_812_order2_gate_t_strict_uac_three_row_linker_v47r1_20260827/run_v0_v10_r1/result.json
43547b06cdf8ab51730fe26b4e9d1c6675c41b7b7089e044acd8de74a208a19b
  cases/max12_812_order2_gate_t_strict_uac_three_row_linker_v47r1_20260827/RESULT.md
bca113ab35a12ba49cf86e1f0c982083ab4000d4f1fbda01491fcc9f13ba8226
  cases/max12_812_order2_gate_t_strict_uac_three_row_linker_v47r1_20260827/FREEZE.sha256
a4103c4f59799cdf7467ba9b613d503d6f94ae56346ba3ff7d54a04d784432a5
  xmodel/max12-812-order2-gate-t-strict-uac-three-row-linker-v47-hostile-review-grok-20260827.md
```

`FREEZE.sha256` contains exactly those four producer files (schema, verifier, `result.json`, `RESULT.md`) and matches each recomputed digest. `RESULT.md` lists the first three of those four and does not hash itself or the freeze; that is the correct freeze split.

---

## 1. Custody and additive boundary — CONFIRMED

### 1.1 Every consumed pin

All R1 `source_pins` rehash against the files on disk. Tails byte `d72f774c…3813848`, tails canonical `6eed03d4…ce387e8`, row census independently `[36, 54, 58, 81, 89, 120, 131]`, sum **569**.

V47 freeze inner lines, independently rehashed:

```text
b1770cc0…703ab2  SCHEMA.json
070d5a96…c7ecaa  GENERATED_MANIFESTS.json
903c6932…c0111   verify_three_row_linker.py
a8be0c67…4c367b  run_v0_v9/result.json
e53dd549…449871  RESULT.md
```

V46 freeze inner lines also rehash (`SCHEMA.json`, `verify_uniform_naturality.py`, `RESULT.md`, `run_v0_v4/result.json`). R1 does not re-walk the V46 freeze; it pins the V46 schema and verifier and uses both. That is the same weak-but-not-gap freeze split already recorded on V47.

V47 `run_v0_v9/result.json` still has `status=PASS-GATE-T-STRICT-UAC-THREE-ROW-LINKER-V47`, twelve baselines, four raised subtails, and `endpoint_replacement_status=WITHHELD_PENDING_DIFFERENT_MODEL_REVIEW`. R1 repeats that withheld bit on its own result. V47 producer files are byte-identical to the pins; R1 does not rewrite them.

Content actually consumed, not merely hashed:

| Pin | Use |
|---|---|
| V47 `SCHEMA.json` | 16 manifests `(id,a,c,r_min,G,T_C2)` |
| V47 freeze / result | inner walk, lifecycle, withheld bit |
| V47 Grok review | repair-token presence (`literal-row bridge`, `GAP/REPAIR`, `per-manifest executable extraction`) |
| V46 schema | polar inventory, tails hash, load/target schedule |
| V46 verifier | `source_pair`, `row_series`, `derive_support` |
| V46R1 schema | typed map `ez_(c+n)->2*CzD1_n`, `ec_(c+n)->2*CcD1_n` |
| V46R1 promotion | lifecycle firewall (`PROMOTED`, `(a,d,r)=(8,3,8)`, `confirmed_but_unpromoted`) |
| tails.json | 569-term contraction |
| tail emitter | tokens `LOAD_WEIGHTS = [2, 6, 10]`, `Lambda^{lambda_power}`, `Lambda^{12 + ell}`, `targets = {1: "0", 2: "mu2"` |

Hash-only (immutability/custody, not a live input to the contraction): V47 verifier and `RESULT.md`; V47 `GENERATED_MANIFESTS.json`; Fable three-row review; V46R1 verifier/result/report/freeze/review. `GENERATED_MANIFESTS.json` carries the same 16 IDs and `(a,c,r_min)` in the same order as V47 `SCHEMA.json`, plus the generated total/D1 alias maps with uniform `C_z=2*CzD1_0`, `C_c=2*CcD1_0`, `R_c=4*BcD1_0`. R1 does not read those alias strings; it executes the V46 jet map. The ID/`(a,c,r)` match is independently confirmed.

### 1.2 Unmodified replay

Command:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -B \
  cases/max12_812_order2_gate_t_strict_uac_three_row_linker_v47r1_20260827/verify_three_row_linker_r1.py
```

Exit 0. Printed object has `status=PASS-GATE-T-STRICT-UAC-THREE-ROW-LINKER-V47R1-LITERAL-CONTRACTION`, `manifest_count=16`, `coefficient_slot_count=48`, `manifest_records_sha256=8ff50e047975e1a4b651c1e6af27b5874a3ac93856981bca2d9b8a5fe4609e48`, mapped-D1 digest `ad0c9b15ce394e554c178521fadb32b3c71318cf3a8d700a2311aceb9d12a850`, raw digest `00720abb62b26d965c87a1ecce0aff5d593833bfa205a221c0cda8cb4687ff95`, eight mutations, `endpoint_replacement_status=WITHHELD_PENDING_DIFFERENT_MODEL_REVIEW`. The verifier compares the live dict to `json.loads(run_v0_v10_r1/result.json)` and would have raised `frozen V47R1 result mismatch`. It did not. No file in `run_v0_v10_r1/` was written.

The frozen-result check is not self-fulfilling: an independent contraction with empty tails yields the zero triple, not the displayed g-triple.

**Custody / additive boundary: CONFIRMED.** V47 remains immutable. Endpoint replacement is still withheld by the producer object; this review is the different-model gate that object asked for.

---

## 2. Exact polynomial engine — CONFIRMED

Producer ring: sparse dicts `monomial-tuple -> Fraction`, addition copies the left summand then cancels zeros, multiplication is bilinear on exponent vectors. Canonical digest is `sha256(json.dumps(..., sort_keys=True, separators=(',',':')))` of per-slot term lists sorted by `json.dumps` of the named monomial.

**Aliasing, live and then discharged.** `PolynomialRing.zero` is a single empty dict. V46 `Series.zeros()` is `[ring.zero] * (n+1)`, so every unwritten grade aliases that object. In-place mutation of `ring.zero` is visible in every aliased slot. Producer `add`/`mul` do **not** mutate in place: `add` starts with `dict(left)`, `mul` allocates a fresh dict, and `if not left or not right: return {}` returns a new empty dict. `poly_power(..., 0)` returns the shared `ring.one`. Those are smells. They are not a live failure on this freeze, because:

1. every write rebinds a list slot to a newly allocated dict;
2. a second engine that never shares an empty dict — fresh `{}` at every `zeros()` grade, copies on `shift` — produced byte-identical canonical triples and the same `manifest_records_sha256` on all sixteen manifests.

**Noncanonical hashes.** Independent expected polynomials, with names in the five-letter D1 ring `(rho, AzD1_0, AcD1_0, CzD1_0, CcD1_0)`:

```text
Phi1_G    : 3/4 AcD1_0 CzD1_0 + 3/4 AzD1_0 CcD1_0
Phi2_G    : 3/4 AcD1_0 CcD1_0 + 3/4 rho^2 AzD1_0 CzD1_0
Phi4_T_C2 : 3/8 CcD1_0^2      + 3/8 rho^2 CzD1_0^2
```

digest `ad0c9b15ce394e554c178521fadb32b3c71318cf3a8d700a2311aceb9d12a850`. Raw-total companion in `(rho, AzRaw_0, AcRaw_0, EzRaw_0, EcRaw_0)`:

```text
Phi1_G    : 3/8 AcRaw_0 EzRaw_0 + 3/8 AzRaw_0 EcRaw_0
Phi2_G    : 3/8 AcRaw_0 EcRaw_0 + 3/8 rho^2 AzRaw_0 EzRaw_0
Phi4_T_C2 : 3/32 EcRaw_0^2      + 3/32 rho^2 EzRaw_0^2
```

digest `00720abb62b26d965c87a1ecce0aff5d593833bfa205a221c0cda8cb4687ff95`. Both match the schema pins.

**Missing variables.** `manifest_variable_names` retains `rho`, `P_1..P_p`, both A/C companions through the declared maximum inclusive, both R companions, all three loads through their maxima inclusive, and `(mu2, mu4, mu6, J)`. Maxima of zero still allocate the `_0` jet (see §4). No charged family used by the V46 polar inventory is omitted from the ring.

**Monkeypatch.** R1 replaces `v46.deterministic_series` with a generic-jet factory and restores it in `finally`. `source_pair` looks up that name in the V46 module globals, so the patch is live. Evidence it is not a no-op: empty tails do not reproduce the g-triple; deleting row-1 term 13 changes `Phi1[G]` (see §7). After restore, the V46 function object is the original.

**Polynomial engine: CONFIRMED.**

---

## 3. Literal emitter bridge / row semantics — CONFIRMED

### 3.1 The program really contracts `F0..F6`

R1 `extract_manifest` does not return the displayed g-triple. It:

1. builds total and mapped-D1 primitives by two formula paths (`source_pair`);
2. contracts frozen tails on rows 1, 2, 4 (`row_series`);
3. reads `row1[G]`, `row2[G]`, `row4[T_C2]`.

Independent reimplementation from V46 `SCHEMA.json` `normative_primitives`, not from the V46 Python, used the same identities

```text
Ctot = sigma^2 S
Rtot = (P^2 + sigma^2 Q)/4
N3   = sigma^3 Az
N2   = sigma^3 Ac
N1   = sigma^3 (P Az + Ez)/2
N0   = sigma^3 (P Ac + Ec)/2
F6=2P, F5=2 Ctot, F4=P^2+2 Rtot,
F3=2 P Ctot + sigma^2 N3, F2=Ctot^2+2 P Rtot+sigma^2 N2,
F1=2 Ctot Rtot + sigma^2 N1, F0=Rtot^2+sigma^2 N0
```

with the V46 jet map on the total path (`az` shifted by `a`, `ez=2 Cz` shifted by `c`, `cs` shifted by `r`, `rs=4 Bc` shifted by `r`) and the D1 path (`Kc=sigma^{r+2} Bz`, `Kr=P^2/4+sigma^{r+2} Bc`, `N3=sigma^{a+3} AzD1`, `N1=sigma^3(P sigma^a AzD1/2 + sigma^c CzD1)`, companion `N0`). On every manifest, `total_f[i] == d1_f[i]` as polynomial series through `T_C2`, and the three extracted slots from the total rows equal the three extracted slots from the D1 rows.

The tail emitter `compile_square_load_ladder.py` names the compiled rows `Phi{ell}` from `tails[str(ell)]` minus `Lambda^{12+ell}*(target)`, with `targets = {1:"0", 2:"mu2", 3:"0", 4:"mu4", ...}` and load weights `[2,6,10]`. V46 `row_series` is that same contraction in the `sigma`-adic chart: monomials in `F0..F6,k10,k6,k2`, load delays `4/12/20`, target subtractions at `28/32/36/38`. Therefore

```text
Phi1 = row 1,   Phi2 = row 2,   Phi4 = row 4.
```

R1 does **not** re-prove `Phi4 = h4+(P/2)h2` from `Inv1`. That identity was the V47 algebraic row, independently confirmed there from the moving-`P` recurrence. The V47 gap was that this identity was never executed against the frozen emitter. R1 executes row 4 of the emitter. That is the correct repair direction: extract the compiled row, do not restate the desired equation.

`P_0 = -2 rho^2` is imposed by the generic factory (independent expansion `P0 = [[-2, [["rho", 2]]]]`). Higher `P_i` remain generic. Load delays match V46 `load_delays_sigma`. Target grades match V46 `target_schedule` (`mu2` at 28, `mu4` at 32). Every accepted `(G, T_C2)` satisfies `G=10+a+c`, `T_C2=10+2c`, `G<28`, `T_C2<32`.

### 3.2 Not a restatement of the three equations

- Empty tails: all three charged slots are the zero polynomial.
- Restricting row 1 to the monomial `F0 F3` with coefficient `3/4` (one of three `3/4` terms in the frozen row) produces extra in-window support `BcD1_0, P_1, AcD1_1, AzD1_1` in `Phi1[G]` on `A2D1`. The displayed two-term g-triple is a sum, not a single tail monomial.
- Deleting frozen row-1 term 13 (zero-based), monomial `F3 F6^3 k10` with coefficient `35/512`, changes `Phi1[15]` on `A2D1` by the extra term `-35/64 P_1^3 AzD1_1 k10_0`. The emitter term is live in the charged slot.

**Emitter semantics: CONFIRMED.**

---

## 4. Finite-jet completeness — CONFIRMED

Independent `derive_support` from V46 `polar_inventory` (arrivals `fixed_delay + sum e_F * contact_F`, then `maxima[F] = max(T_C2 - arrival)` over primitives containing `F`) matches every R1 pin. `G < T_C2` on all sixteen (`d = c-a >= 1`), so a jet that cannot enter `T_C2` cannot enter `G`.

### 4.1 The `maxima = 0` ambiguity, classified

R1 stores only `maxima`, not `active`. A maximum of zero is used both for an active leading jet that arrives exactly at `T_C2` and for a family with no in-window primitive. Independently, with `active` recovered from the same arrival table:

| Manifest | `max=0` and **ACTIVE_LEADING** | `max=0` and **INACTIVE** |
|---|---|---|
| A2D1..A5D1 | `R`, `k10` | `k6`, `k2` |
| A6D1 | `R`, `k10`, `k6` | `k2` |
| A2D2 | `R`, `k10` | `k6`, `k2` |
| A3D2, A4D2 | — | `k6`, `k2` |
| A5D2 | `k6` | `k2` |
| A6D2, A5D3 | — | `k2` |
| A6D3 | `k2` (`k2R` at `22+r=28=T_C2`) | — |
| A1D2_R3 | `k10` | `R`, `k6`, `k2` |
| A2D3_R5 | — | `R`, `k6`, `k2` |
| A3D3_R5 | `R` | `k6`, `k2` |
| A4D3_R5 | `k6` | `k2` |

R1 retains index `_0` whenever `maxima[F]=0` (`range(0+1)`). That is the correct direction: active leading jets are kept, inactive families are over-retained. A consumer that reads `maxima=0` as "drop the family" would be wrong; this verifier does not do that. Over-retained inactive `_0` jets have coefficient zero in all three charged slots.

### 4.2 No omitted higher jet enters a charged slot

Pad `+1` on the declared maximum, independently re-extracted as polynomials, on boundary regimes:

```text
A2D1:   A, p, C, R, k10, k6, k2  each +1
A6D3:   A+1, k2+1
A2D3_R5: R+1, A+1
```

Every probe still equals the displayed D1 triple; extra support remains empty. In particular `AzD1_2` on `A2D1`, `P_2` on `A2D1`, `k2_1` on `A6D3`, and `BzD1_1` on `A2D3_R5` do not appear in `Phi1[G]`, `Phi2[G]`, or `Phi4[T_C2]`.

V46 polar inventory omits `k2 A` (grade `25+a`). On these sixteen contacts `25+a` is always strictly larger than `T_C2`, so the omission cannot under-bound `k2` or `A` here. It is a bound on the inventory, not a hole in this freeze.

**Jet completeness: CONFIRMED.**

---

## 5. All 48 coefficients — CONFIRMED

Uniform exhaustive independent replay, not sampling. Each of the sixteen manifests was contracted by the non-aliasing engine at its declared maxima. All 48 slot polynomials equal the mapped-D1 triple above. Common canonical digest `ad0c9b15ce394e554c178521fadb32b3c71318cf3a8d700a2311aceb9d12a850`. Per-manifest records digest `8ff50e047975e1a4b651c1e6af27b5874a3ac93856981bca2d9b8a5fe4609e48`, matching the freeze. Extra in-window support is empty on every manifest (every retained `P_i`, `AzD1_{>=1}`, `R`, load, and target jet has coefficient zero in the three charged slots, as a polynomial identity, not a numeric sample).

Term counts are `[2,2,2]` on every manifest: exactly the two monomials of each displayed slot.

Variable-count check against `manifest_variable_names`: 19, 19, 19, 19, 19, 24, 27, 27, 27, 28, 36, 37, 24, 31, 31, 33 in schema order, matching the freeze.

Maxima regimes covered, not merely the d=1 wall: d=1 (`p=A=C=1`), A2D2/A1D2_R3 (`p=A=C=2`, `R=0`), A3D2–A5D2 (`R=k10=1`), A6D2 (`k6=1`), A5D3 (`p=A=C=3`, `R=k10=2`, `k6=1`), A6D3 (`k6=2`, `k2` active at 0), A2D3_R5/A3D3_R5 (`R=0`, `k10=2`), A4D3_R5 (`R=1`). All match.

**48 coefficients: CONFIRMED.**

---

## 6. Raw/D1 normalization — CONFIRMED

Independent raw triple is the chart remainder with `C0=(EzRaw z + EcRaw)/2`: coefficients `3/8`, `3/8`, `3/32`. The extra `4` in the `Phi4` denominator is the square of the chart factor. Substituting `EzRaw_n, EcRaw_n -> 2 CzD1_n, 2 CcD1_n` (and `AzRaw->AzD1`, `AcRaw->AcD1`) sends that triple exactly to the displayed D1 triple; substituting factor `1` does not (`3/8` vs `3/4` on `Phi1`/`Phi2`, `3/32` vs `3/8` on `Phi4`).

Live emitter, not just the displayed map: `source_pair` with `c_factor=1` makes `total_f != d1_f` and the extracted total slots unequal to the D1 slots, while the D1 extraction is unchanged and still equals the displayed D1 triple. The factor two is therefore a live total-path identity, not a label on the expected polynomials.

Producer `raw_coordinate_check` hashes the expected raw/D1 triples and the factor-two map and does **not** re-extract a raw-named 569-term series. That is weaker than the D1 contraction, but it is the correct algebraic content of the raw display: after the V46 jet map the total path is already written in D1 names. The live `c_factor` probe plus the sixteen `total==d1` identities close the gap.

**Raw/D1: CONFIRMED.**

---

## 7. Mutations — CONFIRMED

Producer fires eight named controls. Independently:

| Producer control | What the producer actually does | Independent live-fire |
|---|---|---|
| raw C factor-two conflation | `expected_raw_mapped_to_d1(1) == expected_d1` is rejected | same algebraic rejection, **and** `c_factor=1` through the emitter makes `total_f != d1_f` |
| Phi1 `3/4 -> 3/2` | doubled expected `Phi1` equals extracted | rejected (`False`) |
| Phi4 `3/8 -> 3/4` | doubled expected `Phi4` equals extracted | rejected (`False`) |
| inject extra relative A jet | post-hoc `Phi1 += AzD1_1`, support checker | support `{AzD1_1}`; **and** series-level `A` maximum `+1` does not introduce `AzD1_2` into the charged slots |
| omit frozen row-1 tail term | first index whose deletion changes `Phi1[G]` | first live indices `[13,16,17,19,20,21,23,24]`; index **13** is live; residue `-35/64 P_1^3 AzD1_1 k10_0` |
| open row-2 target wall | predicate `G>=28 or T>=32` on the pair `(28,30)` | **predicate-only in the producer.** Independent extraction at degree 32: `row2[28]` support `{mu2}`, and `row2[15]` still equals charged `Phi2` |
| open row-4 target wall | predicate on `(27,32)` | independent `row4[32]` support `{mu4}` |
| drop one of 16 coefficient pins | set-difference `pins[:-1] != frozen ids`, missing `A4D3_R5` | **string/set check in the producer.** Independently all 16 IDs equal V47's 12+4 and all 16 were contracted |

Algebraic/emitter mutations: factor two, both normalizations, tail-13, extra-jet pad. Predicate/set mutations: the two walls and the 16-pin set. The predicate walls are still the right firewall — every frozen `(G,T_C2)` is independently below `(28,32)`, and the target terms are present in `row_series` at those grades — but they are not themselves a coefficient extraction at the wall. That distinction is recorded; it is not a failing identity.

**Mutations: CONFIRMED.**

---

## 8. Promotion / allocation-step replacement — CONFIRMED

V47R1 repairs exactly V47 hostile-review item 5: per-manifest executable extraction of `Phi1[G]`, `Phi2[G]`, `Phi4[T_C2]` from the frozen 569-tail V46 emitter at generic shifted jets, polynomial equality to `(g1,g2,g4)`, vanishing of every extra in-window jet. Endpoint hashes are no longer used as that comparison. They remain localization and theorem-type provenance, which is the V47 repair instruction.

The V47+V47R1 package may therefore replace the moving-root allocation step **only** on the sixteen listed manifests, inheriting:

| IDs | Endpoint | Localization | Theorem type |
|---|---|---|---|
| A2D1, A3D1, A4D1, A5D1 | `D1_A2_A5` | `D(p*k10)` | arcwise/set-theoretic fixed-band emptiness |
| A6D1 | `D1_A6_A7` | `D(p*k10)` | arcwise/set-theoretic fixed-band emptiness |
| A2D2, A3D2, A4D2, A5D2, A6D2, A5D3, A6D3, A1D2_R3, A2D3_R5, A3D3_R5, A4D3_R5 | `D23_LOW_A6` | `D(p*k0)` | arcwise/set-theoretic closed-R-tail emptiness |

Direct three-row open `D(rho) ∩ (D(A0c) ∪ D(A1))` does **not** remove those chart factors. `scheme_unit_ideal` remains false. The radical lemma is not broadened: after inverting `rho` and one of `A0c,A1`, leading `C` lies in the radical of `(Phi1,Phi2,Phi4)`, not in the unit ideal.

Coefficient identities do not create a new endpoint. In particular they do not admit `(a,d,r)=(8,3,8)`, the five lower-`r` `RA^2` contacts, exceptional `E`, the `a=7` load tie, either target wall, `rho=0`, equality faces, positive-order leading loads, staged Rees/terminal receivers, or any unlisted contact.

R1's own `endpoint_replacement_status` remains `WITHHELD_PENDING_DIFFERENT_MODEL_REVIEW` on the frozen producer object. That is the correct producer posture. This review is the different-model gate. After it, replacement of the allocation step on the sixteen IDs is licensed. Global Gate T, order two, maximum twelve, either global `G2` obligation, JC2, and a counterexample remain out of scope.

**Promotion scope: CONFIRMED**, narrowly as above.

---

## Exclusions (unchanged)

Five lower-`r` `RA^2` contacts, exceptional `E`, `a=7` load tie, target walls, `rho=0`, equality faces, positive-order leading loads, staged Rees/terminal receivers, unlisted contacts, global G2, Gate T, order two, maximum twelve, JC2, and a counterexample are excluded. This review does not touch them.
