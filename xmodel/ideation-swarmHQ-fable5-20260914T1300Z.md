# Fable5.1 independent blind, FULL1300 all-degree reset (2026-09-14)

Producer: swarmHQ Fable 5.1 (claude-fable-5-1), independent blind ideator.
Basis bf1e1ffe30af1bfa123449596fd96064e87d655d. Manual reasoning, inert
text/hash/date reads and apply_patch only. No CAS, interpreter, network,
peer, live or linked file was touched. ROOT's sealed blind was not read.

## Read scope and custody

All eight charged inputs were read WHOLE from the lane inputs directory
after matching the eight charged SHA256 pins in charged order
(APPROACHES 0a847f15, COORDINATION 4ce5b29a, SWARM-POLICY 50cf4548,
MASTER46 35537eae, PREVIOUS-SYNTHESIS 5d7063ad, STATE-DELTA 93e51570,
COMMON 0ab0e779, FALLACY-v2 e47fd16c). APPROACHES (626 lines) was read in
five bounded chunks 1-130, 131-260, 261-390, 391-510, 511-626; COORDINATION
(694 lines) in four chunks 1-150, 151-300, 301-600, 601-694. The other six
were single whole reads. No prior-read reuse is declared. Post-readback
pins are reported in the custody section at the end. Recalled external
theorems are UNVERIFIED unless the map itself records them.

## All46 dispositions

1-46: UNCHANGED, every identifier covered. Reason: STATE-DELTA records no
new source point, complete pair, properness certificate, external theorem
or promoted result since FULL2155, and my whole-map scan produced no
candidate that survives the history fingerprint below. Notes without rank
change: 32 (off-diagonal collision ideals) is where the cross-avenue
connection lands; 30 and 29 (affine surfaces, commuting frames) are where
the disproof card lands; 44 stays low because 2407.13795's flaw was
rejected twice; 46 remains certification, not an independent mechanism.

## Ranked gaps

Proof gaps, ranked by what a proof must actually supply:
1. The unit group of the off-diagonal fibre square L of an actual
   full-plane Keller map (connection below). Every normality, ruling and
   conductor endpoint reduces either to it or to injectivity itself.
2. Actual-source first-order Bass selection plus the tested normalization
   (16, 11): the missing polynomial eigenvector/stable-image hypothesis.
3. Non-Galois second legs >=4, d1=2 and primitive/no-block cases (25, 26,
   28): no block-existence theorem.
4. Second-coordinate integrality for the selected integral coordinate (31, 6).

Disproof gaps:
1. Existence of an etale morphism from the pseudo-plane S to A2 (card 2),
   which is exactly a regular scalar pair; nothing recorded obstructs it.
2. Properness of one COMPLETE unretired F10 source ideal at r>=3 (parked).
3. An exact AS109 bounded-support lift with max(deg_y A, deg_y B) >= 12.
4. A K16 all-m point with b*B_m*eta_m != 0.

## Mechanism

NO_NEW_MECHANISM. Evidence: each candidate generated during the scan
collapsed to a recorded node. (a) The swap-anti-invariant primitive
S(P)-S(Q) of the Liouville potential on L is the recorded
polynomial-difference property of the collision-Lagrangian control, which
already fails to recover full-plane origin. (b) The conductor
c = (d_w P_H)(f,g,H)R is CANONICAL-CONDUCTOR-EULER-1. (c) Lifting the target
frame through an etale map to a commuting, everywhere-independent pair of
derivations is the classical LND reformulation (29). (d) Fibrewise
ramification-at-punctures constraints for submersions on S are
Riemann-Hurwitz, implicit in the PP critical-point counts. What is offered
instead is two hypothesis-DELETING reformulations (connection, card 2) whose
checksum costs minutes and which redirect, rather than extend, two
existing endpoints.

## Cross-avenue connection

Avenues 31 <-> 32 <-> 33. Objects: a full-plane Keller F=(f,g), the
admissible radial frame and H = T + x^N of RADIAL-CANONICAL-FINITE-GRAPH-1,
so j_H=(f,g,H): A2 -> Gamma_H is finite, birational and unramified onto a
hypersurface with defining polynomial P_H, and the exact conductor is
c = (d_w P_H)(f,g,H)R by CANONICAL-CONDUCTOR-EULER-1. Let X = A2 x_{A2} A2
be the fibre square, Delta its diagonal (open and closed since F is
unramified) and L = X minus Delta the off-diagonal part (32): a smooth
affine surface, etale over A2 through either projection, with the swap
involution sigma, and pi_1^* omega = pi_2^* omega.

Claim (manual, elementary): Gamma_H is normal iff the sigma-anti-invariant
polynomial delta_H := H o pi_1 - H o pi_2 has no zero on L. Sketch:
Gamma_H is a hypersurface, hence S2, so non-normality is a codimension-one
phenomenon; because j_H is finite and unramified, a codimension-one
singular point of Gamma_H is exactly a point with two preimages, i.e. the
image of the double locus pi_1(V(delta_H) cap L); conversely a zero of
delta_H on L is a non-injective point of the finite unramified j_H, where
it cannot be a local isomorphism. With the promoted chain "Gamma_H normal
=> B = R => F automorphism" this gives: F is an automorphism iff L is empty
or delta_H is a unit on every component of L.

Consequence (a logical distinction, not a theorem): for a genuine
counterexample L is nonempty and delta_H is nonconstant on each component,
so it vanishes somewhere UNLESS it is a nonconstant unit of O(L). The
normality endpoint therefore has no leverage beyond injectivity itself
except through O(L)^x / C^x. This concentrates ROOT/Astra's
normality/ruling preference and Fable's block preference on one object:
the units of the off-diagonal fibre square. The recorded S-chart off square
u=-x, x^2(y+v)=2, on which x is a unit, is the mandatory negative control:
any unit theorem must use full-plane polynomiality. Possibly KNOWN in
composed form; ROOT to checksum against AUDIT's collision/conductor entries.

## Strongest attacks

Proof: determine O(L)^x for actual full-plane Keller maps. A unit u on a
component L_i gives the closed regular form dlog u = a dx + b dy with
a,b in O(L_i) (L_i is etale over A2). If pi_1|L_i were finite over its
image, the norm of u would be a unit on a plane open set, hence a monomial
in the equations of the missing curve; the recorded obstruction is exactly
non-finiteness (missing branches). The attack is to show every unit of L_i
extends across the missing branches using that both projections pull back
the same nowhere-zero two-form. If proved: units are constant, normality
becomes identical to injectivity, and the normality route is closed as a
distinct mechanism (negative but spend-saving). If a nonconstant unit is
exhibited on an actual full-plane off square: no counterexample follows,
only a live candidate for delta_H nowhere zero.

Disproof: card 2, the etale-morphism search on S with the scalar row
deleted and a fibrewise Riemann-Hurwitz filter.

## Decisive experiment / software acceleration

NO_TEST needing compute. The cheapest actual discriminator is a manual
theorem-interface composition pass by ROOT (minutes): compose
RADIAL-CANONICAL-FINITE-GRAPH-1 (normality <=> invertibility),
CANONICAL-CONDUCTOR-EULER-1 (conductor formula) and the unramified
double-locus identification above into the single statement "F automorphism
iff (d_w P_H)(f,g,H) is a nonzero constant iff delta_H is a unit on L".
Outcome A: it composes; record it under 31/32 and stop commissioning
normality/ruling attempts that do not control O(L)^x. Outcome B: a scope
mismatch (fixed rational forms in the conductor formula, or the frame
condition f_m(0,1) != 0) blocks it; record the mismatch and downgrade the
connection to a heuristic. No software acceleration is proposed; no
measured speedup exists to cite.

## Cards

### Card 1 (proof side): unit group of the off-diagonal fibre square

Object: L = (A2 x_{A2} A2) minus Delta for a full-plane Keller F.
Hypotheses: J(f,g)=1 and F not injective (accepted external mapping degree
>=6 makes L nonempty for any counterexample). Missing all-degree closing
implication: "O(L)^x = C^x on every component" is unproved; even if proved
it only makes normality of Gamma_H equivalent to injectivity, so it closes
a route, not JC2. Known-history fingerprint: collision-surface-units GAP
(Astra, September12), collision-Lagrangian control, TRACE-IC/CUTOFF; not
the secant-idempotent or QCS event-rank claims. Cheapest discriminator:
the composition pass above, then one manual attempt to bound the unit rank
of L by the missing-branch valuations of the recorded S-chart control.
Outcomes: constant units (route closed, spend saved) or a nonconstant unit
on a full-plane off square (retained as a live candidate object, no pair).
Dependencies: the promoted normality chain and conductor formula. STOP:
one bounded manual tranche without a unit theorem or such a control.

### Card 2 (disproof side): S-type counterexample equals an etale map S -> A2

Object: S = Spec C[A,U,Z]/(U^2 - A - A^2 Z) with its nowhere-zero exact
volume omega and the accepted surjective etale chart pi: A2 -> S with
pi^* omega = 2 dx wedge dy.

Lemma (manual): O(S)^x = C^x. On A != 0 the equation solves
Z = (U^2 - A)/A^2, so S_{A != 0} is A1_U x G_m,A and every unit there is
c*A^k; such a function is invertible on all of S only for k = 0 because A
vanishes on the double fibre A = 0 (U^2 = 0, Z free). S is integral because
the equation is linear in Z with coprime coefficients A^2 and U^2 - A.

Consequence: for ANY etale morphism phi = (P,Q): S -> A2, dP wedge dQ =
h*omega with h a unit, hence h in C^x; so F = phi o pi is a Keller map of
geometric degree 2*deg(phi) >= 2, a counterexample. Conversely a regular
scalar pair is such a phi. The S/T endpoint is exactly: does the
pseudo-plane S admit an etale morphism to A2? The scalar row is redundant,
and DS-INV-1's nonemptiness equivalences transfer unchanged.

Missing implication: existence or exclusion of an etale S -> A2. Recalled,
UNVERIFIED: no classification of etale morphisms from unique-fibration
surfaces to A2 is recorded; the map's cited ML0 exclusion does not apply
because S is S(2,2,1). Known-history fingerprint: PP-HOM/PP-LIN-U/
PP-SEP-WT/PP-3WT exclude sparse families; U + q(AZ) is the recorded
submersion control; bracket-width and formal-boundary records supply no
pair. The trivial-units lemma and the bare etale reformulation are not in
the map; they may be implicit in "submersion control".

Cheapest discriminator (manual, under one lane-hour): fibrewise
Riemann-Hurwitz filter. If phi is etale then every fibre C_c = {P = c} is a
smooth affine curve on which Q is ramified only at punctures, so
2g(C_c) - 2 + 2*deg(Q|C_c) equals the ramification carried by the punctures
of C_c. For the recorded control P = U + q(AZ) the fibres are rational,
parametrized by w = AZ through A(1+w) = (c - q(w))^2, so no genus
obstruction; the test asks whether one Q keeps all ramification at the
punctures for every c simultaneously. Outcome A: such a Q exists; then
phi o pi is an explicit Keller counterexample candidate needing later CAS
verification outside this lane. Outcome B: a fibre where every polynomial
Q has an interior critical point is exhibited; then this P is excluded for
EVERY mate, a whole-family exclusion rather than a sparse ansatz.
Dependencies: the accepted pi chart and omega = 2 dx wedge dy only. STOP:
one manual tranche; no degree, jet or CAS farm unless outcome A yields an
explicit candidate.

No third card.

## Campaign-systems check

NO_CHANGE. Evidence from this lane: the bounded-chunk read remedy worked
(nine chunks, zero clipping, full coverage of 172 KB in about six minutes
with hash checks before and after). The only observed defect was that a
`cd &&` in my first hash command changed the persisted lane cwd; the
recorded remedy (absolute paths, subshell for apply_patch) already covers
it. No framework, scalar, preflight, r3, worker or accepted-bound renewal.

## Allocation recommendation

CONTINUE protected all-degree research, with the ROOT/Astra
normality/ruling route re-aimed at O(L)^x instead of further ruling or
conductor variants. REDESIGN the S/T construction search as an
etale-morphism existence question with the fibrewise filter (card 2),
dropping the scalar row. STOP, retaining existing parks: scalar/preflight/
r3, accepted-bound rechecks, contact/control/gauge/Hilbert-cycle/
homogeneous-S renewals, prime-degree claims, Bass family enlargement. No
new worker, occupancy lane or successor. Both cards are manual and cost
under one lane-hour each; neither is a launch instruction.

## Custody

Pre-read pins matched in charged order at 13:02 UTC. Post-readback pins
re-verified at 13:11 UTC after the whole own-report readback, all eight
unchanged: 0a847f15, 4ce5b29a, 50cf4548, 35537eae, 5d7063ad, 93e51570,
0ab0e779, e47fd16c. No exit-price assertion is made, so no exit-price
declaration line is present. Word count is below the 2600 cap. Wrapper owns custody;
no local finalizer was run. ALL WRITERS IDLE after this line.

<!-- BODY-END -->
