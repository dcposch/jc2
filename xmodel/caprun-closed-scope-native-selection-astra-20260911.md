# Closed-scope native/host selection — administrative only

First action2026-09-11 10:18:40.906107377 UTC. Fixed reserve10:32 / hard10:35 UTC. Owner /root/model_productivity; basis0d39df3c9fd69c939a8420c54d03228b9077777d. No execution clearance, new code or mathematical claim.

## Selection

No new semantic blocker remains beyond actual identification and the existing execution observations. Select one ordinary Ubuntu amd64 installation with the already-qualified Python3.12.3-1ubuntu0.16 package and executable SHA a92f0f95e883390c7256b2e441484aac06b1002dbe1d924141a77c8d82f96223. This is a conditional candidate specification, not an observed host or permission to allocate. An AMI name or old worker record alone is insufficient. No pre-allocation FIFO trial, signal-reset trial, traceback calibration or future-exec observation is required.

The compatibility report's narrower accepted premise is the seven particular ASCII CALL/RAISE/string-SystemExit paths under exact source/flags, ordinary traceback/excepthook, healthy streams and intact native closure. It is not equivalence of all Python3.12 builds. Ordinary distribution trust means authentic package provenance, faithful source/binary correspondence and unmodified loader/libc/kernel/systemd/helpers; reproducible rebuilding and a new foundation proof are not requested. The retained descriptor signature was not independently verified. The report's prior-worker binary match supplies a comparison target, not current custody.

## Minimum genuine observations, in order

1. **On the selected host, before admission:** fresh exact instance/hostname/boot, retained EBS/durable filesystem/device, ordinary Linux/kernel/systemd/package identities, root UID, and current source/admin/runtime file paths, hashes, ownership and aliases. Match the qualified interpreter/package; identify the selected standard-library and dynamic-extension installation. Obtain the existing six-field native closure: files, directories with complete entries, aliases, absent paths, schema and expected Python path; include ld.so.cache and the exact source-native subset. Declared expected sys.path is not an observation of a running interpreter. Package records alone are not native closure, and hashes alone are not package authenticity.

2. **Administrative helpers:** ROOT must authenticate the selected shell/env/systemd/timeout/procps/util-linux/coreutils and metadata-collector helpers, their resolved paths/packages and required loader/library closure under trusted administrative invocation. Use distribution-controlled binaries with no injected startup/loader hooks or mutable PATH substitutes. The collector's ldd is acceptable only on such already-trusted distribution ELF files, not arbitrary downloaded programs or payloads. Admin/helper evidence remains separate from the exact five-science/three-wrapper inventories; no new science role or Python probe is needed. The native template does not by itself inventory every administrative helper.

3. **Physical access boundary:** identify ROOT versus PID1 PID/mount/cgroup/user namespaces; the visible cgroup2 mount must cover filesystem root, not a narrowed substitute. Inspect actual ancestry, owners, effective/default ACLs and delegation/migration interfaces for source, metadata, channel, output and cgroup paths. Confirm workload UID/GID has no cgroup/channel write route, including writable ancestors or inherited migration descriptors. Mode bits are not ACL evidence. Trusted ROOT's exclusive writer/no-entrant/no-migration/no-replacement commitment remains necessary; snapshots do not prove future exclusivity. The physical template explicitly leaves ACL/ancestry checks external and creates only its declared administrative stage directories, so it is not wholly read-only when later authorized.

Without getfacl, no installation/test is necessary for the narrower no-effective-write predicate: for the unprivileged nonowner workload, verify root ownership and absent group/other write bits on every protected object/ancestor, excluding the expressly authorized writer subtree. Under ordinary POSIX ACL semantics, named-user/group permissions are bounded by the mask represented by group mode bits; default ACLs cannot defeat restrictive creation/final modes. Thus root0700 channel/root0600 FIFO exclude nonroot access even if an ACL exists. This does not prove ACL absence. Ordinary cgroupfs DAC/nondelegation still requires actual mount/ancestry identity; do not assume tmpfs lacks ACLs. Any nonordinary mount or unexplained writable ancestor is STOP. [ACL project's manual, correspondence/creation/access sections](https://man7.org/linux/man-pages/man5/acl.5.html).

4. **Immediately before remote admission:** ROOT must hold a genuine coordinator USER retirement-timer receipt, including the actual service's complete AWS argv, profile personal, region us-east-1, exact selected instance ID and original future UTC, authenticated immediately before admission. A worker timer or merely active timer name is no substitute. The corrected outer DELTA is accepted by ROOT's supplied lifecycle attestation; no further review is proposed. Fresh worker TERM/KILL timers retain their original absolute cutoffs.

5. **After holder creation, before release:** observe actual MainPID/start/InvocationID, waiting sentinel, sole outer membership, namespaces as resolved for that process, cgroup device/inode, all nine empty direct domain leaves, effective controls/ACLs and recorded systemd settings. Inspect properties that the launcher only records, including restart/delegation/kill/runtime settings. Freeze final registration once with genuine identities, native pins and nine exact argv/eight canonical authority digests; CPU origin stays0. Authenticate readback/SHA and original remaining clocks before the separate one-writer token approval. No historical identity, placeholder, old13-phase card or fresh-name recreation substitutes.

## Semantics versus future evidence

The accepted static trace relies on ordinary Linux nonblocking FIFO/EOF, descriptor/signal and same-process exec semantics, ordinary CPython isolated startup and systemd's selected unit properties. These are explicit distribution/OS premises, not facts requiring a new dummy to rediscover. A same-PID exec necessarily belongs to the later authorized release: requiring its observation before release would be circular. Actual post-exec argv/PID/start/cgroup/maps, exact environment/sys.path admission, seven refusal triples, valid sentinel and strict dummy TERM/KILL/reap/kernel-quiet records belong to the selected nine-phase execution and its terminal custody. None is claimed now. Unexpected behavior remains STOP/NONDECISION; accepted stale-positive false-STOP risk is not reopened.

QUANTITY: does one selected host match these specific premises and bindings? Unknown. CHEAPEST TEST is the already-required fresh administrative package/native/physical reconciliation, followed by the existing admission capture; planning wall10 minutes UNMEASURED, not a cap or runtime claim. If matched, no additional semantic gate is indicated. No authority to allocate, install, test, release, retry or launch successful science follows.

All eight objects in six charged groups were prehashed and freshly read WHOLE. Compatibility custody and its expected transaction were verified before its report. One narrow primary ACL-manual lookup supports ROOT's requested no-getfacl fallback; scope is recorded in PRIMARY-SCOPE.md. No other linked body, live lane or host was inspected. Own-only collision/raised-OPEN checks, whole readback and input postpins precede marker-last sealing; expected verification and custody-first handoff follow. ALL WRITERS IDLE at terminal.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7763`.
- Body SHA-256:
  `91c11f0abf9b44d3cb1a10ab55208d7ae261b0e6ebefebdd0cbdb796bf6288df`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
