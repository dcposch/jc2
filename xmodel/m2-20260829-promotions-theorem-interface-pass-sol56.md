# Theorem-interface composition pass: td8 arity, td12 depth-24, and U2 first boundary

Date: 2026-08-29  
Author/lane: Sol 5.6, bounded coordinator composition pass  
Cutoff: campaign evidence through the 2026-08-29 08:15Z allocation refresh  
Lifecycle: sealed desk-algebra/interface report; no canonical promotion by this file

## 0. Executive verdict

The three promotions do not combine into a new JC2, degree-ceiling, landing,
or counterexample theorem.  They do produce three exact campaign interfaces:

1. **`BRIDGE` — route-specific landing/pruning.**  Any exact source or
   landing theorem whose output is the reviewed td8 affine equal-join record
   now composes immediately with the arity theorem to a contradiction.  All
   coefficient-transport, zero-side, layer-10, A-side, and germ-realization
   work whose sole consumer was survival of that record is therefore
   `DUPLICATE`/retired.  No source realization can rescue the route.
2. **`BRIDGE` — exact source pair to td12 finite jet gate.**  An exact
   polynomial pair plus fibre/branch evaluation determines the first
   B-child coefficient vector and, recursively, the 24 graded children.  The
   reviewed td12 theorem then gives a finite necessary rejection test.  The
   campaign's frozen `CornerData` and current decorated-tree record do not
   themselves serialize this silent 24-level jet, so the clean interface is
   a small `BChildJet_24` extractor from the exact source/completion map, not
   a claim that `G2-PSC` has been proved.
3. **`BRIDGE`, already recorded — U2 P0/P1 plus first integral P2.**  For
   fixed absorbed data and budget, the union of the reviewed P0-to-P1 theorem
   and the new integral-equality-boundary theorem makes the outer parameter
   finite for those two first-departure types.  A fixed two-pole case-III
   Q+E5 child can then be appended with a finite reduced pattern fibre, but
   the generic composition still lacks a typed map from the equality-boundary
   record to the Q+E5 merge-state slots.  It is not full-cell finiteness.

One live downstream wording changes: `ladder/REDUCTION.md` still says no
first P2 boundary is covered.  That blanket statement is now false at three
locations.  It should be narrowed to non-integral/non-equality, unclassified,
and nested-variable-base boundaries.  The file's global verdict — no total
landing/full book/cofinal ceiling — remains unchanged.

No exact pairing was found with `G2-BD`, the asymptotic-complement theorem,
the promoted Witt results, or the current D-series germ family.  Those are
genuine scope mismatches, not negative mathematical results about those
lanes.

## 1. Method, cutoff, and evidence custody

This pass implements the schema in `COORDINATION.md:220-229`: object,
hypotheses, field/ring, bounds, conclusion, then both directions of the
claim DAG and the cheapest exact pairing.  It used only explicit campaign
files in this repository, desk algebra, and hashes.  No web, AWS, CAS,
heavy computation, canonical edit, commit, or push was used.  No
`ideation-20260829T0820Z` submission or post-cutoff review report was read.

Canonical snapshots read during the pass:

```text
fc9c69cb062f50aae51797e5d7700084ccfe1329a996893a431537c8e8a0f9ab  COORDINATION.md
7cb5d4a8a3a4162a8a204facb79e68a9ef4f353275912d440170ae9898cd41f8  AUDIT.md
27a208c58af3eb192bff51b2dc8f58cb9f6efcbd22816f88de6c9d428595a05b  APPROACHES.md
3a6298d30a59b7a5ebf9416d1e178d6b1757553b54fa728b5f065b889ad5a9ec  PROGRESS.md
b0b6c276b1fe28a9b94556e29a058264201267dfc496b3100c416f027bc7aa0f  ladder/REDUCTION.md
7679db8aa0526941804e3196920a5ad299bc68c1f5c4ebd82d55388ca7667f77  ladder/BOOK-OFFAXIS.md
```

Load-bearing theorem files:

```text
3c2c9a7ed79cc6d05ec3ba7098c1dc89cabb3547791d777c4bf8971d1ae28e39  xmodel/m2-td8-trunk-exact-charge-r1-sol56-20260829.md
d9db244079c521a2e3643c824b715adb4c934d843f9da18e9140c80d6758f012  xmodel/m2-td8-trunk-exact-charge-r1-hostile-review-opus5-20260829.md
2a151eef1e661464ada47b0e387051733f9c2cb39cc7893366f5b1e09e15e829  xmodel/m2-td12-u1-next-trunk-discriminator-r1-sol56-20260829.md
3f214db8c12d022c2852dfadbbc268d105484343a8f6d4664765a08d3efcea03  xmodel/m2-td12-u1-next-trunk-discriminator-r1-hostile-review-fable5-20260829.md
8c46e6bf6e31faa191c67ba1bac6d489e0fe27741b35223ff30992f7d27a4db8  xmodel/m2-u2-first-p2-boundary-finiteness-r1-sol56-20260829.md
111735765957ef971b720a8dcb373fbe32664047e4028a5a1634b020be405939  xmodel/m2-u2-first-p2-boundary-finiteness-r1-hostile-review-fable5-20260829.md
```

The promoted canonical summaries are `AUDIT.md:8-44`, `:48-70`, and
`:112-123`; the current strategy consumers are `APPROACHES.md:10-53` and
`PROGRESS.md:8-60`.  Where a producer proof and its hostile review differ,
the normalized statement below uses the review-repaired theorem.

## 2. Normalized theorem interfaces

### A. `TD8-TRUNK-EXIT-GE3` / affine route kill

**Object.**  The one reviewed formal affine equal-join family at `td=8`,
with integer `t>=0`; its `(85,35)` trunk has

```text
i = 28(4+3t),  D_F=17i,  kbar_F=7,
p_F = C(eta^17-A)^(3i)(eta^17-B)^(2i),  B=4A/3.
```

The selected object is the actual first-separation exit set below the
multiplicity-`2i` B-direction, not one selected Puiseux ray.

**Hypotheses.**  The reviewed coefficient transport and route record; the
corrected Statement-9.3 descent law per flag; actual-weight Corollary 7.1;
tree non-remerging; the two A-copy exit floors `>=2`; merge charge zero; and
the distinct x-side flag of weight at least one.  See the producer
`§§1-4` and the Opus repair/reconstruction `§§4,9`.

**Field/ring.**  Complex local Newton-Puiseux/formal tree data with exact
rational and integer weight arithmetic.  No polynomial source pair is an
input or output.

**Degree/support bounds.**  Fixed topological degree eight, but unbounded
affine parameter and local full multiplicity `i`.  There is no global
polynomial-degree or support bound.

**All branches.**  Write `q=kappa_H/kappa_F` and use `tau_0>=17/2`.

- If a flag has `q>=2`, its weight is at least
  `q(tau_0-7)>=3`.
- If `q=1`, there is no conjugate shedding.  A unique flag would retain all
  `2i` places and have the impossible nonintegral weight `3/2`; hence at
  least two flags exist and their total weight is at least four.
- Equality at the lower bound three is possible only at
  `q=2, tau_0=17/2`; attainment is not proved or needed.

**Conclusion.**  Trunk exit weight is at least three, so the selected actual
weight is at least `2+2+3+1=8>7`.  This formal affine route is impossible for
every `t`.  No other td8 family, landing theorem, degree range,
counterexample, or JC2 conclusion follows.  Producer body seal
`688dbf41...`; review body seal `51f3ed7c...`.

### B. `TD12-U1-B-CHARGE-8` / `BChildJet_24`

**Object.**  The unique reviewed U1 first trunk on the td12, `m=3`, global
type-`(2,3)` row, at its `(2/3,3)` P1 terminal:

```text
n>=5, n == 1 or 5 (mod 6), direct-entry i=6n,
(nu_F,kbar_F,X_F,M_F,w_F)=(25,17,25,3,2/3),
p=(T-A)^2(T-B), q=eta(T-A)(T-B), T=eta^25,
A=8u, B=9u, mult_B=i, D_F=25i.
```

The exact-price theorem itself only needs positive integer `i`; `i=6n` is
needed for the direct-source first gcd degree.

**Hypotheses.**  The reviewed td12 row and U1 trunk; terminal `psi=2`, shared
ceiling nine, merge and literal pole exits zero; corrected per-flag descent;
and first-separation ownership.  The Fable review's distinct-orbit versus
conjugate-shedding repair is load-bearing (`§§2-4,9`).

**Field/ring.**  Complex local Puiseux/formal graded coefficients.  The
interpolation is exact over `C[A,B]` with `A,B` nonzero and distinct.  It is
not a solution of the coupled Keller recurrences in `C[x,y]`.

**Degree/support bounds.**  Fixed topological degree twelve; unbounded
direct-entry `i=6n`; normalized depth exactly 24 before the cv level 25.  The
formal interpolation respects the local top degree `75i`; that is not a
global source-degree bound.

**All branches.**  For every B-descendant flag,
`wt=q(tau_0-17)` and `tau_0>=25`.

- A parting into distinct direction orbits gives at least two flags, each
  of weight at least eight, total at least sixteen.
- Conjugate shedding may remain one orbit-level flag, but forces `q>=2`, so
  that flag has weight at least sixteen.
- Both violate the td12 ledger.  Therefore no parting occurs before the cv
  level: `N=i`, `tau_0=25`, `q=1`, and `lambda_B=8`; charge nine is
  impossible.
- A distinct-place split exactly at level 25 is allowed by this test.  A
  conjugate denominator jump is separately too expensive.

**Conclusion.**  Every normalized level `1,...,24` must retain the same
denominator and have child polynomial a pure `i`-th power.  At level one,

```text
C_1(z)=sum_{k=0}^i [ (eta-c)^(i-k) ] P_k(eta) z^(i-k),  c^25=B,
deg gcd(C_1,C_1')=i-1 (=6n-1 on direct entries).
```

After a pass, recenter and repeat.  The first failure kills the row; passing
all 24 levels is necessary, not sufficient for realization.  The sibling
`(3/4,4)@nu_F=17` is untouched.  The smallest missing input is

```text
([ (eta-c)^(i-k) ] P_k)_(k=1..i)
```

or a source/Keller recurrence that determines it.  Producer body seal
`b25217e9...`; review body seal `8ef16e10...`.

### C. `U2-FIRST-P2-BOUNDARY-FINITE`

**Object.**  A reduced absorbed-U2 outer-parameter family

```text
r>=2, mu>=1, w in Q_{>0}, B in N fixed,
W_L=w(L+r-1)/L, M_L=gcd(r*mu,r+L), L>=1.
```

A reviewed P0 prefix of modeled cost at most `B`, with standing `nu>=2`,
ends at incoming parent `(W,M)` immediately before its first P2 transition.

**Boundary hypotheses.**  At least one source-licensed root-multiplicity
equality orbit, with

```text
l|M, 1<=m<l, kbar=lW/(l-m) in N*.
```

The review confirms that `l` and `(W,M)` belong to the parent, while the
equality-string length and child `M'=nu*s+1` lie after the cut.

**Field/ring.**  Exact rational/integer/divisibility arithmetic.  This is a
reduced state theorem, not a theorem in a polynomial source ring.

**Bounds.**  For `Mhat=r*mu*2^B` and `What=r*w*Mhat^B`, every boundary weight
lies in the explicit finite set

```text
T_EQ={ k(l-m)/l :
       1<=l<=Mhat, 1<=m<l, 1<=k<=floor(Mhat*What) }.
```

The P0 finite-target product lemma then makes the set of outer `L` effective
and finite.  The conclusion is uniform in equality-string length, child
multiplicity, and all later data.

**All branches and conclusion.**

- P0 ending at P1: finite `L` by the separately reviewed P0/P1 theorem.
- First departure through the displayed integral equality boundary: finite
  `L` by this theorem.  If `T_EQ` is empty, there are no candidates.
- Rational-`kbar` `nu=1`, nested variable base data, a non-equality or
  unclassified first transition, and recursively varying absorbed data are
  outside scope.

No full-cell, source, landing, exact-lambda, degree-ceiling, `G2-PSC`,
`G2-BD`, or JC2 conclusion follows.  Producer body seal `220aa975...`;
review body seal `f2543399...`.

## 3. Exact pairing ledger

The label applies to the stated pairing, not to an entire research avenue.

| Pairing | Label | Exact interface result |
|---|---|---|
| A -> td8 coefficient transport, A-tower, zero-side, layer-10, and route-specific source-germ work | **DUPLICATE** | These were useful only if the same affine route survived. A kills it before those source questions matter. Their local theorems remain true archival facts, but no further work on them can strengthen the route verdict. `APPROACHES.md:12-27`; `AUDIT.md:77-89,125-144`. |
| A -> a future exact source/landing theorem for this same record | **BRIDGE** | `source lands in affine record` plus A gives an immediate contradiction. The bridge is conditional because no total landing theorem identifies an arbitrary counterexample with this record. |
| A's ramification dichotomy -> B's proof | **DUPLICATE** | B uses the same corrected distinction: distinct-orbit split means multiple flags, conjugate shedding means denominator ratio `q>=2`. This proof schema was already consumed by the Fable B review; it gives no further cross-route conclusion. |
| A -> any other td8/M2 family | **NO_HIT** | A fixes one affine equal-join record. `BOOK-OFFAXIS.md:186,634-659` correctly leaves the other composite panels/open full-cell grammar untouched. |
| B <- exact polynomial pair/fibre/completion map | **BRIDGE** | Newton-Puiseux expansion of the exact source determines the displayed coefficient vector and all 24 recentered children, after which B is a finite rejection test. Current `CornerData` cannot do this. |
| B <-> `G2-PSC` | **SCOPE_MISMATCH** at the theorem level; the exact-pair constructor above is the usable bridge | `G2-PSC` asks for two-chart, all-root, source-provenanced forest coverage. B neither supplies nor needs that global coverage. Conversely the current decorated target records characteristic/contact vertices but does not promise the silent level-by-level `BChildJet_24`. Add an extractor; do not call it a proof of `G2-PSC`. |
| B -> sibling `nu_F=17` or other td12 families | **NO_HIT** | The weight gap, terminal budget, and depth are different; the review explicitly leaves that sibling untouched. |
| B -> current D-series/formal-germ lane | **SCOPE_MISMATCH** | B is an exact necessary germ gate for this td12 U1 record, but no live D-series artifact is typed as this record or supplies its B-child vector. A new route-specific germ producer would compose; the current residue-A D43/K0 family does not. |
| C + reviewed U2 P0-to-P1 theorem | **BRIDGE** | Their union closes exactly two fixed-data first-departure types. This composition is already stated in C and `AUDIT.md:112-123`; it is not full U2 closure. |
| C -> first two-pole case-III Q+E5 child, with the merge state fixed | **BRIDGE** | C gives finitely many `L` and finitely many reduced parent/path records. Q+E5 gives a finite effective pattern fibre for each fixed `(mu,w,mu0,kbar,nu_G)` menu, singleton when `mu=1`. Hence the combined reduced `(L,path,pattern)` set is finite after a typed boundary-to-merge slot map and fixed remaining state data. |
| C -> generic Q+E5/full-cell finiteness | **SCOPE_MISMATCH** | C does not emit the child equality-string/`M'`, actual arrival vertex, neutral index, or all Q+E5 state slots; Q+E5 starts with those state/menu slots fixed. The finite product above therefore cannot yet be quantified over every first P2 child. |
| C -> direct nested `nu=1` producer | **SCOPE_MISMATCH** at this cutoff | C fixes the absorbed base and assumes integral `kbar`; the producer `99bbe233...` varies `(L_inner,L_outer)` on one direct rational-`kbar` edge. It is a complementary provisional theorem, not a consequence of C, and intervening P0 chains/recursive base variation remain open. |
| C -> family-aware landing/compiler | **BRIDGE** | C supplies an exact finite outer-parameter projection for two first-departure types. The compiler must still retain last-vertex indices, actual arrivals, later contexts, and every unclassified transition fail-closed. |
| A/B/C -> `G2-BD` | **SCOPE_MISMATCH** | `G2-BD` starts after a residue-A chart has been reached and, for the promoted implication chain, assumes degree-minimality inside that class. None of A/B/C supplies residue-A provenance, `A-SCALE`, or `UCD-A-min`. Numerical type `(2,3)` alone is not the interface. `ladder/REDUCTION.md:1310-1397`. |
| A/B/C -> degree/type cofinality | **SCOPE_MISMATCH** | A and B fix one td and allow unbounded local indices; C fixes base data and budget. Cofinality needs a uniform finite/bounded type menu and a ceiling across counterexamples. The quantifiers point in the opposite direction. `ladder/REDUCTION.md:1333-1367`. |
| A/B/C -> asymptotic-complement cover | **NO_HIT** | The cover theorem consumes an actual complex Keller map and its target asymptotic curve/components. These local formal/reduced records construct neither `A(F)` nor source/target meridian grouping. The known vertical/horizontal custody gap remains (`AUDIT.md:775-808`). |
| A/B/C -> promoted char-p/Witt theorems | **SCOPE_MISMATCH** | A/B use characteristic-zero Puiseux/weight arguments; C is rational state arithmetic. The Witt lane concerns compatible lifts of a specified finite-characteristic polynomial collision with support growth. No common polynomial source or reduction model is supplied. |
| B first child -> a good-prime falsifier after source extraction | **BRIDGE** | This is elementary reduction, not a Witt theorem: after clearing denominators over a number field and choosing a good prime with `p` not dividing `i`, a pure `i`-th power in characteristic zero reduces to one. A single good reduction with `deg gcd(C_1,C_1') != i-1` therefore rejects the char-zero identity. A modular pass proves nothing. |

## 4. The two useful new interface specifications

### 4.1 `BChildJet_24`: the clean td12 source consumer

The exact source-facing object should be no larger than

```text
BChildJet_24 :=
  (PairRef(f,g), fibre a, branch/root c, completion/evaluation map,
   kappa_F, i, (P_k) needed for child 1,
   recentering data and child polynomials C_1,...,C_24).
```

Required checks are:

```text
for j=1..24:
    denominator_j = kappa_F
    degree(C_j)=i and lc(C_j)!=0
    deg gcd(C_j,dC_j/dz)=i-1
    recenter at the unique root
```

The first failed row is a theorem-grade contradiction to the td12 route.
The extractor can be implemented from an exact pair and branch completion;
it does not require global all-root coverage.  This matches the exact-pair
constructor in
`xmodel/g2-psc-typed-source-to-pole-tree-interface-sol-ultra-20260827.md`
(`4c3f2236a11f296f7eb194bb26e8cdc8ff0b91f10d8d7151e6314caa62629304`)
as repaired by Opus
(`f7de3ae12918c9103d595e81ddc7fcb9082ba950698f70966ade3ea576cc79a1`):
the exact pair and evaluation determine every residual, while the frozen
integer ledger does not.  The report's SOURCE/TARGET schema
(`producer §§2-4`; review `§§6-9`) should gain this finite silent-jet
extractor only when B is a named client.

Formal A/B interpolation is not a substitute.  It proves that a passing
first child is compatible with the top/arrival/residue/degree data; it does
not determine the source vector or couple the g-side Keller recurrence.

### 4.2 `U2EqBoundary -> Q+E5State`: the missing reduced slot map

C emits, up to finitely many P0 path types,

```text
(L; W,M; l,m,kbar),
l|M, 1<=m<l, kbar=lW/(l-m) in N*.
```

The Q+E5 fixed-fibre theorem consumes

```text
(mu,w,mu0; kbar,nu_G; pattern),
nu_G>=2, case III, strict NE/R laws.
```

The cheap composition test succeeds only after a literal map identifies
which boundary slot becomes the nonzero arrival multiplicity/state, which
slot supplies the zero arrival, and how the equality-string child data and
actual last-vertex `nu` enter merge legality.  Once those state slots are
fixed, the finite-product proof is immediate:

```text
finite C outer L/path records
  x finite Q+E5 menu for a fixed state
  x finite Q+E5 pattern fibre
  = finite reduced first-P2 case-III records.
```

The load-bearing Q+E5 files are Fable R3
`885e5cc261449890e23d54cb024f19f339424f6138938d2396c2e655244c7092`
and Opus review
`9660ffe9302061c9daf7bc31c8eda57fb9412315283c868d8d072655d2033570`.
They prove fixed-state fibre finiteness, not the missing map or arrival
coverage.  This distinction prevents the finite reduced product from being
misreported as a finite full-cell ledger.

## 5. Downstream consumer audit and conclusion changes

### 5.1 Live repair required in `ladder/REDUCTION.md`

Three blanket statements predate C:

- `ladder/REDUCTION.md:978-981`: “first P2 boundaries and nested U2 are not
  covered”;
- `ladder/REDUCTION.md:997-1000`: “no theorem covers its first P2 boundary”;
- `ladder/REDUCTION.md:1233-1239`: the next theorem is said to need every
  first P2 boundary before nesting.

**Smallest safe fix.**  In each place replace the blanket first-P2 wording
by:

> Fixed absorbed data and budget now give finite outer `L` both for P0-to-P1
> paths and for a first source-licensed `nu>=2` root-multiplicity equality
> boundary with integral `kbar`.  Still open are non-equality/unclassified
> first transitions, rational-`kbar` `nu=1`, actual arrival/neutral-index
> full-cell data, intervening P0 chains into nesting, and recursively varying
> base data.

The conclusion that no total family-aware landing/full-book theorem exists
does not change.  Neither does the off-axis `0 DEAD / 0 ALIVE / 2691 OPEN`
fail-closed status in `ladder/BOOK-OFFAXIS.md:634-659`, because C deliberately
forgets downstream full-cell consumers.

### 5.2 Sound, superseded, and historical consumers

- `ladder/REDUCTION.md:970-977` remains sound in saying a family-record
  codomain is needed: A kills an infinite formal family uniformly, but the
  pre-kill configuration space still cannot be represented by a finite list
  of literal cells.  The record may now carry terminal verdict `DEAD`.
- The current top overlays in `AUDIT.md`, `APPROACHES.md`, and `PROGRESS.md`
  already retire the td8 route, preserve the td12 row only at the depth-24
  gate, and state C at fixed-data scope.
- Older entries saying the td8 family survives, the td12 pole price is
  unknown, or U2 covers only P0-to-P1 are historical layers explicitly
  superseded by the top overlays.  They are not live consumers and require
  no mathematical withdrawal beyond the existing top notices.
- A changes no other td8 panel conclusion.  B changes no sibling td12
  terminal or full-book conclusion.  C changes no landing, cofinality,
  `G2-PSC`, `G2-BD`, or JC2 conclusion.

## 6. Ranked next exact tests

1. **Extract the first td12 B-child vector from the source identities.**
   This is the only currently decisive promoted finite gate.  Form `C_1`,
   check the binomial identities or gcd, and stop at the first failure.
   Build the 24-level recursion only after level one passes.  A good-prime
   screen may reject cheaply after the exact coefficient model and good
   reduction are certified.
2. **Write the one-page `U2EqBoundary -> Q+E5State` slot table.**  Name every
   parent, child, arrival, `nu`, and multiplicity field from
   `BOOK-OFFAXIS.md:480-540` and Q+E5.  Decide whether the unbounded equality
   string/child `M'` enters a Q+E5 input or only a forgotten arrival witness.
   Either outcome is useful: the former isolates the remaining parameter;
   the latter proves finite reduced first-P2 case-III records immediately.
3. **Integrate C's narrow scope into `ladder/REDUCTION.md`.**  This is a
   documentation repair only; it must not upgrade the global landing verdict.
4. **Review, then compose, the direct nested-`nu=1` producer.**  At this
   cutoff its exact file is
   `xmodel/m2-u2-nested-nu1-boundary-r1-sol56-20260829.md`, full
   `99bbe233f8f6f7273b2e5f89705aa5f2189681ce649a8723a66a0f99fa92912b`,
   body `11e44767...`.  If its edge typing passes, substitute one intervening
   P0 terminal frame next; do not infer recursive variable-base finiteness.
5. **Do no further td8 affine realization work.**  Preserve the local
   artifacts as regression fixtures for exit-set arity and source compilers,
   but allocate mathematics to other M2 families.

## 7. Final scope statement

The composition pass yields one route-pruning bridge, one exact source-jet
consumer, one already-reviewed U2 union, and one conditional reduced
U2/Q+E5 product.  It exposes a stale reduction-ledger sentence but no false
global theorem.  The live proof-wide gaps remain total source/landing and
coverage, full-cell typing, `G2-PSC` where a hybrid architecture uses it,
any invoked residue-A `G2-BD`, and independent type/cofinal degree control.

---

Report-body SHA-256 (all bytes before the separator line above):
`8372baf43819469e975fa3300ccd069fe2f5772e4ad7de2dbd4ce18bc06ae1e7`
