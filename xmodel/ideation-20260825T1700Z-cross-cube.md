# Adversarial cross-scan — cube/max-12 owner — `20260825T1700Z`

Status: **post-seal synthesis; no mathematical promotion**.

Cross-read inputs:

```text
Sol   d2437a9663adb851d2badba0b5fb5249cd7f09620e6c2a8a8b57c1295d90d1bd
AS    08dceaceb8f4945287415f5c9b8f205a6ad9f146a11b624f91ec724905381d60
TD6   0e3eb49e4b48b850bf187fbc1c620ce75235df8744e8690c14b404d43ff43634
cube  b056c9209e9190e5cb81574143efd1a072ef9cbecba521f4061335d47ed2ac1e
```

## Strongest independent convergence

All four reports independently choose the same two-stage fork:

1. impose the proportional/common-homogeneous leading-face incidence on the
   **complete** B8/B9 family before taking another digit or selected point;
2. replace extrapolation from finite counts by an exact finite-type local
   test: Smith/Lyapunov--Schmidt, Fitting/formal smoothness, or a Neron
   dilatation of the liftable centre.

This is consensus about the cheapest experiment, not independent evidence
that a component exists.  The dual-host family computations are one
implementation replicated for custody.

## Shared scope bug

The common-total-homogeneous cubic is being conflated with the nontrivial
partial-`y` Kummer class in several proposed AS-to-Q8 adapters.  For an exact
fixed-total-D12 `(9,12)` map,

```text
a9=h^3, b12=h^4,       deg_total(Q)<=12
```

makes `b12` constant, hence `h` constant and Kummer order one after the
licensed scalar extension.  B8 is likewise order one.  The separate
homogeneous cubic `K` in `P9=aK^3,Q12=bK^4` can vary, but it does not make the
map order three.  Therefore:

- AS's phrase “genuine order-three structure” for its Frobenius-lag total
  cubic is unlicensed;
- no horizontal common-cubic survivor can be fed directly to the **selected
  order-three** corrected-Q8 chart;
- a Rees/denominator-cleared adapter may still prove `NO HIT`, but a positive
  proof client must be the **order-one polynomial-core** max-12 source.

A second guard is that quantitative Hensel cannot start from the displayed
nonproportional B9 point: its `x^3y^9` top-face residual is already nonzero
modulo `3^11`.  Intersect the full family with the common-core incidence
first.  A unit Jacobian minor for a row subset is insufficient unless the
remaining completed local ideal is generated or its residual cokernel is
retained; the peer reports mostly state this caveat, and it must remain an
acceptance gate.

## Single best merged action

Run one source-hashed **normalized B9 core-dilatation gate**:

1. consume the producer-exact normalized `A^162` mod-`3^11` family and its
   emitted 23-row monic common-cubic formula;
2. solve/classify that incidence as a complete scheme, preserving
   Frobenius-nonreduced strata and replaying all 276 determinant rows;
3. on each surviving component, form the `3`-saturated Neron dilatation of
   the determinant-plus-core scheme and compute relative SNF/Fitting data;
4. interpret a smooth `F_3` point as an all-depth Hensel section, a unit as
   death of this registered normalized parent, and a singular fibre as the
   next exact centre;
5. export survivors only to the denominator-free **order-one** max-12
   polynomial-core compiler, never to selected order-three Q8.

This action merges Sol's `CORE-SAT/SMITH`, AS's post-cutoff normalized family
and Frobenius-lag formula, TD6's quantitative-Hensel/source-DAG discipline,
and cube's fixed-cap Kummer collapse/dilatation.  It has a decisive endpoint
at every branch and makes further pointwise digit chasing unnecessary.

