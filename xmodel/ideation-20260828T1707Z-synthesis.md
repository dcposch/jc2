# Ideation round `20260828T1707Z` — coordinator synthesis

## Outcome

No proof or counterexample to JC2 is obtained.  The round produced one
promotable narrow theorem, retired one falsely discriminating calculation,
and sharpened the two active computational lanes.

The root result is the pole-chain statement `ROOT-W2`: at `td=6,m=2`, the
two pole chains cannot first meet at `(0,y)`.  The surviving two-pole critical
object is still the **interior** residue-A germ.  Literal MFE composition is
`NO HIT`: its selected-exit weight is `2`, versus cap `3` at R3 and cap `2`
at R4; the q-only resonant orbit is not a `T_a` direction.

The advertised “simultaneous two-parent edge match” is not a discriminator
at Q-level.  Both parents have the same Q-record and reproduce the same
already-satisfied `6/10` equation.  The corrected top proof/disproof test is
one level deeper: **`SUBST-TRANSPORT`**, tracking the actual Puiseux
substitution and common h-family `ominus` constant from each row-1 pole into
the rigid merge pattern over `Q(sqrt(3))`.

## Custody

The frozen packet was
`xmodel/ideation-20260828T1707Z-cross-packet.md`, SHA-256
`9cc1d824e35abbd9f367f59cc94bfde2f6c93f37a099ee48238211b6bbbec8e9`.
The deliberately unblinded cross-reports are:

| lane | SHA-256 |
|---|---|
| Sol Ultra | `a64a58afca4748cc80e3f5a31d6ec4f72762f3ae7b3926effaf8cc0844424b55` |
| Grok | `13db3c55d308814f90f8e62352e1c3df1cdc5ef42bf24c496b08fcd85d445eb2` |
| Fable 5 | `80c3f0d4dc673395725bd78166b9e14b2ec1067b825b0c70c924e387cdcac678` |
| Opus 5 | `10cee374fb93a359602320d13741ec89953fc2c75001e6be7fc54c98ca880116` |

Each report verified the frozen corpus before reading it.  Later coordinator
edits changed several canonical dependency files while Fable/Opus were
running; both disclosed the drift and rechecked their load-bearing quoted
passages.  The report hashes above are the sealed outputs; current canonical
bytes, not their mid-run dependency hashes, govern promotion.

## Proposal dispositions

| proposal | disposition | exact reason / next form |
|---|---|---|
| `ROOT-W2` | **PROMOTE, narrow repaired form** | MP1+MP4+MP5/D5 give the actual parents in the pole-chain root-meet branch `W(2)={2}`; the mixed-root edge theorem requires `0<w<1`.  Do not broaden this to a root contact after an interior merge involving a non-pole/northeast direction. Integration `645b9424...`. |
| Q-level two-parent match | **RETIRE as discriminator** | The parents are symmetric-identical: `(rho,kappabar,mu,n)=(1,5,1,5)` on both sides; both equations are the already-satisfied `6/10`. |
| `SUBST-TRANSPORT` | **RUN FIRST** | Transport actual coefficients and the `ominus` constant across one edge for both labelled/conjugate pole patterns into the common rigid merge. Stop if two independent free scales make it vacuous or if a common h-index is not source-pinned. |
| coefficient-complete jet lift | **RUN ONLY ON SURVIVAL** | A compatible transport object becomes the exact seed; retain the q-only coefficient and cover every denominator/chart branch. |
| `MERGE-DIFFERENT` | **CONJECTURAL / repair target** | A local different length can see the q-only orbit, but the proposed injection to distinct `T_a` cv flags is unproved and may target a flag-free region. It must land in a certified off-tree/asymptotic or h-branch object, not be added directly to MFE. |
| `RES-AV` | **REJECT AS STATED; source gate active** | No promoted statement currently turns the q-only pattern root into a finite, varying asymptotic value. The domain of the proposed `D_g=kappabar-D` reading and the h-index supplying `q` at the merge must be pinned first. |
| `Q-GHOST` | **REJECT in the original tree** | Translating `g` does not create a branch of the `f=a` contact tree. A transposed-fibration construction at a genuinely proved asymptotic value is a separate conjecture, not a repair already in hand. |
| `A47` identities | **PROMOTE as elementary identities** | For `h_1=g^alpha-s_0f^beta`, `J(f,h_1)=alpha*g^(alpha-1)` and `J(h_1,g)=-s_0*beta*f^(beta-1)`. |
| `A47-BUDGET` | **REJECT pending landing/injectivity** | The identities neither attach q-only roots to geometric h-branches nor bound those branches at infinity. Both sides currently demand the missing landing data. |
| parent swap / Galois inversion | **REJECT as a kill** | `2+sqrt(3)` and `2-sqrt(3)` exchange the two labels of one unordered pole pair over `C`; no fixed-point condition follows. Keep only as a substitution consistency check. |
| Fitting/adjugate GGV hybrid | **RUN** | The working ambient-NF adjugate adapter supplies local proof objects; Fitting rank levels supply a canonical decreasing cover invariant. No dense-open-to-component inference. |

## Root consequences and firewalls

The mixed-root integration `aaa4d179...` is edgewise and valid for arbitrary
incoming multiplicity:

```text
X_root = mu_e(1-w_e) = A/B,        0 < w_e < 1.
```

If some `mu_e=1`, repaired MP6 gives the stronger `A=sum(mu)`, `B=r+l`,
`M_root=gcd(sum(mu),r+l)`, and `psi=r+l-1`.  None of those stronger formulas
is exported to an all-`mu>=2` root.  The reviewed sector table is integration
`4d3b36f9...`: it closes several filed pole-chain root perimeters through
`td=12`, but explicitly leaves hidden post-jump/off-axis routes open.  In
particular `td=12,m=4` can hide all four row-1 leaves behind a `2+2` pair of
inner `M>=2` emissions.

The old off-axis “2691-cell superset” also failed this audit: its generator
imposed `M_G | sum(mu_e)` even on all-mixed nodes, although that divisibility
requires the conditional `epsilon=0,k=0` anatomy.  The rows are now labelled
conditional skeletons, with an explicit
`OPEN_UNBOUNDED_MIXED_OR_POSTJUMP` rider; they cannot certify completeness or
support a kill.

## Software / compute synthesis

The GGV route remains fail-closed and AWS-only.  The repaired complement
adapter has now passed one complete `Q1P03` node, which legitimately triggered
the five-branch fanout.  Every node must retain:

- frozen ambient ideal, transform, pivot, endpoint, generator, and script
  hashes;
- zero placeholders, stderr, parser diagnostics, and swap;
- exact rank witness plus exhaustive next-minor vanishing;
- all bordered and original-row kernel identities;
- all endpoint coefficients in ambient normal form;
- typed/stable saturation, explicit empty-open power certificate or proper
  complement;
- a planted-nonzero semantic control such as `NF(E+1)!=0`;
- only `EMPTY`, chart-local `ENDPOINT_DEAD`, explicit `SURVIVOR`, or
  `NO_VERDICT_UNCOVERED` banners.

One `TRIPLE03` branch correctly failed because it recomputed pivots after
saturation and changed the transform basis; the repaired adapter freezes the
node transform and base-changes it into the active quotient.  This is adapter
evidence only, not mathematical evidence against the branch.

## Immediate execution queue

1. Finish `SUBST-TRANSPORT` and the source audit identifying the h-index/q
   pattern at the residue-A merge.  A contradiction enters hostile review;
   a compatible result emits one coefficient-complete seed.
2. On a compatible seed, run the guarded next-jet lift and compute the first
   genuine off-tree/asymptotic or h-branch object.  Do not manufacture an
   MFE charge.
3. Harvest the six AWS complement branches.  Advance by Fitting rank level;
   every proper remainder stays open until explicitly covered.
4. Record the `A47` identities, but spend no campaign time on its budget
   until landing/injectivity exists.
5. Keep the single-pole/SF1 and post-interior root-contact questions separate
   from the promoted pole-chain `ROOT-W2` branch.

## Corrections banked

- The row-1 pole record is `Q=(2,2,2,1,5)`, not
  `Q=(2,2,2,2,5)`: `M=gcd(2,3)=1`.  The old typo caused no downstream
  numerical error because `w=(5-2/2)/2=2` ignores that slot, and the proofs
  already used MP4's correct `M=1`.
- Fable's blind-round provisional `td=7` entry `(a,b,nu)=(2,1,3)` with
  `Lambda=4` is not in the canonical `td=7` entry artifact and is not
  promoted.
- Opus's generic `ROOT-REACH` table included entry tuples not passing the
  exact entry constraints; it is not a completeness oracle.
- Fable's statement that every at/above-merge different target is flag-free,
  and Opus's competing `m_F` interpretation, remain under a direct source
  audit; neither is promoted by this synthesis.

No local heavy computation was run for this synthesis. `jc2-lean` was not
entered, listed, searched, read, built, modified, status-checked, or
controlled.
