# Whole-portfolio synthesis — round `20260827T1349Z`

Date sealed: 2026-08-27 13:49Z  
Date synthesized: 2026-08-27 15:18Z  
Coordinator: Sol

Status: **COORDINATOR NAVIGATION AND ALLOCATION DECISION.**  Evidence keeps
the tier and scope of its producer/review artifact.  This synthesis does not
prove or disprove JC2, a GGV family, Gate T, order two, maximum twelve,
terminal reachability, either `G2` obligation, or global coverage.

## 0. Custody, blindness, and equal standing

The common packet and seven whole-portfolio submissions are:

```text
packet               8af21da712a3de8184ca708d75e7461e831f597aac459317d8871176e73b48c3
Sol                  f493c8f35b52d923be4c5a60368433f83869a9269325099fd3fb1be1d392ceea
Fable5               2421096121410a0895245649204815b8efee337873567cb9e277971294ae2796
Opus5                1c17b61f00079802b20fc459550eea7bc7b09bcdcc98f27c233bde91cf348968
Grok                 941aea0c2a664feb10abca88c9daeb4b07e0c8a2f7265f6f0b7e67f3e5bd2b61
a1_total_lift_design 87487715bd86bb5a6069e0b7f5e0a8c86c57982addae0799b6a9d5b8d2939553
actual_total_g20     b0b9c40a3c29b912f066cc19fc7cd400fd737c0958692dccf0979777170d1fe5
k00_closure_incidence 16bb2575f3cc82a38fd52061d3dbc16ccf9f46132a63aae87ce0b9867703be52
```

Sol, Fable5, Grok, and all three specialist agents were clean-blind.  Opus5
reported one contaminating repository-search line from Grok's submission:
the phrase that the integrating factor of `M` is `H^(-1/2)`.  It opened no
peer report, excluded all round reports immediately thereafter, disclosed the
incident before making novelty claims, and supplied execution chronology
showing that it had derived the identity first.  Its submission is therefore
**BLINDNESS-DEGRADED-BY-ONE-LINE**.  The integrating factor is treated as
convergent, not Opus-unique; the closed-form general criterion beyond that
line is adjudicated separately.

Fable5 and Opus5 received the identical packet, maximum-effort setting,
permissions, shell access, output contract, and no-peer rule.  Both completed
all 46 avenue dispositions and every required branch/card/lifecycle item.
Model identity receives neither a vote nor an evidence bonus.

## 1. Post-cutoff lifecycle events

Two packet premises passed without a scope change and are already promoted:

1. **C5 V2:** exact division-free ordinary-ring membership
   `a1^628 in J_total` using 25 literal rows, at the frozen raw ordered-`a1`
   grade-through-19 scope (`0ab2a7ef...` hostile review).  It closes only the
   raw `D(a1)` chart.
2. **GGV R3:** exact nonexistence of the charged formal jet on the artificial
   squarefree replacement edge `F0=(X^8-1)^2`, `G0=(X^8-1)^3` through weight
   22 (`27fcd256...` hostile review).  It does not exclude the original
   `8_28` object or a GGV family.

The first C5 child has also returned a precise reviewed typed obstruction.  V43C6
freezes all 67 source-symbol images and the 25 C5 row hashes.  V20R2 contains
no serialized C5 ring/row map; on the smallest reviewed V47 endpoint
`A1D2_R3`, the honest contact map sends stage-zero `a1=Az_0` to zero in a
nonzero domain localized at `(-2 rho^2)k10_0`.  Thus `a1` is not a unit, the
localized C5 map cannot exist, and the certificate transports only to the
tautology zero.  Fable5 independently confirmed the full 67-symbol map,
25-row custody, 28-defined/39-missing census, nonzero target domain, and
localized-map obstruction (`cee2a228...`, freeze `394f5390...`, review
`ad855507...`).  Promote only the named endpoint-class transport obstruction
and V20R2 interface unavailability; it licenses no inference in the positive
direction and does not refute C5.

K00's three-prime discovery preflight at `p=32003,65519,65537` joined the
earlier `p=65521` result: every localized prior ideal is the unit ideal.  This
makes `65521` look nonexceptional and raises the expectation of exact-Q
emptiness, but remains theorem-ineligible modular evidence.  A later exact-Q
R6R1 certificate provisionally resolves the selected minor chart:
`W=A1Q1+A3Q3+A4Q4`, so `D(W)` and its normalized `D(k10_0W)` embedding are
empty (`8da4f616...`, additive replay `8cd2afdd...`, internal audit
`f9d2afcb...`).  Producer and auditor share a model family, so Opus5
cross-review is live.  The long exact-Q jobs remain cross-checks.  The chart-
free grade-seven Fitting/degeneracy atlas is released only as a rollback-
tagged descendant, with `W=0` retained and redundant on the base scheme
rather than treated as a separate hypersurface.

V24's separate Opus5 hostile review also passed (`d547baa2...`; adjudication
`0bd97113...`) at its exact conditional scope.  In the normalized
valuation-one source after `k6_0=0`, over the prior ring localized at `W`, the
literal grade-seven matrix has rank five and a free rank-two left kernel; its
inhomogeneous system is solvable exactly when the frozen compatibility
polynomials `Comp1=Comp2=0`.  This theorem neither proves that the localized
prior chart is nonempty nor treats `W=0`, later grades, an arc, or a receiver.
Its old modular-zero JSON field is immutable history but withdrawn as
mathematical evidence.

Two post-round producers are intentionally kept provisional.  First, the D3
raw-global interface stopped correctly at the absent cleanup DAG
(`c7ea900b...`).  A proposed cleanup discriminator has the exact polynomial
identity

```text
u=H+t^6 X-(1/2)t^8,       R=t^14 X-(1/4)t^16,
u^2+R=H^2+2t^6HX-t^8H+t^12X^2.
```

The first D4 freeze incorrectly labeled the X-dependent remainder `R` as the
Morse critical-value series `U(t)`.  It is quarantined before promotion and
its Fable5 review was stopped.  The additive repair must evaluate the actual
49,964-node critical-section DAG under this specialization and prove the
factor-algebra statement `U14=c`; until then even the support-discriminator
conclusion remains provisional.  No global `E22` exists.
The additive D4R1 repair now supplies that provisional evaluation, with
`U14=c`, `U16=-1/4`, `U20=-c^2/8`, and `U22=c/16`
(`8b3dd3c...`, freeze `6beafdb4...`).  Fresh Fable5 hostile review has now
independently confirmed the full 49,964-node factor-local compiler and this
evaluation (`971816d0...`).  The review also shows that four legal rows
already give `U14=c`, so five-row minimality is not promoted.  Descendants
may consume only this factor-local scope; the local/global naturality square,
lower Keller rows, and global `H`-multiple remain separate obligations.
Second, R4 (`11cad1db...`, freeze `9235df1b...`, Grok review
`5187676b...`) extends
R3's artificial replacement-edge mode calculation to every nonconstant
squarefree `H`: the six rational modes remain complete through weight 22,
and the endpoint is excluded for `deg(H)>=2`, while degree one is only
silent.  It also finds the exact nonsquarefree stop `H=X^2`, where
`t^22 F^(-5/4)=t^22 X^(-5)` is a new kernel mode.  Independent Grok hostile
review confirms this exact formal-edge scope.  Raw provenance and genuine
GGV landing remain open.

R5 then continues across arbitrary multiplicities
(`fd164042...`, freeze `3d8ba267...`, Opus5 review `48fff5d5...`).  At weight `n`, a rational mode exists
exactly when `4 | e_p(12-n)` for every factor multiplicity.  Combining its
exact lift with the audited `H=A^2B` endpoint criterion reduces degree-eight
edges to squarefree-part degrees zero or two; degrees four, six, and eight
are excluded by degree, and the normalized quadratic survivor has sole
equation `4a0+D*a2=0`.  Opus5 independently confirms every mathematical
claim; the frozen regression harness has serious coverage gaps, so an
additive strong harness remains due.  Promote R5 as a necessary rational-
endpoint filter only.

D5G has since provisionally crossed D3's review-independent raw-global
custody wall (`59e9bf2c...`, freeze `838b1160...`, internal audit
`14913814...`).  A
separate sparse implementation matches all 32,135 combined terms and 58,572
source contributions in the literal generic `D0,...,D22`, as well as both
`D22=H*Q22+R22` and the seven-coordinate `M` reduction.  The exact
`D22 -> D22+H` mutation changes the global quotient and `M` class while
leaving the factor remainder fixed.  Producer and auditor share a model
family, so different-model Opus5 cross-review is live before promotion.  The
maximum possible scope is this raw-coordinate compiler/custody result; lower
Keller equations, the target, local naturality, and every face conclusion
remain absent.

R5's confirmation releases R6's dependency.  Its internal audit
(`44d2d8a0...`) supports codimension interpreted on constructible closures
and equal to `floor(h/2)` for the full survivor locus, but a different-model
Opus5 review is live before promotion.  R7's core algebraic
characteristic identity passed its first audit, but three claims required
repair (`2dd9d8f1...`).  Additive R7R1 (`9d35c678...`, freeze
`489647cf...`) records the full kernel `C_L[[s]]`, restricts four-character
language to the appropriate `mu4` base change, and deletes the false
suggestion that bounded support truncates the tower.  An exact target gives a
coefficientwise de Rham tower on `p^4=H`; a target only through weight 22
gives `q0` alone, while `q2=F2/(4H)-F1^2/(16H^3)` needs rows through weight
24.  Different-model Fable5 review is live; no R7R1 claim is promoted yet.

## 2. Correctness-adjusted round decision

The seven reports reduce to four mathematical objects and one intelligence
object; superficial card count must not create duplicate launches.

### 2.1 K00 exact incidence — highest immediate binary

All seven reports agree that another serial Cramer expansion is the wrong
default.  The selected `D(k10_0W)` prefix is provisionally closed by a
replayable exact certificate, while `W=0`, other minor charts, later arcs,
and the receiver remain open.  Pending different-model review, the rollback-
tagged honest successor is the whole chart-free Fitting/rank locus, not a
separate `W=0` cut, because `W` lies in the leading base ideal.

The apparent Fitting-versus-dual disagreement is complementary rather than
binary.  Use exact left duals to generate the smallest successive
compatibility hypersurfaces, and require an exact nonemptiness certificate
after every cut.  Use the preregistered Fitting atlas to make the statement
representation-independent, retain rank-drop/`W=0`, and check agreement with
V24 on `D(W)`.  Do not materialize all minors of a `140 x 169` matrix merely
because they define the correct functor.

### 2.2 GGV global endpoint — one finite interface plus one new differential instrument

Grok's polynomial operator observation is exact.  For

```text
H=X^8-1,       M(Y)=4HY'+6H'Y,
M(X^d)=4(d+12)X^(d+7)-4dX^(d-1),
```

`M` is injective and

```text
Q[X] = im(M) direct-sum Q[X]_(degree<=6).
```

The canonical reduction is

```text
[X^7]=0,       [X^n]=((n-7)/(n+5))[X^(n-8)]  (n>=8),
[H]=-(12/13)[1].
```

This was independently proved twice.  The required correction is equally
important: multiplication by `H` followed by the remainder map is an
isomorphism on the seven-dimensional complement,

```text
r(H X^i)=-(12/(i+13))X^i,       0<=i<=6.
```

Therefore an unknown global `H`-multiple can move in **every** cokernel
direction.  Rootwise R2 carrier patterns do not determine one intrinsic
seven-vector.  The theorem is a compact final test only after a raw compiler
has produced a literal global polynomial endpoint.

The sound merged interface is:

```text
literal raw 2S/3S jets
  -> provenance-constrained (V8,U11,U14,sidecars) in Q[X]/(H)
  -> one globally serialized polynomial E22 in Q[X]
  -> rem_M(E22) in Q^7.
```

Inside the middle arrow, the factorwise endpoint condition is

```text
H'*(6 V8 U14 + (9/2)U11^2)=1  in Q[X]/(H),
```

with the R2 zero/nonzero branch restrictions and every factor, conjugation,
deck, chart, and orientation tag.  This finite-etale composition is the
correct successor.  It is not `G2-PSC` and does not bypass raw provenance.

The first fail-closed implementation has already reached the intended stop
condition (`c7ea900b...`, freeze `012acfe5...`).  It freezes 442 raw slots,
the literal R0 face, and every factor/deck/orientation tag, but refuses to
emit a global `E22` or seven-vector because the required
`local_carrier_fixture.raw_to_morse.cleanup_DAG` does not exist.  The concrete
first mismatch is R1's firewall: raw `F14` contains only `X^2`, while the
local carrier fixture uses `U14=X`.  The next child is therefore the explicit
raw-to-Morse cleanup DAG, not another endpoint reducer or branch enumeration.

That child produced the exact remainder identity recorded in Section 1, but
self-audit caught that its first freeze overlabels the remainder as the
critical-value series.  Keep D4 quarantined until an additive freeze evaluates
the actual cleanup DAG and checks `U14=c` in `Q[c]/(c^8-1)`.  D4R1 has now
done so provisionally and is under fresh review.  Even a reviewed PASS would
not supply polynomial gluing or a global endpoint.

Opus5 contributes a second, genuinely distinct instrument.  It rewrites the
R3 endpoint ODE as algebraic de Rham exactness on the boundary curve:

```text
M(Y)=4 H^(-1/2) d/dX(H^(3/2)Y).
```

For `w^2=H=X^8-1`, with `omega=dX/w`, exact reduction gives

```text
[w dX]=[H omega]=-(4/5)[omega] != 0.
```

More generally, write `H=A^2B` with `B` squarefree.  The proposed rational
endpoint criterion is

```text
exists g in k(X): 2H g'+H'g=2H
  iff exists v in k[X]: A=Bv'+(3/2)B'v.
```

Independent hostile audit proves this endpoint criterion (`d382c21f...`) and
the general `(alpha,beta,N)` algebraic formula, with four repairs.  The bridge
from `M(Y)=1` uses `g=4HY`; a reduced denominator divides the even-root factor
`A` and need not equal it; injectivity/codimension of `N_B` is stated for
`deg B>=1` while constant `B` has a kernel and is surjective; and a Kummer
cover must be normalized/minimal with its character tagged.  The smallest
subsidiary counterexample is `H=X^2`: `g=X/2` has denominator one, not `A`.

The general-H rational-mode completeness and denominator-provenance theorem
for the *preceding GGV system* remains open, as do raw provenance and landing.
Hence the proved endpoint criterion is not yet licensed as a GGV-family or
cofinal exclusion.  Its correct first use is a cheap endpoint oracle and
positive/negative fixture inside the raw compiler; the mode bridge is the
next theorem obligation.  Grok's separate hostile review independently
confirms the core criterion and examples (`066095c4...`), refutes any present
degree-uniform GGV license, and sharpens the general formula: radical branch,
primitive-cover normalization, character eigenspace, and the requirement that
the primitive lie in the specified `K(X)` line are all load-bearing.

Fable5 supplies an orthogonal arithmetic refinement.  In the normalized R2
chart, branch two implies

```text
U11(c)^2=lambda*c/36.
```

Hostile audit confirms that over `Q`, branch two is allowed on `Phi1` iff
`[lambda]=1`, on `Phi2` iff `[lambda]=-1`, on `Phi4` iff
`[lambda]=+/-2`, and never on `Phi8`; for `lambda=1` only `X-1` is allowed.
The formal Morse normalization stays in the residue field.  Repairs: this is
arithmetic descent metadata in the pinned normalized chart, not an independent
complex `G2-PSC` field; R2's mixed fixture remains valid as a typing fixture;
and the obstruction vanishes over `Qbar`/`C`.  Use it to prune exact-Q
compiler/search controls, never to exclude the complex family.

### 2.3 Total certificate — bank the leaf and stop local overinvestment

C5 is now a reviewed theorem at its narrow scope.  Its failed first transport
attempt is exactly the desired fail-closed result: shared symbols do not make
a row map, and an endpoint sending `a1` to zero cannot consume a `D(a1)`
certificate.  Stop new exponent ladders and broad certificate-to-receiver
claims.  Preserve any already-near compression result only if it materially
shrinks a future typed proof object.  A `t^s` thickening profile may be useful
as independent certificate/custody insurance, but it cannot outrank K00,
global provenance, or uncovered faces after the reviewed C5 identity exists.

### 2.4 Counterexample realization

Keep two independent clients:

1. a source-reachable exact K00/formal survivor, compatible at every grade,
   followed by a finite-support/conductor algebraization theorem and a literal
   collision; and
2. residual AS109 `n=6` on the surviving content strata, under one fixed
   polynomial support, with exterior polar cancellation and exact `J=1`.

Finite compatible jets are not arcs; arcs are not polynomial maps; all-Witt
growing-support limits are not characteristic-zero polynomial
counterexamples.  The new superelliptic endpoint oracle may name surviving
face classes for guided search, but only after its mode interface is typed.

### 2.5 External claim and square-free clue

The fastest independent discriminator is now consensus.  A Keller map gives
an etale morphism, so the pullback of a reduced principal divisor is reduced;
naive square-free preservation is therefore automatic and cannot itself be
the missing JC2 implication.  Any leverage in `5''s` must lie in factor
descent, connectedness/irreducibility, square-factorial/root closure,
integrality, or a dimension-two theorem connecting one of those to degree
one.

Acquire SSRN 7229358 first and 7229458 second by a lawful interactive,
author, institutional, or library route.  Hash both.  Transcribe the exact
definition and implication graph before reading downstream prose; compare it
to the known square-free-preservation equivalence; then audit the first
dimension-two step for hidden injectivity, finiteness, properness, image-UFD,
normality, factorial closure, `R^0 cap A=R`, or a false dimension count.  No
abstract-level reconstruction is attributed to the author.

The internal bounded client is a divisor-splitting/parity atlas: separate
reduced pullback, factor/component orbits, and descent of the corresponding
principal half-divisors on one GGV control and one collision/automorphism
control.  This connects avenues 7, 25/26, and 31 without starting a broad
Jelonek or monodromy program.

## 3. Correctness-adjusted model evaluation

### Fable5

Fable5 completed a clean-blind, full-contract report faster and more
conservatively.  Its distinctive R4 square-class table survived independent
hostile audit at the core arithmetic and residue-field-descent level.  It
also gave the clearest closure-ladder preparation for the external claim and
correctly kept global landing, terminal incidence, and uncovered faces above
local certificate work.

Corrections matter: Kummer/square-class techniques already existed in
pre-round campaign history, so novelty is the R2 factor table/application,
not the mechanism; the datum vanishes over the algebraic closure and is not a
complex-family exclusion; and the R2 `V8=X^4+1` example remains a valid local
typing fixture.  Fable5 remains an equal-standing whole-portfolio researcher,
with no exemption from scope/history review.

### Opus5

Opus5 was slower and its blindness is discounted by the disclosed single
search line.  It also over-ranked the cofinal consequence before proving the
general-H mode bridge, used the refuted “exact denominator” wording where a
denominator divisibility statement is correct, and proposed a
`t^6` corroboration lane whose priority falls sharply after C5's independent
hostile PASS.

Nevertheless it adds significant capability over and above Fable5.  Its
now-independently-proved superelliptic/de-Rham endpoint criterion, general `(alpha,beta,N)`
formulation, Pell-versus-exactness regression fixture, and explicit
two-sided survivor class are nonduplicate.  It tested the closed criterion
against an independent reference on 33 cases and recorded the counterexample
that killed its own first guess.  Its K00 proposal also contributes a useful
operational rule: every dual-generated cut must carry an exact nonemptiness
invariant before the next cut, with Fitting used for invariant custody rather
than blind determinant expansion.

Together with its prior unique Groebner-free V21 proof, proof-object/custody
reviews, and unusually strong self-correction, this is materially
nonduplicative capability.  **Retain Opus5 as an equal-standing
whole-portfolio researcher.**  This is not a permanent role restriction:
Fable5 was the cleaner ideator in this round; Opus5 supplied the higher-risk,
higher-upside mathematical connection; both require the same hostile-review
and history firewall.

### Other lanes

Grok supplied the exact seven-dimensional polynomial cokernel and the most
explicit K00 Fitting redesign, but initially suggested more branch-vector
determination than rootwise data licenses; the two independent audits repaired
that.  The three specialist agents supplied the finite-etale raw composition,
automatic-reducedness discriminator, bad-prime certificate tower, and the
first C5 typed-transport obstruction.  Sol's unique contribution was the
principal-divisor parity/monodromy connection and the merged launch/rollback
architecture.  Agreement is deduplicated, never vote-counted.

## 4. Merged launches and stop rules

### Launch 1 — exact K00 gate, then dual/Fitting atlas

Already running/preregistered.  Exact-Q returns either a replayed unit
certificate or a proper component/witness.  On survival, generate the next
compatibility by an exact dual, prove the new locus nonempty over Q, and use
the chart-free Fitting atlas to retain rank-drop and `W=0`.  Stop at the first
exact unit or after one grade-seven plus at most one cheap successor without
new codimension/rank information.

### Launch 2 — raw GGV endpoint compiler plus endpoint oracle

Already launched at prototype scope.  Freeze literal raw rows; serialize the
finite-etale carrier equation, sidecars, factor/deck/orientation fields, and a
global `E22`; emit the seven-coordinate polynomial remainder.  In parallel,
hostile-review the superelliptic rational endpoint criterion.  If it passes,
rederive mode completeness first for general squarefree `H`, then for
`H=A^2B`; only after that bridge may the oracle be run across a GGV family
table.  One artificial control and one genuine complete chain precede any
fan-wide sharding.

The prototype has stopped at the first missing provenance field rather than
fabricating an endpoint.  Build and review that raw-to-Morse cleanup DAG
before any later part of this launch is considered reached.

The general-squarefree R4 theorem has now passed independent hostile review
and replaces the degree-eight-only artificial R3 endpoint by the same formal
theorem for every squarefree `deg(H)>=2`.  The explicit `H=X^2` resonance
forbids naive extrapolation, while provisional R5 supplies the correct
multiplicity-dependent mode schedule and endpoint filter.  None of these
outcomes changes the requirement for raw GGV provenance.

### Launch 3 — external dependency audit

Keep lawful acquisition live at low compute cost.  Prepare the exact
square-free/factor-closure implication ladder and controls now.  On receipt,
run two blind line-by-line audits.  Stop at the first fatal implication; bank
any independently valid lemma and do not let a failed external headline alter
the internal exact allocation.

### Continuous capped disproof

One K00/formal algebraization object and residual AS109 only.  No new blind
formal windows, W2/W3/top-band duplicates, ambient random search, or finite-
field census.

## 5. Major-lane decisions and allocation

| lane | decision | immediate rule |
|---|---|---|
| C5 total certificate | **bank / narrow** | Reviewed raw chart closed; V43C6 confirms the first named transport is unavailable/zero. Stop alias-based receiver maps and exponent ladders. |
| K00 common mixed | **continue, redesigned** | Exact-Q binary first; dual cuts with exact nonemptiness, Fitting custody including `W=0`. |
| GGV R1/R2/raw global | **continue and raise** | Raw-to-global polynomial endpoint, seven-vector test, superelliptic endpoint oracle under an explicit mode dependency. |
| Global landing / `G2-PSC` / `G2-BD` / cofinality | **continue at top proof priority** | A face endpoint is an input, never the transport/coverage theorem. |
| V47/V48 strict compiler | **stop as a research lane** | Bank bounded infrastructure; work on equality, positive-load, ramified, terminal, and off-family faces. |
| Ramified/equality/off-family coverage | **continue** | Select from a global coverage inventory, not serial curiosity. |
| AS109 | **continue narrowly** | Surviving `n=6` content strata under fixed support; no duplicate floors. |
| Formal/TD6 | **continue at registered caps** | New work requires source reachability or an algebraization invariant. |
| Matysiak/intelligence | **continue bounded** | Lawful acquisition, dependency first, no abstract theorem. |
| Dead/refuted generic ladders | **stop** | No revival without a named new client and decisive test. |

Reallocate the freed ordered-chart share as:

```text
28%  global landing, GGV raw provenance, G2-PSC/G2-BD separation, cofinal work
27%  terminal/K00 exact incidence and H_K00
17%  ramified/equality/positive-load/off-family coverage
10%  C5 custody/typed integration and other near-complete chart leaves
12%  disciplined counterexample realization (AS109 + one formal object)
 6%  intelligence, external audit, synthesis, and proof infrastructure
```

Review remains asynchronous.  Safe descendants name premises and rollback
subtrees.  Heavy algebra remains AWS-only; modular output schedules work and
never establishes a characteristic-zero result.

## 6. Scope firewall

- `a1^628 in J_total` is one raw chart, not a receiver or a global theorem.
- Failure of a typed C5 transport is a transport result, not a refutation of
  C5.
- Four modular K00 unit ideals are not an exact-Q unit certificate.
- The polynomial `M` cokernel is seven-dimensional, but the unknown
  `H`-multiple spans it; raw provenance is still fully live.
- A rational endpoint exactness theorem is not mode completeness, GGV
  landing, `G2-PSC`, `G2-BD`, or a cofinal ceiling.
- Fable5's square-class table is an exact-Q descent firewall, not a complex
  exclusion.
- Reduced pullback is not factor descent, factorial closure, properness, or
  automorphy.
- A finite jet is not an arc; an arc is not a polynomial counterexample.
- Matysiak remains `ACTIONABLE-UNAUDITED / BLOCKED-BY-PRIMARY-TEXT-ACCESS`.
- The user-owned `jc2-lean` work is separate and untouched.
