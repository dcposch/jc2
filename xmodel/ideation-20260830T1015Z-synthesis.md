# Synthesis — significant-news ideation round `20260830T1015Z`

Coordinator: Sol 5.6 Ultra  
Frozen synthesis basis: `f02aba6913ec6f9ecd1f3ae103475cde05f3ee29`  
Round lifecycle: **BLIND COLLECTION CLOSED / SYNTHESIS PROVISIONAL WHERE
FOCUSED REVIEWS ARE LIVE**

## 0. Custody and outcome

The common frozen packet had full SHA-256
`ce5b9564a8f5cab7eea146e0408bc32950ff8a168a678f340d682fba4e621c58`.
It was given independently to three Sol 5.6 contexts, Fable 5, Opus 5, and
Grok 4.6.  The sealed submissions are:

```text
6dd0d7f47e813b61cad930ddbae908e5a56fbd07662f613a117b39d9e03c5d12
  Sol root; body eb595c1192d88e1e56cdcfc7e59026ec562c331171058d706032e6bc163f35d2
b1485ce38897765d39644831f55c714bcab6ab6a1b9ad0f3718f16c1752bda5f
  Sol global; body ec4b285f3efd17ca5fd01a0e8a32840b707d17997961fdd1eebe9fd3e24be0c7
36240d977254238a9e40882c80e129ce0c3064f44a0e8a608be567058194b3f3
  Sol local; body da71bf4d728ecd2bb5c5c36fb0a85e9b3befc2781e3d8b9fd0d537fe28fc1998
4cda89526079cfeee51fab005fa108986f09ef8408c5d67f7efc81e3748d539a
  Fable 5; body 3f9db83fe78f1f46c7b7cbff3edee9f26547c7a96abec2e2b6d9052e104af5c7
  receipt f9c4551cebbede94de0e59c010ce19f529e770b06d5b5b140f9b3469a08f774c
0345e4973092fb621a64bde13b0cba71adeabd640e0baaba4ec215ff8c8cd3a5
  Opus 5; body 16cf3e290d423ba084dab92c105ab50882532d87f086552d561d34cff3b51092
  receipt 0bf235538af31bfc106d326d2e25b850ef12c6347114299ef2a909a5c37e598d
```

Grok returned HTTP 402 before model work.  Its receipt
`ac4784bb8a0a36f228ee08f841010ec45b3183d4b4f7bc2748a734dc264656cb`
and log `11aba62d...` are `UNAVAILABLE_NO_MODEL_WORK`, not a mathematical
submission or dissent.

Opus disclosed that the shared working tree advanced during its session.  Its
load-bearing charged mathematical files remained byte-identical, but it read
the later q=8 lifecycle line in `AUDIT.md`.  Its mathematics remains useful as
an idea/provisional derivation; its agreement about q=8 is not an independent
blind vote.  Fable and Opus both declared an internal basis different from
the launch receipt basis; the receipts and charged-byte hashes, rather than
those prose declarations, control custody.

Two decisive post-cutoff facts are incorporated rather than pretending the
round froze time:

1. different-model review and binding integration have promoted every
   singular-F5 q=8 row empty in the charged normal, reduced,
   finite-near-infinity fixed-quadratic presentation; and
2. the sealed q=6 local audit
   `48717ce3d8b3c5ef6926e3b5ebcf2a0d94ad7bee42ea4ea1c253fb4fd9463649`
   (body `941d4e27...`, manifest `222d1bff...`) found a real `B3,tau=0`
   correction that invalidates every submission's blanket q=6 total vector.

No submission, review, or synthesis proves JC2 or constructs a counterexample.

## 1. Consensus, disagreements, and portfolio disposition

The strong consensus is operational rather than a new global theorem:

- q=6 is the closest finite proof-side discriminator, but it is a theorem
  about one fixed quadratic presentation and cannot substitute for the
  selector;
- the selector/occurrence arrow remains the largest global proof gap;
- K00 should move from isolated rows to source completeness and
  scheme-theoretic transition images;
- a portable contact-labelled forest/lattice kernel is the best immediate
  mathematical-software investment; and
- review should run asynchronously while reversible descendants proceed.

The numbered portfolio changes conservatively as follows.

| avenues | synthesis disposition | reason |
|---|---|---|
| `28` | **raise execution priority** | Forest, Euler, and ruled-lattice arguments now remove actual cells; q=6 is a finite live client. |
| `31` | **raise execution priority, not truth tier** | Intrinsic discriminant/index work and K00 normal filtrations now have exact clients, but global integrality is untouched. |
| `4,36` | **unchanged; lower row-only/random sublanes** | Source coverage, transition images, and typed falsifiers dominate another isolated grade or generic sparse lottery. |
| `2` | **unchanged principal** | No round submission supplies `G2-PSC`, total landing/coverage, QCS, or a cofinal ceiling. |
| `7,26,30,32` | **hold pending discriminators** | Fable's complement-pi1 idea, Opus's ruling transfer, and Sol's trace-projector QCS are new clients, not reviewed theorems. |
| all others | **unchanged** | No new evidence warrants a tier change; closed/refuted routes stay closed. |

The apparent disagreement about avenues 26/30 is resolved by typing.  Opus
proposes a presentation-free ruling on the intermediate block surface; the
promoted Miyanishi--Sugie input is stated for a **smooth** affine surface,
whereas the block theorem presently gives a **normal** affine `Y` and does
not visibly prove `Y minus branch(g2)` smooth.  A focused clean-room audit is
live.  Until it resolves that hypothesis, this is a high-value composition
test, not a raise in theorem status.

## 2. Q6: exact post-round state

All four substantive researchers rediscovered the marking-corrected identity.
For a strict prime

```text
C_j=a_j*S0+(2a_j+epsilon_j)F-sum_l x_jl P_l,
```

the exact pair term is

```text
C_j.C_k
 =2a_j a_k+a_j epsilon_k+a_k epsilon_j-sum_l x_jl x_kl.       (2.1)
```

This is the correct q=6 replacement for the q=8 formula, because the total
has one residual `S0` contact and one residual `T` contact.  Positive raw
intersection is a forest contradiction only after subtracting the contact
already consumed at prescribed common infinitely-near centres.

The verified local audit now fixes the branch table.

```text
U3/A3:
  4+4, coefficients one, m=(2,2,1), n=2e1+e2,
  c=(0,1,1,2,2,2,2,2,2).

B3/A2, tau!=0:
  m=(2,1), n=3e1; generic 4+2+2;
  at Delta=0 the weighted degree-four block is classified by its exact
  Weierstrass discriminant and may split, remain irreducible, or double;
  c=(0,1,1,2,2,2,2,2,2).

B3/A2, tau=0:
  m=(2,2), n=(2,2); fixed 4+2 plus a weighted degree-two E2 block;
  c=(0,2,0,2,2,2,2,2,2) up to chronological outside relabelling;
  no charged contracted carrier is numerically possible.
```

The family `f=h+uv` realizes the coefficient-two vertical-prime case, so it
cannot be deleted as a formal nuisance.  In any split double-site block, the
two branches may have local mutual contact
`lambda=ord(discriminant)/2`; only `C_i.C_j>lambda` gives an additional path.

Consequences for the blind reports are exact:

- the Sol baseline/carrier energy bounds remain useful for `U3` and
  `B3,tau!=0`, but not as a blanket q=6 theorem;
- Fable's singleton scratch scan is corroboration only and omitted multi-root,
  root-plus-carrier, double-site, and `tau=0` cells;
- Opus's claimed all-modulus scan used the old total vector and unweighted
  prime decomposition, so its headline q=6 closure is **not promoted**;
- Opus's `MARK-EPS` formula and `AFF-FOREST` idea remain valuable inputs to
  the corrected replay; and
- the independent global q=6 lane is now charging the verified local table,
  weighted blocks, and exact contact labels.  Its pre-correction replay had
  zero survivors; the corrected replay and proof are still live.

The maximum present statement is therefore: q=8 is closed; q=6 has a verified
complete local/contact classification and a promising zero-survivor global
calculation, but no binding global closure yet.  Analytic realization,
effectivity, other quadratic strata, and the selector remain separate.

## 3. Selector-side and nonquadratic proposals

### 3.1 Degree-three adjoint defect

The round's numerical `D3PLUS-PG-DEFECT` idea was developed after the cutoff
into the sealed producer
`4eb8fc8ba526441c609448602a122601330ed99db9264d91261ea4cfb810a113`.
For a normal integral class-`(d,3)` incidence dominated rationally by `A2`, it
provisionally proves

```text
h2(O_X)=(d-1)(d-2)
 =sum_p length(R1 r_*O_Y)_p,
```

and packages the same defect as a Grauert--Riemenschneider adjoint scheme of
that length on `X`.  At `d=3` the length-two scheme must map isomorphically to
a length-two subscheme of the coefficient `P1`; equivalently, the defect is
scheme-theoretically horizontal.  This is substantially sharper than the
round's original numerical slope, but it is an obstruction gate, not an
exclusion.  An Opus hostile review is active and must check local duality, the
GR inclusion, the scheme-image length argument, and the Brieskorn negative
control before promotion.

### 3.2 `RULE-ON-Y`

Opus's best unique proposal is to apply the promoted `A1`-ruling instrument
to

```text
A2 --g1 etale quasi-finite--> Y --g2 finite flat--> A2,
U=Y minus branch(g2).
```

This would cross the quadratic-presentation firewall.  The qualitative
conclusion, if its hypotheses hold, is an `A1`-fibration; the quadratic
numerical identity `e(U)=12-r-c` does **not** transfer.  The current suspected
gap is smoothness of all of `U`: etaleness makes points in `im(g1)` smooth but
need not cover singular points outside that image.  Affineness of a principal
branch-divisor complement and the exact log-Kodaira argument also need to be
stated.  A focused audit is live; no canonical raise precedes it.

### 3.3 `AF-PI1-NODAL-SQUEEZE`

Fable proposes: abelian `pi1(A2 minus A(F))` would make the connected cover's
monodromy regular and hence the function-field extension abelian Galois;
Kummer/unit reasoning would then force triviality.  The first conclusion is
likely a repackaging of the classical theorem that the Galois case of JC2 is
true, and it needs a primary-source audit.  The proposed nodal-complement
corollary also needs a utility check: one-place polynomial curves often meet
the line at infinity non-nodally for elementary degree reasons, so the nodal
criterion may add no new restriction.  Retain this as a cheap source/review
card; do not raise Avenue 7 yet.

### 3.4 `BMFD` and trace-projector QCS

`BMFD` asks intrinsic discriminant/index data to force positive minimum cycle
defect over all trace-zero frames.  It directly targets the selector, but its
frame/compactification dependence is severe.  Run the proposed same-algebra
two-frame controls before theorem work; one sign-instability stops the card.

`TRACE-PROJECTOR-QCS` is the most concrete new QCS discriminator.  Clear the
minimal denominator of the canonical diagonal trace projector in
`B tensor B`, compute its DVR elementary divisors, and compare them with the
required quotient-collision event excess.  Unequal-fibre, F5, simple-branch,
and Kummer controls can kill the mechanism cheaply.  A match would still owe
blowup invariance, gluing, and the exact place where `J(F)=1` enters.

## 4. K00 synthesis

Every substantive report independently ranks the work in essentially this
order:

1. source-completeness partition and an uncovered-source falsifier;
2. one scheme-theoretic truncation image on a reviewed predecessor;
3. compatible inverse-system/all-order lifting;
4. actual-map reachability;
5. algebraization and collision retention; and
6. another isolated row only when an image-supported cell asks for it.

The first concrete target remains the reviewed normalized
`e=2,m=2,h10=1` data: compute an exact predecessor/successor image while
retaining source coordinates, localizers, and nilpotents.  Empty, proper, and
dense images are all informative; geometric point projection is not a
scheme-image theorem.

Opus's `JET-CODIM` should be retained only as a diagnostic.  Stabilization of
codimension increments does not by itself produce compatible transition
points or a formal arc, and positive increments force emptiness only after
the changing ambient dimension and every branch are controlled.  It can
prioritize cells and retrodict known closures, but cannot replace the
transition-image theorem.

## 5. Mathematical software and campaign systems

The mathematical-software proposals merge cleanly into
`FOREST-LATTICE-KERNEL/v1`: one exact engine accepting a marked ruled lattice,
local weighted prime table, proximity/contact labels, carrier classes, Euler
budget, and affine-root forest.  It should emit a symbolic energy bound,
orbit-complete enumeration, contact-corrected forest certificates, and a
coverage digest.  Acceptance requires byte-reproducing the reviewed q=8
digest and counts, reproducing the corrected q=6 cells, zero AST `Assert`,
identical ordinary/`-O`/`-OO` output, and both Gram/contact and forged-survivor
negative controls.  The active global q=6 replay is the first implementation
seed.  It remains desk-scale unless a frozen packet crosses the registered
limit; any heavy expansion goes to AWS.

Systems priorities are now evidence-based:

1. **`BASIS-PINNED-READ-ROOT`.**  External/blind lanes must read a detached,
   immutable basis snapshot or fail on per-read hash drift.  Opus supplied a
   live leakage instance.  This is the highest-priority upgrade.
2. **Artifact basis validation.**  `artifact_finalize begin` should reject a
   syntactically valid nonexistent/mismatched commit before creating a lease.
   The coordinator reproduced this failure while opening this very synthesis
   and cleaned the zero-byte partial/lease before restarting with exact HEAD.
3. **`REPLAY-EVIDENCE-GATE`.**  Automate AST, tri-mode, digest, deliberate
   mutation, and infrastructure-error checks for every mathematical replay.
4. **`DAG-ROLLBACK-LINT`.**  Trial a read-only claim-dependency linter after
   the first three, with a strict one-second/no-false-positive gate.
5. **Provider preflight.**  A typed 402/429 probe can reduce failed-lane
   ceremony, but only if its own billing and prompt-custody costs are bounded;
   it ranks below immutable read roots.

None of these systems tasks blocks mathematics.  The first two directly
prevent evidence contamination and bad artifact provenance, so they should
be implemented at the next free software slot.

## 6. Ranked launch portfolio

1. Finish the corrected q=6 global/contact replay and producer; launch a
   different-model hostile review immediately after freeze.  Provisional
   successors need not wait.
2. Complete the active hostile review of the degree-three adjoint-defect
   theorem, then test verticality against one exact cubic different/local
   Weierstrass model.
3. Complete the clean-room `RULE-ON-Y` hypothesis audit.  Promote only the
   maximum theorem surviving the smoothness/affineness check.
4. Type and compute one K00 transition image while a separate lane audits
   source completeness.  Use AWS for any uncertain elimination; do not run
   heavy CAS locally.
5. Run cheap negative controls before investing in speculative selector
   mechanisms: BMFD two-frame instability, trace-projector Smith forms, and
   the Galois/pi1 primary-source audit.
6. Implement the immutable-basis read root and strict artifact-basis gate in
   the next software slot; fold the q=6 replay into the common forest kernel.

Current work is desk-scale and AWS is idle.  AWS becomes mandatory for a
materially growing exact elimination/root packet; no local heavy process is
licensed.  The broad web backstop remains `2026-08-31T03:40Z` absent
significant external news.  Nothing here touches the independently owned
formalization.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `15135`.
- Body SHA-256:
  `dc8cec5e7061fc1aa264db3da611c9778b9d41d104a76626d3672c227972b9cf`.
- Frozen basis: `f02aba6913ec6f9ecd1f3ae103475cde05f3ee29`.
