# Research lane: chamber map and closure program for the (8,10) leaf

Bounded primary research lane (paper mathematics; read-only). Map the
remaining chamber structure of the max11 `(8,10)` leaf and propose its
closure program, in the style that closed `(4,10)` and the 610 linear-root
chamber today.

## Frozen input

Read the committed modules at current `origin/master` of `dcposch/jc2-lean`,
`max11-partial-y/`:

- Committed tracked chain: `LowScale810ScaleZero*` (through the fifth
  defect) and `LowScale810ScaleTwo*` (through the ninth face + seventh
  packet).
- Newly committed green scratch: `Fable810ScaleZeroThirteenthDefectScratch`,
  `Fable810ScaleZeroFourteenthDefectScratch`, and the five Pi consumers
  (`Sol810PiResidualScratch` — grouped exact primitive of the degree-one
  row; `Sol810PiDifferentialBridgeScratch`; `Sol810PiSourceClearingDraftScratch`;
  `Sol810PiLeftUnsolvedUniformBridgeScratch` — weight-112 source power
  relation killing the unsolved order-96 head at roots;
  `Sol810PiSourcePowerBridgeScratch` — RatFunc clearing with
  `C(17592186044416·η)·h^112`).
- The handoff invariant: the scalar-solved h97 jet is NOT source-honest;
  the honest route keeps `m1,tau1,g1,k1` and uses the unsolved uniform h96
  factorization plus the source power/root peel.
- Patterns: the 610 program (source wrapper → head splits → bounded
  h-adic tower with ceiling at the clearing power → cancel-and-expose
  `rho` polynomial vs `rho' = C(j)/h0`) and the 410 endgame (face cascade
  to the degree-0 Keller row → polar collapse → valuation exhaustion).

## Questions

1. **Chamber census.** After the Pi chain, what case tree remains for
   `(8,10)`? Scale-zero: defects beyond the Fourteenth — is there a
   defect ceiling (the analogue of the jet quotient's h-degree ≤ 6)?
   Scale-two: faces beyond the ninth — how many Keller rows remain to
   degree 0? Enumerate the chambers with the exact committed Prop or
   scratch theorem at each frontier.
2. **Tower transfer.** Does the 610 bounded-tower method transfer: what
   is the clearing power (112 from the power bridge?), the pole order,
   and the ceiling for the 810 degree-one row? State the expected
   endgame identity (`rho` polynomial vs which simple-pole equation).
3. **Program.** Output an ordered lane plan: which modules to author, in
   what order, with what each consumes — sized so each step is one
   Grok Lean lane.

## Report

`xmodel/max11-810-chamber-map-grok46-20260831.md`, verdict
`DERIVED`/`PARTIAL`/`BLOCKED` on the map (not on closures), all reasoning
shown, UNVERIFIED labels where needed. No overclaims.
