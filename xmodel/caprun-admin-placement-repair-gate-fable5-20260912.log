cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
The gate report is complete and sealed with the standalone marker at 07:31:32 UTC, well inside the 07:41 reserve. No further edits will be made.

**Outcome:** `xmodel/caprun-admin-placement-repair-gate-fable5-20260912.md` (1346 words, sha256 `6b9b54c4…e48f`). All nine inputs matched their expected pins before the body reads and were unchanged at both post-pins. Sol's printed seal re-derived exactly.

**Verdicts**

- **A CONFIRMED.** Both templates refuse on any unresolved placeholder and on the wrong phrase, and the two phrases are distinct from each other and from the config's seven. Every original identity, deadline, pin, native, holder, mount and base-recursive guard survives verbatim in the unchanged inter-hunk regions. The security script gains no deadline. Neither script starts a holder, touches a worker, creates registration, or writes the FIFO.
- **B CONFIRMED.** The admin tree is a canonical direct `/opt` child, disjoint from the science base in both directions, absent at setup, root:root 0755, and holds exactly five regular non-symlink root:root 0555 files with no subtree, verified by census, mode and installed-manifest hashes. The base 0444 predicate is untouched because the executables live outside it.
- **C CONFIRMED.** The binding chain is acyclic (programs → hashes → manifests → setup → FIFO identity → security). The stage grep makes it impossible to stage an unbound template, so no bound hash can be an unbound hash by fiat. Config and the remote manifest carry the same eight roles in the same order. No authority file is preinstalled.
- **D REFUTED as an unqualified exact-byte predicate; CONFIRMED conditionally; no source fix.** The first-six-lines test accepts a missing final LF, trailing blank lines, and a blank line six followed by anything. The pinned manifest hashes make the consumed file byte-identical to what ROOT pinned, so the narrow claim holds once ROOT constructs and pins a precisely specified five-row file. What is required is a wording correction in the binding contract and Sol's report plus exact ROOT binding, not a source edit.
- **E CONFIRMED with the declared GAP.** Channel 0700, one 0600 FIFO, census, type, link and exact device/inode comparison are all present. Later helpers still compare only path/type/mode. That stays a custody-covered GAP, not a mechanism.
- **F CONFIRMED as design controls, UNEXECUTED.** Every listed negative control maps to a source line; nothing was run. Binding, identities, native closure, clocks and the 120 s liveness remain unmeasured.

**Blocking defect:** none found. **Carried GAPs:** later-phase FIFO identity, cross-file placeholder-name consistency at binding time, unverified empty-row behaviour of `sha256sum --strict -c`, and all runtime facts. ROOT owns the terminal-before-receipt intake from here.
DIVERT path=xmodel/caprun-admin-placement-repair-gate-fable5-20260912.md status=CLEAN body_bytes=10268 body_sha256=6b9b54c4674ed6c7b3d6cc1514708eab5af2d7db29c7f094128d18a7a327e48f
