# Hostile review — TD6 raw `H=P3=0` quotient-field closure

Act as a hostile different-model mathematical reviewer in
`/Users/dc/code/math/jc2`.  Treat every existing producer, evidence, manifest,
report, canonical, prompt, log, and run byte as immutable.  Do not use Bash or
run any computation.  Read in full:

- `xmodel/td6-c1-c2-c3-p3-raw-curve-aws-20260825.md`;
- every file in `cases/td6_c1_c2_c3_p3_raw_curve_aws_20260825/`, including
  the source archive contents through the file-reading tools available to
  you, insofar as those tools expose them;
- `xmodel/td6-c1-c2-c3-trivariate-checkpoint-erratum-20260825.md`;
- the exact prior origin evidence and dependencies named in
  `cases/td6_c1_c2_c3_p3_raw_curve_aws_20260825/DEPENDENCIES.sha256`;
- only the cited source-typing/orbit parents and earlier `H/U/V` certificate
  files needed to audit the meaning and cross-package union.

Charged hashes:

```text
report                   a15c85c5bfd2a4219c2f7540b5e2577f5af123c18b40483a2749118c5ac2bcab
manifest                 9586988908f12a811fd029362c6c1094f13ef8d065961be80ac0a121862cb707
freeze file              ddbf7dfff56f50ec3b00e85261090a51399856003fe1939bb655ab9a0313d512
archive                  b207bbba13ee20d2719ef15504df632f66674d69072d97b83f4c9970d53e0e98
source manifest          7b00ab87e35ca9b2cff9d31d7e0e75cd5cd4fbf5fbe64d1ad68f300c39a2032f
producer                 1b492ad26a2f1c0cdc3ae3983068639163118010ffba9e53c9e6d097d56c9a22
ascending stdout         c38471a004ac311ae01ae851bc98d6223829292ad74db20e2e6d9b7cd79c17b8
reverse stdout           900c38298b6dcaf8ed420afd3d947ca237b5581428d396b063de0875b595a30f
prior origin stdout      c31a0f60eef078150bf7dd59878456fd332d55273181635d5ead11da52f0fc18
dependency pin           6d366b00d58a5ffeffab570817eeaffdad97b9987bfb3c29409d499646d6a644
```

All substantive replay ran on AWS.  Do not rerun it locally.  Inspect the
frozen program and outputs rather than trusting PASS.  Independently attack:

1. **Source typing and specialization.** Verify that the producer rebuilds
   the exact fixed three-center source at `C=3U^2`, not a point sample,
   weighted scaling, or silently normalized component.  Check that the
   genuine P12 and first rows are the licensed original system or have a
   complete checked lift back to it.
2. **Function-field fidelity.** Check algebraically that
   `Q(U)[Z,V]/(Z^2-32Z+128,V^2-ZU^3)` is a field of degree four and is the
   function field of
   `P3=V^4-32V^2U^3+128U^6`, with no component loss, nilpotent adapter, or
   unproved irreducibility assumption.  Audit the implementation of both
   tower layers and their exact inverses, not just the prose argument.
3. **Compatibility certificate.** Check ranks `3470/3602` and `38/132`, the
   genuine 2,885-term P12, the constant remainder `-k/50`, every original-row
   source coefficient, and the plus-one negative control in both pivot
   orders.  Decide whether the second pivot order is genuinely independent
   enough to count as a check and flag any shared failure mode.
4. **Localization and clearing.** Verify that every transport pivot norm,
   raw/first/relation denominator, and termwise product denominator is
   accounted for; that the printed charts `U^17` and `U^19` are complete;
   and that the termwise slot checks plus original-row replay license all of
   `D(U)` rather than only the generic point.
5. **Raw complement.** On `H=P3=0`, derive that `U=0` forces exactly the raw
   center origin.  Audit the separately frozen origin incompatibility and
   dependency hashes, including whether the V14 B3 erratum affects that
   origin certificate.  Check that no embedded/set-theoretic or chart point
   is omitted at the scope actually claimed.
6. **Composition and scope.** Determine whether this package plus the prior
   `H=0` open and `U/V` raw rebuilds truly removes the final producer-exact
   `H=0` debt.  The strongest possible statement is about the fixed
   source-typed three-center section only; it is not a neighborhood,
   full-centering, boundary/dead-stretch, SP-2, maximum-degree, or JC2 result.

State the smallest false identity, missing stratum, missing hypothesis, or
custody defect if one exists.  Give one exact promotable sentence with strict
scope.  Write exactly
`xmodel/td6-c1-c2-c3-p3-raw-curve-review-claude-20260825.md`; edit no other
file.  End with exactly one verdict: `CONFIRMED`, `GAP`, or `REFUTED`.
