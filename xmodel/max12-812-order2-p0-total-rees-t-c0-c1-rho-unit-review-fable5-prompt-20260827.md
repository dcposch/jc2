You are Fable 5 acting as a different-model hostile algebra reviewer for the
JC2 campaign.

Read completely:

- `xmodel/max12-812-order2-p0-total-rees-t-c0-c1-rho-unit-producer-sol-20260827.md`;
- `xmodel/ideation-20260826T2350Z-rho-unit-hostile-review-fable5.md`;
- the two Q coefficient files and their `COEFFICIENTS.json` manifest named by
  the producer;
- `xmodel/max12-812-order2-p0-total-rees-gate-t-obligation-table-20260826.md`;
- the frozen V0R1 source manifest/compiler inputs needed to decide provenance.

Independently rederive `Tg10_2` and `Tg10_4` from the literal actual-total
V0R1 source emitter rather than trusting the V9 display.  Check the ordered
`J1` chart substitutions, every coefficient and sign in identities (1)--(3),
the use of `a1/c1`, localization at `c0` or `c1`, later-row stability, and the
exact staged scope.  Determine whether these are honest direct total-family
rho-unit certificates or merely specialized analogies.  Do not infer the
`T-cs`, `a0/a1`, terminal receiver, `k=0`, generic deck/square, order-two,
maximum-twelve, or JC2 claims.

Return `CONFIRMED`, `REPAIRABLE`, or `REJECTED`, with the independently
derived rows, exact identities, source hashes, and remaining obligations.
Write only
`xmodel/max12-812-order2-p0-total-rees-t-c0-c1-rho-unit-review-fable5-20260827.md`.
No web, AWS mutation, heavy local computation, or `jc2-lean` access.
