# 0310 equal adversarial cross: Fable 5.1 seat

Report `xmodel/ideation-20260909T0310Z-cross-fable5.md`. Lane `/tmp/jc2-lane.mH2Vaf`. Frozen basis `0d39df3c9fd69c939a8420c54d03228b9077777d` (provenance only). Tag `ideation-20260909T0310Z-cross-fable5`. Root invitation ACTIVE; common absolute deadline 2026-09-09 04:40:00 UTC. Lane clock: invitation packet materialised 04:12 UTC; first command 04:12:21 UTC.

External-author lane custody: no artifact transaction, no authored Seal section, no `charge_basis` line (no exit price is asserted or consumed). This is adversarial CROSS review, not a promotion gate: nothing below promotes either provisional candidate, and no unseen gate verdict is consumed. The live F2 joint-gate body/log/receipt and all live peer-cross bodies were NOT opened.

**Tool boundary honoured:** zero mathematical subprocesses. Only `cat/head/tail/sed/ls/date/sha256sum/test` were executed. Every derivation below is factored hand reasoning. No CAS, Python, source expansion, H/R power, solver, AWS/SSH, agent, protected/shared write, or web retrieval.

## 0. Read scope and pins

`sha256sum` over all 21 lane inputs under `/tmp/jc2-lane.mH2Vaf/inputs` was run BEFORE any mathematical body was cited; every digest equals its `charged-inputs.list` line and its PINS.json entry (0 mismatch, 0 missing, 0 extra). Files read WHOLE: common-contract, common-poststate, all four blinds (coordinator 64d3b5a2, astra 47e1475a, fable5 ecd7305a, sol ce12b640), provisional-fixed-degree-composition aeeddd6a, provisional-primary-source-audit 3935d763, accepted-15w-review 41a72b88, accepted-audit15w-only eb386fe3, primary-balanced-top-exclusion 0550f927, PINS.json 27f50f75, original-0310-manifest 8575b68f (metadata: 31 entries, 32 files with self). Frozen context files (coordination 874b613c, approaches d46f037f, master46-history c235fca1, reduction-interfaces 8f8403be, root-checked-allD-admissibility aae419fd, remaining-m90-source-interface 0c5270a4, last-0040-synthesis e97b254a, binding-bd-gal f9720718) were read whole in byte chunks; exact chunk boundaries were re-joined. Where the deadline forced a partial read, the section below says so explicitly.

Scope reminders applied throughout: historical ranks are not current; d2=[K:C(f,g)]=2 and proper Galois second-leg quotients are CLOSED (BD-GAL), d1=2 distinct; accepted 15w is a lifecycle change of the SAME all-D late-contact candidate at exactly its closed-H/constant-J/nondividing 3:5 scope; the two provisional proofs (aeeddd6a, 3935d763) are NEW PROVISIONAL content under a forbidden separate gate.

## 1. Question 1: the normalization route (aeeddd6a, 3935d763) at its exact provisional scope

**Arrow chain, hand-rechecked.** Both provisional documents prove the same chain: (i) a hypothetical ordinary Keller pair of actual total degrees 75/125 over C is a counterexample (Chau/Abhyankar–Moh degree divisibility); (ii) IMPORT ML2025 pp.302–305: a polynomial SOURCE automorphism φ puts P̄=φ(P) in a rectangle [0,r]×[0,s] with a nonzero corner (r,s), r≠s; (iii) r,s>0 (a univariate P̄ in a constant-J pair forces an automorphism); (iv) for every positive weight w the w-leader of P̄ is the corner monomial, the top bracket weight exceeds 0, so [x^r y^s, Q̄_w]=0; the monomial bracket multiplier rj−si with distinct output exponents forces Q̄_w onto the ray of (r,s) and single-monomial; the (1,1)-leader (t,u) is the largest ray multiple and hence the leader for every positive weight; letting w tend to the axes gives Q̄ a rectangle with corner (t,u)∥(r,s); (v) with U=ψ(x),V=ψ(y), ψ=φ⁻¹, M=deg U, N=deg V ≥1, the corner uniquely maximizes Mi+Nj on the rectangle, so deg P0=Mr+Ns, deg Q0=Mt+Nu with top forms αU_M^r V_N^s, no cancellation possible; (vi) proportionality plus the 3:5 ratio gives (r,s)=3(a,b), (t,u)=5(a,b), 25=Ma+Nb, a<b; (vii) IMPORT GGV Def 4.3 + Prop 5.20: (P̄,Q̄) is a literal ordinary (3,5)-pair with en_(1,0)P̄=(3a,3b), standardized by an ordinary automorphism preserving both total degrees and that endpoint; (viii) IMPORT GGHV Thm 2.20 + §5 exhaustive table at v_(1,1)(A0)≤35: hand filter of the ten rows with a+b≤25 leaves only F2 at j=1, A0=(5,20), whence 25=5M+20N forces M=N=1 and actual degrees 75/125 survive; (ix) accepted 15q chain (published-chain discriminator → U,V of degrees 30/50 with [U,V]=c0 x³ → minimal map S → A,B of degrees 15/25, J=c·g², tops H³/H⁵ in the two accepted H patterns) → contradiction. Field transfer: finitely generated coefficient field embeds in C; nonzero leading coefficients, degrees and the constant survive.

I re-derived (iii)–(vi) and the (viii) table filter by hand from the printed family formulas quoted in both documents; they are correct as written. The one place where I would sharpen the text: in (iv) the claim "[P̄_w,Q̄_w]=0" needs the w-homogeneous component of [P̄,Q̄] at weight w(P̄)+w(Q̄)−α−β to be exactly [P̄_w,Q̄_w]; that holds because the bracket is bilinear and lowers weight by exactly α+β on each monomial pair, and since w(P̄)≥α+β (r,s≥1) and w(Q̄)>0 that component sits at positive weight while the actual bracket is a constant. Fine.

**Missing arrows (what the forbidden joint gate must decide; I do not decide them).** (a) The ML normal form must be stated for an ARBITRARY counterexample, not a minimal-degree one, and must yield a rectangle whose corner is in the support; the two reports assert this from pp.302–305; I could not read ML here (not charged) and record it as the single load-bearing external arrow with no in-packet fallback for (A). (b) Def 4.3's exact hypotheses beyond total/x-degree ratio and the endpoint inequality (both reports say there are none); (c) Prop 5.20's final "ordinary input → ordinary automorphism" sentence and endpoint preservation; (d) GGHV §5 exhaustiveness at the printed bound, imported, not replayed; (e) 15q's own imports (published chain, minimal map injectivity on the cone i≥j). None of these is a campaign theorem. Cross alone promotes nothing.

**What (A) does to the M2=90 child, conditionally.** The Moh census rows at (n,m)=(125,75) are necessary characteristic data of a hypothetical ordinary pair with actual total degrees 125 and 75 and constant Jacobian. If (A) passes at its imported tier, no such pair exists, so BOTH (125,75) rows (M2=105 and M2=90) describe a nonexistent object. The M2=90 receiver construction (my blind Card A, Sol Card 2, Astra Card 2 fallback, coordinator Card A) is then unnecessary: not closed by a receiver theorem, but vacuous as a client. Two cautions the contract asks for: this covers only rows with exact degrees (125,75), not every row whose maximum degree is 125 with another m; and it does not identify the Moh rows with the F2 family coefficientwise, it only says both are shadows of the same hypothetical pair. If the gate finds a gap at arrow (a), the M2=90 row stays the sole named finite-125 Moh target and the receiver-shape test remains the cheapest next step; if the gap is at (d), one still has a standard (3,5)-pair with A0=(a,b), a+b≤25, and F2 selection is the exact missing arrow.

**A consequence nobody's blind states.** (A) is Moh-import-free. The 125 verdict would no longer rest on Prop 5.3/ell-shifted transport or the child datum (25,15;21;2;2). My blind's Card C (hand countermodel of the ell-shift transport) therefore loses its "could change the 125 verdict" status: a transport failure would detach 15v's parent attachment only, while (A) still excludes the pair. Card C is downgraded to routine trust hygiene, not launched.

**Crossing the closed-H screen (B) against 15w.** (B): for coprime m,n>1, tops H^m,H^n, constant J, the normalized endpoint (a,b) has gcd d>2 by Cor 7.9 (first assertion, per pair), and H=ζ(U_M^{a/d}V_N^{b/d})^d, so a lower-degree homogeneous polynomial commutes with H; closed H is impossible. Accepted 15w is a theorem with hypotheses that (B) says no actual pair satisfies. That is a vacuous application, not a contradiction: 15w's finite late-T lemma (15s), wedge identity and direct leading-form lemma remain valid abstract tools and method controls. The correct lifecycle reading is: 15w stays PROMOTED at its scope; the closed-H SOURCE program loses its only conceivable client class if (B) passes.

## 2. Question 2: primitive/proper-power warnings versus my blind's automatic reconstruction (RBU)

**What survives of RBU.** The wedge identity survives verbatim in scalar-reference form. For any a(T)=T^M+kernels, b(T), with Euclidean division b'=a'q+ρ in K[s][T] (deg ρ≤M−2) and A=a(R)+F, B=b(R)+q(R)F+G, I re-derived [A,B]=[R,T]+[F,G] with T=a'(R)G−ρ(R)F−(q'(R)/2)F², exactly the coordinator's Card B display; at (M,N)=(3,5) it is the 15w T with ρ=τR+δ. Replacing H=h^e by its reduced base h turns the exponents into (3e,5e), not (3,5): my blind's sentence that "the constant-J reference/wedge/kernel construction applies to every plane Keller pair" is true for the identity and FALSE for the step ord G≥2j. I withdraw that step.

**Where ord G≥2j breaks, by order ledger (hand, no expansion).** Scalar coefficients are weight-homogeneous: the T^k coefficient of ρ has s-order (N−1−k)D0, so the lowest ρ term is r_{M−2}h^{M−2}F_j at order (2e+1)D0+j. In the e=1 framework that order is 3D+j>2j because j≤3D−1, so ρF never precedes the G target and the kernel-shift induction closes. For e≥2, j can exceed (2e+1)D0 (j ranges to 3eD0−1), and then ρF enters BEFORE 2j. The order-l equations for l<min(ord G,2j,8eD0−2) read [h,(ρ(R)F)_l]=0. At the lone lowest order this gives r_{M−2}h^{M−2}[h,F_j]=0; with the normalization absorbing every K[h]-part of the residual into a(T) one has F_j∉K[h], hence [h,F_j]≠0 for closed h, and r_{M−2} is FORCED to vanish, and so on upward while the ρ term is alone. The induction genuinely stalls when a fresh ρ coefficient r_k first shares its order l=(N−1−k)D0+j with ord G: the equation becomes [h,Mh^{M−1}G_l−r_kh^kF_j]=0, so Mh^{M−1}G_l−r_kh^kF_j∈K[h], and the kernel-shift trick cannot remove a G_l of the shape (r_k/M)F_j h^{k−M+1}. So the honest general bound is ord G≥min(2j, j+(2e+1)D0) with a two-branch case split (all early ρ coefficients vanish, or G inherits F's shape at the first shared order). Copying "G≥2j" from e=1 by notation is invalid, exactly as root and Astra warned; late contact then means ord[F,G]≥j+ord G>(8e−1)D0, which is 3j>(8e−1)D0 only on the first branch.

**H0=xy is not an open Keller control.** From the charged primary lines 360–394: Theorem 2.6 applies to P∈L^(l) with (ρ,σ)∈V_{>0}, v_{ρ,σ}(P)>0 and [P,Q]∈K^×, and its clause (4) states st_{ρ,σ}(P)≁(1,1)≁en_{ρ,σ}(P). At (1,1) an ordinary constant-J P with sole leading monomial λ(xy)^k has a monomial leading form, so st=en=(k,k)∼(1,1), contradicting (4). Linear coordinate changes preserve total degree and constant J, so no Keller pair has top form a power of L1L2 with two distinct linear factors of EQUAL multiplicity. My blind's Card B "two-simple-root base case H0=xy, (A,B)=(8e',12e')" is therefore dead as a control; I withdraw it. Clause (4) does NOT exclude x^r y^s with r≠s (st=en=(kr,ks)≁(1,1)), so the poststate's caution stands: only the balanced example is excluded.

**First honest UNEQUAL proper-power test (one, no expansion, no Keller pretence).** Object: closed h=x^r y^s, gcd(r,s)=1, r<s, D0=r+s; scalar references a(T)=T^{3e}+(kernels of index ≤3e−2), b(T)=T^{5e}+lower, over K[s]; F with first order j and F_j∉K[h]. Smallest e: if (B)'s Cor 7.9 import is accepted, every actual pair has d>2, so e=3 is the first admissible exponent and e=2 is a non-source method control only. Test: build the scalar-order ledger of ρ=b'−a'q at (M,N)=(9,15) by hand (coefficient orders (14−k)D0, k≤7), list the orders (2e+1)D0+j=7D0+j,…, and decide for the window j∈(7D0,9D0−1] whether the lone-order forcing above exhausts ρ before the first shared order with 2j, or whether the inherited-shape branch is unavoidable. Output: a correct replacement inequality for ord G, or the exact first competing term. It is a theorem about a hypothetical source class; its only known member-shape, the physical F2 lift (e=5, h=uv⁴), is already retired by 15q, so NO attached unretired family exists. Do not feed it arbitrary polynomial reference data as if they came from a Keller pair.

## 3. Question 3: strongest proof and counterexample directions beyond the retired exact sources

**Two different statements, kept apart.** Accepted 15w is a LATE-CONTACT BOUND: every actual source with the exact closed-H/constant-J/3:5/D≥3 interface has canonical j≤⌊7D/3⌋. Provisional (B) is a WHOLE-CLASS EMPTINESS statement: that interface has no actual member for any D>0. The second, if it passes, dominates the first for source purposes and leaves it as a reusable abstract instrument. The campaign should therefore not build any early-contact theorem inside the closed-H class: it would have no client. Astra's blind Card 1 reaches (B) by a second route (McKay–Wang two-factor bound + closedness gives h=x^r y^s with gcd(r,s)=1 in linear coordinates; an upper-envelope lemma from Theorem 2.6 and Remark 2.5 supplies the rectangle without ML; then Def 4.3, Prop 5.20 and Cor 7.9 give gcd(r,s)>2, contradiction). I re-derived that upper-envelope lemma from the charged primary lines: for ρ>σ>0 coprime, a polynomial Euler element of weight ρ+σ can only contain xy and, when σ=1, y^{ρ+1}; a monomial F contradicts Remark 2.5 on a nonmonomial face, and F=λxy+μy^{ρ+1} has en(F)=(0,ρ+1), which is neither (1,1) nor parallel to the positive corner (a,b), contradicting clause (3). So the two routes to (B) are independent at the entry (ML rectangle versus McKay–Wang plus Euler envelope) and share only Def 4.3, Prop 5.20 and Cor 7.9. That shared trio is the exact trust perimeter of (B); a single primary correction there would take down both routes at once, which the gate should record.

**Sol's ≥N threshold lemma: abstractly valid, zero live payoff.** Statement: R combined-homogeneous of degree D, R(0)=H with ker[H,−]=K[H], T of combined degree ≤N, ord_s[R,T]≥N ⇒ [R,T]=0. Proof check: the pieces T^(n) of distinct combined degree n≤N have brackets of distinct combined degree n+D−2, so the order bound passes to each piece; for a piece with first order ℓ<n the order-ℓ coefficient [H,T_ℓ]=0 forces T_ℓ=aH^i with iD=n−ℓ, and subtracting a s^ℓ R^i raises the order without changing the bracket; at ℓ=n the piece is a scalar times s^n. Finite induction. Sharpness control H=R=uv, T=s^{N−1}u: [R,T]=−s^{N−1}u≠0, so N−1 fails. This is a changed-object countercontrol, not a predicate flip. Application: in 15w's framework N=7D, ord[R,T]≥min(8D−2,3j)≥7D when 3j≥7D and D≥2, so the equality case 3j=7D is also excluded and the bound sharpens to 3j<7D, i.e. an improvement only when 3|D. Payoff: a method control and a tidier abstract lemma; no live client if (B) passes, and the improvement is at most one unit of j. Not a new client, not a card.

**Strongest proof direction.** If (A) passes: the published-style frontier moves by the same three-step pattern (normalization to a standard pair → GGHV chain/table selection → a campaign receiver exclusion of the selected row). The next finite target is the smallest GGHV row above 125 in maximum degree, each needing its own receiver theorem; the all-degree bridge is the proper-power scalar-reference program of §2 with e≥3, whose value is exactly that it addresses the class (B) leaves open. If (A) fails at the ML arrow: the McKay–Wang route still gives the rectangle for two-factor H, and the one-factor case H=L^D is the residual obligation; if it fails at the table import, F2 selection is the missing arrow. No promotion of either follows from this cross.

**Strongest counterexample direction.** With (A) passing, the complete guarded B0 75/125 envelope (remaining-m90 interface) is existentially a unit, the six F2 receivers are already units, and no CE client of degree ≤125 exists; the coordinator's Card C (source-completeness gate) reduces to "consume (A) and stop". A CE lane needs a NEW complete source with its own acceptance theorem from a GGHV row above 125, or an object outside the standard-pair normalization altogether, and by ML + Prop 5.20 there is none of the latter. K16's b·B_m·η_m locus and b=0 boundary stay the only structurally different falsification target (Sol's design), with no new discriminator here and no launch. Witt/char-p remains a discriminator, not an endpoint.

**Attack classification, as required.** The x−y² versus (x+y², y) cancellation example is a changed-object control on the rectangle premise, not a predicate flip. The (xy)^k exclusion is a factor-identity consequence of a printed theorem, not a source implication. The §2 ρ ledger is evaluation of a few order equations, not a finite ideal decision. The §1 table filter is a finite hand decision over ten printed rows whose exhaustiveness is imported. (A)'s "unit" consequence for the B0 envelope is existential nonexistence of points, not a computed certificate.

## 4. Question 4: labels, the manifest count, and the custody lapse

**Manifest count.** I counted the charged original manifest myself: 31 entries (coordination, approaches, progress-sep9, latest-live, audit-guide, accepted-15q-through-15v, master46-history, reduction-interfaces, binding-bd-gal, last-0040-synthesis, last-0040-transaction, last-broad-sweep, minimal-receiver, actual-moh-source, actual-moh-source-gate, remaining-m90-source-interface, 15s proof/gate, 15t proof/gate, 15u contact-cover/three-simple/gate, 15v proof/gate, provisional-allD-descent, provisional-allD-direct, root-checked-allD-admissibility, root-final-packet, root-final-contract, root-release) plus the manifest itself: 32 files. My blind's pin table has exactly those 32 lines. Sol's blind says "all 30 entries" and treats FALLACY-v2 as a "thirty-second input"; that is a miscount by one entry and a misattribution of the appendix, which is prompt text, not a charged file. Sol's systems card (launch overlay with directory_file_count=31 / semantic_input_count=32) was earned from this false premise and is REJECTED; the existing charged-inputs.list already carries basenames and hashes and resolved the count in one command here.

**Custody lapse.** Root's first whole read of my blind preceded completion of its current-pin checks because the comparison expected CLEAN_SEALED while the launcher's actual status for an unsealed external body is CLEAN (seal.py divert semantics: CLEAN = whitespace-only tail after the marker; CLEAN_SEALED = canonical seal present). Subsequent pins passed and no live body was read. I classify this as an ordering defect, not missing scientific bytes, and I note it is the second occurrence of the same class in two rounds (0040: a validator field error, then a shell newline let the read proceed). The remedy is one line in the existing comparison plus the already recorded rule that check and read are separate commands stopping on nonzero; see §6.

**Historical labels corrected.** (1) My blind's Card B base case H0=xy: withdrawn (§2). (2) My blind's RBU "construction applies": narrowed to the identity only (§2). (3) My blind's Card A parity self-check is correct only BEFORE any fractional centering; Astra's control h=π(π²−γ)² shows an odd count can appear after an unlicensed γ^{1/2} translation, so parity is a check on the literal polynomial, not on a centered object; both statements are compatible and the card is superseded by (A) if it passes. (4) Cor 7.9 is printed p.45 (the admissibility intake's p.44 is a typo, as 15w and Astra's blind already say). (5) The (99,66)/D108 closures, d2=2/Galois closure, and "closed-H candidate has no attached client" are current; historical ranks in the 46 table are not.

## 5. Deduplicated cards: three total, ranked

Fingerprints merged: coordinator Card A ≡ Astra Card 2 ≡ root's provisional (A) ≡ Sol Card 2's antecedent ≡ my Card A's superseder → Card 1. Astra Card 1 ≡ root/Astra (B) → Card 2, with Sol Card 1 folded in as a method control. Coordinator Card B ≡ Astra §5 warning ≡ my RBU/Card B → Card 3. Coordinator Card C, my Card C, my §5.1 receiver-shape census and Sol's K16 falsifier are not cards this round (reasons in §3–§4).

### Card 1 (rank 1). Fixed-degree composition (A): attack list for the running gate; M2=90 disposition
- **Object.** Theorem on a HYPOTHETICAL ordinary 75/125 constant-J pair over a characteristic-zero field. No attached unretired family: the six F2 receivers are retired, the (125,75) Moh rows are necessary data of the same hypothetical pair, not families.
- **Dependencies.** Imports (a)–(e) of §1; accepted 15q at its scope; nothing from 15r–15v or any Moh import.
- **Cheapest falsifier.** Documentary, not mathematical: quote the exact ML statement on pp.302–305 and check that its hypothesis is an arbitrary counterexample and its conclusion an in-support corner; then quote Def 4.3 whole and Prop 5.20's last sentence. A counterobject would have to be a Keller pair whose every source automorphism leaves no in-support rectangle corner, which the gate cannot construct; so the test is the quotation.
- **Authority.** The launched different-model joint gate (forbidden here). No new lane.
- **Outcomes.** PASS: 125 closed at the imported tier; M2=90 receiver work VACUOUS and STOPPED; Moh-import perimeter no longer load-bearing for 125; next finite target is the first GGHV row above 125. GAP at (a): fall back to the McKay–Wang/Euler-envelope rectangle for two-factor H and treat one-factor H as the residual; M2=90 stays the sole finite target. GAP at (d): record "standard (3,5)-pair with a+b≤25, F2 selection missing". REFUTED: name the false arrow; nothing else changes.
- **Stop.** One gate verdict. **Disposition:** CONTINUE the gate; STOP M2=90 receiver construction until it reports.

### Card 2 (rank 2). Closed-H whole-class emptiness (B): two-route cross-check and the exact shared perimeter
- **Object.** Theorem on the HYPOTHETICAL class: coprime m,n>1, tops H^m,H^n, constant J, ker[H,−]=K[H]. No attached family; if it passes, none can exist.
- **Dependencies.** Route R1 (root/Astra audit): ML rectangle + Prop 5.20 + Cor 7.9 first assertion. Route R2 (Astra blind Card 1): McKay–Wang Thm 5 (r=s=0) + closedness typing + Euler-envelope lemma from Theorem 2.6/Remark 2.5 + Def 4.3 + Prop 5.20 + Cor 7.9. Shared perimeter: Def 4.3, Prop 5.20, Cor 7.9.
- **Cheapest falsifier.** Prose, 20–30 minutes: check that Cor 7.9's first assertion is per ordinary standard pair (not minimal B) and that Prop 5.20 preserves en_(1,0) literally; check R2's Euler-envelope lemma once more against lines 360–394 (I did; it holds). A refutation must exhibit a closed two-factor H^m,H^n Keller pair, which by Theorem 2.6(4) cannot be balanced.
- **Authority.** Same gate for R1; R2 is an unreviewed blind derivation and needs its own hostile prose read before any citation.
- **Outcomes.** PASS: closed-H source program STOPPED as a client search; 15w/15s retained as instruments; Sol's ≥N lemma banked as a method control only. GAP in the shared trio: both routes fall together; record the exact primary statement at fault. PASS for R2 but not R1: (B) holds without ML, which also supplies the fallback for Card 1.
- **Stop.** One verdict per route. **Disposition:** CONTINUE (gate for R1; one bounded prose read for R2); no descendants.

### Card 3 (rank 3). Proper-power scalar-reference ledger at e=3: the all-degree bridge, correctly stated
- **Object.** Theorem on a HYPOTHETICAL source class: closed h=x^r y^s, gcd(r,s)=1, r<s, tops h^{3e},h^{5e}, constant J, scalar references a(T),b(T) with Euclidean remainder ρ retained. By (B) the first admissible exponent is e=3. No attached unretired family: the only known member-shape (F2, e=5, h=uv⁴) is retired by 15q.
- **Dependencies.** The wedge identity (§2, re-derived); the lone-order forcing r_{M−2}=0 needs F_j∉K[h], which the normalization supplies only if every K[h]-part of the residual is absorbed into a(T) at orders kD0; this normalization must be written before the ledger.
- **Cheapest falsifier.** 45–75 minutes of hand order bookkeeping at (M,N)=(9,15): does the first shared order between a fresh ρ coefficient and ord G occur below 2j for some j∈(7D0,9D0−1]? If yes, the inherited-shape branch G_l=(r_k/M)F_j h^{k−M+1} is live and "G≥2j" is false in general; if no, ord G≥2j is recovered with the forced vanishings as extra lemmas. Either way no H/R expansion and no Keller pretence.
- **Authority.** A bounded prose producer lane plus one different-model gate, only after Card 1/2 verdicts; not this cross.
- **Outcomes.** Replacement inequality → typed lemma candidate for the general-(A,B) late-contact question; unavoidable competing term → the named repair; no closed-h source can be attached → NO HIT, stop. **Disposition:** REDESIGN the reference program around this class before any new theorem family; CONTINUE only after the gate.

## 6. Proof/CE separation, lane dispositions, the one systems change, scope

**Proof path.** Card 1 → Card 2 → Card 3, strictly in that order; each later card is worthless if the earlier one fails in the way that removes its class. **Counterexample path.** No client at degree ≤125 if Card 1 passes; a CE lane needs a new complete source above 125 with its own acceptance theorem; K16 remains the only structurally different falsifier, unlaunched. These paths share no live object.

**Dispositions.** F2 gate: CONTINUE (body forbidden here). M2=90 receiver: STOP pending Card 1. Closed-H client search and any early-contact theorem inside the closed-H class: STOP. 15w/15s instruments: retain, no re-hardening. Moh ell-shift countermodel (my Card C): STOP as a verdict-changer; optional hygiene. Receiver-shape census of the 44 rows: STOP (vacuous for (125,75) if Card 1 passes, and the other rows are not 3:5 clients of anything accepted). All 125 solves, common4, B0 envelope: STOP. K16: unchanged. Broad web sweep: unchanged clock. AWS: zero.

**Systems: ONE earned change, minimum size.** Fix the receipt-first comparison so that an external lane report with status CLEAN is accepted as valid before the whole read, with CLEAN_SEALED reserved for artifact-transaction reports, and keep check and read in separate commands that stop on nonzero. Benefit: removes the exact mechanism of this round's ordering lapse and of the 0040 near-duplicate at one line of change; cost: a focused launcher regression, no new machinery. Rejected: Sol's launch overlay (earned from a miscount), my blind's lemma sheet (deferred: no reference-method producer is scheduled while the class is under Card 2), the coordinator's byte-reduction metric (not a speed claim). NO_NEW_MECHANISM at the campaign level otherwise.

**Scope and terminal status.** This cross asserts no theorem, promotion, exit price, source existence or JC2 conclusion. (A) and (B) remain PROVISIONAL; 15w remains PROMOTED at its scope; all consequences above are conditional on the forbidden gate's verdict, which was not read. No `charge_basis` line is declared. Read scope: every listed lane input whole (§0); no ML, GGV Def 4.3/Prop 5.20/Cor 7.9 or GGHV primary text was available in the lane beyond the charged 4172-byte excerpt, so every statement about those results is an import audit, not a primary read. Actual substantive interval 04:12:21–04:24 UTC, before the 04:40 deadline. Sole write: this file; no shared ledger, no follow-on, all writers stopped after the marker.

## OPEN(S) RAISED

None.

## COLLISIONS

status: EMPTY

- NONE — the report contains no explicitly raised `OPEN[...]` entries. No corpus scan was performed.

<!-- BODY-END -->
