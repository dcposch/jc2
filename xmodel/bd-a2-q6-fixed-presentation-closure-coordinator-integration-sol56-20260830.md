# Binding integration: fixed-presentation q=6 closure

Date: 2026-08-30 UTC  
Coordinator integration: `/root/d3_halphen_independent_audit` for `/root`  
Frozen basis: `ae7d44ea2148980fb1e2d3bd12c413041a0d25d4`  
Lifecycle: **BINDING FIXED-PRESENTATION PROMOTION / q=6 CELL CLOSED**

## 0. Decision

Promote the maximum-safe conclusion of the sealed Opus 5 hostile review.
In the charged reduced normal-singular quadratic `F5` presentation, the
complete `q=6` local contact census, the corrected nonnegative-`B`-degree
lattice enumeration, and the exhaustive curve-typing lemma together exclude
every necessary global boundary configuration for both marked tags

```text
B_3/A_2,                 U_3/A_3.
```

This closes the fixed-presentation `q=6` cell.  The prior objection about an
`A`-degree-zero strict prime is discharged: a curve null for both `A` and `B`
is an exceptional Du Val root, while a nonexceptional `A`-null curve is one
of the already-enumerated contracted carriers.  Every remaining strict prime
has positive `A`-degree, and the local `F5` cluster consumes the full global
degree eight.

The promotion adopts every review correction.  In particular it does not
use the unproved normalization `a(0)=0`; it charges the upstream marked-tag
list at the two omitted parameter boundaries; it records that root/block
machinery is load-bearing in the old positive-degree domain; it attributes
no elimination to the diagnostic energy inequalities; and it states where
the finite enumerator's arity bound is covered by the typing theorem rather
than by software.

The rational-forest input is consumed only in its corrected **morphic** form.
The actual proper-block first leg is an everywhere-defined dominant etale
morphism, so the forest deductions used below remain binding.  No conclusion
is drawn from abstract rational domination.

## 1. Frozen evidence and independent replay

The integration binds the provisional typing repair

```text
d2122d822fdd56e925487800089325b522e5e6d74cbb2da6da496868614c456a
  xmodel/bd-a2-q6-degree-zero-typing-adjudication-sol56-20260830.md
  body 9818 / 650be07da65edee18d7e7d3a8482675c59e61c35c4bfc7a6ed6e90edc0c511c5
  manifest a338fb5cdcb41a729e7113b60b2beebff243a37e500598f2a47b5e08b9a43b22
```

the complete local producer and corrected global enumeration

```text
48717ce3d8b3c5ef6926e3b5ebcf2a0d94ad7bee42ea4ea1c253fb4fd9463649
  xmodel/bd-a2-q6-local-contact-audit-sol56-20260830.md
  body 22514 / 941d4e27b95bee2483b98fa62c4e1b9c9b94e7dff7aa200848ab03977461522d
  manifest 222d1bffbd05d08103712f922e8baf5710fcf58dc23d144503c4ec94a4cbda83

8fea7f545f058d1968ff3a402695f2d7d4bb8e4e15b280c4f2f942132e89e6f4
  xmodel/bd-a2-q6-affine-ade-threat-map-v2-sol56-20260830.md
  body 14628 / 03590225ea52239df4d485b4b7aaac015af4d4e8975aba299b637663f6ce2f34
  manifest cc55620b3d1a3fe61c3e22b7c8251f4550515836beeaa7a02bf7462572b780e6

c2ca2874b9bf80a9612010d034118d0f854a9c0cb6167b0865d22470374a0c56
  ops/q6_f5_affine_ade_threat_replay_v2.py
```

and the earlier hostile review and binding carrier frame

```text
d92beb90aef85742711c5a41e7c9558cb6db64101783de95a5b0d89588fdad74
  xmodel/bd-a2-q6-affine-ade-threat-map-v2-hostile-review-opus5-20260830.md
  body 23422 / c968ed6f60b3cd65868050a49180a5f5bbc37ae2241042e9aa6870290f75c90f

a5bf78c8ab8704884e9bb3c084c88059e01ec7690c1d6f645a9e87a0aa70a884
  xmodel/bd-a2-normal-f5-decorated-carrier-effectivity-reduction-coordinator-integration-sol56-20260830.md
  body 13359 / a946988a77a558786a1226d9d7f05c179c7662f33685bf1acab211ce95e884e0
  manifest 31ee147c6a0280ec50a73a2db3cee5ecf10cabe1e3f60a2ea85c15c0d564ff50
```

The genuinely different-model closure review is

```text
dfbd8c4c0793ee396168e59b6b542c5d39ee55dbfe37ce48207306530641b66e
  xmodel/bd-a2-q6-degree-zero-typing-and-local-contact-hostile-review-opus5-20260830.md
  body 31680 / 43660b5d0516a875590aa98142c6edef2fc8b9b83db612289e1f648397db0a00
  verdict CONFIRM_WITH_CORRECTIONS
```

Its body seal reproduced exactly.  All ten hashes charged by its prompt were
independently recomputed.  The four artifact-backed inputs above passed
`artifact_finalize.py verify` with their stated bases and manifests.  The
corrected morphic rational-forest packet also verified exactly:

```text
d49a44ce65f13f0aba35e0a034923a1362594ba8a21fef27c63cc866ec7b667a
  xmodel/bd-a2-rational-forest-morphic-correction-sol56-20260830.md
  body 6040 / 6e97a5c2bdb8aa679e53338d507bd684161eb53af96cd09fa4f2662fcbba08f0
  manifest d14702bb98d3fd347a9b96ffaa531fefd55aad28df58d37601f6336bfb29029f
```

The v2 replay was rerun unmodified in ordinary, `-O`, and `-OO` modes.  All
three outputs were byte-identical:

```text
bytes 37859
sha256 27ca7c500f411843c34c1aad60d51261b6e66e9ba712eddf48aa67893add8f3e
```

Both mutations failed in all three modes with their exact diagnostics:

```text
--mutate-drop-vertical  -> FAIL:vertical a=0 domain was not exercised
--mutate-contact        -> FAIL:q6 baseline multiplicity sum must be fourteen
```

The v1 engine and v2 wrapper contain zero AST `assert` nodes.  The replay
again returned the corrected raw engine hash
`33d8841e0609857220f80fc3d46e8a33ccfc655ef19bfb42b465ee802ae13216`,
the 830-byte empty-survivor payload hash
`ab2fc3c6c5d47160fa947542dd5d33e375c9221a43df05c10cd99cfb6926b46d`,
142 added metadata rows, and 3,317 added candidate records.

## 2. Exhaustive curve typing

On the minimal Du Val resolution use the nine-blowup marking

```text
B=F,
A=2S0+5F-sum_i P_i,
H=L+S0+T,
A+B=r^*(O_X(1,1)).
```

The downstairs `O_X(1,1)` is the restriction of the Segre very ample bundle.
This is the needed positivity statement; `A` itself is nef and need not be
ample, and the label “reduced finite” must not be read as global finiteness of
`pi`.

Let `C` be an irreducible effective curve with `A.C=0`.

### 2.1 Both-null curves are exceptional roots

If also `B.C=0`, then `(A+B).C=0`.  Projection formula and downstairs
ampleness force `r(C)` to be a point.  Minimal Du Val resolution therefore
types `C` as a smooth rational `(-2)`-root, not as a strict polar prime.

Writing `C=bF-sum_i x_iP_i`, the equations

```text
2b-sum_i x_i=0,                 sum_i x_i^2=2
```

give the exhaustive integral list

```text
+/-(P_i-P_j),                  +/-(F-P_i-P_j).        (2.1)
```

Every `F`-type root meets `S0` once.  The binding fact that no ADE point lies
on `H` away from `p_0` therefore excludes this family from every affine ADE
tree.  At the unique F5 tree there is exactly one such component:

```text
B3/A2: R1=F-P_l-P_o1,
U3/A3: E1=F-P_o1-P_o2.
```

It already occurs with coefficient two in the exceptional multiplicity
divisor:

```text
M_R(B3,tau!=0)=2R1+R2,
M_R(U3)=2E1+2E2+E3,
M_R(B3,tau=0)=2R1+2R2,
A-H=M_R.
```

Adding it again as a strict prime would double-count the same irreducible
curve in `r^*R_X`.

### 2.2 The other `A`-null curves are registered carriers

Suppose `C` is nonexceptional and `B.C>0`.  Its image is a curve contracted
by `pi`.  Since `X` lies in `P2 x P1`, a positive-dimensional `pi`-fibre is
the full `{q} x P1`; hence

```text
B.C=1,                         C^2=-3.
```

The target point `q` is necessarily affine.  If it lay on the infinity line,
the curve would be a component of

```text
pi^*(L_infinity)=H+M_R,
```

whose nonexceptional components `L,S0,T` all have positive `A`-degree and
whose remaining components are the roots of Section 2.1.  Thus `C` is
disjoint from `H`.  The lattice equations then give exactly

```text
Z_J=S0+2F-sum_(j in J)P_j,
J=O union K,                    K subset I, |K|=3.    (2.2)
```

For the nonzero-`tau` total, every `J` contains both outside coordinates,
whose multiplicities are one, so at most one carrier occurs and its
coefficient is one.  For `tau=0`, one required outside coordinate has
multiplicity zero, so no `Z_J` is effective.  These are precisely the
existing `+Z` and no-`Z` cells.

### 2.3 Every strict prime is already localised at `p_0`

Every remaining nonexceptional curve has `A.C>0`.  Globally,

```text
A.C_str=C_str.(H+M_R)=8.
```

At `p_0` alone the fixed F5 equation gives

```text
length C[[v,z]]/(h,h_z)
 =ord_v Res_z(h,h_z)
 =ord_v(-v^8(v+1))=8.
```

Contracted carriers lie over affine target points and do not contribute at
`p_0`.  Hence the local cluster already consumes the full global strict
degree: no strict prime meets infinity away from `p_0`, and the registered
weighted germs exhaust all positive-`A`-degree strict primes.  Sections
2.1--2.3 prove the promised trichotomy and, in particular, prove that no
`A`-degree-zero strict prime exists.

## 3. Corrected local contact census

The exact local equations are

```text
h=z(z-v)((1+v)z-v),
f=h+u(a(z)u+b(z)v+c(z)),             c(0)=0,
p=f_z.
```

The review independently reconstructed every physical site, infinitely-near
chronology, degree, Cartier weight, exceptional vector and pair rule.

### 3.1 `U3/A3`

Here `b(0)=0`, `c'(0)=c1!=0`, and normality forces `a0=a(0)!=0`.  There are
exactly two reduced coefficient-one strict germs of degrees

```text
4+4,             m=(2,2,1),             n=(2,1,0).
```

One is the smooth midpoint germ at the common `S,T` site of `E1`; the other
is the cusp through the `E1-E2` node.  Their determining coefficients depend
only on the nonzero `a0,c1`, so there is no hidden modulus-special subcell.

### 3.2 `B3/A2`

Here `b(0)!=0`; normalize only `b(0)=1` and write

```text
c(z)=-tau z+c2 z^2+... .
```

Do **not** normalize `a(0)` to zero.  The review recomputed the charts with
`a0=a(0)` free.  The first exceptional polar is independent of `a0`, so the
nonzero-modulus census is unchanged.  For `tau!=0`,

```text
m=(2,1),             n=(3,0),             Delta=9-8tau.
```

If `Delta!=0`, the partition is `4+2+2`.  If `Delta=0`, the exact analytic
Weierstrass discriminant gives the exhaustive alternatives

```text
two coefficient-one branches of degrees 2+2;
one coefficient-one irreducible branch of degree 4;
one coefficient-two doubled prime of underlying degree 2.
```

For `tau=0`,

```text
m=(2,2),             n=(2,2).
```

There are fixed coefficient-one germs of degrees four and two on `E1`, plus
an `E2` residual block.  With `a0` free its first-site quadratic and
discriminant are

```text
Q=(a0^2-a0*b1+a1)X^2+2(2a0+c2)XW+3W^2,
D0=a0^2+3a0*b1+4a0*c2+c2^2-3a1.                     (3.1)
```

The nonzero `W^2` coefficient prevents the zero polynomial and prevents a
node collision.  The exact residual alternatives are

```text
4+2+1+1;          4+2+2;          4+2+(2 times 1).
```

The last is a genuine doubled Cartier prime, not two graph vertices.

Two upstream tag boundaries are explicit dependencies, not omitted cells.
The value `tau=1` is the `B_4/A_3` row already eliminated because its
singleton contact is internal.  The remaining coefficient cell
`b(0)=0,c'(0)=0` has corank two and is of `D/E` type, outside the binding
`q=6` tag list `{B_3/A_2,U_3/A_3}`.

No site polynomial vanishes identically; the contact counts saturate every
exceptional component; the quadratic sites do not collide with `q_L`,
`q_ST`, or an ADE node; and the two Weierstrass trichotomies retain arbitrary
infinitely-near depth.  This closes the local-contact review gate with the
corrections above.

## 4. Exact enumeration and what carries the zero

The v2 engine correctly enumerates

```text
a_j=B.C_j>=0
```

for every reduced strict prime.  The only global `B`-degree-zero numerical
classes with positive strict degree are

```text
F,                F-P_o,                F-P_i,
```

with the exact label and unit-contact restrictions in the charged report.
This global use of “vertical” is distinct from the local doubled prime in
`f=h+uv`; that local example does not realize a global marked class.

Across the fourteen affine cells and fourteen rank-zero controls, every
branch-feasible row was sent to the exact weighted-coordinate join.  The
result is

```text
pair-condition matches before carrier filtering: 0;
necessary-lattice survivors:                    0;
energy_pruning_applied in every row:            false.
```

The old positive-`a` domain is not independently empty.  Removing
`connected_coordinate_blocks`, `branch_differences`, and the single-owner
root rule produces eleven relaxed matches in four cells.  Those constraints
are load-bearing and exact:

- `C.(P_p-P_q)=x_p-x_q` gives the coordinate-block equations;
- `n=C_A m` follows from total-pullback orthogonality for the regular polar
  function on the Du Val resolution;
- the single-owner rules follow from the reduced boundary forest, because
  Section 2.3 places every strict prime at the same connected `p_0` cluster.

Here the forest theorem is charged only through the actual everywhere-defined
dominant etale first leg.  The false rational-map strengthening is not used.

The inequalities labelled “energy” remain valid diagnostics for `a>=0`, but
they eliminate no executable row.  In particular degree-one vertical
candidates die in the exact join, not by an energy estimate.  Conversely,
the independently relaxed `a=0` superset has zero pair matches without any
root/block machinery; this is a robustness check, not a replacement for the
positive-domain allocation.

No code extension is required.  Roots, carriers, and strict primes are
separate types in the engine.  An `F`-type exceptional root is already in
`M_R`; inserting it as a strict object would both double-count the curve and
change the frozen total.  A carrier enters only through (2.2), while strict
primes enter through positive `A`-degree partitions.

There is one precise software boundary.  If a fictitious fifth strict prime
of `A`-degree zero is appended, twelve of fourteen cells still return zero,
but the two four-prime `tau=0` cells stop at the registered arity guard
`2<=count<=4`.  Those two cells are closed by the proved typing theorem of
Section 2, not by enumerator coverage of a nonexistent fifth prime.  The
abort is fail-closed and is not evidence for the mathematical theorem.

## 5. Maximum-safe promoted theorem

Fix a reduced normal-singular bidegree-`(2,3)` quadratic incidence

```text
X subset P2 x P1
```

in the actual proper-block first-leg scope.  Assume the binding normal-F5
marking and carrier/effectivity theorem, Euler/`A1`-ruling caps, the corrected
morphic rational-forest theorem, reduced infinity, the statement that no ADE
point lies on `H` away from `p_0`, and the marked `q=6` tag list
`{B_3/A_2,U_3/A_3}`.

Then:

1. every irreducible curve on the minimal Du Val resolution is exhaustively
   typed as an exceptional root, a registered contracted carrier, or a
   strict/nonexceptional prime of positive `A`-degree;
2. the strict polar decomposition at `p_0` belongs to one of the nine
   reviewed local degree/Cartier/contact strata of Section 3 and has total
   weighted `A`-degree eight;
3. after allowing all nonnegative global `B`-degrees, every affine-ADE root
   allocation within the Euler caps and every rank-zero control fails the
   exact local pair conditions before post-match carrier or unlabelled-cycle
   filtering.

Consequently neither `U_3/A_3` nor any `B_3/A_2` modulus admits a necessary
global boundary configuration in this fixed presentation.  Stop all further
reduced normal-singular quadratic-F5 `q=6` lattice and local-contact work.

## 6. Exact boundary of the promotion

This is a necessary-lattice theorem in one fixed quadratic presentation.  It
does not prove that a quadratic frame exists or is minimal, and it does not
assert coefficient realization, effectivity, attainment, a finite cubic
algebra, a polynomial map, a counterexample, or JC2.  It does not cover

```text
nonreduced infinity;
nonfinite or projective-basepoint closures;
degree drop;
nonquadratic incidences;
failure of the upstream q=6 marked-tag list.
```

`REPRESENTATIVE` is not `FULL_ACTUAL_EXIT`, and this integration makes no
exit-price assertion.

The cheapest successor is therefore outside `q=6`: attack quadratic-frame
existence and intrinsic minimization first, since failure there makes the
entire fixed-presentation lattice program vacuous.  The remaining scope
escapes then proceed through nonreduced infinity, nonfinite/projective-
basepoint cases, and degree drops.  Raising the software arity from four to
five would be optional insurance only; it is not required for this theorem
and should not delay the hard stop.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `16383`.
- Body SHA-256:
  `f8728359cb837ec7b54c69f07cada5892f67461c3f0458eff91f0d4566e046e5`.
- Frozen basis: `ae7d44ea2148980fb1e2d3bd12c413041a0d25d4`.
