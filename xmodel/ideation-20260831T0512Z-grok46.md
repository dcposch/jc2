# Grok 4.6 independent significant-news ideation

Date: 2026-08-31T05:12Z  
Lane: equal-standing whole-portfolio researcher (Grok 4.6)  
Git basis: `e66542021690fcc7a1100482716804b2d8f174f7`  
Packet SHA-256 (reproduced before any charged read):

```text
daceadc1d5c22ec752b651f8be53860502ad64020134091c8a765aabf6bdb991
  xmodel/ideation-20260831T0512Z-significant-news-packet.md
```

Charged-input SHA-256 values were reproduced before each file was used; all
nineteen matched the packet. No sibling report, log, receipt, or excluded
workspace was inspected. No Git object and no file other than this report
was written.

Evidence tags used below: **PROVED** = different-model promoted in the
charged ledgers; **PROVISIONAL** = producer-exact or review-integrated but
not to be consumed as promoted (the packet overlay is authoritative after
2026-08-30T23:06Z); **CONJECTURAL** = a proposed implication whose
interfaces are not yet checked; **COMPUTATIONAL** = a finite identity or
empty bounded cell, not a theorem. No proof, counterexample, or avenue
closure is claimed.

A small in-memory rational check on the charged `(4,3)` normal form is
recorded in §3. It is **COMPUTATIONAL** and is not a cover obstruction.

---

## 0. What the overlay actually changes

The 23:06Z and packet overlays move the live laboratory, they do not finish
JC2.

- Generic field degree three is **PROVED** empty: `[C(x,y):C(F,G)]!=3`.
  This is function-field degree, not coordinate or total degree.
- The all-degree nontrivial-branch statement is **useful and PROVISIONAL**.
  The packet overlay overrides later “promoted” wording in the 23:06Z
  integration: descendants may explore it, they may not treat it as a
  promoted all-degree theorem.
- Every actual rank-four proper block has `b1(B)=n22>=1` and a `(2,2)`
  fibre, with monodromy `S4` after the charged Chau/one-place repair
  (**PROVED** in the rank-four integration; no rank-four exclusion).
- Irreducible one-place `S4` meridians force `Delta_aff(B)>=3` (**PROVED**
  as a lower bound, not as attainment). Genus-three cables with `b1=1`
  reduce to `T(3,4)` (**PROVED** as a knot/delta-sequence census).
- The charged irreducible `m=0,n4=0,Delta_aff=3` row is **PROVISIONAL**
  empty by cubic-projection ramification plus a Hurwitz-invariant duplicate
  transposition in a local `C2`. Independent hostile review is still owed.
  The complementary `(4,3)` classification shows the argument does not
  assume immersion.
- The charged `m=1,Delta_aff=3,T(3,4)` row is **OPEN**. Its `(3,1)` packet
  has group `S3`. Recycling the `m=0` duplicate-at-`T31` sentence is
  invalid.
- One-cusp rank-four is a saturated function pair with inverse residual
  Kummer class (**PROVISIONAL**). The surviving gate is an index-four
  subfield of the cyclic/Kummer field obeying the logarithmic Jacobian
  equation, one-valuation regularity, and the saturated `(3,1)+(2,2)`
  packet. Bare invariant-ring, constant-unit, cyclic-deck, and naive
  companion-resultant shortcuts have controls.
- Zero-cusp double-plane is a locally extendable anti-invariant `Pic(D)[3]`
  problem, not a boundary-Fox problem. The spectator
  `(t^3-3t,t^4-2t^2)` is a **PROVED** negative control: affine sign-Fox
  dimension one, `Pic(D)[3]=0`.
- Weighted degree six of the normalized pullback invariant-ring cell is
  **COMPUTATIONAL** empty, by two parameter-independent Jacobian
  coefficients. Degree eight is open.
- No external characteristic-zero JC2 resolution. Strinz’s 30 August note
  is cross-pollination, not promotion input.
- Rank-four proper-block emptiness, even if later proved, is not JC2:
  primitive/no-block maps, degree `>=5`, reducible branch, and the surviving
  cubic affine-base block with `d1>=2` remain.

---

## 1. Whole-avenue reassessment

The dated 46-row table in `APPROACHES.md` is still the inventory. Its
scores and “stuck where” sentences predate the branch-topology laboratory.
The 23:06Z overlay already said to attack the polynomial knot at infinity
across degrees and to treat one-cusp as a saturated two-curve problem; the
numbered table was not rewritten. That is the main staleness.

Disposition relative to the 2026-08-21 table. Only changes are listed;
unlisted rows are `unchanged`.

| # | Disposition | Reason |
|--:|---|---|
| 2 | lower as *critical path*, keep as long-range spine | td-ladder/QCS/`G2-PSC` remain the global landing walls, but they are not the current information-dense laboratory. Local td12 work stays conditional. |
| 3 | lower | Strip ODEs are a scoped theorem, not a live discriminator. |
| 4 | unchanged (disproof) | Formal germs stay independent of rank-four exclusion. |
| 6 | reopen / raise | The one-place theorem is now used on Chau components of `A(F)`, which is the object the surveys already said was correct. The `(4,3)` and `(6,4,3)` normal forms are AM-semigroup calculations. |
| 7 | raise | Chau’s polynomial parametrization is load-bearing for rank four and for degree three. Constructing the whole `A(F)` remains hard; consuming the one-place shape does not wait on that construction. |
| 10, 11, 18, 41, 44 | lower / keep dead | HC4 `NO LEVERAGE`, Zhao ladder, graded maps, naive scaling, Moskowicz-as-proof. |
| 19, 21 | unchanged (disproof) | Witt/polynomial-limit and `A_infinity` stay the honest char-p walls. Degree-six invariant-ring identities fail in characteristic two (`8=4=0`), so that cell is not a char-p no-go. |
| 20 | reopen slightly | p-curvature as a *pattern finder* for the log-Jacobian, not as a JC2 engine. |
| 25 | raise | Meridional transposition `S4` colourings are now a gate, not a generous Riemann-existence census. |
| 26 | raise | Forced non-`A^2` sandwiches and the primitive/no-block horn are the rank-four-independent proof front. |
| 27 | raise | Iterated-cable classification is the genus-three machine. Genus two and four are the same avenue, not a new slogan. |
| 28 | raise | `K_U=0` and the log two-form on the one-cusp pseudo-plane are live. Ordinary BMY slogans stay weak. |
| 30 | raise | Pseudo-plane cyclic covers and Dubouloz–Palka endomorphisms are the one-cusp ambient. Their target is `U`, not `A2`; that control is load-bearing. |
| 31 | raise | Finite-flat second legs, the canonical normalisation, and the monogenic index locus `u^mu-v^mu=0` are the same integrality cluster. |
| 33 | reopen / raise | Ordinary action residues remain `COSTUME`. The logarithmic Jacobian `J_(X,S)=c/(mu X)` is a new, scoped symplectic client. |
| 36 | raise as laboratory, not as search | K00 remaining faces and the invariant-ring cells are structured coefficient problems. Generic SAT search stays worthless. |
| 37, 38, 39, 42, 45, 46 | unchanged-low | No new 10-line class appeared. Motivic slogans stay parked. |

Newly viable dormant connections, not cosmetic restatements:

- Avenues 6 + 25 + 27: one-place semigroup, meridional `S4`, iterated cables. This is the actual genus-three/four engine.
- Avenues 28 + 30 + 33: log Calabi–Yau structure of the cyclic pseudo-plane versus a polynomial map to `A2`.
- Avenues 7 + 31: Chau components plus the inverse-Kummer monogenic index, asking whether that index spends an Orevkov–Chau unit.
- Avenues 19 + 36 + the invariant-ring cell: reduction modulo `p` of parameter-independent Jacobian coefficients as a lift-obstruction probe.
- Avenue 26 independently of the cyclic `S4` laboratory: the primitive/no-proper-block case at every degree, and block index two.

Stale ranking to kill explicitly: “td=6 is the proof backbone, AS109 is the disproof backbone, rank four is a side laboratory.” After the overlay, the highest expected-information desk work is the charged cyclic rank-four horns and the all-degree knot-at-infinity successor. td=6 and K00 remain necessary global routes; they are no longer the cheapest discriminators.

---

## 2. Bottlenecks

**Strongest proof attack.** Finish the irreducible charged genus-three row
by an honest `m=1` argument that never treats the local `S3` at `T31` as a
`C2`, then run the same cubic-projection length-two analysis on every later
one-place row whose normalisation has a degree-three coordinate. In
parallel, attack the one-cusp logarithmic Jacobian as a global index-four
problem, not as polynomiality.

**Strongest counterexample/falsification attack.** Do not wait for rank
four to close. Keep (i) remaining K00 ramified/loaded faces, (ii) the
degree-eight invariant-ring cell as a *pattern* laboratory, (iii) the
primitive/no-block horn, and (iv) a typed Strinz-band crosswalk that
refuses to promote truncation witnesses. A nonempty exact cell with
geometric degree `4mu` would be a plane Keller map and must be quarantined,
not celebrated.

**Independent of the current rank-four proof front.** Genus-two
graph-knot/meridional-rank (all-degree); primitive sandwiches; cubic
affine-base ramification for `d1>=2`; K00; Witt/polynomial limits.

---

## 3. Idea cards

Eight mathematically distinct ideas follow. I1, I5, I6, I8, I9, I10 are
cross-avenue or new rather than refinements of a single live lemma. Scores
are expected information per cost on a 0–10 scale, not probabilities of a
JC2 proof.

### I1. Length-two cubic ramification of the charged `m=1` row

**Implication hoped for.** If the interfaces in this card hold, the
irreducible charged packet

```text
Norm(B)=A1,  Delta_aff=3,  b1=n22=1,  n4=0,  m=|T31|=1,  G=S4
```

is empty, hence the whole irreducible charged genus-three row is empty
once the **PROVISIONAL** `m=0` theorem is independently reviewed.

**Hypotheses.** Charged `(4,3)` normal form; Zariski–van Kampen generation
by the three vertical meridians of the degree-three normalisation
coordinate `X`; point-stabiliser table of the charged `S4` cover; Hurwitz
action preserves the subgroup generated by the whole tuple (already used
for `m=0`).

**Why this sees `S3` rather than recycling `C2`.** The unique `(3,1)`
value has local group `S3`, which may be generated by overlapping
transpositions. No duplicate-colour sentence is applied *at that value*.
The cubic `X=t^3+a t` has ramification divisor of length two. That divisor
is a scheme on the normalisation `A1`, not a quartic fibre type and not a
physical place of `B`. The three objects are kept distinct:

- critical *parameters* of `X` (zeros of `X'=3t^2+a`);
- normalisation *fibres* of `nu:A1->B` (diagonal versus two-point);
- quartic *local partitions* `((2,1,1),(3,1),(2,2))` of `pi`.

The only legal `C2` duplicate is at a `(2,1,1)` value whose local group is
actually `C2`. The legal `S3` sentence is: if all three vertical meridians
live in a sheet-fixing `S3` (double critical scheme supported at the unique
`T31` *value*), transitivity on four letters fails. The legal `T22`
sentence is: if the leftover critical parameter is a conductor endpoint,
the `2+1` cluster is charged to the local group of the unique `(2,2)`
value, which is intransitive on four letters.

**Weakest failure.** Identifying an `X`-critical parameter with a quartic
fibre type without the local-group table; or treating two meridians of a
smooth tangency as a duplicate transposition after a braid tail without a
Hurwitz-invariant subgroup comparison (the `m=0` repair). A second failure
is silent immersion: a diagonal parameter can be a point-bijective
unibranch singularity.

**First falsification test.** On each of the four charged `b1=1` strata of

```text
X=t^3+a t,   Y=t^4+b t^2+c t,   G(s)=s^3+(2a-b)s-c,
```

list the zeros of `X'` and compare them, as *parameters*, with the
admissible two-point fibre of `G` and with the diagonal roots `D(s)=0`.
Then, separately, read the quartic partition only from the charged
point-stabiliser table at the *image point* in `B`. If any `m=1` stratum
has both critical parameters absorbed by the unique `T31` point and the
unique conductor pair, with no leftover `(2,1,1)` value, the cheap kill
fails and the double-critical/`T22` branches must be checked on the nose.

**COMPUTATIONAL observation (not a theorem).** In-memory rational
arithmetic on the charged normal form:

1. `a=0` makes `X'` a double root at `t=0` and makes `D(s)=-3s^2`, hence
   the only diagonal root of `G` is `s=0`. That forces `c=0` to have a
   diagonal at all, and then `G=s(s^2-b)` has either no two-point fibre
   (`b=0`) or two (`b!=0`). So a double critical *parameter* is
   incompatible with `b1=1`. This is an identity on the normal form, not
   a monodromy statement.
2. On stratum II (`a=b=-3r^2`, `c=-2r^3`) one critical parameter is the
   diagonal cusp and the other is a distinct parameter whose `X`-value is
   not the conductor value (`X(r)=-X(2r)` on the specialisations `r=1,2`).
3. On stratum IV (`a=-3q^2/4`, `b=3q^2/2`, `c=-2q^3`) one critical
   parameter is the diagonal unibranch point and the leftover `X`-value
   differs from the `T31` value.
4. Stratum III places both critical parameters on the two diagonals (two
   cusps): this is the spectator shape, `m=2` if both unibranch points are
   `(3,1)`, hence not the charged `m=1` row.
5. Distinct critical parameters cannot share an `X`-value except at `a=0`,
   already outside `b1=1`.

These five identities *suggest* that every `m=1` point of the family has a
leftover `X`-critical parameter whose image in `B` is a singleton fibre at
a different target value from both `T31` and the conductor. If that image
is a smooth point of the charged branch, the local partition is `(2,1,1)`
and the `m=0` Hurwitz-duplicate argument applies *away from* `S3`. That
sentence is still **CONJECTURAL**: it has not reconstructed the local
frame at a smooth tangency of the projection, nor proved that every
singleton leftover is a branch value of `pi` with group `C2`.

**Primary interface.** `2b3cc0b7...` §3–4 (normal form and why cubic
ramification escapes the conductor); `bcfc47b0...` §4 (Hurwitz-invariant
subgroup, `C2` duplicates); point-stabiliser table in `f5dbad98...` §3;
Chau one-place input as bound in `5d7df7ce...` §2.

**Score.** 9.

**Next deliverable.** A two-page exact table, one row per stratum, with
columns `(critical parameters, normalisation fibre type, image in B,
charged local partition, local group, generated subgroup after Hurwitz)`.
Replay: extend the existing `t34` projection script by the four strata;
mutations `--mutate-identify-T31-with-Xp-zero` and
`--mutate-treat-S3-as-C2` must fail. Verdict `EMPTY` or typed `OPEN` per
stratum; no analogy fill.

### I2. Unique `A2` link as a Fox kernel, not as a colouring slogan

**Implication hoped for.** On every `m=1` geometric realisation of
`T(3,4)` with `b1=1`, the restriction

```text
H^1(A2-B, L_sign) --> H^1(M_{A2}, F3) ~= F3
```

is injective, hence `Pic(D)[3]=0` by the exact middle-term kernel of
`60ba8f43...`. This would kill a double-plane *étale* lift. It does **not**
by itself kill the charged `m=1` `S4` row, because `W->D` is already
allowed to ramify at the unique `(3,1)` point.

**Hypotheses.** Exact localisation (0.1) of `60ba8f43...`; reduced
connected `B`; one ordinary-cusp (or `A2` double-plane) singularity and
one node; spectator computation `a25d4b2f...` as the two-cusp control.

**Why this sees `S3`.** An `A2` surface link has `H_1=Z/3`; that is the
resolvent image `S3` at `(3,1)`, not a `C2` duplicate. The node (`A1`
surface link) contributes no mod-three class. The spectator restriction
was `(lambda,lambda)` on two `A2` factors and was injective. One factor
can kill or preserve a one-dimensional affine Fox space; that is a
computation, not an analogy.

**Weakest failure.** Treating `Pic(D)[3]=0` as an `S4` obstruction.
Boundary colourability is already known insufficient. A second failure is
using the spectator braid word for a different stratum.

**First falsification.** Repeat the spectator Fox count on stratum II
(exactly one ordinary cusp, one two-branch conductor). If the affine Fox
space is zero, the kernel is zero for a trivial reason and the test is
uninformative for Pic. If it is one-dimensional and the unique cusp
restriction vanishes, Pic may be nonzero and the idea fails as a kill.

**Primary interface.** `60ba8f43...` (0.1)–(0.3); `a25d4b2f...`;
corrigendum `be4c287f...` for the spectator predecessor hash.

**Score.** 6.

**Next deliverable.** One exact Fox matrix for a single integer point of
stratum II, with the spectator as a live negative control.

### I3. Compactified intersection of the `(4,3)` quartic, with partitions kept off the infinity line

**Implication hoped for.** The projective closure is a rational quartic
smooth at the unique infinity point `[0:1:0]`. Intersection theory of that
quartic with a generic line through a finite `(3,1)` point versus the
conductor pair forces either a second two-point fibre (`b1>=2`) or a
second unibranch point (`m>=2`).

**Hypotheses.** Charged homogenisation in `2b3cc0b7...` §2; arithmetic
genus three; smoothness at infinity.

**Why this sees `S3`.** It never mentions meridians. The `(3,1)` is used
only as a unibranch analytic type of `B`, i.e. as a place of the curve,
not as a cover series.

**Weakest failure.** The compactification of `B` does not see `pi`. A
numerical coincidence on `B` can be realised (spectator, stratum I).

**First falsification.** In stratum II, the curve already exists as a
polynomial parametrisation. If that embedded quartic violates the hoped-for
intersection inequality, the idea is dead on a control.

**Primary interface.** `2b3cc0b7...` (2.1)–(2.3).

**Score.** 3. Useful only as a control on I1, not as a standalone kill.

**Next deliverable.** Compute the intersection multiplicity of the
projective tangent line at the unique unibranch point against the
conductor pair, on stratum II. Stop if the spectator or stratum II already
attains the “forbidden” numbers.

### I4. Genus-four successor, symbolic before enumeration

**Implication hoped for.** The irreducible charged `m=0` packet with
`Delta_aff=4` is either emptied by determinant/`S4` colouring and
semigroup freeness, or reduced to a short explicit list of delta-sequences
to which I1’s leftover-ramification test applies without listing
coefficients.

**Hypotheses.** `g_3(K_infinity)=Delta_aff` and primeness of nontrivial
cables (**PROVED** in `03865aae...`); Assi–Garcia-Sanchez conductor
formula and `h<=log2(g+1)`; charged `b1=n22=1`, `n4=0`, `m=0`;
meridian-compatible boundary surjection.

**Symbolic work that does not wait on enumeration.**

1. One-stage sequences: `(r0-1)(r1-1)=8` with `r0>r1`, coprime, gives
   `(9,2)` and `(5,3)`, i.e. `T(2,9)` and `T(3,5)`.
2. `det T(3,5)=5` is not divisible by 3. The composition
   `S4 -> S3=S4/V4` sends meridian transpositions to `S3` transpositions,
   so a meridional-transposition `S4` colouring would give a Fox
   3-colouring of the infinity knot. Hence `T(3,5)` is excluded by the
   already written determinant gate `60ba8f43...` (0.3), with no
   parametrisation list. This is a colouring obstruction, not a Pic
   attainment.
3. `det T(2,9)=9` is divisible by 3. The boundary `S4` colouring gate is
   then a finite representation count, still with no affine coefficients.
4. Two-step conductor-eight sequences are finite (`h<=2`). The same
   freeness filter that killed `C_(3,±1)(trefoil)` at genus three applies
   verbatim; winding-one cables are not assumed to be “the same knot” and
   must be deleted only by a semigroup or Schubert identity.
5. If a surviving sequence has a degree-three coordinate, I1’s length-two
   ramification test is available before any Groebner computation. If the
   smallest coordinate has degree two (`T(2,9)`), `X'` has degree one and
   the leftover arithmetic changes: one critical parameter, not two.

**Local Fox.** Nodes contribute nothing mod 3; each ordinary cusp
contributes an `F3`. For `m=0` there is no `(3,1)` and therefore no `A2`
double-plane link in the charged table. Affine Fox must still be computed
after the colouring gate, because the spectator taught that boundary
colourability is insufficient *for Pic*. For the `m=0` `S4` row the
primary obstruction remains the quartic lift / local `C2` table, not Pic.

**Quartic-lift gate, last.** Only sequences that already have a transitive
meridional `S4` image and `b1=1` are entitled to a cover-side local-group
argument.

**Why later `S3` does not sneak in.** The `m=0` successor never calls a
local group `S3`. If a unibranch point appears it is a different row
(`m>=1`) and is parked.

**Weakest failure.** Treating the determinant non-divisibility as
`Pic(D)[3]=0` *and* as non-existence of an affine `S4` representation
without the boundary-to-affine surjection. The charged interface supplies
that surjection for one-place polynomial curves; it must be cited, not
re-proved by slogan.

**First falsification.** Count labelled meridional-transposition `S4`
colourings of `T(3,5)` (expected zero) and of `T(2,9)` (unknown). A
positive `T(3,5)` count refutes the determinant bridge as stated.

**Primary interface.** `03865aae...` §§1–3; `03b16c2d...` §§2–3;
`60ba8f43...` (0.2)–(0.3); Schubert cable genus.

**Score.** 7.

**Next deliverable.** A one-page conductor-eight census with four columns:
delta-sequence, knot, `3|det`, number of labelled `S4` transposition
colourings. No coefficient ring.

### I5. Logarithmic Jacobian as a weighted identity in `C(X,S)`, not as polynomiality

**Implication hoped for.** There is no pair `F1,F2` in the single-
valuation ring `O(U)=C[X,X^{-1},S] cap O_(U,eta_Phi)` with

```text
J_(X,S)(F1,F2)=c/(mu X),    C(X,S)/C(F1,F2) of degree 4,
```

and with the already saturated `(3,1)+(2,2)` packet on `B`. This would
empty the one-cusp cyclic horn for every `mu>=2`.

**Hypotheses.** Invariant-ring theorem `0e2e09c8...` (0.2)–(0.5);
primitive single-pole model only as a control, not as the general Laurent
shift; inverse-Kummer successor **PROVISIONAL**; Formanek primitivity of
`X` in the quartic field.

**Tools, with controls.**

- *Field theory.* `M=C(X,S)=K(X)` with `[M:K]=4`. The minimal polynomial
  of `X` over `K` has degree four. Combined with `dF1 ^ dF2 = c dX ^ dS /
  (mu X)`, the trace of `dX/X` along the four-sheeted cover is a
  logarithmic form on a dense open of `A2`. Control: the split DVR
  `T=(u^mu,1,2,3)` realises `e=f=1` at `Phi` for every `mu`, so no local
  discriminant or index argument at `eta_Phi` can finish this.
- *Valuations.* `nu(X)=mu`, `nu(S)=1-r`. Nonnegative orders of `F_i` at
  `Phi` do not forbid `nu(J_(X,S))=-mu`, because `(X,S)` is not a regular
  coordinate system across `Phi`. Leading-term cancellation in the
  invariant monomial basis is the actual condition (already stated in
  `0e2e09c8...` §3). Control: do not compare the two weights `nu(X),nu(S)`
  as a membership test.
- *Logarithmic symplectic geometry.* `U` carries a nowhere-zero two-form
  with `K_U=0`. The pole `1/X` is the coordinate expression of that form,
  not ramification of `pi`. A map `U -> A2` pulling the Euclidean form
  back to that log form is exactly (0.5). Dubouloz–Palka endomorphisms
  target `U`, not `A2`; they are a warning, not a Keller map.
- *Companion resultants.* The residual class is inverse, and the collision
  divisor is the monogenic index `u^mu-v^mu=0`, not a second ruling fibre
  and not ramification of `f_mu`. The live question is whether that index
  spends a positive Orevkov–Chau unit at infinity. Control: the budget is
  already saturated; an extra unit is a contradiction, a zero unit is not.

**Weakest failure.** Calling `O(U)` a polynomial ring; calling the residual
function a second `A1` ruling; inferring non-existence from the degree-six
empty cell; using the pole of (0.5) as ramification.

**First falsification.** In the primitive model, the explicit pair `(u,v)`
of `0e2e09c8...` (6.3) already has nonconstant Jacobian (6.5). Any proposed
“first-jet plus polynomiality implies Keller” lemma must fail on that pair.
A second test: the degree-six cell’s identities `[x^2 y]J=8` and
`[x^4 y^2]J=4` must be recovered as the weight-`(3,3)` and weight-`(6,6)`
parts of `J_(x,y)` on the `(mu,r)=(2,2)` pullback basis, or the pattern is
not understood.

**Primary interface.** `0e2e09c8...` §§0,4–7; `2531a89d...`; inverse-Kummer
successor as **PROVISIONAL**; Dubouloz–Palka as external warning only.

**Score.** 8.

**Next deliverable.** A weight filtration of `J_(X,S)(F1,F2) * X` in the
`(mu,r)=(2,2)` invariant monomials through pullback degree eight, recording
whether any coefficient is parameter-independent. Empty or nonempty, the
output is a leading-form identity or a typed `OPEN`.

### I6. Inverse-Kummer index versus the saturated infinity budget

**Implication hoped for.** The coincidence divisor `u^mu-v^mu=0` meets the
completed infinity link of `B union C0` in a packet that costs at least one
Orevkov–Chau unit, contradicting saturation.

**Hypotheses.** Function-pair theorem `2531a89d...`; inverse residual class
**PROVISIONAL**; immersive `A1` normalisation of `C0`; `C0 != B`.

**Why this is not a naive companion-resultant shortcut.** The residual
class is inverse, not independent, and the collision is an order-index
locus. A resultant that ignores that typing will rediscover the already
killed “second Kummer class” argument.

**Weakest failure.** Spending a budget unit that resolution already counted
in the deleted lines; or treating the self-identification of `C0` as a
branch point of `pi`.

**First falsification.** On the producer’s all-`mu` hyperelliptic firewall
family (polynomial fibres, one cusp orbit, no bad ruling fibre), compute
whether the index locus meets infinity. If it “costs a unit” on a family
that is already budget-consistent, the unit is miscounted.

**Primary interface.** `2531a89d...` §§3–5; successor `bcc5148...` as
quoted in `AUDIT.md` (charged only through that quotation and the
function-pair integration).

**Score.** 7.

**Next deliverable.** A local Puiseux description of `u^mu-v^mu=0` at
infinity in the cyclic coordinates `(x,Y)`, with ramification index and
whether the place already appears in the deleted-line packet.

### I7. Degree-eight invariant-ring cell: AWS decision

**Decision.** Do **not** run a characteristic-zero Groebner of the full
twenty-parameter, fifty-seven-equation cell as a first job. Do run a
*modular leading-form reconnaissance* on idle `r6*` capacity only, after
the I1 table is written. The higher-value exact computation in the
meantime is I1 plus I4’s colouring census, both desk-scale.

**Why not a large job.** The degree-six emptiness was a parameter-
independent identity, found without a Groebner basis. A tiny degree-eight
expansion found no such coefficient. Twenty parameters is a memory wall,
not a topology gate. A unit ideal mod `p` is not a characteristic-zero
theorem; a surviving component is not a map until fibre degree `4mu` and
the boundary packet are checked.

**If the small probe is launched, the packet is:**

- Coefficient ring: `Z` for identities, then `F_p` for
  `p in {32003,65521,104729}` (two of these already appeared as d6
  cross-checks).
- Variables: coefficients of a generic pair in a fixed 13-element
  pullback-degree-eight invariant basis, after the lossless target
  normalisation that makes the linear part `(U,Z)` and the constant
  Jacobian `2` (d6 `RESULT.md`). That is at most twenty parameters; if
  the published 20 already includes that normalisation, do not undo it.
- Term orders: (1) grevlex on `(x,y)` in the Jacobian polynomial, to read
  leading coefficients; (2) grevlex on parameters only after a nonzero
  leading coefficient ideal is generated.
- Equations: vanishing of every nonconstant coefficient of `J(H1,H2)-2`.
  Do not saturate first. If a localisation is later required, extract the
  ideal component, declare the ring, and run the d6 identities as a
  positive empty control and the explicit `(u,v)` pair (6.3) as a negative
  (non-constant Jacobian) control.
- Split: one shard per `(mu,r)` in the charged list
  `(2,2),(3,2),(3,3),(4,2),(5,2)` with `gcd(r-1,mu)=1`; inside `(2,2)`,
  shard by support (which subset of the 13 basis elements appears in
  `H1,H2`), cheap first.
- Mutation: the d6 cell must still print `[x^2 y]J=8` and `[x^4 y^2]J=4`
  on every engine; a forced automorphism pair, if injected, must not be
  reported empty.
- Stopping rule: stop a shard when (a) a parameter-independent nonzero
  Jacobian coefficient appears, (b) the highest-weight part of `J` is
  already the unit ideal over `Q`, or (c) six hours with no repeated
  modular factor and no zero-dimensional component. Do not chase mixed
  components at dimension `>=1` in this probe.
- Ledger-changing output, exactly one of: a characteristic-zero
  parameter-independent coefficient identity; a leading-form theorem that
  the weight-`W` part of `J` cannot be constant; or typed `OPEN` with the
  modular Hilbert series. An empty modular shard is not ledger-changing.

**Score of the small probe.** 5. Score of a full 192-vCPU Groebner. 2.

### I8. Genus-two graph knots, all degrees, independent of rank four

**Implication hoped for.** No transitive meridian-to-transposition
representation in `S_d` for `d>=4` on an irreducible one-place branch with
`Delta_aff=2`. This would empty the next all-degree simple-cover horn after
the one-node and total-delta-two theorems, without any rank-four Euler
ledger.

**Hypotheses.** Nearby-fibre genus equals `Delta_aff`; infinity knot is an
iterated cable; genus-two graph knots are the pentafoil, a connected sum of
two trefoils, or a `(2,±1)`-cable of a trefoil (as already used to *exclude*
genus two in `03865aae...` for `S4`). The all-degree branch component with
nontrivial `pi1` is **PROVISIONAL**.

**New mechanism.** The delta-two `S4` theorem used a double-branched-cover
determinant to kill the prime families and primeness to kill connected
sums. For `d>4` the representation is not `S4`-valued. The cheap
replacement is meridional rank: a transposition moves two letters, so `k`
independent meridians generate a group supported on at most `k+1` letters
if the support graph is a tree. One must *prove* a tree, not assume it.

**Weakest failure.** Importing the `S4` determinant lemma into `S_d`, or
consuming the all-degree theorem as promoted.

**First falsification.** Exhibit a transitive meridian-transposition
image in `S_5` of `pi1(S^3-T(2,5))`. If one exists, the rank bound is
false for `d=5` and the idea must drop to a different invariant (e.g.
cycle type of a longitude).

**Primary interface.** `03865aae...` §0.3 and the Neumann–Rudolph/Schubert
stack charged there; all-degree statement as **PROVISIONAL**.

**Score.** 7.

**Next deliverable.** The support-graph of a putative transposition
representation of the pentafoil group, with the two-meridian trefoil
calculation as a live control.

### I9. Primitive and higher-degree block horns, without inferring JC2 from rank four

**Implication hoped for.** Either every proper block of index `d2=2` is
impossible, or a consistent sandwich model is banked as a control. Separately,
canonical normalisation at field degree `>=5` still needs a cycle, but
`(2,2)`-style fibres no longer force `S4`.

**Hypotheses.** Promoted block structure (non-Galois intermediate, missed
branch divisor); ruling on `U`; morphic forest. No use of the rank-four
Euler numbers.

**Weakest failure.** Applying the cubic point-bijective `R->B` argument at
degree two or five. Degree two has no room for a companion sheet of the
cubic type; degree five admits several partitions.

**First falsification.** Write the complete fibre-partition census for
finite-flat degree two with a generically unramified sheet. If every row
already contradicts `e(U)>=1`, the idea is a lemma, not a research
programme. If a cyclic row survives, it is the new laboratory.

**Primary interface.** Structure/Galois integrations charged inside
`95c99e2d...` and `5d7df7ce...`; ruling `7df557cc...` as cited there.

**Score.** 6.

**Next deliverable.** A degree-two fibre table analogous to the rank-four
census, with `e(U)` as a function of partitions, no resolvent.

### I10. Strinz band staircase as a K00 dictionary, not as a theorem

**Implication hoped for.** A typed correspondence between the campaign’s
literal K00 band objects and the five-periodic K5 staircase, sufficient
either to see that a named campaign face already contains the `q18` pole
or to see that it does not.

**Safe questions only (prose witnesses stay prose).**

1. Does any campaign source-truncation object already implement
   `q20` modulo `span(rbar^4)` at the first resonance, as a defined
   element of a named ring?
2. Does the exact `q18` leading pole, reconstructed from primary
   coefficients rather than from the note, vanish on any *campaign-named*
   face with labels analogous to `(m,k,ell)=(2,1,0)`? A kill inside
   Strinz’s tuned family is not a campaign theorem.
3. Is the robust `q19` quadratic normal form isomorphic, after a declared
   ring map, to a surviving K00 component? Matching names are not a map:
   declare generator order, coefficient field, and image of each generator.
4. Does the campaign possess the missing source-to-reduced adapter whose
   absence makes the reduced Row-40 tower only a reduced-model statement?
5. Independently of occupancy: is the five-periodicity an identity of the
   same unloaded graph that the campaign already proved for V20R2, or a
   different grading?

**Hypotheses.** Websweep `3b7e5db8...` items 1–5; campaign K00 source
rings as already in `APPROACHES.md` Avenue 36. No public exact Strinz
artifacts.

**Weakest failure.** Promoting a finite truncation or a “positive q19
component” to occupancy or to a characteristic-zero map.

**First falsification.** If question 1 has no campaign object whose
definition matches after a declared map, the crosswalk is `OPEN` and no
K5-through-K7 recompile is licensed from this note.

**Primary interface.** `xmodel/websweep-20260831T0454Z.md` only.

**Score.** 5.

**Next deliverable.** A one-page dictionary with columns (Strinz symbol,
campaign symbol or `NO OBJECT`, declared map or `OPEN`). Zero Groebner
bases.

### I11. Characteristic-`p` shadow of the invariant-ring identities

**Implication hoped for.** In characteristic two the d6 identities
`[x^2 y]J=8` and `[x^4 y^2]J=4` vanish, so the d6 cell may become nonempty
mod 2. A Witt lift of any such extra would have unbounded support (the
existing polar-conductor control) or fail at `W2`. Either outcome is a
control on “empty over `Q` implies empty everywhere”, not a
counterexample.

**Hypotheses.** d6 `RESULT.md`; Avenue 19’s growing-support Witt towers.

**Weakest failure.** Advertising a mod-2 point as a complex map.

**First falsification.** Evaluate the two coefficients in characteristic
two on the same ten-parameter family. If they remain nonzero for a
different reason, the idea has no cell.

**Primary interface.** `cases/quartic_invariant_mu2_d6_20260830/RESULT.md`.

**Score.** 4.

**Next deliverable.** One modular expansion in `F_2` and `F_3` of those two
coefficients, with the characteristic-zero identities as a control.

---

## 4. The surviving `m=1,T(3,4)` row

Four attacks, as required. None is a proof.

1. **Local braid/Hurwitz (I1).** This is the only attack that can close the
   charged `S4` row rather than a double-plane shadow of it. It uses `S3`
   by refusing to apply `C2` at `T31` and by asking the length-two scheme
   of `X'` to supply a *different* value whose local group is `C2` or
   intransitive `T22`. The computational scan in §3 makes a leftover
   parameter look generic on the `m=1` strata; that is a hint for the
   table, not a Hurwitz theorem.
2. **Fox/Picard (I2).** Sees `S3` as the `A2` link. Cannot recycle the
   `m=0` “étale `C3` torsor” sentence, because the charged one-cusp
   double-plane ramifies at `(3,1)`. Useful as a Pic constraint and as a
   control on overclaiming colourability.
3. **Compactification (I3).** Sees the unibranch place of `B`, not the
   cover. Weak, but it is the correct object for an intersection identity
   that does not mention meridians.
4. **Polynomial normal form.** Already charged: four `b1=1` strata. The
   remaining contradiction has to be cover-side (I1) or Pic-side (I2).
   Semigroup and `b1` alone do not kill `T(3,4)`; that is **PROVED** in
   `2b3cc0b7...` §0.

If every local-frame check in I1 returns a gap, the honest output is typed
`OPEN` on `m=1`, not a cap.

---

## 5. One-cusp global gate, restated

The live problem is I5 plus I6, not a new cyclic-deck lemma. Sharp
overclaim list, copied as controls rather than as folklore:

- `A cap M = O(U)`, not a polynomial ring.
- `1/X` is not ramification of `pi`.
- `nu(X)` and `nu(S)` do not decide membership.
- Not every Laurent shift is the single-pole model.
- First-order étaleness along `Phi` is realised by `(u,v)` and is not a
  Keller pair.
- The split DVR is not a global quartic field.
- Pseudo-plane endomorphisms are not maps `A2->A2`.
- A bounded AWS empty cell is not a degree-independent theorem.
- The residual function is not a second ruling unless its general fibre is
  `A1`.

Two independent desk attacks should run before any large elimination:
the weight filtration of `X J_(X,S)` (I5) and the infinity packet of
`u^mu-v^mu` (I6).

---

## 6. Higher-degree proof routes and counterexample programmes

Excluding rank-four proper blocks would still leave:

- primitive Keller maps (no proper intermediate);
- field degree `>=5` with a cyclic branch component;
- reducible branch and source-forest rows (`h>=2`), which the one-place
  knot machine does not cover;
- cubic second legs with `d1>=2` (field degree `>=6`), whose remaining
  affine-base shape is `C=A1`, `b0(B)=1`, `S0=empty`, `Q=0`;
- the global selector / td-ladder / QCS spine.

Counterexample-facing programmes that remain honest:

- K00 ramified and loaded faces, with source completeness still unproved;
- invariant-ring cells as coefficient laboratories (I7, I11);
- reduction modulo `p` of scoped identities, never as automatic lifts;
- formal-map algebraisation of a germ at infinity, still not a polynomial
  map;
- bounded search only behind the classical gcd-16 gate and only on
  unregistered actual-total pairs. No `(9,12)` or total-degree-`<=12`
  counterexample hunt.

A coefficient search that ignores those closures is a method control, not
a frontier advance.

---

## 7. Campaign and software improvements

Four incremental items, as required.

1. **Theorem-interface checker.** Implement the already written composition
   pass as a script: each new sealed statement is a record
   `(object, hypotheses, ring, degree/support bounds, conclusion, failed
   stronger readings)`. The script searches the claim DAG for hypothesis
   discharge and prints `BRIDGE`, `SCOPE-MISMATCH`, or `NO HIT`. First
   test: feed the `m=0` **PROVISIONAL** theorem and I4’s `T(3,5)`
   determinant sentence; the output must cite `60ba8f43...` (0.3) or
   report `NO HIT`, never invent a Pic attainment.
2. **Automatic adversarial controls.** Refuse to finalise a replay whose
   manifest lacks a named negative control *and* a mutation that must exit
   nonzero. The `t34` and `m0` scripts already have
   `--mutate-allow-duplicate-s4`; that pattern should be a schema, not an
   accident. First test: run the existing `m0` replay with the mutation
   flag and require nonzero status in ordinary, `-O`, and `-OO`.
3. **AWS packet scheduling / reproducibility.** Idle `r6*` nodes should
   consume only hash-pinned packets that already declare ring, term order,
   primes, saturation policy, mutation, and stopping rule (the I7 shape).
   Retrieval must verify `EVIDENCE.sha256` before any result is read, as
   the d6 job already did. Do not give the 192 stopped vCPUs to an
   undeclared degree-eight Groebner. Box01’s GB21 orbit is unrelated and
   must not be preempted for reconnaissance.
4. **Next-ideation mechanism.** Auto-emit a *charged interface card* for
   every theorem sealed since the previous full round: ten-line exact
   statement, scope, open horns, cheapest discriminator, and the hash of
   the producer. The present round spent most of its budget reconstructing
   nineteen papers whose overlay already knew the live horns. A card file
   `xmodel/interface-cards-<round>.md` is the only new artefact type; it
   is not a second `APPROACHES.md`.

Systems check for this round: **UPGRADE** the interface-card emitter
(item 4). Smallest test: produce cards for the eight charged 2026-08-30
block-descent theorems and diff them against §0 of this report; any
disagreement is a card bug, not a licence to edit ledgers.

---

## 8. Ranked next-wave allocation

Reviews stay background. Tasks marked **P** may consume a named
**PROVISIONAL** parent; tasks marked **R** may not.

| Rank | Owner | Task | Consume provisionally? |
|--:|---|---|---|
| 1 | Sol | I1 local-frame table for `m=1` strata; mutations against `C2`-at-`T31` | **R** on `m=0` (cite it, do not assume promotion); no consumption of the conjectural leftover sentence until the table exists |
| 2 | Internal agent | I4 conductor-eight `S4`/determinant census (`T(3,5)` first) | **P** on `g_3=Delta_aff` and the cable census; **R** on `m=0` emptiness |
| 3 | Opus | I5 weight filtration of `X J_(X,S)` in the `(2,2)` model through degree eight, desk identities only | **P** on the invariant-ring gate and inverse-Kummer; stop before any AWS Groebner |
| 4 | Grok / internal | I9 degree-two sandwich census; I8 pentafoil support-graph | **P** on all-degree nontrivial branch, labelled as such |
| 5 | Fable | Hostile review of the **PROVISIONAL** `m=0` complete obstruction *and* of the `(4,3)` normal-form audit, as a genuinely independent model pass | review, not a consumer |
| 6 | GPT-5.5 (already in flight) | Inverse-Kummer successor; topology interfaces of the all-degree statement | review; overlay keeps all-degree **PROVISIONAL** regardless of a later adjective |
| 7 | Internal | I2 Fox matrix on one stratum-II integer point, spectator as control | **P** on the localisation kernel |
| 8 | Grok | I10 Strinz/K00 dictionary with declared maps or `OPEN` | no parent theorem |
| 9 | AWS idle `r6*` only | I7 modular leading-form probe, after I1 is written or explicitly deferred | **P** on the invariant-ring description; empty modular output is not promotion |
| 10 | Fable, later | Review of any I1 `EMPTY` claim; do not start that review on a draft table | — |
| 11 | Sol / coordinator | Interface-card emitter (software item 4) | no mathematics |
| 12 | AWS large pool | **stopped** for this wave; 192 ready vCPUs stay ready; Box01 continues GB21 | — |

Successor consumption rule: I4 may use the genus-three cable census as
**P**. I1 may use the `(4,3)` classification as **P**. Nobody may use “genus
three is closed” or “rank four is closed”. A positive I7 component with
geometric degree `4mu` is quarantined as a possible map and gets two
independent exact verifications before any public sentence.

Stop conditions: two non-informative I1 interface failures force a
redesign (switch the leftover sentence to a pure braid word on stratum II,
or to I2). A third attempt needs a written justification. Degree-eight
AWS does not start if I1 is still the cheaper discriminator.

---

## 9. What would change my ranking

- A single stratum-II local frame in which the leftover `X`-critical
  value is not a `(2,1,1)/C2` point: I1 drops, I2 and the genus-four
  colouring census rise.
- Independent **REFUTED** on the **PROVISIONAL** `m=0` theorem: I4 is
  redesigned; I1 is untouched because it never applied `C2` at `T31`.
- A parameter-independent degree-eight Jacobian coefficient: I7 becomes a
  theorem-shaped identity and outranks further modular search.
- A declared ring isomorphism from Strinz `q19` to a nonempty K00
  component: I10 becomes a campaign face, still not occupancy.

Until one of those fires, the cheap exact work is the `m=1` table, the
`T(3,5)` colouring kill, and the log-Jacobian weight identity. Everything
else is background, review, or a counterexample laboratory that must
remain open after any rank-four success.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `44210`.
- Body SHA-256:
  `44a9059e7171493c9f7acf43a581c172812093be3a707a35c7e01619b94e4171`.
- Frozen basis: `e40079bf3699311a3955486ee300197e1248d374`.
