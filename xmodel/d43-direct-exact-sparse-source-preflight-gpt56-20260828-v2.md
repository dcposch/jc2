# Direct exact sparse D43 source route: corrected v2 preflight

**Status:** `GO_AFTER_FRESH_HOSTILE_PASS_AND_AWS_REGISTRATION`.
This is a light design and exact-prefix preflight.  No real D43 v2 jet or row
has been produced, no solve has run, and AWS remains prohibited.

## Revision record

This report supersedes, but does not rewrite, the v1 report currently hashed
as `480c7dcd...`.  The v1 sidecar originally named `539d6407...` before an
earlier revision and became stale; it is now resealed to the historical v1
bytes.  The corrected executable preflight is
`cases/d43_exact_sparse_source_preflight_v2.py` with its separately versioned
test.  The v2 packet's `SUPERSESSION.md` records the boundary.

The repairs respond to the sealed hostile report `283d47df...`, Opus audit
`456edb3a...`, Fable template-scope audit `69db2cd8...`, and exact E5/E6
bridge `e1b99600...`.

## Corrected route

The 22-tail support remains credible: the two registered modular points have
the same support, and independent source diagnostics predict exactly 29 live
raw-J rows in bands 20, 30, and 40, tail degree at most two, with full
22-column tail-Jacobian rank at both points.  These are modular shape facts,
not an exact characteristic-zero point.

V2 works in the rank-432 quotient algebra K0[W1,W2].  It substitutes
`HW1=hW1` and `HW2=hW2` in every A-orbit coefficient before expensive
multiplication.  It refuses a surviving EB and uses the exact EB-free B
blocks `((1+eta*t^20)^7-3/2)^6` and power 9.  Light exact tests establish
equality with post-collapse raw A jets through a nontrivial prefix and with
the raw B/GB blocks through D21.

The exact D21 band-20 reference is now collapsed into the same algebra.  Its
two independent HW-sign mutations and omitted-`+42` mutation have distinct
semantic digests.  A real D43 band must equal only the correct digest.

W1 and W2 remain solve coordinates.  There is no `[K:K0]<=16` or degree-6912
claim.  For the displayed localized template subsystem, E plus one
`W1*W2`-unit row is the exact existential E5/E6 projection.  V2 reconstructs
HM and s1F literally and replays both E5 rows and E6.  This suffices for that
displayed subsystem only; other template equations remain separate.

## Operational route

One immutable conditional manifest covers concurrent independent f/g builds,
matched receipts, the exact collapsed-D21 band-20 gate, and—only on that
PASS—the same-run emission and disjoint merge of all 184 rows.  There is no
second-manifest adoption gap and no solver.

The manifest authenticates the operational source list.  The live preflight
uses IMDSv2 to check account, region, instance identity/type, hostname, and a
real instance tag; it also checks the exact registered launch record, CPU
count, memory, disk, zero swap, source hashes, and all competing heavy algebra
processes, including duplicates in the same run directory.  Final artifact
custody is written after atomic terminal markers and binds the terminal
receipt.

## Remaining blocker and claim boundary

The v2 review manifest is intentionally blank and every compute path rejects
it.  The next actions are a different-agent hostile review, then fresh sealed
registration on an idle isolated AWS host.  Only that real run can establish
the exact 155-zero/29-live inventory and emit the 184 exact rows.

Even success would establish only an exact a00pp raw-J row emitter.  Exact
point existence, the displayed template extension, the remaining full
template locus, NF equivalence, all-depth compatibility, a Keller map, and
JC2 remain separate.
