# Critical-news full-spectrum packet — `20260828T1707Z`

Freeze time: 2026-08-28 17:07Z  
Coordinator: Sol Ultra  
Status: **SEALED SAME-INPUT BLIND ROUND**

## 0. Custody gate

Verify every hash below before reading the corresponding file. Fail closed
and report only the mismatch if any value differs. The repository basis is
HEAD `418e413593120d19e15e6546eb50c985f4b1f038` plus the explicitly hashed
dirty artifacts; do not infer a clean worktree.

```text
8c8ccdb2d01645a0f9ecf539cf723fdc68c3f8d6ee169e70d7abf1c751d1f480  APPROACHES.md
1c77721936564afcd44fc7b7d9edf7c39a11cf694cf94c03c3357f383045ae2a  AUDIT.md
3fc3c9d9e024d215040e7b389f3622a9ef1e5c640b848188bd4ff855c9bb35e6  COORDINATION.md
086859692739578885fc1a75bfa96c63caa8b1ef7e047d8a9ba8d9e08d1e1e07  PROGRESS.md
892769117e8c5849f682167b5e504799ee8cfdb58d1d8fbd48368e4f523f58d9  notes.md
ee7ab7177873158bb7374812626a41d0412c1872dad2c083e516c48b6dfc8af5  ladder/REDUCTION.md
ded3051d1a2009498f49bed20e5168d34b823518ab1b94ed340b380b37da6d15  ladder/SIGRAY-AUDIT.md
45d08fdb4789d00a32dd57163f40cd6375bfa3ee15e3ae6d1bc4bc2843fd9743  ladder/SHEET6-2POLE.md
48a5d9cd7d3cae484795c2f7f7e0b2b6e2c5a8d8482e4032d8481a3900b75bf9  ladder/SHEET6-MULTIPOLE.md
ad9ced6c0420902f8cf63edf3636bed8633be511e925f208d24a24c35d71036d  ladder/SHEET6-DEPTH.md
c043ab476de4d1dd99ee829a822b61cc9eea74843c86dd37e7c05569e11e8cb2  xmodel/ideation-20260828T1149Z-synthesis.md
e03a5bec4278362e8941312115b7bb7bdd9a7b6dd3eee6b58d836e5dbeb55701  xmodel/websweep-20260828T0524Z.md
7ec37063df25aa925faf0134635e9bc78341343b2e96354143399a66399c19d5  xmodel/sigray-sections7-9-coordinator-integration-sol-ultra-20260828.md
d534f083d6014838eadfb1be68996d6a0be50744619adcc5025ed780191bc906  xmodel/sigray-sheet6-depth-root-meet-coordinator-integration-20260828.md
c74fc0f99a830e3845e0821188c3f693b9b47d441e5c9f00716ac72ef2f09836  xmodel/sigray-multipole-selected-orbit-attachment-coordinator-integration-20260828.md
f55a00f5259d77766cc8179f3d1248ee0c1320daf411f04758487d2e94e216bb  xmodel/sigray-multipole-selected-orbit-attachment-hostile-fable5-20260828.md
8381659878f039d17cbc4ebb5a3ac364768d690d4a3895d54c29446f7745b060  xmodel/sigray-rootaware-engine-cap-repair-hostile-rereview-gpt5-20260828.md
```

Never enter, list, search, read, build, modify, status, or control the nested
user-owned `jc2-lean` tree.

## 1. Blindness and execution boundary

Read no peer `20260828T1707Z` response before sealing yours. Do not read any
file whose name begins `xmodel/ideation-20260828T1707Z-` except this packet,
your assigned prompt, and your own assigned output. Disclose accidental
contamination and fail closed if it could affect independence.

Do not touch AWS or running jobs. Do not run Singular, msolve, Sage, Lean, or
heavy/uncertain local algebra. Short read-only inspection, hashes, and small
standard-library exact checks are allowed. Do not edit canonical files or
cases. Your only write is the assigned
`xmodel/ideation-20260828T1707Z-<lane>.md`. Print its SHA-256 and disclose the
exact model, files read, checks run, failed attempts, assumptions,
contamination, and scope.

## 2. Authoritative critical news since the 12:24Z round

### 2.1 The Sigray source spine through Section 9 is review-closed at repaired scope

Proposition 5.4's missing pole degree/q-half, repaired Propositions 6.7--6.8,
Lemma 6.1, and an every-fibre replacement for the unproved printed
Proposition 5.8 have all passed independent review. Statement 6.2 remains a
source GAP exactly at its omitted `H in V_a cap T_a^+` premise, but current
consumers establish that domain before using the repaired next-vertex bridge.

The Section 8 audit corrects Proposition 8.4: it excludes `M=1` only at a
certified nonroot down vertex. Root `M=1` is legal; Statement 8.5 gives only
divisibility there. A first attempted Section 7 repair failed because
cross-fibre jump/max `kappa` is not constant. A separate actual-cluster-
weight proof passed: for pairwise-distinct cv flags in a prescribed fibre,

```text
td(f,g) >= 1 + sum_H kappa_H*(pi(H)-1).                 (C7.1*)
```

This proves the inequality needed by corrected Section 9 and review-closes
the campaign theorem `counterexample => td>=6`. It does not restore printed
equation `(22)`, literal `delta_a`, `(22-cl)`, fixed-baseline `kappa`, or an
equality/slack ledger. It does not exclude `td=6`.

### 2.2 Corrected all-`M=1` root-meet theorem

A genuine contact-zero root merge lies in `V2\V1`, hence Proposition 9.3
case I, not case IV. For a lower root `F`, upper parent `G=F+c`, and
`mu=mult(p_F^red,c)`, Proposition 8.1(i) plus Statement 3.17 gives
`P_G=i*mu`; the case-I equations give

```text
X_root=D_F/i=mu*(1-w_G),                    hence w_G<1.  (R1)
```

For an all-`mu=1`, `r`-way root meet, reduced degrees are `(r,r+l)` with
`l>=1`. Direct top-degree cancellation in Proposition 8.1(iv) gives

```text
w_G=l/(r+l) in (0,1).                                      (R2)
```

The exact row-1 `td=6,m=2` depth alphabet is `W={2}`, so every all-`M=1`
root meet in that sector is analytically excluded. The old l-free phase-3
enumeration remains invalid evidence. Root `M=1` itself remains legal: the
exact local `r=2,l=1` ODE/Q model has `w=1/3`; it is neither a Keller map nor
a row-1 reach model. Mixed-`mu`, off-axis, SF1/full-root completeness,
coefficient realizability, and the interior residue remain open.

Four exact root-aware AWS lanes have terminated rc0 with zero swap and
explicit OPEN ledgers: common/root (`1886` OPEN records), H3/HIII (`231`),
and two-pole (`763`), plus an invariance comparison being harvested. These
are diagnostics under an ambient-orbit completeness blocker, not a proof
dependency of (R1)--(R2) and not an exhaustive root census.

### 2.3 Multipole selected-exit/shared inequality is now promoted

Definition 3.3 already makes `T_a^*` the contact quotient:
`I_P(t)=I_Q(t)` iff `t<=O(P,Q)`. A split cannot remerge and every flag has a
representative-independent rootward segment. Let `U^full` be the
set-theoretic union of all pole segments. Along any ray its intersection
with `U^full` is a rootward interval.

For one set of actual selected positive exit-orbits per vertex of `U^full`,
excluding pole arrivals and the rootward continuation, Statement 7.3 gives
cv witnesses with unique attachment vertices. Distinct selected orbits at
one vertex and exits at different vertices have distinct witnesses. Repaired
Propositions 6.7--6.8 exclude the remaining down direction as a pole arrival;
shared suffix vertices and merge arrivals are counted once. Applying
`(C7.1*)` once with the x-side witness proves

```text
sum_(F in U^full) lambda_F^exit <= td(f,g)-1-psi.         (MFE)
```

Fable5 found and repaired one endpoint overstatement: at a pole endpoint,
contact with its pole ray may exceed the endpoint height, but the truncated
quantity `min(v_j,O(P,P_j))` still equals that height, so the intersection
interval and every theorem conclusion survive.

MFE is an inequality only. It does not restore `(22)`, `(22-cl)`, fixed
`kappa`, equality/slack, MP8's categorical no-refinement claim, a root
census, or `td=6` exclusion. The exact composition with residue A is being
checked independently; do not assume either a kill or zero global charge.

### 2.4 The concrete `td=6` frontier

The all-`M=1` root meet is dead, but the nonroot two-pole residue remains:

```text
P1=P2: Q=(2,2,2,2,5), lambda=0
merge: Q=(6,12,3,2,5), reduced (dp,dq)=(6,10), lambda=0
suffix: Q=(42,126,7,3,5), recorded selected-exit cost 2
terminal: psi=2, so 2 <= td-1-psi = 3.
```

The merge uses two simple incoming orbits and one extra q-only orbit. Its
rigid reduced ODE has coefficient ratio `a1/a2=2+-sqrt(3)`. This is a
necessary-condition exhibit, not a realizable Puiseux forest or polynomial
Keller map. Highest-value interfaces include: coefficient-vs-parent H1
matching; actual selected-exit computation; merged-pattern realizability;
and any new inequality that charges an extra q-only orbit or a merge.

The finite all-`b=1` book has no root cells, but mixed-`mu`/off-axis and SF1
root completeness are not in that book. Full-configuration landing,
`RPMC(C)`, independently sourced type control, and a cofinal `td`/complexity
ceiling remain the proof-wide walls. Keep `G2-PSC` and `G2-BD` distinct.

### 2.5 Reducer-safe GGV endpoint recursion

The quotient-ring reducer trap has been repaired by a pinned ambient
standard basis and normal-form replay after every operation. Exact
residual/total ranks are

```text
P,C8P02                 9/104
Q1P02,TRIPLE02          6/101
Q1P03,TRIPLE03          4/99.
```

Each rank has an explicit nonzero-NF witness and exhaustive next-minor
vanishing. These are rank certificates only: no endpoint, component,
radical, saturation, or full rank-locus conclusion follows.

At the cutoff, a zero-swap AWS lane is constructing adjugate right kernels
on explicit nonzero-minor charts, lifting through the 95 rational pivots,
and pulling back `x14*x72+x1*x97`. Preliminary exact results on `P`,
`C8P02`, `Q1P02`, and `Q1P03` give endpoint zero on `D(Delta)` only, with
every complement explicitly `NO_VERDICT_COMPLEMENT_RECURSION_REQUIRED`;
`TRIPLE02/03` are running. A dense-open death never kills a reducible branch.

### 2.6 External and compute state

The 05:24Z broad sweep found no external JC2 proof or counterexample.
Van Dobben de Bruyn arXiv:2608.27341 supplies a possible topology-at-infinity
filter but no plane construction; the naive dimension-two descent fails.
The two Matysiak SSRN manuscripts remain archived as unsound after primary-
text dependency audit. Next broad sweep is due by 2026-08-29 05:24Z.

Heavy computation is AWS-only. Total quota is 512 vCPU, each instance at
most 1 TiB RAM, zero swap required. The active endpoint chart uses 16 vCPU /
128 GiB; additional mathematically nonduplicate instances are available.
Local heavy CAS work is forbidden.

## 3. Whole-campaign task

Think independently about the **entire** JC2 campaign: all 46 numbered
avenues, every current proof/disproof gap, and new mathematics or software
that may bypass the present framing. Read all of `APPROACHES.md`, the current
corrections in `AUDIT.md`, the current day of `PROGRESS.md`, the newest live
state in `notes.md`, `ladder/REDUCTION.md`, the prior synthesis, and the new
reports. Victory may come from a connection nobody has previously proposed;
agreement with this packet is not a goal.

Address these questions inside the whole-portfolio scan:

1. **Compose MFE literally.** Compute the actual selected exit-orbits for
   residue A and its suffix. Does MFE kill it, sharpen its terminal menu, or
   give `NO HIT`? Can a sound refinement price the q-only resonant orbit
   without reviving equality/no-refinement errors?
2. **Push the root identity.** Can (R1) plus mixed-`mu` merge anatomy,
   depth alphabets, divisibility, or coefficient matching exclude broader
   mixed/off-axis roots? State the first exact missing implication. Do not
   extend `w=l/(r+l)` beyond all-`mu=1` without proof.
3. **Find a global composition.** Can contact-tree attachment, actual cluster
   weights, exact terminal orbit laws, conductor/proximity/intersection
   theory, or topology at infinity produce a new depth, pole-count, `td`,
   type, or complexity bound? Give a literal theorem interface or `NO HIT`.
4. **Attack the concrete survivor.** Compare merged-pattern
   realizability, per-edge Proposition 9.3 coefficient matching, h-family
   compatibility, Puiseux algebraization, and a direct collision/nonproper
   construction. Which cheapest exact test has the highest chance of killing
   residue A or turning it into a counterexample seed?
5. **Optimize the GGV recursion.** Given endpoint death only on `D(Delta)`,
   what is the fastest complete treatment of rank-chart complements: a minor
   atlas, Fitting recursion, module presentation, saturation/radical cover,
   or another exact design? Specify AWS fanout and proof-object gates.
6. **Re-rank proof versus disproof.** Which under-resourced route could beat
   the source-tree backbone or finite GGV lane? Seek genuinely new avenues,
   not renamed conductor/Euler identities or larger equivalent Groebner
   jobs.
7. **Accelerate software.** Propose an exact compiler/checker/backend that
   shortens the campaign's critical path: typed Sigray tree generation,
   selected-exit ownership, coefficient-reach matching, distributed rank
   atlases, formal proof-object extraction, or a redesigned HENS-CT engine.

## 4. Required submission contract

Your report must contain all of the following:

1. A compact disposition vector for every numbered avenue 1 through 46:
   `unchanged`, `raise`, `lower`, or `reopen`, with reasons for every change.
   Group unchanged entries compactly, but omit none and never merge `G2-PSC`
   with `G2-BD`.
2. A reranking of the three principal proof bottlenecks and two principal
   disproof/counterexample bottlenecks.
3. At least one genuinely new avenue or mechanism, compared explicitly with
   repository history, and at least one new connection between existing
   avenues.
4. The strongest proof attack and strongest counterexample/falsification
   attack to run next.
5. One software acceleration or decisive experiment, with exact target and
   verification gate.
6. No more than three detailed idea cards. Each names exact dependencies and
   licensed assumptions, cheapest decisive discriminator, materially
   different outcomes, stop/rollback condition, estimated compute/review
   cost, and expected information gain.
7. `continue`, `redesign`, or `stop` for every major current lane, including
   pure Sigray/exact-pair, hybrid G2-PSC, arbitrary-Q/rank-drop GGV,
   lambda-nonzero/raw-window GGV, LF40, K00/order-two/TD6, D43,
   Artin--Schreier, HENS-CT and external intelligence.
8. One likely-missed insight and its cheapest test.
9. A full epistemic ledger: promoted facts, provisional inputs, conjectures,
   failed approaches, hidden assumptions, checks and failed attempts,
   contamination, and exact scope. A model verdict is not mathematical
   evidence.

Do not output a generic brainstorm. Rank by expected information gain per
wall-clock hour and probability of resolving JC2, not ease. New work may run
provisionally and reversibly during review; promotion and expensive fanout
remain review-gated.

