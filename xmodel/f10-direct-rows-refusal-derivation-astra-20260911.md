# Direct-row refusal derivation — conditional metadata preparation

STATIC METADATA PREPARATION; no interpreter execution or observed worker evidence. First action 2026-09-11 04:04:49 UTC, targets absent, all seven allowed source pins matched before fresh reads or explicit same-byte reuse. Original reserve04:19/HARD04:22 UTC remains fixed.

All seven intended branches and their complete conditional exit/stdout/stderr triples are now frozen in DISABLED-POLICY.template.json. Five controls give uncaught ValueError tracebacks through produce.py:382/main:356; two wrong-job controls give exactly `REFUSED: registered CLI contract\n` with SystemExit. All exit1/empty stdout, assuming ordinary healthy CPython handling. Caps and metadata-inventory controls reach the same authority.py:95 branch and have identical bytes; their independence is the mutation, not different observations.

DERIVATION.md records the exact frames, source lines, absolute administrative paths and earlier-check assumptions. The status/source/hash leaves are authority.py82/100/(78->74). Module `main()` and whole raise statements have no caret indicator; producer356 has nine spaces plus20carets, nested load_pinned call78 has11spaces plus37carets. No tildes, optional frames, wildcards, substring matching or observed-run calibration. These conclusions use fresh final source reads and the relevant upstream CPython3.12.0 compiler/display code, not a live executable.

Official source evidence is pinned by full upstream byte hash and exact raw-source excerpt intervals in PINS.json. `Python/compile.c` supplies CALL versus RAISE locations, `Python/traceback.c` the whole-line suppression and space/caret printer, and `Python/pythonrun.c` the SystemExit/display route. Selected excerpts only were read, not the entire upstream files. URLs are exact raw v3.12.0 URLs; web-rendered line indices are not used as raw line numbers.

GAP retained: zero selected/physically authenticated interpreter builds, zero observed refusals, zero authorized executions. An arbitrary3.12 vendor build is not proved byte-identical to this formatter/compiler. ROOT must authenticate the chosen executable/native/startup against these rules, readable pinned ASCII source at `/opt/jc2-r3-directrows-20260911a/science`, ordinary debug ranges, default exception handling and healthy streams. Unexpected import/DMI/native/ACL/resource/formatting failures remain STOP, not alternative acceptable refusals. The source-derived bytes are complete conditional predictions, not an assertion that this missing deployment hypothesis is already true.

The disabled policy retains exact schema/source_files/entries with arithmetic.py UNFORMED as a deliberate non64-hex activation barrier. ROOT supplied its final administrative hash acd07f800707c379aba60bd06f239d38cd83c86867729561367abd2bcc04ce73 and unchanged check_arithmetic hash9d2aac637dbdeaa192cc3fe531e83694f9853b6573556ad5d49362f754b1163e; neither body was accessed. All four known remaining source fields are literal. ROOT alone may issue a new finalized copy and registration after checking the build/install assumptions. There is no arithmetic/place/source admissibility or runtime result.

Own source/runtime files were not changed. The optional saved-slot inverse-control correction was not implemented; accepted scope remains byte-faithfulness/outside-slot invariance with statically checked edit correctness, not an executed arithmetic inverse test. The powerfix affects no charged authority/entry line number.

OPEN quantity: does the future selected build match the fixed conditional traceback bytes under the stipulated installation? Cheapest next step is ROOT's documentary build/native/compiler/formatter check against these pinned excerpts before registration, not a source smoke run or calibration by failure. No cost forecast or automatic execution authority follows. Collision check is own-only and EMPTY; all authored documents receive WHOLE review and current input/owned pin checks before marker-last transaction sealing. Publication tools only; no scientific subprocess of any size, no local Python except the explicit artifact_finalize metadata workflow, no code execution or fixture.

Closeout: own report/derivation/policy/PINS/read-scope and all three exact primary excerpts reviewed WHOLE; seven local postpins unchanged04:11:46UTC, all owned nontransaction hashes recorded and all three raw-source literal sed vectors matched saved bytes04:12:12UTC. Targets still absent before publication. The raised OPEN retains its quantity and cheapest check; no unseen source/body or execution fills it. Final marker follows; original04:19 reserve/04:22 hard unchanged.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `4707`.
- Body SHA-256:
  `aa99202224325f5922c5f95161347c5fba5570075e1a172ff550fd79cd126ec7`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
