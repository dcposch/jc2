# Maximum-12 `(9,12)` corrected-Q8: generic-`c` frozen-`b=1` selected-localizer unit

Date: 2026-08-25  
Status: **PRODUCER-EXACT; HOSTILE SOURCE/SEMANTICS REVIEW PENDING**  
Case: `cases/max12_912_order3_nu_q8_w0_rankdrop_b1_loaded_saturation_aws_20260825/`

## 1. Exact scope

Take the pinned approximate-cubic six-row source

```text
I = (e1,e3,e5,e7,e2,e4)
```

over characteristic zero.  On the rank-drop point with
`d4=1,d2=2`, retain the displacement `u=d2-2` but **freeze `d4=1`**.
Work over the coefficient field `Q(c)` in

```text
R = Q(c)[inv,w,u,x1,x3,x5]
A = x3-2*x5
J = I|_(d2=2+u,d4=1) + (inv*w*x5*A-1).
```

Thus `V(J)` is precisely the selected open `D(w*x5*A)` inside this frozen
`d4=1` slice.  The field `Q(c)` is generic in finite `c`: denominators in `c`
are inverted and exceptional finite values are not classified here.

## 2. Exact computation

The localizer variable `inv` is the first block in
`(dp(1),dp(5))`.  Each lane computes a standard basis `GJ`, contracts `inv`,
and recomputes the contraction basis `GC`; it then adds the landing centre
`w=u=x1=x3=x5=0` and computes `GL`.

Two Singular 4.3.2 algorithms on AWS r6d returned independently:

| engine | rc | wall | max RSS | contraction | landing |
|---|---:|---:|---:|---|---|
| `std` | 0 | 4:34.16 | 104,720 KiB | `GC[1]=1` | `GL[1]=1` |
| `slimgb` | 0 | 4:09.67 | 250,944 KiB | `GC[1]=1` | `GL[1]=1` |

The contraction unit is stronger than the centre test: at ordinary
characteristic-zero CAS trust, it says `J=(1)` already, hence the selected
open in this frozen slice is empty over `Q(c)`.  `GL=(1)` is then a redundant
landing control.

The two frozen inputs differ only by the named standard-basis engine and were
regenerated byte-for-byte from the same source compiler.  They are
engine-algorithm agreement, not independently authored mathematics.

## 3. Custody and replay

Core source pins:

- quotient compiler: `22b0cdc446d1e486d08757df5a3fff83b0506a492f16b9b2cf238966eb947545`;
- order-three source: `a4fdac5d613e1af82ab135e220fedee5e41384c41e3068bcdf8921f60ababbcf`;
- elimination generator: `25f3f2b27200c48e60432392c14c8631540274e63c4e0d3f64aa10ea0dbb3a5a`;
- runner: `828c6617a50bc52dcc3baeee6b2bb3340d6d8cf167d9876c4f074291e25d68bb`.

Accepted endpoint hashes:

| engine | input SHA-256 | stdout SHA-256 | stderr SHA-256 |
|---|---|---|---|
| `std` | `5e43ebd1a4a9c57c539da4ff0ffaa7a2dab5efd4106e1179afcd67ca4c98a403` | `24f566bfdea08c47dec320b14ebecf0faa1f32abd191d05cd88227a6992db1fd` | `f3315c9f40e30db59650d1d5379b4cccc912384608c79f63aac0a2ddf27e43cb` |
| `slimgb` | `600b1ae7240838f625c1779ba130627966a0615dbcc217abb7c2a7fe33e0d01d` | `ed77831623587d8f90bdfc77239f561571731ee03a727e2a28bb57d1e265f895` | `4f6c92d7ccfa605a5c1c1022418690b10bc763ac71fc71e8f00272c51ad84d0f` |

Both generator stderr files are empty, both runner files contain `0`, and the
stderr hashes are `/usr/bin/time -v` records.  The fail-closed replay pins all
bytes, regenerates each input from the compiler, checks the exact imposed-row
tuple/localizer/elimination/landing source, rejects diagnostics, and requires
both unit bases.  Its AWS r6d endpoint is rc zero with stdout
`Q8_W0_RANKDROP_B1_LOCALIZER_ELIMINATION_REPLAY_PASS`:

- replay source SHA-256:
  `99580d459c5aaf696c3ab39b6c7d96c1dfa77d05be44429c1be069bed3e42e84`;
- replay stdout SHA-256:
  `ee25984d0ca1b728a448e1e6b6b56a8f557f74bbb2119c87c7f54b28dbad285a`;
- frozen manifest SHA-256:
  `2a5c5da5b33f4938b018beba7f1737e1cd2f3e81c48c153c0546e0073e242e1f`.

No explicit cofactor expression for `1` in the six source rows plus localizer
is printed by these direct lanes.  A separate exact-Q Singular `modStd` lane
with original-generator reductions and a conditional `lift` certificate is
live on r6d.  Accordingly this report remains producer-exact pending hostile
review of source fidelity, coefficient-field semantics, and the standard-basis
trust step.

## 4. Exact consequence and firewall

At this evidence tier, the honest conclusion is:

> In the exact frozen slice `d4=1,d2=2+u`, the selected localized source is
> empty over `Q(c)`; hence no selected arc or point whose entire coefficient
> curve remains in that slice exists for generic finite `c`.

This does **not** yet exclude:

- exceptional finite values of `c` erased by passage to `Q(c)`;
- an arc landing at `(d2,d4)=(2,1)` but with nonconstant drift in `d4`
  (and corresponding drift in `d2-d4-1`);
- the rest of the doubled rank-drop line `d2-d4=1`;
- other components of the full selected horizontal closure
  `I:(w*x5*(x3-2*x5))^infinity`;
- coefficient infinity, Taylor/terminal realization, a trajectory, the whole
  `(9,12)` cell, maximum twelve, or JC2.

The exact polynomial-parameter `Q[c]` lanes remain charged for the exceptional
finite-`c` polynomial.  The full `Q(c,b)` rank-drop localizer and global exact-Q
selected saturation remain charged for base drift and other rank-drop points.

