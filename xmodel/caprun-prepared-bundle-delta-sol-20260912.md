# Prepared-bundle constants delta — Sol

- First action: `2026-09-12T06:21:31.743350546Z`.
- Basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
- Lifecycle: `INTERNAL-UNREVIEWED`; no launch authority.

## Status

`BLOCKING_CARD_DEFECTS_CONFIRMED; ROOT_REPLACEMENT_CARD_STATICALLY_SUITABLE`. The registration/authorities data model can otherwise be rebound by constants only and then consumed unchanged by caller/binder/stage. This is source-static, unexecuted, unreviewed, and not launch authority.

## Bottom line

The charged old card cannot pass the unchanged binder: `replace_exact` requires the exact sentence “Final registration and holder topology remain unformed in this preholder card.” once, while literal search of the complete 89-line card finds zero occurrences. Independently, that card describes refusal policy SHA `e3c5691f...c7845`, while the prepared registration pins `preflight-policy.json` as `1a632269...f815`. The latter does not mechanically trip binder, but is a real semantic contradiction in the prepared bundle.

No caller, binder, stage, registration, authority, cap, phase, serializer or trust-model mutation is needed. The smallest repair is a constants-only card derivative: add the required sentence once and make the policy digest an EARLY constant required to equal the registration pin. ROOT's frozen replacement card SHA `002d145f...a2ff` does exactly this, also replacing the obsolete old-clock path/SHA with EARLY placeholders and changing “selected” to “may be selected after new exact binding and review.” Its three-hunk diff retains all required literal clauses, nine phases, guards, limits and late token counts. I find it statically suitable as the prepared-card input after ROOT binds every EARLY token. ROOT separately reports that the uncharged policy files hash to `1a632269...f815`; I record that as supplied metadata only, not an independent file/content check.

## Prepared registration and authorities

The registration has the binder's exact 46-key top-level census. It preserves required prepared states: `enabled:false`, `exclusive_no_concurrent_writer:false`, `aggregate_cpu_start_usec:"0"`, `phase_policy:null`, `typed_slot_policy:{}`, `closed_child_guard.root_no_migration:false`, and null outer/all-nine leaf identities. Its source/aggregate caps, five profiles, clean environment, uid/gid, PREFLIGHT_ONLY_9 mode and ordered phase list match `validate_prepared` literals.

A fresh batch requires constants-only replacement of every old `20260911d` derived base/unit/cgroup/output/durable/installed-source path and all four obsolete deadlines. Hostname/instance/boot/PID namespace, Python/setpriv/native digests and durable-device/evidence values are EARLY physical bindings. Scientific source digests, contract/place/review/closed-scope evidence, fixed runtime hashes, caps and schemas are source/static bindings. No invented value appears here.

`AUTHORITIES.preallocation.json` has exactly the nine labels and matches `expected_authority`: seven 11-key general objects and two 10-key valid/dummy policies. The precise negative deltas are preserved: refuse-status disabled; refuse-caps authority CPU 599; refuse-inventory lacks check.py; refuse-source has zero produce hash; refuse-hash's command supplies zero; startup roles use WRONG-JOB; startup-check is mode check with zero input SHA; valid/dummy use their control/dummy profiles and null closed-scope identities. Its seven Python/source-native placeholder pairs are EARLY and must equal registration pins. This file is an input recipe, never nine preinstalled authority outputs.

## Nine argv and late boundary

All nine commands share the exact CAPRUN/setpriv/Python prefix, control profile except dummy RSS 32 MiB, and label-specific stdout/stderr/telemetry/cgroup/authority/writer paths. Their exact tails and authority recipes are enumerated in `BINDING-TABLE.md`. After EARLY batch, host, native and source constants are frozen, binder accepts only these late observation values: holder PID/start/InvocationID; outer device/inode; nine ordered leaf device/inode identities; coordinator receipt SHA; native-manifest equality and native-list SHA. Instance/hostname/boot/PID namespace are frozen early and merely re-compared late.

Binder alone enables the two flags, sets root-no-migration, fills physical identities, updates valid/dummy closed scopes, canonicalizes all nine authority objects and inserts eight authority/policy hashes into argv. Refuse-hash remains literal zero. It reverses every allowlisted structured change and compares the rest of registration/authorities unchanged.

## Text and hash chain

The replacement card has each binder late token once: captured PID, start ticks, InvocationID, coordinator receipt SHA, final-registration SHA. It also retains exactly one disabled heading and one unformed-template sentence, adds exactly one required unformed-topology sentence, and preserves the `Actual worker … hostname …,\n` / `\nboot …,` shapes after EARLY substitution.

The installer requires EARLY constants INSTANCE×2, BOOT×2, HOLDER_CUTOFF×3 and every old base/stage/unit/cgroup/holder-argv literal. Its retained late map matches binder exactly: PID×1, start×1, InvocationID×1, install-release×1, final-card×1, final-manifest×1, final-registration×2. `replace_exact` refuses any leftover placeholder.

Freeze order is acyclic: bind four prepared files early → hash them into exact four-key prepared-pins → late canonical authorities → late registration and hash → card and hash → three-line install manifest using registration/card/native-list hashes → installer → summary. Stage commits exactly 14 outputs. Nine authority JSON remain local expected runtime outputs; only registration, card, manifest, installer and native list are later staging inputs.

## Classification and limits

`BINDING-TABLE.md` is the normative concise field, argv, authority, multiplicity and freeze-order table. Expected absence of current physical metadata is not a defect. The two old-card issues are concrete source/data defects; the supplied replacement resolves them at source-static level. Actual ROOT values, policy/evidence qualification, original-clock consistency, executable installation, timing and runtime success remain future work.

All thirteen inputs were pinned before fresh whole inert reads, except `COORDINATION.md`, whose unchanged exact hash and prior complete whole read from the immediately preceding Sol task were expressly reused. No referenced scientific file was followed. No Python/JS/helper/syntax/import/AST/compile/test/mock/dummy/fixture/runtime/network/SSH/AWS/Git/protected-tree action occurred.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6717`.
- Body SHA-256:
  `9e9b02c116f5b07cd8275112b848bc557e5f480996e6dc6e30e51a4178829e44`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
