# Minimal exec-preserving contact bridge and two exact mutation payloads

2026-09-10. Astra producer, UNREVIEWED SOURCE. First action04:47:52 UTC;
fixed stop05:03:52 UTC, reserve05:01:52 UTC; never reset.
ZERO authored-code execution or mathematical subprocess ANY size.

## 1. Delivered scope

Delivered source-only payload_exec.py, mutate_certificate.py and the short
ROOT-execution-interface.md in the owned box. The original producer,
checker, execution gate, probe and CAPRUN are UNCHANGED. This is an exec
bridge and two exact transformations, not another solver, authority,
supervisor, scheduler, hardening farm or root-classification attempt.
No certificate, mutation coefficient artifact, fixture or runtime receipt
was generated. The two future mutations remain entirely UNEXECUTED.

Exactly ten initial inputs: ROOT-CARD plus its nine listed sources. Card,
new17zt gate, existing execution_gate and probe read WHOLE after current
pins; five earlier same-byte WHOLE reads reused after matching pins as
permitted. CAPRUN was HASH ONLY, no 1198-line review or source change.
No other report, live integer review, solver, corpus or provenance read.
ROOT later permitted ONE pinned official general.doc URL for startup flags;
its selected-section scope and full raw hash are in PINS. No other network.

Accepted17zr and qualified17zt are premises. The checker's reconstruction
independence is arithmetic-level, not an independent uncharged quartic
provenance claim. The direct check(raw) digit-policy gap is avoided by
fresh CLI checking; the mutator sets the digit policy BEFORE parser use.
The omitted-I4/free-z issue is documentary only, not another test farm.
Singular typed-link syntax, numeric printing, installed version and native
closure remain ROOT's unresolved runtime gates, not solved by this bridge.

## 2. Admission before scientific imports; exact dispatch

payload_exec.py accepts EXACTLY AUTHORITY INPUT MODE, with MODE in
produce/check/mutate, on Linux under Python -I -B. Path arguments must be
canonical absolute regular files. The execution_gate.py sibling is hashed
against the accepted cbfe55ff pin before its metadata-only import. Then
the UNCHANGED authorize validates physical host, flags, full CAPRUN/child
argv and caps, original all-file hashes, script/gate/runner pins, and for
check the input at sys.argv[2]. No scientific module is imported there.

The bridge rereads the authority only after authorization, with a 65536-byte
bound and SHA equality to authorize's returned authority_sha256. It then
requires EACH selected source, executable and input to be present in that
same accepted file_sha256 map and unchanged. Selected accepted gate,
runner, producer and checker have fixed known hashes. Native invocations
are pinned by their exact /usr/bin names AND resolved targets if different;
the selected own mutator is separately registered by its current hash.
This cannot be satisfied by declaring only an unrelated file collection.

The fixed dispatches are exactly those in ROOT-execution-interface.md.
Produce INPUT must be the fixed sibling produce.sing. Check invokes the
unchanged checker as a FRESH /usr/bin/python3 -I -B CLI, preserving its
process-local decimal setting. Mutate invokes only the fixed own sibling
mutator and admits the unchanged checker source it will import. Sources
must be regular nonsymlink siblings. Original sources are not edited.

ROOT's specific startup clarification is incorporated: produce executes
/usr/bin/Singular -q --no-rc --no-stdlib --no-tty --random=0 PRODUCE_SING.
The [permitted official source](https://raw.githubusercontent.com/Singular/Singular/186962946a86748b0b15bd9e34f02a85e51692c2/doc/general.doc)
confirms the startup-file/library exclusions, terminal and seed settings.
--no-shell is intentionally absent because that option disables links.
No optional LIB is needed by the unchanged built-in producer. This change
only fixes dispatch startup inputs; it is not an algorithm modification.

The bridge refuses preexisting producer/mutation outputs (including
dangling symlinks), records actual mode/host/instance/job/authority SHA,
selected file hashes, PID/PGID and replacement argv, flushes, and calls
os.execv. There is NO shell, fork, subprocess, setpgid or replacement
supervisor. Exec preserves CAPRUN's recorded child PID/PGID and inherited
limits by process replacement. Live descendant-RSS/cleanup evidence is
still mandatory and has not been fabricated from this source argument.

## 3. Two stored exact mutations, not another computation

The mutation payload is not independently runnable authority. Its ONLY
admitted use is after the bridge's authorize/exec path; ROOT must first
have a positive independent CLI receipt for the exact input bytes.
The wrapper transports admitted input/authority SHA fields in task-local
environment variables. The mutator requires their form and compares its
bounded input read against the admitted input SHA. Those fields bind data;
they are NOT unforgeable authorization tokens or a substitute for the gate.

Before scientific import, the mutator hashes the fixed checker sibling
against2e0dda86. It sets the process-local decimal digit policy, then imports
that pinned parser/Poly. The unchanged parser validates the exact header,
source pins, variable order and every input coefficient. The mutator does
NOT call check(raw) and does not claim positive validation of its source.
It only requires shape/nonzero/univariate F; prior valid identity is an
external ROOT precondition, not invented evidence.

For each changed name F and H4, a shallow dictionary copy receives a NEW
polynomial source[name]+1; the source Polys are not mutated. Serialization
uses exact rational numerator/denominator strings and nonnegative exponent
tuples with the same header and fixed F,H1,H2,H3,H4 block order. All other
coefficients are preserved; whitespace and term order are canonicalized,
not claimed byte-identical. No float, eval, solver or emitted ideal is used.

Each retained output byte string is reparsed with the SAME pinned parser.
For every block, subtracting the original must give exactly1 at the one
selected name and0 at every other name. This verifies the intended delta
and source sameness from the EMITTED bytes, including exact headers. Both
outputs are bounded by64MiB, as is the input. They are then written with
exclusive creation to sibling contact-F-plus-one.cert and
contact-H4-plus-one.cert, reread with a finite bound, compared byte-for-byte
with the retained strings and reparsed. The final marker records actual
input, authority and both output hashes; it is not a CHECK_OK marker.
A late failure may leave one partial output; no atomic pair is claimed.

Conditional on a valid source F=sum HiIi, F+1 has residual1 (or fails
the unchanged zero-F check if the result is zero). H4+1 has residual
-I4=1-zcA, nonzero since at z=0 it is1. Thus separate CLI checker launches
must reject BOTH, with status2 and CHECK_FAIL. A positive independent CLI
launch must return0 with CHECK_OK binding its actual file SHA. These are
static expected outcomes only; no positive or negative execution occurred.

## 4. Runtime/registration boundary and remaining GAP

ROOT owns all enabled authorities, actual instance/native-closure pins,
fresh immutable directories/input custody, finite file/resource caps,
CAPRUN argv, live namespace/PGID/RSS/cleanup dummy, transport and collection.
The exact existing schema and parent argv are documented without creating
an enabled authority. Future output hashes are acquired AFTER collection,
then pinned as actual inputs in three fresh check authorities (original
and two corruptions); they are never predicted or invented here.

The existing five-refusal probe suite, valid POSTAUTHORIZE and descendant
dummy remain mandatory unchanged ROOT regressions. Their prior fixture
packet was not charged or inspected, so no fictional individual results
or replacement matrix is supplied. Sources/authorities must stay immutable
between admitted reads and exec/import; this lane makes no new race-proof
authority claim. The bridge proves no installed engine behavior.

Remaining GAP: independent source FIRST gate plus ROOT's registered
runtime/dummy/native/serializer/control evidence, then a genuine explicit
eliminant if the producer succeeds. Prescribed-root and source/affine/JC2
closure remain outside this bridge. No execution, review launch, promotion
or dependent task is authorized by publication.

## OPEN(S) RAISED

- ASSIGNED GAP ONLY; no new canonical ID: gated execution/collection and
  the actual certificate are still absent. No third production corruption
  or new solver/runtime family is requested.

## COLLISIONS

status: EMPTY FOR THE OWNED MINIMAL BRIDGE

- Own exact report/box were ABSENT at04:47:52 UTC. Own report extraction;
  no corpus, protected/shared, live-body, process, Git or other-lane read.
- Existing gate/probe/CAPRUN and accepted producer/checker unchanged;
  no startup/version/native/CAS/Python-science/AST/syntax/test execution.

Own WHOLE report, both source payloads, ROOT execution interface and PINS
reread as TEXT at04:58:08 UTC; own-report OPEN/collision extraction done.
This completion records those checks only. No authored code, coefficient
payload, import, syntax/AST/compiler, dummy or mathematical subprocess was
executed. ROOT retains custody-FIRST intake and every future gate/launch.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `9438`.
- Body SHA-256:
  `dc751cecbaac586a357847f97b95533ed3b47af9a00c0acdae46b5e5a640b48c`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
