# 0040Z critical round — Fable 5.1 blind (equal full46 contract)

Seat: Fable 5.1, independent blind ideator. Written 2026-09-09, invitation received about 00:52 UTC, common absolute deadline 01:22:00 UTC (set by root at invitation, not reset). Frozen basis `0d39df3c9fd69c939a8420c54d03228b9077777d`; packet freeze 2026-09-09T00:37:47.960176Z. JC2 is unresolved. This report selects and ranks; it promotes nothing, launches nothing, and reads no current-round root/peer report, live gate, code, log or receipt. No `charge_basis` line: no new exit-price claim is made.

## 1. Read scope and custody

All seventeen charged inputs in the lane input directory were read whole, in this order: contract, packet, audit guide, manifest; LIVE, binding BD-GAL row26 integration, AUDIT 15m–15p, all46 master table, previous 2220 synthesis, APPROACHES (whole); COORDINATION (whole, three sed ranges), latest broad sweep, minimal receiver, accepted golden framework, accepted M gate, unreviewed weight-free framework, unreviewed conditional client map. Hashes below were generated with `sha256sum` at write time and agree with MANIFEST.json for every entry.

| bytes | sha256 | file |
|---:|---|---|
| 9004 | `ec1ee90ee5fab386d3ad67bd3f3f57eac0bf044c5e2b7ed1110b8487dcbf3bfb` | MANIFEST.json |
| 18157 | `14c4cdeddf9b1166046759022378775fc7ebc147c7d939579cf93f6b78aa51c6` | accepted-golden-framework.md |
| 15952 | `50045412e9b0d9cf3e640844e768e359bd7dd466e862316e9b4e9380d40c68cf` | accepted-m-gate.md |
| 25451 | `2f5035a92f79c373a6d795ac06d127d77ba626d4fe482829e030f6ad8d61fc4f` | approaches.md |
| 20809 | `2813a5e2294fbfe06d9fb00d32e770a0cff226822e859da5a2609431fd8dc232` | audit-15m-through-15p.md |
| 2624 | `26b804a522e4a07829a05a9ed5f0aacd673e952e175424459d5cb4dff0cc385d` | audit-guide.md |
| 6969 | `f97207189cc80f1a3c1c80dca9cb172dbeeb4fbd99b61266b4ed5c1d3f9b0ff8` | binding-row26-bd-gal.md |
| 4406 | `4c290efbe35c42f2454cb7ae97ceae54ed9ae6d5e5676039d85529f9adbde6e7` | contract.md |
| 48495 | `874b613c1b9bccaf697c78b4ee4aa657442136ed298f5887363c448f056410d8` | coordination.md |
| 13786 | `364431f6267629e2f45e49785b18bf6393f4a9b4f2b59b4d3ee47047a70440dc` | latest-broad-sweep.md |
| 5443 | `1f5302b803d64b92483040328c834b956d2b754e91e50216dede818cd5e15628` | latest-live.md |
| 21506 | `c235fca15b48d29860291e26b7c8056915110deb1a9dc15b7ff1a20926d8d4ac` | master46-history.md |
| 14478 | `7dec79af1aa62946b46ecb209a710dd95e1b226b6b78229a3f232160a5385413` | minimal-receiver.md |
| 4233 | `48f2d56e6efd50cdc263a50fa8d7000731809e9b86abd4f90bd73054f49f7c58` | packet.md |
| 14949 | `179a16393828ea055520e0fc67a79d777be0d563cbe6b87d18c5688daeb57249` | previous-2220-synthesis.md |
| 13204 | `dcb3a30a48c5977005083a3ad7bbc5935e1f2f74ade6c3569a4b45751c213276` | unreviewed-conditional-client-map.md |
| 15640 | `cbc0330b001faeb8692e45c3be3fdcd3c0727b5734e648143642234da5d63696` | unreviewed-weightfree-framework.md |

Extra scoped read-only history, recorded exactly: `grep -c "(9,15)"` over AUDIT.md/APPROACHES.md/PROGRESS.md (one AUDIT hit, its single line inspected, see section 6), `grep -c "resonance census"` over the same three files (zero hits), and a directory listing of `xmodel/ideation-20260909T0040Z-*` file names only. No body of any current-round report was opened. Root's leased local proof and its live gate are outside this evidence. No CAS, solver, AWS, SSH, web, agent, or canonical edit was used; no tiny control ran (the arithmetic below is hand desk arithmetic on degrees and orders only).

## 2. History check against the frozen scope

- **Row26 (binding).** The proper Galois second-leg quotient, including d2=[K:C(f,g)]=2, is CLOSED by BD-GAL (August 30). I do not resurrect it and do not substitute d1=[C(x,y):K]=2. Non-Galois d2>=3, d1=2 and primitive maps remain open, and I supply no new discriminator for them; naming them is not a task.
- **Accepted perimeter.** 15i retires the whole weighted unequal/Q client; 15m+15n+15p retire the whole weighted unequal/golden field-point client (both embeddings, no parity). 15o types the common4/Q coefficient h only. The four common3/common4 clients are open. I treat these exactly as frozen; older APPROACHES paragraphs and the 2220 synthesis do not override 15p.
- **Weight-free candidate (WF) and the conditional interface** are TERMINAL BUT UNREVIEWED. I use them only as objects to attack and calibrate, never as premises. Their conditional consequence (all six clients plus the actual standard-F2 (75,125) family) is not banked here.
- **All-degree coverage** is separate from every finite closure; n<=200 gives 64 residual necessary configurations, not pairs; K16 still owes its all-m product obstruction and b=0 boundary. Nothing below changes that.
- **Previous synthesis** kept 1–46 unchanged and selected simple-L gate, coalesced-M derivation and common4 typing; all three are now done (15n, 15p, 15o). No launch from that synthesis is still pending in the frozen inputs except the deferred common4 h solve.

## 3. Disposition vector, avenues 1–46

Global rank: **all 46 unchanged.** Explicit scoped-task notes, which are priorities inside a task and not global reranks:

- **1 (GGV corner families / degree farm): unchanged.** The whole D125 receiver programme is its descendant and is the current critical path; that is task priority, already reflected, not new global evidence.
- **26 (primitive monodromy / function-field Galois): unchanged, NOT reopen.** Overlay: d2=2 closed; the historical "rule out d2=2" successor in the master table is stale and must not be read as open.
- **29 (LND / Hamiltonian derivations): unchanged.** WF section 2 is a Hamiltonian-derivation centralizer argument; it is a consumed tool, not a new route.
- **32 (collision ideal / injectivity): unchanged.**
- **36 (guided structured-support search): unchanged globally; scoped raise as a method control.** Reason: WF is strictly stronger than what the campaign needs (it drops the 105 lift rows and all polygons), so a WF-falsifier is a cheaper target than a Keller counterexample, and the cheapest instance is a degree-(9,15) toy (section 6). This is a calibration of a proof mechanism, not a counterexample hunt.
- **11, 44 (Mathieu/image ladder; Moskowicz): unchanged (refuted / closed as proof input).** The broad sweep's Long result (two pairs, four variables) reinforces 11's closure and supplies no plane bridge.
- **19, 21 (char-p / Witt; p-adic): unchanged**; the degree-twelve frontier remains the only live bounded partial frontier and nothing in this packet moves it.
- All other rows: unchanged for lack of any new evidence in the seventeen inputs.

## 4. Bottleneck reranking

Proof side, in order:
1. **WF local consumers**: simple-root exclusion (j<=9), d2 and d3 double-root theorems in both regimes, d1 (j<=11 in Q, j<=12 golden), and the golden j12 two-chart resonance comparison. Section 6 shows the last item is the unique resonant step for the actual clients; everything else is strict-margin.
2. **Arbitrary (75,125) to standard-F2 coverage** (Strinz Theorem A / census input) and the beyond-125 shapes of the GGV dichotomy. If WF passes, this immediately becomes the top proof bottleneck, because the six clients and the standard-F2 family are then gone and nothing else at D125 is covered.
3. **K16 all-m b*B*eta obstruction plus the b=0 boundary.**
4. **Uniform exclusion of the 44 u_s=1 receivers** (n<=200 is a test population only).

Disproof side, in order:
1. **A complete unretired source ideal with a measured discriminator.** Today that is common3/common4 (both fields). Under WF these are conditionally unit; so the honest disproof bottleneck is *whether WF is false*, and the cheapest place to see a WF-type mechanism fail is the (9,15) toy, not a 371-variable stream.
2. **K16 ray**: a polynomial solution with b*B*eta nonzero at some m>=4.
3. **Witt/p-adic lift at correction y-degree >= 12** (avenues 19/21).
4. Dimension-three descent and Pinchuk-type deformations stay low.

## 5. New mechanism: degree-forced resonance census for the weight-free class

**Label: NEW (desk arithmetic, unreviewed, mine).** The unreviewed framework fixes e=5. Its sections 2–4 use only: a simple factor of H (geometric integrality of the generic fibre), multiplicities <=2, ordinary degrees, and the identity `[A_s,B_s]=[R_s,T]+[F,G]`. Rerun them for the class

```text
C_e: H homogeneous of degree e in (g,p), g not dividing H, k double factors,
     e-2k>=1 simple factors, D=rad H of degree e-k;
     A,B in K[g,p], deg (3e,5e), tops H^3/H^5, [A,B]=c*g^2, c!=0, char K=0.
```

Rederived (my desk, e general): target order is `8e-4` (36 at e=5); F!=0 and `1<=j<=3e-1`; `H^2` does not divide F_j; at order 2j the centralizer element has degree `7e-2j`, nonzero only when `e | 2j`, and every leader is an H-power divisible by H^2; hence `H | F_j^2`, `D | F_j`, and `j<=2e+k`. H-divisible branch: `j<=2e-1` with `F_j=HC`, `deg C=2e-j`; on the D|C stratum `j<=e+k`. With the framework's balances (`r=min_n ord(c_n)/(6-n)`, initial bracket at `15r`, target at `8e-4`; over-margin `15r>8e-4` is an immediate order contradiction, under-margin gives commuting Euler-homogeneous initials), the initial bracket lands exactly on the target only when one of the following integer identities holds:

| stratum | balance | condition | integer solutions with e-2k>=1 |
|---|---|---|---|
| non-H (d1) | linear coefficient, j=2e+k | 3(2e+k)=8e-4, e=(3k+4)/2 | (e,k)=(5,2) only: golden, j=12 |
| any | constant coefficient q0 | 15q0/6=8e-4, q0=(16e-8)/5 | e≡3 mod 5: e=3 (q0=8, needs j=7=2e+k), e=8 fails (j>=20 > 19) |
| H-div, D∤C (d2) | r<=j/4, j<=2e-1 | 15j/4=8e-4 | none (e=1/2) |
| H-div, D|C (d3) | r<=j/3, j<=e+k | 5(e+k)=8e-4, e=(5k+4)/3 | (e,k)=(3,1) only: H=p^2(p+g), j=4 |

At e=5 the Q pattern (k=1) has no resonance at all: d1 j<=11 gives 33<36, d2 gives <=33.75, d3 gives <=30, and q0=72/5 is not an integer. So, **if the non-resonant local lemmas are e-uniform, WF for H_Q follows from them alone, and the golden j12 two-chart comparison is the single resonant step in the entire actual client set.** The smallest resonant configuration in the whole class is e=3, `H=p^2(p+g)`, degrees (9,15), with ONE double chart (p=0) and two resonant windows (d3 at j=4 with `F_4=lambda*H*p*(p+g)`, and d1 at j=7 with `F_7=lambda*p*(p+g)` and q0=8). There the two-chart comparison trick is unavailable by construction, so any argument that closes e=3 must be chart-local. This is why e=3 is the right calibration object: it isolates "does a resonant endpoint admit actual polynomial solutions" from "do two charts disagree".

Caveats stated plainly: the balance conventions are the framework's and are unreviewed; my census assumes the same Morse-chart bookkeeping at general e; I have not checked that the framework's remainder normalization (2) is literally e-independent for `j<=e` versus `j>e` (at e=5 the split is j<=5 versus j>=6, and the degree argument `3e-j<2e` for `j>e` transfers). Nothing here proves a local lemma.

## 6. Cross-avenue connections

1. **Avenue 1 x avenue 36 (method control).** The resonance census turns the guided-search avenue into a *calibration instrument for a proof mechanism*: solve the (9,15) toy ideal and compare any point's canonical reference order j against the predicted resonant windows {4,7}. A point at any other j refutes the framework's arithmetic; a point at j in {4,7} shows resonance is realizable with one chart; a unit ideal is consistent with a chart-local resonance kill. History: the single AUDIT line containing "(9,15)" is an unrelated forced-nu sub-book statement, not this object.
2. **Avenue 1 x avenue 29.** WF's centralizer step is the Hamiltonian-derivation constant-field argument; its only hypothesis is one simple factor. That is exactly why `H=p^2 g^2 L`-type tops with the target line inside H fall outside the class: the census should not be extended to them without a new centralizer proof.
3. **Conditional interface x counterexample front.** WF drops the 105 lift rows, so a WF-falsifier does not need to lift. But every WF-falsifier with the receiver polygons is a point of an open common3/common4 receiver ideal (the unequal ones are already theorems). So "falsify WF" and "find a receiver point" coincide on the polygon supports, and the only genuinely cheaper falsifier is the e=3 toy.

## 7. Strongest attacks

**Proof attack.** Finish the WF local consumers in the order the census dictates: (a) the strict-margin lemmas (simple root, d2, d3, d1 for j below the margin), which are one Euler/UFD argument each once the chart order bounds are proved without a cubic-in-g reference; (b) the single resonant step, golden j12, by comparing the two double charts at p=0 and M=0 under ONE declared normalization of lambda, orientation and both square-root signs. Hostile checklist for (b), from FALLACY-v2 "variable/ring map" and "prime label": the two chart brackets have conventions `-c*y^2` at p=0 and `c*y^2/t^2` at M=0 (framework table); a sign slip there converts a unit difference into agreement, so the gate must recompute both conventions from `[A,B]_(p,g)=-[A,B]_(g,p)` and the actual root, not from labels. Different-model rule: the framework is Astra's and the local proof is root's (Astra); the hostile gate must be Fable or Sol.

**Falsification attack.** Attack WF where it is strongest-claimed and cheapest to test: solve the e=3 toy (card A). Secondary: if WF passes review, the disproof side must move off D125 entirely, because the remaining 75/125 objects not covered are exactly the non-standard-F2 pairs the chain never covered; no receiver for them exists in the campaign, so a disproof lane there is design work (avenue 4/36 style support choice), not a solve.

## 8. Software acceleration / decisive experiment

The e=3 toy ideal (card A) is the decisive experiment. Size: 55+136=191 coefficient slots minus 26 fixed top slots, plus c and z with `z*c-1`; 276 bilinear bracket rows of degrees 0..22. This is two orders of magnitude below the 371/816 common4 stream and inside the msolve envelope recorded for the k4ray charts. No new software is needed; the acceleration is choosing the smallest object that tests the mechanism.

## 9. Cards (two; the third slot is deliberately unused)

**Card A — e=3 resonance calibration. Label NEW.**
- Object/field/hypotheses: over Q, unknown `A,B in Q[g,p]`, deg 9/15, tops `(p^2(p+g))^3` and `(p^2(p+g))^5` (26 slots fixed), `[A,B]_(g,p)=c*g^2`, ideal `I_3` in the 165 free slots plus c,z with `z*c-1`. Ambient ring polynomial, no quotient, no saturation.
- Dependencies / tier: none on WF, root's proof or any gate; the census of section 5 is the only premise and it is desk arithmetic. Tier at launch: EXPERIMENT, method control, not a frontier object (frontier gate: total degrees 9/15 are classically closed for Keller pairs, but this is not a Keller pair; label method-control with this card as the named client).
- Cheapest discriminator: `msolve -g 2` on one c7i.4xlarge worker, 1-hour cap; if it returns `[1]` in characteristic 0, confirm with an exact Singular `std` on the same worker (char-0 msolve `-g` first-prime caveat per COORDINATION).
- Outcomes: unit ideal, confirmed exact: consistent with a chart-local resonance kill; record and stop. Proper ideal with a rational or algebraic point: compute its canonical reference and j; j in {4,7} means resonant endpoints are realizable with a single chart, so the golden j12 step genuinely needs the two-chart comparison and any single-chart WF argument is suspect; j outside {4,7} refutes the census arithmetic or the balance conventions and must be traced before any WF promotion. Timeout: uninformative, do not escalate memory or time.
- Stop condition: one run, one confirmation, total under two worker-hours.
- Expected information gain: high per dollar; it is the only test in the packet that can produce a falsifier for the WF mechanism at all.

**Card B — e-uniformity desk audit of WF sections 2–4. Label NEW (audit), no compute.**
- Object: the framework text at general e, with the census table of section 5 as the target statement.
- Dependencies / tier: framework text only; prose.
- Cheapest discriminator: one lane-hour by a non-Astra model; deliverable is a list of every inequality that is e-specific.
- Outcomes: fully e-uniform except the listed resonances: WF becomes a family theorem candidate for the (3e,5e) class, strengthening the review target and making card A a literal instance; an e-specific step found: record it, it bounds how far the eventual theorem reaches and tells the hostile gate where to look.
- Stop: after the list; no launch.
- Information gain: moderate; it converts an e=5 proof review into a class review at no compute.

## 10. Continue / redesign / stop for major work

- **Continue:** root's weight-free local proof under its lease, followed by ONE different-model hostile gate (Fable or Sol) that reads the framework and the local proof together and works the section 7 checklist; the framework review itself; all-degree coverage pressure (bottleneck 2) as prose work.
- **Redesign:** the counterexample front. Replace "solve the 372/817 common4/Q h localization" with card A as the first discriminator; return to h only if WF fails review and the toy is unit. This is a sequencing change, not a stop.
- **Defer (LIVE already says so):** the common4/Q h solve; do not build its stream while WF is under review.
- **Stop:** any solver or ansatz on the retired unequal/Q and unequal/golden clients; any reading of the master table row26 successor as open; any new receiver presentation whose only merit is fewer variables.
- **Unchanged:** 12-hour full-round and 24-hour broad-sweep backstops; K16 and u_s=1 fronts stay open obligations without new launches from this report.

## 11. Systems check: UPGRADE, delta-first packet header

Evidence: this packet was 255,106 bytes across seventeen files; reading it consumed roughly the first seven minutes of a thirty-minute window even with parallel reads, and the 2220 synthesis records that root-blind-first ingestion, not model startup, consumed its window. Most of the bytes were unchanged since the 2220 round (COORDINATION, APPROACHES, the master table, the receiver composition).

Smallest measurable test: for the next round, the preparer adds a 2 KB "delta since previous synthesis" block at the top of packet.md listing only the inputs whose hash changed and the promoted entries added since the last round. Measure, from existing receipts only, launch-to-first-section-write latency for each peer seat against this round. Keep the whole packet; the block is an index, not a substitute. Revert if any peer cites the block instead of the charged input. No scheduler, wrapper or telemetry is added.

## 12. Observed contributions and correction burden

Judged on the frozen inputs only. Astra: produced the golden two-regime source, the weight-free framework and the conditional interface; corrections needed were notational (a_s reuse, pre-substitution negative orders) and did not change scope. Root (Astra-model coordinator): independent derivations before each review and the resonant scalar splice; its local proof is unreviewed and outside evidence. Fable: three hostile gates (15m, 15n, 15p) confirmed with scope corrections that survived (lower-B lemma m>=1); its earlier blind ansatz detour was a correction burden. Sol: 15o typing with one wire correction; the block-index revival came from a stale table, not from Sol alone. Opus and Grok: no standing seat is warranted by this packet. Assignment rule that follows: hostile gate of WF must not be Astra; card A is engineering-light and fits Sol or Grok; card B fits Fable.

## OPENS RAISED

- OPEN[WF-RESONANCE-CENSUS] QUANTITY: decide whether the set of resonant (e,k,stratum,j,q0) in class C_e with e-2k>=1 is exactly {(5,2,d1,12,-),(3,1,d3,4,-),(3,1,d1,7,8)}, i.e. whether |set| = 3. CHEAPEST TEST: desk rederivation of section 5 by one non-Astra lane, prose only, one lane-hour, no compute.
- OPEN[E3-TOY-POINT] QUANTITY: decide whether dim V(I_3) >= 0 (proper) or I_3 = (1) for the e=3 toy ideal of card A over Q. CHEAPEST TEST: msolve -g 2 on one c7i.4xlarge worker with a one-hour cap, exact Singular std confirmation if unit; total under two worker-hours.

## 13. Scope limitations and terminal handshake

Limitations: section 5 is my own unreviewed desk arithmetic that inherits the framework's unreviewed balance conventions; card A is a method control on a non-Keller object and its result cannot promote or refute WF by itself, only the mechanism's arithmetic; no all-degree, properness, unit-certificate or JC2 claim is made or implied; the six-client conditional coverage is not banked. I did not verify any manifest hash against the canonical source files, only against the frozen copies. Late/partial status: complete within the 01:22:00 UTC deadline as far as I can tell at write time.

Handshake: single report at the prompt's destination, no owned task box, no shared edit, no launch, no peer or live read. Writer terminal after the seal line below.

## COLLISIONS

status: EMPTY (ops/open_collision.py, run 2026-09-09 01:01 UTC on this report, same-round files excluded from view)

- `OPEN[WF-RESONANCE-CENSUS]`: NONE
- `OPEN[E3-TOY-POINT]`: NONE

<!-- BODY-END -->
