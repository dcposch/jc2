# Custody cleanup: raw-overlap normal-rank/Fitting and ordinary tangent review

Date: 2026-08-25  
Status: **NONMUTATING CUSTODY REPAIR; CONFIRMED**  
Immutable raw review: `xmodel/max12-912-order3-nu-q8-w0-overlap-normal-rank-tangent-review-grok-20260825.md`  
Immutable raw-review SHA-256: `6bdbd48a08e099f4ed3ef6e81078cf8b69fb65429f50b8235ee9a45b087abe67`  
Raw run-metadata SHA-256: `78f526b5d2c4208c5e4acbbe2482916a16a149b1f5acd01687f8e98060dee726`

## 1. Why this companion exists

The immutable raw Grok review is mathematically coherent from its `## 2` through
`## 9` and ends in `CONFIRMED`, but its lines 13--20 were damaged by an output
collision: the beginning of the execution/files-read preface was overwritten by
a second short final answer.  The damaged bytes include the literal fragments
`This review used nThe review...` and `56``.  The raw file is preserved exactly;
it must not be silently edited or cited alone as a clean review artifact.

This companion restores only the lost custody preface, identifies the intact
mathematical body, and records its exact narrow conclusion.  It does not replace,
rewrite, or strengthen any mathematical argument in the raw review.

## 2. Restored execution and files-read disclosure

The review was performed by Grok as a different model from the producer lane,
under the prompt's read-only/no-local-compute rules.  Its recorded run metadata
states: no Bash, local CAS, solver, or network; exit code zero; prompt SHA-256
`3869efa05e51f6471d06f0c32e6fc86c59894c64bfdccc744c6ea71bb2a01a0b`;
and the exact two producer-report hashes below.

The requested and audited materials were:

- `xmodel/max12-912-order3-nu-q8-w0-overlap-normal-rank-fitting-20260825.md`,
  SHA-256 `fca71aaa93f250eb1e13b70fb5abbe97875488fedb54c85abf1499fc31a247df`;
- `xmodel/max12-912-order3-nu-q8-w0-overlap-horizontal-tangent-20260825.md`,
  SHA-256 `e421aec08ad53e72e77f872d4b518875cade6189d0880a5de0170f0305133dbe`;
- the complete frozen Fitting case
  `cases/max12_912_order3_nu_q8_w0_overlap_normal_rank_fitting_aws_20260825/`,
  including preregistration, README, generator, runner, both accepted AWS
  endpoint trees, replay, manifest, freeze, and replay endpoint;
- the complete frozen tangent case
  `cases/max12_912_order3_nu_q8_w0_overlap_horizontal_tangent_aws_20260825/`,
  including preregistrations, README, V1/V2/V3 generators and runners, retained
  negative controls, both accepted V3 AWS endpoint trees, replay, manifests,
  freeze, and replay endpoint; and
- the pinned quotient compiler
  `cases/max12_912_order3_nu_q8_global_quotient_probe_20260824/quotient_compiler.py`
  as needed to verify source identity, the imposed row tuple, and coordinates.

The raw review's intact detailed audit is exactly its sections `## 2` through
`## 9` (raw lines 22--257).  Those sections audit source identity, the matrices
and Fitting saturations, the doubled rank-drop scheme, the independent tangent
calculation, engine/order/host custody, V1/V2 controls, the firewall, process
hardenings, and the verdict.

## 3. Canonical narrow conclusion

For the six-row source `I=(e1,e3,e5,e7,e2,e4)` restricted to the raw overlap
`A3: w=x1=x3=x5=0`, let `M` be the six-by-three normal Jacobian in
`(x1,x3,x5)` and `N=[M|F_w]`.  The reviewed exact statements are:

- `I3(M)=((d2-d4-1)^2)`, including the doubled scheme structure;
- the exact-rank ordinary `delta w=1` incidence closures are
  `K3=(d2,d4)` and `K2=K1=K0=(1)`;
- `K3` is the line `d2=d4=0` with `c` free, disjoint from the rank-drop line,
  and its normal tangent has `dx1=dx3=dx5=0`, so it stays in the boundary sheet;
- the separate two-equation tangent screen has scheme
  `(d2-2*d4,d4*(d4-2))`; its second point `(4,2)` is killed by
  `729*L7=-432`; and
- the rank-drop line, including its rank-one point `(d2,d4)=(2,1)`, carries no
  ordinary `delta w=1` tangent.

This is an **ordinary first-order/Fitting theorem only**.  It does not exclude
ramified or weighted arcs, `delta w=0` directions, higher jets, the full selected
horizontal closure `I:(w*x5*(x3-2*x5))^infinity`, coefficient infinity,
Taylor/terminal realization, trajectories, the full `(9,12)` cell, maximum
twelve, or JC2.  All of those remain charged exactly as in raw review section 7.

## 4. Verdict and citation rule

The mathematical verdict recorded at raw lines 253--257 is `CONFIRMED`.  The
four nonblocking process/prose hardenings at raw lines 240--251 do not alter the
theorem.  Until this custody repair receives its separate reviewer attestation,
canonical ledgers must cite the raw review and this cleanup together and mention
the raw preface corruption.  After attestation, they may cite the pair as the
clean reviewed record; the immutable raw hash remains part of custody.

CONFIRMED
