You are the hostile different-model reviewer for the exact ring reduction
that controls the next ordered-`a1` computation. Work in
`/Users/dc/code/math/jc2`.

Producer:
`xmodel/max12-812-order2-p0-total-rees-j2-a1-rho0-to-total-dvr-design-sol-20260827.md`
SHA-256:
`e3d263d5c0006bc4f05c5b7ff17bfcbd68bab52f7c1d3ccb5d323facd94448f7`

This review must be mathematical, not a prose vote. Check the actual V23/V35
ordered-`a1` presentation, the staged-calculus theorem and converse correction
in `AUDIT.md`, the V38/V39 promotion, and V42 producer. Independently attack:

1. the claimed ambient ring `S=Q[rho,X_19]`, the assertion that rho is the
   only weight-zero variable after the ordered quotient, and the identification
   of the honest ordered chart ideal as `K=J:a1^infinity`; identify any omitted
   chart/Rees/source generator;
2. the specialization map and inclusion
   `sp(J:a1^infinity) subseteq sp(J):a1^infinity`, including the direction;
3. the four-way equivalence between `K+(rho)=1` and a typed identity
   `a1^N U(rho^2) in J`, `U(0)=1`. Check the weight-zero projection,
   saturation clearing, even-rho averaging, and the absence of a genuine
   localizer;
4. the homogeneous even toy counterexample `(f-rho^2*x)` and whether it
   really demonstrates strict specialization/saturation failure;
5. the fixed-weight `Q[t]_(t)` module/colon formulation. Verify whether the
   last coordinates of syzygies of `[A|b]` generate the stated colon, the
   unit-at-`t=0` criterion, and the exactness of support-component pruning;
6. the launch rule `N=6` from V38, and whether a rho-zero positive/negative
   gate is logically sufficient before the univariate solve.

Return separate `CONFIRMED`, `REFUTED`, or `GAP` verdicts for the chart/ring
identification, the certificate equivalence, counterexample, module criterion,
and W30 launch design. If a presentation map is missing, give the smallest
repair and say whether the AWS job is still sound or must stop. Preserve the
firewall: none of this is the certificate result itself, Gate T, or JC2.

Write the complete report to exactly
`xmodel/max12-812-order2-p0-total-rees-j2-a1-rho0-to-total-dvr-design-hostile-review-fable5-20260827.md`.
Touch no other campaign artifact. Do not enter, read, build, status-inspect,
or modify `jc2-lean`. Short desk-scale exact checks only; no heavy local CAS,
no AWS launch, no web sweep, and no canonical-ledger edits.
