# Fable 5 hostile review — Grok td=8 parametric Prop. 8.1(iv) theorem

Producer: Grok 4.6. Work independently from `/Users/dc/code/math/jc2` and
write exactly:

`xmodel/m2-td8-prop81iv-parametric-hostile-review-fable5-20260829.md`

Read the complete target
`xmodel/m2-td8-prop81iv-parametric-primary-grok46-20260829.md`, its packet
`cases/m2_td8_prop81iv_parametric_grok46_20260829/`, the reviewed D2 route
producer/review named by the target, printed Notation/Proposition 8.1 and the
specific Sigray statements cited by the target, and only the directly relevant
promoted ladder derivations. Do not read the active Sol or Opus Prop. 8.1
consumer reports before completing your independent derivation.

Treat every assertion as an allegation. Re-derive from printed notation and
the charged cell, rather than trusting campaign variable names:

1. whether the Prop. 8.1 polynomials are the displayed reduced patterns and
   whether its exponent is `i=14` rather than 7;
2. the distinction between `rho_frame=X/dp=2/3` and
   `rho_ODE=delta/(1-u)=deg(p)/deg(q)=6*nu/(2*nu+1)`, including whether only
   the ratio is pinned or numerical `delta,u` are available;
3. the reduction of the printed identity for
   `p=r(eta^nu)^3`, `q=eta*r(eta^nu)` and the iff condition that the two orbit
   values are opposite, with nonzero right side and uniqueness up to scale;
4. every local root, eta, gcd, searrow, multiplicity, orbit, chart/gauge, and
   full-versus-reduced side condition claimed for all `nu=4+3t`;
5. the exact maximum consequence: local Prop. 8.1(iv) survival only, with
   incoming/trunk lambda exactness, cross-vertex coefficient transport,
   landing, realizability, degree bounds, and JC2 kept outside unless you
   independently prove one.

Replay ordinary and `-O` tests. Build a clean-room exact derivation and
adversarial mutations in `/tmp`, with special attention to a hidden power of
`i`, a false full/reduced degree identification, orbit collision, illegal
gauge, or cancellation that makes the alleged nonzero RHS vanish. Return
`PASS_AT_LOCAL_PROP81IV_SCOPE`, `REPAIR_REQUIRED`, or `FAIL`, state the
narrowest sound replacement theorem, and include full/body hashes plus exact
commands/counts. A PASS authorizes canonical promotion of only that local
formal theorem; it authorizes no AWS launch.

Hard boundaries: no web, AWS, commit, push, canonical edits, heavy CAS, or
local long/high-memory process. Never access/list/search/build/status or
control `jc2-lean`; never run global `git status` or workspace-wide searches.
Modify only the requested report; use `/tmp` for scratch.
