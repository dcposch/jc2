You are Grok, the independent hostile reviewer for the additive `8_28`
Keller-face cusp-jet R1 raw/local type repair and bounded image obstruction.
Work only in `/Users/dc/code/math/jc2`.  Never enter, read, build,
status-inspect, or modify `jc2-lean`.  Do not edit frozen producer files,
launch AWS, run heavy CAS, perform a web sweep, or edit canonical ledgers.

This is an **additive follow-up**, not a retrial or retraction of R0.  The
completed R0 hostile review

```text
171ff47c844331da8971a731c1ffaf20c31ed5b2ba86fdee2d0d81219eb002b0
  xmodel/ggv-8_28-keller-face-cusp-jet-pinning-hostile-review-grok-20260827.md
```

confirmed all thirteen charged R0 items.  Preserve that verdict at R0's
stated **local/rootwise carrier** scope.  R1 makes the local/raw distinction
explicit and narrows the polynomial image obstruction to two named ansatzes;
it must not be interpreted as refuting the rootwise R0 identity or as proving
a face/family exclusion.

Review these exact R1 artifacts:

```text
f21f6435796f197376f76fc9c57c50743e9a356614591ce88e41dcfad4c3e5f0
  cases/ggv_8_28_keller_face_cusp_jet_pinning_r1_20260827/verify_r1.py
392f6c0c3ca70a35b9b466944c243a927430838abe022eb960d7f2315e1684f3
  cases/ggv_8_28_keller_face_cusp_jet_pinning_r1_20260827/RESULT_R1.json
238f51d031b81be14cbbd6756778efbf742a7471b9247e964d78d9f4b1050781
  cases/ggv_8_28_keller_face_cusp_jet_pinning_r1_20260827/README.md
66121bddbe0b004ad1b8960f896d3691c4dd3963068fbfddb6ad3ba15dda7027
  xmodel/ggv-8_28-keller-face-cusp-jet-pinning-r1-sol-20260827.md
05cbb5c046f9aa4b89e1104448e77fd38ec3a146d2422bf84773f81d668bfe3c
  cases/ggv_8_28_keller_face_cusp_jet_pinning_r1_20260827/FREEZE.sha256
```

Do not accept producer `PASS`, printed identities, the Python implementation,
or the completed R0 review as evidence for any new R1 claim.  Independently
rederive and attack only the following:

1. **Custody and additive firewall.** Rehash every charged artifact and every
   dependency pinned by the R1 freeze.  Verify that R0 bytes were not changed.
   State explicitly that the confirmed R0 rootwise identity
   `6 H'(c)V_8(c)U_14(c)=1` remains valid as a local fixture, and that R1 only
   corrects any possible reading of `U_14=X` as a direct raw source slice.
2. **Raw slices.** Starting from
   `S=conv{(0,0),(1,0),(8,28),(0,4)}` and
   `F=t^8 f(t^3X,t^-1)`, `G=t^12 g(t^3X,t^-1)`, independently derive

   ```text
   F_n: max(0,ceil((n-8)/3)) <= i <= 16-n, j=8+3i-n;
   G_n: max(0,ceil((n-12)/3)) <= i <= 24-n, j=12+3i-n.
   ```

   Check all boundary/empty cases used by the report, especially that raw
   `F_14` has only `X^2`, raw `G_21` only `X^3`, and raw `G_22` is empty.
   Verify that a *direct* local term `U_14=X` would pull back to `x y^-3`.
   Also verify the R1 caveat: formal cleanup can produce local `U_14` from a
   combination of lower raw jets, so direct-slice failure alone proves no
   global nonexistence.
3. **First named ansatz (`V_8 U_14`).** Without trusting the verifier, expand
   the complete `n=22` cross contribution for

   ```text
   F_8=P,   G_8=(3/2)HP+CH,
   F_14=A,  G_14=(3/2)HA
   ```

   and prove or refute

   ```text
   E_22 = M(A((3/2)P+C)),  M(Y)=4HY'+6H'Y.
   ```

   Check whether `C` really must be constant under the claimed polynomial
   solution and whether the phrase “every correction inside this two-slot
   ansatz” is exact.  Do not silently extend it to omitted slots or arbitrary
   formal cleanups.
4. **Second named ansatz (`U_11^2`).** Independently compute the self-pair for
   `F_11=A`, `G_11=(3/2)HA` and verify or refute
   `E_22=M(3A^2/4)`.
5. **Image obstruction.** For `H=X^8-1`, independently prove or find a
   counterexample to

   ```text
   deg M(Y)=deg(Y)+7,
   lc M(Y)=(4 deg(Y)+48)lc(Y)
   ```

   for every nonzero polynomial `Y` in the stated characteristic-zero
   coefficient field.  Treat `Y=0` separately.  Decide whether this excludes
   `M(Y)=1` in each of the two named ansatzes.  Recompute
   `M(X/48)=1+(13/12)H` exactly.
6. **Local carrier discussion.** Check the displayed scalar cusp equations
   only to the extent needed to justify why the two named carrier patterns
   are plausible local cases.  Explicitly flag that R1 has *not* proved the
   typed-normal-form theorem for arbitrary raw `2S/3S` jets: higher-`u`
   terms, root-dependent cleanup, determinant units, polynomial provenance,
   and deck/factor gluing remain outside the obstruction unless separately
   proved.
7. **Scope firewall.** The maximum permitted R1 verdict is: exact raw/local
   type repair plus failure of the polynomial equation `M(Y)=1` inside the
   two displayed normalized ansatzes.  It is not a typed-normal-form
   completeness theorem, a full `8_28` face/family exclusion, a Keller-pair
   obstruction, GGV-to-tree transport, `G2-PSC`, `G2-BD`, or JC2.

Return separate `CONFIRMED`, `REFUTED`, or `GAP/REPAIR` verdicts for custody;
R0-preservation/local scope; each raw-slice formula and boundary case; the
local/raw `U_14` distinction; the two ansatz computations; the operator
degree obstruction; the exact `13H/12` remainder; the unproved
typed-normal-form completeness gap; and final scope.  Give the smallest
failing monomial, coefficient identity, degree case, or missing hypothesis
and the minimal additive repair if anything fails.

Desk-scale hashes and independent exact arithmetic only.  Write the complete
review to exactly

`xmodel/ggv-8_28-keller-face-cusp-jet-pinning-r1-hostile-review-grok-20260827.md`

and touch no other campaign artifact.
