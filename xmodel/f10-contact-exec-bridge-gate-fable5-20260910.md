# FIRST hostile gate: contact exec bridge and exact mutation payloads

2026-09-10. Fable5.1 gate. First action 05:16:18 UTC; own report/box ABSENT
then; fixed stop 05:22:00 UTC, reserve 05:20:00 UTC, NEVER RESET.
Twelve immutable inputs in /tmp/jc2-lane.GrPfhf/inputs; all 12 SHA-256
matched the charged BEFORE list before any body was read (sha256sum, text
only). run_capped.py HASH ONLY (4435279d). No source executed, imported,
AST/syntax/compile-checked; no CAS/Python-science subprocess; no network.
The optional general.doc URL was NOT fetched (time-bound); every Singular
startup-flag statement below is therefore UNVERIFIED in this lane.
Accepted 17zt producer/checker static gate is a premise, not re-hardened.

## A. Admission/authority — CONFIRMED with declared limit

- CLI exact: argv length 4, MODE in produce/check/mutate, Linux, -I -B
  (payload_exec.py:39-44). Canonical absolute regular-file arguments
  (30-35): resolve(strict) must equal the literal string, so symlinked or
  relative AUTHORITY/INPUT refuse.
- Gate hashed against cbfe55ff before metadata import, symlink refused
  (51-57). authorize called before any scientific import (58); only stdlib
  modules are imported in the bridge.
- Authority reread bounded (65537) and bound to receipt authority_sha256
  (61-65); operation rechecked against MODE (67-68).
- Selected membership enforced in EVERY mode: script, gate (fixed SHA),
  runner (fixed SHA), /usr/bin/python3 plus resolved target, INPUT
  (101-105); per mode the fixed sibling payload (109/117/120-121), and
  /usr/bin/Singular plus resolved target for produce (112). Unrelated
  declared files cannot substitute. Symlink siblings refused (84-89).
- Custody vs hostile same-user: interface:60-62 and report:131-132 declare
  immutable no-concurrent-writer custody and NO race-proof claim; env vars
  are declared data binding, not authorization (bridge:127, mutator:5-6,
  interface:91-92). Exact limit: the unchanged gate parses read_text
  (gate:18) but hashes read_bytes (gate:72); the bridge binds to the
  SECOND read, so the binding is cooperative-custody evidence only. This
  is inside the declared scope, not a defect of the claim.

## B. Exec/runtime — CONFIRMED; one interface GAP

- os.execv only (142); grep finds no subprocess/fork/setpgid/setsid/
  shell/timeout in bridge or mutator. PID/PGID recorded then preserved by
  replacement; argv[0] absolute, no PATH search.
- Singular argv fixed: -q --no-rc --no-stdlib --no-tty --random=0 (113);
  produce.sing uses kernel commands only (no LIB, no standard.lib procs
  found by grep) and ends with quit, so --no-stdlib and stdin are not
  load-bearing. Flag semantics UNVERIFIED here (URL not fetched).
- Check mode launches a fresh CLI whose main() sets the digit policy before
  parse (check_certificate.py:172-175); no check(raw) API call anywhere.
- Probe/dummy/native-closure duties remain ROOT's, declared UNEXECUTED.
- GAP-B1 (interface): the bridge prints its CONTACT_AUTHORIZED_EXEC JSON
  line to STDOUT before exec (134-139), so the stdout_file is never a lone
  CHECK_OK line. ROOT-execution-interface.md:102 states only "status0,
  CHECK_OK and matching SHA" and does not say CHECK_OK must be matched as a
  line after the bridge event; nor that PRODUCER_STAGE/CANDIDATE_WRITTEN
  markers (which the producer prints to stdout, and -q may mix with
  errors) are never success. Remedy: add both sentences to the interface.

## C. Mutation correctness — CONFIRMED; one declaration GAP

- Bounded read MAX_BYTES+1, size and SHA against the transported input pin
  (32-35); checker pinned against 2e0dda86 before import (43-45); digit
  policy set before exec_module and any parse (46-47).
- Shallow copies: candidate=dict(source); Poly.__add__ builds a new dict
  (checker:29-33), so source Polys are untouched. Header token list equals
  the checker header tuple token for token; block order F,H1,H2,H3,H4;
  numerator/denominator strings are canonical (Fraction reduced, positive
  denominator, str(int) round-trips the INTEGER regex); zero coefficients
  drop, so nilpotent/zero terms never serialize; exponents nonnegative.
- Reparse proves ONLY the selected name +1 and every other delta {} via
  .terms equality (80-86). F=-1 edge gives an empty F block, which the
  checker parses and refuses at "F is zero" as required.
- No check(raw) call; no positive claim (54-55, 103). Exclusive "xb"
  creation, bounded readback, byte compare, reparse (91-98). Partial-pair
  failure declared (89-90; interface:80-82). Marker carries input,
  authority and both output SHAs.
- GAP-C1 (declaration): serialize allocates the token list, joined string
  and bytes (and reparse splits again) BEFORE the >64MiB refusal (70-72);
  neither report nor interface says the refusal is post-allocation and
  only the external RSS cap bounds memory. Remedy: one sentence in the
  interface. Not a code defect.

## D. Future controls — GAP (smallest actual defect)

- Static outcomes are right: original CLI 0/CHECK_OK; F+1 residual 1, or
  zero-F refusal; H4+1 residual -I4 = 1-zcA, nonzero at z=0; both status 2
  and CHECK_FAIL. Variable index for X is 3, consistent in both sources.
- DEFECT-D1: interface:102-104 accepts "status2 and CHECK_FAIL" for both
  corruptions without the reason string. A truncated, mis-staged or
  wrong-header file also yields status 2 CHECK_FAIL, so a broken control
  would count as success. Minimal remedy: require stderr line exactly
  `CHECK_FAIL exact polynomial identity is nonzero` for BOTH corruptions
  (`CHECK_FAIL F is zero` only if F+1 is zero), and treat any other reason
  as control failure. Interface text change only; no source repair.

## Verdict

A CONFIRMED (declared cooperative-custody limit). B CONFIRMED with GAP-B1.
C CONFIRMED with GAP-C1. D GAP: DEFECT-D1 is the smallest actual defect;
remedy is three sentences in ROOT-execution-interface.md. No positive
runtime, native, version or CHECK_OK claim is made or licensed here. No
launch, promotion, follow-on or direct payload invocation is authorized.

## OPEN(S) RAISED

- GAP-B1, GAP-C1, DEFECT-D1 above: interface acceptance-criterion text
  only; ASSIGNED to ROOT's interface revision, no new canonical ID.

## COLLISIONS

status: EMPTY

- Own report and box were ABSENT at 05:16:18 UTC; only own files written.
- No other-lane, corpus, protected, shared, live, Git, proc or network read.

<!-- BODY-END -->
