# Pure-data closed-nine late binder

Complete bounded implementation; NOT independently reviewed or operationally authorized. First action2026-09-11 14:57:44.616982771UTC; original reserve15:17/hard15:20 unchanged. Eight historical text inputs plus ROOT TASK freshly pinned and read WHOLE. No live setup, input-shell execution, worker or release authority. Candidate metadata remains in memory only.

## Outcome

The new pure-data `latebind.py` API emits fourteen candidate byte objects without I/O: nine canonical authority/policy objects, registration, execution card, final input manifest, installer and a typed hash/change summary. Its only imports are copy/hashlib/json/re. It accepts four ROOT-pinned prepared documents, a typed observation JSON record and an explicit ROOT metadata-binding decision. It is not a scheduler, installer, command executor, physical observer or self-authenticating authority.

The historical positive control reconstructs final registration, card, installer and manifest EXACTLY byte-for-byte. All seven non-probe authority objects retain their canonical bytes; only valid/dummy leaf metadata change. Refuse-hash keeps its deliberately zero command digest. A separate in-memory changed-preparation control changes worker, boot, namespace, batch/base and clocks coherently and confirms their preservation; the API is not confined to the historical identity tuple. These are documentary controls, not new worker facts.

The helper reconstructs the entire prepared nine-command/authority relation and fixes the closed-nine schema, phase order, flags, limits, profiles, CPU origin0, paths and environment. It checks individual leaf device/inodes, duplicate identities, exact types, expected placeholder counts and permitted digest positions. A reverse edit verifies that every other parsed registration/authority value is unchanged. Card/installer bytes outside the explicit replacement set remain unchanged. Final native.sha256 is the opaque list digest f5a855a617f317080e23724f2590cfb74aa674986285b89752816bbd991658c0 in the historical test; it is NOT either registration native-inventory digest. No native body was accessed.

The acyclic binding order is policies/authorities → registration → card → three-entry manifest → installer. The manifest excludes the installer; the summary excludes its own hash. The eight nonzero used command digests are derived from actual candidate bytes. The helper rejects registration self-pins, unknown remaining placeholders, false/absent approval fields and prepared hash drift. Supplied attestations must still be authenticated by ROOT outside this code.

## Validation and scope

Authorized command: `python3 -I -S -B box/caprun-latebinder-code-astra-20260911/test_latebind.py`. Initial11-method run passed; final13-method run passed in unittest-reported0.059s. This is local pure-data test time only, NOT a live120second qualification. No syntax/AST/scientific/runtime/dummy test was run. The test harness read the eight pinned historical text files and its own helper; all candidates were constructed only in memory. No candidate file was written or shell text executed.

Negative controls cover missing/duplicate/wrong-type/wrong-path leaves, booleans masquerading as integers, duplicate cgroup identities, wrong decisions/context/observation digest, changed limits/command paths/order, unknown/duplicated placeholders, wrong prepared hash, wrong boot/native role, duplicate JSON keys, floats/nonfinite values and oversized bytes. Input objects remain unchanged. A positive changed-object control confirms that valid and dummy devices need not be identical.

Input bound131072 bytes each; prepared aggregate and returned aggregate524288 bytes; per-output131072 bytes. JSON decoded limits cover depth, nodes, keys, strings and integers. All parser exceptions are failures, never partial success. Complete schema and exact allowed edits are in CONTRACT.md; PINS.json records all nine charged inputs, code hashes and actual read/test scope. Human fresh WHOLE reads recovered all selected ranges without omission; no source/native/provenance body was followed. Own helper/test source was read WHOLE after edits.

ROOT remains responsible for preparing/authenticating future bound bytes, actual timer/native/physical/ACL/current-clock observations, explicit metadata binding, complete final readback, installation and fresh liveness before separate token release. Candidate enabled flags and state text are produced only under the explicit supplied decision, while the summary unconditionally says CANDIDATE_NOT_INSTALLED_NOT_RELEASED. A malicious or mistaken caller can forge attestations; this API does not claim to defend against that trust violation.

## Quantity, cheapest test, collisions

Raised canonical OPEN items:0. No mathematical claim, source decision, retry or new authority is created. The concrete next quantity is whether FIRST independent review confirms the helper's exact allowed-change and candidate-only contract. Cheapest next test is that bounded review of code/contract/controls; an optional later ROOT-authorized host measurement remains a separate decision. Planning review wall20minutes UNMEASURED, not a measured completion promise. Operational latency/120second adequacy remains UNKNOWN.

Own-only collision check: the new report/manifest were absent before the acquired lease; ROOT's TASK was preserved, old evidence and frozen files were not edited. No duplicate report or new framework was created. All nine input postpins matched before sealing. There is no permission for a dependent operational consumer before FIRST review. Publication is an ordinary expected-basis transaction; all writers will be IDLE before custody handoff.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5763`.
- Body SHA-256:
  `4ae160ca7393dbab5e2c4ad1775a36dce94fc639ce684006ba90bf4eb727e619`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
