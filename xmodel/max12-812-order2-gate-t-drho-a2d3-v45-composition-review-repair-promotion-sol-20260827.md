# Promotion after review repair: Gate-T V45 contact `(2,5,>=3)`

Date: 2026-08-27

Status: **PROMOTED AT THE ONE-CONTACT SCOPE BELOW AFTER CANONICAL
DIFFERENT-MODEL HOSTILE REVIEW CONFIRMED.**  This additive artifact does not rewrite
the frozen producer or its provisional result.

## Frozen authority

```text
0c0bdbc1b8cc6225747599ab07de8e0cfda350f621218312e39be1733f2686fd
  cases/max12_812_order2_gate_t_drho_a2d3_composition_v45_20260827/FREEZE.sha256
ada147715900468e727c3c56b76f99d642bc9ffccce6f81a0dac704372036ad8
  cases/max12_812_order2_gate_t_drho_a2d3_composition_v45_20260827/aws_q_r6i/compiled/result.json
27c7c28db2e18522e5e13134a3581bb818e344fa564fa18d473378472d7f203e
  cases/max12_812_order2_gate_t_drho_a2d3_composition_v45_20260827/aws_q_r6i/RESULT.json
53ab25b43632ed85484de5a00439f5abeca1d57d5ec9a0f708a00a06772bc169
  cases/max12_812_order2_gate_t_drho_a2d3_composition_v45_20260827/aws_q_r6i/EVIDENCE.sha256
234cc4021e03e8c40b694e7757bfb2d1bad51e8eafe66c417b231e59550a1af8
  cases/max12_812_order2_gate_t_drho_a2d3_composition_v45_20260827/RESULT.md
a35403456d35cce59320dab7add701303663343fc1b527d96605e39faf3c5188
  xmodel/max12-812-order2-gate-t-drho-a2d3-v45-hostile-review-v3-grok-20260827.md
```

The canonical Grok review independently rehashed the complete V44/V44R1/V44R3 and
`B23` chains, reconstructed the contact ideal, finite map, support bounds, Hensel jets,
localizer, and residue, and returned `CONFIRMED` on every charged item.  The earlier
direct-adapter and Claude/Opus attempts produced no review and are not evidence.

## Promoted theorem

In characteristic zero, after the already reviewed generic-square first-normal,
half-weight, and exact reduced `M=0` gates, the single strict unit-load unique-`AC`
contact

```text
ord(A)=2,  ord(C)=5,  ord(R)>=3;  d=3;  G=17;  T=20
```

is empty on `D(rho*k)`.

The proof has four reviewed layers.

1. V44 constructs all seven unspecialized general-`rho` actual-total rows through
   grade 20 from the frozen 569 tails before any face/contact quotient.  V44R1 fixes
   only a false terminal-jet postcondition; V44R3 fixes only the failed recursive
   parser and validates the exact-Q, `F_65521`, and cross-lane custody.  No face row is
   relabeled as general `rho`.
2. The contact quotient has exactly twenty lower vanishings.  All seven rows vanish in
   grades `0..16` (`119=7*17` coefficients).  The generated total-to-D1 map has 29
   entries, factors `2` on every surviving `C` jet and `4` on every surviving constant
   `R` jet, and keeps the total `k10` jet `k2c` distinct from the leading `k2` load.
   The reviewed/inventory-derived maxima, fail-closed against the total support, are
   `p/A/C=3,R=1,k10=2,k6=k2=0`; `k6,k2` are inactive leading declarations.  Every
   active maximum has a terminal witness and every omitted later jet first enters at
   grade at least 21.
3. The contact-quotiented total rows agree with the separately coded charged `B23`
   rows in all seven rows and grades `0..20`: 147 independent coefficient identities.
   The AWS compiler also repeats them after `rho -> -lambda`, but general-`rho` rows
   are even in `rho`, so the second set is the same identity, not independent
   orientation evidence.
4. The already promoted and hostile-reviewed `B23` endpoint symbolically treats both
   opposite-root allocations and the closed `R` tail.  Its terminal functional has
   exact residue `(3/2)*lambda^2*cv^2`, which pulls back to
   `(3/2)*rho^2*cv^2`.  Since `p=-2*rho^2` and `k0` is the image of `k`, its
   `D(p*k0)` localizer pulls back to exactly `D(rho*k)`, with no hidden inversion.

The Kummer erratum is respected: all stage-zero `J1+J2` pairs are among the killed
jets, and only shifted leading pairs occur in the finite map.  V45's rational Hensel
samples through `lambda3` are software checks; the endpoint's two-orientation theorem
is the pinned symbolic exact-Q identity, not an inference from those samples.

The direct three-row syzygy is only a negative control here.  `RA^2/L^2` enters at
grade 19 for `r=3` and grade 20 for `r=4`, so that shortcut cannot prove this closed
tail.  The raised exact subtail `r>=5` is a distinct theorem and is not imported into
this promotion.

## Firewall

This promotion covers exactly the displayed contact after the named gates.  It is not
a strict unique-`AC` fan or generic-square cover; it says nothing about equality
faces, positive-order leading load, `k=0`, `rho=0`, staged Rees charts, the
terminal/Taylor receiver, either global `G2` obligation, Gate T, order two, maximum
twelve, JC2, or a counterexample.

The completed V45 AWS computation used an already released shared `r6i.16xlarge`
slot; this promotion holds no AWS capacity.
