# Fixed-r3 complete source-cone reconstruction source

Status: STATIC IMPLEMENTATION / UNEXECUTED / UNREADY FOR EXECUTION. No reconstruction, source outcome or run authority. Independent FIRST source review and a separate runtime packet remain required.

Owner /root/contact_collision_geometry. First action 2026-09-11 00:02:34 UTC; exact report, manifest and box absent. Original publication reserve 00:39 UTC / HARD 00:42 UTC, never reset. Basis 0d39df3c9fd69c939a8420c54d03228b9077777d.

Exactly nineteen charged inputs, all fresh WHOLE with current pins; historical headers interpreted only at the ROOT card's accepted scopes. Old r2 source is architecture only. Whole B is rank-seven finite-etale, never assumed a field. No uniform-attempt or potential formula is imported.

## Result and remaining limitations

Seven owned source/text deliverables supply the fixed-r3 producer, independent checker, separate arithmetic implementations, prospective authority interlock, controls and read scope. All authored code is prospective only; no import, syntax/AST check, compilation, test, mathematical subprocess or coefficient output is permitted or performed. This is an implementation claim, not a claim that reconstruction passes or is feasible within its proposed cap.

## 1. Re-derived fixed-r3 contract

Here r=3,m=10,n=17,q=7,d=4. The exact base is the ENTIRE B=Q[V]/P7 at tau=3/10, not a field or selected factor. Both implementations form D,K,gamma from full-boundary(3),(8), then

    S=2K^2-140tau(1+tau-6V)KD+245gamma D^2,
    P7=S/(-245*120*144*tau), W=-K/(210D).

They check its degree/leading coefficient, literal leading t6=t7=0, W and t5 inverses, C=(1+theta+Vtheta^2+Wtheta^3)/W, Dlead=trunc5(c^(17/10))/t5, and 10CDlead'-17C'Dlead=-theta^7. The accepted complete-leading isomorphism and automatic guard are imported at card scope, not re-proved by this code. All inverses are verified in seven coordinates. No irreducibility, primitive residue point, real embedding, source-U inverse or parameter specialization is used.

Physical raw order is X1,X2,X3,X4,z,S,theta with weights1,2,3,4,7,1,3. It follows from the accepted normalized source and gap weights, rather than an r2 index rename. Ahat has weight10; Bhat has weight17; the Jacobian and complete upper target have weight23. Therefore the complete potential theta^j S^i slot inventory is j=0..7, i=0..23-3j: 24+21+18+15+12+9+6+3=108 slots. The two low arrays have21+24=45 slots.

The exact band table is implemented as follows:

| Gaps | Actual consumed variables | Retained outcome |
|---|---|---|
|1,2,3|d_(3-h),v_(6-h),k_(10-h)|X_h, both rows solved|
|4|U,v2,k6|X4 through the modified theta6 target and q2=-U|
|5,6|v_(6-h),k_(10-h)|both rows solved; actual middle H7 units at exponents11/5,21/10|
|7|k3|Psi7 with y=z-U*d0 and full forcing|
|8,9|k2,k1|Psi8,Psi9; fixed U^2 theta^3 and target -U^2 theta^5 at gap8|
|10|E|Psi10, no ell gauge|

Completed early maps have columns(U0,U1,U2), rows(R1,R0,rho). The affine homogeneous rho subtracts 10a*V_part(0) from the whole constant. For middle h=5,6, the inverse entry is `(3(h-3)-2)/(27-h)*H7_h`, namely2H7_5/11 and H7_6/3. This is checked before its actual use. All forcing sums use every earlier actual band, not just homogeneous representatives.

The general critical Lambda is specialized through its finite coefficient functional, not the old r2 value. Late delta2 values are -7 at j=2 and -4 at j=1, followed by subtraction10 and20; the operator coefficient is -(17-h), namely -9 and -8, respectively. Ell uses j0=6/11 and actual mate variation

    theta^2+(6F/11)theta+(13H-24F^2/11)/21.

Each one-column map checks lambda1*g1+lambda0*g0=1, the actual affine parameter, the compatibility Psi=g1*b0-g0*b1, and both residuals `(-lambda0*Psi,lambda1*Psi)`. No compatibility is declared zero during reconstruction. All residuals after substitution remain literal polynomials.

## 2. Whole-B inverses and independent reconstruction

The producer replaces old first-nonzero Gaussian pivots with the determinant/adjugate of each completed3x3 map. Its determinant is inverted only by checked Euclid against the WHOLE monic septic. An invertible matrix over a product algebra has unit determinant; no branch is lost. Six determinant witnesses, D/W/t5, two middle H7 and critical c give exactly twelve named unit witnesses. The checker re-forms the actual operator/rho matrices, re-computes determinants by a separate six-permutation sum, checks both matrix products and verifies application of the emitted inverse to the actual forcing. Each unit is tied to its actual algebraic input and all seven coordinates.

Producer fractional powers use finite binomial expansion. Checker powers use the differential coefficient recurrence c*f'=alpha*c'*f. The checker imports neither producer nor its arithmetic. It rebuilds the complete Euler mate using coefficient-pair Jacobian assembly, reconstructs particular/homogeneous band mates independently, verifies all source coefficient shifts/gauges and both affine maps, and reads back all108 and45 slots including zero and duplicate positions. It independently computes the literal column Bezout representatives with its own rational polynomial arithmetic.

Both programs perform the full normalized inverse-chart substitution S=P*q^3-z*q^2+U*q, theta=q^-1 and reject negative q powers in EITHER Ahat or Bhat. The checker additionally forms the Laurent source back-map z=s^2, theta=s*t, A=s^-3*Ahat, B=s^-5*Bhat, d=s^-1*Dpar,v=s^-2*Vpar,k=s^-3*Kpar,u=s^-1*U,ell=s^-3*E. It compares the whole A, target, bracket and every original low residual, including targets u and1. It checks omega*a*b=1 literally with omega=W*t5*s^8. No s=1 or source normalization is silently imposed.

## 3. Critical graph, all25 generators and strict wire contract

After all raw reconstruction, Psi7 is checked to equal Hq+c*z with c in B and a checked inverse. Set zeta=-c^-1*Hq. The exported graph inventory is exactly K8,K9,K10; A1_i for i1..9; A0_i for i1..12; T. Every row is independently read and compared to the whole raw polynomial after graph substitution. Its weights are8,9,10;19..11;22..11;27, respectively. In particular T is `(z*P1_0-U*P0_0)(X,zeta)`, not the z=1 formula. Also export and check ell=P0_0(X,zeta), Hq, zeta, c,c_inverse, U and g=Hq*ell of weight30. The identity zeta*ell=-c^-1*g checks the precise localization guard.

The accepted source/cone maps, with both low rows, remain the mathematical interpretation; code does not prove a new cover. Under the earlier w^7=s cover, low targets are w^40*(L1-U0*w^3), w^46*(L0-w^3). Keeping L1-U0L0 and L0 invertible is essential. The new homogeneous graph also retains Hq*ell invertible; no g=0 source is admitted. No highest regularity, finite basis, weight30 membership, all-r exclusion or source point is inferred from reconstruction.

Top-level JSON keys are exactly `format,job_tag,interface,base,source,bands,raw,graph`. All mathematical integers are canonical decimal STRINGS; rationals are reduced signed-numerator/positive-denominator `p/q` strings; all seven B coordinates are always present. No mathematical JSON number, float, constant, eval or pickle. Objects reject duplicate keys. Arrays have exact lengths, including ten ordered band packets, twelve named unit witnesses, four raw Psi,21 P1,24 P0,108 Jacobian slots and25 graph slots. Zero polynomials are empty arrays; repeated equation values are retained at their distinct required positions. Sparse terms are strictly lexicographically ordered, unique and have nonzero seven-coordinate coefficients.

Raw polynomials use SEVEN exponents; postgraph polynomials use FOUR, expanded by the checker with z,S,theta all zero. The componentwise wire exponent envelope is(30,15,10,7,4,30,16), truncated to its first four entries for graph data. This safely covers all exported weights through30, raw S powers through23, source theta degree5 and every exported band power through6; the formal coefficient functional is internal and never mistaken for graph data. Precise per-object weight/support checks further restrict this envelope. No old exponent16 cap on ALL coordinates is inherited.

Fixed resource caps are independent acceptance limits, not mathematical support claims:20000 sparse terms per wire polynomial,400000 serialized terms total,100000 internal terms per polynomial,128MiB encoded input/output and4096 characters per rational numerator/denominator. A process-local4300-digit integer conversion guard is installed before any coefficient parse. A valid but larger reconstruction is refused, not truncated or interpreted as a mathematical failure.

## 4. Prospective controls, runtime boundary and verdict

CONTROLS.md specifies six exact semantic payload changes: dL inverse plus one; ell theta2 variation1->2 giving theta4 defect-1; leading B coefficient1->2; known-zero rawP1_20 plus one; zeta plus X1^7; and T plus X1^27. Each preserves the applicable schema and has a proved nonzero mathematical defect. It names the exact checker predicate required; a different earlier parser/resource rejection earns no arithmetic credit. No mutation was emitted or run. The separate resource/schema refusal controls are explicitly not mathematical validation.

The CLI interlock rejects unauthorized host/tag before mathematical imports, requires ROOT-registered source pins and a separately pinned native inventory, checks every listed native byte pin and the interpreter, and sets process-local decimal protection. It is NOT sufficient runtime authority: ROOT must authenticate provenance, complete native/Python-startup/module-path closure, whole-process CPU/wall/RAM/output enforcement, controls and terminal custody. No existing r2 authority transfers. Suggested first observation is UNMEASURED: producer and independent checker each600 CPU seconds/900 wall seconds/8GiB, with128MiB aggregate artifact/log cap and a separately chosen combined deadline. No worker, launch, runtime extension or automatic cap increase is authorized.

Verdict: prospective fixed-r3 complete static source supplied; FIRST review and actual reconstruction/check outcomes remain UNKNOWN. The source has not even been imported, parsed/AST-checked, syntax-checked, compiled or tested. Zero coefficient/candidate/fixture body was read or written. Independent review may expose implementation defects; publication does not erase that possibility. A future literal positive cofactor identity needs its own independent checker and semantic mutation. There is no solver in this packet and no source-zero or JC2 claim.

## OPEN(S) RAISED

No new canonical identifier. QUANTITY: whether this exact whole-B r3 reconstruction completes within its proposed limits and passes independent complete readback; unknown, no coefficient counts/heights/timing measured. CHEAPEST NEXT INSTRUMENT: FIRST static source/control review of these exact seven files, estimated15–20 minutes, UNMEASURED; only after ROOT selection and a separate runtime gate, one producer observation plus fresh checker, proposed maximum1800 combined wall seconds. Any map failure, refusal or timeout is PARTIAL/NONDECISION. Exponent-one g membership is the accepted FUTURE target, not performed here.

## COLLISIONS

status: EMPTY

Own exact targets absent at first action; no shared/frozen input edited. Own-only review, not a corpus novelty claim. The old r2 algorithm is credited as a static architectural reference; r3 indices, whole-B determinant method, seven-exponent raw/full graph interface and new controls are the scoped change. No other lane, root readiness implementation, coefficient solve or review was duplicated. Terminal custody records own WHOLE/current postpins and the expected publication transaction; all writers idle at handoff.

Own WHOLE code/text review completed by00:25:56 UTC, including the final authority path/mode/input-pin binding. Own report WHOLE and all nineteen unchanged current postpins completed00:26:41 UTC. Exact final/manifest/custody absence, no existing marker, and own OPEN/collision checks were confirmed before sealing. A transient drafted low-scale exponent sign was corrected by hand before the complete code read; no code ran. The later ROOT fieldness advisory was not adopted as an extra input or premise. All source files are now frozen; only ordinary report transaction and terminal custody remain. Completion does not grant review, promotion or execution authority.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `12461`.
- Body SHA-256:
  `320623f8183746bb44243a574c1658a40d17083c2b1409e601d1a76286d66dbe`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
