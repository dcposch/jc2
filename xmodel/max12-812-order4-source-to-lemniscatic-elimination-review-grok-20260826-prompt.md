You are the hostile independent reviewer of the first claimed complete
elimination of the `(8,12)` exact-order-four coefficient-infinity leaf in
the plane Jacobian-conjecture campaign.  Work from the named bytes and exact
outputs, not from producer verdict language or prior summaries.

Target:
`xmodel/max12-812-order4-source-to-lemniscatic-elimination-20260826.md`

Required target SHA-256:
`e80b52c9b41136daa027809ce065ffaf69bd43ecb96999e69018f3dda5bb36cb`

The target prints every charged theorem/review/freeze/results SHA.  Rehash
them.  Inspect both full V3 AWS endpoints under
`cases/max12_812_order4_seeded_projection_v3_rawgb_20260826/`, both V2
endpoints under `cases/max12_812_order4_seeded_projection_v2_20260826/`,
their compilers/runners, and the exact prior coefficient-curve output from
which the 71-element basis was extracted.  Do not treat any PASS token as a
theorem without checking what the code actually proves.  Run no local
Singular, Sage, msolve, Lean, or substantive exact Python; source reading,
hashing, and hand algebra suffice.

Independently attack every load-bearing step:

1. Source typing and normalization.  Check that the reviewed order-four
   source has exactly the two alternatives `mu_4=0` and `mu_4!=0`, that the
   latter has no omitted lower loads, that weighted normalization to `r4=1`
   loses no geometric source, and that the corrected saturated ideal `J`
   is exactly the necessary coefficient image.  Check the `r7` saturation
   and `8 dr7/dx=j/u!=0` uses.
2. Custody and fail-closed behavior.  Rehash every V2/V3 manifest and core
   output.  Confirm V1 is unused, V2 prime is accurately labeled a near-pass,
   both V3 hosts consumed identical frozen source and emitted input, and the
   actual zero return is in each `.meta` plus `engine_rc=0` in validation
   (there is no invented `<tag>.rc`).  Audit stdout for `?`, `=FAIL`, and
   especially V2 `// ** GQ is no standard basis` warnings.  Decide whether
   V3's justified characteristic-zero `isSB` mark and zero-warning validator
   repair that issue.  Check whether the independent V2 `a6` output is only
   a control and whether any warning weakens the actual proof.
3. Raw good reduction.  Verify from emitted code that V3 compares
   `std(lead(GredRaw))` and `std(lead(std(GredRaw)))` in both directions,
   before setting the raw positive-characteristic `isSB` attribute.  Prove
   or refute that equality of those generated leading ideals, together with
   equality of the generated polynomial ideals, is exactly sufficient to
   certify `GredRaw` is a Groebner basis.  Check the subsequent marked
   reduction sentinel is meaningful and not circular.  Audit coefficient
   integrality in `Z_(32003)`, monic leading-term survival, term order/ring
   maps, and both native-ideal and native-initial-ideal comparisons.  Also
   prove that the extracted 71-element `G`, before and after nonzero scalar
   normalization, generates exactly the corrected characteristic-zero
   saturated ideal `J`; do not infer this merely from its printed size.
4. Prime lift and dimension.  Re-derive the monic standard-monomial/free
   `Z_(32003)` argument.  Check that the `minAssGTZ` output plus two-sided
   equality actually proves the native special fibre prime, not only one
   minimal component or radical support.  Check exact characteristic-zero
   dimension one is directly charged.  Verify `J` prime and `a6 notin J`
   imply `J:a6^infinity=J`, and that flat extension to `C` prevents every
   geometric component from lying in `a6=0` without requiring the V2
   saturation reductions.
5. Nonverticality.  Attack the reduction argument from a one-dimensional
   special `(a5,a6)` contraction to generic nonconstancy.  Check primitive
   content, possible vanishing of reduced relations, and whether both
   `q=a5^2/a6^3` and `v=a6^8` could be constant despite a nonconstant
   `(a5,a6)` image.  Do not silently infer source dimension from quotient
   dimension: charge `dim J=1` separately.
6. Componentwise projection.  Recheck exact characteristic-zero membership
   in `P(q,v)`, geometric integrality of the residual plane, the finite
   function-field extension, and what happens after base change when the
   source domain can split.  Prove or refute that every geometric coefficient
   component dominates the same residual plane and, after adjoining the
   actual `y=a6^2`, maps nonconstantly to the irreducible normalization of
   `P(q,y^4)=0`.  Look for a vertical component, chart loss, nilpotent,
   denominator, or quotient-vs-source loophole.
7. Source descent.  Verify a genuine source map is nonconstant from the
   terminal derivative, that its one-dimensional image closure is a full
   geometric coefficient component rather than a proper curve inside one,
   and that `q` and `y` are invariant under the exact reviewed deck action
   `a_i -> zeta^(-i)a_i`.  Check the map really descends to
   `C/deck-mu4=P1_x`, extends across poles to the complete target, and stays
   nonconstant.  Re-derive the genus-one/differential contradiction.
8. Exhaustion and firewall.  Check that combining this loaded theorem with
   the confirmed Shioda--Hall `mu_4=0` theorem exhausts exact order four for
   every `U`.  Reject any inference about order two, order one, all `(8,12)`,
   maximum twelve, or JC2.

Identify the smallest failing identity or missing hypothesis if any.  State
the strongest corrected theorem.  A semantic problem in an endpoint or a
gap in source-component coverage is blocking, not a cosmetic nit.

Write the review only to
`xmodel/max12-812-order4-source-to-lemniscatic-elimination-review-grok-20260826.md`.
Include the recomputed target SHA, exact evidence hashes, explicit treatment
of the Singular warnings and return-code custody, and end with exactly one
of `CONFIRMED`, `REPAIR`, or `REJECTED`.  Do not edit the target, V1/V2/V3
artifacts, or any top-level campaign file.
