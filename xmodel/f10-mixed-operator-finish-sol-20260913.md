# f10 mixed operator finish

Status: **UNIT IMPLEMENTED, INTERNAL/UNREVIEWED/UNEXECUTED; PREPARE REFUTED FOR PROMOTION**.

First action: `2026-09-13T00:31:02.105207200Z`. All ten charged pins matched before use. Eight current source/task bodies were read WHOLE; unchanged policy/finalizer WHOLE reads were reused after repinning.

## A. Corrected unit derivative

`science-unit.template.sh` implements only the two assigned repairs. It renders each frozen ISO-Z timer instant through `date -u -d ... '+%Y-%m-%d %H:%M:%S UTC'`, verifies equal epoch seconds, and passes only that systemd-255-compatible form to `--on-calendar`. It neither changes the source instants nor grants a late launch new time.

Post-wait handling now captures `systemctl show` status. When the exact unit remains loaded, the original terminal properties, cgroup emptiness, `reset-failed`, and final cgroup absence path remains. If show fails, the branch requires real wait exit zero, a responsive systemd manager, absence of the exact unit from `list-units --all`, empty property output, and absence of the frozen expected cgroup before recording `UNIT_ALREADY_UNLOADED_AFTER_WAIT`. It never reads blank ControlGroup as evidence. A loaded/failed unit remains on the first branch and the final wait-zero predicate still refuses it.

All service caps, layout, journal-to-late-file capture, source argv, HOME exclusion, absolute timers, census, transfer obligations, and no-retry boundary are unchanged. The accepted dispatcher SHA `079f9ad7b92e64af5c5fc6a5e17de14ffa5245627ee5c8a4ce0ef2a018a2fa5d` was not edited. The new unit is inert/unbound and requires ROOT's independent static review and physical qualification.

## B. PREPARE review

**REFUTED for promotion as written, for one concrete administrative defect.** PREPARE correctly remains placeholder-disabled; verifies EC2/root/cutoff/source pins; arms global TERM/KILL before its first native/assembly/inspection/import service; uses the stated 1 GiB/4 MiB administrative envelopes, 80%/100 ms CPU settings, exact `-I -S -B` or `-E -s -S -B` startup, explicit `--wait` status, independently queried string-only facts, and unchanged assembler/inventory bounds. It emits no dummy/science launch or qualification-success claim.

However, after each of its four `systemd-run --wait` calls, PREPARE unconditionally executes `systemctl show exact-unit.service` under `set -e`. A successful transient unit may already have unloaded, making this command fail and aborting an otherwise successful preparation—the same diagnosed loaded/unloaded condition repaired in the science unit. This is fail-closed, not false acceptance, but it is a load-bearing operational defect. PREPARE needs a separate minimal checked-unloaded derivative and different-model review; none is authored here.

Actual timer parsing, live service/cgroup controls including burst zero, output/receipt bytes, package/native fit, physical ancestry, parent fsync, durable transfer, and cutoff compliance remain ROOT runtime obligations. No worker, service, interpreter, import, dummy, science, or AWS action was executed.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `3132`.
- Body SHA-256:
  `8c3b74d8cd08cba1bc0e1a6ec0caeec6f875d03ffa5b9ba050e0ee0b976fa82a`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
