# Blind whole-portfolio ideation — `20260827T1808Z` — Grok 4.6

Sealed packet: `xmodel/ideation-20260827T1808Z-packet.md`  
Ideator: Grok 4.6 (xAI)  
Date: 2026-08-27  
Status: **BLIND SUBMISSION.**  Not a review of a peer report from this round.
Nothing here promotes a claim, launches AWS, or mutates a canonical ledger.

Verified frozen basis before any mathematical use:

```text
418e413593120d19e15e6546eb50c985f4b1f038  git HEAD
6fb539c679083c7d5de1ecba0ba2f6ac272f8fbf0e65c4d2c6ed049056482fe2  COORDINATION.md
64fda6612ef813f86701618af4886af84e8a6c6f53638c8a856dbd26527d4919  APPROACHES.md
11561125758a9cbf4afae30f2479114df96635acbc69e9444d47a2326a12ab0d  AUDIT.md
06dafa3e3095094a539742ec774225b7c0277fe6178cda3ad094a1a7d054e1b1  PROGRESS.md
eab83c1fe9b5bc8d2ee5ac62f47958d6d0372c7a53e6389dd1a9988ae1889414  notes.md
a03936b7ad4bf8694dcfc54c1156943544abe8ced0f8f6e14760f31b1e69372c  xmodel/ideation-20260827T1349Z-synthesis-sol.md
24edb9cfb606cebeb58d9c20315bedd6d050386ca29542044822a08f30c17003  xmodel/ideation-20260827T1606Z-synthesis-sol.md
a8a1ae4f888a73a7db7ea9d639088c8cdb3c480600e67104955bdd4b535ac5c8  xmodel/websweep-20260827T1348Z.md
```

Pinned delta hashes also match the packet.  Producer descendants
`6e3d9104...` and `94c10fd8...` are treated as reversible design only.

---

## 1. Disposition vector (`1..46`)

Legend: `U` unchanged, `R` raise, `L` lower, `O` reopen.  Reasons only for
non-`U`.

| # | d | reason if not unchanged |
|--:|---|---|
| 1 | R | Promoted q-gates, D5G35 compiler, and a typed lower FACEPIN contract turn the `8_28` farm from a transcription mill into two finite residual schemes.  Still not a family theorem. |
| 2 | U | |
| 3 | R | The upper operators `T_A`, `T_Q` and the lower ODE `4Kg'-3K'g=4K` are the same Euler-residue species as the strip theorem, now with exact rank and a stable cokernel.  Scope remains faces, not depth-three Ore. |
| 4 | R | V26R1F supplies a nonempty homogeneous 3-fold as the live formal remainder.  Algebraization is still unearned; the object to algebraize is now named. |
| 5 | U | |
| 6 | U | |
| 7 | U | |
| 8 | U | |
| 9 | U | |
| 10 | U | |
| 11 | U | |
| 12 | U | |
| 13 | U | |
| 14 | U | |
| 15 | U | |
| 16 | R | `T_A` is now a concrete holonomic operator on `K[X]` whose index equals the stable cokernel dimension 3.  The untried D-module lane finally has a one-line first experiment. |
| 17 | U | |
| 18 | U | |
| 19 | U | |
| 20 | U | |
| 21 | U | |
| 22 | U | |
| 23 | U | |
| 24 | U | |
| 25 | U | The branch-Q genus-3 cover is a cover of the *face polynomial*, not of a Keller generic fibre.  Do not reroute Hurwitz work onto it. |
| 26 | U | Same category trap as 25. |
| 27 | U | |
| 28 | U | |
| 29 | U | |
| 30 | U | |
| 31 | U | Matysiak `5''s` still has no primary text; the 13:49Z divisor-splitting sketch is queued, not new evidence. |
| 32 | U | |
| 33 | O | The COSTUME stop demanded a twisted, client-specific class.  The promoted `[q_n dX]` on `p^4=H` *is* that class, at face scope only.  Reopen strictly as face-character residues, not as ambient `P dQ-x dy`. |
| 34 | U | |
| 35 | U | |
| 36 | R | Two residual schemes are now finite enough to search: the 5-dimensional branch-P `F1` chart inside the affine `D22=1` target, and the six-variable K00 3-fold.  The withdrawn 127-parameter sampler stays dead. |
| 37 | U | |
| 38 | U | |
| 39 | R | Promoted exactness of `q1 dX` is a vanishing in meromorphic de Rham of an explicit cyclic cover, discharging the “no vanishing theorem attached to `J=1`” stop at face scope.  Not a motivic/A^1 program. |
| 40 | U | |
| 41 | U | |
| 42 | U | |
| 43 | U | |
| 44 | U | Closed as a Moskowicz proof; the new Matysiak SSRN is a different document and stays in the intelligence lane. |
| 45 | U | The face ODE is first-order linear and already solved; Kovacic adds nothing. |
| 46 | U | Do not enter `jc2-lean`.  Formalization remains a leaf-identity lane, not a discovery route. |

Counts: 38 unchanged, 6 raise, 0 lower, 1 reopen.

---

## 2. Reranking

### Three highest-value proof bottlenecks

1. **Affine particular solution on `D22=1` after the promoted linear cuts.**
   The 734-generator target is unsolved, but it is no longer 16-dimensional in
   `F1`.  On squarefree branch P, `D23=0` licenses `F1 in im(T_A)` and
   `D1=D2=D3=0` licenses `A^2|F1`; the promoted intersection has dimension
   **5**.  The remaining obstruction is polynomiality of the mixed particular
   solution for `R_n`, `n>=4`, on that 5-space, with `D22=1` excluding the
   homogeneous family `F=U^2`, `G=U^3`.  Decisive object: the scheme
   `D0=...=D21=0`, `D22=1`, `D23=...=D34=0` in the D3 windows, with `F1`
   restricted to a computed basis of `im(T_A) cap A^2 K[X]_(<=7)`.

2. **K00 rank purity, then later-grade compatibility of the surviving 3-fold.**
   V26R1F proves `B+I5(A)` proper of affine dimension three and `I6(A)=0`.
   Until `B+I4(A)` is decided, every full-`P6` superlocus is a one-sided
   kill screen.  Decisive object: the six-variable ideal `B+I5(A)+I4(A)`,
   then at most one nonredundant rank-five chart from the two scalar classes
   of `5 x 5` minors (V27, unrun).

3. **Typed closed-stratum source-to-receiver composition (`G2-PSC` remaining
   independent).**  Open-chart certificates (`a1^628`, `D(W)`) have killed
   inverted loci.  The residual objects are closed Fitting degeneracies.
   `G2-PSC` is still the missing GGV-packet-to-decorated-tree arrow and is
   not discharged by a face endpoint.  Decisive object: a serialized ring
   map from one frozen residual (K00 `Spec(R/(B+I5(A)))`, or a FACEPIN
   lower jet) into a named receiver, with inverted elements tracked rather
   than guessed from shared symbols.

### Two strongest falsification / counterexample attacks

1. **Family kill: LF40 with FACEPIN.**  If the 442-slot lower compiler with
   `K_rho=xi(xi-rho)^7`, target `Dtil_17=-1` and every other `Dtil_n=0`
   through 40, is the unit ideal, the raw pre-final `8_28` family is
   excluded *conditional on the GGV/Horruitiner reduction*.  Decisive
   object: the 740-ish coefficient generators named by the live review debt,
   not a row-17 prefix.  Rollback: the pin is producer-only.

2. **Witness hunt: a rational point of the K00 3-fold, then a compatible
   prolongation.**  V26R1F gives nonemptiness over an algebraic closure of
   a homogeneous 3-fold in six variables.  A `Q`-point, or a proven empty
   `P6`-lift, is a binary.  A finite jet is not an arc and not a
   characteristic-zero counterexample; the attack is the first algebraizable
   candidate, not JC2.  Parallel AS109 `n=6` content strata remain the
   other honest CE client and are not improved by this delta.

---

## 3. New avenue, new connection, history checksum

Searches ran over `AUDIT.md`, `APPROACHES.md`, `PROGRESS.md`, `notes.md`,
`ladder/REDUCTION.md`, and `xmodel/**/*.md`, with the glob
`ideation-20260827T1808Z-*` excluded from reading.  Closest hits are cited.

### 3.1 New avenue — window-stable adjoint cokernel of face-character operators

**Verdict: `NEW`.**

**Mechanism.**  For monic squarefree quartic `A`,

```text
T_A(Q) = 2 A Q' - 3 A' Q
```

has leading coefficient `(2d-12) lc(Q)` in degree `d+3`.  Resonance is only
at `d=6`.  The ODE kernel `Q^2 ~ A^3` is empty on squarefree `A`, so
`T_A: K[X]_(<=d) -> K[X]_(<=d+3)` is injective of rank `d+1` for all large
`d`.  Target dimension `d+4` forces **cokernel dimension 3, independent of
the D3 window**.  The adjoint

```text
L*(mu) = -2 A mu' - 5 A' mu = 0  =>  mu ~ A^{-5/2}
```

identifies those three functionals as residues of `F1 A^{-5/2} dX` on
`y^2=A`.  The same leading-term computation for

```text
T_Q(Q) = 4 A B Q' - (6 A' B + A B') Q
```

gives leading coefficient `(4d-20) lc(Q)` (resonance at `d=5`) and **stable
cokernel 4**.

This is not the claim `codim = dim H^1_{dR}`.  On the branch-Q cover one has
`dim H^1_{dR}=6` and algebraic count 4, already recorded in review
`56a4b940...` §1.2.  The content is the *window-stability* and the *adjoint
basis*, which turn a 16-slot fixture rank into a finite cohomological cut
that cannot be escaped by taking more `F1` coefficients.

**Desk check performed (exact `fractions.Fraction`, no Groebner):**
`A=X^4-1`, target max-degree `N=15,19,23,27` all give `coker T_A = 3`,
`ker=0`.  R5 pair `(A,B)=(X^3-(2/5)X, X^2-1)` gives `coker T_Q=4` at the
same four windows.  `H=A^2` is **not** in `im(T_A)` (augmented rank 13→14).
Intersection dimensions on these fixtures: `9`, `5`, `7`, matching
`AUDIT.md`.  The 5-dimensional space is **not** spanned by monomials
`A^2 X^j`: only `j=3,7` lie in `im(T_A)`; the other three basis elements
are mixed.

**Closest hits, not this mechanism.**

| hit | relation |
|---|---|
| Promoted rank 13/12 in the frozen 16-dimensional window (`AUDIT.md` 18:02Z; `56a4b940...`) | `KNOWN` at one window; no stability, no adjoint basis |
| Injectivity for `deg Q<=20` (`56a4b940...` §1.1) | larger *domain*, same *target*; not cokernel stability |
| Superelliptic endpoint on `w^2=H` (`d382c21f...`, `066095c4...`) | weight-22 `M(Y)=1`, not `q1` |
| FACE-EXP / Opus 13:49Z reopen of avenue 33; Opus 16:06Z raise of avenue 39 | `KNOWN` as “face de Rham vanishing exists”; not the stable Fitting/adjoint of `T_A` |
| R5 kernel schedule `H^{(12-n)/4}` | kernels of `L_n`, not cokernels of `T_*` |

**SCOPE-CONFLICT, recorded so it is not launched:** Fitting ideals of the
*truncated* `T_Q` matrix do **not** see the squarefreeness jump.  Review
`56a4b940...` §1.3 already shows `T_Q` stays rank 12 on degenerations while
extra primitives come from higher-pole ansatze.  Desk check: `A=X^2(X^2-1)`
still has `rank T_A=13`; `A=X^2(X-1)^2` and `A=X^4` drop to 12.  Discriminant
stratification belongs to the complete-pole map, not to `I_k(T_Q)`.

### 3.2 New connection — failed open-chart transport as routing onto a closed Fitting residual

**Verdict: `NEW` as a connection.**  The atoms are `KNOWN`; the routing
theorem is not.

V43C6 proves the honest V47 map `A1D2_R3` sends `a1=Az_0` to zero in a
nonzero domain, so it cannot extend from `S[a1^{-1}]` and C5 pushes to
`0=0`.  V24R6R1 empties `D(W)` because `W` already lies in the base ideal.
V26R1F leaves a closed 3-dimensional rank-`<=4` scheme.  V22R1 already left
a homogeneous dimension-three remainder after excluding `D(F10)`.

Campaign language has treated V43C6 as a *failed transport*.  Type-theoretically
it is a *routing instruction*: maps that kill inverted elements cannot consume
open-chart certificates, and are exactly the maps that can factor through the
closed Fitting residual.  The unique presently composable K00 source is
`Spec(R/(B+I5(A)))`, not another inverted minor chart.

**Closest hits.**  Staged certificate calculus (localizers create closed
residuals) — `KNOWN` as emptiness bookkeeping, not as a positive transport
channel.  V43C6 itself — `KNOWN` obstruction.  “Stop alias-based receiver
maps” (13:49Z synthesis) — `KNOWN` negative rule, not the residual-as-source
rule.

No hit for “rank-`<=4` is the only remaining candidate for typed C5/receiver
transport”.

---

## 4. Five-object interface

| pair | decision |
|---|---|
| Upper q-gates ↔ affine `D22=1` | **Connect.**  The 734-target includes `D23=0`, so `q1` is licensed there.  The homogeneous family `U=H+(V/2)t`, `F=U^2`, `G=U^3` has `E=0` identically and is `q1`-negative at `V=1`; it settles rows 1–14 and **does not** settle `D22=1`.  The producer test `A=X^4-1`, `F1=H`, `D1=...=D21=0`, `D22=1` *before* `D23` is the independence discriminator (`KNOWN` object, packet §Provisional descendants).  After `D23`, `F1=H` is excluded and `F1` lives in the 5-dimensional intersection. |
| Either upper object ↔ lower FACEPIN | **Connect operators, keep clients separate.**  Same 442-slot lattice, opposite edges of `S`, Euler adjoints `A^{-5/2}` versus `K^{-3/4}`.  The lower pin `K_rho=xi(xi-rho)^7` is GGV-reduction-conditional and family-covering; the upper 734-system is an artificial `H=X^8-1` fixture.  FACEPIN already forbids importing `H=X^8-1` into the family.  Do not import `K_rho` into D5G35. |
| Those GGV objects ↔ K00 rank filtration | **Keep separate as sources; transfer methodology.**  Dimension three on both the q-gate cokernel and the K00 3-fold is not a map (different rings).  Transfer: rank purity before superlocus, two-class compression, unit-versus-proper firewall.  V27 is the K00 instance of the same discipline Card 1 proposes for `T_*`. |
| K00 residual ↔ global landing/receiver | **Connect via §3.2.**  Open-chart certificates have done their job.  The next honest map is closed-stratum, with inverted elements tracked. |
| GGV faces ↔ `G2-PSC` / receivers | **Keep separate.**  A face landing is not packet-to-tree fidelity (`ladder/REDUCTION.md` CRITICAL 3).  `G2-BD` is a different arrow and is not invoked by these face compilers. |

No five-way fusion.  The only load-bearing new glue is: **q-gates cut the
affine endpoint after `D23`; failed inversions route K00 onto the rank-`<=4`
closed 3-fold; FACEPIN is a parallel family-kill, not a transport of the
upper fixture.**

---

## 5. Attacks to launch now

**Proof.**  Compute a basis of the 5-dimensional space
`im(T_A) cap A^2 K[X]_(<=7)` for `A=X^4-1`, substitute `F1 = sum_{i=1}^5 t_i B_i`
into the frozen D5G35 rows, and decide the resulting affine scheme
`D4=...=D21=0`, `D22=1`, `D23=...=D34=0` (with `D1=D2=D3` required to become
identities on this chart, as a negative control).  In parallel, and
*without* `D23`, run the packet’s `F1=H` slice: a point shows `q1` is new at
row 23; emptiness shows an earlier affine interaction.

Do **not** consume the cascade parametrization `6e3d9104...` as a theorem.
Use it only as a rollback-tagged particular-solution ansatz after Fable5
seals the reduced dimensions.

**Falsification.**  (i) LF40 emptiness under FACEPIN, with the eight mutations
in `94c10fd8...` §4.6 and the expected 740-generator census recorded before
algebra.  (ii) A rational point of `Spec(Q[d0_1,...,d5_1]/(B+I5(A)))`, or an
exact unit for `B+I5(A)+I4(A)` / the full `P6` rank-pure lift.  Finite jets
stay jets.

---

## 6. Software / AWS acceleration

**Name.**  Linear `F1` chart for the 734-target, plus V27 `BASE4`, without
touching live Box02 jobs.

**Exact inputs.**

- Frozen D5G35 target generators (`f0fe0f5d...` / freeze `1668795f...`).
- Operator `T_A` for `A=X^4-1`, domain `deg Q<=12`.
- The 8 generators `A^2 X^j`, `j=0..7`, from which a 5-dimensional
  intersection basis is extracted by exact kernel of the cokernel pairing
  (not by monomial axes: §3.1).
- V26 compiled matrix `A`, ideal `B`, and `I5(A)` bytes already used by
  V26R1F.

**Parallel decomposition.**

| shard | where | job |
|---|---|---|
| S0 | desk | 5-dimensional basis; residue-functional 3×16 matrix; replay `D1=D2=D3` vanish on the chart |
| S1 | AWS, new tag, not Box02 R2/R5 | six-variable `BASE4`: `B+I5(A)+I4(A)` |
| S2 | AWS, new tag | reduced 734-system on coordinates `(t_1..t_5, remaining raw slots)` |
| S3 optional | AWS, after S0 | `F1=H` independence slice, no `D23` |

Do not shard S2 by the unreviewed gcd strata of `6e3d9104...`.

**Certificate / replay.**  Serialize: the 5×16 basis matrix; the identity
`matrix(T_A) * C = matrix(B_i)` for the intersection; substitution of
`F1=sum t_i B_i` into each `D_n`; for `BASE4`, the same entrywise
`matrix(J)T=matrix(G)` plus Buchberger/independent standard-basis replay as
V26R1F.  A char-0-header `[1]` is not a unit certificate.

**Resource estimate.**  `BASE4` is the same six-variable ring in which
V26R1F finished in 2.17 s at 528 MiB RSS.  Budget 30 min / 8 GiB as a
safety multiple.  Reduced 734 is the unknown: one machine, **six-hour cap**,
≤256 GiB until a memory profile exists.  Host spare capacity ~1.9 TiB;
live Box02 R2/R5 (PIDs 366878, 371365; caps ~19:55Z, ~21:22Z) are
**not** replaced or disturbed.

**Evidence firewall.**  S2 may use only promoted D5G35 rows and promoted
linear intersections.  Cascade formulas, FACEPIN, and V27 compression 2–3
are rollback-tagged if used at all.  No family exclusion, no arc, no JC2.

**Stop rule.**  First exact unit or replayed nonempty basis of the reduced
P-fixture; or `D1..D3` fail to vanish on the 5-chart (scope mismatch — halt
and report); or six-hour cap with telemetry.  Two non-informative S2
attempts force redesign of the coordinate chart, not a larger machine.

---

## 7. Idea cards (three)

### Card A — window-stable adjoint cokernel (`NEW`)

- **Target gap.**  Is the `q1` cut a D3-window artefact, or a finite
  cohomological obstruction with a canonical residue basis?
- **Hypotheses.**  Promoted `T_A`, `T_Q`, squarefree (coprime) hypotheses.
  No cascade, no FACEPIN, no D5G35 solvability.
- **Cheapest discriminator.**  Exact rank of `T_A: K[X]_(<=N-3) -> K[X]_(<=N)`
  for `N=15,19,23,27` on `A=X^4-1`, and of `T_Q` on the R5 pair; then a
  3×16 residue matrix from `A^{-5/2}` whose kernel equals `im(T_A)`.
- **PASS.**  Cokernel constant at 3 (resp. 4); residue kernel matches the
  image.  Licenses Card C’s coordinate change at every larger window.
- **FAIL.**  Cokernel grows with `N`: the gate is truncation, and more `F1`
  slots can escape it.
- **Stop.**  Rank table plus one explicit functional basis, or the first
  mismatch.
- **Rollback.**  None.  Uses only promoted operators.  Does not touch AWS
  jobs.
- **Information gain.**  Either `q1` becomes a finite list of residue
  vanishings (proof-positive, software-positive), or the 16-slot rank is
  demoted to a fixture (strategy-changing).

Desk S0 of §6 already ran the rank half: PASS on four windows.  The residue
basis is the remaining atom.

### Card B — closed-stratum transport routing (`NEW` connection)

- **Target gap.**  After V43C6, is every C5/K00-to-receiver map dead, or
  only the inverted charts?
- **Hypotheses.**  Promoted V43C6, V24R6R1, V26R1F, V20R2 unavailability.
  No V27 result, no GGV face theorem.
- **Cheapest discriminator.**  Restrict the frozen V47 `A1D2_R3` contact map
  to the closed subscheme `a1=0` (and, separately, attempt a typed pullback
  of the six-variable K00 3-fold along any serialized source map that does
  not invert a maximal minor).  Test whether any frozen C5 row or K00
  grade-seven row becomes a nonzero identity.
- **PASS.**  A typed nonzero identity on the closed stratum: the residual is
  composable.
- **FAIL.**  Still `0=0`: this named endpoint class is dead for both open
  and closed sources; stop aliasing and move to a different receiver.
- **Stop.**  One frozen endpoint class, one serialized map, both outcomes
  banked.
- **Rollback.**  Independent of Cards A/C and of V27.  If V26R1F were
  rolled back, the K00 half dies; the `a1=0` half of C5 does not.
- **Information gain.**  Converts a promoted obstruction into either the
  first honest residual transport or a hard negative for that receiver.

### Card C — five-dimensional `F1` chart for the affine endpoint (`KNOWN` object, `NEW` coordinates)

- **Target gap.**  The 734-generator gate treats `F1` as 16-dimensional.
  Promoted intersections cut it to 5 on branch P once `D23=0` is on.
- **Hypotheses.**  Promoted `T_A`, intersection dimension 5, D5G35 compiler,
  `D23=0` inside the target.  Cascade producer **not** a hypothesis.
- **Cheapest discriminator.**  Exact 5-dimensional basis for `A=X^4-1`
  (Card A output); substitute; test `D1=D2=D3` become identities.  Then
  either (i) the `F1=H` no-`D23` slice or (ii) a short exact prefix of
  `D4..D6` on the 5-chart.
- **PASS on (i).**  A point with `D1=...=D21=0`, `D22=1`, `F1=H`: `q1` is
  genuinely new at row 23.
- **FAIL on (i) / PASS on identities.**  Prefix already forces `F1` off
  `{H}` and onto the 5-chart; proceed to the reduced 734-system.
- **FAIL on identities.**  Scope mismatch between row-three existence and
  the `q1` image; halt, do not Groebner.
- **Stop.**  Identity check plus one of: `F1=H` binary, first inconsistent
  reduced row, or six-hour cap on S2.
- **Rollback.**  If Fable5 kills the cascade, Card C is unaffected.  If the
  intersection dimension 5 were gapped, revert to the 13-dimensional
  `im(T_A)` chart, not to 16 raw slots.
- **Information gain.**  Either independence of `q1` from the affine prefix,
  or a 5-parameter affine system that is the honest remainder of the
  734-target.

Closest hit: 16:06Z allocation “freeze the branch-P `q1` certificate and
test its intersection with the prefix.”  That launch did not specify the
non-monomial 5-basis or the `D1..D3` identity control.  Label: `KNOWN`
target, `NEW` coordinate object.

---

## 8. Major-lane lifecycle

| lane | decision | rule |
|---|---|---|
| K00 | **continue** | Run V27 `BASE4` then `MAX5CLASS`.  Do not launch 1,640 charts.  Do not read a proper superlocus as survival until rank purity.  Origin/unloaded local membership stays closed. |
| GGV upper | **continue, with a coordinate redesign** | Keep D5G35 and the q-gates.  Replace 16-dim `F1` Groebner by Card C.  The `F1=H` slice precedes `D23` as the independence test.  Cascade `6e3d9104...` is rollback-tagged: live review already requires `26+c` not `24+c` on Q row five, and forbids “full scheme” language. |
| GGV lower | **continue** | FACEPIN first, then LF40 through row 40, target `Dtil_17=-1`.  Record the 740-generator census before algebra.  Pin is GGV-reduction-conditional; TeX `(1,0)/(0,1)` typo stays in the typed bridge.  No family exclusion until review seals the pin. |
| Coverage / landing | **continue, redesigned transport** | Stop new open-chart exponent ladders (already 13:49Z).  Next maps are closed-stratum (Card B).  Equality/positive-load/ramified/off-family remain coverage debt, not serial curiosity.  V47/V48 stay infrastructure. |
| TD6 / AS | **continue at registered caps** | No new evidence in this delta.  AS109 `n=6` content strata only; no W2/W3, no new floors.  TD6 stays on source-reachable certificates. |
| Formal algebraization | **continue only on named remainders** | The K00 3-fold is the live germ.  No new D-series windows, no DEPTH-STAB revival. |
| Sparse CE search | **redesign** | Search the two residual schemes of §2, not an unconditioned sampler.  The 127-parameter trials stay non-evidence. |
| External-claim audit | **continue bounded** | Matysiak SSRN 7229458 remains `ACTIONABLE-UNAUDITED / BLOCKED-BY-PRIMARY-TEXT-ACCESS`.  Acquire 7229358 then 7229458 lawfully; audit definition and the first dimension-two step; do not rerank internals on an abstract. |

No lane is `stop` that was live.  Dead generic ladders (Mathieu, untwisted
action as ambient residues, HC4 without a later observable, naive scaling)
stay stopped.  Avenue 33 is the one reopen, at face-character scope.

---

## 9. Insight a differently trained researcher is likely to miss

A fixture-rank person sees “13 in 16, so codimension 3” and treats the 16
as sacred.  A curve-cohomology person sees genus 3 (branch Q) or genus 1
(branch P: `y^2=A`) and writes `2g` or `2g+r`, overcounting: the same
review already has `dim H^1_{dR}=6` against algebraic count 4.

The algebraic-analysis move is the **leading-coefficient resonance**.

For monic `deg A=4`,

```text
T_A(X^d) = (2d-12) X^{d+3} + lower.
```

The coefficient `2d-12` vanishes only at `d=6`.  Injectivity of the ODE
(squarefree: `A^3` is not a square) therefore gives, for every `d≠6`,

```text
rank(T_A on deg <= d) = d+1,
dim(target of deg <= d+3) = d+4,
dim coker = 3.
```

The number 3 is forced by the degree law, not by the D3 census 16.  Enlarging
the `F1` window cannot create new `q1` freedom.  The adjoint `mu ~ A^{-5/2}`
then names the three functionals independently of any Groebner basis of a
16×13 matrix.

**Exact check.**  On `A=X^4-1`, the maps into `deg<=N` for `N=15,19,23,27`
already returned cokernel 3 and kernel 0 over `Q` in this session.  A
replay constructs the 3×`(N+1)` residue matrix of `{Res(X^i A^{-5/2} dX)}`
against a basis of polar classes on `y^2=A` and verifies that its kernel
equals `im(T_A)`.  Failure of that equality is a FAIL of Card A, not a
licence to treat 3 as `2g+1`.

The same leading term `4d-20` on `T_Q` forces stable cokernel 4, which is
why branch Q is strictly more expensive than P as an `F1` cut and why `q2`
(surjective in `F2`, no `F1` cut) should not be funded as a substitute.

---

## 10. Conjectural leaps, failed attempts, assumptions, checks

### Conjectural leaps (not used as evidence)

- That the three adjoint residues span the cokernel for *every* squarefree
  quartic, not just `X^4-1`.  The degree law is uniform; the residue
  identification is checked only at the mechanism level.
- That V22R1’s homogeneous 3-fold and V26R1F’s `B+I5(A)` are the same
  scheme.  Compatible, not proved here: `B` is six nonzero grade-two rows
  plus `F10`, matching the V22 remainder’s generators, and dimension stayed
  3 after adjoining `I5(A)`.
- That LF40 emptiness would be a family theorem.  Only after FACEPIN is
  promoted, and only conditional on the GGV reduction.

### Failed attempts in this session

1. **`codim = 2g` or `2g+residues`.**  Discarded: conflicts with
   `56a4b940...` §1.2 (`H^1_{dR}=6`, count 4).  Replaced by the degree-law
   argument.
2. **Fitting of truncated `T_Q` as a discriminant detector.**
   `SCOPE-CONFLICT` with the same review’s degeneration table and with the
   desk ranks (`A=X^2(X^2-1)` still rank 13).
3. **Monomial basis of the 5-dimensional intersection.**  Only
   `A^2 X^3` and `A^2 X^7` lie in `im(T_A)` among `j=0..7`.  A real basis
   requires the kernel of the cokernel pairing.

### Assumptions

- Cascade `6e3d9104...` and FACEPIN `94c10fd8...` are producer evidence
  with live review debt (Q row-five `26+c` max 29; reduced-scheme wording;
  GGV-conditional pin; 740-census).  Not consumed as theorems.
- V27 is `UNRUN / NOT EVIDENCE`.
- Box02 R2/R5 remain healthy and must not be disturbed.
- No validated external JC2 result; Matysiak is blocked by primary text.
- `jc2-lean` is a separate tree and was not entered.

### Checks actually performed

- SHA-256 of all nine frozen common-basis files: match the packet.
- SHA-256 of all seven pinned delta/producer files: match the packet.
- `git rev-parse HEAD` = `418e413593120d19e15e6546eb50c985f4b1f038`.
- Read complete 46-row inventory, current overlays, `AUDIT.md` head
  promotions for this delta, `notes.md` 18:08Z LIVE STATE, both assigned
  syntheses, websweep, V26R1F statement, V27 design, both producer
  descendants, and the charged q-gate review.
- Exact rank tables for `T_A` and `T_Q` at four window sizes; `H` not in
  `im(T_A)`; intersection dimensions 9/5/7; monomial non-basis; three
  repeated-root ranks.
- Repository history/priority grep for every claimed new target, mechanism,
  and synonym, excluding round-peer ideation files from reading.
- No AWS, no heavy local algebra, no canonical-ledger edit, no
  `jc2-lean` access, no peer submission from this round opened.

---

## Blindness and custody

This report is the only file written.  I did not read
`xmodel/ideation-20260827T1808Z-fable5-prompt.md`,
`xmodel/ideation-20260827T1808Z-grok-prompt.md`,
`xmodel/ideation-20260827T1808Z-opus5-prompt.md`, or any other
`ideation-20260827T1808Z-*` file except the sealed packet.  At search time
no peer *submission* for this round existed.  Novelty greps necessarily
matched *prior-round* ideator reports; those matching lines were used only
as closest-hit adjudication (Opus 13:49Z avenue-33 reopen; Opus 16:06Z
avenue-39 raise; 16:06Z q1-intersection allocation).  Their bodies were not
opened.  Ideas in §3 were derived from the promoted operators, the V26/V43
type distinction, and the desk ranks, not from those hits.
