# Independent research charge: cutoff two = full fixed branch-P endpoint fixture

You are Opus 5, an equal-standing independent mathematical researcher in the
plane Jacobian-conjecture campaign.  Work in `/Users/dc/code/math/jc2`.

## Objective

Attack the fixed branch-P upper endpoint system at **cutoff two**.  The raw
variables have minimum weight two, so this is the complete frozen 303-variable,
513-generator fixture, not a smaller square-tail specialization.  It retains
all seven `z_*` weight-two and ten `tt_*` weight-three coordinates and every
later raw slot.  The charged equations are the literal coefficients of
`D4=...=D21=0, D22=1`, with no `D23` and no `G22` slot.

The authoritative source is
`cases/ggv_8_28_upper_endpoint_branch_p_20260827/RAW_DIRECT_SYSTEM.json`
(SHA-256 `ead2fa404a741c5bc55934de6a1651d417701422f4b5311e3de7e3dccca0e5a0`),
whose source inputs are pinned in its manifest/preregistration.  Read the
branch-P preregistration and relevant reviewed q-gate/cascade reports.  You
may inspect the newly frozen tail-5 packet
`cases/ggv_8_28_upper_endpoint_tail5_desk_20260828/` as a source of patterns,
but treat it as provisional pending hostile review and rederive anything you
use.  Tail-7 and tail-6 are reviewed only at their stated specialized scopes.

## Research question

Find the cheapest exact route to either:

1. exclude all characteristic-zero field points of this full fixed fixture,
   with an exhaustive branch cover and literal source-row certificates; or
2. construct a complete exact rational/algebraic endpoint point and replay
   every literal raw row.

A complete result is ideal, but a rigorously typed reduction to a much
smaller exact subsystem is valuable.  Seek structural reductions that avoid
a direct Groebner basis: progressive compatibility spans, the reviewed
branch-P `q1/q2` geometry, radical divisibility over `A=X^4-1`, additive
gauges, invariant-core projection, resultants, local root evaluation, or a
uniform theorem explaining the cutoff-7/6/5 cascade.  Explicitly determine
whether the cutoff-5 two-branch mechanism survives, splits, or fails when
`z_*` and `tt_*` are restored.

## Required discipline

* Reconstruct from literal source rows; do not infer the full fixture from a
  specialized unit.
* Never normalize or invert an endpoint carrier without an explicit cover.
* Separate field-radical implications from scheme identities, and record
  every localization/characteristic assumption.
* A projected point, modular point, proper ideal, numerical solve, timeout,
  or partial basis is not an endpoint witness.
* Prefer exact standard-library rational/polynomial calculations.  Do not
  run heavy local CAS.  Do not launch AWS; instead specify any immutable
  AWS target, algorithm/order, resource estimate, and exact stop markers.
* Do not enter, list, search, read, build, status, or modify `jc2-lean`.
* Do not edit canonical top-level ledgers or existing case packets.

## Output

Write one report only:

`xmodel/ggv-upper-endpoint-tail2-fullfixture-ideation-opus5-20260828.md`

Use `/tmp` for scratch.  Include exact identities or independently checkable
pseudocode, a ranked next-experiment plan, mutation/falsifier design, all
scope firewalls, and a clear status among `EXACT RESULT`, `REDUCTION`, or
`NO ADVANCE`.  If you discover a decisive certificate, serialize its full
mathematical content in the report and give a literal replay recipe; do not
claim promotion before different-model review.
