# Blind full-spectrum ideation — cube/max12 owner — `20260825T0948Z`

Status: **BLIND / PRODUCER IDEATION ONLY / NO PROMOTION**.  I read the sealed
state packet `xmodel/ideation-20260825T0948Z-state.md` at SHA-256
`73cb3cc174d2f0d1cc3bd4af0150a0e83cc05d705372a624d159bffe5f96265b`,
the complete current overlay and all 46 numbered rows of `APPROACHES.md`, and
the relevant current Q8/AS/TD6 audit entries.  I did not read another
`0948Z` submission.  The positive-genus producer is exact but was still in
hostile review at this cutoff, so it is consumed here only provisionally.

## 1. Compact disposition vector over all 46 avenues

Legend: `C` = continue a named bounded client; `R` = redesign before more
work; `H` = hold/reserve; `S` = stop as a live route.  A qualifier after `/`
is the only licensed use.

```text
 1 R/source-regression   2 C/backbone          3 H/named-row-only
 4 C/proper-specialize   5 H/cusp-control      6 S/wrong-object
 7 R/actual-A(F)-only    8 H                   9 H
10 S/no-new-observable  11 S                  12 S
13 H                    14 H/source-audit     15 H
16 H/no-index-yet       17 S                  18 S/control-only
19 C/source-complete    20 S/inside-19-only   21 R/finite-AS-client
22 S                    23 S                  24 S/control-only
25 R/source-coupled     26 C/Q8-client        27 H
28 H/completed-tails    29 H/reuse-Fitting    30 S
31 C/properness-wall    32 R/bounded-family   33 S/costume
34 H                    35 S                  36 C/frozen-grammar
37 R/certificate-only   38 H/saturated-only   39 R/concrete-genus
40 S                    41 S                  42 S
43 H                    44 S/headline         45 H/named-ODE-only
46 C/certification-only
```

The event changes four entries materially.  Row 39 is no longer merely a
cohomological slogan: ordinary normalization genus is now a computable,
source-attached obstruction.  Row 37 is useful only as exact arithmetic on a
pinned curve, not as a census of Keller maps.  Rows 25--26 work in Q8 because
the passport and Galois action are coupled to full source contacts; this does
not revive raw passport enumeration or a global sheet bound.  Row 31 becomes
the remaining Q8 proof obligation: make the local arithmetic attachment live
on a proper model without replacing the source component by its plane image.

## 2. Bottleneck rerank after the positive-genus event

### Proof side

1. **Selected-Q8 proper specialization and component identity.**  The new
   exact count gives `g(H)>0` for the geometrically integral mod-127 plane
   curve.  The shortest proof path is now composition, not further generic
   Groebner reconstruction: attach the existential rational full contact to
   its unique arithmetic source branch, pass to proper normalizations, and
   show that the special source component dominates `H`.  A positive-genus
   quotient already forces positive genus upstairs.  The all-eight contact
   alternative was reviewed away; in the singleton alternative Galois
   conjugacy propagates the genus obstruction to all eight components.
2. **TD6 denominator-free all-beta coverage.**  V54's first nonunit pivot
   shows why quotient-field identities are insufficient.  V55/V56 must
   produce a proof-carrying disjoint union of all localization and boundary
   strata before the fixed-`A3` theorem can move.  This is the best remaining
   independent finite proof gate.
3. **Global landing/coverage and a cofinal ceiling.**  Even a selected-Q8
   exclusion and an arbitrary-center TD6 exclusion remain finite frontiers.
   `G2-PSC`, full configuration landing, and an absolute/cofinal complexity
   bound are still the campaign-wide wall.

### Counterexample/falsification side

1. **AS source-complete accepted-fibre restoration.**  The three displayed
   high-band states were not complete maps; the next honest rows kill the
   displayed 303/513 states and give a left-null obstruction at the displayed
   519 state.  The live Boolector/Z3 calculation over the *whole accepted
   fibre* is the decisive falsifier.  More digit breadth before this closes
   is negative value.
2. **Formal flatness plus collision-open persistence for any global AS
   survivor.**  A SAT point is only a point.  A surviving component needs the
   complete source ideal, a Henselian/characteristic-zero lift, bounded
   support, and a collision localizer that stays a unit.
3. **Source/compiler falsification across all frontiers.**  The Q12/Q8
   correction, AS omitted rows, and TD6 denominator ancestry share the same
   failure mode: a true projected equation is mistaken for the original
   source scheme.  Literal source replay is therefore a mathematical
   discriminator, not just software hygiene.

## 3. New mechanism and cross-connection

### New mechanism: extension-field smooth-point genus oracle

For a pinned geometrically integral curve `C_0/F_q`, exact enumeration of
**smooth affine** `F_(q^r)` points can certify positive normalization genus
without normalization, a discriminant radical, or a coordinate graph.  If a
smooth rational point is known and the smooth affine count exceeds
`q^r+1`, genus zero is impossible because the smooth projective
normalization would be `P1`.  The Q8 producer realizes this at `q=127,r=2`:
`16168>16130`.

This is not row 37's finite-field Keller-map census.  It is a theorem-driven
oracle on one reviewed integral elimination curve, with exact singular
filtering, independent partitions, and a characteristic-zero specialization
firewall.  It should become the first genus triage for every explicit curve
emitted by Q8, TD6 exceptional strata, or an AS local component; only an
inconclusive count proceeds to normalization or branch-value elimination.

### New cross-connection: one arithmetic contact can obstruct a whole
primitive orbit

The corrected rational-contact theorem is existential: at least one of
`v=26,58,67` lies on an `H`-supported mod-127 source component.  That was too
weak for an all-contact graph theorem, but positive genus changes the logic.
The reviewed arithmetic completion attaches the corresponding
characteristic-zero contact branch to the same full source point.  Once a
proper-model specialization theorem shows that branch's normalization has a
special component dominating `H`, its genus is positive.  Primitivity then
leaves one eight-contact component or eight Galois-conjugate singleton
components.  The first is already excluded by infinity; in the second case
one positive-genus singleton makes all conjugates positive genus.  Thus an
existential finite-field contact can obstruct every selected primitive
alternative without identifying which of the three rational contacts lies
on `H`, reconstructing seven rational coordinate functions, or proving a
degree-one global graph.

The charged step is the proper source-component specialization, not the
plane-root statement.  One must prove: scheme-theoretic closure in a proper
model; the full-source regular `R[[w]]` contact; dominance/separability onto
`H`; and the residue-genus inequality.  Projected roots alone do not supply
any of these arrows.

## 4. Three detailed cards

### Card A — Q8 selected-contact positive-genus composition

- **Claim targeted:** no registered actual order-three trajectory can use a
  selected corrected-Q8 contact, conditional only on the reviewed
  cube/Faber landing and selected-branch setup already charged by this lane.
- **Exact dependencies:** reviewed mod-127 source component
  `7d28a7b4...`; reviewed geometric integrality `b3cddbce...` /
  `f2e24aa5...`; corrected rational-contact theorem `c470fd25...` /
  `65834195...`; point-count producer `c4c4ebdf...` with manifest/freeze
  `8de0e649...` / `7f134d21...` and pending hostile review; arithmetic
  bridge `39ae6229...` / `571221ad...`; corrected no-merger lemma
  `ed7bf01a...` / `cc722b52...`; positive-genus specialization audit
  `53830f8b...` / `4842ff19...`; and infinity/primitivity producer/review
  `89691023...`, `9f37fc3f...`, `c77a7330...`.
- **Cheapest discriminator:** a text-and-local-algebra hostile audit of the
  one missing arrow.  At the winning rational contact, construct the proper
  closure of the characteristic-zero selected source component over a
  complete unramified splitting DVR; use the full-source `R[[w]]` completion
  to identify its unique special component; and prove that component maps
  dominantly and separably to `H`.  Then apply the genus inequality on proper
  normalizations.
- **Outcome interpretation:** if the arrow holds, all-eight is already dead
  and all singleton conjugates have positive genus, so a nonconstant `P1`
  trajectory is impossible.  If the closure acquires a different special
  component, is not dominant, or the source presentation changes under
  properization, the composition fails; retain only the standalone mod-127
  genus theorem and resume source-graph/no-merger work.  If hostile review
  rejects the point count, quarantine every descendant immediately.
- **Stop condition:** stop Q8 generic-coordinate Padé and generic-length
  races once this exact selected-contact theorem is independently confirmed;
  otherwise stop the composition at the first missing source/properness
  hypothesis rather than patching it with plane projection data.
- **Information gain / cost:** very high / low-to-medium.  Most inputs are
  reviewed and immutable; the remaining work is a narrow algebraic-geometry
  composition, not a large CAS search.
- **Firewall:** this excludes only the selected-Q8 order-three trajectory
  leaf.  It does not close other `nu!=0` components, polynomial core,
  `(8,12)`, all `(9,12)`, maximum twelve, arbitrary Keller pairs, or JC2.

### Card B — AS complete accepted-fibre source restoration

- **Claim targeted:** decide whether any state in the accepted high-band
  fibre survives the first omitted chronological source rows, rather than
  sampling the three displayed points.
- **Exact dependencies:** frozen accepted-fibre Q5/H6 formula and source
  closure (`FREEZE_SOURCE 440c0ab...`, closure `056a110...`); literal divided
  carries; all 69 retained higher rows; chronological degree-six `H6,J6` and
  degree-five source rows; fixed support/degree grammar; and the collision
  open.
- **Cheapest discriminator:** require the independent Boolector and Z3 jobs
  to agree on SAT/UNSAT for the same source-hashed formula.  For UNSAT, demand
  a replayed proof/certificate or independently checked left-null/Fitting
  witness.  For SAT, replay every original row and immediately compute the
  next source-complete cotangent/Kuranishi obstruction at the returned point.
- **Outcome interpretation:** global UNSAT kills this accepted fibre and
  identifies the restoration layer as the obstruction; it says nothing
  about other endpoint families.  SAT supplies a candidate local source
  point, not a lift or counterexample.  Solver disagreement, missing rows, or
  a failing integer replay is a compiler defect and invalidates the run.
- **Stop condition:** stop breadth search if the global fibre is UNSAT; if it
  is SAT, stop finite-digit accumulation until the complete local source
  ring, flatness, bounded support, and collision-unit questions are answered.
- **Information gain / cost:** very high / medium.  It is already running on
  AWS and separates a genuine counterexample-side branch from three
  seductive projected survivors.
- **Firewall:** neither finite SAT nor formal smoothness alone supplies a
  characteristic-zero polynomial Keller collision.

### Card C — TD6 proof-carrying localization union

- **Claim targeted:** determine whether the fixed-`A3` support-one identity
  extends over every beta/center boundary needed for the registered TD6
  family.
- **Exact dependencies:** both V50 replays of all 54 original rows; reverse
  pivot V52; repaired V54b raw-denominator reporting; V55/V56 factor shards;
  the reviewed fixed-`A3` empty theorem; exact localizers and all coordinate
  boundaries.
- **Cheapest discriminator:** assemble a machine-checked disjoint union of
  the V55/V56 strata, reduce every original row on every chart, and produce
  either a denominator-free identity or cleared identities plus an explicit
  Bezout glue certificate.  One uncovered prime or nonunit divisor is the
  cheapest falsifier.
- **Outcome interpretation:** a complete union with unit coefficient forces
  the registered beta parameter to zero and composes with fixed-`A3`
  emptiness at that exact scope.  A nonunit factor creates a new valuative
  boundary stratum and becomes the sole successor.  A reporter or ancestry
  mismatch is software failure, not mathematical evidence.
- **Stop condition:** after one complete failed union, stop affine pivot-order
  proliferation and normalize only the explicit surviving divisor.  After a
  complete passing union, stop the fixed-center lane and return to landing.
- **Information gain / cost:** high / medium; lower immediate upside than
  Card A, but independent of the maximum-twelve machinery.
- **Firewall:** no fixed-section result implies full TD6, SP-2, arbitrary
  boundary/dead-stretch moduli, a sheet ceiling, or JC2.

## 5. Strongest attacks, software acceleration, and allocation

**Strongest proof attack:** Card A.  It exploits the rank-changing fact that
positive genus needs neither a degree-one coordinate graph nor identification
of all eight mod-127 contacts.  The adversarial question is whether the
special source component dominating `H` is genuinely the specialization of
the selected characteristic-zero component in a proper model.

**Strongest counterexample/falsification attack:** Card B.  It tests the whole
restored accepted fibre at the first honest missing source layer.  A SAT
outcome earns only a local deformation calculation; an UNSAT outcome is a
real finite-family exclusion.

**Software accelerator:** build a content-addressed **curve-obstruction
broker**.  Input is a pinned finite-field plane curve plus its source map and
integrality review.  The broker first enumerates smooth points over small
extensions in independently partitioned AWS shards and compares with
`q^r+1`; only if inconclusive does it invoke discriminant or normalization.
It then emits full-source contact Jacobians, denominator units, proper-model
dependency manifests, and an explicit `standalone curve -> source component
-> characteristic-zero branch` claim DAG.  This packages the successful Q8
method for future TD6 and AS curves while preventing finite-field evidence
from silently becoming a characteristic-zero claim.

**Speed-optimized allocation:** until the point-count review and Card A audit
close, use `50% Q8 / 30% AS / 15% TD6 / 5% global landing-ceiling reserve`.
If Card A confirms, cut Q8 immediately to maintenance and move its share to
AS and the global reserve; more graph reconstruction then has little value.
If Card A fails on a precise proper-model hypothesis, retain only a narrow Q8
repair shard and restore TD6 to 25%.  Do not starve the global reserve:
success in all three finite lanes still leaves landing and cofinality open.

Recommendations: **Q8 continue narrowly** through the composition audit,
then stop or sharply redesign; **AS continue** the source-complete global
fibre and stop point sampling; **TD6 continue** V55/V56 but redesign around
the first honest nonunit divisor if the union fails; **global reserve
continue** on source/transport and ceiling, with no new corner farm.

## 6. History/dedup and adversarial accounting

- Card A is not the earlier generic graph/no-merger program.  Its new input
  is the exact extension-field point excess, and its payoff is a
  positive-genus quotient obstruction from one existential arithmetic
  contact.  It deliberately bypasses seven coordinate reconstructions and
  degree-one uniqueness.
- The extension-field oracle is not a finite-field Keller-map census, a
  Hasse--Weil heuristic, or Newton-interior evidence.  It counts every smooth
  affine point exactly and uses only the genus-zero point count after
  geometric integrality and a rational smooth point are proved.
- Card B is the redesigned successor of the prior AS local-deformation card:
  the new restoration news lowers its first gate to global SAT/UNSAT of the
  source-complete accepted fibre.  The three displayed points receive no
  survivor credit.
- Card C is the existing TD6 denominator-honesty program, retained rather
  than claimed as novel.  V54's `U^4 H` denominator is evidence against any
  shortcut from quotient fields.
- The shared hidden assumption is projection fidelity.  Q8 can confuse `H`
  with the source component, AS can omit chronological rows, and TD6 can
  discard pivot divisors.  Every acceptance gate therefore carries the same
  original source equations, localizer, and proper/boundary model through the
  entire implication.
- I assign no novelty or independent-consensus credit to the ongoing review
  outcome or to any post-cutoff report.  No ideation statement here is a
  theorem.
