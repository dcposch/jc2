# GGV upper endpoint, lambda-zero fixed-even face: generic quadratic-Q symbolic exclusion

Date: 2026-08-28

Verdict: **PASS as a generic fixed-face theorem; NOT a universal arbitrary-Q theorem.**

## Scope and hypotheses

Work on the reviewed `c2 != 0`, exact-`D=0`, `lambda=0` branch, normalized to
`c2=1`, with

```
A = X^4 - 1,    S = U = 0,
Q = q0 + q1 X + q2 X^2,
F5 = 0, F6 = 0, F8 = 0,
```

and with the remaining even tail fixed to zero.  The odd raw coefficients have
the authoritative floors/windows

```
F7:  degrees 0..5,
F9:  degrees 1..7,
F11: degrees 1..5,
F13: degrees 2..3,
F15 = 0.
```

The five odd primitive gates `q7,q9,q11,q13,q15` are imposed exactly, together
with polynomiality and every literal raw window coefficient for the full
characteristic receivers `G9,G11,G13,G15`.  The characteristic modes retained
are `c4,c6,c8`; `c10` cannot enter by weight 15 because `F5=0`.

This packet does **not** impose `G16..G21`, the four-root endpoint system, or
released `F5,F6,F8`/even-tail parameters.  It therefore excludes a fixed even
face, not the whole lambda-zero locus.

## Exact construction

`compile_symbolic_quadratic_q.py` independently reconstructs the odd gates and
the Laurent characteristic.  It pins the reviewed endpoint compiler and the
independent q15 checker.  Clearing the common powers of `A` in each receiver
gives 106 homogeneous linear equations in 105 odd-source, primitive, and
receiver variables.  No polynomiality condition is selected after seeing a
result: all coefficients of the cleared Laurent numerator and every lower and
upper raw-window violation are included.

The raw target on this branch is

```
E = F11[X^1] G11[X^0] - F7[X^0] G15[X^1]
  = f11_1*g11_0 + f_0*g15_1,
```

because the compiler's odd primitive parameterization has `F7[X^0]=-f_0`.

## Exact generic theorem

Let

```
k = Q(q0,q1,q2,c4,c6,c8).
```

Over `k`, exact Gaussian elimination of the 106-by-105 receiver matrix has
column rank 105.  Its row ideal is literally the ideal generated by all 105
receiver variables.  Therefore the odd fiber is the origin over `k`; in
particular `E=0`.

Equivalently, there is a nonempty Zariski-open subset of the six-dimensional
parameter space on which this fixed even prefix cannot meet the target `E=1`.
This is stronger than the earlier finite grid on generic points, but it says
nothing by itself on the maximal-minor rank-drop variety.

AWS exact transcript:

```
VARIABLES=105 EQUATIONS=106
GAUSS_SECONDS=1
ROW_RANK=105
ENDPOINT_NORMAL_FORM=0
ENDPOINT_IDEAL_MEMBER=1
```

The generated Singular input has SHA256
`100b4c12bfca51f133c36cf5f17ee4a8088feb75a8165d1074fa59dc9caeecbc`.
The frozen stdout has SHA256
`965024b915610c9b96787faa6b91215a28f7a7496b22b306ceea6cc41ca3e27c`.

## Exceptional-locus navigation, not promoted

Coefficient-field specializations found ranks

```
c8=0:       104
q1=0:       101
q0=q2=0:    104
q0=q1=0:    101
q1=q2=0:     99
```

and reduced the endpoint to zero at the generic point of each listed stratum.
The `q1=0` elimination introduces the denominator

```
(6144*c6 - 5*(q0+q2)^2) * (6144*c6 - 5*(q0-q2)^2).
```

Those calculations are valuable routing data but are not a closed
specialization proof: Gaussian pivots invert parameter polynomials.  A
maximal-minor computation was left running on AWS to identify the rank-drop
variety.  Until that variety and its deeper strata are covered, the conclusion
must remain **generic**, not universal.  Independently, the frozen 875-point
quadratic-Q/mode grid gave endpoint zero at every point while survivor
dimensions ranged from 0 to 6; that remains sampled evidence only.

## Reproduction and mutation controls

Light custody/shape check:

```
python3 cases/ggv_8_28_upper_endpoint_deep_q1_lambda0_quadratic_q_symbolic_20260828/verify_symbolic_quadratic_q_generic.py
```

The checker pins both upstream sources, the compiler, generated Singular
matrix, equation-name ledger, AWS metadata/stdout, and complete 105-generator
row ideal.  Its `source-hash`, `rank`, and `row-ideal` mutations all fail.
Heavy regeneration and Gaussian elimination belong on AWS.

## Clean next discriminator

Compute and decompose the 105-by-105 maximal-minor ideal of the 106-by-105
matrix.  On each irreducible rank-drop component, compute the odd kernel and
test the endpoint quadratic in the symmetric-square quotient.  Only after
those components are closed should `F6/F8` and the remaining even tail be
released.  This avoids confusing a generic full-rank exclusion with a theorem
on all quadratic `Q`.
