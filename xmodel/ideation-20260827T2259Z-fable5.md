# Blind full-spectrum ideation — round `20260827T2259Z` — lane `fable5`

Producer: **Fable 5** (Anthropic), exact model ID `claude-fable-5`, acting as
equal-standing blind whole-portfolio researcher.
Date: 2026-08-27.
Report path: `xmodel/ideation-20260827T2259Z-fable5.md` (the only file
written this session; SHA-256 printed to stdout at seal, not embedded).

## 0. Custody, blindness, and execution disclosure

Fail-closed gates passed before any content was read:

- `xmodel/ideation-20260827T2259Z-packet.md` recomputed
  `8674f511a6a88801818c7ffda5f1fdfa52ac57e871e72757234a5d0a28291240` — match.
- Every custody line in packet section 1 recomputed and matched byte-for-byte:
  `APPROACHES.md f9ed44df…`, `AUDIT.md aee767b1…`, `COORDINATION.md 743d684b…`,
  `PROGRESS.md 0d22e397…`, `notes.md 654c358e…`,
  `…2137Z-synthesis-sol.md 668d78c9…`,
  `…2137Z-opus5-crossreview-fable5.md b3f3886c…`,
  `…2137Z-fable-linear-vacuity-hostile-review-sol-ultra.md 5beb5550…`,
  `…2137Z-postseal-exact-hostile-review.md 8bad7ca0…`,
  `…2255Z-packet.md d8a01725…`.
- `git rev-parse HEAD` returned `418e413593120d19e15e6546eb50c985f4b1f038` —
  match.  Cleanliness was not inferred; the dirty tree was used only through
  the hashed artifacts above.

Appendix sections 1–4 of the `2255Z` packet were read in full and are followed
here with round identifier `20260827T2259Z` and this output path; its section
0 hash checks were not applied, per the relaunch instruction.

Blindness and boundary compliance: no `2259Z` peer response was read, listed,
globbed, or searched; I did not list `xmodel/` at any point and my only
repository searches were `Grep` calls restricted by explicit glob to the five
canonical Markdown files (history checks for §3/§5; patterns: monodromy-near-
gate, `Picard–Fuchs|Risch|invariant subspace|flat section|creative-telescop`,
and `regular holonomic|six operations|irregularity` in `AUDIT.md`).
`jc2-lean` was never entered, listed, searched, read, built, statused, or
modified.  No AWS or live-lane contact of any kind; no heavy local algebra
(no CAS, no solver, no elimination was run — this submission is hand algebra
plus reading); no canonical file was edited; no external communication.
Files read in full: the two packets, all four hashed `2137Z` xmodel reports,
`COORDINATION.md`.  Files read in part (targeted, after hash verification):
`APPROACHES.md` (overlays through 22:40Z, the complete 46-row master table,
unique-finds/consensus/shortlist sections), `notes.md` (LIVE STATE 22:40Z
through end of file), `PROGRESS.md` (2026-08-27 entry), `AUDIT.md` (head
corrections through the R7R2/G2 blocks, lines 1–224).

Contamination disclosure: my harness auto-loads a persistent memory index
summarizing my own prior reviewed campaign work (including my `2137Z`
cross-review that produced T3/E0, and the R7R1/R7R2/torsor reviews I
performed).  These are my own prior-session artifacts; every load-bearing
fact used below was re-verified this session against the hashed canonical
files, and divergences would have been reported.  The harness-provided git
status snapshot at session start displayed workspace metadata (branch, a
modified `jc2-lean` gitlink line, and untracked `cases/…` directory names);
I did not act on it and it contains no peer round content.  One further
hedge: my session context included the instruction packet chain only; no
web access was used.

Everything below is allocation/ideation input.  Nothing here is a face
exclusion, endpoint decision, family kill, Keller-pair statement, or JC2
result.

## 1. Disposition vector over avenues 1–46

Baseline = the 22:40Z overlay plus the now-canonical E0 correction (both in
the custody set).  `unchanged` means unchanged **relative to that baseline**,
including its already-applied raises/stops.

| # | Disposition | # | Disposition | # | Disposition |
|--:|---|--:|---|--:|---|
| 1 | unchanged | 17 | unchanged | 33 | unchanged |
| 2 | unchanged | 18 | unchanged | 34 | unchanged |
| 3 | **raise** | 19 | unchanged | 35 | unchanged |
| 4 | unchanged | 20 | unchanged | 36 | unchanged |
| 5 | unchanged | 21 | unchanged | 37 | unchanged |
| 6 | unchanged | 22 | unchanged | 38 | unchanged |
| 7 | unchanged | 23 | unchanged | 39 | unchanged |
| 8 | unchanged | 24 | unchanged | 40 | unchanged |
| 9 | unchanged | 25 | **reopen (narrow)** | 41 | unchanged |
| 10 | unchanged | 26 | unchanged | 42 | unchanged |
| 11 | unchanged | 27 | unchanged | 43 | unchanged |
| 12 | unchanged | 28 | unchanged | 44 | unchanged |
| 13 | unchanged | 29 | unchanged | 45 | **reopen (narrow)** |
| 14 | unchanged | 30 | unchanged | 46 | unchanged |
| 15 | unchanged | 31 | unchanged | | |
| 16 | **raise** | 32 | unchanged | | |

Reasons for the four changes (all four are one coherent consequence of E0
plus the promoted R7R2 regularity block; details in §7):

- **3 raise.**  E0 (canonical) removed the only *linear* gate instrument
  above row 27.  The surviving exact instrument class on the upper tower is
  precisely avenue 3's mechanism — residue/period functionals — now with a
  concrete carrier: the four `mu4`-isotypic period sections of the explicit
  gate family (§7).  The marginal value of the residue-functional program
  therefore rose from "depth-extension idea" to "only live gate instrument."
- **16 raise.**  Avenue 16 (D-module/holonomic index) was "untried, exotic,
  no concrete invariant named."  It now has a named concrete object: the
  regular holonomic direct image `N_j` of §7, whose qualitative
  holonomy/regularity is *already discharged by promoted material* (AUDIT
  21:30Z R7R2 block: cyclic modules of `P,W` on the `(X,s)` surface are
  regular holonomic, HTT 6.1.5, and algebraic nonproper direct image
  preserves regularity).  The rank-one index law already lived here; the
  carrier completes it into a mechanism.
- **25 reopen, narrow.**  `FACE-CHAR` failed because face local system and
  Keller passport live over different bases with no supplied map.  The
  repair is not a better bridge but a different base: the *gate family
  itself*, `y^8 = F(sy)` over its own `z = s^4` line, is an explicit,
  campaign-owned family of plane curves to which avenue 25's branch-cycle
  and monodromy tools apply with zero base-typing debt (§3, Card B).  No
  Keller-passport identification is claimed or needed.
- **45 reopen, narrow.**  Grok's original dismissal ("the inverse PDE is
  tautological") was correct for the client proposed then.  The new client
  is different: differential-Galois/Kovacic-style invariant analysis of the
  *gate family's Picard–Fuchs operators* `L_j`, where the tower-survivor
  condition is literally "flat section," i.e., a monodromy/Galois-invariant
  vector (§3).  Same narrow scope as the 25 reopen; the two share one card.

Explicitly retained without change: all closures and stops in the baseline
(11, 18, 44 closed; `FACE-NC`, `FACE-CHAR`-as-passport-bridge, `EXIT-DIFF`,
`C74-PLACE`, `EXIT-RPMC(C)`, LF40 `29 versus 36`, `GATE-MARCH`, ambient
sampling all stopped); 1/2/7/19/26/36 at their 22:40Z allocations; the
rejected raw `D28` `0 versus 4` test stays rejected as untyped.

## 2. Bottleneck rerank

Highest-value proof bottlenecks, in order:

1. **Cofinal total-degree / universal landing theorem** (avenues 2/26/31).
   Unchanged as the global wall: every proof architecture terminates here,
   and no new mechanism this round touches it.  Kept #1 because it blocks
   *every* route, not one face.
2. **Finite control of the nonlinear gate tower** (avenues 1/3/16) — raised
   to co-critical.  Post-E0 the upper tower has *no* decision instrument
   beyond row 27: linear content is finished (27/37 forever), and "more
   rows" is not an instrument.  The carrier of §7 is the typed candidate;
   its qualitative half is already promoted material, so the open gap is
   effectivity (order/indicial bounds), which is unusually well-posed for a
   bottleneck of this rank.  It is also the model for every other
   infinite-tower face the farm will produce.
3. **L3–L5 / `RPMC(C)` typed transport** (avenue 2 backbone).  Unchanged
   content: exact-pair two-chart construction owes truncation (L3) and
   deck/leaf-place fidelity (L5), then `RPMC(C)` and the ceiling.  Ranked
   below 2 this round only because it has an owner and a stable plan, while
   bottleneck 2 just lost its assumed instrument and needs redesign now.

Two strongest counterexample/falsification attacks:

1. **K00 full-`P6` rank-exact source-open nonzero point** (avenue 19; live
   on r6a).  Unchanged strongest finite counterexample lane: a nonzero,
   projective, source-compatible, rank-exact point plus compatible
   prolongation would be the first genuine survivor object.
2. **E0-schedule survivor march into the genuine endpoint.**  Construct an
   explicit `F*` through rows 28–40 using the corrected licensed schedule
   (rows 23–27 linear, then only the small nonlinear carry conditions), then
   spend it against the inhomogeneous `D22 = 1` object, where death actually
   lives.  E0 makes this *cheaper than previously priced*: at the `k`-rows
   from 28 on there is nothing linear to satisfy.  If the nonlinear carry
   residues also vanish generically (cheap test: residues of
   `[NL(F_1..F_5)]` at random rational prefixes), the tower cuts nothing
   and the whole face question collapses onto the endpoint — which is where
   a falsification pipeline wants it.

## 3. Genuinely new mechanism and new connection (history-checked)

**New mechanism — flat-section/monodromy reading of tower survival.**  For a
fixed window point `F`, the licensed class tower is equivalent (over `C`;
spreading out over `K(F)` is a stated obligation, §7) to: the period vector
`Phi(z)` of the form `y^2 dX` on the gate family is **constant in `z`**,
i.e., the class section is a **flat section of the Gauss–Manin local
system** of the explicit family `y^8 = F(sy)` over its `z = s^4` line.
Hence: tower survivors at `F` correspond to monodromy-invariant vectors
(equivalently differential-Galois-invariant vectors of the Fuchsian
Picard–Fuchs operators `L_j`).  This converts "infinitely many rows" into
one finite-dimensional invariant-subspace computation per window stratum,
with a cheap local upper bound (the multiplicity of exponent `0` without
logarithms at `z = 0`) available from indicial data alone.  History check:
no occurrence of this mechanism, "invariant subspace," "flat section," or
monodromy-of-the-gate-family in the canonical files (greps disclosed in
§0); the canonical record names only "creative-telescoping or
fixed-receiver reduction with explicit singular-index control," which is
the recurrence face of the same module but not the invariant/monodromy
decision mechanism.

**New connection — avenues 1/3/16 → 25/45, and a composition of this
round's four promoted pieces.**  The mechanism above hands avenue 25's
branch-cycle tools and avenue 45's Kovacic-style tools a base they never
had: the singular values of the gate family are the roots of the explicit
`y`-discriminant of `y^8 − F(sy)` (computable exactly for numeric `F`),
and local monodromies are algorithmic Puiseux data.  This repairs, by
construction, the exact failure for which `FACE-CHAR` was stopped (no base
map): here the base *is* the family's own `z`-line and no Keller-passport
identification is asserted.  Separately, note the pleasant closure: the
carrier composes all four of this round's promoted results — the torsor
theorem supplies the correct period lattice (`b_1(V_H) = 4(r−1)+c` fixed
cycles, exactly the right count across the four characters), the all-row
Lagrange formula supplies the integrand, R7R2 supplies regular holonomy,
and E0 supplies the statement that only this object's *nonlinear*
dependence carries information.  None of the four pieces was built for the
others; that they assemble exactly is evidence the carrier is the natural
object, not an invented one.

Minor connection (one line, no card): the carrier's outputs are finite
certificates (an operator, initial values, an integer `N0`), which is the
first upper-gate object in avenue 46's formalizable shape; no discovery
dependency.

## 4. Strongest attacks and one acceleration

- **Strongest proof attack.**  Card A (§5): build the four Picard–Fuchs
  operators `L_j` of the gate family on the frozen P stratum, extract
  indicial data at `z = 0`, and prove the stratum-uniform finite-decision
  bound `N0 = k* + B` of §7.  Qualitative holonomy is already promoted
  material; this is the shortest path from promoted theorems to a new
  *decision* theorem anywhere in the portfolio.
- **Strongest falsification attack.**  The K00 full-`P6` rank-exact lane
  (running; do not disturb), followed by the §2 survivor march feeding the
  genuine `D22 = 1` endpoint.  These are complementary: one attacks the
  terminal receiver, the other manufactures the cheapest honest face
  witness the corrected schedule permits.
- **Correctness-preserving acceleration.**  A frozen **class-vector fixture
  bank + recurrence replay verifier**: extend the existing exact
  fraction-elimination harness (the E0 harness pattern) to emit, at a few
  pinned numeric window points on P/Q/`r=1`, the exact class *vectors*
  `c_k` (not just ranks) for `k` up to ~60, hash-freeze them, and provide a
  pure-replay checker that any candidate recurrence/operator from any lane
  (`GATE-REC`, Card A, anyone's telescoper) must annihilate the fixtures
  byte-exactly before adoption.  This is correctness-preserving by
  construction (it can only reject wrong operators, never accept-by-
  authority), desk-scale, and turns every future gate-recurrence
  verification into replay rather than trust.  Not executed this session
  (no-heavy-algebra rule); it is a bounded builder task.

## 5. Cards (three, labelled)

### Card A — `GATE-CARRIER-PF`: effective Picard–Fuchs control of the gate tower — label: KNOWN (target = the live `GATE-REC` lane; the discharge route, constancy formulation, `N0` formula, and replay discriminator below are new content; nothing here assumes that lane's result)

- **Claim to establish.**  On one frozen stratum (P first): the four
  isotypic class sections `gamma_j(z)` are solutions of explicitly
  computed Fuchsian operators `L_j` with coefficients polynomial in the
  window slots, and the tower gate at a window point is equivalent to
  vanishing of the licensed coefficient tail, which is decided by the
  finitely many coefficients up to `N0 = k* + B` (§7 notation).
- **Dependencies** (all promoted): all-row formula (`8bad7ca0…` review),
  R7R1 licensing (`E = t^22` exact target ⇒ all rows), R7R2 regular
  holonomy + six-operations closure (AUDIT 21:30Z, HTT 6.1.5), torsor
  period lattice (`b_1 = 4(r−1)+c`), E0 (canonical).  Provisional inputs:
  none.  Independent of `BI-FACE-FROZEN` (provisional) entirely.
- **Cheapest exact discriminator.**  Two-step, in order.  (i) The `r = 1`
  control `H = (X−a)^8`: `V_H` is four punctured lines, all periods are
  *residues at `X = a`* — rational functionals — so the entire carrier
  program (operator, indices, `N0`) is executable in closed form by hand
  plus light exact arithmetic; every `k_m = 1` there, so E0 makes this
  control *maximally nonlinear* — the perfect sandbox.  (ii) One numeric
  window point on P: derive `L_j` by creative telescoping (AWS-light or
  bounded desk), then replay against the §4 fixture bank.  A secondary
  consistency identity: differentiating `L_j(F)·c(z;F) = 0` at the
  slots-zero point must be compatible with the frozen licensed table
  `3,4,3,4,3,0,3,0,3,0,3,0 | 1,0`.
- **Outcome interpretations.**  Success: the first finite-decision theorem
  for an infinite gate tail, per stratum, with explicit `N0`; feeds
  avenue 1 compression and every future face.  Failure modes and their
  meanings: operator order/indices blow up or the integer-root locus is
  unbounded across strata ⇒ the recurrence face is impractical and the
  monodromy face (Card B) becomes primary; a computed operator failing
  fixture replay ⇒ implementation error, not mathematics (rebuild).
  Irregularity is *not* a possible failure mode — regularity is promoted.
- **Rollback subtree.**  If any of the four promoted dependencies is
  reversed, this card and Card B quarantine automatically; nothing else in
  this report depends on them.
- **Cost / stop.**  `r = 1` control: one desk day.  Numeric-P operator:
  one bounded AWS-light job plus replay.  Stop if two independent
  derivations of `L_j` at the same numeric point disagree after one
  redesign, or if order exceeds ~60 on the control (signals wrong object).

### Card B — `GATE-INV`: monodromy/Galois invariant-subspace kill — label: NEW

- **Claim to test.**  For a window stratum `S`, the monodromy-invariant
  subspace of the gate-family local system (per character `j`) equals the
  span of the trivial background (the `j = 2` block's `[p^2 dX]`
  direction).  If proved on `S`: *no* point of `S` survives the tower
  except through the background — a stratum-level kill that never touches
  rows.  If refuted by a nontrivial invariant vector: a structured
  survivor certificate (an isotrivial subfactor of the family), itself a
  sharp object to hand to the endpoint test.
- **Dependencies.**  Card A's operators for the cheap version (invariant
  vectors are flat solutions, bounded by indicial data at `z = 0`);
  otherwise only the promoted set above.  Avenues 25/45 supply tools, not
  premises.
- **Cheapest exact discriminator.**  Indicial-only upper bound first: from
  `L_j` at `z = 0`, count exponent-`0` non-logarithmic solution slots; if
  that count is already 1 (background) on the control and one numeric
  P-point, the kill holds locally and the global statement reduces to a
  connectivity/irreducibility check at the finite singular values (roots
  of the explicit `y`-discriminant; exact for numeric `F`).
- **Outcomes.**  Bound = background only ⇒ promote a stratum tower-kill
  (necessary-direction only; endpoint untouched).  Bound > background ⇒
  compute one further local monodromy before judging; persistent slack on
  two fixtures ⇒ stop (uninformative), the recurrence face (Card A)
  remains.
- **Rollback subtree.**  Same as Card A.
- **Cost / stop.**  Marginal hours on top of Card A per fixture; stop
  condition as stated.

### Card C — `GATE-ALG-PRIM`: algebraic-primitive certificate between the class tower and `G`-descent — label: NEW (with one direction typed and the converse explicitly conjectural)

- **Exact content.**  R7R1 gives `w_{n+22}' = −(n+2) q_n/16` with
  `W = G/P^12`.  For an actual face/Keller pair, `G` is polynomial, so `W`
  is **algebraic** over `K(F)(X,s)` (in the fixed field `K(F)(X,s,P)`).
  Assembling the licensed tail, a face point must satisfy: the explicit
  algebraic form
  `alpha_F = −(s^21/16)(∂_s(s^2 Q) − 2p^2 s) dX`
  has an `X`-primitive in the *fixed finite extension* `K(F)(X,s,P)` whose
  `s`-expansion starts at order ≥ 23 (weight bookkeeping to be pinned at
  compile time; the identity `∂_s(s^2 Q) = Σ(n+2) q_n s^{n+1}` is checked
  by hand).  Existence of such a primitive is *decidable exactly* by
  algebraic-integration (Hermite reduction + residue lattice) in a fixed
  function field.  This condition is (a) necessary for every polynomial-`G`
  face point, (b) implies the entire class tower (termwise), and (c) is a
  priori **strictly stronger** than the class tower — it sits exactly in
  the gap E0 opened, between the vacuous linear tower and the hard raw
  `G`-window descent.
- **The honest gap.**  The converse — class tower ⇒ algebraic assembly —
  is false-shaped as stated: coefficientwise rational primitives need not
  assemble algebraically (control: `Σ (X^n/n) s^n = −log(1−sX)`, every
  coefficient rational, sum transcendental).  So Card C is a one-way
  *certificate/filter*, not an equivalence; its value is that the forward
  direction is the direction the face problem needs.
- **Dependencies.**  R7R1 (promoted), all-row formula (promoted).  No
  provisional inputs.
- **Cheapest exact discriminator.**  On the `r = 1` control and ~20 random
  rational window points on P: run the primitive decision and compare its
  cut against the class-tower ranks at the same points.  Extra cutting ⇒
  promote as a face filter strictly beyond the tower (then wire it in
  front of the survivor march of §2).  No extra cutting on both fixtures ⇒
  it collapses to the tower there; retain only as a compact certificate
  format for survivors; stop further spend.
- **Rollback subtree.**  None beyond its two promoted dependencies.
- **Cost / stop.**  Desk-scale per point on the control; bounded AWS-light
  for P points.  Stop per the discriminator above.

## 6. Program decisions

- **K00 — continue.**  Full-`P6` rank-exact lane on r6a is the strongest
  finite falsification object; next intersection steps (prior rows, next
  grade, V27 rank-purity) are already correctly sequenced.  No redesign.
- **GGV upper — continue** (five exact Box03 lanes + the 466-generator
  prefix quotient on Box02): let them decide or cap; output review before
  any dependent fanout.  Redesign only the *desk instrument layer*: retire
  what E0 killed (any linear-rate schedule) and stand up the §7 carrier as
  the tower instrument.  Lanes themselves unchanged.
- **LF40 — continue** as a decision computation (r6b + `std/dp`
  cross-check on Box02); obstruction-budget prediction remains discarded;
  the Sol resonance-at-raw-label-20 vs seven-dim compiler cokernel
  interface remains review debt, to be typed before use.
- **Coverage/landing — continue** on the intrinsic exact-pair two-chart
  constructor, L3–L5, `RPMC(C)`, cofinal ceiling.  Do not revive
  `C74-PLACE`/`EXIT-RPMC(C)`.  No redesign warranted by this round.
- **TD6/AS — continue** at current bounded allocation (TD6: repaired
  H19R1 emission with total `F`; AS109: only the three alpha/beta content
  strata).  No expansion, no stop.
- **Formal algebraization — continue** asynchronously on stable reviewed
  identities; never a discovery dependency; `jc2-lean` firewall as is.
  Card A's outputs (operator + initial values + `N0`) are the natural next
  formalization targets once promoted.
- **Sparse search — stop** as ambient search (unchanged); continue only as
  theorem-compressed search on named residuals; the E0-schedule survivor
  march of §2 is the one newly licensed compressed instrument.
- **External claim audit — continue.**  Matysiak SSRN 7229458 remains
  `ACTIONABLE-UNAUDITED`; lawful acquisition remains a human gate; the
  broad web sweep remains due 2026-08-28 13:48Z.

## 7. The critical question — what exact object carries and finitely controls the nonlinear gate information

**Answer.  The four `mu4`-isotypic Gauss–Manin period sections of the gate
family, as sections of an explicitly regular holonomic Picard–Fuchs module
on the `z = s^4` line; finite control is their Fuchsian indicial data.**
Not "more rows": the object below comes with a typed recurrence, a coherent
section, and residue functionals simultaneously, and its qualitative
existence is a corollary of already-promoted theorems.

Construction, with each step's authority named:

1. **The family.**  `P(X,s)` satisfies `P^8 = F(sP) = Σ_{i=0}^{14} F_i(X)
   s^i P^i` (promoted all-row formula block).  Setting `y = P`, this is the
   explicit affine plane-curve family `C_s : y^8 = F(sy)` over the
   `s`-line.  At `s = 0` it degenerates onto `y^8 = H^2 ⊇ V_H`.  The
   substitution `(y,s) → (ζy, ζ^{−1}s)`, `ζ ∈ mu4`, preserves the equation
   (both `y^8` and every `s^i y^i` are fixed), so the family is
   `mu4`-equivariant over `s → ζ^{−1}s`: the natural base variable is
   `z = s^4` and everything splits into the four character blocks — the
   "four-section" structure, derived rather than postulated.
2. **The section.**  Each `q_n ∈ O(V_H)[window]` (poles only along
   `Z(H)`, removed on `U`), so `c(s) = Σ_n [q_n dX] s^n` is a formal
   section of the *fixed* space `H^1_dR(V_H) ⊗ K(F)[[s]]`, splitting into
   four blocks `gamma_j`.  By R7R1, the exact target `E = t^22` licenses
   the full tower; the tower gate at a window point is exactly
   **`c(s) = c(0)`, constancy**, with `c(0) = [p^2 dX]` the trivial
   background (the slots-zero survivor, known).
3. **Periods.**  Pair with the `b_1(V_H) = 4(r−1)+c` fixed twisted cycles
   — the torsor theorem says this is exactly the right count across the
   four characters, i.e., the torsor theorem is the *period lattice* of
   the carrier.  For a fixed cycle `Gamma`, termwise integration on the
   convergence disk gives `Phi_Gamma(s) = ∮_Gamma P(X,s)^2 dX`, and the
   deformed contour `(X, P(X,s))` realizes `Phi_Gamma` as a period of
   `y^2 dX` over the Gauss–Manin transport of `Gamma` in `C_s`.  Tower
   gate ⟺ every `Phi_Gamma` is constant ⟺ the class section is a **flat
   section** (§3).  The `r = 1` control makes the functionals literal
   residues; in general they are the twisted periods dual to
   `H^1_dR(U, ∇_j)` (Deligne pairing; regularity per R7R2, so the pairing
   is perfect).
4. **Holonomy and regularity are already promoted.**  The AUDIT 21:30Z
   R7R2 block states: the cyclic `D`-modules generated by `P` (hence
   `P^2`) on the `(X,s)` surface are regular holonomic (HTT 6.1.5), and
   the algebraic six operations — *including nonproper direct image* —
   preserve regularity.  The direct image along `X` of the `∇_j`-twisted
   module is therefore a regular holonomic `D`-module `N_j` on the
   `z`-line containing `gamma_j`.  Consequently each `Phi_Gamma` satisfies
   a **Fuchsian** ODE `L_j` with coefficients polynomial in `z` and in the
   window slots.  This discharges the *qualitative* half of `GATE-REC`
   from promoted material alone; what remains open is effectivity, which
   is a computation, not a conjecture.  (I do not assume the live
   `GATE-REC` lane's result; if it returns first, merge and credit.)
5. **Finite control.**  Write `L_j` on coefficients as
   `Σ_{t=0}^{B} b_t(k) c_{k+t} = 0`, `b_t` polynomial in `k` with
   coefficients polynomial in the window slots; let `k*` be the largest
   integer root of the leading `b_B` (or −∞).  Then vanishing of the
   licensed coefficients through index `k* + B` forces vanishing of the
   entire tail: **`N0 = k* + B`**, explicit per stratum.  Uniformity over
   a window stratum is constructible: `b_B`'s integer-root locus is
   controlled by polynomials in the slots, exactly the "explicit
   singular-index control" the synthesis demanded; degenerate strata
   (leading coefficient identically zero) are handled by descending
   induction on strata.
6. **Where the nonlinear carry lives.**  The operators' coefficients are
   polynomial in the window; nothing is linearized.  E0 is precisely the
   statement that the *linearization* of the constancy condition at the
   trivial point has rank only 27/37 with every `k_m = 1, m ≥ 28`
   direction in its kernel — i.e., E0 computed the tangent map of this
   carrier and found it small.  The carrier is the object whose full
   nonlinear fibre the tower question actually asks about.
7. **Two decision faces, one module.**  Recurrence face: `N0` per stratum
   (Card A).  Invariant face: constancy ⟺ monodromy/Galois-invariant
   vector; stratum kills or structured survivor certificates (Card B).
   And one certificate face strictly beyond the tower for the *polynomial*
   problem: the algebraic-primitive filter (Card C), which uses the same
   integrand and catches part of what class language provably cannot
   (row-22 precedent: polynomial-window codimension can exceed class
   codimension).

**Stated obligations (not hidden):** (i) the pairing argument is over `C`;
the symbolic-window statement needs a standard spreading-out/specialization
lemma, to be written, not waved; (ii) the coefficient-class-vs-PF-section
identification at resonant indices at `z = 0` must be pinned when `L_j` is
computed (canonical-extension bookkeeping); (iii) the effective order of
`L_j` is bounded by fixed geometry of the generic fibre of `C_s` (a
degree-≤14 plane curve), but the practical size is unknown until computed —
that is Card A's job; (iv) everything here is necessary-direction tower
instrumentation; the endpoint `D22 = 1` and the `G`-window remain the
binding side of the face problem and are untouched.

## 8. Cross-tradition insight, and the full ledger

**Insight likely missed by the other training traditions.**  The campaign
already promoted, in one 21:30Z source-audit block written to *close a
loophole* (the R7R2 irregularity no-go), exactly the two hypotheses —
regular holonomicity of the cyclic `P`-module on the `(X,s)` surface and
regularity-preservation under nonproper algebraic direct image — that
*discharge the qualitative half of `GATE-REC`*.  Read as a D-module
statement (my avenue-16 lineage), the block is not merely a "no
irregularity escape" verdict; it is an affirmative structure theorem for
the gate sections.  The open problem the packet poses ("what carries the
nonlinear gate information") therefore has a two-line qualitative answer
already inside the promoted ledger, and the honest residue of the problem
is effectivity — order, indices, integer roots — which is finite work.
Traditions that treat "is the projected sequence P-recursive" as an
open-ended analytic question will miss that the campaign's own audit
already paid for the hard half under a different label.

**Conjectural leaps (each labelled in place).**  Card B's stratum-kill
hypothesis; Card C's usefulness beyond the tower (its converse is
explicitly false-shaped, with the `−log(1−sX)` control); the practical
smallness of `L_j`'s order; the spreading-out lemma of §7(i).

**Failed attempts this session (disclosed, not repeated silently).**
(1) I attempted to hand-derive the `r = 1` control's Picard–Fuchs operator
in closed form; I established that its periods are residues at `X = a`
(hence rational functionals and an algebraic period map) but did not
complete the operator within session constraints — left to Card A(i).
(2) I considered claiming "class tower ⟺ algebraic `W`" for Card C and
rejected the ⟸ claim myself via the logarithm assembly control above.
(3) I considered an exact comparison `Phi_Gamma(s*) = Phi_Gamma(0)` at a
nonzero rational `s*` as a discriminator and rejected it: period equality
at a point is transcendental, not exact-replayable.  (4) I considered
proposing a licensed-rank-pattern-predicts-operator check and rejected it
as untyped (ranks of tangent maps do not determine the operator).

**Assumptions.**  The hashed canonical files are ground truth; the live
execution snapshot is as of the packet freeze and was deliberately not
re-verified (no AWS contact); `BI-FACE-FROZEN` is treated as provisional
and consumed nowhere above; the `GATE-REC` lane's result is assumed in
neither direction; hand identities used (`∂_s(s^2 Q) = Σ(n+2) q_n
s^{n+1}`, the `mu4`-equivariance of `y^8 = F(sy)`, `q_n ∈ O(V_H)[window]`,
the E0 tangent reading) were derived in-session from the promoted formula
blocks.

**Checks actually run.**  All custody hashes and HEAD (§0); full/partial
reads as listed in §0; the hand derivations of §7 steps 1–6 and the Card C
weight bookkeeping; history greps for §3/§5 novelty restricted to
canonical files; consistency of the disposition vector against the 22:40Z
overlay and the 46-row master table read in full.  No computation beyond
hashing and hand algebra was executed; no fixture, operator, or rank was
recomputed this session — where I cite the E0 tables, R7R2 block, torsor
numbers, or live-lane facts, the authority is the hashed file, not memory.

**Accidental contamination.**  None known beyond the §0 disclosures
(auto-loaded personal memory index; harness-supplied git status metadata).
No peer `2259Z` content was seen; no `2255Z` lane response exists to see;
the `2137Z` reports consumed here are all inside the packet's custody set.

— end of submission —
