# Hostile review prompt: TD6 fixed-A3 q2 H-divisor repaired source DAG V64

Act as an independent different-model hostile mathematical and source
reviewer.  Return exactly one of `CONFIRMED`, `CONFIRMED_WITH_REPAIRS`, or
`REJECTED` as the final token on its own line.

This is a bounded source/custody audit.  Do not run the 28-minute exact
producer, a CAS, a solver, Lean, or any substantive computation locally.
You may use read/search tools and lightweight hashing.  If necessary, extract
the 264 KiB source archive into a fresh `/tmp` directory solely to read the
literal source and manifests; do not execute it.  Do not edit any producer,
case, frozen manifest, shared ledger, or this prompt.  Write only
`xmodel/td6-c1-c2-c3-q2-h-source-dag-repaired-v64-review-grok-20260825.md`.

## Charged inputs

- producer report:
  `xmodel/td6-c1-c2-c3-q2-h-source-dag-repaired-v64-aws-20260825.md`;
- frozen case:
  `cases/td6_c1_c2_c3_q2_h_source_dag_repaired_v64_aws_20260825/`;
- immutable predecessor report:
  `xmodel/td6-c1-c2-c3-q2-h-source-dag-v60-aws-20260825.md`;
- predecessor hostile review:
  `xmodel/td6-c1-c2-c3-q2-h-source-dag-v60-review-grok-20260825.md`;
- nonmutating V60 ancestry erratum:
  `xmodel/td6-c1-c2-c3-q2-h-source-dag-v60-erratum-20260825.md`.

## Mandatory charges

1. Verify `FREEZE.sha256`, `MANIFEST.sha256`, archive/output/artifact hashes,
   rc `0`, source-check closure, and the strict one-host custody statement.
   Stored PASS strings are not authority.
2. Inspect the literal V64 source in the frozen archive.  Check that the
   V60 review's load-bearing correction is genuinely implemented: current
   row 13 traces through exactly previous key `('X-1',14)` plus original
   first rows.  `('X-1',0)` must be a separate arbitrary-degree quadratic
   positive control/cache entry, never an N13 previous edge.  Reconcile this
   with every marker and proof-DAG line; do not repeat the old two-edge prose.
3. Audit the source replay logic rather than trusting digests.  Check that
   arbitrary-degree original rows are preserved; every selected current and
   previous edge is replayed against its original-row source; the staged
   left-null relation is exact; and transitivity in the free equation module
   licenses the unexpanded proof-DAG representation.
4. Audit all repaired controls as live mathematical assertions: omission of
   previous row 14, omission of singleton current row 13, the independent
   quadratic control, frozen-unit `k` with no center-coordinate denominator,
   and the live P12-without-N13 negative object.
5. Check genuine P12 directly: the pinned V44 H=0 object must have exactly
   2,885 terms, 28 nonzero first rows, and 1,640 multiplier terms; must replay
   through original first rows; and must compose with N13 to the exact
   residual `-k/50`.  Confirm that no previous-stage reduction of P12 is
   silently required.
6. Inspect denominator collection/factorization.  Decide whether the literal
   coefficient-leaf ledger plus pinned V44 termwise clear has complete radical
   support exactly
   `{U, V, P3, QH}`, where
   `P3=V^4-32 V^2 U^3+128 U^6` and
   `QH=V^4+8 V^2 U^3-64 U^6`.
   Flag any missing pivot, coefficient, multiplier, or `k` denominator.
7. Enforce scope.  Even on confirmation, the strongest result is only the
   source identity on `H=0,D(U*V*P3*QH)` in the fixed source-typed A3 q2-beta
   section.  The factors `U=0`, `V=0`, `P3=0`, and `QH=0` remain separate
   source-rebuild obligations.  Infer no whole-H/whole-A3/TD6/SP-2/landing/
   JC2 statement and no unused-row completeness theorem.
8. State separately: mathematical identity verdict, ancestry/source verdict,
   factor-ledger verdict, custody verdict, exact licensed scope, and every
   repair or remaining gap.  Record exact hashes/commands used.

End with exactly one verdict token on its own line.
