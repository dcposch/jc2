# Hostile bounded review charge: TD6 V65 source-DAG P3/QH divisors

Act as an adversarial algebra, source-typing, and custody reviewer.  This is a
bounded read-only audit.  Do not edit producer/canonical files and do not run
the substantive replay, CAS, solver, Lean, or heavy Python.  You may hash
files, inspect text/source, extract the small portable archive in private
`/tmp`, and perform short hand or standard-library checks.  Write only the
requested review report.

Read in full:

- `xmodel/td6-c1-c2-c3-q2-p3-qh-source-dag-v65-aws-20260825.md`;
- `cases/td6_c1_c2_c3_q2_p3_qh_source_dag_v65_aws_20260825/README.md`;
- that case's `MANIFEST.sha256`, `FREEZE.sha256`, `verify.py`, all four
  successful P3/QH evidence trees, the q-prime-omission evidence, and the
  portable source archive;
- the earlier correction
  `xmodel/td6-c1-c2-c3-q2-n13-h-zero-v60-scope-erratum-20260825.md` if
  present, plus the reviewed V64 H-generic theorem/report named by the V65
  producer.

Adversarially verify or refute:

1. **Source typing and exact scope.**  The only asserted loci are
   `H=P3=0,D(U)` and `H=QH=0,D(U)` inside the fixed normalized source-typed
   A3 `(c1,c2,c3)=(C,V,U)` q2-beta section.  Check every definition of
   `H`, `P3`, `QH`, `T`, beta, and direct `q_beta'`; no scaling or generic
   fraction-field assertion may silently enlarge this scope.
2. **Original-row ancestry.**  Inspect the executed source and emitted DAG.
   Confirm that current N13 traces through exactly previous row
   `('X-1',14)` and then original first rows.  `('X-1',0)` is an independent
   quadratic positive control/cached block, not a second N13 edge.  Decide
   whether its presence anywhere in the emitted DAG is harmless cache/control
   material or contaminates the claimed N13 ancestry.
3. **Genuine P12 composition.**  Verify that P12 is the actual 2,885-term,
   28-original-row lift, not an affine or synthetic surrogate, and that the
   exact combination with N13 gives the beta-independent unit residual
   `-k/50`.  Audit sign/RHS conventions, multiplier provenance, lambda-prime
   or varying-echelon terms, and the direct q/q-prime contribution.
4. **Denominator ledger.**  Independently inspect every leaf/multiplier in
   both P3 and QH DAGs.  Check that after source composition the complete
   radical support is exactly `{U}` with chart exponent `U^13`, rather than
   hidden P3/QH/V/H/pivot factors.  A printed denominator summary alone is
   insufficient if the source ancestry contradicts it.
5. **Dual-host replay and custody.**  Check rc zero, source-archive closure,
   stage ranks `3470/3602 -> 38/132 -> 38/94 -> 25/56`, exact-expression and
   normalized-stream agreement between Box02 and r6d, artifact hashes,
   manifests, freeze file, and the lightweight verifier.  Explain any
   hostname/path-only differences and whether they are safely normalized.
6. **Controls.**  The direct-q-prime omission run intentionally ends rc one
   only after printing that N13 changes, then hits an absent-record reporter
   `IndexError`.  Treat it solely as a post-math negative control.  Check that
   it is not used as theorem evidence and that its failure actually detects
   omission rather than a generic harness crash.
7. **P3/QH relation.**  On `D(U)`, set `T=V^2/U^3` and verify
   `P3/U^6=T^2-32T+128`, `QH/U^6=T^2+8T-64`, and
   `((5T-136)QHnorm-(5T+64)P3norm)/512=1`.  This proves divisor disjointness
   on `D(U)` but does not by itself eliminate either divisor; only the two
   independently source-replayed unit identities do.
8. **Scope firewall.**  Decide whether the evidence proves exactly those two
   localized divisor theorems and nothing stronger.  It must not be promoted
   as whole P3/QH curves, whole `H=0`, whole A3, TD6, SP-2, landing, or JC2;
   it also does not repair separate `U=0`, `V=0`, or generic-H pieces.

Try to flip the result through a hidden denominator, source-row/echelon
confusion, the forbidden two-previous-row interpretation, cached-row
contamination, q-prime omission, wrong sign, reducible/ill-typed function
field, nonportable serialization, failed-control contamination, or scope
overreach.  Separate mathematical defects from custody/expository nits.

Write
`xmodel/td6-c1-c2-c3-q2-p3-qh-source-dag-v65-review-grok-20260825.md`
and end with exactly one verdict token on its own line: `CONFIRMED`,
`CONFIRMED_WITH_REPAIRS`, or `FAILED`.
