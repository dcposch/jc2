# Fable 5.1 narrow DELTA: coordinator retirement timer binding — A–C verdicts

Reviewer Fable 5.1, DELTA only. First action 2026-09-11 09:56:16 UTC (ROOT prep 09:54:22 UTC); latest launch 10:00; original reserve 10:08 / HARD 10:11 UTC, never reset. Both owned targets absent at 09:56:16Z and again at 09:58:38Z before the first write. Basis 0d39df3c9fd69c939a8420c54d03228b9077777d. Inputs: exactly the six frozen files in the lane input directory plus FALLACY-v2; all six SHA-256 pins matched the assignment before any body was read and are recorded in `box/caprun-admission-timer-binding-gate-fable5-20260911/input_custody.md`. STATIC ONLY: nothing executed, imported, parsed or tested; no host, AWS, SSH, network, git or process action; only sha256sum/diff/cmp/grep/sed/head/wc/date/ls and the mandated apply_patch tool. Line numbers are `cat -n` lines of `launch-worker.template.sh` (L), `parent-outer-admission.template.sh` (P) and the new `outer-admission.template.sh` (N).

**Verdict: A ESTABLISHED, B PASS, C SOUND-CONDITIONAL with the boundary stated in C. Smallest blocking correction: none in the delta; the one mandatory item is the external ROOT precondition, which no byte of either file performs.** This reviews only the interface correction. The accepted holder/outer FIRST, the frozen runtime/source caps, the strict9 preflight/no-science boundary, the ROOT/exclusive/no-migration trust model and the conservative stale-positive STOP risk are consumed unchanged and not re-opened.

## A. Real deployment mismatch — ESTABLISHED under the stated host assignments

Actual selected launcher (L): L4 `cd /home/ubuntu/jc2` fixes the coordinator checkout; L17–27 run `aws ec2 run-instances --profile personal --region us-east-1` and capture the new instance; L29–30 extract `jc2_r3_instance` (`^i-[0-9a-f]+$`); L31–34 `systemd-run --user --unit=jc2-closedchild-preflight9-20260911a-worker-stop --on-calendar=… /usr/local/bin/aws ec2 terminate-instances --profile personal --region us-east-1 --instance-ids "$jc2_r3_instance"`; L35 `systemctl --user is-active …-worker-stop.timer`; L36 prints ORIGINAL_WORKER_TERMINATION_ARMED. Every one of these runs in the coordinator user's systemd manager with the coordinator's `personal` AWS profile. Nothing in L installs, copies or names any unit on the worker: no ssh/scp, no credential or instance-profile argument, and the only worker-side input is the hash-pinned user-data file at L13/L21, which is outside this packet.

Parent outer (P): P4 requires uid 0; P10–11 bind the run to the worker's own DMI asset tag and boot id, so it executes on the worker; P13–17 address `/sys/fs/cgroup/system.slice/…`, the worker's system manager. P31 `systemctl is-active 'JC2_WORKER_STOP_TIMER_PLACEHOLDER'` carries no `--user`, `-H`, `--host` or `--machine` qualifier (grep count 0 in both P and N), so as root it queries the worker's local system manager, which cannot see a `--user` transient timer on another host. Under the stated assignments the line has exactly two outcomes: the unit is unknown or inactive and `set -e` (P3) exits before any timer or unit exists (fail-closed, admission never starts), or an unrelated worker-local unit bearing the substituted name reports active, which verifies nothing about the AWS retirement. Neither outcome observes the actual retirement timer, its target instance or its deadline. The mismatch is real and established by the two files alone; it is a wrong-manager query, not a wrong-schedule one. The token WORKER_STOP_TIMER occurs in no input other than P and the root report. The collected FIRST's section D recorded only "asserted active; schedule not read" and did not have L, so it is neither contradicted nor blamed.

## B. Independent diff — PASS

Regenerated `diff -u P N`: one hunk `@@ -28,7 +28,10 @@`; removed lines = 1 (P31, 56 bytes with newline); added lines = 4 (N31–34, 311 bytes, each beginning `# `); 6006 + 311 − 56 = 6261 bytes, matching the pinned sizes. The comment-stripped executable stream of P minus P31 and that of N are `cmp`-identical; comment lines 4 → 8; line count 95 → 98. Untouched, by line in N: shebang, `set -euo pipefail`, uid and placeholder guard N1–8; release string N9; instance/boot/latest-launch N10–12; unit, cgroup, base and channel names N13–16; cgroup/registration absence and FIFO mode/ACL assertions N17–23; stage cd and both manifests N24–27; holder SHA and 0:444 N28–29; worker TERM/KILL arming with absolute `--on-calendar`, `AccuracySec=1s`, `--kill-whom=all` N35–40 and their `is-active` check N41; armed timestamp N42; 60-second bound N43 with its re-checks N60/N85/N89; EXIT trap N45; the holder command N46–59 with all fifteen resource properties verbatim (RuntimeMaxSec=3000, RuntimeRandomizedExtraSec=0, TimeoutStopSec=5, KillMode=control-group, MemoryMax=8589934592, MemorySwapMax=0, OOMPolicy=kill, TasksMax=32, CPUAccounting=yes, CPUQuota=69%, CPUQuotaPeriodSec=10ms, CPUSchedulingPolicy=other, RestrictRealtime=yes, Delegate=no, Restart=no), `StandardInput=null`, `env -i` with four variables, `python3.12 -I -S -B` and the holder path; sentinel/argv/cgroup/namespace/exe/sole-membership/no-children and kernel assertions (cpu.max `6900 10000`, burst 0, memory.max, swap.max 0, pids.max 32, domain, empty subtree_control) N63–76; record-only show/ps/stat/chrt/controls N77–84; second MainPID/argv/registration re-checks N86–88; capture timestamp, `jc2_admitted=true`, ADMITTED_NOT_RELEASED, STOP message and `exit 2` N90–98. CPU origin is unchanged (no cpu.stat write, no rebasing); no-restart (Restart=no) and no-retry (single `systemd-run`, single window, exit 2) remain. Both templates stay disabled: the guard at N5–8/P5–8 matches all eleven remaining `JC2_*_PLACEHOLDER` tokens in N (twelve in P) and exits 2, and N9/P9 fail independently.

## C. Conditional soundness — SOUND under the stated ROOT precondition; exact boundary below

The deletion removes a check that, by A, never observed the real retirement timer, so it cannot lower actual safety: it removes either a deterministic STOP or an unrelated-unit pass. What remains on the worker is unchanged and sufficient for unit-level bounding: absolute TERM/KILL timers armed and verified active before the holder exists (N35–41), RuntimeMaxSec=3000 (N48), the EXIT trap (N45) and the 60-second window. Instance-level retirement was never a worker duty in the actual design: L31–36 arm it on the coordinator before any admission can occur, and L43–44 make failure of that timer ROOT's immediate exact-ID retirement duty with no retry.

The precondition is a complete and strictly stronger substitute for the deleted line. The deleted line asserted active status only; the precondition requires, immediately before remote admission, the current active timer, its triggered service, the entire ExecStart including `terminate-instances --profile personal --region us-east-1 --instance-ids <id>`, and the original future deadline, then a frozen/hashed receipt bound into the execution card, with any absence or mismatch meaning no admission and reliance on the existing exact-ID retirement. The exact binding it must close, all from supplied bytes: launcher `$jc2_r3_instance` (L29) = the instance in the triggered service's ExecStart (L33–34) = the value substituted for `JC2_INSTANCE_PLACEHOLDER` at N10 = the worker's DMI asset tag read at N10. The identities are the coordinator user manager's `jc2-closedchild-preflight9-20260911a-worker-stop.timer` and `.service` (L31, L35); the deadline is the L32 calendar value read from current manager output, not the L36 log line or a filename. With that duty performed and honestly recorded, the delta is sound: no path in N admits without the worker TERM/KILL active, and no path in N or L creates a surrogate, dummy or unrelated timer.

Exact boundary. The source delta is accepted as a correct interface correction and nothing more. It does not claim deployment readiness: no future host, timer, service, AWS profile or deadline has been observed, and the four comments N31–34 are inert text that implement, enforce and prove nothing. It does not claim adversarial-ROOT safety: a ROOT that skips or falsifies the receipt meets no in-script refusal, exactly as with the accepted ACL/namespace/exclusive-writer duties. The premise is a point-in-time observation; nothing in N can re-observe the coordinator manager after admission (the worker has no view of it), so the interval between receipt and worker stop is covered only by the existing exact-ID retirement clause and the worker-local TERM/KILL. I find no insufficiency in the premise for the stated boundary; the residual is inherent to a trusted-ROOT external duty, not a defect to redesign. The launcher's own `is-active` at L35 is allocation-time evidence and not a substitute, as the root report already says. No dummy remote unit, extra service, numeric scheduler bound, calibration, smoke test, timing reset or optional stdout patch is selected; fresh tmpfs/no-retry remains the stale-sentinel prerequisite; the token is public in 0444 code and not a secret; the holder proof is not re-opened.

Record consistency: the root artifact JSON pins body 5717 bytes, body SHA 4b27f15929204f1ee269909bcd966e4602667e6c611f34263c59649a74398acd, full SHA bb00d8dc15208a621dbec8293f3faf54ed3836cfeba6aed1a1622a4d533f4252, basis 0d39df3c, custody opened 09:52:03Z, closed 09:53:03Z, finalized 09:53:04Z. `head -c 5717 | sha256sum` reproduces the body hash and the standalone BODY-END line ends at byte 5717. The root report's stated pins for P, L, the FIRST and N equal the pins matched here.

## OPENS RAISED

None. No canonical OPEN id, follow-on task, billing or JC2 claim.

## QUANTITY / CHEAPEST TEST / COLLISIONS

QUANTITY: delta = 1 executable line removed + 4 comment lines added + 0 other byte changes; retained worker controls = every executable line of P except P31; A = established, B = pass, C = sound conditional on the external ROOT precondition; deployment readiness = not claimed. CHEAPEST TEST: none further for this delta; the single remaining action is ROOT's own coordinator-side receipt immediately before any admission, outside this lane. Planning wall for this review = 12 minutes UNMEASURED, not runtime clearance.

COLLISIONS (own-only): none. This DELTA consumes the six pinned inputs, reaches the same mismatch finding as the root report by independent diff and launcher read, and adds no OPEN.

Deviation record: no execution, import, syntax, AST, compile, test, dummy, CAS or candidate subprocess; no AWS, SSH, network, git or process control; no Write/Edit or shell-redirection edit; only the mandated apply_patch tool wrote the two owned files, each write under 1000 words by `wc -w`. Pre-read and post-write pins of all six inputs are in the custody file.

Post-write pins and whole readback precede the final marker.

<!-- BODY-END -->
