# Primary research: total typed LANDING-LEDGER

You are exact Fable 5, an equal-standing whole-campaign researcher.  Design
the smallest sound compiler/audit that turns the campaign's amorphous
full-configuration landing gap into a typed, fail-closed ledger.  This is
primary research, not a prose summary and not an implementation commission.

Read these exact files under `/Users/dc/code/math/jc2/`:

- `ladder/REDUCTION.md`;
- `ladder/SHEET6-DEPTH.md`;
- `ladder/SHEET6-MULTIPOLE.md`;
- `ladder/BOOK-OFFAXIS.md`;
- `xmodel/ideation-20260829T0002Z-fable5.md`, Card B;
- `xmodel/ideation-20260829T0002Z-sol56.md`, Card 1.

You may follow only direct citations needed to identify the promoted source
of a transition rule.  Do not read other ideation submissions.  Do not access,
list, search, build, status, or control `jc2-lean`.  No web, AWS, heavy CAS,
canonical edits, commit, or push.

## Charge

1. Define a minimal typed state schema that retains every field needed by the
   promoted move hypotheses and by downstream cell kills: stage, fibre/root,
   pole/branch/deck provenance, entry data, `M`, arrival multiplicities,
   `(nu,dp,dq,kappa,rho,w)`, budget, chart, unused branches, and theorem source.
   Delete any field you can prove derivable; add any missing load-bearing
   field.
2. Give a rule table for every currently promoted move.  For each rule state
   exact preconditions, transformed fields, preserved provenance, source
   theorem, and a negative control that must be rejected.
3. Specify a total three-way classifier: `COVERED(rule,certificate)`,
   `TERMINAL(record)`, or `UNCOVERED(exact_missing_hypothesis)`.  No silent
   skip, default, cap exhaustion, or aggregation across entry contexts.
4. Hand-run the schema on the smallest useful two-pole `td=6` sector through
   the first post-jump boundary.  Determine exactly what is discharged and
   what remains symbolic/uncovered; do not assert finiteness merely because
   the entry menu is finite.
5. Decide whether the ledger should enumerate states, represent parametric
   families, or stop at a symbolic transition frontier.  State the smallest
   first implementation and its acceptance tests.
6. Separate this total-landing obligation from `G2-PSC`, `G2-BD`, `RPMC(C)`,
   and the cofinal degree ceiling.  No implication may cross those interfaces.

Return a theorem/interface-quality design, an exact first packet specification,
and a `CONTINUE`, `REDESIGN`, or `STOP` verdict.  Write only
`/Users/dc/code/math/jc2/xmodel/landing-ledger-primary-research-fable5-20260829.md`
and end with a report-body self-hash.
