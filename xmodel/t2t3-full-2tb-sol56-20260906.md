# Full \((99,66),\delta=2\) direct ideal: 2 TB construction stop custody

## Disposition

**NOT LAUNCHED.** The full 449-variable, 2,754-generator attained-\(T_2+T_3\)
presentation did not close. The whole-table builder was deliberately stopped by
the active coordinator, returned rc 143, and left only partial, non-parseable
output. Consequently no full generator-stream SHA exists, the 466-row literal
subset comparison could not be executed, and none of the three requested
solvers was started. This report records failure custody only and makes no
mathematical verdict.

The requested `LAUNCHED[(99,66) δ=2 FULL direct ideal on 2 TB; harvest due
~+20 h]` sentence is intentionally not asserted: it would be false. The worker
remains running and was not terminated or repurposed.

## 1. Frozen inputs and host limits

Receipt `xmodel/t2t3-full-2tb-sol56-20260906.run.v2` fixes basis
`f554ec1f3bc1f0100f4aa04ae3d2943be586b634` and frozen root
`/tmp/jc2-lane.XRlxKE/inputs`. Before using an input, an `awk` pass mechanically
paired all ten `charged_input_<i>_basename` and `charged_input_<i>_sha256`
fields into `/tmp/t2t3-full-2tb-inputs.sha256`; `sha256sum -c` returned ten
`OK` lines. The manifest SHA-256 is
`15aeab0a6b75725cebc122bd36e383a07084be30ab741ca8a06420bf7a687336`.
In particular, the frozen builder SHA is `77ef23b8…34be3e`, the two msolve
patches are `c49a9fb3…3b2242` and `4f2e7235…860555`, and `FALLACY-v2.md` is
`e47fd16c…9c38f5`; full values are in compact custody.

The required pre-write `df -h /` observation was `/dev/root`: 96G total, 85G
used, 12G available, 88% used. All large artifacts remained on worker-local
scratch. Host writes for this lane stayed far below 2 MB. No ledger,
`jc2-lean`, or `ideation-*` input was read or edited.

## 2. Authorized worker and toolchain

The required command

```text
sh ops/fleet/fleet.sh wait i-025410e620b1d65c9
```

returned `READY`. AWS revalidation at 11:17Z showed instance
`i-025410e620b1d65c9`, private IP `172.30.0.103`, type
`x2idn.32xlarge`, state `running`, and Owner
`coordinator-2tb-20260906`. The worker reports 128 CPUs and
2,097,111,324 KiB RAM. Its two empty instance NVMe devices were joined as an
ext4 RAID0 at `/home/ubuntu/t2t3-full`; after cleanup it had 3.3T available.

msolve 0.10.1 source commit
`185e7b92fa0687f4db68b0f2f453a835668ac132` received both frozen patches.
Patch/reverse checks and `git diff --check` passed; the rebuilt tree reported
64 PASS, zero failures or errors. `/tmp/msolve-patched/bin/msolve` hashes to
`0ffbb5455a6ad94bc85b17c52d75d5cfe59fcb1d494f54bf8d97a0389606a111`.
Singular 4.3.2 at `/usr/bin/Singular` hashes to
`90ab699b7a28486944a167e797d7c8dd38f8f949960b93ee7a6d08ff1c7c46f4`.
The cap runner passed all 17 regression tests. These successful preparation
steps are tool custody, not evidence that a solve launched.

## 3. Full-presentation attempt

The expected frozen contract is 4,470 raw slots, 1,716 expected zeros after
semantic substitution, 2,754 nonzero primitive rows, and 449 variables. Its
source-map, semantic-order, and two-site-matrix hashes are respectively
`778eda93…3e1ea`, `ee778e93…28a4b`, and `547fabda…5cf00`. These are expected
contract values only; the failed run did not certify a completed artifact.

The whole-table derivative had SHA-256
`8ec0e982b77e54a4f5cdb52358e7bb21c2bbf41ed6837c5e8996b1f1d1b89ff5`;
its separately retained patch hashes to `3e63a0b2…2d39`. It started at
2026-09-06T08:48:49Z with:

```text
/usr/bin/time -v -o /home/ubuntu/t2t3-full/logs/full-build.time \
 /usr/bin/python3 -u /home/ubuntu/t2t3-full/drivers/build_direct_whole.py \
 --case 99-delta2 --output /home/ubuntu/t2t3-full/presentations/full
```

The last completed marker was only
`rows=400 terms=1070351 block=T2_upper`. During eager construction of the
whole \(Q\) table, resident memory reached 2,086,234,548 KiB. Four worker-local
512 GiB swapfiles were enabled as construction backstops, but the process still
had not emitted a new marker or a closed row-label stream.

At 2026-09-06T11:11:10Z the coordinator intentionally sent SIGTERM to Python
PID 4923 only. GNU time records `Command terminated by signal 15`, elapsed
2:24:06, user 6,171.46 s, system 2,471.56 s, and the peak RSS above. The
wrapper finalized `full-build.rc=143` at 11:12:55.635Z. Kernel `oom_kill`
remained zero; no OOM, systemd-oomd, timer, cron, watchdog, or session teardown
caused this stop. Worker journal timing instead shows coordinator-host SSH
session 459 from `172.30.0.46` opening at 11:11:10.266954Z. The exact remote
kill argv is unrecoverable because exec auditing was not enabled.

The controlling stop note is preserved on worker and host as
`COORDINATOR_STOP-20260906T1111Z.md`, SHA-256
`dbff415c7e25a5b2b7db6e05fc0c6a983562153e8664ef252a9d43920df5a368`.
It expressly forbids a restart, replacement launch, repurposing, or instance
termination in response to the stop.

No `99-delta2.build.json` or variable map exists. The unclosed partial files
are not solver inputs:

| partial worker artifact | bytes | SHA-256 |
|---|---:|---|
| `full/99-delta2.ms` | 55,245,012 | `a34d57af…a3684` |
| `full/99-delta2.sing` | 73,006,401 | `d408c32e…11a6a` |
| `full/99-delta2.labels.tsv` | 0 | `e3b0c442…b855` |

There is therefore no full-artifact canonical generator-stream SHA to report.
Inventing one, or treating a partial byte hash as one, would violate custody.

## 4. Independently rebuilt 466-row subset

The frozen `build_t2_certificate.py` independently completed `d2-z55` at
09:11:23Z, rc 0. It has 449 variables, 466 generators, and 6,448,959 expanded
terms. Its canonical primitive generator-stream SHA-256 is exactly

```text
a78c6a46e4b50dec6df51712c9c4b8d3c717b52490488d6819563cb5e18044fe
```

The paired `.ms` and `.sing` hashes are `7f593b87…7b572` and
`a604ed31…ccbbc`; metadata, labels, and variable-map hashes are recorded in
custody. This reconstructs the known subset but does not establish literal
inclusion in an absent full artifact. The rowwise checker was correctly not
run: the full labels and variable map required for a byte-and-row-hash match
do not exist. Its result is typed `UNAVAILABLE_NO_FULL_ARTIFACT`, not `true`
or `false`.

## 5. Solver launch gate and process confirmation

All launch gates failed closed. At 2026-09-06T11:17:46Z,
`ps -C msolve -C Singular -C python3` returned no processes, the run tree had
no entries, `full-presentation-validation.json` did not exist, and neither did
`subset-inclusion.json`. Thus there are no first F4 lines, runner receipts,
start times, or expected 20-hour ends.

The prepared but unexecuted contracts were:

| job | intended command core | intended cap | state |
|---|---|---|---|
| `msolve-p1073741827` | patched msolve `-m 5000 -g 2 -t 120 -v 2`, prime 1073741827 | 72,000 s; 1,600 GiB AS | not launched |
| `singular-q-slimgb` | Singular exact-Q `slimgb`, one thread | 72,000 s; 1,200 GiB aggregate RSS | not launched |
| `msolve-p1073741783` | same msolve flags, second prime | same msolve caps | not launched |

The exact intended argv and the absent run paths
`/home/ubuntu/t2t3-full/runs/<job>/` remain in custody. Because no runner was
started, no `runner.rc`, `time.txt`, `caprun.json`, solver stdout, or detached
PID can truthfully be listed.

## 6. Cleanup, preservation, and FALLACY-v2

After Python exited, all four temporary swapfiles were explicitly disabled and
removed. At 11:14:09Z the worker had zero swap, swappiness restored to 60,
2,087,986,444 KiB available memory, and 3.3T scratch free. Partial builder
artifacts, logs, drivers, patched binaries, the complete subset, and the stop
note remain under `/home/ubuntu/t2t3-full`. The instance is still running and
must not be terminated by this lane.

FALLACY-v2 was applied literally. A modular unit would be only a signal; an
exact-Q singleton basis on a proved full presentation would kill this direct
chart only under the compressor theorem's hypotheses. Here there is neither a
full presentation nor a solver result, so no unit/nonunit, chart-kill, or
mathematical conclusion exists. No new exit-price assertion is made, hence no
`charge_basis` declaration applies.

Compact machine-readable custody is
`box/t2t3-full-2tb-20260906/custody.json`. This lane ends as
`COORDINATOR_STOP/NO_FULL_PRESENTATION/NO_SOLVER`.

NOT_LAUNCHED[(99,66) δ=2 FULL direct ideal on 2 TB; coordinator-stopped before full artifact]

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `8430`.
- Body SHA-256:
  `f8da0ddb99118d19749ac8a46507995b2d4d1b2341cd6c2004d25f40c856f70d`.
- Frozen basis: `f554ec1f3bc1f0100f4aa04ae3d2943be586b634`.
