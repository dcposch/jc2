# Quotient/remainder generic controls — static preparation only

2026-09-10. STATIC / UNEXECUTED / UNREVIEWED CODE. No mathematical subprocess, syntax/import/AST/compile/test, fixture, coefficient array, candidate, matrix, certificate, native call or runtime result was produced. First action00:17:09.571501344UTC; both targets absent. Controlling stop00:39UTC, earlier than first+25minutes, with publication reserve00:36. No clock was reset.

## 1. Prepared result and frozen contract

The owned generic_controls.py implements the requested future AUTHORITY OUTPUT entrypoint and its own dense Fraction fixture verifier. PLAN.md gives the exact eight positive and four negative hand expectations, future exclusive output paths, disabled state and outstanding obligations. Owned PINS.json and READ-SCOPE.md bind exactly fifteen current inputs.

The original shared PINS and INTERFACE matched before their WHOLE reads; all twelve listed objects then matched before their bodies. The only additive object is root's API-DELTA a8bdb4fda1199bdf4f553e7d3b12858c05adce32ceed4da78602deaba8bd8d1b, pin-checked before WHOLE read. It resolves the previously unspecified negative exception: the candidate must raise exactly ValueError with string 'generic modulus must be squarefree' for p=v^7. Both authors received that same clarification; no shared file was edited.

The new solver's live body was neither read nor copied. No stub, fallback or execution against the old solver was created. The future flat package must supply generic_candidate(p,fs,q,flint) exactly as the frozen interface states. Missing code is an expected dependency, not a reason to invent a replacement. No static claim about that producer's implementation is made here.

Accepted17zg is consumed only at its proved quotient/remainder design scope, with root's qualifications: the separator functional must be completed on the whole rational basis, and on the g child only the selected leading coefficient is necessarily zero, not the whole generator. The lost-component fixture deliberately has a nonzero constant generator on that child. No foundation is rerun, no source parser is changed, and no actual source artifact or acceptance is in this packet.

## 2. Authority and import order

At module load the harness imports only metadata standard libraries and defines functions/constants. main first checks the unchanged execution_gate.py hash, then calls its existing authorize with operation build and the exact CLI. That gate is reused unchanged, not copied or patched here. It checks the registered EC2 host, exact CAPRUN parent/child argv, -I/-B flags, cwd and all declared file hashes.

After authorization, the unchanged evidence.py hash is checked before import. Its strict JSON loader reads the authority and its installed-environment helper is reused; evidence.verified is NOT called, and no prior source artifact/receipt/ROOT acceptance is supplied. univariate_acceptance must be null. The harness requires exact pins for all six flat files: itself, new solver, and unchanged checker/evidence/algebra/execution_gate. Four retained hashes are also literal constants in the harness. The native interpreter is checked, installed module/version metadata is required, and every declared native dependency has already been hashed by authorize before importing FLINT.

Only after those checks and the absent-output precheck does main import FLINT, verify its installed path/hash/version, import the new generic_candidate, and enter the arithmetic routines. No package discovery, installation, alternate API, process supervisor, cap change, second authority schema, source receipt simulation or ROOT acceptance issuer is added. Completeness of the installed native closure and fresh boot/deadline/cap custody remain root's existing registration/controller obligations; this helper does not claim to discover or certify them independently.

All future worker, caps, argv, paths, clocks, installed package fields and new solver pin remain unassigned/null in the disabled plan; no authority file is issued. The inherited authority is cooperative registration, not same-UID authentication or intrinsic isolation of importable helper functions. A future in-process call is licensed only inside this separately authorized entrypoint.

## 3. Independent full certificate verification

The independent verifier receives the synthetic fixture modulus, not the original source modulus and not a producer matrix. It strict-parses canonical numerator/positive-denominator strings with Fraction normalization; rejects noncanonical rationals and incomplete dimensions/orders; and checks that p is monic of degree seven and squarefree through its own dense rational Euclidean remainder loop. This is independent of solver.py, algebra.py, FLINT and construction_trace. No claimed rank, split list, short certificate or solver flag proves an identity.

For B multiplication, the verifier forms all thirteen dense rational v slots of a product of two seven-coordinate elements, then eliminates v^12 down through v^7 by subtracting the supplied monic p. T multiplication is a separate dense convolution over those reduced coefficients. No division by a B coefficient or inversion in a nonreduced remainder algebra is used by the verifier.

UNIT reconstructs all nine multipliers from the original1638 pairs, with column index(i*26+j)*7+ell. It multiplies every multiplier by the corresponding full fixture row and compares all31*7=217 coordinates with q^5 padded through T^30. It gathers mismatches only after visiting every coordinate, including the high target-zero rows. There is no old full search matrix.

SEPARATOR reconstructs every original column pairing for i=0..8,j=0..25,ell=0..6, even for zero or duplicate generators. It visits all1638 pairings and all217 target-coordinate contributions before deciding the arithmetic predicates. The pairing with q^5 must equal one. The verifier emits only synthetic branch/check-count evidence; no separator is labeled a point, character, actual source result or JC2 conclusion.

The actual-source checker.py remains unchanged and is never imported by this harness. It is hash-pinned as a required retained package member, but the fixture verifier is not an alternate actual-source acceptance route. Generic candidate certificates carrying source/runtime binding fields are refused; the harness summary has its own plainly synthetic scope, not the ROOT source acceptance schema.

## 4. Exact finite inventory and independent hand expectations

The only future synthetic modulus is p=product_(j=0..6)(v-j), and e=product_(j=1..6)(v-j)/720. Both are constructed only inside a future authorized run. The seven distinct rational roots prove p squarefree; e is the idempotent of v=0. No coefficients or fixtures were evaluated now.

There are exactly eight positive candidate cases:

1. All fi=0,q=0: UNIT, using the zero identity.
2. All fi=0,q=1: SEPARATOR, e.g. constant-coordinate extraction.
3. f1=1,q=T^5: UNIT with h1=T^25, retaining the full degree25 multiplier window.
4. f1=2T,q=T: UNIT with h1=T^4/2, detecting raw leading-coefficient normalization errors.
5. f1=T^2,f2=1+T,q=1: UNIT since T^2+(1-T)(1+T)=1. The remainder algebra Q[T]/T^2 is nonreduced and must not be radicalized.
6. f1=e+(1-e)T,q=1-e: SEPARATOR by evaluation at v=1,T=0. The v=0 child has f1=1, so dropping it as a zero generator is an incorrect reading of the split.
7. f1=eT,f2=(1-e)T^2,q=T: UNIT with h1=eT^4,h2=(1-e)T^3. This exercises nonconstant coefficient algebra and different maximal-degree pivots.
8. f1=T,f2=T-1,q=1: UNIT via f1-f2=1; all original generators matter.

Every unmentioned fi is zero. The candidate may output any valid full witness of the expected branch, not necessarily the illustrative one above. The harness does not require a particular rank or construction trace to agree with these hand examples.

Exactly four actual-object negatives follow: scale the positive allzero/q1 separator by2; add1 to the raw2T UNIT multiplier at index0; truncate one separator witness; and change only the coefficient modulus to v^7 before calling the candidate. Each object is deep-copied, demonstrably changed, frozen, reread and hash-bound. The expected exact ValueError strings are respectively 'dual target normalization equals one', 'full 217-coordinate sum h_i f_i equals q^5', 'complete rational witness dimension', and the shared delta's 'generic modulus must be squarefree'. An unrelated exception, a subclass, or an accepted changed object is failure, not a successful control.

The scaled separator still annihilates every column but has target value2. Adding1 changes the raw identity by nonzero2T. The truncated witness fails framing before arithmetic, and the v^7 case must fail the candidate's precondition rather than the verifier; those two are not claimed to be complete arithmetic checks. There is no random/property farm, source-fixture generation or extra parser-hardening batch.

## 5. Frozen outputs, failure behavior and remaining dependencies

The future authority's OUTPUT prefix determines exactly twenty sidecars plus its summary. Eight input files are frozen before the corresponding candidate calls; eight returned certificates are frozen before independent verification; four changed-negative files are frozen before their exact refusal checks. Every sidecar uses exclusive creation, read-only permissions, canonical strict JSON read-back and hash checks. The candidate receives parsed copies and input mutation is refused. All sidecar hashes and every registered code/native hash are rechecked before the successful summary is written exclusively.

All twenty-one names must be absent, including dangling symlinks, before any arithmetic import. A failure or cap keeps whatever files already exist, emits no successful final summary, and is not retried. Registered CAPRUN stdout/stderr/telemetry remain the external record of interruption. No deletion, worker operation or supervisor was added. PLAN.md explicitly requires root to authorize these deterministic output names later.

The static package is not runtime-ready. Syntax, the new producer API implementation, installed FLINT semantics, native closure, the actual candidate branches and every checker outcome are UNTESTED. No fixture, matrix, rank, split count, coefficient height, bit length, byte size, wall/CPU/RSS or performance improvement was measured. The design allows at most one nonempty RREF per candidate; enforcing that producer property belongs to the joint static review, not to a fake runtime count here.

Root will collect this terminal harness and the separate terminal producer for one joint FIRST static gate. Any finite AWS generic validation and any later genuine source decision require separate registrations. Generic runtime success would not supply the actual source artifact/checker receipt/ROOT acceptance; the unchanged real checker remains mandatory there.

## 6. Read scope, ownership and completion

The exact fifteen inputs are in PINS.json. READ-SCOPE.md records WHOLE reads, the recovered capped interval, the additive shared error clarification, and the explicit refusal to read the live new producer. All authoring used apply_patch in the two owned targets. No mathematical subprocess of any size was used; the only subprocesses were documentary reads, clocks, hashes and the existing artifact publication protocol. No network/AWS/SSH/process census, agents, shared edits, corpus, Git status or protected project access occurred.

Own generic_controls.py was read WHOLE as text. Its later read-only-freeze/final-rehash additions, PLAN.md and this substantive report are reread before the unique completion marker. These are static traces and custody checks, not syntax or runtime tests. No old/frozen code or shared interface was modified. No physical valid authority, ROOT acceptance, native environment or mathematical result is generated.

OPEN(S) RAISED: none. The remaining obligations are the already-planned joint FIRST static review and separately authorized finite execution; no new campaign mechanism or automatic follow-on is created. The code is UNREVIEWED and all twelve designed outcomes remain UNEXECUTED.

COLLISIONS: EMPTY — exact owned targets absent before begin; own-only marker/raised-OPEN check, no corpus scan. All writers are IDLE after terminal custody publication before00:39UTC.

Own whole-report and PLAN reads completed00:30UTC; final bounded code additions were reread00:31UTC. All fifteen current input pins matched again, and the final report target was still absent. The code SHA at completion is c369dc11b4e9562b050333225133aedf9cb63a04f3309424fdfab769465d363a. These are documentary checks only; all designed runtime outcomes remain UNEXECUTED. No marker preceded this completion line.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `12858`.
- Body SHA-256:
  `f4d01f59403558d62f16f4dafcad692c455330503afbc1bf20ac30d061c20310`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
