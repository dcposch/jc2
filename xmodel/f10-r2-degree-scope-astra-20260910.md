# Conditional r=2 source-zero: exact degree scope

Status: COMPLETE conditional theorem-interface; NEW numerical-column selection, UNREVIEWED. Manual composition only. The actual certificate and its live FIRST review are not inputs. The complete normalized guarded 16r source algebra at r=2 is assumed zero solely as an explicit conditional hypothesis.

Owner: /root/contact_collision_geometry. First action: 2026-09-10 18:39:21 UTC. Hard stop: 2026-09-10 19:04:00 UTC; publication reserve: 19:01:00 UTC. Basis: 0d39df3c9fd69c939a8420c54d03228b9077777d.

## Scope and input custody

The four initially named reports and eight immutable reduction reports located by the expressly authorized bounded lookup were SHA-pinned before fresh WHOLE reads. Exact pins and read intervals are in the owned PINS.json and terminal custody.

## 1. Result: actual degrees 196 and 336, conditionally excluded

Write A_r for the COMPLETE normalized, gauge-fixed 16r coefficient algebra over Q, including every coefficient of both low residuals and the guard omega*a*b=1. Hypothesis H2 is A_2=0. This report neither proves H2 nor reads its current certificate/review.

At the accepted normalization, published completeness, and source-reduction import tiers specified below, H2 implies: for EVERY characteristic-zero field K, no P,Q in K[X,Y] with nonzero constant Jacobian have ACTUAL ordinary total degrees (196,336), in either order. The conclusion is not restricted to pairs already presented in a chosen F10 chart.

The new ingredient is a manual numerical-selection extension of 16m. For every integer r>=1, an actual Keller pair of degrees

    (28(3r+1), 28(5r+2))

necessarily has, after embedding its finitely generated coefficient field into C, an affine-normalized ordinary F10(r-1) representative over C, after output swap; at r=1 only, a competing F9(2) branch is first excluded by the accepted 16m/16h composition. Precisely, A_r is nonzero as a Q-algebra if and only if such a pair exists over C, equivalently over SOME characteristic-zero field. This is NOT existence over each prescribed K. The reverse-to-source endpoint uses 16r's accepted sufficient compact contract, not a backward reconstruction of a printed chain. This is an existence-decision equivalence, not an isomorphism of original pair spaces or a same-field normalizer theorem.

Consequently H2 removes the r=2 member of this exact numerical column and the entire specified ordinary F10(1) source stratum. Together with the already accepted r=1 degree-scope result, the first two column members are excluded (the second conditionally here); no r>=3 member is decided.

## 2. Accepted general normalization and the new exact ratio test

Put m=3r+1, n=5r+2. The identity 5m-3n=-1 proves gcd(m,n)=1; since m>=4 and n>m, neither divides the other. Work over C. The GENERAL given-pair normalization imported and checked in 16m applies to arbitrary coprime nondividing reduced degrees, not just its original column m=q+2,n=2q+3. Its actual proportional rectangles, with positive inverse-coordinate degrees M,N, give

    28=M*a+N*b >= a+b,  0<a<b.

GGV 5.20 uses only identity/constant y-translation here, preserving the whole ordinary rectangles and attained corners. GGHV's actual-chain and complete M=35 table imports therefore apply with a+b<=28. The full set at this bound is exactly F1--F11,F18,F19, as reproduced and independently checked in the charged 16m proof/gate. Corners (5,20), (6,15), (6,18) are impossible by the exact identity (their common divisor 5,3,6 does not divide 28). Only F1 at (4,12) and F9/F10/F11 at (7,21) remain.

The smaller ratio rho_r=(3r+1)/(5r+2) satisfies 1/2<rho_r<2/3 (indeed rho_r<3/5). F1 has ratio (2j+3)/(3j+4)>2/3, and F11 has ratio (j+2)/(3j+5)<1/2 for every j>=0. Both orientations are exhausted by smaller/larger comparison. They cannot occur.

For F9 the exact equality is

    (j+2)(5r+2)=(2j+3)(3r+1)
    iff r*j=r+1.

With r>=1 and j>=0 integral this requires r=1,j=2; for r>=2 the right solution j=1+1/r is not integral. At r=1 the accepted 16m argument supplies every ordinary/actual-chain antecedent of 16h and excludes that F9 branch. No F9 exclusion theorem is needed at r=2.

F10 is printed in the larger/smaller order (5j+7,3j+4). Hence

    (3j+4)(5r+2)=(5j+7)(3r+1)
    iff j+1=r.

This gives the unique permissible printed index j=r-1. Here 28=7M+21N forces M=N=1. Thus the normalizer and subsequent translation are affine; ordinary total degrees of the representative remain 28m,28n. Its entire supports lie in [0,7e] x [0,21e] and attain (7e,21e), for e=m,n.

The actual table chain has A0=(7,21), A0'=(1,0), selected A1=(13/7,3), direction (7,-2), type II.b, k=1, at the starting ordinary pair, exactly as in 16m's general attachment argument. This is not realization of a table row by fiat. The hypothetical ordinary pair supplies the actual chain by the imported theorem; the exhaustive filter identifies it.

Output swap preserves this profile, not merely its ratio. On its (7,-2) face write x^m F(w), x^n G(w), w=x^2 y^7. The positive-weight leading bracket vanishes, giving mFG'-nF'G=0 and G^m=constant*F^n. Thus a selected multiplicity 3m transfers to 3n. The identical horizontal-face identity transfers standardness; the scalar Jacobian changes sign but stays nonzero. This is the charged FIRST 16m gate's intrinsic-face argument, valid for these arbitrary positive m,n. Consequently the printed F10(r-1) antecedents, including both full rectangles, really hold.

## 3. Necessary source, normalization, and the conditional contradiction

The accepted 16p composition consumes the entire specified ordinary F10 stratum via 16k -> 16l/16n -> 16o, not a chosen formal initial or one contact component. With printed index j=r-1 it has compact index r=j+1. It gives whole polynomials A,B in (S,t), exact t-degrees 3,5, coefficient bounds deg A_i<=m-ri and deg B_i<=n-ri, monic base S^m,S^n, and full bracket c*Delta, where

    Pi=t-u*t^2+S*t^3,
    Delta=1+u*t-ell*t*Pi-t*Pi^2, c!=0.

Actual inverse-source ordinaryness forces the reference scalar relation u=-h. Every negative-z coefficient under S=p*z^3-z^2+u*z, t=z^-1 is retained. In particular the term -c*S^2*t^7 is not replaced by a constant target; ordinaryness in (S,t) alone does not suffice.

The accepted 16r field-point equivalence divides the nonzero leading t-scalars to set A_3=S,B_5=S^2. It keeps a=[S^m]A_0 and b=[S^n]B_0 nonzero by omega*a*b=1 rather than normalizing both again. The gauges A_0(0)=[S]B_3=B_0(0)=0 are proved output translation/shear gauges. All upper Euler rows and both whole inverse boundaries are restored by its exact reconstruction; EVERY coefficient of E1 and E0 remains. Values u=0, ell=0 and S=0 are not removed. These operations are defined over the supplied field; the earlier source normalization may have used a finite algebraic extension.

At r=2 this is exactly m=7,n=12, hence original degrees 196,336. Their affine-normalized top monomials have exponents (49,147) and (84,252). In the compressed (S,t) chart the ordinary degrees are 7,12 and partial t-degrees 3,5; the (1,2)-weighted degrees are 7,12. These different degree notions are not interchangeable.

The charged accepted 17zzj producer/FIRST gate identify the WHOLE normalized A_2 with B[s,s^-1][X1,X2,X3]/I19, including all seven leading components after scalar extension, every retained row, and the Laurent scale. B is the full septic field over Q, not one selected embedding. Mandatory accepted correction: the late operator is 7CV'-6C'V+kD', NOT its old displayed -18. No arithmetic from that report is recomputed here. Hypothesis H2 is about the complete algebra, equivalently this complete corrected presentation; a selected component, modular fibre, highest-form ideal, or s=1 slice being zero would not be H2.

An alleged complex pair therefore supplies a unital field point of A_2, impossible if A_2=0. For any characteristic-zero K, its finitely many coefficients generate a finitely generated field over Q which embeds into C; degrees and the nonzero constant Jacobian survive. This proves the all-K nonexistence without asserting a K-defined normalizer. Conversely, if A_r were nonzero, weak Nullstellensatz gives a Qbar-point; 16r's same-field compact converse and its accepted 16q endpoint give an ordinary Keller pair of exact degrees 28m,28n. Nilpotent parameter algebras need only pass to a residue field for that endpoint.

## 4. Known all-r target and one concrete proposed discriminator

The accepted all-r scale cover already gives the exact whole-source target without assuming r=2's special band eliminations extend. For each r>=3, set nu=(5r+2)/(3r+1), let B_nu be its WHOLE finite-etale rank-seven leading algebra, and use the accepted normalized variables Y (6r+2 of them). In B_nu[Y] define

    J_r=(K1_i(Y,1): 1<=i<=6r+1;
         K0_i(Y,1): 1<=i<=7r+1;
         L1-U0*L0),
    L0=K0_0(Y,1), L1=K1_0(Y,1), U0=U(Y,1).

The KNOWN exact all-r target, not a new mechanism, is:

    for every r>=3, is L0 in radical(J_r)?

Equivalently, for each such r does some integer N_r>=1 satisfy L0^N_r in J_r? No uniform bound for N_r is presumed. This is weaker than unlocalized unitness J_r=(1), and exactly sufficient rather than a stronger unrelated target: the accepted faithful covers prove A_r=0 iff (B_nu[Y]/J_r)[L0^-1]=0, and localization is zero iff a power of L0 lies in J_r. Nilpotents and every leading factor are included. Neither finiteness, highest regularity, nor a basis is needed for this equivalence.

Here the retained mixed row matters: it is the slice of z*K1_0-U*K0_0, of weight 8r+3; its redundancy is unproved. The cover is w^(2r+1)=s, L0=w^3, with free ranks 2r+1 and 3. Its low equations come from K0_0=s^7 and K1_0=U*s^5, not source specializations to 1 and U. These ranks are not ordinary source degrees, and B_nu need not be a field. One component's nilpotence is insufficient.

A uniform proof would, using the accepted r=1 result and H2, exclude the entire precise numerical column (28(3r+1),28(5r+2)), r>=1, and the named ordinary F10 source family. A genuine characteristic-zero guarded point at any r would instead reach 16r's sufficient counterexample endpoint. Merely restating this exact localization target is not an advance toward its decision.

The SINGLE concrete proposed next mathematical test is instead the following NECESSARY relaxation, whose unitness is a stronger sufficient obstruction. Before the scale slice, work over the accepted whole leading ring L=B_nu[s,s^-1] with all 6r+2 lower coefficient variables. In 16r notation h=1-u*d+S*v has exact degree 2r+1 and leading coefficient H=1/(W*s^2), a unit already proved on the whole leading ring. Hence hbar=H^-1*h is monic without any new localization. Reconstruct the actual B by the accepted upper Euler equations and set

    T_r=(all coefficients of rem(E0,hbar^2),
         all coefficients of rem(E1,hbar^2))

in that leading-base/lower-variable ring. Ask whether T_r is the unit ideal UNIFORMLY for r>=3. This is a well-defined double-h compatibility test retaining every root and multiplicity of h; neither squarefreeness nor a chosen root is assumed. Monic division works over arbitrary coefficient rings, including nilpotents. Every complete source satisfies these necessary remainders, so uniform T_r=(1) would imply A_r=0. Conversely an exact point of this RELAXATION refutes this stronger proposed mechanism at that r, not the full-source exclusion. No equivalence or success of this test is claimed.

This changes the tested object: each remainder has degree <4r+2, hence at most 8r+4 scalar slots together, versus 13r+4 full residual slots after the two leading rows have been imposed. Coefficient complexity or runtime need not decrease. Derivatives in E0,E1 must be taken BEFORE remainder reduction; differentiation does not descend to an arbitrary hbar^2 quotient. The first discriminating mathematical artifact would be a parameter-uniform contradiction from these two exact remainders, or one exact guarded relaxation point proving this proposed strengthening fails. All coefficients, feasibility and outcome remain unknown. No solver/review farm, execution, or follow-on is authorized.

## 5. Limits, controls, and inherited trust

No maximum-degree-336 theorem follows: excluding one ordered degree pair (and its swap) does not exclude every smaller pair. Nor is this an arbitrary gcd-28 theorem. The precise index relation is j=r-1 in a SELECTED normalized representative, not a source-coordinate-invariant attached to an arbitrary pair. The ordinary F10 family is only one imported source stratum; no charged theorem says all possible Keller counterexamples normalize into it. That is the missing coverage arrow for a global JC2 conclusion even if all r were closed.

Meaningful changed-hypothesis controls, all manual and not asserted Keller examples:

- If one uses only the bound a+b<=28 instead of 28=Ma+Nb, F2's corner (5,20) remains erroneously available. The exact identity kills it. If one drops the ratio hypothesis, F1's corner (4,12) permits M=4,N=1; its valid numerical ratio 3/4 would give degrees (84,112) at gcd 28. Thus the present F10-column filter cannot be promoted to all gcd-28 pairs.
- Identifying the old numerical-column index q with compact r fails already here: 196/28=7 gives q=5 in 28(q+2), but its mate would then be 364, not 336. The separate exact ratio calculation in section 2 is necessary.
- Zero on one chosen leading component is not zero on a whole product algebra: over a split two-factor base, the quotient by the idempotent (1,0) still has its second factor. Similarly k[x]/(x^2) is nonzero although its reduction has x=0. These controls distinguish whole-algebra zero, guard-nilpotence, and selected reduced tests.
- A field point with a=0 or b=0 loses the attained base degree; a point in the compressed plane without the inverse boundary can include t, whose inverse z^-1 is not ordinary. The accepted guard and every inverse boundary therefore remain load-bearing.

Trust perimeter is unchanged: GENERAL given-pair normalization through 15x (Division Lemma/ML ordinary normalization), GGV Definition 4.3/Proposition 5.20 with their cited interiors, GGHV Theorem 2.20 and actual MN membership/Definition 3.3/(3.20), and WHOLE Section 5 M=35 completeness in both orientations, at the charged 16m producer/FIRST gate's imported scopes. The 16k/16l/16n/16o interiors are used only through the accepted 16p whole-source composition. The compact sufficient degree endpoint is consumed through accepted 16r and its FIRST gate. None of those uncharged originals or provenance paths was reopened or freshly audited. Current qualified statuses, not historical provisional headers, govern.

## 6. Read scope, publication, and own-only review

Exactly twelve immutable reports are mathematical inputs; all were hash-pinned before their fresh WHOLE reads in this task. The four initially assigned reports were read to their Seal/marker: r1 degree producer lines 1--92, its FIRST gate 1--77, all-r scale producer 1--123 and gate 1--65. The latter two were additionally reread whole. The authorized lookup located the eight additional reports, whose exact current hashes all matched the accepted entries before reading: 16m producer 1--166 and gate 1--93; 16p producer 1--244 and gate 1--78; 16r producer 1--251 and gate 1--89; 17zzj transfer 1--180 then 181--362 and gate 1--70. No immutable report read was clipped; no prior-turn WHOLE reuse is claimed.

The permitted AUDIT/APPROACHES lookup was metadata/accepted-scope discovery, not a fresh theorem source. Both were prehashed. Bounded heading/name searches returned a few incidental unrelated matches, not consumed as premises. AUDIT lines 19823--19829 resolved 17zzj; a 19180--19250 lookup interval resolved the unusually spelled 16m/16p/16r headings and filenames. Its display clipped unrelated 16o text; no WHOLE ledger or clipped interval claim is made, and the required science was subsequently read from the twelve pinned immutable reports. No broader corpus, current actual-result review, coefficient body, code, process, or protected tree was opened.

Every write is confined to the owned report/transaction/box and uses apply_patch or the existing documentary transaction. No scientific subprocess of any size, worker, network, source edit, or execution authorization. Prior involvement: this author wrote several charged producers; this is a new conditional scope composition, not an independent review of those proofs. ROOT alone handles any future FIRST review or promotion.

## OPEN(S) RAISED

No new canonical OPEN identifier. H2 is an explicit unverified hypothesis here, owned by a different current review. Section 4 separates KNOWN uniform L0-nilpotence from the unproved proposed double-h remainder test. Its coefficients and outcome are not computed. This report gives no follow-on authority.

## COLLISIONS

status: EMPTY

Owned destinations were absent before the lease. The old r1 degree conclusion is reused as accepted scope, not presented as new; the new ratio-selection column and its conditional r2 consequence are identified explicitly. No corpus-wide novelty claim. Own WHOLE and own-only OPEN/collision checks are complete; all twelve mathematical input postpins match. Terminal custody records the exact pins and expected transaction. H2 and the proposed double-h test remain explicitly unproved here.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `17469`.
- Body SHA-256:
  `e9be84f41d76428b23013e05ac187eba55f86c781d820e047dd968b17a8698be`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
