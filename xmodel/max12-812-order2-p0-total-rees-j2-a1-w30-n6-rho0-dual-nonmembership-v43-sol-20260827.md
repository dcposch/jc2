# V43 weight 30: exact rho-zero dual excludes exponent six

Date: 2026-08-27  
Author: Sol total-lift design lane  
Status: **EXACT Q NONMEMBERSHIP CERTIFICATE; FROZEN V43 NEGATIVE**

## Headline

The exponent-six V43 question has a negative answer.  An exact rational
Macaulay functional `Lambda` satisfies

```text
Lambda(a1^6) = 1,
Lambda(m*Tg_g_j|_(rho=0)) = 0
```

for every complementary-weight monomial `m` and every frozen raw row through
grade 19.  The validator reconstructed and replayed all 284,766 nonzero
rho-zero weight-30 products, not only the 26,200 rows in the target incidence
component.  Therefore

```text
a1^6 is not in J0.
```

Specializing `rho` to zero now shows that there is no total-rho identity

```text
a1^6*U(rho^2) in J,       U(0)=1,
```

and, more generally, no `a1^6*(1+rho*W)` identity.  This is a rigorous
negative answer to the frozen V43 discriminator; it is not a negative answer
for higher powers of `a1`.

## 1. Rings, grading, and the exact implication

Use the corrected literal alphabet

```text
X19^tot = X19^0 union {ez9},       |X19^tot|=66, |X19^0|=65,
S       = Q[rho,X19^tot],
R0      = Q[X19^tot],
sp      : S -> R0,                 rho |-> 0.
```

Every positive variable has its pinned sigma weight and `rho` has weight
zero.  The total raw ideal `J` is generated by the 70 named rows through
grade 19 after `rs=cs=c0=c1=a0=0`; 59 of these rows are nonzero.  Its special
fibre is `J0=sp(J)`.  The extra variable `ez9` is a spectator in `J0`, since
its sole total occurrence is `(3/8)*rho^2*a1*ez9` in `Tg19_2`.

The specialized rows are sigma-homogeneous.  Consequently membership of the
homogeneous target `a1^6`, of weight 30, would have a representation using
only complementary-weight homogeneous pieces of the multipliers.  Thus the
complete weight-30 Macaulay span decides the unrestricted polynomial-ideal
membership question for this target.

Let `C30` be the 284,766 nonzero products obtained with no total-degree or
support cutoff.  The incidence component of `a1^6` contains

```text
26,200 product equations,
66,076 monomials including a1^6,
66,075 dual unknowns after fixing Lambda(a1^6)=1,
616,678 nonzero integer matrix entries.
```

The exact LinBox solve returned a functional with common denominator

```text
34670334774018800828908752076800000
```

and support 3,395, including the target.  Integer replay inside the solver
checked the cleared system.  The independent Python validator then rebuilt
the literal total rows, re-specialized them, rebuilt `C30`, checked all row
hashes and censuses, and evaluated every product over `Q`.  All evaluations
are zero and `Lambda(a1^6)=1`; hence `a1^6` cannot be in the span, so it is
not in `J0`.

If `a1^6 U(rho^2)` lay in `J` with `U(0)=1`, applying `sp` would give
`a1^6 in J0`, a contradiction.  The same argument applies to any factor
`1+rho*W`, without an even-rho assumption on `W`.  No saturation/localization
interchange or Hensel assertion is used in this negative direction.

## 2. Replay controls

The exact validator performed the following fail-closed controls:

- reconstructed all 70 frozen row hashes and all regenerated total-`t` row
  hashes from the pinned literal emitter;
- checked the complete 284,766-product span as well as the 26,200-product
  target component;
- required the exact functional value `[1,1]` on `a1^6`;
- changed the `a1^6` coefficient of product 223,032, a `Tg15_3` product, by
  one and detected exact residual `[1,1]`; and
- required normal exit, no timeout/OOM/failure marker, and a complete result
  and evidence manifest.

The finite-prime `F_65519` lane was still in independent replay when this
exact-Q report was frozen.  It is corroboration only and is not needed for
the conclusion.

## 3. Frozen local evidence

The compiler matrix and coordinate map are harvested at

```text
cases/max12_812_order2_p0_total_rees_j2_a1_total_dvr_w30_v43_20260827/
  aws_r6d_rho0_dual_compiler_20260827T085739Z/
```

with hashes

```text
3ddea4ae8b172fcc43136494f1080756f5d04f183ac61a7a8dcd13b1d4e2f737  HARVEST.sha256
25f580911a9203f9de2158b18773d6ed1d79440c5f4fb19e330a7c4f060f3b9a  compiled/compiler_result.json
8e0431298fa1fc015537ed6a02a7f31710f22eaab54b3da0d77c540d8eaecae4  compiled/rho0_dual_full_component.sms
be700f9999a42784f172010b69be0ed01672915c6ac59d5a851ec0319cb0cb99  compiled/rho0_dual_full_component.rhs
afa0192510f66c6a648f32f5ea898a4d23a8e032e0e34265f1861ab15bf84460  compiled/rho0_dual_unknown_monomials.json
```

The exact solve, full replay, certificate, resource records, and AWS evidence
manifest are harvested at

```text
cases/max12_812_order2_p0_total_rees_j2_a1_total_dvr_w30_v43_20260827/
  aws_r6d_rho0_dual_exact_resume_20260827T091000Z/
```

with hashes

```text
98b41d273b8a4ab20749e702ad14490f52ac772908a6d7b9df29a6302cbee0ab  HARVEST.sha256
8654abe53a36e8c6d0a430baac87535bca83f4710ec8ddd7a99f4f4f8010c5ab  AWS_SOURCE_MANIFEST.sha256
acb72647ac1e0a9e6adfdd802a3769f6beb211033b1b93aa4fb67bbe63ea529f  EVIDENCE.sha256
d063cdefe7802b6ca040f4ae3a4d44c4973baec0481b1383ddf3ed22c34fdfff  RESULT.json
d0c04857c1b58d6bc8671eada4f8a7d2001c945be2a13d08ebde2fd53b4cf713  rho0_dual_exact.solution
6674abaac330754c35e7a8290b0a20c9a7745fd184bce14e8f87e52c4561a9da  validated/rho0_dual_exact_certificate.json
```

The exact run ended with status token
`PASS-A1-TOTAL-DVR-W30-V43-RHO0-DUAL-LINBOX-EXACT-REPLAY`, outcome
`exact-Q-nonmembership`, after 928 seconds.  There is no failure marker in
the text logs.

## 4. Compiler and theorem custody

The launched compiler SHA is

```text
0de6a2b279aeeab8c5a635c1d7658958f3f49f0f94f8473355d281cae9438b00
  compile_total_dvr_w30_v43.py
```

Its additive delta from the previously reviewed `c7ed7309...` producer was
subsequently confirmed on all five requested checks by Fable5:

```text
ae8ecf1887a62afd873fc795d0eeec50ff0aac6e4954813931d09a34c8bfc31a
  xmodel/max12-812-order2-p0-total-rees-j2-a1-v43-compiler-postreview-delta-hostile-review-fable5-20260827.md
```

That review verifies the local compiler delta and its dependence on the
earlier theorem review.  It expressly does not review the remote census or
AWS result; those are established by the frozen compiler output and exact
replay above.  The literal-alphabet preregistration correction is additive
and does not change the frozen N=6 question.

## 5. Scope firewall

This result excludes exponent six only for the full frozen grade-through-19
raw ordered-`T-a1` ideal.  Together with V38 it excludes powers at most six,
but it does not exclude a higher `a1` power, decide the still-running
selected-cascade total dehomogenized lane, prove source/landing coverage,
establish Gate T, close order two or maximum twelve, or prove JC2.

No higher weight/cutoff was launched before the evidence above was frozen.
No canonical ledger or `jc2-lean` artifact was touched by this lane.
