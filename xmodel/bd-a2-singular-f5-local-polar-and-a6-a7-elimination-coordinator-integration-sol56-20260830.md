# Binding integration: singular F5 balanced polar and A6/A7 elimination

Date: 2026-08-30 UTC  
Coordinator: Sol 5.6 Ultra  
Frozen integration basis: `3faa3a10ca9b039fa23813d0b5033e11a6aefdd6`  
Lifecycle: **BINDING BALANCED LOCAL THEOREM / UNBALANCED AND EFFECTIVITY ROWS OPEN**

## 0. Evidence, custody, and disposition

This integration binds the sealed producer

```text
db3fbb623f8f7f1843506daafc4374dfd87d3a5365773bfd2859cb17f1015e77
  xmodel/bd-a2-singular-f5-local-polar-and-contracted-multiplicity-sol56-20260830.md
  body ba54edd2f1dc28b6f4222aeb371f7b4f7379863c7db867d14bfcf78cab86d7b2
```

to the independently reconstructed GPT-5.5 xhigh hostile review

```text
66cbf818a778a2b48137e89b29d09256a3aa45c8659931e4eb8373791a12b185
  xmodel/bd-a2-singular-f5-local-polar-and-a6-a7-elimination-hostile-review-gpt55-20260830.md
  body 17619 / 95f767dcf5bf9c9a45f2da09a16e1744bd4cc49c14dc9c5fb1e1a607e449fb60
11f14900e244108b0f5676da1bdc732469d0005d86cdb43ef8c7edfebe25d1fb
  xmodel/bd-a2-singular-f5-local-polar-and-a6-a7-elimination-hostile-review-gpt55-20260830.run.v2
```

The receipt records exact GPT-5.5, xhigh reasoning, launch basis
`ed261f7ffb18daa2abb948429c2ebd4148a93622`, exit code zero, and final status
`DONE`. Before reading the report, root reproduced the prompt, adapter,
launcher, charge-validator, fallacy, raw report, and raw log hashes, and then
reproduced all seven charged input hashes. The receipt says
`charge_basis_status=ABSENT` because the prompt did not use the validator's
machine-readable charge marker. This is an operational prompt-format defect,
not missing mathematical custody: the prompt printed each charged path and
hash, the reviewer checked them against commit
`2275c1f490517e844572f7b532bde36ecce8cc20`, and root independently repeated
that check. Future prompts should use the validator marker.

During the active lane, one broad root text search displayed a few lines of
the append-only log. They were verbatim echoes of already charged inputs, not
the review conclusion; no lane input, report, receipt, or Git index was
modified. The different-model process and its immutable hash-pinned inputs
were unaffected.

The review returned `CONFIRM_WITH_CORRECTIONS` and found no mathematical gap.
Root also independently reconstructed the decisive `A7` and `A6` Cartan,
Newton, physical-site, graph-cycle, and Euler calculations. This integration
adopts the review's wording repairs and promotes the balanced theorem below.

## 1. Exact balanced local polar theorem

Work over `C` in the charged normal, reduced, finite-near-infinity F5
quadratic-incidence first-leg scope. At the unique singular F5 point use

```text
h=z(z-v)((1+v)z-v),
f=h+u(a(z)u+b(z)v+c(z)),        c(0)=0.
```

The balanced rows are the nondegenerate target-Hessian cell `b(0)!=0`.
After a unit and constant target shear, the parametric Morse lemma gives

```text
f=XY+phi(z),                    ord_z(phi)=s,
B_s/A_(s-1),                   5<=s<=9.
```

The inverse source coordinates have the principal form

```text
v=z+X+O(z^2,zX,zY),
u=u_0(z)-zX+Y+O(z^2X,zY),       ord(u_0)>=3.
```

With `omega=dX wedge dz/X` and `du wedge dv=r omega`, put `P=Xr`. The exact
lower Newton data are

```text
P=unit*X^3-unit*zX^2+unit*phi'X+unit*phi+higher,
lower hull: (3,0)--(2,1)--(0,s).
```

Here `higher` means strictly higher for the two Newton valuations after using
`XY=-phi`; it is not merely an informal total-degree truncation. The first
edge has one simple nonzero root. The second edge is
`X^2=unit*z^(s-1)`. Thus the strict source different has coefficient one on
every strict prime and factors exactly as

```text
s odd:   three reduced germs, A-degrees 2+3+3;
s even:  two reduced germs,   A-degrees 2+6.          (1.1)
```

The total degree eight is independently the exact length

```text
length C[[v,z]]/(h,h_z)=2+3+3=8.
```

It upgrades the lower orders from the inverse expansion to the exact degrees
in (1.1), rules out hidden strict components, and keeps the even Puiseux
factor reduced with Cartier coefficient one.

## 2. Exceptional vectors and physical sites

Number the `A_(s-1)` chain with `E_1` adjacent to the strict F5 fibre
component `L`. Then

```text
m_i=ord_(E_i)(r)=min(i+1,s-i),
n=C_(A_(s-1))*m,

s odd:  n=e_1+2e_((s-1)/2),
s even: n=e_1+e_((s-2)/2)+e_(s/2).                  (2.1)
```

The degree-two germ meets a smooth point of `E_1`. For odd `s`, the two
degree-three germs meet two distinct smooth points of the middle component.
For even `s`, the degree-six germ passes through the node between the two
middle components. A node contributes two entries to `n`; it is still one
analytic germ.

There are two explicit noncancellation checks. At an odd middle component,
the tied initial functions `zX` and `Y` are nonproportional functions on that
component. At `E_1`, `X^2` and `zX` tie, but their initial form is
`X(X-lambda z)`, a nonzero function on `E_1`. The latter sentence repairs an
omission in the producer's central-tie explanation; no vector changes.

The exact rows are

| tag | `m` | `n` | strict degrees |
|---|---|---|---|
| `B_5/A_4` | `(2,3,2,1)` | `e1+2e2` | `2+3+3` |
| `B_6/A_5` | `(2,3,3,2,1)` | `e1+e2+e3` | `2+6` |
| `B_7/A_6` | `(2,3,4,3,2,1)` | `e1+2e3` | `2+3+3` |
| `B_8/A_7` | `(2,3,4,4,3,2,1)` | `e1+e3+e4` | `2+6` |
| `B_9/A_8` | `(2,3,4,5,4,3,2,1)` | `e1+2e4` | `2+3+3` |

## 3. Global prime count and exact eliminations

Pass to one common embedded SNC resolution of the reduced full first-leg
boundary. Every displayed exceptional component belongs to that boundary,
and every strict polar germ is a branch of a boundary ramification prime.
If one global irreducible prime carried two germs at distinct physical sites,
its proper transform and the unique path between the sites in the connected
exceptional tree would form a graph cycle. Two distinct points on one
exceptional component give parallel edges, while a germ through an
exceptional node merely moves onto a subdivision after blowup. Neither case
evades the cycle.

The charged boundary is a rational forest. Therefore

```text
s odd:   k>=3;
s even:  k>=2,                                       (3.1)
```

where `k` counts distinct reduced nonexceptional ramification primes. This is
not the false general inference that distinct analytic germs always define
distinct global primes; the physical two-site boundary cycle is essential.

The binding Euler theorem gives

```text
rho+k<=8,                 rho=(s-1)+r_aff.
```

Consequently

```text
B_7/A_6:  rho>=6, k>=3 -> impossible;
B_8/A_7:  rho>=7, k>=2 -> impossible;
B_9/A_8:  rho>=8, k>=3 -> impossible.                (3.2)
```

The `A8` row was already dead from `k>=1`; the new binding eliminations are
`B_7/A_6` and `B_8/A_7`. The surviving balanced rows sharpen to

```text
B_5/A_4: r_aff+k<=4, k>=3, hence r_aff<=1;
B_6/A_5: r_aff+k<=3, k>=2.                           (3.3)
```

No row in (3.3) is asserted effective.

## 4. The q=6 balanced modulus correction

For `B_3/A_2`, put `v_0=tau*z+O(z^2)` and `sigma=1-tau!=0`. The homogeneous
polar polynomial factors as

```text
(T-sigma)(T^2-(1+2sigma)T+sigma(sigma-1)).            (4.1)
```

For `tau!=0` and `Delta=9-8tau!=0`, it has three distinct nonzero physical
roots on `E_1` and exact degree partition

```text
4+2+2,                 m=(2,1), n=3e_1.              (4.2)
```

At `Delta=0`, the quadratic block is tangent and higher coefficients decide
whether it is one irreducible germ or two tangent germs. At `tau=0`, two
nonzero roots remain at distinct `E_1` sites while the residual block moves
to the opposite endpoint and is higher-coefficient dependent. The universal
safe conclusion is only `k>=2`; the generic subcell has `k>=3`. There is no
coefficient-independent singular `3+5` partition.

## 5. Contracted-carrier multiplicity

At the generic point of an effective affine coefficient-basepoint carrier
`Z`, write the exact target-degree-two local equation over `K=C(Z)` as

```text
F=sP+tQ+s^2A+stB+t^2C.
```

After choosing `P!=0`, dividing by it, and solving
`s=sum_(j>=1)a_jt^j`, the Jacobian is `partial s/partial z`. Therefore

```text
nu_Z=min{j>=1: a_j is nonconstant in K},
1<=nu_Z<=4.                                           (5.1)
```

The four stages are respectively controlled by `Q`, then `C`, then `B`, then
`A`; if all remain constant, generic finiteness fails. If `nu_Z>1`, the
first-jet pencil is rank one and the proportional cubic sections have a
common zero scheme of length three on `Z`, contained in the singular scheme
of the incidence. This is not an ADE-rank bound and does not prove a carrier
exists.

## 6. Scope and immediate successor

Promoted here: the full balanced `B_5`--`B_9` polar factorization, its
exceptional and physical-site data, the global-prime lower bounds, the exact
elimination of `B_7/A_6`, `B_8/A_7`, and `B_9/A_8`, the modulus-dependent
`B_3/A_2` correction, and the generic contracted-carrier range (5.1).

Not promoted: the provisional `U_3/A_3`, `U_5/D_5`, or `U_6/D_6` polar
patterns; effectivity or proximity of any surviving tag; an incidence
equation realizing a tag; a finite cubic algebra; a polynomial map; a
counterexample; or JC2.

The exact successor is the full-family square-completion/Newton calculation
for the three unbalanced rows. In parallel, the reviewed balanced data may be
inserted into the ruled total-transform class system. Any provisional empty
lattice census remains nonbinding until its local vectors, physical sites,
Euler allocations, and forest intersection requirements are frozen and
independently reviewed.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `9593`.
- Body SHA-256:
  `bf17bc52ede2d4fbbf947781d80ec7594b56bc149d6e0e3820e272ea2784e956`.
- Frozen basis: `3faa3a10ca9b039fa23813d0b5033e11a6aefdd6`.
