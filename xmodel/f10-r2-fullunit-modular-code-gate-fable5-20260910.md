# FIRST Fable 5.1 gate: positive-only full-source modular unit instrument (static code review)

status=UNSEALED (no Seal, no charge_basis authored; adapter seals)
lane=f10-r2-fullunit-modular-code-gate-fable5-20260910
gate_model=claude-fable-5-1
first_action_utc=2026-09-10T16:43:28Z (date + sha256sum); stop = earlier of ROOT UNIT LAUNCH+18 min and 17:02:00Z USER TERM (KILL 17:02:05Z); final-3-min reserve entered no later than 16:59:00Z; never reset
mode=STATIC; manual reasoning/text/hash only; ZERO subprocess/import/AST/syntax/compile/test/CAS of any size; tools date, ls, wc, cat, sed, sha256sum, apply_patch only
owned=xmodel/f10-r2-fullunit-modular-code-gate-fable5-20260910.md, box/f10-r2-fullunit-modular-code-gate-fable5-20260910/ (both absent at 16:43:28Z; custody file first, then this skeleton and body)
charged_input=box/f10-r2-fullunit-modular-code-astra-20260910/entry.py
charged_input=box/f10-r2-fullunit-modular-code-astra-20260910/produce.py
charged_input=box/f10-r2-fullunit-modular-code-astra-20260910/check.py
charged_input=box/f10-r2-fullunit-modular-code-astra-20260910/CONTRACT.md
charged_input=xmodel/f10-r2-fullunit-modular-code-astra-20260910.md
charged_input=box/f10-r2-reconstruction-code-astra-20260910/authority.py
charged_input=box/f10-r2-reconstruction-code-astra-20260910/arithmetic.py
charged_input=box/f10-r2-reconstruction-code-astra-20260910/check_arithmetic.py
charged_input=box/f10-r2-fullunit-modular-code-gate-prep-20260910/baseline-produce.py
charged_input=xmodel/f10-r2-modular-fullsource-route-astra-20260910.md
charged_input=xmodel/f10-r2-modular-fullsource-gate-fable5-20260910.md

## 0. Custody, read scope, disclosure

All 11 snapshots in /tmp/jc2-lane.vUP2uM/inputs (distinct basenames) were hashed by sha256sum at 16:43:28Z BEFORE any body read; all 11 digests equal the charged pins byte for byte. The rows were generated from the sha256sum output into box/input_custody.md; this body carries no hex digest. Both owned targets were absent at 16:43:28Z. All 11 files were then read WHOLE with cat -n or sed in bounded reads: entry.py 142 lines, produce.py 216, check.py 198, CONTRACT.md 64, Astra code report 48 (ends at its Seal), authority.py 77, arithmetic.py 277, check_arithmetic.py 208, baseline-produce.py 285, route 392 (two bounded reads 1-200 and 201-392, both complete), prior fullsource gate 75 (ends at its marker). No read was clipped, so no recovery read was needed. baseline-produce.py is treated as the byte-identical alias of the original producer per the packet, not as a new source. Nothing was executed, imported, parsed, compiled or hashed beyond sha256sum of the inputs; no baseline body, candidate, worker, native inventory, network, Git, process, other lane or live file was touched.

Disclosure: this is a different-model FIRST static review of Astra's new unexecuted isolated entry/produce/check. The prior fullsource gate (an input here) and the properness/cyclic gates it cites were authored by Fable 5.1 in earlier sessions; they are consumed at their accepted scope and not re-proved. The conditional lift theorem (fullsource route sections 3-4 with that gate) is imported at scope, not rerun. The original 1652675-byte baseline is CHECKED_BUT_NOT_PROMOTED and its actual-result review is separate; no coefficient body, hash of a candidate, or source outcome is requested or asserted here.

## 1. Verdicts

| item | verdict |
|---|---|
| A entry: exact 17-argv/8-flag vector, non-shadowing layout, metadata/hash/baseline binding before math import, distinct arithmetic per mode, strict wire, no fake authority, distinct job tag, original-schema match | CONFIRMED |
| B literal tau=2/7 P7 in both modes, p=89 primality, simple root 0, all seven denominators before projection, exact 20 source polynomials, six-variable support/weight, z=1 exactly, retained slots, mixed row weight 19 class 4, integrality conservative-sufficient only | CONFIRMED |
| C positive-only 247x754 (755 augmented) cut, no dropped product, deterministic elimination/back substitution, own literal sum=1 readback, failure only INCONCLUSIVE_NO_CERTIFICATE/exit 2, no completeness or Macaulay premise | CONFIRMED; the three documentary constants 247/424/330 are verified by hand below |
| D checker consumes no matrix/pivot/rank, rebuilds 18 generators and mixed row, canonical 18 cofactors with exact caps/classes, dense 31x16x11 all-position identity, matches the accepted 18-row ideal, success only FULL_SPECIAL_UNIT_CHECKED_NO_SOURCE_OUTCOME, no lift | CONFIRMED |
| E exact status distinctions, receipt/candidate digest binding, pre/post rechecks, readbacks, size caps, no retry/prime/cut escalation | CONFIRMED; one non-ValueError STOP path noted, not blocking |
| F negative control design: existing-term +1 mod 89 with deletion at 0, all slots/classes preserved, whole-byte restoration, guaranteed final-identity failure, exact exit-1 traceback | CONFIRMED (design only, unexecuted) |

Smallest blocking fix: none. REFUTED: nothing. GAP: nothing in the code itself; the declared unknowns (runtime, actual place validity, whether this cut contains a witness, the separate WHOLE special highest certificate) are Astra's own and stay UNKNOWN.

## A. Entry boundary and layout

CONFIRMED. entry.py line 38 requires len(argv)=17, the eight flags in the fixed order --registered-job, --root-registration-sha256, --self-sha256, --entry-sha256, --arithmetic, --baseline, --certificate, --receipt at the odd positions, and argv[2] equal to TAG f10-r2-fullunit-modular89-v1, which differs from authority.py's f10-r2-reconstruction-v1. Three pins must be 64 lowercase hex and not all zero. The four path values, argv[0] and the entry file must be absolute, symlink-free (realpath equals path), short, quote/newline free and six-way distinct. sources binds the running role file to --self-sha256, the loaded entry file to --entry-sha256, and the arithmetic path to a hard-coded hash per mode; those two hard-coded digests equal the packet pins of arithmetic.py (produce) and check_arithmetic.py (check), which I compared against the sha256sum output. recheck() hashes all three sources (1 MiB cap) and the baseline (15 MiB cap, exact 1652675 bytes and the dac655a7 digest) with O_NOFOLLOW regular-file reads and a before/after fstat identity check. All of this, the receipt/certificate collision checks and the 4300-digit guard run before any scientific import: arithmetic(ctx) is the first statement of candidate() and verify(), both called only after enter() returns. hashlib/stat/json/importlib are the only imports inside enter. The arithmetic module is loaded by explicit file path under a private module name, so the original directory is never on sys.path and its authority.py/produce.py cannot be imported or shadow anything; with -E -s -S -B and a sibling directory holding only entry/produce/check, the bare import of entry resolves to the pinned sibling and stdlib names are unshadowed. Produce uses only Q, qa, qs, qm, wire_q from arithmetic.py; check uses only Rational, plus, times from check_arithmetic.py; no field/P7-dependent helper is called, so the None module globals are never touched. Both ultimately rest on stdlib fractions.Fraction, which is interpreter-pinned by ROOT, not a shared mathematical helper. The entry docstring and CONTRACT state that the self-supplied hashes and the EC2 DMI string are bindings, not authentication; the new entry cannot authenticate a future ROOT invitation, and TOCTOU on same-user writers is delegated to ROOT's frozen staging (which must also exclude any __pycache__ from the sibling directory, already implied by 'exact three-file sibling directory'). Original-schema match via baseline-produce.py lines 248-268: format, job_tag, interface variables/weights/base/basis/scale/targets/guard/rows (Psi5-7, K1_1-6, K0_1-8, K1_0-U*s^5, K0_0-s^7), field.P7 with eight entries, rows.Psi/K1/K0 of lengths 3/7/9 and source.U are exactly what produce.py lines 52-82/116 and check.py lines 56-83/118 demand; wire_p emits [six exponent strings 0..16, seven rational strings] sorted by exponent with no zero vectors (add/mul/sub drop zero entries), and the entry encode() reproduces the original dumps(sort_keys, compact)+newline ascii bytes, so the canonical-baseline equality cannot spuriously fail.

## B. Place, source authentication, projection

CONFIRMED. dl, kl, ga in produce.py 40-44 and check.py 46-48 are literally baseline-produce.py 20-25 at tau=2/7; the septic is the same three-term sum (2 kl^2 -140 tau (1+tau,-6)kl dl + 245 ga dl^2), reassociated only, with the leading coefficient -245*120*144*tau checked and monic normalisation by that value; produce uses qm/qa/qs, check uses times/plus with list comprehensions, and each is compared to all eight parsed baseline P7 strings. Primality: produce trial-divides 2..9 (81<=89<100), check tests 2..9; both are complete for 89. Root 0: produce evaluates P7bar and its derivative by Horner at 0, which reduce to coefficients 0 and 1; check reads them directly with beta==0 asserted; both go through sufficient (Inconclusive). Every rational (eight modulus coefficients and all seven coordinates of every term of all twenty polynomials) is reduced with a checked 89-unit denominator before the Z=0 projection; the checker multiplies the j>=1 residues by pow(0,j,89), which is 1 at j=0 and 0 otherwise, so killed coordinates are still integrality-checked. The twenty polynomials are Psi (3, weights 5,6,7), K1_0..6 (14-i), K0_0..8 (16-i) and source.U (3), each term with six canonical exponent strings, e[4]=e[5]=0 (no S/vartheta) and full weight e0+2e1+3e2+5e3 equal to the row weight, strictly increasing exponents and a nonzero dense vector. Projection is key=e[:3], summing over the z-degree: exactly z=1, never X1=1 and no truncation. Slots: psis + k1[1:] + k0[1:] + [mixed] is 18 with empty dicts retained; mixed = K1_0 - U*K0_0 computed mod 89 in both files; its terms have weight 14-5a or 19-5(b+c), so the bound 19 and class 4 hold, and the per-slot check uses weight<=h and weight mod 5 = h mod 5. The reduction is the ring map Z_(89)[Z]/(P7) -> F_89, Z->0, well defined because P7bar(0)=0 is checked, so reducing U, K1_0, K0_0 and then forming the mixed row equals reducing the mixed row; unit denominators in the Z-basis are a sufficient membership test for the local order, and neither CONTRACT nor report claims necessity or integrality of unused baseline rows. The 4096/50000 caps cannot fire falsely: wire_p enforced them across the whole original artifact.

## C. Producer cut and elimination

CONFIRMED. monomials(cap, c) enumerates i<=cap, 2j<=cap, 3k<=cap, so every monomial of weight<=cap is covered. Hand count with p3(n)=round((n+3)^2/12) monomials of weight n in weights 1,2,3: rows (weights 0,5,...,30) 1+5+14+27+44+65+91=247; f blocks h=5,6,7 with caps 25,24,23 and classes 0,4,3 give 156+141+127=424; the fifteen g blocks at cap 12 give 20 (class-0 generators, three slots), 16 (class 1, two), 13 (class 2, two), 29 (class 3, four), 24 (class 4, four): 60+32+26+116+96=330; total 754, augmented 755. So the runtime guard at line 147 is consistent and cannot fire spuriously. Products: f blocks have weight<=30-h+h; g blocks have crude bound 12+19=31 with class 0, hence <=30; line 155 additionally refuses any product outside the row inventory. Elimination is forward only, column-major, first nonzero row at or below rank, Fermat inverse with readback, normalised pivot, rows below reduced; the invariant that rows at or below rank vanish in every earlier column (pivot and non-pivot alike) holds, so line 174 is exactly consistency of the augmented system and back substitution over j>col with free coordinates zero yields a solution. Line 192 recomputes the literal sum of all 18 cofactor-generator products and requires the polynomial 1. Inconsistency, dimension mismatch, denominator, place or shape failure all raise ValueError (Inconclusive is a subclass) and main maps every ValueError to INCONCLUSIVE_NO_CERTIFICATE with exit 2; there is no rank-based properness claim, no larger cut, no second prime, and no theorem on completeness of this cut is used or needed: a checked identity is a unit witness by itself.

## D. Independent checker

CONFIRMED. check.py reads only the certificate bytes and the pinned baseline; it never imports produce.py or arithmetic.py (entry.arithmetic loads check_arithmetic.py by path in check mode, pinned by the hard-coded digest), and it consumes no matrix, pivot list, reduced row or rank: the certificate schema (line 133-134) has no such key and any extra key is rejected. It rebuilds P7 with check_arithmetic plus/times, re-parses all twenty source polynomials with its own rational/residue/reconstruct code, forms the mixed row from its own first[0], uu, second[0], and checks the 18 slots with degree list 5,6,7,13..8,15..8,19 and caps 25,24,23,12x15, which equal the producer's weights/limits. Cofactor wire: 18 indexed objects with exactly generator and terms keys, labels in the fixed order, exponents three strings 0..30, strictly increasing, residues in 1..88 (so zero terms cannot appear), per-slot and total term counts <=754, and per-term weight<=cap with weight+degree divisible by 5, all before any arithmetic. The identity is accumulated in a fresh dense 31x16x11 integer box after each product is required to have weight<=30 and class 0 (x<=30, y<=15, z<=10 then fit); afterwards EVERY position is compared with 1 at the origin and 0 elsewhere, so absent, outside-simplex and cancelled positions are all examined and no row-space, quotient or evaluation trust remains. The 18 generators are exactly the accepted ideal of the fullsource route section 2 and prior gate items B/D: Psihat_h(Y,1) for h=5,6,7, K1_i(Y,1) i=1..6, K0_i(Y,1) i=1..8 and the mixed row L1-U0*L0 as one polynomial, with the identification of the baseline rows Psi/K1/K0/source.U with those names an accepted-interface import, not re-derived here. Success writes only FULL_SPECIAL_UNIT_CHECKED_NO_SOURCE_OUTCOME with science_outcome NONE; CONTRACT lines 52-54 and the report state that a lift to zero source needs the separate WHOLE special highest certificate, the integral model and the accepted cover, and that generic regularity or guarded nilpotence is not a substitute, matching the route's sections 6-7 controls.

## E. Status semantics, binding, caps

CONFIRMED. produce.main catches every ValueError from candidate (Inconclusive included) and emits INCONCLUSIVE_NO_CERTIFICATE then SystemExit(2); on success it rechecks sources/baseline, writes the certificate exclusively (2 MiB cap, 0444, fsync, readback) and a CANDIDATE_UNCHECKED receipt carrying its SHA. check.main catches only Inconclusive, rereads the certificate to prove no drift, and emits INCONCLUSIVE_FIXED_PLACE with exit 2; every other ValueError (malformed wire, canonical mismatch, class/cap violation, the final identity) propagates uncaught with a traceback and exit 1 and no receipt. Receipts (32 KiB cap) bind status, mode, prime 89, root 0, the ROOT registration pin, the three absolute source paths with their digests, the baseline digest and byte count, and the certificate digest; receipt() calls recheck() first, so a source or baseline change between start and publication aborts before any receipt exists. Noted, not blocking: a baseline whose top-level shape lacks an expected key would raise KeyError/TypeError rather than ValueError in either mode, giving exit 1 with no receipt; since the baseline is bound to the exact 1652675-byte digest of an artifact whose keys are fixed by baseline-produce.py lines 248-268, this can only happen if ROOT pins a different file, and STOP without receipt is then the correct outcome (CONTRACT line 28). Prime, root, cut 30, cap 12 and the dimension constants are literals; no fallback, retry, second prime, higher cut or farm exists in the code, and nothing in the source can guarantee that the fixed place is sufficient or that a witness exists in this cut.

## F. Negative control design

CONFIRMED as design. Because the checked identity sums to 1, some slot has a nonzero generator and a nonzero cofactor. Adding 1 mod 89 to one existing term of that cofactor (deleting the term exactly when 88 becomes 0) keeps the exponent, hence the cap and class checks, keeps sorted order, keeps all 18 slots and term bounds, and after canonical reserialisation passes every check before line 181; the products of the changed term have the same exponents as before, so the weight/class/box checks still pass. The identity changes by 1*Y^e*g with g nonzero in the domain F_89[Y1,Y2,Y3], so some box position differs from its target and line 181 must fire; inserting a constant into a nonzero-class cofactor would instead fail the class check at line 162, which the design correctly forbids. Restoration re-inserts the original term at its sorted position (or restores its residue) and reserialises; since the original certificate was canonical (line 136), the bytes are identical, which is the required whole-byte restoration. Expected exit-1 signature for the future wrapper: no receipt written, certificate untouched, stderr ending with three frames of the absolute check.py path, line 198 in module calling main(), line 188 in main calling verify(ctx,raw), line 181 in verify raising, then the final line ValueError: CHECK FAILED: fixed-place literal full-source unit identity. The frame lines and final message are fixed by the pinned file; caret decoration lines vary with the interpreter minor version and the path is absolute, so ROOT should bind return code, the three frame line numbers and the final line, not a stderr digest. No mutator, fixture or run is provided or executed here.

## OPEN(S) RAISED

None. No new canonical OPEN. Retained unknowns are the producer's own: actual runtime, validity of the fixed place on the actual baseline, whether the weight-30/12 cut contains a positive witness, and the separate WHOLE special highest certificate needed for any lift. Cheapest next evidence, if ROOT chooses: one registered produce/check sequence on the pinned baseline, then the single designed changed-object control only after a checked success. No charge_basis, no follow-on authority.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly xmodel/f10-r2-fullunit-modular-code-gate-fable5-20260910.md and box/f10-r2-fullunit-modular-code-gate-fable5-20260910/input_custody.md, both absent at 16:43:28Z; no input, shared, frozen, ROOT, worker or other-lane file was touched; no corpus scan, network, Git, agents or subprocess.

## Completion

Bound disclosure: the first report write (header, sections 0-1, A-C) measured 1628 words by wc -w, above the 1500-word per-write bound, because the eleven charged_input lines and the verdict table count as words; every individual section is below the bound. This second write is under the bound. The overage is recorded, not concealed. Authoring attestation: every byte of both owned files was written by apply_patch (custody file 16:49:44Z with hash rows generated from sha256sum output; a first report patch at 16:50:56Z was rejected for a missing end line and wrote nothing; the report was created at 16:51:46Z; this append follows). No Write/Edit tool, no shell redirection, no helper script, no source edit, no execution. Own WHOLE reread of the report and the marker-absence check precede the marker, which is appended last as the only standalone marker with nothing after it. No Seal and no charge_basis are authored. Stop is never reset. All writers idle at the marker.

<!-- BODY-END -->
