# Blind ideation submission — 20260905T0000Z — Sol (GPT-5.6)

## 0. Receipt, blind gate, and executive disposition

**Status:** independent blind submission.  The sealed packet was opened before any
banked report.  I mechanically generated the four-line checksum manifest from
`xmodel/ideation-20260905T0000Z-sol56.run.v2`; `sha256sum -c` returned `OK` for
all **4/4** frozen inputs.  No digest was retyped.  I read no other
`ideation-20260905T0000Z-*` submission, did not inspect `jc2-lean`, and edited no
canonical ledger.  At both receipt checks the only named running lane,
`g9966-n1-batch3-opus5-20260903`, had `initial_status=RUNNING` and no
`final_status`; its `.md` was not opened.

The packet is right about D=108, the three configurations of the banked
((99,66)) skeleton, the K16 atom, and the new per-row compiler.  It is wrong
about the current N1 frontier.  A promoted theorem-interface was not composed
with the later N1 enumeration:

> **The seven nonprinted ((99,66)) rows S1--S7 already fail the promoted bare
> whole-major-tree necessary screen.  S8 is the unique whole-tree survivor and
> is now dead in all three configurations.  Subject only to a short independent
> composition gate, the degree-wide ((99,66)) theorem is already closed; the
> five-row batch is obsolete.**

This is not a lexical coincidence or a new speculative inequality.  Delta
17(hh) promotes `C_FULL_TREE_POLYNOMIAL_ODE` and says the (s=3,n\le100)
screen is exact on Moh's six printed rows (`AUDIT.md:16505-16540`).  The frozen
row audit contains exactly the eight N1 row keys at ((99,66)): S1--S7 have
`group_feasible=false`, each with a forced zero-major-child certificate, and
only S8 has `group_feasible=true`, `is_printed=true`.  I reran
`box/centre-gate-20260903/rerun_screens.py`: all charged counts reproduced in
4.31 s and 13,936 KB RSS.  The local (s=3) calculation uses no passport, no
Xu inequality, no edgewise recentering, and no coefficient Gröbner basis.

My direct answers are:

1. **Q1:** attack K16 by a **spine-depth Rees degeneration plus constrained
   Padé Casoratian**, not another generic resultant.  A top-tail zero is an
   abnormally deep Padé contact; the pre-elimination Euler system should
   propagate its bordered Casoratian by \(2\times2\) transfer matrices whose
   determinants are the proved spine pivots.  Strict degeneration and filtered
   regularity would lift nonvanishing.  The Gaussian-binomial/Schubert series
   is a clue only: the literal Schubert model fails an exact \(t=3\) test.
2. **Q2:** **stop killing the five rows.**  The uniform argument across all
   eight is already banked: universal sibling extension plus the forced
   zero-factor at (j=2) kills S1--S7, and the coefficient theorem kills the
   sole survivor S8.  Moh's necessity theorem is citable only for the scoped
   normalized minimal two-point state.  “Exactly eight path assignments” is a
   campaign finite enumeration, not a printed Moh theorem; Moh's unshipped
   program cannot be cited as reproducible proof.
3. **Q3:** compile an exhaustive constructible leaf atlas, prove exact
   lower-band parametrization as a **compiler contract**, then batch
   `guided_gb`.  Do not launch 776 raw ideals, and do not seek a scalar depending
   only on skeleton arithmetic.  The safe conditional upper bound is 776 split
   leaves, or 1,072 after adjoining the 296 no-split complements.  One
   four-pass 10-minute cap is at most 517 core-hours; a 12-leaf/D=144 pilot must
   earn that spend.
4. **Q4:** fixed-degree finite verification does not become an all-degree
   theorem by repetition.  K16 itself supplies a cofinal screened family, and
   K16 is one H2 receiver, not H2.  Universal H1, universal theorem (T), and a
   proved exhaustive router would close the scoped two-point
   (M_s=n-2) stratum; the objects presently named do not.  The most valuable
   next global object is a source-complete **TWOPOINT-ROUTER/ATLAS theorem**.

The single first lane is nevertheless smaller and more urgent: independently
gate the seven-row whole-tree composition and assemble the degree-wide
((99,66)) theorem.  It should finish before any new K16 or census compute is
started.

## 1. Disposition vector and bottleneck reranking

### 1.1 Disposition vector (changes only)

Relative to the live `APPROACHES.md` overlay, **all numbered avenues not listed
here are unchanged**.

| row | disposition | narrow reason for the change |
|---:|:---:|---|
| 1, GGV/corner-family degree farm | **RAISE** | The top-pin + exact lower-band parametrization + guided-basis chain has become a reproducible bounded-census compiler, not merely a raw degree farm.  The raise is for covered per-row theorem production, not for an all-degree ceiling. |
| 2, sheet-number/Eggers--Wall/dicritical trees | **RAISE** | The promoted whole-major-tree obligation is strong enough to remove seven N1 rows uniformly.  It is now a proof-producing screen as well as an indexer; the exact source-to-row interface must be treated as load-bearing. |
| 4, formal-germ/Padé/gluing/toric deformation | **REOPEN narrowly** | K16 exposes a finite pre-elimination Rees target: a dynamic Padé table whose Casoratian transfers by the proved Euler pivots.  This does not revive unrestricted depth prolongation or “formal germ implies polynomial.” |
| 36, guided search | **LOWER on N1; unchanged elsewhere** | S1--S4,S7 should not consume coefficient search after an upstream necessary-screen kill.  Coverage-certified H1 leaves and an exact (t=8) K16 falsifier remain legitimate clients. |

Subroutes: **STOP** the running five-row N1 kill, generic K16 degree/resultant
arguments, and any skeleton-only augmented-Schur scalar.  **CONTINUE** exact
fixed-(t) K16 tests only when they discriminate the new filtration, and
continue `guided_gb` only behind a source/coverage manifest and a lower-band
compiler.

### 1.2 Proof bottlenecks, reranked

There are two time scales.  On the next-hour scale:

1. **Compose the promoted whole-tree theorem with the N1 row list.**  This is
   seven finite rational inequalities plus one row-key bijection.  If confirmed,
   it closes the most visible fixed-degree target without another large chart.
2. **Audit scope and write the theorem statement.**  Keep “no normalized
   minimal two-point counterexample of degrees ((99,66))” distinct from a
   claim outside Moh's hypotheses; explicitly consume the already proved S8
   three-configuration theorem.

On the actual program scale:

1. **Router/coverage:** turn an actual decorated two-point state into an
   exhaustive constructible union of H1 split leaves and H2 no-split
   descendants, with physical place, order, partition, ring map, gauge and
   receiver all serialized.
2. **Universal H2 receiver:** prove theorem (T) for every attained receiver.
   The K16 nonzerodivisor/radical atom is the best structured prototype, not
   the universe.
3. **H1 leaf compiler:** make pinning and exact lower-band parametrization a
   theorem-producing transformation and then execute the bounded D<=200 atlas.
4. **All-degree organization:** find a well-founded descent, a degree ceiling,
   or finitely many parametric certificate families.  Merely increasing the
   census cutoff is not progress on this bottleneck.

The **strongest immediate proof attack** is the S1--S7 forced-zero composition.
The strongest new uniform proof attack is Card 1's spine--Padé Casoratian.

### 1.3 Disproof and falsification bottlenecks

The cleanest falsifier of the K16 receiver is not a positive-dimensional cone
or a modular component.  It is an exact characteristic-zero point of

```text
I_{8,+} + (1-z*tau_8)
```

with the declared (A_8)-factor and ring map.  Such a point refutes (8.1) at
the first open index.  A point of only (Gamma\cap V(Q_8)) refutes the stronger
nonzerodivisor route but must still be tested against the weaker radical target;
the two claims are not interchangeable (`xmodel/k16-nzd-gamma-gpt55-20260903.md:
379-450`).

For H1, the best falsification attack is a **covered** residual component in
the first resonant D=144 chart after every complement and localizer is retained.
It would refute a proposed first-page Fitting kill, not exhibit a Keller pair.
Only full reconstruction of (F,G\in\mathbb Q[x,y]), verification of every
omitted coefficient, and (F_xG_y-F_yG_x=1) would promote it toward a
counterexample.  This is deliberately stricter than treating a necessary-chart
survivor as attainment.

### 1.4 Two new connections

First, the K16 complete-intersection numerators are Gaussian binomials with
the degrees of Grassmannian Chern relations.  The literal Schubert forms are
not the actual \(B_r,C_r\), but that failed comparison points to the missing
datum: the pre-elimination Euler transfer.  Combining its shortest-path
filtration with the discarded-tail Padé identity turns a static Hilbert-series
analogy into a testable Casoratian recurrence, without reversing the length
implication.

Second, the N1 collision connects **source necessity** to **claim propagation**:
the whole-tree theorem consumes path-arithmetic rows before any H1/H2
coefficient chart is built.  The same dependency should become an executable
precondition of every future census launch, not a fact remembered by a human.

## 2. Q1 — K16: a new structural attack

### 2.1 Exact target and what is genuinely unavailable

Factor (A_t=\mathbb Q[y]/(H_t)) and work over one resulting field (K).  Put

```text
P_t=K[b4,q_2,...,q_(t-1)],       deg=(1,2,...,t-1),
N_t=((C_r,B_r))_(1<=r<t),        deg(B_r)=t+1+r,
                                  deg(C_r)=2t+2+r,
R_t=P_t/I_2(N_t).
```

On the rank-one incidence algebra

```text
Rtilde_t=P_t[beta]/(C_r+beta*B_r : 1<=r<t),
Q_t=a_0*beta^2+b_0*beta+c_0,
```

the desired strong statement is that (Q_t) avoids every nonvertex minimal
prime.  This is the nonzerodivisor criterion recorded in
`xmodel/k16-nzd-gamma-gpt55-20260903.md:153-200`.  The exact incidence chart is
global only after the ([u:v]) projective-bundle chart is included; (u=0) is
safe because (a_0) is a unit (`xmodel/k16-brcr-closedform-sol56-20260903.md:
719-733`).  All claims below are factorwise and retain that chart.

The known Hilbert series and

```text
d_Gamma(t) = (binom(3t+1,t-1)-binom(2t,t-1))/(t+1)
```

count the weighted points only after expected height is known.  They do not
evaluate (Q_t) there.  Likewise, (a_0\ne0) says the incidence quadratic is
genuinely quadratic, not nowhere zero.  Reusing either inference would repeat
the exact floor/attainment error ruled out at
`xmodel/k16-nzd-gamma-gpt55-20260903.md:271-317`.  Ordinary weighted leaders,
coprime-leader subsets, generic degree data, one-row (W_1), and pairwise
resultants have also already failed.  The new idea must expose coefficient
structure invisible to those tests.

### 2.2 A useful false lead, and the corrected Padé--Casoratian mechanism

Let (e_1,\ldots,e_{t-1}) have degrees (1,\ldots,t-1), and define complete
symmetric functions by

```text
sum_(n>=0) h_n z^n =
  1/(1-e_1*z+e_2*z^2-...+(-1)^(t-1)e_(t-1)*z^(t-1)).
```

There is a nonaccidental numerical signature:

```text
P_t/(B_1,...,B_(t-1)) has conditional series
  prod_(j=t+2)^(2t)(1-s^j) / prod_(i=1)^(t-1)(1-s^i)
  = [2t choose t-1]_s,

P_t/(C_1,...,C_(t-1)) has conditional series
  prod_(j=2t+3)^(3t+1)(1-s^j) / prod_(i=1)^(t-1)(1-s^i)
  = [3t+1 choose t-1]_s.
```

These are exactly the Hilbert series of the Chern-class presentations

```text
K[e_1,...,e_(t-1)]/(h_(t+2),...,h_(2t))
and
K[e_1,...,e_(t-1)]/(h_(2t+3),...,h_(3t+1))
```

for the corresponding Grassmannian Schubert rings.  But the tempting literal
identification is **false**.  At \(t=3\), modulo 32003 at \(y=7608\), natural
elementary coordinates give

~~~text
B1=5408*u^5-14108*u^3*v+4713*u*v^2,
B2=5963*u^6-5475*u^4*v+8946*u^2*v^2-2907*v^3,
~~~

not scalar multiples of the standard \(h_5,h_6\).  Worse, a raw \(h\)-model
admits divisors of \(1+\beta z^{t+1}+\beta^2z^{2t+2}\), so its top quadratic
can be a zero divisor.  The Gaussian-binomial coincidence is a clue to a
filtration, not a regularity theorem.

The correction is to retain the actual Euler transfer before elimination.  Put

~~~text
A=yb3-L^2*C,       B=LS-b3*T-g*b2,
E=A*V' + B*U' + g*y.
~~~

A top-tail zero is exactly \(\deg_X E<t\).  With \(z=X^{-1}\),
\(f(z)=z^tV'(1/z)/U'(1/z)\),
\(A^*=z^{t+1}A(1/z)\), and \(B^*=z^{2t+1}B(1/z)\), it forces

~~~text
A^* f + B^* = O(z^(3t+2)).
~~~

Abstract Padé existence is too weak; the needed extra normality is the
self-consistency imposed by the Euler half-integration and the triangular
spine pivots.

Here is the precise proposed lemma.

**CONJECTURE[CASORATIAN-NORMALITY_t].**  Put a shortest-path filtration on the
pre-elimination convolution system: in the finite triangular inverse, depth is
the number of strict-lower edges; in a convolution, it is the number of
nonleading/off-diagonal factors.  Then:

1. the Rees degeneration is strict, so
   \({\rm gr}(S/(G))={\rm gr}(S)/({\rm in}\,G)\);
2. two adjacent Padé boundary states obey \(s_{j+1}=M_js_j\);
3. \(\det M_j\) is a unit associate of the already proved spine pivot \(p_j\);
4. the terminal bordered Casoratian is
   \({\rm in}(T_{t,2t-1})\).

The discrete Wronskian/Dodgson identity then gives

~~~text
D_terminal = D_initial * product_j det(M_j) != 0.
~~~

Thus the initial top row is regular on the graded incidence curve, and
filtered regularity lifts it to the original ring.  State the terminal claim
chart-safely as the saturation of
\(I_2({\rm in}N)+({\rm in}W)\) away from \({\rm in}J_0\), never through one
\(W_r\).  The exact convolution system and pivots are at
`xmodel/k16-brcr-closedform-sol56-20260903.md:350-424`; the finite path
inverse is at lines 470--513.

This evades the earlier negatives for a precise reason.  It acts before
\(\sigma_t\), where the coefficient array is a two-state transfer recurrence;
it does not seek monomial leaders among full-support \(B_r,C_r\), infer from
degree/length, or take a global resultant.  The banked “no fixed
hypergeometric/Wronskian after \(\sigma_t\)” result does not exclude a dynamic
pre-elimination Padé table.

### 2.3 Cheapest discriminator

Introduce a formal depth parameter in the existing uneliminated emitter.  Test
four fibres: \(t=3\), \(p=32003\), \(y=7608,10680\), and \(t=4\),
\(p=32029\), \(y=25378,31563\).  On each:

1. build adjacent bordered Padé minors;
2. verify the proposed \(2\times2\) transfer and Dodgson identities by normal
   form only;
3. verify \(\det M_j/p_j\) is a nonzero scalar;
4. run the small graded incidence saturation/dimension check.

**Bound:** 120 seconds per fibre, under 8 minutes and 1 GB total.  The
\(t=2,y=2/5\) fibre must pass; \(t=2,y=1/5\) must fail at the
\(a_0\)/incidence gate.  **Transfer failure:** discard the Padé mechanism.
**Transfer pass but graded zero divisor:** the filtration is too coarse.
**Only one conjugate passes:** an illicit factor inversion occurred.
**Bad \(t=2\) passes:** the gate is wrong.  No weight tuning after a failure.

## 3. Q2 — degree-wide (99,66): the packet has a promoted-screen collision

### 3.1 The finite certificate

Every N1 row has

```text
n=99, m=66, M=(-66,M2,97), d=(99,33,11,1), s=3.
```

At (j=2), set

```text
P=3*V3,  Q=V3*(99-M2)/11,  h=33/(99-M2),
A=den(delta2),  rho=P mod A.
```

The local Galois action makes every nonzero root orbit have size (A).  Thus

```text
p(pi)=pi^b * product_l (pi^A-c_l)^(u_l),    b == P (mod A).
```

This is the sourced/derived orbit calculation at
`xmodel/whole-tree-review-grok46-20260903.md:290-307`.  If
(rho>h), every factorization contains an above-threshold zero packet.  Moh
Proposition 5.3 applies to **every** above-threshold factor, not only the one
chosen for the displayed (V)-path; the source quantifier audit is at
`xmodel/whole-tree-review-grok46-20260903.md:157-173,252-281`.  Endpoint failure
or the pure-zero Proposition 5.6 leaf then rejects the entire row.

The exact eight-row table is:

| ID | (M_2) | (V_2,V_3) | (A) | (P) | (rho) | (h) | possible forced (b); result |
|---|---:|:---:|---:|---:|---:|:---:|---|
| S1 | -22 | 1,9 | 14 | 27 | 13 | (3/11) | 13,27; endpoint fail |
| S2 | 22 | 1,7 | 16 | 21 | 5 | (3/7) | 5 pure-zero; 21 endpoint fail |
| S3 | 22 | 5,7 | 16 | 21 | 5 | (3/7) | same as S2 |
| S4 | 22 | 1,8 | 11 | 24 | 2 | (3/7) | 2,13,24; endpoint fail |
| S5 | 22 | 1,10 | 23 | 30 | 7 | (3/7) | 7,30; endpoint fail |
| S6 | 55 | 2,10 | 13 | 30 | 4 | (3/4) | 4,17,30; endpoint fail |
| S7 | 77 | 8,7 | 13 | 21 | 8 | (3/2) | 8 pure-zero; 21 endpoint fail |
| S8 | 77 | 8,8 | 3 | 24 | 0 | (3/2) | (b=0) allowed; sole survivor |

For the only endpoint-passing zero choices, there is no allowed intermediate
centre exponent:

```text
S2/S3: delta2=9/16, delta1(b=5)=7/12;
S7:    delta2=7/13, delta1(b=8)=8/13.
```

After Moh's affine removal the bottom root is literally
(pi t^{\delta_1}), so Proposition 5.6 applies directly
(`xmodel/whole-tree-review-grok46-20260903.md:191-201`).  The general
`ZERO-FACTOR-CENTRE` repair is promoted and valid, but this local (n=99)
certificate does not need its more delicate general-(L) extension.

The frozen bare-tree artifact
`box/mohprog-drivers-20260903/full-tree-n100-audit.json` records S1--S7 as
`group_feasible=false` and S8 as true.  The later N1 enumeration gives the same
eight row keys (`xmodel/g9966-n1-batch2-sol56-20260903.md:68-95`).  Therefore
this is an exact key-by-key composition, not a comparison of aggregate counts.

### 3.2 Typed conclusion

```text
PROVED-IN-SOURCE:  each above-threshold sibling must extend; a pure-zero
                   bottom tower contradicts the minimal search premise.
DERIVED/EXACT:     Galois orbit partition, the eight rational certificates,
                   and the exhaustive finite DP.
PROMOTED:          the whole-tree screen and ZERO-FACTOR-CENTRE, AUDIT 17(hh).
PROVED/PROMOTED:   S8 is dead in split B, split C, and unsplit A.
COMPOSITION:       S1..S7 dead upstream + S8 dead downstream.
```

Consequently the packet's “3 dead / 5 running” description is stale.  The
mathematical conclusion should be promoted after one hostile interface gate:
there is no normalized, non-simultaneously-reducible minimal counterexample of
degrees ((99,66)) in Moh's (M_s=n-2) two-point scope.  If the existing
normalization theorem routes every degree-((99,66)) counterexample into that
scope as the dossier claims, this is the advertised degree-wide theorem.  The
gate must state that routing hypothesis; it must not silently enlarge the
source theorem.

### 3.3 What may and may not be cited from Moh

Moh pp.200--201 do provide necessary conditions for the normalized
least-degree/non-simultaneously-reducible two-point search.  The transcription
starts at `xmodel/census-rebase-opus5-20260902.md:63-85`.  Thus it is legitimate
to say: an actual counterexample **in that scope** supplies characteristic data
and a full tower satisfying the local extension theorems.

It is not legitimate to cite Moh for “the printed conditions (1)--(13) produce
exactly eight ((99,66)) rows.”  The eight are an independently exhaustive
campaign enumeration of the printed arithmetic predicates.  Moh instead says
his computer program “produces only” the six printed exceptions, without
publishing the program (`xmodel/census-rebase-opus5-20260902.md:342-357`).
Printed (1)--(13) alone produce 658 rows at (n\le100), including eight at
((99,66)).  His private artifact therefore remains unreproducible.

That bibliographic gap does **not** block the local theorem.  The eight-row
enumeration is a conservative superset, the universal sibling theorem is
sourced, and the table above is a transparent replacement for Moh's hidden
selection at this degree.  Phrase the paper claim as a new reconstruction of
the missing program step, not as reproduction of unpublished code.

### 3.4 Cheapest gate and interpretation

**Quantity:** verify one bijection of eight row keys, seven values of
((A,P,\rho,h)), every endpoint choice of (b), and the S8 dependency.
**Cheapest test:** an independent exact-`Fraction` script plus journal-page
audit of pp.180--183, 188--189, 200--201; script under 0.1 s/negligible memory,
hostile mathematical read under 45 minutes.  **Confirm:** close N1, stop batch3,
assemble the theorem.  **Refute:** name the first row and the exact missing
hypothesis; only then resume that row's coefficient chart.  Aggregate-count
agreement alone is not sufficient.

## 4. Q3 — price the (H1) census as a compiler, not a monolithic theorem

### 4.1 Choose a hybrid, in a strict order

The three choices in Q3 are not peers.  The efficient order is:

1. build a **coverage-certified face/order/partition atlas**;
2. prove pinning plus exact lower-band parametrization as a semantics-preserving
   compiler transformation;
3. split constructibly on nonunit pivots and run batched `guided_gb` only on
   the residual leaves;
4. mine the resulting exact Schur/Fitting residues for a later uniform theorem.

Do not launch a raw batched sweep.  Existing corrected charts with 110--143
variables and 352--598 rows already time out at 180--1,800 seconds, and some
files occupy 58--256 MB (`xmodel/order-basis-full-gpt55-20260903.md:139-181,
303-315`).  In contrast, the proved K8/K9 preprocessing reduced 58/74 variables
to 36; K9 formation took 205/368 seconds and the exact-Q bases then 11/28
seconds (`xmodel/k4ray-K89-sol56-20260903.md:403-462`).  The speedup belongs to
the exact parametrization, not to blind parallelism.

Do not seek an “augmented Schur number” determined only by
((d_s,u_s,v_s,\delta,\text{weights})).  The ((99,66)) units 6264 and 64 use
different pole pages, while D=108 dies on a common-incidence page.  The final
row depends on partition, gauge, low-tower coefficients and jets.  What remains
plausible is a **leafwise Fitting theorem over the full parameter ring**.

### 4.2 Exact workload, including the hidden multiplier

The binding sharpened cohort has 296 row assignments in 166 groups, with

```text
u_s=2: 136 groups;  u_s=3: 23;  u_s=4: 5;  u_s=5: 2.
```

The provenance and raw-to-screened counts are recorded at
`xmodel/h1-nonres-census-sol56-20260903.md:52-97`.  Of the 166 groups, 91 have
a strict candidate order and 75 have none; there are 274 group/order pairs
(`ibid.:99-117`).  Groups are not solver jobs.  Re-expanding the lower-(V)
rows gives **155 rows with an order and 396 row/order pairs**.  For each order,
expanding every genuine partition of (u_s) except the one-part nonsplit
partition gives the safe conditional bound

```text
sum_rows #orders(row)*(partition_number(u_s)-1) = 776 split leaves,
by u_s: 228 + 178 + 208 + 162.
```

I independently recomputed those integers from
`box/xufloor-20260903/results.json` using exact `Fraction` arithmetic.  Adding
one no-split complement for every base row gives at most **1,072 atlas leaves**.
This is a pre-face-ODE upper bound, not 1,072 actual configurations and not an
attainment count.  At N1, 66 order/partition candidates collapsed to 12 exact
face survivors (`xmodel/g9966-n1-batch2-sol56-20260903.md:347-439`), but those
rows are now known to fail the upstream whole-tree screen.  That ratio is not
even an operative-cohort calibration; remeasure pruning on the D=144 pilot.

### 4.3 Compiler contract

For each fully decorated leaf, the compiler must emit:

```text
source row and physical minor place;
strict order and multiplicity partition;
open/closed localizers and a proof their children cover the parent;
gauge map and declared source/target rings;
top pins and exact inverse substitution for every eliminated lower-band variable;
remaining generator union with provenance and target-row checks.
```

Eliminate only rational units.  If a candidate pivot is a polynomial (S),
compile both (D(S)) and (V(S)); never divide by (S) and lose the exceptional
locus.  On (D(S)), take the Schur complement.  On (V(S)), recompile.  This
Noetherian constructible recursion is the honest bridge between 776 unrelated
bases and a nonexistent skeleton scalar.  Cache common face/top blocks, but
retain lower-(M,V) data in the cache key.

The certificate battery is staged:

```text
face ODE -> exact parametrization/target gate -> one good-prime screen ->
two independent good-prime controls -> exact Q -> reduce(1,G)=0 and controls.
```

Finite-field units screen priorities; every inhomogeneous/localized death must
end in exact characteristic zero.  A positive-dimensional or nonunit leaf is
only a necessary-chart survivor.

### 4.4 Price and go/no-go gate

With the user's hard cap of 10 minutes and 4 GB per CAS call:

* one cached formation/parametrization call for all 776 leaves is at most
  **129.3 core-hours**;
* one first-prime call on every leaf adds another 129.3 core-hours;
* if perversely every leaf reaches all three primes and exact Q, the four CAS
  calls cost **517.3 core-hours**, or **646.7 core-hours including formation**;
* ideal 32-worker wall time is 16.2--20.2 hours before queue and I/O overhead.

That worst-case fleet spend is unjustified today.  Budget **2--4 engineer-days**
for the atlas/compiler and a three-leaf first probe, then at most 12 stratified
leaves.  A credible post-pruning target is 50--150 core-hours, but it must be
earned by measured pilot survival and cache rates, not assumed.

The first H1 discriminator is the lexicographically first NONRES-false group

```text
(144,96; M=(-72,84,102,142), V_s=4),
(u_s,v_s)=(2,4), delta=3/2, partition=[1,1].
```

It has exactly three lower-(V) rows.  Build each only through the first
resonant page, apply the lower-band compiler, and compute the residual Fitting
ideal after the declared localization.  **Pilot bound:** 150 minutes lane time;
each build or CAS call <=10 minutes/4 GB.  **Green:** all three charts reach
<=50 residual variables and <=150 rows, target gates pass, and at least one is
decided exactly.  Then expand to a 12-leaf stratified pilot.  **Red:** any chart
exceeds those size/formation caps or a complement cannot be serialized; stop
the fleet plan and repair the compiler.  **Mathematical survivor:** falsifies a
universal first-page kill, not H1 or JC2.

## 5. Q4 — fixed-degree closure is not an all-degree proof

### 5.1 The right generalization of the (99,66) method

The lesson is not “enumerate every raw skeleton and run a basis.”  It is

```text
enumerate raw path arithmetic
 -> attach every promoted global-screen witness
 -> discard failed rows
 -> build coefficient charts only for operative survivors.
```

At ((99,66)), this is (8\to1\to0): the whole-tree screen removes 87.5% before
chart formation, and the already proved S8 theorem removes the last row.  The
name collision explains why this did not happen automatically:
`Skel.full_ok()` in `box/moh_skeleton_full.py` means only the printed path
conditions (8)--(13); it is not `C_FULL_TREE`.  The N1 builder called the former
and never called `FT.full_tree_*`.  “Full” denoted two different theorem
interfaces.

For each fixed degree, Moh's integer search is finite.  Across all degrees it is
an unbounded family whose complexity and number of rows grow.  The operative
D<=200 stage already had 1,420 rows / 686 groups before the sharpened Xu cut
(`AUDIT.md:16536-16540`).  More decisively, the promoted K16 family supplies a
screen survivor for every (t\ge1), with original degrees
((48t+16,32t+16)) and descended degrees ((12t+4,8t+4))
(`AUDIT.md:16185-16214`).  Hence no finite cutoff follows from the current
screens.  An all-degree proof needs at least one of:

* an absolute degree/cover-degree ceiling;
* a well-founded descent that strictly decreases a declared invariant;
* finitely many parametric receiver families with uniform certificates.

Repeated fixed-degree elimination supplies none of these by itself.

### 5.2 The packet conflates a prototype with a quantified theorem

K16 is a highly structured H2 receiver family.  Proving its EN-curve atom kills
one cofinal ray.  It does not prove theorem (T) for every monomial-Jacobian pair
produced by descent.  Thus the phrase “with K16 (H2)” is too strong: the needed
quantifier is

```text
for every attained receiver state R output by the router, T(R) holds.
```

K16 supplies one infinite set of such (R), not the universal quantifier.

Conversely, even universal H1 and universal (T) do not apply until the input is
routed.  The missing interfaces are concrete:

1. **First separation.**  The incoming minor order and physical place are not
   the displayed major skeleton radius.  A lower bound is not an attained
   order or partition.
2. **Joint-atlas necessity.**  Every strict-below split needs an exhaustive
   cover by charts with gauges, localizers, source/target ring maps, and every
   overlap.  A numerical skeleton is not intrinsically “split.”
3. **No-split complement.**  Equality at the ceiling belongs here, not to the
   strict-below split list.  Denominator shedding and the incoming index must
   be carried into the descendant.
4. **Route-to-state and height.**  Effective height (s'>2) descendants remain;
   a height-two receiver list is not exhaustive.
5. **U-negative configurations.**  Rows with (V'_2>d'_2) lack a licensed
   split/no-split atlas and cannot be silently discarded.
6. **Ordinary support.**  The polynomial obtained in ((\gamma,\pi)) after
   Proposition 6.3 is not automatically the ordinary total-degree/order chart
   consumed by the receiver solver.
7. **Receiver coverage.**  Every attained tuple
   ((n',m',M',V',k,s',\text{partition})) needs either a named uniform family or
   its own theorem, with the fixed target index kept distinct from incoming
   and legacy search indices.

These are the `OPEN[ROUTING-MAPS]` components summarized in
`xmodel/ideation-20260904T1200Z-synthesis.md:15-17,27`.  U-negative and higher
height are mathematical branches, not missing software records.

### 5.3 What would constitute a finish

The following implication is valid as a program specification:

```text
EXHAUSTIVE TWOPOINT ROUTER
and H1 inconsistency on every attained strict-split leaf
and H2 descent plus T on every attained no-split receiver
=> no counterexample in the normalized two-point M_s=n-2 stratum.
```

It still does not prove all plane JC2.  The one-place/Abhyankar--Moh side is a
separate program, and the normalizing route into the two-point state must keep
its own hypotheses.  This is the exact scope correction already present in the
packet; it should not be weakened in the theorem title.

### 5.4 Single most valuable global object

Build a **SCREEN-COMPOSED TWOPOINT-ROUTER/ATLAS theorem**, with an executable
witness but a mathematical statement independent of the implementation.

Input: a fully decorated actual normalized two-point state.  Output: an
exhaustive constructible cover whose child is either

```text
(H1) physical minor place + strict first order + partition + joint-chart map,
or
(H2) no-split descendant + full ordinary-support inventory + receiver ID.
```

Every child carries a vector of path, whole-tree, ODE, Xu/floor and later
screen statuses, with theorem versions and rejection witnesses.  The theorem
asserts that the children cover the source; the program merely checks and
serializes that assertion.  A missing child is `UNROUTED`, never absent by
default.

The first instance is the proposed N1 composition certificate: raw eight,
whole-tree survivor S8, then the three-configuration S8 kill.  The cheapest
coverage regression after that is D=108, because its known split leaf and K8
no-split leaf are independently dead.  A pass makes existing certificates
consumable and reveals the actual universe quantified by theorem (T); a fail
names the first routing leak before another large computation is funded.

## 6. Three idea cards

### Card 1 — SPINE-PADE-CASORATIAN normality

**Label/fingerprint:** `NEW`; target = K16 top-row nonzerodivisor;
mechanism = strict shortest-path Rees filtration plus dynamic Padé transfer;
object = pre-elimination coefficient array; discriminator = bordered
Casoratian/Dodgson identities.  This is the round's genuinely new avenue.

**Claim sought.**  Prove CASORATIAN-NORMALITY_t: adjacent boundary states
satisfy \(s_{j+1}=M_js_j\), each \(\det M_j\) is a unit associate of the proved
spine pivot, and the terminal Casoratian is the initial top row.  Strictness
plus filtered regularity then proves the nonzerodivisor factorwise.

**Dependencies.**  Exact pre-elimination convolution recurrence, unit pivots,
TAIL-SPLIT and the full projective incidence atlas.  Both Rees strictness and
the terminal identity are obligations; Gaussian-binomial lengths supply no
implication.

**Cheapest discriminator.**  Build adjacent minors on the two conjugate fibres
at \(t=3,4\), verify transfer, Dodgson and \(\det M_j/p_j\), then run the small
graded incidence gate.  Bound: four 120-second fibres, under 8 minutes/1 GB.
Good \(t=2\) must pass and bad \(t=2\) must fail.

**Outcome interpretation.**  A full pass licenses an all-\(t\) transfer
induction.  Transfer failure discards the Padé interpretation; transfer with a
graded zero divisor means the filtration is too coarse; one-conjugate success
means an illegal factor inversion.  **Stop condition:** the first failed
transfer or branch control—no post-hoc weight tuning.  **Expected information
gain:** high: it either exposes a product of known units or kills the only
mechanism here that uses the hidden Euler self-consistency.

### Card 2 — N1 forced-zero composition certificate

**Label/fingerprint:** `KNOWN theorem + MISCOMPOSED interface`; target =
degree-wide ((99,66)) N1; mechanism = Galois residue forces a major zero
sibling; object = the eight row keys; discriminator = an independent endpoint
table.  Its novelty is the cross-ledger composition, not the local lemma.

**Claim sought.**  Under the precise normalized Moh hypotheses, S1--S7 fail
`C_FULL_TREE` and S8 is the only path assignment reaching coefficient level.
Compose with the complete S8 three-configuration theorem.

**Dependencies.**  Source necessity/routing into the (M_s=n-2) state; exact
eight-row conservative enumeration; universal quantifier in Proposition 5.3
and p.200(4); bottom pure-zero Proposition 5.6; row-key identity; promoted S8
kill.  Passport, ODE, Xu, edgewise recentering and the S5/S6 coefficient
certificates are deliberately absent.

**Cheapest discriminator.**  Independent `Fraction` verifier for all
(A,P,\rho,h,b,A_1) entries, plus a hostile read of four Moh page ranges.
CPU <1 second, review 30--45 minutes, <1 GB.

**Outcome interpretation.**

* Exact survivor set `{S8}` and source quantifiers confirmed: promote the
  composition, stop batch3, and write the scoped degree theorem.
* A row-key mismatch: repair the adapter; do not run a chart until the actual
  unmatched row is named.
* A source-hypothesis failure on one (b): resume only that row and retain the
  other six kills.

**Stop condition:** after two independent arithmetic implementations and one
source audit agree; additional five-row Gröbner work then has zero theorem
value.  **Expected information gain:** decisive and immediate—either the
published degree gap closes or the precise surviving row is exposed.

### Card 3 — constructible H1 Fitting compiler

**Label/fingerprint:** `NEW mechanism / existing target`; target = bounded H1
census; mechanism = localize-or-specialize pivot recursion plus exact
lower-band compilation; object = full decorated leaf parameter ring;
discriminator = the D=144 resonant group.

**Claim sought.**  For every bounded leaf, a finite constructible recursion
produces a coverage-preserving reduced system.  On each (D(S)) child the
augmented Schur/Fitting row is computed after unit pivots; every (V(S)) child
is retained and recompiled.  This is a uniform compiler theorem, not a uniform
claim that every final residue is nonzero.

**Dependencies.**  Screen-composed 296-row cohort; actual first-separation and
partition atlas; exact pin/lower-band identities; target/generator-union gates;
`guided_gb` controls.  The cache key must include lower (M,V), gauge and
localizer data, not just a top signature.

**Cheapest discriminator.**  Compile the three D=144, (u=2,v=4,\delta=3/2),
`[1,1]` leaves through the first resonant page.  Bound: 150 lane-minutes, every
CAS call <=10 minutes/4 GB, <=50 residual variables and <=150 rows.

**Outcome interpretation.**  Three covered exact units validate expansion to
a 12-leaf pilot.  A covered proper component refutes a universal first-page
kill but is not a pair.  A missing complement or oversized system is an
engineering/theorem-interface failure and blocks the fleet sweep.

**Stop condition:** any uncovered exceptional pivot locus, or two of the first
three leaves exceeding the size cap.  **Expected information gain:** high on
economics and medium on uniform mathematics; it converts the 776-leaf bound
from guesswork into a measured schedule and yields exact residue data from
which a real stratumwise theorem could be mined.

## 7. Single first lane and running-lane disposition

### Single first lane: `g9966-whole-tree-composition-gate`

This lane outranks K16 and H1 because it can close a famous fixed-degree case
with no heavy algebra.

**Inputs:** Moh pp.180--183, 188--189, 200--201; promoted 17(hh); the bare
full-tree driver/artifact; the N1 eight-row enumeration; the S8 necessity and
three configuration certificates.  **Tasks:** independently regenerate the
eight path rows; emit the table in Section 3; verify each allowed (b) endpoint
and the two pure-zero leaves; prove the exact row-key bijection; state the
normalization/routing hypotheses; assemble one dependency DAG ending in the
degree theorem.  A second reader must attack the universal sibling quantifier.

**Bound:** 45 minutes for producer, 45 minutes for hostile reader; CPU under one
minute, RAM under 1 GB.  No coefficient-chart CAS.  **Deliverable:** theorem
statement plus executable JSON containing `raw=8`, `C_FULL_TREE={S8}`, all
seven rejection witnesses, and the S8 certificate hashes.  **Stop:** either a
confirmed composition or one precisely typed surviving row/hypothesis.

### Running lane: **STOP / INTERRUPT, then redesign only as a regression**

The receipt for `g9966-n1-batch3-opus5-20260903` still says
`initial_status=RUNNING`, start `2026-09-04T23:43:29Z`, with no `final_status`.
I inspected only that receipt.  Its theorem remit is exactly S1--S4,S7, all of
which fail the promoted upstream screen.  Stop spending on the 2,271--3,441
coefficient charts.

If it seals before interruption, do not discard the artifacts: bank them only
as `guided_gb` performance/regression data, never as required N1 evidence.  A
redesigned turn may independently replay the forced-zero table and diagnose
why `full_ok` was confused with `C_FULL_TREE`; it should not continue the five
kills.

## 8. One systems upgrade

### UPGRADE — operative-screen provenance DAG and scheduler interlock

This round exposed a state-propagation failure, not a retrieval shortage.
`Skel.full_ok()` means “printed path checks complete,” while `C_FULL_TREE`
means “every major sibling can be extended.”  The later N1 jobs interpreted the
first name as if it included the second.  The promoted screen was visible in
`AUDIT.md` and `notes.md` but absent from each row's machine-readable ancestry.

Add an `OPERATIVE_SCREEN_VECTOR` to every census row and make it a hard
scheduler prerequisite.  Minimal schema:

```text
row_key; scope/normalization; enumerator hash;
PATH_1_13: {version,status,witness};
C_FULL_TREE: {version,status,witness};
POLY, ODE, XU_FLOOR: {version,status,witness};
screen_conjunction_hash; downstream chart IDs and invalidation epoch.
```

Rename or schema-alias `full_ok` to `path_8_13_ok`; code may not expose an
unqualified `full` predicate.  Before a chart is formed, the scheduler loads
the current promoted conjunction, verifies its version/hash, and refuses every
row with a failed necessary predicate.  Promotion of a new screen invalidates
all descendant jobs whose vector lacks that version.  Rejection witnesses are
content-addressed and travel with the row; aggregate counts never authorize a
kill.

**Smallest useful implementation/test:** wrap the existing enumerator and
full-tree functions without changing their mathematics, then run four
regressions:

```text
(99,66): PATH count 8, C_FULL_TREE survivor exactly S8;
D=105: operative survivor set empty;
D=117: operative survivor set empty;
Moh's six printed rows: all retained;
```

The first regression must make a synthetic batch3 launch fail closed and print
the seven row-level witnesses.  A K16 survivor is the negative control: it must
remain schedulable.  Estimated implementation: one developer-day; regression
runtime under ten seconds and 100 MB.

This is independent of Card 3's mathematical chart compiler.  The existing
`ops/open_collision.py` is intentionally lexical and reviews newly named
OPENs; it cannot detect that two differently named predicates have composable
hypotheses/conclusions.  The provenance DAG implements COORDINATION's required
theorem-interface composition pass at launch time.

## 9. OPEN register, collision scan, and guardrail audit

### OPENS RAISED OR SHARPENED

- OPEN[K16-SPINE-PADE-CASORATIAN]

  Bound the two missing obligations for every \(t\ge3\): Rees strictness of the
  shortest-path filtration and equality of the terminal bordered Casoratian
  with the initial top row.  **Cheapest test:** four \(t=3,4\) conjugate-fibre
  transfer checks plus two \(t=2\) controls; existing emitter, normal-form and
  small saturation gates; under 8 minutes and 1 GB.

- OPEN[FALSE-SKELETON-AUGMENTED-ROW]

  Bound each of the exactly three operative D=144 resonant pilot charts by at
  most 50 residual variables and 150 rows, and decide whether its localized
  augmented Fitting ideal is the unit ideal.  **Cheapest test:** source/atlas
  gate, lower-band compiler, one modular screen and exact Q as needed; 150
  lane-minutes, with every CAS call under 10 minutes and 4 GB.

- OPEN[ROUTING-MAPS]

  Bound the D=108 prototype's uncovered branch count by zero while retaining
  one strict-split child and its no-split complement, then state the universal
  source-cover invariant.  **Cheapest test:** serialize the two known D=108
  leaves with source/target rings, gauges, localizers, screen vectors and
  receiver IDs; one lane-day, with no CAS call above 10 minutes/4 GB.

### OPENS UNTOUCHED

`OPEN[K16-Q-NONVANISHING-ON-GAMMA]` and the weaker exact
`OPEN[K16-(8.1)-RADICAL]` remain open for all \(t\), despite fixed
\(t\le7\).  `OPEN[U-NEGATIVE-CONFIG]`, higher effective height,
ordinary-support transport and universal receiver coverage remain components
of `OPEN[ROUTING-MAPS]`.  Moh's missing historical program artifact
remains open globally, but is irrelevant to the eight-row local replacement.

### Guardrail audit

Flags, physical places, cover series and first-separation orders remain
separate.  Every census count is a necessary-screen count, never attainment.
The EN length is not used backwards.  Every proposed localization retains its
closed complement.  Modular units are screens unless properness applies;
inhomogeneous kills require exact Q.  Ring maps and \(A_t\)-factors are
declared, and a point on a necessary chart is not a Keller pair.  No new exit
price is asserted, so no `charge_basis` line is due.

## COLLISIONS

status: CANDIDATES

- `OPEN[K16-SPINE-PADE-CASORATIAN]` (report:866): NONE

### OPEN[FALSE-SKELETON-AUGMENTED-ROW]

- `AUDIT.md:17744` — ## INTEGRATION #17 DELTA (ttttt) (2026-09-04T13:18Z, census + refutation, producer Sol; the NONRES arithmetic kill is REFUTED, and (H1) has NO arithmetic/coprimality theorem shortcut): NONRES(S) is TRUE on 124/166 groups, FALSE on 42; on...

### OPEN[ROUTING-MAPS]

- `AUDIT.md:17733` — ## INTEGRATION #17 DELTA (qqqqq) (2026-09-04T12:47Z, round 20260904T1200Z synthesis, five sealed blind submissions): (1) Q1 K16 SETTLED by 17(ppppp) (5/5): the three named routes are PROVED impossible, length is a theorem, residual = CRI...
- `xmodel/ideation-20260904T1200Z-synthesis.md:17` — (H1)∧(H2) is a program ALONG MOH'S TWO-POINT LINE, NOT a full reduction of plane JC2. Grok's four leaks: (1) ONE-PLACE — Abhyankar–Moh / one-cusp / rank-four is a different program (Moh p.194 restricts to M_s = n−2 and TWO distinct point...
- `xmodel/ideation-20260904T1200Z-synthesis.md:27` — ## 4. OPENs raised (merged, collision-scanned): OPEN[K16-RANK-CRITERION], OPEN[8.1-VS-V0] (τ_t∈radical is the weaker JC2 target), OPEN[UNSPLIT-CONFIG-LEMMA] (= the k=4 ray), OPEN[MOH-CENSUS-N1] (eight V-assignments; not source-closable),...
- `xmodel/ideation-20260905T0000Z-packet.md:5` — PROGRAM (5/5, unchanged): JC2 along Moh's two-point M_s=n−2 line = (H1) split ⇒ joint chart inconsistent ∧ (H2) no-split ⇒ descent + theorem (T). NOT all of plane JC2 (one-place/Abhyankar–Moh is a separate program; the routing maps are t...

## 10. Submission-contract checklist

Delivered: changes-only vector; bottleneck reranking; typed Q1--Q4; exactly
three cards; one first lane; receipt-only running-lane decision; mathematical
acceleration; and one separate systems upgrade.

The round-guarded collision scan ran on this unsealed report with zero context
lines.  Its hit for `FALSE-SKELETON-AUGMENTED-ROW` confirms the existing
D=144/NONRES alias, not a closure.  Its `ROUTING-MAPS` hits confirm that
the proposed atlas sharpens an existing scope gap.  The new K16 Casoratian name
has no lexical collision.  Collision candidates were not used as mathematical
evidence.

Final posture: **gate \((99,66)\), stop its obsolete batch, test the K16
transfer, then pilot H1 behind the screen interlock.**

<!-- BODY-END -->
