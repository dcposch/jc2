**LEMMA [CHILD-INTEGRALITY] — a finite necessary obstruction after licensed descent**

The lemma is proved with a **finite cover of all possible child major signatures** as its exhaustion hypothesis. It certifies R063's forced sum **71/4**. A singleton own vector alone is insufficient. R025–R028 and R057–R058 remain **OPEN[TRANSPORT-EXHAUSTION]**; no further exclusion is claimed.

**Custody.** The receipt's numbered basename/hash fields were joined by `awk`; `sha256sum -c` returned **9/9 OK** before mathematical use. Manifest: `box/child-integrality-lemma-20260906/charged-inputs.sha256`. Mathematical inputs were only `/tmp/jc2-lane.rbo6IG/inputs`. No fleet, ledger edits, `jc2-lean`, or `ideation-*` input was used.

**M** denotes [Moh](../refs/moh1983_jram340_configurations_of_roots.pdf), **X** [Xu v4](../refs/xu2016_intersection_numbers_split_minor_roots_arxiv1604.07683v4.pdf), using printed pages. The frozen PDFs and images of the decisive formulas were checked directly. Links name charged originals; reads used verified copies.

The frozen R063 gate §§2.2, 2.5, 4–5 corrects the earlier shift report and census. Its correction governs here. The own-data, child-top, and depth claims named 17(vvvvvvvv), 17(ooooooo)(B), and 17(gggggggggg) are treated through the frozen instrument and print; no separate uncharged reports were read.

**Statement.** Work over algebraically closed $K$ of characteristic zero. A prime labels a child; derivatives are $P'_Y$. Use reduced $p=T_1^\psi,g$ of degrees $m<n$, with $M_1=-m$. Realizations are Keller pairs, monic in $y$, with total degrees equal to their $y$-degrees and $m/\gcd(m,n)>1$. Let $A$ be necessary data in Moh's reduced normalized setting, including $M_s=n-2$, $\delta_s=-1$, the major tower and principal minor direction. Put

\[
d=d_s,\quad v=V_s,\quad u=d-v>0,\quad
\ell=v-u-1\ge0,\qquad H=1+\ell=v-u.
\]

Suppose the following hold for every polynomial pair realizing $A$.

1. **Descent license.** Proposition 6.4 supplies the radius condition when $u=1$. When $u\ge2$, a proof supplies the actual principal minor radius $\delta^*_{s-1}\ge v/u$ required by Proposition 6.3. A numerical guess about that radius is insufficient.
2. **Own-data cover.** The child's own data belong to finite $\mathcal O(A)$: correlated $V'$ vectors, effective full characteristic chains/tops, centre stabilizers, and continuation radii/orders. A determined vector is the singleton case; a prefix with unidentified terminal part is incomplete.
3. **Exhaustive completion.** The procedure produces finite $\mathcal C(A)$ covering every actual child: all final major discs, actual $P'_\xi$-root counts $\rho_\sigma$, and radii $\delta_\sigma$, with omitted roots proved bounded under $Q'$. The p.171 form of Proposition 4.6 applies through $r=1$. All siblings are included; the family may contain unrealizable numerical signatures.

For $I(P'_\xi,Q')$ require bounded roots to have order zero. Alternatively replace the output by $Q'-\eta$ for independent generic $\eta$ and rename it $Q'$: pole data stay unchanged and bounded values have order zero. An exceptional fixed fibre otherwise needs its positive-order correction. R063 is entirely major.

> **LEMMA [CHILD-INTEGRALITY].** Under these hypotheses define, for each $C\in\mathcal C(A)$,
>
> \[
> S(C)=\frac{n'}{n'+m'}\sum_{\sigma\in C_M}
>       \rho_\sigma(H-\delta_\sigma),\qquad
> n'=un/d,\quad m'=um/d.
> \]
>
> If $S(C)\notin\mathbb Z$ for every compatible signature $C$, no polynomial pair realizes $A$. The same conclusion holds if the certified exhaustive family is empty. A nonempty family with an integral member survives this obstruction; this does not assert realization.

“Compatible” refers to declared necessary conditions in a family **containing every actual image**. A larger finite outer family is safe. Finiteness makes the check terminate; the implication itself needs only coverage. Empty-family exclusion is absence of any image, not evaluation of a nonexistent sum.

**Proof, polynomiality and the integer.** Normalize the two parent directions as in M pp.194–195, with $z=y-bx-e$, $b\ne0$. Absorb the finite truncation of the principal expansion into $c(X)$. The ordered generator map is

\[
\Phi^*:K[x,y]\longrightarrow K[X,X^{-1},Y],\qquad
y\mapsto X^{-u},\quad
x\mapsto b^{-1}(X^{-u}-e-c(X)-X^vY).
\]

Here $X=\gamma$, $Y=\pi$. Set $P'=\Phi^*p$, $Q'=\Phi^*g$. Proposition 6.3(1), p.197, places both in **$K[X,Y]$**; (2) gives monicity and degrees $m',n'$. Its p.198 proof uses the common truncation, the inversion lemma, and cancellation of negative substitution powers.

Directly, $x_Y=-X^v/b$, $y_X=-uX^{-u-1}$, $y_Y=0$, hence $J_{X,Y}(x,y)=-(u/b)X^{v-u-1}$. The parent determinant is a nonzero constant; changing from Moh's output order $(Q',P')$ to $(P',Q')$ changes only that constant. Consequently

\[
J_{X,Y}(P',Q')=cX^\ell,\qquad c\in K^*.
\]

This agrees with the positive power in p.197(3). The reciprocal powers printed in the last determinant on p.198 conflict with its preceding substitution; the displayed derivatives resolve that printing error. Proposition 6.4, pp.198–199, proves the required radius inequality at $u=1$, and does not supply it at arbitrary $u>1$.

Over $K(\xi)$, the resultant $R(X)=\operatorname{Res}_Y(P'-\xi,Q')$ is a nonzero polynomial. For example, as a polynomial in $\xi$ it has nonzero constant leading coefficient of degree $n'$, by monicity of $Q'$. Thus X §2, p.1, gives literally

\[
I(P'_\xi,Q')=\deg_XR\in\mathbb Z_{\ge0}.
\tag{CI.1}
\]

With $X=t^{-1}$, factor the monic $P'_\xi$ over the Puiseux closure. The norm formula is

\[
R(t^{-1})=\prod_{P'_\xi(\alpha)=0}Q'(t^{-1},\alpha),\qquad
\deg_XR=-\sum_\alpha\operatorname{ord}_tQ'(\alpha).
\tag{CI.2}
\]

There is no cover division: $\rho_\sigma$ counts individual roots, including conjugates separately.

**Proof, the shifted final-major identity and its scope.** X Lemma 4.1, p.4, is already stated for arbitrary $J(X,Y)$. Its chain rule gives

\[
Q'_Y(\alpha)\frac{dP'(\alpha)}{dt}
-P'_Y(\alpha)\frac{dQ'(\alpha)}{dt}=-c t^{-\ell-2}.
\tag{CI.3}
\]

Equivalently, the right side of M Proposition 4.1 for a disc of radius $\delta$ is $-c h_p(\sigma)t^{\delta-2-\ell}$. At a final major disc the nonzero leading determinant therefore gives

\[
\lambda_P+\lambda_Q=\delta-H.
\]

M Proposition 4.6(2), (3)*, and its $r=1$ conclusion give the stronger individual relations

\[
\lambda_P=m'\lambda,\quad \lambda_Q=n'\lambda,\quad
\lambda=\frac{\delta-H}{n'-M'_1}
       =\frac{\delta-H}{n'+m'}<0.
\tag{CI.4}
\]

The constant differential equation at $r=1$ forces the leading $P'$- and $Q'$-polynomials to be squarefree and disjoint: at a repeated root, or a common root, both terms of that equation would vanish. Thus every $P'_\xi$-root in the disc has $Q'$-order exactly $\lambda_Q$. These are final discs; their squarefree leading polynomials are not a license to introduce another major level below them. The reduced final degrees here are greater than one as required by X's definition; a degree-one exceptional endpoint requires its own endpoint treatment.

All remaining roots have order zero by the stated convention. Substitution in (CI.2) proves the exact shifted version of X Theorem 5.1, p.7:

\[
\boxed{I(P'_\xi,Q')
=-\sum_{C_M}\rho_\sigma\lambda_{Q,\sigma}
=\frac{n'}{n'+m'}\sum_{C_M}\rho_\sigma(1+\ell-\delta_\sigma).}
\tag{CI.5}
\]

This proves the identity for a certified major cover. X's global final-root lemma invokes his proportional lemma under a Keller hypothesis; its child analogue needs a separate proof or the following local disc argument.

The required local replacement is explicit. In M Proposition 4.2, p.165, the order comparison for $h=T_{r+1}^\psi$ becomes

\[
n\lambda-\mu_{r+1}\lambda-1
<\delta-2-\ell+(-\mu_{r+1}+M_{r+1})\lambda
\iff\lambda<\frac{\delta-H}{n-M_{r+1}}.
\tag{CI.6}
\]

Under the remaining hypotheses the leading differential expression vanishes; the rest of p.165 gives the distribution detector. Use this strengthened condition in Proposition 4.4, together with its shifted (6) on p.169. The p.171 Remark supplies Proposition 4.6(3)*. Citing p.169 alone misses condition (3).

The transition proof in M pp.180–183 also shifts locally. In its contradiction argument take $M_{r-1}<B<M_r$; its p.181 chain becomes

\[
\lambda=\frac{\delta^*-H}{n-B}
<\frac{\delta^*-H}{n-M_{r-1}}<\cdots
<\frac{\delta^*-H}{n-M_1}<0.
\]

On p.182, $\varepsilon=(\delta^*-H)V_r/(d_r\lambda)-1>-1$. The shifted nonsplitting hypotheses therefore exclude an earlier split. At the prescribed radius, Proposition 4.6 applies at **$r-1$**. The same local substitution in pp.191–193 gives minor threshold $H$. Dividing orders and radii by $H>0$ checks the homogeneous algebra: with $z=t^H$ the disc Jacobian is $-(c/H)z^{\delta/H-2}$. This checks local order arguments; it supplies neither polynomiality in $z^{-1}$ nor a global top theorem.

**The (4.4) side condition.** Although (CI.5) does not need the minor inequality, its shifted proof must be audited. X Proposition 3.3 is Jacobian-free. At a final minor disc take related $P'_\xi$- and $P'_Y$-roots $\alpha,\beta$. Squarefreeness gives $\operatorname{ord}(\beta-\alpha)=\delta$. Maximal contact with the $Q'$-roots $\gamma_j$ gives

\[
q_j=\operatorname{ord}(\alpha-\gamma_j)\le\delta,
\quad \max q_j=\delta,\quad \sum_jq_j=0.
\]

Put $E=\{j:q_j=\delta\}$. For $j\notin E$, ultrametricity gives $\operatorname{ord}(\beta-\gamma_j)=q_j$; for $j\in E$ it gives $r_j\ge\delta$, with possible strict inequality. A term of the derivative product for $Q'_Y(\beta)$, omitting an index in $E$, has order

\[
-\delta+\sum_{j\in E\setminus\{i\}}(r_j-\delta)\ge-\delta.
\]

Omitting $i\notin E$ gives $-q_i+\sum_E(r_j-\delta)>-\delta$. Cancellation can only raise the order. Therefore

\[
\operatorname{ord}Q'_Y(\beta)\ge-\delta.
\tag{CI.7}
\]

The bound is **unchanged by $\ell$**. It does not assert $\operatorname{ord}Q'(\beta)=0$, the overstatement repaired by the frozen gate.

Under a complete Xu final minor cover, Proposition 3.3 assigns $\rho_\sigma-1$ zero-order critical roots to each minor disc. A zero-order split of $P'-\xi$ is generically squarefree; a terminal major split has negative order and no further splitting inside its simple roots. Other critical roots have negative order, since generic $\xi$ excludes positive order. Applying (CI.3) gives

\[
\begin{aligned}
I(P'_\xi,P'_YQ')&=Hm'+\sum_{C_m}\rho_\sigma(\delta_\sigma-H),\\
I(P'_Y,P'_\xi Q'_Y)&\le H(m'-1)
 +\sum_{C_m}(\rho_\sigma-1)(\delta_\sigma-H).
\end{aligned}
\tag{CI.8}
\]

The second line is $(4.4)^\ell$. Negative-order differentiation lowers order by one; at order zero use (CI.7). The thresholds are $\delta<H$ and $\delta>H$; the latter follows from X Lemma 4.4's next correction of order $\epsilon>0$, giving $\delta\ge H+\epsilon$.

One cannot retain X's next assertion $I(P'_Y,Q'_Y)=0$ at $\ell>0$. Instead

\[
\operatorname{Res}_Y(P'_Y,Q'_Y)=c_0X^k,\qquad k\in\mathbb Z_{\ge0}.
\]

Indeed any common specialized derivative root lies over $X=0$, by the Jacobian; the leading $Y$-coefficients are the nonzero constants $m',n'$, so there is no lost root at infinity and no common polynomial factor. Subtracting the two lines of (CI.8) yields $I(P'_\xi,Q')\ge H+\sum_{C_m}(\delta-H)+k$. In the minor-free case equality holds: $I=H+k$. This corroborates integrality but is not substituted for the complete-sum identity.

**Own data and finiteness.** The [frozen instrument](/tmp/jc2-lane.rbo6IG/inputs/descend_own.py:104) has a restricted reduced source scope. Its relevant content is:

* Degrees are $n'=un/d$, $m'=um/d$, $\mu'_i=(u/d)\mu_i$. On the identified characteristic prefix, $M'_i=(u/d)M_i$, $d'_i=(u/d)d_i$. Factor out $d$ for the gcd assertion; use M p.150's recurrence $\mu_i=(d_{i-1}/d_i)\mu_{i-1}+M_i-M_{i-1}$ for $M'$. Identifying these retained quasi-roots with the child's own prefix is the child-top reduction, not an inference from degree scaling alone. At fixed $X$, child $Y$ is affine in parent $x$ over $K(y)$ for the characteristic calculation; Xu packets instead use **$X\to\infty$**.
* The last retained gcd is $d'_s=u$. At $u=1$ the identified prefix is complete: no further gcd drop is possible. Drop an ineffective $n'-1$ tail only under M p.174. At $u>1$, code lines 249–258 retain `OPEN_CHILD_TERMINAL_IDENTIFICATION`; a descent radius does not resolve it. Supply a finite certified family of full extensions.
* Own multiplicities are correlated first-support alternatives, not copied marginal choices. Before first nonzero support, set $W_s=u$, $P_i=V_{i+1}d_i/d_{i+1}$, $\delta_i=a_i/b_i$ in lowest terms. A zero coefficient requires $b_i\mid P_i-V_i$ and gives
  \[
  W_i=(d_i/d_{i+1})W_{i+1}-\delta_i(P_i-V_i).
  \]
  First nonzero support $j$ requires $\delta_j>0$, $b_jV_j\le P_j$; then $V'_i=V_i$ for $i\le j$, $V'_i=W_i$ for $i>j$. These exhaust zero/nonzero choices. Under inversion, a source $b$-orbit at radius $a/b>0$ produces $a$ inverse coefficient discs; subtract their root counts from the child total to obtain the zero group. This proves the subtraction via M Lemma 6.2 and the instrument's lines 70–101, 151–181. M Proposition 5.6 excludes the reduced all-zero bottom route.
* Inverse radii are $v-u/\delta_i$ on the zero-centred portion and $H-(u/\delta_j)(1-\delta_i)$ below first support. On a confirmed complete top they agree with $H$ times Definition 5.1's formula. Code lines 183–194 additionally prune source sibling routes; that needs a sound extension proof. Keeping the unpruned outer routes is safe.

Even singleton $V'$ leaves sibling choices. Finiteness concerns **major signatures**, modulo coefficients preserving counts, radii, and conjugacy; it does not assert finitely many full coefficient-level or minor re-expansions.

Here is the printed termination argument, rather than a depth cap. At level $r\ge2$, write

\[
P=\deg p,\qquad Q=P(n'-M'_r)/d'_r.
\]

These are positive integers. Proposition 4.6 gives at most $Q$ distinct factors of $p$, with positive multiplicities summing to $P$. Every above-average factor continues to **$r-1$ on the same full list**, then terminates at $r=1$. Thus a path has at most $s'-1$ transitions, even without loss of $P'$-roots at every transition. Proper root-tree splits also lose roots, by M Definition 1.1(5), pp.144–145. That bound alone does not bound radii or unsplit levels; the fixed list and forced radius formula do.

At a below-average factor, shifted M Proposition 6.1 keeps roots distributed until orders reach zero and excludes an earlier major final split. It becomes a bounded sink, contributing zero after generic normalization. Later minor splitting is irrelevant to (CI.5). Missing boundedness or top coverage remains OPEN.

Finite branching, finite depth, and root conservation now follow. Every actual root follows one factor, every major reaches a final disc, and every packet is counted once. A realizing parent would therefore induce $C\in\mathcal C(A)$ with integral $S(C)$ by (CI.1), (CI.5), a contradiction. An empty exhaustive family contradicts existence of $C$ directly. **QED.**

**Exact finite algorithm.** Input is the licensed correlated own-data family with the statement's coverage certificates. All arithmetic is rational with exact divisibility. A necessary relaxation is allowed if it still contains every actual image.

1. Verify descent, full top, and finite own-data coverage. Preserve correlated vectors/routes. Missing radius, top, stabilizer, or boundedness produces `SET-VALUED / OPEN[...]`, outside the lemma's certified input class. Finite ambiguity alone does not prevent a verdict.
2. Start each record at its certified top with state $(r,P,\delta,\lambda,L)$, selected-route constraints, root counts, and $\lambda=(\delta-H)/(n'-M'_r)$. A top containing all roots starts at $L=1$: its common pre-split coefficients equal those of the polynomial root average. Other incoming stabilizers must be supplied.
3. Put $A_0=\operatorname{den}(L\delta)$. Enumerate every integer $0\le z\le P$ with $A_0\mid P-z$, and every integer partition $(a_1,\ldots,a_k)$ of $(P-z)/A_0$. This represents $p=\pi^z\prod_{j=1}^k(\pi^{A_0}-c_j)^{a_j}$, with distinct nonzero $c_j$. Require $1_{z>0}+A_0k\le Q$, all distinguished multiplicities and route constraints, and exclude any multiplicity equal to $P/Q=d'_r/(n'-M'_r)$. The last exclusion follows directly from the leading differential equation: writing $T_r=p^e q$, its reduced form is $p q'-(Q/P)p'q=Cp$; at a root of multiplicity $a$, the nonzero leading coefficient is proportional to $1-(Q/P)a$. At equality it cannot equal $C\ne0$.
4. Handle **every** factor. Require integral counts $\rho_P=m'a/d'_r$, $\rho_Q=n'a/d'_r$. Zero keeps $L$; nonzero changes it to $\operatorname{lcm}(L,\operatorname{den}\delta)$ and represents $A_0$ conjugate discs. No intermediate denominator increase is possible under the nonsplitting certificate: it would create distinct conjugate centres and an earlier split.
5. For an above-average factor put $\kappa=m'\lambda+\rho_P(H-\delta)>0$, $W=n'-M'_{r-1}$. The exact successor is
   \[
   \delta_{\rm next}=H-\frac{W\kappa}{W\rho_P-m'},\quad
   \lambda_{\rm next}=\frac{\delta_{\rm next}-H}{W},\quad
   P_{\rm next}=a d'_{r-1}/d'_r.
   \]
   These follow from root-product linearity $\lambda_P(\Delta)=m'\lambda+\rho_P(\Delta-\delta)$ and the next printed equality. Verify all local hypotheses and recurse at $r-1$. Below average, record a bounded sink with its full root count and zero major contribution; equality was excluded in step 3.
6. At $r=1$ charge $n'\rho_P(H-\delta)/(n'+m')$. Optional proved necessary filters are final Galois residues $(\rho_P,\rho_Q)\equiv(0,1)$ or $(1,0)$ modulo $\operatorname{den}(L\delta)$, and coefficient-level ODE feasibility. Omitting a filter enlarges the necessary family. Check root conservation; combine all siblings and correlated own alternatives by finite unions/products.
7. Test the **complete sum**, never individual summands. All nonintegral gives `DEAD / CHILD-INTEGRALITY`; certified emptiness gives `DEAD / NO-COMPATIBLE-COMPLETION`. Any integral member gives `SURVIVES`, with retained signatures/sums, meaning only survival of this screen. Unresolved coverage gives `SET-VALUED` even if every explored branch failed. Exhausted multi-vector families use the same existential survival and universal exclusion rules.

The uncapped [check.py](../box/child-integrality-lemma-20260906/check.py) implements this numerical outer screen for the controls and extracts five pure functions from frozen `descend_own.py`. It does not import the uncharged `OwnVRouteTree` dependency or claim a full instrument run. Outer-route coverage suffices without its pruning. [checks.json](../box/child-integrality-lemma-20260906/checks.json) records fractions, counts, and outputs. Exactness concerns the declared necessary family, not polynomial realization.

**R063 application.** The frozen gate's parent data give

\[
(n,m)=(168,112),\quad M=(-112,140,160,166),\quad
d=(168,56,28,4,2),\quad V=(3,21,3).
\]

Their radii are $(3/4,3/10,1/5,-1)$; $u=1,v=3,H=2$. At index 3, $P=21$: nonzero selected multiplicity 21 would require $5\cdot21\le21$, so only the zero route remains and $W_3=7$. At index 2, $P=42$: a zero selected multiplicity 3 would require $10\mid39$, so first support is nonzero. Hence even the unpruned outer own family is the singleton

\[
(n',m')=(42,28),\quad M'=(-28,35,40),\quad
d'=(42,14,7,1),\quad V'=(3,7),\quad
\delta'=(7/6,-1/3,-2).
\]

At $D'_3$, $P=7,Q=2,A_0=1$, and selected multiplicity 7 force one factor, up to translation. At $D'_2$, $P=14,Q=7,A_0=3$. Write $p=\pi^z\prod(\pi^3-c_j)^{a_j}$. The degree equation and occurrence of multiplicity 3 leave $z\in\{2,5,8,11,14\}$; $z=2$ is the forbidden average, and $8,11,14$ cannot contain the required nonzero orbit. Thus

\[
p'_2=\pi^5(\pi^3-c)^3.
\]

The four packets have $P'$-counts $(10,6,6,6)$ and $Q'$-counts $(15,9,9,9)$, exhausting 28 and 42. Their reduced multiplicities 5 and 3 exceed the threshold 2. Proposition 5.3 sends them directly from index 2 to index 1. For multiplicity $a$, $\rho=2a$, $\kappa=14(a-2)/3$; therefore

\[
\delta_{\rm fin}=2-\frac{70\kappa}{70\rho-28},\qquad
S_{\rm packet}=\frac{42\rho\kappa}{70\rho-28}.
\]

The three nonzero packets have radius $7/6$ and contribution 3 each. The zero packet has radius $13/24$ and contribution $35/4$. No minor roots or additional major levels remain. Thus

\[
\boxed{I'_M=9+35/4=71/4\notin\mathbb Z.}
\]

The zero packet has actual $L=1$, modulus 24, and counts $(10,15)$ failing final residues. Keeping its ungated signature proves nonintegrality; filtering gives a certified empty family. Both exclude R063. Nonzero packets have $L=3$, modulus 2; their stabilizer cannot be assigned to the zero packet. No free continuation after index 1 is licensed.

Arithmetic returns ungated sums $\{71/4\}$, $\{8,592/29\}$, $\{8,146/13\}$ for R063, R009, R050; Galois filtering leaves $\varnothing,\{8\},\{8\}$. Controls verify finite-union survival, certified versus uncertified emptiness, fractional summands totaling an integer, and (CI.7) with $\operatorname{ord}Q'(\beta)\ne0$.

**Transport and the six rows: exact resolution.** On the reduced actual-image locus of the frozen orbit transport theorem, a parent major orbit has $N$ discs, $\rho$ roots per disc, first nonzero centre exponent $0<\epsilon<\delta$, and orders $\lambda_P,\lambda_Q$. Its image has

\[
N'=\epsilon N,\quad \rho'=\rho,\quad
\delta'=H+(u/\epsilon)(\delta-1),\quad
\lambda'_Q=(u/\epsilon)\lambda_Q.
\]

Thus $-N'\rho'\lambda'_Q=u(-N\rho\lambda_Q)$. Every child pole comes from a parent pole; polynomials have no poles at finite parent points. Summing gives **$I'_M=uI_M$**. Principal minors can move to finite child base and finite parent-line roots can supply child minors. Neither all packets nor minor lower bounds are preserved.

A “printed-complete parent configuration” has two meanings. For an actual partition, or jointly compatible root data satisfying all transport hypotheses, the child sum is its transported integer. A purported exhaustive child list omitting this image contradicts those premises: a parent premise, child coverage, or filter must fail.

A **necessary numerical** table reaching every printed endpoint still need not specify a polynomial pair or the joint centres, coefficients, finite-line and endpoint information for transport. It can pass parent integrality yet have no realizable child. A proved exhaustive child obstruction then excludes all realizing parents consistently with transport. A difference between independently enumerated tables alone does not prove this.

R063's earlier parent value 19 is not an actual transported integer. The gate identifies coarse-lattice/continuation artifacts; the transport report diagnoses its invalid zero-centred arrays. The valid transverse suborbit contributes 9 on both sides.

For R025–R028 and R057–R058 the frozen replay uses depth 3, cap 4 and supplies no universal maps or verified finite covers. The later gate corrects its claim that all six were parent-dead and leaves their child arguments ungated. Detailed own records and full route certificates are absent here. Their status is

`SET-VALUED / OPEN[TRANSPORT-EXHAUSTION]: certify every compatible child top, route, sibling continuation, and actual stabilizer; reconcile every purported complete parent image with the child list.`

An exhaustive nonintegral or empty child family would give `DEAD`. A certified fully transportable parent configuration, however, must have its mapped integer in a sound child enumeration. These census categories do not establish an “exactly these rows” classification of the lemma's possible applications.

**FALLACY-v2.** Counts are disjoint actual roots; stabilizers use actual nonzero support. Equality is separated from the minor floor, and necessary signatures from polynomial witnesses. The ring map and distinct base limits are declared. There is no new exit-price assertion. Promote the conditional lemma and R063 application; retain the six-row and unrestricted-final-cover OPENs. No further kills or ledger edits.

<!-- BODY-END -->
