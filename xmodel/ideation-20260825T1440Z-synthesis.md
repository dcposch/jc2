# Significant-news whole-portfolio synthesis — 2026-08-25 14:40Z

Closed: 2026-08-25 14:53Z.  This is a scheduling and experiment-design
record, not mathematical promotion.

## Trigger and sealed inputs

The round was triggered before the ordinary twelve-hour floor by two material
events:

1. the corrected AS row-8 Q9 projection is fibrewise surjective and exactly
   uniform, so the scalar `omega` is not an obstruction at Q9 alone; and
2. the repaired TD6 V64 source DAG is independently confirmed on
   `H=0,D(U*V*P3*QH)`.

All four owners independently rescanned the full 46-row portfolio before
cross-pollination.  The sealed inputs are:

| submission | SHA-256 |
|---|---|
| Sol blind scan | `86187db5ea142e783beb442a0536d162cc59734d14e8a9ef2271eb6807df5d7f` |
| cube/Q8 blind scan | `15a057a76a9776f6b05f58286ecb1f58d2bb47c0fc842018602aa48a06788959` |
| AS blind scan | `59ea8144a9aa65e690a0f5217a9e5201155fa7f1c1196e42ee08a2261bebffd2` |
| TD6 blind scan | `0d7b454cf90f421b516894cfa781562fd283ba9265eceb3f785d9fbe8a7e9348` |
| cube cross-scan | `192d27ec4fdaffc44783bf72cae63d3abc38c1c659a188e414a6d71460007235` |
| AS cross-scan | `d0c8464345376a362bce74465cad25be0560623781d70b7e408a9f77ebaca1fb` |
| TD6 cross-scan | `5a26b92bacce204bc4a4f6ad71d95c5fb71bb961811c0e8ff8b9e42014bcf79e` |

The cube blind note's reading of `zero-partial` was corrected without changing
its sealed bytes by erratum SHA
`bea627778611418a54c5ed3f20f011d71382302c8267c37e5fda5d16109e645d`.
The TD6 owner made the same correction explicitly at the start of its
post-collection cross-scan.  `Zero-partial` means a nonzero/surjective affine
coordinate whose zero hyperplane is nonempty, not an identically zero source
class.

## Portfolio verdict

The full scan did not expose a new global proof or counterexample avenue that
should displace the three live clients.  It did expose one common bottleneck:
each front lane has a strong exact fact on a localization, fibre, or projected
state and now needs a source-honest transition across the omitted strata.

| lane | banked fact | decisive missing transition |
|---|---|---|
| Q8 | reviewed finite interior/cylinder/ordinary-tangent exclusions and localized rank-drop controls | finite exceptional `c`, moving `d4`, source-horizontal projective closure including `c=infinity`, then terminal/Taylor realization |
| AS | every current nonempty Q9 fibre has an `omega=0` hyperplane; displayed degree-at-most-three Q3 successors die at row 8 | mixed predecessor/fresh terms and the complete Q8--Q3 restoration correspondence over the full fixed-support source scheme |
| TD6 | V64 original-row unit on `H=0,D(U*V*P3*QH)` | exact source identities on the four boundary leaves and an explicit five-piece `H=0` cover |

The allocation is therefore **Q8 40% / AS 30% / TD6 25% / global 5%**.
This supersedes the 11:40Z `45/30/20/5` scheduling allocation.  Allocation is
an information-rate decision, never evidence for a theorem.

## Ranked execution cards

### 1. Q8 coefficient-compact horizontal closure — 40%

Finish one proof-carrying compactification of the selected rank-drop source:

1. extract an exact original-generator cofactor over `Q(c)` and factor every
   denominator in `Q[c]`;
2. compute the finite `Q[c]` saturation before specialization, retaining
   moving `d4` and earlier source drift;
3. treat every exceptional finite factor directly;
4. compare homogenize-after-saturation with saturate-after-homogenization,
   including all source/localizer rows, irrelevant-ideal saturation, and
   contraction to the same affine chart; and
5. add joint projective/weighted charts when `d4` or another source
   coordinate can scale with `c`, rather than treating only the frozen
   `d4=1` infinity point.

Only source-horizontal survivors receive weighted initial-cone, terminal,
and both true-centre Taylor tests.  A unit landing ideal in the frozen-`d4`
slice is a finite-slice endpoint, not a global selected-open theorem.  Stop
generic fraction-field variants after a checked denominator cofactor and stop
jet sampling once exact saturation decides the relevant closure.

### 2. AS full transition scheme — 30%

Do not launch more standalone `omega` counts.  The first discriminator is the
complete mixed second-difference audit.  For a fixed predecessor and fresh
correction `27U+81V`, fresh--fresh quadratic terms vanish modulo 243, so a
`Z/9` linear module is plausible fibrewise.  Globally, an order-nine
predecessor displacement can contribute

```text
(9K)(27U)/243 = K U  (mod 3),
```

so the module is not constant over a varying Q3 base unless every such mixed
coefficient is killed by an exact source identity.

Compile all 91 original-source rows, all fixed-support output degrees 0--7,
the full Q3 kernel, fresh order-27/order-81 digits, declared integral
divisibilities, and every mixed Q3/fresh pair.  Then:

- if every mixed block vanishes exactly, freeze the fibrewise `Z/9` theorem
  and construct the complete module over its named base algebra;
- if any mixed block survives, stratify the predecessor base by Fitting ideals
  and solve linear modules only fibrewise; and
- in either case, intersect `omega=0` with the actual chronological Q8--Q3
  restoration and next-carry rows before drawing any exclusion or survival
  inference.

Only a complete fixed-support integral coefficient scheme may seed
Jacobian/Fitting, singular Hensel/deflation, Greenberg/Artin-bound, or
low-height rational-reconstruction work.  A smooth compatible `Z_3` point or
a certified formal branch would become the disproof client; an empty scheme
would exclude only that fixed support.

### 3. TD6 exact five-piece `H=0` cover — 25%

On `D(U)`, set `T=V^2/U^3`.  The exact identity

```text
((5T-136)(T^2+8T-64) - (5T+64)(T^2-32T+128))/512 = 1
```

proves that the `P3=0` and `QH=0` leaves are disjoint; it proves neither leaf
empty.  Complete exactly these five source-replayed pieces:

1. `H=0,D(U*V*P3*QH)` — reviewed V64;
2. `V=H=0,D(U)`;
3. `H=P3=0,D(U)`;
4. `H=QH=0,D(U)`; and
5. `H=U=0` rebuilt from the raw source rows.

The composed original-row DAG must itself emit denominator radical
`rad(U*V*P3*QH)`.  For set-theoretic emptiness, exact empty leaves plus the
cover suffice; a global Cech/cofactor glue is needed only if the claimed
output is one global identity.  Stop pivot-order proliferation.  Closing all
five leaves proves only `H=0` in the fixed source-typed A3/q2-beta section,
not `H!=0`, the B3 debt, all TD6, landing, or JC2.

### 4. Global wall reserve — 5%

Retain only falsifiable clients for the proof-wide missing obligations:
complete landing/coverage, `G2-PSC` where the GGV bridge is used, every
invoked `G2-BD`, and an absolute or cofinal complexity ceiling.  The most
promising reserve connections are:

- normalized blowup/Rees-integrality invariants extracted from a complete
  source boundary book; and
- a rationality contradiction for an actual irreducible component of the
  nonproperness set `A(F)`.

Positive genus is not enough: a positive-genus curve can finitely cover
`P1`.  The asymptotic-set bridge remains inactive until honest landing,
component tracking, and a degree-one/birational function-field map are
proved.  Greenberg likewise remains reserve until a single complete integral
finite-support scheme and an effective bound are named.

## Shared software accelerator

Build one source-hashed constructible-cover/transition compiler instead of
lane-specific ad hoc wrappers.  A certificate object should carry:

- immutable original source hashes and typed row/modulus provenance;
- exact transformation matrices, cofactors, and original-row proof DAGs;
- denominator radicals and cleared Bezout identities;
- saturation-before-specialization and contraction checks;
- affine, projective, weighted, and irrelevant-ideal chart metadata;
- Fitting/rank strata over the named base algebra, retaining nilpotents and
  torsion where the successor uses them; and
- a machine-checkable set-theoretic cover statement, distinct from any
  stronger global-cofactor claim.

The compiler's chief negative control is changing the order of projection,
localization, saturation, or specialization.  These operations must never be
commuted without an exact comparison certificate.

## Parallelism and stop/go rules

Producers remain on the critical path; hostile reviews run asynchronously.
A source-replayed result with reasonable producer confidence may launch a
reversible successor provisionally, but canonical promotion waits for review
and never exceeds the reviewed scope.  Mirror engines are useful when they
exercise genuinely different code paths; byte-identical reruns are custody
checks, not independent mathematics.

All sustained or uncertain computation is AWS-only.  The local Mac is for
editing, hashes, SSH/control-plane work, and lightweight cloud-model adapters.

The next mandatory blind whole-portfolio round is due no later than
**2026-08-26 02:53Z**, or immediately on earlier significant news.  The broad
web sweep remains due by **2026-08-26 01:26Z**.  No result in this round
proves or disproves the plane Jacobian conjecture.
