# F10 r1 retained17-row engineering execution

2026-09-09. Producer engineering PASS, pending independent runtime review.
This is representation validation only: no solver, ideal decision, source
point or exclusion. All mathematics ran on the one explicitly authorized AWS
worker. No local mathematical import, compilation, test or replay occurred.

## 1. Custody, authority and fixed stop

First action17:57:36 UTC. Both new owned targets were absent before transaction
begin. The accepted17g gate SHA f8a34d1c96be944fa28f8d5c5b7d60ee9ad093d7a81634c34b404524b7949435
was current-pinned before its WHOLE read. The six frozen runtime sources and
CAPRUN matched before staging; exact source/read scopes are in input-pins.json
and READ-SCOPE.md. Accepted code bodies already read WHOLE were reused only
after their byte hashes matched. No optional hardening or code edit was made.

The only worker controlled was i-08d2a40f272ee9fa2, c7i.4xlarge,
Owner=f10-r1-exact-artifact-validation-20260909,
EBSvol-0574e0fa5aed1f5e3. Exact AWS describe initially found it STOPPED;
the start response was observed17:58:17, RUNNING was confirmed17:58:40. Linux, Amazon EC2
DMI, instance tag, hostname ip-172-30-0-72 and interpreter SHA
a92f0f95e883390c7256b2e441484aac06b1002dbe1d924141a77c8d82f96223
matched on the private address172.30.0.72. A process-name-only preflight on
that owned worker found system services and no unrelated mathematical job;
no other machine or protected project was inspected.

Fresh exclusive directory:
/home/ubuntu/f10-r1-retained-runtime-validation-20260909.
The original exact.json/admissibility/frontier were regular copies, not links.
All eleven absolute runtime input hashes plus the enabled registration matched
on remote before dispatch. Registration SHA:
453c2a9dc6f00acf13f0c35a01afe61050fa99a25a98ce8dd41f53e3a396d125.
The new registration was externally pinned, frozen, and had no concurrent
authority writer. The prior disabled plan and every frozen source stayed intact.

Root's fixed mathematical deadline18:14:00UTC, task deadline18:17:00UTC,
aggregate750 seconds and independent worker STOP timer18:18:00UTC were not
reset. Per-file limit16777216 bytes was inherited through the unchanged caller.
Profiles were exactly root-assigned: probe5/3/2GiB, dummy5/3/32MiB,
transform180/170/2GiB, full-check180/170/2GiB, controls300/280/2GiB
(wall seconds/CPU seconds/sampled RSS). The fifteen-second admission margin
and original stored post-return cutoff were unchanged. These configured caps
are not measured CPU use or strict realtime guarantees.

The outer frozen dispatcher PID1408 started18:01:30 and was observed with its
exact argv. It completed18:01:41.890711. A later attempt to obtain its proc
stat found it already absent; no live main-dispatcher stat capture is claimed.
Each CAPRUN parent/child identity and namespace was captured by the frozen
caller/instrument before its payload. Their exact records remain in remote/.

## 2. Exact finite run and measurements

The unchanged batch completed the required order: five authority refusals,
valid sentinel, descendant dummy, transformer, genuine production checker,
and the finite in-process semantic harness. No retry, cap increase, altered
representation, earlier-band reduction, patch or extra mathematical run occurred.

All five refusals were ordinary nonzero exits before a sentinel write. Their
terminal stderr names the expected disabled authority, wrong host, full parent
argv mismatch, cap/argv mismatch and absent input-pin reasons. The valid
sentinel passed. The dummy leader1490 exited while child1492 retained the same
PGID/namespace and ignored TERM. CAPRUN measured77971456 RSS bytes against the
33554432-byte cap, returned125 with RESOURCE_CAP/rss, and recorded exactly
MATCH/SENT15/MATCH/SENT9, matching PID/PGID/start identity, cleanup and reap.
The leader's own returncode0 is not confused with the capped runner exit125.

| Mathematical child | PID=PGID | CAPRUN wall seconds | Maximum sampled group RSS bytes | Result |
|---|---:|---:|---:|---|
| transform | 1503 | 0.814091058 | 22761472 | NORMAL_EXIT/0 |
| genuine full-check | 1520 | 1.557439739 | 26034176 | NORMAL_EXIT/0 |
| semantic-controls | 1550 | 6.726916533 | 30838784 | NORMAL_EXIT/0 |

First mathematical admission was18:01:32.5760264; batch end was
18:01:41.890711, well before every fixed cutoff. The caller's aggregate
envelope is approximately9.315 seconds, distinct from the individual CAPRUN
wall measurements. These are observations of this one run, not forecasts for
solving or larger representations. No actual total CPU-use measurement is
invented from the registered CPU limits.

The transformer emitted retained.json SHA
88de7ea789cd77696e4e73015921554b57c10356017df8ee6d71fe5c51c21037,
307729 bytes. The immutable original was88634 bytes. The new representation
is therefore larger in serialized bytes despite eliminating three zero slots;
no speedup or smaller-byte claim follows. The genuine full receipt SHA
d356240c68f2fef7b511d66df0265fcead95402f3ebc93e46b4ed4041aea1916
reports PASS-REPRESENTATION-NOT-IDEAL-DECISION, all20 original slots,17 retained
slots, all8 bracket bands and10 inverse-pole slots, together with forcing,
envelopes, gauge, original-row substitution and top/guard restoration.

All17 retained rows have nonzero term counts in that receipt; the two highest
rows and guard have zero terms. The largest listed row is E0/S3 with271
retained terms versus78 original terms. The retained lower-parameter degree
reaches6, signed s exponent reaches-6, v degree reaches6, and recorded rational
bit length reaches69 at E0/S7. These figures are taken from the authorized
checker receipt, not a new local algebra/statistics pass. Bit length refers
to its stated numerator-or-denominator maximum, not a claim about integral
coefficient magnitude or a solved system.

## 3. Controls: exact evidence and its limits

The harness produced all twelve EXPECTED-REJECTION outcomes on freshly
written, hash-bound changed fixtures: dropped retained row, wrong L inverse,
wrong scale, wrong modulus, lost negative-s sign, forbidden lower exponent,
missing pole inventory, missing leading-a restoration, changed leading B5,
float contamination, wrong canonical string9007199254740993 and its rounded
integer string9007199254740992. The restoration omission produced the expected
KeyError string 'a'. Every expected class and reason is in controls.receipt.json.

These twelve checks use checker.verify in-process after a separately successful
production checker.main receipt. They are NOT twelve production-entrypoint
runs. Fresh fixture hashes and rereads precede semantic verification; no stale
hash rejection is counted as a semantic success. The two large strings both
fail original row substitution at E1/S0; they do not demonstrate a successful
correct-large-coefficient payload distinguished from its rounded neighbour.
Harness exact-string persistence is not positive nested-parser fidelity.
The genuine actual-payload run is the separate positive evidence.

No altered A/B negative is credited to a late bracket or inverse-pole equation:
the accepted checker would reject such an alteration at earlier source binding.
The omitted pole slot tests inventory only. Complete bracket/pole equations
are covered by the genuine positive read-back, exactly at the accepted17g tier.
No optimized-mode matrix, control-ORIGINAL variant or production API change
was introduced.

## 4. Terminal collection and independent metadata checks

Before reading any result body, the SSH caller had terminated and exact owned
script processes were absent. At18:02:10 a remote checksum manifest covered
all100 retained files. Those files were pulled and all100 local hashes matched
BEFORE batch/full/control receipt bodies were read. No live artifact or live
report was consumed. remote-prepull.sha256 preserves that first manifest.

The subsequent local work was metadata only. metadata-audit.json checks all
ten ordered operation records, authority and telemetry hashes, exact root
profiles, literal registered parent/child vectors (including the prescribed
invalid variants), input/stream hashes, namespace, PID=PGID and post-return
cutoff. argv-fingerprints.json independently matches every actual CAPRUN
canonical child-argv digest against its pinned authority, using the instrument's
compact JSON-plus-newline definition. No checker or mathematical artifact was
imported or re-executed locally. The raw authority files retain every full
argv/cap/source binding for independent review.

After reading only terminal metadata to obtain the ten actual PGIDs, an
independent remote query found no member of any of those groups, no dispatcher
1408 and no anchored owned-script process. The STOP response was observed18:03:19 and
exact AWS describe confirmed STOPPED18:04:17. Owner/type/volume remained
unchanged. No instance was terminated, no volume deleted, and the retained
EBS remains recoverable. Root's backup stop timer was left untouched.

Documentary deviations: the initial guessed ops/fleet.sh path was absent;
a narrow ops filename lookup and root's exact correction located
ops/fleet/fleet.sh, read WHOLE. Only its run/pull transport and matching direct
rsync/key flags were used; no wait/install/launch/ips/term mode. The private
key body was never read. An initial metadata projection used non-existent
telemetry field aliases and returned nulls; the exact schema was then read
and the report uses wall_elapsed_seconds/runner_exit_code only. These were
metadata corrections, not failures, patches or retries of the batch.

## Completion and remaining authority

Producer engineering PASS at the exact retained17-row representation scope,
pending different-model runtime review. No ideal unit/properness, rational or
algebraic point, source exclusion or automatic follow-on solve is established.
The earlier complete-Q attempts remain inconclusive; this run was not one.

Pending quantity: one independent review of this frozen runtime evidence.
Cheapest test: verify the terminal custody/source/argv/telemetry vectors and
read the genuine checker/control receipts at their qualified scope. No further
worker or mathematical execution is authorized by this report. Own WHOLE
read and own raised-OPEN/scope check precede the completion marker; no new OPEN
identifier is introduced. Final custody includes the100 pulled files, current
inputs and all owned metadata/report/transaction. All writers and owned
children are idle; the exact worker is STOPPED before terminal handoff.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `10578`.
- Body SHA-256:
  `a977ae9ef2aeb96061c7009753a8b5fd6741840ca82440d58a21549ff504cb6a`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
