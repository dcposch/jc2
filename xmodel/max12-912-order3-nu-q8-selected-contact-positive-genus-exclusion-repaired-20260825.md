# Selected-Q8 positive-genus trajectory exclusion — repaired composition

Date: 2026-08-25  
Status: **PRODUCER-EXACT REPAIRED COMPOSITION; fresh hostile review pending**

## 1. Exact theorem and firewall

Assume the reviewed cube/Faber and selected-global-quotient reductions place
an **actual** order-three trajectory in the strict leaf

```text
k=mu=0,  nu!=0,
```

and that its selected coefficient component meets a corrected-Q8 contact.
Then no such trajectory exists.

This statement is conditional on that registered selected-leaf landing.  It
does not exclude every `nu!=0` component, the polynomial core, `(8,12)`, all
of `(9,12)`, maximum degree twelve, an arbitrary Keller pair, or JC2.

## 2. Immutable inputs

| Input | Producer / successor SHA-256 | Independent review SHA-256 | Consumed statement |
|---|---|---|---|
| Selected global quotient | `2102e5d730af7d9bd4a434a99ea9b7213cdf08016becd0e49061f5210ec0feaa` | `49e8d9092e257a003f39171d0ec53fca91cc8f5c6e02841816f88aad127e6b39` | same six-row selected source and actual-trajectory quotient |
| Mod-127 source component | `7d28a7b4b7d3ecd3179efc73f0251754fbfea87614b6eb6daef2d7d649dba202` | `ebd0024dfe2ea1623f22ba483b93d76dd9ff2aabe5f9f96eabc7b115928c7164` | an integral relevant source component dominates `H` |
| Plane `H` integrality | `b3cddbce9a118c60bf789df374941367a327b6608871a562ddffceebc3b900d0` | `f2e24aa5baa336067479eab7549f74bd2ceda7f61b5da2a049ea75be73d835dc` | `H/F_127` is geometrically integral |
| Rational full contact on `H` | `c470fd253d59cc2f3377e5a2c03baa345c22b365522a3eb17ae320992fb5d925` | `658341959f39cdcad00e5f42d440af8bdb7fb2454b6676c96e6b00bddb10b562` | at least one of `v=26,58,67` lies on an `H`-supported source component |
| Arithmetic full-contact bridge | `39ae622919f5eba41e4e1a18d6777749d38c34a1be4a10569f65295815a11490` | `571221ad8a87c23a1ad15b4b77da3c8b9332c4d93a521d2ef6c143d5f143046d` | common integral source, full `8 x 8` unit, and the same generic selected germ |
| Positive genus of `H` | `c4c4ebdf59a95ff84c37ebbedcd46608fb32f34077790c9bdbdaac776f64a00a` | `1b79cddc6f3107c9bc722c21135fb0c87fa3e9793311c071c7a5a57a55d5bb8e` | exact `F_(127^2)` count proves `g(H)>0` |
| Conditional curve-theory audit | `53830f8b28a20491e2ef08810070c029101b9ca95db52f2d6814de29cb654c18` | `4842ff192308c2c0577be48dd370af589d822747b4f0b1109ef938aa83d26c75` | proper/ruled-residue route at its conditional scope |
| Infinity and primitive grouping | `89691023f2703eba5c0cd29d65bc8732d837905fe41b01f64ae9d3b908068168`; `9f37fc3fc7933b911bea56258f677da77a823caa8daf9ce01c03b0884c502c8f` | `c77a73307bfda48fdde7792c523e09ee4d565c5782bcf71ed13d13b1d613a57c` | nonconstant trajectory map and partition `8` or `1+...+1` |

The positive-genus producer manifest has SHA-256
`8de0e649576effad425482583fa5a474b422a5e991446888fdad9743c12578b7`;
its freeze has SHA-256
`7f134d213c45512883eb65f79b5882c160b917e1d3c21a2cc9875ca9e8fa49fd`.
The point-count review custody manifest has SHA-256
`8e7a31fd59f352d3df935465df54763e78adf3a1ff461b1bf19d6e84df2768f0`.
The repaired no-merger lemma, generic length, coordinate reconstruction, and
all-eight mod-127 grouping are not used.

## 3. Reviewed positive genus

For the pinned geometrically integral plane curve `H(w,v)=0`, the exact
complete count over `F_(127^2)` is

```text
affine points       16174
singular affine         6
smooth affine       16168
#P1(F_(127^2))      16130.
```

Smooth affine points inject into rational points of the smooth projective
normalization.  A geometrically integral genus-zero curve with the reviewed
rational smooth point is `P1`, which has only 16,130 points.  Thus

```text
g(Htilde)>0.                                             (3.1)
```

This is now a standalone different-model-CONFIRMED input.  The two AWS
counts are identical-source partitioned replay, not independent engines;
that residual engine trust is immaterial to the exact scope of the reviewed
verdict and remains disclosed.

## 4. Whole-source attachment at one full contact

The reviewed `40,960>37,010` contact theorem supplies a point

```text
q=(w=0,v=q_v),  q_v in {26,58,67},
```

on an integral mod-127 selected-source component `C` whose plane projection
dominates `H`.  It is not necessary to identify which of the three points.

Work first over the complete/Henselian unramified splitting DVR `R` of the
reviewed arithmetic bridge, with fraction field `K`, residue field `k`, and
arithmetic uniformizer 127.  Let `X/R` be the bridge's common eight-equation
localized source.  The full relative `8 x 8` determinant is an `R`-unit at
`q`; formal implicit functions therefore give the whole-source arithmetic
formal neighborhood

```text
Spf R[[w]].                                               (4.1)
```

Here the statements `K[[w]]` and `k[[w]]` mean the completed generic and
special **fibre** local rings of (4.1).  They are not being identified with
an uncompleted localization of `R[[w]]`.  The reviewed six-row uniqueness
identifies the generic fibre germ with the selected characteristic-zero
component `Y`.

Let `Z` be the scheme-theoretic closure of `Y` in `X`.  In (4.1), its ideal
maps to zero in the generic fibre completion `K[[w]]`: that generic formal
neighborhood is precisely the unique germ of `Y`.  The injection
`R[[w]] -> K[[w]]` then makes the ideal itself zero.  Hence the completed
special fibre of `Z` is `k[[w]]`.  It has one reduced branch and one local
irreducible component through `q`, with multiplicity one.

The same full-source completion also has only one special component through
`q`.  Since the reviewed contact theorem places `q` on the `H`-supported
component `C`, the special branch of the horizontal closure is dense in
`C`.  This is a whole-source component identity.  Plane point incidence
alone would not establish it.

The determinant unit, rather than any charged fibre away from `w=0`, is what
makes `w` the local parameter at `q`.  Together with the separate plane
unit `H_v(q)!=0`, it also gives local ramification index one for
`Ctilde -> Htilde` and hence separability.  Separability is a useful check,
but the genus argument below does not need it.

## 5. The attached special component has positive geometric genus

Normalize projective closures.  Dominance gives a nonconstant finite map

```text
Ctilde -> Htilde.
```

If `Ctilde` were geometrically rational, then after passing to algebraic
closures the induced inclusion

```text
kbar(Htilde) -> kbar(Ctilde)=kbar(t)
```

would make `kbar(Htilde)` rational by geometric Lueroth.  This contradicts
(3.1).  Therefore

```text
g(Ctilde)>0.                                             (5.1)
```

This proof neither claims that `k(C)` itself is `k`-rational nor requires a
separable map.  The optional separable Riemann--Hurwitz check from Section 4
gives the same conclusion.

## 6. Proper/valuation obstruction to a rational generic component

Assume for contradiction that an actual trajectory lies on `Y`.  The
reviewed infinity/passport input gives a nonconstant map from its source
`P1_x` to the smooth projective normalization `Ytilde`.  Over an algebraic
closure of `K`, geometric Lueroth makes `Ytilde` geometrically rational.
This does **not** assert that it is already `P1` over `K`.

A smooth projective geometrically rational curve acquires a rational point
over a finite extension `K'/K`.  Enlarge `K'` if necessary to contain the
finite constants used by the trajectory, complete the corresponding finite
DVR extension `R'/R`, and denote its residue field by `k'`.  Then

```text
Ytilde_(K') ~= P1_(K').                                  (6.1)
```

Geometric integrality of `C` and (5.1) survive the residue-field extension.

Now take the projective closure of the integral horizontal scheme `Z_R'`
and normalize it.  Properness is load-bearing.  The generic point of the
special component `C_(k')` has a point above it in this proper normal model;
equivalently, the divisorial valuation defined by `C` extends to the common
function field.  Its center is an irreducible vertical component `Gamma`
mapping dominantly, hence generically finitely, to `C_(k')`.  The whole-source
identity in Section 4 ensures this is the specialization of the selected
horizontal component, not an unrelated plane component.  One may dominate
this model by a regular proper arithmetic-surface model; the resulting
component still maps dominantly to `C`.

On the other hand, (6.1) identifies the generic function field with
`K'(t)`.  The ruled-residue theorem for a residually transcendental
divisorial valuation of `K'(t)` says

```text
kappa(Gamma)=ell(s)
```

for the finite relative constant extension `ell/k'`.  Dominance gives

```text
k'(C) -> kappa(Gamma),
```

and after adjoining `ell` this is an inclusion
`ell(C) -> ell(s)`.  Geometric Lueroth would make `C` rational, contradicting
(5.1).  Thus `Ytilde` is not geometrically rational and admits no nonconstant
map from `P1`.

This is the proof.  Semistable reduction and arithmetic-genus
semicontinuity are only intuition here; neither is used as a substitute for
the proper divisorial-valuation argument.

## 7. Primitive alternatives

The reviewed Galois-primitivity theorem permits exactly

```text
one characteristic-zero component containing all eight contacts, or
eight singleton characteristic-zero components.
```

The all-eight alternative is already excluded for an actual trajectory by
the reviewed infinity theorem: two distinct contacts cannot both be the
single image of `x=infinity`.

In the singleton alternative, Section 6 excludes a trajectory on the one
component `Y` attached at `q`.  The eight characteristic-zero singleton
components are `Qbar`-isomorphic Galois conjugates, so their smooth
projective normalizations have the same geometric genus.  Hence none is
dominated by `P1`.  This uses isomorphism of the characteristic-zero
components; it does **not** claim that all eight specializations are
`H`-supported.

Neither primitive alternative therefore carries the registered actual
selected-Q8 trajectory.

## 8. Exact remaining review debt

All computational and arithmetic inputs used above are frozen and reviewed,
including the standalone positive-genus count.  The sole debt for this file
is a fresh hostile review of the repaired composition, specifically the
finite-DVR rationalization, proper-component/valuation step, geometric
Lueroth uses, fibre-completion language, and singleton propagation.

No generic length, global coordinate graph, all-contact mod-127 grouping,
Padé reconstruction, Taylor-pole enumeration, terminal reconstruction, full
maximum-twelve, or JC2 conclusion may consume this report.
