# f10-contact-gram-runtime-gate-fable5-20260910 — FIRST Fable 5.1 STATIC gate: contact Gram runtime dispatcher and two-control mutator

status: UNSEALED (adapter seals; no Seal section and no charge_basis authored here; the standalone BODY-END marker is appended LAST)
lane: f10-contact-gram-runtime-gate-fable5-20260910
first_action_utc: 2026-09-10T20:42:57Z (sha256sum of all fourteen inputs and absence check of both owned targets before any body read)
stop_utc: 2026-09-10T21:04:00Z hard (launch+22, earlier than 21:08:00Z); publication reserve 21:01:00Z; original USER TERM 21:08:00/KILL 21:08:05 armed before launch, never reset
mode: manual mathematics, text, hash and metadata only; ZERO subprocess/CAS/Python import/AST/syntax/compile/test/fixture/coefficient work; every statement is a hand reconstruction from the fourteen frozen snapshots in /tmp/jc2-lane.dfUXCD/inputs
owned: xmodel/f10-contact-gram-runtime-gate-fable5-20260910.md and box/f10-contact-gram-runtime-gate-fable5-20260910/input_custody.md, both absent at 20:42:57Z; every authored byte via apply_patch only

## 0. Charged inputs, read attestation and effective authority

All fourteen inputs were hashed BEFORE any body read and every digest equals the charged list; the full sha256sum output is reproduced in the owned input_custody.md and this report carries only abbreviated forms. No two basenames collide. The first thirteen were read WHOLE: runtime report (58 lines, sealed body 6465 B), CONTRACT.md (68), READ-SCOPE.md (15), CONTRACT-DELTA.md (9), VALIDATION.md (11), mutate.py (185), probe.py (64), producer.py (282), checker.py (309), authority.py (145), the code gate (77) and the middle-real runtime gate (80), each by one bounded read reaching EOF or its marker. Two tool displays clipped: dispatch.py (583 lines) was recovered by the 1–41 preview plus three bounded range reads 40–200, 201–400, 401–583 reaching EOF, and the two prior gates were re-read whole in separate calls. run_capped.py 4435279d…96c2 is HASHONLY as charged; its accepted controller semantics enter only through the old middle-real runtime gate. Producer/checker bodies were read for CLI, exit, stdout and receipt interfaces only; their mathematics is the accepted FIRST source gate's. No provenance, live ledger, old dispatcher body or file outside the snapshot was opened; nothing was executed.

Effective release under review: the NEW dispatch.py 61f21a2c…c126 and mutate.py 70857a11…d77c, calling the unchanged producer afc617ae…ee, checker ceca0a07…06, corrected authority 96ca421f…41 (flags -E -s -S -B per CONTRACT-DELTA) and the byte-identical old probe d0df7ba6…39f4, under the unchanged CAPRUN 4435279d…96c2. The old Fable gate is a same-model scoped infrastructure import (CAPRUN interface, dummy lifecycle, refusal literals), not a new foundation review. Nothing below is an execution result.

## A. Effective release, exact role pins and early authority: CONFIRMED

`Batch.__init__` (dispatch.py 118–204) refuses before any child unless: euid 0 on linux; registration canonical, non-symlink, root-owned, no write bits, mode exactly 0444, ≤64 KiB, strict-pairs JSON, schema `f10-contact-gram-runtime/v1`, enabled True, literal JOB, exclusivity flag, caps == JOINT (600/550/2 GiB/16 MiB), profiles == PROFILES, preflight 180, positive int uid/gid, EC2 DMI vendor and `i-hex` asset tag equal to the registered instance, hostname; exactly the nine roles python/setpriv/caprun/producer/checker/authority/probe/mutator/dispatcher, each root-owned in root-owned no-0o022 ancestry (all but python/setpriv also 0o222-free); python == realpath(sys.executable); dispatcher == resolved `__file__`; outer vector `[python,-I,-S,-B,dispatcher,--registration,REG]` equal BOTH to `outer_argv` and to `/proc/self/cmdline`; six sibling sources in `source_root` and cwd == source_root; three distinct direct children of an exact dedicated tmpfs ≤16 MiB; absent durable target on a registered ext4/xfs st_dev; owned v2 cgroup membership and PID-namespace link; two offset-explicit future deadlines; native manifest schema/1..1500 bound and its pin; every role path pinned; CAPRUN pin equal to the literal 4435279d…96c2; and, fixing the old gate's A GAP, `self.pins[str(self.files[role])]==ROLE_PINS[role]` for producer/checker/authority/probe/mutator (line 194–195) with literal basenames producer.py/checker.py/authority.py/probe.py/mutate.py (201–203): the actual installed file at each role path must carry the accepted hash, a coincidentally named pin elsewhere cannot satisfy it. The three-entry SCIENCE dict (22–25) equals authority.py's (17–21) name for name and hash for hash (own text comparison); each `science_paths[name]` must be an immutable strict file with that basename and pin (196–200). Each child authority (226–247) carries exact `argv`, `executable` = the role python path, `python_flags` ['-E','-s','-S','-B'], `full_argv` = python+flags+argv, `science_paths`, caps, the registered mathematical deadline (endswith '+00:00', satisfying authority.py's offset regex), pins plus the frozen input and, for the mutator, the positive receipt; it is written O_EXCL 0444 by root and re-hashed before Popen (298). Metadata preflight precedes scientific imports in all three science CLIs: `authority.authorize` is called before `from fractions import Fraction` in producer.py 266–270, checker.py 282–286 and mutate.py 98/116–117; dispatch.py imports no scientific module at all and never parses certificate coefficients. Outer dispatcher and CAPRUN both run `-I -S -B` (141, 283). Retained ROOT duties, exactly as CONTRACT states and as the task fixes: argv[0] literally equal to sys.executable for authority.py's orig_argv test, o+rx ancestry and o+r on every pinned file for the science uid (`immutable()` tests only absence of write bits), clear environment beyond the three cleared loader/PYTHONPATH variables, native closure completeness, exclusivity. No physical or native observation is reused. CONFIRMED.

## B. Nine retained preflights and exact shutdown predicates: CONFIRMED

Hand-traced through authority.py for each `Batch.authority` mutation with the probe's `'REFUSAL: '+str(exc)` stderr and exit 2: 'disabled' → line 66 'disabled or mismatched registration'; 'wronghost' (hostname+'-WRONG', instance intact) → 74–75 'registered physical identity mismatch'; 'argv' (script_argv+['WRONG'] against sys.argv, tested at 76 before full_argv at 83) → 'cwd or full script argv mismatch'; 'caps' (rss_bytes 1, after executable/flags/full_argv/science pass) → 88–90 'caps mismatch'; 'missing' (producer pin deleted from a COPY of the pins, 227/244) → the required loop 112–115 after the science-path loop passes → 'required input pin mismatch'. dispatch.py 375–381 demands exactly these five strings with CAPRUN NORMAL_EXIT/2, resource null, empty stdout and absent payload; no generic nonzero rc counts anywhere (`normal()` 370–372). Valid probe: sentinel POST_AUTHORIZE with registered uid/gid (382–385). Two actual wrong-job science refusals (386–391): producer argv `producer.py --job WRONG-JOB --registration AUTH --output PAYLOAD` and checker argv `checker.py --job WRONG-JOB --registration AUTH --input frozen/startup-absent.input --receipt PAYLOAD` both satisfy their argparse required sets; `tag != JOB` raises at authority.py 58 before any registration or input stat, so the absent input is never touched and stderr is exactly 'INCONCLUSIVE: wrong job or platform\n', exit 2, no output. Dummy (392–415): unchanged probe fork/handshake/orphan/64 MiB under the 32 MiB profile, RESOURCE_CAP/rss, term_sent/kill_sent/leader_reaped/cleanup_complete, identity MATCH at before-term and before-kill with signals 15 and 9, four-event sequence, same PGID and namespace, distinct orphan pid, observed RSS > 32 MiB; the CAPRUN-side lifecycle is the accepted old gate C. Count: five refusals + valid + two startup + dummy = nine, all under the separate 180 s preflight window, before any mathematics. Shutdown predicates: every run requires group quiet (cgroup.procs, zombies tolerated) BEFORE telemetry, stream hashes or payload reads (334 precedes 349+), post authority hash, revalidate, frozen-input and positive-receipt post-drift, leader reaped by start identity, and stream path/hash/size binding. Code was not executed; all evidence is future. CONFIRMED.

## C. Exact outcome protocol, one formation, one independent check: CONFIRMED

Producer (416–426): rc must be 0 or 2 with NORMAL_EXIT and resource null; payload regular, non-symlink, ≤LIMIT−4096 (the producer's own emit ceiling LIMIT−RECEIPT_LIMIT is the same number); stderr empty; stdout exactly `PRODUCER_<CANDIDATE|INCONCLUSIVE> sha256=<fresh sha of the bytes> bytes=<st_size>\n`, the label taken from rc and cross-bound to producer.py 274–275 which prints the artifact status and returns 0 iff CANDIDATE. A traceback, an 'INCONCLUSIVE: …' exception line, a missing or partial artifact, or a resource cap fails one of these and STOPs; nothing reads the artifact's own status header. `freeze` renames into frozen/certificate.json, chowns root, chmods 0444, re-hashes through `immutable(…,True)` and fsyncs writer and frozen directories. Checker (427–456) receives that frozen path as `--input` with a fresh authority pin keyed by the canonical path, which checker.py 294 looks up by `path.resolve()`; receipt ≤4096 (checker ceiling min(4096, LIMIT−raw) is 4096 whenever the artifact passed its cap); required bindings schema `f10-contact-gram-check/v1`, `code == CODE` (the old gate's receipt-code GAP is fixed), `input_sha256 == before_sha`, `registration_sha256 == sha(check authority)`, stderr empty, stdout == status+'\n'. rc 2 branch: keys exactly common ∪ {reason}, status INCONCLUSIVE, reason one of the two literals at checker.py 266/268 (verified character for character), receipt frozen as checked-inconclusive.receipt.json, terminal CHECKED_INCONCLUSIVE_NO_SOURCE_OUTCOME after 9+2 = 11 calls with controls False. rc 0 branch: keys exactly common ∪ {scope, identity, j, degree_t, degree_v, bernstein_entries, retained_edges} (checker 269–273 plus the four `update` keys), literal scope and identity strings equal to checker.py 270–271 including 'Gram determinant equality NOT independently checked', degree_v '52', retained edges, bounded integer strings with 0≤j≤52, degree_t = 52−j and entries = (53−j)·53 (checker (n+1)·53 with n = 52−j). A producer-INCONCLUSIVE artifact still receives the checker (427 is unconditional after the freeze), and only the checker's verdict is used. Uncaught IndexError/AttributeError exit 1 with a traceback fail `normal()` — refusal, never success. Counts: success 9+1+1+1+2 = 14, checked-INCONCLUSIVE 11; the loop at 375 is over a five-entry dict and the startup loop over two roles, no off-by-one. CONFIRMED.

## D. One independent mutator, two fixed controls after a positive check: CONFIRMED

mutate.py imports argparse/copy/hashlib/json/os/pathlib/re/sys and the metadata authority only; no producer or checker symbol. Order: `authority.authorize('mutate', job, AUTH, [uphi,bernstein,receipt], input)` (98) → `code_pins()==CODE` (99) → self pin by resolved `__file__` (100–101, the ROLE_PINS mutator path) → positive receipt pin, ≤4096, strict JSON, schema/positive status/code (102–108) → input regular, ≤LIMIT−4096, raw sha equal to the authority pin AND to the receipt's input_sha256 (109–114) → only then `from fractions import Fraction` (116). `strict_json(raw, wire=True)` rejects duplicate keys, floats, NaN/Infinity and every JSON integer; `validate()` (52–83) independently checks the exact ten keys, schema, science == authority.SCIENCE, code == CODE, variables, the seven literal bounds, status/reason, the exact four-polynomial inventory, term shape, strictly increasing exponent tuples, the weight 52/64 and V 0/3/3/5 envelopes, nonzero canonical reduced rationals with positive denominator, ≤12000 bits and ≤4096 characters (regex before int, under the authority's 4096-digit conversion guard; 12000 bits is ≤3613 digits so `str()` of a mutated value never trips it), j in 0..52, dense keys, degree_t = 52−j, degree_v '52', 53−j rows of 53 rationals. `canonical(doc)==raw` (120) uses the same json.dumps arguments as authority.emit, so a genuine producer artifact round-trips byte for byte. Control 1 (121–138): the constant term, if present, is `terms[0]` because tuples are sorted ascending; old = its pair or ['0','1']; new = old+1 re-reduced and re-bounded ('fixed mutation unavailable' on cap); deletion iff new is zero; insertion at index 0 preserves order; `validate(first)` re-runs the whole schema; the inverse restores the whole Uphi subtree and requires whole-byte equality with raw. Control 2 (139–147): the EXISTING entry [0][0] (present in every valid rectangle) +1, no search; coordinate inverse; whole-byte equality. Three distinct SHAs required (150). Expected checker branches, by the accepted source: Uphi+1 keeps every envelope and changes the identity by 1·Phi, whose V^4 coefficient 1 the V-free R cannot match, so checker.py 231–232 raises 'full contact cofactor identity' after parsing and reconstruction, before pullback; Bernstein[0][0]+1 keeps a reduced pair (gcd(n+d,d)=gcd(n,d)) and changes the expansion by the nonzero basis element (1−t)^(52−j)(1−v)^52, so 260–261 raises 'full Bernstein expansion' after identity, pullback, factor count and rectangle checks and before the row-0 test. Each raise is caught by the checker's `__main__` handler → stderr `INCONCLUSIVE: <message>\n`, exit 2, no emit, so no receipt and empty stdout; dispatch.py 500–507 demands exactly NORMAL_EXIT/2, resource null, absent receipt, empty stdout and the exact string, so stale-hash ('required input pin mismatch'), parser, authority, resource or traceback outcomes never count. Receipt (151–165) keys equal the dispatcher's eleven (463–472); variant keys base ∪ {constant_existed, term_deleted} / {row, column} (474–486); paths equal writer/uphi.json and writer/bernstein.json, which the dispatcher freezes root-0444 and pins in two FRESH authorities for the SAME checker path (503–504, 228–229). Mutator normal 0 prints only TWO_MUTATIONS_PREPARED_NO_SCIENCE with empty stderr (460–461); any 'unavailable' ValueError exits 2 and STOPs at `normal(t,0)`. The controls run only after the positive branch (the rc-2 branch returns at 444). CONFIRMED. Precision note, not blocking: on an unavailable control the dispatcher's STOP reason is the generic 'unexpected non-normal or child return'; the explicit cause lives in the custodied mutate.stderr.

## E. Physical supervision and resource sums: CONFIRMED as fail-closed, GAP (liveness) on the negative profile

Literal JOINT/PROFILES must equal the registration; profiles [wall, softCPU, RSS] preflight [5,3,2 GiB], dummy [5,3,32 MiB], produce [400,359], check [90,89], mutate [10,9], negative [30,29]; CAPRUN hard CPU is soft+1, so child hard CPU sums to 360+90+10+30+30 = 520 ≤ 550 and nominal science wall to 560 ≤ 600. Accounting is real, not assumed: `math_begin`/`math_cpu_start` are set once at the producer's admission and never reset (252–256); each admission needs `wall+15` inside both 600−elapsed and absolute−now (260–261); the live loop SIGTERMs CAPRUN at before+wall+15 or at the absolute deadline and raises a typed STOP either way (313–319); post-return requires now<absolute, elapsed ≤600 and RUSAGE_CHILDREN CPU ≤550 including reaped CAPRUN/ps overhead (341–346); preflight has its own 180 s aggregate. Two consequences that are honest STOPs, not false positives: (1) the +15 admission margin means that if every phase ran to its wall cap the cumulative dispatcher overhead (revalidate hashes every pin AND every native-closure file, up to 1500, twice per run) before the second negative may not exceed 25 s, not 40; (2) the 0.05 s ps sampling is hard-coded in the command vector (285), so the 30 s CPU slack absorbs all wrapper CPU over 560 s. RSS is CAPRUN's sampled group RSS; RLIMIT_FSIZE = LIMIT is supplementary; the tmpfs capacity ≤16 MiB is the aggregate physical bound for baseline, two variants, receipts, authorities, telemetry and streams: the mutator checks the triple sum plus receipt ≤ LIMIT and free space ≥ outputs+65536 (164–171), the dispatcher re-checks the triple after freezing (498), the baseline is renamed not copied, and any later ENOSPC is an exception → STOP_NONDECISION with custody attempted. No retry, cap rise or alternate engine exists in either source. Namespace/PGID/exact-descendant checks, quiet-before-body and immutable pre/post pins are as in B. Durable custody fsyncs every durable directory bottom-up (556–557) but not the durable parent, and the outer stdout location is unverified: both remain the explicit ROOT duties the task lists.

GAP (liveness, the smallest genuinely blocking correction if the 14-call success status is to be reachable): the negative-bernstein checker performs the SAME computation as the positive check up to its final comparison (checker.py 228–261: parse, reconstruction, three products, identity, pullback, edge factors, rectangle parse, full expansion), and the negative-uphi checker performs everything through the three products; yet their profile is [30,29] against the positive check's [90,89]. If the positive check consumes more than 29 s CPU or 30 s wall, the second control hits RESOURCE_CAP by construction and the batch ends STOP_NONDECISION after a genuine positive receipt. This is fail-closed and unmeasured, not a soundness defect. Smallest correction: make 'negative' dominate 'check' (e.g. [90,89,RSS]) and rebalance the phase sum under 600 (e.g. produce [300,259] + 90 + 10 + 90 + 90 = 580), changing together dispatch.py PROFILES (36–38), the registration's profiles/commands, CONTRACT.md's profile sentence and the runtime report's budget paragraph. That is a ROOT/author re-registration decision; no other clause is affected.

## F. First bounded execution readiness: CONFIRMED for one observation run; success reachability conditional on E

No blocking soundness defect was found in the new caller or mutator: every acceptance predicate is exact, every failure is a typed STOP, no verdict is promoted from a producer header, no control is counted on a parser, authority, stale-hash or resource branch, and no worker is held idle by this review. After ROOT's fully prepared offline deployment (exact installed role hashes, resolved interpreter as argv[0], 0755 ancestry and o+r pins for the science uid, source and native manifest outside the tmpfs, canonical escape-free cgroup/loader paths, integer st_dev, pure v2 unit, all fourteen literal command vectors plus the outer vector, minimal environment, offset deadlines leaving custody time, native closure) one bounded execution is ready. With the profiles as frozen, ROOT should expect that run to reach at most the mutator and the first control unless the positive check is fast; that outcome would still be honest custody and the first measurement of the check cost. No certificate positivity, contact, source, degree or JC2 claim follows from this gate.

## Verdicts and smallest correction

| item | verdict |
|---|---|
| A effective release, exact role-path pins, SCIENCE equality, corrected flags/full_argv/science_paths, metadata-before-import, outer -I -S -B | CONFIRMED |
| B five literal refusals, sentinel, two actual wrong-job CLIs, dummy, nine preflights, quiet-before-body | CONFIRMED |
| C producer 0/2 exact stdout/artifact protocol, freeze, checker 0/2 receipt keys and literals, 14/11 counts, no header promotion | CONFIRMED |
| D independent mutator order, canonical wire checks, Uphi+1 and Bernstein[0][0]+1, whole-byte inverses, fresh pins, exact negative branches | CONFIRMED |
| E accounting, admission margins, tmpfs aggregate, no retry | CONFIRMED fail-closed; GAP liveness: negative profile [30,29] below the identical-work positive check [90,89] |
| F one bounded observation run after ROOT deployment | CONFIRMED (success status conditional on E) |

REFUTED: none. Blocking soundness defect: none. Smallest correction, only if the 14-call success status must be reachable in the first run: raise the negative profile to dominate the check profile and rebalance the phase sum under 600, in the four places named under E. Profiles remain UNMEASURED; metadata parsing here is never coefficient arithmetic or source positivity.

## OPEN(S) RAISED

None. No new canonical OPEN ID. The runtime report's single residual quantity (independent static correctness of this exact frozen interface) is answered by this gate with the E liveness GAP; one separately ROOT-registered bounded execution remains unauthorized here.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly the report and box/f10-contact-gram-runtime-gate-fable5-20260910/input_custody.md, both absent at first action; apply_patch only; no Seal, no charge_basis, no source edit, no fixture, no subprocess, no execution of any input.

## Completion

Authoring attestation: both owned files were written by apply_patch only (custody file and report skeleton with sections 0–F, verdicts, OPEN(S) and COLLISIONS in one transaction at 20:52:20 UTC; this section afterwards); no Write/Edit tool, no shell redirection to any owned file, no helper-generated text, no execution of any kind. Own checks completed 20:53:01 UTC before this section and before the 21:01:00 reserve: all fourteen input postpins re-hashed, equal to the charged list (sha256sum -c: 14 OK) and byte-identical to the custody rows; zero standalone markers in both owned files (the status line names the marker in prose only); zero charge_basis lines and zero Seal headings; all fourteen distinct 64-hex tokens in the owned files are live input digests with no foreign token, and each of the seven abbreviated prefixes resolves to exactly one live pin; every section is under 1500 words (largest 501). Own WHOLE unclipped read of the report (63 lines, 21191 bytes before this section) and of the custody file (18 lines) completed 20:53:01 UTC. Read scope: thirteen inputs WHOLE with the two clipped displays recovered by bounded range reads, run_capped.py HASHONLY as charged. Raised-OPEN check (none) and collision check (EMPTY) precede the marker. Verdict for a reader of this section alone: A–D and F CONFIRMED, E CONFIRMED fail-closed with one liveness GAP: the negative-control profile [30,29] is below the positive check profile [90,89] although the Bernstein control repeats the whole positive computation, so the 14-call success status is reachable only if the positive check fits 29 s CPU; smallest correction is a PROFILES rebalance in dispatch.py 36–38 plus the matching registration, CONTRACT and report clauses, a ROOT/author decision. No soundness defect, no false-positive path, no follow-on, computation, registration, seal or charge_basis is authored; ROOT retains custody, deployment, native closure and any runtime authority. The marker below is the only standalone marker in this file and nothing follows it. All writers idle at the marker.

<!-- BODY-END -->
