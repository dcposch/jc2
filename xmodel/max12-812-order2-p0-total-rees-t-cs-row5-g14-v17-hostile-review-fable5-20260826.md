# Hostile review: total row-5 grade-14 coefficient export V17

Reviewer: Fable 5 (independent, hostile). Date: 2026-08-26.
Package: `cases/max12_812_order2_p0_total_rees_t_cs_row5_g14_export_v17_20260826/`.
No producer PASS string, prose claim, or finite-field agreement was trusted.
Every number below was recomputed with a from-scratch exact-`Fraction`
sparse-polynomial engine written for this review (staged in `/tmp`, no repo
edits besides this report), not by re-running the producer's compiler.

## Verdict summary

The package is honest. My independent engine, fed only the frozen
`tails.json` row 5 and the literal total source written in the
preregistration, reproduces both exported coefficient files
**byte-for-byte**, matches the V9 grade-10/11/12 exports exactly over Q and
mod 65521, and confirms the grade-14 term count (304), rho parity, the exact
odd-sheet value `-(21/320)*b^5*w^2`, and the negative normalization control
(`11/5` gives `-(41/640)*b^5*w^2`). The known V8/V9 doubled-`TPhi` defect
provably cannot reach the bridged coefficients. Custody gaps found are
attestation-strength gaps only and are fully covered by the byte-identical
independent reproduction.

## 1. Freeze, harvest, custody

Verified locally:

- `FREEZE.sha256`: all 10 entries OK (5 package sources, the pinned replay
  `replay_row5_grade14.py` = `2c5528…`, `tails.json` = `d72f77…`, both V9
  `COEFFICIENTS.json` manifests, `ops/aws_exact_lane.sh`).
- On-host freeze re-check at job start in both lanes
  (`freeze_check.stdout`, all OK, hash-bound in evidence).
- `EVIDENCE.sha256` per lane: all 18 listed files match the harvested local
  tree byte-for-byte after AWS→local path rewrite; **no** local file in
  either lane directory is uncovered by its manifest.
- `RESULT.json` internal cross-references: 19/19 checks pass per lane —
  `compiler_result_sha256` = digest of `compiled/result.json`;
  coefficient / control-script / stdout / resource-stderr digests all match
  the harvested files; `validator.stdout` JSON equals `RESULT.json`;
  compiled `result.json` pins replay, tails, and preregistration hashes to
  the frozen values; recorded V9 reference digests (`5c1d7a…`, `ca08b2…`,
  `9389b3…` for Q) equal the actual local V9 poly files; the bridge scripts
  embed the V9 reference texts **verbatim** and the `G14` line equals the
  exported `.poly` file text.
- Return codes: `compiler_rc=0`, `engine_rc=0`, `validator_rc=0` in both
  lanes; `/usr/bin/time` reports `Exit status: 0` for compile and engine; no
  `FAIL_`, `?`, `Killed`, or memory tokens anywhere. All nine required
  Singular tokens appear exactly once per lane. The identical
  `stdout_sha256` across the two lanes is expected: the control scripts
  print only the fixed token list.
- Timing is coherent: launches 21:42:48Z on two hosts
  (`ip-172-30-0-186` for Q, `ip-172-30-0-45` for p65521), engine at
  21:42:53Z, local harvest mtimes 14:43 PDT = 21:43Z.

Custody gaps (stated, none load-bearing):

1. `EVIDENCE.sha256` is generated by `run_aws.sh` itself at job end —
   self-attestation, no external co-signature, no EC2 instance-identity
   document, no independent harvest log.
2. The `require_aws` gate (DMI vendor file + `JC2_REGISTERED_AWS_LANE` env
   var) is spoofable by any Linux host; "registered AWS lane" is a policy
   attestation, not evidence.
3. `launch_host.sh` maps Box02/Box03/r6d to public IPs, but the harvested
   registrations record only internal hostnames, and the remote
   `*.outer.stdout`/`*.outer.stderr` launcher logs were not harvested; the
   box↔lane assignment is not reconstructible from the package.
4. The Singular tokens `V17_NORMALIZED_ODD_SPECIALIZATION=1` and
   `V17_NEGATIVE_NORMALIZATION_CONTROL=1` are **unconditional prints**; those
   two checks are enforced only Python-side in the compiler (which fails
   closed before writing the script). The Singular engine independently
   checks only the three bridges, `G14!=0`, and rho parity.

All four gaps are closed for the mathematical claim by §4: the exported
bytes are exactly what honest recomputation from frozen inputs yields.

## 2. Compiler trace to the literal total source

`export_row5_g14_v17.py` was read line-by-line and re-implemented
independently:

- **Source**: `p` has `p[0] = -2*rho^2` and `p[i] = 2*ell_i` for `i = 1..14`
  — the literal total moving-p source, with `rho` and `ell1` free.
  `c = sigma^2*(cs + cs1*sigma + … + cs12*sigma^12)`,
  `r = (p^2 + sigma^2*(rs + … + rs12*sigma^12))/4`, `rs` free.
- **A/C corrections**: `az/ac/ez/ec` dense at degrees 0–9
  (`a1,aa1,aaa1,az3..az9` / `a0,aa0,aaa0,ac3..ac9` / `c1,e1,ee1,ez3..ez9` /
  `c0,e0,ee0,ec3..ec9`), entering the emitter at sigma-degree `d+5`
  (shift 3 then 2). Degrees ≤ 9 are exactly the entries that can reach grade
  14 (`d+5 ≤ 14`); the frozen replay's degree-10/11 entries are dead above
  the truncation in both codes. **No omission.**
- **Loads**: `k10 = (k,k1,k2c,k10_3..k10_10)` at shift 4 (live `d ≤ 10`),
  `k6 = (k6,k6_1,k6_2)` at shift 12 (live `d ≤ 2`), `k2` at shift 20 —
  structurally zero through grade 14 in both the replay and V17. Of the 89
  tails, 30 carry the `k10` load, 11 carry `k6`, 4 carry `k2`; the `k6`- and
  `k2`-loaded tails contribute nothing ≤ grade 14 as a computed fact, with
  the loads included, not by omission.
- **Tails**: row "5" of the frozen `tails.json` has exactly 89 entries; all
  are length-10, satisfy the weight contract
  `sum(e_i * w_i) = 17` with `w = (8,7,6,5,4,3,2,2,6,10)`, have load
  exponents in {0,1}, string-encoded rational coefficients (no floats), no
  duplicate monomials, no zero coefficients. The canonical all-tail digest I
  recomputed, `6eed03d4…`, equals the pin inside the independent replay.
- **No stale specialized-source import**: the replay module's specialized
  odd source lives in its `main()`, which is not executed under
  `exec_module` (the `__name__` guard holds); V17 imports only the
  arithmetic primitives, and pins both the replay file and `tails.json` by
  hash **before** exec — the static-pin gap seen earlier in
  `shared_faber_probe` does not recur here.
- **No duplicate conventions**: a cross-slot name-collision audit over all
  ten series is empty. The degree-≤2 slot names (`aaa1`, `aaa0`, `ee1`,
  `ee0`, `k1`, `k2c`) are locked to V9's convention by the grade-12 bridge
  (all six appear in `Tg12_5`); `k2c` correctly avoids the dead `k2` load
  name. The support of the four exported grades is exactly the declared
  40-variable ring.
- **No post-freeze mutation**: frozen hashes verified locally and on host;
  §4 reproduces the outputs from the frozen inputs alone.

## 3. Grade-10/11/12 bridge against V9, and the TPhi defect

My engine's `total[10]`, `total[11]`, `total[12]` match the frozen V9
exports `Tg10_5/Tg11_5/Tg12_5` **exactly** — term-by-term over Q
(5 / 18 / 58 terms) and, independently reduced, mod 65521 against the
`aws_p65521_v9` files. All 42 V9 coefficient file digests in both V9 lanes
match their `COEFFICIENTS.json` manifests.

**The doubled-`TPhi` rebuild defect (V8 review M1) cannot affect any
coefficient used here.** In the harvested V9 row-5 program
(`t_rs0_row5_q.sing`, 2043 lines) the extraction prints and the V9-inserted
`write(...)` exports sit at lines 1385–1412, strictly **before** the second
`TPhi=0;` at line 1432 that opens the rebuilt release-line schedule. The V9
compile census (`export_count == 6` per row) would also have failed had the
insertion markers been duplicated. The exports are first-schedule values;
my recomputation independently confirms they are the true coefficients.

Separately, I found and explain the V9 telemetry discrepancy: V9's
`coefficient_term_telemetry` (6/19/59) exceeds the true term counts
(5/18/58) by one because `validate_coeffexport_v9.py:128` counts sign
characters (`value.count("+") + value.count("-") + 1`) and each of these
polynomials has a negative leading coefficient. Cosmetic only: V17 consumes
the hash-bound files, never the telemetry.

## 4. Grade-14 coefficient: independent reproduction

From the frozen inputs alone, my engine (plain iterative powering, its own
monomial representation — algorithmically independent of the replay's
repeated-squaring code) produces:

- `total[14]` with exactly **304 terms**, nonzero, and even in `rho` at
  every exported grade (10–14, including the unexported grade 13).
- Re-serialized in the producer's format, my `total[14]` is
  **byte-identical** to `aws_q/compiled/Tg14_5_q.poly`
  (sha256 `91d969…`, the value bound in `RESULT.json` and evidence), and
  its mod-65521 image is byte-identical to
  `aws_p65521/compiled/Tg14_5_p65521.poly` (`760d4f…`).
- Grades 0–9 vanish identically; grade 13 has 141 terms (computed, not
  exported or claimed — consistent with the registered scope).
- Contributing-tail counts match `result.json`: 73/73/73/83.
- No grade-14 coefficient vanishes mod 65521 or mod 32003, so the modular
  lane has full 304-term support.

**Odd-sheet specialization.** Under
`rho=ell1=rs=a0=c1=c0=e0=0, cs=b, e1=b^2*w, k=(12/5)*w^2`, grades 10–13 of
the total series vanish identically and grade 14 collapses to exactly
`-(21/320)*b^5*w^2` — coherent with the independently reviewed odd replay at
every grade, not just 14. The cancellation is sharp: precisely two of the
304 monomials contain none of the zeroed variables,

```text
(-3/64)*cs*e1^2      -> -3/64            * b^5*w^2
(-1/128)*cs^5*k      -> -(1/128)*(12/5)  * b^5*w^2 = -3/160 * b^5*w^2
```

and `-15/320 - 6/320 = -21/320` by hand. Every other live correction term
(302 of them, including all `a*/aa*/aaa*/az/ac/ez/ec/k1/k2c/k10_*`
corrections) carries a zeroed variable and dies; nothing was dropped before
substitution — the byte-identical file is the pre-substitution object.

**Negative control.** Replacing `12/5` by `11/5` changes only the `cs^5*k`
survivor: `-3/64 - (1/128)(11/5) = -41/640 ≠ -21/320`. The control is
genuine, not vacuous: the output is affine in the `k`-normalization with
nonzero slope `-(1/128)*b^5*w^2`, so it separates every wrong normalization,
and the producer's fail condition (equal to the registered value or to the
`12/5` result) is checked Python-side before any artifact is written.

## 5. F65521 lane is a control only

In the compiler, the characteristic influences nothing but (a) which V9
reference lane is loaded for the bridge, (b) coefficient serialization, and
(c) the ring characteristic of the control script. The series arithmetic is
exact `Fraction` in both lanes. The characteristic-zero lane reads only the
characteristic-zero V9 references and runs its Singular bridge over Q. No
modular result feeds the characteristic-zero claim in either direction; the
p65521 lane is an independent encoding/software control, which my mod-65521
recomputation confirms independently anyway.

## 6. Scope enforcement

The preregistration and `RESULT.json` statuses claim only a source-honest
total coefficient export with a reviewed low-grade bridge. Confirmed limits:

- It does **not** make the T-cs ideal a unit and does not prove `rho` is a
  unit (a separate V18 case exists for that question and is not reviewed
  here).
- It does **not** form the ordered Rees chart, close Gate T, order two, or
  maximum twelve, and does **not** resolve JC2.
- The grade-14 value is unit-relevant only after the odd-sheet
  specialization on `D(b*w)`; the 304-term total coefficient itself carries
  free correction variables.
- Grade 13 (141 terms) exists in the computed series but is unexported and
  unclaimed.

## Review gaps

- No local Singular execution and no AWS access this session; engine-side
  bridge equality was instead re-established by my own exact algebra, which
  is strictly stronger than replaying the PASS.
- AWS host provenance rests on the self-attested logs (§1 gaps 1–3);
  the mathematics does not depend on it.

## Strongest surviving statement

The frozen 89-tail row-5 emitter, evaluated on the literal total source
`p = -2*rho^2 + sum_{i=1..14} 2*ell_i*sigma^i` with the complete live
correction and load series, has grade-10/11/12 coefficients exactly equal to
the frozen V9 exports, and its grade-14 coefficient is the exported 304-term,
`rho`-even polynomial whose odd-sheet specialization is exactly
`-(21/320)*b^5*w^2`, with the `11/5` normalization control distinguishing.

`VERDICT: CONFIRMED`
