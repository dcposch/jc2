# Fixed-r3 one-place full-row-rank instrument — STATIC, UNEXECUTED

Owner: model_productivity. First action 2026-09-11 01:05:49 UTC. Original publication reserve 01:36 UTC; hard stop 01:39 UTC, unchanged. Basis 0d39df3c9fd69c939a8420c54d03228b9077777d.

This package supplies prospective finder/checker source, not an actual finite place, baseline, rank result, certificate, deployment or execution authority. The accepted conditional rank and complete-source interfaces are imported at their scoped statements. No scientific program, syntax checker, import, AST, test, fixture, coefficient construction, or mathematical subprocess was executed.

## Deliverable

Four siblings implement an AWS/ROOT metadata interlock, strict wire parser, one-place finder, and independent inverse-witness checker. The two mathematical implementations independently rebuild the literal monic septic at tau=3/10, validate the prescribed residue field, reduce all 25 complete graph slots, and enumerate the entire weight-30 matrix. They share only serialization/authority glue, never field arithmetic, numerical columns, elimination, or a producer-supplied matrix.

The source and full contract are in box/f10-source-cone-r3-rank-code-astra-20260911/. Publication remains UNREVIEWED STATIC; future ROOT qualification and a separate first different-model source review are prerequisites to any further use.

## Complete witness and independence

The prospective finder works at exactly one prescribed prime p<=2147483647 and monic irreducible factor phi of the actual monic P7, with residue degree 1..7. Both mathematical sides independently check primality, divisibility and irreducibility, actual P7 equality, full seven-coordinate rational integrality, and homogeneous support before projection. All 25 graph slots and the guarded target g are retained; extra graph metadata is conservatively denominator checked too. No characteristic-zero component, root, favorable prime or actual coefficients have been selected.

The fixed universe is all 297 weight-30 monomials and all 1453 slot/multiplier columns. Finder performs finite-field pivoting on the complete rectangular map, reconstructs its selected square minor and emits a 297-by-297 inverse. Checker does not trust its matrix, elimination or determinant: it independently uses row-minus-multiplier lookup into the full frozen graph slots and a distinct finite-field multiplication implementation to check every entry of M W=I. A supplied rank or a solved right-hand side is insufficient. Source parsing is deliberately not a second Euler/source verifier: the authenticated ROOT qualification of the original complete source packet is an explicit separate premise.

The independent arithmetic branches reconstruct the same rational septic by different coefficient accumulation code and enumerate monomials by nested loops versus recursion. Only the strict serializer and authority interlock are shared. The checker never imports finder.py, accepted producer arithmetic, or source checker arithmetic. Its single positive status is R3_FULL_ROW_RANK_CHECKED_NO_AUTOMATIC_SOURCE_PROMOTION; science is NONE in all receipts. Accepted rank19z supplies the conditional full-field theorem, not an observed rank result.

## Fail-closed boundaries

The exact contract binds Linux/AWS, fixed tag/modes, effective argv with one typed authority-hash hole, four ROOT-owned frozen source siblings, interpreter/native inventory, registered caps/expiry, baseline and genuine prior source-checker success evidence, place, certificate and absent receipt. ROOT must separately authenticate startup/native closure, physical identity, registration, cgroup supervision, aggregate storage and quiet durable collection. Local hash equality is not evidence that a source check actually occurred and does not authenticate itself. No dispatcher or CAPRUN modification is delivered.

Strict wire rejects native JSON numbers, duplicate keys, malformed rationals, missing coordinates, bad sparse ordering and wrong weights. Canonical residue strings are dense and fixed-length over the selected field. Invalid place, nonintegral dense coordinates, malformed input, rank deficiency and cap outcomes are distinct nondecisions; no failed test proves a source point, negative membership or regularity failure. Write, authority, external cap and unexpected failures are STOP. Partial output is never a certificate. The fixed scope does not exploit possible denominator-clearing improvements or used-column-only integrality.

The sole prospective semantic control changes the constant coordinate of inverse[0][0] by +1 modulo p, with fresh authority binding and exact inverse restoration. Its product defect is the nonzero first column of an invertible M. Only the exact full-identity rejection counts; schema, stale pin, place, resource and authorization refusals do not. This is SPEC/NOT_RUN; no mutator, fixture or extra control farm exists.

## Decision and remaining quantity

Outcome: COMPLETE STATIC CANDIDATE, NOT EXECUTED OR ACCEPTED CODE. The required unknown is rank of the actual complete map at one genuinely specified good residue field, not a sampled submatrix or generator subset. A nonzero 297-minor with independently checked inverse is the cheapest selected decisive instrument in this task; the accepted exact unit-pivot method remains a separate fallback. No new mechanism, ideal, canonical OPEN ID or source closure is claimed.

The quantitative prospective budget is one jointly registered 600-second wall / 550-second CPU / 8-GiB observation, with 128-MiB baseline, 16-MiB certificate and 32768-byte receipt bounds. These numbers are unmeasured ceilings only. Exact phase allocation, actual matrix cost, certificate fit, aggregate storage and feasibility are UNKNOWN; no numeric runtime estimate is evidence. No prime farm, retry, alternative engine, worker, review launch or descendant is authorized.

## Read and ownership audit

All ten charged objects were freshly read whole after current-pin checks, including the root card, full source code, conditional rank producer/review and source interface. PINS.json and READ-SCOPE.md identify exact paths, hashes, lengths and clipping recovery. No prior-body reuse was claimed. Source/checker status and schema were additionally verified by literal text inspection of the already charged sources. Only documentary transaction metadata was executed.

All four own source files and all authored documentation received whole manual text review; no syntax/import/test claim is made. Input postpins and owned hashes are in terminal custody, excluding custody itself from its self-referential vector. Own raised-OPEN quantity: one existing prospective exact-rank observation, no new ID; cheapest test and unmeasured numerical ceiling are stated above. Collision scope is solely the new report/manifest/box and its own transaction, not an uncharged historical search. The transaction is sealed only after final whole review and the unique last marker. All writers will be IDLE before metadata-first collection.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7097`.
- Body SHA-256:
  `d3fc718e3ca04557850b43bd7ce66e6346d9936cdb33569360f57f7ae27989d9`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
