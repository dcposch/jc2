# Promotion: D1 target-free primary-C2 closed tails, `3<=c<=7`

Date: 2026-08-26

Status: **PROMOTED AFTER INDEPENDENT HOSTILE REVIEW.**

## Promoted theorem

After the frozen generic-square and D1 unit-load source gates, over
characteristic zero the complete seven Faber equations are
scheme-theoretically empty on

```
D(p*k0),
ord(C)=c for an integer 3<=c<=7,
ord(A)>=c,
ord(R)>=c-1.
```

At the ceiling `T=10+2c`, the unique pole-two source is
`(3/8)C^2/L^2`; every other licensed boundary source has pole one and every
target begins at grade 28.  On the finite etale root cover the two terminal
forms are `(3/8)C(+lambda)^2` and `(3/8)C(-lambda)^2`.  Their ideals are
units before radicals on both exact-C charts `D(c1)` and `D(c0)`.
Increasing the A or R contact only delays source families, so the result
covers the stated closed tails without inverting an A or R coefficient.

## Immutable custody

- producer `RESULT.md` SHA-256:
  `ed61c925e91f96af4caa62dc03c9c3c4c730a36196e0e5199db8ce877ab2dbd4`
- producer `EVIDENCE.sha256` SHA-256:
  `0e00de55745e4f004847cd50f8a951551b3b62f98d4285a1c4b0e3ee3aebf682`
- producer `PRODUCER_FREEZE.sha256` SHA-256:
  `b9ca9fa21a94b167e451490eef8a1c7fdf7c01c3e989c606cc367a8ac8197087`
- hostile review SHA-256:
  `90a05d4663214af5e2a0791dd05c03d0f97787f91fccc07cf8c8be805ead593c`
  (`CONFIRMED`)

The exact-Q Box02 endpoint and the independent F65519/F65521 controls all
returned rc 0, zero swap, and byte-identical mathematical stdout SHA-256
`0567178e69d23bdf79f55defefd4ba6729f237c95349ba2708cc416e9a32cf91`.
The programs use ordinary polynomial rings and explicit reductions; the
Singular qring noncanonical-assignment hazard does not occur.

## Firewall

The promotion stops at `c=7`.  At `c=8`, `k6*C/L` begins at grade 25 and
its grade-26 moving connection contributes a new pole-two term, so the pure
square functional is not licensed.  This theorem does not cover `c>=8`, a
tied or other primary face, positive leading load, `p=0`, `k0=0`, the exact
square zero section, another D1/terminal/global chart, fan exhaustiveness,
the whole square component, maximum twelve, or JC2.
