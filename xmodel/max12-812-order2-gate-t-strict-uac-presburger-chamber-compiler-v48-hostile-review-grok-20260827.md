# Hostile review: V48 bounded strict unique-`AC` Presburger chamber compiler

| Field | Value |
|---|---|
| Charged target | additive V48 strict unique-`AC` Presburger chamber compiler |
| Reviewer / model | Grok 4.6 (xAI). Independent different-model adversarial desk review |
| Method | SHA-256 of every charged file, both inner freezes, and all ten `source_pins` before any producer `PASS` line was treated as evidence; a second `Fraction` enumerator that expands the four SCHEMA atoms inside the four binomial summands from nested counts and sequential binomial coefficients, never reading `COMPILED.json` or importing `compile_presburger_chambers.generate_universe` for the expected table; closed-form affine lower bounds (no scan over `r`) for the unique-`AC` and global-pole inequalities; independent 16/8 classification; post-compilation import of the pinned D23 miner; live reason-disables plus hostile predicate patches (`>` vs `>=`, floor vs trunc, RA2 deletion, cancelled-as-nonzero, `R=0` never-persistent, target-wall removal). Producer `PASS-GATE-T-STRICT-UAC-PRESBURGER-CHAMBERS-V48-PRODUCER`, frozen `COMPILED.json` equality, V47/V47R1's reviewed 12+4 list, and miner 24/24 agreement were assertions to attack, not evidence |
| Repo | `/Users/dc/code/math/jc2` |
| Git HEAD | `418e413593120d19e15e6546eb50c985f4b1f038` (charged files are untracked relative to that commit; custody is the assignment SHA-256) |
| Date | 2026-08-27 |

Independently recomputed SHA-256 of the five charged files match the review assignment. Every `source_pins` entry rehashes. Both V47 and V47R1 freeze inner walks match. A second engine, using a different atom-count bound (max degree from cheapest-atom cost, not `(cap-delay)//atom_fixed`) and sequential `C(alpha,n)`, reproduced the 425+40 table, the three advertised cancellations, the 424+417 inequalities, the compressed Presburger formula, all sixteen rays, all eight negatives, and all sixteen V47 inventory hashes. The producer was then replayed with `PYTHONDONTWRITEBYTECODE=1 python3 -B` and no `--write-once`; the live object equals frozen `COMPILED.json` (exit 0). No file other than this review was written. No frozen producer artifact, canonical ledger, AWS job, heavy CAS, web sweep, or `jc2-lean` tree was entered or modified. No `__pycache__` or `.pyc` remains under the V48 or miner trees.

This review does **not** establish a strict-fan cover, does **not** claim exhaustiveness outside the registered V47 box, does **not** create a new endpoint or a stronger endpoint theorem, and does **not** conclude `rho=0`, equality faces, positive-order loads, staged Rees charts, terminal receivers, Gate T, order two, maximum twelve, G2-PSC, G2-BD, JC2, or a counterexample.

---

## Verdicts

| # | Mandated attack | Verdict |
|---|---|---|
| 1 | Custody | **CONFIRMED** |
| 2 | Primitive recomputation / cancellations | **CONFIRMED** |
| 3 | Grade-cap completeness | **CONFIRMED** |
| 4 | Inequality completeness | **CONFIRMED** |
| 5 | Presburger solving | **CONFIRMED** |
| 6 | Exact 16/8 classification | **CONFIRMED** |
| 7 | Miner independence | **CONFIRMED** |
| 8 | All eight mutations | **CONFIRMED** |
| 9 | Implementation / replay | **CONFIRMED** |
| 10 | No-cover scope | **CONFIRMED** |

**Smallest failing identity:** none of the three advertised cancellations, none of the 424 unique-`AC` or 417 global-pole inequalities, none of the sixteen inventory hashes, none of the eight negative witnesses.

**Smallest failing hypothesis:** none. The 16-ray list is the solution of the affine unique-`AC` / global-pole inequalities on the registered box, not a restatement of V47's curated contact list.

**Smallest repair:** none.

**Exact V48 scope after this review: eligible to be integrated as a bounded Presburger chamber compiler of the already reviewed V47/V47R1 predicate on the registered sixteen rays plus eight frozen negative controls.** V47/V47R1 remain the mathematical endpoint authority (localizations and theorem types are inherited, not restated). Integration is of a machine-checked semilinear restatement of that contact list. It is not a new endpoint, not a unit-ideal claim, and not a license to drop upstream chart factors.

---

## 0. Custody of the five charged artifacts

Recomputed SHA-256, all matching the assignment:

```text
29d206524de1eb2faadaa3d25a261376c7be380f5b669ed9cf5fa436b263cafe
  cases/max12_812_order2_gate_t_strict_uac_presburger_chamber_compiler_v48_20260827/SCHEMA.json
1a7dfe1c852e5ac655ed12e4efc80a9a1c8fd3ca2aa0ae0b98f3c17ebd04b575
  cases/max12_812_order2_gate_t_strict_uac_presburger_chamber_compiler_v48_20260827/compile_presburger_chambers.py
43ff29257e917b4ae5e264866a5159861a5439477b31d74e1a9c0afa6baf285a
  cases/max12_812_order2_gate_t_strict_uac_presburger_chamber_compiler_v48_20260827/COMPILED.json
2d1c954573cefe8b5f42e57cac5eed1b05161298c5668e40c6abaa9e9ca16ca1
  cases/max12_812_order2_gate_t_strict_uac_presburger_chamber_compiler_v48_20260827/RESULT.md
e6a765327de4a22dc8b54d3fedb1b518f638997257856b85905a0b1ef67ce646
  cases/max12_812_order2_gate_t_strict_uac_presburger_chamber_compiler_v48_20260827/FREEZE.sha256
```

`FREEZE.sha256` contains exactly those four producer files (schema, compiler, `COMPILED.json`, `RESULT.md`) and matches each recomputed digest. `RESULT.md` lists the first three of those four and does not hash itself or the freeze; that is the correct freeze split.

---

## 1. Custody — CONFIRMED

### 1.1 Every claimed pin rehashes; inner freezes walk

All ten `SCHEMA.json` `source_pins` match the files on disk. V47 freeze inner lines, independently rehashed:

```text
b1770cc0…703ab2  SCHEMA.json
070d5a96…c7ecaa  GENERATED_MANIFESTS.json
903c6932…c0111   verify_three_row_linker.py
a8be0c67…4c367b  run_v0_v9/result.json
e53dd549…449871  RESULT.md
```

V47R1 freeze inner lines, independently rehashed:

```text
b895315a…aa908  SCHEMA_R1.json
70085a3e…77d7   verify_three_row_linker_r1.py
6c162247…c8270  run_v0_v10_r1/result.json
43547b06…0a19b  RESULT.md
```

V47 and V47R1 producer bytes are unchanged relative to those freezes. V48 is a new untracked directory. It does not rewrite V47/V47R1. Additive boundary holds.

### 1.2 Consumed versus hash-only

Content actually consumed, not merely hashed:

| Pin | Use |
|---|---|
| V46 `SCHEMA.json` | polar-template uniqueness in the independently generated table |
| V47 `SCHEMA.json` | 12+4 comparison *after* independent compilation; negative-control coordinates |
| V47 `run_v0_v9/result.json` | negative reason-list comparison; manifest ID set |
| V47 freeze | inner walk |
| V47R1 `run_v0_v10_r1/result.json` | 16 IDs; attaches coefficient digest `ad0c9b15…12a850` |
| V47R1 freeze | inner walk |
| D23 `mine_support.py` | 24-cell comparison imported *after* compilation |

Hash-only (immutability/custody, not a live input to the chamber map): V47 Grok review, V47R1 `SCHEMA_R1.json`, V47R1 Grok review. Same weak-but-not-gap freeze split already recorded on V47R1. The compiler never reads those three files' contents.

### 1.3 Generated output is not an input to its own proof

`generate_universe` reads only `SCHEMA.json` atoms/summands/cap. `compile_result` builds the 16 rays from the affine table and the registered box *before* opening V47 manifests. `COMPILED.json` is compared at the end of `main` as a freeze check; it is not fed back into the enumerator. The D23 miner is loaded only after the independent list exists, and no region is accepted from it.

**Custody: CONFIRMED.**

---

## 2. Independent primitive recomputation / cancellations — CONFIRMED

Atoms, from `SCHEMA.json` only:

```text
id  fixed  L-denom  (R,A,C)  scalar
R1    2        2    (1,0,0)     2
R2    4        4    (2,0,0)     1
A     5        3    (0,1,0)     1
C     5        4    (0,0,1)     1
```

Summands:

```text
unloaded  alpha=3/2  delay=0
k10       alpha=5/4  delay=4
k6        alpha=3/4  delay=12
k2        alpha=1/4  delay=20
```

A route with counts `(n1,n2,nA,nC)` of degree `n=sum ni` contributes

```text
C(alpha,n) * n!/(n1! n2! nA! nC!) * 2^{n1}
```

at

```text
fixed = delay + 2 n1 + 4 n2 + 5 nA + 5 nC
denom = 2 n1 + 4 n2 + 3 nA + 4 nC = 2R + 3A + 4C
pole  = denom - 4 alpha
R     = n1 + 2 n2,   A = nA,   C = nC.
```

`4*alpha` is an integer for every charged summand (`6,5,3,1`); the producer's `int(4*alpha)` is exact on this freeze, not a truncation. Aggregation is over `(summand, load, fixed, R, A, C, pole)`. Because `fixed = delay + 2R + 5(A+C)` and `pole = 2R+3A+4C-4 alpha`, the key is essentially `(summand, R, A, C)`: distinct `(n1,n2)` with the same `R` land on one key and can cancel.

Independent enumerator: nested counts with max degree `cap//2 + pad + 2` (cheapest atom cost 2), then drop `fixed>34`. Sequential binomial `C(alpha,n) = prod_{j<n} (alpha-j)/(j+1)`, not the producer's "multiply then divide by `n!`". Degree 0 skipped; `pole<=0` skipped (polar filter; see below).

Exact census, matching the freeze:

```text
nonzero aggregated signatures     425
cancelled aggregated signatures    40
universal-table digest            9dfb45d24a6a0f3341f7d0ae14941ecd65fc3902b3c75092dc6883c6e1121863
```

Atom-bound `+1` and `+2` pads, and a cap-40 run cut back to `fixed<=34`, reproduce the identical 425+40 tables.

### 2.1 Three advertised cancellations, hand-expanded

Unloaded `R^4` (`fixed=8, R=4, A=0, C=0, pole=2`):

| counts `(n1,n2,nA,nC)` | coeff |
|---|---|
| `(4,0,0,0)` four R1 | `C(3/2,4)*16 = 3/8` |
| `(2,1,0,0)` two R1 + one R2 | `C(3/2,3)*3*4 = -3/4` |
| `(0,2,0,0)` two R2 | `C(3/2,2) = 3/8` |

Sum `3/8 - 3/4 + 3/8 = 0`. Present in the cancelled table with those three route coefficients.

Unloaded `R^2 C` (`fixed=9, R=2, C=1, pole=2`): `(2,0,0,1) = -3/4`, `(0,1,0,1) = 3/4`, sum `0`.

Unloaded `R^2 A` (`fixed=9, R=2, A=1, pole=1`): `(2,0,1,0) = -3/4`, `(0,1,1,0) = 3/4`, sum `0`.

Treating the cancelled signatures as if they were nonzero is load-bearing: `A2D1` would raise from `r_min=2` to `3` (the `R^4` global-pole bound `8+4r > 16` forces `r>=3`), and `A3D2` would raise from `3` to `4`. Live-patched on the producer `compile_threshold`.

### 2.2 Permitted targets and V46 polar templates

Independently:

```text
AC  unloaded  fixed=10  (R,A,C)=(0,1,1)  pole=1  coeff=3/4
C2  unloaded  fixed=10  (R,A,C)=(0,0,2)  pole=2  coeff=3/8
RA2 unloaded  fixed=12  (R,A,C)=(1,2,0)  pole=2  coeff=-3/8
A3  unloaded  fixed=15  (R,A,C)=(0,3,0)  pole=3  coeff=-1/16
```

`AC` is `C(3/2,2)*2 = 3/8 * 2 = 3/4`. `C2` is `C(3/2,2) = 3/8`. All ten V46 polar templates occur uniquely in this table, including `k10R3` (`5/16`), `k10RC` (`5/8`), `k10R2A` (`-5/32`), `k10A2` (`5/32`), `k6C` (`3/4`), `k2R` (`1/2`). Every `R,A,C` exponent in the nonzero table is nonnegative. Minimum kept pole is 1; minimum fixed delay is 10; maximum fixed delay is 34.

### 2.3 Polar filter is load-bearing and inherited

Eighteen `pole<=0` routes are dropped (regular / non-polar monomials: `R, R^2, R^3, A, A^2, C, RA, RC`, …). Keeping them enlarges the universe from 425 to 440 and puts 13 extra signatures in the `A2D1` `G=15` window, so unique-`AC` fails. This is the V47 polar predicate, not a V48 novelty: uniqueness is among pole-`>=1` aggregated primitives. Restated here because the filter is operative.

**Primitive recomputation / cancellations: CONFIRMED.**

---

## 3. Grade-cap completeness — CONFIRMED

First grade is `fixed + R r + A a + C c`. All of `R,A,C,a,c,r` are nonnegative on the charged domain, so `first_grade >= fixed`. A primitive with `fixed > T_C2` cannot sit in a window of length `T_C2`.

Charged maxima:

| class | max `T_C2` |
|---|---|
| registered positives (`a<=6`, `T=10+2c`, `c<=9`) | 28 |
| target-wall predicate `T_C2<32` | 31 |
| frozen negative `A9D3_K2_TARGET` (`c=12`) | 34 |

Cap 34 equals the largest charged `T_C2`. Pad `+1`/`+2` and a cap-40 run restricted to `fixed<=34` add no signature. For compiling `r_min`, high-fixed primitives are automatically after `G` and `T` at `r=0`; missing them cannot change a threshold. Low-fixed high-`R` primitives, which become visible as `r` grows, have *small* `fixed` and are inside the cap.

Cap 34 is complete for every charged positive and negative comparison.

**Grade-cap completeness: CONFIRMED.**

---

## 4. Inequality completeness — CONFIRMED

From the independent table, not from the producer count `len(universe)-1`:

- Unique-`AC`: every nonzero aggregated primitive except unloaded `AC/L`. **424** inequalities, 424 distinct keys. `AC` is present, so this is exactly `universe \ {AC}`.
- Global-pole: every nonzero pole-`>=2` primitive except unloaded `C^2/L^2`. Pole-`>=2` total 418, minus `C2`, **417** inequalities, 417 distinct keys.

The two permitted targets are exactly those two SCHEMA records. `C2` also appears in the unique-`AC` list (it is not `AC`); that inequality is redundant on the box because `T_C2 - G = c-a = d >= 1`, so `C2` sits strictly after `G` identically.

Attacks:

- **Coefficient cancellation.** Routes with the same `(summand,R,A,C)` share a key; the three advertised zeros are the load-bearing instances. Incomplete aggregation would add extra pole-`>=2` walls (section 2.1).
- **Equal-grade ties.** At every accepted `r_min`, the only primitive with `first_grade = G` is `AC`. `A7D1` is the charged equal-grade failure: `k6C` at grade `17+c = G` when `a=7`.
- **Pole `>=2`.** Generic exclusion of every pole-`>=2` signature except `C2`. `RA2`/`A3` labels are hardcoded cores used only to name the reason; the geometric cut does not depend on those names.
- **`R=0` persistent walls.** `A3` has `R=0`, first grade `15+3a`. Need `15+3a > 10+2c`, i.e. `5+a > 2d`. For `d=3` this is `a>1`. `(a,d)=(1,3)` is a persistent wall, independent of `r`. That is `E_A1D3`.
- **Strict versus weak.** Over integers, `first_grade > T` is `first_grade >= T+1`. Weakening to `>= T` (equal-grade permitted) changes the chamber map: `A1D2` drops from `r>=3` to `r>=2`, `A2D3`/`A3D3`/`A4D3` drop from `r>=5` to `r>=4`, and compile-side `A1D3` ceases to be persistent. Live classify with a `< T` pole window accepts the equal-`T` RA2 cells `A1D2_R2`, `A2D3_R4`, `A3D3_R4`, `A4D3_R4` and the equal-`T` A3 cell `E_A1D3`. It does **not** accept `A2D3_R3`, where RA2 sits at `T-1=19`. The strict cut is load-bearing.
- **Integer rounding.** `r >= floor((rhs-const)/R)+1` is the exact conversion of `const + R r > rhs` for positive `R`. Truncation toward 0 disagrees on 380 primitive-level bounds in the box (negative non-integral quotients: floor gives `0`, trunc gives `1`). None of those raise any of the 16 `r_min` values, because every registered `r0` is already `>=2`. The producer uses floor, which is the correct conversion.
- **Endpoint floors.** `r_min = max(r0, max finite bounds)`. For `d=1`, RA2 only needs `r>=1`, so the registered floor `r0=a` binds.
- **Target walls `G<28`, `T_C2<32`.** No registered `a<=6` cell has `G>=28` or `T>=32` (max `G=25`, max `T=28`). The walls fire on `A9D3` (`G=31`, `T=34`) together with `NONUNIQUE_G` and `OTHER_GLOBAL_POLE`. They are inherited V47 constraints, operative on that control, not a cut of the 16.

**Inequality completeness: CONFIRMED.**

---

## 5. Presburger solving — CONFIRMED

Registered box, independently enumerated from SCHEMA guards (`c=a+d>=3`, `r0=a+s_min`, `r0>=2`, `a+3 s_min > d`):

```text
(a,d,c,r0) =
  (1,2,3,2), (1,3,4,2),
  (2,1,3,2), (2,2,4,3), (2,3,5,3),
  (3,1,4,3), (3,2,5,3), (3,3,6,4),
  (4,1,5,4), (4,2,6,4), (4,3,7,4),
  (5,1,6,5), (5,2,7,5), (5,3,8,5),
  (6,1,7,6), (6,2,8,6), (6,3,9,6).
```

Seventeen cells. No scan over `r`: for each cell, `r_min` is the maximum of `r0` and every finite `required_r`, or rejection if any `R=0` primitive is persistently visible or a target wall fires.

Closed-form RA2 global-pole bound, used as a check on the solver rather than as a substitute:

```text
12 + r + 2a > 10+2c  =>  r > 2d-2  =>  r >= 2d-1.
```

So `d=1` needs `r>=1` (absorbed by `r0=a`), `d=2` needs `r>=3`, `d=3` needs `r>=5`. Combined with the `A3` persistent wall `a>1` on `d=3` and the registered floors, the solver returns exactly

```text
(2<=a<=6 and d=1 and c=a+1 and r>=a)
OR (1<=a<=6 and d=2 and c=a+2 and r>=a and r>=3)
OR (2<=a<=6 and d=3 and c=a+3 and r>=a and r>=5).
```

Compressed-formula SHA256 `1827f42b4517a547e84c70db72c690a19a68432d40d8027d16bc87f75f3ced5c`. Expanded 16-clause SHA256 `e0d55bbc0415dedfc448134b56ce94b3bef64756d8989f8e2e3589d743b4ee73` (clause order is the registered `a`-outer/`d`-inner walk: `A1D2_R3, A2D1, …, A6D3`).

The only rejected cell in the box is `(a,d)=(1,3)`, persistent `A3` at grade `18=T_C2`. It is absent from the compressed formula (`d=3` starts at `a>=2`).

Equivalence inside the registered domain: for every one of the 17 cells and every `r` from `r0` through `r0+20`, the compressed formula, the compiled `r>=r_min`, and live classify agree (zero mismatches). The formula is a three-family compression of the 16-map, not a quantified-elimination of `Z^4`; on this finite declared domain it equals all and only the sixteen expanded rays.

Nonnegative `R` makes each accepted set a closed tail: `first_grade` is nondecreasing in `r`, `G` and `T_C2` do not depend on `r`, and the constraints are “not too early”. Sentinels `r_min+1`, `r_min+17`, and `r_min+100` all remain accepted. `R=0` primitives are either always after the wall or persistent failures; they never become newly visible at large `r`.

**Presburger solving: CONFIRMED.**

---

## 6. Exact 16/8 classification — CONFIRMED

Derived *before* reading V47's 12+4 list. Then compared.

### 6.1 Twelve baselines and four raised subtails

| ID | a | d | c | r0 | r_min | class | n | predecessor |
|---|---|---|---|---|---|---|---|---|
| A2D1 | 2 | 1 | 3 | 2 | 2 | baseline | 4 | — |
| A3D1 | 3 | 1 | 4 | 3 | 3 | baseline | 3 | — |
| A4D1 | 4 | 1 | 5 | 4 | 4 | baseline | 3 | — |
| A5D1 | 5 | 1 | 6 | 5 | 5 | baseline | 3 | — |
| A6D1 | 6 | 1 | 7 | 6 | 6 | baseline | 4 | — |
| A2D2 | 2 | 2 | 4 | 3 | 3 | baseline | 4 | — |
| A3D2 | 3 | 2 | 5 | 3 | 3 | baseline | 5 | — |
| A4D2 | 4 | 2 | 6 | 4 | 4 | baseline | 5 | — |
| A5D2 | 5 | 2 | 7 | 5 | 5 | baseline | 5 | — |
| A6D2 | 6 | 2 | 8 | 6 | 6 | baseline | 5 | — |
| A5D3 | 5 | 3 | 8 | 5 | 5 | baseline | 7 | — |
| A6D3 | 6 | 3 | 9 | 6 | 6 | baseline | 8 | — |
| A1D2_R3 | 1 | 2 | 3 | 2 | 3 | raised | 3 | RA2 at r=2, grade 16=T |
| A2D3_R5 | 2 | 3 | 5 | 3 | 5 | raised | 3 | RA2 at r=4, grade 20=T |
| A3D3_R5 | 3 | 3 | 6 | 4 | 5 | raised | 4 | RA2 at r=4, grade 22=T |
| A4D3_R5 | 4 | 3 | 7 | 4 | 5 | raised | 5 | RA2 at r=4, grade 24=T |

Primitive counts sum to **71**. Every accepted `r_min` classifies as unique-`AC` / unique global-pole; every raised predecessor fails. Binding active facets at `r_min`: RA2 for the four raised rays; endpoint-floor `UNIQUE_G` (typically `k10R3` and/or `k10RC`) and occasionally `k10` pole-`>=2` companions for the twelve baselines. All sixteen independent inventory SHA-256 values equal the V47 schema byte-for-byte. Chamber-record digest `ecd642fe8360d95a38e5c33e564fd21541087ef4624b2c9dd482d2b8617eeb43`. Endpoint IDs/authority/review hashes and the V47R1 coefficient digest `ad0c9b15ce394e554c178521fadb32b3c71318cf3a8d700a2311aceb9d12a850` are attached after derivation; V48 creates none.

### 6.2 Eight negatives, one exact primitive witness each

| ID | (a,d,c,r) | G, T | reasons | exact witness |
|---|---|---|---|---|
| A1D2_R2_RA2 | (1,2,3,2) | 14, 16 | RA2 | unloaded RA2 `-3/8` at grade **16=T** |
| A2D3_R3_RA2 | (2,3,5,3) | 17, 20 | RA2 | unloaded RA2 `-3/8` at grade **19=T-1** |
| A2D3_R4_RA2 | (2,3,5,4) | 17, 20 | RA2 | unloaded RA2 `-3/8` at grade **20=T** |
| A3D3_R4_RA2 | (3,3,6,4) | 19, 22 | RA2 | unloaded RA2 `-3/8` at grade **22=T** |
| A4D3_R4_RA2 | (4,3,7,4) | 21, 24 | RA2 | unloaded RA2 `-3/8` at grade **24=T** |
| E_A1D3_R20_A3 | (1,3,4,20) | 15, 18 | A3 | unloaded A3 `-1/16` at grade **18=T**, `R=0` |
| A7D1_K6_TIE | (7,1,8,7) | 25, 26 | NONUNIQUE_G | `k6C` `3/4` at grade **25=G** |
| A9D3_K2_TARGET | (9,3,12,9) | 31, 34 | NONUNIQUE_G, OTHER_GLOBAL_POLE, G_TARGET_WALL, T_TARGET_WALL | `k2R` `1/2` at **31=G**; `k6C` `3/4` at **29<=G**; `k2A` `1/4` pole 2 at **34=T**; walls `G=31>=28`, `T=34>=32` |

`A2D3_R3` is not an equal-`T` tie; it is one grade deeper. The RA2 bound `r>=5` is what excludes both `r=3` and `r=4`.

**Exact 16/8 classification: CONFIRMED.**

---

## 7. Miner independence — CONFIRMED

The pinned miner `mine_support.py` (`0e94e540…52fe9a`) is imported by `importlib` *after* the 16 rays and 8 negatives are compiled. Its `main()` is AWS-only (`require_aws`); that path is not invoked. `enumerate_primitives` is a different enumerator:

- V48: universal fixed-delay cap 34, independent of `(a,c,r)`, then filter `first_grade <= T`.
- Miner: contact-specific grade budget `T - delay`, atom costs `fixed + r R + a A + c C`, plus `local_pole_upper_at_A_root` (allocation at an `A`-root). V48 strips those local-pole fields before comparison.

Same four atoms and the same binomial formula, different bounds and different semantics. Agreement is on visible polar monomials (signature + coefficient + first grade), not on a local-pole theorem and not on a shared serialized expected table. Independently re-run on all 24 cells, including the miner's own `+1` pad: **24/24** agree, pad-stable. No region is accepted from the miner.

This is a genuine cross-check, not a tautology. It would fail if V48's universal cap missed a contact-visible primitive or if the two binomial implementations diverged.

**Miner independence: CONFIRMED.**

---

## 8. All eight mutations — CONFIRMED

Producer `classify(..., disabled=S)` is an operative predicate, not a string compare of expected output. Evidence: disabling `NONUNIQUE_G` on `A1D2_R2` still rejects with `RA2`; disabling `RA2` accepts. Each of the eight charged disables was live-fired on both the independent classifier and the imported producer `classify`:

| control | disabled set | reject → accept |
|---|---|---|
| A1D2_R2_RA2 | `{RA2}` | yes |
| A2D3_R3_RA2 | `{RA2}` | yes |
| A2D3_R4_RA2 | `{RA2}` | yes |
| A3D3_R4_RA2 | `{RA2}` | yes |
| A4D3_R4_RA2 | `{RA2}` | yes |
| E_A1D3_R20_A3 | `{A3}` | yes |
| A7D1_K6_TIE | `{NONUNIQUE_G}` | yes |
| A9D3_K2_TARGET | all four reasons | yes |

Partial disable of any one of A9D3's four reasons leaves the other three. The mutation is a forbidden relaxation of the recorded reason set, not a new mathematical permission.

Hostile boundary mutations, live-patched on producer predicates (no file edits):

| patch | effect |
|---|---|
| `>` to `>=` in `required_bound` | `A1D2` 3→2, `A2D3`/`A3D3`/`A4D3` 5→4, `A1D3` becomes a compiled ray at `r=4` |
| pole window `<= T` to `< T` | accepts equal-`T` RA2/A3 negatives; does **not** accept `A2D3_R3` |
| trunc-toward-0 instead of floor | 380 primitive-level diffs; **no** change to any of the 16 `r_min` (`r0>=2`) |
| delete RA2 from the universe | `A1D2` 3→2, `A2D3` 5→3, `A3D3` 5→4, `A4D3` 5→4 (all fall to `r0`) |
| delete non-binding `k2R` | no `r_min` change in the box |
| delete redundant `C2` unique-`G` | `A2D1` stays 2 |
| cancelled zeros treated as coeff `1` | `A2D1` 2→3, `A3D2` 3→4 |
| `R=0` never persistent | `A1D3` compiled at `r=5` (classify still rejects A3: two-layer defence) |
| drop target walls only | A9D3 still rejected (`NONUNIQUE_G`, `OTHER_GLOBAL_POLE`); walls are operative on the reason list, not the 16 |
| include `pole<=0` | `A2D1` unique-`AC` fails (13 extra in-window regulars) |

A string-only or expected-output-only mutation was not used.

**All eight mutations: CONFIRMED.**

---

## 9. Implementation / replay — CONFIRMED

Replay, no output write:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -B \
  cases/max12_812_order2_gate_t_strict_uac_presburger_chamber_compiler_v48_20260827/compile_presburger_chambers.py
```

Exit 0. Live dict equals `json.loads(COMPILED.json)`. Status `PASS-GATE-T-STRICT-UAC-PRESBURGER-CHAMBERS-V48-PRODUCER`. `fan_cover_status=NOT_CLAIMED`. Standard library only. No `__pycache__` / `.pyc` left under V48 or the miner.

Audit:

- **Exact rationals.** `fractions.Fraction` throughout. Binomial over `Q`. `4*alpha` integral on this freeze.
- **Aliasing.** `with_grade` copies before stamping `first_grade`; universe items are not mutated. `defaultdict(Fraction)` allocates a fresh zero per missing key. Facets store references to universe dicts; JSON dump is a snapshot. A dict copy of a universe item can be mutated without changing the table.
- **Enumeration caps.** Producer bounds `(cap-delay)//atom_fixed`; independent max-degree pad agrees. Both desk-scale.
- **Canonical serialization.** `json.dumps(..., sort_keys=True, separators=(',',':'))`. Independent content digest of the universal table equals the producer digest, including cancelled-route order (both iterate `n1` slowest, atoms in SCHEMA order).
- **Monkeypatch leakage.** Miner loaded under a distinct module name; no patch of producer internals. Producer does not patch the miner.
- **Hard-coded IDs / counts.** Census `425/40` is a checksum of an independently reproduced table. `RA2`/`A3` cores are labels on a generic pole-`>=2` cut. Compressed formula is a 16-map compression, then checked against derived `(a,d)->r_min`. Chamber IDs are generated `A{a}D{d}(_R{r_min})`, not a hardcoded 16-list. `C2_PIN` never fires on this freeze (`C2` first grade is identically `T_C2`).
- **Frozen equality cannot mask an independently wrong compiler.** Replay-vs-`COMPILED.json` is consistency of this producer with its freeze. Correctness is the second enumerator, which never read `COMPILED.json` and still matched the table digest, both formula digests, the 16 inventory hashes, and the 8 reason sets. An empty or uncancelled table does not reproduce those hashes.

**Implementation / replay: CONFIRMED.**

---

## 10. No-cover scope — CONFIRMED

SCHEMA `does_not_claim`, RESULT §7, and `COMPILED.json` `fan_cover_status=NOT_CLAIMED` / `scope` agree:

V48 is a machine-checked Presburger restatement of the already reviewed V47/V47R1 strict unique-`AC` global-pole predicate on the explicitly registered sixteen rays plus eight frozen negative controls. It attaches reviewed V46 V0/V1/V3/V4 source naturality and the reviewed V47R1 literal 569-tail coefficient digest by pin. Endpoint localizations and theorem types remain inherited.

It does not claim:

- strict-fan coverage, or that the registered box is the only box with strict chambers;
- exhaustiveness outside `a=1..6`, `d=1..3`, `c=a+d>=3` with the registered `r0`;
- a new endpoint or a stronger endpoint theorem type;
- `rho=0`, equality faces, positive-order loads, staged Rees charts, or a terminal receiver;
- Gate T, order two, maximum twelve, G2-PSC, G2-BD, JC2, or a counterexample.

**No-cover scope: CONFIRMED.**

---

## Integration decision

V48 **may be integrated as a bounded chamber compiler** of the V47/V47R1 strict unique-`AC` predicate on exactly the sixteen closed rays

```text
A2D1, A3D1, A4D1, A5D1, A6D1,
A2D2, A3D2, A4D2, A5D2, A6D2,
A5D3, A6D3,
A1D2_R3, A2D3_R5, A3D3_R5, A4D3_R5
```

together with the eight frozen negative controls above.

V47/V47R1 remain the mathematical endpoint authority. V48 does not replace their localizations, theorem types, or the V47R1 literal three-row contraction. It supplies an auditable affine derivation of the same contact list, compressed to three Presburger families, inside the already registered box.
