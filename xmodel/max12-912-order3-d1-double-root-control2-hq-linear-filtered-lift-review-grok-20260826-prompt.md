# Hostile review charge: D1 control-2 24-column hQ lift V8

Act as a hostile commutative-algebra/tropical-weight/source-custody referee.
This is a text-only review: do not run local Singular, Sage, msolve, gfan,
Lean, algebra compilers, or substantive Python.  You may read all frozen
sources and certificates, recompute hashes, compare files, inspect JSON with
lightweight tools, and check displayed rational/linear/weight identities.

Read the complete package

```text
cases/max12_912_order3_d1_double_root_control2_hq_linear_filtered_lift_v8_20260826/
```

and the full pinned source chain named by `SOURCE_CLOSURE.sha256`.  Also read
the prerequisite V4--V7 producer results and hostile reviews needed to audit
ancestry.  Pin these load-bearing endpoints:

```text
RESULT  7d3973e84c37d158a1f88796fdd5747e32d13e7fabd071ece80f6ea5a4c7942d
FREEZE  85a6ddb3b72fce2b2cce6399ba69315bd869ad9eae76461de6963a446ed648ff
V7 source a4500936c83c82ef12d68c145ef6672688d401292c05dab36b95002df4138de3
V7 review 32b63dec622ce24e777f872e898d2004da10f18c2aab49ee695f11d1f7177494
```

Charge every point below.

1. Recompute all freeze/source/result hashes and audit pre-GO registration,
   AWS identity, caps, rc, stderr, PASS uniqueness, source equality, and
   forward/reverse independence.  Verify that deleting only `tag` and
   `order` gives canonical certificate SHA `7bae4055...`.
2. Audit literal reconstruction of the reviewed V7 witness, including the
   q2, scalar-h, and scalar-h-squared corrections, the h-zero witness, fixed
   target loads, and exact row/source hashes.  Check that the compiler cannot
   substitute a hand-refactored row system.
3. Verify the exact 24 columns are all `h*q_j*E_i(0)` for eight rows and
   `q_j={q2,q1,q0}`.  Audit that the projected union is exactly the complete
   Q-R, `R^2/Q^3`, and `Q^2R` h-linear grades; that earlier zero grades are
   included as zero targets; and that the input Q-squared-R piece has eight
   terms and SHA `6c42c451...`.
4. Audit exact RREF rank 9, pivots, label order, signs, and the free-zero
   solution.  Verify the only nonzero additions are
   `F1-=h*q1/36` and `F2-=h*q0/72`, and that the projected residual is
   literally zero.
5. Audit that the emitted 48-term polynomial SHA `e549fd68...` is the exact
   full product `sum F_i E_i(h)`, not a truncation or congruence.  Check lower
   h custody, every recorded coefficient/exponent, support mask, absence of
   hidden moving `a,p,c,k,mu,nu,x,y`, and exact target coefficient/sign.
6. Independently charge all 48 weight forms.  With
   `alpha=15/2`, `beta=5+u`, `delta=5+v`, verify there is no negative
   eta-zero corner margin; all 30 non-target equality-face terms have a
   positive `u` or `v` slope; `la^20*tau` is strict for `T>0`; and therefore
   `la^20` is uniformly unique least for every `u,v,T>0` iff `eta>=0` as a
   bound uniform over the open quadrant.  Pay special attention to the one
   h-squared term and to the meaning of negative eta in the sharpness clause.
7. Distinguish the full-polynomial weight theorem from completeness of the
   24-column module search.  The former may be exact even though the latter
   is only the stated projection.  Do not promote moving axis/cusp, moving
   loads, omitted normal directions, a formal neighborhood, the whole
   double-root fan, D1, or JC2.

Produce exactly one review file at

```text
xmodel/max12-912-order3-d1-double-root-control2-hq-linear-filtered-lift-review-grok-20260826.md
```

Include: overall verdict, smallest failing identity if any, smallest missing
hypothesis for any stronger theorem, evidence tier, exact promotion text if
confirmed, and an explicit firewall.  End with exactly one token:
`HQ_LINEAR_FILTERED_LIFT_CONFIRMED`, `REPAIR_REQUIRED`, or `INCONCLUSIVE`.
