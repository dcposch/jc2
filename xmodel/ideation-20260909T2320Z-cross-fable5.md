# 2320 adversarial CROSS — Fable 5.1 seat

Status: CROSS / COMPLETE. Invitation received 23:41:48 UTC (late; no reset); fixed stop 23:51:05, final-2-minute reserve from 23:49:05; independent ROOTTERM 23:50:40. All 13 charged objects SHA256-verified in invitation order BEFORE any read (13/13 MATCH; generated list at box/ideation-20260909T2320Z-cross-fable5/input-hashes.sha256, produced by sha256sum, not typed). Two long objects (master46-history+binding-bd-gal+bgv-primary-EXCERPT, approaches.md) overflowed the display and were recovered WHOLE from the persisted tool output before any judgment below. No live peer-cross, no provenance links, no CAS/Python/network/Git/proc/agents/AWS; text and hash metadata only. Strategy judgment, not a promotion, theorem, or code/math run. No Seal, no charge_basis authored.

## 0. Custody and reuse

I wrote the 2320 Fable blind and the FIRST gate of 17m (both charged here); I re-read them as charged text and correct my own blind below rather than defend it. Private memory on this host was not charged and nothing rests on it. Root and Astra blinds were read only as charged copies.

## 1. Post-freeze delta and lifecycle correction (own errors first)

- 17zd was ALREADY accepted at the original freeze. My blind's opening label "UNREVIEWED/PROVISIONAL" for the Astra BGV discriminator was a lifecycle error; withdrawn. Nothing in my blind depended on that label.
- 17ze (same-residue paired-contact exclusion) is now accepted at its exact scope. The residual contact set is therefore the NINE congruence pairs r4:j1,2,3; r5:j2,4,6; r6:j2,4,5 (mod 7), unconditionally within the accepted chain, not "nine or twelve".
- 17zf is STOP-NONDECISION: SIGXCPU at 550 CPU s, sampled peak about 1.45 GB, BELOW the 2 GiB cap, no phase marker, no candidate, no checker. My blind's Card A sentences "rank profile WITHOUT solving" and "failing at 2 GiB would indicate coefficient height" are withdrawn: the cap was CPU not RSS, a rank profile IS an elimination, and no matrix completion was observed. The only measured fact is: no candidate from the full 217x1638 design within 550 CPU s. Coefficient height remains UNMEASURED, not diagnosed.

## 2. Ranked combined strategy

1. Task A (proof side, manual): the terminal AFFINE forcing composition (root Card 2; Astra section 4 last paragraph). Runs first.
2. Task B (disproof side, static design only): certificate-preserving compression of the 17m decision (root sketch, tightened in section 8). No compute.
3. Deferred: second-prime contact arithmetic (my Card B), component-safe gcd/subresultant route (Astra Card 1 fallback), any registered run.
4. Stopped/withdrawn: matrix-statistics run (my Card A), BD-GAL x BGV block composition (my section 4), receipt-stamp upgrade (my section 8), automatic rerun/cap increase/third Q12.

Systems: NO_CHANGE (section 7). Disagreements are resolved by reasoning in sections 3-7; where I disagreed with root or Astra in my blind, the reasoning goes against my blind.

## 3. Q1: compression vs component-safe gcd vs matrix statistics

A measured internal failure phase is NOT required before a 20-minute STATIC design of a structure-preserving alternative. Reason: the design is a proof-level object whose correctness is decided against 17m's already accepted contract (exponent 5, multiplier window 25, full 217-coordinate checker), not against the cause of 17zf's death. A measured phase IS required before (i) any speedup claim, (ii) choosing runtime caps for a registered run, and (iii) asserting that the alternative "addresses" the observed failure. 17zf supplies exactly one usable fact, "no candidate at the full design within 550 CPU s", which licenses preferring a smaller exact system over a same-design rerun but forecasts nothing.

Ranking of the three: (a) compression sketch first, because it emits multipliers in the unchanged full-checker format and its correctness is 17m section 3 made algorithmic; (b) component-safe gcd/subresultant second, because it is the multi-step Euclidean version of the same idea but needs a NEW recombination/certificate contract (gcd witnesses are not degree-25 multipliers) and thus a new binding review; (c) matrix statistics LAST and not selected: it costs a registered run, decides nothing, and its "rank profile" is itself an elimination. Astra's "redesign only after a named bottleneck" and root's "static design now" are reconciled as: design now, run only after a separately justified discriminator.

## 4. Q2: affine forcing obstruction vs further contact arithmetic

The ONE bounded discriminating task is Task A: compose the accepted band interfaces 17q/17s/17o/17v, normalize the last stage as an affine linear map in the remaining late-band parameters, and NAME the first terminal forcing coefficient with its full dependence on earlier free parameters. It must run BEFORE any second-prime table. Reasons: (i) even total elimination of the nine residual contact classes only makes the homogeneous contact column unimodular, and a unimodular column solves its own affine equation; (ii) the middle band is H7*Y+J=0, which on H7 invertible reconstructs Y and on H7=0 retains J=0, neither a contradiction; (iii) my p=5 proposal needs redoing 17za's integrality with the literal A_z coefficients (the 1/120 term breaks the 7-unit denominator step), so it is not cheap, and its best outcome is fewer congruence classes, not a final contradiction. Only after Task A says whether contact-exception emptiness is CONSUMED by the forcing row does contact arithmetic have a client.

## 5. Q3: BGV proper intermediate field, cheap scope objection CONFIRMED

If pi lies in a proper intermediate field K with C(f,g) subset K subsetneq C(x,y), then C(f,g)(pi) subset K, so the graph morphism pi x e has generic degree [C(x,y):C(f,g)(pi)] >= d1 >= 2 onto its image and is NOT birational. Weak type <= 1 (Definition 22.2) fails at the birationality hypothesis BEFORE any singular-divisor saturation or collision analysis. Consequently my blind's section-4 composition ("choose pi inside B_K so that j factors through g1") is vacuous: such pi are excluded by hypothesis, not by 17zd collisions. Label: SCOPE-CONFLICT, withdrawn; no divisor task is built. What survives: a positive BGV attachment needs a genuinely primitive pi (which exists generically) plus saturation of EVERY singular-pullback divisor, and 17zd only restricts base-affine repairs of a failed pi.

## 6. Q4: existential counterexample and the degree <= 35 ambiguity

Agreed: a verified NONZERO certificate (lambda^t M=0 on all 1638 columns, lambda^t b=1, over Q) plus verified faithful maps (17l cover, 16q contract) supplies an EXISTENTIAL algebraic counterexample; an explicit rational or printed point is not a logical prerequisite. Exact reading of "degree <= 35": the point is L-valued for a FIELD L=K[T]/(h) where K is ONE field component of B ([K:Q]<=7) and h is an irreducible factor of the component gcd ([L:K]<=5); it is not a point "over B" (a B-algebra map to a field kills every other component) and not a Q-rational point. Through the monic cubic cover of 17l the full-contract point lives over a field of degree <= 105 over Q; 16q then gives ordinary polynomials of actual degrees 112/196 with nonzero constant Jacobian over that number field, hence over C. Mandatory before any counterexample language: same-object binding of bytes, complete-input acceptance, exact separator, and the faithful maps. No NONZERO certificate exists at this freeze.

## 7. Q5 and the SEPARATE systems decision: NO_CHANGE

My blind's receipt-stamp upgrade is withdrawn. The recorded misses (22:51 to 22:58; 22:22:39 at 77 s) were consumer-side misses, not absence of launcher information; the 23:13:50 launch confirmed the actual model child at 23:13:52 in the same orchestration call with no code change. A stamp written into the receipt path adds actionable detection only if someone reads it in-window, and root must NOT consume live receipts, so its consumer would be a separately scoped operational channel, which is a new framework with no measured remaining failure mode. No time benefit is derived from packet size or the late invitation; real misses and fixed deadlines are retained as recorded.

## 8. Coordinator compression sketch attacked (and tightened)

Correct as stated: on a field component with selected f0 of maximal degree d>=1, q^5 in I iff q^5 is congruent mod f0 to a combination of the other generators with multipliers of degree < d (reduce any expression mod f0); rows rank_Q(B_a)*d_a <= 35 and columns 8*d_a*rank_Q(B_a) <= 280 in total over components since the ranks sum to 7; h0 by exact division has degree <= 25-d by 17m section 3; padding to degree 25 feeds the UNCHANGED checker. Separator pullback lambda=mu*rho: rho (reduction mod f0 then coordinates) is B-linear hence Q-linear; it kills every column i=0 (f0 is 0 mod f0) and maps every column i!=0, j>=d into the span of the small columns (T^j mod f0 is a B-combination of T^j', j'<d), so mu*rho annihilates ALL 1638 full columns and sends q^5 to mu(b_small)=1. This is a two-line proof, not a gap.

Attacks and the missing maps: (i) division with remainder by f0 over the full B needs lc(f0) a UNIT of B; the sketch's "split only if necessary" is made precise WITHOUT factoring p: compute gcd(lc(f0)(v), p(v)) in Q[v]; if 1, no split; if a proper factor g, p squarefree gives the exact CRT split Q[v]/g x Q[v]/(p/g), with explicit idempotents from the Bezout identity, both parts retained. (ii) The all-f_i-zero-on-a-component case is detected by gcd(p, all coefficients of all f_i); the separating coefficient functional must be composed with that component's idempotent projection or it will not annihilate other components' columns. (iii) d_a varies by component; the 25 window is uniform only after padding. (iv) Reducedness of B (17a seven-orbit premise via 17b D) is load-bearing exactly as in 17m; over a nonreduced component the small system can "separate" a zero quotient (my gate's 1+eT^5 control). (v) No speed, height, or native-behavior claim follows; only the algebraic dimension drop is a fact. Simplest certificate-preserving option: this sketch with the two gcd triggers; the full Euclidean gcd route is the fallback, not the first design.

## 9. Two immediate mathematical tasks (manual, at most 20 minutes each, no compute)

Task A — affine forcing endpoint. Object: complete all-r residual pair after the exact triangular band maps over the actual leading finite algebra with all guards. Endpoint: the named first terminal forcing coefficient with full dependence on earlier free parameters, and EITHER one explicit functional on the true cokernel with unit value on the actual forcing, OR the verdict NO_CLOSING_OBSTRUCTION naming the unconsumed nonlinear row. Stop: 20 minutes; or when the next obstruction needs unbounded new support without a recurrence; or when the result is only a renamed contact/determinant condition. Label: KNOWN goal (1110), NEW integration target; no theorem claimed.

Task B — compression design with proof obligations. Object: the nine f_i and q of the accepted 17x artifact over B=Q[v]/p, rank 7. Endpoint: a written algorithm whose ONLY outputs are full-checker certificates (degree-25 padded multipliers, or a 217-coordinate separator lambda=mu*rho), with the gcd(lc(f0),p) and gcd(p, all coefficients) split triggers, exact projection/recombination, and all-zero/d=0 branches. Stop: 20 minutes, or if any step needs a general new framework (full factorization custody, a new certificate contract). Label: KNOWN algebra (17m section 3), NEW engineering; no run authorized.

## 10. Rejected alternatives and labels

- Matrix-statistics run (my Card A): REJECTED; KNOWN unmeasured quantity, wrong diagnoses withdrawn.
- Second-prime (p=5) contact table (my Card B): DEFERRED behind Task A; KNOWN mechanism, NEW instance, no final contradiction possible.
- BD-GAL x BGV block composition (my section 4): WITHDRAWN, SCOPE-CONFLICT (birationality fails first).
- Component-safe gcd/subresultant (Astra Card 1 fallback): KNOWN proposal, held as fallback to Task B.
- Automatic rerun, cap/RAM/prime increase, third Q12, extra engine, generic farm, norm/primitive BGV shortcuts: STOPPED, as in all three blinds.
- Root's compression sketch: NEW cross-stage engineering from KNOWN accepted algebra; not a duplicate of any executed work.

## OPEN(S) RAISED

None new. The two tasks name their quantities and stops; no canonical OPEN ID, compute, AWS, remote, review, agent, or shared change is authorized.

## COLLISIONS

status: EMPTY — own-only target/report check (both targets absent at 23:42:55 before creation); no corpus scan.

Completion custody: own WHOLE read of this body at 23:45:52 UTC (sections 0-10, OPEN(S) RAISED none, COLLISIONS EMPTY); one apply_patch delete attempt failed on a missing header and changed nothing; all writes were apply_patch into this file and the owned box only. All writers IDLE at the marker below; no follow-on.

<!-- BODY-END -->
