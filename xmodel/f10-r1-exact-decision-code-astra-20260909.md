# F10 r1 exact decision code — preparation only

Basis: 0d39df3c9fd69c939a8420c54d03228b9077777d. Start 2026-09-09 13:51:18 UTC; controlling cap 14:11:18 UTC. Status: INTERNAL-UNREVIEWED CODE, DISABLED, NO EXECUTION. This is not a decision, certificate, compute registration, or launch authorization.

## Frozen scope and output

The sole mathematical input is the accepted exact.json artifact SHA256 168bdfd3a791f24a6631de66ae4e71c3f209ee9eb0d21fc466f9aa802f5ad576. All twelve variables remain, in order u,ell,d0,d1,v0,v1,v2,k1,k2,k3,k4,omega, over Q with global dp. All twenty named slots remain, including zero rows and the guard: E1/S0..S8, E0/S0..S9, GUARD/omega*a*b-1. No eleven-variable transform, localization, specialization, maximum-h restriction, new source, second prime, second engine or second order is introduced. The accepted representation endpoint is retained at 16q/r/u/w scope; its interiors were not re-proved. The accepted runtime gate was read whole after current hashing. The root-qualified eight-versus-nine operation-count prose discrepancy does not change its representation-only result.

New files are under box/f10-r1-exact-decision-code-20260909. engine.py emits the fixed exact rows and one Singular script, runs one std(I), and only on the unit candidate branch requests the same-engine target-one lift. certificate.py independently parses and checks rows and either certificate. caller.py adapts the reviewed exact-argv caller to this narrowly scoped batch. semantic_tests.py contains finite designed changed-object tests. execution_gate.py and probe.py are byte-identical copies of the reviewed originals. caller-v2-to-decision.diff is the literal source delta. CAPRUN is unchanged and not copied. READ-SCOPE.md and PENDING.md specify the actual reads and outstanding checks.

## Exact engine wire and certificate meaning

The Singular script uses a Q ring in the fixed variable order and dp, explicitly disables probabilistic and stdhilb options, and exports terms using exponent vectors plus numerator/denominator strings. It does not expose textual polynomial evaluation to the verifier. Headers bind the artifact, field, order and variables. Every ROW/0..19 block is emitted, including empty blocks. The parser requires their exact order and exact rational equality with the original serialized rows, and rejects unknown/trailing output, warnings, malformed or noncanonical rationals, floats, duplicate terms and truncated framing. The future engine invocation and its output receipt are separately hash-bound. No actual Singular input, basis or certificate was generated during this preparation.

The unit branch exports U and twenty source-row cofactors T. Verification requires U to be a nonzero rational constant and checks sum(I_i*T_i)=U exactly. A zero residual alone, U=0 or a variable multiplier is insufficient. Dividing the checked identity by that rational proves one belongs to the original ideal.

The proper branch exports nonzero monic candidate basis elements. The independent verifier checks every S-pair reduces to zero in global dp, no leading monomial is one, and every one of the twenty original rows reduces to zero. Thus the original ideal is contained in a certified proper ideal; reverse containment and a full transformation matrix are unnecessary for this properness conclusion. This does not claim equality, a dimension or a rational point. Unsupported output or resource exhaustion remains INCONCLUSIVE.

The sparse exact checker is independent of the emitter and old builder. Its rational arithmetic is Fraction-based, with a separately implemented dp comparator and deterministic polynomial division. Reusing the accepted artifact hash does not replace the future row-by-row read-back. The future certificate receipt additionally binds engine identity, argv, script/output hashes and registered paths.

## Disabled execution contract

REGISTRATION.disabled.json intentionally has enabled=false and unset root-owned instance, deadlines, installed engine path/version/hash and other authority fields. No installed binary or API compatibility is invented. The old enabled caller and its registrations are unchanged. The new root registration must pin the caller externally, avoiding a recursive self-hash, and must pin the artifact, admissibility, all helpers, interpreter and actual engine. Physical AWS/DMI/hostname checks refuse math-hq; fresh exclusive paths and exact full CAPRUN argv are mandatory. The engine is a nested child of the recorded Python child without a new process group. Its raw argv, PID/start/namespace and common PGID are recorded and checked.

There are two fixed modes, not two representations. A separately authorized validate-only job runs the same caller's five actual authority refusals, valid sentinel and descendant-RSS/TERM-ignoring/KILL control, then the finite semantic controls; it does not run Singular. A later separately authorized decide job requires a pinned successful validation receipt matching the current certificate and test hashes and repeats fresh caller controls. Only then may it run the one engine and verifier. Neither job is authorized here.

Proposed engine caps are 60 seconds wall/50 CPU/2 GiB sampled RSS/16 MiB per file; verifier and semantic controls use 30/25/2 GiB/16 MiB. One child is active at a time. The existing CAPRUN remains the sole supervisor. The caller retains exact typed cleanup-event validation, start/PGID matching, integer signals, a conservative +15-second admission margin and explicit post-return cutoff check, together with a 120-second aggregate mathematical ceiling. Root supplies new absolute clocks; this does not reset the expired 13:35 authority. Sampled RSS and cleanup are not a strict real-time guarantee. Any mismatch, cap or late return stops without patching, retrying, increasing limits or changing order.

## Designed tests and limits

The test source includes actual artifact row/guard removal and variable-order mutations; independently parsed positive protocol controls; malformed/truncated/trailing protocol controls; a genuine integer above 2^53 with float and rounded canonical-string attacks; bad cofactors; zero and variable U examples whose weak lift residual really is zero; a failed S-pair and failed input-containment example. These are CODE/DESIGNED ONLY. No test, parser predicate, arithmetic script, compile, import, engine probe or mathematical subprocess was executed. No measured rows, terms, degrees, timing, RSS or certificate-size claim is made for the new code.

Current official Singular documentation was read narrowly for [lift/std and term export](https://raw.githubusercontent.com/Singular/Singular/spielwiese/doc/reference.doc), [CLI/startup options](https://raw.githubusercontent.com/Singular/Singular/spielwiese/doc/general.doc), and [global dp](https://raw.githubusercontent.com/Singular/Singular/spielwiese/doc/pdata.doc). Exact nodes/ranges and a failed unused URL are recorded in READ-SCOPE.md. These current web reads do not establish the future installed version's compatibility. In particular syntax/output formatting and very fast nested-process identity capture remain untested; missing identity or unsupported output fails closed rather than supplying evidence.

The next review should target the new emitter/protocol, exact certificate semantics, finite mutations and caller delta—not repeat accepted source mathematics or CAPRUN controls absent a specific issue. Installed-binary verification, registered control execution, actual row read-back and both real certificate branches are pending. Old sources and authorities were preserved. All publication tooling used only documentary/metadata operations. Own source files and this complete report were statically read; the final custody manifest pins every owned file and the current retained inputs. All writers are idle at terminal handoff.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7919`.
- Body SHA-256:
  `55482cc192d8e094a4e1997c335c173a7209a9b0489d8766ea651a4b562426c0`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
