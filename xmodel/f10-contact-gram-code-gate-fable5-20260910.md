# f10-contact-gram-code-gate-fable5-20260910 — FIRST Fable 5.1 STATIC source gate: contact Gram certificate source and corrected startup authority

status: UNSEALED (adapter seals; no Seal section and no charge_basis authored here; the standalone BODY-END marker is appended LAST)
lane: f10-contact-gram-code-gate-fable5-20260910
first_action_utc: 2026-09-10T20:18:15Z (sha256sum of all fourteen inputs before any body read)
stop_utc: 2026-09-10T20:36:00Z hard (earlier than launch+20); publication reserve 20:33:00Z; original USER TERM 20:36:00/KILL 20:36:05 armed before launch, never reset
mode: manual mathematics, text, hash and metadata only; ZERO subprocess/CAS/Python import/AST/syntax/compile/test/fixture/coefficient work; every statement is a hand reconstruction from the fourteen frozen snapshots in /tmp/jc2-lane.ptT4Up/inputs
owned: xmodel/f10-contact-gram-code-gate-fable5-20260910.md and box/f10-contact-gram-code-gate-fable5-20260910/input_custody.md, both absent at 20:18:15Z; every authored byte via apply_patch only

## 0. Charged inputs, read attestation and effective authority

All fourteen inputs were hashed BEFORE any body read and every digest equals the charged list; the full sha256sum output is reproduced in the owned input_custody.md, and this report carries only abbreviated forms. No two basenames collide. Each file was read WHOLE by one bounded unclipped read reaching its EOF, Seal or BODY-END marker: producer.py 283 lines, checker.py 310 lines, overlay authority.py 146 lines, CONTRACT.md, VALIDATION.md, DISABLED-REGISTRATION.json, READ-SCOPE.md, CONTRACT-DELTA.md, the producer report (astra 20260910, sealed body 6208 B), the startup-fix report (sealed body 2795 B), and the three science premises plus the old middle real-domain code gate. No clipping occurred, so nothing needed recovery. No provenance was followed, no original outside the snapshot was opened, and no accepted foundation was re-reviewed.

Effective authority under review is ONLY the overlay authority.py (96ca421f…41) co-installed in future with the unchanged producer (afc617ae…ee) and checker (ceca0a07…06). CONTRACT-DELTA supersedes the original flags wording and the disabled template's flags value only; the effective startup flags reviewed are -E -s -S -B. The symbolic Gram premise (real-window producer plus its FIRST gate) and the literal quartic premise (symmetric remainder report) enter only at their stated scope: the four literal remainders, the W elimination, the monic quartic, the nilpotent-preserving four-row equivalence over rings where s-3 is a unit, and the Gram rank equivalence at real window points. The producer report's section 5 reality converse and the incidental closed-triangle "equivalently" phrase are NOT adopted: positivity on the prescribed triangle is sufficient for prescribed pairs only. The old middle real-domain gate supplies acknowledged reuse qualifications (import path, offset date, resolved pin keys, raw-byte receipt digest), not candidate-only runtime admission.

## A. Independent source binding: CONFIRMED

Producer `sources()` (three axes s,p,V; no W): d=210(s-3); K=p-(s-3)(s-4); D0=(-5s^3+62s^2-259s+394+(10s-62)p)/2; N=420V^3-210(s-1)V^2+63KV+D0; A=120V^3+180(s-5)V^2+30(s^2-p-9s+26)V+s^3-2sp-14(s^2-p)+71s-154; Phi=(dA+120(s-5+6V)N)/302400. All equal the accepted displays (1),(2). fa0 is F_a0 at W=0 (a3=30V-14, a2|W=0=71-270V+180V^2, a0|W=0=120-720V+1080V^2-240V^3); the W-linear coefficient of F_a0 is -120p+720-1440V and the W^2 coefficient is 360, so q0=d^2 fa0+d(-120p+720-1440V)N+360N^2 equals d^2 F_a0(V,N/d) exactly. I0 is the W-free part of I=F_b0-7F_a0 with D1=-6p^2+(6s^2-80s+378)p-480 and W coefficient 630(p-4+4V+4V^2), so q1=d I0+630(p-4+4V+4V^2)N equals d I(V,N/d). CONFIRMED.

Checker `reconstruct()` starts from the eight literal quartic coefficients a3..a0, b3..b0 in a separate four-axis algebra (s,p,V,W); each string matches the accepted symmetric report line by line (a1 carries -600W+720VW, b0 carries 2520W-7560VW+2520V^2W+2520W^2). X4lin=s^3-2sp, X4const=p^2-s^2p, X3lin=s^2-p reproduce the monic reductions of X^2-sX+p, so fa1,fa0,fb1,fb0 are the four literal remainders. elimination=fb1-(7/2)fa1 must have W-degree exactly 1 with W coefficient -d and N its W^0 part: this recomputes the accepted W elimination by collection rather than copying N. `clear(poly,e)` forms sum_k coeff_W^k(poly) N^k d^(e-k)=d^e poly(V,N/d), refusing W-degree above e; hence phi=clear(fa1,1)/302400, q0=clear(fa0,2), q1=clear(fb0-7fa0,1), with the W^2 cancellation of I independently enforced by the degree refusal. Monic V-degree-4 with leading coefficient exactly 1 and V-degrees at most 6,5 are required. Only the rational constants 7/2 and 1/302400 are divided: no new parameter divisor, no dropped constant row, no V=1/3 omission. The nilpotent-preserving four-row equivalence is consumed as an accepted scoped premise. Section 5 reality converse unused; the receipt scope string says prescribed triangle only. CONFIRMED.

## B. Exact producer and sufficient smaller certificate: CONFIRMED

`divide(poly,phi)` requires phi's V^4 coefficient to be exactly the constant 1 and no higher V power, then for k from the initial top V-degree down to 4 subtracts coeff_k V^(k-4) phi; each step kills V^k exactly, introduces only lower powers, and the final residual is refused if any V^4 survives. Eight columns are rem(V^j q_i,phi), i=0,1, j=0..3, padded by `coeff(2,i)` for i=0..3, so dropped degrees and zero slots are empty polynomials. `primitive` takes the LCM of every denominator across the whole column, then the GCD of the integerised absolute numerators, and multiplies the WHOLE column by the constant c=den/content>0, or 1 when the column is zero. No polynomial content is divided. B[i][j]=cols[j][i] is the 4x8 matrix M diag(c_j). G=BB^T. h[i]=(-1)^i det(G without row 0 and column i) is the cofactor C_0i=adj(G)_i0 (G symmetric, transpose harmless), so R=sum_i G_0i h_i=det G and Gh=Re0 by alien cofactors. `det` is a fixed Laplace expansion along row 0 with sign (-1)^j on 3x3 and smaller minors, empty matrix 1, no parameter division.

z_j=c_j (B^T h)_j, so sum_j M_ij z_j=(B B^T h)_i=R delta_i0: the element sum_j z_j rem(V^j Q,Phi)=rem(u0 Q0+u1 Q1,Phi) equals R V^0 because the z_j are V-free. u0,u1 are recovered from all eight scaled entries in original column order. Then R-u0Q0-u1Q1 lies in (Phi); the producer divides it by Phi and requires a zero remainder, which fixes the quotient sign so that R=U0Q0+U1Q1+UphiPhi. If R=0 the identity still closes, and `remove_edge` refuses 'zero R polynomial' with no artifact: no nonzero determinant is assumed anywhere.

Envelopes by hand (wt s=V=1, p=2): column weights 6..9 and 5..8, row i subtracts i; G_ik<=18-i-k; adj(G)_i0 is a sum of three-entry products over rows 1,2,3 and columns other than i, weight <=54-6-(6-i)=42+i; Cauchy-Binet gives R<=52 (the plain Laplace bound would be 60); z_c<=w_c+42, so U0<=48+2j<=54 and U1<=47+2j<=53; the numerator <=max(52,60,58)=60; monic division by Phi, whose V^i coefficient has weight <=4-i, keeps every quotient term at <=56. V-degrees 3,3 and (9-4)=5. The source checks exactly weight 52 for R and 64 for the three cofactors with V caps 0,3,3,5, so the wire bound 64 contains 56. Nothing measured. CONFIRMED.

## C. Checker independence and full identity: CONFIRMED

checker.py imports argparse, hashlib, pathlib, re, sys and the metadata authority only; no producer symbol and no producer arithmetic. Its algebra is a dictionary on 4-tuples with `power` by repeated multiplication, `synthetic` division, and `checked()` canonicalisation dropping zeros and enforcing term/exponent/bit caps. `polynomial()` parses the wire into 4-tuples with W=0 under the literal envelopes (R weight 52, V 0; U0,U1,Uphi weight 64, V 3,3,5); an empty list is the zero polynomial and multiplies to {}. `identity=add(mul(U0,q0),mul(U1,q1),mul(Uphi,phi))` is compared to R by whole-dictionary equality, so EVERY coefficient including every positive V power must match; no evaluation, sampling or determinant header exists. Exponents of the products stay below the internal cap 100 (s<=70, p<=35, V<=9). The receipt records 'Gram determinant equality NOT independently checked'; a verified identity with R>0 on the domain is a CONTACT rank certificate because at each domain point s>=10/3 so d is a unit, the specialised identity puts 1 in (Phi,Q0,Q1) in R[V], and the accepted equivalence then gives no complex (V,W) contact there. CONFIRMED.

## D. Domain and strict sign: CONFIRMED

a=(6+t)/21, b=(3-3t+7tv)/21: t=0 gives (2/7,1/7); t=1 gives a=1/3, b=v/3; for fixed t, v=0 gives b=(1-t)/7=1-3a and v=1 gives b=(3+4t)/21=4a-1, b affine in v. So 0<=t<1, 0<=v<=1 covers 2/7<=a<1/3, 1-3a<=b<=4a-1 exactly, the closed prescribed triangle minus its a=1/3 edge. s=4-a-b and p=4-2(a+b)+ab=(2-a)(2-b) in both sources. Bidegrees: s is (1,1), p is (2,1) in (t,v), so s^i p^j with i+2j<=52 has t-degree <=52 and v-degree <=i+j<=52; denominators 21^(i+2j) are cleared by the fixed 21^52. The producer enforces the WEIGHTED envelope k=0, i+2j<=52 before substituting and the (52,52) rectangle after; the checker's nested Horner (s outer, p inner) enforces the same rectangle. Endpoint hat(0,0)=21^52 R(2/7 point); since a,b at t=0 are v-free, hat(0,v) is that constant.

Maximal (1-t)^j: producer tests poly(1,v)=0 by summing coefficients over i, divides ascendingly (a_i=cumulative b_k, top residual b_deg+a_(deg-1)=0 checked) and stops at the first nonzero value, so j is maximal and j=0 is returned when hat(1,v) is already nonzero; count<=52. Checker divides synthetically by (t-1) with root 1 and negates the quotient, stopping at the first nonzero remainder, and requires count==j with 0<=j<=52 and the reduced rectangle (52-j,52). Both retain t=0, v=0, v=1. Bernstein: producer converts separably at fixed degree (52-j,52) via t^k=sum_{i>=k} C(i,k)/C(n,k) B_i,n and likewise in v; checker expands sum b_ij C(n,i)t^i(1-t)^(n-i) C(52,j)v^j(1-v)^(52-j) into monomials with the binomial signs and compares the whole dictionary to reduced. All 53 row-0 entries equal the endpoint (an identity for a genuine artifact, since reduced(0,v)=endpoint and the v basis sums to 1 and is independent). Strict sign: with all entries >=0 and row 0 = endpoint>0, reduced>=endpoint (1-t)^(52-j)>0 for every t<1 and every v in [0,1], hence hat>0 there; no t=1 corner is needed and none is checked. No subdivision or elevation exists. Neither source carries an epsilon or sign flip; endpoint<=0 and any negative entry return INCONCLUSIVE (producer status with complete artifact; checker receipt), never a contact or counterexample. CONFIRMED.

## E. Wire, authority and exit protocol: CONFIRMED with retained qualifications

Wire: producer emits only strings (sorted exponent-tuple terms of reduced Fractions with zero terms dropped, dense Bernstein rows with ["0","1"] zeros, j and degrees as strings) through json.dumps sort_keys/compact/allow_nan=False, 'xb' exclusive, fsync, ceiling 16 MiB minus 4096. Checker: canonical decimal regex with the 4096-character test before int(), positive reduced denominators, 12000-bit cap, strictly increasing exponent tuples (duplicates and disorder rejected), zero terms rejected, exact ten-key schema, schema string, science and code dicts equal to the installed authority values, literal variables and bounds, status in {CANDIDATE, INCONCLUSIVE}, exact four-polynomial inventory, exact Bernstein keys, degree_t=52-j, degree_v '52', n+1 rows of 53 rationals; duplicate JSON keys, floats, NaN/Infinity and every JSON integer are rejected in wire mode; size checked from stat and again from the bytes read. The receipt binds raw_sha of the verified bytes, compared to the pin keyed by the resolved path (the old TOCTOU qualification is fixed), plus code pins and the registration digest; ceiling min(4096, 16 MiB minus artifact).

Authority ordering: sys.set_int_max_str_digits guard (fails closed below 3.11 without the backport), JOB tag and Linux, registration resolved/regular/non-symlink/uid 0/mode 0444/<=64 KiB, strict JSON (ints allowed there), enabled/jobtag/operation/exclusive flags, EC2 DMI vendor and i-hex asset tag, instance/hostname, cwd and full script argv, realpath interpreter, exact flags ['-E','-s','-S','-B'] against both python_flags and sys.orig_argv==[sys.executable]+flags+sys.argv, full_argv, SCIENCE dict (its three digests equal the charged real-window, quartic-gate and symmetric-remainder inputs), exact caps 600/550/2 GiB/16 MiB, offset-explicit tz-aware unexpired deadline (a trailing Z is refused: the old offset-date qualification is fixed), pins covering the three co-installed sources, the interpreter realpath, the three science files by exact basename and the checker input, every pin key resolved-regular and re-hashed (resolved-path qualification fixed), outputs equal, absolute, absent, non-symlink; all before `from fractions import Fraction`/math, and P()/checked() refuse while the globals are None. postcheck re-hashes registration and pins and re-tests the deadline before emit. -S removes site initialisation only; without -I/-P the script directory stays on sys.path, so the sibling import path qualification is retained; -E also neutralises PYTHONSAFEPATH. The disabled template still lists three flags, superseded by CONTRACT-DELTA; it cannot authorize (enabled false, root/0444 first). Exit protocol: producer normal 0 with CANDIDATE artifact; normal 2 WITH complete INCONCLUSIVE artifact and a stdout PRODUCER_INCONCLUSIVE sha/bytes line; structural or resource failure prints INCONCLUSIVE on stderr, exit 2, no or partial artifact that the checker's pin/parser rejects. Checker accepts either producer status without trusting it, returns 0 only for PRESCRIBED_CONTACT_EXCLUDED_BY_CERTIFICATE after every predicate, 2 with receipt for sign-INCONCLUSIVE, 2 without receipt for any exception. Retained ROOT qualifications, not defects: argv[0] must literally equal sys.executable for the orig_argv test; interpreter >=3.10 for orig_argv; the future registration must use four flags; IndexError/AttributeError are outside the handler tuple (uncaught traceback, exit 1, still fail-closed). Source authority is metadata gating; ROOT owns native closure, cgroup/CAPRUN, exclusivity and aggregate caps. CONFIRMED.

## F. Two discriminating controls and scope: CONFIRMED as designs only

Uphi+1 keeps weight 0 and V-degree 0 inside the envelope, sorted order (constant tuple first) and the inventory; the identity changes by exactly 1*Phi, whose V^4 coefficient 1 cannot be matched by the V-free R, so the checker reaches ValueError('full contact cofactor identity') after parsing, exit 2, no receipt. Bernstein entry+1 keeps n+1 rows of 53 reduced strings (gcd(num+den,den)=1) and changes the expansion by one nonzero tensor basis element, so 'full Bernstein expansion' fires after identity, pullback and factor checks and before the row-0 test. Both require a fresh raw-byte pin in a new registration; a stale pin refusal ('required input pin mismatch'), a bit-cap crossing or any schema refusal is an early parser/authority branch and must not be counted, exactly as VALIDATION states; unavailability is declared instead. Inverse restoration is whole-byte equality after reverting and re-serialising with the original serializer. No fixture, mutator or runtime evidence exists. No positivity, matrix value, point, source zero, degree theorem or JC2 result follows from this static correctness; a future successful contact test would still need composition with the accepted equivalence at its true scope, and no launch authority comes from this reviewer.

## Verdicts and smallest correction

| item | verdict |
|---|---|
| A producer N/A/I/Q0/Q1 versus checker literal four-remainder reconstruction, W elimination, d, 302400 monic, no lost divisor or row | CONFIRMED |
| B eight padded columns, constant column scaling, G, cofactor signs, R=det G, z and U0/U1 recovery, Uphi sign, bounds 52/54/53/56<=64 | CONFIRMED |
| C checker independence, full-dictionary identity, envelopes, CONTACT rank scope | CONFIRMED |
| D domain map, weighted pullback, 21^52, maximal j including 0, retained edges, separable conversion versus expansion, strict sign, no epsilon | CONFIRMED |
| E wire canonicality, early metadata authorization, -E -s -S -B, fixed old qualifications, exit/receipt cases | CONFIRMED (qualifications retained) |
| F two controls reach the stated mathematical branches; designs only | CONFIRMED |

REFUTED: none. GAP: none blocking. Smallest truly blocking correction: none; the only items are ROOT entry-contract parameters already stated in CONTRACT/CONTRACT-DELTA (four flags, argv[0] equal to sys.executable, resolved pin keys, offset deadline). No optional foundation audit debt is manufactured. Caps 600 wall/550 CPU/2 GiB/16 MiB remain prospective and unmeasured.

## OPEN(S) RAISED

None. No new canonical OPEN ID. The producer's two task-local pending quantities stand: this static review is delivered; one separately ROOT-registered bounded formation and independent check remains unauthorized here.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly the report and box/f10-contact-gram-code-gate-fable5-20260910/input_custody.md, both absent at first action; apply_patch only; no Seal, no charge_basis, no source edit, no fixture, no subprocess, no execution of any input.

## Completion

Authoring attestation: both owned files were written by apply_patch only (custody file 20:25:39 UTC; report skeleton 20:24:30, sections A-C 20:25:39, sections D-F with verdicts 20:26:35, this section afterwards); no Write/Edit tool, no redirection to any file, no helper-generated text. Own checks completed 20:27:37 UTC before this section and before the 20:33:00 reserve: all fourteen input postpins re-hashed and equal to the charged list; zero standalone markers in both owned files; zero charge_basis lines and zero Seal headings; every 64-hex token in both owned files (fourteen) equals a live input digest and the custody rows equal the live sha256sum output byte for byte; the three abbreviated tokens resolve to pins. Own WHOLE read of the report (72 lines, 17460 bytes) completed 20:27:47 UTC. Raised-OPEN check (none) and collision check (EMPTY) precede the marker. No follow-on, computation, registration, seal or charge_basis is authored; ROOT retains custody, terminal intake, native closure and any runtime authority. The marker below is the only standalone marker in this file and nothing follows it. All writers idle at the marker.

<!-- BODY-END -->
