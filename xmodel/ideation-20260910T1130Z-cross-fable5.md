# 1130 adversarial cross — Fable 5.1 (UNSEALED SKELETON, no marker)

lane: ideation-20260910T1130Z-cross-fable5
author: Claude Fable 5.1 (cross; own blind attacked as an independent claim)
first action: 2026-09-10T11:49:22Z (date); hard stop 11:54:00Z; reserve 11:52:30Z; never reset
custody: 10 snapshots in /tmp/jc2-lane.vSH4V0/inputs, hashes listed in the charge matched the charged list verbatim before body (hash-before-body); apply_patch only; no science execution

## 0. Read scope

Read WHOLE: DELTA-AND-CONTRACT, packet-guide, coordinator blind, fable5 blind, astra-source blind. Read PARTIAL (headings plus Card 1 bodies, clipped by the reader cap and the 11:54 stop): astra-geometry, astra-construction. NOT read this turn: bgv-primary-EXCERPT, r2-cyclic-gate, r2-cyclic-producer (same-byte prior WHOLE reads by this model family exist for the gate and excerpt; they are NOT claimed as fresh reads and no fact below depends on them). No original, peer cross, live file, network, process, Git, corpus or CAS was touched. Time from invitation to reserve was about three minutes; this cross is explicitly PARTIAL at that scope.

## 1. Verdict per claimed novelty (all five blinds, equal standing)

**Fable Card 3 (unconditional r2 decision by a 3-row GB plus linear algebra).** Attacked as an independent claim. (a) "d <= 35 by weighted Bezout, equality iff s.o.p." is UNPROVED as stated: f5,f6,f7 are not weighted homogeneous, so 35 is an intersection number of their closures in a weighted projective 3-space; an affine length bound when the intersection at infinity is improper needs a refined-Bezout theorem there, which the blind never cites. The bound is also dispensable: the GB returns d exactly. Delete the bound, keep the computed d. (b) J versus J0: E=(R0/J0)[t0^-1] is a degree-0 (cyclic-invariant) piece. The blind's "t0^d in J in R" is equivalent to "t0^d in J0 in R0" only if J0 = J cap R0, i.e. J is homogeneous for the cyclic grading and R0/J0 -> R/J is injective. Not verified here: typed OPEN, checkable by reading 17zzr's definition of J0. (c) Exponent: nilpotency index of t0 in R/J is at most dim_B(R/J) <= d, so t0^d is the right power ONLY after d is computed; using 7 (17zzr) presupposes the s.o.p. premise the card claims to avoid. (d) Complete NONZERO endpoint: E != 0 gives a maximal ideal with residue field finite over B and t0 a unit there, hence a point of A over an at most cubic extension. That is existential and correct, BUT only if every guard of the 19-row algebra is either inverted in E or checked at the point; the blind names only L0. Any un-inverted guard makes the "field point" possibly non-Keller. Verdict: valid shape, three unverified links (bound, J0, guards); "expected minutes" is unmeasured and must be struck.

**Fable Card 1 (s.o.p. premise as a unit-ideal test in B[p,q]).** The dehomogenization at the weight-1 coordinate is legitimate over Bbar and the Nullstellensatz over B makes "unit ideal" equivalent to "no nontrivial common zero with Y1 != 0"; with 17zzp on X1=0 this decides the s.o.p. question. The inference "non-unit => gr R infinite" is FALSE: dim_B gr R = dim_B R, so gr R is infinite iff R is. What a non-unit refutes is the Macaulay identity in(I)=(H5,H6,H7) and therefore 17zzr's 35 and 7-per-class counts, not finiteness. "Expected seconds" is unmeasured; strike. Same object as astra-source Card 1 and the astra-construction prerequisite (three blinds, one test; agreement is not evidence).

**Fable Card 2 (different-divisor saturation).** Two gaps. Set-theoretic equality V(D(g0)) = divisorial preimage of Sing Z is asserted from the BGV proof, but the excerpt was not reread this turn and the equality needs both étaleness and that Sing Z is exactly the non-immersion locus of (g0,e); OPEN. Saturation must hold for each irreducible component individually (BGV's "every individual singular divisor"), not for the union; the card's factorisation D(g0) = beta prod h_i(f,g)^{q_i} silently uses per-component saturation plus irreducibility of each e^{-1}V(h_i), which is exactly what a counterexample would violate. The proposed calibration on BGV's char-p family is NOT discriminating: there weak type 1 fails by degree congruent to 1 mod p (inseparability), so an unsaturated component of V(D(x)) in char p says nothing universal about char 0, and one example can only exhibit, never prove, "infinity-type only". Verdict: NO_NEW_MECHANISM unless a char-0 statement replaces the calibration; the reformulation is a KNOWN-content restatement.

**Coordinator Card 1/2 and ranking.** RAISE 36 is a scheduling call, not a promotion; consistent with the packet. The statement that a nonzero finite Q-algebra with source-correct back-maps "suffices existentially" is right but inherits the guard caveat above. Card 2's composition target Y=-J/H7 is unattacked. The "mod-prime resultant only with a proved integral model and nonzero-specialization argument" line is exactly the condition astra-source Card 1 tries to meet.

**Astra-source Card 1 (proper special-fiber emptiness at a prime of B over 89).** The valuative argument is sound in shape: weighted projective space over a DVR is proper, so an emptied complete special fiber excludes a generic nonzero point. Three preconditions the card only partly names: (i) each H_h must be made primitive (if some H_h reduces to 0 the special fiber enlarges and the test becomes vacuous); (ii) the residue field is that of the chosen prime of the degree-7 field B, possibly of degree >1; (iii) both charts X1=0 and X1!=0 in the SAME integral model. Nonempty special fiber is NONDECISION. The p*T=1 control is correct. Verdict: a cheaper sufficient test whose negative is uninformative; strictly dominated by the exact char-0 unit-ideal test when that is feasible.

**Astra-construction Card 1 and the parameter action.** "If the chart is empty, the regular-sequence theorem gives seven dimensions per cyclic component" is conditional on 17zzr as accepted; fine. The claimed "-6 not -18 late operator coefficient" is an unverifiable numeric assertion here; it must be replayed from the row emitter, not cited. The zeta*s parameter action shows a family symmetry that changes inputs and outputs together; it correctly refutes the avenue-26 shortcut (cubic coefficient extension is not an intermediate field of a fixed Keller map), agreeing with astra-source section 5. Not an accepted source-map theorem; no task needed.

**Astra-geometry Card 1 (112/196 -> r1 source arrow audit).** Its outcome "17zze excludes precisely that source class" overreaches: the r-label is a parameter choice, and the packet itself says the coefficient parameter moved from r1 to r2, so the arrow can at best cover the r=1 subclass, and only if the r-invariant is well defined on actual pairs rather than coordinates. That well-definedness is the exact next lemma and is not in the card. "One 20-minute pass" is unmeasured. DUPLICATE of the packet's own scope statement unless the quantifier chain is printed; subject to ROOT's canonical checksum.

**Convergence note.** All five blinds route to the same two objects (X1!=0 chart, then the complete r2 quotient). Per contract this agreement promotes nothing.

## 2. Cheapest discriminating task

One exact char-0 Gröbner computation of (H5,H6,H7)(1,p,q) in B[p,q], B=Q[Z]/P7, from verified reconstructed coefficients, with the X1=0 half taken from 17zzp. Two variables, three rows. It is strictly smaller than Fable Card 3's three-variable zero-dimensionality test and decides the sole 17zzr premise either way. No runtime is asserted.

## 3. Dependencies, outcomes, stop

Dependencies: r2 runtime FIRST gate, one checked reconstruction, accepted 17zzj/k/p/r. Outcomes: unit -> 17zzr counts licensed, then the complete t0^7 in J0 test with all 15 rows, nilpotents and every guard; non-unit -> the 35/7 counts are void, fall back to Fable Card 3 with d computed (after the J0 and guard OPENs are closed) or the 18-row localized ideal; positive-dimensional actual (f5,f6,f7) -> structural information only. Stop: first missing denominator-exceptional fiber, first unverified back-map, two non-informative attempts. Nothing here implies all 112/196, max 140/196, all-F10 or JC2.

## 4. Systems choice

ONE documentary change: the coordinator's byte-preserved APPROACHES history snapshot, accepted only on measured byte counts and a link-scope check. Fable's >60 KB pre-split and astra-source's queue practice are folded into NO_CHANGE.

## OPEN(S) RAISED

None canonical. Typed OPENs, task-local: (O1) is J0 = J cap R0 in 17zzr; (O2) which guards beyond L0 must be inverted in E for the NONZERO endpoint; (O3) refined weighted Bezout for the d <= 35 bound (dispensable); (O4) set-theoretic V(D(g0)) identity and per-component saturation in BGV 27.5. No charge_basis: no exit-price assertion is made.

## COLLISIONS

status: EMPTY. Own-only check; both owned targets absent at 11:49:22Z; all writes via apply_patch; no corpus scan; no seal.

## Completion

PARTIAL: geometry and construction read only at Card-1 depth; gate, producer and BGV excerpt not reread this turn. Sections 0-4, OPEN and COLLISIONS reviewed WHOLE by the author before the single marker below. Writers idle.

<!-- BODY-END -->
