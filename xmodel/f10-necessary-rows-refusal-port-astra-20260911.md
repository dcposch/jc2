# Necessary-row refusal-policy port

STATIC / DISABLED / CONDITIONAL documentary preparation. First action 2026-09-11 06:20:14 UTC; owned targets absent and all ten input hashes matched before charged body reads. Original publication reserve 06:31 UTC / hard stop 06:34 UTC, unchanged. No interpreter/scientific execution, worker, live review, coefficient or fixture access is authorized or performed.

The deliverable is seven exact source-derived refusal triples at the new administrative source path, not an enabled policy, source change, source FIRST, future-host attestation or observed result.

## Result

All seven triples are derived conditionally. At `/opt/jc2-r3-necessaryrows-20260911a/science`, the five complete ValueError tracebacks use the actual new producer frames 369 (module) and 343 (authorization in main). Authority locations and source lines are unchanged: status82, limits/inventory95, source100, and hash78 then74. The authorization CALL still produces nine spaces and twenty carets; the nested load_pinned CALL retains eleven spaces and thirty-seven carets. Whole-line calls/raises have no indicator line.

The two wrong-job startup controls print only `REFUSED: registered CLI contract` plus LF. Fresh checker text confirms module514 -> authorization496, before scientific import497 and try498; SystemExit prints none of those frames. All seven retain exit1 and empty stdout on the imported ordinary CPython/healthy-stream assumptions. Caps and inventory have identical branch/bytes and differ only in their registered metadata mutations. No wildcard, regex, fallback bytes or observed-run calibration is supplied.

The owned box contains `DISABLED-POLICY.template.json`, `DERIVATION.md`, `POLICY.diff` and `PINS.json`. The diff changes only the preflight schema, administrative source path, two producer line numbers and three changed source hashes. The two startup output strings, all five exception/source-line strings, spacing and caret counts are preserved. Both arithmetic hashes remain unchanged.

## Trust and deployment boundary

The old complete derivation and completed Ubuntu compatibility report are consumed at their exact documentary scope: the nominated ordinary Ubuntu amd64 3.12.3-1ubuntu0.16 package, ASCII source locations and ordinary formatter/exception handling. No upstream/package source or provenance file was fetched or reread. The executable/package relationship does not identify a future worker or its native closure, hooks, source files, position tables or streams.

PINS records the separate actual final five-source vector. The policy itself deliberately retains `arithmetic.py: UNFORMED` and exactly its three keys schema/source_files/entries; no enabled field is added. It cannot pass the dispatcher's exact final-source binding until ROOT makes a NEW finalized policy copy with actual installation/native/contract/registration facts. The charged ROOT candidate remains a pending static candidate, not execution release. No live FIRST report/log/receipt was accessed.

## Checks and remaining quantity

New authority/producer/checker and all four documents were fresh WHOLE after all ten pins. Dispatcher uses explicit same-agent prior-WHOLE reuse with current hash and narrow current text checks. Arithmetic and check_arithmetic are HASH_ONLY. No scientific module or runtime was changed; no coefficient/place/fixture work, source admissibility claim, optional mutation hardening or worker operation occurred.

Only inert source-text/metadata reading, hashing, literal diff and authorized documentary publication ran. No interpreter experiment, scientific subprocess, import, AST, syntax, compile or test of any size ran. This is a conditional byte derivation, not evidence that any refusal was observed.

OPEN quantity: one future-host/install/native identification and seven exact refusal comparisons; execution cost is UNMEASURED and no numerical performance estimate or cap-sufficiency claim is made. Cheapest next check is ROOT's exact template/source-line comparison, then the already-required actual native/source/registration binding. Earlier startup/import/DMI/ACL/resource/display failures are STOP and do not justify broadening accepted output bytes. No unresolved text-location gap remains; actual deployment remains unformed.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `4321`.
- Body SHA-256:
  `05cf712a67a442b608aa7032de120bdf81d5a9d0be802c8bc93ce5760f644c59`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
