# Blind whole-portfolio ideation — Fable5 — round `20260827T1606Z`

Model: Fable 5 (Anthropic), `claude-fable-5`, via `ops/adapters/claude.sh`.
Role: equal-standing core co-researcher, clean-blind submission.
Date: 2026-08-27 (after 16:06Z seal).

Basis verification: git HEAD `418e413593120d19e15e6546eb50c985f4b1f038` matches the
packet. All seven frozen-basis hashes and all six pinned delta hashes
(D5G, R6, R7R1, D5N, Grok degree-8 raw-support, V24R6R1 adjudication) were
recomputed and match. Two same-day files postdate the seal by local mtime
(`ggv-quarter-root-characteristic-de-rham-tower-r7r1-hostile-review-fable5-20260827.md`,
09:17:25 local; `ggv-degree8-r5-survivor-raw-support-analysis-hostile-audit-sol-ultra-20260827.md`,
09:22:06 local, vs packet 09:08:46 local) and were **not read**. Full
blindness disclosure in §10.

Evidence tier of everything new below: **IDEATION / DESK-DERIVED,
UNREVIEWED.** Derivations marked `[desk-checked]` were verified by exact
`Fraction` arithmetic in this session (§9 lists every check). Nothing here
is promoted evidence; every consumed provisional premise is named with its
rollback.

---

## 1. Disposition vector over all 46 avenues

| # | Avenue | Disposition |
|--:|---|---|
| 1 | GGV corner farm | **raise** |
| 2 | Sigray / Eggers–Wall ladder | **raise (small)** |
| 3 | Strip ODEs / residue functional | unchanged |
| 4 | Formal-germ D-series | unchanged |
| 5 | JvdK degree descent | unchanged |
| 6 | Abhyankar–Moh one-place | unchanged |
| 7 | Jelonek A(F) | unchanged |
| 8 | Formal-inverse combinatorics | unchanged |
| 9 | Lee–Li Conjecture E | unchanged |
| 10 | HC4 bridge | unchanged (stopped at `NO LEVERAGE`) |
| 11 | Mathieu/GMC ladder | unchanged (refuted) |
| 12 | Face isolation / p-adic multinomials | unchanged |
| 13 | Dixmier DC(2) | unchanged |
| 14 | End(A_1) / Zheglov audit | unchanged (T2 replay still the named CE-side debt) |
| 15 | Spectral surfaces / commuting PDOs | unchanged |
| 16 | D-module / holonomic index | unchanged |
| 17 | BCW cubic stabilization | unchanged |
| 18 | Graded / GIT | unchanged (closed) |
| 19 | Char-p + Witt lifting (incl. AS109, max-twelve frontier) | unchanged |
| 20 | p-curvature formalism | unchanged |
| 21 | p-adic injectivity / Hensel | unchanged |
| 22 | Integral points / heights | unchanged |
| 23 | Analytic global inverse | unchanged |
| 24 | Real JC / Pinchuk | unchanged |
| 25 | Monodromy passports | unchanged |
| 26 | Primitive-group td bound | unchanged (still the consensus cheap untried pick; not this round's marginal capacity) |
| 27 | Links at infinity / splice | unchanged |
| 28 | Log surfaces / BMY | unchanged |
| 29 | LND / Gauss–Manin kappa | unchanged (kinship with the tower noted in §4.3; no new client) |
| 30 | ML invariant / affine classification | unchanged |
| 31 | ZMT / Rees integrality | unchanged |
| 32 | Collision ideal / injectivity | unchanged |
| 33 | Symplectic residues | unchanged (its "genuinely twisted class" demand is now met *inside* avenues 1/2 by the mu4 tower classes; reopening it as a standalone program would duplicate) |
| 34 | 2D tangent-sweep / pole removal | unchanged |
| 35 | Dim-3 descent | unchanged |
| 36 | Guided CE search | unchanged (R6 parametrization makes any future guided search sharper, but that is a compiler note, not a rank change) |
| 37 | Finite-field census | unchanged |
| 38 | Tropical | unchanged |
| 39 | Cohomological cluster | **raise (scoped)** |
| 40 | Free-associative lift | unchanged |
| 41 | Scaling deformation | unchanged (refuted principle) |
| 42 | Markus–Yamabe | unchanged |
| 43 | Ritt decomposition | unchanged |
| 44 | Moskowicz prime-td | unchanged (closed as proof input) |
| 45 | Differential Galois | unchanged (the tower is a rigorous univariate de Rham reduction, but it lives in avenues 1/2; no standalone Kovacic program follows) |
| 46 | Lean / formalization | unchanged |

Reasons for the three changes.

**Avenue 1 — raise.** The delta assembles, for the first time, a complete
*elimination architecture* for one whole corner face: the R7R1
characteristic tower is an F-side eliminant of the exact determinant
system; the G-side is uniquely determined at weights 15–21 by injective
linear maps; the endpoint is one bilinear evaluation. §3 makes this exact
and §5 Card 1 shows the tower already bites at row one with an explicit
codimension-3 gate `[desk-checked]`. Avenue 1's recorded stuck-point ("no
general corner endgame") now has a named candidate mechanism, testable on a
second corner (Card 3).

**Avenue 2 — small raise.** The tower classes `[q_n dX]` on the mu4 cover
are intrinsic, coordinate-free, *complex-valid* invariants of the corner
data. The R4 square-class audit explicitly recorded that no complex
`G2-PSC` fidelity field was proved (square classes die over `C`). The
tower classes are the first candidate packet fields that survive base
change to `C`; see §4.3. This is a design contribution to the transport
obligation, not a transport theorem.

**Avenue 39 — scoped raise.** Its recorded stop was "refuse to start until
a 10-line class is written." A ten-line class now exists and provably
kills things at corner-local scope: `[q_0 dX] = [w dX]` on `w^2=H` is the
promoted endpoint obstruction (R5 via trace descent), and §3 extends the
same species to all rows. The raise is strictly at corner-local scope; no
global vanishing statement attached to `J=1` exists.

No avenue is lowered or reopened: the delta adds instruments to the two
busy routes and changes no negative evidence elsewhere.

---

## 2. Bottleneck reranking and falsification attacks

### Proof bottlenecks, ranked

1. **The `8_28` lower-row gate plus global endpoint** (avenues 1/2). The
   608-generator ideal `I_<22` plus `D22=1` plus `D23..D35=0` is the whole
   face at the frozen raw support. §3 shows this is no longer a
   400-variable bilinear elimination problem: it decomposes into an F-side
   analytic tower, a G-side unique completion, and one bilinear
   evaluation. This face is the flagship test of the entire raw-landing
   architecture; either outcome restructures avenue 1.
2. **K00 exact incidence binary** (max-twelve terminal path). The
   V24R6R1 `D(W)` certificate awaits Opus5 cross-audit; the chart-free
   Fitting/rank atlas and the long exact-Q jobs are correctly designed.
   This is now review-bound and compute-bound, not idea-bound.
3. **`G2-PSC` transport/fidelity.** Still the deepest global wall. The
   delta contributes a candidate invariant species (§4.3) but no transport
   theorem. It stays above cofinality because every hybrid route owes it.

### Strongest falsification / counterexample attacks

1. **Solve Grok's `n=16` quadratic gate for a positive bounded fixture.**
   The unreviewed producer report reduces the next incidence to 17
   cokernel conditions, quadratic in at most 10 variables after a
   9-variable linear elimination, on frozen exact data. If a fixture with
   `D_15..D_21` consistent and `D22=1` exists at the truncated tier, the
   cheap-exclusion hope for the face dies early and the tower gates (which
   need rows 23/24 licensed) become the only wall; if no fixture exists,
   the exclusion program gains a concrete rung. Cheap and decisive either
   way. (Requires hostile review of the Grok report first, or in-lane
   rederivation of its window/dimension tables; §7.)
2. **AS109 residual `n=6` on the surviving content strata** under one
   fixed polynomial support with exact `J=1` and exterior polar
   cancellation. This remains the only disciplined characteristic-p seed
   with a live route to a characteristic-zero object; everything else on
   the disproof side is history-closed or capped.

The K00/formal algebraization client stays third: it is gated behind the
exact incidence binary and is already correctly staged.

---

## 3. Required connection: differential tower × raw determinant/lower gate × survivor geometry

**Verdict: these are one object, not three.** The composition is exact and
I can state it with proofs at desk tier. One tier separation must be kept
(end of section).

### 3.1 The tower is the F-side eliminant

Setup as in R7R1 (provisional; consumed with rollback): `F_0=H^2`,
`G_0=H^3`, `H=X^8-1`-shaped degree-eight leading data, raw slots from the
frozen D3 lattice, `P=F^{1/8}`, `s=t/P`, `Q=P^2=F^{1/4}=sum q_n s^n`,
`L=K(X)(p)`, `p^4=H`.

Three observations that do not appear assembled anywhere in pre-cutoff
history:

1. **The `q_n` depend on `F` alone.** `Q=F^{1/4}` re-expanded in `s=t/P`
   involves only `F_1,...,F_14` (the raw `F` windows, 124 free slots) and
   `H`. `G` never enters. I rederived `q_1` independently:
   `F^{1/4}=H^{1/2}(1+F_1t/H^2+...)^{1/4}` gives the `t`-coefficient
   `F_1/(4p^6)`, and `t=sP≈sp` converts it to `q_1=F_1/(4p^5)`, matching
   R7R1 (0.4) `[desk-checked]`.
2. **Finitely many honest equations license the infinite tower.** By the
   D3 lattice inequalities (rederived: the `F_n` window
   `max(0,ceil((n-8)/3)) <= i <= 16-n` is empty for `n>=15`, and `G`
   stops at 21), the determinant `E` is a polynomial of `t`-degree at most
   35. A genuine landing has `[f,g]=1`, hence `E=t^22` *exactly*, which is
   the finite system `D_1..D_21=0, D22=1, D23..D35=0` — thirteen rows
   beyond the endpoint, the rest automatic. R7R1 then gives
   `w_{n+22}'=-(n+2)q_n/16` for **every** `n>=0`: an infinite family of
   exactness conditions `[q_n dX]=0` in `L`, bearing on only 124 F-slots.
   (R7R1 states the licensing; the weaponization against the finite `F`
   window is the point here.)
3. **The G-side is not free either.** Grok's maps
   `L_n(G)=2H[(12-n)H'G-4HG']` for `n=15..21` are injective on the raw
   `G_n` windows, so once `F` and the lower `G` are chosen, `G_15..G_21`
   are *unique* when they exist, and `D22=mixed_22` is a single bilinear
   evaluation with no `G_22` handle (the `G_22` window is empty; rederived
   from the lattice).

So the exact-tier face decision decomposes:

```text
(F-gates)  every [q_n dX]=0 in L           — analytic, F-only;
(G-step)   mixed_n in im(L_n), n=15..21    — unique G-completion;
(endpoint) mixed_22 = 1                    — one bilinear evaluation.
```

No Gröbner elimination on 400 bilinear slots is required to *decide* the
face; staged linear algebra plus finitely many nonlinear gates suffices.

### 3.2 The G-side has the same de Rham structure `[desk-checked]`

New unification: with `gamma_n=(n-12)/4`,

```text
L_n(G) = -8 H^{2-gamma_n} (H^{gamma_n} G)'.
```

Verified exactly for all `n=15..21` (`2(12-n)=-8gamma_n` plus a random-`G`
polynomial identity check). So `mixed_n in im(L_n)` is exactness of
`H^{gamma_n-2} mixed_n dX` against the window, with quarter-integer
exponents: rational for `n=16,20` (`gamma=1,2`), mu4-twisted otherwise.
The F-side tower rows, the G-side lower maps, and the promoted endpoint
criterion (`q_0` row = R5, via `d(vB^{3/2})=B^{1/2}(Bv'+(3/2)B'v)dX`) are
therefore **one mu4-graded de Rham complex on `y^4=H`**. The whole face is
one twisted-derivative-image problem with shifting quarter exponents.

### 3.3 Survivor geometry says where the complex lives, and the tower bites there

R5/R6 (`q_0` row) restrict degree-eight leading data to two strata:

- **Branch P** (`H=A^2`, `deg A=4`): `p^2=±A`, so `L` is the function
  field of `w^2=±A` — genus one. New result-shaped observation
  `[desk-checked, desk-proved for squarefree quartic A]`: the `q_1` row,
  `[F_1 dX/(4A^2 w)]=0`, cuts the 16-dimensional raw `F_1` window by
  **codimension exactly 3**. Proof: odd primitives with poles only over
  `A` and infinity are exactly `Qw/A^2`, `Q in K[X]` (higher `A`-orders
  force `A|Q`; lower orders absorb via `Q -> AQ`), and
  `d(Qw/A^2)=[2Q'A-3QA']dX/(2wA^2)`, with `Q mapsto 2Q'A-3QA'` injective
  (a kernel would force `Q^2 ~ A^3`) and degree-bounded to `deg Q<=12`.
  So the exact subspace is 13-dimensional; rank 13 of the 27 candidate
  generators confirmed exactly for `A=X^4-1`. The three cut functionals
  are one residue pair at infinity plus the two compact `H^1_dR` classes
  of the genus-one curve. Branch-point residues vanish identically
  (parity: `dX ~ w,dw`), and the gate is **twist-invariant**: the image
  of `Q mapsto 2Q'A-3QA'` is the same subspace on `w^2=A` and `w^2=-A`.
- **Branch Q** (`H=A^2B`, `deg B=2`, `A=N_B(v)`): `K(X)(p^2)=K(X)(sqrt B)`
  is a rational conic; `L` is a double cover of it branched (generically)
  at the 6 conic points over roots of `A` plus the 2 `B`-ramification
  points — by Hurwitz a genus-three curve (computed here, unverified).
  The `q_1` row lives there; expected codimension larger than 3.
- **The `q_2` row is genus-zero residue calculus** on both branches:
  `q_2=F_2/(4H)-F_1^2/(16H^3)` is rational, and exactness in `L` descends
  to `K(X)` (Galois averaging after the mu4 base change), i.e. all
  residues vanish. Nonvacuity is already proved: on branch P with
  `F_1=0`, `F_2=1`, `A=X^4-1`, the residue of `dX/(4A^2)` at `X=1` is
  `-3/64 != 0` `[desk-checked]`, so the fill `F_1=0`, `F_2=const!=0` is
  excluded for exact-tier landings on branch P.

So the tower is *not* vacuous on the survivors: row one already cuts three
dimensions on branch P, and row two cuts more. Since the tower is
infinite and `F` has 124 slots, the realistic endgame is that the tower
forces `F` into a small, structured locus (leading edge, modes, and the
support-preserving part of the gauge group), after which the injective
G-completion forces `G_15..G_21` and the endpoint evaluation decides the
face. A sharpened version of Grok's unproved attempt #9 becomes the
natural target (Card 2).

### 3.4 The one separation to keep

Tier firewall: at the **truncated tier** (bounded formal fixtures with
`D22=1` but `D23..D35` free) only `q_0` is licensed; `q_1` needs row 23
and `q_2` row 24 (R7R1). The Grok finite object and any fixture search
live at that tier until the live D5G extension freezes `D23,D24`. At the
**exact tier** (genuine landings) the whole tower is licensed now, modulo
R7R1's provisional status. Ledgers must keep the two tiers separate;
conflating them would overclaim exactly the way the packet forbids.

---

## 4. New mechanism, new avenue, new cross-avenue connections

History search performed before claiming novelty (§9 lists queries): the
endpoint (`q_0`) de Rham criterion, its general `(alpha,beta,N)` endpoint
mechanics with primitive-cover repair, the seven-dimensional `M` cokernel,
square-class packet fields, and the Pell-vs-exactness distinction all
exist in pre-cutoff history. The following do not.

### 4.1 New mechanism (label: NEW): the mu4 de Rham complex of a corner face

§3.2–3.3 in one sentence: *every row of the exact `8_28` face system —
F-side tower rows, G-side lower maps, and the endpoint — is a
twisted-derivative image/window condition `P in im(H^a d/dX H^{-b})` with
quarter-integer exponents on the cover `y^4=H`, and restricted to the two
R6 survivor strata it becomes finite exact linear algebra on curves of
genus one (P) and three (Q) plus genus-zero residue calculus.* The
decisive new tests are the `q_1`/`q_2` gates (Card 1) and the reciprocity
target (Card 2). Deduplication fingerprint: obstruction = exact-tier
landing on the frozen `8_28` raw support; mechanism = mu4-twisted
exactness/integrating factors; object = `[q_n dX]` on `y^4=H` restricted
to R6 strata; decisive test = codimension of the `q_1`/`q_2` gates.

### 4.2 New avenue candidate (label: NEW, registration proposed): a corner-to-cyclic-cover functor as the farm endgame

Avenue 1's recorded gap is "no general corner endgame." Proposal: for a
general corner with weighted degrees `(m,n)` and edge `F_0=H^a`,
`G_0=H^b`, the R7 conjugacy should produce a characteristic tower on a
cyclic cover of order `m/a` (here `8/2=4`), with the mu-grading by
`n+const mod m/a`; corners with `m/a<=2` give purely
rational/hyperelliptic towers (residue calculus only). The reviewed
superelliptic review already supplies the primitive-cover mechanics at
*endpoint* scope; the all-rows tower for a second corner is untried. If
the derivation goes through on one other live farm corner, the farm gains
a uniform necessary-filter mill (Card 3); if it fails, the `8_28`
structure is exceptional and we learn why. This is falsifiable and cheap.

### 4.3 New cross-avenue connections (label: NEW)

1. **Tower classes as `G2-PSC` packet fields (avenues 1 ↔ 2).** The R4
   square-class audit recorded that the square-class field is arithmetic
   descent metadata and "no new complex `G2-PSC` fidelity field is
   proved." The classes `[q_n dX] in H^1_dR` of the corner cover are
   coordinate-free, functorial under the deck/orientation data the
   compilers already tag, and **survive base change to `C`** — the first
   candidate fidelity fields of the missing species. Proposal: when the
   raw-to-Morse/naturality chain next freezes a packet schema, include
   the licensed tower classes as named fields, with the exact licensing
   tier recorded per field. This is a schema proposal, not a transport
   theorem; `G2-PSC` remains open.
2. **Cartier shadow of the tower (avenues 1 ↔ 19/21).** Exactness of a
   rational differential has a cheap mod-p necessary screen via the
   Cartier operator, which the AS109 lane already uses in anger
   (derivative-image membership in `F_109[x]`). Tower-gate clients can
   therefore pre-screen candidate `F`-fills mod two primes before any
   exact run. Strictly discovery-only, never evidence (§6 firewall).
3. **Known, for the record (label: KNOWN):** the Pell-receiver
   (`w^2=Q`) versus exactness (`w^2=H`) distinction was already drawn in
   the superelliptic hostile review; I add nothing there and flag it to
   prevent a future duplicate.

---

## 5. Idea cards (three)

### Card 1 — Survivor-curve tower gates `q_1`/`q_2` (F-side eliminant, degree-eight face)

- **Target gap:** no tractable necessary conditions on the raw `F` slots
  beyond the `q_0`/endpoint row; the 608-generator lower gate is
  otherwise attacked head-on.
- **Dependencies:** R7R1 (PROVISIONAL, review live — its (0.2)/(0.4)
  identities are independently rederivable at desk); R5 (promoted); R6
  (PROVISIONAL — needed only for the survivor chart, avoidable by working
  with generic `(A,B,v)`); frozen D3 windows (rederived from the lattice
  inequalities in-lane); D5G extension (needed only for the truncated
  tier). No Grok-report claim is consumed without in-lane rederivation.
- **Cheapest discriminator:** already partially executed `[desk-checked]`:
  branch-P `q_1` gate has codimension exactly 3; `q_2` residue gate
  nonvacuous. The card completes: (i) branch-P `q_1` functionals as three
  explicit linear forms on `F_1` (reduction recurrences on `w^2=A`, per
  the reviewed `[X^n omega]` style); (ii) branch-Q `q_1` on the
  genus-three cover, generic `(B,v)`; (iii) full `q_2` gate with the
  `F_1^2` quadratic term on both branches; (iv) special strata (`A`
  non-squarefree, `4|delta` denser modes) and the `deg Q=6` resonance;
  (v) a support-preserving gauge direction as a positive control (it must
  pass the gates) and `F_2=const` as the frozen negative control.
- **PASS meaning:** explicit new necessary linear/quadratic gates on raw
  `F` for every exact-tier landing on each survivor branch — the first
  wall-cutting instrument past `q_0`; producer report to hostile review.
- **FAIL meaning:** a gate computes to codimension 0 on a branch — the
  tower is weak at that row; bank the vacuity, move one row deeper, and
  if `q_1..q_3` are all vacuous on a branch, downgrade the tower eliminant
  for that branch and report the separation honestly.
- **Stop condition:** rows `q_1,q_2` (at most `q_3`) on both branches;
  desk/exact only; no Gröbner, no AWS; one report.
- **Rollback subtree:** everything tags R7R1/R6 PROVISIONAL; an R7R1
  review failure retracts the licensing (the gates survive as conditional
  statements about the conjugacy identity, which is independently
  rederivable); an R6 failure retracts only the stratum labels.
- **Expected information gain:** high — either the first F-side gates
  beyond the endpoint, or a decisive weakness result that redirects the
  tower program before anyone funds it.

### Card 2 — F/G reciprocity and the `mixed_22` exclusion identity

- **Target gap:** Grok's failed attempt #9: no mechanism connects
  lower-row solvability to the endpoint value. If the lower gates force
  `mixed_22 in (H)`, then `D22=1` is impossible at every root of `H` and
  the face dies at the frozen raw support (both survivor branches,
  exact tier and truncated tier alike).
- **Mechanism:** by §3.2 all rows are one mu4 de Rham complex; the
  natural candidate is a residue-pairing/reciprocity identity on `y^4=H`
  expressing `mixed_22 mod (H)` as a sum of pairings of lower-row
  obstruction classes, which the gates annihilate. D5N's free-ring
  conjugacy (its (2.1)–(2.2)) is the honest starting identity.
- **Dependencies:** D5G/D5N (PROVISIONAL), R7R1 (PROVISIONAL), Grok
  report (producer-only; every consumed table rederived or
  review-gated first).
- **Cheapest discriminator, staged:** (0) the integrating-factor identity
  — already verified; (1) attempt the derivation from the free-ring
  identity, recording the exact failure point if it breaks; (2) if a
  candidate identity emerges, verify it on the leading-edge and
  single-mode fills (exactly computable); (3) only then attempt fills
  satisfying partial lower gates (the `n=16` quadratic gate is the known
  hard step; do not launch a Gröbner search).
- **PASS meaning:** complete exclusion of both degree-eight survivors at
  the frozen raw support — the largest available GGV result; straight to
  different-model hostile review.
- **FAIL meaning:** an explicit counterexample identity/failure point —
  attempt #9 is then permanently closed, redirecting all exclusion hope
  to Card 1 gates plus the staged linear/quadratic cascade.
- **Stop condition:** one bounded effort (a working session per stage,
  three stages); stop at the first structural failure or first candidate
  identity, whichever comes first.
- **Rollback subtree:** children tag D5N/R7R1 PROVISIONAL; no other
  consumers.
- **Expected information gain:** decisive either way: proof, or permanent
  closure of the cheapest-exclusion shortcut with a named obstruction.

### Card 3 — Second-corner tower derivation (new-avenue registration test)

- **Target gap:** avenue 1's "no general corner endgame"; §4.2.
- **Dependencies:** R7R1's derivation pattern (self-contained; redo, do
  not cite); one other corner family's frozen edge data from the farm;
  the reviewed primitive-cover mechanics at endpoint scope.
- **Cheapest discriminator:** pure desk derivation of the conjugacy and
  characteristic equation for one other live corner with different
  `(m,n,a,b)`; control: specialize the general derivation back to
  `(8,12,2,3)` and require literal agreement with R7R1's
  `w_{n+22}'=-(n+2)q_n/16`.
- **PASS meaning:** register the corner-to-cover functor as a new avenue
  (uniform necessary-filter mill over the farm; endpoint filter, tower
  rows, and packet-field species for every corner).
- **FAIL meaning:** the `8_28` tower is exceptional; bank exactly which
  step used `(2,3)`-coprimality or the `mod 4` grading, which itself
  sharpens the `G2-PSC` packet design.
- **Stop condition:** one derivation plus the specialization control; no
  compute beyond desk algebra.
- **Rollback subtree:** none beyond the derivation file; consumes no
  provisional numeric artifact.
- **Expected information gain:** medium-high — converts a one-family
  instrument into a farm-wide program or localizes its magic.

No K00 card: the K00 lane's next steps (Opus5 cross-audits, the exact-Q
binary, the Fitting atlas with the parser repair) are already correctly
designed and owned; a new card there would duplicate, and my marginal
value this round is on the GGV side. This is a deliberate allocation, not
an oversight.

---

## 6. Software/AWS acceleration with evidence firewall

**One shared determinant-contribution artifact `D_0..D_35` plus a
licensing manifest and a tiny exact gate library.**

The live D5G extension, the typed R7R1 interface, any Grok-object client,
and the Card 1/2 gates all consume the same bilinear contribution records
`(F_i, G_j)`-pair, derivative side, weights, `X`-degree, rational
coefficient. Proposal: freeze the extension's output once in D5G's
existing record schema (per-weight contribution digests, as D5G already
does for `n<=22`), add a machine-readable licensing manifest (which `q_n`
each truncation level licenses, per R7R1's rule `n+22<N`), and ship a
dependency-free pure-Python `Fraction` library for the two gate
primitives: twisted-derivative-image membership
(`P in im(Q mapsto c1 Q'H + c2 Q H')` with window bounds) and rational
residue vectors. Every consumer then reads one frozen artifact instead of
regenerating convolutions three times.

**Evidence firewall:** (i) consumers must replay the per-weight digests
and the independent dense-series specialization guard (the D5G-style
deterministic full-slot specialization, extended to `t^36`, implemented
disjointly from the sparse compiler) before any use; (ii) all modular
work — Cartier shadows, prime screens of gate ranks — is labeled
discovery-only and never enters a theorem chain; (iii) gate-library
outputs become evidence only through a frozen `RESULT.json` with exact
inputs/hashes plus different-model hostile review; (iv) the artifact
carries raw contributions only — no D4R1/D5N conclusions ride along, so a
rollback of any provisional parent never contaminates the cache; (v) the
licensing manifest is data, not permission: exact-tier claims must still
cite R7R1's lifecycle state at consumption time.

---

## 7. Major-lane decisions

| Lane | Decision | Note |
|---|---|---|
| D5G direct extension `D0..D35` + typed R7R1 interface (live) | **continue, raise** | Its artifact becomes the shared cache of §6; on freeze, rows 23/24 license `q_1,q_2` at the truncated tier. |
| D5N lower-row-gate successor | **redesign (mild)** | Tower-first elimination: attack `I_<22` through the F-side gates and the unique G-completion, not head-on Gröbner over 608 generators. |
| D4R1 descendants / naturality chain | **continue** | Already correctly consumed by D5N at typed scope. |
| R6 (Opus5 cross-audit live) | **continue / await review** | On confirmation, its `(B,v)` chart is Card 1's parameter space. |
| R7R1 (different-model review live) | **continue / await review** | Not read here; all Card 1/2 consumption tagged PROVISIONAL. |
| Grok degree-8 raw-support object | **continue, review-first** | Producer-only; launch different-model hostile review before any consumer trusts its tables; §2's falsification attack 1 follows it. |
| K00 V24R6R1 cross-audit + V26R1 Fitting atlas + parser repair | **continue** | As designed; the atlas stays rollback-tagged until the Opus5 audit returns. |
| K00 long exact-Q R2/R5 jobs | **continue** | Independent cross-checks; do not retire on the fast certificate alone. |
| AS109 `n=6` content strata | **continue (narrow)** | Plus the §4.3 Cartier-shadow screen as discovery tooling. |
| TD6 H19R1 | **continue at caps** | No new information this round. |
| Matysiak SSRN acquisition/audit | **continue (bounded)** | Lawful acquisition; dependency-first audit; no reranking without primary text. |
| C5 / V43 custody | **keep banked** | No new exponent ladders; typed-transport rule stands. |
| V47/V48 strict compiler | **keep stopped** | Bank as infrastructure. |

Allocation proposal (delta from the 1349Z synthesis 28/27/17/10/12/6):

```text
30%  GGV landing / raw provenance / tower eliminant / G2 separation
25%  K00 / terminal exact incidence
15%  ramified/equality/positive-load/off-family coverage
10%  certificate custody and near-complete leaves
12%  disciplined disproof (AS109 + one formal object)
 8%  intelligence, external audit, infrastructure, synthesis
```

Rationale: the tower gates are unusually cheap per bit of information and
the K00 lane is presently review/compute-bound, not idea-bound.

---

## 8. The insight a comparably strong, differently-trained researcher most likely misses

**The exact-tier face decision decouples, and thirteen extra polynomial
rows buy an infinite tower against a 124-dimensional unknown.**

A strong researcher trained on elimination theory sees `D_1..D_21=0,
D22=1` as a bilinear system in 400 slots and reaches for Gröbner
machinery or staged sampling — precisely where the campaign's Monte Carlo
already died (`n=15`, codimension 16). The miss is a conjunction:

1. Raw support makes `E` a polynomial of `t`-degree at most 35, so a
   genuine landing satisfies the *finite* system with thirteen rows past
   the endpoint — and that finite system licenses the *infinite* tower
   `[q_n dX]=0` for all `n` (R7R1's conjugacy), bearing on the `F` slots
   alone.
2. The `G` side is then rigid, not searchable: `L_15..L_21` are injective
   (and are the same twisted-derivative species,
   `L_n=-8H^{2-gamma_n}(H^{gamma_n}·)'`, `gamma_n=(n-12)/4`
   `[desk-checked]`), so `G_15..G_21` are determined and `D22` is one
   bilinear number.
3. The tower actually bites at row one: on branch P the `q_1` gate is
   codimension exactly 3 (proof in §3.3, rank confirmed exactly for
   `A=X^4-1`), and it is *twist-invariant* — the image of
   `Q mapsto 2Q'A-3QA'` is the same subspace on `w^2=A` and `w^2=-A`, so
   no arithmetic subtlety weakens it over `Q`.
4. The complementary trap (which my own prior round's square-class work
   makes me watch for): branch-point residues on these odd differentials
   vanish for parity reasons (`dX ~ w,dw`), so a researcher computing
   residues naively at branch points concludes "no conditions" and
   discards the row; the actual cut is the infinity-residue pair plus the
   two compact de Rham classes. I made exactly this sign-parity error in
   a first scratch derivation this session and corrected it (§9).

Testable immediately: the 27-generator rank computation of §3.3
reproduces `13 = 16 - 3` for any squarefree quartic `A`, and the `q_2`
residue `-3/64` for `F_2=1`, `A=X^4-1` is a two-line check.

---

## 9. Honest ledger: leaps, failed attempts, checks performed

### Conjectural leaps (explicitly unproved)

1. The reciprocity/exclusion identity of Card 2 (`mixed_22 in (H)` given
   the lower gates) is a target, not a theorem; D4R1's `U14=c` shows
   carriers synthesize locally, so plausibility rests only on the
   complex's structure.
2. The corner-to-cover functor and the `m/a` cover-order prediction
   (Card 3) are untested beyond `(8,12,2,3)`.
3. Branch-Q genus three is my Hurwitz count, unverified; the branch-Q
   `q_1` codimension is not computed.
4. The branch-P `q_1` codimension-3 proof assumes `A` squarefree; special
   strata (`4|delta` etc.) are Card 1 work.
5. The "tower forces `F` into leading edge + modes + gauge" endgame is a
   working hypothesis, not evidence.

### Failed attempts and self-corrections this session

1. First scratch derivation claimed nonzero branch-point residues for the
   `q_1` differentials; wrong — `dX ~ w,dw` parity kills them. Corrected
   before use; the surviving functionals are the infinity pair plus two
   de Rham classes.
2. Proposed the `y -> y+c` translation gauge as a positive control for
   the `q_1` gate; computed its induced `delta F_1 = 2cH(4H+3XH')`, which
   has `X`-degree 16 and leaves the raw `F_1` window — that gauge does
   not preserve the frozen support, so the control must use a
   support-preserving direction instead. Banked as a firewall note: the
   gates are support-relative, like the whole raw-landing frame.
3. Initially misread the three delta objects as needing a merger
   *argument*; the merger is an identity chain (§3), and the honest
   residual separation is the licensing-tier firewall (§3.4).

### Checks actually performed

1. Hash verification: git HEAD; all 7 packet basis hashes; all 4 pinned
   producer hashes; Grok report hash; V24R6R1 adjudication hash — all
   match.
2. mtime screening: identified two post-seal same-day files and did not
   read them.
3. `[desk-checked]` exact computations (pure-Python `Fraction`):
   integrating-factor identity for `n=15..21` (coefficient identity plus
   random-`G` polynomial check); `q_2` residue `-3/64` at `X=1` for
   `A=X^4-1`, `F_2=1`, `F_1=0`; `q_1` gate rank `13` of `16` for
   `A=X^4-1` over the 27 candidate generators, max generator degree 15.
4. Hand algebra, double-checked in-session: `d(Qw/A^2)` formula (spot
   check at `Q=1`); primitive-space completeness (`A|Q` descent for
   higher pole orders; `Q -> AQ` absorption for lower); injectivity of
   `Q mapsto 2Q'A-3QA'`; twist-invariance of its image; `q_1=F_1/(4p^5)`
   rederivation from `F^{1/4}`; `d(vB^{3/2})` recovery of the R5 endpoint
   operator; D3 window emptiness for `F_{15+}` and `G_22` from the
   lattice inequalities; licensing bound `n+22<N`.
5. Targeted history searches (novelty gate), all excluding
   `ideation-20260827T1606Z-*` and the two post-seal files: "de Rham",
   "integrating factor", "Serre duality"/"reciprocity", "elliptic
   curve"/genus in GGV files, "packet field"/"fidelity field",
   `(n-12)/4`/`L_15`/`L_16`, tower-on-survivor phrasings. Findings
   recorded in §4 (what is KNOWN vs NEW).

### What I did not do

No AWS, no CAS, no heavy algebra, no `jc2-lean` access or status
inspection, no canonical-ledger edits, no case-directory writes, no reads
of any other `1606Z` file, no reads of the two post-seal audit/review
files, no reads of prior-round peer submissions (one caveat in §10).

---

## 10. Blindness disclosure

- I read only the round prompt and the sealed packet among `1606Z`
  artifacts, and excluded that pattern from every repository search.
- I did not read `xmodel/ggv-quarter-root-characteristic-de-rham-tower-r7r1-hostile-review-fable5-20260827.md`
  or `xmodel/ggv-degree8-r5-survivor-raw-support-analysis-hostile-audit-sol-ultra-20260827.md`;
  both postdate the packet seal by mtime and are results of lanes the
  packet declares live/out-of-snapshot.
- I did not open any individual prior-round ideator submission. One
  contamination caveat: a targeted grep for "packet field" displayed five
  fragmentary matching lines from **my own** prior-round submission
  (`ideation-20260827T1349Z-fable5.md`, card-title/rollback lines about
  the square-class packet field) alongside the R4 audit lines. The
  content was already fully covered by the sealed synthesis and the R4
  audit; no other lane's submission content was displayed beyond file
  names in `-l` listings. I treat this as a self-echo, not peer
  contamination, and disclose it for the coordinator to weigh.
- Shell use was confined to reading in-basis/pre-cutoff files, hashing,
  `stat`, searches, and the three small exact `Fraction` computations
  listed in §9.
- This file is the only artifact written.
