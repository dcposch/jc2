# Hostile cross-review charge: cutoff-four upper-endpoint field obstruction

You are Grok 4.6 in an independent hostile mathematical-review lane.  Work
in `/Users/dc/code/math/jc2`.  Audit the claimed exact result in

```text
cases/ggv_8_28_upper_endpoint_tail4_desk_20260828/
RESULT.md sha256 f5b3b6ec80648972f66b0bd057f0128bffba6682dc84d9348ded49093205ca9e
analyze_tail4.py sha256 2e4edbaded44a53eee1f4639f55617fbb94159c68be98d1c45f8b350b55d663c
TAIL4_DESK_ANALYSIS.json sha256 3a1dc7eff0598e76cf0a284e2b4bc93d6e887b1069079cb0ecc5035783221fe5
```

The producer claims that the fixed branch-P square-baseline specialization
obtained by setting every raw deformation parameter of weight below four to
zero has no characteristic-zero field-valued endpoint point.  This is a
field-radical statement, not scheme emptiness or a JC2 theorem.

Do a clean-room audit, not a plausibility summary.  Producer replay is
allowed, but reconstruct load-bearing identities with fresh exact code or
independent algebra.  No heavy local CAS; do not access `jc2-lean`.  Write
only

```text
xmodel/ggv-upper-endpoint-tail4-field-obstruction-crossreview-grok46-20260828.md
```

plus temporary files outside the repository; do not edit campaign ledgers.

Charge these points:

1. Verify source/evidence hashes and replay.  Independently reconstruct the
   cutoff-four specialization from the literal raw recurrence: census,
   prefix rank/nullity, remaining constraints, endpoint signs, and carrier
   lifts.  Detect stale specialization or row scaling.
2. Reconstruct every D8--D15 compatibility/remainder model, progressive
   substitution, degree window, and source-row provenance.  Police all eight
   characteristic-zero field-radical implications, the cumulative D14/D15
   use, scalar equations, and absence of carrier normalization.
3. Verify `E13,E14,E15`, the carrier formula, the licensed additive
   `p161=F8[X^0]` gauge, and the D18 invariant
   `v*(b*v+r^2)` with literal provenance.
4. On `v=0`, independently expand the six-generator cofactor packet to the
   literal constant `1`, checking endpoint/carrier signs and absence of
   localization.
5. On `v!=0`, independently derive
   `b=-tau^2*v`, `h=(3/8)*v*tau^2*(4*tau-1)`,
   `x=(tau^2/2)*(12*tau^2+6*tau+1)`, and
   `v=-(3/4)*tau^2*P3`, where
   `P3=(4*tau+1)*(16*tau^2+6*tau+1)`.  Verify from charged rows that the
   serialized D20 combination is exactly `tau^9*P3^3`, including staged and
   literal-source provenance.  Confirm `v!=0` implies `tau*P3!=0`, that the
   displayed roots are only the excluded boundary, and that endpoint/D21/
   D22 are not silently used.
6. Run hostile mutations of source coefficients, shifts, endpoint signs,
   `P3`, D20 cofactors, and localization.  Search actively for a missed
   field branch, denominator loss, or scope overclaim.

Return `PASS`, `REPAIR`, or `FAIL`; rank issues, state the strongest exact
theorem, and give an unambiguous promotion recommendation with enough exact
ranks/identities/mutation residuals for coordinator verification.
