# Different-model hostile review — direct nested U2 finiteness

Act as Fable 5, independently and adversarially. Work only in
`/Users/dc/code/math/jc2`. Never enter, enumerate, search, read, build,
status, modify, or control `jc2-lean`. Do not read any
`ideation-20260829T0820Z-*.md` submission. Do not use web or AWS. Do not edit
canonical files, source packets, prompts, adapters, or the target. Do not
commit or push. Short exact desk checks are allowed; no heavy local compute.

Review target:

- `xmodel/m2-u2-nested-nu1-boundary-r1-sol56-20260829.md`
- full SHA-256
  `99bbe233f8f6f7273b2e5f89705aa5f2189681ce649a8723a66a0f99fa92912b`
- body SHA-256
  `11e44767526044590b2f3ed8ceaed6b0aa3eaa98b30008e15bc725149d8b90cf`

Verify hashes first, then rebuild the claim from its hash-pinned reviewed
dependencies and the printed source grammar. The producer claims effective
finiteness for a first **direct** U2-to-U2 `nu=1` boundary with fixed base
data, via

```text
n = a[R(L+r-1)-K]/(LK) in N*,
```

and kills the only vertical resonance by the polynomial ODE lemma for `R|K`.

Charge every load-bearing point:

1. Recompute both U2 frames and distinguish frame weight, ODE slope,
   multiplicity, arrival count, and extra degree. Check the quantifiers on
   fixed `a,r,mu,R,ell` and varying positive integers `L,K`.
2. Verify that a direct edge between these precise typed vertices is case I,
   that its rule is `kbar_out=kbar_in+n/nu_in` with `nu_in=1`, and that no
   mandatory intermediate edge, correction term, or orientation reversal is
   omitted. If the source licenses several readings, state each separately.
3. Re-derive equations (E) and (D), positivity bounds for `K,n`, the unique
   `D=N=0` resonance, and every rational-denominator/integrality condition.
   Try to construct an infinite family missed by the enumeration.
4. Rebuild the lemma: for `deg P=R>=2`, `K=mR`, can
   `RPS'-KP'S=c!=0` have a degree-`K` polynomial solution? Check leading-term
   cancellation, repeated roots, constants, degree drop, and characteristic.
5. Audit the equal-weight sibling discussion and determine whether it is
   needed for the theorem or hides another unbounded parameter.
6. Check all arrival/MP2/divisibility/T1 filters and whether finiteness of
   reduced `(L,K)` pairs actually survives when `ell` ranges over its claimed
   finite divisor set.
7. State the maximum safe consequence and sharp perimeter: direct edge only,
   fixed base data only; no intervening P0 chain, recursive base control,
   full-cell finiteness, landing, degree ceiling, or JC2.

Return `PASS`, `PASS_WITH_REPAIR`, `REPAIR_REQUIRED`, or `FAIL`, with exact
clause findings and the cheapest next discriminator. Write only
`xmodel/m2-u2-nested-nu1-boundary-r1-hostile-review-fable5-20260829.md`,
including provenance and a body SHA-256 footer. Your final CLI response must
be only the concise verdict, report path, and seals.
