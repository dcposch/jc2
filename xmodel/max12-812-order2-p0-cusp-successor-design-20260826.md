# `(8,12)` order two: correction-aware `p=0` cusp successor

Date: 2026-08-26

Status: **EXACT RAW-CHART REDUCTION AND PREREGISTERED SOURCE SUCCESSOR.
THE GRADE-ELEVEN FORMULAS BELOW ARE HAND-DERIVED UNTIL THE COMPLETE-SOURCE
REPLAY PASSES.  NO CUSP, SQUARE-BRANCH, OR ORDER-TWO VERDICT.**

## 0. Exact scope

This is the disjoint successor to the eliminated odd collision sheet.  Work
after the reviewed sharp third-tail gate has forced `M=0`, at the
quadruple-root center `p=0`, on

```text
D(rs*k0).
```

Thus the leading constant direction of

```text
R=cs*z+rs/4
```

and the leading load `k0` are units.  The killed odd sheet had `rs=0` and
`cs*k0!=0`; it is not reopened here.  The boundary `k0=0` is a separate
positive-order/zero-load fan and is not silently included.

Use `sigma` with `Lambda=sigma^2`.  Every source client retains moving `p`,
all corrections in `R,A,C,k10`, the later loads `k6,k2`, and every target at
its exact absolute grade.  No reduced equation is substituted before the
complete raw rows at that grade have been formed.

## 1. The raw grade-ten cusp is already a Laurent-polynomial chart

At `p=0`, write

```text
A=a1*z+a0,
C=(c1*z+c0)/2,
R=cs*z+rs/4.
```

The complete half-weight source rows, up to nonzero rational row scalars,
are generated by

```text
E1=(15/256)k0*cs*rs^2 +(3/8)(a1*c0+a0*c1),
E2=(5/1024)k0*rs^3 +(3/8)a0*c0 +(3/32)c1^2,
E3=(3/16)c0*c1,
E4=(3/32)c0^2.                                    (1.1)
```

This raw ideal, not merely its radical, simplifies on `D(rs*k0)`.  Scaling
`E2` gives

```text
c1^2+4*a0*c0+(5/96)k0*rs^3=0.                     (1.2)
```

Multiply (1.2) by `c0` and subtract the `c0*c1` and `c0^2` rows.  The
remainder is `(5/96)k0*rs^3*c0`, so localization forces `c0=0`.  Hence the
localized raw ideal equals

```text
I10=(
 c0,
 5*k0*rs^3+96*c1^2,
 15*k0*cs*rs^2+96*a0*c1
).                                                  (1.3)
```

In particular (1.2) makes `c1` a unit on this chart.  There is no retained
nilpotent or conductor debt away from `rs=0`.

A denominator-minimal rational parameterization is

```text
rs =-(96/5)*k*tau^2,
c1 = (96/5)*k^2*tau^3,
a0 = k*tau*d,
cs =-d/3,
k0 = k,
k*tau != 0.                                        (1.4)
```

Its inverse on (1.3) is

```text
tau=-c1/(k0*rs),       d=-3*cs=a0/(k0*tau).         (1.5)
```

Thus (1.4)--(1.5) give an actual isomorphism of the localized raw quotient
with a Laurent-polynomial ring in `k,tau` and the remaining free source
coordinates.  This is stronger and cleaner than taking a radical and then
asking a normalization engine to rediscover the standard cusp parameter.

## 2. Complete first successor receiver

Before source formation put only

```text
L=z^2,
D0=L*A0+C0,
A0=a1*z+a0,                 C0=(c1*z+c0)/2,
R0=cs*z+rs/4,

p=2*sigma*ell1+O(sigma^2),
A=A0+sigma*A1+O(sigma^2),   A1=aa1*z+aa0,
C=C0+sigma*C1+O(sigma^2),   C1=(e1*z+e0)/2,
R=R0+sigma*R1+O(sigma^2),   R1=cs1*z+rs1/4,
k10=k0+sigma*k1+O(sigma^2).
```

Expanding the full unloaded and `k10` terms one grade beyond the reviewed
half-weight row predicts the negative Laurent receiver

```text
H11 = [
   (3/4)*D0*(L*A1+C1)/L^2
  -(3/4)*ell1*D0*C0/L^3
  +(5/8)*k0*D0*R0/L
  +(5/16)*k1*R0^3/L
  +(15/16)*k0*R0^2*R1/L
  -(5/16)*k0*ell1*R0^3/L^2
]_- .                                               (2.1)
```

The `(5/8)` term is mandatory: it is the first nonpolynomial coefficient of
the charged cross term `sigma^9*k10*D*K^(1/2)`.  The last three terms retain
the moving load, moving `R`, and moving double-root center.  Later loads and
targets cannot occur at grade eleven, but the complete-source compiler must
verify their absence rather than delete them from the registered source.

If `h10[j]` and `h11[j]` are the coefficients of `z^-j`, the moving
ordinary/Faber coordinates obey the lower-unitriangular connection

```text
g11[ell]=h11[ell]+((ell-2)/2)*ell1*h10[ell-2]
                                                    (ell>=3),             (2.2)
```

with the second term zero for `ell<3`.  Since the full grade-ten ideal is
retained, (2.2) may be reduced only after both sides have been formed.  On
the exact normalized chart (1.4), every `h10` coefficient vanishes
identically, so the new source rows equal the coefficients of (2.1).

## 3. Predicted grade-eleven triangular quotient

Substitute (1.4) only after the raw grade-ten and grade-eleven rows exist.
The four negative Laurent coefficients of (2.1) are triangular.  In order,

```text
[z^-4]H11 = -(3/32)*ell1*c1^2
           = -(864/25)*k^4*tau^6*ell1,

[z^-3]H11 mod (ell1)
           = (3/16)*c1*e0
           = (18/5)*k^2*tau^3*e0.                  (3.1)
```

The next two rows solve uniquely

```text
e1 = (48/5)*k*k1*tau^3 -(3/2)*k*tau*rs1,

aa0 = (1/2)*k1*tau*d -(5/192)*d*rs1/tau
      +4*k^2*tau^2-3*k*tau*cs1.                   (3.2)
```

Every pivot coefficient is a unit on `D(k*tau)`.  Therefore the prediction
is that grade eleven is a unique horizontal prolongation, not an obstruction:

```text
ell1=e0=0,
```

with `e1,aa0` given by (3.2), while `a1,aa1,cs1,rs1,k1` and all later
corrections remain free.  The exact-Q client must check (2.1)--(2.2), the
four pivots, and both ideal containments in the Laurent chart.  A standard
basis of a prematurely radicalized or zero-correction slice is not an
accepted endpoint.

## 4. Horizontal continuation and first possible new actors

Form grades twelve and later from the complete source before substituting
the grade-eleven pivots.  At grade twelve retain at least

```text
p=2*sigma*ell1+2*sigma^2*ell2+...,
R=R0+sigma*R1+sigma^2*R2+...,
A=A0+sigma*A1+sigma^2*A2+...,
C=C0+sigma*C1+sigma^2*C2+...,
k10=k+sigma*k1+sigma^2*k2c+....
```

The fourth binomial of `K^(5/2)`, proportional to `k*R0^4/L^3`, first
appears at grade twelve and must be kept together with the second moving
center, all quadratic first corrections, and the second `D^2/K` variation.
The first potentially nonpolynomial `k6` term occurs at absolute grade
sixteen; `k2` first occurs later, and the terminal targets first occur at
grades 28, 32, 36, and 38.  These are source-typing bounds, not permission
to remove the corresponding series from the compiler.

The preferred continuation is incremental exact collection:

1. freeze all seven raw rows at each absolute grade;
2. append the preceding raw ideal without solving future variables;
3. only then use the unit triangular pivots already certified on
   `D(k*tau)`;
4. stop immediately at a raw Laurent unit, or freeze the exact normalized
   survivor before advancing one grade.

This horizontal procedure avoids a monolithic radical and preserves a
possible formal branch as readily as it detects an obstruction.

## 5. Chart routing, terminal scope, and Gate A

The reviewed nonsquare-discriminant K3 theorem cannot be imported at the
central cusp: its license requires a proved source-row transform to
`(kappa,e,U^2,F)` on `D(b*m)`, whereas the quadruple center has separation
coordinate `b=0`.  A later root-separation overlap may cross-route only after
the complete moving-`p` source proves that row transform and `b*m!=0`.
Positive-order `p` or an analogy between cusp parameters is insufficient.

If the cusp survives the early horizontal grades, the terminal `[6,2]`
infinity client may continue the same complete coefficient source directly;
it does not need a sheet-to-global map.  Finite Taylor pullbacks do require a
fresh cusp-to-global algebraization/overlap gate.  The retired odd-sheet Gate
A and its typed Taylor payloads neither prove nor disprove that new gate.

Zero/infinity receivers are also distinct: a terminal unit on this exact
infinity chart kills the cusp; a nonunit receiver must be normalized and
continued.  No terminal or Taylor symbol may be projected from (1.4) by
analogy alone.

## 6. Minimal AWS clients and stop conditions

1. **Cusp grade 10/11, exact Q:** reconstruct all seven canonical source
   rows from the complete primitive coefficients; verify absolute grading,
   raw ideal equality (1.3), normalization and inverse (1.4)--(1.5), analytic
   receiver (2.1), moving-Faber connection (2.2), and triangular formulas
   (3.1)--(3.2).
2. **Independent control:** replay the identical frozen source over
   `F_65521`; exact Q alone carries the characteristic-zero theorem.  Check
   that `2,3,5,rs,k,tau` and every pivot scalar remain nonzero.
3. **Grades 12--16:** as soon as the exact-Q 10/11 source identities pass,
   launch an incremental exact-Q collector and an independent good-prime
   control without waiting for review.  Keep the raw predecessor, all second
   corrections, the fourth `k10` binomial, and the first `k6` face.
4. **Terminal continuation:** if grades 12--16 leave a component, emit all
   seven source rows through grade 38 with the exact target contract, then
   solve incrementally.  Taylor emitters may stop only at
   `TYPED_WAITING_CUSP_GATE_A` until their new global overlap is proved.

A positive endpoint is one of: a literal raw Laurent unit; an exact
triangular Laurent-polynomial quotient; a frozen normalized component; or a
licensed `D(b*m)` row-transform into the reviewed nonsquare K3 theorem.  A
timeout, a modular-only unit, a radical without the raw ideal, a missing
correction/load, or an unproved chart overlap is `NO_VERDICT`.

## 7. Explicit nonclaims

This note does not yet promote (2.1)--(3.2) as complete-source identities.
It does not treat `k0=0`, the killed odd sheet, fractional or ramified
base/load slopes, a root-separation overlap, any other infinity support, or
either finite Taylor family.  It does not construct or exclude a strict
cusp arc, close the whole square component, close order two or `(8,12)`,
prove maximum twelve, or prove JC2.
