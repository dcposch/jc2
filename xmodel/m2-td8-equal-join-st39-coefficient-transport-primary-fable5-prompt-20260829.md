# Fable 5 primary research — td=8 equal-join Statement 3.9 coefficient transport

Work independently from `/Users/dc/code/math/jc2` and write exactly:

`xmodel/m2-td8-equal-join-st39-coefficient-transport-primary-fable5-20260829.md`

This is primary mathematics, not review. Inputs allowed: the reviewed D2
route producer/review, your passed local route ODE review
`xmodel/m2-td8-equal-join-prop81iv-route-hostile-review-fable5-20260829.md`,
the exact printed Statement 3.9 and all definitions/statements needed to
transport leading coefficients through `F*c`, plus directly relevant
promoted ladder corrections. Do not read active Grok/Opus transport, lambda,
semilinear-review, or ideation output.

For the full displayed tree—two identical poles, their rigid `(21,15)`
A-steps, the affine equal join, rigid `(85,35)` trunk, and terminal—derive the
actual cross-vertex coefficient system, not only degrees and local ODEs. The
local ratios are established provisional inputs:

- each incoming `(21,15)` pattern forces its two orbit values in ratio `3/2`;
- the merge forces its two arriving orbit bases to be opposite;
- the trunk `(85,35)` pattern forces its two orbit values in ratio `4/3`.

Pin a common suitable `kappa`, every local `eta` change, all powers of the
Prop. 8.1 exponent `i`, and Statement 3.9(ii)'s equality of the parent root's
leading coefficient with the child's top coefficient. Track independently
the two incoming gauges, pole coefficients, branch/root choices, deck actions,
and all allowed constant rescalings. Determine whether the local gauges can
be chosen simultaneously for every `t>=0`, only a finite residue/exceptional
set, or never.

Seek in order:

1. an exact incompatibility (including a root-of-unity or exponent-parity
   obstruction) that kills the entire affine route;
2. an explicit compatible formal coefficient system, which promotes the
   route from independent local survival to Statement-3.9 transport survival;
3. if printed data do not determine the needed map, the smallest missing
   source coefficient/jet and a finite typed symbolic discriminator.

Do not confuse transport survival with source landing or realization by a
single polynomial pair. Test whether the merge's two opposite values are
attached to the correct multiplicity-3 incoming orbits, and whether the trunk
ratio consumes the merge scale or introduces a genuinely new gauge. Include
clean-room hand algebra and a small exact standard-library packet if useful.

Return `ROUTE_KILLED_BY_ST39`, `ST39_TRANSPORT_SURVIVES`, or `PARTIAL`, with
the narrowest maximum consequence, exact next lemma, full/body hashes, and
replay commands/counts. No AWS is authorized.

Hard boundaries: no web, AWS, commit, push, canonical edits, heavy CAS, or
local long/high-memory process. Never access/list/search/build/status or
control `jc2-lean`; never run global `git status` or workspace-wide searches.
Modify only the requested report and a uniquely named packet under `cases/`
if needed; use `/tmp` for scratch.
