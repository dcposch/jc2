# Hybrid81: two tiny actual-engine controls, preparation only

2026-09-07. Author `/root/model_productivity`. **LOCAL PREP ONLY: no AWS/SSH,
deployment, production-source access, CAS, authority or engine result.** A73-line
fixed-operation helper is prepared in
`box/d125-hybrid81-engine-prep-20260907/` (below B). It adds no runner/controller
framework and changes none of the frozen caller/adapter/checker/fixture files.
The live Sol caller-delta gate was not read; this new helper requires its own
scoped different-model review before actual execution.

## Fixed helper and boundaries

B/engine.py SHA:
`a87debfce5ca01817c03c2b180fd514d8e45269b1173c6f4a562255883188ae5`.
Only operations `hybrid` and `alarm` exist. Before output, the helper validates
the literal operation and exact future helper/cwd/authority paths, calls repaired
`D.context(...,'control')`, and checks a strictly matching engineering extension
binding operation, helper SHA, fixed tiny-fixture SHA and physical ROOT-GREEN SHA.
Solver mode and all other operations are rejected. Root's physical engineering
GREEN must explicitly pin these bytes; no token or GREEN is created here.

Hard code pins are repaired driver
`ce599a26a0ed051eddd71125f492bb0b19c4ef86cdf97a6bc887f5cf47cade5a`,
exact.py `7ca2b24e…`, hybrid.py `c9755a71…`; accepted CAPRUN `4435279d…` is reused.
The helper requires its exact CAPRUN-parent argv and own PID=PGID, calls
`D.arm_deadline` and `D.limits('control')`, then execs Singular in place.
It never forks, creates a session or opens D.SOURCE. Identity includes current
boot/host, PID/PGID/start_ticks, namespace/cgroup, authority/helper pins and actual
remaining timer. `source_access:false` specifically means no production source;
the pinned tiny fixture is intentionally read by the hybrid control.

Future exact cwd is `/home/ubuntu/d125-hybrid81-exact-solver-20260907`, helper
`engine.py` at its root. Output pairs are exclusively
`engineering/hybrid.{sing,identity.json}` and
`engineering/alarm.{sing,identity.json}`. Root's existing controller/CAPRUN owns
the distinct stdout/stderr/telemetry/launch/receipt paths and prechecks all of
them with lexists. Neither helper nor plan overwrites an existing path.

## Control1: genuinely new hybrid syntax and I/T interface

Reuse Sol's1698-byte tiny JSONL in place, SHA
`d00df83bafddcd3436dad44034bdb1680d306f6d67f8bfa83d4b32de19961bbf`.
The helper validates that pin, calls H.read_hybrid in tiny mode, and appends
the unchanged **control=True** footer. It emits all seven rational/zero/duplicate/
inverse-guard slots and direct lift to1, with no slimgb/std/dimension command.
The exact explicit `1+-1*x` form is deliberately retained for actual-engine
validation; local reparse alone is not claimed to establish Singular acceptance.

Sol's saved `.sing` uses the production footer containing slimgb. It is not an
engineering input and is never executed here. No duplicate JSONL fixture or
changed source system is introduced.

Later actual success requires normal0, literally empty stderr, complete fixture/
Q-dp-ring-bound JC2CERT1 blocks, seven engine and cofactor columns, correct I_SIZE
nonzero count4, map `[0,1,0,1,4,0,6]` and exact recomposition over all seven slots.
`check_hybrid` verifies these obligations with the unchanged exact checker and
has a64KiB tiny stdout bound. Normal/-O replay of actual SMALL output is planned,
not presently executed. This tests the new adapter route, not merely the old
generic D.control_input fixture.

## Control2: pinned Singular alarm interaction

The fixed107-byte alarm program creates one tiny ring/int, prints
`JC2_HYBRID81_ALARM_READY`, then toggles an integer forever. It neither computes
a basis nor reads/expands any source equations. The repaired D.arm_deadline is
called with `min(context_duration,1.0)` for this operation, arming fresh after
context and before output/input preparation. It does not modify D.CAPS or reset
the production timer. This is a future test of actual Singular startup/alarm
behavior, not a claim that the earlier Python exec probe proved that behavior.

An external unchanged CAPRUN at most10wall/CPU seconds and512MiB sampledRSS
fails safe if the engine resets/ignores its inherited alarm. Payload limits are
512MiB AS,64MiB FSIZE per regular file/core0; root's prlimit also covers runner
streams. Alarm success requires the ready marker followed by actual SIGALRM14
termination around the registered at-most1s timer, with matching identity/caps.
External timeout, parse error, rc0, missing marker or another signal is not a
pass; retain the precise GAP/failure and stop. No retry or timer enlargement.

## Disabled registration, checks and custody

B/REGISTRATION.md SHA
`364a5d8c57a967d6dc3467db13eb0b19ba6c7924a573dc5b3ea67da7eb6f1ee5`
contains the exact expanded CAPRUN command template, two distinct authority
paths/schema extension, expected terminal outcomes and harvest boundaries.
It does not replace the frozen solver registration `38bc496f…` or authorize
deployment. No automatic continuation or gate combination is assumed.

Fresh Owner/DMI/boot/hostname/EBS/process/resources and exclusive-path checks
remain root/operator duties. Only retained i-0da0cebfc97c9fd54/EBS
vol-0eb6450d18ffa89f1 may later be used. Root alone STARTs/STOPs; never terminate,
delete, retag or displace another owner. The accepted FSIZE/descendant/long-print
batch is reused, not repeated. Full source and construction files stay untouched
on EBS. Actual control arithmetic must await separate root engineering GREEN.

Six tiny mock/stdlib methods passed normally and optimized in0.533628s combined
wall,0.535849 child CPU and29620KiB peak RSS, under30wall/25CPU/512MiB. They test
operation/mode/helper/fixture/GREEN binding rejects; byte-derived control footer;
complete synthetic I/T replay and corrupted cofactor/index/stderr/missing-guard
rejects; alarm text only; and actual local entry rejection before context/writes.
AST checks prohibit removable assertions, production source calls and new process/
session/timer machinery. These are not fabricated live engine controls.

B/test_engine.py SHA
`6bbd6499957e05ad02d180b9e2d61d80bbfe2666a68582f857f7d8569781818f`;
B/test-results.json SHA
`3e89d4a8e43624c60ab810946fba110b9fd657dc91feacff13f0acf71885ab9c`.
Replay from B: `python3 [-O] -B -m unittest -v test_engine`.
All four artifact pins and exact dependencies are in B/custody.json SHA
`d4d57ce782a4c4873a03fa97f8467f94468943700c6c0f2a6fd802fcedec017c`.
Original caller/exact/adapter/fixture hashes were rechecked unchanged after tests.
All children and writers are terminal. **SEALED / STOP / IDLE.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6659`.
- Body SHA-256:
  `9e2aff71d37b69e5511f1c25b1ca305e3762d80f1f9e961bde548e227946381a`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
