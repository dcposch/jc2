# Hostile review charge: D1 weighted triple-root two-chart V10

Act as a hostile algebraic-geometry, tropical-weight, and source-custody
referee.  This is a text-only review: do not run local Singular, Sage,
msolve, Lean, algebra compilers, or substantive Python.  You may recompute
hashes, compare files, inspect JSON with lightweight tools, and check the
displayed rational, polynomial, and valuation identities by hand.

Read every file in

```text
cases/max12_912_order3_d1_triple_root_weighted_two_chart_v10_20260826/
```

and every source named by `SOURCE_CLOSURE.sha256` and `FREEZE.sha256`.
Read the complete V9 and V8 producer/review ancestry and the pinned
squarefree common-cubic order-20 theorem and hostile review.  Pin:

```text
V10 RESULT   3d6300ced43c4d358f292538c2247655a7ad4f6f0fa674ada4cb27ed3cddaf03
V10 FREEZE   12ce6fe5b0b98666a62c283fd9ab3d5e84dad26041f85da5525a9cee941f1666
V10 CLOSURE  a4f5b5fa169115d586f4ed3cbe4fe8c6e1c2b78630f0e5e1f9645d44d859bbba
V9 RESULT    a061aea2bab3abcd9a8e88a3e86f18de233f46515cd2965bafa79ff1516642e2
V9 review    f81c5b054c2ff331176bd39005ad8f826b92efbfd5e16b1f7f20ae7dcbab7226
squarefree   1b6e629affdc5a41c5abb7e06d63c8dba026bcc46df0a6f21bfed724589189c5
sqfree review 6883ef76710176ae9e091249224730e3ff181fa76c66c767b53b26c770c101d5
```

Charge every point below.

1. Recompute freeze/source/result hashes and audit registration, AWS identity,
   pre-GO state, exact tags, caps, PIDs, rc, stderr, PASS uniqueness, source
   equality, and forward/reverse independence.  Verify that deleting only
   `tag` and `order` gives identical sorted certificates of SHA
   `25910f14424f0a021b00bf3d440b506b05aafc5c0980ee94beefe58608d5e12b`.
   Charge the preserved PID-capture erratum and AppleDouble sidecars: decide
   whether either affected source identity, pre-GO custody, or execution.
2. Audit literal reconstruction of the reviewed 48-term V9 universal witness,
   SHA `53cf4b65...`, from the full V8--V1 ancestry and charged ordinary tails.
   With `wt(a)=1,wt(h)=3`, verify the exact axis-coefficient formula on every
   term, the clearing power three, and the normalization
   `ell=wt(Lambda)-wt(a)>0`.  Check that all axis coefficients vanish, every
   corner margin is nonnegative, the margin histogram is `32,11,5` at
   `0,5/2,5`, and precisely 30 zero-margin non-target terms have a positive
   open `u/v/eta/T/H` slope.  Do not infer positivity merely from Python
   truthiness without checking that the slopes are nonnegative exponents.
3. In the a-chart `h=a^3*s`, independently verify the residual depressed
   cubic, discriminant `-27*s*(s+4)`, digest/certificate transcription, and
   double-root values `s=0,-4`.  Charge whether this proves the stated whole
   finite-axis cone for the literal fixed-load V8 witness rather than one h
   grade, and identify every hypothesis used from V8/V9.
4. In the h-chart `h=t^3,a=t*b`, verify the residual cubic
   `z^3-3b^2 z+(2b^3+1)` and discriminant `-27*(4b^3+1)`.  Audit from the
   frozen charged source that row i has ordinary weight `12+i`, including
   target weights 15, 18, 20.  Most importantly, decide whether this scaling
   covariance is sufficient, with positive rescaled Lambda and the exact
   ordinary-source hypotheses, to invoke the reviewed squarefree order-20
   theorem.  Look for a hidden moving-load, tau, field-extension, valuation,
   source-chart, or inverse-substitution mismatch.
5. Check the overlap: on `4*b^3+1=0`, show that `s=b^(-3)=-4`, and verify the
   opposite-axis coordinate change `a'=-a,h'=h+4a^3` preserves both `p` and
   `c`.  Charge whether the two weighted charts really cover every claimed
   noncentral directional case and whether either `s=0`, `s=-4`, or the
   cubic-root base extension leaves an unacknowledged gap.
6. Separate exact identities from the mathematical consequence.  State the
   strongest theorem actually licensed, the smallest missing hypothesis for
   any stronger theorem, and the smallest failing identity if any.  Preserve
   the firewall: frozen ordinary common-cubic source, fixed V8 loads,
   positive rescaled Lambda, reviewed ancestry; no central `a=h=0`
   identically, nonpositive rescaled Lambda, moving V8 loads, other source
   chart, global landing/accessibility, D1, or JC2 conclusion.

Produce exactly one review file at

```text
xmodel/max12-912-order3-d1-triple-root-weighted-two-chart-v10-review-grok-20260826.md
```

and write no other file.  Include overall verdict, a charge-by-charge audit,
smallest failing identity, smallest missing hypothesis for a stronger
theorem, evidence tier, exact promotion text if confirmed, and explicit
firewall.  End with exactly one token:
`WEIGHTED_TWO_CHART_V10_CONFIRMED`, `REPAIR_REQUIRED`, or `INCONCLUSIVE`.

