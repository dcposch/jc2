You are GPT-5.6 Sol, co-researcher on the plane-Jacobian-Conjecture campaign.
Repo: /Users/dc/code/math/jc72108. Char 0. This is a CHEAP, decisive truth test
for CONJECTURE DIR(C) (the G5 crux), the test named in xmodel/sol-truth.md §3.
Write to xmodel/sol-dircensus.md.

READ: xmodel/sol-truth.md §1 (the DIR ratio R_P and its EXACT structure),
xmodel/sol-bridge2.md (the different/contact ledger, Delta_gamma = pole order of
g), xmodel/sol-pc.md §0 (DIR statement). Accept as EXACT/PROVED.

EXACT SETUP. At a proper boundary root P of H (F_d = xi H^alpha, G_e = eta H^beta,
deg H = B, root mult mu_P), the DIR ratio is
    R_P = B * Delta_P / (alpha beta mu_P),   Delta_P = sum_{gamma|P, pole} p_gamma,
where p_gamma = pole order of g on branch gamma. DIR(C) <=> R_P <= C for all
Keller-admissible roots. Known points: filed two-pole residue-A root R_P = 4/3
(equality); Henon control R_P = 1/2; non-Keller class-kill R_P = B^2 - B/beta
-> inf (sanity divergence, killed by the pure-minor identity). High mu_P LOWERS
R_P; the dangerous axis is mu_P/B -> 0 with Delta_P >= 1.

TASK -- a rootwise CENSUS. Enumerate the campaign's filed admissible inventories
(the td<=12 books, the residue-A template, every banked (alpha,beta) type and
pole configuration you can extract from AUDIT.md / SHEET6*/ the ladder entries).
For each, record (B, alpha, beta, mu_P, sum_gamma p_gamma) and compute R_P.

  Q1. Across ALL filed Keller-admissible roots, what is the MAX observed R_P?
      Is it 4/3, or does any banked configuration exceed it? Tabulate.
  Q2. Can you construct (even formally / book-relative) a Keller-admissible root
      with mu_P/B -> 0 and Delta_P >= 1 pushing R_P above any fixed C? The
      constraint: it must be pole-order data of an actual constant-Jacobian
      germ (so p_gamma tied to the pole of g via the ledger), NOT the non-Keller
      class-kill mechanism. Does the pure-minor identity CAP sum p_gamma per
      root in terms of mu_P (which would bound R_P and prove DIR)?
  Q3. VERDICT: does the census support DIR(C) with a specific finite C (give it),
      leave it neutral, or expose a Keller-admissible growth direction (= a DIR
      counterexample lead, name it precisely)?

Be quantitative: produce the actual table of (B,alpha,beta,mu_P,sum p,R_P) rows.
Label each row's tier (KELLER CONTROL / BOOK-RELATIVE / FORMAL). If you can bound
sum_gamma p_gamma <= (something)*mu_P from the ledger + Keller identity, that is
a DIR proof sketch -- state it as CONJECTURE with the missing step. Honest tiers.
Terse, technical.
