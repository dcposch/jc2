# Furter primary-proof/interface gate: preparation only

2026-09-07. **READY, NOT LAUNCHED.** One independent Fable review prompt is
frozen in `box/furter-primary-gate-prep-20260907/review.prompt.md`. It has
eleven explicit `charged_input=` lines, unique basenames, and the literal
`{{LANE_INPUTS}}` staging token. Root retains review launch and acceptance.
No new theorem or JC2 implication is promoted by this preparation.

## Custody and reading

Before any packet preparation, the prior sweep artifact transaction and all
194 entries of `box/websweep-20260907T0735Z/custody.json` matched. The final
metadata-only freeze rechecked all194 pins, both relevant artifact
transactions, every copied file and the prompt's exact charged set. No drift
was found and no frozen source was edited. The successful author PDF is
charged, not the empty failed mirror fetch.

I reread the entire producer Typst proof, including all eight sections and
corollaries; Furter's original introduction, R(m,n)/R(m), ambient E versus G,
polydegree/partial-order definitions, exact Theorem B and adjacent remarks,
and Lemma11/Corollary12 with their proofs. The original PDF and its convenience
text extraction are both charged; its complete Theorem B proof was not
independently reconstructed. The future gate may retain that published theorem
as an explicit external dependency. It must compare the producer PDF with
the whole Typst proof rather than assume semantic agreement from common
repository provenance.

The complete frozen07:31 APPROACHES and terminal torsor report were reread.
The additional terminal full14c unit-normalization report was read wholly,
transaction-verified and copied unchanged. This supplies the actual guarded
coefficient ring and rank-six faithfully flat cover, not only a strategy
summary. Its presence does not turn the sufficient odd source into an
exhaustive JC2 normalization. The earlier strategy snapshot is explicitly
dated rather than represented as the latest live state.

## Exact review discriminator

The prompt separately gates the attracting-full-basin proof, distinct finite
critical-value count, scalar perturbation/branch construction/exact multiplier
identity, composition bound, R(m,n) indexing and characteristic-zero extension.
Secondary finite-flat/rank, sharpness and length-two polydegree consequences
receive separate verdicts; an optional parabolic-petal remark cannot silently
stand in for the main proof. Restricted Strong Factorial is distinguished from
the full conjecture and its uncharged external equivalence.

Actual-source attachment is a separate question. The reviewer must produce a
ring/place/coordinate map with all hypotheses, or identify the first missing
arrow. Neither boundary degrees15/25 nor closure inside Aut(A2) identifies the
guarded source with near-inverse univariate polynomials or an automorphism
family. For those two univariate degrees, contact at least40 would be needed
to contradict the claimed bound39; no such source identity is supplied here.
The full14c guard, moving faces and all polynomiality rows remain mandatory.
The torsor's open immersion into a finite normalization is not already
surjective. GAP on one proposed attachment is not a no-go for all future
applications of a valid theorem.

No mathematical computation was useful for preparing this review and none was
run. The only execution was bounded metadata hashing/transaction validation
(normal exit0, about0.04seconds in the final pass); no CAS, AWS/SSH, external
fetch/write, lane launch, live ideation read, protected-tree inspection or
shared-ledger edit occurred. A future reviewer may use only genuinely useful
degree-at-most5 standard-library controls under the stated tiny caps.

## Frozen handoff

- Prompt SHA256: `f6bbc0a0e62ac07e926539124cf89cca8c586bddea2375c5a76f044ecb6e7f62`.
- `input-pins.json` SHA256: `3efdcac76996fe011489bf509697ee05eb36610fd2d7891eee58bff57a1a207e`.
- `custody.json` SHA256: `8d58667ba4ceb9c3d4601acd6982e2c46db5f04ae990502f36cec755bc749aec`.
- Producer commit: `b17b6b9f7440b12fd85df2c2db9c98209ecc174d`.
- Producer Typst SHA256: `8a6e152c797c227529e88572db140fcfb5b6c3ad61650c2a94fc6304cb334af6`.
- Producer PDF SHA256: `0e9662c12bba4204c12ce6e1a2140687d138d33a520f771320f87129879f445d`.
- Original Furter author PDF SHA256: `957fbaa09bf2158217905b5d43b313d97401f43be3ee362fd112de1e62186a7f`.

`verification.json` retains both transaction receipts and the194-pin check;
`input-pins.json` maps each copied mathematical input to its immutable original.
The fourteen owned evidence files are pinned by custody. All subprocesses are
terminal. **STOP / IDLE: packet frozen; no launch or follow-on authority.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `4704`.
- Body SHA-256:
  `7dd7bd5ee24edbc41e194773117c279211f117a6291e50688734e634e0bde70e`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
