# Significant-news whole-portfolio ideation — order-two owner

Date: 2026-08-26 07:40Z

Blindness: this packet was written without reading any sibling packet from
this event round.

News consumed:

- the entire exact-order-four leaf is eliminated;
- the order-two first-normal support is square union discriminant;
- the nonsquare discriminant component is now source-exact and
  review-confirmed empty at its unique normalized K3 ray;
- D1 has a full fixed-chart `q2` lift and weighted triple-root clients, but
  global/cone coverage remains the firewall;
- TD6 has a rank-nine first map with a 24-dimensional kernel and a live
  narrow K2 gate;
- the terminal extremal-abc / weighted-tree frontend is finite at fixed U;
- all substantive computation remains AWS-only.

I rescanned all 46 canonical approaches.  The new evidence materially
changes rows 1--4 and creates useful bridges to rows 25, 31, 38, and 46.  It
does not revive the refuted/structurally blocked rows 5--24, 26--30, 32--37,
or 39--45 except through the explicit connections below.

## Ranked new connections and experiments

### 1. Projectivized Veronese normal ray for the remaining square component

The generic-square grade-four raw ideal reported by the source ladder is

```text
(v1^2,v0*v1,v0^2)=(v0,v1)^2.
```

This is the two-variable analogue of the doubled normal `U^2` that made the
discriminant chart look deceptively nonreduced.  Its normalized blowup is
the ordinary blowup of `(v0,v1)`, with exceptional direction
`[v0:v1] in P1`; complementary normals have weight two.  Therefore the
correct square successor is not the grade-five zero-correction slice.  It
is a two-chart normalized-ray client retaining the higher `M1*S2/L` term,
all complementary corrections, and `k10*R^3/L` at the same weight.

Cheapest falsifier: compile only the last three transformed source rows on
the charts `v0=1` and `v1=1`, with the overlap identity checked before any
unit computation.  If their resultants vanish identically, the ray survives
and the monolithic square ladder should stay deprioritized.  If they are
unit, the remaining generic square component dies by exactly the mechanism
that just killed the discriminant component.

Expected proof-discriminating value: **highest**.  Allocate the main
order-two producer lane here immediately.

### 2. A generic normalized-Rees fan compiler for every local campaign leaf

The order-two success suggests a reusable proof engine:

1. source-match the first obstruction ideal exactly;
2. retain its raw nonreduced scheme;
3. compute primitive Rees valuations / Newton fan rays relative to the
   deformation parameter;
4. compile one correction-aware source client per ray and every face;
5. prove overlap/exhaustiveness before consuming unit endpoints.

This joins the productive parts of rows 1 (Newton faces), 2 (boundary
trees), 4 (formal germs), 31 (Rees valuations), and 38 (tropical geometry)
without making the invalid global claim that a tropical prevariety equals
the source.  It directly targets the recurring coverage failures in D1 and
TD6.

Cheapest falsifier: feed the compiler the known order-two ideal
`(kappa,e,U^2,F)` and the D1 V10 weighted triple-root raw ideal.  It must
return exactly the reviewed `(1,2,2,2)` ray in the first case and both known
D1 charts in the second.  A missing known chart rejects the compiler before
new mathematics is trusted.

Expected value: **very high software multiplier**.  One engineer/research
agent should build this while producers continue by hand.

### 3. Replace source-ideal Groebner comparisons by algebraic row transforms

The exact K2 source promotion that timed out for an hour became seven
instant polynomial identities after writing

```text
q^4+b*v*q^3+e*v^2*q^2=1,
A^-j=v^j*q^-j.
```

This is not special to the discriminant point.  Every fixed terminal Faber
system has an algebraic equation for the local coordinate `q`; its Faber
rows and principal-part rows differ by a finite unitriangular matrix.
Generate that matrix once, hash it, and use coefficientwise identities as
the source-promotion gate.  This can remove standard-basis bottlenecks in
D1, TD6, the square client, and the order-one fixed-load curves.

Cheapest separator: replay the D1 V8/B exact rows and the TD6 current first
map through generated transforms.  Demand byte-level identities before
replacing any existing reviewed ideal endpoint.

Expected value: **high**, because it simultaneously improves speed,
characteristic-zero rigor, and Lean formalizability (row 46).

### 4. Deck-character descent may kill the square chart before its next jet

For a depressed quartic, `K=L^2` forces `L=z^2+s`; hence the square leading
quartic is fixed by the order-two deck involution.  The first-normal theorem
then kills the generic `M` direction.  What remains is a sharply typed
character problem: the two square-normal coordinates, the nonzero `k10`
load, and the terminal `r7` must occupy prescribed deck characters.  A
correction direction of the wrong character cannot pair with the invariant
`L` to create the required anti-invariant terminal derivative.

Cheapest falsifier: extract the deck character of every variable in the
square grade-four source (including the two normalized-ray coordinates) and
write the character table of the first nonzero tail and `r7'`.  If both
exceptional P1 charts contain a character-compatible monomial, this hand
route gives no kill; otherwise it eliminates that chart without elimination
algebra.  Do this before a larger square Kuranishi computation.

Expected value: **medium-high hand theorem**, cheap enough to run in
parallel with item 1.

### 5. Couple terminal weighted trees to local multiplicity strata, not to
raw radicand residues

The earlier attempted comparison with the lemniscatic `{1,1,2}` signature
failed because it compared different covers.  A licensed coupling is
instead: terminal weighted tree -> source Kummer cover -> deck characters
of the coefficient map -> allowed first-normal quartic multiplicity type.
The square/discriminant dichotomy is invariant source data and can be
attached to each finite U=3/U=4 terminal tree without identifying the
source cover with a residual coefficient cover.

Cheapest falsifier: on the three reviewed U=3 order-two terminal clients,
compute only the deck-character/multiplicity compatibility table.  If every
tree admits both square and discriminant types, stop.  If some types vanish,
the 5/44-style terminal census becomes a genuine lower-tail prefilter.

Expected value: **medium**, but it scales the current U=2 proof to all fixed
U and links rows 2 and 25 cleanly.

### 6. Test whether the D1 and TD6 “24” kernels are the same symbol complex

D1's full projected `hQ` solve has 24 columns, while TD6's first-stage map
has a 24-dimensional kernel.  This may be coincidence, but both arise after
removing Faber-coordinate choices and taking a first boundary deformation.
The direct row-transform result suggests comparing their multigraded symbol
complexes rather than their coordinate matrices.

Cheapest falsifier: compute the exact multigraded Hilbert character of the
D1 V8 correction module and the TD6 kernel after the natural boundary-degree
shift.  If the characters differ, stop.  If they agree, search for a
multiplication/Koszul intertwiner.  Such an intertwiner could transfer a
known D1 lift or obstruction into TD6 and would connect rows 1--4 at the
deformation-complex level.

Expected value: **speculative but sharply falsifiable**; one spare AWS core,
not a main lane.

## Allocation delta

- 45%: correction-aware square `P1` normalized rays (item 1);
- 20%: normalized-Rees fan/source-row compiler (items 2--3);
- 15%: D1 global/cone coverage successors;
- 15%: TD6 narrow K2 successor;
- 5%: deck/tree and D1--TD6 character tests (items 4--6).

Do not return to monolithic square saturation, generic random-map search,
or topology-only passports while these finite exact separators remain.
