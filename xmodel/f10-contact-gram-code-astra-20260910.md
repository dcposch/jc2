# Contact Gram certificate source preparation

STATIC / UNEXECUTED. First action2026-09-10 19:52:59UTC; publication reserve20:12 and hard20:15 unchanged. Exactly eight input hashes matched before fresh WHOLE reads; no science execution or coefficient artifact.

## Outcome

Complete prospective producer.py, genuinely separate checker.py, metadata authority.py, disabled registration, CONTRACT, two-control VALIDATION, READ-SCOPE and PINS are supplied. No source has been imported, parsed as Python, syntax-checked, compiled or executed. No certificate, matrix, coefficient artifact, fixture, determinant/sign result, contact point or worker action exists. This is a source preparation, requiring FIRST different-model code review and separate ROOT runtime authority.

All eight charged texts were freshly read WHOLE after current hashes; clipped symmetric-report text was recovered. Old arithmetic is reused only as two separate source-text patterns, not as new equations, domain or runtime. The accepted new gate supplies the nilpotent-retaining ideal bridge. Its section5 reality converse is EXCLUDED, and its incidental closed-triangle 'equivalently' wording is expressly rejected: positivity on the prescribed triangle is sufficient, not equivalent to full-window/discrete-family positivity.

## Construction and independent check

Producer uses d,N,A,I and the literal cleared Q0/Q1; eight monic quartic remainder columns are normalized by positive rational CONSTANT denominator-LCM/content factors, zero column factor1. It constructs B, G=BB^T, row-zero cofactors h, R, then diag(c)B^T h; grouping the eight entries yields U0,U1. Full monic division yields Uphi, with zero remainder required. No parameter factor division occurs. Four polynomials R,U0,U1,Uphi plus sign data suffice; no matrix/inverse header is trusted.

Checker imports no producer or producer arithmetic. It starts with all literal quartic a3..a0,b3..b0 coefficients in a different four-variable sparse algebra, reconstructs EVERY Fa1/Fa0/Fb1/Fb0, derives W elimination by coefficient collection and substitutes W=N/d with exact denominator clearing. It checks the FULL identity R=U0Q0+U1Q1+UphiPhi, including all positive V terms. It does not independently assert Gram determinant equality; any complete verified identity and nonvanishing R is sufficient. This distinction is literal in the future receipt.

Manual bounds: entries have column weight minus row index. Constant normalization changes no degree. Cauchy--Binet bounds R by52; adj(G)_{i0}<=42+i gives U0<=54,U1<=53, numerator<=60 and Uphi<=56. Hence selected cofactor weight64 and V degrees3,3,5 are safe. The proof uses no generic-rank assumption or ignored nilpotents. All actual counts/bit-lengths/costs remain unknown.

## Fixed sign certificate

a=(6+t)/21,b=(3-3t+7tv)/21 maps t=0 to(2/7,1/7) and t=1 to the omitted edge a=1/3; for fixed t it fills1-3a<=b<=4a-1. Thus t<1 covers the required prescribed triangle. Producer cached substitution and checker nested Horner via p=(2-a)(2-b) are distinct. Scaling by positive21^52 accounts for p's denominator21^2. Remove only maximal(1-t)^j with j INCLUDING0; retain t0/v0/v1. Fixed Bernstein degree(52-j,52), without elevation/subdivision, is converted separably by producer and expanded separably by checker. All nonnegative entries and all53 t0 entries equal a strictly positive endpoint imply strict positivity for every t<1. No reality of contact coefficients is assumed.

## Status, authority and remaining checks

Producer normal0/CANDIDATE and normal2 WITH complete INCONCLUSIVE artifact both remain eligible for the SAME independent checker. In particular negative Bernstein data is not silently discarded. Checker completes identity/factor/expansion before an honest sign-INCONCLUSIVE receipt. Structural/resource failures or partial files confer no candidate or verdict. The final positive receipt scope is prescribed CONTACT only, not full-source or JC2.

Exact rational-string schema rejects duplicate/numeric/noncanonical/zero polynomial terms and preserves dense zeros. MAX_TERMS20000/MAX_BITS12000/4096decimal characters and16MiB combined certificate/receipt ceiling are explicit. Receipt SHA binds actual verified raw bytes. ROOT-owned resolved source/science/interpreter/input pins, exact-E -s -B startup/sys.orig_argv, offset-explicit deadline and exclusive absent outputs are required before arithmetic imports. A text-splice defect in the initial NEW authority draft was caught on WHOLE manual read and repaired before freezing; no source was ever executed. This is author review, not syntax/runtime evidence.

Prospective600wall/550CPU/2GiB/16MiB joint limits are unmeasured; authority is not a supervisor. ROOT still needs a capped wrapper, native closure, full actual argv, immutable inputs, per-phase/absolute cutoffs and terminal group/custody enforcement. No such registration or launch is supplied. Exactly two prospective mathematical corruptions are specified: Uphi+1 and one existing Bernstein coefficient+1, with canonical limits/fresh hashes/whole-byte inverse restoration and exact full-identity/full-expansion rejection predicates. No fixture or mutator is authored.

## OPEN and publication

No new canonical OPEN. Two task-local pending quantities: three unexecuted source modules require FIRST static review; zero emitted artifacts require one separately registered bounded formation/read-back if review succeeds. Actual positivity is unknown; a failed sufficient sign certificate is not a contact point. No speedup, source closure or follow-on is claimed. Own targets were absent; own WHOLE/source review and OPEN/collision/current-pin checks precede marker LAST, transaction close/finalize/expected verify and custody-FIRST all-writers-IDLE handoff.

Own WHOLE source/docs/report review, all eight unchanged input postpins, marker-absence and final-target collision checks completed20:03:42UTC. The metadata-only authority import precedes authorization; Fraction/math and scientific construction follow it. No test or mathematical execution occurred. The completion marker below is appended only after these checks; all subsequent work is publication metadata.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6208`.
- Body SHA-256:
  `802b814e9275d0dc7f056e08f9716ea021ccb1a908541fbb0324df9ac197e96b`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
