# Blind ideation — Grok 4.6 — round `20260904T1200Z`

Lane: Grok 4.6 (xAI), equal-standing independent researcher. Blind against
the sealed packet and banked repository reports only. No other
`ideation-20260904T1200Z-*` submission was opened (the coordinator's
committed file was not read). Canonical ledgers were not edited.
`jc2-lean` was not inspected.

## 0. Custody

Charged-input hashes verified mechanically from the lane receipt
`xmodel/ideation-20260904T1200Z-grok46.run.v2`: `awk` paired
`charged_input_<i>_sha256=` with `charged_input_<i>_basename=` under
`lane_inputs_dir=/tmp/jc2-lane.DYUfp2/inputs`, and `sha256sum -c`
returned **4/4 OK**. Direct `sha256sum` of the four frozen files agrees.
No digit was retyped.

```text
1ee8e07b8da6a786157d08c076e12f7d065c7cb09d436d4ff85a3a7a0faef7a7  ideation-20260904T1200Z-packet.md
aa9054f24a3845d206f0f66908b042b90b72158bc74e25feb9b7deed0578fcc3  ideation-20260904T0000Z-synthesis.md
e47fd16cfcc91bc7bfdac4ba1b5f46152db8235e6609dca6e29a5966549c38f5  FALLACY-v2.md
c66b70e460a609ff5210fd16015fc7b2ff18dda26f68597a0d817c5b9aeb19d5  COORDINATION.md
```

Packet frozen at the 12:00Z full-spectrum contract. Lane receipt
`basis=bb2a1a78491a2c85b4c11ee75803f47f1a43d60f`. Binding live state:
`notes.md` EVENT 2026-09-04T11:36Z and the 11:14Z/11:36Z AUDIT deltas
17(nnnnn)–(ooooo). Freshest banked mathematics: the named `-20260903`
reports (all `final_status=DONE` at packet freeze except the running
structural lane). `FALLACY-v2` in force. No new exit-price ⇒ no
`charge_basis`. Desk CAS only (Python/SymPy/stdlib, `pdftotext` /
`pdftoppm`, existing Singular TSV/meta; one core; ≪ 4 GB; no AWS).

Firewall: flag ≠ place ≠ cover series; `REPRESENTATIVE` is not
`FULL_ACTUAL_EXIT`; a floor is not attainment; a relaxation being empty
*is* a nonexistence proof. Descended 4-tuple `(n', m', M_2', V_2', k)`
is not an original skeleton. Prime marks on descended data are labels.

Running-lane protocol: `k16-hsop-length-allt-opus5-20260903.run.v2` was
read first. At the start of this turn it had `initial_status=RUNNING`
and no `final_status`. Before writing Q1 it was rechecked:
`final_status=DONE`, `end_utc=2026-09-04T12:03:36Z`,
`report_state=BODY_SEALED`, `charge_basis_status=ABSENT`. The sealed
report was then opened (the packet forbids opening only while the
receipt lacks `final_status`). No other in-progress `.md` was opened.

Read: the packet; APPROACHES overlays; AUDIT 17(tttt)–(ooooo);
PROGRESS 2026-09-04 digest; notes.md 08:22Z–11:36Z; named DONE
reports; COORDINATION.md; FALLACY-v2. Moh §§5–6 and pp.200–210
(`pdftotext`, PDF page = Moh page − 139; `pdftoppm` of p.209); Xu §8.
Drivers under `box/*-20260903/` used as data, not edited.

---

## 1. Direct answer: the coordinator's headline is half-right, and
the (H1) half of it is a category error

The packet says both halves of JC2-along-Moh have exhausted foreground
computation and now need structural theorems (or a better solver), and
that the `disc(p_red)` theorem route for (H1) is refuted. The second
clause is right. The first clause mixes two programs that do not fail
the same way and do not want the same next object.

**(H2) / K = 16.** Foreground Gröbner is exhausted at t = 7
(17(yyyy), 17(mmmmm)). A structural theorem *was* the right demand.
The running lane `k16-hsop-length-allt-opus5` has now sealed PARTIAL
and answers the packet's Q1 more sharply than the packet posed it:

- the *length* `L_t = 2·binom(3t+1, t−1)` is a theorem for every
  t ≥ 2 (CI-SERIES: `P_t(s) = (1+s^{t+1})[3t+1 choose t−1]_s`);
- the three routes the packet named as the all-t proof — weighted
  Fröberg, a triangular regular-sequence change of generators with
  coprime leading monomials, a degree-only Bézout/resultant dim-0
  bound — are **impossible**, not unfinished (NO-COPRIME-LEADERS,
  DEGREE-BLIND);
- Z-EMPTY_t via the linear-form Jacobian is **not** the better all-t
  target;
- the residual is Criterion RANK: a non-vanishing along a curve.

That is a harvested structural theorem plus a typed residual, not a
request to keep computing cones.

**(H1) / MINOR-EMPTY.** Foreground computation is *not* exhausted in
the sense that forces a theorem. What is exhausted is one solver
shape (raw `slimgb` on a 58–256 MB full-D1 blob) applied to the wrong
object. 17(ooooo) refutes disc-invertibility as a kill; 17(lllll)
already says there is no universal final minor from the datum.
Pretending the missing object is `disc(p_red) ∈ radical(I_joint)`
repeats the same mistake in Fitting language: on the distinct-root
locus the Vandermonde is invertible, so the interpolation block is
*solvable*. Inconsistency needs an extra inhomogeneous row after that
interpolation. Moh, Xu, and the ladder do not supply one uniformly.
The honest (H1) deliverable is the census, with a *better scheduler*
(h-adic bands, high power first), not a Fitting theorem that the
sources do not contain.

**Case (A) is mis-filed as `OPEN[FULL-BASIS-TIMEOUT]`-class.** The
unsplit configuration of (99,66) is Moh p.209's first possibility,
transformed to `(27,18; M_2=21, V_2=8, δ_2=−1, δ_1=0, J=X^4)` and
reduced by *the same Tschirnhausen method as the printed third case*
to **10 coefficients**. The corrected full-D1 over-approximation of
that 4-tuple has `u'=1`, `δ_1'=0`, `B=−1`, about **126 unknowns**
(this session; sibling of D = 108 no-split at 112). That is the
timeout class. It is not Moh's chart. Putting (A) on the same critical
path as `(25,15; 21; 2; k=2)` (u' = 3, 141 unknowns, 598 mostly
degree-≥4 rows) is a variable-map error: matching names `δ_1=0` do
not identify the charts.

**(H1) ∧ (H2) is not a complete reduction of plane JC2.** It is a
reduction of Moh's two-point line, and even there it leaks. Q4.

---

## 2. Q1 — the length is a theorem; dim-0 is Criterion RANK;
Z-EMPTY is the wrong retarget

### 2.1 Typed claims

**C1.1 (PROVED, harvested this hour).** For every t ≥ 2,

```text
P_t(s) = prod_{d=2t+2}^{3t+1}(1-s^d) / D_t(s)
       = (1 + s^{t+1}) [3t+1 choose t-1]_s,
L_t    = P_t(1) = 2·binom(3t+1, t-1)
       = prod_{d=2t+2}^{3t+1} d / ((t-1)! (t+1))
       = (t/(t+1)) binom(3t+1, t),
```

a palindromic polynomial of degree `(2t−1)(t+1)` with nonnegative
coefficients. The identity `(1−s^{2t+2})/(1−s^{t+1}) = 1+s^{t+1}` is
the whole cancellation. This session independently checks the integer
form of `L_t` at t = 2..10 (14, 90, 572, 3640, 23256, 149226, 961400,
6216210, 40320150). The factor 2 is `deg_{b_3} T_{t,2t−1}`, not a
fibre artefact (TAIL-SPLIT).

**C1.2 (PROVED, harvested).** For t ≥ 3, `a_0 = −α_t` is a unit of
`A_t` on both factors at every split index (norm identity; the only
integer root of the norm is t = 2). Hence `M_t := S_t/(T_{t,2t−1})`
is a free graded `P_t`-module of rank 2 on `{1, b_3}`, and

```text
J_t^{tail} = (T_{t,2t−1}, G_1, …, G_{t−1}),
G_r = B_r b_3 + C_r   (b_3-linear, wt 2t+2+r).
```

hsop of the tail ⇔ dim `M_t/(G) = 0`. The two factors of `P_t(s)` are
exactly this rank and the CI series of t−1 forms of degrees
2t+3..3t+1 in weights 1..t−1.

**C1.3 (REFUTED as posed).** The packet's three named all-t routes
cannot prove dim = 0.

- *Weighted Fröberg / CI Hilbert numerator.* `P_t` is the series the
  quotient **has if** the tail is an hsop. Implication:
  `dim = 0 ⇒ length = L_t`, never the converse (FALLACY:
  floor/attainment). DEGREE-BLIND: the two fibres of `A_2` have
  identical degree data (weights 6, 7 in variables of weight 1, 3) and
  opposite answers (`y=1/5`: dim 1, `b_4^3` divides both rows;
  `y=2/5`: dim 0, length 14 = `L_2`).
- *Coprime-leading-monomial generators.* NO-COPRIME-LEADERS: for every
  t ≥ 3 except `{3,5}`, no monomial order and no t generators make the
  tail a coprime-leader regular sequence (pure powers of distinct
  variables; degrees forced to 2t+2..3t+1; no SDR — two primes in
  `[2t+3,3t+1]` both require weight 1). Naive `wp` leaders are all
  powers of the same variable `b_4`; `in(tail) = (b_4^{2t+2})`,
  dimension t−1.
- *Degree-only Bézout.* Same witness. Lemma ELIMINANT's `V(W)=0` is
  sufficient, not exact, and costs Bézout `2^{t−2} L_t` (180 = 2·90 at
  t = 3): the charged surcharge, not a shortcut.

**C1.4 (NOT the better all-t target).** Z-EMPTY_t
(`V(I_{t,+}) ∩ {τ_t = 0} = {0}`) is the cone plus one more homogeneous
generator `T^{hom}_{t,0}`. It is not smaller than the tail. It does
not imply (V0) without dim `I_{t,+} = 0`. Lemma CONE already says
(8.1) ⇔ `τ_t ∈ √I_{t,+}` once dim 0 holds; (V0) ⇔ (T) ∧ Z-EMPTY is
true as a splitting of (V0) and useless as a *proof plan* for (V0),
because (T) for all t is what (V0) was supposed to give. The linear
form `J = cγ + π E_t(h)` is a structural identity (17(kkkkk)) and a
reason DEP is vacuous; it is not an all-t replacement for the tail.

**C1.5 (the sharp residual).** For t ≥ 3, with `N` the (t−1)×2 matrix
of rows `(C_r, B_r)` over `P_t`,

```text
V(J_t^{tail}) = {0}
  <=>  (i) V(B_1..B_{t−1}, C_1..C_{t−1}) = {0} in Spec P_t, and
       (ii) every p ≠ 0 with rank N(p) = 1 whose kernel is spanned by
            a vector of nonzero first coordinate has W_r(p) ≠ 0 for
            some r.
```

The locus `rank N ≤ 1` of a (t−1)×2 matrix in a (t−1)-dimensional
space has expected dimension 1. The all-t statement is a non-vanishing
along a *curve*. `V(W)=0` is sufficient, not exact (FALLACY:
carrier/attainment). The banked analysis of `b_r, c_r` says they carry
the whole 2t+1-step spine and have no closed form in t by that route.
That is OPEN[K16-RANK-CURVE], not a Gröbner rerun and not Z-EMPTY.

**C1.6 (scope of what this buys).** Tail hsop at t ⇒ dim `I_{t,+}=0`
by containment ⇒ (V0) ⇒ (8.1) ⇒ (T) on the K = 16 ray at that t.
This is JC2 along the only known cofinal screened family
(17(ll): "only known", not "only"). It is not JC2, and it is not (H1).

### 2.2 Cheapest tests

- **Identity (desk, done here).** `2·binom(3t+1,t−1)` versus
  `∏_{d=2t+2}^{3t+1} d / ((t−1)!(t+1))` at t = 2..10: equal.
- **DEGREE-BLIND (banked exact, reproduced in the harvested lane).**
  t = 2, both factors of `A_2`, exact over Q: dim 1 vs dim 0 length 14.
- **RANK at t = 3 (desk, Card II).** `P_3` has two variables
  `(b_4, q_{2,0})`; `N` is 2×2; the rank-1 locus is one determinant.
  Evaluate `W_1, W_2` on that curve minus the origin. Regenerated rows
  live in `box/k16hsop-20260903/tail_t3_rows.json`. Bound: < 2 min,
  ≪ 4 GB. If some component has both W vanishing, tail regularity
  fails at t = 3 (contradicts the banked exact length 90 — a control).
  If every component is nonvanishing, the t = 3 curve is the method
  client for t = 4, 5.
- **STOP:** any further fixed-t `std`/`slimgb` of the cone or tail at
  t ≥ 7; any Macaulay matrix of `wp` leading forms; any Z-EMPTY
  computation that adjoins `T_{t,0}` to the full cone.

### 2.3 Answer

The length statement is proved for all t. The hsop / dim-0 statement
is not, and the packet's three named proof routes are refuted as
mechanisms. Z-EMPTY is a strictly weaker statement that does not
unlock (T). The sharpest provable all-t objects right now are
CI-SERIES, TAIL-SPLIT, NO-COPRIME-LEADERS, DEGREE-BLIND, and Criterion
RANK as an exact residual. The cheapest test of the residual is the
t = 3 rank-1 curve, not another Gröbner run.

---

## 3. Q2 — case (A) is Moh's 10-coefficient chart, not a 126-unknown
over-approximation; N1 is not in §§5–6; N2 is four face-ODEs

### 3.1 Typed claims

**C2.1 (source, p.209 image + text).** Inside the (99,66) skeleton,
Moh's trichotomy is: `g_σ` is a power of a linear polynomial (A,
unsplit) or the 9th power of a cubic with precisely two roots (B/C,
split). For (A) the data transform to

```text
n=27, m=18, M_2=21, V_2=8, δ_2=−1, δ_1=0, Jacobian X^4.
```

"The above mentioned method" — the Tschirnhausen reduction printed on
the same page for the previous case, `f = h^3+β_2 h+β_3`,
`g = h^4 + (4/3)(β_2 h^2+β_3 h) + …` with `deg_y γ* < deg h = 4` and
`deg_y δ* < deg h^2 = 8` — "can be used to reduce the number of
coefficients to 10." This is Appendix II arithmetic, not Theorem 1.2
order-basis.

**C2.2 (this session).** The *corrected full-D1 over-approximation* of
the same 4-tuple, using `order_basis_full.py`'s closed forms, is

```text
K=9, e=3, q=2, u'=1, R=5, Π=5, δ_1'=0, δ_2'=−1, B=−1,
h-inventory 17, α-dims [18,27,36], β-dims [27], ~126 unknowns.
```

Sibling of D = 108 no-split `(24,16; 18; 7; k=4)`:
`K=8, u'=1, δ_1'=0, B=−1`, ~112 unknowns, 472 rows, 181 MB,
`slimgb` timeout 1800 s (17(nnnnn)). (25,15) is a different object:
`K=5, u'=3, δ_1'=7/5`, 141 unknowns, 598 rows. Desk census of those
TSVs: (25,15) degree-in-identifiers `{1:6, 2:32, 3:48, 4+:512}`;
D = 108 `{1:8, 2:25, 3:43, 4+:396}`. The six linear rows of (25,15)
all live at `h`-power 6 and 7; the eight linear rows of D = 108 live
at `h`-power 3 and 4. Early bands are dense high-degree Jacobian
bilinear/multilinear forms. Raw `slimgb` on the blob is the wrong
solver *and* the wrong chart for (A).

**C2.3.** 17(tt)'s 14-unknown SATURATED-EMPTY of branch A is the
`δ_1=0` instrument-fail slice (standalone `−c`; 17(ggggg),
QUALIFICATION on 17(tttt)). It is not Moh's 10-coefficient chart.
Demoting 17(tt) does **not** put (A) on the 126-unknown critical path.
It puts (A) on the Appendix II critical path, which Moh already
claimed was 10 coefficients.

**C2.4 (N1).** GAP[MOH-CENSUS-COMPLETENESS] is not "does a Keller pair
have some skeleton?" in the abstract. The dossier's missing sentence
is: a pair of degrees (99,66) normalised as Moh p.194 / Theorem p.200
has `M = (−66, 77, 97), V = (8,8)`. Printed (1)–(13) at `n=99, m=66`
admit **8** V-assignments (dossier §0.3), Moh prints one. §§5–6
(major discs, Prop 6.1–6.4, the Theorem at p.200) justify the *search
conditions* (1)–(13) and the two-point reduction. They do not contain
the extra filters (ODE / passport / Prop 5.6 zero-chain / POLY) that
cut 8 to 1. Those filters are the campaign's operative screen
`C_FULL_TREE_POLYNOMIAL_ODE ∧ XU` (17(hh)+(eee)+(fff)), not a
paragraph of Moh. A source-read of §§5–6 will not close N1.

**C2.5 (N2).** Two pieces were glued.

- `den(δ) ≤ u_s`: this is N5, **PROVED** by the Galois-orbit argument
  (17(rrr), gated 17(sss)) at the top packet. Xu §8 asserts it as a
  tool ("denominator of order δ ≤ u_s = 3") without proof; the
  campaign has the proof. Do not keep listing it as a literature gap
  of THEOREM 8.1.
- Xu 8(i): one sentence. After restricting to `1 < δ < 8/3` and
  `den ≤ 3`, the candidates are `{4/3, 3/2, 5/3, 2, 7/3, 5/2}`; Cor
  7.5 cuts `{4/3, 3/2, 5/3, 2}` to two-part splits. The sentence
  "When `δ ≠ 2` and `δ ≠ 5/2`. From equation 7.1, we can step by step
  deduce `q(π) = p(π)^{13}(π−c)` and the equation 7.1 can not hold
  for it" removes `{4/3, 3/2, 5/3, 7/3}`. That sentence is not a
  proof. The dossier already names the remedy: four finite face-ODE
  computations of the size of its own §4.3. This is not a source-read
  of Xu §8; §8 is the claim.

**C2.6.** THEOREM 8.1 for the *split* branches is therefore
conditional on N1 (census uniqueness of the printed V-assignment)
plus the four 8(i) ODEs. N3, N4 are discharged gauges (17(jjjjj)).
The split kills (6264, 64) are untouched. The degree statement
"no Keller pair of degrees (99,66)" additionally needs case (A) and
N1.

### 3.2 Cheapest tests

- **Case (A).** Reconstruct Moh's 10-coefficient chart from p.209
  (the printed `h, β_2, β_3, γ*, δ*` bounds, Jacobian `X^4`) and
  saturate over Q. Bound: 10 unknowns, Appendix II scale; the
  (15,10) method-client on the next page died in the same language.
  Controls: tame two-point automorphism survives; a deliberately
  omitted necessary coefficient admits a false point. Do **not** emit
  `order_basis_full` for (27,18). Stop if the reconstruction cannot
  source every imposed equation to p.209 / Prop 6.3 inverse.
- **N1.** Run the operative screen at `(n,m)=(99,66)` only: list the
  8 (1)–(13) V-assignments and which die under TREE+ODE+POLY+XU.
  Quantity: 8 rows. If exactly one survives and it is V = (8,8), N1
  is a campaign theorem about the *screened* census, still not a
  sentence of Moh. If more than one survives, THEOREM 8.1's degree
  reading is false and each extra skeleton is a new client.
- **N2 / 8(i).** Four face-ODE jobs, one per
  `δ ∈ {4/3, 3/2, 5/3, 7/3}`, equation (7.1) as in Xu §8, localisation
  at the genuine-split open. Expected: `q = p^{13}(π−c)` and a
  contradiction, or a surviving p. Either outcome is a typed close or
  a typed extra split. Bound: the dossier's own §4.3 size.

### 3.3 Answer

Decide (A) by Moh's 10-coefficient chart, not the corrected full-basis
engine. Do not source-read N1 closed from §§5–6. Close the den-bound
half of N2 as already proved (N5). Close the 8(i) half by four face
ODEs. THEOREM 8.1 (split) becomes unconditional except N1 after those
four jobs; the degree statement waits on (A) and N1.

---

## 4. Q3 — there is no uniform (H1) theorem in the sources; the
minimal compute change is high-`h` banding, not more `slimgb`

### 4.1 Typed claims

**C3.1 (agree with 17(ooooo)).** Disc nonvanishing of `p_red` makes
the packet-evaluation block invertible, hence its equations solvable.
It does not make them inconsistent. 6264 and 64 are pole/Jacobian
Schur residues, not disc values. Only D = 108 δ = 3 has
`disc ∈ I` nontrivially, via `[1,1] → [2]` collapse. REFUTED as a
mechanism for MINOR-EMPTY.

**C3.2 (the packet's replacement is the same mistake).** The "missing
theorem" `disc(p_red) ∈ radical(I_joint)` for every admissible datum
is a Fitting rewrite of the disc route. On the distinct-root locus
the discriminant is a square of a unit in the coordinate ring of the
split; membership in the radical of a *unit* ideal is trivial after
the kill and false before it. 17(lllll) FILTRATION-PAGE-RULE already
says there is no universal final minor from
`(d_s, u_s, V_s, δ, weights)`: first obstruction on incidence (D=108),
on stage-4 pole/Jacobian ((99,66) δ=2), on a pole-Schur page at
t-power 8 (δ=5/2). A uniform Schur/Fitting *row* would have to live
on a page the three certificates do not share.

**C3.3 (ladder law is a derived row, not a kill).** 17(iiiii)
PARTIAL[LADDER-LAW]: Jacobian band coefficients
`c(p,k) = ±[e A (K−p) − n(k−k_0)]` reproduce 66/66 entries and
`6264 = 8·783`. That is a uniform *description of a family of rows*.
Those rows kill (99,66) δ=2 and do not appear in the D = 108
certificate. A valuation / Newton-polygon argument at the second
point is Moh Prop 6.1 (order identity, already used). (99,66) died
at coefficient level after 7,161 coordinates; polygons do not kill it.

**C3.4.** MINOR-EMPTY as a theorem is currently out of reach **because
the sources do not contain a uniform extra inhomogeneous row**, not
because the campaign has been too timid to write Fitting language.
The honest (H1) deliverable is the census of the 166 `u_s>1` groups
at D ≤ 200 (17(fff)), incidence-first for `u_s=2` quadratic faces
(D = 108 template), Jacobian/pole-first for `u_s≥3` ((99,66)
template). That is a bounded batch once the engine is the *certificate
slice* (8 and 7 inner rows at (99,66); 10 at D=108), not the 7,161
chart.

**C3.5 (minimal compute change for the leftover (H2) full-basis
systems).** The 1800 s timeouts (17(nnnnn)) were `slimgb`, 1 thread,
on 58 MB ((25,15) partition 3) and 181 MB (D=108) blobs. This
session: 598 and 472 rows, of which 6 and 8 are linear, all at the
*highest* `h`-powers. The (99,66) engine died because it staged
linear Q* pivots in Jacobian/pole bands. The analogous move here is:

```text
order the TSV by decreasing h_power;
run Q* on the linear rows at the top band (6 rows at h=6,7 for (25,15);
  8 rows at h=3,4 for D=108);
propagate;
only then touch the degree-2,3,≥4 remainder.
```

Term-order experiments (`dp` vs `wp`) and modular+CRT without this
cut do not address the measured shape. Targeted saturation at `c` is
already in the emitters (`sat=c`). msolve/F4 on the *post-linear*
residual is the second lever, not the first.

**C3.6.** Even that change does not make (A) a full-basis client.
(A) is 10 coefficients in Moh's chart and ~126 in the over-approx.
Do not spend a 1800 s seat on (A)'s over-approx.

### 4.2 Cheapest tests

- **Banding (Card III software).** Sort
  `box/orderbasis-20260903/rows/25_15_21_2_k2_part_3_full_rows.tsv`
  by `h_power` descending; Gaussian-eliminate the 6 linear rows over
  `F_32051`; report rank, remaining unknowns, remaining row degrees.
  Bound: seconds (the TSV is 58 MB to scan, the linear block is 6
  rows). If the linear block is the old `−c` family, STOP and do not
  advertise a kill. If it drops the unknown count below ~80 with a
  quadratic residual, that residual is the first honest `slimgb`/`msolve`
  client.
- **One more `u_s=2` incidence client** (not 166 groups). Smallest
  screened `u_s=2` quadratic-face row other than D=108; common-`h_{s−1}`
  block only; `c ≠ 0`. Template: D=108's 10-row slice (17(lllll)).
- **STOP:** a Fitting-lane whose deliverable is
  `disc(p_red) ∈ rad(I_joint)`; a Newton-polygon all-`u_s` theorem;
  a 166-group batch on the unbanded over-approx.

### 4.3 Answer

No uniform theorem route to MINOR-EMPTY is visible in Moh, Xu, the
ladder, or the three certificates. Commit to the census, priced as
certificate-slice incidence-first (`u_s=2`) then Jacobian/pole-first
(`u_s≥3`), not as 166 full 7,000-coordinate charts. The minimal
compute change that can even in principle finish the leftover
full-basis (H2) systems is high-`h` linear banding of the existing
TSVs. It will not finish (A); (A) is Card I.

---

## 5. Q4 — the two-half program leaks; the single next object is
Moh's 10-coefficient (A)

### 5.1 Where the two-half sentence is right

A Moh skeleton of a nontrivial Keller pair either carries a genuine
principal-minor split, in which the necessary joint two-point system
is the (H1) client, or it does not, in which Prop 6.3/6.4 (when
licensed) lands a monomial-Jacobian order chart, every terminal chart
of which is (T). Neither half is a census of original degrees. K = 16
is a prototype of (H2), not the flagship of JC2. The 00:00Z five-of-five
conjunction stands as a *program along Moh's two-point line*.

### 5.2 Where it is not a reduction of plane JC2

**Leak 1 — one-place.** Abhyankar–Moh / rank-four / one-cusp work is a
different program. Moh p.194 explicitly restricts to `M_s = n−2` and
two distinct points at infinity. (H1)∧(H2) never claimed to eat that
half; the packet's "reduction of plane JC2" over-reads the 00:00Z
sentence "JC2 in the plane, along Moh's line".

**Leak 2 — N1.** Eight (1)–(13)-admissible V-assignments at (99,66).
(H1)∧(H2) on the *printed* skeleton does not decide the other seven.

**Leak 3 — U-NEGATIVE.** Twenty rows in the 14:41Z appendix2 partial
had `V_2' > d_2'`, so the s'=2 A/B chart does not apply. Neither split
nor licensed Theorem-1.2. Third configuration.

**Leak 4 — residual height s' > 2.** OPEN[DESCENT-STATE-S3],
OPEN[T-HEIGHT] (14 rows at (96,64), `s_eff=3`). (T) as a height-2
four-integer problem is not universal.

**Leak 5 — unsplit `u_s ≥ 2` failing `δ*_{s−1} ≥ v_s/u_s`.** Then
neither (H1) nor licensed (H2). Case (A) does transform; the leak is
the general unsplit stratum.

**Leak 6 — `δ_1'=0` is two classes.** `u'=1` (A, D=108 no-split; open
x-support) versus `u'≥2`. Lumping them as one FULL-BASIS-TIMEOUT class
is the packet's (A) error.

### 5.3 The single most valuable next object

Not the Fitting row (ghost). Not another t ≥ 7 cone (exhausted). Not
Z-EMPTY (wrong target). Not 166 groups (batch, not the object).

The harvested K16 residual (RANK-CURVE) is the right *structural*
object for the unbounded family, and it should be a card. It is not
the first lane: the hsop report itself says `(b_r, c_r)` carry the
spine and have no closed form in t by that route, so a RANK lane is a
pattern-hunt at t = 3, 4, 5, not a proof in 180 minutes unless the
t = 3 curve is unexpectedly rigid.

The first object is **Moh p.209's 10-coefficient chart of case (A)**.
It is the remaining configuration of the (99,66) skeleton; it is the
remaining hole in Moh's ≤ 100 theorem once N1 is scoped; it is 10
unknowns in the source, not 126 in the over-approx; it is (H2)-shaped
(`J = X^4`, monomial Jacobian, descended) so a kill is a (T) client
and a SURVIVES is a `REPRESENTATIVE` of a Keller pair of degrees
(99,66) unsplit. That is the only finite object in this round whose
both outcomes are campaign-moving.

---

## 6. Disposition vector (APPROACHES rows, changes only)

The 08:56Z overlay's (H1)∧(H2) retyping stands as a *Moh-line*
program, not as plane JC2. The 16:36Z overlay's "(99,66) is the top
target" is split: split configurations closed, (A) open on a
10-coefficient chart. Row scores from 08-31 rank-four overlays are
not reopened.

| # | change | reason |
|--:|---|---|
| 1 | **raise** | (T) now has three presentations: K16 tail (CI-SERIES + RANK residual), Theorem-1.2 order chart (leftover 4-tuples), and Moh's Appendix II 10-coefficient chart (case (A)). Certificates of row 1 kill the cofinal family *and* the unsplit (99,66) configuration. |
| 2 | **unchanged as screen; do not raise as proof** | The tree generates targets. U-NEGATIVE and s'>2 are coefficient-level leaks, not a reason to reopen the sieve. |
| 5 | **unchanged retype** | Prop 6.3 is not Jung–van der Kulk. D = 108 no-split map is CLOSED as a substitution (17(fffff)); emptiness is OPEN[ORDINARY-SUPPORT] / FULL-BASIS-TIMEOUT. Do not shear. |
| 6 | **raise** | Case (A) *is* AM on the descended one-place curve with Jacobian `X^4`, in Moh's own 10-coefficient language. The order-basis over-approx is not this row. |
| 7 | unchanged | GLUE is two places of one pair, not a construction of A(F). |
| 16 / 45 | keep lower as ceiling | PS-GROWTH is shape. |
| 25 | keep lower as a finish | Hurwitz tree-determined (17(mmm)). |
| 29 | keep, not first | Node ODE; downstream of (T). |
| 32 | **lower as a general order-chart tool; keep as (H1) instrument** | Q* killed (99,66) because thousands of outer rows were linear. The leftover order-basis TSVs have 6 and 8 linear rows. Q* is the first band, not the solver. |
| 36 | **raise, retarget** | CE search: SURVIVES of case (A) 10-coeff, or of RANK-CURVE at some t ≥ 8, or of a leftover 4-tuple. Typed `REPRESENTATIVE` until algebraized. Not original (25,15) (empty census, gcd 5). Not a Fitting-row interpolant. |
| 43 | keep mild raise | Ritt shape of a Lemma-2.1 polynomial is what (T) attacks. |

Unchanged: 3, 4, 8–15, 17–24, 26–28, 30–31, 33–35, 37–42, 44, 46.
STOP as proof routes: disc/Fitting MINOR-EMPTY; fixed-t cone at t ≥ 7;
coprime-leader Fröberg; native preprocessing (already); 166-group
full-chart census as a *theorem*. RAISE: case (A) 10-coeff; RANK-CURVE
as the K16 residual; high-`h` banding as the leftover-4-tuple solver.

---

## 7. Reranked bottlenecks

**Proof, in order.**

1. Case (A) 10-coefficient chart (Moh p.209). Bounded: 10 unknowns.
   Closes the (99,66) configuration trichotomy on the split-killed
   skeleton, or produces a `REPRESENTATIVE`.
2. Four Xu 8(i) face-ODEs. Bounded: four jobs of dossier-§4.3 size.
   Makes THEOREM 8.1 (split) unconditional except N1.
3. Criterion RANK at t = 3, then 4, 5 (pattern, not a Gröbner rerun).
   Bounded: t = 3 is two variables. This is (V0) for all t *if* a
   uniform non-vanishing appears; it is a method-client if not.
4. N1 at (99,66): 8 V-assignments under the operative screen.
   Bounded: 8 rows, no engine.
5. High-`h` banding of the (25,15) and D = 108 no-split TSVs, then
   msolve/F4 on the residual if the unknown count drops. Bounded: the
   TSVs already exist.
6. One next `u_s=2` incidence client. Not 166 groups.

**Disproof, in order.**

1. SURVIVES of case (A) 10-coeff. Typed `REPRESENTATIVE` until it is a
   pair in `k[x,y]` of degrees (99,66).
2. A component of the RANK-CURVE at some t ≥ 3 on which every `W_r`
   vanishes (tail not hsop). At t = 3 this would contradict banked
   length 90 — a control, not a CE. At t ≥ 8 it is a CE of (V0).
3. A leftover 4-tuple whose banded residual is nonunit.
4. An extra (99,66) V-assignment that survives the operative screen
   (N1 failure as a degree statement).

PIN-NOT-CEILING, `C_FULL_TREE`, and Xu's principal floor stay demoted
as proof programs: they generate the target list.

---

## 8. New avenue and new connection

**New avenue: Appendix II Tschirnhausen as the `δ_1=0`, `u'=1`
solver.** When the slope vanishes, Theorem 1.2 D1 inventory opens the
x-direction and the over-approx jumps to 100+ unknowns (case (A) 126,
D = 108 no-split 112, versus K16 t = 1 over-approx 44 and the sparse
K16 chart 18). Moh already solved this class by depressing `f = h^3 +
β_2 h + β_3` and bounding the remainder degrees. That avenue is
source-licensed for case (A) on p.209. Discriminator: the 10-coeff
ideal is `[1]` or not, in minutes. If it is `[1]`, the avenue is the
solver for every `δ_1'=0`, `u'=1` descendant (D = 108 no-split is the
second client, with k = 4 rather than Moh's printed table). If it is
nonunit, (99,66) unsplit is a genuine residual pair and the campaign
has its first honest (99,66) CE candidate.

**New connection: TAIL-SPLIT is the same Tschirnhausen, on the cone.**
Eliminating `b_3` quadratically (`a_0` a unit, `M_t` free of rank 2,
`G_r` linear in `b_3`) is Moh's "reduce to 10 coefficients" move in
the K16 chart: one quadratic, then a linear system on the remainder.
The factor 2 in `L_t` is the degree of that quadratic. The reason
DEGREE-BLIND fails at t = 2, y = 1/5 is that `a_0` vanishes and the
depression is impossible — the same failure mode as a Tschirnhausen
step that loses the leading term. Case (A)'s Jacobian `X^4` is the
k = 4 instance of the monomial-Jacobian world that K16 inhabits at
k = 0. (A) and the ray are the same theorem in two presentations;
the 126-unknown over-approx is a third, unnecessary presentation.

---

## 9. Strongest proof attack, strongest counterexample attack

**Proof.** Reconstruct Moh p.209's 10-coefficient system for
`(27,18; 21; 8; k=4)` from the printed method (depress `f` to
`h^3 + β_2 h + β_3`, expand `g` in `h`, impose polynomiality by
cancelling negative powers of `y`, Jacobian `X^4`), saturate over Q.
Mechanism: it is a necessary chart of the unsplit configuration
(Moh's own transformation of the (99,66) skeleton). Fail-closed
gates: every imposed equation sourced to p.209 or to the inverse of
Prop 6.3; tame two-point automorphism survives; the old 14-unknown
`−c` generator is *not* a row. Empty ⇒ (A) dead, (99,66) skeleton
closed except N1. Nonempty ⇒ stop and algebraize, do not enlarge
the chart.

**Counterexample.** A point of that 10-coefficient locus, lifted
through the p.209 transformation Ω to a pair `(F,G)` of degrees
(99,66). Typed `REPRESENTATIVE` until polynomiality of both
coordinates is checked in `k[x,y]`. Second: a component of
RANK-CURVE at t ≥ 8 with all `W_r = 0`. Do not advertise emptiness
of original `(25,15)` (no such skeleton; gcd 5 classically closed)
or a joint-chart SURVIVES at (99,66) split (both branches are `[1]`).

---

## 10. Software acceleration / decisive experiment

**Experiment.** High-`h` linear banding of the already-emitted
(25,15) partition-3 TSV over `F_32051`.

- Instrument: the TSV
  `box/orderbasis-20260903/rows/25_15_21_2_k2_part_3_full_rows.tsv`
  (598 rows, 141 unknowns, `h_power` in column 2); a 40-line Python
  Gaussian eliminator over a 16-bit prime; no Singular until the
  residual is printed.
- Controls: the 6 linear rows are at `h_power ∈ {6,7}` (this
  session); the native gate `target_xk_level0_nonzero=1` must remain
  visible (do not resurrect `−c` as an isolated kill); tame
  automorphism control of 17(nnnnn) must still pass on the residual.
- Outcomes: unknown count drops below ~80 with quadratic residual →
  first honest `msolve`/`slimgb` client of the leftover (H2) systems.
  Linear block is the old `−c` family → STOP, the over-approx still
  contains the instrument-fail. Timeout on 6×141 Gaussian → the TSV
  parser is the bottleneck, not the algebra (fix the parser).
- Do not ship a 1800 s `slimgb` on the 58 MB blob as this experiment.
- Bound: < 5 min, ≪ 4 GB.

---

## 11. Campaign-systems check

**`UPGRADE` — TSV `h_power` band preflight in the order-basis
emitter, before any `std`/`slimgb`.**

Rotate: reproducibility and mathematical-opportunity-cost of solver
choice. Independent of the (V0) mathematics.

Evidence, this round: (i) 17(nnnnn) spent 1800 s × 2 on `slimgb` of
58 MB and 181 MB systems and reported OPEN[FULL-BASIS-TIMEOUT] with
no basis marker; (ii) those TSVs already carry `h_power` and contain
6 and 8 linear rows, all at the top band; (iii) the (99,66) engine's
only successful kills were staged linear Q* in high bands; (iv) the
emitter's `system` subcommand has `choices=("std","slimgb")` and no
band option (`order_basis_full.py` argparse). A 30-line preflight
that prints

```text
LINEAR_ROWS, LINEAR_HPOWERS, RANK_OVER_P, UNK_AFTER, RESIDUAL_DEG_HIST
```

would have made the timeouts a *measured next-band size* rather than
a blank wall, and would have blocked a 1800 s seat on a blob whose
first 6 rows are the whole linear content.

Smallest test: the experiment of §10, written to
`box/orderbasis-20260903/band_preflight.json`, no change to live
lanes except a coordinator note on the next (25,15) or D = 108
attempt. Implementation: one function next to `diagnose_partition`.

No `NO_CHANGE`: the 1800 s blank wall is live in 17(nnnnn). A
DONE-lane `LIVE_MATH=` receipt field would have let this round
consume the hsop harvest without a 40 KB open; recorded, not built.

---

## 12. Idea cards (three)

### Card I — Moh p.209 10-coefficient chart of case (A)

**Dependencies.** Moh p.209 (image + text); Prop 6.3 inverse for the
transformation `(99,66) unsplit → (27,18; J=X^4)`; QUALIFICATION on
17(tttt) (17(tt) is not this chart); 17(jjjjj) gauges (not used);
FALLACY-v2 variable/ring map. Does *not* depend on
`order_basis_full.py`, on (25,15), or on disc/Fitting.

**Cheapest discriminator.** Build the 10-coefficient ideal from the
printed method and compute a Gröbner basis over Q (and one 16-bit
prime). Bound: 10 unknowns; expected minutes.

**Outcomes.**

- `[1]` over Q, controls pass, every row sourced → (A) dead;
  NO-KELLER-PAIR-WITH-THE-(99,66)-SKELETON extends to the unsplit
  configuration, still conditional on N1. Paper-grade except N1 and
  the four 8(i) ODEs.
- Nonunit, dim 0, finite points → algebraize one point through Ω;
  `REPRESENTATIVE` of an unsplit (99,66) pair, or a chart error.
- Nonunit, dim > 0 → the printed 10-coeff reduction is missing
  equations; STOP and source-read p.209 against the reconstruction;
  do not enlarge to 126 unknowns.
- Reconstruction cannot source the Jacobian `X^4` or the remainder
  bounds → STOP, typed OPEN[A-TEN-SOURCE].

**Stop.** 90 min, or at the first unsourced equation, or at a dim > 0
without a sourced missing family.

**Information gain.** The remaining (99,66) configuration. Highest EV
in the round.

### Card II — Criterion RANK at t = 3, then 4

**Dependencies.** Harvested k16-hsop report (CI-SERIES, TAIL-SPLIT,
ELIMINANT, RANK); regenerated rows
`box/k16hsop-20260903/tail_t{3,4}_rows.json`; banked exact tail
lengths 90, 572. Does *not* depend on Z-EMPTY, coprime leaders, or
Fröberg.

**Cheapest discriminator.** At t = 3, `P_3 = A_3[b_4, q_{2,0}]`;
form `N` (2×2), the curve `det N = 0`, and `W_1, W_2` on that curve
minus the origin. Bound: < 2 min from the JSON.

**Outcomes.**

- Every component of `det N=0` except 0 has some `W_r ≠ 0`, and
  clause (i) holds → t = 3 RANK passes; repeat at t = 4 (four
  variables; expected curve). Two rigid curves with a uniform
  description is a theorem-candidate. None is a theorem.
- A component with all `W_r = 0` at t = 3 → contradicts banked
  length 90 (control failure: row regeneration or RANK statement).
- Timeout at t = 4 → stop; t = 3 is the method-client, not a
  Gröbner attack on t = 7.

**Stop.** 90 min, or after t = 4, or at a control failure.

**Information gain.** Whether RANK is a pattern or a restatement of
the spine. Does not finish (V0) for all t in this card.

### Card III — four Xu 8(i) face-ODEs

**Dependencies.** Xu §8 sentence (i); Xu equation (7.1); Cor 7.5;
N5/17(sss) for the denominator bound (consumed, not re-proved);
dossier §4.3 as size template. Does *not* depend on the engine
kills.

**Cheapest discriminator.** For each
`δ ∈ {4/3, 3/2, 5/3, 7/3}`, run equation (7.1) on the two-part
split licensed by Cor 7.5 and test Xu's deduction
`q = p^{13}(π−c)`. Bound: four jobs, dossier-§4.3 size.

**Outcomes.**

- All four contradict as Xu claims → GAP[XU-8(i)-STEP] closes;
  THEOREM 8.1 (split) residual is N1 only.
- One δ survives with a genuine p → extra split, classification
  incomplete, new (H1) client at (99,66).
- Deduction `q = p^{13}(π−c)` holds but (7.1) does not contradict →
  Xu's last clause is false; typed OPEN, do not fill by analogy.

**Stop.** 90 min, or at the first surviving extra split (then
redesign the classification, do not continue the other three as a
batch).

**Information gain.** The last literature gap that actually touches
the split kills' exhaustiveness.

---

## 13. Continue / redesign / stop

### Named running lane (receipt, then harvest)

`k16-hsop-length-allt-opus5-20260903`

| field | value |
|---|---|
| `start_utc` | 2026-09-04T11:15:55Z |
| `end_utc` | 2026-09-04T12:03:36Z |
| `final_status` | DONE |
| `report_state` | BODY_SEALED |
| `charge_basis_status` | ABSENT |
| `adapter_exit_code` | 0 |
| `report_sha256` | `a2637d38f715334fec463d947c6be29d2efd93e9ee2175a57e9e6eda266edd83` |

**Harvest / stop.** Do not continue (it is finished). Do not redesign
the same prompt (the three named routes are refuted; a rerun cannot
prove dim = 0 by Fröberg, coprime leaders, or degree-only Bézout).
The residual is Card II, a *different* prompt (Criterion RANK, t = 3
first), not a continuation of "derive `L_t` combinatorially".

### Single first lane

**`g9966-caseA-ten-coeff`** (Card I). Owner: a different model from
the necessity-dossier producer (Opus) and the gauge producer (this
lane) — Sol or GPT-5.5. Inputs: Moh p.209 (PDF page 70), Prop 6.3,
QUALIFICATION on 17(tttt), FALLACY-v2. Stop at 90 min or at the
first unsourced equation. This is the (99,66) seat.

Second, not simultaneous unless seats are free: Card III (four
face-ODEs; paper-grade for THEOREM 8.1 split). Card II (RANK at
t = 3) is the K16 successor and should not wait on (A), but it is
not the first lane: t = 3 is a control against length 90, and
uniformity is not in a 90-minute sympy job.

Do not launch: disc/Fitting MINOR-EMPTY; `order_basis_full` for
(27,18); fixed-t cone at t ≥ 7; a 166-group batch.

---

## OPENS RAISED

**OPEN[A-TEN-COEFF].** Decide the 10-coefficient Appendix II chart of
Moh p.209 for `(27,18; M_2=21, V_2=8, δ_2=−1, δ_1=0, J=X^4)`.
Quantity: 10 unknowns, one ideal over Q. Cheapest test: Card I.
Do not identify this chart with the 14-unknown 17(tt) slice or with
the ~126-unknown full-D1 over-approx (this session: K=9, u'=1,
δ_1'=0, B=−1, h-inventory 17).

**OPEN[K16-RANK-CURVE].** Decide Criterion RANK for all t ≥ 3: (i)
the 2(t−1) forms `(B_r, C_r)` have only the origin in common, and
(ii) on every nonzero point of the expected curve `rank N = 1` with
ker spanned by a vector of nonzero first coordinate, some eliminant
`W_r` is nonzero. Quantity: at t = 3, one curve in
`A_3[b_4, q_{2,0}]`; lengths 90, 572 are controls at t = 3, 4.
Cheapest test: Card II. Sufficient criterion `V(W)=0` is not this
OPEN (it is strictly stronger; Bézout `2^{t−2} L_t`).

**OPEN[XU-8I-FACE-ODE].** Decide Xu §8(i) for
`δ ∈ {4/3, 3/2, 5/3, 7/3}`: equation (7.1) forces
`q = p^{13}(π−c)` and that equation cannot hold. Quantity: four
finite face-ODE systems. Cheapest test: Card III. The den-bound
`den(δ) ≤ u_s` is *not* this OPEN (N5, 17(rrr)/(sss)).

**OPEN[U-NEGATIVE-CHART]** (carried, sharpened). Decide the
Theorem-1.2 chart when `V_2' > d_2'` after Prop 6.3. Quantity: 20
rows in the 14:41Z appendix2 partial (untyped), a distinct class
from split-joint and from licensed s'=2 order charts. Cheapest
test: one smallest U-NEGATIVE 4-tuple, source-read of Thm 1.2
hypotheses, no analogy from the A/B chart. Do not fill by cap.

**OPEN[PROP63-UNSPLIT-LICENSE]** (new, bounded). Decide whether
every unsplit `u_s ≥ 2` row of the operative screen satisfies
Prop 6.3's `δ*_{s−1} ≥ v_s/u_s` (so it descends) or fails it (so
it is neither (H1) nor (H2)). Quantity: the unsplit residue of the
166 `u_s>1` groups at D ≤ 200, starting at one row other than
(99,66)(A) and D = 108. Cheapest test: compute `δ*` on the smallest
unsplit `u_s=2` row; compare to `v_s/u_s`. Missing route-to-state
data means fallback, not a cap.

Not raised: cofinal emptiness of the screen; a closed-form `ρ_t`;
"Keller pairs cannot split"; a 166-group census as a theorem; a
Fitting-row theorem; Z-EMPTY as an all-t target; (V0) for all t
(that is OPEN[K16-RANK-CURVE]); original `(25,15)` as a CE.

---

## 14. Desk CAS

Stdlib + SymPy, one core, no AWS, no `sat()`. Drivers under
`box/*-20260903/` read, not edited.

- **`L_t` identity.** For t = 2..10,
  `2·binom(3t+1,t−1) = ∏_{d=2t+2}^{3t+1} d / ((t−1)!(t+1))`
  equals 14, 90, 572, 3640, 23256, 149226, 961400, 6216210,
  40320150. Matches CI-SERIES and every banked measurement.
- **Full-D1 unknown counts** from `order_basis_full.py` closed forms
  (inventory only, no Singular):

  | row | K | u' | δ_1' | B | h | ~unk |
  |---|---:|---:|---|---|---:|---:|
  | (27,18;21;8;k=4) (A) | 9 | 1 | 0 | −1 | 17 | 126 |
  | (24,16;18;7;k=4) | 8 | 1 | 0 | −1 | 15 | 112 |
  | (25,15;21;2;k=2) | 5 | 3 | 7/5 | −1/5 | 9 | 143 |
  | (16,12;13;3;k=1) | 4 | 1 | 1/4 | −1/4 | 4 | 44 |
  | (28,20;25;3;k=1) | 4 | 1 | 2/7 | −1/7 | 4 | 73 |
  | (15,10;11;3;k=2) | 5 | 2 | 1/2 | −1/2 | 8 | 63 |

  Banked meta agrees on (25,15) 141 and D=108 110 (off-by-one is the
  `+c` saturation variable).
- **TSV degree / band census.** (25,15) part 3: 598 rows, identifier-
  degree `{1:6, 2:32, 3:48, 4+:512}`, `h_power`
  `{0:102, 1:102, 2:102, 3:102, 4:85, 5:60, 6:35, 7:10}`, linear
  rows only at h=6 (2) and h=7 (4). D=108: 472 rows, linear only at
  h=3 (1) and h=4 (7).
- **Moh p.209.** `pdftoppm` PDF page 70 (journal 209): 10-coefficient
  method printed above the (27,18) table; "the above mentioned
  method can be used to reduce the number of coefficients to 10."
- **Xu §8.** `pdftotext`: (i) is the one-sentence deduction for
  `δ ≠ 2, 5/2`; (ii) and (iii) proved; den ≤ 3 asserted; (99,66)
  left open at δ = 5/2 in 2016 (now killed in the joint chart).

No `charge_basis`. No ledger edit.

---

## 15. What I am not claiming

- (V0) for all t. That is OPEN[K16-RANK-CURVE].
- That CI-SERIES implies dim = 0. DEGREE-BLIND forbids it.
- That RANK is proved at any t by this submission. t = 3 is a card.
- Necessity of Moh's 10-coefficient chart beyond p.209's claim. Card I
  has to source every row.
- That N1 dies under the operative screen. Eight rows, not run here.
- That Xu 8(i) is false. It is unproved; four ODEs decide it.
- A uniform MINOR-EMPTY theorem. Disc is dead; Fitting is the same
  mistake; the census is the deliverable.
- That high-`h` banding finishes (25,15). It is the first linear
  block of 6 rows.
- That (H1)∧(H2) is plane JC2. It is Moh's two-point line, leaky.
- A new exit price.
- Any identification of flag, place, and cover series; of (25,15)
  with (27,18); of 17(tt)'s 14 unknowns with Moh's 10; of
  `REPRESENTATIVE` with a pair; of `V(W)=0` with Criterion RANK.

## COLLISIONS

`python3 ops/open_collision.py xmodel/ideation-20260904T1200Z-grok46.md`
returned `status: CANDIDATES` (review candidates, never closures).
Same-round `ideation-20260904T1200Z-*` submissions were excluded by
the corpus guard; they were not opened. The named hsop lane is now
`final_status=DONE`, so its sealed report and the post-harvest AUDIT
lines are in the corpus.

- `OPEN[A-TEN-COEFF]`: NONE. New.
- `OPEN[K16-RANK-CURVE]`: hits AUDIT 17(mmmmm) (length measured, dim
  open), the harvested hsop integration (TAIL-SPLIT / CRITERION RANK
  as the residual, not a proof of (V0) for all t), notes.md 12:05Z
  EVENT, and the prompt of a post-freeze successor
  `k16-rank-criterion-fable5` (not opened; no `final_status` consumed).
  Those lines *state* RANK as the residual; they do not close it. Card
  II is that residual. First lane stays Card I.
- `OPEN[XU-8I-FACE-ODE]`: hits 17(zz) (Xu §8 enumerated; 8(i) the
  unproved sentence), 17(hhhhh) N2, and older (99,66) lift prompts.
  Banked reports *name* the four-δ gap; they do not run the four ODEs.
- `OPEN[U-NEGATIVE-CHART]`: hits notes.md 14:41Z (20 SHAPE-FAIL:U-NEGATIVE
  rows) and lexical "chart" noise in max12 reviews (not opened). The
  14:41Z note is the origin of this OPEN, still untyped. Not a closure.
- `OPEN[PROP63-UNSPLIT-LICENSE]`: NONE. New.

## Seal

The skeleton contained no seal marker. No canonical ledger edited.
`jc2-lean` not inspected. No other `ideation-20260904T1200Z-*`
submission read. The named running lane was opened only after
`final_status=DONE`. No `charge_basis`. Desk CAS only.
<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `45656`.
- Body SHA-256:
  `47733b1b35f013f3906a9b2b8c941b18eca88bcf2ca6749cec4144cf357b1ce0`.
- Frozen basis: `bb2a1a78491a2c85b4c11ee75803f47f1a43d60f`.
