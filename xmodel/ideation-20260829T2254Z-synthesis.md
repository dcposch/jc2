# Synthesis — significant-news ideation round `20260829T2254Z`

Coordinator: Sol 5.6 Ultra  
Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`  
Round lifecycle: **BLIND ROUND CLOSED / CROSS-POLLINATION CLOSED / FOCUSED REVIEWS INTEGRATED AS NOTED**

## 0. Custody and outcome

The same frozen whole-portfolio state was given independently to Sol, a
second Sol context, Fable 5, Opus 5, and Grok 4.6. Sol, Fable, and Opus
returned sealed reports. Grok returned HTTP 402 before model work; its receipt
is retained as `UNAVAILABLE_NO_MODEL_WORK`, not disagreement or a vote.

The blind full-file/body hashes are:

```text
bc66a6271e0ca9096e96455336d4799cd96224825ad15d27c62d62e109c29399
  Sol primary; body 17486 / a07d360e0900b7ef498f49fd83cf1c68001d207e4ff519ece151962e7e8951db
72aa958a0e69606886e035fecca8c85790ec2bb3ebc20c065938879fcdddd297
  Sol alternate; body 28201 / 0477ab88d170343f70b1f353e88d7f112bc35308631228cdf85302c472b83fd5
af5eedb745002a12d6e2a92d7ce828db1fe9d7e3d581826bcf5305154872185b
  Fable 5; body 30831 / fe68a4dbe7e845ab24079f01be92d466703f6335a66277a34808eee32a70da27
d8e7328479232094b1a77ec8560d85e0a70e4288b18e49d506602bd746d6aa36
  Opus 5; body 34609 / 2b9e2f08f4e58252d984944a34e9d78fa5b596bfe1a350aadc073ed39d393b30
```

Adversarial cross-pollination then closed independently:

```text
e1ce01691b8ba4366a2249c3653830e0219f6f611ae5b782b34eb2da183a5971
  Fable full; body 42096 / 0c2adb5a883121d2151fdd92f2ce7182f0cc7b84b3e15961b2d6bd2bc92da599
6e48015c296c9677237daee2a21a20195d5fc346e163dbcbaa68b74e46bab296
  Opus full; body 53234 / 647bca6600e31816c1f7ac14f2b061d7be574cf653d7102ac063375627d0346b
```

Identity is not a vote. The synthesis below takes the weakest conclusion
surviving the attacks and later focused reviews. No proof or counterexample
to JC2 was found, and no finite-jet exclusion is read as a map or attainment.

## 1. Portfolio disposition

No numbered avenue changes global tier. Avenue 2 remains the principal
proof route: the actual-map occurrence/coverage arrow and QCS remain the
highest-value global gaps. Avenues 4 and 36 receive higher execution priority
because the ramified K00 atlas now has exact live clients; this is not higher
counterexample probability. Avenue 31 is active as a Rees/normal-filtration
tool with a concrete K00 client, not as an integrality proof of JC2. The
labelled degree-six PALF sub-lane of Avenue 27 remains lowered after its
upstream fibre-type mismatch; the broader splice/plumbing avenue is unchanged.

The proof-side portfolio stays deliberately plural:

1. construct or kill the off-diagonal fibre-square event module needed for a
   direct QCS rank inequality;
2. exploit the reviewed intermediate-block structure theorem: every
   nontrivial block gives a forced non-`A^2` étale sandwich with a missed
   branch divisor, while the former codimension-one/minimality route is
   closed;
3. seek an actual-map atypical-value/ACV count only with the Keller partner
   and boundary dictionary explicit; and
4. preserve the global selector/occurrence route rather than importing the
   conditional `td=12` laboratory into an untyped map.

The counterexample/falsification portfolio remains: source completeness and
reachability first; K00 exact ramified cells second; algebraization only after
a typed compatible all-order object exists. The closed literal integer atlas
does not close ramified DVR incidence or other supports.

## 2. Unloaded K00 geometry: sharp theorem and corrected object

Let `I` be the seven-row unloaded ideal and `J` the four-generator ideal of
the exact surface graph. The blind pigeonhole argument gave
`J^17 subset I subset J^2`. Fable's focused hostile reconstruction sharply
strengthened this to

```text
J^5 subset I subset J^2,
J^4 not-subset I,       I not-subset J^3.
```

Thus `sqrt(I)=J`; for `K=J/I`, `K^5=0` but `K^4!=0`; and
`Proj(gr_K(A/I))` is the empty scheme after arbitrary base change. The exact
valuation sandwich is `2*v(J) <= v(I) <= 5*v(J)` whenever the ideal values
are finite. It does not compute integral closures or Rees valuations.

A fresh AWS V2 computation and independently rebuilt replay-B confirm all 56
generators of `J^5` and the explicit noncontainment witness `f5^4`. The
sealed report is
`208ecb8e15f1d0601ee2c35ab3e56b73f0643fdc470665e8d12bf5460cdd1a44`
(12,138-byte body `20f8ed14...`). V1 and replay A remain typed `NO_VERDICT`:
the diagnostic firewall rejected both planted and accidental Singular errors
even though Singular returned zero and later text printed PASS.

This refutes the blind identification of the quartic with the nonempty locus
of the true projectivized normal cone. The maximum reviewed quartic theorem
is instead about the seven degree-two normal jets. Their nonzero common-zero
locus is exactly

```text
Delta=S^3+S^2+72*S*T^2+64*T^2-432*T^4=0.
```

The seven-quadric cone is two-dimensional only at the node, one-dimensional
at every other geometric point of `Delta`, and the origin off `Delta`. The
irreducible rational quartic has one node, two conjugate cusps, and a
four-fold tangent at infinity. Its nodal branches have sign-matched flat
limits on the familiar transverse fan rays. This is an incidence of distinct
objects. The seven rows are not a filtered standard basis:
`(Q_1,...,Q_7)` is strictly smaller than `in_J(I)`, which contains every
degree-five normal monomial. Detailed initial-ideal counts and the
multiplicity-four refinement should not become machine dependencies until a
portable replay is frozen.

## 3. Ramified K00 progress after the round cutoff

The independently reviewed all-face theorem now promotes emptiness through
G7 for every positive-or-infinite K6/K2/target boundary face at
`e=2,m=1`, on the declared C6/K10/Jdet opens. No coefficient-blind extension
in ramification index follows.

Two complete transition producers then advanced the first resonance:

- `e=3,m=1`, `ord_tau(d)=1`: Opus independently reconstructs and promotes
  the full reduced point-fan exclusion through G9. The old G8 fixture is
  genuine and is killed at G9; all second and third surface coefficients and
  the relevant normal blocks are retained, while only `k10[0]` can actually
  occur. The in-cell formal-arc exclusion follows by truncation. Binding
  integration is `6c719521...`/body `3c08e46b...`.
- `e=2,m=2`, `ord_tau(d)=2`: the full reduced point fan is producer-empty
  through G10. This is not the substitution shadow of the valuation-one
  cell. A composed arc has only even coefficients, whereas the full cell has
  a new `d[3]` transition at G5 and an explicit non-even component surviving
  G0--G8 before its G9 row-six kill. Different-model review remains required.

Both are finite-jet point-set theorems on one normalized support. Neither is
source completeness, an all-order lifting theorem, an attained arc, a
polynomial map, or a JC2 conclusion.

The literal normalized integer atlas is now reviewed empty after the R4-00
rank-zero and nonzero-rank packets passed independent reconstruction. This
finishes that literal finite-jet atlas only. Ramified `(e,m)`, the C6=0 tip,
K10/Jdet boundary faces, other supports, and the atlas-to-map arrow remain.

## 4. Proof-side new objects and falsifiers

### 4.1 Off-diagonal pair square

`PAIR-SQUARE-QCS/v1` is a genuinely new application of a known fibre-square
object. Its desired map from a reduced pole module to a quotient-collision
event module could prove the QCS rank inequality directly. It remains
speculative: compactification/blowup invariance, the event-module dimension,
and the injection itself are unconstructed. Because the off-diagonal etale
cover alone depends only on monodromy and boundary data, a valid construction
must name where `J(F)=1` enters through the boundary geometry.

### 4.2 Intermediate block surface

Fable's hostile reconstruction turns the proposed codimension-one criterion
into a stronger, but differently directed, theorem. For every proper
intermediate field of a hypothetical non-invertible Keller map, the integral
closure gives

```text
A^2 --g1, etale and quasi-finite--> Y --g2, finite flat--> A^2,
```

where `Y` is a normal affine surface not isomorphic to `A^2`. The branch
divisor of `g2` is nonempty and is missed pointwise by `g1`; its discriminant
image lies in `A(F)`. Thus the codimension-one-image antecedent is always
false, and the alternative `Y isomorphic to A^2` is also impossible. This
route cannot prove minimal-degree primitivity. The honest successor is to
rule out such étale sandwiches, provisionally beginning at block index two.
A CRT/minimal-polynomial repair supplies the missing normalization-fibre
bound, so every closed discriminant point satisfies the promoted upper bound
`#F^{-1}(z)<=deg(F)-d1`. This is not an attainment statement. The citation-
dependent polynomial-parametrization decoration remains withheld; it is not
needed for the promoted structure theorem.

### 4.3 ACV negative control

The proposed pair-independent inequality `#atypical >= s-1` is false. Opus
gave the exact primitive, critical-point-free polynomial
`f=x+x^2*y+x^3*y^2`, with three places at infinity but only one atypical
value. This does not refute a Keller-pair theorem. It proves that the partner
and `J(F)=1` are load-bearing premises and narrows any ACV experiment to
actual pairs with a pinned boundary-place dictionary.

### 4.4 Literal monomial chart

The polynomial-origin divisibility calculation is correct but terminates its
own client. Simultaneous regularity in the literal monomial chart forces
`F,G in K+xK[x,y]`, so the Jacobian lies in `(x)` and the pair contracts the
y-axis. No quasi-finite Keller pair occupies that chart. The rational-
termination descendants stop there; translated, iterated-blowup, Puiseux,
or pole-retaining charts require their own support modules.

## 5. Software and campaign machinery

The three mathematical-software proposals merge into one future transition
compiler: split into exact surface plus normal coordinates, emit literal
grades, decide the newest-block rank/cokernel before nonlinear elimination,
retain tangential slots, and memoize by `(e,m,load calendar,normal-jet
stratum)`. The current exact e3/e2m2 replays are regression fixtures. This
compiler is not yet implemented and must preserve the loaded-source firewall.

The round selected exactly one campaign-systems trial:
`SECTIONED-INDEPENDENT-CALLS/v1`. Opus hostile review found a fail-open
post-write error path and reserved-marker smuggling. Both and all seven
secondary findings were repaired: anchored `O_NOFOLLOW` reads, stable
identity checks, complete filesystem-error typing, reserved vocabulary,
control-path rejection, duplicate-key rejection, an explicitly unverified
`prompt_sha256_declared`, and corrected documentation. Focused tests pass
9/9 in ordinary, `-O`, and `-OO`; the full operations suite passes 47/47 in
ordinary and optimized modes. The repaired bytes remain unwired and require
stable-basis hostile re-review.

One policy incident is recorded without mathematical reinterpretation:
Fable's focused quartic review ran several local Singular jobs, including one
86-second refinement and one abandoned 300-second decomposition, despite the
campaign's AWS-only rule for uncertain/heavy CAS. The report's exact verdicts
stand as review evidence, but no such external prompt may authorize local CAS
again; portable replay or AWS is mandatory for machine reuse.

## 6. Ranked next launch portfolio

1. Review e2m2 in the background and open the missing `e=3,m=2` residue
   without serializing unrelated descendants. The e3 hostile review and
   sharp-exponent AWS replay are now complete.
2. On K00, enumerate the next source-completeness partition rather than
   extrapolating from three closed calendars. Use the sharp `J^5/J^2` window
   as a pruning invariant, not as an all-cell theorem.
3. On the proof side, run the pair-square definition/invariance gate and a
   degree-two étale-sandwich obstruction independently. Keep QCS occurrence
   and primitive monodromy live, but do not reuse the now-refuted
   codimension-one-image or `Y isomorphic to A^2` bridges.
4. Run the narrowed Keller-pair ACV control only if a concrete pair-level
   boundary object is supplied; do not repeat the scalar-polynomial claim.
5. Re-review the repaired sectioned-output prototype in the background. No
   mathematics waits on it.

No broad web sweep was performed in this round. The last sweep clock remains
2026-08-29T03:40Z; absent significant external news, the next broad sweep is
not due before 2026-08-30T03:40Z.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `12596`.
- Body SHA-256:
  `37ef009d2388ae8a02c4b3647eaaa898ea0f29acb2ce2de37f97e1bb5e1ce71c`.
- Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`.
