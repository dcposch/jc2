# Hostile review — Gate-T contact composition V45, `(ord A, ord C, ord R)=(2,5,>=3)`

| Field | Value |
|---|---|
| Target | `cases/max12_812_order2_gate_t_drho_a2d3_composition_v45_20260827/` |
| Overall verdict | **CONFIRMED** at the narrow surviving theorem below |
| Smallest failing identity | none at this contact |
| Smallest omitted later jet through `T=20` | none; first omitted names start at grade 21 |
| Reviewer / model | Grok 4.6 (xAI). Independent hostile rederivation. Producer `PASS` labels, AWS status banners, and any Claude/Opus review attempt are not evidence |
| Method | Independent SHA-256 of every named pin and nested evidence file; independent derivation of the twenty lower vanishings, the 29-entry total-to-D1 map and factors `2`/`4`, the six in-window polar primitives, jet maxima, Hensel jet through `lambda3`, residue `(3/2)*rho^2*cv^2`, and `D(rho*k)`; desk replay of the four three-row syzygies and the `B23` `r=3,4` negative control; source-constructor comparison for the finite-jet identity. No five-minute seven-row expansion, no heavy local CAS, no AWS launch, no web sweep, no canonical-ledger edit, no `jc2-lean` access |
| Repo | `/Users/dc/code/math/jc2` |
| Git HEAD | `418e413593120d19e15e6546eb50c985f4b1f038` (frozen producer bytes, not git cleanliness, are the authority) |
| Date | 2026-08-27 |

The original attack prompt

```text
26ef60d445e9edf6b4909acf97da9f744161bb40e7cfe72a86a47a6db3517051
  xmodel/max12-812-order2-gate-t-drho-a2d3-v45-hostile-review-opus5-prompt-20260827.md
```

rehashes to the required digest. Claude/Opus review attempts are account-capped or failed and are not used. This Grok lane is the canonical different-model review. No file other than this review was written.

---

## Verdict

**CONFIRMED.**

On characteristic zero, after the already reviewed generic-square first-normal, half-weight, and exact reduced `M=0` gates, the single strict unit-load unique-`AC` contact

```text
ord(A)=2,  ord(C)=5,  ord(R)>=3;  d=3;  G=17;  T=20
```

has a finite total-to-charged-`D1` source identity through grade 20. The identity is the 29-entry shifted map generated from the pinned `B23` inventory, with factor `2` on every surviving `C` jet and factor `4` on every surviving `Rs` jet. After the twenty lower contact vanishings, grades `0..16` of all seven rows vanish, no discarded later jet occurs through `T=20`, and the contact-quotiented actual-total rows are the `B23` rows after this renaming. The reviewed `B23` emptiness of the closed-`R` tail on `D(p*k0)` therefore pulls back to emptiness of this total contact on `D(rho*k)`.

The Kummer erratum is consumed: stage-zero `J1+J2` pairs are among the twenty killed jets, and only shifted leading pairs are mapped. The cheap three-row certificate is a negative control, not a substitute: `RA^2/L^2` is in-window at `r=3` (grade 19) and `r=4` (grade 20); the distinct raised subtail `r>=5` is outside this composition.

This is one provisional contact. It is not a strict unique-`AC` cover, a generic-square cover, an equality-face or positive-order-load statement, a theorem at `k=0` or `rho=0`, a staged Rees-chart or terminal-receiver theorem, either global `G2` obligation, Gate T, order two, maximum twelve, JC2, or a counterexample.

**CONFIRMED**

---

## Verdict table

| Item | Charge | Verdict |
|---|---|---|
| Actual-total G20 custody | V44 / V44R1 / failed parser / V44R3 exact-Q, `F_65521`, cross-lane; seven row and canonical hashes | **CONFIRMED** |
| Seven unspecialized-before-quotient rows | live V44 emitter rebuild, then contact quotient; no face-row relabel | **CONFIRMED** |
| Twenty contact vanishings / support completeness | `ord(A,C,R)=(2,5,>=3)`; grades `0..16`; every source jet through `T=20`; omitted later jets; `k2c` trap | **CONFIRMED** |
| Exact maxima `p/A/C=3,R=1,k10=2,k6=k2=0` | import from `B23` then fail-closed against total support; inactive families distinguished | **CONFIRMED** |
| Finite total-to-D1 map and factors `2`/`4` | all 29 entries; direction total-to-D1; factor `2` on `C`, factor `4` on the whole `Rs` family | **CONFIRMED** |
| 147 row coefficients per deck | coefficientwise identity of seven rows, grades `0..20`; second deck is `rho`-even tautology | **CONFIRMED** (147 independent; 294 not independent) |
| `B23` endpoint import | inventory, compiler, exact-Q transcript, promotion, hostile review; six primitives; recurrence; target/load freedom | **CONFIRMED** |
| Both moving-root / deck orientations through `lambda3` | Hensel identities; `B23` symbolic plus/minus allocations; V45 samples do not replace `B23` | **CONFIRMED** |
| Exact residue `(3/2)*rho^2*cv^2` | `(3/8)(2 lambda cv)^2` after opposite-root allocation | **CONFIRMED** |
| Localization `D(rho*k)` | pullback of `D(p*k0)` after named gates; no silent extra inversion | **CONFIRMED** |
| Direct three-row syzygy | negative control only; `RA^2/L^2` blocks `r=3,4`; `r>=5` is a distinct raised subtail | **CONFIRMED** as negative control |
| Replay adequacy | AWS compiler computes the identity; desk attacks rederive the map/Hensel/residue/primitives; validator is a contract checker | **CONFIRMED** |
| Scope / firewall | one contact; listed exclusions held | **CONFIRMED** |

No item is **REFUTED**. No item is **GAP**. Documentary overclaims (treating the second deck as extra orientation evidence; wording “derive the maxima” for an imported then fail-closed inventory) are repaired in the surviving theorem; they do not break an identity.

---

## Strongest exact surviving theorem

After the reviewed generic-square first-normal, half-weight, and exact reduced `M=0` gates, in characteristic zero, on the localized chart `D(rho*k)`, the strict unit-load unique-`AC` contact

```text
ord(A)=2,  ord(C)=5,  ord(R)>=3,  d=3,  G=17,  T=20
```

satisfies all of the following.

1. The seven unspecialized general-`rho` actual-total rows through grade 20, rebuilt from the pinned V44 emitter and the frozen 569 tails before any face or contact quotient, match the V44R1/V44R3 rational canonical hashes

   ```text
   Tg20_1  22abf65561092b6e792e4888beb88ec3dab2ba212e02c03144f8c0170ced4569
   Tg20_2  0976519e0817e5ff6a6d38ced8bec5bf9007fd847e3c84e42b112bb817282c54
   Tg20_3  0b5e234b2221d7a4287a175776ce8ddd353ebe2d8e0f2b2c20654f4ac14f4b52
   Tg20_4  48644b59b02afdd09f449ff8e2cc0154e1d678d0f3eee76aea4371565d98661e
   Tg20_5  1337ae0295463210d1f415dde66209c2bfe57510dbb45cdb3f6c7501b98eed20
   Tg20_6  5dd5b9f141903e8aa08acb500bea895a8435d9f395ca959b9bed3e6678977456
   Tg20_7  5c17a3dd96b17c5a82fd9f160536d109fb79387f3ba60c0cbd1728bb95be438d
   ```

   These are general-`rho` rows, not `rho=0` face rows.

2. The twenty lower vanishings of this contact are exactly

   ```text
   a0, a1, aa0, aa1,
   c0, c1, e0, e1, ee0, ee1, ez3, ez4, ec3, ec4,
   cs, cs1, cs2, rs, rs1, rs2.
   ```

   After imposing them, every coefficient of all seven rows in grades `0..16` is zero (`119=7*17` zeros), and no variable outside the 29-element finite-jet support occurs through grade 20.

3. The finite total-to-`D1` map has exactly 29 entries. It is a ring homomorphism on jets, in the direction *total surviving names to charged `D1` names*, generated from the pinned `B23` block `(a,d,r_floor)=(2,3,3)`:

   ```text
   rho |-> lam
   ell1,ell2,ell3 |-> ell1,ell2,ell3
   aaa1,az3,az4,az5 |-> a1D, a1D_1, a1D_2, a1D_3          (factor 1)
   aaa0,ac3,ac4,ac5 |-> a0D, a0D_1, a0D_2, a0D_3          (factor 1)
   ez5,ez6,ez7,ez8 |-> c1D, c1D_1, c1D_2, c1D_3            (factor 2)
   ec5,ec6,ec7,ec8 |-> c0D, c0D_1, c0D_2, c0D_3            (factor 2)
   cs3,cs4 |-> b1, b1_1                                    (factor 1)
   rs3,rs4 |-> b0, b0_1                                    (factor 4)
   k,k1,k2c |-> k0, k0_1, k0_2                             (factor 1)
   k6 |-> k60,   k2 |-> k20                                (inactive)
   ```

   Factor `2` on `C` is forced by the mismatch of the total formula `n1=(1/2)(P Az + Ez)` against the `D1` formula `n1=(1/2)P Az + Cz`. Factor `4` on the whole `Rs` family is forced by the total formula `R=(1/4)(P^2 + sigma^2 Rs)` against the `D1` formula `kr=P^2/4 + sigma^2 b0`. The naming `k2c` is the second `k10` jet; it is not the leading `k2` load.

4. Relative jet depths through `T=20` are `p/A/C=3`, `R=1`, `k10=2`, `k6=k2=0`. The first five have actual terminal source witnesses `ell3`, `az5/ac5`, `ez8/ec8`, `cs4/rs4`, `k2c`. The last two are inactive leading declarations: surviving support of `k6` and `k2` is empty through grade 20. Depth zero still introduces the stem in the ring; it does not assert a source witness.

5. The seven contact-quotiented total rows equal the separately coded charged `D1` `B23` rows at every grade `0..20`. That is 147 coefficient identities. Because the unspecialized rows are `rho`-even, the second deck `rho |-> -lam` is the same identity, not a second orientation theorem. Window-hash agreement for `g=17..20` is the same tautology.

6. The pinned `B23` endpoint, already promoted after a CONFIRMED different-model review, empties this closed-`R` tail on `D(p*k0)` by the moving-root functional

   ```text
   N(lambda)=Phi4 + lambda*(Phi3+(P/4)*Phi1),
   lambda^2 = -P/2,
   ```

   with both opposite-root allocations, lower coefficients zero in grades `G..T-1`, and terminal coefficient `(3/2)*lambda^2*cv^2`. Transporting by the map and `lambda^2=rho^2` gives residue `(3/2)*rho^2*cv^2`. The localizer pulls back as `D(rho*k)`: `p=-2 rho^2` so `D(p)=D(rho)` in characteristic zero, and `k0` is the image of leading `k`. Exact `ord(C)=5` supplies the unit `cv`. No further element is inverted.

Consequently this contact is excluded on `D(rho*k)`. Raising `ord(R)` above 3 delays every `R`-family and cannot create an earlier local double pole, so the closed tail `ord(R)>=3` is included.

---

## Smallest additive repair

The identities hold. The producer text is slightly stronger than the evidence in three documentary places. The smallest repair, which a narrow `AUDIT.md` paragraph must incorporate rather than copy `RESULT.md` verbatim, is:

1. Replace “294 exact equalities” as orientation evidence by “147 independent coefficient identities; the minus deck is the `rho`-even tautology of the plus deck.” Keep the AWS census of 294 as a mechanical double run of the same identity.
2. Replace “derive the finite maxima” by “import the reviewed `B23` jet maxima, generate the 29-entry map from them, then fail-closed: no surviving later jet, and terminal witnesses for every active family.” Explicitly label `k6` and `k2` inactive.
3. Separate symbolic from sampled: `B23` both-orientation emptiness is a symbolic identity in the pinned exact-Q transcript; V45 `orientation_check` is two rational samples of Hensel identities that are true as power-series coefficient comparisons through `lambda3`. Samples do not replace `B23`.
4. Record the three-row interface as a negative control only: `RA^2/L^2` at grades 19 and 20 blocks the cheap certificate at `r=3,4`. The raised subtail `r>=5` is a different theorem.

No algebraic repair of the map, residue, localizer, or G20 rebuild is required.

---

## Eligibility for narrow `AUDIT.md` promotion

**Eligible**, of the surviving theorem above, as one contact only.

Eligible means: a different-model hostile review has independently rederived the finite-jet identity and the localizer, rehashed the G20 and `B23` custody, and confirmed the exclusion on `D(rho*k)` after the named generic-square gates. The promotion paragraph must use the surviving theorem and the four documentary repairs. It must not promote the strict unique-`AC` fan, equality faces, positive-order load, `k=0`, `rho=0`, staged Rees or terminal receivers, `G2-PSC`, `G2-BD`, Gate T, order two, maximum twelve, JC2, or a counterexample. It must not treat the `r>=5` three-row subtail as part of this contact.

The producer already labels the exclusion provisional pending this review. After this `CONFIRMED` verdict the provisional label may be replaced by the narrow promotion above. This review does not itself edit `AUDIT.md`.

---

## 1. Actual-total G20 custody and the seven unspecialized rows

### 1.1 What V44, V44R1, and V44R3 actually prove

V44 constructs the seven unspecialized general-`rho` actual-total rows through grade 20 from the V33/V35 source extended by one jet in every family that can enter at grade 20, the V28 row functor, and the frozen 569 tails. It records `general_rho_rows_constructed_before_face=true`: the `rho=0` ordered-`a1` face is applied only after construction, as an unrelated anchor. Independently, `build_source_series_20` begins `P[0]=-2 rho^2` and never kills `rho` or `a0`. A hash is not a formula theorem; the formula theorem is this emitter.

V44 then imposed the wrong receiver postcondition: it rejected the correct *absence* of ten terminal source jets from the receiver rows. That is a false fail, not a false construction.

V44R1 is a one-site transform of the same V44 bytes: it flips the receiver gate to “must be absent”, retags the lane, and records `terminal_grade20_receiver_free=true`. Both exact-Q and `F_65521` compilers passed. The V44R1 *validator* failed closed: its recursive post-parser exhausted the default recursion limit (V44R2 then hit Python 3.12’s AST constructor on the largest left-associated row). Compiler PASS plus validator FAIL is the preserved lineage, not a rewrite.

V44R3 replaces only the parser, by an iterative split of serialized `+`/`*`/`^` terms, wrapping the pinned V44R2 validator. The successful lane name `v44r2_compat_v44r3` discloses the first incompatible registration spelling. V44R3 proves: the immutable V44R1 compiler outputs parse, the seven rational canonical hashes agree across exact-Q and `F_65521`, and the coefficientwise modular shadow matches on two hosts. It does not prove a contact identity.

Term census (exact-Q, pinned in V44R3 `RESULT.md` and both validator results):

```text
row:       1     2     3     4     5     6      7
terms:   691  1451  2681  1460  6209  2974  11091
total: 26557
```

Serialized exact-Q row hashes, independently recomputed on the seven frozen `.poly` files and matching both V44R1 exact-Q `row_sha256` and V44R3 `RESULT_q.json`:

```text
Tg20_1  5798ba4114619a70bdf3b8e0f3c7b7f395ee7a975057ac70fe03eb78ca91b621
Tg20_2  049209af36cd6e84bc968d8cc65edb4a2e7ba42a445654f690cae033302ec8e2
Tg20_3  d5447387de383e9359daf53fe0d109dd372219f22325fccae0be06909cf4bb0b
Tg20_4  adcc88ca36948b30c8561eaf68a0d72d87c5714b160d26d8dcd67b9593f85698
Tg20_5  93bb5499f10b33f93e01d6a8233b8e2f24699430600c58d56cf2d0b9356307b4
Tg20_6  60390cdcf399fea5f1f36ff84179a3193a6e39584e318bf672dcf781fc37a3a4
Tg20_7  ee8b6a83ec4d5b1cee9a3cc178b34d7f4e8f11250965a565424603409dfbf9fa
```

The `F_65521` serialized hashes differ, as they must; the rational canonical hashes and the through-20 truncation hashes are identical on both characteristics.

### 1.2 What V45 actually does with that custody

V45 pins V44, V44R1 compiler, V44R1 exact-Q `result.json`, V44R3 freeze and both validators, and the three V44R3 result objects. It loads the V44 *emitter* `build_source_series_20` (not the V44R1 wrapper body), rebuilds all seven rows through grade 20 with V28 and the 569 tails, then checks:

- V44R1 status `PASS-ACT-TOT-G20-CUSTODY-V44R1-COMPILER`, characteristic 0, `compiler_sha256` the V44R1 wrapper, `base_compiler_sha256` the V44 emitter, `terminal_grade20_receiver_free` true;
- V44R3 statuses `...-Q`, `...-P65521`, `...-CROSSLANE`, with `v44r1_compiler_result_sha256` / `q_result_sha256` equal to the pinned V44R1 exact-Q result;
- each serialized `.poly` digest equals V44R1 `row_sha256`;
- each *live rebuilt* grade-20 polynomial’s canonical digest equals V44R1 `grade20_rational_canonical_sha256`.

Only after those checks does it impose the contact kill. That is a genuine unspecialized-before-quotient rebuild, not a relabel of a face row and not a treatment of a hash as a formula. The seven live grade-20 canonical matches are the formula check; the seven `.poly` hashes are the serialization pin; V44R3 is the parser/cross-lane pin.

V45 loads the typed census parser module and never calls it. That is dead custody, not a silent use of the failed recursive parser.

Prefix grades `0..19` of the live rebuild are not separately pinned in V45. They are the same functor whose grade-20 slice matches, and V44 itself matched 140 prefix equalities against V35. This is not a hole in the contact identity: the contact kill is applied to the live series at every grade, and the first in-window polar is at 17.

**G20 custody: CONFIRMED. Seven unspecialized-before-quotient rows: CONFIRMED.**

---

## 2. Contact ideal, twenty vanishings, support completeness, `k2c`

### 2.1 The twenty lower vanishings, independently

Exact orders `ord(A)=2`, `ord(C)=5`, `ord(R)>=3` kill every jet of each family strictly below that order. The total naming convention, read from the V44 emitter and not from V45’s result table, is

```text
Az: a1, aa1, aaa1, az3, az4, ...
Ac: a0, aa0, aaa0, ac3, ac4, ...
Ez: c1, e1, ee1, ez3, ez4, ...
Ec: c0, e0, ee0, ec3, ec4, ...
Cs: cs, cs1, cs2, cs3, ...
Rs: rs, rs1, rs2, rs3, ...
```

Killing indices `<2` in `A`, `<5` in `C`, `<3` in `R` produces exactly the twenty names listed in the surviving theorem. They coincide with `lower_contact_vanishings` in the frozen `result.json`. Intersection of killed names with the 29 mapped names is empty: the contact ideal and the finite map do not overlap.

The Kummer erratum is enforced by this kill: `delta_D1` annihilates `J1+J2=(rs,cs,c0,c1,a0,a1)`, and those six names are among the twenty. Stage-zero root matrices are not used. The mapped pairs start at `aaa0/aaa1`, `ez5/ec5`, `cs3/rs3`.

### 2.2 Grades `0..16` vanish independently of the AWS census

The first polar primitive of this contact is unloaded `AC/L` at

```text
G = 10 + a + c = 10 + 2 + 5 = 17,
```

coefficient `3/4`, pole 1. Independently enumerating every in-window polar (Section 4) gives six primitives, all of first grade at least 17. After the twenty vanishings there is therefore no source monomial of grade `<17`. Seven rows times 17 grades is 119 zeros. The AWS census `pre_G_zero_coefficients=119` is the live-row confirmation of this inventory bound, not an independent generator of it.

### 2.3 Every source jet through `T=20`; omitted later jets

Jet ceilings on a contact of terminal grade `T` are `T` minus the first grade of a primitive carrying that stem, or `0` if none occurs (and the stem is still declared). Independent computation from the six primitives:

```text
p  = 20 - 17 = 3          (moving P, first polar at G)
A  = 20 - 17 = 3          (AC)
C  = 20 - 17 = 3          (AC; C^2 at 20 gives 0)
R  = 20 - 19 = 1          (RA^2 and both k10 R-families at 19)
k10= 20 - 18 = 2          (k10 A^2 at 18)
k6 = default 0            (k6 C first at 17+c=22)
k2 = default 0            (k2 R first at 22+r=25)
mu2= mu4 = 0              (targets at 28 and 32)
```

These are exactly the pinned `B23` `jet_maxima`. Relative to the contact starts `A` at 2, `C` at 5, `R` at 3, the last in-window names are `az5/ac5`, `ez8/ec8`, `cs4/rs4`, `ell3`, `k2c`. First omitted names (`az6`, `ez9`, `cs5`, `ell4`, `k10_3`, …) have first possible grade `21`. The unspecialized grade-20 support of row 1 contains forty-five such later names; after the twenty vanishings none of them survives. That is the fail-closed completeness check `later-jet dependence`. It is not a claim that those jets are absent from the unspecialized rows.

### 2.4 The `k2c` naming trap, and inactive `k6`,`k2`

The total `k10` series is named `k, k1, k2c, k10_3, ...`. The total `k2` series is named `k2`. These are different families. The map sends `k2c -> k0_2` and `k2 -> k20`. Surviving support contains `k2c` at grade 20 in rows `1,2,3,5,7` and contains `k2` nowhere. A collision that sent `k2` to `k0_2` would have been a false map; it is not the map that was generated.

`k6` occurs in the unspecialized grade-20 support, but every occurrence is in a monomial that also contains a killed lower jet: surviving support of `k6` is empty. `k2` does not occur even unspecialized in row 1 through grade 20. Depth zero for these two families means “leading stem declared, no extra jet, no in-window primitive.” It does not mean a leading load is present. V45’s `maximal_source_witnesses` correctly omits them and requires witnesses only for `p,A,C,R,k10`.

Row 6 has empty surviving support in every mapped name: after the contact kill, row 6 is the zero polynomial through grade 20. That is an extra vanishing, consistent with the identity (the `D1` row 6 is then also zero) and not used as an obstruction. Rows `1,2,3,4,5,7` carry the witnesses.

**Contact/support completeness: CONFIRMED. Maxima, with inactive families distinguished: CONFIRMED.**

---

## 3. Finite shifted map, factors `2` and `4`, 147 coefficients

### 3.1 Independent derivation of the 29 entries and the two factors

Compare the two source constructors, which are the only load-bearing formulae.

Total emitter (`build_source_series_20`):

```text
P[0] = -2 rho^2,   P[i] = 2 ell_i
Cs-series = sigma^2 * (cs + cs1 sigma + ...)
R-series = (1/4) (P^2 + sigma^2 Rs)
Az, Ac, Ez, Ec unshifted in sigma
n3 = sigma^3 Az,  n2 = sigma^3 Ac
n1 = sigma^3 * (1/2)(P Az + Ez)
n0 = sigma^3 * (1/2)(P Ac + Ec)
coeff_5 = 2 * Cs-series
coeff_4 = P^2 + 2 R-series
```

Charged `D1` emitter (`build_d1_series`), already at this contact’s shifts:

```text
P[0] = -2 lam^2,   P[i] = 2 ell_i  (i=1,2,3)
Az = sigma^2 a1D,  Ac = sigma^2 a0D
Cz = sigma^5 c1D,  Cc = sigma^5 c0D
kc = sigma^5 b1,   kr = P^2/4 + sigma^5 b0
n1 = sigma^3 * ((1/2) P Az + Cz)
coeff_5 = 2 kc
coeff_4 = P^2 + 2 kr
```

After killing indices below `(2,5,3)`:

- `P[0]`: `rho |-> lam` sends `-2 rho^2` to `-2 lam^2`. Direction is total to `D1`.
- `A`: surviving `aaa1 = Az[2]` sits at the same series place as `sigma^2 a1D`. Factor 1.
- `C` in `n1`: total contributes `(1/2) Ez`, `D1` contributes `Cz`. Matching requires `Ez_surviving = 2 Cz`, i.e. factor `2` on every surviving `Ez`/`Ec` jet. The first surviving names are `ez5/ec5`, matching `sigma^5`.
- `R` z-part, `coeff_5`: total `2 sigma^2 Cs`, after `cs3` is `2 sigma^5 b1` if `cs3 |-> b1` with factor 1.
- `R` constant part, `coeff_4`: total extra term is `(1/2) sigma^2 Rs`. `D1` extra term is `2 sigma^5 b0`. Matching requires `Rs_surviving = 4 b0`. Factor `4` applies to the whole `Rs` family, including the first jet `rs4 |-> 4 b0_1`, not only to a scalar constant. Producer wording “constant `R` component” means the non-`z` summand `Rs` as opposed to `Cs`.
- `k10`: both sides shift the three-jet series by `sigma^4`. Names `k,k1,k2c` to `k0,k0_1,k0_2`.
- Inactive `k6`,`k2`: declared by the same pattern, unused in surviving support.

This produces exactly the 29-line table in frozen `finite_jet_map`. Count: `1+3+4+4+4+4+2+2+3+1+1=29`. The map is generated from the inventory maxima by `derive_contact_map`; it is not a handwritten 29-line literal. The maxima themselves are imported from `B23` (repair 2).

### 3.2 Direction and functoriality

`linear_map` substitutes total names by scaled `D1` names and compares to a separately built `D1` row. That is total-to-`D1` factorization: after the contact ideal, the total source *is* the charged `D1` source. Emptiness of the `B23` rows therefore transports.

The row functor `v28.build_row` is polynomial in the coefficient series and linear in each load series, with the same 569 tails on both branches. Source-series intertwining therefore implies row intertwining at every grade. The AWS compiler still compares all seven rows at all 21 grades against a separately coded `D1` construction (`build_d1_series` is not a copy of the total emitter with names rewritten; `n1`/`n0` and the `R` packing differ, which is why the factors exist). That is a real comparison, not a tautology of one function called twice.

### 3.3 The 147 coefficients, and why 294 are not two orientations

Each deck compares 7 rows times 21 grades, including the 119 structural zeros and the zero row 6. Comparing all 147 is meaningful: it is the complete truncated-row identity, not only the `g=17..20` windows whose hashes are recorded.

V44 rejects any monomial with odd `rho` exponent. The contact kill does not introduce odd `rho`. Every surviving polynomial is even in `rho`. The two substitutions `rho |-> +lam` and `rho |-> -lam` therefore yield identical images. Equality of the fourteen window hashes is this evenness, not evidence that both root allocations work.

The genuine independent count is 147. The AWS double run of 294 is a mechanical check that the evenness survived the kill and the map. It is not a second orientation theorem. Repair 1.

**Finite map and factors 2/4: CONFIRMED. 147 coefficients: CONFIRMED as one identity, not two.**

---

## 4. `B23` endpoint import

### 4.1 Pins

Independently rehashed and matching:

```text
b2fe07fdd2f855598dde5c6ef9828909c94496eb5e196756eff504056cde2769
  compile_local_row.py
c8eb56409a49ff6ad76d496c7ba38645eceb82b02a9bb67a5cce987e128d5f48
  aws_v7_q/compiled/source_inventory.json
ad447f4daae1c62374c392e8fb297052fde2eafe20efcf8ba1595384ff457775
  aws_v7_q stdout (byte-identical on F_65521)
8b92c22bebae73e3efd793c7c7541c53caf8e956f5ce181240c164f3362864da
  promotion
841f0d6ccbb596fa4ace3f08760f039eb288030efa91075112e915d1320592a3
  hostile review (Grok, CONFIRMED, 2026-08-26)
```

V45 requires the `(a,d,r_floor)=(2,3,3)` inventory block to equal

```text
G=17, T=20, a=2, d=3, r_floor=3, s_min=1, maxpole=2,
negative=false, primitive_count=6,
jet_maxima={A:3,C:3,R:1,k10:2,k2:0,k6:0,mu2:0,mu4:0,p:3}.
```

It does. Each of the eight `B23_*` transcript markers occurs exactly once in the pinned stdout, including `BOTH_ORIENTATIONS=1`, `LOCAL_COEFFICIENT=3/2*lam^2*cv^2`, and `ENDPOINT=PASS_EMPTY_CLOSED_R_TAIL`.

The promotion states emptiness of this closed-`R` tail on `D(p*k0)` after the frozen square/`D1` source gates, by the moving-root functional, both orientations, residue `(3/2) lambda^2 gamma^2`. The 2026-08-26 Grok review independently rederived unique first `AC/L`, global pole `<=2`, the `L^2` recurrence, Hensel, both allocations, target-freeness of rows 1/3/4 through `T<=28`, and the unit `(3/2)lambda^2 cv^2`. That review is imported as a pin, not re-executed as Singular. This composition review rederives the in-window primitives, maxima, residue transport, and localizer pullback, which is what an import attack requires.

### 4.2 Six in-window polar primitives, independently

From the four-summand binomial of `f=K^2+sigma^5 D` with `K=L^2+sigma^2 R`, `D=LA+C`, and the `k10` load `sigma^4 f^{5/4}`, the in-window primitives for `(a,c,r)=(2,5,3)` are exactly:

| first grade | family | coeff | pole | local pole at `A`-root |
|---:|---|---|---:|---:|
| 17 | unloaded `AC/L` | `3/4` | 1 | 1 |
| 18 | `k10 A^2/L` | `5/32` | 1 | 1 |
| 19 | unloaded `RA^2/L^2` | `-3/8` | 2 | 1 |
| 19 | `k10 RC/L` | `5/8` | 1 | 1 |
| 19 | `k10 R^3/L` | `5/16` | 1 | 1 |
| 20 | unloaded `C^2/L^2` | `3/8` | 2 | 2 |

Grades: `10+a+c=17`, `14+2a=18`, `12+r+2a=19`, `11+r+c=19`, `10+3r=19`, `10+2c=20`. Unique first polar is `AC` at 17. Unique local double pole at the `A`-root through `T` is `C^2` at 20. Global pole ceiling 2. Pad-plus-one of the pinned miner reproduces the same six signatures; there is no cutoff hole. No `k6`, no `k2`, no `A^3`, no row-2/row-4 target.

Moving `P = p + 2 sigma ell1 + 2 sigma^2 ell2 + 2 sigma^3 ell3`. The pole-two recurrences `h_{k+4}+P h_{k+2}+(P^2/4)h_k=0` for `k=1,2,3` are the compiled `B23` flags `MOVING_L2_RECURRENCE=1`. Target firewall: row 4 subtracts `sigma^{32} mu4`, and `T=20<32`; row 2 subtracts `sigma^{28} mu20`, and `T=20<28`. Load freedom: `k6` and `k2` are out of window; `k10` is present and included.

Opposite-root allocations on `D(p)` with linear leading `A0,C0` and squarefree `L=z^2+P/2`:

```text
plus:  A0=au(z-lam), C0=cv(z+lam), evaluate N at +lam
minus: A0=au(z+lam), C0=cv(z-lam), evaluate N at -lam.
```

These are exhaustive: `Nfirst=0` forces `L` to divide `A0 C0`, and a squarefree quadratic cannot split otherwise without `p=0` or a zero leading factor. Exact `ord(A)=2` and exact `ord(C)=5` exclude `A0=0` and `C0=0`. V45 writes the same pair as `A0=au(z-lambda0)`, `C0=cv(z+lambda0)` with `lambda0=epsilon rho`; `epsilon=-1` is the minus allocation. Stage-zero `(a0,a1,c0,c1)` are not these pairs.

**B23 endpoint import: CONFIRMED.**

---

## 5. Orientations through `lambda3`, residue, localizer

### 5.1 Hensel through `lambda3` is a symbolic identity

`lambda^2 = -P/2` with `P=-2 rho^2 + 2 ell1 sigma + 2 ell2 sigma^2 + 2 ell3 sigma^3` expands as

```text
lambda0^2 = rho^2
2 lambda0 lambda1 + ell1 = 0
lambda1^2 + 2 lambda0 lambda2 + ell2 = 0
2 lambda0 lambda3 + 2 lambda1 lambda2 + ell3 = 0
```

hence, on `D(rho)`,

```text
lambda0 = epsilon rho
lambda1 = -ell1 / (2 lambda0)
lambda2 = -(ell2 + lambda1^2) / (2 lambda0)
lambda3 = -(ell3 + 2 lambda1 lambda2) / (2 lambda0).
```

These are the four coefficient identities of `lambda^2+P/2=0` through `sigma^3`, which is `pmax=3`. They do not depend on a sample. Independently substituting the two frozen rational samples

```text
(rho,ell1,ell2,ell3,au,cv)
  = (1, 2, 3, 5, 7, 11)
  = (3/2, -4, 9/5, -2, 11/3, 5/7)
```

for `epsilon=±1` recovers V45’s recorded jets, including `(-9/2)` and `(2098/1215)`, and returns four zeros. The sample check is a sanity printout of an identity.

Deck exchange: `epsilon=+1` and `epsilon=-1` are swapped by `rho |-> -rho`. That statement is true for the Hensel jet. It is not why the mapped *rows* are equal; those are equal by `rho`-evenness even before Hensel is used.

### 5.2 Which orientation conclusions are symbolic, which are sampled

| Claim | Status |
|---|---|
| Hensel formulae through `lambda3` | symbolic identity; V45 samples it |
| Complementary leading allocation `(z-lambda0)(z+lambda0)=z^2-rho^2` | symbolic identity; V45 samples the constant/`z`/`z^2` coefficients |
| Both opposite-root allocations empty `N` in grades `17..19` and give the residue at 20 | symbolic, in the pinned `B23` exact-Q transcript and the CONFIRMED 2026-08-26 review |
| Mapped row windows equal on `rho |-> ±lam` | true by `rho`-evenness; not an orientation theorem |
| Combined evidence adequate to transport `B23` emptiness | yes: symbolic `B23` plus the independently derived finite-jet identity |

Repair 3: do not advertise V45 `orientation_check` as a symbolic row identity.

### 5.3 Residue `(3/2)*rho^2*cv^2`

After either allocation, leading `C0=cv(z mp lambda)`. At the `A`-root the value is `cv*(±2 lambda)`. The unique local double pole at grade `T=20` is unloaded `C^2/L^2` with coefficient `3/8`. Its `L^2` numerator at that root is

```text
(3/8) * (2 lambda cv)^2 = (3/2) lambda^2 cv^2.
```

Characteristic zero supplies `2` and `3`. Every other in-window family is globally pole 1, or globally pole 2 with enough allocated `A` zeros that the local pole at the `A`-root is at most 1 (`RA^2` at 19 has local pole 1 after two `A` zeros). Simple poles vanish at a root of `L`. Hence grades `G..T-1` of `N(lambda)` vanish and grade `T` is exactly this unit on `D(lambda*cv)`.

On the total side `lambda^2=rho^2`, so the residue is `(3/2)*rho^2*cv^2`. On `D1` it is `(3/2)*lam^2*cv^2`. The map identifies them. Independently, the same two rational samples give `363/2` and `675/392`, matching both V45’s recorded `terminal_residue` and `(3/8)(2 lambda0 cv)^2`.

### 5.4 Localization `D(rho*k)`, no silent extra inversion

`B23` inverts `lam` (equivalently `p`, via `lam^2=-p/2`), `cv`, and the chart unit `k0`. Compiled unit ideal:

```text
( (3/2) lam^2 cv^2,  ilam*lam-1,  icv*cv-1,  ik0*k0-1 ).
```

Promotion names the chart `D(p*k0)`; exact `ord(C)` is already the contact, so `cv` is a unit on the exact-order locus. Pullback along the 29-map:

- `p=-2 rho^2`, characteristic not 2, so `V(p)=V(rho)` and `D(p)` pulls back to `D(rho)`;
- `k0` is the image of leading `k`, and `k` occurs in surviving support, so `D(k0)` pulls back to `D(k)`;
- `lam` is `±rho` at order zero, so `D(lam)` is again `D(rho)`.

The resulting open is `D(rho*k)`. V45 does not invert `au`, `ell`, leading `R`, `k6`, `k2`, or a target. It does not pass to a Rees chart or a terminal receiver. The named generic-square gates are those already assumed by the promoted `B23` statement; they are not a new localization. `k=0` and `rho=0` are excluded by the open, matching the firewall.

**Orientations through lambda3: CONFIRMED. Residue: CONFIRMED. Localizer `D(rho*k)`: CONFIRMED.**

---

## 6. Direct-coordinate negative control

The pinned three-row report and replay rehash to the required digests

```text
2354703a559a5da2b4f2032560a5740b2cda1f3160f5c6598dcdbe1c4c1432be
  xmodel/max12-812-order2-gate-t-strict-uac-three-row-syzygy-interface-sol-20260827.md
56edb06f57c657534b46f1c406363de7821e240ccc1eb7d8529be7eeb2c5c3f4
  xmodel/max12-812-order2-gate-t-strict-uac-three-row-syzygy-interface-replay-20260827.py
```

Desk replay prints `PASS-UAC-THREE-ROW-SYZYGY-INTERFACE`, four syzygies, `CURRENT_B23_R3_R4=REJECT`, `CURRENT_B23_RGE5=ACCEPT`. Independently:

```text
RA^2/L^2 first grade = 12 + r + 2a = 16 + r.
```

For `T=20`: in-window at `r=3` (grade 19) and `r=4` (grade 20); first omitted at `r=5` (grade 21). The four polynomial identities

```text
C1 g2 - C0c g1 = (3/8) A1 D,   ...
```

hold, and they would empty exact nonzero leading `C` given exact nonzero leading `A` on `D(rho)` *if* `Phi1[G]`, `Phi2[G]`, `Phi4[T]` were literally `(g1,g2,g4)` with no extra double pole. At `r=3,4` the extra double pole `RA^2/L^2` contaminates that triple. At `r>=5` it does not.

This composition is the closed tail `r>=3`, which includes the contaminated slices `r=3` and `r=4`. The cheap certificate therefore cannot replace `B23`. The raised subtail `r>=5` is a distinct theorem, not proved here and not needed here. Repair 4.

**Negative control: CONFIRMED. Not a B23 substitute.**

---

## 7. Replay adequacy and scope

### 7.1 What the V45 compiler computes, imports, and samples

Computes, on the registered exact-Q lane `...v45_20260827T031200Z_q`, host `ip-172-30-0-34`, one core, 5m38.30s, 199032 KiB RSS, zero swap, exit 0:

- live rebuild of seven unspecialized rows through grade 20;
- contact kill, later-jet fail-closed, 119 pre-`G` zeros;
- 147+147 coefficient comparisons against a separately coded `D1` source;
- fourteen window hashes and their pairwise agreement;
- two-sample Hensel/allocation/residue printout.

Imports: V44 emitter, V44R1/V44R3 G20 custody, 569 tails, V20/V28, `B23` inventory and transcript markers, `B23` promotion and CONFIRMED review, Kummer producer/review/erratum as hash pins.

Samples: Hensel jet and leading allocation on two rational points, two signs. Not the rows.

The validator checks JSON contracts, stdout tokens, GNU-time `Exit status: 0`, and absence of diagnostic strings. It does not re-expand rows. That is adequate as an AWS contract, not as a second mathematical proof. The mathematical replay of the identity is the audited compiler plus the independent source-constructor derivation in Section 3. The five-minute expansion was not rerun locally, as forbidden; functoriality of `build_row` closes that cut.

Frozen compiler `result.json` digest `ada147715900468e727c3c56b76f99d642bc9ffccce6f81a0dac704372036ad8` matches `RESULT.json`, `EVIDENCE.sha256`, `PASS_EVIDENCE.sha256`, `RESULT.md`, and compiler stdout `RESULT_SHA256=...`. On-host `freeze_check.stdout` records `OK` for every `FREEZE.sha256` row.

### 7.2 Scope

Preregistration, compiler `scope`, `RESULT.md`, and the outcome string all state one contact. Independently, nothing in the map, the `B23` import, or the localizer extends to:

- the rest of the strict unique-`AC` fan, including `(2,4,>=3)` and `(1,4,>=2)`;
- equality faces (`a+3s=d`);
- positive-order leading load, the `a=7` `k6` tie, or `a>=8` load-first chambers;
- `k=0` or `rho=0`;
- staged Rees charts or the terminal receiver;
- `G2-PSC` or `G2-BD`;
- Gate T, order two, maximum twelve, JC2;
- a counterexample verdict;
- the raised three-row subtail `r>=5`.

**Replay adequacy: CONFIRMED. Scope: CONFIRMED.**

---

## 8. Custody hashes independently recomputed

Prompt and producer (all match the attack prompt / `FREEZE.sha256` / `PINS`):

```text
26ef60d445e9edf6b4909acf97da9f744161bb40e7cfe72a86a47a6db3517051  original attack prompt
b380e704a5e1d62471dfbba3afc7c544325d6786bfdae5bffe0ae6a746b3ea5a  PREREGISTRATION.md
ec984c008dec7273fa18c91b753a369a76795b78257423526d6981361d92347e  verify_drho_a2d3_v45.py
75cdc378922f3891796b311edf3b3b1a2b258c365f5db61c7089a75de1e8cb05  validate_drho_a2d3_v45.py
0c0bdbc1b8cc6225747599ab07de8e0cfda350f621218312e39be1733f2686fd  FREEZE.sha256
ada147715900468e727c3c56b76f99d642bc9ffccce6f81a0dac704372036ad8  compiled/result.json
27c7c28db2e18522e5e13134a3581bb818e344fa564fa18d473378472d7f203e  RESULT.json
53ab25b43632ed85484de5a00439f5abeca1d57d5ec9a0f708a00a06772bc169  EVIDENCE.sha256
234cc4021e03e8c40b694e7757bfb2d1bad51e8eafe66c417b231e59550a1af8  RESULT.md
```

Every `PINS` path named in the compiler, including V44/V44R1/V44R3, V20/V28/parser/replay/tails, `B23` compiler/inventory/stdout/promotion/review, and the three Kummer files, rehashes to the digest printed in frozen `exact_input_sha256`. Local AWS run artifacts rehash to the eleven `EVIDENCE.sha256` rows, including compiler stdout `4fb6982171…` and stderr `69aa11df76…`.

Kummer erratum pin `614ddcdb17ae233cd2babea5b45f329f57a7e28e13c7bc0616e78c6bc1731571` is the controlling correction: stage-zero `J1+J2` pairs vanish on this contact.

---

## Appendix. Desk-scale independent checks performed

No sibling replay file was written. The following were executed in this review and are reproducible by hand or by a few dozen lines of `Fraction` arithmetic.

A. Hensel identities through `lambda3` on both recorded samples and both signs: four zeros; residue equals `(3/8)(2 lambda0 cv)^2` in each of four cases.

B. Generation of the twenty killed names and the 29-entry map from `(a,c,r)=(2,5,3)` and the `B23` maxima, including `k2c |-> k0_2` distinct from `k2 |-> k20`, and empty intersection of killed and mapped names.

C. Independent six-primitive table from binomial first-grade formulae; miner pad-plus-one agrees; derived maxima `{p,A,C,R,k10,k6,k2}={3,3,3,1,2,0,0}`.

D. `RA^2` first grades 19, 20, 21 for `r=3,4,5`; three-row replay `REJECT` at `r=3,4` and `ACCEPT` at `r>=5`.

E. Eight `B23` stdout markers each of multiplicity one.

F. Seven V44R1 exact-Q `.poly` digests equal to `row_sha256`; V44R3 exact-Q and `F_65521` share rational canonical hashes and through-20 truncation hashes; cross-lane `q_result_sha256` is the V44R1 exact-Q result.

G. Surviving support: 29 keys, empty only `k6` and `k2`; rows appearing `{1,2,3,4,5,7}`; row 6 identically zero after the kill; every claimed maximal witness nonempty.

These checks attack every item in the original prompt. They do not expand the 26557-term unspecialized rows.

---

## Verdict (restated)

**CONFIRMED**, of the strongest exact surviving theorem in this review, for the single contact `(ord A, ord C, ord R)=(2,5,>=3)`, `d=3`, `G=17`, `T=20`, on `D(rho*k)`, after the named generic-square gates. Smallest additive repair: four documentary clarifications, no algebraic change. Eligible for narrow `AUDIT.md` promotion of that theorem only.

**CONFIRMED**
