You are an independent hostile algebraic-geometry referee.  Review only the
frozen tied square/D1 producer below.  Do not edit any producer, prompt,
ledger, or existing artifact.  Write exactly one report:

`xmodel/max12-812-order2-square-d1-tied-a1c5r2-maxpole2-hostile-review-grok-20260826.md`

End with exactly `CONFIRMED` or `REJECTED`.

Producer directory:

`cases/max12_812_order2_square_owner_d1_tied_a1c5r2_maxpole2_20260826/`

Primary custody:

```text
RESULT.md SHA256
f09b44f18011e592f8366a380d1bba723a223d4c424fbf9cbfa951a8de87d994

PRODUCER_FREEZE.sha256 SHA256
7813393ee756923f73754fd6c3e1ba8e153594ffa7ccf3963926e765761adede

EVIDENCE.sha256 SHA256
d4802e87047438882c5f825980d7bf622436636e4a94c8c8429ad46a826a8be1

source FREEZE.sha256 SHA256
b7f931512b676500fe93504c5598837ad0fa2c834a1e1d9d37504550ffe2ed08

V2 source archive SHA256
4b903ffcb76b3d584f64105b821372d82580ad851415e1538d7dc84bd8a5367d

generic miner specification SHA256
c4eba2d79521d6c621223dc1b36bb298e7a805bf1cd595b593f444d14f148358
```

Claimed scope is exactly the unit-load D1 cell

```text
ord(A)=1, ord(C)=5, ord(R)=2 on D(p*k0), through absolute grade 16.
```

Audit adversarially and independently:

1. Rehash all producer/source/evidence manifests and the three nested
   compiled manifests.  Exact `Q` is the theorem endpoint; `F_65519` and
   `F_65521` are controls only.  Confirm zero swap and validator semantics.
2. Re-enumerate from the frozen four binomial summands every polar primitive
   through grade 16.  Decide whether the census is exactly `AC,A2,R3,RA2`,
   whether all start in grade 16, and whether `RA2` is the unique pole-two
   family.  Do not import the separate support-miner verdict.
3. Inspect the complete seven literal source rows, recursive extraction, and
   analytic/Faber bridge.  Check source/load/target ceilings mechanically;
   in particular, justify why no normal/connection jet and no target can
   occur at the first grade.
4. Reconstruct the pole-two negative-tail numerator.  Distinguish the full
   cleared numerator from its canonical remainder modulo `L^2`.  Verify the
   exact discarded ordinary quotient, all recurrence signs, both root Faber
   functionals, and both derivative-row syzygies.  Decide whether the V1
   literal-equality failure is only a normalization sentinel or conceals a
   mathematical defect.
5. Audit the four root-value charts for nonzero linear `A,R`.  Verify the
   determinant `2*lambda`, the two same-root unit ideals, the two
   opposite-root residual allocations, and the scheme-theoretic unit ideals
   after adding the appropriate derivative functional.  Look for omitted
   root charts, nilpotent/embedded solutions, an illicit saturation, or an
   unjustified exact-contact inverse.
6. In both deck orientations, divide the common numerator by `L` exactly and
   verify the quotient terminals `+/- (5/2)k0*lambda^3*bu^3` and derivative
   terminals `5*k0*lambda^4*bu^3`.  Check that `AC`, `A2`, and divided `RA2`
   vanish at the nominated `A` root and that `R3` cannot cancel there.
7. Check the omitted-`R3` negative control, the statement that the first
   pole functional gives allocation rather than emptiness, etale descent on
   `D(p)`, and every firewall.  Determine whether the old hand-triage sentence
   “at c=5, AC may cancel” is actually refuted, but do not edit that memo.

State the smallest failing identity or earliest missing source family if any.
Producer PASS strings are not evidence by themselves.  Do not broaden the
scope to another face, another load chamber, order two, maximum twelve, or
JC2.
