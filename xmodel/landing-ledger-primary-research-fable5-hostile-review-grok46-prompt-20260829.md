# Hostile review: Fable5 LANDING-LEDGER design and td=6 hand-run

You are Grok 4.6, acting as an independent adversarial mathematical referee.
Review the sealed-body primary report
`/Users/dc/code/math/jc2/xmodel/landing-ledger-primary-research-fable5-20260829.md`.
Its full-file SHA-256 is
`26bdd150a5a4bace23069b65f3234de6080fc7fb083f46a92773ec276a6fc689`;
its body hash above the self-hash line is
`83fe23123f35275e551ef8d1704da1181dea9c346a4915cd0e15c6db82998700`.
Verify both before substantive review.

Read as authoritative comparison sources only:

- `ladder/REDUCTION.md` Critical 4--7 and its live dependency/claim sections;
- `ladder/SHEET6-DEPTH.md`;
- `ladder/SHEET6-MULTIPOLE.md`;
- `ladder/BOOK-OFFAXIS.md`;
- the exact Fable report above.

Do not read other ideation reports.  Do not access, list, search, build,
status, or control `jc2-lean`.  No web, AWS, heavy/local CAS, canonical edits,
commit, or push.  Bounded exact integer/Fraction scratch checks are allowed.

Audit independently, not by stylistic commentary:

1. Is the stored schema actually sufficient?  Attack every claimed derived
   field, especially `rho=kbar/dq` across merges, `M=gcd(dp,dq)` at entry and
   after jumps, `psi`, `w_cert`, zero-direction provenance, and whether later
   merge equations need data the proposed quotient deleted.
2. Is the candidate grammar complete enough to justify a total three-way
   classifier, or does Fable silently inherit a known-open mixed/off-axis
   family?  Distinguish total *typing* from finite/decidable enumeration.
3. Recompute the complete `td=6,m=2` entry, merge, eta-factor, and first
   post-jump arithmetic.  In particular check the infinite family
   `nu_G=1, eps_q=1, l odd>=5`: `(dp,dq)`, `kbar`, `X`, `M`, trunk `w`, all
   stated filters, and whether a missing lambda/admissibility/log constraint
   already kills it.
4. Decide whether `dp|dq` is a real promising bridge to the promoted td=7
   zero-chain law or only a superficial analogy.  State the exact additional
   hypothesis needed and the cheapest theorem/discriminator.
5. Attack A1--A5 and the claimed minimal packet ordering.  A design that
   compiles only the answer it was told is not a completeness certificate.
6. Give a verdict from `PASS`, `PASS_WITH_NARROWING`, `REPAIR_REQUIRED`, or
   `REJECT`, with explicit promoted-safe statements, quarantined statements,
   and minimal repairs.  No JC2/landing/ceiling extrapolation.

Write only
`/Users/dc/code/math/jc2/xmodel/landing-ledger-primary-research-fable5-hostile-review-grok46-20260829.md`
and end with a report-body self-hash.
