# Grok46 hostile review — row-34 zero-tail class-to-raw endpoint bridge

You are Grok 4.6 acting as an independent hostile mathematical reviewer in
`/Users/dc/code/math/jc2`.

The charged producer file is immutable at:

```text
c068afae0b00f0017d886d28e8fa2797a03c1ca0f7ac2b8cd195bbf108f7e537
  xmodel/ggv-quarter-root-r34-gwindow-bridge-r0-sol-ultra-20260828.md
```

Read it completely and fail closed if the hash differs.  Read only the exact
dependency files and frozen source JSON it names when necessary.  Do not
reread mutable canonical ledgers, inspect or traverse `jc2-lean`, touch AWS,
run heavy local computation, or read unrelated ideation submissions.  Small
independent exact desk arithmetic is allowed.

Independently rederive and classify every atom `CONFIRMED`, `REPAIRED`,
`GAP`, or `REFUTED`:

1. the literal F/G window formula, especially `G_21 in <X^3>` and
   `G_22=0`;
2. the full characteristic solution, including the arbitrary X-constant
   series `Phi(s)`, endpoint term and normalization `c_0=1`;
3. the original-coordinate reconstruction
   `G=P_hat^12 W(X,t/P_hat)` and every exponent/sign in its displayed
   expansion;
4. reduction modulo `(u,v)` and whether arbitrary characteristic constants
   can mix into or rescue weight 22;
5. the independent raw recurrence calculation of `D_22`, with every allowed
   `G_16,G_18,G_20`, and the unit-ideal argument over
   `Q[u,v]/(uv,u^4,v^2)`;
6. whether endpoint emptiness is rollback-independent for the displayed F,
   even though its interpretation as a row-34 class section remains
   provisional;
7. all firewalls: this is one zero-tail Artin slice, not the full branch-P
   family, a face exclusion, landing theorem, counterexample, or JC2 result.

Do not merely rerun the producer script.  Give an independent coefficient
derivation, directly attack the possibility that lower/higher `c_m` or
nilpotent corrections alter `G_22`, and state the narrowest promotion and
stop/continue recommendation.  Include hashes, exact replay if useful, and
pinned model/CLI identity.

Write exactly one output and nowhere else:

- `xmodel/ggv-quarter-root-r34-gwindow-bridge-crossreview-grok46-20260828.md`
