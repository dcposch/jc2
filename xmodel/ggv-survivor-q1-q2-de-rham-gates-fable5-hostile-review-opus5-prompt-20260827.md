# Hostile review: Fable5 survivor-curve `q1/q2` de Rham gates

Review only the mathematical atom in §§3.1–3.3 and Card 1 of
`xmodel/ideation-20260827T1606Z-fable5.md` (SHA-256
`bb9c54afd086db383dcab3519f556f65ae5abb134ba3555f57c7a6cdbf045257`).
You are a different-model hostile referee. Reprove from corrected R7R1 and
the D3 raw windows; do not use agreement with your own ideation as evidence.

Charge separately:

1. The exact `q1=F1/(4p^5)` and
   `q2=F2/(4H)-F1^2/(16H^3)` formulas and their licensing rows.
2. On the generic perfect-square branch `H=A^2`, `deg A=4`, `A`
   squarefree, reduce `q1 dX` to the normalization `w^2=A` and prove or
   refute that exact raw numerators `F1 in K[X]_(<=15)` are exactly the
   image of `Q -> 2AQ'-3A'Q` for `deg Q<=12`; check injectivity, primitive
   completeness, infinity/branch behavior, base field, twist invariance,
   and the claimed codimension exactly three.
3. Reproduce or refute the rank-13 fixture for `A=X^4-1`, and supply at
   least one load-bearing mutation outside the image.
4. Audit the `q2` descent-to-`K(X)` statement and the residue `-3/64` for
   `F1=0,F2=1,A=X^4-1`; distinguish one excluded fill from a branch gate.
5. Probe special perfect-square strata (`A` nonsquarefree and `4|delta`)
   and the quadratic-squarefree-part branch enough to bound the claim's
   scope. No generic extrapolation without proof.
6. Audit the strategic surrounding statement that the exact face
   "decouples" into F-gates, unique G-completion and one endpoint. In
   particular account for the lower rows `D1,...,D14` and the post-seal
   correction that generic compatibility already imposes
   `H | F1^2` at weight 2, recorded in
   `xmodel/ggv-degree8-r5-survivor-raw-support-analysis-hostile-audit-sol-ultra-20260827.md`.

For every item write `CONFIRMED`, `REFUTED`, or `GAP/REPAIR`; separate the
q1 theorem atom from any overstatement in the surrounding strategy. State
the maximum promotable scope and cleanest exact successor. Do not review or
consume the Opus5 peer ideation submission, edit canonical ledgers, launch
AWS, run heavy local algebra, or touch/read/status-inspect `jc2-lean`.
Desk-scale exact scripts in `/tmp` are allowed.

Write exactly one repository file:
`xmodel/ggv-survivor-q1-q2-de-rham-gates-fable5-hostile-review-opus5-20260827.md`.
Touch nothing else. End with the report SHA-256 and review verdict.
