# Hostile review: D1 `a=8` complete `k6`/`mu2` transition V3

Read-only hostile review, except write exactly one report:

```text
xmodel/max12-812-order2-square-d1-a8-k6-mu2-v3-hostile-review-grok-20260826.md
```

End with exactly one verdict `CONFIRMED`, `REPAIR`, or `REJECTED`, followed
by `ORDER2_SQUARE_D1_A8_K6_MU2_V3_<VERDICT>`.  Do not edit producer files,
ledgers, or `jc2-lean`; do not run local Singular, Sage, msolve, Lean, or
substantive exact Python.

Immutable target:

```text
67e0c08119e67d410904e63f55b5586131bfa987e4b7d7045c02e24dd6a2e9a7
  cases/max12_812_order2_square_owner_d1_a8_k6_mu2_fullsupport_v3_k60count_20260826/RESULT.md
dcd6c3eb2d390375f1c56bc638215a4e447efe680a4c5f345c18011945514f7f
  cases/max12_812_order2_square_owner_d1_a8_k6_mu2_fullsupport_v3_k60count_20260826/FREEZE.sha256
a3a64018a350a61b02ea53aec7edbd56e159eb8def325d78d05fbd59ecb1effb
  cases/max12_812_order2_square_owner_d1_a8_k6_mu2_fullsupport_v3_k60count_20260826/EVIDENCE.sha256
```

Verify all manifests first.  Then attack every load-bearing point:

1. Establish exact V1/V2/V3 custody.  V1 must be immutable and fail closed
   only because the positive-valuation ring omitted formal `k60`; V2 must
   actually insert the intended six declarations/map images and fail before
   CAS only because one pre-existing q0 occurrence made its global count
   seven.  Confirm V3 changes only the wrapper postcondition and that its
   generated input is exactly frozen V1 plus those six symbol insertions.
2. Check ring and map arity at all six sites.  The new `k60` must have the
   identity image and be absent from every qplus equation, whose load remains
   exactly `sigma*k61+sigma^2*k62`; reject any accidental qplus constant load.
3. Verify that the compiler reconstructs all seven frozen literal
   **Faber/source** tails, not merely Laurent rows, with all `k10,k6,k2`
   loads and every `mu2,mu4,mu6,J` target.  Audit the moving
   Laurent-to-Faber `T0+T1` bridge explicitly.  In particular do not import
   any Laurent-only affine-mu2/P2 claim.
4. Recompute exact source sigma support for fixed
   `ord(A)=8,ord(C)=9,ord(R)>=8`.  On `D(k60)`, prove grade 26 has exactly the
   asserted simple `k60*C/L` module.  On `V(k60)`, prove grades 27/28 are
   exactly `U0+S0 ; U1+KRC+S1+target(mu2)`.  Show no k2, additional k6,
   unloaded, or other target module is in-window.
5. Audit `SOURCE_SUPPORT_FILTER`: allowed variables must be correct at every
   grade and `diff` must not hide omitted dependence.  Verify exact source
   divisibility/quotient identities and both analytic row identities.
6. On `D(k60)`, independently derive the first two coefficients of the
   proper fraction and show that seven-tail vanishing forces `C0=0` on the
   registered chart.  Charge all localization/nonzero hypotheses.
7. On `V(k60)`, derive the leading cleared factor `C0*(A0+k61)` and prove
   the two shifted root maps exhaust the opposite-root allocations for
   squarefree `L` on `D(p)`.
8. Recompute the complete source/Faber transform of the grade-28 `mu2`
   target.  Verify the stated `h2,h4,h6`, signs and constants, and that its
   cleared `L^2` numerator is exactly a multiple of `L`.  Check the moving
   `U1/S1` connection cancellation at `A0=-k61`, all simple terms, and the
   surviving `(3/2)*lambda^2*cv^2` residue in both orientations.
9. Audit all three deletion controls.  Confirm each deletes a generically
   nonzero mathematical module rather than a presentation artifact.
10. Prove the two k6 sections are exhaustive for finite-order/identically
    zero k6: `D(k60)` versus `V(k60)`, with arbitrary `k61,k62` including
    zero.  Verify omitted higher k6 jets cannot enter grades 27/28.  Likewise
    check eta covers exact `ord(R)=8` and every `ord(R)>8` in the claimed
    window without an unbounded substitution.
11. Verify AWS host/tag/rc/validator/diagnostic/resource custody, exact Q
    versus finite-field roles, and that no warning pattern escaped the
    validator.
12. Enforce the firewall: only fixed a=8 on `D(p*k10)` after the cited
    upstream gates, set/arc support rather than scheme structure.  No a>=9,
    p=0/k10=0, positive-order k10, whole-square/order-two claim.  Ensure the
    exact tied-load Chebyshev/Pell survivor is not contradicted.

Return the smallest failing identity or hypothesis.  Do not silently repair
a substantive gap.
