# Adversarial exchange 1435: Fable 5.1 cross report

2026-09-06, lane `ideation-20260906T1435Z-cross-fable5`, basis `0d39df3c`, inputs `/tmp/jc2-lane.TXaYV1/inputs` only. Collection order accepted from `collection.json`: all four blind bodies hash-pinned (Astra `8be796f1…`, Fable `4c5953ae…`, Sol `348bb4bf…`, coordinator `ded6160a…`) before root read any peer; the six-hour gap delayed collection, not completion. Read whole: packet, collection, all four blind reports, `astra_diagonal.py`, the D125 client interface, my D125 contract gate, the triangular normalization producer. Read in part: my normalization gate and the Euler gate (grep for the polynomiality clause), Roy `make_band`/`digest_linear_constraints`/`build_payload` setup, GGV §1 definitions, Remark 2.5, Theorem 2.6, Proposition 2.11 with proof. No live cross report, log, receipt, AWS, CAS, classifier, ledger or `jc2-lean` touched. Nothing here is a point, unit, properness, global-125 or JC2 claim.

## 1. Primary gate: Astra Card 1, terminal-face constant pivots

**Verdict: CONFIRMED as a desk theorem, with one GAP on what it buys.** Every load-bearing step was re-derived from the original source contract, not from Astra's text.

**Faces.** Under `Phi(u)=X^5`, `Phi(v)=Y+X^-1`, a source monomial `u^i v^j` contributes `X^a Y^b` with `a-b=ell=5i-j`, `0<=b<=j`. The terminal halfspaces `5a-17b<=3` and `<=5` are imposed by the jets (`make_band`: `e>=ceil((17ell-h)/12)`), not implied by the supports; my script records raw support weights up to 75 and 125 above the faces. On the face, `5ell-12b=3` with `ell<=15` forces `b≡1 (mod 5)`, giving exactly `(4,1),(21,6)`; `5ell-12b=5` with `ell<=25` forces `b≡0 (mod 5)`, giving `(1,0),(18,5),(35,10)`. The five Roy pins `t^4z^3, t^21z^15, tz, t^18z^13, t^35z^25` are precisely these monomials (`t^e z^L = X^e Y^(e-L)`), so `P*=X^4Y+X^21Y^6` and `Q*=-X-3X^18Y^5-(9/5)X^35Y^10` are forced by source plus pins; my literal bracket gives `[P*,Q*]=X^4`. CONFIRMED.

**Kernel.** With `T=X^17Y^5` (weight 0), any homogeneous `S=X^aY^b g(T)`, `b` minimal, `g(0)!=0`. From the monomial bracket I get `[X^4Y,X^aY^bT^k]=(4b-a+3k)X^(a+3)Y^bT^k` and `[X^21Y^6,X^aY^bT^k]=(21b-6a+3k)X^(a+3)Y^bT^(k+1)`, which sums to Astra's `X^(a+3)Y^b{3T(1+T)g'+[(4b-a)+(21b-6a)T]g}`. Vanishing forces `a=4b`, then `(1+T)g'=bg`, so `S=cP*^b`, weight `3b`. Q differences satisfy the pins, so they have weight `<=4`; only `b=0,1` survive, removed by the gauges `[X^0Y^0]Phi(Q)=0` and `[X^21Y^6]Phi(Q)=0`. CONFIRMED.

**Gauges preserve everything.** `Q -> Q-aP-c` with `a=[X^21Y^6]Phi(Q)` (P's coefficient there is the pin 1) and `c` the transformed constant: `Supp P ⊂ Supp Q`; `Phi(P)` has weight `<=3<5`, so Q's jets and all three Q pins are untouched; `[P,Q]` unchanged; `beta=[u^25v^100]Q` unchanged because `i<=15` in P, so `Z*alpha*beta-1` is preserved; `c` only moves `Q_(0,0)`, on band 0 with jet order 0. Both gauge rows are the first post-jet Taylor coefficient of their band (band 15: `t_min=21`; band 0: `t_min=0`), hence consistent with the jets. This is a **field-point equivalence** of the same tier as the triangular normalization: every point of the full guarded ideal `I` has a gauged point and conversely, so properness is equivalent. It is not ideal equality, and Astra does not claim that. CONFIRMED.

**Graded rank.** I did not assume Astra's "adapted basis has leading orders r..n-1". My script computes each band's leading Taylor orders by echelon form of the binomial matrix after the jet rows, then forms the graded columns `(ell+b,b)`, checks the column formula against the literal bracket with `P*` on seven monomials, and ranks each weight group. Result: 1760 columns, rank 1760, 596 groups, blocks at most 6×5; restoring the constant gauge gives one defect at weight 0, restoring the P gauge one at weight 3, restoring both gives both. Astra's script, replayed from the read-only snapshot (SHA `e7a72d4d…`), prints all three PASS lines in 0.1 s. Because `Phi(P)=P*+lower`, the leading form of `[Phi(P),dQ]` is `[P*,gr dQ]`, so injectivity of the graded map gives injectivity of `dQ -> [Phi(P),dQ]` for **every** P on the P source base (P jets plus two pins, nothing else). Ordering by weight, the 1760 selected rows form a block-triangular matrix with fixed rational diagonal blocks and P-linear entries below. CONFIRMED.

**Own mutations (changed objects), all in `box/ideation-20260906T1435Z-cross-fable5/gate_check.py`, SHA `4f59aa62…`, 20.7 s, exit 0, results in `result.txt` SHA `6e473dab…`:**

| Mutation | Result | Reading |
|---|---|---|
| Drop `X^21Y^6` from `P*` (monomial face) | rank 1759 | the second face monomial is load-bearing for exactly the Q column `(4,1)` on band 3, which stays a column since the gauge removes `(21,6)` on band 15 |
| Pin ratio `(21,6)->2` | rank 1760 | the theorem needs both `P*` coefficients nonzero, not their values; kernel becomes `c(1+2T)^b`, still gauged |
| Restore jet slot `(8,2)` alone | no defect | `P*^2` needs `(25,7),(42,12)` too |
| Restore `(8,2),(25,7),(42,12)` | one defect at weight 6 | jets, not gauges, exclude the higher centralizer powers |
| Sign of Q pin `(18,5)` | top bracket fails | as in Astra's control |

**Normalized subchart: survives with the SAME two gauges; computed, not asserted.** Raw counts 571/1551 reproduced from the rectangles `15×60`, `25×100` with pure vertical tops. Faces unchanged. The gauge shift stays inside the subchart (`Supp P_norm ⊂ Supp Q_norm`, `j<=60<100`, `(0,0)` present). For a subspace spanned by fewer binomial columns the leading orders are again `r..n'-1` (any `n'` distinct exponents give an invertible truncated binomial matrix), so the normalized columns are a prefix subset of the full columns, verified as a set inclusion. Graded rank: 1410 columns, rank 1410, 476 groups; without gauges exactly the weight-0 and weight-3 kernels. Unknown counts: Q 1410 (=1551−136−3−2), P base 516 (=571−53−2). No new gauge is needed. The post-cutoff normalization confirmation (field-point equivalence only) composes with this at the same tier: `I` proper ⇔ `I_norm` proper ⇔ `I_norm+gauges` proper.

**Reconstruction claim: CONFIRMED as linear algebra, GAP as a cost claim.** For each P the gauged Q is unique if it exists, and back-substitution through constant pivots writes each Q coordinate as a polynomial in P with no parameter denominators; the remaining rows become polynomial conditions on 516 (or 651) P unknowns. What is not established: the degree and term growth of those substituted Q expressions across up to 476 weight levels, the number and size of residual P rows, or any comparison with a plain bilinear stream. Small constant blocks are not measured fill or runtime; the recorded C-pivot fill-in failure at 99 is the relevant precedent. Astra states this limit; I make it the card's explicit stop condition. Astra's history reference `AUDIT.md:18231–18246` is not in this lane's inputs and was not verified.

## 2. Adjudication of my blind Laurent-count claim

Root's objection is correct and refutes exactly my Card 1 clause (b): "every irreducible factor of the face polynomial divides a separable `f` of degree at most `floor((rho+sigma)/sigma)+1`". Proposition 2.11(1) does give "every irreducible factor of `p` divides `f`", but `F=x^(u/l)y^v f(z)` lives in `L^(l)=K[x^(1/l),x^(-1/l),y]`, so on the weight line `rho·i/l+sigma·j=rho+sigma` the x-exponent may be arbitrarily negative and `deg f = y`-extent is unbounded; at `rho=sigma=1`, `t^(2-b)pi^b` exists for every `b`. My count silently assumed `F` polynomial in `t^(1/l)`. Theorem 2.6(1) supplies that only for `P,Q ∈ L:=K[x,y]` (GGV line 23 defines `L` as the polynomial ring, which also settles that the D108/D125 Euler envelope's polynomial `E` is licensed); a constantized Moh child in `K[t^(1/l),pi]` is not in `L`. Clauses (a) and (c) of my card (endpoint condition, Prop 2.11(2) start/end inequality) do not use the count and stand as stated. **Repair:** the count returns exactly under a pole-removal hypothesis making the Euler element polynomial in `t^(1/l)`; that is Astra's Card 2 lemma (minimum-gamma term of the face with positive pi-degree). Under it the y-extent is at most `floor((rho+sigma)/sigma)`, so the distinct-root bound is `floor((rho+sigma)/sigma)`, one less than I wrote. Nothing about receiver nonexistence follows; the ray-uniform attack in my §3 is conditional on that lemma and on a forced non-monomial face, neither established.

## 3. Deduplication by target, mechanism, object, test

- **D125 CE client, same object, three mechanisms.** Coordinator card 1 (source-preserving normalization then one measured construction pilot), Sol card 2 (two-path literal ordinary-J stream with a fixture gate), Astra card 1 (constant-pivot Q elimination). Coordinator's software card and Sol card 2 are the same test and merge. Astra card 1 is a downstream elimination design, sequenced after a measured complete stream. My blind card 2 (published-chain analogue at `(5,20)`) is a different mechanism on the same target; its `q/sigma` arithmetic is a guess, so it is a desk dependency, not a launch card.
- **Monomial-J Euler, same object.** Astra card 2 (restricted pole-removal lemma) and my card 1 (GGV Laurent clauses on constantized children) merge: my (b) is a corollary of Astra's lemma, my (a)/(c) are the lemma-free part. One card.
- **K16.** All four agree: redesign around the global product target, stop square/ramification/finite-m recurrences. No card.
- **D108.** Sol card 1 (axis-shear splice) shrinks proof dependency only; root's ruling that it does not reopen the retired client and has low global value is accepted. Parked, not launched.
- **z-graph obstruction.** Closed at scope by all; no card.

Scope objections that override agreement: none found against the four reports beyond my own count above. Astra's "constant determinant" wording is fine as stated; any reading of it as a solver win is unsupported.

## 4. Strongest attacks

**Proof (all-degree):** unchanged order: a source-compatible realizability obstruction for the `u_s=1` receivers, then the K16 global product target. The only new ingredient is the restricted pole-removal lemma; if it gates, it converts GGV's Laurent clauses into a finite root-count screen on forced faces, which is the first exact-tier constraint available for the K7 chart. Still no uniform invariant.

**CE:** properness of the exact guarded D125 source ideal, now in the normalized gauged chart (516 P + 1410 Q unknowns, row index bound 3792 before gauges), decided by a complete literal stream, not by projection or carrier survivors. Certification is not the gap; honest complete construction and cost are.

## 5. Two launchable cards

**Card A: D125 complete-source measured stream, normalized chart, two gauges.** Object: the exact guarded ideal restricted to the rectangles with pure tops plus `[X^0Y^0]Phi(Q)=0`, `[X^21Y^6]Phi(Q)=0`. Dependencies: the post-cutoff confirmed contract and normalization (field-point tier), Section 1 here, exporter code root review (pending, not read here). Test: emit every ordinary coefficient of `J(P,Q)-1/5` from the 571+1551 source monomials, and independently the transformed 241-layer rows, and compare after the fixed binomial map; record actual nonzero rows, terms and bytes. Then a depth-limited pilot of the constant-pivot elimination: substitute the top `k` weight levels of Q only, log term growth per level. Outcomes: matching streams license a source-complete client; a measured growth law decides whether the Astra elimination or a plain bilinear presentation is the solver input. Stop: any row mismatch, any guard loss, or growth exceeding the bilinear size by the second decade of levels. No solver, no properness claim.

**Card B: pole-removal lemma gate, then Laurent-clause screen on calibration children.** Object: `P,Q ∈ K[gamma,pi]`, `[P,Q]=c gamma^k`, face direction `(rho,sigma)`, `rho,sigma>0`. Test 1: hostile desk review of Astra's lemma (the leading-coefficient argument `u deg r - s deg e<0`), including its positive control `R=gamma pi`, `E=gamma^3 pi/2` and the falsification pair `P=gamma^2 pi+pi^2`, `Q=-gamma^2/2-pi`. Test 2, only if the lemma holds: apply Theorem 2.6 endpoint clause, Prop 2.11(1) with the corrected bound `floor((rho+sigma)/sigma)`, and Prop 2.11(2) to the forced faces of the (99,66)-descended calibration children before any receiver. Outcomes: lemma fails, bank the negative and stop; lemma holds and the screen is vacuous on the calibrations, bank as vacuous; a violation on a forced face is an internal kill to be gated separately. Stop: vacuous on both calibrations. Dependencies: forced-face data are outside this lane's inputs; GGV's Lemma [8] debt remains external.

## 6. Software choice and systems

**Software:** the cheapest complete-source discriminator is Card A's two-construction stream, not a further elimination expansion. The elimination is admitted only as a depth-limited measurement inside Card A, because its block ranks say nothing about fill.

**Systems: UPGRADE, policy only, no new ledger/launcher/adapter.** Evidence: all blind lanes were sealed by 15:02, and the 15:25/15:50/16:10/16:35 deadlines were all missed by root absence alone, a six-hour single point of failure that no exposed wake mechanism can remove. Smallest useful changes: record the four missed deadlines as cadence debt; make "collection first, then any new task" the fixed rule on root wake, which is what actually happened and should not depend on judgement; treat the K7 harvest (`.73` live 21:24, caps 70 GiB/20 h, ~04:30 Sep 7) the same way, since the 20-hour caps end the jobs whether or not root is present. My blind preflight-assert scanner is downgraded to optional: the post-cutoff confirmations report normal and `-O` runs already. No 12 h backstop change.

## 7. Continue, redesign, stop

- D125: CONTINUE as Card A; REDESIGN the client around the normalized gauged chart; solver only after measurement.
- Monomial-J receivers and census (avenue 29): CONTINUE as Card B; direct ordinary Euler port stays STOPPED.
- K16: REDESIGN around the global product target; STOP square, ramification, Pell, finite-m.
- D108 and 99 frontier: STOP; Sol's splice parked as proof-debt work.
- K7: CONTINUE untouched to harvest; my blind lifter specification remains the missing acceptance tool for a chart point.
- z-graph and constant-projection hunt: STOP at scope.

## 8. Post-cutoff deltas and unread dependencies

The contract and normalization confirmations are consumed at their stated tier: field-point equivalence over characteristic zero, no ideal equality, scheme isomorphism or mod-p transfer; the transformed/ordinary row equivalence is the only ideal-level statement and is what lets Astra's pivots be read as fixed rational combinations of ordinary rows. The exporter preflight is terminal and unread here; it supplies rows and guards, not properness, and Card A depends on its pending code review. Unread this lane: `d125-physical-exporter-preflight-astra`, `monomial-jacobian-euler-interface-astra` (cited by Astra, not reread), GGHV text, Strinz ledger, Roy audit functions, the D108 reports beyond the grep, GGV Theorem 2.6 clause (4) as cited in my blind card, and any AUDIT line cited by peers. No point, unit, properness, global-125 theorem or JC2 solution is claimed.

<!-- BODY-END -->
