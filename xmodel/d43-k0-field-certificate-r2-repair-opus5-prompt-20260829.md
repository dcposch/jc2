# Production task: D43 K0 dual-engine certificate R2

You are exact Opus 5 at maximum reasoning.  Act as the primary producer of a
fresh R2 source packet; do not patch or overwrite R1.  This is a certificate
engineering task with mathematical gate repair, not a theorem-discovery
claim.

Read, in full, under `/Users/dc/code/math/jc2/`:

- `cases/d43_k0_field_certificate_r1_20260828/`, excluding its new `custody/`
  subtree until the next bullet;
- the three harvested JSON files under
  `cases/d43_k0_field_certificate_r1_20260828/custody/ephemeral_20260829T010727Z/terminal/`;
- `xmodel/d43-k0-field-certificate-r1-hostile-review-fable5-20260829.md`;
- `xmodel/d43-k0-splitting-primary-research-opus5-20260828.md`;
- `xmodel/d43-k0-splitting-hostile-review-grok46-20260828.md`;
- the K0 blocks at the top of `AUDIT.md` and `PROGRESS.md` only.

Do not access, list, search, build, status, or control `jc2-lean`.  Do not run
`git status` or any workspace-wide search.  No web, AWS, local PARI/GP,
Singular, heavy CAS, canonical-ledger edits, commit, or push.  Bounded stdlib
Python tests are allowed.  Treat the promoted paper theorem as fixed; do not
broaden its scope.

## Mandatory R2 repairs

Create a fresh packet only at
`/Users/dc/code/math/jc2/cases/d43_k0_field_certificate_r2_20260829/` and a
producer report only at
`/Users/dc/code/math/jc2/xmodel/d43-k0-field-certificate-r2-repair-opus5-20260829.md`.

1. Repair the observed R1 AWS failure without weakening isolated execution.
   `python3 -I -B runner_r1.py` omitted the script directory and the bare
   `k0_field_checker_r1` import failed before either engine.  R2 must load its
   sealed sibling modules deterministically from the extracted, manifest-
   verified packet directory (or equivalently insert only that exact sealed
   directory), and preflight must execute a real `python -I -B` import smoke
   that would reproduce R1's failure.
2. Repair every Fable D1--D8 item, not merely document it:
   theorem-status/census entanglement; honest rank-432 leading-relation gate;
   honest free-polynomial-ring Galois-stability gate; exact frame and
   pointbank cardinality/prime-set pins; route duplicated constants through
   Spec; close all named mutation gaps; make the GP E-conditional banner read
   and check the literal E data (or honestly type it as single-engine and
   change the cross-engine terminal); and require reviewer verdict exactly
   PASS while rejecting producer-model case/spacing variants.
3. Where cheap, close D9--D13 too: timeout drift, census-padding constants,
   atomic-emission collision semantics, static-census fragility, and dead
   code.  If any is deliberately retained, give a precise non-load-bearing
   reason and mutation or refusal test.
4. Add negative controls for the exact R1 isolated-import failure and each
   repaired authority/gate surface.  No gate may pass merely because it
   recomputes a literal from itself.  Separate theorem truth from execution
   integrity and from corroboration.
5. Update route/schema/file names to R2 coherently.  Preserve AWS-only live
   identity, exact root-owned binary/archive/authorization binding, one core,
   1 GiB, zero swap, one atomic run lease, and fail-closed terminal behavior.
   A crashed/no-verdict R2 authorization must not be reusable.
6. Build a fresh deterministic archive, payload manifest, source seal,
   preregistration, review request, authorization template, and systemd
   template.  The packet must remain `SOURCE_READY_AWS_NOT_AUTHORIZED`.
   Execute all bounded no-GP preflights and mutation tests.  Do not claim the
   GP syntax/API has run.

The report must enumerate byte hashes, exact tests and mutation counts, map
each D1--D13 item to a repair or explicit retained debt, and state the only
licensed next action: a different-model hostile source review.  End the
report with a body self-hash.  Write nowhere else.
