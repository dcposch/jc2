You are Opus5 acting as an equal independent JC2 co-researcher.  Your only
repository write is

`xmodel/max12-812-order2-p0-total-rees-t-cs-direct-total-certificate-opus5-20260827.md`.

Read in full:

1. `xmodel/total-rees-localized-rho-unit-staged-calculus-promotion-sol-20260827.md`
2. `xmodel/max12-812-order2-p0-total-rees-t-cs-drop-g14-witness-opus5-20260827.md`
3. `xmodel/max12-812-order2-p0-total-rees-t-cs-drop-g14-witness-hostile-review-grok-20260827.md`
4. `xmodel/max12-812-order2-p0-total-rees-t-cs-grade14-decisive-fibre-promotion-sol-20260827.md`
5. the frozen V18/V18R1/V19 preregistrations and compilers under their case
   directories, plus the exact-Q V9/V17 row files they consume.

The registered localized special fibre is now completely understood, but it
is not yet a direct total-family certificate.  Work in the honest ordered
`T-cs` presentation, with bilinears

```text
rs-cs*qrs, c0-cs*qc0, c1-cs*qc1,
```

ordered equation `qrs=0`, exceptional coordinate `cs`, and genuine source
localizer `k`.  Derive, if possible, an exact polynomial identity of the
reviewed type

```text
cs^N * k^M * (1 + rho*W)
  in (literal total rows Tg10_1..Tg12_7, Tg14_5,
      T-cs bilinears, qrs),
```

for explicit finite `N,M>=0` (prefer the smallest; `M>0` is allowed and
costs the already registered `k=0` residual).  Do not invert `cs`, do not
call it a unit, and do not infer the identity merely from
`1 in (rows,qrs,rho,1-u*cs,1-v*k)`.  The rho cofactor must have the exact
divisibility/type needed for `1+rho*W` after exceptional saturation and
localization only at `k`.

Exploit the reviewed forcing chain and rational prefix fibre to seek a
triangular hand certificate before any generic Groebner route.  You may use
light exact sparse arithmetic locally.  Do not run Singular, Groebner bases,
or heavy/uncertain local CAS; do not mutate AWS; do not access `jc2-lean` or
the web.  If heavy algebra is genuinely necessary, give an AWS-ready,
fail-closed sparse certificate-search design with exact generator order,
degree/monomial ansatz, negative controls, memory strategy, and stop rule.

Required verdict, exactly one:

- `DIRECT_CERTIFICATE` with full coefficients and independent expansion;
- `EXACT_PREFIX_OBSTRUCTION` with a concrete prime/arc/torsion witness
  proving no certificate of the requested type at this prefix;
- `BOUNDED_AWS_DESIGN` with the cheapest decisive search; or
- `NO_VERDICT` with the first missing algebraic datum.

Separately explain precisely what V19's eventual inverse-variable cofactor
would have to satisfy after clearing `u,v`; do not assume a raw unit
certificate meets the total-family type.  Retain all V9/V17 source-
provenance debts and every broader nonclaim.  Do not infer full `T-cs`,
`k=0`, Gate T, order two, maximum twelve, or JC2.
