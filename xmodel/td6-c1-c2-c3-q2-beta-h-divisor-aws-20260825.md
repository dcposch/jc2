# TD6 q2-beta H-divisor closure — AWS exact replay

## Verdict

**PRODUCER-EXACT whole-`H=0` closure; promotion held on the independent V43
canonical generic gate; hostile review pending.**

On `H=C-3U^2=0`, independent r6d and Box03 V44 runs give byte-identical
canonical stdout (SHA256 `609349a53624531c1617104e2998ecf057192fea45bbe0ab41d7c2f24a1daf81`).
The exact genuine-P12 plus staged-N13 original-row identity has constant unit
residual `-k/50`, and its complete denominator radical is precisely
`U V P3`, where `P3=V^4-32V^2U^3+128U^6`.

The frozen all-beta `U=0` theorem, the frozen all-beta `V=H=0` rational-line
identity, and the frozen all-beta `H=P3=0` exceptional-curve composition
close those three factors.  Hence the whole set-theoretic `H=0` divisor is
empty for every beta in the fixed source-typed A3 section.

V39 is retained only as a custody-negative control.  Its exact algebra,
base P12, ranks, denominators, and residual were unaffected, but four SHA
fields (first-pivot, beta-tail, N13 value, N13 multiplier) included process
addresses through legacy `repr(E3)`.  V44 replaces them with exact-coordinate
serialization and agrees byte-for-byte across hosts.

The immutable package is
`cases/td6_c1_c2_c3_q2_beta_h_divisor_aws_20260825/`.  Campaign promotion is
deliberately held until V43 independently closes the same portability gate
on the generic open.  No `B3`, fourth-modulus, whole-TD6, SP-2, landing, or
JC2 claim is made here.
