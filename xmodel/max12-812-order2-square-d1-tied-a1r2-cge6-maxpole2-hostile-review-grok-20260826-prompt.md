You are an independent hostile algebraic-geometry referee.  Review only the
frozen tied D1 `(a,r)=(1,2)`, closed `c>=6` producer below.  Do not edit any
producer, prompt, ledger, audit, or existing artifact.  Write exactly one
report:

`xmodel/max12-812-order2-square-d1-tied-a1r2-cge6-maxpole2-hostile-review-grok-20260826.md`

End with exactly `CONFIRMED` or `REJECTED`.

Producer directory:

`cases/max12_812_order2_square_owner_d1_tied_a1r2_cge6_maxpole2_20260826/`

Primary custody:

```text
RESULT.md SHA256
6466b99a050d7fca6f3e8bc7333391046accd5521cf7c4aeb37f8b1554193a49

PRODUCER_FREEZE.sha256 SHA256
594514315e73d201a5d494e8de79076278aa8a25327dfbef38a71e5cb8b67cfc

EVIDENCE.sha256 SHA256
56fd5a06baa161b833a27c18613102287c09a18c259915395888229edddae438

source FREEZE.sha256 SHA256
67abf5ad91fc3f9db0b794779ad594d301c75e5029445a7c79842f7d95b4f0e5

source archive SHA256 on all three hosts
682a8d9ab3c04e7ebd4dd7e04f23d114b0c064b5269c8df3563d4cf5acb0c5a6
```

Claimed scope is exactly

```text
D(p*k0), ord(A)=1, ord(R)=2, ord(C)>=6,
```

through the decisive absolute grade 16.  Audit adversarially:

1. Rehash every source/producer/evidence row and all three nested compiled
   manifests.  Check host/tag/rc/validator/zero-swap custody.  Exact `Q` is
   the theorem endpoint; `F_65519,F_65521` are controls.  Confirm the
   generated Singular programs use ordinary polynomial rings, not `qring`.
2. Independently enumerate all four binomial summands through grade 16 at
   `(A,C,R)=(1,6,2)`.  Decide whether exactly `A2,R3,RA2` occur, all in grade
   16, whether `RA2` is the unique pole-two family, whether padded bounds are
   stable, and whether `AC` first occurs only at grade 17.
3. Inspect all seven literal rows, recursive extraction, and the independent
   analytic source.  Verify the coefficientwise grade-16 bridge and exact
   absence of `C`, higher normal/connection jets, lower loads, and every
   target.  In particular, audit that the analytic substitution really uses
   `sigma^6*C`, not the adjacent `c=5` source.
4. Reconstruct the canonical negative-tail numerator modulo `L^2`, retain
   the exact ordinary quotient, and check the pole-two recurrence, both root
   Faber identities, and both derivative-row syzygies with all signs.
5. Exhaust the four root-value charts for exact nonzero linear `A,R`.
   Verify both same-root unit ideals, both opposite-root survivors, exact
   division by `L`, terminals `+/- (5/2)k0*lambda^3*bu^3`, derivative
   terminals `5*k0*lambda^4*bu^3`, and the omit-`R3` negative control.
   Look for nilpotent/embedded solutions or illicit saturation.
6. Audit the closed `ord(C)>=6` claim scheme-theoretically: no `C` leading
   coefficient may be inverted, raising `C` must not reveal a lower-grade
   family or alter the decisive row, and no exact-contact density argument
   is allowed.
7. Keep the separate `(1,5,2)` four-family producer and its live review out
   of this verdict.  Enforce the `c<=5`, other-face, load, `p=0`, `k0=0`,
   global/terminal, order-two, maximum-twelve, and JC2 firewalls.

State the earliest missing primitive or smallest failing identity if any.
Producer PASS strings are not evidence by themselves.  Do not import the
support miner or hand triage as proof.
