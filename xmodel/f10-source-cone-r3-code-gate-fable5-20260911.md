# f10-source-cone-r3-code-gate-fable5-20260911 — FIRST static review of the fixed-r3 complete source reconstruction (Fable 5.1)

status: UNSEALED (adapter seals; no Seal or charge_basis authored). First action 2026-09-11T00:35:19Z; both owned targets ABSENT at that instant. Original reserve 00:59 UTC / HARD 01:03 UTC, never reset. Basis 0d39df3c9fd69c939a8420c54d03228b9077777d. All 21 charged inputs in /tmp/jc2-lane.WfOowK/inputs hashed equal to the charged pin list BEFORE any body was read (sha256sum, 21 unique basenames). Each read FRESH_WHOLE through EOF/Seal; check.py (540 lines) was read in three bounded pieces 1-220, 221-500, 500-540 after its first aggregate output clipped at 30 KB. No live/provenance/uncharged source, no r2 references, no ledgers. Static text, hash and metadata only; zero CAS, subprocess, import, AST, syntax, compile or test; no coefficient, fixture or candidate body. Accepted premises (Euler 16r, early 17q, middle 17s with prescribed H7 units 17zzu, critical 17o, ell 17zzh, scale 17zzm, late composition 15z, whole-leading 17zzd, denominator 17zzb, kernel 16z, cone 17z) are consumed at their scoped tiers and not re-proved.

## Verdicts A–D (literal algebra, bands, Euler/Jacobian/scale, graph)

**A CONFIRMED.** The base is the literal whole rank-7 B=Q[V]/P7 at tau=3/10. produce.py 13-25 and check.py 103-115 rebuild dL, KL, gamma and the septic S=2K^2-140tau(1+tau-6V)KD+245gamma D^2 with the exact coefficient lists of the univariate report (1)-(2) and full-boundary (3) (I re-derived each of the eleven rational coefficient factors from u=2-tau by hand; all agree, including 840(1-tau^2) and 42(1+tau)(2+tau)(4tau-3)). Degree 8 and leading coefficient -245*120*144*tau are checked, then P7 is made monic. Every inverse is `fi`/`inverse`: extended Euclid against the WHOLE monic P7 with the gcd required to be exactly [1] (arithmetic.py 60-69, check_arithmetic.py 83-90), followed by a literal product check; a zero or component-wise nonunit input yields gcd=P7 or a nontrivial gcd and is refused. This is the exact unit criterion of Q[V]/(P7) with no field, factor, or nonzero-as-unit assumption. `scalar`/`value` refuse any parameter-dependent inversion. `det3` is the row-0 cofactor expansion; `invert_matrix` forms invdet*(-1)^(i+j)*M_ji (adjugate transposed correctly) and verifies BOTH products (arithmetic.py 143-160); the checker re-derives the determinant by a six-permutation sum, both products, and det*invdet=1 (check.py 247-309). `completion` (arithmetic.py 175-183) computes gcd(gcd(P7,c1),c0) with two-stage Bezout, which is exactly the unit-ideal test of (c1,c0) in B since Q[V] is a PID; the c1=0 branch is handled (l1=0) with no first-nonzero pivot. Twelve named witnesses match the checker inventory (check.py 138-139). No hidden localization: pivots are fixed integers 10j-3(17-h), j-3i, and only a (via C(0)=1/W) and integer denominators enter `series`/`integrate`.

**B CONFIRMED.** Gaps 1..10 are the loop produce.py 67-162. Forcing W_h (68-72) is the full ordered cross sum ((10-i)U_i V_j' -(17-h+i)U_i' V_j) over ALL earlier actual bands; the checker re-forms it by coefficient convolution without derivatives (check.py 259-267). Operator op (46-50) is m C V'-(n-h)C'V+(m-h)U D'-n U'D; pivots 10j-3(17-h) match early (12)/middle (11); resonance raises. Homogeneous rho = 10a[theta^0]V_lin - 17b U_0 with the particular part subtracted (80-81, 97-98; checker 321-322) — early (28) retained. Early rho=X_h (86), matrix columns (U0,U1,U2), rows (R1,R0,rho); h=4 uses the modified target -2q theta^6 on the U2 column and full target -2U2 theta^6 (78, 95), q=-U (103; checker 335). Middle h=5,6: inverse[2][2] is checked equal to (3(h-3)-2)/(27-h)*H7_h = 2H7_5/11, H7_6/3 (89-90; middle (20) delta4/kappa), then rho is solved so U2=0 and V4=0 are literally checked (91-101; checker 326-329) with H7 inverted only via `fi` — the accepted 17zzu unit is re-verified, not assumed. Critical h=7: A_part=(z-U d0)theta with d0=[theta^2]A_3, target -2z theta^5, Lambda functional F P6-P5/2 from C^2∫R/C^2 (131-137, critical (15)), completion identity lambda1 c1+lambda0 c0=1 CHECKED (140). Gap 8: fixed U^2 theta^3 and target -U^2 theta^5 (117-119; checker 355). Late pivots -7,-17,-27 and -4,-14,-24 with operator coefficient -9,-8 (=-beta) verified by hand from 10j-3(17-h). Ell h=10: no A variation, target variation -theta^4, literal column theta^2+(6F/11)theta+(13H-24F^2/11)/21 checked (149-154; ell (5) at r=3). Each one-column step checks value=-(l1 b1+l0 b0), Psi=c1 b0-c0 b1 and the residual pair (-l0 Psi, l1 Psi) (141-146; checker 385-391) — critical (20)-(21), late (9)-(10). The automatic theta^5 row at gaps 7-10 is CHECKED by `upper`'s residual test (61-62), not assumed. Both affine directions: checker 315-319 (forward row map and inverse actually used). Coefficient degrees deg Dpar<=3, Vpar<=6, Kpar<=10 and early A degree<=2 (check.py 169-171, 313-314). Readbacks d_(3-h), v_(6-h)-U d_(7-h), k_(10-h), k_3..k_1, E (check.py 330-337, 392-395) match the band table.

**C CONFIRMED.** Producer Euler (181-196) implements 16r (5) literally: I re-derived E_j B_j = delta_(j+2) -(j+1)f'B_(j+1)+2fB'_(j+1)-(j+2)h'B_(j+2)+hB'_(j+2)-(j+3)k'B_(j+3) from (lA_k'B_l-kA_kB_l')t^(k+l-1) and it matches term by term; the checker assembles the same rows as Σ_k (l A_k' B_l - k A_k B_l') without the producer's Q_j text (178-195). Gauges: k(0)=0 (kp constant, check.py 175), [S]B3=0 and B0(0)=0 arise because resonant slots are never populated and are explicitly checked (196-197). The band-vs-Euler equality for all eleven bands (197-204) ties the two constructions. Jacobian J=A_S B_t-A_t B_S (205) versus the checker's coefficient-pair sum (404-410). Slot inventory 24+21+18+15+12+9+6+3=108 and 21+24=45 recomputed by me; upper slots j>=2 forced zero and support i<=23-3j checked (216-227; 413-425). Band readback of the 45 low slots: P1[20-h], P0[23-h] equal {} for h<7 and (-l0 Psi, l1 Psi) for h=7..10 (228-232; 427-441). BOTH inverse charts S=Pq^3-zq^2+Uq, theta=q^-1 applied to Ahat AND Bhat with negative-q rejection (236-245; 445-454); I verified this is the normalized image of 16r §6 (z_orig=sq, P=ps^3). Leading a=1/W, b=1/t5 and W t5 Wi t5i=1 (246-248). z=s^2 appears only in the checker's Laurent back-map `to_scale` (460-493), which I verified exponent by exponent: A=s^-3 Ahat(S,st), B=s^-5, upper and J at s^-7, P1 rows at s^-6, P0 at s^-7, both low targets u and 1 subtracted at i=0, guard W t5 s^8 * a * b = 1. This is exactly scale (2)-(4); the producer never sets s=1.

**D CONFIRMED.** Psi7=Hq+c z with c a B-scalar forced by weight (7=wt z) and the exact shape checked (249-251; 504-506); c inverted by `fi`/`inverse` and its witness tied to actual use (252; 507-510). zeta=-c^-1 Hq. Rows: Psi8,Psi9,Psi10 -> K8,K9,K10; P1[1..9] -> A1_1..A1_9; P0[1..12] -> A0_1..A0_12; T=(z P1_0 - U P0_0)(X,zeta) formed BEFORE substitution (255-256; 515-516). Weights 8,9,10; 19..11; 22..11; 27 verified against cross B1 and enforced (259-260; 519-521). ell=P0_0(X,zeta) weight 23, g=Hq*ell weight 30, U raw-bound; guard zeta*ell=-c^-1 g checked (263; 522-530). Zero slots are legal empty arrays and duplicates keep distinct positions (poly() accepts [] as {}). Every graph object is recomputed by the checker from its own already-verified raw Psi/P1/P0, so no candidate-owned target exists.

## Verdicts E–H (checker independence, authority, controls, corrections)

**E CONFIRMED with one retained fragility.** check.py imports only authority and check_arithmetic; check_arithmetic imports only fractions (line 2). No producer symbol is referenced. Arithmetic is genuinely separate: modular reduction by direct monic subtraction (check_arithmetic 60-68) versus long division; fractional powers by the recurrence k f_k = Σ (alpha i -(k-i)) c_i f_(k-i) (194-206, which I re-derived from c f' = alpha c' f) versus binomial expansion; graph substitution by Horner versus per-term powers. Wire validation is strict: parse_int/float/constant all raise (21-29), duplicate keys raise (13-19), exact key sets (31-33), exact array lengths (35-38), canonical decimals with no leading zero and no -0 (40-51), reduced signed/positive rationals via Fraction round-trip (53-63), seven-coordinate elements (65-66), strictly increasing unique exponent tuples with nonzero values and componentwise bounds (30,15,10,7,4,30,16)/(30,15,10,7) (70-87), per-object weight envelopes, per-polynomial 20000 and aggregate 400000 term caps, 128 MiB byte cap. Fragility (literal-identity, not authentic-source): check.py 379-382 requires the emitted (lambda1,lambda0) at gaps 8-10 to EQUAL the checker's own two-stage Euclid representatives, and 371-377 requires the gap-7 pair to equal the same Lambda formula the producer uses. Both are deterministic, so a correct producer passes, but a valid alternative Bezout pair would be rejected as a false negative; the validity predicate itself is line 370 (`whole column completion`), which is the one that carries mathematical weight. Second note: the checker's `oper` sums A-coefficients only for theta^0..theta^2 (check.py 224-228); this is exact for every actual band (A degree<=2 is separately enforced at 313-314) but is not a general operator. Neither is blocking.

**F CONFIRMED.** authority.py orders: mode, platform, DMI vendor == 'Amazon EC2', nine-argument registered CLI with the literal tag, then `set_int_max_str_digits(4300)`, and only then `import hashlib, json` (lines 12-29). The mathematical modules are imported inside build()/check_file() after authorize() returns (produce.py 6, check.py 7). Registration schema keys, ROOT-CAPRUN status, mode, absolute artifact path, UNFORMED-for-produce / pinned-input-for-check, LIMITS equality, exact five-file inventory with byte pins, interpreter pin, separately pinned native inventory with absolute paths, all with a 64 KiB read cap and 64-hex pin format check (30-94). Output is created with mode 'xb' (produce.py 294). Everything is process-local and static; the file itself states it proves bytes, not provenance, closure or cgroup limits. No runtime-regression acceptance is implied by this verdict.

**G CONFIRMED.** I traced each CONTROLS.md mutation to the first checker predicate it can reach, in file order. (1) units.dL inverse coordinate 0 plus one: units are iterated in sorted key order W, critical_c, dL, ... so the first failure is `all-seven-coordinate inverse product dL` (check.py 145) with defect u*e0=dL, nonzero because dL is a unit in a nonzero ring. (2) bands[9].B_var theta^2 coefficient 1 to 2: envelopes pass (exponents unchanged), the literal fixed-forcing tuple (361) does not involve B_var, and the first B_var comparison is `independent column variation` (363); defect -theta^4 per the -1 pivot. (3) Bhat [S^2 theta^5] 1 to 2: envelope 17 passes; first comparison is `independently reconstructed full Euler B` (195). (4) raw.P1[20] zero to one: the 108-slot loop reads jacobian_slots[44] (unchanged, passes); the 45-slot loop reaches i=20 and fails `literal 45-slot low readback` (433), proving P1 is read and not regenerated. (5) graph.zeta plus X1^7: `Hq literal` (511) passes, `graph substitution literal` (512) fails; c*X1^7 nonzero since c is a unit. (6) graph.slots[24] plus X1^27: within bound 30, labels K8..A0_12 pass, `all25 literal graph rows T` fails (520). All six defects are nonzero on the whole B and reach the intended mathematical predicate rather than a schema refusal. None was run; no baseline exists.

**H CONFIRMED as static, with no blocking correction found.** I found no line that must change for the mathematics to be literal. Retained usable source: all five modules as pinned. Non-blocking observations for the runtime packet: (i) `power(chartS, ex[5])` is recomputed per term up to S^23 (produce.py 242; check.py 451) — quadratic redundancy, unmeasured; (ii) all products are dict-sparse with rank-7 Fraction arithmetic (about fifty Fraction multiplications per coefficient product), and the full Jacobian is a product of two weight-10/17 polynomials in five graded variables, so 10^8–10^10 Fraction operations are plausible; the proposed 600 CPU s / 900 wall s per process is an unmeasured acceptance limit that may refuse a valid run (PARTIAL/NONDECISION, as CONTROLS.md itself states); (iii) the internal 100000-term cap is checked after every accumulation and is a refusal, not truncation. The code is not execution-ready: no registration JSON, native inventory, interpreter pin or cgroup enforcement exists, and no import or syntax check has been performed by anyone. A static pass establishes neither baseline nor feasibility nor a source outcome.

## Verdict summary, retained scope and blocking corrections

A CONFIRMED, B CONFIRMED, C CONFIRMED, D CONFIRMED, E CONFIRMED (one non-blocking literal-identity fragility at check.py 371-382), F CONFIRMED (static), G CONFIRMED (prospective, unrun), H CONFIRMED as static with NO blocking correction. No REFUTED item and no GAP in the source itself. Authentic-source defects: none found. Literal-identity defects: none found; the two fragilities in E can only cause false negatives. Everything the accepted premises assert (H7 units, column unimodularity, critical c unit, completed-map determinants) is re-verified by whole-B Euclid in both programs rather than assumed, so a failed run would be a NONDECISION that reopens the premise or the code, never a source conclusion. The optional source-cone performance claims (600 CPU s, 900 wall s, 8 GiB, 128 MiB) are not adopted; they are unmeasured refusal limits. This static pass establishes no baseline, no control result, no feasibility, no native/startup/registration authority and no source outcome. It does not broaden into a solver, the exponent-one membership test, or the modular full-rank idea.

## OPEN(S) RAISED

No new canonical identifier. QUANTITY: whether the pinned r3 producer forms its artifact and the pinned checker returns CHECKED_R3_25_GRAPH_SLOTS_45_LOW_108_JACOBIAN_NO_SOURCE_OUTCOME on it, under the whole rank-7 B, within whatever limits ROOT registers; unknown, no coefficient count, height or timing measured by anyone. CHEAPEST INSTRUMENT: after a separate ROOT runtime/registration/native/startup gate, one producer observation plus one fresh checker process, then the six CONTROLS.md semantic mutations each with its own registered input pin; record the exact predicate reached. GATE: refusal, timeout or cap hit is PARTIAL/NONDECISION. NUMERIC WALL ESTIMATE (static, UNMEASURED): pure-Python Fraction arithmetic in rank-7 B on dict-sparse polynomials with five graded variables makes the Jacobian and Euler products the dominant cost; I estimate 10^8 to 10^10 Fraction operations, i.e. tens of minutes to several hours per process, so the proposed 600 CPU s / 900 wall s is more likely to refuse than to admit a valid run; ROOT should register a first observation with a generous wall cap (of order hours, not minutes) and measure before any tighter policy.

## COLLISIONS

status: EMPTY. Own-only check: this lane wrote exactly xmodel/f10-source-cone-r3-code-gate-fable5-20260911.md and box/f10-source-cone-r3-code-gate-fable5-20260911/input_custody.md, both absent at 00:35:19Z; apply_patch only (skeleton, A-D, E-H, custody, one hash-row correction, this final section); no Seal, no charge_basis, no Write/Edit, no redirection into owned files, no generated text, no input edit, no live/provenance/peer body. One typed custody hash was caught by a programmatic cross-check and corrected before finalization; all 21 custody rows then verified with sha256sum -c (21 OK) at 00:51:42Z, which is also the post-read pin verification of all 21 inputs (unchanged from the charged list). Own final WHOLE read of both owned files follows this write and precedes handoff; the marker below is the only standalone marker and nothing follows it. All writers idle after the WHOLE read.

<!-- BODY-END -->
