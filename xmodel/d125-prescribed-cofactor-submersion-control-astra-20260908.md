# Prescribed D125 cofactor: an exact polynomial-submersion countercontrol

2026-09-08. **FOUND: the prescribed cofactor does not obstruct a polynomial submersion.** This is a countercontrol to that proposed exclusion mechanism, not a Keller pair or a point of the full D125 source. The construction and proof are independent of the prior low-u-degree obstruction.

## Explicit construction and full critical-point proof

Let K be any characteristic-zero field and choose a in K with a!=0. Put

    N=uv⁵−v²−1,  L=uv³−1,
    M=vL²+aN,
    P=N M.

On C=(N=0), v is a unit and uv³=1+v⁻². Hence L|C=v⁻² and **M|C=v⁻³ exactly**. With M0=v+uv²−uv⁴, direct multiplication also gives

    M=M0+N H,  H=uv²+a.

Thus the new term is genuinely higher in u than the excluded H=h(v) family; no normalization of the actual full source is being asserted. We use k=1 solely for this relaxation.

On the open v!=0, the coordinates (z,v)=(N,v) are an isomorphism of affine cylinders, with inverse u=(z+v²+1)/v⁵ and Jacobian v⁵. Since N+1=v²L,

    P=z(z+1)²/v³+a z².

Partial derivatives in THESE coordinates are

    P_z=(z+1)(3z+1)/v³+2az,
    (P_v)_z-fixed=−3z(z+1)²/v⁴.

A critical point on this open would have z=0 or z=−1. At z=0, P_z=v⁻³!=0; at z=−1, P_z=−2a!=0. Thus no critical point exists on v!=0, over K or any field extension. In the original polynomial coordinates, along the ENTIRE remaining line v=0,

    P(u,0)=a,  P_u(u,0)=0,  P_v(u,0)=−1.

This excludes every remaining critical point. It is a universal algebraic submersion proof, not sampling. A denominator-free identity checked by the code is

    v⁵P_v−N_vP_u=−3vN(N+1)²,
    P_u=v²(N+1)(3N+1)+2aNv⁵.

These give the same two-branch proof without confusing the original v derivative with the fixed-z derivative.

## Why the correction matters; exact scope

Deleting aN from M preserves the cofactor on C but gives P=NvL². It has a critical point at (u,v)=(1,1), where N=−1 and L=0. The aN² correction removes this multiple-factor critical locus while retaining the prescribed normal cofactor. This is an actual changed-polynomial negative control.

For a=1 the constructed P is a small degree15 polynomial in the PHYSICAL variables u,v, with u-degree3. The actual D125 physical P has degree75 and u-degree15; its fixed faces, origin and other coefficient equations are NOT imposed here. In particular our P(u,0)=a!=0 already differs from the original source's origin normalization. No Keller mate is constructed, no coefficient ideal is declared proper, and no source-family coverage or JC2 conclusion follows.

The exact conclusion is nevertheless decisive: neither the embedding C nor the simultaneous conditions P=N M, M|C=v⁻³ and polynomial submersion can exclude the client. A subsequent obstruction would have to use genuinely additional conditions, for example the full Keller-mate equations or actual fixed source coefficients. The earlier cofactor-only search is therefore stopped, not extended to a degree census.

## History, controls and custody

Named checks in APPROACHES, ladder/REDUCTION and the terminal marked-component/fiber reports found no already charged version of this formula; no exhaustive novelty claim is made. A brief targeted primary discovery pass considered classical nonsingular-polynomial constructions, but no external theorem or classification is used in the proof. No new primary authority is needed or claimed; the exact polynomial identity is the certificate.

The final v2 receipt records eight capped normal/−O runs, zero Assert nodes, byte-identical positive witnesses, and actual changed-L-sign, changed-derivative-sign and omitted-a failures. The omitted-a mode evaluates BOTH derivatives of its changed polynomial at(1,1); its failure is not merely a flipped parameter predicate. The first development receipt is retained separately, since that version rejected a=0 at the unit precondition while its positive run independently verified the same critical point. Both generated witnesses agree; v2 is the charged final replay.

Inputs and every owned final/development artifact are pinned in the fresh box custody. No full degree15/25 RECEIVER or actual high-degree SOURCE expansion, CAS, solver, AWS, extra agent, live peer or shared/protected edit was used. **All writers idle at terminal custody; transaction verified; STOP/IDLE.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `4427`.
- Body SHA-256:
  `b9bdb4f1aa717721b802391f217afdd37a6fdb6a49505c339db5241d44cb3714`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
