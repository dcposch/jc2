# FIRST Fable5.1 gate: basis-free weight-30 modular unit bound

status=UNSEALED (no Seal, no charge_basis authored; adapter seals)
lane=f10-r2-modular-macaulay-bound-gate-fable5-20260910
gate_model=claude-fable-5-1
first_action_utc=2026-09-10T15:49:57Z (hash command); stop = earlier of ROOT unit launch + 18 min (the 1080 s runtime started before the first action, so at or before 16:07:57Z) and 16:10:00Z USER TERM (KILL 16:10:05Z); final-3-min reserve; never reset
mode=STATIC; manual math/text/hash only; ZERO subprocess/import/AST/syntax/compile/test/CAS of any size, no counts by machine; tools date, ls, wc, sha256sum, head, grep, cat, apply_patch only
owned=xmodel/f10-r2-modular-macaulay-bound-gate-fable5-20260910.md, box/f10-r2-modular-macaulay-bound-gate-fable5-20260910/ (both absent at 15:49:57Z; unsealed skeleton first, then body)
charged_input=xmodel/f10-r2-modular-macaulay-bound-astra-20260910.md
charged_input=xmodel/f10-r2-modular-fullsource-route-astra-20260910.md
charged_input=xmodel/f10-r2-modular-fullsource-gate-fable5-20260910.md
charged_input=xmodel/f10-r2-cyclic-core-astra-20260910.md
charged_input=xmodel/f10-r2-cyclic-core-gate-fable5-20260910.md

## 0. Custody, read scope, disclosure

All 5 snapshots in /tmp/jc2-lane.VWMsgQ/inputs (distinct basenames) were hashed by sha256sum at 15:49:57Z BEFORE any body read; all 5 digests equal the charged pins byte for byte (rows generated from the sha256sum output into box/input_custody.md and cross-checked by grep; this body carries no hex digest). Both owned targets were absent at 15:49:57Z. All 5 were then read WHOLE in parallel single bounded reads: macaulay-bound producer (241 lines, ends at its Seal), fullsource route (392, at Seal), fullsource gate (75, at marker), cyclic core (228, at Seal), cyclic gate (65, at marker). No read was clipped, so no recovery read was needed. The three printed body seals were reproduced by hashing the declared body byte counts (recorded in the custody file). ROOT's stated producer custody and expected transaction are not among the inputs and were NOT verified: no live file, worker file, new code, coefficient body, provenance, corpus, Git, network, process or other lane was accessed; nothing was executed, imported, parsed or compiled; every count below is a hand sum.

Disclosure: this is a different-model FIRST review of the Astra producer. Two of the five inputs, the fullsource gate and the cyclic gate, were authored by Fable 5.1 earlier today, as were the gates behind the accepted imports. Imports are consumed at exact accepted scope and not re-proved: the cyclic core (17zzr as named by ROOT) gives the unconditional identity A = E[s]/(s^3 - t0) with all fifteen rows and the nineteen-row cover, plus the CONDITIONAL 35 and seven-per-class lengths; the fullsource local-model theorem (17zzw) gives, under an authenticated integral model with WHOLE special highest regularity, finiteness of Q_O and the Nakayama lift of a modular unit identity to zero generic guarded source, integrality owed on f5,f6,f7 and the used rows only. No actual coefficient, place, matrix, rank, basis, unit certificate or source outcome exists in the inputs, and none is asserted here. ROOT's bound is tested, not assumed.

## 1. Verdicts

| item | verdict |
|---|---|
| A filtered strictness (1) with Koszul degree control, class projection, every characteristic (section 2) | CONFIRMED |
| B completeness under full special highest regularity; soundness of a literal identity always; scope = full unlocalized Q (sections 1, 3) | CONFIRMED |
| C hand counts 247 rows, 424 + 330 = 754 columns; rank 240 and 7 x 330 quotient; 156 x 548 fourteen-row variant (section 4) | CONFIRMED, every count reproduced |
| D changed control (x^5, x^6, x^7; x y^7 - 1): Q = 0 at weight 75, no weight-30 identity, strictness holds, spanning fails (section 5) | CONFIRMED |
| E positive certificate plus authenticated 17zzw packet gives guarded source zero; no negative inference; fixed shape only (sections 3, 6) | CONFIRMED CONDITIONAL |

Smallest blocking correction: none. REFUTED: nothing. No uncorrected imported step.

## A. Filtered strictness (1)

CONFIRMED. Own replay over the residue field k, S = k[Y1,Y2,Y3] with weights 1,2,3, f_h = H_h + (terms of weight below h), H5,H6,H7 a regular sequence. Let p = sum b_h f_h, D = max_h (wt b_h + h). If D > wt p, the weight-D part of the right side is sum_h b_{h,top} H_h, because b_h f_h contributes to weight D only through the weight-(D-h) part of b_h times the weight-h part H_h of f_h; so sum b_{h,top} H_h = 0 is a homogeneous syzygy. The Koszul complex of a regular sequence is acyclic in positive homological degree and is graded, so the syzygy is the image of a homogeneous element of the second exterior power: b_{h,top} = sum_j c_{hj} H_j with c_{hj} = -c_{jh}, c_{hh} = 0, wt c_{hj} = D-h-j (zero when negative); the sign convention is immaterial. Set b'_h = b_h - sum_j c_{hj} f_j. Then sum_h b'_h f_h = p - sum_{h,j} c_{hj} f_j f_h = p, the (h,j) and (j,h) terms cancelling because c_{hj} + c_{jh} = 0, which holds in characteristic 2 too since the exterior square has no e_h wedge e_h term. The weight-(D-h) part of b'_h is b_{h,top} - sum_j c_{hj} H_j = 0, and the added terms c_{hj}(f_j - H_j) have weight below D-h-j+j = D-h, so wt b'_h + h < D, including the lower terms of the f_j. D is a non-negative integer that cannot fall below wt p while p is nonzero (the sum would have too small a weight), so the descent terminates with D <= wt p <= N and every b_h in S_{<=N-h}; a negative N-h forces b_h = 0, so I meets S_{<=4} in zero, consistent with dim R = 35 and 1 not in I. For p = 0 the zero representation is used. Class projection: f_h is Z/5-homogeneous of class h, so for p of class 0 replacing each b_h by its class-(-h) monomial part preserves the sum and cannot raise weight; this is projection onto a direct monomial grading, no averaging or division by five, valid in characteristic 5. No O, flatness, lift or basis enters: the statement is over k for the actual special f_h with lower terms, and the report is right that an H-only column set would prove membership in (H), not in I.

## B. Completeness and soundness

CONFIRMED. Spanning: with (Hbar) regular, k[Y]/(H) has Hilbert series S5 S7 (1-t+t^2), replayed in the cyclic gate, a polynomial of degree 12, so every monomial b of weight n > 12 lies in (H)_n: b = sum a_h H_h with a_h homogeneous of weight n-h and class n-h, hence b = -sum a_h (f_h - H_h) in R, lower weight, same class; induction spans R by monomials of weight <= 12 and each R_c by those of class c (project a spanning set onto class c). This is the special-field form of 17zzw section 3 and needs no lift. If Q = 0 then 1 = sum a_j g_j in R; its class-0 component is sum (a_j)_{-d_j} g_j = 1 since g_j has class d_j; choose b_j in S_{<=12,-d_j} representing (a_j)_{-d_j}. Then p = 1 - sum b_j g_j lies in I, has class 0, and weight <= 12 + 19 = 31, hence <= 30 because class-0 weights are multiples of five. Item A at N = 30 with projection gives c_h in S_{<=30-h,-h} and 1 = sum b_j g_j + sum c_h f_h, a literal identity in S_{<=30,0} whose terms are exactly the prescribed columns, so 1 lies in the column span. Conversely any column identity is a polynomial identity in (f,g), so Q = 0 with no hypothesis, and it certifies the subideal of the used rows. All fifteen actual rows appear with their lower terms, the mixed row as one polynomial of upper weight 19 and class 4 (U0 L0: 3 + 1; L1: 14 mod 5); replayed classes K1_i 3,2,1,0,4,3 and K0_i 0,4,3,2,1,0,4,3, multiplicities (3,2,2,4,4). Scope is stated correctly: the equivalence concerns the full unlocalized special Q; Q_k nonzero with zero guarded source is possible (17zzw section 6 second control), so the test is sufficient, not necessary, for the source.

## C. Hand counts and rank statements

CONFIRMED by hand. p(n) counts (a,b,c) with a + 2b + 3c = n as the sum over c of floor((n-3c)/2) + 1. Own table p(0..30) = 1,1,2,3,4,5,7,8,10,12,14,16,19,21,24,27,30,33,37,40,44,48,52,56,61,65,70,75,80,85,91. Rows: p at 0,5,10,15,20,25,30 = 1+5+14+27+44+65+91 = 247. f5 columns (wt m <= 25, class 0): 156; f6 (wt <= 24, class 4, weights 4,9,14,19,24): 4+12+24+40+61 = 141; f7 (wt <= 23, class 3, weights 3,8,13,18,23): 3+10+21+37+56 = 127; total 424. Monomials of weight <= 12 by class: 20, 24, 29, 13, 16, sum 102 = sum p(0..12). A row of class d takes multipliers of class -d, so 3*20 + 2*16 + 2*13 + 4*29 + 4*24 = 330. Total 754, every column inside S_{<=30,0} since wt m + w_j <= 31 and is 0 mod 5. Rank: S_{<=30,0}/(I meet S_{<=30,0}) maps onto R_0, of dimension 7 by gr R = k[Y]/(H) (conditional, cyclic core section 2), so by A the compatibility span has rank 247 - 7 = 240. Koszul Euler check: exterior-square shifts 11,12,13 give class-restricted counts over weights {4,9,14,19}, {3,8,13,18}, {2,7,12,17} of 80, 71, 62, the cube shift 18 over {2,7,12} gives 29, and 424 - 213 + 29 = 240 with 247 - 240 = 7, consistent. Chain: under the premise the g-columns reduce onto sum_j R_{-d_j} g_j = J_0 (cyclic core (3)), the combined cokernel is Q_0, so full rank 247 iff J_0 = R_0 iff 1 in J_0 (degree-zero part of an ideal) iff Q = 0; no pivot choice or seven coordinates are named, correctly. Fourteen-row variant: D = 5 floor((15+12)/5) = 25; rows 1+5+14+27+44+65 = 156; f columns 91 (weights 0,...,20, class 0), 80 (4,9,14,19), 71 (3,8,13,18); g columns with multiplicities (3,2,2,4,3): 60+32+26+116+72 = 306; total 548. Passing it implies Q = 0 through the subideal, failing it decides nothing, so it is a stronger test and not an equivalence; no alternate run is selected.

## D. Changed control: unit ideal missed at weight 30

CONFIRMED, replayed literally over any field k with x,y,z of weights 1,2,3. f5 = x^5, f6 = x^6, f7 = x^7 have weights 5,6,7 and classes 0,1,2; g = x y^7 - 1 has upper weight 1 + 14 = 15, class 0 and drop 15, a legitimate K0_1 shape; the other fourteen slots are zero. The highest locus of (x^5,x^6,x^7) is the plane x = 0, so the regularity premise fails. Put t = x y^7. In Q, t^5 = x^5 y^35 = 0 and t = 1, so 1 = 0 and Q = 0. Identity (2): (1+t+t^2+t^3+t^4)(t-1) = t^5 - 1, hence x^5 y^35 - (1+t+t^2+t^3+t^4) g = x^5 y^35 - x^5 y^35 + 1 = 1; its largest product weight is 5 + 70 = 75, also reached by t^4 g at 60 + 15. No weight-30 matrix identity: suppose 1 = sum c_h f_h + a g with a in S_{<=12,0}. Modulo the monomial ideal (x^5) every f_h vanishes, leaving 1 = a (t - 1) in k[x,y,z]/(x^5), where t is nilpotent, so t - 1 is a unit with the UNIQUE inverse -(1+t+t^2+t^3+t^4); the residue of a is that element, whose monomial t^4 = x^4 y^28 has x-degree 4 and survives modulo (x^5), so a must contain a monomial of weight 4 + 56 = 60 > 12, indeed > 15. Contradiction, in every characteristic including 5. Strictness (1) still holds for this I: (x^5,x^6,x^7) = (x^5), p = x^5 q gives q of weight wt p - 5, and the x^6, x^7 slots may be taken zero. So strictness alone never yields completeness; the failing ingredient is the finite spanning of R = k[x,y,z]/(x^5) by weight <= 12, false because y and z are free. Changed objects only, not actual source coefficients or Keller points, as stated; the 17zzw lifting countercontrols are untouched.

## E. Certificate, lift, negative semantics, frontier benefit

CONFIRMED CONDITIONAL. A found identity is a literal 1 = sum a_h fbar_h + sum b_j gbar_j in k[Y] (columns are literal coefficient vectors, no reduction in any quotient), so it is exactly 17zzw's identity (1) for the ideal of the used rows. With an authenticated integral local model O (fraction field the accepted FIELD B, residue field k), integral f5,f6,f7 and used g_j with their correct generic images, and a certified WHOLE special highest emptiness, Q_O is finite, Nakayama gives Q_O = 0, then Q_B = 0, C_B = E_B = 0 and A = E_B[s]/(s^3 - t0) = 0: the accepted generic guarded source zero. No rational basis, multiplication table, cofactor lift, flatness or reducedness is used. Negative side, correctly stated by the report: nothing follows from a rejected candidate; even an exact rank below 247 under the premise certifies only Q_k nonzero, which gives neither Q_B nonzero (Q_O may be torsion) nor guarded-source survival (17zzw section 6 second control); modular t0-nilpotence or L0^35 membership never lifts (section 6 first control). All bounds are slot counts of a fixed shape, not runtime, height, place or coefficient statements. Exact usable frontier benefit, without any new review farm, worker or solver authority: once the model, integrality and special highest certificates exist, the special unit question is ONE fixed 247-by-754 linear system over k in literal coefficients, complete for Q_k = 0 under the premise, so an exact rank decides Q_k = 0 rather than a single candidate search; its positive certificate is at most 754 residue-field scalars checked by coefficient equality in 247 slots, with no verified 35-basis, no 7x105 reductions and no multiplication table; a positive outcome feeds the accepted 17zzw lift directly, a negative outcome is INCONCLUSIVE for the source. If pursued it belongs in one later code/input/runtime/result gate; nothing is executed, registered, launched or allocated here.

## OPEN(S) RAISED

None. No new canonical OPEN. The retained unknowns are the producer's own: the authenticated model, integrality of f and the used rows, the WHOLE special highest certificate, and membership of 1 in the actual 247-by-754 column span. No charge_basis, no follow-on authority, no new global theorem.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly xmodel/f10-r2-modular-macaulay-bound-gate-fable5-20260910.md and box/f10-r2-modular-macaulay-bound-gate-fable5-20260910/input_custody.md, both absent at 15:49:57Z; no shared, frozen, input, ROOT, worker, code or old file was touched; no corpus scan, no other lane, no network, no scientific subprocess, no Git.

## Completion

Authoring attestation: every byte of both owned files was written by apply_patch (skeleton and custody 15:55:25Z; sections 0-1 and A-C 15:58:16Z, about 1490 words; sections D-Completion 15:59:01Z; the marker after this block), no Write/Edit tool, no shell redirection, no helper script, no source edit, no execution. Honest patch record: the first attempt at the A-C append lacked the end line, was rejected and changed nothing, and was resent unchanged; the D-Completion append, anchored on a blank context line with an end-of-file mark, landed ABOVE the header at 15:59:01Z, and was moved to the tail by a single delete/re-add patch just after 16:00Z with no text change other than this sentence. The body carries no 64-hex digest. Own WHOLE reread of this report (header, sections 0-1, A-E, OPEN(S) RAISED none, COLLISIONS EMPTY) is done before the marker is appended; the marker is confirmed absent before that append. No Seal and no charge_basis are authored; the adapter seals. Stop is the fixed cap, never reset. The marker below is the only standalone marker in this file and nothing follows it. All writers idle at the marker.

<!-- BODY-END -->
