# Independent blind whole-portfolio ideation — `20260829T2254Z` Sol 5.6 alternate

Date: 2026-08-29  
Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`  
Status: **BLIND IDEATION / NO MATHEMATICAL PROMOTION / NO JC2 RESULT**

## 0. Verdict

There is no proof or counterexample in this submission.  The new information
changes research allocation, not evidence tier.

The strongest genuinely new proof experiment is to replace the presently
postulated quotient-collision boundary complex by one extracted from the
**normalized off-diagonal fibre square of the actual Keller cover**.  This is
a concrete connection among Avenues 7, 16, 25, 28, 31, and 32.  It supplies
the base, functor, and off-diagonal connection that the 2026-08-27 nearby-
cycle proposal lacked.  Its sharp target is an injection

```text
reduced pole module (rank s-1) -> quotient-collision event module
```

whose target has rank `E_gen`.  Such an injection would prove the generic
QCS inequality directly, without pretending that every local collision has
positive excess.  Failure would expose an actual labelled kernel rather than
another unlabelled filling count.

The strongest new selector connection is to choose a counterexample of
minimal **topological degree** and study a monodromy block through its
explicit intermediate normal affine surface.  If every such intermediate
surface is `A^2`, a block gives a lower-degree polynomial Keller factor and
contradicts minimality.  If it is not `A^2`, its boundary/conductor is a
concrete possible source for the pair-square collision class.  Repository
history previously named the missing implication “imprimitivity must give a
smaller polynomial Keller map”; it did not formulate this intermediate-
surface dichotomy.

On K00, closing `R4-00` remains the cheapest finite win, but the
`e=3,m=1,G9` carry is now the highest-information mathematical cell.  A
broad all-ramification enumeration before that discriminator would encode a
false coefficient-blind recurrence.  Source completeness, inverse-limit
lifting, actual-source reachability, and algebraization remain separate and
strictly downstream of finite-jet survival.

## 1. Custody and scope

I verified the state packet at full SHA-256
`d1cc6a649b120253675c1694a2afc4b0c929393cd11f66c99f2635b49185236a`
and verified the frozen `HEAD`.  I also verified the charged hashes of
`APPROACHES.md`, `ladder/REDUCTION.md`, and the prior `1808Z` synthesis before
using them.  The novelty searches below were confined to the charged
canonical history and that prior synthesis.  No sibling file from this blind
round, web source, external message, or formalization tree was consulted.

The maximum global implication remains

```text
JC2 false
 -> some globally GGV-minimal standard counterexample
 -> the same pair in a td-preserving Sigray normalization
 -> td >= 6
 -> a finite entry menu at each fixed td.
```

It does not select `td=12`, type `(2,3)`, U1, B25, S17, or a complete book.
`G2-PSC` is packet/sheet fidelity; `G2-BD` is bounded delay after a residue-A
landing.  Neither implies the other.  B and S occurrences, maps, and values
remain separately typed.  All dispositions below are priority judgments,
not theorem promotions.

## 2. All 46 avenues: compact disposition

| # | Avenue (abbreviated) | Disposition | Reason |
|---:|---|---|---|
| 1 | GGV corner farm | unchanged | Exact bounded families remain useful, but neither a degree nor `td` ceiling follows. |
| 2 | Sheet/tree ladder | unchanged | Still the principal proof backbone; selector, source theorem, full landing, and all-`td` control remain upstream. |
| 3 | Strip/residue ODE | unchanged | Genuine scoped theorem, with no new depth-three or off-strip extension. |
| 4 | Formal germs/algebraization | **raise** | The exact `e=3` G8 survivor makes transition images and all-order lifting, rather than another isolated jet, an immediate discriminator. |
| 5 | Jung–van der Kulk descent | unchanged | Cusp-leading forms still block a degree-lowering lemma. |
| 6 | Abhyankar–Moh recognition | unchanged | The correct one-place object remains an unconstructed component of `A(F)`. |
| 7 | Nonproperness / `A(F)` | **raise** | The pair-square proposal gives `A(F)` a precise role as the base boundary of an actual finite etale cover, not a raw direction pencil. |
| 8 | Formal inverse trees | unchanged | No uniform truncation mechanism or new plane-specific cancellation appeared. |
| 9 | Conjecture E | unchanged | Sparse recurrence remains interesting but unbounded quantification is untouched. |
| 10 | HC4 bridge | unchanged | The named quintic observable returned `NO LEVERAGE`; no replacement observable is specified. |
| 11 | Mathieu/GMC/Poisson ladder | unchanged | The route remains refuted. |
| 12 | Face isolation / p-adic multinomials | unchanged | One determinant identity still has no parameter tending to infinity. |
| 13 | Dixmier `DC(2)` | unchanged | Bounded slices do not repair the missing noncommutative normal form. |
| 14 | `End(A1)` disproof audit | unchanged | Defensive T2 replay remains possible but has no candidate endomorphism. |
| 15 | Spectral surfaces/PDOs | unchanged | The required two-dimensional dictionary is still the research problem. |
| 16 | D-module/holonomic index | **reopen** | A concrete client now exists: direct/nearby-cycle data of the actual off-diagonal finite cover, with specified base and support. |
| 17 | BCW/Druzkowski stabilization | unchanged | Plane-origin recognition in a higher-dimensional class remains absent. |
| 18 | Graded/GIT symmetry | unchanged | The graded counterexample hunt remains closed; orbit limits lose nonproperness. |
| 19 | Characteristic-`p`/Witt lift | unchanged | All-level growing-support survival is not a bounded polynomial lift. |
| 20 | `p`-curvature formalism | unchanged | Its known implication bridges still consume false stronger conjectures. |
| 21 | `p`-adic injectivity/model theory | unchanged | Hensel sheets do not remove `A_infinity` or produce a rational global action. |
| 22 | Diophantine heights | unchanged | Uniform control of the unknown pencil is still inaccessible. |
| 23 | Analytic global inverse | unchanged | Holomorphic and dimension-free metric slogans retain exact countercontrols. |
| 24 | Real/Pinchuk | unchanged | Real noninjectivity remains poorly coupled to complex Keller structure. |
| 25 | Monodromy/passports | unchanged | Pair-square data may become a source-labelled client, but no coupled branch-cycle obstruction exists yet. |
| 26 | Primitive monodromy bound | unchanged | Minimal-`td` factorization makes a sharper first test, but no group-theoretic `td` bound is presently earned. |
| 27 | Links/plumbing/PALF | **lower** | The equality germ/page-genus mismatch confirms that unlabelled topology and Euler counts are not a QCS bridge. |
| 28 | Log surfaces/BMY | unchanged | Pair-square compactification is only a candidate source packet; the required boundary divisor data have not yet been constructed. |
| 29 | Hamiltonian/LND frames | unchanged | The Gauss–Manin obstruction remains a reformulation without a universal receiver. |
| 30 | Affine-surface classification | **raise** | A monodromy block now names a specific intermediate normal surface whose being `A^2` has a lower-`td` consequence. |
| 31 | Integrality/ZMT/Rees | **raise** | The same intermediate algebra and pair-square boundary give Rees valuations a concrete, actual-map client. |
| 32 | Off-diagonal collision ideal | **reopen** | The exact secant simplification asked for source-derived saturated boundary data; the pair-square boundary is a precise candidate, not another unit-ideal attack. |
| 33 | Symplectic residues/generating functions | **lower** | Untwisted residues are exact-form bookkeeping, and zero analytic excess defeats bare positivity; retain only client-specific classes. |
| 34 | Two-dimensional sweep/pole removal | unchanged | No spare-coordinate or denominator-removal mechanism appeared. |
| 35 | Descent from dimension at least three | unchanged | Known counterexample mechanisms remain structurally three-dimensional. |
| 36 | Guided sparse/source search | **raise** | One integer K00 cell remains and the ramified G9 test can decisively validate or refute the next normal-form mechanism. |
| 37 | Finite-field census | unchanged | Tiny-characteristic extras still do not address bounded characteristic-zero lifting. |
| 38 | Tropical geometry | **raise** | The exact `(e,m,h)` load walls and the `m=2e` mixed wall finally provide a named, source-derived polyhedral object; use it only to route initial ideals. |
| 39 | Cohomological cluster | unchanged | Pair-square cohomology is accounted under the concrete Avenue-16 experiment; slogans without that object stay low. |
| 40 | Free-associative lift | unchanged | Commutative determinant one still does not imply free invertibility. |
| 41 | Scaling deformation | unchanged | The principle remains falsified by higher-dimensional counterexamples. |
| 42 | Markus–Yamabe | unchanged | Constant Jacobian does not supply the required spectrum. |
| 43 | Ritt decomposition | unchanged | Composite-coordinate reductions still return to the same polygon constraints. |
| 44 | “No prime `td`” | unchanged | The claimed proof remains invalid; only the repaired membership theorem survives. |
| 45 | Differential Galois | unchanged | The inverse PDE remains tautological outside the known ODE strip. |
| 46 | Formal certification | unchanged | Valuable for leaf rigor and scope, not a discovery route or finite-universe theorem. |

The net change is deliberately concentrated: raise the two source-complete
falsification instruments (4, 36), the first exact ramification fan organizer
(38), and the actual algebraic objects needed by the new proof experiment
(7, 16, 30, 31, 32); lower only the two source-free topological/exact-form
shortcuts (27, 33).

## 3. Reranked gaps

### 3.1 Proof gaps

1. **Global selector or uniform bypass.**  There is no reviewed arrow from a
   hypothetical counterexample to bounded `td`, type, entry, U1, or a named
   B/S occurrence.  An all-`td`, actual-map theorem may bypass selection;
   conditional td12 work cannot.
2. **Polynomial-origin QCS incidence.**  One needs either strict quotient
   weight from actual polynomial/rational origin plus the separate
   `#atypical >= s-1` incidence, or a direct canonical injection of the
   reduced pole module into an `E_gen`-dimensional collision module.  The
   analytic equality surrogate refutes pointwise strictness without such an
   origin input.
3. **Actual source packet and full configuration landing.**  A `PairRef`,
   connected generic fibre, complete quotient-line/baseline inventory, and
   a corrected source/tree theorem must precede residue-A arithmetic.  The
   on-axis marked-event theorem is not a full downstream book; off-axis,
   composite single-pole, and post-jump `M>=2` sectors remain.
4. **Cofinal type/degree control.**  `KJN(C)` is type-relative.  Neither
   primitive monodromy, bounded GGV farms, nor local books currently bounds
   all types or topological degrees.
5. **Foundation and provenance closure.**  The distributed Sigray repairs
   need one source theorem and a total fail-closed configuration-to-record
   map.  This is independent of `G2-PSC`; a pure Sigray proof may bypass
   `G2-PSC` but not source fidelity or full landing.

### 3.2 Counterexample and falsification gaps

1. **Source completeness/reachability.**  No theorem says that every actual
   counterexample degeneration lands in normalized V20R2 K00, its declared
   support/open, receiver, and Gate-T source.  Closing a source atlas without
   this arrow is not a JC2 theorem.
2. **Valuative closure.**  Integer `K[[Lambda]]` sections miss ramified DVR
   arcs.  Other `e,m`, load faces, `C6=0`, and supports remain.  The exact
   all-face `e=2,m=1` corollary is still review-gated.
3. **All-order lifting.**  A compatible finite jet is not an inverse-limit
   point.  The `e=3,m=1` G8 fixture proves that killing a coefficient can
   manufacture a false obstruction; G9 is the first honest test.
4. **Arc-to-source and algebraization.**  Even a formal source arc is not an
   algebraic germ, attained source point, polynomial map, or counterexample.
   Reachability from upstream map data must be proved before an
   algebraization theorem has a correctly typed input.
5. **Bounded polynomial descent in the characteristic-`p` lane.**  Growing
   support at every Witt level and a restricted-analytic limit do not give a
   characteristic-zero polynomial.  A fixed-support or controlled-degree
   tower remains the discriminator.
6. **Independent disproof mechanisms.**  `End(A1)`, two-dimensional pole
   removal, and higher-dimensional descent remain legitimate falsifiers but
   presently have no candidate object comparable in precision to K00 or the
   Artin–Schreier tower.

## 4. Global selector and QCS: prove/falsify/bypass portfolio

| Objective | Prove lane | Falsify lane | Bypass lane |
|---|---|---|---|
| Global selector | Choose a minimal-`td` counterexample; prove every nontrivial monodromy block algebraizes through `A^2`, hence minimality forces primitive monodromy.  Add group constraints only after that theorem. | Construct an intermediate normal surface of the exact block type which is not `A^2`, or show primitive groups satisfying every actual infinity constraint occur cofinally.  Either result stops a false selector without touching JC2. | Prove an all-`td` source theorem plus uniform obstruction, or prove QCS directly from the actual cover; then no td12/U1 selector is needed. |
| QCS / strictness | Build the pair-square pole-to-event injection.  A narrower alternative is `STRICT-COLLIDE-POLY` plus an independent pole/atypical incidence theorem. | Seek a rational, source-compatible polynomial-origin equality germ with zero weight excess.  The known analytic square-root surrogate is a control, not this falsifier. | Derive `E_gen >= s-1` globally from boundary incidence, avoiding a claim of positive excess at each collision. |
| Conditional td12 laboratory | Only after an actual td12/U1 source packet, run the family-aware sextet and B/S occurrence/value gates. | Retain `U1*(R)` and family continuations as necessary-data controls against selectors based only on reduced laws. | A uniform pair-square or all-td sheet theorem makes the conditional laboratory unnecessary. |

No row above licenses combining a formal tree, abstract cover, reduced floors,
and local series counts into an actual `PairRef`.

## 5. Detailed research cards

### Card 1 — `PAIR-SQUARE-QCS/v1`

- **Novelty:** `NEW`.  Charged history contains the collision ideal, an
  abstract pole-incidence/suspension proposal, and a rejected zero-locus
  nearby-cycle proposal.  It contains no off-diagonal fibre-square or pair-
  cover construction.  The earlier rejection explicitly demanded a base,
  functor, and off-diagonal connection; this card specifies all three.
- **Dependencies:** an actual nonproper polynomial Keller map `F`, its
  maximal target open `U` over which `F` is finite etale, the finite cover
  `X=F^{-1}(U) -> U`, a source-labelled compactification, and the exact
  quotient-line/baseline definition of `E_gen`.  Residue-A formal data alone
  are not an input.
- **Exact theorem/experiment:** form

  ```text
  Z = (X x_U X) minus diagonal
  ```

  and the normalized closure of `Z` in a product compactification.  Define a
  boundary pair-event chain complex functorially under admissible blowups.
  Prove that its quotient-collision module `C_coll(F)` has dimension
  `E_gen`, and construct

  ```text
  iota_F : reduced H_0(pole components; Q) -> C_coll(F).
  ```

  The target theorem is injectivity of `iota_F`; since the source has rank
  `s-1`, it gives `E_gen >= s-1`.  A secondary output is a conductor/weight
  class whose nonvanishing would imply strict excess at a labelled event.
- **Cheapest discriminator:** before computing any example, prove or refute
  independence of the chain complex from the chosen normal-crossings
  compactification and match its local boundary matrix to the already
  reviewed collision-capacity-minus-excess formula.  Use the type-`(2,3)`
  analytic equality surrogate as a negative input-control: it must fail the
  actual rational-cover hypothesis rather than be silently counted as a
  Keller instance.
- **Outcome meanings:** (i) a canonical injective map proves the QCS rank
  inequality at the stated actual-map scope; it does not by itself finish
  the remaining global reduction; (ii) a canonical noninjective map gives an
  explicit labelled kernel and falsifies this bridge, not QCS or JC2; (iii)
  compactification dependence shows the construction is another topological
  costume and stops the card; (iv) a well-defined weight class but no global
  injection narrows the result to `STRICT-COLLIDE-POLY`.
- **Stop condition:** stop before PALF/Kirby enumeration if the event module
  cannot be identified canonically with `E_gen`, if blowup invariance fails,
  or if source labels are discarded.
- **Expected information gain:** very high.  One desk theorem either supplies
  the missing boundary complex from an actual source or explains exactly why
  that strategy cannot be canonical.
- **Compute class:** desk algebraic topology/normalization and small exact
  integer matrices; no heavy CAS.  Any later normalization elimination is a
  separately preregistered AWS task.
- **Parallel ownership:** prove owner—compactification and functorial chain
  map; falsify owner—blowup/analytic equality controls; bypass owner—derive
  QCS rank directly without local strictness or a td selector.

### Card 2 — `MIN-TD-BLOCK-SURFACE/v1`

- **Novelty:** `NEW` connection.  The prior synthesis says that vertical
  imprimitivity would have to extend to full monodromy and then a smaller
  polynomial Keller map.  History search found no minimal-`td` card and no
  construction of the intermediate normal surface or its boundary
  alternative.
- **Dependencies:** assume JC2 false and choose a counterexample `F` with
  minimal positive topological degree `d`; use full generic monodromy, not a
  one-fibre passport.  For a block field `K`, set

  ```text
  B_K = integral closure of C[f,g] in K,
  Y_K = Spec(B_K).
  ```

  Normality of `C[x,y]` places `B_K` inside `C[x,y]`, so `A^2 -> Y_K -> A^2`
  is an actual algebraic diagram.
- **Exact theorem/experiment:** prove the intermediate-`A^2` theorem for a
  planar Keller map: every nontrivial monodromy block has
  `B_K isomorphic to C[u,v]`.  Then the diagram is a factorization by
  polynomial Keller maps; at least one nonautomorphic factor has degree
  strictly between `1` and `d`, contradicting minimality.  The fallback
  dichotomy is: either `Y_K=A^2`, or a specified boundary/Rees divisor of
  `Y_K` maps nontrivially to the Card-1 pair-event module.
- **Cheapest discriminator:** derive the class group, Makar-Limanov, and
  log-boundary conditions forced on `Y_K`; test them on compositions of tame
  automorphisms as positive controls and on small generically finite
  non-Keller polynomial maps as negative controls.  Do not run a primitive-
  group database until block algebraization is typed.
- **Outcome meanings:** (i) the `A^2` theorem makes a minimal-`td`
  counterexample primitive and gives Avenue 26 a legitimate client; (ii) a
  non-`A^2` example satisfying all Keller-derived intermediate conditions
  refutes the factor selector and becomes a concrete Avenue 30/31 boundary
  object; (iii) a nonzero boundary class feeds QCS strictness; (iv) a result
  valid only when `F` is already finite is circular for a counterexample and
  is `COSTUME`.
- **Stop condition:** stop if the proposed invariant uses properness of
  `A^2 -> A^2`, assumes finite generation beyond the integral closure, or
  turns a local fibre block into global two-dimensional monodromy without a
  theorem.
- **Expected information gain:** high.  It converts the vague selector into
  a clean surface classification/factorization dichotomy and may expose the
  exact obstruction to monodromy descent.
- **Compute class:** desk commutative algebra and surface theory; seconds-
  scale exact rings only.  Any broad class-group computation is AWS-only
  after a frozen finite presentation exists.
- **Parallel ownership:** prove owner—intermediate algebra and Jacobian
  factorization; falsify owner—normal-surface countermodels; bypass owner—map
  a non-`A^2` boundary directly into the Card-1 collision class.

### Card 3 — `K00-E3M1-G9-TRANSITION/v1`

- **Novelty:** `KNOWN`.  This is the exact mandated successor to the new G8
  fixture, upgraded from a point check to a transition-image theorem.  It is
  not a claim of an all-`e` recurrence.
- **Dependencies:** frozen literal V20R2 rows; the exact `e=3,m=1` G8
  fixture; `C6`, `k10`, and `Jdet` unit opens; every second-surface
  coefficient; four free directions in `v`; the full five-dimensional G8
  lift fibre; all seven rows.  The unloaded radical may be used only after a
  coefficient block is independently unloaded.
- **Exact theorem/experiment:** emit G9 literally and compute the
  constructible image of the G9 lift equations over the complete G8
  rank-one solution fibre, eliminating only the newest coefficient block.
  Return exactly one of: empty image, a proper nonempty image with equations
  and localizers, or a dominant transition with fibre dimension.  The known
  G8 fixture is a mandatory old-pass control, and changing its second surface
  coefficient is a mandatory fail control.
- **Cheapest discriminator:** symbolic row emission, linear newest-block
  rank, and cokernel reduction on the two stable signs.  This should decide
  whether G9 is a constant unit obstruction before any nonlinear
  elimination.
- **Outcome meanings:** (i) empty kills this `e=3,m=1` rank-one G8 branch at
  G9, not all ramified cells; (ii) proper nonempty yields an exact G9 finite
  jet and the next transition ideal, not an arc; (iii) dominant transition
  raises all-order obstruction/lifting above more finite grades; (iv) a load
  collision revises the calendar and forces a coupled fan rather than an
  isolated K10 proof.
- **Stop condition:** stop and retype if any mixed loaded row is replaced by
  `R=0`, any boundary series is assumed to have exact order one, an old free
  coefficient is set to zero, or the frozen source/replay custody changes.
- **Expected information gain:** very high within K00.  It decides whether
  the first counterexample to naive induction is only a one-grade delay or a
  genuine lift mechanism.
- **Compute class:** desk source emission/rank design first.  Any uncertain
  elimination is preregistered and moved to AWS; no heavy local CAS.
- **Parallel ownership:** prove owner—empty transition certificate; falsify
  owner—explicit G9 lift and mutation controls; bypass owner—derive a normal-
  bundle obstruction map uniform in stable rank without assuming its
  vanishing.

## 6. K00 allocation and the role of software

The K00 tasks should not be read as one serial ladder:

| Task | Priority and exact value |
|---|---|
| `R4-00` closure/review | Finish immediately because it is cheap and would close the literal integer atlas.  Its provisional rank-one/rank-two packets still require hostile review.  Even success says nothing about ramified arcs. |
| `e=3,m=1,G9` | Highest-information new cell.  It tests the first honest failure of the old induction while all late loads are still separated. |
| General `(e,m,h)` calendars | Build after G9 as a rational polyhedral routing table with separate normal and surface-restricted orders.  The wall `m=2e` and every K10/K6/K2/target tie must be explicit.  A calendar is not an emptiness theorem. |
| Source completeness | Run in parallel at the specification/proof level: enumerate supports, `C6=0`, load faces, receiver/Gate T, and prove which actual degenerations the source functor covers.  This has the largest multiplier on every local kill. |
| All-order lifting | Invoke only for a constructible transition survivor.  Study images of successive jet maps, not isolated nonempty fibres. |
| Reachability | After a formal source arc exists, prove it comes from the upstream normalized map data; no formal-source survivor supplies this automatically. |
| Algebraization | Last, after a reachable arc.  Artin/Greenberg language is inapplicable until the complete integral finite-type scheme and hypotheses are present. |

The mathematical-software accelerator is `JET-IMAGE/v1`, a source-hashed
transition compiler shared by Avenues 4, 19, and 36.  Given frozen sparse
rows, a valuation/load manifest, opens, and an old solution stratum, it emits
the next coefficient map, newest-block Jacobian, Fitting/rank strata,
projection-image obligations, and mandatory old-pass/fail mutations.  Its
certificate vocabulary is restricted to

```text
EMPTY_TRANSITION
PROPER_NONEMPTY_FINITE_JET_IMAGE
DOMINANT_FINITE_JET_TRANSITION
UNKNOWN
```

and contains no `ARC` token.  Exact linear/cokernel stages run locally in
seconds; nonlinear image closure is a frozen AWS job.  The decisive first
experiment is Card 3.  Reusing the same transition semantics on the
Artin–Schreier Witt tower would measure support escape rather than confuse
nonempty digits with a characteristic-zero polynomial.

## 7. Campaign-systems card — `UPGRADE: CLAIM-CONTRACT-LINT/v1`

- **Problem:** byte seals protect custody, but recent reports still require
  manual checking that title, lifecycle, theorem prose, replay conclusion,
  opens, and object kind agree.  The literal `finite jet -> arc` and
  `producer -> promoted` mistakes are high-cost ingestion failures.
- **Upgrade:** add an opt-in, report-local machine-readable claim contract
  recording `object_kind`, quantifier/ring, grades, opens, source hash set,
  evidence tier, reviewer requirement, and forbidden stronger nouns.  A
  linter compares that block with replay JSON and seal metadata.  This is not
  authentication and does not decide mathematics.
- **Novelty check:** the charged history has a byte-seal helper and mentions
  a future access fence; it contains no scope/lifecycle claim linter.
- **Smallest measurable acceptance test:** on synthetic files only, one
  correct finite-jet producer passes and five one-byte/one-field mutations
  fail independently: `arc` substituted for `finite_jet`, `promoted` for
  `review_gated`, one omitted open, one wrong dependency hash, and replay
  conclusion mismatch.  Ordinary and optimized Python must print identical
  results in under one second and leave the fixture tree byte-identical.
- **Stop condition:** do not parse free-form mathematics to infer truth, do
  not make the contract canonical evidence, and do not migrate callers until
  synthetic review passes.  A linter pass means only internal claim
  consistency.

## 8. Ranked launch portfolio

1. **Card 1 desk gate:** define the pair-square event module and decide
   compactification invariance/matching to `E_gen`.  Run prove and falsify
   owners concurrently; stop before topology search if the module is not
   canonical.
2. **Card 3 plus cheap integer closure:** emit the `e=3,m=1,G9` transition
   while existing hostile review of `R4-00` unit packets continues.  The G9
   descendant may start provisionally from frozen exact inputs without
   waiting for the all-face or unloaded-radical reviews; any promotion still
   needs a different-model hostile review.
3. **Card 2 desk discriminator:** construct the intermediate algebra of a
   monodromy block and prove the factor-Jacobian lemma before any group
   census.  Surface countermodels and the proof run in parallel.
4. **Software/system microtasks:** specify `JET-IMAGE/v1` from the G9 source
   and implement `CLAIM-CONTRACT-LINT/v1` only on synthetic fixtures.  These
   do not block mathematics.

No heavy local computation is justified.  No outcome above is an attained
valuation, formal arc, algebraic germ, map, counterexample, or JC2 result
unless the separately named arrows are actually proved.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `28201`.
- Body SHA-256:
  `0477ab88d170343f70b1f353e88d7f112bc35308631228cdf85302c472b83fd5`.
- Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`.
