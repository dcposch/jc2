You are the hostile independent reviewer of the first claimed complete
elimination of the `(8,12)` exact-order-four coefficient-infinity leaf.
Review the frozen named bytes, not producer verdict language.  This is a
narrow evidence review: do not print `git status`, enumerate the worktree,
or inspect unrelated dirty files.  Read only the target and the charged
artifacts named by it and below.  Run no local Singular, Sage, msolve, Lean,
or substantive exact Python; hashing, source reading, and hand algebra are
enough.

Target:
`xmodel/max12-812-order4-source-to-lemniscatic-elimination-20260826.md`

Required target SHA-256:
`0ac7da1db8eb37c638f4d8014ffc4e62741c808212dccd5c6e8cbb1d4602d49b`

The previous review tag was deliberately cancelled before a report because
the producer added the missing exact saturation-seed custody chain.  Do not
consume that cancelled run or its obsolete target SHA.

Inspect both V3 AWS endpoints under
`cases/max12_812_order4_seeded_projection_v3_rawgb_20260826/`, the relevant
V2 endpoints under
`cases/max12_812_order4_seeded_projection_v2_20260826/`, their frozen
compilers/runners/manifests, and the exact source saturation input/stdout
from which the 71-element basis was extracted.  Rehash all charged evidence.
Do not treat a PASS token as a theorem without checking the code.

Independently attack all of the following load-bearing points.

1. Exact source and exhaustion.  Check that the reviewed source theorem has
   exactly `mu4=0` and `mu4!=0`, that the latter has no omitted lower Faber
   loads, that weighted normalization to `r4=1` loses no geometric source,
   and that `8 dr7/dx=j/u!=0` proves `r7` nonconstant.  Check the final use of
   the independently confirmed Shioda--Hall theorem in the `mu4=0` branch.

2. Saturation-seed equality, not containment.  Rehash the exact source input
   and stdout:

   `d2fedf6c9b2b09787c7e45a00f1626ac10f7d4c68c44030102758021a55c9496`
   and
   `5e780dcf82d2a3ac17c3ae7957c819c8b7b8a1a1de6231f88e4f808a2b348c45`.

   Verify the source really constructs the intended six-generator `Itail`,
   checks its independent shifted-polynomial presentation, executes
   `list SS=modSat(Itail,ideal(r7)); ideal J=modStd(SS[1]);`, and prints the
   unique 71-line basis between unambiguous delimiters with all required
   sentinels.  Audit compiler SHA
   `7ede3c5a34bf4d2a1592dedaa268307bd464c8dd5b22936f684999aefbb1a5bd`:
   it must pin both full SHAs, reject missing/nonunique sentinels and
   delimiters, require exactly 71 basis lines, and copy those exact bytes
   into `JQ`.  Verify scalar normalization preserves the generated Q-ideal
   and raw/normalized ideal equality is checked.  `SOURCE_TO_SEEDED_Q=1`
   alone proves only containment and is insufficient.  Fail the theorem if
   this custody/equality chain is not actually closed or if `JQ` could be an
   overrestrictive guessed component.

3. Endpoint custody.  Rehash every V2/V3 manifest, compiler, emitted input,
   stdout, metadata, and validation file consumed by the target.  Confirm V1
   is unused and V2 prime is only a near-pass.  Confirm both V3 hosts consumed
   identical frozen source and emitted input.  The actual engine return is
   `rc=0` in each `.meta` and `engine_rc=0` in validation; no `<tag>.rc`
   exists.  Audit stdout for `?`, `=FAIL`, and `// ** GQ is no standard basis`.
   Explain whether V3's justified Q-basis `isSB` mark and zero-warning
   validator repair the V2 warning.  The V2 `a6` endpoint is an independent
   control, not a premise for reductions against an unflagged basis.

4. Raw good reduction.  Verify V3 compares
   `std(lead(GredRaw))` and `std(lead(std(GredRaw)))` both ways before marking
   the raw positive-characteristic list as a standard basis.  Prove or
   refute that equality of those generated leading ideals, together with
   equality of the generated polynomial ideals, is exactly sufficient for
   `GredRaw` to be a Groebner basis.  Check the later reduction sentinel is
   meaningful and noncircular.  Audit every coefficient lies in
   `Z_(32003)`, monic leading terms survive, ring maps and term orders agree,
   and native ideal and initial-ideal comparisons are two-sided.

5. Prime lift and dimension.  Check `minAssGTZ` plus two-sided ideal equality
   proves the native special fibre is itself prime.  Re-derive that a monic
   standard basis gives a free standard-monomial `Z_(32003)`-module and that
   a domain special fibre forces a domain generic fibre even in positive
   dimension.  Charge exact characteristic-zero `dim J=1` separately.
   Verify `J` prime and `a6 notin J` imply `J:a6^infinity=J`, including after
   extension to C; do not infer this solely from the V2 `a6` control.

6. Nonverticality.  Attack the argument from the one-dimensional special
   `(a5,a6)` contraction to generic nonverticality.  Check primitive content,
   possible disappearing relations, and whether both
   `q=a5^2/a6^3` and `v=a6^8` could nevertheless be constant.  Keep this
   separate from `dim J=1`.

7. Componentwise projection.  Recheck exact Q-membership in `P(q,v)`,
   geometric integrality of the residual plane, finiteness of the relevant
   function-field extension, and the passage after C-base change if the
   source domain geometrically splits.  Prove or refute that every geometric
   coefficient component dominates the same residual plane and, after
   adjoining the actual `y=a6^2`, maps nonconstantly to the irreducible
   normalization of `P(q,y^4)=0`.  Search for vertical components, chart
   loss, nilpotents, denominators, or a quotient/source mismatch.

8. Actual source component.  Verify a genuine source has nonconstant
   coefficient map from `r7`, and that its image closure is a full
   one-dimensional geometric coefficient component rather than a proper
   curve within one.  Verify `a6`-chart coverage for that component.

9. Descent.  Check the exact deck characters really make
   `q=a5^2/a6^3` and `y=a6^2` invariant, so the map descends to
   `C/mu4=P1_x`; verify extension across poles and preservation of
   nonconstancy.  Independently re-derive the genus-one/regular-differential
   contradiction and the use of the confirmed lemniscatic target and
   tangent erratum.

10. Scope.  Confirm that `mu4=0` plus `mu4!=0` exhaust exact order four for
    all U.  Reject any inference about order two, order one, all `(8,12)`,
    maximum twelve, or JC2.

Identify the smallest failing identity or missing hypothesis, if any, and
state the strongest corrected theorem.  A semantic endpoint problem,
saturation-custody gap, or source-component coverage gap is blocking, not
cosmetic.

Write the review only to
`xmodel/max12-812-order4-source-to-lemniscatic-elimination-review-grok-v2-20260826.md`.
Include the recomputed target SHA and exact evidence hashes, explicitly treat
the Singular warnings and return-code custody, and end with exactly one of
`CONFIRMED`, `REPAIR`, or `REJECTED`.  Do not edit the target, any V1/V2/V3
artifact, or a top-level campaign file.
