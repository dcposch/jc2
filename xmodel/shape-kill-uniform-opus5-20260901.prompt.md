# Research lane: SHAPE-KILL — uniform kills for the residual shape families

You are the flagship lane on OPEN[CAMPAIGN-PIN-D1-DMIN-BOUND] from
the opposite side: instead of bounding d_min, kill each surviving
shape family UNIFORMLY in the degree. The reviewed cage: an escaping
N=4 residual curve has g = gcd(d,n) >= 2 and reduced shape
(d/g, n/g) in { (u,1), (odd u, 2), (4,3) }; the reviewed battery
disposed of everything with d <= 9; the campaign-pin lane found no
promoted d_min bound, so the families continue formally at
d = 10, 11, ....

charged_input=xmodel/campaign-pin-gpt55-20260901.md
charged_input=xmodel/block-descent-a1-rowkill-coordinator-integration-fable5-20260901.md
charged_input=xmodel/row-nf-exhaustiveness-hostile-review-sol56-20260901.md

Your inputs are frozen read-only copies in `{{LANE_INPUTS}}`;
verify these SHA-256 hashes first and stop on mismatch:

```text
cff4116c19728da875c141133f3b7e739e979ebcb606694272cb14ee6e5130c1  {{LANE_INPUTS}}/campaign-pin-gpt55-20260901.md
46e08515b12d21780b727c9035872fdb3a9bfb01c4c8ebc74d0efc950b6258fc  {{LANE_INPUTS}}/block-descent-a1-rowkill-coordinator-integration-fable5-20260901.md
723a3cc18b1b01cb451c19dbe27bb63778d09a125048e1c94591f4f94ba4f5ac  {{LANE_INPUTS}}/row-nf-exhaustiveness-hostile-review-sol56-20260901.md
```

For each family, attack uniformly, fail closed:

1. **(u,1) family** (n | d, n = g): the infinity structure at
   general u — derive the delta-sequence shape and the analogue of
   the EXHAUST semigroup argument: with 3 in S_D? no — derive what
   IS in S_D for shape (u,1); the promoted block theorems (A'(1)-(4)
   at n'=1: ALL tube products equal — the (4,2)-style collapse!)
   may kill the whole family: at n'=1 the outer cable is the
  identity-permutation full twist and the promoted collapse forced
   im <= S_3 at (4,2) — re-run that argument at general (ug, g):
   does the block-product equality force |im| <= 6 uniformly? If
   yes, the family dies at EVERY degree.
2. **(odd u, 2) family**: outer shape (u,2) coprime (u odd) — the
   promoted coprime Theorem A reruns at the outer level (as at
   (6,4) = (3,2)): derive the order/parity constraints at general
   odd u and combine with the promoted Theorem A(3) fork; then the
   NO-TORUS/Shirane mechanism is degree-6-specific — determine
   whether the INF-TRIVIAL + even-word mechanism alone (which is
   NOT degree-specific) plus the S_3-resolvent descent gives a
   contradiction with a DEGREE-INDEPENDENT classification input
   (normal S_3 triple covers of P^2 with branch degree 2d: for
   which d does a Shirane/Tokunaga-type rigidity exist? — fetch
   Tokunaga T91's general framework and extract the branch-divisor
   condition at general even degree: the Cardano form F = -4a^3-27b^2
   with deg a = 2d/3... derive the divisibility obstruction: branch
   degree 2d needs 3 | 2d or the two-form structure — type exactly).
3. **(4,3) family** (g >= 2 scalings of the coprime (4,3)): the
   coprime (4,3) itself is in the promoted kill-list survivors —
   wait: (4,3) COPRIME was killed by the promoted coprime Main
   Theorem; the family here is (4g, 3g): outer shape (4,3) coprime —
   rerun Theorem A at the outer level and the cable analysis;
   determine the general-g fate.
4. Assemble: each family KILLED uniformly (then the d_min bound is
   unnecessary and the campaign pin closes: the N=4 residual is
   EXACTLY the six candidate types), or the exact surviving
   (family, degree-range) pairs typed.

Desk-scale exact reasoning only; you may fetch and hash primary
literature (record exact sources); never run any CAS or computation of
uncertain duration on this machine. Do not edit canonical ledgers, any
charged file, or inspect `jc2-lean`. 6 hours hard budget.

Write one report and no other file:

```text
xmodel/shape-kill-uniform-opus5-20260901.md
```

Create the report file with a skeleton of section headers as your very
first action — the skeleton must NOT contain the `<!-- BODY-END -->`
marker. Append each completed section as you finish it, as a separate
bounded write (aim under 1,500 words per write). Only after the final
section is on disk, append the standalone `<!-- BODY-END -->` line.
If the budget runs short, finish the current section, type the rest
OPEN in one short paragraph each, then seal. Keep it under roughly
6,000 words. Do not include a `charge_basis` declaration.
