# CAPRUN late-flow completion — Sol engineering packet

- First action: `2026-09-12T04:57:46.654447877Z`.
- Basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
- Lifecycle: `INTERNAL-UNREVIEWED`, disabled/unbound.
- Scope: fixed-code/late-data ADMIT, ATTEST, INSTALL, and separately authorized RELEASE entry points; no worker or science action.

## Status

`COMPLETE_INERT_COMPOSITION`, not reviewed, bound, armed, installed, or run. No worker/batch was selected. Publication supplies all fixed consumers and phase composition; prospective use still requires preholder constant binding, installation, a different-model source review, and ROOT's four separate invocations.

## Deliverables

- `FROZEN-CONFIG.template.json`: one frozen constant object for exact worker, transport, original clocks, coordinator receipt/native digests, exact timer/service property maps, prepared inputs, installed fixed scripts, and four separate ROOT entry authorities.
- `FLOW.tail.template.js`: concrete `ADMIT`, `ATTEST`, `INSTALL`, and `RELEASE` implementations. `compose-preholder.template.sh` appends this tail to the unchanged pinned `ORCHESTRATION-KERNEL.js` and freezes four entry sources before admission.
- `observe-and-leaves.template.sh`: fixed preholder-installed consumer of exactly three late positional DATA values. It rechecks host/boot/all four namespaces/holder argv and identity, caps, root-only migration surfaces, registration absence, FIFO, output-child emptiness and the original stop; creates the exact nine fresh leaves; then emits the binder's exact observation schema.
- `release-check.template.sh`: separately called fixed release guard. It freshly rechecks late identity, holder age, host/namespaces/cgroup/caps, exact registration and card installation, manifest/native hashes, exact empty leaf identities, empty authority/frozen/writer directories and FIFO identity before exec of the unchanged bounded FIFO writer.
- `PREHOLDER-MANIFEST.template.sha256`: exact ten-item preholder identity census: four entry sources, frozen config, admission, observer, receiver, release guard, and FIFO writer.
- `STATIC-CONTROLS.md`: manual controls against the six identified defects and authority-boundary audit.

## Concrete phase composition

`ADMIT` first authenticates the exact coordinator receipt and native-list digests and exact retirement timer/service property censuses. It invokes the already installed unchanged admission through bounded SSH, extracts PID/start/invocation only with the banked literal parser, and exclusively publishes an admission data record. It terminates at `ADMITTED_DATA_FROZEN_NOT_ATTESTED`.

`ATTEST`, in a distinct ROOT-selected invocation, repeats the coordinator checks, replays the admission parser, and passes the three late scalars only as positional data to the fixed observer. The observer creates no authority files. After exact schema, host, digest, ordered-leaf and distinct-identity validation, this entry exclusively publishes the observation and the caller-compatible ROOT decision. It terminates at `ROOT_ATTESTED_NOT_INSTALLED_NOT_RELEASED`.

`INSTALL` again checks coordinator state, admission, observation and decision. It invokes unchanged caller `73596b11...` with the original six supplied inputs. It applies the returned add-only patch, performs an exact fourteen-path/hash readback, runs the banked all-five receiver preflight, then streams only registration, card, install manifest, installer, and native list through five bounded exclusive receiver calls. It authenticates the exact remote five-file census and repeats coordinator checks immediately before invoking the unchanged generated installer. It terminates at `INSTALLED_NOT_RELEASED`.

The nine `authority/*.json` candidates and `SUMMARY.json` remain local documentary candidate outputs. They are never transferred or preinstalled. This preserves dispatcher `52b3d916...`: dispatcher creates each actual authority itself with `O_EXCL` after its live revalidation.

`RELEASE` is a fourth source and distinct ROOT-selected invocation. It repeats coordinator, admission, observation and decision authentication, obtains the installed registration digest, and calls the fixed release guard. That guard is the only predecessor of the unchanged FIFO writer, whose FIFO open and token write occur inside the two-second timed child. There is no install-to-release fallthrough.

## Clock, session, and output boundaries

All local administrators, SSH calls, systemd queries, five streams, installer and release guard use the minimum of the three frozen original absolute stops with phase reserves. The remote observer and release guard independently check the bound earliest original epoch and the holder's monotonic `/proc/uptime` age against 120 seconds. Nothing rebases or extends a clock.

Retained sessions use the banked store handle. A matching nonterminal handle is polled; after its stop it may only be collected and cannot start a successor. Terminal, unknown, or handle-free prior state refuses relaunch. There are no retries.

The preholder whole-tmpfs-empty predicate remains admission-only: after holder launch `outer.stdout`/`outer.stderr` legitimately exist. Late consumers instead require the three dispatcher output children `authority`, `frozen`, and `writer` to be empty, plus the exact holder-only outer cgroup and exact nine empty leaves.

## Manual control result and limits

Manual source review found explicit controls for all six banked defects: retained sessions, LF checksum framing, fail-fast five-name exclusive receive, actual receipt/native digest equality plus exact timer/service properties, late values consumed as DATA, and absolute bounds around transport and FIFO blocking. No syntax, AST, import, compile, test, shell-helper, dummy, fixture, payload, SSH, AWS, worker, or science execution was performed.

The deliverables intentionally contain placeholders and `armed:false`; they therefore fail closed now. This is not a runtime readiness claim. Before any prospective holder, ROOT must bind all placeholders, install the fixed scripts, set exact reviewed timer/service property maps and original clocks, compose all four entry files, freeze the ten-item manifest, and obtain a different-model review. A residual placeholder or missing concrete predicate is a STOP. No science/rank/place/source conclusion is made.

## Read and pin scope

All charged inputs in `PINS.json` were hashed before fresh whole inert reads. Final artifact hashes are recorded there. Own sources were whole-read after their final edits. The protected `jc2-lean` tree and public mirror were not entered, enumerated, read, searched, status-checked, built, or controlled.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6719`.
- Body SHA-256:
  `5ce2f64bce97a7507e67f76a7a438023f28c89cffb1ac7fb56637fc9ac903f5a`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
