# Closed child-scope CAPRUN implementation

Status: INTERNAL / UNREVIEWED / STATIC / UNEXECUTED. No registration or run authority.
First action 2026-09-11 07:31:32 UTC; reserve 07:55, hard 08:00, unchanged.
Basis: 0d39df3c9fd69c939a8420c54d03228b9077777d.

## Delivered scope

Four new snapshots, four literal unified diffs, and a disabled setup/registration contract implement the selected closed-child completion guard. Original ops/runtime/scientific files remain untouched. The source JOB and authority/native/source/cap/argv interfaces remain, with a distinct guarded runtime schema and explicit per-phase closed-scope fields. New code is INTERNAL/UNREVIEWED and has not been imported, syntax-checked, compiled, tested or run. No worker, scientific input, fixture, registration or new supervisor is created.

The source census is three initially authorized source files plus ROOT's explicit fourth-source correction: mutate.py embeds its root_policy; it does not import probe. The fourth owned copy changes only metadata identity/membership/schema/mode checks. Its entire coordinate-mutation main and saved-slot restoration remain unchanged. The completed diagnosis, its transaction and custody add three documentary inputs. All seven current hashes were checked before their fresh WHOLE text reads; clipped displays were recovered. No source-producer/checker/arithmetic body or archived runtime payload was read in this task. PINS.json records exact paths/hashes and read scope.

## Guard and binding

ROOT must create an initially empty, immutable-identity leaf per allowed label under the existing owned outer cgroup. ROOT/CAPRUN/dispatcher remain outer; the existing unprivileged setpriv child is placed into its leaf by the trusted preexec stage before workload exec/fork. A bounded private pipe attests the placed leader's PID/PGID/start/boot/PID namespace/cgroup identity. All cgroup and attestation descriptors close before workload exec. Parent-side attestation precedes any normal completion. The new snapshot requires all eight closed-scope arguments; absent arguments or scope cannot silently select the old behavior.

Kernel populated=0, not sampled ps absence, gates normal completion and each cleanup/bootstrap completion/reap path. Empty ps with populated1 continues the same bounded monitor. The unreaped leader anchors exact PGID/start validation for TERM/KILL. Bootstrap direct-PID signaling avoids a Popen convenience method that might poll/reap. Missing placement, malformed events, identity drift or still-populated scope yields failure. The old conservative sampled-live-list cleanup predicate remains additional: it can reject stale positive scan data, never certify an empty leaf by itself.

The dispatcher validates exact leaf inventory and all leaf populations before any terminal result or durable-copy read, not just outer cgroup.procs. Actual runner remains outer, captured child belongs to its registered leaf, and terminal closed_scope attestation must bind the original PID/PGID/start and current leaf identity. ROOT supplies the new registration and actual inode/device/boot/native facts. The contract explicitly requires no external entrant, migration, delegation, write ACL or concurrent ROOT writer; malicious ROOT is not modeled.

The legal topology keeps outer and leaves as domains with empty subtree_control; effective memory/CPU/task caps remain on the outer domain. No nonexistent leaf memory.max is required. The [kernel documentation](https://docs.kernel.org/admin-guide/cgroup-v2.html) supplies hierarchical populated and inheritance/topology interfaces at the limited sections listed in CONTRACT/PINS, not a whole-manual or future-host certification. The [Python subprocess documentation](https://docs.python.org/3/library/subprocess.html) supplies the preexec/thread/fd/session interface; installed-version compatibility is untested. No raw web-page digest is invented.

## Modes, preserved mechanisms and limits

FULL_13 retains all original thirteen phases and scientific statuses. PREFLIGHT_ONLY_9 requires exactly the first nine command/leaf labels, empty typed_slot_policy and null phase_policy; it returns CLOSED_SCOPE_PREFLIGHTS_9_CHECKED_NO_SCIENCE immediately after the unchanged strict dummy, without forming the four scientific phase authorities. The seven refusal entrypoints still run only if separately registered; their successful-science paths are not authorized. ROOT policy helpers remain distinct from scientific authority. Retaining FULL_13 is not permission to run it.

The dummy still performs exactly its original fork, handshake, parent exit, TERM-ignore, touched64MiB allocation and pause; its event identity adds only membership. All four event/order/PID/PGID/namespace/RSS/TERM/KILL/reap/no-live checks remain, with kernel quiet and inherited leaf membership added. No sleeps-as-proof, retry, cap raise, native allowlist relaxation or semantic mutation change was introduced. Fixed phase profiles, original absolute clocks, sampled aggregate CPU accounting, output caps, exact flags, source/native pre/post hashes, ROOT clear environment, freeze and durable-copy barriers remain.

Resource limits are not strengthened into unproved guarantees. RSS is still sampled in the PGID, CPU is inherited per process, and outer containment/CPU budgets remain ROOT duties. A PGID escape cannot produce false completion while the child leaf remains populated, but may require outer recovery. Preexec/Popen/kernel I/O can stall; existing outer unit/timer containment must cover that window. Failed preexec has no workload descendants by the trusted closure; missing attestation remains failure even if CPython internally reaps its failed child. Terminal failure can require ROOT recovery, not retrospective PASS.

## Manual review and pending evidence

Own complete source/diff/contract readback checked required scope arguments, attestation fd closure, main/reap and all cleanup paths, final fail-closed barrier, dispatcher hard pin, per-label mode/argv attachment, embedded helper consistency, unchanged dummy and mutator algorithms. This is producer textual inspection, not independent validation or a syntax test. The literal diffs are the review boundary. No source point, rank, ideal, manuscript or scientific conclusion is produced.

OPEN quantity: one focused different-model FIRST on these four sources and their setup contract, followed only after new ROOT authorization by one nine-phase AWS-only discriminator. Cheapest planned observation: unchanged5wall/3soft4hard per control/dummy,32MiB dummy RSS,0.05s sample,1s TERM grace; nine nominal walls45s. A120s observation/collection allowance is an UNMEASURED planning estimate within the old outer budget, not a reset or guarantee. Require real orphan membership/RSS/TERM/KILL/kernel-quiet/terminal custody evidence. No historical race reproduction is claimed unless actually observed. Any cap/mismatch/missing observation => STOP/NONDECISION, no automatic retry.

All scientific source/setup/native/worker/actual-clocks fields remain future ROOT obligations. No runtime readiness, FIRST pass or enabled registration is claimed. Publication completion consists of own WHOLE/read-scope/OPEN/collision checks, unchanged seven postpins, final source hashes, unique marker LAST, expected transaction verification and custody-first idle handoff.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7388`.
- Body SHA-256:
  `0cfbb2bd2978d65f262886d3192806a2d1dd270c859d7e955e8e19e23e007ec4`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
