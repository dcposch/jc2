# D125 hybrid pilot readiness: prepared, with explicit authority gap

2026-09-07. **PREP ONLY / NO LAUNCH.** The prospective one-shot registration, disabled authority templates and minimal engineering wrapper are implemented. Actual engineering remains blocked on root's decision about GAP-AUTH below and acceptance of the pending instrument gate. Nothing was deployed or executed on AWS; no source, production metadata,81-coordinate map,803-row loop, CAS, solver or descendant fixture was run. Old GREENs remain spent.

## Deliverable and unchanged payload

Owned box: `box/d125-hybrid-pilot-readiness-20260907/`. It contains130-line `REGISTRATION.md`,104-line `host_control.py`,87-line tiny tests, two disabled JSON templates, full normal/optimized evidence and custody. The registration pin is `3b04131bc72c3ea352a865144ac152119f8b070e89709249ef91623e0b3d8b9f`; host-control pin is `3ea4ba7cd62600ba351a27640ee44815adc70fbd27e18729c180f2a550f215ba`.

The sole prospective production client is the normalized unequal/Q odd81-coordinate whole-beta15 slice, all803 original positions plus zk−1, one construction and one original-source Laurent-cover replay. Mathematical hybrid gate `bff1b2073c3fb878458de346fa1b3c87ab378ce5b7a5320e3a634b7b7da0950c` is accepted by root; normalization14c remains accepted. The Sol instrument review was live and no body, code, log or receipt from it was read. Constructor `aa65e710…`, replay `06475a2a…`, baseline `ec2fa2d1…` and CAPRUN `4435279d…` remain unchanged. A later changed pin invalidates this registration; no automatic version substitution is licensed.

## GAP-AUTH is not closed by the wrapper

The frozen authorizer requires exactly root/authority.json, schema `jc2.hybrid-affine-authority/v1`, root_green=true and construction_only=true. It cannot accept a separate engineering schema/path. Its main entry ignores an extra engineering_control_only field. Consequently an engineering packet satisfying that gate is **not intrinsically incapable of source access or construction**. A different C.main dispatch could consume it. The tiny suite explicitly confirms this limitation at the real pure authorizer checks; it is not reported as a security PASS.

Root must choose whether to accept the exact guarded engineering dispatch under cooperative ownership or obtain a separately reviewed narrow code delta before actual engineering. No pending/frozen code was patched, and this task issues no authority or GREEN. Both JSON templates have root_green=false, gates_accepted=false, expiry0 and unresolved fresh boot/GREEN/code-gate fields. Their template filenames also differ from the required root authority path. They cannot accidentally authorize the frozen production entry as written.

For the exact proposed wrapper dispatch only, the wrapper validates engineering scope, current host/boot, matching registered control name, code/registration/physical-GREEN/gate pins and10s caps. It installs process-local production/metadata tripwires and an open-audit fence for the source and production outputs, then calls the REAL frozen authorize. The returned arithmetic context's production flag is disabled. Only `(1+x)(1−x)` and a four-slot degree3 normalized Hermite/lift fixture execute. The tripwires do not modify any file and do not claim to protect a separate process or authenticate against the same UID.

## One-shot custody and output eligibility

Only the existing root-owned i-0da0cebfc97c9fd54 and retained EBS vol-0eb6450d18ffa89f1 are in prospective scope; root reported it STOPPED, and this task did not query it. The new path is `/home/ubuntu/d125-hybrid-affine-pilot-20260907`. Root must freshly verify AWS Owner/EBS/state, Linux Amazon EC2 DMI, boot, hostname, EBS mount, resources and no inherited arithmetic/unexpected owner before any separately authorized deployment. No START/STOP/termination/deletion authority follows here.

The readonly `paths` control checks **both construction.jsonl and replay-result.json**, plus pilot stdout/stderr/telemetry, with lexists so a dangling symlink also blocks launch. Recheck immediately before the single dispatch after an exclusive controller launch claim. This is not an atomic reservation of both payload outputs: frozen main opens replay-result only after constructing, so a competing/stale file could waste arithmetic, although xb prevents overwrite. The registration requires sole cooperative directory ownership and explicit absence checks, not a false race-free claim.

Future production limits remain one shared300 wall/CPU seconds,4GiB AS and sampled group RSS,128MiB FSIZE per regular file,100k polynomial terms,1m multiplication pairs/retained terms and4096 coefficient bits. All output paths are exclusive; no retry, cap enlargement, solver, subset or alternative normalization. The registration gives exact CAPRUN argv. The direct unchanged constructor payload uses `python3 -E -s -B`, not `-I`, because it imports its adjacent baseline module before any path bootstrap. Runner and engineering wrapper use `-I -B`; the wrapper inserts the pinned workdir before importing construct. No new production bootstrap or code edit is introduced.

Stage stderr intentionally contains metadata. Success requires terminal NORMAL_EXIT/child0, complete804-position footer, ALL_803_ORIGINAL_ROWS_COVER_TRANSPORTED receipt, source/code/output hashes and fresh exact-group absence/reaping; an empty-stderr rule would be wrong. Partial/capped output is INCONCLUSIVE. No stream result is an ideal decision or a point.

## Narrow engineering and checks

After root resolves GAP-AUTH, the proposed authorize control has10 wall/CPU seconds and512MiB AS/RSS. One optional same-pattern orphan/RSS/TERM→KILL fixture is registered only for the named restarted-boot/new-workdir group-custody gap:512MiB AS,64MiB sampled RSS,96MiB descendant allocation, expected resource-cap cleanup. It is not a new supervisor campaign; no parser, print or FSIZE-overflow retests are proposed. Each requires a fresh matching root packet; spent packets must be preserved and must not be reused or silently overwritten. Both prospective controls stay within a30-second engineering arithmetic batch. No descendant control ran locally.

Ten local methods passed normally and−O, final paired command0.410s, each interpreter capped at12 CPU seconds/512MiB and externally30s; `-B` precedes imports. They exercise the real frozen authorize with explicitly mocked host/file facts and OS-limit setters, actual tiny normalized arithmetic, wrong boot/registration/GREEN/gate/caps/pins, missing authority, disabled templates, production/source tripwires, source-open audit callback, and actual regular-file/dangling-link changes at both output names. Production metadata functions are trapped and were not called. Tests also rehash every frozen payload file and verify zero Assert nodes. No assertion of actual-host validation is made.

Read perimeter was the named terminal parity readiness/engineering pattern, accepted CAPRUN interface, frozen current payload, and the explicitly accepted mathematical gate's hash only. No live peer or protected project was accessed. All new evidence is pinned in custody. **STOP: prepared with GAP-AUTH; no current execution authority, all writers/jobs idle at handoff.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7310`.
- Body SHA-256:
  `3c7977faf2c1c7badfe81a1370f9b4ff367c94ca3c679d2ee0758651b538f283`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
