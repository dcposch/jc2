# Task: construct evidence-grade AWS deciders for both open `R4-00` cells

You are Fable 5, an equal primary researcher in the plane Jacobian-conjecture
campaign. Work independently and exactly. The deliverable is
`xmodel/k00-r4-00-aws-packet-builder-fable5-20260829.md`.

Read `COORDINATION.md`, `ops/FLEET.md`, and the pinned source packet below.
Do not inspect, list, search, stat, build, modify, or control `jc2-lean` in any
way. Use scoped git commands excluding it. Heavy or uncertain algebra/CAS is
AWS-only; the local Mac may be used only for editing, hashing, orchestration,
and seconds-scale low-memory checks. Use `apply_patch` for repository edits.

Pinned inputs:

```text
0ffc4f741eb2b6a8257dd90c47f194f950752150f57468d81a7779406ab60ed3
  xmodel/k00-r4-00-entry-solve-fable5-20260829.md
d72f774c53c7dcb259eefc10ff2a02202e1049b61c17486b1ba22e3773813848
  cases/max12_812_order2_u2_62_strict_rees_20260825/aws_compile_v2_jsat/run/output/compiled_v2/tails.json
2ac7653cf0c7a39970d54286b85974b39a2e6deb116839720cb1ae91f8ad6d2b
  cases/max12_812_order2_u2_62_k00_common_lambda19_kuranishi_v20_20260827/compile_contracted_source_v20r2.py
```

The first SHA line above intentionally pins the sealed full report. Verify all
three before consuming them. The target is the two typed-open cells from that
report:

- `R4-00-R1-BASE-DECIDE` over `Q(i)`, saturated by `q*kappa` and excluding
  `(s,t)=(0,0)`;
- `R4-00-R2-BASE-DECIDE` over `Q`, saturated by `D*kappa` and excluding
  `(s,t)=(0,0)`.

Rebuild every literal row/generator from the frozen tails and compiler. Do not
use the producer's numerical indications or prose-only omitted term counts as
an oracle. Produce deterministic, checked-in generator scripts and immutable
CAS input packets sufficient for a clean machine to reproduce both ideals.
Add route-specific fail-closed AWS runners that record host, EC2 DMI identity,
UTC, exact input hashes, tool versions, seeds/primes, argv, resource use,
return code, and output hashes. Where helpful, create several independent
modular screening shards, but keep their epistemic status explicit.

Audit a campaign-owned host directly immediately before launch and do not
touch the separate user formalization instance. Box02, Box03, and the r6 nodes
were idle at 2026-08-29T21:54Z except box01 had one unrelated one-core campaign
tail-builder; direct state is authoritative. You may launch the two jobs (and
independent primes/engines if capacity helps) once the packets self-check.

Important correctness requirements:

1. Preserve field-valued/scheme, jet/arc/map, floor/attainment, and
   producer/reviewer distinctions.
2. `msolve` characteristic-zero `[1]` can be only a first-prime shortcut.
   Modular `[1]` results are screening evidence. Theorem-tier emptiness needs
   an exact rational certificate `1=sum h_i f_i` or an equivalent independently
   checked identity. A non-unit exact-Q component still requires exact checking
   of the constructible opens and the grade-19 `Jdet_0` survival condition.
3. Test generator independence against direct literal-row substitution and
   mutations; verify the two branch signs for R1 and clear denominators without
   changing constructible loci.
4. If the alleged five-condition reductions are incomplete or any source
   formula is wrong, do not launch a misleading decider. Document the precise
   defect and the smallest clean repair.
5. Do not edit canonical ledgers. End the report with standalone
   `<!-- BODY-END -->`; do not add a seal block.

Report exact artifact paths and hashes, remote host/dir/PIDs, current resource
snapshot, and the precise result/certificate needed to close each cell. If a
fully faithful packet cannot yet be built, produce the strongest exact
reconstruction and a sharply scoped blocker rather than substituting a weaker
problem.
