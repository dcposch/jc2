# 1110 adversarial cross: fixed units, not a generic minors ideal

2026-09-09 Astra/Volta. Actual local start 11:38:37 UTC; original controlling
target 11:49:28 UTC, unchanged from collection plus fifteen minutes.
Basis 0d39df3c9fd69c939a8420c54d03228b9077777d is provenance only.

## Scope and merged verdict

All 27 charged objects were pinned before the new bodies were read.
I reuse the 19 byte-identical whole reads from my blind and my own blind
body/transaction, and read all newly released objects WHOLE. No live or
uncharged science was accessed. The sole accepted delta is16q: the compact
CE contract, full boundary module and39/53 parametrization. The smaller
blind graphs remain UNREVIEWED; this cross cannot promote my own result.

Two survivors, not three independent votes:

| Merged object | Proposals deduplicated | Disposition |
|---|---|---|
| Complete full-B reconstruction, all r | Root Card2, Astra Card1, Fable Card2 and the linear part of Card1 | Highest-information next mathematical test; provisional fixed-unit graph |
| Complete r=1 source ideal decision | Root Card1, Astra Card2, Fable Card1 | One bounded follow-up opportunity; no generic-minors shortcut |

Root Card3's marked-boundary investigation presently has no condition
independent of the complete rows. Bank NO_NEW_MECHANISM for that separate
card and stop it. Its negative field/ring checks remain valuable safeguards.
The algebraic mechanism itself is KNOWN in the frozen top-row advice and
earlier mate work; its new complete source attachment and explicit residual
reduction are the earned delta. No exhaustive novelty claim follows.

## Exact corrections to Fable's proposed mate ideal

Linearity in B for fixed A is correct. The next statement, that solvability
is therefore one ideal of minors in the A variables, is false as stated.
The elementary family a*x=1 has solution locus a!=0, not a closed subset
of the parameter line. In general one must take a union of rank strata,
each with its pivot-open constraints; the augmented rank equality is not
represented by simply setting one unspecified collection of minors to zero.
Elimination followed by closure can add parameter values with no mate.

Here the monic coefficient of B_0 must be an inhomogeneous equation in the
linear problem. The variable kappa_B and its nonzero constraint cannot
disappear from a claimed <=12-variable closed ideal without a justified
projection/stratification or an exact reconstruction. A random mod-p rank
probe supplies neither. Thus Fable's exact-reduction and two-orders-of-
magnitude cost claims are unsupported; the matrix itself is useful, not
its asserted global closed projection or predicted speed.

There is also a real, expected homogeneous kernel. B -> B+beta*A+gamma
preserves every inverse condition, [A,B], B_5 and the monic S^n coefficient
of B_0, because deg A_0=m<n. It preserves all coefficient bounds: subtracting
A never exceeds B's larger bounds. These two directions exist on the full
contract, not merely at generic points. They are not by themselves a rank
surprise, hidden pathology or builder error.

For fixed A and fixed B_5, the top-down argument actually bounds the
homogeneous kernel exactly. The difference of two mates has t-degree<=4.
Its t^6 row kills the t^4 coefficient. The next row leaves beta*S at t^3;
subtract (beta/kappa_A)*A. Rows t^4,t^3 then kill coefficients t^2,t^1,
and row t^2 leaves only a constant. The unused lower rows do not create
new freedom. This uses kappa_A invertible and polynomial coefficients,
not an assumed generic matrix rank. With kappa_B/c allowed to vary, that
fixed-leading-coefficient qualification must not be omitted.

Fable's explicit t^6 coefficient formula and its boundary consequences
B_4(0)=0 and -4kappa_B*A_2(0)+kappa_A*B_4'(0)=2cu agree with the full
equation. These are useful named-row facts, not an ideal decision or an
exclusion. The full inverse conditions remain mandatory.

## Survivor 1 — hostile audit of the fixed-unit full graph

**Object:** accepted16q's complete Q-contract, m=3r+1,n=5r+2,r>=1,
p=t-ut^2+St^3, x=St, y=St^2-ut, target c*Delta. Write

    A=a(S)+kappa*p+b(S)x+d(S)y,
    deg a<=m, deg b<=2r, deg d<=r,
    A_3=kappa*S, B_5=lambda*S^2, c=kappa*lambda.

The product c is already a unit, so both factors are units over any
Q-algebra satisfying the guard. This is a decisive distinction from
Fable's parameter-dependent rank pivots. Neither u nor S nor a determinant
in the free A coefficients is inverted. Root's temporary A_3=S,B_5=S^2
normalization is usable only if its changed A_0/B_0 leading scalars are
retained; it cannot also preserve both original monic coefficients by fiat.
Keeping kappa,lambda avoids that bookkeeping issue entirely.

**Manual audit of my blind's graph, not independent acceptance.** For
k=4,3,2,1,0 the t^(k+2) equation has next unknown
kappa*(k-3S*d/dS)B_k. All other B indices are higher. Its degree envelope
is n-rk, because m+n-r(k+3)-1=n-rk. The only zero diagonal entries on
allowed S powers are (k,i)=(3,1) and (0,0). Their two compatibility scalars
must survive. All other divisions are by rational nonzero constants and
kappa. There is therefore no unexamined rank-drop stratum in this proposed
elimination, including at u=ell=0 or over nonreduced coefficient quotients.

The literal first block independently agrees with Fable's row:

    B_4=S^2 e-2u*lambda*S,
    kappa*(2e+3S e')=lambda*(d+5S d').

Its coefficient multipliers 2+3i never vanish. The explicit target term
2cuS is needed for the cancellation; changing it is not an innocuous sign
convention. Conversely (3-3S*d/dS)B=S has no polynomial solution: deleting
the first resonance is an actual changed-equation failure, not just a
different variable count.

The gauges A_0(0)=B_0(0)=0 and [S]B_3=0 are obtained by constant target
translations and the whole B shear. They preserve the source endpoint,
unit guard, module membership, all bounds and both monic highest powers.
They do not set kappa=lambda=1. After them my proposed free list has
3r coefficients of a, 2r+1 of b, r+1 of d, and kappa,lambda,u,ell,w with
w*kappa*lambda=1: 6r+7 coordinates, not a solution-space dimension.

The **complete residual perimeter** is the two resonances, every t^0/t^1
Jacobian coefficient, B_0's monic S^n coefficient, all seven B inverse-pole
scalar rows, and the guard. The two low Jacobian envelopes have 7r+3 and
6r+3 slots. Thus 13r+17 is an overinclusive residual-slot bound. The A
inverse conditions hold identically by its module form, while top t^7 and
the five descending levels were used, not discarded. At r=1 this is the
proposed13/30 graph. The smaller count is not proved merely by16q, and
substituted parameter degree/fill is still unmeasured.

**Next test and stop:** one <=20-minute different-model manual review of
this exact graph, its two directions, gauges and residual list. PASS licenses
consideration of an exact reduced presentation, not a solver run or a
uniform obstruction. A nonunit pivot or omitted condition rejects the
graph and returns to the accepted39/53 or raw49/63 specification. No old
source-power reconstruction or a new generic-rank farm is needed.

## Survivor 2 — one complete source decision, not a third proof vote

**Object and arrow:** I_1 over Q, or a reviewed equivalent guarded
presentation. Accepted16q now supplies the sufficient endpoint: a complete
characteristic-zero point gives ordinary P,Q of degrees112/196 with
constant nonzero Jacobian and hence nonautomorphy by the named classical
degree theorem. Exact properness of the complete ideal gives such a point;
no rational-point restriction is needed. A verified unit, together with
the accepted actual-source/D28 imports, closes the exceptional column
entry and therefore that D28 column, not all numerical degrees or JC2.

Prioritize Survivor1 because one short uniform derivation can either expose
an actual obstruction or remove most B coordinates before any expansion.
Do not let it become an indefinite dependency: the accepted complete
baseline remains available if the new graph fails. Then, under separate
implementation authority, freeze ONE r=1 full system and its exact map;
measure coefficient degrees/terms and validate all rows before choosing
coordinates or a fixed decision budget. A proposed initial ten-minute
exact-Q attempt is a stop cap, not a performance promise. Root's proposal
to choose resources from measured construction data is preferable to
Fable's 1–2 fleet-hour estimate and generic rank probe as a default package.

Outcomes: exact unit excludes the named source; exact properness or a
verified nonzero finite-Q-algebra point reaches the CE endpoint; timeout,
incomplete elimination, mod-p rank/point or memory growth is NONDECISION;
builder/map disagreement invalidates the artifact. All positive-looking
results still require independent full-row verification. No larger-r
sweep, automatic second prime, cap increase or source expansion follows.
Uniform incompatibility of the residual equations outranks serial r tests
as a proof result, but no such incompatibility has yet been derived.

## Portfolio and separate systems adjudication

The allocation differences in avenues5/31/34 are mostly naming of the
same tool chain, not independent scientific disagreements. Jung–van der
Kulk is a recognizer here, not a reopened descent program. A finite free
rank-three ring over K[S,p] is not a degree-three Keller map over K[A,B].
Nor is it a Poisson subalgebra for the displayed bracket. Root's lower-ranked
geometry card has no new nonautomatic invariant yet; stop rather than
relaunch unsigned norms or a cubic classification.

Keep the genuine global deficits visible: F10 is one ordinary actual
stratum, not all source profiles; D28 is one column, not all degrees;
G2-PSC is not G2-BD; the K16 all-m product and b=0 cases remain; proper
Galois/d2=2 BD-GAL closure is not d1=2 or all non-Galois blocks. The broad
and social clocks are unchanged. The current full-source opportunity
does not prove those other avenues empty or useless.

**Systems: NO_CHANGE.** The common timestamps put root finalization at
11:17:40 and Fable receipt start at11:19:09: 89 seconds. They do not assign
the entire six-minute planned-release miss to post-seal preflight, so they
cannot justify a promised >=5-minute reduction by a new release utility.
Fable's causal claim is unsupported, not a demonstrated adapter defect.
The smallest future evidence test is to separate root authoring, sealing,
preflight and adapter-start intervals in the existing records. A future
utility would need actual wrong-vector, changed-pin and occupied-target
refusal tests as well as equal valid release, not just a matching successful
dry run. No new wrapper, probe or duplicate ledger is earned in this cross.

CONTINUE the source-attached uniform mate problem and independent global
landing interfaces; REDESIGN the r=1 decision through an exact complete
presentation; STOP the generic closed-minors claim, kernel-as-error claim,
unmeasured speed ratios, finite-jet substitutes and retired clients. These
are two deduplicated survivors with explicit alternatives and stop points,
not promotion, an emitted system, an exclusion or a counterexample.

## OPEN(S) RAISED

None.

## COLLISIONS

status: EMPTY

- NONE — own-only raised-OPEN check; no corpus scan or novelty claim.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `11398`.
- Body SHA-256:
  `0e775ac078c31115a27eec60fbd7d5e0c8e3b2571f2abfe3fbbac570255f5ae2`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
