# One bounded raw-native record assembler

Verdict: CONCRETE SOURCE IMPLEMENTED, STATIC/UNEXECUTED, FIRST PENDING.
This closes the source-writing part of the identified raw-to-manifest
assembly gap, not native qualification, runtime readiness or a scientific gap.
The previous goal turn made PROGRESS by collecting the launcher and starting
its independent FIRST; this turn implements the separate bounded metadata
conversion while that exact review remains live. No reviewer consumes these
new bytes before ROOT completion.

## Artifact and scope

box/r3-native-record-assembler-root-20260912/native_records.py,
SHA44bd5d3b18d5552d811dd42071b8d54a6096e42f5ceac939e04cd1bd1ae70e0e,
247lines12900bytes. CONTRACT.md in the same directory,
SHAb5876b7297d98c90c719c9d4901caa0e7e67503e375c3f7aca7d1e2d11206416,
112lines6855bytes. Whole current source and contract read17:36:47, after
the three prepublication changes described below. No program execution,
syntax/AST/import/compile check, fixture, CAS or scientific helper was run.

The single exact CLI takes bounded raw bytes plus their externally supplied
SHA, canonical expected-facts JSON plus its SHA, and one fresh output path.
It reads no file named in a raw native record: such paths are parsed purely
lexically. It performs no native discovery, subprocess, scientific import,
registration generation, approval, AWS or service operation.

The output preserves the exact six-field f10-native-closure/v1 schema, and
the separate job_tag/files subset uses CURRENT necessary-rows job identity.
It also retains raw stat fields and emits every canonical file checksum plus
an assembly receipt. Full/subset size ceilings are the bootstrap's unchanged
1MiB/64KiB. Whole output is additionally bounded4MiB outside working/native/
science/wrapper inventories. This does not change the128MiB working mount,
8MiB runtime metadata limit, source code, preflight policy or any source cap.

The collector SHA, expected host/boot/namespace and package facts are external
ROOT assertions. A matching synthetic record can pass this grammar while
being no installed native closure. Status is therefore explicitly
ASSEMBLED_METADATA_ONLY_UNQUALIFIED/science_outcome NONE. No model verdict,
hash, JSON property or converter receipt supplies authority or freshness.

## Checks and failure semantics

Exact raw grammar covers fixed physical/package/Python-path header; unique
timestamps and final counts; FILE/SHA adjacency; canonical disjoint path
roles; both directions of direct-child listing; alias targets and required
roots; modes/counts/serialized sizes. All facts leaves are strings; numeric
facts literals, floats and nonfinite values are refused. Native metadata
counts are not mathematical integers or scientific coefficient exports.

All output bytes and limits are checked before mkdir. Fresh0700 output then
five exclusive0444 files, per-file fsync/whole-byte readback, output/parent
directory fsync, exact final inventory and unchanged inputs. There is no
overwrite, retry or cleanup. A failure retains partial outputs; a late failure
can leave a complete-looking receipt, so terminal exit0 and independently
matched source/input/receipt/output postpins are mandatory before consumption.
This is not an adversarial-writer or crash-proof global transaction claim.
Filesystem I/O and memory/runtime remain externally bounded and unmeasured.

The source-visible positive control is a deliberately synthetic4file/2dir/
2alias record with the required paths, absent zip and matching expected
header. It establishes only that the structural predicates admit a literal
witness; no fixture or runtime success is claimed. Negative source traces:
delete the lib-dynload ENTRY but retain its DIRECTORY/counts and recompute the
raw pin; break FILE/SHA adjacency; point an alias to absent; corrupt counts;
retain a wrong input pin; or use a preexisting output directory. Each reaches
a named refusal before output creation. No negative was executed.

During manual readback, ROOT added reverse present-child/list membership and
explicit double-leading-slash rejection, then fsync of the output parent.
These were edits to the owned unpublished draft only; no previously frozen
source, collector, bootstrap, launcher or historical artifact was changed.

## Input provenance, review and next boundary

COORDINATION33cfa6106972fe557a95ca81a9a05bb72300a82b2fbb7908915441cf75377597
is current; the same806lines were fully read in the preceding uninterrupted
coordinator context and the current hash was revalidated before action.
Fresh SHA-before-WHOLE reads17:30:25:

- NATIVE-METHOD.md, db511496cbfa43d06f2d5234cd02cf92de0950545b2a67ea0af27477fd5a7561;
- native-metadata.template.sh, b386a6cd2d732e6618b994fdbdec67d8e2a59fc997206f296b4cf55513684ed0;
- NATIVE-METADATA-RECIPE.md, 584f044f60c64c3e1421271cd2d63ad4fa48d09dbfb50999a5e430356dd1a2f5.

Paths for these three are literal in the preparation note. The recipe was
read WHOLE, but only its grammar/six-field transformation is selected: no
old binary-as-fresh-fact, direct-rows tag,13-phase policy or launch authority.
Current bootstrapf04f1b7b16bed194e5d7b503a78fe9bd36e3bd58da3696adf6a40bfdbdcd17d2
was rehashed; the earlier source interface read is supplemented by the exact
276–333 reread before close. This is not a renewed full-runner review.

One different-model actual-code FIRST of the completed assembler is owed
before use. No worker or parser execution is selected by this publication.
A separately bounded metadata qualification and concrete package binding
remain, alongside launcher/native/host/accounting/retirement obligations.
The actual r3 necessary rows and297rank remain UNRUN, all F10r>=3 remains
open, and even all-F10 closure would not resolve JC2. Global ranking, full
ideation/web clocks and scientific-machinery48h debt are unchanged.

Publication uses the unchanged administrative finalizer, marker last after
WHOLE readback, expected-manifest verification, source/contract0444 freeze
and all final pins. No staged files, commit, deletion, scientific computation,
worker allocation or protected-tree/mirror inspection.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6151`.
- Body SHA-256:
  `64d0e6b6201eaf6e906b18396275709f2739982474b448e759c91b2ca15e31a8`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
