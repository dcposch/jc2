You are an independent hostile algebraic-geometry referee.  Review only the
frozen tied D1 `(a,c,r)=(3,3,2)` C2 maximal-pole producer below.  Do not edit
any producer, prompt, ledger, audit, promotion, or existing artifact.  Write
exactly one report:

`xmodel/max12-812-order2-square-d1-tied-a3c3r2-c2-maxpole2-hostile-review-grok-20260826.md`

End with exactly `CONFIRMED` or `REJECTED`.

Producer directory:

`cases/max12_812_order2_square_owner_d1_tied_a3c3r2_c2_maxpole2_20260826/`

Primary custody:

```text
RESULT.md SHA256
b0ae59bfe59705c95114eebebcf639a0a27832710ddbe89a1bf1646e69e40c9b

PRODUCER_FREEZE.sha256 SHA256
41fdc7c3e3abd7b16159467484f2f4610337709d13dc7a0dbdbe4277a3407cc5

EVIDENCE.sha256 SHA256
2bff9bc7358a5fd05a9a6dfa3efd11629ba24381359eb4c94dec134ae419299e

source FREEZE.sha256 SHA256
30673eead795eb94dfc1f1b46fe39327bfe58ed3d5acac773d717ec5421488e8

source archive SHA256 on all three hosts
a5380e75cdc897e61d1ce88f508d7f343a90ca43b4e917b9304c2744a9b325c1
```

Claimed scope is exactly

```text
D(p*k0), ord(A)=3, ord(C)=3, ord(R)=2,
```

through decisive absolute grade 16.  Audit adversarially:

1. Rehash every source/producer/evidence row and all three nested compiled
   manifests.  Check host/tag/rc/validator/zero-swap custody.  Exact `Q` is
   the theorem endpoint; `F_65519,F_65521` are controls.  Singular 4.3.2
   quotient-ring assignments are not canonically normalized for raw
   `==0`, `subst`, or `diff`; therefore confirm literally that every
   generated program uses an ordinary polynomial ring and that no conclusion
   depends on unsafe `qring` semantics.
2. Independently enumerate all four frozen binomial summands through grade
   16 at `(A,C,R)=(3,3,2)`.  Decide whether exactly `AC,C2,R3,RC` occur,
   with the displayed coefficients, whether all first occur in grade 16,
   whether `C2` is uniquely pole two, and whether padded bounds are stable.
3. Inspect all seven literal rows, recursive extraction, and the independent
   analytic source.  Verify the coefficientwise grade-16 bridge and exact
   absence of higher normal/connection jets, lower loads, and targets.  Look
   for any omitted source column at the ceiling.
4. Reconstruct the canonical numerator modulo `L^2` from all seven row
   coefficients.  Verify the fully cleared numerator, exact ordinary
   quotient, pole-two recurrence, and both root Faber identities with every
   sign.  Reject any false Laurent-versus-ordinary equality.
5. On `p=-2*lambda^2`, verify that only `(3/8)C(+lambda)^2` and
   `(3/8)C(-lambda)^2` survive.  Audit the determinant and the two
   coefficient charts `D(c1),D(c0)`: do they really cover exact nonzero
   linear `C`, and do both localized ideals contain 1 before radicals?
   Check for nilpotent/embedded solutions, illicit saturation, or a hidden
   inversion.  Verify the omit-`C2` negative control.
6. Keep every adjacent face and every generic maximal-pole extrapolation out
   of the verdict.  Enforce the positive-load, `p=0`, `k0=0`, zero-section,
   global/terminal, fan-exhaustiveness, order-two, maximum-twelve, and JC2
   firewalls.

State the earliest missing primitive or smallest failing identity if any.
Producer PASS strings are not evidence by themselves.  Do not import the
support miner or hand triage as proof.
