# Final literal mixed-dummy operator commands

Status: **COMPLETE INERT COMMAND / UNBOUND / UNEXECUTED / NO AUTHORITY**.
First action `2026-09-12T22:25:41.616855275Z`.

`COMMANDS.md` replaces the prior prose-only obligations with one finite command
sequence. It prebinds the exact unit and `/system.slice/UNIT.service` cgroup
path in the future registration, arms absolute TERM-at20s and KILL-at25s timers
before launch, uses one blocking `systemd-run --wait`, captures actual numeric
exit125, and cancels timers only after terminal cgroup empty/absent proof.
Terminal blank/default ControlGroup output is not used as authority.

The launch retains root CAPRUN with KILL/SETUID/SETGID only, then exact setpriv
UID/GID drop; CPU80%/100ms,32GiB memory/AS, swap0,256MiB FSIZE,20+5-second
service cleanup and separate256MiB tmpfs. System HOME is untouched; only
task-specific temp/cache variables point into output. All host/tool/native/
deadline values remain visibly unbound.

Literal bounded checks cover runner and child streams, LF/two-line shape,
canonical jq reserialization, exact v2/nested values, boot/ns/cgroup/UID/GID/
rlimit/control data, telemetry argv/caps/identity/RSS/TERM/KILL/reap fields,
numeric service exit, final unit cgroup emptiness/removal, timers inactive,
file bounds, census and hashes. Jq itself is an explicit future pinned/native-
qualified input, not assumed installed. Any failure is STOP without retry.

This procedure does not prove no escape or true peak from self-report/sampling,
nor does it qualify a host, package, native closure, timer or worker. ROOT must
still authenticate physical facts, absolute timing, immutable ancestry,
durable transfer and retirement. No science import is present.

Fresh WHOLE reads covered all six pinned inputs. Final WHOLE readback covers
COMMANDS, PINS, report and manifest; strict postpins precede custody. No
interpreter/import/syntax/AST/dummy/CAS/AWS/network/process action occurred.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `1977`.
- Body SHA-256:
  `5fe750f6b2ff806cf3704872a746464f3597dc0be7bfee501b6d4b19a0798aec`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
