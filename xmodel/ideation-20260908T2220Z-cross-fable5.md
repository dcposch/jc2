# 2220Z adversarial cross — Fable lane: source attachments compared, not votes

2026-09-08. Cross stage, NOT blind. Inputs: whole `delta.md`, whole `audit-15m-only.md`, all four collected blind bodies (collected file hashes: coordinator 32c5ce2b…, astra 68238342…, fable5 0b041a3b…, sol56 2958b219…), packet, collection.json and the frozen/poststate snapshots listed in the invitation. Original blind cutoff 22:26:47.147953 and the poststate (15m accepted 23:00) are kept separate below. Sol's receipt is 348.85 s late; its mathematics is weighed equally. Timing: invitation opened 23:11:25, target 23:19:34; body written 23:17:09, completion marker 23:17:38, one hash-label correction 23:17:52, all before the target; no reset. JC2 remains unresolved. No charge_basis: no exit price is declared.

## 1. Fingerprint deduplication (independent origins preserved)

| FP | Mechanism | Origins (tier at origin) | Merged status |
|---|---|---|---|
| A | Non-H resonant closure: local order-36 scalar c=5λ³t(t−1)²/9 vs global face scalar c=−5λ³t/9, gap = unit 3ρ | Root consumer 1770c414… (post-cutoff, sealed); Sol `GOLD-36-SPLICE` (blind, complete); Fable §3 identity (blind, local half only) | SAME mechanism; ACCEPTED as 15m. Cross agreement adds nothing to the promotion. |
| B | Lower-B initial elimination by highest coefficient/Euler weight, before factorization | Root Card 1 (blind); Astra Card 1 (blind, independent); Fable `OPEN[GOLDEN-H-DIVISIBLE-LOWER-G]` (blind, G-block side of the same obligation) | MERGED, lemma verified below; source attachment OPEN |
| C | Hermite value/derivative typing of C in F_j=HC | Sol Card 2 (blind) | Kept as a typing of C only; see §2 |
| D | common4 full-Jacobian weight implication / one bad coefficient h | Root Card 2 (blind); Astra Card 2 (blind, localization form) | MERGED; no literal h supplied by anyone |
| E | One literal 15j attachment | Astra Card 3; Root item (iii); Sol "redesign toward attachment" | MERGED; second orthogonal candidate |
| F | Fable 18-parameter nonodd resonant ansatz | Fable Card 1 + `OPEN[GOLDEN-NONODD-RESONANT-ANSATZ]` | RETIRED by 15m (below) |

**Retirement of F.** The ansatz fixes F₁₂=λΔ with Δ=pLM of degree 3, so H∤F₁₂: every point of that scheme lies in the non-H branch that 15m excludes without parity. It names no unexcluded object. Both Fable OPEN identifiers are absorbed: F into 15m, the lower-G one into FP-B.

**Fable's "any consumer must use second-order rows" is refuted as a necessity claim, while its computation stands.** I reran the displayed order-36 identity as a free-symbol dict polynomial (30 s cap, stdlib, `box/…-cross-fable5/check_order36.py`): all six coefficients match Fable's display; 15m's `(6e−10k²/3)Y⁵+ek p^{1/2}` is the same after the 10h/p normalization root notes. So the local system is consistent (u≠0, v=b=0, e=5u²/9), exactly as Sol's falsification control says. What closes it is not a second s-order row but a DISTINCT SOURCE INPUT: the accepted receiver face relation `c=−(5/9)a³/κ` (minimal-receiver.md:146) with κ=t²=ρ and a=[g²p]A=λt, the latter because every lower R component has weight ≤1 while g²p has weight 3. The unit (t−1)²+1=3ρ (rechecked in Q(ρ) arithmetic) is the contradiction. Fable's hazard was correct about the local expansion alone and wrong to conclude that only deeper s-orders remain; the global weighted face is s-independent. No new accepted proof gate is needed.

## 2. FP-B: the elementary lemma, checked; the attachment, not

**Lemma (both forms correct).** Root: P monic of Z-degree m over a differential field in p, Q≠0 of Z-degree n and E-homogeneous of weight L for E=p∂_p+hZ∂_Z. The Z^{m+n−1} coefficient of [P,Q] is m·(q_n)' since P_p has Z-degree ≤m−1; commutation gives (q_n)'=0, and homogeneity gives p(q_n)'=(L−nh)q_n, so q_n=0 once L≠nh. Root's version does not need P homogeneous. Astra's form p[P,Q]=βP_YQ−mhPQ_Y (I re-derived it) gives the same top coefficient m·lead(Q)(β−nh). Only L≠nh is used, not L>nh. Root's inequality 25−ν−nh ≥ 25−(5/2)(ν/r) > 0 for ν≥nr, ν<10r, h=5/2−r is arithmetically right, CONDITIONAL on the combined weight of a B initial at s-order ν being 25−ν, which must be read off the frozen grading, not assumed.

**What the lemma does not do.** It says nothing until the source supplies (i) the exact A initial `(Z²+b)³+C(Z)` with C≠0 and Z-degree ≤3 (the Z⁵/Z⁴ step forcing W=Z²+b fails at degree 4 or 5; note the Morse expansion of HC can reach Z-degree 2+deg_g C=4, so the degree bound is a real obligation, not bookkeeping); (ii) for every B initial before 10r, its degree n<10 and order ν≥nr, including the G-block orders in [2j,5j/2) that Fable's OPEN names; (iii) that no same-order cross term cancels the lowest bracket. These are exactly the "H-divisible source attachment" and are harder than the lemma. Do not confuse the two.

**Hermite detector (FP-C) merged only as a typing of C.** C∈K[g,p] is s-independent, so Sol's CRT map K(p)[g]/(LM²)→K(p)×K(p)[ε]/(ε²) is legitimate, and with deg_g C≤2 the three cases are exhaustive: (H1) C(−p,p)≠0; (H2) L|C, M∤C; (H3) C=LM·c(p), whose ε-coefficient p(t−1)c(p) is automatically nonzero, j≤6. The map does NOT choose the moving chart. Moving-source regimes are: (S) simple-L sheet, no Morse coordinate, root map with unit derivative; (Co) double branch, coalesced, ord ξ≥2r; (Se) double branch, separated, ord ξ<2r; plus the degenerate ξ≡0 / P0(g_c,p)≡0 infinite orders. H1 maps to (S) — the map is justified there because the L root is simple and static. H2 and H3 each split into (Co)/(Se), and there the Hermite value/derivative only supplies the transverse coefficient at s-order j+2r or j+3r; the actual r=min(j/4 or j/3, q0/6, …) also depends on the H-branch P0 remainder's own constant and linear ζ terms, which are NOT the non-H values (q0≥j+1, linear order exactly j) and must be re-derived. Root's Card 1 assumes ord ξ≥2r explicitly (Co only); Fable's Card 2 assumed it silently. Sol's "differentiate the lowest bracket transversely and work over the residue field" in H3 is the right refusal of UFD over dual numbers.

**Cheapest next source discriminator: Astra's (S)/H1 subcase.** No κ, no ξ, no two-sheet balance; A initial is a depressed cubic Z³+uZ+v at η=min(j/2, ord P0|_root /3), lower B initials are Z-free and die by the lemma with n=0 (weight 25−ν≠0 since ν<5η≤22.5), and the 3/5 monic Euler argument finishes. Obligations to write, in order: the H1 transverse value u (Hermite value on L) is the coefficient that makes u or v nonzero; the P0 remainder's constant on the L sheet; the G-block cannot enter below 5η. Budget 20–30 min desk plus one independent hostile gate. (Co) on H2 is second (Root Card 1 as written, with (i)–(iii) proved), (Se) and H3 last; stop after one noninformative attempt per chart.

## 3. FP-D/E and the orthogonal seat

Root asks whether the full Jacobian rows force w(A)≤3, w(B)≤5 after p→p+μ; Astra localizes the same question at one bad coefficient h via I_full+(zh−1) over the complete 371/816 client with all 105 inverse-lift rows. These are one task read in two directions. Correct reading of the endpoint: a proper (non-unit) localized ideal over the algebraic closure yields an algebraic point of the COMPLETE sufficient source, hence a characteristic-zero counterexample endpoint by the accepted lift; no Q(ρ)-rational point is assumed or needed, and a unit result kills only h≠0. Nobody supplied h, the centered coordinate map, or a solve object, so nothing is launchable.

**Seat decision:** give the orthogonal seat to common4 SOURCE TYPING, desk-only: name the centered map, list every A/B coefficient of weight exceeding 3/5, and test whether the existing rows already force each to zero (Root's highest-excess-weight argument is the proof direction, Astra's h the falsification direction of the same table). Stop on GAP with the first unforced h recorded; no LP, no solver. 15j literal attachment (FP-E) is the second seat only if a fully named unretired u_s=1/split client with every 15j hypothesis exists in the frozen record; a degree-ratio match alone is SCOPE-CONFLICT. Sol's block-index-two raise names no discriminator in this packet and gets no seat; it stays an all-degree reserve.

**Global vector.** Root, Astra and Fable: all 46 unchanged. Sol: lower 1,4,10,34,35,36, raise 26,29, on preference. Merged: all 46 unchanged; Sol's changes recorded as preference-only, adoptable only with a new discriminator. Poststate 15m is a bounded subbranch closure inside avenue 1/29/38 scope, still the only non-H branch result; the completed 2212 sweep is PARTIAL/NO ACTION and supplies no all-degree coverage.

## 4. Systems: NO_CHANGE, with the numbers already in hand

Collection receipts: freeze 22:26:47; both external lanes started 22:46:35 (19 min 48 s later, structural: root blind-first seal precedes invitations); Fable done 22:54:41 (in time), Sol done 23:02:36 (late). Fable's proposal to set the target as max(freeze+30, confirm+25) is a deadline reset by formula and conflicts with the contract's freeze-relative arithmetic; declined. Sol's three observation metrics are answered by these receipts without new machinery; keep recording them from run.v2, no wrapper. Astra's practice of preparing invitations and verifying pins while root writes its blind is the only bounded adjustment selected; it uses existing tools and changes no authority. Fable blind's private control is unretained: its 0.1 s claim is unverified; my rerun above replaces it and is retained in the box. Fable's COLLISIONS block quotes `notes.md`/`AUDIT.md` lines: that block's format is the lane-level OPEN-identifier collision scanner output over the report corpus, appended at seal time, not a semantic read by the model; I did not inspect the scanner or canonical tree and do not assert an event beyond the format match.

## 5. Merged cards, arrows, recommendation

- **Card B-S (simple-L, H1):** first missing arrow = exact A-initial cubic coefficients from HC on the L sheet with the P0 constant retained. Owner: Astra (source); reviewer: Fable hostile gate. Falsification endpoint: an explicit commuting depressed cubic/quintic pair at the derived orders.
- **Card B-Co (double branch, H2/H3, coalesced):** first missing arrow = Z-degree ≤3 and nonvanishing of the transverse correction, then the B-degree/order bound (ii). Owner: Root; reviewer: Astra. Separated regime OPEN regardless.
- **Card D (common4 typing):** first missing arrow = the literal bad-coefficient table after centering. Owner: Sol (exact representation); reviewer: Root.
- **Card E (15j client):** typing pass only; stop at first mandatory mismatch.

Recommendation: CONTINUE B-S then B-Co and D; REDESIGN any lower-B argument that still demands ord G≥2j or a monic degree-10 B initial before excluding lower ones; STOP the nonodd ansatz, all unequal/Q solver, cap and prime retries. Cross agreement is not promotion; 15m stays the sole accepted non-H result and no golden, common, D125 or JC2 closure follows.

## 6. Terminal handshake

Owned writes: this file and `box/ideation-20260908T2220Z-cross-fable5/` (check script, output, input pins). No Seal authored, no canonical/protected inspection, no agents, CAS, solver, AWS, or high H/R powers; one stdlib check under prlimit 25 s CPU / 512 MiB. Author STOP/IDLE after the marker.

<!-- BODY-END -->
