# Hostile review: Opus5 cap-free M>=2 budget quotient

You are Grok 4.6, acting as a different-model adversarial referee.  Review
the exact report
`/Users/dc/code/math/jc2/xmodel/m2-budget-quotient-primary-research-opus5-20260829.md`.
Its full-file SHA-256 is
`ffb83ec1fcbfbbb18573a030e80e22c20a7d78bb92a5e7a6a5ce4d7b9b96e7af`;
its claimed body hash through the stated 43,203-byte line is
`b161421ccb04a62307b87be4a60f130ded64fe53228bd600c960f2c502632283`.
Verify both first.

Comparison sources only:

- `ladder/BOOK-OFFAXIS.md` sections 10 P0--P5 and 11--11a;
- `ladder/SHEET6-DEPTH.md` cited R1/DS laws;
- `ladder/SHEET6-MULTIPOLE.md` MP1--MP8 and open mixed-merge statements;
- `ladder/REDUCTION.md` CRITICAL 4--7 and section 5.2;
- the Opus report above;
- your own already-written
  `xmodel/m2-budget-quotient-falsifier-grok46-20260829.md` only for an
  explicit agreement/disagreement crosswalk, not as authority.

Do not access, list, search, build, status, or control `jc2-lean`.  No web,
AWS, heavy/local CAS, canonical edits, git status, commit, or push.  Bounded
exact scratch arithmetic is allowed.  Write only the review named below.

Reconstruct every theorem.  In particular attack:

1. `T`-law `M | P-s*eps` and `T!=0`, including epsilon=0/no-root typing and
   whether it really discharges the recorded R2.2(D) rider.
2. The `C_e=0` iff claim.  Test unequal arrival multiplicities, non-minimal
   edges, `nu=1`, and the distinction between “some edge has C=0” and “an
   unbounded legal family exists.”  Check all-edge searrow consistency.
3. The claim that every zero-cost merge has `eps=0,k=0`.  A case-III
   0-arrival has `eps=mu0` but prices zero; determine whether this breaks the
   global M bound or only requires a separate bound.  Attack unpriced `lex`
   q-extras and mixed all-mu>=2 merges explicitly.
4. Theorem 4's divisibility/inequality and the derived case-III
   `nu<=mu0*num(w0)` bound.  Track which `M` is used and every cancellation.
5. The explicit bound
   `M<=M_entry*max(m,2^(m-1))*2^B`: prove it for the exact claimed scope or
   give a legal counterfamily.  Separate chain-only truth from whole
   configuration/merge truth.
6. Infinite equal-(mu,w) family: check all R1/R2 shape, integrality,
   `gcd(M,nu)=1`, arrival reachability, budget, and whether the td=7 T1 law
   can legally be applied outside its exact class.  Distinguish formal local
   cells from reachable fixed-entry configurations.
7. The three-family regeneration of the 17-row td=7 table and the proposed
   D2 td=8 discriminator.
8. Compare with your prior result: which of L1--L3, special-nu tables,
   colon rejection, and ranking/lasso conclusions survive?  Note that a
   Diophantine `C=0/C!=0` split is not literally an ideal-colon operation.

Give `PASS`, `PASS_WITH_NARROWING`, `REPAIR_REQUIRED`, or `REJECT`.  List
each theorem/subclause as confirmed, repaired, or refuted; state the strongest
promotion-safe result and cheapest next exact discriminator.  No landing,
ceiling, realizability, or JC2 extrapolation.

Write only
`/Users/dc/code/math/jc2/xmodel/m2-budget-quotient-primary-research-opus5-hostile-review-grok46-20260829.md`
and end with a report-body self-hash.
