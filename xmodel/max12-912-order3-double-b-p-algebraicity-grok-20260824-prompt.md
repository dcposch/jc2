# Research gate — double-`B` leaf and algebraicity of `p`

Work as an independent exact-algebra researcher in
`/Users/dc/code/math/jc2`. The target is the normalized maximum-12 `(9,12)`
order-three leaf

```text
k=mu=0, nu=1,
r1=r2=r3=r4=r5=r7=0, r6=1,
3*p+10*r8=0.
```

Read the confirmed root-free norm producer/review, the exact source compiler
`cases/max12_912_order3_fibre_20260824/order3_fibre.py`, and the unfrozen
support-learning scripts under
`cases/max12_912_order3_nu1_probe_20260824/`. Reconstruct source rows rather
than trusting modular output.

Current discovery evidence, not a premise: modulo `1000003`, the full
double-`B` ideal has dimension one and degree five; its `p=0` slice has
dimension one/degree five with free `q`, while its `p=1` slice is empty.

Try to prove or refute over characteristic zero that the elimination ideal in
`Q[p]` is nonzero. Prefer the smallest source-honest certificate:

1. exact elimination polynomial `P(p)` plus a reproducible membership or
   Groebner certificate;
2. a weighted-homogeneous elimination in `(p,nu,r8)` followed by the
   `nu=1`, `3*nu*p+10*r8=0` slice; or
3. a direct invariant identity forcing `p` algebraic over the constants.

Separate embedded/nilpotent structure, the `p=0` positive-dimensional
component, and every nonzero-`p` exceptional component. If `P(p)` exists,
explain carefully why an actual function-field trajectory has constant `p`
and hence constant `r8=-3p/10`, contradicting `9*r8'=j/u`. If the projection
is dense, identify the smallest counterexample to the proposed kill and the
right next coordinate/differential gate. Modular evidence may discover
support but cannot certify characteristic zero.

Write exactly
`xmodel/max12-912-order3-double-b-p-algebraicity-grok-20260824.md`.
Do not edit any producer, case, canonical, coordination, prompt, log, run, or
review file. Use `/tmp` for scratch. End with one research status:
`PROVED`, `REFUTED`, or `INCONCLUSIVE`, and state the strict scope.
