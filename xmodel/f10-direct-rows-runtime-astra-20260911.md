# Direct rows runtime port — source-only preparation

SOURCE-ONLY IMPLEMENTATION / DISABLED / UNEXECUTED / UNREVIEWED. First action2026-09-11T03:22:20.666745876Z; new report, transaction and box absent. Original reserve03:50/HARD03:53UTC September11 unchanged. Exactly ROOT-CARD plus ten pinned inputs, all FRESH_WHOLE; clipped aggregate reads recovered. No peer source, coefficient payload, scientific execution, worker or network access. This packet cannot authorize execution before both source and runtime FIRST and a fresh ROOT registration.

## Deliverable and changes

Prepared owned dispatch.py, probe.py, mutate.py, CONTRACT.md, DISABLED-REGISTRATION.json, literal DISPATCH.diff, PINS.json and READ-SCOPE.md. All source, including syntax, is UNEXECUTED. This is the concrete first-source-evaluation wrapper, not the parked canonical rank runtime. No actual source implementation is presumed conforming: its five final hashes, accepted contract manifest, complete reference vector, prescribed place and exact seven preflight predicates are future ROOT fields.

The dispatcher is a text derivative of the charged accepted source runtime. Its same thirteen labels, CAPRUN invocation/identity, startup/native/physical checks, sampled budgets, stop policy, quiet-before-read, telemetry, move/freeze and durable EBS custody remain. The diff exposes only new job/schema, ROOT final source/contract/place bindings, entry-path/policy changes and exact new wire/status/T predicate. Obsolete rational-checker traceback matching is removed; no new supervisor, retry, cap increase or source reconstruction is introduced.

Five early refusals now execute ACTUAL produce.py. Wrong-tag tests execute the actual produce.py/check.py. Valid and dummy instead execute an explicitly separate ROOT-policy-only helper; no spoofed sys.argv or scientific authorize call exists. The valid event is ROOT_POLICY_ONLY_SENTINEL. The dummy retains the original four events, same-group orphan allocation, TERM/KILL identity and RSS/cleanup conditions. Neither claims science authorization or qualification.

New science authorities contain exactly eleven keys with contract_sha256, source_pins and place added. The five installed absolute source paths must agree with the five files hashes and ROOT pins. Remaining source-reference keys are ROOT's fixed documentary vector, not candidate authority. The wrapper does not consume the scientific helper's return mapping or infer mathematical correctness from it. ROOT review clearance and a separate immutable source-hash-bound preflight policy are required; their final values are UNFORMED.

## Exact binding and mutation reasoning

Seven scientific refusals require ROOT's COMPLETE literal exit/stdout/stderr policy derived against the eventual actual source and reviewed before activation. Normal nonzero exit, absent output, empty stdout and exact bounded ASCII stderr are all required. There is no regex/substr/wildcard semantic acceptance and no guessed old traceback. The canonical negative predicate is fixed now: normalexit2, empty stdout, stderr exactly CHECK FAILED: direct-source graph slot T followed by newline, no traceback. Other errors or resources STOP.

Only two checker authorization hashes and the mutator ROOT-registration hash retain typed future slots. Probe commands use deterministic ROOT-policy digests with a ROOT path but no recursive ROOT hash. Literal ROOT command vectors bind every actual suffix; the dispatcher compares the full expanded CAPRUN argv before launch. This resolves entry-path authentication without a self-hash cycle. Root-owned immutable/no concurrent writer is the explicit trust model, not hash-spelling as authority.

The actual producer and positive checker each run ONCE. Only their exact new success lines, complete normal terminal telemetry, input/current-source pin checks and quiet status allow the ROOT dispatcher to freeze the genuine positive record. The mutator independently authenticates that record and reconstructs the expected eleven-key positive authority; it never calls the checker or its authorize function, and imports no scientific module.

After its own metadata barriers it parses all30 typed graph polynomials/two dense field scalars, canonical decimal strings, exact weights, slot count, numerical exponent order and joint term/byte caps. Only graph.slots[24]'s field-constant coordinate at exponent(27,0,0,0) changes by1 modulo ROOT's authenticated p. Missing term insertion and zero-vector deletion preserve canonical form, all25 positions and every other byte's mathematical value. Whole-byte inverse restoration and changed SHA are mandatory; insertion refuses if either term ceiling has no spare slot. The mutation is a nonzero weight27 monomial over the genuinely checked field, not an unlicensed nonconstant cofactor or field division. Input validity comes from the genuine positive checker, not the mutator's structural checks. Actual semantic reachability/resource slack remain unmeasured.

## Retained limits and explicit remaining readiness

Per-source900wall/600CPU/8GiB/128MiB; aggregate3000wall/2100CPU/8GiB/128MiB; control5/3, dummy5/3 with32MiB RSS, producer/check900/599 and mutator30/20 unchanged. Packet budget120MiB jointly plus8MiB metadata/streams. Same2050 sampled cumulative CPU cutoff and50 nominal cleanup allowance are NOT a kernel hard aggregate guarantee. Same original four UTC boundaries and wall+15 admission; ROOT binds actual enforcement/native/physical facts later. No measured operation count, producer/checker speed comparison or feasibility prediction from old FIRST is adopted.

No blocking implementation placeholder is knowingly substituted for a test in the selected wrapper. Deployment remains blocked on two FIRST reviews, final scientific API conformance, seven exact source refusal entries, actual ROOT source/reference/contract/place bindings and native/worker/clock/command/cgroup/output/custody registration. The disabled JSON intentionally cannot pass those checks. Inherited transient-map/ESRCH or missed-live-capture false STOP risks remain; no favorable-observation retry or optional hardening is introduced.

OPEN quantity: whether the eventually finalized source and this13-label port satisfy exact entry/mutation predicates and finish under the unchanged limits. Cheapest next check is source FIRST plus this runtime FIRST on terminal bytes; ROOT may then separately authorize one observation batch. Planning25–30minutes is UNMEASURED documentary planning, not scientific runtime. No fixture, actual place, row data, rank, rational baseline, source result or JC2 claim; no follow-on authority.

## Own review and custody

All three owned sources and documents were reviewed WHOLE as static text; no AST/syntax/import/test or mathematical subprocess. Literal diff was read WHOLE. Own-only targets and current input/owned pins are checked before marker/seal. The accepted originals, older boxes and live peers remain untouched. Only ordinary artifact_finalize metadata publication is used. Final transaction/custody carry exact terminal hashes; ROOT receives custody FIRST after all writers are idle.

Closing review: final body reread WHOLE at03:40:10UTC; prior source/document WHOLE review completed03:36:55UTC and all11 input/current owned pins matched03:37:52UTC. The explicit OPEN above retains its quantity and cheapest next check; it is an unexecuted validation obligation, not an asserted pass. Publication occurs before the original reserve and hard cap.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7542`.
- Body SHA-256:
  `b0d868ed9cbe2fc647ce36a2cd2902f4a7b0a0252a349851ab89876abd711bcd`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
