# TRIPLE02 node-1 closed-successor resume r1 — source producer

You are Fable 5 acting as an independent expert in computational algebra,
adversarial scientific-computing custody, and Python/Singular pipeline design.
Work in `/Users/dc/code/math/jc2`.

## Objective

Produce a **source-only, AWS-ready, fail-closed resume packet** for the exact
TRIPLE02 closed successor

`V(I_node1 + (Delta_node1))`.

The node-1 proper open `D(Delta_node1)` has already been reviewed and proved
endpoint-dead.  The closed successor is still open.  Begin directly with the
three literal generators

1. the TRIPLE02 branch factor,
2. the root chart delta, and
3. the archived node-1 selected rank-six minor `Delta_node1`,

with inherited rank upper bound 6.  Do not recompute or silently depend on the
node-1 open saturation.  Continue the frozen finite Fitting/chart recursion on
that closed ideal, with global node numbering beginning at 2.

## Frozen evidence

- R3 TRIPLE02 terminal archive:
  `cases/ggv_8_28_upper_endpoint_quotient_nf_endpoint_complements_r3_20260828/custody/terminals/ggv_endpoint_complement_triple02_r3_20260828T175423Z_i0f089.terminal.tar.gz`
  SHA-256
  `e5bdd2b25cde0dccd8f1e6b8f0e1fcc815d409426308afb49c45df86af53a1e1`.
- Reviewed node-1 proper-open r5 terminal archive:
  `cases/ggv_8_28_upper_endpoint_triple02_proper_open_resume_r1_20260828/custody/terminals/ggv_triple02_proper_open_resume_r5_20260828T233745Z_r6d.terminal.tar.gz`
  SHA-256
  `4b8ffc1c16828b22b2e76a3aa92a25e8c1f472190a708f30a7b71ba952cbef7e`.
  This proves only `EXACT_ENDPOINT_DEAD_ON_NODE1_PROPER_OPEN_ONLY`; it is
  provenance for routing, not a source of a closed-successor answer.
- Frozen r5 recursor:
  `cases/ggv_8_28_upper_endpoint_quotient_nf_endpoint_complements_r5_20260828/recurse_component.py`
  SHA-256
  `d679a4d7fb4bf2619fbb3ff47ea6f386ba1c75fdc477276cabf3bec8a2b2cd90`.
- The R3 archive contains the literal source generators in
  `output/node_001/NODE_INPUT.json`, the exact rank-six witness in
  `output/node_001/NODE_001_SIZE_6_WITNESS.tsv`, the reducer/residual/pivot
  artifacts, and their terminal manifest.  Independently bind and validate
  the required members.  Never trust filenames or archive extraction without
  traversal/link/device/duplicate/member-census checks.

## Required output

Create only the fresh directory

`cases/ggv_8_28_upper_endpoint_triple02_closed_successor_resume_r1_20260829/`

and the report

`xmodel/triple02-node1-closed-successor-resume-r1-fable5-20260829.md`.

The packet must contain everything needed for a later reviewed AWS pilot:
preregistration and scope, deterministic builder/resume code, fail-closed
classifier, worker/supervisor/launcher or precise integration with a frozen
reviewed launcher, source/input manifests, safe archive handling, lightweight
generator/selfchecks, hostile diagnostic fixtures, and exact expected source
archive/hash instructions.  Do not modify any existing file.

## Mathematical and lifecycle requirements

- Establish the resumed starting ideal exactly as the two archived node
  generators plus the archived rank-six witness normal form.  Check literal
  hashes, semantic replay in the generated Singular script, generator census,
  and inherited rank-bound provenance.
- The closed-successor route must never enter the already-settled node-1
  proper-open chart or its pure-delta power search.
- Preserve the r5 recursive contract: exact reducer replay, exhaustive
  support-matchable minor census from the inherited bound downward, explicit
  nonzero witness, saturation with two-containment/stability evidence,
  closed-successor construction, full right-transform/base change, all 106
  row kernel identities, endpoint coefficients, nonzero plants, and narrow
  ring-level classifications.  Repair any known weak custody pattern rather
  than copying it blindly.
- Distinguish ideal equality, saturation equality, ring-level endpoint
  nonzero, radical/nonemptiness, and geometric existence.  Never infer a
  whole-component or JC2 conclusion from a chart-local result.
- A terminal answer may be exact component death on a finite cover, a narrow
  ring-level survivor pending nilpotence/radical/geometric checks, a bounded
  no-verdict remainder, or fail-closed adapter/resource failure.  Exactly one
  terminal classification is allowed.
- Include strong sticky latches, exclusive lock plus run nonce, stage and
  whole-job timeouts, process-group cleanup, full tagged orphan censuses,
  resource/swap gates, deterministic fresh output, source immutability/hash
  gates, terminal manifest, archive replay, and no success marker before all
  gates and classification complete.

## Resource and safety rules

- **Do not run Singular, msolve, Sage, PARI, or any heavy CAS locally.**
- **Do not launch, inspect, tag, stop, or otherwise touch AWS.**
- Lightweight Python syntax/unit/hostile tests are allowed, but no large or
  long computation.
- Do not inspect or touch `jc2-lean`.
- Do not edit canonical campaign ledgers, commit, or push.
- Treat all existing work as user-owned.  Do not delete or overwrite it.

## Report contract

The report must state: source files and hashes; exact archived members and
hashes used; mathematical scope; how the closed successor is reconstructed;
why the settled open route cannot be re-entered; all hostile fixtures and
their outcomes; remaining risks; and one verdict among
`SOURCE_PACKET_READY_FOR_DIFFERENT_MODEL_REVIEW`, `REPAIR_REQUIRED`, or
`BLOCKED_WITH_EXACT_REASON`.

Do not claim AWS authorization.  A different-model hostile source review is
mandatory before any launch.
