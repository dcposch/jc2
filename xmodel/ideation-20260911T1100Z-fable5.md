# Independent FULL46 strategy blind — Fable5.1 (1100 round)

BLIND STRATEGY REPORT / no scientific execution. Basis 0d39df3c9fd69c939a8420c54d03228b9077777d. Science cutoff 2026-09-11 10:54 UTC. First own action 11:05:44 UTC; own report and box were absent. Original reserve 11:20:00 / HARD 11:23:00 UTC, never reset. All eleven frozen inputs were SHA-256 matched to the invitation before any read; PINS.json and common-contract.md were read WHOLE first, then the nine other bodies WHOLE in bounded unclipped ranges (approaches.md in three ranges 1–350, 351–700, 701–1025). No peer or ROOT blind was read. No promotion, launch or new theorem is claimed here.

## 1. Full46 disposition

Vector over all 46 IDs, judged fresh from the packet, not copied from the 2300 vote:

- 1, 29, 31, 36, 38: KEEP RAISED as ONE attached source program (complete homogeneous cone E_r, accepted 17zzzzzzzzzzzzzzzzz), but LOWER the operational weight of the fixed r3 experiment relative to the uniform question. Reason: in any positively graded presentation of the source that is Artinian (every REG-type finiteness premise), the guard g has positive weight 9r+3 and is therefore nilpotent, so E_r=0 follows with no computation. A positive r3 exponent-one identity is thus the EXPECTED outcome under REG and does not discriminate the regime where REG fails; its value is cofactor data for the missing uniform argument, not a decision. Four operational failures (timeout, 100000-term guard, dummy lifecycle, calendar grammar) are harness facts and do not change this mathematical weighting.
- 12: LOWER for further middle/late residue farms only; 17zzu/17zzzzzzzzzzzzzz closed those columns.
- 26, 30, 31 (block/primitive): UNCHANGED, with the history correction adopted: d2=2 and proper Galois quotients are excluded (BD-GAL); d1=2 source blocks and non-Galois d2>=3/primitive maps remain, with no new typed discriminator in this packet.
- 16, 20: UNCHANGED. Zhang 2609.10180 gives holonomicity in positive characteristic; the char-0 bridge needs simplicity of the pulled-back module, which no input supplies.
- 19, 21: UNCHANGED; AS109 remains the second disproof lane with the degree-twelve correction frontier.
- 32: UNCHANGED; the three-generator collision presentation stays an exact simplification without a bounded client.
- 2–11, 13–15, 17, 18, 22–25, 27, 28, 33–35, 37, 39–46: UNCHANGED. No ID is REOPENED. The pseudo-plane results PP-HOM-1/PP-LIN-U-1/PP-SEP-WT-1/PP-3WT-1 carry no avenue raise: they are model-organism theorems on R=k[A,U,Z]/(U^2-A-A^2Z), already covered for no-mate purposes by classical one-line injectivity, and the arbitrary-H boundary GAP is an H-only question that does not feed a JC2 proof or counterexample.

## 2. Proof and disproof bottleneck ranking; mechanism; connection

Proof bottlenecks, strongest first: (P1) the uniform all-r F10 residual, now stated sharply as: every component of the cone V(I_hom) other than the last axis lies in V(H_q) union V(ell); the manual uniform identity attempt ended NO_IDENTITY and no mechanism exists. (P2) global source coverage/landing/type-cofinality (G2-PSC, G2-BD, the KJN type menu), unchanged and indispensable; EXIT-RPMC is not cheaper than RPMC. (P3) primitive/non-Galois block finiteness (d1=2, d2>=3). (P4) K16 all-m b*B*eta and the b=0 boundary.

Disproof bottlenecks: (D1) an exact nonzero COMPLETE guarded E_3, which through the accepted covers reconstructs a Keller pair of degrees 280/476 over a number field; this is the only live finite object whose nonvanishing is a counterexample. (D2) a genuinely algebraized AS109 lift with correction y-degree at least twelve. (D3) nothing on the pseudo-plane: a constant-bracket non-automorphism there would only show which plane-specific input a proof must use.

NO_NEW_GLOBAL_MECHANISM. One NEW bounded instrument, derived here by hand from the accepted weights only: the 297-row full-rank criterion of the one-place contract (23z/25z) asks that (I_hom)_30 be ALL of weight 30, which is strictly stronger than g in I_hom. A weight-30 monomial has a nonzero matrix row only if some generator contains a monomial dividing it. The monomial X_2*X_4^7 is divisible only by X_4^k or X_2*X_4^k, which can occur only in generators of weight divisible by 4 (K_8; weights 12, 16, 20) or congruent to 2 mod 4 (K_10; weights 14, 18, 22). If those twelve coefficients vanish, the row is identically zero, rank is at most 296 at EVERY place, and the deployed success criterion is unattainable, while g (which has no X_2*X_4^7 term because 4 does not divide 7 or 23 minus 2) may still lie in I_hom. Support-cover is therefore a necessary precheck for the instrument, not a new source theorem.

Cross-avenue connection: the three-weight pseudo-plane proof and the F10 cone question are the same discipline, restrict a global object to a one-variable curve and certify roots off the discarded factors. The last-axis restriction X_1=X_3=0, X_2^2=0 of the 25 slots is the F10 analogue of the u-chart eliminant; it also computes the deferred necessary-REG norm c_3 (the pure X_4^{w/4} coefficients at weights 8, 12, 16, 20), which at r3 is a field element, so unit iff nonzero.

## 3. Cards (at most three)

Card 1, support-cover precheck. Object: the twelve coefficients above (six pure X_4 powers, six X_2*X_4^k) of the actual r3 slots. Hypotheses: the accepted cone presentation and weight table (variables 1,2,3,4; slots 8,9,10; 11–19; 11–22; 27; g weight 30); the 297-row criterion as deployed. Cheapest honest discriminator: restrict all 25 slots to the dual-number line X_1=X_3=0, X_2^2=0 before any expansion; this is a one-variable truncated expansion far below the 100000-term wall, manual if the row formulas are explicit. Gate: STATIC/manual, FIRST if adopted. Wall: UNMEASURED, planning figure under one hour manual. Outcomes: all twelve zero means the full-rank instrument is structurally dead and must be re-gated to rank 296 on the X_2*X_4^7-free subspace W, which then contains (I_hom)_30 and g, so rank 296 becomes SUFFICIENT for E_3=0; any nonzero means the instrument is not obstructed by this row and may proceed. Stop after the twelve coefficients. Information gain: decides whether the next relaunch can succeed at all, and yields c_3 for free.

Card 2, REG-nilpotency reading. Object: the claim that in every positively graded Artinian presentation of the source the positive-weight guard is nilpotent, so REG alone gives E_r=0 and the uniform problem is exactly the non-Artinian components. Hypotheses: g homogeneous of positive weight in the graded presentation. Test: ten-minute manual check against the accepted kernel and cone presentations. Outcomes: confirmed means the r3 positive outcome is expected under REG and the disposition lowering in Section 1 stands; refuted means withdraw that lowering. Stop after one reading. No third card: the pseudo-plane lane has no decision-changing test left.

## 4. Strongest attacks, software experiment, systems decision

Strongest proof attack: on the chart H_q not zero, zeta=-H_q/c is an honest coordinate and the r rows K_h are the H-band; prove that the 7r+1 remaining rows force ell=0 on every component there, i.e. attack ell in I_hom + (H_q-inverted) directly rather than g. Strongest disproof attack: exact nonzero E_3 through the faithful covers; the honest instrument is exact membership, not rank at one place, because rank can only drop under specialization and a place failure is nondecision. Software experiment: the dual-number restriction of Card 1 run once; it replaces the inverse-chart expansion that hit the term guard with a two-term truncation and is the cheapest object whose zero outcome changes the plan.

Systems: NO_CHANGE to shared machinery. The four r3 stops were four different harness defects, and the last worker cost 22 minutes for zero mathematics; each added assurance layer has so far added a new failure mode. The only concrete fix is the one-line calendar literal already diagnosed (2026-09-11 11:10:00 UTC form). Tradeoff: no regression from no change; a new serializer or lifecycle framework would cost another static FIRST and has no measured benefit. No relaunch authority is claimed.

## 5. Continue / redesign / stop

REDESIGN the r3 execution, CONTINUE its mathematics, STOP the duplicates. Concretely: run Card 1 statically before any relaunch; if the 297 criterion is attainable, the next batch is the already accepted 29z necessary-row vector with the calendar literal fixed and nothing else added; if not, re-gate to rank 296 on W. Continue the uniform off-axis question as the real proof front. Stop further pseudo-plane sparse-family chases, identical selfmap/deck checks and BGV echo reviews; they are DUPLICATE at their valid scope. No pressure applies: all-F10 is not JC2, and this report endorses no extra compute or review beyond the two cards.

## 6. Read scope, postpins, quantity, collisions, closeout

ERRATUM to Section 2: read "because H_q has weight 7 and 4 divides neither 7 nor 5" in place of "because 4 does not divide 7 or 23 minus 2"; the conclusion (g has no X_2*X_4^7 term) is unchanged.

READ SCOPE: all eleven inputs WHOLE. PINS.json and common-contract.md first via one whole cat; approaches.md via three bounded reads 1–350, 351–700, 701–1025 (file ends at 1025) with no clipping; master46-history (54 lines), reduction-interfaces (142), previous-synthesis (105), earlier-synthesis (73), latest-websweep (73), current-operations (126), boundary-test (98), three-weight-proof (109) each in one whole read. No peer, ROOT blind, linked, corpus, protected or shared object was opened. This is a FULL read, not a partial.

POSTPINS: all eleven inputs were rehashed at 11:14:08 UTC after the Section 1–5 writes and again match the invitation and PINS.json byte for byte; the custody table in box/ideation-20260911T1100Z-fable5/input_custody.md lists every value. Own report was read WHOLE at 11:14:17 UTC (8966 bytes, no marker, no clipping) before this section.

QUANTITY: whether the twelve axis coefficients of the actual r3 slots (pure X_4^{w/4} at weights 8, 12, 16, 20; X_2*X_4^{(w-2)/4} at weights 10, 14, 18, 22) are all zero. CHEAPEST TEST: the dual-number line restriction X_1=X_3=0, X_2^2=0 of the 25 slots, static or manual, wall UNMEASURED (planning figure under one hour if the row formulas are explicit; not a runtime forecast). Both outcomes are interpreted in Card 1. No canonical OPEN identifier is raised; no promotion, theorem, launch, relaunch authority or coefficient artifact is claimed. Section 1 lowering of the fixed r3 weight is conditional on Card 2.

COLLISIONS (own-only, documentary): both owned targets were absent at 11:12:07 UTC and only they were written, every byte through the external apply_patch adapter. Within the packet, the twelve coefficients overlap the deferred necessary-REG norm c_r and the accepted unit-pivot/full-rank contracts; the support-cover reading of the 297 criterion is a NEW instrument on the same objects, not a new theorem, and its novelty against the wider history is unchecked here. The pseudo-plane, BGV and selfmap items are marked DUPLICATE at their valid scopes per the contract, not re-reviewed.

BOUNDS HONORED: manual mathematics, static text and hashes only; no scientific subprocess, import, AST, syntax, compile, test, dummy or CAS of any size; no source, coefficient, fixture or baseline payload; no network, AWS, SSH, Git or process control; no agents; no input edit or disclosure. Closeout before the ORIGINAL 11:20:00 reserve. Only adapter custody and sealing follow the final marker; none is authored here.

END OF SECTIONS.

<!-- BODY-END -->
