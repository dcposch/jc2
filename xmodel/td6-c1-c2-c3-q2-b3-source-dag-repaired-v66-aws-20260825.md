# TD6 fixed-A3 q2 `B3=0` repaired source-DAG checkpoint

V66 gives a producer-exact original-row source identity on the birational
`B3=0` chart for the fixed source-typed A3 q2-beta section.  Renaming the
producer's chart symbols `(C,V)` to `(t,w)`, current row 13 traces through
exactly previous row `('X-1',14)` and original first rows; row
`('X-1',0)` is a separate quadratic positive control.  Genuine P12 has
counts `2893/28/1649`, replays directly through original first rows, and
composes with `N13=k*beta/25` to the exact unit residual `-k/50`.

The complete proof-DAG-leaf plus pinned-P12 denominator radical is exactly

```text
{t, w, t-2, 2t-1, t^2-4t+2}.
```

Thus the producer licenses only
`B3=0,D(t*w*(t-2)*(2t-1)*(t^2-4t+2))`.  Every displayed factor locus must be
rebuilt directly from source.  No whole-`B3=0`, whole fixed-A3, TD6, SP-2,
landing, or JC2 inference is made.

Frozen case:
`cases/td6_c1_c2_c3_q2_b3_source_dag_repaired_v66_aws_20260825/`.

Custody hashes:

- source archive: `dbb97927b097cf2eb5876f782c2b94cfc8b2830b07fe4db288db8aa13fdfbfe6`;
- Box02 / r6d stdout: `c7c3342a...` / `26341867...`;
- byte-identical proof DAG: `3751f623e2f60e899f0214e2151e6a2f5923ee2d6a07a9b9424dd87a2877def8`;
- byte-identical denominator ledger: `fc655dea4b85f836d7372f1b7c6e34835b8403d0b755a154c82911dfff605b7e`.

Status: **PRODUCER-EXACT / HOSTILE REVIEW PENDING / STRICT GENERIC-CHART OPEN.**
