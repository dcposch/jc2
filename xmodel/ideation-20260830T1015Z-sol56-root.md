# Blind whole-portfolio ideation — coordinator Sol 5.6

Round: `20260830T1015Z`  
Common packet: full `ce5b9564a8f5cab7eea146e0408bc32950ff8a168a678f340d682fba4e621c58`  
Frozen author basis: `dcd6cf9ccce35e7deee956245ca46d2dfd00d5d2`  
Lifecycle: **BLIND SUBMISSION / IDEAS AND DERIVATIONS UNREVIEWED**

## 0. Outcome

The q=8 closure materially raises the fixed-block geometric route, but the
campaign must distinguish two very different distances:

```text
distance inside the fixed quadratic finite-reduced cell:  two q=6 rows;
distance from arbitrary minimal Keller map to that cell:  still unbounded.
```

My highest-information immediate experiment is a q=6 extension of the new
intersection identity. A first exact desk derivation below already shows that
the baseline and every contracted-carrier cell die before enumeration; hence
any q=6 survivor must use an affine ADE tree. The finite successor is to
enumerate only those low-rank affine trees. In parallel, the strongest bypass
is not another quadratic coefficient sweep but an adjoint/conductor theorem
that preserves geometric genus for all coefficient degrees at least three.
On the counterexample side, another isolated K00 grade has lower value than a
scheme-theoretic transition-image/source-completeness test.

No idea here is a proof of JC2, a map, or an independently reviewed theorem.

## 1. All-avenue disposition

| avenues | disposition | reason |
|---|---|---|
| `28,30,31` | **raise** | The reviewed rational-forest, `A1`-Euler, block-surface, discriminant/index, and exact ruled-lattice results now compose into a concrete finite geometric client. Avenue 31 is raised only for intrinsic presentation/control and transition images, not for the tautology “prove finiteness.” |
| `4` | **unchanged globally; lower more grade-only execution** | Exact K00 cells remain the best disciplined disproof laboratory, but the marginal next row is below source completeness, truncation-image compatibility, all-order lifting, and algebraization. |
| `36` | **unchanged globally; lower unconstrained search execution** | The quadratic funnel gives no reason that random sparse maps hit its fixed presentation; search remains useful only as a source/atlas falsifier with a named structured client. |
| `2` | **unchanged, still principal** | It remains the only developed global selector architecture; q=8 progress does not supply `G2-PSC`, `G2-BD`, or an absolute/cofinal sheet ceiling. |
| `1,3,5-27,29,32-35,37-46` | **unchanged** | No new evidence changes their truth status or global rank. In particular `11` and `41` stay dead, `44` stays refuted-as-proof, and `46` remains rigor infrastructure rather than discovery authority. |

No numbered avenue is reopened. The new mechanisms below live at the
interfaces of existing avenues rather than multiplying the inventory.

## 2. Reranked gaps

### Proof side

1. **Global selector / intrinsic presentation.** Show that an arbitrary
   minimal counterexample supplies a proper cubic block and a bounded
   trace-zero presentation, or bypass this by a presentation-independent
   normalization theorem. This is upstream of every fixed quadratic closure.
2. **The two q=6 singular-F5 rows.** Complete the affine-ADE-decorated global
   lattice for `B3/A2` and `U3/A3`, with the balanced modulus strata kept
   separate. This is the closest finite geometric obstruction.
3. **Excluded fixed-block strata.** Nonreduced infinity, projective
   nonfiniteness/basepoints, degree drops, and singular/non-normal coefficient
   degree at least three must be closed without silently assuming a good
   basis.
4. **Independent sheet ladder.** `G2-PSC`, `G2-BD`, QCS, and full landing/
   coverage remain separate global bottlenecks even if the quadratic client
   closes.

### Counterexample/falsification side

1. Prove or refute source completeness for the K00 normal forms and compute a
   scheme-theoretic image of one truncation transition.
2. Decide compatible all-order lifting and algebraization on a surviving
   source component; finite field points and formal prefixes do not suffice.
3. Test reachability from an honest Keller pair and whether the normalization
   charts cover the asserted source boundary.
4. Use sparse/collision search only as a bounded falsifier of one claimed
   atlas or selector, not as a generic map lottery.

## 3. Detailed research cards

### Card 1 — q=6 shared-contact intersection identity

**Novelty:** `NEW-COMPOSITE`. Repository search contains the q=6 marked
classes, polar factors, Euler budgets, and the q=8 positive-intersection
identity, but not this q=6 specialization or its carrier consequence.

**Dependencies:** binding `F2` marking and rational forest; reviewed balanced
`B3/A2` local theorem; provisional `U3/A3` local theorem; q=8 review only for
confidence in the method, not for the algebra below.

For either q=6 marking the exceptional different equals the infinity cycle,
so before affine ADE subtraction the nonexceptional ramification total is

```text
R^o=4S0+9F-sum c_i P_i,
c=(0,1,1,2,2,2,2,2,2),       sum c=14, sum c^2=26.   (3.1)
```

Here the zero coordinate is `L`, the two ones are the outside set, and the
six twos form `I6`. Direct intersection gives

```text
R^o.S0=R^o.T=1,       R^o.L=0.                        (3.2)
```

The local calculations distinguish one polar prime at the shared `S,T`
physical site: the `T=sigma` branch for `B3` and the smooth degree-four branch
for `U3`. All other local polar primes use distinct sites. In the no-affine-
ADE cell, write their classes

```text
C_j=a_j S0+b_jF-sum_i x_ji P_i,       sum a_j=4.
```

Nonnegative intersection with `S0` and (3.2) force one distinguished index
`r` with `b_r=2a_r+1` and all others `b_j=2a_j`. If `d_j=A.C_j`, then

```text
sum_i x_ji=5a_j+2*1_(j=r)-d_j,
C_j.C_k=2a_ja_k+a_j*1_(k=r)+a_k*1_(j=r)-sum_i x_ji x_ki.
```

Since `sum d_j=8` and integer squares dominate entries,

```text
sum_(j<k) C_j.C_k
 >= 2sum_(j<k)a_ja_k +(4-a_r) -(26-14)/2.            (3.3)
```

For generic `B3`, positive `a` is a permutation of `(2,1,1)`, so (3.3) is
at least six. For `U3`, a positive pair sums to four, so it is at least one.
The special `B3` two-prime cells also remain positive. This contradicts the
forest pairwise-disjointness gate.

Every q=6 contracted class contains both outside coordinates. Subtracting a
carrier once leaves

```text
3S0+7F,      multiset(c')=(0,0,0,1,1,1,2,2,2),
sum c'=9,    sum (c')^2=15,                            (3.4)
```

and the same bound is still positive for both rows. A second carrier or
Cartier coefficient at least two makes an outside coordinate negative.
Thus, subject to checking the shared-contact identification literally, every
q=6 survivor must contain an affine ADE tree; contracted carriers do not
provide an escape.

**Cheapest discriminator:** independently reconstruct (3.1)--(3.4), verify
the distinguished physical contact on a common SNC resolution, then stream
all H-disjoint `D9` root embeddings of total affine rank at most four using
the promoted ADE Cartier table. Do not impose proximity until the relaxed
set is known.

**Outcomes:** empty relaxed set closes q=6 and, after review, the reduced
finite normal-singular fixed-quadratic stratum. Survivors identify exact
affine ADE/root/multiplicity cells for analytic effectivity. A failed shared-
contact premise narrows the result to a baseline calculation and triggers a
local embedded-resolution repair.

**Stop condition / compute:** stop after exact rank-four orbit exhaustion or
the first independently checked survivor. Standard-library desk work first;
AWS only if orbit decoration exceeds 60 CPU seconds or 1 GiB. Run local proof,
clean-room falsifier, and root-embedding bypass concurrently.

### Card 2 — adjoint persistence beyond quadratic coefficient degree

**Novelty:** `NEW-CONNECTION`, joining the smooth `p_g` exclusion,
normalization/conductor corrections, `DISC8-INDEX`, and block-surface
classification. History records smooth degree-at-least-three closure and
special degenerate strata, but no adjoint-ideal lower bound uniform over
normal and nonnormal degenerations.

**Target theorem:** for an irreducible proper cubic-block incidence of
coefficient degree `d>=3` admitting the charged dominant `A2` first leg, the
normalization retains a nonzero canonical form (or the conductor creates a
positive-genus/cyclic boundary), contradicting `p_g=0` and the rational
forest. Formulate this as a lower bound on the adjoint ideal of the infinity
curve plus conductor correction, not as semicontinuity across an unspecified
family.

**Cheapest discriminator:** do `d=3` first. Classify how much the finite
singularity/conductor correction must erase from the ambient canonical
system; compare with the degree-eight discriminant and degree-four index
budget. Produce either one surviving canonical section or an explicit
degenerate rational control showing the bound is false.

**Outcomes:** success gives an intrinsic coefficient-degree ceiling for any
existing cubic block frame and makes the quadratic funnel far more global.
A control with `p_g=0` supplies the exact next singularity/conductor stratum
instead of a vague “degenerate infinity” remainder. Failure blocks this
bypass without touching q=6.

**Stop condition / compute:** stop after the `d=3` adjoint linear system and
all correction lengths are exact; source review and desk symbolic algebra,
with any large elimination registered on AWS. Assign proof, explicit
countermodel, and discriminant/index comparison separately.

### Card 3 — K00 transition-image/source-completeness gate

**Novelty:** `KNOWN TARGET / NEW PRIORITY`. Earlier rounds repeatedly named
truncation images; the new fact is the opportunity-cost decision to prefer
one exact image theorem over another isolated grade.

**Target:** on the smallest reviewed predecessor with more than one successor
cell, construct the scheme-theoretic source-to-jet morphism and compute the
Fitting/elimination ideal of its image at the next truncation. Track the
literal integer numerator before any characteristic reduction. Ask whether
all formal successors used by K00 occur in the image and whether every source
point is represented by one of the normalized charts.

**Cheapest discriminator:** one transition, ordinary and deliberately
perturbed negative controls, compared with the sharp unloaded filtration
`J^5 subset I subset J^2`. A missing survivor proves noncoverage of the
current atlas; a proper image supplies new equations stronger than a row;
dense image says further grade work may again be rational.

**Stop condition / compute:** stop at the first exact image/counterimage
certificate. Design locally, execute any Gröbner/Fitting elimination on AWS.
Run a source-atlas falsifier in parallel. This remains finite-jet work until
an all-order theorem is separately proved.

## 4. Mathematical software and campaign-systems cards

**Mathematical software:** parameterize the reviewed q=8 lattice replay by
`(S0-contact,T-contact,c,d,local-site partition)` and add the q=6 special
component. Stream only affine root systems of rank at most four and report
closed-form lower bounds before enumeration counts. Acceptance is agreement
with every q=8 digest plus hand-derived q=6 baseline/carrier bounds and a
forged shared-contact mutation that must fail in ordinary, `-O`, and `-OO`.

**Systems UPGRADE — validate artifact bases before lease creation.** During
this blind round, root twice supplied a guessed 40-hex expansion of a short
hash to `artifact_finalize.py begin`. The tool validated syntax but not that
the value named a Git commit; root caught the mismatch from the returned
record before writing either zero-byte partial. The smallest robust repair is
an opt-in then default-on `--require-git-commit` gate that resolves the basis
in the repository, requires the canonical full object ID to match exactly,
and leaves no lease/partial on failure. Tests: accept exact HEAD; reject a
well-formed nonexistent hash and an abbreviated/mismatched hash; prove zero
filesystem residue; preserve an explicit non-Git mode only for tested
temporary fixtures. Measure prevention of bad-basis leases without changing
seal or manifest semantics.

## 5. Ranked launch portfolio

1. Run Card 1's q=6 shared-contact/root-system proof and falsifier while the
   q=8 hostile review proceeds. Do not wait for review to explore reversibly.
2. Start the bounded `d=3` adjoint/conductor discriminator independently; it
   attacks the presentation ceiling rather than another q=6 descendant.
3. Specify one K00 transition-image map and send only its reviewed heavy
   elimination to AWS.
4. Implement the basis-validation systems gate after focused tests, without
   blocking mathematics.
5. Keep Avenue 2 selector/QCS work alive; no fixed-block closure is allowed to
   masquerade as the missing global arrow.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `12942`.
- Body SHA-256:
  `eb595c1192d88e1e56cdcfc7e59026ec562c331171058d706032e6bc163f35d2`.
- Frozen basis: `dcd6cf9ccce35e7deee956245ca46d2dfd00d5d2`.
