# Blind ideation — 20260905T0200Z — Astra (GPT-6, PRIMARY)

Status: COMPLETE BLIND SUBMISSION / INTERNAL-UNREVIEWED. This is the blind submission for the sealed off-cycle round. The four charged frozen inputs passed a mechanically generated `sha256sum -c` manifest from this lane's `.run.v2` receipt. The packet was read before campaign reports. Other submissions for this round and both running-lane drafts are excluded.

This report was assembled in bounded writes and sealed only after completion. New work is unreviewed; the recommendations preserve the source/lifecycle distinctions below.

## 1. Decisions first, and disposition changes

**The packet overstates both the finite residual and the all-degree compression.** The first correction helps: two of its fourteen excess rows already die under the promoted Xu screen, leaving **twelve rows in six full characteristic-data classes**, before consuming anything from the running Opus lane. The second correction costs: the actual sharpened u_s>=2 cohort compresses **296 -> 132**, not approximately 65, on the proposed four-integer key. That key also merges different inherited characteristic sequences. A kill can still be shared through a proved common necessary receiver, but equality of four integers is not that proof.

**K16 is one precise atom on one prototype. It is not the unique structural gap of the programme.** Neither a finite census nor one cofinal family supplies an all-degree covering theorem. The single most valuable next object is a **certificate-bearing receiver atlas**: an explicit source-to-receiver map for every branch being charged, with an honest UNASSIGNED output. Its first finite test is already available: the sharpened 296-row cohort, its 132 coarse keys, and its higher-depth collisions. This should precede a large class sweep. The finite Moh closure can continue alongside it.

The K16 proposal below changes the mechanism, from an undifferentiated polynomial-solution classification to a **linked quartic factorization with a differential constraint**. It is exploratory, with exact identity checks; it is not another assertion that a discriminant, Hilbert series, or dimension count kills a chart. The global polynomial truncation remains essential.

All 46 numbered avenues in APPROACHES.md were considered. The following are the changes; **every omitted number is `unchanged`**. Labels apply to the specified client, not to an unsourced general theorem bearing the same name.

| Avenue | Disposition | Change and reason |
|---|---|---|
| 1 — Newton/monomial-Jacobian receivers | **raise** | Move construction and transfer of necessary receiver equations ahead of a raw class farm. The finite Moh residue is now twelve, but all twelve have descended depth greater than two. The 296-row audit exhibits coarse-key collisions before any split geometry. This is a concrete correctness and amortization problem for the main receiver engine. |
| 2 — boundary/tree route | **raise** | Give total routing and receiver coverage an explicit proof allocation. First-separation and route-to-state cannot be collapsed into integer invariants or a finite list of successful arrivals. This is a separate interface obligation from Moh's already-sourced split/radius alternative. |
| 29 — differential/Hamiltonian equations | **raise** | Use the actual linked coefficients of F3 via the quartic product identity in Q1. Do not claim local nilpotence, invoke a generic Abel solution-count bound, or infer polynomial nonexistence from formal recursions. |
| 36 — guided falsification | **raise** | Prioritize falsifying the proposed class equivalence and chart coverage, then a fully covered tau!=0 fibre. Those are cheap potential failures of this proof programme. A nonzero descended solution or a modular point is still not a Keller counterexample. |
| 38 — valuations/tropical methods | **reopen, narrowly** | Root valuations of the explicit linked quartic and its differential factor are a new, coefficient-sensitive client. Use the full collision locus and leading-coefficient branches. Tropical prevarieties, generic squarefreeness and valuations of arbitrary same-degree equations are insufficient. |

Proof allocation: **finite Moh closure**, **receiver coverage**, **K16 truncation**, then an **all-degree covering/termination theorem**. This is an expenditure order, not an implication chain. Global reduction interfaces have greater logical scope than K16. Falsification allocation: break a claimed source-to-receiver map first; seek an exact normalized B*eta!=0 point second; only an actual polynomial Keller lift supplies a disproof. A modular or necessary-chart survivor does not. Avenues 19/21 retain their bounded-support obligation; no fresh reason was found to restart stopped implication ladders, bare passports or ordinary exact-form residues.

Evidence language in this report: **BANKED** means consumed at the exact lifecycle and scope of the cited ledger; **MEASURED-EXACT** means fresh rational/integer desk computation, same-model and unreviewed; **DERIVED-HERE** is a displayed algebraic derivation; **OPEN** is a mathematical obligation; **PROPOSED** is a scheduling or instrument choice. Same-model helpers contributed independent calculations and adversarial reads, not different-model promotion votes.

## 2. Q1 — attack differential-compatible factors of a linked quartic

**Answer: use the linked coefficients to turn the Abel atom into a factor-selection problem over a fixed quartic cover.** The new bounded object is a degree-four polynomial depending on one dimensionless scalar, not an arbitrary degree-growing Abel equation. This does not yet prove rigidity. It identifies exactly where a root-cover argument must do more than count degrees, and exposes exceptional scalar fibres that a generic argument must retain.

The input is the banked producer equivalence in `xmodel/k16-8point1-astra-20260905.md:294–470`, with the scope corrections at `:75–136`, and AUDIT 17(hhhhhh). Work factorwise over an algebraic closure of

    A_t = Q[y]/H_t(y),  q=2t+1,  3d^2=t+1,
    H_t=12q^2 y^2-12q(t+1)y+(t+1)(3t+2),
    y=(d+t+1)/(2q),  t>=3.

Here y is a scalar unit. In the Abel notation, C is monic of degree t-1, W has degree 2t+1, W(0)=-B, eta=W'(0), and

    lead(W)=omega=3(2d-1)/(4y^2(4t+1)).

A failure of (8.1) is exactly a normalized polynomial solution of F1–F3 with B*eta!=0. The b=0 branch remains part of the target. For this section K(x) is a polynomial, not the original gcd parameter K_orig=16 or the descended gcd K'. Primes below are literal derivatives with respect to x.

### 2.1 A polynomial identity, with no root divisions

Set

    Z=xW-b^2/4,       K=x^2 C-yb,
    L=-Bx+3K(K+yb)/(2y^2),
    H=2xZ'-3Z+L,
    Psi=3K^3(K+2yb)/(16y^4)-3BxK^2/(4y^2)
         -b*eta*x^2*K/(2y)-B*eta*x^3.

**DERIVED-HERE:** F3 is equivalent to

    Z H = Psi.                                             (Q1.1)

This is a polynomial identity, including b=0 and repeated roots. The only scalar denominators are units already declared. Under the normalized leading balance,

    deg Z = deg H = 2t+2,        deg Psi = 4t+4,
    Z(0)=-b^2/4,  Z'(0)=-B,  [x^2]Z=eta.

The leading coefficient equation is the already-satisfied normalizer equation

    (4t+1)omega^2 + 3omega/(2y^2) - 3/(16y^4)=0.

It is a consistency check, not a contradiction. The new structural requirement is that a half-degree factor of the *specific* Psi must have its cofactor equal to the prescribed differential expression H.

On b*B*eta!=0, reduction modulo K gives

    Z(2xZ'-3Z-Bx) = -B*eta*x^3 mod K.              (Q1.2)

Because K(0)=-yb!=0, x is a unit modulo K. Thus Z is a unit in the full Artin algebra k[x]/(K), even when K has repeated roots. Also Psi(0)=-3b^4/16!=0, and no root of Psi is a root of K. These are useful exact unit statements. They do not force the ideal to be inconsistent; the old discriminant/pivot fallacy would begin precisely at that inference.

### 2.2 The fixed quartic and its exceptional fibres

For b*B*eta!=0 put

    lambda=b^2*eta/B^2,  chi=Bx/b^2,
    u=K/(yx),  v=b*u/B,  w=1/v.

At a root of Psi, all divisions in this display are legal by (Q1.2). The root equation becomes

    chi = Phi_lambda(w),
    Phi_lambda(w)=(16lambda/3)w^4+(8lambda/3)w^3+4w^2-2w.  (Q1.3)

Indeed, 16Psi/x^3 is the linked quartic

    3x u^4+6b u^3-12B u^2-8b*eta*u-16B*eta.

The substitution w=r-1/8 gives

    Phi_lambda(r-1/8)
      =(16lambda/3)r^4+(4-lambda/2)r^2
        +(lambda/12-3)r+5/16-lambda/256.             (Q1.4)

This is the promised coefficient-sensitive compression. The critical-point discriminant has nonzero-constant multiple

    lambda*(3lambda^2-69lambda-32),

and, on lambda!=0, the discriminant of the **monic cubic whose roots are the three critical values** is exactly

    27*(lambda-36)^2*(3lambda^2-69lambda-32)^3/(2^28*lambda^5).

Since lambda!=0 here, the cover has three exceptional scalar values: lambda=36 and the two roots of 3lambda^2-69lambda-32. At lambda=36 the centered quartic is exactly

    192r^4-14r^2+11/64,

a composition of two quadratics. Its finite critical values are -1/12 and 11/64. These computations stratify the **quartic cover's ramification**, not every tangency of its intersection with the graph determined by C. Repeated roots of Psi can still occur at generic lambda, and repeated roots of C are never removed.

This is a genuine new connection: the polynomial differential equation of avenue 29 becomes a constrained divisor on a degree-four cover, making the ramification methods of avenue 25 and valuation methods of avenue 38 coefficient-sensitive. It is not a revival of the bare K16 Belyi passport, which the banked report already showed cannot distinguish its exceptional fibres.

### 2.3 What would force rigidity, and the cheapest discriminator

Let n_Z=2t+2. If Z is squarefree, Z divides Psi, and the leading normalization holds, (Q1.1) is equivalent to the single remainder condition

    R_Z = NF_Z(2x(Z')^2+L Z'-Psi') = 0,   deg R_Z<n_Z.       (Q1.5)

Here NF_Z is literal polynomial remainder in the declared field[x], with omega inverted as a scalar unit. To prove the converse, put D=Psi/Z-H. Differentiation of Psi=Z(Psi/Z) shows that (Q1.5) implies Z divides Z'D. Squarefreeness gives Z divides D. The leading balance gives deg D<n_Z, so D=0. At a root a of multiplicity m>1, the exact replacement is **all m quotient jets** of D at a; (Q1.5) alone retains only part of them. The collision-inclusive formulation is D=0 in k[x]/(Z), again with deg D<n_Z. A prospective proof must work in that local algebra or retain the corresponding Hasse derivatives.

The strongest proof attack is therefore: show that **no normalized half-degree divisor of this linked Psi can satisfy its differential cofactor conditions when B*eta!=0**, using the fixed quartic's ramification and the constrained graph w=Byx/(bK). The bounded obstruction is the n_Z-dimensional remainder vector, augmented by the full multiplicity jets when needed. The target is zero admissible divisors for every t>=3 and each normalizer factor. This remains OPEN. The cover stratification is the new mechanism; it is not a proof that the target vector is nonzero.

The strongest falsification attack starts on the **compositional fibre lambda=36**, rather than treating a generic ramification calculation as universal. In the original solved-spine coordinates it is the scale-invariant equation

    b3^2*(3R/y)-36*b2^2=0,
    R=U'(b4)+b3*C(b4),

together with I_{t,+} and a Rabinowitsch equation retaining b3*b2*R!=0. The first unknown indices worth a bounded pilot are t=8 and the rational normalizer factors at t=11, d=+/-2, y=7/23 or 5/23. A solution must be reconstructed in F1–F3 and checked exactly over characteristic zero. A modular unit or nonempty modular fibre decides no characteristic-zero statement by itself. The b3=0 chart is separate and remains indispensable; lambda is not a coordinate there.

**Cheapest tests:** the symbolic identities and controls were checked in seconds. In particular ZH-Psi=x^2*(F3_LHS-F3_RHS), with no x-localization. After the present K16 lane seals and is harvested, cap a first exact lambda=36 pilot at t=8 at **eight minutes / <4 GB**, using the solved-spine emitter and guided_gb. A finite-fibre unit is not an all-t theorem. Stop on timeout or an uncovered vanished leader. The mutated-coefficient and repeated-root controls below are mandatory tests of any proposed global invariant.

For all t, b=0 retains the banked factorization at `k16-8point1-astra-20260905.md:525–535`; it cannot be inferred from a lambda limit with B*eta inverted. The two roots of 3lambda^2-69lambda-32 and generic-lambda graph tangencies must likewise survive the chart inventory. No real ordering of roots is assumed: the coefficients and solutions live over algebraic closures.

**History:** the banked report already supplies Abel equivalence, the finite free ordered-root cover, the boundary-collision length 6t^2 and the b=0 factorization. Its :506–530 proves formal nonzero B*eta solutions exist to arbitrary origin-jet order. The novelty here is the all-b product, fixed quartic, exceptional lambda strata and differential-compatible divisor criterion. The stopped degree-only and bare-passport mechanisms remain stopped.

A further **DERIVED-HERE negative result** makes the test honest on b!=0 too. For arbitrary formal C, and units b,B,eta,y, write Z=-b^2/4-Bx+eta*x^2+a3*x^3+.... The product residual vanishes identically through x^3; for n>=4 its coefficient of a_n is -(b^2/2)(n-3). Thus a3 is free and every later a_n is recursively determined. Formal nonzero B*eta solutions exist on the generic lambda chart as well as the exceptional ones. This supplies no polynomial truncation and no normalized top coefficient. It rules out a proposed proof whose only content is local compatibility or a claim that the equation forces lambda into the exceptional locus.

Exact scripts and controls are in `box/ideation-astra-20260905/abel_adversary/` and `abelcheck/`. The literal-F3 identity and discriminants were recomputed in two separate same-model scripts. One control at t=11, d=2, y=7/23 uses C=x^10 and Z=(529/980)x^24+x^2-x-1/4. Psi_mut=ZH has the same degree 48, leading coefficient, and first four jets as true Psi, but their x^4 coefficients differ by 1. This is the explicit reason a degree/passport argument is inadequate. It is not a K16 witness.

## 3. Q2 — twelve live rows, six characteristic towers, one finite theorem

**Answer: the advertised fourteen are a conservative tree-only workload, not the exact live remainder after all promoted screens.** Two were already killed in AUDIT 17(eee), before this round. The fresh computation consumes that promoted theorem; it does not invent a stronger Xu inequality.

The exact replay starts with 658 path-arithmetic rows at n<=100. The frozen operative tree screen leaves twenty: six printed rows and fourteen excess rows, all with u_s=1. This matches every banked key. Applying the **original promoted Xu screen** to those fourteen kills exactly

    (n,m)=(90,60), M2..M4=(45,80,88),
    V=(2,5,4) or V=(3,5,4),
    IM_max=43/7 < Im_min=7.

The two bounds cannot hold for the same actual full tree. No equality/attainment is inferred from a lower bound. Source: `AUDIT.md:17145–17155`, `xmodel/xu-screen-gate-fable5-20260903.md:169–182,218–230`, and the exact historical gate rows in `box/xuscreen-gate-20260903/results.json:10711,10749`. The source assumptions are a monic Jacobian pair, the two-point setting, generic xi, Xu Corollary 5.3, and the promoted operative full-tree model containing the actual tree. The n/m dictionary is n=deg g, m=deg f. The historical driver and both imported enumeration modules were mechanically hash-checked against the gate receipt, 3/3. The later principal-floor strengthening is not needed.

Consequently the remaining ancestor multiplicities are **(90,60)x2, (96,64)x4, (96,72)x6**. The appropriate finite table is below. M' includes M1'=-m'. An inherited V entry is still ancestor metadata until the chart theorem assigns its descended geometric meaning.

| Descended (n',m';ell) | Full inherited M' | d' | Live rows | Auxiliary pi-degree tower |
|---|---|---|---:|---|
| (16,12;3) | (-12,6,13) | (16,4,2,1) | 3 | (16,12,30,53) |
| (18,12;2) | (-12,2,9) | (18,6,2,1) | 2 | (18,12,22,59) |
| (24,16;1) | (-16,-12,-2,5) | (24,8,4,2,1) | 1 | (24,16,44,78,149) |
| (24,16;1) | (-16,12,17) | (24,8,4,1) | 3 | (24,16,20,35) |
| (24,18;1) | (-18,-15,14) | (24,6,3,1) | 1 | (24,18,69,109) |
| (24,18;1) | (-18,9,20) | (24,6,3,1) | 2 | (24,18,45,79) |

These are **four coarse degree/Jacobian bins, six characteristic towers, twelve V-labelled rows**. Eleven descendants have s'=3 and one has s'=4. None is an s'=2 K16/Appendix-II receiver. The terminal anchors n'-M'_last-1 are respectively 2,8,18,6,9,3, all positive; an old zero-anchor repair is not the issue. The auxiliary degrees follow Moh's characteristic recurrence, not a new size estimate: their maximum is 149. Pair degree at most 24 therefore does not imply a small full-tower coefficient system.

The arithmetic transformations are n'=n*u_s/d_s, m'=m*u_s/d_s, inherited M_i'=M_i*u_s/d_s, and ell=V_s-u_s-1. At u_s=1, Moh Prop. 6.4 makes the radius automatic. This is the strongest common theorem presently available across the twelve. It does not by itself make the twelve coefficient ideals empty.

**Only residuals?** Yes as a finite remaining row list within the frozen induction audit, after composing sourced enumeration, tree necessity, existing printed-row kills, the two Xu citations and condition-(3) (99,66) closure. The source-to-coefficient maps for these twelve longer towers are still owed. The degree-decreasing induction, not the isolated (99,66) citation, handles simultaneously reducible pairs.

**Right sweep?** Batch by full characteristic tower and a proved common necessary superset, retaining V fallbacks. The old two-level emitter is explicitly scoped to s'=2 (`appendix2-compiler-grok46-20260903.md:62–73,158–163,377–418`); four matching degree/Jacobian triples do not license its support. There is no uniform killing inequality proved here. In particular M2'<m' on the twelve does not revive the previously refuted raw M2>m predicate. A uniform kill must consume the additional characteristic hypotheses and actual coefficient identities.

**Cheapest tests and price.** Enumeration took under one second/about 19 MB; the Xu replay about 0.1 seconds. Next buy one full-tower coverage pass, six common class emitters and at most twelve V fallbacks. Start with (16,12;3), but include s'=4 (24,16;1) in the first smoke test. Branch on vanished leaders; prove support before using a residual-size gate to schedule CAS.

Allocate **two producer lane-days plus independent replay/assembly**, with each CAS attempt capped at 540 seconds and <4 GB. This prices emission and triage, not guaranteed closure; a firm estimate before the higher-depth pilot is unsupported. Exact unit certificates with total source coverage close rows. Oversized emitters, surviving ideals and timeouts return their precise obstruction. The running Opus lane owns this work, and the two obsolete Xu-killed charts should receive no compute.

## 4. Q3 — compression is an optimization to prove, not an H1 theorem

**Answer: a mechanized census is useful, but the packet assigns the descended-class compression to the wrong half of the dichotomy and extrapolates the wrong cohort.** The previous Opus submission explicitly records that equality of descended data does not imply equality of joint charts, and that a class kill discharges only `DEAD[DESCENT-HALF]` (`xmodel/ideation-20260905T0000Z-opus5.md:359–365`). Its existing `OPEN[DESCENT-CLASS-SPLIT-HALF]` at :626 already raises the transfer problem. This is a known omitted qualification, not a newly discovered theorem.

H1 is the genuine split/minor-empty obligation. Conditional descent deals with the complementary radius-licensed branch. A common descended ideal can optimize the latter; it does not automatically supply H1's branch-specific augmented row. The seven screen kills at (99,66) and the split certificates show exactly why source-specific cheaper mechanisms should run before descent charts. Nothing here reopens those promoted deaths.

**MEASURED-EXACT:** the correct computation on the banked sharpened candidate cohort is:

| Quantity | Actual count |
|---|---:|
| Source rows in the selected cohort | 296 |
| Four-integer keys (n',m',ell,V2') | **132** |
| Triples (n',m',ell) | 52 |
| Fully decorated records retaining inherited M', V, height | 296 |
| Four-integer keys containing different inherited decorations | 43 |
| Conditional descendant heights 2 / 3 / 4 | **19 / 88 / 189** |
| Distinct coprime degree ratios | 9 |
| Rows with ratio 3:2 | 177 |

Thus the relevant arithmetic ratios are **296/132=2.2424...** and **296/52=5.6923...**. The prior 6209->1359=4.5688... computation concerns a different, raw census. Dividing 296 by that ratio to price approximately 65 charts is contradicted by the exact key count. The wider 310-row POLY/ODE cohort gives 140 quadruple keys and 53 triples.

Custody matters here: the selected cohort is the auxiliary banked `box/xufloor-20260903/results.json`, mechanically hash-verified against `box/h1nonres-20260903/summary.json`. The upstream report's qualification at `xmodel/h1-nonres-census-sol56-20260903.md:52–81` remains: this is a measured candidate cohort, not a fresh promoted universal census. The replay preserves every original membership key and both cohort definitions in `box/ideation-astra-20260905/cohort/cohort-compression.json`. No realizability of an arithmetic row is asserted.

### 4.1 A two-row falsifier of the proposed class key

Take source degree (126,84), V=(5,10,5), and respectively

    M2..M4=(-14,63,124),
    M2..M4=(112,119,124).

Both map arithmetically to (n',m',ell,V2')=(36,24,2,5). But their full inherited sequences, including M1'=-24, are

    (-24,-4,18),    (-24,32,34).

Their terminal anchors are 17 and 1; under the licensed descent their terminal radii are -3/17 and -3. The projection forgets even the order filtration. An emitter that silently emits the same ordinary support from that key has erased necessary source data.

This **does not imply 296 separate Gröbner solves**. It proves that compression needs an additional map. A large common necessary superset can be enough: if every actual descendant maps into it and its ideal is unit, its larger solution set causes no soundness problem. It may even produce greater compression than 132. Conversely, a surviving common superset can require finer branches. The number of distinct decorations is not a lower bound on the number of certificates needed.

For the same reason, the older difference 6209-1359=4850 is not a theorem that at least 4850 split jobs remain. It counts repetitions of an arithmetic projection. Screens can kill fibres, and a future split-transfer theorem can share them. Every charged ancestor owes a split disposition; that is bookkeeping coverage, not a lower bound on distinct computations.

### 4.2 The immediate arrow and the all-degree work

On the chosen Moh route, the binding routing gap is **receiver coverage**: actual source pair -> licensed descendant -> some exact necessary coefficient ideal that the engine kills. Its payload includes the full effective characteristic tree, correct order/support bounds, every coefficient row, normalization charts, and denominator/vanished-leader branches. Ordinary-support completeness belongs inside this arrow. A uniform K16 identity does not repair it for higher-depth descendants.

The Sigray first-separation and route-to-state interfaces remain distinct campaign obligations. In particular, `ladder/REDUCTION.md:1122–1187` requires arrival data, offsets, unused branches and continuation provenance, not merely a scalar state or a successful sample route. A direct Moh proof need not solve every Sigray interface. The packet's generic phrase “routing maps” should be replaced by the exact arrow used by each proposed theorem.

The all-degree path is therefore **conditional**: enumerate a bounded cohort to test the maps; build a receiver atlas and per-source split/radius dispositions; prove coefficient-level emptiness for each atlas family; then prove every remaining parameter family is covered or descends by a well-founded measure. A growing sweep is an instrument and a counterexample detector. It is not a terminating proof until that last covering/reduction theorem exists.

**Cheapest decisive test:** replay the two (126,84) rows and ask the emitter for its source map, filtration, coefficient ring and complete row inventory. This takes seconds for metadata; allow **90 minutes of human/agent construction and at most eight minutes of exact CAS** for a first shared necessary receiver or an explicit scope refusal. Pass means two verified source maps into one proved necessary ideal; fail means a named omitted hypothesis, not an unexplained timeout. Before any census campaign, count covered source branches, not merely emitted keys. The finite target is 296/296 source records with explicit branch dispositions and zero silently dropped states; it is not 296 immediate deaths.

## 5. Q4 — the valid dichotomy is wider than the current receivers

**Answer: retain H1/H2 as a sourced split-versus-descent programme, but reject “census plus K16-type rays” as an exhaustive reduction presently proved.** The missing cases are already visible in the finite cohort: higher-height descendants and different degree-ratio directions. The missing theorem is total coverage, not another name for the K16 atom.

Do not manufacture a new radius gap. The banked `prop63-radius-gate-opus5-20260905.md:139–179` rederives the alternative from Moh's proof: either delta*>=v_s/u_s, licensing Prop. 6.3, or a genuine split occurs in [1,v_s/u_s), with deg p dividing u_s and 2<=q<=deg p<=u_s; the denominator bound is at most u_s. For u_s=1 the split alternative is impossible. Thus “unsplit but radius fails” is not an extra case to reopen. What is missing is a receiver theorem for **every descendant in the licensed alternative**, not just the current two-level normal forms.

The lattice also needs its omitted coordinates. Write

    (n',m')=(a K',b K'),  gcd(a,b)=1.

The (3K',2K') lattice is the a:b=3:2 slice. For the K16 ray itself,

    (n',m')=(12t+4,8t+4),   K'=gcd(n',m')=4,
    (a,b)=(3t+1,2t+1),     ell=1,
    2n'-3m'=-4.

So **not one K16 descendant lies on the 3:2 slice**, and the entire K16 ray would collapse to the single point (K',ell)=(4,1) if its coprime ratio were forgotten. Original K_orig=16 and descended K'=4 are different objects. The two-dimensional lattice is a projection, just as ell=4 was a slice. This identity is a one-line decisive test of any claimed universal parameterization.

A safe universal record therefore retains, or supplies a theorem allowing it to forget,

    (a,b,K',ell; effective characteristic tree;
     support, branch, normalization and localization data).

The candidate atlas can still have elegant shared families. But “cofinal” only says some degrees tend to infinity. It does not say their complement is finite, nor that every remaining ratio, depth and split type lies on one of those families. A finite cover by theorem schemes, or a terminating reduction into them, is the missing finish object. The analogous warning against finite-range completeness is explicit in `ladder/REDUCTION.md:1243–1261`.

Retain U-NEGATIVE (V2'>d2') as a coverage branch until sourced impossible or assigned a receiver. Do not repeat the old count twenty as current: the later snapshot has zero among its forty-eight height-two u_s=1 descendants. Higher height is directly present: its 1,110 u_s=1 records have heights 2/3/4/5 in counts 48/330/596/136. Neither census fact proves an all-degree statement or realization.

**The single most valuable next object is a source-to-receiver atlas with certificate pullbacks.** For a source chart ring R_alpha, its necessary ideal I_alpha, a receiver ring S_beta with ideal J_beta, and a declared localization, provide a literal homomorphism

    phi_alpha_beta : S_beta -> R_alpha[1/d_alpha]
    with phi_alpha_beta(J_beta) contained in I_alpha R_alpha[1/d_alpha].

If 1=sum a_i f_i in J_beta, applying phi transfers that exact certificate. There is no need to prove full scheme isomorphism between ancestors, and no permission to infer phi from matching variable names. Every source must be covered by at least one chart; the d_alpha=0 loci require their own branches or a proved exclusion. Generator order, scalar field factors, constants and nonzero conditions must be recorded. A compiler should return an explicit UNASSIGNED state whenever the requisite source theorem is absent.

This connects avenue 1 to the explicit ideal-certificate discipline developed in avenue 32, without importing the Keller secant lemma into a monomial-Jacobian ring where its hypotheses may fail. The transferable item is the checked polynomial identity and its ring map. The strongest bypass is to identify a certificate whose used hypotheses lie in a common necessary subideal, thereby avoiding a proof that the entire ancestor charts coincide.

**Bounded quantity and cheapest test:** first decide the number of the 132 coarse keys with a verified shared receiver, and retain 296 source records with complete branch statuses. Pilot on the explicit collided key (36,24,2,5), one established closed-row control, and the s'=4 finite Moh class. Ninety minutes should buy either three honest interface records or a precise first missing theorem. It should not buy a claim of all-degree reduction. This is the first new lane I would launch.

## 6. Three idea cards

### Card A — NEW: differential-compatible quartic divisors

**Fingerprint/dependencies:** K16 rigidity / linked quartic and differential factor / Phi_lambda / E36 exact test; charges the banked F3 equivalence and scalar normalizer, not a height theorem. **Discriminator:** the exact identity/control suite passes; after harvesting the present lane, spend at most eight minutes/<4 GB on E36 at the first useful unclosed index. Retain EDelta, its generic complement, b=0 and collision jets in eventual coverage.

**Outcomes:** an exact unit excludes only that fibre/index; an exact B*eta!=0 point must reconstruct in F3 before refuting the atom; a control failure refutes the proposed mechanism; timeout supplies only cost information. **Stop:** degree, generic passport, bare factorization or squarefree residues alone. **Information gain:** whether the fixed quartic supplies a global truncation restriction, and which exceptional geometry it must handle. This is a proof/falsification experiment, not extrapolation from finite fibres.

### Card B — KNOWN TARGET, CORRECTED WORKLOAD: six Moh towers

**Fingerprint/dependencies:** full <=100 induction / source-first descent and common necessary ideals / twelve ancestors, six towers / higher-depth emitter; charges promoted tree/Xu screens, Prop. 6.4, printed-row kills and condition-(3) closure. **Discriminator:** 658->20->14->12 is replayed; next emit the smallest (16,12;3) tower and the s'=4 (24,16;1) coverage control. Budget six common emitters, at most twelve V fallbacks, <9 minutes/<4 GB per CAS attempt. The existing Opus lane owns this.

**Outcomes:** a covered common unit kills mapped members; a too-small support kills no omitted source; a surviving superset needs finer V charts; emitter refusal identifies the missing theorem. **Stop:** using an s'=2 template or guessed M2 inequality. **Information gain:** a finite induction dossier or a precise higher-depth obstruction list. The two Xu citation kills require no new chart computation.

### Card C — KNOWN INTERFACE, NEW MEASUREMENT: certificate-pullback atlas

**Fingerprint/dependencies:** safe batching / necessary-ideal maps / 296 sources, 132 keys, 43 decorated collisions / explicit (126,84) pair; charges source-qualified descent, full support and exact ideal identities. **Discriminator:** ninety-minute pilot on the collided key, one closed control and the s'=4 Moh class; at most one eight-minute CAS check. Output ring maps, denominator branches, used rows and coverage status; a proved common necessary superset counts as success.

**Outcomes:** verified maps license sharing; a filtration mismatch splits a key or enlarges the receiver; UNASSIGNED preserves the source while exposing missing mathematics; an invalid pullback rejects the certificate. **Stop:** any silently dropped branch or unproved replacement of a source by scalar state. **Information gain:** actual reusable work and the first cofinal-cover theorem needed. This is the strongest cheap falsifier of the coordinator's universal framing.

## 7. The single first new lane, and the two named lane decisions

**FIRST NEW LANE: RECEIVER-ATLAS-PILOT, 90 minutes.** Run Card C. Its acceptance object is three audited interfaces: the collided (36,24,2,5) class with both ancestors, an established closed-row control, and the s'=4 finite Moh class. Each interface gives either a checked common necessary receiver with source maps or a typed missing support/normalization theorem. Count source records and retained branch obligations. Do not launch a 132-key farm until this pilot can distinguish a shared key from a justified shared certificate. This lane runs alongside the finite closure work; it does not reopen (99,66) or wait for all-t K16.

The following are **receipt-only recommendations**. At initial inspection both receipts had initial_status=RUNNING and no final_status. Later receipt changes are post-packet operational events, not frozen mathematical inputs:

| Lane | Last receipt observed | Decision |
|---|---|---|
| moh100-14rows-opus5-20260905 | DONE; BODY_SEALED; end 2026-09-05T02:14:51Z | **CONTINUE the finite objective; harvest the completed run first.** Amend its review contract to twelve live rows/six full towers plus two promoted Xu citations. Do not commission overlapping computation until the completed mathematical payload is read after blind collection. No verdict about its kills is inferred here. |
| k16-f3abel-astra-20260905 | DONE; BODY_SEALED_AFTER_DIVERT; end 2026-09-05T02:16:46Z | **REDESIGN the next continuation around an explicit global truncation invariant.** First harvest the completed/diverted body. Compare its result with Card A before any new launch. A coefficient-sensitive solution theorem remains a valid objective; another generic Abel or local-jet reformulation is not sufficient continuation evidence. The receipt alone says nothing about success, failure, or overlap with the quartic object. |

Neither protected report, its computational output, nor a same-round submission was consumed as mathematical input. The final collision scan excludes both protected lane prefixes even though their receipts have since completed. These recommendations assess the packet's objectives and the observed receipt states; they are not a review of unread work.

## 8. One campaign-systems upgrade — collision provenance labels

**UPGRADE, bounded prototype:** attach receipt lifecycle and corpus membership to OPEN-collision candidates, while excluding prompt metadata and explicitly receipt-only lane prefixes. This is separate from the mathematical emitter proposal. The stock guard correctly excluded same-round submissions and then-running reports at first inspection, but its test is only presence of final_status. The metadata-only audit found 3,967 candidate paths, including **17 explicitly PARTIAL_NO_MARKER reports** (14 with final_status=DONE), 50 sealed-after-divert reports, and many legacy/unrecorded entries. These are different evidence contexts; none should silently acquire the same interpretation because it matched a keyword. A diverted report may contain sound mathematics, so diversion is a label, not a rejection.

The prototype `corpus_status_audit.py` reads receipt metadata, not report bodies, and completed in under one second. `blind_collision_scan.py` invokes the **unmodified ops/open_collision.py engine** with additional exclusions for `.prompt.md` and both protected lane prefixes, even after completion. It banks exact membership and receipt hashes in `collision-corpus.json`; the mathematical reports remain untouched. The initial stock-corpus lexical search was refined to exclude prompt metadata before the final history/collision scan. No protected in-progress report supplied a finding.

**Smallest useful acceptance test:** zero included same-round submissions, zero included protected lane-prefix files, explicit legacy/divert/partial labels, and unchanged matching semantics on the retained corpus. The first two are executable assertions in the wrapper; the metadata audit demonstrates the label need. Promotion still requires reading the matched mathematical source. Adopt the annotations and explicit corpus exclusions after a separate systems review; this submission changes no canonical tool or operating policy.

## OPENs raised or sharpened

- **OPEN[K16-QUARTIC-DIVISOR]** — NEW. QUANTITY: decide whether the number of normalized differential-compatible degree-(2t+2) divisors of the linked degree-(4t+4) quartic numerator equals 0 for every t>=3 with B*eta!=0. CHEAPEST TEST: exact symbolic controls already pass in seconds; next E36 fibre, original-spine emitter plus exact guided_gb, eight minutes/<4 GB, with b=0 and collision strata retained as separate obligations.
- **OPEN[DESCENT-CLASS-SPLIT-HALF]** — EXISTING, SHARPENED. QUANTITY: bound verified common receiver coverage among 132 coarse descended keys and 296 source records; every source retains a split/descent disposition, and no arithmetic compression alone counts as a kill. CHEAPEST TEST: the two (126,84) sources mapping to (36,24,2,5), exact support and ring-map audit, ninety-minute pilot with at most eight minutes CAS.
- **OPEN[MOH-PROGRAM-ARTIFACT]** — EXISTING, SHARPENED. QUANTITY: reduce the live twelve excess rows in six characteristic towers to 0 after the two promoted Xu citations; retain complete higher-depth receiver coverage. CHEAPEST TEST: subsecond tree/Xu replay completed, then the two full-tower emitter controls in Q2, one source pass before <9-minute/<4 GB exact jobs.

The global all-degree covering theorem remains the existing receiver/routing obligation, not a fourth newly coined OPEN. The guarded history scan found the old descent-half qualification and the old Xu deletions; they are **KNOWN corrections to the packet**. Its quartic/Abel keyword hits concerned unrelated quartic boundary topology. That lexical NO HIT does not certify originality; the specific formulas and discriminators are the novelty claimed here.

## Completion checks

The four frozen input hashes were verified mechanically at intake and rechecked unchanged. The Xu source manifest (3/3), Moh artifacts (11/11), and cohort manifest (3/3) pass. Exact algebra/control logs pass; no large CAS job ran. No new exit-price assertion is made; `validate_charge_basis.py` returns ABSENT. Canonical ledgers and jc2-lean were untouched.

The following is the full guarded scanner output for all three declared OPENs. Hits are review candidates. The post-packet 17(jjjjj) hit concerns the protected completed Moh lane and is parked, not consumed in the blind recommendations. Older hits are interpreted through their superseding audit scopes; NONE for the quartic is only a lexical result.

## COLLISIONS

status: CANDIDATES

- `OPEN[K16-QUARTIC-DIVISOR]` (report:307): NONE

### OPEN[DESCENT-CLASS-SPLIT-HALF]

- `AUDIT.md:17766` — ## INTEGRATION #17 DELTA (eeeeee) (2026-09-05T00:37Z, round 20260905T0000Z synthesis, five sealed blind submissions): (1) N1 CLOSABLE BY CITATION (Fable Q2, gate pending): replaying the promoted whole-tree operative screen on the eight (...

### OPEN[MOH-PROGRAM-ARTIFACT]

- `AUDIT.md:17760` — ## INTEGRATION #17 DELTA (bbbbbb) (2026-09-04T21:47Z, PROVED-HERE exact, producer Sol; MILESTONE — the last two k=4-ray census rows CLOSED, so D=108 is CLOSED at skeleton level and the (99,66) skeleton verdict is COMPLETE for all three c...
- `AUDIT.md:17762` — ## INTEGRATION #17 DELTA (cccccc) (2026-09-04T22:22Z, PARTIAL, producer GPT-5.5; N1 enumeration CONFIRMED and reduced to a finite list of 6 skeleton kills): the frozen Moh enumerator CONFIRMS exactly EIGHT (1)–(13)-admissible characteris...
- `AUDIT.md:17764` — ## INTEGRATION #17 DELTA (dddddd) (2026-09-04T23:42Z, PARTIAL + ERRATUM, producer Sol; N1 tally corrected to 3 dead / 5 open, and an exponent error in 17(cccccc)'s S6 kill is caught and fixed): S5 is DEAD (UNIT_IDEAL_CHAR0, exact-Q; comp...
- `AUDIT.md:17766` — ## INTEGRATION #17 DELTA (eeeeee) (2026-09-05T00:37Z, round 20260905T0000Z synthesis, five sealed blind submissions): (1) N1 CLOSABLE BY CITATION (Fable Q2, gate pending): replaying the promoted whole-tree operative screen on the eight (...
- `AUDIT.md:17768` — ## INTEGRATION #17 DELTA (ffffff) (2026-09-05T00:55Z, PROMOTED, hostile gate Grok on 17(eeeeee)(A); THE PUBLISHED OPEN (99,66) CASE IS CLOSED DEGREE-WIDE): N1 GATE PASSED — DEGREE-WIDE (99,66) NO-KELLER-PAIR CLOSED BY CITATION. The charg...
- `AUDIT.md:17772` — ## INTEGRATION #17 DELTA (hhhhhh) (2026-09-05T01:36Z, PARTIAL, producer Astra (gpt-6-astra, debut) on OPEN[K16-8.1-RADICAL]): the K16 atom is reformulated and sharpened, not closed. PROVED (uniform): the boundary and coordinate lemmas an...
- `AUDIT.md:17776` — ## INTEGRATION #17 DELTA (jjjjjj) (2026-09-05T02:16Z, PARTIAL + structural characterization, producer Opus; the Moh ≤100 residual is now a precisely-characterized PARALLEL compute task): 0 of 14 excess rows killed (compute-bound: the s'≥...
- `notes.md:20777` — ## 2026-09-03T11:15Z EVENT — CRITICAL: MOH-PROGRAM second reader sealed (Sol, 46KB): WHOLE-MAJOR-TREE necessity recovered from Props 4.6/5.3/5.6 + p.200 — every above-threshold factor at every node must extend; C_FULL_TREE cuts 658 → 60 ...
- `notes.md:20779` — ## 2026-09-03T11:16Z EVENT — D105-RANK-GATE sealed (Sol, 38KB; box/d105rank-drivers-20260903/): honest REFUSAL — the charged inputs (census chain + capacity + the rigid (2,3,1) star's relative leading coefficients) do NOT determine the f...
- `notes.md:20785` — ## 2026-09-03T11:44Z EVENTS — whole-tree Grok gate CONFIRMED → delta 17(r) PROMOTED (frontier: D = 105 and 117 empty on the screened census; both fixed-N rays dead; D = 108 first screened survivor); NEXT-COEFF/PS review → delta 17(s). DC...
- `notes.md:21106` — ## 2026-09-05T00:55Z EVENT — `n1-screen-gate-grok46` PASSED: DEGREE-WIDE (99,66) CLOSED BY CITATION. All 8 admissible skeletons dead (7 by the promoted whole-tree screen — S1/S4/S5/S6 at partition level, S2/S3/S7 by ungated Prop 5.6; S8 ...
- `xmodel/ideation-20260903T1200Z-grok46.md:457` — | queued P202 residue through sibling system | **redesign** | `n≤100` TREE excess is 54 rows of OPEN[MOH-PROGRAM-ARTIFACT]; pass through `descend_once` first. Most `s≥4` rows become `s'=3` `(T)` instances, not original-pair Gröbner. |
- `xmodel/ideation-20260903T1200Z-grok46.md:737` — test: replay Moh's two minor-disc distributions on the `D=108` row.
- `xmodel/ideation-20260904T1200Z-packet.md:9` — - Necessity dossier THEOREM 8.1 (conditional), 17(hhhhh): chart-is-a-bijection (k^7161), forced top forms, outer counts, pivot closed form |coeff| = 9(99−3n−11i) all CONFIRMED from frozen sources. The two NEW gauge gaps N3/N4 DISCHARGED ...
- `xmodel/ideation-20260904T1200Z-packet.md:11` — - QUALIFICATION (17(jjjjj), 17(nnnnn)): the UNSPLIT configuration (A) = (27,18; δ₁=0) is NOT killed — its 17(tt) "empty" was the δ₁=0 −c INSTRUMENT-FAIL artifact (17(ggggg)); now OPEN[FULL-BASIS-TIMEOUT]-class. So "no Keller pair with th...
- `xmodel/ideation-20260905T0000Z-fable5.md:726` — - `xmodel/ideation-20260903T1200Z-fable5.md:75` — **1.1 The 52 excess rows.** `full-tree-ode-excess-witnesses.json` (52 rows = 58 − Moh's six):
- `xmodel/ideation-20260905T0000Z-synthesis.md:7` — **(A) N1 is closable by CITATION, not by five joint-chart kills (Fable Q2).** Replaying the charged whole-tree screen driver (box/centre-gate-20260903/rerun_screens.py, imports unmodified) on the eight (1)–(13)-admissible (99,66) skeleto...
- `xmodel/ideation-20260905T0200Z-packet.md:7` — HEADLINE (today): the PUBLISHED OPEN (99,66) CASE of Moh's ≤100 induction (condition (3), non-simultaneously-reducible — the case Xu 2016 called "open") is CLOSED DEGREE-WIDE (17(ffffff), Grok gate): all 8 (1)–(13)-admissible skeletons d...

<!-- BODY-END -->
