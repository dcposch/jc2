# Hostile review: candidate direct T-a0 cubic certificate

Work in `/Users/dc/code/math/jc2` as Opus 5, an independent hostile reviewer.
Do not edit canonical files, case inputs, or `jc2-lean`.  Your sole write is

`xmodel/max12-812-order2-p0-total-rees-j2-a0-cubic-certificate-hostile-review-opus5-20260827.md`.

V22R1 is promoted; V23R1 is a producer-tier specialization census.  V24 has
just returned exact-Q rational-function lifts in

`cases/max12_812_order2_p0_total_rees_j2_rational_function_screen_v24_20260827/`.

Do not trust its PASS, lift, compiler, producer reasoning, or the identity
below.  Rehash the V23R1 result and literal `output_r1/a0_chart` polynomial
bytes, independently parse the needed full rows, and check chart typing from
the reviewed staged calculus.

Candidate identity, with `q=qa1`:

```text
a0^3*(1+3*q^2*rho^2)
 = (6*ell1*cs1-8*rho^2*cs2)*Tg11_1
   -2*rs2*Tg11_2
   -6*rho^2*cs1*Tg12_1
   -2*rs1*Tg12_2
   -4*cs1*Tg12_3
   -16*Tg15_6.
```

Audit independently:

1. coefficientwise exact-Q residual and at least one finite-field replay;
2. every cited row's literal source/hash provenance and that no unlisted
   divided row or rational-function cofactor remains;
3. the `T-a0` chart substitution `J1=(rs,cs,c0,c1)=0`,
   `a1=a0*qa1`, with only `a0` exceptional;
4. that `1+3*qa1^2*rho^2 = 1+rho*W` with polynomial
   `W=3*qa1^2*rho`, and that no forbidden localization is hidden;
5. whether exceptional saturation legitimately closes the whole registered
   chart at the finite-prefix/source scope, and precisely what remains before
   any Gate-T/order-two claim;
6. V24 custody, controls, `degBound` warnings, and whether the
   rational-function discovery path creates any logical dependency after the
   displayed polynomial identity is independently verified.

Try coefficient/sign mutations so the replay is demonstrably discriminating.
Return CONFIRMED, REFUTED, or GAP, with repairs and the strongest exact theorem
safe to promote.  Do only light exact parsing; no AWS mutation, no heavy local
CAS, no web.
