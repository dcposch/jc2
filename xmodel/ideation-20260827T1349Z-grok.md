# Blind significant-news whole-portfolio ideation — Grok — `20260827T1349Z`

Status: **FROZEN BLIND SUBMISSION**. Independent of every other
`xmodel/ideation-20260827T1349Z-*` file except the sealed packet and this
lane's prompt. No canonical ledger, case, producer, or review artifact was
edited. No AWS job was launched. No `jc2-lean` inspection. No heavy local
CAS.

- Lane: xAI/Grok whole-portfolio researcher
- Sealed packet: `xmodel/ideation-20260827T1349Z-packet.md`
- Packet SHA-256 (verified on disk):
  `8af21da712a3de8184ca708d75e7461e831f597aac459317d8871176e73b48c3`
- Git basis: `418e413593120d19e15e6546eb50c985f4b1f038`

Evidence labels:

- **THEOREM** — different-model confirmed / freeze-promoted statement
- **DISCOVERY** — exact mathematics whose independent result review is
  live or whose validator is not yet fail-closed
- **NAVIGATION** — ranking or routing, not a theorem
- **PROVISIONAL** — exact but unreviewed reasoning in this report
- **SPECULATION** — testable guess

No proof or counterexample of JC2 exists at this freeze. Matysiak SSRN
7229458 is **not** a theorem. The C5 identity `a1^628` is a named
provisional premise with a rollback subtree. Model identity is not a
vote.

---

## 0. Custody

Required canonical hashes, verified on disk against the packet:

| File | SHA-256 |
|---|---|
| `APPROACHES.md` | `1112706cd6182d23e360a6e76e5555fcd54264c6470155157f3f7ce42366482b` |
| `AUDIT.md` | `fca661f1c1c041b2a8648d2b6e5bf5f4834d3e44004bada23e5182ca79b08fbc` |
| `PROGRESS.md` | `7ba7ef761d9ee3022eb861daab798d1980efa4fcc881d1675b6ddc0d1c4db40e` |
| `COORDINATION.md` | `039bcdabd9959066b74141262acf109bd9ece326580e56c81f24493c8336c76a` |
| `notes.md` | `d2ace1aeb635711daa6f4eaca77644ee65bf5861157509f8c1dff83e93db8105` |
| `xmodel/ideation-20260827T1104Z-synthesis-sol.md` | `df8c920f019f1d99053af6d81f4eee52ad88c23d2c8e7bc513782ea06efe0eba` |
| `xmodel/websweep-20260827T1348Z.md` | `a8a1ae4f888a73a7db7ea9d639088c8cdb3c480600e67104955bdd4b535ac5c8` |

Live state consumed: `notes.md` block `2026-08-27 13:49Z`. No other
`ideation-20260827T1349Z-*` submission, prompt, or log was opened.

History checksum before any novelty claim (do not re-propose): origin
funnel; two-fibre decision; selected-row one-sidedness; constructive
converter `q a1^(D+M s)` and exponent `4+6M=628`; N-duals as
non-decision; `t`-adic V43-to-K00 Newton cone (`SCOPE-CONFLICT` in the
`1104Z` synthesis); individual K10/K6/K2 classes killing the unloaded
germ (`REFUTED READING`); specialized `t=c` as a *refutation of a
reviewed* `5 t^6 a1^4` identity; certificate-sized pullback renamed
`G2-PSC`; unique-`AC` Hensel degeneration into the terminal (Grok
`1104Z` M2); rho-only deformation-row support census (Grok `1104Z` M1);
AS109 top-two-band / W2/W3; Lee–Li row-9; primitive-group sidecar as
critical path; Bernstein–Sato idle probe; stage-zero Kummer composition;
serial Cramer to grade 19 on K00; R3 as an original-`8_28` exclusion;
Fitting stratification as a *slogan already in the 11:56Z overlay*
without a new stop-Cramer discriminator.

---

## 1. Disposition vector over every numbered avenue

Relative to the live `APPROACHES.md` master table plus the 11:56Z overlay
and this packet's delta. `U` unchanged, `R` raise, `L` lower, `O` reopen.
Reasons only for non-`U`. Previous-round raises that this delta does not
strengthen are not re-voted.

| ID | Call | Reason if not `U` |
|--:|:---:|---|
| 1 | R | R1+R2 now make the `8_28` local carrier a complete two-type dichotomy, including arbitrary higher-`u` sidecars (**THEOREM**). The remaining global object is finite: the 7-dimensional cokernel of `M(Y)=4HY'+6H'Y` (Card B). This is the first time that face has a well-posed, certificate-sized global obstruction space. R3 does **not** raise this row: it excludes only an artificial squarefree replacement edge. |
| 2 | R | Same finite packet fields (Galois-stable `V_8`-type, 7-vector in `coker(M)`) are the missing *inputs* to `G2-PSC`, not `G2-PSC` itself. Keep `G2-PSC` and `G2-BD` distinct. V47R1/V48 compress unique-`AC` bookkeeping only; ramified/`rho=0`/equality/positive-load/off-family remain the coverage wall. Raise is for a now-named finite transport defect, not for a tree functor. |
| 3 | U | Strip/ODE/`(8,28)` subcase-(1) y-axis block unchanged. Vertex-gap is not the C5 converter. |
| 4 | R | V22 leaves a proper homogeneous affine dimension-three closed prefix on `F10=0`. V24's modular zero forms are vacuous, so a finite compatible jet has **not** been shown empty or nonempty over `Q`. If the Fitting remainder of Card A is nonempty, that remainder is now the unique on-family formal-germ/algebraization object. Still not a polynomial Keller map. Do not infer an arc from a finite jet. |
| 5 | U | Amalgam rigidity still does not bite non-automorphisms. |
| 6 | U | Residue-A fibres remain the wrong one-place object. |
| 7 | R | Matysiak's advertised dimension argument, *if* it is anything, lives on nonproperness/`A(F)`. Independently of his theorem, the Jedrzejewicz–Zieliński / de Bondt–Yan square-free preservation law supplies a finite `w(f,g)` test that this campaign has never run (Card C). Raise is for that testable connection, not for treating SSRN 7229458 as progress. |
| 8 | U | No new truncation bound. |
| 9 | U | Conjecture E remains an unbounded implication ladder. Idle-capacity only. |
| 10 | U | HC4 quintic module remains `NO LEVERAGE`. |
| 11 | U | Zhao/Mathieu ladder remains dead. |
| 12 | U | No new integer-to-infinity. Face-isolation is adjacent to Card C but not cheaper than the named `8_28`/`AS109` tests. |
| 13 | U | DC(2) still strictly stronger than JC2. |
| 14 | U | No candidate non-surjective `A_1` endomorphism. |
| 15 | U | Burchnall–Chaundy dictionary still incomplete. |
| 16 | U | No named holonomic index. |
| 17 | U | Cubic-linear class in high dimension still contains genuine CEs. |
| 18 | U | Graded CE hunting stays closed (Shaska). |
| 19 | U | Low-Witt `v_109(beta)=1` Cartier remains a one-stratum cut (**THEOREM**). Residual `n=6` is still the cheapest char-p CE handle. Do not lower further; do not raise without a `v>=2` mechanism. Stop top-two-band/W2/W3 stands. |
| 20 | U | Tsuchimoto/p-curvature still consumes false PC(2)/JC(4). |
| 21 | U | Hensel noninjectivity of the seed is unchanged. |
| 22 | U | Siegel still points the wrong way. |
| 23 | U | Holomorphic analogue remains false. |
| 24 | U | Pinchuk maps still have nonconstant `J`. |
| 25 | U | Residue-A passports remain too generous. |
| 26 | U | Primitive-group `td` bound still has no proved support bound. No-contention sidecar only. |
| 27 | U | Admissible splice diagrams still outrun algebraic realizability. |
| 28 | U | Dual-pencil defect remains `JUMP-ONLY / TYPE-FAIL`. |
| 29 | U | Ordinary `kappa(P)` still has no universal receiver. |
| 30 | U | ML invariant still does not see the embedding. |
| 31 | R | C5, if promoted, is a displayed integrality/`a1`-nilpotency identity in one chart, not ZMT. Separately, Matysiak's image-subring step will be an integrality/UFD/algebraic-closedness claim if it is anything; that is this row's audit target. Two independent reasons, neither of which presently closes the avenue. |
| 32 | R | The advertised “classical dimension argument” is almost certainly an injectivity/`k[x,y]/k[f,g]` dimension count. Raise as the *audit and falsification* seat, not as a proof engine. Cynk–Rusek already makes injective ⇒ automorphism in char 0; circular use of injectivity is the expected failure mode. |
| 33 | U | Ordinary action residues remain `COSTUME`. |
| 34 | U | No spare coordinate to absorb a 2D pole. |
| 35 | U | Dim-3 mechanisms remain structurally 3-dimensional. |
| 36 | U | Do not reopen ambient SAT/sparse search because a chart is provisionally closed. Named AS109/`8_28` tests live on rows 19 and 1. |
| 37 | U | Finite-field extras will not lift. |
| 38 | U | No tropical invariant is named. |
| 39 | U | `coker(M)` is a face-level class, not a 10-line class attached to `J=1`. Keep it on rows 1–2. |
| 40 | U | Commutative `det=1` still does not give free invertibility. |
| 41 | U | Naive scaling remains falsified by the dim-3 CE. |
| 42 | U | Arrow still points the wrong way. |
| 43 | U | Ritt still collapses to GGV after Aut-reduction. |
| 44 | U | Moskowicz remains `REFUTED-AS-PROOF`. The Jedrzejewicz–Zieliński square-factorial package cited in that review is Card C's *literature source*, not a reason to reopen this row. |
| 45 | U | Inverse PDE remains tautological. `M` as a differential operator is row 1, not Kovacic. |
| 46 | U | Formalization cannot manufacture the finite universe. `jc2-lean` stays outside campaign custody. |

Changed: `1, 2, 4, 7, 31, 32` raised. Nothing lowered or reopened.

### Current global gaps (after the delta, not before it)

Still missing, and **not** supplied by a provisional `a1^628` or by an
unaudited SSRN abstract:

1. A promoted total identity `a1^N in J_total` for the frozen ordered
   `T-a1` chart (C5 is the candidate; review is live).
2. Terminal/Taylor receiver emptiness, equivalently closure-first `H_K00`.
3. Honest source/landing coverage outside strict unique-`AC` on `D(rho)`.
4. Ramified `rho=0` deck/square fibres, equality/positive-load faces,
   `k=0`, off-family timing.
5. Global `G2-PSC` (packet-to-decorated-tree) and any invoked `G2-BD`.
6. Cofinal degree/type ceiling.
7. An explicit characteristic-zero counterexample: residual AS109
   `deg_y Q=6`, unbounded-total `(8,12)`/`(9,12)`, a mixed-load
   algebraized K00 germ, or a similarly exact TD6 survivor.
8. Lawful primary-text audit of Matysiak 7229458/7229358.

C5 PASS would discharge (1) at frozen grade-through-19 scope and
collapse on-family landing onto (2)+(3)+(4). It would not discharge
(5)–(8).

---

## 2. Reranked bottlenecks

For an actual resolution of JC2, after this packet:

1. **Source/landing, `G2-PSC`, `G2-BD`, cofinality.** Still the global
   wall. New this round: the `8_28` local theory is complete through
   weight 22, so the missing global step is no longer “find the
   carrier.” It is: serialize raw `2S/3S` provenance, glue decks, and
   land in `im(M)` (Card B). One named-face compiler is not `G2-PSC`.
2. **Terminal receiver / mixed K00.** V20R2 is the compiler; V21 three
   separate local classes; V22 excludes only `D(F10)` and leaves a
   proper dimension-three cone. Successive Cramer (V24) is the wrong
   next engine: its modular normal forms were vacuous. Replace by
   representation-independent Fitting on that cone (Card A), then
   honest `Lambda<=19` reachability and `H_K00`.
3. **Ramified / equality / off-family coverage.** V47R1/V48 do not
   touch these. Do not let V43/K00 density eat this budget.
4. **Finite chart completion.** Ordered `T-a1` via C5 is the shortest
   high-confidence *local* win. It is not the global theorem. G5
   circuit/pole minimizer may shrink 628; it cannot close the receiver.
5. **Counterexample realization.** Residual AS109 `n=6` content
   `v_109(beta)>=2`; Fitting-supported K00 remainder if nonempty, then
   algebraization; order-two square/discriminant survivors; capped TD6.
   Matysiak is intelligence, not a CE or a proof.

Immediate wall-clock: finish C5 hostile review in parallel with Card A
and Card B. Do not wait on C5 to start Fitting or the `coker(M)`
compiler. Every C5 descendant names `DEP-C5` and the rollback in §8.

---

## 3. New mechanism and new cross-avenue connection

### 3.1 New mechanism — `coker(M) ≅ Q[X]_{deg≤6}` is the complete global `H`-multiple obstruction

**Label: NEW.** History check: R1 proves `M(Y)=1` has no polynomial
solution inside two named ansatzes, and records the local remainder
`M(X/48)=1+(13/12)H`. R2 classifies rootwise carriers as `6V_8 U_14`
or `(9/2)U_11^2` and pins one mixed etale example `V_8=X^4+1`. R3
turns the *same* ODE into a rational-mode exclusion of the artificial
edge `F_0=H^2`, `G_0=H^3`. Repository search for `coker(M)` hits the
TD6 `P12` module and unrelated Euler operators, not this operator on
`Q[X]`. No artifact states that `M: Q[X]→Q[X]` is injective with
7-dimensional cokernel complemented by `Q[X]_{deg≤6}`.

Let `H=X^8-1` and `M(Y)=4HY'+6H'Y`. Leading coefficient
`(4d+48)lc(Y)` never vanishes, so `M` is injective. The lowest image
degree is 7, and for every truncation `V_n → V_{n+7}` the cokernel has
dimension 7. Hence

```text
Q[X] = im(M) ⊕ Q[X]_{deg ≤ 6}.
```

Any global weight-22 scalar `E_22 ∈ Q[X]` has a unique remainder
`r(E_22)` of degree at most 6, and `E_22 ∈ im(M)` iff `r(E_22)=0`.
The unit target `1` has `r(1)=1≠0`, which is R1. The class of `H`
itself is also nonzero in `coker(M)`: `M(aX+b)=52a X^8 + 48b X^7 - 4a`
cannot equal `X^8-1`. So “control the global `H`-multiple” is not an
open-ended polynomial hunt. It is vanishing of a 7-vector.

R3's squarefree replacement is the *wrong* degeneration for original
`8_28`. The integrating factor of `M` is `H^{-1/2}`; making `H` a
square makes `sqrt(H)` polynomial and changes type, which is the
already-reviewed order-two first-normal gate `K|N^2`. Making `H` more
squarefree (R3) is the opposite degeneration and cannot exclude the
original edge.

### 3.2 New connection — square-free image-subring law (avenues 7, 31, 32, 1) independent of Matysiak's theorem

**Label: NEW as a campaign test; KNOWN as literature.**
Jedrzejewicz–Zieliński (arXiv:1611.07439, 1601.01508) and de Bondt–Yan:
for a characteristic-zero endomorphism `φ` of `k[x,y]`,

```text
jac(φ) ∈ k\{0}
  ⇔  φ maps irreducibles to square-free polynomials
  ⇔  φ maps square-free polynomials to square-free polynomials
  ⇔  Irr(k[f,g]) ⊂ Sqf(k[x,y]).
```

JC2 is then: every such `φ` is an automorphism. The campaign cites
Jedrzejewicz only as upstream of Moskowicz (`REFUTED-AS-PROOF`). It has
never run a finite `w(f,g)` square-freeness test on a named GGV pair or
an AS109 lift.

This is the honest independent content of Matysiak's `5''s` slogan. If
`5''s` is equivalent to the displayed law, it is Keller in costume and
the “dimension argument” must still prove automorphism; expected
circularities are injectivity, factorial closure of `k[f,g]`,
UFD/normality of the image, and `dim k[x,y]/k[f,g]=0`. If `5''s` is
strictly stronger than Keller, it may not hold for all Keller maps and
cannot be a JC2 proof without a separate implication Keller ⇒ `5''s`.

Cross-avenue: on `8_28`, `φ(w)=w(f,g)` for square-free `w` of low
degree is a discriminant in the deformation parameter `τ` of the
reviewed non-Keller control `g_τ`. That discriminant is a packet field
adjacent to, and distinct from, Jac and pole mass. On AS109, the same
law forbids a non-square-free linear pencil, which is a cheap filter on
residual `n=6` cores, orthogonal to the low-Witt Cartier cut.

### 3.3 Answer to the packet's K00 question

Do **not** continue successive constructible Cramer strata as the main
engine. V24's two compatibility polynomials have 110,117 and 83,298
terms; over `F_65521` the localized prior-plus-`F10` ideal is the unit
ideal, so those zeros were vacuous. That is a software and a
mathematical warning: inverting V23's minor `W` can empty the chart
for a bad-prime or genuine reason, and Cramer cannot see `W=0`.

**Replace** the Cramer presentation by Fitting of the grade-seven
newest-variable map on the closed cone `L=(Q1,...,Q6,F10)`,
fraction-free over `Q`. The degeneracy locus of that map (including
`W=0`) is the only representation-independent closed remainder.

**Connect** to terminal incidence as follows, not by a V43 chain map.
`F10` is a homogeneous quartic, the `Q_i` are homogeneous, so `0∈V(L)`
automatically. The origin of `x`-space is the *higher-valuation leak*
`v(d)>1`, not a valuation-one point. Genuine val-1 remainder is
`Proj V(L)`. `H_K00` asks whether the receiver origin in the original
`d`-coordinates lies on the Fitting-supported prolongation, after
honest source weights. A finite compatible jet still does not exclude
an arc.

---

## 4. Strongest proof attack and strongest falsification attack

**Proof.** After C5, the shortest honest path is: (i) promote or roll
back `a1^628` at frozen ordered-`T-a1` scope only; (ii) empty the
K00 val-1 mixed prefix by Fitting of the jet maps on `L`, then
`H_K00`; (iii) on the named `8_28` face, compile raw `2S/3S` jets into
`coker(M)` and demand the 7-vector vanish, with Galois-stable branch
type as a packet field. (ii) and (iii) do not wait on (i). None of
these is Gate T, a cofinal ceiling, or JC2.

**Falsification.** Residual AS109 `n=6` on the complementary content
strata `v_109(beta)>=2` remains the cheapest polynomial-CE search with
a theorem-level filter. Second: if Card A's Fitting remainder is
nonempty over `Q`, try to algebraize that mixed germ (avenue 4) with
bounded conductor — a formal survivor is not a Keller map until
support, algebraization, collision, and Jacobian replay. Third: order-two
square/discriminant components that still lift. Do **not** treat
Matysiak as a CE or as a proof. Do **not** infer a CE from a nonempty
finite K00 jet.

---

## 5. Software / AWS acceleration and correctness firewall

**Job.** Fraction-free Fitting of the honest grade-seven newest-variable
block of V20R2, restricted to the cone `L=(Q1,...,Q6,F10)`, over exact
`Q`. Emit `Fitt_k` of that map, in particular the degeneracy ideal
cutting rank `<5` (the `W=0` locus) and the obstruction module whose
Cramer presentation on `D(W)` is V24's `(C6,C7)`. One AWS core, modest
RAM; this is smaller than contracting 226-term Cramer minors into
110k-term polynomials. Do **not** run heavy Singular locally.

**Firewall, all must hold or FAIL_CLOSED:**

1. Reconstruct the grade-seven matrix from the frozen V20R2 DAG, not
   from V24's contracted polynomials. Contraction-before-specialization
   remains mandatory.
2. On `D(W)`, the Fitting-supported scheme must agree set-theoretically
   with `V(C6,C7)` after saturating at `k10_0 W`. A mismatch is a
   representation error, not a new stratum.
3. `F10` remains nonzero modulo `(Q1,...,Q6)` (V22 **THEOREM**). If
   Fitting “empties” `L` by dropping `F10`, the job is invalid.
4. The `F_65521` unit-ideal diagnostic is a mutation control, not
   evidence: if `Fitt` is `(1)` over `Q` on `D(W)`, that *explains* the
   vacuous modular forms; if not, the modular localization inverted an
   extra element and must not be quoted as emptiness.
5. Homogeneity: generators of `L` are homogeneous; Fitting must be too.
   The cone point `x=0` is the higher-valuation leak, not a val-1 witness.
6. Scope sentence, frozen: this decides the grade-seven Fitting-supported
   remainder of the val-1, `kappa≠0` prefix, conditional on V20/V21/V22.
   It does not decide grades 8–19, other valuations, an arc, `H_K00`,
   order two, maximum twelve, or JC2.

V24's live exact-Q properness job is a *chart* test on `D(W)`. This
job includes `W=0` and replaces Cramer as the prolongation language.
If the chart job already returns empty over `Q`, this job's first
output is the degeneracy locus, not a second Cramer identity.

---

## 6. Idea cards (three)

### Card A — K00 Fitting remainder, not Cramer
**(proof-side, PROVISIONAL; redesign of the V23/V24 lane)**

- **Dependencies.** V20R2 compiler (**THEOREM**); V21 three separate
  local classes (**THEOREM**); V22 `D(F10)` exclusion and proper
  homogeneous `dim L=3` (**THEOREM**); V23 generic rank five
  (provisional input); V24 Cramer as a `D(W)` *control*, not as a
  source. Premise `DEP-V24` is **not** required.
- **Cheapest discriminator.** Exact-Q Fitting of the grade-seven
  newest-variable map on `L` (§5). Three exclusive outcomes: unit on
  `D(W)` and unit at `W=0`; unit on `D(W)` but proper degeneracy
  locus; proper remainder on `D(W)`.
- **PASS, empty on `D(W)` and at `W=0`.** This val-1 `kappa≠0` prefix
  dies at grade seven. Next is other coefficient valuations or
  `H_K00` at the actual receiver, not grade eight Cramer.
- **PASS, empty on `D(W)`, proper `W=0`.** The remainder is the
  degeneracy locus. Continue Fitting there; do not invert a zero minor.
- **PASS, proper on `D(W)`.** V24's `C6=C7=0` was a genuine cut after
  all; replace those 110k-term generators by Fitting generators and
  compute dimension. Still not an arc.
- **FAIL (scope/ring/F10 dropped/disagreement with V24 on `D(W)`).**
  Bank the failure. Keep the live chart-properness job. Do not
  interpret FAIL as nonempty.
- **Stop.** One Fitting generation at grade seven. At most one cheap
  grade-eight Fitting if grade seven is proper and the matrix stays
  small. No Cramer to grade 19. No inference of arc exclusion.
- **Rollback subtree.** Any descendant that treated V24's modular zeros
  as exact-Q membership; any “grade-seven compatible jet exists”
  slogan that used those zeros. V22's nonempty cone statement stands
  until Fitting over `Q` actually empties it.
- **Expected information gain.** Either kills the first mixed closed
  prefix or produces the first representation-independent nonempty
  remainder, which is the honest K00 object. High.

### Card B — `coker(M)` 7-vector as the `8_28` global landing test
**(proof-side, PROVISIONAL; GGV/`G2-PSC` input, not `G2-PSC`)**

- **Dependencies.** R1 raw/local type wall (**THEOREM**); R2 two-carrier
  completeness through weight 22 (**THEOREM**); the reviewed non-Keller
  `8_28` control pair. R3 is **not** a dependency (wrong edge).
- **Cheapest discriminator.** For each Galois-stable vanishing pattern
  of `V_8` on the four `Q`-irreducible factors
  `Φ_1 Φ_2 Φ_4 Φ_8` of `H=X^8-1` (16 types, not 256 unordered
  colourings), interpolate the R2 local units, reduce the resulting
  global `E_22` into `Q[X]_{deg≤6}` by subtracting an `M(Y)`, and test
  whether the 7-vector is zero. Desk-to-tiny-AWS exact linear algebra
  plus the already-written R2 verifier's mixed-factor fixture.
- **PASS, some type has 7-vector zero.** That type is the only legal
  global polynomial landing of the local theory. Next: a source-pinned
  raw `2S/3S` compiler that realises that type, or a proof that raw
  jets cannot. Still not a face exclusion and not `G2-PSC`.
- **PASS, all 16 types have nonzero 7-vector, for every polynomial
  interpolant of degree `<8`.** Then no low-degree global `Y` works,
  matching R1, now uniformly in branch type. Higher-degree `Y` cannot
  hit a constant (leading-coefficient law). Remaining loophole is only
  non-polynomial cleanup mixing raw jets into local `U,V` *before* the
  Morse chart — the provenance compiler, now with a zero target in
  `coker(M)` rather than an open-ended `H`-multiple.
- **FAIL (missed Galois orbit, treated R3's replacement edge as
  original `8_28`, identified a formal Morse chart with a global
  automorphism).** Fail closed. Do not exclude the face.
- **Stop.** 16 types, degree-`<8` interpolants, one 7-vector each. Do
  not enumerate 256 unordered root colourings. Do not call this
  `G2-PSC` or `G2-BD`.
- **Rollback.** None of R1/R2 is at risk. R3 descendants that claim
  original-`8_28` exclusion remain unlicensed.
- **Expected information gain.** Turns “control the global `H`-multiple”
  into a finite vanishing condition and names the exact remaining
  loophole. High for avenue 1; medium for `G2-PSC` (supplies a packet
  field, not the functor).

### Card C — Finite square-free image test, plus Matysiak audit tree
**(intelligence + disproof-side necessary condition, PROVISIONAL)**

- **Dependencies.** De Bondt–Yan / Jedrzejewicz–Zieliński equivalence
  above (literature, not a campaign theorem). Reviewed `8_28` control
  `f=B^2-x-y^8`, `g_τ=B^3-2x^2 y^2+y^{12}+τ x y^{15}`. AS109 residual
  `n=6` seed. SSRN 7229458/7229358 **unacquired**.
- **Cheapest discriminator, independent of primary text.** For the
  control pair at `τ=1`, search square-free `w` of total degree `≤4`
  such that `w(f,g)` is not square-free. Record the lowest-degree
  witness. Repeat along `τ`. Compare the vanishing locus of that
  discriminant with `Jac(f,g_τ)=0` and with the reviewed pole-mass
  jump `τ:1→2`.
- **PASS, square-free failure is strictly coarser or independent of
  Jac vanishing.** Then square-free preservation sees a packet field
  that the coarse GGV ledger and Jac do not. Keep it as a Keller-face
  filter. It still does not exclude `8_28` Keller lifts.
- **PASS, square-free failure is exactly the non-Keller locus.** Then
  the finite fragment is Keller in costume on this family (as the
  literature equivalence predicts). Useful negative control; stop the
  *search*, keep the law as an audit checklist for Matysiak.
- **FAIL (computes on a non-square-free `w`, or treats the control pair
  as Keller).** Discard.
- **Stop.** Degree `≤4` on one named pair, plus a one-page AS109 pencil
  filter. No ambient search. No claim about SSRN 7229458.
- **Matysiak branch (acquisition).** Fastest lawful path: interactive
  SSRN session for *both* 7229358 then 7229458; else author/institution
  mirror or direct contact (Matysiak's prior square-free papers are
  with Jedrzejewicz–Zieliński). Hash bytes. No scrape-through-403, no
  shadow library.
- **Matysiak branch (audit, only after bytes).** (1) Write the exact
  definition of `5''s` from 7229358 and the implication Keller ⇒ `5''s`
  or the converse. (2) Compare with the displayed de Bondt–Yan
  equivalence; if identical, the JC2 paper must still prove
  automorphism. (3) Isolate the first genuinely dimension-two step and
  the dimension count. (4) Flag circular injectivity, factorial
  closure, image UFD/normality, algebraic closedness of `k[f,g]` in
  `k[x,y]`. (5) Map any surviving lemma into rows 7/31/32 and rerank
  only then.
- **Rollback.** None of the internal exact lanes. If the paper is
  acquired and the first dim-2 step is circular, classify
  `REFUTED-AS-PROOF` analogously to Moskowicz and keep Card C's finite
  test only.
- **Expected information gain.** Acquisition: high for competitive
  posture, zero for internal certificates until audit. Finite test:
  medium, cheap, and the only Matysiak-adjacent mathematics that does
  not wait on Cloudflare.

---

## 7. Continue / redesign / stop for current major lanes

| Lane | Call | Exact stop |
|---|---|---|
| V43C5 V2 total `a1^628` | **continue as `DEP-C5`**; hostile review live | Promote only the frozen-chart identity. Rollback §8. Do not start N7. |
| V43G5 eleven-row / pole minimizer | **continue**, one compression job | May lower 628; cannot close receiver. Stop if C5 is `REFUTED`. |
| V43 C1/C2 uncorrected cascade | **stop** | Already withdrawn (**THEOREM**). |
| Selected-row generic, N-duals, unseeded N7 | **stop** | Existence on `D(a1)` is two-fibre; C5 is the displayed identity. |
| K00 D9+ pure-coefficient | **stop permanently** | V14R1 local membership. |
| K00 V20R2 compiler | **continue as source authority** | No endpoint. |
| K00 V21 three local classes | **continue as separate classes** | Do not merge by slogan. Do not kill the unloaded germ with them. |
| K00 V22 closed prefix | **continue as the cone `L`** | Do not infer emptiness from modular V24. |
| K00 V23/V24 Cramer prolongation | **redesign → Card A Fitting** | Stop serial Cramer. Keep V24 as `D(W)` control under review. |
| Exact-Q properness of V24 chart | **continue** (already the packet's next gate) | Chart-only; Card A includes `W=0`. |
| V43-to-K00 chain map / `t`-adic cone | **stop** | `t=-1/4` is a K00 unit; no row map. Failure to build the map is a result. |
| GGV non-Keller `8_28` fidelity control | **continue as packet-insufficiency theorem** | Not Keller, not `G2-PSC`. |
| GGV R1/R2 | **continue**; Card B is the successor | Do not identify Morse charts with source automorphisms. |
| GGV R3 | **continue review at stated scope only** | Artificial squarefree replacement edge. Do not apply to original `8_28`. Stop further replacement-edge clones. |
| Raw-to-`(U,V,W)` provenance compiler | **continue**, targeted at `coker(M)=0` | This is the remaining `8_28` wall. |
| V47R1 / V48 | **continue as bounded compilers** | No new endpoint, no fan cover, `(8,3,8)` unpromoted. |
| Unique-`AC` endpoint manifests | **continue as slots free** | Equality/ramified/`k=0` are not V47. |
| Equality / positive-load / ramified `rho=0` / off-family | **continue**, do not starve | Coverage wall. |
| `H_K00` closure-first | **continue**, after or in parallel with Card A | Restriction-first is a negative control. |
| AS109 `n=6` three-way content split | **continue** | Only where Cartier can cut. No W2/W3, no top band, no floor-seven slogan. |
| AS109 `n=2..5` and prime targets | **stop** | History-closed. |
| TD6 H19R2 / registered caps | **continue** | Independent SP-2/td6 gap. No new atlas, no whole-TD6 claim. |
| Serial TD6 `q` / serial affine-Faber `H` | **stop** | Prior stop stands. |
| Order-two square/discriminant component recursion | **continue** | Do not collapse into V43/K00. R3 is not this degeneration; the square degeneration of `H` is. |
| Formal-germ algebraization | **continue only on a Fitting-supported nonempty remainder** | Modular nonempty ≠ germ ≠ char-0 ≠ polynomial. |
| Primitive-group census | **continue as no-contention sidecar** | No AWS algebra quota. |
| Lee–Li / Bernstein–Sato | **hold**, idle only | Unchanged. |
| Ambient random Keller search / Jelonek-as-object | **stop** | Card C is a named finite test, not a search. |
| Stage-zero Kummer composition | **stop** | REFUTED. |
| `jc2-lean` | **do not touch** | User-owned, outside custody. |
| Matysiak 7229458/7229358 | **launch acquisition + Card C**; do not divert C5/K00/`8_28` | Classify `ACTIONABLE-UNAUDITED` until primary-text audit. |
| Broad web sweep | **unchanged clock** | Next quiet deadline remains the campaign's `2026-08-28T00:00Z` unless a primary-text event fires. |

### Allocation until the next decisive event

```text
28%  K00 Fitting remainder, honest Lambda<=19 reachability, H_K00
22%  8_28 raw-to-coker(M) compiler, G2-PSC/G2-BD kept distinct, cofinal ceiling
12%  V43C5 review + G5 compression (shrink; no exponent ladder)
15%  ramified / equality / positive-load / off-family coverage
13%  CE: AS109 n=6 content, order-two survivors, capped TD6, germ algebraization
10%  Matysiak lawful acquisition/audit, Card C finite test, integration
```

Review stays nonblocking. `DEP-C5` descendants at most two, one
unreviewed generation, named rollback. A C5 PASS that matches the
packet's complete identity and scope is a coalesced lifecycle event,
not a reseal.

---

## 8. C5 PASS/reversal and Matysiak acquisition/audit branches

### 8.1 What C5 actually is

**PROVISIONAL, `DEP-C5`.** In `S=Q[t,X19_total]`, from reviewed
`a1^104 = sum C_j R_j(0)` and reviewed `5 t^6 a1^4 = sum G_i R_i(t)`,
the converter sets

```text
H = -sum_j C_j (R_j(t)-R_j(0))/t ,     x=a1^104 ,     y=tH ,
Phi = sum_{k=0}^5 x^{5-k} y^k ,
a1^4 * ((x-y) Phi + y^6) = a1^4 x^6 = a1^628 .
```

The division by `t` is claimed to be polynomial (exact difference
quotient). The serialized circuit uses 25 of 59 nonzero rows, 950
nodes, no localization, and remains a certificate under `t→rho^2`.
AWS: 239s, one core, 153,180 KiB, zero swap. V1 was a census software
failure (`59` vs `70` slot map), not an algebraic verdict; V2 is the
additive census repair with the same algebra.

Even a promoted PASS closes only the frozen literal total raw
ordered-`a1` chart. It does not supply a terminal receiver map,
normalized K00 reachability, Gate T, source/landing coverage, order
two, maximum twelve, or JC2.

### 8.2 Tree C5-PASS
(review `CONFIRMED`, same identity, same ring, same 25-row support,
mutations still rejected)

1. Promote `a1^628 ∈ J_total` at frozen grade-through-19 ordered-`T-a1`
   scope. Together with promoted `a0^3`, both registered `J2` charts of
   the named unit-load family are closed at this source depth.
2. **Stop:** exponent ladders, N-duals, selected-row rhetoric, any
   slogan that the terminal died, any V43-to-K00 evaluation of `t^6`.
3. Move the 12% V43 budget to terminal/K00 and coverage. Keep at most
   G5 as a compression sidecar.
4. On-family geometry is now a nilpotent thickening of `V(J1+J2)` plus
   ramified/equality/off-family faces. `H_K00` and Card A become the
   landing critical path.
5. **Do not** specialise the identity at `a1=0` and expect a receiver
   relation: `0=sum M_j R_j` is tautological there.

### 8.3 Tree C5-REVERSAL — split by object

**R1. Review `REFUTED` or independent replay finds `a1^628` not in
`J_total` (mathematical false certificate).**

- Un-use `DEP-C5`. Un-close any ordered-chart slogan that consumed it.
- Preserve reviewed `a1^104 ∈ J0` and reviewed `5 t^6 a1^4 ∈ J_total`.
  Those are separate theorems. The converter — exact difference
  quotient for `H`, the identity `x-y ∈ J_total`, content `5`, or
  row-support — is the failing step.
- Next: isolate which conjunct failed. If `H` is not polynomial, the
  difference quotient leaked a pole; that is a ring-map bug, not a
  two-fibre failure. If `x-y` is not in `J_total`, the special-fibre
  lift into the total ring is the missing object (the original C5
  obligation). If the Phi-algebra is wrong, the cyclotomic identity
  was mis-serialized.
- Do **not** revive N7 as an existence method. Generic unit + special
  emptiness still decide *existence* of some total identity; C5 was
  the displayed exponent.
- Rollback: G5 descendants that assumed exponent 628; any “J2 closed”
  reading. Receiver/K00/`8_28` work does not roll back.

**R2. Review `GAP` (scope, homogenization, `U(0)` smuggled, census,
zero-row hash, `t`-division hidden in a node).**

- Fail closed. Do not promote. Keep the 950-node DAG as a debug
  object. Repair the failing node; do not rerun the 59-row decision
  and do not reopen C1/C2.

**R3. Review `CONFIRMED` but a later source mutation or grade-20+ row
makes the identity non-persistent.**

- Persistence after later rows can only get easier for membership.
  A *contradiction* with a later row is a source-custody failure of
  the frozen corpus, not of C5. Quarantine the corpus, not the
  converter algebra.

**R4. C5 holds, G5 finds a smaller pole `s<6` or smaller support.**

- Re-run the converter on the smaller pole; exponent `4+104 s` may
  drop. This is compression, not a reversal. Keep 628 as a valid
  (non-minimal) identity until the smaller one replays.

### 8.4 Matysiak branches

**M-BLOCKED (current).** Primary text unavailable. Classification
remains `ACTIONABLE-UNAUDITED / BLOCKED-BY-PRIMARY-TEXT-ACCESS`. Run
Card C's finite test and lawful acquisition. **Do not** slow C5, Card A,
or Card B. Do not write “JC2 solved” or “external progress.”

**M-ACQUIRED.** Hash both PDFs. Audit 7229358 first: definition of
`5''s`, hypotheses, direction of Keller ⇔ `5''s`. Then 7229458: first
dimension-two step, dimension count, circularity checklist (§6 Card C).
Rerank avenues 7/31/32 only after that audit. A surviving *lemma* may
be imported; a surviving *claim of JC2* may not, until the implication
to automorphism is checked against Cynk–Rusek and against image-UFD.

**M-REFUTED-AS-PROOF.** Expected if `5''s` ≡ de Bondt–Yan square-free
preservation and the dimension step assumes injectivity, factoriality
of `k[f,g]`, or `dim=0`. Classify like Moskowicz: headline unestablished.
Keep Card C's finite test and the literature equivalence. 2024
idempotent-ideals advertisement is prior, not a refutation by itself.

**M-SURVIVING-LEMMA.** If some square-free/image-subring lemma is
honest and strictly stronger than “Keller maps have square-free
coordinates” (which is elementary from `Jac=c`≠0), map it onto
`8_28` face polynomials and AS109 pencils, then decide a bounded
client. Do not restart a square-free *program* as a replacement for
landing/`G2-PSC`.

---

## 9. Scope firewall

- A provisional 950-node identity is not a promoted identity.
- A promoted `a1^628` is not terminal emptiness, not Gate T, not JC2.
- Vacuous modular normal forms are not characteristic-zero emptiness
  and not a compatible jet.
- A finite compatible jet is not an arc and not a polynomial map.
- `coker(M)=0` is not `G2-PSC`; `G2-PSC` is not `G2-BD`.
- R3 is not original `8_28`.
- Square-free preservation is equivalent to Keller in the
  Jedrzejewicz–de Bondt–Yan law; it is not JC2.
- An SSRN abstract is not a theorem.
- V47/V48 are not a fan cover.
- This report does not prove or disprove JC2.
