# Promotion: D1 weighted triple-root two-chart V10

Date: 2026-08-26

Status: **PROMOTED EXACT DIRECTIONAL THEOREM IN THE FROZEN ORDINARY SOURCE;
NO D1 OR JC2 VERDICT.**

## Frozen producer and review

```text
3d6300ced43c4d358f292538c2247655a7ad4f6f0fa674ada4cb27ed3cddaf03
  cases/max12_912_order3_d1_triple_root_weighted_two_chart_v10_20260826/RESULT.md
12ce6fe5b0b98666a62c283fd9ab3d5e84dad26041f85da5525a9cee941f1666
  cases/max12_912_order3_d1_triple_root_weighted_two_chart_v10_20260826/FREEZE.sha256
a497ce3e63569bedfaa3ba2c547e90bc86ade50e6db9d2895693332a67a8ad1e
  xmodel/max12-912-order3-d1-triple-root-weighted-two-chart-v10-review-grok-20260826.md
```

The hostile text-only review returned
`WEIGHTED_TWO_CHART_V10_CONFIRMED`.  It independently checked the complete
freeze and ancestry, both AWS traversals, all 48 emitted exponent records,
both residual discriminants, the chart overlap, and the opposite-axis map.
No mathematical or custody identity failed.

The review prompt transcribed the pinned squarefree theorem SHA incorrectly
as

```text
1b6e629affdc5a41c5abb7e06d63c8dba026bcc46df0a6f21bfed724589189c5.
```

The live theorem, V10 `SOURCE_CLOSURE.sha256`, V10 compiler constant, both
AWS certificates, the squarefree review target, and the squarefree review
prompt all agree on the correct SHA

```text
1b6e629affdc5a73995e0ab2d7f051ad772d92f75bb61e67362782fcb8f5b4ac.
```

This is a review-prompt transcription error only.  It did not enter the
frozen package or either computation.

## Promoted statement

In the pinned ordinary-tail eight-row module, with fixed loads
`k=nu=0, mu=2/3`, charged source SHA
`67343b569db54f61f4a9e56e4262c0bf4fa781323a1287addbaf4fff93844623`,
and the reviewed V9 universal witness of SHA

```text
53cf4b656c4415324a74862ec108b902ac8eade22733b2cb3a58cd3e447491e0,
```

the weighted blow-up of the triple-root point with

```text
wt(a)=1, wt(h)=3
```

covers every noncentral direction under positive rescaled Lambda.

In the `a`-chart `h=a^3*s`, all 48 terms have zero axis coefficient.  Their
closed-corner margins are nonnegative with histogram

```text
0:32, 5/2:11, 5:5.
```

The target and positive-tau split are the two distinguished equality terms;
each of the other 30 equality terms has positive `u` or `v` slope in the
reviewed open cell.  The exact residual discriminant is

```text
-27*s*(s+4),
```

so its double-root walls are `s=0,-4`.

In the `h`-chart `h=t^3, a=t*b`, the exact residual discriminant is

```text
-27*(4*b^3+1).
```

On its squarefree open, ordinary row homogeneity `12+i`, the positive
rescaled-Lambda hypothesis, and the reviewed squarefree order-20 theorem
exclude the residual arc.  On `4*b^3+1=0`, one has `s=b^(-3)=-4`; the change

```text
a'=-a, h'=h+4*a^3
```

preserves `p=-3*a^2, c=2*a^3+h`, so this wall is exactly the already-covered
opposite `a`-chart boundary.  These two charts therefore exhaust the
noncentral directional weighted cone for the literal frozen witness.

## Scope firewall

This promotion does not by itself cover the central cone vertex,
nonpositive rescaled Lambda, moving loads, formal-series loads, another
source chart or normal direction, or global landing/accessibility.  It does
not prove D1 or JC2.  The central, rescaled-Lambda, and constant-load issues
are charged separately in V11 rather than silently imported here.
