# Blind whole-portfolio ideation — Grok 4.6 — round `20260829T1808Z`

Lane: Grok 4.6 (xAI), equal-standing independent researcher.
Produced blind against the sealed packet only. No other
`ideation-20260829T1808Z-*` file, peer prompt, log, run record, or
peer submission was opened. No web, AWS action, canonical edit,
commit, push, or heavy CAS. The separately owned `jc2-lean` tree was
not entered, enumerated, listed, searched, read, status-checked,
built, or modified. Bare `G2` is not used.

## 0. Custody

Model identity: Grok 4.6, xAI.
Clean basis (verified `git rev-parse HEAD`):
`e930fa90b8ee9d86220a2cff72fba94e04b20baa`.

Packet full-file SHA-256 (recomputed, matches the charge):

```text
f58ee9b50d640860fa36f34f12f39bcbb0f8fb852e2ace90be8e62323e09ccfb
  xmodel/ideation-20260829T1808Z-state.md
```

Packet body through its first BODY-END marker, including the
terminating newline: `9040` bytes,
`d48e33c594731b4e6f0761c21c28460a15e15a7a9ea80801692b2fdc1a62529b`.

Authoritative roots named by the packet (recomputed this session,
byte-exact match):

```text
6f5006f46e158bce2211a7d408e206ceef0e54d8b0781d1fa92905d57f2cabcf
  APPROACHES.md
bff17fd8acb0f71fbf1ddc0b5f9d1ca30886222e03ea1f9309a92ac5cff707a0
  AUDIT.md
f947045b4d747efe89a8dadc37ab46e13985ce6099e3bbd40dbd93e3b2489083
  COORDINATION.md
460675f20fccbf774067f067d57b09f93a4d8e811fc0f7b7e899957a9a0ea0f0
  PROGRESS.md
2adfb3b6c25552654e3bc6fbac3b54e6d3ace56a861125cde201317455450c57
  ladder/REDUCTION.md
4c2fd1de827361f06bcbfdd465e0cdb6f03c63203c9943f89af60b8aaaa7637d
  xmodel/u1-star-interface-no-go-coordinator-integration-sol56-93d-20260829.md
2fcf8b9d9d0f7a28c9425bf73d30ba893015c69074afef57d2425309826ace50
  xmodel/td12-u1-first-nonneutral-sextet-coordinator-integration-sol56-93d-20260829.md
6c2ca262a32488ea964d648a21f9a5e008f47f270529d9d24af3021b73ddf6f3
  xmodel/k00-valuation6-hand-obstruction-coordinator-integration-sol56-93d-20260829.md
1e3e50e7b06f6d1f5d3e24bacc93af38b3ba3c17477ecab992ad376bb5e19e7d
  xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md
f602fb81b6e90272462b2263eaeb0eb40550b7d4ce1b87d0e9916bb45322c10b
  xmodel/ideation-20260829T1517Z-synthesis.md
```

Additional files read as history, mathematical source, or systems
evidence, not as a second queue (recomputed):

```text
e47fd16cfcc91bc7bfdac4ba1b5f46152db8235e6609dca6e29a5966549c38f5
  FALLACY-v2.md
2d30bbf12a4c2bbaff135cfc490d51b6f1521369a892d9876fb087edb5bec732
  xmodel/u1-star-pcb-independent-audit-sol56-93d-20260829.md
444c309ef73968271ba0a0ae5d17396079fb24c25b5d72617bce05a19fa46177
  xmodel/u1-star-pcb-hostile-review-grok46-93d-20260829.md
1d79eeeed18949085e9adb7078b4a6f534e9474de6c83ed113aef4e659857f52
  xmodel/td12-occurrence-coverage-attack-grok46-93d-20260829.md
c9563ced370809e0fe48843ac2ae768dc1decb880f8c8624804148236605ae6b
  xmodel/k00-valuation6-successor-design-sol56-93d-20260829.md
e4ec4431d679f44b27d35cbdc3c195c2383f21a57adc63e879be632c13af8a31
  xmodel/k00-higher-valuation-contraction-and-r2-preflight-sol56-93d-20260829.md
5d9cd05c5d14ad69259e084148c5d605fe01c90fa52cb466889005c57c9f7eef
  xmodel/k00-r2-y0-grade8-hostile-review-opus5-93d-20260829.md
b01c48821d22a3425861b172693a8c4f0e9e0ad45c6f5360da2ca485605d700d
  xmodel/ideation-20260829T1517Z-grok46.md
0fdce1553f817222be92a8cfa6c2616c7c5f73e8686550702d3cf25d373a5fda
  ops/lane.sh
cb705ebe7b92c258c20ea5f75386b39cb71aef1ffb1cd8e8a3b07e7f3328bbac
  ops/status.sh
a89658bc2bf01c4cbbc601f3b9db60e46168d37f00367fdc87ef2e94b19a125e
  ops/validate_charge_basis.py
4435279df8f987c667ebc2fae8e4bd987916032561cb3faf1e07a57789d196c2
  ops/run_capped.py
```

Firewall (applied below). A cv flag is not a physical place and is not a
cover series. `REPRESENTATIVE` is not `FULL_ACTUAL_EXIT`. The latter
aliases `FULL_ACTUAL_FIRST_SEPARATION` and is a complete-carrier lower
floor, never attainment. Reduced configurations are not maps. A lower
floor is not exact. Finite jets are not arcs, germs, polynomial maps,
or JC2 results. B and S stay route-separated. Merge-free is not
Statement 8.5. Target `nu_G` is not incoming `nu_U`. Matching names are
not a ring map. If no safe replacement exists, the report returns typed
`OPEN`. No new exit price is asserted, so `charge_basis` is absent.

This lane does not recycle `K00-POLARIZATION-SHEAR`,
`FIBREWISE-FIRST-EVENT-RECORD` as a principal selector,
`Rad(t)=t^r-A` as automatic Galois conjugacy, finite-s as a td12 U1
consumer, `j=42`, S `j=13`, or the already-accepted `CAPRUN/v1`
wrapper. The `20260829T1517Z` ranking is reassessed, not inherited.

## 1. Disposition vector (avenues 1–46)

Legend: `U` unchanged, `R` raise, `L` lower, `O` reopen. Reasons only
for changes, plus a short firewall note where this delta might be
misread as a move. Homonymy does not move a row.

| # | d | reason if changed, else firewall |
|--:|:-:|---|
| 1 | U | GGV farm still has no td ceiling and corners still do not determine poles (`TRANSPORT` 5.2–5.3). The new selector gap makes the T2 pair’s recoverable ledger more *relevant*, not more *tractable*. Not a raise of the degree farm. |
| 2 | U | Remains principal. Internal bottleneck is reordered: global actual-source selector first, conditional td12 laboratory second. The reduced-interface program is now a promoted no-go (`U1*(R)`), not a demotion of the numbered avenue. |
| 3 | U | Strip/residue `R_{k,d2}` is not the B landing ODE and is not a td selector. |
| 4 | U | Remaining K00 jets are not algebraization clients. DEPTH-STAB stays unarmed. |
| 5 | U | No JvdK descent from this delta. Coefficient-space shears are not pair shears. |
| 6 | U | |
| 7 | U | Finite-s and `U1*(R)` do not construct `A(F)`. Flag carriers are not asymptotic components. |
| 8 | U | |
| 9 | U | |
| 10 | U | `NO LEVERAGE` stands. |
| 11 | U | Refuted ladder. |
| 12 | U | |
| 13 | U | |
| 14 | U | |
| 15 | U | |
| 16 | U | Still no named holonomic invariant. HENS-CT timeout stands. |
| 17 | U | |
| 18 | U | Shaska closure stands. Identical reduced pole entries do not force a `G_m` symmetry of a map. Galois conjugacy of T1 roots is not automatic. |
| 19 | U | No new degree-twelve occurrence. K00 is not Witt. |
| 20 | U | |
| 21 | U | |
| 22 | U | |
| 23 | U | |
| 24 | U | |
| 25 | U | `U1*(R)` does not license a monodromy census. Riemann existence remains generous. |
| 26 | U | `td=4R` is never prime, so this delta does not help a primitive-group bound. Stay down. |
| 27 | U | Finite-s remains banked. `U1*(R)` is an equality merge, not a first-separation splice client. Do not reopen. |
| 28 | U | |
| 29 | U | |
| 30 | U | |
| 31 | U | |
| 32 | U | No complete-map client. A jet is not that client. |
| 33 | U | Untwisted residues remain `COSTUME`. Card 1 reuses the elementary automorphism only as a Section-7 object control, not as a residue revival. |
| 34 | U | |
| 35 | U | |
| 36 | R | Last round left the avenue open but unarmed after the valuation-one kill. This delta supplies an exact finite remaining menu on normalized V20R2: valuations `2,3,4,5`, none attained; `v>=6` and the old-plane `y=0` sublane are promoted dead. That is a live falsification client, not a promise that a jet is a map. |
| 37 | U | |
| 38 | U | Direct identities dominate tropicalization of the same generators. |
| 39 | U | No 10-line class. |
| 40 | U | |
| 41 | U | Principle falsified. |
| 42 | U | |
| 43 | U | |
| 44 | U | Closed as proof input. |
| 45 | U | Inverse PDE remains tautological. |
| 46 | U | Formalization is not a route. `jc2-lean` stays outside the campaign boundary; the fence incident is a systems card, not a Lean-as-proof raise. |

Only numbered raise: Avenue 36. No numbered lower or reopen. Unanimous
negative space this round: no `PCB` or `PCB-EXCESS` as a premise; no
quartet revival; no grade-seven Fitting packet; no AWS K00 job; no
`PairRef` from a jet; no finite-s reprice of B25/S17; no exit-price
declaration.

## 2. Reranked bottlenecks

Global selection is listed separately from the conditional td12
laboratory. Endgame-P of a checkable JC2 proof remains modest until a
selector or a cofinal ceiling exists (`REDUCTION.md` T9–T10 / Critical 7).

### 2.1 Global proof gaps

1. **Actual degree / type / entry / U1 selector, or a proof that no
   such selection is needed.** The maximum reviewed spine is only
   `JC2 false -> GGV-minimal standard pair -> Sigray normalization of
   that same pair, td preserved -> td>=6 -> finite entry menu at each
   fixed td`. Nothing in it selects `td=12`, type `(2,3)`, three poles,
   U1, B25, or S17. GGV minimality minimizes the published base-scale
   gcd, not topological degree. `U1*(R)` for every odd `R>=3` now proves
   that the reduced merge/trunk/budget/T1 interface cannot supply the
   missing arrow: it admits type `(2,3)`, `R` identical entries
   `(a,b,nu_P)=(1,2,3)`, `td=4R`, a U1 star, and a reviewed
   nondegenerate local T1 continuation at `k=1`. These are formal
   necessary-data configurations, not maps. This is the principal
   proof gap.
2. **`PCB-EXCESS`, typed `OPEN`.** The proposed Section-7 / Riemann–
   Hurwitz derivation of `td >= s + sum wt` is false. Flag/place/series
   identification is forbidden. The component-aware identity is
   `sum_nonpole(e-1)=2G-2c+td+s`. The exact missing candidate is
   `sum_i (I_i - wt_i(a)) >= s(a)-1`. Current Section 7 proves only that
   the left side is nonnegative. A “unit per pole cluster” repair is
   already adverse (local specialization transports one nearby simple
   direction). Do not assume the inequality. Prove it from new source
   geometry, falsify it on an honest actual-map model, or bypass it.
3. **Independently sourced type menu and a cofinal `td` ceiling.**
   Unchanged endgame wall. A finite entry menu at each fixed `td` is
   not a uniform menu over all `d`. KJN remains type-relative.
4. **Universal full-configuration landing / coverage.** T8 is
   conditional, on-axis, first-jump only. Off-axis `b>=2` and every
   post-jump `M>=2` suffix remain `OPEN`. `RPMC(C)` is still
   type-relative.
5. **`G2-PSC` / `G2-BD`.** Owed only by a hybrid architecture and by
   residue-A respectively. Pure Sigray bypasses `G2-PSC` and still owes
   (1)–(4). Zero active spending.

### 2.2 Conditional td12 laboratory (not a selector)

Given an actual normalized td12 three-pole U1 occurrence with pair,
fibre, merge, and neutral-prefix provenance retained, the first
nonneutral reduced child is MP2-dead or one of six rows:

```text
A7, C5, B25, S17, D13, E11.
```

The quartet is rejected. D13 and E11 are not absorbing merely because
the current vertex is P1-shaped. Exact continuations lower terminal
`psi`; their lower-floor totals meet ceiling 10; local T1 rows pass.
The correct lemma is `TD12-U1-FAMILY-AWARE-DETOUR-EXCLUSION/v2`,
source-typing A7/C5/D13/E11 plus W1--W3 and pure-epsilon descendants.
Reduced tails are non-exclusion witnesses, never attainment. This
laboratory does not discharge gap 2.1.1.

Invariant invisible to the reduced witnesses: exact actual exit
weights, Statement 3.9/3.18 index/shear transport, physical
component/place/edge, and finite Puiseux prefixes. The reduced tuple
`(nu,dp,dq,E,kbar,X,w,M,floor)` does not carry them.

### 2.3 Counterexample / falsification gaps

1. **Normalized V20R2 valuations 2, 3, 4, and 5**, none attained.
   Valuation one is dead on `D(s) union D(t)`. Every `v_Lambda(d)>=6`
   is impossible: grade 12 gives `Q1(x)=Q3(x)=0`, and the grade-19
   contraction forces `Jdet[0]=0` via `D_K6^[2]=-4 Q1 - 32 Q3`.
2. **Valuation-two nonzero-`y` rank strata**, presently
   `REVIEW_DERIVED_UNREVIEWED`. Independent reconstruction is mandatory
   before use. The stopped grade-seven Fitting packet stays stopped.
3. **Off-plane components of the leading cone `Q(x)=0` at valuation
   two**, even after any old-plane closure. The plane
   `ell=(2s,t/8,s,t,s,2t)` is one ruling, not the cone.
4. **Other normalizations / supports** of Avenue 36. The v>=6 theorem
   uses `k6[0]=0` as a load-bearing boundary zero; it does not close
   K00 as a whole.
5. **Char-p / Witt polynomial descent** (Avenue 19) remains reserve.
   First live bounded partial-degree frontier remains maximum twelve.
   No all-Witt-to-polynomial inference.

What this news does *not* rerank: landing totality, `RPMC(C)`,
`G2-PSC`, `G2-BD`, ACS construction debt, LL-1 as an occurrence
engine, or any numbered avenue other than 36.

## 3. New mechanism, new connection, composition pass

### 3.1 New mechanism — `K00-V25-FIRST-LOAD-CALENDAR` (NEW)

The serialized minimum transverse degrees

```text
(D10, D6, D2, u2, u4, u6, -h/4) = (3, 2, 2, 1, 1, 1, 0)
```

are source-level and valuation-independent. They were used only to
kill `Lambda^6 | d`. The same table schedules the remaining menu.
With `k6[0]=0` and `k10[0] != 0`, the first live contracted grades are

```text
Lambda^2 k10 D10   at  2 + 3v
Lambda^6 k6  D6    at  7 + 2v     (uses k6[1] and D6^[2])
Jacobian target    at  19         (always)
raw quadrics Q_i   at  2v
```

so the remaining valuations are not homogeneous:

| v | Q_i | D10 | k6 D6 | Jdet | first load-free window after Q_i |
|--:|----:|----:|------:|-----:|---|
| 2 | 4 | 8 | 11 | 19 | none: D10 meets grade 8 (the promoted y=0 window) |
| 3 | 6 | 11 | 13 | 19 | grades 7–10 |
| 4 | 8 | 14 | 15 | 19 | grades 9–13 |
| 5 | 10 | 17 | 17 | 19 | grades 11–16 |

Mechanism, not a restatement of the v>=6 kill: treat the four values
as four coefficient windows on one and the same projective leading
cone `V(Q)`, with D10/k6/Jdet entering at the grades above. Close
synonyms checked: successor-design §3 (window written only after
`Lambda^6 | d`; grades 0–11 vanish identically only under that
substitution); rge7 review (`2+3r` used for `r>=7`); r2 preflight
(“other components of this quadratic leading cone” named only for
v=2). The calendar, the v=3 load-free window 7–10, and the
identification of `V(Q)` as the common leading locus of *all four*
remaining valuations are **NEW**. Card 3.

### 3.2 New connection — Avenue 33 costume map consumes Avenue 2’s
`PCB-EXCESS` control (NEW)

Avenue 33 already ran the elementary automorphism `(x, y+x^n)` as an
untwisted action-residue gate and returned `COSTUME`. That object is
an honest actual polynomial Keller map. `PCB-EXCESS` is an inequality
on the finite-value quotients `U_i` of repaired Section 7, a different
identity. Connection, not a residue revival: feed the same actual map
into the L3/L4/L5-exact plus repaired Section-7 package, compute
`s(a)`, each `I_i`, each `wt_i(a)`, and the signed excess
`sum (I_i - wt_i(a)) - (s(a)-1)`, or return typed `OPEN` if those
objects are undefined at `td=1`.

This is not the audit’s curve-level `g(t)=t+t^{-1}` RH control, not
the “`P_i` linear / `w_i` constant” local zero-excess situation, and
not a flag/place identification. Fail-closed readings:

- objects undefined at `td=1`: `PCB-EXCESS` owes extra hypotheses
  (`td>=6` and/or nonautomorphic); the CE-only statement stays `OPEN`;
- objects defined and excess `< s-1`: general-map `PCB-EXCESS`
  `REFUTED`; a CE-only restatement must be rewritten before reuse;
- objects defined and excess `>= s-1`: consistent, not a CE theorem.

Do not transport a residue pin, a generating function, or an
untwisted polar divisor. Card 1.

Second, weaker connection (recorded, not a card): D13/E11 already
carry promoted full-exit floor 10 against terminal ceiling 10. That
zero slack is a numerical fact in the sextet integration, not a new
consumer of finite-s as a U1 carrier (still `SCOPE-CONFLICT` for
`m=3`). It becomes an actual-weight discriminator only after a
source-typed strict-excess proof; Card 2 attacks that and does not
identify the floor with attainment.

### 3.3 Theorem-interface composition

| Theorem (lifecycle) | Object | Consumes / discharged by | Result |
|---|---|---|---|
| Unconditional spine through finite entry menu (`promoted`) | GGV-min pair, Sigray nf, `td>=6` | T2, TRANSPORT 3.1, T5, Prop 5.8 replacement | **HIT** as the selector’s parent. **NO HIT** with td12, type `(2,3)`, U1, B25/S17. |
| Abstract `U1*(R)` (`promoted`, interface scope) | odd `R>=3`, `(1,2,3)^R`, `td=4R` | reduced merge/trunk/T1 at `k=1` | **HIT** as no-go for Sigma_cfg. **NO HIT** with occurrence or a map. |
| PCB from §7 / RH (`refuted` as derivation; bare inequality `OPEN`) | `td >= s+sum wt` | Euler `td-1=sum I_i`; RH on places | **NO HIT** with a selector. Card 1 attacks the named repair, does not assume it. |
| Conditional sextet (`promoted`) | A7,C5,B25,S17,D13,E11 | actual td12 U1 + provenance | **HIT** as the laboratory’s first-event menu. **NO HIT** with landing or attainment. |
| Finite-s full-actual carrier (`promoted`, lower-floor only) | `a(P)=max_j min(v_j,O(P,P_j))` | one fibre/component; physical flags | **NO HIT** with `U1*(R)` as a pole-path consumer. Numerical full-exit floors on D13/E11 remain floors. |
| V20R2 `v>=6` impossible (`promoted`) | `C6=1`, `k6[0]=0`, `Jdet[0]!=0` | Q1,Q3 at grade 12; (3.2); grade 19 | **HIT** with Card 3’s calendar. **NO HIT** with v=2–5 existence. |
| Old-plane v=2, `y=0`, through grade 8 (`promoted`) | `d=Lambda^2 ell + Lambda^4 z + O(Lambda^5)` | A=B=0; cubics on `D(kappa) cap (D(s) cup D(t))` | **HIT** as positive control for Card 3. **NO HIT** with nonzero-`y` or off-plane. |
| Opus F9–F11 (`REVIEW_DERIVED_UNREVIEWED`) | rank-2 kill; old-plane y-invariance; Q vs Qbar | grade-8 `D,F` eliminations | **HIT** as Card 3’s reconstruction target. **NO HIT** until different-model replay. Do not use. |
| B/S bridges (`promoted` at named occurrence) | landing ODE; shear/floor/sibling | named B25 or S17 plus prefix | **NO HIT** with selector or sextet exclusion. Stop homogeneous descendants. |

Honest global reading: no present composition sends a hypothetical
minimal counterexample into a currently enumerated book. A speculative
“minimal CE must be td12/U1/B25 or S17” lemma is **not typed**. Record
as `OPEN`, do not launch.

## 4. Strongest attacks

**Global selector.** Do not spend the principal slot on another
reduced row, on `TD12-U1-ACTUAL-LANDING` as if it were the missing
arrow, on homogeneous-window descendants, or on assuming `PCB-EXCESS`.
Strongest prove/falsify/bypass:

- Prove: a source-typed excess injection into
  `sum (I_i - wt_i(a))` that does not identify flags with places and
  does not use a unit-per-pole argument already marked adverse.
- Falsify: Card 1’s actual automorphism (or any honest actual Keller
  map on which Section 7 objects exist) with total excess `< s-1`.
- Bypass: an actual-pair invariant invisible to Sigma_cfg that bounds
  `R` or forbids identical `(1,2,3)` occupancy. The local T1 polynomial
  `R(t)=t^R - A_*` always solves at the reduced tier; gluing its `R`
  roots to leading coefficients of `R` actual pole chains of one
  polynomial is the named missing consumer (PCB audit §2.2). Naive
  MP6(b)/chart identification `deg p_pole = i_G mu` is already known
  to kill the reviewed td12 U1 object and is forbidden. If no such
  invariant types, return `OPEN`.

The T1-root glue is **not** automatic Galois conjugacy and is **not**
an Avenue 18 revival.

**Conditional td12 discriminator.** The reduced laws cannot exclude
A7/C5/D13/E11. The cheapest actual-source test is Card 2: D13’s named
continuation has AF2 floor 9, promoted full-exit floor 10, terminal
ceiling 10; E11 has a continuation with both floors 10 against ceiling
10. Slack at the full-exit dictionary is zero. Any source-typed strict
actual-weight excess of 1 kills that continuation; equality remains
non-exclusion, never attainment. A7/C5 still have slack 1 at AF2 and
must not be treated as equally tight. Parallel obligations, not
substitutes: Statement 3.9 transport `i_F = 3 n i_G` on the first
trunk edge, W1--W3 and pure-epsilon fixtures, fail-closed unclassified
events. Verdicts stay
`PROVED_DETOUR_EXCLUSION` /
`REFUTED_BY_ACTUAL_SOURCE_TAIL` /
`SOURCE_TRANSPORT_OPEN`.
The first still would not select td12/U1.

## 5. Strongest K00 valuation-2--5 attack

Exact scope: the frozen normalized V20R2 source over a
characteristic-zero field, `C6=1`,
`k6[0]=k2[0]=mu2[0]=mu4[0]=mu6[0]=0`, `k10[0] != 0`, and
`Jdet[0] != 0` where used. Not another support, not a polynomial map,
not K00 as a whole, not JC2.

Cheapest honest discriminator, in order:

1. Independently reconstruct Opus F9–F11 from `tails.json` and the
   declared V20R2 map, over `Q` and over an algebraic closure
   separately. Do not consume the derived claims. Desk exact
   rationals; no Singular; no AWS.
2. If F11 confirms: over `Q`, the old-plane valuation-two lane is
   empty at grade 8 (because `u^2+64 v^2=0` forces `u=v=0` when `-1`
   is not a square). Over `Qbar`, only the rank-one stratum
   `u = ±8 i v`, `v != 0`, remains on the old plane. Those two
   readings must not be mixed.
3. If F9 confirms, the rank-two stratum is dead and the next old-plane
   packet is N-R as specified in the Opus review §7, still desk.
4. In parallel, not after AWS: apply the first-load calendar at
   valuation three. Grade 6 is `Q(x)=0` on the leading vector, the
   same cone; grades 7–10 are the cheapest remaining window before
   D10 enters at 11. This is a hand/Python expansion, not a Fitting
   chart.
5. Off-plane valuation two, `x` not on `ell`, with `Q(x)=0` and
   `DQ(x)[y]=0`, is a separate client. Do not treat old-plane closure
   as cone closure.

Stop conditions: any reconstruction mismatch with the frozen tails is
`GAP` on the derived claim, not a licence to run the stopped
grade-seven packet. A compatible finite jet is not an arc. No AWS
until a nonempty desk jet has to be pushed toward `Lambda^19`.

## 6. Mathematical-software acceleration / decisive experiment

**Object.** Independent exact reconstruction of F9–F11, with the
promoted `y=0` kill as a positive control, and a valuation-three
grade-6 leading-cone census from the same rebuilt rows.

**Placement.** Desk first. Python 3 stdlib plus `fractions.Fraction`
against the frozen hashes already in the r2 integration:

```text
d72f774c53c7dcb259eefc10ff2a02202e1049b61c17486b1ba22e3773813848
  tails.json
940d9e42e1fce079f4bfcb5e1a319884c4b606f32f50ba435d5dc294ed7ff24a
  K_VECTOR.txt
cade1704d973750e455829a3c3783d1c8444a4bb6e2b14e37e8277c83393819c
  CONTRACTED_TARGET.txt
2ac7653cf0c7a39970d54286b85974b39a2e6deb116839720cb1ae91f8ad6d2b
  compile_contracted_source_v20r2.py
```

No Singular, no cap token, no AWS for this step. Resource bound: 60
CPU seconds and 1 GiB RSS per process. The Opus audit of the same
rebuild ran in well under one second per script; that is the expected
class.

**Ring map (FALLACY variable/ring-map rule).** Declare
`C0=(1+d0)/256`, `C1=d1`, `C2=(1+d2)/16`, `C3=d3`,
`C4=(3+d4)/8`, `C5=d5`, `C6=1`, generator order
`(d0,...,d5, k10, k6, k2)`, coefficient field `Q`, image checks on
the seven rebuilt rows against `tails.json`. Matching names prove
nothing. Saturation is not used; if a later descendant wraps `sat()`,
extract the ideal component, assert its ring, and run both controls.

**Controls.**

- Positive: replay the promoted `y=0` grade-eight cubics; they must
  have no common zero on `D(kappa) cap (D(s) cup D(t))`.
- Negative: mutate `16 t^2` in `a=(s^2, st/8, 16 t^2, 0,0,0)` to
  `15 t^2`; the `y=0` identity must fail.
- Field split: report F11 over `Q` and over `Q(i)` as different
  sentences.
- Non-map control: label every output `FORMAL_JET_ENVELOPE`.

If N-R is nonempty and the jet must be prolonged toward grade 19,
preregister an AWS packet then; do not launch it in this report.

## 7. Campaign-systems card — `UPGRADE`

Axis: time to a sealed replayable certificate, and report-to-ingestion
latency. Mathematical scheduling penalty: none.

Live evidence against `NO_CHANGE`:

- `ops/lane.sh` validates `charge_basis` and refuses duplicate tags,
  but never checks a report body seal. A malformed producer seal has
  already forced a quarantine on an otherwise repairable S-bridge.
- The Opus r2 review records that a producer recipe claiming a
  *unique* BODY-END marker can occur twice in one file (the marker
  and a quoted recipe). First-occurrence hashing is the reproducible
  convention; `rfind` is not. Coordinator ingestion still repeats
  this hash by hand.
- `CAPRUN/v1` is accepted, opt-in, with no caller migration. Do not
  migrate callers in this packet.
- The same r2 review traversed `jc2-lean` via `grep -rl` and later
  statted it. `ops/status.sh` already excludes that path for `git
  status`; search tools do not. A search-fence is useful but cannot
  be enforced on a model the way a lane-level seal check can. It is
  recorded, not this card’s payload.

Smallest bounded packet: `ops/verify_body_seal.py`, wired into
`ops/lane.sh` after the report exists, sibling to
`ops/validate_charge_basis.py`.

Contract:

- Body is every byte through the *first* BODY-END marker line,
  including its terminating newline.
- Parse the post-body seal for `Body bytes` and `Body SHA-256`.
- Exit 0 only on exact match of both.
- If the marker occurs more than once in the file, succeed only when
  the first occurrence is the body terminator and the later
  occurrences lie strictly after it; emit
  `marker_count=N` in stdout. If a quoted marker appears *before* the
  terminator, fail closed (`rc=7`).
- Do not infer `charge_basis`. Do not touch `jc2-lean`.
- No canonical-file mutation beyond the new helper, its test, and the
  existing lane hook.

Smallest measurable acceptance test (ordinary and `-O`,
byte-identical logs):

1. A fixture report with one terminator and a matching seal exits 0;
   printed `body_bytes` and `body_sha256` match `python3` hashing.
2. Flip one body byte, or increment the declared byte count: nonzero
   exit, `final_status` not `DONE` if run through `lane.sh`.
3. A fixture with a quoted marker in the *body* and a later real
   terminator exits nonzero (`rc=7`).
4. A fixture with the real terminator first and a quoted marker in
   the post-body seal exits 0 and prints `marker_count=2`.
5. A grep fixture on the helper source fails if `rfind` is used to
   locate the marker.

Positive control: fixture 1. Negative controls: fixtures 2 and 3.
Do not migrate CAPRUN callers. Do not churn `FALLACY-v2.md`. Next
systems checkpoint remains `2026-08-31T16:35Z`.

## 8. Idea cards (three) and launch portfolio

### Card 1 — `AUT-SECTION7-EXCESS-CONTROL`
[`NEW` experiment on a `KNOWN` `OPEN` target; Avenue 33 map is `KNOWN`]

**Fingerprint.**
`elementary-Aut|(x,y+x^n)|L3/L4/L5-exact|Section-7 I_i,wt_i|excess vs s-1|OPEN-or-REFUTE-or-COSTUME`

**Target.** Whether `PCB-EXCESS` is even a statement about actual
Keller maps, by evaluating it on an honest actual polynomial map.
Not a residue revival, not a CE, not a td12 landing.

**Dependencies.** Promoted: repaired Section 7 / Cor 7.1 as
inequality; Prop 5.1 sidedness; L3/L4/L5-exact for a normalized
Lemma 2.1 rectangle/NE-corner fibre; Avenue 33’s elementary
automorphism as an actual map. Excluded: PCB as a premise; RH as a
flag sum; unit-per-pole; `U1*(R)` as a refutation.

**Exact proposed theorem or experiment.** For `F=(x, y+x^n)`,
`n>=2`, after any Sigray normalization the pair admits, compute on
one fibre `a` the Section-7 package `(U_i, I_i, wt_i(a), s(a))` or
prove those objects are undefined. Then evaluate
`sum (I_i - wt_i(a)) ?>= s(a)-1`.

**Cheapest discriminator.** Existence of the objects at `td=1`,
before any excess arithmetic.

**Outcomes.** Undefined: `PCB-EXCESS` needs extra hypotheses; CE-only
form stays `OPEN`. Defined and excess `< s-1`: general-map statement
`REFUTED`. Defined and excess `>= s-1`: `COSTUME` relative to
automorphisms; CE case remains `OPEN`. Any flag/place identification
is `GAP`.

**Stop.** First undefined object, or first typed excess, or first
firewall violation. Do not iterate `n` after one clean reading.

**Information gain.** Removes `PCB-EXCESS` as an untyped selector
candidate, or writes the exact extra hypothesis it owes.

**Compute class.** Desk. No CAS.

**Ownership.** Prove: source-typed excess on a CE fibre after objects
exist. Falsify: this card. Bypass: T1-root-to-pole-leading glue with
the MP6(b) identification forbidden; if untyped, `OPEN`.

**Provisional descendant.** None that assumes the inequality.
Promotion still needs different-model hostile review.

Novelty search: Avenue 33 used this map for residues (`COSTUME`);
PCB audit used `g(t)=t+t^{-1}` and a linear/`w` constant control;
neither ran L3/L4/L5 plus Section 7 on `(x, y+x^n)`.

### Card 2 — `DETOUR-FULL-EXIT-TIGHTNESS`
[`NEW` attack on `KNOWN` slack-zero numbers]

**Fingerprint.**
`td12-U1-conditional|D13/E11|full-exit-floor=ceiling=10|strict-actual-excess-or-OPEN`

**Target.** Whether an actual-source strict weight excess of 1 exists
on a named D13 or E11 continuation. Not occurrence, not a selector,
not attainment.

**Dependencies.** Promoted sextet; named continuations
`D13 -> (539,99)` at `nu=49` and `E11 -> (893,95)` at `nu=47` /
`E11 -> (154,22)` at `nu=7`; full-exit = first-separation as a
*floor*; Cor 7.1 inequality. Excluded: treating P1 shape as
absorption; using AF2 equality as actual equality; charging a flag
without a first-separation set.

**Exact proposed theorem or experiment.** On each named continuation,
source-type the actual exit weights of every priced step. If any
actual weight is strictly larger than its AF2 floor, the full-exit
total exceeds 10 and that continuation is actually excluded. If every
actual weight equals its floor, report lower-floor non-exclusion and
stop. If source prefixes are missing, `SOURCE_TRANSPORT_OPEN`.

**Cheapest discriminator.** One strict inequality on one priced step
of D13 or E11.

**Outcomes.** Strict excess: that continuation dies; the other three
detour starts remain. All equalities: non-exclusion, never a map.
Missing provenance: `OPEN`. Do not delete D13/E11 by interior
terminal budgets.

**Stop.** First strict excess, or first missing Statement 3.9/3.18
datum, or first unclassified actual event.

**Information gain.** Turns the sextet’s zero-slack arithmetic into a
binary actual test, or records the exact missing source datum.

**Compute class.** Desk. No AWS.

**Ownership.** Prove: actual excess from Puiseux/source. Falsify: a
source-compatible equality profile on a named tail. Bypass: family-
aware exclusion by index/shear contradiction without weights.

**Provisional descendant.** Statement 3.9 ledger on the first trunk
edge of the *conditional* td12 U1 star, `i_F = 3 n i_G` with
`n>=5`, `n ≡ 1 or 5 (mod 6)`, without identifying pole and merge
charts. Promotion still needs different-model review.

Novelty search: sextet integration already prints floor 10 = ceiling
10 as non-exclusion; A7/C5 tails have slack 1; PCB audit’s cheapest
local discriminator was the whole family-aware packet. Tightness of
D13/E11 as a *first* actual-weight cut is not that packet.

### Card 3 — `K00-F9F11-REPLAY` plus calendar descendant
[`NEW` calendar; F9–F11 are `KNOWN` unreviewed derived claims]

**Fingerprint.**
`V20R2|v=2 old-plane|reconstruct F9-F11 over Q and Qbar|v=3 grades 7-10|no Fitting-G7`

**Target.** Whether the derived rank-stratum claims survive an
independent rebuild, and whether valuation three dies in its
load-free window. Not a map.

**Dependencies.** Promoted: v>=6 kill; old-plane `y=0` grade-eight
kill; min-degree table; V20R2 compiler map. Provisional: F9–F11.
Excluded: grade-seven Fitting packet; `K00-RENORM`; treating a jet
as a germ.

**Exact proposed theorem or experiment.** Rebuild the seven rows from
`tails.json`. Replay F9 (`D,F` eliminations on `u^2+64v^2 != 0`),
F10 (grade-eight coefficients independent of the old-plane
`(s',t')`), F11 (Q-emptiness of `u^2+64v^2=0` versus Qbar rank-one
survivor). Separately expand valuation three at grades 6–10 on
`V(Q)`.

**Cheapest discriminator.** F9’s two `d_{*,5}`-free, `kappa`-free
grade-eight combinations. Then F11’s field split. Then v=3 grade 6
as `Q(x)=0`.

**Outcomes.** F9/F11 confirmed: old-plane v=2 dies over `Q` at grade
8; over `Qbar` the next desk packet is N-R. F9 refuted: derived
claims return `GAP`; v=2 nonzero-`y` stays open; do not launch N-R.
v=3 window unit: that valuation dies on this support. v=3 proper:
report the leading locus and stop.

**Stop.** First mismatch with frozen tails; or first unit/proper
reading of the v=3 window; or N-R emptiness. No AWS in this card.

**Information gain.** Collapses the remaining v=2 old-plane, or
isolates a single rank-one chart, and either kills or arms v=3
without a Groebner job.

**Compute class.** Desk exact Python, <60 s, <1 GiB. AWS only if a
later descendant must prolong a nonempty jet to grade 19, and only
after preregistration.

**Ownership.** Prove: independent identities F9–F11 and/or a v=3
unit. Falsify: a rebuilt common zero on a claimed-empty stratum, or
a v=3 leading vector through grade 10. Bypass: a different
normalization/support; that is a new source client, not this card.

**Provisional descendant (may start without waiting for review of
F9–F11).** The v=3 grade-6 cone census from the already promoted
quadrics `Q_i`, with ring map as in §6. It does not consume F9–F11.
Promotion of any kill still needs different-model review.

Novelty search: F9–F11 are written in the Opus review and explicitly
unpromoted; the calendar is not in the v>=6 successor design; the
r2 preflight names off-plane v=2 but does not schedule v=3–5.

### Ranked launch portfolio

Run 1 and 3 in parallel immediately (proof-side selector control,
counterexample-side remaining menu). Run 2 in parallel on the
conditional laboratory; it must not consume the global slot.

1. Card 3 — cheapest certificate, desk, mandated reconstruction,
   Avenue 36 now live.
2. Card 1 — principal proof attack on the named `OPEN` selector
   repair, fail-closed on object existence.
3. Card 2 — conditional td12 actual-weight cut on the two rows the
   quartet falsely deleted.

Do not launch: grade-seven Fitting, v>=6 CAS, `j=42`, S `j=13`,
PCB as a premise, quartet descendants, AWS K00, `jc2-lean` work,
CAPRUN caller migration, or any packet whose only client is the dead
valuation-one seed.

Reviews remain background/nonblocking once provisional confidence is
reasonable. Promotion still requires different-model hostile review.

Nothing here proves a global occurrence theorem, a degree bound, a
polynomial Keller pair, a counterexample, or JC2. A lower floor is
not attainment. A finite formal jet is not a map.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `34560`.
- Body SHA-256: `9257ea3daecedb627746d1b8da879776c0ccc4480fcfc77f01160a51681cb50a`.
- Frozen basis: `e930fa90b8ee9d86220a2cff72fba94e04b20baa`.
