# Hostile different-model review: GCD3 `(6,9)` lower-Pfaffian successor

Act as a hostile independent mathematical reviewer. Review the frozen producer

`xmodel/gcd3-69-lower-pfaffian-successor-20260824.md`

at SHA-256

`7671785519bf4e55b602f117740bd8d8571c6982a8916235407a2bb0a2263043`.

The charged clean-bank basis is
`1e4480c14f2ab9c4145eb6f6c74f0ac348baf76a`. Verify the frozen case
manifest and freeze record from the repository root:

`cases/gcd3_69_lower_pfaffian_successor_20260824/MANIFEST.sha256`

at SHA-256
`cff1e62028af45f81eb4f00ca72f65733d03a94dc95d29614c09139532c817b1`,
and

`cases/gcd3_69_lower_pfaffian_successor_20260824/FREEZE.sha256`

at SHA-256
`e48ba73639bf582409da246f4ee6f83add26a6b67528b329c691075d5f510874`.

Reread the frozen first common-cubic producer and its completed hostile review.
Treat the predecessor only at its reviewed scope. Do not import full-cubic
boundary divisibility, cube-core alignment, polynomiality of `z=s y+r`, or a
general `(6,9)` exclusion.

Return exactly one self-contained Markdown report at

`xmodel/gcd3-69-lower-pfaffian-successor-review-grok-20260824.md`.

Give an overall verdict and a numbered verdict for every claim below. Re-run
both registered replays, but independently reconstruct each load-bearing
finite calculation with a second derivation or engine in scratch space. PASS
strings and producer assertions are not authority. Do not edit producer,
case, canonical, ladder, notes, prompt, log, or run files.

1. **Input perimeter and source bracket.** Verify every frozen hash and the
   exact conditional input: nontrivial cubic Kummer extension
   `L=k(x)(s)`, `s^3=h in k[x]`, `3|deg(h)`, the aligned depressed normal
   form with five moving `a_i` and constant `kappa`, and
   `J_(x,y)=s(f_x g_z-f_z g_x)`. Independently reconstruct `g0` from the
   eight high rows before using the five lower rows. Police the exclusions:
   cube mismatch and aligned cube-core constants are not inputs.
2. **Four first integrals and Kummer constants.** Starting from the
   undifferentiated `f,g`, independently derive the `z^4,...,z^0` coefficient
   matrix. Verify the four triangular corrections in (3), closedness and
   exactness of every one-form, formulas/normalizations for `I4,I3,I2,I1`,
   and weights `(1,2,0,1)`. Check that their derivatives vanish in precisely
   the asserted order and that the constant field argument licenses only
   `I4=I3=I1=0`, `I2=mu in k`.
3. **Universal invariant-fiber decomposition.** Independently compute the
   radical and associated primes of the four invariant equations over the
   universal ring, not just at samples or generic parameter values. Verify
   that `PA` and `PB` are the only minimal reduced primes, the displayed
   `PE` is embedded with `PA subset PE` ideal-wise, and
   `rad(I)=PA intersect PB`. Audit base-field/geometric primeness and the
   prime-kernel argument that a generic path into the field `L` cannot switch
   reduced sheets. Freeze the smallest missing component or specialization
   if the universal assertion fails.
4. **Zero-bracket sheet `PA`.** Derive `d=w/4 in k`,
   `mu=9d^2/4+3kappa d`, and exactly
   `f=K^2+d`, `g=K^3+(kappa+3d/2)K`. Differentiate with moving `K` and
   constant `d,kappa` and verify that the entire source bracket is zero,
   including nonzero `d` and `kappa`; hence the nonzero terminal row cannot
   occur.
5. **Elliptic sheet and terminal orientation.** Pull the polynomial terminal
   one-form to `PB` before dividing by any coordinate. Independently verify
   both coefficients in (10), the definitions and Kummer weights of `X,Y,C`,
   the exact curve `Y^2=3X^3+4096C`, and
   `beta=(7Y^2-12288C)dY/(147456X)` with the correct sign and factor. Check
   rigorously that `X=sR`, `Y,R in k(x)` and that `beta=j/s` yields both
   equations (12) and the cleared equation (13), with no lost factor of `s`,
   `3`, or `j`.
6. **`C != 0` and every no-loss case.** Handle constant `Y` before clearing;
   in particular check `X identically 0` directly in the polynomial form.
   For nonconstant rational `Y`, justify surjectivity and why at least one of
   the two roots of `7Y^2-12288C` has a finite preimage. At a preimage of
   ramification index `e`, verify that `Y^2-4096C` and `X` are nonzero and
   that the valuation is exactly `ord(h)=3-6e<0`. Test poles, ramification,
   `Y'=0`, infinity, and chart escape separately; do not infer the result
   from a generic unramified point.
7. **`C=0`, shifted DS, and all rank loci.** Without division, verify
   `EB=(3/4)X^2(X-3a4^2)`. Check that `X=0` lies on the zero-bracket sheet and
   that `X=3a4^2`, `a4=4lambda` gives exactly
   `f=f_lambda-2kappa/3`, `g=g_lambda`. Recompute the bracket
   `567lambda^6` and determinant `(3^18*7/32)lambda^20`. Derive the terminal
   ODE and its descent to (21), then independently audit every finite-place
   and infinity valuation used from the predecessor, including constant
   `h`, the cancelled case, `k>=1`, one-zero support, leading coefficients,
   `3|deg(h) => 3|k`, and why this makes `h` a cube. Explain why the constant
   subtraction changes none of it. Treat `lambda=0`, isolated zeros, the
   unshifted determinant factors in (22), and every denominator/rank-zero
   stratum without generic matrix inversion.
8. **Exact promotion and quarantine.** The maximum possible promotion is a
   conditional exclusion of the aligned **nontrivial-Kummer** `(6,9)` branch
   under the reviewed high-row/history input. It uses no boundary reduction.
   It does not address the live `delta!=0` cube-mismatch branch, aligned cube
   branches with weight-unforced constants, arbitrary `(6,9)`, a global
   landing theorem, or JC2. Decide whether the cube-core high-row system with
   every unforced constant is the smallest honest successor.

For each claim label `CONFIRMED`, `PARTIAL`, `REFUTED`, or `TYPE-FAIL`, give
the smallest failing identity or logical implication if any. State the exact
promotion and quarantine at both the top and bottom of the report.
