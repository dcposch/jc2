# PROP 5.5(k) — Opus 5 — 2026-09-03

Lane `prop55k-opus5-20260903` (flagship). Frozen inputs in
`/tmp/jc2-lane.kW1RZi/inputs`; **all 11 SHA-256 verified before any read**
(stop-on-mismatch did not trigger). Moh 1983 read from
`refs/moh1983_jram340_configurations_of_roots.pdf` (`6c8847a8…`, matches),
rendered at 300 dpi with `pdftoppm`; journal page `N` = PDF page `N−139`.
Pages opened **as images**: 151, 164, 168, 170, 174, 178, 179, 181, 186,
187, 188, 197, 207, 208, 210. Every Moh citation below is `SOURCE-READ`
(page given) or typed `SOURCE-UNVERIFIED`.

Discipline: no canonical ledger edited; `jc2-lean` not inspected; no
`ideation-20260903T1200Z-*` file and no running lane's report opened.
Drivers in `box/prop55k-drivers-20260903/`. Desk-scale: total CAS time
≈ 6 min, one core, < 300 MB.

Prime marks: `n', m', M_i', V_i', d_i', δ_i', k, A', 𝔄` are **labels** for
descended data, never derivatives. `f_i'(x)`, `g_σ'(π)`, `T'_{1,σ}(π)`
**are** derivatives (Moh p.151, p.187). `P` denotes the descended
polynomial of Prop 6.3; the integer `n*+m*` is written `Π` throughout to
avoid the collision.

## 0. Headline

1. **The Jacobian enters Moh's radius system in exactly one place**, and it
   is not Lemma 5.2 or Def 5.1(3): it is the factor `J_{x,y}(f,g) = 1` in
   the chain rule of **Prop 4.1, p.164** (`SOURCE-READ`). Replacing it by
   `c x^k` replaces the constant `−1` by `−(k+1)` in Prop 4.4(3),(6) and
   Prop 4.6(3), and **nowhere else**. The rest of the system is linear and
   homogeneous in the `δ`'s, so the unique solution scales:
   `δ_i^{(k)} = (k+1)·δ_i^{(0)}`. That is a **proof of the rule Φ**
   (charged `descent-radii` lane, where it was fitted). `PROVED-HERE`.
2. **Closed form.** With `d_2' = gcd(n',m')`, `n* = n'/d_2'`,
   `m* = m'/d_2'`, `Π = n*+m*`, `U_2' = d_2'−V_2'`, `R = n'−M_2'−1`:
   ```
        δ_2' = −(k+1)/R ,        δ_1' = (k+1)(Π U_2' − R) / ( R (Π V_2' − 1) ) .
   ```
   At `k = 0, R = 1` this is Moh's `((n*+m*)U_2−1)/((n*+m*)V_2−1) = B/A`
   verbatim (p.187). It reproduces **all ten** printed p.207 rationals with
   zero free parameters, agrees with `(k+1)·`Def 5.1(3), and — new here —
   is confirmed by **independent high-precision Newton–Puiseux measurement
   on 20 genuinely existing pairs** with `J = cγ^k` (§3.3). `PROVED-HERE`.
3. **The r = 1 ODE is Jacobian-monomial-tolerant, verbatim.**
   `D(n', −M_1', ḡ_σ(π), T^{ψ}_{1,σ}(π)) = nonzero constant` survives
   `J = cγ^k` with only the value of the constant changed (`1/λ ↦ c/λ`):
   the `k` shifts the `t`-order of **both** sides of Prop 4.1(1) equally,
   through the already-shifted `λ`. Hence Appendix I (Prop A.1–A.5) and
   both coprimality conclusions of p.187 apply **unchanged**. `PROVED-HERE`.
   The ODE does **not** acquire a `π^k`.
4. **PROP 5.5(k) is TRUE as a theorem but FALSE as a contradiction for
   `k ≥ 1`.** Stated exactly in §2.6. Its content at `δ_2' = −1` is
   `d_2' ≤ (k+1)V_2'` plus `g_0 | (k+2)V_2' − d_2'`, `g_0 ≥ V_2'`. At
   `k = 0` this collides with Moh's `d_2 > V_2` and gives Prop 5.5. For
   `k ≥ 1` it is **sharp and cannot be improved**: the existing family
   `P = π^{k+1} − γ^{k+1}`, `Q = P^q + aπ` (`J = −(k+1)a γ^k`) attains
   `d_2' = (k+1)V_2'` with equality, for every `k ≥ 1` and every `q ≥ 2`.
5. **A correction I had to make mid-lane, and its consequence.** Moh's
   Galois step (p.188) uses the modulus `A' = denom(δ_1')`. That is valid
   only when the common part of `σ_1` lies in `k((t))`. Applying `A'`
   blindly to the descended rows **kills two genuinely existing control
   pairs** — I produced them and checked their radii numerically. The
   correct modulus is `𝔄 = A'/gcd(A', L)`, `L` the index of the
   `t`-exponent lattice of the common part. Two independent confirmations
   that the correction is right: (a) the fail-closed controls; (b) under
   the uncorrected modulus Moh's **own** printed row `(21,14; 16; 2; X)`
   would be killed by Prop A.5 at Prop 5.5 level, which would make his
   Appendix-II work on that row unnecessary. §2.5, §3.4.
6. **G2 / G3 (`D = 105`): NOT killed.** With the uncorrected modulus they
   are killed outright; with the corrected one they survive
   (`𝔄 = 2`, residues `(1,0)`). I report the negative. `delta 17(q)`'s
   G2/G3 remain open at Prop 5.5(k) level; the charged
   `descent-radii` lane's `COUNTING-BOUND` is unchanged, not consumed.
7. **What PROP 5.5(k) does buy.** With the corrected modulus plus a **new**
   step (Prop A.5 simplicity, §2.4, which Moh does not use at p.188):
   `k = 0` kills 980/980 admissible rows (`n' ≤ 60`), i.e. it reproves
   Prop 5.5; `k = 1..8` kills **88.4–95.5 %** (`n' ≤ 60`), leaving `74, 135, 167, 213,
   208, 251, 239, 257` rows. All five p.207 rows and G2/G3 are in the
   residue. The residual set is characterised exactly: it is the set where
   the two `π`-degrees have residues `{0,1}` mod `𝔄`. §3.5.
8. **Reading.** Theorem (T) does **not** follow from Prop 5.5(k) at
   `δ_2' = −1`, and the obstruction is named exactly (§4.1): the
   degeneration of Def 5.1(2)'s window from `V_2 > d_2/2` to
   `V_2' > d_2'/(k+2)`. Appendix II's extra data **is** what fills it, and
   I state the uniform form as `CONJ[APPII-UNIFORM]` with its exact datum
   (§4.2): the approximate root `h` of degree `d_2'`, the remainder-degree
   bounds, and the minor-disc root split.

## 1. Task (1) — Prop 5.5 rederived for `J = c`, and where the Jacobian enters

`SOURCE-READ` p.186–188. Setting: `g` monic in `y`, `deg_y g = n > 2`,
`s = 2` effective characteristic pairs. Moh argues by contradiction against
the alternative "`k[x,y] = k[T_1^ψ,g] = k[f,g]` or a degree-reducing
automorphism exists".

**(1a) Prop 5.4 ⇒ `δ_2 = −1`, `M_2 = n−2`.** `M_1 = μ_1 = −deg_y T_1^ψ < 0`
forces `i = 2`; then `δ_2 = −1/(n−M_2−1) = −1`, so `n − M_2 = 2` (p.187 top,
p.186 bottom). *Jacobian entry #1*: the numerator `−1` of
`δ_s = −1/(n−M_s−1)` is Prop 5.1(2), p.174, whose proof invokes Prop 4.4
and Prop 4.6.

**(1b) The window.** p.186: `V_3 d_2/d_3 > V_2 > d_2/(n−M_2) = d_2/2` and
`V_3 = d_3`, so `d_2 > V_2 > d_2/2`, hence
`0 < U_2 = d_2 − V_2 < V_2` (p.187). *No Jacobian*: Def 5.1(2) is a
root-count (major-disc) condition. The strict upper bound `V_2 < d_2` is
also forced independently: `V_2 = d_2` would put all `n` roots of `g` in
`D_1` (Def 5.1(1)), so every pair of roots would have
`ord ≥ δ_1 > δ_2`, contradicting `δ_2 = min ord(τ_i−τ_j)` (Prop 5.1, p.173).

**(1c) The radius fraction.** Def 5.1(3), p.179, at `s = 2`, `i = 1`, with
`n − M_2 = 2`, `n − M_2 − 1 = 1`:
```
   δ_1 = 1 − (n−M_1)[2V_2 − d_2] / [V_2(n−M_1) − d_2]
       = [(n−M_1)(d_2−V_2) − d_2] / [(n−M_1)V_2 − d_2] ,
```
and with `n = d_2 n*`, `−M_1 = d_2 m*`, so `n−M_1 = d_2(n*+m*) = d_2Π`:
```
   δ_1 = (Π U_2 − 1)/(Π V_2 − 1) = B/A  (reduced) ,     0 < δ_1 < 1 , A > 1 .
```
*Jacobian entry #2*: the `1` subtracted in `n−M_s−1` — the only
inhomogeneous constant in Def 5.1(3).

**(1d) The `r = 1` ODE.** Prop 4.6, p.170 (`SOURCE-READ`): if `r = 1` then
`D(n, −M_1, g_σ(π), T^ψ_{1,σ}(π)) = nonzero constant`, with
`D(a,b,p,q) = a p q' − b q p'` (Def 4.1, p.164). *Jacobian entry #3, and it
is the root of all the others.* Prop 4.1, p.164:
```
   J_{t,π}(h(σ), g(σ)) = [λ_h h_σ g'_σ − λ_g g_σ h'_σ] t^{λ_g+λ_h−1} + ⋯
                       = J_{f,g}(h,g)·J_{x,y}(f,g)·J_{t,π}(x,σ)
                       = h_f(σ) · 1 · (−t^{δ−2}) .
```
The `1` **is** `J_{x,y}(f,g)`. Matching `t`-orders of the two displayed
lines is precisely Prop 4.6(3): `(n−M_r)λ = −1 + δ`; Moh spells this
equivalence out on p.170. With `r = 1`, `λ_g = nλ`, `λ_h = (−M_1)λ`,
`ord h_f(σ) = (−μ_1+M_1)λ = 0`, the bracket is `−λ·D(n,−M_1,g_σ,T_{1,σ})`
and the leading coefficient of `h_f(σ)` is a nonzero constant, giving
`D(…) = nonzero constant`.

**(1e) Coprimality.** From `D(n,−M_1,g_σ,T_{1,σ}) = c ≠ 0` directly: a
common root `ξ` of `g_σ, T_{1,σ}` gives `D(ξ) = 0`; a common root of
`g'_σ, T'_{1,σ}` likewise. So
`(g_σ,T_{1,σ}) = 1` **and** `(g'_σ,T'_{1,σ}) = 1` (p.187). Prop A.5,
p.207 (`SOURCE-READ`) gives the strictly stronger
`g_σ(π)·T_{1,σ}(π)` **has only simple roots** — Moh states A.5 here but
uses only the two coprimalities.

**(1f) Galois / the alternatives (p.188).** `deg g_σ = n*V_2`,
`deg T^ψ_{1,σ} = m*V_2` (Def 5.1(1), p.179; Prop 4.6(4),(5), p.170).
With `σ = Σa_jt^j + πt^{δ_1}` and the `a_j` in `k((t))`, the generator `ζ`
of `Gal(k((t^{1/A}))/k((t)))` acts on `σ` by `π ↦ ξ^Bπ`, whence
`g_σ(ξ^Bπ) = ξ^{Aλ_g} g_σ(π)`: **every `π`-exponent of `g_σ` is `≡ deg g_σ`
mod `A`**, and likewise for `T_{1,σ}`. Moh's two consequences: `A ∤ n*V_2 ⇒
π | g_σ`; `A | n*V_2 ⇒ g_σ ∈ k[π^A] ⇒ π | g'_σ`. With (1e) this forces the
exclusive alternative
```
   (12)   A | n*V_2  and  A ∤ m*V_2      or      (13)   A ∤ n*V_2  and  A | m*V_2 .
```

**(1g) The inequality chain.** `A | ΠV_2 − 1` by construction. In branch
(12), `A | m*V_2 − 1`; put `n*V_2 = AD`, `m*V_2 − 1 = AE`. Then
`(A,V_2) = 1`, `V_2 | D`, `A(D+E) = ΠV_2 − 1`, and with
`1 − δ_1 = Π(V_2−U_2)/(ΠV_2−1) = ΠC/A`, `V_2 − U_2 = C(D+E)`:
```
   V_2 > V_2 − U_2 = C(D+E) ≥ D ≥ V_2 .     Contradiction.  (p.188)
```
Branch (13) is symmetric. **Restated divisibility-style** (equivalent, and
the form that generalises): put `g_0 = gcd(ΠU_2−1, ΠV_2−1)`, so
`A = (ΠV_2−1)/g_0` and `D+E = g_0`. Then
```
   g_0 ≥ V_2   (from V_2 | D, resp. V_2 | E)   and   g_0 | 2V_2 − d_2 ,
```
the second because `g_0 | V_2(ΠU_2−1) − U_2(ΠV_2−1) = U_2 − V_2`. Since
`0 < 2V_2 − d_2 = V_2 − U_2 < V_2 ≤ g_0`, a positive integer would divide a
strictly smaller positive integer. ∎

**Summary of Jacobian entry points.** #3 (Prop 4.1's factor
`J_{x,y}(f,g) = 1`) is primitive; #1 and #2 are consequences of it, and
Lemma 5.2 (p.178) and Def 5.1(3) are *identities* that merely carry the
constant `−1` forward. The Galois step (1f) and the window (1b) are
**Jacobian-free**. Lemma 2.1 (p.151) is the same statement in the `η`-gauge:
`J = c` iff `f_i'(x) = 0` for `i < n−1` and `deg_x f_{n−1} = 1`.

## 2. Task (2) — the generalisation to `J = cγ^k`

### 2.1 Lemma 2.1(k) `PROVED-HERE`

With `g = η^{-n}`, `f = η^{-m} + Σ_{i>−m} f_i(x)η^i` (Moh p.151), the chain
rule gives `J_{x,y}(f,g) = n(Σ f_i'(x)η^i)(η^{-n+1}+⋯)`, the second factor a
unit of `k[x][[η]]` times `η^{-n+1}`. Hence
```
   J_{x,y}(f,g) = c x^k   ⟺   f_i'(x) = 0  (i < n−1)   and   f'_{n−1}(x) = (c/n)x^k
                          ⟺   ord_η(Σf_i(x)η^i − Σf_i(0)η^i) = n−1  and  deg_x f_{n−1} = k+1 .
```
So `deg_x f_{n−1}` goes `1 ↦ k+1`; `M_i ≤ n−1` still holds
(`f_{n−1} ≠ 0`, `gcd(n,n−1) = 1`).

### 2.2 Prop 4.1(k), 4.4(k), 4.6(k) `PROVED-HERE`

`J_{t,π}(x,σ) = x_tσ_π − x_πσ_t = −t^{δ−2}` is Jacobian-free. Substituting
`J_{x,y}(f,g) = c x^k = c t^{-k}` into Prop 4.1's chain rule:
```
   J_{t,π}(h(σ),g(σ)) = [λ_h h_σ g'_σ − λ_g g_σ h'_σ] t^{λ_g+λ_h−1} + ⋯ = −c·h_f(σ)·t^{−2+δ−k} .
```
Equating the two `t`-orders, exactly as Moh does on p.170:
```
   λ_g + λ_h − 1 = ord h_f(σ) − 2 + δ − k
   ⟺  nλ + (−μ_r)λ − 1 = (−μ_r+M_r)λ − 2 + δ − k
   ⟺  (n−M_r)λ = −(k+1) + δ .
```
Therefore **Prop 4.6(3) becomes `λ = (−(k+1)+δ)/(n−m_r)`, and Prop 4.4(3),(6)
acquire `−(k+1)` in place of `−1`.** Conditions (1),(2),(4),(5),(7) of
Prop 4.4 and (1),(2),(4),(5) of Prop 4.6, and all five conclusions of
Prop 4.6 for `r ≥ 2`, are untouched: they are degree/root-count/
distribution-detector statements with no `J` in them.

### 2.3 The `r = 1` ODE is unchanged `PROVED-HERE`

For `r = 1` the bracket in Prop 4.1(1) is `−λ·D(n,−M_1,g_σ,T_{1,σ})` at
`t`-order `(n−M_1)λ − 1 = (−(k+1)+δ) − 1 = δ−k−2`, and the right side is
`−c·h_f(σ)t^{−2+δ−k}` with `ord h_f(σ) = (−μ_1+M_1)λ = 0` and constant
leading coefficient. The two orders **agree identically**, so
```
   D(n', −M_1', ḡ_σ(π), T^{ψ}_{1,σ}(π))  =  nonzero constant           (unchanged; value c/λ)
```
The monomial `γ^k` is absorbed entirely into the modified `λ` (equivalently
into `δ_1'`); no `π^k` appears. Consequently **Prop A.5 and both
coprimality conclusions of §1(1e) hold verbatim for every `k`.** This is the
precise sense in which the campaign's exact machinery is "monomial-Jacobian
tolerant" — and it is stronger than the `delta 17(i)` additivity statement:
the ODE is not merely shifted, it is *invariant*.

### 2.4 Φ, and the closed form `PROVED-HERE`

The radius system consists of: `ord g(σ) = nλ`, `ord T_i^ψ(σ) = (−μ_i)λ`
(p.181) — linear homogeneous in the `δ`'s — closed by the single relation
`(n−M_r)λ = −(k+1) + δ`. Scaling `(δ,λ) ↦ (k+1)(δ,λ)` maps the `k = 0`
system to the `k` system bijectively. Hence
```
   Φ :   δ_i^{(k)} = (k+1) · δ_i^{(0)} = (k+1) · Def 5.1(3)(n',m',M_2',V_2'; s'=2) .
```
This **proves** the rule fitted in the charged `descent-radii` lane, and
explains why raw Def 5.1(3) misses p.207 by exactly the factors `2,2,2,3,3`.
Evaluating at `s' = 2` with `R = n'−M_2'−1`, `Π = n*+m*`, `U_2' = d_2'−V_2'`:
```
   δ_2' = −(k+1)/R ,
   δ_1' = (k+1)·[ Π U_2' − R ] / [ R (Π V_2' − 1) ] .
```
Special cases: `δ_2' = −1 ⇔ R = k+1 ⇔ M_2' = n'−k−2`, and then
`δ_1' = (ΠU_2' − (k+1))/(ΠV_2' − 1)` — Moh's `B/A` with the single
substitution `1 ↦ k+1` **in the numerator only**. `δ_2' = −1/2 ⇔ R = 2(k+1)`
and then `δ_1' = (ΠU_2' − 2(k+1))/(2(ΠV_2'−1))`.

`prop55k.py` §A: closed form `==` `(k+1)·`Def 5.1(3) `==` printed p.207 on
**all ten** rationals (`box/prop55k-drivers-20260903/prop55k.out`).

Two facts that break Moh's `k = 0` hypotheses and must be carried:

* **`δ_1'` need not be a true fraction.** `7/6` and `4/3` are printed on
  p.207. So `0 < δ_1 < 1 ⇒ A > 1` (p.187) is unavailable; `A' > 1` becomes a
  hypothesis. Bounded cost: `A' = 1` on `0, 0, 29, 33, 51, 68, 72, 78, 99`
  rows for `k = 0..8` at `n' ≤ 60`. Typed `OPEN[A-PRIME-ONE]`.
* **The window opens.** Def 5.1(2) gives `V_2' > d_2'/(n'−M_2') = d_2'/(R+1)`;
  at `k = 0, R = 1` this is `V_2 > d_2/2` hence `U_2 < V_2`, but for `k ≥ 1`
  it is only `V_2' > d_2'/(k+2)`, and `U_2' > V_2'` does occur (p.207 rows
  `(21,14) V_2'=2` with `U_2'=5`, `(15,10) V_2'=2` with `U_2'=3`).
  The upper bound `V_2' < d_2'` survives (§1(1b), Jacobian-free).

### 2.5 The Galois step, corrected `PROVED-HERE` + `CORRECTED-HERE`

Moh's p.188 argument needs the common part `Σa_jt^j` of `σ_1` to lie in
`k((t))`, i.e. to have **integral** `t`-exponents; only then does the
generator of `Gal(k((t^{1/A'}))/k((t)))` fix it and act on `σ` by
`π ↦ ξ^{B'}π`. Moh does supply this for his own rows: p.207,
"*We shall choose `x, y` properly so that the `π`-root in the major disc
`D_1` is of the following form `σ = πt^{1/4}`*" — no `a_j` at all
(`SOURCE-READ`; this is also the hypothesis shape of Prop 5.6, p.188).

Let `L` be the index of the lattice generated by `ℤ` and the exponents of
the common part, and let `A' = denom(δ_1')`. Working over `K = k((t^{1/L}))`,
the least `𝔄` with `𝔄 δ_1' ∈ (1/L)ℤ` is
```
   𝔄 = A' / gcd(A', L)          [ Moh p.188 is the case L = 1, 𝔄 = A' ] ,
```
and `Gal(K(t^{1/(L𝔄)})/K)` acts by `π ↦ ζπ` with `ζ` of order `𝔄`. Hence:

> **Lemma G(k).** Every `π`-exponent of `ḡ_σ` is `≡ deg ḡ_σ = n*V_2'`
> (mod `𝔄`); every `π`-exponent of `T^ψ_{1,σ}` is `≡ deg T^ψ_{1,σ} = m*V_2'`
> (mod `𝔄`).

`L ≥ denom(δ_2')` always (`δ_2'` is the leading exponent of the common part
when the top disc's leading coefficients differ, which they do by
`δ_2' = min ord(τ_i−τ_j)`). When `δ_2' = −1`, `denom(δ_2') = 1` and the
leading exponent is integral; whether an *intermediate* fractional exponent
can appear before `δ_1'` (a ramification without a major disc) is typed
`OPEN[COMMON-PART-LATTICE]`. All 20 controls of §3.3 realise
`L = denom(δ_2')` exactly. **The lane uses `L = denom(δ_2')`, which is
fail-closed on every control; the uncorrected `L = 1` is not (§3.4).**

Two new consequences beyond Moh's (12)/(13):

* **XOR (Moh).** Writing `r_g = n*V_2' mod 𝔄`, `r_T = m*V_2' mod 𝔄`: if
  `r_g = r_T = 0` then `π | ḡ'_σ` and `π | T'_{1,σ}` (needs `𝔄 ≥ 2`);
  if `r_g ≠ 0 ≠ r_T` then `π | ḡ_σ` and `π | T_{1,σ}`. Either contradicts
  `D = c ≠ 0` (§2.3). So exactly one of `r_g, r_T` is `0`.
* **A5-SIMPLE (new).** Lemma G(k) gives `π^{r_g} ‖ ḡ_σ` (the least exponent
  present is `≡ r_g` mod `𝔄`, hence `≥ r_g`). Prop A.5 (p.207) says
  `ḡ_σ·T_{1,σ}` has only **simple** roots, so `r_g ≤ 1` and `r_T ≤ 1`.
  Combined with XOR: **`{r_g, r_T} = {0,1}` mod `𝔄`.** Moh does not use
  this at p.188; it is what makes the `k = 0` census close by pure
  arithmetic (§3.5) and is the sharpest level-1 statement available.

### 2.6 PROP 5.5(k) — statement

> **PROP 5.5(k).** Let `(P,Q) ∈ k[γ,π]` be monic in `π` with
> `deg P = deg_π P = m'`, `deg Q = deg_π Q = n' > m' ≥ 2`, and
> `J_{γ,π}(P,Q) = cγ^k`, `c ≠ 0`. Suppose its tree data satisfies Def 5.1
> with `s' = 2`, characteristic pairs `M_1' = −m'`, `M_2'`, disc numbers
> `V_2'`, `V_3' = d_3'`, and put `d_2' = gcd(n',m')`, `n* = n'/d_2'`,
> `m* = m'/d_2'`, `Π = n*+m*`, `U_2' = d_2'−V_2'`, `R = n'−M_2'−1`, and
> `δ_1' = (k+1)(ΠU_2'−R)/(R(ΠV_2'−1)) = B'/A'` reduced. Assume `A' > 1`
> and let `𝔄 = A'/gcd(A',L)` with `L` as in §2.5. Then
>
> **(i)** `{ n*V_2' mod 𝔄 , m*V_2' mod 𝔄 } = {0, 1}`;
> **(ii)** if in addition `δ_2' = −1` (i.e. `R = k+1`) and `L = 1`, then with
> `g_0 = gcd(ΠU_2' − (k+1), ΠV_2' − 1)`,
> ```
>    g_0 ≥ V_2'   and   g_0 | (k+2)V_2' − d_2' ,     hence     d_2' ≤ (k+1)V_2' ;
> ```
> **(iii)** always `V_2' < d_2'` and `V_2' > d_2'/(R+1)`.
>
> For `k = 0`, (ii) reads `d_2 ≤ V_2` and contradicts (iii): **no such pair
> exists**, which is Moh's Prop 5.5. For `k ≥ 1`, (ii) and (iii) are jointly
> satisfiable and (ii) is **sharp** (§3.3, family A attains equality).

*Proof of (ii).* Branch `𝔄 | n*V_2'`: since `A' | ΠV_2'−1` (the denominator
before reduction, `R = k+1`) we get `A' | m*V_2'−1`, so `(A',V_2') = 1` and
`V_2' | D` where `n*V_2' = A'D`; with `m*V_2'−1 = A'E ≥ 0`,
`A'(D+E) = ΠV_2'−1` gives `D+E = g_0 ≥ D ≥ V_2'`. The other branch is
symmetric with `V_2' | E`. And
`g_0 | V_2'(ΠU_2'−(k+1)) − U_2'(ΠV_2'−1) = U_2' − (k+1)V_2'`, i.e.
`g_0 | (k+2)V_2' − d_2'`, which is `> 0` by (iii). Combining,
`V_2' ≤ g_0 ≤ (k+2)V_2' − d_2'`. ∎

`PROVED-HERE`, modulo the two typed `OPEN`s (`A-PRIME-ONE`,
`COMMON-PART-LATTICE`) and the `SOURCE-READ` inputs Def 5.1(1)(2)(3),
Prop 4.1, 4.6, A.5.

## 3. Task (3) — tests

### 3.1 Moh's five descended rows (p.207) — **none is killed**

`prop55k.py` §B. `Π U_2'`, `R`, `δ`, `A'`, `𝔄`, residues `(r_g,r_T)`:

| row (`n',m'; M_2'; V_2'; J`) | `d_2'` | `Π` | `U_2'` | `R` | `δ_2'` | `δ_1'` | `A'` | `𝔄` | `(r_g,r_T)` | verdict |
|---|---|---|---|---|---|---|---|---|---|---|
| `(16,12; 13; 3; X)` | 4 | 7 | 1 | 2 | `−1` | `1/4` | 4 | 4 | `(0,1)` | SURVIVES |
| `(21,14; 16; 2; X)` | 7 | 5 | 5 | 4 | `−1/2` | `7/6` | 6 | 3 | `(0,1)` | SURVIVES |
| `(21,14; 18; 5; X)` | 7 | 5 | 2 | 2 | `−1` | `1/3` | 3 | 3 | `(0,1)` | SURVIVES |
| `(15,10; 11; 3; X²)` | 5 | 5 | 2 | 3 | `−1` | `1/2` | 2 | 2 | `(1,0)` | SURVIVES |
| `(15,10; 11; 2; X²)` | 5 | 5 | 3 | 3 | `−1` | `4/3` | 3 | 3 | `(0,1)` | SURVIVES |

Every row sits exactly on `{r_g,r_T} = {0,1}`, and on the three `δ_2' = −1`
rows the divisibility chain of PROP 5.5(k)(ii) is **saturated**
(`g_0 = (k+2)V_2'−d_2'` = `5, 8, 3` respectively). **PROP 5.5(k) does not
kill Moh's rows uniformly, or at all.** That is consistent — Appendix II
exists precisely because they survive the `§5` machinery.

### 3.2 `D = 105` descended G2, G3 — **not killed** (negative reported)

`G2 = (15,10; 4; 1; X⁴)`: `R = 10`, `δ_2' = −1/2`, `δ_1' = 5/4`
(this reproduces the charged `descent-radii` §4.3 value independently, from
the closed form), `A' = 4`, `L = 2`, `𝔄 = 2`,
`(deg ḡ_σ, deg T_σ) = (n*V_2', m*V_2') = (3,2)`, residues `(1,0)`.
`G3 = (21,14; 8; 1; X²)`: `R = 12`, `δ_2' = −1/4`, `δ_1' = 9/8`, `A' = 8`,
`L = 4`, `𝔄 = 2`, degrees `(3,2)`, residues `(1,0)`.
Both satisfy `{r_g,r_T} = {0,1}`: **SURVIVE**.

Under the uncorrected modulus `𝔄 = A'` both are killed four ways
(`GALOIS`-neither, two `A5-SIMPLE`, `MONOMIAL`: `A' > max(3,2)` forces both
leading forms to be `π`-monomials, whence `D ≡ 0`). I held that as the
lane's headline for about twenty minutes; the controls of §3.3 refuted it.
`lattice_compare.out` records both.

### 3.3 Fail-closed controls — genuine pairs with `J = cγ^k`

The full solution set of `J_{γ,π}(P,Q) = cγ^k` for
`P = φ(π) + eγ^{k+1}` (φ monic, `deg φ = m' ≥ k+1`) is `Q = aπ + ψ(P)`
(`ker J(P,−) = k[P]`; `J(P, aπ) = a P_γ = ae(k+1)γ^k`). This gives an
explicit, **existing** family in Moh's descended gauge:
```
   P = φ(π) + e γ^{k+1} ,    Q = ψ(P) + a π ,     J = a e (k+1) γ^k ,
   deg P = deg_π P = m' ,    deg Q = deg_π Q = n' = q m' ,   both monic in π .
```
For each of 20 instances `control_pairs.py` (i) checks `J` exactly as a
`γ`-monomial and the gauge `deg = deg_π`, (ii) reads `(M_1',M_2',d_i')` from
the `η`-expansion with the charged `etaexp.char_data` (rerun from the frozen
copy), (iii) **measures** `δ_2'`, `δ_1'` and the `Q`-root cluster sizes by
`mpmath` Newton–Puiseux at `dps = 200` with a two-point fit
(`X = 30, 10⁴`), (iv) reads `V_2'` off Def 5.1(1), (v) compares with the
closed form, (vi) runs the test.

Results (`control_pairs.out`): **20/20 have `s' = 2`; 19/20 match the closed
form to `< 2·10⁻³`** and the 20th (`C3`, `δ_1' = 7/2`) to `7·10⁻³`,
monotonically improving with the fit range — a slow-log artefact, not a
disagreement. **0/20 are killed by PROP 5.5(k).** Sample:

```
  P = π²−γ², Q = P²+aπ   (n',m')=(4,2)  k=1  M_2'=1  V_2'=1  δ'=(−1, 1/2)   SURVIVES
  P = π³−γ³, Q = P³+aπ   (n',m')=(9,3)  k=2  M_2'=5  V_2'=1  δ'=(−1, 5/3)   SURVIVES
  P = π⁵−γ⁵, Q = P³+aπ   (n',m')=(15,5) k=4  M_2'=9  V_2'=1  δ'=(−1, 11/3)  SURVIVES
  P = π⁴−γ², Q = P²+aπ   (n',m')=(8,4)  k=1  M_2'=3  V_2'=1  δ'=(−1/2, 5/4) SURVIVES
  P = π⁶−γ³, Q = P²+aπ   (n',m')=(12,6) k=2  M_2'=5  V_2'=1  δ'=(−1/2, 9/4) SURVIVES
  P = π⁵+π³+7−γ⁵, Q = P²+3P+π  (10,5)   k=4  M_2'=4  V_2'=1  δ'=(−1, 7/2)   SURVIVES
```

**Sharpness.** Family A (`φ = π^{k+1}`, `e = −1`, `ψ = u^q`) has
`d_2' = m' = k+1`, `n* = q`, `m* = 1`, `V_2' = 1`, `U_2' = k`,
`Π = q+1`, `R = k+1`, and closed form
`δ_1' = ((q+1)k − (k+1))/q = (qk−1)/q` — reproduced by hand from the
Puiseux expansion `ε ∼ X^{(1−q k)/q}` and by the numerics. Its
PROP 5.5(k)(ii) data is `g_0 = 1 = V_2'` and
`(k+2)V_2' − d_2' = 1`, i.e. **`d_2' = (k+1)V_2'` with equality**. So
inequality (ii) cannot be strengthened for any `k ≥ 1`. This is also an
independent confirmation of Φ on objects that exist, complementing the
charged lane's confirmation on Moh's printed shapes.

I did not use the `autoscan2.py` population as the control source: those
are parent-gauge automorphisms with constant Jacobian, and the charged
`descent-radii` §1.3 already established that all twelve fail Moh's (4) or
(6) and are disjoint from the census, so descending them would produce
nothing in the target class. The family above exhibits the descended
objects directly and is cheaper and sharper.

### 3.4 The control that broke the first version (`CORRECTED-HERE`)

`P = π⁴ − γ²`, `Q = P² + π`, `J = −2γ`. Exactly:
`(n',m') = (8,4)`, `d_2' = 4`, `n* = 2`, `m* = 1`, `M_2' = 3`, `V_2' = 1`,
`δ_2' = −1/2`, `δ_1' = 5/4`, `A' = 4`, `deg ḡ_σ = 2`, `deg T_σ = 1`.
Under `𝔄 = A' = 4` the test says both leading forms are `π`-monomials, hence
`D ≡ 0` — but the pair exists. Computing the leading forms by hand with
`σ = ω t^{-1/2} + π t^{5/4}` (`ω⁴ = 1`):
```
   P(σ) = 4ω³π t^{-1/4} + 6ω²π² t^{3/2} + ⋯      →  T_σ(π) = 4ω³π
   Q(σ) = (16ω⁶π² + ω) t^{-1/2} + ⋯              →  ḡ_σ(π) = 16ω⁶π² + ω
```
`ḡ_σ` has exponents `{0,2}` — one class mod **2**, not mod 4. The centre
`ωt^{-1/2}` is not in `k((t))`, so `L = 2` and `𝔄 = 2`. Hence §2.5.

Independent confirmation from Moh himself: under `𝔄 = A'` his own printed
row `(21,14; 16; 2; X)` (`δ_2' = −1/2`, `A' = 6`, degrees `(6,4)`,
residues `(0,4)`) would be killed at Prop 5.5 level by `A5-SIMPLE`
(`π⁴ ‖ T_σ`), and Appendix II's 373-coefficient reduction on that case would
be pointless. With `𝔄 = 3` it survives with residues `(0,1)`. Both checks
agree that `L = denom(δ_2')`, not `1`, on `δ_2' ≠ −1` rows.

### 3.5 Census — bounded quantity

`census55k.py`, `δ_2' = −1` rows (`R = k+1`), `n' ≤ 60`, all `m'` with
`gcd ≥ 2`, all `V_2'` admissible under Def 5.1(2) and `V_2' < d_2'`:

```
   k   rows  A'=1  killed     %     survivors      residual = {r_g,r_T} = {0,1}
   0    980     0    980  100.0 %         0                  0
   1   1654     0   1580   95.5 %        74                 74
   2   1936    29   1772   92.9 %       135                135
   3   2082    33   1882   91.8 %       167                167
   4   2169    51   1905   89.9 %       213                213
   5   2228    68   1952   90.4 %       208                208
   6   2264    72   1941   88.5 %       251                251
   7   2288    78   1971   89.2 %       239                239
   8   2306    99   1950   88.4 %       257                257
```

Readings:

* `k = 0`: **980/980**, and (extended to `n' ≤ 120`) `5338/5338`. So
  PROP 5.5(0) reproves Prop 5.5. The kill is always by the Galois step
  alone — Moh's XOR is *arithmetically unsatisfiable* at `k = 0`; his chain
  §1(1g) is exactly the proof of that unsatisfiability, and my census is an
  independent numerical verification of it for `n' ≤ 120`.
* `k ≥ 1`: the survivor set equals the XOR set exactly, and the
  divisibility chain (ii) kills **none** of it — on every survivor
  `g_0 ≥ V_2'` and `g_0 | (k+2)V_2'−d_2'` hold (with `g_0` a proper divisor
  of `(k+2)V_2'−d_2'` in 1047 of 2333 survivors at `n' ≤ 80`, an equality in
  the other 1286). **Moh's inequality contributes nothing once `k ≥ 1`.**
* The whole cut for `k ≥ 1` comes from Lemma G(k) + A5-SIMPLE, i.e. from the
  *leading-form* level of the expansion.

## 4. Task (4) — reading

### 4.1 PROP 5.5(k) fails for `k ≥ 1`; the exact obstruction

Theorem (T) at `δ_2' = −1` does **not** follow from Prop 5.5(k). The
obstruction is a single, nameable degeneration, and it is **not** in the
Galois part, **not** in the ODE, and **not** in Appendix I:

> **OBSTRUCTION-55(k).** Def 5.1(2)'s lower window
> `V_i > d_i/(n−M_i)` reads `V_2 > d_2/2` at `k = 0` because
> `δ_2 = −1` forces `n−M_2 = 2`, and reads `V_2' > d_2'/(k+2)` at exponent
> `k` because `δ_2' = −1` forces `n'−M_2' = k+2`. Moh's chain closes only
> because `U_2 < V_2`; for `k ≥ 1` the window admits `U_2' ≤ k V_2'`, and
> the chain closes to the *consistent* statement `d_2' ≤ (k+1)V_2'`.

It is realised: `P = π^{k+1} − γ^{k+1}`, `Q = P^q + aπ` exists, has
`δ_2' = −1`, `s' = 2`, and attains `d_2' = (k+1)V_2'` (§3.3). So no
sharpening of Moh's §5 argument at the leading-form level can produce a
contradiction at `δ_2' = −1` for `k ≥ 1`. Secondary, smaller residues:
`OPEN[A-PRIME-ONE]` (`A' = 1`, `≤ 4.3 %` of rows at `k ≤ 8`) and
`OPEN[COMMON-PART-LATTICE]` (whether `L > denom(δ_2')` can occur, which
would only *weaken* the test further).

For `δ_2' ≠ −1` the situation is strictly worse: `L > 1` shrinks the Galois
modulus to `𝔄 = A'/gcd(A',L)`, and both p.207's `(21,14) V_2'=2` row and
the `D = 105` pairs G2/G3 land in the residue.

### 4.2 What fills it: `CONJ[APPII-UNIFORM]` and its datum

Appendix II (`SOURCE-READ` pp.207, 208, 210) does **not** use more of the
leading form. It descends one level, to the *approximate root and its
remainders*, and it uses the **minor disc**. Concretely, on
`(15,10; 11; 3; X²)` (p.210) Moh writes
```
   f = h² + 2β ,   g = h³ + 3βh + (3/2)α + (3/2)γ/h − (1/2)β³/h³ + a/h + b f^{-6/10} + c(x³+d*) f^{-9/10} + ⋯
   β² = α h + γ ,  deg_y γ < deg_y h = 5 ,
```
and then **the requirement that the `y^{-1}`-power series cancel** (because
`g` is a polynomial) produces the exact identities
`β³ = (3γ+a)h² + εh + δ` with `deg_y ε, δ < 4` and
`(1/2)ε = b f^{4/10} + c(x³+d*)y + O(y^{-1})`; combined with
"*the inverse transformation implies that there is a minor disc, thus in the
major disc `D_2` there are precisely three subdiscs which contain 2, 2, 6 of
roots `f`*" and "*the datum `δ_1 = 1/2` implies that the polynomial* `h` …".
On `(16,12; 13; 3; X)` (p.208) the same shape with
`ḡ = h⁴+α_1h³+α_2h²+α_3h+α_4`, `f̄ = h³+β_2h+β_3`, `deg_y h = 4`,
`deg_y α_i, β_i ≤ 3`, `ord α_i(σ) ≥ i(−1/4)`, `ord h(σ) ≥ −1/4`.

Note `deg_y h = 4 = d_2'` on `(16,12)` and `deg_y h = 5 = d_2'` on
`(15,10)`, with `ḡ = h^{n*} + ⋯`, `f̄ = h^{m*} + ⋯` in both. That is the
uniform statement:

> **`CONJ[APPII-UNIFORM]`.** Let `(P,Q)` be a descended pair as in
> PROP 5.5(k), and let `h` be the approximate root of `P` of degree
> `d_2' = gcd(n',m')`, so that `P = h^{m*} + Σ_{i≥2} β_i h^{m*−i}` and
> `Q = h^{n*} + Σ_{i≥1} α_i h^{n*−i}` with `deg_π α_i, β_i < d_2'`. Then the
> level-1 data (`ḡ_σ, T_{1,σ}`, `D = c`, Lemma G(k)) is consistent whenever
> PROP 5.5(k) is; and the pair is excluded by the **level-2 datum**
> ```
>    (a)  the order conditions  ord α_i(σ) ≥ i·δ_1^{min} ,  ord β_i(σ) ≥ i·δ_1^{min}
>         forced by δ_1' (Moh's "ord α_i(σ) ≥ i(−1/4)", p.208) ;
>    (b)  the remainder-degree bounds  deg_π α_i, deg_π β_i < d_2'
>         and the second-level bounds (deg_y γ < d_2', deg_y ε,δ < d_2'−1) ;
>    (c)  the minor-disc root split of P inside D_2, forced by the inverse
>         of Prop 6.3 (u_s = 1) — for (15,10) the 2,2,6 split ;
> ```
> together with the polynomiality of `Q` (cancellation of the `π^{-1}`-tail).
> Bounded quantity: (a)+(b)+(c) is exactly the datum Appendix II uses, and
> the conjecture is that it suffices **uniformly in `k`**.

The cheapest test of the conjecture: instantiate (a)–(c) on **G2**
(`15,10; 4; 1; X⁴`, `d_2' = 5`, `n* = 3`, `m* = 2`, `δ_1' = 5/4`,
`δ_2' = −1/2`) and on one member of the control family that must **not**
die (`P = π⁵−γ⁵`, `Q = P³+aπ`, `d_2' = 5`, `n* = 3`, `m* = 1`), i.e. run
the (15,10)-shaped Appendix-II reduction of p.210 with the descended
`δ_1' = 5/4` order conditions instead of `1/2`. That is a bounded
`< 30 min` computation of the same size as the charged `descent-radii`
§4.3 slice, and it is the one experiment I would spend the next lane on. I
did **not** run it here: it is Appendix-II work, outside this lane's charge,
and the charged lane's `COUNTING-BOUND` on G2/G3 stands unconsumed.

### 4.3 Status of theorem (T)

* **`δ_2' = −1`, `k = 0`:** Prop 5.5 as printed. Nothing new needed.
* **`δ_2' = −1`, `k ≥ 1`:** (T) does **not** follow. PROP 5.5(k) reduces
  every such descended pair to the finite condition
  `{n*V_2', m*V_2'} ≡ {0,1}` mod `𝔄` together with `d_2' ≤ (k+1)V_2'` and
  `g_0 | (k+2)V_2'−d_2'`, `g_0 ≥ V_2'` — an **88.4–95.5 % screen** (`n' ≤ 60`), not a kill.
  The residue is `74/135/167/213/208/251/239/257` rows at `k = 1..8`,
  `n' ≤ 60`, and includes all of Moh's p.207 rows.
* **`δ_2' ≠ −1`:** additionally the Galois modulus degrades; G2/G3 sit here.
* **`u_s > 1`:** untouched — still `OPEN[MINOR-DICHOTOMY]`, not addressed
  by this lane.

So the flagship question is answered **negatively, with the obstruction
named and realised by an existing family**, and with the replacement
(Appendix II's level-2 datum) stated as a conjecture with its exact inputs.

## 5. Bounded quantities and OPEN accounting

| item | bounded quantity | cheapest test | status |
|---|---|---|---|
| Φ / closed form | 10 printed rationals + 20 measured pairs | `prop55k.py §A`, `control_pairs.py` | **PROVED-HERE**, 30/30 |
| `r=1` ODE under `γ^k` | 1 identity | order-match in Prop 4.1(1) | **PROVED-HERE** |
| PROP 5.5(k)(ii) | 1 inequality `d_2' ≤ (k+1)V_2'` | family A attains equality | **PROVED-HERE + sharp** |
| `OPEN[A-PRIME-ONE]` | `0..99` rows per `k ≤ 8`, `n'≤60` (max 4.3 %) | Prop 5.6 (p.188) covers `σ_1 = πt^{δ_1}` | typed OPEN |
| `OPEN[COMMON-PART-LATTICE]` | 1 yes/no: can `L > denom(δ_2')`? | expand `σ_1` for one `δ_2'=−1` census row to order `δ_1'` | typed OPEN |
| `CONJ[APPII-UNIFORM]` | 3 data (a),(b),(c) | p.210 reduction on G2 + control `π⁵−γ⁵` | typed CONJ |
| G2/G3 | 2 rows | above | **not killed here**; charged COUNTING-BOUND stands |
| `OPEN[MINOR-DICHOTOMY]` (`u_s>1`) | — | — | untouched by this lane |

No `OPEN` was filled by cap or analogy; no exit-price assertion is made in
this report, so no `charge_basis` line is due.

## 6. Typed block

```text
PROVED-HERE   Phi is forced: J_{x,y}(f,g)=1 in Prop 4.1 (p.164) is the only
              Jacobian entry point; J = c x^k sends the constant -1 of
              Prop 4.4(3),(6) / Prop 4.6(3) to -(k+1) and nothing else; the
              remaining system is linear homogeneous in delta, so
              delta_i^{(k)} = (k+1) delta_i^{(0)}.
PROVED-HERE   delta_2' = -(k+1)/R,  delta_1' = (k+1)(Pi U_2' - R)/(R(Pi V_2' - 1)),
              R = n'-M_2'-1, Pi = n*+m*.  Reproduces all ten p.207 rationals and
              is confirmed by Newton-Puiseux measurement on 20 existing pairs.
PROVED-HERE   D(n',-M_1',g_sigma,T_{1,sigma}) = nonzero constant is INVARIANT
              under J = c gamma^k (the constant changes, not the equation);
              hence Prop A.5 and both coprimality conclusions hold verbatim.
PROVED-HERE   PROP 5.5(k) as stated in section 2.6: {n*V_2', m*V_2'} = {0,1}
              mod A_frak ; and at delta_2' = -1, L = 1:
              g_0 >= V_2', g_0 | (k+2)V_2' - d_2', hence d_2' <= (k+1)V_2'.
              k = 0 => contradiction (Moh Prop 5.5, re-proved and
              census-verified 5338/5338 at n' <= 120).
PROVED-HERE   The bound d_2' <= (k+1) V_2' is SHARP for every k >= 1:
              P = pi^{k+1} - gamma^{k+1}, Q = P^q + a pi, J = -(k+1) a gamma^k,
              exists, has s'=2, delta_2'=-1, V_2'=1, d_2'=k+1.
CORRECTED-HERE Moh's p.188 Galois modulus A' is valid only for common part in
              k((t)).  Correct modulus A_frak = A'/gcd(A',L).  Using A' kills the
              existing pairs (8,4) and (12,6) above, and would kill Moh's own
              printed (21,14;16;2;X) row.  Lane uses L = denom(delta_2').
REFUTED-HERE  (own earlier claim, held ~20 min) "PROP 5.5(k) kills G2 and G3".
              It does not; that was the uncorrected modulus.
SOURCE-READ   Lemma 2.1 p.151; Prop 4.1 + Def 4.1 p.164; Prop 4.4 p.168;
              Prop 4.6 p.170; Prop 5.1(2) p.174; Lemma 5.2 p.178;
              Def 5.1 p.179; Prop 5.4 end p.186; Prop 5.5 pp.186-188;
              Prop 5.6 p.188; Prop 6.3 p.197; Prop A.1 p.202, Prop A.5 p.207;
              Appendix II pp.207, 208, 210 (table, order conditions,
              approximate-root remainders, 2/2/6 minor-disc split).
OPEN[A-PRIME-ONE]           A' = 1 makes the Galois step vacuous; 0..99 rows per
                            k <= 8 at n' <= 60.  Prop 5.6 is the natural cover.
OPEN[COMMON-PART-LATTICE]   can L exceed denom(delta_2') (intermediate
                            ramification with no major disc)?  Only weakens.
CONJ[APPII-UNIFORM]         section 4.2, with datum (a) order conditions from
                            delta_1', (b) remainder degrees < d_2', (c) minor-disc
                            root split; cheapest test named.
NOT-CONSUMED                AUDIT delta 17(p),(q),(r); charged descent-radii
                            COUNTING-BOUND on G2/G3; OPEN[MINOR-DICHOTOMY].
```

## 7. Drivers

`box/prop55k-drivers-20260903/`

* `prop55k.py` — Def 5.1(3), Φ, the closed form, and `TEST-55(k)` (Galois
  with the corrected modulus, A5-SIMPLE, monomial degeneracy, the
  divisibility chain); §A–§F self-tests including the `k = 0` positive
  control. Output `prop55k.out`.
* `census55k.py` — mechanism-by-mechanism census, `k = 0..8`, `n' ≤ 60`
  (and `k = 0`, `n' ≤ 120`). Output `census55k.out`.
* `control_pairs.py` — the existing-pair family, exact `J` and gauge checks,
  `etaexp.char_data` (charged driver, rerun from the frozen copy), `mpmath`
  Newton–Puiseux measurement at `dps = 200`, `V_2'` from Def 5.1(1), and the
  fail-closed verdict. Output `control_pairs.out`.
* `lattice_compare.out` — the two moduli side by side on G2, G3, the p.207
  `(21,14)` row and four controls; the record of the correction.
* `render_pages.sh` — re-renders exactly the 15 pages this lane opened
  (PDF pages 12, 25, 29, 31, 35, 39, 40, 42, 47, 48, 49, 58, 68, 69, 71);
  the PNGs themselves are not checked in. `moh.txt` — `pdftotext`
  navigation aid only (its OCR is unreliable and is never cited).

Reruns are deterministic; no seeds, no randomness anywhere in this lane.

## COLLISIONS

* `P` is Moh's descended polynomial (Prop 6.3) **and** the natural name for
  `n*+m*`. This report writes `Π` for `n*+m*`; `prop55k.py` uses the local
  variable `P` for `n*+m*` inside `test55k` only, never for a polynomial.
* `k` is the base field **and** the Jacobian exponent throughout the charge.
  Kept as charged; the field appears only as `k[x,y]`, `k((t))`, `k⟪t⟫`.
* `δ` is a logarithmic radius **and**, in Moh p.210 eq. (3), a remainder
  polynomial `δ` with `deg_y δ < 4`. Only §4.2 quotes the latter, marked.
* `A` is Moh's reduced denominator of `δ_1` **and**, in Appendix II p.208,
  the polynomial `A(x,y)` of `h = yA + b_4`. §4.2 is the only place the
  latter could appear; it does not.
* `s` is the number of characteristic pairs; `σ, σ*, σ̄` are `π`-roots;
  `s'` is the descended count. No `sat()` wrapping occurs in this lane.

<!-- BODY-END -->
