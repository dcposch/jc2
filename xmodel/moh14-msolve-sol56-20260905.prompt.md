# FAST-SOLVER lane (break the Moh ≤100 compute wall with msolve; AUDIT 17(ssssss)): the 12 residual u_s=1 rows' FULL Theorem-1.2 descended monomial-Jacobian order charts (6 classes, 77–378 unknowns) are the RIGHT+COMPLETE object, but Singular std is COMPUTE-BOUND (no termination in 60 min / 11 GB). Re-solve them with MSOLVE (via qqideal/msolveio, installed on the fleet) — a specialised fast Gröbner/real-solving engine — to decide EMPTINESS (unit ideal ⇒ (T) ⇒ row dead) per class

OPERATIONAL: fleet workers up: c7i 172.30.0.7/.18/.28 (64GB), r7i 172.30.0.166/.254 (247GB); SSH ~/.ssh/jc2-fleet (ubuntu@IP); charts + the FIXED builder (builder_fix.py) + the completed full-chart generators are at /home/ubuntu/jc2/box/moh14-charts-20260905/ on the workers (rsync from math-hq if stale); qqideal 0.2.0 + msolveio 0.2.1 + msolve are installed (verify: python3 -c "import qqideal,msolveio"; which msolve). Use setsid/nohup so jobs survive disconnect; one class per worker in parallel. Task: (1) for each of the 6 classes, load the FULL-chart generators (the rows.tsv / the guided .sing ideal that Singular std timed out on) and convert to msolve input (qqideal wraps this: exact QQ ideal → msolve Gröbner); RABINOWITSCH the localization (c, T·c−1) as the current charts do, so "empty" = unit ideal = the row dies; (2) run msolve (Gröbner basis / dimension / real-root or just the [1] test) — msolve is often orders of magnitude faster than Singular std; report per class: UNIT (msolve GB = [1] ⇒ dead) / POSITIVE-DIM or a solution point (survives — extract (P,Q), test J = c·x^ℓ exactly) / TIMEOUT (report the deepest, and whether msolve got further than Singular); use modular msolve first (fast) then confirm the UNIT results exact-Q (msolve over QQ) — a msolve unit over QQ is a real char-0 kill; (3) controls: reproduce a KNOWN kill as a msolve sanity check (e.g. the D=108 δ=3 stage-0 unit 17(ddddd), or (99,66) δ=2 6264 — load a small banked killing system and confirm msolve returns [1]); a tame automorphism system stays non-unit; (4) VERDICT: k of 6 classes UNIT via msolve; if all 6 UNIT ⇒ the 12 rows die ⇒ with 17(ffffff) the FULL Moh ≤100 theorem RESOLVES (MAJOR — say so prominently, and it is the first full closure of a Moh ≤N theorem via this campaign); else PARTIAL: which classes msolve killed, which still TIMEOUT, and whether msolve materially beat Singular std (informs whether ≤100 is closable by compute or needs a structural argument). FALLACY-v2 (msolve unit is a kill only over the correct ring with the localization; confirm exact-Q for any UNIT; a surviving point is a counterexample only if it lifts to a Keller pair). ≤ 210 min; you MAY edit box/moh14-charts-20260905/; no other ledger edits; no jc2-lean; no ideation-*.
Report: xmodel/moh14-msolve-sol56-20260905.md
Seal-at-completion (<!-- BODY-END -->); 15-30KB; 210 min.
charged_input=xmodel/moh14-fullorder-grok46-20260905.md
charged_input=xmodel/moh14-fix-solve-opus5-20260905.md
charged_input=box/lib/guided_gb.py
charged_input=box/moh_skeleton_full.py
charged_input=FALLACY-v2.md
charged_input=box/moh14-charts-20260905/modular_solve.py
charged_input=box/moh14-charts-20260905/jacobian_check.py
charged_input=box/moh14-charts-20260905/sprime3_compiler.py

Your inputs are frozen read-only copies in {{LANE_INPUTS}};
verify these SHA-256 hashes first, MECHANICALLY and without retyping any digit: the lane receipt `xmodel/moh14-msolve-sol56-20260905.run.v2` carries `charged_input_<i>_sha256=` and `charged_input_<i>_basename=` lines; generate the manifest with awk and run `sha256sum -c`; stop only on a real content mismatch:

```text
26ea98c552af9b7bb817cea598f2aa037672269db0fb79c340ffaac6a0716999  {{LANE_INPUTS}}/moh14-fullorder-grok46-20260905.md
2372687402bdb5b83a9ecd28d206f215abb834508f9a937b94c81350a54f42e2  {{LANE_INPUTS}}/moh14-fix-solve-opus5-20260905.md
501f3b1fed8ad0d26c7535a3c79d6ca74c448a4555f94570a4835d9f740781f3  {{LANE_INPUTS}}/guided_gb.py
d20bf0841a1ba2b229d423bb948e6c4474a4f5a83f55148071cae39cb6c506c2  {{LANE_INPUTS}}/moh_skeleton_full.py
e47fd16cfcc91bc7bfdac4ba1b5f46152db8235e6609dca6e29a5966549c38f5  {{LANE_INPUTS}}/FALLACY-v2.md
febdf0bd7a3b2f422fdf5faa4b7f8eb92971cbb41bf0b697b3517b72e092ace6  {{LANE_INPUTS}}/modular_solve.py
3865d138e98a0b78b325a2a7d7d3f19315c8bdb27d36108f897c2dd6346395c9  {{LANE_INPUTS}}/jacobian_check.py
e6fdaf819e105b51cde75ecd832b41b8d0be564db9f79bf2aaf09d6afc77d363  {{LANE_INPUTS}}/sprime3_compiler.py
```
