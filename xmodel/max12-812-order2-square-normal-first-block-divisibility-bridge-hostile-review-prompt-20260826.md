# Hostile review charge — square-normal first-block divisibility bridge

Date: 2026-08-26

Required report path:

```text
xmodel/max12-812-order2-square-normal-first-block-divisibility-bridge-hostile-review-grok-20260826.md
```

Return exactly one overall verdict `CONFIRMED`, `REPAIR`, or `REFUTED`.
Do not edit the theorem, producer cases, shared ledgers, or `jc2-lean`.

## Charged theorem

```text
xmodel/max12-812-order2-square-normal-first-block-divisibility-bridge-theorem-20260826.md
b8ebd8fc3c7b40dc5d7e953c6f0b89eb0041c92dfc12dc2b8257e5e2ad10ad03
```

## Required attacks

1. Recompute the theorem hash and distinguish its elementary algebra from
   campaign scope language.
2. Independently divide a general cubic square by a depressed monic quartic
   and verify the exact triangular formulas for `h1,...,h4`, including signs.
3. Decide whether four negative coefficients are necessary and sufficient
   for `Q|N^2`; test the claimed `R=1` sharpness control.
4. Reprove `Q|N^2 iff product f^ceil(e_f/2)|N` over `k[z]`, and attack the
   degree-three conclusion on `Q=A^2D`.  Check every gcd/squarefree
   hypothesis and the degenerate countercontrol.
5. Expand the repeated-`A` incidence chart and verify that `E!=0` and
   `(4a)^2-4E!=0` give the claimed coprime/squarefree conditions.  Do not
   silently promote the incidence root coordinate to a global regular
   coefficient coordinate.
6. Re-derive the binomial coefficient `3/8` in the literal square-normal
   expansion.  Check exactly when the ordinary lower-unitriangular Faber
   connection licenses replacement of raw negative coefficients.
7. Enforce the timing firewall: at a tied grade the lemma classifies only
   the unloaded summand and does not delete forcing, higher corrections,
   torsion, or overlap data.
8. Reject any total-Rees, total-fan, order-two, maximum-twelve, or JC2
   strengthening.

Name the smallest wrong formula or missing hypothesis for any non-confirming
verdict.
