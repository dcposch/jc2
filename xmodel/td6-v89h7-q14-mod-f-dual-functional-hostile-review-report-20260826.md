# Hostile review — TD6 V89H7 minimal q14 dual functional

| Field | Value |
|---|---|
| Target | V89H7 `RESULT.md`, `FREEZE.sha256`, `EVIDENCE.sha256`, `SOURCE.sha256`, `PAYLOAD_CLOSURE.sha256`, `source.tar.gz`, client; parent V89H6 `RESULT.md` and `FREEZE.sha256`; both V2 evidence trees |
| Overall verdict | **CONFIRMED** |
| Smallest failing identity | none |
| Smallest bad coefficient / denominator / omission | none |
| Reviewer / model | Grok 4.6 (xAI). Independent hostile algebra review. Producer `PASS` banners, dual-host byte identity, and RSS/elapsed lines are custody only |
| Method | SHA-256 of every charged pin and every consumed manifest row; flint identities in `Q[V,U]` and `Q[C,V,U]`; independent evaluation of the frozen V89H6 17-record positive class at `(V,U)=(4,1)` modulo `1000033`; source review of V87 reconstruction, H5 projection/division, H6 full-pivot inverse and `F=0` map, and the H7 functional extraction. No Singular, Sage, Lean, or `jc2-lean`. The AWS client was not re-executed (wrapper gated to Amazon Linux); the exact functional is recovered from the pinned source plus the frozen artifacts |
| Repo | `/Users/dc/code/math/jc2` |
| Git HEAD | `418e413593120d19e15e6546eb50c985f4b1f038` |
| Date | 2026-08-26 |

Prompt SHA-256 `832712c0d4e57c7bbfd0622c177cdc8e227e09734241fca18417092992c11471` matched. Independently recomputed SHA-256 of every required primary pin matches. Every path named in `FREEZE.sha256` (10/10), `EVIDENCE.sha256` (36/36), `SOURCE.sha256` (12/12), and `PAYLOAD_CLOSURE.sha256` (56/56) rehashes to the printed digest. Every `SOURCE.sha256` row exists at the case root and as `source/<path>` inside `source.tar.gz`, and both copies match. All 56 payload-closure files exist inside the same tarball and match. Nested compiler hash pins consumed from that archive also match. Producer `TD6-V89H7-Q14-MOD-F-DUAL-FUNCTIONAL PASS` was not used as algebra. V1 is a packaging-negative run (`rc=1`, no algebraic construction) and is not used as mathematical evidence. No file other than this review was written.

---

## Verdict

**CONFIRMED.**

On the residue obtained by setting `q2,...,q13=0`, omitting `q15` as the lower target-shear of the frozen pair `(p,q)=(t^{15},q)`, and keeping independent untruncated `q14,q16,...,q24`, the client rebuilds genuine P12 and all 38 original packed FIRST sources, then imposes `F=CU-V^2+U^3=0` by the two-sided chart `C=(V^2-U^3)/U` on `D(U)`. The raw common denominator is exactly `U*H=U(C-3U^2)`, coprime to `F`.

The specialized 38-by-38 FIRST pivot block is polynomially invertible over `E3[q]` (no nonconstant-q inverse). Both products of the inverse are the identity, the original sources are recovered from the normalized rows, the normalized rows are monic in distinct pivot variables with pivot-free tails, and all 38 original sources reduce to zero. The associated rewriting system is therefore a unique normal form for the entire ideal generated by original FIRST after `F=0`, not a displayed-row subset and not the H5 chosen splitting.

The K-linear functional

```text
lambda(g) = E3 scalar coordinate 0 of the coefficient of
            q14 * (empty parameter monomial)
            in NF_FIRST(g mod F),
K = Frac(Q[V,U])
```

vanishes on that ideal, is not a pivot coordinate, and is not obtained by truncating q-support. On literal P12 it equals

```text
N / [V^3 (V^2-4U^3)^2]
```

with `N` the displayed nonzero polynomial of `RESULT.md` lines 45–50. Flint gcd of `N` against that denominator is `1`, so the denominator is exact. On `F=0`, `B3=V^4` and `U*H=V^2-4U^3`, so every inverted factor is already a unit on `D(U*H*B3)`.

An independent finite-algebra check at the preregistered point `p=1000033`, `(C,V,U)=(15,4,1)` (where `F=0`, `H=12`, `B3=256`) separately inverts original FIRST at `q14=0` and `q14=1`. The complete 17-record difference equals the evaluation of the exact class, and the selected coordinate is `159760 != 0`. That is validation, not the characteristic-zero proof.

This is one nonzero q14 cokernel class modulo full original FIRST+`(F)` in the exact low-q-zero / sheared-q15 scope. It is not `1 in (P12,FIRST,F)`, a source-point exclusion, a low-q unit-chart result, total-Rees lifting, whole A3, TD6, SP-2, or JC2.

**CONFIRMED**

---

## Verdict table

| Issue | Charge | Finding |
|---|---|---|
| 0. Frozen pins | hashes | all required pins match, including parent V89H6 `RESULT.md` / `FREEZE.sha256` |
| 0. Manifests | every named file | `FREEZE` 10/10, `EVIDENCE` 36/36, `SOURCE` 12/12, `PAYLOAD_CLOSURE` 56/56; tarball copies match |
| 0. Dual AWS V2 | hosts, archive, rc | distinct Box02 / r6d; archive `f34c65a4…`; both `rc=0`; math artifacts and stdout byte-identical |
| 0. V1 packaging-negative | not math evidence | both V1 `rc=1`; missing nested `q2_beta_dual` import; no algebraic construction; erratum accurate |
| 1. Literal scope | P12, 38 FIRST, q-support | V87 rebuild; `Q_EXPONENTS` skips 15; H5 `TAIL=(14,16..24)`, `LOW=(2..13)`; untruncated `QPoly` |
| 2. Exact F specialization | `C=(V^2-U^3)/U` on `D(U)` | `SPECIAL_C` substitution; raw common den `U*H`; `gcd(U*H,F)=1` |
| 3. Full original-FIRST quotient | SCC/block inverse, both products, recovery, monic distinct pivots, pivot-free tails, 38 sources → 0 | holds; NF kills the whole original-FIRST ideal, not H5 splitting |
| 4. Functional well-definedness | K-linear, factors through the quotient, untruncated, nonzero numerator | holds; flint `N ≠ 0` and `gcd(N,den)=1` |
| 5. Denominator firewall | `den=V^3(V^2-4U^3)^2`; `B3=V^4`; `U*H=V^2-4U^3` | holds; no new inverted factor |
| 6. Fresh-prime independence | prime, point, two inversions, complete-class equality, `159760≠0` | holds as validation only |
| 7. Custody / omissions / reporter asserts | Box02/r6d identity; source/payload complete; V1 classified; no circular asserts | holds |
| 8. Conclusion strength | one nonzero q14 cokernel class in the stated scope | slogans and `RESULT.md` keep the firewall |

---

## 1. Custody

Recomputed SHA-256 of the required primary pins:

| Artifact | SHA-256 | Role |
|---|---|---|
| `RESULT.md` | `49b9dcba0bb5956dfdbb10293b7bdf3e440a39b127929089c2402ffb06db32cd` | producer result |
| `FREEZE.sha256` | `7115c67a564b1f174a2e7337a1e64566b8ad2a422405c7243dfee9e8de77a6e0` | freeze |
| `EVIDENCE.sha256` | `1565fea5f95c65db6112b44eca984fc103660bea722421bdaead77847db925d0` | evidence manifest |
| `SOURCE.sha256` | `20d35b9990712df8bb64d11dc3b0ead5cbd5ae7242f2ca5f0c838e050dfcc290` | source manifest |
| `PAYLOAD_CLOSURE.sha256` | `cbe7e0be9d49f5aa994790332e2fe0368c32780eac81ee3ddededdf7bb3d5cff` | 56-file payload closure |
| `source.tar.gz` | `f34c65a49711fb03cd9f283ed0ff54bdd80f412a09c7dd9cfa2f531dcdddd950` | V2 source archive |
| client `replay_v89h7_q14_mod_f_dual_functional.py` | `28b0848974a92e786767e7da071c9f2235e720717d223a8b7d0558d290fe78c9` | controlling replay |
| parent V89H6 `RESULT.md` | `6a5bd3fe15d6d98b9ef61b7e134bfc0a0ea03fcb834af47b64122b4f1d9feae6` | parent theorem |
| parent V89H6 `FREEZE.sha256` | `82635ec98f9808d2d4c359643aacf3de20c96a37449ff63e18854aeb9f9cbf2c` | parent freeze |

Nested compiler pins reached from the frozen H7 client, independently rehashed:

| Pin | SHA-256 |
|---|---|
| H6 parent client | `1301a09f0497abac5197de6b4afc0eba54b5c8de2b05d2cf11db63fab454d757` |
| H5 parent client | `a81c358b58f8b97ec0dfd97128d0f9539782eda70e66a6747a4c2827a1539b9b` |
| V87 parent client | `7c0890abbce996a221f9389372fa3705742d6ecd72531891bfeb5dfc2f1b9463` |
| V86 parent client | `5b160a2bd18434e6142c33be6775e0c212c22ebc67a52d08b246dd0223decd7c` |
| V85 parent client | `ab9753073d694242d4359dc1edc86774724f62d28d15b7c671ba0b796262100e` |
| generic parent shard | `a4ccd5d81101fdb82ed2f61571c258e2054961d161f4a98461b8f8614d3fd85e` |
| V85 source inventory | `9a612bb64ee974a506a1f3b7fcb471fb8924220ca6d24320af0fc9b8a7de6b7a` |
| F-raw P12 certificate | `8e892ffa914e0f93969abb9722f926486843f59cf77f5861c5ce3d3438281482` |

The H6 client copy inside the H7 case is byte-identical to the parent V89H6 client. Byte-identical mathematical stdout is `04e3be64acaa1890e43786913c7921cb22b775a1f81a7ed46cb8967b393b5b3e` on both V2 hosts. Mathematical output files:

| Output | SHA-256 |
|---|---|
| `Q14_DUAL_FUNCTIONAL_EXACT.txt` | `bdf711b8b1951e462111e497bc520448503ec700d4ad4f1e86827a5227633233` |
| `Q14_DUAL_FUNCTIONAL_FRESH_PRIME.tsv` | `82101c4be864b9227e7a5526de3352724b43187a51589964be872211245b76b9` |
| `Q14_DUAL_FUNCTIONAL_FRESH_PRIME_CLASS.tsv` | `d06b9f29227c3ab081c301314a5567050de949dffd1638c9595e56ae42f64153` |
| `Q14_DUAL_FUNCTIONAL_RESULT.txt` | `68d30118f996a1b59dd3386131e188b18e6fefe189ef496c52ad3ebb47cb3e13` |

Box02 (`ip-172-30-0-186`, pid 300323, tag `…box02v2_20260826T184459Z`) and r6d (`ip-172-30-0-45`, pid 321535, tag `…r6dv2_20260826T184459Z`) are distinct hosts with the same archive `f34c65a4…`, both `rc=0`, and zero swap. Mathematical stdout and the four output files are byte-identical. Host stderr differs only in `/usr/bin/time` counters (7:11.54 vs 7:16.50; RSS 295432 vs 296156 KiB, both below 296 MiB). `launch.meta`, pids, and finish timestamps differ as they should. Dual-host equality is custody, not the identity.

`source.tar.gz` contains 184 members, including AppleDouble `._*` metadata, but no bytecode. Every charged `SOURCE.sha256` and `PAYLOAD_CLOSURE.sha256` path is present and hash-correct. The case-directory `payload/` tree also has local `__pycache__` (32 `.pyc` files); those files are absent from the tarball and from the 56-file non-bytecode closure.

V1 (both hosts `rc=1`) failed at import of the shard's nested `td6_c1_c2_c3_q2_beta_dual_20260825/replay.py` (`FileNotFoundError`, `evidence/v1_packaging_negative/box02/stderr` lines 29–35). The V1 archive listing has no `q2_beta_dual` member. Both V1 stdout copies are byte-identical (`7df92a2a…`) and stop after the SOURCE checksums, before any producer banner or FIRST construction. `V1_PACKAGING_ERRATUM.md` lines 5–15 classify this correctly as a source-packaging negative. The H7 client never reads V1 outputs.

---

## 2. Charge 1 — literal scope

V87 `Q_EXPONENTS = tuple(list(range(2, 15)) + list(range(16, 25)))` (`replay_v87tfaq_total_f_allq.py` lines 32–34) has length 22 and does not contain 15. `QPoly` is an untruncated sparse ring on that set (class docstring line 41; constructor lines 52–54 refuse any exponent outside `Q_ORDER`; `inverse` at lines 111–118 fail-closes unless the support is the empty q-monomial).

H5 splits that set as `TAIL = (14,) + tuple(range(16, 25))` and `LOW = tuple(range(2, 14))` (`replay_v89h5_q14_high_unimodular_total_f_v1.py` lines 26–29) and asserts the union is exactly `Q_EXPONENTS`. Projection of a `QPoly` keeps a monomial if and only if every exponent lies in `TAIL` (lines 35–41). Empty monomials (q-constants) are kept. A mixed monomial such as `q2 q14` is dropped, which is the specialization `q2=…=q13=0`. A monomial `q14 q16` is kept: q16…q24 are not truncated.

Why `q15` is a legitimate quotient coordinate, not a silently omitted source modulus:

- f-transport is built from `{15: 1}` at degree 15 (`replay_v87tfaq_total_f_allq.py` lines 317–320): frozen `p=t^{15}`.
- g-transport is built from `{1: 1, 25: 1}` only (lines 321–324): frozen `q=t+…+t^{25}`.
- Holding the degree-15 coefficient of `q` at zero is the reviewed target shear. The H7 client asserts `seen[exponent]==1` for every tail exponent (`replay_v89h7_q14_mod_f_dual_functional.py` line 75) and `events==2` with `len(first)==38` (line 74).

Genuine P12 is the inlined degree-12 raw CURRENT formula `compile_current_degree12` (`replay_v87tfaq_total_f_allq.py` lines 260–310), not staged CURRENT/PREVIOUS/POLE. Genuine FIRST is `qd.pack("X-2", qd.first_band_polynomials(f1,g1))` (lines 250–257). H7 rebuilds both live (`replay_v89h7_q14_mod_f_dual_functional.py` lines 69–73) and then projects by H5. Wrapper environment `TD6_Q_SCOPE=tail-q14-q16-q24` (`run_v89h7_q14_mod_f_dual_functional.sh` line 11) matches this split.

This is not a low-q unit chart, an independent q15 source modulus, a total-Rees lift, or a TD6 statement.

---

## 3. Charge 2 — exact F specialization

`F = C*U - V**2 + U**3` and `H = C - 3*U**2` are the V85 literals (`replay_v85tf1_total_f.py` lines 44–45). The two-sided chart is `SPECIAL_C = (Rat3(V)**2 - Rat3(U)**3) / Rat3(U)` (line 47). `specialize_polynomial_value` (lines 149–161) substitutes that chart coefficientwise on `Q[C,V,U]`, which is regular precisely on `D(U)`. Substituting into `F` gives

```text
((V^2-U^3)/U)*U - V^2 + U^3 = 0.
```

H7 applies this map to the already-projected FIRST and P12 via `h6.specialize_first` / `h6.specialize_polynomial` (`replay_v89h6_q14_mod_f_cokernel.py` lines 30–59; H7 lines 86–90). Before specialization it demands the raw common denominator of P12 and the 38 original sources (`H7` lines 79–81):

```text
assert raw_common.gcd(F).total_degree() == 0
assert raw_common == U*H
```

Stdout records `raw_source_common_denominator=(C*U - 3*U^3)`, which is `U*H`. `U` does not divide `F` in `Q[C,V,U]` (it would have to divide `V^2`). `H` does not divide `F` (restricting to `C=3U^2` leaves `4U^3-V^2 ≠ 0`). The asserted coprimeness is correct.

---

## 4. Charge 3 — full original-FIRST quotient

H7 recomputes the H6 inverse; it does not copy H6 remainder files.

`h6.invert_full_q_pivot` (`replay_v89h6_q14_mod_f_cokernel.py` lines 62–132) inverts the full 38-by-38 specialized pivot block:

1. Invert the q-constant block `A0` over E3 (`h5.inverse_constant_matrix`).
2. Form `B = A0^{-1} A = I+N` with vanishing constant term off the diagonal.
3. Take SCCs of the support graph of `N`. Cyclic blocks are inverted by division-free adjugate, refusing any determinant other than `QPoly(1)` (`replay_v89h5_q14_high_unimodular_total_f_v1.py` lines 132–150).
4. The condensation is unitriangular in a topological order and inverted polynomially (`polynomial_matrix_inverse`, H5 lines 347–370).
5. Both products `B * inverse_B` and `inverse_B * B` are asserted to be the identity (H6 lines 118–119). `T = inverse_B A0^{-1}` then satisfies `T A = I` (H6 line 123), and H7 re-asserts both `T A = I` and `A T = I` (H7 lines 97–98).

`QPoly.inverse` cannot invert a nonconstant q polynomial. The only inverses are E3 units (q-constants) and the unit cyclic determinant `1`. This is not an H5 chosen splitting: H5's own `SCC = (0,1,2,3,4,5,6,13)` constant is unused here; the graph is computed from specialized `N`. Parent V89H6 emitted that same 8-cycle as the unique cyclic component with unit determinant; H7's two-sided identities re-check the inverse without relying on that artifact.

Normalized rows are original-source linear combinations via `T` (H7 lines 99–101). Recovered rows via `A` equal the specialized original sources (lines 102–105). `base_pivot_columns` asserts 38 distinct pivots (H5 line 195). H7 then checks that each normalized source has coefficient `1` on its own pivot monomial and that every other monomial is pivot-free (lines 107–113). Tails therefore cannot reintroduce a pivot, so reduction order is irrelevant and the remainder is unique.

`reduce_quiet` (H7 lines 29–47) is the same monomial rewriting as `h5.divide_by_normalized_first` (H5 lines 380–410), without collecting quotients. H7 asserts every specialized original source reduces to zero (lines 114–117). `divide_by_normalized_first` additionally replays `remainder + sum quotients * normalized = P12` (H5 lines 406–409). H7 pushes those quotients through `T` and replays against the original sources (H7 lines 122–129). Stdout ends at `p12_division_pivot=38/38;remainder_terms=17`, matching the 17-parameter positive class (empty monomial carries both the q-zero unit and the q14 term, so 17 keys).

These facts make NF the quotient by the ideal generated by all 38 original FIRST sources in the jet polynomial ring over `K[q14,q16,…,q24]`. The two-sided coefficient-ring identities identify that ideal with the ideal generated by the normalized rows. Unique remainder then vanishes on the whole ideal, not merely on the displayed generators and not merely on an H5 SCC splitting.

---

## 5. Charge 4 — functional well-definedness

H7 extracts

```text
positive = h6.positive_part(remainder)
assert h6.qpoly_support(positive) == [(14,)]
q14_empty = positive[()].coefficients[(14,)]
functional = Rat3.coerce(m.r.scalar_coordinates(E3.coerce(q14_empty))[0])
assert functional
```

(`replay_v89h7_q14_mod_f_dual_functional.py` lines 130–136). `positive_part` (`replay_v89h6_q14_mod_f_cokernel.py` lines 148–156) drops the empty q-monomial; `qpoly_support` (lines 159–165) collects the remaining q-monomials. The empty parameter monomial is not a pivot (pivots are jet variables). After the pivot-free-tail reduction the remainder contains no pivot variables, so this coordinate is not a pivot contaminant. q-support of the whole positive remainder is exactly `(14,)`, which is a computed fact about P12, not a truncation of `TAIL`.

K-linearity is the linearity of monomial reduction over `K`. Vanishing on original FIRST is the reduction of all 38 original sources to zero. The functional therefore factors through the claimed quotient.

The emitted numerator is the polynomial in `RESULT.md` lines 45–50, matching `Q14_DUAL_FUNCTIONAL_EXACT.txt` line 6 and matching coordinate 0 of the empty-monomial `(14,)` record in the frozen parent V89H6 positive class (SHA `56ebf08a…`). Flint factorization

```text
N = (728/375) U^2 (5297 V^{10} + 2233333 V^8 U^3 + 41929988 V^6 U^6
                 - 14613611 V^4 U^9 - 28627660 V^2 U^{12} + 24400 U^{15})
```

is not the zero polynomial. Coordinates 8–17 of the same E3 element are asserted zero (H7 line 137); coordinates 1–7 are live dual-Q data and are not part of this functional. Taking only coordinate 0 is the preregistered minimal dual (`run_v89h7_q14_mod_f_dual_functional.sh` line 14; H7 lines 65–66).

---

## 6. Charge 5 — denominator firewall

H7 asserts `functional.denominator == V**3 * (V**2 - 4*U**3)**2` (lines 138–139). The emitted denominator `V^7 - 8 V^5 U^3 + 16 V^3 U^6` (`Q14_DUAL_FUNCTIONAL_EXACT.txt` line 7) expands to that product. Flint factors it as `V^3 (V^2-4U^3)^2` and reports `gcd(N, den) = 1` in `Q[V,U]`, so no cancelled extra factor is hidden in both numerator and denominator.

On `F=0`:

- `U*H = U(C-3U^2)`. Substituting `CU = V^2-U^3` gives `V^2-4U^3`.
- The frozen B3 sextic (`payload/jc2/cases/td6_c1_c3_two_center_cover_20260824/c1_c2_c3_trivariate.py` lines 53–56)

  ```text
  B3 = 4 C^2 U^2 - 4 C V^2 U + 24 C U^4 + V^4 - 20 V^2 U^3 + 20 U^6
  ```

  specializes under `C=(V^2-U^3)/U` to `V^4` (direct expansion, all other monomials cancel). H7 asserts both identities live via `v85.specialize_e3` (lines 140–143).

Every prime factor of `den(lambda(P12))` is therefore a radical factor of `U*H*B3` after `F=0`. Units on `D(U*H*B3)` remain units when raised to the displayed powers. No factor of `F` or of the unregistered H1 polynomial `K` is inverted.

---

## 7. Charge 6 — fresh-prime independence

`1000033` is prime (trial division through `floor(sqrt(p))=1000`, matching `build_mod_factory` at H6 lines 312–314 after H7 mutates `h6.PRIME` at line 167). The point `(C,V,U)=(15,4,1)` satisfies `F=15-16+1=0`, `H=15-3=12`, and `B3=256=4^4`. Direct evaluation of the B3 sextic at that point is 256.

H7 inverts original (not specialized) FIRST at `q14=0` and `q14=1` separately (`prime_remainder`, H6 lines 444–488; H7 lines 178–183), subtracts, and compares the complete remainder against `evaluate_qpoly` of the exact positive class (H7 lines 184–190). `evaluate_qpoly` (H6 lines 335–344) sets every q-exponent other than 14 to zero: this is a point of the untruncated tail ring, not a second proof over that ring. The exact class has q-support exactly `(14,)`, so the evaluation is faithful for the class.

Independent check, not using H7's AWS remainder path: every one of the 17 frozen V89H6 positive-class records, evaluated at `(V,U)=(4,1)` modulo `1000033`, equals the corresponding row of `Q14_DUAL_FUNCTIONAL_FRESH_PRIME_CLASS.tsv`. The empty-monomial coordinate 0 is `159760`. Direct evaluation of `N/den` at `(4,1)` is `605650778369/9000`, which reduces to `159760` modulo `1000033`. The selected value is nonzero.

This confirms the exact functional at one licensed point. It is not the characteristic-zero theorem.

---

## 8. Charge 7 — custody, omissions, reporter assertions

Box02/r6d mathematical artifacts and stdout are byte-identical (section 1). Source and payload hashes are complete, including the nested `q2_beta_dual` file that V1 omitted. The V1 missing-import erratum names that file (`V1_PACKAGING_ERRATUM.md` lines 8–10) and matches both V1 tracebacks.

Reporter writes such as `functional_annihilates_full_original_FIRST_module=true` (`H7` line 155) are preceded by the two-sided maps, the pivot-free-tail check, and the 38-source reduction, not assumed. `functional_P12_nonzero=true` follows `assert functional`. Firewall slogans in `Q14_DUAL_FUNCTIONAL_RESULT.txt` lines 6–10 are all `false` for the banned conclusions. No client assert takes the cokernel nonvanishing as a hypothesis.

Leftover H5V2 filenames in the case directory (`V2_RESULT.md`, `SOURCE_V2.sha256`, …) are not in `FREEZE.sha256` and are not consumed. They do not affect the charged package.

---

## 9. Charge 8 — conclusion strength

The strongest statement supported by the package is:

> After exact `F=0` on `D(U*H*B3)`, in the ring with `q2=…=q13=0`, q15 absent only under the reviewed target shear, and independent untruncated `q14,q16,…,q24`, the class of literal P12 in the quotient by the full original FIRST ideal is nonzero, as witnessed by a K-linear functional that annihilates that ideal and takes the displayed nonzero value on P12.

`RESULT.md` lines 87–91 and the result artifact lines 6–10 refuse `1 in (P12,FIRST,F)`, source-point exclusion, low-q unit charts, total-Rees, whole A3, TD6, SP-2, and JC2. That is the correct strength.

---

VERDICT: CONFIRMED
