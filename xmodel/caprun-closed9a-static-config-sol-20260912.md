# Batch-a static configuration — Sol

- First action: `2026-09-12T08:20:04.621627742Z`.
- Basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
- State: disabled, unbound, source-static, unexecuted.

## Status

**COMPLETE STATIC PREALLOCATION; DISABLED/UNBOUND.** The constants-only config derivative is not an actual `FROZEN-CONFIG.json`, physical placement, worker fact, or launch authority.

## Result

`FROZEN-CONFIG.preallocation.json` is a byte-exact ordered transformation of the charged corrected template using the 45 substitutions and counts in `PORT-MAP.json` (54 total occurrences). It preserves `armed:false`, `review_state:"UNREVIEWED_UNBOUND"`, `exclusive_no_concurrent_writer:false`, all four separate ROOT entry authorities, and the runtime-output/non-preinstallation disposition.

The three immutable millisecond stops, selected batch paths, existing record-directory identity, five record filenames, owner strings, coordinator file locations, transport file locations, prepared paths, fixed controller/helper roles, remote base/unit/cgroup, and remote eight-role paths are populated as static constants. Local fixed paths are exactly 14 unique corresponding roles; remote fixed paths are exactly eight unique corresponding roles.

ROOT corrected two task-supplied executable basenames during authoring: the reviewed setup installs `outer-admission.sh` and `observe-and-leaves.sh`, not `admission.sh` and `observe.sh`. The task remains unchanged; the config and port map use the corrected paths and cite ROOT's supplied setup-template SHA provenance.

The setup stage is `/home/ubuntu/jc2-closedchild-preflight9-20260912a/stage`; the final transfer stage is distinctly `/opt/jc2-closedchild-preflight9-20260912a-final-stage`. Config `remote.stage` and the receiver's future binding use only the final stage. The installed manifest and native list remain in setup stage. ROOT must later bind the installer's single stage literal to the final stage; the installer was not edited here.

## Unresolved boundary

Eleven distinct config placeholder names remain with exact multiplicities in `EARLY-CONSTANTS.json`: worker host/instance/hostname/boot/PID namespace; receipt, coordinator-native, and remote-native digests; retirement timer unit, service unit (twice), and ExecStart. No worker exists. The key and known-hosts bytes, record existence, both physical stages, coordinator evidence, remote evidence, and timer/service state were not inspected or asserted. Existing `active`, `loaded`, and `oneshot` strings are future predicates, not observations. The omitted native `dd`/`getconf` and full dependencies still require ROOT qualification.

All six inputs were pinned before fresh whole reads and post-pinned unchanged. Whole readback covered all owned outputs and the finalized report. Embedded references remained inert. No JSON runtime roundtrip, source/shell/science execution, syntax/AST/import/compile/test/mock/fixture, worker/process action, private-key read, network/SSH/AWS action, or protected-tree access occurred.

Disposition: **static constants complete; physical/evidence binding and all enabling state remain outstanding.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `3178`.
- Body SHA-256:
  `19dafdb7fdf3a58e03ad515e5aa20d5ee79bb684c9520078dfd6eed1794ab26d`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
