# F10 r1 normalized runtime code preparation

Status: DISABLED / UNEXECUTED CODE PREPARATION ONLY. This is an adaptation for one finite normalized representation batch under accepted QUALIFIED STATIC17n, not runtime acceptance or a solver. No worker, caps, deadlines or execution invitation is issued by this packet.

First action2026-09-09 19:38:57 UTC; controlling stop19:58:57 UTC (actual+20 minutes, earlier than20:00). Targets were absent. Basis `0d39df3c9fd69c939a8420c54d03228b9077777d`. Eleven exact inputs matched before reads/copies. `READ-SCOPE.md` records fresh WHOLE text reads and reuse of the prior owned report/custody. No adapter, current review, other science, live body or provenance-only file was consumed.

## 1. Complete delivered delta

The new own box contains the adapted `dispatch_batch.py`, `semantic_controls.py`, exact byte-identical copies of the normalized generator/checker/arithmetic and the accepted helper/probe, literal `caller.diff`, disabled registration template, validation plan and current pins. The original normalized files and old retained caller/harness remain unchanged. No additional arithmetic backend or API was introduced.

The caller change is confined to the normalized registration/profile/operation names, fixed generator/checker/algebra file vector, normalized artifact filename, correct checker receipt status/source/artifact fields, and final receipt's explicit backend/code hashes. All code governing caps, resource inheritance, PID namespaces/PGIDs, identity checks, parent/child argv, exclusive outputs, authority freeze, input pre/post pins, admission, aggregate cutoff and post-return failure is unchanged. The exact literal diff makes this auditable.

The normalized backend's missing embedded algebra hash is not patched. Instead the future registration and every per-operation authority must bind the exact algebra file, and batch/control receipts name that hash alongside the exact normalized artifact. Caller and harness hashes plus interpreter hash are future external root-preflight fields; there is no impossible self-hash assertion. The runtime vector is eleven local files plus the interpreter. CAPRUN remains the literal reviewed4435279d... external instrument, not a source edited or read here.

No new file is executable by authority: the only supplied registration is differently named `dispatch.registration.DISABLED.json`, with enabled=false and null worker/cwd/timing/caps. The caller requires a fresh enabled root registration at its exact future path. No registered worker or absolute run clock is implied by a template.

## 2. One future batch, accurately counted

The unchanged preflight consists of five actual authority refusals (disabled, wronghost, argv, cap, missing-input), the genuine valid sentinel, and the same-PGID descendant RSS/TERM-ignoring/KILL test. The dummy retains all four typed identity/signal events, observed leader PID/PGID/start equality, exact integer signals, namespace checks and no owned live PGID after cleanup.

Only after complete preflight does the future caller run one normalized generator.main, one genuine checker.main against original exact.json, and one capped semantic harness. These are three mathematical children after seven preflight children, not a matrix of production negative invocations. Mathematical aggregate begins with the generator. All operations retain the15-second admission margin, original stored absolute cutoff, post-return check, inherited16MiB file cap, one child at a time and exclusive per-operation authority with no concurrent writer. Root alone sets future profile values, aggregate and mathematical/task deadlines. There is no reuse or reset of an expired clock, no retry/escalation and no strict real-time guarantee beyond the accepted sampled/cleanup limitations.

The genuine checker receipt must be the normalized representation status and bind original source and exact generated artifact. Its full-condition scope is all20 source slots, nine G, two H, eight bracket bands and ten inverse-pole slots. The actual checker verifies the source coefficient/row bindings, whole mate and target/bracket, inverse polynomiality, gauges, top restoration and maps. Inventory values alone are not acceptance.

## 3. Eight changed-object controls

The harness uses only the unchanged backend to construct exact canonical fixtures. It calls the unchanged checker.verify in process, never generator.generate, and does not repeat a positive verification. Each changed object is written exclusively, hashed, reread, checked for exact expected exception/reason, then rehashed. An unchanged fixture or unexpected failure stops the harness. The genuine original production receipt and source/code/artifact pins are prerequisites; an in-process call is not an independent lifecycle attestation.

The finite inventory is:

| Fixture | Exact intended ValueError reason |
| --- | --- |
| completed-matrix-entry, add1 to N1[0][0] | source-bound completed matrix |
| historic-third-forcing-index | full affine forcing including third correction |
| full-Pi-sign | full Pi |
| H0-projection, add1 | H0 whole constant target |
| H1-projection, add1 | H1 whole linear target |
| float-anywhere | float forbidden |
| noncanonical-rational-string, plus-prefixed L coefficient | canonical rational |
| canonical-wrong-large-integer in H0 | H0 whole constant target |

All remain UNEXECUTED. Expected reasons are derived from static control flow, not claimed observations. Matrix/third forcing failures are source-binding failures, and Pi/H0/H1 failures are the named projections. They do not prove negative late bracket/pole-path coverage. Genuine positive execution is mandatory for those full equations. Floats are rejected early; noncanonical strings reach the exact parser; the changed canonical integer exceeds2^53 and has a genuine binary64 rounding precondition. This provides no positive nested-parser large-payload fidelity claim and is not labeled one.

## 4. Manual audit of the historical WRONGINDEX fixture

This fixture reproduces the specific displayed finite rule, not a random c3 perturbation. Write theta=t/S, leading C=theta^3+F theta^2+H theta+a, leading mate D=sum D_i theta^i, and g0=z-U*d0. Here d0 is map2 slot2, NOT the leading mate coefficient D_0. At the third affine-forcing stage k1=0. The relevant cross term from the A component S*g0*theta and S^7 D is

    [S*g0*theta,S^7 D]=S^6*g0*(theta D'-7D).

Moving it to the forcing side supplies g0(7D-theta D'), hence the correct finite coefficients are N_i=-W_i+(7-i)g0 D_i. The old printed recipe is N_i=-W_i+(6-i)g0 D_(i+1). For the displayed i=0,...,4 it changes

    deltaN_i=g0*((6-i)D_(i+1)-(7-i)D_i).

The already-fixed high slots leave the quadratic part V0+V1 theta+V2 theta^2 governed by 4CV'-4C'V. Its theta4,theta3,theta2 coefficients are respectively

    -4V2, -8V1, -12V0-4F V1+4H V2.

Its remaining theta1,theta0 coefficients are 8aV2-8F V0 and 4aV1-4H V0. Therefore the exact changes induced by the old finite recipe are

    deltaV2=-deltaN4/4,
    deltaV1=-deltaN3/8,
    deltaV0=(-4F deltaV1+4H deltaV2-deltaN2)/12,
    deltaC1=8a deltaV2-8F deltaV0-deltaN1,
    deltaC0=4a deltaV1-4H deltaV0-deltaN0.

The harness adds these two changes to actual c3 in its original row order, leaves h3 untouched, stores all delta wires as mutation evidence, and requires at least one changed forcing coefficient. It does not claim a complete alternative source pair or rederive untested high forcing under the erroneous rule. The required rejection is specifically the full original affine-forcing check, before later elimination/compatibility comparisons. This derivation is manual; no polynomial coefficient was constructed in preparation.

## 5. Retained qualifications, evidence and stop

Qualified STATIC17n does not supply runtime evidence. We retain root's precise corrections: an oversized unused c2 tail is not inventoried, while the genuine generator supplies two entries; original residual rows are checked rather than container fields; gauges alone do not prove resonance forcing, which remains covered by the accepted16r interface and full source/upper equations; f5 is the accepted shared five-partition literal; h3/h4 evidence is accepted at its documented scope. No extra f5 route, backend hardening, helper repair, optional control matrix or univariate reduction is added.

All implementation files were inspected as text only. No compile/import/test, mathematical subprocess of any size, source coefficient read/construction, CAS, solver, AWS/SSH, process census, web, child agent or shared/canonical edit occurred. No artifact or fixture was emitted by executing code. The inherited caller contains runtime process checks, but none ran here.

All term counts, actual degrees/bit lengths, timings and RSS remain unmeasured. Future output uses the unchanged checker's measured_rows and existing caller/CAPRUN telemetry; smaller coordinates are not assumed cheaper. A cap, missing receipt, wrong reason, source mismatch or late return is STOP/NONDECISION, not a representation or ideal verdict. No point, properness, empty-ideal or family claim follows from engineering PASS.

Two pending quantities remain: one focused static delta review, then one separately root-registered finite validation batch if authorized. The cheapest next check is review of the literal caller diff and eight fixture semantics against these fixed code inputs. No runtime-readiness or dispatch promise is made. Own raised-OPEN scan introduces no canonical OPEN identifier; these are explicitly pending software checks. Own substantive body and files were read before completion marker and all writers stop on publication.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `9741`.
- Body SHA-256:
  `9d0a9c4ab90d64744b2e9774a6df1e22f925fdf340d4458f706e80b2756c28c4`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
