# Hostile review prompt: TD6 V89H7 minimal q14 dual functional

You are an independent hostile algebra reviewer.  Audit the frozen producer
package below without editing it and write exactly one report to

```text
xmodel/td6-v89h7-q14-mod-f-dual-functional-hostile-review-report-20260826.md
```

The final line must be exactly one of `VERDICT: CONFIRMED`,
`VERDICT: REPAIR`, or `VERDICT: REJECT`.

## Frozen dependencies

Repository root: `jc2/`.

```text
case = cases/td6_c1_c2_c3_q14_mod_f_dual_functional_v89h7_aws_20260826/
RESULT.md SHA256 = 49b9dcba0bb5956dfdbb10293b7bdf3e440a39b127929089c2402ffb06db32cd
FREEZE.sha256 SHA256 = 7115c67a564b1f174a2e7337a1e64566b8ad2a422405c7243dfee9e8de77a6e0
EVIDENCE.sha256 SHA256 = 1565fea5f95c65db6112b44eca984fc103660bea722421bdaead77847db925d0
SOURCE.sha256 SHA256 = 20d35b9990712df8bb64d11dc3b0ead5cbd5ae7242f2ca5f0c838e050dfcc290
PAYLOAD_CLOSURE.sha256 SHA256 = cbe7e0be9d49f5aa994790332e2fe0368c32780eac81ee3ddededdf7bb3d5cff
source.tar.gz SHA256 = f34c65a49711fb03cd9f283ed0ff54bdd80f412a09c7dd9cfa2f531dcdddd950
client SHA256 = 28b0848974a92e786767e7da071c9f2235e720717d223a8b7d0558d290fe78c9
parent V89H6 RESULT SHA256 = 6a5bd3fe15d6d98b9ef61b7e134bfc0a0ea03fcb834af47b64122b4f1d9feae6
parent V89H6 FREEZE.sha256 SHA256 = 82635ec98f9808d2d4c359643aacf3de20c96a37449ff63e18854aeb9f9cbf2c
```

First verify every frozen hash and both V2 rc files.  V1 is deliberately a
packaging-negative run; ensure the producer does not use it as mathematical
evidence.

## Claims to attack

1. **Literal scope.** Confirm the client rebuilds genuine P12 and all 38
   original FIRST sources with exactly `q2,...,q13=0`, q15 absent only under
   the separately reviewed target shear, and `q14,q16,...,q24` independent
   and untruncated.  Reject any wider low-q, q15-source, total-Rees, or TD6
   reading.

2. **Exact F specialization.** Check that `F=0` is imposed by the two-sided
   coordinate map `C=(V^2-U^3)/U` only on registered `D(U)`, and that the raw
   common denominator is exactly `U*H`, coprime to F.

3. **Full original-FIRST quotient.** Hostile-audit the SCC/block inverse,
   both products of the 38-by-38 maps, recovery of every original source,
   monic distinct pivot variables, pivot-free tails, and reduction of all 38
   original sources to zero.  Decide whether these facts really make normal
   form kill the entire ideal generated by original FIRST, not merely the
   displayed rows or an H5 splitting.

4. **Functional well-definedness.** Audit the K-linear functional

   ```text
   lambda(g) = E3 scalar coordinate 0 of coefficient
               [empty parameter monomial, q14]
               in NF_FIRST(g mod F), K=Frac(Q[V,U]).
   ```

   Check that it factors through the claimed quotient, is not contaminated
   by pivot variables or q-coordinate truncation, and that the emitted exact
   numerator is a genuinely nonzero polynomial.

5. **Denominator firewall.** Verify exactly
   `den(lambda(P12))=V^3*(V^2-4U^3)^2`, and on F=0 the identities `B3=V^4`
   and `U*H=V^2-4U^3`.  Confirm this licenses every denominator on
   `D(U*H*B3)` without silently inverting a new factor.

6. **Fresh-prime independence.** Check primality of 1000033, the point
   `(C,V,U)=(15,4,1)`, `F=0,H=12,B3=256`, the two separate original-FIRST
   inversions at q14=0 and 1, complete-class equality with exact evaluation,
   and selected value `159760 != 0`.  Treat this as validation, not as the
   proof of the characteristic-zero theorem.

7. **Custody and omissions.** Verify Box02/r6d mathematical artifacts and
   stdout are byte-identical, source/payload hashes are complete, and the
   V1 missing-import erratum is accurately classified.  Inspect the client
   for reporter assertions that accidentally assume the conclusion.

8. **Conclusion strength.** The strongest allowed conclusion is one
   nonzero q14 cokernel class modulo full original FIRST+(F) in the exact
   low-q-zero/sheared-q15 scope.  It is not `1 in (P12,FIRST,F)`, a source
   point exclusion, a low-q unit-chart result, total-Rees lifting, whole A3,
   TD6, SP-2, or JC2.

Report every issue with exact file and line references.  If repairable, give
the smallest exact wording/code repair and say which claims survive.

