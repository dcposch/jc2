# Significant-news blind whole-46 delta — D1 owner

Date: 2026-08-26 04:40Z

Independence: written after the V8 producer packet and current canonical
ledgers, without reading any other packet from this significant-news round.
The V8 hostile review and all AWS lanes continued asynchronously.

## News and rank changes

The new D1 fact is qualitatively stronger than another threshold sample.  In
the fixed chart

```text
a=1, p=-3, c=2+h, k=nu=0, mu=2/3,
```

the two q-linear corrections `F1-=h*q1/36`, `F2-=h*q0/72` produce one exact
48-term identity with `Lambda^20` uniformly unique for every `eta>=0` on the
open `u,v>0` cell.  Subject to hostile review, positive-h filtration on this
chart is finished: there is no reason to compute further h grades.  The D1
question has changed from filtered lifting to coordinate/stratum coverage.

Other post-04:15 evidence changes the surrounding portfolio: exact order two
has a reviewed first normal/divisibility ideal; TD6's generic FIRST map is
surjective and its producer-tier previous/pole map vanishes on all 24
transport directions; exact order four remains completely eliminated.

My independent 46-row triage is:

- raised by the new evidence: avenues `2,4,25,28,31,38,46`;
- active but not materially reranked: `1,3,6,7,9,19,21,26,27,29,32,36`;
- reserve/tooling or anomaly-triggered only: `5,8,13,14,18,20,33,34,35,37,43,44,45`;
- unchanged low/refuted/category-error: `10,11,12,15,16,17,22,23,24,30,39,40,41,42`.

Thus all 46 canonical avenues were reconsidered.  The rank increase is not a
claim that D1 is globally closed: it reflects that explicit initial-ideal
certificates, Rees valuations, boundary-coordinate actions, and formal
finite-determinacy now meet at one concrete transport problem.

## New connections and experiments, ranked

### 1. Normalize the entire smooth discriminant by an exact coordinate-action certificate

The depressed double-root locus is

```text
(p,c)=(-3a^2,2a^3),   K=(z-a)^2(z+2a).
```

On `a!=0`, every nearby cubic becomes, after the etale choice
`a^2=-p/3`, `c=2a^3+h`.  Scaling `z=a*w` sends the axis to `a=1`; the
ordinary-tail rows and loads should transform by explicit characters after
rescaling Q, R, Lambda, and the target rows.  If the V8 identity is a
semi-invariant of this action, it transports verbatim to every smooth point
of the discriminant and every normal valuation, rather than merely to one
axis.

Cheapest falsifier: on AWS, solve the integer character equations for every
monomial in the nine frozen rows, eight V8 multipliers, and both target
loads.  Emit the first inconsistent monomial if no common character exists;
otherwise substitute the resulting powers of `a` and verify the exact
universal identity with `a` symbolic.  This is the single highest-value next
D1 experiment.

### 2. Convert smooth-discriminant coverage into a one-point support theorem

In the axis/normal coordinates,

```text
Delta=-4p^3-27c^2=-27*h*(4a^3+h).
```

For unit `a` and positive h valuation, `4a^3+h` is a unit, so h and Delta
have the same valuation.  Combine three exact objects: the squarefree
order-20 theorem on `D(Delta)`, the V8 normal witness, and the coordinate
action above.  The intended conclusion is not D1 emptiness but a support
statement: any remaining nonunit Rees/initial endpoint over the `(p,c)`
plane is supported over the singular discriminant point `p=c=0`.

Cheapest falsifier: form the source-identical universal row module over
`Q[a,h]`, localize at `a(4a^3+h)`, and check the transported identity
coefficientwise.  A single uncancelled target/load monomial refutes the
support claim.  Do not infer this from fibrewise witnesses without the
universal identity.

### 3. Use the discriminant cusp's weighted blow-up as the complete next fan

The only point left by card 2 is the triple root.  Its natural blow-up has
weights `wt(a)=1`, `wt(h)=3`, since `Delta=-27h(4a^3+h)`.  The chart
`s=h/a^3` is precisely where coordinate transport of V8 should apply; the
complementary chart is the genuinely new triple-root normal form.  This
replaces an uncontrolled Newton fan by two geometrically forced charts.

Cheapest falsifier: transport all 48 V8 weight forms with symbolic axis
valuation `A` and list their exact halfspaces in `(A,H,beta,delta)`.  If the
certificate covers `H>=3A`, freeze that cone and launch only the `H<3A`
triple-root chart.  If it fails within `H>=3A`, the first failing monomial
identifies the missing axis/load correction.  This is also the cleanest
finite-determinacy firewall: no assertion beyond the two weighted charts.

### 4. Identify D1 strata with collision edges in the Davenport--Zannier tree boundary

The terminal frontend is a weighted bicolored plane tree; D1's cubic root
patterns are collision strata of the lower-tail common cubic.  The
squarefree/double/triple discriminant stratification should therefore label
edges and vertices of the compactified terminal-tree moduli.  If the
constructive Lu--Song permutation model supplies all collision adjacencies,
then the source-to-D1 landing debt may reduce to a finite labeled adjacency
check for each terminal profile, with the V8/squarefree certificates attached
to every nontriple edge.

Cheapest falsifier: for the reviewed `U<=4` census, generate the collision
adjacency graph of the 49 classes and compare its local multiplicity data
against the D1 squarefree/double/triple charts.  One legal degeneration not
represented by those charts kills the proposed bridge.  Success would be a
small, auditable prototype of `G2-PSC`, not the global bridge itself.

### 5. Reuse the D1 filtered corrections as a homological-perturbation template for TD6

V8 shows that successive low-grade residuals are killed by a sparse right
inverse to the initial row map and that the process can terminate in a finite
polynomial identity.  TD6 now has exactly the analogous prerequisites: a
reviewed surjective FIRST map and a producer-tier zero previous/pole map on a
24-dimensional kernel.  Choose and freeze one exact right inverse to FIRST,
then transfer the quadratic previous/pole obstruction to the kernel by the
homological-perturbation formula instead of repeatedly rebuilding all axis
lifts.

Cheapest falsifier: compute one four-axis quadratic block both by the frozen
full replay and by the transferred formula on AWS.  Any mismatch rejects the
shortcut; equality licenses the 21-block parallel compiler and gives a
canonical Kuranishi/Fitting source.

### 6. Make explicit witness cones a reusable proof object

V8's mathematical endpoint is only 48 rational coefficients plus affine
weight forms.  A generic certificate schema can check: source hashes, exact
row combination, distinguished target coefficient, and positivity of every
other affine form on a rational polyhedral cone.  The same schema applies to
order-two first-contact strata and tropical/Rees clients, while a small Lean
theorem can consume the emitted certificate without claiming the global
landing theorem.

Cheapest falsifier: make the schema reproduce the reviewed q2/V4--V7 and V8
thresholds from frozen records, including endpoint strictness.  A mismatch is
a software/control failure; success reduces review latency but is not itself
a JC2 avenue.

## Allocation delta

Do not spend more D1 capacity on positive-h grades in the fixed chart.  Give
D1 a short, high-intensity transport burst: two independent coordinate-action
encodings, one universal `Q[a,h]` identity, and the weighted cusp two-chart
split.  If card 1 fails, repair only its first inconsistent character/load;
if it succeeds, shift nearly all D1 compute to the triple-root chart.

Provisional new allocation of fresh capacity is `50/15/30/5` for exact
order two/order one, D1 transport/triple-root, TD6 quadratic/current, and
protected global/formal work.  D1's percentage falls because V8 removes the
entire positive-h serial ladder, not because its remaining coverage theorem
is low value.  The best experiment outside D1 is the exact order-two
first-normal ideal decomposition followed immediately by lower-tail/Taylor
tests on each nonunit component; the best cross-portfolio software investment
is the shared sparse homological-perturbation/certificate compiler.

## Firewalls

V8 is producer-tier until its hostile review closes.  Even after review it is
one fixed axis/cusp/load and one charged weight cell.  Coordinate covariance,
Delta-only support, collision-tree landing, and finite determinacy are new
theorem obligations, not consequences of fibrewise success.  No statement
here closes the triple-root point, all D1, maximum twelve, a cofinal degree
bound, or JC2.
