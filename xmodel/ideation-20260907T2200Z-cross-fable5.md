# Cross-pollination — 2200Z round — fable5

All 17 frozen inputs re-hashed against `cross-input-pins.json`: every sha256 matches. Collector index and all four blind bodies read whole. History checksum here is limited to the charged frozen APPROACHES, REDUCTION, COORDINATION, packet and four D125 reports; AUDIT, PROGRESS and notes snapshots are NOT charged in this round (explicit source ignorance). One stdlib control, `box/ideation-20260907T2200Z-cross-fable5/control.py`: 8 capped runs normal/-O, 0.1 s each, zero Assert nodes; positive mode passes, the changed-lift and changed-low-row mutations fail, the parity mutation is non-discriminating for the g² row (support-forced). No theorem, point, unit or global125 claim; finite-boundary exclusion is not guarded emptiness.

## 1. Dedup corrections

- **X3 has two branches under one premise**, omitted by the index: odd j gives C³≡0 mod V hence V|C, a kill against 14v; even j gives the square-class test with D=F_{3j/2}. Both depend on the same missing arrow.
- **X2, root's cubic geometry and X3 are three consumers of ONE premise** (§2); keep them distinct, but the index should list them as a consumer tree, plus the new nodal-branch kill.
- **X5 counter-controls**: add the p=0 axis (below). No mistaken merge otherwise; Sol's card 2 is correctly separated from Astra C.

## 2. Compatible combination: common premise and consumer map

Ring K[g,p]/(V), K=Q̄, V=g³+p³−3p, E its smooth closure, σ=(−g,−p), fixed points O and the three points at infinity. **Premise (Σ)**: the earliest residue of identity (I) not divisible by R is a scalar polynomial relation Φ(C,D)=0 in K[g,p]/(V), with C=F_j/R even (14v), D odd and REGULAR, C(O)=0, V∤C, ord_O(C)≥4. Consumers, each keeping its own hypotheses:

- quadratic D²=C(aC+b): a≠0 killed by the 14y unit-parity lemma; a=0,b≠0 gives C=D²/b, a square class.
- cubic D²=C(C²+aC+b): b≠0, a²≠4b is root's smooth genus-one kill, verified at desk (Γ smooth, (0,0) nonsingular with local parameter D, ord_{(0,0)}C=2, so e_O=ord_O(C)/2≥2 while Riemann–Hurwitz in char 0 forbids ramification). b=0,a≠0 is a NEW two-line kill: ψ=D/C is odd, ψ²=C+a has order 0 at O, but an odd function vanishes at a σ-fixed point. a²=4b≠0 gives C=(D/(C+a/2))²; a=b=0 is X3.
- **Residual of every surviving branch**: C|_E=ψ² with ψ odd, regular on affine E (div D=(3/2)div C there). Since dσ=−1 at each fixed point, odd functions have odd order, so ord(C)≡2 mod 4 at O and at the three infinity points, even orders elsewhere. On the 13 admissible slots ord_O(g^ip^j)=i+3j, so **[gp]C=0 is forced, and [g²p²]C=0 whenever [p²]C=0**. The 14y collision has ord_O(C)=10, consistent.

Correction to my blind: this is a geometric divisor-parity condition on E/σ≅P¹ (no 2-torsion obstruction on P¹), not "Kummer/2-descent"; the arithmetic square class over Q is irrelevant to Q̄-nonemptiness. The single missing arrow for all three consumers is (Σ) itself: with α_i≠0 for some i<j, terms α_i[R,G_b]≡α_i·p²[V,G_b] mod V enter orders below 3j, are neither R-multiples nor squares, and break both the V|F_a induction and the residue shape. Root's low-alpha adjudication is consumed only as "claims 1–6 confirmed, toys overclaim"; no source-to-relation arrow is inferred from it.

## 3. Attack on the strongest proposal I did not originate: Sol's k-grading (X4)

**What works.** Degree-deficit weights wt(A_ij)=15−i−j, wt(B_kl)=25−k−l make every Jacobian, fixed-face, support and parity row homogeneous with wt(k)=12, wt(e)=24. The g² Jacobian row is exactly −k·e by the supports alone (control §3), consistent with a k³ target of weight 36. So a grading exists on the torus-invariant part of the source.

**First missing arrow: the ordinary-lift rows.** φ(p)=v⁴u−v−v⁻¹ mixes total degrees. The (u⁰,v⁻¹) row of φ(A) reads
```
-4167 - a - k + 2x - 3y - 10A05 + 6A14 - 3A23 + A32 + ... (43 of 44 slots)
```
with the constant −4167 from the fixed face H³ (B side: −2452815 + e − …). A generator with a nonzero constant and k at coefficient −1 is inhomogeneous for every nonnegative grading with wt(k)>0. So the generator-level LP Sol proposes returns INFEASIBLE for this trivial reason and measures nothing about components; "mandatory constant rows" is exactly where it dies. Deleting lift rows is SCOPE-CONFLICT: they are the receiver-to-physical map.

**Ideal-level residual.** J homogeneous means each weight component of that row lies in J. The low rows force wt(x)=wt(y)=wt(k) and wt(e)=2wt(k), and by 9e=5kx, x²=3ky the k-component satisfies on U
```
-k + 2x - 3y = -(9e - 5k^2)^2 / (25 k^3)      (control §4)
```
Exact consequence: for the natural grading the k-component is in J iff 9e=5k² on U, i.e. iff the 14c g²-target constant is −(5/9)k³. One read of that constant decides it. If it is anything else, any admissible grading needs another slot of weight wt(k) carrying a nonzero function on U, and the question becomes J-membership, not linear feasibility. Map issue: hybrid81 coordinates are k-localized; the unguarded 14t ring S must be used.

## 4. Attack on the strongest competitor: my own X3

Source arrow: identical to (Σ); my blind derivation is the frozen ansatz h=α=δ=0 above j, and §2 names the contaminating terms. Regularity: ψ=3D/C is regular only if D is a polynomial coefficient; under contamination D is no longer F_{3j/2}. Existing collision: C≡(pL²)² satisfies X3 trivially, so X3 has never pruned anything; it is a weaker necessary consequence of the frozen 3j equation, and its only value is a C-only test (odd ψ in L(4·∞) has dimension 5 against 13 slots, so the passing locus is proper). Novelty downgraded to "prepared consumer"; no pruning exists before (Σ). Cheapest falsifier: any actual residue calculation producing C with [gp]C≠0 refutes (Σ) or X3.

## 5. hybrid81 is SPENT

Facts: WALL_TIMEOUT at 300.082 s, RSS 0.97 GB, input 1.27 MB: time-bound, not memory-bound. No identical rerun, cap or order change. Recommendation: (0) read the seven retained artifacts for the engine's degree/step position, zero cost; (1) ONE modular run at ONE prime on the identical input under the identical 300 s cap, with the F4 degree/matrix log as the measured output. The observation that changes action is completion versus non-completion. Non-completion: STOP the 81-variable presentation as structure-bound; no cap would rescue it. Completion with a proper basis: the certified endpoint remains the accepted exact certificate (exact verified proper basis, or an exact point in a nonzero finite Q-algebra checked against every row via the Laurent cover), and the modular basis only supplies leading terms and dimension for that single exact attempt; modular properness is not evidence. Completion with [1]: a signal, banked, needing lifted cofactors. Primes are not votes. Stop rule: one modular run, then either one exact certification attempt or STOP.

## 6. Furter (X5)

Core passed; no client. Named candidates at desk: g=0 axis (Astra): a(0)=b(0)=0 and a'(0)=a01=0 give (a∘b)'(0)=0, multiplier-one fails, contact exactly 1, confirmed. p=0 axis (new, tiny): A(g,0)≡0 and B(g,0)=e·g by support (control §3), so no client. Plane dynamics: Hénon basins without critical points, no analogue. Sol's route typing names no route: GAP; my recollection that the licensed descent is a negative-power substitution is not in the charged set and is not used. Verdict: STOP the interface pass; keep 5/8/23/43 unchanged.

## 7. Revised top-three allocations

1. **Exact-decision lane**: the §5 measurement is the cheapest separating experiment for the largest allocation (a worker plus root time). Outcomes and stop rule as in §5.
2. **Pure center**: consumers are ready (§2 tree); allocate only to the arrow (Σ). Stop rule: if the low-alpha six statements yield neither ord(α)≥j nor a regular scalar relation, stop consumer preparation and record (Σ) as the wall.
3. **X4**: replace the LP by the one-line target-constant read (§3); pursue membership only if the constant is −5/9. Not a raise of avenue 38.

X6 unchanged: no uniform invariant was supplied by any report.

## 8. Lanes

- hybrid81: SPENT → §5. Low-alpha: CONTINUE at its scope, uncharged here. Volta grading discriminator: not read; root should confirm it includes lift rows and fixed-face constants, else its verdict is vacuous.
- Furter: STOP after the core gate. Broad sweep: CONTINUE at the September 8 07:56:45 backstop. K16, degree 99, D108: unchanged retired/stopped.

## 9. Systems

NO_CHANGE. No newly observed defect. If the retained artifacts contain no progress log, verbose progress capture on capped solves is the only candidate worth one test; that is a conditional, not a request.

## 10. Changes to my blind

Card C: "several primes" becomes one prime with a log, artifacts read first, explicit stop rule. Card A: Kummer wording corrected, pruning claim downgraded, ord≡2 mod 4 consequence and nodal kill added, placed as the residual of the consumer tree. Card B: REDESIGN becomes STOP after the core, p=0 axis added. All 46 avenues unchanged stands.

## 11. Classification after the limited checksum

X1 successor KNOWN mechanism, NEW stop rule. X2 KNOWN (14y). Root's genus-one pairing NEW, conditional. X3 NEW, conditional; nodal kill NEW tiny. X4 SCOPE-CONFLICT if lift rows are dropped, otherwise reformulated NEW (membership). X5: Astra's screen NEW from 14g, p=0 axis NEW, Sol's route GAP, root's pass STOP. X6 KNOWN. No exit price asserted.

<!-- BODY-END -->
