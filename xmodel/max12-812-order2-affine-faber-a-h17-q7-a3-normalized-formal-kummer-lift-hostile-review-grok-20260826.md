# Hostile review — H17/q7/a3 normalized formal Kummer lift

| Field | Value |
|---|---|
| Charged theorem | `xmodel/max12-812-order2-affine-faber-a-h17-q7-a3-normalized-formal-kummer-lift-theorem-20260826.md` |
| Theorem SHA-256 | `8af6d72e0044314ca091c4ae0d74876f57ba718a572ccde6f32a597fa2e41a97` |
| Grade-48 predecessor review | `e7df100dcbed28bf85e14108bb033c1663ae6334c810696e31d9927362a17f1c` |
| Sequential grade-51 review | `465f53cd423d94645f0b99f4e96d18853bb469b96dc2f85590dc7fe912d158ec` |
| Sequential V6 `RESULT.md` | `4cfbda6e3188e72460aa46d5a2d5bf95b384755d53551d883b569df00666fc7f` |
| Overall verdict | **CONFIRMED** |
| Smallest failing identity | none |
| Smallest missing hypothesis | none inside the normalized complete-local graph on the two registered opens `D(a3 p m Xbar(0))` and `D(a3 p m Ybar(0))` |
| Reviewer / model | Grok 4.6 (xAI). Independent hostile audit of the all-orders composition. Different model family from the producer. No producer status line, no charged `PASS`/`ENDPOINT` token, no validator string, and no finite `CONFIRMED` banner is itself an all-orders proof |
| Method | SHA-256 of every charged pin before reading producer promotion language; independent reconstruction of the first-block successive pivots from the confirmed grade-48 face; independent substitution of that face into the frozen grade-51 functionals; independent product of the seven triangular diagonals including sign and the powers of `a3,p,m,x/y,2`; structural weight argument that the `t=0` second Jacobian cannot see a future jet of `(d6,kappa)`; coefficient form of the complete-local implicit-function theorem applied in two stages. Characteristic 65521 was not used as characteristic-zero algebra |
| Repo | `/Users/dc/code/math/jc2` |
| Git HEAD | `418e413593120d19e15e6546eb50c985f4b1f038` |
| Date | 2026-08-26 |

Independently recomputed SHA-256 of the four files named in the review
prompt match those pins. Producer status language in the theorem
("PROVISIONAL THEOREM", "dual-AWS PASS", "under hostile review") was
not used as algebra. The finite grade-51 hostile review is itself a
charged pin and is now `CONFIRMED`; that fact licenses only the constant
face, the two projective charts, divisibility of the remaining rows
through weight 50 after the first five solves, and the two constant
formulas for `kappa(0)` and `d6(0)`. The all-orders step was rederived
from the implicit-function theorem, not inferred from a longer finite
truncation. No file other than this review was written. Charged
artifacts, shared ledgers, and `jc2-lean` were not edited.

---

## Pin / verdict table

| Pin | SHA-256 | Role | Verdict |
|---|---|---|---|
| charged theorem | `8af6d72e0044314ca091c4ae0d74876f57ba718a572ccde6f32a597fa2e41a97` | all-orders composition under review | **CONFIRMED** |
| grade-48 predecessor hostile review | `e7df100dcbed28bf85e14108bb033c1663ae6334c810696e31d9927362a17f1c` | constant first face, two charts, load graph, vanishing below 48 | match; finite algebra consumed |
| sequential grade-51 hostile review | `465f53cd423d94645f0b99f4e96d18853bb469b96dc2f85590dc7fe912d158ec` | first five pivots through 51, `P5=P7=0` in 48--50, `K51`/`H51` | match; finite algebra consumed |
| V6 `RESULT.md` | `4cfbda6e3188e72460aa46d5a2d5bf95b384755d53551d883b569df00666fc7f` | navigation only; finite producer | match; not all-orders evidence |

Smallest failing identity: none.

Smallest missing hypothesis: none inside the stated complete-local
normalized graph. Literal source/Rees accessibility, both Taylor
pullbacks, terminal `[6,2]`, order two, maximum twelve, and JC2 are
nonclaims, not holes.

---

## Verdict

**CONFIRMED.**

On a complete local characteristic-zero algebra with uniformizer `t`, in
the already registered two-sided normalized affine-Faber coordinate
chart with the displayed load graph, the seven complete ordinary-Faber
row series are well-defined formal functions of the coordinate series.
Dividing the five rows `(P1,P3,P6,P2,P4)` by `t^48` is a formal
isomorphism on that graph: the grade-48 predecessor supplies vanishing
below 48 after load-graph cancellation, and the successive unit pivots

```text
3m/4,   -3 x m/8,   p^4/128,   -1,   -1     (X chart),
3m/4,   -3 y m/8,   p^4/128,   -1,   -1     (Y chart)
```

are the constant Jacobian of that first block on
`D(a3 p m Xbar(0))` and `D(a3 p m Ybar(0))` respectively. The same
matrix recurs at every higher coefficient. After substituting the first
formal graph, the registered combinations `K=E H3+H5` and
`H=2 E^2 P3+16 E P5+64 P7-(E^3/2) P1` are divisible by `t^51`, and the
constant Jacobian in `(d6,kappa)` is triangular with diagonal
`(-a3 p^5/32, 5 a3^3 p^7/4)`. Both entries are units on the same opens.
The product of the seven successive pivots is

```text
45 x m^2 a3^4 p^{16} / 2^{19}
```

on the `X` chart, and the same monomial with `x` replaced by `y` on the
`Y` chart. Standard coefficient recursion therefore produces a unique
formal solution in the seven dependent series for every sufficiently
centred choice of the remaining formal inputs, including arbitrary
`mu6=t^{54} nu6` and an arbitrary unit-leading determinant target
`J=t^{57} j`. The constant receiver and `d6(0)` formula are exactly the
two finite grade-51 identities. The statement is a normalized
complete-local graph theorem only.

**CONFIRMED**

---

## Hashes and charged artifacts

Recomputed SHA-256, all matching the required pins:

| Artifact | SHA-256 | Role |
|---|---|---|
| charged theorem | `8af6d72e0044314ca091c4ae0d74876f57ba718a572ccde6f32a597fa2e41a97` | all-orders claim |
| grade-48 predecessor review | `e7df100dcbed28bf85e14108bb033c1663ae6334c810696e31d9927362a17f1c` | finite first face |
| sequential grade-51 review | `465f53cd423d94645f0b99f4e96d18853bb469b96dc2f85590dc7fe912d158ec` | finite second face |
| V6 `RESULT.md` | `4cfbda6e3188e72460aa46d5a2d5bf95b384755d53551d883b569df00666fc7f` | finite producer; navigation |

No other file was a charged pin. Finite `PASS_*` tokens, AWS banners, and
characteristic-65521 stdout were not used as characteristic-zero
evidence.

---

## Attack 1 — complete row functions; first five divided equations; unit triangular Jacobian on both charts

The ordinary-Faber tails are polynomials in the ten-slot factors
`(B0,...,B6,K10,K6,K2)`. Substituting the displayed complete series

```text
a=t^3 A,     lambda=t^{17},
X=t^7 Xbar,  Y=t^7 Ybar,
R_i=t^{14} Rbar_i,  S_i=t^{14} Sbar_i,
K10=t^{42} kappa,
K6 =t^{42}((15/32) kappa E^2 + t^6 d6),
K2 =t^{42}((15/256) kappa E^4 + t^6 d2),
mu2=t^{42}(-(5/4096) kappa E^6 + t^6 dm),
mu4=t^{48} d4,
mu6=t^{54} nu6,   J=t^{57} j,
```

with `A(0)=a3`, `E(0)=p`, `M(0)=m` yields seven formal series `P1,...,P7`
in `t`. This is polynomial composition, not a truncated tail and not a
finite jet window. The load formulae are a coordinate reparametrization
of the registered graph (the pairs `(K10,K6)`, `(K10,K2)`, `(K10,mu2)`
are tied through weight 47, with first free corrections at weight 48),
not a claim that an arbitrary load of valuation 42 is being parametrized.

The grade-48 predecessor supplies vanishing of all seven rows in weights
`0..47` after that graph cancellation, including the two genuine centre
terms at weight 48. Division of `(P1,P3,P6,P2,P4)` by the uniformizer
power `t^{48}` is therefore an isomorphism of formal series, not a
division in the residue field. The constant terms of the divided rows
are the confirmed grade-48 polynomials

```text
C1 =  (3/4) m s0 - (3/8) m^2 r1,
C3 =  (3/16) m p s0 - (3/8) m x y - (3/32) m^2 p r1,
C6 =  (1/128) d2 p^4 - (1/512) d6 p^6
      - (3/64) m^2 p^2 r0 + (3/64) p^2 y^2 - (15/1024) kk a^2 p^7,
C2 =  (3/128) d6 p^4 - (1/8) d2 p^2 - dm
      - (3/8) m^2 r0 + (3/8) y^2 + (15/128) kk a^2 p^5,
C4 =  - d4 + (3/32) m^2 x^2 - (3/16) m^2 p r0 - (3/16) p y^2.
```

Independent successive elimination, in the charged order of dependent
series `(Sbar0, Ybar, d2, dm, d4)` on the `X` chart and with `Ybar`
replaced by `Xbar` on the `Y` chart:

- `∂C1/∂s0 = 3m/4`, a unit on `D(m)`. Solving gives `s0 = (1/2) m r1`.
- After that substitution the `r1` pair in `C3` cancels and
  `C3 = -(3/8) m x y`. On `D(x m)` this is a unit times `y`, so
  `Ybar(0)=0` on the `X` chart; on `D(y m)` it is a unit times `x`.
  The displayed second pivots `-3 x m/8` and `-3 y m/8` are exactly
  these remaining linear coefficients.
- `∂C6/∂d2 = p^4/128`, a unit on `D(p)`. The `y^2` term in `C6` is
  quadratic. On the `X` chart it vanishes with `y=0` and does not
  change the `d2` coefficient. On the `Y` chart `y` is free and `x` is
  dependent; `C6` has no `x` at this weight, so the `d2` pivot is
  unchanged.
- `∂C2/∂dm = -1` and `∂C4/∂d4 = -1` after the `d2` solve. Neither
  coefficient involves a non-unit.

The off-diagonal `∂C6/∂Ybar` is proportional to `Ybar` and vanishes at
the `X`-chart point `Ybar(0)=0`; `∂C4/∂Xbar` is proportional to `Xbar`
and vanishes at the `Y`-chart point `Xbar(0)=0`. Evaluated at the
constant solution, the first 5×5 block is triangular with the stated
diagonal. These five numbers do not involve `d6(0)` or `kappa(0)`, so
the first-block Jacobian remains a unit for arbitrary values of the
second-block constants. Targets `mu6` and `J` have valuations 54 and 57
and cannot meet the constant face.

This is the Jacobian of the complete divided series at `t=0`, not a
grade-48 truncation of some other matrix: the constant term of
`t^{-48} P_i` is precisely the weight-48 coefficient. Higher jets of
`A,E,M,Xbar,...` first appear in strictly positive powers of `t` and
do not affect the constant Jacobian. The implicit-function theorem
then uses this same invertible matrix as the coefficient of every new
unknown `U_n`.

---

## Attack 2 — after the first graph, `K` and `H` are divisible by `t^{51}`; second diagonal

The registered inverse-Faber combinations used by the finite grade-51
source, and invoked by the theorem, are the complete polynomials in
`B=4a`:

```text
H3 = P3 - (B/2) P2 + (5 B^2/32 - E/4) P1,
H5 = P5 - B P4 + (21 B^2/32 - 3 E/4) P3
     + (-5 B^3/16 + 3 B E/4) P2
     + (195 B^4/2048 - 45 B^2 E/128 + 5 E^2/32) P1,
K  = E H3 + H5,
H  = 2 E^2 P3 + 16 E P5 + 64 P7 - (E^3/2) P1.
```

After the first block, `P1=P2=P3=P4=0` as formal series (four of the
five solved rows; `P6=0` is the remaining first-block equation, used to
solve `d2` but not needed for this reduction of `K,H`). Every
positive-`B` summand is then zero, and

```text
K = P5,     H = 16 E P5 + 64 P7
```

identically as series, not merely at weight 51. The sequential grade-51
review establishes `P5=P7=0` in weights 48, 49 and 50 on both charts
after the five ordinary pivots at each of those grades, with a jet
window covering every abstract monomial that can reach weight 51. Those
twelve vanishing compatibilities are therefore exactly divisibility of
`K` and `H` by `t^{51}` after the first formal graph. Defining
`Khat=t^{-51} K` and `Hhat=t^{-51} H` is again a formal isomorphism.

A lookahead attack on this divisibility would require a first-block jet
of excess `>=4` to meet weight `<=50`. The predecessor's complete
abstract valuation cut already kills every monomial below 48 as a
polynomial identity in the coordinates, so a higher jet of the same
monomial remains zero. The sequential review's excess-3 window is
complete for weights `<=51`. No omitted `s04`, `d24`, or `kk4` term can
create a coefficient of `t^{50}` in `K` or `H` after the first graph.

The constant second block is the Jacobian of `(Khat(0), Hhat(0))` in
`(d6(0), kappa(0))` after substituting the first-block *constant*
solutions. The frozen raw functional

```text
K51 = 4 d40 a3 - dm0 p a3 + (1/8) d20 p^3 a3
      - (9/128) d60 p^5 a3 + (3/2) y0^2 p a3
      - (3/8) x0^2 m^2 a3 - (1/16) m^3 p
      - (65/128) kk0 a3^3 p^6
```

depends only on excess-0 first-block names together with `d60,kk0`.
Structurally this is forced by the weights: `d2,dm,d4,d6` enter `K6,K2,
mu2,mu4` at weight 48, and `a=t^3 A` supplies the extra weight 3 that
lands at 51, so `[t^{51}] K` sees `d20,dm0,d40,d60` and cannot see
`d21` (weight 49+3=52). Independently substituting the grade-48
predecessor

```text
d40 = 3 x0^2 m^2/32 - 3 r00 m^2 p/16 - 3 y0^2 p/16,
d20 = d60 p^2/4 + 6 (r00 m^2 - y0^2)/p^2 + (15/8) kk0 a3^2 p^3,
dm0 = - d60 p^4/128 - (9/8) r00 m^2 + (9/8) y0^2 - (15/128) kk0 a3^2 p^5
```

into `K51` produces the coefficient identities, all checked in
characteristic-zero rationals,

```text
d60 p^5 a3 :  1/128 + 1/32 - 9/128 = -1/32,
r00 m^2 p a3 : -3/4 + 9/8 + 3/4     =  9/8,
y0^2 p a3    : -3/4 - 9/8 - 3/4 + 3/2 = -9/8,
x0^2 m^2 a3  :  3/8 - 3/8            =  0,
kk0 a3^3 p^6 : 15/128 + 15/64 - 65/128 = -5/32.
```

Hence after the first graph

```text
K51 = - (a3 p^5 / 32) d60 + (9/8) a3 p (r00 m^2 - y0^2)
      - (5/32) kk0 a3^3 p^6 - (1/16) m^3 p,
```

and `∂Khat/∂d6 |_{t=0} = - a3 p^5 / 32`. This is a unit on `D(a3 p)`.
The off-diagonal `*` is `-5 a3^3 p^6 / 32` and is irrelevant to
invertibility.

The frozen raw functional

```text
H51 = -2 m^3 p^2 + (5/4) kk0 a3^3 p^7
```

contains none of the twenty-one first-block names and no `d60`.
Sequential substitution cannot change it. Directly,
`∂Hhat/∂d6 |_{t=0} = 0` and `∂Hhat/∂kappa |_{t=0} = 5 a3^3 p^7 / 4`,
a unit on `D(a3 p)`. Equivalently, the series identity
`H = 16 E P5 + 64 P7` after the first graph, together with the
grade-51 expansions of `P5` and `P7` before the `d60` solve, cancels
the `d60` and `r00` (and `y0^2`) pairs:

```text
16 p · (-a3 p^5 / 32) + 64 · (a3 p^6 / 128) = 0,
16 p · (-5 a3^3 p^6 / 32) + 64 · (15 a3^3 p^7 / 256) = 5 a3^3 p^7 / 4.
```

The displayed triangular matrix is therefore the actual `t=0` Jacobian
in `(d6,kappa)`, not a Gaussian artefact of a longer truncation.

---

## Attack 3 — full determinant, sign, and powers of `a3,p,m,x/y,2`

The triangular representative is the ordered pair
equations `(P1,P3,P6,P2,P4,K,H)` against dependents
`(Sbar0, Ybar, d2, dm, d4, d6, kappa)` on the `X` chart, and with
`Ybar` replaced by `Xbar` on the `Y` chart. Its determinant is the
product of the seven successive pivots. In characteristic zero,

```text
(3m/4) · (-3 x m/8) · (p^4/128) · (-1) · (-1)
    · (- a3 p^5 / 32) · (5 a3^3 p^7 / 4)
  = (3m/4) · (-3 x m/8) · (p^4/128) · (- a3 p^5 / 32) · (5 a3^3 p^7 / 4)
  = 45 x m^2 a3^4 p^{16} / 2^{19}.
```

The two displayed minus signs (the `Ybar`/`Xbar` pivot and the `d6`
pivot) multiply to a plus; `(-1)·(-1)` from `(dm,d4)` is plus. The
denominator is `4 · 8 · 128 · 32 · 4 = 2^2 · 2^3 · 2^7 · 2^5 · 2^2
= 2^{19}`. The monomial in the units is `m · (x m) · p^4 · a3 p^5 ·
a3^3 p^7 = x m^2 a3^4 p^{16}`. Replacing `x` by `y` is the `Y`-chart
formula. Both are units on the registered opens in characteristic
zero (`45 = 3^2·5` and `2^{19}` are invertible). This is the
determinant of the triangular representative named in the theorem, not
an unordered 7×7 matrix up to a row permutation of unknown sign.

---

## Attack 4 — all-orders step: graph substitution, recursion, uniqueness, free `mu6` and unit-leading `J`

Write `U` for the five first-block series and `W=(d6,kappa)` for the
second-block series, with remaining series collected as free inputs
`V`. The first block is a map of complete local series

```text
F(t, U, W, V) = t^{-48} (P1, P3, P6, P2, P4)
```

with a solution modulo `t` supplied by the grade-48 face, and with
`∂F/∂U` a unit at `t=0` independently of `W(0)`. The coefficient of
`t^n` is affine linear in the new unknown `U_n` with that same
invertible Jacobian, plus an inhomogeneous polynomial in lower `U`
jets and in `(W,V)` jets of order at most `n`. Completeness of the
local ring produces a unique formal graph `U=U(t;W,V)`. Substituting
a formal graph into a formal series is legitimate; there is no
analytic (convergent) division.

The second block is then

```text
G(t, W, V) = t^{-51} (K, H) |_{U=U(t;W,V)}.
```

`G(0,W(0),V(0))` depends only on constant jets: as already noted,
weight 51 cannot see `d61` or `kk1`. Its Jacobian in `W` is the
triangular unit matrix of Attack 2. The same coefficient recursion
therefore uniquely solves `W_n` at every order, for arbitrary free
`V`. Uniqueness of `(U,W)` given `V` is the uniqueness of the two
stages, equivalently the invertibility of the 7×7 triangular
representative.

`mu6=t^{54} nu6` is attached to row 6 and first meets the divided first
block at order `t^6`. It enters the inhomogeneous term of the `d2`
equation at that order and does not touch either constant Jacobian.
It may be prescribed arbitrarily. `J=t^{57} j` is attached only to
row 7 (predecessor: `P7` carries `-J/4`), hence only to `H` after the
first graph, and first meets `Hhat` at order `t^6`. The constant
second Jacobian does not involve `j`. One may therefore prescribe
`j(0)` to be a unit; the order-`t^6` equation for `kappa` absorbs
that choice uniquely. The resulting normalized ordinary-Faber formal
solution has `J=t^{57} j(t)` generically nonzero. Neither prescription
is a future-jet solve: each free jet of order `n` is an input to the
unknowns of the same order.

A Jacobian that existed only as a finite-truncation artefact would
have to change when excess-1 and excess-2 jets of `U` are restored.
Those jets do not appear in `K51` or `H51`. The implicit-function
theorem then re-uses the constant Jacobian at every later order, so
the triangular diagonals persist as the coefficients of `(d6_n,
kappa_n)` in `(Khat_n, Hhat_n)` for all `n`. Extending the finite
reduction from grade 51 to grade 52 is not required and would not be
an all-orders proof.

---

## Attack 5 — constant receiver and `d6(0)` match the finite review exactly

`Hhat(0)=0` is the frozen identity `H51=0`:

```text
-2 m^3 p^2 + (5/4) kappa(0) a3^3 p^7 = 0,
```

equivalently on `D(p)`

```text
(p^2 / 4) (5 kappa(0) a3^3 p^5 - 8 m^3) = 0,
```

hence

```text
kappa(0) = 8 m^3 / (5 a3^3 p^5).
```

This is the sequential-review residual
`-2 m^3 p^2 + (5/4) kk0 a3^3 p^7` and the V6 `RESULT.md` receiver
`5 kk0 a3^3 p^5 - 8 m^3 = 0`. No inversion of `kappa` is used; the
formula shows that `kappa(0)` is a unit on the stated open.

`Khat(0)=0` with the substituted polynomial of Attack 2, multiplied
through by the unit `-32/(a3 p^5)`, is

```text
d6(0) = 36 (Rbar0(0) m^2 - Ybar(0)^2) / p^4
        - 5 kappa(0) a3^2 p - 2 m^3 / (a3 p^4),
```

because `(9/8)·32=36`, `32·(5/32)=5`, and `32·(1/16)=2`. On the `X`
chart the first-block cross pivot has `Ybar(0)=0`, so the displayed
square drops; on the `Y` chart it remains. These are the two finite
`d60` formulae, including the localization at `a3`.

---

## Attack 6 — scope; omitted-target / nonanalytic-division / lookahead / truncation attempts

The theorem's own firewall is accurate. The objects are complete
normalized series on a complete local chart with uniformizer `t`. The
parameter `t` is not identified with a literal source/Rees parameter.
No total-Rees point, moving-source overlap, symmetric-algebra torsion,
blowup compatibility, terminal `[6,2]`, or either finite Taylor
pullback is claimed. The statement is not a polynomial Jacobian pair,
not a counterexample, and does not close order two, `(8,12)`, maximum
twelve, or JC2.

Falsification attempts that failed:

- *Omitted target.* Seven ordinary rows, seven dependent series
  `(Sbar0, Ybar` or `Xbar, d2, dm, d4, d6, kappa)`. The even target
  `mu6` and the odd target `J` are declared free and enter at 54 and
  57; complementary and tangent series are free. Attaching them as
  targets is the standard Faber convention already used by the
  predecessor (`P6` carries `mu6`, `P7` carries `-J/4`). Setting them
  to zero in the finite V5 window is correct below those weights, not
  an omission in the all-orders map.
- *Nonanalytic division.* The only divisions are by powers of the
  uniformizer after vanishing of lower coefficients, and by the
  monomial units `m`, `x` or `y`, `p`, `a3` on the registered opens.
  No residue-field non-unit is inverted. Convergence is not claimed.
- *Future-jet / lookahead dependence.* `[t^{51}] K` cannot contain
  `d61` or `U_n` for `n>=1` of the first-block series, by the weight
  count `48+3=51`. The first-block unknown `U_n` is solved from the
  same-order coefficient of the first five divided rows, using `W` and
  `V` only through order `n`. The second-block unknown `W_n` is then
  solved from `G_n`. Same-order coupling, not lookahead.
- *Jacobian as finite-truncation artefact.* The implicit-function
  theorem consumes only the constant Jacobian. That Jacobian was
  recomputed from the complete weight-48 face and the complete
  weight-51 functionals after the first constant graph, and is stable
  under restoration of every jet that can meet those weights.

The theorem header still says the finite grade-51 reduction is "under
hostile review". That sentence is stale metadata: the charged
sequential review is `CONFIRMED`. It is not a failed identity.

---

## Strongest licensed statement

On a complete local characteristic-zero algebra with uniformizer `t`,
restricted to the registered two-sided normalized affine-Faber graph
with the displayed load reparametrization, and on the opens
`D(a3 p m Xbar(0))` and `D(a3 p m Ybar(0))`, the seven complete
ordinary-Faber equations admit a unique formal solution in the seven
dependent series for every sufficiently centred choice of the remaining
formal inputs. The constant locus is the rational/Kummer receiver
`5 kappa(0) a3^3 p^5 - 8 m^3 = 0` with the displayed `d6(0)`. One may
prescribe `mu6` arbitrarily and one may prescribe a unit-leading
`J=t^{57} j`. This is not a literal-source theorem and not a Jacobian
conjecture statement.

---

CONFIRMED
