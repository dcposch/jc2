cli=codex-cli 0.152.0 model=gpt-5.6-sol effort=ultra config=ignored approval=never sandbox=danger-full-access
OpenAI Codex v0.152.0
--------
workdir: /Users/dc/code/math/jc2
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: ultra
reasoning summaries: none
session id: 01a06165-806a-7132-b508-52423915945f
--------
user
# Systems lane: OPEN-COLLISION hook + PREFLIGHT hard gate (bounded implementation)

Round 20260902T0741Z adopted one new systems upgrade and re-specified the
standing one (charged synthesis, sections 6 and 9; the Opus and Sol
submissions carry the designs):
(A) OPEN/BANKED COLLISION CHECK (Opus §8.3). Before a report is sealed,
    extract every OPEN[...] it RAISES and grep the banked corpus
    (xmodel/*.md, AUDIT.md, notes.md, APPROACHES.md) for the OPEN's key
    nouns plus the quantity it asks to bound; emit a COLLISIONS section
    listing candidate matches with file:line. Fail-CLOSED: an empty
    COLLISIONS section must be explicit, never absent. The coordinator
    ran the smallest useful test by hand and it PASSED (grep for
    OPEN[DEG-AF-VS-N]'s bounded quantity surfaces
    companion-curve-alln-opus5-20260902.md lines 685/785/836). Deliver
    ops/open_collision.py (pure python, no third-party deps), a
    focused test file ops/test_open_collision.py whose fixtures include
    that exact retroactive case and one negative case (an OPEN with no
    collision must yield an explicit empty COLLISIONS block), and a
    one-paragraph integration note on how lane.sh or seal.py would call
    it at seal time (describe; do NOT modify lane.sh or seal.py — live
    lanes are running under them).
(B) PREFLIGHT AS A HARD LAUNCH GATE (Sol §10). Specify and implement the
    encoding-faithfulness preflight as a gate for every N >= 6
    realisation job: before solver launch the generator must (i) emit the
    reduced approximate root beside the ideal in the declared coefficient
    ring and order; (ii) exact-diff each encoded coefficient condition;
    (iii) fail the old (8,6,19) false-positive canary on the corrected
    86A cell; (iv) pass the explicit four-node (9,6,2) curve on corrected
    96B; (v) fail closed on any ring / map / open-factor / generator-order
    mismatch; plus the two-field output typing
    artifact = NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT |
    FULL_EO_POINT | CURVE | REPRESENTATION | FULL_COVER | POLYNOMIAL_PAIR
    and attainment = NECESSARY | REALIZED | ADMISSIBLE_PULLBACK |
    ACTUAL_MAP | COUNTEREXAMPLE_CERTIFIED, with a checked transition table
    that rejects "A2 cell -> B3 kill", "local torsion -> global torsion",
    and FULL_EO_POINT -> ACTUAL_MAP without every declared arrow; and a
    source-fidelity canary: a generator whose EQ2_even omits -2qE1 must
    FAIL the preflight (the campaign's CELL-32 spec had exactly that
    defect; see the synthesis). Reuse the corrected ideals and semantic
    controls of xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md
    if present; deliver box/preflight.py + ops/test_preflight.py with
    those five checks and the canary as tests, fail-closed by default.
    Default stack: qqideal 0.2.0 + msolveio 0.2.1 (msolve 0.10.1); tests
    must not need msolve to run (mock or skip cleanly when the binary is
    absent).
Discipline: implementation lane, no mathematics claimed; no canonical
ledger edits; do not modify lane.sh, seal.py, artifact_finalize.py, or
any running lane's files; do not inspect jc2-lean; keep every test under
10 seconds. Report: a short typed block per deliverable (file, tests
run, pass/fail counts, integration note) in
xmodel/systems-collision-preflight-sol56-20260902.md
Seal-at-completion; bounded writes; target 8-15KB; 60 minutes.
charged_input=xmodel/ideation-20260902T0741Z-opus5.md
charged_input=xmodel/ideation-20260902T0741Z-sol56.md
charged_input=xmodel/ideation-20260902T0741Z-synthesis.md

Your inputs are frozen read-only copies in /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs;
verify these SHA-256 hashes first and stop on mismatch:

```text
e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md
717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md
4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md
```


# FALLACY-v2.md: campaign reasoning guardrail

Check before promotion.

- **Flag/place/series.** Never identify a cv flag, physical place, and cover
  series. Separate strict-below from at-level parting and denominator shedding.
- **Per-ray/exit-set charge.** Charge only a typed first-separation set; prove
  flags distinct and count each once.
- **Carrier/attainment.** `REPRESENTATIVE` is not `FULL_ACTUAL_EXIT`. The
  latter aliases `FULL_ACTUAL_FIRST_SEPARATION` and supplies only a floor,
  never attainment.
- **Pole/interior.** Use pole identities only after checking the vertex class
  and every source hypothesis.
- **Floor/attainment.** A lower bound is not exact; equality needs a theorem or
  a witness covering every omitted term.
- **`sat()` wrapping.** Extract the ideal component, assert its ring, and run
  positive and negative controls.
- **Raw remainder degree.** Normal-form in the declared quotient/localization,
  branch on vanished leaders, and handle zero.
- **Variable/ring map.** Declare the map, generator order, coefficient field,
  and image checks; matching names prove nothing.
- **Prime label/derivative.** A prime mark is a label unless the source defines
  differentiation; rename ambiguity.
- **Merge-free/M-descent.** Statement 8.5 needs each lower endpoint outside
  `V_{2,a}`. A regular dirty `V_{2,a}` step can change `M` without an MP0 merge.
- **Target/arrival index.** Keep fixed `nu_G` distinct from incoming `nu_U` and
  legacy search indices; missing route-to-state data means fallback.

If no safe replacement exists, return typed `OPEN`; never fill a gap by cap or
analogy.

For an exit claim, add exactly one line:
`charge_basis={"delta":"3/2","branch":"q>=2","flag_count":1,"citation":"path:line"}`.
Allowed branches: `q=1-exact`, `q>=2`, `multi-flag`. This declares, never
infers, a basis. Declare only a new exit-price assertion; consuming a promoted
price needs no line. Cite its mathematical source, not a prior validator result.

codex
I’ll verify the three frozen inputs first. If they match, I’ll split the collision hook and preflight gate into parallel, bounded implementation tracks, then integrate and run the focused tests.
exec
/bin/zsh -lc 'shasum -a 256 /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md' in /Users/dc/code/math/jc2
 succeeded in 0ms:
perl: warning: Setting locale failed.
perl: warning: Please check that your locale settings:
	LC_ALL = "C.UTF-8",
	LC_CTYPE = "C.UTF-8",
	LANG = "en_US.UTF-8"
    are supported and installed on your system.
perl: warning: Falling back to a fallback locale ("en_US.UTF-8").
e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md
717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md
4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md

codex
The hashes match exactly. I’m now reading the repository guidance and the charged design sections; in parallel I’ll assign the two independent deliverables to separate agents and reserve the final report/integration review here.
exec
/bin/zsh -lc "pwd && rg --files -g 'AGENTS.md' -g '"'!jc2-lean/**'"' -g '"'!**/jc2-lean/**'"' && git status --short" in /Users/dc/code/math/jc2
 exited 1 in 0ms:
/Users/dc/code/math/jc2

exec
/bin/zsh -lc 'git status --short' in /Users/dc/code/math/jc2
 succeeded in 0ms:
jc2-lean: Operation not permitted
 M pilot-local.log
?? xmodel/a2-e2-p0-gate-gpt55-20260902.log
?? xmodel/a2-e2-p0-gate-gpt55-20260902.md
?? xmodel/a2-e2-p0-gate-gpt55-20260902.run.v2
?? xmodel/a2-ubound-opus5-20260902.log
?? xmodel/a2-ubound-opus5-20260902.md
?? xmodel/a2-ubound-opus5-20260902.overflow
?? xmodel/a2-ubound-opus5-20260902.raw.md
?? xmodel/a2-ubound-opus5-20260902.run.v2
?? xmodel/b3-e-geometry-opus5-20260902.log
?? xmodel/b3-e-geometry-opus5-20260902.md
?? xmodel/b3-e-geometry-opus5-20260902.overflow
?? xmodel/b3-e-geometry-opus5-20260902.raw.md
?? xmodel/b3-e-geometry-opus5-20260902.run.v2
?? xmodel/b3-e-geometry-review-grok46-20260902.log
?? xmodel/b3-e-geometry-review-grok46-20260902.run.v2
?? xmodel/chau-delta-budget-gpt55-20260902.log
?? xmodel/chau-delta-budget-gpt55-20260902.md
?? xmodel/chau-delta-budget-gpt55-20260902.md.artifact.json
?? xmodel/chau-delta-budget-gpt55-20260902.run.v2
?? xmodel/cusp-a-n8-gate-opus5-20260902.log
?? xmodel/cusp-a-n8-gate-opus5-20260902.md
?? xmodel/cusp-a-n8-gate-opus5-20260902.run.v2
?? xmodel/cusp-a-void-countermodel-grok46-20260902.log
?? xmodel/cusp-a-void-countermodel-grok46-20260902.md
?? xmodel/cusp-a-void-countermodel-grok46-20260902.run.v2
?? xmodel/cusp-a-void-review-gpt55-20260902.log
?? xmodel/cusp-a-void-review-gpt55-20260902.md
?? xmodel/cusp-a-void-review-gpt55-20260902.md.artifact.json
?? xmodel/cusp-a-void-review-gpt55-20260902.run.v2
?? xmodel/deg-af-vs-n-opus5-20260902.log
?? xmodel/deg-af-vs-n-opus5-20260902.run.v2
?? xmodel/ideation-20260902T0741Z-fable51.log
?? xmodel/ideation-20260902T0741Z-fable51.md
?? xmodel/ideation-20260902T0741Z-fable51.overflow
?? xmodel/ideation-20260902T0741Z-fable51.raw.md
?? xmodel/ideation-20260902T0741Z-fable51.run.v2
?? xmodel/ideation-20260902T0741Z-grok46.log
?? xmodel/ideation-20260902T0741Z-grok46.md
?? xmodel/ideation-20260902T0741Z-grok46.run.v2
?? xmodel/ideation-20260902T0741Z-opus5.log
?? xmodel/ideation-20260902T0741Z-opus5.md
?? xmodel/ideation-20260902T0741Z-opus5.overflow
?? xmodel/ideation-20260902T0741Z-opus5.raw.md
?? xmodel/ideation-20260902T0741Z-opus5.run.v2
?? xmodel/ideation-20260902T0741Z-sol56.log
?? xmodel/ideation-20260902T0741Z-sol56.md
?? xmodel/ideation-20260902T0741Z-sol56.run.v2
?? xmodel/phi-immersion-adjudication-sol56-20260902.log
?? xmodel/phi-immersion-adjudication-sol56-20260902.md
?? xmodel/phi-immersion-adjudication-sol56-20260902.overflow
?? xmodel/phi-immersion-adjudication-sol56-20260902.raw.md
?? xmodel/phi-immersion-adjudication-sol56-20260902.run.v2
?? xmodel/systems-collision-preflight-sol56-20260902.log
?? xmodel/systems-collision-preflight-sol56-20260902.run.v2
?? xmodel/web-sweep-20260902-grok46.log
?? xmodel/web-sweep-20260902-grok46.md
?? xmodel/web-sweep-20260902-grok46.run.v2

exec
/bin/zsh -lc "rg -n -C 5 '8\\.3|COLLISION|OPEN\\[DEG-AF-VS-N\\]|PREFLIGHT|10\\.|EQ2_even|86A|96B|artifact|attainment|transition|msolve|qqideal' /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md" in /Users/dc/code/math/jc2
 succeeded in 0ms:
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-181-N = 4 and say whether it is N-uniform). Label: NEW as a target; mechanism
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-182-family KNOWN (row 28 reopened with data; rows 6, 27, 31 raised).
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-183-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-184-## 4. The Chau degree cap: an index failure, and a disagreement resolved by scope
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-185-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md:186:Opus §3: OPEN[DEG-AF-VS-N] — ranked first by both H2 flagships, one of
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-187-which wrote "could not find it promoted anywhere" — is answered in the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-188-form the delta-budget needs by the Chau clauses banked the same day on the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-189-REDUCIBLE branch (COMPANION C6/C7/C8; M ≤ K; sum deg D_i ≤ max(deg P,
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-190-deg Q)): under H2, deg A_F = M·max(d,e) with one place at infinity. Opus
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-191-also raises CLAIM [D] (UNREVIEWED): at (9,6) the cap is saturated by D_1
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-199-in terms of max(deg P, deg Q), which N does not bound (Opus's own
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-200-OPEN[N-VS-MAPDEG], "plausibly false"). The companion report itself flags
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-201-the cap as gauge-dependent on the x,y coordinates (line 836), which is
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-202-Grok's point from the other side.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-203-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md:204:Disposition: OPEN[DEG-AF-VS-N] RETYPED → OPEN[DEG-AF-CHAU] (answered,
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-205-cross-branch, at Chau's typing) + OPEN[N-VS-MAPDEG] (open) + Grok's
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-206-Aut-normalised min-degree question. Desk lane launched 08:22Z:
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-207-`chau-delta-budget-gpt55-20260902` (verify the transfer; delta_infty
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-208-(M,d,e) in closed form with the (9,6,2) reconciliation control; the (B2)
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-209-budget per admissible (M,d,e); CLAIM [D] with its three checks; the (B3)
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-244-Four cards: Opus — OPEN/BANKED collision check at seal time (evidence:
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-245-the DEG-AF-VS-N index failure of §4); Fable lane — an OBJECT-LEDGER per
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-246-named object (evidence: the immersed-vs-cuspidal join of §2); Grok —
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-247-certainty tags on the A2 cell ledger; coordinator — a durable LANES.log.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-248-Chosen: the collision check. Its smallest useful test was run by the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md:249:coordinator at 08:28Z: grepping the banked corpus for OPEN[DEG-AF-VS-N]'s
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-250-bounded quantity surfaces companion-curve-alln:685/785/836 — PASS.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-251-Implementation (seal-time hook `ops/open_collision.py`, fail-closed
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md:252:COLLISIONS section) is queued for a Sol systems lane in the next window.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-253-Grok's certainty words are adopted free in the LIVE STATE cell table
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-254-(Q-TWO-ENGINE (1,3),(1,5); MODULAR (1,7); BOUNDARY-THEOREM (2,6);
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-255-RUNNING (1,9)). The object ledger is the next candidate (same failure
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md:256:class). PREFLIGHT continues, unchanged.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-257-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-258-## 7. Disposition digest and lane calls
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-259-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-260-Gaps: (a) RETYPE (map-germ; DEPTH-BOUND/PARAM-U; possibly moot for N ≤ 16
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-261-pending §2); (b) RAISE — the H2 neck, attacked through E (§3); (c) CLOSE
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-309-permitted by the contract — and no sibling submission; it is blind on the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-310-round but not on the P0. Content, deduplicated against §§1–8:
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-311-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-312-- **Independent adoption of the E2 P0** ("harvest P0, review, then retire
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-313-  all old wall verdicts") and a NAMING FIX the ledger adopts now: the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md:314:  residual EQUATION is `EQ2_even`; `E2` is the coefficient polynomial
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-315-  b Z eta^2. Every A2 document must use the two names.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-316-- **DEV-PROJECTION / WALL-PIN (Card 1, NEW, corrected-system):** in the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-317-  s-free deviation system of [AU] (X = 4b eta^2 r − s^2,
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-318-  Y = 4b^2 eta q − 3a s^2, deg ≤ 4e − 1 on branch (A)), eliminate
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-319-  everything but the wall parameter c: J_e = I_e ∩ Q[c]. Unit ideal ⇒
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-342-  reviews checked, plus the negative cross-connection that the proof
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-343-  cannot transfer to (B3) (forced cusp preimage y_0; chi_c(E) = 1 − 4k).
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-344-  Already covered by §1's two arms; recorded as a third concurring read.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-345-- **PRIME-7 scout (Card 3):** as Grok's Card III, demoted by Sol itself
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-346-  to a scout "never an N = 6 substitute". Deferred with §5.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md:347:- **Systems:** promote PREFLIGHT from queued hygiene to a HARD launch
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-348-  gate for every N ≥ 6 realisation job, with the two-field
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md:349:  artifact/attainment typing and a source-fidelity canary
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md:350:  (old `EQ2_even` missing −2qE1). Evidence is strong (six earlier nodal
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md:351:  ideals were unfaithful). Disposition: PREFLIGHT remains the standing
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-352-  tracked upgrade from the prior round and Sol's hard-gate form is
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-353-  adopted as its specification; the collision check (§6) stays this
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-354-  round's one new upgrade. Both to a Sol systems lane next window.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-355-- Disposition vector: raises rows 2, 7, 25, 27, 39; retypes 5, 6, 26,
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-356-  28–31, 36; gaps (c) lower, (i) lower, (l) lower, (a),(b),(d)–(h),(k)
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-357-  retype — consistent with §7 except that Sol keeps (b) as "find a
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-358-  uniform invariant via the N = 4 regression", which is exactly what the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-359-  Γ card operationalises.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-360-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md:361:## 10. Round close and clocks
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-362-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-363-Closed 08:57Z (DEGRADED: 3/4 blind + coordinator). Launched or in flight
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-364-from this round: `b3-e-geometry-opus5` (flagship), the CUSP-A-VOID pair
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-365-(both PASSED → integration #9 bound, 00e55ac2), `phi-immersion-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-synthesis.md-366-adjudication-sol56` (CONFIRMED → A2 re-filed), `chau-delta-budget-gpt55`
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-17-and an actual Keller map are kept distinct. I make no new exit-price assertion.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-18-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-19-## 1. Executive thesis and reframing
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-20-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-21-The packet's “one A2 theorem” framing is already obsolete. `[AU]` finds a
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:22:load-bearing missing `-2qE1` in `EQ2_even`, refutes RAY-2, and un-derives the wall;
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-23-in corrected Chamber II it makes only each fixed `(e,c)` slice finite outside
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-24-named branches, while `c` remains unbounded `[AU:45-102]`. Separately, `[CG]`
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-25-claims two all-degree closures of case (A) `[CG:43-75]`. Both are high-value
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-26-but unreviewed. `H2` still means exactly that `A_F` is irreducible
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-27-`[M:26-30]`; its complement is reducible `A_F`, and no bounded-`N`
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-179-D := (s C1-q E1)T + (W/2)(s C2-q E2)
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-180-     + (kappa/2)(E1 C2-C1 E2) = 0.                 (DET-EO)
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-181-```
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-182-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-183-Here `E2` is the coefficient polynomial `E2(Z)=b Z eta^2`, not the equation
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:184:called E2 in SPEC; call the latter `EQ2_even`. `DET-EO` uses only E0/O0/O1,
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-185-which `[AU]` rederived directly `[AU:67-70]`, and does not consume flawed
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:186:`EQ2_even`. The tempting specialization to the old E1 wall and RAY-2 is
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:187:illegal: corrected `EQ2_even` contains `-2qE1`, the wall is underived,
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-188-and RAY-2 is refuted `[AU:805-816]`.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-189-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-190-The decisive new move in corrected Chamber II is a **projection to the free
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-191-`c`-line**. Substitute `[AU]`'s deviations
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-192-
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-327-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-328-## 7. Software acceleration and decisive experiment
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-329-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-330-### `CORRECTED-DEVBOX`
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-331-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:332:Retire the old wall-cell generator: its `EQ2_even` omitted `-2qE1`, so its outputs are
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-333-not free-`c` oracles `[AU:734-747]`. Build a source-first deviation
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-334-engine instead:
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-335-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-336-1. regenerate all seven coefficient identities from Even/Odd; emit the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-337-   coefficient field, ordered generators, and source-to-residual images, and
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-346-   using their respective `4e-1` and `2g-1` caps; use two engines with explicit
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-347-   Rabinowitsch variables, never a `sat()` wrapper;
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-348-5. return `UNIT_IDEAL`, `DEVBOX_LOCUS`, or typed
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-349-   `OPEN[branch/ring map]`, plus all specialization images.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-350-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:351:Smallest useful test: make old `EQ2_even` fail by exactly `-2qE1`; retain wall-imposed
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-352-`(1,3)` EMPTY only as a stale-wall canary; require corrected free-`c` decisions
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-353-for `(1,3),(1,5),(1,7)` from scratch; and preserve a synthetic
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-354-shifted-generator positive control, labeled as synthetic rather than a
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-355-full-system point. Rank loss is not a recurrence
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-356-until augmented ranks, exceptional branches, and both boundary conditions pass.
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-403-| `cusp-a-n8-gate-opus5-20260902` | **harvest/review, then stop** | Audit both all-`N` case-(A) chains and their hypotheses. If confirmed, merge the closure and archive the N8 subgroup queue; do not spend more on Alexander minors. |
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-404-| `web-sweep-20260902-grok46` | **harvest, then stop** | Record only primary-source results with a direct live client. Queue post-freeze finds for their own gate; do not extend a threat-free broad sweep. |
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-405-| box01 A2 cells | **stop stale jobs; reissue corrected only** | `(1,3)/(1,5)/(1,7)` lack corrected free-`c` decisions. Preserve wall logs as canaries, not verdicts; rerun only after corrected preflight. |
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-406-| Box03 `869` | **stop at the existing cap** | Harvest a completed result, but do not relaunch or add fallback compute. It is confirmatory inside accepted `N=4` closure and nothing at the frontier depends on it. |
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-407-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:408:## 10. Campaign-systems check
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-409-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-410-### UPGRADE — make encoding faithfulness a hard launch gate
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-411-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:412:The packet calls PREFLIGHT the tracked upgrade `[P:135-145]`; promote it from
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-413-queued hygiene to a prerequisite for every `N>=6` realization job. This is not
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-414-ceremonial: all six earlier nodal ideals used unfaithful raw/sliced coefficient
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-415-conditions, including two apparent two-engine EMPTY results
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-416-(`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:189-215`).
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-417-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-418-**Smallest useful test.** Before solver launch, the generator must (i) emit the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-419-reduced approximate root beside the ideal in the declared coefficient ring and
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-420-order; (ii) exact-diff each encoded coefficient condition; (iii) make the old
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:421:`(8,6,19)` false-positive canary fail the corrected `86A` cell; (iv) make the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:422:explicit four-node `(9,6,2)` curve pass corrected `96B`; and (v) fail closed on
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-423-any ring, map, open-factor, or generator-order mismatch. The corrected ideals
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-424-and semantic controls already exist
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-425-`[xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511]`.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-426-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-427-Use two orthogonal output fields, not the draft's lossy single enum:
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-428-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-429-```text
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:430:artifact = NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT |
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-431-           FULL_EO_POINT | CURVE | REPRESENTATION | FULL_COVER |
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-432-           POLYNOMIAL_PAIR
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:433:attainment = NECESSARY | REALIZED | ADMISSIBLE_PULLBACK |
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-434-             ACTUAL_MAP | COUNTEREXAMPLE_CERTIFIED
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-435-```
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-436-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:437:A checked transition table must reject “A2 cell -> B3 kill”, “local torsion ->
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-438-global torsion”, and `FULL_EO_POINT -> ACTUAL_MAP` without every Section 6
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md:439:arrow. Add old-`EQ2_even`/missing-`-2qE1` as a source-fidelity canary. This
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-440-small type check protects both analytic and curve workflows.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-441-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-442-## 11. Allocation, falsifiers, and close
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-443-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md-444-Allocate frontier effort in this order: (1) independent review and ledger merge
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-25-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-26-## 1. Verdict, up front
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-27-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-28-```text
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-29-(1) THE ROUND'S FIND -- an index failure, not a mathematics gap.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:30:    OPEN[DEG-AF-VS-N], named by BOTH H2-branch flagships as the single
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-31-    highest-value successor and by MPRIME as "could not find it promoted
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-32-    anywhere in the record", is answered -- in the form the delta-budget
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-33-    needs -- by Chau Thm 1/Cor 1/Cor 2, banked and machine-corrected on
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-34-    the REDUCIBLE branch the same day (COMPANION clauses C6/C7/C8 and its
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-35-    derived M <= K).  Under H2: A_F is a polynomial image of bidegree
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-98-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-99-## 3. NEW CROSS-CONNECTION: the reducible branch already holds the H2 branch's top successor
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-100-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-101-### 3.1 The collision
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-102-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:103:`HORN-FLAGSHIP` Sec 5 ranks `OPEN[DEG-AF-VS-N]` first among "what would
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-104-compose", because the `(B3)` delta-budget
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-105-`delta(p,q) + sum_i t_i + delta_infty = (n-1)(n-2)/2`, `n = deg closure(A_F)`,
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:106:"is a budget, not a bound, because `OPEN[DEG-AF-VS-N]` is open"
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-107-(`horn-flagship-opus5-20260902.md:369`). `MPRIME-ALLN-H2:682` repeats it
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-108-for `(B2)` — "a bound `n <= f(N)` converts directly into a bound on
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-109-`beta`" — and adds "I could not find it promoted anywhere in the record".
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-110-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-111-It is in the record, on the other branch, from the same day and the same
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-137-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-138-It does **not** bound `n` by the geometric degree `N`: `deg P * deg Q >= N`
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-139-runs the wrong way and the surplus hides at infinity. Honest retype:
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-140-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-141-```text
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:142: OPEN[DEG-AF-VS-N]  RETYPE -> (i) OPEN[DEG-AF-CHAU]: ANSWERED (banked,
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-143-   cross-branch) -- n = M max(d,e), bidegree (Md,Me), one place at
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-144-   infinity, M <= K.   (ii) OPEN[N-VS-MAPDEG]: is max(deg P,deg Q)
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-145-   bounded by N at the reduced representative?  Different question,
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-146-   plausibly false, and the delta-budget does NOT need it.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-147-```
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-397-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-398-```text
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-399- 1. delta-BUDGET CLOSURE.  Compute delta_infty(M,d,e) in closed form and
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-400-    substitute into (n-1)(n-2)/2.  Closes: a beta-bound for (B2) at
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-401-    5..16; a finite (p,q),k list for (B3) at N=4.  Cost: desk.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:402:    [was: OPEN[DEG-AF-VS-N], ranked #1 by HF -- same slot, now reachable]
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-403- 2. THE CEILING.  ANTI-MONOTONE (Sec 4.2).  The only route to an
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-404-    unbounded-N obstruction anyone has named.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-405- 3. A2 DEPTH-PROFILE + PARAM-U (Sec 5).  Retires the horn's analytic
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-406-    residual without a U-bound.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-407- 4. (B3) CUSP-KILL REPLACEMENT through the ends of E (HF successor 2;
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-475-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-476-## 8. Software acceleration / decisive experiment, and a systems UPGRADE
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-477-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-478-### 8.1 Acceleration: `PARAM-U` as a generic-parameter Groebner driver
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-479-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:480:The campaign's default stack (`qqideal` + `msolve 0.10.1`) is a
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-481-*ground-field* stack: it decides one cell at a time over `Q`. The A2
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-482-window on box01 is therefore linear in the number of cells and infinite
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-483-in `U`. `PARAM-U` needs one capability the stack does not currently
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-484-exercise: **Groebner over `Q(U)`** (or over `Q` with `U` adjoined and the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-485-result saturated at the pivot denominators). Two routes, both cheap:
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-501-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-502-`DEPTH-PROFILE` (Sec 5.3). Minutes of CPU on already-decided cells; three
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-503-mutually exclusive read-outs; settles whether the A2 lane's whole
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-504-leading-order programme can terminate.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-505-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:506:### 8.3 Campaign-systems check — **UPGRADE**
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-507-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-508-Not `NO_CHANGE`. The evidence is Sec 3.3: on **2026-09-02**, two
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:509:H2-branch flagships ranked `OPEN[DEG-AF-VS-N]` as their top successor,
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-510-one writing "I could not find it promoted anywhere in the record", while
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-511-the *same producer model on the same day* derived `M <= K` and
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-512-`sum deg D_i <= max(deg P, deg Q)` in the reducible-branch report. The
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-513-packet then reprinted the cap under gap (f) as a reducible-branch handle
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-514-and under gap (b)/(e) as an open question, in the same document, without
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-515-connecting them. This is not a review failure — every item was correctly
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-516-typed. It is an **index** failure: `OPEN[...]` tokens and banked lemmas
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-517-live in different namespaces and are never matched.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-518-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-519-```text
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:520:UPGRADE CARD -- OPEN/BANKED COLLISION CHECK
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-521-WHAT   Before a report is sealed, extract every OPEN[...] it RAISES,
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-522-       and grep the banked corpus (xmodel/*.md, AUDIT.md, notes.md) for
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-523-       the OPEN's key nouns plus the quantity it asks to bound.  Emit a
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:524:       COLLISIONS section listing candidate matches with file:line.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:525:       Fail-CLOSED: an empty COLLISIONS section must be explicit, never
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-526-       absent (per the SECTIONED-OUTPUT repair discipline).
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-527-SMALLEST USEFUL TEST
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-528-       Run it retroactively over the OPENs raised on 2026-09-02 only.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-529-       PASS iff it surfaces companion-curve-alln:682-687 when fed
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:530:       OPEN[DEG-AF-VS-N].  One afternoon; no new infrastructure; the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-531-       corpus is already on disk and already grep-shaped.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:532:IF IT PASSES   make it a seal-time hook, like PREFLIGHT.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-533-IF IT FAILS    the namespaces are genuinely disjoint and the right fix
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-534-       is a curated OPEN register with an explicit "quantity bounded"
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-535-       field -- more expensive, and now justified by a measurement.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-536-```
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-537-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-538-This is strictly higher-value than the currently tracked upgrade
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:539:(`PREFLIGHT` encoding faithfulness), which guards against a failure mode
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:540:that has not yet cost the campaign a flagship successor. `PREFLIGHT`
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-541-should continue; this should go in front of it.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-542-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-543-## 9. Idea cards (three)
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-544-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-545-### CARD 1 — `CHAU-DELTA` (proof; cheapest; highest ratio)
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-604-              the realisation -> SIROCCO -> ZvK -> S_N pipeline for the
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-605-              decision step.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-606-CHEAPEST      n = 4 and n = 5 only, M max(d,e) in {4,5}: solve for
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-607-DISCRIMINATOR coefficients making the image have one (p,q) cusp with
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-608-              3|pq and 2|pq, plus k double points.  A small polynomial
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:609:              system; qqideal/msolve territory, minutes.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-610-OUTCOMES      NONEMPTY -> feed the pipeline.  Kill => (B3) N=4 closes by
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-611-                          exhaustion (with Card 1's finite list).
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-612-                          Survive => FIRST object past representation
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-613-                          level; escalate immediately, all seats.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-614-              EMPTY at every admitted (M,d,e) -> (B3) N=4 EMPTY, and with
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-616-                          with no CUSP-KILL replacement needed.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-617-STOP          If Card 1 admits n > 12, stop sweeping and report the list.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-618-INFO GAIN     Highest of the three, and symmetric: both outcomes advance.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-619-```
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-620-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:621:## 10. Current lanes — continue / redesign / stop
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-622-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-623-| Lane | Call | Reason |
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-624-|---|---|---|
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-625-| `a2-ubound-opus5-20260902` | **REDESIGN** | Keep `O0/O1` (a hard dependency for anything downstream) and the negative route. Drop the search for a literal `U`-bound: `C5` removes the symmetry that would supply it. Insert `DEPTH-PROFILE` then `PARAM-U`. The `E0` unit-Wronskian sub-route deserves a sharpened statement: `q r' - p' s = kappa/2` is a *constant* Wronskian-type identity, and the classical fact that `W(f,g) = const != 0` forces `deg <= 1` on a genuine Wronskian is the model to aim at; state precisely which pair `E0` is a Wronskian of before invoking Mason-Stothers. |
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-626-| `cusp-a-n8-gate-opus5-20260902` | **CONTINUE to completion, do not reseat** | Necessary but not binding-first (gap (c)). Let the running seat finish; do not renew. |
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-637-```text
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-638-Flag/place/series.        No cv flag, place or cover series is identified
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-639-                          anywhere.  Sec 3 keeps deg A_F (a place-count-
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-640-                          free degree), the parametrisation bidegree, and
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-641-                          the geometric degree N strictly apart, and
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:642:                          Sec 3.2 splits OPEN[DEG-AF-VS-N] into two
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-643-                          questions precisely to stop the conflation.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-644-Per-ray/exit-set charge.  No exit claim; no charge_basis line emitted.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:645:Carrier/attainment.       Sec 3.3's (9,6,2) datum is REPRESENTATIVE (one
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-646-                          realised curve, one degree pair); it is NOT
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-647-                          promoted to the numerical row, and the CLAIM is
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-648-                          typed [D] UNREVIEWED with three named checks.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-649-Pole/interior.            No pole identity used.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:650:Floor/attainment.         (CHAU-M) is an inequality and is used only as
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-651-                          an inequality.  The delta-budget is an IDENTITY
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-652-                          and is called a budget, not a bound, until
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-653-                          delta_infty is computed (Card 1's stop
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-654-                          condition covers the case where it is not a
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-655-                          closed form).  Sec 4.2's Riemann-Hurwitz side
--
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-711-                gap (l) answered NO.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-712-NOT CLAIMED     any closure of (B2), (B3), (B1) or case (A) at any N;
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-713-                any bound of deg A_F in the geometric degree; any
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-714-                promotion of CLAIM [D]; any statement about the existence
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-715-                of F; the correctness of Chau's Thm 1 beyond its typing.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:716:SYSTEMS         UPGRADE (Sec 8.3): OPEN/BANKED collision check.  Smallest
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md:717:                useful test named; ranked ahead of PREFLIGHT.
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-718-CONTRACT        All contract items delivered.  DEVIATION: 15-25KB target
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-719-                overrun (~36KB).  Cause: the contract enumerates 46 rows,
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-720-                12 gaps, two rankings, two attacks, an experiment, a
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-721-                systems card, three cards and five lanes; Secs 1-3 were
/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md-722-                compressed twice rather than dropping required items.
--
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-3-**Status:** SEALED — encoding audit closed; five nodal types OPEN and `(9,6,2)` REALIZED.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-4-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-5-## 0. Scope, frozen inputs, and hash gate
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-6-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-7-This is a fail-closed semantic audit of the six coefficient systems in
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:8:`msolve-prep-realization-grok46-20260831.md`.  It does not rerun a CAS and it
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-9-does not edit a ledger.  Before reading the inputs I recomputed:
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-10-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-11-```text
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:12:475ca486f3e2eed4a71e842dbe4f16ad9db43efd46c19a9318c22490e0d1010e  type86A-sliced-witness-pt0.m2.txt
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:13:c54d53f0a55586eb0fdd84d3aa311c6d64125a2cded8bec77aad1c3a8f40ec7f  msolve-prep-realization-grok46-20260831.md
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-14-3985fbedea72315c0ff3ffd4185de465ba16922bca872623fe2f02fe34cffc08  nodal-realization-86-96-grok46-20260831.md
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-15-763eec05eb6bc00f1c13e6ff25c2b275ef3c97f20a22e6ba34621244981c56c9  block-descent-a1-alldegree-h2-coordinator-integration-fable5-20260901.md
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-16-```
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-17-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-18-All four match the charge.  Line references below are to those frozen bytes
--
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-119-nonzero `hat_k_j` contributes exponent `27-j`.  Vanishing the earlier
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-120-non-`a`-divisible terms pins the local `beta_1`.  Exact reduced-root degree
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-121-`c` implies that local condition, via `beta_1=32-c` or `27-c`, but the
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-122-converse can fail even under properness: a higher global gap with unchanged
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-123-gcd is locally removable.  The relevant same-gcd obstructions here are
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:124:degree 10 for 86B/C/D, additionally degree 4 for 86D, and degree 3 for 96B.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-125-Thus an exact three-entry global `Delta` must be imposed by reducing the
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-126-polynomial second approximate root to degree `c`, not by the local exponent
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-127-alone.  For reference, the **local** normalized-series dictionary is
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-128-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-129-```text
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:130:86A: hat_h_{21,19,17,15,13}=0,             hat_h_11 != 0;
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-131-86B: preceding plus hat_h_11=0,             hat_h_9  != 0;
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-132-86C: preceding plus hat_h_9=0,              hat_h_7  != 0;
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-133-86D: preceding plus hat_h_{7,5}=0,          hat_h_3  != 0;
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-134-96A: hat_k_{16,14,13,11,10,8,7,5}=0,       hat_k_4  != 0;
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:135:96B: preceding plus hat_k_4=0,              hat_k_2  != 0.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-136-```
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-137-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-138-These hats are not the raw `h_j,k_j`.  Write `bar_h_j,bar_k_j` for the
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-139-coefficients after the triangular global semigroup reduction specified in
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-140-7.1.  The exact global dictionary is:
--
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-147-| `(8,6,3)` | 29 | preceding set plus `j={7,5,4}` | `bar_h_3 != 0` |
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-148-| `(9,6,4)` | 23 | `bar_k_j=0`, `j={16,14,13,11,10,8,7,5}` | `bar_k_4 != 0` |
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-149-| `(9,6,2)` | 25 | preceding set plus `j={4,3}` | `bar_k_2 != 0` |
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-150-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-151-The chart identities at levels 23 and 17 remain automatic.  Degrees 10
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:152:and 4 in the `(8,6)` table and degree 3 in the 96B row are genuine global
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-153-gaps, even though the corresponding local exponents 22, 28 and 24 are
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-154-divisible by `a` and analytically removable.  Conversely, the raw `(9,6)`
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-155-jobs unnecessarily zero the semigroup levels 15, 12, 9 and 6 instead of
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-156-solving for their reduction coefficients.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-157-
--
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-374-that those eight lines alone replay the reduced degree-30 ordered scheme.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-375-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-376-## 6. Per-ideal faithfulness verdicts and exact corrections
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-377-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-378-All six ideals are **UNFAITHFUL** to their advertised exhaustive meaning.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:379:For A/B the error admits false positives; for 96A/96B it cuts to a strict
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-380-slice and admits false negatives; C/D inherit the A/B construction before
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-381-their computations finish.  The exact replacements are specified in 7.1.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-382-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-383-| job | typed audit finding |
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-384-|---|---|
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-385-| `type86_A` | **UNFAITHFUL, witnessed false positive.** The charged point passes every raw generator/open but has `Delta=(8,6,19)`. |
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-386-| `type86_B` | **UNFAITHFUL.** Its raw equations make the same unreduced substitution; the charged sliced points' `delta_inf=6` are consistent with the same actual `c=19`, not target `c=9`. |
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:387:| `type86_C` | **UNFAITHFUL before computation.** The exact characteristic-base counterpoint in Section 4 passes old C but has a nonzero corrected degree-17 residual; replace the raw equations and add degree 10. |
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-388-| `type86_D` | **UNFAITHFUL before computation.** The Section 4 characteristic-base point passes old D but has corrected `g_15!=0`; the raw list also omits gap residuals at 10 and 4. |
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-389-| `type96_A` | **UNFAITHFUL strict slice.** EMPTY kills only the slice `a15=a12=a9=a6=0`, not the full type. |
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-390-| `type96_B` | **UNFAITHFUL strict slice.** The same; in fact an exact four-node realization exists below. |
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-391-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-392-Two supplementary formulas already on disk give useful exact negative
--
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-505-degree-2/3 covers, although the old cover colon is harmless if its ring and
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-506-map are asserted.  Required semantic controls for the rerun are:
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-507-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-508-1. the charged witness must fail corrected A and be classified at `c=19`;
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-509-2. the displayed `(9,6,4)` point must satisfy corrected 96A;
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:510:3. the displayed four-node `(9,6,2)` point must satisfy corrected 96B;
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-511-4. the existing `(6,4,3)` nodal and monomial-cover controls remain.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-512-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-513-### 7.2 Binding retyping
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-514-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-515-| advertised type | old suite label | sound label now | next action |
--
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-540-  named auxiliary coefficients.  Matching variable names are not used as a
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-541-  map proof.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-542-- **Open and `sat()` discipline:** opens are Rabinowitsch equations on the
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-543-  corrected leading coefficient.  Colon/resultant operations are not allowed
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-544-  to change the semantic meaning of the base ideal.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:545:- **Floor/attainment:** corrected NONEMPTY for `(9,6,4)` is not promoted to a
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-546-  nodal witness.  `(9,6,2)` is promoted only because four explicit transverse
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-547-  pairs exhaust the independently computed affine delta.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-548-- **Evidence custody:** the charged A payload contains no 15-node transcript
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-549-  and has `Nexpected=3`; its nodality is consumed only as the charge states it.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-550-  The infinity calculation is independently exact.

exec
/bin/zsh -lc "sed -n '520,620p' /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md && sed -n '500,590p' /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md && sed -n '395,515p' xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md" in /Users/dc/code/math/jc2
 succeeded in 0ms:
### 8.2 Decisive experiment

`DEPTH-PROFILE` (Sec 5.3). Minutes of CPU on already-decided cells; three
mutually exclusive read-outs; settles whether the A2 lane's whole
leading-order programme can terminate.

### 8.3 Campaign-systems check — **UPGRADE**

Not `NO_CHANGE`. The evidence is Sec 3.3: on **2026-09-02**, two
H2-branch flagships ranked `OPEN[DEG-AF-VS-N]` as their top successor,
one writing "I could not find it promoted anywhere in the record", while
the *same producer model on the same day* derived `M <= K` and
`sum deg D_i <= max(deg P, deg Q)` in the reducible-branch report. The
packet then reprinted the cap under gap (f) as a reducible-branch handle
and under gap (b)/(e) as an open question, in the same document, without
connecting them. This is not a review failure — every item was correctly
typed. It is an **index** failure: `OPEN[...]` tokens and banked lemmas
live in different namespaces and are never matched.

```text
UPGRADE CARD -- OPEN/BANKED COLLISION CHECK
WHAT   Before a report is sealed, extract every OPEN[...] it RAISES,
       and grep the banked corpus (xmodel/*.md, AUDIT.md, notes.md) for
       the OPEN's key nouns plus the quantity it asks to bound.  Emit a
       COLLISIONS section listing candidate matches with file:line.
       Fail-CLOSED: an empty COLLISIONS section must be explicit, never
       absent (per the SECTIONED-OUTPUT repair discipline).
SMALLEST USEFUL TEST
       Run it retroactively over the OPENs raised on 2026-09-02 only.
       PASS iff it surfaces companion-curve-alln:682-687 when fed
       OPEN[DEG-AF-VS-N].  One afternoon; no new infrastructure; the
       corpus is already on disk and already grep-shaped.
IF IT PASSES   make it a seal-time hook, like PREFLIGHT.
IF IT FAILS    the namespaces are genuinely disjoint and the right fix
       is a curated OPEN register with an explicit "quantity bounded"
       field -- more expensive, and now justified by a measurement.
```

This is strictly higher-value than the currently tracked upgrade
(`PREFLIGHT` encoding faithfulness), which guards against a failure mode
that has not yet cost the campaign a flagship successor. `PREFLIGHT`
should continue; this should go in front of it.

## 9. Idea cards (three)

### CARD 1 — `CHAU-DELTA` (proof; cheapest; highest ratio)

```text
GOAL          Closed-form delta_infty(M,d,e); substitute into the
              delta-budget; derive a beta-bound for (B2) 5..16 and a
              finite (p,q),k list for (B3) at N=4.
DEPENDENCIES  Chau Thm 1 / Cor 1 / Cor 2 [refs on disk]; COMPANION C6-C8
              incl. its leading-form correction; MI Lemma A (one place at
              infinity); HF Prop 3.2 and THEOREM B3-N4.  NO new theory.
CHEAPEST      Write delta_infty for the one-place branch of a bidegree-
DISCRIMINATOR (Md,Me) parametrisation at M=1 first (semigroup <d,e>,
              delta = (d-1)(e-1)/2) and check the budget on the realised
              (9,6,2) curve: n=9, delta_infty should reconcile with the
              curve's known singularities.  One page.
OUTCOMES      Budget < 2 at some N in 5..10  -> (B2) window KILLED there.
              Budget finite but >= 2         -> explicit (M,d,e) residual
                                               list; still a strict gain.
              Reconciliation FAILS on (9,6,2)-> one of C6/C8/Lemma A is
                                               mis-typed; that is itself
                                               a high-value find.
STOP          If delta_infty needs the full Puiseux expansion of the
              parametrisation rather than (M,d,e) alone, stop and type
              OPEN[DELTA-INFTY-NOT-NUMERICAL]; the budget is then not a
              closed form and Card 1 is dead.
INFO GAIN     Very high per unit cost: 12 degrees of (B2), plus the
              enumerability of (B3) at N=4.
```

### CARD 2 — `PARAM-U` (proof; changes the A2 lane's complexity class)

```text
GOAL          Decide the A2 E1-wall for all U at fixed e, with no U-bound.
DEPENDENCIES  RAY-KILL Secs 2-4 (the wall, RAY-1/DEP/2, the cell sizes);
              CELL-32 Sec 1 (Euler-eigenvalue leaders, LINEAR in the
              degree indices -- this is the load-bearing fact);
              OPEN[A2-O0-O1] must be resolved first or Lad_k is built
              from an incomplete equation set.
CHEAPEST      DEPTH-PROFILE (Sec 5.3) on the four decided cells.
DISCRIMINATOR
OUTCOMES      k* bounded, uniform in U -> run PARAM-U; horn's analytic
                                          residual retired.
              k* grows with U           -> PARAM-U dead; the growth rate
                                          is the first quantitative fact
                                          about the residual.
              no finite k*              -> the leading-order programme
                                          cannot terminate; STOP the

For `(9,6,4)`, put

```text
p=t^9+3t^7+21/4*t^5+35/8*t^3+63/32*t,
q0=t^6+2t^4+5/2*t^2,                  y=q0+3/4.
```

The identity recorded and coefficient-checkable at
`xmodel/hf-twin-964-grok46-20260831.md:109-133` is

```text
y^3-p^2=27/1024*(8t^4+13t^2+16).
```

Hence, in the **shared constant-zero chart**,

```text
p^2-q0^3-(9/4)q0^2-(27/16)q0-27/64
  =-27/1024*(8t^4+13t^2+16).
```

This is a proper corrected-locus point of exact degree 4, while raw
`p^2-q0^3` has degree 12.  It also passes the affine-immersion open.  With
`z=t^2`,

```text
q0'=t*(6z^2+8z+5),
p' mod (6z^2+8z+5)=-27z/8-117/32.
```

The latter can vanish only at `z=-13/12`, where the quadratic has value
`27/8`, and `p'(0)=63/32`; hence `p',q0'` have no common root.  Exact
degree 4 together with `gcd(9,6,4)=1` also rules out a common right
component.  Thus the full corrected open is already NONEMPTY.
The cited desk analysis proves four nodes and leaves affine delta two as
either two more nodes or one `A_3` (`ibid.:135-157`), so it does not by
itself settle the six-node realization question.

For `(9,6,2)`, put

```text
q=t^6+8t^2,                 p=t^9+12t^5+24t,
p^2-q^3-64q=64t^2.
```

This lies in the shared chart and is proper: the identity recovers `t^2`,
then `t=p/(t^8+12t^4+24)`.  Its infinity type is therefore
`Delta=(9,6,2)`, `beta_1=25`, `delta_inf=24`, so `delta_aff=28-24=4`.
Let `w=t^4`.  Each of the two nonzero simple roots of
`w^2+12w+24` yields two pairs `{t,-t}`, one for each sign of `t^2`.
Within a fixed `w` those two pair-images have opposite nonzero
`q=t^2(w+8)`, while
`q^2=w*(w+8)^2=-8*(w+12)` distinguishes the two `w`-families.  Thus all
four images are distinct.  At them

```text
p'=8w(w+6) != 0,             q'=2t(3w+8) != 0,
```

and the velocities at `t,-t` have nonzero determinant.  They are four
ordinary nodes.  Since they exhaust `delta_aff=4`, there are no omitted
singularities.  This independently recovers the pair calculation in
`xmodel/block-descent-a1-genus-four-cable-b1-hostile-review-grok46-20260831.md:245-267`.
Thus `type96_B` is not merely NONEMPTY after correction: its advertised
nodal type is **REALIZED**.

## 7. Re-typed realization suite and rerun specification

### 7.1 Correct characteristic incidence ideals

Do not patch the old files by shifting an `h` or `k` subscript.  Introduce
the coefficients of the reduced second approximate root explicitly.

For `(8,6)` use

```text
G86 = p^3-q^4
    + a22*p^2*q + a20*p*q^2 + a18*q^3
    + a16*p^2   + a14*p*q   + a12*q^2
    + a8*p + a6*q.
```

The subscript is the leading pole degree.  Retain only terms of weight
strictly greater than the target `c`, let `g_j=[t^j]G86`, and use:

| job | auxiliary terms retained | closed equations | Rabinowitsch open |
|---|---|---|---|
| A, `c=11` | `a22,...,a12` | `g_22,...,g_12` | `u*g_11-1` |
| B, `c=9` | `a22,...,a12` | `g_22,...,g_10` | `u*g_9-1` |
| C, `c=7` | preceding plus `a8` | `g_22,...,g_8` | `u*g_7-1` |
| D, `c=3` | preceding plus `a6` | `g_22,...,g_4` | `u*g_3-1` |

For `(9,6)` use

```text
G96 = p^2-q^3 + a15*p*q + a12*q^2 + a9*p + a6*q.
```

For A (`c=4`) impose `[t^j]G96=0` for `5<=j<=16` and invert
`[t^4]G96`; for B (`c=2`) impose the coefficients for `3<=j<=16`
and invert `[t^2]G96`.  Constants may be omitted because every target
degree is positive.  These incidence systems are triangular in the `a_w`;
they may be eliminated to obtain the `bar_h/bar_k` equations in Section 3.
Their independent corrected equation counts in the eleven chart variables
are `5,7,8,11,8,10`, respectively.

On each corrected locus, retain the affine tests in their proper roles:
invert `Res_t(p',q')`; then run the reduced unordered double-point check
with `N=11,10,9,7,6,4`.  The corrected `c`-open already excludes common
degree-2/3 covers, although the old cover colon is harmless if its ring and
map are asserted.  Required semantic controls for the rerun are:

1. the charged witness must fail corrected A and be classified at `c=19`;
2. the displayed `(9,6,4)` point must satisfy corrected 96A;
3. the displayed four-node `(9,6,2)` point must satisfy corrected 96B;
4. the existing `(6,4,3)` nodal and monomial-cover controls remain.

### 7.2 Binding retyping

| advertised type | old suite label | sound label now | next action |

exec
/bin/zsh -lc "rg -n '"'^## 10|PREFLIGHT|artifact =|attainment =|A2 cell|B3 kill|local torsion|FULL_EO_POINT|reduced approximate root|source-fidelity'"' /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md" in /Users/dc/code/math/jc2
 succeeded in 0ms:
299:  -> FULL_EO_POINT in the declared quotient R
405:| box01 A2 cells | **stop stale jobs; reissue corrected only** | `(1,3)/(1,5)/(1,7)` lack corrected free-`c` decisions. Preserve wall logs as canaries, not verdicts; rerun only after corrected preflight. |
408:## 10. Campaign-systems check
412:The packet calls PREFLIGHT the tracked upgrade `[P:135-145]`; promote it from
419:reduced approximate root beside the ideal in the declared coefficient ring and
430:artifact = NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT |
431:           FULL_EO_POINT | CURVE | REPRESENTATION | FULL_COVER |
433:attainment = NECESSARY | REALIZED | ADMISSIBLE_PULLBACK |
437:A checked transition table must reject “A2 cell -> B3 kill”, “local torsion ->
438:global torsion”, and `FULL_EO_POINT -> ACTUAL_MAP` without every Section 6
439:arrow. Add old-`EQ2_even`/missing-`-2qE1` as a source-fidelity canary. This

exec
/bin/zsh -lc "sed -n '280,330p' /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md && sed -n '408,445p' /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-sol56.md" in /Users/dc/code/math/jc2
 succeeded in 0ms:
locus. Adjoin `s` and, pointwise in `C[Z]`, require zero remainders for

```text
eta^2 | (s^2+X),       eta | (3a s^2+Y),
s | (q partial_Z(r)-kappa/2).
```

Set `r=(s^2+X)/(4b eta^2)`, `q=(3a s^2+Y)/(4b^2 eta)`; the last quotient is
`partial_Z(p)`, which must integrate to the declared `p` in characteristic
zero. Check all three in the declared coefficient ring, branching on vanished
leaders and zero. Only then label `SUBSYSTEM_POINT`. Search exceptional `c`,
full `OPEN[A2-K2E]`, the three low-`k` strata, and Chambers I/III separately. This outranks
an undifferentiated `S_6` search because a landed full solution has an explicit
route toward a polynomial pair, whereas a permutation tuple does not.

The state ladder is mandatory:

```text
SUBSYSTEM_POINT
  -> FULL_EO_POINT in the declared quotient R
  -> ADMISSIBLE_PULLBACK (all opens, boundary/general-fibre data,
                          and [K:C(f,g)]=4 checked)
  -> POLYNOMIAL_KELLER_PAIR under the explicit source chart
  -> COUNTEREXAMPLE only after noninvertibility/geometric degree is certified.
```

The explicit chart
`A=x^2, U=x+x^3y, Z=2y+x^2y^2` sends any landed full quotient solution to a
polynomial pair with constant Jacobian. Conditional on the separately checked
rank-four condition, its geometric degree is `2*4=8`; thus a terminating
admissible solution would be counterexample-level rather than a representation
(`xmodel/theta-reopen-explicit-pullback-sol56-20260831.md:412-449`). None of
those later arrows may be inferred from a residual cell.

Outcomes are deliberately symmetric. A unit ideal with positive and negative
controls is a proof lead for that declared branch. Rank deficiency supplies
only a candidate kernel: require augmented-rank compatibility at every level,
all exceptional branches, and both terminal/bottom boundary conditions before
calling it a recurrence. A terminating orbit is checked directly in `R`, then
through the source chart. A formal or nonterminating orbit is only formal;
vanished leaders and missing ring maps return typed `OPEN`.

In parallel but second priority, build the `N=6` census around actual curves.
The `N=4` B3 Keller realization is impossible; its representation/curve data
are regression controls, not a counterexample target. Their value is to force
any proposed group-to-geometry bridge to find the missing obstruction before
that bridge is trusted at `N=6`.

## 7. Software acceleration and decisive experiment

### `CORRECTED-DEVBOX`
## 10. Campaign-systems check

### UPGRADE — make encoding faithfulness a hard launch gate

The packet calls PREFLIGHT the tracked upgrade `[P:135-145]`; promote it from
queued hygiene to a prerequisite for every `N>=6` realization job. This is not
ceremonial: all six earlier nodal ideals used unfaithful raw/sliced coefficient
conditions, including two apparent two-engine EMPTY results
(`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:189-215`).

**Smallest useful test.** Before solver launch, the generator must (i) emit the
reduced approximate root beside the ideal in the declared coefficient ring and
order; (ii) exact-diff each encoded coefficient condition; (iii) make the old
`(8,6,19)` false-positive canary fail the corrected `86A` cell; (iv) make the
explicit four-node `(9,6,2)` curve pass corrected `96B`; and (v) fail closed on
any ring, map, open-factor, or generator-order mismatch. The corrected ideals
and semantic controls already exist
`[xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511]`.

Use two orthogonal output fields, not the draft's lossy single enum:

```text
artifact = NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT |
           FULL_EO_POINT | CURVE | REPRESENTATION | FULL_COVER |
           POLYNOMIAL_PAIR
attainment = NECESSARY | REALIZED | ADMISSIBLE_PULLBACK |
             ACTUAL_MAP | COUNTEREXAMPLE_CERTIFIED
```

A checked transition table must reject “A2 cell -> B3 kill”, “local torsion ->
global torsion”, and `FULL_EO_POINT -> ACTUAL_MAP` without every Section 6
arrow. Add old-`EQ2_even`/missing-`-2qE1` as a source-fidelity canary. This
small type check protects both analytic and curve workflows.

## 11. Allocation, falsifiers, and close

Allocate frontier effort in this order: (1) independent review and ledger merge
of the A2 P0 and all-`N` case-(A) closure; (2) corrected DEV-PROJECTION and its

exec
/bin/zsh -lc "find ops box -maxdepth 2 -type f -not -path '*/__pycache__/*' | sort | sed -n '1,240p'" in /Users/dc/code/math/jc2
 succeeded in 0ms:
box/a2_cells_jobs.py
box/a2_cells_run.py
box/b3e-drivers-20260902/README.md
box/b3e-drivers-20260902/anti.py
box/b3e-drivers-20260902/embed.py
box/b3e-drivers-20260902/embed2.py
box/b3e-drivers-20260902/embed3.py
box/b3e-drivers-20260902/embed4.py
box/b3e-drivers-20260902/embed5.py
box/b3e-drivers-20260902/embed6.py
box/b3e-drivers-20260902/seal.txt
box/b3e-drivers-20260902/struct.py
box/bmfact962_native_enum.log
box/bmfact962_result.json
box/bmfact964_enum.py
box/bmfact964_enum_run.log
box/bmfact964_result.json
box/bmfact_962.sage
box/bmfact_964.sage
box/bmfact_enum.py
box/bmfact_selfcheck.py
box/corrected_8611.m2
box/corrected_8611.ms
box/corrected_863.m2
box/corrected_863.ms
box/corrected_867.m2
box/corrected_867.ms
box/corrected_869.m2
box/corrected_869.ms
box/corrected_964.m2
box/corrected_964.ms
box/cover_h1.py
box/covergeo.py
box/cuspa-n8-drivers-20260902/README.md
box/cuspa-n8-drivers-20260902/alex.py
box/cuspa-n8-drivers-20260902/alex2.py
box/cuspa-n8-drivers-20260902/alex3.py
box/cuspa-n8-drivers-20260902/cover_h1.py
box/cuspa-n8-drivers-20260902/ctrl.py
box/cuspa-n8-drivers-20260902/fam.py
box/cuspa-n8-drivers-20260902/gd.py
box/cuspa-n8-drivers-20260902/gd1.py
box/cuspa-n8-drivers-20260902/ledger.py
box/cuspa-n8-drivers-20260902/more.py
box/cuspa-n8-drivers-20260902/rs_fox.py
box/cuspa-n8-drivers-20260902/snf.py
box/cuspa-n8-drivers-20260902/surv.pkl
box/cuspa-n8-drivers-20260902/survivors.py
box/cuspa-n8-drivers-20260902/sweep.py
box/cuspa-n8-drivers-20260902/sweep8.py
box/open_triage.py
box/qq_oracle_jobs.py
box/qq_oracle_run.py
box/qq_oracle_window_run.log
box/raykill-cells-20260902/README.md
box/raykill-cells-20260902/cell.py
box/raykill-cells-20260902/cellp.py
box/raykill-cells-20260902/cells.log
box/raykill-cells-20260902/cells2.log
box/raykill-cells-20260902/cellsP.log
box/raykill-cells-20260902/controls.log
box/raykill-cells-20260902/queue.sh
box/raykill-cells-20260902/queue2.sh
box/raykill-cells-20260902/t10_order2.py
box/raykill-cells-20260902/t11_cell13.py
box/raykill-cells-20260902/t12_exact13.py
box/raykill-cells-20260902/t13_cells.py
box/raykill-cells-20260902/t13b.py
box/raykill-cells-20260902/t14_scaling.py
box/raykill-cells-20260902/t15.py
box/raykill-cells-20260902/t16.py
box/raykill-cells-20260902/t17_e0.py
box/raykill-cells-20260902/t18_planted.py
box/raykill-cells-20260902/t19_ident.py
box/raykill-cells-20260902/t1_toprows.py
box/raykill-cells-20260902/t20_weights.py
box/raykill-cells-20260902/t21_rho.py
box/raykill-cells-20260902/t2_rows12.py
box/raykill-cells-20260902/t2b.py
box/raykill-cells-20260902/t3.py
box/raykill-cells-20260902/t4_ray.py
box/raykill-cells-20260902/t5_next.py
box/raykill-cells-20260902/t6_consist.py
box/raykill-cells-20260902/t7_cokernel.py
box/raykill-cells-20260902/t8_verify.py
box/raykill-cells-20260902/t9_norm.py
box/raykill-cells-20260902/wall.py
box/raykill-cells-20260902/wall2.py
ops/FLEET.md
ops/adapters/claude.sh
ops/adapters/codex.sh
ops/adapters/codex55.sh
ops/adapters/grok.sh
ops/adapters/opus.sh
ops/ade_cartan_pair_enumerate.py
ops/artifact_finalize.py
ops/aws_as_d7_optional_run.sh
ops/aws_as_next_top_carry_run.sh
ops/aws_d3_one_support_clean_hb_run.sh
ops/aws_doubleb_corrected_r6c_launch.sh
ops/aws_doubleb_lift_run.sh
ops/aws_doubleb_lift_std_run.sh
ops/aws_doubleb_run.sh
ops/aws_doubleb_user_data.sh
ops/aws_exact_lane.sh
ops/aws_lean_verify.sh
ops/aws_q8_component_grouping_run.sh
ops/aws_q8_global_quotient_evidence_launch.sh
ops/aws_q8_invariant_support_run.sh
ops/aws_td6_v4_lane.sh
ops/aws_td6_v4_launch.sh
ops/aws_td6_v5_exceptional_launch.sh
ops/aws_td6_v5_h_sparse_launch.sh
ops/bd_a2_d3_sectioned_one_support_universal_ramification_replay.py
ops/bd_a2_d3_sectioned_two_support_control_replay.py
ops/bd_a2_d3_sectioned_two_support_universal_dichotomy_replay.py
ops/block_descent_a1_cubic_acyclic_branch_monodromy_replay.py
ops/block_descent_a1_cubic_affine_survivor_replay.py
ops/block_descent_a1_cubic_euler_replay.py
ops/block_descent_a1_cubic_reducible_branch_replay.py
ops/block_descent_a1_euler_ledger_replay.py
ops/block_descent_a1_genus_four_cable_b1_replay.py
ops/block_descent_a1_genus_ladder_c34_c40_s4_replay.py
ops/block_descent_a1_genus_ladder_next_s4_replay.py
ops/block_descent_a1_genus_ladder_s4_replay.py
ops/block_descent_a1_genus_three_cable_b1_replay.py
ops/block_descent_a1_genus_three_m0_ramification_replay.py
ops/block_descent_a1_genus_three_spectator_fox_kernel_replay.py
ops/block_descent_a1_genus_three_t34_m0_projection_replay.py
ops/block_descent_a1_genus_three_t34_m1_ramification_replay.py
ops/block_descent_a1_quartic_branch_topology_replay.py
ops/block_descent_a1_quartic_cycle0_all_minimal_packets_replay.py
ops/block_descent_a1_quartic_cycle0_braid_escape_replay.py
ops/block_descent_a1_quartic_cycle0_polynomial_link_replay.py
ops/block_descent_a1_quartic_cycle1_pseudoplane_replay.py
ops/block_descent_a1_quartic_discriminant_double_plane_replay.py
ops/block_descent_a1_quartic_minimal_cycle_replay.py
ops/block_descent_a1_rank4_r1_splice_coloring_replay.py
ops/block_descent_a1_rank4_reducible_tree_ledger_replay.py
ops/block_descent_a1_total_delta_two_iterated_knot_s4_replay.py
ops/block_descent_all_degree_acyclic_companion_replay.py
ops/block_descent_one_node_all_degree_meridional_rank_replay.py
ops/d3_halphen_cfs_state_machine_replay.py
ops/d3_halphen_global_control_replay.py
ops/d3_halphen_two_support_weighted_boundary_replay.py
ops/d3_halphen_weighted_boundary_replay.py
ops/d3_one_support_clean_hb_generate.py
ops/d3_triple_line_first_jet_invariant_replay.py
ops/d3_triple_line_first_jet_invariant_replay_v2.py
ops/d9_fibre_signature_enumerate.py
ops/f5_singular_polar_replay.py
ops/f5_unbalanced_polar_replay.py
ops/f5_unbalanced_polar_replay_v2.py
ops/frontier_gate.py
ops/lane.sh
ops/lane_detach.py
ops/lane_eta.py
ops/lane_process_pid.awk
ops/launch_triple02_proper_open_r5_r6d_20260828.sh
ops/modp_contam_inventory_20260810.txt
ops/msolve-issue-draft.md
ops/q6_f5_affine_ade_threat_replay.py
ops/q6_f5_affine_ade_threat_replay_v2.py
ops/q8_f5_global_lattice_replay.py
ops/reduce_msp.py
ops/replay_gate.py
ops/run_capped.py
ops/run_td6_coordinate12_h19.sh
ops/seal.py
ops/sectioned_output.py
ops/sg_autoupdate.sh
ops/sol_lane.sh
ops/status.sh
ops/stuck7_post.sh
ops/stuck7_verdicts.sh
ops/telemetry_samples/core23_ext.snap20260818.v2log
ops/telemetry_samples/directionb_core2_fiber_p105337.v2log
ops/telemetry_samples/directionb_core2_p105337.v2log
ops/test_artifact_finalize.py
ops/test_frontier_gate.py
ops/test_lane_detach.py
ops/test_lane_fallacy.py
ops/test_lane_process_pid.py
ops/test_quartic_invariant_mu2_d8_generator.py
ops/test_replay_gate.py
ops/test_run_capped.py
ops/test_seal.py
ops/test_sectioned_output.py
ops/tokens.py
ops/validate_charge_basis.py

exec
/bin/zsh -lc "sed -n '1,240p' ops/test_sectioned_output.py && sed -n '1,260p' ops/frontier_gate.py && sed -n '1,220p' ops/test_frontier_gate.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
#!/usr/bin/env python3
"""Regression gates for SECTIONED-INDEPENDENT-CALLS/v1."""

from __future__ import annotations

import hashlib
import json
from pathlib import Path
import shutil
import subprocess
import sys
import tempfile
import unittest


TOOL = Path(__file__).with_name("sectioned_output.py")


def sha(data: bytes) -> str:
    return hashlib.sha256(data).hexdigest()


class SectionedOutputTest(unittest.TestCase):
    def setUp(self) -> None:
        self.root = Path(tempfile.mkdtemp(prefix="sectioned-output-test-"))
        self.addCleanup(shutil.rmtree, self.root, ignore_errors=True)
        (self.root / "parts").mkdir()
        self.manifest = {
            "schema": 1,
            "tag": "synthetic-three-section",
            "basis": "0" * 40,
            "output": "assembled.md",
            "sections": [
                {
                    "id": section_id,
                    "path": f"parts/{section_id}.md",
                    "prompt_sha256_declared": sha(f"prompt-{section_id}".encode()),
                    "max_bytes": 256,
                }
                for section_id in ("one", "two", "three")
            ],
        }
        (self.root / "manifest.json").write_text(
            json.dumps(self.manifest, sort_keys=True, separators=(",", ":")) + "\n",
            encoding="utf-8",
        )

    def write_section(self, section_id: str, body: str | None = None) -> bytes:
        if body is None:
            body = f"## {section_id}\n\nBounded result for {section_id}.\n"
        data = (body + f"\n<!-- SECTION-END {section_id} -->\n").encode()
        (self.root / "parts" / f"{section_id}.md").write_bytes(data)
        return data

    def run_tool(
        self, command: str, *, optimized: bool = False
    ) -> subprocess.CompletedProcess[str]:
        args = [sys.executable]
        if optimized:
            args.append("-O")
        args.extend(
            [
                str(TOOL),
                command,
                "manifest.json",
                "--root",
                str(self.root),
            ]
        )
        return subprocess.run(
            args,
            text=True,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            timeout=10,
            check=False,
        )

    def test_partial_receipt_then_complete_assembly(self) -> None:
        first = self.write_section("one")
        second = self.write_section("two")
        partial = self.run_tool("status")
        self.assertEqual(partial.returncode, 4, partial.stderr)
        receipt = json.loads(partial.stdout)
        self.assertEqual(receipt["status"], "TRUNCATED_AFTER_SECTION_two")
        self.assertEqual(receipt["next_section"], "three")
        self.assertEqual([row["sha256"] for row in receipt["completed"]], [sha(first), sha(second)])

        third = self.write_section("three")
        complete = self.run_tool("status")
        self.assertEqual(complete.returncode, 0, complete.stderr)
        self.assertEqual(json.loads(complete.stdout)["status"], "COMPLETE")

        assembled = self.run_tool("assemble")
        self.assertEqual(assembled.returncode, 0, assembled.stderr)
        record = json.loads(assembled.stdout)
        self.assertEqual(record["status"], "ASSEMBLED")
        output = (self.root / "assembled.md").read_bytes()
        self.assertEqual(record["output_sha256"], sha(output))
        self.assertTrue(output.endswith(b"<!-- BODY-END -->\n"))
        self.assertIn(first[: -len(b"<!-- SECTION-END one -->\n")], output)
        self.assertIn(second[: -len(b"<!-- SECTION-END two -->\n")], output)
        self.assertIn(third[: -len(b"<!-- SECTION-END three -->\n")], output)
        self.assertEqual((self.root / "parts" / "one.md").read_bytes(), first)
        self.assertEqual((self.root / "parts" / "two.md").read_bytes(), second)
        self.assertEqual((self.root / "parts" / "three.md").read_bytes(), third)

    def test_ordinary_and_optimized_status_are_identical(self) -> None:
        self.write_section("one")
        self.write_section("two")
        ordinary = self.run_tool("status")
        optimized = self.run_tool("status", optimized=True)
        self.assertEqual((ordinary.returncode, ordinary.stdout, ordinary.stderr),
                         (optimized.returncode, optimized.stdout, optimized.stderr))

    def test_noncontiguous_sections_fail_closed(self) -> None:
        self.write_section("one")
        self.write_section("three")
        result = self.run_tool("status")
        self.assertEqual(result.returncode, 2)
        self.assertIn("noncontiguous sections", result.stderr)

    def test_marker_budget_and_body_end_fail_independently(self) -> None:
        cases = (
            ("wrong-marker", "body\n<!-- SECTION-END wrong -->\n", "exact standalone marker"),
            (
                "premature-body-end",
                "body\n<!-- BODY-END -->\n<!-- SECTION-END one -->\n",
                "reserved marker vocabulary",
            ),
            ("over-budget", "x" * 300 + "\n<!-- SECTION-END one -->\n", "exceeds max_bytes"),
        )
        for name, content, message in cases:
            with self.subTest(name=name):
                for path in (self.root / "parts").glob("*.md"):
                    path.unlink()
                (self.root / "parts" / "one.md").write_text(content, encoding="utf-8")
                result = self.run_tool("status")
                self.assertEqual(result.returncode, 2)
                self.assertIn(message, result.stderr)

    def test_symlink_section_and_existing_output_are_refused(self) -> None:
        outside = self.root / "outside.md"
        outside.write_text("body\n<!-- SECTION-END one -->\n", encoding="utf-8")
        (self.root / "parts" / "one.md").symlink_to(outside)
        linked = self.run_tool("status")
        self.assertEqual(linked.returncode, 2)
        self.assertIn("regular non-symlink", linked.stderr)

        (self.root / "parts" / "one.md").unlink()
        self.write_section("one")
        self.write_section("two")
        self.write_section("three")
        (self.root / "assembled.md").write_text("do not overwrite\n", encoding="utf-8")
        existing = self.run_tool("assemble")
        self.assertEqual(existing.returncode, 2)
        self.assertIn("refusing overwrite", existing.stderr)
        self.assertEqual((self.root / "assembled.md").read_text(), "do not overwrite\n")

    def test_reserved_marker_vocabulary_cannot_be_smuggled(self) -> None:
        bodies = (
            "safe prefix\n <!-- BODY-END -->\n",
            "safe prefix x<!-- SECTION-END forged -->\n",
            "safe prefix\n <!-- SECTION forged sha256=" + "f" * 64 + " -->\n",
        )
        for body in bodies:
            with self.subTest(body=body):
                for path in (self.root / "parts").glob("*.md"):
                    path.unlink()
                self.write_section("one", body)
                result = self.run_tool("status")
                self.assertEqual(result.returncode, 2)
                self.assertIn("reserved marker vocabulary", result.stderr)

    def test_duplicate_manifest_keys_fail_closed(self) -> None:
        raw = (self.root / "manifest.json").read_text(encoding="utf-8")
        duplicate = raw.replace('"schema":1', '"schema":1,"schema":1', 1)
        (self.root / "manifest.json").write_text(duplicate, encoding="utf-8")
        result = self.run_tool("status")
        self.assertEqual(result.returncode, 2)
        self.assertIn("duplicate key", result.stderr)

    def test_control_character_in_path_fails_closed(self) -> None:
        self.manifest["sections"][0]["path"] = "parts/invalid\u0000name.md"
        (self.root / "manifest.json").write_text(
            json.dumps(self.manifest, sort_keys=True, separators=(",", ":")) + "\n",
            encoding="utf-8",
        )
        result = self.run_tool("status")
        self.assertEqual(result.returncode, 2)
        self.assertIn("control character", result.stderr)

    def test_output_creation_oserror_uses_contract_exit(self) -> None:
        for section_id in ("one", "two", "three"):
            self.write_section(section_id)
        self.manifest["output"] = "x" * 300
        (self.root / "manifest.json").write_text(
            json.dumps(self.manifest, sort_keys=True, separators=(",", ":")) + "\n",
            encoding="utf-8",
        )
        result = self.run_tool("assemble")
        self.assertEqual(result.returncode, 2)
        self.assertIn("cannot create output exclusively", result.stderr)
        self.assertNotIn("Traceback", result.stderr)


if __name__ == "__main__":
    unittest.main()
#!/usr/bin/env python3
"""Fail-closed classical admissibility gate for new JC2 compute lanes.

This gate deliberately implements only the primary-source-checked
Guccione--Guccione--Valqui/Heitmann necessary condition

    gcd(deg_total(P), deg_total(Q)) >= 16

for a characteristic-zero counterexample.  Passing means only that this
single classical theorem does not close the registered degree scope.
"""

from __future__ import annotations

import argparse
import json
import math
import sys


THEOREM = "GGV-Heitmann-gcd16"
SOURCE = "https://arxiv.org/abs/1401.1784"


def parser() -> argparse.ArgumentParser:
    p = argparse.ArgumentParser(description=__doc__)
    scope = p.add_mutually_exclusive_group(required=True)
    scope.add_argument(
        "--total-degrees",
        nargs=2,
        type=int,
        metavar=("DEG_P", "DEG_Q"),
        help="fixed actual total-degree pair",
    )
    scope.add_argument(
        "--total-cap",
        type=int,
        metavar="D",
        help="both actual total degrees are at most D",
    )
    scope.add_argument(
        "--partial-y-degrees",
        nargs=2,
        type=int,
        metavar=("DEGY_P", "DEGY_Q"),
        help="partial-y degree pair; requires --total-unbounded",
    )
    p.add_argument(
        "--total-unbounded",
        action="store_true",
        help="certify that no finite total/x-degree cap is imposed",
    )
    p.add_argument(
        "--purpose",
        choices=("frontier", "method-control"),
        default="frontier",
        help="frontier lanes fail if classically closed; controls are labelled",
    )
    p.add_argument("--tag", required=True, help="registered unique lane tag")
    return p


def main(argv: list[str] | None = None) -> int:
    args = parser().parse_args(argv)

    supplied = args.total_degrees or args.partial_y_degrees
    values = supplied if supplied is not None else [args.total_cap]
    if any(value is None or value <= 0 for value in values):
        parser().error("all degrees and caps must be positive integers")
    if args.partial_y_degrees and not args.total_unbounded:
        parser().error("--partial-y-degrees requires --total-unbounded")
    if args.total_unbounded and not args.partial_y_degrees:
        parser().error("--total-unbounded is valid only with --partial-y-degrees")

    closed = False
    reason: str
    registered_scope: dict[str, object]

    if args.total_degrees:
        deg_p, deg_q = args.total_degrees
        degree_gcd = math.gcd(deg_p, deg_q)
        closed = degree_gcd < 16
        registered_scope = {
            "kind": "fixed_actual_total_degrees",
            "deg_total_P": deg_p,
            "deg_total_Q": deg_q,
            "gcd": degree_gcd,
        }
        reason = (
            f"gcd({deg_p},{deg_q})={degree_gcd}<16"
            if closed
            else f"gcd({deg_p},{deg_q})={degree_gcd}; this gate is inconclusive"
        )
    elif args.total_cap is not None:
        cap = args.total_cap
        closed = cap < 16
        registered_scope = {"kind": "common_total_degree_cap", "cap": cap}
        reason = (
            f"both total degrees are <= {cap}<16, hence their gcd is <16"
            if closed
            else f"the cap {cap} permits gcd >=16; this gate is inconclusive"
        )
    else:
        deg_y_p, deg_y_q = args.partial_y_degrees
        registered_scope = {
            "kind": "partial_y_only_total_unbounded",
            "deg_y_P": deg_y_p,
            "deg_y_Q": deg_y_q,
        }
        reason = (
            "partial-y bounds do not bound actual total degrees; "
            "the total-degree gcd theorem is inconclusive"
        )

    if closed and args.purpose == "frontier":
        verdict = "REFUSE_CLASSICALLY_CLOSED"
        exit_code = 3
    elif closed:
        verdict = "METHOD_CONTROL_ONLY"
        exit_code = 0
    else:
        verdict = "NOT_CLOSED_BY_THIS_GATE"
        exit_code = 0

    print(
        json.dumps(
            {
                "tag": args.tag,
                "purpose": args.purpose,
                "registered_scope": registered_scope,
                "theorem": THEOREM,
                "source": SOURCE,
                "verdict": verdict,
                "reason": reason,
                "warning": (
                    "NOT_CLOSED_BY_THIS_GATE is not evidence that the scope "
                    "contains a counterexample or is otherwise viable"
                ),
            },
            sort_keys=True,
        )
    )
    return exit_code


if __name__ == "__main__":
    sys.exit(main())
#!/usr/bin/env python3

from __future__ import annotations

import json
import subprocess
import sys
import unittest
from pathlib import Path


GATE = Path(__file__).with_name("frontier_gate.py")


def run_gate(*args: str) -> tuple[int, dict[str, object]]:
    proc = subprocess.run(
        [sys.executable, str(GATE), *args],
        check=False,
        capture_output=True,
        text=True,
    )
    return proc.returncode, json.loads(proc.stdout)


class FrontierGateTests(unittest.TestCase):
    def test_exact_9_12_frontier_is_refused(self) -> None:
        rc, result = run_gate(
            "--total-degrees", "9", "12", "--tag", "test-exact"
        )
        self.assertEqual(rc, 3)
        self.assertEqual(result["verdict"], "REFUSE_CLASSICALLY_CLOSED")

    def test_d12_envelope_frontier_is_refused(self) -> None:
        rc, result = run_gate("--total-cap", "12", "--tag", "test-cap")
        self.assertEqual(rc, 3)
        self.assertEqual(result["verdict"], "REFUSE_CLASSICALLY_CLOSED")

    def test_closed_method_control_is_allowed_and_labelled(self) -> None:
        rc, result = run_gate(
            "--total-degrees",
            "8",
            "12",
            "--purpose",
            "method-control",
            "--tag",
            "test-control",
        )
        self.assertEqual(rc, 0)
        self.assertEqual(result["verdict"], "METHOD_CONTROL_ONLY")

    def test_unbounded_total_partial_y_is_not_misclosed(self) -> None:
        for lower in (8, 9):
            with self.subTest(lower=lower):
                rc, result = run_gate(
                    "--partial-y-degrees",
                    str(lower),
                    "12",
                    "--total-unbounded",
                    "--tag",
                    f"test-partial-y-{lower}-12",
                )
                self.assertEqual(rc, 0)
                self.assertEqual(result["verdict"], "NOT_CLOSED_BY_THIS_GATE")


if __name__ == "__main__":
    unittest.main()

exec
/bin/zsh -lc "find xmodel -maxdepth 1 -type f -name 'systems-*.md' -print | sort | tail -20" in /Users/dc/code/math/jc2
 succeeded in 0ms:
xmodel/systems-collision-preflight-sol56-20260902.prompt.md
xmodel/systems-custody-hardening-software-review-sol56-20260831.md
xmodel/systems-custody-hardening-software-review-sol56-20260831.prompt.md
xmodel/systems-custody-p0-patch-sol56-20260831.md
xmodel/systems-custody-p0-patch-sol56-20260831.prompt.md
xmodel/systems-semantic-replay-v1-sol56-20260831.md
xmodel/systems-semantic-replay-v1-sol56-20260831.prompt.md

exec
/bin/zsh -lc "sed -n '1,240p' xmodel/systems-semantic-replay-v1-sol56-20260831.md && sed -n '1,220p' xmodel/systems-custody-p0-patch-sol56-20260831.md" in /Users/dc/code/math/jc2
 succeeded in 0ms:
# SEMANTIC-REPLAY/v1 — replay-integrity CI gate

## 1. Scope and input integrity

This report proposes an additive replay-certificate schema and an apply-ready,
pure-standard-library checker patch.  It does not integrate the checker with a
CI runner and does not alter any existing artifact convention or tool.

Before reading the charged inputs, I recomputed SHA-256 over the three frozen
copies.  All three matched the supplied values exactly:

```text
2793104bcf746eab690ca75a2aaf2ef105b64e7be0c1826eb1fa9ed51ba992ef  inputs/ideation-20260831T1033Z-synthesis.md
513fe4c022b3a84531c0e42e4cc61a1c9cf2732dde4a5931cb6f8384e166ad4d  inputs/block-descent-a1-rank4-reducible-component-tree-ledger-grok46-20260831.md
1500eeb24e1a9f4b2d27daeca85f6ed735a962ae2f7d26e8f923f38ea7d1de7a  inputs/block-descent-a1-genus-ladder-conductor12-14-18-20-22-28-s4-census-sol56-20260831.md
```

Here and below, `inputs/…` abbreviates the frozen directory
`/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.NdLXLC/inputs/`.

## 2. Findings from the charged artifacts

The controlling contract is
`inputs/ideation-20260831T1033Z-synthesis.md:115-123`.  In particular, lines
118-123 require the declaration per promoted claim and name the two acceptance
artifacts.

### 2.1 Quarantined reducible ledger: negative control

The ledger is itself labelled "not a review and not a promotion"
(`block-descent-a1-rank4-reducible-component-tree-ledger-grok46-20260831.md:3-5`),
but the synthesis explicitly selects its finite replay as the negative systems
fixture.  Its finite claim is the complete incidence-tree/inertia enumeration,
not a finite enumeration of the unbounded `n22` families: see lines 64-66,
114-130, and 204-213.  The latter passage reports 16 rows, four unconditional
kills, and twelve rows live without the open missing-multiplicity identity.

The replay passage at lines 231-250 names a script, three mutation switches, a
baseline stdout hash, a payload hash, and the fact that every mutation exits
nonzero.  It supplies no `SEMANTIC-REPLAY/v1` declaration, no per-claim binding,
no accepted or rejected witness record emitted by the named path, and no
mutated claim projection or digest.  Human row labels such as `OPEN`, `S4`, and
`EULER` are not evidence that those rows were driven as replay controls.

Therefore the charged ledger **fails as-is with exit 2 (`MALFORMED`)**.  Missing
mutation evidence is not observed invariance, and assigning it exit 3 would
invent an equality that was never measured.  A structurally valid certificate
whose alleged controls were absent from the generator/path transcript would
instead fail with exit 4 (`FAIL-BYPASS`).

### 2.2 Reviewed conductor census: insufficient certificate evidence

The census contains useful ingredients:

- the sealed arithmetic core (a candidate generator dependency) and its hash at
  `block-descent-a1-genus-ladder-conductor12-14-18-20-22-28-s4-census-sol56-20260831.md:81-103`;
- the consuming replay path and its hash at lines 275-280;
- the meaning of marked and unmarked rows plus a same-census positive/negative
  pair at lines 219-225: `(6,4,9)*72` is accepted and `(5,4)` is rejected with
  count zero; and
- one whole-stdout hash, one payload hash, and three mutation switch names at
  lines 281-303.

That is not enough to write a compliant certificate from the artifact.  The
missing data are exactly:

1. an executable generator invocation and output protocol; lines 81-103 call
   the hash-pinned file an imported arithmetic core, not a record emitter;
2. a machine transcript binding the two human-readable row entries to records
   emitted by that generator and consumed by the declared production path;
3. a baseline `complete-results/v1` projection and per-claim digest (the
   supplied digest covers the aggregate stdout/payload);
4. a generator mutation bound to the promoted claim which successfully emits
   records for the same controls; and
5. the resulting mutated claim digest.  Lines 294-303 state only that the
   switches exit nonzero.  Elsewhere, lines 167-168 even say the framing
   mutation fails before a new verdict is consumed.  Process failure is not a
   changed claim digest.

There is an additional typing mismatch: the documented switches mutate the
consuming compiler/criterion/promotion replay, whereas the contract calls for
a generator mutation.  The artifact does not declare a mutation of the sealed
delta-sequence generator.

Accordingly, the artifact-derived partial census certificate in the tests
**fails with exit 2 (`MALFORMED`)** and names the absent generator mutation.
This is the contract's explicit insufficient-artifact branch.  Supplying
placeholder digests, treating the whole-stdout hash as a per-claim digest, or
treating a nonzero exit as a mutated claim would manufacture evidence and is
not done.

## 3. Replay-certificate declaration format

The certificate is UTF-8 JSON.  Paths are relative to the certificate.  The
format is additive: an existing artifact may reference this new sidecar; no
existing field is renamed, and the checker does not rewrite artifacts.

```json
{
  "schema": "SEMANTIC-REPLAY/v1",
  "claims": [
    {
      "id": "complete-conductor-census",
      "generator": {
        "id": "delta-sequence-generator",
        "script": "../ops/generate_rows.py",
        "sha256": "<64 lowercase hex>",
        "args": ["--emit-semantic-replay"]
      },
      "production_path": {
        "id": "row-to-braid-to-s4-verdict",
        "script": "../ops/classify_rows.py",
        "sha256": "<64 lowercase hex>",
        "args": ["--consume-semantic-replay"]
      },
      "claim_projection": "complete-results/v1",
      "witnesses": {
        "accepted": {"conductor": 12, "row": [6, 4, 9]},
        "rejected": {"conductor": 12, "row": [5, 4]}
      },
      "mutation": {
        "id": "promote-zero-control",
        "generator_args_append": ["--mutate-generated-zero-row"],
        "claim_sha256": "<64 lowercase hex>"
      },
      "baseline_claim_sha256": "<64 lowercase hex>"
    }
  ]
}
```

The two scripts use a deliberately small JSON protocol.  A normal or mutated
generator run emits:

```json
{
  "protocol": "SEMANTIC-REPLAY/v1/generator",
  "generator_id": "delta-sequence-generator",
  "records": [
    {"witness": {"conductor": 12, "row": [5, 4]}, "data": {"count": 0}}
  ]
}
```

The gate canonically passes only the protocol, generator identifier, and exact
`{witness,data}` records to the hash-pinned production path on standard input;
generator diagnostics and mutation context are not exposed to that path.  The
path emits:

```json
{
  "protocol": "SEMANTIC-REPLAY/v1/path",
  "path_id": "row-to-braid-to-s4-verdict",
  "claim_id": "complete-conductor-census",
  "results": [
    {
      "witness": {"conductor": 12, "row": [5, 4]},
      "decision": "reject",
      "source_record_sha256": "<SHA-256 of the exact generator record>",
      "claim_fragment": {"labelled_full_s4_count": 0}
    }
  ]
}
```

For each run the checker requires a one-to-one equality, by canonical witness
object, between generator records and path results, and requires every result
to carry the SHA-256 of its complete canonical `{witness,data}` source record.
Unknown, duplicate, omitted, or record-substituted witnesses are bypasses.  On
the baseline run it also requires the two declared controls to occur with
opposite declared decisions.  On the mutant run both control identities must
still be generated and reach the same path; their decisions may change.

`complete-results/v1` is the canonical object

```text
{"claim_id": ID, "projection": "complete-results/v1", "results": RESULTS}
```

where `RESULTS` contains every `{witness, decision, claim_fragment}` sorted by
the canonical witness bytes.  Canonical JSON uses sorted keys, no insignificant
whitespace, ASCII escapes, UTF-8, and no floats or non-finite values.  The gate
computes SHA-256 itself, checks the baseline commitment, and requires the live
baseline and mutant digests to differ.  For a non-invariant run it then checks
the mutant commitment.  Hashing the complete result ledger makes the finite
domain, rejected rows, and accepted fragments part of the promoted claim;
reordering alone is inert.

Only generator arguments may change in the metamorphic run.  The production
path, its arguments, and its file hash remain fixed.  Each entrypoint hash is
rechecked immediately before every invocation, including between generator and
path runs.  Both are invoked as argument arrays with the running Python
interpreter (`shell=False`), with bytecode disabled and a 60-second per-process
timeout.  The gate itself uses no network service or CAS.

Exit classification is deterministic:

| exit | status | meaning |
|---:|---|---|
| 0 | `PASS` | every claim has bound controls and distinct committed live digests |
| 2 | `MALFORMED` | unreadable/invalid JSON or a missing/ill-typed declaration |
| 3 | `FAIL-INVARIANCE` | otherwise valid baseline and mutant projections have the same digest |
| 4 | `FAIL-BYPASS` | hash, execution, protocol, record binding, witness, or non-invariant digest-commitment provenance fails |

The checker validates the entire static schema before executing anything.  It
requires valid provenance for both live projections, then reports equal live
digests as invariance before consulting the mutant's predicted commitment.  A
bypass in any other claim still takes precedence over a recorded invariance.

## 4. Proposed implementation and tests

The following is the single patch to apply.  It adds only the checker and its
unit tests.

```diff
diff --git a/ops/replay_gate.py b/ops/replay_gate.py
new file mode 100755
--- /dev/null
+++ b/ops/replay_gate.py
@@ -0,0 +1,547 @@
+#!/usr/bin/env python3
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import subprocess
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any
+
+
+SCHEMA = "SEMANTIC-REPLAY/v1"
+GENERATOR_PROTOCOL = f"{SCHEMA}/generator"
+PATH_PROTOCOL = f"{SCHEMA}/path"
+PROJECTION = "complete-results/v1"
+TIMEOUT_SECONDS = 60
# D1 — Canonical seal boundary

`verify_bytes` now accepts exactly the UTF-8 byte string emitted by `_canonical_seal`; its fixed post-body line ending is LF, while an LF- or CRLF-terminated marker remains part of the body and is hashed byte-for-byte.  The divert classifier consequently treats a valid canonical seal plus even one suffix byte (and every CRLF-encoded or otherwise noncanonical seal tail) as overflow, preserving the untouched input in the raw bank and the complete tail in the overflow bank instead of reporting `CLEAN_SEALED`.

# D2 — Unterminated marker

Divert still detects an exact standalone marker at EOF, but it no longer calls that report clean when the marker has no LF or CRLF terminator: it returns the distinct, non-mutating `UNTERMINATED_MARKER` status.  `lane.sh` maps that status to `report_state=PARTIAL_NO_MARKER` and forces final rc 7, matching `verify`'s existing refusal to regard the same bytes as sealed.

# D3 — Divert race and cleanup

The diversion attempt tracks only the raw and overflow files it successfully created; every pre-replace failure removes those files, so an occupied overflow target cannot strand a new raw artifact or disturb the pre-existing overflow.  The existing last-moment report reread is changed from an error-only comparison into a retry boundary: if bytes or metadata changed, the staged report and this attempt's sidecars are removed, the newly read stable bytes are reclassified, and only an unchanged classification reaches `os.replace`; the requested larger fsync/manifest transaction redesign remains out of scope.

# S1 — Charged-path lexical hardening

The launcher scopes `LC_ALL=C` to charged-path shell patterns and ASCII folds, rejects absolute paths plus `.`/`..` components, folds case for both the `jc2-lean` exclusion and basename-uniqueness key, and calls a small `os.lstat` walker that rejects a symlink at every charged relative-path component without resolving it.  Lexical exclusion runs before that walker, so excluded aliases are refused without traversal; the deliberately scoped fix retains the existing hash/copy flow rather than introducing the deferred dirfd/openat redesign, and the provider keeps its inherited locale.

# S5 — Uniform hardening exit status and adapter receipt

Launch-time report-path and charged-input contract refusals now return 7, as do all post-run BODY-END boundary failures regardless of a nonzero provider result; a no-marker report remains informational only when no BODY-END contract was declared.  The status returned directly by `wait` is captured before any lane override and added to the receipt as `adapter_exit_code`, while the existing `exit_code` field continues to record the final lane rc, preserving both facts without renaming or changing any existing receipt key.

# R4 — Receipt-safe paths

Before any prompt-derived path can be emitted into a receipt, the launcher rejects Unicode control, format, surrogate, and line-separator categories: the CLI-supplied prompt pathname is checked without reflecting the unsafe value, and each charged path is checked before diagnostics, manifest construction, or receipt emission.  These refusals use the same rc 7 hardening-contract path, safe path fields use `printf` rather than implementation-defined `echo`, printable non-ASCII prompt filenames remain allowed, and charged paths remain intentionally ASCII-only under the S1 bytewise policy.

# Unified diff

```diff
--- ops/lane.sh
+++ ops/lane.sh
@@ -42,6 +42,24 @@
   echo "unknown adapter: $adapter" >&2
   exit 2
 }
+command -v python3 >/dev/null 2>&1 || {
+  echo "python3 is required for lane validation" >&2
+  exit 2
+}
+receipt_path_is_safe() {
+  python3 - "$1" <<'PY'
+import sys
+import unicodedata
+
+for character in sys.argv[1]:
+    if unicodedata.category(character) in {"Cc", "Cf", "Cs", "Zl", "Zp"}:
+        raise SystemExit(1)
+PY
+}
+if ! receipt_path_is_safe "$prompt_file"; then
+  echo "prompt path contains a control or line-separator character; run refused" >&2
+  exit 7
+fi
 [ -f "$prompt_file" ] || {
   echo "prompt file not found: $prompt_file" >&2
   exit 2
@@ -57,10 +75,6 @@
 seal_tool=$script_dir/seal.py
 [ -f "$seal_tool" ] && [ ! -L "$seal_tool" ] || {
   echo "required seal tool missing: $seal_tool" >&2
-  exit 2
-}
-command -v python3 >/dev/null 2>&1 || {
-  echo "python3 is required for charge-basis validation" >&2
   exit 2
 }
 sandbox_exec=/usr/bin/sandbox-exec
@@ -162,6 +176,7 @@
 inputs_dir=$lane_tmp_dir/inputs
 inputs_manifest=$lane_tmp_dir/charged-inputs.list
 inputs_post_manifest=$lane_tmp_dir/charged-inputs-post.list
+input_basename_keys=$lane_tmp_dir/charged-input-basename-keys.list
 # Keep this lexical: boundary setup must neither resolve nor inspect the excluded
 # nested worktree.  Seatbelt resolves aliases when enforcing the path filter.
 excluded_tree=$repo_root/jc2-lean
@@ -215,7 +230,7 @@
 # response cannot be lost to a misdirected or unwritten report.
 if ! grep -F "xmodel/$tag.md" "$prompt_snapshot" >/dev/null 2>&1; then
   echo "prompt does not name its mandated report path xmodel/$tag.md; run refused" >&2
-  exit 2
+  exit 7
 fi
 
 # A prompt that instructs the BODY-END convention declares the marker
@@ -235,53 +250,102 @@
 if grep -F "$input_placeholder" "$prompt_snapshot" >/dev/null 2>&1; then
   prompt_has_placeholder=1
 fi
+charged_path_is_lexically_valid() {
+  (
+    LC_ALL=C
+    export LC_ALL
+    case "$1" in
+      /*|*..*|*[!A-Za-z0-9/._-]*) exit 1 ;;
+    esac
+    case "/$1/" in
+      */./*) exit 1 ;;
+    esac
+    exit 0
+  )
+}
+charged_path_is_regular_without_symlinks() {
+  python3 - "$repo_root" "$1" <<'PY'
+import os
+import stat
+import sys
+
+root, relative = sys.argv[1:]
+parts = relative.split("/")
+current = root
+for index, component in enumerate(parts):
+    current = os.path.join(current, component)
+    try:
+        info = os.lstat(current)
+    except OSError:
+        raise SystemExit(1)
+    if stat.S_ISLNK(info.st_mode):
+        raise SystemExit(1)
+    if index < len(parts) - 1 and not stat.S_ISDIR(info.st_mode):
+        raise SystemExit(1)
+    if index == len(parts) - 1 and not stat.S_ISREG(info.st_mode):
+        raise SystemExit(1)
+PY
+}
 input_count=0
 if [ -n "$charged_inputs" ] || [ "$prompt_has_placeholder" -eq 1 ]; then
   if [ -z "$charged_inputs" ] || [ "$prompt_has_placeholder" -eq 0 ]; then
     echo "charged_input declarations and the $input_placeholder placeholder must appear together; run refused" >&2
-    exit 2
+    exit 7
   fi
   case "$inputs_dir" in
     *[!A-Za-z0-9/._-]*)
       echo "lane inputs directory has unsupported characters: $inputs_dir" >&2
-      exit 2
+      exit 7
       ;;
   esac
   mkdir "$inputs_dir" || exit 2
   while IFS= read -r charged_rel; do
     [ -n "$charged_rel" ] || continue
-    case "$charged_rel" in
-      /*|*..*|*[!A-Za-z0-9/._-]*)
-        echo "invalid charged input path: $charged_rel" >&2
-        exit 2
-        ;;
+    if ! receipt_path_is_safe "$charged_rel"; then
+      echo "charged input path contains a control or line-separator character; run refused" >&2
+      exit 7
+    fi
+    if ! charged_path_is_lexically_valid "$charged_rel"; then
+      echo "invalid charged input path: $charged_rel" >&2
+      exit 7
+    fi
+    charged_rel_fold=$(printf '%s' "$charged_rel" | LC_ALL=C tr 'A-Z' 'a-z') || exit 2
+    case "$charged_rel_fold" in
       jc2-lean|jc2-lean/*)
         echo "charged input inside the excluded tree refused: $charged_rel" >&2
-        exit 2
+        exit 7
         ;;
     esac
+    if ! charged_path_is_regular_without_symlinks "$charged_rel"; then
+      echo "charged input missing or not a regular file, or has a symlink component: $charged_rel" >&2
+      exit 7
+    fi
     charged_src=$repo_root/$charged_rel
-    if [ ! -f "$charged_src" ] || [ -L "$charged_src" ]; then
-      echo "charged input missing or not a regular file: $charged_rel" >&2
-      exit 2
+    charged_base=$(basename "$charged_rel")
+    charged_base_key=$(printf '%s' "$charged_base" | LC_ALL=C tr 'A-Z' 'a-z') || exit 2
+    charged_dest=$inputs_dir/$charged_base
+    duplicate_base=0
+    if [ -f "$input_basename_keys" ]; then
+      if grep -Fqx -e "$charged_base_key" "$input_basename_keys"; then
+        duplicate_base=1
+      fi
     fi
-    charged_base=$(basename "$charged_rel")
-    charged_dest=$inputs_dir/$charged_base
-    if [ -e "$charged_dest" ]; then
+    if [ "$duplicate_base" -eq 1 ] || [ -e "$charged_dest" ]; then
       echo "duplicate charged input basename: $charged_base" >&2
-      exit 2
+      exit 7
     fi
+    printf '%s\n' "$charged_base_key" >> "$input_basename_keys" || exit 2
     charged_src_sha=$(sha256_file "$charged_src") || exit 2
     cp "$charged_src" "$charged_dest" || exit 2
     chmod 400 "$charged_dest" || exit 2
     charged_snap_sha=$(sha256_file "$charged_dest") || exit 2
     if [ "$charged_snap_sha" != "$charged_src_sha" ]; then
       echo "charged input changed while being snapshotted: $charged_rel" >&2
-      exit 2
+      exit 7
     fi
     input_count=$((input_count + 1))
     printf '%s %s %s\n' "$charged_rel" "$charged_base" "$charged_src_sha" \
       >> "$inputs_manifest"
   done <<CHARGED_INPUTS_EOF
 $charged_inputs
 CHARGED_INPUTS_EOF
@@ -339,7 +403,7 @@
   echo "pid=$$"
   echo "host=$(hostname)"
   echo "basis=$basis"
-  echo "prompt=$prompt_file"
+  printf '%s\n' "prompt=$prompt_file"
   echo "prompt_sha256=$prompt_sha"
   echo "adapter_sha256=$adapter_sha"
   echo "launcher=ops/lane.sh"
@@ -381,7 +445,8 @@
 child_pid=$!
 echo "child_pid=$child_pid" >> "$run_file"
 wait "$child_pid"
-rc=$?
+adapter_exit_code=$?
+rc=$adapter_exit_code
 
 if [ -n "$caught_signal" ]; then
   while kill -0 "$child_pid" 2>/dev/null; do

exec
/bin/zsh -lc "sed -n '1,260p' xmodel/systems-collision-preflight-sol56-20260902.prompt.md" in /Users/dc/code/math/jc2
 succeeded in 0ms:
# Systems lane: OPEN-COLLISION hook + PREFLIGHT hard gate (bounded implementation)

Round 20260902T0741Z adopted one new systems upgrade and re-specified the
standing one (charged synthesis, sections 6 and 9; the Opus and Sol
submissions carry the designs):
(A) OPEN/BANKED COLLISION CHECK (Opus §8.3). Before a report is sealed,
    extract every OPEN[...] it RAISES and grep the banked corpus
    (xmodel/*.md, AUDIT.md, notes.md, APPROACHES.md) for the OPEN's key
    nouns plus the quantity it asks to bound; emit a COLLISIONS section
    listing candidate matches with file:line. Fail-CLOSED: an empty
    COLLISIONS section must be explicit, never absent. The coordinator
    ran the smallest useful test by hand and it PASSED (grep for
    OPEN[DEG-AF-VS-N]'s bounded quantity surfaces
    companion-curve-alln-opus5-20260902.md lines 685/785/836). Deliver
    ops/open_collision.py (pure python, no third-party deps), a
    focused test file ops/test_open_collision.py whose fixtures include
    that exact retroactive case and one negative case (an OPEN with no
    collision must yield an explicit empty COLLISIONS block), and a
    one-paragraph integration note on how lane.sh or seal.py would call
    it at seal time (describe; do NOT modify lane.sh or seal.py — live
    lanes are running under them).
(B) PREFLIGHT AS A HARD LAUNCH GATE (Sol §10). Specify and implement the
    encoding-faithfulness preflight as a gate for every N >= 6
    realisation job: before solver launch the generator must (i) emit the
    reduced approximate root beside the ideal in the declared coefficient
    ring and order; (ii) exact-diff each encoded coefficient condition;
    (iii) fail the old (8,6,19) false-positive canary on the corrected
    86A cell; (iv) pass the explicit four-node (9,6,2) curve on corrected
    96B; (v) fail closed on any ring / map / open-factor / generator-order
    mismatch; plus the two-field output typing
    artifact = NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT |
    FULL_EO_POINT | CURVE | REPRESENTATION | FULL_COVER | POLYNOMIAL_PAIR
    and attainment = NECESSARY | REALIZED | ADMISSIBLE_PULLBACK |
    ACTUAL_MAP | COUNTEREXAMPLE_CERTIFIED, with a checked transition table
    that rejects "A2 cell -> B3 kill", "local torsion -> global torsion",
    and FULL_EO_POINT -> ACTUAL_MAP without every declared arrow; and a
    source-fidelity canary: a generator whose EQ2_even omits -2qE1 must
    FAIL the preflight (the campaign's CELL-32 spec had exactly that
    defect; see the synthesis). Reuse the corrected ideals and semantic
    controls of xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md
    if present; deliver box/preflight.py + ops/test_preflight.py with
    those five checks and the canary as tests, fail-closed by default.
    Default stack: qqideal 0.2.0 + msolveio 0.2.1 (msolve 0.10.1); tests
    must not need msolve to run (mock or skip cleanly when the binary is
    absent).
Discipline: implementation lane, no mathematics claimed; no canonical
ledger edits; do not modify lane.sh, seal.py, artifact_finalize.py, or
any running lane's files; do not inspect jc2-lean; keep every test under
10 seconds. Report: a short typed block per deliverable (file, tests
run, pass/fail counts, integration note) in
xmodel/systems-collision-preflight-sol56-20260902.md
Seal-at-completion; bounded writes; target 8-15KB; 60 minutes.
charged_input=xmodel/ideation-20260902T0741Z-opus5.md
charged_input=xmodel/ideation-20260902T0741Z-sol56.md
charged_input=xmodel/ideation-20260902T0741Z-synthesis.md

Your inputs are frozen read-only copies in {{LANE_INPUTS}};
verify these SHA-256 hashes first and stop on mismatch:

```text
e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  {{LANE_INPUTS}}/ideation-20260902T0741Z-opus5.md
717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  {{LANE_INPUTS}}/ideation-20260902T0741Z-sol56.md
4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  {{LANE_INPUTS}}/ideation-20260902T0741Z-synthesis.md
```

exec
/bin/zsh -lc 'python3 ops/seal.py --help' in /Users/dc/code/math/jc2
 succeeded in 0ms:
usage: seal.py [-h] {verify,stamp,divert} ...

Stamp or verify the campaign's BODY-END report integrity seal. Only a
standalone ``<!-- BODY-END -->`` line is a marker. Inline or quoted mentions
are ordinary body text. The sole canonical post-body seal is the exact UTF-8
serialization returned by the module's canonical-seal serializer: its text,
order, wrapping, punctuation, and LF line endings are fixed. CRLF is accepted
in the body and marker line, where both bytes are hashed, but never in the
post-body seal. Any other post-body prefix, suffix, CRLF, or lone carriage
return makes the seal invalid. The tool never discovers files: every path must
be supplied explicitly. A body hash detects accidental or concurrent mutation;
it is not an authenticity signature. Full-file hashes and external run
receipts remain separate custody evidence.

positional arguments:
  {verify,stamp,divert}
    verify              verify one or more sealed reports
    stamp               append a canonical seal atomically
    divert              bank non-whitespace overflow after a sole BODY-END,
                        truncating the charged report to its body; never
                        repairs zero/multiple markers

options:
  -h, --help            show this help message and exit

exec
/bin/zsh -lc "sed -n '1,260p' box/corrected_8611.m2 && sed -n '1,260p' box/corrected_964.m2 && sed -n '1,240p' box/corrected_8611.ms && sed -n '1,240p' box/corrected_964.ms" in /Users/dc/code/math/jc2
 succeeded in 0ms:
-- corrected_8611.m2
-- corrected Delta=(8,6,11) incidence
-- CONVENTION: GROBNER-mode output basis=[1] means UNIT IDEAL means EMPTY.
-- Companion .ms file is comment-free: msolve parses the first line as the
-- variable list (campaign 2026-08-24 in-band-comment hazard).
-- M2 is authoritative. Footguns observed: diff(var,poly); first degree;
-- no pi as a variable; name maps before applying; close stdin (exit 0).
-- No saturate(). Opens are Rabinowitsch. Rings asserted.
-- Corrected (8,6,11) incidence ideal, audit r2 §7.1.
-- G86 = p^3-q^4 + a22 p^2 q + a20 p q^2 + a18 q^3 + a16 p^2 + a14 p q + a12 q^2.
-- Closed: g_j = [t^j] G86 = 0 for j=12..22 (all degrees, not raw odds).
-- Open: u*g_11-1. Chart identities: g_23=0 automatically.
-- I_DP post-check length N=delta_aff=11 is not in this Groebner job.
-- ARCHIVAL / LOWEST PRIORITY. Rep-level dead. Do not consume P1-P3 AWS time.
-- in-chart gcd-2 cover: B=D=F=gam=e=0.

R0 = QQ[a22, a20, a18, a16, a14, a12, B, C, D, E, F, G, b, gam, d, e, f, MonomialOrder => GRevLex];
assert(isPolynomialRing R0);
assert(numgens R0 == 17);
assert(coefficientRing R0 === QQ);

St = R0[symbol t];
assert(numgens St == 18);
use St;
p = t^8 + B*t^5 + C*t^4 + D*t^3 + E*t^2 + F*t + G;
q = t^6 + b*t^4 + gam*t^3 + d*t^2 + e*t + f;
G86poly = p^3 - q^4 + a22*p^2*q + a20*p*q^2 + a18*q^3 + a16*p^2 + a14*p*q + a12*q^2;

-- Name the ring map before applying (footgun: unnamed maps).
n0 = numgens R0;
assert(numgens St == n0 + 1);
if St_0 == t then (
  killt = map(R0, St, prepend(0_R0, gens R0));
) else (
  assert(St_n0 == t);
  killt = map(R0, St, append(gens R0, 0_R0));
);
assert(killt(t) == 0_R0);

gj = (j) -> killt(coefficient(G86poly, t^j));
closedIdx = {22, 21, 20, 19, 18, 17, 16, 15, 14, 13, 12};
closedGens = apply(closedIdx, j -> gj(j));
I0 = ideal closedGens;
assert(ring I0 === R0);
gopen = gj(11);
use R0;
assert(ring gopen === R0);
R = R0[u];
assert(numgens R == 18);
Iopen = sub(I0, R) + ideal(sub(gopen, R)*u - 1);
assert(ring Iopen === R);

Cover0 = ideal(B, D, F, gam, e);
assert(ring Cover0 === R0);
Cover = sub(Cover0, R);
assert(ring Cover === R);

colonInf = (I, J) -> (
  assert(ring I === ring J);
  K := I;
  npass := 0;
  while true do (
    npass = npass + 1;
    if npass > 50 then error "colonInf: exceeded 50 passes";
    Knext := K : J;
    assert(ring Knext === ring I);
    if Knext == K then break;
    K = Knext;
  );
  K
);

Icolon = colonInf(Iopen, Cover);
assert(ring Icolon === R);

-- immersive open: Res_t(p', q') != 0. diff(var, poly) argument order.
dp = diff(t, p);
dq = diff(t, q);
resSt = resultant(dp, dq, t);
res0 = killt(resSt);
assert(ring res0 === R0);
Rv = R0[u, v];
Iimm = sub(I0, Rv) + ideal(sub(gopen, Rv)*u - 1, sub(res0, Rv)*v - 1);
assert(ring Iimm === Rv);
IimmColon = colonInf(Iimm, sub(Cover0, Rv));
assert(ring IimmColon === Rv);

<< "=== corrected_8611 dim Iopen " << dim Iopen << endl;
<< "=== corrected_8611 dim Icolon " << dim Icolon << endl;
<< "=== corrected_8611 dim IimmColon " << dim IimmColon << endl;
Gb = gens gb Iopen;
<< "=== corrected_8611 gb Iopen " << Gb << endl;
if Iopen == ideal(1_R) then << "=== corrected_8611 EMPTY" << endl else << "=== corrected_8611 NONEMPTY" << endl;
-- GROBNER-mode companion: basis=[1] means UNIT IDEAL means EMPTY.
exit 0
-- corrected_964.m2
-- corrected Delta=(9,6,4) incidence
-- CONVENTION: GROBNER-mode output basis=[1] means UNIT IDEAL means EMPTY.
-- Companion .ms file is comment-free: msolve parses the first line as the
-- variable list (campaign 2026-08-24 in-band-comment hazard).
-- M2 is authoritative. Footguns observed: diff(var,poly); first degree;
-- no pi as a variable; name maps before applying; close stdin (exit 0).
-- No saturate(). Opens are Rabinowitsch. Rings asserted.
-- Corrected (9,6,4) incidence ideal, audit r2 §7.1.
-- G96 = p^2-q^3 + a15 p q + a12 q^2 + a9 p + a6 q.
-- Closed: g_j = [t^j] G96 = 0 for 5<=j<=16 (all degrees).
-- Open: u*g_4-1. Chart identity: g_17=0 automatically. Constants omitted.
-- SIX-NODE QUESTION: delta_aff=6 (audit §3/§7.1; pa(9)=28, beta_1=23, delta_inf=22).
-- Corrected locus is known NONEMPTY (HF-twin). Iopen emptiness is NOT the nodal test.
-- REALIZED iff some closed point of Iopen passes idpPostcheck(p,q,6).

R0 = QQ[a15, a12, a9, a6, A, P6, B, P4, C, P2, D, a, Q3, b, Q1, MonomialOrder => GRevLex];
assert(isPolynomialRing R0);
assert(numgens R0 == 15);
assert(coefficientRing R0 === QQ);

St = R0[symbol t];
assert(numgens St == 1);
use St;
p = t^9 + A*t^7 + P6*t^6 + B*t^5 + P4*t^4 + C*t^3 + P2*t^2 + D*t;
q = t^6 + a*t^4 + Q3*t^3 + b*t^2 + Q1*t;
G96poly = p^2 - q^3 + a15*p*q + a12*q^2 + a9*p + a6*q;

-- Name the ring map before applying (footgun: unnamed maps).
killt = map(R0, St, {0_R0});
assert(killt(t) == 0_R0);

gj = (j) -> coefficient(t^j, G96poly);
closedIdx = {16, 15, 14, 13, 12, 11, 10, 9, 8, 7, 6, 5};
closedGens = apply(closedIdx, j -> gj(j));
I0 = ideal closedGens;
assert(ring I0 === R0);
gopen = gj(4);
use R0;
assert(ring gopen === R0);
R = R0[u];
assert(numgens R == 1);
Iopen = sub(I0, R) + ideal(sub(gopen, R)*u - 1);
assert(ring Iopen === R);

-- even-odd cover: P6=P4=P2=Q3=Q1=0 (degree-3 cover of a (3,2) cusp)
Cover0 = ideal(P6, P4, P2, Q3, Q1);
assert(ring Cover0 === R0);
Cover = sub(Cover0, R);
assert(ring Cover === R);

colonInf = (I, J) -> (
  assert(ring I === ring J);
  K := I;
  npass := 0;
  while true do (
    npass = npass + 1;
    if npass > 50 then error "colonInf: exceeded 50 passes";
    Knext := K : J;
    assert(ring Knext === ring I);
    if Knext == K then break;
    K = Knext;
  );
  K
);

Icolon = colonInf(Iopen, Cover);
assert(ring Icolon === R);

dp = diff(t, p);
dq = diff(t, q);
resSt = resultant(dp, dq, t);
res0 = killt(resSt);
assert(ring res0 === R0);
Rv = R0[u, v];
Iimm = sub(I0, Rv) + ideal(sub(gopen, Rv)*u - 1, sub(res0, Rv)*v - 1);
assert(ring Iimm === Rv);
IimmColon = colonInf(Iimm, sub(Cover0, Rv));
assert(ring IimmColon === Rv);

<< "=== corrected_964 dim Iopen " << dim Iopen << endl;
<< "=== corrected_964 dim Icolon " << dim Icolon << endl;
<< "=== corrected_964 dim IimmColon " << dim IimmColon << endl;
Gb = gens gb Iopen;
<< "=== corrected_964 gb Iopen " << Gb << endl;
if Iopen == ideal(1_R) then << "=== corrected_964 EMPTY" << endl else << "=== corrected_964 NONEMPTY" << endl;

-- Double-point scheme post-check for a CLOSED point (p0,q0) in QQ[tt].
-- N = delta_aff = 6. No saturate(). Variable Pi, not pi.
-- Conversion: Delta(tt^n) = sig*Delta(tt^{n-1}) - Pi*Delta(tt^{n-2}),
-- Delta(tt^0)=0, Delta(tt^1)=1.

dividedDiffTable = (maxn, sig, Pi) -> (
  L := new MutableList from toList((maxn+1):0_(ring sig));
  L#0 = 0_(ring sig);
  if maxn >= 1 then L#1 = 1_(ring sig);
  if maxn >= 2 then L#2 = sig;
  for n from 3 to maxn do L#n = sig*(L#(n-1)) - Pi*(L#(n-2));
  toList L
);

polyToDelta = (f, L) -> (
  Kt := ring f;
  tt := Kt_0;
  df := first degree f;
  acc := 0_(ring L#1);
  for n from 0 to df do (
    c := coefficient(tt^n, f);
    acc = acc + sub(c, ring L#1) * L#n;
  );
  acc
);

idpPostcheck = method();
idpPostcheck(RingElement, RingElement, ZZ) := (p0, q0, N) -> (
  Kt := ring p0;
  assert(Kt === ring q0);
  assert(numgens Kt == 1);
  tt := Kt_0;
  dp0 := diff(tt, p0);
  dq0 := diff(tt, q0);
  gg := gcd(dp0, dq0);
  immersive := (gg == 1_Kt);
  Rsp := QQ[sig, Pi, MonomialOrder => GRevLex];
  sig := Rsp_0;
  Pi := Rsp_1;
  maxn := max(first degree p0, first degree q0);
  L := dividedDiffTable(maxn, sig, Pi);
  Dp := polyToDelta(p0, L);
  Dq := polyToDelta(q0, L);
  I := ideal(Dp, Dq);
  assert(ring I === Rsp);
  << "idp: dim=" << dim I << " degree=" << (if dim I == 0 then degree I else -1) << endl;
  << "idp: immersive=" << immersive << endl;
  if dim I != 0 then (
    << "idp: FAIL not 0-dimensional" << endl;
    return false;
  );
  if degree I != N then (
    << "idp: FAIL length " << degree I << " != expected " << N << endl;
    return false;
  );
  if I != radical I then (
    << "idp: FAIL not reduced" << endl;
    return false;
  );
  << "idp: PASS reduced length " << N << endl;
  true
);

<< "=== corrected_964 six-node question: idpPostcheck(p0,q0,6) on a closed point of Iopen" << endl;
<< "=== corrected_964 corrected locus is known NONEMPTY (HF-twin); EMPTY of Iopen would contradict that control" << endl;

-- GROBNER-mode companion: basis=[1] means UNIT IDEAL means EMPTY.
exit 0
a22,a20,a18,a16,a14,a12,B,C,D,E,F,G,b,gam,d,e,f,u
0
a22-4*b,
3*B-4*gam,
a22*b+a20+3*C-6*b^2-4*d,
2*a22*B+a22*gam+3*D-12*b*gam-4*e,
2*a22*C+a22*d+2*a20*b+a18+3*B^2+3*E-4*b^3-12*b*d-6*gam^2-4*f,
2*a22*B*b+2*a22*D+a22*e+a20*B+2*a20*gam+6*B*C+3*F-12*b^2*gam-12*b*e-12*gam*d,
a22*B^2+2*a22*B*gam+2*a22*C*b+2*a22*E+a22*f+a20*C+a20*b^2+2*a20*d+3*a18*b+a16+6*B*D+3*C^2+3*G-b^4-12*b^2*d-12*b*gam^2-12*b*f-12*gam*e-6*d^2,
2*a22*B*C+2*a22*B*d+2*a22*C*gam+2*a22*D*b+2*a22*F+2*a20*B*b+a20*D+2*a20*b*gam+2*a20*e+3*a18*gam+B^3+6*B*E+6*C*D-4*b^3*gam-12*b^2*e-24*b*gam*d-4*gam^3-12*gam*f-12*d*e,
a22*B^2*b+2*a22*B*D+2*a22*B*e+a22*C^2+2*a22*C*d+2*a22*D*gam+2*a22*E*b+2*a22*G+2*a20*B*gam+2*a20*C*b+a20*E+2*a20*b*d+a20*gam^2+2*a20*f+3*a18*b^2+3*a18*d+a14+3*B^2*C+6*B*F+6*C*E+3*D^2-4*b^3*d-6*b^2*gam^2-12*b^2*f-24*b*gam*e-12*b*d^2-12*gam^2*d-12*d*f-6*e^2,
a22*B^2*gam+2*a22*B*C*b+2*a22*B*E+2*a22*B*f+2*a22*C*D+2*a22*C*e+2*a22*D*d+2*a22*E*gam+2*a22*F*b+a20*B*b^2+2*a20*B*d+2*a20*C*gam+2*a20*D*b+a20*F+2*a20*b*e+2*a20*gam*d+6*a18*b*gam+3*a18*e+2*a16*B+3*B^2*D+3*B*C^2+6*B*G+6*C*F+6*D*E-4*b^3*e-12*b^2*gam*d-4*b*gam^3-24*b*gam*f-24*b*d*e-12*gam^2*e-12*gam*d^2-12*e*f,
a22*B^2*d+2*a22*B*C*gam+2*a22*B*D*b+2*a22*B*F+a22*C^2*b+2*a22*C*E+2*a22*C*f+a22*D^2+2*a22*D*e+2*a22*E*d+2*a22*F*gam+2*a22*G*b+2*a20*B*b*gam+2*a20*B*e+a20*C*b^2+2*a20*C*d+2*a20*D*gam+2*a20*E*b+a20*G+2*a20*b*f+2*a20*gam*e+a20*d^2+a18*b^3+6*a18*b*d+3*a18*gam^2+3*a18*f+2*a16*C+a14*b+a12+3*B^2*E+6*B*C*D+C^3+6*C*G+6*D*F+3*E^2-4*b^3*f-12*b^2*gam*e-6*b^2*d^2-12*b*gam^2*d-24*b*d*f-12*b*e^2-gam^4-12*gam^2*f-24*gam*d*e-4*d^3-6*f^2,
a22*B^2*e*u+2*a22*B*C*d*u+2*a22*B*D*gam*u+2*a22*B*E*b*u+2*a22*B*G*u+a22*C^2*gam*u+2*a22*C*D*b*u+2*a22*C*F*u+2*a22*D*E*u+2*a22*D*f*u+2*a22*E*e*u+2*a22*F*d*u+2*a22*G*gam*u+2*a20*B*b*d*u+a20*B*gam^2*u+2*a20*B*f*u+2*a20*C*b*gam*u+2*a20*C*e*u+a20*D*b^2*u+2*a20*D*d*u+2*a20*E*gam*u+2*a20*F*b*u+2*a20*gam*f*u+2*a20*d*e*u+3*a18*b^2*gam*u+6*a18*b*e*u+6*a18*gam*d*u+2*a16*D*u+a14*B*u+a14*gam*u+3*B^2*F*u+6*B*C*E*u+3*B*D^2*u+3*C^2*D*u+6*D*G*u+6*E*F*u-12*b^2*gam*f*u-12*b^2*d*e*u-12*b*gam^2*e*u-12*b*gam*d^2*u-24*b*e*f*u-4*gam^3*d*u-24*gam*d*f*u-12*gam*e^2*u-12*d^2*e*u-1
a15,a12,a9,a6,A,P6,B,P4,C,P2,D,a,Q3,b,Q1,u
0
2*A-3*a,
a15+2*P6-3*Q3,
A^2+2*B-3*a^2-3*b,
a15*A+a15*a+2*A*P6+2*P4-6*a*Q3-3*Q1,
a15*P6+a15*Q3+a12+2*A*B+P6^2+2*C-a^3-6*a*b-3*Q3^2,
a15*A*a+a15*B+a15*b+2*A*P4+2*P6*B+2*P2-3*a^2*Q3-6*a*Q1-6*Q3*b,
a15*A*Q3+a15*P6*a+a15*P4+a15*Q1+2*a12*a+2*A*C+2*P6*P4+B^2+2*D-3*a^2*b-3*a*Q3^2-6*Q3*Q1-3*b^2,
a15*A*b+a15*P6*Q3+a15*B*a+a15*C+2*a12*Q3+a9+2*A*P2+2*P6*C+2*B*P4-3*a^2*Q1-6*a*Q3*b-Q3^3-6*b*Q1,
a15*A*Q1+a15*P6*b+a15*B*Q3+a15*P4*a+a15*P2+a12*a^2+2*a12*b+2*A*D+2*P6*P2+2*B*C+P4^2-6*a*Q3*Q1-3*a*b^2-3*Q3^2*b-3*Q1^2,
a15*P6*Q1+a15*B*b+a15*P4*Q3+a15*C*a+a15*D+2*a12*a*Q3+2*a12*Q1+a9*A+2*P6*D+2*B*P2+2*P4*C-6*a*b*Q1-3*Q3^2*Q1-3*Q3*b^2,
a15*B*Q1+a15*P4*b+a15*C*Q3+a15*P2*a+2*a12*a*b+a12*Q3^2+a9*P6+a6+2*B*D+2*P4*P2+C^2-3*a*Q1^2-6*Q3*b*Q1-b^3,
a15*P4*Q1+a15*C*b+a15*P2*Q3+a15*D*a+2*a12*a*Q1+2*a12*Q3*b+a9*B+2*P4*D+2*C*P2-3*Q3*Q1^2-3*b^2*Q1,
a15*C*Q1*u+a15*P2*b*u+a15*D*Q3*u+2*a12*Q3*Q1*u+a12*b^2*u+a9*P4*u+a6*a*u+2*C*D*u+P2^2*u-3*b*Q1^2*u-1

exec
/bin/zsh -lc "sed -n '175,275p' xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md && sed -n '276,375p' xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md" in /Users/dc/code/math/jc2
 succeeded in 0ms:
concern is about a multiple cover, not about two physical places, and the
corrected `c`-open already excludes it.  Affine immersivity and reduced
double points remain separate nodality tests.

Already at the second level the raw and reduced equations differ.  On
`h_21=0`, reduction of the degree-22 term gives

```text
bar_h_19 = h_19 + 4*b*(2*B+gamma),
```

in the normalization used here.  Thus `h_19=0` is not an off-by-one
version of the right equation; it is the wrong, unreduced coefficient.

## 4. Extraction and audit of the six generated ideals

The frozen generators are unambiguous (MSOLVE-PREP lines 140--655):

| job | frozen closed equations and open | reported suite state | encoding verdict |
|---|---|---|---|
| `type86_A` | raw `h_21,...,h_13=0`; raw `h_11!=0` | NONEMPTY | **UNFAITHFUL** |
| `type86_B` | raw `h_21,...,h_11=0`; raw `h_9!=0` | NONEMPTY | **UNFAITHFUL** |
| `type86_C` | raw `h_21,...,h_9=0`; raw `h_7!=0` | pending/timeout | **UNFAITHFUL** |
| `type86_D` | raw `h_21,...,h_5=0`; raw `h_3!=0` | pending/timeout | **UNFAITHFUL** |
| `type96_A` | raw `k_16,...,k_5=0`; raw `k_4!=0` | EMPTY in both engines | **UNFAITHFUL** |
| `type96_B` | raw `k_16,...,k_3=0`; raw `k_2!=0` | EMPTY in both engines | **UNFAITHFUL** |

For the four `(8,6)` jobs, the raw odd coefficients are taken before the
even pole-order terms have been reduced.  The charged witness below is an
actual false positive for A.  The same algebra affects every later level;
B's charged sliced points have the same `delta_inf=6` symptom.  In addition,
the B/C/D specs omit the reduced gap-degree-10 equation and D omits the
reduced gap-degree-4 equation.  This is neither a harmless index shift nor a
missing Rabinowitsch inverse.

The omitted equations are independent triangular obstructions, not optional
normalizations.  Once the coefficient of every available semigroup monomial
has been solved at its leading weight, degrees 10 and 4 remain because
`10,4` are gaps of `<8,6>`; no later auxiliary monomial can absorb either
coefficient.  The later raw equations `h_7,h_5,h_3` inspect different
coefficients before that reduction and cannot replace these gap equations.
There is also a small exact counterpoint for C.  Choose `gamma != 0`, put

```text
b=d=e=D=0,       B=4*gamma/3,       E=-B^2,
f=-107*gamma^2/81,  F=-2*B*C,       G=3*C^2/2,
C^3=-1648*gamma^4/10935.
```

Then direct substitution gives `h_21=...=h_9=0` and
`h_7=15*B^3*C^2 != 0`, so this is an old-C characteristic point.  But
`h_20=3C`; after the compulsory cancellation `G86=H-3C*p*q^2+...`, its
degree-17 coefficient is `-3C(B+2gamma)=-10C*gamma != 0`.  Thus even C's
later raw vanishings do not repair the high-level reduction error.

There is an equally explicit D counterpoint.  Over `K=QQ(s)`, `s^2=201`, set

```text
gamma=1, B=4/3, E=(s-5)/27, f=(2s-21)/81,
C=D=F=G=b=d=e=0.
```

With `u=t^3`, one has `p=t^2*(u^2+B*u+E)` and `q=u^2+u+f`, so `H` has only
degrees divisible by 3.  The only closed D equations not automatic by
support are

```text
h_21=3B-4=0,
h_15=B^3-4+6BE-12f=0,
h_9=4E^2-12f^2-4f=0;
```

the last two follow from `2E-3f=11/27` and
`E^2-3f^2-f=0`.  Meanwhile `h_3=-4f^3 != 0`, so the old D open holds.
But `h_22=h_20=0` while
`h_18=3(E+B^2)-(6+4f)=(s-15)/81 != 0`.  Correct reduction therefore sets
`a18=-h_18`; since `[t^15]q^3=3` and no other remaining auxiliary term has
degree 15, `g_15=-3h_18 != 0`.  Thus D too has a witnessed false positive,
independently of its omitted necessary gap residuals at 10 and 4.

The C/D counterpoints certify non-equivalence of the **characteristic base
ideals**.  They are not asserted to survive the separate affine-immersion
open (the displayed D point does not).  A downstream open may discard a
particular false positive, but it cannot turn the wrong base equations into
an exhaustive characteristic encoding or make an EMPTY result kill the
corrected target.

For the two `(9,6)` jobs the defect has the opposite polarity.  Setting every
raw coefficient above `c` to zero forces the *pure binomial slice*
`p^2-q^3` itself to have degree `c`.  The general reduced approximate root
also contains `p*q,q^2,p,q`.  The old locus is therefore sufficient when it
has a point: its forward type implication remains sound.  It is not
necessary and hence is not a faithful exact/exhaustive encoding.  An EMPTY
result proves only

> no point of that pure-binomial slice survives the stated immersion/colon
> opens.

It does **not** prove that the corrected characteristic locus is empty, and
therefore proves no `(9,6,c)` type kill.  This corrects the overclaim in
MSOLVE-PREP 2.3 (lines 131--138).  The resultant inverse and cover colon are
downstream restrictions on the wrong base ideal and cannot repair its
meaning.  The coordinator's warning that NONEMPTY remained provisional
(the frozen integration, lines 89--90) was therefore necessary but not
sufficient: EMPTY also had to remain provisional on encoding faithfulness.

## 5. Exact desk check of the charged type86_A witness

Let `zeta^2-zeta+18474=0`.  Reading the eleven values in the chart order
`(B,C,D,E,F,G,b,gamma,d,e,f)` gives

```text
p=t^8+t^5+(294-zeta)/144*t^3+(150-zeta)/48*t,
q=t^6+t^4+3/4*t^3+t^2-(138+zeta)/192*t+(2*zeta-71)/144.
```

The two roots of the quadratic give coefficient-conjugate curves, not two
places.  Direct substitution, using `zeta^2=zeta-18474`, gives

```text
h_21=h_19=h_17=h_15=h_13=0,
h_11=(355746-27671*zeta)/110592 != 0.
```

For instance `h_19=-9+3D-4e=0`; the irrationality of `zeta` proves the
displayed `h_11` is nonzero.  Thus this is genuinely a point of the old A
locus, not a variable-order accident.

Now reduce rather than merely inspect `H=p^3-q^4`.  Its leading coefficients
at this point are

```text
h_22=-4, h_21=0, h_20=-10, h_19=0.
```

The first two standard monomials have pole degrees 22 and 20, so put

```text
R=H+4*p^2*q+6*p*q^2.
```

Degrees 22, 21 and 20 cancel, whereas

```text
[t^19]R=h_19+4*(2B+gamma)=0+4*(2+3/4)=11.
```

Therefore the reduced second approximate root has degree `c=19`, not 11.
This single calculation exhibits the exact semantic bug.

For an independent local check let `s=t^-1`,

```text
P=s^8 p(s^-1)=1+s^3+D*s^5+F*s^7,
Q=s^6 q(s^-1)=1+s^2+3/4*s^3+s^4+e*s^5+f*s^6,
x=q/p=s^2 Q/P,                 y=1/p=s^8/P.
```

Expansion only through the deciding order gives

```text
x=s^2+s^4-1/4*s^5+s^6+(e-1-D)*s^7+O(s^8),
y=s^8-s^11-D*s^13+O(s^14).
```

The analytic Tschirnhausen coordinate

```text
y_star=y-x^4+4*x^5-10*x^6
```

satisfies

```text
y_star=(2+3D-4e)*s^13+O(s^14)=11*s^13+O(s^14).
```

After replacing `s` by the unique uniformizer with `x=r^2`, the branch is
`(x,y_star)=(r^2,11r^13+...)`.  It is irreducible with one Puiseux pair
`(2,13)`: `beta_1=13`, `delta_inf=(2-1)(13-1)/2=6`, and
`Delta=(8,6,32-13)=(8,6,19)`.

Properness is also desk-visible.  A nontrivial common right component would
have degree two.  Write a normalized quadratic as `phi=t^2+a*t+c`.  The
missing `t^7` coefficient of `p=P(phi)` forces `a=0`, after which `P(phi)`
is even, contradicting the nonzero `t^5` coefficient of `p`.  Hence the map
is birational.  Only `t=infinity` maps to the infinity line, so there is one
physical place.  The `+/-r` series are conjugates of that branch.

Conditioning on the charged machine statement of fifteen ordinary affine
nodes, the genus check is exact:

```text
p_a(degree 8)=21=delta_aff 15+delta_inf 6.
```

The frozen witness file itself is only a coefficient payload: line 6 says
`Nexpected=3` (neither 11 nor 15) and it contains no postcheck output.
Accordingly this audit verifies the infinity/genus split, but does not claim
that those eight lines alone replay the reduced degree-30 ordered scheme.


exec
/bin/zsh -lc "rg -n -C 8 'PREFLIGHT|encoding faithfulness|artifact.*attainment|transition table' xmodel/*.md -g '"'!systems-collision-preflight-sol56-20260902.md'"' -g '"'!systems-collision-preflight-sol56-20260902.prompt.md'"' | head -300" in /Users/dc/code/math/jc2
 succeeded in 0ms:
xmodel/a2-dev-projection-sol56-20260902.prompt.md-38-    (OPEN[A2-K2E]) and Chamber I as separate declared branches; never
xmodel/a2-dev-projection-sol56-20260902.prompt.md-39-    impose the retracted wall.
xmodel/a2-dev-projection-sol56-20260902.prompt.md-40-(3) T4 REPAIR ATTEMPT. C32 T4 (deg eta = 0 impossible on the live section)
xmodel/a2-dev-projection-sol56-20260902.prompt.md-41-    is OPEN because its proof used the old EQ2_even. Re-run the e = 0
xmodel/a2-dev-projection-sol56-20260902.prompt.md-42-    section on the corrected system (it is a finite computation) and
xmodel/a2-dev-projection-sol56-20260902.prompt.md-43-    either restore T4 with a corrected proof or exhibit the surviving
xmodel/a2-dev-projection-sol56-20260902.prompt.md-44-    locus.
xmodel/a2-dev-projection-sol56-20260902.prompt.md-45-(4) STATE LADDER. Any nonempty locus is a SUBSYSTEM_POINT at most; label
xmodel/a2-dev-projection-sol56-20260902.prompt.md:46:    by the two-field typing (artifact / attainment) of the charged Sol
xmodel/a2-dev-projection-sol56-20260902.prompt.md-47-    submission; do not call anything a curve, a map, or a (B3) object.
xmodel/a2-dev-projection-sol56-20260902.prompt.md-48-    Reconstruction of (r, q, p) from (X, Y, s) requires the three exact
xmodel/a2-dev-projection-sol56-20260902.prompt.md-49-    divisibilities eta^2 | (s^2 + X), eta | (3a s^2 + Y),
xmodel/a2-dev-projection-sol56-20260902.prompt.md-50-    s | (q r' - kappa/2) — check them pointwise if a locus appears.
xmodel/a2-dev-projection-sol56-20260902.prompt.md-51-Engines: qqideal 0.2.0 + msolveio 0.2.1 (msolve 0.10.1) as the default,
xmodel/a2-dev-projection-sol56-20260902.prompt.md-52-sympy as the second engine for anything called EMPTY; explicit
xmodel/a2-dev-projection-sol56-20260902.prompt.md-53-Rabinowitsch variables, no sat() wrappers; positive control = a planted
xmodel/a2-dev-projection-sol56-20260902.prompt.md-54-point on the corrected system. Desk-scale (< 15 min, < 4 GB per job);
--
xmodel/as109-sextic-survivor-review-grok-20260824.md-106-A polynomial instantiation with nonconstant `h,r,A,B,C` and the formulas (3.2) reproduced
xmodel/as109-sextic-survivor-review-grok-20260824.md-107-
xmodel/as109-sextic-survivor-review-grok-20260824.md-108-```text
xmodel/as109-sextic-survivor-review-grok-20260824.md-109-J_(x,y) = h (I_2' z^2 + I_1' z + E0)
xmodel/as109-sextic-survivor-review-grok-20260824.md-110-```
xmodel/as109-sextic-survivor-review-grok-20260824.md-111-
xmodel/as109-sextic-survivor-review-grok-20260824.md-112-identically, with `z=hy+r`. On the control (7.11) with `t=x`, `h=1`, one has `I_2=I_1=0` and `J` equal to the displayed factor (7.12), a nonconstant polynomial of degree 11, hence not a Keller pair. Both boundary polynomials matched `f(x,0)` and `g(x,0)-alpha D_0-epsilon`.
xmodel/as109-sextic-survivor-review-grok-20260824.md-113-
xmodel/as109-sextic-survivor-review-grok-20260824.md:114:The parent preflight replay was rerun only as integrity control (`verdict = SEXTIC-Y-PREFLIGHT-SURVIVING-PFAFFIAN-GATE`, `lift_found=false`, `jc2_inference=false`). Its `(5,6)` content is not used.
xmodel/as109-sextic-survivor-review-grok-20260824.md-115-
xmodel/as109-sextic-survivor-review-grok-20260824.md-116----
xmodel/as109-sextic-survivor-review-grok-20260824.md-117-
xmodel/as109-sextic-survivor-review-grok-20260824.md-118-## Claim 1 — common depressed normal form
xmodel/as109-sextic-survivor-review-grok-20260824.md-119-
xmodel/as109-sextic-survivor-review-grok-20260824.md-120-**CONFIRMED.**
xmodel/as109-sextic-survivor-review-grok-20260824.md-121-
xmodel/as109-sextic-survivor-review-grok-20260824.md-122-For a genuine pair of actual `y`-degrees `(4,6)`, the top Jacobian row is `6 a_4' b_6 - 4 a_4 b_6' = 0`. Unique factorization in `Kbar[x]` and constant target scaling give `a_4=H^2`, `b_6=H^3` with `H in Kbar[x]`, `H!=0`. Write `N=3 a_3 H - 2 b_5`. The next row expands as an identity of derivations
--
xmodel/as109-sextic-preflight-review-grok-20260824.md:1:# Hostile different-model review — AS109 SEXTIC FRONTIER PREFLIGHT
xmodel/as109-sextic-preflight-review-grok-20260824.md-2-
xmodel/as109-sextic-preflight-review-grok-20260824.md-3-| Field | Value |
xmodel/as109-sextic-preflight-review-grok-20260824.md-4-|---|---|
xmodel/as109-sextic-preflight-review-grok-20260824.md-5-| Claim under review | Characteristic-zero preflight: after target `GL_2` and divisible-degree shears, the only new actual `y`-degree pairs with maximum degree six are the imprimitive `(4,6)` split and the coprime `(5,6)` Pfaffian; neither is closed here; no `y`-degree-`<=6` theorem, no raised AS109 floor, no lift, no JC2 inference |
xmodel/as109-sextic-preflight-review-grok-20260824.md-6-| Overall verdict | **CONFIRMED** |
xmodel/as109-sextic-preflight-review-grok-20260824.md-7-| Smallest missing hypothesis | none that breaks a numbered claim (non-blocking precisions: algebraic closure is used to write `H=h^2` on the `(4,6)` nonzero-`kappa` branch; a polynomial primitive of `omega` modulo `dI_3` exists and is recorded below as successor input, not as a producer theorem) |
xmodel/as109-sextic-preflight-review-grok-20260824.md-8-| Evidence tier | independent exact expansion over a generic characteristic-zero coefficient ring (hand Jacobian by `y`-degree for the pair sweep and the `(4,6)` `[y^8]` row, Wronskian / logarithmic derivative in `K(x)`, UFD valuations in `K[x]` and `Kbar[x]`, chain-rule Jacobian of the depressed `(5,6)` form, direct differentiation of `I_3,I_2`, exterior derivative of `omega`); a second sparse engine over `Q` with `Fraction` coefficients that does not import the producer replay; unmodified rerun of `verify_sextic_preflight.py` as regression control; confirmed quartic theorem consumed only as scoped input for pairs with both `y`-degrees `<=4`; quintic work recorded as independently landed and **not consumed** |
xmodel/as109-sextic-preflight-review-grok-20260824.md-9-| Reviewer / model | Grok 4.6 (xAI). Different model family from the producer (OpenAI Codex, GPT-5 family) |
--
xmodel/as109-sextic-preflight-review-grok-20260824.md-25-The committed basis is exactly `51aa1cc210b20d6c57c5bb0ba3b4a6dfa5fc8f54`. Named producer artifacts remain uncommitted on top of that basis. No producer, canonical, ledger, freeze, or case file was edited. No enumerator was written or run. The `(4,6)` lower rows and a monic `(5,6)` eliminant were not pursued. No AWS call was made.
xmodel/as109-sextic-preflight-review-grok-20260824.md-26-
xmodel/as109-sextic-preflight-review-grok-20260824.md-27-Write `K` for an arbitrary characteristic-zero field, `Kbar` for an algebraic closure, `J(f,g)=f_x g_y-f_y g_x`, and `p=109`. Actual `y`-degrees are used throughout.
xmodel/as109-sextic-preflight-review-grok-20260824.md-28-
xmodel/as109-sextic-preflight-review-grok-20260824.md-29----
xmodel/as109-sextic-preflight-review-grok-20260824.md-30-
xmodel/as109-sextic-preflight-review-grok-20260824.md-31-## Promotion
xmodel/as109-sextic-preflight-review-grok-20260824.md-32-
xmodel/as109-sextic-preflight-review-grok-20260824.md:33:**Accept `SEXTIC-Y-PREFLIGHT-SURVIVING-PFAFFIAN-GATE` at the stated scope.**
xmodel/as109-sextic-preflight-review-grok-20260824.md-34-
xmodel/as109-sextic-preflight-review-grok-20260824.md-35-- After constant target operations and divisible-degree shears, the only new actual pairs with maximum `y`-degree six are `(4,6)` and `(5,6)`. Both remain. Neither branch of `(4,6)` is empty. The coprime pair `(5,6)` reduces to the displayed Pfaffian (7.4) and does not presently yield a monic eliminant or a polynomiality theorem.
xmodel/as109-sextic-preflight-review-grok-20260824.md-36-- The confirmed quartic conclusion is unchanged: an exact AS109 lift must have correction `y`-degree at least five in one coordinate. This file does **not** raise that floor to six or seven.
xmodel/as109-sextic-preflight-review-grok-20260824.md-37-
xmodel/as109-sextic-preflight-review-grok-20260824.md-38-**Do not promote this to:** a `y`-degree-`<=6` field theorem; a strengthened AS109 exclusion; nonexistence or existence of an arbitrary finite-support AS109 lift; a found lift; a characteristic-zero point; a JC2 counterexample or disproof; `HEIGHT-CERT`; cap widening; an exponent rectangle; a finite Witt-level inference; or a novelty / priority claim.
xmodel/as109-sextic-preflight-review-grok-20260824.md-39-
xmodel/as109-sextic-preflight-review-grok-20260824.md-40-**Do not consume the independently landed quintic theorem here.** Even if a later coordinator consumes that `CONFIRMED` quintic review, the two sextic patterns below still block a degree-`<=6` theorem. Divisible shears `(1,6),(2,6),(3,6)` would then reduce into the quintic range, but that reduction is not used in this preflight and is not used in this review.
xmodel/as109-sextic-preflight-review-grok-20260824.md-41-
--
xmodel/as109-sextic-preflight-review-grok-20260824.md-43-
xmodel/as109-sextic-preflight-review-grok-20260824.md-44-- `(4,6)`: a lower-row compiler for both branches of (3.4) — the `kappa=0` common quadratic generator, without treating top-coefficient compatibility as a full normal form, and the `kappa!=0` constant depression mismatch `lambda/12`, which is not target-sheared away.
xmodel/as109-sextic-preflight-review-grok-20260824.md-45-- `(5,6)`: start from the polynomial primitive `I_1` recorded in the successor-input section below (`dI_1=omega-(2A/5)\,dI_3`), together with a weighted-projective pole analysis of the remaining `h\,eta(X')=jbar` equation and both polynomial boundary identities (8.1). Do not search blindly for an ambient integrating factor of `omega`. Do not treat the rational trajectory (8.3) as a Keller pair.
xmodel/as109-sextic-preflight-review-grok-20260824.md-46-
xmodel/as109-sextic-preflight-review-grok-20260824.md-47----
xmodel/as109-sextic-preflight-review-grok-20260824.md-48-
xmodel/as109-sextic-preflight-review-grok-20260824.md-49-## Quarantine
xmodel/as109-sextic-preflight-review-grok-20260824.md-50-
xmodel/as109-sextic-preflight-review-grok-20260824.md:51:No result here proves or disproves JC2. Producer JSON strings `SEXTIC-Y-PREFLIGHT-SURVIVING-PFAFFIAN-GATE` and `ODE-ALONE-DOES-NOT-CLOSE` were not used as evidence; the identities below were re-derived. The quartic parent is recorded as landed `CONFIRMED` and is consumed only in its stated `y`-degree-`<=4` scope. The quintic parent/review are recorded as independently `CONFIRMED` and are **not** used to close any sextic pair, to raise the AS109 floor, or to infer that divisible sextic shears are automorphisms. Finite-support search, Hensel, and Witt data are unused. Priority is quarantined from the mathematical verdict.
xmodel/as109-sextic-preflight-review-grok-20260824.md-52-
xmodel/as109-sextic-preflight-review-grok-20260824.md-53-The rational path (8.3) is a negative control: it solves the Pfaffian ODE with a nonzero last row and fails the second polynomial boundary. It is not a lift and is not evidence that a lift exists.
xmodel/as109-sextic-preflight-review-grok-20260824.md-54-
xmodel/as109-sextic-preflight-review-grok-20260824.md-55----
xmodel/as109-sextic-preflight-review-grok-20260824.md-56-
xmodel/as109-sextic-preflight-review-grok-20260824.md-57-## Scope (not enlarged)
xmodel/as109-sextic-preflight-review-grok-20260824.md-58-
xmodel/as109-sextic-preflight-review-grok-20260824.md-59-One prime `p=109`, one seed `(x-x^{109},y)`, and a characteristic-zero field calculation over `K` (temporarily over `Kbar`). The object of the file is a preflight classification and an exact `(5,6)` normal form, not a theorem that every sextic-`y` Keller pair is an automorphism. Arbitrary finite `x`-degree is in scope *inside* the displayed identities. Target `GL_2`, translations, and polynomial target shears are automorphy tests over `K` (or `Kbar`); they need not preserve the integral AS109 seed chart. Quintic identities, generic exponent search, exponent rectangles, finite-Witt inference, and AWS are out of scope.
--
xmodel/as109-sextic-preflight-review-grok-20260824.md-96-
xmodel/as109-sextic-preflight-review-grok-20260824.md-97-```sh
xmodel/as109-sextic-preflight-review-grok-20260824.md-98-python3 cases/as109_sextic_frontier_preflight_20260824/verify_sextic_preflight.py
xmodel/as109-sextic-preflight-review-grok-20260824.md-99-```
xmodel/as109-sextic-preflight-review-grok-20260824.md-100-
xmodel/as109-sextic-preflight-review-grok-20260824.md-101-Exit code 0. Top-level fields:
xmodel/as109-sextic-preflight-review-grok-20260824.md-102-
xmodel/as109-sextic-preflight-review-grok-20260824.md-103-```text
xmodel/as109-sextic-preflight-review-grok-20260824.md:104:verdict = SEXTIC-Y-PREFLIGHT-SURVIVING-PFAFFIAN-GATE
xmodel/as109-sextic-preflight-review-grok-20260824.md-105-full_sextic_theorem_proved = false
xmodel/as109-sextic-preflight-review-grok-20260824.md-106-enumeration_run = false
xmodel/as109-sextic-preflight-review-grok-20260824.md-107-lift_found = false
xmodel/as109-sextic-preflight-review-grok-20260824.md-108-jc2_inference = false
xmodel/as109-sextic-preflight-review-grok-20260824.md-109-field_characteristic = 0
xmodel/as109-sextic-preflight-review-grok-20260824.md-110-```
xmodel/as109-sextic-preflight-review-grok-20260824.md-111-
xmodel/as109-sextic-preflight-review-grok-20260824.md-112-The replay is a finite exact regression control (formal differential algebra on named coefficients, plus integer bivariate Jacobians). The coefficient derivations below are the proofs for arbitrary `x`-degree.
--
xmodel/as109-sextic-frontier-preflight-20260824.md-401-J(x+y^5,y+y^6)=1+6y^5
xmodel/as109-sextic-frontier-preflight-20260824.md-402-```
xmodel/as109-sextic-frontier-preflight-20260824.md-403-
xmodel/as109-sextic-frontier-preflight-20260824.md-404-shows that a superficial `(5,6)` shape is not enough.
xmodel/as109-sextic-frontier-preflight-20260824.md-405-
xmodel/as109-sextic-frontier-preflight-20260824.md-406-Expected top-level output is
xmodel/as109-sextic-frontier-preflight-20260824.md-407-
xmodel/as109-sextic-frontier-preflight-20260824.md-408-```text
xmodel/as109-sextic-frontier-preflight-20260824.md:409:verdict = SEXTIC-Y-PREFLIGHT-SURVIVING-PFAFFIAN-GATE
xmodel/as109-sextic-frontier-preflight-20260824.md-410-full_sextic_theorem_proved = false
xmodel/as109-sextic-frontier-preflight-20260824.md-411-enumeration_run = false
xmodel/as109-sextic-frontier-preflight-20260824.md-412-lift_found = false
xmodel/as109-sextic-frontier-preflight-20260824.md-413-jc2_inference = false
xmodel/as109-sextic-frontier-preflight-20260824.md-414-```
xmodel/as109-sextic-frontier-preflight-20260824.md-415-
xmodel/as109-sextic-frontier-preflight-20260824.md-416-No finite formal support in the replay is used as a search cap.
xmodel/as109-sextic-frontier-preflight-20260824.md-417-
--
xmodel/as109-sextic-preflight-review-grok-20260824-prompt.md:1:# Hostile different-model review — AS109 SEXTIC FRONTIER PREFLIGHT
xmodel/as109-sextic-preflight-review-grok-20260824-prompt.md-2-
xmodel/as109-sextic-preflight-review-grok-20260824-prompt.md-3-You are the independent different-model reviewer. Work in
xmodel/as109-sextic-preflight-review-grok-20260824-prompt.md-4-`/Users/dc/code/math/jc2` at committed basis
xmodel/as109-sextic-preflight-review-grok-20260824-prompt.md-5-`51aa1cc210b20d6c57c5bb0ba3b4a6dfa5fc8f54`, with frozen uncommitted
xmodel/as109-sextic-preflight-review-grok-20260824-prompt.md-6-producer/review artifacts on top.
xmodel/as109-sextic-preflight-review-grok-20260824-prompt.md-7-
xmodel/as109-sextic-preflight-review-grok-20260824-prompt.md-8-Read in full:
xmodel/as109-sextic-preflight-review-grok-20260824-prompt.md-9-
--
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-1-# D43 common-integral emitter: bounded preflight and reducer-free source lane
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-2-
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-3-**Date:** 2026-08-28  
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-4-**Producer:** GPT-5.6  
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-5-**Scope:** residue-A, `a00pp`, B-frozen no-log `W1*W2 != 0` chart  
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md:6:**Status:** **COEFFICIENT/SPECIALIZATION PREFLIGHT IMPLEMENTED; COMMON 34+184 EMISSION STILL OPEN**
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-7-
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-8-## Executive verdict
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-9-
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-10-There are two different goals which the current discussion had conflated.
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-11-
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-12-1. A **source-defined characteristic-zero or `Z_p` point** does not logically
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-13-   require an integral lift of the 509-element D23 reduced Groebner basis.
xmodel/d43-common-integral-emitter-preflight-gpt56-20260828.md-14-   In principle it is enough to emit, in one explicit exact source ambient,
--
xmodel/corrected-863-kill-review-gpt55-20260901.md-275-```
xmodel/corrected-863-kill-review-gpt55-20260901.md-276-
xmodel/corrected-863-kill-review-gpt55-20260901.md-277-So `coefficient(t^j, G86poly)` extracts the coefficient of the monomial `t^j`
xmodel/corrected-863-kill-review-gpt55-20260901.md-278-and returns an element of `R0`, with all `a` variables and chart variables in
xmodel/corrected-863-kill-review-gpt55-20260901.md-279-the base.  That is exactly what the audit specification means by `[t^j]G86`.
xmodel/corrected-863-kill-review-gpt55-20260901.md-280-
xmodel/corrected-863-kill-review-gpt55-20260901.md-281-I did not have a local `M2` executable on PATH, so I could not rerun a live M2
xmodel/corrected-863-kill-review-gpt55-20260901.md-282-probe in this review environment.  The as-run M2 result is consumed from the
xmodel/corrected-863-kill-review-gpt55-20260901.md:283:charge (`rc=0`, char 0, `gb=|1|`).  For encoding faithfulness, the static check
xmodel/corrected-863-kill-review-gpt55-20260901.md-284-is decisive because the patched line uses the documented signature and the
xmodel/corrected-863-kill-review-gpt55-20260901.md-285-same-ring condition is enforced by the file structure and asserts.
xmodel/corrected-863-kill-review-gpt55-20260901.md-286-
xmodel/corrected-863-kill-review-gpt55-20260901.md-287-I see no silent-zero hazard in the patched extraction:
xmodel/corrected-863-kill-review-gpt55-20260901.md-288-
xmodel/corrected-863-kill-review-gpt55-20260901.md-289-1. Wrong-ring monomial: no.  `use St` precedes construction of `p`, `q`,
xmodel/corrected-863-kill-review-gpt55-20260901.md-290-   `G86poly`, and `closedGens`; the symbol `t` is the generator of `St`.
xmodel/corrected-863-kill-review-gpt55-20260901.md-291-   `t^j` and `G86poly` are in the same ring.
--
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-271-> opens.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-272-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-273-It does **not** prove that the corrected characteristic locus is empty, and
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-274-therefore proves no `(9,6,c)` type kill.  This corrects the overclaim in
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-275-MSOLVE-PREP 2.3 (lines 131--138).  The resultant inverse and cover colon are
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-276-downstream restrictions on the wrong base ideal and cannot repair its
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-277-meaning.  The coordinator's warning that NONEMPTY remained provisional
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-278-(the frozen integration, lines 89--90) was therefore necessary but not
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:279:sufficient: EMPTY also had to remain provisional on encoding faithfulness.
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-280-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-281-## 5. Exact desk check of the charged type86_A witness
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-282-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-283-Let `zeta^2-zeta+18474=0`.  Reading the eleven values in the chart order
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-284-`(B,C,D,E,F,G,b,gamma,d,e,f)` gives
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-285-
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-286-```text
xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md-287-p=t^8+t^5+(294-zeta)/144*t^3+(150-zeta)/48*t,
--
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-69-| Nonlinear rational-termination on the literal nodal chart | Sol blind Card B | **REFUTED CLIENT (a fortiori)** | Dead for a reason more elementary than the note's: §5.1. |
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-70-| Pair-square off-diagonal event module | Sol-alt Card 1 | **NEW APPLICATION, first discriminator sharpened** | `Z=(X×_U X)∖Δ` depends only on the étale cover, i.e. on the monodromy group and boundary — it cannot see `J=1` (§5.2.1). Target is numerically `d ≥ s+Σb_i`. |
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-71-| `ATYP-COUNT` via asymptotic critical values | Fable §3.1 / Card 3 | **NEW mechanism, TARGET FALSIFIED as stated** | `f = x + x²y + x³y²` is primitive, has **no critical points**, `s=3`, `#atypical=1 < s−1` (§5.2.2). Stage A is answered before it was run. |
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-72-| Intermediate block surface `B_K` | Sol-alt Card 2 | **KNOWN direction, SHARPENED here** | `B_K ⊆ C[x,y]` and finiteness over `C[f,g]` both CONFIRMED. The `A²` theorem is the wrong target; the right one is a codim-1 image criterion (§5.3). |
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-73-| Collision-partner Galois descent | Opus blind Card C | **LOWER (self)** | The base field is algebraically closed; §5.2.3 states the only surviving typed form and I do not launch it. |
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-74-| Tougeron layer on the `e=3` fixture | Fable Card 1 | **POST-EVENT DEFER** | The branch it was to decide is dead at G9. Reactivation condition stated verbatim at §4.5. |
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-75-| Avenue 33 period instrument | Fable §3.3 | **SELF-REFUTED, RECORDED** | Agreed; do not re-derive. |
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-76-| Avenue 27 PALF sub-lane | Opus / Sol-alt lower; Fable unchanged | **DISAGREEMENT, resolved to LOWER** | The A2-filling category is empty for a submersion; that is a scope fact, not a rank preference. |
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md:77:| `ADAPTER-PREFLIGHT` checkpoint clause | Opus blind §9 | **REFUTED (self)** | A runner cannot force checkpointing inside one monolithic provider response; the packet says so and it is right (§6.2). |
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-78-| Systems: preflight / cap-retry / sectioning / claim-lint | Opus / Sol / Fable / Sol-alt | **SYSTEMS FAMILY — one only** | Selected: sectioned *independent* calls (§6.2). |
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-79-
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-80----
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-81-
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-82-## 2. Typed verdicts
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-83-
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-84-**V1 — `J^17 ⊆ I`: `CONFIRMED` (conditional on the four reviewed fifth-power
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-85-certificates).** Proof audited: any generator of `J^17` is a monomial
--
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-717-verdict differs, the compiler is wrong and is discarded, not tuned.
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-718-
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-719-### 6.2 Exactly one campaign-systems trial
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-720-
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-721-**Selected: `SECTIONED-INDEPENDENT-CALLS/v1`** (Fable's card, corrected).
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-722-
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-723-Comparison, with my own blind card judged first:
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-724-
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md:725:- *My `ADAPTER-PREFLIGHT` checkpoint clause is **refuted**.* I proposed that
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-726-  each lane "checkpoint at 80% of its declared budget". A runner cannot force
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-727-  a model to checkpoint inside one monolithic provider response; the packet
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-728-  says so and it is correct. That clause is withdrawn.
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-729-- *Provider preflight: `NO_CHANGE`.* It cannot restore an exhausted balance,
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-730-  and the 402 lane was already typed correctly and cost no verdict. Fable's
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-731-  evidence for this is right and my blind card overvalued it.
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-732-- *One-shot cap retry:* pays full cost again with no guarantee, and cannot
xmodel/ideation-20260829T2254Z-crosspoll-opus5.md-733-  produce partial value from the first attempt.
--
xmodel/ideation-20260830T1015Z-fable5.md-358-`dcfbe164178ea93e85c608526edb588ea96aaf6d6488c3865660c708b79f3da7` and the
xmodel/ideation-20260830T1015Z-fable5.md-359-zeros of Section 5.3. Decisive experiment: the full q=6 allocation closure
xmodel/ideation-20260830T1015Z-fable5.md-360-(all multi-root/`Z` combinations) — projected well under a minute of desk
xmodel/ideation-20260830T1015Z-fable5.md-361-time. The same kernel then serves nonreduced-infinity and higher-block
xmodel/ideation-20260830T1015Z-fable5.md-362-strata, and replaces per-packet ad hoc replays.
xmodel/ideation-20260830T1015Z-fable5.md-363-
xmodel/ideation-20260830T1015Z-fable5.md-364-## 8. Campaign-systems `UPGRADE` card
xmodel/ideation-20260830T1015Z-fable5.md-365-
xmodel/ideation-20260830T1015Z-fable5.md:366:`PROVIDER-CAPACITY-PREFLIGHT/v1`. Evidence of need: repeated full launch
xmodel/ideation-20260830T1015Z-fable5.md-367-choreographies (immutable prompt freeze, custody, receipts) consumed by
xmodel/ideation-20260830T1015Z-fable5.md-368-HTTP 402/spend-ceiling refusals before any model work — Grok twice, Fable
xmodel/ideation-20260830T1015Z-fable5.md-369-and Opus at the 05:32Z review attempt, and again per state packet §1.5.
xmodel/ideation-20260830T1015Z-fable5.md-370-Change: a capped no-op capacity probe per provider before prompt freeze;
xmodel/ideation-20260830T1015Z-fable5.md-371-on refusal, the lane is recorded `UNAVAILABLE_NO_MODEL_WORK` without
xmodel/ideation-20260830T1015Z-fable5.md-372-creating a prompt artifact or consuming the packet's launch slot.
xmodel/ideation-20260830T1015Z-fable5.md-373-Smallest measurable acceptance test: mocked 402/429/200 endpoints —
xmodel/ideation-20260830T1015Z-fable5.md-374-(i) 402/429 produce a typed `UNAVAILABLE_NO_MODEL_WORK` receipt, zero new
--
xmodel/ideation-20260902T0022Z-synthesis.md-142-  OPEN[SOURCE-IS-C2] superseded by OPEN[SOURCE-OPEN-U] (charged
xmodel/ideation-20260902T0022Z-synthesis.md-143-  to SOURCE-GATE lane); OPEN[NA-R1-SHARPNESS-IRREDUCIBLE]
xmodel/ideation-20260902T0022Z-synthesis.md-144-  charged (Card II lane); OPEN[SHAPE-2-INNER-g>=3] RETIRED
xmodel/ideation-20260902T0022Z-synthesis.md-145-  (closed by CABLE-3); OPEN[BMFACT-STRAND-VS-BLOCK] charged
xmodel/ideation-20260902T0022Z-synthesis.md-146-  (SAGE-NATIVE running + SOURCE-GATE consumes cycle types only);
xmodel/ideation-20260902T0022Z-synthesis.md-147-  OPEN[A2-CELL-32] already owned (horn lane).
xmodel/ideation-20260902T0022Z-synthesis.md-148-- ONE TRACKED UPGRADE (grok §7, chosen over fable's
xmodel/ideation-20260902T0022Z-synthesis.md-149-  FAIL-CLOSED-LINT and sol's SNAPSHOT-READSET as
xmodel/ideation-20260902T0022Z-synthesis.md:150:  highest-value/lowest-risk): encoding-faithfulness PREFLIGHT as
xmodel/ideation-20260902T0022Z-synthesis.md-151-  a required gate on every nodal-type Groebner job — emit the
xmodel/ideation-20260902T0022Z-synthesis.md-152-  reduced approximate root beside the .ms, diff against the raw
xmodel/ideation-20260902T0022Z-synthesis.md-153-  binomial, refuse on nonempty diff; positive control = (9,6,2)
xmodel/ideation-20260902T0022Z-synthesis.md-154-  corrected, negative control = the Delta=(8,6,19) false
xmodel/ideation-20260902T0022Z-synthesis.md-155-  positive. Queued to the qqideal oracle harvest (P1 disagreement
xmodel/ideation-20260902T0022Z-synthesis.md-156-  machinery already exists). FAIL-CLOSED-LINT and
xmodel/ideation-20260902T0022Z-synthesis.md-157-  SNAPSHOT-READSET are RECORDED as next-window candidates, not
xmodel/ideation-20260902T0022Z-synthesis.md-158-  lost; sol's canary test design is banked in its submission.
--
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-1-# K00 old-plane valuation-two zero-odd-correction exclusion — binding integration
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-2-
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-3-Date: 2026-08-29 UTC  
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-4-Coordinator: Sol 5.6  
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-5-Frozen basis: `93db679d3160c957b0610297afccc1f2fad53125`  
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md:6:Lifecycle: `PROMOTED_EXACT_SUBLANE_EXCLUSION / PREFLIGHT_REJECTED`
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-7-
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-8-## 0. Binding disposition
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-9-
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-10-Opus 5 independently rebuilt all seven V20R2 rows from the frozen 569 tails
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-11-and returned `PASS_WITH_INDEXING_REPAIRS` on the producer's grade-eight
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-12-exclusion.  The promoted statement is:
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-13-
xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md-14-> In the normalized V20R2 source over a characteristic-zero field, no
--
xmodel/ideation-20260902T0741Z-fable51.raw.md-276-realize `A_F = D_1 ∪ D_2` with the CAGE-N-R2 profile, run SIROCCO -> ZvK ->
xmodel/ideation-20260902T0741Z-fable51.raw.md-277-`S_6` with the fixed-sheet constraint of §5.3 imposed per component
xmodel/ideation-20260902T0741Z-fable51.raw.md-278-(`a^{(i)}` fixed sheets of each `Loc_p` for `p ∈ D_i`): the constraint is
xmodel/ideation-20260902T0741Z-fable51.raw.md-279-free to impose and is expected to cut the representation space by an order
xmodel/ideation-20260902T0741Z-fable51.raw.md-280-of magnitude before any curve-level work.
xmodel/ideation-20260902T0741Z-fable51.raw.md-281-
xmodel/ideation-20260902T0741Z-fable51.raw.md-282-## 7. Software acceleration / decisive experiment
xmodel/ideation-20260902T0741Z-fable51.raw.md-283-
xmodel/ideation-20260902T0741Z-fable51.raw.md:284:**PROFILE-PREFLIGHT (decisive, one afternoon).** For any analytic model the
xmodel/ideation-20260902T0741Z-fable51.raw.md-285-campaign builds (the A2 model today, a (B3) model tomorrow), compute
xmodel/ideation-20260902T0741Z-fable51.raw.md-286-mechanically before any Gröbner job: (1) `A_iota` of the declared first map
xmodel/ideation-20260902T0741Z-fable51.raw.md-287-(limit test along the escaping family; for the A2 model the 3-line check of
xmodel/ideation-20260902T0741Z-fable51.raw.md-288-§1.2 item 1); (2) `pi(A_iota)` and the immersivity of its parametrization
xmodel/ideation-20260902T0741Z-fable51.raw.md-289-(gcd of the two derivative polynomials); (3) the THEOREM PROFILE case this
xmodel/ideation-20260902T0741Z-fable51.raw.md-290-forces under H2. Emit `PROFILE = (0)|(A)|(B1)|(B2)|(B3)|REDUCIBLE` beside the
xmodel/ideation-20260902T0741Z-fable51.raw.md-291-`.ms` file and REFUSE the job when the profile is already EMPTY at the
xmodel/ideation-20260902T0741Z-fable51.raw.md-292-target N. Positive control: the A2 model must tag `(0)/(B1)`; negative
--
xmodel/ideation-20260902T0741Z-fable51.raw.md-390-
xmodel/ideation-20260902T0741Z-fable51.raw.md-391-```text
xmodel/ideation-20260902T0741Z-fable51.raw.md-392-PROVED-HERE/UNREVIEWED   CUSP-A-ALL-N   (Sec 1.1)   case (A) EMPTY, every N
xmodel/ideation-20260902T0741Z-fable51.raw.md-393-PROVED-HERE/UNREVIEWED   PHI-IMMERSION  (Sec 1.2)   A2 model => A_F immersed;
xmodel/ideation-20260902T0741Z-fable51.raw.md-394-                                                     under H2 profile (0)/(B1);
xmodel/ideation-20260902T0741Z-fable51.raw.md-395-                                                     EMPTY N<=16 by MPRIME
xmodel/ideation-20260902T0741Z-fable51.raw.md-396-DERIVED                  #cusps(E) = a_{p_0};  E_1 ⊔ E_2 in case (A) a=2;
xmodel/ideation-20260902T0741Z-fable51.raw.md-397-                         (B3) N=4 branch points over double points only
xmodel/ideation-20260902T0741Z-fable51.raw.md:398:PROPOSED                 LOCAL-ISO TRANSPORT as a rule; PROFILE-PREFLIGHT;
xmodel/ideation-20260902T0741Z-fable51.raw.md-399-                         OBJECT-LEDGER; Cards A-C; Magnus-on-S lens
xmodel/ideation-20260902T0741Z-fable51.raw.md-400-NOT CLAIMED              any all-degree closure; any (B3) kill; anything about
xmodel/ideation-20260902T0741Z-fable51.raw.md-401-                         the reducible branch beyond a prefilter; N=4 re-hardening
xmodel/ideation-20260902T0741Z-fable51.raw.md-402-```
xmodel/ideation-20260902T0741Z-fable51.raw.md-403-
xmodel/ideation-20260902T0741Z-fable51.raw.md-404-Timing: sealed at completion, within the 75-minute window; not DEGRADED.
xmodel/ideation-20260902T0741Z-fable51.raw.md-405-Size deviation: body about 26KB against the 15-25KB target; logged, not trimmed (Sec 1 proofs are the deliverable).

exec
/bin/zsh -lc "rg -n -C 12 'PREFLIGHT|approximate root|false-positive|86A|96B|source-fidelity' xmodel/ideation-20260902T0022Z-sol56.md xmodel/ideation-20260902T0022Z-grok46.md xmodel/ideation-20260902T0022Z-*.md 2>/dev/null | head -500" in /Users/dc/code/math/jc2
 succeeded in 0ms:
xmodel/ideation-20260902T0022Z-synthesis.md-138-
xmodel/ideation-20260902T0022Z-synthesis.md-139-- ADOPTED FREE (opus §8, instrument delivered): OPEN-token triage
xmodel/ideation-20260902T0022Z-synthesis.md-140-  at every round close. MEASURED now: 74 raised / 20 charged /
xmodel/ideation-20260902T0022Z-synthesis.md-141-  54 orphaned. This synthesis charges or retires five orphans:
xmodel/ideation-20260902T0022Z-synthesis.md-142-  OPEN[SOURCE-IS-C2] superseded by OPEN[SOURCE-OPEN-U] (charged
xmodel/ideation-20260902T0022Z-synthesis.md-143-  to SOURCE-GATE lane); OPEN[NA-R1-SHARPNESS-IRREDUCIBLE]
xmodel/ideation-20260902T0022Z-synthesis.md-144-  charged (Card II lane); OPEN[SHAPE-2-INNER-g>=3] RETIRED
xmodel/ideation-20260902T0022Z-synthesis.md-145-  (closed by CABLE-3); OPEN[BMFACT-STRAND-VS-BLOCK] charged
xmodel/ideation-20260902T0022Z-synthesis.md-146-  (SAGE-NATIVE running + SOURCE-GATE consumes cycle types only);
xmodel/ideation-20260902T0022Z-synthesis.md-147-  OPEN[A2-CELL-32] already owned (horn lane).
xmodel/ideation-20260902T0022Z-synthesis.md-148-- ONE TRACKED UPGRADE (grok §7, chosen over fable's
xmodel/ideation-20260902T0022Z-synthesis.md-149-  FAIL-CLOSED-LINT and sol's SNAPSHOT-READSET as
xmodel/ideation-20260902T0022Z-synthesis.md:150:  highest-value/lowest-risk): encoding-faithfulness PREFLIGHT as
xmodel/ideation-20260902T0022Z-synthesis.md-151-  a required gate on every nodal-type Groebner job — emit the
xmodel/ideation-20260902T0022Z-synthesis.md:152:  reduced approximate root beside the .ms, diff against the raw
xmodel/ideation-20260902T0022Z-synthesis.md-153-  binomial, refuse on nonempty diff; positive control = (9,6,2)
xmodel/ideation-20260902T0022Z-synthesis.md-154-  corrected, negative control = the Delta=(8,6,19) false
xmodel/ideation-20260902T0022Z-synthesis.md-155-  positive. Queued to the qqideal oracle harvest (P1 disagreement
xmodel/ideation-20260902T0022Z-synthesis.md-156-  machinery already exists). FAIL-CLOSED-LINT and
xmodel/ideation-20260902T0022Z-synthesis.md-157-  SNAPSHOT-READSET are RECORDED as next-window candidates, not
xmodel/ideation-20260902T0022Z-synthesis.md-158-  lost; sol's canary test design is banked in its submission.
xmodel/ideation-20260902T0022Z-synthesis.md-159-
xmodel/ideation-20260902T0022Z-synthesis.md-160-## 8. Launch table (this synthesis)
xmodel/ideation-20260902T0022Z-synthesis.md-161-
xmodel/ideation-20260902T0022Z-synthesis.md-162-| lane | seat | charge |
xmodel/ideation-20260902T0022Z-synthesis.md-163-|---|---|---|
xmodel/ideation-20260902T0022Z-synthesis.md-164-| source-gate-962 (merge: retype + 3 razors, Mode 1 on (9,6,2)) | opus (#3/5) | opus+grok+sol submissions, rep-96, sheet-gate |
--
xmodel/ideation-20260902T0022Z-grok46.md-246-dual graph, never Domrina vertex names on a Sigray/Orevkov tree.
xmodel/ideation-20260902T0022Z-grok46.md-247-
xmodel/ideation-20260902T0022Z-grok46.md-248-First `(9,6,2)` run is the §5 attack. First `(8,6,9)` run either
xmodel/ideation-20260902T0022Z-grok46.md-249-computes `c_2(T)` from the type or returns `OPEN` (curve-dependent,
xmodel/ideation-20260902T0022Z-grok46.md-250-TB-GERM §6). PARAM is the next library release, not this experiment.
xmodel/ideation-20260902T0022Z-grok46.md-251-
xmodel/ideation-20260902T0022Z-grok46.md-252-## 7. Campaign-systems check
xmodel/ideation-20260902T0022Z-grok46.md-253-
xmodel/ideation-20260902T0022Z-grok46.md-254-**`UPGRADE`** — encoding-faithfulness preflight as a required
xmodel/ideation-20260902T0022Z-grok46.md-255-gate of every Groebner / msolve job.
xmodel/ideation-20260902T0022Z-grok46.md-256-
xmodel/ideation-20260902T0022Z-grok46.md-257-Evidence: all six suite ideals were UNFAITHFUL (raw binomial vs
xmodel/ideation-20260902T0022Z-grok46.md:258:reduced approximate root); `(9,6,2)` was a false EMPTY. That is
xmodel/ideation-20260902T0022Z-grok46.md-259-the week's costliest silent-zero, larger than footguns #10/#11.
xmodel/ideation-20260902T0022Z-grok46.md-260-FALLACY-v2 names remainder degree; nothing yet *refuses* a `.ms`
xmodel/ideation-20260902T0022Z-grok46.md-261-of raw `p^a-q^b` coefficients. Attach at the qqideal oracle (P1
xmodel/ideation-20260902T0022Z-grok46.md-262-disagreement = ORACLE-P0). Seal-at-completion is already adopted.
xmodel/ideation-20260902T0022Z-grok46.md-263-
xmodel/ideation-20260902T0022Z-grok46.md-264-Smallest upgrade. (1) Every nodal-type job emits the reduced
xmodel/ideation-20260902T0022Z-grok46.md:265:second approximate root beside the `.ms` and diffs it against the
xmodel/ideation-20260902T0022Z-grok46.md-266-raw binomial; nonempty diff = `UNFAITHFUL`, solver refused.
xmodel/ideation-20260902T0022Z-grok46.md-267-Positive control: realized `(9,6,2)` passes corrected, fails old.
xmodel/ideation-20260902T0022Z-grok46.md-268-Negative control: encoding-faithfulness r2 point A
xmodel/ideation-20260902T0022Z-grok46.md-269-(`Delta=(8,6,19)`) must be rejected. (2) Record remainder degree
xmodel/ideation-20260902T0022Z-grok46.md-270-in the declared quotient, branch on vanished leaders, handle
xmodel/ideation-20260902T0022Z-grok46.md-271-zero; no boolean emptiness.
xmodel/ideation-20260902T0022Z-grok46.md-272-
xmodel/ideation-20260902T0022Z-grok46.md-273-Not the upgrade: do not retire M2 before the oracle reports; do
xmodel/ideation-20260902T0022Z-grok46.md-274-not block Card I on PARAM; do not change the promotion rule.
xmodel/ideation-20260902T0022Z-grok46.md-275-Clause 2 is new: no frozen input requires remainder-degree as a
xmodel/ideation-20260902T0022Z-grok46.md-276-solver refusal.
xmodel/ideation-20260902T0022Z-grok46.md-277-
--
xmodel/ideation-20260902T0022Z-grok46.md-246-dual graph, never Domrina vertex names on a Sigray/Orevkov tree.
xmodel/ideation-20260902T0022Z-grok46.md-247-
xmodel/ideation-20260902T0022Z-grok46.md-248-First `(9,6,2)` run is the §5 attack. First `(8,6,9)` run either
xmodel/ideation-20260902T0022Z-grok46.md-249-computes `c_2(T)` from the type or returns `OPEN` (curve-dependent,
xmodel/ideation-20260902T0022Z-grok46.md-250-TB-GERM §6). PARAM is the next library release, not this experiment.
xmodel/ideation-20260902T0022Z-grok46.md-251-
xmodel/ideation-20260902T0022Z-grok46.md-252-## 7. Campaign-systems check
xmodel/ideation-20260902T0022Z-grok46.md-253-
xmodel/ideation-20260902T0022Z-grok46.md-254-**`UPGRADE`** — encoding-faithfulness preflight as a required
xmodel/ideation-20260902T0022Z-grok46.md-255-gate of every Groebner / msolve job.
xmodel/ideation-20260902T0022Z-grok46.md-256-
xmodel/ideation-20260902T0022Z-grok46.md-257-Evidence: all six suite ideals were UNFAITHFUL (raw binomial vs
xmodel/ideation-20260902T0022Z-grok46.md:258:reduced approximate root); `(9,6,2)` was a false EMPTY. That is
xmodel/ideation-20260902T0022Z-grok46.md-259-the week's costliest silent-zero, larger than footguns #10/#11.
xmodel/ideation-20260902T0022Z-grok46.md-260-FALLACY-v2 names remainder degree; nothing yet *refuses* a `.ms`
xmodel/ideation-20260902T0022Z-grok46.md-261-of raw `p^a-q^b` coefficients. Attach at the qqideal oracle (P1
xmodel/ideation-20260902T0022Z-grok46.md-262-disagreement = ORACLE-P0). Seal-at-completion is already adopted.
xmodel/ideation-20260902T0022Z-grok46.md-263-
xmodel/ideation-20260902T0022Z-grok46.md-264-Smallest upgrade. (1) Every nodal-type job emits the reduced
xmodel/ideation-20260902T0022Z-grok46.md:265:second approximate root beside the `.ms` and diffs it against the
xmodel/ideation-20260902T0022Z-grok46.md-266-raw binomial; nonempty diff = `UNFAITHFUL`, solver refused.
xmodel/ideation-20260902T0022Z-grok46.md-267-Positive control: realized `(9,6,2)` passes corrected, fails old.
xmodel/ideation-20260902T0022Z-grok46.md-268-Negative control: encoding-faithfulness r2 point A
xmodel/ideation-20260902T0022Z-grok46.md-269-(`Delta=(8,6,19)`) must be rejected. (2) Record remainder degree
xmodel/ideation-20260902T0022Z-grok46.md-270-in the declared quotient, branch on vanished leaders, handle
xmodel/ideation-20260902T0022Z-grok46.md-271-zero; no boolean emptiness.
xmodel/ideation-20260902T0022Z-grok46.md-272-
xmodel/ideation-20260902T0022Z-grok46.md-273-Not the upgrade: do not retire M2 before the oracle reports; do
xmodel/ideation-20260902T0022Z-grok46.md-274-not block Card I on PARAM; do not change the promotion rule.
xmodel/ideation-20260902T0022Z-grok46.md-275-Clause 2 is new: no frozen input requires remainder-degree as a
xmodel/ideation-20260902T0022Z-grok46.md-276-solver refusal.
xmodel/ideation-20260902T0022Z-grok46.md-277-

exec
/bin/zsh -lc "sed -n '675,845p' xmodel/companion-curve-alln-opus5-20260902.md" in /Users/dc/code/math/jc2
 succeeded in 0ms:
2. `OPEN[REP-96-SOURCE-IS-C2]` - Riemann existence gives a normal surface `Y`, not
   `Y ~ C^2`. Unchanged, and still the largest gap.
3. A new one, typed here: even given a representation and a source, `A_F` must be
   the *actual* non-properness set of the resulting map, which pins `R_0(u,v)` up
   to a constant, hence pins `M = M_1 + M_2` and, through Chau's resultant,
   `M <= K = deg P / d`. `OPEN[COMPANION-R0-REALISATION]`, SS7.

On item 3, one derived remark `[D]`: `Res_y(P-u,Q-v) = R_0 x^N + ... + R_N` has
`deg_u <= deg_y Q = Ke` and `deg_v <= deg_y P = Kd`, while `R_0`'s weighted-leading
form `(B^du^e - A^ev^d)^M` has `deg_u = Me`, `deg_v = Md`; hence **`M <= K`**, i.e.
`sum_i k_i m_i <= K` and `sum_i deg D_i <= max(deg P, deg Q)`. Nothing is bounded
absolutely (`deg P` is free), but this is the exact currency in which a degree-cap
attack - `OPEN[N5-DEGREE-CAP]`, restored as high-leverage by CAGE SS2.5 - must be
paid.

## 6. The machine job

The charge asks for the exact job that would decide `D_2`'s existence. **The base
question no longer needs one**: existence at `m = 1` is decided positive by the
SS5.3 witness, which doubles as the positive control for anything below. What a
machine can still decide, cheaply, is the *stratification* of the companion family
- which contact profiles against `D_1` are attainable. Two jobs, in priority order.

### JOB-A (small, decisive for a named stratum): the affinely-disjoint companion

*Question.* Is there a multiplicity-`m` companion of the `(9,6,2)` curve that meets
`D_1` **only** at `P_inf`, i.e. with maximal contact `(D_1.D_2)_{P_inf} = 27m`?

*Ring.* `Q[a_{3m-1},...,a_0, b_{2m-2},...,b_0]`, no auxiliary variables: leading
coefficients fixed to `1` by C7 + `xi`-scaling, `b_{2m-1} := 0` by `xi`-translation.
Dimension `5m - 1`; at `m = 1`, `Q[a_2,a_1,a_0,b_0]`.

*Generators.* Form `S(xi) := f_1(a(xi), b(xi)) in Q[coeffs][xi]` with `f_1` the
closed form of SS3.3. Let `I_A` be generated by all
coefficients of positive powers of `xi`, i.e. `[xi^k]S` for
`1 <= k <= 18m - min(3,m)` (the Lemma CONTACT ceiling; generators above the actual
degree are zero and harmless). Disjointness is `S = const != 0`, so the decision is
`V(I_A) != {}` together with `[xi^0]S != 0` on it. At `m = 1` the actual degree is
`12`, so `I_A` is **12 nonzero generators in 4 variables**.

*Decision.* `msolve -f I_A.ms -o out` in its default rational-input mode (0.10.1,
live on Box03): output `[-1]` is "no solution in the algebraic closure", i.e.
`V(I_A) = {}`; `msolve -g 2` gives the reduced grevlex basis for a hand audit;
`qqideal` answers the rational-point refinement if the closure verdict is nonempty.
Emptiness is the expected answer and would prove that **every** `m = 1` companion
of the `(9,6,2)` curve meets it affinely - the first nontrivial constraint tying
the two components together.

*Controls, mandatory.* (+) Replace `f_1` by the companion's own `f_2`: then
`S = 0`, `I_A = (0)`, `V = A^4` - catches a substitution/ring error. (-) Drop C7
(set `A_2 = 2`): `S` jumps to degree `18`, `I_A` gains 6 generators, and any
emptiness verdict is then for a different reason. (0) Feed the SS5.3 witness
`(a_2,a_1,a_0,b_0) = (0,1,1,3)`: `S` must have degree `12`, so the witness **fails**
`I_A` - confirming the job asks the intended question.

### JOB-B (the one with decision power over the row): the braid factorisation

`OPEN[REP-96-BM-FACTORISATION]` is unchanged by this lane and is still the cheapest
decisive step on the row. Not a Groebner job: eight tangency transports for a
degree-`9` curve whose singular-fibre census is already exact (REP96 SS7 R1: eight
simple tangency fibres, one fibre `x = 0` carrying all four nodes, ledger
`8.1 + 4.2 = 16 = 2 delta_aff + d - 1`). SS5.1 supplies the set-up data: the nodes
are the roots of `v^4+96v^2+1536` on `u = 0`, the tangency parameters the roots of
`3t^8+20t^4+8` with pairwise distinct `p`-values. **Prefer JOB-B to JOB-A** if only
one can be run.

*What must not be run.* Any job that tries to decide the companion by imposing
`(M')`. By THEOREM COLLAPSE-N the resulting ideal is `(0)` up to the budget, and
the job would return a vacuous NONEMPTY. This is the concrete cost of the negative
in SS2 and the reason it was worth proving.

## 7. Typed verdict block, OPENs, deviations

```text
LANE                 COMPANION-CURVE-ALLN   (Path-2 flagship, reducible branch)
VERDICT              CLOSED-NEGATIVE as an obstruction lane.
                     The forced companion is realizable at every degree; the
                     identity charged to constrain it is information-free.

THEOREM COLLAPSE-N   PROVED-HERE, UNREVIEWED, machine-controlled.
                     LHS(M'-bf) - RHS(M'-bf) = (N-1) - K_tot - sum_i W_i chi~_i.
                     Given (AGG): (M'-bf) <=> chi~_i = 1 for all i <=> Lemma A.
                     Corollary: (M') is not an independent gate in RED-N.

THEOREM COMPANION-   PROVED-HERE, UNREVIEWED, machine-controlled (21 shapes +
  EXISTS             3 negative + 8 repaired).  A three-row explicit family
                     realizes C1-C8 for every coprime (d,e) with max(d,e)>=2,
                     every value of A^e/B^d, and every multiplicity m not
                     excluded by MULT-2.   N_0 DOES NOT EXIST.

Lemma CONTACT        PROVED-HERE, UNREVIEWED, near-sharp on 3 machine controls.
Lemma EMB-AUTO       PROVED-HERE (Abhyankar-Moh).
Corollary MULT-2     PROVED-HERE, UNREVIEWED.  If min(d,e) = 1 then every
                     component of A_F has m_i >= 2, i.e. deg D_i >= 2 max(d,e).
                     The lane's only outright kill; silent at (d,e) = (3,2).
Chau AUDIT-CC-2/3    Cor 1 leading form is (B^d u^e - A^e v^d)^M, not
                     (A^e u^e - B^d v^d)^M; Cor 2's c satisfies c^e = A^e/B^d.
                     Machine-verified. Harmless in the monic normalisation.

(9,6,2) SUBSTRATE    Companion existence DECIDED POSITIVE at N = 4, m = 1, with
                     an explicit witness over Q. The row SURVIVES this attack.
                     Status unchanged at OPEN[REP-96-BM-FACTORISATION] and
                     OPEN[REP-96-SOURCE-IS-C2].
```

**OPENs closed.** `OPEN[REP-96-MPRIME-COMPANION]` - **closed NEGATIVE**: the
identity never binds, at any `N`, on any profile, for any pinning of `D_2`.

**OPENs opened.** `OPEN[COMPANION-R0-REALISATION]`: `A_F` must be the zero set of
the leading resultant coefficient `R_0`, which pins `M = sum_i k_i m_i` and forces
`M <= K = deg P/d`, i.e. `sum_i deg D_i <= max(deg P, deg Q)`. This is the exact
currency of `OPEN[N5-DEGREE-CAP]` / `OPEN[RED-N-DMIN-BOUND]`, restored as
high-leverage by CAGE SS2.5, and it is the only place found in this lane where the
companion's degree can be reached at all.
`OPEN[COMPANION-AFFINE-DISJOINT]`: JOB-A, SS6.

**OPENs untouched.** `OPEN[REP-96-BM-FACTORISATION]`, `OPEN[REP-96-SOURCE-IS-C2]`,
`OPEN[CUSP-2-AT-CONTRACTED-ATTACHMENT]`, `OPEN[PI1-S_N-CAGE(...)]`,
`OPEN[RED-N-ORDINARY-GENERATION]`, `OPEN[RED-N-BLOCK-SYSTEM]`,
`OPEN[ZARISKI-GENERIC-LINE-CUSTODY]`, `OPEN[THREE-TRIVIALS-ONE-OWNER]`.

**Deviations from the charge.** (a) The charge frames tasks (1)-(2) as steps toward
an all-degree contradiction. Task (1) refutes the premise that `(M')` can supply
one, so task (2) was run against the *cage's* forced datum rather than against
`(M')`'s output, and its verdict is the exhibition the charge authorised as a
success rather than a failure. (b) Task (3) asks for a Groebner job deciding `D_2`'s
existence; existence is decided by hand plus an exact witness, so SS6 specifies the
job for the residual stratum question and names the higher-leverage successor
instead of manufacturing a decision job for a settled question. (c) One non-charged
campaign file and one primary source were read; both are disclosed in SS0 with
hashes. (d) No `charge_basis` line: no exit-price assertion is made anywhere here.
(e) **Size.** The body overruns the charged 25-35 KB target by roughly 30%. Three
compression passes were run; what remains is the two theorems with their proofs,
the (C1)-(C11) datum, the `(9,6,2)` instantiation with its verified witness, the
control ledger the hostile standard requires, and the job spec. Nothing was dropped
to hit a byte count, and the overrun is reported rather than paid for with
verified mathematics or with controls.

## 8. FALLACY-v2 self-check

*Flag/place/series.* Three separations are load-bearing. (i) The two `b` symbols
the charge warns about are written `b_dic` and `b_br` throughout, plus `b_box` for
the third one hiding inside Prop 2.4's proof; SS0 fixes all three. (ii) "Component
of `A_F`" vs "component in the branch locus" (AUDIT-CC-1): Prop 2.4's ceiling is
licensed only on the second, and applying it to a companion would falsely kill the
promoted `N = 4` profile. (iii) Asymptotic coefficient `c` vs its `e`-th power
(AUDIT-CC-2): only `c^e = A^e/B^d` is component-independent.

*Carrier/attainment.* SS5.3 is typed as a curve pair satisfying a necessary cage,
never as a witness for a Keller map; SS5.5 states the three remaining gaps.
`THEOREM COMPANION-EXISTS` is an attainment claim **about curves with prescribed
data** and is proved by construction plus a 21-shape machine check, not inferred
from non-emptiness of a cage.

*Floor/attainment.* Lemma CONTACT is one-sided and used only so; its controls are
reported with actual values (`12` vs bound `17`; `33` vs `34`, twice) so it is not
read as an equality. `M <= K` in SS5.5 is a ceiling and is not converted into a cap.

*Raw remainder degree / variable-ring map.* Every degree statement about `A_F` is
weighted (`wt(u)=d, wt(v)=e`, declared at each use) or tied to a named
parametrisation with its leading coefficients. The one raw-degree relation,
`sum_i deg D_i <= max(deg P, deg Q)`, is flagged as gauge-dependent on the `x,y`
side and is not a `d_min` bound. The substitution `u -> a(xi), v -> b(xi)` in SS3.4
and SS6 declares source ring, target ring, images and the grading it respects.

*Controls.* SS2.5: one positive, three negative (`chi~_2 = 0,-1,-3`, each matching
the predicted defect), one 33116-case randomised; the failed first harness is
reported, not discarded. SS4.2: 21 positive shapes, 3 negative, 8 repaired - and the
negative control is what found the `min(d,e) = 1` failure of the first family, which
inspection had missed and which turned into Corollary MULT-2; the wrong first
statement is reported rather than silently replaced. SS6 specifies `(+)`,`(-)`,`(0)`

codex
The controlling text confirms two important boundaries: the collision tool is a candidate index, not a mathematical resolver, and PREFLIGHT must validate exact emitted structure before any solver runs. I’m using those boundaries to review the implementations for fail-closed behavior and for typed, non-promotional outputs.
collab: Wait
exec
/bin/zsh -lc 'for p in ops/open_collision.py ops/test_open_collision.py box/preflight.py ops/test_preflight.py; do if [ -e "$p" ]; then wc -c -l "$p"; fi; done' in /Users/dc/code/math/jc2
 succeeded in 0ms:
collab: Wait
exec
/bin/zsh -lc 'python3 ops/seal.py stamp --help && python3 ops/seal.py verify --help' in /Users/dc/code/math/jc2
 succeeded in 0ms:
usage: seal.py stamp [-h] --basis BASIS file

positional arguments:
  file

options:
  -h, --help     show this help message and exit
  --basis BASIS
usage: seal.py verify [-h] [--expected-basis EXPECTED_BASIS] files [files ...]

positional arguments:
  files

options:
  -h, --help            show this help message and exit
  --expected-basis, --expect-basis EXPECTED_BASIS
                        require this exact 40-character frozen basis (never
                        inferred)

exec
/bin/zsh -lc "sed -n '1,40p' xmodel/systems-collision-preflight-sol56-20260902.run.v2" in /Users/dc/code/math/jc2
 succeeded in 0ms:
run_schema=2
tag=systems-collision-preflight-sol56-20260902
adapter=codex
pid=45717
host=dc-mbp-m2.local
basis=621c9edc6bb8932e4aed6ac9805a5684a4436789
prompt=/Users/dc/code/math/jc2/xmodel/systems-collision-preflight-sol56-20260902.prompt.md
prompt_sha256=30b6200971ca1575a80be6d796e7eca8f85758f5d52c666b08bc0090bdd4b172
adapter_sha256=250c127256b5e19f3ea39f7d1b62599dbf0846072f8c6aff0495b111146cc60d
launcher=ops/lane.sh
launcher_sha256=fded27273d1b1d519826c6e5072355bd8371b077e172c8e52a3354b978e87d1f
sandbox_enforcement=MACOS_SEATBELT
sandbox_profile_sha256=13aea71df04c1ffff9a7df13b3622f2cdc3f3cba20a865e9a33da4c30e6af2fe
charge_basis_validator=ops/validate_charge_basis.py
charge_basis_validator_sha256=a89658bc2bf01c4cbbc601f3b9db60e46168d37f00367fdc87ef2e94b19a125e
seal_tool=ops/seal.py
seal_tool_sha256=222e6de4076e1a3169ff27bd0f6b9deff8c2389aa2ff9dd44b8b4ec90c2ea776
body_end_contract=NONE
charged_inputs=3
lane_inputs_dir=/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs
charged_input_1=xmodel/ideation-20260902T0741Z-opus5.md
charged_input_1_basename=ideation-20260902T0741Z-opus5.md
charged_input_1_sha256=e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce
charged_input_2=xmodel/ideation-20260902T0741Z-sol56.md
charged_input_2_basename=ideation-20260902T0741Z-sol56.md
charged_input_2_sha256=717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11
charged_input_3=xmodel/ideation-20260902T0741Z-synthesis.md
charged_input_3_basename=ideation-20260902T0741Z-synthesis.md
charged_input_3_sha256=4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d
fallacy=FALLACY-v2.md
fallacy_bytes=1985
fallacy_sha256=e47fd16cfcc91bc7bfdac4ba1b5f46152db8235e6609dca6e29a5966549c38f5
model_prompt=/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/model-prompt.txt
model_prompt_sha256=f73ab5cc52083b6b4e6a1719f34d5df62c3aef8d037136693bd594a04408f83f
start_utc=2026-09-02T09:13:56Z
provenance_scope=DECLARED_LANE_INPUTS
initial_status=RUNNING
child_pid=45865

exec
/bin/zsh -lc 'tail -16 xmodel/systems-semantic-replay-v1-sol56-20260831.md' in /Users/dc/code/math/jc2
 succeeded in 0ms:

OK
```

The six cases are: compliant synthetic pass, invariant generator mutation,
path-witness bypass, malformed certificate, charged ledger negative fixture,
and charged census insufficient-evidence fixture.  The tests create their
helper scripts and certificates only inside `TemporaryDirectory`; the proposed
patch adds no fixture files and makes no CI-runner change.

The hash boundary is the two declared Python entrypoints.  If an entrypoint
imports other custody-critical code, that dependency must remain independently
pinned by the replay or be promoted to an entrypoint in a successor schema;
v1 does not infer dependency custody from an import name.

<!-- BODY-END -->

exec
/bin/zsh -lc "rg -l '"'^seal: BODY-END/v1$'"' xmodel/*.md | head -5 | xargs -I{} sh -c 'echo {}; tail -12 \"{}\"' " in /Users/dc/code/math/jc2
 succeeded in 1ms:
exec
/bin/zsh -lc "rg -n 'BODY-END-SHA|body_sha256|seal-v' xmodel/*.md | head -40" in /Users/dc/code/math/jc2
 succeeded in 0ms:
xmodel/bd-a2-d3-halphen-weighted-boundary-hostile-rereview-fable5-20260830.md:15:artifact manifests' `body_bytes`/`body_sha256` fields:
xmodel/block-descent-a1-quartic-cycle1-pseudoplane-companion-hostile-review-opus5-20260830.md:44:  matching `artifact.body_sha256`, `custody.source_sha256`, and the in-file seal;
xmodel/bd-a2-d3-local-degreecap-countercontrol-r1-hostile-review-opus5-20260830.md:29:both the seal block and `body_sha256`/`source_sha256` in the artifact JSON.
xmodel/block-descent-a1-quartic-cycle1-function-pair-hostile-review-gpt55-20260830.md:27:{"artifact":{"body_bytes":21468,"body_sha256":"ca809efbfbf2e613e74254d27a2b37fc4cef44610569a03cf7aa69da11b9b78d","file_bytes":21801,"frozen_basis":"06a4110854d7ad38525daccfa7895943259a3812","full_sha256":"0a6272060a03c0071ad8b4c71aef00f3c724284d9590dcab61a2e75169a8713f","name":"block-descent-a1-quartic-cycle1-function-pair-cyclic-normalization-sol56-20260830.md","published_mode":"0444"},"custody":{"closed_utc":"2026-08-30T22:37:37Z","finalized_utc":"2026-08-30T22:37:41Z","lease_id":"b6700119e3bdb144606273f2e7ae5144","opened_utc":"2026-08-30T22:25:55Z","owner":"rank4_cycle1_function_pair","source_bytes":21468,"source_sha256":"ca809efbfbf2e613e74254d27a2b37fc4cef44610569a03cf7aa69da11b9b78d"},"schema":"jc2.artifact-finalize/v1"}
xmodel/block-descent-a1-quartic-discriminant-double-plane-hostile-review-gpt55-20260830.md:29:artifact.body_sha256=4b1a7bf4b829fb2bf653a5421375d8caac159491c3a0986df0428e0b5f77c285
xmodel/ideation-20260829T0250Z-synthesis-fable5.md:449:report_body_sha256 = 1bc47e35b193e2b75cc9499532c6589c02f3b4fc64d232a2ae47213ced88e770
xmodel/ideation-20260829T0820Z-opus5.md:718:report_body_sha256 = 17dacd754d82ce19ae07b3ec21e6fa82f2134ccda6e15abb1eaafcfeae033c37
xmodel/ideation-20260829T0820Z-fable5.md:472:report_body_sha256 = 7c97c36fb9ba5b76d9d99c1ffd2c93232ac06d8bcee6f61472b921b20fbc3be0
xmodel/ideation-20260829T0250Z-opus5.md:847:report_body_sha256 = 82d84021f34fc88f0075de81528ef27476e68afce93e7e8196449912b89fa760
xmodel/m2-td8-prop81iv-parametric-primary-grok46-20260829.md:460:report_body_sha256 = cc7035dec2949f674064f7495c209abbf1037dee3179f1abd876a8e292366416
xmodel/k00-grade5-rank0-plane-twoinput-primary-fable5-40c-20260829.md:612:body_sha256     = c3b4293509e741f42ea0df1ad301b2a859ecb2aa46f31b2d5c7afda707327271
xmodel/ideation-20260829T0250Z-grok46-rerun.md:468:report_body_sha256 = eea80419ae3ad1db2ed34cc5ea766abc4c90009bb196f0a6d9a601b76dccb8b3
xmodel/ideation-20260829T0820Z-crosspoll-grok46.md:825:report_body_sha256 = 97814f6dbc00ecc9d1584c5654c2285d21822c44a3e72c2b323868ae0bcbd349
xmodel/ideation-20260829T0820Z-crosspoll-opus5.md:773:report_body_sha256 = 6e8a9d1947903a45a466d9230fda8bcd24efdbfed5ca4fb16dce6396a0813a9b
xmodel/ideation-20260829T1808Z-crosspoll-opus5.md:862:  artifacts** — 8/8 `PASS`, and every reported `body_bytes`/`body_sha256`
xmodel/k00-grade4-rank0-plane-primary-opus5-92e-20260829.md:909:body_sha256     = f6356d974f2f183d571297384d33f9161c2dd12d6a21c10a3fdc3200591cb6a2
xmodel/m2-u2-nu1-unbounded-lex-primary-hostile-review-fable5-20260829.md:402:report_body_sha256 = 2a59c1961b9bf0a1677edc43289670f67ea42442b0f1e1928f4db78cb2645c8b
xmodel/ideation-20260829T0250Z-sol56.md:432:report_body_sha256 = ecbcd993f04a448e33315e90aa904dfa380dc42d448306be75458c8d1ec2d9af
xmodel/ideation-20260829T0820Z-grok46.md:428:report_body_sha256 = 349dfed1ecc23aed09a46b2ef0efb33d41ff96073302bad553ec6ad25482acad
xmodel/m2-td12-u1-trunk-consumer-primary-grok46-20260829.md:480:report_body_sha256 = ea43c28125b61710d1af58a90de1928a7c05f93734950c6d89814cd3c99d5be2
xmodel/ideation-20260829T0820Z-crosspoll-fable5.md:489:report_body_sha256 = 5c3ae2c807d0722bcf43cceb4c11a17526abd8c208b7032c21cbcae9d8874322
xmodel/k00-rees-sandwich-sol56-20260829.md:43:The Opus blind report was seal-verified before Section 1 was read:
xmodel/ideation-20260829T0250Z-fable5.md:449:report_body_sha256 = 5b90f45745f00dfc1b01c9838c82a1b396b6cdae305c789f2214658e0a6a00db
xmodel/m2-td8-equal-join-zero-side-initialization-primary-grok46-20260829.md:531:report_body_sha256 = 3d07291357a266225f6e269d876c62dc6a18c7271ac3a730999fede430c696f1
xmodel/m2-td12-u1-trunk-consumer-hostile-review-opus5-20260829.md:657:report_body_sha256 = 86ccd2f1f36a37fce7f038ce3a162468f61aea4974dfd778a370ab3e04fa7e3e
xmodel/ideation-20260829T1808Z-crosspoll-fable5.md:344:Independent convergences preserved: (1) three separate seal-validator
xmodel/m2-u2-nu1-unbounded-lex-primary-grok46-20260829.md:561:report_body_sha256 = 49fdda2b84e4a405f6432239546826a943326a5357f10efb26eaa8b2a149a1b6
xmodel/m2-td8-equal-join-zero-side-initialization-hostile-review-opus5-20260829.md:635:report_body_sha256 = 0dee73677e476253dd57abd4d007dbd6912c40d668352878bdf7fc7c08ca42a3
xmodel/ideation-20260829T1808Z-grok46.md:509:   printed `body_bytes` and `body_sha256` match `python3` hashing.
xmodel/m2-budget-quotient-primary-research-opus5-hostile-review-grok46-20260829.md:39:matches the brief and the report's own `report_body_sha256` line.
xmodel/m2-budget-quotient-primary-research-opus5-hostile-review-grok46-20260829.md:825:report_body_sha256 = 7af0e75fb1b62dd9394fd60f63de1bbbb6e5b76526f025eb4b4e393aa27506ed
xmodel/m2-equal-join-semilinear-primary-research-hostile-review-grok46-20260829.md:44:matches the report's own `report_body_sha256` line.
xmodel/m2-equal-join-semilinear-primary-research-hostile-review-grok46-20260829.md:823:report_body_sha256 = cf935fe9674aa36cf7f73eaaee841c7b38307707a6e39416d1a0afe02a0c6d39
xmodel/m2-td8-first-extra-jet-exact-lambda-primary-opus5-20260829.md:618:report_body_sha256 = f1ae6c2b1dd94ab2dcf26dfb16e149067eab0617b549817d5620c728d6936d11
xmodel/m2-td8-cv-weight-contact-gate-hostile-review-fable5-20260829.md:87:reproduces (cut = all bytes above the `report_body_sha256` line). All five
xmodel/m2-td8-cv-weight-contact-gate-hostile-review-fable5-20260829.md:411:report_body_sha256 = f972253c571a547eab2bf67c79298d59a47fb800a4b367710458e343d93a2fa6
xmodel/m2-td8-prop81iv-parametric-hostile-review-fable5-20260829.md:495:report_body_sha256 = 9e594a2343bfcdfe8b2631506543139210f1fa178f4a616c7e09d93ba54da82b
xmodel/m2-budget-quotient-primary-research-opus5-20260829.md:751:report_body_sha256 = b161421ccb04a62307b87be4a60f130ded64fe53228bd600c960f2c502632283
xmodel/m2-td8-equal-join-route-family-hostile-review-grok46-20260829.md:499:report_body_sha256 = a53a78dbabaaa6f1c2a30247ad016b99bf2d2011132736a2e8b7c5f098552c49
xmodel/m2-td8-first-extra-jet-exact-lambda-primary-hostile-review-fable5-20260829.md:451:report_body_sha256 = 463a8f4860f950445a9ff784b1c1b41729762bbebd530477b2e1b53717d564a7

exec
/bin/zsh -lc "rg -n 'canonical_seal|SEAL' ops/seal.py | head -40 && sed -n '40,105p' ops/seal.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
260:    canonical = _canonical_seal(len(body), actual_sha, basis)
273:def _canonical_seal(body_bytes: int, body_sha: str, basis: str) -> bytes:
300:        sealed = body + _canonical_seal(len(body), digest, basis)
437:                    "status": "CLEAN_SEALED",


class SealError(ValueError):
    """A report is unsafe to read or has an invalid seal."""


class SealDurabilityWarning(SealError):
    """The installed seal verified, but its directory sync failed."""


def _validate_basis(basis: str, *, label: str = "basis") -> None:
    if HEX40.fullmatch(basis) is None:
        raise SealError(
            f"{label} must be exactly 40 lowercase hexadecimal characters"
        )


def _open_parent(path: Path) -> tuple[int, str, Path, os.stat_result]:
    """Open an anchored parent directory; leave the final name unfollowed."""
    if not path.name or path.name in {".", ".."}:
        raise SealError("target must name a file")
    try:
        parent = path.parent.resolve(strict=True)
    except OSError as exc:
        raise SealError(f"cannot resolve target parent: {exc}") from exc
    try:
        before = parent.lstat()
    except OSError as exc:
        raise SealError(f"cannot inspect target parent: {exc}") from exc
    if stat.S_ISLNK(before.st_mode) or not stat.S_ISDIR(before.st_mode):
        raise SealError("target parent is a symlink or is not a directory")
    flags = os.O_RDONLY | getattr(os, "O_DIRECTORY", 0)
    if hasattr(os, "O_NOFOLLOW"):
        flags |= os.O_NOFOLLOW
    try:
        parent_fd = os.open(parent, flags)
    except OSError as exc:
        raise SealError(f"cannot open target parent: {exc}") from exc
    try:
        info = os.fstat(parent_fd)
        if not stat.S_ISDIR(info.st_mode) or _identity(info) != _identity(before):
            raise SealError("target parent identity changed during open")
        _check_parent_path(parent, info)
        return parent_fd, path.name, parent, info
    except Exception:
        os.close(parent_fd)
        raise


def _check_parent_path(parent: Path, expected: os.stat_result) -> None:
    try:
        current = parent.lstat()
    except OSError as exc:
        raise SealError(f"cannot recheck target parent: {exc}") from exc
    if stat.S_ISLNK(current.st_mode) or not stat.S_ISDIR(current.st_mode):
        raise SealError("target parent changed to a symlink or non-directory")
    if _identity(current) != _identity(expected):
        raise SealError("target parent identity changed")


def _identity(info: os.stat_result) -> tuple[int, int]:
    return info.st_dev, info.st_ino


def _snapshot(info: os.stat_result) -> tuple[int, int, int, int, int, int]:
    return (

exec
/bin/zsh -lc "sed -n '250,315p' ops/seal.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
            f"body byte mismatch: declared {declared_bytes}, actual {len(body)}"
        )
    if declared_sha != actual_sha:
        raise SealError(
            f"body SHA-256 mismatch: declared {declared_sha}, actual {actual_sha}"
        )
    if expected_basis is not None and basis != expected_basis:
        raise SealError(
            f"frozen basis mismatch: declared {basis}, expected {expected_basis}"
        )
    canonical = _canonical_seal(len(body), actual_sha, basis)
    if data[boundary:] != canonical:
        raise SealError("post-body seal is not the canonical LF serialization")
    return len(body), actual_sha, basis


def verify_path(
    path: Path, *, expected_basis: str | None = None
) -> tuple[int, str, str]:
    data, _, _ = _read_regular(path)
    return verify_bytes(data, expected_basis=expected_basis)


def _canonical_seal(body_bytes: int, body_sha: str, basis: str) -> bytes:
    return (
        "\n## Seal\n\n"
        "- Body definition: every byte through the unique standalone "
        "`<!-- BODY-END -->` line,\n"
        "  including its terminating newline; this seal is outside the body.\n"
        f"- Body bytes: `{body_bytes}`.\n"
        "- Body SHA-256:\n"
        f"  `{body_sha}`.\n"
        f"- Frozen basis: `{basis}`.\n"
    ).encode("utf-8")


def stamp_path(path: Path, basis: str) -> tuple[int, str, str]:
    _validate_basis(basis)
    parent_fd, name, parent, parent_info = _open_parent(path)
    temp_name: str | None = None
    durability_error: OSError | None = None
    try:
        data, original = _read_regular_at(parent_fd, name)
        boundary = _body_boundary(data)
        if data[boundary:].strip():
            raise SealError(
                "refusing to stamp: non-whitespace content already follows BODY-END"
            )
        body = data[:boundary]
        digest = hashlib.sha256(body).hexdigest()
        sealed = body + _canonical_seal(len(body), digest, basis)

        try:
            current = os.stat(name, dir_fd=parent_fd, follow_symlinks=False)
        except OSError as exc:
            raise SealError(f"cannot recheck target before replacement: {exc}") from exc
        if stat.S_ISLNK(current.st_mode) or not stat.S_ISREG(current.st_mode):
            raise SealError("target changed to a symlink or non-regular file")
        if _identity(current) != _identity(original):
            raise SealError("target identity changed while preparing seal")

        temp_fd, temp_name = _create_temp_at(
            parent_fd, name, stat.S_IMODE(original.st_mode)
        )
        with os.fdopen(temp_fd, "wb") as handle:
            handle.write(sealed)

collab: Wait
exec
/bin/zsh -lc 'for p in ops/open_collision.py ops/test_open_collision.py box/preflight.py ops/test_preflight.py; do if [ -e "$p" ]; then wc -c -l "$p"; fi; done' in /Users/dc/code/math/jc2
 succeeded in 0ms:
exec
/bin/zsh -lc "nl -ba xmodel/companion-curve-alln-opus5-20260902.md | sed -n '680,690p;780,790p;831,840p'" in /Users/dc/code/math/jc2
 succeeded in 0ms:
   680	   `M <= K = deg P / d`. `OPEN[COMPANION-R0-REALISATION]`, SS7.
   681	
   682	On item 3, one derived remark `[D]`: `Res_y(P-u,Q-v) = R_0 x^N + ... + R_N` has
   683	`deg_u <= deg_y Q = Ke` and `deg_v <= deg_y P = Kd`, while `R_0`'s weighted-leading
   684	form `(B^du^e - A^ev^d)^M` has `deg_u = Me`, `deg_v = Md`; hence **`M <= K`**, i.e.
   685	`sum_i k_i m_i <= K` and `sum_i deg D_i <= max(deg P, deg Q)`. Nothing is bounded
   686	absolutely (`deg P` is free), but this is the exact currency in which a degree-cap
   687	attack - `OPEN[N5-DEGREE-CAP]`, restored as high-leverage by CAGE SS2.5 - must be
   688	paid.
   689	
   690	## 6. The machine job
   780	**OPENs closed.** `OPEN[REP-96-MPRIME-COMPANION]` - **closed NEGATIVE**: the
   781	identity never binds, at any `N`, on any profile, for any pinning of `D_2`.
   782	
   783	**OPENs opened.** `OPEN[COMPANION-R0-REALISATION]`: `A_F` must be the zero set of
   784	the leading resultant coefficient `R_0`, which pins `M = sum_i k_i m_i` and forces
   785	`M <= K = deg P/d`, i.e. `sum_i deg D_i <= max(deg P, deg Q)`. This is the exact
   786	currency of `OPEN[N5-DEGREE-CAP]` / `OPEN[RED-N-DMIN-BOUND]`, restored as
   787	high-leverage by CAGE SS2.5, and it is the only place found in this lane where the
   788	companion's degree can be reached at all.
   789	`OPEN[COMPANION-AFFINE-DISJOINT]`: JOB-A, SS6.
   790	
   831	read as an equality. `M <= K` in SS5.5 is a ceiling and is not converted into a cap.
   832	
   833	*Raw remainder degree / variable-ring map.* Every degree statement about `A_F` is
   834	weighted (`wt(u)=d, wt(v)=e`, declared at each use) or tied to a named
   835	parametrisation with its leading coefficients. The one raw-degree relation,
   836	`sum_i deg D_i <= max(deg P, deg Q)`, is flagged as gauge-dependent on the `x,y`
   837	side and is not a `d_min` bound. The substitution `u -> a(xi), v -> b(xi)` in SS3.4
   838	and SS6 declares source ring, target ring, images and the grading it respects.
   839	
   840	*Controls.* SS2.5: one positive, three negative (`chi~_2 = 0,-1,-3`, each matching

exec
/bin/zsh -lc "nl -ba /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md | sed -n '180,280p;520,565p'" in /Users/dc/code/math/jc2
 succeeded in 0ms:
   180	```text
   181	CLAIM [D], UNREVIEWED, cheap to check.  At (9,6) the Chau cap is
   182	SATURATED BY D_1 ALONE, so no Keller map of bidegree (9,6) has a
   183	reducible A_F containing the realised (9,6,2) component:
   184	OPEN[COMPANION-R0-REALISATION] is decided NEGATIVE at (9,6,2) by
   185	substitution into a banked inequality.  COMPANION SS7 typed
   186	M = M_1+M_2 <= K as "the exact currency" and did not evaluate it on its
   187	own witness.
   188	```
   189	
   190	Three checks before promotion, flagged rather than assumed: **(a)**
   191	additivity — `R_0 = c prod_i f_i^{a_i}`, `a_i >= 1`, and the weighted-
   192	leading form of a product is the product of the forms, so
   193	`M = sum a_i m_i >= sum m_i`; write this out. **(b)** `m_1 = 3` must be a
   194	property of the numerical *type* (`deg D_1 = 9` plus `C6`), not of the
   195	realisation. **(c)** Chau's normalisation (`P,Q` monic in `y`; a generic
   196	linear change alters neither degrees nor `A_F`) and `deg_x Res = N`,
   197	consumed at his typing.
   198	
   199	If (a)-(c) hold, this is also a **general reducible-branch razor**:
   200	`sum_i deg D_i <= max(deg P, deg Q)` is checkable on every numerical type
   201	the census emits, as a one-line prefilter in front of the most expensive
   202	instrument the campaign runs.
   203	
   204	## 4. The all-degree ceiling (gap (e)), and a candidate second inequality
   205	
   206	### 4.1 Why every H2 closure is an interval
   207	
   208	Restating `H2` precisely, as the packet asks: `A_F` is Jelonek's
   209	non-properness set of the Keller map `F`, a curve, nonempty iff `F` is
   210	not an automorphism; `(H2)` is "`A_F` irreducible", and its complement is
   211	the reducible branch. The two are not symmetric: under `H2`, Chau
   212	`C7`/`C8` force one place at infinity on one curve, whereas reducibility
   213	forces several components through one point with the same `(d,e)` — which
   214	is why Lemma CONTACT bites there and has no `H2` analogue.
   215	
   216	Every `THEOREM PROFILE` bound is `a-1 <= (R_max(W)+beta)(2a-N)` with
   217	`N/2 < a <= N-2`: left side linear in `a`, right side linear with slope
   218	`2C`. For `C >= 1` it is eventually satisfiable; the window closes only
   219	while `C` is too small. So bounded-`N` architecture can only ever produce
   220	intervals `[3, f(C)]`, and `C` is a local invariant with no reason to
   221	shrink. Answering the packet directly: **no**, it cannot finish without a
   222	second inequality of opposite monotonicity in the same variable.
   223	
   224	### 4.2 NEW AVENUE — ANTI-MONOTONE: run the covering cage on the same `(N,a)` grid
   225	
   226	The counting fails exactly when `a -> N-2`, i.e. `D_gap` large. That is
   227	precisely the regime in which the cusp/homology instruments are
   228	strongest, because they are statements about `E = F^{-1}(A_F)`, a degree-
   229	`a` cover of a rational curve with one place at infinity:
   230	
   231	```text
   232	  HC-1        r(E) <= a                    grows with a
   233	  CENTRAL-RANK r = j - 1                   pins j from r
   234	  ORBIFOLD-CAGE s + s' = M + 2 - j         couples j to the local data
   235	  B3-COMPONENT j = orbit count of the (2.5.1) monodromy
   236	  B3-N4        E's genus and place count from the puncture signs
   237	```
   238	
   239	and, crucially, `E` is an **affine plane curve**: it embeds in `C^2`, and
   240	by `(CHAU-M)` applied to `F` its degree is capped. A degree-`a` cover of
   241	a rational one-place curve, branched only over the `<= s` singular points
   242	and infinity, has genus growing with `a` by Riemann-Hurwitz; but a curve
   243	of bounded degree in `C^2` has genus bounded by `(deg-1)(deg-2)/2`. Two
   244	bounds on the same `g(E)`, one increasing in `a` and one capped.
   245	
   246	```text
   247	AVENUE ANTI-MONOTONE.  For each N, tabulate
   248	   (i)  the counting-admissible a-range from THEOREM PROFILE, and
   249	   (ii) the covering-admissible a-range from Riemann-Hurwitz on
   250	        E -> A_F together with the genus cap from deg E <= (a bound
   251	        via CHAU-M and deg F).
   252	   A crossing at some N would be the campaign's FIRST degree-monotone
   253	   obstruction.  If the ranges never cross, the tabulation says so
   254	   explicitly and the ceiling question is answered NEGATIVELY, which is
   255	   also information the campaign does not have.
   256	```
   257	
   258	Cheapest discriminator: a spreadsheet, not a CAS. `N = 4..20`,
   259	`a in (N/2, N-2]`, `W >= 2`, the `(K)`/`(L)`/`[P3]` data from MI. It is
   260	an afternoon of desk work and it either produces the missing invariant
   261	or retires the hope, and either outcome is worth more than another
   262	bounded window.
   263	
   264	*Honest limit.* I have not verified that the genus cap on `E` is
   265	effective — the branching data of `E -> A_F` at infinity is exactly
   266	`OPEN[B3-INFINITY-RANK]`, which HF left open, and without it the
   267	Riemann-Hurwitz side is a floor with an unpinned correction. So this is
   268	an avenue with one named dependency, not a proof sketch.
   269	
   270	## 5. NEW MECHANISM — PARAM-U: decide the A2 cells for all large `U` at once
   271	
   272	### 5.1 The reframing
   273	
   274	`OPEN[A2-U-BOUND]` asks for `U <= f(e)`. Two reasons to go around it:
   275	RAY-KILL `(C5)` proves there is **no** `Z`-scaling covariance (the
   276	`kappa`-weight is forced to be both `2` and `1`), so the natural
   277	isobaric argument that would bound `U` is provably unavailable; and a
   278	bound is not what the computation needs.
   279	
   280	What the computation needs is a **depth** bound. Write `Lad_k` for the
   520	UPGRADE CARD -- OPEN/BANKED COLLISION CHECK
   521	WHAT   Before a report is sealed, extract every OPEN[...] it RAISES,
   522	       and grep the banked corpus (xmodel/*.md, AUDIT.md, notes.md) for
   523	       the OPEN's key nouns plus the quantity it asks to bound.  Emit a
   524	       COLLISIONS section listing candidate matches with file:line.
   525	       Fail-CLOSED: an empty COLLISIONS section must be explicit, never
   526	       absent (per the SECTIONED-OUTPUT repair discipline).
   527	SMALLEST USEFUL TEST
   528	       Run it retroactively over the OPENs raised on 2026-09-02 only.
   529	       PASS iff it surfaces companion-curve-alln:682-687 when fed
   530	       OPEN[DEG-AF-VS-N].  One afternoon; no new infrastructure; the
   531	       corpus is already on disk and already grep-shaped.
   532	IF IT PASSES   make it a seal-time hook, like PREFLIGHT.
   533	IF IT FAILS    the namespaces are genuinely disjoint and the right fix
   534	       is a curated OPEN register with an explicit "quantity bounded"
   535	       field -- more expensive, and now justified by a measurement.
   536	```
   537	
   538	This is strictly higher-value than the currently tracked upgrade
   539	(`PREFLIGHT` encoding faithfulness), which guards against a failure mode
   540	that has not yet cost the campaign a flagship successor. `PREFLIGHT`
   541	should continue; this should go in front of it.
   542	
   543	## 9. Idea cards (three)
   544	
   545	### CARD 1 — `CHAU-DELTA` (proof; cheapest; highest ratio)
   546	
   547	```text
   548	GOAL          Closed-form delta_infty(M,d,e); substitute into the
   549	              delta-budget; derive a beta-bound for (B2) 5..16 and a
   550	              finite (p,q),k list for (B3) at N=4.
   551	DEPENDENCIES  Chau Thm 1 / Cor 1 / Cor 2 [refs on disk]; COMPANION C6-C8
   552	              incl. its leading-form correction; MI Lemma A (one place at
   553	              infinity); HF Prop 3.2 and THEOREM B3-N4.  NO new theory.
   554	CHEAPEST      Write delta_infty for the one-place branch of a bidegree-
   555	DISCRIMINATOR (Md,Me) parametrisation at M=1 first (semigroup <d,e>,
   556	              delta = (d-1)(e-1)/2) and check the budget on the realised
   557	              (9,6,2) curve: n=9, delta_infty should reconcile with the
   558	              curve's known singularities.  One page.
   559	OUTCOMES      Budget < 2 at some N in 5..10  -> (B2) window KILLED there.
   560	              Budget finite but >= 2         -> explicit (M,d,e) residual
   561	                                               list; still a strict gain.
   562	              Reconciliation FAILS on (9,6,2)-> one of C6/C8/Lemma A is
   563	                                               mis-typed; that is itself
   564	                                               a high-value find.
   565	STOP          If delta_infty needs the full Puiseux expansion of the

exec
/bin/zsh -lc "rg -n -C 10 'OPEN\\[DEG-AF-VS-N\\]' /private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/ideation-20260902T0741Z-opus5.md" in /Users/dc/code/math/jc2
 succeeded in 0ms:
20-
21-No `charge_basis` line: this report asserts no new exit price.
22-
23-Desk work only. One PDF extraction, no CAS, no AWS, no `jc2-lean`,
24-no canonical ledger touched.
25-
26-## 1. Verdict, up front
27-
28-```text
29-(1) THE ROUND'S FIND -- an index failure, not a mathematics gap.
30:    OPEN[DEG-AF-VS-N], named by BOTH H2-branch flagships as the single
31-    highest-value successor and by MPRIME as "could not find it promoted
32-    anywhere in the record", is answered -- in the form the delta-budget
33-    needs -- by Chau Thm 1/Cor 1/Cor 2, banked and machine-corrected on
34-    the REDUCIBLE branch the same day (COMPANION clauses C6/C7/C8 and its
35-    derived M <= K).  Under H2: A_F is a polynomial image of bidegree
36-    (Md,Me) with ONE place at infinity, deg A_F = M max(d,e), and
37-    deg A_F <= max(deg P, deg Q).  Sec 3.
38-(2) AND THE SUBSTITUTION WAS NEVER MADE.  At the (9,6) degree pair the
39-    cap is saturated by D_1 alone (deg D_1 = 9 = max(9,6)), so no
40-    companion of any degree fits: OPEN[COMPANION-R0-REALISATION] looks
--
93-a proven absence of symmetry. Sec 5 goes around it.
94-
95-What the framing gets right, and I reinforce: "H2 complete at every
96-degree" is REFUTED/overpromoted. Nothing below weakens that — `(B2)` and
97-`(B3)` stay open at `5<=N<=16`, `(B1)` at `N>=17`, case `(A)` at `N>=8`.
98-
99-## 3. NEW CROSS-CONNECTION: the reducible branch already holds the H2 branch's top successor
100-
101-### 3.1 The collision
102-
103:`HORN-FLAGSHIP` Sec 5 ranks `OPEN[DEG-AF-VS-N]` first among "what would
104-compose", because the `(B3)` delta-budget
105-`delta(p,q) + sum_i t_i + delta_infty = (n-1)(n-2)/2`, `n = deg closure(A_F)`,
106:"is a budget, not a bound, because `OPEN[DEG-AF-VS-N]` is open"
107-(`horn-flagship-opus5-20260902.md:369`). `MPRIME-ALLN-H2:682` repeats it
108-for `(B2)` — "a bound `n <= f(N)` converts directly into a bound on
109-`beta`" — and adds "I could not find it promoted anywhere in the record".
110-
111-It is in the record, on the other branch, from the same day and the same
112-producer model. `COMPANION-CURVE-ALLN` banks from
113-`refs/chau2004_nonproper_value_set_arxiv_math0305088.pdf`:
114-
115-```text
116- C6  deg a_i = m_i d, deg b_i = m_i e, gcd(d,e)=1, the SAME (d,e) for
--
132-   (CHAU-N)  n = M max(d,e);  A_F = image of xi |-> (a,b) of bidegree
133-             (Md, Me); ONE place at infinity;  n <= max(deg P, deg Q).
134-```
135-
136-### 3.2 What that buys, and what it does not
137-
138-It does **not** bound `n` by the geometric degree `N`: `deg P * deg Q >= N`
139-runs the wrong way and the surplus hides at infinity. Honest retype:
140-
141-```text
142: OPEN[DEG-AF-VS-N]  RETYPE -> (i) OPEN[DEG-AF-CHAU]: ANSWERED (banked,
143-   cross-branch) -- n = M max(d,e), bidegree (Md,Me), one place at
144-   infinity, M <= K.   (ii) OPEN[N-VS-MAPDEG]: is max(deg P,deg Q)
145-   bounded by N at the reduced representative?  Different question,
146-   plausibly false, and the delta-budget does NOT need it.
147-```
148-
149-The budget needs `n` and `delta_infty`, and `(CHAU-N)` gives both as
150-functions of `(M,d,e)`: `n` directly, and `delta_infty` because `C8`
151-makes the branch at infinity a single place with Newton-Puiseux exponent
152-`d/e`, so its `delta` is the semigroup `delta` of that place. **Computing
--
392-| (l) non-termination as positive signal | **ANSWERED: NO** | Sec 5.2, from RAY-KILL's own size table. |
393-
394-## 7. Reranked bottlenecks, and the two strongest attacks
395-
396-### 7.1 Proof-side bottlenecks (reranked)
397-
398-```text
399- 1. delta-BUDGET CLOSURE.  Compute delta_infty(M,d,e) in closed form and
400-    substitute into (n-1)(n-2)/2.  Closes: a beta-bound for (B2) at
401-    5..16; a finite (p,q),k list for (B3) at N=4.  Cost: desk.
402:    [was: OPEN[DEG-AF-VS-N], ranked #1 by HF -- same slot, now reachable]
403- 2. THE CEILING.  ANTI-MONOTONE (Sec 4.2).  The only route to an
404-    unbounded-N obstruction anyone has named.
405- 3. A2 DEPTH-PROFILE + PARAM-U (Sec 5).  Retires the horn's analytic
406-    residual without a U-bound.
407- 4. (B3) CUSP-KILL REPLACEMENT through the ends of E (HF successor 2;
408-    APPROACHES row 27).  Needs OPEN[B3-INFINITY-RANK].
409- 5. CASE (A) N >= 8 GATE.  Necessary, not binding-first.
410- 6. (B1) at N >= 17.  Do not start until 1-3 are done: it is the only
411-    region where a bounded-N method is provably useless.
412-```
--
499-
500-### 8.2 Decisive experiment
501-
502-`DEPTH-PROFILE` (Sec 5.3). Minutes of CPU on already-decided cells; three
503-mutually exclusive read-outs; settles whether the A2 lane's whole
504-leading-order programme can terminate.
505-
506-### 8.3 Campaign-systems check — **UPGRADE**
507-
508-Not `NO_CHANGE`. The evidence is Sec 3.3: on **2026-09-02**, two
509:H2-branch flagships ranked `OPEN[DEG-AF-VS-N]` as their top successor,
510-one writing "I could not find it promoted anywhere in the record", while
511-the *same producer model on the same day* derived `M <= K` and
512-`sum deg D_i <= max(deg P, deg Q)` in the reducible-branch report. The
513-packet then reprinted the cap under gap (f) as a reducible-branch handle
514-and under gap (b)/(e) as an open question, in the same document, without
515-connecting them. This is not a review failure — every item was correctly
516-typed. It is an **index** failure: `OPEN[...]` tokens and banked lemmas
517-live in different namespaces and are never matched.
518-
519-```text
520-UPGRADE CARD -- OPEN/BANKED COLLISION CHECK
521-WHAT   Before a report is sealed, extract every OPEN[...] it RAISES,
522-       and grep the banked corpus (xmodel/*.md, AUDIT.md, notes.md) for
523-       the OPEN's key nouns plus the quantity it asks to bound.  Emit a
524-       COLLISIONS section listing candidate matches with file:line.
525-       Fail-CLOSED: an empty COLLISIONS section must be explicit, never
526-       absent (per the SECTIONED-OUTPUT repair discipline).
527-SMALLEST USEFUL TEST
528-       Run it retroactively over the OPENs raised on 2026-09-02 only.
529-       PASS iff it surfaces companion-curve-alln:682-687 when fed
530:       OPEN[DEG-AF-VS-N].  One afternoon; no new infrastructure; the
531-       corpus is already on disk and already grep-shaped.
532-IF IT PASSES   make it a seal-time hook, like PREFLIGHT.
533-IF IT FAILS    the namespaces are genuinely disjoint and the right fix
534-       is a curated OPEN register with an explicit "quantity bounded"
535-       field -- more expensive, and now justified by a measurement.
536-```
537-
538-This is strictly higher-value than the currently tracked upgrade
539-(`PREFLIGHT` encoding faithfulness), which guards against a failure mode
540-that has not yet cost the campaign a flagship successor. `PREFLIGHT`
--
632-on `ANTI-MONOTONE` (Sec 4.2). It is the only named route to a
633-degree-monotone obstruction, and it costs a spreadsheet.
634-
635-## 11. FALLACY-v2 audit
636-
637-```text
638-Flag/place/series.        No cv flag, place or cover series is identified
639-                          anywhere.  Sec 3 keeps deg A_F (a place-count-
640-                          free degree), the parametrisation bidegree, and
641-                          the geometric degree N strictly apart, and
642:                          Sec 3.2 splits OPEN[DEG-AF-VS-N] into two
643-                          questions precisely to stop the conflation.
644-Per-ray/exit-set charge.  No exit claim; no charge_basis line emitted.
645-Carrier/attainment.       Sec 3.3's (9,6,2) datum is REPRESENTATIVE (one
646-                          realised curve, one degree pair); it is NOT
647-                          promoted to the numerical row, and the CLAIM is
648-                          typed [D] UNREVIEWED with three named checks.
649-Pole/interior.            No pole identity used.
650-Floor/attainment.         (CHAU-M) is an inequality and is used only as
651-                          an inequality.  The delta-budget is an IDENTITY
652-                          and is called a budget, not a bound, until

collab: Wait
exec
/bin/zsh -lc "sed -n '360,500p' box/qq_oracle_jobs.py && sed -n '560,760p' box/qq_oracle_jobs.py && sed -n '760,885p' box/qq_oracle_jobs.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:

def _umul(a: UPoly, b: UPoly) -> UPoly:
    out: UPoly = {}
    for i, ci in a.items():
        for j, cj in b.items():
            deg = i + j
            prod = ci * cj
            out[deg] = out[deg] + prod if deg in out else prod
    return {d: c for d, c in out.items() if not c.is_zero()}


def _upow(a: UPoly, n: int) -> UPoly:
    if n < 0:
        raise ValueError("negative power")
    if not a:
        if n == 0:
            raise ValueError("0^0")
        return {}
    names = next(iter(a.values())).names
    result: UPoly = {0: _const(names, 1)}
    if n == 0:
        return result
    base = a
    exp = n
    while exp:
        if exp & 1:
            result = _umul(result, base)
        base = _umul(base, base)
        exp >>= 1
    return result


def _uscale(a: UPoly, mv: MV) -> UPoly:
    return {d: c * mv for d, c in a.items()}


# ---------------------------------------------------------------------------
# G86 / G96
# ---------------------------------------------------------------------------

def expand_g86(aux: Sequence[str]) -> Tuple[Tuple[str, ...], UPoly]:
    """Kill-review §2 / audit 7.1 G86 in the declared (8,6) chart."""
    names = tuple(aux) + CHART86
    one = _const(names, 1)
    p: UPoly = {
        8: one,
        5: _var(names, "B"),
        4: _var(names, "C"),
        3: _var(names, "D"),
        2: _var(names, "E"),
        1: _var(names, "F"),
        0: _var(names, "G"),
    }
    q: UPoly = {
        6: one,
        4: _var(names, "b"),
        3: _var(names, "gam"),
        2: _var(names, "d"),
        1: _var(names, "e"),
        0: _var(names, "f"),
    }
    g = _usub(_upow(p, 3), _upow(q, 4))
    p2 = _upow(p, 2)
    q2 = _upow(q, 2)
    q3 = _upow(q, 3)
    aux_set = set(aux)
    if "a22" in aux_set:
        g = _uadd(g, _uscale(_umul(p2, q), _var(names, "a22")))
    if "a20" in aux_set:
        g = _uadd(g, _uscale(_umul(p, q2), _var(names, "a20")))
    if "a18" in aux_set:
        g = _uadd(g, _uscale(q3, _var(names, "a18")))
    if "a16" in aux_set:
        g = _uadd(g, _uscale(p2, _var(names, "a16")))
    if "a14" in aux_set:
        g = _uadd(g, _uscale(_umul(p, q), _var(names, "a14")))
    if "a12" in aux_set:
        g = _uadd(g, _uscale(q2, _var(names, "a12")))
    if "a8" in aux_set:
        g = _uadd(g, _uscale(p, _var(names, "a8")))
    if "a6" in aux_set:
        g = _uadd(g, _uscale(q, _var(names, "a6")))
    return names, g


def expand_g96(aux: Sequence[str] = AUX_96) -> Tuple[Tuple[str, ...], UPoly]:
    """Audit 7.1 / codegen §5 G96 in the declared (9,6) chart."""
    names = tuple(aux) + CHART96
    one = _const(names, 1)
    p: UPoly = {
        9: one,
        7: _var(names, "A"),
        6: _var(names, "P6"),
        5: _var(names, "B"),
        4: _var(names, "P4"),
        3: _var(names, "C"),
        2: _var(names, "P2"),
        1: _var(names, "D"),
    }
    q: UPoly = {
        6: one,
        4: _var(names, "a"),
        3: _var(names, "Q3"),
        2: _var(names, "b"),
        1: _var(names, "Q1"),
    }
    g = _usub(_upow(p, 2), _upow(q, 3))
    aux_set = set(aux)
    if "a15" in aux_set:
        g = _uadd(g, _uscale(_umul(p, q), _var(names, "a15")))
    if "a12" in aux_set:
        g = _uadd(g, _uscale(_upow(q, 2), _var(names, "a12")))
    if "a9" in aux_set:
        g = _uadd(g, _uscale(p, _var(names, "a9")))
    if "a6" in aux_set:
        g = _uadd(g, _uscale(q, _var(names, "a6")))
    return names, g


def coeff_t(g: UPoly, j: int, names: Tuple[str, ...]) -> MV:
    return g.get(j, MV(names, {}))


# ---------------------------------------------------------------------------
# msolve text: render and parse (expanded unique-monomial sums)
# ---------------------------------------------------------------------------

_TOKEN_RE = re.compile(r"[A-Za-z][A-Za-z0-9]*|[0-9]+|[+\-*/^]")


def render_mv(mv: MV) -> str:
    """Expanded unique-monomial sum. Zero is ``0``. No parentheses."""
    items = [(exp, c) for exp, c in mv.terms.items() if not _is_zero(c)]
    if not items:
        return "0"
    if any(isinstance(c, QZeta) for _, c in items):
        raise TypeError("cannot emit QQ(zeta) coefficients to msolve")

    def grevlex_key(exp: Exp) -> Tuple:
        return (-sum(exp), tuple(-e for e in reversed(exp)))

                raise ValueError(f"truncated term in {source!r}")
            tok = tokens[pos]
            if tok.isdigit():
                if not leading:
                    raise ValueError(f"mid-term numeral {tok} in {source!r}")
                coeff_num = int(tok)
                pos += 1
                if pos < ntok and tokens[pos] == "/":
                    pos += 1
                    if pos >= ntok or not tokens[pos].isdigit():
                        raise ValueError(f"bad denominator in {source!r}")
                    coeff_den = int(tokens[pos])
                    if coeff_den == 0:
                        raise ValueError("zero denominator")
                    pos += 1
            elif tok[0].isalpha():
                if tok not in index:
                    raise ValueError(f"unknown identifier {tok!r} in {source!r}")
                pos += 1
                power = 1
                if pos < ntok and tokens[pos] == "^":
                    pos += 1
                    if pos >= ntok or not tokens[pos].isdigit():
                        raise ValueError(f"bad exponent of {tok} in {source!r}")
                    power = int(tokens[pos])
                    pos += 1
                exp[index[tok]] += power
            else:
                raise ValueError(f"unexpected {tok!r} in {source!r}")
            leading = False
            if pos >= ntok or tokens[pos] in ("+", "-"):
                break
            if tokens[pos] != "*":
                raise ValueError(f"expected '*', got {tokens[pos]!r} in {source!r}")
            pos += 1
        key = tuple(exp)
        value = Fraction(sign * coeff_num, coeff_den)
        terms[key] = terms.get(key, Fraction(0)) + value
    return MV(names, terms)


def closed_open_generators(spec: JobSpec) -> Tuple[Tuple[str, ...], List[str], str, MV]:
    """Return (var_names, closed msolve strings, open msolve string, open MV)."""
    if spec.family == "86":
        names, g = expand_g86(spec.aux)
    else:
        names, g = expand_g96(spec.aux)
    closed: List[str] = []
    for j in range(spec.closed_hi, spec.closed_lo - 1, -1):
        gj = coeff_t(g, j, names)
        if gj.is_zero():
            raise AssertionError(
                f"{spec.job_id}: g_{j} vanished identically; the closed "
                f"range {spec.closed_hi}..{spec.closed_lo} is a required slot"
            )
        closed.append(render_mv(gj))
    gopen = coeff_t(g, spec.open_deg, names)
    if gopen.is_zero():
        raise AssertionError(f"{spec.job_id}: g_{spec.open_deg} vanished identically")
    return names, closed, render_mv(gopen), gopen


def load_ms_generators(path: Path) -> Tuple[List[str], List[str]]:
    text = path.read_text(encoding="utf-8")
    if not text.endswith("\n"):
        raise ValueError(f"{path} is not POSIX text (missing trailing newline)")
    lines = text.splitlines()
    variables = lines[0].split(",")
    if lines[1] != "0":
        raise ValueError(f"{path} characteristic line is {lines[1]!r}, not '0'")
    gens = []
    for line in lines[2:]:
        s = line.strip()
        if s.endswith(","):
            s = s[:-1]
        gens.append(s)
    return variables, gens


def subst_mv(mv: MV, values: Mapping[str, Coeff]) -> MV:
    idx = {n: i for i, n in enumerate(mv.names)}
    pairs = [(idx[n], values[n]) for n in values if n in idx]
    out: Dict[Exp, Coeff] = {}
    for exp, coeff in mv.terms.items():
        new_exp = list(exp)
        c: Coeff = coeff
        for i, val in pairs:
            power = new_exp[i]
            new_exp[i] = 0
            if power:
                c = c * (val ** power)  # type: ignore[operator]
        key = tuple(new_exp)
        out[key] = out[key] + c if key in out else c  # type: ignore[operator]
    return MV(mv.names, out)


# ---------------------------------------------------------------------------
# Self-checks (pure Fraction / QQ(zeta); no msolve, no qqideal)
# ---------------------------------------------------------------------------

def _lines_leading_identities() -> List[str]:
    """Kill-review §2 leading coefficients (independent of the three controls)."""
    names, g = expand_g86(AUX_D)
    expected = {
        22: "a22-4*b",
        21: "3*B-4*gam",
        20: "a22*b+a20+3*C-6*b^2-4*d",
        19: "2*a22*B+a22*gam+3*D-12*b*gam-4*e",
    }
    lines = ["=== leading G86 identities (kill-review §2) ==="]
    ok = True
    for j, src in expected.items():
        got = coeff_t(g, j, names)
        want = parse_mv(src, names)
        match = got == want
        ok = ok and match
        lines.append(
            f"g_{j} == {src}: {'match' if match else 'MISMATCH'}"
        )
        if not match:
            lines.append(f"  got {render_mv(got)}")
    # g_23 identically 0 (chart identity)
    g23 = coeff_t(g, 23, names)
    z = g23.is_zero()
    ok = ok and z
    lines.append(f"g_23 identically 0: {'match' if z else 'MISMATCH'}")
    lines.append(
        "LEADING-IDENTITIES: PASS" if ok else "LEADING-IDENTITIES: FAIL"
    )
    return lines


def _lines_ms_encoding() -> List[str]:
    """Fraction closed/open generators vs hashed reviewed .ms files."""
    lines = ["=== encoding comparison vs reviewed .ms (polynomial equality) ==="]
    all_ok = True
    for spec in SPECS.values():
        path = BOX_DIR / f"{spec.ms_stem}.ms"
        ms_vars, ms_gens = load_ms_generators(path)
        names, closed, open_s, gopen = closed_open_generators(spec)
        # .ms vars = names + (u,)
        if tuple(ms_vars) != names + ("u",):
            all_ok = False
            lines.append(
                f"{spec.ms_stem}: VAR-MISMATCH ms={ms_vars} vs {names + ('u',)}"
            )
            continue
        if len(ms_gens) != len(closed) + 1:
            all_ok = False
            lines.append(
                f"{spec.ms_stem}: GEN-COUNT ms={len(ms_gens)} vs closed+open={len(closed)+1}"
            )
            continue
        ms_names = tuple(ms_vars)
        job_ok = True
        for i, (ours, theirs) in enumerate(zip(closed, ms_gens[:-1])):
            if parse_mv(ours, ms_names) != parse_mv(theirs, ms_names):
                job_ok = False
                lines.append(
                    f"{spec.ms_stem}: closed[{i}] (deg {spec.closed_hi - i}) MISMATCH"
                )
                break
        # last generator is u*gopen - 1
        u_names = ms_names
        u_exp = [0] * len(u_names)
        u_exp[u_names.index("u")] = 1
        u_poly = MV(u_names, {tuple(u_exp): Fraction(1)})
        want_open = parse_mv(open_s, u_names) * u_poly + _const(u_names, -1)
        got_open = parse_mv(ms_gens[-1], u_names)
        if want_open != got_open:
            job_ok = False
            lines.append(f"{spec.ms_stem}: open u*g_{spec.open_deg}-1 MISMATCH")
        all_ok = all_ok and job_ok
        n_closed = spec.closed_hi - spec.closed_lo + 1
        lines.append(
            f"{spec.ms_stem}: {'match' if job_ok else 'MISMATCH'} "
            f"({n_closed} closed + open g_{spec.open_deg}; "
            f"vars {len(names)}+u)"
        )
    lines.append(
        "MS-ENCODING: PASS" if all_ok else "MS-ENCODING: FAIL"
    )
    return lines


def _charged_a_point() -> Dict[str, Coeff]:
    """Audit §5 / codegen §7 (i). Chart order (B,C,D,E,F,G,b,gam,d,e,f)."""
    z = QZeta(Fraction(0), Fraction(1))
    return {
        "B": Fraction(1),
        "C": Fraction(0),
        "D": (Fraction(294) - z) / Fraction(144),
        "E": Fraction(0),
        "F": (Fraction(150) - z) / Fraction(48),
        "G": Fraction(0),
        "b": Fraction(1),
        "gam": Fraction(3, 4),
        "d": Fraction(1),
        "e": -(Fraction(138) + z) / Fraction(192),
        "f": (Fraction(2) * z - Fraction(71)) / Fraction(144),
    }
    }


def _fmt_coeff(c: object) -> str:
    if isinstance(c, QZeta):
        return str(c)
    if isinstance(c, Fraction):
        return _frac_str(c)
    return str(c)


def _eval_const(mv: MV, values: Mapping[str, Coeff]) -> Coeff:
    got = subst_mv(mv, values).as_constant()
    if got is None:
        raise AssertionError("substitution did not yield a constant")
    return got


def _lines_check_i() -> List[str]:
    """Charged A false-positive vs corrected (8,6,11): required FAIL of membership."""
    lines = ["=== (i) charged A false-positive vs corrected (8,6,11) ==="]
    names, g = expand_g86(AUX_A)
    pt = _charged_a_point()
    # g22 = a22 - 4 b; at the point b=1 so a22-4
    g22 = subst_mv(coeff_t(g, 22, names), pt)
    lines.append(f"check(i) g22 at point (in a22): {render_mv(g22)}")
    g21 = _eval_const(coeff_t(g, 21, names), pt)
    lines.append(f"check(i) g21 at point: {_fmt_coeff(g21)}")
    # raw H = G86 with auxiliaries zero
    names_h, h = expand_g86(())
    h19 = _eval_const(coeff_t(h, 19, names_h), pt)
    lines.append(f"check(i) h19 at point: {_fmt_coeff(h19)}")
    # g19 residual at a22 = 4 b = 4
    g19 = subst_mv(coeff_t(g, 19, names), {**pt, "a22": Fraction(4)})
    g19c = g19.as_constant()
    lines.append(f"check(i) g19 residual (a22=4): {_fmt_coeff(g19c)}")
    h11 = _eval_const(coeff_t(h, 11, names_h), pt)
    lines.append(f"check(i) raw h11 at point: {_fmt_coeff(h11)}")
    # membership of the corrected closed system: g19 is a closed equation
    # for (8,6,11) (closed g_22..g_12) and is the constant 11, independent
    # of the remaining auxiliaries. FAIL of membership is the required outcome.
    fail_membership = (g19c == 11) and (g21 == 0) and (h19 == 0)
    # also record the displayed h11
    want_h11 = QZeta(Fraction(59291, 18432), Fraction(-27671, 110592))
    h11_ok = h11 == want_h11
    if fail_membership and h11_ok:
        lines.append(
            "SELF-CHECK (i) charged A false-positive vs corrected (8,6,11): "
            "PASS — old A holds (g21=0, h19=0, "
            "h11=59291/18432 - 27671*zeta/110592 != 0) but corrected residual "
            "g19|a22=4b equals 11 != 0, so the point is not on V(g_22,...,g_12)"
        )
        lines.append("CHECK(i): PASS")
    else:
        lines.append(
            "SELF-CHECK (i) FAIL — "
            f"g21={_fmt_coeff(g21)} h19={_fmt_coeff(h19)} "
            f"g19={_fmt_coeff(g19c)} h11_ok={h11_ok}"
        )
        lines.append("CHECK(i): FAIL")
    return lines


def _lines_check_ii() -> List[str]:
    """(9,6,2) exhibit curve vs corrected (9,6,2) membership: required PASS."""
    lines = ["=== (ii) (9,6,2) exhibit vs corrected membership ==="]
    names, g = expand_g96()
    # q = t^6+8 t^2, p = t^9+12 t^5+24 t
    # chart: B=12, D=24, b=8, rest of mixed/A/C/... zero
    pt: Dict[str, Coeff] = {
        "A": Fraction(0), "P6": Fraction(0), "B": Fraction(12),
        "P4": Fraction(0), "C": Fraction(0), "P2": Fraction(0),
        "D": Fraction(24),
        "a": Fraction(0), "Q3": Fraction(0), "b": Fraction(8),
        "Q1": Fraction(0),
        "a15": Fraction(0), "a12": Fraction(0), "a9": Fraction(0),
        "a6": Fraction(-64),
    }
    closed_vals = []
    for j in range(16, 2, -1):  # 16..3
        closed_vals.append(_eval_const(coeff_t(g, j, names), pt))
    g2 = _eval_const(coeff_t(g, 2, names), pt)
    g1 = _eval_const(coeff_t(g, 1, names), pt)
    g0 = _eval_const(coeff_t(g, 0, names), pt)
    lines.append(
        "check(ii) closed g_16..g_3: ["
        + ", ".join(_fmt_coeff(v) for v in closed_vals)
        + "]"
    )
    lines.append(f"check(ii) g2: {_fmt_coeff(g2)}")
    lines.append(f"check(ii) g0,g1: {_fmt_coeff(g0)} {_fmt_coeff(g1)}")
    ok = all(v == 0 for v in closed_vals) and g2 == 64 and g1 == 0 and g0 == 0
    if ok:
        lines.append(
            "SELF-CHECK (ii) (9,6,2) curve q=t^6+8t^2, p=t^9+12t^5+24t vs "
            "corrected (9,6,2): PASS — aux (a15,a12,a9,a6)=(0,0,0,-64) gives "
            "g_j=0 for 3<=j<=16 and g_2=64 != 0"
        )
        lines.append("CHECK(ii): PASS")
    else:
        lines.append("SELF-CHECK (ii) FAIL")
        lines.append("CHECK(ii): FAIL")
    return lines


def _lines_check_iii() -> List[str]:
    """HF-twin (9,6,4) vs corrected 964 locus (pre-nodal): required PASS."""
    lines = ["=== (iii) HF-twin (9,6,4) vs corrected locus (pre-nodal) ==="]
    names, g = expand_g96()
    # shared constant-zero chart (audit §6 / codegen §7 (iii))
    pt: Dict[str, Coeff] = {
        "A": Fraction(3), "P6": Fraction(0), "B": Fraction(21, 4),
        "P4": Fraction(0), "C": Fraction(35, 8), "P2": Fraction(0),
        "D": Fraction(63, 32),
        "a": Fraction(2), "Q3": Fraction(0), "b": Fraction(5, 2),
        "Q1": Fraction(0),
        "a15": Fraction(0), "a12": Fraction(-9, 4), "a9": Fraction(0),
        "a6": Fraction(-27, 16),
    }
    closed_vals = []
    for j in range(16, 4, -1):  # 16..5
        closed_vals.append(_eval_const(coeff_t(g, j, names), pt))
    g4 = _eval_const(coeff_t(g, 4, names), pt)
    rest = [_eval_const(coeff_t(g, j, names), pt) for j in (3, 2, 1, 0)]
    lines.append(
        "check(iii) closed g_16..g_5: ["

collab: Wait
exec
/bin/zsh -lc 'for p in ops/open_collision.py ops/test_open_collision.py box/preflight.py ops/test_preflight.py; do if [ -e "$p" ]; then wc -c -l "$p"; fi; done' in /Users/dc/code/math/jc2
 succeeded in 0ms:
exec
/bin/zsh -lc "sed -n '35,95p' xmodel/a2-e2-p0-gate-gpt55-20260902.md 2>/dev/null || true && sed -n '590,645p' box/a2_cells_jobs.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
P = p + A C1 + A^2 C2,       Q = q + A D1 + A^2 D2,
R0 = r + A E1 + A^2 E2,      S0 = s,
H = A + A^2 Z,               chi = 2 + 4 A Z,
J(X,Y)=X_A Y_Z-X_Z Y_A,
WA(X,Y)=X_A Y-X Y_A,         WZ(X,Y)=X_Z Y-X Y_Z.
```

The coefficient comparison was `actual coefficient - SPEC display`.  For `E0`
the comparison is against the displayed left side `2(q r' - p' s)`.

```text
[A^0]Odd  - O0 display   = 0
[A^1]Odd  - O1 display   = 0
[A^2]Odd  - O2 display   = 0

[A^0]Even - E0 display   = 0
[A^1]Even - E1 display   = 0
[A^2]Even - E2 display   = -2*(2Z*b*eta*eta' + b*eta^2 + G)*q
                             = -2*q*E1
[A^3]Even - E3 display   = 0
```

Thus the charged P0 is confirmed exactly:

```text
[A^2]Even = (SPEC's displayed E2) - 2 q E1,
E1 = (b Z eta^2)' + G.
```

No other discrepancy occurs among the seven displayed residual equations.  The
same rebuild also gives `[A^3]Odd = 0` and `[A^5]Even = 0`.  SPEC's positive
control with `P=Z`, `Q=0`, `R0=0`, `S0=sum_{n=0}^J L_n A^n`, `L_0=-1`,
`L_n=-(2nZ/(2n+1))L_{n-1}` was checked at `J=1,2,3,4`: `Odd=0` and
`Even = 2 - 4(J+1)Z L_J A^(J+1)` exactly.

## 2. C32 inheritance and T1-T5 use

C32 section 6 inherits the `E2` discrepancy.  After substituting C32's T1
closed form for `C1`, `c5=0`, `D1=D2'+(3a/2b)eta G`, and `E1=E2'+G`, I compared
C32's section 6 split with SPEC's old `E2` display:

```text
(C32 E2eq|_{G=0} + Delta_2) - (SPEC old E2) = 0
corrected E2 - (C32 E2eq|_{G=0} + Delta_2) = -2 q E1.
```

The correction splits as

```text
-2 q E1 = -2b(2Z eta' + eta) eta q - 2qG.
```

So neither C32 summand is independently a corrected summand; the displayed
section 6 equation is exactly the old SPEC equation.

Dependency audit for C32's named theorems:

```text
T1: does not use E2.  It uses O2.  Unaffected by the E2 omission.
T2: does not use E2.  However, it has an independent sign/transcription fault;
    see the next paragraph.
    D1 = eta2 * phi + d * eta * G
    # C32:198--203 (C1 substitution); this is not itself EQ4.
    C1 = d * (psi * s + Fraction(1, 2) * Phi)

    # C32 (1.1), lines 99--105.
    EQ1 = (
        6 * D1 * rp
        - 4 * qp * E1
        + 2 * q * derivative(E1)
        + 4 * C1 * sp
        - 2 * derivative(C1) * s
        + 2 * kappa * Z
    )

    # C32:631--639, E2eq at G=0, with a=b=1 and d=3/2.
    E2G0 = (
        6 * Z * eta2 * (3 * eta + 4 * Z * etap) * rp
        + 2
        * (
            eta2 * q
            + 10 * Z * eta * etap * q
            + 4 * (Z ** 2) * (etap ** 2 + eta * etapp) * q
            - 6 * Z * eta2 * qp
            - 4 * (Z ** 2) * eta * etap * qp
        )
        + 12
        * Z
        * eta2
        * etap
        * (eta * etap - Z * (etap ** 2) + Z * eta * etapp)
        + 2
        * d
        * Z
        * (3 * eta * s * sp - 2 * (2 * etap + Z * etapp) * s2)
    )

    # C32:640--643, the complete live Delta_2; no leader/cap replacement.
    Delta2 = (
        6 * Z * eta2 * etap * Gp
        + 6 * eta * (eta * etap - 3 * Z * (etap ** 2) + Z * eta * etapp) * G
        + d * (eta * derivative(G2) - 4 * etap * G2)
        + 8 * d * Z * eta * G * rp
        + 4 * Z * (q * Gp - qp * G)
        + d * (Phi * s + 2 * Z * (Phi * sp - Phip * s))
    )
    EQ2 = E2G0 + Delta2

    # C32 T2, lines 249--255: all five families in the displayed order.
    Xi = (
        12 * eta3 * etap * (etap + 2 * Z * etapp)
        + 4 * d * s * (eta * sp - etap * s)
        + 8 * eta * (q * etap - qp * eta)
        + 12 * eta3 * rp
        + 12
        * eta
        * ((eta * etapp - etap ** 2) * G + eta * etap * Gp)

collab: Wait
exec
/bin/zsh -lc 'for p in ops/open_collision.py ops/test_open_collision.py box/preflight.py ops/test_preflight.py; do if [ -e "$p" ]; then wc -c -l "$p"; fi; done' in /Users/dc/code/math/jc2
 succeeded in 0ms:
codex
The source audit found the exact canary algebra already implemented in a reusable standard-library module: corrected 86A rejects on residual `g19 = 11`, while corrected 96B accepts with `g2 = 64`. I’m requiring the gate to recompute those facts, not accept self-reported booleans; the default CLI will also enforce the pinned toolchain while tests inject a mock probe.
collab: Wait
exec
/bin/zsh -lc "wc -c -l ops/open_collision.py ops/test_open_collision.py && sed -n '1,320p' ops/open_collision.py && sed -n '1,320p' ops/test_open_collision.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
     444   14189 ops/open_collision.py
     105    4084 ops/test_open_collision.py
     549   18273 total
#!/usr/bin/env python3
"""Find possible collisions between newly raised OPENs and banked reports.

The tool is deliberately lexical: a hit is a review candidate, never a claim
that an OPEN is closed.  It reads one report, extracts OPEN entries under an
explicit ``OPEN(S) RAISED`` or ``OPEN(S) OPENED`` heading, and scans the banked
corpus for relation-bearing lines whose local context shares the OPEN's key
nouns and bounded-quantity vocabulary.

The rendered Markdown always contains a ``COLLISIONS`` section.  Read or
contract errors produce an ERROR block and a non-zero exit instead of a
partial or silently empty result.
"""

from __future__ import annotations

import argparse
from dataclasses import dataclass
from pathlib import Path
import re
import sys
from typing import Iterable, Sequence


OPEN_RE = re.compile(r"OPEN\[([^]\r\n]+)\]")
RAISED_RE = re.compile(
    r"\bOPENS?\s+(?:RAISED(?:\s+OR\s+SHARPENED)?|OPENED)\b",
    re.IGNORECASE,
)
OTHER_OPEN_STATUS_RE = re.compile(
    r"\bOPENS?\s+(?:CLOSED|RESOLVED|ANSWERED|UNTOUCHED|RE-RANKED|RETIRED)\b",
    re.IGNORECASE,
)
ENTRY_RE = re.compile(
    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
)
INLINE_RAISE_RE = re.compile(r"\bRAISES?\b.{0,80}?(OPEN\[[^]\r\n]+\])", re.I)
RELATION_RE = re.compile(
    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
    r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
    r"at\s+most|at\s+least)\b",
    re.IGNORECASE,
)
WORD_RE = re.compile(r"[A-Za-z][A-Za-z0-9_]*")

# These are connective/status words, not useful collision keys.  One-letter
# symbols are also omitted: in this corpus they make searches far too broad.
STOP_TERMS = {
    "a",
    "an",
    "and",
    "are",
    "as",
    "at",
    "be",
    "bound",
    "bounded",
    "bounding",
    "bounds",
    "by",
    "candidate",
    "cap",
    "capped",
    "caps",
    "does",
    "for",
    "from",
    "has",
    "have",
    "in",
    "into",
    "is",
    "it",
    "its",
    "none",
    "of",
    "on",
    "open",
    "opened",
    "or",
    "quantity",
    "raised",
    "raises",
    "than",
    "that",
    "the",
    "this",
    "to",
    "versus",
    "vs",
    "what",
    "which",
    "with",
}
REQUIRED_TOP_LEVEL = ("AUDIT.md", "notes.md", "APPROACHES.md")
DEFAULT_CONTEXT_LINES = 8


class CollisionError(RuntimeError):
    """The scan could not satisfy its fail-closed input contract."""


@dataclass(frozen=True)
class OpenQuestion:
    identifier: str
    report_line: int
    description: str
    key_terms: frozenset[str]
    quantity_terms: frozenset[str]

    @property
    def token(self) -> str:
        return f"OPEN[{self.identifier}]"


@dataclass(frozen=True)
class Collision:
    identifier: str
    path: str
    line: int
    text: str


def _canonical_term(word: str) -> str:
    term = word.replace("_", "").casefold()
    if term in {"degree", "degrees"}:
        return "deg"
    return term


def _terms(text: str) -> frozenset[str]:
    values = {_canonical_term(word) for word in WORD_RE.findall(text)}
    return frozenset(
        term for term in values if len(term) >= 2 and term not in STOP_TERMS
    )


def _identifier_terms(identifier: str) -> frozenset[str]:
    return _terms(" ".join(re.split(r"[^A-Za-z0-9_]+", identifier)))


def _quantity_terms(description: str) -> frozenset[str]:
    fragments = [
        fragment
        for fragment in re.split(r"(?<=[.;])\s+|\n+", description)
        if RELATION_RE.search(fragment)
    ]
    return _terms(" ".join(fragments))


def _section_boundary(line: str) -> bool:
    stripped = line.strip()
    if not stripped:
        return False
    if re.match(r"^#{1,6}\s", stripped):
        return True
    if re.match(r"^\*\*[^*].*\*\*(?:\s|$)", stripped):
        return True
    return bool(re.match(r"^[A-Z][A-Z0-9 _/()'-]{2,}\s{2,}\S", line))


def extract_raised_opens(report: str) -> tuple[OpenQuestion, ...]:
    """Extract entries explicitly presented as raised/opened by ``report``."""

    entries: list[tuple[str, int, str]] = []
    active = False
    current_identifier: str | None = None
    current_line = 0
    current_text: list[str] = []

    def finish_entry() -> None:
        nonlocal current_identifier, current_line, current_text
        if current_identifier is not None:
            entries.append(
                (
                    current_identifier,
                    current_line,
                    " ".join(part.strip() for part in current_text if part.strip()),
                )
            )
        current_identifier = None
        current_line = 0
        current_text = []

    def start_entry(token_text: str, lineno: int, description: str) -> None:
        nonlocal current_identifier, current_line, current_text
        finish_entry()
        match = OPEN_RE.search(token_text)
        if match is None:  # guarded by callers; retain a fail-closed invariant
            raise CollisionError(f"line {lineno}: malformed OPEN token")
        identifier = match.group(1).strip()
        if not identifier or len(identifier) > 160 or any(
            ord(char) < 32 for char in identifier
        ):
            raise CollisionError(f"line {lineno}: invalid OPEN identifier")
        current_identifier = identifier
        current_line = lineno
        current_text = [description]

    for lineno, line in enumerate(report.splitlines(), start=1):
        raised = RAISED_RE.search(line)
        if raised is not None:
            finish_entry()
            active = True
            tail = line[raised.end() :]
            token = OPEN_RE.search(tail)
            if token is not None:
                start_entry(token.group(0), lineno, tail[token.start() :])
            continue

        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
            finish_entry()
            active = False

        if active:
            entry = ENTRY_RE.match(line)
            if entry is not None:
                start_entry(entry.group(1), lineno, line[entry.start(1) :])
            elif current_identifier is not None:
                current_text.append(line)
            continue

        inline = INLINE_RAISE_RE.search(line)
        if inline is not None:
            start_entry(inline.group(1), lineno, line[inline.start(1) :])
            finish_entry()

    finish_entry()

    # Keep first-seen report order but merge repeated raised entries so one OPEN
    # cannot produce duplicate candidate lists.
    merged: dict[str, tuple[int, list[str]]] = {}
    for identifier, lineno, description in entries:
        if identifier not in merged:
            merged[identifier] = (lineno, [description])
        else:
            merged[identifier][1].append(description)

    questions: list[OpenQuestion] = []
    for identifier, (lineno, descriptions) in merged.items():
        description = " ".join(descriptions)
        key_terms = _identifier_terms(identifier)
        quantity_terms = _quantity_terms(description) or key_terms
        if not key_terms and not quantity_terms:
            raise CollisionError(
                f"{identifier!r} at report line {lineno} has no safe search terms"
            )
        questions.append(
            OpenQuestion(
                identifier,
                lineno,
                description,
                key_terms,
                quantity_terms,
            )
        )
    return tuple(questions)


def _read_utf8(path: Path, label: str) -> str:
    try:
        if path.is_symlink() or not path.is_file():
            raise OSError("not a regular non-symlink file")
        return path.read_text(encoding="utf-8")
    except (OSError, UnicodeError) as exc:
        raise CollisionError(f"cannot read {label} {path}: {exc}") from exc


def banked_corpus(root: Path, report: Path) -> tuple[Path, ...]:
    """Resolve the exact banked corpus, excluding the report being sealed."""

    try:
        root = root.resolve(strict=True)
        report_real = report.resolve(strict=True)
    except OSError as exc:
        raise CollisionError(f"cannot resolve root/report: {exc}") from exc

    xmodel = root / "xmodel"
    if xmodel.is_symlink() or not xmodel.is_dir():
        raise CollisionError(f"banked corpus directory is unavailable: {xmodel}")

    paths = list(xmodel.glob("*.md"))
    if not paths:
        raise CollisionError(f"banked corpus has no xmodel/*.md files: {xmodel}")
    paths.extend(root / name for name in REQUIRED_TOP_LEVEL)

    result: list[Path] = []
    for path in sorted(paths, key=lambda item: item.as_posix()):
        try:
            resolved = path.resolve(strict=True)
        except OSError as exc:
            raise CollisionError(f"cannot resolve corpus path {path}: {exc}") from exc
        if resolved == report_real:
            continue
        if path.is_symlink() or not path.is_file():
            raise CollisionError(f"corpus entry is not a regular non-symlink file: {path}")
        result.append(path)
    return tuple(result)


def find_collisions(
    questions: Sequence[OpenQuestion],
    corpus: Iterable[Path],
    root: Path,
    *,
    context_lines: int = DEFAULT_CONTEXT_LINES,
) -> tuple[Collision, ...]:
    """Return deterministic lexical candidates; no result implies closure."""

    if context_lines < 0 or context_lines > 50:
        raise CollisionError("context_lines must be between 0 and 50")
    try:
        root_real = root.resolve(strict=True)
    except OSError as exc:
        raise CollisionError(f"cannot resolve corpus root {root}: {exc}") from exc

    matches: list[Collision] = []
    for path in corpus:
        text = _read_utf8(path, "corpus file")
        lines = text.splitlines()
#!/usr/bin/env python3
"""Focused regression tests for the OPEN/banked collision hook."""

from __future__ import annotations

from pathlib import Path
import re
import subprocess
import sys
import tempfile
import unittest


TOOL = Path(__file__).with_name("open_collision.py")


class OpenCollisionTests(unittest.TestCase):
    def setUp(self) -> None:
        self.temporary = tempfile.TemporaryDirectory()
        self.addCleanup(self.temporary.cleanup)
        self.root = Path(self.temporary.name)
        (self.root / "xmodel").mkdir()
        for name in ("AUDIT.md", "notes.md", "APPROACHES.md"):
            (self.root / name).write_text("banked control\n", encoding="utf-8")

    def run_tool(self, report_text: str) -> subprocess.CompletedProcess[str]:
        report = self.root / "report.md"
        report.write_text(report_text, encoding="utf-8")
        return subprocess.run(
            [sys.executable, str(TOOL), str(report), "--root", str(self.root)],
            text=True,
            capture_output=True,
            timeout=10,
            check=False,
        )

    def test_deg_af_retroactive_case_surfaces_all_three_banked_lines(self) -> None:
        """Reproduce the coordinator's 2026-09-02 smallest useful test."""

        # Exact source lines and positions from
        # xmodel/companion-curve-alln-opus5-20260902.md:677,685,783,785,833,836.
        lines = ["banked filler"] * 836
        lines[676] = (
            "3. A new one, typed here: even given a representation and a source, "
            "`A_F` must be"
        )
        lines[684] = (
            "`sum_i k_i m_i <= K` and `sum_i deg D_i <= max(deg P, deg Q)`. "
            "Nothing is bounded"
        )
        lines[782] = (
            "**OPENs opened.** `OPEN[COMPANION-R0-REALISATION]`: `A_F` must be "
            "the zero set of"
        )
        lines[784] = (
            "`M <= K = deg P/d`, i.e. `sum_i deg D_i <= max(deg P, deg Q)`. "
            "This is the exact"
        )
        lines[832] = (
            "*Raw remainder degree / variable-ring map.* Every degree statement "
            "about `A_F` is"
        )
        lines[835] = (
            "`sum_i deg D_i <= max(deg P, deg Q)`, is flagged as gauge-dependent "
            "on the `x,y`"
        )
        fixture = self.root / "xmodel" / "companion-curve-alln-opus5-20260902.md"
        fixture.write_text("\n".join(lines) + "\n", encoding="utf-8")

        result = self.run_tool(
            "OPENS RAISED      OPEN[DEG-AF-VS-N]  bounded quantity: "
            "sum_i deg D_i <= max(deg P, deg Q)\n"
        )
        self.assertEqual(result.returncode, 0, result.stderr)
        self.assertIn("## COLLISIONS\n", result.stdout)
        self.assertIn("status: CANDIDATES", result.stdout)
        positions = re.findall(
            r"companion-curve-alln-opus5-20260902\.md:(\d+)", result.stdout
        )
        self.assertEqual(positions, ["685", "785", "836"])

    def test_no_candidate_emits_explicit_empty_collisions_block(self) -> None:
        (self.root / "xmodel" / "unrelated.md").write_text(
            "The ordinary degree satisfies d <= 12.\n", encoding="utf-8"
        )
        result = self.run_tool(
            "**OPENs raised.**\n\n"
            "* `OPEN[QUASIFOAM-MERIDIAN-COUNT]` — bound the quasifoam "
            "meridian count.\n"
        )
        self.assertEqual(result.returncode, 0, result.stderr)
        self.assertIn("## COLLISIONS\n\nstatus: EMPTY", result.stdout)
        self.assertIn("`OPEN[QUASIFOAM-MERIDIAN-COUNT]` (report:3): NONE", result.stdout)

    def test_missing_required_bank_file_fails_closed_with_a_block(self) -> None:
        (self.root / "xmodel" / "bank.md").write_text("x <= y\n", encoding="utf-8")
        (self.root / "APPROACHES.md").unlink()
        result = self.run_tool("No OPEN is raised by this report.\n")
        self.assertEqual(result.returncode, 2)
        self.assertIn("## COLLISIONS\n\nstatus: ERROR", result.stdout)
        self.assertIn("APPROACHES.md", result.stdout)


if __name__ == "__main__":
    unittest.main()

exec
/bin/zsh -lc "sed -n '250,500p' ops/open_collision.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
                identifier,
                lineno,
                description,
                key_terms,
                quantity_terms,
            )
        )
    return tuple(questions)


def _read_utf8(path: Path, label: str) -> str:
    try:
        if path.is_symlink() or not path.is_file():
            raise OSError("not a regular non-symlink file")
        return path.read_text(encoding="utf-8")
    except (OSError, UnicodeError) as exc:
        raise CollisionError(f"cannot read {label} {path}: {exc}") from exc


def banked_corpus(root: Path, report: Path) -> tuple[Path, ...]:
    """Resolve the exact banked corpus, excluding the report being sealed."""

    try:
        root = root.resolve(strict=True)
        report_real = report.resolve(strict=True)
    except OSError as exc:
        raise CollisionError(f"cannot resolve root/report: {exc}") from exc

    xmodel = root / "xmodel"
    if xmodel.is_symlink() or not xmodel.is_dir():
        raise CollisionError(f"banked corpus directory is unavailable: {xmodel}")

    paths = list(xmodel.glob("*.md"))
    if not paths:
        raise CollisionError(f"banked corpus has no xmodel/*.md files: {xmodel}")
    paths.extend(root / name for name in REQUIRED_TOP_LEVEL)

    result: list[Path] = []
    for path in sorted(paths, key=lambda item: item.as_posix()):
        try:
            resolved = path.resolve(strict=True)
        except OSError as exc:
            raise CollisionError(f"cannot resolve corpus path {path}: {exc}") from exc
        if resolved == report_real:
            continue
        if path.is_symlink() or not path.is_file():
            raise CollisionError(f"corpus entry is not a regular non-symlink file: {path}")
        result.append(path)
    return tuple(result)


def find_collisions(
    questions: Sequence[OpenQuestion],
    corpus: Iterable[Path],
    root: Path,
    *,
    context_lines: int = DEFAULT_CONTEXT_LINES,
) -> tuple[Collision, ...]:
    """Return deterministic lexical candidates; no result implies closure."""

    if context_lines < 0 or context_lines > 50:
        raise CollisionError("context_lines must be between 0 and 50")
    try:
        root_real = root.resolve(strict=True)
    except OSError as exc:
        raise CollisionError(f"cannot resolve corpus root {root}: {exc}") from exc

    matches: list[Collision] = []
    for path in corpus:
        text = _read_utf8(path, "corpus file")
        lines = text.splitlines()
        line_terms = [_terms(line) for line in lines]
        try:
            display_path = path.resolve(strict=True).relative_to(root_real).as_posix()
        except (OSError, ValueError) as exc:
            raise CollisionError(f"corpus path escapes root: {path}") from exc

        for index, line in enumerate(lines):
            if not RELATION_RE.search(line):
                continue
            if OPEN_RE.search(line) or "COLLISIONS" in line:
                continue
            low = max(0, index - context_lines)
            high = min(len(lines), index + context_lines + 1)
            context = frozenset().union(*line_terms[low:high])
            on_line = line_terms[index]

            for question in questions:
                key_need = min(2, len(question.key_terms))
                if key_need and len(question.key_terms & context) < key_need:
                    continue
                quantity = question.quantity_terms
                context_need = min(2, len(quantity))
                if context_need and len(quantity & context) < context_need:
                    continue
                line_need = 2 if len(quantity) >= 3 else 1
                if len(quantity & on_line) < line_need:
                    continue
                snippet = " ".join(line.strip().split())
                if len(snippet) > 240:
                    snippet = snippet[:237] + "..."
                matches.append(
                    Collision(question.identifier, display_path, index + 1, snippet)
                )

    return tuple(
        sorted(matches, key=lambda item: (item.identifier, item.path, item.line))
    )


def render_collisions(
    questions: Sequence[OpenQuestion], collisions: Sequence[Collision]
) -> str:
    """Render the mandatory Markdown block, including explicit empty states."""

    grouped: dict[str, list[Collision]] = {question.identifier: [] for question in questions}
    for collision in collisions:
        grouped.setdefault(collision.identifier, []).append(collision)

    output = ["## COLLISIONS", ""]
    if not questions:
        output.extend(
            [
                "status: EMPTY",
                "",
                "- NONE — the report contains no explicitly raised `OPEN[...]` entries.",
            ]
        )
    else:
        output.append("status: CANDIDATES" if collisions else "status: EMPTY")
        output.append("")
        for question in questions:
            candidates = grouped.get(question.identifier, [])
            if not candidates:
                output.append(
                    f"- `{question.token}` (report:{question.report_line}): NONE"
                )
                continue
            output.append(f"### {question.token}")
            output.append("")
            for candidate in candidates:
                output.append(
                    f"- `{candidate.path}:{candidate.line}` — {candidate.text}"
                )
            output.append("")
        while output[-1] == "":
            output.pop()
    return "\n".join(output) + "\n"


def _error_block(reason: str) -> str:
    safe_reason = " ".join(reason.splitlines())
    return f"## COLLISIONS\n\nstatus: ERROR\n\n- scan failed closed: {safe_reason}\n"


def parser() -> argparse.ArgumentParser:
    value = argparse.ArgumentParser(description=__doc__)
    value.add_argument("report", type=Path, help="unsealed Markdown report")
    value.add_argument(
        "--root",
        type=Path,
        default=Path.cwd(),
        help="repository root containing xmodel/ and the banked top-level files",
    )
    value.add_argument(
        "--context-lines",
        type=int,
        default=DEFAULT_CONTEXT_LINES,
        help="lines on either side used to match key nouns (default: 8)",
    )
    return value


def main(argv: list[str] | None = None) -> int:
    args = parser().parse_args(argv)
    try:
        report_text = _read_utf8(args.report, "report")
        questions = extract_raised_opens(report_text)
        corpus = banked_corpus(args.root, args.report)
        collisions = find_collisions(
            questions,
            corpus,
            args.root,
            context_lines=args.context_lines,
        )
    except CollisionError as exc:
        print(_error_block(str(exc)), end="")
        print(f"open_collision: {exc}", file=sys.stderr)
        return 2
    print(render_collisions(questions, collisions), end="")
    return 0


if __name__ == "__main__":
    sys.exit(main())

collab: Wait
exec
/bin/zsh -lc '/usr/bin/time -p python3 -m unittest -v ops/test_open_collision.py' in /Users/dc/code/math/jc2
 succeeded in 46ms:
ok

----------------------------------------------------------------------
Ran 3 tests in 0.129s

OK
real 0.18
user 0.13
sys 0.03

collab: Wait
exec
/bin/zsh -lc 'for p in box/preflight.py ops/test_preflight.py; do if [ -e "$p" ]; then wc -c -l "$p"; fi; done' in /Users/dc/code/math/jc2
 succeeded in 0ms:
     745   28705 box/preflight.py

exec
/bin/zsh -lc "sed -n '1,260p' box/preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
#!/usr/bin/env python3
"""Fail-closed encoding preflight for ``N >= 6`` realization jobs.

The JSON contract is the output of :func:`manifest_to_dict`.  A generator
emits one manifest beside its solver ideal, then launch code runs::

    python3 box/preflight.py job.preflight.json

Exit zero means that the declared QQ/GRevLex ring and explicit ring map, the
entire reduced approximate root, every ordered ideal generator, the
Rabinowitsch open, the source ``EQ2_even`` identity, the typed output, the
built-in 86A/96B controls, and the pinned solver stack all passed.  Missing or
unknown data is an error.  No solver is launched by this module.

The exact corrected ``G86``/``G96`` expansions are reused from
``box/qq_oracle_jobs.py`` (encoding-faithfulness audit r2, section 7.1).
Only the standard library is imported at module load; qqideal and msolveio are
checked by distribution metadata, and msolve is queried only by the default
launch-stack probe.
"""
from __future__ import annotations

import argparse
import importlib.metadata
import json
import shutil
import subprocess
import sys
from dataclasses import dataclass
from enum import Enum
from fractions import Fraction
from pathlib import Path
from typing import Any, Dict, Iterable, Mapping, Optional, Sequence, Tuple

try:  # package import (tests / ``python -m box.preflight``)
    from box import qq_oracle_jobs as algebra
except ModuleNotFoundError:  # direct ``python box/preflight.py``
    import qq_oracle_jobs as algebra  # type: ignore[no-redef]


EXPECTED_PACKAGES = {"qqideal": "0.2.0", "msolveio": "0.2.1"}
EXPECTED_MSOLVE = "0.10.1"
SCHEMA_VERSION = 1


class Artifact(str, Enum):
    NUMERICAL_PROFILE = "NUMERICAL_PROFILE"
    FORMAL_EO_SERIES = "FORMAL_EO_SERIES"
    SUBSYSTEM_POINT = "SUBSYSTEM_POINT"
    FULL_EO_POINT = "FULL_EO_POINT"
    CURVE = "CURVE"
    REPRESENTATION = "REPRESENTATION"
    FULL_COVER = "FULL_COVER"
    POLYNOMIAL_PAIR = "POLYNOMIAL_PAIR"


class Attainment(str, Enum):
    NECESSARY = "NECESSARY"
    REALIZED = "REALIZED"
    ADMISSIBLE_PULLBACK = "ADMISSIBLE_PULLBACK"
    ACTUAL_MAP = "ACTUAL_MAP"
    COUNTEREXAMPLE_CERTIFIED = "COUNTEREXAMPLE_CERTIFIED"


class Arrow(str, Enum):
    EO_ALL_OPENS = "EO_ALL_OPENS"
    BOUNDARY_DATA = "BOUNDARY_DATA"
    GENERAL_FIBRE_DATA = "GENERAL_FIBRE_DATA"
    FIELD_DEGREE_FOUR = "FIELD_DEGREE_FOUR"
    SOURCE_CHART = "SOURCE_CHART"
    NONINVERTIBILITY = "NONINVERTIBILITY"
    GEOMETRIC_DEGREE = "GEOMETRIC_DEGREE"


@dataclass(frozen=True)
class TypedOutput:
    artifact: Artifact
    attainment: Attainment


@dataclass(frozen=True)
class Transition:
    source: TypedOutput
    target: TypedOutput
    source_domain: str
    target_domain: str
    source_scope: str = "GLOBAL"
    target_scope: str = "GLOBAL"
    claim: str = "POINT"
    arrows: Tuple[Arrow, ...] = ()


@dataclass(frozen=True)
class PolynomialSlot:
    name: str
    expression: str


@dataclass(frozen=True)
class RootCoefficient:
    degree: int
    expression: str


@dataclass(frozen=True)
class OpenFactor:
    name: str
    expression: str
    inverse: str
    equation_name: str


@dataclass(frozen=True)
class SourceFidelity:
    """Data needed to derive corrected EQ2_even, rather than trust a label."""

    ring_generators: Tuple[str, ...]
    old_eq2: str
    q: str
    e1: str
    encoded_eq2_even: str


@dataclass(frozen=True)
class StackObservation:
    package_versions: Mapping[str, Optional[str]]
    msolve_version: Optional[str]
    msolve_binary: Optional[str] = None


@dataclass(frozen=True)
class Manifest:
    schema_version: int
    job_id: str
    degree_n: int
    cell: str
    coefficient_field: str
    monomial_order: str
    ring_generators: Tuple[str, ...]
    map_source_order: Tuple[str, ...]
    map_images: Tuple[str, ...]
    reduced_root: Tuple[RootCoefficient, ...]
    ideal_generators: Tuple[PolynomialSlot, ...]
    generator_order: Tuple[str, ...]
    open_factors: Tuple[OpenFactor, ...]
    source_fidelity: SourceFidelity
    output: TypedOutput
    transitions: Tuple[Transition, ...]
    package_versions: Mapping[str, str]
    msolve_version: str


@dataclass(frozen=True)
class PreflightResult:
    ok: bool
    job_id: str
    checks: Tuple[str, ...]
    errors: Tuple[str, ...]
    output: Optional[TypedOutput]

    def to_dict(self) -> Dict[str, Any]:
        return {
            "ok": self.ok,
            "job_id": self.job_id,
            "checks": list(self.checks),
            "errors": list(self.errors),
            "artifact": self.output.artifact.value if self.output else None,
            "attainment": self.output.attainment.value if self.output else None,
        }


@dataclass(frozen=True)
class _Cell:
    family: str
    delta: Tuple[int, int, int]
    auxiliary: Tuple[str, ...]
    closed_hi: int
    closed_lo: int
    open_degree: int


CELLS: Mapping[str, _Cell] = {
    "86A": _Cell("86", (8, 6, 11), algebra.AUX_A, 22, 12, 11),
    "86B": _Cell("86", (8, 6, 9), algebra.AUX_B, 22, 10, 9),
    "86C": _Cell("86", (8, 6, 7), algebra.AUX_C, 22, 8, 7),
    "86D": _Cell("86", (8, 6, 3), algebra.AUX_D, 22, 4, 3),
    "96A": _Cell("96", (9, 6, 4), algebra.AUX_96, 16, 5, 4),
    "96B": _Cell("96", (9, 6, 2), algebra.AUX_96, 16, 3, 2),
}


_OUTPUTS: Mapping[Artifact, frozenset[Attainment]] = {
    Artifact.NUMERICAL_PROFILE: frozenset({Attainment.NECESSARY}),
    Artifact.FORMAL_EO_SERIES: frozenset({Attainment.NECESSARY}),
    Artifact.SUBSYSTEM_POINT: frozenset({Attainment.REALIZED}),
    Artifact.FULL_EO_POINT: frozenset(
        {Attainment.REALIZED, Attainment.ADMISSIBLE_PULLBACK}
    ),
    Artifact.CURVE: frozenset({Attainment.REALIZED}),
    Artifact.REPRESENTATION: frozenset(
        {Attainment.NECESSARY, Attainment.REALIZED}
    ),
    Artifact.FULL_COVER: frozenset({Attainment.REALIZED}),
    Artifact.POLYNOMIAL_PAIR: frozenset(
        {
            Attainment.REALIZED,
            Attainment.ACTUAL_MAP,
            Attainment.COUNTEREXAMPLE_CERTIFIED,
        }
    ),
}

_PULLBACK_ARROWS = frozenset(
    {
        Arrow.EO_ALL_OPENS,
        Arrow.BOUNDARY_DATA,
        Arrow.GENERAL_FIBRE_DATA,
        Arrow.FIELD_DEGREE_FOUR,
    }
)
_MAP_ARROWS = _PULLBACK_ARROWS | {Arrow.SOURCE_CHART}
_TRANSITIONS: Mapping[Tuple[TypedOutput, TypedOutput], frozenset[Arrow]] = {
    (
        TypedOutput(Artifact.FULL_EO_POINT, Attainment.REALIZED),
        TypedOutput(Artifact.FULL_EO_POINT, Attainment.ADMISSIBLE_PULLBACK),
    ): _PULLBACK_ARROWS,
    (
        TypedOutput(Artifact.FULL_EO_POINT, Attainment.ADMISSIBLE_PULLBACK),
        TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.ACTUAL_MAP),
    ): frozenset({Arrow.SOURCE_CHART}),
    (
        TypedOutput(Artifact.FULL_EO_POINT, Attainment.REALIZED),
        TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.ACTUAL_MAP),
    ): _MAP_ARROWS,
    (
        TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.ACTUAL_MAP),
        TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.COUNTEREXAMPLE_CERTIFIED),
    ): frozenset({Arrow.NONINVERTIBILITY, Arrow.GEOMETRIC_DEGREE}),
}


class PreflightError(ValueError):
    pass


def _expand(cell: _Cell):
    if cell.family == "86":
        return algebra.expand_g86(cell.auxiliary)
    return algebra.expand_g96(cell.auxiliary)


def _mv_constant(names: Tuple[str, ...], value: object):
    coefficient = Fraction(value)  # corrected roots are over QQ
    if not coefficient:
        return algebra.MV(names, {})
    return algebra.MV(names, {(0,) * len(names): coefficient})


def _mv_power(poly, exponent: int):
    result = _mv_constant(poly.names, 1)

exec
/bin/zsh -lc "sed -n '260,540p' box/preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
    result = _mv_constant(poly.names, 1)
    base = poly
    while exponent:
        if exponent & 1:
            result = result * base
        base = base * base
        exponent >>= 1
    return result


def _map_mv(poly, source_names, images, target_names):
    if poly.names != tuple(source_names):
        raise PreflightError("internal source-ring mismatch")
    result = _mv_constant(target_names, 0)
    for exponents, coefficient in poly.terms.items():
        term = _mv_constant(target_names, coefficient)
        for image, exponent in zip(images, exponents):
            if exponent:
                term = term * _mv_power(image, exponent)
        result = result + term
    return result


def _variable_index(poly) -> Optional[int]:
    if len(poly.terms) != 1:
        return None
    exponent, coefficient = next(iter(poly.terms.items()))
    if Fraction(coefficient) != 1 or sum(exponent) != 1:
        return None
    try:
        return exponent.index(1)
    except ValueError:
        return None


def validate_typed_output(output: TypedOutput) -> None:
    if output.attainment not in _OUTPUTS.get(output.artifact, frozenset()):
        raise PreflightError(
            f"invalid artifact/attainment pair: "
            f"{output.artifact.value}/{output.attainment.value}"
        )


def validate_transition(transition: Transition) -> None:
    """Apply the explicit, fail-closed semantic transition table."""
    validate_typed_output(transition.source)
    validate_typed_output(transition.target)
    source_domain = transition.source_domain.upper()
    target_domain = transition.target_domain.upper()
    claim = transition.claim.upper()
    if (
        source_domain == "A2"
        and target_domain == "B3"
        and claim == "KILL"
    ):
        raise PreflightError("forbidden transition: A2 cell -> B3 kill")
    if (
        claim == "TORSION"
        and transition.source_scope.upper() == "LOCAL"
        and transition.target_scope.upper() == "GLOBAL"
    ):
        raise PreflightError("forbidden transition: local torsion -> global torsion")
    if source_domain != target_domain:
        raise PreflightError("cross-domain transition has no declared object map")
    if transition.source == transition.target:
        return
    required = _TRANSITIONS.get((transition.source, transition.target))
    if required is None:
        raise PreflightError("transition is absent from the checked table")
    supplied = frozenset(transition.arrows)
    missing = required - supplied
    extra = supplied - required
    if missing or extra:
        detail = []
        if missing:
            detail.append("missing=" + ",".join(sorted(a.value for a in missing)))
        if extra:
            detail.append("unexpected=" + ",".join(sorted(a.value for a in extra)))
        raise PreflightError("transition arrow mismatch: " + " ".join(detail))


def _validate_source_fidelity(source: SourceFidelity) -> None:
    names = source.ring_generators
    if not names or len(names) != len(set(names)):
        raise PreflightError("source-fidelity ring is empty or has duplicates")
    try:
        old = algebra.parse_mv(source.old_eq2, names)
        q = algebra.parse_mv(source.q, names)
        e1 = algebra.parse_mv(source.e1, names)
        encoded = algebra.parse_mv(source.encoded_eq2_even, names)
    except (TypeError, ValueError) as exc:
        raise PreflightError(f"EQ2_even parse/ring failure: {exc}") from exc
    expected = old - 2 * q * e1
    if encoded != expected:
        difference = encoded - expected
        raise PreflightError(
            "EQ2_even source-fidelity mismatch (expected old_EQ2-2*q*E1; "
            f"exact diff {algebra.render_mv(difference)})"
        )


def _validate_manifest(manifest: Manifest, checks: list[str]) -> None:
    if manifest.schema_version != SCHEMA_VERSION:
        raise PreflightError("unsupported manifest schema")
    if manifest.degree_n < 6:
        raise PreflightError("this hard launch gate requires declared N >= 6")
    if manifest.cell not in CELLS:
        raise PreflightError(f"unknown corrected cell {manifest.cell!r}")
    if manifest.coefficient_field != "QQ" or manifest.monomial_order != "GRevLex":
        raise PreflightError("coefficient ring must be QQ with GRevLex order")

    cell = CELLS[manifest.cell]
    source_names, root = _expand(cell)
    expected_source_order = source_names + ("u",)
    if manifest.map_source_order != expected_source_order:
        raise PreflightError("ring-map source generator order mismatch")
    target_names = manifest.ring_generators
    if len(target_names) != len(expected_source_order) or len(set(target_names)) != len(target_names):
        raise PreflightError("declared coefficient-ring generator mismatch")
    try:
        images = tuple(algebra.parse_mv(expr, target_names) for expr in manifest.map_images)
    except (TypeError, ValueError) as exc:
        raise PreflightError(f"ring-map image failure: {exc}") from exc
    if len(images) != len(expected_source_order):
        raise PreflightError("ring-map image count mismatch")
    image_indices = tuple(_variable_index(image) for image in images)
    if None in image_indices or set(image_indices) != set(range(len(target_names))):
        raise PreflightError("ring map must be an explicit generator isomorphism")
    checks.append("ring-field-order-map")

    mapped_root = {
        degree: _map_mv(coefficient, source_names, images[:-1], target_names)
        for degree, coefficient in root.items()
    }
    emitted_degrees = tuple(slot.degree for slot in manifest.reduced_root)
    expected_degrees = tuple(sorted(mapped_root, reverse=True))
    if emitted_degrees != expected_degrees:
        raise PreflightError("reduced-root coefficient degree/order mismatch")
    emitted_root = {}
    for slot in manifest.reduced_root:
        try:
            emitted_root[slot.degree] = algebra.parse_mv(slot.expression, target_names)
        except (TypeError, ValueError) as exc:
            raise PreflightError(f"reduced-root coefficient parse failure: {exc}") from exc
        if emitted_root[slot.degree] != mapped_root[slot.degree]:
            raise PreflightError(f"reduced-root exact diff failed at t^{slot.degree}")
    checks.append("reduced-approximate-root")

    closed_names = tuple(
        f"g_{degree}" for degree in range(cell.closed_hi, cell.closed_lo - 1, -1)
    )
    open_name = f"open_g_{cell.open_degree}"
    expected_order = closed_names + (open_name,)
    if manifest.generator_order != expected_order:
        raise PreflightError("ideal generator-order mismatch")
    if tuple(slot.name for slot in manifest.ideal_generators) != expected_order:
        raise PreflightError("ideal emission order does not match generator_order")
    condition_by_name = {slot.name: slot.expression for slot in manifest.ideal_generators}
    if len(condition_by_name) != len(manifest.ideal_generators):
        raise PreflightError("duplicate ideal generator name")
    for degree, name in zip(
        range(cell.closed_hi, cell.closed_lo - 1, -1), closed_names
    ):
        try:
            encoded = algebra.parse_mv(condition_by_name[name], target_names)
        except (TypeError, ValueError) as exc:
            raise PreflightError(f"encoded condition {name} failed to parse: {exc}") from exc
        if encoded != emitted_root.get(degree) or encoded != mapped_root.get(degree):
            raise PreflightError(f"exact coefficient diff failed for {name}")
    checks.append("encoded-coefficient-exact-diff")

    if len(manifest.open_factors) != 1:
        raise PreflightError("exactly one corrected leading-coefficient open is required")
    opened = manifest.open_factors[0]
    if opened.name != f"g_{cell.open_degree}" or opened.equation_name != open_name:
        raise PreflightError("open-factor name/degree mismatch")
    try:
        factor = algebra.parse_mv(opened.expression, target_names)
        inverse = algebra.parse_mv(opened.inverse, target_names)
        open_equation = algebra.parse_mv(condition_by_name[open_name], target_names)
    except (TypeError, ValueError) as exc:
        raise PreflightError(f"open-factor parse/ring failure: {exc}") from exc
    expected_factor = emitted_root.get(cell.open_degree)
    if expected_factor is None or factor != expected_factor or factor.is_zero():
        raise PreflightError("open factor is not the corrected leading coefficient")
    if inverse != images[-1]:
        raise PreflightError("open inverse does not match the declared ring map")
    if open_equation != inverse * factor - 1:
        raise PreflightError("Rabinowitsch open equation mismatch")
    checks.append("open-factor-rabinowitsch")

    _validate_source_fidelity(manifest.source_fidelity)
    checks.append("source-fidelity-EQ2_even")
    validate_typed_output(manifest.output)
    for transition in manifest.transitions:
        validate_transition(transition)
    checks.append("typed-output-transitions")


def run_builtin_canaries() -> Tuple[bool, Tuple[str, ...]]:
    """Run exact 86A-negative, 96B-positive, and EQ2 omission controls."""
    messages = []
    try:
        names86, g86 = algebra.expand_g86(algebra.AUX_A)
        point86 = algebra._charged_a_point()
        residual = algebra.subst_mv(
            algebra.coeff_t(g86, 19, names86),
            {**point86, "a22": Fraction(4)},
        ).as_constant()
        if residual != 11:
            raise PreflightError(f"86A canary residual is {residual!r}, not 11")
        messages.append("86A-rejects-old-(8,6,19)")

        names96, g96 = algebra.expand_g96(algebra.AUX_96)
        point96 = {
            "A": 0, "P6": 0, "B": 12, "P4": 0, "C": 0, "P2": 0,
            "D": 24, "a": 0, "Q3": 0, "b": 8, "Q1": 0,
            "a15": 0, "a12": 0, "a9": 0, "a6": -64,
        }
        closed = [
            algebra.subst_mv(algebra.coeff_t(g96, degree, names96), point96).as_constant()
            for degree in range(16, 2, -1)
        ]
        leading = algebra.subst_mv(
            algebra.coeff_t(g96, 2, names96), point96
        ).as_constant()
        if any(value != 0 for value in closed) or leading != 64:
            raise PreflightError("96B four-node curve failed corrected membership")
        messages.append("96B-accepts-four-node-(9,6,2)")

        corrected = SourceFidelity(
            ("oldEQ2", "q", "E1"), "oldEQ2", "q", "E1",
            "oldEQ2-2*q*E1",
        )
        stale = SourceFidelity(
            ("oldEQ2", "q", "E1"), "oldEQ2", "q", "E1", "oldEQ2"
        )
        _validate_source_fidelity(corrected)
        try:
            _validate_source_fidelity(stale)
        except PreflightError:
            pass
        else:
            raise PreflightError("missing -2*q*E1 source canary was accepted")
        messages.append("EQ2_even-omission-rejected")
    except Exception as exc:
        return False, tuple(messages + [f"{type(exc).__name__}: {exc}"])
    return True, tuple(messages)


def probe_default_stack() -> StackObservation:
    packages: Dict[str, Optional[str]] = {}
    for package in EXPECTED_PACKAGES:
        try:
            packages[package] = importlib.metadata.version(package)
        except importlib.metadata.PackageNotFoundError:
            packages[package] = None
    binary = shutil.which("msolve")
    version = None
    if binary:
        try:
            completed = subprocess.run(
                [binary, "--version"], capture_output=True, text=True,
                timeout=2.0, check=False,
            )
            line = (completed.stdout or completed.stderr).strip().splitlines()
            if completed.returncode == 0 and line:
                version = line[0]
        except (OSError, subprocess.SubprocessError):
            version = None
    return StackObservation(packages, version, binary)


def _validate_stack(manifest: Manifest, observed: StackObservation) -> None:
    if dict(manifest.package_versions) != EXPECTED_PACKAGES:
        raise PreflightError("manifest package pins differ from the default stack")
    if manifest.msolve_version != EXPECTED_MSOLVE:
        raise PreflightError("manifest msolve pin differs from the default stack")
    if dict(observed.package_versions) != EXPECTED_PACKAGES:
        raise PreflightError(
            f"package version gate failed: observed={dict(observed.package_versions)!r}"

exec
/bin/zsh -lc "sed -n '540,820p' box/preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
            f"package version gate failed: observed={dict(observed.package_versions)!r}"
        )
    if observed.msolve_version != EXPECTED_MSOLVE:
        raise PreflightError(
            f"msolve version gate failed: observed={observed.msolve_version!r}"
        )


def preflight(
    manifest: Manifest,
    *,
    stack_observation: Optional[StackObservation] = None,
) -> PreflightResult:
    """Validate a launch manifest; actual stack probing is the default."""
    checks: list[str] = []
    try:
        _validate_manifest(manifest, checks)
        canaries_ok, canary_messages = run_builtin_canaries()
        if not canaries_ok:
            raise PreflightError("built-in semantic canary failed: " + "; ".join(canary_messages))
        checks.extend(canary_messages)
        observed = stack_observation if stack_observation is not None else probe_default_stack()
        _validate_stack(manifest, observed)
        checks.append("default-stack-pins")
    except Exception as exc:
        return PreflightResult(
            False, getattr(manifest, "job_id", "<unparsed>"), tuple(checks),
            (f"{type(exc).__name__}: {exc}",), getattr(manifest, "output", None),
        )
    return PreflightResult(True, manifest.job_id, tuple(checks), (), manifest.output)


def _open_equation(factor: str, inverse: str, names: Tuple[str, ...]) -> str:
    fpoly = algebra.parse_mv(factor, names)
    ipoly = algebra.parse_mv(inverse, names)
    return algebra.render_mv(ipoly * fpoly - 1)


def build_reference_manifest(
    cell_name: str = "96B",
    *,
    degree_n: int = 6,
    output: TypedOutput = TypedOutput(Artifact.CURVE, Attainment.REALIZED),
) -> Manifest:
    """Construct a faithful in-memory manifest; useful to generators/tests."""
    cell = CELLS[cell_name]
    source_names, root = _expand(cell)
    target_names = source_names + ("u",)
    reduced_root = tuple(
        RootCoefficient(degree, algebra.render_mv(root[degree]))
        for degree in sorted(root, reverse=True)
    )
    ideal = []
    order = []
    for degree in range(cell.closed_hi, cell.closed_lo - 1, -1):
        name = f"g_{degree}"
        order.append(name)
        ideal.append(PolynomialSlot(name, algebra.render_mv(root[degree])))
    factor_name = f"g_{cell.open_degree}"
    factor = algebra.render_mv(root[cell.open_degree])
    equation_name = f"open_g_{cell.open_degree}"
    order.append(equation_name)
    ideal.append(
        PolynomialSlot(equation_name, _open_equation(factor, "u", target_names))
    )
    return Manifest(
        SCHEMA_VERSION, f"reference-{cell_name}", degree_n, cell_name, "QQ",
        "GRevLex", target_names, target_names, target_names, reduced_root,
        tuple(ideal), tuple(order),
        (OpenFactor(factor_name, factor, "u", equation_name),),
        SourceFidelity(
            ("oldEQ2", "q", "E1"), "oldEQ2", "q", "E1",
            "oldEQ2-2*q*E1",
        ),
        output, (), dict(EXPECTED_PACKAGES), EXPECTED_MSOLVE,
    )


def manifest_to_dict(manifest: Manifest) -> Dict[str, Any]:
    def typed(value: TypedOutput) -> Dict[str, str]:
        return {"artifact": value.artifact.value, "attainment": value.attainment.value}

    return {
        "schema_version": manifest.schema_version,
        "job_id": manifest.job_id,
        "degree_n": manifest.degree_n,
        "cell": manifest.cell,
        "coefficient_field": manifest.coefficient_field,
        "monomial_order": manifest.monomial_order,
        "ring_generators": list(manifest.ring_generators),
        "ring_map": {
            "source_order": list(manifest.map_source_order),
            "images": list(manifest.map_images),
        },
        "reduced_root": [
            {"degree": slot.degree, "expression": slot.expression}
            for slot in manifest.reduced_root
        ],
        "ideal_generators": [
            {"name": slot.name, "expression": slot.expression}
            for slot in manifest.ideal_generators
        ],
        "generator_order": list(manifest.generator_order),
        "open_factors": [vars(item) for item in manifest.open_factors],
        "source_fidelity": {
            "ring_generators": list(manifest.source_fidelity.ring_generators),
            "old_eq2": manifest.source_fidelity.old_eq2,
            "q": manifest.source_fidelity.q,
            "e1": manifest.source_fidelity.e1,
            "encoded_eq2_even": manifest.source_fidelity.encoded_eq2_even,
        },
        "output": typed(manifest.output),
        "transitions": [
            {
                "source": typed(item.source), "target": typed(item.target),
                "source_domain": item.source_domain,
                "target_domain": item.target_domain,
                "source_scope": item.source_scope, "target_scope": item.target_scope,
                "claim": item.claim,
                "arrows": [arrow.value for arrow in item.arrows],
            }
            for item in manifest.transitions
        ],
        "stack": {
            "packages": dict(manifest.package_versions),
            "msolve": manifest.msolve_version,
        },
    }


def manifest_from_dict(raw: Mapping[str, Any]) -> Manifest:
    """Parse the strict JSON contract.  Missing/extra top-level keys fail."""
    required = {
        "schema_version", "job_id", "degree_n", "cell", "coefficient_field",
        "monomial_order", "ring_generators", "ring_map", "reduced_root",
        "ideal_generators", "generator_order", "open_factors",
        "source_fidelity", "output", "transitions", "stack",
    }
    if set(raw) != required:
        raise PreflightError(
            f"manifest keys mismatch: missing={sorted(required-set(raw))} "
            f"extra={sorted(set(raw)-required)}"
        )
    try:
        output = raw["output"]
        transitions = tuple(
            Transition(
                TypedOutput(Artifact(item["source"]["artifact"]), Attainment(item["source"]["attainment"])),
                TypedOutput(Artifact(item["target"]["artifact"]), Attainment(item["target"]["attainment"])),
                str(item["source_domain"]), str(item["target_domain"]),
                str(item.get("source_scope", "GLOBAL")),
                str(item.get("target_scope", "GLOBAL")), str(item.get("claim", "POINT")),
                tuple(Arrow(value) for value in item.get("arrows", [])),
            )
            for item in raw["transitions"]
        )
        source = raw["source_fidelity"]
        ring_map = raw["ring_map"]
        stack = raw["stack"]
        return Manifest(
            int(raw["schema_version"]), str(raw["job_id"]), int(raw["degree_n"]),
            str(raw["cell"]), str(raw["coefficient_field"]),
            str(raw["monomial_order"]), tuple(raw["ring_generators"]),
            tuple(ring_map["source_order"]), tuple(ring_map["images"]),
            tuple(RootCoefficient(int(x["degree"]), str(x["expression"])) for x in raw["reduced_root"]),
            tuple(PolynomialSlot(str(x["name"]), str(x["expression"])) for x in raw["ideal_generators"]),
            tuple(raw["generator_order"]),
            tuple(OpenFactor(str(x["name"]), str(x["expression"]), str(x["inverse"]), str(x["equation_name"])) for x in raw["open_factors"]),
            SourceFidelity(
                tuple(source["ring_generators"]), str(source["old_eq2"]),
                str(source["q"]), str(source["e1"]),
                str(source["encoded_eq2_even"]),
            ),
            TypedOutput(Artifact(output["artifact"]), Attainment(output["attainment"])),
            transitions, dict(stack["packages"]), str(stack["msolve"]),
        )
    except (KeyError, TypeError, ValueError) as exc:
        raise PreflightError(f"invalid manifest structure: {exc}") from exc


def main(argv: Optional[Sequence[str]] = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("manifest", nargs="?", help="preflight JSON emitted by the generator")
    parser.add_argument(
        "--self-test", action="store_true",
        help="run the pure exact built-in canaries only (does not approve launch)",
    )
    args = parser.parse_args(argv)
    if args.self_test:
        ok, messages = run_builtin_canaries()
        print(json.dumps({"ok": ok, "checks": messages}, indent=2))
        return 0 if ok else 1
    if not args.manifest:
        parser.error("manifest is required unless --self-test is used")
    try:
        raw = json.loads(Path(args.manifest).read_text(encoding="utf-8"))
        manifest = manifest_from_dict(raw)
        result = preflight(manifest)
    except Exception as exc:
        result = PreflightResult(False, "<unparsed>", (), (f"{type(exc).__name__}: {exc}",), None)
    print(json.dumps(result.to_dict(), indent=2, sort_keys=True))
    return 0 if result.ok else 1


if __name__ == "__main__":
    raise SystemExit(main())

exec
/bin/zsh -lc 'msolve --version' in /Users/dc/code/math/jc2
 succeeded in 0ms:
0.10.1

exec
/bin/zsh -lc 'python3 box/preflight.py --self-test' in /Users/dc/code/math/jc2
 succeeded in 6ms:
{
  "ok": true,
  "checks": [
    "86A-rejects-old-(8,6,19)",
    "96B-accepts-four-node-(9,6,2)",
    "EQ2_even-omission-rejected"
  ]
}

exec
/bin/zsh -lc "if [ -e ops/test_preflight.py ]; then wc -c -l ops/test_preflight.py; sed -n '1,360p' ops/test_preflight.py; fi" in /Users/dc/code/math/jc2
 succeeded in 0ms:
     183    7529 ops/test_preflight.py
#!/usr/bin/env python3
"""Focused, solver-free tests for the N>=6 encoding preflight gate."""
from __future__ import annotations

import json
import sys
import unittest
from dataclasses import replace
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
if str(ROOT) not in sys.path:
    sys.path.insert(0, str(ROOT))

from box import preflight as gate  # noqa: E402


GOOD_STACK = gate.StackObservation(
    dict(gate.EXPECTED_PACKAGES), gate.EXPECTED_MSOLVE, "/mock/msolve"
)


class EncodingPreflightTests(unittest.TestCase):
    def run_gate(self, manifest: gate.Manifest) -> gate.PreflightResult:
        # No test invokes msolve or imports qqideal/msolveio.
        return gate.preflight(manifest, stack_observation=GOOD_STACK)

    def test_faithful_96b_emits_root_and_exact_diffs_every_condition(self) -> None:
        result = self.run_gate(gate.build_reference_manifest("96B"))
        self.assertTrue(result.ok, result.errors)
        self.assertIn("reduced-approximate-root", result.checks)
        self.assertIn("encoded-coefficient-exact-diff", result.checks)
        self.assertIn("open-factor-rabinowitsch", result.checks)
        # Corrected 96B is g_16,...,g_3=0 with g_2 open.
        manifest = gate.build_reference_manifest("96B")
        self.assertEqual(
            manifest.generator_order,
            tuple(f"g_{j}" for j in range(16, 2, -1)) + ("open_g_2",),
        )

    def test_exact_diff_rejects_root_or_ideal_coefficient_mutation(self) -> None:
        manifest = gate.build_reference_manifest("86A")
        first_root = replace(
            manifest.reduced_root[0],
            expression=manifest.reduced_root[0].expression + "+1",
        )
        bad_root = replace(
            manifest, reduced_root=(first_root,) + manifest.reduced_root[1:]
        )
        self.assertFalse(self.run_gate(bad_root).ok)

        first_condition = replace(
            manifest.ideal_generators[0],
            expression=manifest.ideal_generators[0].expression + "+1",
        )
        bad_condition = replace(
            manifest,
            ideal_generators=(first_condition,) + manifest.ideal_generators[1:],
        )
        result = self.run_gate(bad_condition)
        self.assertFalse(result.ok)
        self.assertIn("exact coefficient diff", result.errors[0])

    def test_old_8619_false_positive_is_rejected_by_corrected_86a(self) -> None:
        ok, checks = gate.run_builtin_canaries()
        self.assertTrue(ok, checks)
        self.assertIn("86A-rejects-old-(8,6,19)", checks)
        # The canary computes forced g_19=11, rather than trusting a flag.
        names, root = gate.algebra.expand_g86(gate.algebra.AUX_A)
        value = gate.algebra.subst_mv(
            gate.algebra.coeff_t(root, 19, names),
            {**gate.algebra._charged_a_point(), "a22": 4},
        ).as_constant()
        self.assertEqual(value, 11)

    def test_four_node_962_curve_passes_corrected_96b(self) -> None:
        ok, checks = gate.run_builtin_canaries()
        self.assertTrue(ok, checks)
        self.assertIn("96B-accepts-four-node-(9,6,2)", checks)

    def test_fail_closed_on_ring_map_open_and_generator_order_mismatch(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        mutations = {
            "ring": replace(manifest, coefficient_field="QQbar"),
            "map": replace(
                manifest,
                map_images=(manifest.map_images[1],) + manifest.map_images[1:],
            ),
            "open": replace(
                manifest,
                open_factors=(
                    replace(
                        manifest.open_factors[0],
                        expression=manifest.open_factors[0].expression + "+1",
                    ),
                ),
            ),
            "order": replace(
                manifest, generator_order=tuple(reversed(manifest.generator_order))
            ),
        }
        for label, malformed in mutations.items():
            with self.subTest(label=label):
                result = self.run_gate(malformed)
                self.assertFalse(result.ok)
                self.assertTrue(result.errors)

    def test_source_fidelity_omitting_minus_2qE1_fails(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        stale = replace(manifest.source_fidelity, encoded_eq2_even="oldEQ2")
        result = self.run_gate(replace(manifest, source_fidelity=stale))
        self.assertFalse(result.ok)
        self.assertIn("EQ2_even source-fidelity mismatch", result.errors[0])

    def test_n_below_six_and_stack_drift_fail_closed(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        self.assertFalse(self.run_gate(replace(manifest, degree_n=5)).ok)
        absent = gate.StackObservation(
            {"qqideal": None, "msolveio": None}, None, None
        )
        result = gate.preflight(manifest, stack_observation=absent)
        self.assertFalse(result.ok)
        self.assertIn("package version gate failed", result.errors[0])

    def test_json_contract_round_trip(self) -> None:
        manifest = gate.build_reference_manifest("86A")
        payload = json.loads(json.dumps(gate.manifest_to_dict(manifest)))
        parsed = gate.manifest_from_dict(payload)
        self.assertEqual(parsed, manifest)
        self.assertTrue(self.run_gate(parsed).ok)
        payload["misspelled_field"] = True
        with self.assertRaises(gate.PreflightError):
            gate.manifest_from_dict(payload)


class TypedTransitionTests(unittest.TestCase):
    def test_two_field_output_rejects_impossible_pair(self) -> None:
        with self.assertRaises(gate.PreflightError):
            gate.validate_typed_output(
                gate.TypedOutput(
                    gate.Artifact.NUMERICAL_PROFILE, gate.Attainment.ACTUAL_MAP
                )
            )

    def test_rejects_a2_cell_to_b3_kill(self) -> None:
        transition = gate.Transition(
            gate.TypedOutput(gate.Artifact.SUBSYSTEM_POINT, gate.Attainment.REALIZED),
            gate.TypedOutput(gate.Artifact.REPRESENTATION, gate.Attainment.NECESSARY),
            "A2", "B3", claim="KILL",
        )
        with self.assertRaisesRegex(gate.PreflightError, "A2 cell -> B3 kill"):
            gate.validate_transition(transition)

    def test_rejects_local_torsion_to_global_torsion(self) -> None:
        state = gate.TypedOutput(
            gate.Artifact.REPRESENTATION, gate.Attainment.NECESSARY
        )
        transition = gate.Transition(
            state, state, "B3", "B3", "LOCAL", "GLOBAL", "TORSION"
        )
        with self.assertRaisesRegex(
            gate.PreflightError, "local torsion -> global torsion"
        ):
            gate.validate_transition(transition)

    def test_full_eo_to_actual_map_requires_every_declared_arrow(self) -> None:
        source = gate.TypedOutput(
            gate.Artifact.FULL_EO_POINT, gate.Attainment.REALIZED
        )
        target = gate.TypedOutput(
            gate.Artifact.POLYNOMIAL_PAIR, gate.Attainment.ACTUAL_MAP
        )
        missing_source_chart = gate.Transition(
            source, target, "A2", "A2", arrows=tuple(gate._PULLBACK_ARROWS)
        )
        with self.assertRaisesRegex(gate.PreflightError, "SOURCE_CHART"):
            gate.validate_transition(missing_source_chart)
        complete = replace(missing_source_chart, arrows=tuple(gate._MAP_ARROWS))
        gate.validate_transition(complete)


if __name__ == "__main__":
    unittest.main(verbosity=2)

exec
/bin/zsh -lc '/usr/bin/time -p python3 -m unittest -v ops/test_preflight.py' in /Users/dc/code/math/jc2
 succeeded in 1118ms:
ok
test_fail_closed_on_ring_map_open_and_generator_order_mismatch (ops.test_preflight.EncodingPreflightTests.test_fail_closed_on_ring_map_open_and_generator_order_mismatch) ... ok
test_faithful_96b_emits_root_and_exact_diffs_every_condition (ops.test_preflight.EncodingPreflightTests.test_faithful_96b_emits_root_and_exact_diffs_every_condition) ... ok
test_four_node_962_curve_passes_corrected_96b (ops.test_preflight.EncodingPreflightTests.test_four_node_962_curve_passes_corrected_96b) ... ok
test_json_contract_round_trip (ops.test_preflight.EncodingPreflightTests.test_json_contract_round_trip) ... ok
test_n_below_six_and_stack_drift_fail_closed (ops.test_preflight.EncodingPreflightTests.test_n_below_six_and_stack_drift_fail_closed) ... ok
test_old_8619_false_positive_is_rejected_by_corrected_86a (ops.test_preflight.EncodingPreflightTests.test_old_8619_false_positive_is_rejected_by_corrected_86a) ... ok
test_source_fidelity_omitting_minus_2qE1_fails (ops.test_preflight.EncodingPreflightTests.test_source_fidelity_omitting_minus_2qE1_fails) ... ok
test_full_eo_to_actual_map_requires_every_declared_arrow (ops.test_preflight.TypedTransitionTests.test_full_eo_to_actual_map_requires_every_declared_arrow) ... ok
test_rejects_a2_cell_to_b3_kill (ops.test_preflight.TypedTransitionTests.test_rejects_a2_cell_to_b3_kill) ... ok
test_rejects_local_torsion_to_global_torsion (ops.test_preflight.TypedTransitionTests.test_rejects_local_torsion_to_global_torsion) ... ok
test_two_field_output_rejects_impossible_pair (ops.test_preflight.TypedTransitionTests.test_two_field_output_rejects_impossible_pair) ... ok

----------------------------------------------------------------------
Ran 12 tests in 1.163s

OK
real 1.24
user 1.18
sys 0.02

collab: Wait
collab: Wait
exec
/bin/zsh -lc "rg -n 'FULL_EO_EQUATIONS|DECLARED_QUOTIENT|TORSION' box/preflight.py ops/test_preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
ops/test_preflight.py:159:            state, state, "B3", "B3", "LOCAL", "GLOBAL", "TORSION"
box/preflight.py:66:    FULL_EO_EQUATIONS = "FULL_EO_EQUATIONS"
box/preflight.py:67:    DECLARED_QUOTIENT = "DECLARED_QUOTIENT"
box/preflight.py:223:_FULL_EO_ARROWS = frozenset({Arrow.FULL_EO_EQUATIONS, Arrow.DECLARED_QUOTIENT})
box/preflight.py:324:        "TORSION" in claim

exec
/bin/zsh -lc "sed -n '55,80p' box/preflight.py && sed -n '210,350p' box/preflight.py && tail -90 ops/test_preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:


class Attainment(str, Enum):
    NECESSARY = "NECESSARY"
    REALIZED = "REALIZED"
    ADMISSIBLE_PULLBACK = "ADMISSIBLE_PULLBACK"
    ACTUAL_MAP = "ACTUAL_MAP"
    COUNTEREXAMPLE_CERTIFIED = "COUNTEREXAMPLE_CERTIFIED"


class Arrow(str, Enum):
    FULL_EO_EQUATIONS = "FULL_EO_EQUATIONS"
    DECLARED_QUOTIENT = "DECLARED_QUOTIENT"
    EO_ALL_OPENS = "EO_ALL_OPENS"
    BOUNDARY_DATA = "BOUNDARY_DATA"
    GENERAL_FIBRE_DATA = "GENERAL_FIBRE_DATA"
    FIELD_DEGREE_FOUR = "FIELD_DEGREE_FOUR"
    SOURCE_CHART = "SOURCE_CHART"
    NONINVERTIBILITY = "NONINVERTIBILITY"
    GEOMETRIC_DEGREE = "GEOMETRIC_DEGREE"


@dataclass(frozen=True)
class TypedOutput:
    artifact: Artifact
    attainment: Attainment
            Attainment.COUNTEREXAMPLE_CERTIFIED,
        }
    ),
}

_PULLBACK_ARROWS = frozenset(
    {
        Arrow.EO_ALL_OPENS,
        Arrow.BOUNDARY_DATA,
        Arrow.GENERAL_FIBRE_DATA,
        Arrow.FIELD_DEGREE_FOUR,
    }
)
_FULL_EO_ARROWS = frozenset({Arrow.FULL_EO_EQUATIONS, Arrow.DECLARED_QUOTIENT})
_MAP_ARROWS = _PULLBACK_ARROWS | {Arrow.SOURCE_CHART}
_TRANSITIONS: Mapping[Tuple[TypedOutput, TypedOutput], frozenset[Arrow]] = {
    (
        TypedOutput(Artifact.SUBSYSTEM_POINT, Attainment.REALIZED),
        TypedOutput(Artifact.FULL_EO_POINT, Attainment.REALIZED),
    ): _FULL_EO_ARROWS,
    (
        TypedOutput(Artifact.FULL_EO_POINT, Attainment.REALIZED),
        TypedOutput(Artifact.FULL_EO_POINT, Attainment.ADMISSIBLE_PULLBACK),
    ): _PULLBACK_ARROWS,
    (
        TypedOutput(Artifact.FULL_EO_POINT, Attainment.ADMISSIBLE_PULLBACK),
        TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.ACTUAL_MAP),
    ): frozenset({Arrow.SOURCE_CHART}),
    (
        TypedOutput(Artifact.FULL_EO_POINT, Attainment.REALIZED),
        TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.ACTUAL_MAP),
    ): _MAP_ARROWS,
    (
        TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.ACTUAL_MAP),
        TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.COUNTEREXAMPLE_CERTIFIED),
    ): frozenset({Arrow.NONINVERTIBILITY, Arrow.GEOMETRIC_DEGREE}),
}


class PreflightError(ValueError):
    pass


def _expand(cell: _Cell):
    if cell.family == "86":
        return algebra.expand_g86(cell.auxiliary)
    return algebra.expand_g96(cell.auxiliary)


def _mv_constant(names: Tuple[str, ...], value: object):
    coefficient = Fraction(value)  # corrected roots are over QQ
    if not coefficient:
        return algebra.MV(names, {})
    return algebra.MV(names, {(0,) * len(names): coefficient})


def _mv_power(poly, exponent: int):
    result = _mv_constant(poly.names, 1)
    base = poly
    while exponent:
        if exponent & 1:
            result = result * base
        base = base * base
        exponent >>= 1
    return result


def _map_mv(poly, source_names, images, target_names):
    if poly.names != tuple(source_names):
        raise PreflightError("internal source-ring mismatch")
    result = _mv_constant(target_names, 0)
    for exponents, coefficient in poly.terms.items():
        term = _mv_constant(target_names, coefficient)
        for image, exponent in zip(images, exponents):
            if exponent:
                term = term * _mv_power(image, exponent)
        result = result + term
    return result


def _variable_index(poly) -> Optional[int]:
    if len(poly.terms) != 1:
        return None
    exponent, coefficient = next(iter(poly.terms.items()))
    if Fraction(coefficient) != 1 or sum(exponent) != 1:
        return None
    try:
        return exponent.index(1)
    except ValueError:
        return None


def validate_typed_output(output: TypedOutput) -> None:
    if output.attainment not in _OUTPUTS.get(output.artifact, frozenset()):
        raise PreflightError(
            f"invalid artifact/attainment pair: "
            f"{output.artifact.value}/{output.attainment.value}"
        )


def validate_transition(transition: Transition) -> None:
    """Apply the explicit, fail-closed semantic transition table."""
    validate_typed_output(transition.source)
    validate_typed_output(transition.target)
    source_domain = transition.source_domain.upper()
    target_domain = transition.target_domain.upper()
    claim = transition.claim.upper()
    if (
        source_domain == "A2"
        and target_domain == "B3"
        and claim == "KILL"
    ):
        raise PreflightError("forbidden transition: A2 cell -> B3 kill")
    if (
        "TORSION" in claim
        and transition.source_scope.upper() == "LOCAL"
        and transition.target_scope.upper() == "GLOBAL"
    ):
        raise PreflightError("forbidden transition: local torsion -> global torsion")
    if source_domain != target_domain:
        raise PreflightError("cross-domain transition has no declared object map")
    if transition.source == transition.target:
        return
    required = _TRANSITIONS.get((transition.source, transition.target))
    if required is None:
        raise PreflightError("transition is absent from the checked table")
    supplied = frozenset(transition.arrows)
    missing = required - supplied
    extra = supplied - required
    if missing or extra:
        detail = []
        if missing:
            detail.append("missing=" + ",".join(sorted(a.value for a in missing)))
        if extra:
            detail.append("unexpected=" + ",".join(sorted(a.value for a in extra)))
        raise PreflightError("transition arrow mismatch: " + " ".join(detail))


def _validate_source_fidelity(source: SourceFidelity) -> None:
    names = source.ring_generators
    if not names or len(names) != len(set(names)):
                        expression=manifest.open_factors[0].expression + "+1",
                    ),
                ),
            ),
            "order": replace(
                manifest, generator_order=tuple(reversed(manifest.generator_order))
            ),
        }
        for label, malformed in mutations.items():
            with self.subTest(label=label):
                result = self.run_gate(malformed)
                self.assertFalse(result.ok)
                self.assertTrue(result.errors)

    def test_source_fidelity_omitting_minus_2qE1_fails(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        stale = replace(manifest.source_fidelity, encoded_eq2_even="oldEQ2")
        result = self.run_gate(replace(manifest, source_fidelity=stale))
        self.assertFalse(result.ok)
        self.assertIn("EQ2_even source-fidelity mismatch", result.errors[0])

    def test_n_below_six_and_stack_drift_fail_closed(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        self.assertFalse(self.run_gate(replace(manifest, degree_n=5)).ok)
        absent = gate.StackObservation(
            {"qqideal": None, "msolveio": None}, None, None
        )
        result = gate.preflight(manifest, stack_observation=absent)
        self.assertFalse(result.ok)
        self.assertIn("package version gate failed", result.errors[0])

    def test_json_contract_round_trip(self) -> None:
        manifest = gate.build_reference_manifest("86A")
        payload = json.loads(json.dumps(gate.manifest_to_dict(manifest)))
        parsed = gate.manifest_from_dict(payload)
        self.assertEqual(parsed, manifest)
        self.assertTrue(self.run_gate(parsed).ok)
        payload["misspelled_field"] = True
        with self.assertRaises(gate.PreflightError):
            gate.manifest_from_dict(payload)


class TypedTransitionTests(unittest.TestCase):
    def test_two_field_output_rejects_impossible_pair(self) -> None:
        with self.assertRaises(gate.PreflightError):
            gate.validate_typed_output(
                gate.TypedOutput(
                    gate.Artifact.NUMERICAL_PROFILE, gate.Attainment.ACTUAL_MAP
                )
            )

    def test_rejects_a2_cell_to_b3_kill(self) -> None:
        transition = gate.Transition(
            gate.TypedOutput(gate.Artifact.SUBSYSTEM_POINT, gate.Attainment.REALIZED),
            gate.TypedOutput(gate.Artifact.REPRESENTATION, gate.Attainment.NECESSARY),
            "A2", "B3", claim="KILL",
        )
        with self.assertRaisesRegex(gate.PreflightError, "A2 cell -> B3 kill"):
            gate.validate_transition(transition)

    def test_rejects_local_torsion_to_global_torsion(self) -> None:
        state = gate.TypedOutput(
            gate.Artifact.REPRESENTATION, gate.Attainment.NECESSARY
        )
        transition = gate.Transition(
            state, state, "B3", "B3", "LOCAL", "GLOBAL", "TORSION"
        )
        with self.assertRaisesRegex(
            gate.PreflightError, "local torsion -> global torsion"
        ):
            gate.validate_transition(transition)

    def test_full_eo_to_actual_map_requires_every_declared_arrow(self) -> None:
        source = gate.TypedOutput(
            gate.Artifact.FULL_EO_POINT, gate.Attainment.REALIZED
        )
        target = gate.TypedOutput(
            gate.Artifact.POLYNOMIAL_PAIR, gate.Attainment.ACTUAL_MAP
        )
        missing_source_chart = gate.Transition(
            source, target, "A2", "A2", arrows=tuple(gate._PULLBACK_ARROWS)
        )
        with self.assertRaisesRegex(gate.PreflightError, "SOURCE_CHART"):
            gate.validate_transition(missing_source_chart)
        complete = replace(missing_source_chart, arrows=tuple(gate._MAP_ARROWS))
        gate.validate_transition(complete)


if __name__ == "__main__":
    unittest.main(verbosity=2)

collab: Wait
exec
/bin/zsh -lc "wc -l ops/test_open_collision.py ops/test_preflight.py && rg -n 'MAX_|ambiguous|multiple|malformed|quantity|subsystem' ops/open_collision.py ops/test_open_collision.py ops/test_preflight.py | tail -80" in /Users/dc/code/math/jc2
 succeeded in 0ms:
     105 ops/test_open_collision.py
     199 ops/test_preflight.py
     304 total
ops/test_preflight.py:102:        for label, malformed in mutations.items():
ops/test_preflight.py:104:                result = self.run_gate(malformed)
ops/test_preflight.py:170:    def test_subsystem_to_full_eo_requires_equations_and_quotient(self) -> None:
ops/test_open_collision.py:71:            "OPENS RAISED      OPEN[DEG-AF-VS-N]  bounded quantity: "
ops/open_collision.py:8:nouns and bounded-quantity vocabulary.
ops/open_collision.py:91:    "quantity",
ops/open_collision.py:120:    quantity_terms: frozenset[str]
ops/open_collision.py:153:def _quantity_terms(description: str) -> frozenset[str]:
ops/open_collision.py:163:        # the bounded quantity is the text after the cue.  For passive forms,
ops/open_collision.py:215:            raise CollisionError(f"line {lineno}: malformed OPEN token")
ops/open_collision.py:268:        quantity_terms = _quantity_terms(description) or key_terms
ops/open_collision.py:269:        if not key_terms and not quantity_terms:
ops/open_collision.py:279:                quantity_terms,
ops/open_collision.py:363:                # The adopted card says key nouns *plus* the bounded quantity,
ops/open_collision.py:370:                quantity = question.quantity_terms
ops/open_collision.py:371:                context_need = min(2, len(quantity))
ops/open_collision.py:372:                if context_need and len(quantity & context) < context_need:
ops/open_collision.py:374:                line_need = 2 if len(quantity) >= 3 else 1
ops/open_collision.py:375:                if len(quantity & on_line) < line_need:

exec
/bin/zsh -lc "sed -n '20,290p' ops/open_collision.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
import re
import sys
from typing import Iterable, Sequence


OPEN_RE = re.compile(r"OPEN\[([^]\r\n]+)\]")
RAISED_RE = re.compile(
    r"\bOPENS?\s+(?:RAISED(?:\s+OR\s+SHARPENED)?|OPENED)\b",
    re.IGNORECASE,
)
OTHER_OPEN_STATUS_RE = re.compile(
    r"\bOPENS?\s+(?:CLOSED|RESOLVED|ANSWERED|UNTOUCHED|RE-RANKED|RETIRED)\b",
    re.IGNORECASE,
)
ENTRY_RE = re.compile(
    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
)
INLINE_RAISE_RE = re.compile(r"\bRAISES?\b.{0,80}?(OPEN\[[^]\r\n]+\])", re.I)
BOUND_CUE_RE = re.compile(
    r"\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
    r"at\s+most|at\s+least)\b",
    re.IGNORECASE,
)
RELATION_RE = re.compile(
    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
    r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
    r"at\s+most|at\s+least)\b",
    re.IGNORECASE,
)
WORD_RE = re.compile(r"[A-Za-z][A-Za-z0-9_]*")

# These are connective/status words, not useful collision keys.  One-letter
# symbols are also omitted: in this corpus they make searches far too broad.
STOP_TERMS = {
    "a",
    "an",
    "and",
    "are",
    "as",
    "at",
    "be",
    "bound",
    "bounded",
    "bounding",
    "bounds",
    "by",
    "candidate",
    "cap",
    "capped",
    "caps",
    "does",
    "every",
    "for",
    "from",
    "has",
    "have",
    "hold",
    "holds",
    "in",
    "into",
    "is",
    "it",
    "its",
    "none",
    "of",
    "on",
    "one",
    "open",
    "opened",
    "only",
    "or",
    "quantity",
    "raised",
    "raises",
    "than",
    "that",
    "the",
    "this",
    "terms",
    "to",
    "versus",
    "vs",
    "what",
    "which",
    "with",
}
REQUIRED_TOP_LEVEL = ("AUDIT.md", "notes.md", "APPROACHES.md")
DEFAULT_CONTEXT_LINES = 8


class CollisionError(RuntimeError):
    """The scan could not satisfy its fail-closed input contract."""


@dataclass(frozen=True)
class OpenQuestion:
    identifier: str
    report_line: int
    description: str
    key_terms: frozenset[str]
    quantity_terms: frozenset[str]

    @property
    def token(self) -> str:
        return f"OPEN[{self.identifier}]"


@dataclass(frozen=True)
class Collision:
    identifier: str
    path: str
    line: int
    text: str


def _canonical_term(word: str) -> str:
    term = word.replace("_", "").casefold()
    if term in {"degree", "degrees"}:
        return "deg"
    return term


def _terms(text: str) -> frozenset[str]:
    values = {_canonical_term(word) for word in WORD_RE.findall(text)}
    return frozenset(
        term for term in values if len(term) >= 2 and term not in STOP_TERMS
    )


def _identifier_terms(identifier: str) -> frozenset[str]:
    return _terms(" ".join(re.split(r"[^A-Za-z0-9_]+", identifier)))


def _quantity_terms(description: str) -> frozenset[str]:
    for fragment in re.split(r"(?<=[.!?;])\s+|\n+", description):
        if not RELATION_RE.search(fragment):
            continue
        without_token = OPEN_RE.sub(" ", fragment)
        cue = BOUND_CUE_RE.search(without_token)
        if cue is None:
            fragments.append(without_token)
            continue
        # In the campaign's usual imperative form ("bound X in terms of Y"),
        # the bounded quantity is the text after the cue.  For passive forms,
        # retain both sides so "X is bounded by Y" does not discard X.
        after = without_token[cue.end() :]
        if re.match(r"\s+by\b", after, re.IGNORECASE):
            relevant = without_token
        else:
            relevant = after
        values = _terms(relevant)
        if values:
            return values
    return frozenset()


def _section_boundary(line: str) -> bool:
    stripped = line.strip()
    if not stripped:
        return False
    if re.match(r"^#{1,6}\s", stripped):
        return True
    if re.match(r"^\*\*[^*].*\*\*(?:\s|$)", stripped):
        return True
    return bool(re.match(r"^[A-Z][A-Z0-9 _/()'-]{2,}\s{2,}\S", line))


def extract_raised_opens(report: str) -> tuple[OpenQuestion, ...]:
    """Extract entries explicitly presented as raised/opened by ``report``."""

    entries: list[tuple[str, int, str]] = []
    active = False
    current_identifier: str | None = None
    current_line = 0
    current_text: list[str] = []

    def finish_entry() -> None:
        nonlocal current_identifier, current_line, current_text
        if current_identifier is not None:
            entries.append(
                (
                    current_identifier,
                    current_line,
                    " ".join(part.strip() for part in current_text if part.strip()),
                )
            )
        current_identifier = None
        current_line = 0
        current_text = []

    def start_entry(token_text: str, lineno: int, description: str) -> None:
        nonlocal current_identifier, current_line, current_text
        finish_entry()
        match = OPEN_RE.search(token_text)
        if match is None:  # guarded by callers; retain a fail-closed invariant
            raise CollisionError(f"line {lineno}: malformed OPEN token")
        identifier = match.group(1).strip()
        if not identifier or len(identifier) > 160 or any(
            ord(char) < 32 for char in identifier
        ):
            raise CollisionError(f"line {lineno}: invalid OPEN identifier")
        current_identifier = identifier
        current_line = lineno
        current_text = [description]

    for lineno, line in enumerate(report.splitlines(), start=1):
        raised = RAISED_RE.search(line)
        if raised is not None:
            finish_entry()
            active = True
            tail = line[raised.end() :]
            token = OPEN_RE.search(tail)
            if token is not None:
                start_entry(token.group(0), lineno, tail[token.start() :])
            continue

        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
            finish_entry()
            active = False

        if active:
            entry = ENTRY_RE.match(line)
            if entry is not None:
                start_entry(entry.group(1), lineno, line[entry.start(1) :])
            elif current_identifier is not None:
                current_text.append(line)
            continue

        inline = INLINE_RAISE_RE.search(line)
        if inline is not None:
            start_entry(inline.group(1), lineno, line[inline.start(1) :])
            finish_entry()

    finish_entry()

    # Keep first-seen report order but merge repeated raised entries so one OPEN
    # cannot produce duplicate candidate lists.
    merged: dict[str, tuple[int, list[str]]] = {}
    for identifier, lineno, description in entries:
        if identifier not in merged:
            merged[identifier] = (lineno, [description])
        else:
            merged[identifier][1].append(description)

    questions: list[OpenQuestion] = []
    for identifier, (lineno, descriptions) in merged.items():
        description = " ".join(descriptions)
        key_terms = _identifier_terms(identifier)
        quantity_terms = _quantity_terms(description) or key_terms
        if not key_terms and not quantity_terms:
            raise CollisionError(
                f"{identifier!r} at report line {lineno} has no safe search terms"
            )
        questions.append(
            OpenQuestion(
                identifier,
                lineno,
                description,
                key_terms,
                quantity_terms,
            )
        )
    return tuple(questions)


def _read_utf8(path: Path, label: str) -> str:
    try:
        if path.is_symlink() or not path.is_file():
            raise OSError("not a regular non-symlink file")
        return path.read_text(encoding="utf-8")
    except (OSError, UnicodeError) as exc:

exec
/bin/zsh -lc "sed -n '155,205p' ops/test_preflight.py && /usr/bin/time -p python3 -m unittest -v ops/test_preflight.py" in /Users/dc/code/math/jc2
 succeeded in 1026ms:
ok
test_fail_closed_on_ring_map_open_and_generator_order_mismatch (ops.test_preflight.EncodingPreflightTests.test_fail_closed_on_ring_map_open_and_generator_order_mismatch) ... ok
test_faithful_96b_emits_root_and_exact_diffs_every_condition (ops.test_preflight.EncodingPreflightTests.test_faithful_96b_emits_root_and_exact_diffs_every_condition) ... ok
test_four_node_962_curve_passes_corrected_96b (ops.test_preflight.EncodingPreflightTests.test_four_node_962_curve_passes_corrected_96b) ... ok
test_json_contract_round_trip (ops.test_preflight.EncodingPreflightTests.test_json_contract_round_trip) ... ok
test_n_below_six_and_stack_drift_fail_closed (ops.test_preflight.EncodingPreflightTests.test_n_below_six_and_stack_drift_fail_closed) ... ok
test_old_8619_false_positive_is_rejected_by_corrected_86a (ops.test_preflight.EncodingPreflightTests.test_old_8619_false_positive_is_rejected_by_corrected_86a) ... ok
test_source_fidelity_omitting_minus_2qE1_fails (ops.test_preflight.EncodingPreflightTests.test_source_fidelity_omitting_minus_2qE1_fails) ... ok
test_full_eo_to_actual_map_requires_every_declared_arrow (ops.test_preflight.TypedTransitionTests.test_full_eo_to_actual_map_requires_every_declared_arrow) ... ok
test_rejects_a2_cell_to_b3_kill (ops.test_preflight.TypedTransitionTests.test_rejects_a2_cell_to_b3_kill) ... ok
test_rejects_local_torsion_to_global_torsion (ops.test_preflight.TypedTransitionTests.test_rejects_local_torsion_to_global_torsion) ... ok
test_subsystem_to_full_eo_requires_equations_and_quotient (ops.test_preflight.TypedTransitionTests.test_subsystem_to_full_eo_requires_equations_and_quotient) ... ok
test_two_field_output_rejects_impossible_pair (ops.test_preflight.TypedTransitionTests.test_two_field_output_rejects_impossible_pair) ... ok

----------------------------------------------------------------------
Ran 13 tests in 1.103s

OK
real 1.16
user 1.14
sys 0.01

exec
/bin/zsh -lc "python3 - <<'PY'
from box import preflight as p
stack=p.StackObservation(dict(p.EXPECTED_PACKAGES),p.EXPECTED_MSOLVE,'/mock/msolve')
for cell in p.CELLS:
    result=p.preflight(p.build_reference_manifest(cell), stack_observation=stack)
    print(cell, 'PASS' if result.ok else result.errors)
PY" in /Users/dc/code/math/jc2
 succeeded in 1313ms:
86A PASS
86B PASS
86C PASS
86D PASS
96A PASS
96B PASS

collab: Wait
codex
Both focused suites are currently green (collision 3/3; preflight 13/13, each well under 10 seconds). A hostile pass caught edge cases in the collision parser—same-line multiple entries, malformed tokens, and overly broad fallback searches—so that hook is being tightened before I treat the result as final.
exec
/bin/zsh -lc "python3 - <<'PY'
from box import preflight as p
s=p.probe_default_stack()
print(dict(s.package_versions))
print(s.msolve_version)
PY" in /Users/dc/code/math/jc2
 succeeded in 0ms:
{'qqideal': None, 'msolveio': None}
0.10.1

exec
/bin/zsh -lc "rg -n '_expect|nested|bool|old_closed|h11|canary' box/preflight.py | tail -80 && sed -n '470,560p' box/preflight.py && sed -n '650,810p' box/preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
157:    ok: bool
466:def run_builtin_canaries() -> Tuple[bool, Tuple[str, ...]]:
473:        old_closed = [
482:        if any(value != 0 for value in old_closed) or old_open == 0:
497:                f"86A canary did not reduce to degree 19 with residual 11: "
532:            raise PreflightError("missing -2*q*E1 source canary was accepted")
586:        canaries_ok, canary_messages = run_builtin_canaries()
588:            raise PreflightError("built-in semantic canary failed: " + "; ".join(canary_messages))
589:        checks.extend(canary_messages)
725:    if not isinstance(value, int) or isinstance(value, bool):
        names86, g86 = algebra.expand_g86(algebra.AUX_A)
        point86 = algebra._charged_a_point()
        raw_names86, raw86 = algebra.expand_g86(())
        old_closed = [
            algebra.subst_mv(
                algebra.coeff_t(raw86, degree, raw_names86), point86
            ).as_constant()
            for degree in (21, 19, 17, 15, 13)
        ]
        old_open = algebra.subst_mv(
            algebra.coeff_t(raw86, 11, raw_names86), point86
        ).as_constant()
        if any(value != 0 for value in old_closed) or old_open == 0:
            raise PreflightError("charged (8,6,19) point is not an old-86A positive")
        corrected_point = {**point86, "a22": Fraction(4), "a20": Fraction(6)}
        corrected_top = [
            algebra.subst_mv(
                algebra.coeff_t(g86, degree, names86), corrected_point
            ).as_constant()
            for degree in (22, 21, 20)
        ]
        residual = algebra.subst_mv(
            algebra.coeff_t(g86, 19, names86),
            corrected_point,
        ).as_constant()
        if any(value != 0 for value in corrected_top) or residual != 11:
            raise PreflightError(
                f"86A canary did not reduce to degree 19 with residual 11: "
                f"top={corrected_top!r}, g19={residual!r}"
            )
        messages.append("86A-rejects-old-(8,6,19)")

        names96, g96 = algebra.expand_g96(algebra.AUX_96)
        point96 = {
            "A": 0, "P6": 0, "B": 12, "P4": 0, "C": 0, "P2": 0,
            "D": 24, "a": 0, "Q3": 0, "b": 8, "Q1": 0,
            "a15": 0, "a12": 0, "a9": 0, "a6": -64,
        }
        closed = [
            algebra.subst_mv(algebra.coeff_t(g96, degree, names96), point96).as_constant()
            for degree in range(16, 2, -1)
        ]
        leading = algebra.subst_mv(
            algebra.coeff_t(g96, 2, names96), point96
        ).as_constant()
        if any(value != 0 for value in closed) or leading != 64:
            raise PreflightError("96B four-node curve failed corrected membership")
        messages.append("96B-accepts-four-node-(9,6,2)")

        corrected = SourceFidelity(
            ("oldEQ2", "q", "E1"), "oldEQ2", "q", "E1",
            "oldEQ2-2*q*E1",
        )
        stale = SourceFidelity(
            ("oldEQ2", "q", "E1"), "oldEQ2", "q", "E1", "oldEQ2"
        )
        _validate_source_fidelity(corrected)
        try:
            _validate_source_fidelity(stale)
        except PreflightError:
            pass
        else:
            raise PreflightError("missing -2*q*E1 source canary was accepted")
        messages.append("EQ2_even-omission-rejected")
    except Exception as exc:
        return False, tuple(messages + [f"{type(exc).__name__}: {exc}"])
    return True, tuple(messages)


def probe_default_stack() -> StackObservation:
    packages: Dict[str, Optional[str]] = {}
    for package in EXPECTED_PACKAGES:
        try:
            packages[package] = importlib.metadata.version(package)
        except importlib.metadata.PackageNotFoundError:
            packages[package] = None
    binary = shutil.which("msolve")
    version = None
    if binary:
        try:
            completed = subprocess.run(
                [binary, "--version"], capture_output=True, text=True,
                timeout=2.0, check=False,
            )
            line = (completed.stdout or completed.stderr).strip().splitlines()
            if completed.returncode == 0 and line:
                version = line[0]
        except (OSError, subprocess.SubprocessError):
            version = None
    return StackObservation(packages, version, binary)


    return {
        "schema_version": manifest.schema_version,
        "job_id": manifest.job_id,
        "degree_n": manifest.degree_n,
        "cell": manifest.cell,
        "coefficient_field": manifest.coefficient_field,
        "monomial_order": manifest.monomial_order,
        "ring_generators": list(manifest.ring_generators),
        "ring_map": {
            "source_order": list(manifest.map_source_order),
            "images": list(manifest.map_images),
        },
        "reduced_root": [
            {"degree": slot.degree, "expression": slot.expression}
            for slot in manifest.reduced_root
        ],
        "ideal_generators": [
            {"name": slot.name, "expression": slot.expression}
            for slot in manifest.ideal_generators
        ],
        "generator_order": list(manifest.generator_order),
        "open_factors": [vars(item) for item in manifest.open_factors],
        "source_fidelity": {
            "ring_generators": list(manifest.source_fidelity.ring_generators),
            "old_eq2": manifest.source_fidelity.old_eq2,
            "q": manifest.source_fidelity.q,
            "e1": manifest.source_fidelity.e1,
            "encoded_eq2_even": manifest.source_fidelity.encoded_eq2_even,
        },
        "output": typed(manifest.output),
        "transitions": [
            {
                "source": typed(item.source), "target": typed(item.target),
                "source_domain": item.source_domain,
                "target_domain": item.target_domain,
                "source_scope": item.source_scope, "target_scope": item.target_scope,
                "claim": item.claim,
                "arrows": [arrow.value for arrow in item.arrows],
            }
            for item in manifest.transitions
        ],
        "stack": {
            "packages": dict(manifest.package_versions),
            "msolve": manifest.msolve_version,
        },
    }


def _json_object(value: Any, keys: Iterable[str], label: str) -> Mapping[str, Any]:
    expected = set(keys)
    if not isinstance(value, Mapping) or any(not isinstance(k, str) for k in value):
        raise PreflightError(f"{label} must be a JSON object")
    actual = set(value)
    if actual != expected:
        raise PreflightError(
            f"{label} keys mismatch: missing={sorted(expected-actual)} "
            f"extra={sorted(actual-expected)}"
        )
    return value


def _json_array(value: Any, label: str) -> list[Any]:
    if not isinstance(value, list):
        raise PreflightError(f"{label} must be a JSON array")
    return value


def _json_string(value: Any, label: str) -> str:
    if not isinstance(value, str):
        raise PreflightError(f"{label} must be a string")
    return value


def _json_integer(value: Any, label: str) -> int:
    if not isinstance(value, int) or isinstance(value, bool):
        raise PreflightError(f"{label} must be an integer")
    return value


def _json_strings(value: Any, label: str) -> Tuple[str, ...]:
    return tuple(
        _json_string(item, f"{label}[{index}]")
        for index, item in enumerate(_json_array(value, label))
    )


def _typed_from_json(value: Any, label: str) -> TypedOutput:
    item = _json_object(value, {"artifact", "attainment"}, label)
    return TypedOutput(
        Artifact(_json_string(item["artifact"], f"{label}.artifact")),
        Attainment(_json_string(item["attainment"], f"{label}.attainment")),
    )


def manifest_from_dict(raw: Mapping[str, Any]) -> Manifest:
    """Parse the strict JSON contract without primitive-value coercions."""
    required = {
        "schema_version", "job_id", "degree_n", "cell", "coefficient_field",
        "monomial_order", "ring_generators", "ring_map", "reduced_root",
        "ideal_generators", "generator_order", "open_factors",
        "source_fidelity", "output", "transitions", "stack",
    }
    top = _json_object(raw, required, "manifest")
    try:
        ring_map = _json_object(
            top["ring_map"], {"source_order", "images"}, "ring_map"
        )
        source = _json_object(
            top["source_fidelity"],
            {"ring_generators", "old_eq2", "q", "e1", "encoded_eq2_even"},
            "source_fidelity",
        )
        stack = _json_object(top["stack"], {"packages", "msolve"}, "stack")
        packages_raw = _json_object(
            stack["packages"], set(EXPECTED_PACKAGES), "stack.packages"
        )
        packages = {
            name: _json_string(packages_raw[name], f"stack.packages.{name}")
            for name in EXPECTED_PACKAGES
        }
        roots = []
        for index, value in enumerate(_json_array(top["reduced_root"], "reduced_root")):
            item = _json_object(value, {"degree", "expression"}, f"reduced_root[{index}]")
            roots.append(
                RootCoefficient(
                    _json_integer(item["degree"], f"reduced_root[{index}].degree"),
                    _json_string(item["expression"], f"reduced_root[{index}].expression"),
                )
            )
        ideals = []
        for index, value in enumerate(_json_array(top["ideal_generators"], "ideal_generators")):
            item = _json_object(value, {"name", "expression"}, f"ideal_generators[{index}]")
            ideals.append(
                PolynomialSlot(
                    _json_string(item["name"], f"ideal_generators[{index}].name"),
                    _json_string(item["expression"], f"ideal_generators[{index}].expression"),
                )
            )
        opens = []
        open_keys = {"name", "expression", "inverse", "equation_name"}
        for index, value in enumerate(_json_array(top["open_factors"], "open_factors")):
            item = _json_object(value, open_keys, f"open_factors[{index}]")
            opens.append(
                OpenFactor(*(
                    _json_string(item[name], f"open_factors[{index}].{name}")
                    for name in ("name", "expression", "inverse", "equation_name")
                ))
            )
        transitions = []
        transition_keys = {
            "source", "target", "source_domain", "target_domain",
            "source_scope", "target_scope", "claim", "arrows",
        }
        for index, value in enumerate(_json_array(top["transitions"], "transitions")):
            label = f"transitions[{index}]"
            item = _json_object(value, transition_keys, label)
            transitions.append(
                Transition(
                    _typed_from_json(item["source"], f"{label}.source"),
                    _typed_from_json(item["target"], f"{label}.target"),

exec
/bin/zsh -lc "sed -n '810,930p' box/preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
                    _typed_from_json(item["target"], f"{label}.target"),
                    _json_string(item["source_domain"], f"{label}.source_domain"),
                    _json_string(item["target_domain"], f"{label}.target_domain"),
                    _json_string(item["source_scope"], f"{label}.source_scope"),
                    _json_string(item["target_scope"], f"{label}.target_scope"),
                    _json_string(item["claim"], f"{label}.claim"),
                    tuple(
                        Arrow(value)
                        for value in _json_strings(item["arrows"], f"{label}.arrows")
                    ),
                )
            )
        return Manifest(
            _json_integer(top["schema_version"], "schema_version"),
            _json_string(top["job_id"], "job_id"),
            _json_integer(top["degree_n"], "degree_n"),
            _json_string(top["cell"], "cell"),
            _json_string(top["coefficient_field"], "coefficient_field"),
            _json_string(top["monomial_order"], "monomial_order"),
            _json_strings(top["ring_generators"], "ring_generators"),
            _json_strings(ring_map["source_order"], "ring_map.source_order"),
            _json_strings(ring_map["images"], "ring_map.images"),
            tuple(roots), tuple(ideals),
            _json_strings(top["generator_order"], "generator_order"),
            tuple(opens),
            SourceFidelity(
                _json_strings(source["ring_generators"], "source_fidelity.ring_generators"),
                _json_string(source["old_eq2"], "source_fidelity.old_eq2"),
                _json_string(source["q"], "source_fidelity.q"),
                _json_string(source["e1"], "source_fidelity.e1"),
                _json_string(source["encoded_eq2_even"], "source_fidelity.encoded_eq2_even"),
            ),
            _typed_from_json(top["output"], "output"), tuple(transitions),
            packages, _json_string(stack["msolve"], "stack.msolve"),
        )
    except (KeyError, TypeError, ValueError) as exc:
        if isinstance(exc, PreflightError):
            raise
        raise PreflightError(f"invalid manifest structure: {exc}") from exc


def main(argv: Optional[Sequence[str]] = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("manifest", nargs="?", help="preflight JSON emitted by the generator")
    parser.add_argument(
        "--self-test", action="store_true",
        help="run the pure exact built-in canaries only (does not approve launch)",
    )
    args = parser.parse_args(argv)
    if args.self_test:
        ok, messages = run_builtin_canaries()
        print(json.dumps({"ok": ok, "checks": messages}, indent=2))
        return 0 if ok else 1
    if not args.manifest:
        parser.error("manifest is required unless --self-test is used")
    try:
        raw = json.loads(Path(args.manifest).read_text(encoding="utf-8"))
        manifest = manifest_from_dict(raw)
        result = preflight(manifest)
    except Exception as exc:
        result = PreflightResult(False, "<unparsed>", (), (f"{type(exc).__name__}: {exc}",), None)
    print(json.dumps(result.to_dict(), indent=2, sort_keys=True))
    return 0 if result.ok else 1


if __name__ == "__main__":
    raise SystemExit(main())

collab: Wait
exec
/bin/zsh -lc "wc -l ops/test_open_collision.py && sed -n '20,290p' ops/open_collision.py | tail -280 && sed -n '1,240p' ops/test_open_collision.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
     160 ops/test_open_collision.py
import re
import sys
from typing import Iterable, Sequence


OPEN_RE = re.compile(r"OPEN\[([^]\r\n]+)\]")
RAISED_RE = re.compile(
    r"^\s*(?:#{1,6}\s+|\*\*)?OPENS?\s+"
    r"(?:RAISED(?:\s+OR\s+SHARPENED)?|OPENED)\b",
    re.IGNORECASE,
)
OTHER_OPEN_STATUS_RE = re.compile(
    r"^\s*(?:#{1,6}\s+|\*\*)?OPENS?\s+"
    r"(?:CLOSED|RESOLVED|ANSWERED|UNTOUCHED|RE-RANKED|RETIRED)\b",
    re.IGNORECASE,
)
ENTRY_RE = re.compile(
    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
)
INLINE_RAISE_RE = re.compile(
    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
    r"RAISES?\s*:?\s*(OPEN\[[^]\r\n]+\])",
    re.IGNORECASE,
)
BOUND_CUE_RE = re.compile(
    r"\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
    r"at\s+most|at\s+least)\b",
    re.IGNORECASE,
)
RELATION_RE = re.compile(
    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
    r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
    r"at\s+most|at\s+least)\b",
    re.IGNORECASE,
)
WORD_RE = re.compile(r"[A-Za-z][A-Za-z0-9_]*")

# These are connective/status words, not useful collision keys.  One-letter
# symbols are also omitted: in this corpus they make searches far too broad.
STOP_TERMS = {
    "a",
    "an",
    "and",
    "are",
    "as",
    "at",
    "be",
    "bound",
    "bounded",
    "bounding",
    "bounds",
    "by",
    "candidate",
    "cap",
    "capped",
    "caps",
    "does",
    "every",
    "for",
    "from",
    "has",
    "have",
    "hold",
    "holds",
    "in",
    "into",
    "is",
    "it",
    "its",
    "none",
    "of",
    "on",
    "one",
    "open",
    "opened",
    "only",
    "or",
    "quantity",
    "raised",
    "raises",
    "than",
    "that",
    "the",
    "this",
    "terms",
    "to",
    "versus",
    "vs",
    "what",
    "which",
    "with",
}
REQUIRED_TOP_LEVEL = ("AUDIT.md", "notes.md", "APPROACHES.md")
DEFAULT_CONTEXT_LINES = 8
DEFAULT_MAX_CANDIDATES = 100


class CollisionError(RuntimeError):
    """The scan could not satisfy its fail-closed input contract."""


@dataclass(frozen=True)
class OpenQuestion:
    identifier: str
    report_line: int
    description: str
    key_terms: frozenset[str]
    quantity_terms: frozenset[str]

    @property
    def token(self) -> str:
        return f"OPEN[{self.identifier}]"


@dataclass(frozen=True)
class Collision:
    identifier: str
    path: str
    line: int
    text: str


def _canonical_term(word: str) -> str:
    term = word.replace("_", "").casefold()
    if term in {"degree", "degrees"}:
        return "deg"
    return term


def _terms(text: str) -> frozenset[str]:
    values = {_canonical_term(word) for word in WORD_RE.findall(text)}
    return frozenset(
        term for term in values if len(term) >= 2 and term not in STOP_TERMS
    )


def _identifier_terms(identifier: str) -> frozenset[str]:
    return _terms(" ".join(re.split(r"[^A-Za-z0-9_]+", identifier)))


def _quantity_terms(description: str) -> frozenset[str]:
    for fragment in re.split(r"(?<=[.!?;])\s+|\n+", description):
        without_token = OPEN_RE.sub(" ", fragment)
        if not RELATION_RE.search(without_token):
            continue
        cue = BOUND_CUE_RE.search(without_token)
        if cue is None:
            values = _terms(without_token)
            if values:
                return values
            continue
        # In the campaign's usual imperative form ("bound X in terms of Y"),
        # the bounded quantity is the text after the cue.  For passive forms,
        # retain both sides so "X is bounded by Y" does not discard X.
        after = without_token[cue.end() :]
        if re.match(r"\s+by\b", after, re.IGNORECASE):
            relevant = without_token
        else:
            relevant = after
        values = _terms(relevant)
        if values:
            return values
    return frozenset()


def _section_boundary(line: str) -> bool:
    stripped = line.strip()
    if not stripped:
        return False
    if re.match(r"^#{1,6}\s", stripped):
        return True
    if re.match(r"^\*\*[^*].*\*\*(?:\s|$)", stripped):
        return True
    return bool(re.match(r"^[A-Z][A-Z0-9 _/()'-]{2,}\s{2,}\S", line))


def extract_raised_opens(report: str) -> tuple[OpenQuestion, ...]:
    """Extract entries explicitly presented as raised/opened by ``report``."""

    entries: list[tuple[str, int, str]] = []
    active = False
    current_identifier: str | None = None
    current_line = 0
    current_text: list[str] = []

    def finish_entry() -> None:
        nonlocal current_identifier, current_line, current_text
        if current_identifier is not None:
            entries.append(
                (
                    current_identifier,
                    current_line,
                    " ".join(part.strip() for part in current_text if part.strip()),
                )
            )
        current_identifier = None
        current_line = 0
        current_text = []

    def start_entry(token_text: str, lineno: int, description: str) -> None:
        nonlocal current_identifier, current_line, current_text
        finish_entry()
        match = OPEN_RE.search(token_text)
        if match is None:  # guarded by callers; retain a fail-closed invariant
            raise CollisionError(f"line {lineno}: malformed OPEN token")
        identifier = match.group(1).strip()
        if not identifier or len(identifier) > 160 or any(
            ord(char) < 32 for char in identifier
        ):
            raise CollisionError(f"line {lineno}: invalid OPEN identifier")
        current_identifier = identifier
        current_line = lineno
        current_text = [description]

    def start_entries(text: str, lineno: int) -> bool:
        """Start every same-line OPEN entry and reject broken bracket syntax."""

        tokens = list(OPEN_RE.finditer(text))
        remainder = OPEN_RE.sub("", text)
        if "OPEN[" in remainder:
            raise CollisionError(f"line {lineno}: malformed OPEN token")
        for index, token in enumerate(tokens):
            end = tokens[index + 1].start() if index + 1 < len(tokens) else len(text)
            start_entry(token.group(0), lineno, text[token.start() : end])
        return bool(tokens)

    for lineno, line in enumerate(report.splitlines(), start=1):
        raised = RAISED_RE.search(line)
        if raised is not None:
            finish_entry()
            active = True
            tail = line[raised.end() :]
            start_entries(tail, lineno)
            continue

        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
            finish_entry()
            active = False

        if active:
            if "OPEN[" in OPEN_RE.sub("", line):
                raise CollisionError(f"line {lineno}: malformed OPEN token")
            entry = ENTRY_RE.match(line)
            if entry is not None:
                start_entries(line[entry.start(1) :], lineno)
            elif current_identifier is not None:
                current_text.append(line)
            continue

        inline = INLINE_RAISE_RE.search(line)
        if inline is not None:
            start_entries(line[inline.start(1) :], lineno)
            finish_entry()

    finish_entry()

    # Keep first-seen report order but merge repeated raised entries so one OPEN
    # cannot produce duplicate candidate lists.
    merged: dict[str, tuple[int, list[str]]] = {}
    for identifier, lineno, description in entries:
        if identifier not in merged:
            merged[identifier] = (lineno, [description])
        else:
            merged[identifier][1].append(description)

    questions: list[OpenQuestion] = []
    for identifier, (lineno, descriptions) in merged.items():
        description = " ".join(descriptions)
        key_terms = _identifier_terms(identifier)
        quantity_terms = _quantity_terms(description)
        if not quantity_terms:
#!/usr/bin/env python3
"""Focused regression tests for the OPEN/banked collision hook."""

from __future__ import annotations

from pathlib import Path
import re
import subprocess
import sys
import tempfile
import unittest


TOOL = Path(__file__).with_name("open_collision.py")


class OpenCollisionTests(unittest.TestCase):
    def setUp(self) -> None:
        self.temporary = tempfile.TemporaryDirectory()
        self.addCleanup(self.temporary.cleanup)
        self.root = Path(self.temporary.name)
        (self.root / "xmodel").mkdir()
        for name in ("AUDIT.md", "notes.md", "APPROACHES.md"):
            (self.root / name).write_text("banked control\n", encoding="utf-8")

    def run_tool(self, report_text: str) -> subprocess.CompletedProcess[str]:
        report = self.root / "report.md"
        report.write_text(report_text, encoding="utf-8")
        return subprocess.run(
            [sys.executable, str(TOOL), str(report), "--root", str(self.root)],
            text=True,
            capture_output=True,
            timeout=10,
            check=False,
        )

    def test_deg_af_retroactive_case_surfaces_all_three_banked_lines(self) -> None:
        """Reproduce the coordinator's 2026-09-02 smallest useful test."""

        # Exact source lines and positions from
        # xmodel/companion-curve-alln-opus5-20260902.md:677,685,783,785,833,836.
        lines = ["banked filler"] * 836
        lines[676] = (
            "3. A new one, typed here: even given a representation and a source, "
            "`A_F` must be"
        )
        lines[684] = (
            "`sum_i k_i m_i <= K` and `sum_i deg D_i <= max(deg P, deg Q)`. "
            "Nothing is bounded"
        )
        lines[782] = (
            "**OPENs opened.** `OPEN[COMPANION-R0-REALISATION]`: `A_F` must be "
            "the zero set of"
        )
        lines[784] = (
            "`M <= K = deg P/d`, i.e. `sum_i deg D_i <= max(deg P, deg Q)`. "
            "This is the exact"
        )
        lines[832] = (
            "*Raw remainder degree / variable-ring map.* Every degree statement "
            "about `A_F` is"
        )
        lines[835] = (
            "`sum_i deg D_i <= max(deg P, deg Q)`, is flagged as gauge-dependent "
            "on the `x,y`"
        )
        fixture = self.root / "xmodel" / "companion-curve-alln-opus5-20260902.md"
        fixture.write_text("\n".join(lines) + "\n", encoding="utf-8")

        result = self.run_tool(
            "OPENS RAISED      OPEN[DEG-AF-VS-N]  bounded quantity: "
            "sum_i deg D_i <= max(deg P, deg Q)\n"
        )
        self.assertEqual(result.returncode, 0, result.stderr)
        self.assertIn("## COLLISIONS\n", result.stdout)
        self.assertIn("status: CANDIDATES", result.stdout)
        positions = re.findall(
            r"companion-curve-alln-opus5-20260902\.md:(\d+)", result.stdout
        )
        self.assertEqual(positions, ["685", "785", "836"])

    def test_no_candidate_emits_explicit_empty_collisions_block(self) -> None:
        (self.root / "xmodel" / "unrelated.md").write_text(
            "The ordinary degree satisfies d <= 12.\n", encoding="utf-8"
        )
        result = self.run_tool(
            "**OPENs raised.**\n\n"
            "* `OPEN[QUASIFOAM-MERIDIAN-COUNT]` — bound the quasifoam "
            "meridian count.\n"
        )
        self.assertEqual(result.returncode, 0, result.stderr)
        self.assertIn("## COLLISIONS\n\nstatus: EMPTY", result.stdout)
        self.assertIn("`OPEN[QUASIFOAM-MERIDIAN-COUNT]` (report:3): NONE", result.stdout)

    def test_missing_required_bank_file_fails_closed_with_a_block(self) -> None:
        (self.root / "xmodel" / "bank.md").write_text("x <= y\n", encoding="utf-8")
        (self.root / "APPROACHES.md").unlink()
        result = self.run_tool("No OPEN is raised by this report.\n")
        self.assertEqual(result.returncode, 2)
        self.assertIn("## COLLISIONS\n\nstatus: ERROR", result.stdout)
        self.assertIn("APPROACHES.md", result.stdout)

    def test_multiple_same_line_raised_opens_are_all_checked(self) -> None:
        (self.root / "xmodel" / "bank.md").write_text(
            "unrelated rank <= 4\n", encoding="utf-8"
        )
        result = self.run_tool(
            "OPENS RAISED "
            "OPEN[FIRST-ALPHA] bound alpha_count <= 1; "
            "OPEN[SECOND-BETA] bound beta_count <= 2\n"
        )
        self.assertEqual(result.returncode, 0, result.stderr)
        self.assertIn("`OPEN[FIRST-ALPHA]` (report:1): NONE", result.stdout)
        self.assertIn("`OPEN[SECOND-BETA]` (report:1): NONE", result.stdout)

    def test_malformed_open_in_raised_section_fails_closed(self) -> None:
        (self.root / "xmodel" / "bank.md").write_text("x <= y\n", encoding="utf-8")
        result = self.run_tool("OPENS RAISED\n- OPEN[MALFORMED\n")
        self.assertEqual(result.returncode, 2)
        self.assertIn("status: ERROR", result.stdout)
        self.assertIn("malformed OPEN token", result.stdout)

    def test_bound_inside_identifier_is_not_a_quantity_description(self) -> None:
        (self.root / "xmodel" / "bank.md").write_text("x <= y\n", encoding="utf-8")
        result = self.run_tool(
            "OPENS RAISED OPEN[A2-DEPTH-BOUND] "
            "(replaces OPEN[A2-U-BOUND])\n"
        )
        self.assertEqual(result.returncode, 2)
        self.assertIn("status: ERROR", result.stdout)
        self.assertIn("lacks an explicit bounded-quantity description", result.stdout)

    def test_prose_about_prior_raised_opens_does_not_activate_a_scan(self) -> None:
        (self.root / "xmodel" / "bank.md").write_text(
            "sum_i deg D_i <= max(deg P, deg Q)\n", encoding="utf-8"
        )
        result = self.run_tool(
            "Run this retroactively over the OPENs raised on 2026-09-02.\n"
            "PASS iff it surfaces OPEN[DEG-AF-VS-N].\n"
        )
        self.assertEqual(result.returncode, 0, result.stderr)
        self.assertIn("status: EMPTY", result.stdout)
        self.assertIn("report contains no explicitly raised", result.stdout)

    def test_candidate_fanout_is_bounded_and_fails_closed(self) -> None:
        rows = ["alpha meridian count <= limit"] * 101
        (self.root / "xmodel" / "bank.md").write_text(
            "\n".join(rows) + "\n", encoding="utf-8"
        )
        result = self.run_tool(
            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
            "bound alpha meridian count <= limit\n"
        )
        self.assertEqual(result.returncode, 2)
        self.assertIn("status: ERROR", result.stdout)
        self.assertIn("more than 100 candidates", result.stdout)


if __name__ == "__main__":
    unittest.main()

exec
/bin/zsh -lc "sed -n '250,470p' ops/open_collision.py && /usr/bin/time -p python3 -m unittest -v ops/test_open_collision.py && /usr/bin/time -p python3 -O -m unittest ops/test_open_collision.py" in /Users/dc/code/math/jc2
 succeeded in 698ms:
ok
test_deg_af_retroactive_case_surfaces_all_three_banked_lines (ops.test_open_collision.OpenCollisionTests.test_deg_af_retroactive_case_surfaces_all_three_banked_lines)
Reproduce the coordinator's 2026-09-02 smallest useful test. ... ok
test_malformed_open_in_raised_section_fails_closed (ops.test_open_collision.OpenCollisionTests.test_malformed_open_in_raised_section_fails_closed) ... ok
test_missing_required_bank_file_fails_closed_with_a_block (ops.test_open_collision.OpenCollisionTests.test_missing_required_bank_file_fails_closed_with_a_block) ... ok
test_multiple_same_line_raised_opens_are_all_checked (ops.test_open_collision.OpenCollisionTests.test_multiple_same_line_raised_opens_are_all_checked) ... ok
test_no_candidate_emits_explicit_empty_collisions_block (ops.test_open_collision.OpenCollisionTests.test_no_candidate_emits_explicit_empty_collisions_block) ... ok
test_prose_about_prior_raised_opens_does_not_activate_a_scan (ops.test_open_collision.OpenCollisionTests.test_prose_about_prior_raised_opens_does_not_activate_a_scan) ... ok

----------------------------------------------------------------------
Ran 8 tests in 0.382s

OK
real 0.43
user 0.32
sys 0.08
........
----------------------------------------------------------------------
Ran 8 tests in 0.349s

OK
real 0.39
user 0.30
sys 0.07

exec
/bin/zsh -lc "sed -n '470,560p' ops/open_collision.py && ls -l ops/open_collision.py ops/test_open_collision.py box/preflight.py ops/test_preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:

def parser() -> argparse.ArgumentParser:
    value = argparse.ArgumentParser(description=__doc__)
    value.add_argument("report", type=Path, help="unsealed Markdown report")
    value.add_argument(
        "--root",
        type=Path,
        default=Path.cwd(),
        help="repository root containing xmodel/ and the banked top-level files",
    )
    value.add_argument(
        "--context-lines",
        type=int,
        default=DEFAULT_CONTEXT_LINES,
        help="lines on either side used to match key nouns (default: 8)",
    )
    value.add_argument(
        "--max-candidates",
        type=int,
        default=DEFAULT_MAX_CANDIDATES,
        help="fail closed above this many candidates per OPEN (default: 100)",
    )
    return value


def main(argv: list[str] | None = None) -> int:
    args = parser().parse_args(argv)
    try:
        report_text = _read_utf8(args.report, "report")
        questions = extract_raised_opens(report_text)
        corpus = banked_corpus(args.root, args.report)
        collisions = find_collisions(
            questions,
            corpus,
            args.root,
            context_lines=args.context_lines,
            max_candidates=args.max_candidates,
        )
    except CollisionError as exc:
        print(_error_block(str(exc)), end="")
        print(f"open_collision: {exc}", file=sys.stderr)
        return 2
    print(render_collisions(questions, collisions), end="")
    return 0


if __name__ == "__main__":
    sys.exit(main())
-rw-r--r--@ 1 dc  staff  34167 Sep  2 02:31 box/preflight.py
-rwxr-xr-x@ 1 dc  staff  17064 Sep  2 02:31 ops/open_collision.py
-rwxr-xr-x@ 1 dc  staff   6705 Sep  2 02:31 ops/test_open_collision.py
-rw-r--r--@ 1 dc  staff   8340 Sep  2 02:30 ops/test_preflight.py

exec
/bin/zsh -lc 'ls -l box/qq_oracle_jobs.py box/a2_cells_jobs.py ops/test_frontier_gate.py' in /Users/dc/code/math/jc2
 succeeded in 0ms:
-rw-r--r--@ 1 dc  staff  32842 Sep  1 22:52 box/a2_cells_jobs.py
-rw-r--r--@ 1 dc  staff  44052 Sep  1 17:05 box/qq_oracle_jobs.py
-rwxr-xr-x@ 1 dc  staff   1985 Aug 26 16:07 ops/test_frontier_gate.py

exec
/bin/zsh -lc "ls -l ops/test_sectioned_output.py ops/test_artifact_finalize.py | sed -n '1,5p'" in /Users/dc/code/math/jc2
 succeeded in 0ms:
-rwxr-xr-x@ 1 dc  staff  22231 Aug 31 06:15 ops/test_artifact_finalize.py
-rwxr-xr-x@ 1 dc  staff   8664 Aug 29 17:23 ops/test_sectioned_output.py

collab: Wait
exec
/bin/zsh -lc "rg -n 'provenance|contiguous|vacu|source-fidelity|scope|Domain|Scope|transition.*output|COUNTEREXAMPLE' box/preflight.py ops/test_preflight.py | tail -120" in /Users/dc/code/math/jc2
 succeeded in 0ms:
ops/test_preflight.py:113:        self.assertIn("EQ2_even source-fidelity mismatch", result.errors[0])
box/preflight.py:62:    COUNTEREXAMPLE_CERTIFIED = "COUNTEREXAMPLE_CERTIFIED"
box/preflight.py:89:    source_scope: str = "GLOBAL"
box/preflight.py:90:    target_scope: str = "GLOBAL"
box/preflight.py:210:            Attainment.COUNTEREXAMPLE_CERTIFIED,
box/preflight.py:247:        TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.COUNTEREXAMPLE_CERTIFIED),
box/preflight.py:319:    source_scope = transition.source_scope
box/preflight.py:320:    target_scope = transition.target_scope
box/preflight.py:324:    if source_scope not in _ALLOWED_SCOPES or target_scope not in _ALLOWED_SCOPES:
box/preflight.py:325:        raise PreflightError("unknown or non-canonical transition scope")
box/preflight.py:336:        and source_scope == "LOCAL"
box/preflight.py:337:        and target_scope == "GLOBAL"
box/preflight.py:363:        raise PreflightError("source-fidelity generator order must be oldEQ2,q,E1")
box/preflight.py:373:        raise PreflightError("source-fidelity aliases are not the canonical source images")
box/preflight.py:376:        raise PreflightError("source-fidelity correction -2*q*E1 is vacuous")
box/preflight.py:381:            "EQ2_even source-fidelity mismatch (expected old_EQ2-2*q*E1; "
box/preflight.py:477:    checks.append("source-fidelity-EQ2_even")
box/preflight.py:485:        TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.COUNTEREXAMPLE_CERTIFIED),
box/preflight.py:489:            raise PreflightError("advanced output has no checked provenance path")
box/preflight.py:493:                raise PreflightError("transition path is not contiguous in typed state")
box/preflight.py:495:                raise PreflightError("transition path is not contiguous in domain")
box/preflight.py:496:            if previous.target_scope != following.source_scope:
box/preflight.py:497:                raise PreflightError("transition path is not contiguous in scope")
box/preflight.py:498:        if manifest.transitions[-1].target != manifest.output:
box/preflight.py:723:                "source_scope": item.source_scope, "target_scope": item.target_scope,
box/preflight.py:839:            "source_scope", "target_scope", "claim", "arrows",
box/preflight.py:850:                    _json_string(item["source_scope"], f"{label}.source_scope"),
box/preflight.py:851:                    _json_string(item["target_scope"], f"{label}.target_scope"),

exec
/bin/zsh -lc "sed -n '300,395p' box/preflight.py && sed -n '450,515p' box/preflight.py && sed -n '100,230p' ops/test_preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
        return exponent.index(1)
    except ValueError:
        return None


def validate_typed_output(output: TypedOutput) -> None:
    if output.attainment not in _OUTPUTS.get(output.artifact, frozenset()):
        raise PreflightError(
            f"invalid artifact/attainment pair: "
            f"{output.artifact.value}/{output.attainment.value}"
        )


def validate_transition(transition: Transition) -> None:
    """Apply the explicit, fail-closed semantic transition table."""
    validate_typed_output(transition.source)
    validate_typed_output(transition.target)
    source_domain = transition.source_domain
    target_domain = transition.target_domain
    source_scope = transition.source_scope
    target_scope = transition.target_scope
    claim = transition.claim
    if source_domain not in _ALLOWED_DOMAINS or target_domain not in _ALLOWED_DOMAINS:
        raise PreflightError("unknown or non-canonical transition domain")
    if source_scope not in _ALLOWED_SCOPES or target_scope not in _ALLOWED_SCOPES:
        raise PreflightError("unknown or non-canonical transition scope")
    if claim not in _ALLOWED_CLAIMS:
        raise PreflightError("unknown or non-canonical transition claim")
    if (
        source_domain == "A2"
        and target_domain == "B3"
        and claim == "KILL"
    ):
        raise PreflightError("forbidden transition: A2 cell -> B3 kill")
    if (
        "TORSION" in claim
        and source_scope == "LOCAL"
        and target_scope == "GLOBAL"
    ):
        raise PreflightError("forbidden transition: local torsion -> global torsion")
    if source_domain != target_domain:
        raise PreflightError("cross-domain transition has no declared object map")
    if transition.source == transition.target:
        return
    required = _TRANSITIONS.get((transition.source, transition.target))
    if required is None:
        raise PreflightError("transition is absent from the checked table")
    supplied = frozenset(transition.arrows)
    missing = required - supplied
    extra = supplied - required
    if missing or extra:
        detail = []
        if missing:
            detail.append("missing=" + ",".join(sorted(a.value for a in missing)))
        if extra:
            detail.append("unexpected=" + ",".join(sorted(a.value for a in extra)))
        raise PreflightError("transition arrow mismatch: " + " ".join(detail))


def _validate_source_fidelity(source: SourceFidelity) -> None:
    names = source.ring_generators
    canonical_names = ("oldEQ2", "q", "E1")
    if names != canonical_names:
        raise PreflightError("source-fidelity generator order must be oldEQ2,q,E1")
    try:
        old = algebra.parse_mv(source.old_eq2, names)
        q = algebra.parse_mv(source.q, names)
        e1 = algebra.parse_mv(source.e1, names)
        encoded = algebra.parse_mv(source.encoded_eq2_even, names)
    except (TypeError, ValueError) as exc:
        raise PreflightError(f"EQ2_even parse/ring failure: {exc}") from exc
    canonical = tuple(algebra.parse_mv(name, names) for name in canonical_names)
    if (old, q, e1) != canonical:
        raise PreflightError("source-fidelity aliases are not the canonical source images")
    correction = 2 * q * e1
    if q.is_zero() or e1.is_zero() or correction.is_zero():
        raise PreflightError("source-fidelity correction -2*q*E1 is vacuous")
    expected = old - 2 * q * e1
    if encoded != expected:
        difference = encoded - expected
        raise PreflightError(
            "EQ2_even source-fidelity mismatch (expected old_EQ2-2*q*E1; "
            f"exact diff {algebra.render_mv(difference)})"
        )


def _validate_manifest(manifest: Manifest, checks: list[str]) -> None:
    if manifest.schema_version != SCHEMA_VERSION:
        raise PreflightError("unsupported manifest schema")
    if manifest.degree_n < 6:
        raise PreflightError("this hard launch gate requires declared N >= 6")
    if manifest.cell not in CELLS:
        raise PreflightError(f"unknown corrected cell {manifest.cell!r}")
    if manifest.coefficient_field != "QQ" or manifest.monomial_order != "GRevLex":
        raise PreflightError("coefficient ring must be QQ with GRevLex order")

        except (TypeError, ValueError) as exc:
            raise PreflightError(f"encoded condition {name} failed to parse: {exc}") from exc
        if encoded != emitted_root.get(degree) or encoded != mapped_root.get(degree):
            raise PreflightError(f"exact coefficient diff failed for {name}")
    checks.append("encoded-coefficient-exact-diff")

    if len(manifest.open_factors) != 1:
        raise PreflightError("exactly one corrected leading-coefficient open is required")
    opened = manifest.open_factors[0]
    if opened.name != f"g_{cell.open_degree}" or opened.equation_name != open_name:
        raise PreflightError("open-factor name/degree mismatch")
    try:
        factor = algebra.parse_mv(opened.expression, target_names)
        inverse = algebra.parse_mv(opened.inverse, target_names)
        open_equation = algebra.parse_mv(condition_by_name[open_name], target_names)
    except (TypeError, ValueError) as exc:
        raise PreflightError(f"open-factor parse/ring failure: {exc}") from exc
    expected_factor = emitted_root.get(cell.open_degree)
    if expected_factor is None or factor != expected_factor or factor.is_zero():
        raise PreflightError("open factor is not the corrected leading coefficient")
    if inverse != images[-1]:
        raise PreflightError("open inverse does not match the declared ring map")
    if open_equation != inverse * factor - 1:
        raise PreflightError("Rabinowitsch open equation mismatch")
    checks.append("open-factor-rabinowitsch")

    _validate_source_fidelity(manifest.source_fidelity)
    checks.append("source-fidelity-EQ2_even")
    validate_typed_output(manifest.output)
    for transition in manifest.transitions:
        validate_transition(transition)
    advanced_outputs = {
        TypedOutput(Artifact.FULL_EO_POINT, Attainment.REALIZED),
        TypedOutput(Artifact.FULL_EO_POINT, Attainment.ADMISSIBLE_PULLBACK),
        TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.ACTUAL_MAP),
        TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.COUNTEREXAMPLE_CERTIFIED),
    }
    if not manifest.transitions:
        if manifest.output in advanced_outputs:
            raise PreflightError("advanced output has no checked provenance path")
    else:
        for previous, following in zip(manifest.transitions, manifest.transitions[1:]):
            if previous.target != following.source:
                raise PreflightError("transition path is not contiguous in typed state")
            if previous.target_domain != following.source_domain:
                raise PreflightError("transition path is not contiguous in domain")
            if previous.target_scope != following.source_scope:
                raise PreflightError("transition path is not contiguous in scope")
        if manifest.transitions[-1].target != manifest.output:
            raise PreflightError("manifest output is not the end of its transition path")
    checks.append("typed-output-transitions")


def run_builtin_canaries() -> Tuple[bool, Tuple[str, ...]]:
    """Run exact 86A-negative, 96B-positive, and EQ2 omission controls."""
    messages = []
    try:
        names86, g86 = algebra.expand_g86(algebra.AUX_A)
        point86 = algebra._charged_a_point()
        raw_names86, raw86 = algebra.expand_g86(())
        old_closed = [
            algebra.subst_mv(
                algebra.coeff_t(raw86, degree, raw_names86), point86
            ).as_constant()
            for degree in (21, 19, 17, 15, 13)
        ]
            ),
        }
        for label, malformed in mutations.items():
            with self.subTest(label=label):
                result = self.run_gate(malformed)
                self.assertFalse(result.ok)
                self.assertTrue(result.errors)

    def test_source_fidelity_omitting_minus_2qE1_fails(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        stale = replace(manifest.source_fidelity, encoded_eq2_even="oldEQ2")
        result = self.run_gate(replace(manifest, source_fidelity=stale))
        self.assertFalse(result.ok)
        self.assertIn("EQ2_even source-fidelity mismatch", result.errors[0])

    def test_n_below_six_and_stack_drift_fail_closed(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        self.assertFalse(self.run_gate(replace(manifest, degree_n=5)).ok)
        absent = gate.StackObservation(
            {"qqideal": None, "msolveio": None}, None, None
        )
        result = gate.preflight(manifest, stack_observation=absent)
        self.assertFalse(result.ok)
        self.assertIn("package version gate failed", result.errors[0])

    def test_json_contract_round_trip(self) -> None:
        manifest = gate.build_reference_manifest("86A")
        payload = json.loads(json.dumps(gate.manifest_to_dict(manifest)))
        parsed = gate.manifest_from_dict(payload)
        self.assertEqual(parsed, manifest)
        self.assertTrue(self.run_gate(parsed).ok)
        payload["misspelled_field"] = True
        with self.assertRaises(gate.PreflightError):
            gate.manifest_from_dict(payload)
        del payload["misspelled_field"]
        payload["degree_n"] = "6"
        with self.assertRaisesRegex(gate.PreflightError, "must be an integer"):
            gate.manifest_from_dict(payload)


class TypedTransitionTests(unittest.TestCase):
    def test_two_field_output_rejects_impossible_pair(self) -> None:
        with self.assertRaises(gate.PreflightError):
            gate.validate_typed_output(
                gate.TypedOutput(
                    gate.Artifact.NUMERICAL_PROFILE, gate.Attainment.ACTUAL_MAP
                )
            )

    def test_rejects_a2_cell_to_b3_kill(self) -> None:
        transition = gate.Transition(
            gate.TypedOutput(gate.Artifact.SUBSYSTEM_POINT, gate.Attainment.REALIZED),
            gate.TypedOutput(gate.Artifact.REPRESENTATION, gate.Attainment.NECESSARY),
            "A2", "B3", claim="KILL",
        )
        with self.assertRaisesRegex(gate.PreflightError, "A2 cell -> B3 kill"):
            gate.validate_transition(transition)

    def test_rejects_local_torsion_to_global_torsion(self) -> None:
        state = gate.TypedOutput(
            gate.Artifact.REPRESENTATION, gate.Attainment.NECESSARY
        )
        transition = gate.Transition(
            state, state, "B3", "B3", "LOCAL", "GLOBAL", "TORSION_ORDER"
        )
        with self.assertRaisesRegex(
            gate.PreflightError, "local torsion -> global torsion"
        ):
            gate.validate_transition(transition)

    def test_subsystem_to_full_eo_requires_equations_and_quotient(self) -> None:
        transition = gate.Transition(
            gate.TypedOutput(gate.Artifact.SUBSYSTEM_POINT, gate.Attainment.REALIZED),
            gate.TypedOutput(gate.Artifact.FULL_EO_POINT, gate.Attainment.REALIZED),
            "A2", "A2", arrows=(gate.Arrow.FULL_EO_EQUATIONS,),
        )
        with self.assertRaisesRegex(gate.PreflightError, "DECLARED_QUOTIENT"):
            gate.validate_transition(transition)
        gate.validate_transition(
            replace(transition, arrows=tuple(gate._FULL_EO_ARROWS))
        )

    def test_full_eo_to_actual_map_requires_every_declared_arrow(self) -> None:
        source = gate.TypedOutput(
            gate.Artifact.FULL_EO_POINT, gate.Attainment.REALIZED
        )
        target = gate.TypedOutput(
            gate.Artifact.POLYNOMIAL_PAIR, gate.Attainment.ACTUAL_MAP
        )
        missing_source_chart = gate.Transition(
            source, target, "A2", "A2", arrows=tuple(gate._PULLBACK_ARROWS)
        )
        with self.assertRaisesRegex(gate.PreflightError, "SOURCE_CHART"):
            gate.validate_transition(missing_source_chart)
        complete = replace(missing_source_chart, arrows=tuple(gate._MAP_ARROWS))
        gate.validate_transition(complete)


if __name__ == "__main__":
    unittest.main(verbosity=2)

exec
/bin/zsh -lc "rg -n '"'^    def test_'"' ops/test_preflight.py && rg -n '_ALLOWED_' box/preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
28:    def test_faithful_96b_emits_root_and_exact_diffs_every_condition(self) -> None:
41:    def test_exact_diff_rejects_root_or_ideal_coefficient_mutation(self) -> None:
64:    def test_old_8619_false_positive_is_rejected_by_corrected_86a(self) -> None:
76:    def test_four_node_962_curve_passes_corrected_96b(self) -> None:
81:    def test_fail_closed_on_ring_map_open_and_generator_order_mismatch(self) -> None:
108:    def test_source_fidelity_omitting_minus_2qE1_fails(self) -> None:
115:    def test_source_fidelity_cannot_be_renamed_or_vacuous(self) -> None:
122:    def test_n_below_six_and_stack_drift_fail_closed(self) -> None:
132:    def test_json_contract_round_trip(self) -> None:
148:    def test_two_field_output_rejects_impossible_pair(self) -> None:
156:    def test_rejects_a2_cell_to_b3_kill(self) -> None:
165:    def test_rejects_local_torsion_to_global_torsion(self) -> None:
179:    def test_subsystem_to_full_eo_requires_equations_and_quotient(self) -> None:
191:    def test_full_eo_to_actual_map_requires_every_declared_arrow(self) -> None:
206:    def test_advanced_output_must_end_a_checked_provenance_path(self) -> None:
225:_ALLOWED_DOMAINS = frozenset({"A2", "B3", "CURVE", "REPRESENTATION", "COVER", "MAP"})
226:_ALLOWED_SCOPES = frozenset({"LOCAL", "GLOBAL"})
227:_ALLOWED_CLAIMS = frozenset({"POINT", "KILL", "TORSION", "TORSION_ORDER"})
322:    if source_domain not in _ALLOWED_DOMAINS or target_domain not in _ALLOWED_DOMAINS:
324:    if source_scope not in _ALLOWED_SCOPES or target_scope not in _ALLOWED_SCOPES:
326:    if claim not in _ALLOWED_CLAIMS:

exec
/bin/zsh -lc "sed -n '108,130p' ops/test_preflight.py && sed -n '200,245p' ops/test_preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
    def test_source_fidelity_omitting_minus_2qE1_fails(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        stale = replace(manifest.source_fidelity, encoded_eq2_even="oldEQ2")
        result = self.run_gate(replace(manifest, source_fidelity=stale))
        self.assertFalse(result.ok)
        self.assertIn("EQ2_even source-fidelity mismatch", result.errors[0])

    def test_source_fidelity_cannot_be_renamed_or_vacuous(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        vacuous = gate.SourceFidelity(("x",), "0", "0", "0", "0")
        result = self.run_gate(replace(manifest, source_fidelity=vacuous))
        self.assertFalse(result.ok)
        self.assertIn("generator order", result.errors[0])

    def test_n_below_six_and_stack_drift_fail_closed(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        self.assertFalse(self.run_gate(replace(manifest, degree_n=5)).ok)
        absent = gate.StackObservation(
            {"qqideal": None, "msolveio": None}, None, None
        )
        result = gate.preflight(manifest, stack_observation=absent)
        self.assertFalse(result.ok)
        self.assertIn("package version gate failed", result.errors[0])
        )
        with self.assertRaisesRegex(gate.PreflightError, "SOURCE_CHART"):
            gate.validate_transition(missing_source_chart)
        complete = replace(missing_source_chart, arrows=tuple(gate._MAP_ARROWS))
        gate.validate_transition(complete)

    def test_advanced_output_must_end_a_checked_provenance_path(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        actual = gate.TypedOutput(
            gate.Artifact.POLYNOMIAL_PAIR, gate.Attainment.ACTUAL_MAP
        )
        uncertified = self.run_gate(replace(manifest, output=actual))
        self.assertFalse(uncertified.ok)
        self.assertIn("no checked provenance path", uncertified.errors[0])

        source = gate.TypedOutput(
            gate.Artifact.FULL_EO_POINT, gate.Attainment.REALIZED
        )
        transition = gate.Transition(
            source, actual, "A2", "A2", arrows=tuple(gate._MAP_ARROWS)
        )
        certified = self.run_gate(
            replace(manifest, output=actual, transitions=(transition,))
        )
        self.assertTrue(certified.ok, certified.errors)

        counterexample = gate.TypedOutput(
            gate.Artifact.POLYNOMIAL_PAIR,
            gate.Attainment.COUNTEREXAMPLE_CERTIFIED,
        )
        result = self.run_gate(replace(manifest, output=counterexample))
        self.assertFalse(result.ok)
        self.assertIn("no checked provenance path", result.errors[0])


if __name__ == "__main__":
    unittest.main(verbosity=2)

exec
/bin/zsh -lc '/usr/bin/time -p python3 -m unittest -v ops/test_preflight.py && python3 box/preflight.py --self-test' in /Users/dc/code/math/jc2
 succeeded in 1820ms:
ok
test_fail_closed_on_ring_map_open_and_generator_order_mismatch (ops.test_preflight.EncodingPreflightTests.test_fail_closed_on_ring_map_open_and_generator_order_mismatch) ... ok
test_faithful_96b_emits_root_and_exact_diffs_every_condition (ops.test_preflight.EncodingPreflightTests.test_faithful_96b_emits_root_and_exact_diffs_every_condition) ... ok
test_four_node_962_curve_passes_corrected_96b (ops.test_preflight.EncodingPreflightTests.test_four_node_962_curve_passes_corrected_96b) ... ok
test_json_contract_round_trip (ops.test_preflight.EncodingPreflightTests.test_json_contract_round_trip) ... ok
test_n_below_six_and_stack_drift_fail_closed (ops.test_preflight.EncodingPreflightTests.test_n_below_six_and_stack_drift_fail_closed) ... ok
test_old_8619_false_positive_is_rejected_by_corrected_86a (ops.test_preflight.EncodingPreflightTests.test_old_8619_false_positive_is_rejected_by_corrected_86a) ... ok
test_source_fidelity_cannot_be_renamed_or_vacuous (ops.test_preflight.EncodingPreflightTests.test_source_fidelity_cannot_be_renamed_or_vacuous) ... ok
test_source_fidelity_omitting_minus_2qE1_fails (ops.test_preflight.EncodingPreflightTests.test_source_fidelity_omitting_minus_2qE1_fails) ... ok
test_advanced_output_must_end_a_checked_provenance_path (ops.test_preflight.TypedTransitionTests.test_advanced_output_must_end_a_checked_provenance_path) ... ok
test_full_eo_to_actual_map_requires_every_declared_arrow (ops.test_preflight.TypedTransitionTests.test_full_eo_to_actual_map_requires_every_declared_arrow) ... ok
test_rejects_a2_cell_to_b3_kill (ops.test_preflight.TypedTransitionTests.test_rejects_a2_cell_to_b3_kill) ... ok
test_rejects_local_torsion_to_global_torsion (ops.test_preflight.TypedTransitionTests.test_rejects_local_torsion_to_global_torsion) ... ok
test_subsystem_to_full_eo_requires_equations_and_quotient (ops.test_preflight.TypedTransitionTests.test_subsystem_to_full_eo_requires_equations_and_quotient) ... ok
test_two_field_output_rejects_impossible_pair (ops.test_preflight.TypedTransitionTests.test_two_field_output_rejects_impossible_pair) ... ok

----------------------------------------------------------------------
Ran 15 tests in 1.688s

OK
real 1.75
user 1.71
sys 0.01
{
  "ok": true,
  "checks": [
    "86A-rejects-old-(8,6,19)",
    "96B-accepts-four-node-(9,6,2)",
    "EQ2_even-omission-rejected"
  ]
}

collab: Wait
exec
/bin/zsh -lc "rg -n 'self-transition|provenance path must start|SUBSYSTEM_POINT' box/preflight.py ops/test_preflight.py | tail -40" in /Users/dc/code/math/jc2
 succeeded in 0ms:
ops/test_preflight.py:161:            gate.TypedOutput(gate.Artifact.SUBSYSTEM_POINT, gate.Attainment.REALIZED),
ops/test_preflight.py:184:            gate.TypedOutput(gate.Artifact.SUBSYSTEM_POINT, gate.Attainment.REALIZED),
box/preflight.py:49:    SUBSYSTEM_POINT = "SUBSYSTEM_POINT"
box/preflight.py:197:    Artifact.SUBSYSTEM_POINT: frozenset({Attainment.REALIZED}),
box/preflight.py:230:        TypedOutput(Artifact.SUBSYSTEM_POINT, Attainment.REALIZED),
box/preflight.py:481:    subsystem = TypedOutput(Artifact.SUBSYSTEM_POINT, Attainment.REALIZED)

exec
/bin/zsh -lc "sed -n '310,350p' box/preflight.py && sed -n '475,515p' box/preflight.py && sed -n '200,270p' ops/test_preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
        )


def validate_transition(transition: Transition) -> None:
    """Apply the explicit, fail-closed semantic transition table."""
    validate_typed_output(transition.source)
    validate_typed_output(transition.target)
    source_domain = transition.source_domain
    target_domain = transition.target_domain
    source_scope = transition.source_scope
    target_scope = transition.target_scope
    claim = transition.claim
    if source_domain not in _ALLOWED_DOMAINS or target_domain not in _ALLOWED_DOMAINS:
        raise PreflightError("unknown or non-canonical transition domain")
    if source_scope not in _ALLOWED_SCOPES or target_scope not in _ALLOWED_SCOPES:
        raise PreflightError("unknown or non-canonical transition scope")
    if claim not in _ALLOWED_CLAIMS:
        raise PreflightError("unknown or non-canonical transition claim")
    if (
        source_domain == "A2"
        and target_domain == "B3"
        and claim == "KILL"
    ):
        raise PreflightError("forbidden transition: A2 cell -> B3 kill")
    if (
        "TORSION" in claim
        and source_scope == "LOCAL"
        and target_scope == "GLOBAL"
    ):
        raise PreflightError("forbidden transition: local torsion -> global torsion")
    if source_domain != target_domain:
        raise PreflightError("cross-domain transition has no declared object map")
    if transition.source == transition.target:
        raise PreflightError("transition source and target are identical")
    required = _TRANSITIONS.get((transition.source, transition.target))
    if required is None:
        raise PreflightError("transition is absent from the checked table")
    supplied = frozenset(transition.arrows)
    missing = required - supplied
    extra = supplied - required
    if missing or extra:

    _validate_source_fidelity(manifest.source_fidelity)
    checks.append("source-fidelity-EQ2_even")
    validate_typed_output(manifest.output)
    for transition in manifest.transitions:
        validate_transition(transition)
    subsystem = TypedOutput(Artifact.SUBSYSTEM_POINT, Attainment.REALIZED)
    full_eo = TypedOutput(Artifact.FULL_EO_POINT, Attainment.REALIZED)
    pullback = TypedOutput(Artifact.FULL_EO_POINT, Attainment.ADMISSIBLE_PULLBACK)
    actual_map = TypedOutput(Artifact.POLYNOMIAL_PAIR, Attainment.ACTUAL_MAP)
    counterexample = TypedOutput(
        Artifact.POLYNOMIAL_PAIR, Attainment.COUNTEREXAMPLE_CERTIFIED
    )
    required_paths = {
        full_eo: (subsystem, full_eo),
        pullback: (subsystem, full_eo, pullback),
        actual_map: (subsystem, full_eo, pullback, actual_map),
        counterexample: (subsystem, full_eo, pullback, actual_map, counterexample),
    }
    if not manifest.transitions:
        if manifest.output in required_paths:
            raise PreflightError("advanced output has no checked provenance path")
    else:
        for previous, following in zip(manifest.transitions, manifest.transitions[1:]):
            if previous.target != following.source:
                raise PreflightError("transition path is not contiguous in typed state")
            if previous.target_domain != following.source_domain:
                raise PreflightError("transition path is not contiguous in domain")
            if previous.target_scope != following.source_scope:
                raise PreflightError("transition path is not contiguous in scope")
        if manifest.transitions[-1].target != manifest.output:
            raise PreflightError("manifest output is not the end of its transition path")
        required_states = required_paths.get(manifest.output)
        if required_states is not None:
            actual_states = (manifest.transitions[0].source,) + tuple(
                transition.target for transition in manifest.transitions
            )
            if actual_states != required_states:
                raise PreflightError(
                    "advanced output does not follow the complete subsystem-to-output ladder"
                )
        )
        missing_source_chart = gate.Transition(
            source, target, "A2", "A2", arrows=tuple(gate._PULLBACK_ARROWS)
        )
        with self.assertRaisesRegex(gate.PreflightError, "SOURCE_CHART"):
            gate.validate_transition(missing_source_chart)
        complete = replace(missing_source_chart, arrows=tuple(gate._MAP_ARROWS))
        gate.validate_transition(complete)

    def test_advanced_output_must_end_a_checked_provenance_path(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        actual = gate.TypedOutput(
            gate.Artifact.POLYNOMIAL_PAIR, gate.Attainment.ACTUAL_MAP
        )
        uncertified = self.run_gate(replace(manifest, output=actual))
        self.assertFalse(uncertified.ok)
        self.assertIn("no checked provenance path", uncertified.errors[0])

        subsystem = gate.TypedOutput(
            gate.Artifact.SUBSYSTEM_POINT, gate.Attainment.REALIZED
        )
        full_eo = gate.TypedOutput(
            gate.Artifact.FULL_EO_POINT, gate.Attainment.REALIZED
        )
        pullback = gate.TypedOutput(
            gate.Artifact.FULL_EO_POINT, gate.Attainment.ADMISSIBLE_PULLBACK
        )
        transitions = (
            gate.Transition(
                subsystem, full_eo, "A2", "A2",
                arrows=tuple(gate._FULL_EO_ARROWS),
            ),
            gate.Transition(
                full_eo, pullback, "A2", "A2",
                arrows=tuple(gate._PULLBACK_ARROWS),
            ),
            gate.Transition(
                pullback, actual, "A2", "A2",
                arrows=(gate.Arrow.SOURCE_CHART,),
            ),
        )
        certified = self.run_gate(
            replace(manifest, output=actual, transitions=transitions)
        )
        self.assertTrue(certified.ok, certified.errors)

        self_loop = gate.Transition(actual, actual, "A2", "A2")
        result = self.run_gate(
            replace(manifest, output=actual, transitions=(self_loop,))
        )
        self.assertFalse(result.ok)
        self.assertIn("identical", result.errors[0])

        late_start = transitions[-1:]
        result = self.run_gate(
            replace(manifest, output=actual, transitions=late_start)
        )
        self.assertFalse(result.ok)
        self.assertIn("complete subsystem-to-output ladder", result.errors[0])

        counterexample = gate.TypedOutput(
            gate.Artifact.POLYNOMIAL_PAIR,
            gate.Attainment.COUNTEREXAMPLE_CERTIFIED,
        )
        result = self.run_gate(replace(manifest, output=counterexample))
        self.assertFalse(result.ok)
        self.assertIn("no checked provenance path", result.errors[0])


if __name__ == "__main__":
    unittest.main(verbosity=2)

exec
/bin/zsh -lc '/usr/bin/time -p python3 -m unittest -v ops/test_preflight.py' in /Users/dc/code/math/jc2
 succeeded in 1713ms:
ok
test_fail_closed_on_ring_map_open_and_generator_order_mismatch (ops.test_preflight.EncodingPreflightTests.test_fail_closed_on_ring_map_open_and_generator_order_mismatch) ... ok
test_faithful_96b_emits_root_and_exact_diffs_every_condition (ops.test_preflight.EncodingPreflightTests.test_faithful_96b_emits_root_and_exact_diffs_every_condition) ... ok
test_four_node_962_curve_passes_corrected_96b (ops.test_preflight.EncodingPreflightTests.test_four_node_962_curve_passes_corrected_96b) ... ok
test_json_contract_round_trip (ops.test_preflight.EncodingPreflightTests.test_json_contract_round_trip) ... ok
test_n_below_six_and_stack_drift_fail_closed (ops.test_preflight.EncodingPreflightTests.test_n_below_six_and_stack_drift_fail_closed) ... ok
test_old_8619_false_positive_is_rejected_by_corrected_86a (ops.test_preflight.EncodingPreflightTests.test_old_8619_false_positive_is_rejected_by_corrected_86a) ... ok
test_source_fidelity_cannot_be_renamed_or_vacuous (ops.test_preflight.EncodingPreflightTests.test_source_fidelity_cannot_be_renamed_or_vacuous) ... ok
test_source_fidelity_omitting_minus_2qE1_fails (ops.test_preflight.EncodingPreflightTests.test_source_fidelity_omitting_minus_2qE1_fails) ... ok
test_advanced_output_must_end_a_checked_provenance_path (ops.test_preflight.TypedTransitionTests.test_advanced_output_must_end_a_checked_provenance_path) ... ok
test_full_eo_to_actual_map_requires_every_declared_arrow (ops.test_preflight.TypedTransitionTests.test_full_eo_to_actual_map_requires_every_declared_arrow) ... ok
test_rejects_a2_cell_to_b3_kill (ops.test_preflight.TypedTransitionTests.test_rejects_a2_cell_to_b3_kill) ... ok
test_rejects_local_torsion_to_global_torsion (ops.test_preflight.TypedTransitionTests.test_rejects_local_torsion_to_global_torsion) ... ok
test_subsystem_to_full_eo_requires_equations_and_quotient (ops.test_preflight.TypedTransitionTests.test_subsystem_to_full_eo_requires_equations_and_quotient) ... ok
test_two_field_output_rejects_impossible_pair (ops.test_preflight.TypedTransitionTests.test_two_field_output_rejects_impossible_pair) ... ok

----------------------------------------------------------------------
Ran 15 tests in 1.773s

OK
real 1.84
user 1.80
sys 0.01

collab: Wait
exec
/bin/zsh -lc "sed -n '490,525p' box/preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
        pullback: (subsystem, full_eo, pullback),
        actual_map: (subsystem, full_eo, pullback, actual_map),
        counterexample: (subsystem, full_eo, pullback, actual_map, counterexample),
    }
    if not manifest.transitions:
        if manifest.output in required_paths:
            raise PreflightError("advanced output has no checked provenance path")
    else:
        for previous, following in zip(manifest.transitions, manifest.transitions[1:]):
            if previous.target != following.source:
                raise PreflightError("transition path is not contiguous in typed state")
            if previous.target_domain != following.source_domain:
                raise PreflightError("transition path is not contiguous in domain")
            if previous.target_scope != following.source_scope:
                raise PreflightError("transition path is not contiguous in scope")
        if manifest.transitions[-1].target != manifest.output:
            raise PreflightError("manifest output is not the end of its transition path")
        required_states = required_paths.get(manifest.output)
        if required_states is not None:
            actual_states = (manifest.transitions[0].source,) + tuple(
                transition.target for transition in manifest.transitions
            )
            if actual_states != required_states:
                raise PreflightError(
                    "advanced output does not follow the complete subsystem-to-output ladder"
                )
            if any(
                transition.source_domain != "A2"
                or transition.target_domain != "A2"
                or transition.source_scope != "GLOBAL"
                or transition.target_scope != "GLOBAL"
                for transition in manifest.transitions
            ):
                raise PreflightError(
                    "advanced output provenance must remain in the global A2 workflow"
                )

collab: Wait
exec
/bin/zsh -lc 'python3 -m py_compile ops/open_collision.py ops/test_open_collision.py box/preflight.py ops/test_preflight.py && /usr/bin/time -p python3 -m unittest -v ops/test_open_collision.py ops/test_preflight.py' in /Users/dc/code/math/jc2
 succeeded in 2016ms:
ok
test_candidate_fanout_is_bounded_and_fails_closed (ops.test_open_collision.OpenCollisionTests.test_candidate_fanout_is_bounded_and_fails_closed) ... ok
test_deg_af_retroactive_case_surfaces_all_three_banked_lines (ops.test_open_collision.OpenCollisionTests.test_deg_af_retroactive_case_surfaces_all_three_banked_lines)
Reproduce the coordinator's 2026-09-02 smallest useful test. ... ok
test_malformed_open_in_raised_section_fails_closed (ops.test_open_collision.OpenCollisionTests.test_malformed_open_in_raised_section_fails_closed) ... ok
test_missing_required_bank_file_fails_closed_with_a_block (ops.test_open_collision.OpenCollisionTests.test_missing_required_bank_file_fails_closed_with_a_block) ... ok
test_multiple_same_line_raised_opens_are_all_checked (ops.test_open_collision.OpenCollisionTests.test_multiple_same_line_raised_opens_are_all_checked) ... ok
test_no_candidate_emits_explicit_empty_collisions_block (ops.test_open_collision.OpenCollisionTests.test_no_candidate_emits_explicit_empty_collisions_block) ... ok
test_prose_about_prior_raised_opens_does_not_activate_a_scan (ops.test_open_collision.OpenCollisionTests.test_prose_about_prior_raised_opens_does_not_activate_a_scan) ... ok
test_exact_diff_rejects_root_or_ideal_coefficient_mutation (ops.test_preflight.EncodingPreflightTests.test_exact_diff_rejects_root_or_ideal_coefficient_mutation) ... ok
test_fail_closed_on_ring_map_open_and_generator_order_mismatch (ops.test_preflight.EncodingPreflightTests.test_fail_closed_on_ring_map_open_and_generator_order_mismatch) ... ok
test_faithful_96b_emits_root_and_exact_diffs_every_condition (ops.test_preflight.EncodingPreflightTests.test_faithful_96b_emits_root_and_exact_diffs_every_condition) ... ok
test_four_node_962_curve_passes_corrected_96b (ops.test_preflight.EncodingPreflightTests.test_four_node_962_curve_passes_corrected_96b) ... ok
test_json_contract_round_trip (ops.test_preflight.EncodingPreflightTests.test_json_contract_round_trip) ... ok
test_n_below_six_and_stack_drift_fail_closed (ops.test_preflight.EncodingPreflightTests.test_n_below_six_and_stack_drift_fail_closed) ... ok
test_old_8619_false_positive_is_rejected_by_corrected_86a (ops.test_preflight.EncodingPreflightTests.test_old_8619_false_positive_is_rejected_by_corrected_86a) ... ok
test_source_fidelity_cannot_be_renamed_or_vacuous (ops.test_preflight.EncodingPreflightTests.test_source_fidelity_cannot_be_renamed_or_vacuous) ... ok
test_source_fidelity_omitting_minus_2qE1_fails (ops.test_preflight.EncodingPreflightTests.test_source_fidelity_omitting_minus_2qE1_fails) ... ok
test_advanced_output_must_end_a_checked_provenance_path (ops.test_preflight.TypedTransitionTests.test_advanced_output_must_end_a_checked_provenance_path) ... ok
test_full_eo_to_actual_map_requires_every_declared_arrow (ops.test_preflight.TypedTransitionTests.test_full_eo_to_actual_map_requires_every_declared_arrow) ... ok
test_rejects_a2_cell_to_b3_kill (ops.test_preflight.TypedTransitionTests.test_rejects_a2_cell_to_b3_kill) ... ok
test_rejects_local_torsion_to_global_torsion (ops.test_preflight.TypedTransitionTests.test_rejects_local_torsion_to_global_torsion) ... ok
test_subsystem_to_full_eo_requires_equations_and_quotient (ops.test_preflight.TypedTransitionTests.test_subsystem_to_full_eo_requires_equations_and_quotient) ... ok
test_two_field_output_rejects_impossible_pair (ops.test_preflight.TypedTransitionTests.test_two_field_output_rejects_impossible_pair) ... ok

----------------------------------------------------------------------
Ran 23 tests in 2.039s

OK
real 2.09
user 1.99
sys 0.08

codex
The final review caught one remaining false negative: banked relation lines that themselves carry an older `OPEN[...]` label were being skipped. That label is useful provenance, not a reason to hide the candidate, so I’m removing the skip and pinning the case with a regression.
apply patch
patch: completed
/Users/dc/code/math/jc2/ops/open_collision.py
/Users/dc/code/math/jc2/ops/test_open_collision.py
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..af37f0183fc0cc281feb2a927a9ed922dfb338e8
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..8c280cd53468d9615267cc259712457e15dfbac9
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,19 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..af37f0183fc0cc281feb2a927a9ed922dfb338e8
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..8c280cd53468d9615267cc259712457e15dfbac9
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,19 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "rg -n 'provenance, not|banked_relation_with_open' ops/open_collision.py ops/test_open_collision.py && python3 -m unittest -v ops/test_open_collision.py" in /Users/dc/code/math/jc2
 succeeded in 311ms:
ok
test_deg_af_retroactive_case_surfaces_all_three_banked_lines (ops.test_open_collision.OpenCollisionTests.test_deg_af_retroactive_case_surfaces_all_three_banked_lines)
Reproduce the coordinator's 2026-09-02 smallest useful test. ... ok
test_malformed_open_in_raised_section_fails_closed (ops.test_open_collision.OpenCollisionTests.test_malformed_open_in_raised_section_fails_closed) ... ok
test_missing_required_bank_file_fails_closed_with_a_block (ops.test_open_collision.OpenCollisionTests.test_missing_required_bank_file_fails_closed_with_a_block) ... ok
test_multiple_same_line_raised_opens_are_all_checked (ops.test_open_collision.OpenCollisionTests.test_multiple_same_line_raised_opens_are_all_checked) ... ok
test_no_candidate_emits_explicit_empty_collisions_block (ops.test_open_collision.OpenCollisionTests.test_no_candidate_emits_explicit_empty_collisions_block) ... ok
test_prose_about_prior_raised_opens_does_not_activate_a_scan (ops.test_open_collision.OpenCollisionTests.test_prose_about_prior_raised_opens_does_not_activate_a_scan) ... ok

----------------------------------------------------------------------
Ran 9 tests in 0.395s

OK

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..af37f0183fc0cc281feb2a927a9ed922dfb338e8
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..8c280cd53468d9615267cc259712457e15dfbac9
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,19 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

apply patch
patch: completed
/Users/dc/code/math/jc2/ops/open_collision.py
/Users/dc/code/math/jc2/ops/test_open_collision.py
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc 'python3 -m unittest -v ops/test_open_collision.py' in /Users/dc/code/math/jc2
 succeeded in 340ms:
ok
test_candidate_fanout_is_bounded_and_fails_closed (ops.test_open_collision.OpenCollisionTests.test_candidate_fanout_is_bounded_and_fails_closed) ... ok
test_deg_af_retroactive_case_surfaces_all_three_banked_lines (ops.test_open_collision.OpenCollisionTests.test_deg_af_retroactive_case_surfaces_all_three_banked_lines)
Reproduce the coordinator's 2026-09-02 smallest useful test. ... ok
test_malformed_open_in_raised_section_fails_closed (ops.test_open_collision.OpenCollisionTests.test_malformed_open_in_raised_section_fails_closed) ... ok
test_missing_required_bank_file_fails_closed_with_a_block (ops.test_open_collision.OpenCollisionTests.test_missing_required_bank_file_fails_closed_with_a_block) ... ok
test_multiple_same_line_raised_opens_are_all_checked (ops.test_open_collision.OpenCollisionTests.test_multiple_same_line_raised_opens_are_all_checked) ... ok
test_no_candidate_emits_explicit_empty_collisions_block (ops.test_open_collision.OpenCollisionTests.test_no_candidate_emits_explicit_empty_collisions_block) ... ok
test_prose_about_prior_raised_opens_does_not_activate_a_scan (ops.test_open_collision.OpenCollisionTests.test_prose_about_prior_raised_opens_does_not_activate_a_scan) ... ok

----------------------------------------------------------------------
Ran 10 tests in 0.425s

OK

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "rg -n 'manifest_from_json|duplicate' box/preflight.py ops/test_preflight.py | tail -30 && sed -n '875,930p' box/preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
box/preflight.py:446:        raise PreflightError("duplicate ideal generator name")
box/preflight.py:916:def _reject_duplicate_json_keys(pairs: Sequence[Tuple[str, Any]]) -> Dict[str, Any]:
box/preflight.py:920:            raise PreflightError(f"duplicate JSON key {key!r}")
box/preflight.py:925:def manifest_from_json(source: str) -> Manifest:
box/preflight.py:926:    """Load the strict contract, rejecting duplicate keys at every depth."""
box/preflight.py:928:        raw = json.loads(source, object_pairs_hook=_reject_duplicate_json_keys)
box/preflight.py:949:        manifest = manifest_from_json(Path(args.manifest).read_text(encoding="utf-8"))
ops/test_preflight.py:147:        duplicated = encoded.replace(
ops/test_preflight.py:150:        with self.assertRaisesRegex(gate.PreflightError, "duplicate JSON key"):
ops/test_preflight.py:151:            gate.manifest_from_json(duplicated)
                    _typed_from_json(item["target"], f"{label}.target"),
                    _json_string(item["source_domain"], f"{label}.source_domain"),
                    _json_string(item["target_domain"], f"{label}.target_domain"),
                    _json_string(item["source_scope"], f"{label}.source_scope"),
                    _json_string(item["target_scope"], f"{label}.target_scope"),
                    _json_string(item["claim"], f"{label}.claim"),
                    tuple(
                        Arrow(value)
                        for value in _json_strings(item["arrows"], f"{label}.arrows")
                    ),
                )
            )
        return Manifest(
            _json_integer(top["schema_version"], "schema_version"),
            _json_string(top["job_id"], "job_id"),
            _json_integer(top["degree_n"], "degree_n"),
            _json_string(top["cell"], "cell"),
            _json_string(top["coefficient_field"], "coefficient_field"),
            _json_string(top["monomial_order"], "monomial_order"),
            _json_strings(top["ring_generators"], "ring_generators"),
            _json_strings(ring_map["source_order"], "ring_map.source_order"),
            _json_strings(ring_map["images"], "ring_map.images"),
            tuple(roots), tuple(ideals),
            _json_strings(top["generator_order"], "generator_order"),
            tuple(opens),
            SourceFidelity(
                _json_strings(source["ring_generators"], "source_fidelity.ring_generators"),
                _json_string(source["old_eq2"], "source_fidelity.old_eq2"),
                _json_string(source["q"], "source_fidelity.q"),
                _json_string(source["e1"], "source_fidelity.e1"),
                _json_string(source["encoded_eq2_even"], "source_fidelity.encoded_eq2_even"),
            ),
            _typed_from_json(top["output"], "output"), tuple(transitions),
            packages, _json_string(stack["msolve"], "stack.msolve"),
        )
    except (KeyError, TypeError, ValueError) as exc:
        if isinstance(exc, PreflightError):
            raise
        raise PreflightError(f"invalid manifest structure: {exc}") from exc


def _reject_duplicate_json_keys(pairs: Sequence[Tuple[str, Any]]) -> Dict[str, Any]:
    result: Dict[str, Any] = {}
    for key, value in pairs:
        if key in result:
            raise PreflightError(f"duplicate JSON key {key!r}")
        result[key] = value
    return result


def manifest_from_json(source: str) -> Manifest:
    """Load the strict contract, rejecting duplicate keys at every depth."""
    try:
        raw = json.loads(source, object_pairs_hook=_reject_duplicate_json_keys)
    except (json.JSONDecodeError, TypeError) as exc:
        raise PreflightError(f"invalid JSON: {exc}") from exc

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "sed -n '925,965p' box/preflight.py && sed -n '132,156p' ops/test_preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
def manifest_from_json(source: str) -> Manifest:
    """Load the strict contract, rejecting duplicate keys at every depth."""
    try:
        raw = json.loads(source, object_pairs_hook=_reject_duplicate_json_keys)
    except (json.JSONDecodeError, TypeError) as exc:
        raise PreflightError(f"invalid JSON: {exc}") from exc
    return manifest_from_dict(raw)


def main(argv: Optional[Sequence[str]] = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("manifest", nargs="?", help="preflight JSON emitted by the generator")
    parser.add_argument(
        "--self-test", action="store_true",
        help="run the pure exact built-in canaries only (does not approve launch)",
    )
    args = parser.parse_args(argv)
    if args.self_test:
        ok, messages = run_builtin_canaries()
        print(json.dumps({"ok": ok, "checks": messages}, indent=2))
        return 0 if ok else 1
    if not args.manifest:
        parser.error("manifest is required unless --self-test is used")
    try:
        manifest = manifest_from_json(Path(args.manifest).read_text(encoding="utf-8"))
        result = preflight(manifest)
    except Exception as exc:
        result = PreflightResult(False, "<unparsed>", (), (f"{type(exc).__name__}: {exc}",), None)
    print(json.dumps(result.to_dict(), indent=2, sort_keys=True))
    return 0 if result.ok else 1


if __name__ == "__main__":
    raise SystemExit(main())
    def test_json_contract_round_trip(self) -> None:
        manifest = gate.build_reference_manifest("86A")
        payload = json.loads(json.dumps(gate.manifest_to_dict(manifest)))
        parsed = gate.manifest_from_dict(payload)
        self.assertEqual(parsed, manifest)
        self.assertTrue(self.run_gate(parsed).ok)
        payload["misspelled_field"] = True
        with self.assertRaises(gate.PreflightError):
            gate.manifest_from_dict(payload)
        del payload["misspelled_field"]
        payload["degree_n"] = "6"
        with self.assertRaisesRegex(gate.PreflightError, "must be an integer"):
            gate.manifest_from_dict(payload)

        encoded = json.dumps(gate.manifest_to_dict(manifest))
        duplicated = encoded.replace(
            '"degree_n": 6', '"degree_n": 5, "degree_n": 6', 1
        )
        with self.assertRaisesRegex(gate.PreflightError, "duplicate JSON key"):
            gate.manifest_from_json(duplicated)


class TypedTransitionTests(unittest.TestCase):
    def run_gate(self, manifest: gate.Manifest) -> gate.PreflightResult:
        return gate.preflight(manifest, stack_observation=GOOD_STACK)

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

collab: Wait
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

collab: Wait
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "rg -n 'solver_input|solver-input|sha256|stale' box/preflight.py ops/test_preflight.py | tail -100" in /Users/dc/code/math/jc2
 succeeded in 0ms:
ops/test_preflight.py:110:        stale = replace(manifest.source_fidelity, encoded_eq2_even="oldEQ2")
ops/test_preflight.py:111:        result = self.run_gate(replace(manifest, source_fidelity=stale))
box/preflight.py:150:    solver_input_sha256: str
box/preflight.py:592:        stale = SourceFidelity(
box/preflight.py:597:            _validate_source_fidelity(stale)

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "sed -n '120,170p' box/preflight.py && rg -n '_validate_solver|render_solver|solver_input' box/preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
    """Data needed to derive corrected EQ2_even, rather than trust a label."""

    ring_generators: Tuple[str, ...]
    old_eq2: str
    q: str
    e1: str
    encoded_eq2_even: str


@dataclass(frozen=True)
class StackObservation:
    package_versions: Mapping[str, Optional[str]]
    msolve_version: Optional[str]
    msolve_binary: Optional[str] = None


@dataclass(frozen=True)
class Manifest:
    schema_version: int
    job_id: str
    degree_n: int
    cell: str
    coefficient_field: str
    monomial_order: str
    ring_generators: Tuple[str, ...]
    map_source_order: Tuple[str, ...]
    map_images: Tuple[str, ...]
    reduced_root: Tuple[RootCoefficient, ...]
    ideal_generators: Tuple[PolynomialSlot, ...]
    generator_order: Tuple[str, ...]
    solver_input_sha256: str
    open_factors: Tuple[OpenFactor, ...]
    source_fidelity: SourceFidelity
    output: TypedOutput
    transitions: Tuple[Transition, ...]
    package_versions: Mapping[str, str]
    msolve_version: str


@dataclass(frozen=True)
class PreflightResult:
    ok: bool
    job_id: str
    checks: Tuple[str, ...]
    errors: Tuple[str, ...]
    output: Optional[TypedOutput]

    def to_dict(self) -> Dict[str, Any]:
        return {
            "ok": self.ok,
            "job_id": self.job_id,
150:    solver_input_sha256: str

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

collab: Wait
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "rg -n 'solver_input|solver-input|sha256|stale solver' box/preflight.py ops/test_preflight.py | tail -150" in /Users/dc/code/math/jc2
 succeeded in 0ms:
box/preflight.py:150:    solver_input_sha256: str
box/preflight.py:646:def render_solver_input(manifest: Manifest) -> bytes:
box/preflight.py:655:def _validate_solver_input(manifest: Manifest, payload: Optional[bytes]) -> None:
box/preflight.py:660:    declared = manifest.solver_input_sha256
box/preflight.py:667:        raise PreflightError("solver_input_sha256 is not canonical lowercase SHA-256")
box/preflight.py:668:    observed = hashlib.sha256(payload).hexdigest()
box/preflight.py:727:    solver_input: Optional[bytes] = None,
box/preflight.py:734:        _validate_solver_input(manifest, solver_input)
box/preflight.py:735:        checks.append("solver-input-sha256-and-rows")

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "sed -n '630,750p' box/preflight.py && sed -n '750,840p' box/preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:

def _validate_stack(manifest: Manifest, observed: StackObservation) -> None:
    if dict(manifest.package_versions) != EXPECTED_PACKAGES:
        raise PreflightError("manifest package pins differ from the default stack")
    if manifest.msolve_version != EXPECTED_MSOLVE:
        raise PreflightError("manifest msolve pin differs from the default stack")
    if dict(observed.package_versions) != EXPECTED_PACKAGES:
        raise PreflightError(
            f"package version gate failed: observed={dict(observed.package_versions)!r}"
        )
    if observed.msolve_version != EXPECTED_MSOLVE:
        raise PreflightError(
            f"msolve version gate failed: observed={observed.msolve_version!r}"
        )


def render_solver_input(manifest: Manifest) -> bytes:
    """Render the canonical msolve payload represented by a manifest."""
    lines = [",".join(manifest.ring_generators), "0"]
    for index, generator in enumerate(manifest.ideal_generators):
        suffix = "," if index + 1 < len(manifest.ideal_generators) else ""
        lines.append(generator.expression + suffix)
    return ("\n".join(lines) + "\n").encode("utf-8")


def _validate_solver_input(manifest: Manifest, payload: Optional[bytes]) -> None:
    if payload is None:
        raise PreflightError("solver input bytes/path are required for launch approval")
    if not isinstance(payload, bytes):
        raise PreflightError("solver input must be supplied as exact bytes")
    declared = manifest.solver_input_sha256
    if (
        not isinstance(declared, str)
        or len(declared) != 64
        or declared != declared.lower()
        or any(character not in "0123456789abcdef" for character in declared)
    ):
        raise PreflightError("solver_input_sha256 is not canonical lowercase SHA-256")
    observed = hashlib.sha256(payload).hexdigest()
    if observed != declared:
        raise PreflightError(
            f"solver input SHA-256 mismatch: observed={observed} declared={declared}"
        )
    if not payload.endswith(b"\n") or b"\r" in payload or b"\x00" in payload:
        raise PreflightError("solver input must be NUL-free POSIX text with a final LF")
    try:
        text = payload.decode("utf-8")
    except UnicodeDecodeError as exc:
        raise PreflightError(f"solver input is not UTF-8: {exc}") from exc
    lines = text.splitlines()
    if len(lines) < 3:
        raise PreflightError("solver input is missing ring, characteristic, or generators")
    if tuple(lines[0].split(",")) != manifest.ring_generators:
        raise PreflightError("solver input variable line/order differs from manifest")
    if lines[1] != "0":
        raise PreflightError("solver input characteristic is not declared QQ/0")
    encoded_rows = lines[2:]
    if len(encoded_rows) != len(manifest.ideal_generators):
        raise PreflightError("solver input generator count differs from manifest")
    parsed_rows = []
    for index, line in enumerate(encoded_rows):
        if not line or line != line.strip():
            raise PreflightError(f"solver input generator row {index} is empty/padded")
        is_last = index + 1 == len(encoded_rows)
        if is_last:
            if line.endswith(","):
                raise PreflightError("final solver generator must not have a trailing comma")
            expression = line
        else:
            if not line.endswith(",") or line.endswith(",,"):
                raise PreflightError("nonfinal solver generator must have one trailing comma")
            expression = line[:-1]
        try:
            parsed_rows.append(algebra.parse_mv(expression, manifest.ring_generators))
        except (TypeError, ValueError) as exc:
            raise PreflightError(f"solver input generator {index} failed to parse: {exc}") from exc
    for index, (parsed, declared_generator) in enumerate(
        zip(parsed_rows, manifest.ideal_generators)
    ):
        try:
            expected = algebra.parse_mv(
                declared_generator.expression, manifest.ring_generators
            )
        except (TypeError, ValueError) as exc:
            raise PreflightError(
                f"manifest solver generator {index} failed to parse: {exc}"
            ) from exc
        if parsed != expected:
            raise PreflightError(
                f"solver input generator {index} differs polynomially from manifest"
            )



def preflight(
    manifest: Manifest,
    *,
    solver_input: Optional[bytes] = None,
    stack_observation: Optional[StackObservation] = None,
) -> PreflightResult:
    """Validate a launch manifest; actual stack probing is the default."""
    checks: list[str] = []
    try:
        _validate_manifest(manifest, checks)
        _validate_solver_input(manifest, solver_input)
        checks.append("solver-input-sha256-and-rows")
        canaries_ok, canary_messages = run_builtin_canaries()
        if not canaries_ok:
            raise PreflightError("built-in semantic canary failed: " + "; ".join(canary_messages))
        checks.extend(canary_messages)
        observed = stack_observation if stack_observation is not None else probe_default_stack()
        _validate_stack(manifest, observed)
        checks.append("default-stack-pins")
    except Exception as exc:
        return PreflightResult(
            False, getattr(manifest, "job_id", "<unparsed>"), tuple(checks),
            (f"{type(exc).__name__}: {exc}",), getattr(manifest, "output", None),
        )
    return PreflightResult(True, manifest.job_id, tuple(checks), (), manifest.output)



def _open_equation(factor: str, inverse: str, names: Tuple[str, ...]) -> str:
    fpoly = algebra.parse_mv(factor, names)
    ipoly = algebra.parse_mv(inverse, names)
    return algebra.render_mv(ipoly * fpoly - 1)


def build_reference_manifest(
    cell_name: str = "96B",
    *,
    degree_n: int = 6,
    output: TypedOutput = TypedOutput(Artifact.CURVE, Attainment.REALIZED),
) -> Manifest:
    """Construct a faithful in-memory manifest; useful to generators/tests."""
    cell = CELLS[cell_name]
    source_names, root = _expand(cell)
    target_names = source_names + ("u",)
    reduced_root = tuple(
        RootCoefficient(degree, algebra.render_mv(root[degree]))
        for degree in sorted(root, reverse=True)
    )
    ideal = []
    order = []
    for degree in range(cell.closed_hi, cell.closed_lo - 1, -1):
        name = f"g_{degree}"
        order.append(name)
        ideal.append(PolynomialSlot(name, algebra.render_mv(root[degree])))
    factor_name = f"g_{cell.open_degree}"
    factor = algebra.render_mv(root[cell.open_degree])
    equation_name = f"open_g_{cell.open_degree}"
    order.append(equation_name)
    ideal.append(
        PolynomialSlot(equation_name, _open_equation(factor, "u", target_names))
    )
    return Manifest(
        SCHEMA_VERSION, f"reference-{cell_name}", degree_n, cell_name, "QQ",
        "GRevLex", target_names, target_names, target_names, reduced_root,
        tuple(ideal), tuple(order),
        (OpenFactor(factor_name, factor, "u", equation_name),),
        SourceFidelity(
            ("oldEQ2", "q", "E1"), "oldEQ2", "q", "E1",
            "oldEQ2-2*q*E1",
        ),
        output, (), dict(EXPECTED_PACKAGES), EXPECTED_MSOLVE,
    )


def manifest_to_dict(manifest: Manifest) -> Dict[str, Any]:
    def typed(value: TypedOutput) -> Dict[str, str]:
        return {"artifact": value.artifact.value, "attainment": value.attainment.value}

    return {
        "schema_version": manifest.schema_version,
        "job_id": manifest.job_id,
        "degree_n": manifest.degree_n,
        "cell": manifest.cell,
        "coefficient_field": manifest.coefficient_field,
        "monomial_order": manifest.monomial_order,
        "ring_generators": list(manifest.ring_generators),
        "ring_map": {
            "source_order": list(manifest.map_source_order),
            "images": list(manifest.map_images),
        },
        "reduced_root": [
            {"degree": slot.degree, "expression": slot.expression}
            for slot in manifest.reduced_root
        ],
        "ideal_generators": [
            {"name": slot.name, "expression": slot.expression}
            for slot in manifest.ideal_generators
        ],
        "generator_order": list(manifest.generator_order),
        "open_factors": [vars(item) for item in manifest.open_factors],
        "source_fidelity": {
            "ring_generators": list(manifest.source_fidelity.ring_generators),
            "old_eq2": manifest.source_fidelity.old_eq2,
            "q": manifest.source_fidelity.q,
            "e1": manifest.source_fidelity.e1,
            "encoded_eq2_even": manifest.source_fidelity.encoded_eq2_even,
        },
        "output": typed(manifest.output),
        "transitions": [
            {
                "source": typed(item.source), "target": typed(item.target),
                "source_domain": item.source_domain,
                "target_domain": item.target_domain,
                "source_scope": item.source_scope, "target_scope": item.target_scope,
                "claim": item.claim,
                "arrows": [arrow.value for arrow in item.arrows],
            }
            for item in manifest.transitions

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

2026-09-02T09:43:07.430004Z ERROR codex_core::tools::router: error=apply_patch verification failed: Failed to find expected lines in /Users/dc/code/math/jc2/box/preflight.py:
        _validate_manifest(manifest, checks)
        _validate_solver_input(manifest, solver_input)
        checks.append("solver-input-sha256-and-rows")
        checks.append("solver-input-sha256-and-rows")
exec
/bin/zsh -lc "rg -n 'solver_input_sha256' box/preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
150:    solver_input_sha256: str
660:    declared = manifest.solver_input_sha256
667:        raise PreflightError("solver_input_sha256 is not canonical lowercase SHA-256")

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

collab: Wait
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..4707437d04c6d5618e0ea2ea5f4b2a4a780831c6
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..85a0ce81907baf167664a09dc45dba0357b73c69
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,34 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

apply patch
patch: completed
/Users/dc/code/math/jc2/ops/open_collision.py
/Users/dc/code/math/jc2/ops/test_open_collision.py
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc 'python3 -m unittest -v ops/test_open_collision.py' in /Users/dc/code/math/jc2
 succeeded in 376ms:
ok
test_candidate_fanout_is_bounded_and_fails_closed (ops.test_open_collision.OpenCollisionTests.test_candidate_fanout_is_bounded_and_fails_closed) ... ok
test_deg_af_retroactive_case_surfaces_all_three_banked_lines (ops.test_open_collision.OpenCollisionTests.test_deg_af_retroactive_case_surfaces_all_three_banked_lines)
Reproduce the coordinator's 2026-09-02 smallest useful test. ... ok
test_latex_inequality_is_scanned_as_a_bounded_relation (ops.test_open_collision.OpenCollisionTests.test_latex_inequality_is_scanned_as_a_bounded_relation) ... ok
test_malformed_open_in_raised_section_fails_closed (ops.test_open_collision.OpenCollisionTests.test_malformed_open_in_raised_section_fails_closed) ... ok
test_missing_required_bank_file_fails_closed_with_a_block (ops.test_open_collision.OpenCollisionTests.test_missing_required_bank_file_fails_closed_with_a_block) ... ok
test_multiple_same_line_raised_opens_are_all_checked (ops.test_open_collision.OpenCollisionTests.test_multiple_same_line_raised_opens_are_all_checked) ... ok
test_no_candidate_emits_explicit_empty_collisions_block (ops.test_open_collision.OpenCollisionTests.test_no_candidate_emits_explicit_empty_collisions_block) ... ok
test_prose_about_prior_raised_opens_does_not_activate_a_scan (ops.test_open_collision.OpenCollisionTests.test_prose_about_prior_raised_opens_does_not_activate_a_scan) ... ok

----------------------------------------------------------------------
Ran 11 tests in 0.470s

OK

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

collab: Wait
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "rg -n 'solver_input_sha256|solver-input|solver_input' box/preflight.py ops/test_preflight.py | tail -160" in /Users/dc/code/math/jc2
 succeeded in 0ms:
box/preflight.py:150:    solver_input_sha256: str
box/preflight.py:646:def render_solver_input(manifest: Manifest) -> bytes:
box/preflight.py:655:def _validate_solver_input(manifest: Manifest, payload: Optional[bytes]) -> None:
box/preflight.py:660:    declared = manifest.solver_input_sha256
box/preflight.py:667:        raise PreflightError("solver_input_sha256 is not canonical lowercase SHA-256")
box/preflight.py:727:    solver_input: Optional[bytes] = None,
box/preflight.py:734:        _validate_solver_input(manifest, solver_input)
box/preflight.py:735:        checks.append("solver-input-sha256-and-rows")
box/preflight.py:797:        solver_input_sha256=hashlib.sha256(render_solver_input(draft)).hexdigest(),
box/preflight.py:826:        "solver_input_sha256": manifest.solver_input_sha256,
box/preflight.py:905:        "ideal_generators", "generator_order", "solver_input_sha256", "open_factors",
box/preflight.py:989:            _json_string(top["solver_input_sha256"], "solver_input_sha256"),
box/preflight.py:1028:    parser.add_argument("solver_input", nargs="?", help="exact .ms file that would be launched")
box/preflight.py:1038:    if not args.manifest or not args.solver_input:
box/preflight.py:1039:        parser.error("manifest and solver_input are required unless --self-test is used")
box/preflight.py:1043:            manifest, solver_input=Path(args.solver_input).read_bytes()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "sed -n '760,815p' box/preflight.py && sed -n '810,845p' box/preflight.py && sed -n '1015,1055p' box/preflight.py && sed -n '1,90p' ops/test_preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
    degree_n: int = 6,
    output: TypedOutput = TypedOutput(Artifact.CURVE, Attainment.REALIZED),
) -> Manifest:
    """Construct a faithful in-memory manifest; useful to generators/tests."""
    cell = CELLS[cell_name]
    source_names, root = _expand(cell)
    target_names = source_names + ("u",)
    reduced_root = tuple(
        RootCoefficient(degree, algebra.render_mv(root[degree]))
        for degree in sorted(root, reverse=True)
    )
    ideal = []
    order = []
    for degree in range(cell.closed_hi, cell.closed_lo - 1, -1):
        name = f"g_{degree}"
        order.append(name)
        ideal.append(PolynomialSlot(name, algebra.render_mv(root[degree])))
    factor_name = f"g_{cell.open_degree}"
    factor = algebra.render_mv(root[cell.open_degree])
    equation_name = f"open_g_{cell.open_degree}"
    order.append(equation_name)
    ideal.append(
        PolynomialSlot(equation_name, _open_equation(factor, "u", target_names))
    )
    draft = Manifest(
        SCHEMA_VERSION, f"reference-{cell_name}", degree_n, cell_name, "QQ",
        "GRevLex", target_names, target_names, target_names, reduced_root,
        tuple(ideal), tuple(order), "",
        (OpenFactor(factor_name, factor, "u", equation_name),),
        SourceFidelity(
            ("oldEQ2", "q", "E1"), "oldEQ2", "q", "E1",
            "oldEQ2-2*q*E1",
        ),
        output, (), dict(EXPECTED_PACKAGES), EXPECTED_MSOLVE,
    )
    return replace(
        draft,
        solver_input_sha256=hashlib.sha256(render_solver_input(draft)).hexdigest(),
    )


def manifest_to_dict(manifest: Manifest) -> Dict[str, Any]:
    def typed(value: TypedOutput) -> Dict[str, str]:
        return {"artifact": value.artifact.value, "attainment": value.attainment.value}

    return {
        "schema_version": manifest.schema_version,
        "job_id": manifest.job_id,
        "degree_n": manifest.degree_n,
        "cell": manifest.cell,
        "coefficient_field": manifest.coefficient_field,
        "monomial_order": manifest.monomial_order,
        "ring_generators": list(manifest.ring_generators),
        "ring_map": {
            "source_order": list(manifest.map_source_order),
            "images": list(manifest.map_images),
        "coefficient_field": manifest.coefficient_field,
        "monomial_order": manifest.monomial_order,
        "ring_generators": list(manifest.ring_generators),
        "ring_map": {
            "source_order": list(manifest.map_source_order),
            "images": list(manifest.map_images),
        },
        "reduced_root": [
            {"degree": slot.degree, "expression": slot.expression}
            for slot in manifest.reduced_root
        ],
        "ideal_generators": [
            {"name": slot.name, "expression": slot.expression}
            for slot in manifest.ideal_generators
        ],
        "generator_order": list(manifest.generator_order),
        "solver_input_sha256": manifest.solver_input_sha256,
        "open_factors": [vars(item) for item in manifest.open_factors],
        "source_fidelity": {
            "ring_generators": list(manifest.source_fidelity.ring_generators),
            "old_eq2": manifest.source_fidelity.old_eq2,
            "q": manifest.source_fidelity.q,
            "e1": manifest.source_fidelity.e1,
            "encoded_eq2_even": manifest.source_fidelity.encoded_eq2_even,
        },
        "output": typed(manifest.output),
        "transitions": [
            {
                "source": typed(item.source), "target": typed(item.target),
                "source_domain": item.source_domain,
                "target_domain": item.target_domain,
                "source_scope": item.source_scope, "target_scope": item.target_scope,
                "claim": item.claim,
                "arrows": [arrow.value for arrow in item.arrows],
            }
            for item in manifest.transitions

def manifest_from_json(source: str) -> Manifest:
    """Load the strict contract, rejecting duplicate keys at every depth."""
    try:
        raw = json.loads(source, object_pairs_hook=_reject_duplicate_json_keys)
    except (json.JSONDecodeError, TypeError) as exc:
        raise PreflightError(f"invalid JSON: {exc}") from exc
    return manifest_from_dict(raw)


def main(argv: Optional[Sequence[str]] = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("manifest", nargs="?", help="preflight JSON emitted by the generator")
    parser.add_argument("solver_input", nargs="?", help="exact .ms file that would be launched")
    parser.add_argument(
        "--self-test", action="store_true",
        help="run the pure exact built-in canaries only (does not approve launch)",
    )
    args = parser.parse_args(argv)
    if args.self_test:
        ok, messages = run_builtin_canaries()
        print(json.dumps({"ok": ok, "checks": messages}, indent=2))
        return 0 if ok else 1
    if not args.manifest or not args.solver_input:
        parser.error("manifest and solver_input are required unless --self-test is used")
    try:
        manifest = manifest_from_json(Path(args.manifest).read_text(encoding="utf-8"))
        result = preflight(
            manifest, solver_input=Path(args.solver_input).read_bytes()
        )
    except Exception as exc:
        result = PreflightResult(False, "<unparsed>", (), (f"{type(exc).__name__}: {exc}",), None)
    print(json.dumps(result.to_dict(), indent=2, sort_keys=True))
    return 0 if result.ok else 1


if __name__ == "__main__":
    raise SystemExit(main())
#!/usr/bin/env python3
"""Focused, solver-free tests for the N>=6 encoding preflight gate."""
from __future__ import annotations

import json
import sys
import unittest
from dataclasses import replace
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
if str(ROOT) not in sys.path:
    sys.path.insert(0, str(ROOT))

from box import preflight as gate  # noqa: E402


GOOD_STACK = gate.StackObservation(
    dict(gate.EXPECTED_PACKAGES), gate.EXPECTED_MSOLVE, "/mock/msolve"
)


class EncodingPreflightTests(unittest.TestCase):
    def run_gate(self, manifest: gate.Manifest) -> gate.PreflightResult:
        # No test invokes msolve or imports qqideal/msolveio.
        return gate.preflight(manifest, stack_observation=GOOD_STACK)

    def test_faithful_96b_emits_root_and_exact_diffs_every_condition(self) -> None:
        result = self.run_gate(gate.build_reference_manifest("96B"))
        self.assertTrue(result.ok, result.errors)
        self.assertIn("reduced-approximate-root", result.checks)
        self.assertIn("encoded-coefficient-exact-diff", result.checks)
        self.assertIn("open-factor-rabinowitsch", result.checks)
        # Corrected 96B is g_16,...,g_3=0 with g_2 open.
        manifest = gate.build_reference_manifest("96B")
        self.assertEqual(
            manifest.generator_order,
            tuple(f"g_{j}" for j in range(16, 2, -1)) + ("open_g_2",),
        )

    def test_exact_diff_rejects_root_or_ideal_coefficient_mutation(self) -> None:
        manifest = gate.build_reference_manifest("86A")
        first_root = replace(
            manifest.reduced_root[0],
            expression=manifest.reduced_root[0].expression + "+1",
        )
        bad_root = replace(
            manifest, reduced_root=(first_root,) + manifest.reduced_root[1:]
        )
        self.assertFalse(self.run_gate(bad_root).ok)

        first_condition = replace(
            manifest.ideal_generators[0],
            expression=manifest.ideal_generators[0].expression + "+1",
        )
        bad_condition = replace(
            manifest,
            ideal_generators=(first_condition,) + manifest.ideal_generators[1:],
        )
        result = self.run_gate(bad_condition)
        self.assertFalse(result.ok)
        self.assertIn("exact coefficient diff", result.errors[0])

    def test_old_8619_false_positive_is_rejected_by_corrected_86a(self) -> None:
        ok, checks = gate.run_builtin_canaries()
        self.assertTrue(ok, checks)
        self.assertIn("86A-rejects-old-(8,6,19)", checks)
        # The canary computes forced g_19=11, rather than trusting a flag.
        names, root = gate.algebra.expand_g86(gate.algebra.AUX_A)
        value = gate.algebra.subst_mv(
            gate.algebra.coeff_t(root, 19, names),
            {**gate.algebra._charged_a_point(), "a22": 4},
        ).as_constant()
        self.assertEqual(value, 11)

    def test_four_node_962_curve_passes_corrected_96b(self) -> None:
        ok, checks = gate.run_builtin_canaries()
        self.assertTrue(ok, checks)
        self.assertIn("96B-accepts-four-node-(9,6,2)", checks)

    def test_fail_closed_on_ring_map_open_and_generator_order_mismatch(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        mutations = {
            "ring": replace(manifest, coefficient_field="QQbar"),
            "map": replace(
                manifest,
                map_images=(manifest.map_images[1],) + manifest.map_images[1:],
            ),
            "open": replace(
                manifest,

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "nl -ba box/preflight.py | sed -n '805,850p'" in /Users/dc/code/math/jc2
 succeeded in 0ms:
   805	    return {
   806	        "schema_version": manifest.schema_version,
   807	        "job_id": manifest.job_id,
   808	        "degree_n": manifest.degree_n,
   809	        "cell": manifest.cell,
   810	        "coefficient_field": manifest.coefficient_field,
   811	        "monomial_order": manifest.monomial_order,
   812	        "ring_generators": list(manifest.ring_generators),
   813	        "ring_map": {
   814	            "source_order": list(manifest.map_source_order),
   815	            "images": list(manifest.map_images),
   816	        },
   817	        "reduced_root": [
   818	            {"degree": slot.degree, "expression": slot.expression}
   819	            for slot in manifest.reduced_root
   820	        ],
   821	        "ideal_generators": [
   822	            {"name": slot.name, "expression": slot.expression}
   823	            for slot in manifest.ideal_generators
   824	        ],
   825	        "generator_order": list(manifest.generator_order),
   826	        "solver_input_sha256": manifest.solver_input_sha256,
   827	        "open_factors": [vars(item) for item in manifest.open_factors],
   828	        "source_fidelity": {
   829	            "ring_generators": list(manifest.source_fidelity.ring_generators),
   830	            "old_eq2": manifest.source_fidelity.old_eq2,
   831	            "q": manifest.source_fidelity.q,
   832	            "e1": manifest.source_fidelity.e1,
   833	            "encoded_eq2_even": manifest.source_fidelity.encoded_eq2_even,
   834	        },
   835	        "output": typed(manifest.output),
   836	        "transitions": [
   837	            {
   838	                "source": typed(item.source), "target": typed(item.target),
   839	                "source_domain": item.source_domain,
   840	                "target_domain": item.target_domain,
   841	                "source_scope": item.source_scope, "target_scope": item.target_scope,
   842	                "claim": item.claim,
   843	                "arrows": [arrow.value for arrow in item.arrows],
   844	            }
   845	            for item in manifest.transitions
   846	        ],
   847	        "stack": {
   848	            "packages": dict(manifest.package_versions),
   849	            "msolve": manifest.msolve_version,
   850	        },

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

collab: Wait
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc '/usr/bin/time -p python3 -m unittest -v ops/test_preflight.py' in /Users/dc/code/math/jc2
 succeeded in 2091ms:
ok
test_fail_closed_on_ring_map_open_and_generator_order_mismatch (ops.test_preflight.EncodingPreflightTests.test_fail_closed_on_ring_map_open_and_generator_order_mismatch) ... ok
test_faithful_96b_emits_root_and_exact_diffs_every_condition (ops.test_preflight.EncodingPreflightTests.test_faithful_96b_emits_root_and_exact_diffs_every_condition) ... ok
test_four_node_962_curve_passes_corrected_96b (ops.test_preflight.EncodingPreflightTests.test_four_node_962_curve_passes_corrected_96b) ... ok
test_json_contract_round_trip (ops.test_preflight.EncodingPreflightTests.test_json_contract_round_trip) ... ok
test_n_below_six_and_stack_drift_fail_closed (ops.test_preflight.EncodingPreflightTests.test_n_below_six_and_stack_drift_fail_closed) ... ok
test_old_8619_false_positive_is_rejected_by_corrected_86a (ops.test_preflight.EncodingPreflightTests.test_old_8619_false_positive_is_rejected_by_corrected_86a) ... ok
test_solver_input_is_required_and_bound_beyond_its_digest (ops.test_preflight.EncodingPreflightTests.test_solver_input_is_required_and_bound_beyond_its_digest) ... ok
test_source_fidelity_cannot_be_renamed_or_vacuous (ops.test_preflight.EncodingPreflightTests.test_source_fidelity_cannot_be_renamed_or_vacuous) ... ok
test_source_fidelity_omitting_minus_2qE1_fails (ops.test_preflight.EncodingPreflightTests.test_source_fidelity_omitting_minus_2qE1_fails) ... ok
test_advanced_output_must_end_a_checked_provenance_path (ops.test_preflight.TypedTransitionTests.test_advanced_output_must_end_a_checked_provenance_path) ... ok
test_full_eo_to_actual_map_requires_every_declared_arrow (ops.test_preflight.TypedTransitionTests.test_full_eo_to_actual_map_requires_every_declared_arrow) ... ok
test_rejects_a2_cell_to_b3_kill (ops.test_preflight.TypedTransitionTests.test_rejects_a2_cell_to_b3_kill) ... ok
test_rejects_local_torsion_to_global_torsion (ops.test_preflight.TypedTransitionTests.test_rejects_local_torsion_to_global_torsion) ... ok
test_subsystem_to_full_eo_requires_equations_and_quotient (ops.test_preflight.TypedTransitionTests.test_subsystem_to_full_eo_requires_equations_and_quotient) ... ok
test_two_field_output_rejects_impossible_pair (ops.test_preflight.TypedTransitionTests.test_two_field_output_rejects_impossible_pair) ... ok

----------------------------------------------------------------------
Ran 16 tests in 2.144s

OK
real 2.22
user 2.16
sys 0.02

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "sed -n '15,150p' ops/test_preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:

from box import preflight as gate  # noqa: E402


GOOD_STACK = gate.StackObservation(
    dict(gate.EXPECTED_PACKAGES), gate.EXPECTED_MSOLVE, "/mock/msolve"
)


class EncodingPreflightTests(unittest.TestCase):
    def run_gate(
        self, manifest: gate.Manifest, solver_input: bytes | None = None
    ) -> gate.PreflightResult:
        # No test invokes msolve or imports qqideal/msolveio.
        payload = gate.render_solver_input(manifest) if solver_input is None else solver_input
        return gate.preflight(
            manifest, solver_input=payload, stack_observation=GOOD_STACK
        )

    def test_faithful_96b_emits_root_and_exact_diffs_every_condition(self) -> None:
        result = self.run_gate(gate.build_reference_manifest("96B"))
        self.assertTrue(result.ok, result.errors)
        self.assertIn("reduced-approximate-root", result.checks)
        self.assertIn("encoded-coefficient-exact-diff", result.checks)
        self.assertIn("open-factor-rabinowitsch", result.checks)
        self.assertIn("solver-input-sha256-and-rows", result.checks)
        # Corrected 96B is g_16,...,g_3=0 with g_2 open.
        manifest = gate.build_reference_manifest("96B")
        self.assertEqual(
            manifest.generator_order,
            tuple(f"g_{j}" for j in range(16, 2, -1)) + ("open_g_2",),
        )

    def test_exact_diff_rejects_root_or_ideal_coefficient_mutation(self) -> None:
        manifest = gate.build_reference_manifest("86A")
        first_root = replace(
            manifest.reduced_root[0],
            expression=manifest.reduced_root[0].expression + "+1",
        )
        bad_root = replace(
            manifest, reduced_root=(first_root,) + manifest.reduced_root[1:]
        )
        self.assertFalse(self.run_gate(bad_root).ok)

        first_condition = replace(
            manifest.ideal_generators[0],
            expression=manifest.ideal_generators[0].expression + "+1",
        )
        bad_condition = replace(
            manifest,
            ideal_generators=(first_condition,) + manifest.ideal_generators[1:],
        )
        result = self.run_gate(bad_condition)
        self.assertFalse(result.ok)
        self.assertIn("exact coefficient diff", result.errors[0])

    def test_old_8619_false_positive_is_rejected_by_corrected_86a(self) -> None:
        ok, checks = gate.run_builtin_canaries()
        self.assertTrue(ok, checks)
        self.assertIn("86A-rejects-old-(8,6,19)", checks)
        # The canary computes forced g_19=11, rather than trusting a flag.
        names, root = gate.algebra.expand_g86(gate.algebra.AUX_A)
        value = gate.algebra.subst_mv(
            gate.algebra.coeff_t(root, 19, names),
            {**gate.algebra._charged_a_point(), "a22": 4},
        ).as_constant()
        self.assertEqual(value, 11)

    def test_four_node_962_curve_passes_corrected_96b(self) -> None:
        ok, checks = gate.run_builtin_canaries()
        self.assertTrue(ok, checks)
        self.assertIn("96B-accepts-four-node-(9,6,2)", checks)

    def test_fail_closed_on_ring_map_open_and_generator_order_mismatch(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        mutations = {
            "ring": replace(manifest, coefficient_field="QQbar"),
            "map": replace(
                manifest,
                map_images=(manifest.map_images[1],) + manifest.map_images[1:],
            ),
            "open": replace(
                manifest,
                open_factors=(
                    replace(
                        manifest.open_factors[0],
                        expression=manifest.open_factors[0].expression + "+1",
                    ),
                ),
            ),
            "order": replace(
                manifest, generator_order=tuple(reversed(manifest.generator_order))
            ),
        }
        for label, malformed in mutations.items():
            with self.subTest(label=label):
                result = self.run_gate(malformed)
                self.assertFalse(result.ok)
                self.assertTrue(result.errors)

    def test_source_fidelity_omitting_minus_2qE1_fails(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        stale = replace(manifest.source_fidelity, encoded_eq2_even="oldEQ2")
        result = self.run_gate(replace(manifest, source_fidelity=stale))
        self.assertFalse(result.ok)
        self.assertIn("EQ2_even source-fidelity mismatch", result.errors[0])

    def test_source_fidelity_cannot_be_renamed_or_vacuous(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        vacuous = gate.SourceFidelity(("x",), "0", "0", "0", "0")
        result = self.run_gate(replace(manifest, source_fidelity=vacuous))
        self.assertFalse(result.ok)
        self.assertIn("generator order", result.errors[0])

    def test_n_below_six_and_stack_drift_fail_closed(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        self.assertFalse(self.run_gate(replace(manifest, degree_n=5)).ok)
        absent = gate.StackObservation(
            {"qqideal": None, "msolveio": None}, None, None
        )
        result = gate.preflight(
            manifest,
            solver_input=gate.render_solver_input(manifest),
            stack_observation=absent,
        )
        self.assertFalse(result.ok)
        self.assertIn("package version gate failed", result.errors[0])

    def test_solver_input_is_required_and_bound_beyond_its_digest(self) -> None:
        manifest = gate.build_reference_manifest("96B")
        missing = gate.preflight(manifest, stack_observation=GOOD_STACK)
        self.assertFalse(missing.ok)
        self.assertIn("solver input bytes/path are required", missing.errors[0])

        original = gate.render_solver_input(manifest).decode("utf-8").splitlines()
        mutations = {}

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "sed -n '150,205p' ops/test_preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
        mutations = {}
        variable_lines = list(original)
        variables = variable_lines[0].split(",")
        variables[0], variables[1] = variables[1], variables[0]
        variable_lines[0] = ",".join(variables)
        mutations["variable-order"] = variable_lines
        characteristic_lines = list(original)
        characteristic_lines[1] = "65521"
        mutations["characteristic"] = characteristic_lines
        closed_lines = list(original)
        closed_lines[2] = "0,"
        mutations["closed-generator"] = closed_lines
        open_lines = list(original)
        open_lines[-1] = "0"
        mutations["rabinowitsch-open"] = open_lines

        for label, lines in mutations.items():
            with self.subTest(label=label):
                payload = ("\n".join(lines) + "\n").encode("utf-8")
                # Even if a stale generator updates the manifest digest, parsed
                # ring/characteristic/row comparison must still reject it.
                digest_matched = replace(
                    manifest,
                    solver_input_sha256=hashlib.sha256(payload).hexdigest(),
                )
                result = self.run_gate(digest_matched, payload)
                self.assertFalse(result.ok)
                self.assertTrue(result.errors)

        banked_payload = (ROOT / "box" / "corrected_8611.ms").read_bytes()
        banked_manifest = replace(
            gate.build_reference_manifest("86A"),
            solver_input_sha256=hashlib.sha256(banked_payload).hexdigest(),
        )
        result = self.run_gate(banked_manifest, banked_payload)
        self.assertTrue(result.ok, result.errors)

    def test_json_contract_round_trip(self) -> None:
        manifest = gate.build_reference_manifest("86A")
        payload = json.loads(json.dumps(gate.manifest_to_dict(manifest)))
        parsed = gate.manifest_from_dict(payload)
        self.assertEqual(parsed, manifest)
        self.assertTrue(self.run_gate(parsed).ok)
        payload["misspelled_field"] = True
        with self.assertRaises(gate.PreflightError):
            gate.manifest_from_dict(payload)
        del payload["misspelled_field"]
        payload["degree_n"] = "6"
        with self.assertRaisesRegex(gate.PreflightError, "must be an integer"):
            gate.manifest_from_dict(payload)

        encoded = json.dumps(gate.manifest_to_dict(manifest))
        duplicated = encoded.replace(
            '"degree_n": 6', '"degree_n": 5, "degree_n": 6', 1
        )
        with self.assertRaisesRegex(gate.PreflightError, "duplicate JSON key"):

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "sed -n '1,16p' ops/test_preflight.py && sed -n '1,32p' box/preflight.py" in /Users/dc/code/math/jc2
 succeeded in 0ms:
#!/usr/bin/env python3
"""Focused, solver-free tests for the N>=6 encoding preflight gate."""
from __future__ import annotations

import json
import hashlib
import sys
import unittest
from dataclasses import replace
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
if str(ROOT) not in sys.path:
    sys.path.insert(0, str(ROOT))

from box import preflight as gate  # noqa: E402
#!/usr/bin/env python3
"""Fail-closed encoding preflight for ``N >= 6`` realization jobs.

The JSON contract is the output of :func:`manifest_to_dict`.  A generator
emits one manifest beside its solver ideal, then launch code runs::

    python3 box/preflight.py job.preflight.json job.ms

Exit zero means that the declared QQ/GRevLex ring and explicit ring map, the
entire reduced approximate root, every ordered ideal generator, the
Rabinowitsch open, the source ``EQ2_even`` identity, the typed output, the
built-in 86A/96B controls, and the pinned solver stack all passed.  The second
path is mandatory: its exact SHA-256 and its parsed ring/characteristic/rows
must match the manifest, which prevents approving one ideal and launching
another.  Missing or unknown data is an error.  No solver is launched here.

The exact corrected ``G86``/``G96`` expansions are reused from
``box/qq_oracle_jobs.py`` (encoding-faithfulness audit r2, section 7.1).
Only the standard library is imported at module load; qqideal and msolveio are
checked by distribution metadata, and msolve is queried only by the default
launch-stack probe.
"""
from __future__ import annotations

import argparse
import hashlib
import importlib.metadata
import json
import shutil
import subprocess
import sys
from dataclasses import dataclass, replace

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

collab: Wait
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

codex
The hard gate is now bound to the exact `.ms` bytes that will be launched: it checks the SHA-256, variable order, characteristic, every closed row, and the Rabinowitsch row. Both independent reviews now pass; I’m running the final normal/optimized suites and then I’ll write and seal the bounded report.
exec
/bin/zsh -lc '/usr/bin/time -p python3 -m unittest -v ops/test_open_collision.py ops/test_preflight.py' in /Users/dc/code/math/jc2
exec
/bin/zsh -lc '/usr/bin/time -p python3 -O -m unittest -q ops/test_open_collision.py ops/test_preflight.py' in /Users/dc/code/math/jc2
exec
/bin/zsh -lc 'python3 -m py_compile ops/open_collision.py ops/test_open_collision.py box/preflight.py ops/test_preflight.py && python3 box/preflight.py --self-test' in /Users/dc/code/math/jc2
 succeeded in 122ms:
{
  "ok": true,
  "checks": [
    "86A-rejects-old-(8,6,19)",
    "96B-accepts-four-node-(9,6,2)",
    "EQ2_even-omission-rejected"
  ]
}

 succeeded in 2523ms:
ok
test_bound_inside_identifier_is_not_a_quantity_description (ops.test_open_collision.OpenCollisionTests.test_bound_inside_identifier_is_not_a_quantity_description) ... ok
test_candidate_fanout_is_bounded_and_fails_closed (ops.test_open_collision.OpenCollisionTests.test_candidate_fanout_is_bounded_and_fails_closed) ... ok
test_deg_af_retroactive_case_surfaces_all_three_banked_lines (ops.test_open_collision.OpenCollisionTests.test_deg_af_retroactive_case_surfaces_all_three_banked_lines)
Reproduce the coordinator's 2026-09-02 smallest useful test. ... ok
test_latex_inequality_is_scanned_as_a_bounded_relation (ops.test_open_collision.OpenCollisionTests.test_latex_inequality_is_scanned_as_a_bounded_relation) ... ok
test_malformed_open_in_raised_section_fails_closed (ops.test_open_collision.OpenCollisionTests.test_malformed_open_in_raised_section_fails_closed) ... ok
test_missing_required_bank_file_fails_closed_with_a_block (ops.test_open_collision.OpenCollisionTests.test_missing_required_bank_file_fails_closed_with_a_block) ... ok
test_multiple_same_line_raised_opens_are_all_checked (ops.test_open_collision.OpenCollisionTests.test_multiple_same_line_raised_opens_are_all_checked) ... ok
test_no_candidate_emits_explicit_empty_collisions_block (ops.test_open_collision.OpenCollisionTests.test_no_candidate_emits_explicit_empty_collisions_block) ... ok
test_prose_about_prior_raised_opens_does_not_activate_a_scan (ops.test_open_collision.OpenCollisionTests.test_prose_about_prior_raised_opens_does_not_activate_a_scan) ... ok
test_exact_diff_rejects_root_or_ideal_coefficient_mutation (ops.test_preflight.EncodingPreflightTests.test_exact_diff_rejects_root_or_ideal_coefficient_mutation) ... ok
test_fail_closed_on_ring_map_open_and_generator_order_mismatch (ops.test_preflight.EncodingPreflightTests.test_fail_closed_on_ring_map_open_and_generator_order_mismatch) ... ok
test_faithful_96b_emits_root_and_exact_diffs_every_condition (ops.test_preflight.EncodingPreflightTests.test_faithful_96b_emits_root_and_exact_diffs_every_condition) ... ok
test_four_node_962_curve_passes_corrected_96b (ops.test_preflight.EncodingPreflightTests.test_four_node_962_curve_passes_corrected_96b) ... ok
test_json_contract_round_trip (ops.test_preflight.EncodingPreflightTests.test_json_contract_round_trip) ... ok
test_n_below_six_and_stack_drift_fail_closed (ops.test_preflight.EncodingPreflightTests.test_n_below_six_and_stack_drift_fail_closed) ... ok
test_old_8619_false_positive_is_rejected_by_corrected_86a (ops.test_preflight.EncodingPreflightTests.test_old_8619_false_positive_is_rejected_by_corrected_86a) ... ok
test_solver_input_is_required_and_bound_beyond_its_digest (ops.test_preflight.EncodingPreflightTests.test_solver_input_is_required_and_bound_beyond_its_digest) ... ok
test_source_fidelity_cannot_be_renamed_or_vacuous (ops.test_preflight.EncodingPreflightTests.test_source_fidelity_cannot_be_renamed_or_vacuous) ... ok
test_source_fidelity_omitting_minus_2qE1_fails (ops.test_preflight.EncodingPreflightTests.test_source_fidelity_omitting_minus_2qE1_fails) ... ok
test_advanced_output_must_end_a_checked_provenance_path (ops.test_preflight.TypedTransitionTests.test_advanced_output_must_end_a_checked_provenance_path) ... ok
test_full_eo_to_actual_map_requires_every_declared_arrow (ops.test_preflight.TypedTransitionTests.test_full_eo_to_actual_map_requires_every_declared_arrow) ... ok
test_rejects_a2_cell_to_b3_kill (ops.test_preflight.TypedTransitionTests.test_rejects_a2_cell_to_b3_kill) ... ok
test_rejects_local_torsion_to_global_torsion (ops.test_preflight.TypedTransitionTests.test_rejects_local_torsion_to_global_torsion) ... ok
test_subsystem_to_full_eo_requires_equations_and_quotient (ops.test_preflight.TypedTransitionTests.test_subsystem_to_full_eo_requires_equations_and_quotient) ... ok
test_two_field_output_rejects_impossible_pair (ops.test_preflight.TypedTransitionTests.test_two_field_output_rejects_impossible_pair) ... ok

----------------------------------------------------------------------
Ran 27 tests in 2.589s

OK
real 2.65
user 2.51
sys 0.11

 succeeded in 2561ms:
----------------------------------------------------------------------
Ran 27 tests in 2.605s

OK
real 2.69
user 2.52
sys 0.12

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "python3 - <<'PY'
from box import preflight as p
stack = p.StackObservation(dict(p.EXPECTED_PACKAGES), p.EXPECTED_MSOLVE, '/mock/msolve')
for cell in p.CELLS:
    manifest = p.build_reference_manifest(cell)
    result = p.preflight(manifest, solver_input=p.render_solver_input(manifest), stack_observation=stack)
    print(f'{cell}: {\"PASS\" if result.ok else result.errors}')
PY" in /Users/dc/code/math/jc2
 succeeded in 1646ms:
86A: PASS
86B: PASS
86C: PASS
86D: PASS
96A: PASS
96B: PASS

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

exec
/bin/zsh -lc "python3 - <<'PY'
from box import preflight as p
s = p.probe_default_stack()
print('qqideal=' + str(s.package_versions['qqideal']))
print('msolveio=' + str(s.package_versions['msolveio']))
print('msolve=' + str(s.msolve_version))
PY" in /Users/dc/code/math/jc2
 succeeded in 0ms:
qqideal=None
msolveio=None
msolve=0.10.1

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()

apply patch
patch: completed
/Users/dc/code/math/jc2/xmodel/systems-collision-preflight-sol56-20260902.md
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()
diff --git a/xmodel/systems-collision-preflight-sol56-20260902.md b/xmodel/systems-collision-preflight-sol56-20260902.md
new file mode 100644
index 0000000000000000000000000000000000000000..17487f1ddfde01bdc85807e0e118d2896f8ca500
--- /dev/null
+++ b/xmodel/systems-collision-preflight-sol56-20260902.md
@@ -0,0 +1,276 @@
+# OPEN-collision hook and realization PREFLIGHT hard gate
+
+**Lane:** `systems-collision-preflight-sol56-20260902`  
+**Status:** IMPLEMENTED / FOCUSED TESTS PASS  
+**Scope:** implementation and regression controls only; no mathematical claim,
+promotion, canonical-ledger edit, or solver verdict.
+
+## 0. Frozen-input gate and write boundary
+
+Before reading the charged designs, SHA-256 was recomputed over the three
+frozen copies.  All values matched the charge exactly:
+
+```text
+e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  inputs/ideation-20260902T0741Z-opus5.md
+717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  inputs/ideation-20260902T0741Z-sol56.md
+4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  inputs/ideation-20260902T0741Z-synthesis.md
+```
+
+Here `inputs/` abbreviates the frozen lane directory
+`/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/`.
+The implementation follows the collision card at frozen Opus lines 519--535,
+the hard-gate specification at frozen Sol lines 408--439, and the corrected
+incidence/control specification in
+`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511`.
+
+Writes are bounded to the four requested implementation/test files and this
+report.  `ops/lane.sh`, `ops/seal.py`, `ops/artifact_finalize.py`, canonical
+ledgers, and other running-lane artifacts were not modified.  The excluded
+tree was not inspected.
+
+## 1. Typed deliverable A -- OPEN/banked collision check
+
+```text
+deliverable       = OPEN_BANKED_COLLISION_CHECK
+implementation    = ops/open_collision.py
+focused_tests     = ops/test_open_collision.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_open_collision.py
+pass_fail_skip     = 11 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 11 passed / 0 failed under python3 -O
+runtime            = 0.47 s focused; every individual test < 10 s
+integration_note   = Section 1.4
+```
+
+### 1.1 Input and extraction contract
+
+`open_collision.py` is pure Python and imports only the standard library.  It
+takes an unsealed report and repository root.  It recognizes explicit,
+anchored raised/opened headings, list entries, bold entries, heading entries,
+and the narrow inline form `THIS REPORT RAISES: OPEN[...]`.  Multiple tokens on
+one declared line are all retained.  Prose that merely discusses previously
+raised items does not activate extraction.  Broken bracket syntax inside an
+active section is an error, not an empty result.
+
+Each extracted token must carry an explicit bounded-quantity description.  A
+word such as `BOUND` inside the token identifier is removed before quantity
+parsing and cannot masquerade as that description.  This deliberate
+fail-closed requirement prevents a vague identifier from expanding into a
+hundreds-of-kilobytes seal artifact.  A producer with an unsearchable question
+must first state the quantity or refine its schema; the hook does not invent
+one.
+
+The corpus is exactly the charged set: nonrecursive `xmodel/*.md`, plus
+top-level `AUDIT.md`, `notes.md`, and `APPROACHES.md`.  The report being sealed
+is excluded by resolved identity.  A missing required file, symlinked corpus
+entry, unreadable/non-UTF-8 file, escaping path, invalid context window, or
+candidate fan-out above the declared cap returns exit 2 with an explicit
+`status: ERROR` block.
+
+### 1.2 Candidate semantics and output
+
+The search is lexical and candidate-only.  It requires the identifier's key
+nouns in a local context window and bounded-quantity terms on the
+relation-bearing line.  It recognizes ASCII, Unicode, and common LaTeX
+inequalities.  A relation line is still eligible when it carries an older
+`OPEN[...]` label; that label is useful provenance, not evidence against a
+collision.  Results are sorted deterministically and rendered as portable
+repository-relative `file:line` locations.  A candidate does not close,
+answer, or promote anything.
+
+Every successful invocation emits exactly one Markdown section headed
+`## COLLISIONS`.  With candidates it reports `status: CANDIDATES`; with no
+candidate or no declared raised item it reports `status: EMPTY` and an
+explicit `NONE` row.  The section is therefore never silently absent.  Errors
+also use the same heading with `status: ERROR`, while the nonzero exit blocks a
+seal wrapper.
+
+### 1.3 Required controls
+
+The retroactive fixture preserves the relevant source text at the exact
+1-based positions from
+`xmodel/companion-curve-alln-opus5-20260902.md`.  Feeding the bounded quantity
+for `OPEN[DEG-AF-VS-N]` produces, in order:
+
+```text
+xmodel/companion-curve-alln-opus5-20260902.md:685
+xmodel/companion-curve-alln-opus5-20260902.md:785
+xmodel/companion-curve-alln-opus5-20260902.md:836
+```
+
+The negative fixture declares a synthetic quantity with no banked match and
+asserts an explicit `status: EMPTY` block and per-token `NONE`.  Additional
+regressions cover same-line multiple tokens, malformed syntax, missing corpus
+files, prose false activation, identifier/quantity confusion, bounded fan-out,
+older labels on candidate lines, bold/heading entries, and LaTeX inequalities.
+
+### 1.4 Seal-time integration note
+
+Immediately before the standalone `BODY-END` marker is written, a future
+`lane.sh` seal phase (or a pre-step called by `seal.py`) would run
+`python3 ops/open_collision.py "$report" --root "$repo"`; any nonzero status
+aborts sealing, while exit 0 supplies the report's single `## COLLISIONS`
+block, which is inserted before `BODY-END` and only then hashed and stamped.
+The wrapper must reject a second block rather than append twice.  This lane
+describes that call boundary only and makes no change to either live tool.
+
+## 2. Typed deliverable B -- encoding-faithfulness PREFLIGHT
+
+```text
+deliverable       = N_GE_6_REALIZATION_PREFLIGHT
+implementation    = box/preflight.py
+focused_tests     = ops/test_preflight.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_preflight.py
+pass_fail_skip     = 16 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 16 passed / 0 failed under python3 -O
+runtime            = 2.14 s focused; every individual test < 10 s
+builtin_canaries   = 3 passed / 0 failed
+integration_note   = Section 2.6
+```
+
+### 2.1 Strict generator/solver contract
+
+The gate consumes a strict JSON manifest and the exact `.ms` file intended for
+launch:
+
+```text
+python3 box/preflight.py job.preflight.json job.ms
+```
+
+Unknown, missing, extra, duplicate, or wrongly typed JSON fields fail.  The
+manifest declares `N`, corrected cell, coefficient field, monomial order,
+ordered ring generators, explicit source order and generator images, the
+entire reduced approximate root, ordered ideal rows, the open factor and
+inverse, source-fidelity data, two-field output type, transition evidence, the
+pinned stack, and `solver_input_sha256`.  `N < 6`, any unknown cell family, a
+non-`QQ` field, or a non-`GRevLex` order is denied.  The bounded native cell set
+is `86A`, `86B`, `86C`, `86D`, `96A`, and `96B`; a new family requires an
+explicit schema extension and otherwise fails closed.
+
+The second CLI path is mandatory.  Its bytes must match the manifest SHA-256,
+be NUL-free UTF-8 POSIX text, and end in LF.  The gate parses the actual
+variable line, characteristic-zero line, comma framing, generator count, every
+ordered closed row, and the final Rabinowitsch row, then compares the parsed
+polynomials to the checked manifest.  Thus a correct sidecar cannot approve a
+stale or different ideal, even if that stale file's digest is copied into a
+new manifest.
+
+### 2.2 Exact root, coefficient, ring-map, and open checks
+
+The corrected expansions are recomputed exactly over `Fraction` arithmetic by
+reusing `box/qq_oracle_jobs.py`: the audit's `G86` with the target-dependent
+auxiliary terms and `G96` with `a15*p*q + a12*q^2 + a9*p + a6*q`.  The emitted
+root must contain exactly the recomputed nonzero coefficient sequence in
+descending degree.  Every encoded closed condition is parsed as a polynomial
+and exact-diffed against both that emitted coefficient and the independently
+recomputed coefficient; rendered-string equality is not trusted.
+
+The declared source ring order is fixed for the chosen cell.  Map images are
+parsed in the target ring and must form an explicit permutation isomorphism of
+the declared generators.  The manifest and actual solver variable orders must
+agree.  Exactly one open is permitted: the corrected `g_c` factor must be
+nonzero, its inverse must be the mapped `u`, and the actual final solver row
+must be polynomially equal to `u*g_c-1`.  No `sat()` wrapper is used.
+
+### 2.3 Fixed semantic and source-fidelity canaries
+
+The built-in controls are computations, not self-reported booleans:
+
+1. The charged old-A point is first checked to satisfy the old 86A closed rows
+   and old open.  Corrected top cancellation then forces the degree-19
+   residual `g19=11`, so corrected 86A rejects it.
+2. For `q=t^6+8*t^2`, `p=t^9+12*t^5+24*t`, and
+   `(a15,a12,a9,a6)=(0,0,0,-64)`, corrected 96B recomputes every closed
+   coefficient `g16,...,g3` as zero and the open coefficient `g2=64`.
+3. Source fidelity uses the canonical nonvacuous tuple
+   `(oldEQ2,q,E1)` and exact identity `EQ2_even=oldEQ2-2*q*E1`.
+   Renamed/zero aliases and the stale expression omitting `-2*q*E1` fail.
+
+These are encoding regression controls only.  Their execution here makes no
+new statement about curves, maps, attainment, or emptiness.
+
+### 2.4 Orthogonal output typing
+
+The accepted artifact field is exactly:
+
+```text
+NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT | FULL_EO_POINT |
+CURVE | REPRESENTATION | FULL_COVER | POLYNOMIAL_PAIR
+```
+
+The accepted attainment field is independently:
+
+```text
+NECESSARY | REALIZED | ADMISSIBLE_PULLBACK | ACTUAL_MAP |
+COUNTEREXAMPLE_CERTIFIED
+```
+
+Impossible pairings, unknown values, and advanced outputs without a checked
+provenance path are rejected.  The fields are emitted separately in the JSON
+result; no lossy single verdict is synthesized.
+
+### 2.5 Checked transition ladder
+
+Advanced A2 outputs must follow the complete, contiguous global path below;
+self-loops, late starts, unknown domains/scopes/claims, missing arrows, extra
+arrows, and noncontiguous states all fail.
+
+| From | To | Exact required arrows |
+|---|---|---|
+| `SUBSYSTEM_POINT/REALIZED` | `FULL_EO_POINT/REALIZED` | `FULL_EO_EQUATIONS`, `DECLARED_QUOTIENT` |
+| `FULL_EO_POINT/REALIZED` | `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `EO_ALL_OPENS`, `BOUNDARY_DATA`, `GENERAL_FIBRE_DATA`, `FIELD_DEGREE_FOUR` |
+| `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `POLYNOMIAL_PAIR/ACTUAL_MAP` | `SOURCE_CHART` |
+| `POLYNOMIAL_PAIR/ACTUAL_MAP` | `POLYNOMIAL_PAIR/COUNTEREXAMPLE_CERTIFIED` | `NONINVERTIBILITY`, `GEOMETRIC_DEGREE` |
+
+The table explicitly rejects an A2-to-B3 kill, local-to-global torsion, and a
+full-E/O point promoted to an actual map without the complete intermediate
+state and every declared arrow.
+
+### 2.6 Launch integration note
+
+A realization generator writes the strict manifest and `.ms` payload from the
+same ordered coefficient data, records the payload SHA-256 in the manifest,
+and the launch wrapper invokes `python3 box/preflight.py manifest.json job.ms`
+before starting qqideal/msolve; only exit 0 permits launch, the emitted JSON is
+retained as custody evidence, and the wrapper launches those same hash-pinned
+bytes (or rechecks the digest immediately before exec).  This lane implements
+the gate but does not alter any currently running launcher.
+
+## 3. Verification ledger
+
+Final local commands and results:
+
+```text
+python3 -m py_compile ops/open_collision.py ops/test_open_collision.py \
+    box/preflight.py ops/test_preflight.py
+PASS
+
+python3 -m unittest -v ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.589s -- OK
+
+python3 -O -m unittest -q ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.605s -- OK
+
+python3 box/preflight.py --self-test
+3/3 exact canaries PASS
+```
+
+All six reference manifests (`86A/B/C/D`, `96A/B`) also pass with their
+canonical in-memory solver payloads and an injected matching stack observation.
+No unit test invokes `msolve` or imports `qqideal`/`msolveio`.  The local live
+stack probe found `msolve=0.10.1`, while `qqideal` and `msolveio` are absent;
+therefore a real launch on this host is correctly refused until the pinned
+`qqideal=0.2.0` and `msolveio=0.2.1` packages are present.
+
+## 4. Guardrail and disposition
+
+This is an implementation lane.  Collision hits are candidates for human
+review, never closures.  Solver preflight success means only that the encoded
+job, controls, stack, solver bytes, and declared type transitions satisfy this
+contract; it is not a solver result or a mathematical realization.  The
+variable/ring map is explicit, the open is Rabinowitsch, raw remainder slots
+are exact-diffed in the declared ring/order, and attainment cannot be inferred
+from a weaker carrier.  No exit-price assertion is made, so no `charge_basis`
+line is emitted.
+

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()
diff --git a/xmodel/systems-collision-preflight-sol56-20260902.md b/xmodel/systems-collision-preflight-sol56-20260902.md
new file mode 100644
index 0000000000000000000000000000000000000000..17487f1ddfde01bdc85807e0e118d2896f8ca500
--- /dev/null
+++ b/xmodel/systems-collision-preflight-sol56-20260902.md
@@ -0,0 +1,276 @@
+# OPEN-collision hook and realization PREFLIGHT hard gate
+
+**Lane:** `systems-collision-preflight-sol56-20260902`  
+**Status:** IMPLEMENTED / FOCUSED TESTS PASS  
+**Scope:** implementation and regression controls only; no mathematical claim,
+promotion, canonical-ledger edit, or solver verdict.
+
+## 0. Frozen-input gate and write boundary
+
+Before reading the charged designs, SHA-256 was recomputed over the three
+frozen copies.  All values matched the charge exactly:
+
+```text
+e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  inputs/ideation-20260902T0741Z-opus5.md
+717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  inputs/ideation-20260902T0741Z-sol56.md
+4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  inputs/ideation-20260902T0741Z-synthesis.md
+```
+
+Here `inputs/` abbreviates the frozen lane directory
+`/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/`.
+The implementation follows the collision card at frozen Opus lines 519--535,
+the hard-gate specification at frozen Sol lines 408--439, and the corrected
+incidence/control specification in
+`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511`.
+
+Writes are bounded to the four requested implementation/test files and this
+report.  `ops/lane.sh`, `ops/seal.py`, `ops/artifact_finalize.py`, canonical
+ledgers, and other running-lane artifacts were not modified.  The excluded
+tree was not inspected.
+
+## 1. Typed deliverable A -- OPEN/banked collision check
+
+```text
+deliverable       = OPEN_BANKED_COLLISION_CHECK
+implementation    = ops/open_collision.py
+focused_tests     = ops/test_open_collision.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_open_collision.py
+pass_fail_skip     = 11 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 11 passed / 0 failed under python3 -O
+runtime            = 0.47 s focused; every individual test < 10 s
+integration_note   = Section 1.4
+```
+
+### 1.1 Input and extraction contract
+
+`open_collision.py` is pure Python and imports only the standard library.  It
+takes an unsealed report and repository root.  It recognizes explicit,
+anchored raised/opened headings, list entries, bold entries, heading entries,
+and the narrow inline form `THIS REPORT RAISES: OPEN[...]`.  Multiple tokens on
+one declared line are all retained.  Prose that merely discusses previously
+raised items does not activate extraction.  Broken bracket syntax inside an
+active section is an error, not an empty result.
+
+Each extracted token must carry an explicit bounded-quantity description.  A
+word such as `BOUND` inside the token identifier is removed before quantity
+parsing and cannot masquerade as that description.  This deliberate
+fail-closed requirement prevents a vague identifier from expanding into a
+hundreds-of-kilobytes seal artifact.  A producer with an unsearchable question
+must first state the quantity or refine its schema; the hook does not invent
+one.
+
+The corpus is exactly the charged set: nonrecursive `xmodel/*.md`, plus
+top-level `AUDIT.md`, `notes.md`, and `APPROACHES.md`.  The report being sealed
+is excluded by resolved identity.  A missing required file, symlinked corpus
+entry, unreadable/non-UTF-8 file, escaping path, invalid context window, or
+candidate fan-out above the declared cap returns exit 2 with an explicit
+`status: ERROR` block.
+
+### 1.2 Candidate semantics and output
+
+The search is lexical and candidate-only.  It requires the identifier's key
+nouns in a local context window and bounded-quantity terms on the
+relation-bearing line.  It recognizes ASCII, Unicode, and common LaTeX
+inequalities.  A relation line is still eligible when it carries an older
+`OPEN[...]` label; that label is useful provenance, not evidence against a
+collision.  Results are sorted deterministically and rendered as portable
+repository-relative `file:line` locations.  A candidate does not close,
+answer, or promote anything.
+
+Every successful invocation emits exactly one Markdown section headed
+`## COLLISIONS`.  With candidates it reports `status: CANDIDATES`; with no
+candidate or no declared raised item it reports `status: EMPTY` and an
+explicit `NONE` row.  The section is therefore never silently absent.  Errors
+also use the same heading with `status: ERROR`, while the nonzero exit blocks a
+seal wrapper.
+
+### 1.3 Required controls
+
+The retroactive fixture preserves the relevant source text at the exact
+1-based positions from
+`xmodel/companion-curve-alln-opus5-20260902.md`.  Feeding the bounded quantity
+for `OPEN[DEG-AF-VS-N]` produces, in order:
+
+```text
+xmodel/companion-curve-alln-opus5-20260902.md:685
+xmodel/companion-curve-alln-opus5-20260902.md:785
+xmodel/companion-curve-alln-opus5-20260902.md:836
+```
+
+The negative fixture declares a synthetic quantity with no banked match and
+asserts an explicit `status: EMPTY` block and per-token `NONE`.  Additional
+regressions cover same-line multiple tokens, malformed syntax, missing corpus
+files, prose false activation, identifier/quantity confusion, bounded fan-out,
+older labels on candidate lines, bold/heading entries, and LaTeX inequalities.
+
+### 1.4 Seal-time integration note
+
+Immediately before the standalone `BODY-END` marker is written, a future
+`lane.sh` seal phase (or a pre-step called by `seal.py`) would run
+`python3 ops/open_collision.py "$report" --root "$repo"`; any nonzero status
+aborts sealing, while exit 0 supplies the report's single `## COLLISIONS`
+block, which is inserted before `BODY-END` and only then hashed and stamped.
+The wrapper must reject a second block rather than append twice.  This lane
+describes that call boundary only and makes no change to either live tool.
+
+## 2. Typed deliverable B -- encoding-faithfulness PREFLIGHT
+
+```text
+deliverable       = N_GE_6_REALIZATION_PREFLIGHT
+implementation    = box/preflight.py
+focused_tests     = ops/test_preflight.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_preflight.py
+pass_fail_skip     = 16 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 16 passed / 0 failed under python3 -O
+runtime            = 2.14 s focused; every individual test < 10 s
+builtin_canaries   = 3 passed / 0 failed
+integration_note   = Section 2.6
+```
+
+### 2.1 Strict generator/solver contract
+
+The gate consumes a strict JSON manifest and the exact `.ms` file intended for
+launch:
+
+```text
+python3 box/preflight.py job.preflight.json job.ms
+```
+
+Unknown, missing, extra, duplicate, or wrongly typed JSON fields fail.  The
+manifest declares `N`, corrected cell, coefficient field, monomial order,
+ordered ring generators, explicit source order and generator images, the
+entire reduced approximate root, ordered ideal rows, the open factor and
+inverse, source-fidelity data, two-field output type, transition evidence, the
+pinned stack, and `solver_input_sha256`.  `N < 6`, any unknown cell family, a
+non-`QQ` field, or a non-`GRevLex` order is denied.  The bounded native cell set
+is `86A`, `86B`, `86C`, `86D`, `96A`, and `96B`; a new family requires an
+explicit schema extension and otherwise fails closed.
+
+The second CLI path is mandatory.  Its bytes must match the manifest SHA-256,
+be NUL-free UTF-8 POSIX text, and end in LF.  The gate parses the actual
+variable line, characteristic-zero line, comma framing, generator count, every
+ordered closed row, and the final Rabinowitsch row, then compares the parsed
+polynomials to the checked manifest.  Thus a correct sidecar cannot approve a
+stale or different ideal, even if that stale file's digest is copied into a
+new manifest.
+
+### 2.2 Exact root, coefficient, ring-map, and open checks
+
+The corrected expansions are recomputed exactly over `Fraction` arithmetic by
+reusing `box/qq_oracle_jobs.py`: the audit's `G86` with the target-dependent
+auxiliary terms and `G96` with `a15*p*q + a12*q^2 + a9*p + a6*q`.  The emitted
+root must contain exactly the recomputed nonzero coefficient sequence in
+descending degree.  Every encoded closed condition is parsed as a polynomial
+and exact-diffed against both that emitted coefficient and the independently
+recomputed coefficient; rendered-string equality is not trusted.
+
+The declared source ring order is fixed for the chosen cell.  Map images are
+parsed in the target ring and must form an explicit permutation isomorphism of
+the declared generators.  The manifest and actual solver variable orders must
+agree.  Exactly one open is permitted: the corrected `g_c` factor must be
+nonzero, its inverse must be the mapped `u`, and the actual final solver row
+must be polynomially equal to `u*g_c-1`.  No `sat()` wrapper is used.
+
+### 2.3 Fixed semantic and source-fidelity canaries
+
+The built-in controls are computations, not self-reported booleans:
+
+1. The charged old-A point is first checked to satisfy the old 86A closed rows
+   and old open.  Corrected top cancellation then forces the degree-19
+   residual `g19=11`, so corrected 86A rejects it.
+2. For `q=t^6+8*t^2`, `p=t^9+12*t^5+24*t`, and
+   `(a15,a12,a9,a6)=(0,0,0,-64)`, corrected 96B recomputes every closed
+   coefficient `g16,...,g3` as zero and the open coefficient `g2=64`.
+3. Source fidelity uses the canonical nonvacuous tuple
+   `(oldEQ2,q,E1)` and exact identity `EQ2_even=oldEQ2-2*q*E1`.
+   Renamed/zero aliases and the stale expression omitting `-2*q*E1` fail.
+
+These are encoding regression controls only.  Their execution here makes no
+new statement about curves, maps, attainment, or emptiness.
+
+### 2.4 Orthogonal output typing
+
+The accepted artifact field is exactly:
+
+```text
+NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT | FULL_EO_POINT |
+CURVE | REPRESENTATION | FULL_COVER | POLYNOMIAL_PAIR
+```
+
+The accepted attainment field is independently:
+
+```text
+NECESSARY | REALIZED | ADMISSIBLE_PULLBACK | ACTUAL_MAP |
+COUNTEREXAMPLE_CERTIFIED
+```
+
+Impossible pairings, unknown values, and advanced outputs without a checked
+provenance path are rejected.  The fields are emitted separately in the JSON
+result; no lossy single verdict is synthesized.
+
+### 2.5 Checked transition ladder
+
+Advanced A2 outputs must follow the complete, contiguous global path below;
+self-loops, late starts, unknown domains/scopes/claims, missing arrows, extra
+arrows, and noncontiguous states all fail.
+
+| From | To | Exact required arrows |
+|---|---|---|
+| `SUBSYSTEM_POINT/REALIZED` | `FULL_EO_POINT/REALIZED` | `FULL_EO_EQUATIONS`, `DECLARED_QUOTIENT` |
+| `FULL_EO_POINT/REALIZED` | `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `EO_ALL_OPENS`, `BOUNDARY_DATA`, `GENERAL_FIBRE_DATA`, `FIELD_DEGREE_FOUR` |
+| `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `POLYNOMIAL_PAIR/ACTUAL_MAP` | `SOURCE_CHART` |
+| `POLYNOMIAL_PAIR/ACTUAL_MAP` | `POLYNOMIAL_PAIR/COUNTEREXAMPLE_CERTIFIED` | `NONINVERTIBILITY`, `GEOMETRIC_DEGREE` |
+
+The table explicitly rejects an A2-to-B3 kill, local-to-global torsion, and a
+full-E/O point promoted to an actual map without the complete intermediate
+state and every declared arrow.
+
+### 2.6 Launch integration note
+
+A realization generator writes the strict manifest and `.ms` payload from the
+same ordered coefficient data, records the payload SHA-256 in the manifest,
+and the launch wrapper invokes `python3 box/preflight.py manifest.json job.ms`
+before starting qqideal/msolve; only exit 0 permits launch, the emitted JSON is
+retained as custody evidence, and the wrapper launches those same hash-pinned
+bytes (or rechecks the digest immediately before exec).  This lane implements
+the gate but does not alter any currently running launcher.
+
+## 3. Verification ledger
+
+Final local commands and results:
+
+```text
+python3 -m py_compile ops/open_collision.py ops/test_open_collision.py \
+    box/preflight.py ops/test_preflight.py
+PASS
+
+python3 -m unittest -v ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.589s -- OK
+
+python3 -O -m unittest -q ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.605s -- OK
+
+python3 box/preflight.py --self-test
+3/3 exact canaries PASS
+```
+
+All six reference manifests (`86A/B/C/D`, `96A/B`) also pass with their
+canonical in-memory solver payloads and an injected matching stack observation.
+No unit test invokes `msolve` or imports `qqideal`/`msolveio`.  The local live
+stack probe found `msolve=0.10.1`, while `qqideal` and `msolveio` are absent;
+therefore a real launch on this host is correctly refused until the pinned
+`qqideal=0.2.0` and `msolveio=0.2.1` packages are present.
+
+## 4. Guardrail and disposition
+
+This is an implementation lane.  Collision hits are candidates for human
+review, never closures.  Solver preflight success means only that the encoded
+job, controls, stack, solver bytes, and declared type transitions satisfy this
+contract; it is not a solver result or a mathematical realization.  The
+variable/ring map is explicit, the open is Rabinowitsch, raw remainder slots
+are exact-diffed in the declared ring/order, and attainment cannot be inferred
+from a weaker carrier.  No exit-price assertion is made, so no `charge_basis`
+line is emitted.
+

exec
/bin/zsh -lc 'wc -c -l xmodel/systems-collision-preflight-sol56-20260902.md' in /Users/dc/code/math/jc2
 succeeded in 0ms:
     276   13030 xmodel/systems-collision-preflight-sol56-20260902.md

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()
diff --git a/xmodel/systems-collision-preflight-sol56-20260902.md b/xmodel/systems-collision-preflight-sol56-20260902.md
new file mode 100644
index 0000000000000000000000000000000000000000..17487f1ddfde01bdc85807e0e118d2896f8ca500
--- /dev/null
+++ b/xmodel/systems-collision-preflight-sol56-20260902.md
@@ -0,0 +1,276 @@
+# OPEN-collision hook and realization PREFLIGHT hard gate
+
+**Lane:** `systems-collision-preflight-sol56-20260902`  
+**Status:** IMPLEMENTED / FOCUSED TESTS PASS  
+**Scope:** implementation and regression controls only; no mathematical claim,
+promotion, canonical-ledger edit, or solver verdict.
+
+## 0. Frozen-input gate and write boundary
+
+Before reading the charged designs, SHA-256 was recomputed over the three
+frozen copies.  All values matched the charge exactly:
+
+```text
+e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  inputs/ideation-20260902T0741Z-opus5.md
+717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  inputs/ideation-20260902T0741Z-sol56.md
+4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  inputs/ideation-20260902T0741Z-synthesis.md
+```
+
+Here `inputs/` abbreviates the frozen lane directory
+`/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/`.
+The implementation follows the collision card at frozen Opus lines 519--535,
+the hard-gate specification at frozen Sol lines 408--439, and the corrected
+incidence/control specification in
+`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511`.
+
+Writes are bounded to the four requested implementation/test files and this
+report.  `ops/lane.sh`, `ops/seal.py`, `ops/artifact_finalize.py`, canonical
+ledgers, and other running-lane artifacts were not modified.  The excluded
+tree was not inspected.
+
+## 1. Typed deliverable A -- OPEN/banked collision check
+
+```text
+deliverable       = OPEN_BANKED_COLLISION_CHECK
+implementation    = ops/open_collision.py
+focused_tests     = ops/test_open_collision.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_open_collision.py
+pass_fail_skip     = 11 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 11 passed / 0 failed under python3 -O
+runtime            = 0.47 s focused; every individual test < 10 s
+integration_note   = Section 1.4
+```
+
+### 1.1 Input and extraction contract
+
+`open_collision.py` is pure Python and imports only the standard library.  It
+takes an unsealed report and repository root.  It recognizes explicit,
+anchored raised/opened headings, list entries, bold entries, heading entries,
+and the narrow inline form `THIS REPORT RAISES: OPEN[...]`.  Multiple tokens on
+one declared line are all retained.  Prose that merely discusses previously
+raised items does not activate extraction.  Broken bracket syntax inside an
+active section is an error, not an empty result.
+
+Each extracted token must carry an explicit bounded-quantity description.  A
+word such as `BOUND` inside the token identifier is removed before quantity
+parsing and cannot masquerade as that description.  This deliberate
+fail-closed requirement prevents a vague identifier from expanding into a
+hundreds-of-kilobytes seal artifact.  A producer with an unsearchable question
+must first state the quantity or refine its schema; the hook does not invent
+one.
+
+The corpus is exactly the charged set: nonrecursive `xmodel/*.md`, plus
+top-level `AUDIT.md`, `notes.md`, and `APPROACHES.md`.  The report being sealed
+is excluded by resolved identity.  A missing required file, symlinked corpus
+entry, unreadable/non-UTF-8 file, escaping path, invalid context window, or
+candidate fan-out above the declared cap returns exit 2 with an explicit
+`status: ERROR` block.
+
+### 1.2 Candidate semantics and output
+
+The search is lexical and candidate-only.  It requires the identifier's key
+nouns in a local context window and bounded-quantity terms on the
+relation-bearing line.  It recognizes ASCII, Unicode, and common LaTeX
+inequalities.  A relation line is still eligible when it carries an older
+`OPEN[...]` label; that label is useful provenance, not evidence against a
+collision.  Results are sorted deterministically and rendered as portable
+repository-relative `file:line` locations.  A candidate does not close,
+answer, or promote anything.
+
+Every successful invocation emits exactly one Markdown section headed
+`## COLLISIONS`.  With candidates it reports `status: CANDIDATES`; with no
+candidate or no declared raised item it reports `status: EMPTY` and an
+explicit `NONE` row.  The section is therefore never silently absent.  Errors
+also use the same heading with `status: ERROR`, while the nonzero exit blocks a
+seal wrapper.
+
+### 1.3 Required controls
+
+The retroactive fixture preserves the relevant source text at the exact
+1-based positions from
+`xmodel/companion-curve-alln-opus5-20260902.md`.  Feeding the bounded quantity
+for `OPEN[DEG-AF-VS-N]` produces, in order:
+
+```text
+xmodel/companion-curve-alln-opus5-20260902.md:685
+xmodel/companion-curve-alln-opus5-20260902.md:785
+xmodel/companion-curve-alln-opus5-20260902.md:836
+```
+
+The negative fixture declares a synthetic quantity with no banked match and
+asserts an explicit `status: EMPTY` block and per-token `NONE`.  Additional
+regressions cover same-line multiple tokens, malformed syntax, missing corpus
+files, prose false activation, identifier/quantity confusion, bounded fan-out,
+older labels on candidate lines, bold/heading entries, and LaTeX inequalities.
+
+### 1.4 Seal-time integration note
+
+Immediately before the standalone `BODY-END` marker is written, a future
+`lane.sh` seal phase (or a pre-step called by `seal.py`) would run
+`python3 ops/open_collision.py "$report" --root "$repo"`; any nonzero status
+aborts sealing, while exit 0 supplies the report's single `## COLLISIONS`
+block, which is inserted before `BODY-END` and only then hashed and stamped.
+The wrapper must reject a second block rather than append twice.  This lane
+describes that call boundary only and makes no change to either live tool.
+
+## 2. Typed deliverable B -- encoding-faithfulness PREFLIGHT
+
+```text
+deliverable       = N_GE_6_REALIZATION_PREFLIGHT
+implementation    = box/preflight.py
+focused_tests     = ops/test_preflight.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_preflight.py
+pass_fail_skip     = 16 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 16 passed / 0 failed under python3 -O
+runtime            = 2.14 s focused; every individual test < 10 s
+builtin_canaries   = 3 passed / 0 failed
+integration_note   = Section 2.6
+```
+
+### 2.1 Strict generator/solver contract
+
+The gate consumes a strict JSON manifest and the exact `.ms` file intended for
+launch:
+
+```text
+python3 box/preflight.py job.preflight.json job.ms
+```
+
+Unknown, missing, extra, duplicate, or wrongly typed JSON fields fail.  The
+manifest declares `N`, corrected cell, coefficient field, monomial order,
+ordered ring generators, explicit source order and generator images, the
+entire reduced approximate root, ordered ideal rows, the open factor and
+inverse, source-fidelity data, two-field output type, transition evidence, the
+pinned stack, and `solver_input_sha256`.  `N < 6`, any unknown cell family, a
+non-`QQ` field, or a non-`GRevLex` order is denied.  The bounded native cell set
+is `86A`, `86B`, `86C`, `86D`, `96A`, and `96B`; a new family requires an
+explicit schema extension and otherwise fails closed.
+
+The second CLI path is mandatory.  Its bytes must match the manifest SHA-256,
+be NUL-free UTF-8 POSIX text, and end in LF.  The gate parses the actual
+variable line, characteristic-zero line, comma framing, generator count, every
+ordered closed row, and the final Rabinowitsch row, then compares the parsed
+polynomials to the checked manifest.  Thus a correct sidecar cannot approve a
+stale or different ideal, even if that stale file's digest is copied into a
+new manifest.
+
+### 2.2 Exact root, coefficient, ring-map, and open checks
+
+The corrected expansions are recomputed exactly over `Fraction` arithmetic by
+reusing `box/qq_oracle_jobs.py`: the audit's `G86` with the target-dependent
+auxiliary terms and `G96` with `a15*p*q + a12*q^2 + a9*p + a6*q`.  The emitted
+root must contain exactly the recomputed nonzero coefficient sequence in
+descending degree.  Every encoded closed condition is parsed as a polynomial
+and exact-diffed against both that emitted coefficient and the independently
+recomputed coefficient; rendered-string equality is not trusted.
+
+The declared source ring order is fixed for the chosen cell.  Map images are
+parsed in the target ring and must form an explicit permutation isomorphism of
+the declared generators.  The manifest and actual solver variable orders must
+agree.  Exactly one open is permitted: the corrected `g_c` factor must be
+nonzero, its inverse must be the mapped `u`, and the actual final solver row
+must be polynomially equal to `u*g_c-1`.  No `sat()` wrapper is used.
+
+### 2.3 Fixed semantic and source-fidelity canaries
+
+The built-in controls are computations, not self-reported booleans:
+
+1. The charged old-A point is first checked to satisfy the old 86A closed rows
+   and old open.  Corrected top cancellation then forces the degree-19
+   residual `g19=11`, so corrected 86A rejects it.
+2. For `q=t^6+8*t^2`, `p=t^9+12*t^5+24*t`, and
+   `(a15,a12,a9,a6)=(0,0,0,-64)`, corrected 96B recomputes every closed
+   coefficient `g16,...,g3` as zero and the open coefficient `g2=64`.
+3. Source fidelity uses the canonical nonvacuous tuple
+   `(oldEQ2,q,E1)` and exact identity `EQ2_even=oldEQ2-2*q*E1`.
+   Renamed/zero aliases and the stale expression omitting `-2*q*E1` fail.
+
+These are encoding regression controls only.  Their execution here makes no
+new statement about curves, maps, attainment, or emptiness.
+
+### 2.4 Orthogonal output typing
+
+The accepted artifact field is exactly:
+
+```text
+NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT | FULL_EO_POINT |
+CURVE | REPRESENTATION | FULL_COVER | POLYNOMIAL_PAIR
+```
+
+The accepted attainment field is independently:
+
+```text
+NECESSARY | REALIZED | ADMISSIBLE_PULLBACK | ACTUAL_MAP |
+COUNTEREXAMPLE_CERTIFIED
+```
+
+Impossible pairings, unknown values, and advanced outputs without a checked
+provenance path are rejected.  The fields are emitted separately in the JSON
+result; no lossy single verdict is synthesized.
+
+### 2.5 Checked transition ladder
+
+Advanced A2 outputs must follow the complete, contiguous global path below;
+self-loops, late starts, unknown domains/scopes/claims, missing arrows, extra
+arrows, and noncontiguous states all fail.
+
+| From | To | Exact required arrows |
+|---|---|---|
+| `SUBSYSTEM_POINT/REALIZED` | `FULL_EO_POINT/REALIZED` | `FULL_EO_EQUATIONS`, `DECLARED_QUOTIENT` |
+| `FULL_EO_POINT/REALIZED` | `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `EO_ALL_OPENS`, `BOUNDARY_DATA`, `GENERAL_FIBRE_DATA`, `FIELD_DEGREE_FOUR` |
+| `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `POLYNOMIAL_PAIR/ACTUAL_MAP` | `SOURCE_CHART` |
+| `POLYNOMIAL_PAIR/ACTUAL_MAP` | `POLYNOMIAL_PAIR/COUNTEREXAMPLE_CERTIFIED` | `NONINVERTIBILITY`, `GEOMETRIC_DEGREE` |
+
+The table explicitly rejects an A2-to-B3 kill, local-to-global torsion, and a
+full-E/O point promoted to an actual map without the complete intermediate
+state and every declared arrow.
+
+### 2.6 Launch integration note
+
+A realization generator writes the strict manifest and `.ms` payload from the
+same ordered coefficient data, records the payload SHA-256 in the manifest,
+and the launch wrapper invokes `python3 box/preflight.py manifest.json job.ms`
+before starting qqideal/msolve; only exit 0 permits launch, the emitted JSON is
+retained as custody evidence, and the wrapper launches those same hash-pinned
+bytes (or rechecks the digest immediately before exec).  This lane implements
+the gate but does not alter any currently running launcher.
+
+## 3. Verification ledger
+
+Final local commands and results:
+
+```text
+python3 -m py_compile ops/open_collision.py ops/test_open_collision.py \
+    box/preflight.py ops/test_preflight.py
+PASS
+
+python3 -m unittest -v ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.589s -- OK
+
+python3 -O -m unittest -q ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.605s -- OK
+
+python3 box/preflight.py --self-test
+3/3 exact canaries PASS
+```
+
+All six reference manifests (`86A/B/C/D`, `96A/B`) also pass with their
+canonical in-memory solver payloads and an injected matching stack observation.
+No unit test invokes `msolve` or imports `qqideal`/`msolveio`.  The local live
+stack probe found `msolve=0.10.1`, while `qqideal` and `msolveio` are absent;
+therefore a real launch on this host is correctly refused until the pinned
+`qqideal=0.2.0` and `msolveio=0.2.1` packages are present.
+
+## 4. Guardrail and disposition
+
+This is an implementation lane.  Collision hits are candidates for human
+review, never closures.  Solver preflight success means only that the encoded
+job, controls, stack, solver bytes, and declared type transitions satisfy this
+contract; it is not a solver result or a mathematical realization.  The
+variable/ring map is explicit, the open is Rabinowitsch, raw remainder slots
+are exact-diffed in the declared ring/order, and attainment cannot be inferred
+from a weaker carrier.  No exit-price assertion is made, so no `charge_basis`
+line is emitted.
+

exec
/bin/zsh -lc 'python3 ops/open_collision.py xmodel/systems-collision-preflight-sol56-20260902.md --root /Users/dc/code/math/jc2' in /Users/dc/code/math/jc2
 succeeded in 6640ms:
## COLLISIONS

status: EMPTY

- NONE — the report contains no explicitly raised `OPEN[...]` entries.

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()
diff --git a/xmodel/systems-collision-preflight-sol56-20260902.md b/xmodel/systems-collision-preflight-sol56-20260902.md
new file mode 100644
index 0000000000000000000000000000000000000000..17487f1ddfde01bdc85807e0e118d2896f8ca500
--- /dev/null
+++ b/xmodel/systems-collision-preflight-sol56-20260902.md
@@ -0,0 +1,276 @@
+# OPEN-collision hook and realization PREFLIGHT hard gate
+
+**Lane:** `systems-collision-preflight-sol56-20260902`  
+**Status:** IMPLEMENTED / FOCUSED TESTS PASS  
+**Scope:** implementation and regression controls only; no mathematical claim,
+promotion, canonical-ledger edit, or solver verdict.
+
+## 0. Frozen-input gate and write boundary
+
+Before reading the charged designs, SHA-256 was recomputed over the three
+frozen copies.  All values matched the charge exactly:
+
+```text
+e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  inputs/ideation-20260902T0741Z-opus5.md
+717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  inputs/ideation-20260902T0741Z-sol56.md
+4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  inputs/ideation-20260902T0741Z-synthesis.md
+```
+
+Here `inputs/` abbreviates the frozen lane directory
+`/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/`.
+The implementation follows the collision card at frozen Opus lines 519--535,
+the hard-gate specification at frozen Sol lines 408--439, and the corrected
+incidence/control specification in
+`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511`.
+
+Writes are bounded to the four requested implementation/test files and this
+report.  `ops/lane.sh`, `ops/seal.py`, `ops/artifact_finalize.py`, canonical
+ledgers, and other running-lane artifacts were not modified.  The excluded
+tree was not inspected.
+
+## 1. Typed deliverable A -- OPEN/banked collision check
+
+```text
+deliverable       = OPEN_BANKED_COLLISION_CHECK
+implementation    = ops/open_collision.py
+focused_tests     = ops/test_open_collision.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_open_collision.py
+pass_fail_skip     = 11 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 11 passed / 0 failed under python3 -O
+runtime            = 0.47 s focused; every individual test < 10 s
+integration_note   = Section 1.4
+```
+
+### 1.1 Input and extraction contract
+
+`open_collision.py` is pure Python and imports only the standard library.  It
+takes an unsealed report and repository root.  It recognizes explicit,
+anchored raised/opened headings, list entries, bold entries, heading entries,
+and the narrow inline form `THIS REPORT RAISES: OPEN[...]`.  Multiple tokens on
+one declared line are all retained.  Prose that merely discusses previously
+raised items does not activate extraction.  Broken bracket syntax inside an
+active section is an error, not an empty result.
+
+Each extracted token must carry an explicit bounded-quantity description.  A
+word such as `BOUND` inside the token identifier is removed before quantity
+parsing and cannot masquerade as that description.  This deliberate
+fail-closed requirement prevents a vague identifier from expanding into a
+hundreds-of-kilobytes seal artifact.  A producer with an unsearchable question
+must first state the quantity or refine its schema; the hook does not invent
+one.
+
+The corpus is exactly the charged set: nonrecursive `xmodel/*.md`, plus
+top-level `AUDIT.md`, `notes.md`, and `APPROACHES.md`.  The report being sealed
+is excluded by resolved identity.  A missing required file, symlinked corpus
+entry, unreadable/non-UTF-8 file, escaping path, invalid context window, or
+candidate fan-out above the declared cap returns exit 2 with an explicit
+`status: ERROR` block.
+
+### 1.2 Candidate semantics and output
+
+The search is lexical and candidate-only.  It requires the identifier's key
+nouns in a local context window and bounded-quantity terms on the
+relation-bearing line.  It recognizes ASCII, Unicode, and common LaTeX
+inequalities.  A relation line is still eligible when it carries an older
+`OPEN[...]` label; that label is useful provenance, not evidence against a
+collision.  Results are sorted deterministically and rendered as portable
+repository-relative `file:line` locations.  A candidate does not close,
+answer, or promote anything.
+
+Every successful invocation emits exactly one Markdown section headed
+`## COLLISIONS`.  With candidates it reports `status: CANDIDATES`; with no
+candidate or no declared raised item it reports `status: EMPTY` and an
+explicit `NONE` row.  The section is therefore never silently absent.  Errors
+also use the same heading with `status: ERROR`, while the nonzero exit blocks a
+seal wrapper.
+
+### 1.3 Required controls
+
+The retroactive fixture preserves the relevant source text at the exact
+1-based positions from
+`xmodel/companion-curve-alln-opus5-20260902.md`.  Feeding the bounded quantity
+for `OPEN[DEG-AF-VS-N]` produces, in order:
+
+```text
+xmodel/companion-curve-alln-opus5-20260902.md:685
+xmodel/companion-curve-alln-opus5-20260902.md:785
+xmodel/companion-curve-alln-opus5-20260902.md:836
+```
+
+The negative fixture declares a synthetic quantity with no banked match and
+asserts an explicit `status: EMPTY` block and per-token `NONE`.  Additional
+regressions cover same-line multiple tokens, malformed syntax, missing corpus
+files, prose false activation, identifier/quantity confusion, bounded fan-out,
+older labels on candidate lines, bold/heading entries, and LaTeX inequalities.
+
+### 1.4 Seal-time integration note
+
+Immediately before the standalone `BODY-END` marker is written, a future
+`lane.sh` seal phase (or a pre-step called by `seal.py`) would run
+`python3 ops/open_collision.py "$report" --root "$repo"`; any nonzero status
+aborts sealing, while exit 0 supplies the report's single `## COLLISIONS`
+block, which is inserted before `BODY-END` and only then hashed and stamped.
+The wrapper must reject a second block rather than append twice.  This lane
+describes that call boundary only and makes no change to either live tool.
+
+## 2. Typed deliverable B -- encoding-faithfulness PREFLIGHT
+
+```text
+deliverable       = N_GE_6_REALIZATION_PREFLIGHT
+implementation    = box/preflight.py
+focused_tests     = ops/test_preflight.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_preflight.py
+pass_fail_skip     = 16 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 16 passed / 0 failed under python3 -O
+runtime            = 2.14 s focused; every individual test < 10 s
+builtin_canaries   = 3 passed / 0 failed
+integration_note   = Section 2.6
+```
+
+### 2.1 Strict generator/solver contract
+
+The gate consumes a strict JSON manifest and the exact `.ms` file intended for
+launch:
+
+```text
+python3 box/preflight.py job.preflight.json job.ms
+```
+
+Unknown, missing, extra, duplicate, or wrongly typed JSON fields fail.  The
+manifest declares `N`, corrected cell, coefficient field, monomial order,
+ordered ring generators, explicit source order and generator images, the
+entire reduced approximate root, ordered ideal rows, the open factor and
+inverse, source-fidelity data, two-field output type, transition evidence, the
+pinned stack, and `solver_input_sha256`.  `N < 6`, any unknown cell family, a
+non-`QQ` field, or a non-`GRevLex` order is denied.  The bounded native cell set
+is `86A`, `86B`, `86C`, `86D`, `96A`, and `96B`; a new family requires an
+explicit schema extension and otherwise fails closed.
+
+The second CLI path is mandatory.  Its bytes must match the manifest SHA-256,
+be NUL-free UTF-8 POSIX text, and end in LF.  The gate parses the actual
+variable line, characteristic-zero line, comma framing, generator count, every
+ordered closed row, and the final Rabinowitsch row, then compares the parsed
+polynomials to the checked manifest.  Thus a correct sidecar cannot approve a
+stale or different ideal, even if that stale file's digest is copied into a
+new manifest.
+
+### 2.2 Exact root, coefficient, ring-map, and open checks
+
+The corrected expansions are recomputed exactly over `Fraction` arithmetic by
+reusing `box/qq_oracle_jobs.py`: the audit's `G86` with the target-dependent
+auxiliary terms and `G96` with `a15*p*q + a12*q^2 + a9*p + a6*q`.  The emitted
+root must contain exactly the recomputed nonzero coefficient sequence in
+descending degree.  Every encoded closed condition is parsed as a polynomial
+and exact-diffed against both that emitted coefficient and the independently
+recomputed coefficient; rendered-string equality is not trusted.
+
+The declared source ring order is fixed for the chosen cell.  Map images are
+parsed in the target ring and must form an explicit permutation isomorphism of
+the declared generators.  The manifest and actual solver variable orders must
+agree.  Exactly one open is permitted: the corrected `g_c` factor must be
+nonzero, its inverse must be the mapped `u`, and the actual final solver row
+must be polynomially equal to `u*g_c-1`.  No `sat()` wrapper is used.
+
+### 2.3 Fixed semantic and source-fidelity canaries
+
+The built-in controls are computations, not self-reported booleans:
+
+1. The charged old-A point is first checked to satisfy the old 86A closed rows
+   and old open.  Corrected top cancellation then forces the degree-19
+   residual `g19=11`, so corrected 86A rejects it.
+2. For `q=t^6+8*t^2`, `p=t^9+12*t^5+24*t`, and
+   `(a15,a12,a9,a6)=(0,0,0,-64)`, corrected 96B recomputes every closed
+   coefficient `g16,...,g3` as zero and the open coefficient `g2=64`.
+3. Source fidelity uses the canonical nonvacuous tuple
+   `(oldEQ2,q,E1)` and exact identity `EQ2_even=oldEQ2-2*q*E1`.
+   Renamed/zero aliases and the stale expression omitting `-2*q*E1` fail.
+
+These are encoding regression controls only.  Their execution here makes no
+new statement about curves, maps, attainment, or emptiness.
+
+### 2.4 Orthogonal output typing
+
+The accepted artifact field is exactly:
+
+```text
+NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT | FULL_EO_POINT |
+CURVE | REPRESENTATION | FULL_COVER | POLYNOMIAL_PAIR
+```
+
+The accepted attainment field is independently:
+
+```text
+NECESSARY | REALIZED | ADMISSIBLE_PULLBACK | ACTUAL_MAP |
+COUNTEREXAMPLE_CERTIFIED
+```
+
+Impossible pairings, unknown values, and advanced outputs without a checked
+provenance path are rejected.  The fields are emitted separately in the JSON
+result; no lossy single verdict is synthesized.
+
+### 2.5 Checked transition ladder
+
+Advanced A2 outputs must follow the complete, contiguous global path below;
+self-loops, late starts, unknown domains/scopes/claims, missing arrows, extra
+arrows, and noncontiguous states all fail.
+
+| From | To | Exact required arrows |
+|---|---|---|
+| `SUBSYSTEM_POINT/REALIZED` | `FULL_EO_POINT/REALIZED` | `FULL_EO_EQUATIONS`, `DECLARED_QUOTIENT` |
+| `FULL_EO_POINT/REALIZED` | `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `EO_ALL_OPENS`, `BOUNDARY_DATA`, `GENERAL_FIBRE_DATA`, `FIELD_DEGREE_FOUR` |
+| `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `POLYNOMIAL_PAIR/ACTUAL_MAP` | `SOURCE_CHART` |
+| `POLYNOMIAL_PAIR/ACTUAL_MAP` | `POLYNOMIAL_PAIR/COUNTEREXAMPLE_CERTIFIED` | `NONINVERTIBILITY`, `GEOMETRIC_DEGREE` |
+
+The table explicitly rejects an A2-to-B3 kill, local-to-global torsion, and a
+full-E/O point promoted to an actual map without the complete intermediate
+state and every declared arrow.
+
+### 2.6 Launch integration note
+
+A realization generator writes the strict manifest and `.ms` payload from the
+same ordered coefficient data, records the payload SHA-256 in the manifest,
+and the launch wrapper invokes `python3 box/preflight.py manifest.json job.ms`
+before starting qqideal/msolve; only exit 0 permits launch, the emitted JSON is
+retained as custody evidence, and the wrapper launches those same hash-pinned
+bytes (or rechecks the digest immediately before exec).  This lane implements
+the gate but does not alter any currently running launcher.
+
+## 3. Verification ledger
+
+Final local commands and results:
+
+```text
+python3 -m py_compile ops/open_collision.py ops/test_open_collision.py \
+    box/preflight.py ops/test_preflight.py
+PASS
+
+python3 -m unittest -v ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.589s -- OK
+
+python3 -O -m unittest -q ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.605s -- OK
+
+python3 box/preflight.py --self-test
+3/3 exact canaries PASS
+```
+
+All six reference manifests (`86A/B/C/D`, `96A/B`) also pass with their
+canonical in-memory solver payloads and an injected matching stack observation.
+No unit test invokes `msolve` or imports `qqideal`/`msolveio`.  The local live
+stack probe found `msolve=0.10.1`, while `qqideal` and `msolveio` are absent;
+therefore a real launch on this host is correctly refused until the pinned
+`qqideal=0.2.0` and `msolveio=0.2.1` packages are present.
+
+## 4. Guardrail and disposition
+
+This is an implementation lane.  Collision hits are candidates for human
+review, never closures.  Solver preflight success means only that the encoded
+job, controls, stack, solver bytes, and declared type transitions satisfy this
+contract; it is not a solver result or a mathematical realization.  The
+variable/ring map is explicit, the open is Rabinowitsch, raw remainder slots
+are exact-diffed in the declared ring/order, and attainment cannot be inferred
+from a weaker carrier.  No exit-price assertion is made, so no `charge_basis`
+line is emitted.
+

apply patch
patch: completed
/Users/dc/code/math/jc2/xmodel/systems-collision-preflight-sol56-20260902.md
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()
diff --git a/xmodel/systems-collision-preflight-sol56-20260902.md b/xmodel/systems-collision-preflight-sol56-20260902.md
new file mode 100644
index 0000000000000000000000000000000000000000..f1d8d1ea856cdb58301f3411e1088537ef610323
--- /dev/null
+++ b/xmodel/systems-collision-preflight-sol56-20260902.md
@@ -0,0 +1,283 @@
+# OPEN-collision hook and realization PREFLIGHT hard gate
+
+**Lane:** `systems-collision-preflight-sol56-20260902`  
+**Status:** IMPLEMENTED / FOCUSED TESTS PASS  
+**Scope:** implementation and regression controls only; no mathematical claim,
+promotion, canonical-ledger edit, or solver verdict.
+
+## 0. Frozen-input gate and write boundary
+
+Before reading the charged designs, SHA-256 was recomputed over the three
+frozen copies.  All values matched the charge exactly:
+
+```text
+e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  inputs/ideation-20260902T0741Z-opus5.md
+717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  inputs/ideation-20260902T0741Z-sol56.md
+4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  inputs/ideation-20260902T0741Z-synthesis.md
+```
+
+Here `inputs/` abbreviates the frozen lane directory
+`/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/`.
+The implementation follows the collision card at frozen Opus lines 519--535,
+the hard-gate specification at frozen Sol lines 408--439, and the corrected
+incidence/control specification in
+`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511`.
+
+Writes are bounded to the four requested implementation/test files and this
+report.  `ops/lane.sh`, `ops/seal.py`, `ops/artifact_finalize.py`, canonical
+ledgers, and other running-lane artifacts were not modified.  The excluded
+tree was not inspected.
+
+## 1. Typed deliverable A -- OPEN/banked collision check
+
+```text
+deliverable       = OPEN_BANKED_COLLISION_CHECK
+implementation    = ops/open_collision.py
+focused_tests     = ops/test_open_collision.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_open_collision.py
+pass_fail_skip     = 11 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 11 passed / 0 failed under python3 -O
+runtime            = 0.47 s focused; every individual test < 10 s
+integration_note   = Section 1.4
+```
+
+### 1.1 Input and extraction contract
+
+`open_collision.py` is pure Python and imports only the standard library.  It
+takes an unsealed report and repository root.  It recognizes explicit,
+anchored raised/opened headings, list entries, bold entries, heading entries,
+and the narrow inline form `THIS REPORT RAISES: OPEN[...]`.  Multiple tokens on
+one declared line are all retained.  Prose that merely discusses previously
+raised items does not activate extraction.  Broken bracket syntax inside an
+active section is an error, not an empty result.
+
+Each extracted token must carry an explicit bounded-quantity description.  A
+word such as `BOUND` inside the token identifier is removed before quantity
+parsing and cannot masquerade as that description.  This deliberate
+fail-closed requirement prevents a vague identifier from expanding into a
+hundreds-of-kilobytes seal artifact.  A producer with an unsearchable question
+must first state the quantity or refine its schema; the hook does not invent
+one.
+
+The corpus is exactly the charged set: nonrecursive `xmodel/*.md`, plus
+top-level `AUDIT.md`, `notes.md`, and `APPROACHES.md`.  The report being sealed
+is excluded by resolved identity.  A missing required file, symlinked corpus
+entry, unreadable/non-UTF-8 file, escaping path, invalid context window, or
+candidate fan-out above the declared cap returns exit 2 with an explicit
+`status: ERROR` block.
+
+### 1.2 Candidate semantics and output
+
+The search is lexical and candidate-only.  It requires the identifier's key
+nouns in a local context window and bounded-quantity terms on the
+relation-bearing line.  It recognizes ASCII, Unicode, and common LaTeX
+inequalities.  A relation line is still eligible when it carries an older
+`OPEN[...]` label; that label is useful provenance, not evidence against a
+collision.  Results are sorted deterministically and rendered as portable
+repository-relative `file:line` locations.  A candidate does not close,
+answer, or promote anything.
+
+Every successful invocation emits exactly one Markdown section headed
+`## COLLISIONS`.  With candidates it reports `status: CANDIDATES`; with no
+candidate or no declared raised item it reports `status: EMPTY` and an
+explicit `NONE` row.  The section is therefore never silently absent.  Errors
+also use the same heading with `status: ERROR`, while the nonzero exit blocks a
+seal wrapper.
+
+### 1.3 Required controls
+
+The retroactive fixture preserves the relevant source text at the exact
+1-based positions from
+`xmodel/companion-curve-alln-opus5-20260902.md`.  Feeding the bounded quantity
+for `OPEN[DEG-AF-VS-N]` produces, in order:
+
+```text
+xmodel/companion-curve-alln-opus5-20260902.md:685
+xmodel/companion-curve-alln-opus5-20260902.md:785
+xmodel/companion-curve-alln-opus5-20260902.md:836
+```
+
+The negative fixture declares a synthetic quantity with no banked match and
+asserts an explicit `status: EMPTY` block and per-token `NONE`.  Additional
+regressions cover same-line multiple tokens, malformed syntax, missing corpus
+files, prose false activation, identifier/quantity confusion, bounded fan-out,
+older labels on candidate lines, bold/heading entries, and LaTeX inequalities.
+
+### 1.4 Seal-time integration note
+
+Immediately before the standalone `BODY-END` marker is written, a future
+`lane.sh` seal phase (or a pre-step called by `seal.py`) would run
+`python3 ops/open_collision.py "$report" --root "$repo"`; any nonzero status
+aborts sealing, while exit 0 supplies the report's single `## COLLISIONS`
+block, which is inserted before `BODY-END` and only then hashed and stamped.
+The wrapper must reject a second block rather than append twice.  This lane
+describes that call boundary only and makes no change to either live tool.
+
+## 2. Typed deliverable B -- encoding-faithfulness PREFLIGHT
+
+```text
+deliverable       = N_GE_6_REALIZATION_PREFLIGHT
+implementation    = box/preflight.py
+focused_tests     = ops/test_preflight.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_preflight.py
+pass_fail_skip     = 16 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 16 passed / 0 failed under python3 -O
+runtime            = 2.14 s focused; every individual test < 10 s
+builtin_canaries   = 3 passed / 0 failed
+integration_note   = Section 2.6
+```
+
+### 2.1 Strict generator/solver contract
+
+The gate consumes a strict JSON manifest and the exact `.ms` file intended for
+launch:
+
+```text
+python3 box/preflight.py job.preflight.json job.ms
+```
+
+Unknown, missing, extra, duplicate, or wrongly typed JSON fields fail.  The
+manifest declares `N`, corrected cell, coefficient field, monomial order,
+ordered ring generators, explicit source order and generator images, the
+entire reduced approximate root, ordered ideal rows, the open factor and
+inverse, source-fidelity data, two-field output type, transition evidence, the
+pinned stack, and `solver_input_sha256`.  `N < 6`, any unknown cell family, a
+non-`QQ` field, or a non-`GRevLex` order is denied.  The bounded native cell set
+is `86A`, `86B`, `86C`, `86D`, `96A`, and `96B`; a new family requires an
+explicit schema extension and otherwise fails closed.
+
+The second CLI path is mandatory.  Its bytes must match the manifest SHA-256,
+be NUL-free UTF-8 POSIX text, and end in LF.  The gate parses the actual
+variable line, characteristic-zero line, comma framing, generator count, every
+ordered closed row, and the final Rabinowitsch row, then compares the parsed
+polynomials to the checked manifest.  Thus a correct sidecar cannot approve a
+stale or different ideal, even if that stale file's digest is copied into a
+new manifest.
+
+### 2.2 Exact root, coefficient, ring-map, and open checks
+
+The corrected expansions are recomputed exactly over `Fraction` arithmetic by
+reusing `box/qq_oracle_jobs.py`: the audit's `G86` with the target-dependent
+auxiliary terms and `G96` with `a15*p*q + a12*q^2 + a9*p + a6*q`.  The emitted
+root must contain exactly the recomputed nonzero coefficient sequence in
+descending degree.  Every encoded closed condition is parsed as a polynomial
+and exact-diffed against both that emitted coefficient and the independently
+recomputed coefficient; rendered-string equality is not trusted.
+
+The declared source ring order is fixed for the chosen cell.  Map images are
+parsed in the target ring and must form an explicit permutation isomorphism of
+the declared generators.  The manifest and actual solver variable orders must
+agree.  Exactly one open is permitted: the corrected `g_c` factor must be
+nonzero, its inverse must be the mapped `u`, and the actual final solver row
+must be polynomially equal to `u*g_c-1`.  No `sat()` wrapper is used.
+
+### 2.3 Fixed semantic and source-fidelity canaries
+
+The built-in controls are computations, not self-reported booleans:
+
+1. The charged old-A point is first checked to satisfy the old 86A closed rows
+   and old open.  Corrected top cancellation then forces the degree-19
+   residual `g19=11`, so corrected 86A rejects it.
+2. For `q=t^6+8*t^2`, `p=t^9+12*t^5+24*t`, and
+   `(a15,a12,a9,a6)=(0,0,0,-64)`, corrected 96B recomputes every closed
+   coefficient `g16,...,g3` as zero and the open coefficient `g2=64`.
+3. Source fidelity uses the canonical nonvacuous tuple
+   `(oldEQ2,q,E1)` and exact identity `EQ2_even=oldEQ2-2*q*E1`.
+   Renamed/zero aliases and the stale expression omitting `-2*q*E1` fail.
+
+These are encoding regression controls only.  Their execution here makes no
+new statement about curves, maps, attainment, or emptiness.
+
+### 2.4 Orthogonal output typing
+
+The accepted artifact field is exactly:
+
+```text
+NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT | FULL_EO_POINT |
+CURVE | REPRESENTATION | FULL_COVER | POLYNOMIAL_PAIR
+```
+
+The accepted attainment field is independently:
+
+```text
+NECESSARY | REALIZED | ADMISSIBLE_PULLBACK | ACTUAL_MAP |
+COUNTEREXAMPLE_CERTIFIED
+```
+
+Impossible pairings, unknown values, and advanced outputs without a checked
+provenance path are rejected.  The fields are emitted separately in the JSON
+result; no lossy single verdict is synthesized.
+
+### 2.5 Checked transition ladder
+
+Advanced A2 outputs must follow the complete, contiguous global path below;
+self-loops, late starts, unknown domains/scopes/claims, missing arrows, extra
+arrows, and noncontiguous states all fail.
+
+| From | To | Exact required arrows |
+|---|---|---|
+| `SUBSYSTEM_POINT/REALIZED` | `FULL_EO_POINT/REALIZED` | `FULL_EO_EQUATIONS`, `DECLARED_QUOTIENT` |
+| `FULL_EO_POINT/REALIZED` | `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `EO_ALL_OPENS`, `BOUNDARY_DATA`, `GENERAL_FIBRE_DATA`, `FIELD_DEGREE_FOUR` |
+| `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `POLYNOMIAL_PAIR/ACTUAL_MAP` | `SOURCE_CHART` |
+| `POLYNOMIAL_PAIR/ACTUAL_MAP` | `POLYNOMIAL_PAIR/COUNTEREXAMPLE_CERTIFIED` | `NONINVERTIBILITY`, `GEOMETRIC_DEGREE` |
+
+The table explicitly rejects an A2-to-B3 kill, local-to-global torsion, and a
+full-E/O point promoted to an actual map without the complete intermediate
+state and every declared arrow.
+
+### 2.6 Launch integration note
+
+A realization generator writes the strict manifest and `.ms` payload from the
+same ordered coefficient data, records the payload SHA-256 in the manifest,
+and the launch wrapper invokes `python3 box/preflight.py manifest.json job.ms`
+before starting qqideal/msolve; only exit 0 permits launch, the emitted JSON is
+retained as custody evidence, and the wrapper launches those same hash-pinned
+bytes (or rechecks the digest immediately before exec).  This lane implements
+the gate but does not alter any currently running launcher.
+
+## 3. Verification ledger
+
+Final local commands and results:
+
+```text
+python3 -m py_compile ops/open_collision.py ops/test_open_collision.py \
+    box/preflight.py ops/test_preflight.py
+PASS
+
+python3 -m unittest -v ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.589s -- OK
+
+python3 -O -m unittest -q ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.605s -- OK
+
+python3 box/preflight.py --self-test
+3/3 exact canaries PASS
+```
+
+All six reference manifests (`86A/B/C/D`, `96A/B`) also pass with their
+canonical in-memory solver payloads and an injected matching stack observation.
+No unit test invokes `msolve` or imports `qqideal`/`msolveio`.  The local live
+stack probe found `msolve=0.10.1`, while `qqideal` and `msolveio` are absent;
+therefore a real launch on this host is correctly refused until the pinned
+`qqideal=0.2.0` and `msolveio=0.2.1` packages are present.
+
+## 4. Guardrail and disposition
+
+This is an implementation lane.  Collision hits are candidates for human
+review, never closures.  Solver preflight success means only that the encoded
+job, controls, stack, solver bytes, and declared type transitions satisfy this
+contract; it is not a solver result or a mathematical realization.  The
+variable/ring map is explicit, the open is Rabinowitsch, raw remainder slots
+are exact-diffed in the declared ring/order, and attainment cannot be inferred
+from a weaker carrier.  No exit-price assertion is made, so no `charge_basis`
+line is emitted.
+
+## COLLISIONS
+
+status: EMPTY
+
+- NONE — the report contains no explicitly raised `OPEN[...]` entries.
+
+<!-- BODY-END -->

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()
diff --git a/xmodel/systems-collision-preflight-sol56-20260902.md b/xmodel/systems-collision-preflight-sol56-20260902.md
new file mode 100644
index 0000000000000000000000000000000000000000..f1d8d1ea856cdb58301f3411e1088537ef610323
--- /dev/null
+++ b/xmodel/systems-collision-preflight-sol56-20260902.md
@@ -0,0 +1,283 @@
+# OPEN-collision hook and realization PREFLIGHT hard gate
+
+**Lane:** `systems-collision-preflight-sol56-20260902`  
+**Status:** IMPLEMENTED / FOCUSED TESTS PASS  
+**Scope:** implementation and regression controls only; no mathematical claim,
+promotion, canonical-ledger edit, or solver verdict.
+
+## 0. Frozen-input gate and write boundary
+
+Before reading the charged designs, SHA-256 was recomputed over the three
+frozen copies.  All values matched the charge exactly:
+
+```text
+e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  inputs/ideation-20260902T0741Z-opus5.md
+717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  inputs/ideation-20260902T0741Z-sol56.md
+4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  inputs/ideation-20260902T0741Z-synthesis.md
+```
+
+Here `inputs/` abbreviates the frozen lane directory
+`/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/`.
+The implementation follows the collision card at frozen Opus lines 519--535,
+the hard-gate specification at frozen Sol lines 408--439, and the corrected
+incidence/control specification in
+`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511`.
+
+Writes are bounded to the four requested implementation/test files and this
+report.  `ops/lane.sh`, `ops/seal.py`, `ops/artifact_finalize.py`, canonical
+ledgers, and other running-lane artifacts were not modified.  The excluded
+tree was not inspected.
+
+## 1. Typed deliverable A -- OPEN/banked collision check
+
+```text
+deliverable       = OPEN_BANKED_COLLISION_CHECK
+implementation    = ops/open_collision.py
+focused_tests     = ops/test_open_collision.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_open_collision.py
+pass_fail_skip     = 11 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 11 passed / 0 failed under python3 -O
+runtime            = 0.47 s focused; every individual test < 10 s
+integration_note   = Section 1.4
+```
+
+### 1.1 Input and extraction contract
+
+`open_collision.py` is pure Python and imports only the standard library.  It
+takes an unsealed report and repository root.  It recognizes explicit,
+anchored raised/opened headings, list entries, bold entries, heading entries,
+and the narrow inline form `THIS REPORT RAISES: OPEN[...]`.  Multiple tokens on
+one declared line are all retained.  Prose that merely discusses previously
+raised items does not activate extraction.  Broken bracket syntax inside an
+active section is an error, not an empty result.
+
+Each extracted token must carry an explicit bounded-quantity description.  A
+word such as `BOUND` inside the token identifier is removed before quantity
+parsing and cannot masquerade as that description.  This deliberate
+fail-closed requirement prevents a vague identifier from expanding into a
+hundreds-of-kilobytes seal artifact.  A producer with an unsearchable question
+must first state the quantity or refine its schema; the hook does not invent
+one.
+
+The corpus is exactly the charged set: nonrecursive `xmodel/*.md`, plus
+top-level `AUDIT.md`, `notes.md`, and `APPROACHES.md`.  The report being sealed
+is excluded by resolved identity.  A missing required file, symlinked corpus
+entry, unreadable/non-UTF-8 file, escaping path, invalid context window, or
+candidate fan-out above the declared cap returns exit 2 with an explicit
+`status: ERROR` block.
+
+### 1.2 Candidate semantics and output
+
+The search is lexical and candidate-only.  It requires the identifier's key
+nouns in a local context window and bounded-quantity terms on the
+relation-bearing line.  It recognizes ASCII, Unicode, and common LaTeX
+inequalities.  A relation line is still eligible when it carries an older
+`OPEN[...]` label; that label is useful provenance, not evidence against a
+collision.  Results are sorted deterministically and rendered as portable
+repository-relative `file:line` locations.  A candidate does not close,
+answer, or promote anything.
+
+Every successful invocation emits exactly one Markdown section headed
+`## COLLISIONS`.  With candidates it reports `status: CANDIDATES`; with no
+candidate or no declared raised item it reports `status: EMPTY` and an
+explicit `NONE` row.  The section is therefore never silently absent.  Errors
+also use the same heading with `status: ERROR`, while the nonzero exit blocks a
+seal wrapper.
+
+### 1.3 Required controls
+
+The retroactive fixture preserves the relevant source text at the exact
+1-based positions from
+`xmodel/companion-curve-alln-opus5-20260902.md`.  Feeding the bounded quantity
+for `OPEN[DEG-AF-VS-N]` produces, in order:
+
+```text
+xmodel/companion-curve-alln-opus5-20260902.md:685
+xmodel/companion-curve-alln-opus5-20260902.md:785
+xmodel/companion-curve-alln-opus5-20260902.md:836
+```
+
+The negative fixture declares a synthetic quantity with no banked match and
+asserts an explicit `status: EMPTY` block and per-token `NONE`.  Additional
+regressions cover same-line multiple tokens, malformed syntax, missing corpus
+files, prose false activation, identifier/quantity confusion, bounded fan-out,
+older labels on candidate lines, bold/heading entries, and LaTeX inequalities.
+
+### 1.4 Seal-time integration note
+
+Immediately before the standalone `BODY-END` marker is written, a future
+`lane.sh` seal phase (or a pre-step called by `seal.py`) would run
+`python3 ops/open_collision.py "$report" --root "$repo"`; any nonzero status
+aborts sealing, while exit 0 supplies the report's single `## COLLISIONS`
+block, which is inserted before `BODY-END` and only then hashed and stamped.
+The wrapper must reject a second block rather than append twice.  This lane
+describes that call boundary only and makes no change to either live tool.
+
+## 2. Typed deliverable B -- encoding-faithfulness PREFLIGHT
+
+```text
+deliverable       = N_GE_6_REALIZATION_PREFLIGHT
+implementation    = box/preflight.py
+focused_tests     = ops/test_preflight.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_preflight.py
+pass_fail_skip     = 16 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 16 passed / 0 failed under python3 -O
+runtime            = 2.14 s focused; every individual test < 10 s
+builtin_canaries   = 3 passed / 0 failed
+integration_note   = Section 2.6
+```
+
+### 2.1 Strict generator/solver contract
+
+The gate consumes a strict JSON manifest and the exact `.ms` file intended for
+launch:
+
+```text
+python3 box/preflight.py job.preflight.json job.ms
+```
+
+Unknown, missing, extra, duplicate, or wrongly typed JSON fields fail.  The
+manifest declares `N`, corrected cell, coefficient field, monomial order,
+ordered ring generators, explicit source order and generator images, the
+entire reduced approximate root, ordered ideal rows, the open factor and
+inverse, source-fidelity data, two-field output type, transition evidence, the
+pinned stack, and `solver_input_sha256`.  `N < 6`, any unknown cell family, a
+non-`QQ` field, or a non-`GRevLex` order is denied.  The bounded native cell set
+is `86A`, `86B`, `86C`, `86D`, `96A`, and `96B`; a new family requires an
+explicit schema extension and otherwise fails closed.
+
+The second CLI path is mandatory.  Its bytes must match the manifest SHA-256,
+be NUL-free UTF-8 POSIX text, and end in LF.  The gate parses the actual
+variable line, characteristic-zero line, comma framing, generator count, every
+ordered closed row, and the final Rabinowitsch row, then compares the parsed
+polynomials to the checked manifest.  Thus a correct sidecar cannot approve a
+stale or different ideal, even if that stale file's digest is copied into a
+new manifest.
+
+### 2.2 Exact root, coefficient, ring-map, and open checks
+
+The corrected expansions are recomputed exactly over `Fraction` arithmetic by
+reusing `box/qq_oracle_jobs.py`: the audit's `G86` with the target-dependent
+auxiliary terms and `G96` with `a15*p*q + a12*q^2 + a9*p + a6*q`.  The emitted
+root must contain exactly the recomputed nonzero coefficient sequence in
+descending degree.  Every encoded closed condition is parsed as a polynomial
+and exact-diffed against both that emitted coefficient and the independently
+recomputed coefficient; rendered-string equality is not trusted.
+
+The declared source ring order is fixed for the chosen cell.  Map images are
+parsed in the target ring and must form an explicit permutation isomorphism of
+the declared generators.  The manifest and actual solver variable orders must
+agree.  Exactly one open is permitted: the corrected `g_c` factor must be
+nonzero, its inverse must be the mapped `u`, and the actual final solver row
+must be polynomially equal to `u*g_c-1`.  No `sat()` wrapper is used.
+
+### 2.3 Fixed semantic and source-fidelity canaries
+
+The built-in controls are computations, not self-reported booleans:
+
+1. The charged old-A point is first checked to satisfy the old 86A closed rows
+   and old open.  Corrected top cancellation then forces the degree-19
+   residual `g19=11`, so corrected 86A rejects it.
+2. For `q=t^6+8*t^2`, `p=t^9+12*t^5+24*t`, and
+   `(a15,a12,a9,a6)=(0,0,0,-64)`, corrected 96B recomputes every closed
+   coefficient `g16,...,g3` as zero and the open coefficient `g2=64`.
+3. Source fidelity uses the canonical nonvacuous tuple
+   `(oldEQ2,q,E1)` and exact identity `EQ2_even=oldEQ2-2*q*E1`.
+   Renamed/zero aliases and the stale expression omitting `-2*q*E1` fail.
+
+These are encoding regression controls only.  Their execution here makes no
+new statement about curves, maps, attainment, or emptiness.
+
+### 2.4 Orthogonal output typing
+
+The accepted artifact field is exactly:
+
+```text
+NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT | FULL_EO_POINT |
+CURVE | REPRESENTATION | FULL_COVER | POLYNOMIAL_PAIR
+```
+
+The accepted attainment field is independently:
+
+```text
+NECESSARY | REALIZED | ADMISSIBLE_PULLBACK | ACTUAL_MAP |
+COUNTEREXAMPLE_CERTIFIED
+```
+
+Impossible pairings, unknown values, and advanced outputs without a checked
+provenance path are rejected.  The fields are emitted separately in the JSON
+result; no lossy single verdict is synthesized.
+
+### 2.5 Checked transition ladder
+
+Advanced A2 outputs must follow the complete, contiguous global path below;
+self-loops, late starts, unknown domains/scopes/claims, missing arrows, extra
+arrows, and noncontiguous states all fail.
+
+| From | To | Exact required arrows |
+|---|---|---|
+| `SUBSYSTEM_POINT/REALIZED` | `FULL_EO_POINT/REALIZED` | `FULL_EO_EQUATIONS`, `DECLARED_QUOTIENT` |
+| `FULL_EO_POINT/REALIZED` | `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `EO_ALL_OPENS`, `BOUNDARY_DATA`, `GENERAL_FIBRE_DATA`, `FIELD_DEGREE_FOUR` |
+| `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `POLYNOMIAL_PAIR/ACTUAL_MAP` | `SOURCE_CHART` |
+| `POLYNOMIAL_PAIR/ACTUAL_MAP` | `POLYNOMIAL_PAIR/COUNTEREXAMPLE_CERTIFIED` | `NONINVERTIBILITY`, `GEOMETRIC_DEGREE` |
+
+The table explicitly rejects an A2-to-B3 kill, local-to-global torsion, and a
+full-E/O point promoted to an actual map without the complete intermediate
+state and every declared arrow.
+
+### 2.6 Launch integration note
+
+A realization generator writes the strict manifest and `.ms` payload from the
+same ordered coefficient data, records the payload SHA-256 in the manifest,
+and the launch wrapper invokes `python3 box/preflight.py manifest.json job.ms`
+before starting qqideal/msolve; only exit 0 permits launch, the emitted JSON is
+retained as custody evidence, and the wrapper launches those same hash-pinned
+bytes (or rechecks the digest immediately before exec).  This lane implements
+the gate but does not alter any currently running launcher.
+
+## 3. Verification ledger
+
+Final local commands and results:
+
+```text
+python3 -m py_compile ops/open_collision.py ops/test_open_collision.py \
+    box/preflight.py ops/test_preflight.py
+PASS
+
+python3 -m unittest -v ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.589s -- OK
+
+python3 -O -m unittest -q ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.605s -- OK
+
+python3 box/preflight.py --self-test
+3/3 exact canaries PASS
+```
+
+All six reference manifests (`86A/B/C/D`, `96A/B`) also pass with their
+canonical in-memory solver payloads and an injected matching stack observation.
+No unit test invokes `msolve` or imports `qqideal`/`msolveio`.  The local live
+stack probe found `msolve=0.10.1`, while `qqideal` and `msolveio` are absent;
+therefore a real launch on this host is correctly refused until the pinned
+`qqideal=0.2.0` and `msolveio=0.2.1` packages are present.
+
+## 4. Guardrail and disposition
+
+This is an implementation lane.  Collision hits are candidates for human
+review, never closures.  Solver preflight success means only that the encoded
+job, controls, stack, solver bytes, and declared type transitions satisfy this
+contract; it is not a solver result or a mathematical realization.  The
+variable/ring map is explicit, the open is Rabinowitsch, raw remainder slots
+are exact-diffed in the declared ring/order, and attainment cannot be inferred
+from a weaker carrier.  No exit-price assertion is made, so no `charge_basis`
+line is emitted.
+
+## COLLISIONS
+
+status: EMPTY
+
+- NONE — the report contains no explicitly raised `OPEN[...]` entries.
+
+<!-- BODY-END -->

codex
The report is within the requested size bound and the new collision hook scanned it successfully, producing an explicit empty block. I’m stamping that exact body against the frozen basis now, then I’ll verify the seal and check that only the requested files were added.
exec
/bin/zsh -lc 'python3 ops/seal.py stamp --basis 621c9edc6bb8932e4aed6ac9805a5684a4436789 xmodel/systems-collision-preflight-sol56-20260902.md' in /Users/dc/code/math/jc2
 succeeded in 0ms:
STAMPED path=xmodel/systems-collision-preflight-sol56-20260902.md body_bytes=13152 body_sha256=3325b7a9c1b4d456c0722854641f4d316b385d84cb21692147e8a0dd41dd693b basis=621c9edc6bb8932e4aed6ac9805a5684a4436789

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()
diff --git a/xmodel/systems-collision-preflight-sol56-20260902.md b/xmodel/systems-collision-preflight-sol56-20260902.md
new file mode 100644
index 0000000000000000000000000000000000000000..f1d8d1ea856cdb58301f3411e1088537ef610323
--- /dev/null
+++ b/xmodel/systems-collision-preflight-sol56-20260902.md
@@ -0,0 +1,283 @@
+# OPEN-collision hook and realization PREFLIGHT hard gate
+
+**Lane:** `systems-collision-preflight-sol56-20260902`  
+**Status:** IMPLEMENTED / FOCUSED TESTS PASS  
+**Scope:** implementation and regression controls only; no mathematical claim,
+promotion, canonical-ledger edit, or solver verdict.
+
+## 0. Frozen-input gate and write boundary
+
+Before reading the charged designs, SHA-256 was recomputed over the three
+frozen copies.  All values matched the charge exactly:
+
+```text
+e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  inputs/ideation-20260902T0741Z-opus5.md
+717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  inputs/ideation-20260902T0741Z-sol56.md
+4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  inputs/ideation-20260902T0741Z-synthesis.md
+```
+
+Here `inputs/` abbreviates the frozen lane directory
+`/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/`.
+The implementation follows the collision card at frozen Opus lines 519--535,
+the hard-gate specification at frozen Sol lines 408--439, and the corrected
+incidence/control specification in
+`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511`.
+
+Writes are bounded to the four requested implementation/test files and this
+report.  `ops/lane.sh`, `ops/seal.py`, `ops/artifact_finalize.py`, canonical
+ledgers, and other running-lane artifacts were not modified.  The excluded
+tree was not inspected.
+
+## 1. Typed deliverable A -- OPEN/banked collision check
+
+```text
+deliverable       = OPEN_BANKED_COLLISION_CHECK
+implementation    = ops/open_collision.py
+focused_tests     = ops/test_open_collision.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_open_collision.py
+pass_fail_skip     = 11 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 11 passed / 0 failed under python3 -O
+runtime            = 0.47 s focused; every individual test < 10 s
+integration_note   = Section 1.4
+```
+
+### 1.1 Input and extraction contract
+
+`open_collision.py` is pure Python and imports only the standard library.  It
+takes an unsealed report and repository root.  It recognizes explicit,
+anchored raised/opened headings, list entries, bold entries, heading entries,
+and the narrow inline form `THIS REPORT RAISES: OPEN[...]`.  Multiple tokens on
+one declared line are all retained.  Prose that merely discusses previously
+raised items does not activate extraction.  Broken bracket syntax inside an
+active section is an error, not an empty result.
+
+Each extracted token must carry an explicit bounded-quantity description.  A
+word such as `BOUND` inside the token identifier is removed before quantity
+parsing and cannot masquerade as that description.  This deliberate
+fail-closed requirement prevents a vague identifier from expanding into a
+hundreds-of-kilobytes seal artifact.  A producer with an unsearchable question
+must first state the quantity or refine its schema; the hook does not invent
+one.
+
+The corpus is exactly the charged set: nonrecursive `xmodel/*.md`, plus
+top-level `AUDIT.md`, `notes.md`, and `APPROACHES.md`.  The report being sealed
+is excluded by resolved identity.  A missing required file, symlinked corpus
+entry, unreadable/non-UTF-8 file, escaping path, invalid context window, or
+candidate fan-out above the declared cap returns exit 2 with an explicit
+`status: ERROR` block.
+
+### 1.2 Candidate semantics and output
+
+The search is lexical and candidate-only.  It requires the identifier's key
+nouns in a local context window and bounded-quantity terms on the
+relation-bearing line.  It recognizes ASCII, Unicode, and common LaTeX
+inequalities.  A relation line is still eligible when it carries an older
+`OPEN[...]` label; that label is useful provenance, not evidence against a
+collision.  Results are sorted deterministically and rendered as portable
+repository-relative `file:line` locations.  A candidate does not close,
+answer, or promote anything.
+
+Every successful invocation emits exactly one Markdown section headed
+`## COLLISIONS`.  With candidates it reports `status: CANDIDATES`; with no
+candidate or no declared raised item it reports `status: EMPTY` and an
+explicit `NONE` row.  The section is therefore never silently absent.  Errors
+also use the same heading with `status: ERROR`, while the nonzero exit blocks a
+seal wrapper.
+
+### 1.3 Required controls
+
+The retroactive fixture preserves the relevant source text at the exact
+1-based positions from
+`xmodel/companion-curve-alln-opus5-20260902.md`.  Feeding the bounded quantity
+for `OPEN[DEG-AF-VS-N]` produces, in order:
+
+```text
+xmodel/companion-curve-alln-opus5-20260902.md:685
+xmodel/companion-curve-alln-opus5-20260902.md:785
+xmodel/companion-curve-alln-opus5-20260902.md:836
+```
+
+The negative fixture declares a synthetic quantity with no banked match and
+asserts an explicit `status: EMPTY` block and per-token `NONE`.  Additional
+regressions cover same-line multiple tokens, malformed syntax, missing corpus
+files, prose false activation, identifier/quantity confusion, bounded fan-out,
+older labels on candidate lines, bold/heading entries, and LaTeX inequalities.
+
+### 1.4 Seal-time integration note
+
+Immediately before the standalone `BODY-END` marker is written, a future
+`lane.sh` seal phase (or a pre-step called by `seal.py`) would run
+`python3 ops/open_collision.py "$report" --root "$repo"`; any nonzero status
+aborts sealing, while exit 0 supplies the report's single `## COLLISIONS`
+block, which is inserted before `BODY-END` and only then hashed and stamped.
+The wrapper must reject a second block rather than append twice.  This lane
+describes that call boundary only and makes no change to either live tool.
+
+## 2. Typed deliverable B -- encoding-faithfulness PREFLIGHT
+
+```text
+deliverable       = N_GE_6_REALIZATION_PREFLIGHT
+implementation    = box/preflight.py
+focused_tests     = ops/test_preflight.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_preflight.py
+pass_fail_skip     = 16 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 16 passed / 0 failed under python3 -O
+runtime            = 2.14 s focused; every individual test < 10 s
+builtin_canaries   = 3 passed / 0 failed
+integration_note   = Section 2.6
+```
+
+### 2.1 Strict generator/solver contract
+
+The gate consumes a strict JSON manifest and the exact `.ms` file intended for
+launch:
+
+```text
+python3 box/preflight.py job.preflight.json job.ms
+```
+
+Unknown, missing, extra, duplicate, or wrongly typed JSON fields fail.  The
+manifest declares `N`, corrected cell, coefficient field, monomial order,
+ordered ring generators, explicit source order and generator images, the
+entire reduced approximate root, ordered ideal rows, the open factor and
+inverse, source-fidelity data, two-field output type, transition evidence, the
+pinned stack, and `solver_input_sha256`.  `N < 6`, any unknown cell family, a
+non-`QQ` field, or a non-`GRevLex` order is denied.  The bounded native cell set
+is `86A`, `86B`, `86C`, `86D`, `96A`, and `96B`; a new family requires an
+explicit schema extension and otherwise fails closed.
+
+The second CLI path is mandatory.  Its bytes must match the manifest SHA-256,
+be NUL-free UTF-8 POSIX text, and end in LF.  The gate parses the actual
+variable line, characteristic-zero line, comma framing, generator count, every
+ordered closed row, and the final Rabinowitsch row, then compares the parsed
+polynomials to the checked manifest.  Thus a correct sidecar cannot approve a
+stale or different ideal, even if that stale file's digest is copied into a
+new manifest.
+
+### 2.2 Exact root, coefficient, ring-map, and open checks
+
+The corrected expansions are recomputed exactly over `Fraction` arithmetic by
+reusing `box/qq_oracle_jobs.py`: the audit's `G86` with the target-dependent
+auxiliary terms and `G96` with `a15*p*q + a12*q^2 + a9*p + a6*q`.  The emitted
+root must contain exactly the recomputed nonzero coefficient sequence in
+descending degree.  Every encoded closed condition is parsed as a polynomial
+and exact-diffed against both that emitted coefficient and the independently
+recomputed coefficient; rendered-string equality is not trusted.
+
+The declared source ring order is fixed for the chosen cell.  Map images are
+parsed in the target ring and must form an explicit permutation isomorphism of
+the declared generators.  The manifest and actual solver variable orders must
+agree.  Exactly one open is permitted: the corrected `g_c` factor must be
+nonzero, its inverse must be the mapped `u`, and the actual final solver row
+must be polynomially equal to `u*g_c-1`.  No `sat()` wrapper is used.
+
+### 2.3 Fixed semantic and source-fidelity canaries
+
+The built-in controls are computations, not self-reported booleans:
+
+1. The charged old-A point is first checked to satisfy the old 86A closed rows
+   and old open.  Corrected top cancellation then forces the degree-19
+   residual `g19=11`, so corrected 86A rejects it.
+2. For `q=t^6+8*t^2`, `p=t^9+12*t^5+24*t`, and
+   `(a15,a12,a9,a6)=(0,0,0,-64)`, corrected 96B recomputes every closed
+   coefficient `g16,...,g3` as zero and the open coefficient `g2=64`.
+3. Source fidelity uses the canonical nonvacuous tuple
+   `(oldEQ2,q,E1)` and exact identity `EQ2_even=oldEQ2-2*q*E1`.
+   Renamed/zero aliases and the stale expression omitting `-2*q*E1` fail.
+
+These are encoding regression controls only.  Their execution here makes no
+new statement about curves, maps, attainment, or emptiness.
+
+### 2.4 Orthogonal output typing
+
+The accepted artifact field is exactly:
+
+```text
+NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT | FULL_EO_POINT |
+CURVE | REPRESENTATION | FULL_COVER | POLYNOMIAL_PAIR
+```
+
+The accepted attainment field is independently:
+
+```text
+NECESSARY | REALIZED | ADMISSIBLE_PULLBACK | ACTUAL_MAP |
+COUNTEREXAMPLE_CERTIFIED
+```
+
+Impossible pairings, unknown values, and advanced outputs without a checked
+provenance path are rejected.  The fields are emitted separately in the JSON
+result; no lossy single verdict is synthesized.
+
+### 2.5 Checked transition ladder
+
+Advanced A2 outputs must follow the complete, contiguous global path below;
+self-loops, late starts, unknown domains/scopes/claims, missing arrows, extra
+arrows, and noncontiguous states all fail.
+
+| From | To | Exact required arrows |
+|---|---|---|
+| `SUBSYSTEM_POINT/REALIZED` | `FULL_EO_POINT/REALIZED` | `FULL_EO_EQUATIONS`, `DECLARED_QUOTIENT` |
+| `FULL_EO_POINT/REALIZED` | `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `EO_ALL_OPENS`, `BOUNDARY_DATA`, `GENERAL_FIBRE_DATA`, `FIELD_DEGREE_FOUR` |
+| `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `POLYNOMIAL_PAIR/ACTUAL_MAP` | `SOURCE_CHART` |
+| `POLYNOMIAL_PAIR/ACTUAL_MAP` | `POLYNOMIAL_PAIR/COUNTEREXAMPLE_CERTIFIED` | `NONINVERTIBILITY`, `GEOMETRIC_DEGREE` |
+
+The table explicitly rejects an A2-to-B3 kill, local-to-global torsion, and a
+full-E/O point promoted to an actual map without the complete intermediate
+state and every declared arrow.
+
+### 2.6 Launch integration note
+
+A realization generator writes the strict manifest and `.ms` payload from the
+same ordered coefficient data, records the payload SHA-256 in the manifest,
+and the launch wrapper invokes `python3 box/preflight.py manifest.json job.ms`
+before starting qqideal/msolve; only exit 0 permits launch, the emitted JSON is
+retained as custody evidence, and the wrapper launches those same hash-pinned
+bytes (or rechecks the digest immediately before exec).  This lane implements
+the gate but does not alter any currently running launcher.
+
+## 3. Verification ledger
+
+Final local commands and results:
+
+```text
+python3 -m py_compile ops/open_collision.py ops/test_open_collision.py \
+    box/preflight.py ops/test_preflight.py
+PASS
+
+python3 -m unittest -v ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.589s -- OK
+
+python3 -O -m unittest -q ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.605s -- OK
+
+python3 box/preflight.py --self-test
+3/3 exact canaries PASS
+```
+
+All six reference manifests (`86A/B/C/D`, `96A/B`) also pass with their
+canonical in-memory solver payloads and an injected matching stack observation.
+No unit test invokes `msolve` or imports `qqideal`/`msolveio`.  The local live
+stack probe found `msolve=0.10.1`, while `qqideal` and `msolveio` are absent;
+therefore a real launch on this host is correctly refused until the pinned
+`qqideal=0.2.0` and `msolveio=0.2.1` packages are present.
+
+## 4. Guardrail and disposition
+
+This is an implementation lane.  Collision hits are candidates for human
+review, never closures.  Solver preflight success means only that the encoded
+job, controls, stack, solver bytes, and declared type transitions satisfy this
+contract; it is not a solver result or a mathematical realization.  The
+variable/ring map is explicit, the open is Rabinowitsch, raw remainder slots
+are exact-diffed in the declared ring/order, and attainment cannot be inferred
+from a weaker carrier.  No exit-price assertion is made, so no `charge_basis`
+line is emitted.
+
+## COLLISIONS
+
+status: EMPTY
+
+- NONE — the report contains no explicitly raised `OPEN[...]` entries.
+
+<!-- BODY-END -->

exec
/bin/zsh -lc 'python3 ops/seal.py verify --expected-basis 621c9edc6bb8932e4aed6ac9805a5684a4436789 xmodel/systems-collision-preflight-sol56-20260902.md && wc -c -l ops/open_collision.py ops/test_open_collision.py box/preflight.py ops/test_preflight.py xmodel/systems-collision-preflight-sol56-20260902.md && git status --short -- ops/open_collision.py ops/test_open_collision.py box/preflight.py ops/test_preflight.py xmodel/systems-collision-preflight-sol56-20260902.md ops/lane.sh ops/seal.py ops/artifact_finalize.py' in /Users/dc/code/math/jc2
 succeeded in 0ms:
PASS path=xmodel/systems-collision-preflight-sol56-20260902.md body_bytes=13152 body_sha256=3325b7a9c1b4d456c0722854641f4d316b385d84cb21692147e8a0dd41dd693b basis=621c9edc6bb8932e4aed6ac9805a5684a4436789
     522   17442 ops/open_collision.py
     201    8533 ops/test_open_collision.py
    1052   43119 box/preflight.py
     336   14139 ops/test_preflight.py
     292   13485 xmodel/systems-collision-preflight-sol56-20260902.md
    2403   96718 total
?? box/preflight.py
?? ops/open_collision.py
?? ops/test_open_collision.py
?? ops/test_preflight.py
?? xmodel/systems-collision-preflight-sol56-20260902.md

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()
diff --git a/xmodel/systems-collision-preflight-sol56-20260902.md b/xmodel/systems-collision-preflight-sol56-20260902.md
new file mode 100644
index 0000000000000000000000000000000000000000..f1d8d1ea856cdb58301f3411e1088537ef610323
--- /dev/null
+++ b/xmodel/systems-collision-preflight-sol56-20260902.md
@@ -0,0 +1,283 @@
+# OPEN-collision hook and realization PREFLIGHT hard gate
+
+**Lane:** `systems-collision-preflight-sol56-20260902`  
+**Status:** IMPLEMENTED / FOCUSED TESTS PASS  
+**Scope:** implementation and regression controls only; no mathematical claim,
+promotion, canonical-ledger edit, or solver verdict.
+
+## 0. Frozen-input gate and write boundary
+
+Before reading the charged designs, SHA-256 was recomputed over the three
+frozen copies.  All values matched the charge exactly:
+
+```text
+e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  inputs/ideation-20260902T0741Z-opus5.md
+717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  inputs/ideation-20260902T0741Z-sol56.md
+4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  inputs/ideation-20260902T0741Z-synthesis.md
+```
+
+Here `inputs/` abbreviates the frozen lane directory
+`/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/`.
+The implementation follows the collision card at frozen Opus lines 519--535,
+the hard-gate specification at frozen Sol lines 408--439, and the corrected
+incidence/control specification in
+`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511`.
+
+Writes are bounded to the four requested implementation/test files and this
+report.  `ops/lane.sh`, `ops/seal.py`, `ops/artifact_finalize.py`, canonical
+ledgers, and other running-lane artifacts were not modified.  The excluded
+tree was not inspected.
+
+## 1. Typed deliverable A -- OPEN/banked collision check
+
+```text
+deliverable       = OPEN_BANKED_COLLISION_CHECK
+implementation    = ops/open_collision.py
+focused_tests     = ops/test_open_collision.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_open_collision.py
+pass_fail_skip     = 11 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 11 passed / 0 failed under python3 -O
+runtime            = 0.47 s focused; every individual test < 10 s
+integration_note   = Section 1.4
+```
+
+### 1.1 Input and extraction contract
+
+`open_collision.py` is pure Python and imports only the standard library.  It
+takes an unsealed report and repository root.  It recognizes explicit,
+anchored raised/opened headings, list entries, bold entries, heading entries,
+and the narrow inline form `THIS REPORT RAISES: OPEN[...]`.  Multiple tokens on
+one declared line are all retained.  Prose that merely discusses previously
+raised items does not activate extraction.  Broken bracket syntax inside an
+active section is an error, not an empty result.
+
+Each extracted token must carry an explicit bounded-quantity description.  A
+word such as `BOUND` inside the token identifier is removed before quantity
+parsing and cannot masquerade as that description.  This deliberate
+fail-closed requirement prevents a vague identifier from expanding into a
+hundreds-of-kilobytes seal artifact.  A producer with an unsearchable question
+must first state the quantity or refine its schema; the hook does not invent
+one.
+
+The corpus is exactly the charged set: nonrecursive `xmodel/*.md`, plus
+top-level `AUDIT.md`, `notes.md`, and `APPROACHES.md`.  The report being sealed
+is excluded by resolved identity.  A missing required file, symlinked corpus
+entry, unreadable/non-UTF-8 file, escaping path, invalid context window, or
+candidate fan-out above the declared cap returns exit 2 with an explicit
+`status: ERROR` block.
+
+### 1.2 Candidate semantics and output
+
+The search is lexical and candidate-only.  It requires the identifier's key
+nouns in a local context window and bounded-quantity terms on the
+relation-bearing line.  It recognizes ASCII, Unicode, and common LaTeX
+inequalities.  A relation line is still eligible when it carries an older
+`OPEN[...]` label; that label is useful provenance, not evidence against a
+collision.  Results are sorted deterministically and rendered as portable
+repository-relative `file:line` locations.  A candidate does not close,
+answer, or promote anything.
+
+Every successful invocation emits exactly one Markdown section headed
+`## COLLISIONS`.  With candidates it reports `status: CANDIDATES`; with no
+candidate or no declared raised item it reports `status: EMPTY` and an
+explicit `NONE` row.  The section is therefore never silently absent.  Errors
+also use the same heading with `status: ERROR`, while the nonzero exit blocks a
+seal wrapper.
+
+### 1.3 Required controls
+
+The retroactive fixture preserves the relevant source text at the exact
+1-based positions from
+`xmodel/companion-curve-alln-opus5-20260902.md`.  Feeding the bounded quantity
+for `OPEN[DEG-AF-VS-N]` produces, in order:
+
+```text
+xmodel/companion-curve-alln-opus5-20260902.md:685
+xmodel/companion-curve-alln-opus5-20260902.md:785
+xmodel/companion-curve-alln-opus5-20260902.md:836
+```
+
+The negative fixture declares a synthetic quantity with no banked match and
+asserts an explicit `status: EMPTY` block and per-token `NONE`.  Additional
+regressions cover same-line multiple tokens, malformed syntax, missing corpus
+files, prose false activation, identifier/quantity confusion, bounded fan-out,
+older labels on candidate lines, bold/heading entries, and LaTeX inequalities.
+
+### 1.4 Seal-time integration note
+
+Immediately before the standalone `BODY-END` marker is written, a future
+`lane.sh` seal phase (or a pre-step called by `seal.py`) would run
+`python3 ops/open_collision.py "$report" --root "$repo"`; any nonzero status
+aborts sealing, while exit 0 supplies the report's single `## COLLISIONS`
+block, which is inserted before `BODY-END` and only then hashed and stamped.
+The wrapper must reject a second block rather than append twice.  This lane
+describes that call boundary only and makes no change to either live tool.
+
+## 2. Typed deliverable B -- encoding-faithfulness PREFLIGHT
+
+```text
+deliverable       = N_GE_6_REALIZATION_PREFLIGHT
+implementation    = box/preflight.py
+focused_tests     = ops/test_preflight.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_preflight.py
+pass_fail_skip     = 16 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 16 passed / 0 failed under python3 -O
+runtime            = 2.14 s focused; every individual test < 10 s
+builtin_canaries   = 3 passed / 0 failed
+integration_note   = Section 2.6
+```
+
+### 2.1 Strict generator/solver contract
+
+The gate consumes a strict JSON manifest and the exact `.ms` file intended for
+launch:
+
+```text
+python3 box/preflight.py job.preflight.json job.ms
+```
+
+Unknown, missing, extra, duplicate, or wrongly typed JSON fields fail.  The
+manifest declares `N`, corrected cell, coefficient field, monomial order,
+ordered ring generators, explicit source order and generator images, the
+entire reduced approximate root, ordered ideal rows, the open factor and
+inverse, source-fidelity data, two-field output type, transition evidence, the
+pinned stack, and `solver_input_sha256`.  `N < 6`, any unknown cell family, a
+non-`QQ` field, or a non-`GRevLex` order is denied.  The bounded native cell set
+is `86A`, `86B`, `86C`, `86D`, `96A`, and `96B`; a new family requires an
+explicit schema extension and otherwise fails closed.
+
+The second CLI path is mandatory.  Its bytes must match the manifest SHA-256,
+be NUL-free UTF-8 POSIX text, and end in LF.  The gate parses the actual
+variable line, characteristic-zero line, comma framing, generator count, every
+ordered closed row, and the final Rabinowitsch row, then compares the parsed
+polynomials to the checked manifest.  Thus a correct sidecar cannot approve a
+stale or different ideal, even if that stale file's digest is copied into a
+new manifest.
+
+### 2.2 Exact root, coefficient, ring-map, and open checks
+
+The corrected expansions are recomputed exactly over `Fraction` arithmetic by
+reusing `box/qq_oracle_jobs.py`: the audit's `G86` with the target-dependent
+auxiliary terms and `G96` with `a15*p*q + a12*q^2 + a9*p + a6*q`.  The emitted
+root must contain exactly the recomputed nonzero coefficient sequence in
+descending degree.  Every encoded closed condition is parsed as a polynomial
+and exact-diffed against both that emitted coefficient and the independently
+recomputed coefficient; rendered-string equality is not trusted.
+
+The declared source ring order is fixed for the chosen cell.  Map images are
+parsed in the target ring and must form an explicit permutation isomorphism of
+the declared generators.  The manifest and actual solver variable orders must
+agree.  Exactly one open is permitted: the corrected `g_c` factor must be
+nonzero, its inverse must be the mapped `u`, and the actual final solver row
+must be polynomially equal to `u*g_c-1`.  No `sat()` wrapper is used.
+
+### 2.3 Fixed semantic and source-fidelity canaries
+
+The built-in controls are computations, not self-reported booleans:
+
+1. The charged old-A point is first checked to satisfy the old 86A closed rows
+   and old open.  Corrected top cancellation then forces the degree-19
+   residual `g19=11`, so corrected 86A rejects it.
+2. For `q=t^6+8*t^2`, `p=t^9+12*t^5+24*t`, and
+   `(a15,a12,a9,a6)=(0,0,0,-64)`, corrected 96B recomputes every closed
+   coefficient `g16,...,g3` as zero and the open coefficient `g2=64`.
+3. Source fidelity uses the canonical nonvacuous tuple
+   `(oldEQ2,q,E1)` and exact identity `EQ2_even=oldEQ2-2*q*E1`.
+   Renamed/zero aliases and the stale expression omitting `-2*q*E1` fail.
+
+These are encoding regression controls only.  Their execution here makes no
+new statement about curves, maps, attainment, or emptiness.
+
+### 2.4 Orthogonal output typing
+
+The accepted artifact field is exactly:
+
+```text
+NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT | FULL_EO_POINT |
+CURVE | REPRESENTATION | FULL_COVER | POLYNOMIAL_PAIR
+```
+
+The accepted attainment field is independently:
+
+```text
+NECESSARY | REALIZED | ADMISSIBLE_PULLBACK | ACTUAL_MAP |
+COUNTEREXAMPLE_CERTIFIED
+```
+
+Impossible pairings, unknown values, and advanced outputs without a checked
+provenance path are rejected.  The fields are emitted separately in the JSON
+result; no lossy single verdict is synthesized.
+
+### 2.5 Checked transition ladder
+
+Advanced A2 outputs must follow the complete, contiguous global path below;
+self-loops, late starts, unknown domains/scopes/claims, missing arrows, extra
+arrows, and noncontiguous states all fail.
+
+| From | To | Exact required arrows |
+|---|---|---|
+| `SUBSYSTEM_POINT/REALIZED` | `FULL_EO_POINT/REALIZED` | `FULL_EO_EQUATIONS`, `DECLARED_QUOTIENT` |
+| `FULL_EO_POINT/REALIZED` | `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `EO_ALL_OPENS`, `BOUNDARY_DATA`, `GENERAL_FIBRE_DATA`, `FIELD_DEGREE_FOUR` |
+| `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `POLYNOMIAL_PAIR/ACTUAL_MAP` | `SOURCE_CHART` |
+| `POLYNOMIAL_PAIR/ACTUAL_MAP` | `POLYNOMIAL_PAIR/COUNTEREXAMPLE_CERTIFIED` | `NONINVERTIBILITY`, `GEOMETRIC_DEGREE` |
+
+The table explicitly rejects an A2-to-B3 kill, local-to-global torsion, and a
+full-E/O point promoted to an actual map without the complete intermediate
+state and every declared arrow.
+
+### 2.6 Launch integration note
+
+A realization generator writes the strict manifest and `.ms` payload from the
+same ordered coefficient data, records the payload SHA-256 in the manifest,
+and the launch wrapper invokes `python3 box/preflight.py manifest.json job.ms`
+before starting qqideal/msolve; only exit 0 permits launch, the emitted JSON is
+retained as custody evidence, and the wrapper launches those same hash-pinned
+bytes (or rechecks the digest immediately before exec).  This lane implements
+the gate but does not alter any currently running launcher.
+
+## 3. Verification ledger
+
+Final local commands and results:
+
+```text
+python3 -m py_compile ops/open_collision.py ops/test_open_collision.py \
+    box/preflight.py ops/test_preflight.py
+PASS
+
+python3 -m unittest -v ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.589s -- OK
+
+python3 -O -m unittest -q ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.605s -- OK
+
+python3 box/preflight.py --self-test
+3/3 exact canaries PASS
+```
+
+All six reference manifests (`86A/B/C/D`, `96A/B`) also pass with their
+canonical in-memory solver payloads and an injected matching stack observation.
+No unit test invokes `msolve` or imports `qqideal`/`msolveio`.  The local live
+stack probe found `msolve=0.10.1`, while `qqideal` and `msolveio` are absent;
+therefore a real launch on this host is correctly refused until the pinned
+`qqideal=0.2.0` and `msolveio=0.2.1` packages are present.
+
+## 4. Guardrail and disposition
+
+This is an implementation lane.  Collision hits are candidates for human
+review, never closures.  Solver preflight success means only that the encoded
+job, controls, stack, solver bytes, and declared type transitions satisfy this
+contract; it is not a solver result or a mathematical realization.  The
+variable/ring map is explicit, the open is Rabinowitsch, raw remainder slots
+are exact-diffed in the declared ring/order, and attainment cannot be inferred
+from a weaker carrier.  No exit-price assertion is made, so no `charge_basis`
+line is emitted.
+
+## COLLISIONS
+
+status: EMPTY
+
+- NONE — the report contains no explicitly raised `OPEN[...]` entries.
+
+<!-- BODY-END -->

exec
/bin/zsh -lc 'tail -24 xmodel/systems-collision-preflight-sol56-20260902.md' in /Users/dc/code/math/jc2
 succeeded in 0ms:
review, never closures.  Solver preflight success means only that the encoded
job, controls, stack, solver bytes, and declared type transitions satisfy this
contract; it is not a solver result or a mathematical realization.  The
variable/ring map is explicit, the open is Rabinowitsch, raw remainder slots
are exact-diffed in the declared ring/order, and attainment cannot be inferred
from a weaker carrier.  No exit-price assertion is made, so no `charge_basis`
line is emitted.

## COLLISIONS

status: EMPTY

- NONE — the report contains no explicitly raised `OPEN[...]` entries.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `13152`.
- Body SHA-256:
  `3325b7a9c1b4d456c0722854641f4d316b385d84cb21692147e8a0dd41dd693b`.
- Frozen basis: `621c9edc6bb8932e4aed6ac9805a5684a4436789`.

diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()
diff --git a/xmodel/systems-collision-preflight-sol56-20260902.md b/xmodel/systems-collision-preflight-sol56-20260902.md
new file mode 100644
index 0000000000000000000000000000000000000000..f1d8d1ea856cdb58301f3411e1088537ef610323
--- /dev/null
+++ b/xmodel/systems-collision-preflight-sol56-20260902.md
@@ -0,0 +1,283 @@
+# OPEN-collision hook and realization PREFLIGHT hard gate
+
+**Lane:** `systems-collision-preflight-sol56-20260902`  
+**Status:** IMPLEMENTED / FOCUSED TESTS PASS  
+**Scope:** implementation and regression controls only; no mathematical claim,
+promotion, canonical-ledger edit, or solver verdict.
+
+## 0. Frozen-input gate and write boundary
+
+Before reading the charged designs, SHA-256 was recomputed over the three
+frozen copies.  All values matched the charge exactly:
+
+```text
+e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  inputs/ideation-20260902T0741Z-opus5.md
+717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  inputs/ideation-20260902T0741Z-sol56.md
+4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  inputs/ideation-20260902T0741Z-synthesis.md
+```
+
+Here `inputs/` abbreviates the frozen lane directory
+`/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/`.
+The implementation follows the collision card at frozen Opus lines 519--535,
+the hard-gate specification at frozen Sol lines 408--439, and the corrected
+incidence/control specification in
+`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511`.
+
+Writes are bounded to the four requested implementation/test files and this
+report.  `ops/lane.sh`, `ops/seal.py`, `ops/artifact_finalize.py`, canonical
+ledgers, and other running-lane artifacts were not modified.  The excluded
+tree was not inspected.
+
+## 1. Typed deliverable A -- OPEN/banked collision check
+
+```text
+deliverable       = OPEN_BANKED_COLLISION_CHECK
+implementation    = ops/open_collision.py
+focused_tests     = ops/test_open_collision.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_open_collision.py
+pass_fail_skip     = 11 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 11 passed / 0 failed under python3 -O
+runtime            = 0.47 s focused; every individual test < 10 s
+integration_note   = Section 1.4
+```
+
+### 1.1 Input and extraction contract
+
+`open_collision.py` is pure Python and imports only the standard library.  It
+takes an unsealed report and repository root.  It recognizes explicit,
+anchored raised/opened headings, list entries, bold entries, heading entries,
+and the narrow inline form `THIS REPORT RAISES: OPEN[...]`.  Multiple tokens on
+one declared line are all retained.  Prose that merely discusses previously
+raised items does not activate extraction.  Broken bracket syntax inside an
+active section is an error, not an empty result.
+
+Each extracted token must carry an explicit bounded-quantity description.  A
+word such as `BOUND` inside the token identifier is removed before quantity
+parsing and cannot masquerade as that description.  This deliberate
+fail-closed requirement prevents a vague identifier from expanding into a
+hundreds-of-kilobytes seal artifact.  A producer with an unsearchable question
+must first state the quantity or refine its schema; the hook does not invent
+one.
+
+The corpus is exactly the charged set: nonrecursive `xmodel/*.md`, plus
+top-level `AUDIT.md`, `notes.md`, and `APPROACHES.md`.  The report being sealed
+is excluded by resolved identity.  A missing required file, symlinked corpus
+entry, unreadable/non-UTF-8 file, escaping path, invalid context window, or
+candidate fan-out above the declared cap returns exit 2 with an explicit
+`status: ERROR` block.
+
+### 1.2 Candidate semantics and output
+
+The search is lexical and candidate-only.  It requires the identifier's key
+nouns in a local context window and bounded-quantity terms on the
+relation-bearing line.  It recognizes ASCII, Unicode, and common LaTeX
+inequalities.  A relation line is still eligible when it carries an older
+`OPEN[...]` label; that label is useful provenance, not evidence against a
+collision.  Results are sorted deterministically and rendered as portable
+repository-relative `file:line` locations.  A candidate does not close,
+answer, or promote anything.
+
+Every successful invocation emits exactly one Markdown section headed
+`## COLLISIONS`.  With candidates it reports `status: CANDIDATES`; with no
+candidate or no declared raised item it reports `status: EMPTY` and an
+explicit `NONE` row.  The section is therefore never silently absent.  Errors
+also use the same heading with `status: ERROR`, while the nonzero exit blocks a
+seal wrapper.
+
+### 1.3 Required controls
+
+The retroactive fixture preserves the relevant source text at the exact
+1-based positions from
+`xmodel/companion-curve-alln-opus5-20260902.md`.  Feeding the bounded quantity
+for `OPEN[DEG-AF-VS-N]` produces, in order:
+
+```text
+xmodel/companion-curve-alln-opus5-20260902.md:685
+xmodel/companion-curve-alln-opus5-20260902.md:785
+xmodel/companion-curve-alln-opus5-20260902.md:836
+```
+
+The negative fixture declares a synthetic quantity with no banked match and
+asserts an explicit `status: EMPTY` block and per-token `NONE`.  Additional
+regressions cover same-line multiple tokens, malformed syntax, missing corpus
+files, prose false activation, identifier/quantity confusion, bounded fan-out,
+older labels on candidate lines, bold/heading entries, and LaTeX inequalities.
+
+### 1.4 Seal-time integration note
+
+Immediately before the standalone `BODY-END` marker is written, a future
+`lane.sh` seal phase (or a pre-step called by `seal.py`) would run
+`python3 ops/open_collision.py "$report" --root "$repo"`; any nonzero status
+aborts sealing, while exit 0 supplies the report's single `## COLLISIONS`
+block, which is inserted before `BODY-END` and only then hashed and stamped.
+The wrapper must reject a second block rather than append twice.  This lane
+describes that call boundary only and makes no change to either live tool.
+
+## 2. Typed deliverable B -- encoding-faithfulness PREFLIGHT
+
+```text
+deliverable       = N_GE_6_REALIZATION_PREFLIGHT
+implementation    = box/preflight.py
+focused_tests     = ops/test_preflight.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_preflight.py
+pass_fail_skip     = 16 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 16 passed / 0 failed under python3 -O
+runtime            = 2.14 s focused; every individual test < 10 s
+builtin_canaries   = 3 passed / 0 failed
+integration_note   = Section 2.6
+```
+
+### 2.1 Strict generator/solver contract
+
+The gate consumes a strict JSON manifest and the exact `.ms` file intended for
+launch:
+
+```text
+python3 box/preflight.py job.preflight.json job.ms
+```
+
+Unknown, missing, extra, duplicate, or wrongly typed JSON fields fail.  The
+manifest declares `N`, corrected cell, coefficient field, monomial order,
+ordered ring generators, explicit source order and generator images, the
+entire reduced approximate root, ordered ideal rows, the open factor and
+inverse, source-fidelity data, two-field output type, transition evidence, the
+pinned stack, and `solver_input_sha256`.  `N < 6`, any unknown cell family, a
+non-`QQ` field, or a non-`GRevLex` order is denied.  The bounded native cell set
+is `86A`, `86B`, `86C`, `86D`, `96A`, and `96B`; a new family requires an
+explicit schema extension and otherwise fails closed.
+
+The second CLI path is mandatory.  Its bytes must match the manifest SHA-256,
+be NUL-free UTF-8 POSIX text, and end in LF.  The gate parses the actual
+variable line, characteristic-zero line, comma framing, generator count, every
+ordered closed row, and the final Rabinowitsch row, then compares the parsed
+polynomials to the checked manifest.  Thus a correct sidecar cannot approve a
+stale or different ideal, even if that stale file's digest is copied into a
+new manifest.
+
+### 2.2 Exact root, coefficient, ring-map, and open checks
+
+The corrected expansions are recomputed exactly over `Fraction` arithmetic by
+reusing `box/qq_oracle_jobs.py`: the audit's `G86` with the target-dependent
+auxiliary terms and `G96` with `a15*p*q + a12*q^2 + a9*p + a6*q`.  The emitted
+root must contain exactly the recomputed nonzero coefficient sequence in
+descending degree.  Every encoded closed condition is parsed as a polynomial
+and exact-diffed against both that emitted coefficient and the independently
+recomputed coefficient; rendered-string equality is not trusted.
+
+The declared source ring order is fixed for the chosen cell.  Map images are
+parsed in the target ring and must form an explicit permutation isomorphism of
+the declared generators.  The manifest and actual solver variable orders must
+agree.  Exactly one open is permitted: the corrected `g_c` factor must be
+nonzero, its inverse must be the mapped `u`, and the actual final solver row
+must be polynomially equal to `u*g_c-1`.  No `sat()` wrapper is used.
+
+### 2.3 Fixed semantic and source-fidelity canaries
+
+The built-in controls are computations, not self-reported booleans:
+
+1. The charged old-A point is first checked to satisfy the old 86A closed rows
+   and old open.  Corrected top cancellation then forces the degree-19
+   residual `g19=11`, so corrected 86A rejects it.
+2. For `q=t^6+8*t^2`, `p=t^9+12*t^5+24*t`, and
+   `(a15,a12,a9,a6)=(0,0,0,-64)`, corrected 96B recomputes every closed
+   coefficient `g16,...,g3` as zero and the open coefficient `g2=64`.
+3. Source fidelity uses the canonical nonvacuous tuple
+   `(oldEQ2,q,E1)` and exact identity `EQ2_even=oldEQ2-2*q*E1`.
+   Renamed/zero aliases and the stale expression omitting `-2*q*E1` fail.
+
+These are encoding regression controls only.  Their execution here makes no
+new statement about curves, maps, attainment, or emptiness.
+
+### 2.4 Orthogonal output typing
+
+The accepted artifact field is exactly:
+
+```text
+NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT | FULL_EO_POINT |
+CURVE | REPRESENTATION | FULL_COVER | POLYNOMIAL_PAIR
+```
+
+The accepted attainment field is independently:
+
+```text
+NECESSARY | REALIZED | ADMISSIBLE_PULLBACK | ACTUAL_MAP |
+COUNTEREXAMPLE_CERTIFIED
+```
+
+Impossible pairings, unknown values, and advanced outputs without a checked
+provenance path are rejected.  The fields are emitted separately in the JSON
+result; no lossy single verdict is synthesized.
+
+### 2.5 Checked transition ladder
+
+Advanced A2 outputs must follow the complete, contiguous global path below;
+self-loops, late starts, unknown domains/scopes/claims, missing arrows, extra
+arrows, and noncontiguous states all fail.
+
+| From | To | Exact required arrows |
+|---|---|---|
+| `SUBSYSTEM_POINT/REALIZED` | `FULL_EO_POINT/REALIZED` | `FULL_EO_EQUATIONS`, `DECLARED_QUOTIENT` |
+| `FULL_EO_POINT/REALIZED` | `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `EO_ALL_OPENS`, `BOUNDARY_DATA`, `GENERAL_FIBRE_DATA`, `FIELD_DEGREE_FOUR` |
+| `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `POLYNOMIAL_PAIR/ACTUAL_MAP` | `SOURCE_CHART` |
+| `POLYNOMIAL_PAIR/ACTUAL_MAP` | `POLYNOMIAL_PAIR/COUNTEREXAMPLE_CERTIFIED` | `NONINVERTIBILITY`, `GEOMETRIC_DEGREE` |
+
+The table explicitly rejects an A2-to-B3 kill, local-to-global torsion, and a
+full-E/O point promoted to an actual map without the complete intermediate
+state and every declared arrow.
+
+### 2.6 Launch integration note
+
+A realization generator writes the strict manifest and `.ms` payload from the
+same ordered coefficient data, records the payload SHA-256 in the manifest,
+and the launch wrapper invokes `python3 box/preflight.py manifest.json job.ms`
+before starting qqideal/msolve; only exit 0 permits launch, the emitted JSON is
+retained as custody evidence, and the wrapper launches those same hash-pinned
+bytes (or rechecks the digest immediately before exec).  This lane implements
+the gate but does not alter any currently running launcher.
+
+## 3. Verification ledger
+
+Final local commands and results:
+
+```text
+python3 -m py_compile ops/open_collision.py ops/test_open_collision.py \
+    box/preflight.py ops/test_preflight.py
+PASS
+
+python3 -m unittest -v ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.589s -- OK
+
+python3 -O -m unittest -q ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.605s -- OK
+
+python3 box/preflight.py --self-test
+3/3 exact canaries PASS
+```
+
+All six reference manifests (`86A/B/C/D`, `96A/B`) also pass with their
+canonical in-memory solver payloads and an injected matching stack observation.
+No unit test invokes `msolve` or imports `qqideal`/`msolveio`.  The local live
+stack probe found `msolve=0.10.1`, while `qqideal` and `msolveio` are absent;
+therefore a real launch on this host is correctly refused until the pinned
+`qqideal=0.2.0` and `msolveio=0.2.1` packages are present.
+
+## 4. Guardrail and disposition
+
+This is an implementation lane.  Collision hits are candidates for human
+review, never closures.  Solver preflight success means only that the encoded
+job, controls, stack, solver bytes, and declared type transitions satisfy this
+contract; it is not a solver result or a mathematical realization.  The
+variable/ring map is explicit, the open is Rabinowitsch, raw remainder slots
+are exact-diffed in the declared ring/order, and attainment cannot be inferred
+from a weaker carrier.  No exit-price assertion is made, so no `charge_basis`
+line is emitted.
+
+## COLLISIONS
+
+status: EMPTY
+
+- NONE — the report contains no explicitly raised `OPEN[...]` entries.
+
+<!-- BODY-END -->

codex
Implemented and sealed successfully.

- [OPEN collision hook](/Users/dc/code/math/jc2/ops/open_collision.py) with [11 focused tests](/Users/dc/code/math/jc2/ops/test_open_collision.py), including exact lines 685/785/836 and explicit empty output.
- [PREFLIGHT hard gate](/Users/dc/code/math/jc2/box/preflight.py) with [16 focused tests](/Users/dc/code/math/jc2/ops/test_preflight.py), exact canaries, transition enforcement, and solver-input binding.
- [Sealed report](/Users/dc/code/math/jc2/xmodel/systems-collision-preflight-sol56-20260902.md).

Verification: 27/27 tests passed normally and under `python -O`; `py_compile` passed; 3/3 built-in canaries passed. Seal SHA-256: `3325b7a9c1b4d456c0722854641f4d316b385d84cb21692147e8a0dd41dd693b`.

The local host has `msolve 0.10.1`; absent `qqideal`/`msolveio` packages correctly cause real launches to fail closed. No prohibited files were modified.
diff --git a/ops/open_collision.py b/ops/open_collision.py
index 6599ee3ca01cf40e9b66190d7980f422bde50c79..539c6d4bbed8ca213261358a6b4d17b0b18e32cf
--- a/ops/open_collision.py
+++ b/ops/open_collision.py
@@ -34,7 +34,8 @@
     re.IGNORECASE,
 )
 ENTRY_RE = re.compile(
-    r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
+    r"^\s*(?:(?:#{1,6}|[-*+]|\d+[.)])\s+)?"
+    r"(?:\*\*)?`?(OPEN\[[^]\r\n]+\])"
 )
 INLINE_RAISE_RE = re.compile(
     r"^\s*(?:(?:[-*+]|\d+[.)])\s+)?(?:THIS\s+REPORT\s+)?"
@@ -47,7 +48,7 @@
     re.IGNORECASE,
 )
 RELATION_RE = re.compile(
-    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥])"
+    r"(?:<=|>=|==|(?<![<>=])=(?!=)|[<>≤≥]|\\(?:leq?|geq?)\b)"
     r"|\b(?:bound(?:s|ed|ing)?|cap(?:s|ped|ping)?|ceiling|floor|"
     r"at\s+most|at\s+least)\b",
     re.IGNORECASE,
@@ -252,16 +253,15 @@
             start_entries(tail, lineno)
             continue
 
-        if active and (OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line)):
-            finish_entry()
-            active = False
-
         if active:
             if "OPEN[" in OPEN_RE.sub("", line):
                 raise CollisionError(f"line {lineno}: malformed OPEN token")
             entry = ENTRY_RE.match(line)
             if entry is not None:
                 start_entries(line[entry.start(1) :], lineno)
+            elif OTHER_OPEN_STATUS_RE.search(line) or _section_boundary(line):
+                finish_entry()
+                active = False
             elif current_identifier is not None:
                 current_text.append(line)
             continue
@@ -381,7 +381,12 @@
         for index, line in enumerate(lines):
             if not RELATION_RE.search(line):
                 continue
-            if OPEN_RE.search(line) or "COLLISIONS" in line:
+            # A banked inequality may still carry the OPEN label under which
+            # it was recorded.  That label is provenance, not a reason to
+            # suppress an otherwise matching candidate.  Only generated
+            # collision-output lines are excluded here; the report itself is
+            # already excluded from the corpus by ``banked_corpus``.
+            if "COLLISIONS" in line:
                 continue
             low = max(0, index - context_lines)
             high = min(len(lines), index + context_lines + 1)
diff --git a/ops/test_open_collision.py b/ops/test_open_collision.py
index 54dc273d23e621092a1937c9da7b469f111c3ed5..a2f257d527951332480af1099f696db8238681fd
--- a/ops/test_open_collision.py
+++ b/ops/test_open_collision.py
@@ -155,6 +155,47 @@
         self.assertIn("status: ERROR", result.stdout)
         self.assertIn("more than 100 candidates", result.stdout)
 
+    def test_banked_relation_with_open_label_remains_a_candidate(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "OPEN[OLDER-ALPHA] alpha meridian count <= limit\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
+    def test_bold_and_heading_entries_are_not_mistaken_for_boundaries(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            "alpha meridian count <= limit\n"
+            "beta orbit count <= ceiling\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "**OPENs raised.**\n\n"
+            "**OPEN[ALPHA-MERIDIAN-COUNT]** — bound alpha meridian count.\n"
+            "### OPEN[BETA-ORBIT-COUNT] — bound beta orbit count.\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("### OPEN[ALPHA-MERIDIAN-COUNT]", result.stdout)
+        self.assertIn("### OPEN[BETA-ORBIT-COUNT]", result.stdout)
+
+    def test_latex_inequality_is_scanned_as_a_bounded_relation(self) -> None:
+        (self.root / "xmodel" / "bank.md").write_text(
+            r"alpha meridian count \leq limit" + "\n",
+            encoding="utf-8",
+        )
+        result = self.run_tool(
+            "OPENS RAISED OPEN[ALPHA-MERIDIAN-COUNT] "
+            "bound alpha meridian count <= limit\n"
+        )
+        self.assertEqual(result.returncode, 0, result.stderr)
+        self.assertIn("status: CANDIDATES", result.stdout)
+        self.assertIn("`xmodel/bank.md:1`", result.stdout)
+
 
 if __name__ == "__main__":
     unittest.main()
diff --git a/xmodel/systems-collision-preflight-sol56-20260902.md b/xmodel/systems-collision-preflight-sol56-20260902.md
new file mode 100644
index 0000000000000000000000000000000000000000..f1d8d1ea856cdb58301f3411e1088537ef610323
--- /dev/null
+++ b/xmodel/systems-collision-preflight-sol56-20260902.md
@@ -0,0 +1,283 @@
+# OPEN-collision hook and realization PREFLIGHT hard gate
+
+**Lane:** `systems-collision-preflight-sol56-20260902`  
+**Status:** IMPLEMENTED / FOCUSED TESTS PASS  
+**Scope:** implementation and regression controls only; no mathematical claim,
+promotion, canonical-ledger edit, or solver verdict.
+
+## 0. Frozen-input gate and write boundary
+
+Before reading the charged designs, SHA-256 was recomputed over the three
+frozen copies.  All values matched the charge exactly:
+
+```text
+e67b2cd027e0cdbfc249d057d929e72a3e553ca61256f48b9978bedda358e9ce  inputs/ideation-20260902T0741Z-opus5.md
+717134c733adc17b3b70409b018858fb1311c37608a24f0eebe65ae79aec8b11  inputs/ideation-20260902T0741Z-sol56.md
+4408cfff1eb99c4f12a81c8d1b46906df260dc790ca357240dfd4567f08d140d  inputs/ideation-20260902T0741Z-synthesis.md
+```
+
+Here `inputs/` abbreviates the frozen lane directory
+`/private/var/folders/80/jm5p82hn56g0crpvv9xjzrc00000gn/T/jc2-lane.Ng2gjj/inputs/`.
+The implementation follows the collision card at frozen Opus lines 519--535,
+the hard-gate specification at frozen Sol lines 408--439, and the corrected
+incidence/control specification in
+`xmodel/encoding-faithfulness-audit-r2-sol56-20260901.md:462-511`.
+
+Writes are bounded to the four requested implementation/test files and this
+report.  `ops/lane.sh`, `ops/seal.py`, `ops/artifact_finalize.py`, canonical
+ledgers, and other running-lane artifacts were not modified.  The excluded
+tree was not inspected.
+
+## 1. Typed deliverable A -- OPEN/banked collision check
+
+```text
+deliverable       = OPEN_BANKED_COLLISION_CHECK
+implementation    = ops/open_collision.py
+focused_tests     = ops/test_open_collision.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_open_collision.py
+pass_fail_skip     = 11 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 11 passed / 0 failed under python3 -O
+runtime            = 0.47 s focused; every individual test < 10 s
+integration_note   = Section 1.4
+```
+
+### 1.1 Input and extraction contract
+
+`open_collision.py` is pure Python and imports only the standard library.  It
+takes an unsealed report and repository root.  It recognizes explicit,
+anchored raised/opened headings, list entries, bold entries, heading entries,
+and the narrow inline form `THIS REPORT RAISES: OPEN[...]`.  Multiple tokens on
+one declared line are all retained.  Prose that merely discusses previously
+raised items does not activate extraction.  Broken bracket syntax inside an
+active section is an error, not an empty result.
+
+Each extracted token must carry an explicit bounded-quantity description.  A
+word such as `BOUND` inside the token identifier is removed before quantity
+parsing and cannot masquerade as that description.  This deliberate
+fail-closed requirement prevents a vague identifier from expanding into a
+hundreds-of-kilobytes seal artifact.  A producer with an unsearchable question
+must first state the quantity or refine its schema; the hook does not invent
+one.
+
+The corpus is exactly the charged set: nonrecursive `xmodel/*.md`, plus
+top-level `AUDIT.md`, `notes.md`, and `APPROACHES.md`.  The report being sealed
+is excluded by resolved identity.  A missing required file, symlinked corpus
+entry, unreadable/non-UTF-8 file, escaping path, invalid context window, or
+candidate fan-out above the declared cap returns exit 2 with an explicit
+`status: ERROR` block.
+
+### 1.2 Candidate semantics and output
+
+The search is lexical and candidate-only.  It requires the identifier's key
+nouns in a local context window and bounded-quantity terms on the
+relation-bearing line.  It recognizes ASCII, Unicode, and common LaTeX
+inequalities.  A relation line is still eligible when it carries an older
+`OPEN[...]` label; that label is useful provenance, not evidence against a
+collision.  Results are sorted deterministically and rendered as portable
+repository-relative `file:line` locations.  A candidate does not close,
+answer, or promote anything.
+
+Every successful invocation emits exactly one Markdown section headed
+`## COLLISIONS`.  With candidates it reports `status: CANDIDATES`; with no
+candidate or no declared raised item it reports `status: EMPTY` and an
+explicit `NONE` row.  The section is therefore never silently absent.  Errors
+also use the same heading with `status: ERROR`, while the nonzero exit blocks a
+seal wrapper.
+
+### 1.3 Required controls
+
+The retroactive fixture preserves the relevant source text at the exact
+1-based positions from
+`xmodel/companion-curve-alln-opus5-20260902.md`.  Feeding the bounded quantity
+for `OPEN[DEG-AF-VS-N]` produces, in order:
+
+```text
+xmodel/companion-curve-alln-opus5-20260902.md:685
+xmodel/companion-curve-alln-opus5-20260902.md:785
+xmodel/companion-curve-alln-opus5-20260902.md:836
+```
+
+The negative fixture declares a synthetic quantity with no banked match and
+asserts an explicit `status: EMPTY` block and per-token `NONE`.  Additional
+regressions cover same-line multiple tokens, malformed syntax, missing corpus
+files, prose false activation, identifier/quantity confusion, bounded fan-out,
+older labels on candidate lines, bold/heading entries, and LaTeX inequalities.
+
+### 1.4 Seal-time integration note
+
+Immediately before the standalone `BODY-END` marker is written, a future
+`lane.sh` seal phase (or a pre-step called by `seal.py`) would run
+`python3 ops/open_collision.py "$report" --root "$repo"`; any nonzero status
+aborts sealing, while exit 0 supplies the report's single `## COLLISIONS`
+block, which is inserted before `BODY-END` and only then hashed and stamped.
+The wrapper must reject a second block rather than append twice.  This lane
+describes that call boundary only and makes no change to either live tool.
+
+## 2. Typed deliverable B -- encoding-faithfulness PREFLIGHT
+
+```text
+deliverable       = N_GE_6_REALIZATION_PREFLIGHT
+implementation    = box/preflight.py
+focused_tests     = ops/test_preflight.py
+status             = PASS
+tests_run          = python3 -m unittest -v ops/test_preflight.py
+pass_fail_skip     = 16 passed / 0 failed / 0 errors / 0 skipped
+optimized_run      = 16 passed / 0 failed under python3 -O
+runtime            = 2.14 s focused; every individual test < 10 s
+builtin_canaries   = 3 passed / 0 failed
+integration_note   = Section 2.6
+```
+
+### 2.1 Strict generator/solver contract
+
+The gate consumes a strict JSON manifest and the exact `.ms` file intended for
+launch:
+
+```text
+python3 box/preflight.py job.preflight.json job.ms
+```
+
+Unknown, missing, extra, duplicate, or wrongly typed JSON fields fail.  The
+manifest declares `N`, corrected cell, coefficient field, monomial order,
+ordered ring generators, explicit source order and generator images, the
+entire reduced approximate root, ordered ideal rows, the open factor and
+inverse, source-fidelity data, two-field output type, transition evidence, the
+pinned stack, and `solver_input_sha256`.  `N < 6`, any unknown cell family, a
+non-`QQ` field, or a non-`GRevLex` order is denied.  The bounded native cell set
+is `86A`, `86B`, `86C`, `86D`, `96A`, and `96B`; a new family requires an
+explicit schema extension and otherwise fails closed.
+
+The second CLI path is mandatory.  Its bytes must match the manifest SHA-256,
+be NUL-free UTF-8 POSIX text, and end in LF.  The gate parses the actual
+variable line, characteristic-zero line, comma framing, generator count, every
+ordered closed row, and the final Rabinowitsch row, then compares the parsed
+polynomials to the checked manifest.  Thus a correct sidecar cannot approve a
+stale or different ideal, even if that stale file's digest is copied into a
+new manifest.
+
+### 2.2 Exact root, coefficient, ring-map, and open checks
+
+The corrected expansions are recomputed exactly over `Fraction` arithmetic by
+reusing `box/qq_oracle_jobs.py`: the audit's `G86` with the target-dependent
+auxiliary terms and `G96` with `a15*p*q + a12*q^2 + a9*p + a6*q`.  The emitted
+root must contain exactly the recomputed nonzero coefficient sequence in
+descending degree.  Every encoded closed condition is parsed as a polynomial
+and exact-diffed against both that emitted coefficient and the independently
+recomputed coefficient; rendered-string equality is not trusted.
+
+The declared source ring order is fixed for the chosen cell.  Map images are
+parsed in the target ring and must form an explicit permutation isomorphism of
+the declared generators.  The manifest and actual solver variable orders must
+agree.  Exactly one open is permitted: the corrected `g_c` factor must be
+nonzero, its inverse must be the mapped `u`, and the actual final solver row
+must be polynomially equal to `u*g_c-1`.  No `sat()` wrapper is used.
+
+### 2.3 Fixed semantic and source-fidelity canaries
+
+The built-in controls are computations, not self-reported booleans:
+
+1. The charged old-A point is first checked to satisfy the old 86A closed rows
+   and old open.  Corrected top cancellation then forces the degree-19
+   residual `g19=11`, so corrected 86A rejects it.
+2. For `q=t^6+8*t^2`, `p=t^9+12*t^5+24*t`, and
+   `(a15,a12,a9,a6)=(0,0,0,-64)`, corrected 96B recomputes every closed
+   coefficient `g16,...,g3` as zero and the open coefficient `g2=64`.
+3. Source fidelity uses the canonical nonvacuous tuple
+   `(oldEQ2,q,E1)` and exact identity `EQ2_even=oldEQ2-2*q*E1`.
+   Renamed/zero aliases and the stale expression omitting `-2*q*E1` fail.
+
+These are encoding regression controls only.  Their execution here makes no
+new statement about curves, maps, attainment, or emptiness.
+
+### 2.4 Orthogonal output typing
+
+The accepted artifact field is exactly:
+
+```text
+NUMERICAL_PROFILE | FORMAL_EO_SERIES | SUBSYSTEM_POINT | FULL_EO_POINT |
+CURVE | REPRESENTATION | FULL_COVER | POLYNOMIAL_PAIR
+```
+
+The accepted attainment field is independently:
+
+```text
+NECESSARY | REALIZED | ADMISSIBLE_PULLBACK | ACTUAL_MAP |
+COUNTEREXAMPLE_CERTIFIED
+```
+
+Impossible pairings, unknown values, and advanced outputs without a checked
+provenance path are rejected.  The fields are emitted separately in the JSON
+result; no lossy single verdict is synthesized.
+
+### 2.5 Checked transition ladder
+
+Advanced A2 outputs must follow the complete, contiguous global path below;
+self-loops, late starts, unknown domains/scopes/claims, missing arrows, extra
+arrows, and noncontiguous states all fail.
+
+| From | To | Exact required arrows |
+|---|---|---|
+| `SUBSYSTEM_POINT/REALIZED` | `FULL_EO_POINT/REALIZED` | `FULL_EO_EQUATIONS`, `DECLARED_QUOTIENT` |
+| `FULL_EO_POINT/REALIZED` | `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `EO_ALL_OPENS`, `BOUNDARY_DATA`, `GENERAL_FIBRE_DATA`, `FIELD_DEGREE_FOUR` |
+| `FULL_EO_POINT/ADMISSIBLE_PULLBACK` | `POLYNOMIAL_PAIR/ACTUAL_MAP` | `SOURCE_CHART` |
+| `POLYNOMIAL_PAIR/ACTUAL_MAP` | `POLYNOMIAL_PAIR/COUNTEREXAMPLE_CERTIFIED` | `NONINVERTIBILITY`, `GEOMETRIC_DEGREE` |
+
+The table explicitly rejects an A2-to-B3 kill, local-to-global torsion, and a
+full-E/O point promoted to an actual map without the complete intermediate
+state and every declared arrow.
+
+### 2.6 Launch integration note
+
+A realization generator writes the strict manifest and `.ms` payload from the
+same ordered coefficient data, records the payload SHA-256 in the manifest,
+and the launch wrapper invokes `python3 box/preflight.py manifest.json job.ms`
+before starting qqideal/msolve; only exit 0 permits launch, the emitted JSON is
+retained as custody evidence, and the wrapper launches those same hash-pinned
+bytes (or rechecks the digest immediately before exec).  This lane implements
+the gate but does not alter any currently running launcher.
+
+## 3. Verification ledger
+
+Final local commands and results:
+
+```text
+python3 -m py_compile ops/open_collision.py ops/test_open_collision.py \
+    box/preflight.py ops/test_preflight.py
+PASS
+
+python3 -m unittest -v ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.589s -- OK
+
+python3 -O -m unittest -q ops/test_open_collision.py ops/test_preflight.py
+Ran 27 tests in 2.605s -- OK
+
+python3 box/preflight.py --self-test
+3/3 exact canaries PASS
+```
+
+All six reference manifests (`86A/B/C/D`, `96A/B`) also pass with their
+canonical in-memory solver payloads and an injected matching stack observation.
+No unit test invokes `msolve` or imports `qqideal`/`msolveio`.  The local live
+stack probe found `msolve=0.10.1`, while `qqideal` and `msolveio` are absent;
+therefore a real launch on this host is correctly refused until the pinned
+`qqideal=0.2.0` and `msolveio=0.2.1` packages are present.
+
+## 4. Guardrail and disposition
+
+This is an implementation lane.  Collision hits are candidates for human
+review, never closures.  Solver preflight success means only that the encoded
+job, controls, stack, solver bytes, and declared type transitions satisfy this
+contract; it is not a solver result or a mathematical realization.  The
+variable/ring map is explicit, the open is Rabinowitsch, raw remainder slots
+are exact-diffed in the declared ring/order, and attainment cannot be inferred
+from a weaker carrier.  No exit-price assertion is made, so no `charge_basis`
+line is emitted.
+
+## COLLISIONS
+
+status: EMPTY
+
+- NONE — the report contains no explicitly raised `OPEN[...]` entries.
+
+<!-- BODY-END -->

tokens used
310,344
Implemented and sealed successfully.

- [OPEN collision hook](/Users/dc/code/math/jc2/ops/open_collision.py) with [11 focused tests](/Users/dc/code/math/jc2/ops/test_open_collision.py), including exact lines 685/785/836 and explicit empty output.
- [PREFLIGHT hard gate](/Users/dc/code/math/jc2/box/preflight.py) with [16 focused tests](/Users/dc/code/math/jc2/ops/test_preflight.py), exact canaries, transition enforcement, and solver-input binding.
- [Sealed report](/Users/dc/code/math/jc2/xmodel/systems-collision-preflight-sol56-20260902.md).

Verification: 27/27 tests passed normally and under `python -O`; `py_compile` passed; 3/3 built-in canaries passed. Seal SHA-256: `3325b7a9c1b4d456c0722854641f4d316b385d84cb21692147e8a0dd41dd693b`.

The local host has `msolve 0.10.1`; absent `qqideal`/`msolveio` packages correctly cause real launches to fail closed. No prohibited files were modified.
DIVERT path=xmodel/systems-collision-preflight-sol56-20260902.md status=CLEAN_SEALED body_bytes=13152 body_sha256=3325b7a9c1b4d456c0722854641f4d316b385d84cb21692147e8a0dd41dd693b
