# D125 parity pilot readiness — preparation only

2026-09-07. **Prospective registration, engineering-only host control, authority schema notes and custody plan are ready. Nothing was launched.** The frozen producer/root caller files remain unchanged; their independent review is still a prerequisite. No live gate artifact, full source input, AWS/SSH/CAS, worker control or production expansion was accessed.

## Frozen experiment

`box/d125-parity-pilot-readiness-20260907/REGISTRATION.md` registers only the original-lambda3 unequal/Q22-coordinate μ2 slice: one shared300-second construction and literal replay of all803 original equations. It neither authorizes a solve nor changes the source, gauges or field. No retry, expanded budget, normalized moving-face variant or lambda3=1 is permitted.

The exact root-owned caller is `box/d125-parity-compression-pilot-prep-20260907/run_once.py`, SHA `d164af0b576fd7d8f7e3c3a294da8211df996595677aeac75267d2bf9546d40f`. I read it and its tiny tests completely. It creates no descendants, invokes the frozen constructor and literal803-row replay in one process, checks the shared deadline and reserves an exclusive replay receipt. The accepted CAPRUN file, SHA `4435279df8f987c667ebc2fae8e4bd987916032561cb3faf1e07a57789d196c2`, was read fully and remains unchanged.

Registration includes exact future production argv, code/source/gate pins, paths, launch identity and terminal criteria. Resources are300 wall/CPU seconds,4GiB AS and sampled exact-group RSS,128MiB per regular file,100,000 terms per polynomial,1,000,000 multiplication pairs/retained terms,4,096 coefficient bits. The root caller does not reset the absolute budget for replay.

The prospective `prlimit` prefix supplies FSIZE128MiB/core0 before Python; frozen authorize reapplies128MiB, **not64MiB**. CAPRUN does not meter output bytes. The internal artifact limit is not an aggregate log/disk quota. The registration therefore preserves separate file bounds and requires adequate EBS space plus safety margin. The existence/version/hash of prlimit and Python must be checked on future readiness; no package install is implied.

## Host control prepared, not executed on AWS

`host_control.py` checks exact Linux/Amazon EC2 instance, boot, cwd, mathematical gate, registration and frozen code pins. Its authority schema and path are engineering-only and cannot be consumed by `run_once`. It records hostname, PID/PGID/start_ticks/cgroup/PID namespace before the payload and records post-authorization limits.

The authorize control calls **the real frozen `construct.authorize`**, then only `(1+x)(1-x)=1-x²` with production arithmetic false. It never calls `main`, `reconstruct`, a replay or a source reader. A separate rss mode reuses the terminal reviewed cleanup mechanism: parent exits, its same-PGID descendant ignores TERM and allocates96MiB, and unchanged CAPRUN must observe its64MiB RSS limit, TERM→KILL, leader reaping and group emptiness. That future fixture has10-second wall/CPU and512MiB AS caps. The two controls are bounded by30 seconds overall and fail closed without retry. **No fork/RSS fixture ran locally.**

The source mechanism and its terminal old-boot telemetry are pinned from `box/d125-defect-order-engineering-20260907`; those receipts do not count as fresh-host success. Any restarted host requires new root engineering GREEN and a fresh actual control receipt.

## Local evidence and remaining approvals

Six tiny test methods pass normally and under `-O`, final combined0.507 seconds, under30-second wall/24-second CPU/512MiB AS limits. They execute the actual comparison code against changed boot, mathematical gate, registration, code, caps, missing authority and wrong host. A positive control calls actual frozen authorize with only host/OS reads and limit setters mocked, while production entry points are instrumented to raise. Wrong boot and missing root authority are also rejected through that real authorize function. Zero Assert syntax nodes occur in the host script. These are declared mocks, not fabricated actual-host receipts.

The registration remains contingent on root accepting the independent code gate, STARTing only retained `i-0da0cebfc97c9fd54`, validating current Owner/EBS/boot/mount/resources and absence of unexpected live owners, then authorizing engineering. Only a later explicit construction GREEN may dispatch the one pilot. Root retains instance STOP authority; no termination or EBS deletion is ever permitted. The worker was root-reported STOPPED and was not queried here.

Harvest must be receipt-first after terminal: normal child0, empty stderr, complete construction footer and803-row replay receipt, unchanged input/code pins, exact output hashes, reaped leader and fresh absence of all registered group members. A footer without successful replay/terminal custody is insufficient. Cap/failure/partial output means incomplete, not an ideal result. All artifacts stay on EBS; source copies are never overwritten.

Primary owned pins:

- REGISTRATION.md: `9435f425ddd135846480dd1cdb08674de3b376803a1f73e93804a546c5b5d107`.
- host_control.py: `f91a2c250689330a6d8155a5704ca1b703179cb342df686d683442c858798860`.
- test-results-seal.json: `5af841d516b6a9d0ff36cc0b53f81f684b526df0871a026cc927ae390b959486`.

All16 source/code/registration/control evidence pins are in the owned `pins.json`; exact pending steps and no-action custody are in `custody.json`. The final mock command is `python3 box/d125-parity-pilot-readiness-20260907/run_tests.py fresh-evidence-name.json` (exclusive new evidence path). All local children returned/reaped. **All writers idle on transactional publication; no compute authority or mathematical promotion follows.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5752`.
- Body SHA-256:
  `c1f50aef0cb4dd18bb2aedd9891398656b916e3bd6bae8f3d481d2279b3f7b5d`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
