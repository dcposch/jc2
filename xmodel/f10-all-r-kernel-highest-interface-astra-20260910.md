# All-r kernel presentation and highest-block interface

Status: NEW PRESENTATION AND CONDITIONAL HIGHEST-BLOCK INTERFACE, UNREVIEWED. Uniform highest regularity is NOT proved. The sole provisional late-arrow dependency at invitation was subsequently discharged by ROOT's lifecycle attestation (section 7). No source-zero or computation claim.

First action 2026-09-10 22:09:03 UTC; own report, manifest and box absent. Original publication reserve 22:28 UTC; HARD stop 22:31 UTC, never reset. Owner /root/contact_collision_geometry; basis 0d39df3c9fd69c939a8420c54d03228b9077777d.

## 1. Charged scope and result

Fix r>=2, m=3r+1, n=5r+2, q=2r+1 and d=r+1. Import only the card and its seven pinned reports, at the card's accepted scopes. Put B=B_nu, nu=n/m; B is the WHOLE finite-etale rank-seven Q-algebra, not a selected field. Its leading algebra is B[s,s^-1]. The accepted middle H7-unit premise is supplied by the card; the late structural arrow was conditional at invitation, with its later ROOT-attested acceptance recorded in section 7. Historical provisional headers do not replace these assigned qualifications.

RESULT. Under the stipulated band premises, the complete source has a post-band presentation with exactly d kernel generators over B[s,s^-1]. The faithful scale slice has d generators over B, d distinguished compatibility slots of weights q,...,m, another 7r positive-index slots, one mixed low slot, and the indispensable localization at L0. Thus there are 8r+2 equation slots before localization, or 8r+3 after adjoining an inverse variable. Slots need not be nonzero or independent. At r=2 this gives three generators and eighteen equations plus guard, but the derivation below does not use the r2 benchmark.

The weighted homogeneous highest parts H_q,...,H_m are well-defined. Their simultaneous projective emptiness is an ADDITIONAL unproved condition, not a consequence of column unitness. If regular on every B component, they give length binomial(3r+1,r+1) over B and top filtered weight 2r(r+1). A concrete necessary scalar test on the last-kernel axis and a proved model comparison are in sections 5--6.

## 2. Literal band composition and row inventory

Use the normalized free-z polynomial envelope of the accepted scale report:

    Ahat=S*tau^3+(S*Dpar-U)*tau^2
          +(z-U*Dpar+S*Vpar)*tau+Kpar,
    Pihat=z*tau-U*tau^2+S*tau^3,
    [Ahat,Bhat]=s^7+U*s^5*tau-E*tau*Pihat-tau*Pihat^2.

The lower normalized coefficients are d_i (0<=i<r), v_i (0<=i<2r), k_i (1<=i<=3r), U,E. Their weights are r-i,2r-i,m-i,d,m; wt(z)=q. The leading coefficients are in B, and z=s^2 only when reading back the source. All five Euler reconstructions, both automatic resonances, fixed [S]B3=B0(0)=0 gauges and every target term remain as in the inputs.

For gap h set the paired slots to K1_(6r+2-h),K0_(7r+2-h). The following is an exhaustive coefficient/row map, not a subtraction-only count:

| Gap | Actual normalized variables consumed | Replacement | Paired-slot outcome |
| --- | --- | --- | --- |
| 1<=h<=r | d_(r-h),v_(2r-h),k_(m-h) | Y_h | both solved |
| h=d=r+1 | U,v_(r-1),k_(2r) | Y_d | both solved by modified target inverse |
| r+2<=h<=2r | v_(2r-h),k_(m-h) | none | both solved using the accepted H7_h unit |
| h=q=2r+1 | k_r | none | retain Psi_q |
| h=m-j, j=r-1,...,1 | k_j | none | retain Psi_h |
| h=m=3r+1 | E | none | retain Psi_m |

The first rows use the accepted homogeneous-rho convention, subtracting ma*V_part(0) from the whole constant. Every W_h uses all earlier actual bands, including their particular solutions. At h=d the actual coefficient is q2=-U and the modified theta6 target is retained; q2 is not a new independent coefficient. In the middle, l_h=v_(2r-h)-U*d_(2r+1-h) and its inverse shift are retained. H7_h*Y_h+J_h is solved as Y_h=-J_h/H7_h, not discarded as a homogeneous equation.

The critical step uses its actual y=z-U*d0 and complete mixed forcing. Its fixed rational pivots m,2m,3m and specified left inverse eliminate k_r with one compatibility. The late steps are ordered j=r-1 downward to 1: their forcing uses only preceding gaps, and their fixed pivots delta2=-(3j+1),delta1=delta2-m,delta0=delta2-2m are units. At j=r-1 the fixed B3(0)=U^2 and target -U^2*tau^5 remain in the particular forcing. Ell enters later than all these rows; its accepted column eliminates E last. No k_0 gauge, source-U inversion or new mate freedom is introduced.

For every one-column step, with actual pair b1+g1*x,b0+g0*x and a leading-ring Bezout identity lambda1*g1+lambda0*g0=1, the literal replacement is

    x=-(lambda1*b1+lambda0*b0),
    Psi=g1*b0-g0*b1=0.

The determinant-one row map carries the pair to (x+lambda1*b1+lambda0*b0,Psi). Its inverse gives the original residual pair as (-lambda0*Psi,lambda1*Psi) after substitution. Thus the two quotient maps are polynomial, valid with nilpotents, and every outside row receives the same substitution. They do NOT assert that arbitrary forcing lies in the column image.

The table consumes every lower d,v,k,U,E coefficient, leaving precisely Y_1,...,Y_d. Specifically middle consumes v_(r-2),...,v0 and k_(2r-1),...,k_(r+1); then critical/late consume k_r,...,k1. The d kernel coordinates are unrestricted only before the retained compatibilities are imposed.

The original two top slots already define B. The table consumes exactly paired gaps 1,...,m, replacing gaps q,...,m by d compatibilities. The untouched positive-index slots are EXACTLY

    K1_i, 1<=i<=3r;       K0_i, 1<=i<=4r.

Both index-zero rows remain. No redundant-looking row is removed, and the two inverse-polynomiality conditions and original top guard are carried by the accepted reconstruction and leading base.

## 3. Homogeneous descent, actual scale and the faithful z=1 cover

Normalize before performing the table. The normalized leading polynomials C,D have coefficients in B and still obey mCD'-nC'D=-theta^7. Hence every homogeneous column, every early completed inverse and every middle H7 coefficient lies in B. The unit statements over B[s,s^-1] descend to B: an identity 1=sum g_i*lambda_i(s) with g_i in B yields a Bezout identity by taking Laurent constant coefficients. For a single element this gives its inverse in B. Thus choose all one-column Bezout coefficients in B, of weight zero; do not choose scale-dependent representatives that destroy the grading.

Give Y_h weight h. Inductively each band map is homogeneous: its upper pivots and leading inverse entries have weight zero, W_h has weight h, and its actual earlier target terms have the same weight. At the modified step U has weight d; at the critical step z and U*d0 both have weight q; at the first late boundary U^2 has weight 2d; E has weight m. Middle inverses are leading B units. Every reconstructed coefficient therefore has its original assigned weight as a polynomial in B[Y_1,...,Y_d,z]. The rho coordinate uses normalized leading data and has exactly its gap weight; it is not an unrescaled original rho assigned a convenient new degree.

These free-z identities are justified, not an extra source specialization. Under z=s^2 the normalized equations are the invertible change of the actual source. The map B[Y,z] -> B[Y,s,s^-1], z -> s^2, is injective, even with nilpotents, by distinct Laurent powers. Thus the polynomial identities and their inverse identities hold in the free-z envelope. No inversion except already-proved leading units occurs. This establishes the commutation with scale normalization which the uneliminated scale report itself did not assume.

Write P1_i(Y,z),P0_i(Y,z) for the COMPLETE original K rows after every substitution, and U_*(Y,z) for recovered U. Their weights are 6r+2-i,7r+2-i,d. The compatibility Psi_h(Y,z) has weight h, for q<=h<=m. In particular U_* has no z term because d<q. The actual post-band ring is

    B[s,s^-1,Y]/(
      Psi_h(Y,s^2), q<=h<=m;
      P1_i(Y,s^2), 1<=i<=3r;
      P0_i(Y,s^2), 1<=i<=4r;
      P1_0(Y,s^2)-U_*(Y)*s^5;
      P0_0(Y,s^2)-s^7).

This is an exact quotient presentation of the normalized source, with the original field/gauge back-map still at its accepted scope. The normalized source is not obtained by setting s=1.

For the faithful cover, adjoin w^q=s and set Y_h=w^(2h)*X_h. Homogeneity gives z=s^2=w^(2q), so each weight-k polynomial is w^(2k) times its (X,1) slice. Define

    f_h=Psi_h(X,1), A1_i=P1_i(X,1), A0_i=P0_i(X,1),
    L1=P1_0(X,1), L0=P0_0(X,1), U0=U_*(X),
    Tlow=L1-U0*L0,
    C_r=B[X_1,...,X_d,L0^-1]/
      (f_q,...,f_m; A1_i (1..3r); A0_i (1..4r); Tlow).

The two low rows are exactly w^(12r+4)*(L1-U0*w^3) and w^(14r+4)*(L0-w^3). Their replacement retains Tlow and L0=w^3. Consequently

    source[w]/(w^q-s) ~= C_r[w]/(w^3-L0).

The maps are s=w^q,Y_h=w^(2h)X_h and X_h=w^(-2h)Y_h, with L0^-1=w^-3. The extensions are free of ranks q and three respectively and therefore faithfully flat over arbitrary bases. The source is zero iff C_r is zero. This is not a same-field point descent statement: root-taking may extend a coefficient field. The top guard reads back as omega=W*t5*w^(8q), and no L0=0 component is licensed. Both low rows, every target term, the full mate and all source guards are preserved.

The low mixed row is the z=1 slice of z*P1_0-U_*P0_0, of weight 8r+3, not a row of weight 6r+2. L0 has filtered weight at most 7r+2. Every weight-k slice has monomials of weighted degree at most k and congruent to k modulo q; thus the entire presentation retains its Z/q grading. These are support envelopes, not claims of nonzero coefficients or independent rows.

## 4. Highest block and the precise conditional finite interface

Since m<2q, the homogeneous compatibilities have the exact shape

    Psi_h(X,z)=H_h(X)+z*J_(h-q)(X), q<=h<=m,

where H_h=Psi_h(X,0) has weighted degree h (or is zero), and J_(h-q) is homogeneous of the indicated weight (or zero). Thus f_h=H_h+J_(h-q) is filtered of degree at most h, with highest FORM H_h, not an asserted nonzero highest term. In the critical row J_0 is a unit of B: the accepted critical zero-earlier-band control, after normalization, gives Psi_q(0,z)=c*z with c a leading unit. At X=0 the reconstructed earlier bands vanish; this is an exact control of the constant term, not a regularity proof.

Define REG_r to mean that H_q,...,H_m have no nonzero common point over the algebraic closure of ANY residue field of B, in weighted projective space of variable weights 1,...,d. Equivalently their homogeneous ideal has only the origin as affine support on every component. Because B is a finite product of characteristic-zero fields, its polynomial ring is componentwise Cohen--Macaulay; d homogeneous positive-weight equations have height d precisely in this origin-only case, and then form a regular sequence. This is a proof over all components, not a choice of one acceptable leading field.

Under REG_r the Hilbert series, rank and top weight are

    Hilb(t)= product_(h=q)^m (1-t^h) /
              product_(i=1)^d (1-t^i),
    D_r=product_(h=q)^m h / product_(i=1)^d i
       =(3r+1)!/((2r)!*(r+1)!)=binomial(3r+1,r+1),
    T_r=sum_(h=q)^m h-sum_(i=1)^d i=2r(r+1).

For the inhomogeneous quotient R=B[X]/(f_q,...,f_m), regularity gives the stronger strictness identity

    (f_q,...,f_m) intersect F_N
       =sum_(h=q)^m f_h*F_(N-h)

for every weighted filtration bound N. Indeed a representation with excessive largest product degree has a highest homogeneous syzygy among the H_h. The first Koszul syzygies generate that syzygy module for a regular sequence; subtracting the corresponding skew combinations of the f_h leaves the represented polynomial unchanged and lowers the largest product degree. Iterating terminates. This argument is componentwise over B, preserves all components, and does not assume a chosen basis or a computed Groebner basis.

Therefore gr R=B[X]/(H_q,...,H_m), R is finite free of rank D_r over B (Q-dimension 7D_r), and all monomials of weighted degree <=T_r span R. A basis can be chosen, but no common monomial basis or multiplication table is emitted. This statement is CONDITIONAL on REG_r; neither d equations nor their assigned weights establish it.

Let J be the ideal of R generated by the remaining 7r positive-index rows and Tlow. Then C_r=(R/J)[L0^-1]. Under REG_r the exact finite-algebra discriminator is

    C_r=0 iff L0^(D_r) belongs to J in R.

Proof: localization-zero is equivalent to nilpotence of L0 in R/J. On each B component the quotient dimension is at most D_r, so a nilpotent multiplication operator has nilpotence index at most D_r. This retains nilpotents and every row; membership of 1 would be a stronger sufficient test. No cofactor, basis, rank, membership outcome or source exclusion is claimed. Without REG_r this finite dimension/exponent argument is not licensed.

## 5. A concrete necessary actual test: the last-kernel axis

This is an actual forcing discriminator, not another column-rank assertion. Set X_1=...=X_(d-1)=0 and z=0 in the fixed accepted-rho coordinates. Every polynomial of weight not divisible by d vanishes on this axis. Among the d weights q=2d-1,...,m=3d-2 the ONLY multiple of d is 2d. Therefore

    H_h(0,...,0,X_d)=0 for h!=2d,
    H_(2d)(0,...,0,X_d)=c_r*X_d^2, c_r in B.

REG_r necessarily implies c_r is a UNIT of the whole B. If c_r vanishes in any residue field of B, that entire nonzero axis is a highest-form countercomponent on that leading component. This is neither an assertion that such a component exists nor a source point. Conversely c_r unit excludes only this axis, not all weighted projective points.

Here is a compact exact definition of c_r directly from the charged maps, preserving the often-dangerous first-late boundary. Let P(theta),Q(theta) be the modified gap-d homogeneous kernel pair with rho=1 and zero residual from the early report's section 7, formed using the NORMALIZED leading C,D in B. Thus

    deg P<=2, deg Q<=4,
    P2=e, Q4=2e,
    e=7[theta^7](C/a)^((m+n-d)/m)/(m+n-d).

These equations specify the actual completed inverse, not just any pair with those top coefficients. Its full coefficients are given by the charged finite Tbase/N_U/N_V formulas. On the axis the only earlier nonzero gap before 2d is this pair times X_d: all other intermediate weights are not multiples of d. In particular the critical variable, of weight 2d-1, specializes to zero when z=0.

At gap 2d the source variable is k_j with j=r-1=d-2, and beta=2r+1+j=3r. Define the actual self-bracket and fixed mate contribution

    W_axis=(m-d)*P*Q'-(n-d)*P'*Q,
    A_fixed=e^2*theta^3,
    N_axis=W_axis+m*C*A_fixed'-beta*C'*A_fixed
                         +e^2*theta^5.

The final summand is the moved actual target -U^2*theta^5, since U=-e*X_d. The fixed A_fixed is B3(0)=U^2 with X_d^2 factored out. The theta5 coefficient checks WITHOUT expansion of any lower coefficient:

    [theta5]W_axis=4(2m-n-d)e^2=-4e^2,
    [theta5](m*C*A_fixed'-beta*C'*A_fixed)=3e^2,
    [theta5]N_axis=(-4+3+1)e^2=0.

There is no higher coefficient. Solve uniquely for Z(theta) of degree <=2 so the theta4,theta3,theta2 coefficients of

    m*C*Z'-beta*C'*Z+N_axis

vanish, using delta2=-(3j+1),delta1=delta2-m,delta0=delta2-2m. Call the residual b1*theta+b0. Let (g1,g0) be the actual homogeneous k_j column from the same late upper reconstruction. Then the exact scalar is

    c_r=g1*b0-g0*b1.

Everything in this definition lies in B; only accepted leading units and fixed rational denominators occur. No expanded coefficient or unit decision has been emitted. This is the determinant compatibility of actual quadratic forcing, not a claim that g1 or g0 being nonzero controls c_r. Dropping A_fixed would change the unchecked theta5 coefficient to -3e^2; dropping the target would change it to -e^2. Those are changed equations, not the actual highest block. The formula remains valid if e is zero or nonunit.

This gives a genuinely smaller necessary discriminator before attempting the whole simultaneous locus: determine whether this ONE specified element c_r is a unit of B for every r. Its value/unitness is unknown here. Even success would leave the rest of REG_r unresolved; no computation or follow-on is authorized.

## 6. Regularity is new; a proved comparison model is not an identification

The accepted early completed inverses, middle units and critical/late/ell unit columns do NOT imply REG_r. Exact changed-forcing control: keep d free variables of weights 1,...,d and a unit column (1,0) for every high slot; give its second forcing X_1^h for h=q,...,m, adding z to the h=q forcing. Every column elimination is valid, every stated weight is retained, and the critical constant coefficient is a unit. Nevertheless all highest forms X_1^h have the nonzero locus X_1=0 in common. This is a changed system, not a counterexample to the actual equations. Moreover its z=1 high quotient is already zero: X_1^(q+1)-X_1*(X_1^q+1)=-X_1, then the first row gives 1. Thus highest failure does NOT imply infinite or nonzero actual inhomogeneous quotient.

ROOT proposed a comparison model during this task; it is not a charged theorem premise. Its regularity can be proved independently by hand. For

    c(T)=1+X_1*T+...+X_d*T^d,
    M_h=[T^h]c(T)^(q/d), q<=h<=q+d-1=m,

the weights agree with the actual block. Suppose all d consecutive coefficients vanish over an algebraic closure of a characteristic-zero field. From c*f'=(q/d)c'*f, where f=c^(q/d)=sum a_N*T^N, the exact recurrence is N*a_N=sum_(i=1)^min(d,N)(((q/d)+1)*i-N)*X_i*a_(N-i). It uses at most the preceding d coefficients and a nonzero integer as its forward pivot. Starting at N=q+d, it forces every later coefficient zero. Hence f is a polynomial of degree <q. But f^d=c^q, and gcd(d,q)=gcd(d,2d-1)=1. Multiplicity comparison at every root makes c a d-th power. If c is nonconstant then deg c>=d and deg f>=q, a contradiction. Thus c=1 and all X_i=0. The model is regular and has the Hilbert series in section 4.

What is NOT proved is an identification of the ACTUAL H_h with these M_h. A sufficient exact missing identity would be a graded B-polynomial automorphism of the kernel coordinates together with an invertible weight-preserving row change carrying the actual block to the model block. Matching weights and lengths, or the r2 example, gives no such identity. The axis check is already mandatory: the model's only surviving axis coefficient is

    M_(2d)=binomial(q/d,2)*X_d^2
           =((2d-1)(d-1)/(2d^2))*X_d^2,

a nonzero rational unit. A graded triangular identification would have to match the actual c_r up to units. The actual c_r has not been evaluated, so even that necessary comparison remains open. No uniform regularity, finite-model computation, all-source exclusion or JC2 conclusion is claimed.

## 7. Scope and documentary completion

Exactly the card and its seven listed reports were hash-pinned before report bodies. The card was itself hash-checked before its WHOLE read. All seven reports were read fresh WHOLE: Euler 1--135 and 136--251; early 1--165 and 166--322; middle 1--145 and 146--282; critical 1--135 and 136--260; ell 1--248; scale 1--123; late 1--187. An initial combined Euler/early output clipped; both were then re-read in the listed complete bounded intervals, so no missing text or silent WHOLE claim remains. No prior-turn WHOLE reuse. Historical references inside those reports were not followed.

The conditional late composition is the only provisional dependency; ROOT's card supplies the other accepted scopes, including middle units. No live FIRST body, actual certificate, coefficient/fixture/candidate payload, source code, mathematical subprocess of ANY size, network, AWS/SSH, process/Git/protected/corpus access, agents or shared edit occurred. All new mathematics is manual and all authored writes use apply_patch plus the existing documentary transaction. Previous involvement is disclosed: this author wrote several charged producers and the late conditional report, and is composing accepted interfaces rather than re-reviewing their foundations. ROOT alone owns independent intake/review/promotion.

Subsequent lifecycle attestation ONLY: ROOT reported at 22:18 UTC that the late-composition FIRST passed A--F and that ROOT accepted its whole-leading unit/forced-compatibility scope after independent terminal custody. Thus the sole provisional parent was discharged during this task. No new gate or receipt was read, charged or used to add mathematics; exactly the original eight input files remain. The conditional derivation records its original status, and this NEW composition and REG_r claim receive no automatic promotion. REG_r remains unproved.

## OPEN(S) RAISED

No new canonical task ID or execution authority. REG_r is the precise unproved simultaneous highest-kernel condition. Its smallest concrete necessary test here is unitness of the explicitly defined first-late quadratic-forcing scalar c_r on the whole B. The model-conjugacy identity is a sufficient uniform candidate, not an established arrow. Full source zero additionally requires all retained rows and the L0 localization even if REG_r holds.

## COLLISIONS

status: EMPTY

Own-only scope; no corpus-wide novelty claim. The old band maps, middle units, late unit arrow and scale covers are credited; the new content is their homogeneous complete composition, exact retained inventory, conditional highest interface and necessary actual last-axis forcing test. Own fresh WHOLE draft/PINS review, all eight unchanged current pins, and own-only OPEN/collision checks completed by 2026-09-10 22:20:40 UTC. The final lifecycle/header clarification and this completion line were separately re-read before the sole final marker. No further mathematical exploration followed.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `21761`.
- Body SHA-256:
  `b1289ec8c4b2ced1980a126f7a08f7d536b79ae39497f25f1a75168f63105043`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
