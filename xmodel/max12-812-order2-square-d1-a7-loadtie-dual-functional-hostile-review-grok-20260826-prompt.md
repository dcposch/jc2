# Hostile review charge: D1 `a=7` load-tie dual-functional wall

You are the independent hostile different-model reviewer.  Work read-only
except for the one required report.  Do not trust producer PASS markers,
validator strings, or finite-field agreement as mathematical authority.
Rehash custody and rederive the claim from the frozen seven-row source.
Do not run heavy CAS locally; hashing and small hand/symbolic checks are
allowed.  Do not mutate any producer, prompt, ledger, or unrelated file.

Repository: `/Users/dc/code/math/jc2`.

Required report path (write exactly this file and no other file):

```text
xmodel/max12-812-order2-square-d1-a7-loadtie-dual-functional-hostile-review-grok-20260826.md
```

Primary frozen custody:

```text
69048f6fd2f3ddf78e8328840d0400fe29d523c5258fbd5c154b211559c433aa
  cases/max12_812_order2_square_owner_d1_a7_loadtie_dual_functional_20260826/RESULT.md
7c1d9f1bf8ff636c21a5be7736652b46bb24c6bfa2cb42149714d42be121de65
  cases/max12_812_order2_square_owner_d1_a7_loadtie_dual_functional_20260826/AWS_LAUNCH_METADATA.md
7c3e059fc48cde62bbfcca4378e682d3c919071961e7db443061b6ca82fb48b8
  cases/max12_812_order2_square_owner_d1_a7_loadtie_dual_functional_20260826/FAILED_ATTEMPTS.md
478bee84248b6ee9ccca1a033093a30f5fafdbe091a76b5a0ad8ef89ab0c28f8
  cases/max12_812_order2_square_owner_d1_a7_loadtie_dual_functional_20260826/EVIDENCE.sha256
b22a61508cde775693355fbfc4beda71950382c6561ba8ea5ef92a4b0f6b2d0d
  cases/max12_812_order2_square_owner_d1_a7_loadtie_dual_functional_20260826/FREEZE.sha256
bb662b3d25cd12dec712dc2763e1efd6dd09b1dd084babce65bcb9effb0daac0
  cases/max12_812_order2_square_owner_d1_a7_loadtie_dual_functional_20260826/PRODUCER_FREEZE.sha256
d77145b5984601e4887f9597f7867e4a1a80929b2aa35607b1447feee61d1149
  cases/max12_812_order2_square_owner_d1_a7_loadtie_dual_functional_20260826/compile_a7_loadtie.py
```

Charge every item below.

1. Rehash every row in `FREEZE.sha256`, `EVIDENCE.sha256`, and
   `PRODUCER_FREEZE.sha256`.  Distinguish exact Q, the characteristic-zero
   endpoint, from the independently compiled `F_65521` and `F_65519`
   screens.  Check engine rc, compiler/Singular diagnostics, source archive,
   resource metadata, and zero swap.  Check that the recorded failed
   preflight was only the stated parenthesization bug and supplied no
   imported verdict.
2. Independently enumerate every source primitive through `T=28` for
   `(a,d,r)=(7,2,>=7)` and through `T=30` for `(7,3,>=7)`.  Confirm counts
   five and seven, global pole ceiling two, and sole pole-two primitive
   `(3/8)C^2/L^2` at `T`.  Identify the earliest missing primitive if the
   inventories are incomplete.
3. Reconstruct all seven literal Faber source rows, not merely the compact
   analytic emitter.  Check delayed `k10,k6,k2` loads, moving `p(sigma)`,
   all mechanically required normal/load jets, row-2 `mu20` target through
   grade 30, and the target-free status of rows 1,3,4.  Verify the compact
   and literal sources agree modulo the registered ceiling and audit every
   sigma quotient.
4. Rederive the first-wall collision.  At grades 26 and 27 respectively,
   verify that the complete tied column is exactly
   `(3/4)C(A+k60)/L`.  Writing `A+k60=uz+v` and `C=xz+y`, verify
   `r_z=uy+vx`, `r_0=vy-(p/2)ux`, determinant
   `Delta_C=y^2+(p/2)x^2`, and the asserted rank-two implication on
   `D(Delta_C)`.  Inspect the rank-one face rather than silently dividing
   by `Delta_C`.
5. Independently derive the pole-two reconstruction
   `N=h1*z^3+h2*z^2+(h3+p*h1)z+(h4+p*h2)` and both moving-root Faber
   functionals
   `Phi4 +/- lambda*(Phi3+(p/4)Phi1)=N(+/-lambda)` for
   `lambda(sigma)^2=-p(sigma)/2`.  Check signs, moving-root corrections,
   and the full pole-two recurrence.
6. Verify directly that all pole-one columns pair to zero through `T`, and
   that the first nonzero target pair is exactly
   `q_+=(3/8)(c1*lambda+c0)^2`,
   `q_-=(3/8)(-c1*lambda+c0)^2`.  Recompute the two unit-ideal assertions
   on `D(lambda*c1*k0)` and `D(lambda*c0*k0)` before radicals.  Decide
   whether these charts literally cover exact `ord(C)=7+d`, including the
   rank-one `Delta_C=0` chamber.
7. Audit the finite-etale descent on `D(p)` and the closed
   `ord(R)>=7` tail language: make sure no leading `R` coefficient was
   inverted and no equality/positive-order-load face was imported.
8. Enforce the firewall.  A confirmed verdict closes only the two named
   strict `a=7,d=2,3` cells after cited upstream gates on `D(p*k0)`.
   There is no claim for `a=8,9`, target-shadow walls, another D1 face,
   the whole square component, maximum twelve, or JC2.

Classify every issue as a mathematical defect, custody/software defect, or
scope wording defect.  State the smallest failing identity and earliest
missing source monomial if any.  End with exactly one verdict word on its
own line: `CONFIRMED`, `REFUTED`, or `INCONCLUSIVE`.
