# Hostile review — genuine `Q8` non-parity formal branch

Act as a hostile different-model mathematical reviewer in
`/Users/dc/code/math/jc2`. Read in full:

- `xmodel/max12-912-order3-nu-q8-formal-branch-20260824.md`;
- every file in `cases/max12_912_order3_nu_q8_formal_branch_20260824/`;
- the corrected `Q8` erratum report and case;
- the original coefficient compiler and the reviewed parity-genus-five pair;
- the quarantined `Q12` branch and its refutation only as negative controls.

Charged hashes:

```text
report    37ce842eece0e79f0768bdfd492be70905bb83ba62af93965b4c4a2a3d6f03a3
manifest  d4b54c5a3e93ee9cc696017b9e87b6e0deb7b29253239e1df5f1e87fac063a31
freeze    0047d3482a26a10daacb032aa08e6da888552274c09f1d85083cdf5ae40d9ebb
```

Independently reconstruct and attack:

1. all eight source tails and the exact seven imposed fibre rows;
2. the corrected `1+3v` chart and absence of any `Q12` descendant input;
3. both claimed rank-three minors at every `Q8` root and all localization
   factors needed to call them units;
4. `gcd(Q8,Q8')=gcd(Q8,num(R6'))=1` and existence of loaded points;
5. the equivariant elimination: which six variables are removed, which odd
   equation remains, and why it has the form `t*Phi(s,t^2)`;
6. the Schur-complement relation `Phi(s,0)=unit*det(J_N)`;
7. why the loaded equation makes `p` a formal function of `v`, and why
   squarefreeness of `Q8` implies `dPhi/ds != 0`; and
8. reducedness, smoothness, distinct tangents, and exact scope of the two
   claimed formal branches.

Attack hidden denominators, confusion between the seven-row fibre and the
eighth terminal row, an unproved equivariant coordinate choice, misuse of a
minor that vanishes at some algebraic `Q8` root, and any implicit carryover
from the refuted `Q12` package. Run the replay, but use an independent exact
derivation for the verdict.

Write exactly
`xmodel/max12-912-order3-nu-q8-formal-branch-review-grok-20260824.md`.
Do not edit producer, case, canonical, coordination, prompt, log, run, or
other review files. End with exactly one verdict: `CONFIRMED`, `GAP`, or
`REFUTED`, identifying the smallest failing identity or hypothesis.
