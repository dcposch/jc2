# K=8, b=11, q1 single-run lane

Lane `k8-b11-q1-sol56-20260905`; evidence and drivers:
`box/k8-b11-q1-20260905/`.

## Verdict

**OPEN: neither cover is theorem-tier Q-certified (0/2).** The isolated msolve 0.10.1 run on a
characteristic-zero-header q1 input hit its watchdog without a basis after
9,003.334 seconds wall, with maximum RSS 129,556,692 KiB and empty-output
SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
A bounded exact-Q Singular lift produced no cofactor
identity before its watchdog.

This does **not** close `K=8,b=11`.  The frozen round-2 premise called q0 an
exact-Q unit after a lone msolve 0.10.1 `[1]`, but the superseding campaign
audit demotes that output: this engine can return its first machine-prime
unit before CRT/rational reconstruction while printing characteristic 0.
Repository-wide search found no independent rational q0 cofactor identity.
Thus q0 is historical `UNIT_SIGNAL_MODULAR_ONLY`; neither cover is currently
theorem-tier Q-certified.  The stratum and D=108 no-split arm remain
OPEN.  No exit price is asserted, so no `charge_basis` declaration appears.

See `COORDINATION.md:509-512`, `AUDIT.md:10111-10127`, and
`xmodel/grok-msolve-erratum.md:47-119`; their exact hashes are retained in
`custody/msolve-char0-caveat.txt`.  They change evidence typing, not the frozen
chart construction.

## 1. Frozen and chart-file custody

First, `awk` paired the receipt's numbered
`charged_input_<i>_basename`/`_sha256` fields into
`custody/charged-inputs.sha256`; `sha256sum -c` returned five `OK` results for
`/tmp/jc2-lane.hD1PiF/inputs` (`custody/charged-inputs.check.log`).  No digest
was retyped and no frozen content mismatched.

The original `.ms` was absent from both named producer directories and `/tmp`
(`custody/original-input-search.txt`), so this lane does not claim to recover
its inode.  Round 2's retained record, copied as
`custody/round2-K8_B11_Q1.json`, fixes:

```text
file/characteristic  K8_B11_Q1_p0.ms / 0
SHA-256              66bb78cfec123e11c5b9c780056b5949aa1c5c198caeb3c668aab1973d6697eb
bytes/vars/gens      65003245 / 97 / 330
body SHA-256         f6247690c1836e0fd7168f9c7c64caa1e6de4740ae35523382e5a6f21100a4a0
```

The chart was re-emitted once with
`r2_solve.py 8 11 --pin-index 1 --threads 64 --form-timeout 5400 --no-run`.
Source hashes are in `custody/driver-sources.sha256`.  Formation exited 0 in
28:06.38, maximum RSS 4,727,732 KiB.  The emitted Q-header file matches the
historical SHA and all counts byte for byte; the p=32003 companion matches
`bdcde96858f3a16c32f8bf9af405c64fbadcb743a1a67ee8f1f568e17fce7c23`.
This is byte-exact re-emission, not mere algebraic similarity.  Prelude
markers include `TARGET_FOUND 1`, `PRE__CST_ZERO 0`, `PRE__HOMOG_I 1`,
`PRE__HOMOG_CST 1`; the last two generators are `CSTP-1` and
`q1*q1_inv-1`.

## 2. Worker and isolated execution

One `r7i.16xlarge` was launched by the required command: instance
`i-06d4a0da61650ede5`, IP `172.30.0.97`, owner
`k8-b11-q1-sol56-20260905`, launch `2026-09-05T21:16:31Z`.
`fleet.sh wait` installed msolve 0.10.1; binary SHA-256:
`0436525b06fe83b1a6a00097a96d9bcafdc40d8de6315c724b9ada9a4c04ff5f`.

At `21:54:22Z` preflight rechecked the input tuple, found 64 processors, load
`0.01 0.43 0.69`, and zero CAS processes.  `dispatch.sh` launched the detached
job at `21:54:36Z`; its inner command was

```text
timeout --signal=TERM --kill-after=30s 9000s \
  msolve -g 2 -t 64 -v 2 --random-seed 0 \
  -f .../K8_B11_Q1_p0.ms -o .../exact_q.msout
```

This was a dedicated worker: no other job was dispatched during the run, and
every process poll observed the target msolve with no competing CAS.  Ten-minute process/F4 polls are in
`custody/exact-poll.log`.  By `22:46:34Z` it had run 51:58 and reached round
196, degree 7: linear algebra completed on a 4,024,993 by 10,777,112 matrix
(5,199 new, 23,204 zero rows), with RSS 58,259,704 KiB.  This exceeds the
prior contended attempts' recorded frontier but is progress, not a verdict.

## 3. Characteristic-zero-header result

```text
start/end UTC       2026-09-05T21:54:36.776827727Z / 2026-09-06T00:24:40.112665977Z
exit/watchdog       rc 124 / 9,000 s (watchdog timeout)
wall / max RSS      9,003.334 s / 129,556,692 KiB
reported basis      none; empty output
output bytes/SHA    0 / e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
max F4 degree       7
largest matrix      4,024,993 x 10,777,112, degree 7, round 196 (by entries)
```

The watchdog expired while round 679 had selected 42,152 of 194,322 pairs at
degree 7; that live round never printed matrix dimensions.  This is an engine
limit, not a nonunit, dimension, point, or necessary-chart survivor.

## 4. Exact rational q1 status

One direct Singular 4.3.2 `liftstd` attempt declared `Q[v0,...,v96]` in the
original 330-generator order, under a 2,700-second watchdog, 100-GiB VM ceiling,
and 2-GiB output ceiling.  It timed out with rc 124 after 45:01.24; peak RSS was
25,994,856 KiB.  Output contained only `CERT__NGEN 330` and the timeout halt,
with no unit, direct-check, cofactor, or certificate marker.  Therefore no
independent rational q1 identity completed, and an msolve `[1]`, if printed,
would remain unconfirmed over Q.  The 19-file lift manifest hashes to
`dc3dd44e1b839292040e6213c04355bcca1ae11c1da3577a8672fb9fb0a43dab`.

A solver-free feasibility audit verified several small localized consequences,
but a weight-15 span screen left a nonzero 688-term remainder for `q1^3` with
rank 2,418 at each of four primes.  This is only a screen; it explains why no
hand-sized cofactor was assumed.  The reproducible record is
`certificate-audit/static_q3_span_audit.json`.

The q0 audit searched 6,316 certificate-like paths and hashed all 532 retained
`.ms` files (1,608,720,229 bytes), while pruning every forbidden tree, and
found no rational cofactor identity
(`box/k8-b11-q1-20260905/certificate-audit/q0_certificate_search.stdout`,
SHA-256 `fbe58b25710f19cc6a8930ce20155d06f3d86d597193c1ff68774aece6cdac70`).  A q1 lift
cannot repair this independent missing cover certificate.

## 5. Sequential p=32003 screen

Only after the first job ended, the idle worker ran the p=32003 file; no jobs
overlapped.  It differs only on line 2 and has the expected SHA above.

```text
command       msolve -g 2 -t 64 -v 2 --random-seed 0 ...
start/end     2026-09-06T00:25:17.729910790Z / 00:26:02.889232598Z
wall / RSS    45.157 s / 3,214,652 KiB
result        rc 124, watchdog timeout, no basis
output SHA    e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 (0 bytes)
```

The shortened screen reached degree 7 (15,443 selected pairs); its largest
printed matrix was 70,368 by 252,667 at degree 6.  It was shortened solely to
preserve the 200-minute lane deadline after the exact job consumed its full
watchdog.  A finite-field result would be a screen only and never enter the Q
verdict; this timeout supplies no modular verdict.

## 6. FALLACY-v2 typing and scope

The row-dropping implication requires the K=7 gate's ring correction
(`xmodel/k7-strata-gate-sol56-20260905.md:17-29`):

```text
S = Q[h_ij, B_ij, active q, q_pin_inv],   T = S[lam],
I_light*T subset I_full in T.
```

No geometric coordinate is dropped; only the full construction introduces
`lam`.  A verified `1 in I_light` extends to `I_light*T=T`, then `I_full=T`;
the converse is unused.  This types round 2's row-only implication
(`xmodel/k4ray-strata-r2-opus5-20260905.md:235-242`).

The opens `{q0!=0}` and `{q0=0,q1!=0}` cover exact degree 11, but closure
requires independent theorem-tier Q certificates on both.  Neither cover is
theorem-tier Q-certified: q0 remains a first-prime signal and the q1 lift timed
out.  Timeouts and modular units are neither survivors nor closures.  No floor,
attainment, representative, or exit-price claim is introduced.

## 7. Artifacts and termination

The output root retains the input check and search, historical/re-emission
records, source hashes, preflight/dispatch/poll logs, each job's
custody/time/verbose/result/output hashes, the static and full-lift
`certificate-audit/` manifests, and
`custody/artifacts.sha256` (116 entries; SHA-256
`37c1a16755584cc240ad12492ae0162072ba0efa112d4f30c2775e70d44fe6b9`),
verified all `OK`.

After retrieval, `fleet.sh term` targeted only
`i-06d4a0da61650ede5`.  At `2026-09-06T00:27:23Z` its state was
`terminated` (`custody/worker-termination.log`); this preceded sealing.  No
other worker was touched.  Only after that terminated-state record was written
was the final artifact manifest hashed and checked; report sealing came last.
No ledger, `jc2-lean`, or `ideation-*` file was edited.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `8691`.
- Body SHA-256:
  `1017a6340bd56857037e0f23d6e6a0f48e48ceace43b08f22e94d6b317404df0`.
- Frozen basis: `d57a3d7de9f5d30534c3276602c7f868d05208f1`.
