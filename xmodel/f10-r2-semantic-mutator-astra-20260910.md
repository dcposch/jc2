# Actual r2 controls6/10: prospective minimal semantic mutator

2026-09-10. STATIC SOURCE ONLY; UNREVIEWED, UNEXECUTED, not registered. First action13:03:28.342851507UTC; cap13:17:28.342851507UTC; three-minute reserve13:14:28.342851507UTC. Exact targets absent before lease. Publication basis0d39df3c9fd69c939a8420c54d03228b9077777d is provenance only.

Final ROOT delta selects exactly controls6 AND10, dropping8. The newly terminal actual-result gate was hash-checked and read whole; no live gate body was read. No coefficient packet is read or created locally. All original fifteen controls remain NOT RUN.

## Delivered scope

mutate_controls.py and CONTRACT.md freeze the advisory19-argument standalone CLI and bounded receipt. No general dispatcher, science-file edit or authority transplant. The original authority's produce/check nine-argument interface is untouched. The new CLI host/tag barrier precedes JSON/Fraction/payload; external prospective ROOT authentication, native closure, source-path freeze, CAPRUN and aggregate limits remain explicit obligations.

Control6 targets the LIST entry bands[6], gap'7', replacing only B_var by[]. It verifies the original theta² unit coefficient and restores the whole original object. Control10 independently starts from the original; it selects the largest absolute rational in Psi/K1/K0 with the precise first-tie rule. It changes only one coordinate, deletes a term only when all seven coordinates vanish, and handles the all-zero-row constant insertion. The receipt binds exact selection indices/exponents/old/new strings and fixture hashes. Its inverse edit restores the original canonical bytes including any deleted/added term. Baseline and every scientific semantic field outside the selected mutation remain unchanged.

## Static checks and meaningful controls

Whole source was manually read; no Python import, syntax, AST, arithmetic, fixture or test was executed. Canonical integer/rational parsing is bounded before Fraction conversion. All numerical coefficient locations in field/source/bands/rows are validated independently of checker arithmetic. The pinned baseline and canonical whole-object serialization preserve metadata. Files are exclusive, fsynced and frozen; receipt last,32KiB hard cap. Overflow/partial output is STOP, not cap adjustment.

Manual changed-object cases: (i) a largest coefficient -1 becomes0; if another basis coordinate remains nonzero the term MUST remain; (ii) if all seven become0 the term is removed and reverse reinsertion restores its exact index; (iii) tied magnitudes retain the first family/row/term/basis index; (iv) empty rows insert one constant term in firstPsi, reverse removes it; (v) control6 cannot be zero-to-zero because theta² coefficient1 is checked. These are design checks, not executed cases or PASS evidence.

Actual pinned check.py gives control6 upper-band-residual ValueError at216 via329; control10 Psi gives all-three-Psi-slots ValueError at check_arithmetic.py208 via376, while K1/K0 gives literal-retained-K-coefficient via383. CONTRACT freezes complete ordered traceback frames, exact paths/lines/source text/final message, tightly bounded native caret markers, NORMAL_EXIT1 and empty stdout. Unexpected parser/pin/resource failure does not count. This corrects the review's two loose control10 descriptions: Psi can fail without array-length change, and one zero coordinate does not imply zero term.

The underlying packet's earlier normal checker exit is accepted documentary evidence, not assumed source truth. These future two checker runs must establish actual semantic rejection; this writer produces only UNCHECKED fixtures and metadata. No producer or positive-checker rerun.

## Publication and remaining work

Nine exact input pins/WHOLE scope are in READ-SCOPE.md; coefficient body remains unread. The original first+14-minute cap is unchanged. ROOT must first review this static source and its narrow future adapter, then separately register any execution. No runtime/time/height/result claim or source acceptance is minted.

## OPENS RAISED

None. Existing first static review, prospective registration and two actual negative observations remain; no new canonical OPEN or general framework.

## COLLISIONS

EMPTY. Exact report/transaction/box absent before the no-overwrite lease; own whole and completion checks before the final marker. All authored bytes use apply_patch. No source, original packet, shared document or old immutable artifact edited.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `4518`.
- Body SHA-256:
  `15c681e3e7d9ca35650eaf395bc1a9c24c17f25e4a79248537a5c89543f62b86`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
