You are the hostile different-model reviewer for the producer-checked bounded
R-root verdict `DIFFERENT-INSUFFICIENT`. Work in
`/Users/dc/code/math/jc2`. The producer is OpenAI Codex. Write only
`xmodel/review-norm-moment-sep-grok.md`; do not edit source/data/shared ledgers,
use the network, run the preregistered-but-unreached native GGV type gate, or
launch descendants.

Read in full:

- `cases/round2_norm_moment_sep/PREREGISTRATION.md`
- `cases/round2_norm_moment_sep/README.md`
- `cases/round2_norm_moment_sep/norm_moment_sep.py`
- `cases/round2_norm_moment_sep/results.json`
- `xmodel/round2-norm-moment-sep-20260824.md`
- `xmodel/round1-trace-regularity-20260824.md`
- `xmodel/review-round1-proof-gates-claude.md`

Attack these clauses separately:

1. The implementation independently rederives, rather than imports, the
   local algebra `QQ(q)((u))/QQ(q)((t))`, `t=u^e`, its trace filter,
   different, and trace-pairing discriminant; the two different calculations
   agree exactly.
2. The triangular automorphism and `(x^2,xy)` nonproper/non-Keller controls
   give the stated zero/pole principal parts, so the receiver is neither blind
   to poles nor merely accepting frozen expected values.
3. For the two `e=42` Darboux completions, the finite local algebra,
   different/conductor, selected retained contact/gcd decoration, coordinate
   valuations, local Jacobian two-form, and complete first principal part of
   `Tr(x)` really agree. Coordinate multiplication data are correctly kept as
   a separate differing field.
4. Independently recompute
   `Tr(x_b)=42(t^-2+t^-1+b*t)` and
   `Tr(x_b^2)=42(t^-4+2t^-3+t^-2+2b*t^-1+2b+b^2*t^2)`, so the quadratic
   residues are 84 and 168. Verify the first two `y_b` moments have no negative
   principal part and the displayed wedge identity holds.
5. The correct scoped conclusion is only that the listed local
   algebra/different/contact/Jacobian data do not determine the quadratic
   coordinate moment. It is not a polynomial Keller countermodel, does not
   refute global signed or polynomial-origin identities, does not run the
   native source type gate, and implies nothing about TRACE-REG or JC2.

Run the documented deterministic replay into scratch and verify the stdout
hash. Independently calculate representative matrices/traces without trusting
stored verdict strings or record hashes. Try to find a Laurent truncation
mistake, split-uniformizer ambiguity, different/discriminant sign error,
coordinate-data leakage into the supposedly fixed fields, a wedge-sign error,
or an overbroad conclusion.

Return exactly `CONFIRMED`, `CONFIRMED WITH GAPS`, `GAPS`, or `REFUTED`, with
the smallest failing identity if applicable. Put verdict and promotion guidance
near the top; record reviewer/model/CLI, UTC, basis/dirty perimeter, exact
commands, hashes, independent derivations, gaps, and final scope.
