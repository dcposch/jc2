# Whole-portfolio blind ideation — Fable 5 lane — `20260828T1149Z`

Model: **Fable 5** (`claude-fable-5`), equal-standing blind whole-portfolio
researcher.  Date: 2026-08-28.  Status: **SEALED BLIND SUBMISSION** under
packet `xmodel/ideation-20260828T1149Z-packet.md` (SHA-256
`31094db14e41506e121b037bd001e077d6bd80d6b4f38ae49a071116e52fef94`,
recomputed live and matching before reading).

## 0. Custody, blindness, and execution disclosure

- Every custody-gate hash in packet §0 was recomputed live before reading the
  corresponding file; **all 8 canonical + 15 report/checker hashes match**.
  No mismatch; nothing failed closed.
- No file beginning `xmodel/ideation-20260828T1149Z-` other than the packet
  and this output was read, listed, globbed, or otherwise accessed.  No peer
  response was seen.  No contamination occurred.
- `jc2-lean` was never entered, listed, searched, read, built, statused, or
  modified.  No AWS, no web, no Singular/msolve/Sage/Lean, no heavy algebra.
- Local execution: SHA-256 hashing; file reads; and two small standard-library
  exact replays explicitly licensed by the packet:
  `cases/g2_intrinsic_exact_pair_l3_l5_opus5_review_20260828/verify.py`
  → `RESULT: PASS (all 264 checks passed)`, and
  `cases/sigray_prop42_constant_shift_repair_20260828/verify_constant_shift.py`
  → `PASS`, exit 0, 125,788 grouped assertions (matching the coordinator
  replay count).  The AWS-tier checkers
  (`verify_symbolic_quadratic_q_generic.py` and the unit-S chain) were hash-
  verified but **not** executed here; their claims are consumed at the review
  tier recorded in the packet, not re-established by me.
- Files read: the packet; **all** of `APPROACHES.md` (including the full
  46-row master table); `AUDIT.md` top corrections (all 2026-08-28 entries);
  the full 2026-08-28 day of `PROGRESS.md`; `notes.md` newest live state and
  all later events (09:55Z live state through the 11:45Z review event) plus
  the file head for orientation; all of `ladder/SIGRAY-AUDIT.md`; the
  executive verdict, gap-list frame, and §7 of `ladder/REDUCTION.md`;
  `COORDINATION.md` structure and roster; the full prior synthesis
  `xmodel/ideation-20260828T0702Z-synthesis.md`; the exact-pair producer in
  full and its Opus5 hostile review (§§0–2, §8); the reduced-denominator
  sibling review head; the Prop 4.2 producer (§§1–2) and Opus5 r1 review
  head/verdict; the quadratic-Q report; the unit-S first-correction producer
  and review heads; the order-4 and order-5 report heads; the full websweep;
  and the Matysiak first-look head and dispositions.
- The only write of this lane is this file.  A model verdict is not
  mathematical evidence; everything below is strategy, design, and exact-work
  specification unless it cites a reviewed artifact.

## 1. Executive summary

The round's two promotions (intrinsic L3/L4/L5-exact; Sigray Proposition 4.2
completely repaired) move the pure-Sigray route's frontier **past the
constructor/decoration layer**.  The first genuinely open mathematical
implication on that route is now the **universal landing theorem** (cap-free
`M>=2` continuation plus `b>=2` off-axis completeness, REDUCTION CRITICAL
4–6), but the *cheapest movable obstruction* sits just before it: three
consumed-but-unbanked source-trust items (Prop 5.4's q-half, St 6.2's climb
clause at its consuming sites, and the unaudited §7 items Prop 7.3/7.5/Not
7.1 that the promoted books consume).  Prop 5.1's finite-nonzero-puncture
repair is **bypassable** on the current pole-vertex critical path.

On the GGV side, the compression is real but the current instruments are
saturated: four arbitrary-Q GB/msolve variants and three symbolic jobs are
racing toward identical timeout shapes.  I propose replacing them with a
**rank-stratified kernel/endpoint discriminator** (a structurally different
exact object), and replacing lambda-nonzero face-order increments with a
**face-residue redundancy induction** whose order-5 case is already the
reviewed D12 row.

The genuinely new mechanism this round enables — and which no prior round
could have run — is a **certified coupled-inertia CSP**: the promoted L5
terminal law plus the repaired mass formula turn each td=6 book entry into
exact coupled boundary data for both coordinate fibrations, which is
precisely the "new geometric control of finite inertia/support" that the
0702Z synthesis demanded before any monodromy reopening.  Avenue 25 reopens
narrowly on that basis; Avenue 26 stays down.

No item here proves or disproves JC2.

## 2. Packet §3 questions

### Q1. First exact unproved implication on the shortest pure-Sigray route

Route order after this round, with layer-by-layer status:

1. **Selection/normalization** — available (Not 2.3 reduction VERIFIED;
   pure-Sigray minimization bypasses `G2-PSC` by construction; the unused
   GGV farm is then not an input).
2. **Constructor** — available (L1/L2; L3/L4/L5-exact promoted).  The
   normalized rectangle/NE-corner hypothesis is **part of L4-exact** and must
   be re-verified after any minimizing operation (`y-x^2` control).
3. **Decoration** — available: towers via repaired Prop 4.2 + Prop 4.3
   (audit-reconstructed proof closes); `kappa_F` via the jump/max reading now
   *forced by the printed text* through the corrected St 3.8 (the favorable
   H5a upgrade); Q-data via Not 5.3; `Lambda >= beta` via Prop 5.7.  The
   full 4.2 erratum (delete p. 20 Remark, keep (7), Facts A/B, cross-multiply
   6.2/6.3) must travel with every consumer.
4. **Source-trust residue actually consumed by the route** (first unproved
   items hit, in consumption order):
   - **Prop 5.4 q-half** — GAP with a *supplied, verified but unbanked*
     repair inside `ladder/SIGRAY-AUDIT.md`; consumed via St 5.2(ii)'s
     ν-menu by the PROMOTED TDUNIFORM R2 prime theorem and AF3 parity pin.
   - **Prop 5.8 any-`a` strength** — GAP in print, but the campaign repair is
     promoted (REDUCTION T7, modulo its recorded one-line patch), so the
     entry menu at fixed td is available.
   - **St 6.2 "in particular" clause** — GAP; the corrected statement
     (`H in V_a ∩ T_a^+`) is proved in the audit; what is *unproved* is that
     each of the four named consuming climb/regularity sites (AF2 R2, III
     regularity, A2P R2, the St 9.6–9.9 analogues) actually has the added
     hypothesis.  This is a finite verification, not open mathematics.
   - **The unaudited stratum Props 6.3–6.8 and §§7–9** — the promoted books
     consume Prop 7.3, Prop 7.5, Not 7.1, and St 9.6–9.9; the audit's riders
     already flag exactly these (St 3.14's twist enters 7.3; the Not 3.13/3.14
     corrected reading is what keeps Not 7.1/Prop 7.5 alive).
5. **Landing** — the **first genuinely open mathematical implication**:
   T9(c)/T10, i.e. the cap-free `M>=2` continuation/state theorem plus
   `b>=2` off-axis completeness, giving "every entry-menu configuration lands
   in a complete finite book."  Nothing on file even conjecturally proves it.
6. **`RPMC(C)`** — open conjecture (DIR ⟺ PC ⟺ RPMC ⟹ KJN; converse
   unproved).
7. **Type control** — open: KJN is type-relative; an independent bounded
   `(alpha,beta)` menu with provenance is required and absent.
8. **Cofinality** — open: no absolute or cofinal td ceiling.

**Prop 5.1 verdict: bypassable, not critical-path.**  Every campaign use is
at g-poles where `b=0` is valid (audit Tier 3: "the gap is dormant"), the
corrected `b=g(P)` reading exists with the thesis's own §7 corroborating,
and pole purity flows through the repaired 5.8 rather than through
finite-value thresholds.  It becomes load-bearing only if finite-end
structure re-enters (e.g. §7 dicritical analysis, or the finite-end coupling
in Card 1 below) — flag it verify-on-use there.

### Q2. Composition of the terminal certificate + orbit law + corrected tower with RH / conductor / semigroup / proximity / Chau / mass formulas

**NO HIT on a new global bound.**  Reasons, partner by partner:

- The **mass formula IS the Riemann–Hurwitz-at-infinity statement**: repaired
  Prop 5.8 says td = ΣΛ over `T_{a,pole}`, i.e. the fibre of `ĝ` over ∞ is
  entirely supported at pole places (pole purity).  Composing it with RH
  again returns genus bookkeeping the campaign already has ("nothing bounds
  genus or td", rows 7/26).
- The **corrected 4.2 corner cannot increment pole mass**: the `(1,0,c)`
  corner lies in `T_a^nearrow`, while every pole characteristic path is
  `T_a^searrow` and corner-free — so the Λ ledger at pole vertices is
  untouched by the new terminal degree `(mu-1)deg p_F+1`.  The hoped-for
  "corner ⟹ per-vertex mass floor `beta+1`" interface is structurally
  impossible.
- **Conductor/delta/semigroup/proximity** data are per-place and
  type-relative; they feed KJN-shaped inequalities that only become bounds
  after an independent type menu — exactly the wall KJN's type-relativity
  already names.
- **Chau's finite-end identity** prunes passports (169→48, all 48 survive);
  the certificate makes its inputs entry-exact (see Card 1) but supplies no
  degree ceiling.

**Salvage — a literal (non-bound) interface `MT` worth executing:** for each
pole vertex `F` on a searrow path, the tuple
`(alpha,beta; kappa_F, nu_F, Lambda_F, deg p_F, mu_F, terminal degrees in
M_F)` must satisfy: `Lambda_F >= beta` (5.7); `nu_F < deg p_F` (5.7/5.3(iv));
the ν-menu of repaired-5.4/St 5.2(ii); the 4.2-corrected tower degrees with
corners confined to nearrow; and td = ΣΛ (repaired 5.8).  Recomputing the
td = 6..8 entry menus under `MT` is finite, exact, and can only shrink them.
It is menu tightening, not a global bound, and I label it exactly that.

### Q3. Next source repair for maximum fanout

Ranked by fanout per cost, each with its cheapest proof/refutation/bypass
triad:

1. **Bank the Prop 5.4 q-half repair** (winner).  Proof: the derivation is
   already written and verified inside the audit ("exact null-space
   computation on three patterns", load-bearing on `alpha>=2`); extract it to
   a standalone producer and run one hostile review.  Refutation: closed —
   genuine `alpha=1` counterexample solutions exist but St 2.1 gives
   `alpha>=2` on the route.  Bypass: none needed.  Fanout: instantly
   unconditionalizes two PROMOTED theorems (TDUNIFORM R2 prime theorem, AF3
   parity pin) and the ν-menu every `MT` recomputation uses.  Cost: hours.
2. **St 6.2 consuming-site sweep.**  Proof: corrected statement already
   derived.  Refutation: the abstract `d_H<0` counterconfiguration is already
   on file, so the unqualified clause is dead.  Bypass (the live item):
   mechanically verify `H in V_a ∩ T_a^+` at each of the four consuming
   climb sites; A2P-REVIEW already tracks the hypothesis, so this is a
   grep-and-check pass.  Fanout: protects the climb/regularity workhorse
   across four promoted docs and preempts a §9 audit blocker.  Cost: hours.
3. **Targeted §7 audit of exactly Prop 7.3, Prop 7.5, Not 7.1** — not a
   wholesale §7–9 audit.  These are the only §7 items the promoted books
   consume, and the known St 3.14 twist gap enters 7.3.  Cheapest order:
   refutation first (hunt a root-of-unity-twist counterexample to 7.3's
   pattern preservation, since the audit already knows the twist is real),
   then proof under the corrected `d_{f-a}` reading with the twist carried
   explicitly.  Cost: one audit lane, days.
4. **Execute the 4.2-erratum-mandated cross-multiplication custody of
   Props 6.2/6.3** — already specified by the promoted erratum; cheap;
   verify the corrected (ii′) branch really is the only one consumed.
5. **Prop 5.1: defer** (bypassable, Q1).  **Props 6.4–6.8: defer** until a
   consumer names one.  **§§8–9: audit St 9.6–9.9 just-in-time** when the
   landing design names its exact climb steps.

### Q4. Intrinsic bypass of the hybrid source; reverse discharge

**Forward:** the pure-Sigray architecture already bypasses the hybrid GGV
source, and L3–L5-exact now make its constructor available.  The genuinely
new consequence is **instance-level**: `H-TRUNC`/`G2-PSC` is an obligation
only for transporting the *abstract selected* GGV packet; for any *explicit*
polynomial pair, the intrinsic constructor **is** the transport — one can
compile the pair's certified decorated tree directly and test book
conditions, with no hybrid arrow.  That is the EPTC software item (§6).
This does not bypass landing or `RPMC(C)` as universal theorems: the
certificates say nothing about the `M>=2` continuation wall.

**Converse: NO.**  Every reviewed GGV endpoint fact is fixed-fixture and
field-valued (the `A=X^4-1,V0=1` fixture, tails 4–7, the fixed even faces,
the quadratic-Q generic face), while the pure route's hypotheses are
universal.  The one candidate interface — the general-`H` endpoint criterion
`A=Bv'+(3/2)B'v` and the mode law `4|e_i(12-n)` (R4/R5/R6) pricing tree book
cells — requires a tree→cascade transport that is the mirror image of
`G2-PSC` and is equally unproved.  Do not silently transfer in either
direction.  (`G2-PSC` and `G2-BD` remain distinct obligations; nothing in
this round touches `G2-BD`.)

### Q5. Fastest exact discriminators for the GGV finite lane

**(a) Quadratic-Q rank-drop variety.**  The decisive structure fact: the
rank-drop locus lives in the **six-dimensional parameter space**
`(q0,q1,q2,c4,c6,c8)`, and the matrix is linear in the 105 unknowns.  Do not
rerun ideal-level GB.  Staged design (Card 2):

1. Fraction-free Bareiss with content stripping to 104 generic pivots;
   serialize the pivot manifest and the two residual polynomials
   `u1,u2 in Q[q,c]`.  Off pivot degenerations, rank-drop ⊆ `V(u1,u2)`.
2. On each component of `V(u1,u2)`: adjugate/last-minor kernel vector `v`,
   then test the endpoint composition `E∘v ≡ 0` (the raw origin bilinear
   `E=f11_1*g11_0+f_0*g15_1`).
3. Recurse on pivot-vanishing strata (Fitting stratification), smallest
   stratum first; exploit the graded/sparse row supports for block
   triangularization.

Stop conditions: `E∘v ≡ 0` on every stratum ⟹ **face endpoint-dead
theorem** (close the quadratic-Q face); a named component with `E∘v ≠ 0` ⟹
explicit survivor parameters, immediately fed to G16–G21/four-root; hard
43,200 s per stage with mandatory serialized markers; **no unchanged
relaunch** of the four capped GB/msolve variants (packet already mandates).

**(b) Lambda-nonzero raw windows.**  Two discriminators, neither a face-order
increment:

1. **Face-residue redundancy induction** (theorem interface): conjecture
   that for every k, the relative-order-k face residue at the unit-S branch
   lies in the ideal generated by the earlier determinant-row numerators.
   Evidence: order 4 cancels exactly by the two reviewed D12 lifts; order 5
   is `J*(20c2J+3N)` — literally the reviewed `R12r` numerator up to a unit.
   Test first at order 6 (desk/small exact).  Proof ⟹ **stop all unit-S
   face-order work permanently** (the expansion is a repackaged cascade);
   failure at some k ⟹ that k is a genuinely new cut.  Either outcome is
   decisive.
2. **Unit-S receiver-matrix sibling**: compile the odd receiver system on
   the unit-S branch over its branch parameter field and compute its exact
   rank/kernel — the same machinery that produced the 106×105 theorem,
   pointed at the other branch.  One compile job, 1,800–43,200 s inner cap,
   exact terminal marker mandatory; timeout = no verdict.

### Q6. Under-resourced counterexample avenue

Two, both specified and idle:

1. **K00 compressed rank-exact successor (V27).**  The design doc exists
   (`xmodel/max12-812-order2-k00-v27-rank-compressed-atlas-successor-design-sol-20260827.md`);
   the 0702Z synthesis explicitly conditions launch on freezing the exact
   rank-open target and fail-closed stop.  Nobody has frozen it.  This is the
   strongest finite non-GGV falsifier and it is starved while GGV consumes
   the round.  Action: freeze, then launch one lane (rank-purity prepass
   first, then the two-scalar-class minor collapse).
2. **Deep-locus deformable-survivor test.**  The literal D0–D21 point's
   exact tangent obstruction is an automatic rank-zero phenomenon (0702Z);
   the specified successor — the quadratic/Kuranishi equation on the
   17-dimensional tangent kernel — has not run.  It is the cheapest current
   discriminator between isolated rigidity and a deformable survivor
   component, i.e. the character-zero algebraization mechanism's entry test.

What **not** to fund: fixed-support AS digit chasing.  The reviewed record
is bimodal — fixed support dies at finite depth, growing support has a
nonpolynomial restricted-analytic limit — so the only honest bounded-support
family target remains the max-12 `(8,12)`/`(9,12)` frontier with the
`y`-degree ≥ 12 AS109 correction floor.  Formal or modular survival is not a
counterexample; every candidate must name its algebraization/collision step
before compute.

### Q7. Composition pass and new mechanisms

See §5 (new mechanism + connection), §6 (software), and the cards.  The full
pass also produced: the `MT` menu-tightening interface (Q2); the
face-residue redundancy induction (Q5b); the kernel/endpoint discriminator
(Q5a); the instance-level `G2-PSC` bypass reading of L3–L5 (Q4); and the
§7 just-in-time audit ordering (Q3).  Software beyond EPTC: automate the
St 6.2-style *consuming-site hypothesis sweep* (index every book invocation
of an audited-with-rider statement and machine-check the rider), which turns
future audit riders from prose into regression tests.

## 3. Contract item 1 — disposition vector, avenues 1–46

Changes (with reasons):

- **2 (Sigray sheet ladder): RAISE.**  L3/L4/L5-exact plus the complete
  Prop 4.2 repair clear the constructor/decoration layer; the audit census is
  now 21/33/10/2/6 with a favorable H5a upgrade.  The route's frontier is the
  landing theorem, with a cheap source-trust package (Card 3) in front of it.
  `G2-PSC` and `G2-BD` remain distinct; neither is discharged.
- **4 (formal-germ certification/algebraization): RAISE (narrow).**  The
  deep-locus Kuranishi second-order test on the 17-dim tangent kernel is now
  the cheapest algebraization discriminator in the portfolio and is unrun.
  Raise only that step; no broad germ program returns.
- **16 (D-module/holonomic index): LOWER (execution priority only).**  The
  charged HENS-CT control burned its full four-hour cap inside
  `annihilator_of_composition` and never reached creative telescoping; the
  math scope (de Rham index law, rank-one control) is unchanged, but no new
  launches this window until a structurally different backend adapter passes
  the upstream certificate example.
- **25 (fiber monodromy/passports): REOPEN (narrow).**  The 0702Z synthesis
  refused a reopening "without new geometric control of finite inertia/
  support."  The promoted L5 terminal law plus the repaired mass formula now
  supply exactly that control: certified coupled per-place data
  `(chart, e_S, Lambda_S)` per td=6 entry.  Reopen strictly as the coupled
  per-entry CSP of Card 1, not as any generic census.
- **26 (primitive-monodromy td bound): UNCHANGED — deliberately not
  reopened.**  The new certificates change per-entry realizability (avenue
  25's CSP), not the group-theoretic cofinal bound; the banked
  `sol-monodromy-td.md` census stays negative and the `A_d/S_d` wall stands.

Unchanged, grouped (current tiers retained; no omissions):

- **1** — unchanged rank, internally redesigned: sole endpoint target
  `A|V0`; instruments per Q5 (kernel/endpoint discriminator; redundancy
  induction; unit-S receiver); no fixed-`V0=1`, proper-divisor, tail,
  cutoff-3, or face-order-increment work.
- **3, 5, 6, 8, 9, 12, 13, 14, 15, 17, 18, 20, 21, 22, 23, 24, 27, 28, 29,
  30, 31, 32, 33, 34, 35, 38, 39, 40, 41, 42, 43, 45** — unchanged at their
  recorded tiers (tried/closed/low/instrument), no new evidence this round.
  Notes: 17's low rank is reinforced by the Matysiak audit's
  monomial-similarity-only theorem (Jordan reductions of Družkowski forms
  are DOA); 30/18 retain the single licensed van Dobben `KEF-ONE-VERTEX`
  test; 35 is further discouraged by the reviewed failure of the naive
  symmetric-power descent.
- **7** — unchanged; the `A(F)`/KEF bridge stays a backup card gated on one
  completed boundary book.
- **10, 11, 44** — unchanged (stopped/refuted/closed as proof inputs).
- **19** — unchanged: max-12 `(8,12)`/`(9,12)` frontier; AS109 `y>=12`
  floor; `n=6` content strata only.
- **36** — unchanged: theorem-compressed search on named residuals only;
  ambient sampling stays stopped.
- **37** — unchanged (closed as census).
- **46** — unchanged: asynchronous formalization; the nested `jc2-lean` tree
  is user-owned and untouched.

## 4. Contract item 2 — bottleneck reranking

Proof side:

1. **Universal landing/coverage** (cap-free `M>=2` state theorem + `b>=2`
   off-axis completeness), with its cheapest movable part being the
   source-trust package (5.4 banking, St 6.2 sweep, targeted 7.3/7.5/Not 7.1
   audit).  This overtakes everything now that L3–L5 and 4.2 are cleared.
2. **`RPMC(C)` → type menu → cofinal ceiling** (the KJN type-relativity
   wall).  No composition in Q2 dents it.
3. **Deep `A|V0` endpoint exclusion** — the sole surviving local branch-P
   endpoint locus and the best short-horizon information source; its
   `lambda=0` even face is nearly closed and the rank-drop variety is the
   named residue.

Disproof side:

1. **K00 source-open, rank-exact, full-`P6` point plus prolongation** (V27
   freeze-then-launch).
2. **A deep-locus family surviving endpoint and algebraization** — decided
   next by the Kuranishi quadratic on the 17-dimensional tangent kernel; a
   deformable survivor component would be the first genuine char-zero
   candidate family, an isolated point retires the lead.

## 5. Contract item 3 — new mechanism and new connection

**New mechanism — certified coupled-inertia CSP (Card 1).**  History
checksum: `xmodel/sol-monodromy-td.md` executed the *generic* td=6..9 group
census (negative; `A_d/S_d` wall); avenue 25's coupled two-coordinate
branch-cycle CSP was scored (S:7) but never executed with real data; the
Chau filter pruned passports 169→48 with all 48 surviving; and the 0702Z
synthesis rejected reopening for lack of finite inertia control.  What is
new *as of 11:40–11:45Z today*: the promoted L5 law (`orbit=e_S`,
`stabilizer=kappa/e_S`, terminal record `(h,chart,kappa,F)` with
`deg p_{h,F}=1`) plus repaired 5.8 make the coupled local data of **both**
coordinate fibrations exact and entry-indexed.  No prior round possessed
certified coupled data, so this composition was never runnable before.

**New connection — Avenue 2 × Avenue 25 × Avenues 7/26.**  The tree menu
(2) supplies per-entry place data; the coupled CSP (25) consumes them; the
Chau finite-end identity and marked divisibility (7/26) become *entry-level*
constraints because L4-exact enumerates the finite ends (places outside
`T_{a,pole}`) exactly.  Secondary new connection (Q4): Avenue 2 × Avenue 1 at
the *instance* level — the intrinsic constructor is a per-pair `G2-PSC`
bypass, so explicit GGV/K00 survivors can be compiled straight into
decorated tree data (EPTC), a bridge no prior overlay names.

## 6. Contract items 4–5 — strongest attacks; software

**Strongest proof attack to run next:** the SIGRAY-FANOUT-TRIAD (Card 3)
immediately — it is desk-cheap and de-risks everything the landing design
will consume — while the `A|V0` sublanes continue with the Card 2
discriminator as their decisive instrument.  The first landing-design task
after the triad: state the cap-free `M>=2` state theorem as a finite-state
claim over the certified terminal records, so that its difficulty is finally
measured against an exact object rather than prose.

**Strongest counterexample/falsification attack to run next:** the
deep-locus **Kuranishi quadratic on the 17-dimensional tangent kernel** of
the literal D0–D21 point (cheapest, sharpest), with the **K00 V27
freeze-then-launch** as the parallel finite falsifier.

**Software acceleration (exact target and verification gate) — EPTC, the
exact-pair certified tree compiler.**  Target: a standard-library exact
program taking an explicit squarefree `h=f-a` and emitting the decorated
two-chart forest with per-node proof objects: strict truncations, residual
polynomials `p_{h,F}`, terminal certificates `deg p_{h,F}=1`, `kappa`,
`e_S=kappa/gcd(kappa,supp F)`, orbit/stabilizer sizes, Hensel continuation
on the shifted `t`-cleared equation, `g`-pole tags with
`ord_t=(kappa/e_S)ord_S` rescaling, the Λ ledger, and 4.2-corrected towers
including `(1,0,c)` corners with terminal degrees entering `M_F`.
Verification gate (all five mandatory before any campaign use): (1) `y-x^2`
must FAIL L4 coverage (negative control); (2) a tame automorphism pair must
yield a trivial ledger; (3) td = ΣΛ cross-checked against an independently
computed td on one explicit non-Keller control (the repo's explicit `8_28`
realization); (4) numeric replay of the orbit/stabilizer law at every
terminal; (5) every order read on the cover asserted through the
`kappa/e_S` rescaling.  Uses: Card 1's data generator; the `MT` menu
recomputation; instance-level `G2-PSC` bypass; regression harness for the
§7–9 just-in-time audits.

## 7. Contract item 6 — idea cards (three)

### Card 1 — `TD6-MENU-CSP`: certified coupled-inertia realizability on the td=6 entry menu

- **Exact dependencies:** promoted L5 terminal law (`5d219c53...` review);
  repaired Prop 5.8 entry menu (REDUCTION T7 + its one-line patch rider);
  the td=6 eight terminal classes (four single-pole, four two-pole) at their
  stated conditional tiers; promoted Chau finite-end identity and marked
  divisibility filter; `sol-monodromy-td.md` as the generic negative
  control; EPTC (or hand compilation for the first entry).
- **Licensed assumptions:** normalized rectangle/NE-corner hypothesis (part
  of L4-exact); menu cells consumed only at their recorded tiers; Prop 5.1
  verify-on-use if finite-end thresholds at `g(P) in C*` enter.
- **Mechanism:** each entry determines coupled boundary data — places `S`
  with chart membership and `e_S` for both coordinate maps, pole masses
  `Lambda_S` with ΣΛ=6, finite-end count and congruence data.  Pose the
  two-coordinate branch-cycle CSP (both fibrations, transitivity, the
  Jacobian pairing, Chau congruences) per entry and solve exactly.
- **Cheapest decisive discriminator:** the smallest single-pole td=6 entry.
- **Materially different outcomes:** (i) an entry is unrealizable ⟹ first
  topology-side book-cell kill — a new exclusion mechanism orthogonal to
  book algebra; (ii) all eight entries realizable ⟹ bank the census and
  permanently retire coupled-CSP reopening proposals (stale-packet
  correlation insurance); (iii) typing failure ⟹ names the exact missing
  decoration, feeding the Q3 audit queue.
- **Stop/rollback:** fail closed on any conditional-tier cell; stop after
  the eight td=6 classes; no td=7+ extension without a round decision.
- **Cost:** desk plus hours of small exact computation; one hostile review;
  no AWS required.
- **Expected information gain:** high per hour — a kill validates a new
  mechanism; a clean pass closes a recurring proposal channel; both are
  durable.

### Card 2 — `QQ-KERMOD`: rank-stratified kernel/endpoint discriminator (+ unit-S receiver sibling)

- **Exact dependencies:** report `0775a496...` (106×105 generic rank 105)
  with checker `83ab6c75...` (coordinator-replayed; different-model review
  still due — consumed as provisional); the 17-generator exact dedup core;
  the reviewed odd-gate/window compilers; for the sibling, the reviewed
  first-correction lemma (`79076a9c...`).
- **Licensed assumptions:** fixed even face scope
  (`e=F8=F10=F12=F14=0, c2=1`); G16–G21/four-root and released even tails
  are explicitly out of scope; conclusions are face-level only.  The
  provisional order-4/5 unit-S chain is *not* consumed.
- **Design:** stages 1–3 of Q5(a) (Bareiss residual pair → adjugate kernel +
  `E∘v` per component → Fitting recursion), plus the unit-S receiver-matrix
  compile of Q5(b)(2).
- **Cheapest decisive discriminator:** stage 1's residual pair `(u1,u2)` —
  two polynomials in six variables — followed by the `E∘v` test on
  `V(u1,u2)`.
- **Materially different outcomes:** face endpoint-dead theorem (closes the
  quadratic-Q face; the lambda-zero branch then reduces to released-tail /
  G16–G21 systems); or explicit survivor parameters (the first genuine
  deep-locus candidate family — route to G16–G21/four-root at once); or a
  structured partial (banked pivot manifest + residual pair guiding a block
  redesign) — strictly more informative than the current four-way GB
  timeout.
- **Stop/rollback:** 43,200 s per stage; mandatory serialized markers
  (pivot manifest, residual pair, component list, `E∘v` remainders);
  absence of a marker = no verdict; no unchanged relaunch of the capped
  GB/msolve variants; all stages proof-carrying, so rollback discards
  nothing.
- **Cost:** one to three one-core AWS jobs, ≤1 day each, zero-swap hosts per
  standing policy; one design review; one review of any theorem-grade
  outcome.
- **Expected information gain:** converts an instrument stalemate into a
  face-level decision; the dead-face outcome is a lane closure, the survivor
  outcome is the best current counterexample-side lead inside GGV.

### Card 3 — `SIGRAY-FANOUT-TRIAD`: source-trust package for the pure route

- **Items:** (a) bank the Prop 5.4 q-half repair as a standalone producer +
  hostile review; (b) file the corrected St 6.2 statement and run the
  automated consuming-site hypothesis sweep (AF2 R2, III regularity, A2P
  R2, St 9.6–9.9 analogues); (c) targeted audit of Prop 7.3/Prop 7.5/Not
  7.1 under the corrected `d_{f-a}` reading with the St 3.14 twist carried
  explicitly (refutation-first ordering); (d) execute the 4.2-erratum's
  mandated Prop 6.2/6.3 cross-multiplication custody.
- **Exact dependencies:** `ladder/SIGRAY-AUDIT.md` (`6d1c34d1...`); the 4.2
  repair pair (`10bc55d5...`/`47f2b608...`); the audit's supplied 5.4
  derivation; `refs/sigray_full.pdf` at its pinned hash.
- **Licensed assumptions:** none beyond the printed source and the already
  promoted corrected readings (jump/max `kappa_F`; `d_{f-a}`).
- **Cheapest decisive discriminators:** (a) one review; (b) one grep+check
  pass; (c) a twist counterexample hunt before any proof effort.
- **Materially different outcomes:** all-pass ⟹ the route's consumed-source
  perimeter through the book-consumed §7 items is clean and the landing
  design starts on trusted ground; a 7.3/7.5 refutation ⟹ a load-bearing
  correction to the books' pattern preservation (immediate rerank); a
  St 6.2 site without the hypothesis ⟹ a named hole in a promoted climb
  (quarantine that consumer only).
- **Stop/rollback:** item-wise; a failure quarantines exactly its
  consumers; nothing else moves.
- **Cost:** three to four desk producers + hostile reviews; days; no AWS.
- **Expected information gain:** removes the cheapest movable part of
  bottleneck P1 and unconditionalizes two promoted theorems; every later
  landing/`RPMC` effort consumes this trust perimeter.

## 8. Contract item 7 — lane calls

- **Pure Sigray/exact-pair:** **continue** (raised).  Next: Card 3, then the
  finite-state formulation of the cap-free `M>=2` landing claim.
- **Hybrid `G2-PSC`:** **redesign.**  Park the universal `H-TRUNC` transport;
  retain only instance-level compilation of explicit pairs via EPTC.
  `G2-BD` is a separate, unmerged obligation attached to residue-A routes;
  it is untouched this round and stays open.
- **Arbitrary-Q / rank-drop GGV:** **redesign** per Card 2; let the four
  capped variants expire; no unchanged relaunches.
- **Lambda-nonzero / raw-window GGV:** **redesign**: no face-order
  increments; run the order-6 redundancy induction and the unit-S receiver
  compile; treat orders 4/5 as provisional until the queued different-model
  review lands.
- **LF40:** **continue narrowly** — one genuinely different frozen
  engine/order at most; otherwise idle (idle AWS beats a stale duplicate);
  the 03:14Z cap is operational non-evidence.
- **K00:** **continue** — freeze the V27 exact rank-open target and
  fail-closed stop, then launch; disproof bottleneck #1.
- **Order-two / TD6:** **continue narrowly** under existing gates (TD6's
  next certificate composition; remaining order-two strata); small share.
- **D43:** **continue** existing jobs only; no expansion.
- **Artin–Schreier:** **continue narrowly** at the max-12 frontier and the
  AS109 `n=6` content strata; no digit chasing; no fixed-support families
  without a named algebraization step.
- **HENS-CT:** **stop (park).**  The generic
  `annihilator_of_composition` path is dead by demonstration; revival
  requires a structurally different (fraction-field or alternative-backend)
  adapter that first passes the upstream algebraic-integral certificate
  example plus mutations.
- **External intelligence:** **continue** at cadence (next broad sweep due
  2026-08-29 05:24Z); Matysiak stays `REFUTED-ON-AUDIT / NO RERANK`; the
  single source-tracked van Dobben `KEF-ONE-VERTEX` test license stands.

## 9. Contract item 8 — likely-missed insight

**The Prop 5.4 q-half repair already exists, verified, inside
`ladder/SIGRAY-AUDIT.md`, and was never banked** — while TDUNIFORM R2 (the
prime theorem) and AF3's parity pin are PROMOTED on exactly the unproven
half (the audit's own Tier 1 text says the repair "should be banked as the
supporting derivation").  Cheapest test: extract that derivation into a
standalone producer note and run one different-model hostile review — desk
work, hours, no compute.  (Card 3(a); called out separately because it is
the one place where the campaign is currently consuming an unproved
statement whose proof it already possesses.)

## 10. Contract item 9 — epistemic ledger

**Promoted/reviewed facts consumed (with review provenance):** intrinsic
L3/L4/L5-exact + terminal law and orbit/stabilizer formulas (`841c848e...`,
`5e8bc3c5...`, Opus5 `5d219c53...`, checker `26e24ec...` — locally replayed
PASS 264/264); Sigray Prop 4.2 complete constant-shift repair + downstream
`l=0` typing (`10bc55d5...`, Opus5 `47f2b608...`, checker `8396a44c...` —
locally replayed PASS, 125,788 grouped assertions); audit census
21/33/10/2/6 with six named GAPs; unit-root transport ⟹ sole endpoint locus
`A|V0` (`21064740...`/`cbf8498e...`); fixed `A=X^4-1,V0=1` fixture
field-empty (`a294cdf7...`); tails 4–7 field-empty (reviews `adbe2aa4...`,
`0312ef63...`, `040583c6...`, Opus5 tail-7); D7–D22 cascade rungs (Grok46
reviews `0a075a73...`, `9363d7cc...`, `e4788bb8...`, `c2015d74...`); q1/q3
composition + `lambda=0 ⟹ S=U=0` (`76529aa2...`, `61edbc23...`); active-c2
D8–D15 repair (`042dbacd...`); q5–q13/q15 non-exclusion (`b649e082...`,
`18831713...`); raw origin bilinear target + all-even kill (`c940ba04...`,
`7f19cf19...`); fixed `Q=0`/`Q=X` slice exclusions (`37103f26...`,
`8b91115e...`); active-c2 literal D16–D22 tail point (`64f9d4a1...`);
unit-S first correction regular through order three (`79076a9c...`); Chau
finite-end filter 169→48; monodromy census negative (`b8144631...`); K00
V26R1F intrinsic survival + V27 design; Matysiak `REFUTED-ON-AUDIT`
(`c5655b43...`).

**Provisional inputs (not consumed as theorems here):** quadratic-Q
106×105 rank-105 (`0775a496...`; checker coordinator-replayed; different-
model review outstanding — Cards treat it as provisional); unit-S order-4/5
cancellation chain (`874bcd98...`, `aa88f190...`; review queued); the
875-fiber origin-isotropy grid (navigation only); every "producer-exact"
tag in the live notes.

**Conjectures proposed here (unproved):** face-residue redundancy at all
orders; the possibility of a coupled-CSP book-cell kill; menu-tightening
`MT` shrinking the td=6..8 menus; the finite-state formulability of the
cap-free `M>=2` landing claim.

**Failed approaches consumed as negative facts:** four arbitrary-Q GB/msolve
caps + three Box03 symbolic caps (operational no-verdicts); HENS-CT
control timeout at `CONTROL_COMPOSITION`; LF40/Box02/Box03/K00 resource
caps; the five-mode cutoff-two reduction (exact counterexample
`U=A^2+t/2`); the translation shear as a raw gauge; naive van Dobben
symmetric-power descent; Matysiak's two proofs; the generic monodromy
census as a kill mechanism.

**Hidden assumptions flagged:** the normalized rectangle/NE-corner
hypothesis inside L4-exact (fails for `y-x^2`); menu cells' conditional
tiers; the Prop 5.8 one-line patch rider; the St 3.14 root-of-unity twist
whenever exact leading coefficients cross fibers; the jump/max `kappa_F`
and `d_{f-a}` corrected readings everywhere the books are quoted; the
`ord_t=(kappa/e_S)ord_S` rescaling on every cover-read order; `Q(t)→Q(rho)`
style specialization orders in any future total-chart claims.

**Checks run and failed attempts this session:** SHA-256 recomputation of
all 23 pinned files (all match); two licensed standard-library checker
replays (both PASS, quoted above); no CAS, no AWS, no other execution.  One
benign shell quoting slip (a literal `====` echo) aborted one read batch;
the affected files were read in follow-up commands.  No mathematical
attempt was made, so none failed.

**Contamination:** none (details in §0).

**Exact scope:** this document is a strategy/design submission by one model
lane.  Its verdicts are not mathematical evidence; its cards license no
promotion; review gates all promotion, publication, irreversible action, and
expensive fanout.  Nothing here proves or disproves JC2.
