# Model availability smoke test — 2026-08-28

This is an availability test only. Do not inspect, list, search, or modify the
repository. Do not use the web.

Use your shell/Bash tool exactly once to execute:

```sh
/usr/bin/printf 'JC2_MODEL_SHELL_OK_20260828\n'
```

Then return exactly these two lines, replacing `<configured model>` with the
model identity selected by your launcher:

```text
SMOKE_MODEL=<configured model>
SMOKE_SHELL=JC2_MODEL_SHELL_OK_20260828
```
