# Fable 5 hostile review — Opus td=8 exact separation law and jet freedom

Producer: Opus 5. Work independently from `/Users/dc/code/math/jc2` and write
exactly:

`xmodel/m2-td8-first-extra-jet-exact-lambda-primary-hostile-review-fable5-20260829.md`

Read the complete target
`xmodel/m2-td8-first-extra-jet-exact-lambda-primary-opus5-20260829.md`, packet
`cases/m2_td8_first_extra_jet_exact_lambda_opus5_20260829/`, its charged prior
reports/reviews, and every printed/canonical clause the target uses. Verify
the target full hash
`991e1b350ad2f8b2b82808fdc84dec71154f9ae17250c18953a36ac8f6588508`
and body hash
`f1ae6c2b1dd94ab2dcf26dfb16e149067eab0617b549817d5620c728d6936d11`.
Treat every claimed theorem and every reading of Sigray as an allegation. Do
not read the active Fable coefficient-transport output or any later model
output.

Audit independently, prioritizing these possible failure modes:

1. Re-read Statement 3.9, Statements 3.10/3.13/3.14, Proposition 3.1,
   Proposition 7.3, (18), and the corrected Statement 9.3(24). Check every
   domain hypothesis: `F`, `F*c`, `I(u)`, `T_a` direction, generic-fiber
   perturbation, `d_f>=0`, and whether the cv vertex really is the same
   `u_0`. Decide whether `kappa_H(pi(H)-1)` is proved integral here.
2. Re-derive Theorem A from Puiseux conjugacy. Attack
   `D_F=integral N`, `N(0+)=m`,
   `kappa_H/kappa_F=E_+/E_0`, the alleged divisibility/integrality of that
   ratio, `E_+<=m`, and the equality case. Look for missing normalization
   factors, changes of suitable kappa, or a branch counted by `p_{F*c}` but
   not by the same place.
3. Reconstruct the A-step menu from scratch. Check the identification
   `m=2`, the single possible degree drop, contact versus conjugate cases,
   allowed values of `theta`, endpoint cases `theta=7`, possible multiple
   coincident drops, and the iff `Delta_A=2 <=> deg(p_H)=2`.
4. Reconstruct the trunk criterion. Check `m=2 i_F`, `D=17 i_F`, the lower
   bound, ramification ratio, and the equivalence between `Delta=2`,
   `kappa_H=kappa_F`, `tau_0=9`, and exactly one `i_F` unit of lost area.
5. Audit the derived subtop vanishing law (V) and semi-invariance law (W).
   Verify all signs, indices, stabilizers, inverse weights, and the use
   `nu_F | kappa_F`. Then attack Theorem E's two formal fillings: global
   polynomial degree/support constraints at every root, compatibility with
   both `f` and `g`, whether `s=1` truly implies first-level separation,
   whether `s=3/8` establishes compatibility with arbitrarily late
   separation, and whether either example proves more than a single free
   diagonal coefficient.
6. Re-audit Theorem F's complete exit set and budget accounting. Look for
   omitted vertices/charges, a wrong `psi`, confusion between lower bounds
   and exact charges, or an unjustified use of Proposition 7.5. Verify that a
   single A-charge at least 3 really kills every family member uniformly.
7. Replay both test modes, independently reconstruct core arithmetic, and add
   hostile mutations in `/tmp`. Tests that only restate the producer formulas
   do not validate source semantics.

Return one lead verdict: `PASS_AT_RECORDED_FORMAL_SCOPE`, `PASS_WITH_REPAIR`,
`REPAIR_REQUIRED`, or `FAIL`. Give a clause-level scorecard separating the
exact descent law, finite A-menu, trunk criterion, jet underdetermination, and
budget knife-edge. Preserve every valid narrow residue even if a stronger
claim fails. Include exact commands/counts, full/body hashes, maximum safe
consequence, and the best next lemma. No pass establishes exact lambda,
transport/gluing, landing, realization, a panel exclusion, a degree bound, or
JC2. No AWS is authorized.

Hard boundaries: no web, AWS, commit, push, canonical edits, heavy CAS, or
local long/high-memory process. Never access/list/search/build/status or
control `jc2-lean`; never run global `git status` or workspace-wide searches.
Modify only the requested report; use `/tmp` for scratch.
