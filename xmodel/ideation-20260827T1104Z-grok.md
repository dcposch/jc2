# Blind significant-news whole-portfolio ideation — Grok 4.6 — `20260827T1104Z`

Status: **FROZEN BLIND SUBMISSION**. Independent of every other
`xmodel/ideation-20260827T1104Z-*` file. No canonical ledger, case,
producer/review artifact, or other file was edited. No AWS job was
launched. No web. No `jc2-lean`. No heavy local CAS.

- Lane: xAI/Grok whole-portfolio researcher
- Sealed packet: `xmodel/ideation-20260827T1104Z-packet.md`
- Packet SHA-256 (verified on disk):
  `cbbbabbd1c0c9795cfeb5069e3cf964778f90452b1897dced35ab0a2150250a7`

Evidence labels:

- **THEOREM** — different-model confirmed / freeze-promoted statement
- **DISCOVERY** — exact mathematics whose independent result review is
  live or whose validator/custody is not yet fail-closed
- **NAVIGATION** — ranking or routing, not a theorem
- **PROVISIONAL** — exact but unreviewed reasoning in this report
- **SPECULATION** — testable guess

No proof or counterexample of JC2 exists at this freeze. Model identity
is not a vote. Historical artifacts are used only at their recorded
scope.

---

## 0. Custody and charged news

Required pre-event files, verified SHA-256:

| File | SHA-256 |
|---|---|
| `xmodel/ideation-20260827T0935Z-packet.md` | `e871e3d71a8777ca50f7c796bf7547023a8c2ca8e2930c6a0981ddcebd6ae3db` |
| `xmodel/ideation-20260827T0935Z-synthesis-sol.md` | `e7d3aeb0bbc992b488de46f35e8888cf5c7512c1231718d39b611d4eccd3321e` |
| `xmodel/ideation-20260827T0635Z-synthesis-sol.md` | `2e3305cceae32cb55fdb0bcd21be007b61c0ae5aeaea34bc284ce0ff3e3f4b86` |
| `xmodel/websweep-20260826T2355Z.md` | `2a708fc18a9349acf1bab0951ceb76fd79126b0d62f519ad79b6aadd43ec01ce` |

Live canonical files consumed as newest state, not as the 09:35Z
snapshot (those snapshot hashes are stale by later overlays):

| File | SHA-256 now |
|---|---|
| `APPROACHES.md` | `4367f6001cd0f0ce7fad9f4bf4e373ff176091b83fed381b9be9601cc8d5a337` |
| `AUDIT.md` | `bc60572c9346122333f2a57ef40bc816c0abb859e4af791939dc7fbbaa8886c4` |
| `PROGRESS.md` | `13fe8c2b204b80694e7df4a9ca7b265a9d56a13c66649d5d3abcf9981c01ee37` |
| `COORDINATION.md` | `d3a21b363c96f5cb6e245d3bcacdc9396fee53e9b1e4a1e26fdb928cd31a4b9a` |

Pinned delta artifacts, independently rehashed:

| Artifact | SHA-256 |
|---|---|
| V43G2 `RESULT.md` | `60b76bd4d3a8106a1e670073ae1f84218e1d62ae21187d79d75abf0bbcc48028` |
| V43G2 freeze file | `bbb8b2d637be3458c0e67cfabbe3fa594ea521cb6a1151a5a242f937abf4c04d` |
| compiler result | `fac99098b36f5875b53c8d66439f34ca59b47e7f25a58f97dba1cb8cddd3806c` |
| generated Singular | `7e3149d9ff89274e31c1068a51903dd9e2e716168141bd6286fa6b47d56328b3` |
| basis file (literal ASCII `1`) | `6b86b273ff34fce19d6b804eff5a3f5747ada4eaa22f1d49c01e52ddb7875b4b` |
| normal form (literal ASCII `0`) | `9a271f2a916b0b6ee6cecb2426f0b3206ef074578be55d9bc94f6f3fe3ab86aa` |
| V14R1 hostile review | `25e6a3f2e5a9bfdf0a34692cd49c2e7046b5b66374540bca1cbd99d3a08d5ee8` |
| V16R1 hostile review | `dc0b2d80aaee5086ec65d2dd567d8d7f86cf95a331c7f09b72b101a5783d5c73` |
| V46R1 promotion | `b862e00f6ac80c3392cd365aad5d7001608e2a75fabfa4a76bb2f4c751e1f0fc` |
| AS109 two-band producer | `7640715607c640beae845855feb261a0d207ea9409a1ccf8a915899f4461ec10` |
| AS109 two-band Sol review | `571c5e2bda00cf9221debcd43b26f635016a68622714bbf8bbb0e2a68f33525f` |

The basis hash is SHA-256 of the single byte `0x31`; the on-disk file
`generic_groebner_basis.txt` is that byte. The normal-form file is the
single byte `0x30`. Those hashes are content, not promotion.

No other `ideation-20260827T1104Z-*` submission, prompt, or log was
opened. In-progress V43G2/V47 review logs were not used as evidence;
the packet pins and `RESULT.md` are the charged objects.

History checksum against `20260827T0935Z` (do not re-propose): origin
funnel; two-fibre decision; selected-row one-sidedness; constructive
converter `q a1^(D+M s)`; N-duals as non-decision; lucky `J0`
specialization as an exponent floor-raiser; infinitesimal special-fibre
sandwich `7 <= N_{J0} <= N_17`; Faber-functor three-substitutions
dictionary; K00 mixed cokernel / six-row first-order obstruction;
unique-`AC` formula-level linker; AS109 top-two-band face plus W2/W3
gauge; Lee–Li row-9 retarget; primitive-group sidecar; Bernstein–Sato
idle probe; Jelonek component hygiene; duals-as-separators; generic
`L43` as non-receiver; dual-as-primal rewrite.

---

## 1. Fresh scan of every avenue family and every global gap

Legend: `U` unchanged, `R` raise, `L` lower, `O` reopen, relative to
the live `APPROACHES.md` table plus the 09:35Z synthesis, updated only
by this packet. Reasons only for non-`U`. Previous-round raises that
this delta does not strengthen are not re-voted.

| ID | Call | Reason if not `U` |
|--:|:---:|---|
| 1 | U | No new GGV corner, no cofinal `td` engine, no `3p` lemma. Ordered-chart news is not a packet/sheet. |
| 2 | U | V46R1 is already promoted infrastructure. The generic unit does **not** empty unique-`AC` chambers, does not cover ramified/`rho=0`/equality faces, and does not supply `G2-PSC`. Remaining work on this row is endpoint manifests plus uncovered faces, not another raise. |
| 3 | U | Strip/ODE scope unchanged. |
| 4 | R | After a provisional generic unit plus promoted special-fibre emptiness on `D(a1)`, every remaining on-family geometric point of the named unit-load collision sits in a nilpotent thickening of the terminal. V14R1+V16R1 make the unloaded germ locally solvable with forced even-slot vanishing; V17's finite-field first-order classes are nonzero. This is now the unique on-family formal-germ / algebraization object, not a parallel K00 curiosity. Still not a polynomial Keller map. |
| 5 | U | Amalgam rigidity still does not bite non-automorphisms. |
| 6 | U | Residue-A fibres remain the wrong one-place object. |
| 7 | U | A reachable mixed K00 germ would be how one *starts* an `A(F)` direction, not a reason to reopen the A(F)-as-object program. Connection only. |
| 8 | U | Inverse-Faber functoriality remains a collision generating function, not a truncation bound. |
| 9 | U | Conjecture E remains an unbounded implication ladder. Lee–Li compiler retarget stays idle-capacity only. |
| 10 | U | HC4 quintic module remains `NO LEVERAGE`. |
| 11 | U | Zhao/Mathieu ladder remains dead. |
| 12 | U | No new integer-to-infinity. AS109 two-band tautology is not a Wilson-face isolation. |
| 13 | U | DC(2) still strictly stronger than JC2. |
| 14 | U | No candidate non-surjective `A_1` endomorphism. |
| 15 | U | Burchnall–Chaundy dictionary still incomplete. |
| 16 | U | A Groebner unit is not a holonomic index. Bernstein–Sato stays idle-afternoon only. |
| 17 | U | Cubic-linear class in high dimension still contains genuine CEs. |
| 18 | U | Keep as a proof-side grading tool only. Graded CE hunting stays closed (Shaska). |
| 19 | L | The bounded top-two-band attack returned `TWO_BANDS_TAUTOLOGICAL`; W2/W3 gauge-conductor duplicates reviewed wild-symplectic/polar-conductor state and cannot change unrestricted exact-lift existence. Stop that face card and its duplicate. Residual `deg_y Q=6` remains open, so the avenue is not closed; its cheapest handle just died. |
| 20 | U | Tsuchimoto/p-curvature still consumes false PC(2)/JC(4). |
| 21 | U | Hensel noninjectivity of the seed is unchanged. No new 109-adic DVR lemma. |
| 22 | U | Siegel still points the wrong way. |
| 23 | U | Holomorphic analogue remains false. |
| 24 | U | Real Pinchuk maps still have nonconstant `J`. |
| 25 | U | Residue-A passports remain too generous. |
| 26 | U | Primitive-group `td` bound still has no proved support bound. Keep the no-contention sidecar; do not raise without a new group-theoretic handle. |
| 27 | U | Admissible splice diagrams still outrun algebraic realizability. |
| 28 | U | Dual-pencil defect remains `JUMP-ONLY / TYPE-FAIL`. |
| 29 | U | Ordinary `kappa(P)` still has no universal receiver. |
| 30 | U | ML invariant still does not see the embedding. |
| 31 | R | The generic conjunct of the reviewed two-fibre theorem is now an exact `Q(t)` unit for the frozen full corpus (**DISCOVERY**, not promoted). Together with promoted V42 special-fibre emptiness this *decides existence* of some `a1^N U(rho^2) in J` with `U(0)=1`, conditionally on V43G2 review and the pinned field bridge. The remaining object on this row is a displayed identity, not another decision computation. |
| 32 | U | Receiver emptiness is still Rees/routing/`H_K00`, not the three-generator secant collision unit. |
| 33 | U | Ordinary action residues remain `COSTUME`. W2/W3 was a duplicate of already-reviewed polar/wild-symplectic work; stopping it is not a raise. |
| 34 | U | No spare coordinate to absorb a 2D pole. |
| 35 | U | Dim-3 mechanisms remain structurally 3-dimensional. |
| 36 | U | Generic sparse/SAT remains dead; named F-only work is rows 19/21. Do not reopen ambient search because a chart closed. |
| 37 | U | Finite-field extras will not lift. Finite-field *probes of a Q-ideal* in §7 are method, not this census. |
| 38 | U | No tropical invariant is named. |
| 39 | U | No 10-line class attached to `J=1`. |
| 40 | U | Commutative `det=1` still does not give free invertibility. |
| 41 | U | Naive scaling remains falsified by the dim-3 CE. |
| 42 | U | Arrow still points the wrong way. |
| 43 | U | Ritt still collapses to GGV after Aut-reduction. |
| 44 | U | Moskowicz remains `REFUTED-AS-PROOF`. The residual `n=6` uses the repaired 2018 partial-`y` theorem, not the refuted prime-td paper. |
| 45 | U | Inverse PDE remains tautological. |
| 46 | U | Formalization cannot manufacture the finite universe; `jc2-lean` stays a read-only asynchronous verifier. |

Changed: `4, 31` raised; `19` lowered. Nothing reopened.

### Current global gaps (after the delta, not before it)

Still missing, and **not** supplied by a generic unit:

1. A replayed total identity `a1^N U(rho^2) in J`, `U(0)=1` (**certificate**, not existence).
2. Terminal/Taylor receiver emptiness, equivalently closure-first `H_K00`.
3. Honest source/landing coverage outside strict unique-`AC` on `D(rho)`.
4. Ramified `rho=0` deck/square fibre (on-family this now *collapses toward* the terminal thickening; it does not vanish).
5. Equality/load faces, unique-`RC`/`R3`, `k=0`, off-family timing.
6. Global `G2-PSC` and any invoked `G2-BD`.
7. Cofinal degree/type ceiling; a reduction of the whole Jacobian problem to this finite frontier.
8. An explicit characteristic-zero counterexample: residual AS109 `deg_y Q=6`, unbounded-total `(8,12)`/`(9,12)`, or a mixed-load algebraized K00 germ.

TD6 original-FIRST plus total-`F` (H19R2) remains an independent SP-2/td6 gap, not this packet's critical path.

---

## 2. Strongest implication of the generic unit, and the fastest certificate path

### Unconditional (what V43G2 actually is)

**DISCOVERY, packet-narrow.** For the frozen grade-through-19 ordered-`a1`
literal-total ideal `J`, after `a1=1` and the exact linear `ez9` pivot
`Tg19_2:(3/8)t a1 ez9` with `t=rho^2`, the remaining 58-row / 64-variable
ideal over `Q(t)` has complete reduced `dp` basis literally `1`, and
`reduce(1,G)` is literally `0`. Peak RSS 19,616 KiB, wall 0.10 seconds.
Censuses claimed: 70 named slots, 59 nonzero rows, 66 positive variables,
65 on the `rho=0` face, sole general-only variable `ez9`, eight rows
vanishing at `rho=0`
(`Tg11_2,Tg11_3,Tg11_5,Tg11_7,Tg12_5,Tg12_7,Tg13_7,Tg14_6`).

By the pinned injective degree-two faithfully flat extension
`Q(t) -> Q(rho)`, `t |-> rho^2`, this is exactly the generic `Q(rho)`
conjunct of the reviewed two-fibre theorem:

```text
a1 in sqrt(J Q(rho)[X])
  <=>  J|(a1=1) = (1)  in Q(rho)[X\{a1}].
```

It is **not** `K+(rho)=(1)`, **not** a displayed `N`, **not** a
polynomial identity in `J`, and **not** persistence after later rows
in any sense stronger than “later rows can only make membership
easier.” The `ez9` pivot *divides by `t` over `Q(t)`*. Every
denominator, every positive `t`-valuation, and every weight class
modulo `wt(a1)=5` is still hidden in the unexported multipliers.

### Conditional (what it becomes if the two-fibre inputs hold)

**PROVISIONAL, usable under label `DEP-V43G2`.** The reviewed
equivalence is

```text
K+(rho)=(1)
  <=>  [generic unit] and [special unit]
  <=>  exists N, U, U(0)=1, a1^N U(rho^2) in J.
```

V42 already supplies the special conjunct (promoted, unsplit existence
on `D(a1)` at `rho=0`, not the displayed branch exponent 8). V43G2
supplies the generic conjunct as a producer-PASS exact unit. Therefore
**existence** of a total-rho certificate for this frozen chart is a
provisional theorem. The campaign's promotion object remains the
literal replay.

Do not wait for hostile review to *think* this, or to launch labeled
descendants. Do wait to *state it as promoted*, and keep a named
rollback subtree (§3).

### What this does *not* imply, even after promotion

- Terminal receiver emptiness.
- Source reachability of any K00 jet.
- Unique-`AC` chamber emptiness, V46R1 endpoint union, or V47.
- Ramified-deck comparison as a global square theorem.
- `G2-PSC`, `G2-BD`, a `td` bound, order two, maximum twelve, JC2.

If the total identity later promotes, the two registered `J2` charts
are closed and the landing critical path is the terminal plus coverage.
That sentence is the *correct* collapse, and it is still not Gate T.

### Fastest path to a certificate-grade object

V43G3 is already the right first job: independent byte-compare of the
58-row `Q(t)` script, `liftstd`, `J*T=G`, `J*C=1`, serialize all 58
multipliers. Its own preregistration correctly **does not**
rehomogenize. Do not enlarge that job.

Fastest successor, in this order, each fail-closed:

1. **Read `s = v_t(cleared cofactor)` from the serialized `C`.** This
   is a desk-to-tiny-AWS parse, not a second Groebner. The `ez9` pivot
   makes `s >= 0` the expected generic case, not an anomaly.
2. **If `s = 0` and some coefficient of the rehomogenized form is a
   unit at `t=0`:** weight-project modulo 5, rehomogenize, clear
   content, replay against the original 59 literal rows. The special
   tree is then a consistency check, not an input. Stop waiting on
   `a1^104`.
3. **If `s > 0`:** combine with a special identity
   `a1^M - B = t H` by the reviewed converter
   `q a1^(D+M s) in J`. The live candidate `M=104` is usable only
   after its own serialized zero. If `a1^104` is still replaying, a
   *weaker displayed special power* (V42's existence, or the 17-row
   dehomogenized unit after a hash check that those rows lie in `J0`)
   already proves *some* `M`; the converter can run on that `M` as
   soon as it is polynomial, and 104 is only a candidate for a small
   explicit `M`.
4. **Do not reconstruct 192-GiB Nullstellensatz multipliers.** The
   0.10-second unit is evidence that the Bézout identity is small. If
   `liftstd` wants a tebibyte, that is a software failure of the lift,
   not a reason to revive high-memory extraction.
5. **Independent engine in parallel, not in series (§7).** A
   Singular-free specialization attack can refute the unit before
   V43G3 returns. It cannot promote.

The 17-row special unit and the N-ladder are not on this path. V43G1
stays as a cross-check until V43G3 PASS plus row replay, then archives.

---

## 3. Successor trees

### Tree A — total certificate PASS
(V43G2 review CONFIRMED, V43G3 `J*C=1`, rehomogenized replay zero,
converter replay zero if `s>0`, different-model review of the identity
PASS)

1. Promote the generic unit at its exact `Q(t)`/`Q(rho)` scope, then
   the total identity `a1^N U(rho^2) in J`, `U(0)=1`.
2. Close the frozen ordered `T-a1` chart. Together with promoted
   `a0^3 in I` this closes both registered `J2` charts for the named
   unit-load family, grade-through-19 source.
3. **Stop:** N-duals; unseeded N7; V43G1 as a live lane; selected-row
   generic as discovery (retain one replay as a sparse-identity
   control); high-memory multiplier extraction; any slogan that the
   terminal died.
4. **Immediate landing retarget:** closure-first `H_K00`, mixed
   six-row first-order load cokernel (exact Q of V17), honest
   `Lambda<=19` source/target/`Jdet` reachability, equality/load/`k=0`
   faces. Unique-`AC` continues as endpoint manifests plus V47 review,
   not as a J2 chart.
5. **Rollback if a later review reversal hits the identity:** un-close
   `T-a1`, restore the certificate lane, keep receiver work (it was
   already load-bearing under the origin funnel). Do not restore
   N-duals unless the reversal specifically refutes the generic unit.

### Tree B — replay or review failure

Split by *which* object failed. These are not the same rollback.

**B1. V43G2 review REFUTED or independent specialization finds a
nonunit (mathematical false unit).**

- Un-use `DEP-V43G2`. Do not speak of a two-fibre decision.
- Run the §7 finite-field `t=c` attack to completion if not already
  the refutation.
- If the 58-row system is a genuine nonunit over `Q(t)`, the frozen
  chart has a generic-fibre survivor on `D(a1)`. Origin-funnel
  parametrization from the terminal becomes the *negative-branch*
  producer (already scored). Unhold grade-20+ only to cut that
  survivor, as navigation, prefix-relative.
- Special fibre remains empty on `D(a1)` (V42). That pair is now the
  interesting geometry: empty special, nonempty generic.
- V43G1, selected-row, and constructive `a1^104` remain; they cannot
  override a full-system generic nonunit.
- Do **not** revive the per-N ladder as an existence method. A
  generic nonunit *is* the existence decision, negative.

**B2. V43G2 holds (or is still provisional) but V43G3 fails
closed** (source drift, `liftstd` timeout/OOM, `J*T=G` or `J*C=1`
fails, nonunit inconsistent with V43G2).

- Existence remains provisionally usable under `DEP-V43G2`.
- The failure is a lift/software problem. Next instrument is not a
  bigger RAM cap as a first reflex. The original standard basis was
  0.10 seconds / 19 MiB; a tebibyte lift is a bug in reconstruction
  (wrong ring, dropped parameter, pivot row omitted from the source
  map, or `liftstd` on a non-global ordering).
- Alternatives, AWS only, one at a time: `lift`/`syz` of `1` in the
  same frozen ring; reconstruct a specialized Bézout identity at
  several `t=c` and attempt rational reconstruction of multipliers;
  selected-row *unit* subideal as a smaller lift (one-sided, but a
  unit subideal gives a legal identity in the full ideal).
- Keep `a1^104` running; it is the special input, independent of the
  generic lift.
- Named rollback of descendants that consumed a *displayed* identity:
  none should have launched. Descendants that consumed only existence
  (`DEP-V43G2`) stay.

**B3. Identity replays, hostile review GAP/REFUTED** (weight-mod-5
miss, homogenization of an inhomogeneous slice, `U(0)=0` smuggled,
converter used with a non-polynomial `M`, row-manifest mismatch).

- Fail closed. Do not promote. Keep the serialized `C` as a debug
  object. Repair the exact failing step; do not rerun the 59-row
  decision.

**B4. `s>0` and special certificate absent.**

- No total identity yet. Existence still provisional. Do not invent
  `U(0)=1` from a `t`-divisible cofactor (already refuted as a
  Bézout-implies-unit claim). Wait for a polynomial special `M`, or
  extract `M` from the 17-row unit after custody. Inconsistency
  sentinel: if the same-ideal special control would fail, V43G3 must
  not report success.

---

## 4. New mechanisms (history-deduplicated)

Apparent novelty is labelled. The coordinator's checksum should test
these against colon/saturation notes, the staged converse `593f953b...`,
the origin funnel, the converter, K00-MIXED-COKERNEL, and “ramified
fibre is Cards A–B.”

### M1. Rho-only deformation module, then a `T-a0`-style sparse identity
**(proof-side, PROVISIONAL)**

`J` differs from its special-visible part by eight rows that vanish at
`rho=0`, plus the unique `t a1 ez9` term. After `a1=1` and the `ez9`
pivot, the generic unit is equivalent to

```text
I_vis + I_def = (1)  in Q(t)[X'],
```

where `I_def` is generated by the eight rho-only rows and `I_vis` by
the rest. If `I_vis` is already `(1)`, selected-row lanes should have
seen it; the interesting case is that `I_def` generates the unit ideal
in the generic fibre of the special-visible quotient.

This is not “try a selected subideal hoping for a unit” (already live,
one-sided). It is the *converse structure of a known full unit*: the
Bézout support is predicted to concentrate on those eight rows. After
V43G3 serializes `C`, the Boolean support of the eight deformation
multipliers is a discovery, not a search. Hunt a closed-form identity
in that support, analogous to the thirteen-row `a0^3` certificate that
closed `T-a0`.

Cheapest discriminator: once `C` exists, print eight valuations and
eight “is identically zero?” bits. If five of eight multipliers vanish,
the certificate lives in a three-row deformation. If all eight vanish,
`I_vis` was already a unit and a selected-row replay is the certificate.
Stop: one support census plus at most one sparse-identity attempt by
hand/exact arithmetic; do not launch a new 58-row Groebner to sparsify.

Dedup: selected-row unit; converter; constructive cascade; origin
funnel. The new piece is support-localization of a *known* unit onto
the deformation rows, aimed at an `a0^3`-style theorem rather than a
58-line `liftstd` dump.

### M2. Hensel degeneration of unique-`AC` into the terminal
**(cross-avenue, PROVISIONAL)**

Unique-`AC` on `D(rho)` needs two distinct Hensel roots, hence
`lambda_0 = epsilon * rho ≠ 0`. The formula-level linker therefore
cannot see `rho=0` (already scored). The new map is the *limit*, not
the linker:

a unique-`AC` contact family parametrized by `rho` has, if the jets
stay finite, a collision limit as `lambda_0 -> 0`. Under `DEP-V43G2`
that limit cannot have `a1` invertible, because the generic ordered
chart is empty on `D(a1)`. Combined with promoted `a0^3`, it cannot
have `a0` invertible either. The only on-family landing of such a
limit is a point of the `J1=J2=0` thickening, i.e. a K00 jet.

So: unique-`AC` chambers that admit a finite `rho -> 0` jet supply
*initial data for the mixed terminal germ*. Chambers whose jets blow
up do not give polynomial limits and remain ramified in the base.
This is not stage-zero Kummer composition (refuted: that source map
killed `J1+J2`). It uses actual unique-`AC` families and a genuine
limit, and it does not transport V46R1 identities to `rho=0`.

Cheapest discriminator: pick one promoted unique-`AC` endpoint with a
complete polar inventory; substitute the explicit `lambda_0`-series;
read the orders of `a0,a1` as `rho -> 0`. If both orders are positive,
record the leading mixed-load jet and feed it as a K00 reachability
test vector. If either order is zero, that chamber cannot degenerate
into the terminal and is not a receiver input. Stop: two endpoints
(one low contact, one load-wall). Fail closed on naive `p=-2 rho^2`
without connection jets.

Dedup: origin funnel (support geometry for either generic outcome);
“ramified fibre is not a unique-`AC` contact”; V46R1 scope firewall.
The new piece is a one-way degeneration *from* unique-`AC` *to* K00
initial jets, licensed only on the positive generic-unit branch.

### M3. Independent dual-vanishing / `t=c` nonunit attack on the 0.10-second unit
**(disproof-side, PROVISIONAL as a card; the attack itself is standard)**

A Groebner basis `{1}` in 0.10 seconds over `Q(t)` in 64 variables is
either a genuinely tiny identity or a compiler/engine false unit
(dropped row, illegal `t`-pivot, parameter-ring bug, AppleDouble-not-
the-issue but source drift is). `liftstd` in the *same* Singular
session is not an independent engine.

Attack, AWS, flint or a second Singular over `F_p`, never local:

- Freeze the 58 dehomogenized polynomials by hash.
- For several good primes including `65521` / `65519`, draw
  preregistered `c in F_p^*` (never `c=0`: that is the special fibre).
- Compute a standard basis of the specialized 58-row ideal over `F_p`.
- **Any nonunit at any `c ≠ 0` refutes V43G2** (a unit over `Q(t)`
  remains a unit after almost every specialization of `t`).
- Optional complement: Macaulay dual of the same specialized system.
  A nonzero dual at any degree is the same refutation. Dual vanishing
  through a registered degree bound is corroboration, not promotion.

This is the opposite of the V43 `a1^6` dual (which proved
*nonmembership* in `J0`). Using dual vanishing / specialized units to
*stress-test a claimed unit* is not a campaign card I can find.

Stop: one nonunit (rollback Tree B1), or a registered number of random
`c` (e.g. 16) plus two primes without a nonunit (corroboration only).
Do not treat sixteen units as `K+(rho)=(1)`.

Dedup: lucky `J0` specialization (different ring, different variable,
different purpose: it raised `N`, it did not attack a `Q(t)` unit);
V43 dual; selected-row modular lanes.

### M4. Obligatory first-order mixed load as the unique remaining on-family CE
**(disproof-side, PROVISIONAL; descendant of K00-MIXED-COKERNEL, not a duplicate of it)**

K00-MIXED-COKERNEL already said: a nonzero six-row first-order class
is the first load-aware compiled exclusion. What this packet adds is
*uniqueness after J2 collapse*, plus a stop on waiting for V18.

Under `DEP-V43G2` plus promoted `T-a0` plus V42, there is no remaining
on-family point with `a0` or `a1` a unit. V14R1 says the unloaded
normalized germ is locally solvable. V16R1 says every local
representation has `q2(0)=q4(0)=q6(0)=0`. V17 finite-field control
says the individual `k10,k6,k2` first-order classes in the six-row
quotient are nonzero. Exact Q of V17 is still live.

If exact Q confirms those three classes, the unloaded formal germ is
already dead at order one, **before** V18's first cumulative filtered
break. The unique remaining on-family CE candidate is a three-parameter
mixed-load infinitesimal of K00, which must still be intersected with
honest `Lambda<=19` source/target/`Jdet` reachability. V18 remains the
right tool for the *compiled* filtered leading class, not for deciding
whether an unloaded germ exists.

Cheapest discriminator: finish exact-Q V17. Nonzero on all three
directions: stop any unloaded Padé/D-series algebraization; feed the
three classes as the only first-order CE ansatz. Some class zero over
Q: that direction remains an unloaded first-order modulus and V18 is
needed there. Stop: V17 exact Q plus one reachability intersection.
Do not infer a germ from a compiled nonzero class. Do not infer a
polynomial map from a formal mixed germ.

Dedup: Card B attainability of `M2,M4,M6(0)`; V16R1 promotion; packet
text on V17/V18. The new piece is the uniqueness claim after the
generic unit, and the instruction not to wait for V18 to kill the
unloaded germ.

---

## 5. Launch / continue / hold / stop, and top-five allocation

| Lane | Call | Exact stop |
|---|---|---|
| V43G2 generic unit as a *decision* | **continue as provisional `DEP-V43G2`** | Stop *speaking of it as promoted* until different-model review. Rollback Tree B1 on REFUTED. |
| V43G3 `liftstd` / `J*C=1` | **continue, do not enlarge** | PASS with both matrix replays, or FAIL_ / timeout / source drift. One RAM retry only if peak RSS of the *decision* (19 MiB) and the lift differ by a factor suggesting a ring bug, not “needs 1 TiB.” |
| Rehomogenize / weight-mod-5 / clear / replay | **launch as V43G3 successor, labeled** | One replay of the frozen 59 rows. If `s=0`, do not wait on 104. |
| Constructive `a1^104` | **continue** | Needed iff `s>0`. Stop as a *decision* lane; it is converter input `M`. No claim until serialized zero. |
| V43G1 redundant special-first full run | **continue until V43G3+replay PASS, then archive** | Do not retarget. |
| N-duals, unseeded N7, 192-GiB multipliers | **stop** | Existence, if V43G2 holds, is decided. Floor `N<=6` remains a consistency gate only. |
| Selected-row generic | **hold as discovery; keep as sparse-identity control** | A selected unit is already implied by the full unit. A selected nonunit proves nothing. |
| K00 D9+ | **stop permanently** | V14R1: `h=63 d4+20`, `h(0)=20`, local membership, hence all finite `(d)`-adic truncations compatible. |
| V17 exact Q first-order six-row classes | **continue, raise priority** | Nonzero on all three: unloaded germ dead at order one (compiled). Zero on a class: that direction stays for V18. Then honest `Lambda<=19` intersection. |
| V18 cumulative filtered break / exact dual | **hold until V17 exact Q** | Must still be intersected with source reachability; do not exclude an arc from a compiled class alone. |
| Mixed `Lambda^19` / `mu` / `Jdet` / `H_K00` | **continue, now the landing critical path under `DEP-V43G2`** | Restriction-first remains a negative control, not a substitute. Rollback of the *J2-closed* reading if Tree B1. Receiver work itself does not roll back (origin funnel). |
| V11/V13 local-order repair | **stop** | Software `G=I*T` failure; one repair already expired as a strategy. |
| V46R1 unique-`AC` infrastructure | **already promoted; no new exporters** | Serial `ACT-TOT-G22/G24+` stay off the critical path. |
| V47 fail-closed three-row linker | **continue review; unpromoted** | Do not replace any endpoint before different-model review. Mutation and `k2c` alias guards stay mandatory. |
| Unique-`AC` endpoint manifests | **launch as slots free** | Per-contact frozen polar/load/target inventory. `(8,3,8)` stays unpromoted. Stop at named equality/ramified/`k=0` faces: those are not V46R1. |
| M2 Hensel degeneration | **launch desk-scale, two endpoints** | Stop after two; fail closed on naive `p=-2 rho^2`. |
| Equality / unique-`RC`/`R3` / `k=0` | **continue narrowly** | Not licensed by V46R1 or by the generic unit. |
| AS109 two-band face and W2/W3 gauge | **stop** | `TWO_BANDS_TAUTOLOGICAL`; W2/W3 cannot change unrestricted exact-lift existence. Residual `deg_y Q=6` is neither constructed nor excluded; do not spend a third band without a new mechanism. |
| AS109 `n=2..5` and prime target degrees | **stop** | History-closed. |
| Primitive-group census | **continue as no-contention sidecar** | Orevkov `(48,64)` control. No AWS algebra quota. |
| Lee–Li row-9 retarget | **hold, idle-capacity only** | Unchanged. |
| Bernstein–Sato | **hold, idle afternoon only** | Circular unless the hypersurface is pinned. |
| Jelonek / ambient random search | **stop** | Citation firewall only. |
| Full `3P-E31` | **hold** | Global-minimal scope firewall. |
| Raw receiver exports / generic `L43` | **stop** | Unchanged. |
| Dual-as-primal rewrite | **stop** | Unchanged. |
| Graded CE hunting | **stop** | Shaska. |
| Stage-zero Kummer composition | **stop** | REFUTED. Finite-flat base change stays. |
| H19R2 original-FIRST / total-`F` | **continue** | Independent of this packet. Not whole-TD6. |
| Serial TD6 `q` rows / serial affine-Faber `H` | **stop** | Prior-round stop stands. |
| `jc2-lean` | **do not touch** | Read-only nested boundary. |
| Broad web sweep | **unchanged clock** | This round does not reset `2026-08-28T00:00Z`. |

### Correctness-adjusted top five

The 09:35Z 40% ordered-`a1` decision budget is spent as a *decision*.
What remains on that row is a small lift plus replay.

```text
30%  terminal mixed deformation, honest reachability, H_K00
20%  V43G3 lift, t-valuation read-off, rehomogenized replay, converter
     if s>0, plus the independent t=c attack
20%  unique-AC endpoint manifests, V47 review, equality/ramified/k=0
     coverage, M2 degeneration (desk)
15%  orthogonal CE: residual AS109 n=6 without two-band/W2W3,
     (8,12)/(9,12), primitive-group sidecar, TD6 H19R2
15%  hostile review, software gates, integration, web surveillance
```

Review remains nonblocking. `DEP-V43G2` descendants may start under
that label. A review reversal rolls back only the dependent subtree
named in §3.

---

## 6. Attacks on scope inflation

These are the inflations this packet most invites. Each is false.

**Ordered-chart closure versus terminal receiver.**
A promoted `a1^N U(rho^2) in J` empties the frozen ordered `T-a1`
chart on `D(a1)`, in both fibres. Points with `a1=0` are exactly the
points the saturation was designed to discard. The terminal is
`V(J1+J2)`, possibly thickened. V42+V43G2 together make those points
the *only* remaining on-family candidates; they do not empty them.
`Phi7` killing the literal `K00` section on `D(sigma*Jdet)` still does
not make the blowup fibre empty. `H_K00` remains the incidence object.

**Source reachability.**
V14R1 is unloaded, normalized, local-at-`m`, `C6=1`. V16R1 is the
order-zero representation space of that same chart. V17 is a compiled
six-row cokernel, finite-field as control. None of these is the owner
source `pTot = -2 rho^2 + 2 sigma ell_1 + ...`. A nonzero mixed class
that is not in the honest `Lambda<=19` image is a compilation costume.
Naive `p=-2 rho^2` remains an illegal emitter.

**Coverage.**
V46R1 is a coefficient-ring identity plus typed root maps on `D(rho)`.
It does not supply endpoint manifests, non-unique faces, `rho=0`,
equality/load faces, receiver coverage, G2, or Gate T. V47, even after
PASS, replaces at most twelve baselines plus four raised subtails, and
only after hostile review. Eleven (or twelve) promoted unique-`AC`
endpoints are not a square cover and are not a Rees cover.

**G2.**
`G2-PSC` is packet/sheet transport into a decorated Sigray tree.
`G2-BD` begins only after a residue-A datum. Closing two `J2` charts
of one named unit-load family in a maximum-twelve / order-two source
does not transport a GGV corner and does not bound delay. Keep the
labels unambiguous. A pure Sigray campaign that wants to ignore GGV
still owes Sigray source, complete landing, and type/total-degree
control; this packet supplies none of those.

**JC2.**
Even the strongest Tree A promotion closes two registered charts of
one family at a frozen grade. It does not close off-family `k10=0`,
does not close all types, does not give a cofinal `td` ceiling, does
not algebraize a germ, and does not exclude `(8,12)`, `(9,12)`, or
AS109 `deg_y Q=6`. Stating `K+(rho)=(1)` as promoted before V43G3
replay is the same inflation one step earlier: existence is not the
campaign's promotion object, and existence itself is still
review-gated.

**Further inflations specific to this unit.**

- `Q(t)` unit after `a1=1` is not a polynomial identity: the pivot
  inverts `t`, dehomogenization inverts `a1`, and weight-mod-5
  projection is load-bearing.
- Basis `{1}` in 0.10 seconds is not a displayed exponent and is not
  an excuse to skip `liftstd`.
- `a1^104` is not a theorem.
- V42's `a1^8` is a branch ideal, not the unsplit special identity.
- AppleDouble files in the source manifest are hashed and unread by
  the compiler, per `RESULT.md`; that sentence is a custody claim for
  review, not a mathematical hypothesis.
- Finite-field agreement at specialized `t=c` does not promote a
  `Q(t)` theorem.

---

## 7. Software / compute acceleration (AWS only)

**Name.** `V43G2-T-SPECIALIZATION-ATTACK`

**Purpose.** Independently try to *refute* the 0.10-second `Q(t)` unit
with a second engine, without touching the local Mac and without
duplicating V43G3 `liftstd`.

**Input (hash-pinned, read-only).** The frozen V43G2 generated
Singular and the 58 dehomogenized polynomials it actually ran; compiler
result `fac99098...`; generated script `7e3149d9...`. Byte-compare
before any solve. If the harvest disagrees with the freeze, fail closed
and do not compute.

**Job, one spare AWS box, flint or a fresh Singular, several good
primes `{65519,65521,...}`.**

1. Parse the 58 polynomials over `F_p[t][X']`.
2. For a preregistered list of `c in F_p^*` (length 16, never 0),
   specialize `t |-> c` and compute a reduced `dp` basis.
3. Emit `UNIT` / `NONUNIT` / `FAIL_` per `(p,c)`, with basis-size,
   peak RSS, and a one-coefficient mutation control: corrupting a
   single input coefficient of a known row must not leave a unit if
   the uncorrupted specialized basis was a unit *and* the mutation is
   visible in that fibre. (If the mutation is absorbed, record
   `MUTATION_ABSORBED` and do not treat that `c` as a control.)
4. Optional cheap complement on the same box: for two of those
   specialized fibres, compute a Macaulay dual through a registered
   degree equal to the maximum input row degree. Nonzero dual =
   `NONUNIT`.

**Outcomes.**

- Any `NONUNIT` at `c ≠ 0`: Tree B1. V43G2 cannot stand. Stop the
  certificate converter. Keep V42. Unhold grade-20+ only as survivor
  navigation.
- All registered `(p,c)` return `UNIT` and mutations fire: Singular-free
  corroboration of the generic conjunct, **not** a promotion, **not**
  `K+(rho)=(1)`. V43G3 remains the certificate path.
- Timeout/OOM on a specialized 58-row system over `F_p`: no verdict
  (the `Q(t)` run was 0.10 seconds over `Q(t)`, so this should be
  smaller; failure here is itself evidence of source drift).

**What this is not.** It is not a second `liftstd`. It is not the old
`J0` exponent-floor specialization (that specialized *variables other
than `a1`* in the special fibre). It does not invert `t=0`. It does
not run on the Mac. It does not edit ledgers.

**If V43G3 returns first.** A second, tiny job parses the 58
multipliers, prints `s = v_t` of each, prints the Boolean support on
the eight rho-only rows (M1), and hands the rest to the existing
weight-mod-5 homogenizer. That job is validators-plus-census, not a
new solver.

---

## 8. What this round does not do

Nothing here proves or disproves JC2, Gate T, order two, maximum
twelve, either `G2` obligation, source/landing coverage, or a terminal
receiver. V43G2 is a provisional exact generic-fibre unit, not a
promoted total identity. V14R1 and V16R1 are unloaded local theorems.
V46R1 is infrastructure. AS109 two-band work is stopped as tautological.
The two registered `J2` charts are not both closed until a total
`T-a1` identity promotes.

The strongest exact news in this packet is local and positive in the
useful sense: the last open `J2` chart has a producer-PASS generic
unit, so existence of a total-rho certificate is a labeled provisional
theorem, and the landing budget should move to the terminal *under
that label* rather than wait for fifty-eight multipliers to become a
religion. The multipliers remain the promotion object.
)
