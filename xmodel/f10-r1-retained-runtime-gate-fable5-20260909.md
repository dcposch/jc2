# F10 r1 retained-row runtime packet: narrow static gate over the new caller and finite controls

2026-09-09, Fable 5.1 (claude-fable-5-1). STATIC REVIEW ONLY. Launch 17:39:44 UTC; controlling stop 17:58:00 UTC (the original absolute stop, earlier than launch+20 min = 17:59:44 UTC). No compilation, import, execution, test, fixture, mathematical subprocess, CAS, script or arithmetic replay of any size occurred. Runtime tests = 0, transformed artifacts = 0, controls executed = 0, workers allocated = 0, caps filled = 0. Nothing here is a runtime acceptance, a dispatch registration, a solver licence or an ideal decision.

## Custody and read scope

All twelve charged inputs were hashed in /tmp/jc2-lane.OkZDsH/inputs with sha256sum before any read; all twelve digests matched the ordered list in the charge (12/12). Root custody first: both owned targets were absent at launch (ls refused both paths), the frozen basis 0d39df3c9fd69c939a8420c54d03228b9077777d of input 2 equals the HEAD I recorded, and the first 6191 bytes of input 1 hash to the body digest 9f9ae25026aeb47b0eb94987d67ecb4ab61ef62305559916d00ced243a54211b recorded in input 2 (custody, not mathematics). All twelve inputs were read WHOLE with cat -n. Inputs 7, 8, 9 and 12 carry exactly the hashes that input 3 pins at dispatch_batch.py:164-168; the transformer, checker and helper hashes equal the prior gate's accepted rows 3-5 (input 11), and the helper and probe hashes equal the old caller's pins (input 10, lines 109 and 111; the old caller pinned a different, superseded checker at line 108 and no transformer). They are therefore treated as unchanged accepted copies and are not re-proved. Input 10 was checked hunk by hunk against input 3: every + line appears verbatim in input 3 at the expected line, and no hunk touches the dummy identity block (input 3, lines 110-133) or quiet() (31-39). CAPRUN 4435279d is an unchanged external premise; its body was not read. No other body, exact.json coefficient, corpus, provenance, network, AWS, SSH, process census, agent or worker was touched. Owned outputs: this file and box/f10-r1-retained-runtime-gate-fable5-20260909/ only; no source or harness was modified. Tools: date, ls, sha256sum, head -c piped to sha256sum, git rev-parse, mkdir, cat -n, wc, grep, comm, and apply_patch for this report.

## A. Caller migration: STATIC CONFIRMED (no defect found; inherited limitations named)

Finite eleven-file vector. dispatch_batch.py:163-171 pins eight named files by constant (transformer, checker, helper, CAPRUN, probe, exact.json, admissibility, frontier); line 173 adds itself, semantic_controls.py and /usr/bin/python3; line 174 demands set equality with root's registration keys, so the vector is exactly eleven, no more and no fewer. Lines 175-176 force the eight constants; lines 177-178 force every supplied entry to be a 64-character string equal to the actual digest, which is how the self, harness and interpreter pins are enforced without an embedded self-hash. The registration file itself is added to fixed_files at line 179 and therefore hashed in every child (execution_gate.py:68-70) and post-checked per operation (line 97). External pinning of the registration is root's duty (input 6, lines 11-12), as charged.

Physical checks. Line 143 demands the caller itself runs -I -B; line 144 demands cwd equals the resolved ROOT and equals the registered cwd string; lines 146-148 demand DMI vendor Amazon EC2, board_asset_tag equal to the registered instance and hostname equal to the registered one and not math-hq. The child re-checks vendor, i- prefix, instance and host (execution_gate.py:25-30). A null instance or hostname in the registration is a refusal at 147/148, and the template (input 5) is refused earlier at 141-142 because enabled is false. No inherited worker or deadline remains: input 10 lines 7-10 and 103-112 remove the hardcoded ROOT, instance, job, deadline, hostname ip-172-30-0-72 and interpreter constant; the interpreter is now root-registered, by design.

Argv authority unchanged. Child and parent argv (lines 53-61) are byte-for-byte the old construction (input 10, unchanged context lines 39-43) and are stored in the per-operation authority (73-77); the helper compares both literally (execution_gate.py:33-57). Operation labels match each child: build for transformer.py:212, check for checker.py:302, semantic_controls.py:16 and probe.py:6.

Exclusive fresh outputs and frozen authority. Lines 71-72 refuse if any of the seven per-operation files pre-exists; 180-181 refuse if any of the six job outputs pre-exists; all caller writes go through open('x') (21-23, 88). The authority is written exclusively, chmod 0444 and hashed (83-85), rehashed after return (98) and its pre-hash is what the record stores (101). Sentinel and fixture paths are not in the pre-existence lists, but probe.py:7 and semantic_controls.py:53 open them with 'x', so a stale file yields a nonzero child exit and STOP at line 135, never a false PASS; that is fail-safe, not a hole.

Pre/post pins. Fixed files are hashed once at 178 and re-checked after every operation at 97; a fixed trailing input is re-hashed before the operation at 68; new trailing inputs (retained.json at full-check, retained.json and full.receipt.json at semantic-controls) are hashed at 70 and enter the child authority, so the helper hashes them again before any work. Between full-check and semantic-controls, retained.json is re-hashed fresh rather than carried, but semantic_controls.py:41-44 binds it to the genuine receipt's retained_sha256, which closes that gap under the cooperative rule.

Cutoffs. Lines 150-154 require two explicit +00:00 strings and store DEADLINE as their minimum; 155-156 require an integer aggregate cap. Line 46 is the fifteen-second admission against the stored absolute cutoff for every operation; 47-51 tighten mathematical operations to min(DEADLINE, first_math+AGGREGATE); line 106 refuses on return at or after the stored cutoff for every operation, a tightening versus the old mathematical-only test (input 10, lines 84-85). Bool cannot pass as int at 156 and 161 because type() is used.

Same sequence. Lines 182-186 run the five defects, gate-valid and dummy in the reviewed order with root profiles; dummy RSS is pinned to 33554432 at 162 against the probe's 64 MiB descendant. I traced each defect to its helper refusal: disabled hits execution_gate.py:19-20; wronghost 29-30; extra argv 41-43; rss '1' hits 56-57 as an argv mismatch (the string passes 58-61); missing-input deletes the frontier.json key (line 82, same string form as the fixed key because ROOT is resolved) and hits 66-67 since the probe registers as check with frontier.json as argv[2]. Every refusal precedes any file write, and 108-109 demand nonzero NORMAL_EXIT plus no sentinel. The dummy block 110-133 and the PGID/cleanup checks (96, 111-117) are unchanged.

Inherited limitations, not new vulnerabilities and not hardened here: defect refusals are typed only as nonzero exit, the stderr reason string is not asserted (unchanged from the accepted caller; gate-valid running afterwards on the same host exposes any environmental refusal); sampled RSS and cleanup overshoot; cooperative no-concurrent-writer rule; the second unpinned authority read (transformer.py:215, checker.py:305 and likewise semantic_controls.py:30) is the accepted GAP-C1 pattern, deliberately DEFAULT-OFF. Two staging notes for root, neither a code change: stage exact.json as a regular file, not a symlink, or line 66 registers it twice under the resolved path; no file named plain or descendant may sit in ROOT or line 65 will hash it harmlessly into the authority.

## B. Genuine transform, checker.main and harness order: STATIC CONFIRMED (all twelve failure paths traced)

Order and argv. dispatch_batch.py:188-192 runs transform, then full-check, then reads the receipt verdict, then semantic-controls; no control precedes the genuine positive. Argv lengths match every entrypoint: transformer.py:210 expects 4, checker.py:300 expects 5, semantic_controls.py:14 expects 6, and the caller supplies exactly those trailing counts. The receipt fields the harness consumes (verdict, source_sha256, retained_sha256, checker_sha256, original_slots, retained_slots, full_bracket_bands, inverse_pole_slots) are all emitted by checker.py:295-296 and 317-320. Producer binding is transitive: checker.main:313-315 ties implementation_sha256 to the registered transformer pin, and the harness binds to that receipt through the retained hash at 41-44 and re-checks the original against SOURCE_SHA at 39 and the checker code against its registration at 40.

Authority before work. semantic_controls.py imports only sys, pathlib and the helper at top level (6-10); authorize() is line 16; copy, hashlib, json and checker are imported at 18-21 only afterwards. Importing checker executes no mathematics (checker.py:1-12 plus function definitions; main is guarded at 327). The helper's check-side rule at execution_gate.py:66 sees exact.json as argv[2] for the harness, which is a fixed pin.

No stale-hash substitute. reject() (49-69) deep-copies, mutates outside the try, writes the fixture with 'x', hashes it, rereads it from disk, demands equality with the mutated object, and feeds the reread object directly to checker.verify, which contains no artifact-hash logic at all: its only binding is the constant source_sha256 field at checker.py:41, which every fixture keeps. A stale-hash refusal is therefore impossible inside verify. Acceptance raises 'changed object accepted' (65); a wrong class or string raises with the observed class and message (62-63); fixture drift is refused at 66. Mutation preconditions (78, 90, 93, 95) use require outside the try, so a failed precondition is an uncaught RuntimeError and a caller STOP, never an expected rejection. Every mutation necessarily changes an object that the genuine receipt accepted (a nonempty Linv, F, modulus, B5, pole list, restoration key and row list), so a no-op cannot masquerade as a rejection.

Failure order traced against checker.verify (float 38; schema 39-41; modulus 136; units 137-153; mapping 161-163; A/B length 199, read 200, A form 202, B5 203; slots 204-213; gauges 214-215; bands 222-224; slot map 227; forcing 228-233; resonances 234; IDs 237-238; residuals 239-243; rows 244-254; poles 257-273; restoration 274-282):
- dropped-retained-row: nothing before 237 reads rows; 237 raises ValueError 'all20 original slots'.
- incorrect-L-inverse: dense([]) is {} (117 passes on an empty wire), inventory 138 and basis 139 pass, 140 passes, 141 raises 'L inverse identity'.
- incorrect-scale: mapping[3]=[] reads as {}, 161 passes, the zip reaches index 3 after three unchanged entries, 163 raises 'retained scale/parameter mapping'.
- incorrect-modulus: 136 raises 'wrong modulus' before any unit arithmetic.
- negative-s-sign-loss: mapping[6] is H with every s exponent -2 (checker.py:157), so the precondition at 78 holds on a genuine artifact; negating axis 8 keeps 127 and 129 satisfied (uniform shift, v-only ordering) and 163 raises the mapping string at index 6 after indices 0-5 pass.
- negative-lower-exponent: mapping[0] is the single term u, axis 0 set to -1 fails 127 at zip index 0, 'Laurent/basis exponent domain'.
- omitted-pole-slot: 257 raises 'all ten pole slots' before any pole arithmetic; inventory only, as charged.
- omitted-restoration-slot: 274-279 pass, 281 indexes data['restoration']['a'] and raises KeyError whose str is the quoted key "'a'", exactly the harness expectation at 84.
- incorrect-leading-B5: 199 still sees six entries, read([]) is {}, 202 passes on A, 203 raises 'leading B coefficient'.
- float-anywhere: 0.5 survives JSON round-trip as float, no_float(original) passes, no_float(data) raises 'floating point forbidden anywhere' at 38.
- large-canonical-string and rounded-canonical-string: see C.
All twelve classes and strings match the harness table (71-99) and VALIDATION.md 64-75. Twelve cases are counted at 100 and labelled 'checker.verify IN-PROCESS, not checker.main' at 69; VALIDATION.md 56-59 and input 1 lines 57-62 say the same. No frozen checker variant, control ORIGINAL, optimized-mode matrix or new runtime framework exists in the packet.

## C. Precision pair, negative-coverage wording, measurements: STATIC CONFIRMED (claims correctly bounded)

Precision pair. semantic_controls.py:88-90 sets big = 9007199254740993 and rounded = int(float(big)); by the binary64 round-to-even rule 2^53+1 rounds to 2^53, so rounded is 9007199254740992 and the precondition big > 2^53 and rounded != big is a genuine, distinct-integer precondition that STOPs the batch if it ever fails. wrong_large_row (91-97) pins the first row id E1/S0 (checker.py:235 makes ids[0] exactly that), builds a single constant term with the exact decimal string and requires the row to change. Both fixtures reach checker.py:249: read() accepts the wire (width 12, all zero exponents, canonical positive string that round-trips through int at 26-30), and the constant differs from the substituted original row, so both raise 'original row substitution E1/S0'. The pair therefore requires two semantic rejections with a distinct string pair. It does not, and the packet does not claim it does, show that the checker would tell a correct 2^53+1 coefficient from its rounded neighbour in a real payload: both fixtures are rejected for the same reason, and the exact-string round-trip at 58 is harness JSON retention only. semantic_controls.py:107-108, VALIDATION.md 77-82 and input 1 lines 70-75 state exactly that limit; no production API exposes the nested parser.

Late negative coverage. Any altered A or B fails checker.py:208 (or 202 for A) before the band tests at 222-224 or the pole tests at 271-273; the packet claims no late bracket or pole negative (harness 'negative_limit' at 110, VALIDATION.md 84-87, input 1 lines 77-79). Omitted pole slot is an inventory check at 257 and is described as inventory. Omitted restoration a is a KeyError at 281 and is expected as KeyError "'a'", not an invented message; this closes the prior gate's GAP-D1 wording point. The single genuine checker.main run is named as the only full bracket and pole read-back (VALIDATION.md 47-53 and 87; input 1 lines 51-55 and 79-80).

Measurement fields. checker.py:285-294 record per row: original term count, original max parameter degree and rational bits, retained term count, max lower degree over the eight parameters, min and max signed s exponent, max v degree, max rational bits; 320 records artifact bytes. The harness records fixture bytes and hash per case (67); the caller records return code, start and end UTC, cutoffs and telemetry hash (99-104). No speedup, cap-feasibility or memory forecast appears in inputs 1, 3, 4 or 6 (input 1 lines 82-88; VALIDATION.md 89-99 say counts, sizes and runtime are unknown). I add none.

## D. Readiness for ONE root-registered bounded engineering run: READY (qualified), no blocking correction found

Verdict. After root creates a fresh exclusive directory on an owned EC2 instance, verifies ownership, DMI, hostname and interpreter, arms its own exact-worker stop timer, writes and externally pins a new registration with all five profiles, both +00:00 cutoffs and the aggregate cap, and a fresh same-caller preflight (five refusals, valid sentinel, dummy) passes, this packet is ready for ONE bounded engineering run of transform, full-check and semantic-controls. This is not a solver or dispatch licence, and no ideal point, properness, unit or exclusion is implied or requested. I found no correction that must precede that run. Every refusal in the caller is STOP without retry (196-201), every child output is exclusive, and a failure at any step leaves the partial evidence in place.

Explicit unexecuted controls retained: the twelve in-process rejections, the genuine transform and the genuine checker.main receipt remain unexecuted claims; the cost of twelve verify calls, the retained artifact size against the inherited 16 MiB per-file limit (each fixture is a full copy of the retained artifact, written under the same limit), and the CAPRUN telemetry are unknown until the run. Future cutoff and worker registration remain root's, exactly as input 6 lines 9-16 state; no default budget exists in the template.

Non-blocking improvements, offered only as options for a later packet, not applied and not required: assert the expected refusal string from each defect's .stderr file at dispatch_batch.py:108; add the sentinel and fixture paths to the pre-existence lists at 72 and 181; rehash the second authority read in the three mains against custody['authority_sha256'] if GAP-C1 is ever switched on.

## Custody table (generated from sha256sum output, charged order)

| # | input | sha256 | match |
|---|-------|--------|-------|
| 1 | f10-r1-retained-runtime-code-astra-20260909.md | 3ebc924b86a9763a36e69485717403a3fd1108210880351f1245c0afe01d64c6 | yes |
| 2 | f10-r1-retained-runtime-code-astra-20260909.md.artifact.json | 63ab0792ea50bc49fdb4bc31bc73b385f0b35e4244b2acd14119029d355c2e66 | yes |
| 3 | dispatch_batch.py | 2116bd168cca7bdf902b692471a2005e444f111dcddbe593a5d30480aaaadf70 | yes |
| 4 | semantic_controls.py | 3876ff5a6fd2935211fcafadf2c0fb9877ff0d7f3667338bb7f50cd8836d7316 | yes |
| 5 | dispatch.registration.DISABLED.json | e1a6852b7d9c03ac3e1acaf4c92a88303c0945e51f8987948a7555af7baf2fae | yes |
| 6 | VALIDATION.md | 0e99afc1371b822fd96ddc84183fc83a958a1c6d60a7b6432b1c7a5e1a2283b7 | yes |
| 7 | checker.py | 7d80b261df626fa84ab72d7ff06e3c37e1ba6f6e98e40d6e7d2142d2397aacda | yes |
| 8 | execution_gate.py | cbfe55ff11503cee094dc49e54b656d209806aa37ee30cc281902902152049c6 | yes |
| 9 | probe.py | 02913a1caf8cb5ebe2ec7c404ede0247a1954baee3751af8b2645496f6b6e1a7 | yes |
| 10 | caller.diff | 60cf6ccb737c24d1ae085eb0a4d4b7ffd46ebcdebd028e8f7a247ebeb589405a | yes |
| 11 | f10-r1-retained-row-static-gate-fable5-20260909.md | 647677995311ec13a5d82ae412ad31c89ed18a6a7673e380ee328ae7ff6af8fd | yes |
| 12 | transformer.py | ff5d08c8b8f0ec30b071893cc9cf99db3a3a56a30bb54d653a19c0a8dd5df869 | yes |

The same twelve lines, in charged order, are in box/f10-r1-retained-runtime-gate-fable5-20260909/input-custody.sha256, written by redirecting sha256sum output. The caller's constant pins for checker, helper, probe and transformer (dispatch_batch.py:164-168) equal rows 7, 8, 9 and 12; the CAPRUN pin 4435279df8f987c667ebc2fae8e4bd987916032561cb3faf1e07a57789d196c2 and the exact.json pin 168bdfd3a791f24a6631de66ae4e71c3f209ee9eb0d21fc466f9aa802f5ad576 equal the old caller's and the prior gate's values (input 10 line 110, input 11 row 11) and were not re-derived from any body.

## GAPs, limitations, OPEN

Summary verdicts: A CONFIRMED; B CONFIRMED; C CONFIRMED; D READY (qualified) with no blocking correction. No runtime pass, emitted row, receipt, ideal unit, properness, point, exclusion or JC2 claim is made. No Seal and no charge_basis are authored.

GAPs left open at the deadline: (i) no compilation, so syntax of inputs 3 and 4 is verified by reading only (I found no syntax or API error, including the generator unpack at dispatch_batch.py:44, the closure over the function-local checker import, and KeyError str form); (ii) whether root's staged exact.json, admissibility.md, frontier.json and CAPRUN match the constant pins is a runtime refusal, not checked here; (iii) retained artifact size, fixture sizes, wall and RSS are unmeasured and no forecast is offered; (iv) the old caller file named in the diff header was not read, so the diff's minus side rests on input 10 alone (its plus side was verified against input 3); (v) own-only collision check, no corpus novelty scan; (vi) the byte identity of execution_gate.py with the helper the prior gate reviewed rests on equal hashes (row 8 equals input 11 row 5), not on a byte comparison.

Deviations from the charge: none. No cat or heredoc write to any owned file; the custody file was written by redirecting sha256sum; the report was written with apply_patch only. No source, harness, shared or canonical file was modified.

## OPEN(S) RAISED

- OPEN[F10-R1-RETAINED-RUNTIME-PREFLIGHT-AND-BATCH] QUANTITY: one preflight.PASS.json with exactly seven records (five refusals with nonzero NORMAL_EXIT and no sentinel, one valid sentinel, one dummy with the MATCH/SENT15/MATCH/SENT9 sequence) followed by one batch.PASS.json whose full.receipt.json carries verdict PASS-REPRESENTATION-NOT-IDEAL-DECISION and whose controls.receipt.json lists exactly twelve EXPECTED-REJECTION cases; value <= 1 of each, or exactly one named STOP string in batch.STOP.json. CHEAPEST TEST: the single root-registered run described in D on a fresh directory with the same caller, after root's own preflight. WALL: unknown; the preflight is seconds-scale by construction (probe sleeps 20 s but dies at the 32 MiB cap), the three mathematical children are bounded only by root's caps, and any estimate of mine would be a forecast the charge forbids.

## COLLISIONS

status: EMPTY

- NONE — own-only raised-OPEN check: the one OPEN above does not repeat the prior gate's OPEN[F10-R1-RETAINED-CODE-RUNTIME-READBACK] (input 11 line 95), which it supersedes by adding the preflight and controls quantities; this report and its box only, no corpus scan.

## Own whole-read and completion

Own whole-read at 17:47:15 UTC with cat -n: sections custody/read scope, A, B, C, D, custody table, GAPs/limitations/deviations, OPEN(S) RAISED (one entry with quantity, relation token, cheapest test, wall stated as unknown), COLLISIONS. All fifteen 64-hex tokens in this report were cross-checked by comm in both directions against the owned input-custody.sha256: the twelve custody digests match 12/12 with none unmatched, and the three tokens outside that file are the input-2 body digest, the CAPRUN pin (inputs 3 and 10) and the exact.json pin (inputs 3, 7, 11, 12), each confirmed by grep in those inputs. The 40-hex basis equals HEAD. One sentence in the custody section was corrected in the final write to stop over-crediting the old caller for the checker pin. No placeholder remains. No Seal, no charge_basis, no marker before this line. Completed before the 17:58:00 UTC stop; all writers idle; no execution authority, worker, cap or registration was created.

<!-- BODY-END -->
