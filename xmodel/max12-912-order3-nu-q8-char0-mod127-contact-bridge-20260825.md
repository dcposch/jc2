# Selected-Q8 characteristic-zero/mod-127 full-contact bridge

Date: 2026-08-25  
Status: **PRODUCER-EXACT ARITHMETIC-LOCAL AND SOURCE-IDENTITY THEOREM;
hostile review required**

## 1. Exact result

The reviewed characteristic-zero corrected-Q8 contact and the frozen
characteristic-127 full contact are points of one common divided localized
source over `Z_(127)`.  After a common unramified DVR extension splitting the
corrected octic, each of the eight characteristic-zero contacts is integral,
reduces coordinate-for-coordinate to the corresponding full mod-127 contact,
and has full relative Jacobian a 127-unit.

Consequently the arithmetic completed local source at each marked contact is

```text
R[[w]],
```

with generic and special fibres `K[[w]]` and `k[[w]]`.  In particular the
scheme-theoretic closure of the reviewed characteristic-zero selected branch
contains the full mod-127 point, and the special source has one regular local
branch there in every dimension.

This discharges the boundary-local integral-specialization and exact-source-
identity requirements.  It does not prove that the eight mod-127 points lie
on one global component; that degree-one/all-contact gate remains separate.

## 2. Common arithmetic source

The producer hash-pins the reviewed approximate-cubic quotient compiler and
emits the same six sparse rows through two independent entry points:

```text
e1=r1/t, e3=r3/t, e5=r5/t, e7=r7/t, e2=r2, e4=r4.
```

Every rational row scalar has 127-unit denominator.  Their canonical exact
and reduced fingerprints are

```text
Q rows:      6088819ab51296dc64110ec7d36b71a48754782c42192ba09643a7be5cdacd58
F127 rows:   4a085f479a4ca3e293fe46feb53843aed8b5d7475b70b67be111c14190b3a419.
```

The common graph presentation is

```text
Y = Spec Z_(127)[w,c,d2,d4,x1,x3,x5,inv,v] /
    (e1,e3,e5,e7,e2,e4,
     v*x5-x3+2*x5,
     inv*x5*(x3-2*x5)-1).
```

Crucially, this localizer does **not** invert `w`.  On the generic punctured
open, `w` is already invertible and eliminating `(v,inv)` recovers exactly
the reviewed characteristic-zero six-row quotient.  Keeping `w=0` gives the
full-contact closure used modulo 127.  Thus the primitive grouping,
infinity theorem, arithmetic closure, and mod-127 source refer to geometric
components of base changes of this same scheme, not merely to projected
roots of `H(w,v)`.

## 3. Integral contacts

Because `-999` is a 127-unit and `Q8bar` is squarefree, the monic corrected
octic defines a finite etale `Z_(127)`-algebra.  After a common finite
unramified extension whose residue field splits `Q8bar`, Hensel gives eight
pairwise distinct integral roots `v_i`.

In `Q[v]/(Q8)` the producer reconstructs

```text
x5=-36*v^2*(3v^2+3v+1)/(3v^2-2),
x3=x5*(v+2),
x1=x5*(v+1)+x5^2*(3v+1)/(9v),
```

then solves `(c,d2,d4)` from the same three normal divided rows and sets
`inv=(x5*(x3-2*x5))^(-1)`.  It verifies:

- all six characteristic-zero divided rows vanish;
- the `v` relation and inverse-localizer row vanish;
- every coefficient denominator of all eight coordinates, the normal solve
  determinant, both localization factors, and the full determinant is a
  127-unit;
- all eight coordinate reductions equal the frozen mod-127 section exactly.

The comparison consumes the corrected factor `(3v+1)/(9v)`, never the
quarantined Q12-era slice.

## 4. Full arithmetic implicit-function unit

Take the relative Jacobian of the eight displayed equations with respect to

```text
(c,d2,d4,x1,x3,x5,inv,v).
```

The exact characteristic-zero determinant is 127-integral and reduces to

```text
[101,42,36,116,8,110,107,14]
```

in the basis `1,v,...,v^7`.  Its gcd with `Q8bar` is one and its norm is

```text
88 mod 127.
```

This is exactly the determinant frozen by the independent mod-127
full-contact endpoint.  Hence it is a unit at all eight conjugate contacts.
Formal implicit functions over the common DVR solve all eight internal
coordinates uniquely as elements of `R[[w]]`, proving the completion stated
in Section 1.

The generic completed branch is the reviewed selected branch because the
same six divided characteristic-zero rows have a unique solution with `w` a
parameter.  Adjoining `t` with `t^2=w` recovers the reviewed non-parity branch
in coefficient space; this is ramification in the branch parameter, not in
the arithmetic uniformizer 127.

## 5. Independent replay and custody

AWS r6d (`ip-172-30-0-45`) tag

```text
q8_char0_mod127_contact_bridge_r6d_v2
```

completed both producer and independent replay at rc zero.  The producer
used 26,652 KiB maximum RSS in 7.48 seconds; the replay used 25,612 KiB in
9.42 seconds.  The replay does not import the producer: it reruns the frozen
mod-127 full-contact program from its own pinned source, directly reduces
every exact rational coordinate and determinant coefficient, and independently
checks the source-row reduction and non-`w` localizer manifest.

Pinned hashes are

```text
producer.py       8360bed8333cca482b348eeced6bf89c9c4852748241178da47fbfa715354ebd
independent.py    fc58a2d4eb381e9756294d93f9d577d1b6017af61c2e2307a5b4dd77668fd000
run_remote.sh     3d9afa57f9e46753b43cd65dbfbeb4d95e8fe991b78d333e2415f1f0eeb06497
producer.json     a07c952362238f49bddf01f04779158de0c50a05c8a4ebbddf3df5f6fd1342b1
replay.json       40aebed20bf518f6845f40438b85f35c87b3e48b16108b9ce9a81f0baa65f1e9
```

The first r6d launch is retained as a negative custody control: it failed
closed before mathematics because the partial remote repository lacked the
frozen mod-127 dependency.  Version 2 synced and hash-verified that dependency
before launch.

## 6. Exact scope and remaining gate

This theorem is arithmetic-local.  It proves integral specialization of all
eight reviewed characteristic-zero contact branches and exact identity of
the source schemes used on the two sides of reduction.  It does not by itself
prove that one mod-127 component contains all eight contacts, source degree
one, a global coordinate graph, or absence of unrelated components.

If the separate seeded/generic route proves that a single mod-127 component
contains all eight full contacts, the repaired no-merger lemma may consume
the `R[[w]]` neighborhoods here to force the eight characteristic-zero
selected branches onto one geometric component.  Only then may the reviewed
primitive grouping and infinity theorem exclude an actual trajectory at
their registered scope.  Taylor realization, other `(9,12)` strata,
maximum twelve, a counterexample, and JC2 remain open.
