# Gate: complete D125 zero-k field classification and general unramified obstruction (Fable 5.1, 2026-09-07)

Bounded 18-minute hostile gate of `xmodel/d125-zero-k-boundary-classification-astra-20260907.md` (frozen SHA `129947ee…`) with its `check.py` (`61226b60…`) and `final-witness.json` (`fc345a15…`), read from `/tmp/jc2-lane.3223i6/inputs` only. Root's whole-proof read, 14 pins, eight-run replay, witness and receipt (`494e4746…`) and the transaction root (`5c5e7efb…`) are taken as given; the transaction is not among the frozen files. The frozen `check.py` was run once read-only: stdout hashes to `fc345a15…`, exit 0. Scope is the field-point classification of the unguarded k=0 parity-fixed unequal/rational client (H=p²(p³+g³)), plus the narrow repair of the predecessor pure-power gate (`9d6f594f…`, TERMINAL, unpromoted). No CAS, AWS, SSH, solver, live peer, shared ledger, adapter or full 15/25 expansion was used.

## Verdict table

| item | verdict |
|---|---|
| 1. Every field-valued unguarded k=0 point is uniquely (R_t³+αR_t, R_t⁵+βR_t³+γR_t) over the same field: generator degree 1 or 5, degree 1 excluded, root-free normalization r=g₅/f₃², origin, central inversion, oddness of S, F, G | **CONFIRMED** |
| 2. Newt₀(F(S))=d·Newt₀(S) with arbitrary weights; literal A polygon gives exactly the lower slots gp², p³, p; ordinariness from full A rows; two negative rows force a=b+3, c=−b−3; converse covers every literal condition without expanding powers | **CONFIRMED** |
| 3. GAP-POLYGON resolved from the literal client vertices; pure-power interface (A₁ leader g²p, centralizer, −f′A₁/3, t=−3 radical, all-branch divisibility, whole β₁₅ shear) fits the source | **CONFIRMED**, with the checker mismatch disclosed in §3 |
| 4. General order-two jets: α≠0, α=0/γ≠0 and α=γ=0 branches; no unramified order-two k-jet through any field center | **CONFIRMED** (composition consumes the §3 pure-power obstruction) |
| 5. Stop: unguarded points only, no ramified/infinity/nilpotent/flatness/generic-fibre/properness/JC2 claim, no emptiness upgrade, coverage limited to the parity-fixed client | **CONFIRMED** |

## 1. Literal polygon fit (closes GAP-POLYGON)

The charged `client.py` states, at `VERTICES['unequal']`, A vertices (0,0),(0,15),(9,6),(2,1) and B vertices (0,0),(0,25),(15,10),(1,0), inner weight (5,−7); its `lattice` keeps closed-polygon lattice points by a cross-product sign test. Own control C1 parses these literals from the frozen file's AST and recomputes the lattice with the same convention. The A edges are i=0, i+j=15, 5i−7j=3 and i=2j; the B edges are i=0, i+j=25, 5i−7j=5 and j=0. Exact lattice implications, checked point by point:

- A lattice = {(i,j)∈Z²_{≥0}: i+j≤15, 5i−7j≤3}, 83 points, 44 odd; the edge i≤2j is automatic on lattice points (i≥2j+1 forces 5i−7j≥3j+5). Weight-3 slots exactly (2,1),(9,6); degree-15 slots i=0..9.
- B lattice = {i+j≤25, 5i−7j≤5}, 215 points, 112 odd; weight-5 slots exactly (1,0),(8,5),(15,10). A lattice ⊂ B lattice, max degree 15<25, max weight 3<5.
- (1/3)P_A, read as (3i,3j) inside the literal polygon: 13 lattice points, 7 odd; the four degree-5 ones (0,5),(1,4),(2,3),(3,2) are fixed by S_top=H; the lower odd ones are exactly (0,1),(0,3),(1,2), i.e. p, p³, gp². (5/3)P_A lattice ⊂ B lattice.
- Negative-row envelope by exponent bookkeeping only (e=5t+2s−i−j): A gives exactly the 30 rows t≤2, 5t−15≤e<0; B the 75 rows t≤4; A rows ⊂ B rows.

The fixed zeros are the client's outer-face zeros (6 on A, 10 on B), inner-face slots and the origin; the source scope in the report is `field: Q`, `geometric_scope: algebraically closed characteristic zero`, unequal case, rational branch. The 44/112 census of the predecessor is now derived from the vertices instead of matching a count; the classification's `check.py` also defines its A/B sets by inequalities (line 111), which this fit shows are the literal lattices.

## 2. Classification (items 1–2)

Imported statements were read whole from the primary text: Lemma 4 (char 0, dependence iff Jacobian rank 1, proof by citation, external trust as the producer states), Lemma 5 (dependent f,g lie in k[h] for closed h; proof via Noether normalization and Proposition 1), Corollary 1 (integral closure of k[f] is k[h], h generative, unique up to affine change). Both A₀,B₀ are nonconstant by their fixed tops, so A₀=F(S), B₀=G(S) with S closed; K[S] is integral over K[A₀] and integrally closed, hence is the Corollary 1 closure.

Degree: deg S divides 15 and 25. Degree 1 would make H³ a scalar 15th power of a linear form, but p divides H³ exactly six times (p³+g³ is g³ mod p). So deg S=5, deg F=3, deg G=5. Normalization: f₃J³=H³ and g₅J⁵=H⁵ give q=J/H with q³=f₃⁻¹, q⁵=g₅⁻¹, so q=q⁶/q⁵=g₅/f₃²∈K*; S↦(S−S(0))/q makes S_top=H, S(0)=0, F,G monic, all over K (control C3, three sample scalars, wrong-factor mutation rejected). Inversion σ=(−g,−p) fixes A₀ up to sign, so preserves K[A₀] and its closure K[S]; σ(S)=aS+b, b=0 at the origin, a²=1, a=1 would make A₀ even against its odd top; hence S odd, and injectivity of K[T]→K[g,p] forces F, G odd: A₀=S³+αS, B₀=S⁵+βS³+γS. Uniqueness: the residual affine freedom is scale 1, translation 0, and S is closed (prime degree 5, non-power top). Item 1 CONFIRMED.

Support: for w any linear weight, h_{Newt₀(V)}(w)=max(0,max_supp w)≥0; when positive, V^d has the strictly largest weight and its leading form is a nonzero d-th power in a domain; when zero, every power has weight ≤0, so both support functions vanish. Equality of all support functions gives Newt₀(F(V))=d·Newt₀(V). Since supp A₀ lies in the literal A lattice, Newt₀(S)⊂(1/3)P_A, whose lattice is §1: S=H+a·gp²+b·p³+c·p. Ordinariness: W=φ(S) satisfies W³+αW=φ(A₀)∈K[u,v] once ALL 30 A-negative rows vanish; either K[u,v] is integrally closed, or the lowest negative v-power n of W gives an uncancelled v^{3n}. Control C2 expands only the degree-five generic S: the complete negative part is (a−b−3)v⁻³+(2a−3b−c−9)v⁻¹ (with φ(H), φ(gp²), φ(p³) parts −3v⁻³−9v⁻¹, v⁻³+2v⁻¹, −v⁻³−3v⁻¹), so a=b+3, c=−b−3, t=b, and φ(R_t) is ordinary for symbolic t. Converse without expansion: R_t is odd, vanishes at the origin, lies in (1/3)P_A with top H and (5,−7)-leader g³p²; powers add the support inequalities, the degree-15/25 parts of R³/R⁵ are H³/H⁵, the weight-3/5 parts are the single monomials g⁹p⁶/g¹⁵p¹⁰ (lower powers have weight ≤1,3), moving slots are zero, lifts are polynomials in W, and [F(R),G(R)]=0. Item 2 CONFIRMED, field points only.

## 3. Pure-power interface and checker disclosure

Interface fit against §1: A₁'s (5,−7)-leading form is exactly g²p because the literal A lattice has weight-3 slots (2,1),(9,6) only, (9,6) is fixed to 1 for all k, and A(2,1)=k; total-face fixing gives deg A₁≤13. Centralizer of R_t is K[R_t] by Lemmas 4–5 with the same degree-1 exclusion (p-multiplicity 2 of H). Own C5 verifies with R=g and random small A₁,A₂,B₂: order one R²[R,3B₁−5R²A₁], order two [R,E] with E=3R²B₂−5R⁴A₂−(5/3)RA₁²−(1/3)f′(R)A₁; factor −1 is rejected. t=−3: R=p²C, p²|R confirmed on the degree-five R, radical pC with leader g³p; leader g³p² for all t; neither divides g²p; a≠0 gives R|A₁, a=0 gives R|A₁² (constant of h/R killed at the origin). β₁₅: [p¹⁵]R³=1 and s_t=[p¹⁵]R⁵=t⁵−20t⁴−30t³+180t²+270t computed from the exponent triples (0,5,0),(1,3,1),(2,1,2) with multinomials 1,20,30, never from R⁵; the whole shear B→B−(s_t+β)A gives β′=−s_t, γ′=γ−α(s_t+β) (symbolic identity in K[T]), and the client leaves B_(0,15) free, so this gauge is optional.

**Disclosure.** The predecessor's `gate_controls.py` (`953455a9…`) violates its instruction: line 84 builds R³ and R⁵ symbolically in t, and lines 130–134 build the degree-15/25 jet pairs A=R_t³+kA₁+k²A₂, B=R_t⁵+… at t=2 and bracket them; its emitted `no_A15_B25_expansion=true` (line 147) is false. Root did not replay it. Its artifacts are preserved and were not executed or reused here; every fact it supported is re-established above by the degree-five R, exponent triples and R=g brackets. The classification's own `check.py` does not expand R³/R⁵; its flag is accurate. With the polygon fit and compliant controls done, the pure-power obstruction is consumable. Item 3 CONFIRMED.

## 4. General order-two jets (item 4)

With a=3R²+α, b=5R⁴+3βR²+γ, order one gives [R,aB₁−bA₁]=0, so aB₁−bA₁=f(R). α≠0: over K̄ choose r≠0 with 3r²+α=0. If b(r)≠0, then modulo R−r, A₁ is congruent to a constant; R−r has leader g³p² (its constant has weight 0<1), A₁−const has leader g²p, and leaders multiply in K̄[g,p]: impossible. So b(r)=0=b(−r), (R²−r²)=a/3 divides the even b: γ=αβ−5α²/9, q=5R²/3+β−5α/9 (C5 checks a·q=b). Division of f by a in K[T] with remainder ρ of degree ≤1 gives a(R)(B₁−qA₁−h(R))=ρ(R), impossible by degree unless ρ=0: B₁=qA₁+h(R). Order two: [A₁,qA₁+h(R)]=−(10/3)RA₁[R,A₁]−h′(R)[R,A₁], so the k² coefficient is [R,E] with E=a(B₂−qA₂)−(5/3)RA₁²−h′(R)A₁ (C5 verifies on R=g; the factor mutation is rejected). E∈K[R], hence a constant e modulo R−r. Irreducibility of R−r for every r≠0: as a cubic in g over K̄(p) divided by p², the constant term has p-valuation −2, the g-coefficient t+3 has valuation 0 or ∞ (t=−3), and a Laurent root of valuation n gives valuations {3n,n,−2} with a unique minimum for every integer n (C5, both term sets); no root in K̄((p)), so irreducible over K̄(p); primitivity holds since the constant coefficient is −r mod p; Gauss's lemma. In the domain K̄[g,p]/(R−r), (5r/3)A₁²+h′(r)A₁+e=0 splits over K̄, so A₁ is congruent to a constant, the same impossible leader. α=0, γ≠0: modulo R, γA₁+f(0)≡0, the origin kills f(0), so R|A₁, impossible. α=γ=0: B→B−βA preserves A, the bracket, the literal B lattice (A lattice ⊂ B lattice), both B faces (3<5, 15<25), origins, parity and the negative rows (A's 30 rows ⊂ B's 75), giving a jet through (R³,R⁵), excluded by §3. Item 4 CONFIRMED as a composed proof: no unramified order-two k-jet through any field center.

## 5. Stop

The guard zk−1 is omitted before k=0, so these points are not points of the guarded N; nothing about k=s^m, coefficients at infinity, nilpotent boundary structure, flatness, generic fibre, properness or units, or JC2 is claimed or drawn, and no-jets is not upgraded to emptiness. Coverage is the parity-fixed unequal/rational client; the golden branch and common_3/common_4 cases are outside this classification. Item 5 CONFIRMED.

## 6. Controls and typed gaps

Own `box/d125-zero-k-classification-gate-fable5-20260907/gate_controls.py` (SHA `37e664c9…`, stdlib, zero Assert nodes, seven pins of frozen inputs, no frozen edit): C1 literal polygon fit, C2 generic degree-five lift and R_t recovery, C3 same-field scalar, C4 combinatorial triples and shear identity, C5 R=g brackets and valuations. Eight runs (normal/−O × none, `--mutate-source-face` with the common_3 vertex (3,0) replacing (2,1), `--mutate-missing-negative-row`, `--mutate-wrong-factor`): witnesses byte-identical (`1461b907…`), replay `5fbe22ab…`, 2.4 s total, 40 MB peak, caps 30 wall/25 CPU s, 512 MiB. Each mutation exits 1 with its intended message in both modes.

Typed gaps, none charged to the classification: **GAP-CHECKER-FLAG** (predecessor artifact only, §3). **GAP-LEMMA4** external trust for the Jacobian criterion, as declared. **GAP-TRANSACTION** the transaction root is not in the frozen inputs and is taken from root. No exit-price claim is made, so no charge basis is declared. **STOP:** review only.

<!-- BODY-END -->
