# TD6 V82S2 kernel-dead first gate — producer theorem

Verdict: **PRODUCER-EXACT; HOSTILE REVIEW PENDING**.

Over the generic symbolic-center field `E(C,V,U)`, both transport-kernel
dead-stretch tangent axes `d10` and `d15` have exact zero first-stage
conormal.  Each exact source column is genuinely present before elimination
(1,108 raw entries for d10 and 739 for d15), disappears under the omission
negative control, and reduces through the original-row first echelon to no
dependent compatibility coordinate.  First rank is `38/132`; both actual
tables are header-only, and the denominator is the unit `1`.

The corrected producer was run independently on r6d and Box03.  All four
lanes returned rc0.  Actual and synthetic empty-table bytes agree at SHA256
`68b4a3a151d2eb15e04b6bb1b0bb6e5656423289e4dca1260844476105ea6692`.
After normalizing only hostname, registered run tag, and absolute run root,
the two host stdouts are byte-identical per axis.

The immutable predecessor V82S computations had already reached the same
empty coordinate dictionaries but failed while constructing the unit
denominator with invalid `CTX(1)`.  V82S2 uses the ring's exported `ONE`,
asserts `ONE == CTX.constant(1)`, separately exercises a synthetic
header-only positive control, and reruns the entire algebra.  No predecessor
stdout is used as a verdict.

Composed provisionally with frozen V81C (33-axis transport rank nine and
kernel `Q22 + d10 + d15`) and hostile-reviewed V78 (the 22 q directions have
zero first conormal), the full 24-dimensional transport kernel survives the
first gate.  This composition remains producer-tier until V81C/V82S2 review;
the present theorem itself is the narrower two-axis first-stage statement.

Scope is fixed source-typed A3, generic fraction field, and square-zero
transport-plus-first source incidence only.  It gives no previous/pole/current
claim, second-order or nonlinear neighborhood, family, full TD6, SP-2, or
JC2 conclusion.

Frozen case:
`cases/td6_c1_c2_c3_kernel_dead_first_v82s2_aws_20260826/`.
