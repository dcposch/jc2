# Coordinator integration: generic K00 `m=2` cells at `e=2,3`

Date: 2026-08-30 UTC  
Coordinator: Sol 5.6 Ultra  
Frozen basis: `0d7544ebd5cb12def6bac892646010301098be3c`  
Lifecycle: **BINDING INTEGRATION / TWO DIFFERENT-MODEL-CONFIRMED FINITE-CELL EXCLUSIONS**

## 0. Charged evidence and disposition

This integration binds the conservative intersection of two Sol producers
and two independent Fable reconstructions:

```text
fa5a4ef6a1b2c0640c6363b84e6b547a044920b15249f7c72b25ffbcda2c1733
  xmodel/k00-ram-e2m2-g0-g10-complete-rankfan-sol56-20260829.md
efd2f4fea4f58ee4783d9b373ee68f2ff191a9923b08bf2ead8dd1c59a4d3749
  xmodel/k00-ram-e2m2-g0-g10-complete-rankfan-replay-sol56-20260829.py
4710349b6082623213a6074cb92ebc7944fb5ce751a06112f2fa560e043bb69c
  xmodel/k00-ram-e2m2-g0-g10-complete-rankfan-hostile-review-fable5-20260830.md

3a4565b8e7163acb3cea8f1504123cab3e1c04ca936110bdaf047952181188ae
  xmodel/k00-ram-e3m2-g0-g12-complete-rankfan-sol56-20260830.md
7cbc45a13d84c7921e7548c7229fcd28e8c28cdd4ff6a11c4c5643b24e426a33
  xmodel/k00-ram-e3m2-g0-g12-complete-rankfan-replay-sol56-20260830.py
347bf2766cc72ffa1f5d5ee7b75ff718b8fb1fe2681a26e5d36eeee8a760e2f6
  xmodel/k00-ram-e3m2-g0-g12-complete-rankfan-hostile-review-fable5-20260830.md
```

Both review verdicts are `CONFIRMED`. Each reviewer rebuilt the frozen
569-tail source, calendar, graph recentering, cone/rank fan, every terminal,
and ordinary/optimized replay independently. The maximum statements below
are the reviewed point-set claims after applying the recorded corrections.

One operational sentence in the `e=3,m=2` review reports an unscoped parent
worktree status and mentions the excluded nested formalization. That status
observation was outside the review mandate and is not consumed here. It has
no mathematical role; only the hash-pinned campaign files and computations
listed above carry this integration.

## 1. Promoted `e=2,m=2` theorem

Over an algebraically closed characteristic-zero field, on the normalized
generic-ray cell

```text
Lambda=tau^2,  C6=1,  ord_tau(d)=2,
k10[0]!=0,     Jdet[0]!=0,
```

with the later boundary series arbitrary at every order not excluded by the
displayed K10 unit condition, the literal seven-row source satisfies

```text
V(G0,...,G10) intersect D(k10[0])
                intersect (union_i D(d_i[2])) = empty.       (1.1)
```

The complete transition fan is exhaustive. First normal order two dies at
G6; first normal order three has an honest odd G8 survivor and dies at G9;
first normal order four and every higher/exact-surface branch die at G10.
The final rank-one and rank-zero terminals use `k10[0]`; K6, K2, and all
targets cannot arrive through G10. The Jdet unit is a cell label but is not
used in this prefix.

The odd survivor is intrinsic, not one fixture: on each Gaussian rank-one
component, nonzero `N3` and `N5` are forced. Hence the `m=2` jet functor is
strictly larger at G8 than the coefficient-sparse `tau^2` pullback of an
`m=1` packet. No coefficient-blind base-change induction is licensed.

Binding review corrections:

1. the canonical JSON certificate has 6,036 bytes, not the stale 5,958-byte
   prose figure; its pinned digest and replay banner are correct;
2. the displayed W2 `192->191` comparison is a weak syntactic control, not a
   pipeline mutation; independent reconstruction and the live odd-fixture,
   surface, and K10 controls carry the result;
3. the replay allows arbitrary tau-series coefficients for the boundary
   inputs, a superset of a coefficient-sparse `Lambda=tau^2` pullback. The
   promoted emptiness is on that larger retained-series convention.

## 2. Promoted `e=3,m=2` theorem

Over an algebraically closed characteristic-zero field, on the normalized
generic-ray cell

```text
Lambda=tau^3,  C6=1,  ord_tau(d)=2,
k10[0]!=0,     Jdet[0]!=0,
k6,k2,mu2,mu4,mu6 in tau*K[[tau]] or identically zero,
```

the literal source satisfies

```text
V(G0,...,G12) intersect D(k10[0])
                intersect (union_i D(d_i[2])) = empty.       (2.1)
```

Nominal K10 arrival at G10 cancels exactly at G10 and G11. The exhaustive
fan has the following sharp chronology:

```text
n=2: all nonzero ranks die at G6; rank zero recenters;
n=3: rank two dies G8, rank one dies G9, rank zero recenters;
n=4: rank two dies G10, rank one genuinely survives G10/G11
     and dies by the unloaded row-six unit at G12; rank zero recenters;
n=5: rank two, rank one, rank zero, higher order, and exact surface
     all die at G12, with k10[0] load-bearing in rank one/zero.
```

The G11 fixture is genuine and first fails at `G12_6=i/32`. A separate
fixture with `k10[0]=0` survives all rows through G12, proving the unit open
is necessary and that the complementary face is not closed by (2.1).
Jdet is again an unconsumed cell label; (2.1) remains true on the larger
open obtained by dropping `D(Jdet[0])`, but this integration retains the
registered cell statement.

The review noted two non-load-bearing harness weaknesses: one cone embedding
coefficient is protected only by custody in the producer replay, and one
constant W-slope guard is vacuous. Its independent symbolic cone audit and
live source/engine mutations close the corresponding mathematics. Neither
weak control is promoted as evidence by itself.

## 3. Exact joint consequence and boundary

Together with the previously promoted `e=2,m=1` G7 and `e=3,m=1` G9 cells,
the normalized generic C6/K10 ray now has four reviewed empty cells:

```text
(e,m)=(2,1) through G7,
(e,m)=(3,1) through G9,
(e,m)=(2,2) through G10,
(e,m)=(3,2) through G12.
```

These four terminal grades equal `2e+3m`, but their transition maps and
survivors differ. The equality is an observation on four cells, not an
all-`(e,m)` theorem or recurrence. The separate general calendar/meta packet
remains review-gated and is not promoted by this integration.

A formal solution inside either exact registered cell would truncate to a
point forbidden by (1.1) or (2.1), so no formal arc exists inside that same
cell. This one-way consequence does not provide source completeness,
reachability, converse lifting, compatible all-order data, a polynomial map,
or a JC2 conclusion.

Not covered: `C6=0`; `k10[0]=0`; positive/infinite Jdet order; another K00
support/load ray; `(e,m)` outside the four rows; nonreduced scheme points;
or an actual-map occurrence theorem.

## 4. Ranked successors

The cheapest source-partition child is the `e=2,m=2` face

```text
ord_tau(k10)=1,
```

where the G10 loaded terminals vanish and literal re-extraction first opens
at G11. The `k10=0` face is separate. For `e=3,m=2`, the corresponding
positive-order face has genuine G12 survivors and first receives `k10[1]`
at G13. Neither boundary result follows by shifting a unit-face equation;
each must be compiled from the frozen source with every reachable jet slot
retained.

The cheapest untouched unit-ray cell suggested by the exact calendar is
`(e,m)=(4,1)` at G11, subject to independent review of that calendar and a
fresh literal transition extraction.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7067`.
- Body SHA-256:
  `fd511b4ec206bf704ef88931aab1fd7d84f0cd2197c94062a75cceb8866c855f`.
- Frozen basis: `0d7544ebd5cb12def6bac892646010301098be3c`.
