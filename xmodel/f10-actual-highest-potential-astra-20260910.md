# Actual highest block and rational-power potential discriminator

Status: NO_CLOSING_IDENTITY. New necessary graded-equivalence discriminator and an exact actual critical mixed-coefficient identity, UNREVIEWED. Neither actual model equivalence nor highest regularity is proved. The sole provisional master dependency at invitation was subsequently discharged by ROOT lifecycle attestation; no new gate was read.

First action 2026-09-10 22:39:09 UTC; report, manifest and box targets absent. Original publication reserve 22:59 UTC, HARD23:02 UTC, never reset. Owner /root/contact_collision_geometry; basis 0d39df3c9fd69c939a8420c54d03228b9077777d.

## 1. Exact scope and outcome

Fix r>=2, d=r+1, q=2d-1, m=3d-2, n=5d-3 and N=q+d=3d-1. Let B be the WHOLE finite-etale rank-seven leading Q-algebra. The master gives actual homogeneous highest compatibilities H_q,...,H_m in B[X_1,...,X_d], wt(X_i)=i. It is used at its stated whole-ring, forcing, gauge and faithful-cover scope. This task began as its sole conditional child; section 6 records ROOT's later acceptance attestation, not an extra input read.

The exact full-forcing advance is gamma*Psi_q=z+Gamma(W_q), where gamma is a leading unit, Gamma is the accepted critical coefficient functional, and W_q is the ENTIRE actual earlier mixed forcing. It is proved in section 3 without discarding y=z-U*d0. This is not a potential or a source-exclusion identity. A separate concrete obstruction to the proposed graded equivalence is the ACTUAL coefficient

    a_mix=[X_(d-1)*X_d]H_q.

It must be a UNIT of the whole B for such an equivalence. Section 3 gives a finite residue-functional identity for it from the accepted source bands, with the critical y=-U*d0 term retained. This is distinct from the master's unknown pure-last-axis coefficient c_r. No value or unitness of a_mix or c_r is established. A failure of the raw gradient normalization would not refute all coordinate/row changes; regularity itself does not require a_mix to be a unit.

## 2. Model gradient, and a coordinate-invariant necessary test

The card's NEW gradient identity is correct by formal differentiation. Put

    c(T)=1+sum_(i=1)^d X_i*T^i,
    V_model=(d/N)*[T^N]c(T)^(N/d),
    M_h=[T^h]c(T)^(q/d).

Then partial_i V_model=[T^(N-i)]c(T)^(N/d-1)=M_(N-i). The two scalar derivative factors cancel, and N/d-1=q/d. Every expression is a finite coefficient polynomial over Q; V_model has weight N. The master already proves regularity of this model; it is not re-proved here and says nothing new about the actual H block.

Consider the last-two-variable plane P defined by X_1=...=X_(d-2)=0. Write x=X_(d-1), y=X_d, of weights d-1,d. Weight arithmetic gives, for every d>=3,

    H_q|P=a_mix*x*y,
    H_(q+1)|P=c_r*y^2 + (only if d=3) b_plane*x^3.

Indeed the only solution of (d-1)i+d*j=2d-1 is (i,j)=(1,1). At weight 2d there is (0,2), and the only extra solution for d>=3 is (3,0) at d=3. The symbol b_plane denotes that actual coefficient if present, not an evaluated value or the leading mate constant. The coefficient c_r is exactly the master's last-axis scalar; its formula is not duplicated or expanded.

The plane P is preserved by EVERY graded B-polynomial automorphism. On P, coordinates of weights less than d-1 vanish, X_(d-1) maps to rho*x, and X_d maps to sigma*y, with rho,sigma units in B. There is no monomial of weight d in x alone for d>=3. The inverse automorphism proves the scalar factors are units; they cannot be chosen merely nonzero on one component.

Likewise an invertible weight-preserving row change among weights q,...,m can alter the smallest row q only by a leading B UNIT: there is no lower-weight row to add and negative-weight entries are zero. The model's smallest row restricted to P is

    M_q|P=(q/d)*(q/d-1)*x*y
          =((2d-1)(d-1)/d^2)*x*y,

whose scalar is a nonzero rational unit. Therefore any graded coordinate equivalence followed by an invertible weight-preserving row equivalence between the actual and model blocks NECESSARILY has a_mix in B^times. If a_mix vanishes on even one residue field of B, that component obstructs this particular class of model equivalences. No component may be dropped. This is a new necessary test for MODEL equivalence, not for REG_r.

An exact changed-object control separates those claims. For d=3 take variables of weights 1,2,3 and the block of weights 5,6,7

    (X_1^5, X_2^3+X_3^2, X_2^2*X_3).

Its only common affine point over an algebraic closure is the origin: first X_1=0, then X_2^2*X_3=0 and X_2^3+X_3^2=0 force X_2=X_3=0. Thus it is regular. Yet its critical mixed coefficient on P is zero, whereas the model coefficient is a unit, so no graded equivalence of the specified kind exists. This is NOT an actual source block or counterexample to its regularity. It proves that even regularity and matching weighted Hilbert data would not supply the desired identification.

## 3. Exact actual a_mix identity, retaining the critical fixed band

Use NORMALIZED leading polynomials C=theta^3+F*theta^2+Hlead*theta+a and D, with m*C*D'-n*C'*D=-theta^7; a,Hlead and b=D(0) are leading units. All derivatives in this section are theta derivatives. Let P_-,Q_- be the accepted homogeneous-rho=1 kernel pair at gap r=d-1, and P_+,Q_+ the separately MODIFIED homogeneous-rho=1 pair at gap d. They are uniquely specified by the charged early report's completed inverse and its modified inverse respectively, not arbitrary polynomials with the same leading terms. Write

    P_-,2=p, Q_-,4=p/2;
    P_+,2=e, Q_+,4=2e.

The first equality for Q_- follows from its literal theta6 row delta4=-2; the second is the actual modified theta6 row. Neither p nor e is presumed a unit. No coefficients of these pairs are expanded here.

On P at z=0, the two first bands are x(P_-,Q_-) and y(P_+,Q_+). In particular the actual source coefficients satisfy

    d0=p*x, U=-e*y,
    ycrit=z-U*d0=e*p*x*y.

Thus the critical theta term is NOT zero on this plane. A possible intervening band at gap 2(d-1), proportional to x^2, cannot contribute to the coefficient x*y at total gap q: it would require an absent gap-one term. Weight arithmetic excludes every other contribution to that coefficient. The full mixed bracket at x*y is therefore

    Wmix=(m-r)*P_-*Q_+'-(n-d)*P_-'*Q_+
         +(m-d)*P_+*Q_-'-(n-r)*P_+'*Q_-.

The direct critical target is -2z*theta^5 and is zero at z=0. Factoring out x*y in the actual critical band, write

    Ycrit=e*p*theta+k, deg Vcrit<=2,
    m*(C*Vcrit'-C'*Vcrit)+r*Ycrit*D'-n*Ycrit'*D
                    =-Wmix+R,
    R=R1*theta+R0.

This includes the fixed critical product as well as both residual rows and all mixed forcing. Its theta5 cancellation is explicit:

    [theta5]Wmix=2e*p,
    [theta5](r*Ycrit*D'-n*Ycrit'*D)=(5r-n)e*p=-2e*p.

It would be wrong to keep Wmix but replace Ycrit by k. The three actual upper pivots are m,2m,3m as in the charged critical report; they define the whole Vcrit with the fixed gauge. The variable k is the coefficient at this factored critical monomial, not an added source parameter or a gauge.

Extend the accepted finite critical functional to degree-at-most-five polynomials by the SAME coefficient formula

    Gamma(F)=(21a/(2Hlead))*[theta^7] C^2
                    integral_0^theta F(tau)/C(tau)^2 d tau.

This is finite formal coefficient extraction, not an analytic residue or a new localization. The integral divides only fixed nonzero integers; C has unit constant a. Let lambda1,lambda0 be the accepted critical left inverse of the homogeneous k column chi. Its determinant-one complement is vperp=-lambda0*theta+lambda1. Set gamma=Gamma(vperp). The accepted critical zero-earlier control proves gamma*c_z=1 for its compatibility coefficient c_z, so gamma is a unit of the WHOLE leading base and hence of normalized B.

For the actual equation just displayed, put T=m*C*Vcrit-n*D*Ycrit. A direct differentiation using n+r=2m gives the exact identity

    C*T'-2C'*T=2theta^7*Ycrit+C*(R-Wmix),
    (T/C^2)'=2theta^7*Ycrit/C^3+(R-Wmix)/C^2.

Here deg T<=6. The retained term 2theta^7*Ycrit/C^3 integrates to order at least eight; it is only for THIS coefficient extraction that it cannot contribute to theta7. The integration constant times C^2 also has no theta7 coefficient. Consequently Gamma(R)=Gamma(Wmix). This is not a shorthand which drops ycrit before upper reconstruction.

After the accepted critical elimination, R=vperp*a_mix at coefficient x*y. Therefore the new exact identity is

    gamma*a_mix=Gamma(Wmix),
    a_mix=gamma^(-1)*Gamma(Wmix).                 (*)

Every symbol in (*) is specified by the accepted early/modified/critical maps over the whole B. It does not assume any later compatibility or source solution, and no actual coefficient array or value is generated. Since gamma is a unit, the model-equivalence obstruction from section 2 is exactly the unknown unitness of Gamma(Wmix). This is a distinct mixed-kernel scalar, not the master's pure-last-axis calculation. Its unitness is NOT established by the homogeneous chi-column unitness.

The same calculation yields a STRONGER full critical identity, not restricted to P. Retain the ENTIRE actual earlier forcing W_q and Ycrit=(z-U*d0)*theta+k_r. The actual target is now -2z*theta5. With the same T, the exact equation is

    C*T'-2C'*T=2theta^7*Ycrit
                   -2z*C*theta5-C*W_q+C*R.

The accepted finite coefficient calculation gives Gamma(theta5)=1/2. Taking the justified theta7 coefficient as above therefore gives Gamma(R)=z+Gamma(W_q). After the actual critical elimination this is the universal polynomial identity

    gamma*Psi_q=z+Gamma(W_q).                    (**)

All earlier particular bands remain inside W_q; neither U*d0 nor the target is omitted. Earlier gaps are less than q, so W_q is independent of z in the normalized graded envelope. Hence gamma*H_q=Gamma(W_q(X)) at z=0. On the faithful z=1 slice the actual critical row is exactly 1+Gamma(W_q(X))=0 up to the unit gamma. This is an exact forcing interface, not a solution or an identity making that polynomial zero before imposing it. It does not justify inverting Gamma(W_q) in the unconstrained polynomial ring. Every other high and low row, gauge, inverse-polynomiality condition and guard remains untouched.

## 4. Exact curvature test; why a fixed-target variational argument is insufficient

For the actual reversed block put G_i=H_(N-i) and form the polynomial one-form alpha=sum_i G_i*dX_i. There is a canonical weighted radial candidate

    V_rad=(1/N)*sum_(i=1)^d i*X_i*G_i.

It has weight N, but is NOT asserted to be a potential for the block. Weighted Euler's identity for G_j, of weight N-j, gives the exact defect formula

    partial_j V_rad-G_j
       =(1/N)*sum_i i*X_i*(partial_j G_i-partial_i G_j).

Consequently closedness of alpha would prove dV_rad=alpha, and conversely being a gradient forces closedness. This makes the missing actual curvature statement explicit; it does not prove it. The same test may be applied after a SPECIFIED invertible row/coordinate change, not after merely naming a convenient normalization.

On the last-two plane the pullback of alpha is

    (c_r*y^2 + (only if d=3)b_plane*x^3)*dx
                       +a_mix*x*y*dy.

Its exact exterior derivative is

    (a_mix-2c_r)*y*dx wedge dy.

For specified leading-unit rescalings eta_(d-1),eta_d of these two reversed rows, the condition becomes eta_d*a_mix=2eta_(d-1)*c_r. No such identity or rescalings have been established from the actual coefficients. If both scalars were units, this single relation alone could be arranged by a row rescaling, so it would not settle all cross-partials or graded model equivalence. Failure in one raw normalization refutes only that normalization. This is a necessary differential test, not a new value claim for the master's axis scalar.

The ACTUAL target cannot be held fixed when varying the kernel variables. In the normalized source write

    Pi=z*tau-U*tau^2+S*tau^3,
    Delta=s^7+U*s^5*tau-E*tau*Pi-tau*Pi^2.

Let d_X act on source coefficients with B,S,tau,s,z fixed. Then d_X Pi=-tau^2*d_X U, and direct differentiation gives

    d_X Delta=(s^5*tau+E*tau^3+2tau^3*Pi)*d_X U
                                      -tau*Pi*d_X E.

For the homogeneous upper target at z=0 this reads

    d_X Delta^+=(E*tau^3-2U*tau^5+2S*tau^6)*d_X U
                            -tau*(-U*tau^2+S*tau^3)*d_X E.

For a variation indexed i the residual derivative is EXACTLY

    partial_i([Ahat,Bhat]-Delta)
       =[partial_i Ahat,Bhat]+[Ahat,partial_i Bhat]
                                            -partial_i Delta.

These identities are polynomial and remain valid after all actual band substitutions, including E=E_*(X,z) and U=U_*(X). They are ambient coefficient identities before imposing the remaining source equations, not a claim that every coefficient direction is tangent at a full source point. They preserve the actual modified theta6 target, the -U^2*theta5 term and the lower source targets. As a changed-parameter control, even at formal U=E=0 the upper-target variation in a U direction is 2S*tau^6, not zero. Thus a derivation which uses a fixed-target linearized Jacobian has changed the source operator. This does NOT prove that the final actual curvature is nonzero: cancellations with the reconstructed mate and a suitable row transformation remain possible and unproved.

No zero-target homogeneous surrogate, missing B3(0)=U^2 term, discarded critical y term or omitted low row is used in (**). Free z=0 is an associated-graded diagnostic, not a source specialization or a z=1 point. A gradient identity by itself would still not imply an isolated critical locus; the actual identification with the already-regular model would need its own exact invertible maps.

## 5. Verdict and precise remaining obligation

Verdict: NO_CLOSING_IDENTITY. No actual potential whose gradient is the transformed H block, no graded model equivalence and no new REG_r proof is obtained. The bankable actual advance is (**), together with (*) and the necessary MODEL-equivalence unit condition a_mix in B^times. Its smallest exact unresolved scalar is Gamma(Wmix), over the entire B, with the two charged kernel pairs and the complete critical fixed term specified above. A zero on one B component would obstruct the proposed graded equivalence there; a unit result would merely pass this test and leave the other curvature and coefficient identities open.

The complete bridge would require explicit graded coordinate and weight-preserving row transformations, proof that their diagonal/determinant factors are units on every component, and exact equality with the model gradient. Neither assigned weights, homogeneous column units, the radial candidate, one cross-partial, nor model regularity supplies that bridge. No coefficient implementation queue, solver plan, source exclusion, degree bound, whole-F10 closure or JC2 conclusion is proposed.

## 6. Read scope and publication

Exactly six inputs: the ROOT card and its five reports. The card hash was checked before its WHOLE 1--33 read; all six hashes then matched before fresh WHOLE report reads: master 1--211, Euler 1--251, early 1--322, middle 1--282, critical 1--260. All were read through their final markers/Seals without clipping. No prior WHOLE reuse and no linked provenance traversal. No actual coefficient/fixture/candidate body or live peer/gate/receipt was read.

ROOT's lifecycle message at 22:42 reported accepted FIRST A--G clearance for the master after independent terminal custody and current-pin/WHOLE intake. Its producer delta2=-(3j+1) remains the correct formula; no reviewer typo is imported. This discharges the sole provisional parent without adding a file input or promoting this new result. ROOT's gradient and full-critical-identity suggestions were treated as proposals and checked manually above, not as additional accepted theorem premises.

ZERO mathematical subprocesses of ANY size, coefficient payload generation, code/import/AST/syntax/test/CAS, network/AWS/SSH/process/Git/protected/corpus/shared access or new agents occurred. All authored bytes use apply_patch plus the existing documentary artifact transaction. Previous involvement is disclosed: this author wrote the charged master and several parent producers; this is new manual composition, not a fresh foundation review. ROOT alone owns intake, any FIRST review and promotion. No follow-on authority is created.

## OPEN(S) RAISED

No new canonical task ID. QUANTITY: unitness of the exact mixed-kernel scalar Gamma(Wmix) on the whole leading B, unknown; this is a necessary graded-model-equivalence test, not the REG_r condition. CHEAPEST mathematical discriminator: simplify (*) using the charged two finite kernel inverses and critical functional while retaining y=z-U*d0; no such coefficient/unit calculation is claimed completed or authorized to run. Actual full curvature/graded model identity remains unproved.

## COLLISIONS

status: EMPTY

Own-only review; no corpus-wide novelty claim. Model regularity and the old axis formula are credited, not duplicated. The new full critical forcing identity, its mixed coefficient projection and the precise model-equivalence obstruction are distinguished from those premises. All six current input pins, own PINS WHOLE and own-only OPEN/collision checks matched/completed at 22:51:37 UTC; the final draft was read fresh WHOLE without clipping by 22:52:17 UTC. This documentary completion line was separately re-read before the sole final marker. No further mathematical exploration followed.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `17629`.
- Body SHA-256:
  `f34dabc94bb589a470d4c119e0c84b5896263129a726e710706ad577d25e34af`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
