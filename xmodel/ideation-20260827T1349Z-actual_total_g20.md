# Blind whole-portfolio ideation — `actual_total_g20` — `20260827T1349Z`

Date: 2026-08-27  
Packet SHA-256:
`8af21da712a3de8184ca708d75e7461e831f597aac459317d8871176e73b48c3`

Status: **SEALED BLIND SUBMISSION.**  I read the packet, its frozen canonical
inputs, the prior `1104Z` synthesis, and the frozen web sweep.  I did not read
another `ideation-20260827T1349Z-*` submission.  Events after the packet
cutoff are not used below.  In particular, GGV R3 remains a named
**PROVISIONAL** premise under hostile review.

## 0. Decision summary

The provisional total certificate should be finished and reviewed, but a
PASS is a local chart closure and should release, not attract, critical-path
capacity.  The proof portfolio should then concentrate on two independent
global interfaces:

1. compile raw polynomial GGV data into a factor- and deck-tagged finite
   etale algebra and decide the now-exhaustive R2 carrier equation there; and
2. replace serial K00 jet anecdotes by exact-Q constructible/Fitting
   obstructions on the reviewed common mixed source.

The best counterexample lane remains polynomial realization of a
source-reachable formal or `p`-adic survivor, with residual AS109 `n=6` as
the most concrete bounded client.  Random ambient Keller search still has
poor information gain.

Matysiak's claimed proof is an urgent acquisition/audit lane, not evidence.
Its square-free language suggests one precise internal interface test with
the finite-etale factor descent below, but no theorem may be imported from
the abstract.

## 1. Complete avenue disposition vector

Legend: `U=unchanged`, `R=raise`, `L=lower`, `O=reopen`.  This is relative
to the frozen `APPROACHES.md` overlay, not to the historical 2026-08-21
scores.

| avenues | disposition |
|---|---|
| 1--6 | **1 L**, **2 R**, 3 U, 4 U, 5 U, 6 U |
| 7--12 | 7 U, 8 U, 9 U, 10 U, 11 U, 12 U |
| 13--18 | 13 U, 14 U, 15 U, 16 U, 17 U, 18 U |
| 19--24 | 19 U, 20 U, 21 U, 22 U, 23 U, 24 U |
| 25--30 | 25 U, 26 U, 27 U, 28 U, 29 U, 30 U |
| 31--36 | **31 R**, 32 U, 33 U, **34 R**, 35 U, 36 U |
| 37--42 | 37 U, 38 U, 39 U, 40 U, 41 U, 42 U |
| 43--46 | 43 U, 44 U, 45 U, 46 U |

Reasons for every change:

- **1, lower:** do not spend new capacity enumerating another bounded GGV
  degree/corner farm.  The current local inventory is already ahead of the
  global source/landing, fidelity, and cofinal-ceiling theorems.  V47/V48
  further reduce the value of serial bookkeeping inside one registered box.
  Existing exact chains remain indispensable fixtures for avenue 2.
- **2, raise:** R2 has reduced the first determinant carrier, even with all
  higher-`u` sidecars, to two factorwise forms.  The remaining raw/global
  problem is therefore finite enough to receive a new exact descent test
  rather than another informal transport proposal.  This raises marginal
  capacity, not theorem status: no `G2-PSC`, `G2-BD`, coverage, or ceiling
  follows from R2 or provisional R3.
- **31, raise:** the finite-etale quotient needed to glue the R2 factors is
  an exact integrality/Zariski-main client, and the newly discovered external
  claim explicitly advertises a square-free/image-subring condition.  The
  raise is to one bounded internal descent experiment and a dependency-first
  source audit, not to belief in the unaudited proof.
- **34, raise:** provisional R3 supplies the first sharp campaign client for
  rational pole removal in this boundary setting: enumerate resonant
  rational modes, then use pole order and a polynomial-image operator at the
  target coefficient.  Even on PASS it excludes only the artificial
  squarefree replacement edge; the raise is to generalize/test the mechanism
  on one genuine face.

No avenue is reopened.  In particular, row 44 stays closed as a proof input;
an unrelated new proof claim does not rehabilitate Moskowicz's refuted
argument.  The provisional C5 and V24 results are too local or conditional
to change any other top-level avenue ranking.

## 2. Reranked bottlenecks

### Proof bottlenecks

1. **Complete source, landing, fidelity, and cofinality.**  Starting from an
   arbitrary Keller pair, land in a complete paired boundary object; prove
   the packet-to-decorated-tree map with fibre, conjugation, deck, pole, and
   residual-cancellation fidelity (`G2-PSC` where that hybrid architecture
   is used); cover equality, positive-load, ramified, and off-family faces;
   and prove an absolute/cofinal type or degree ceiling.  A finite list of
   emptied contacts is not this theorem.
2. **Terminal receiver reachability.**  On the reviewed common K00 mixed
   source, decide the exact constructible strata with all load weights,
   targets, and `Jdet`, and then distinguish a compatible finite jet from an
   all-order/source-reachable arc.  Three separate local covectors and a
   conditional Cramer relation are not one terminal obstruction.
3. **Global polynomial provenance inside one honest boundary chain.**  R2 is
   rootwise complete, but the formal cleanup must be the image of the raw
   `2S/3S` slices, factor choices must glue over `Q`, and the global
   `H`-multiple must be controlled.  This is the best bounded attack on
   bottleneck 1.
4. **`G2-BD` for residue-A descendants.**  Keep this architecture-specific
   boundary-divisor input separate from `G2-PSC`; closing one does not close
   the other.
5. **Local chart closures.**  C5 may close the frozen ordered-`a1` raw chart,
   but after review it becomes a reusable leaf and drops below the preceding
   interfaces.

### Counterexample / falsification bottlenecks

1. **Polynomial algebraization with source provenance.**  Turn one exact,
   source-reachable formal infinity survivor into a finite polynomial pair,
   with bounded support/conductor and a direct `J=1` replay.  Formal
   consistency or a Padé fit alone is not a counterexample.
2. **A bounded polynomial lift of a collision.**  Residual AS109 `n=6` is
   the most explicit current object.  It must survive all integral equations
   in a fixed finite support, not merely every Witt level with growing
   support; a fixed `Z_109` polynomial lift would then be a serious
   characteristic-zero route.
3. **An actual collision and classical-frontier compliance.**  Any survivor
   must carry an explicit off-diagonal collision and pass the honest total-
   degree gate.  Neither a local nonzero class nor a nonproper formal germ
   automatically supplies it.
4. **Falsifying a global bridge.**  A matched-visible-data Keller control
   that changes a decorated tree field would refute a proposed `G2-PSC`
   packet.  The existing `8_28` mutation is only non-Keller and therefore
   diagnoses missing fields without falsifying the Keller bridge.

## 3. New mechanism and cross-avenue connection

### 3.1 New execution: finite-etale carrier descent

Let

```text
A = Q[X]/(H),       h' = H' mod H,
```

for the squarefree common edge polynomial.  Instead of allocating roots and
later asking whether their local coefficients came from one raw source,
compile every allowed raw coefficient and every permitted cleanup through
weight 22 directly into `A`.  The output consists of provenance-constrained
elements

```text
V8, U11, U14 in A
```

plus the lower-weight vanishing relations and deck/conjugation tags.  R2's
two factorwise cases then have the single necessary carrier equation

```text
h' * (6*V8*U14 + (9/2)*U11^2) = 1_A,                 (3.1)
```

with `U11=0` on every factor where `V8` is nonzero and `V8=0` on the
complementary factors.  Enumerate the finitely many rational factor divisors
of `H`, or equivalently their idempotents, and impose the appropriate
zero/unit saturation on each factor.  The multiplication table and trace
pairing of `A` turn (3.1), raw-image membership, and the deck equations into
a finite exact constructible system.  An exact elimination or a trace/Macaulay
dual can then certify that the unit is outside the reachable carrier image;
a solution returns global rational coefficients rather than unrelated local
interpolants.

This changes the order of work: **descent precedes root allocation**.  It
attacks precisely R2's mixed-factor and global-`H` gap while preserving the
raw/local firewall.  A failure for the artificial edge is only that edge; a
failure on a frozen genuine complete chain would be a real boundary-family
exclusion, still not a fan cover or degree ceiling.

### 3.2 Repository/history check

I searched the frozen canonical files and historical reports for
`rational-mode`, `F^(3/2)`, `pole ODE`, `mixed-factor`, `idempotent`,
`Q[X]/(H)`, `trace pairing`, `Fitting obstruction`, `square-free factor`,
and `Matysiak`, and also ran exact-string commit-history searches.

- `F^(3/2)`, pole ODEs, and finite-etale trace pairings are **KNOWN** tools
  elsewhere in the repository; none is claimed new.
- R2 and its hostile review already state the factorwise dichotomy and say
  that `gcd(H,V8)` or idempotents of `Q[X]/(H)` separate mixed branches.
  That observation is **KNOWN**.
- Fitting/cokernel proposals already occur in K00 and TD6 reports; Card 2
  below is a redesigned execution, not a new avenue.
- I found no pre-R3 artifact giving its complete six-mode rational normal
  form, and no artifact composing the **raw permitted coefficient-image
  modules**, the R2 idempotent split, equation (3.1), and an exact
  trace/elimination obstruction.  That finite-etale raw-image composition is
  the genuinely **NEW EXECUTION/MECHANISM** proposed here.
- The repository contains no primary definition of Matysiak's `5''s`; only
  the frozen web-sweep metadata is present.  Therefore no internal statement
  may be identified with that condition yet.

### 3.3 New cross-avenue connection

This is a concrete connection among **2 (boundary trees), 31
(integrality/finite-etale descent), and 34 (rational pole removal)**.  R2
supplies the factorwise local alternatives; avenue 31 supplies descent in
the reduced algebra `A`; provisional R3 supplies a rational-mode normal form
and pole-image obstruction that can be applied before or after (3.1).

The Matysiak claim adds a conditional test, not a premise.  Once `5''s` is
read, ask whether it merely preserves reducedness of a squarefree element or
actually transports factorization/gcd data in the image subring strongly
enough to preserve the primitive idempotent partition used above.  Test the
asserted implication on the mixed R2 fixture
`H=(X^4+1)(X^4-1), V8=X^4+1` and on automorphisms before using it.  If the
condition cannot even type this factor partition, there is no bridge.  If it
does, it may furnish exactly one missing descent lemma, but it still must be
proved from the Keller hypotheses without factorial-closure or properness
circularity.

## 4. Strongest attacks

### Strongest proof attack

Build the finite-etale carrier-descent compiler on the frozen `8_28`
Keller-face replacement, then immediately port it to one **genuine complete
GGV chain**, retaining all fibre/deck/pole labels.  In parallel, let the K00
lane decide exact-Q constructible terminal strata.  These are independent:
the first attacks global landing/fidelity and the second attacks the terminal
receiver.  Neither should wait for the other or for ordinary review latency.

The decisive proof-side success is not “one more local formula.”  It is an
exact certificate that the raw global coefficient image cannot satisfy
(3.1) on a genuine chain, or a provenance-complete solution that can be fed
unchanged into the decorated-tree and terminal compiler.  Run one chain only
before deciding whether to shard the whole inventory.

### Strongest counterexample/falsification attack

Select the deepest source-reachable formal survivor with a named collision
target—prefer residual AS109 `n=6` as the bounded arithmetic client—and solve
for a **fixed** support over `Z_109`, including all coefficient equations,
the collision, and the three alpha/beta content strata.  Search supports by
incremental unsat cores, not by expanding a blind rectangle.  A fixed
polynomial lift with exact determinant and a certified collision would
immediately outrank all proof-side leaves; continued survival only with
growing support is evidence for a restricted-analytic limit, not a
counterexample.  The formal-germ lane should receive the same bounded-
conductor/algebraization gate.

## 5. Software/AWS acceleration and firewall

Implement one provenance-carrying exact algebra service with two job types:

1. finite-etale quotient jobs for each divisor/idempotent pattern of `H` and
   each frozen GGV face; and
2. K00 exact-Q component/Fitting jobs after modular rank scouts.

Sparse modular runs may discover pivots, factor partitions, and candidate
duals across AWS shards.  Only rational reconstruction followed by an exact
`Q` replay may produce a verdict.  Use many small, memory-capped instances;
do not reserve a 1-TiB host until telemetry shows that one exact component
needs it.  The local machine runs orchestration and desk replays only.

Correctness firewall:

- pin the packet, source manifests, raw-row hashes, quotient polynomial,
  factorization, chart, and deck orientation in every job;
- serialize the raw-to-cleanup-to-`A` map, never only its endpoint values;
- recompute the original coefficientwise determinant `E` directly from raw
  `F,G` after every purported carrier solution;
- require an independent quotient-multiplication engine to agree with the
  sparse-series engine;
- treat modular emptiness, a zero normal form in a localized unit ideal, and
  numerical evaluation as discovery only;
- fire at least a relaxed-raw-slice positive control, a deck/orientation
  mutation, an omitted-global-`H` mutation, a target-unit mutation, and one
  factor-partition mutation;
- emit exact certificates and replay commands, then obtain different-model
  hostile review before promotion.

## 6. Detailed idea cards

### Card 1 — finite-etale raw carrier descent

- **Dependencies:** reviewed R1/R2; frozen raw `2S/3S` support; a typed formal
  cleanup through weight 22; squarefree `H`; exact fibre, conjugation, deck,
  and chart tags.  R3 is optional and remains provisional.
- **Cheapest discriminator:** for `H=X^8-1`, construct `A`, compile the exact
  allowable images of `V8,U11,U14`, enumerate the rational factor partitions,
  and decide (3.1) with its lower-weight/zero-unit conditions.  Run one
  relaxed-slice positive mutation that admits the old local interpolant.
- **PASS interpretation:** an exact raw-provenance solution survives.  Feed
  it into a direct raw `E_0,...,E_22` replay and the next jet; do not call it
  a Keller pair or `G2-PSC`.
- **FAIL interpretation:** an exact Nullstellensatz/dual certificate excludes
  the frozen replacement edge at raw-global scope.  On a genuine chain, the
  same kind of certificate excludes only that registered face/chain.
- **Stop:** stop after one exact base verdict plus one positive mutation and,
  if technically successful, one genuine complete chain.  Do not launch a
  fan-wide shard before that second result.
- **Rollback:** a compiler/review failure withdraws only this global-image
  verdict; R1's type repair and R2's rootwise carrier theorem remain.
- **Expected information gain:** very high.  It directly separates “local
  carriers exist” from “one polynomial source realizes them,” the present
  global wall.

### Card 2 — exact-Q K00 Fitting/constructible gate

- **Dependencies:** reviewed V20R2 and V22; frozen V23 block; V24 may be used
  only at the exact scope surviving its hostile review, including all pivot
  and normalization localizers.
- **Cheapest discriminator:** over `Q`, decide properness/nonemptiness of the
  prior ideal plus `F10` and the two grade-seven compatibility polynomials
  (write them as `mathcal C_6,mathcal C_7` to avoid the chart-coordinate
  collision).  Saturate in the stated order.  On each surviving component,
  compute the representation-independent next Fitting minor or rational
  left dual, not an arbitrary Cramer presentation.
- **PASS interpretation:** exact unit ideal empties only that constructible
  chart/stratum; a proper ideal returns exact components on which the next
  weighted grade proceeds.  Neither outcome alone decides an all-order arc.
- **FAIL interpretation:** if V24 review narrows or refutes its theorem,
  rebuild from the reviewed V23 block; if computation merely exhausts
  resources, change representation rather than infer geometry.
- **Stop:** stop serial prolongation after two consecutive grades with no
  codimension/rank change and no new exact dual, then redesign toward an
  all-order derived obstruction.  Stop immediately on missing source or
  localization provenance.
- **Rollback:** quarantine only V24-dependent strata; V20R2, V21, and V22
  retain their reviewed scopes.
- **Expected information gain:** high.  It tells us whether the first closed
  K00 prefix is actually dying or merely moving into a smaller component.

### Card 3 — dependency-first `5''s` acquisition and audit

- **Dependencies:** lawful bytes for SSRN 7229358 and 7229458, preserved with
  hashes.  No abstract-level mathematical premise is allowed.
- **Cheapest discriminator:** read 7229358 first.  Formalize the exact
  definition and quantifiers of `5''s`, then trace the first implication from
  constant Jacobian to that condition and the first implication from it to
  automorphy.  Test both directions on polynomial automorphisms and simple
  non-Keller endomorphisms/subrings.  Only then inspect the first genuinely
  dimension-two step in 7229458.
- **PASS interpretation:** if every dependency is noncircular and the first
  dimension-two step survives independent reconstruction, launch two
  different-model full audits and map each lemma to the claim DAG.  Do not
  announce a solution before the whole proof and dependencies pass.
- **FAIL interpretation:** a hidden injectivity, properness, birationality,
  factorial-closure, image-UFD/normality, or false dimension count stops the
  proof claim.  Preserve any independently correct squarefree lemma and test
  it through the finite-etale carrier interface.
- **Stop:** stop the proof audit at the first load-bearing fatal gap and
  produce a precise counterexample or circularity trace; do not spend cycles
  polishing downstream prose.  If access remains blocked, use author contact,
  an interactive lawful session, or institutional/library acquisition and
  otherwise keep a low-cost alert only.
- **Rollback:** external-audit descendants only; no internal promoted claim
  changes because the abstract was never a premise.
- **Expected information gain:** maximal conditional on acquisition: either
  an externally claimed resolution enters real audit or a tempting global
  shortcut is removed quickly.

## 7. Current major-lane decisions

| major lane | decision | exact next action / stop rule |
|---|---|---|
| V43C5 pure total certificate | **continue** | Finish hostile review.  On exact-scope PASS, bank the chart and release its compute share; on reversal, follow section 8. |
| V43 certificate minimization / alternate exporters | **redesign** | Keep only custody-sized compression or a support-sized receiver-map client after PASS.  Stop pole/minimal-support searches that cannot change transport cost or theorem scope. |
| Common K00 V20--V24 | **redesign** | Exact-Q saturated component/Fitting gate from Card 2; reviews remain asynchronous.  Do not infer an arc from any finite compatible jet. |
| Global GGV / `G2-PSC` | **redesign** | Finite-etale raw-image descent first, then one genuine complete-chain packet/tree replay.  No new degree farm. |
| GGV R3 artificial edge | **continue** | Harvest hostile review; promote only its exact replacement-edge scope on PASS.  Generalize only through Card 1 and one genuine face. |
| `G2-BD` residue-A boundary input | **continue** | Maintain a separate dependency and client; never mark it discharged by a `G2-PSC` prototype. |
| V47R1/V48 strict contacts | **stop** as a critical lane | Bank the reviewed 12+4/8 compiler.  Reopen compute only for a coverage theorem's named equality, positive-load, ramified, or off-family client; do not enumerate more interior contacts. |
| Equality / positive-load / `rho=0` / off-family coverage | **continue** | Choose faces from a global coverage inventory, not serial curiosity; each endpoint retains theorem type and localization. |
| AS109 residual `n=6` | **continue** | Three content strata and one source-complete fixed-support lift/algebraization test.  Stop W2/W3, top-band, and blind rectangles. |
| Existing capped TD6 survivor | **continue** | Finish only the already registered exact/source-complete cap.  Stop new parallel bands unless it returns a new algebraization invariant. |
| Formal-germ/algebraization | **continue** | Select one source-reachable survivor and impose bounded conductor, exact gluing, collision, and polynomial realization as the next gate. |
| Ambient sparse/random counterexample search | **stop** | Search only a named residual family above the classical frontier with exact collision and support provenance. |
| Matysiak/external intelligence | **continue** | Lawful two-paper acquisition, dependency first, hashes and independent audits; low-cost alert while blocked. |
| Reviews and replication | **continue** | Run in background; only reviewed exact scope enters canonical ledgers. |

## 8. Explicit branch analysis

### 8.1 Provisional C5 total certificate

**Exact-scope PASS.**  Promote only

```text
a1^628 in J_total subset Q[t,X19_total]
```

for the frozen literal grade-through-19 raw ordered chart.  Consequently the
localization at `a1` of that exact ideal is the unit ideal.  Preserve the
25-row circuit and all source hashes.  Mark the ordered raw chart closed,
stop alternative converter/exporter searches, and transfer almost all of
that capacity to K00 and global landing/cofinality.  The sole useful child is
a fail-closed, support-sized row map to one named receiver.  A PASS of that
map is ordinary transported membership at that receiver; it is not
`G2-PSC`, terminal reachability, coverage, Gate T, order two, maximum twelve,
or JC2.

**PASS with custody/presentation repair or narrowed scope.**  Preserve C5
bytes, add a repair artifact, and promote only what the hostile review
actually recomputes.  No descendant assumes chart closure until source
identity, ambient ring, target, and all 25 rows are confirmed.

**Mathematical reversal or scope change.**  Quarantine C5 and every child
that uses `a1^628`.  Retain the two independently reviewed parents exactly:
`a1^104 in J0` and `5*t^6*a1^4 in J_total`.  Diagnose whether the failure is
the special-to-total lift, converter algebra, row custody, or target replay;
only the failed interface is rebuilt.  Do not withdraw K00, R1/R2, V47/V48,
or AS109 results, and do not revive a `t`-division argument.

### 8.2 Matysiak acquisition/audit branches

**Both PDFs acquired.**  Hash and freeze them, audit 7229358 before 7229458,
and reconstruct definitions and arrows independently.  If the dependency
and first dimension-two step pass, run two full different-model audits in
parallel.  A complete PASS would immediately supersede the internal
schedule, subject to human publication/communication decisions; anything
less is a lemma, not a solution.

**Only 7229458 acquired.**  Extract its dependency graph and the exact cited
statements, but do not approve any step whose proof is deferred to 7229358.
Continue acquiring the predecessor.

**Access remains blocked.**  Do not evade Cloudflare.  Use an interactive
lawful session, author/institution mirror, direct author request, or library
route; retain one low-cost alert.  Independently test only generic
squarefree/image-subring formulations, clearly labeled as campaign
hypotheses rather than Matysiak's condition.

**Dependency fails or is circular.**  Record the first exact fatal step and
one countermodel where possible, stop downstream proof audit, and salvage
only independently valid lemmas.  The internal portfolio is unchanged.

**A useful squarefree lemma survives but the proof does not.**  Type it
against the finite-etale carrier descent: does it preserve the factor
idempotents and raw-image membership actually needed for (3.1), or merely
say that a polynomial has no repeated factor?  Promote only the former exact
interface if separately proved.

## 9. Allocation and scope firewall

After a C5 PASS, a fast allocation is approximately

```text
32%  global source/landing, finite-etale carrier descent, G2-PSC/cofinality
28%  common K00 exact-Q/Fitting and terminal reachability
15%  equality/ramified/off-family coverage and separate G2-BD
15%  disciplined counterexample algebraization (AS109/formal survivor)
 5%  external acquisition/audit
 5%  software integration and independent review overhead beyond lane-local review
```

The chart PASS itself earns essentially no continuing 20% allocation.  If it
reverses, at most one bounded repair lane is restored; global/K00 work does
not wait.

Nothing in this submission proves or disproves JC2.  In particular:

- a total raw chart certificate is not a receiver, landing, or cover;
- a finite-etale carrier solution is not a polynomial Keller pair;
- an exclusion of one artificial or genuine face is not a fan cover or
  cofinal ceiling;
- a finite K00 compatible jet is not an arc, and a local class is not a
  source-reachable obstruction;
- a squarefree abstract slogan is not Matysiak's unavailable definition or
  theorem;
- `G2-PSC` and `G2-BD` remain distinct obligations.

