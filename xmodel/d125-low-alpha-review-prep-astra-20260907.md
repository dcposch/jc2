# D125 low-alpha: one independent Fable gate prepared, not launched

2026-09-07. **PREP ONLY.** One <=20-minute independent Fable review is specified
in `box/d125-low-alpha-review-prep-20260907/prompt.txt`. Root must whole-read
and replay before deciding whether to launch. No launch, promotion, mathematical
descendant, source change, worker action or blind-round packet mutation occurred.
The new2200 blind round was not read. This is a separate post-snapshot gate.

The prompt has12 explicit `charged_input=` declarations,12 unique basenames,
and literal `{{LANE_INPUTS}}`. It charges the complete terminal low-alpha
report/transaction, current checker, both final witnesses, final replay/custody,
the exact accepted14c/f/g/v proof reports, and a byte-identical renamed helper.
The only renamed file is `zero-k-factor-helper.py`, copied from the existing
degree-five helper whose basename otherwise collides with the main `check.py`.
No charged input was edited. All15 entries in the producer's custody were
rechecked; this is pin verification, not another proof or replay gate.

`PINS.json` records all12 complete paths, basenames, sizes and hashes, the
helper's original path/hash, the unused proposed reviewer report name, and
the explicit no-launch/root-review conditions. The proposed output is
`xmodel/d125-pure-low-alpha-gate-fable5-20260907.md`, with a separate owned
reviewer box. This prep did not reserve or create those reviewer outputs.

The required review covers the moving critical-root embedding and scalar
extension, coefficient field and nonconstancy/parity, ALL scalar b_s(R_s),
truncation [F,G]>=3j, possible cancellation in the min(d+f,a/2+2f) step,
necessary3a>=2j and its unresolved stronger reading. It separately checks
the actual unguarded prefix against full faces/lifts/raw rows, its failure
of the saturated low equations, and the corrected b3 scalar identity.
The existing accepted premises remain at their exact external-theorem trust;
no unrelated historically provisional descendant is charged as a premise.

The main checker cannot run directly in an arbitrary flat corpus because its
helper lookup uses a repository-relative layout. The prompt explicitly tells
the reviewer to reconstruct ONLY the two pinned files in a fresh owned scratch
hierarchy, byte-identically, and check their hashes before/after. It requires
all six named modes normally and with -O, <=30wall/25CPU/512MiB per arithmetic
child, Python -B before imports, exact final-witness stdout comparison and
mutation-specific failure checks. Path-dependent traceback hashes need not
match after relocation. No uncharged sibling or preliminary output is needed.
No actual high source powers, full source stream, CAS, AWS/SSH or solver is
permitted. No new runner/framework was added or executed in this prep.

Prompt SHA256:
`88398e5cbc35f7589af8a14ee9f274398527f1a878c6055700c6d78d0d21d384`.
PINS SHA256:
`d5462bbb0393acf34cb803451f02ab329a743923ad4155c15ab6425b101214c3`.
Renamed helper SHA256:
`25be0862730df8b66076eb59a2d1d0909fbcf563ea73ce16924affdc7c70ad66`.

Publication uses begin/close/finalize/verify. Own artifact/input custody is
recorded beside the prompt. All writers are idle at completion. Root's
whole-read/replay and any launch remain pending. **STOP/IDLE.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `3298`.
- Body SHA-256:
  `596533e3e339e014670d826fdbc482905d8072511d5aa0b35396ab128aced168`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
