# One-place full-row-rank certificate for the complete r3 source cone

NEW CLIENT APPLICATION OF KNOWN RANK SPECIALIZATION; UNREVIEWED manual theorem/acceptance contract. No actual rank, place, coefficient, certificate or source outcome. First action 2026-09-11 00:32:23 UTC; report, manifest and box absent. Original reserve 00:43 / HARD 00:46 UTC unchanged. Exactly seven sources freshly read WHOLE after their complete current-pin barrier.

## 1. Precise positive implication

**Conditional YES.** Import the accepted complete r3 cone and weight-30 map

    A:B^1453 -> B^297, b=coefficients of g=H_7*ell.

Here B is the ACTUAL number field, not just an unspecified rank-seven algebra. The charged irreducibility theorem applies because r=3 is in the class 3 modulo 7: its septic is irreducible over Q_7, hence Q. Monic rational rescaling does not change B. This is an accepted import, not a new factorization proof, and says nothing about which prospective reduction prime is suitable.

Let O be a domain embedded in B, with fraction field B, containing the entries of A, and let pi:O->k be a genuine finite residue-field map. Suppose the independently authenticated reduction Abar has row rank 297 over k. Equivalently, some 297 distinct columns give M with det(Mbar)!=0.

Determinants commute with pi, so Delta=det(M) is nonzero in O and therefore in B. Since B is a FIELD, Delta is invertible. For the actual b, set the selected coordinates to Delta^-1*adj(M)*b and all others to zero. This is an existence formula in B, not a cofactor computation; it proves Ax=b. If O is local at the chosen place, Delta is already a unit there, but local solvability is not needed. No REG, Nakayama, projective properness, point lifting or finite compatibility basis is used.

The accepted homogeneous map then gives g in I_hom. Consequently E_3=(B[X]/I_hom)[g^-1]=0; accepted cone/root-cover equivalence gives C_3=0 and the complete normalized r3 source empty at its original imported scope. The cone has all 25 slots: weights 8,9,10; 11..19; 11..22; 27, variables of weights 1,2,3,4, guard weight 30. Every source/mate/gauge/inverse/top-unit condition remains in the authenticated construction. No numerical-degree selection or all-F10/JC2 conclusion is added. Source quotient nilpotents cause no problem.

A nonzero Delta in this number field remains nonzero under EVERY characteristic-zero field embedding. Thus one suitable residue place suffices for all complex embeddings, without checking every factor of the reduced septic. This is not selection of a favorable characteristic-zero component: B has only one. No actual suitable place or nonzero minor is known here.

## 2. Integral-model and authentication obligations

A convenient sufficient model is O0=Z[1/N,theta] inside B=Q[theta]/(P), where P is the exact accepted monic septic, its rational coefficients and all dense matrix-entry denominators divide powers of N. Take a prime p not dividing N and an authenticated irreducible factor phi of Pbar over F_p; k=F_p[T]/(phi), theta maps to its distinguished class. Localizing O0 at this kernel gives the local domain O. A degree-one root is optional; a residue extension of degree at most seven is permitted. Verify phi divides Pbar and is irreducible, and verify p prime. A simple root or squarefree Pbar is a convenient extra condition, not logically necessary for this determinant argument; a genuine residue-field quotient is necessary.

This model must represent the ACTUAL P and source coefficients. A random rank-seven modulus or unrelated matrix is not sufficient. Full seven-coordinate B entries must be parsed and compared against the exact source identity before evaluation at the chosen place. Rational rescaling of P must be tracked exactly. Every inverse used in constructing an integral expression must have nonzero residue if that expression is reduced directly; a characteristic-zero unit can have positive valuation at the chosen place.

Conservatively requiring p-unit denominators in all rational coordinates of all 25 generators, g and P gives a straightforward complete baseline reduction. A denominator divisible by p blocks THAT reduction. Algebraic cancellation may give a different locally integral representation, but such a replacement needs an independently proved identity; no silent truncation, rescaling or expression rebuilding is licensed. Clearing exact nonzero denominators before reduction can also preserve generic rank, but the clearing factors and the resulting integral matrix must be authenticated, and success remains unproved.

Strictly, full-rank transfer only needs an integral selected minor, not integrality of b: b is solved over B afterwards. Indeed b need not be numerically reduced or materialized at all: accepted homogeneity already places g in the entire weight-30 codomain that the minor spans. Its exact source/guard identity remains a proof premise. Requiring the entire source baseline integral is a conservative future instrument contract, not an extra mathematical necessity. This distinction cannot excuse an unauthenticated target or omitted source row.

Hash equality binds bytes, not their correctness. The source reconstruction and coefficient-to-column correspondence need independent acceptance; a rank checker cannot discover all malformed reconstructions merely by multiplying the matrix it was handed. The charged unit-pivot gate's contrary suggestion and its reconstruction-runtime prediction are explicitly NOT adopted.

## 3. Smallest honest witness and controls

A prospective certificate supplies: the authenticated complete source/monic-P identity and pins; the integral-model map and checked denominator inverses; p and the finite field presentation; 297 distinct column IDs from the canonical 1453-column map; and exact evidence that the resulting 297-by-297 minor is invertible. Column IDs are literal pairs (source slot, multiplier monomial), with the accepted weights. The checker independently reconstructs each selected column over ALL 297 target monomials from the complete baseline, then reduces it by the authenticated residue map. It never accepts a producer-supplied matrix as the source oracle. All 25 source slots stay in the baseline; the positive minor need not use every generator.

For example an inverse Wbar is sufficient evidence: check Mbar*Wbar=I in all 88,209 positions, including prescribed zeros. This checks full rank without trusting a determinant label or elimination status. Exact finite-field elimination with independently checked factors is another possible representation; no new checker or arithmetic is authored here. Neither certificate needs to discover rational cofactors. A literal characteristic-zero cofactor certificate remains an alternative accepted evidence route.

Changed-base control: B'=Q x Q, A=[(1,0)], b=(1,1). Reduction at a place of the first factor has full row rank, yet Ax=b is impossible on the second factor. The conclusion for the WHOLE B' fails because the lifted determinant is a nonunit. Thus finite-etale rank seven ALONE would not license one-place sufficiency. The accepted actual r3 irreducibility is load-bearing.

Changed-rank control over Z_(p): A=(1,0)^T, b=(0,p)^T. Modulo p the system is solvable, but over Q its second row reads 0=p. Mere modular consistency therefore cannot replace full row rank. Conversely A=(1,0)^T, b=(1,0)^T is solvable over Q despite rank one rather than two. A=0,b=0 makes the same point at rank zero. Failure of the stronger rank test does not imply nonmembership, nonzero localization or a source point. Rank can also drop at a bad place even when the generic matrix is surjective.

Meaningful future witness mutation: if Mbar*Wbar=I, replace one entry of Wbar by that entry plus 1 in k. The product changes by a nonzero column of invertible Mbar in one column position, so the full identity check must reject. This is a hand-proved changed-certificate control, not a generated fixture or observed rejection. Unauthenticated sources cannot be rescued by a valid inverse.

## 4. Comparison and bounded recommendation

The accepted exact unit-pivot route searches for the PARTICULAR b over B and finishes with a full rational-polynomial identity. It can succeed even when A is not surjective. At actual r3, fieldness additionally makes every nonzero pivot a unit: ignoring caps, its exhaustive search/back-substitution has ordinary field elimination completeness, although its authorized failure status remains INCONCLUSIVE and no implementation is changed. The disconnected-base stall control remains valid for its more general contract, not an obstruction to this actual field.

The modular route replaces rational cofactor discovery by a stronger, target-independent positive witness. It potentially reduces arithmetic height and output burden; neither gain is measured. Source reconstruction may still be costly, but no dominant-cost claim is justified. All 297 rows must be covered; 1453 columns exceeding 297 is not evidence of independence. No structural impossibility of actual full row rank is derived from the charged inputs, and no generic-rank conjecture is adopted. Actual rank, a good integral place and feasibility remain UNKNOWN.

Recommendation: CONDITIONAL candidate acceleration, with the accepted exact unit-pivot instrument retained as a separately selected fallback, not an automatic retry. Cheapest discriminating quantity is one authenticated nonzero 297-minor of the actual weight-30 map at one genuine finite place. Before any computation, a focused manual FIRST of this implication/field attachment is the cheapest instrument; planning estimate 10 minutes. For a future separately registered modular attempt, a nominal 600-second wall budget is a proposed single-trial ceiling, NOT a prediction that reconstruction, rank discovery or checking fits it. Coefficient size and actual wall cost are unknown. Stop on bad denominators, invalid place, deficient rank or cap; no prime/r/exponent farm is authorized.

This is a KNOWN determinant-specialization mechanism attached to a NEW accepted complete-source client, not a new global theorem. It needs no REG or Nakayama, and it supplies no actual certificate today. The source implication is exactly the accepted r3 normalized-cone implication and no broader numerical degree or all-r claim. No canonical OPEN ID is raised; the quantity, cheapest test and numeric planning wall are above. Own-only collision review finds no new method novelty; no corpus search was permitted. All seven sources were fresh WHOLE, no provenance or other lane consumed. ZERO scientific subprocess, code, coefficient/fixture body, network or shared edit. Own WHOLE/postpins/marker and transactional custody complete this documentary task; no follow-on authority.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `10816`.
- Body SHA-256:
  `68535062f0e931bb252f4cf813bbb84008d2474d8b5ebae487b8698e1d5eb1cd`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
