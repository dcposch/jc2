# Mixed univariate data-certificate implementation

SOURCE ONLY / UNEXECUTED / UNREVIEWED. First action 2026-09-12 19:10:15 UTC; original reserve 19:27 and HARD 19:30 UTC. All six frozen input pins matched before scientific body reads; owned targets were absent. No actual coefficient artifact, native host, authority, calculation or result exists from this task.

## Scope

This packet replaces executable certificate ingestion with bounded canonical JSON data and an independently reconstructed exact identity over Q[t,X]. The proposed one computation is extended gcd over Q(t)[X], not a multivariate standard basis. Whole-algebra unitness is conditional on the accepted septic chart and on a future complete checked identity whose scalar does not vanish at any actual integer r>=2. No source-forcing attachment or global exclusion is supplied.

## Implemented result

Three source files: produce.py, check.py and metadata-only authority.py. The producer forms P0=(t-2)P and F=U^7B(t,X,V/U), uses one Q(t)[X] extended gcd, clears every parameter denominator by LCM and emits only exact coefficient data. It checks its own identity but labels the artifact CANDIDATE_UNCHECKED. The separate checker owns symbols/parser/reconstruction and never imports producer or interprets input as source. It checks the whole identity and factors N over Q[t] to exhaust all possible rational parameter roots, translating each to the sole possible integer r. A true exception is INCONCLUSIVE for this certificate, not a source point.

The checker uses finite falling-factorial/logarithm sums instead of the producer's recurrences, old D/Kold and E coefficients instead of the producer's compact septic construction, and rational substitution instead of termwise substitution. Literal runtime checks attach D=4U, Kold=-840V, Sold=470400P0 and degree7 leading9(t-2). Thus the new instantiation has an explicit independent check; it is not assumed correct because the old17zzd theorem was accepted. The old chart theorem itself, including automatic Y*d5 guard and whole-algebra scope, is imported without re-hardening. No new denominator localization or discarded factor/component.

Shared trusted arithmetic is Sympy, not independent implementations of rational algorithms. Official [polynomial API documentation](https://docs.sympy.org/latest/modules/polys/reference.html) specifies the selected Bezout orientation and irreducible-factor interface. No installed version or compatibility was observed. Domain construction details and runtime startup remain qualification obligations; the bounded documentation read and one failed internal link are recorded exactly in READ-SCOPE.

## Static controls and safeguards

Changing A1 to A1+1 forces residual P0 nonzero and must reach the full-identity failure. Multiplying A1,A2,N by7t-12 preserves the identity but forces actual r=2 into the scalar exception branch. PASS/exit text cannot become code: the input is a strict bounded canonical JSON object; coefficients/exponents are normalized strings, no numeric JSON or eval/sympify is used. These are manual negative traces and planned future controls, not generated artifacts or test passes. All control edits require actual changed hashes and whole-byte inverse restoration; any earlier refusal/resource failure does not establish semantic coverage.

Authority precedes CAS import/construction; disabled/null template is not executable authorization. The four source/interpreter pins, exact role/argv, resolved paths, current EC2/boot identity, UTC window and installed per-process limits are checked. One typed slot6 substitutes the authority digest, avoiding a self-hash cycle. Future checker authority pins the ACTUAL frozen candidate; no candidate hash is guessed. Fresh outputs use O_EXCL, fsync and byte reread; ROOT still owns exclusive parents and terminal custody.

The source supplies no wrapper, installer, mutator or enabled registration. ROOT must qualify the actual interpreter/library/native closure and environment, strict descendant/RSS/TERM/KILL cleanup, closed scopes, immutable source/input administration and whole-batch3600wall/3300CPU/32GiB/256MiB ceiling. Admission/end checks and per-process rlimits do not guarantee aggregate wall/CPU/RSS. A pinned qualification document is an external ROOT assertion, not a parsed proof of installed behavior. No timing, memory, degree of an emitted N, coefficient bitlength or library availability is measured.

## Completion limits, quantity and custody

STATIC IMPLEMENTATION COMPLETE at source-text scope; entirely UNEXECUTED/UNREVIEWED. No syntax/import/AST/test or stripped-check success claim. The certificate bound is16MiB,100000 terms per polynomial, t-degree16384, X-degrees20/6/0,8192-bit rational parts; these are deterministic refusal ceilings, not an established fit. Receipt32KiB ceilings and all status/exit/artifact cases are frozen in CONTRACT. No genuine scientific result exists here.

Remaining QUANTITY: does this one gcdex produce a conforming certificate whose N has no actual integer-r root? CHEAPEST TEST: one actual-code FIRST followed only by a separately ROOT-registered AWS formation/check and finite controls under the unchanged3600wall/3300CPU/32GiB/256MiB maximum; numeric cost is UNMEASURED, not a fit prediction. Missing runtime API/native/caps/cleanup qualification is an explicit external GAP, not license to run. No retry, field/prime/r farm, source exclusion, REG/all-F10/JC2 or new foundation/novelty claim.

COLLISIONS: owned box/report absent at first action; only the new leased report and this box were authored. Old failed experiment and every input remain immutable. Own WHOLE source/doc/report readback and input postpins precede the final marker. PINS/READ-SCOPE hold all six inputs, separate administrative instrument and selected official-doc URLs. Finalizer begin/close/finalize/expected-verify and terminal custody are required; the final custody excludes its own hash and records actual completion separately. No new canonical OPEN ID or follow-on authority.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6074`.
- Body SHA-256:
  `f223c3e013d07cd1bb2b7b3c0a97a78bf07b68d4384cc7458a8bb621a9f0344b`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
