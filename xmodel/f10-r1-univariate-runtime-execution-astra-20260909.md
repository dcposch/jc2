# F10 r1 univariate runtime: one finite engineering validation

Status: PRODUCER ENGINEERING PASS, NOT AN IDEAL DECISION. One exact registered adapter run, one genuine univariate checker.main and seven in-process semantic negatives completed. This report does not promote the runtime evidence; root collection and the first independent evidence gate remain required. No solver, point, properness, unitness, source-family exclusion or subsequent execution is authorized.

First action: 2026-09-09 20:57:25 UTC. Controlling task cap21:15:00 UTC, mathematical deadline21:12:00 UTC and independent worker stop21:16:00 UTC were never reset. The new report was absent. The existing box contained three root-issued files, all pinned before WHOLE reads and preserved unchanged. Basis0d39df3c9fd69c939a8420c54d03228b9077777d. Exact22 local inputs and remote interpreter pin/read scopes are in input-pins.json and READ-SCOPE.md.

## 1. Custody before payload, physical authority and terminal stop

Root REGISTRATION.md d32b3a05..., dispatch.registration.json db06dab1... and staging.json1844c108... controlled this run. The issued ROOT-ACCEPTANCE.json6451f6b4... was consumed read-only; its exact payload appeared in all ten per-operation authorities. No acceptance was minted here. Accepted17p/17t/17u scope is retained, including root's U/Pi qualification and the corrected distinction sys.argv5/6/7 versus FULL Python -I -B vectors8/9/10.

Before starting, the exact user timer was ACTIVE/waiting for21:16. AWS described only i-08d2a40f272ee9fa2: STOPPED, c7i.4xlarge, private172.30.0.72, Owner=f10-r1-exact-artifact-validation-20260909, retained vol-0574e0fa5aed1f5e3. Current Standard quota1920 and delayed CloudWatch usage212 at20:55 were recorded; they are not instantaneous capacity or price guarantees. No other instance or volume was inspected or altered.

The authorized start returned pending, then fresh AWS described RUNNING. Linux, Amazon EC2 vendor, exact DMI instance, hostname ip-172-30-0-72 and /usr/bin/python3 SHAa92f0f95... matched. The exact remote directory /home/ubuntu/f10-r1-univariate-runtime-validation-20260909 was absent, created exclusively, and all13 staged regular files plus root registration and interpreter were rehashed before launch. Neither plain nor descendant nor outer output existed. Code/backend/helper/probe/CAPRUN bytes were unchanged and staging copies were read-only. Only fleet run/push/pull were used; no install, allocation, alternate worker, repair or retry occurred.

The exact system unit began21:00:15 UTC, MainPID2045, invocation2f08d2fd65b54542a68661585cb8f9f5, uid/gidubuntu, RuntimeMax900s, TimeoutStop5s, MemoryMax3221225472 and KillModecontrol-group. Exact ExecStart was Python -I -B dispatch_batch.py in the registered cwd. The first live owned cgroup capture showed caller2045, CAPRUN2054 and probe2097 with their actual stat/cmdline relationships. initial-live-tree.txt is an explicitly labeled documentary summary, not a raw byte-identical transcript.

At21:00:49 the exact unit was inactive/dead, caller2045 and its owned cgroup absent. The initially observed child IDs2054/2097 were also absent before the terminal hash capture. No result payload had been read. All100 remote files were then SHA-captured, the entire fresh directory was pulled, and every byte passed sha256sum -c before any receipt/body read. The terminal manifest is remote-terminal.sha256, SHAa4e55d474a67bb535e4d8e9abf3c60c9785aa5c1ddbf37e3e9967be20e402414. There is no unclaimed missing artifact.

The exact worker was stopped promptly; fresh AWS positively confirmed STOPPED21:01:40 with unchanged Owner/EBS. Root independently confirmed STOPPED21:02:08 and cancelled its backup. Disk and all outputs remain recoverable; nothing was deleted or terminated. No further worker action occurred. The transient unit had already dropped its final timestamps/PID fields, so no final outer CPU, peak or exit timestamp is inferred from reset systemctl properties; per-child telemetry and batch receipts supply actual ending times.

## 2. Frozen representation and genuine receipt

The incoming normalized artifact e62734b04ddc7ad55a64f5350639332f533aa94c3526cd5b2f53378e4d96b750 and distinct prior normalized.full.receipt.json062cf0c0296f67154736de1cbf7c43378f649da49bbd89f141950f58272f3aab were bound by the root acceptance. Original source digest168bdfd3... and normalized generator/checker versions were retained by the unchanged evidence helper. The incoming normalized coefficient body was HASH ONLY locally, not replayed.

The new complete artifact is remote/univariate.json:

- SHA3cfec9aaec77c3f1c15f0192aee0fa3fc2b958ca4a41261cb079ec87a71d769a;
- genuine checker byte count72603;
- new full receipt SHA0c22af83d8846a6dbf77b424a482a0963559c9c51151539605f1c9a4f5ad0714;
- status PASS-NINE-ROW-ADAPTER-NOT-IDEAL-DECISION;
- checker authority d971a3510f468be232d3d491257d7ff59e13b060d8c340504b0bd70517636e0c;
- all nine row slots, guard factors r and h0, exact original-source input_evidence.

This was the actual checker.main child with normal rc0, not a fabricated receipt or in-process positive replacement. Its full registered argv length is9. Adapter.main length8 and semantic-controls.main length10 likewise agree with their telemetry and exact authority vectors. All ten authorities and all ten telemetries were loaded after custody for documentary full-argv/cap/hash comparisons, rather than only a selected subset. Their20 recorded hashes match the terminal manifest; all source/added-artifact pins and root acceptance payloads agree, preserving the five intended refusal defects. preflight.PASS records equal the first seven batch.PASS records.

The accepted full-normalized source checker remains a prerequisite, not re-proved by this adapter. The unchanged independent univariate checker reassembles compatibility coefficients and reconstructs all rows/guard by its accepted route. The coefficient algebra remains the entire B, no factor or field assumption. The cubic reconstruction is the accepted symbolic parameter-cover contract, not a degree112/196 expansion or a source-map degree assertion.

## 3. Actual finite preflight and timings

The five refusal children each returned1 under NORMAL_EXIT, with no post-authorize sentinel:

| Case | Actual exception text |
|---|---|
| disabled | no active registered authority |
| wrong host | not the registered AWS host |
| changed parent argv | full parent argv differs from registered capped runner |
| changed registered RSS cap | registration is not the exact required CAPRUN argv |
| omitted frontier pin | checker input artifact must be explicitly hash-registered |

The valid child returned0 and wrote POSTAUTHORIZE. The dummy also wrote its sentinel and exercised an actual orphaned same-group descendant: leader2134 exited0, child2136 had PPID1 and PGID2134. Aggregate sampled RSS77946880 exceeded33554432. CAPRUN returned125 with RESOURCE_CAP/rss, exact identity sequence MATCH/SENT15/MATCH/SENT9, matching leader start identity, integer signals, term_sent/kill_sent/cleanup_complete/leader_reaped true and no live group before reaping. The caller independently checked the same event sequence, namespace and final group quietness.

The dummy cap is intentional validation, not a failed mathematical job. All three mathematical children had rc0, NORMAL_EXIT, resource null and empty stderr:

| Actual child | CAPRUN UTC interval | Wall seconds | Sampled group peak bytes |
|---|---|---:|---:|
| adapt |21:00:16.635648–21:00:16.922659|0.28702134|21286912|
| full-check |21:00:16.993517–21:00:17.282262|0.288754405|22069248|
| semantic-controls |21:00:17.353819–21:00:18.143008|0.789200979|22609920|

These are this run's measured CAPRUN wall/group-RSS figures, not CPU, cost or speed forecasts. The stored mathematical hard cutoff was21:10:16.590184, the earlier of absolute21:12 and first_math+600. All ten records returned before their stored cutoff with 15-second admission margin and remaining_group_live=[]. The batch ended21:00:18.169100. No deadline, cap or source changed; no mathematical positive rerun occurred.

Registered caps remained adapt120wall/110CPU/2GiB, check120/110/2GiB, controls240/220/2GiB, probe5/3/2GiB and dummy5/3/32MiB, with inherited16MiB file limit. CPU is an inherited per-process limit, not aggregate CPU measurement. Sampled RSS and cleanup admit overshoot, not strict real-time guarantees. Outer lifecycle telemetry beyond the initial captured unit properties is unmeasured.

## 4. Seven actual changed-object controls

controls.receipt.json SHA c8f955d2255dc222af0e2e94813b202ef95d3a5adee70b3a3cfa1aee9a6256fa binds the genuine new receipt/artifact, exact harness cc04757e..., accepted code and root-source evidence. Every listed case is EXPECTED-REJECTION, exact ValueError, explicitly checker.verify IN-PROCESS rather than checker.main. Fresh changed fixtures were exclusively written/reread/hashbound by the frozen harness, and all seven fixture hashes independently match the terminal manifest. No fixture arithmetic or checker was rerun locally.

| Case | Exact reason | Fixture SHA prefix | Bytes |
|---|---|---|---:|
| Phi3-coefficient |all Phi3 coefficients|6fcaec0a19fd5387|72609|
| c-inverse |actual c inverse identity|0a7931739db8e7e7|72604|
| affine-r-constant |affine r constant|3940f45ced34f660|72611|
| indexed-row-coefficient |literal univariate row E1/S1|0184dfad2501d024|72619|
| missing-guard-factor |guard equation and both factors retained|381fcfc0d380319d|72598|
| guard-q-coefficient |entire guard q=r*h0|87d9691bc48073d2|72634|
| omitted-dense-slot |every univariate coefficient slot including zeros|01a7c4ca4d404ea7|71652|

The last fixture records actual removed dense slot3; it is not described as an assumed zero slot. Its wire was seen only as documentary strings in the receipt, not evaluated. No extra float/precision matrix, COVER mutation, late-bracket/pole negative or authority claim is added. All original normalized source evidence stays prerequisite; these controls test univariate semantics only.

## 5. Measured inventory, retained trust and limitations

In literal row order E1/S1,E1/S2,E1/S3,E0/S1,E0/S2,E0/S3,E0/S4,P4,P11, the genuine checker reports actual degrees

    3,3,2,4,3,3,2,2,5

and rational-basis term counts

    28,28,21,35,28,28,21,21,42.

Guard degree is5; both factors are retained. These are receipt measurements, not a separate local recount. No zero/duplicate/drop slot was removed by the representation. Maximum coefficient bitlength was NOT emitted by the frozen checker and remains UNMEASURED; no unregistered statistics script was run. No leading algebra factorization, row gcd, ideal decision, root count, nonemptiness or solve permission follows.

Read scope is exact: current-pinned root registration/staging, accepted code/report/gates and issued acceptance; fleet documentation and script; admitted source admissibility/frontier. Frozen code WHOLE reads were reused after current identical hashes. After all100 output hashes were secured, batch/preflight/full/control receipts, dummy stdout, all10 authority/telemetry metadata, refusal stderr and positive stdout/sentinels were read. Incoming normalized and new univariate coefficient artifacts themselves remain hash-only locally. Accepted helper/CAPRUN/code correctness and observed genuine execution are retained trust, not re-hardened foundations.

The normalized evidence gate's selected-authority and Pi-order qualifications are retained: it had selected input review, not an all-authority audit, and Pi rejection precedes bracket/poles. Its 8+12 authority/telemetry hash count is20, not10+12. This new run's all-ten metadata reconciliation is separately recorded. It also records one local documentary correction: an initial JSON comparison incorrectly treated object key order as significant; key-sorted metadata comparison resolved it. That was not a remote mismatch, code patch, coefficient calculation or retry. No source bytes were changed.

## 6. Completion and pending scope

This producer's runtime result is complete within the fixed task cap, subject to root's custody-first collection and independent runtime-evidence review. Cheapest remaining check is that single evidence review of the frozen receipt/telemetry/authority/fixture hash chain against accepted17p/17t/17u, not another mathematical run. No new canonical OPEN identifier is raised. Quantities still unmeasured are maximum coefficient bitlength, outer CPU/peak and any future solver behavior; this task does not authorize measuring them.

Own WHOLE report read, raised-OPEN quantity/cheapest-test check and own-only collision review precede the completion marker. The report target was absent; the three root-frozen box files are unchanged. All owned/input hashes, expected transaction, exact worker-stop evidence and100 remote hashes are banked in terminal custody excluding itself. No publication to external parties, shared ledger edit, subagent, follow-on or worker duty remains.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `13043`.
- Body SHA-256:
  `67dbd705edb35c8399b39a09c17103cde6da867d4a9b9bfdae833c65e04ada6e`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
