# Ideation round 20260903T1200Z — coordinator synthesis (2026-09-03T12:34Z; Opus 5 cloud seat)

Packet: `ideation-20260903T1200Z-packet.md` (sha256 c90fa687…, basis 4250a6e4). Five blind
submissions, all sealed: opus5-coordinator (own, committed before any read), gpt55
(39KB), grok46 (41KB), opus5 (45KB), sol56 (40KB), fable5 (43KB). Integration deltas
banked during the round: 17(u) (Opus second gate: gap-free vs gapped whole-tree
screens, OPEN[CENTRE-SUPPORT]), 17(v) (screened census; K = 16 ray PROVISIONAL),
17(w) (K = 16 ray PROMOTED via GPT-5.5 gate). Ledger HEAD at synthesis: 8a1b2df4.

## 1. The round's verdict on the packet

The packet asked whether the all-degree program is "boundary → N; tree → multiplicity
structure; coefficients → the rest", whether the screened census can be a proof (Q1),
what the coefficient level is in uniform form (Q2), how to attack theorem (T) (Q3), and
whether a mechanism outside the three layers exists (Q4). The five submissions agree on
the stack and disagree with the packet on the program, in the same direction:

1. **Q1 is settled NO** (delta 17(w), PROMOTED, gap-free). The K = 16 ray n = 48t+16,
   m = 32t+16, M = (n−12, n−2), V = (3,3) survives PATH-ARITH + whole-tree + ODE + nested
   packing for every t ≥ 1 (Grok producer; Grok's blind rediscovery; GPT-5.5 hostile gate
   CONFIRMED all six items; Opus predicted the mechanism — the congruence A₂ | P — before
   the family was found). The screen is a target-list generator. Both named fixed-N rays
   died to A₂ ∤ P with constant residue; A₂ | P is attainable cofinally. Fable adds the
   negative control: homotheties of Moh's six are not screened families.
2. **The proof must leave the skeleton.** Q4 answered by Grok/GPT-5.5/Opus: there is no
   fourth invariant of the original pair; descent is a coordinate change that collapses
   the tree to a few integers and the coefficients to Lemma 2.1 + Appendix II. Opus's
   composite screen after descent is vacuous (delta 17(u)/(v) already banked). Fable names
   the two skeleton-external mechanisms with tests — the pencil (g ↦ g − c invariance;
   OPEN[PENCIL-COMPLETION]) and the unramified fibre map (Gelfand–Leray form equals df
   on {g = c}; Riemann–Hurwitz 2g_c − 2 + N + r_prop = Σ_np(e_P − 1)) — typed as
   necessary conditions, not proof routes.
3. **Q2/Q3: theorem (T) is the program, but its universal form is (T≤4), not the
   four-integer height-2 form.** Sol's material correction: the first D = 108 clients
   (108,72; M = (24,78,106)/(48,90,106); V = (1,1,5)|(1,4,5); u_s = 1) descend to
   (18,12; γ³) with M' = (4,13)/(8,13), s' = 3; the V'-map is SOURCE-INFERRED and Φ was
   proved on s' = 2 rows only — delta 17(t)'s "any descended pair" is a scope
   over-promotion (OPEN[DESCENT-STATE-S3]; Sol's systems upgrade: producer/reviewed/
   promoted scope fields). Opus independently: the Φ anchor n' − M'_{s'} − 1 vanishes on
   two thirds of the frontier (OPEN[DESCENT-ANCHOR]). NEITHER bites on the K = 16 ray
   (s = 3 → s' = 2; anchor = 2). Fable: the k-twisted p.189 argument yields only
   deg_γ P(γ,0) ≤ k — a shape condition, not a kill — which is why Appendix II needs
   coefficients; iterate descent while u' = 1 (Moh's (16,12) → (4,3)); terminal objects
   are height-1 monomial-Jacobian pairs = GGV regular corner (APPROACHES row 1).
4. **Q1 residue, positive (Fable, PROVED-HERE/UNREVIEWED, gate running):**
   OPEN[FULL-TREE-RECENTER] closes positive — the only integral intermediate radius is
   δ_j = 0, a nonzero label there is removable by y ↦ y − c (Moh p.190), Prop 5.6's proof
   is chart-covariant; MEASURED 38/52 excess rows = exactly the POLY kills; residue at
   n ≤ 100 = 14 rows, all s ≥ 4, u_s = 1; at s = 3 the screen is EXACT (Moh's six). If the
   Opus gate confirms, the POLY column becomes operative (D ≤ 200: 1,420/686/459) and
   OPEN[MOH-PROGRAM-ARTIFACT] shrinks to 14 rows.
5. **Literature closure (web-verified this round, EXTERNAL):** Neumann–Norbury 1998
   (arXiv math/9805093) state that a polynomial map C² → C with irreducible rational
   fibres cannot be a component of a counterexample, "proved several times" (Kaliman
   1993 Proc. AMS 117; Lê–Weber 1995 C. R. 320) and in fact a consequence of
   Miyanishi–Sugie 1980; Nguyen Van Chau 2008 (arXiv 0804.3172): a constant-Jacobian
   map is invertible if P and Q are rational polynomials. Hence OPEN[MF-RATIONAL]
   closes NEGATIVE for counterexamples: g_c ≥ 1 for every component's generic fibre;
   with Fable §5.2, Σ_np(e_P − 1) ≥ N + r_prop. (AUDIT delta 17(x) below.)

## 2. The program after this round (replaces the packet's three-layer sentence)

Boundary pins N = O(1) [PROMOTED]. Whole-major-tree + ODE + nested packing is a
necessary, uniform-in-D, fail-closed target generator on integer skeletons — exact at
s = 3 for n ≤ 100, cofinally nonempty [PROMOTED]. The proof is a coefficient theorem
in the descended coordinate: for u_s = 1, Prop 6.3/6.4 (finite reduction, not
induction) to a monomial-Jacobian pair with the ENTIRE inherited tree (height ≤ 4);
for u_s > 1, OPEN[MINOR-DICHOTOMY] (p.209). Uniform coefficient object (Sol Card 1):
the fibre product of exact A.3 node schemes (discriminant-saturated weighted
Vandermonde) over edge jet maps, plus global polynomiality; the tree-decorated moment
engine is a rewrite of it and is deferred. The canonical unbounded test family is the
K = 16 ray → (12t+4, 8t+4; M₂' = 12t+1, V₂' = 3, J = cγ): t = 1 is Moh's Appendix II
kill; t = 2 = (28,20) is the first honest instance above Moh's table. A t-uniform
obstruction on the ray is a proof of JC2 along the only known cofinal screened family
(not JC2). Counterexample side: a pair in the (28,20) shape, or a SURVIVES from the
compiler on the D = 108 (18,12; γ³) descendants (REPRESENTATIVE until lifted through the
one-way descent edge to an actual (108,72) Keller point).

## 3. Lanes launched from this round (all live at synthesis)

| lane | model | what | from |
|---|---|---|---|
| k16-ray-gate-gpt55 | GPT-5.5 | gate on the ray — SEALED, CONFIRMED (17(w)) | coordinator |
| k16-ray-T-newton-sol56-v3 | Sol | (T) on the ray: Card III Newton/Lemma 2.1 uniform in t, then Appendix II at t = 2 (28,20) | Grok Cards I/III |
| d108-1812-ramification-sol56 | Sol | ramification absorption on the two (18,12; γ³) D = 108 descendants; recover the s' = 3 state | Sol §3/Card 2 |
| recenter-gate-opus5 | Opus | page-image gate of Fable §2.1 (POLY column) | Fable single first lane |
| sibling-coefficients-sol56, t-ladder-sol56, prop55k-opus5, centre-support-grok46, descent-anchor-audit-grok46, rigid-congruence-grok46, appendix2-compiler-grok46 | — | pre-round lanes, running; the compiler is to be fed (28,20) and the two (18,12; γ³) systems at seal | — |

Dispositions adopted: box01/fleet restart HOLD (Sol, Fable, GPT-5.5 concur: no typed
residual ideal exceeds one core yet); tree-decorated moment engine DEFER (all five);
p.202 residue through the sibling system SECOND BATCH; D ≤ 400 emptiness search STOP
(the space does not empty); G2/G3 as (T) clients STOP (calibration only).

## 4. OPENs raised this round (bounded; cheapest test)

OPEN[T-K16] (theorem (T) on the ray; lane running). OPEN[DESCENT-STATE-S3] (V'-map and Φ
at s' = 3; one D = 108 row; lane d108-1812 recovers it). OPEN[DESCENT-ANCHOR] (Opus;
lane running). OPEN[DESCENT-WINDOW-TOP] (engine WINDOW-FAIL is the new-top window of
(6), does not descend; 17 rows at D = 108; not a kill). OPEN[D108-RESIDUE] (two
(18,12; γ³) + one u_s = 2 row). OPEN[TWISTED-64] (Prop 6.4 for J = cγ^k; one page;
test on (16,12) → (4,3)). OPEN[PENCIL-COMPLETION] (Fable §5.1; test on a composition
control then Moh's (64,48)). OPEN[MINOR-DICHOTOMY] carried (first client (108,72;
M = (81,106); V = (7,7))). Closed this round: OPEN[K16-RAY] (PROMOTED), OPEN[MF-RATIONAL]
(EXTERNAL, negative for counterexamples), Q3(f)-after-screen (NO).

## 5. Systems upgrades adopted

(a) Sol: promotion atoms carry producer_scope / reviewed_scope / promoted_scope; the
delta 17(t) "any descended pair" clause is re-typed to s' = 2 pending the s' = 3 gate.
(b) Coordinator: lane prompts instruct mechanical hash verification from the run
receipt (two Sol lanes failed the gate by hand-transcribing digests). (c) Round
trigger: the next full round fires at 20260904T0000Z or on a critical trigger (a
SATURATED-EMPTY uniform in t on the ray, a SURVIVES on (28,20) or (18,12; γ³), or a
refutation by the recentring gate).

<!-- BODY-END -->
