# Gate T: reviewed `D(rho)` / D1 `a=2` contact repair

Date: 2026-08-27

Status: **REVIEW REPAIRS R1--R4 APPLIED ADDITIVELY; THE NARROW CONTACT
THEOREM IS REVIEW-CONFIRMED AND ELIGIBLE FOR NARROW PROMOTION.**

Immutable producer and review:

```text
db0ecebf37cf8283c27e36ed8f0ba10ae9bcf1887d1854affe73cf61c8f367cd
  xmodel/max12-812-order2-gate-t-drho-d1-a2-composition-sol-20260827.md
518d4b250fa797d64d748017488f5f9daa3900b900f2e91000ca7124d08a220f
  xmodel/max12-812-order2-gate-t-drho-d1-a2-composition-hostile-review-fable5-20260827.md
```

The producer is not silently rewritten.  This note supplies the review's
smallest repairs and controls the surviving statement.

## Repairs

**R1 — custody.**  The additive replay pins the V22R1 wrapper, all seven
exact-Q `Tg15` row bytes, the frozen 569 tails, and
`compile_cge3_universal.py`, in addition to pinning and rerunning the
original replay's twelve-file custody/transcription checks.  This discharges
the Kummer erratum's explicit wrapper/row-byte directive.

**R2 — evidentiary wording.**  The desk replay pins frozen inputs and
re-checks transcriptions of the support window, allocation, residue formula,
and quotient-versus-cover countermodel.  It does not independently derive
the rows or terminal residue.  Row-level verification is carried by the
frozen dual-AWS `D1AC`/finite-band certifications and Fable5's independent
exact reconstruction.  The original §8 sentence must be read only in this
repaired sense.

**R3 — complete finite map.**  In (4.2), in addition to the displayed
entries, the complete through-grade-16 map explicitly sends

```text
k1, k2c, k10_i (i>=3), k6_i (i>=1), k2_i (i>=1) -> 0.
```

Fable5's independent support computation proves these jets do not occur in
the contact rows through grade 16; `k2c` is the third `k10` jet and is not a
`k2`-load jet.

**R4 — notation.**  Write the coefficient field as `K` and the leading
unit-`k10` coefficient as `k10lead`.  The registered open is
`D(rho*k10lead)`; there is no collision with the base field.

**Strengthening.**  On the contact quotient all seven total rows vanish at
every grade `0..14`.  Thus grades 15 and 16 are the complete row content
through the endpoint ceiling, not merely a selected window.

## Reviewed theorem

Let `K` be a characteristic-zero field and `V` a normalized DVR.  In the
registered post-gate Kummer total source `p0=-2*rho^2`, after the reviewed
generic-square first-normal, half-weight, and exact reduced `M=0` gates,
there is no total-source arc on `D(rho*k10lead)` with

```text
ord(A)=2, ord(C)=3, ord(R)>=2.
```

The finite-jet contact map uses shifted pairs

```text
A0±=aaa0±rho*aaa1,
C0±=(ec3±rho*ez3)/2,
R0±=rs2/4±rho*cs2,
```

not the annihilated stage-zero pairs.  The exact total rows vanish through
grade 14, factor through the reviewed D1 endpoint at grades 15--16, and on
both allocations the terminal numerator has residue
`(3/2)*rho^2*cv^2`.  The localizer identity is
`D(p*k10lead)=D(rho*k10lead)` after `p=-2*rho^2`.

This is arcwise/set-theoretic one-contact exclusion only.  It is not a
cover of `D(rho)`, a ramified-fibre statement, a generic-square fan cover,
an equality-face or positive-order-load theorem, a Rees/receiver theorem,
either `G2` obligation, Gate T, order two, maximum twelve, JC2, or a
counterexample.

The additive replay must emit
`PASS-GATE-T-DRHO-D1-A2-REVIEW-REPAIR-V2`.  Its role remains custody and
transcription; the independent Fable5 reconstruction is the mathematical
hostile review.

