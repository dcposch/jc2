# Direct rows-only wire: smaller serialization, identical source obligation

ROOT STATIC PROPOSAL / UNREVIEWED / NO IMPLEMENTATION. First02:35:00UTC,
original publication reserve02:39/HARD02:42 today, never reset.
Basis0d39df3c9fd69c939a8420c54d03228b9077777d. Own report/manifest/box absent.
Accepted23z supplies the necessary-row theorem,19z the rank implication.
The new detailed operation/assertion contract remains under its FIRST:
this note assumes its eventual accepted version; it does NOT promote it.

## Decision and exact invariant

Propose replacing only the prospective FULL-TRACE serialized object with a
ROWS-ONLY serialized object. Keep the exact fixed leading program, denominator
tests/inverse input/product checks, all ten formal bands/full forcing, fixed
projections/gauges, separate Euler mate,108/45/63 inventories,22 low-band
differences, BOTH inverse boundaries and scale/auxiliary expressions. All
mandatory hard arithmetic/type/source-expression checks remain. All stipulated
diagnostic expressions are computed internally and classified exactly as before;
their nonzero residues still do not mean generic identities fail or source points
exist. They are not serialized, and no producer-supplied diagnostic is an oracle.

The independent checker reconstructs this complete program itself, using its
own arithmetic and alternate formulas as prescribed. It compares the displayed
graph values with its own results. It never reads or imports the producer's
scientific arithmetic, forcing, matrix, bands or evaluator. Sharing a reviewed
metadata interlock alone is permitted; such code must not contain polynomial
or finite-field mathematics. ROOT retains source/native/caller/custody gates.

This does NOT propose omitting computation nodes by dead-code analysis,
discarding source equations, shrinking their fixed supports, adding diagnostic
generators, selecting a component, changing a completion, or passing original
assertions by a residue-only zero test. A later proposal for any such change
would be separate. All25 alternative rows, including zero/duplicate slots,
and the exact guard remain. No rational-baseline qualification is fabricated.

## Projection lemma — known verification logic, new client interface

For one ROOT-specified genuine place let Eval_C denote a correct implementation
of the eventually reviewed source-attached finite circuit C. Let projection P
retain just the graph tuple defined below. A producer emits P(Eval_C); an
independent checker computes Eval_C independently and compares every displayed
component with P of its own result. On acceptance, the displayed graph is
exactly the independently reconstructed reduction of the source-attached
alternative graph. There is no need to infer full-trace equality from P:
P is NOT asserted injective, and no omitted candidate trace is authenticated.

Accepted23z uses precisely those alternative rows, their weights and the
generic guard identification in the canonical LOCALIZED source algebra.
Every F'_j maps to zero there and g' maps to its unit g. Authenticated full
row rank of their weight30 map at this genuine place transfers to B by19z/23z;
all297 monomials then lie in their degree30 span, in particular g'. Thus the
canonical localized source algebra is zero. This is the existing conditional
proof, not a new exclusion. No intermediate trace value needs to occur in the
certificate for this implication: correctness of its reconstructed contribution
is supplied by the reviewed independent evaluator, not by an absent assertion.

The lemma has TWO indispensable premises: correctness/source attachment of C,
and faithful independent implementation of C. This note proves neither. Hashes,
successful canonical parsing, a matching header or an inverse for a
candidate-owned unrelated matrix do not discharge them. A false full circuit
does not become sound under projection. Pending operation FIRST remains binding.

## Literal prospective wire

Use new schema `f10-r3-direct-rows/v1` and job_tag
`f10-source-cone-r3-direct-rows-v1`. Exact top-level keys:
schema,job_tag,contract_sha256,source_pins,place,graph.
contract_sha256 binds ROOT's separate immutable manifest of the actually
accepted operation/assertion version, this rows-wire version and their reviews;
it is not a self-hash. source_pins must equal ROOT's fixed expected vector of
the actual independent scientific files and literal source references exactly.
These future actual values are not generated by this note or supplied by the
untrusted candidate as authority.

place has exactly p,degree,phi,P7_mod_p. p/degree and all scalar coordinates
are canonical decimal strings. 2<=p<2^31, degree f in1..7. Phi has f+1 ground
coordinates, leading1; P7_mod_p has exactly8, leading1. Both match independent
literal reconstruction/ROOT prescription; exact primality, phi irreducibility
and divisibility, all fixed rational pivots and all scalar inverse nonvanishing
conditions are checked. No prime/factor search or actual place here.

graph has exactly Hq,c,c_inverse,zeta,slots,ell,g,U. c/c_inverse are dense arrays
of exactly f ground coordinates in0..p-1. Each other graph value is a typed
polynomial `{"kind":"graph","terms":[...]}`; slots is exactly25 such polynomials.
Each term is [exponents,coefficient], exponents exactly4 nonnegative canonical
decimal strings for X1,X2,X3,X4, coefficient a dense length-f nonzero field
element. Strict ascending numerical lexicographic exponent order, no duplicate
terms; zero polynomial is the empty terms array. No mathematical JSON numbers,
floats, nonfinite constants, signed residues, leading zeros, -0, implicit
reduction, sparse coefficient arrays or unspecified keys. Canonical ASCII JSON,
sorted object keys, no insignificant spaces, exactly one final newline;
parser demands exact serialization round-trip.

Slots in literal order: K8,K9,K10; A1_i for i1..9; A0_i for i1..12; T.
Weights8,9,10;19..11;22..11;27 respectively; variable weights1,2,3,4.
Hq and zeta weight7, U weight4, ell weight23, g weight30. Zero/duplicate slots
remain. Independently check Psi7=Hq+c*z, actual c inverse, zeta=-Hq/c, all25
literal source-derived graph rows, ell, g=Hq*ell and zeta*ell=-c_inverse*g.
No claim that g has nonzero reduction is needed for the full-rank theorem.
There are30 polynomial positions and two field scalars in graph, not the
hundreds of upstream trace/diagnostic positions in the earlier wire.

Keep the conservative20000terms/poly,400000terms aggregate,100000terms/internal
poly,128MiB encoded-object limits; absolute exponent256 is additionally narrowed
by the literal graph weights. Internal typed Laurent/source supports and their
original limits remain, not just graph limits. Every cap failure STOP; no
truncation. This is NOT a new wall/CPU/memory/runtime authorization.

## Statuses, control and explicit non-compatibility

Prospective producer stdout:
FORMED_UNCHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME
Prospective positive checker stdout:
CHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME
Each is one newline-terminated line. They are specifications, not observations.
A fresh correctly rebound T+X1^27 mutation, unchanged elsewhere and with
canonical inverse byte-restoration, must reach exactly
`CHECK FAILED: direct-source graph slot T\n`, normal exit2, empty stdout.
The checker must compare the T polynomial at that named predicate, not merely
compare the whole serialized document at an earlier generic mismatch.
Parser/hash/source/place/resource refusal is not semantic rejection.

No rank certificate/schema/code is included in this source-wire proposal.
A later source-qualified rank client must bind this NEW rows schema to the
authentic checker receipt and exact input hash; reconstruct all297 coefficient
rows and1453 permitted labels; require297 distinct actual source-column IDs;
verify all88209 entries of the selected matrix times its inverse. The prescribed
inverse[0][0] constant-coordinate+1 control remains prospective for that later
client. Current canonical rational rank22z and its parked runtime are NOT
compatible and cannot be repurposed by changing a status string.

Countercontrols: dropping a diagnostic value from serialization cannot license
accepting a candidate's invented graph: the independently reconstructed T
still catches the stated nonzero mutation. Conversely two different full
internal traces could project to the same graph; acceptance makes no claim
about equality of those traces. A valid inverse of an unrelated identity matrix
still proves nothing about these generators. These are logical controls only;
no matrix/fixture or arithmetic computation was generated.

## Implementation split and priority

If both the operation and rows-wire FIRSTs pass at usable scope, use parallel
bounded independent producer/arithmetic and checker/arithmetic authors on the
same frozen contracts, with a small agreed metadata-only authority API. No
cross-reading of live source or shared mathematical helpers. Combine their
completed, frozen candidates in ONE different-model static code gate before
any runtime port. Exact source/operator obligations do not change with this split.
No lane is launched or given authority here; future original clocks must be
fixed at invitation, never backdated from this proposal.

Expected benefit is lower serialization/parser/schema coupling and a smaller
certificate, not less polynomial reconstruction or a measured runtime gain.
Computing all stipulated diagnostics internally may still dominate costs.
The fixed128MiB/term ceilings can still refuse. If a concrete coupling/gap
makes the split less reliable, retain the full-trace contract instead.
No larger cap, solver retry, prime farm or new worker is requested.

OPEN quantity: does this exact wire projection preserve source/rank soundness
and the reachable T semantic predicate under independently reconstructed
complete C? Cheapest test: one focused different-model manual FIRST of this
projection/wire against23z and the pending operation contract,10–12minute
UNMEASURED planning wall; no re-proof of field/cone foundations or implementation.
This is a KNOWN verification/projection principle applied to this new client,
not mathematical novelty or a new canonical OPEN identifier. Bounded checksum:
accepted23z section5 already asks independent25-row reconstruction, and19z
asks source-bound columns; neither requires serializing every upstream value.
The pending full-trace wire is an instrument choice, not a new source theorem.

## Read scope and custody

Exactly six scientific/design inputs were pinned at02:35:00: accepted direct
theorem e6df0a6458c682128c841a054e4cfb0be00cb2efde0325743f197cdcb0564725;
its FIRST1aca7ad822cf88a95b30b2cb43c4a07227a78ca389fa28679e8fc1947e530ab0;
rank theorem11d1b3a34edfde4d736a946d5543576e79f37fb6a45c737fffa07b3c360e037b;
pending OPERATIONS3be532afc6e784842694cf02d4399573228362abb47ae36130ef97c4274faa59,
ASSERTIONS61b9ce5e1f9b40a765dcb2a2ea5aa3a928197b61bc41cb5cf945632b99ad4de2,
WIREb9060f0bbc14e92464079b0daca12faa1ba2d367f9ecc7307ba301689de5b77d.
WHOLE same-ROOT reuse for the direct theorem (02:33:21) and the three documents
(02:20–02:21), current pins reconfirmed; FIRST and rank theorem were freshly
WHOLE-read02:35:25. No unresolved clipping, no claim of six new FRESH_WHOLE reads.
Old report headers do not replace canonical accepted23z/19z scopes; the three
new documents remain design inputs, not accepted mathematical premises.
Their live FIRST output is NOT read, charged or modified.

All authored bytes apply_patch, leased private partial only until finalize.
Own WHOLE/current postpins/OPEN and own-target collision before markerLAST,
then close/finalize/expected VERIFY. No scientific subprocess/import/AST/syntax/
compile/test/CAS/dummy, coefficient payload, actual place/matrix/fixture, network,
AWS, protected/shared source edits or delegation. Documentary publication
only. No source outcome or JC2 result; no follow-on authority.

Own WHOLE and all six current postpins completed02:38:09UTC, before original
02:39 reserve. Exact owned-target collision/OPEN/cheapest-test review complete.
Publication only follows; no further mathematical work or output after marker.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `12225`.
- Body SHA-256:
  `9d5dd7ef35bd0a41a81b78feb0287a708214965ca2fb909eeea018ad74df508a`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
