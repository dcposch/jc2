# Exact thirteen-file administrative port — Sol

- First action: `2026-09-12T08:03:39.307279366Z`.
- Basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
- State: source-static, disabled/unbound, unexecuted.

## Status

**COMPLETE SOURCE-STATIC PORT; DISABLED/UNBOUND.** Exactly thirteen derivatives were produced by the six ordered literal global substitutions in `TASK.md`, with no other byte change. This is not a worker, installation, binding, or runtime result.

## Exact transformation result

For every derivative, a fresh six-stage transformation stream from the pinned source had the same full SHA-256 as the published file. `PORT-MAP.json` records source/output hashes and byte counts, every per-file substitution count, and every remaining `JC2_*_PLACEHOLDER` name with multiplicity. Aggregate substitution counts in task order are `16, 0, 0, 1, 1, 15`.

Substitutions 2 and 3 had zero occurrences across all thirteen sources and therefore introduced no bytes. Eight derivatives were byte-identical to their sources: native metadata, setup, preholder security, observer, release check, receiver, FIFO writer, and card; their equality is explicit in the map. The other five have exact changed hashes recorded there. The only byte-length change is outer admission (`6021` to `6246`) because fifteen batch placeholders expand by fifteen bytes each; all other output lengths equal their inputs.

All selected legacy batch/date/clock literals are absent after the authorized substitutions. No additional old literal was silently rewritten. The card's two accepted FIRST hashes remain historical provenance, while source paths containing old box names occur only in `PINS.json`/`PORT-MAP.json` provenance. All still-needed physical, native, host, namespace, manifest, authority, holder, receipt, and release values remain placeholders with unchanged names/multiplicities. No EARLY unknown or approved LATE token was guessed or promoted.

## Census and controls

The owned output census is exactly the thirteen unchanged basenames plus `PINS.json`, `READ-SCOPE.md`, `PORT-MAP.json`, `custody.json`, and this report's expected artifact manifest. Target names were absent before creation except the ROOT-supplied `TASK.md`, which remains unchanged.

All seventeen inputs were pinned before whole reads and re-hashed unchanged after authoring. Whole reads covered `TASK.md`, the three metadata inputs, and all thirteen source files without following embedded paths. Owned derivatives and metadata received whole readback. Administrative controls were textual only: ordered substitutions, hashes, byte/line counts, placeholder occurrence census, and an old-reference scan. No source or scientific code was executed; no syntax, AST, import, compile, mock, fixture, test, process control, network, SSH, AWS, or protected-tree access occurred.

Disposition: **exact administrative port complete as inert bytes; disabled/unbound and subject to ROOT's independent terminal intake.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `2997`.
- Body SHA-256:
  `4a480ad34a1201816568ed76d642018a1940ac62ece5dc9d35f31ada86b9d9f7`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
