# Opus5 hostile review — row-34 Artin ALG-PRIM bridge

You are Opus 5 acting as an independent hostile mathematical reviewer in
`/Users/dc/code/math/jc2`.

The charged producer file is immutable at:

```text
525c408e92b7f2cb11f5152e002e60a20a331d268283f1be30fb13b5a104b990
  xmodel/ggv-quarter-root-r34-algprim-artin-bridge-r0-sol-ultra-20260827.md
```

Read it completely and fail closed if the hash differs.  Read only its exact
named dependencies when needed.  Do not reread mutable canonical ledgers,
inspect or traverse `jc2-lean`, touch AWS, run heavy local computation, or
read unrelated ideation submissions.  Small independent exact desk scripts
are allowed.

Hostile charge, atom by atom:

1. verify that the zero-tail specialization is a section of the stated
   provisional row-34 class scheme over
   `R=Q[u,v]/(uv,u^4,v^2)` and respects all frozen source windows;
2. independently expand the implicit `Q=P^2` equation and verify formal
   Hensel uniqueness for the claimed whole series
   `Q=epsilon*A+u*A'*s^2+(v/4)*s^6` on both branch signs;
3. rederive `alpha_F`, the displayed polynomial primitive, and every power,
   coefficient, and sign;
4. decide whether the whole coefficientwise class tower really vanishes on
   this section, rather than only through row 34;
5. audit the field-vs-Artin, rational-vs-algebraic, whole-series-vs-
   truncation, class-vs-raw, and primitive-vs-polynomial-G firewalls;
6. decide the exact operational consequence: whether later class/ALG-PRIM
   work should stop on this section and raw determinant/G-window
   compatibility is indeed the next honest interface.

The parent row-34 descendant remains separately under Fable5 review.  State
your conclusions conditionally on its displayed quotient unless you directly
rederive the needed parent atoms; do not silently promote the parent.

Classify every atom `CONFIRMED`, `REPAIRED`, `GAP`, or `REFUTED`; include
source hashes, independent arithmetic/replay, pinned model/CLI identity, and
the narrowest promotion recommendation.

Write exactly one output and nowhere else:

- `xmodel/ggv-quarter-root-r34-algprim-artin-bridge-crossreview-opus5-20260828.md`
