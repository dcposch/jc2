You are an independent hostile algebraic-geometry referee.  Review only the
frozen D1 target-free primary-C2 producer below.  Do not edit any producer,
prompt, ledger, audit, promotion, or existing artifact.  Write exactly one
report:

`xmodel/max12-812-order2-square-d1-c2-targetfree-c3c7-hostile-review-grok-20260826.md`

End with exactly `CONFIRMED` or `REJECTED`.

Producer directory:

`cases/max12_812_order2_square_owner_d1_c2_targetfree_c3c7_20260826/`

Primary custody:

```text
RESULT.md SHA256
ed61c925e91f96af4caa62dc03c9c3c4c730a36196e0e5199db8ce877ab2dbd4

PRODUCER_FREEZE.sha256 SHA256
b9ca9fa21a94b167e451490eef8a1c7fdf7c01c3e989c606cc367a8ac8197087

EVIDENCE.sha256 SHA256
0e00de55745e4f004847cd50f8a951551b3b62f98d4285a1c4b0e3ee3aebf682

source FREEZE.sha256 SHA256
331eabdc06e6e1507a1b25a5bef0920f5a4fe908491988fee82bca08065dd4c7

source archive SHA256 on all three hosts
74f830bfce1546d415244a7bb646c6a288e6c63114200aba1c9db3a0d2b19bce
```

Claimed scope is exactly

```text
D(p*k0), ord(C)=c, 3<=c<=7,
ord(A)>=c, ord(R)>=c-1.
```

The five decisive ceilings are `T=10+2c=16,18,20,22,24`.  Audit
adversarially:

1. Rehash every source/producer/evidence row and all three nested compiled
   manifests.  Check host/tag/rc/validator/zero-swap custody.  Exact `Q` is
   the theorem endpoint; `F_65519,F_65521` are controls.  Singular 4.3.2
   quotient-ring assignments are not canonically normalized for raw
   `==0`, `subst`, or `diff`; confirm literally that every generated program
   uses an ordinary polynomial ring and no conclusion uses unsafe `qring`
   semantics.
2. Independently enumerate all four frozen binomial summands at the five
   boundary contacts `(A,C,R)=(c,c,c-1)` through their respective ceilings,
   with a genuinely complete/padded cutoff.  Decide whether the complete
   first-grade sources are exactly: `AC,C2,R3,RC` for `c=3`;
   `AC,C2,RC` for `c=4,5,6`; and `AC,C2,RC,k6*C` for `c=7`, with the claimed
   coefficients and unique maximal pole `C2`.
3. Inspect all seven literal rows and independently assembled analytic rows
   at each threshold.  Verify every coefficientwise bridge, recurrence, and
   source-exact sentinel rather than trusting PASS strings.  Look for an
   omitted normal or connection jet, delayed load, target, or lower-grade
   family.  Confirm `k2` is absent and all targets start at grade 28.
4. Prove or refute the closed-tail widening.  Raising `ord(A)` or `ord(R)`
   must not reveal a formerly excluded family, lower a first grade, alter the
   pole ceiling, change target cancellation, or require inverting an `A` or
   `R` coefficient.  The exact-contact boundary checks alone are not enough;
   audit the exponent/cost argument uniformly over the two inequalities.
5. For every threshold reconstruct the canonical numerator modulo `L^2`,
   retain/check any discarded ordinary quotient, and verify the pole-two
   recurrence and both root Faber signs.  On `p=-2*lambda^2`, check that the
   only root terminals are `(3/8)C(+lambda)^2` and
   `(3/8)C(-lambda)^2`.  Audit the root-evaluation determinant and the two
   coefficient charts `D(c1),D(c0)`: do they cover exact nonzero linear `C`,
   and do both localized ideals contain 1 before radicals without hidden
   inversions?  Verify the omit-`C2` control.
6. Treat `c=8` as a required excluded negative control.  Independently check
   that `k6*C/L` first appears at grade 25 and its grade-26 moving-connection
   correction contributes a nonzero pole-two term proportional to
   `-ell1*k6*C/L^2`, so the pure-square functional cannot be extrapolated.
7. Keep every `c>=8` cell, other primary/tied faces, positive-order leading
   load, `p=0`, `k0=0`, zero section, global/terminal chart, fan
   exhaustiveness, order two, maximum twelve, and JC2 out of the verdict.

State the earliest missing primitive or smallest failing identity if any.
Producer PASS strings and the support miner are not proof.
