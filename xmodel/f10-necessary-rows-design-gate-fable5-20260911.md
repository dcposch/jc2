# f10 necessary-rows design gate (Fable 5.1) — FIRST

lane_start_utc=2026-09-11 05:39:29
owned_targets_absent_at_start=true (both ls ENOENT at 05:39:29)
clocks: ORIGINAL reserve 2026-09-11 05:52 UTC / HARD 05:55 UTC, never reset
status: COMPLETE; verdicts A CONFIRMED, B CONFIRMED (banked 23z/19z scopes), C CONFIRMED with one named smallest omission (authority module pin/rebinding; rename sweep must spare the T literal), D READY for bounded authoring plus DELTA FIRST, no execution approved

## Input custody
ALL8 hashed before any body read; see box/f10-necessary-rows-design-gate-fable5-20260911/input_custody.md

## A. Dependency and side effects
**CONFIRMED: dead arithmetic branch in BOTH frozen bodies.** In produce.py (pin 8a7b...) the branch is lines 265-277: the two `inverse`-kind variables, `chartS`, the `inverses`/`poles` lists, the loop over (Ahat,-3,30),(Bhat,-5,51) and the 'inverse chart bound' raise at 275. Its only later reader is line 332, and only through the two conjuncts `len(inverses)!=2 or len(poles)!=2`; the third conjunct `len(scale_record)!=13` reads SCALE, not the branch. I read every line 278-352 for the names bound in 265-277 (Pin, qin, chartS, inverses, poles, transformed, obj, low, high, ex, cc, e, term): none is read again at build scope. ex/cc/e reappear only inside the nested function `scaled` (282-287) as its own locals; low1/low0 (303) are different names. In check.py (pin 5052...) the branch is 390-404; its names iq, P, iz, chartS, inverse_maps, poles, source, minimum, maximum, inverse_poly, ex, v, key, term are never read after 404 (405-504 checked line by line: `key` is rebound only as a loop target at 498, `P` is never P1/P0, ex/v/key inside `to_scale` 410-416 are its own locals). The two `eq(len(...),2)` at 403-404 are tautologies: the loop appends exactly twice or raises.

Side effects: every callee of the branch returns a fresh object. arithmetic.py: `add` 197-205, `mul` 216-226, `shift` 260-264 build a new `Poly r`; `power` 233-240 rebinds only its local `a`; `retype` 182-183 and `Poly.__init__` 154-157 copy the dict; `bounded` 167-179 only reads and raises; `var`/`const` call `field`, which reads P/PHI/DEGREE and writes nothing. The only module writers are `initialize` 99-129, `fi` 132-141 (UNITS), `denominator` 144-151 (DENOMINATORS) and `wire_p` 342-350 (WIRE_TERMS); none is called in 265-277. check_arithmetic.py: `Poly.__init__` 146-151 copies then runs read-only `bounded` 159-170; `cast` 183-185 copies; `add` 188-198, `mul` 205-215, `power` 221-225, `move` 245-249 are fresh; the only global writer is `initialize` 99-117. Ahat/Bhat/U/zr and A/B/U are iterated by `.items()` and never assigned into. The branch calls no `record`/`diagnostic`, `fi`/`unit`, `denominator`, matrix inverse or wire function, so the 150/111/39 partition (produce 330, check 471-477), the 15 UNITS (334-336 / 487-493), the 125 denominators (337-338 / 111-112) and the wire counters are the same computations with or without it.

Control dependencies: the branch can only raise ('inverse chart bound' 275; STOP_INTERNAL_TERM_CAP / 'typed exponent cap' inside bounded/mul; check.py 'inverse Laurent envelope' 400 and internal caps, all Stop -> STOP_NONDECISION exit 2). Deleting it removes those stops and the 2+2 length conjuncts, so the acceptance domain widens exactly by inputs that formerly died there; no retained value or predicate changes, because every later value is a function of upstream objects the branch never wrote. This is a syntactic dataflow fact over the whole input domain, not an inference from ROOT's one residue run. Retained and distinct: SCALE `A_inverse` = origA minus the Laurent-S `reconstruction` (produce 297-300, check 422-436); the 15 scalar `fi`/`unit` inverses; the six two-sided matrix products (105 / 179-197); the leading a/b/guard equalities at 278-280 / 405-407, which sit after the branch but read only Ahat, Bhat, W, t5.

## B. Exact source-exclusion composition
**CONFIRMED at the banked scopes of 23z/19z; no removed numerical predicate is a premise.** The accepted chain (wire FIRST section A, line 18) is: (i) 23z: the 25 rows of the canonical source-attached circuit C lie in ker(B[X]->A), A=(B[X]/I)[g^-1], with g' congruent to the unit g; (ii) a faithful implementation at a genuine place computes the reductions of those rows; (iii) 19z: a nonzero 297-minor of the weight-30 map lifts to the degree-7 field B, so g' is in the row ideal and A=0. Canonical inverse-polynomiality is a hypothesis on C inside (i), a SOURCE hypothesis that defines A; neither frozen body observes, tests or certifies it. What the bodies computed was the ALTERNATIVE Laurent-q image of the alternative Ahat/Bhat (S -> Pq^3 - zq^2 + Uq, theta -> q^-1), whose poles were kept in lists and never compared (ASSERTIONS row 236-245: "No zero test gates acceptance"; ROOT pins 55-58). So the alternative coefficients are a premise of none of (i)-(iii). By A, the new instrument computes syntactically the same Hq, c, zeta, 25 rows, ell, g, U from the same upstream expressions, so (ii) is preserved verbatim and (iii) consumes only those. The composition survives with the branch uncomputed.

The only numerical predicates removed are 'inverse chart bound' (produce 275) / 'inverse Laurent envelope' (check 400), i.e. -3<=n<=30 for A and -5<=n<=51 for B, plus the branch-internal caps. That bound is a consequence of the raw envelope already enforced at produce 186-188, 241-245 and check 323-324, 351-352: a term S^i theta^j z^r with i+3j+7r<=D, j<=J maps to q-exponents n=i+2a+b-j in [-J,3D]=[-3,30] / [-5,51], so the check cannot fail once the earlier envelope checks pass; it was a redundant tripwire, not an independent premise, and 23z/19z never cite it. What is surrendered is service, not soundness: no claim that the alternative inverse is polynomial, has vanishing poles, is ordinary or has any support may be drawn from the new instrument; those quantities are NOT_COMPUTED, and the old bodies never asserted them either. No zero, nilpotence or ordinaryness statement about the unevaluated expression is made here. One naming caution: wire FIRST line 24 speaks of the full inverse_A/inverse_B polynomials as a hard equality; in both frozen bodies the only hard equality of that shape is SCALE `A_inverse` (Laurent-S), which stays, and the Laurent-q maps have no equality consumer. The ROOT pins (lines 55-58) already supersede that wording; the new contract must not describe the deleted branch as a removed equality.

## C. Minimum new-instrument boundary
**CONFIRMED sufficient, with one smallest omission named.** Exact deletion set: produce.py 265-277 whole, and at 332 only the two conjuncts `len(inverses)!=2 or len(poles)!=2` (keep `len(scale_record)!=13` and its raise); check.py 390-404 whole (comment, iq/P/iz, chartS, both lists, loop, 'inverse Laurent envelope' need, two length eq). Nothing else moves: 278-280 / 405-407 leading a, b and guarded product stay; 282-316 / 410-455 SCALE with all 13 fields and six comparison groups stay; 318-352 / 479-503 graph, 25 slots, mixed T (324 / 483), ell, g, U stay; the septic/place (18-25 / 56-71), 125 denominators, 15 units, forcing, bands, matrix products, installation, Euler gauges, 108/45/63 positions, 22 readbacks, 150 diagnostics with 111/39, Laurent-S auxiliaries (200-205 / 316-320) all stay. arithmetic.py and check_arithmetic.py need NO edit; the 'inverse' entries in their kind tables (arithmetic 170, check_arithmetic 12/161) become inert and must be left as is (no refactor). The wire is untouched: produce 347-352 already emits exactly 6 top keys and 8 graph keys, 30 polynomials plus 2 scalars, and never referenced the branch, so the metadata-only API is unchanged by construction.

Literal migration, exactly four sites: produce 347 schema and job_tag, produce 378 FORMED line; check 42-43 schema/job_tag, check 504 CHECKED line. The T rejection must stay byte-identical: check.py 502 builds 'direct-source graph slot '+label, and main 518-522 prints 'CHECK FAILED: '+str(exc) to stderr with exit 2 and empty stdout. A naive rename sweep of 'direct' would silently break the prescribed literal; the authoring instruction must exclude check.py 496-502 from any rename. The mkstemp prefix '.direct-rows-' at produce 365 is not a contract literal; whichever choice is made must be declared, not inherited. NOT_COMPUTED for the alternative Laurent-q inverse/pole coefficients belongs in the new contract document and status vocabulary, never as a new JSON key, dummy length-2 list or evaluated-map record.

Smallest omission: the requirement list does not name the `authority` module (imported at produce 2, check 2 and called at 356, 511, 516, 520), which is outside these eight inputs and unreviewed here. If it binds the job tag, artifact path pattern or the old source vector, the new job fails or, worse, launders an old registration. The new contract must list authority's hash inside source_pins and state its rebinding to f10-source-cone-r3-necessary-rows-v1. Provenance note: ASSERTIONS.md cites produce/check SHAs 76ad6c85/b9176ee7 and ROOT pins line 68-69 pin checker b9176ee7, while this packet freezes 8a7b.../5052...; my line numbers refer to the packet pins, and the new source hashes must be issued against whatever ROOT freezes, not the ASSERTIONS-era SHAs.

## D. Readiness and cheapest next discriminator
**READY for bounded independent authoring plus a focused source/runtime DELTA FIRST; no execution approved.** The dependency lemma holds (A) and the exclusion composition survives (B), so the deletion is a legitimate narrower instrument, not a mathematical shortcut. The DELTA FIRST is cheap and exact: a line diff of each new body against the packet pins must show only the deletion set and the four literal sites of C, the 'direct-source graph slot T' literal intact, authority rebound and pinned, and no arithmetic-module change. Any other hunk is a refusal. No inherited runtime binding, old receipt or unseen patch is approved; no launch, rank client or rank/source outcome follows.

Runtime is UNMEASURED and no speedup is claimed. ROOT's attributed NORMAL_EXIT1 died inside the deleted branch (produce 274 -> add 204 -> bounded 169 after 218.021 s), so SCALE (two scale-kind Jacobians, 45 low differences), the 150-diagnostic partition and the graph substitution (power(zeta,e[4]) per z-degree, Horner in the checker) were never reached by that run; whether they fit the 100000 internal, 20000/400000 wire and 128 MiB caps is unknown. The redesign's 3234/14490 transient bounds apply to the block design only, not to SCALE or graph objects. If the eventual single-place run stops on cap, bad place, STOP_BAD_COLUMN_PLACE or anomaly, that is NONDECISION, not evidence about A versus B or FINAL support. The dependency does not fail, so no obstruction is stated and no return to full streaming is forced; the block design remains an unreviewed fallback.

## Remaining quantity / cheapest test / UNMEASURED planning wall
QUANTITY (no new canonical OPEN ID): whether the necessary-row producer and the independent checker, at one ROOT-registered genuine place under new contract/source hashes, both complete within the existing caps with identical 30+2 graph positions, and whether the freshly rebound T+X1^27 fixture yields exactly `CHECK FAILED: direct-source graph slot T`, exit 2, empty stdout. Unknown; nothing computed. CHEAPEST TEST, in order: (1) two bounded authors apply the deletion set and four literals; (2) one different-model DELTA FIRST on the diff plus authority/contract/source pins; (3) only then one registered place evaluation with the T control. PLANNING WALL: this FIRST's 12-15 minute figure and any authoring figure are UNMEASURED planning proposals, not runtime forecasts; no operation count or speed is claimed.

## Own-only collision statement
COLLISIONS: EMPTY. This lane wrote exactly xmodel/f10-necessary-rows-design-gate-fable5-20260911.md and box/f10-necessary-rows-design-gate-fable5-20260911/input_custody.md, both absent at 05:39:29 UTC; apply_patch only, each write under 1500 words; no Seal, charge_basis, transaction, other output, source code, Write/Edit or redirection into owned files; no subprocess science, import, AST, compile, test, CAS, fixture, prime search, network, AWS, SSH, process control, git, peer, live, protected or provenance access. Tools: date, ls, sha256sum, wc, cat, sed, apply_patch. Postpins and own WHOLE readback follow below, before the marker.

## Closeout
All eight postpins recomputed 05:45:18 UTC equal the charged list and the pre-pins (custody file). Own WHOLE readback of both owned files completed 05:45:18 UTC, untruncated, marker count 0 at that time. REFUTED: none. GAP: none mathematical; the single interface omission is stated in C. No new canonical OPEN identifier, no charge_basis line (no exit-price assertion is made), no clock extension. This closeout and the marker are the final bounded apply_patch write, before the ORIGINAL 05:52 reserve; all writers are idle after it.

<!-- BODY-END -->
