# r2 highest forms: proper specialization and a smaller finite-eliminant bridge

2026-09-10. MANUAL / UNREVIEWED. First action14:30:37.282597869UTC; fixed stop14:50:00UTC, publication reserve14:47:00UTC, never reset. Own targets absent before lease. Basis0d39df3c9fd69c939a8420c54d03228b9077777d is provenance only. No source code or coefficient artifact is authored here; no scientific execution, baseline body read, prime test or coefficient reduction occurred.

## 1. Outcome and exact accepted perimeter

CONDITIONAL GO for a small modular alternative; NOT an actual highest-form certificate. The useful additional bridge is: a modular cF+dG=1 plus a unit ACTUAL leading coefficient of F or G makes A[u]/(F,G) finite and then zero over the local coefficient ring A. This proves the characteristic-zero affine chart empty and can safely be combined with the accepted characteristic-zero infinity theorem. It needs no modular infinity proof. Without that finite-model condition, mixing characteristic-zero infinity with modular affine emptiness is invalid.

The charged cyclic-core gate explicitly imports irreducibility of P7 over Q and calls B=Q[Z]/P7 a FIELD; the cyclic producer likewise starts with the accepted degree-seven field. This report consumes that accepted import, does not prove irreducibility, and does not infer it merely from degree seven or generic behavior. The charged infinity gate supplies the characteristic-zero X1=0 endpoint over this B. None of its displayed numerical residues are used as new modular data. The highest-instrument gate accepts the denominator-free chart identities and their conditional transport. Its unresolved actual witness is not presumed to exist.

The old reconstruction producer was freshly read whole to identify all normalization, unit-selection and recurrence obligations, not to run or revalidate it. The actual packet SHA dac655a7ce75287b06010171bce66af1a644aff81de9620a452457a5f65a9587,1652675B is only a future specification. No assertion is made about any prime, root, denominator, leading coefficient or certificate for those unread bytes. Baseline lifecycle beyond the charged reports is not imported.

## 2. Sufficient proper integral-model theorem

Let A be a local domain, fraction field B, residue field k, and let H5,H6,H7 belong to A[X1,X2,X3], homogeneous of positive weights5,6,7 for variable weights1,2,3. Let

    Z=Proj A[X1,X2,X3]/(H5,H6,H7).

Then Z is proper over Spec A. One elementary construction of the ambient proper space uses the finite graded extension A[X1^6,X2^3,X3^2] subset A[X1,X2,X3]; the former is a polynomial ring with all three generators of degree6, and the latter is finite integral over it. The induced weighted-projective morphism to ordinary P2_A is finite. Z is closed in that proper weighted projective space. No flatness or preservation of coefficient leaders is required for this statement.

If the geometric special fibre Z over an algebraic closure of k is empty, then the special fibre is empty by faithful field extension. If Z_B were nonempty, the closed image of the proper morphism Z -> Spec A would contain the generic point and therefore its closure, including the closed point. This contradicts special-fibre emptiness. In fact a nonempty closed subset of the spectrum of a local ring contains its closed point, so Z itself is empty. This retains scheme-theoretic nilpotents: emptiness is not a smoothness or reducedness assumption. For a number-field place a DVR A is a convenient sufficient choice.

Over B, empty weighted projective zero locus means the only geometric affine common zero of the positive homogeneous forms is the origin. The accepted cyclic-core theorem can then consume this parameter-sequence condition, conditionally yielding its dimension35/seven-per-class consequences. This report does not construct its basis, multiplication maps, full-source membership certificate or source point/unit, and proves no all-r or JC2 statement.

Unsafe contrast: A[u]/(pi*u-1), for a DVR uniformizer pi, has empty special fibre but nonempty generic fibre. Its projective closure pi*X-Y=0 in P1_A has generic point [1:pi] and special point [1:0]. On the chart Y!=0 the special fibre is empty; the generic infinity Y=0 is also empty; nevertheless the generic chart is nonempty. Thus generic infinity exclusion plus special affine emptiness cannot simply be combined. The missing special point escaped to infinity.

## 3. One finite place suffices, with the FIELD premise explicit

With the accepted field B, choose ONE finite place and an integral local model A inside B having fraction field B. A sufficient checkable construction is: P7 monic with coefficients in Z_(p), p a proved rational prime, and a residue root beta in F_p with P7(beta)=0 and P7'(beta)!=0. The localization of Z_(p)[Z]/P7 at (p,Z-beta) is then an etale local order, hence a DVR with fraction field B and residue F_p. A residue field extension and a verified irreducible factor can replace a linear root, but it is not necessary when a linear root is available. Repeated-root/bad-order cases require additional integral-model proof and are not silently covered by this convenient certificate.

There is NO homomorphism from the entire characteristic-zero field B to F_p. Reduction is a map A -> k. A selected place is not a selected characteristic-zero component when B is a field. Proper emptiness over its fraction field is emptiness over B, which remains empty under EVERY complex embedding B -> C. Thus a full seven-dimensional mod-p product computation is not mathematically necessary. Exact coefficient provenance still uses the complete seven-coordinate characteristic-zero representation.

If B were instead a finite product, the conclusion would fail for the untested factors. For a hand control take B=Q x Q and e=(1,0). The three forms e(X1^5+X2X3), e(X2^3+X3^2), eX1^7 have weights5,6,7. Their first component has only the origin, while all forms vanish on the second component. Testing a finite place of only the first component cannot prove the whole B-system empty. Degree-seven presentation alone cannot rule out this defect. The present field premise is an accepted import, not a new arithmetic proof.

## 4. Integrality: wire reduction and localized formulas are different contracts

Direct dense-wire reduction must account for every rational coordinate of every ORIGINAL coefficient used to define the model, not merely nonzero residues surviving projection. For this highest-form task that means all terms of the three complete Psi rows, exact support/weights before z=0, and all seven rational coordinates per coefficient, as well as the entire monic P7. Unrelated lower K rows are not mathematical premises of this model, although the baseline hash binds the whole packet. If a verifier claims to reduce/rebuild the whole packet instead, it must check all its coefficients and all divisions as well.

If each rational denominator in the chosen dense presentation is prime to p, evaluation Z -> beta is immediate and each denominator inverse must be checked. A denominator divisible by p blocks that NAIVE rule, even when the resulting element of B is locally integral. For example in a field Q(theta) with theta^2=1+p, at an odd-prime place theta=1 mod p, the element (theta-1)/p equals 1/(theta+1). Its dense rational presentation has a p-denominator while the second expression is integral at the place. This is an illustrative changed field, not actual r2 data; assume the displayed quadratic defines a field.

A localized-expression route must prove exact equality in B of each replacement with the frozen original coefficient and exhibit its denominator as a unit of A. Merely clearing denominators by a nonunit may change the special fibre and cannot manufacture a favorable reduction. A valid homogeneous model over A with the same generic fibre is enough for properness, but its literal special fibre must actually be certified.

For the finite-eliminant Route F below, the logically minimal integral model is only F,G in A[u]; the H coefficients themselves can remain in B if the exact generic ideal identities and the integral F/G representations are independently proved. Reducing every original H coefficient directly is a simpler sufficient implementation contract, not a necessary mathematical condition for that variant. Claiming cancellation of p-denominators in F/G without this exact equality audit is not an alternative proof.

Rebuilding from the charged reconstruction formulas requires all selected inversions to remain valid in A: rational pivots/binomial divisions, dL, W, t5, H7, each early/middle matrix inverse, and the actual chosen late completion denominators, including intermediate expressions used in whole mixed forcing. A nonzero B-element need not be a unit at the selected place. Changing an inversion branch is allowed only with an exact proof that the reconstructed highest forms agree with the original B-forms; a separately convenient mod-p recurrence is not that proof. No claim that prime89 or any other prime passes these checks is made here.

## 5. Two sound modular certificates; one is smaller

### Route P: the whole special projective fibre

After an integral model is authenticated, re-extract the exact special-fibre forms. On X1!=0 use weighted scaling to X1=1 and put u=X2,v=X3. The accepted support identities give

    H5=P+Qv,
    H6=B3+B1v+B0v^2,
    H7=C3+C2v+C1v^2,
    F=Q^2 B3-PQ B1+B0 P^2,
    G=Q^2 C3-PQ C2+C1 P^2,       deg_u F,G<=5.

These identities and their cofactor expressions are polynomial identities over A, so reduce without any division by Q. A checked cF+dG=1 over k[u] excludes the whole special affine chart, including Q=0. To apply section2 one must ALSO exclude X1=0 in THIS same special fibre. Its weight-forced forms are alpha X2X3, beta X2^3+gamma X3^2, delta X2^2X3. Nonzero alpha,beta,gamma in k suffice over every algebraic extension: the first equation gives X2X3=0, the second kills the remaining coordinate. These three residues must be independently obtained from the same original integral model. The accepted characteristic-zero nonvanishing cannot be transported backwards to supply them. Delta is not needed for this sufficient boundary certificate.

### Route F: a finite projected eliminant quotient, no modular infinity proof

Here is the new smaller sufficient lemma. Let F,G be the actual elements of A[u] defined above. Assume either is a constant unit, or ONE has positive ACTUAL degree d and a unit leading coefficient in A. If

    cbar*Fbar+dbar*Gbar=1 in k[u],

then (F,G)=A[u]. Proof: after dividing the chosen polynomial by its unit leader, A[u]/(F,G) is a finite A-module, generated by at most 1,u,...,u^(d-1). Its reduction modulo the maximal ideal is zero by the displayed identity. Nakayama gives the zero module. This is an exact ideal conclusion, not a field-point or torsion-free assertion; the quotient may have arbitrary nilpotents before the argument. A constant unit is immediate.

The already-accepted denominator-free identities

    F=Q^2 H6-H5[Q B1+B0(Qv-P)],
    G=Q^2 H7-H5[Q C2+C1(Qv-P)]

then give 1 in the generic chart ideal in B[u,v]. NOW it is legitimate to combine generic-chart emptiness with the accepted generic X1=0 theorem. There is no mixing of fibres in this final step. The proper object used is the finite eliminant quotient, not a presumed proper original affine chart. No integrality of v or unit B0/C1 is needed.

An economical sufficient test is that the residue of [u^5]F OR [u^5]G is nonzero: the degree<=5 bound is exact, hence that is the actual leading term. In particular [u^5]F=([u]Q)^2*[u^3]B3. Zero reduced fifth coefficients do not invalidate the theorem, but a lower reduced degree d requires an exact characteristic-zero proof that all higher coefficients vanish, NOT just vanish modulo p. The unsafe pi*u-1 example has leading coefficient nonunit and demonstrates the missing hypothesis. If no leading-unit condition or full projective boundary proof is available, the modular identity alone is NO-GO for generic emptiness.

## 6. Smallest honest certificate and independent verification

One explicit finite-place certificate must bind: the original packet hash/bytes and reconstruction theorem tier; the complete literal P7 and accepted field import; p with a verifiable primality certificate (or independently bounded exact primality check); beta/root and derivative checks, or fully specified residue extension; the actual local order; and all coefficient-integrality evidence used. It then supplies the reductions of the WHOLE required highest coefficients and finite-field cofactors cbar,dbar. The independent checker must re-extract/reduce the original complete rows, reject forbidden S/vartheta or wrong weights before substitution, reconstruct P,Q,F,G independently and verify cbar Fbar+dbar Gbar=1, not trust a gcd label or producer tables.

Route P additionally supplies and verifies the same-fibre alpha,beta,gamma nonzero checks, or another complete projective infinity certificate. Route F instead supplies the exact leader/degree proof and its nonzero residue. Both preserve Q=0, zero rows, constants and degree drops. No one may replace all these checks by a point sample, evaluation at a few u-values, a bare resultant value without degree/model justification, or seven-dimensional generic irreducibility rhetoric.

Justified shape bounds, NOT measured payload sizes: after z=0 the weight5/6/7 forms have at most5/7/8 monomial slots, respectively, hence20 B-coefficient slots. Before z=0 the full three Psi rows have at most24 such slots; every B coefficient has seven rational coordinates, so up to168 original rational slots are involved. No assertion that these slots are nonzero is made. Cofactors of degree<=5 use at most12 residue-field elements and verification checks11 u-coefficients. Over a linear-root place these are scalar F_p values, versus up to84 rational coordinates for the exact instrument's two degree-five B-cofactors and its77 fixed identity positions. With residue extension degree f, scalar storage/checking expands accordingly. Model, denominator and input-provenance checks do not disappear, and large original rational strings may dominate them. There is no measured speedup, height bound or timing forecast.

The exact instrument remains useful and accepted; the modular path merely avoids displaying a rational B[u] Bezout witness if its local finite/proper prerequisites pass. A nonempty modular projective fibre, unsupported place, nonunit selected denominator, failed finite-field cofactor or lost leader is INCONCLUSIVE for the actual characteristic-zero regularity question. Even an empty affine chart can fail this particular eliminant certificate: P=1,Q=0,H6=H7=0 gives F=G=0 while H5=1 already excludes the chart. Certificate failure is not a source point.

## 7. Decision, read scope and stopping point

CONDITIONAL GO: after the ordinary FIRST review/registration steps, prefer the finite-eliminant variant if a fully authenticated integral place supports a modular cofactor and a unit actual leader. Otherwise use a complete special-projective certificate or retain the exact accepted instrument. No execution, new checker implementation, runtime registration, prime search or abandonment is authorized by this report.

One cheapest discriminating quantity is the pair of actual reduced fifth coefficients (f5,g5), at a place whose integrality audit passes, together with whether the exact reduced F/G admit the bounded modular identity. A nonzero member resolves the degree/finite-model obstacle immediately; if both vanish, the exact higher-degree cancellation or same-fibre infinity obligation remains. All are UNKNOWN here, with unknown evaluation cost. This is not another source existence/exclusion or global regularity claim.

All nine charged hashes matched before reads. Seven explicitly reused same-byte prior WHOLE reads: highest CONTRACT/produce/check, original arithmetic, infinity gate, cyclic producer/gate. Fresh WHOLE: current highest gate and original reconstruction producer. The producer was intentionally read in ranges1-260 and261-285; there was no clipped read and no omitted tail. The old producer's accepted normalization code, not its unexecuted output, was consumed. No other source or provenance was followed. The existing documentary begin invocation first rejected a missing --owner; corrected begin acquired the sole lease, with no scientific execution or artifact overwrite.

## OPENS RAISED

None new. The original highest regularity question is retained. Exact unresolved local-model/leader/certificate quantities are stated above; no new canonical OPEN or follow-on authority.

## COLLISIONS

EMPTY. Owned targets absent at first action; report and expected transaction absence rechecked before publication. All edits are apply_patch, own WHOLE review precedes completion marker. No prior sealed file or shared input changed.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `17013`.
- Body SHA-256:
  `4c2c764bf474d541dc26b9412bdb2a9a9b5cf7e1724d063a104e28a38053223a`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
