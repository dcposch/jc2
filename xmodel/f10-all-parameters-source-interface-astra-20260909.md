# All-parameter F10: an ordinary triple-root receiver interface

September9,2026; owner /root/nonemptiness_certificate. Basis `0d39df3c9fd69c939a8420c54d03228b9077777d`. **PROVED HERE at the explicit source/import scope; PROVISIONAL pending different-model review.** This is a necessary source map, not a receiver exclusion, existence claim or general degree classification. All algebra is manual and factored; ZERO mathematical subprocesses of any size.

## 1. Exact source and conclusion

Let k have characteristic zero, q be an integer with q>=0, and retain the printed F10 orientation

`m=5q+7, n=3q+4`.

Thus m>n>1 and `3m-5n=1`, so m,n are coprime. Suppose ordinary `P0,Q0 in k[u,v]` have nonzero constant bracket c0 and satisfy BOTH:

- Their supports are contained in `[0,7e] x [0,21e]`, with `(7e,21e)` attained, for e=m,n respectively.
- They are an ordinary standard(m,n) pair with ACTUAL starting chain `A0=(7,21), A0'=(1,0)`, direction `(7,-2)`, typeII.b, selected child `A1=(13/7,3)`, as in GGHV1708 Theorem2.20 and Section5 F10, k=1. The child and selected multiplicity are source data, not merely an admissible numerical label.

Then, over a finite algebraic extension of k, there are ordinary receivers `A,B in k'[g,p]` with

`ell_(2,1)(A)=H^m, ell_(2,1)(B)=H^n, H=p(p^2-g)^3, [A,B]_(g,p)=c*g, c!=0`.

Their full supports lie in e times

`Delta={I,J>=0: 2I+J<=7, I-J<=2}`,

and their opposite exposed faces are

`ell_(1,-1)(A)=(-1)^m g^(2m)(gp+1)^m`,
`ell_(1,-1)(B)=(-1)^n g^(2n)(gp+1)^n`.

The nonzero vertices e(2,0),e(3,1),e(0,7) are attained. Their actual ordinary degrees are7m,7n, their g-degrees3m,3n, and the unique total leaders are p^(7m),p^(7n). Output constants can make the origin attained without changing these faces or the bracket; no origin guard is needed for the map.

The injective polynomial substitution `g=a^2,p=b` therefore gives ordinary degrees7m,7n with total tops

`K(a,b)^m,K(a,b)^n`, where `K=b(b^2-a^2)^3`,

and bracket `2c*a^3`. Swapping the OUTPUTS, if a later theorem needs increasing degrees, swaps m,n and negates c. No swap is silently made here. K has one simple and two triple lines; no simple/double local exclusion is applied.

## 2. Primary/history scope and the triple starting face

The printed Section5 row is literally F10, `(7,21),(1,0),(13/7,3),k=1,5q+7,3q+4`. Definition3.3 and formula(3.20) parameterize by N0, so q=0 is included. F11 has the same corners but k=2 and another exponent family; it is not this assignment. The narrow current history search found the printed row and the0730 warning about F10(0)'s swapped4:7 ratio. It did not find an already supplied F10 map in the named searched interfaces. This is not an exhaustive novelty search. The old F9 map and accepted16a/b provide a proof pattern and imported opposite-edge scope, not an F10 conclusion.

At `(7,-2)`, the actual normalized endpoints give weights7m,7n. Their expected bracket weight is `7(m+n)-5>0`, so the leading bracket vanishes. GGV1401 Proposition2.1, including its polynomial clause, gives an ordinary common root R with

`ell(P0)=alpha R^m, ell(Q0)=beta R^n`,
`R=u r(z), z=u^2 v^7`,

where r is monic cubic and r(0)!=0; alpha,beta are nonzero. This follows from the integer solutions of weight7 and the two attained endpoints. It is not a fractional child used as a polynomial source.

In Theorem2.20(8), the child second coordinate is the selected multiplicity divided by m. Thus the selected root has multiplicity3m. In its fractional face variable `t=u^(2/7)v`, the face is `alpha u^m r(t^7)^m`. A nonzero root of t^7-a is simple in characteristic zero, so the corresponding root of r has multiplicity3. Consequently

`r(z)=(z-a0)^3`, with `a0!=0`.

The same root serves Q by common powers; its multiplicity is3n. Here a0 denotes the root parameter, not the later cover coordinate a. After r is fixed over k, a0 is already its negative quadratic coefficient divided by3. Working over kbar from the start is also harmless. The equality `(1,0)+3(2/7,1)=(13/7,3)` checks the child without changing orientation.

The rectangle is an explicit antecedent: bare standardness in GGV Definition4.3 permits `k[u,u^-1,v]` and does not imply it. This report proves no normalizer or all-same-degree F10 selection. The hypothetical normalized source itself has actual degrees28m,28n by its attained joint corners.

## 3. Common vertical translation and the triple Euler element

Let f(u) be the coefficient of v^(21m) in P0. It has degree7m and nonzero leading coefficient alpha. GGV Theorem2.6 at(0,1), using its ORDINARY clause, gives an Euler element `v h(u)` with

`21m h'f-h f'=f`.

For deg h=d>=2 the highest coefficient has nonzero multiplier `21m d-7m` and exceeds the degree on the right; constant h also fails. Hence h=A0*u+B0, `14m A0=1`, and `f=alpha(u+r0)^(7m)` for r0=B0/A0. The highest-v coefficient hQ of Q0 satisfies

`21n f' hQ-21m f hQ'=0`,

so `hQ=beta(u+r0)^(7n)` with the SAME shift. The substitution u->u-r0 preserves both rectangles/corners and lowers `(7,-2)` weight on every changed term. It preserves the whole starting face and a0. At maximal u-degree7e, the weight bound forces v-degree at least21e; the rectangle forces equality. Thus the opposite maximal-u coefficient is already pure and is unchanged as well.

Now derive the Euler element at the original `(7,-2)` face independently for the triple root. If E is the element from Theorem2.6 for P0, then `[mE,R]=R`. Every ordinary weight5 monomial is `u^(1+2i)v^(1+7i)`, so write `mE=uv f0(z)`. The exact product-rule identity is

`5z r' f0-7z r f0'-r f0=r`.                                      (1)

For r=(z-a0)^3, division by r gives

`(15z/(z-a0)-1)f0-7z f0'=1`.                                   (2)

The pole forces f0(a0)=0. Degree greater than2 is impossible because its highest coefficient has multiplier `14-7 deg(f0)`. Thus put `f0=(z-a0)(A z+B)`. The left side of(2) is exactly

`(7B+8a0 A)z+a0 B`.

It follows that `B=1/a0`, `A=-7/(8a0^2)`, and uniquely

`f0=-(z-a0)(7z-8a0)/(8a0^2)`.                                 (3)

This is a genuine polynomial Euler element, not a contradiction to the triple face. Its quadratic coefficient is nonzero. Consequently the endpoint of E is `(5,15)=(5/7)(7,21)`. No F9 double/simple ratio equation was reused: the F10 triple has no second root to constrain.

Swap `(u,v)=(y,x)`, retaining output order, and call the translated/swapped pair P,Q. The bracket becomes -c0. The common coordinatewise dominant corners are `C_e=(21e,7e)`, each unique at its maximal x- and y-degrees. Total-degree and y-degree ratios are m/n. The unchanged starting direction is `d0=(-2,7)`, normalized start `(21,7)`, other endpoint `(0,1)`, value7e. The negative of the swapped Euler element has start `(15,5)=(5/7)(21,7)`, so its reduced denominator is `q_E=7`, independent of q,m,n.

This is the ACTUAL constructed Euler element at that face, not an arbitrary solution: in `k[x,x^-1,y]`, the common face root is `R0=y(x^7 y^2-a0)^3`, with y-valuation1. If two homogeneous weight5 Euler elements differed by Z!=0, Proposition2.1 would give `Z^7=unit*R0^5`, hence `7 ord_y Z=5`, impossible in the integer-y lattice. This uniqueness also preserves q_E after the Laurent cut below.

## 4. Whole-positive-interval hypotheses and the first integer cut

Choose the nearest lower edge at C_e in the UNION of the two polygons, with primitive normal d=(rho,sigma), rho>0>sigma. Up to that edge both support functions are attained at C_e. Such a lower edge exists: the starting face retains `(0,e)` as well as C_e, and the corner is the unique coordinatewise maximum.

At least one of P(x,0),Q(x,0) is nonconstant; otherwise both x-derivatives vanish on y=0 and the constant bracket vanishes there. Ordinaryness and rho>0 then give a positive d-value for that member. The proportional corners imply `3rho+sigma>0` for BOTH. Positivity on the entire required interval from d to d0 follows because both endpoints evaluate positively at the same corner and `7rho+2sigma>rho>0` makes this sector smaller than a half-circle. All intermediate directions are positive linear combinations of its endpoints.

The expected face-bracket weight at d is

`7(m+n)(3rho+sigma)-(rho+sigma)`
`=(7(m+n)-1)(3rho+sigma)+2rho>0`.                              (4)

Thus the two faces commute. Proposition2.1 and their positive m/n weight ratio force both to be edges if either is one. No m<n assumption is used.

All Corollary7.4 hypotheses are now literal: l=1, nonzero constant bracket; total/y degree ratios m/n; coprime m,n>1; d0 in V>=0 and Dir(P), positive starting weight; normalized start `(21,7)` in Z x N with7<21; whole interval positivity to the stopping direction of Proposition7.3; and the constructed Euler denominator7. Cor7.4 therefore makes each d-face a7e-th power of a homogeneous root with endpoint `(3,1)`.

At this first cut the face is ordinary. A Laurent polynomial whose positive power is ordinary has nonnegative minimum x-exponent, so the root is ordinary and y-linear. Since it is nonmonomial, it is

`a1 x^3 y+b1 x^(3-k)`, `a1 b1!=0`.

Its integer x-lattice forces k integer and d=(1,-k); this is a DERIVED fact, not an assumption that polygon slopes are integral. Positivity gives0<k<3. Lower-side1605 Cor1.6 forbids k=1; its Proposition2.1 also excludes0<k<1 for an ordinary Jacobian pair with endpoint C_e, since21e>7e>0. The section's minimality discussion is not a hypothesis of that proposition. Hence k=2.

Common powers give the SAME nonzero lambda in both exact faces:

`ell_(1,-2)(P)=alpha x^(21m)(y-lambda x^-2)^(7m)`,
`ell_(1,-2)(Q)=beta x^(21n)(y-lambda x^-2)^(7n)`.

Lambda is recovered by the next coefficient divided by -7e times the leading coefficient; no generic localization or independent choice for each polynomial occurs. Apply the Laurent automorphism `psi(x)=x, psi(y)=y+lambda x^-2`, determinant1. This sign collapses the face to its corner.

## 5. The next edge exists; rational slopes and full support

Every changed exponent under psi is `(i,j)->(i-2t,j-t)`. The corners remain unique maxima of both coordinates and total degree, so the corollary's degree ratios are unchanged. The d0-face is unchanged, since the two substituted terms have d0-weights7 and4. In particular its NONZERO endpoint `(0,e)` persists alongside C_e. The next lower edge at the corner therefore exists in the finite Laurent polygon with finite positive slope. It is not inferred merely from nonmonomiality of an unrelated Laurent polynomial.

The collapsed first face shows its slope kappa is strictly greater than2. If2<kappa<3, its value `7e(3-kappa)>0`, the same whole-interval argument and(4) apply. The d0-face Euler element is still the constructed one by the uniqueness above. Cor7.4 again gives a y-linear root with endpoint(3,1), now possibly Laurent in x. Its other exponent `3-kappa` is still an INTEGER; thus kappa is integer, contradiction. This excludes all rational intermediate slopes. No corollary is applied at the zero-valued slope3 boundary.

For the actual next slope kappa>=3 and any support point(i,j), the edge inequality and independent j<=7e give

`i-21e<=kappa(j-7e)<=3(j-7e)`, hence `i<=3j`.

Before psi, ordinaryness and the original starting face give `i,j>=0`, `-2i+7j<=7e`, and consequently `i-2j>=(3/7)i-2e>=-2e`, with equality ONLY at(0,e). The shear preserves i-2j, lowers -2i+7j by3t, and keeps j>=0. Thus the COMPLETE post-cut supports satisfy

`j>=0, i<=3j, -2i+7j<=7e, i-2j>=-2e`.                         (5)

No unspecified coefficient cancellation or selected fractional child is used to claim polynomiality.

## 6. Exact maps, faces, scalings and field

Substitute `x=g^-1,y=g^3 p`. Its literal exponent map is `(i,j)->(I,J)=(3j-i,j)`. Inequalities(5) become `I,J>=0`, `2I+J<=7e`, `I-J<=2e`: both entire outputs are ordinary. The complete composite from the unshifted original source is

`(u,v)=(g^3 p+lambda g^2-r0, g^-1)`.                           (6)

Its determinant in(g,p) is +g, since `u_p=g^3`, `v_g=-g^-2`, `v_p=0`. This also equals swap(-1) times final toric determinant(-g); both translations have determinant1. Therefore the unnormalized receiver bracket is c0*g.

The top root becomes `Hbar=p(p^2-a0 g)^3`. The upper faces are `alpha Hbar^m,beta Hbar^n`. Equality in the pre-cut i-2j bound had only the original point(0,e), with coefficient `alpha_e r(0)^e`, where `r(0)=-a0^3`. Its whole shear contribution gives exactly the lower face

`alpha_e (-a0^3)^e g^(2e)(gp+lambda)^e`.                        (7)

Thus(2e,0) has a unique nonzero contribution proportional to lambda^e, and(3e,e),(0,7e) are inherited from the unchanged starting endpoints. These are attained vertices, not just hull inclusions.

Choose mu!=0 with `mu^3=a0*lambda` and put `kappa0=lambda/mu`. Substitute `(g,p)->(kappa0 g,mu p)`, and divide the two outputs respectively by `alpha mu^(7m)` and `beta mu^(7n)`. Then

`a0 kappa0/mu^2=1`, `lambda/(kappa0 mu)=1`,
`(-a0^3)kappa0^3/mu^6=-1`.

These equations prove BOTH normalized face formulas in Section1. The new Jacobian coefficient is

`c=c0*kappa0^2*mu/(alpha beta mu^(7(m+n)))!=0`.

It was not set to1. The cube root may require a finite algebraic extension. All other constructions use polynomial coefficient ratios or finitely many algebraic scalar roots, so a finite extension suffices; no chosen global normalizer or unsupported same-field descent is asserted. The source already supplies its rectangle and actual chain.

Since `I+J<=7e-I`, equality at total degree7e is possible only at(0,7e), whose coefficient is1. The g-bound follows from `J>=I-2e` and `2I+J<=7e`, yielding I<=3e, attained at(3e,e). Under the cover(g,p)=(a^2,b), weighted degree becomes total degree exactly; its determinant is2a. Hence its top is `b(b^2-a^2)^3` to the e-th power and its bracket is `2c a^3`, with the same source-output orientation.

The inverse of(6) is rational: `g=v^-1`, `p=v^3(u+r0)-lambda v`, determinant+v. This identity is not a reverse-polynomiality theorem for arbitrary receivers. A receiver point alone is not a Keller source.

## 7. Controls and precise result

These are hand-derived changed-object/hypothesis controls, not executed tests or full Keller examples.

- Replacing f0 in(3) by `(z-a0)/a0` gives the left side of(2) equal to `1+7z/a0`, not1. Omitting the quadratic Euler term really changes the identity and loses the denominator calculation.
- Changing r to a double/simple cubic changes the selected child to y-coordinate2 and changes the Euler equation. The F9 root ratio cannot be imported into F10 by renaming its second root.
- Using the negative shear `y->y-lambda x^-2` leaves `y-2lambda x^-2` on the original face. Its endpoint(7e,0) violates i<=3j and would yield a negative g-exponent. The PLUS sign is load-bearing.
- On a fractional x-lattice, `x^3y+x^(3-kappa)` can be homogeneous for a nonintegral2<kappa<3. The integer-cut conclusion specifically requires L^(1), not the L^(7) selected child.
- Dropping the rectangle while retaining a corner and total-degree bound allows extra terms `u^(7e-1)v^(21e+1)`; these lower the starting weight but destroy the asserted highest-v bound. This is a scope control, not a Keller pair.
- The ordinary receiver `(g,gp)` has bracket g but its first component reverses to v^-1. Thus the target Jacobian alone does not supply a reverse source lift.

**Result:** every source satisfying the stated ordinary rectangular ACTUAL-F10 conditions has the claimed ordinary receiver and quadratic-cover top/target, at the named GGV/GGHV/lower-side imported-theorem tier. No additional source gap was found in this bounded derivation. This is PROVISIONAL until reviewed. It neither excludes the receiver nor proves any source exists. No all-numerical-degree selection, all-q attainment, global minimality, complete coefficient ideal, solver permission, degree bound or JC2 conclusion is asserted. The two triple factors are outside the accepted simple/double consumer's hypotheses, and no local higher-multiplicity theorem was consumed.

## 8. Read scopes and documentary custody

All five scientific inputs were pinned BEFORE current WHOLE/selected reads. Accepted16a/b joint gate f33820d2... was read WHOLE for its exact imported statement scope. The frozen all-q F9 source report744dbc9c... was read WHOLE as an UNREVIEWED proof pattern, not a premise of the F10 conclusion. No live/current F2, F9, proof-gate or peer body/log/receipt was read.

Primary retained versions and exact scopes:

- GGV arXiv1401.1784v3, https://arxiv.org/pdf/1401.1784v3, text SHA `e3694dde3f83c2ab6ed8d957fc6b53472e6a6dd55486af1dff345eade39e37b1`: lines100–188 definitions;218–280 whole common-power proposition/proof;350–405 whole Euler theorem/proof;680–751 standard definition/context;1870–2182 whole opposite-edge Proposition7.1/Cor7.2 proofs, mirrored7.3/7.4 statements and proof instructions,7.6 and relevant proof. The few adjacent items are context, not new imports.
- GGHV arXiv1708.07936v1, https://arxiv.org/pdf/1708.07936v1, text SHA `49df06d11bbc4556a9b09771cdd60ca40a75b7542d327bbb622e09434e6a467e`:30–100 ordinary rectangle introduction;155–205 minimal/admissibility context;640–820 whole2.20 statement/proof;1184–1240 family definition and N0 parameterization;1350–1415 complete first/second Section5 tables and neighboring context. No enumeration rerun or uncharged subsequent exclusion is claimed.
- Guccione–Guccione arXiv1605.09430v2, https://arxiv.org/pdf/1605.09430v2, text SHA `77ae98339d7a70c476d89c01a2d38ba3aa1af98fbb0ba9da32c1759fde87a221`:160–303, including the whole residue theorem, slope-one corollary and lower-edge Proposition2.1 with its proof. Later Proposition2.2 is not used.

These are complete selected theorem/proof reads, not whole-paper claims. The printed mirrored proofs and named interior dependencies retain their imported trust. No new web fetch, mathematical subprocess, CAS, source/power expansion, coefficient builder, checker, AWS/SSH, agent, protected/shared/public write or new generic collector occurred. Only metadata operations and own transactional publication. The narrow history search's attempt at a nonexistent REDUCTION.md is recorded as a path miss; it was replaced by the exact existing frozen reduction-interface snapshot, without a repository sweep.

Full own body and own raised-OPEN/collision check precede BODY-END. Inputs, read scope and terminal custody are retained in the matching box; all writers IDLE before handoff. No follow-on is authorized.

## OPEN(S) RAISED

None.

## COLLISIONS

status: EMPTY

- NONE — own-only check, no corpus scan.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `18636`.
- Body SHA-256:
  `5b2bb27a536d08854f3446e390ef80a503eb1fdd402864cccd0b0da8af1edb5c`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
