# FIRST Fable 5.1: rows-only source interface and explicit correction partition

lane: f10-direct-rows-wire-gate-fable5-20260911
model: claude-fable-5-1
actual_first_utc_start: 2026-09-11T02:55:28Z
reserve: 2026-09-11 03:08 UTC (HARD 03:11 UTC), original, never reset
own_targets_absent_at_start: xmodel/f10-direct-rows-wire-gate-fable5-20260911.md ABSENT; box/f10-direct-rows-wire-gate-fable5-20260911/ ABSENT
inputs: EXACT9 in /tmp/jc2-lane.4kF5lM/inputs, all nine sha256 pins verified BEFORE any body (see input_custody.md)

## 0. Scope and collection facts (provenance, not proof)

Designated scope is ONLY the new rows projection/wire (f10-direct-rows-wire-root, body sha 9d5dd7ef...) and the ROOT addendum (ROOT-ADOPTED-CORRECTIONS, body sha 66343950...). The three original documents, their FIRST, accepted 23z (place-admissible completions give 25 rows in ker(B[X]->A) with g' congruent to the unit g), 19z (nonzero 297-minor at a genuine place transfers to the FIELD B) and the entire degree-7 field B are consumed at their established scopes and not re-proved. No source point, canonical-rank, unsaturated-ideal, I_30, all-r or JC2 claim; a residue zero never proves a generic zero. Manual mathematics and static text only; zero CAS/subprocess/import/AST/compile/test; hash and metadata tools only.

Collection facts, disclosed as received and not re-derived: the direct-circuit author completed 02:18:44 and the original FIRST ended 02:37:27; ROOT checked all writers/PIDs/cgroup absent before receipt-FIRST at 02:38:56, then read report/custody WHOLE with current pins. That FIRST's body claims its own final WHOLE read at 02:37:30, three seconds AFTER its recorded end; I record the discrepancy and do not resolve it. The ROOT rows-proposal transaction verified 02:38:36; its custody was created 02:39:51, after its 02:39 reserve and before its 02:42 hard stop. Its six inputs were a mixture of same-ROOT WHOLE reuse and two fresh WHOLE reads (its own text says so at lines 178-180). The ROOT addendum sealed/verified 02:51-52 as a separate immutable publication. One rows-manifest reverification mistyped the basis and refused; the corrected expected-basis verify succeeded 02:52 with no byte mutation. No actual source experiment exists in this packet.

## A. Projection logic

**CONFIRMED.** The lemma (rows note lines 36-61) is sound at exactly its stated strength. The conditional theorem chain is: (i) C is the corrected source-attached circuit, so by 23z each of its 25 rows is pi of an element of ker(B[X]->A) and g' is congruent to the unit g; (ii) a faithful independent implementation of C at a genuine place produces those reductions; (iii) 19z lifts a nonzero 297-minor of the weight-30 map of those reductions to B, forcing A=0. Only the 25 rows, their weights, Hq, c, zeta, ell, g and U enter (ii) and (iii). No intermediate value of either implementation is a premise of any step, so serializing none of them removes no premise. Circularity: the producer's graph is a claim, the checker's own Eval_C is the authority; acceptance means the claim equals the checker's reconstruction, and the certificate that later feeds 19z is the checker-accepted graph, not a producer assertion. contract_sha256 binds ROOT's manifest of the accepted operation version and is not a self-hash; source_pins bind the actual implementations, ROOT-supplied later. Nothing in the chain cites a producer intermediate. Illicit inference: the note explicitly disclaims injectivity of P and any trace-equality conclusion (line 44-45, 133-136); the theorem never needs trace equality because two traces with the same graph give the same 19z input. What the projection does change is diagnostic power, not soundness: producer/checker agreement on 32 positions instead of hundreds localises an implementation divergence less finely, but a divergence that reaches the rows still fails the comparison, and one that does not reach the rows is irrelevant to (iii). The ROOT addendum keeps every stipulated diagnostic computed and classified INTERNALLY in both implementations, so each canonical-anomaly tripwire still stops its own process before FORMED/CHECKED. One condition must be stated as a hard requirement rather than left implicit: the checker's CHECKED line may be emitted only after its OWN internal partition passes, never on graph agreement alone; the rows note says the partition is applied "exactly as before" (line 18) and ROOT line 118 says computation remains even if not serialized, which together give this. The STOP_BAD_COLUMN_PLACE and denominator stops likewise remain inside each implementation; a producer that silently substituted a coordinate would produce rows the checker's c1-first rule does not reproduce, hence REJECTED, not accepted.

## B. Correction completeness and scope; SCALE partition

**Diagnostic partition CONFIRMED; the six SCALE stops CONFIRMED for four fields and CONDITIONAL on one definitional sentence for low1/low0.**

Counts, recomputed by hand from the WIRE inventories: upper h1..6 part+basis0..2 = 6x4 = 24; upper:7 part+variation = 2; early 6x2 = 12; middle 4; install 2; Euler-band h0..7 = 8; low-band h0..7, j1,0 = 16; upper-J with 23-i-3j<=7: per j=2..7 the row lengths are 18,15,12,9,6,3 (sum 63) and the band>=8 slots (i<=15-3j) number 10,7,4,1,0,0 = 22, leaving 41; auxiliary 2. Canonical segment 24+2+12+4+2+8+16+41+2 = 111. Alternative segment 6+2+3+6+22 = 39. 111+39 = 150 = 32+12+4+2+2+11+63+22+2, the full labelled inventory; the partition is exhaustive with no residual category, and the two pole subrecords of inverse_A/inverse_B are classified separately. Every table count and index range in the addendum is exact. The residual-vs-full-inverse distinction is right: the full inverse_A/inverse_B Laurent polynomials must EQUAL the reconstruction (hard), only their negative-q subrecords are classified, and those may be nonzero, which is what 23z needs since inverse polynomiality is a hypothesis on the CANONICAL construction only. All 25 generators, both boundaries, 108/45/63, the 22 readbacks and every computed value are preserved (addendum lines 30-33, 84-85). No generic identity is promoted: the addendum states the tripwires are acceptance restrictions and that a false-negative stop is NONDECISION; the original FIRST's "accepted characteristic-zero identities" phrasing is thereby demoted, correctly, since 23z gives congruences in A only. A canonical-segment record with nonzero residue can therefore only mean a transcription error or a canonical recipe that would itself abort at its own raises; stopping and reopening is the right disposition, and the tripwires cannot produce a wrong acceptance.

SCALE comparisons, checked from the literal Laurent definitions. Write Ahat = S theta^3 + a2 theta^2 + a1 theta + a0 with Dformal = (a2+U)/S, Vformal = (a1-z+U Dformal)/S, K = a0, so a2 = S Dformal - U and a1 = S Vformal + z - U Dformal. Substituting z=s^2, theta=st and multiplying by s^-3: origA = S t^3 + s^-1(S Dformal - U) t^2 + s^-2(S Vformal + s^2 - U Dformal) t + s^-3 K = S t^3 + (S origd - origu) t^2 + (1 - origu origd + S origv) t + origk, using origd=s^-1 Dformal, origv=s^-2 Vformal, origu=s^-1 U, origk=s^-3 K. So A_inverse is identically zero as Laurent expressions; the equality is a definitional identity, safe to enforce. Pi = z theta - U theta^2 + S theta^3 becomes s^3(t - origu t^2 + S t^3) = s^3 origPi; Delta = -E theta Pi - theta Pi^2 becomes -s^4 E t origPi - s^7 t origPi^2 = s^7(-origell t origPi - t origPi^2) = s^7 origUpper with E = s^3 origell; so target_inverse is identically zero. Since d/dt = s d/dtheta and S is untouched, origA_S = s^-3 Ahat_S, origA_t = s^-2 Ahat_theta, origB_S = s^-5 Bhat_S, origB_t = s^-4 Bhat_theta (all after substitution), giving origJ = s^-7 J exactly; bracket_scale is identically zero. Hence origResidual = s^-7 R - 1 - origu t with R = J - Delta, and its coefficient of S^i t^1 is s^-6 P1_i(z=s^2) minus origu at i=0 only, its coefficient of S^i t^0 is s^-7 P0_i(z=s^2) minus 1 at i=0 only (U has no S, z or theta, so origu t sits at S^0 t^1). These match OPERATIONS line 53's targets P1_0 - U s^5 and P0_0 - s^7 after clearing s^-6, s^-7. guard_product: a = C0 = 1/W (c has constant term 1) and b = D0 = 1/t5 (t has constant term 1), so W t5 a b = 1 is an exact B-scalar identity by construction; with the s^8 of omega absorbed by the s-exponents of the two reconstructed original leading coefficients, the product-minus-1 is identically zero. The operand definitions live in check.py 460-500, which is not in this packet, so the rows contract must name the two coefficients and their s-exponents explicitly (optional refinement, cannot cause a wrong acceptance).

The one item that CAN cause universal NONDECISION: WIRE line 21 says low1 "has 21 differences from s^-6 P1_i(z=s^2) minus origu only at i0", and ROOT line 71-77 calls them "literal coefficients of origResidual with exactly the prescribed -origu and -1 shifts". Two readings exist. Reading (a): record_i = [S^i t^1]origResidual - (s^-6 P1_i(s^2) - delta_i0 origu), identically zero by the computation above, a pure transcription tripwire. Reading (b): record_i = s^-6 P1_i(s^2) - delta_i0 origu itself, which EQUALS [S^i t^1]origResidual, i.e. the reindexed low source row before z->zeta; these are the generators A1_i, A0_i up to reindex and are generically nonzero (if all vanished the weight-30 map would have rank 0). Under reading (b) the addendum's "must vanish" is a false equality and every correct run of a correct recipe STOPs: a never-passes implementation, not acceptance. The original WIRE plausibly intended (b) as a diagnostic (comparisons to the canonical source "are diagnostics", OPERATIONS 53), which is exactly why the addendum's promotion to a hard stop needs the definition fixed. Minimal correction: state in the rows contract that scale-low1:i and scale-low0:i are the DIFFERENCES of reading (a), identically zero by the reindex identity, and that the coefficients of origResidual themselves are not required to vanish. With that sentence, B is CONFIRMED in full; without it, the SCALE low stops are REFUTED as written.

## C. Strict new wire identity

**CONFIRMED, with two interface pins required before two authors start.** Top keys schema, job_tag, contract_sha256, source_pins, place, graph: exactly 6. graph keys Hq, c, c_inverse, zeta, slots, ell, g, U: exactly 8; polynomial positions Hq, zeta, ell, g, U plus 25 slots = 30; scalars c, c_inverse = 2. Slots K8,K9,K10; A1_1..A1_9; A0_1..A0_12; T = 3+9+12+1 = 25, weights 8,9,10; 20-i for i=1..9 (19..11); 23-i for i=1..12 (22..11); 27; Hq, zeta 7, U 4, ell 23, g 30 = 7+23; all consistent with OPERATIONS line 57-59 and the original FIRST's derivation. place keys p, degree, phi, P7_mod_p, decimal strings, f in 1..7, phi f+1 entries with leading 1, P7_mod_p exactly 8 with leading 1, recomputed from the literal rational program. Canonical bytes: ASCII JSON, sorted keys, no insignificant spaces, one final newline, exact round trip; dense length-f coefficient arrays of canonical residues 0..p-1, nonzero element, no JSON numbers/floats/signed/leading zeros/-0; exponents four nonnegative canonical decimal strings in strict ascending NUMERICAL lexicographic order (integer comparison e1,e2,e3,e4, an improvement over string order); empty terms for zero. Caps 20000/400000/100000/128MiB are refusal ceilings; the exponent cap 256 is never binding because the largest graph exponent is 30 (X1^30 in g). Schema f10-r3-direct-rows/v1 and job f10-source-cone-r3-direct-rows-v1 and the two status lines FORMED_UNCHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME / CHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME are disjoint from the full-trace schema/job/status and from the r3 reconstruction wire, so no receipt can be confused. Leak check: the only full-trace construct reused is the typed record with kind "graph"; no band/raw/laurent_S/inverse/scale kind, no denominators/leading/bands/physical/raw/diagnostics section, no selections field, and the internal check Psi7 = Hq + c z stays internal (z is not a graph variable). No full-trace semantics leak. Two authors would diverge on: (1) coefficient ORDER of the phi and P7_mod_p arrays, ascending power (constant first, leading 1 last, matching the dense scalar basis 1,V,..,V^(f-1)) versus descending; neither document states it. (2) The low1/low0 definition of B. Optional: lowercase 64-hex for source_pins values and the sha algorithm name for contract_sha256; the stream (stderr) of the CHECK FAILED line; the per-term weight predicate sum i*e_i = weight stated as a parse rule. With ROOT's metadata pins supplied later, contract/source authority is otherwise sufficiently specified.

## D. Reachable semantic control and checker independence

**CONFIRMED.** The checker reconstructs the complete circuit from literal constants and the place and compares all 30 polynomials and 2 scalars; the producer supplies nothing the checker consumes as arithmetic input, so it cannot certify its own graph. Mutation T+X1^27: weight 27 preserved; (27,0,0,0) is the unique maximal exponent tuple of weight 27, so canonical insertion is at the end of the terms list and the document stays canonical; with fresh whole-byte rebinding the metadata stage passes; the parse round trip is INPUT-only (re-serialise the parsed document and compare to the received bytes) and passes; place/denominator/leading/band/Euler/diagnostic stages are internal to the checker and untouched by the fixture; Hq, c, c_inverse, zeta, slots 0..23, ell, g, U compare equal; slot 24 disagrees by the nonzero field polynomial X1^27, giving exactly `CHECK FAILED: direct-source graph slot T\n`, exit 2, empty stdout. No earlier required comparison masks it: there is no self-hash, no producer-declared document hash, no aggregate over slots, and g = Hq*ell and zeta*ell = -c_inverse*g do not involve T. The one way to mask it would be a whole-document comparison of the received bytes against the checker's own serialisation BEFORE per-slot comparison; the rows note forbids that (line 119-120) and the contract should say the round trip is input-only. Reverse: removing the X1^27 coefficient returns the abstract object to its original, and canonical serialisation is a function of the object, so all bytes are restored. No omitted field enables acceptance of an unrelated matrix: the rows wire carries no matrix, and the later rank client rebuilds all 297 columns from the checker-accepted rows bound to the receipt and exact input hash. The inverse W[0][0] constant-coordinate +1 control remains SPEC for that later client with all 88209 products, outside this source-wire code. No actual control exists.

## E. Readiness decision

**READY, conditional on two one-sentence pins.** The rows-only interface preserves the conditional theorem (A), the corrected partition closes the original blocking item (B), the wire is strict and disjoint (C), and the semantic predicate is reachable (D). It merits two bounded independent source-code authors on the fixed full circuit, sharing only a metadata-only interlock with no polynomial or finite-field mathematics, followed by ONE combined different-model FIRST before any runtime. BLOCKING before authoring (interface, cheap, no mathematics): (1) define scale-low1:i and scale-low0:i as the reading-(a) differences of B, identically zero by the reindex identity; without it the addendum's all-comparison stop is a universal NONDECISION. (2) fix the coefficient order of phi and P7_mod_p. OPTIONAL: guard_product operand names and s-exponents; hex case and algorithm for the pin fields; stderr for the failure line; the weight predicate as a parse rule; a sentence that CHECKED requires the checker's own internal partition to pass. No coupling requires the original full trace; the smaller serialisation removes no polynomial computation, and the internal diagnostics may still dominate cost, which is unmeasured. No rank-source code, compute deployment, prime or factor search, enlarged cap, extra farm or source outcome is ordered or implied.

## F. OPEN quantity, cheapest exact next test, planning wall, collision

No new canonical OPEN identifier. QUANTITY: whether two independent implementations of the corrected circuit, at one ROOT-supplied genuine place with every D0/D1/D2 and all fifteen scalar residues nonzero and all 111 canonical-segment tripwires and six scale comparisons zero, agree on the 30+2 graph positions; unknown, nothing computed. CHEAPEST EXACT NEXT TEST: apply the two textual pins, then the two bounded authoring tasks and the single combined static FIRST; only then one single-place evaluation plus the T+X1^27 control. NUMERIC PLANNING WALL: the 10-12 minute figure for this FIRST and the 30-minute authoring figure are UNMEASURED planning proposals, not runtime forecasts; no operation count or speed is claimed. Refusal, STOP_BAD_COLUMN_PLACE, canonical anomaly, bad place or cap is PARTIAL/NONDECISION.

COLLISIONS: EMPTY. Own-only: this lane wrote exactly this report and box/f10-direct-rows-wire-gate-fable5-20260911/input_custody.md, both absent at 02:55:28Z; apply_patch only, bounded writes under 1500 words (skeleton, 0/A/B, C/D/E/F, custody, marker); no Seal, charge_basis, transaction, Write/Edit, redirection into owned files, generated mathematics, code, input edit, network, process control, git, peer, live, shared, protected, provenance or ledger access. Zero scientific subprocess; tools used: date, ls, wc, sha256sum, cut, apply_patch. Post-read pins of all nine inputs recomputed after the reads and equal to the charged list (custody file). Own final WHOLE read of both owned files precedes the marker; the marker is the only standalone marker and nothing follows it; writers idle after it.

Verdicts: A CONFIRMED; B CONFIRMED for the diagnostic partition (111 canonical-segment, 39 alternative-segment, exhaustive over 150 labels) and for A_inverse, target_inverse, bracket_scale, guard_product, CONDITIONAL on one definitional sentence for low1/low0 (REFUTED as written under reading (b)); C CONFIRMED with two interface pins; D CONFIRMED; E READY conditional on those two pins. REFUTED: none mathematical. GAP: none. Additional tools used after the line above: awk, tr, cat (documentary only). One marker patch attempt at 03:04:06Z failed on a context mismatch and changed no bytes. Own final WHOLE read of both owned files completed 03:04:06Z, all nine post-read pins equal to the charged list at 03:03:24Z; this line written 03:04:26Z, before the 03:08:00Z reserve; this line and the marker are the last bounded apply_patch write; all writers are idle after it.

<!-- BODY-END -->
