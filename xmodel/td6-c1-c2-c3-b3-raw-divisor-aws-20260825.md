# TD6 fixed three-center section — exact `B3` closure

## Verdict

**PRODUCER-EXACT PASS; hostile review pending.**

At the fixed source-typed normalized three-center TD6 section, the direct
birational V27 replay reduces genuine P12 to `-k/50` on `B3=0` away from the
complete parameter divisor

```text
tau w (tau-2) (2tau-1) (tau^2-4tau+2)=0.
```

The `tau,w,tau-2` chart boundaries route exactly to the already frozen whole
`U=0`, `V=0`, or origin certificates.  Two exact V22 quotient-field pivot
orders close `tau^2-4tau+2=0`; clean V26 closes `tau=1/2`, with an
original-row source replay and complete chart `w^34`.  Therefore the whole
set-theoretic `B3=0` divisor is empty.

Combining this with the frozen generic certificate on `D(U H B3)`, the whole
raw `U=0` divisor, and the two-review-confirmed whole `H=0` divisor gives the
exact constructible cover

```text
A^3_{C,V,U}=D(U H B3) union V(U) union V(H) union V(B3).
```

Thus no `(C,V,U)` over any extension of the coefficient field satisfies the
transported first-band/genuine-P12 necessary equations in this fixed
normalized section.  A clean V26 ascending mirror remains live as redundant
independent custody; top-level promotion is held until it returns, by
coordinator policy.

The frozen package is
`cases/td6_c1_c2_c3_b3_raw_divisor_aws_20260825/`.  No neighborhood,
boundary/dead-stretch, whole-TD6, SP-2, maximum-degree, or JC2 statement is
made.
