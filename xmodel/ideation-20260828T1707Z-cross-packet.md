# JC2 `20260828T1707Z` cross-pollination packet

Timestamp: `2026-08-28T17:35Z`

## Frozen source corpus

Before reading, verify all five hashes and fail closed on a mismatch:

```text
1e40e7106e2a3c16b6a6fe92e6d2387228fb1260ae5633e686b456e9f9d95f1c  xmodel/ideation-20260828T1707Z-packet.md
2706e64ef3999e0d94682beeaeda5b52563c2eae93e65cabe59833301f98fdad  xmodel/ideation-20260828T1707Z-sol-ultra.md
4cbb91826b43caf2461b427d074035f54ea8ebef38fe363af96d0b7a44ba153c  xmodel/ideation-20260828T1707Z-grok.md
01cffc70f24297de399b945bab171fd2a7f2a38bda0706a5575c0d403be662f5  xmodel/ideation-20260828T1707Z-fable5.md
029dfc674dee72b631d8e6d4ac686bd1e916b6f802a5ad6e33b5c7ee848c8c5b  xmodel/ideation-20260828T1707Z-opus5.md
```

Read all four reports completely.  They are now deliberately unblinded.

## Post-cutoff overlay A — mixed root window (hostile-confirmed)

Verify and read:

```text
aaa4d179992c51210eb70a30127c8c32d0856bf11b9b044802bec1746cbcacb0  xmodel/sigray-mixed-root-window-coordinator-integration-20260828.md
```

The safe headline is edgewise: at any genuine contact-zero root merge,
every actual searrow parent edge is Proposition 9.3 case I and satisfies

```text
X_R=mu_e(1-w_e)=A/B,
w_e=1-A/(mu_e B),
0<w_e<1.
```

This includes mixed and all-`mu>=2` edges.  If one `mu_e=1`, full repaired
MP6 root anatomy applies.  For all-`mu>=2`, do not infer `A=sum mu`,
`B=r+l`, `k=0`, no northeast roots, or `B>A`.

An independent read-only census found that the theorem kills all generated
all-`b=1` root rows through `td=12`; off-axis/mixed root completeness remains
open.  The proposed stronger `td=6` two-pole root-sector closure hinges on a
narrow audit of `L1a(b)`/whether every such root meet receives a certified
row-1 `M=1`, `W={2}` branch.  Do that audit rather than assuming it.

## Post-cutoff overlay B — MFE composition

The selected-exit theorem is green:

```text
sum_{F in U_full} lambda_F^exit <= td-1-psi.
```

Literal composition on residue A gives no kill.  The two poles and merge
contribute zero selected-exit weight; the non-chain suffix orbit contributes
2.  For the `R3` terminal (`psi=2`) the cap is 3, so the exact witness weight
is only known to be 2 or 3.  For `R4` (`psi=3`) it is exactly 2.  The q-only
resonant orbit is not a p-root/tree direction/cv witness, so no factor 6 or 7
is licensed and ordinary `T_a`-supported lambda refinements cannot price it.

Primary reports:

```text
86b491adc6ba6b21fcec5a8722126d80f3666d8e3d9f2cdcf4568d408bbc83a8  xmodel/sigray-multipole-global-first-exit-partition-sol-ultra-20260828.md
9f4526f209366098f12bbe60387a190c6d2942a374c05fad092917bc79145f14  xmodel/sigray-multipole-selected-orbit-attachment-repair-gpt56-20260828.md
f55a00f5259d77766cc8179f3d1248ee0c1320daf411f04758487d2e94e216bb  xmodel/sigray-multipole-selected-orbit-attachment-hostile-fable5-20260828.md
c74fc0f99a830e3845e0821188c3f693b9b47d441e5c9f00716ac72ef2f09836  xmodel/sigray-multipole-selected-orbit-attachment-coordinator-integration-20260828.md
```

## Post-cutoff overlay C — exact GGV endpoint state

All six exact rank-2 `D(Delta)` endpoint charts completed on AWS:

```text
95 pivots replayed;
all bordered identities and lifts have normal form zero;
all endpoint coefficients vanish;
every I+(Delta) is proper.
```

Thus only `ENDPOINT_DEAD_ONLY_ON_D(Delta)` is licensed; every complement
`V(I+(Delta))` remains open.  Frozen campaign result/custody hashes are
`dd460760...` and `71cd2bcd...` (archive `1fe30b1c...`).

The first q1p03 complement-recursion pilot failed only at the adapter: a
literal `REDUCER_PLACEHOLDER` survived generation, Singular returned rc0
with parser diagnostics, and the required 95-pivot marker gate correctly
rejected it before any mathematical inference.  Failed archive SHA is
`33e20a4d5f8e3646d66b7ac2f646924fcfc84a2b3f3a014785a8eb3cb3d84815`.
A substitution-only repair is being independently mutation-gated; fanout is
held until one full exact node passes.  Compare this recursion architecture
against the proposed Fitting-level/module alternatives, but do not treat the
adapter failure as evidence about the mathematics.

## Cross-review charge

Produce a concise, adversarial synthesis—not another independent portfolio
survey.  Address all of the following:

1. Audit `ROOT-W2` and the claimed complete two-pole `td=6` root-sector
   closure line by line.  Identify the exact weakest dependency and state
   `PROMOTE`, `REPAIR`, or `REJECT`.
2. Compare and, where possible, combine the residue-A proposals:
   simultaneous coefficient/parent match, `MERGE-DIFFERENT`, `RES-AV`,
   `Q-GHOST`, `A47-BUDGET`, and coefficient-complete seed-to-jet lifting.
   Check whether each sees the q-only resonant orbit geometrically rather
   than merely renaming it.
3. Verify the elementary identity behind `A47`, then state the first exact
   missing implication needed to turn it into a budget or contradiction.
4. Decide the fastest complete GGV route: repaired complement recursion,
   Fitting-level atlas/module presentation, or a hybrid.  Give a fail-closed
   proof-object contract and an AWS fanout trigger.
5. Extract at most three genuinely new connections that arise only after
   reading the peers and post-cutoff overlays.
6. Return a ranked 6-hour execution queue with explicit stop rules and a
   proof-versus-disproof resource split.
7. Distinguish promoted facts, provisional claims, conjectures, and failed
   ideas.  Quote no result more broadly than its scope.

Read additional non-`jc2-lean` source files only when needed to check a
dependency.  Never enter, list, search, read, build, modify, status, or
control `jc2-lean`.  Do not run heavy computation locally; use AWS for any
uncertain or heavy CAS.  Do not modify canonical ledgers or any peer report.
Write only the assigned cross-review file and print its SHA-256.
