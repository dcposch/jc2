# Weight-free composition gate preparation

September 9, 2026. PREP ONLY; no gate launch or mathematical promotion. The prepared external target is `xmodel/d125-weightfree-composition-gate-fable5-20260909.md`, with a bounded30-minute hostile review measured from actual model startup.

The immutable source framework and Lorentz client interface report/transaction pairs were verified before the client body was read. Whole client and published-chain gate bodies were read for exact endpoint scope; the minimal-receiver and sufficient-lift bodies had already been read whole at their identical pinned versions. No live root partial or live peer gate was read. Root released the local-consumer final bytes at00:39:27: report SHA d4fd2a0e6549edbf9e65f8aa59e2fc2a1d6bbbc0299188fdd6adf96f340c8f51, transaction SHA826419958c0a0b38e9c16bf17afe5d6f3749c42baf6aeaf42b363715a088cd4c. Both pins and transaction were verified before the final proof was copied and read whole. All nine original/snapshot pairs are frozen; all three report transactions were checked.

The prompt has six targets: weight-free reference/kernel construction; local factor cover and general Morse coordinates; coalesced order analysis and lower-B lemma; separated/simple cases and all infinite-order/cancellation cases; BOTH golden resonances with exact signs and no weighted-face replacement; and conditional six-client/finite-ideal/standard-F2 composition. Source and consumer are unreviewed; the map is conditional. Existing accepted map inputs are consumed at statement/interface scope, without another foundational checker farm.

The vector has eleven unique basenames: packet/PINS, framework and transaction, root local consumer and transaction, client interface and transaction, minimal receiver, sufficient lift, and published-chain gate. Declarations use literal repeated `charged_input=` lines and `{{LANE_INPUTS}}`, not indexed declaration syntax. Finalization runs the exact `sed -n 's/^charged_input=//p'` expression used by unchanged ops/lane.sh, compares the complete ordered vector, verifies every current source/snapshot hash and requires unique basenames. Its replayable metadata is preflight.json; publication.json and custody.json record transaction verification and terminal writer state.

No charge_basis declaration or new exit price is included. The external author must emit only the report, no authored Seal, and finish with one standalone BODY-END marker only after substantive completion. Optional tiny controls must retain exact evidence; no unsupported private-runtime claim is requested. No actual H/R high powers, full source, A15/B25 or complete6/10 pair expansion, CAS/AWS/SSH/web, agents, shared/protected edits or launch is authorized.

Preparation changes no mathematical verdict. Root alone may launch after consuming the completed transaction, current input vector and whole prompt. No launch or mathematical subprocess occurred. All owned writers stop after publication/custody finalization.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `3017`.
- Body SHA-256:
  `720e03e990a7de68c16f3ebeb38ea951f2606c0079b1aef614779a919fc74823`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
