# Hostile gate: delta=5/2 stage-8 kill for the (99,66) joint chart

Date: 2026-09-04 UTC.  Computation artifacts were generated from the
September 3 lane inputs under `box/g9966d52gate-20260903/`.

## Verdict

`CONFIRMED[DELTA52-STAGE8-DECLARED-NECESSARY-FINITE-CHART-KILL]`.

The charged stage-7 endpoint was replayed mechanically: exact localized
dimension `869`, outer D2 rank `5598`, outer D1 rank `176`, joint Q-star rank
`59`, and zero residue.  The pinned stage-8 system was then rerun in the
foreground with `timeout 3000` and CPU affinity `0,1,2,3`.  It exited `0`;
the validated stage-8 ledger is `DEAD`.

The seven new Q-star pivots are exactly the Jacobian degree-155 rows
`k=35..41` on `B1c_7_19..B1c_7_25`, with coefficients

```text
675, 576, 477, 378, 279, 180, 81.
```

After all 66 cumulative rational pivots, the only reduced residue is

```text
stage8_G_local16_coord0 = 64.
```

Since the coefficient field is `Q`, this gives the explicit membership
certificate

```text
1 = (1/64) * stage8_G_local16_coord0.
```

The raw residue ideal is already `[1]`; localizing at `c != 0` cannot restore
a point.  An independent characteristic-zero Singular replay of the emitted
Rabinowitsch script returned dimension `-1`, standard basis `1`, and controls
`0,1,0`.

Together with the charged delta-2 stage-4 unit row
`stage4_J_d159_k35 = 6264`, independently replayed here with the same
Singular controls, both declared split branches are empty inside the declared
joint chart.

## Custody

The 23 charged inputs in `/tmp/jc2-lane.MvSYEH/inputs` were checked before
any replay.  The manifest was generated mechanically from
`xmodel/g9966-delta52-kill-gate-gpt55-20260903.run.v2`, pairing each indexed
`charged_input_<i>_sha256` with the corresponding
`charged_input_<i>_basename`; `sha256sum -c` returned `OK` for every input.
No digest was manually retyped.  The resulting gate manifest is
`box/g9966d52gate-20260903/charged-inputs.sha256`, SHA-256
`fad58e40a54625eef336ca634c18568db08769bdfd6872c8ad8c2005954fba63`.

The pinned continuation engine is
`box/g9966band-20260903/band_engine.py`, SHA-256
`3bd2937d75d59d59edeca1b89eba286fd2711f2d608109e7fd8971b64e059ac9`.  Its
transitive frozen view is the earlier Sol input lane
`/tmp/jc2-lane.cNPqDa/inputs`; the gate driver restored and verified that
view from `xmodel/g9966-global-band-sol56-20260903.run.v2`, without changing
the engine.

The stage-7 endpoint custody replay confirmed:

```text
stage7.json bytes      43433
stage7.json SHA-256    a85d602dc4599c600c12b04e2c3140f00dafafbb5793b3b05039050cba6159b3
dimension              869
residue_count          0
dimension formula      6702 - 5598 - 176 - 59 = 869
```

The regenerated stage-8 next-system manifest is byte-identical to the charged
one:

```text
next-stage8-system.json SHA-256
a050af8c5d4bbb703babb2ce2ee57a70c411eb801226ac9f5cdcffbfa0bfa699
```

It carries the accepted stage-7 path/hash, the 869 unresolved generator names,
the pinned engine hash, and argv

```text
python3 box/g9966band-20260903/band_engine.py --branch delta52 --stage 8
```

The 869-name list was regenerated byte-for-byte as
`box/g9966d52gate-20260903/stage7-free-coefficients.txt`, SHA-256
`30d4f3e027d4bb1db62dce3b7226e15f0ac8a74654724d0ede595cc0d10bbbe9`.

## Stage-8 Replay

The foreground run was:

```text
timeout 3000 bash box/g9966d52gate-20260903/run_stage8.sh
```

The wrapper checks the pinned engine hash, sets numerical-library thread
counts to `1`, sets `PYTHONHASHSEED=0`, applies `taskset -c 0,1,2,3`, and runs
the engine with `--branch delta52 --stage 8 --emit-singular`.  It returned

```text
stage=8 status=0 cpu_set=0,1,2,3
```

The promoted and validated result is
`box/g9966d52gate-20260903/runs/delta52/stage8.json`, SHA-256
`095c340d7729f97df9a1f1f4709c5afcde00be32fac15cb3ced540e4ae15ccb9`.
Its expression-level row hash and residue hash match the charged Sol report:

```text
all_labeled_rows_hash  56dbca47ef766b098f5d65321324f85909a4c9a65d1a1c10079240b41f5c9ec6
residual_hash          e356693da067de90565ae0275ecd5d6779737e962b9d01476fc46b355ce3ccb4
```

The whole stage-8 JSON hash differs from the charged Sol JSON because elapsed
resource fields are run-local.  The mathematical hashes, residue, Singular
script hash, band specification, and pivot data agree.

Validated stage-8 band:

| item | value |
|---|---:|
| outer D1 offset | none |
| pole local power | 16 |
| F pole exponent | -173 |
| G pole exponent | -110 |
| Jacobian t-power | 8 |
| Jacobian total degree | 155 |
| Jacobian rows | 156, `k=0..155` |
| F pole rows | 2, `coord0`, `coord2` |
| G pole rows | 2, `coord0`, `coord2` |
| external wall / RSS | `1870.89 s / 279056 KiB` |

The new row family is therefore exactly 160 rows: four pole rows and the full
degree-155 Jacobian row family.  There is no stage-8 outer-D1 row, because
the outer D1 boundary was exhausted at offset 7 and cumulative rank 176.

The stage history is

```text
1063 -> 1016 -> 974 -> 940 -> 913 -> 894 -> 880 -> 869 -> DEAD.
```

The run stopped by unit ideal, not by the two-band stagnation rule, a small
nonempty dimension, or a budget stop.

## Pivots And Residue

The seven new pivots, all in `Q*`, are:

| row | pivot | coefficient |
|---|---|---:|
| `stage8_J_d155_k35` | `B1c_7_19` | 675 |
| `stage8_J_d155_k36` | `B1c_7_20` | 576 |
| `stage8_J_d155_k37` | `B1c_7_21` | 477 |
| `stage8_J_d155_k38` | `B1c_7_22` | 378 |
| `stage8_J_d155_k39` | `B1c_7_23` | 279 |
| `stage8_J_d155_k40` | `B1c_7_24` | 180 |
| `stage8_J_d155_k41` | `B1c_7_25` | 81 |

The localization parameter `c` is never eligible for Q-star elimination:
the engine reduces with `set(all_free)-{c}` and asserts that `c` remains.
Every cumulative pivot coefficient parses as a nonzero rational.  There are
no retired pivots.

Before the residue, the formal remaining-generator count is `862`.  This is
not a surviving family dimension.  The single residual row is the exact normal
form

```text
stage8_G_local16_coord0 = 64.
```

The emitted Singular script is
`box/g9966d52gate-20260903/runs/delta52/stage8.sing`, SHA-256
`36f7f21f9faa3f7fec6b1d9353a194c17c6aecb8d2a5c2e48096b680c6e77b2e`.  It is
the characteristic-zero ring `Q[c,Zc]` with ideal `64, Zc*c-1`, plus the
three controls.  The independent replay output has SHA-256
`236517b4a8e5567c586d39194059d49207001416a1d922efaec1fc3d2b9c7b73` and
prints:

```text
BEGIN_DIM
-1
END_DIM
BEGIN_GB
1
END_GB
BEGIN_CONTROLS
0
1
0
END_CONTROLS
```

The controls mean: the deliberately empty localization is unit, the point
localization is nonunit, and the raw constant residue ideal is unit.

## K2c Coordinate Change

The stage engine computes in the declared `K2c` basis:

```text
K2c_r_q = [t^r(w-1)^q](K3^3 + (t^22 C2)K3 + t^33 C3),
```

for low `q`, after the major h2 D2 structural state.  The charged structural
certificate states that the unit leader is `C3c_r_q` for `q<=10` and
`C2c_r_(q-11)` for `11<=q<=21`.

This gate reconstructs and serializes the inverse as a compact triangular
ledger:

```text
box/g9966d52gate-20260903/k2c-c2c3-inverse.json
SHA-256 88e717d691f6ab35336e3a0a387463f4c319d5917c37c417a54f8a2dddf9a768
```

The ledger has 495 inverse rules, one for each original low-q `C2c/C3c`
coordinate:

```text
C2c variables                         187
C3c variables                         308
low-q nonidentity output slots         495
D2 fixed low-q slots                   389
D2 identity slots outside low-q        (1,22), (1,23), (2,22)
K2c coordinates before h2-D1           106
K2c coordinates after h2-D1             99
inverse_rules_sha256
aabd8ce03875fcab6432b27f0af8151a2e796ff717d646b8c66af4cb58f37495
```

The inverse is compact, not fully expanded.  Each rule has diagonal
coefficient `1` and every nonleader `C2c` dependency is solved earlier in the
`(r,-q)` order.  The seven h2-D1 eliminations are also recorded:

```text
K2c_11_16, K2c_15_13, K2c_19_10, K2c_23_7, K2c_27_4,
K2c_10_17, K2c_14_14
```

with coefficients

```text
1, -3, 9, -27, 81, 1, -3.
```

This is enough for the stage-8 conclusion.  A unit-triangular polynomial
coordinate change is a quotient-ring isomorphism, and quotient-ring
isomorphisms preserve the unit ideal.  Thus the constant residue `64` proves
emptiness equally in the declared `K2c` basis and in the original
`C2c/C3c` presentation; a fully expanded inverse would be useful for
inspection but is not needed to conclude `[1]`.

## Necessity Of Row Families

All row-family claims here are typed as necessary conditions inside the
declared chart, not as attainment of a Keller map.

`CONFIRMED[COMMON-CHART-ROWS]`.  The chart is the fixed-top triangular tower

```text
h3 = P + H
h2 = h3^3 + C2 h3 + C3
F  = h2^3 + A2 h2 + A3
G  = h2^2 + B1 h2 + B2
P  = y^3(y-x)^8.
```

The label reversal is declared: chart `F` is Moh/Xu `g` of degree 99 and chart
`G` is Moh/Xu `f` of degree 66.  The minor line at `y=0` and the major line
at `y=x` are a linear/torus gauge of the two fixed lines, not a degree swap
and not a numerical slice.

`CONFIRMED[MAJOR-H2-K2C-ROWS]`.  Moh Theorem 1.2 supplies lower-order
inequalities along the major tower.  Strict-below support entries are
vanishing rows; equality faces are not rows unless the fixed leading face
itself supplies the equality.  The common major-h2 structural state has 389
unit-triangular D2 pivots and seven h2-D1 pivots, leaving the declared 99
`K2c` directions.

`CONFIRMED[OUTER-D2-D1-ROWS]`.  The outer blocks `A2,A3,B1,B2` are constrained
by the same Theorem 1.2 inequalities at the first point.  D2 deletes exactly
5598 strict-below coordinates with thresholds `189,285,93,189`.  Outer D1
offsets `0..7` contribute raw-row/rank counts

```text
64/41, 52/38, 40/33, 29/25, 20/19, 11/11, 6/6, 3/3.
```

The cumulative outer D1 rank is `176`, and stage 8 has no new outer row.  The
already-deleted D1 labels are recorded as redundant, not as extra rank.

`CONFIRMED[DELTA52-BRANCH-ROWS]`.  The delta-5/2 branch state is

```text
p = pi(pi^2-c),     c != 0,
q1 = -2 integral p^3   up to the normalized scaling used in Xu's ODE,
```

with the Galois-forced `[1,1,1]` cubic split and the charged compatibility
`b0=0`.  The engine keeps `u,v,c` symbolic and localizes only by `c`; it does
not set `c=1`.

`CONFIRMED[POLE-ROWS]`.  Direct `F,G` pole rows are coefficients in the
branch-specific principal-minor Laurent expansion.  For delta-5/2 the
normalization shifts local powers by `189` for `F` and `126` for `G`.
Therefore stage-8 local power `16` is the original Laurent exponent

```text
F: 16 - 189 = -173
G: 16 - 126 = -110.
```

The assertion "pole local power 16, `(F,G)=(-173,-110)`" means: impose the
two raw-support coordinate rows at local power 16 for `F` and the two
corresponding rows for `G`, namely `coord0` and `coord2`.  These rows are
forced because an actual polynomial pair in the classified branch cannot
retain forbidden negative Laurent coefficients beyond the prescribed
principal-minor face.  Stage 8 kills on one of these necessary rows:
`stage8_G_local16_coord0`.

`CONFIRMED[JACOBIAN-ROWS]`.  The identity `J(F,G)=1` forces every positive
homogeneous coefficient of the normalized Jacobian to vanish.  The top
degree-163 part cancels since `F_99=P^9` and `G_66=P^6`.  The engine imposes
the charged degree-162 prefix, then stage 0 `k=27`, stage 1 the remaining
degree-162 coefficients, and for stage `n>=2` every coefficient in total
degree `163-n`.  Stage 8 therefore imposes all 156 coefficients of degree
155.  These are necessary rows of `J=1`, not an optional strengthening.

## No Unlicensed Specialization

`CONFIRMED[NO-UNLICENSED-SPECIALIZATION]`.

The gauge `P=y^3(y-x)^8` only fixes the two lines at infinity and their
placement.  The approximate-root Tschirnhausen omissions are characteristic
zero normalizations.  The split datum `delta=5/2`, `p=pi(pi^2-c)`, `c!=0`,
and the reduced `q1` ODE are imported branch hypotheses from the charged
classification; they are not extra numerical cuts.  The chart's `y=0` minor
line is the declared branch place.  No value is substituted for `u`, `v`,
`c`, or any outer coordinate.  The contradiction appears as the integer `64`
before localization.

## Branch Specificity And Controls

`CONFIRMED[BRANCH-SPECIFIC]`.  The same pinned engine and common row builders
do not kill every branch at the same place.  The charged delta-2 branch dies
at stage 4 with first unit row

```text
stage4_J_d159_k35 = 6264,
1 = (1/6264) * stage4_J_d159_k35.
```

This gate replayed its emitted Singular system from
`box/g9966band-20260903/runs/delta2/stage4.sing`; it again returned
dimension `-1`, basis `1`, and controls `0,1,0`.  Delta-5/2 instead remains
consistent through stage 7 and dies only at stage 8, on a `G` pole row.
That rules out the explanation that the shared outer rows or the Q-star
mechanism automatically kill the chart independent of branch data.

The `(64,48) -> (16,12)` sanity control was rerun through the available
charged control machinery.  The stage engine itself is hard-coded to the
`(99,66)` skeleton, so running that exact executable on `(16,12)` would be an
unlicensed embedding.  The p.208 `(16,12)` ansatz control uses the same
saturation/wrapper discipline: 17 coefficients, 77 Jacobian rows, wrapper
`T*kappa-1`, and both printed-c5 and compiler-c6 readings over `GF(32003)`.
Both reduce to `[1]`; the wrapper controls over `Q[kappa,T]` pass.

A tame two-point automorphism control also survives: the family

```text
F = a*x + y,     G = (a-1)*x + y
```

has `J=1`, and the shared ansatz row is only `A-C-1`; the slice
`(2*x+y, x+y)` is a witness.  The `(99,66)` s=3 tower, outer D1/D2 bands, and
T2/T3 bridge are `NOT-APPLICABLE[degrees (1,1)]` to this control.

## Theorem And Dependency Chain

The exact theorem supported by this gate is:

```text
For every Keller pair (F,G) of degrees (99,66) in Moh's gauge with
skeleton M=(-66,77,97), V=(8,8), lying in the declared joint chart,
if the principal-minor branch classification is exhaustive with
delta in {2,5/2}, then no such pair exists.
```

The dependency chain is exactly:

1. `17(sss)`: for every Keller pair of degrees `(99,66)` in Moh's gauge with
   skeleton `M=(-66,77,97)`, `V=(8,8)`, the relevant principal-minor split
   order is in `{2,5/2}`; the branch equations and nonvanishing data are the
   declared ones.
2. Design/outer/source gates: the fixed top chart, label reversal, major h3/h2
   rows, outer D2/D1 rows, direct pole rows, and direct Jacobian rows are
   necessary row families for a Keller pair in this skeleton and chart.
3. K2c coordinate gate: the imported low-q `K2c` computation basis is
   unit-triangular and invertible on the declared structural state, so the
   quotient in `K2c` is isomorphic to the quotient in the original low-q
   `C2c/C3c` coordinates.
4. `17(pppp)`: the delta-2 branch is empty; the audited unit row is
   `stage4_J_d159_k35 = 6264`.
5. This gate: the delta-5/2 branch is empty; the audited unit row is
   `stage8_G_local16_coord0 = 64`.

With those dependencies accepted, both classified branches are dead, hence
there is no Keller pair in the stated `(99,66)` skeleton and chart.  This is
not a theorem about unrestricted JC2 or about other skeletons.

On the unsplit `[3]` / Moh branch-A issue: no additional input is needed if
`17(sss)` is understood as the promoted exhaustive branch classification for
the skeleton, including the exclusion of the linear-power/unsplit alternative
by its predecessor gate `17(tt)` and the major rows.  If `17(sss)` is read
only as a classification of genuine split branches and leaves that unsplit
alternative outside `{2,5/2}`, then the branch-A/predecessor kill must be
listed as a separate dependency.  The present stage-8 gate does not by itself
kill a branch outside the delta-5/2 joint chart.

## FALLACY-v2 Audit

No cv flag, physical place, and cover series are identified.  The major
D1/D2 radii, the principal-minor split order, the pole filtration, and the
Jacobian homogeneous filtration remain distinct.

Strict-below support deletion is separated from equality-face data and from
denominator shedding.  Theorem 1.2 is used as a lower-bound source for
necessary vanishings; no lower bound is promoted to exact attainment.

The stage-7 dimension `869` is an exact dimension of the displayed localized
quotient with zero residue.  The stage-8 pre-residue count `862` is not a
surviving family dimension.  The final stage-8 dimension is null because the
ideal is `[1]`.

The `sat()`/Rabinowitsch wrappers are declared: `Zc*c-1` for delta-5/2 and
`Zrho*rho-1` for delta-2.  Both independent Singular replays have empty,
point, and raw controls `0,1,0`.

The variable/ring map is stated: field `Q`, Q-star pivots only on nonzero
rational coefficients, lexicographic symbol order, and `c` or `rho` excluded
from pivots.  Matching names are not used as proof of a coordinate map; the
K2c inverse is typed by the unit-triangular ledger.

No prime-labelled object is treated as a derivative without declaration.
The derivative in the ODE discussion is the branch variable derivative.

No exit-price assertion is introduced, so no `charge_basis` line is emitted.
No `REPRESENTATIVE`, `FULL_ACTUAL_EXIT`, `KELLER` witness, or recomposed
counterexample is claimed.

## Artifact Inventory

New gate artifacts:

| artifact | purpose | SHA-256 |
|---|---|---|
| `charged-inputs.sha256` | 23-input frozen manifest | `fad58e40a54625eef336ca634c18568db08769bdfd6872c8ad8c2005954fba63` |
| `custody-stage7.json` | stage-7 endpoint and manifest replay | `a0486a0224afcd74cf275af35b11e1babed4f0127d03946500689e4a02c3bbe0` |
| `k2c-c2c3-inverse.json` | compact unit-triangular inverse ledger | `88e717d691f6ab35336e3a0a387463f4c319d5917c37c417a54f8a2dddf9a768` |
| `runs/delta52/stage8.json` | fresh pinned stage-8 replay | `095c340d7729f97df9a1f1f4709c5afcde00be32fac15cb3ced540e4ae15ccb9` |
| `runs/delta52/stage8.sing` | emitted delta-5/2 Singular system | `36f7f21f9faa3f7fec6b1d9353a194c17c6aecb8d2a5c2e48096b680c6e77b2e` |
| `stage8-singular-replay.out` | independent Singular replay | `236517b4a8e5567c586d39194059d49207001416a1d922efaec1fc3d2b9c7b73` |
| `delta2-stage4-singular-replay.out` | independent delta-2 replay | `236517b4a8e5567c586d39194059d49207001416a1d922efaec1fc3d2b9c7b73` |
| `stage8-summary.json` | accepted stage-8 summary | `bff5ae0ddeca31051e571e7cc7e5c9f5a09b70bb0f0766c36a9f1e2348b8b33b` |
| `death-certificate.json` | both-branch unit certificate | `ef5cf3aabac8721f7a39d523fc03de557fc3e16a7bfbe4335a910a0b0bad1f79` |
| `controls-replay.json` | `(16,12)` and automorphism controls | `b59498485cdb36c37d454e74c7cb1a2ca9d24256a9fbc92b48fa5bbc50ce63bc` |
| `artifact-manifest.sha256` | gate artifact manifest | `b010f388f50a79c638b0f419ff7481764ab54cd71ab10b1ebf7642c938fe9b98` |

All gate jobs were foreground jobs under `timeout 3000`.  No ledger was
edited; no Lean lane or excluded in-progress report was used.  The only
writes are the bounded gate artifacts under `box/g9966d52gate-20260903/` and
this report.

Final typed result:

```text
delta=5/2: DEAD[DECLARED-NECESSARY-FINITE-CHART] at stage 8.
delta=2:   DEAD[DECLARED-NECESSARY-FINITE-CHART] at stage 4.
both declared branches: NO-SURVIVING-BRANCH[DECLARED-(99,66)-JOINT-CHART].
conditional skeleton theorem: as stated in the dependency chain above.
```

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `19329`.
- Body SHA-256:
  `866a1f48eee31e4985ab666734f642b104e87202e8e974fe7625dc62f121e191`.
- Frozen basis: `43bc921d5b6f5547639987e527af51aea51ae283`.
