cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
Review is complete and published to the single owned target at 06:02:17 UTC, before the 06:16:00 deadline. No further items are needed.

**Custody.** All six charged inputs hashed to the invitation pins before any read. Every file was read whole. No AUDIT, cross, live lane, canonical, PINS source path or historical checker was opened. Zero mathematical subprocesses ran; only date, ls, mkdir, cat, sha256sum and the report write.

**Verdicts, each re-derived by hand.**
- **A CONFIRMED.** The regrouping into anti-diagonals is a basis change in the Laurent ring, the map from index pairs to monomials is injective, so no cross-anti-diagonal cancellation exists. The criterion for positive s is exactly the stated divisibility, negative and zero s impose nothing, the binomial signs and the row count of 42 are right, and the outer face makes its m rows redundant.
- **B CONFIRMED.** The corner monomial is the unique top-degree term with coefficient 1, so actual degrees are exactly 84 and 140. The determinant is v, and the Laurent identity for the bracket descends to the polynomial ring, so the Jacobian is constant on all of the plane. The only external import in the sufficiency chain is the degree-divisibility theorem for plane automorphisms, which the contract names but does not cite.
- **C CONFIRMED.** The accepted diagonal scaling composes with the receiver map into an invertible affine transform of the source, so every normalized receiver has an ordinary reverse and meets every row. Coverage is over C at the accepted 16a/b tier; sufficiency is elementary. The report keeps that distinction correctly.
- **D CONFIRMED at the import tier.** I additionally checked what the report asserts but does not perform: the two faces coincide at the shared vertex and agree there, so the ideal is not trivially the unit ideal. Both top-weight bracket rows vanish rather than being constants. The properness equivalence holds exactly as stated.
- **E CONFIRMED.** Envelope counts 124, 321, 445, 447, 42 and 1344 all reproduce by hand. The zero-Jacobian control is exact, and it shows the unguarded ideal has a rational point, so any unit certificate must use the guard essentially.

**No REFUTED item, no GAP.** No OPEN raised, collisions EMPTY, no seal or charge_basis authored.

**Terminal metadata.**

| item | value |
|---|---|
| report | xmodel/f9-exact-reverse-source-contract-gate-fable5-20260909.md |
| file bytes, body-end included | 10305 |
| full SHA-256 | 81b6e559a32d2eebbdda85b0fd5fd41a675921759d0c4e6d2cb240c7d3f35c7f |
| evidence box | box/f9-exact-reverse-source-contract-gate-fable5-20260909/ (input hashes, report hash) |

All writers idle. The only processes matching the lane name are the harness itself and the shell that ran the final verification. No descendants were spawned.
DIVERT path=xmodel/f9-exact-reverse-source-contract-gate-fable5-20260909.md status=CLEAN body_bytes=10305 body_sha256=81b6e559a32d2eebbdda85b0fd5fd41a675921759d0c4e6d2cb240c7d3f35c7f
