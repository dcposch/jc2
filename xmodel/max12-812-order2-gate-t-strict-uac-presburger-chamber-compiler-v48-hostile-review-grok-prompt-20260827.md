You are Grok, the independent hostile reviewer for the additive V48 strict
unique-`AC` Presburger chamber compiler.  Work only in
`/Users/dc/code/math/jc2`.  Never enter, read, build, status-inspect, or modify
`jc2-lean`.  Do not edit frozen producer files, launch AWS, run heavy CAS,
perform a web sweep, or edit a canonical ledger.

Review these exact charged artifacts:

```text
29d206524de1eb2faadaa3d25a261376c7be380f5b669ed9cf5fa436b263cafe
  cases/max12_812_order2_gate_t_strict_uac_presburger_chamber_compiler_v48_20260827/SCHEMA.json
1a7dfe1c852e5ac655ed12e4efc80a9a1c8fd3ca2aa0ae0b98f3c17ebd04b575
  cases/max12_812_order2_gate_t_strict_uac_presburger_chamber_compiler_v48_20260827/compile_presburger_chambers.py
43ff29257e917b4ae5e264866a5159861a5439477b31d74e1a9c0afa6baf285a
  cases/max12_812_order2_gate_t_strict_uac_presburger_chamber_compiler_v48_20260827/COMPILED.json
2d1c954573cefe8b5f42e57cac5eed1b05161298c5668e40c6abaa9e9ca16ca1
  cases/max12_812_order2_gate_t_strict_uac_presburger_chamber_compiler_v48_20260827/RESULT.md
e6a765327de4a22dc8b54d3fedb1b518f638997257856b85905a0b1ef67ce646
  cases/max12_812_order2_gate_t_strict_uac_presburger_chamber_compiler_v48_20260827/FREEZE.sha256
```

Do not accept the producer `PASS`, the frozen `COMPILED.json`, V47/V47R1's
reviewed contact list, or the imported support miner as evidence for V48's
compiler result.  Independently rederive and attack:

1. **Custody.** Rehash every charged file and walk the V47 and V47R1 inner
   freezes and all ten source pins.  Check that every claimed pin is actually
   consumed, that V48 is additive, and that no generated output is silently
   used as an input to its own proof.
2. **Independent primitive recomputation.** From only the four atom records
   and four binomial summands in `SCHEMA.json`, independently expand and
   aggregate routes over `Q`; do not obtain the expected table from producer
   code or `COMPILED.json`.  Check rational coefficients, denominator/pole
   exponents, canonical keys/digests, the 425 nonzero and 40 cancelled counts,
   atom-bound `+1` stability, and especially the three advertised
   cancellations.  Decide whether fixed-delay cap 34 is complete for every
   charged positive and negative comparison.
3. **Inequality completeness.** Independently derive the strict unique-`AC`
   and global-pole inequalities from the recomputed table.  Check that there
   are exactly 424 and 417 respectively, that every excluded primitive is
   represented once after aggregation, and that the two permitted targets
   are exactly unloaded `AC/L` and unloaded `C^2/L^2`.  Attack coefficient
   cancellation, equal-grade ties, pole `>=2`, `R=0` persistent walls, strict
   versus weak inequalities, integer rounding, endpoint floors, and target
   walls `G<28`, `T_C2<32`.
4. **Presburger solution.** Solve the affine inequalities independently,
   without scanning over `r` or reading the producer's ray bases first.  In
   exactly the registered V47 box, check both equivalence and exact boundary
   behavior of

   ```text
   (2<=a<=6 and d=1 and c=a+1 and r>=a)
   OR (1<=a<=6 and d=2 and c=a+2 and r>=a and r>=3)
   OR (2<=a<=6 and d=3 and c=a+3 and r>=a and r>=5).
   ```

   Verify nonnegative `R` really makes each accepted set a closed tail and
   that the compressed formula equals all and only the sixteen expanded rays
   *inside the registered domain*.
5. **Exact 16/8 classification.** Independently reproduce these twelve
   baselines

   ```text
   A2D1 A3D1 A4D1 A5D1 A6D1
   A2D2 A3D2 A4D2 A5D2 A6D2
   A5D3 A6D3
   ```

   and four raised subtails

   ```text
   A1D2_R3 A2D3_R5 A3D3_R5 A4D3_R5.
   ```

   Check their predecessor failures, complete primitive inventories, all
   active affine facets, V47 manifest/inventory equality, endpoint/hash
   attachments, and the 71 total inventory rows.  Separately reproduce all
   eight negative reason sets and give one exact primitive witness for each:

   ```text
   A1D2_R2_RA2 A2D3_R3_RA2 A2D3_R4_RA2 A3D3_R4_RA2
   A4D3_R4_RA2 E_A1D3_R20_A3 A7D1_K6_TIE A9D3_K2_TARGET.
   ```
6. **Independent miner comparison.** Re-run the pinned D23 support miner and
   its `+1` pad comparison on all 24 cells.  Confirm it is genuinely
   independently imported after compilation, identify its different
   semantics, and check that 24/24 agreement is not tautological or obtained
   by comparing shared serialized expected values.
7. **Live mutations.** Execute and inspect every one of the eight
   reason-specific negative mutations.  For each, verify it modifies an
   operative mathematical predicate, disables exactly the observed reason
   set, and changes rejection to acceptance.  Add hostile boundary mutations
   for `<` versus `<=`, floor-rounding, deletion of a non-target inequality,
   cancellation failure, `R=0` mishandling, and target-wall removal.  A
   string-only or expected-output-only mutation is not evidence.
8. **Replay and implementation.** Replay with
   `PYTHONDONTWRITEBYTECODE=1 python3 -B` and no output write.  Audit exact
   rational arithmetic, tuple/dictionary aliasing, enumeration caps,
   canonical serialization, digest construction, monkeypatch leakage,
   hard-coded IDs, and whether the equality to `COMPILED.json` can mask an
   independently wrong compiler.
9. **Scope firewall.** Decide only whether V48 is an audited Presburger
   restatement of the already reviewed V47/V47R1 predicate on the explicitly
   registered sixteen rays plus eight controls.  It must claim no strict-fan
   coverage, no exhaustiveness outside the registered box, no new endpoint,
   and no stronger endpoint theorem.  It must not imply `rho=0`, equality
   faces, positive-order loads, staged Rees charts, terminal receivers,
   Gate T, order two, maximum twelve, G2-PSC, G2-BD, JC2, or a counterexample.

Return separate `CONFIRMED`, `REFUTED`, or `GAP/REPAIR` verdicts for custody,
primitive recomputation/cancellations, grade-cap completeness, inequality
completeness, Presburger solving, exact 16/8 classification, miner
independence, all eight mutations, implementation/replay, and no-cover scope.
Give the smallest failing identity, missing inequality, boundary cell, or
hypothesis and the minimal additive repair if anything fails.  State exactly
whether V48 may be integrated as a bounded chamber compiler, while preserving
V47/V47R1 as the mathematical endpoint authority.

Desk-scale hashes and exact Python only.  Write the complete review to exactly

`xmodel/max12-812-order2-gate-t-strict-uac-presburger-chamber-compiler-v48-hostile-review-grok-20260827.md`

and touch no other campaign artifact.
