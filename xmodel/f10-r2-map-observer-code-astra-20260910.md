# r2 diagnostic map-observer dispatcher: static code preparation

STATIC ONLY / DISABLED / UNEXECUTED. First action 2026-09-10 14:29:27 UTC; fixed reserve14:43 and hard stop14:46. Own targets absent and all seven input hashes matched before body reads. No worker, registration, reproduction or execution authority. This owned derivative runs only two authority refusals prospectively and preserves strict map-failure evidence.

## Delivered implementation

Owned dispatch.py is a plain copy-derived replacement, not an importing subclass. The byte-identical probe.py is retained. dispatch.diff gives the complete literal delta. CONTRACT.md specifies exact roles, vectors, observation wire, failure/custody semantics and the prospective review plan. DISABLED-REGISTRATION.json has enabledfalse and null worker/path/pin/command/deadline fields; it cannot dispatch. PINS.json records exactly seven source inputs. All source is unexecuted, including syntax/import/AST/compile/test; no fixture or actual coefficient was constructed/read.

The distinct observer outer job/schema admit exactly refuse-status then refuse-caps. Both are existing cheap negative authority probes, not scientific entrypoints. Each is5 wall/3 softCPU/2GiB; combined10 child-wall/8 hard-childCPU. Original600wall/500CPU/2GiB/16MiB ceiling, sampled480 cutoff,15-second admission/return margin and absolute ROOT deadlines remain. No positive, startup, dummy, mutator, check6/check10 or other preflight can be selected. The five original science hashes and unchanged CAPRUN hash remain literal; wrapper shrinks to exactly dispatcher/probe, with no baseline or mutator role.

## Narrow diagnostic changes

identity records start/finish timestamps around its existing non-atomic sequence; it does not claim a coherent exec image. check_maps keeps the first canonical-file or alias rejection strict. Before raising it emits one fixed exclusive map-rejection.UNVALIDATED.json with label/phase/role, root/native pins, timestamps, whole available identity/argv/mapped-path snapshot, and the first rejected raw/resolved path plus membership/alias booleans. The wire explicitly says validation_passedfalse and non_atomic_proc_readstrue. Missing identity is a typed STOP with null snapshot/path, not a skipped map test. No pathname is presumed and no allowlist changes.

The record is capped65536 bytes and admitted against the existing1MiB metadata reserve; all observer outputs are metadata within the16MiB mount. Oversize, missing-file/write/resolve failures remain STOP rather than silently omitting fields. File and parent fsync precede the original rejection. Existing terminal quiet/hash/copy/fsync custody includes this fixed path automatically whenever cleanup and storage permit.

The initial revalidation moves from the tail of construction to the start of execute, still before all children. This lets main retain the initialized Batch and perform durable custody if initial self-map validation rejects. At child observation the old broad FileNotFoundError handler is narrowed to the children-list read so diagnostic write failures cannot be swallowed. Actual runner equality and actual probe live capture are now required for completion; missing a fast probe yields STOP/no retry. Those small stricter changes are explicit FIRST-review deltas, not undocumented hardening.

Semantic receipt/parser/traceback/fixture methods and unused scientific dispatch paths are removed. Existing physical identity, native six-field closure, pins/inventories, literal full argv and prelaunch authority hash, privilege drop, CAPRUN groups, quiet-before-result reads, cleanup and durable EBS copy remain. The existing polling delay is unchanged, not a new wait-for-favorable-map mechanism. No temporary output deletion, unmount, installation or worker action is implemented.

## Outcomes and limitations

Only two exact refusal exits with full required identity/telemetry checks can yield DIAGNOSTIC_NO_MAP_FAILURE_OBSERVED, scienceNONE. That means neither a repaired closure nor permission to retry semantic work. Any map rejection, cap/mismatch, unavailable live identity or incomplete cleanup is STOP. Before Batch initialization establishes custody paths, ROOT capture may still be needed. Record-size, filesystem and cleanup errors can prevent detailed evidence; they remain explicit STOP/STOP_CUSTODY_INCOMPLETE. The observer does not promise guaranteed real-time cleanup, a complete dynamic native closure or a recoverable snapshot after every OS error.

The original missing path remains unknown. Distinguishing omitted transient/stable mappings from mixed /proc observations requires prospective evidence. A guessed loader pathname is not pinned by this package. No current runtime, preflight success, mathematical or scientific outcome is claimed.

## Validation and custody

Manual whole-source review traced two-label reachability, unchanged negative authority shapes, exact startup vectors, rejection-before-runner-live preservation, observation caps, cleanup propagation and terminal status. One FIRST different-model source/instrument gate is required. The cheapest subsequent check is one separately ROOT-registered two-refusal observer run, not a semantic replay or a broader test matrix. Live capture, storage timing and native cause are unmeasured.

Own whole report/code review, raised-OPEN quantity/cheapest-test review and collision check precede the final marker. No new campaign OPEN identifier. All frozen inputs remain unchanged. Terminal publication/custody hashes bind code, diff, contract, disabled template, read scope, source pins and sealed report/transaction. All writers IDLE at handoff; no follow-on authority.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5719`.
- Body SHA-256:
  `4c230631be705b3d422cf3c7bb1bc0e8ac9550f5e14fa1cd5b1ca70a14e20695`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
