# f10-source-cone-r3-runtime-gate-fable5-20260911 — FIRST Fable 5.1 static gate on the fixed-r3 runtime dispatcher, probe, mutator and disabled registration

status: UNSEALED (adapter seals; no Seal or charge_basis authored). STATIC ONLY: manual text and hash reading; zero subprocess, import, AST, syntax, compile, test, CAS or dummy execution; no coefficient, baseline, fixture or candidate body exists or was generated.
opened: 2026-09-11T01:11:09.939Z (true first action: date, absence check of both owned targets, listing of the input directory). Both owned targets were ABSENT at that instant: xmodel/f10-source-cone-r3-runtime-gate-fable5-20260911.md and box/f10-source-cone-r3-runtime-gate-fable5-20260911/ (directory absent).
reserve: ORIGINAL publication reserve 2026-09-11 01:41 UTC / HARD 01:44 UTC, never reset. Runtime 2400 does not extend it.
basis: 0d39df3c9fd69c939a8420c54d03228b9077777d.
writes: apply_patch only; owned targets are exactly this file and box/f10-source-cone-r3-runtime-gate-fable5-20260911/input_custody.md.

## 0. Custody — 16 charged inputs, pinned 01:11:16.808 UTC BEFORE any body read

All 16 sha256sum values at /tmp/jc2-lane.j28L2a/inputs equal the charged list verbatim (16 unique basenames). The full hash table, line and byte counts, read mode and post-read pins are in box/f10-source-cone-r3-runtime-gate-fable5-20260911/input_custody.md. Every file was read FRESH_WHOLE through EOF in this task: dispatch.py in bounded sections 1-215, 216-430, 431-637; check.py in 1-270, 271-540; run_capped.py in 1-240, 241-480, 481-720, 721-960, 961-1198; all others in one read. No read clipped; no earlier CLI WHOLE attestation reused; no linked, provenance, live or uncharged source read (the author's PINS.json, ROOT card, old dispatchers and old reports named in custody.json were NOT read).

Scope imported from the two charged FIRST reports only at stated scope: the r2 runtime gate (558fa2f1…) for the unchanged CAPRUN API/telemetry reading and its ESRCH-race GAP; the r3 code gate (712ac602…) for the accepted static clearance of the five science files and its CONTROLS trace (6) and numeric wall estimate. Neither is transferable r3 runtime authority.

## 1. Verdicts A–H

Line numbers refer to dispatch.py unless named. Every statement is a hand reading of the charged text.

### A. New authority schema, simple subset vs full closure, startup authorization — CONFIRMED (ROOT obligations, no defect)

Schema: `authorize()` 338-351 emits exactly the eight keys job_tag/status/authority/mode/artifact/files/runtime/limits that authority.py:70 demands, with runtime exactly {python_sha256, native_inventory_path, native_inventory_sha256} (authority.py:92), artifact.sha256 = 'UNFORMED' for mode produce and the frozen input hash (or 64 zeros for the absent startup-check path) for mode check (341; authority.py:79-84). Refusal trace through the ACTUAL r3 authority, in its check order (70 keys, 72 status, 75 mode/path, 79-84 artifact, 85 limits/inventory, 88-90 per-file pins in FILES order, 92-95 runtime/interpreter, 96-101 subset): refuse-status → `no ROOT registration` (72-73); refuse-caps '599' → `registered limits/source inventory` (85); refuse-inventory (check.py key removed) → the same message at 85 (set(files) ≠ set(FILES)); refuse-source (produce.py zeroed; authority.py and arithmetic.py hash first) → `source pin: produce.py` (88-90); refuse-hash (64 zeros supplied) → `authority/inventory byte pin` (64-65) before any key check. All five are ValueError, caught by probe.py:32-35 into `ValueError: MESSAGE\n`, exit 1, no sentinel; 472-477 demand exactly those strings, NORMAL_EXIT/1, empty stdout, absent payload. The valid probe returns argv[8] and writes the single POST_AUTHORIZE event (probe.py:39-46; 478-482). Simple subset: 203-210 pin it by SHA, cap it at the same 65536 bytes authority.py:63-64 reads, require keys exactly {job_tag, files} with the r3 tag, a nonempty dict containing the interpreter path, and every entry equal to the FULL closure's pin; it is consumed only by authority.py:96-101 as the child's extra native check and never substitutes for the full closure. Full closure: 196-202 (1 MiB, schema, exactly six fields, python_path == the outer -I -S -B sys.path, 0<files≤3000, 0<directories≤1000) and every revalidate 279-299 (pins and native files 64-hex and hash-equal; exact sorted directory listings with every child listed; aliases resolve to listed targets; absent paths not lexist; sys.path and both ps paths in closure) plus mapped-file closure of dispatcher, runner and science child (305, 419, 433 via 307-313). Startup: 155-247 check euid 0, 0444 canonical registration, schema/enabled, tag/exclusivity, literal cap profiles, four-key environment equal to live os.environ, positive int UID/GID, EC2 DMI vendor and `i-hex` asset tag, hostname, boot_id, PID namespace, canonical owned cgroup with `/proc/self/cgroup` match and memory.max 8589934592, digit-string CPU origin ≤ current cpu.stat usage, exactly six role files with interpreter == realpath(sys.executable) and dispatcher == resolved __file__, distinct root 0755 science/wrapper directories with cwd == wrapper and mutate.py the literal third sibling, literal outer argv against /proc/self/cmdline, CAPRUN literal SHA, science pins by full path, output children fresh/0700 writer, exact tmpfs, code and all static inputs outside the mount (222-223 closes the r2 E note), literal outer stream bindings, absent ext4/xfs durable target with registered st_dev, ordered original deadlines, exact thirteen command keys, literal typed_slot_policy and literal phase_policy. The mutator repeats ROOT/host/boot/namespace/cgroup/UID/GID/source/mutator-pin checks as the science user (mutate.py:65-95). Obligations, not defects: Python ≥3.11 (set_int_max_str_digits), argv[0] equal to the canonical interpreter, dispatcher RLIMIT_CPU hard ≥ 600, CONFIG_PROC_CHILDREN, no 1777 ancestor, escape-free canonical paths, board_asset_tag/sys_vendor readable by the science UID (kernel dmi-id exports both 0444).

### B. Formed-pin dependency, typed slots, phase records — CONFIRMED

Slot logic 386-401: the registered vector must be a list of str; for check-positive/check-negative the flag `--authorization-sha256` and the token AUTHORIZATION_SHA256 must each occur exactly once with the token immediately after the flag, and only then is the token replaced by the freshly emitted authority digest; for mutate the same holds for `--root-registration-sha256`/ROOT_REGISTRATION_SHA256 replaced by the registration SHA; in every other phase both tokens are forbidden as list elements (the loop rebinds `token` inside the matched branch, which does not disturb Python's for-iteration, so both tokens are tested in every phase). 401 then requires the fully built CAPRUN vector to equal the expanded registration byte for byte, so every other argument, including `--input FROZEN/artifact.json` and `--input FROZEN/mixed-row.json` (literal pre-run paths from phase_policy 242-245), is literal. Necessity: only the two checker authorities embed a hash of a not-yet-formed artifact (341); the eleven other authority files are deterministic from registered data (canonical `json.dumps(sort_keys, (',',':'))+'\n'`, 102), so ROOT can and must precompute their digests literally, and refuse-hash supplies 64 zeros (363). Phase record: 513-519 emits positive.json only after the positive run returned through group_quiet (445), revalidate, post-launch pin drift, joint caps (446-448), NORMAL_EXIT/0 (510) and the exact stdout literal (511-512); it binds ROOT SHA, literal phase policy, baseline path/SHA/bytes, science pins, check-positive authority path/SHA and stdout SHA. mutate.py:96-113 requires that file root 0444 at the registered positive_record path with exactly those eight keys, matching root SHA (argv token), policy, source pins, baseline pin/size and authorization binding, then rewrites argv to the nine-item checker contract and calls the ACTUAL `authorize('check')` (116-119), which re-derives the baseline pin from check-positive.json (authority.py:82-84) before any scientific parse. This is a derivative record of ROOT's registered program under the trusted-ROOT model (CONTRACT 17-18), not a child-supplied authority. Non-blocking wart: for label mutate, 362 also emits auth/mutate.json in mode produce naming the already-frozen baseline as 'UNFORMED'; the mutator never reads it, so it is harmless but misleading in the durable inventory. Smallest repair: skip `authorize()` when role == 'mutate' (or point it at the unused payload path).

### C. Exactly thirteen commands, order, bounds — CONFIRMED

LABELS 37-39 are the thirteen names; 237 requires the registration's command keys to equal them; `execute()` 471-545 runs the five refusals in the contract's dict order, valid, startup-produce, startup-check, dummy, produce, check-positive, mutate, check-negative, and 545 requires the run ledger to equal LABELS in order; 354 forbids any second launch of a label. Wrong-tag entries are the actual produce.py/check.py (375) with tag WRONG-JOB (364); both import only `authority` at module level (produce.py:2, check.py:2) and call `authorize` first (produce.py:288, check.py:535), which refuses at authority.py:23-27 with SystemExit `REFUSED: registered CLI contract` before any file, hash or input read; 483-486 demand exit 1, that exact stderr line, empty stdout, no payload. Dummy: probe.py:47-69 fork/pipe control unchanged from r2 (leader exits after handshake, orphan ignores TERM, touches 64 MiB, pauses); CAPRUN samples the exact PGID, TERM 15, 1 s grace, KILL 9, reaps the zombie leader (run_capped.py:860-913, 475-518); 487-502 require RESOURCE_CAP/rss, both signals sent, leader reaped, cleanup complete, empty live list, exactly one MATCH check and one SENT record per stage with the leader's pid/pgid/start identity, the four-event sequence with parent/orphan identity equalities, all pgids equal to the leader and max RSS > 32 MiB. Every run is admitted only if elapsed+wall+15 < 3000, now+wall+15 < mathematical deadline and sampled cgroup CPU < 2050 (357-358, 407), is watched at 50 ms with the same budget and the size budget (438-440), and is SIGTERMed with a 15 s wait on any escape (441-444). No second producer, no baseline copy (freeze is `os.rename`, 560), one mutator, one fresh negative checker process.

### D. Source stdout/exit/traceback integration, aggregate limits — CONFIRMED

Producer literal `FORMED_UNCHECKED_R3_25_SLOTS_NO_SOURCE_OUTCOME\n` (produce.py:296; 505), positive checker literal `CHECKED_R3_25_GRAPH_SLOTS_45_LOW_108_JACOBIAN_NO_SOURCE_OUTCOME\n` (check.py:531,536; 511-512), both NORMAL_EXIT/0 with empty stderr. Negative: 542-544 require NORMAL_EXIT, child_signal None, child_exit_code 1, empty stdout, then `semantic_traceback` 568-583: ≤8192 ASCII bytes, exact header, four ordered frame pairs, each optionally followed by one ≤512-byte caret line matching ` +[~^]*\^[~^]*\n` (Python 3.11-3.13 emit carets only for sub-line spans, e.g. under `check_file(path)`), and the final line exactly `ValueError: CHECK FAILED: all25 literal graph rows T\n` with nothing after it. I re-read the four locations as numbered source text: check.py:540 `main()` in <module>; check.py:536 `print(check_file(path))` in main; check.py:520 `eq(p, rawrow, 'all25 literal graph rows ' + label)` in check_file; check_arithmetic.py:227 `raise ValueError('CHECK FAILED: ' + label)` in require; the statement strings at 572-574 match letter for letter, and the file names are the absolute science paths because the script is launched by absolute path and check_arithmetic is found through sys.path[0]. Any parser refusal, authority ValueError/SystemExit, resource stop or other row label produces a different frame chain or final line and fails closed. The final status 546-550 carries `science_outcome: NONE`; a full success is `RECONSTRUCTION_AND_MIXED_ROW_REJECTION_CHECKED_NO_SOURCE_OUTCOME` and nothing about a source zero, rank or JC2. Limits: TOTAL/PACKET/META = 134217728/125829120/8388608 (41); `size_budget` 321-336 sums the four packet paths (writer and frozen baseline and fixture) against 120 MiB and everything else against 8 MiB, stat-only, refusing symlinks; the mutator's own bound `len(changed)+len(raw) <= 125829120` (mutate.py:184) is the same constant; RLIMIT_FSIZE 120 MiB and core 0 are inherited by every child (409-411); every metadata record is ≤64 KiB on emit and load (69-71, 101-103).

### E. Independent semantic mutation graph.slots[24] += X1^27 — CONFIRMED (one edge obligation)

Selection: names index 24 is 'T' (produce.py:264, check.py:92), the mixed row (z·P1_0 − U·P0_0)(X, zeta), graph weight 27 (produce.py:259, check.py:519); X1 has graph weight 1, so X1^27 has weight 27 and the mutated row still satisfies the envelope at check.py:521; exponent 27 ≤ the graph bound 30. Canonical arithmetic: mutate.py:142-145 parses with duplicate-key and no-number hooks and requires `wire(doc) == raw`, where `wire` is exactly produce.py:291's `json.dumps(sort_keys, separators=(',',':'), ensure_ascii)+'\n'`, so the baseline is proven canonical before any edit and every untouched value re-serializes byte-identically. 150-167 validate only the selected row against the checker's own wire grammar (four exponent strings `0|[1-9][0-9]*` bounded (30,15,10,7), strictly increasing tuples, seven rationals `(?:0|-?[1-9][0-9]{0,4095})/[1-9][0-9]{0,4095}` that round-trip through Fraction reduced, nonzero vectors); 168-182 add 1 to coordinate 0 (the basis element 1 of Q[V]/P7 in the basis 1,V,…,V^6 that both arithmetic.py `field` and check_arithmetic.py `F` use) of the (27,0,0,0) term, re-encode `num/den` reduced and bound-checked, delete the term if the vector vanishes, or insert `[['27','0','0','0'],['1/1','0/1'×6]]` in numeric exponent order when absent. 183-186 require the changed wire to differ and the inverse slot restore to reproduce the WHOLE original bytes; 187-197 write fixture and receipt O_EXCL with fsync, readback-hash both, and the receipt is FORMED_UNCHECKED with checker_run false (dispatch 526-532 demands exactly that). Traceback predicate: all bytes before doc['graph']['slots'][24] are the baseline's, which passed check_file once, so every predicate through check.py:517 passes again; the loop 518-521 passes labels K8…A0_12 unchanged and at 'T' compares the parsed dict to `wanted[24]` recomputed from raw P1[0], P0[0], U and zeta (515-516); the mutated coefficient tuple differs from the baseline's by the nonzero element 1 (or the key set differs), so `eq` raises at line 520 with label T, giving the exact four-frame chain of D with no parser, hash, refusal or resource masquerade. Edge obligation, fail-closed: the checker's aggregate term counter (check.py:73-75) allows exactly 400000 and the producer's wire cap (arithmetic.py:293) allows exactly 400000, so a baseline sitting exactly at the cap would make the inserted-term branch fail at `aggregate term cap` inside `poly` — a different frame chain → STOP_NONDECISION, never a false pass; the mutator only enforces the per-row 20000 cap (150, 171). Smallest repair if ROOT wants it closed: refuse in mutate.py when the whole-document term count equals 400000 and a term is inserted (one count over all wired polynomials).

### F. Quiet-before-read, telemetry barrier, immutable clocks and cumulative accounting — CONFIRMED

`run()` calls group_quiet and revalidate before the authority file is created (355 → 362) and again immediately after CAPRUN returns (445-446) BEFORE telemetry load (449), stream hash/size binding (457-458), post record, and before `execute()` reads stdout/stderr/payload/receipt/fixture (477-535); `freeze` (555) and `durable_copy` (586) repeat it. group_quiet 315-319 tolerates only zombies in the owned cgroup. Telemetry barrier 449-458: schema CAPRUN/v1, argv SHA recomputed with CAPRUN's exact `json.dumps(ensure_ascii=False, separators)+'\n'` (run_capped.py:184-188), argv count, runner exit code == Popen returncode, cwd == science dir, caps == profile, pid == pgid with `boot=<registered>;start_ticks=` prefix (run_capped.py:307-311), leader reaped by identity difference, stdout/stderr path/SHA/bytes against the files. Clocks: `self.begin` is monotonic before any hash (154); the four UTC stops are read once from the SHA-pinned registration (232-236, ordered admission ≤ mathematical < task < worker) and the registration SHA is re-checked at every revalidate (262), so no stop can move; admission is re-checked per phase (357), `budget()` 257-259 enforces elapsed+reserve < 3000, now+reserve < mathematical stop and sampled cgroup CPU − registered origin < 2050 (in-flight phases included via 438), post-return caps ≤ 3000/≤ 2100 (448), custody ≤ task stop/3000/2100 (610-612). Memory: cgroup memory.max 8589934592 at init and every revalidate (174, 268) plus per-child RSS caps; tmpfs ≤ 128 MiB at every revalidate (302-303). Allocation sums recomputed: wall 9×5+900+900+900+30 = 2775; hard child CPU 9×4+3×600+21 = 1857; reserves 225/243 — static sums, unmeasured. CAPRUN is byte-pinned (23, 193) and unchanged; its RLIMIT_CPU and sampled RSS are per-child, and the 2050/2100 aggregate is a sampled dispatcher cutoff, exactly as CONTRACT 149-158 states (the r2 D text discrepancy is resolved in this contract).

### G. Disabled template, ROOT duties, blocking defects — CONFIRMED (no blocking static defect)

DISABLED-REGISTRATION.json has schema `f10-source-cone-r3-runtime/v1` and `enabled: false`, so `Batch.__init__` dies at 159 before any write; its key set covers every field the dispatcher and mutator read (37 keys incl. thirteen command names equal to LABELS, literal typed_slot_policy equal to 238-241, source_limits/limits/profiles/environment equal to 30-40, phase_policy with the literal mutation string and UNREGISTERED placeholders that ROOT must replace by FROZEN/artifact.json, FROZEN/mixed-row.json, the five science pins, LIMITS and AUTHORITY/positive.json exactly as 242-245 and mutate.py:97-101 require). Genuine activation, real instance/boot/namespace/cgroup values, both native inventories, registered command vectors with precomputed authority digests, UID/GID, durable device and the four stops remain ROOT's duty (CONTRACT 3-6, 30-42, 46-53). Blocking static defect: NONE found on a whole hand read of dispatch.py, probe.py and mutate.py against the pinned authority/produce/check text. Non-blocking, with smallest repairs: (1) inherited r2 GAP, `identity()` 148 catches only FileNotFoundError, so a process dying between the `children` read and the /proc opens raises ProcessLookupError into STOP_NONDECISION — a false STOP, never a false positive; repair `except (FileNotFoundError, ProcessLookupError)`; (2) the spurious produce-mode auth/mutate.json of B; (3) the 400000 edge of E. No rerun, cap change or supervisor migration is proposed.

### H. Prospective liveness — GAP (typed liveness, not a defect)

No positive, control, native, startup, cgroup, coefficient, baseline, fixture, timing or JC2 observation exists; the count is thirteen commands proposed, zero run. The per-phase 900 wall / 599 CPU profiles are unmeasured acceptance limits; the charged r3 code gate estimated 10^8–10^10 Fraction operations per science process and judged 600 CPU s more likely to refuse than admit a valid run, and the checker does strictly more work than the producer (independent rebuild, 108-slot readback, both inverse charts, two Laurent back-maps). The mutator's 30/20 profile must cover one json.loads and two canonical json.dumps of a ≤120 MiB wire plus three SHA passes; plausible, unmeasured. Any cap hit is NONDECISION with a stop after ≤900 s of wall and no information beyond "cap hit"; raising caps is not licensed here and is ROOT's registration choice after a first measurement. A future clean batch means only RECONSTRUCTION_AND_MIXED_ROW_REJECTION_CHECKED_NO_SOURCE_OUTCOME.

## 2. Verdict table

| item | verdict |
|---|---|
| A schema, ≤64 KiB subset ⊂ six-field closure, full startup authorization | CONFIRMED; ROOT obligations listed |
| B typed AUTHORIZATION_SHA256 (two checks) / ROOT_REGISTRATION_SHA256 (mutate), all else literal; positive.json derivative record | CONFIRMED; wart: unused produce-mode auth/mutate.json |
| C thirteen commands, nine preflights incl. WRONG-JOB and live TERM/KILL dummy, one producer/positive/mutator/negative | CONFIRMED |
| D stdout/exit/four-frame traceback chain; 120 MiB packets / 8 MiB metadata / 64 KiB records | CONFIRMED |
| E graph.slots[24] += X1^27 exact, canonical, inverse-restored; reaches check.py:520 label T | CONFIRMED; 400000 aggregate edge fails closed |
| F quiet before every terminal read; telemetry barrier; immutable stops; sampled 2050/2100 cumulative CPU | CONFIRMED |
| G disabled template; ROOT duties; blocking defect | NONE blocking; three non-blocking repairs |
| H liveness | GAP: unmeasured; code-gate estimate says 599 CPU s likely refuses |

## 3. OPEN(S) RAISED

No new canonical identifier. QUANTITY: whether the pinned thirteen-command batch, under the fixed 900/599 per-phase caps and the 3000/2100 envelope, reaches the producer literal, the positive literal and the exact label-T traceback on an actual ROOT-registered EC2 host; every observation UNKNOWN. CHEAPEST INSTRUMENT: ROOT's single registered batch as specified, preceded by the nine no-math preflights it already contains; optionally ROOT registers a first producer-only measurement with a generous wall cap before committing the fixed profile. NUMERIC PLANNING WALL (static, UNMEASURED): 2775 s child allocation inside 3000 s; the code gate's per-process estimate (tens of minutes to hours) exceeds 900 s, so the modal outcome of the batch as registered is NONDECISION at produce after ~900 s, not a verdict. This review consumed about fifteen minutes of static reading and writing; no CAS or process was run.

## 4. COLLISIONS

status: EMPTY — own-only inspection: both owned targets absent at 01:11:09.939Z; this lane wrote exactly xmodel/f10-source-cone-r3-runtime-gate-fable5-20260911.md and box/f10-source-cone-r3-runtime-gate-fable5-20260911/input_custody.md by apply_patch; no other file, input, source, CAPRUN, frozen report, protected or shared object touched; no corpus scan.

## 5. Completion attestation

Timeline (UTC, 2026-09-11): 01:11:09.939 first action and target absence; 01:11:16.808 all 16 pre-read pins equal to the charged list; 01:11-01:19 all 16 bodies FRESH_WHOLE to EOF; 01:20:03 skeleton (no marker) by apply_patch; 01:21:07 verdicts A–D; 01:22:18.258 all 16 post-read pins unchanged; 01:23:15 verdicts E–H, table, OPEN, COLLISIONS; 01:23:42 input_custody.md; 01:24:00 own WHOLE read of both owned files; 01:24:23 every 64-hex token in both owned files cross-checked against the pin list and the 16 custody rows re-verified live with sha256sum -c (16 OK); this section and the marker last, inside the original 01:41 reserve, never reset. Tools: date, ls, sha256sum, wc, cat, sed, awk, pwd, grep, comm-free pipelines, apply_patch only; no Write/Edit, no redirection into any owned or other file, no generated text, no artifact transaction, no input edit. Zero mathematical subprocess/import/AST/syntax/compile/test/CAS of any size including any no-math dummy; zero coefficient/candidate/baseline/fixture body; zero network/AWS/SSH/process control/Git/agents/protected/shared/live/provenance/extra sources. Only control 6 (graph.slots[24] += X1^27) is selected for the future negative check; the other five remain NOT_RUN/SPEC. No duplicate positive run, solver, modular-place task, source replay, worker or artifact generation. No Seal and no charge_basis authored; the adapter seals. Final own-file hashes are reported in the terminal response. All writers idle after the marker below, which is the only standalone marker in this file and is followed by nothing.

<!-- BODY-END -->
