# Hostile review: `P4P1-SOURCE-HONESTY`

| Field | Value |
|---|---|
| Claim | `P4P1-SOURCE-HONESTY` (band-42 modular compiler interface only) |
| Verdict | **CONFIRMED** |
| Classification | `ORIGIN-ONLY` (frozen taxonomy; not `SOURCE-ZERO`, not `LOAD-BEARING`) |
| Smallest failing witness | none |
| Evidence tier | integer source identity; actual `rung_kernel` subtraction on empty and all ten real checkpoint rows; exact coefficientwise `I23` normal forms; two-prime origin / off-origin / zero-line arithmetic |
| Reviewer | Grok 4.6 (adversarial different-model verifier) |
| CLI | `grok 1.0.5 (5115b46bc909) [stable]` |
| Python | 3.14.6 (`/opt/homebrew/opt/python@3.14/bin/python3.14`) |
| Host | `dc-mbp-m2.local`, Darwin arm64 |
| UTC | 2026-08-24T03:48:28Z |
| Repo | `/Users/dc/code/math/jc2` |
| Git HEAD | `8bf25a52f5f6c20886b77c6d9aeeb879eb18f8e4` |
| Scratch | `/tmp/p4p1-honesty-grok-USLljF` (fresh `mktemp -d`) |

**Promotion.** The following may be promoted off producer-checked provisional, as modular compiler-interface fact at `p=105337,105673`, fiber `a00pp`, band 42 only:

1. The exact source identity
   \(\Delta[t^{42}]\mathcal E=42 S_M G_M(3\alpha-2\beta)p(\eta)^4 p'(\eta)\).
2. Both registered checkpoint banks omit `Xf_alpha` and `Xg_beta`. The live assembler `d43_nf_certificate.rung_kernel` adjoins precisely that correction. Dictionary subtraction of checkpoint from final row equals the source delta on all ten eta exponents.
3. All twenty sidecar coefficients are already their own exact `I23` normal forms (nonzero constants; zero reduction steps; empty membership traces).
4. The named completion \(\alpha=\beta=0\) is unchanged. The preregistered control \((1,0)\) changes all ten rows. The sidecar zero locus is the line \(3\alpha-2\beta=0\), not a one-point set.

Nothing else may be promoted.

**Quarantine if this review is treated as a failure.** Do not feed `P4P1-SOURCE-HONESTY` into an integral emitter, a general D43 nonemptiness claim, a new depth, a compatible tail, a germ, a characteristic-zero point, or a JC2 inference. Do not upgrade `ORIGIN-ONLY` to `SOURCE-ZERO`. Do not treat the line \(3\alpha-2\beta=0\) as a licence that every prior D43 graph witness is origin-safe. Graph reconstruction witnesses at nonzero \(\alpha\) are outside this gate.

**Dirty-state perimeter.** HEAD matches the preregistered basis `8bf25a52f5f6c20886b77c6d9aeeb879eb18f8e4`. Tracked dirt is `APPROACHES.md`, `AUDIT.md`, `COORDINATION.md`, `PROGRESS.md`, `notes.md`, `ops/FLEET.md`, `ops/status.sh`, and `pilot-local.log`; none is an input of this gate. The case directory and producer report are untracked post-basis artifacts. Frozen inputs were rehashed here and match `provenance.json` and `MANIFEST.sha256` byte-for-byte.

Reviewer discipline: producer, replay, assembler, and reducer read in full before execution; scratch output only under `/tmp/p4p1-honesty-grok-USLljF`; no network; no solver or remote/AWS job; no shared-ledger edit; no repository write except this file. Stored `PASS` strings, producer `verdict`, and replay `classification` are not authority. Verdicts rest on independently recomputed polynomials, execution of the live `rung_kernel` path, exact `nf_trace` plus an independent “LT does not divide 1” check, and substitution of the linear form at origin, \((1,0)\), and two points of \(3\alpha-2\beta=0\).

---

## Claim under review (not enlarged)

1. Preregistration precedes execution, the frozen verdict taxonomy is the one applied, provenance hashes and the case manifest match disk, and the exact replay commands regenerate the banked JSON byte-for-byte.
2. Independently,
   \[
   \Delta[t^{42}]\mathcal E
   =42 S_M G_M(3\alpha-2\beta)p(\eta)^4 p'(\eta),
   \]
   with the ten integer coefficients of \(p^4 p'\) at
   \(h=2,5,8,11,14,17,20,23,26,29\).
3. The actual `rung_kernel` assembler, on empty input and on the real band-42 checkpoints, adjoins exactly that correction: dictionary subtraction of checkpoint from final row equals the source delta, and every pre-existing checkpoint group is unchanged.
4. Both checkpoint banks omit the sidecar names. Every coefficient’s exact normal form through each registered 509-element `I23` grevlex basis is the nonzero constant itself.
5. The named origin \((\alpha,\beta)=(0,0)\) is unchanged; the control \((1,0)\) changes all ten rows; the zero locus is the line \(3\alpha-2\beta=0\) at both primes.
6. Prior modular *origin* D43 certificates used \(\alpha=\beta=0\). The defect closed here is an overbroad source-provenance slogan, not a wrong source formula.
7. No integral emitter, general D43 nonemptiness, depth, compatible tail, germ, characteristic-zero, or JC2 inference is consumed or emitted.

---

## Verdict table

| Clause | Verdict | What would have flipped it |
|---|---|---|
| Preregistration chronology, frozen taxonomy, provenance/manifest hashes, byte-replay | **CONFIRMED** | results before prereg; silent `ORIGIN-ONLY` after a live `SOURCE-ZERO`; hash mismatch; scratch JSON differs |
| Integer identity and ten coefficients of \(p^4 p'\) | **CONFIRMED** | eta-table ≠ z-table ≠ `d43_family2.P4P1`; four-term ≠ factored; extra eta support; \(126\alpha-84\beta\) fails |
| Actual `rung_kernel` empty and real subtraction, all ten rows | **CONFIRMED** | empty delta ≠ source scalars; any real row whose `final-checkpoint` is not exactly the two sidecar constants; old groups mutated |
| Sidecar omitted from both banks; exact `I23` NF of all twenty constants | **CONFIRMED** | a sidecar name in any deep tuple; a degree-0 GB lead; `nf_trace` step > 0; remainder 0 |
| Origin \((0,0)\), control \((1,0)\), line \(3\alpha-2\beta=0\) at both primes | **CONFIRMED** | a nonzero origin row; a vanishing \((1,0)\) row; a point of the line with nonzero correction; \(42 S_M G_M p^4 p'\) zero at a registered \(h\) |
| Prior origin claims used zero sidecar; defect is slogan not formula | **CONFIRMED** | a banked origin/full certificate with \(\alpha\neq 0\) or \(\beta\neq 0\) used as the named completion; a wrong Euler identity |
| Scope: no integral / D43 nonemptiness / depth / tail / germ / char-0 / JC2 | **HONEST** | hidden promotion past the compiler interface |

---

## What was rerun, independently, and not

```bash
SCRATCH=$(mktemp -d /tmp/p4p1-honesty-grok-XXXXXX)
# SCRATCH=/tmp/p4p1-honesty-grok-USLljF

/opt/homebrew/opt/python@3.14/bin/python3.14 \
  cases/round2_p4p1_honesty/p4p1_honesty.py \
  --output "$SCRATCH/results.json" \
  --provenance-output "$SCRATCH/provenance.json"

/opt/homebrew/opt/python@3.14/bin/python3.14 \
  cases/round2_p4p1_honesty/replay.py \
  --input cases/round2_p4p1_honesty/results.json \
  --output "$SCRATCH/replay.json"
```

Scratch producer JSON is **byte-identical** to `cases/round2_p4p1_honesty/results.json` (`cmp` silent; SHA-256 `d0123adb31a1ee7aead8c7b5b0899fc68094a87700bbdff69867514f2efd002c`). Scratch replay JSON is **byte-identical** to `cases/round2_p4p1_honesty/replay.json` (`cmp` silent; SHA-256 `c7a48727b6f5e21966258210b56bd0a4e066657d40ae0821b99d6a4b81840e27`). Scratch provenance JSON is byte-identical to the banked file. Not stale.

Independent work, **not** `p4p1_honesty.build` / stored `PASS` strings as authority, and **not** the registered `replay.py` token grep as a substitute for executing the assembler:

- Rehashed all 8 `MANIFEST.sha256` paths and all 10 `provenance.json` sources against disk: **0 mismatches**. Recomputed producer `canonical_claim_sha256` from the JSON object: match.
- Recomputed \(p(\eta)^4 p'(\eta)\) over \(\mathbb Z\) from \(p=\eta^6-6\eta^3+6\) and independently from \(z=\eta^3\), \(p=z^2-6z+6\), \(p'=6\eta^2(z-3)\). Support is exactly the ten registered \(h\).
- Re-derived the \(t^{42}\) multipliers two ways (four-term Euler extraction and the factored product identity) without importing the producer.
- Loaded both band-42 checkpoint pickles. Complete monomial census of sidecar names. Executed the live `d43_nf_certificate.rung_kernel` on empty input and on all ten real rows at both primes. Dictionary-subtracted.
- Parsed both 509-element grevlex bases from the `.out.txt` headers and via `d25_reduce.parse_msolve_gb`. Ran `nf_trace` on all twenty constants. Independently checked that no leading monomial has degree 0, so none divides \(1\). Reduced \(W_1 uW_1\) as a negative control that the reducer is not a no-op.
- Substituted the linear form at \((0,0)\), \((1,0)\), \((2,3)\), and \((1,3\cdot 2^{-1})\).

Not done, and not required by the scoped claim:

- No jet rebuild of \(\Phi_0=S_M p^2\), \(\Gamma_0=G_M p^3\) from a radical chart (the written Euler expression and the already-reviewed D-state row-42 identity are the source; this gate is the compiler interface).
- No `eplus43` run, no D43 point replay, no integral emitter, no msolve, no new depth.

---

## Hashes

| object | SHA-256 |
|---|---|
| `cases/round2_p4p1_honesty/PREREGISTRATION.md` | `ea4593958acfddb66366a32d45693329052f24ae99d71cc26d43ad3fcf68f4e6` |
| `cases/round2_p4p1_honesty/provenance.json` | `87d0e620f5c4275f9c6f5b8f5d559601066d8520e99d3e4bd6a0d804dbed50b5` |
| `cases/round2_p4p1_honesty/p4p1_honesty.py` | `c8d0c688f0bbe7ae1f47b6777c93db914df7835dd6c1eb6ee2c131662a49fb1f` |
| `cases/round2_p4p1_honesty/results.json` (banked and scratch rerun) | `d0123adb31a1ee7aead8c7b5b0899fc68094a87700bbdff69867514f2efd002c` |
| `cases/round2_p4p1_honesty/replay.py` | `45af02c3d76b960c8cf71c8864d4c133b17304b4a5686785d1d41d7c1378df57` |
| `cases/round2_p4p1_honesty/replay.json` (banked and scratch rerun) | `c7a48727b6f5e21966258210b56bd0a4e066657d40ae0821b99d6a4b81840e27` |
| `cases/round2_p4p1_honesty/verify.py` | `86b8ac931ae2f963b4659344a95f6cf5ca2f24907cb58612ed3510fc95d9e2ba` |
| `xmodel/round2-p4p1-honesty-20260824.md` | `c744c983c41055f45a509b0764b60d07312d5b172ef162183fa4342b7be1042f` |
| producer `canonical_claim_sha256` | `f147c46d55f46fd9eb009023a8beb104a93327612815c9f59d008e005d692334` |
| independent-core SHA-256 | `9245288b87d8bebd80af19de80e5798eb78716fa720dfbca12fbe10b26555b4d` |
| scratch `independent_audit.json` | `642cb9d120451b48aaf65c480064299afdba6f57ac6cec994ce4d1331eac6491` |

Provenance sources, all matching disk:

| source | SHA-256 |
|---|---|
| `cases/d25_reduce.py` | `443d8122f06d101f93d02bc29554893065137aad80a2f0daf5e5bfb8092836ae` |
| `cases/d43_family2.py` | `8c0f7fd3ea1efb54293036e1caa481519682251ec554e8dac4cb254b4f2ca013` |
| `cases/d43_nf_certificate.py` | `41865448d3de9d0d47a9c0d086a343ea7a46b00688699d2e8b304adb0be5945b` |
| `cases/eplus43.py` | `94db6c25df5254a040522b7bd5afe38f19577c267c8e61f57b05d3b0dfd3fe1c` |
| `cases/d43red/d43red_p105337_a00pp_band42.pkl` | `6160fd650ac3076c8063295c905a8e53b5ce6cde82ef9608e2a910abd1fec0ce` |
| `cases/d43red/d43red_p105673_a00pp_band42.pkl` | `e6420d4eba9a49c8a0fd082466031468bce1930fbd3713ee0f60fbaa10bd1a85` |
| `cases/directionb_det23_gb_p105337.out.txt` | `3ba965d8d57439fce5e6febd6a3f7a3ab3bcad74337768eec18681bf0e2a2acc` |
| `cases/directionb_det23_gb_p105673.out.txt` | `cda3a0d61c6ab8973f3d6f4ec6b45abba0007514789323445200a948738e72de` |
| `xmodel/review-d43-nf-fid-grok.md` | `85501e3c0f8b05b0a415b008d8041061d45b8517967c2ea0df76d236fca12f05` |
| `xmodel/ideation-20260824T0156Z-synthesis.md` | `9c0455ddae2388d7f5256ec46094c0d97dd5bc6748f2b61177f736cc23581f50` |

GB hashes match the earlier `D43-NF-FID` review. Checkpoint pickles are 380 734 282 and 380 792 415 bytes.

---

## 1. Preregistration, taxonomy, provenance, replay

Claimed freeze: `2026-08-24T03:11:17Z`, before executing the gate. File mtimes (UTC) are consistent with that claim and with the written commands:

| mtime (UTC) | path |
|---|---|
| 03:11:48 | `PREREGISTRATION.md` |
| 03:15:46 | `p4p1_honesty.py` |
| 03:16:04 | `provenance.json`, `results.json` |
| 03:17:59 | `replay.py` |
| 03:18:12 | `replay.json` |
| 03:19:00 | `verify.py` |
| 03:20:02 | `xmodel/round2-p4p1-honesty-20260824.md` |
| 03:20:26 | `MANIFEST.sha256` |

The 31-second gap between the frozen timestamp and the preregistration mtime is the write of the frozen text, not a post-hoc backdate of a result. Results, replay, report, and manifest all come after the freeze file. `BASE_COMMIT` in provenance is the live HEAD.

Frozen verdicts, in the required order: `SOURCE-ZERO`, `ORIGIN-ONLY`, `LOAD-BEARING`, `INCONCLUSIVE`. The producer hardcodes `"classification": "ORIGIN-ONLY"` and `assert`s the conditions of that class. It cannot emit `SOURCE-ZERO` or `LOAD-BEARING` as a string: a zero remainder or a changed origin would `AssertionError`. That is a completeness hole in the driver, not a live misclassification. Independently: all twenty remainders are nonzero, so not `SOURCE-ZERO`; origin substitution is the zero tuple, so not `LOAD-BEARING`; assembler matches source, so not `INCONCLUSIVE`. The live class is `ORIGIN-ONLY`. The preregistration already states that this label means *harmless only for the named origin completion* and does not claim a one-point zero locus.

`replay.py` imports neither `p4p1_honesty.py` nor `d43_nf_certificate`. It recomputes \(p^4 p'\) via \(z=\eta^3\), uses the factored Euler identity, reparses both checkpoints and both GB files, and token-greps the two assembly sources. It does **not** execute `rung_kernel`. That is a weaker independent path than the producer’s assembler clause. This review executed the live assembler; the registered replay remains a useful non-import check of table, hashes, and GB headers.

---

## 2. Integer source identity

Written Euler operator, matching `valuation_e2.euler_rows` / `eplus43._bexpr`:

\[
\mathcal E=(\theta\Phi-12\Phi)\Gamma_\eta-\Phi_\eta(\theta\Gamma-18\Gamma)+42 t^{20}.
\]

\(\theta\) on slot \(s\) is the multiplier \(s\), implemented as `theta_shift(J, off)` with slot multiplier \(s-\mathrm{off}\). Gauge \(+42 t^{20}\) does not meet degree 42. Put \(\Phi=U_f\Phi_0\), \(\Gamma=U_g\Gamma_0\), \(\Phi_0=S_M p^2\), \(\Gamma_0=G_M p^3\), \(U_f=1+\alpha t^{42}\), \(U_g=1+\beta t^{42}\), \(p=p(\eta)\). Then \(\Phi_0,\Gamma_0\) are \(t\)-independent, \(\Gamma_{0,\eta}=3 G_M p^2 p'\), \(\Phi_{0,\eta}=2 S_M p p'\), and \(\theta U_f=42\alpha t^{42}\).

Four-term extraction at \(t^{42}\), in units of \(S_M G_M p^4 p'\):

| piece | multiplier |
|---|---:|
| \((\theta\Phi-12\Phi)_{42}\cdot(\Gamma_\eta)_0\) | \(30\cdot 3=90\) \(\alpha\) |
| \(-\Phi_{\eta,42}\cdot(\theta\Gamma-18\Gamma)_0\) | \(-2\cdot(-18)=36\) \(\alpha\) |
| \((\theta\Phi-12\Phi)_0\cdot(\Gamma_\eta)_{42}\) | \((-12)\cdot 3=-36\) \(\beta\) |
| \(-\Phi_{\eta,0}\cdot(\theta\Gamma-18\Gamma)_{42}\) | \(-2\cdot 24=-48\) \(\beta\) |

Total \(126\alpha-84\beta=42(3\alpha-2\beta)\).

Factored product identity, matching `eplus43.identity_32_path`:

\[
\mathcal B(U_f\Phi,U_g\Gamma)=U_f U_g\mathcal B(\Phi,\Gamma)
+U_g(\theta U_f)\Phi\Gamma_\eta
-U_f(\theta U_g)\Phi_\eta\Gamma.
\]

Degree-zero bilinear: \((-12)\cdot 3-2\cdot(-18)=0\). Remaining terms at degree 42: \(42\alpha\cdot 3-42\beta\cdot 2=126\alpha-84\beta\). The two derivations agree before any modular specialisation.

Integer polynomial, two independent multiplications, not imported from `P4P1`:

```text
h       2       5        8       11       14      17      20    23   26  29
P4P1 -23328  101088  -186624  191808  -120528  47952  -12096  1872 -162   6
```

The eta-basis computation (`p=[6,0,0,-6,0,0,1]`, four self-products, times \(p'=[0,0,-18,0,0,6]\)) equals the z-basis computation (`q=[6,-6,1]\), \(q^4\cdot(-18+6z)\), then shift by \(\eta^2\)). Support is exactly those ten exponents; there is no extra eta power. The literal `P4P1` in `d43_family2.py` (the table `rung_kernel` actually reads) equals this recompute. `S_M=7^{12}2^{-6}\), \(G_M=-7^{18}2^{-9}\).

This is the same displayed row-42 identity already confirmed over \(\mathbb Q\) in the D-state review. This gate does not depend on that review’s matrices; it re-derives the coefficient identity from the written operator.

---

## 3. Actual assembler: empty and real subtraction

Live code, `cases/d43_nf_certificate.py` `rung_kernel`:

```python
if k == 42 and h in F.P4P1:
    base = 42 * SM * GM % p * (F.P4P1[h] % p) % p
    groups[("Xf_alpha",)] = {zero_pk: 3 * base % p}
    groups[("Xg_beta",)] = {zero_pk: -2 * base % p}
```

This **assigns** the two sidecar groups. `d43_family2.analyze` **adds** into `poly[za]`. On the registered banks they coincide, because the sidecar keys are absent (section 4). An injected preexisting `("Xf_alpha",)` term is wiped by `rung_kernel` and combined by `family2`; that divergence is **not live** on either registered checkpoint.

Empty input, both primes: `rung_kernel({}, 42, p)` returns the ten HS rows, each exactly

```text
{("Xf_alpha",): {0: 3*base}, ("Xg_beta",): {0: -2*base}}
```

with `base = 42 S_M G_M P4P1[h]`, matching the independently computed modular scalars coefficient-for-coefficient.

Real checkpoints, **all ten rows in one call**, both primes: dictionary subtraction `final − checkpoint` equals that same two-key delta; every pre-existing checkpoint group is byte-equal in the final row; an independent ADD reconstruction equals the ASSIGN final row. The producer only compared `h=29`. The other nine rows were a completeness hole in the producer driver and are now closed.

`h=29` remainders, matching the producer report:

| prime | \(\alpha\) | \(\beta\) |
|---:|---:|---:|
| 105337 | 96138 | 41245 |
| 105673 | 93734 | 78408 |

Packed constant monomial is integer `0`; `unpack` is twenty-two zeros; `GBVARS` is the 22-name tuple in the checkpoint payload and in both GB headers.

---

## 4. Sidecar omission and exact `I23` normal forms

Both banks: `band=42`, `fiber=a00pp`, prime match, `gbvars` = `GBVARS`, row keys exactly `{(h,42): h in HS}`. Sidecar-name census over every deep tuple: **0 hits**, including mixed tuples that would contain a sidecar together with another name.

| prime | deep groups | NF terms | sidecar hits | GB size | min LT deg |
|---:|---:|---:|---:|---:|---:|
| 105337 | 131679 | 15 695 212 | 0 | 509 | 1 |
| 105673 | 131679 | 15 695 214 | 0 | 509 | 1 |

Both GB files: characteristic matches the prime; variable order is exactly `GBVARS`; monomial order is graded reverse lexicographical; 509 elements; every printed lead is monic `1*`; every lead involves a `GBVARS` name (no constant polynomial). Parsed LT degrees run from 1 to 16. In particular the files contain `W1*uW1 + (p-1)` and `W2*uW2 + (p-1)`, i.e. \(W_i uW_i-1\), whose leads have degree 2.

Ring-extension / ordering attacks that were actually tested:

| risk | result |
|---|---|
| Sidecar names among the 22 base variables | Rejected: `Xf_alpha`,`Xg_beta` ∉ `GBVARS` |
| Wrong `GBVARS` order vs GB header | Rejected: header order = payload `gbvars` = `DR.GBVARS` |
| `pack([0]*22)` not the constant | Rejected: packed value `0`, unpack all zeros |
| Coefficient is not the constant monomial | Assembler writes `{0: coeff}` only |
| A degree-0 lead divides `1` | No such lead; min LT degree 1 |
| `nf_trace` is a no-op stub | \(W_1 uW_1\) reduces in one step to `{0: 1}` at both primes |
| Constants reduced in an extended 24-variable order | Unnecessary: no base LT divides `1`, and sidecar is an external deep symbol |

Exact `nf_trace({0: coeff}, gb, p)` on all twenty coefficients: `normal == {0: coeff}`, `steps == 0`, empty trace. An independent scan “does any LT have degree 0?” agrees. This is an identity certificate: a positive-degree monomial cannot divide \(1\), so a nonzero field element is already reduced. It is not interpolation and not sampling.

The slogan “constant coefficients cannot reduce” is true **on these bases** because they are proper (no `1` in the GB). It would be false if a GB element were a unit. The \(W_1 uW_1\) negative control shows the same reducer *does* reduce a non-constant.

---

## 5. Origin, off-origin, zero line

The correction is the linear form \(c_h(3\alpha-2\beta)\) with
\(c_h=42 S_M G_M P4P1[h]\neq 0\) at every registered \(h\) and both primes
(\(42,S_M,G_M,P4P1[h]\) all units: \(S_M\in\{51107,21876\}\),
\(G_M\in\{33066,10258\}\); all ten table entries nonzero mod \(p\); \(p>42\);
\(2,3\nmid p\)). Therefore its zero locus is exactly the line \(3\alpha-2\beta=0\).

| assignment | p=105337 | p=105673 |
|---|---|---|
| \((\alpha,\beta)=(0,0)\) | all ten rows 0 | all ten rows 0 |
| \((1,0)\) | all ten \(\alpha\)-coeffs nonzero | all ten \(\alpha\)-coeffs nonzero |
| \((2,3)\) | all ten 0 | all ten 0 |
| \((1, 3\cdot 2^{-1})\) | \(\beta=52670\), all ten 0 | \(\beta=52838\), all ten 0 |

The producer’s origin control is algebraically tautological (`origin = {h: 0}`). That is a driver weakness, not a false claim: there is no constant term, so the origin vanishing is the linear form. The off-origin control is a genuine check that every \(\alpha\)-coefficient is nonzero. This review substituted the form rather than accepting the stored booleans, and added two points of the asserted line.

\(\alpha/\beta \equiv -3/2\) on every row at both primes.

---

## 6. Prior origin / D43 claims, slogan versus mathematics

Named origin / full certificates, sidecar values as stored:

| artifact | \(\alpha\) | \(\beta\) |
|---|---:|---:|
| `d43_full_certificate_p105337.json` `full_184` | 0 | 0 |
| `d43_full_certificate_p105673.json` `full_184` | 0 | 0 |
| `d43_full_floor_p105337.json` | 0 | 0 |
| `d43_full_floor_p105673.json` | 0 | 0 |

No banked modular *origin* certificate used a nonzero sidecar. Adding the exact correction does not change those origin rows. The earlier modular origin vanishing is not invalidated by wrong source mathematics.

Separately, the graph-reconstruction witnesses are **not** origin completions:

| artifact | \(\alpha\) | \(\beta\) | \(3\alpha-2\beta\) |
|---|---:|---:|---:|
| `d43_graph_witness_p105337.json` | 93268 | 0 | 69130 ≠ 0 |
| `d43_graph_witness_p105673.json` | 98561 | 0 | 84337 ≠ 0 |

Those points already advertise a negative control `Xf_alpha += 1` breaking ten graph rows. They used the assembler, off the sidecar zero line. This gate does not certify them, does not replay their vanishing, and does not enlarge to D43 nonemptiness. It does record that “ORIGIN-ONLY” is about the named \(\alpha=\beta=0\) completion, not about every stored D43 JSON.

There is no wrong band-42 source formula. The old checkpoint-to-final gap was provenance and scope debt: the 184 source-to-NF traces cover checkpoint NFs, not the assembled sidecar. That is exactly the gap flagged in `xmodel/review-d43-nf-fid-grok.md`. This audit closes it.

---

## 7. Boundary (must not be enlarged)

Checkpoints contain only `band, fiber, gbvars, prime, rows`. The identity stops at the 509-element D23 basis over \(\mathbb F_p\), extended by independent sidecar symbols with no new relations. None of the following is certified by this claim or this review:

- an integral or \(p\)-adic emitter
- general D43 nonemptiness, a compatible tail, a new depth
- a formal germ, inverse limit, characteristic-zero map, or counterexample
- a JC2 inference
- vanishing of graph-reconstruction witnesses at nonzero \(\alpha\)

`replay.py`’s `nonclaim` and the producer `nonclaims` list match this boundary. The producer report does not promote past it. `AUDIT.md` already contains a producer-written section; this review does not edit that ledger.

---

## Clean wording that may enter `AUDIT.md`

Exact replacement for the compiler-honesty clause (and no more):

> The band-42 checkpoint rows at the registered `a00pp` / `I23` cells were source-to-NF traced modulo `I23`. They contain neither `Xf_alpha` nor `Xg_beta`. Final assembly by `rung_kernel` adjoins the exact source identity \(\Delta[t^{42}]\mathcal E=42 S_M G_M(3\alpha-2\beta)p(\eta)^4 p'(\eta)\). Dictionary subtraction of checkpoint from final row equals that correction on all ten eta exponents. The twenty coefficients are nonzero constants, already in exact `I23` normal form (empty traces). The named origin completion \(\alpha=\beta=0\) is unchanged; the sidecar zero locus is the line \(3\alpha-2\beta=0\). This is `ORIGIN-ONLY` compiler honesty, not `SOURCE-ZERO`, and not a D43, integral, germ, characteristic-zero, or JC2 theorem. Graph reconstruction JSONs with nonzero \(\alpha\) are outside this statement.

Do **not** enter: an integral re-emission licence, a claim that the correction’s zero locus is only the origin, a claim that graph witnesses are origin-safe, or a claim that checkpoint traces cover assembled sidecar terms without the displayed correction.

---

## Adversarial attack log

| # | Attack | Result |
|---|---|---|
| A1 | Results written before preregistration | **Miss.** Freeze `03:11:17Z`; prereg file `03:11:48Z`; results `03:16:04Z`; manifest last. |
| A2 | Manifest / provenance hash mismatch | **Miss.** 8/8 manifest paths and 10/10 provenance sources match disk. Scratch rerun byte-identical. Canonical claim hash recomputed. |
| A3 | `replay.py` imports the producer or the assembler and is circular | **Miss** on import. **Partial** on strength: replay token-greps assembly and does not execute `rung_kernel`. Independent execution closes the assembler clause. |
| A4 | Producer hardcodes `ORIGIN-ONLY` and cannot emit `SOURCE-ZERO` | **Fired as driver hole, not live.** Remainders independently nonzero; origin independently zero. A zero-remainder world would crash rather than report `SOURCE-ZERO`. |
| A5 | \(p^4 p'\) imported from `P4P1` rather than recomputed | **Miss.** Eta-basis, z-basis, and the `d43_family2` literal agree. Support exactly HS. |
| A6 | Four-term ≠ factored, or \(\theta\) off-by-one (\(42\) vs \(42-12\)) | **Miss.** Both paths give \(126\alpha-84\beta\). `theta_shift` uses slot multiplier \(s-\mathrm{off}\). |
| A7 | Gauge \(42 t^{20}\) contaminates degree 42 | **Miss.** |
| A8 | Empty `rung_kernel` delta ≠ source scalars | **Miss**, all ten rows, both primes. |
| A9 | Real subtraction only tested at `h=29`; other rows differ | **Fired on producer, closed here.** All ten real rows, both primes: exact delta, old groups unchanged. |
| A10 | `rung_kernel` ASSIGN vs `family2` ADD | **Fired only on injected sidecar.** Not live: sidecar keys absent from both banks; ADD equals ASSIGN on the registered rows. |
| A11 | Sidecar hidden in a mixed deep tuple or non-constant base monomial | **Miss.** 0 sidecar hits; assembler writes packed `0` only. |
| A12 | GB not 509 / not grevlex / wrong characteristic / wrong variable order | **Miss.** Headers and `parse_msolve_gb` agree; order = `GBVARS`. |
| A13 | A constant lead divides `1`, so “constants cannot reduce” is false | **Miss.** Min LT degree 1. |
| A14 | `nf_trace` is a stub that always returns the input | **Miss.** \(W_1 uW_1\mapsto 1\) in one step at both primes. All twenty sidecar NFs have 0 steps. |
| A15 | Ring extension: reducing \(c\cdot\mathrm{Xf\_alpha}\) in 24 variables against a 22-variable GB | **Miss** as a live error. Sidecar is an external deep symbol; base monomial is `1`; no base LT divides `1`. |
| A16 | Variable-order mistake changes the NF of a constant | **Miss.** Total-degree of every LT is order-independent; none is 0. |
| A17 | Origin control is hardcoded `0` | **Fired as driver tautology, not false math.** Independent substitution of the linear form; plus two points of the line. |
| A18 | \((1,0)\) does not change every row, or \(3\alpha-2\beta=0\) is not the full zero locus | **Miss.** All ten \(\alpha\)-coeffs nonzero; \(c_h\neq 0\); \((2,3)\) and \((1,3/2)\) vanish. |
| A19 | A prior modular origin/D43 *origin* certificate used nonzero sidecar | **Miss** for origin/full certificates (\(\alpha=\beta=0\)). **Fired** for graph witnesses (\(\alpha\in\{93268,98561\}\), \(\beta=0\), off the line). Those are not the named origin and are out of gate. |
| A20 | Wrong mathematics, not merely an overbroad slogan | **Miss.** Identity, assembler, and NFs hold. The closed defect is “checkpoint traces cover assembled sidecar terms.” |
| A21 | Hidden promotion to integral / D43 nonemptiness / germ / char-0 / JC2 | **Miss.** Nonclaims match the freeze. This review does not enlarge them. |

Independent audit: **196/196 checks passed, 0 failures.**

---

## Downstream

Safe to consume: the displayed exact source identity; sidecar absence from the two band-42 checkpoint banks; exact `rung_kernel` subtraction; exact constant `I23` normal forms; the `ORIGIN-ONLY` classification at the named \(\alpha=\beta=0\) completion; the line \(3\alpha-2\beta=0\) as the sidecar zero locus over the two registered fields.

Not safe to consume as a consequence: any integral re-emission, any D43 nonemptiness or compatible-tail statement, any graph-witness vanishing at nonzero \(\alpha\), any germ or characteristic-zero point, any JC2 inference.

This file is the only repository write.
