# Verification lane: B0 proof report — countermodel and computation arm

You are the second arm of a paired review (independent of the
line-by-line gate; do not coordinate). Countermodels and explicit
computations only.

charged_input=xmodel/b0-trivial-dicritical-proof-opus5-20260831.md
charged_input=xmodel/block-descent-a1-mprime-coordinator-integration-fable5-20260831.md

Your inputs are frozen read-only copies in `{{LANE_INPUTS}}`;
verify these SHA-256 hashes first and stop on mismatch:

```text
b37ec3bfd41eb22c14e29b06284100a61c50fd849150fefe2eab89e1d1a78c55  {{LANE_INPUTS}}/b0-trivial-dicritical-proof-opus5-20260831.md
69970f4d2c4a2c5760e116400b1b27426edc41fdf6a8d15936df14cc567855c9  {{LANE_INPUTS}}/block-descent-a1-mprime-coordinator-integration-fable5-20260831.md
```

Targets, exact arithmetic only:

1. Lemma 3.4 / (3.2): verify the identity numerically on FIVE explicit
   non-Keller control configurations of your own design (choose N, a,
   c, nu, s, a_p data satisfying/violating each hypothesis), confirming
   it constrains exactly as claimed and that dropping `2a<=N` or
   `corr_l=0` admits the configurations the report says survive.
2. The N=4 profile table: independently enumerate all multisets of
   pairs (mu>=1, corr>=0) with sum(mu+corr)=3, each summand >=1 — is
   the table complete? Then the N=5 surviving-profile claim in
   OPEN[B0-GENERAL-N]: enumerate sum=4 and apply the report's own
   exclusions; confirm exactly (2,0)+(1,1) and (2,1)+(1,0) survive
   with a trivial dicritical.
3. Theorem 4.3(5): re-derive the aggregate identity for at least two
   concrete shapes (s_0=1 and s_0=2), checking c_0=1 is forced and the
   r_1 dichotomy at s_0=3.
4. The group theory: transitive subgroup of S_4 generated by conjugate
   transpositions; two transpositions with orbit sizes (2,2) are
   disjoint; a cyclic transitive subgroup of S_N is generated by an
   N-cycle; verify Thm 4.3(3)-(4) group claims and the weaker
   sufficient form in OPEN[B0-N4-REDUCIBLE-PI1].
5. Prop 2.1/2.2 charts: verify the dictionary on one explicit worked
   example (pick k=2, l=3, a concrete series) computing both Jacobians
   by hand; verify `mu_l>=1` forcing and the l=k+1 unobstructedness
   claim of Prop 2.3.
6. Hunt a countermodel to Cor 3.6 (irreducible A_F singular) among
   non-Keller quasi-finite maps: the report claims Keller is needed —
   exhibit a non-Keller map with irreducible SMOOTH A_F (e.g. from the
   (x, y+xy^2) family or similar) to confirm the hypothesis is doing
   work, or report none found at desk.

Verdict per target: HOLDS / BROKEN (explicit countermodel) /
UNTESTABLE-AT-DESK.

Desk-scale exact reasoning only; you may fetch and hash primary
literature (record exact sources); never run any CAS or computation of
uncertain duration on this machine. Do not edit canonical ledgers, any
charged file, or inspect `jc2-lean`. 3 hours hard budget.

Write one report and no other file:

```text
xmodel/b0-proof-verification-grok46-20260831.md
```

Create the report file with a skeleton of section headers as your very
first action and append each completed section as you finish it. Keep
it under roughly 4,500 words. End its body with a single standalone
`<!-- BODY-END -->` line and write absolutely nothing after that line.
Do not include a `charge_basis` declaration.
