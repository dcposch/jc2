# Hostile review: nonnormal quadratic content-cone dichotomy

You are GPT-5.5 xhigh, an independent hostile algebraic reviewer for the
plane Jacobian-conjecture campaign. Work in `/Users/dc/code/math/jc2` on
frozen git basis `dca72076aa1615b0b1286fd4428a1acac7b65963`.

Read and review this sealed exact provisional producer in full:

```text
6b324824e0c075b938bd5b7015d4e8d134bad7cb845ac7597212b61d91dfa7a5
  xmodel/bd-a2-nonnormal-quadratic-content-cone-dichotomy-sol56-20260830.md
  body 16427 / 36bb8913697d3ce1bd725beed0e2089b444db425ab038f77530b2020c738d34a
  manifest 95487ead150d2ffba5aedba64974340aaf7695df806355398e0588356140a2cb
```

Its frozen authorship basis is `145f96d65021ef364a5189c4ca385fe09d7b4f46`;
its producer commit is the review basis above. Read every charged promoted
input and the exact nonmonogenic/proper-block theorem used in Sections 5--6.
Enforce their written scopes.

Independently reconstruct and attack:

1. Verify the Miranda multiplication table and invariance of the coefficient
   content ideal under every global trace-zero `GL_2(C[u,v])` basis change.
   Separate intrinsic content/discriminant/degree minimum from
   presentation-dependent infinity data.
2. Prove the affine `R_1` criterion. Check integrality, `S_2`, the Stein
   isomorphism off the finite content-zero set, and whether divisorial
   singularity along an exceptional `P1` is exactly `I_B subset m_q^2`.
   Look for any other affine codimension-one nonnormal locus.
3. Audit the quadratic content-cone contradiction: the positive grading,
   normality and integrality of `Proj(B)`, Hilbert polynomial/genus two,
   `Frac(B)=C(C_0)(s)`, and why the dominant first leg would force an
   impossible rational-surface map to a positive-genus curve.
4. Rebuild the projective `R_1` test and prove the repeated-component list is
   exhaustive. Check primitivity/Gauss, tangent and normal jets, all
   multiplicities, and every vertical/section bidegree convention.
5. For repeated constant sections, verify the fixed-root/unit dichotomy and
   the exact monogenic determinant criterion. For a repeated vertical fibre,
   verify descent `B=D[r]`, inheritance of normality, rationality of the
   completed curve, the unit argument forcing one puncture, `D=C[t]`, and
   monogenicity. Attack hidden multi-point, residue-degree, and affine-unit
   cases.
6. Check that the only survivor is exactly
   `Phi^h=Q^2L+TQS+T^2R` with irreducible `(1,1)` `Q`. Independently compute
   the affine target discriminant and prove or refute the total-degree bound
   `deg Delta<=6`; scrutinize the direction-dependent fibre-coordinate change
   and possible denominators/cancellation.
7. State the maximum theorem safe to promote. Keep the proposed blowup,
   normalization/conductor quartic, genus-one heuristic, basis-orbit coverage,
   polynomial maps, counterexamples, and JC2 separate unless actually proved.

Return an itemized verdict (`CONFIRMED`, `CONFIRM_WITH_CORRECTIONS`, `GAP`, or
`REFUTED`), exact repairs, and the cheapest decisive successor. Hard cap:
8,000 tokens and 28,000 UTF-8 bytes. Do not inspect, list, search, stat, build,
modify, or control `jc2-lean`; the process sandbox also enforces this. Do not
run local heavy CAS or Singular. Do not edit inputs, canonical files, scripts,
or dependencies. Write exactly one report:

```text
xmodel/bd-a2-nonnormal-quadratic-content-cone-dichotomy-hostile-review-gpt55-20260830.md
```

End with one standalone `<!-- BODY-END -->` line and no seal block.
