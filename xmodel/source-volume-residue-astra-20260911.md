# A nonzero source-volume residue on the unequal-depth surface

Author: Astra geometry. MANUAL / UNREVIEWED; no automatic promotion.
First action2026-09-11 23:43:31 UTC. Original reserve2026-09-12 00:00UTC,
HARD00:03UTC unchanged. Exactly TASK and its two reports, all current-pinned
before fresh WHOLE reads. ROOT's proposed formulas are checked below,
not treated as established premises. No linked source or execution.

## Whole affine scheme and charts

Verdict: the explicit Y is smooth integral affine, and its nonzero residue
excludes ALL the proposed extending constant-Jacobian pairs. This is a
scaffold exclusion, not an actual-Keller source classification.

Let B=C[x,z,y,w]/(xy-z(z-1),xw-zy,y^2-(z-1)w), Y=Spec B.
The opens D(z-1),D(z) cover its ENTIRE affine scheme. On the first set
u=y/(z-1). The relations give z=xu, y=u(xu-1), w=u^2(xu-1), and hence

    B_(z-1) = C[x,u,(xu-1)^-1].

On the second set v=w/z^2. First xw=zy gives y=xvz; then xy=z(z-1)
gives z=1+x^2v. Thus

    B_z = C[x,v,(1+x^2v)^-1],
    y=xv(1+x^2v),  w=v(1+x^2v)^2.

Each equality is an isomorphism with inverse given by the displayed
rational expression: substitution verifies all three relations and both
compositions. These two reduced smooth domains have nonempty intersection
(for example x=1,z=2,y=2,w=4). Since they cover Y, there is no extra
component or nilpotent: Y is reduced, irreducible, hence integral and smooth.

The fibre x=0 is scheme-theoretically the disjoint union

    D0={x=z=0, w=-y^2},       E={x=0,z=1,y=0}.

Both are reduced affine lines. Put U0=Y-E=D(x) union D(z-1).
The formulas u=z/x on D(x) and u=y/(z-1) on D(z-1) agree, producing
the full isomorphism

    A2_(x,u) -> U0: (x,xu,u(xu-1),u^2(xu-1)).

Similarly U1=Y-D0=D(x) union D(z) is the full A2_(x,v) chart with
the above coordinates and v=(z-1)/x^2 on D(x). These charts cover Y;
their intersection is exactly D(x), since xy=z(z-1). The transition is

    u=xv+x^-1.

Thus the asserted charts hold at x=0 as well as on the dense overlap.
In U1, E is x=0 with coordinate v=w.

The form omega=dx wedge du becomes x dx wedge dv and glues globally.
It is nowhere zero on U0 and has order1 precisely on E. Therefore
div(omega)=E and K_Y=[E]. In fact this class is nonzero: B_x is the
factorial ring C[x,x^-1,z], so divisor classes are generated by D0,E.
A principal relation supported there comes from a unit c*x^j of B_x;
div(x)=D0+E, with both orders1 by the charts. These are the only relations,
so Cl(Y)=Z[E]. This checks the canonical-class distinction, not finiteness
of any proposed map from Y.

## Unrestricted residue obstruction

Set alpha0=-u dx on U0 and alpha1=-xv dx on U1. Their exterior derivatives
are respectively dx wedge du and x dx wedge dv, both omega. On the overlap,

    alpha0-alpha1=-dx/x.

Suppose a GLOBAL REGULAR one-form beta satisfied d beta=omega. Then
beta|Ui-alpha_i is a closed polynomial one-form on A2, hence d f_i for
a polynomial f_i. This elementary polynomial Poincare fact follows by
integrating the dx coefficient, then integrating the remaining coefficient
in the other variable; closedness makes that remainder independent of x.
Characteristic0 licenses division by positive integers.

It follows on the overlap that -dx/x=d(f1-f0). But the overlap ring is
C[x,x^-1,u], with v=x^-1u-x^-2. For any Laurent polynomial h in that ring,
the coefficient of x^-1 in partial_x h is zero. Equivalently, d h=-dx/x
would first force partial_u h=0 and then contradict the derivative of a
one-variable Laurent polynomial. Thus beta cannot exist. This is a direct
nonzero Cech residue calculation, with no appeal to a comparison theorem,
topological period, completion or finite-map hypothesis.

Now let P,Q be ANY elements of B whose restrictions to U0 satisfy
J_(x,u)(P,Q)=c in C*. On U0, dP wedge dQ=c omega; both sides are global
regular two-forms on the smooth integral Y, so equality holds on Y.
The global regular form c^-1 P dQ would be a primitive of omega,
contradiction. No support, degree, homogeneity or shape restriction on
P,Q was used. There are no such extending Keller pairs.

The proposed counterexample implication, had a pair existed, is also exact.
If its restriction were a polynomial automorphism of A2_(x,u), its inverse
would express u polynomially in P,Q, making u regular on Y. But on U1,
u=(1+x^2v)/x has a genuine simple pole along E. This contradiction does
not rely on a claim that every nonproper etale map has a prescribed image.
The actual result is exclusion of this antecedent, not a counterexample.

## Sharpness, controls, and source limits

On any two-plane-chart gluing with intersection x!=0 and transition
u=x^m v+g(x), m>=0 and g Laurent polynomial, the same argument uses
omega=dx wedge du=x^m dx wedge dv and primitives -u dx,-x^m v dx.
Their difference is -g(x)dx. Thus an extending Keller pair requires
Res_(x=0)g(x)dx=0. This is a NECESSARY condition. No sufficiency,
affineness, separatedness, or classification of such gluings is asserted.

For g=lambda*x^-k, k>=2, including any constant lambda, the following
forms are polynomial on their respective charts:

    beta0=-(k*u*dx+x*du)/(k-1),
    beta1=-((m+k)*x^m*v*dx+x^(m+1)*dv)/(k-1).

Indeed substitution in k*u*dx+x*du cancels the two terms
lambda*k*x^-k*dx. The remaining expression is exactly beta1's numerator.
Differentiation gives d beta0=dx wedge du and
d beta1=x^m dx wedge dv. They therefore give a GLOBAL regular primitive.
This is a full algebraic control, not merely a vanishing numerical residue.
Consequently the order m of the volume zero, or K=mE when that divisor
description applies, cannot by itself force the obstruction above.

For the charged old T:t^2-1=x^2 Z, its plus chart y=(t-1)/x^2 and
minus chart v=(t+1)/x^2 satisfy y=v-2x^-2. Thus m=0,k=2,lambda=-2.
Its nowhere-zero omega_T=dx wedge dt/x^2 has the global primitive

    -2y dx-x dy  on the plus chart,
    -2v dx-x dv  on the minus chart.

The formula agrees exactly, including the sign and coefficient2. It proves
omega_T exact, NOT that a primitive has the special form P dQ with
{P,Q} constant. The unrestricted T scalar-pair question therefore stays open.
The old equal-depth reduced-boundary control also remains what its report
claimed: reduced boundary does not determine infinity-fibre multiplicity;
it was not a finite Keller normalization.

SOURCE-APPLICABILITY GAP: nothing charged shows that an arbitrary actual
Keller normalization, or even every normal-index2 one-dicritical source,
is this Y or has this transition with nonzero residue. Matching K_Y=[E]
alone is insufficient. General normalization may have different charts,
boundary geometry or singularities. This scaffold cannot host extending
pairs, but no global source-landing theorem, stratum exclusion, JC2 theorem,
positive construction or novelty claim follows. ROOT's independent
primary/history investigation is not consumed here.

QUANTITY: existence of unrestricted extending constant-Jacobian pairs on
the exact affine Y. CHEAPEST TEST performed: all-locus chart algebra and
the single Laurent residue, planning <=15minutes UNMEASURED manual wall;
no mathematical subprocess or coefficient artifact. It gives an exact
negative answer for Y and exact zero-residue controls, with no successor
or new canonical OPEN selected.

Documentary checks: exactly three input bodies fresh WHOLE, including the
two seals, after current pins; no prior-read reuse claimed. Own report and
PINS receive WHOLE readback, all three postpins and collision/scope/quantity
checks before the sole BODY-END. Existing close/finalize/expected verify,
sealed WHOLE readback and terminal custody follow. All authored bytes stay
in the owned lease/report/PINS/custody; ROOT TASK and prior reports unchanged.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `7770`.
- Body SHA-256:
  `d385786a60086dfbdcf13487fc6fc7214a2c99761525dc6374a8102660a28ef0`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
