cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
Review is published and all closing checks pass at 09:46:24 UTC, inside the 09:48:30 reserve. The marker is the unique last line, the body carries no hex digest, and the 16 custody rows in the box file equal a live sha256sum of the inputs.

**Verdict (static, nothing executed):**

- **A, B, C, D CONFIRMED.** I rederived the septic leading coefficient, the checker's series recurrence, the upper-solve pivots, the gap-4 inverse entry (4/15)H7, the gap-6 forced fixed term, the determinant-one pair identity, the band targets from T·Pihat² and E·T·Pihat, the 80-slot count, and the inverse-chart scaling. Producer and checker are separate algorithms with no producer import, no embedding, and no parameter inversion.
- **E CONFIRMED on text read.** All imported names exist, globals are reset, zero terms never reach the wire, the parser rejects noncanonical rationals before conversion under the 4300 guard, and the aggregate term count is identical on both sides.
- **F GAP (entry contract).** Sibling imports rely on the script directory being on sys.path, which fails under `-I` on Python 3.11 or later, and no interpreter flags are stated. The directory listing is never checked, so an extra module file could shadow the stdlib. Neither CLI prints the hash of the bytes it wrote or checked. Registration authenticity and native closure remain wrapper-only, as the code itself says.
- **G CONFIRMED as specifications**, with two wrong branch attributions in CONTROLS.md: control 2 is caught by the affine-base comparison, not the checker's self-identity for N; control 7 rejects after the band mathematics, not before.

**Smallest fixes** are in section H of the report: two sys.path lines or an explicit wrapper flag set with an exclusive directory, one hash-print line per CLI, and two text corrections. Any code edit re-pins all five hashes in the future registration JSON.

Owned files written by apply_patch only: `xmodel/f10-r2-reconstruction-code-gate-fable5-20260910.md` and `box/f10-r2-reconstruction-code-gate-fable5-20260910/input_custody.md`. No Seal, no charge_basis, no OPEN raised, collisions EMPTY. Nothing further is authorized from this lane.
DIVERT path=xmodel/f10-r2-reconstruction-code-gate-fable5-20260910.md status=CLEAN body_bytes=18320 body_sha256=3e9a06cf3aced19aa6a8ab5c742c57ffc1d1da21bb8a0270030a83285720502b
