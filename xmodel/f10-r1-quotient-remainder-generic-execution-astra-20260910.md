# F10 quotient/remainder generic execution: stopped at SSH preflight

Owner: Astra `/root/model_productivity`. Basis `0d39df3c9fd69c939a8420c54d03228b9077777d` is publication provenance only.

## Outcome

STOPPED-BEFORE-REMOTE-PREFLIGHT. The first SSH connection returned exit255, `Host key verification failed.` No remote session command executed through this executor, and no retry or host-key override followed. Zero deployment files, derivative authorities, SYSTEM units, seven-control invocations, generic mathematical invocations, or synthetic certificates were created by this executor. This is an operational preflight failure, not a failed mathematical fixture or evidence against the solver. Static17zh remains at its prior scope; runtime/migration acceptance is NOT established.

Only registered worker `i-08d2a40f272ee9fa2` was stopped. STOP requested at2026-09-10T01:08:17.593750289Z; EC2 read-only confirmation issued at01:08:43.156988318Z returned STOPPED, private172.30.0.72, sole retained EBS `vol-0574e0fa5aed1f5e3`. No terminate/delete/resize/install or other-instance action occurred. No remote payload existed to collect from this attempt. There are no executor-created CAPRUN/child processes or groups to harvest; this is not a claim to have inspected every host process.

## Clocks and failed command

First action2026-09-10T01:03:35.773979633Z, with both owned final report and box absent. Controlling task end01:27UTC (earlier than first+25min); mathematical absolute end01:20UTC; first-math+240sec never activated because first_math is null. Worker STOP occurred before01:22 and before root's independently installed01:23 backup timer. Root owns timer cancellation after its independent physical confirmation. No clock was reset.

Exact failed SSH argv is stored as an array in `observations.json`; its shell rendering is:

```text
ssh -i /home/ubuntu/.ssh/jc2-fleet -o BatchMode=yes -o ConnectTimeout=10 ubuntu@107.21.89.35 'date -u +%Y-%m-%dT%H:%M:%S.%NZ; hostname; cat /sys/devices/virtual/dmi/id/sys_vendor /sys/devices/virtual/dmi/id/board_asset_tag /proc/sys/kernel/random/boot_id; readlink /proc/self/ns/pid; /usr/bin/python3 --version; test ! -e /home/ubuntu/jc2-f10-r1-quotient-remainder-generic-20260910 && test ! -L /home/ubuntu/jc2-f10-r1-quotient-remainder-generic-20260910 && echo EXACT_CWD_ABSENT; free -b; ps -eo pid,ppid,pgid,stat,args --no-headers'
```

There was no `StrictHostKeyChecking` override. The failed call itself emitted no local UTC timestamp and its remote `date` never executed. Its exact UTC is therefore unavailable; the honest recorded bracket is01:07:15.521796372–01:08:17.593750289UTC. Tool observation b7330c returned exit255 and the literal SSH error. Neither changed-key versus unknown-key cause nor host authenticity was diagnosed. In particular, remote cwd absence, boot/DMI, native100-file/interpreter equality and resource/process preflight remained UNVERIFIED by this executor.

Reconciliation, explicitly root-supplied metadata only: root subsequently reported its separate private-IP SSH around01:07, using `StrictHostKeyChecking=no`, saw the expected host/instance and boot29faf30e-c4aa-451b-943c-b95925f24fdc; generic and gate-valid SYSTEM units were notfound. This is not our successful connection, does not repair our preflight, and did not authorize a retry. Root explicitly instructed preservation of STOP/no retry. The public-IP SSH failure and root's differently configured private-IP observation must not be conflated.

## Exact read and pin scope

Eleven local charged objects are listed with full SHA256 and byte counts in `input-pins.json`: root registration, root pins, accepted static gate, and its eight named task sources. All were checked before body reuse and all eleven match again at01:09:20.253957UTC. ROOT-PINS declares100 native files plus interpreter; reading that declaration is NOT remote verification of those files.

WHOLE newly read in this task: root registration, ROOT-PINS, static gate, new solver.py332lines, probe.py21lines, and unchanged CAPRUN1198lines (three contiguous chunks through EOF). WHOLE same-byte prior reads explicitly reused only after matching current pins: checker.py, evidence.py, algebra.py, execution_gate.py and our generic_controls.py. No mathematical import, syntax/AST/compile/test, actual-source input, receipt, acceptance, coefficient generation, or local arithmetic subprocess occurred. Existing publication tooling and metadata-only JSON/hash/clock commands were used. No new dispatcher, helper, authority schema or mathematical code was authored; no frozen code or shared ledger was edited.

## Retention and limits

Owned `observations.json` retains the failed literal SSH argv, exact refusal, non-exact failure-clock bracket, STOP request/response and fresh STOPPED response, plus separately attributed root reconciliation. It is a documentary transcription of tool results, not a CAPRUN telemetry receipt. `input-pins.json` retains the eleven current input pins. Final custody pins those owned records, this sealed report and its artifact transaction, with a fresh input comparison; custody excludes itself.

Seven preflight controls: NOT RUN. Generic8positive/4negative batch: NOT RUN. Child wall/CPU/RSS, engine behavior, certificate correctness, descendant cleanup regression and runtime/migration gate: NO EVIDENCE. There is no synthetic or source PASS, no root acceptance, no mathematical outcome, and no automatic follow-on authority. A future attempt, if root chooses one, requires explicit new direction rather than reuse of this stopped registration. This report requests no repair, execution or new research task.

Raised mathematical OPEN count:0. The missing operational prerequisite is an authenticated SSH host identity under an explicitly authorized future connection policy; the cheapest next check would be root-controlled host-key reconciliation, not a mathematical run. Its outcome and cost are unassessed here. No such check or retry is authorized by this report.

All owned writers will be closed after the unique completion marker, artifact close/finalize/expected verification, and final custody publication; terminal handshake follows only after that completion.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6247`.
- Body SHA-256:
  `955a2fc3e45c307d52e4a88692d2682539fa27c3a2ec1d261d078e9b9ef1ee3e`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
