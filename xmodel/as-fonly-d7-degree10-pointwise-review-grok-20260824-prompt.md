# Hostile different-model review — AS F-only D7 degree-ten pointwise gate

Work in `/Users/dc/code/math/jc2` on committed basis
`a04affb7247fb5e87cad4e87f5926ab440254b24`.  The producer artifacts are
newer frozen working-tree bytes.  Do not edit producer, case, canonical,
prompt/log/run, ladder, coordination, or erratum files.

Read in full:

- `xmodel/as-fonly-d7-degree10-pointwise-20260824.md`;
- every file in `cases/as_fonly_d7_degree10_pointwise_20260824/`;
- the confirmed divided-carry predecessor and its hostile review named by the
  producer.

Frozen hashes:

```text
producer report  e4311a80aef2d3617c7b449e16cb19d34cfe25d31c558bb68f78ddc02fcae4ab
MANIFEST         426210cbdbd61e0ec0fa92d39344d3746c8a4a421df7c07c70f5a0cab968e2a5
FREEZE           c92a911df945c64f8d148819e22aa985a6393be3643cec8002474c89e272bab1
static replay    f1461b2826019bc10069d0222957b47f3fc6d3847af871274349a793c21cc2e5
```

Verify the manifest and registered replays, but use them only as regression.
Independently reconstruct and attack every load-bearing claim:

1. Re-expand the integer determinant and confirm the divided-carry orientation,
   source of the degree-ten row, and why no omitted lower or degree-six
   coefficient contributes there.
2. Audit the exact source compression from the confirmed 40-variable/30-row
   nonreduced predecessor.  Verify all affine factors, the displayed high ideal
   `H`, dimensions, and that no radicalization or hidden localization occurs.
3. Independently regenerate the 8-by-9 affine digit system and its inhomogeneous
   column.  Check coefficient signs and the claim that the accepted divergence
   solve is reversible.
4. Recompute pointwise ranks over field-valued points using Fitting/minor ideals:
   rank zero exactly vertical, every nonzero point rank three, and no ranks
   one, two, or four.  Distinguish statements modulo `H`, `sqrt(H)`, and
   saturation by the degree-five ideal.
5. Recompute compatibility from the original nonreduced `H`, then prove or
   refute the field-support equality
   `sqrt(J)=C_a intersect C_g`.  Audit both cone charts, their coverage,
   dimensions, and the nonreduced `z^3` multiplicity without turning it into a
   scheme-theoretic two-component claim.
6. Rebuild the vertical, `a`, and `g` integer controls directly.  Verify the
   mod-27/mod-81 assertions and that the displayed `a=1` point—not the whole
   endpoint family—fails the separate cap-seven degree-eight boundary.
7. Check all scope arrows.  The result must not become full next-digit
   classification, full D7 emptiness, all-depth lifting/nonlifting, a
   characteristic-zero statement, a counterexample, or JC2.

Use exact arithmetic and at least one independently written reconstruction or
matrix-changing check.  Do not launch AWS.  Write exactly one report:

`xmodel/as-fonly-d7-degree10-pointwise-review-grok-20260824.md`

Give `CONFIRMED`, `REFUTED`, or `GAP` overall and per item, checked hashes,
smallest failing identity if any, exact promotion language, and exact
quarantine language.

