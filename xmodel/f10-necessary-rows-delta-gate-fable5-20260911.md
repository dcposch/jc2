# FIRST Fable5.1: necessary-row combined source/runtime DELTA gate

Lane: f10-necessary-rows-delta-gate-fable5-20260911. Started 2026-09-11 06:18 UTC. Reserve 06:39 UTC, HARD 06:42 UTC (original, never reset).

## 0. Custody and read scope

status=COMPLETE. Verdicts: A CONFIRMED, B CONFIRMED, C CONFIRMED (release obligations pending, no defect), D CONFIRMED at the imported 23z/19z/28z scopes only, E READY at the static gate boundary (deployment fields missing, no source/runtime defect). No GAP verdict, no REFUTED verdict, no new canonical OPEN ID, no charge_basis (no exit-price assertion is made).

All 26 pins at /tmp/jc2-lane.47oEv6/inputs equal the charged list (sha256sum -c: 26 OK, rc 0) at 06:20:55 UTC BEFORE any body read; the table is in box/f10-necessary-rows-delta-gate-fable5-20260911/input_custody.md. The lane inputs directory is read-only, so scratch went to /tmp/f10dg-scratch. Method: date, ls, sha256sum, wc, cat, sed, grep, diff, cmp, apply_patch; static text and manual reasoning only; zero subprocess science, import, AST, compile, test, CAS, dummy, fixture, prime, network, AWS, SSH, process control, git, agents, protected or live access. No source implemented or edited.

Independent text diff: `diff -u old-X.py X.py` was run for all six pairs and compared with the retained diff after the first two header lines with cmp: produce 49/49 lines, authority 11/11, check 44/44, dispatch 93/93, probe 29/29, mutate 52/52, all HUNKS IDENTICAL; only the two filename/mtime header lines differ (retained headers name old/new, predecessor/necessary-rows, or the two box paths with mtimes). The ROOT assertion "every actual diff equals retained bytes" is therefore independently CONFIRMED.

Read scope, honestly: the six .diff files WHOLE; ROOT-ADOPTED-NECESSARY-ROWS.md WHOLE through its Seal (144 lines); ROOT-SOURCE-CONTRACT.json WHOLE (68); ROOT-NECESSARY-ROWS-CANDIDATE.json WHOLE (141); the design gate (44), code gate (53) and runtime gate (66) reports WHOLE through their markers; authority.py new WHOLE (133 lines). CONTEXT reads only, not whole reads: old-produce.py 262-280 and 328-338; produce.py 315-325, import lines 2-7 and 344-347, usage lines of zr/shift/U (76, 113, 196-227, 311) and the mkstemp line 352; old-check.py 386-408; check.py 1-9 (imports), usage lines of move/sym/cast (123-316), 74-92, 469-488 and main 494-514; dispatch.py 398-410 plus a grep of schema/status/literal lines; probe.py and mutate.py grep of schema/JOB/literal lines only. arithmetic.py and check_arithmetic.py HASH_ONLY at acd07f80 and 9d2aac63. No linked, provenance, live or uncharged input was opened.

## A. Exact scientific delta

CONFIRMED. Producer (old 8a7bd82e, 382 lines, 19562 bytes; new 50781428, 369 lines, 18811 bytes). Hunk 1 `@@ -262,19 +262,6 @@` deletes exactly old 265-277: `Pin,qin=var(5,'inverse'),var(6,'inverse')` (265), the chartS construction (266-267), `inverses=[];poles=[]` (268), the (Ahat,-3,30),(Bhat,-5,51) loop (269-274), the 'inverse chart bound' raise (275) and the two appends (276-277); thirteen lines, matching 382-369. New 265-267 are the retained 'leading a', 'leading b' and 'leading guard' equalities, immediately after the deleted branch as ROOT item 1 requires. Hunk 2 turns old 332 `if len(inverses)!=2 or len(poles)!=2 or len(scale_record)!=13:` into new 319 `if len(scale_record)!=13:`; the raise 'boundary/scale inventory' stays at new 320, and the neighbouring 25-row check (316), 150/111/39 partition check (317-318), 15 UNITS set (321-323) and DENOMINATORS order check (324-325) are unchanged context. Hunk 3 changes only the two literals `'schema':'f10-r3-necessary-rows/v1','job_tag':'f10-source-cone-r3-necessary-rows-v1'` at new 334 (old 347); the other four top keys and the graph dict remain unchanged context, so no new wire key or dummy inverse/pole list exists. Hunk 4 changes the success line at new 365 (old 378). The internal `tempfile.mkstemp(prefix='.direct-rows-',dir=parent)` is retained byte-identical at new 352 (old 365); the old file's two 'direct-rows' tokens become one, and the only DIRECT_ROWS token becomes NECESSARY_ROWS. Names still bound after the deletion remain live elsewhere: zr at 198/201/206/311, shift at 76/113/196/200-201/224/227, U and power in Pi at 206; nothing outside 265-277 becomes dead, and the now-unused 'inverse' kind entries stay in arithmetic.py unchanged.

Checker (old 50528847, 529 lines; new 1d2d5b54, 514 lines). Hunk 1 changes only the two literals at 42-43. Hunk 2 `@@ -387,21 +387,6 @@` deletes exactly old 390-404: the comment "BOTH full Laurent-q boundary maps..." (390), `iq,P,iz = sym(...)` (391), chartS (392), the two lists (393), the loop (394-402) including the 'inverse Laurent envelope' need at 400, and the two length equalities 'BOTH full inverse charts'/'BOTH separate pole records' (403-404); fifteen lines, matching 529-514. New 390-392 are the retained 'installed first leading scalar', 'Euler second leading scalar' and 'leading guarded inverse product' comparisons. Hunk 3 changes the return literal at new 489 (old 504). move/sym/cast remain used at 123, 150, 155, 166, 212, 235, 308, 314, 316 and 375, so no import goes dead. One unchanged byte worth naming: the line-1 docstring still reads "Independent direct-rows checker"; ROOT item 3 selects no comment/docstring edit, so this is conformant, not a defect. Authority: only line 5 TAG changed (133 lines both, +3 bytes); FILES, LIMITS and every check at 11-133 are byte-identical.

Arithmetic engines: acd07f80 and 9d2aac63 equal both ROOT-SOURCE-CONTRACT source_files and the candidate source_files; not reread. The candidate's five-hash source vector (804c7aff, acd07f80, 50781428, 9d2aac63, 1d2d5b54) equals the charged bytes hash for hash. Dependencies the delta actually changes, beyond the accepted 28z lemma: (i) the two stop surfaces 'inverse chart bound' (old produce 275) and 'inverse Laurent envelope' (old check 400) and the internal cap stops reachable only inside the branch are gone, which is the disclosed acceptance-domain widening and nothing else; (ii) the deleted call `power(chartS,ex[5])` at old produce 273 was the only large-exponent consumer named by the code gate's F blocker, so the prescribed power() correction already pinned in acd07f80 keeps no inverse-chart consumer, which changes no retained value; (iii) no graph, diagnostic, UNITS, DENOMINATORS, SCALE or wire counter reads any deleted name. Retained graph and diagnostics are therefore preserved verbatim.

## B. Runtime delta and literal closure

CONFIRMED. Token census over the three pairs, old to new: dispatch.py 'direct-rows' 7 to 0, 'DIRECT_ROWS' 4 to 0, 'necessary-rows' 0 to 7, 'NECESSARY_ROWS' 0 to 4, bytes 43549 to 43582 (+33 = 11 replacements times 3 bytes); probe.py 3/0 to 0/3, bytes 10642 to 10651 (+9); mutate.py 5/1 to 0/5 and 1, bytes 16196 to 16214 (+18). Counts of 'direct-source' (2/0/0), 'CAPRUN' (11/0/1) and 'native-closure' (1/1/1) are identical old and new in every file, so no direct-source, native-closure, CAPRUN/v1 or generic-direct rename happened. Each hunk is a single-line literal substitution with no algorithm, cap, profile, order, predicate or argv change: dispatch 22 JOB, 154 runtime registration schema, 211 preflight-policy schema, 370 root-policy schema, 546 producer stdout literal, 552 checker stdout literal, 554 positive-phase schema, 569 T-mutation receipt schema, 587 terminal status, 627 custody schema, 657 main return test; probe 12 JOB, 68 runtime schema, 151 root-policy schema; mutate 12 JOB, 68 runtime schema, 152 positive-phase schema, 156 checker-stdout hash literal, 187 wire schema, 245 T-mutation schema. LIMITS/PROFILES (dispatch 24-26 context), LABELS 32-34, the mutation string 'graph.slots[24] += X1^27 over ROOT place' (571 context, mutate 248), typed slots 428-440 and the tag/tail construction 403-405 are unchanged.

Exact T rejection: dispatch 583-585 still requires child_signal None, child_exit_code 2, empty stdout and stderr bytes exactly `CHECK FAILED: direct-source graph slot T\n`; 589 keeps negative_predicate 'direct-source graph slot T'. The producing text is the per-slot loop check.py 485-487, `eq(record,actual,'direct-source graph slot '+label)` inside `for label,record,actual,weight in zip(...)`, before the aggregate `eq(len(wanted_slots),25,...)` at 488; main 503-507 catches Mismatch first, reauthenticates, prints 'CHECK FAILED: '+str(exc) to stderr and raises SystemExit(2), while print(verdict) at 501 lies only on the success path, so stdout is empty. Parser, hash, cap and Stop failures print 'STOP_NONDECISION: ...' (508-510) and authority refusals raise 'REFUSED: ...' SystemExit; neither can produce the T bytes, and a traceback cannot either. The rejection therefore remains the individual slot comparison, not an aggregate, parser, hash, authority, traceback or generic nonzero exit.

## C. Source-authority-runtime composition

CONFIRMED as a static composition; every live binding is explicitly pending. Literal chain: authority.py:5 TAG, produce.py:334 job_tag, check.py:43 expected job tag, dispatch.py:22, probe.py:12 and mutate.py:12 JOB and the candidate's job_tag are all `f10-source-cone-r3-necessary-rows-v1`. authority 25 requires argv[1:3] == ['--registered-job', TAG] and the dispatcher supplies JOB at 403-404 (WRONG-JOB only for the two startup labels); authority 81 requires meta job_tag == TAG and dispatch 376 emits job_tag JOB; authority 123 requires the native simple inventory job_tag == TAG and dispatch 236 requires simple['job_tag'] == JOB. FILES (authority 6) equals SOURCE_NAMES (dispatch 24, probe 13, mutate 13). authority 98-100 digests all five installed files including authority.py itself against meta['files'], and 107-109 requires source_pins[join(base,name)] == files[name] for each, so a registration must carry 804c7aff in BOTH files and source_pins; the candidate's source_files does, and the dispatcher's 189-193 rule agrees. The four-key return (132-133), eleven-key authority (79) and the dispatcher's eleven-key emission (376-383, unchanged context) are untouched, so the mutator's unchanged eleven-key reconstruction (163-169) still equals the dispatcher's check-positive object.

Wire and phase: produce 334 emits the new schema, check 42 demands it, mutate 187 demands it. Dispatch 552 requires the new checker literal, 554 emits the new positive-phase schema, 559 stores sha(out); mutate 152 and 156 require exactly that schema and the SHA-256 of the new literal, so an old positive record (old schema, old stdout hash) is refused at 152/156, an old registration is refused at dispatch 154-155, probe 68 and mutate 68 (schema and jobtag) and at authority 81, and old source hashes cannot pass authority 98-100 against the new produce/check bytes. The mutator still receives the ROOT hash (144), the frozen baseline binding (157-159) and, per dispatch 576 and the typed check-negative slot 428-437, a fresh fixture digest before the negative checker; unchanged lines. Pins: all five source, three runtime, six predecessor and six diff hashes in the candidate equal the charged bytes (custody table). Pending, not defects: enabled is false and the candidate schema is a candidate-contract schema, so dispatch 154 and probe/mutate 68 cannot accept it; seven refusal triples must be re-derived from the NEW text, because produce.py frames after old 277 shift by -13 (main now 355-369) and check.py frames after old 404 shift by -15 (main now 494-511), so any registered stderr or traceback bytes citing old line numbers are stale; a new contract digest, actual absolute installed paths, native manifests, physical identities and immutable authorities are unformed. Disabled metadata is not a release.

## D. Full-source implication and honest service

CONFIRMED at the imported scopes only, nothing new. At accepted 23z/19z/28z scopes the SAME alternative 25 rows vanish in the canonical LOCALIZED A=(B[X]/I)[g^-1] with B the entire degree-7 field and g' mapping to its original unit; a genuine place-bound nonzero 297-minor lifts to rank 297 over B, puts g' in the row ideal and gives A=0. Canonical inverse polynomiality remains a SOURCE HYPOTHESIS inside A, not a numerical property of the alternative Ahat/Bhat, and the deleted branch never tested it. The omitted alternative Laurent-q inverse/pole coefficients are NOT_COMPUTED: no zero, ordinary, checked, dummy or evaluated status is assigned, and no list or JSON field represents them (A). Old acceptance-domain equality is not claimed; the two branch-local stop surfaces named in A are deleted. Retained by unchanged text: all 125 denominators and 15 scalar nodes (produce 321-325, check 487-493 region unchanged), forcing, bands, matrix products, installation, Euler gauges, 108/45/63 positions, 22 readbacks, 150 diagnostics with 111/39 (produce 317-318, check 469-488 context), Laurent-S auxiliaries (produce 200-201, check 314-316), SCALE 13 fields and six groups (produce 319), the SAME 25 rows and guard (produce 311/317, check 469/485-488). No canonical matrix rank, unsaturated-ideal equality, source point, all-r statement or JC2 conclusion follows from this gate.

## E. Readiness at the exact gate boundary

READY, statically, for the remaining exact-refusal/ROOT-registration binding and then ONE capped 13-phase AWS observation; no registration or execution is approved here. Source/runtime defect: NONE found in the delta. Missing deployment fields, all named by the candidate's bindings_required_before_execution and not fillable by this review: enabled runtime registration under schema `f10-necessary-rows-runtime/v1` with new jobtag, source_files and absolute source_pins including authority 804c7aff, a new contract_sha256, the seven exact refusal triples derived from the new line numbers (C), native closure and simple manifests, physical/worker identities, thirteen literal command vectors, four stops. No rank client before an actual independently checked graph plus the fresh exact T negative. SCALE, graph and diagnostics cost is UNMEASURED; the unchanged 900/599, 3000/2100, 8 GiB, 128 MiB, 100000/20000/400000 caps are refusal ceilings, not measured feasibility. Bad place, anomaly, cap or noncompletion is NONDECISION. The optional inverse-restoration hardening stays OFF, no cap raise, no streaming framework, no repeat foundation review.

## F. Remaining quantity, cheapest test, planning wall, collision statement

QUANTITY (no new canonical OPEN ID): whether the necessary-row producer and independent checker complete at one genuinely registered place with identical 30+2 positions inside the unchanged caps, and whether the freshly rebound T+X1^27 fixture yields exactly `CHECK FAILED: direct-source graph slot T`, exit 2, empty stdout; unknown, nothing computed. CHEAPEST TEST: bind the seven text-derived refusal triples and the new registration (contract digest, paths, manifests), then exactly one capped registered 13-phase observation; no local scientific test of any size. PLANNING WALL: this gate's own 15-20 minute figure is an UNMEASURED planning proposal inside the ORIGINAL 06:39/06:42 clocks; no speed, cap-sufficiency or scientific outcome is claimed.

COLLISIONS: EMPTY. Own-only: this lane wrote exactly xmodel/f10-necessary-rows-delta-gate-fable5-20260911.md and box/f10-necessary-rows-delta-gate-fable5-20260911/input_custody.md (plus that box directory), both absent at 06:18:19 UTC, by apply_patch only with each write under 1500 words; no Seal, charge_basis, transaction, other artifact, source edit, Write/Edit or redirection into owned files; scratch files only under /tmp/f10dg-scratch.

## Closeout

All 26 post-pins recomputed at 06:27:18 UTC after every input read equal the charged list and the pre-pins (custody file). Own WHOLE readback of both owned files completed at 06:27:50 UTC, untruncated, with zero placeholders, zero markers and every 64-hex token in both files matched against live sha256sum output (0 unmatched). Corrected in this final write: the 25-row count line is new produce 316, and the row references in D read produce 311/316. wc reports about 2200 words, of which line citations, hashes and numerals are a large share; substantive prose is inside the 1400-2200 target. REFUTED: none. GAP: none; every open item is a pending ROOT release obligation named in C and E, not a defect. No clock extension, no retry, no charge_basis, no new canonical OPEN ID. This paragraph and the marker below are the final bounded apply_patch write, before the ORIGINAL 06:39 reserve; all writers are IDLE after it and no follow-on authority is issued.

<!-- BODY-END -->
