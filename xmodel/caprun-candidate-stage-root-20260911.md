# Candidate staging: one measured offline caller repair

Producer-checked / PENDING FIRST. ROOT, September11. Basis
0d39df3c9fd69c939a8420c54d03228b9077777d. Original18:00 reserve/18:03 HARD.
This is operational preparation toward the selected r3 source test, not a
mathematical result, successful preflight or authorization to retry batch b/c.

## Outcome and why this changes the failed workflow

The missing metadata caller is now implemented and tested on the exact
historical fixture. In one actual functions.exec call, ROOT created a private
directory, reconstructed the accepted binder's14 candidates, applied their
UTF-8-preserving patch and verified every whole-file hash:78889bytes,14/14,
400milliseconds orchestrator elapsed. The old ASCII decoding fails on the
retained em dash in the same card. Nine short documentary test methods pass.

This is not a claim that complete startup fits120seconds. It measures only
local candidate generation/staging/hash readback. The distinction matters:
batch b reached its holder limit after document composition; batch c's inline
caller failed ASCII decoding and its corrected transport finished too late.
Neither reached a phase. Preparing the binder alone left the caller untested.
The new deliverable fixes that missing boundary in a pretested single call;
it does not change the holder, runtime, science, caps or source equations.

## Scope and safety boundary

The source API is stage_patch.py,70 lines, no I/O or execution. It accepts
exactly the14 names, verifies the summary's13 byte commitments and candidate
context/status, preserves UTF-8/LF bytes, and returns an add-file patch plus
all14 expected hashes. Prefixes are limited to the named private exercise
namespace; contents cannot name an additional file. The actual apply_patch
tool and independent sha256sum readback are existing tools, not a new installer.

The historical fixture adapter pins the accepted helper and its test harness;
that harness pins the8 old text inputs before use. It imports only this
documentary helper/harness, not any candidate shell, native or scientific
code. It requires HISTORICAL_TEST. It is NOT a ready actual-observation CLI.
The underlying pure transport can consume authenticated actual candidates,
but a future ROOT invocation must bind them separately. No historical worker,
PID, inode, approval text or enabled flag is reused as current authority.

ROOT supplies a fresh private directory and exclusive no-concurrent-writer
trust. Add File is not an O_EXCL write; the API alone cannot establish absence,
symlink safety, no concurrent writer, input authentication, physical state or
tool correctness. No retry/overwrite of partial staging is allowed. SUMMARY
stays with all13 candidates; no candidate was installed or executed. The
existing binder's native-list authentication and complete prepared-text hygiene
duties remain, including its previously documented lowercase-placeholder gap.

## Checks and exact evidence

The original helper, harness, contract and FIRST report matched current
accepted pins after exploratory WHOLE reads. ROOT read new code WHOLE after
authoring and before publication. The8 historical texts were read/pinned by
the authenticated fixture harness; this is not a claim of fresh human-WHOLE
reads of every fixture. No extra source/native body or linked runtime was
executed. The holder/leaf/installer and b/c closeouts were read as historical
dependency evidence only; none was run or altered.

Command: python3 -I -S -B
box/caprun-candidate-stage-root-20260911/test_stage_patch.py.
Initial9 methods passed in unittest0.017seconds; the final log is tests.stdout.
Both are ordinary-mode documentary checks, not optimized-mode evidence.
Controls cover all14 missing files, one extra path, all13 changed non-summary
commitments, false summary/context/release flag, duplicate key, bad text,
unsafe prefix, patch-looking content and unchanged input objects. The exact
historical card fails the old ASCII decoding but reconstructs byte-for-byte
through the new UTF-8 patch. The test parser independently rebuilds all14
payloads from the restricted add-file grammar.

Actual retained directory:
box/caprun-stage-exercise-20260911.XXo03Rv3/.
EXERCISE.json records its14 postwrite hashes and400ms measured elapsed;
RECIPE.js preserves the executed tool sequence, with explanatory comments
and formatting only. The stage remained HISTORICAL_TEST. The4 main historical
documents reproduce exactly, and all9 authority/policy objects plus SUMMARY
have checked whole-file commitments. No physical-host timing, network latency,
full semantic readback, remote installation or token-release cost is included.

## Decision and remaining work

Prefer this pretested single-call candidate staging over ad hoc composition
inside a future holder interval. Moving the actual same-unit identities wholly
before admission would change the existing checks: PID/start/invocation and
the9 leaf identities belong to the admitted holder's cgroup. Such a different
protocol was not designed or authorized here. Neither a longer holder limit
nor another unprepared caller is selected.

FIRST review of the small new transport and its boundary is the next gate.
Even acceptance supplies only an offline tool: fresh preparation, actual
observations, semantic qualification, complete final readback, installation,
liveness and separate ROOT release are still required. No source run or
worker follows automatically. Source baseline/place/rank remain absent,
all-r F10 is unresolved, and even all-F10 would not resolve JC2.

QUANTITY: exact14-output byte preservation and rejection of the specified
transport errors, not source zero. CHEAPEST TEST: the completed documentary
tests and actual single-call staging; next independent code/boundary review
is planned15--20minutes UNMEASURED. No new canonical OPEN. No Fable/AWS use
for production of this packet, scientific subprocess, protected access or
shared runtime modification. Prior mathematical stops and cadence remain.

## COLLISIONS

status: EMPTY

- Own report/manifest/box were absent before creation; the private candidate
  directory was freshly allocated by mktemp. No existing artifact was replaced.
  This is an owned-target check, not a corpus novelty or mathematical claim.

Final own WHOLE and current22-file pin inventory completed before this marker;
all14 retained staged hashes rechecked. PINS.json records exact read scope.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6463`.
- Body SHA-256:
  `4e4afd6d43c7552930f86f8c5311fc7d32dd0ab528342b6191b41d4a284171a7`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
