# Independent hostile gate: all-parameter F2 SOURCE necessity arrow (Fable 5.1, 2026-09-09)

Status: **NECESSITY-ONLY SOURCE GATE, complete.** Reviewed producer `f2-all-parameters-source-interface-astra-20260909.md` (lane copy SHA-256 `097d765044f1b1a743e53126b4e985d0975ef6b428bbe6159b084e3e17bbd64a`, body `1371c1b3…`, 21714 body bytes) from `/tmp/jc2-lane.9dZetY/inputs`. Launched 09:05:21 UTC by root release; stop by 09:30 UTC. All eleven charged files read whole (pins in the owned box, `input-pins.sha256`). ZERO mathematical subprocesses; every identity below was re-derived by hand. No old checker replay, no AWS/SSH, no agent, no shared edit. No seal and no charge_basis authored here. Frozen basis `0d39df3c9fd69c939a8420c54d03228b9077777d`.

Reviewed statement (necessity only): for every q>=0, m=q+2, n=2q+3, every ordinary rectangular ACTUAL standard F2 pair (P0,Q0) over a characteristic-zero K with the literal antecedents A0=(5,20), A0'=(1,0), direction (5,-1) type II.b, A1=(7/5,2), is carried by the finite map (6) of the producer to ordinary A,B over a finite extension K' with entire tops H^m,H^n, H in the two displayed quintic forms, and [A,B]=c g², c!=0. Neither existence of such a pair at any q nor any local exclusion is a premise or a conclusion.

## Verdict table

| target | verdict | first basis |
|---|---|---|
| A. source/ring/field interface | **CONFIRMED** (imported tier: GGV Cor 5.7, GGHV Thm 2.20(8)/(2.5)) | rectangle + (5,-1) face force pure corners; Prop 1.13 + Prop 2.1(2b) give R=u r(uv^5) in L; (2.5) with A1=(7/5,2) forces m_lambda=2m, hence multiplicity exactly 2 in r; A0'=(1,0) forces r(0)!=0 |
| B. centering and root normalization | **CONFIRMED** | own ODE: a0=Au+B, 15mA=1, (u+r0)f'=5m f, same r0 for h by n f'h=m f h'; both shears keep the whole (5,-1) face and every corner; a in K(a) only |
| C. quartic Euler classification | **CONFIRMED** | own two-variable bracket gives (1) with RHS r; deg f=3, f(0)=-1, (2) at each root; both cases solved by full coefficient comparison, cubic term cancels identically; endpoint (4,16), q=5 for every m |
| D. opposite-edge transport | **CONFIRMED** (imported tier: Prop 7.3/Cor 7.4 "mimic", 1605 Cor 1.6 via Newton–Puiseux) | common edge by Prop 1.13 + Prop 2.1(2b); positivity on the whole arc by the half-plane argument; integer k in {2,3}; unique Euler element after cuts by ord_y; i<=4j from the supporting line with kappa>=4 |
| E. single x^-5 cut, T4, whole pair ordinary | **CONFIRMED** | (i,j)->(i-5t,j-t) keeps (5); T4 exponent map (4j-i,j) unimodular; H computed both cases; map (6) determinant a g² |
| F. Jacobian/scaling/scope | **CONFIRMED** | c=c1/(alpha beta), c1=a c0; tau-scaling exponent 4-5(m+n); the "coprime m,n>1" generalization is within the proof; all-q conditional follows; nothing stronger is claimed |

Overall scoped result: **CONFIRMED** as a conditional necessary arrow at the ordinary rectangular actual-F2 scope, with the named imports retained at their tier. No REFUTED item; no GAP in the necessary direction. No family retirement, reverse sufficiency, degree selection or JC2 conclusion follows, and none is asserted.

## A. Interface (CONFIRMED)

Hypotheses actually consumed, all literal in the producer's §1: P0,Q0 in K[u,v], [P0,Q0]=c0!=0; support in [0,5e]x[0,20e] with (5e,20e) attained; the starting triple with A0=(1/m)en_{5,-1}(P0)=(5,20), A0'=(1/m)st_{5,-1}(P0)=(1,0); (A0,(5,-1)) a regular corner of type II.b of the standard (m,n)-pair (GGHV Thm 2.20(13), t=0 because l_1=5); selected child A1=(7/5,2).

Own checks. Definition 4.3 (excerpt lines 711-731): v_{1,1}=25e and v_{1,0}=5e ratios m/n, v_{1,-1}(en_{1,0}P0)=-15m<0; standard needs v_{1,-1}(st_{1,0}P0)<0, which is NOT given by the rectangle alone but follows from the face: v_{5,-1}(P0)=5m forces j=20m at i=5m, so the top-u coefficient is the single corner. gcd(q+2,2q+3)=1 for every q>=0; GGHV Def 3.3/(3.20) index j in N0 (excerpt lines 1184-1243), F2 row line 1367ff. Corollary 5.7 (excerpt lines 1052-1075) is what supplies (5,-1) in Dir(Q0) with en_{5,-1}(Q0)=(5n,20n); the rectangle alone would allow v_{5,-1}(Q0)>5n (e.g. a term u^{5n}). This import is indispensable and correctly named by the producer. Prop 1.13: 5m+5n-4>0 so the faces commute; Prop 2.1(2b) with tau=5m, mu=5n: P face = alpha0 R^m, Q face = beta0 R^n, R in L by the last clause. Support of R on 5i-j=5 between (1,0) and (5,20): R=u r(uv^5), deg r=4, r(0)!=0 because st(R)=(1,0)=A0'. (2.5): A1=(1,0)+(m_lambda/m)(1/5,1)=(7/5,2) gives m_lambda=2m exactly; z^5-a_i has five distinct roots for a_i!=0 so multiplicity in r(z^5) equals multiplicity in r; patterns with a part equal to 2: (2,1,1),(2,2) only. Endpoint conventions audited by Remark 1.7 cross products: (5,-1)x((5m,20m)-(m,0))=104m>0; after the swap (-1,5)x((0,e)-(20e,5e))=104e>0. Polynomial clauses used: Thm 2.6(1) at (0,1) and (5,-1); Prop 2.1(2b) last clause; 1605 Prop 2.1/Cor 1.6 only at the first, still polynomial, cut. Laurent clauses: Cor 7.4 and the cuts live in K'[x^{±1},y]. The bare Definition 4.3 Laurent-standard scope is not covered and the producer does not claim it.

## B. Centering (CONFIRMED)

Thm 2.6 at (0,1) in V_{>0}, v=20m>0, P0,Q0 in L, so F=v a0(u), a0 in K[u]. Own bracket: [v a0, f v^{20m}]=v^{20m}(20m a0' f - a0 f') = f v^{20m}. Leading coefficient of the left side for deg a0=d>=1 is 5mA alpha(4d-1)!=0, so degree 5m+d-1 must equal 5m: d=1; d=0 impossible. Then (Au+B)f'=(20mA-1)f, top coefficient 5mA=20mA-1, A=1/(15m), 20mA-1=1/3, hence (u+r0)f'=5m f with r0=B/A, f=alpha0(u+r0)^{5m} (partial fractions in char 0), r0 in K from the u^{5m-1} coefficient ratio. Bracket of the (0,1) faces vanishes by Prop 1.13 (20m+20n-1>0): 20n f'h=20m f h' so h'/h=5n/(u+r0), same r0. Shear u->u-r0: monomial (i,j) spreads to (i',j), i'<=i, strictly lower (5,-1) weight for i'<i, so the whole starting face, R, A0, A0', A1 are unchanged; rectangle kept; both top coefficients pure. Diagonal u->a u with a the selected double root of r (a in K(a)): r(aw)=a^4 rhat(w), face scalars alpha=alpha0 a^{5m}, beta=beta0 a^{5n}, c1=a c0. No fifth root, no descent to K asserted, and in the (2,2) case rho lies in K' automatically because (w-rho)^2=r/(w-1)^2 in K'[w].

## C. Quartic Euler ODE (CONFIRMED)

Thm 2.6 at (5,-1) in V_{>0}: [F,alpha R^m]=alpha R^m gives [mF,R]=R. Weight-4 monomials with j>=0 satisfy 5i=4+j, so i>=1 even in L^{(1)}: mE=uv f(w), f a polynomial. Own bracket with w=uv^5: d_u(uvf)=v(f+wf'), d_v(uvf)=u(f+5wf'), d_u(ur)=r+wr', d_v(ur)=5u^2v^4 r'. Then [uvf,ur]=u(4wfr'-5wf'r-fr), so (1) holds with RHS r, not r/3 (the 1/3 of the accepted (3,5) chain is the m=3 specialization). Division by r: pole at a root lambda!=0 of r unless f(lambda)=0. Leading coefficient c_f(16-5d-1)=c_f(15-5d) at degree d+4 versus deg r=4: d=3 (d=0 forces f=0). w=0 gives f(0)=-1, so st F=(1,1), en F=(4,16)=(4/5)(5,20), q=5 for every m. At a root of multiplicity e: (4e-5) lambda f'(lambda)=1, so every root of r is simple in f.
(2,1,1): three residue equations; eliminating C gives a1(a1-b1)=3(1-b1) and its mirror, subtraction gives a1+b1=3, back-substitution a1^2-3a1+3=0 so a1b1=3, (1-a1)(1-b1)=1, C=1/3. The residual (1)/r-1 is a polynomial of degree <=2 (the w^3 coefficient 16C-15C-C vanishes) with three distinct zeros, so the ENTIRE ODE holds; f(0)=-C a1 b1=-1 consistent. Discriminant -3: roots distinct, nonzero, !=1.
(2,2): (3) expanded with S=1+rho: w^3 coefficient 6C-5C-C=0 identically; w^2: 5D+3SC; w^1: -2SD-6 rho C; w^0: -rho D=1. So D=-1/rho, C=(1+rho)/(3 rho^2), S^2=5 rho, i.e. rho^2-3rho+1=0; rho!=0,1,-1, both roots kept, f(0)=-1 consistent. Selecting the other double root gives 1/rho, which satisfies the same quadratic. Repeated-root, unit and exceptional cases are therefore exhausted: r=(w-1)^2 h, h in {w^2-3w+3,(w-rho)^2}, h(1)!=0.

## D. Opposite-edge transport (CONFIRMED, imports retained)

After (u,v)=(y,x): bracket -c1; C_e=(20e,5e) is the unique max-x point (pure top-v coefficient from B) and unique max-y point (pure top-u coefficient from A). Cor 7.4 preamble: v_{1,1} ratio 25m/25n, v_{0,1} ratio 5m/5n. d0=(-1,5) in V_{>=0} and in Dir(P), value 5e; st_{d0}(P)/m=(20,5), 5<20; F: st=(16,4)=(4/5)(20,5) (sign flipped to keep [F,l(P)]=l(P)); p/q=4/5.
Common edge. The edge arriving at C_e counterclockwise has d=(rho,sigma) with (1,-5)<d<(1,0) (normal cone contains (1,0),(0,1), angle<pi). Both P(x,0),Q(x,0) constant would kill [P,Q] on y=0, so some x^i, i>=1, gives 4 rho+sigma>0 for that member's own edge; for d=max(d_P,d_Q) the value is evaluated at both corners, and an arc of angle<pi with both endpoints in the open half-plane 4rho+sigma>0 lies inside it, so positivity holds on all of [d,d0] for both members. Own bracket gap: 5(m+n)(4rho+sigma)-(rho+sigma)=(5(m+n)-1)(4rho+sigma)+3rho>0, so [l_d P,l_d Q]=0; if one face were the corner monomial, Prop 2.1(2b) would make the other a power of a monomial. Hence d_P=d_Q=d, and (rho~,sigma~)<=d for P (and for Q with F_Q=(m/n)F).
Shape. Cor 7.4: l_d(P)=R^{5m} up to a scalar (its proof extracts a qm-th root of gamma2; over non-closed K keep the scalar, which changes nothing below). en(R)=(4,1), v_{0,1}(R)=1, R in L^{(1)}: R=a2 x^4 y+b2 x^{i0}, i0 in Z, rho i0=4rho+sigma forces rho=1, d=(1,-k), k=4-i0; positivity k<4; 1605 Prop 2.1 (self-contained "Jacobian pair in L", (20m,5m), (0,-1)<d<(1,0)) forbids k=1 through Cor 1.6; k in {2,3}. Commutation gives Q's face as a scalar times R^{5n}, same root lambda_k=-b2/a2, which equals (coefficient at (20e-k,5e-1))/(5e*(corner coefficient)) in the current field. Cut y->y+lambda_k x^{-k}: det 1, (i,j)->(i-kt,j-t), d0-weight drops by (5-k)t>0, total degree by (k+1)t, y-degree by t, so the d0 face, both ratios and both unique corners persist; the d face becomes the corner monomial.
Euler persistence. Any weight-4 Z with [Z,R0]=0 satisfies Z^5=const R0^4 (Prop 2.1(2a)); ord_y R0=1 since r(0)!=0, so 5 ord_y Z=4 is impossible: the Euler element is unique in every L^{(l)} and equals the ODE solution, so q=5 persists after every cut. Prop 2.11 is not used at rho<0.
Rational slopes. The next edge d'=max over the union has (1,-5)<d'<d. If 4rho'+sigma'>0 the same three steps (positivity on [d',d0], gap (4), Cor 7.4 in L^{(1)}) force an integer slope strictly between k and 4: only 3 after 2, none after 3; every non-integer rational slope below 4 is thereby excluded and at most two cuts occur. Otherwise kappa''>=4 for the union direction, both values attained at C_e even if only one member has an edge there, and for every support point i-20e<=kappa''(j-5e)<=4(j-5e) because j<=5e. This yields (5): j>=0, i<=4j, -i+5j<=5e for the ENTIRE pair; the zero-valued boundary (1,-4) is not fed to any corollary. Imported tier retained: Prop 7.3/Cor 7.4 (proved by "mimic" of 7.1/7.2, whose own proofs cite omitted Props 3.7, Cor 3.6, Remarks 2.10/3.3) and 1605 Prop 1.2 ([6, Cor 13.15]).

## E. Selected cut, T4, whole-pair ordinaryness (CONFIRMED)

y->y+x^{-5} is an automorphism of K'[x^{±1},y] with det 1 (the chain's fractional v->v+lambda u^{-1/5} is read, not used). It sends w->w+1, so R0'=(y+x^{-5})w^2 h(w+1), homogeneous of d0-weight 5, whole face alpha R0'^m; ord_y R0'=2 with lowest term h(1)x^5y^2, endpoint (5,2). (i,j)->(i-5t,j-t): j-t>=0, (i-5t)-4(j-t)=i-4j-t<=0, -i+5j unchanged, so (5) holds for all of P1,Q1; bracket still -c1. T4: exponent map (I,J)=(4j-i,j), matrix [[-1,4],[0,1]], det -1, so no cancellation is needed and I,J>=0, I+J=-i+5j<=5e: T4(P1),T4(Q1) are ordinary of total degree exactly 5e, whose degree-5e part is exactly T4 of the d0 face. T4(R0')=g^4(p+g)(p/g)^2 h(1+p/g)=p^2(p+g)g^2h(1+p/g); h=w^2-3w+3 gives (p+g)(p^2-gp+g^2)=p^3+g^3; h=(w-rho)^2 gives (p+(1-rho)g)^2. Both monic in p. d(x,y)/d(g,p)=-g^2, so [T4P1,T4Q1]=(-c1)(-g^2)=c1 g^2. Composite (6): u=a(g^4p+lambda2 g^2+lambda3 g^3+g^5)-r0, v=g^{-1}, determinant u_p v_g... = -(a g^4)(-g^{-2})=a g^2, consistent. A later x^{-3} cut would send x^4y to x^4y+mu x with (1,0) outside i<=4j; stopping is load-bearing and the producer stops.

## F. Sign, scaling, scope (CONFIRMED)

Dividing by alpha,beta: tops H^m,H^n, c=c1/(alpha beta)=a c0/(alpha0 beta0 a^{5(m+n)})!=0; A,B monic in p of degrees 5m,5n because lower total degree means lower p-degree. tau-scaling A_tau=tau^{-5m}A(tau g,tau p): bracket c tau^{2-5(m+n)}(tau g)^2=c tau^{4-5(m+n)}g^2; -36 at (3,5) matches the accepted minimal interface. Nowhere is n=2m-1 used: only gcd(m,n)=1, m,n>1, the ratios and the chain data, so the producer's "coprime m,n>1 with this profile" claim is within its proof (possibly vacuous off the printed family, which is an existence question outside scope). The all-q conclusion follows as a conditional for every q>=0 including q=0 (m=2: 2|4, 15mA=1 fine). Manual controls (rectangle mutation, RHS 1 vs 1/3, fractional lattice, x^{-3} cut, (g,g^2p)) are illustrations, not Keller objects; they refute nothing and are not used as evidence. Not established and not claimed: reverse lift, same-degree F2 selection, inner polygons or 3e g-degree bounds for general q, local exclusion, JC2.

## Read perimeter and custody

Read whole: the eleven lane files only (sizes and SHA-256 in `box/f2-all-parameters-source-gate-fable5-20260909/input-pins.sha256`, identical to the charged list). Original paths inside them were not followed; source-input-pins.json used as provenance only. No canonical ledger, protected project, peer body, live gate or F9/F10 material read. Commands run: date, ls, cat, wc, sha256sum, mkdir, git rev-parse, heredoc writes. Nothing mathematical was executed.

## OPENS RAISED

None. No collision: the basenames `f2-all-parameters-source-gate-fable5-20260909.md` and the owned box did not exist before this write (checked 09:18:43 UTC).

<!-- BODY-END -->
