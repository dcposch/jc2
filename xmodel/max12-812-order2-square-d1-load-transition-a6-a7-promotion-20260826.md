# Promotion: complete-source D1 `a=6,7` lower-load transition

Date: 2026-08-26

Status: **PROMOTED AFTER DIFFERENT-MODEL HOSTILE REVIEW.**

## Frozen custody

```text
f3c987b630a2895defabcc1043768497f2e8caad538f58122bb4e17b317c0a9c
  cases/max12_812_order2_square_owner_d1_load_transition_a6_a7_fullsupport_v2_t2_20260826/RESULT.md
eb0058746a406c9d36c11d00c97884e7e937f93cf693763401aa957c432990ee
  cases/max12_812_order2_square_owner_d1_load_transition_a6_a7_fullsupport_v2_t2_20260826/EVIDENCE.sha256
b2a38e4f5c38166789c5d73ecae989c30ed8168c5fc6a58fc3058eb5535e1ad5
  cases/max12_812_order2_square_owner_d1_load_transition_a6_a7_fullsupport_v2_t2_20260826/FREEZE.sha256
3e601cf34eb23270daa067794087155b7179607d27af01f84a60624447f949e3
  xmodel/max12-812-order2-square-d1-load-transition-a6-a7-v2-hostile-review-grok-20260826.md
```

The immutable V1 alignment failure is a negative control.  V2 repaired
exactly the two `k6*C/L` tail-alignment tokens; the review rederived that
repair and rehashed the full source/evidence chain.  Exact Q is the theorem
endpoint; `F_65521` is an independent software control.

## Promoted statement

After the reviewed generic-square first-normal, half-weight, and `M=0`
gates, no finite-order normalized source arc on `D(p*k10)` has

```text
ord_sigma(A)=a,  ord_sigma(C)=a+1,  ord_sigma(R)>=a,
a in {6,7}.
```

The complete two-grade receivers retain all new lower-load timing:

```text
a=6, grades 23,24:  AC ; next(AC)+k10*RC+k60*C;
a=7, grades 25,26:  AC+k60*C ; next block including k61*C.
```

No `k2`, terminal target, or further polar family enters those windows.
After the exhaustive opposite-root allocations (shifted by `k60` at `a=7`),
the simple lower-load terms acquire an extra `L` and the next `C^2/L^2`
numerator still evaluates to `(3/2)*lambda^2*cv^2` at the allocated `A`
root.  The residual face `A0+k60 identically 0` is killed by the same
double-pole degree argument.  The free leading `R` section, including zero,
covers every `ord(R)>=a` in the stated window.

## Firewall

This is arcwise/set-theoretic fixed-band emptiness, not reduced scheme
structure.  It does not cover `a>=8`, another contact or fan face,
positive-order `k10`, `p=0`, `k10=0`, the whole square component, order two,
`(8,12)`, maximum twelve, or JC2.
