# FULL2240 cross — source rank is not source selection

Author: Astra geometry. Strategy only; no theorem promotion or execution authority.
First action 2026-09-11 23:01:10 UTC; original reserve23:16/HARD23:19 unchanged.

## Read scope

CROSS-PINS matched before WHOLE, and all16 entries matched before listed bodies.
The original ten objects are REUSED_WHOLE from this same agent's FULL2240 blind,
completed22:53:36 today: fresh WHOLE then except source-selection.md, itself
reused at its exact original22:33:03 WHOLE pin. This cross reads all four
blinds, postblind history and cross-contract fresh WHOLE. No linked sources,
peer-cross reports, scientific execution or shared edits are admitted.

## Merged mathematical verdict

NO_NEW_CLOSING_MECHANISM. Merge the three trace/Mellin proposals, but not
their unsupported selection arrows. ROOT's local pairing card has hit its
own duplicate stop; its precise product control is not a polynomial source.

Write A=C[p,q], R=C[x,y], n=[FracR:FracA], N=R/A, M=Tr(R),
K0=kerTr, B=C[e_p,e_q], E=FracB. The literal D-module sequence is

    0 -> K0 -> N -> M/A -> 0.

If Tr(f) belongs to A, subtract Tr(f)/n; K0 intersects A trivially.
Trace equivariance and flat B-localization then give the rank identity.
This does not require an A-linear trace section. Finite E-ranks still use
the named holonomic/Mellin premises; they are not consequences of field trace.

The source blind's STRICT positivity is sound at the stated Bass tier.
Indeed (FracA)R=FracR: the former is a finitely generated algebraic domain
over the field FracA, hence a finite field. For n>1 take a nonzero
traceless field element, clear a target denominator, and obtain nonzero
K0. Its injection into Bass-B-torsion-free N keeps it nonzero after
localization. Consequently rho(K0)>=1. Fable's alternative K0=0 is
therefore impossible for n>1, not a useful nontrivial-source shortcut.
Equality rho(N)=rho(M/A) would already force n=1. No stronger comparison
between E-rank and the field degree follows from this argument.

The IC shift can be fixed without a computational sign experiment.
Under the stipulated finite-normalization/de Rham comparison,
H^k(L_i)=H^(k-1)(Dtilde_i). Top right-exactness gives H^2(L_D)=0
from H^2(M)=0. Thus H^1(Dtilde_i)=0. A connected smooth affine curve has
first Betti number 2g+s-1, with s>=1, so its normalization is A1.
These standard comparison/curve facts remain externally trusted, not
fresh primary-verified here. They reproduce polynomial-curve rationality,
not a new properness theorem.

After restricting to the target torus V, the ambient dimension2 sign is
positive but the curve direct-image shift is negative. Hence

    rho(L_i) = -chi(Dtilde_i over V).

It becomes n_i-1 only when Dtilde_i=A1 and neither target coordinate
vanishes identically there; n_i is the finite number of distinct axis
preimages, not intersection multiplicity. If a component lies in an axis,
its torus restriction is empty and contributes0; do not assign it a finite
n_i. Generic axes can remove this issue but change the Euler operators
and must be used consistently throughout. The hand checks pq-1 and
p+q-1 give0 and1 respectively. The first is C*, not an admissible
actual normalization under the preceding H^2 deduction. No Weyl-Gröbner
calculation is needed to check these two signs.

For the merged selection proposal the FIRST unsupported existence arrow
is an actual boundary component with n_i=2, or some other specified
delta-stable quotient line. IC simplicity does not give such an E-line;
a minimal-order source vector may instead lie in K0. Even if a quotient
line E ell is supplied, put delta ell=a ell, lift v, and b=delta v-a v.
A lift of THIS line requires (delta-a)w=-b in (K0)_E. This is an additive
difference-equation obstruction, not an E-linear rank condition. Positive
kernel rank neither forces nor forbids its solvability. A line entirely
in the kernel is another possibility. Polynomial-G/resonant normalization
remains a separate second Bass gap even after a source line is found;
finite-prefix lifting is already discharged.

## Euler redundancy and cheapest-test decision

There is an exact bookkeeping comparison, not a new violated inequality.
Let epsilon(y)=n-#F^-1(y), a constructible deficit supported on actual D.
Finite etale degree n off D and Euler additivity give

    rho(N) = chi(F^-1(V)) = -integral_(D intersect V) epsilon dchi,
    rho(K0) = -integral_(D intersect V) epsilon dchi
               + sum_i chi(Dtilde_i over V).

Thus the numerical expression is ordinary fibre Euler data minus boundary
IC data. Strict positivity additionally uses Bass torsion-freeness and
the actual field extension; it is not obtained by changing an Euler sign.
The packet supplies neither a configuration violating positivity nor
geometric estimates proving positivity automatic from the older Euler
ledger alone. Claiming either redundancy or a new geometric exclusion
would therefore overstate this comparison.

QUANTITY: Delta=rho(K0) for a stipulated ACTUAL source configuration.
CHEAPEST TEST: a single <=600-second UNMEASURED manual comparison, but
only after naming source-specific bounds that force Delta<=0, or an
actual source constraint on the displayed lifting class. None is supplied
by any blind. Decision NOW: STOP/NO_GAIN, no lane, generic-module farm,
larger operator family, assumed quotient line or new canonical OPEN.
The current cross performed the hypothesis/sign audit instead.

## Allocation and terminal checks

The support-degree warning survives. DS-INV supplies the dominant etale
degree2 selfmap E_T=j q with E_T*omega=2omega, preserving each exact
Laurent support. A scalar pair defines a dominant generically finite map
of degree d; iteration and rescaling its second coordinate give degree
2^k d with the SAME supports. Finite function-field degrees multiply;
ordinary coefficient/total degrees need not stay bounded. Thus a universal
covering-degree ceiling for that fixed profile would itself exclude the
first pair. This neither constructs that pair nor refutes bounds involving
additional data not preserved by E_T. It strengthens the warning against
support-only search completeness, not the case for a larger construction run.

Proof rankings differ because reach and readiness differ: actual trace
structure has all-map scope but no contradictory source condition; the
complete F10 program has the nearest finite certificate but no qualified
present input; G2 still owes global landing/type/cofinality. Keep this
distinction instead of choosing by model votes. For construction, S/T is
the compact independent sufficient endpoint; a COMPLETE characteristic-zero
source point has a finite algebraic endpoint. Neither has a candidate.
Fable's necessary-row runtime is NOT a properness test for the complete
source: its implication is one-way. AS109 still lacks polynomial support.

Reject another F10 shot NOW. It needs an independently qualified full
release, source agreement, guards and semantic T rejection, not merely
a different launch style. Trace-card completion would not provide these
prerequisites. A297minor remains the same conditional finite experiment;
failed rank/rows/caps remains NONDECISION. No worker, cap increase, new
runtime code or mathematical software is selected. Existing authentic
output, if later obtained, may inform the accepted compression test;
residue-only support zeros cannot replace exact-B hypotheses.

All packet-curation proposals merge into ROOT's already-owned documentary
cleanup. No extra systems lane: preserve46 IDs, live arrows, stops and
tiers while measuring byte reduction and scope omissions. It is not a
runtime controller or permission to treat indexes as proof. Keep Astra
primary and selective Fable FIRST, not occupancy or an echo-review farm.

Survivors suitable for a single different-model FIRST ONLY if ROOT elects
to bank new mathematics are the strict-positive kernel composition and
support-degree consequence, with their named external tiers and conditional
existence scope. This cross itself promotes nothing. The sign audit and
failed shortcut do not justify another gate alone. No other lane offers
a genuine new actual object/test NOW; preserve the uniform source and
construction questions rather than filling capacity.

Own collision/OPEN audit: no fresh theorem status, source point, measured
wall estimate, launch or global JC2 claim. Original inputs/blinds remain
unchanged. Final WHOLE readback and all17 postpins precede BODY-END;
only transaction/custody follows sealing.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `8578`.
- Body SHA-256:
  `bc7703dfe166c08788cf8fb011ac7f50c81e4bd539d243b8d355f91f4beb5772`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
