# Ideation 2026-09-06T00:00Z — grok46 (blind sealed)

Lane `ideation-20260906T0000Z-grok46`. Frozen receipt
`xmodel/ideation-20260906T0000Z-grok46.run.v2`, basis `de78ca950def644a22c1bf5b6c877be8d043d0de`.
Notes: `box/ideation-20260906T0000Z/grok46/notes.md`. No ledger, no `jc2-lean`, no other
`ideation-20260906T0000Z-*` body, no artifact tree.

**Custody.** Manifest built with `awk` from the receipt's paired
`charged_input_<i>_sha256=` / `_basename=` lines, prefixed by `lane_inputs_dir`;
`sha256sum -c` returned **12/12 OK**. Xu 2016 was opened from
`refs/xu2016_intersection_numbers_split_minor_roots_arxiv1604.07683v4.pdf` only to
print equation (7.1), Lemma 7.1 and Corollary 7.5 (packet-named candidates); it is
not a charged lane file and not an ideation submission.

No new exit-price assertion, so no FALLACY-v2 `charge_basis=` line.

---

## 0. Verdict

Every remaining obligation is the non-nilpotence of one scalar on one necessary chart.

| Obligation | Coordinate | Weight | Chart | How to decide (not a truncated Jacobian band) |
|---|---|---|---|---|
| (99,66) δ=2 | `ρ` (or `a` in `π(π+3a)²(π−2a)`) | K3-lead `t⁹` | two-point, `ρ≠0` | Xu (7.1) holds by §8 (8.2); the next `t`-jet of (7.1) is the first inhomogeneous `J=1` row on the face |
| (99,66) δ=5/2 | `c` in `p=π(π²−c)` | same, `τ`-units | two-point, `c≠0` | (7.1) holds *identically* (`q₁=−2∫p³`); (7.1)+1 decides `c` |
| D=108 δ=3 (R012) | `c` in `(π−μ)²−c` | K3-lead `t⁸` | free-mean two-point, `c≠0` | one roster leaf `ρ=3`, `λ=[1,1]`; Cor 7.5 threshold `8/3<3`, order-1 semigroup does not kill |
| other family-C ES | leading modulus of `p(π)` of shape `λ` | face at exact `ρ=P/Q` | split-window leaf | Xu (7.1)+Cor 7.5, univariate. Roster: **36** leaves; residual65 prices 38 — do not invent two |
| family A/B receivers | child Jacobian unit `T` in `Tc−1` (or child T2-leader `λ'`) | 0 in Appendix-II; T2-leader at `L′−D′₂` | G_i receiver, min 199 unk | Moh p.207–210 Thm 1.2 truncation, not the 199–4889 dump |
| R009, R050 (margin 0) | exact *final* major `δ_σ`, not Def 5.1(3) radius | one Galois step on the major face | Xu Thm 5.1 equality | extra major split strictly decreases `IM`; margin 0 dies |
| K16 (R) | `T₂=3b²w₃+18Bw₂−10η²` | `4t` | `J_t` | degree-`N` closing of the nondegenerate L-recursion lies in `(T₂)` |

**Q1.** Band charts are retired below `T_0=n+m−2`. Characteristic-degree rows break `Δ`
(Theorems A/B) but were adjoined to ~2500 generators and died compute-bound. The
non-truncation kill of a split branch is Xu's face Jacobian (7.1) on the typed ES
leaf, plus the single next `t`-jet that sees `J=1`. The ring is univariate in `π`
plus `O(u_s)` moduli. Where (7.1) contradicts the Galois shape, the leaf dies in
minutes. Where it holds identically — (99,66) δ=5/2, possibly D=108 δ=3 — that +1
jet is the distinguished row for `c`.

**Q2.** Every printed arithmetic predicate is a Prop 6.3 fixed point (residual65,
46/46 complete rows). The condition that breaks invariance is Xu Theorem 5.1 as an
*equality on final major π-roots*. Inversion transports the selected minor cluster,
not unselected major-final `δ_σ`. Two targets at `n≤200`: R009 `(192,128)` and R050
`(196,56)`, both family A, both margin 0. Child source-support *arithmetic* is
transported; the child's *chart* is the receiver (Q1).

**Q3.** Abel / residues / involution stand as negatives. The surviving route is the
global closing `l_{N+1}=0` of the nondegenerate L-recursion, as an element of the
4-jet ideal `(T₂)`. Equivalence `T₂=0 ⇔ L_{\mathrm{pivot}}=0 ⇔ j_0≥3 ⇔
\mathrm{ord}_0\mathrm{Disc}_Y(Q_P)≥6` is tacnode Theorem 5.1. Missing: every
polynomial `L` of degree `N` empties the nondegenerate alternative. `(T)_8` gives
`T₂∈\mathrm{rad}\,J_t` for `t≤8` as a cone-corollary; that is not the uniform
statement.

---

## 1. What is already decided (charged)

**Cone-vertex.** On `Δ`: `F=h_2^3+bh_2+c'`, `G=h_2^2+dh_2+e`, `J≡0` by `2n=3m` and
the block degrees. Positive-degree Jacobian rows vanish; pole rows are `h_2`-only,
consistent to full depth (pivots 102/76/94, residual empty, raw-substitution
witnesses). `T_0=n+m−2` is 178/163/163. The engines' leading-pole `=0` (no target)
is a spurious unit at stages 92/60/77/50. Band truncation is not an instrument.

**Char-degree.** Theorem A = Moh Prop 2.2 p.152: `deg_y T_i^ψ=D_i` with scalar-unit
leader for `M_i<e`. Clients: `D_2=55,63`, leader depths 143/153, *before* `J_0`.
Theorem B: those degrees plus actual D2 faces `(π³−1)^{24,16}` / `(π⁴−1)^{21,14}`
exclude every `J≡0` point (`k_0\mid\gcd(n,m,D_2)` vs face multiplicities 24 or 21).
Finite augmented-chart decision: compute-bound OPEN (2535–2931 gens; 34 time-bound,
5 memory-bound, zero accepted units). D=108 branch-exclusion needs the repaired
free-mean chart, not the inherited mean-zero restriction.

**Residual 65.** Families A 23 / B 22 / C 20 after Xu Cor 5.3 removes R001. Charts
min 199, median 883, max 4889; none < 70. Second arithmetic generation kills 0.
Family C: 20 prefixes, D2 remains, split-window `TYPED_ES_LEAVES_PLUS_D2_BRANCH`.
Roster lists 36 `ES_NECESSARY_LEAF_NOT_ATTAINMENT` records; residual65 prices 38.

**K16.** `(T)` for `t≤8` (H2b `[1]` at p=32027, properness). Uniform gap = `T₂`.

---

## 2. Q1 — non-truncation instrument for split branches

### Q1-A (primary). Inhomogeneous Xu face-Jacobian on each typed ES leaf

**Statement.** Let `(f,g)` be a monic Jacobian pair, `u_s≥2`, and let `σ` be a
`π`-root of the principal minor cluster at exact contact `δ=ρ=P/Q` in the roster
window `1<ρ<v_s/u_s`, `1≤Q≤u_s`, with face polynomial `p(π)` of Galois shape `λ`.
Write `T_s` for the last effective approximate root. Xu p.11, after Moh Prop 4.1:

```text
(7.1)     ∂(T_s(σ), g(σ)) / ∂(t,π)  =  −J · (T_s)_f(σ) · t^{−2+δ}.
```

If `J` is a nonzero constant, the leading `t`-coefficient is the homogeneous face
ODE (Xu §8; residual65 §5(e)). The coefficient of `t^{−1+δ}` is an inhomogeneous
polynomial in the 1-jet of the arc and in the face modulus (`c` or `ρ`).

**Claim.** On every roster leaf except those where the leading coefficient of (7.1)
vanishes identically, the leading ODE forces `p` to be a pure power, contradicting
the leaf's distinct-root shape: the leaf is empty as a necessary first-separation.
On the residual identically-satisfying leaves — Xu §8 names (99,66) `δ=5/2`, with
`q_1=−2∫p^3` and `p=π(π²−c)` still free — the `t^{−1+δ}` coefficient is the
distinguished row for `c` (resp. `ρ` on `δ=2`, where (7.1) holds by the two-root
identity (8.2) and does not force `ρ=0`). Nilpotence of that scalar collapses the
leaf to a one-point (D2) germ, already carried as the alternative.

This is not a truncation of the two-point coefficient chart: no outer block, no
Jacobian band at `t`-power `n+m−2`, no `h_2` pole schedule.

**Printed sources.** Xu (7.1); §8 (8.1)–(8.2) for (99,66) data
`n=99,m=66,M=(77,97),V=(8,8),δ₂=1/3,−μ₂=55,−μ₃=145`, and the statement that `δ=5/2`
stays open because (7.1) holds for any cubic `p`. Lemma 7.1 (planar semigroup) and
Cor 7.5 (`u_s>1` and `δ<(v_s+1)/(u_s+1)` ⇒ cannot split to `u_s` distinct roots).
Moh Prop 2.2 p.152 licenses replacing `T_s` by `T_2` (degrees 55/63) in (7.1) along
the same arc. Roster: R015 `ρ∈{2,5/2}`, R012 `ρ=3`, `λ=[1,1]`; Cor 7.5 thresholds
`9/4=2.25` and `8/3≈2.667`. Surviving leaves sit on or above the threshold, or
split to fewer than `u_s` roots (R015 `ρ=2` has `λ=[2,1]`). Cone-vertex: the face
ODE is homogeneous and cannot see `J=1`; the +1 jet is that inhomogeneity.

**Instrument.** For each of 36 roster leaves, in `k[π,c]` (or `ρ`), localizer
`Zc·c−1` (no `sat()`): (1) build `p` of shape `λ` from the roster fields; (2) impose
leading (7.1) as a differential polynomial in `(p,q)` of degrees `u_s` and
`u_s(−μ_s+n−2)/d_s+1`; (3) if the leading coefficient is a unit in the localized
face ring, the leaf is empty; if identically zero, extract `[t^{−1+δ}]` and reduce
again. Positive control: Xu §8, `δ∉{2,5/2}` must be a unit. Negative control:
`δ=5/2` leading coefficient must be the zero polynomial; the +1 jet must be a
*nonzero* polynomial in `c`, or the proposal is false as a kill.

**Cost.** Univariate, `u_s≤5`. Seconds to a few minutes per leaf over `Q`. No fleet.
Disk: one JSON of 36 residues.

**Refute.** (i) A roster leaf whose localized leading (7.1) ideal is proper with
Galois shape intact. (ii) On (99,66) `δ=5/2`, the `t^{−1+δ}` coefficient vanishing
identically in `c` with `Zc·c−1` still consistent — then Q1-A does not decide `c`.
(iii) A leaf outside the roster predicate (`Q>u_s` or `ρ≥v_s/u_s`) is out of scope.

FALLACY: (7.1) holding is necessary, not a kill; holding identically is the opposite
of a kill. Split-window survival is not joint-chart attainment.

### Q1-B. T2-face matching (chart reduction of the char-degree ring)

**Statement.** Theorem A's binomial targets
`Q_{55}=λ y^{15}(y−x)^{40}`, `Q_{63}=λ y^{14}(y−x)^{49}`
(char-degree (3); Moh Prop 4.5 p.169 + Prop 2.2) are necessary on any realization
chart. On the minor face ring
`R_{\mathrm{face}}=k[\mathrm{jets},\,c\text{ or }\rho,\,\text{free }K2c/Hc,\,λ,\,a,b,c,d,e_0]`
they already exclude `Δ` (`55` not divisible by `33`). Non-nilpotence of `c` is
those same rows on `R_{\mathrm{face}}` localized at `c`, without outer D1/D2
preblocks and without Jacobian bands.

**Reduction.** Cone-vertex entry table: the four `Δ`-constants enter `KF,KG` above
the F/G pole targets and *below* the T2 leader ((99,66) δ=2: `b` at 93, T2 leader
143, `J_0` at 163). So the T2 leader is the first block that sees both the face and
the four constants. Non-constant outer monomials enter T2 rows only when their
`t`-weight plus `ord_t K2(σ)` meets a T2 tag `≥ L−D_2`. Count those monomials;
cap the ring at the ones that meet the 16/15 leader tags plus the char-degree
stage-0 cut (`D≤29,C≤60` on (99,66)). If the count exceeds 80, stop and report it.

**Sources.** Moh Prop 2.2 p.152, Prop 3.1 p.157, Prop 4.5 p.169; char-degree §§2–6;
cone-vertex §3.

**Instrument / cost / refute.** Exact-`Q` emitter of the 16/15 leader rows plus
`Zλ−1`, `Zc·c−1` on the declared face ring; grevlex. Positive toy already in
char-degree r2; negative control = the `Δ` witness, which must force `λ=0`. If
`|gens|≤80`: local Singular, under an hour. If not: emit the count, no GB. Refute:
a proper localized face+T2 ideal with `cλ≠0` (existential face survivor, not a
pair), or a necessary T2 coefficient at depth 143 that uses a deleted outer
monomial.

### Q1-C (chart-avoiding; exact hypotheses; does not kill the 36)

Xu Lemma 7.1(ii) + Prop 7.3: a split of the principal minor at order 1 whose
`T_i`-degrees are `r` times the original `δ`-sequence, `1≤r≤u_s`, forces
`−μ_s−2∈Γ(−μ_0,…,−μ_{s−1})`, contradicting Lemma 7.1(ii). Cor 7.5 extends this to
`δ<(v_s+1)/(u_s+1)` for a split into `u_s` distinct roots.

**Hypotheses.** Jacobian pair; `f` monic in `y` with two points at infinity (Moh
Lemma 5.3 p.185: highest form of `g` has two roots); effective index `s>2`;
`δ`-sequence `(−μ_0/d_{s+1},…,−μ_s/d_{s+1})`; the split-to-`u_s`-roots clause of
Cor 7.5.

**This does not kill the residual leaves.** They live at `ρ≥(v_s+1)/(u_s+1)` or
split to fewer than `u_s` roots. Abhyankar–Moh embeddings of the line ([AM75])
require *one* place at infinity; the split germ is the opposite. Genus of a
degree-55 curve with infinite multiplicities 15+40 gives `p_a=1431`, `δ≥885`,
remainder `≤546` — a floor, not a contradiction. Do not run a genus lane.
h-support at the split face is already Xu §8 (`T_2(σ)=p^5 t^{5(−8+3δ)}+⋯`): input
to (7.1), not a second obstruction.

---

## 3. Q2 — not descent-invariant

### Q2-A (primary). Xu Thm 5.1 on *final* major π-roots; targets R009 and R050

**Statement.** Xu Theorem 5.1 p.7 is the equality
`I(f_ξ,g)=IM(f,g)=(n/(m+n))Σ_{σ∈P_M}|D_σ^f|(1−δ_σ)`
summed over **final** major `π`-roots. The census uses Def 5.1(3) disc radii and a
floor for `Im`. That upper-bounds `IM` only if no major disc splits further. p.200
Theorem (4) licenses extending the tower through any major subdisc. A further major
split increases final `δ_σ` and strictly decreases `IM`. On a margin-0 row,
`IM_{\max}=\mathrm{Im}_{\min}` already; any licensed extra major split yields
`IM<\mathrm{Im}`, which Cor 5.3 forbids.

The two margin-0 rows (residual65 §5): R009 `(192,128)`, `M=(−128,148,190)`,
`V=(2,3)`, `δ=(19/24,5/16,−1)`, child `(48,32;M'=37;V'=2;δ'=(4/3,−1/5))`, 388 unk,
`ℓ=1`. R050 `(196,56)`, `M=(−56,184,194)`, `V=(4,3)`, `δ=(19/28,1/4,−1)`, child
`(49,14;M'=46;V'=4;δ'=(5/7,−1))`, 1392 unk, `ℓ=1`. R011 has margin 1.

**Not descent-invariant.** Prop 6.3 inverts the selected minor cluster (`π↔y`,
`γ↔x`; own-data-gate §3.4). Final major `δ_σ` lives on unselected major discs of
the parent. Residual65 §4: (W), (GO), (B), (SD), child `(12')/(13')` are the parent
transported (`A'_1=A_1^{\mathrm{act}}`, `N'=N`, `M'=M` at `u_s=1`). Exact-final
`IM` is not.

**Sources.** Xu Thm 5.1, Cor 5.3; Moh p.200(4); census-coverage §4 (major-leaf
expression typed as a bound, not attainment); residual65 §5; FALLACY
floor/attainment.

**Instrument / cost / refute.** For R009 and R050 only: at major level `r=2`, list
Galois patterns with a major sibling of multiplicity `>lo` (existing (GO) engine,
1–36 patterns). For each, assign the extended tower's `δ` (Def 5.1(3) on the
sibling) and recompute `IM`. Reject the row if every licensed pattern has
`IM<\mathrm{Im}_{\min}`. Cost: seconds, exact rationals, two JSON objects. Refute:
a licensed pattern on R009 or R050 with exact-final `IM≥\mathrm{Im}_{\min}`; or a
printed line that selected-tower `δ_1` is already final for these `V`-vectors. Do
not apply the unprinted `V_s/u_s−1` floor at `u_s>1` (48 diagnostic kills, not
licensed).

### Q2-B. Theorem A attainment, uniformly, as a coefficient condition

Moh Prop 2.2 is not a predicate on `(n,m,M,V)`. The number `D_i` is
descent-invariant; the equality `deg_y T_i^ψ=D_i` with unit leader is not — a
specialization may drop degree (`Δ`: `kl` vs `D_2` not divisible). So “`λ` is a
unit” is a uniform necessary condition on all 65 and is not a sieve fixed point.

On family A/B this is the extra receiver generator `T`
(`generators_including_Tc_minus_1`). On family C it waits on prefix completion.

**Cheap slice.** Moh Appendix II p.207–210 decided the five `n≤100` children by
Thm 1.2 coherent truncation of `σ*`, 244/373/202 → 17/22/10 coefficients. G_i
receivers recover the *unreduced* counts (199/370/241) and stop. The missing
reduction is printed. First new target after Moh's own R002/R003 controls: R005
child `(27,18)`, 307 unreduced. Stop and type OPEN if Thm 1.2's accuracy cannot be
written for `δ'_{s'}≠−1` (R009 has `δ'_2=−1/5`). Refute: R002 reduced and empty
*before* Moh's last cancellation (over-restriction, the historical (99,66)
disease).

A second-place invariant at `y=∞` that is not the child: Lemma 5.3's complementary
root has multiplicity `n u_s/d_s=n'`. At `u_s=1` that *is* the child (transported).
At `u_s≥2` it is the D2 alternative, not a new arithmetic predicate.

---

## 4. Q3 — T₂ after three dead routes

Tacnode Theorem 3.1: `b≠0`, `N≥3`, `Q_P` has exactly one polynomial root `L`; the
residual cubic is irreducible over `k(x)`; `L̃` is algebraic of degree 3, never a
polynomial, never equal to `L`. Involution, ∞-branches, and termination of
`j_0∈[2,3N]` produce no contradiction. The OPEN is

```text
T₂ = 3b²w₃ + 18 B w₂ − 10 η² = −2(6P₀P₄ + 9P₁P₃ + 5P₂²)
```

with `T₂=0 ⇔ L_{\mathrm{pivot}}=0 ⇔ j_0≥3 ⇔ ord_0 Disc_Y(Q_P)≥6` (tacnode Thm 5.1;
exact `t=3,4`, modular `t=5`). `T₂∈J_t` fails non-vacuously at scheme level. The
statement lives in the radical and must see polynomiality of `L`. `(T)_8` implies
`T₂∈\mathrm{rad}\,J_t` for `t≤8` as a cone-corollary; next ray index after `t=8`
is `t=17` or `26` (`N=3d^2`), not a 12-hour GB.

### Q3-A (primary). The degree-N closing lies in `(T₂)`

**Statement.** On the nondegenerate locus `L_{\mathrm{pivot}}≠0` (`j_0=2`, the
alternative Thm 3.1 did not exclude), the dual recursion
`[x^j]D=(3/4)b² l_j+f_j` (unit in `l_j`) determines every `l_j` for `j≥2` from the
4-jet of `P` and from `b`. Polynomiality of `L` is `l_N=1/y` and `l_j=0` for
`j>N`. **Claim:** in `k[b,B,η,w₂,w₃]` localized at `b L_{\mathrm{pivot}}`, the
first closing `l_{N+1}=0` already lies in `(T₂)`. Hence there is no polynomial `L`
of degree `N` on the nondegenerate locus, so every polynomial solution has `T₂=0`.
With tacnode Thm 5.1 this is `(R)`.

**Hypotheses.** `b≠0`; `(UF)` with the marked jets of tacnode §1; `N=deg L=t+1≥3`;
nondegenerate recursion; no involution, no residues at ∞, no Abel.

**Sources.** Tacnode Prop 2.3, Thm 5.1, §4; `(UF)` `(θ−3)(P²)+GP=R`; minpoly
`3d²=N`; frozen `controls_t{3,4}_raw.sing`.

**Instrument.** (1) *Lean-checkable identity:*
`L_{\mathrm{pivot}}^2+4T₂+24E_2=0` in the free 4-jet (K=10 ALL_PASS). This is the
equivalence, not `(R)`. (2) *Membership, t=3 and t=4 only:* on the frozen `Q(d)`
standard bases (vdim 66/338, socle 14/21), eliminate `c_1,…,c_{t−1}` onto the 4-jet
and test whether the elimination ideal is `(T₂)` (or `(T₂^n)` with `n·4t≤` socle,
vacuous/non-vacuous as in tacnode §6). At `t=3`, `wt T₂=12≤14` and `T₂∉J` is
already recorded: the test is generation of the *elimination*, not `T₂∈J`.
(3) *Generic-N tail:* `l_{N+1}` along the nondegenerate recursion, reduced modulo
`T₂`; remainder zero as a polynomial identity proves Q3-A for all `t`.

**Cost.** (1) a degree-4 identity. (2) seconds / ~10 s, already timed. (3) free-jet
to order `N+1`, comparable to the K=10 driver. No `t=9` Gröbner, no new H2b.

**Refute.** A free-jet point with `T₂≠0` and `l_{N+1}=0`. Or at `t=3` or `4`, a
4-jet elimination form of weight `≤` socle that does not vanish when `T₂=0`. A
modular `t=5` point is a control, not a promotion. Do not argue `3\mid j_0` from
`N=3d^2` (equivalent to the claim). Do not use `(T)_t` and call it uniform.

### Q3-B (Lean-checkable, strictly weaker)

The four equivalences of tacnode Thm 5.1 as 4-jet polynomial identities, plus
`[x^4]\mathrm{Disc}(F_0)=−16T₂/(9b²)`. Hypotheses: `b≠0`, jets through order 4,
`E_2` the fourth `(UF)` row. Not `(R)`. Cost: one identity file. Refute: mismatch
with `tacnode_involution_K10.json`.

---

## 5. Top-3 avenues, next 12 hours

Disk: 2.9 GB free. No fleet copies, no CAS dumps. Each lane: a report plus JSON
≤ 1 MB.

**1. `xu-71-es-leaves` (Q1-A).** Only instrument that can move family C without the
Gröbner wall; it is exactly “is `c` nilpotent on the necessary face?” Input:
roster leaves; Xu (7.1)/Cor 7.5; R012 and R015 first, then the other 18 `u_s≥2`
rows. Ring: `k[π,c]`, `Zc·c−1`, declared `(t,π)` as in Xu §8. Success: a unit on
every leaf of at least one family-C row (that row becomes D2-only prefix, still
not a pair); or a +1-jet polynomial in `c` on (99,66) `δ=5/2` whose only localized
root is `c=0`. Fail-closed: if the +1 jet on `δ=5/2` is identically zero, type
`OPEN[XU71-PLUS-ONE-VACUOUS]` and stop. Resource: one local CPU, exact `Q`, ≤ 4 h.
Controls: reproduce Xu §8.

**2. `t2-face-cap80` (Q1-B).** Theorems A/B exist; r2 died on the wrong ring. Input:
char-degree targets (3); cone-vertex free K2c lists (16/27/7 leftovers); D=108
*repaired free-mean* chart. Success: verified unit of the localized face+T2-leader
ring (exact-`Q` replay, controls `[0,1]`, tags = coefficient-minus-target), or a
proper ideal typed as an existential face survivor (`J` may still have degree
≤20/25). Fail-closed: `|gens|>80` after the support cut — emit the count, OPEN, no
GB. Resource: local Singular, ≤ 6 h; abort if RSS > 8 GiB.

**3. `k16-t2-closing-t34` (Q3-A).** `(T)_8` is done; the uniform statement is the
K16 remainder; frozen `t=3,4` bases exist; a negative answer kills Q3-A in minutes.
Input: `controls_t{3,4}_raw.sing`, identity map on `(c_i,b)`, minpoly `3d²−N`.
Success: 4-jet elimination equal to `(T₂)` (non-vacuous against socle 14/21) plus
`l_{N+1}≡0\pmod{T₂}` in free jets. Fail-closed: nonzero remainder →
`OPEN[K16-CLOSING-NOT-IN-T2]`, no `t=9`. Resource: local Singular, ≤ 3 h.

**Not in the 12 hours.** Deep band engines; 2530-generator char-degree replay;
second arithmetic generation; genus; `s≤5` cap; unprinted `u_s>1` Xu floor; any
other 00:00Z body.

---

## 6. FALLACY-v2

- **Necessary ≠ sufficient.** Xu (7.1) on a leaf is necessary; identical vanishing
  is not a kill. Child (W)/(GO)/(B) passing is not a pair.
- **Floor ≠ attainment.** Census `IM_{\max}` is a bound. `T_0` is exact on the
  corrected row set. `D_i` as a number is not Prop 2.2. Margin 0 is not a kill
  until exact-final `δ_σ` moves it.
- **A row needs its printed line.** Every leaf used is a roster object of type
  `ES_NECESSARY_LEAF_NOT_ATTAINMENT`. 36 from roster, 38 from residual65: both
  charged, not merged by analogy. Moh Prop 2.2 is p.152. Xu Thm 5.1 is the
  equality on `P_M`, not Cor 5.3.
- **Flag / place / series.** Face modulus `c`, physical minor place, and cover
  series `π(π²−c)` are not identified; `Δ` is outer coordinates only.
- **Carrier.** Existential T2 family is not a certified canonical `T_i`. A proper
  augmented ideal would be an existential necessary-chart survivor, not a pair.
- **`sat()` / ring map.** Explicit inverse rows `Zc·c−1`, `Zλ−1`. Face ring and
  generator order declared in the lane spec. No modular promotion.
- **Merge-free.** Family C stays prefix; no `M'_{s'+1}` invented.
- **Target / arrival.** `D_2` is not the band stage, not `T_0`, not `n+m−2`, not
  the ES `ρ`.

No `charge_basis` line.

---

## OPENS RAISED

```text
OPEN[ROSTER-ES-LEAF-COUNT-36-VS-38]
  QUANTITY: ES_NECESSARY_LEAF records on the 20 family-C rows.
  STATUS: roster.jsonl enumerates 36; residual65-structure prices 38.
  CHEAPEST TEST: one-line reducer on roster; 10 seconds.
  BLAST RADIUS: none on mathematics; two invented leaves would be a FALLACY miss.

OPEN[XU71-PLUS-ONE]
  QUANTITY: [t^{−1+δ}] of Xu (7.1) as a polynomial in the face modulus, on each
    leaf where the leading coefficient of (7.1) is identically zero. Identified:
    (99,66) δ=5/2; candidates D=108 δ=3 and (99,66) δ=2 (leading holds by (8.2)).
  STATUS: leading identical vanishing is printed (Xu §8); the +1 jet is not.
  CHEAPEST TEST: avenue 1, R015 then R012.
  BLAST RADIUS: if the +1 jet is identically zero, Q1-A does not decide c and the
    two-point chart remains compute-bound. If it forces c=0, those split branches
    die without Gröbner.

OPEN[K16-CLOSING-NOT-IN-T2]
  QUANTITY: remainder of l_{N+1} modulo T₂ on the nondegenerate locus.
  STATUS: proposed; not computed. Equivalence T₂=0 ⇔ j₀≥3 is proved.
  CHEAPEST TEST: avenue 3, t=3 elimination, minutes.
  BLAST RADIUS: a nonzero remainder kills Q3-A and returns K16 uniform to
    OPEN[K16-UF-DEGENERATE-TACNODE] with no new involution route.
```

## OPENS RETAINED

`OPEN[CTOP-EXTRA-CONDITION-E]`, `OPEN[PROP6.3-RADIUS-US>1]`,
`OPEN[CHILD-TERMINAL-SUPPORT-US>1]` (family C after the leaves);
`OPEN[SECOND-GEN-NUMERICS-DESCENT-INVARIANT]`;
`OPEN[K16-UF-DEGENERATE-TACNODE]` (equivalent to `T₂∈rad J_t`; Q3-A is a route);
`OPEN[K16-UF-CONTACT-ORDER-BOUND]` (`j₀≤N` unused; do not assume it);
`OPEN[K16-INTRINSIC-SLICE-UNIT]` (first index without exact `(T)` is now 9; the
uniform `T₂` statement is not a per-`t` GB);
`OPEN[RESONANCE-FILTER-EXACT-FORM]` (one extra `n≤200` row; not this round).

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `23953`.
- Body SHA-256:
  `f18e6caa0bcfa7ca4fef4c9b987294c6cbf553211d13213db81d468907e05363`.
- Frozen basis: `de78ca950def644a22c1bf5b6c877be8d043d0de`.
