# Hostile review request — selected Q8 frozen-`d4` finite-`c` landing unit

Act as a hostile algebraic-geometry/source-fidelity referee.  Work read-only;
do not edit repository files.  Do not run local CAS, Bash, solver, network, or
long Python.  The frozen AWS Singular endpoints are the accepted
computations.

Read the producer report
`xmodel/max12-912-order3-nu-q8-w0-rankdrop-b1-frozen-d4-finite-c-unit-20260825.md`,
the whole frozen result package in
`cases/max12_912_order3_nu_q8_w0_rankdrop_b1_loaded_saturation_aws_20260825/`
whose names begin `PARAM_` or `aws_*_param_`, the pinned compiler and order3
parent, and the reviewed generic-`c`/slope-two reports plus ramified-audit
errata.

Charge explicitly:

1. Verify the source rows, map `d4=1,d2=2+u`, coefficient ring with `c` a
   polynomial variable over `Q`, selected load `w*x5*(x3-2*x5)`, and Singular
   `sat`/contraction semantics.
2. Verify that adding the centre `(w,u,x1,x3,x5)` and obtaining `GL=(1)`
   excludes every finite `c` at that frozen-slice landing, with no hidden
   division by a polynomial in `c`.
3. Refuse the stronger statement `GC=(1)` or global selected-open emptiness:
   the output certifies only `Landing=(1)`.
4. Audit the currently frozen Box02 `std/dp` endpoint: rc, diagnostics,
   source hashes, exact printed unit basis, timing, and software-negative
   controls.  Treat the still-running independent Box03/`modStd` mirrors as a
   custody/redundancy debt, not as evidence already present and not as a
   defect in the narrow one-engine algebraic implication.
5. Enforce the firewall: moving `d4`, full rank-drop/Hsrc, coefficient
   infinity, other coordinate infinity, terminal/Taylor realization,
   trajectories, all `(9,12)`, maximum twelve, and JC2 remain open.

End with `CONFIRMED`, `CONFIRMED_WITH_REPAIRS`, or `NOT_CONFIRMED`.  Name the
smallest remaining exact boundary gate.
