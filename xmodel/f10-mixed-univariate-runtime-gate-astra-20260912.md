# FIRST: mixed-univariate runtime v2

Reviewer: Astra, independent of Sol's runtime derivative. STATIC SOURCE REVIEW;
no interpreter, test, fixture, CAS, worker or network execution. Actual first
action 2026-09-12 20:05:31.263361486 UTC; targets absent then. Fixed publication
reserve 20:19 / hard 20:23 UTC, including custody, never reset.

The thirteen assigned inputs and the administrative finalizer were hash-checked
before bodies. All thirteen inputs were freshly read WHOLE, including the
authority whose earlier author-read provenance is corrected by the erratum.
The science FIRST is imported only at its declared scope; this review audits
the new composition, not the unchanged algebraic foundations.

## Verdict

**CONFIRMED, qualified static composition; no blocking defect identified in
the new derivative.** Execution qualification remains **GAP / ROOT-owned**.
This is not an observed run, an aggregate-cap guarantee, or permission to run.
The disabled template remains false/null. A summary is expressly not ROOT
acceptance and establishes neither source exclusion nor JC2.

| Exact issue | Verdict |
|---|---|
| Six phases, full argv and authority-SHA slot | CONFIRMED |
| Repaired UID/sibling layout and frozen candidate attachment | CONFIRMED under stated installation predicates |
| Parser and both genuine algebraic mutations/inverse restoration | CONFIRMED; successful admission within caps unmeasured |
| CAPRUN exit mapping and fixed dummy acceptance predicates | CONFIRMED within sampled exact-PGID scope, not kernel quiescence |
| Actual native/API, aggregate caps, terminal custody | GAP: expressly external, not proved by a qualification hash |
| Original repair provenance | ERRATUM RETAINED; no retrospective pin claim |

## A. Composition, startup and immutable data

Dispatch lines 7, 99, 113–130 give exactly dummy, parser-negative, produce,
check-positive, identity-negative, exception-negative; the producer and genuine
positive checker each run once. Any unexpected status aborts, not retries.
The producer's ordinary exit 2 for nonconstant generic gcd stops at line 107:
there is no candidate or successful six-phase summary, though ROOT must retain
its inconclusive receipt. A positive checker finding actual exceptions likewise
stops before the mutations. This wrapper deliberately requires a positive
candidate before testing its two semantic controls.

At lines 102–105 the scientific vector is exactly Python, `-E -s -S -B`,
SCRIPT, then the ordered flags. The authority stores `command[5:]`, hence its
slot 6 and full-vector slot 11 are the same SHA value. Only that literal typed
slot is substituted after canonical authority bytes are frozen and hashed.
Authority lines 79–84 reconstruct identical `sys.argv`; no SHA cycle exists.
The wrapper supplies no extra interpreter flags. CAPRUN itself is invoked as
`Python CAPRUN ...`, without those science flags: its actual default startup,
and the dispatcher startup, therefore belong in ROOT's native qualification.

Dispatch lines 71, 79–95 reject root/boolean UID/GID, pin the dispatcher and
seven other executable/source files, and require canonical co-installed
authority/produce/check siblings matching authority's four-source inventory
(those three plus the resolved interpreter). The root0755 mount and root0755
admin/science0700 output siblings fix traversal and rename exposure: with
qualified immutable ancestors, the science UID can read admin0444 inputs but
cannot replace their names. Mount census and fresh-empty children are checked.
Actual tmpfs type/size, absence of nested mounts/aliases/ACL escape, readable
ancestry and same physical bounded filesystem are external predicates, not
consequences of `ismount` or lexical containment alone.

Candidate bytes are bounded-read from producer output, exclusively copied,
fsynced, chmod0444 and reread in admin before checker authority is formed.
Its raw SHA is bound to the producer receipt, checker authority and checked
result. Each mutant gets its own exclusive input and new SHA-bound authority;
neither reuses the positive input authority. All science output/receipt and
child stream paths are on output; caller-created authority/input/summary data
are on admin. ROOT must place the caller's own streams there too. The summary
tests selected receipt fields, not every key of an independent hostile receipt
schema; its authenticity relies on the pinned genuine checker and isolated
writer, whose code writes both receipts before returning.

## B. Exact negative traces

1. The duplicate-key bytes at dispatch117 reach checker12 after the genuine
authority guard. Authority21 raises `duplicate JSON key`; checker128–130
produces exactly exit1, empty stdout, `STOP: duplicate JSON key\n`, no results.
Dispatch118–119 require all of these. An input-SHA refusal or arbitrary exit1
has different stderr and cannot pass.
2. `mutate_identity` canonically combines the (0,0) coefficient with one,
deleting it if zero. It cannot be a zero-to-zero edit. Subtraction of one must
restore the entire original canonical byte string (dispatch125). Since the
positive identity held, the new residual is exactly nonzero P0, whose leading
X coefficient is 9(t−2). Checker91 then raises the exact full-identity message;
dispatch126–127 require normal child1, empty stdout, that complete stderr and
absent output/receipt. No traceback matching or substring acceptance is used:
the science entry catches the ValueError itself.
3. Multiplying every A1/A2/N by 7t−12 preserves the full identity. The inverse
uses c_i=7a_(i−1)−12a_i, a_−1=0, and separately checks c_top=7a_(top−1).
It therefore restores every polynomial, including empty cofactors, and the
entire original serialization. N is nonzero, so the edit is genuine. The
positive result had no actual-r exceptions; the added factor has t=12/7 and
r=(2−t)/(3t−5)=2. Checker91 still passes; 93–121 write the inconclusive result
and companion receipt, with sole exception `"2"`, then exit2 without stdout.
Dispatch requires normal2, empty streams and the exact status/list/input SHA.
A parser/native/resource error cannot masquerade as this branch.

These conclusions are conditional on the transformed artifact fitting the
unchanged degree/term/bit/byte guards. The transformations can increase those
quantities; a boundary-sized valid positive artifact need not admit them.
Such a case stops and earns no semantic-control success. No actual artifact
size, coefficient bound, Sympy runtime or control completion has been measured.

## C. Lifecycle and whole-task qualification

CAPRUN returns the actual normal child code; RSS cap returns125, wall timeout
124, runner failure70, signals a separate status. Thus normal1 and normal2 are
not confused with caps by dispatch107–110. The dummy specifically requires125,
`RESOURCE_CAP`/`rss`, TERM and KILL sent, leader reaped, cleanup complete and an
empty recorded live list. The probe forks once, parent exits immediately,
same-PGID child ignores TERM and touches64MiB against the32MiB RSS threshold.
CAPRUN retains the leader's start/PGID identity for signaling, samples aggregate
nonzombie group RSS and escalates TERM→KILL. The exited leader's status0 does
not itself count as dummy success; resource/cleanup telemetry is mandatory.

Qualification: probe has no handshake or emitted PID record, and the caller
does not compare its readiness stdout, actual PID/start/argv or RSS value to
the telemetry. CAPRUN's whole-process-list samples are non-atomic; an empty
sample is not kernel-confirmed absence. A missed orphan yielding NORMAL_EXIT0
would fail this dummy's125 predicate, but no general race-freedom or namespace
attestation is proved. ROOT must collect and bind actual runner/child identities,
read the telemetry identity checks and descendant RSS, and independently confirm
terminal owned-cgroup emptiness. No migration to a newer runner is implicit.

The registration's cap strings are checked, not installed by this source.
CAPRUN CPU3290/3291 is inherited per process, not aggregate; RSS30GiB is
sampled PGID RSS, not whole-cgroup memory. Authority separately checks inherited
CPU/AS/FSIZE limits. ROOT must qualify a single non-escaping cgroup covering
dispatcher, CAPRUN, ps, native threads and descendants, fair scheduling,
`cpu.max=80000 100000`, burst0, and the original3540s window **including all
cleanup**, not3540s plus an extra shutdown grace. Ideal quota accounting is
2832 CPU seconds before period/burst qualifications, below3300; actual kernel,
scheduler and shutdown behavior are unobserved. `memory.max=34359738368` is an
enforcement threshold allowing kernel-documented temporary overshoot, not a
literal instantaneous memory theorem. Swap and escape surfaces need ROOT's
qualified policy. The256MiB mount must bound all streams, temporary/cache files,
inputs and outputs jointly; per-file FSIZE does not establish that aggregate.

ROOT must bind installed interpreter/Sympy APIs, native/import closure,
source-directory census and pyc behavior before launch, then preserve current
source/native pins. Science verifies each listed native file before importing
CAS and again before successful outputs, but completeness is external. The
qualification file is merely hashed; its contents and actual host facts are
not interpreted here. Freeze ROOT's single-writer/no-delegation promises and
external deadline/retirement before any registration is enabled. On every STOP,
retain partial bytes after genuine terminality; secure durable EBS/local custody
and parent-directory fsync before unmount/retirement. Dispatcher `exclusive()`
fsyncs files, not parent directories, and implements neither recovery nor that
external durable transfer. These are explicit deployment obligations, not
additional code changes requested by this gate.

## Scope and closeout

The science FIRST's algebra/parser scope is imported, not a new scalar result.
The erratum's prior custody-hash typo and omitted unpinned authority read remain
historical defects; this review's fresh matching authority pin authenticates
only this review. No earlier custody is silently repaired.

No canonical OPEN is raised. Remaining quantity: whether this exact six-phase
client completes under qualified unchanged bounds. Cheapest discriminator is
one separately ROOT-registered AWS batch after host qualification, maximum3540s
including cleanup; this is an UNMEASURED planning ceiling, not an authorization,
runtime estimate or expectation of fit. No second gcd, retry or cap increase.
Own-only collision check: distinct target absent initially, only leased report
and its PINS/custody authored; no source, shared ledger or other lane edited.
Complete own readback and all thirteen postpins precede the final marker.

Postpins matched 2026-09-12 20:12:45.847936082 UTC; complete report and PINS
readback followed. The own-only marker/OPEN search returned no matches before
this completion marker. No scientific or source-processing program was run.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `11032`.
- Body SHA-256:
  `7f1c9d05a38bb2e39ccd3fa8d494c7cfdc9da5ac1472b4395dbb3417211aa441`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
