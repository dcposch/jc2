# F10 r1 disabled univariate runtime caller and finite controls

Status: CODE PREPARATION ONLY; DISABLED, UNEXECUTED, uncompiled and internally unreviewed. Accepted17p adapter and17r caller interfaces are consumed at their exact qualified static scopes. No current normalized runtime result or receipt was read, accepted or invented here. This packet is not runtime authorization, mathematical evidence or an ideal decision.

First action: 2026-09-09 20:23:30 UTC. Controlling cap: 20:43:00 UTC, unchanged. New box/report absence was checked before transaction begin. Basis: 0d39df3c9fd69c939a8420c54d03228b9077777d. All12 allowed inputs matched current SHA before body reads/copies; exact paths and read scopes are in input-pins.json and READ-SCOPE.md. No provenance-only path was followed.

## 1. Delivered minimal delta

The new box contains a narrow dispatch_batch.py adaptation, semantic_controls.py, literal caller.diff, DISABLED registration and VALIDATION.md. It also carries byte-identical adapter.py, checker.py, input_evidence.py, algebra.py, execution_gate.py and probe.py copies. Their six hashes match the accepted originals. No checker/API/backend/helper/CAPRUN change, new supervisor, solver or source presentation is introduced.

The caller source is 9be0c70a3ab8342a79509e039860d31b6d46401ee3aeb211e24f913b3ef69558. The new finite harness is cc04757ed669dab0a506579079478bf71f41ba8debde0de83944ecdd7b416a90. These are prepared-source hashes, not successful-runtime receipts.

The old run() body changes only by copying registration.normalized_acceptance into the emitted authority. All process launch, full parent/child argv, cap strings, PID/PGID/start/namespace checks, exact TERM/KILL event sequence, file limit, admission, aggregate cutoff, post-return cutoff, exclusive output, frozen authority and pre/post source-pinning code remains unchanged. main() changes only the schema/profile name, exact staged vector, prerequisite acceptance checks, three entrypoints, output names and receipt binding fields. No optional hardening was selected.

The six copied headers retain their historical UNEXECUTED/provisional wording. The current root acceptance of their exact interfaces is not a byte edit. Root's U-slot identification qualification is retained: the prerequisite normalized checker binds slot0 to original u and Pi. The univariate checker is not patched or re-hardened here.

## 2. Genuine source prerequisite, not a fabricated receipt

The future root registration must supply normalized_acceptance with status ROOT-ACCEPTED-GENUINE-CHECKER-RECEIPT, the two fixed normalized generator/checker version hashes, and the actual frozen normalized artifact and prior full-checker receipt hashes. Here it is null. The caller requires the field and exact versions before any probe or mathematical dispatch, ties its two digests to the fixed input vector, checks every current file and copies the field into every per-operation authority.

The prior normalized input is normalized.json and its receipt is normalized.full.receipt.json. The NEW univariate output is univariate.json with NEW univariate.full.receipt.json. These names are distinct throughout the caller, harness, template and plan. The normalized generator/checker version hashes are metadata constants only; their conflicting same-basename code is not staged. The future check authority DOES pin adapter.py, in addition to checker, arithmetic and input_evidence.

The exact runtime vector is 13 local files plus /usr/bin/python3. It includes all eight supplied code files, CAPRUN, admissibility, frontier, normalized artifact and prior receipt. The caller/semantic hashes are supplied by root external preflight; six immutable copies and three external instruments have fixed expected hashes; the two incoming artifact hashes come from root acceptance. Original exact.json is not rebuilt or needed as a staged body: its immutable digest is retained through the accepted normalized source and receipt evidence.

The unchanged input_evidence helper independently checks the root field, input hashes, backend versions, original source digest and prior full-condition receipt inventory: 20 original slots, nine G, two H, ten inverse poles and eight bracket bands. It cannot issue root acceptance. A correctly spelled JSON receipt or matching status is not execution evidence; root must retain genuine normalized execution and terminal-custody evidence before registering it. This is the existing cooperative authority boundary, not a new authentication system.

## 3. One finite batch, exact entrypoints and limits

The unchanged preflight consists of five actual authority refusals, one valid sentinel and one same-PGID RSS descendant dummy. The five refused cases are disabled authority, wrong host, extra parent argv token, wrong RSS cap and omitted frontier input. The dummy retains measured descendant RSS over its fixed cap, TERM ignored then KILL, exact four identity events and integer signal checks, namespace/PID/PGID/start equality, reaping and no live owned group.

Only after that preflight may three mathematical children run: adapter.main once; checker.main once; semantic_controls.main once. Their trailing argument counts are respectively three, four and five, giving sys.argv lengths five, six and seven including the script path and authority. The adapter uses operation build; the other two use check. The genuine checker receipt must bind the new artifact and exact source input_evidence before the control harness starts.

There is no in-process positive rerun. The separate previously accepted normalized positive is an input prerequisite, not another child in this batch. Thus the designed batch has seven preflight children and three mathematical children; the last child contains seven negative verify calls.

The template has enabled false and null worker, job, cwd, acceptance, deadlines, aggregate and profile values. Root alone may later choose a fresh exact registration, actual caps/deadlines and any worker. The existing 15-second admission margin, original stored hard cutoff including aggregate, post-return time check, inherited 16 MiB file size limit, no concurrent authority writer, fresh outputs, exact CAPRUN argv and one-child-at-a-time discipline remain. CAPRUN creates its recorded child group; this caller does not add another PGID. Sampled RSS and cleanup are not strict real-time guarantees. Every cap, late return, mismatch or refusal is STOP/NONDECISION with no retry or escalation.

## 4. Seven semantic negatives and their limits

The harness first passes unchanged authorize and verified_input, binds all four incoming paths, the four adapter/checker/backend/evidence code hashes, and the genuine new full-checker receipt. Only then is Ring constructed. No adapter generate path is imported or used. Its arithmetic is the accepted shared exact B backend, not an independent implementation.

Each fixture is a deep copy of the genuine new artifact, must actually differ, is exclusively written with canonical rational strings, SHA-bound, reread and checked for exact JSON equality before checker.verify(normalized,reread). Its hash is rechecked after rejection. Exact exception class and message are required. A wrong reason or accidental unchanged fixture stops the batch. These are IN-PROCESS semantic tests, not seven production-entrypoint or stale-hash refusal tests.

| Changed object | Required ValueError |
|---|---|
| Compatibility coefficient a plus exact B constant 1 | all Phi3 coefficients |
| c_inverse plus exact B constant 1 | actual c inverse identity |
| r[0] plus exact B constant 1 | affine r constant |
| E1/S1 constant coefficient plus exact B constant 1 | literal univariate row E1/S1 |
| Guard list loses h0, with q unchanged | guard equation and both factors retained |
| q[0] plus exact B constant 1 | entire guard q=r*h0 |
| E1/S1 loses dense coefficient slot3 | every univariate coefficient slot including zeros |

The plus-one changes are canonical exact wires. Because the genuine c is a verified unit, adding one to its inverse cannot preserve the inverse product. The dense-slot fixture records the removed wire; it never assumes that slot is zero. No division by b, factor selection, COVER mutation, large-integer/float matrix, additional backend test or original-source power expansion is introduced.

These expected reasons were traced by static read against the accepted checker, not observed by execution. The original normalized A/B and source evidence remain unchanged. The negatives cover the new univariate semantics only; they do not claim new original-source bracket/pole negative coverage or independent lifecycle attestation.

## 5. Static checks, read scope and pending quantities

The literal diff was generated and read against both allowed frozen old and owned new caller sources. All six copies matched their expected SHA. Both new code bodies were read WHOLE statically. All12 allowed bodies were read WHOLE; an initially truncated combined display was followed by separate complete code displays. The new plan, disabled template, exact pins and this own substantive report are read before closure. No current normalized artifact, runtime receipt, live gate, other science, ledger or protected path was inspected.

Only documentary metadata, hash/diff/text reads, apply_patch and the existing publication tool ran. One first patch orchestration had a JavaScript syntax error before any nested tool/file action; corrected text was then applied. No compilation, import, test, mathematical or arithmetic subprocess of any size, fixture creation, CAS, AWS/SSH, network, process census, subagent or shared/frozen edit occurred.

Pending quantities are: one independent static delta review of the two new files; one root-supplied accepted normalized input/receipt binding; and, only if root separately authorizes it, one genuine univariate batch with seven exact negative outcomes or an honest STOP. Cheapest next check is static review of caller.diff, acceptance propagation and seven expected paths against the accepted checker. Syntax, installed runtime compatibility, fixture sizes/costs and exact reasons remain unmeasured until such a registered run. No extra runtime authority is inferred.

Only the future genuine checker and standard telemetry may supply actual degrees, guard degree, basis-term counts, bytes, elapsed time and sampled RSS. This report contains no measured or forecast coefficient size, speed or memory. Representation/control PASS would not establish properness, nonemptiness, a source point, a solve/certificate, or permission for a follow-on.

Own raised-OPEN review: no new mathematical or canonical OPEN identifier is raised. The three finite pending software/provenance quantities above are explicit, with the cheapest next test stated; none is silently closed. Own collision review is limited to the new report/box, both absent before begin; no corpus scan is claimed. All writers stop after transactional publication and current-pin custody.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `11069`.
- Body SHA-256:
  `4dcc0eb4879e28f44df3c70e1666d0a822bf5d6c87ab24c7451a60838e1a4628`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
