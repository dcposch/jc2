You are the hostile different-model reviewer for
`xmodel/intake-supermind-guo-20260824.md`. Work in
`/Users/dc/code/math/jc2`. Review the exact evidence perimeter, not the social
prestige or prose conclusion.

Use a fresh `mktemp -d` outside the repository. Pin the exact SuperMind commit
and Guo tag/commit named in the report. Inspect untrusted code before executing
it; use no credentials or external writes. You may reuse bounded verifier
commands but must independently inspect the mathematical markers and avoid
expanding into optional discovery jobs.

Adjudicate these clauses separately:

1. SuperMind's manifest and exact first-layer/Case-II checks pass, and its
   pinned primary Case-I Singular input really establishes the stated
   characteristic-zero 52-dimensional/good-specialization full-rank terminal.
   Keep the capped Python regeneration edge explicit.
2. Guo's authoritative 790-file manifest, logical/global audits, degree-21
   verifier, characteristic-zero FGLM calculation, and row-split identities
   pass at the stated exact/regression tiers; Sage-only terminal expansions
   remain a replay GAP and must not be silently promoted.
3. The two artifacts encode the same two Proposition-4.3 Laurent systems via
   the stated lossless variable/slice rename, and the claimed exact degree-five
   field maps to Helali/Suzuki reduce to zero.
4. Neither path uses msolve's `-g` unit shortcut.
5. Correctness/equivalence does not establish social or derivational
   independence, and no unconditional `(72,108)`, degree-125, or JC2 claim
   follows without the GGV reduction/transcription bridge.

At minimum independently substitute one terminal or field identity, inspect
the good-specialization logic (not only PASS strings), verify the exact rename
against source formulas, and rerun the cheapest representative checks. Stop a
branch at a false identity or unrecoverable manifest. Do not contact authors,
edit public records, or edit source/artifacts.

Return a per-clause verdict and one overall verdict from `CONFIRMED`,
`CONFIRMED WITH GAPS`, `GAPS`, or `REFUTED`. State what may be promoted and
what remains trusted-external/producer-checked. Write only
`xmodel/review-external-intake-grok.md`; do not edit shared ledgers. Record
model/CLI/version, basis, UTC, commands, pins, hashes, and hard perimeter.
