# Hybrid81 single-control operator — local preparation only

2026-09-07, resumed21:39 UTC after interruption. **PREPARED, NOT EXECUTED.** No worker access, AWS/SSH, deployment, valid authority, actual engine, construction/source read or solver occurred. Earlier sweep bytes are untouched. New operator code remains subject to root review; neither its existence nor its tests authorize execution.

## Smallest adapted operator

`box/d125-hybrid81-controller-prep-20260907/controller.py` adapts the existing terminal `engineering_batch.py` mechanics to **one** literal `hybrid` or `alarm` invocation at `/home/ubuntu/d125-hybrid81-exact-solver-20260907`. It adds no runner, session layer, engine policy, source representation or retry loop. Driver/exact/hybrid/helper/CAPRUN remain unchanged.

Before its first write, the controller checks validated root-supplied boot, physical GREEN digest, its own digest and a worker-clock dispatch bound at most60 seconds ahead; exact Linux/AmazonEC2/instance/cwd/path; frozen code, fixture, registration and binary hashes; and exclusive per-operation output paths. Its literal CAPRUN vector is compared against the helper's `expected_parent_argv` before Popen. Root retains external EC2 Owner/EBS/current-mount/resource/workload/accepted-gate checks: the operator does not claim to authenticate those from local DMI. Authority is cooperative custody, not same-UID security isolation.

The only generated token is schema `jc2.d125-hybrid81-solver-authority/v1`, mode `engineering_control`, with the unchanged driver's caps/pins and a nine-and-a-half-second absolute lifetime. Its additional `engineering` object binds operation/helper/fixture/physical GREEN exactly. Source pins are opaque constants; no production source is opened. `alarm` additionally requires root to supply the exact hash of a same-boot accepted `hybrid.receipt.json`. A previous pass does not authorize alarm by itself.

The command is the fixed registration vector: outer `prlimit --fsize=67108864:67108864 --core=0:0`; unchanged Python/CAPRUN with10wall/10CPU/512MiB sampled group RSS; then unchanged engine payload. Helper sets512MiB AS and the same file/core limits. No solver phase can be selected. The inherited alarm is the helper's existing<=1-second timer, not a changed production timer.

Controller, live post-prlimit runner and live child identities carry PID/PGID/start_ticks/boot/namespace/cgroup/argv. Helper identity plus CAPRUN start_identity bind the executed child. The controller prints identity metadata promptly and reads no live algebraic output. If a tiny child exits before required live capture, the outcome is **GAP**, never an automatic retry.

Terminal telemetry is read first. Normal hybrid acceptance requires original tiny fixture pin, empty stderr, complete engine output, no remaining group, unchanged pins and the unchanged `engine.check_hybrid` run normally and with `-O`. Those future exact checkers are synchronous, tiny, bounded and reaped. Alarm requires the exact ready marker, actual signal14/return142, reported timer<=1 second and measured wall<2 seconds; a10-second resource cap cannot pass. Missing artifacts, identity failures or wrong termination remain GAP. Artifacts are exclusive and retained. Rare missing telemetry/still-live runner demands root custody; the operator does not invent a group-kill authorization or report terminal cleanup without evidence.

## Frozen interfaces and actual local checks

Charged helper `8d8da939caf88c75ae167fa6cf862011c76a593c105c88970efcd9baf16c7cdf`; registration `364a5d8c57a967d6dc3467db13eb0b19ba6c7924a573dc5b3ea67da7eb6f1ee5`; driver `ce599a26…`, exact `7ca2b24e…`, hybrid `c9755a71…`, CAPRUN `4435279d…`, fixture `d00df83b…`, with full hashes embedded in code. Root's terminal Fable parent-argv gate is a supplied dependency; this task does not independently re-promote it or reread/replay its entire packet.

Actual local tests ran under30wall/25CPU/512MiB, `-I -B`, normally and `-O`, with empty stderr. Tests accept both pure operation vectors; reject10 changed scalar/authority prerequisites and58 actual vector-slot mutations; compare against independently split frozen registration; reject solver operation; and invoke the real controller main under mocks, proving wrong current cwd rejects **before hash reads, writes or dispatch**. Controller/tests have no Assert gates. This is not a mock of the full successful host/deploy/engine lifecycle. No valid token or engine fixture output was generated locally.

`OPERATOR.md` gives disabled placeholder argv, exact placement and stop checklist. Existing FSIZE/RSS/long-print evidence is reused, not rerun. Actual hybrid serialization/index/cofactor output and actual Singular alarm interaction remain pending root-authorized controls. The previously published Sol `.sing` fixture must not be executed: the helper derives `control=True` input from its pinned JSONL.

## Custody and stop

- Controller SHA256: `9b857c30b60dfbdd92c2b3bd027f90cb10c9a83f816486d33e42cf16bdc3834f`.
- Two-mode test receipt: `e06bafe13d4d58cb32b72a123b1ea2fb47174f4d01ff533529db4be3ee462f1b`.
- Owned custody: `534d227ae3bd1e3b4ab4696373afd89b095716b97e4da15ccc4c59e362f2f9ac`.

All files are in the fresh owned box; old/pending files and canonical ledgers were not edited. All local mock children are terminal, no remote writers exist, and root alone decides worker START/deployment/engineering GREEN/STOP. **STOP/IDLE following transactional publication.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5521`.
- Body SHA-256:
  `c7dcfffa581923be38e0a17955bf5d377f5146d3fb2d4a1ce5c04097bb604e38`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
