# Quadratic graphs: a uniform source-local residue

Manual producer-level co-research, not promotion. First actual action2026-09-12 11:20:14 UTC; original reserve11:39/HARD11:42 unchanged. TASK and all three input pins matched before charged bodies; own destinations absent. No scientific execution or external input.

## Quantity

For the literal target triple restricted to H=c+k y², c*k≠0, verify the proposed actual collision branch and residue, including every complex k and the exceptional cubic parameter. This proof must be independent of the preceding constant-graph residue result and its live review.

## Result

**CONFIRMED manually for every complex c*k≠0.** There is a genuine normalized collision branch with

    Res(alpha0_first−alpha0_second)=3/(2k)≠0,
    alpha0=x dy.

Consequently no target-polynomial one-form on this fixed triple pulls back to an area primitive, and no pair in C[p_H,q_H,r_H] has nonzero constant Jacobian. This is a producer theorem requiring independent FIRST, not promotion or a global JC2 claim. It is logically independent of the earlier constant-graph residue assertion. ROOT supplies the raw proposal; all load-bearing identities and quantifiers are verified below.

## 1. Literal collision and both graph equations

On x≠0 set t=1/x,s=y+t,W=R. Solving R=2x−3x²y−x³z gives z=5t²−3st−Wt³. Substituting into the supplied P,Q gives

    U=s²+st−Ws³, V=4s+2t−3Ws².

The cancellations use (s−t)²(3s+t)=3s³−5s²t+st²+t³; this is the same polynomial chart, not a claim about the old residue. Eliminating t gives Ws³−2s²+Vs−2U=0 and 2t=3Ws²−4s+V. For two roots s,s' with sigma=s+s',delta=s−s', the common coefficient is

    V=2sigma−W(sigma²−ss').

Put a=3W sigma/4−1,b=W delta/4,u=a−b,v=a+b. Factoring the derivative formula at both roots gives

    C=(a+1)/(3b), s=delta(C+1)/2, s'=delta(C−1)/2,
    t=delta v, t'=−delta u, W=4b/delta.

Conversely those formulas and V give the same cubic value at s,s', hence the common U=(Ws³−2s²+Vs)/2. This establishes actual equality of U,V,W, without requiring the two z values to be equal.

Define Y=(C+1)/2−v, Y'=(C−1)/2+u, so y=delta Y,y'=delta Y'. Direct substitution gives z=delta²Z,z'=delta²Z', with

    Z=5v²−3(C+1)v/2−4bv³,
    Z'=5u²+3(C−1)u/2+4bu³.

Their difference and sum are E/b and K, where

    E=−a²−a+20ab²−3b²−8a³b²−24ab⁴,
    K=(10−24b²)a²−4a−1+10b²−8b⁴.

These follow by expanding v=a+b,u=a−b; the difference before substituting C is20ab−3aC−3b−8a³b−24ab³. Also Y−Y'=1−2a and Y+Y'=C−2b. Therefore the new equations are exactly

    E_k=E−(k/3)(1−2a)(a+1−6b²)=0,
    K_k=K−k(Y²+Y'²),  delta²=2c/K_k.

Indeed (Z−kY²)−(Z'−kY'²)=E_k/b, while their sum is K_k. On the indicated open these equations give BOTH delta²(Z−kY²)=c and delta²(Z'−kY'²)=c. Both source maps thus lie on their respective literal graph z=c+k y².

## 2. A good root exists for EVERY nonzero k

On a=b one has E(b,b)=−b(1−2b)²(8b²+8b+1). The further factorization b+1−6b²=(1−2b)(3b+1) proves

    E_k(b,b)=−(1−2b)² P_k(b)/3,
    P_k(b)=24b³+24b²+3(1+k)b+k.

Every cubic P_k has a simple complex root. Otherwise its only possible multiplicity partition would be a triple root. Its b² coefficient would force that root to be −1/3, whereas direct substitution gives P_k(−1/3)=7/9 for every k. Furthermore P_k(0)=k≠0. The only possible parameter admitting the other forbidden root1/2 is k=−21/5, since P_k(1/2)=(21+5k)/2.

At that exceptional parameter, direct multiplication verifies

    P_k(b)=(b−1/2)(24b²+36b+42/5).

The quadratic discriminant is2448/5≠0. Its values at0 and1/2 are42/5 and162/5, both nonzero. Its two roots are consequently good SIMPLE roots of the cubic. Thus for every k≠0 we may choose a simple root B≠0,1/2; no generic-parameter qualification or sampled cases remain.

At (a,b)=(B,B), differentiating E_k(b+u,b) in b at fixed u gives

    −(1−2B)² P_k'(B)/3≠0.

The curve is smooth there and u is an actual local parameter, with b=b(u). Select its irreducible component and normalized projective completion. No derivative of b beyond its existence is needed.

## 3. Unit cover, actual source limits, and residue

At u=0,

    Y'_0=(1−2B)/(6B)≠0,  Z'_0=0.

Since E_k=0, Z−kY²=Z'−kY'² there, so

    K_k(0)=−2k(Y'_0)²≠0.

Adjoining delta²=2c/K_k is therefore locally UNRAMIFIED. Over C each chosen local square root is a unit; whether the global cover splits or not, choose the normalized branch with delta0=y0/Y'_0, where y0²=−c/k. Both y0 and delta0 are nonzero. The parameter u retains valuation1; no factor2 from the earlier constant case is imported.

Here b,v,K_k,delta are units and u is nonzero generically. The formulas give two actual rational source maps on a nonempty open of this component, distinct because s−s'=delta≠0. The first t=delta v is a unit and its source coordinates extend regularly to

    x_first=(1−2B)/(12B²y0), y_first=(6B+1)y0.

For the y identity, divide Y0 by Y'_0 and factor −12B²+4B+1=(6B+1)(1−2B). In contrast x_second=−1/(delta u) has a simple pole while y_second=delta Y' tends to y0. The common target has finite limit

    U=(1+4B)y0²/3, V=2(1+2B)y0, W=2(1−2B)/(3y0),

obtained from the polynomial chart at s'=y0,t'=0 and checked by equality with the regular first map. This does NOT treat the second source point at t'=0 as finite.

Differentiate the exact second graph equation in u:

    c+k*y_second²
       =delta²[5u²+3(C−1)u/2+4bu³].

At u=0 the bracket vanishes. Derivatives of delta and C are therefore multiplied by u or by the vanishing bracket; the u² and u³ terms have no first-order contribution. The entire derivative is

    2k*y0*y_second'(0)=3delta0²(C0−1)/2
                                  =3delta0*y0.

Thus y_second'(0)=3delta0/(2k). Multiplying by x_second=−1/(delta0 u)+O(1) proves

    Res(x_second dy_second)=−3/(2k).

The first source coordinates are regular, so its residue is zero. Hence the difference has residue3/(2k). This includes every possible first-order contribution and is independent of c,B or the sign of y0. The chosen cover is unramified, so this is already the normalized residue.

## 4. All-degree obstruction and controls

If a target-polynomial one-form alpha pulled back to an area primitive, its pullback minus x dy would be a closed polynomial one-form on A2, hence dF with F polynomial. Ordinary homogeneous radial contraction proves this Poincare statement with only positive-integer divisions. Pullback to the equal-target source maps then forces their x dy difference to equal d(F_second−F_first), a rational exact differential on the normalized collision cover.

Such a differential has zero residue at every projective place: the coefficient of du/u cannot arise by differentiating a Laurent series. This applies at the meromorphic second source map as well. The computed nonzero residue contradicts it. A pair f,g in the target subalgebra with J(f,g)=j≠0 would give the forbidden target-polynomial form f dg/j. This excludes all degrees, not a finite ansatz.

The exceptional k=−21/5 check prevents a generic-root shortcut. At k=0 the asserted K_k value is zero, so this unit/unramified proof fails; at c=0 the chosen delta0 is zero and the regular-first-source proof fails. Neither excluded limit is silently imported from another theorem. No extension to nonconstant h(xy), other graph families, arbitrary plane maps or JC2 is made. No previous residue result or its live FIRST is used.

Quantity settled: one uniform nonzero source-local residue for c*k≠0. The20-minute allocation is UNMEASURED manual planning, not runtime evidence. No further pole, parameter sample, computation or successor is selected.

## Scope and closeout

TASK/candidate freshly WHOLE after pins. Constant-graph context report reused at same-agent exact-pin10:38 WHOLE; COORDINATION at05:51–05:53. No earlier collision/divergence report, current review or linked body is an input. Own WHOLE/postpins/quantity/control/collision check precedes marker LAST. No canonical OPEN, shared edit, runtime or successor authority.

## COLLISIONS

status: EMPTY

- Own-only identifier/scope check. ROOT candidate supplies the proposal, not independent validation; this is a new producer theorem requiring different-model FIRST. No broader family or previous assertion is promoted here.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `8520`.
- Body SHA-256:
  `e8d7c36d41b1ad2692ba9769a0364ac09bfbd4aed76eedf548ec8a1562685e57`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
