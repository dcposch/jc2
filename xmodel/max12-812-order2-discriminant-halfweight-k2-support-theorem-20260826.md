# `(8,12)` order two: exact discriminant half-weight K2 support

Date: 2026-08-26

Status: **PRODUCER THEOREM FOR THE ANALYTIC K2 GATE.  APPLICATION TO THE
FROZEN FULL SOURCE AWAITS THE EXACT-Q V2 SOURCE/ANALYTIC IDEAL-EQUALITY
ENDPOINT.  NO LIFT OR ORDER-TWO VERDICT.**

## 0. Charged inputs

This note consumes:

```text
ddbc758039621f70eae586b5482be3bfdd92754e990cdb015fa4c6d2e0eac0b1
  xmodel/max12-812-order2-discriminant-rank-halfweight-kuranishi-20260826.md
3576b8ca6692ed1d1d47319436ae6b85e981cabf8b90e4155c794b75797c9722
  cases/max12_812_order2_disc_halfweight_support_v2_20260826/FREEZE.sha256
31faaba6a21250c218a37673d4d3c8b07883432223d70716bf7b50384ba607c4
  cases/max12_812_order2_disc_halfweight_support_v2_20260826/RESULT.md
570d149b58a2b5583cb86820e281b30911f21bd124959409a01c304d07cf7279
  cases/max12_812_order2_disc_halfweight_support_v2_20260826/RESULTS.sha256
```

The exact-Q stdout has SHA
`7de0a587270481736b0b85b76cffce761567d7778d014cc0ddd4cd557ed0cb26`;
the independent characteristic-32003 stdout has SHA
`9dca3aaaaa628083f60c553823f67d9decfc4664e588828ffdb27be11f72336d`.
Both fail-closed validators passed.

## 1. Gate and first-contact open

Use the notation of the charged half-weight theorem.  Thus

```text
Q=A^2+b*A+e,
K0=A^2*Q,
N0=m*A*Q,
K1=x*A,
N1=y*A+h,
h=m*x/2.
```

The first-normal discriminant parametrization has `N0=m*A*Q` and
`k10=0`.  If `m=0`, all four normal coefficients and `k10` vanish, so the
point lies on the zero-normal section removed by the first-contact
saturation by
`(n0,n1,n2,n3,k10)`.  Hence every discriminant point relevant to this
contact-one chart lies in `D(m)`.  This is why all divisions by `m` below
are licensed.

After solving the first two complementary K2 rows (their coefficients are
units on `D(m)`), put

```text
(1+b*t+e*t^2)^(5/2) = sum_(n>=0) c_n*t^n,
t^2*(y-h*t)^2/(1+b*t+e*t^2) = sum_(n>=0) u_n*t^n.
```

The five remaining K2 rows are

```text
6*u_3-m^3+16*kappa*c_13 = 0,
6*u_n+16*kappa*c_(10+n) = 0,       n=4,5,6,7.        (1.1)
```

The two sequences are generated by the exact recurrences

```text
u_n+b*u_(n-1)+e*u_(n-2)=0                         (n>=5),
(n+1)c_(n+1)=b*(5/2-n)c_n+e*(6-n)c_(n-1).          (1.2)
```

No terminal or lower-load variable occurs at this grade.

## 2. Exact support

Let `I_K2` be the ideal of (1.1) in
`Q[b,e,h,y,kappa,m]`, after saturation by `m`.

1. The chart `D(kappa)` is empty:

   ```text
   (I_K2:kappa^infinity)=(1).                       (2.1)
   ```

2. On `kappa=0`, the chart `D(e)` is empty:

   ```text
   ((I_K2+(kappa)):e^infinity)=(1).                 (2.2)
   ```

3. On `D(b*m)`, set

   ```text
   U=h+b*y,
   V=m^3+6*h*y.
   ```

   Then the exact nonreduced K2 ideal is

   ```text
   I_K2 = (kappa,e,U^2,V+6*y*U).                    (2.3)
   ```

   Its radical is

   ```text
   sqrt(I_K2)=(kappa,e,U,V).                        (2.4)
   ```

Consequently the reduced support on `D(b*m)` is exactly

```text
e=0,
kappa=0,
h=-b*y,
6*b*y^2=m^3.                                        (2.5)
```

The raw scheme is a doubled normal direction `U`: this thickness must be
retained in the K3 successor and must not be replaced by the radical before
computing its conormal/kernel.

## 3. Certificate and algebra check

The exact-Q standard basis on `D(b*m)` is

```text
kappa,
e,
6*b*y^2+m^3+12*h*y,
b*m^3-6*h^2,
m^6+12*h*y*m^3+36*h^2*y^2.                          (3.1)
```

With `U,V` as above, the last three generators are

```text
V+6*y*U,
b*(V+6*y*U)-6*U^2,
V^2.
```

Thus (3.1) gives (2.3) in both directions in characteristic zero, and
taking radicals gives (2.4).  The independent good-prime lane reproduces
the dimensions, both unit-chart tests, the raw scheme form, the radical,
and both ideal-containment sentinels.  Statements (2.1)--(2.2) are also
exact-Q endpoints, not deductions from good reduction.

The recurrence combinations from rows six and seven supply a separate hand
check of the support restriction.  If `kappa!=0`, they force

```text
b*c_15+2*e*c_14=0,
b*c_16+2*e*c_15=0.
```

Together with (1.2), the `e!=0` branch is driven to the square divisor
`b^2=4e`; the first three rows then exclude it.  The `e=0` branch is also
excluded unless `kappa=0`.  With `kappa=0`, (1.1)--(1.2) force `e=0` and
reduce to (3.1).  The exact-Q ideal certificate charges these last finite
steps and protects against a recurrence indexing or scalar error.

## 4. Chart routing and source firewall

In discriminant coordinates `b=4a` and `e=d+3a^2`.  The omitted divisor
`b=0` inside `e=0` is `a=d=0`, hence
`K0=A^4`, a square-intersection point.  It must be routed to the separate
rank-two square analysis; it is not declared empty here.

The frozen full-source V2 package has `FREEZE.sha256` SHA
`bbaa31fa56398f255b47d6d74307149bd35166272f6bcf7ae73a9957193739e6`.
Its two modular controls verify equality between the seven exact pulled-back
source rows and the seven analytic K2 rows, as well as the ordinary matched
unit endpoint.  The exact-Q equality run is a promotion gate: until its
fail-closed endpoint is frozen, (2.1)--(2.5) are asserted for the analytic
K2 gate only, not promoted as an unconditional statement about every frozen
source component.

## 5. Next gate and exact scope

The reduced survivor can be rationally written on `D(b*m)` as

```text
m=6*b*t^2,
y=6*b*t^3,
h=-6*b^2*t^3,
x=-2*b*t,                                             (5.1)
```

with `b*t!=0`.  Formula (5.1) is only a coordinate on the reduced support.
The K3 compiler must begin from the full ideal (2.3), compute its
conormal/kernel, and then use weighted charts along (5.1).  It must retain
all scaled lower loads and the terminal/Taylor source receivers; it may not
substitute only the radical and call the result exhaustive.

This theorem classifies one K2 initial obstruction.  It neither constructs
nor excludes a K3 lift, handles the square component or its intersection,
imposes the `[6,2]` terminal passport and both Taylor boundaries, closes
order two, closes `(8,12)`, proves maximum twelve, nor proves JC2.
