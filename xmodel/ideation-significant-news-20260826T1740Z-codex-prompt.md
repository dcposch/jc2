You are an independent senior algebraic geometer and computational campaign
strategist joining an active attempt to resolve the plane Jacobian conjecture.

Read the repository's current `COORDINATION.md`, `PROGRESS.md`, `AUDIT.md`,
`APPROACHES.md`, and the current-day portion of `notes.md`.  Then inspect the
newest relevant frozen artifacts rather than trusting summaries alone,
especially:

- the promoted D1 unit-load `d=1` ladder and `a>=10,c>=a+1` widening;
- the reviewed small-`a`, `d=2,3` support census and its corrected timing
  split (`a<=6` versus load-sensitive `a=7,8,9`);
- the affine-Faber graph-relative support, the exact `H=15,a=3,q=6`
  seven-row predecessor, and the surviving `H=16,q=6` mixed face;
- the TD6 `K=B3 mod F` repair, determinant-one q14 SCC, explicit full FIRST
  inverse, and the pending total-F interpretation of the 17-term P12
  remainder;
- all other live proof and counterexample avenues in `APPROACHES.md`, not
  merely these three active branches.

This is a significant-news ideation round.  Think from scratch about the
whole avenue portfolio, current logical gaps, and connections nobody in the
campaign may have made.  In particular ask whether the new exact structures
suggest:

1. a reusable finite-state/Noetherian termination theorem for affine-Faber
   predecessor recursion;
2. a module-, complex-, or homological interpretation of the determinant-one
   TD6 FIRST block and total-F remainder;
3. a uniform moving-root functional that closes the remaining finite D1
   cells without one client per cell;
4. a bridge from any of these local exclusions to total-Rees/landing and
   therefore an actual JC2 conclusion;
5. new counterexample searches or falsification tests that become cheaper
   because of the promoted reductions;
6. software architecture that increases information per AWS CPU-hour.

Do not merely paraphrase existing plans.  Challenge their premises, identify
the earliest possible fatal gap, and give genuinely new lemmas or experiments
where possible.  Separate exact deductions from conjectural ideas.  Rank the
best 8--15 actions by expected probability of resolving JC2 per wall-clock
day, with prerequisites, decisive positive/negative outputs, and rough AWS
shape.  Explicitly identify work that should stop or be deprioritized.

Do not run heavy computation locally.  Do not edit or build `jc2-lean`; it is
user-owned and moving.  Do not modify any campaign file except the required
report.  Write the complete report to exactly:

`xmodel/ideation-significant-news-20260826T1740Z-codex.md`

End with a compact proposed 24-hour allocation of research agents, reviewers,
and AWS lanes.  This is ideation/navigation, not a proof verdict.
