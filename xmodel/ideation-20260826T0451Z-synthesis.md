# Significant-news whole-portfolio synthesis

Date: 2026-08-26 04:51Z

Status: **COORDINATOR SYNTHESIS; STRATEGY/ALLOCATION ONLY.  PRODUCER-TIER
NEWS REMAINS BEHIND ITS OWN REVIEW FIREWALL.**

## Inputs and independence

The synthesis consumes these frozen packets:

```text
7c4aa10586011b4df599b8dc2404ba024386ec835f09f2538bb386a28d581185
  xmodel/ideation-20260826T0440Z-root-significant-news.md
e3c9270dcfe064248c3ab7b130c93861785bd002ec375e2b6dd118b80d966da9
  xmodel/ideation-20260826T0440Z-d1-owner-significant-news.md
07fd84bd9f80ee0ec3335013ee50eb9ac1a61bb7f220b63582560e45d2cecaf1
  xmodel/ideation-20260826T0440Z-order2-owner-significant-news.md
6e1ba646b94f429a517999d0184b36cdc5057a744e4125d68ab8ec9ab9507562
  xmodel/ideation-20260826T0440Z-td6-owner-significant-news.md
```

Root, D1, and order-two packets are clean blind inputs.  The TD6 owner
correctly disclosed that a broad filename search exposed sibling headings,
though not body text, before its freeze.  Its overlapping themes are not
counted as an independent vote.  Its TD6-specific reasoning is retained with
that caveat.  This leaves three clean whole-46 scans, sufficient for the
consensus below without laundering the fourth.

## Post-freeze evidence included only as lifecycle-labelled news

Two results landed after the blind inputs froze:

1. The dual-prime order-two `Q1*` navigation endpoint is nonunit at both
   `p=32003,65521`, with exactly two modular minimal components of dimensions
   three and four.  RESULTS/custody SHAs are `c474e05f...` / `818f5d15...`.
   This is not a characteristic-zero decomposition.
2. The exact principal-part/UFD producer SHA `988d659f...` proves the
   unitriangular principal-part formula and, on `k10=0`, the criterion
   `K|N^2`; its nonzero-normal support is parameterized by square and
   nonsquare discriminant branches.  It does not yet exclude nonsquare
   `k10!=0` cancellation or prove scheme reducedness.  Hostile review is live.
3. D1 V9 dual AWS producer endpoints agree on exact row/load/target
   covariance and a cleared universal identity over the unit-axis chart
   `D(a)`.  This provisionally transports V8 across the smooth discriminant;
   V8/V9 review and freeze chains remain mandatory, and the triple root is
   untouched.

These observations choose immediate experiments.  They do not retroactively
change the blind vote or enter `AUDIT.md` before review.

## Consensus

### 1. Stop the D1 positive-`h` serial ladder

All three clean packets treat V8 as a qualitative transition.  If its review
confirms the exact 48-term witness, every positive transverse-`h` weight in
the fixed full-support chart is covered by one finite identity.  Further
positive-`h` grades are dominated work.  D1's remaining job is coverage:

```text
fixed axis -> smooth discriminant D(a) -> weighted triple-root charts.
```

The first arrow is already producer-positive in V9.  Review it in the
background while launching the geometrically forced weighted blow-up
`wt(a)=1, wt(h)=3`; do not return to arbitrary ray sampling.

### 2. Replace order-two first-jet elimination by exact discriminant branches

The clean packets independently identify the binomial principal part

```text
(3/8)N^2/K + k10 K^(5/2)
```

as the structural object.  The producer theorem now verifies the formula and
the `k10=0` UFD criterion.  The next proof obligations are exact and small:

- hostile review of the principal-part theorem;
- print and match both modular prime bases by invariant signatures;
- prove the `k10!=0` Padé lemma and the exact ideal/radical identities, with
  nilpotent/embedded-component scope stated honestly;
- compute the next divided jet separately on the square and nonsquare
  discriminant parameterizations;
- send every rational component immediately to terminal-divisor and Taylor
  tests; use normalization genus only after component coverage is proved.

Opaque eight-variable first-jet primary decomposition is retired as a proof
strategy; it remains a cross-control.

### 3. Test the order-one `r7` divisor before genus

The order-one owner's new connection is strictly sharper than the initial
generic-genus plan.  The reviewed terminal theorem gives, for `U>=2`,

```text
r7=lambda+gamma (x-a)^(1-U).
```

Therefore a source-relevant component needs a normalization on which
`r7-lambda` has exactly one pole and one zero, each of order `U-1`; in
particular its `r7` map has only the two corresponding branch values.  Once
the dual fixed-load probes return, compute the complete divisor and critical
values of the retained `r7` graph before a plane-genus calculation.  Two pole
places kill a component even when its normalization is rational.  A survivor
is parameterized through `r7` and sent directly to all six tails, terminal
power, and Taylor constraints.  Never saturate by `r7`.

### 4. TD6 proceeds to Kuranishi plus a cheap symbol control

Generic FIRST axes are retired.  Freeze/review the producer composition
`Q_prev=Q`, then compute the licensed quadratic map

```text
K2_prev: Sym^2(Q) -> C_prev.
```

Use a frozen right inverse/homological-perturbation formula and validate one
four-axis block against the full replay before expanding all 300 pairs.
Exploit exact presentation symmetries and orbit representatives.  In parallel,
one Spencer/Cartan first-prolongation calculation is a cheap go/no-go control:
non-involutivity names compatibility rows; involutivity warns that repeated
local depth may be low-value and shifts attention to algebraization/landing.
Current-base adjoint derivatives remain symbol-reconstruction data, not the
tangent kernel.

### 5. Build one proof-carrying filtered-complex interface

D1 and TD6 share an engineering pattern even if their two occurrences of
dimension 24 are accidental: original-row ancestry, a filtered differential,
a chosen right inverse, residual/Kuranishi coefficients, localization/Fitting
custody, and exact cone certificates.  Reuse a certificate intermediate
representation and verifier, with route-specific frontends.  A deliberately
mutated coefficient is mandatory as a negative control.  Do not infer a
general mathematical equivalence merely from software reuse.

### 6. Global walls are unchanged

No packet supplies universal landing/coverage, a cofinal type/degree ceiling,
or an explicit characteristic-zero counterexample.  `G2-PSC` and `G2-BD`
remain distinct.  Rows 8--18 and 22--45 gain no implication shortcut; formal
certification rises only as proof infrastructure.  The 5% protected lane must
remain live precisely because local success does not solve these walls.

## Allocation

New work and newly free AWS capacity are allocated approximately

```text
35%  order-two exact branches / next divided jets
15%  order-one r7-divisor and source-relevant components
15%  D1 smooth-discriminant review / triple-root coverage
30%  TD6 Stage A review / K2 / one Spencer control
 5%  protected global, char-p, formal, and external-intelligence work.
```

Equivalently: **order two/order one 50%, D1 15%, TD6 30%, protected 5%.**
Reviews remain asynchronous.  Reasonably exact provisional results launch
cheap reversible successors immediately, while promotion and expensive
commitment remain review-gated.

## AWS/software placement

The seven running AWS instances already total the user-authorized 512-vCPU
ceiling.  Box01, Box02, and r6a--c were CPU-saturated at the last audit;
Box03 and r6d had roughly one hundred aggregate cores free, ample RAM, and
zero swap.  Fill those hosts with independent discriminators first.  When
Box02's queue drains, it may be audited, stopped, and replaced by two or more
workers of at most 1 TiB RAM without exceeding 512.  Heavy/CAS/Lean/exact-
algebra computation remains forbidden on the Mac.

Parallelize mathematical alternatives, multiplicity branches, primes,
load controls, and hostile reconstructions.  Do not consume free cores on
many identical opaque Groebner jobs.

## Immediate queue and stop rules

1. V8/V9 hostile reviews; if confirmed, retire smooth-discriminant D1
   production and attack only the weighted triple-root charts.  If refuted,
   roll back V9 and repair the first named identity.
2. Principal-part hostile review, explicit dual-prime component bases, exact
   `k10!=0` Padé/ideal theorem, then two small second jets.
3. Harvest order-one probes and run `r7` divisor/critical-value tests before
   normalization genus.
4. Freeze/review TD6 Stage A; validate one transferred K2 block, then shard
   orbit representatives and run one symbol-prolongation control.
5. Freeze a coherent repository checkpoint after these current bounded
   review/harvest files land; verify manifests, commit/push, and restore a
   fully clean status before opening another broad production wave.

Next blind whole-portfolio backstop: `2026-08-26T16:40Z`, or immediately on
another result that closes/opens a route or changes allocation.  Quiet web
sweep remains due at `2026-08-27T00:59Z` unless external significant news
arrives first.
