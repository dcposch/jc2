# f10-source-cone-r3-rank-code-gate-fable5-20260911 — FIRST static review of the one-place full-rank source instrument (Fable 5.1)

status: UNSEALED (adapter seals; no Seal or charge_basis authored). First action 2026-09-11T01:38:24Z; both owned targets ABSENT at that instant (ls 01:38:57Z). Original reserve 02:03:00Z / HARD 02:06:00Z, never reset. Basis 0d39df3c9fd69c939a8420c54d03228b9077777d. All 15 charged inputs in /tmp/jc2-lane.ubjvQO/inputs hashed equal to the charged pin list BEFORE any body was read (`sha256sum -c --strict`, 15 OK, exit 0, 01:39:20Z; rows in box/f10-source-cone-r3-rank-code-gate-fable5-20260911/input_custody.md). Each read FRESH_WHOLE through EOF; check.py (540 lines) in two contiguous ranges 1-270 and 271-540; no clipping. Static text, hash and metadata only: zero CAS, subprocess, import, AST, syntax, compile or test, no coefficient, fixture, baseline, place or candidate body, no provenance, live, peer, protected, network, Git or agent access. Accepted imports consumed at scope only: complete source cone (cone17z), the actual ENTIRE degree-seven FIELD B=Q[V]/P7 at r=3, tau=3/10 (eight coefficients, degree seven), the conditional one-place rank theorem (rank19z), and the source checker at STATIC scope (source20z). No coefficient, baseline, place, rank, control or source outcome exists; none is claimed here.

Verdict summary: A CONFIRMED, B CONFIRMED, C CONFIRMED, D CONFIRMED, E CONFIRMED (non-blocking labeling notes), F CONFIRMED as static with named EXTERNAL obligations, G CONFIRMED as SPEC/NOT_RUN, H CONFIRMED static; liveness/aggregate-storage/phase-profile GAP (external, unmeasured). REFUTED: none. No line must change for the mathematics to be literal.

## Verdicts A-D (septic and wire, place and field, complete map, checker identity)

**A CONFIRMED.** finder.py 28-37 forms D, K, G with the same eleven rational factors as produce.py 13-21 and check.py 103-111 (2-3tau, 1+tau, 2+tau, 3+tau, 4tau-3, 1-tau^2, 12, 42, 840, -840, -120tau, -30tau, 180tau) and septic = 2K*K - 140tau*((1+tau-6V)K)*D + 245*G*D*D. Degrees: K*K has 7 entries, (1+tau-6V)KD has 5+2=7, GDD has 4+2+2=8, so qadd gives length 8 and the only index-7 contribution is 245*(-120tau)*144, which is the literal -245*120*144*tau required at line 37; P=septic/lead is monic (38). checker.py 23-40 accumulates the same septic by explicit index loops: 2k_i k_j at i+j, -140t(1+t)k_i d_j at i+j and +840t k_i d_j at i+j+1 (the -6V branch of the middle factor), 245g_i d_j d_h at i+j+h; leading term checked at 39, monic at 40. I re-expanded the middle factor by hand: (1+t-6V)KD = (1+t)KD - 6V*KD, so -140t times it gives exactly the two accumulations at lines 33-34. Both sides then require the wire's eight P7 rationals to equal their own monic list (finder 42, checker 44); neither trusts the baseline's modulus. Graph attachment: wire.py 92-111 pins format, job_tag, the literal interface (labels K8,K9,K10,A1_1..A1_9,A0_1..A0_12,T; raw/graph variables and weights; targets, guard, graph_guard, jacobian_order, low_order) byte-for-byte against produce.py 265-272 and check.py 92-99, the base/source/raw/graph key sets and array lengths 10, 4, 21, 24, 108, 25, and reads each graph term as [4 exponents, 7 rationals] exactly as check.py's poly(a, graph=True) does (bounds 30,15,10,7; strictly increasing exponent tuples; nonzero seven-vector). Slot weights: finder 154 and checker 137 both give 8,9,10; 19..11; 22..11; 27, equal to produce.py 259 and the accepted cone (6r+2-i, 7r+2-i, 8r+3 at r=3). Homogeneity of every term of every slot is enforced before that term's reduction (finder 159, checker 141), likewise Hq 7, zeta 7, ell 23, g 30, U 4. Denominators: finder rational() 124-128 and checker scalar() 83-87 raise NonIntegral whenever d is divisible by p, and they are applied to all seven coordinates of every term (element 130-132, reduce_vector 133-135), including 0/1 coordinates and coordinates whose numerator later reduces to zero, for all 25 slots, the five extras and c, c_inverse (finder 163-167, checker 144-148). No clearing, rescaling or integral-basis change exists anywhere. One labeling note, not a defect: a p dividing a denominator of the monic P7 itself surfaces as NONDECISION: NONINTEGRAL_BASELINE (finder 65, checker 89) rather than INVALID_PLACE; both programs agree and both are nondecisions.

**B CONFIRMED.** Place syntax: p is a canonical decimal in 0..2147483647 and phi a list of 2..8 canonical residues in 0..p-1; any WireError there becomes InvalidPlace (finder 47-53, checker 47-53). Primality: finder rejects p<2 and even p>2, then trial-divides by odd d with d*d<=p (55-61); checker trial-divides by every trial with trial*trial<=p from 2 (56-60). I checked p=2,3,4,9,25 by hand on both: 2 and 3 pass, 4, 9, 25 fail. Monicity phi[-1]==1 (62, 62-63) makes f=len(phi)-1 the genuine degree 1..7. Divisibility: the monic P7 residues are reduced modulo phi (finder remainder 89-99 by long division with a Fermat inverse of the leading coefficient; checker residue 71-81 with pow(b,-1,p)); nonzero remainder is InvalidPlace. Irreducibility: both iterate h <- h^p for i=1..f, require gcd(h-X, phi)=1 for every i<=floor(f/2), then X^(p^f)=X (finder 68-75, checker 122-131). This is exact: a reducible phi of degree f has an irreducible factor of degree d<=f/2 (two factors of degree >f/2 would exceed f), and that factor divides X^(p^d)-X; conversely an irreducible phi of degree f is coprime to X^(p^i)-X for every i<f and satisfies the final identity. Degenerate branches: if h-X reduces to zero the finder's gcd loop returns phi (length f+1, not 1) and the checker's gcd_degree returns f, so a splitting phi is refused; f=1 skips the gcd loop and requires X^p=X, which holds for any linear phi; phi=X is accepted iff P7(0) reduces to 0. Field arithmetic: finder pmul/remainder (101-108), checker multiply by monic subtraction from the top (97-106) and left-to-right power (108-114) are distinct implementations; the checker never divides. finder inv() 144-151 uses a^(p^f-2) and then verifies a*b=1, raising a plain ValueError (STOP, not a nondecision) on failure; zero pivots cannot reach it because pivots are chosen by any(). Bad-place refusals are therefore composite p, out-of-range p, nonmonic or non-dividing or reducible phi, degree outside 1..7, non-string or over-range coefficients, each as INVALID_PLACE.

**C CONFIRMED.** Rows: finder monomials(30) (169-177) enumerates (i,j,k,l) with i+2j+3k+4l=30 by nested ascending loops; checker monomials (150-157) recurses in the same variable order, so both lists are the same lexicographic sequence of exponent tuples. Columns: catalogue/columns are slot-major then that sequence at weight 30-w_j (finder 180, checker 160-162). Own count: p_4(30)=297 and sum of p_4(30-w_j) = (136+120+108) + (27+34+39+47+54+64+72+84+94) + (15+18+23+515) + 3 = 364+515+571+3 = 1453, and both programs additionally require exactly these dimensions (finder 181, checker 163). Zero slots contribute p_4(30-w) all-zero columns; duplicate slots contribute duplicate column families; both stay in the universe and the IDs are positions in the complete catalogue. column(cid) (184-191) writes F_j[ex] at row ex+mon; ex distinct implies rows distinct, and homogeneity puts every row inside the 297. Pivoting (193-216): forward elimination in column order with invariant "rows >= rank vanish in all columns < j", pivot = first row >= rank with any() nonzero, swap, scale by ff.inv, eliminate below over k>=j only, which the invariant makes exact; a column with no candidate is dependent on earlier pivots and is skipped. Selected pivot columns of an echelon reduction of the original matrix are independent, appended in increasing order, so `columns` is distinct and increasing. Rank<297 returns (None, rank) and main writes the NONDECISION_RANK_DEFICIENT receipt with that rank and exits 2 (255-259); no negative membership is minted. The square minor is rebuilt from projected data, not from the reduced work array (218-219), and Gauss-Jordan on [M|I] (220-234) yields invmat = M^-1 with the reduced M verified equal to I (235-238; failure is STOP). Every selected column is therefore genuinely a (slot, multiplier) pair of the complete 1453-column map.

**D CONFIRMED.** checker.py 181-189 rebuilds the 297 x 297 matrix by row-minus-multiplier lookup: entry(row, (slot, mon)) = F_slot[row-mon] if row-mon is nonnegative, else zero, which is the coefficient of X^row in X^mon*F_slot and coincides with the finder's shift convention. It uses its own rows and columns lists, its own reduction of the frozen slots, and only the certificate's column IDs (a choice) and inverse (the witness). Lines 190-196 compute all 297^3 = 26,198,073 products and compare all 88,209 positions, zeros included, raising IdentityFailure at the first mismatch. Soundness needs nothing from the finder: if M_checker*W=I holds then M_checker is invertible over k, whatever the finder did; a differing row or column order on the finder's side could only produce a false negative. No rank, determinant, elimination trace, reduced system or right-hand side is read; finder_sha256 (171) is provenance only; g is denominator/support-checked but never enters the identity. Witness parsing (173-178) enforces 297 distinct increasing IDs in 0..1452 and 297 x 297 dense fixed-f residue vectors with canonical strings in 0..p-1 under the checker's own f. Arithmetic independence holds as stated in the candidate: monic-subtraction multiply versus long-division pmul, left-to-right versus right-to-left powering, recursive versus nested enumeration, index-loop versus convolution septic. Shared code is wire.py (parsing, no field arithmetic) and authority.py only; checker.py never imports finder.py or any source arithmetic.

## Verdicts E-H (wire, authority, control, static completeness)

**E CONFIRMED.** wire.decode (43-75) parses with parse_int/parse_float/parse_constant all raising, duplicate keys raising, then walks every node: strings capped at 8193, arrays at 400,000, objects at 256 with keys at 256, depth 32, nodes 16,000,000, and any non-string scalar (true/false/null survive json.loads and are caught here) is a WireError. integer() (24-31) admits only 1..4096 canonical decimal characters, no leading zero, no "-0", optional sign only when signed, and rational() (34-40) requires exactly one "/", positive denominator and gcd 1, so 0/1 is the unique zero. sys.set_int_max_str_digits(4300) is fixed in authority.py 40 before json is imported, above the 4096 cap, so int() never raises on an admitted string. Exponent bounds (30,15,10,7), strict ascending exponent tuples, nonzero seven-vectors, per-polynomial 20,000 and aggregate 400,000 term caps (115-127) are the same admissibility rules as check.py 40-87, and the 8193 string cap equals its 4096+1+4096 rational maximum, so a baseline the accepted source checker admitted is never refused on shape here; wire's aggregate counts only the 30 graph polynomials, at most check.py's global count. No JSON number is ever produced or consumed: certificate and receipt are json.dumps of strings, lists and dicts of strings (finder 240-246, authority 202-208). Load is whole-file readback with cap+1 read, byte cap and sha256 equality (78-85). Qualification binding is distinct from re-checking: wire.baseline validates only the graph section and the eight P7 rationals, while authority.py 139-147 ties the baseline bytes (size and sha) to a ROOT-issued record whose fixed fields name the accepted source checker sha b9176ee7... (the charged check.py, which I verified is the pinned input) and its exact success stdout, which equals check.py line 531 plus print's newline. That is byte custody of an external acceptance, not a rerun. Precision note, not a defect: CONTRACT line 13 says whole homogeneous support is checked "before reduction"; the code checks and reduces term by term, so a baseline with both a bad denominator and a later inhomogeneous term reports whichever nondecision its earlier term reaches. Neither outcome accepts anything.

**F CONFIRMED (static), with residual EXTERNAL obligations.** Import order: authority.py imports os and sys only; finder.py 2 and checker.py 2 import authority only; wire and fractions are imported inside solve/verify/main after authorize() returns. authorize() checks mode and Linux, DMI vendor "Amazon EC2", the nine-entry registered CLI with literal tag and --output/--input by mode, the four interpreter flags, the exact four-file listing of its own realpath directory, removes that directory from sys.path, fixes the digit bound, and only then imports hashlib, json, stat, datetime (14-44), restoring the sibling path at 214 after every pin. The authority JSON is read frozen (uid 0, 0444, 64 KiB cap, pin format, whole readback, 89-96), keys are exact (99-100), fixed values and LIMITS literal (101-103). The typed hole: full_argv must be a 14-entry list equal to [sys.executable,-E,-s,-S,-B]+sys.argv with exactly one ROOT_AUTHORIZATION_SHA256 token, at index 11, the position of sys.argv[6]; only that entry is expanded and the result must equal both the expected vector and the live /proc/self/cmdline (104-115); sys.argv[0] must be the canonical sibling entry path (116-117). Source: ROOT-owned 0755 directory, four roles with canonical paths and frozen 0444 pinned bytes (118-124); interpreter hash (127); separate frozen native inventory {job_tag, files} with every entry hashed (128-132); expiry in the future, rechecked in postcheck (134-138). Baseline size and sha with canonical decimal bytes, the exact qualification dictionary, artifact binding (UNFORMED and absent for find; frozen pinned certificate for check), receipt UNFORMED and absent, place keys {p, phi}, and input/output separation over authority, baseline, qualification, inventory, sources, native entries and interpreter, with receipt distinct from artifact (139-164). write_new is O_EXCL|O_NOFOLLOW at 0600, write, flush, fchmod 0444, fsync, parent fsync, readback hash and length (180-195). finish() runs the full postcheck first, binds the current certificate hash, and writes the receipt with exactly the CONTRACT's thirteen keys and science NONE (197-209). Semantics are as contracted: authority refusals raise plain ValueError and are not caught by the four semantic except clauses (finder 266-277, checker 210-225), so they are STOP; rank deficiency writes a post-pinned receipt then exits 2; identity failure runs postcheck, prints the exact stderr line, exits 2 with no receipt. Residual EXTERNAL obligations, stated without asserting any runtime defect: ROOT must authenticate the four source bytes and the interpreter before launch (authority.py cannot prove its own authenticity); supply startup/native/loaded-map closure beyond the bounded inventory, a clear environment and a launcher whose argv[0] is literally sys.executable with the canonical script path; establish physical instance, UID/GID, cgroup, CPU/wall/RSS caps, a trusted clock, and the absence of hard-link or bind-mount aliases of protected paths (separation is by canonical path string); issue the qualification record only after the accepted source checker actually succeeded, since a byte-identical fabricated record passes; choose the single place; and own quiet durable collection. Two labeling notes: a receipt over 32768 bytes is a STOP ('output cap' ValueError), whereas an oversize certificate is INTERNAL_CAP (finder 261-262); and the checker receipt records the count 88209, not a per-position listing.

**G CONFIRMED (SPEC/NOT_RUN).** Own proof: replacing the constant coordinate a of W_00 by (a+1) mod p adds the field element one to W_00, so W' = W + e_0 e_0^T and M W' = I + (M e_0) e_0^T, which differs from I in column 0 by column 0 of M; an invertible M has no zero column, so at least one of the 297 positions (i,0) fails and verify() raises IdentityFailure at the first such i. This holds at p=2 (1 is nonzero) and for every f (the constant coordinate of one is 1). The mutated token remains a canonical residue string of the same fixed length f, so wire parsing, weights and denominators are untouched, and the only way to reach the mutation is a fresh ROOT authorization for the new certificate sha; with the old sha the run stops at 'certificate binding' (authority STOP), which CONTROLS.md correctly excludes. The exact rejection is exit 2, empty stdout, no receipt, stderr "CHECK FAILED: full inverse identity" after a passing postcheck (checker 210-213), distinct from MALFORMED_INPUT, INVALID_PLACE, NONINTEGRAL_BASELINE, INTERNAL_CAP and STOP. The three manual perimeter controls (product base, deficient-rank solvability, p-divisible denominator) are consistent with the accepted rank19z gate. Every failure path in both programs is a nondecision or STOP; the sole positive statuses are RANK_CANDIDATE_UNCHECKED and R3_FULL_ROW_RANK_CHECKED_NO_AUTOMATIC_SOURCE_PROMOTION with science NONE, so no failure and no success mints a source point, a negative membership or a rank claim beyond one checked minor at one place.

**H CONFIRMED as static; liveness GAP.** The package is statically complete for its contract: four siblings, CONTRACT, CONTROLS, READ-SCOPE, and terminal custody whose ten owned hashes and byte counts equal the charged pins (I re-derived all byte counts with wc -c and all three sealed body hashes of the charged reports with head|sha256sum; every value reproduces). What does not exist: any registration JSON, native inventory, qualification record, baseline, place, certificate, receipt, syntax check, import or execution by anyone; a supervisor for the 600 s / 550 s / 8 GiB limits (CONTRACT line 46 says so); phase profiles; an aggregate storage budget. Format arithmetic only, not a runtime claim: a certificate at f=7 with ten-digit residues is under 8.3 MB (297*297 elements of at most 93 bytes), inside the 16 MiB cap for every admissible p and f. Whether the pure-Python elimination over F_(p^f) and the 297^3-product check fit any registered wall/CPU cap is UNMEASURED; a cap hit is INTERNAL_CAP or STOP, never evidence, and no cap increase is recommended without a measurement. No rank, coefficient, control, source exclusion or JC2 result is observed or implied.

## Retained scope and blocking corrections

A-H: no REFUTED item; the only GAP is external liveness/aggregate-storage/phase-profile readiness. Blocking corrections: none. Retained usable source: authority.py, wire.py, finder.py, checker.py exactly as pinned, with CONTRACT.md and CONTROLS.md. Non-blocking notes recorded above: P7-denominator places labeled NONINTEGRAL_BASELINE; term-wise rather than whole-first support checking; receipt overflow is STOP; the receipt carries the count 88209. The accepted review corrections are respected by the code: no denominator clearing, no used-column-only integrality, no quotient-ring relaxation, eight coefficients for the degree-seven P7, and a field place with an irreducible phi. This gate accepts no code for execution, no baseline, no place, no control result and no feasibility; the accepted conditional theorem is consumed, not re-proved, and its promotion to a source conclusion would still need ROOT qualification of the actual complete source, an actual checked certificate, and separate acceptance.

## OPEN(S) RAISED

No new canonical identifier. QUANTITY: whether the actual complete weight-30 map (297 x 1453 over the actual r3 field B) has a 297-minor with nonzero reduction at one genuinely ROOT-specified place (p, phi) of the conservative integral model, witnessed by an independently checked inverse; unknown, no coefficient, place or rank observed by anyone. CHEAPEST INSTRUMENT: after ROOT's separate runtime/registration/native/startup gate and a genuinely issued qualification of the actual baseline, one finder run then one fresh checker process at the single registered place, then the one CONTROLS.md mutation with its own fresh authorization; record the exact status or stderr line reached. GATE: INVALID_PLACE, NONINTEGRAL_BASELINE, MALFORMED_INPUT, INTERNAL_CAP, NONDECISION_RANK_DEFICIENT and STOP are all nondecisions; no prime farm, retry, alternate engine or cap increase. NUMERIC PLANNING WALL: the registered 600 s wall / 550 s CPU / 8 GiB and the 128 MiB / 16 MiB / 32768 B file bounds are UNMEASURED ceilings, not authorization or feasibility; the only static counts are the code's own upper bound of 297*296*1453 row operations in the elimination and 297^3 field products in the check, which are counts and not runtimes, and are not accepted operation counts.

## COLLISIONS

status: EMPTY. Own-only check: this lane wrote exactly xmodel/f10-source-cone-r3-rank-code-gate-fable5-20260911.md and box/f10-source-cone-r3-rank-code-gate-fable5-20260911/input_custody.md, both absent at 01:38:57Z; apply_patch only (custody, skeleton, A-D, E-H, this section, then the custody post-read append and the single marker line); no Seal, no charge_basis, no Write/Edit, no redirection into owned files, no generated mathematical output, no input edit, no live/provenance/peer/protected body, no transaction authored. The 15 input postpins are re-verified with sha256sum -c after this write and recorded in the custody file; own final WHOLE read of both owned files precedes the marker write, and the marker below is the only standalone marker with nothing after it. All writers idle after the marker write.

<!-- BODY-END -->
