# (99,66) N1 skeleton enumeration and partial computational closure

Date: 2026-09-04

Lane: `g9966-n1-skeletons-gpt55-20260903`

Status: `PARTIAL`

## Verdict

The frozen Moh enumerator confirms the N1 premise: at `(n,m)=(99,66)`,
Moh's printed conditions `(1)-(13)` admit exactly eight characteristic-data
assignments. The banked row is the published/Moh-selected row

```text
M=(-66,77,97),  d=(99,33,11,1),  V_3=8, V_2=8,  u_s=3, v_s=8.
```

Among the seven non-`(8,8)` rows, this lane kills exactly one:

```text
S6: M=(-66,55,97), V_3=10, V_2=2, u_s=1
    -> descended row (9,6; M_2'=5, V_2'=2; k=2)
    -> full order-basis partition [1]
    -> three finite-field unit screens plus exact-Q UNIT_IDEAL_CHAR0.
```

The other six non-banked rows remain typed `OPEN`. Therefore N1 is **not**
closed here, and no degree-wide `(99,66)` theorem is promoted. The degree-wide
milestone would be notify-worthy only after all seven non-banked rows are dead;
that did not happen in this lane.

## Input Custody

The receipt

```text
xmodel/g9966-n1-skeletons-gpt55-20260903.run.v2
```

was parsed mechanically. The command joined each indexed
`charged_input_<i>_sha256=` line to its matching
`charged_input_<i>_basename=` line with `awk`, wrote
`/tmp/g9966-n1-sha256.manifest`, and ran `sha256sum -c` on that generated
manifest. No digest was retyped into the command.

Result:

```text
14 /tmp/g9966-n1-sha256.manifest
14/14 OK
manifest sha256 2d8404a11566434183b283bdfb449dbb2bff81bc9224817492bf06ba9b026e18
```

The live repo copies used by the drivers match the frozen inputs:

```text
d20bf0841a1ba2b229d423bb948e6c4474a4f5a83f55148071cae39cb6c506c2  box/moh_skeleton_full.py
501f3b1fed8ad0d26c7535a3c79d6ca74c448a4555f94570a4835d9f740781f3  box/lib/guided_gb.py
f4e0e98e930f5b4ac9f54c8d3660fb8ca9c58dfd20a0500a456df77b47191639  box/orderbasis-20260903/order_basis_full.py
```

The lane artifact manifest is:

```text
box/g9966n1-20260903/artifacts.sha256
sha256 84f5e8a86bb4e4662c6f93767bf85a2c07377c5cf290bd6ae83a404849f56a6f
103 files; sha256sum -c passed.
```

No ledger, `jc2-lean`, or `ideation-*` file was edited.

## Enumeration

Driver:

```text
box/g9966n1-20260903/n1_skeletons.py
```

Output:

```text
box/g9966n1-20260903/skeletons.json
sha256 397694c78c55c113db327bfc8f0983de8c4c69a53fd7ac0e875b3f056a8d937c
```

Command result:

```text
count 8
```

All eight rows have

```text
s=3,  M_1=-66,  M_3=97=n-2,  d=(d_1,d_2,d_3,d_4)=(99,33,11,1).
```

Notation: in this report `v_s=V_s=V_3` and `u_s=d_s-v_s=11-V_3`. This is
not the integration helper named `Skel.u` inside `moh_skeleton_full.py`.

| id | `M` | `V_3,V_2` | `u_s,v_s` | `delta_1,delta_2,delta_3` | `A_1,A_2` | split-order window |
|---|---|---:|---:|---|---:|---|
| S1 | `(-66,-22,97)` | `9,1` | `2,9` | `2/7, 3/14, -1` | `1,14` | `1 < delta < 9/2`; detector orders `3/2,2,5/2,3,7/2,4` |
| S2 | `(-66,22,97)` | `7,1` | `4,7` | `11/16, 9/16, -1` | `1,16` | `1 < delta < 7/4`; detector orders `5/4,4/3,3/2,5/3` |
| S3 | `(-66,22,97)` | `7,5` | `4,7` | `7/12, 9/16, -1` | `3,16` | `1 < delta < 7/4`; detector orders `5/4,4/3,3/2,5/3` |
| S4 | `(-66,22,97)` | `8,1` | `3,8` | `6/11, 4/11, -1` | `1,11` | `1 < delta < 8/3`; detector orders `4/3,3/2,5/3,2,7/3,5/2` |
| S5 | `(-66,22,97)` | `10,1` | `1,10` | `8/23, 2/23, -1` | `1,23` | no genuine split packet (`u_s=1`) |
| S6 | `(-66,55,97)` | `10,2` | `1,10` | `14/39, 1/13, -1` | `3,13` | no genuine split packet (`u_s=1`) |
| S7 | `(-66,77,97)` | `7,8` | `4,7` | `8/13, 7/13, -1` | `1,13` | `1 < delta < 7/4`; detector orders `5/4,4/3,3/2,5/3` |
| S8 | `(-66,77,97)` | `8,8` | `3,8` | `4/9, 1/3, -1` | `3,3` | `1 < delta < 8/3`; detector orders `4/3,3/2,5/3,2,7/3,5/2`; banked |

Each row satisfies `windows_ok=true` and `full_ok=true`; the bottom
`(12)/(13)` test passes on all eight. The banked row S8 is Moh p.202's
published `(99,66)` row and is the row closed by the existing split and
unsplit computations, modulo the N1 skeleton-selection issue.

## Descent Map Used

For a skeleton with top complementary multiplicity `u_s`, the unsplit
descent data used in the charged `(99,66)` banked case are:

```text
n'   = u_s * (n/d_s)
m'   = u_s * (m/d_s)
M_2' = u_s * (M_2/d_s)
V_2' = V_2
K'   = gcd(n',m')
k    = n' - M_2' - 2.
```

For the banked row S8 this gives

```text
(27,18; M_2'=21, V_2'=8; k=4), K'=9,
```

matching the charged K=9 case-A row. This lane uses the same map to type the
other rows, but only promotes a death where the downstream chart was actually
computed.

The descended rows are:

| id | `u_s` | descended unsplit row | status of available instrument |
|---|---:|---|---|
| S1 | 2 | `(18,12; M_2'=-4, V_2'=1; k=20)`, `K'=6`, `u'=5` | outside current K=7/8/9 k=4 pin certificate family; no joint split chart emitted |
| S2 | 4 | `(36,24; M_2'=8, V_2'=1; k=26)`, `K'=12`, `u'=11` | large unsplit pin/order chart required; split chart unbuilt |
| S3 | 4 | `(36,24; M_2'=8, V_2'=5; k=26)`, `K'=12`, `u'=7` | large unsplit pin/order chart required; split chart unbuilt |
| S4 | 3 | `(27,18; M_2'=6, V_2'=1; k=19)`, `K'=9`, `u'=8` | K=9 but not the banked `V_2'=8,k=4` chart; split chart unbuilt |
| S5 | 1 | `(9,6; M_2'=2, V_2'=1; k=5)`, `K'=3`, `u'=2` | order-basis systems emitted; open by timeout |
| S6 | 1 | `(9,6; M_2'=5, V_2'=2; k=2)`, `K'=3`, `u'=1` | order-basis system emitted and killed |
| S7 | 4 | `(36,24; M_2'=28, V_2'=8; k=6)`, `K'=12`, `u'=4` | large unsplit pin/order chart required; split chart unbuilt |
| S8 | 3 | `(27,18; M_2'=21, V_2'=8; k=4)`, `K'=9`, `u'=1` | banked: all three configurations dead |

## Triage

S5 and S6 are `u_s=1` rows. They have no genuine split of the principal-minor
packet. The applicable instrument is the Prop 6.3/6.4 descent to a
monomial-Jacobian order chart, implemented here by the corrected full
order-basis emitter from `order_basis_full.py` and replayed through
`box/lib/guided_gb.py`.

S1, S2, S3, S4, and S7 are `u_s>=2` rows. A full skeleton death would require
two disjoint duties:

```text
split branch:
  a joint two-point chart or minor-root classification at every detector order
  listed in the enumeration table;

unsplit branch:
  the pin + lower-band + guided_gb method on the descended row listed above.
```

The banked joint chart and K=9 pin/lower-band certificates do not transfer by
matching only `u_s` or the split-order set. Their chart hypotheses include the
banked row's actual `M_2=77,V_3=8,V_2=8` data and the descended
`(27,18;21;8;k=4)` row. S4 has the same `(u_s,v_s)=(3,8)` split-order window
as S8, but it descends to `(27,18;6;1;k=19)`, so it is not the banked chart.

The auxiliary integration `N` screen from `moh_skeleton_full.py` was also
checked. It is useful as a priority signal only. Some non-banked groups have
no mixed `N in [6,16]`, but this report does not promote those screens as
deaths because no charged source in this lane supplies the necessary promotion
hook for these rows.

## New Computation: S6 Dead

The S6 skeleton is:

```text
M=(-66,55,97),  V_3=10, V_2=2,  u_s=1, v_s=10.
```

The descended order-basis row is:

```text
(n',m'; M_2',V_2'; k) = (9,6; 5,2; 2).
K'=3, e'=3, q'=2, u'=1.
delta_1'=2/9, delta_2'=-1, B=-5/9.
```

Artifacts:

```text
builder:
  box/g9966n1-20260903/orderbasis/builders/n1_S6_9_6_5_2_k2_part_1_full_builder.sing
rows:
  box/g9966n1-20260903/orderbasis/rows/n1_S6_9_6_5_2_k2_part_1_full_rows.tsv
meta:
  box/g9966n1-20260903/orderbasis/meta/n1_S6_9_6_5_2_k2_part_1_full.json
guided summary:
  box/g9966n1-20260903/guided/S6_part1/guided-summary.json
  sha256 6ae6a6e32fcce496abecde094efb17074d4a83437e038838d1d21b4c709e2b28
```

Native builder controls:

```text
NATIVE_GATE level0_deg_x_before_minus_c=5
NATIVE_GATE target_xk_level0_nonzero=1
NATIVE_DONE equations=54
NATIVE_DONE max_nf_deg_x_after_minus_c=5
```

The guided systems use the 54 order-basis rows plus the single localizer
`T*(sat)-1`; the solver ring has 25 chart variables before `T`. Every Singular
call used one core and was run through `stdbuf -oL -eL`.

Guided results:

| char | verdict | wall | basis | dim | unit | normal forms | missing markers |
|---:|---|---:|---:|---:|---:|---:|---|
| 32003 | `MODULAR_ONLY` | `0.068865s` | 1 | -1 | true | all zero | none |
| 32009 | `MODULAR_ONLY` | `0.037868s` | 1 | -1 | true | all zero | none |
| 32027 | `MODULAR_ONLY` | `0.023568s` | 1 | -1 | true | all zero | none |
| 0 | `UNIT_IDEAL_CHAR0` | `0.035824s` | 1 | -1 | true | all zero | none |

The finite-field unit ideals are recorded only as screens. The death is the
literal exact-Q computation:

```text
verdict=UNIT_IDEAL_CHAR0
returncode=0
timed_out=false
nf_all_zero=true
basis_size=1
dimension=-1
exact script sha256 50e10684a706fcda2bdee70ac0bdb1b1ad7469ec79633b0868cd284ab3820ad3
exact stdout sha256 702cc21d5c1c1b68f1c1d0920066f3fd8ba8e816de67c2d34118601d42c2f689
```

Conclusion:

```text
S6 DEAD by exact-Q UNIT_IDEAL_CHAR0 in the descended full order chart.
```

## New Computation: S5 Open

The S5 skeleton is:

```text
M=(-66,22,97),  V_3=10, V_2=1,  u_s=1, v_s=10.
```

The descended order-basis row is:

```text
(n',m'; M_2',V_2'; k) = (9,6; 2,1; 5).
K'=3, e'=3, q'=2, u'=2.
delta_1'=1, delta_2'=-1, B=-1.
```

Allowed root partitions are `[2]` and `[1,1]`. Both were emitted from the
corrected full order-basis builder.

### S5 Partition `[2]`

Artifacts:

```text
builder:
  box/g9966n1-20260903/orderbasis/builders/n1_S5_9_6_2_1_k5_part_2_full_builder.sing
rows:
  box/g9966n1-20260903/orderbasis/rows/n1_S5_9_6_2_1_k5_part_2_full_rows.tsv
guided summary:
  box/g9966n1-20260903/guided/S5_part2/guided-summary.json
  sha256 4bd9e5f2c0b0a00c411ae07d7785042fe6cb67108821170d0744d4f6a9ab2209
```

Native builder controls:

```text
NATIVE_GATE level0_deg_x_before_minus_c=12
NATIVE_GATE target_xk_level0_nonzero=1
NATIVE_DONE equations=103
NATIVE_DONE max_nf_deg_x_after_minus_c=12
```

Guided results:

| char | verdict | wall | basis | dim | unit | normal forms |
|---:|---|---:|---:|---:|---:|---:|
| 32003 | `MODULAR_ONLY` | `7.388208s` | 1 | -1 | true | all zero |
| 32009 | `MODULAR_ONLY` | `7.492472s` | 1 | -1 | true | all zero |
| 32027 | `MODULAR_ONLY` | `7.158966s` | 1 | -1 | true | all zero |
| 0 | `INCONCLUSIVE_TIMEOUT` | `120.066659s` | none | none | false | no basis marker |

The earlier raw exact-Q `std` run also reached its 300-second cap and wrote
`halt 1` without `MAIN_DONE`. Since this is an inhomogeneous/localized system,
the three modular units do not promote to characteristic zero.

Status:

```text
S5 [2] OPEN[FULL-BASIS-TIMEOUT].
```

### S5 Partition `[1,1]`

Artifacts:

```text
builder:
  box/g9966n1-20260903/orderbasis/builders/n1_S5_9_6_2_1_k5_part_1_1_full_builder.sing
rows:
  box/g9966n1-20260903/orderbasis/rows/n1_S5_9_6_2_1_k5_part_1_1_full_rows.tsv
guided summary:
  box/g9966n1-20260903/guided/S5_part1_1/guided-summary.json
  sha256 d0689187e0dd12b353daf95d3d581473963dee40b33efa7aaae1b623ac8ce88b
```

Native builder controls:

```text
NATIVE_GATE level0_deg_x_before_minus_c=12
NATIVE_GATE target_xk_level0_nonzero=1
NATIVE_DONE equations=103
NATIVE_DONE max_nf_deg_x_after_minus_c=12
```

The guided `F_32003` run timed out:

```text
char=32003
verdict=INCONCLUSIVE_TIMEOUT
elapsed_seconds=120.035444
timed_out=true
missing markers: NF_ALL_ZERO, UNIT, DIM, BASIS_SIZE, LEAD_DIM, ACCEPT, NF_ZERO
```

The raw 300-second `std` attempts over `F_32003` and `F_32009` also wrote
`halt 1` without a basis marker. The `F_32027` and Q runs were not launched
after the first repeated timeout, because they could not turn the partition
into a valid death without a completed exact-Q result.

Status:

```text
S5 [1,1] OPEN[FULL-BASIS-TIMEOUT].
S5 skeleton OPEN because both partitions must die and neither has exact-Q death.
```

## Banked Row S8

S8 is the existing `(99,66)` skeleton:

```text
M=(-66,77,97), V_3=8, V_2=8, u_s=3, v_s=8.
```

Consumed banked results:

```text
split configurations:
  delta=2, partition [2,1]
  delta=5/2, partition [1,1,1]
  killed by the joint two-point chart / pivot ledger.

unsplit configuration:
  descended case (27,18; M_2'=21,V_2'=8;k=4), K'=9
  killed by the pin + lower-band + guided_gb method.
```

The charged K=8/K=9 report records the unsplit K=9 internal split A and B
exact-Q unit certificates:

```text
K9 A: UNIT_IDEAL_CHAR0, Q wall 11.087997s, 97 generators, NF all zero.
K9 B: UNIT_IDEAL_CHAR0, Q wall 27.618173s, 99 generators, NF all zero.
```

The banked manifest was rechecked:

```text
box/k4rayk89-20260903/authoritative-seal-sol56/authoritative-artifacts.sha256
sha256sum -c passed.
```

The standard guided controls were also rechecked from the banked control
manifest:

```text
box/k4rayk89-20260903/controls-artifacts/artifacts.sha256
sha256sum -c passed.
```

That control suite includes:

```text
tame automorphism survives:
  exact-Q nonunit, dim 0, length 1 for a literal polynomial automorphism.

Hilbert-hint perturbation fails:
  main accepted with length 3640; perturbed hint rejected with the same computed length.

charged K=7 pinned light:
  exact-Q UNIT_IDEAL_CHAR0 replay, basis 1, dim -1, NF clean.

charged K=4 and K=5 low-K controls:
  exact-Q UNIT_IDEAL_CHAR0 replays; calibration only, no transfer.
```

S8 remains:

```text
BANKED DEAD, modulo only N1 when used as the unique degree-(99,66) skeleton.
```

## Open Non-Banked `u_s>=2` Rows

The following rows are not killed in this lane. Each requires an actual branch
certificate; none is dead merely because it is not the Moh-selected row.

### S1

```text
M=(-66,-22,97), V_3=9, V_2=1, u_s=2, v_s=9.
split detector orders: 3/2, 2, 5/2, 3, 7/2, 4.
unsplit descended row: (18,12; M_2'=-4,V_2'=1;k=20), K'=6, u'=5.
```

Required systems:

```text
split: joint two-point/minor chart at every listed detector order, with
       degree-u_s leading polynomial and the S1 characteristic data.
unsplit: pin/lower-band/order chart for the K'=6, k=20 descended row.
```

Reason open: no charged detector-window chart or K'=6/k=20 pin system was
available or emitted in this lane. `OPEN[UNBUILT-SPLIT-AND-UNSPLIT-CHARTS]`.

### S2

```text
M=(-66,22,97), V_3=7, V_2=1, u_s=4, v_s=7.
split detector orders: 5/4, 4/3, 3/2, 5/3.
unsplit descended row: (36,24; M_2'=8,V_2'=1;k=26), K'=12, u'=11.
```

Required systems:

```text
split: joint two-point/minor chart at the four listed detector orders.
unsplit: K'=12 pin + lower-band + guided_gb chart with V_2'=1, k=26.
```

Reason open: outside the K=7/8/9 k=4 solved family; expected heavy K'=12
formation and no exact-Q system emitted. `OPEN[UNBUILT-K12-CHART]`.

### S3

```text
M=(-66,22,97), V_3=7, V_2=5, u_s=4, v_s=7.
split detector orders: 5/4, 4/3, 3/2, 5/3.
unsplit descended row: (36,24; M_2'=8,V_2'=5;k=26), K'=12, u'=7.
```

Required systems:

```text
split: same detector orders as S2 but with V_2=5 and the S3 bottom data.
unsplit: K'=12 pin/lower-band chart with V_2'=5, k=26.
```

Reason open: same chart families as S2 are unbuilt, and a matching bottom
`V_2` is not a harmless relabeling. `OPEN[UNBUILT-K12-CHART]`.

### S4

```text
M=(-66,22,97), V_3=8, V_2=1, u_s=3, v_s=8.
split detector orders: 4/3, 3/2, 5/3, 2, 7/3, 5/2.
unsplit descended row: (27,18; M_2'=6,V_2'=1;k=19), K'=9, u'=8.
```

Required systems:

```text
split: joint two-point/minor chart in the same numerical order window as S8,
       but with S4 characteristic data.
unsplit: K'=9 pin/lower-band chart with V_2'=1, k=19.
```

Reason open: the banked K=9 certificate is for `(27,18;21,8;k=4)`, not this
row. `OPEN[NONBANKED-K9-CHART-UNBUILT]`.

### S7

```text
M=(-66,77,97), V_3=7, V_2=8, u_s=4, v_s=7.
split detector orders: 5/4, 4/3, 3/2, 5/3.
unsplit descended row: (36,24; M_2'=28,V_2'=8;k=6), K'=12, u'=4.
```

Required systems:

```text
split: joint two-point/minor chart at the four listed detector orders.
unsplit: K'=12 pin/lower-band chart with V_2'=8, k=6.
```

Reason open: the integration screen has no mixed `N in [6,16]`, but that is
not promoted here; the actual K'=12 and split systems remain uncomputed.
`OPEN[UNBUILT-K12-CHART]`.

## Open `u_s=1` Row S5

S5 has no split branch, so the exact system is already explicit:

```text
descent row (9,6; M_2'=2,V_2'=1;k=5),
full order-basis partitions [2] and [1,1],
localization by T*(sat)-1,
rows emitted under box/g9966n1-20260903/orderbasis/.
```

The `[2]` partition has three clean finite-field unit screens but exact Q
timed out. The `[1,1]` partition timed out already over `F_32003`. Therefore:

```text
S5 OPEN[FULL-BASIS-TIMEOUT].
```

No modular unit is promoted to characteristic zero.

## Run Log

Commands were foreground jobs with explicit timeouts and one-core Singular
processes.

```text
sha256sum -c /tmp/g9966-n1-sha256.manifest
python3 -m py_compile box/g9966n1-20260903/n1_skeletons.py
python3 box/g9966n1-20260903/n1_skeletons.py
python3 -m py_compile box/g9966n1-20260903/n1_orderbasis_probe.py
python3 box/g9966n1-20260903/n1_orderbasis_probe.py prepare --rows S5,S6
python3 box/g9966n1-20260903/n1_orderbasis_probe.py build-rows --builder-timeout 180
python3 box/g9966n1-20260903/n1_orderbasis_probe.py emit-systems --chars 0,32003,32009,32027
python3 box/g9966n1-20260903/n1_orderbasis_probe.py solve --solve-timeout 300
python3 -m py_compile box/g9966n1-20260903/n1_guided_orderbasis.py
python3 box/g9966n1-20260903/n1_guided_orderbasis.py --meta ...S6... --chars 32003,32009,32027,0 --timeout 120
python3 box/g9966n1-20260903/n1_guided_orderbasis.py --meta ...S5_part_2... --chars 32003,32009,32027,0 --timeout 120
python3 box/g9966n1-20260903/n1_guided_orderbasis.py --meta ...S5_part_1_1... --chars 32003 --timeout 120
sha256sum -c box/k4rayk89-20260903/controls-artifacts/artifacts.sha256
sha256sum -c box/k4rayk89-20260903/authoritative-seal-sol56/authoritative-artifacts.sha256
sha256sum -c box/g9966n1-20260903/artifacts.sha256
```

One early solve batch was interrupted after the S5 `[2]` exact-Q run had
stalled; the orphaned child process was found and terminated before further
work. Those partial raw logs are retained only as timeout diagnostics. The
promoted S6 certificate is the later guided replay, not the interrupted batch.

## FALLACY-v2 Audit

Skeleton deaths are promoted only from actual certificates. S6 has a literal
exact-Q unit ideal in the descended full order chart. S5 has modular units and
timeouts, not a death. S1-S4 and S7 have typed chart obligations, not deaths.

No flag/place/series identification is made. The split detector order is not
identified with an actual split unless a branch chart is supplied. For
`u_s>=2`, the split and unsplit duties are kept separate.

No modular unit is promoted to characteristic zero. The S6 finite-field runs
are screens; the exact-Q guided run supplies the death. S5 modular units are
explicitly left as `MODULAR_ONLY`.

The order-basis systems extract the ideal component in the declared ring and
localize by a displayed `T*(sat)-1` generator. Builder gates verify that the
target `x^k` row exists before subtracting `c*x^k`.

The old sparse order-chart deaths are not used for S5. The corrected full
order-basis report warned that sparse prefix charts can be strict
sub-approximations; this lane used the corrected full inventory for the
`u_s=1` probes.

No exit-price assertion is made, so no `charge_basis` line is emitted.

## Final Typed Result

```text
ENUMERATION: 8 Moh-(1)-(13)-admissible skeletons at (99,66).

BANKED:
  S8 = V=(8,8) is the Moh p.202 row; already dead in all three configurations,
  modulo N1.

NEW DEAD:
  S6 = (M_2,V_3,V_2)=(55,10,2)
       DEAD by exact-Q UNIT_IDEAL_CHAR0 in the descended full order chart.

OPEN:
  S1, S2, S3, S4, S7 need unbuilt u_s>=2 split and unsplit chart systems.
  S5 has emitted u_s=1 descent systems but remains OPEN[FULL-BASIS-TIMEOUT].

OVERALL:
  PARTIAL, 1 of 7 non-banked rows dead.
  N1 not closed.
  No degree-wide NO-KELLER-PAIR theorem for (99,66) is promoted here.
```

<!-- BODY-END -->
