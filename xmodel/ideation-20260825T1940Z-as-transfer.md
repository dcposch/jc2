# Blind whole-portfolio delta: transfer finite-state obstruction machinery beyond D12

Date: 2026-08-25 19:40Z  
Owner: AS / common-core lane  
Protocol: independent read of the current 46-row `APPROACHES.md`, current
gaps/evidence in `PROGRESS.md`, and the fixed-D12 classical-closure note.  No
sibling submission from this round was read.  No computation was launched.

## Significant-news correction

The strict B9 calculation produced a useful *method* object: an exact global
fresh-digit elimination followed by a Kuranishi map, compressed from 176 to
110 original output coordinates with a certificate.  It is not a live
counterexample search.  GGV's `gcd(deg P,deg Q)>=16` condition closes every
characteristic-zero envelope with both total degrees at most 12, including
the broad B8/B9 cells and every degree-drop stratum.  The only live receiver
for this machinery keeps a bounded partial-`y` profile while allowing
coefficient-`x` and total degrees to grow without a fixed cap.

This changes the routing, not the validity of the frozen D12 arithmetic.  D12
is now a calibration corpus for source compilers, cokernel compression,
nilpotent/rank strata, and proof custody.  Allocate no new solver expansion to
it solely to seek a counterexample.

## Ranked transfer bets

### 1. Coefficient-function Ore--Cartier state system

For the live `(9,12)` partial-`y` box, write

```text
P = sum_(i=0)^9 p_i(x)y^i,     Q = sum_(j=0)^12 q_j(x)y^j.
```

The determinant equation has exactly 21 `y`-coefficient equations, in 23
unknown polynomial coefficient functions.  At a source point its exact
linearization is

```text
D_F(U,V) = U_x Q_y + P_x V_y - U_y Q_x - P_y V_x.
```

This is a concrete 21-by-23 first-order differential/Ore operator, not the
previously unnamed D-module slogan.  Its image, kernel, Cartier cokernel in
characteristic 3, and boundary condition at `x=infinity` are the natural
unbounded-degree analogues of the frozen finite Kuranishi map.  Polynomiality
is an infinity/tail condition; it must not be replaced by a finite coefficient
cap.

Cheapest source-honest experiment: register one complete AS partial-`y` source
chart; compile the top `x`-tail transition and cokernel for symbolic tail
degree `N`, then compare the exact systems at `N,N+3,N+6` on AWS.  A finite
state claim is licensed only by a symbolic recurrence or a proved degree
bound, never by agreement of those controls.  Stop this route if the minimal
state/cokernel dimension grows with `N` and no invariant quotient appears
after two symbolic prolongations.

This directly fuses avenues 3, 16, 19, 21, 29 and 45, with avenues 4 and 46 as
certificate infrastructure.

### 2. Cofinal common-leading-core reduction with symbolic degree

For a characteristic-zero Keller pair of total degrees `m=rd`, `n=sd`, with
`gcd(r,s)=1`, leading-form dependence gives

```text
P_m = a H^r,     Q_n = b H^s,     deg(H)=d.
```

If `e=deg_y(H)>=1`, the partial-`y` bounds force `r e<=9` and `s e<=12`, so
only finitely many `(r,s,e)` occur although `d>=16` is unbounded.  Homogeneity
also gives

```text
H = x^(d-e) h_e(x,y),     deg(h_e)=e.
```

Thus a large part of the infinite total-degree frontier can be represented by
a bounded low-`y` core plus the integer parameter `d`.  The pure-`x` stratum
`e=0` is separate: the partial-`y` bounds do not bound `r,s`, so it must not be
silently folded into the finite menu.

Cheapest experiment: enumerate the finite `e>=1` triples, derive the first
two or three determinant bands over `Z[d]`, and compute their exact
content/Fitting factors.  In the 3-adic lane stratify by the valuation data of
`d`, not merely `d mod 3`.  A useful output is either a uniform killed band or
a bounded transition state.  Stop if the required band depth or state rank
grows with `d` (or `v_3(d)`) without a recurrence; do not replace that failure
by sampling more degrees.

This is the cleanest bridge from avenues 1--3, 5, 6, 28, 31 and 43 to the live
partial-degree frontier.

### 3. Normalization-covariant obstruction atlas

The finite Kuranishi coordinate is only portable if it transforms correctly
under the source/target normalizations used by the boundary books.  Package
the image/cokernel/Fitting data as a chart object with explicit transition
matrices, including rank-drop and nonreduced strata.  Use the already closed
B8/B9 D12 charts as a cheap covariance control: compute the same obstruction
before and after the exact tame transport and demand equality of the pulled
back Fitting ideals.  Failure identifies missing source/gauge state; success
licenses the compiler architecture, not a D12 counterexample claim.

The first live client is then an unbounded-total partial-`y` chart attached to
a source-honest infinity/boundary datum.  This joins avenues 2, 4, 7, 21, 25,
27, 32, 38 and 46.  Stop if chart transitions require the discarded full
coefficient history rather than a bounded obstruction state.

## Whole-46 disposition

- Direct receivers of the transfer are avenues 1--4, 7, 16, 19, 21, 28, 29,
  31, 32, 45 and 46.
- Useful structural or falsification controls are 5, 6, 25--27, 33, 34, 38
  and 43.
- This event supplies no new allocation reason for 8--15, 17, 18, 20,
  22--24, 30, 35--37, 39--42 or 44: those lanes are already refuted, are
  strictly stronger/category-mismatched, or still lack a named invariant and
  live receiver.  In particular generic sparse/SAT search (36) is not a
  substitute for a cofinal unbounded-total chart.

## Allocation and global wall

Proposed next-cycle allocation:

```text
40%  source-first unbounded partial-y Ore/Cartier tail compiler
25%  Q8/source-horizontal infinity and saturation coverage
20%  TD6/global boundary-cover completion
10%  normalization-covariance and certificate software
 5%  explicit landing/cofinality theorem work
```

Spend zero new theorem-search CPU on strict fixed-D12 formulas.  Retain only
bounded D12 replay/covariance controls.

The global wall remains explicit: even a theorem for one unbounded-total
partial-`y` residue family does not resolve JC2 without a proved landing or
cofinal partial-`y` bound.  Conversely, a finite-state recurrence seen in a
closed D12 box does not transfer until the source equations, infinity state,
normalization transitions, and collision/integrality firewalls are all
reconstructed in the live unbounded family.

## Preregistered first gate

Before any heavy launch, freeze a frontier-gate specification containing:

1. the complete 21 coefficient-function equations and source normalization;
2. the exact Ore linearization and Cartier/carry convention;
3. the chosen symbolic tail degree and all infinity boundary rows;
4. the state equivalence relation and transition reconstruction test;
5. positive omission/orientation controls and a literal determinant replay;
6. the stop rule above and a firewall against interpreting bounded `N`
   controls as cofinal coverage.

That is the cheapest experiment which can genuinely distinguish transferable
finite-state structure from another fixed-cap artifact.

