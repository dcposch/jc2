# D125 exceptional center: a uniform critical-resultant restriction

2026-09-07. **DESK PASS, PROVISIONAL pending independent review.** Every genuine finite arc of the accepted odd moving-face source approaching t₀=−3 must have

    Δ₀ := γ−βα+5α²/9 = 0

at its field-valued center A₀=R³+αR, B₀=R⁵+βR³+γR, R=R_{−3}. This excludes the entire exceptional-center open set Δ₀≠0 for arbitrary finite ramification, without the unreviewed generic uniform theorem. It does NOT exclude the tuned Δ₀=0 strata, prove existence of any degeneration, or decide the guarded source.

The formula Δ₀=0 already appeared as a necessary FIRST unramified condition in the accepted classification/deformation interface. The new scoped content is its uniform finite-arc necessity, with all intermediate orders retained. The bounded history check read the named terminal source/classification/low/pure-power reports and searched the matching canonical approach entries; no exhaustive novelty claim is made. Neither live uniform nor other live gate bodies were read.

## 1. Source, field and the two exceptional branches

Work over any characteristic-zero field K with coefficients in K[[s]]. Assume all full source Jacobian and ordinary-lift rows, and a nonzero k(s) of finite positive order m. The inverse guard is interpreted only over K((s)); it need not extend to the special point. No normalization k=s^m or field extension is required.

Use the accepted14c source,14f field classification and14g saturated low equations. Set

    R=p²(p³+g³−3p),  S=p(p²+gp−1),
    a(z)=3z²+α,   b(z)=5z⁴+3βz²+γ.

R is NOT squarefree, and no squarefree inference is used. It has degree5, origin order3, total top H=p²(p³+g³), weight(5,−7) equal1, and [g²p]R=[p]R=0. Its ordinary lift is under φ(g)=v⁻¹, φ(p)=v⁴u−v−v⁻¹, with φ(R)(u,0)=3u.

The accepted full low equations give [p]A=0, 9e=5kx and x²=3ky, where e=[p]B, x=[gp²]A and y=[p³]A. Since x₀=0, e has order at least m+1. At this center y₀=−3α. Thus:

| Center | Already forced by low rows | New necessary condition |
|---|---|---|
| α≠0 | y is a unit, m is even, ord(x)=m/2 | γ=βα−5α²/9 |
| α=0 | y₀=0; no even-m conclusion follows alone | γ=0 |

The new condition is unchanged by a whole target shear B→B−λA: β→β−λ and γ→γ−λα. Therefore the argument applies before or after the optional whole B₁₅ slice; it never deletes a β term without its γ correction.

## 2. The fixed scalar pivot when Δ₀≠0

The exact scalar identity is

    b(z)=a(z)(5z²/3+β−5α/9)+Δ₀.                    (1)

Suppose Δ₀≠0. For polynomial receiver coefficients A_ℓ,B_ℓ, if their first-order relation at this center is

    a(R)[R,B_ℓ]−b(R)[R,A_ℓ]=0,

then a(R)B_ℓ−b(R)A_ℓ=f(R) for a scalar polynomial f. The centralizer is K[R]: the accepted characteristic-zero common-generator theorem, prime degree5 and the non-power top of R prove this even though R is not squarefree. The imported primary is Arzhantsev–Petravchuk, [Lemmas4–5 p5](https://arxiv.org/pdf/math/0608157v2), at the same external-theorem trust already charged in14f.

Everything is odd. The remainder of f(z) modulo the even quadratic a(z) is therefore λz. By (1), reduction modulo a gives A_ℓ≡cR with c=−λ/Δ₀. Explicit polynomial Bezout, not a generic rank assertion, yields

    A_ℓ=cR+a(R)U,
    B_ℓ=b(R)U+dR+f₃R³.                             (2)

To see the second statement, substitute the first into the bracket relation; cancellation of a(R) gives [R,B_ℓ−b(R)U]=0. The degree≤23 and oddness of that difference leave only R and R³. Denominators here are scalar units Δ₀,3 and9 in K. No source parameter is localized on the tuned Δ₀=0 stratum: it is explicitly excluded from this lemma.

## 3. The actual small quotient support and lift

For every positive coefficient A_ℓ, the full fixed total face gives deg A_ℓ≤13, and its source weight is≤3. Since deg a(R)=10 and weight a(R)=2, (2) implies U is odd, deg U≤3 and weight U≤1. These three conditions ALONE leave exactly

    U=u₁p+u₃p³+u₁₂gp².

No lower polygon inequality is transported through division. In particular, when α=0 the lower-edge weight of a differs; that fact is irrelevant to this enumeration.

A_ℓ and cR lift ordinarily. The leading v⁰ coefficient of φ(a(R)) is 27u²+α, a nonzero polynomial even when α=0. Hence φ(U) cannot have a negative lowest v-power: its product with that nonzero leading coefficient could not cancel. The COMPLETE negative part of φ(U) has rows

    [v⁻³]=−u₃+u₁₂,      [v⁻¹]=−u₁−3u₃+2u₁₂.

Consequently U=qS. Since R has origin order3 and S has p coefficient−1, (2) now gives the exact low linear coefficients

    [p]A_ℓ=−αq,       [p]B_ℓ=−γq.                  (3)

If α≠0, the full [p]A=0 equation forces q=0. If α=0, Δ₀≠0 means γ≠0; for ℓ≤m the low relation ord(e)≥m+1 forces q=0 instead. Thus at every such coefficient to which the first-order relation applies, A_ℓ=cR and B_ℓ=dR+f₃R³.

## 4. Why this is uniform in ramification

Induct for ℓ=1,…,m. The target bracket −5k(s)³g²/9 has order3m, strictly greater than m. Assume all earlier positive A_i,B_i are polynomials in this SAME fixed R; this is vacuous at ℓ=1. Every cross bracket [A_i,B_(ℓ−i)] with 0<i<ℓ is then exactly zero. The coefficient equation at ℓ is precisely the first-order relation in §2, with the original a,b. No mixed/nonmultiple order or moving coefficient is discarded. Sections2–3 show A_ℓ,B_ℓ are again polynomials in R.

At ℓ=m this is impossible: A_m=cR has [g²p]=0, whereas the actual prescribed A inner face has [g²p]A=k(s), whose coefficient at m is nonzero. This proves Δ₀=0 is necessary. The argument stops at the FIRST moving face, not at arbitrarily many Jacobian orders; no repeated m-case calculation is needed.

It is a theorem about genuine finite arcs over fields, not arbitrary nilpotent coefficient bases. The use of low saturation is justified by the nonzero generic k in a domain. No inverse for a nilpotent k has been introduced.

## 5. Exact remaining gap and an actual boundary countercontrol

On the remaining α≠0,Δ₀=0 stratum, (1) has no scalar unit remainder: a divides b. The first-order relation only gives B_ℓ=q(R)A_ℓ+h(R), leaving genuine transverse possibilities. The accepted unramified order-two theorem does not supply a uniform ramified obstruction there. A tiny formal R=g control with A₁=p³ and B₁=(5g²/3−10/9)p³ at α=2,β=0,γ=−20/9 commutes to first order. This is only a bracket-interface toy: p³ does not satisfy the actual ordinary lift and it is NOT a full-source jet.

When α=0, the remaining condition is γ=0; a whole βA shear makes the center pure powers. The generic squarefree/origin argument cannot simply be copied. An ACTUAL full unguarded boundary family demonstrates the origin problem:

    k≡0,  A=R³+sR,  B=R⁵,  R=R_{−3}.

By the accepted boundary classification, it obeys all fixed faces, ordinaryness, zero bracket and saturated low equations, without expanding any full powers. But its formal cube root has first correction1/(3R), with receiver-origin valuation−3. Thus no-linear-A does NOT force positive origin valuation of cube-root corrections at this exceptional center. This countercontrol has k identically zero, so it neither bypasses the new theorem nor supplies a ramified guarded survivor. The repeated p² factor also prevents transferring generic squarefreeness statements.

The cheapest remaining theorem interface is therefore the tuned critical-fiber stratum α≠0,Δ₀=0, separately from the α=γ=0 pure center. No further theorem or computation is claimed here, and neither stratum has been ruled out uniformly.

## 6. Evidence and custody

Twelve final capped normal/−O controls pass with byte-identical rational-string witnesses and zero Assert nodes. Actual mutations change the scalar remainder sign, omit one of the two U-lift rows, omit the actual low-A or low-B constraint, or perturb the full first-order bracket identity. The source expansions are only degree≤5 factors and cubic U lifts; powers through5 occur solely in formal R=g toys. No actual R³,R⁵,R²S, degree15/25 pair,803-row source, CAS/AWS/SSH or solve was expanded.

Final witness SHA256 `ccf4b97501b337ef02bd1d6d5733dc47f004b513af90a312819779297e395145`. Use ordinary and −O invocations of `python3 -B box/d125-exceptional-center-discriminator-20260907/check.py`; `--record` is exclusive-output production. The preliminary witness is retained; final replay removes an unnecessary lower-edge restriction from the U enumeration. The witness bytes are unchanged, and only `final-replay.json` is the charged current-code run.

All exact input and owned pins are in custody. This is a new PROVISIONAL finite-arc restriction, not a new full-source equation that may automatically be added to the guarded client. No dependence on the unreviewed uniform generic result, no degeneration-existence assertion, no exceptional closure/guarded-emptiness or JC2 claim. All writers idle on handoff. **STOP/IDLE.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `9273`.
- Body SHA-256:
  `9c9645d71da745bc0f00f9aaf5efbd44a3ea015892fc107cf293e5628c30926d`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
