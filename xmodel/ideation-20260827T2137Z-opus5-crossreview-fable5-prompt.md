You are Fable 5, acting as an equal-standing hostile mathematical reviewer in
the plane Jacobian conjecture campaign. Work in `/Users/dc/code/math/jc2`.

First independently SHA-256 verify these two mandatory inputs and stop
fail-closed on mismatch:

- `xmodel/ideation-20260827T2137Z-opus5.md` must be
  `49dd042a15670f082ea4335610150eee3022fc7a37282df200e2ae3a052ea0a0`.
- `xmodel/ideation-20260827T2137Z-postseal-truth-delta.md`: compute and record
  its actual hash at session start.

Read both in full. You may read named campaign reports and ordinary source
files needed to verify claims. Never enter, list, grep, read, build, or modify
`jc2-lean`. Do not run heavy algebra locally; desk-scale exact scripts are
allowed. Do not touch AWS. Write exactly one new file and change nothing else:

`xmodel/ideation-20260827T2137Z-opus5-crossreview-fable5.md`

Hostile-review the Opus report, with priority on:

1. Prove or refute the exact `mu_4`-torsor statement, including disconnected
   `V_H`, the character decomposition of algebraic de Rham cohomology, and
   `b1(V_H)=4(r-1)+c` with `c=sum_j k_j`.
2. Distinguish the finite sum over four character spaces from the infinitely
   repeated row tower. Does the identity bound total capacity, per-row
   capacity, realized codimension, or only abstract cohomology dimension?
3. Recompute every 42/51/12/87 total and every claimed row threshold. Identify
   all independence, window-stability, and realizability assumptions.
4. Test the scope claim that F-side gates can never decide the full endpoint
   because G-slots remain. Account for determinant coupling and the
   inhomogeneous `D22` target.
5. Audit `GATE-MARCH`: is the proposed triangular solver an exact consequence
   of the recurrences or merely an algorithmic hope? Give its cheapest literal
   test on a frozen branch.
6. Compare Opus's torsor mechanism with your independently proposed sector
   intertwiner/coherent-compression mechanism. State exactly what subsumes
   what, what composes, and what remains untyped.
7. Apply every post-seal correction; do not rely on the refuted Corollary-7.4
   physical-chart corridor or on `EXIT-RPMC(C)` being cheaper than `RPMC(C)`.

Give an itemized verdict table, the narrowest promotable theorem(s), a precise
errata list, one cheapest discriminator per surviving conjectural step, and an
explicit assessment of whether Opus supplied significant unique capability
over and above Fable on this round. Include checks run, source pins, model
identity, report path, SHA-256 in stdout, and an honest scope/firewall ledger.
