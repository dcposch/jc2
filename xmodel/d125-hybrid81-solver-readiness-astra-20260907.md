# Hybrid81 exact solver: disabled worker-readiness plan

2026-09-07. Author `/root/model_productivity`. **PREP ONLY / NO CURRENT WORKER
READINESS OR AUTHORITY.** No new runner/framework, fixture or executable is needed
at this stage. Root's worker remains reported STOPPED; there was no AWS/SSH,
remote access, deployment, source parse, CAS or solver call in this task.

The exact checklist is
`box/d125-hybrid81-solver-readiness-20260907/READINESS.md`, SHA
`aca30ac4e95b2eeb7a6bd67d48939284d81d12586bb0744c5ce39c7e601e5549`.
The parameter/evidence manifest is the adjacent `REUSE.json`, SHA
`78eea04c20c8658c5d00b8dfa29ba3857cb06f1b33731014bec3b6985a7fafba`.
It deliberately has authorized=false and null fresh-boot/gate/fixture fields.
It is not an authority schema accepted by the frozen driver. Only its JSON
syntax/status was checked locally; no host controls were executed or fabricated.

## Exact reuse and one new engineering interface

Frozen `box/d125-hybrid81-solver-prep-20260907/REGISTRATION.md` remains unchanged,
SHA `38bc496f9f2eb7f7519c70bbabe0eab8586c56264b7e20c0078fc71cb818cd6d`.
The new checklist does not replace that code-bound registration. Likewise no
charged adapter/caller/checker/test code or live Sol artifact was changed/read.
Sol may supply an independent tiny fixture; its existence and contents are
unknown until terminal handoff, so this task neither duplicates nor claims it.

The old driver control branch alone is insufficient: it emits its generic
zero/duplicate ideal without going through the new hybrid adapter. The later
engineering action must serialize a declared tiny hybrid stream via
`H.read_hybrid(...,production=False)`, append the unchanged
`D.footer(...,control=True)`, and import that ordinary input in actual Singular,
using only direct lift to1, not slimgb/std/dimension. Require rational terms,
zero/duplicate positions and a nontrivial inverse guard with an independently
known unit identity. The actual complete I_SIZE/I/G/T output must map and replay
against every tiny literal slot normally and under `-O` with empty stderr.

A small future helper can reuse `D.context(authority,'control')`,
`D.limits('control')`, exact CAPRUN-parent/PGID enforcement and exec-in-place
Singular. Its fixed fixture/helper hashes must enter root's physical engineering
GREEN before dispatch. No helper is implemented now while the independent gate
may supply it. There is no production SOURCE read in that control, and a
control-mode token cannot authorize the frozen driver's decision/verify modes.

No generic safety rewrite is proposed. The accepted unchanged limits/CAPRUN and
same pinned binaries already have concrete evidence:

- stdout/stderr inherited-FSIZE controls:4096-byte output and SIGXFSZ on both
  streams through the same driver.limits function (64MiB remains production).
- TERM-ignoring descendant RSS control: exact-anchor validation, TERM/KILL,
  leader reaping and whole-group absence.
- Actual long explicit rational/polynomial printing and exact parsing.

The four exact old evidence paths/hashes are in REUSE.json. The terminal old
engineering report and150-line harness were read, and these four local hashes
were rechecked. A fresh boot/path/Owner/process audit is still necessary, but
does not alone invalidate the old controls. Only a named changed-path cleanup
gap would justify one separately authorized old no-CAS fixture, not an automatic
repeat of all prior controls. Material executable/runner drift instead blocks.

## Fresh worker and bounded dispatch

Only retained `i-0da0cebfc97c9fd54`, Owner
`coordinator-factored-jacobian-20260906`, EBS `vol-0eb6450d18ffa89f1` is eligible.
Root alone STARTs/STOPs; never terminate/delete/retag. Resolve current running IP
after root START. Fresh Linux/Amazon EC2 DMI board_asset_tag, boot, hostname,
EBS serial/root mount, processes/autostart, memory/swap/disk and pinned binaries
must match. Unexpected owner/workload/path/storage is a stop, not cleanup scope.
Never reuse a historical PID as a signal target after reboot.

Exact new cwd is `/home/ubuntu/d125-hybrid81-exact-solver-20260907`; it must be
absent before separately authorized exclusive deployment. Preserve the frozen
construction at `/home/ubuntu/d125-hybrid-affine-pilot-20260907/construction.jsonl`,
SHA `4ea526b2680ef41154332be3059ae5543c2678fdf8fb6a95d02176e8eb929272`,
and original source on the same retained EBS. REUSE.json contains all code,
source/registration and expected Singular/Python/prlimit/CAPRUN pins; these are
future checks, not newly observed worker state.

Engineering and production require distinct fresh physical GREENs and authority
paths. A later tiny engine job is at most10wall/CPU512MiB,64MiBFSIZE/core0.
The prospective full attempt remains one300-second parse/serialize/import/
slimgb/conditional lift, optional120-second exact verification and450 total,
16GiB AS/sampledRSS,64MiBFSIZE per stream/core0,2MiB generated input including
suffix. No separate full import warmup is implied. No source subset, gauge,
order change, cap raise, retry or token refresh follows from this checklist.

Before every authorized dispatch, use lexists for all authority/engineering/
decision/verify output paths; bind the exact code/gate/fixture/source/boot and
absolute deadline. Retain prompt actual PID/PGID/start_ticks/namespace/cgroup
metadata, observe metadata only while live, read terminal telemetry first and
prove current whole-group absence. Partial/capped/invalid results are
INCONCLUSIVE. Exact unit/properness criteria stay those of the frozen plan.

There is no new standing full algebraic replay requirement: accepted separate
cover-replayer review plus completed all803 replay remains the source evidence.
Only the new adapter/caller review, concrete tiny actual-engine interface and
root's fresh physical dispatch checks are pending. All local writers are now
terminal. **IDLE, waiting for separate root physical GREEN.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5957`.
- Body SHA-256:
  `10571344762ecda1efa8063fdca62d65349bae8e722189f90032e3dc1366f0f3`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
