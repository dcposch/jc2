# Blind whole-portfolio ideation — Fable5 — round `20260827T2137Z`

Author: Fable 5 (Anthropic), exact model `claude-fable-5`, adapter
`ops/adapters/claude.sh`.
Role: equal-standing whole-portfolio core co-researcher; independent
submission over the complete 46-avenue portfolio.
Status: **SEALED BLIND SUBMISSION.**  Written before reading any peer
report of this round.  Everything below is ideation/allocation input, not
promoted evidence; nothing here is a JC2 result.

## 0. Custody, inputs, and execution boundary

Verified before use (all match the sealed packet):

```text
a492f5eeeb5786f9061bf3730a077b6e256e8386c5dcb42c978bbda5519aac80  xmodel/ideation-20260827T2137Z-packet.md
96c3d71627b1786c15cfb069fae8880530d0fd2e27f633459be84dc429e27855  COORDINATION.md
122e7fa1e51d187799cdb61f75c18e63fd93499e40c371222da202604c39bd54  APPROACHES.md
8376ac2e5a156f8ab2c45def8f5c2e5f1c4ceaf68df1ab81f50fc4d04317910a  AUDIT.md
daa54a2b52f74b4986668cc1922ca1a2a332d474b50f0edf713631ebea9a5c61  PROGRESS.md
eee2ec8609fdcf388e392e85ba9fc87c83f7873986b173d25fd74713dac3c457  notes.md
23029f3486e3a429e446610c36859625502e8f49b2d8b5db2bd9ef0359d45e6f  xmodel/ideation-20260827T1808Z-synthesis-sol.md
a8a1ae4f888a73a7db7ea9d639088c8cdb3c480600e67104955bdd4b535ac5c8  xmodel/websweep-20260827T1348Z.md
git basis 418e413593120d19e15e6546eb50c985f4b1f038 (matches)
```

Read in full or in the packet-directed portions: the packet, the complete
46-row inventory and all superseding overlays in `APPROACHES.md`, the
newest `AUDIT.md` correction blocks (through the 16:48Z block),
`PROGRESS.md` 2026-08-27 through 17:04Z, the 20:20Z/20:47Z/21:17Z/21:30Z
event and live-state blocks at the end of `notes.md`, `COORDINATION.md`,
the 18:08Z synthesis (dedup baseline), and the 13:48Z websweep.  For
novelty adjudication I additionally read
`xmodel/g2-pure-sigray-bypass-after-transpose-sol-ultra-20260827.md`
(a packet-listed provisional atom) and ran targeted repository searches
that explicitly excluded the pattern `xmodel/ideation-20260827T2137Z-*`.

Not read: any peer submission of this round (a directory listing exposed
peer *filenames* only — `sol` and `grok46` submissions exist; zero bytes of
their content were read); the individual 18:08Z ideator reports (one
history grep surfaced a single line of my own 18:08Z report, disclosed in
§10); anything under `jc2-lean` (never entered, listed, or searched).  No
AWS action was taken or planned from this session; all checks below are
desk-scale hand algebra plus hashes.

## 1. Disposition vector over avenues 1–46

Reference state is the canonical post-21:30Z overlay.  `unchanged` means
"keep the current canonical level and scope," including levels that are
already raised.  Reasons are given for every non-unchanged entry.

| # | Disposition | Reason (non-unchanged only) |
|--:|---|---|
| 1 | unchanged (stays raised) | — |
| 2 | **raise** | Post-transpose there is exactly one same-pair route left: pure-Sigray intrinsic two-chart forest plus the `C74-PLACE` → `EXIT-RPMC(C)` corridor interfaces.  Add the two-pencil Riemann–Hurwitz integer budget as a refute-only acceptance test on every compiled forest (§3.2, card 3), so coverage work becomes self-auditing while Opus5 review returns. |
| 3 | **raise** | The promoted upper gate law transports to the lower `(4,-1)` face with `r=2`, making the lower tower's unwindowed obstruction spaces 1–2 dimensional; a complete finite windowed ledger for LF40 rows 18–40 is desk-computable and predicts/validates a live AWS decision (card 2).  This is not another isolated upper tower row. |
| 4 | unchanged (narrow raise stands) | — |
| 5 | unchanged | — |
| 6 | unchanged | — |
| 7 | **raise (modest)** | The finite-end balance `sum e_S = td + b1 - 1` and Sol's (4.4)–(4.5) give the first exact campaign handle on the finite-end mass of `A(F)` along a pencil; A(F) place data become partially forest-computable rather than pure recon.  Instrumented raise only; no new solver. |
| 8 | unchanged | — |
| 9 | unchanged | — |
| 10 | unchanged | — |
| 11 | unchanged (refuted) | — |
| 12 | unchanged | — |
| 13 | unchanged | — |
| 14 | unchanged | — |
| 15 | unchanged | — |
| 16 | **raise** | The declared open end of the promoted gate law — "the exact identity `E=t^22` licenses an infinite tower, for which no capacity bound is known" — is exactly a regular-holonomic finiteness problem.  Card 1 names the concrete invariant (sector-module rank and an effective determinacy order `N0`) and a desk-scale first test.  This is the highest-leverage avenue-16 client yet named. |
| 17 | unchanged | — |
| 18 | unchanged (closed) | — |
| 19 | unchanged (stays raised) | — |
| 20 | unchanged | — |
| 21 | unchanged | — |
| 22 | unchanged | — |
| 23 | unchanged | — |
| 24 | unchanged | — |
| 25 | **raise** | Two independent doors opened at once: the R7R2 source audit explicitly leaves the branch-cycle CSP outside the regularity cut, and the intrinsic forest decorations (`e_S`, pole degrees `lambda_S`) determine full passports of `g` on `f`-fibres, making the coupled two-coordinate CSP concretely runnable on live `8_28` data for the first time.  The R-H identity is its abelian first layer. |
| 26 | unchanged (stays modestly raised) | — |
| 27 | unchanged | — |
| 28 | unchanged | — |
| 29 | unchanged | — |
| 30 | unchanged | — |
| 31 | unchanged | — |
| 32 | unchanged | — |
| 33 | unchanged (absorbed instrument only) | — |
| 34 | unchanged | — |
| 35 | unchanged | — |
| 36 | unchanged (disciplined raise stands) | — |
| 37 | unchanged | — |
| 38 | unchanged | — |
| 39 | unchanged | — |
| 40 | unchanged | — |
| 41 | unchanged (dead) | — |
| 42 | unchanged | — |
| 43 | unchanged | — |
| 44 | unchanged (closed as proof input) | — |
| 45 | unchanged | — |
| 46 | unchanged | — |

No avenue is lowered or reopened this round: the 21:30Z overlay already
performed the two required narrowings (anti-standard chain stop inside
avenue 2; naive filtered-bundling stop inside avenue 16), and I find no
evidence to reverse or extend them.

## 2. Bottleneck rerank

Highest-value proof bottlenecks, in order:

1. **Same-pair two-chart coverage: `C74-PLACE` then `EXIT-RPMC(C)`.**
   After the transpose discriminator, the conjugated chain is a second
   source theorem, not the other chart of the fixed pair, so every
   family-level ambition flows through the corridor route.  `EXIT-RPMC(C)`
   is also the only named path to a type-relative degree theorem
   `d <= C*alpha*beta` — the same object as bottleneck 3's intermediate
   form.
2. **Effective finiteness of the licensed `q`-tower per survivor stratum**
   (new; card 1).  Without it, even a decided affine endpoint closes only a
   finite sub-tower and no face; with it, any endpoint verdict upgrades to
   a stratum verdict with an explicit determinacy order `N0`.  I rank this
   above the endpoint decision itself because the endpoint lanes are
   already running and this is the only ingredient that converts their
   output into family statements.
3. **The absolute/cofinal topological-degree ceiling** — unchanged as the
   hardest; its cheapest live probes remain avenue 26 plus the passport
   data of §3.2.

Strongest falsification/counterexample attacks, in order:

1. **K00 full-`P6` rank-exact nonzero witness plus prolongation** (live on
   r6a).  A source-open, rank-exactly-two, full-`P6`-compatible nonzero
   point is the closest object in the campaign to an actual counterexample
   seed; the six-variable rank-two stratum is already proved nonempty over
   `Qbar`, so the remaining risk is concentrated exactly where the lane is
   looking.
2. **A proper (non-unit) LF40 row-40 fixture.**  Because FACEPIN is
   family-pinned (`K_rho = xi*(xi-rho)^7` forced), a proper terminal ideal
   is a genuine formal-family survivor on the lower face, conditional only
   on the primary GGV reduction — qualitatively stronger than any upper
   fixture survivor, which is fixture-bound.  §9 argues on obstruction-
   budget grounds that proper is at least as likely as the unit the lower
   bound hope prefers, so this lane doubles as a falsification attack.

## 3. New mechanism and new connections (history-checked)

### 3.1 Coherent compression of the licensed `q`-tower — label: NEW (mechanism)

Target obstruction: the promoted gate-law's declared open end — the exact
target `E=t^22` licenses an infinite tower `{q_n}` with "no capacity bound
or nonemptiness theorem" (AUDIT 19:37Z block; APPROACHES line "eventual
capacity is unbounded by the present theorem").

Mechanism.  The infinite tower is the coefficient list of the single
algebraic identity `E=t^22`.  On the F-side, the row-`m` gate
(`m=n+22`) is the class `[q_n dX]` in
`H^1_dR(U, nabla_m)`, `U=A^1-Z(H)`, `nabla_m=d+(m/4)dH/H` (promoted).
Three exact observations assemble these infinitely many classes into one
finite object:

1. **Sector intertwiner (new exact identity, hand-verified).**  With
   `T_m(Y) = 4HY' + (m-12)H'Y` (the promoted row operator),

   ```text
   T_m(H*Y) = 4H(HY)' + (m-12)H'HY = H*(4HY' + (m-8)H'Y) = H*T_{m+4}(Y).
   ```

   Hence multiplication by `H` maps `im(T_{m+4})` into `im(T_m)` and
   induces `coker(T_{m+4}) -> coker(T_m)`, `[f] |-> [H*f]`; over the
   localized ring `K[X, 1/H]` it is an isomorphism (and `k_{m+4}=k_m`
   because `4 | 4e_i`).  So within each residue class of `m mod 4` the
   row cokernels are one module, not an unbounded list.  This identity is
   the exact algebra behind the promoted warning that integer-shifting the
   twist changes only the finite window presentation.
2. **Algebraic assembly.**  The R7R2 source audit proves `P` and `W` are
   algebraic over `C(X,s)` and their cyclic modules regular holonomic.
   The `q_n` are the coefficients of explicit algebraic functionals of the
   `F`-series: for the even (untwisted) sector I verified by hand that
   with `F = H^2 + F1*t + F2*t^2 + ...`,

   ```text
   [t^2] (1/2)*sqrt(F) = F2/(4H) - F1^2/(16H^3) = q2   (exact match),
   ```

   while the odd sectors carry the exact `p`-powers
   (`q1 = (F1/(4H))*p^{-1}`, `q3 = H*p*(...)`), i.e. the sector characters
   cycle mod 4 exactly as the gate law predicts.  (Negative control,
   disclosed: `q3` is *not* a scalar multiple of `[t^3](1/2)sqrt(F)` —
   per-term ratios `1` vs `3/4` — so the naive single-function guess fails
   beyond the untwisted sector; the correct closed forms must be taken
   from the frozen R7R1 construction.  This is a documented failed
   attempt, not a gap in the mechanism: it shows only that each sector has
   its own algebraic assembly.)
3. **Formal vanishing is finitely determined.**  Assemble the sector-`j`
   gate classes into a section `gamma_j(s)` of the finite-rank coherent
   module `M_j` (the sector cokernel family over the `s`-line, relative to
   the prefix-survivor scheme).  If all its `s`-coefficients vanish, then
   `gamma_j` lies in `∩_N s^N M_j`, which is zero by Krull on each
   integral stratum (torsion-freeness on the discriminant-localized
   locus, then integrality).  So "all rows of sector j vanish" is finitely
   many conditions.  Effectivity: pair `gamma_j` with a Galois-symmetrized
   basis of algebraic sections of the dual module via the residue pairing;
   each pairing `c_i(s)` is *rational* in `s` with a resultant-computable
   degree bound; a rational function vanishing past its degree bound is
   zero.  Hence there is an explicit `N0` (per stratum, per sector) such
   that rows `q_0..q_{N0}` decide the entire tower on the generic point of
   the stratum.

Consequence if it holds: the gate tower has *finite effective content* on
every integral survivor stratum, closing the promoted law's open end and
upgrading any finite endpoint decision into a stratum decision.  Failure
modes are themselves informative (see card 1).

History search and label.  Searched `Gauss–Manin`, `Deligne extension`,
`formal vanishing`, `Krull`, `vanishing cycle`, `infinite tower`,
`finite determinacy`, `residue pairing`, `cup product`, `intertwin`,
`T_m(H`, excluding this round's pattern.  Closest hits: the promoted gate
law itself (AUDIT 19:37Z) which *states* the open end; R7R1 ("licenses the
whole tower"); the R7R2 regular-holonomicity audit (supplies ingredient 2
but draws no tower-finiteness conclusion); avenue 29's classical
Gauss–Manin remark (different object); one line of my own 18:08Z report
calling residue pairings the tower's "classical home" (framing only, no
compression or finiteness claim — disclosed in §10).  No prior statement
of the intertwiner, the coherent-section assembly, or an effective `N0`
exists in the ledgers.  **Label: NEW.**

### 3.2 Riemann–Hurwitz budget and passports as forest acceptance tests — label: KNOWN parent / NEW specific test

Parent facts (KNOWN — Sol's `g2-pure-sigray-bypass` report, packet-listed,
Opus5 review live): for a Keller pair, on the generic fibre `C` of `f`,
the second coordinate `g` is unramified at finite points, and

```text
sum_{finite ends S} e_S = td + b1(C) - 1        (4.3 there)
```

with the consequences (pole saturation = automorphism endpoint;
`sum e_S >= td`; the three-coordinate identity (4.5)), and the requirement
that a pure full-tree target must type the `e_S`.  I independently
rederived (4.3) by the same Riemann–Hurwitz computation (§10, check list)
— agreement, not independent evidence.

The specific test I propose is not in that report or elsewhere in the
ledgers (searched `validator`, `budget`, `acceptance`, `passport`,
`monodromy`, `CSP`, `transitiv`, `two-sided` against it and the repo):

1. **Refute-only partial-forest semantics.**  A partially decorated forest
   (e.g. one chart, or corridor places only) yields a *lower bound*
   `sum_{known finite ends} e_S`; if that partial sum already exceeds
   `td + b1 - 1` (both sides forest-computable: `td` from pole degrees,
   `b1` from genus/end data), the compilation is wrong — fail closed.  A
   passing partial forest certifies nothing.  This is the correct
   one-sided logic under the `C74-PLACE` chart-completeness caveat: the
   packet forbids silently asserting completeness, and this test never
   needs it.
2. **Two-pencil budget.**  The same identity for `f` on `g`-fibres gives a
   second independent integer equation; a compiled two-chart forest of the
   *pair* must satisfy both.  Two equations, zero extra theory.
3. **Passport layer (avenue 25 composition).**  Complete decorations
   determine the full branch data of `g|_C`: cycle type over infinity from
   the pole degrees `lambda_S` (summing to `td`), cycle types over each
   finite asymptotic value from the `e_S`, all other fibres unbranched.
   Riemann existence plus transitivity plus product-one is then a strictly
   stronger derived check than (4.3) — (4.3) is exactly its Euler
   characteristic shadow.  The packet lists Avenue-25 usefulness of the
   identity as "open"; this is the concrete composition.  It runs only
   after all-root completeness; before that, only layer 1 applies.

**Label: parent identity and `e_S`-decoration requirement KNOWN
(Sol `9147ada2...`, provisional, review debt); the refute-only semantics,
two-pencil budget, and CSP-as-derived-check composition NEW as a decisive
test.**  Both layers are cheap desk checks bolted onto card 3.

### 3.3 Second new connection: gate-law transfer to the lower face

The promoted upper de Rham gate law (avenue 16/3) transports to the lower
`(4,-1)` face (avenue 1's LF40): same operator family `T_m` with `K` in
place of `H`, weights `(8,12)` shared, but with `r = deg rad(K) = 2` and
`e=(1,7)`, so `k_m = 1  iff  4|m` (since `gcd(7,4)=1`), and every
unwindowed row obstruction space has dimension `1` or `2`.  Two exact
consistency anchors, both hand-verified: the promoted lower endpoint ODE
`4Kg'-3K'g=4K` is the `m`-offset instance whose twist has residues
`-3/4` and `-21/4 = -1/4 mod Z` (both nontrivial, `k=0`,
`dim coker = r-1 = 1`); and the upper endpoint `M(Y)=4HY'+6H'Y` on
`H=X^8-1` has `k_22=0`, `r=8`, predicting the exactly-7-dimensional
cokernel that D5G froze.  Details and the decision-relevant consequence
are in card 2 and §9.  Searched `lower` + `de Rham/nabla/gate law`,
`dK/K`, `4Kc'`: no prior transfer exists; the 17:04Z block promoted the
endpoint ODE and `gamma = 3 mod 4` necessity only.  **Label: NEW instance
of a KNOWN mechanism (the promoted law), with a new decisive test (the
windowed ledger against a live AWS lane).**

## 4. Composition tests among the named objects

| Pair | Verdict | Content |
|---|---|---|
| intrinsic two-chart forest × `C74-PLACE` | composes, guarded | Corridor roots become places on the *visited* chart only; the other chart needs the conjugated source theorem (filed Conjecture A, now abstract Theorem A that cannot supply the same fixed pair) or intrinsic enumeration.  Chart completeness must be an explicit certificate, never an assumption — this is the packet's own guard, kept load-bearing. |
| `C74-PLACE` × `EXIT-RPMC(C)` | composes, sequential | Places/proximity centers first; the exit-capacity inequality is per proper leading root and quantifies over exit subtrees rooted at first corridor exits.  Valid order; no shortcut exists that proves the capacity clause without the placement clause. |
| finite-end R-H × forest | composes one-sidedly | Refute-only on partial forests (§3.2.1); full validation only after all-root completeness.  Two-pencil version gives two independent budgets. |
| finite-end R-H × branch-cycle CSP (25) | composes after completeness | (4.3) is the Euler shadow of the passport CSP; decorations → passports → Riemann existence + transitivity as the stronger derived check.  Invalid before all-root completeness except in refute-only mode. |
| finite-end R-H × `EXIT-RPMC(C)` | proposal only — dictionary untyped | The `e_S` are geometric end multiplicities on generic fibres; exit discrepancies live in the Newton–Puiseux tree of the pair.  The bridge is via dicritical places, but no typed exit↔end map exists yet.  Until it does, (4.3) bounds total finite-end mass, not exit energy.  **Principled separation: do not substitute the global budget for the per-root capacity inequality.** |
| upper de Rham gates × affine endpoint | composes with licensing | The endpoint `m=22` is itself the first gate row (7-dim cokernel on the artificial `H`).  Rows `>=23` require their determinant rows imposed; the live branch-P fixture deliberately omits `D23/G22/q1`, so **a fixture PROPER does not imply a family survivor** (gates unimposed) and a fixture UNIT kills only the fixture.  Card 1 is the missing converter from endpoint verdicts to stratum verdicts. |
| upper gates × lower LF40 | mechanism transports, statements do not | §3.3: same operator family, different face, different `K`, different licensing.  The two faces couple only through the full corner family under the primary GGV reduction; there is no row-level map between them.  **Separation: never port a row identity across faces.** |
| K00 rank filtration × global landing/receiver | disjoint programs | K00 is max12/order-2; the GGV `8_28` obligations are a different source family.  Shared vocabulary ("receiver", "rank") is not a map; the promoted rule that `l0*b` cannot enter the six-variable BASE4 ring is the model for this separation.  K00's own receiver obligations (full `P6`, prolongation) are internal to its lane. |
| branch-cycle CSP × upper de Rham gates | proposal only — dictionary untyped | The `mu4` sector characters of the gate law are face/leading-form data; the CSP consumes monodromy of the actual fibration.  A leading-form-to-link-at-infinity dictionary would let the `k_m` schedule constrain local CSP data, but no such typed dictionary exists in the campaign.  Flag as a cheap future probe, not a composition. |
| forest/coverage × landing-receiver obligations | validator ≠ landing | The acceptance tests of §3.2 audit compiled forests; they do not discharge Sigray decoration, landing, delay, or degree obligations.  **Separation: passing every budget/passport check is necessary, never sufficient.** |

## 5. Strongest attacks to launch now

**Proof attack.**  `C74-PLACE`, stated and proved first for one exact
generic fibre and one chart of the live `8_28` corridor
`ell(P)=alpha*R^12`, `ell(Q)=beta*R^8`, with every residual root retained.
Decisive object: the all-root place/proximity-center map itself, delivered
with cumulative translations, deck stabilizers, and intrinsic branch IDs,
and audited by the two-pencil R-H integer budget in refute-only mode
(§3.2).  This is desk-scale theorem work, already identified by the
coordinator as the narrower target; my addition is the built-in
fail-closed acceptance test, which converts a subtle transport theorem
into a checkable compiler.

**Counterexample/falsification attack.**  Keep the r6a K00 full-`P6`
rank-two lane as the primary attack; decisive object: a nonzero point of
`V(J2) \ V(J1)` satisfying the full-`P6` source equations.  Reversible
desk-scale complement (no AWS, no relaunch): slice the six-variable
projective surface `Proj(R/J2)` (affine dim 3) with a generic rational
hyperplane pair, solve the resulting zero-dimensional system exactly over
a number field, and test `I2`-nonvanishing at each point — an explicit
rank-exactly-two witness (over `Qbar`) that becomes a concrete
prolongation center the moment the r6a lane needs one.  This respects the
promoted instruction not to spend on another witness *minor*: it spends on
a witness *point* of the already-promoted stratum.

## 6. Correctness-preserving software/AWS acceleration: monotone prefix-unit ladder

Principle (KNOWN in the campaign — the V16 review records that a subset
ideal's unit conclusion lifts to any larger ideal; an old parked msolve
note used the phrase "prefix ladder" in the unrelated d43 context; my
contribution is the application and certificate discipline — label:
KNOWN-PRINCIPLE / NEW-APPLICATION):
if `I_1 subset I_2` as literal generator sets, then `1 in I_1` implies
`1 in I_2`; a unit certificate on any *prefix subset* of the upper
branch-P endpoint system decides the full fixture UNIT.  Properness of a
prefix decides nothing — one-sided, fail-closed.

Design, honoring live jobs and the held quotient:

- **Inputs (exact, frozen):** the complete source manifest
  `c66b1396...`, the held 466-generator quotient system `8e25502c...` with
  its Singular input `afd9565d...` and its already-replayed 47-omission
  cofactor manifest.  No new mathematics enters; every stage is a literal
  generator-subset of the frozen system.
- **Decomposition:** a ladder of nested prefix subsets
  `S_1 subset S_2 subset ... subset S_k = full`, with `S_j` chosen by row
  weight (e.g. rows through `D14`, through `D18`, through `D22`+raw
  prefix, then the held 466-generator quotient as the top rung below
  full).  At most two rungs run concurrently, one core each, only on
  freed Box03 capacity *after* the current five exact lanes cap
  (approximately 01:13–03:04Z), and only if a fresh process audit shows
  zero swap and ≥150 GiB retained headroom — the same preconditions
  already preregistered for the held quotient, which this design contains
  as its top rung rather than replaces.
- **Certificate/replay format:** a rung reporting UNIT must emit the exact
  cofactor vector over its subset; replay = one exact-Q multiplication
  against the frozen subset manifest (minutes, local-legal).  The ladder
  result is guidance until that replay passes; then it is decisive for
  the full fixture by monotonicity.  A rung reporting proper emits
  nothing promotable and escalates to the next rung.
- **Resource estimate:** per rung ≤128 GiB VM, ≤2 h, matching the
  preregistered fallback envelope; expected win: if the fixture is unit,
  the smallest unit prefix is typically far cheaper than the full system
  (fewer generators, lower Gröbner blowup), potentially deciding UNIT
  hours earlier at strictly equal evidence tier.
- **Evidence firewall:** subset properness is never reported upward;
  no rung result touches the five live lanes; the authoritative literal
  raw `lp` lane retains authority for a PROPER outcome, which always
  requires the full 303-slot rational replay.
- **Stop rule:** kill the ladder the moment any full lane returns; kill a
  rung at its cap; at most one relaunch per rung on infrastructure
  failure; the whole ladder is abandoned if two rungs return proper
  (evidence the fixture is likely proper, where the ladder has no power).

## 7. Idea cards (three)

### Card 1 — Coherent compression of the licensed `q`-tower

- **Target gap:** the promoted gate law's open end: no capacity bound for
  the infinite tower licensed by exact `E=t^22`; endpoint verdicts do not
  close faces.
- **Dependencies:** promoted R7R1 (tower licensing), promoted gate law
  (row cokernels), R7R2 R2 source audit (algebraicity/regularity of
  `P,W`), frozen D3 polygon windows.  All promoted or source-confirmed;
  no unreviewed premise.
- **Mechanism:** §3.1 — sector intertwiner `T_m(HY)=H*T_{m+4}(Y)`
  (hand-verified), algebraic assembly of each sector's gate classes into a
  section `gamma_j(s)` of a finite-rank module, Krull finite determinacy,
  effective `N0` via rational residue pairings.
- **Cheapest discriminator:** on the branch-P stratum with `H=A^2`,
  `A=X^4-1` (the live fixture's `H`), build the untwisted-sector module
  and its residue pairing; verify the pairing's `s`-expansion reproduces
  the known `q2` gate values; extract the rational degree bound `N0`.
  Desk-scale exact linear algebra; two days.
- **PASS means:** the tower over that stratum has effective content
  `<= N0` per sector — announce the first tower-capacity theorem and wire
  it to whatever the upper endpoint lanes return.  **FAIL means:** either
  the pairing is not rational (Galois bookkeeping error — repair by
  symmetrization; second failure kills the effective route but not the
  Krull route) or a sector fails to assemble algebraically — which would
  be a genuine discovery that the tower has transcendental content,
  itself reportable and a reason to lower avenue-16 expectations.
- **Stop condition:** two desk-days for the discriminator; do not touch
  AWS; do not extend to a second stratum before hostile review of the
  first.
- **Rollback subtree:** nothing downstream consumes this until reviewed;
  self-contained.
- **Expected information gain:** high — it is the only named mechanism
  that converts the running endpoint decisions into family statements,
  and both of its failure modes are informative.

### Card 2 — Lower-face windowed obstruction ledger for LF40

- **Target gap:** the live LF40 lanes (r6b, Box02) will return a terminal
  basis with no independent mathematical prediction; and rows 18–40's
  obstruction structure is unknown despite the face being family-pinned.
- **Dependencies:** promoted lower `(4,-1)` grading block (17:04Z:
  recurrence, `141+301=442` census, `F17` empty, `G17=Q[xi]_{<=7}`, 28
  G-slots at weights 18–24, endpoint ODE), promoted upper gate law as the
  mechanism template, LF40 R1 freeze for the literal windows.  The
  lower R7R1-analogue (which rows carry which twists) is *derived by this
  card*, not assumed.
- **Mechanism:** §3.3.  Derive the lower quarter-root characteristic from
  `Etil=-tau^17*J` with `J=1` (a complete finite system — rows beyond 40
  vanish identically, so no infinite-tower issue on this face); then for
  each row `m=18..40` compute the exact windowed codimension
  `c_m = dim(row space) - dim T_m(window_m) - ...` from the frozen
  polygon.  Unwindowed budget for orientation only:
  `sum_{m=18}^{40} (r-1+k_m) = 23 + 6 = 29` scalar conditions
  (`k_m=1` at `m=20,24,28,32,36,40`), against 36 G-side unknowns at and
  above the target weight — near-critical, so the windowed census decides.
- **Cheapest discriminator:** compute the ledger for rows 18–24 only
  (where the 28 G-slots live) and compare against the first nontrivial
  rows of whichever LF40 lane finishes its early rows first; the ledger
  must reproduce each row's elimination rank exactly.
- **PASS means:** an exact linear survivor-dimension bound for the whole
  LF40 fixture, predicting proper-vs-unit direction before the Gröbner
  run terminates, and afterwards serving as an independent row-by-row
  validator of the terminal basis (the same role the 740-generator replay
  played for the compiler, one level up).  **FAIL means:** the first row
  where the rank-one law breaks names a genuinely coupled lower-face
  operator — exactly the depth-three coupling avenue 3 is looking for;
  bank it as that avenue's first concrete object.
- **Stop condition:** three desk-days or the first LF40 terminal event,
  whichever first; the ledger never delays or modifies the AWS lanes.
- **Rollback subtree:** none until reviewed; if the derived licensing
  schedule is wrong, only this card's outputs are quarantined.
- **Expected information gain:** high per cost — a live AWS decision
  gains an independent predictor, and either outcome of the derivation
  feeds a raised avenue.

### Card 3 — `C74-PLACE` with built-in R-H/passport acceptance

- **Target gap:** the single remaining same-pair coverage route has no
  self-audit; transport/fidelity errors in forest compilation are exactly
  the class of bug the campaign keeps finding at compilers.
- **Dependencies:** VGG Proposition 7.3/Corollary 7.4 (provisional
  corridors), the transpose discriminator (promoted), Sol's (4.3)–(4.5)
  (provisional, Opus5 review live — used only as a *validator*, so a
  review failure would remove the validator, not corrupt the theorem),
  Riemann existence (classical) for the passport layer.
- **Mechanism:** prove the place/proximity-center map for one exact
  generic fibre and one chart with all residual roots retained (Sol's
  recommended probe), and require every compiled forest to pass:
  (i) partial-budget refute-only check `sum_known e_S <= td + b1 - 1`;
  (ii) after completeness certificates, both pencil budgets with
  equality; (iii) the passport CSP (transitive, product-one, correct
  cycle types) as the strictly stronger layer.
- **Cheapest discriminator:** run (i) on the existing live-corner data
  (one deck orbit, four roots, known pole tags) — an afternoon; it either
  passes silently or catches a decoration error immediately.
- **PASS means:** the corridor route acquires a fail-closed compiler
  discipline before its first theorem is even promoted; subsequent
  `EXIT-RPMC(C)` work inherits audited inputs.  **FAIL (budget violation)
  means:** a concrete decoration/transport bug with the first violated
  integer named — cheaper than any hostile review cycle.
- **Stop condition:** the validator layer is bounded (two desk-days);
  the `C74-PLACE` theorem itself follows the coordinator's existing
  probe plan and Opus5 review outcome.
- **Rollback subtree:** if Opus5 review refutes (4.3)/(4.5), drop layers
  (i)–(ii), keep the theorem work; if it confirms, promote the validator
  with it.
- **Expected information gain:** medium-high; mostly risk reduction on
  the campaign's top proof bottleneck, plus the first concrete avenue-25
  composition.

## 8. Lane dispositions

| Lane | Disposition | Note |
|---|---|---|
| K00 (r6a full-`P6` rank-two; held six-variable R4) | **continue** | No relaunch churn; keep R4 held; add the reversible desk witness-point slice of §5 only as side work. |
| GGV upper (five Box03 exact lanes; held 466-gen quotient) | **continue** | Amend only the *fallback*: on cap-out, run the §6 prefix-unit ladder (which contains the held quotient as its top rung) under the already-preregistered resource preconditions. |
| GGV lower (LF40 R1 r6b; Box02 std/dp cross-check) | **continue** | Add card 2's desk ledger as an independent predictor/validator; never touch the running lanes. |
| Coverage/landing | **continue as redesigned** | The 21:30Z redesign (pure-Sigray + `C74-PLACE`/`EXIT-RPMC`) is correct; add card 3's acceptance layers; await Opus5 review of the corridor atoms before promotion. |
| TD6/AS | **continue** | Bounded: AS109 three alpha/beta content strata intersection stands; TD6's H19R1 successor when scheduled; no new compute demand from me. |
| Formal algebraization | **continue (narrow)** | The fixed-`H` saturated germ is the exact client; `jc2-lean` remains untouched and unconsumed. |
| Sparse counterexample search | **continue (disciplined)** | Theorem-compressed survivor schemes and nonzero/source-open K00 charts only; ambient sampling stays stopped. |
| External-claim audit | **continue** | Matysiak SSRN remains ACTIONABLE-UNAUDITED; retry lawful acquisition; next broad sweep due 2026-08-28 13:48Z. |

## 9. One insight a differently trained researcher is likely to miss

**The lower face is nearly scalar, and the LF40 decision therefore lives
entirely in window combinatorics — with a two-line root-evaluation
mechanism that already reproduces the promoted `gamma = 3 mod 4` law.**

A researcher pattern-matching from the upper face (where `H=X^8-1` gives
`r=8` and 7–8-dimensional row obstruction spaces) will expect the lower
tower to be governed by comparably large intrinsic cokernels.  It is not:
`K = xi*(xi-rho)^7` has `r=2`, so every unwindowed lower row obstruction
space has dimension `1` or `2` (`= 1 + [4|m]`, since `gcd(7,4)=1` makes
`4|7m` equivalent to `4|m`).  The intrinsic obstruction content of rows
18–40 is at most `23 + 6 = 29` scalars.  Whether LF40 is unit or proper is
therefore decided almost entirely by the *windowed* census — which slots
the frozen polygon actually provides per weight — not by the connection
theory.  Exact check of the mechanism, at the endpoint row:

For `4Kg' - 3K'g = 4K`: at the simple root `xi=0` write `K = xi*v`,
`v(0) != 0`; evaluating gives `-3v(0)g(0) = 0`, so `g(0)=0`.  At the
multiplicity-7 root, dividing by `(xi-rho)^6` and evaluating gives
`-21u(rho)g(rho) = 0`, so `g(rho)=0` — **one evaluation condition per
distinct root, independent of multiplicity**.  Setting `g=(xi-rho)h` and
evaluating again gives `(4-3*7)h(rho) = 4`, i.e. `h(rho) = -4/17`:
no further local obstruction.  The leading-degree equation allows
`deg g = 1` (coefficient `a(4-3*deg K) = 4`, solvable) or
`deg g = 3*deg(K)/4`; but a degree-one `g` with the two forced zeros
`g(0)=g(rho)=0` is identically zero — the degree-one "solution" is a
leading-order mirage.  Hence only `deg g = 3(gamma+1)/4` survives, which
is integral iff `gamma = 3 mod 4` — recovering the promoted necessity
from first principles in two steps, and with it the general rule: on the
lower face, each distinct root costs one evaluation, the degree law costs
one congruence, and everything else is window bookkeeping.  Two practical
consequences: (a) card 2's ledger is small and decisive, not an
approximation; (b) on obstruction-budget grounds (≈29 intrinsic scalars
against 36 G-side unknowns at and above the target weight, before
windowing), a PROPER LF40 outcome is at least as plausible as the unit
the conditional 108→125 hope prefers — the campaign should be emotionally
and operationally ready for a formal lower-face survivor, which §2 ranks
as falsification attack 2.

## 10. Honest ledger

Conjectural leaps (labeled, none promoted):

1. Card 1's algebraic assembly of each sector's gate classes into one
   coherent section is a construction plan, verified only at the
   `q2`/untwisted-sector sample and the odd-sector character cycling; the
   exact closed forms per sector must be derived from the frozen R7R1
   report.  The window↔`H^1` reconciliation is assumed from the promoted
   review per row, unwindowed-first in my argument.
2. Card 2's licensed-row schedule for the lower face (which rows carry
   which twists) is asserted by analogy and is the card's first task; the
   `23+6=29` budget is the unwindowed count conditional on that schedule.
   The `36` G-side unknown count aggregates `G17` (8) plus weights 18–24
   (28) and ignores how the row-17 target block consumes freedom;
   windowing can move conditions in both directions.
3. §3.2's exit↔end dictionary (R-H vs `EXIT-RPMC`) is explicitly untyped;
   I claim only the refute-only and passport layers.
4. The Sol (4.3)–(4.5) atoms are provisional (Opus5 review was live at
   seal); every use here is validator-side and degrades gracefully if
   review narrows them.

Failed/negative attempts (disclosed so no one pays twice):

- The single-function guess `q(s) = (1/2)sqrt(F)` for the whole tower
  fails at `q3` (per-term ratios `1` vs `3/4`); only the untwisted sector
  can match it.  Documented in §3.1.
- I looked for a lower-face local obstruction at the multiplicity-7 root
  beyond the evaluation condition and found none (`h(rho) = -4/17`
  solvable); the `gamma` congruence is purely global/degree — a nuance
  worth recording because it redirects any future lower-face mode search
  away from local analysis.

Assumptions: the frozen basis bytes are as hashed; the promoted ledger
blocks I cite are correct at their stated scopes; no peer content
influenced this report; heavy algebra stays AWS-only and none was run.

Checks actually performed (all desk-scale, this session): SHA-256
verification of the packet and all seven basis files and the git basis
commit; independent rederivation of the finite-end R-H identity
(`chi`/ramification bookkeeping, agreeing with Sol's (4.2)–(4.3));
hand verification of the intertwiner `T_m(HY)=H*T_{m+4}(Y)`; hand
verification of `[t^2](1/2)sqrt(F) = q2` and the `q1/q3` sector
characters, plus the `q3` mismatch negative control; the lower-endpoint
local analysis (`g(0)=g(rho)=0`, `h(rho)=-4/17`, degree dichotomy,
recovery of `gamma = 3 mod 4`); the `k_m` shift-invariance check
(`4|e_i m iff 4|e_i(m+4)`); the upper anchor `k_22=0, r=8 => dim 7`
against the promoted 7-dimensional `M(Y)` cokernel; the counts
`23` rows, `6` multiples of 4 in `[18,40]`, `8+28=36` G-side slots;
targeted novelty greps (all excluding `xmodel/ideation-20260827T2137Z-*`)
for Riemann–Hurwitz/finite ends, Gauss–Manin/Krull/formal vanishing/
finite determinacy, residue/period pairings, intertwiners, prefix/unit
monotonicity, lower-face de Rham transfer, and validator/passport
composition, with closest hits named inline in §3 and §6.

Blindness disclosure: I read the sealed packet and the seven hashed basis
files, the packet-listed Sol bypass report, and (for §3 novelty
adjudication) grep-level fragments of named historical reports.  Peer
submissions for this round exist (`sol`, `grok46`); I saw their filenames
in a directory listing and read zero bytes of their content.  One history
grep returned a single line of my own 18:08Z submission ("residue
pairings … classical home"), disclosed and credited against my own
novelty claim in §3.1; no other 18:08Z ideator report content was read.
The `jc2-lean` tree was never entered, listed, searched, or built.  My
persistent session memory contains summaries of my own prior reviewed
work on this campaign (the same results recorded in the promoted
ledgers); it contains no peer content from this round.  No AWS resource
was launched, inspected, or disturbed.

Report path: `xmodel/ideation-20260827T2137Z-fable5.md`.
SHA-256: computed immediately after sealing and stated in the submission
message (a file cannot contain its own hash).
Model identity: Fable 5 (Anthropic), exact model ID `claude-fable-5`.
