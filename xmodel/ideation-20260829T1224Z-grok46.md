# Blind whole-portfolio ideation — Grok 4.6 — round `20260829T1224Z`

Lane: Grok 4.6 (xAI), equal-standing independent researcher.
Produced blind against the sealed packet only. No other
`20260829T1224Z` lane prompt, log, run record, or report was opened.
No web sweep, AWS action, canonical edit, commit, push, or heavy
computation. The separately owned `jc2-lean` tree was not entered,
enumerated, listed, searched, read, status-checked, built, or modified.
Bare `G2` is not used.

## 0. Custody

Model identity: Grok 4.6, xAI.
Clean basis (verified `git rev-parse HEAD`):
`ccb6cd52eeab95f169b40f0a48668c3acd7a607e`.

Packet hashes (recomputed, byte-exact):

```text
f79671cf460240ce6e200ccca30a34e65b90e305f15f5c636f7262f73db86baa  full file (8700 bytes)
f33cd9786a1bfd914c9253addd951532af3cd359a600da212209984aa6562783  body (8546 bytes; all bytes before `## Seal`)
```

Canonical roots (recomputed, all match the packet):

```text
0ab0a8c643607526c12b33ffc762f91f680d4ada820415441e6d71dbab9c6b17  COORDINATION.md
701feb58eaee99a53e024dc57dfd024f24c20fe985124224ed52a29cdc0da493  APPROACHES.md
a058aad4b2faa1a235a9d31903844e12594428045e3e7c76d05f23a7423de9fd  AUDIT.md
1a0d5fc7445ed662a50fe99fe53702b7f3d41aaf14c432aea5405f536a5f4708  PROGRESS.md
a2f49415ddf72edb62fc4a86c3628a708b9e488e6b2f2871fc1b5117b53bfc80  notes.md
29270ff6192fcee2eecb4ba68578010b2b3f0c5519feb71dc67baa7f68bb784b  ladder/REDUCTION.md
b2b85ef6e4c2204ae3318089204c08ae077bf091f0165da0531f9d55e35b4d26  ops/FLEET.md
c63bd1673b2b180173799f5bee07f7fc0e51047945a41010a28a0aeeeeb92253  FALLACY.md
```

Read: the complete packet; `APPROACHES.md` overlays plus the 46-row
master table; current promotion/correction blocks at the top of
`AUDIT.md`; the complete `2026-08-29` entry of `PROGRESS.md`; newest
`LIVE STATE` (`2026-08-29 12:20Z`) and no later event; `G2-PSC`/`G2-BD`
map and Critical 4–7 in `ladder/REDUCTION.md`; `ops/FLEET.md`;
`FALLACY.md`; the prior-round synthesis named by the packet
(`xmodel/ideation-20260829T0820Z-synthesis.md`); and only those
producer/review reports whose hashes the packet or the live canonicals
already name. Pre-cutoff material was used as history checksum, never
as a substitute for the sealed packet.

Firewall (applied below). A cv flag is not a physical place and is not
a conjugate Puiseux series. `REPRESENTATIVE` is not
`FULL_ACTUAL_EXIT`. The latter aliases `FULL_ACTUAL_FIRST_SEPARATION`
and is a lower floor, never attainment. Do not identify one labelled
route-kill with all U2, all one-P0, or all td12. Do not identify a
formal jet envelope with a `PairRef`. Do not infer occurrence from
`ALIVE`. Merge-free is not Statement 8.5. Target `nu_G` is not incoming
`nu_U`. If no safe replacement exists, the report returns typed `OPEN`.

This lane does not recycle its `20260829T0820Z` cards `FLAG-ARITY`,
`SIBLING-ARITY`, `B-RECURRENCE`, or `NESTED-U2-EDGE`. Those clients
are promoted, subsumed, or rerouted by the new evidence.

## 1. Disposition vector (avenues 1–46)

Legend: `U` unchanged, `R` raise, `L` lower, `O` reopen. Reasons only
for changes. Homonymy does not move a row. Lifecycle tags in reasons
are `promoted`, `provisional`, `formal`, `book-relative`, or
`conjectural`.

| # | d | reason if changed |
|--:|:-:|---|
| 1 | U | |
| 2 | R | Execution, not endgame-P of a cofinal ceiling. Promoted: labelled `NESTED-U2-P0-U2-RAY/v1` is dead as an actual td12 landing at `REPRESENTATIVE` scope; two-pole `FULL_ACTUAL_FIRST_SEPARATION` licenses the LL-1 `13->7` floor replay; td12 Keller recurrence and own-order absorption are exact, with `r=3i/2` fixed on actual type `(2,3)`. Provisional: cascade rank gives no formal B24/S16 kill. The live M2 edge is now remaining U2/one-P0 labels plus route-separated S17-then-B25 source packets, not a shared recurrence engine and not the dead label. |
| 3 | U | Homonymy firewall: the reduced `Z`-operator is a source-jet ODE, not a strip residue functional. Do not raise Żołądek A.7/Ore. |
| 4 | U | Still no `PairRef`. The provisional cascade is a formal envelope (cokernel two for B, three for sibling) plus binomial absorption through `s<=i`. Formal jet ≠ germ ≠ polynomial. Arm narrowly only if Card 1 emits a completed parent polynomial. |
| 5 | U | |
| 6 | U | Three `(1,2,3)` poles are infinity places of the pencil, not `A(F)` components and not AM embeddings of `A^1`. Identifying them is `SCOPE-CONFLICT`. |
| 7 | U | Pole-purity still does not construct a component-labelled `A(F)` packet. ACS cover remains infrastructure. |
| 8 | U | |
| 9 | U | |
| 10 | U | |
| 11 | U | |
| 12 | U | |
| 13 | U | |
| 14 | U | |
| 15 | U | |
| 16 | U | HENS-CT backend timeout stands; the `Z`-operator is not a holonomic-index client. |
| 17 | U | |
| 18 | U | Type `(2,3)` and `r=3i/2` do not reopen a graded CE hunt. Shaska closure stands. |
| 19 | U | Witt/char-p stays reserve. Formal td12 jets are not polynomial lifts. |
| 20 | U | |
| 21 | U | |
| 22 | U | |
| 23 | U | |
| 24 | U | |
| 25 | U | Stay down. Local flag arity is not target-component inertia. The two-pole carrier theorem does not license a monodromy census. |
| 26 | U | Stay down as a global bound. Killing one U2 label, or noting td12 is composite, does not license a primitive-group census. |
| 27 | U | |
| 28 | U | ACS-CEIL stays stopped; signed Euler is not a positive `td-1` decomposition. |
| 29 | U | |
| 30 | U | |
| 31 | U | Same `A(F)`-packet block as avenue 7. |
| 32 | U | No named bounded family until a `PairRef` exists. |
| 33 | U | Untwisted residues remain `COSTUME`. Three-pole local residues are not a new class. |
| 34 | U | |
| 35 | U | |
| 36 | U | K00 remains the protected bounded characteristic-zero seed. Formal td12 cells and LL-1 `ALIVE` rows do not displace it. |
| 37 | U | |
| 38 | U | Direct valuation/image arithmetic on the reduced `Z`-operator still dominates tropicalization. |
| 39 | U | No 10-line class. |
| 40 | U | |
| 41 | U | |
| 42 | U | |
| 43 | U | |
| 44 | U | Closed as proof input. |
| 45 | U | Inverse PDE remains tautological; U2 ODE and the `Z`-operator are not Kovacic. |
| 46 | U | Formalization is not a route; `jc2-lean` stays outside the campaign boundary. |

Unanimous negative space this round: no numerical `kbar`/`nu` cap; no
engine migration of Q+E5; no AWS math launch; no revival of the killed
td8 affine family or the killed labelled one-P0 U2 td12 landing; no
identification of family-kill with a `td` ceiling; no Galois conjugacy
of the two S17 evaluations; no PairRef from a formal envelope.

## 2. Reranked bottlenecks

Proof-side, by expected information per desk-day, then by endgame
necessity. Endgame-P of a checkable JC2 proof remains modest:
family-kills are necessary book work, not a cofinal ceiling
(`REDUCTION.md` Critical 7).

1. **Remaining U2 / one-P0 labels after the promoted labelled-ray kill.**
   The corrected `NESTED-U2-P0-U2-RAY/v1` is dead at actual td12 landing,
   `REPRESENTATIVE` selected-exit/MFE scope (`5+5+1=11>td-2=10`). Direct
   adjacent U2-to-U2 edges are finite at promoted actual-edge scope.
   Leaks that the kill explicitly does not touch: other one-P0 labels,
   intervening P0 compositions, nonintegral/rational first boundaries,
   recursively variable bases, and dirty `V_{2,a}` M-transport. Cards 2
   and 3 attack the two cheapest typed pieces of that residue.
2. **Route-separated S17 parent source packet, then B25.** Promoted:
   sibling exact charge `4+4`, rigid T1 `B/A=(9+/-3i)/8`, shared parent
   jet family without automatic Galois conjugacy; B charge exactly eight;
   symbolic Keller convolution and own-order absorption; `r=3i/2` on
   actual type `(2,3)`. Provisional: cascade rank, injective reduced
   operator, binomial absorption through `s<=i`, no formal depth-window
   kill. Missing: `PairRef`, polynomial completion, first beyond-floor
   obstruction (Card 1). Sibling depth 16 is strictly cheaper than B
   depth 24 and must not share a packet.
3. **LL-1 remaining seven rows as floors only.** Promoted two-pole
   carrier plus exact `13->7` replay. Survivors, book-relative,
   `ALIVE` = conservative superset:
   `(2/3,3,2)`, `(3/4,4,2)`, `(2/5,5,3)`, `(2/7,7,4)`, `(2/9,9,4)`,
   `(3/10,10,4)`, `(3/8,8,4)`. Provisional software: LL1-R4, 166/166,
   different-model review debt. No occurrence, attainment, or other-td
   result.
4. **Q+E5 fixed-index equal-regime pattern**, promoted at pattern-algebra
   scope. Route-to-state map, arrival/i-sync, and realization remain
   `OPEN`. Keep `NUCAP=500`. Do not feed an LL-1 row into Q+E5 without a
   declared route-to-state map (`FALLACY.md` target/arrival index).
5. **Total family-aware landing** (Critical 4). Codomain = finite residue
   plus labelled family records. U1 is a schema; U2 and case-III remain
   named leaks, now narrowed.
6. **Independently sourced type menu + cofinal `td` ceiling.** Unchanged
   endgame wall. No new theorem supplies it.
7. **`RPMC(C)`**, still type-relative.
8. **`G2-PSC`**, owed only by a hybrid GGV-to-Sigray architecture. Pure
   Sigray bypasses it. Zero active spending.
9. **`G2-BD`**, dormant until residue A.

Counterexample / falsification side:

1. **S17 parent polynomial completion** — strongest formal-to-germ seed
   *if it exists*. Shared parent for both sibling evaluations; still not
   a Keller pair, not a collision, not JC2.
2. **B25** if sibling completion is empty. Same caveats.
3. **Remaining U2 if Cards 2–3 return free dirty transport or a surviving
   other-label inner frame.** Infinite reduced leak of Critical 5; not a
   map.
4. **K00 source-open rank-exact full-`P6` point** — strongest bounded
   characteristic-zero seed. Unchanged, protected.
5. **LL-1 slack-0 `ALIVE_FRAGILE` rows** as book-relative attainment
   targets, never as occurrence. Secondary to K00.
6. **D43 exact source point** — finite algebraization ladder only.

What this news does *not* rerank: landing totality, `RPMC(C)`,
type/cofinal control, `G2-PSC`, `G2-BD`, ACS construction debt, K00,
D43 custody. The portfolio moves inside Avenue 2's coefficient/source
and remaining-U2 coverage tier, not onto a new global bridge.

## 3. New mechanism, new connection, composition pass

### 3.1 New mechanism — `DIRTY-V2A-EXIT-TYPE` (NEW)

The campaign now has a promoted negative: merge-free does not certify
edgewise `G notin V_{2,a}`, so Statement 8.5 cannot be composed to
`M_H | b_P`. Canonical off-axis source records a merge-free dirty step
`(w,M)=(3/2,2) -> M=3`. That exception blocked a `td>=15` proof; it has
not been given its own exit-set type.

Mechanism: treat a regular dirty `V_{2,a}` vertex as a typed vertex
class and ask whether its M-change is

- a first-separation flag (charge it, once, with a declared
  `charge_basis`), or
- an interior M-change with no new flag (typed leak), or
- `OPEN` (no safe replacement).

This is not Statement 8.5, not the repaired source-mass floor
`mass >= beta` / `max(beta,2alpha)`, not MP0, and not the root-meet
case `(0,y) in V_{2,a}\V_{1,a}` (Proposition 9.3 case I). Close
synonyms checked: FALLACY merge-free/M-descent; Fable U2 source-mass
`FAIL` plus erratum `09ac8eef...`; BOOK-OFFAXIS dirty step; selected-exit
MFE; two-pole full-actual attachment. The *positive* exit-typing of the
dirty step is **NEW**. Card 3.

### 3.2 New connection — inner nested-U2 merge consumes the promoted two-pole carrier (NEW)

The labelled one-P0 kill is logically independent of the two-pole
`FULL_ACTUAL_FIRST_SEPARATION` theorem and did not consume it. The
labelled inner picture is two pole branches of multiplicity three
arriving at an inner U2; the outer sibling is a third chain. Locally the
inner vertex is a two-pole path union. The outer vertex is three-pole.

Dictionary, not identification:

- LL-1 / two-pole theorem: complete distinct actual carrier on a
  two-pole path union, lower floor only, promoted; finite-pole-union
  extension is observed in the hostile proof and **not promoted**
  (`ladder/SHEET6-MULTIPOLE.md` carrier-type rider).
- Inner nested U2: two incoming one-pole chains. If and only if the
  promoted hypotheses hold (not merely “two things meet”), the inner
  merge may reprice from `REPRESENTATIVE` AF2 to full-actual floors.
  Nonintegral inner `delta` jumps from a representative price to
  `ceil(2*delta)`. Flags already charged on the chains are not charged
  again.
- Outer three-chain vertex: `OPEN` at full-actual until an `s=3`
  theorem exists. Do not analogize.

This composes the two significant-news promotions of the checkpoint on
the *remaining* U2/one-P0 labels, which is the only honest consumer
left after the labelled ray died. Failure of the hypothesis check is
`NO HIT`, not a U2 kill. Card 2.

Second, weaker connection (recorded, not launched): the dead labelled
U2 equality profile and the live td12 U1 first-trunk family both sit on
three `(a,b,nu)=(1,2,3)` poles of type `(2,3)`. Merge-pattern (U1
equal-join versus nested U2 versus mixed) is the remaining invariant of
that skeleton. Matching pole numbers do not identify the routes.
`SCOPE-CONFLICT` to copy a U2 budget lemma onto U1 or conversely.

### 3.3 Theorem-interface composition

| Theorem (lifecycle) | Object | Consumes / discharged by | Result |
|---|---|---|---|
| Labelled one-P0 U2 td12 kill (`promoted`, `REPRESENTATIVE`) | actual landing of `NESTED-U2-P0-U2-RAY/v1` | source-mass equality, cap-free P0 closure `(1,3,5)`, selected-exit/MFE | **HIT** as stop on that label. **NO HIT** with other U2/one-P0, all td12, landing, `G2-PSC`, `G2-BD`, JC2. |
| Two-pole `FULL_ACTUAL_FIRST_SEPARATION` (`promoted`) | complete actual carrier on a two-pole path union | Def 3.3, St 7.3/3.13, C7.1 | **HIT** with LL-1 `13->7` floors. **HIT** with remaining inner U2 only after Card 2 hypothesis check. **NO HIT** with generic MP8, three-pole outer U2, or attainment. |
| td12 recurrence + absorption (`promoted`); cascade rank (`provisional`) | formal `Z`-operator, binomial response through `s<=i` | type `(2,3)` ⇒ `r=3i/2`; injectivity on `C[t]` | **NO HIT** with B24/S16 kill. **HIT** with Card 1 as the first beyond-floor test. **NO HIT** with `PairRef`. |
| Direct U2-to-U2 finiteness (`promoted`, actual adjacent edge) | `nu=1` edge with fixed pre-inner data | absorbed U2 form, ODE at unique resonance | **NO HIT** with intervening P0 or stage-R rows. |
| Q+E5 fixed-index menu (`promoted`, pattern scope) | certified case-III equal-regime state | unique `(dp,dq)` and no-partition R2.2 | **NO HIT** with LL-1 rows until a route-to-state map exists. |
| Pole-purity price 0 (`promoted`) | actual pole vertices | Prop 5.1/5.5/7.2 | **NO HIT** with ACS/`A(F)`. |

Honest global reading: the new theorems remain Avenue-2 internal. A
speculative uniform overpay on every three-`(1,2,3)` skeleton would
bypass landing; it is **not typed**. Record as a sketch, do not launch.

## 4. Strongest attacks

**Proof.** Run Card 2 (inner-U2 two-pole hypothesis check and, if
licensed, full-actual reprice of remaining one-P0 labels) in parallel
with Card 3 (dirty `V_{2,a}` exit type). In parallel, run Card 1 on the
S17 parent at the first beyond-floor order; only if that test is
inconclusive or passes, open a route-separated S17 `PairRef` packet,
then B25. Do not spend the proof slot on the dead labelled ray, on
further formal-cascade descendants, on Q+E5 engine edits, on `G2-PSC`
hybrid transport, or on ACS-CEIL. Do not apply Statement 8.5 to dirty
steps.

**Counterexample / falsification.** The honest surviving source seed is
the S17 parent jet family of the reviewed td12 U1 sibling

```text
(nu,dp,dq,kbar,X,w,M)=(17,68,52,13,17,3/4,4),
t=eta^17,  B/A=(9+/-3i)/8,  charge 4+4,  depth 16,
shared parent P_k, Galois conjugacy unforced,
```

subject to `r=3i/2 in Z`, `i>=16` even, and polynomial completion
beyond `s<=i`. If Card 1 or a later `PairRef` emits a polynomial parent
compatible with `J=1`, that is the strongest formal germ seed now on
the board. It is still not a Keller pair. Secondary falsifier: remaining
U2 infinite after Cards 2–3. K00 is the strongest *bounded* char-0 seed
and is not displaced. Explicitly reject: treating the killed labelled
U2 family as a near-miss germ; treating the provisional cascade cokernel
as a coefficient vector; treating LL-1 `ALIVE` as occurrence; ambient
SAT search.

## 5. Mathematical-software acceleration / decisive experiment

**Object.** First beyond-floor obstruction for the S17 parent: the
reduced operator

```text
L_s(Z) = D_F p Z' - i (D_F - s) p' Z
```

on the polynomial ring `C[t]` (unbounded degree; no numerical cap), at
`s=i+1`, with `i=16`, `r=24`, `p` the sibling parent polynomial whose
T1 roots are `A` and `B/A=(9+/-3i)/8`, against the truncated binomial
particular solution that the provisional cascade says absorbs all formal
rows through `s<=i`.

**Placement.** Desk. Python stdlib `Fraction` and sparse univariate
dicts only, in the same style as Fable's `/tmp/bchild_absorb_check.py`
controls. No CAS, no cap token, no AWS. Expected cost: one researcher
plus a few minutes to an hour of exact arithmetic, far below fleet
thresholds in `ops/FLEET.md`.

**Controls.** Positive: replay absorption `A_s[dP]+B_s[(r c_g/i)p^{r-i}dP]=0`
at one S17 tuple (must remain identically zero; no root/genericity
input). Negative: a split `Z` that the vanishing ladder forbids must
not be accepted as image. Plant the formal-window binomial as a
non-`PairRef` control: it may satisfy `s<=i` and must not be labelled
`PAIRREF`.

**If the linear form at `s=i+1` is inconclusive** (image contains the
binomial response, or the translation from cascade domains to `C[t]` is
untyped): freeze a reviewed AWS packet for a route-separated S17
`PairRef` only then. Do not occupy 512-vCPU quota speculatively. Current
headroom is irrelevant until a scoped packet exists.

This is the Card 1 discriminator. It is not a 16-level recurrence
engine and not a B25 job.

## 6. Campaign-systems card — `UPGRADE`

Axis: reproducibility and claim-DAG typing of coefficient artifacts
(the live mathematical risk of this checkpoint; FALLACY.md already
covers inference prose).

The provisional cascade is exact on its formal envelope and is already
being misreadable as a `PairRef`. The packet and the AUDIT both forbid
that identification; the machinery does not yet fail closed on it.

Smallest useful bounded test:

- a four-value header enum required of every coefficient-lane report:
  `FORMAL_JET_ENVELOPE | PAIRREF | POLYNOMIAL_COMPLETION | SOURCE_PACKET`;
- a grep fixture that fails if a descendant cites
  `30aa29260c2bc3b206f757a90fe42ec6dcd98c733a7bf073bd0a86c9f48cdf67`
  (or body `805e5512...`) as `PAIRREF`;
- plant the cascade report plus the integral-index erratum
  `b50d8fdf...` as negative controls (must match
  `FORMAL_JET_ENVELOPE`);
- plant the AUDIT sentence that B25 and S17 need separate `PairRef`
  packets as a positive control for the missing-object state.

Do not churn FALLACY.md, `charge_basis` validation, the declared-input
recorder, or `.ignore`. Do not read `jc2-lean`. Next systems checkpoint
remains `2026-08-31T09:30Z`. This upgrade is one header-enum plus a
fixture, not a rewrite.

Evidence against `NO_CHANGE`: the cascade/PairRef confusion is named in
the sealed packet as the explicit next custody failure mode, and LL1-R4
already shows that promoted math plus provisional software can drift in
opposite directions without a typed object kind.

## 7. Idea cards (three)

### Card 1 — `S17-BEYOND-FLOOR`
[`KNOWN` target; `NEW` first test]

**Fingerprint.**
`td12-S17-parent|beyond-floor-linear-form-on-coker|reduced-Z-at-s=i+1|i=16-r=24-binomial-particular-plus-coker3`

**Target.** First obstruction past the provisional formal envelope for
the sibling parent jet family, not a shared B/sibling recurrence and
not a B25 packet.

**Dependencies.** Promoted: sibling charge `4+4`, T1 ratio, shared
`P_k` family without automatic Galois conjugacy, Keller convolution,
absorption identity, `r=3i/2` on actual type `(2,3)`. Provisional:
cascade injectivity and `dim C[t]/T_s(C[t])=q`, binomial absorption
through `s<=i`, cokernel three for sibling. Excluded: numerical t-cap,
treating the envelope as `PairRef`, B25 coefficients, Galois
identification of the two evaluations.

**Cheapest discriminator.** The desk experiment of §5: on `C[t]`, no
degree cap, test whether the binomial particular solution at `s=i+1`
(`i=16`, `r=24`) lies in `im(L_s)`. If the cascade domain/target
translation is untyped, return `OPEN`.

**Outcomes.**

- Not in the image: sibling polynomial completion obstructed at the
  first beyond-floor order. B25 becomes the remaining U1 source seed.
  Do not call this a td12 book kill.
- In the image, with an explicit polynomial `Z`: first genuine piece of
  an S17 `PairRef`; continue one more order or induct; do not claim
  depth 16 from one order; arm Avenue 4 only on that polynomial.
- In the image for a formal series but not for any polynomial: that is
  the honest `FORMAL_JET_ENVELOPE` versus `POLYNOMIAL_COMPLETION`
  split; record it and stop the formal descendants.
- Untyped domain: `OPEN`. Do not fill by window cap `N` or by the
  number `75r-3s`.

**Stop.** One exact `s=i+1` image verdict, an `OPEN` on typing, or the
first polynomial `Z`.

**Information.** Binary on the cheaper of the two U1 source routes,
without a 16-level engine and without AWS.

### Card 2 — `INNER-U2-TWO-POLE-FULL-ACTUAL`
[`NEW` consumer of a `promoted` theorem]

**Fingerprint.**
`remaining-U2-one-P0-inner-merge|two-pole-FULL_ACTUAL_FIRST_SEPARATION-consumer|inner-U2-two-pole-path-union|hypothesis-check-then-reprice-nonintegral-inner-delta`

**Target.** Other one-P0 / nested-U2 labels than the dead
`NESTED-U2-P0-U2-RAY/v1`, at their inner U2 vertex only.

**Dependencies.** Promoted: two-pole full-actual attachment
(`1b3be27d...` / integration `8cb13514...`); labelled-ray kill at
`REPRESENTATIVE` (do not reuse as a global U2 theorem); direct-edge
finiteness at actual adjacent scope; source-mass floor
`mass>=max(beta,2alpha)` for `mu>=2`. Not used: unpromoted finite-pole
union extension; generic MP8 as full-actual; Statement 8.5; floors as
attainment.

**Cheapest discriminator.** Write the inner vertex as a two-pole path
union and check every source hypothesis of the promoted theorem
(fibre, up non-chain children, unique outer intersection, disjoint
complete flag sets). If any hypothesis fails, `NO HIT`. If they hold,
reprice each inner direction by the safe full-actual floor (`delta` if
positive integral, `ceil(2*delta)` otherwise), count each flag once,
and compare to the labelled inner representative prices. Desk identity.
No CAS.

**Outcomes.**

- Hypothesis fail: `NO HIT`. Do not analogize from LL-1.
- Licensed and some remaining label has nonintegral inner `delta`
  whose full-actual floor already exceeds its budget share: that label
  dies as an actual landing, still not all U2.
- Licensed and all remaining inner deltas are integral and match the
  representative floors: no new kill; proceed to Card 3 / intervening
  P0.
- Double-counting of chain AF2 flags with merge flags: stop and repair
  the charge set; do not add prices.

**Stop.** A written hypothesis verdict plus at most one reprice ledger
for remaining one-P0 inner frames.

**Information.** Decides whether the promoted two-pole carrier, which
the labelled kill did not use, kills any surviving U2 label without
new source work.

No new exit claim is made in this card, so no `charge_basis` line is
declared. Any later exit claim must add exactly one such line.

### Card 3 — `DIRTY-V2A-EXIT-TYPE`
[`NEW` mechanism]

**Fingerprint.**
`remaining-U2-M-transport|dirty-V2a-first-separation-typing|regular-dirty-V_{2,a}-step-(3/2,2)->M=3|edgewise-cv-flag-existence-and-charge-or-OPEN`

**Target.** The canonical merge-free dirty step that changes `M` at
`(w,M)=(3/2,2)` without an MP0 merge, which remaining U2 labels can
use to avoid the unique eligible P0 state `(w,M,lambda_min)=(1,3,5)`.

**Dependencies.** Promoted: vertex-class discipline (pole vs
`V_{1,a}` vs `V_{2,a}` vs root); selected-exit MFE as
`REPRESENTATIVE`; source-mass floor without Statement 8.5; FALLACY
merge-free/M-descent. The root-meet theorem for `(0,y) in
V_{2,a}\V_{1,a}` is a different object. Not used: withdrawn `td>=15`
ASM; cap-by-analogy prices.

**Cheapest discriminator.** On that named step, determine the vertex
class from the source. If it is not regular dirty `V_{2,a}`, stop
(`NO HIT` or retarget). If it is, decide whether an up-child
first-separation flag exists. Desk source comparison; no CAS.

**Outcomes.**

- A typed first-separation flag exists: charge it once with a declared
  `charge_basis`. Remaining U2 labels that rely on this step as free
  M-descent pay the flag and may die at td12. Still not all U2.
- No new flag, interior M-change: typed leak of Critical 5 persists;
  absolute-index / `prod(nu_seg)` / gluing remain the consumers.
- Vertex class or flag existence untyped: `OPEN`. Never fill by
  Statement 8.5, by MP0, or by the source-mass floor.

**Stop.** One vertex-class-plus-flag verdict, or `OPEN`.

**Information.** Decides whether the exception that blocked `td>=15`
is itself an exit charge or a genuine uncovered M-transport.

If an exit is later claimed for this step, add exactly one line of the
form
`charge_basis={"delta":"...","branch":"q=1-exact|q>=2|multi-flag","flag_count":1,"citation":"path:line"}`.
This report does not declare one.

## 8. Major-lane recommendations

Explicit lifecycle of inputs: `promoted` / `provisional` / `formal` /
`book-relative` / `conjectural`.

| Lane | Rec | note |
|---|---|---|
| labelled `NESTED-U2-P0-U2-RAY/v1` | **stop** | Promoted dead at actual td12 landing, `REPRESENTATIVE`. Recycle as a regression fixture. Do not glue, index-bound, or realize it. |
| other U2 / one-P0 | **continue** | Promoted: direct adjacent edge finite; source-mass floor `td>=12` for the corrected family; labelled ray dead. Run Cards 2–3. Intervening P0 and recursively variable bases stay open. Do not claim U2 closed. |
| td12 S17 source | **continue**, first | Promoted recurrence/absorption/T1/charge. Missing `PairRef`. Card 1 is the first beyond-floor test. Stop treating the two evaluations as Galois conjugates. |
| td12 B25 source | **continue**, after S17 | Promoted `lambda_B=8`, `N=i`, depth-24 gate. Separate packet. Do not return to pole pricing. |
| `TD12-FORMAL-CASCADE-RANK/v1` | **continue** as background review only; **stop** descendants | Provisional, same-model, integral-index erratum applied. No formal kill. Do not fan out under COORDINATION speculative-child limits. |
| LL-1 remaining seven | **continue** as floors | Promoted math `13->7`. Book-relative `ALIVE` ≠ occurrence. Provisional LL1-R4 software: different-model review, no further descendant. |
| Q+E5 | **continue** pattern consumer; **stop** engine migration | Promoted fixed-index menu and no-partition R2.2. Keep `NUCAP=500`, `cell_check`, `solve_arr`. Standalone checker only after software review. |
| remaining M2 families | **continue** as residue census | Include every `td=8` configuration that is not the killed affine equal-join, and every td12 configuration that is not the dead labelled U2 ray. |
| `G2-PSC` | **stop** spending | Pure-Sigray de facto. Reinstate only if a hybrid argument is actually built. |
| `G2-BD` | **stop** / dormant | Until residue A. |
| landing totality | **continue** as infrastructure | Not the critical path. LL-2 remains family-record keyed. Critical 4 stands. |
| type / cofinal degree | **continue** as the endgame wall | **Stop** advertising family-kills as a ceiling. |
| asymptotic complement | **continue** as infrastructure | **Stop** ACS-CEIL and TDIC-SET. |
| characteristic-p / Witt | **continue** at reserve | No new polynomial-limit evidence. |
| analytic / topological (25–28/30) | **stop** the coupled bet | Stay down. |
| counterexample search | **continue** K00 protected; **stop** ambient | Arm Avenue 4 only on a Card-1 nonempty polynomial `Z`. Formal cell ≠ map. |
| AWS / heavy CAS | **stop** until a reviewed packet | Desk only. Fleet idle by design. |
| `jc2-lean` | **do not enter** | Separately owned. |

## 9. History labels (checksum)

| Item | Label |
|---|---|
| `DIRTY-V2A-EXIT-TYPE` | `NEW` mechanism; dirty step `KNOWN` as a St 8.5 blocker |
| `INNER-U2-TWO-POLE-FULL-ACTUAL` | `NEW` connection/consumer; two-pole theorem `promoted`; finite-pole-union extension `KNOWN` unpromoted |
| `S17-BEYOND-FLOOR` | `KNOWN` target (packet-named); first `s=i+1` image test `NEW` |
| three-`(1,2,3)` skeleton U1/U2 | recorded connection; identification without merge-pattern `SCOPE-CONFLICT` |
| labelled one-P0 U2 td12 landing | `promoted` dead, `REPRESENTATIVE` only |
| LL-1 `13->7` | `promoted` floors; software `provisional`; `ALIVE` book-relative |
| cascade rank / binomial through `s<=i` | `provisional` formal envelope, not `PairRef` |
| `r=3i/2` on actual type `(2,3)` | `promoted` |
| sibling Galois conjugacy | `KNOWN` unforced |
| Q+E5 engine migration | `KNOWN` stop |
| numerical `kbar` cap | `KNOWN` false |
| flag = conjugate series | `KNOWN` false |
| merge-free ⇒ St 8.5 | `KNOWN` false |
| `FLAG-ARITY` / `SIBLING-ARITY` / `B-RECURRENCE` / `NESTED-U2-EDGE` from `0820Z` | `DUPLICATE` / subsumed; not reused |
| uniform-overpay global proof | `NEW` sketch; untyped; do not launch |
| artifact-kind enum | `NEW` systems |

No proof or counterexample to JC2 follows from anything in this report.

*Report body ends. The seal below covers everything above this line.*

## Seal

- Body length: `29568` bytes (all bytes before this heading).
- Body SHA-256: `3d3dba78eb59d2a83521d00f0fcab51d46900586088c87d095ce0ae21f31f194`.
- Packet verified this session:
  `f79671cf460240ce6e200ccca30a34e65b90e305f15f5c636f7262f73db86baa` (full),
  `f33cd9786a1bfd914c9253addd951532af3cd359a600da212209984aa6562783` (body 8546).
- Basis: `ccb6cd52eeab95f169b40f0a48668c3acd7a607e`.
