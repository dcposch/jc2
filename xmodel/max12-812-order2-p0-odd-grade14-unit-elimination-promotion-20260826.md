# `(8,12)` order two: `p=0` odd-sheet grade-14 unit elimination

Date: 2026-08-26

Status: **PROMOTED EXACT NECESSARY THEOREM AT THE NAMED NORMALIZED ODD
COLLISION SHEET.**

## Promoted theorem

After the reviewed sharp third-tail gate has forced `M=0`, consider the
quadruple-root odd collision component

```text
p=0,       rs=0,       b=cs,       b*k0 != 0,
```

in the source-typed `(8,12)`, exact-order-two, terminal `[6,2]` client.  The
reviewed grade-eleven/twelve support is

```text
e0=a0=ell1=0,
12*e1^2-5*k0*b^4=0,
```

together with its unsuppressed final predecessor row `F`.  Normalize the
quadratic sheet by

```text
w=e1/b^2,       k0=(12/5)*w^2,       b*w != 0.
```

In the complete normalized terminal source, before using `F`, any grade-13
equation, a radical, or a saturation, the fifth tail row at absolute
`sigma`-grade fourteen is exactly

```text
E_(5,14)=-(21/320)*b^5*w^2
        =-(21/320)*b*e1^2
        =-(7/256)*k0*b^5.                         (1)
```

Row five has no target.  Formula (1) is therefore a raw source equation and
a unit on `D(b*w)=D(b*k0)`.  Consequently the normalized odd component

```text
V(rs) intersect D(cs*k0)
```

does not admit a terminal `[6,2]` prolongation through grade fourteen.  No
later terminal row or finite Taylor equation can restore this component.

The exact grade-13 triangular prolongation theorem remains correct and useful
as a source control, but it is not needed for the elimination: the decisive
grade-14 unit exists before its four pivots are substituted.

## Source and independent replay custody

The complete terminal DAG identifies `(ell,grade)=(5,14)` with equal source
and full root `270200`, expression SHA-256
`da69527730e513efa4027e670f8322b83941368877c6c7e841bcf5220cfe1030`.
Equality of the roots is the exact target-absence sentinel.

The exact-Q grade-14 producer, frozen at source-manifest SHA-256
`b018dc4e7a7cefa38cd019ed786921c93f293a03136eb647dfaf5100d23b0710`,
emits (1) directly.  Its validator is
`PASS_G14_EXACT_NAVIGATION`; the reduced JSON, result JSON, and validation
SHAs are respectively
`6e6b3f3a5b6a3f4fee4e48cff3bcfffc5e08ef788d2397e1c20b76e561fb5d09`,
`7a4bc318db0d75fa4f5950adea142f7403eef96784da02be7224658a50a3d147`,
and `8c36acf2bfb9314b544305e405b3616f0860645cf2c61a8399f974fa45ba4099`.

An independently implemented sparse collector, frozen at SHA-256
`9a4f6014f8df125fcfaabad9f659991648e910f53f004a98d1065dd9b969b092`,
reconstructs the normalized primitive series without importing the terminal
DAG or grade-14 reducer.  It processes all 89 canonical fifth-tail monomials:
42 contribute at grade fourteen, 249 raw coefficient monomials collect, and
all correction-dependent terms cancel to (1).  The exact-Q identity is
proved before the modular controls; the surviving coefficient is nonzero in
both controls (`700 mod 32003`, `20680 mod 65521`).  Independent Box03 and
r6d lanes both report `PASS_P0_ODD_ROW5_G14_REPLAY`.

The hostile review is **CONFIRMED** at SHA-256
`5cbfe1d8f7bebb8672d3c3e998791f3c743b9174b699b2bc85de321e95996a97`.
It independently checked absolute grading, tail sign and index, source/full
orientation, target absence, the focused Faber coefficients, normalization,
raw-ideal use, all correction/load retention, and the Gate-A firewall.

## Gate retirement and firewall

Gate A (sheet-to-global coefficient/R0 algebraization) and the two typed
finite Taylor pullbacks are **retired only for this eliminated sheet**: the
raw terminal unit makes them unnecessary here.  They are not proved, and no
global rational square root or deck compatibility is inferred.

This promotion does **not** treat the separate `p=0` cusp on `D(rs)`, the
`p!=0` positive-order-load or fractional/ramified fan, any omitted infinity
support, any other terminal profile, the entire square branch, all order two,
all `(8,12)`, maximum twelve, or JC2.  The immediate disjoint successor is
the correction-aware `p=0` cusp chart `D(rs)`.
