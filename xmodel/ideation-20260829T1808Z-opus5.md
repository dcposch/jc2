# Blind whole-portfolio ideation — Opus 5 — round `20260829T1808Z`

Researcher: Opus 5 (equal-standing).  
Frozen basis: `e930fa90b8ee9d86220a2cff72fba94e04b20baa` (verified `git rev-parse HEAD`).  
Mode: blind. No peer submission, prompt, log, or run record from this round was
read. No web. No canonical file edited, no commit, no push.

## 0. Custody, verification, and one execution disclosure

Recomputed before use, all matching the state packet:

```text
f58ee9b50d640860fa36f34f12f39bcbb0f8fb852e2ace90be8e62323e09ccfb
  xmodel/ideation-20260829T1808Z-state.md
d48e33c594731b4e6f0761c21c28460a15e15a7a9ea80801692b2fdc1a62529b
  state-packet body through BODY-END, 9040 bytes (recomputed, matches its seal)
6f5006f46e158bce2211a7d408e206ceef0e54d8b0781d1fa92905d57f2cabcf  APPROACHES.md
bff17fd8acb0f71fbf1ddc0b5f9d1ca30886222e03ea1f9309a92ac5cff707a0  AUDIT.md
f947045b4d747efe89a8dadc37ab46e13985ce6099e3bbd40dbd93e3b2489083  COORDINATION.md
460675f20fccbf774067f067d57b09f93a4d8e811fc0f7b7e899957a9a0ea0f0  PROGRESS.md
2adfb3b6c25552654e3bc6fbac3b54e6d3ace56a861125cde201317455450c57  ladder/REDUCTION.md
4c2fd1de827361f06bcbfdd465e0cdb6f03c63203c9943f89af60b8aaaa7637d
  xmodel/u1-star-interface-no-go-coordinator-integration-sol56-93d-20260829.md
2fcf8b9d9d0f7a28c9425bf73d30ba893015c69074afef57d2425309826ace50
  xmodel/td12-u1-first-nonneutral-sextet-coordinator-integration-sol56-93d-20260829.md
6c2ca262a32488ea964d648a21f9a5e008f47f270529d9d24af3021b73ddf6f3
  xmodel/k00-valuation6-hand-obstruction-coordinator-integration-sol56-93d-20260829.md
1e3e50e7b06f6d1f5d3e24bacc93af38b3ba3c17477ecab992ad376bb5e19e7d
  xmodel/k00-r2-y0-grade8-coordinator-integration-sol56-93d-20260829.md
2ac7653cf0c7a39970d54286b85974b39a2e6deb116839720cb1ae91f8ad6d2b
  cases/max12_812_order2_u2_62_k00_common_lambda19_kuranishi_v20_20260827/
  compile_contracted_source_v20r2.py
```

Also read for history and novelty testing (not charged as inputs to a claim):
`ladder/TDBOUND.md`, `ladder/TRANSPORT.md` §§2–3 and the T-link table,
`ladder/SHEET6-CLASSICAL.md` §§1–2, `AUDIT.md` lines 9230–9610,
`xmodel/sol-lateral3.md` §4, `xmodel/ideation-20260829T1517Z-synthesis.md`.

**Execution disclosure (fail-closed, reported unprompted).** My first search of
this session was a bare `grep -rl -i "jelonek" .` from the repository root. It
ran for roughly 120 seconds before I killed it, and in that time it certainly
traversed and read files under the fenced `jc2-lean` path. No `jc2-lean` object
was modified, built, or used in any mathematical statement below, and no
`jc2-lean` content entered my reasoning. Every later search used `rg` (which
consumes the repository's existing `.ignore`) or explicit paths. This is the
**second** disclosed traversal of the fence in two days — the K00 `r=2`
grade-eight review disclosed the same failure mode with the same tool. That
recurrence, not my individual error, is the object of the systems card in §7.

## 1. Compact disposition over all 46 numbered avenues

Baseline is the `APPROACHES.md` master table plus the 2026-08-29 17:58Z
overlay. `unchanged` means I found no reason to move it this round.

| # | Avenue | Disposition | Reason (this round) |
|--:|---|---|---|
| 1 | GGV corner farm + degree farm | **lower (relative)** | New: selecting the GGV-minimal pair *forfeits* topological-degree minimality (§3.6). The pure-Sigray branch can select td-minimal; the GGV branch cannot. That is a structural cost the farm has never been charged. |
| 2 | Sheet ladder / Eggers–Wall / Sigray | unchanged (principal) | Retarget inside the avenue: the first slot should be the scale datum `mu_P/B` (§3.1–3.3), not another local reduced row. |
| 3 | Vertex-gap / strip ODEs / residue functional | unchanged | Still mechanism supply with no source-provenanced client. |
| 4 | Formal-germ certification + algebraization | unchanged | Nonempty formal data are not arcs, germs, or maps. |
| 5 | Jung–van der Kulk descent | unchanged | Amalgam rigidity still does not bite on non-automorphisms. |
| 6 | Abhyankar–Moh one-place | **raise (small)** | The "correct one-place objects are `A(F)` components, place data unpinned" blocker is partly answered: §3.4–3.5 make the local place data of each component exactly the boundary contact orders the books already compute. |
| 7 | Nonproperness / Jelonek `A(F)` | **raise** | Status change, not a re-score. Grok's blocker was "computing `A(F)` is the compactification problem again". §3.4/§3.5 show you never need to construct `A(F)`: its per-component *defect* and *inertia cycle type* are already determined by the non-pole boundary places. |
| 8 | Formal-inverse combinatorics | unchanged | — |
| 9 | Lee–Li Conjecture E | unchanged | — |
| 10 | HC4 ⇒ JC2 | unchanged | `NO LEVERAGE` stands; no later observable named. |
| 11 | Mathieu / GMC / Zhao ladder | unchanged (refuted) | — |
| 12 | Face isolation / p-adic multinomials | unchanged | — |
| 13 | Dixmier DC(2) | unchanged | — |
| 14 | End(A_1) / Zheglov audit | unchanged | — |
| 15 | Spectral surfaces / commuting PDOs | unchanged | — |
| 16 | D-module / holonomic index | unchanged | Still no named invariant. |
| 17 | BCW / Druzkowski stabilization | unchanged | — |
| 18 | Graded / equivariant / GIT | unchanged (closed) | — |
| 19 | Char-p + Witt lifting | unchanged | Degree-twelve frontier unchanged by this round. |
| 20 | Reduction mod p / p-curvature | unchanged | — |
| 21 | p-adic injectivity / Hensel | unchanged | — |
| 22 | Diophantine integral points | unchanged | — |
| 23 | Analytic global inverse | unchanged | — |
| 24 | Real JC / Pinchuk | unchanged | — |
| 25 | Fiber monodromy / dessins / passports | **raise** | The 2026 test used *admissible* passports (Riemann existence, "too generous", 169/169 pass). §3.5 makes the passport **determined**, not merely admissible: the inertia cycle type at a generic point of each `A(F)` component is exactly the multiset of `g`-contact orders at the non-pole boundary places. That removes the stated blocker. |
| 26 | Primitive-monodromy td bound | **reopen + raise** | Two of the three things it lacked now exist: a *free* td-minimality hypothesis in the pure-Sigray branch (§3.6) and seam-1, the inertia-support theorem (§3.5). Only "block quotient descends to a polynomial Keller map" remains. |
| 27 | Links at infinity / splice | unchanged | — |
| 28 | Log surfaces / BMY | unchanged | BMY still `NEEDS-DATA`; §3.4 is the affine Euler ledger, not a log-Chern input. |
| 29 | LND / commuting frames | unchanged | — |
| 30 | Affine-surface classification / ML | unchanged | — |
| 31 | Integrality / ZMT / Rees valuations | unchanged | — |
| 32 | Off-diagonal collision ideal | unchanged | Three-generator presentation stands; still injectivity. |
| 33 | Symplectic exactness / action residues | unchanged | `COSTUME` stands. |
| 34 | 2D tangent-sweep / pole removal | unchanged | — |
| 35 | Descent of dim ≥ 3 counterexamples | unchanged | — |
| 36 | Guided CE search (K00 lane) | unchanged status, **sub-scope reopened** | §5: the valuation-two *leading cone* is a 4-dimensional linear space; the promoted grade-eight exclusion covers a 2-dimensional subplane of it. The remaining valuation-two work is larger than the phrase "other components" suggests, and its shape is now exactly known. |
| 37 | Finite-field census | unchanged | — |
| 38 | Tropical geometry | unchanged | — |
| 39 | Cohomological cluster | unchanged | — |
| 40 | Free-associative lift | unchanged | — |
| 41 | Naive scaling deformation | unchanged (dead) | — |
| 42 | Markus–Yamabe | unchanged | — |
| 43 | Ritt decomposition / composite coordinates | **raise (small)** | Reframed: it is not a standalone route but the *descent step* of Avenue 26's block dichotomy (§3.6, §4.3). |
| 44 | Moskowicz "no prime td" | unchanged (closed as proof input) | — |
| 45 | Differential Galois / Liouvillian | unchanged | — |
| 46 | Lean / AI formal certification | unchanged | Out of scope this round by the tool boundary. |

Net: 1 lowered, 5 raised, 1 sub-scope reopened, 39 unchanged. No avenue is
newly closed and none is newly refuted by this report.

## 2. Reranked gap lists

Global selection and the conditional td12 laboratory are kept apart, as
required. Ranking is by *how much of the remaining distance to JC2 the gap
owns*, not by tractability.

### 2.1 Global proof gaps

| rank | gap | status | what changed this round |
|--:|---|---|---|
| G1 | **No topological-degree ceiling and no global selector.** | open | Newly surfaced: the repository *already contains* a named sufficient crux for exactly this, `DIR(C) ⟺ PC(C) ⟺ RPMC(C) ⇒ KJN(C) ⇒ td ≤ C·αβ` (AUDIT 9356–9505), with `TDBOUND` its summed form. §3.1–3.3 put it in Sigray-tree coordinates and show `U1*(R)` is its sharpest formal countermodel. G1 is therefore not "unnamed" — it is one unproved per-point capacity inequality plus a type-menu theorem. |
| G2 | **Bounded / cofinal type menu.** | open | Unchanged, but promoted in rank: even a full proof of `KJN(1)` leaves `td ≤ αβ` *type-relative*. Without a type bound this is not a ceiling. This is now the co-equal partner of G1, not a footnote. |
| G3 | **`CRITICAL 4` — no universal full-configuration landing/coverage theorem.** | open | Unchanged. The minimally plausible codomain remains finite exceptional residue plus normalized family records. |
| G4 | **`G2-PSC`.** | open (hybrid branch only) | Rank drops one place *conditionally*: §3.6 makes the pure-Sigray branch strictly more attractive, and that branch does not owe `G2-PSC`. It owes G5 instead. |
| G5 | **`HIGH 3` — Sigray source trust, §§7–9 unaudited.** | open | Rank **rises**. The pure-Sigray branch is the branch that can use td-minimality, and it rests entirely on arbitrary-pair `T4` plus the unrefereed §§7–9 layer. Choosing that branch converts a source-audit debt into a load-bearing dependency. |
| G6 | **`CRITICAL 5/6` — off-axis and post-jump completeness.** | open | Unchanged. |
| G7 | **`CRITICAL 2` quantifier (GGV minimality is existential and is not td-minimality).** | open, partly addressable | §3.6: in the pure-Sigray branch this is replaced by a genuinely free selection. Not closed — traded. |

### 2.2 Counterexample / falsification gaps

| rank | gap | status | note |
|--:|---|---|---|
| C1 | **K00 V20R2 valuation two, off the old plane.** | open | §5: the leading cone is the 4-plane `{A=B=0}`; the excluded old plane is a 2-plane inside it. Cheapest live counterexample-side object in the portfolio. |
| C2 | **K00 V20R2 valuations three, four, five.** | open | Each needs its own leading-vector cover; §5.3 gives the exact grade-19 budget that decides which is cheapest. |
| C3 | **Realizability of any `U1*(R)`, `R ≥ 3`.** | new entry, open | Dual-use: an actual source realization refutes `RPMC(1)`/`TDBOUND` and destroys the only named ceiling route; a proof that none is realizable is the first genuine extra-`s` theorem. |
| C4 | **Char-p Witt lift at the degree-twelve frontier.** | open | Unchanged. |
| C5 | **GGV farm / `(72,108)` transcription bridge.** | open | Unchanged. |
| C6 | **Formal-germ D-series algebraization.** | open | Unchanged; unarmed. |

### 2.3 Conditional td12 laboratory (kept separate)

| rank | item | status |
|--:|---|---|
| T1 | `TD12-U1-FAMILY-AWARE-DETOUR-EXCLUSION/v2` over A7/C5/D13/E11 + W1–W3 + pure-epsilon descendants | open, conditional |
| T2 | Actual td12/U1 occurrence (antecedent of everything in T1) | open |
| T3 | B25/S17 source values, cap, `PairRef`, terminal deviation rows | open |

**Ordering claim.** T2 dominates T1. The entire laboratory sits at one entry
whose scale-free pole mass is `E_MR = 2` (§3.3), and `RPMC(1)` — the campaign's
own named ceiling crux — asserts `E_MR ≤ 1`. Spending the primary slot on T1
while T2 is in direct tension with a repo-internal conjecture that would make
T1 vacuous is a misallocation. This does not refute T1; it reprices it.

## 3. New mechanism, new connection, and the supporting exact statements

Everything in this section is derived by hand from promoted repository facts
plus elementary intersection theory and covering space theory. Nothing here
asserts an occurrence, an attainment, a map, or a counterexample.

### 3.1 `OP-1` (KNOWN-EQUIVALENT, new form): the Bézout localization `Δ_P = Λ_P`

Setting, exactly as in the `KJN`/`RPMC` lane (AUDIT 9356 ff.): `f,g` Keller of
degrees `d=αB`, `e=βB`, `B = gcd(d,e)`, common leading power `F_d = ξH^α`,
`G_e = ηH^β`, `deg H = B`, `div_{L∞}(H) = Σ_P μ_P·P`, `Σ_P μ_P = B`. For
generic `λ,ν` put `n_P = i_P(F-λZ^d, G-νZ^e)` and `Δ_P = αβBμ_P - n_P`.

Claim: **`Δ_P` is the total pole order of `g` on the branches of the generic
`f`-fibre lying over `P`.**

Proof. Let `p` run over the branches (places at infinity) of the closure of
`{f=λ}` at `P`, and let `m_p = ord_p Z`. Choosing a linear form `L` with
`L(P)≠0`, the local equation of `{G=νZ^e}` is `(G-νZ^e)/L^e = (Z/L)^e (g-ν)`,
so `i_p = e·m_p + ord_p(g-ν)`. For generic `ν`, `ord_p(g-ν)` is `-λ_p` at a
pole place and `0` otherwise. Since `F_d = ξH^α`, the local intersection of
the fibre with `L∞` at `P` is `Σ_p m_p = α μ_P`. Hence
`n_P = eαμ_P - Σ_p λ_p = αβBμ_P - Σ_p λ_p`, i.e. `Δ_P = Σ_{p|P} λ_p`. ∎

Novelty: **KNOWN-EQUIVALENT.** Under the standard chart normalization
`m_γ = ord_γ y` this is the promoted `EXACT POLAR BRIDGE`
`Δ_P = Σ_γ max{0, ord_γ F_X - (d-2)m_γ}` (there
`ord_γ F_X - (d-2)m_γ = m_γ + ord_γ f_x = λ_γ`, using `ord_γ f_x = λ_γ - m_γ`
from `dy = f_x·dg` on the fibre). The content added is the *form*: `Δ_P` is a
pole order, i.e. exactly the Sigray decoration `Λ_P`. Summing recovers the
promoted `Σ_F Λ_F = td` and `E_MR = td/(αβ)`.

### 3.2 `OP-2` (NEW, per-point form): `RPMC` in Sigray-tree coordinates

Because `Λ_P = αβ·a_P b_P/ν_P` (`TDBOUND` §1 reformulation) and
`E_P = Δ_P/(αβ)`, `OP-1` gives `E_P = a_P b_P/ν_P`. Therefore

```text
RPMC(C)  <=>  a_P·b_P/ν_P  <=  C·μ_P/B      for every proper root P,
KJN(C)   <=>  Σ_P a_P·b_P/ν_P  <=  C        (the promoted summed TDBOUND form).
```

Equivalently, in raw analytic terms: *the total pole order of `g` on the
branches of the generic `f`-fibre over `P` is at most `C·αβ·μ_P/B`.* The
trivial bound from `λ_p ≤ e·m_p` is `Λ_P ≤ αβBμ_P`; the conjecture asks for a
factor `B²` improvement. That is precisely the "no finite `B`-independent `C`"
wall recorded in AUDIT 9458.

Desk-scale controls I ran (exact rationals, `/tmp/opus_check.py`, < 1 s):

```text
residue-A  (α,β)=(2,3), two poles (a,b,ν)=(1,1,2):  Λ_P=3, td=6, E_MR=1
           B=84, μ_P=42:  αβμ_P/B = 3 = Λ_P        -> RPMC(1) AT EQUALITY
Hénon tower r=0,1,2 (automorphism control, td=1):
           B=42·2^r, α,β=1,2, μ_P=B, Λ_P=1:  αβμ_P/B = 2 >= 1  -> holds, slack 2
```

`RPMC(1)` being *tight* on the campaign's one realized record and slack on the
promoted automorphism control is the strongest structural evidence the
conjecture has; it is not a proof and I do not treat it as one.

### 3.3 `OP-3` (NEW, diagnosis): the reduced interface is the `(μ,B)`-forgetting quotient

The promoted entry datum is `E = (α,β; s; {(Λ_i,a_i,b_i,ν_i)})`
(`REDUCTION.md` §0). It contains **no** `μ_P` and **no** `B`. Both are
determined by an actual pair and both are dropped by the reduced record.
`OP-2` says the ceiling lives exactly in the dropped data. Consequences:

1. Every reduced-tier family with unbounded `td` at fixed type that the
   campaign has produced lives in this quotient, and each is a formal
   `RPMC(1)` countermodel: AUDIT 9342 Lemma 3.1 (`κ_P=6` fixed, `b` odd → ∞,
   `E_MR = b`, `td = 6b`) from 2026-08-23, and now `U1*(R)` with
   `E_MR = 2R/3`, `td = 4R`. The 2026-08-29 interface no-go is therefore the
   *second* independent instance of one phenomenon, not a new one — but it is
   the instance with the strongest local certification (a reviewed
   nondegenerate Proposition 8.1(iv) row at `k=1`).
2. The "interface no-go" has an exact explanation: the reduced merge/trunk
   laws cannot bound `td` because they are scale-free and the bound is a
   statement about scale. This is stronger and more useful than "the interface
   is insufficient": it names the single missing decoration.
3. Exact arithmetic of the collision (checked): at type `(2,3)` with entries
   `(a,b,ν)=(1,2,3)`, `RPMC(1)` forces `Σ 2/3 ≤ 1`, i.e. `R ≤ 1`, hence
   `td = 4 < 6`, which the published Żołądek bound excludes. So
   **`RPMC(1)` together with `td ≥ 6` annihilates the whole `U1*(R)` family**,
   including `R=3`, which is the td12 three-pole U1 entry (`E_MR = 2`).

I state the disjunction rather than a verdict: **either `RPMC(1)` is false, or
no `U1*(R)` occurs — in particular the entire conditional td12/U1/B25/S17
laboratory has no actual occurrence.** The reduced interface cannot decide
between these, by its own no-go.

### 3.4 `OP-4` (NEW form): the non-properness defect ledger

Let `S_F` be the Jelonek non-properness set, `δ(a,b) = td - #F^{-1}(a,b)`.
Bézout on `{f=λ}` against `{g=c}`, with all affine intersections transverse
because `J=1`, gives directly

```text
δ(λ,c)  =  Σ_{p at ∞ of the generic f-fibre with g(p)=c}  ord_p(g-c).
```

Summing over `c`, and writing `s`, `n` for the numbers of pole and non-pole
places at infinity of the normalized fibre and `G` for its genus,

```text
Δ(λ) := Σ_c δ(λ,c) = Σ_{non-pole p} e_p = td - χ(fibre) = td + s + n + 2G - 2.
```

The second equality is the promoted fibrewise Riemann–Hurwitz identity
(`div(dg)` is supported at infinity because `dg = dy/f_x = -dx/f_y` is the
canonical fibre differential and `J=1`); the first and third are the new form.
Because `f` has no critical points it is primitive, so the generic fibre is
connected and `c = 1` in the component-aware identity — an actual-source fact
the reduced record does not carry.

Consequences that are free (no conjecture): `Δ ≥ td` always, with equality iff
`G = 0` and `s = n = 1`; `n ≥ 1` (else `F` is proper, hence an automorphism);
and `Σ_i δ_i·deg(f|_{S_i}) = Δ` over the components `S_i` of `S_F`.

### 3.5 `OP-5` (NEW): exact inertia cycle type on `A(F)`

Fix generic `λ`. Then `g|_{fibre} : F_λ → C` is a degree-`td` map, unramified
on the affine part, non-proper exactly over
`T_λ = {c : (λ,c) ∈ S_F}`, and a genuine connected covering over `C \ T_λ`.
Hence its monodromy group `G_λ ≤ S_td` is transitive and is generated by the
`|T_λ|` meridians. For `c ∈ T_λ`:

```text
cycle type of the meridian at c
   =  ( e_p : p a non-pole place at infinity with g(p)=c )  +  1^{td-δ(λ,c)}.
```

Reason: each of the `td-δ` affine preimages is a point where `F` is a local
biholomorphism, so its sheet is fixed; each non-pole place `p` with `g(p)=c`
contributes the `e_p` solutions of `g = c+ε` near `p`, cyclically permuted.
So `|supp| = δ(λ,c)`, and the number of nontrivial cycles is the number of
such places.

This is the **inertia-support theorem** named as seam 1 of
`xmodel/sol-lateral3.md` §4 and never supplied. It is exact, not an
admissibility constraint, and its inputs — the non-pole boundary places and
their `g`-contact orders — are data the books already compute. It upgrades
Avenue 25 from "all 169 passports admissible, no kill" to "the passport is
determined". `G_λ ≤ G`, so `G_λ` primitive implies the full monodromy group
`G` primitive.

Immediate usable dichotomy: if some `c ∈ T_λ` has exactly one non-pole place
with `e_p = 2`, the meridian is a **transposition**; a primitive subgroup of
`S_td` containing a transposition is `S_td`. If instead some meridian is a
`p`-cycle with `p` prime and `p ≤ td-3`, Jordan's theorem gives `A_td` or
`S_td`. Otherwise `G_λ` — hence possibly `G` — is imprimitive, and a block
system appears.

### 3.6 `OP-6` (NEW as a spine statement): td-minimal selection is free in the pure-Sigray branch

The promoted spine selects a **GGV-minimal** pair, i.e. minimizes
`gcd(deg P, deg Q)`. That selection is existential and, as `CRITICAL 2`
records, says nothing about `td`. But `td` is a positive integer, so a
counterexample of **minimal topological degree** may be selected outright, and
`REDUCTION.md` T4 records that *arbitrary-pair* Sigray normalization is the
existing Sigray/Abhyankar theorem and preserves `td`. Hence:

```text
JC2 false
  -> some counterexample F with td(F) minimal over all counterexamples
  -> a Sigray normalization of that same pair, td preserved and still minimal
  -> td >= 6
  -> a finite entry menu at that td,
  PLUS the free hypothesis: no counterexample has smaller topological degree.
```

Trade, stated honestly: this branch gives up GGV's rectangles and
`TRANSPORT.md` Theorem 3.1's lexicographic minimality, so it cannot use the
farm as input and its normalization rests on the unrefereed Sigray layer
(gap G5). It is not a strict improvement; it is a different, currently
unexercised, branch. `sol-lateral3.md` §4 assumed minimal-`td` selection
without noticing that the promoted spine's GGV selection forbids it; that
incompatibility is, I believe, why the hypothesis has never been used.

The new hypothesis is only worth something if imprimitivity is fatal, which is
Avenue 26/43's remaining seam (§4.3).

### 3.7 The new avenue and the new connection, named

**NEW AVENUE `SCALE-PIN` (mechanism).** Promote `(μ_P)_P` with `Σμ_P = B` to a
first-class decoration of the Sigray boundary record, alongside
`(Λ_i,a_i,b_i,ν_i)`, and develop its transport law under entry, merge, trunk,
and detour operations. Free content already available: `Σ_{p|P} m_p = αμ_P`;
`Λ_P = Σ_{p|P} λ_p`; `Σ_P Λ_P = td`; `λ_p ≤ βB·m_p`; `μ_P ≥ 1` integral;
`div(dg)` supported at infinity. Conjectural content: `RPMC(1)`. Novelty check:
`μ_i` appears in the `KJN` lane (AUDIT `ENERGY LOCALIZATION`), and `TDBOUND`
states the summed inequality; what does not exist anywhere in the repository is
`μ` as a decoration of the configuration/book interface, or any transport law
for it. Classification: **NEW** as an interface object, **KNOWN** as an
ingredient of the capacity lane.

**NEW CONNECTION.** Avenue 2's ladder and the `KJN/RPMC/PC/DIR` capacity lane
are the same problem in two coordinate systems, joined by `Λ_P = Δ_P`
(`OP-1`), and the round's headline `U1*(R)` no-go is a direct formal
countermodel to the capacity lane's `C=1` case (`OP-3`). Neither the 17:58Z
overlay, the `U1*` binding integration, nor `REDUCTION.md`'s frontier overlay
cites `TDBOUND` or `KJN`; the statement "no promoted arrow bounds `td` above"
is true but omits that the repository contains a fully reduced sufficient
crux for exactly that arrow, and that this round's new family is its sharpest
test object. Classification: **NEW** as a connection; both endpoints
**KNOWN**.

## 4. Attacking the global selector, and the td12 detour discriminator

### 4.1 Prove

Target `DIR(1) ⟺ PC(1) ⟺ RPMC(1)`. In the `OP-2` form the statement to prove
is: *the pole mass of `g` over each proper root `P` of the common leading form
satisfies `B·Λ_P ≤ αβ·μ_P`.* Two features make this the right target rather
than a restatement:

- it is **tight** on the one realized record (equality at residue-A), so it is
  not an over-ask;
- it is **local at one boundary point**, so it can be attacked with the
  promoted per-root structures — the thick-line Smith degeneration with
  transverse defect exactly `αμ_P - 1`, the energy localization
  `E_P = ½Σ_{p>P}(R_p/α - S_p/β)²`, and the proved quantum `E_P ≥ 1/(αβ)`.

In energy form, `RPMC(1)` says the number of contact levels above `P` at which
`R_p/α ≠ S_p/β` is at most `2α²β²μ_P/B`, i.e. *almost every level above every
proper root is exactly proportional*. That is a statement about the
`Corollary 7.4` proportional centers, and it is where `J(f,g)=1` must do work
that finite generation, convexity, Hodge index, and mixed volume provably
cannot (AUDIT 9236–9241 rules those out by the class-kill decoy).

### 4.2 Falsify

The cheapest falsifier is not a new construction: it is `U1*(R)`. If any
`U1*(R)`, `R ≥ 3`, is realized by an actual normalized Keller counterexample,
`RPMC(1)`, `KJN(1)`, and `TDBOUND` all die simultaneously, and the sheet ladder
loses its only named route to a ceiling. That makes the *occurrence* question
for `U1*(R)` a first-class proof-side object, not merely a counterexample-side
curiosity. Note the asymmetry: non-occurrence of `U1*(R)` does **not** prove
`RPMC(1)`.

Empirical testing of `RPMC` on actual Keller pairs is impossible in principle:
every actual Keller pair available is an automorphism, and for an automorphism
the leading form is a power of a single linear form, so `μ_P = B` at the
unique boundary point and the inequality reads `B ≤ αβB`, which is vacuous.
Any "empirical support" for `TDBOUND` gathered from filed configurations
inherits the round-2 `COINCIDENCE-RISK` finding. I therefore recommend
**closing the empirical lane on `TDBOUND` entirely** and treating `RPMC(1)` as
a pure proof/refutation target.

### 4.3 Bypass

Replace the ceiling by a **descent**. With `OP-6`'s free td-minimality and
`OP-5`'s exact inertia data:

```text
G_λ imprimitive  ->  block system  ->  intermediate field C(f,g) ⊂ L ⊂ C(x,y)
                 ->  [OPEN SEAM] L has an A^2 open model and the induced map is
                     polynomial, Keller, non-proper, with smaller td
                 ->  contradicts td-minimality.
G_λ primitive    ->  small-support meridians (OP-5) + Jordan/minimal-degree
                 ->  G_λ ∈ {A_td, S_td}, then discriminant parity separates.
```

The single open seam is the bracketed one, and it is exactly Avenue 43's
subject matter. This bypass needs **no** `td` ceiling and **no** bounded type
menu — it is the only architecture in the portfolio with that property. Its
cost is that it must be run in the pure-Sigray branch, hence it inherits G5.

### 4.4 The strongest actual-source discriminator for the td12 detour families

The state packet asks which actual-source invariant is invisible to the reduced
witnesses. Answer: **`μ_P/B`**, the normalized multiplicity of the common
leading divisor at each pole's boundary point (§3.3). It is not a function of
`(α,β,Λ,a,b,ν)`; it is determined by the actual pair; and it is the exact
quantity the ceiling conjecture bounds.

Crucially this discriminator acts **above** the detour split: A7, C5, B25, S17,
D13, E11 are all descendants of the single td12 three-pole U1 entry, which has
`E_MR = 2`. A `μ`-based test therefore cannot separate the sextet — it either
kills their common antecedent or says nothing about them. That is the correct
reading, and it is a reason to move the primary slot from
`TD12-U1-FAMILY-AWARE-DETOUR-EXCLUSION/v2` to the entry.

Fail-closed test design: for the td12 entry, ask whether **any** integers
`B ≥ 1` and `μ_1,μ_2,μ_3 ≥ 1` with `Σμ_i = B` satisfy every *proved* constraint
(`Σ_{p|P_i} m_p = αμ_i`, `Λ_i = Σλ_p = 4`, `λ_p ≤ βB·m_p`, integrality). Three
typed outcomes:

```text
NO_ADMISSIBLE_SCALE : the entry dies unconditionally, with no conjecture used;
ADMISSIBLE_WITH_RPMC1_VIOLATION : the entry survives free facts but requires
    E_MR = 2 > 1, so it is live iff RPMC(1) is false — report and stop;
ADMISSIBLE_AND_RPMC1_COMPATIBLE : my §3.3 arithmetic is wrong; re-derive.
```

I do **not** predict the first outcome. I expect the second, which is still
worth having, because it converts "the interface cannot decide" into "exactly
one named conjecture decides".

## 5. K00 valuations two through five

### 5.1 An exact scope correction: the valuation-two leading cone is a 4-plane

From the promoted grade-eight integration alone, without touching case data:
the seven raw quadrics satisfy `Q1+8Q3 = (3/2048)·A·B` and
`Q4 = (3/524288)(B² - 64A²)`, with
`A = 16w1 - 4w3 + w5` and `B = w0 - 4w2 + 2w4`. Hence
`Q4 = 0 ⟹ B = ±8A`, and substituting into `AB = 0` gives `A = 0`, then
`B = 0`. Conversely the integration records that every `Q_i` vanishes on
`A = B = 0`. Therefore, set-theoretically over a characteristic-zero field,

```text
V(Q_1,...,Q_7)  =  V(A,B)  ⊂  A^6,     a LINEAR subspace of dimension 4.
```

`A` and `B` have disjoint variable supports, so they are independent; I
verified `rank{A,B} = 2` and that the old plane
`ℓ(s,t) = (2s, t/8, s, t, s, 2t)` satisfies `A(ℓ) = B(ℓ) = 0` exactly, with
`dim ℓ = 2`.

**Consequence.** The valuation-two leading vector `d[2]` ranges over an
irreducible 4-dimensional linear space. The promoted grade-eight exclusion
covers the 2-dimensional subplane `ℓ(s,t)`. The remaining locus is
codimension-2 *inside the same component*, not a set of unknown "other
components" — so the residual work has a known shape and a known size, and it
is a two-parameter extension of a computation that is already done and
reviewed, not a search. This is a scope **correction**, not a new obstruction:
it makes the valuation-two lane larger than the current prose suggests and
simultaneously makes it cheaper to finish than "other components" implies.

Caveat, stated: this uses the integration's own displayed identities. If any of
the three displayed relations is a transcription of a stronger scheme-level
statement, the set-theoretic conclusion still holds (radicals only shrink the
variety), but the *scheme* structure on the cone is nonreduced and is not
described by this argument.

### 5.2 Where the old plane came from, and why that matters

`ℓ(s,t)` is inherited from the valuation-**one** grade-four/grade-five
normalization (`ell=(2s,t/8,s,t,s,2t)`, `mu=(s²,st/8,16t²,0,0,0)`). The
valuation-one seed is now dead. There is no promoted argument that the same
2-plane is the right slice of the valuation-two cone, and the 1517Z synthesis
records that the period-two renormalization and the translated-quadratic
functor — the two candidate group actions that could have justified the
restriction — are both refuted. So the restriction to `ℓ` is currently
**inherited convention, not a licensed normalization**.

### 5.3 Cheapest honest discriminator for valuations two through five

Do not start with algebra. Start with the **grade-19 budget table**, which is
pure bookkeeping over the promoted contraction

```text
Rmix = Λ² k10 D10 + Λ⁶ k6 D6 + Λ^10 k2 D2 + Λ^14 μ2 u2 + Λ^16 μ4 u4
     + Λ^18 μ6 u6 - Λ^19 Jdet·h/4,      h = 20 + 63 d4,
min transverse degrees (D10,D6,D2,u2,u4,u6,-h/4) = (3,2,2,1,1,1,0),
(-h/4)(0) = -5.
```

With `v = v_Λ(d)` and the frozen boundary zeros, the earliest `Λ`-order at
which each term can contribute is `2+3v`, `7+2v`, `11+2v`, `15+v`, `17+v`,
`19+v`, and exactly `19` for the last. Reaching grade 19 therefore requires:

```text
term            live iff        v=2  v=3  v=4  v=5  v>=6
k10·D10         3v <= 17        yes  yes  yes  yes  no
k6·D6           2v <= 12        yes  yes  yes  yes  yes (v=6 only)
k2·D2           2v <=  8        yes  yes  yes  no   no
μ2·u2            v <=  4        yes  yes  yes  no   no
μ4·u4            v <=  2        yes  no   no   no   no
μ6·u6            never          no   no   no   no   no
```

This reproduces the promoted `v ≥ 6` theorem exactly (only `k6·D6` survives at
`v=6`, and grade 12 kills it via `Q1(x)=Q3(x)=0` together with
`D_K6^[2] = -4Q1 - 32Q3`), and it shows the true ordering of difficulty:

```text
v = 5 : two live terms  -> cheapest, closest in shape to the v>=6 proof
v = 4 : four live terms
v = 3 : four live terms
v = 2 : five live terms -> most expensive, and also the one with the
                           unexamined 2-parameter leading directions
```

Recommended order: **`v = 5` first, then `v = 4`, then `v = 3`, then `v = 2`**,
which is the reverse of the current attention. The `v = 5` case has a two-term
grade-19 row whose constant part is `-5·Jdet[0] ≠ 0`; the obstruction is
whether `k10[·]·D10` and `k6[·]·D6` can cancel it subject to the grade-`2v`
leading-cone condition `A(x)=B(x)=0`. That is a hand-sized question and it
needs no CAS.

Explicit non-claim: none of this excludes any valuation. It states the exact
budget and the cheapest order of attack.

### 5.4 Disposition of the review-derived unreviewed claims

My own prior review derived a grade-eight rank-two-stratum kill, invariance
along the old plane, and an over-`Q` closure of the old-plane lane, isolating
`u = ±8iv, v ≠ 0` over the algebraic closure as the smallest survivor. These
remain `REVIEW_DERIVED_UNREVIEWED` and I do not consume them here. §5.1 is
independent of all of them and is derived only from the integration's own
displayed identities. If the review-derived claims are later promoted, they
close the old plane and the residual valuation-two work is exactly the
codimension-2 complement described in §5.1 — the two results compose cleanly.

## 6. Mathematical-software acceleration / decisive experiment

**`SCALE-AUDIT/v1` — exact `μ`-admissibility and `RPMC` slack over the filed
corpus.** Not a CAS job; a small exact-rational and small-integer feasibility
pass.

Input: every filed entry record `(α,β,s,{(Λ_i,a_i,b_i,ν_i)})` in the on-axis,
off-axis, td-7, td-11, td-12, and sextet-antecedent tables, plus `U1*(R)` for
`R = 3,5,7,9`, plus the promoted controls (residue-A; the Hénon tower at
`r = 0,1,2`; the class-kill decoy as a negative control).

Per record, compute exactly: `Λ_i` from `αβ a_i b_i/ν_i` and check it against
the filed `Λ_i` (a free consistency gate that will catch transcription drift);
`td = ΣΛ_i` against the filed `td`; `E_MR = Σ a_i b_i/ν_i`; and then decide the
integer feasibility problem of §4.4 over `B` and `(μ_i)`. Report per record one
of the three typed outcomes of §4.4, plus the `RPMC(1)` slack
`min_i (αβμ_i/B)/Λ_i` over feasible scales.

Why it is decisive rather than decorative: it is the first computation in the
campaign that spans **both** coordinate systems, and it has three distinct
kill modes — a filed `Λ_i` that disagrees with `αβ a_i b_i/ν_i` is a data
defect; a record with no admissible `(B,μ)` is an unconditional kill; and a
record with `E_MR ≤ 1` that the books nonetheless killed by a route
independent of the census construction is a genuine `TDBOUND` falsifier and
would retire the whole ceiling lane. Compute class: **desk-scale**, seconds,
pure Python integers and `fractions`, no CAS, no AWS. Stop rule: report and
stop on the first `NO_ADMISSIBLE_SCALE` or the first independent
`E_MR ≤ 1` kill.

Explicit warning against the obvious misuse: the sextet rows A7/C5/B25/S17/
D13/E11 share one antecedent entry and are therefore **one** sample, not six.
Any tally that counts them separately reproduces the round-2
`COINCIDENCE-RISK` error.

## 7. Campaign-systems card

**`UPGRADE — FENCE-DEFAULT/v1`** (proposal only; I created no files outside my
report and `/tmp`).

Evidence. The repository already has a root `.ignore` containing `jc2-lean/`,
and `rg` honours it: `rg --files . | grep -c '^\./jc2-lean/'` returns `0`
today. But `.ignore` is consumed only by ripgrep-family tools. Bare
`grep -r`, `find`, `ls -R`, `du -a`, and `shasum -r` do not consume it, and
those are precisely the tools that produced the two disclosed fence
traversals: the K00 `r=2` grade-eight hostile review's `grep -rl`, and mine in
§0 today. Two incidents in two days, same tool family, after the fence was
already documented as "intentionally stronger than a Git ignore rule". The
defect is not researcher discipline; it is that the fence is opt-in for the
POSIX tools and default-on only for `rg`.

Change (smallest that removes the class):

1. `ops/rgrep` — a wrapper, `exec grep -r --exclude-dir=jc2-lean
   --exclude-dir=.git --exclude-dir=__pycache__ "$@"`.
2. `ops/fence_check.sh` — asserts, per tool family, that the fence holds:
   `rg` via `.ignore`; `ops/rgrep` via `--exclude-dir`; and *records as a
   known-unfenced control* that bare `grep -r` does traverse, so the failure
   mode is documented rather than assumed absent.
3. One line in the standing prompt boilerplate: *use `rg` or `ops/rgrep`;
   never bare `grep -r`, `find`, or `ls -R` from the repository root.*

Smallest measurable acceptance test:

```text
ops/fence_check.sh
  A: rg --files . | grep -c '^\./jc2-lean/'          == 0     (must pass)
  B: ops/rgrep -l "" . 2>/dev/null | grep -c jc2-lean == 0     (must pass)
  C: control — bare grep -r reaches the fence         != 0     (must be
     reported, proving the test is not vacuous)
exit 0 iff A and B pass and C is reported.
```

Longitudinal acceptance: disclosed fence traversals per round drops from the
current 1-per-round to 0 for two consecutive rounds. Runtime: under one
second. No caller migration is forced; no mathematical claim depends on it.
This is not `NO_CHANGE`: the recurrence is documented twice and the fix is
three files and one prompt line.

Second, smaller observation, filed without a card: the `perl`/locale warning
that `shasum` emits on this host prepends four lines of noise to every hash
verification, which is a real transcription hazard in custody blocks. A single
`LC_ALL=C` in the hashing helper would remove it. Not worth a checkpoint on its
own; worth folding into the next systems change.

## 8. Research cards and ranked launch portfolio

### CARD A — `SCALE-PIN` / `RPMC(1)` in tree coordinates

- **Novelty**: `NEW` as an interface object and as the per-point form
  (`OP-2`); `KNOWN` as a capacity-lane ingredient (`AUDIT` 9356 ff.,
  `TDBOUND` §2 summed form). Not a `SCOPE-CONFLICT`: it adds a decoration, it
  does not restate a promoted theorem.
- **Dependencies**: promoted pure-boundary identity `F_X G_Y - F_Y G_X = jZ^M`;
  `ENERGY LOCALIZATION`; `POINT-BASIS = INTERSECTION DEFECT`; `TDBOUND` §2;
  `REDUCTION.md` §0 entry conventions. No `jc2-lean`, no AWS.
- **Exact proposed theorem**: for every Sigray-normalized Keller pair and every
  proper root `P` of the common leading form, `B·Λ_P ≤ αβ·μ_P`, where `Λ_P` is
  the total pole order of `g` on the branches of the generic `f`-fibre over `P`
  and `μ_P` is the multiplicity of `P` in `div_{L∞}(H)`. Equivalently
  `a_P b_P/ν_P ≤ μ_P/B`.
- **Cheapest discriminator**: `SCALE-AUDIT/v1` (§6), then the two-level energy
  question — is the number of non-proportional contact levels above `P` bounded
  by `2α²β²μ_P/B`?
- **Outcome meanings**: proof ⟹ `KJN(1)` ⟹ `td ≤ αβ` at each provenanced fixed
  type, and (with G2, a type menu) the first genuine ceiling, and the td12
  laboratory becomes vacuous. A counterexample within the Keller class ⟹ the
  whole capacity lane and `TDBOUND` retire, and Avenue 2 has no named ceiling
  route left. A counterexample only in a non-Keller class ⟹ no information
  (the class-kill decoy already occupies that position).
- **Stop condition**: stop if the per-point form is shown to be *strictly*
  weaker than the summed form on a promoted record, or if any promoted record
  violates the free constraints (which would mean my `OP-1` normalization is
  wrong).
- **Expected information gain**: high. It converts "no arrow bounds td" into
  one named inequality that is tight on the campaign's only realized record.
- **Compute class**: desk-scale for the audit; the theorem is proof work.
- **Parallel ownership**: prove — capacity/`DIR` lane; falsify — `U1*(R)`
  occurrence lane; bypass — `CARD B`.
- **Provisional descendant that may start without review**: the `SCALE-AUDIT`
  consistency gate (`Λ_i =? αβ a_i b_i/ν_i`, `td =? ΣΛ_i`) over the filed
  corpus. It uses only promoted identities and can only find data defects.

### CARD B — `INERTIA-EXACT`: determined passports, td-minimality, block descent

- **Novelty**: `OP-5` is `NEW`; the surrounding program is `KNOWN` and
  unexecuted (`sol-lateral3.md` §4, Avenues 25/26/43). `OP-6` is `NEW` as a
  spine statement. Not a `DUPLICATE`: the 2026 passport work tested
  *admissibility*; this determines the passport.
- **Dependencies**: `OP-4`, `OP-5`; `REDUCTION.md` T4 arbitrary-pair Sigray
  normalization; the books' non-pole boundary place and contact-order tables.
- **Exact proposed theorem (stage 1, expected provable)**: for a Keller pair
  and generic `λ`, the monodromy of `g|_{f=λ}` over `C \ T_λ` is transitive
  with meridian cycle types `(e_p : g(p)=c) + 1^{td-δ(λ,c)}`, and
  `Σ_{non-pole p}(e_p - 1) = 2G - 2 + td + s`.
- **Exact proposed theorem (stage 2, the open seam)**: a nontrivial block
  system for that monodromy yields a polynomial Keller counterexample of
  strictly smaller topological degree.
- **Cheapest discriminator**: take the filed td-6 residue-A record — where
  `s = 2`, both pole orders are `3`, and the non-pole contact data are
  computed — and write down the *determined* meridian cycle types. Compare with
  the 169 admissible `S_6` passports. If the determined passport is a strict
  subset, the method has kill power the admissibility test never had; if it
  reproduces all 169, stage 1 is exact but has no `td=6` leverage and the value
  is entirely in stage 2.
- **Outcome meanings**: stage 1 alone raises Avenues 7/25/26. Stage 1 plus a
  transposition or short-prime-cycle meridian forces `A_td`/`S_td` under
  primitivity. Stage 2 proved ⟹ a descent architecture needing neither a `td`
  ceiling nor a type menu — the only such architecture in the portfolio.
  Stage 2 refuted ⟹ Avenue 26 is closed for good and `sol-lateral3.md` §4 can
  be retired.
- **Stop condition**: stop stage 2 if a block quotient is exhibited whose
  normalization has no `A²` open model; that is the documented classical risk.
- **Expected information gain**: high on stage 1 (cheap, exact, immediately
  reprices three avenues); speculative on stage 2.
- **Compute class**: desk-scale. Stage 1 is bookkeeping against existing
  tables; the group theory is `GAP`-sized but can be done by hand at `td ≤ 14`.
- **Parallel ownership**: prove — stage 1 owner; falsify — stage 2 seam owner
  (Avenue 43); bypass — none needed.
- **Provisional descendant that may start without review**: the residue-A
  determined-passport computation. It consumes only promoted `td=6` boundary
  data.

### CARD C — `K00-V2-CONE`: the unexamined two parameters at valuation two

- **Novelty**: `NEW` as an exact scope statement (`V(Q) = V(A,B)`, a 4-plane,
  with the excluded old plane of codimension 2 inside it). `KNOWN` that
  something remained: the binding integration says "other components". The
  correction is that there are no other components — one component, and a
  codimension-2 slice of it has been done.
- **Dependencies**: the promoted grade-eight integration's three displayed
  identities only. Does not depend on my `REVIEW_DERIVED_UNREVIEWED` claims.
- **Exact proposed experiment**: rerun the exact grade-four-through-eight
  calculation with `x = d[2]` a general point of `{A=B=0}` (four parameters,
  or three after any licensed scaling) rather than `ℓ(s,t)` (two). The quadrics
  `Q_i`, the loads `L_i`, and the row relations `Q1+8Q3 = (3/2048)AB`,
  `Q4 = (3/524288)(B²-64A²)`, `L1+8L3 = 0`, `L4 = 0` are unchanged; only the
  shift vector `a` and the residual rows change.
- **Cheapest discriminator**: before any algebra, run the §5.3 grade-19 budget
  table and confirm the ordering `v=5 < v=4 ≈ v=3 < v=2`. Then do `v=5` — two
  live terms — first.
- **Outcome meanings**: `v=5` excluded ⟹ three valuations remain and the
  method is confirmed to extend below six. `v=5` survivable ⟹ the grade-19
  contraction stops being the universal instrument and the lane needs a second
  obstruction. Grade-eight exclusion extended to all of `{A=B=0}` ⟹ valuation
  two closes on this support.
- **Stop condition**: stop and report `OPEN` rather than normalizing a leading
  coordinate without a licensed action — the period-two renormalization and the
  translated-quadratic functor are both already refuted, so no such licence is
  currently available.
- **Expected information gain**: high on the counterexample side; this is the
  cheapest live object in that half of the portfolio.
- **Compute class**: desk-scale for the budget table and for `v=5`. If the
  general-`x` grade-eight rerun needs elimination beyond hand size, it is
  **AWS-only and must be preregistered** with ring, equations, controls,
  outputs, resource cap, and stop rule. Do not run uncertain CAS locally; the
  1517Z round already banked one `RESOURCE_CAP_NO_VERDICT` incident from
  exactly that.
- **Parallel ownership**: prove (exclusion) — K00 owner; falsify (existence of
  a compatible jet) — same owner, opposite hypothesis, same computation;
  bypass — none.
- **Provisional descendant that may start without review**: the §5.3 grade-19
  budget table. It is arithmetic over promoted minimum degrees.

### Ranked launch portfolio

1. **`CARD B` stage 1** — cheapest, exact, and it reprices Avenues 7, 25, 26
   immediately. Desk-scale.
2. **`SCALE-AUDIT/v1`** (§6) — desk-scale, three independent kill modes, and it
   is the prerequisite instrument for `CARD A`.
3. **`CARD C` at `v=5`** — cheapest live counterexample-side object, and it
   tests whether the grade-19 instrument extends below six.
4. **`CARD A`** — the highest-value target and the slowest; it should not
   occupy the primary slot until `SCALE-AUDIT` has confirmed the arithmetic
   across the corpus.
5. **`CARD C` general-`x` grade eight** — after the `v=5` result, and
   preregistered if it exceeds hand size.
6. **`CARD B` stage 2** — highest variance; start only after stage 1 lands.
7. **`TD12-U1-FAMILY-AWARE-DETOUR-EXCLUSION/v2`** — demoted, not cancelled.
   It is downstream of an entry that one named repo conjecture would make
   vacuous. It should not hold the primary slot.

## 9. Non-claims

Nothing here proves or disproves JC2. No occurrence theorem, no attainment, no
`PairRef`, no source value, no degree ceiling, no type menu, no algebraization,
no polynomial Keller pair, and no counterexample is asserted. `U1*(R)` and the
AUDIT Lemma 3.1 family remain formal necessary-data configurations, not maps;
their collision with `RPMC(1)` is a disjunction, not a verdict on either side.
`RPMC(1)`, `PC(1)`, `DIR(1)`, and `TDBOUND` remain conjectural; I neither prove
nor refute them. `OP-6` is a licensed selection in the pure-Sigray branch only,
and it trades `G2-PSC` for `HIGH 3`. The §5.1 cone statement is set-theoretic
over a characteristic-zero field and says nothing about the nonreduced scheme
structure, and it excludes no valuation. A lower floor is not attainment, a
reduced equality is not a scheme equality, and a finite jet is not a formal
arc, an algebraic germ, a polynomial map, a counterexample, or a JC2 result.
`B` and `S` are kept separate throughout. No new exit price is derived and
none is asserted; accordingly no `charge_basis` line appears in this report.

No canonical file was edited, no commit or push was made, no AWS resource was
used, no web access occurred, and no peer submission, prompt, log, or run
record from round `20260829T1808Z` was read. The single execution irregularity
is disclosed in §0.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `47459`.
- Body SHA-256: `486b11c0082831f295f0505fee5482d1ab1d955964e793b5a93914eed5ebdc86`.
- Frozen basis: `e930fa90b8ee9d86220a2cff72fba94e04b20baa`.
