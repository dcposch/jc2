# Mixed scalar: recover the KNOWN septic and use a univariate test

ROOT September12,2026; publication begun19:03UTC. DOCUMENTARY/MANUAL,
UNPROMOTED; a recovered interface and changed experiment, not a new
finite-rank theorem, unit certificate, source result or measured speedup.

## Priority checksum and exact inputs

The tentative two-cubic-to-septic simplification is ALREADY accepted as
AUDIT17zzd (September10). The September11 mixed-scalar FIRST did not charge
that earlier packet and left even coprimality/dimension of its two cubics
undecided. Do not pay for another foundation review or advertise rediscovery.
The useful delta is composing the old whole-leading algebra with the NEWER
mixed-scalar B, changing the selected computation to univariate Bezout.

All four reports SHA-before-WHOLE, including current readbacks:

- xmodel/f10-middle-univariate-unit-astra-20260910.md
  890e5c9c63a333bf873e963821c368d2dc080476eaf12c9fc6a10dce53db75e5;
- xmodel/f10-middle-septic-gate-fable5-20260910.md
  cff18a06c050d64074ba9af70cea61b28b15614a61691125ebf26d3012577c62;
- xmodel/f10-mixed-kernel-scalar-astra-20260911.md
  f6fe0986808f697e5e437af7f40ae5064cb609c92887a42a10963e91d09191c8;
- xmodel/f10-mixed-scalar-unit-gate-fable5-20260911.md
  aa8b0148f8bfb63ef072b23106f92bafa703a6b83033984e17a62f49eb70230f.

Targeted canonical/history reads, not a whole-corpus novelty audit. The
earlier septic theorem and its inherited premise qualifications are imported,
not re-proved by current file hashes. No scientific execution.

## Literal parameter/coordinate map

Write t=nu=(5r+2)/(3r+1), r>=2, so 5/3<t<=12/7. The old tau is
2-t; its V,W are exactly the newer X,Y: both normalize the same polynomial
to 1+u+Xu^2+Yu^3. There is no map between unrelated same-named rings.
Let c_j=binomial(t,j) and d_j=[u^j](1+u+Xu^2+Yu^3)^t as in the inputs.

For a compact source instantiation define polynomials in X over Q(t):

    U=3X^2-3(t-1)X-(t-3)(3t-4)/4,
    V=X^3+(t-3)(t-1)X^2+(t-3)(t-4)(4t-5)X/20
                       +(t-3)(t-4)(t-5)(3t-4)/420,
    K=X^3+3(t-3)X^2/2+(t-3)(t-4)X/4
                       +(t-3)(t-4)(t-5)/120,
    P=3V^2/(t-2)+(6X+t-3)VU+KU^2.

Termwise expansion of the charged cubics gives

    d6/c3=3Y^2/(t-2)+(6X+t-3)Y+K,
    (d7-(t-2)d6)/c3=UY-V.

Here c3 and t-2 are nonzero rational units for all actual r. To compare
with the accepted September10 formulas, old D=4U, old K=-840V, and old
S=470400(t-2)P. Its E=120(t-2)d6/c3. Thus this P has degree7 with
leading coefficient9, and defines the SAME whole rank-seven algebra.
Do not confuse the compact K above with old K or with the scalar's K_r.

An explicit cross-check of the denominator mapping: put

    x0=-3(t-3)/14, q=X+(t-2)(t-1),
    A=(3t-5)(3t-4)(2t-3)/15,
    U0=(t-3)(6t-11)/196, M=3(X+x0-(t-1)).

Then V=qU/3+A(X-x0), U=U0+M(X-x0), so

    A U0=(A+Mq/3)U-MV.

Both A,U0 are nonzero rationals for every actual r. In UY=V this gives
an explicit inverse of U, namely (A+Mq/3-MY)/(A U0). Modulo U, P is
3V^2/(t-2); the same Bezout identity implies gcd(P,U)=1. These are the
old D,K Bezout identities rescaled, not a new denominator theorem.

Consequently the already accepted exact whole-ring map is

    S_r=Q[X,Y,(Yd5)^-1]/(d6,d7) = Q[X]/(P), Y=V/U.

The guard is automatic by17zzd, not dropped experimentally. Its proof
uses m deg(d)=n deg(phi) at any hypothetical guard-zero residue field,
gcd(m,n)=1 and m>=7>deg(phi). Squarefreeness/rank7 remain imported;
the unit-certificate implication below would also hold with nilpotents.

## Selected discriminator, certificates and stop

Let B(t,X,Y) be the exact mixed-scalar coefficient from the September11
inputs. Its proved ordinary degree<=7 makes

    F(t,X)=U^7 B(t,X,V/U)

a polynomial in X with coefficients in Q(t), degree<=21. Since U is a
unit, B is a unit of the whole actual S_r iff gcd(P,F)=1 in Q[X].
There is no remaining Yd5 saturation, chosen field or parameter sample.

Selected experiment: one extended Euclidean calculation over Q(t)[X],
retaining an exact cleared identity

    A1(t,X)P(t,X)+A2(t,X)F(t,X)=N(t), N != 0.

An independently reconstructed polynomial identity, followed by exact
exclusion of every root N((5r+2)/(3r+1))=0 for integer r>=2, is sufficient.
Factor N over Q[t]; nonlinear irreducibles have no rational root, and a
linear root t0 corresponds to r=(2-t0)/(3t0-5), unless t0=5/3 (no finite r).
Generic gcd1 without this exception analysis is not an all-r result.
Any actual-r denominator exception, generic common factor or budget failure
is a precise nondecision, not a source point. No automatic retry/farm.

Certificate bytes must be DATA, not source executed before checking. The
current multivariable packet's executable-certificate defect needs repair;
the mathematical algorithm can change at the same bounded rewrite. The
checker must own its ring, reconstruct P/F independently, parse only exact
coefficient/exponent data and check every coefficient before emitting PASS.
The source/ring normalization above needs actual-code review as instantiated;
no new foundational mathematical FIRST is selected for the duplicate theorem.

Controls: at t=5/3 the old denominator proof legitimately loses A!=0, and
the cube has B=0; no uniform generic-specialization shortcut. An altered
cofactor constant must fail the full reconstructed identity; a certificate
containing PASS/exit text must be rejected as invalid data, not executed.
These are proposed checks, not executed results. The scalar unit question,
actual-source arrays, REG/model comparison, source zero, allF10 and JC2
remain open. No new canonical OPEN, worker, authority, CAS/helper/import/
AST/syntax/test/dummy, protected-tree or mirror action.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5711`.
- Body SHA-256:
  `b18f02300566a145479a373b225348987fedfd6b7e1fff20a18423ea3ca1c640`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
