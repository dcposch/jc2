# Ideation 20260904T1200Z GPT-5.5

## 0. Custody And Verdict

Mechanical custody check first: I regenerated the four-input manifest from
`xmodel/ideation-20260904T1200Z-gpt55.run.v2` by joining the
`charged_input_<i>_sha256` and `charged_input_<i>_basename` receipt lines, then
ran `sha256sum -c` against `/tmp/jc2-lane.aJaAJ8/inputs`. All four frozen
inputs returned `OK`. The first attempt only failed because I tried to write the
manifest inside the read-only frozen input directory; no content mismatch was
present.

I read the sealed packet first. I did not read any
`ideation-20260904T1200Z-*` submission. For the live running lane
`k16-hsop-length-allt-opus5-20260903`, I read only its `.run.v2` receipt; it has
`initial_status=RUNNING` and no `final_status`, so I did not open its `.md`.
No canonical ledger was edited, and I did not inspect `jc2-lean`.

Bottom-line verdict:

1. The packet headline is mostly right: foreground fixed-system computation is
   exhausted on both current halves.
2. The coordinator framing still overstates the reduction interface. The
   dichotomy "split or no split" is tautological only after the typed
   first-separation and route-to-state maps are supplied. Case (A) and D=108
   no-split show the dangerous middle: no principal split in the tested window,
   but no clean order-chart death either.
3. The first theorem target should be K16 tail hsop, not Z-EMPTY and not the
   discriminant route. Z-EMPTY is a useful failure mode diagnostic; it is not a
   smaller all-t proof object.
4. For (H1), I do not see a uniform theorem route that is currently launchable.
   The only honest theorem route is an augmented Schur/Fitting theorem, and the
   freshest obstruction-core report explicitly says the final Schur scalar is
   not determined by the small skeleton data alone
   (`xmodel/two-place-obstruction-core-sol56-20260903.md:648-663`). The campaign
   should commit to the corrected census engine while running only a small
   Fitting preflight as a falsifiable theorem probe.

## 1. Disposition Vector

Changes only, relative to the `2026-09-04T08:56Z` overlay in
`APPROACHES.md`.

| row | disposition | reason |
|---:|---|---|
| 1 | RAISE / RETYPE | The monomial-Jacobian receiver is now the only narrow structural object with a concrete invariant: K16 tail hsop length `L_t = 2*binom(3t+1,t-1)`. Retype away from fixed-t cone dimension and toward an indexed initial-ideal/regular-sequence theorem. |
| 2 | RAISE / RETYPE | The sheet/Moh machinery is no longer a ceiling or row census; it is the route-to-state interface for `(H1) split` versus `(H2) descent`. It needs a no-third-case theorem with explicit route data, not another raw skeleton count. |
| 25/26 | RAISE slightly | Coupled branch-cycle/Galois data are not the main proof, but they are now relevant as a reduction-audit tool for N1 and for "no hidden third configuration". A passport-only lane is still low; a source-state map audit is not. |
| 32 | REOPEN / RETYPE | The global off-diagonal collision ideal remains circular, but a local Schur/Fitting compiler for the joint two-point obstruction is now exactly the missing (H1) theorem interface. Keep it local and datum-typed. |
| 36 | RAISE as bounded census engineering only | After `disc(p_red)` failed as a theorem route, the corrected engine on the 166 `u_s>1` groups becomes the honest (H1) deliverable. This is not random support search and not proof by sampling. |
| 46 | LOWER as discovery route | Formalization is useful only after the route-to-state and chart-necessity statements stabilize. It should not consume discovery bandwidth now, and this lane was explicitly forbidden from inspecting `jc2-lean`. |

No change: STOP fixed-t cone-dim lanes at `t >= 8`; STOP native preprocessing
as a main line; keep row 29 as an already-raised Moh-filtration derivative
receiver, not an independent slogan.

## 2. Q1 K16

Typed claim Q1.1: the strongest presently provable K16 statement is conditional,
not promotional:

```text
K16-TAIL-HSOP-COND(t):
  In S_t = A_t[b4,q2_0,...,q(t-1)_0,b3],
  weights = (1,2,...,t-1,t+1),
  if the t rows T_{t,t},...,T_{t,2t-1} have a homogeneous
  zero-dimensional quotient, then they are an hsop/regular sequence and
  the length is
      L_t = 2*binom(3t+1,t-1).
```

This is not a conjectural Hilbert-series slogan. It is exactly the
Cohen-Macaulay implication used by the Hilbert-guided lane:
`dim S_t/J_t^tail = 0` makes the `t` rows a regular sequence and forces the
complete-intersection series
`prod_{d=2t+2}^{3t+1}(1-s^d) / ((1-s) prod_{j=2}^{t-1}(1-s^j)(1-s^{t+1}))`
(`xmodel/k16-square-tail-stdhilb-gpt55-20260903.md:21-35`). The measured
lengths are `90, 572, 3640, 23256, 149226, 961400` for `t=3..8`
(`xmodel/k16-square-tail-stdhilb-gpt55-20260903.md:37-46`).

Desk check run here: `python3 box/k16hsop-20260903/ci_series_identity.py`
completed in about two seconds and verified the closed length identity through
`t=24`, including the banked measured values `L_3..L_7`. The companion
`pure_power_sdr.py` shows that the naive pure-power/matching proof is dead:
the required SDR exists only for `t=3` and `t=5`; for `t>=16` Nagura gives two
primes in `[2t+3,3t+1]`, both requiring weight `1`, so Hall fails. Therefore
the all-t proof should not try to assign every row degree to a pure power of a
distinct variable.

Typed claim Q1.2: the likely structural route is a triangular initial-ideal
proof, not a resultant proof. Fable's TOPTAIL writes the b4=1 top-tail rows as
quadratics in `b3`; eliminating `b3` creates the known `2^(t-2)` surcharge. That
is a signal to keep `b3` and prove a monomial/triangular staircase in the
original `t` variables. The target is:

```text
K16-TAIL-INITIAL(t):
  after an indexed triangular change preserving weights and A_t fibres,
  in(T_{t,t},...,T_{t,2t-1}) contains a stable monomial ideal M_t
  whose quotient has exactly L_t standard monomials.
```

The cheapest test is not another full `t=8` standard basis. It is to mine the
completed `t=5,6` two-fibre bases and the accepted `t=7,b0` basis for the
leading staircase, then compare it to a closed Ferrers rule. The `t=7,b0`
transcript already passed `dim=0`, `vdim=149226`, and all tail normal forms
with a 14,493-element basis
(`xmodel/k16-square-tail-stdhilb-gpt55-20260903.md:185-211`), while `t=7,b1`
aborted before checks (`xmodel/k16-square-tail-stdhilb-gpt55-20260903.md:214-231`).
If the same staircase rule fits `t=5,6` both fibres and `t=7,b0`, run only the
degree-wise Macaulay rank checks for `t=7,b1` up to the predicted Hilbert
function. A rank-profile failure is a refutation of the proposed initial
staircase; a pass gives a theorem blueprint without asking Singular to emit a
full basis.

Typed claim Q1.3: Z-EMPTY is not the better all-t target unless the route-to-JC
actually needs z-type cone points. The DEP lane proves the correct identity

```text
J(Q_p,P_p) = c*gamma + pi*E_t(h),  E_t(h)=sum_{k=0}^{2t-1} T_{t,k}(p) h^k
```

and replaces the false dependent-pair target by
`(V0)_t <=> (T)_t and Z-EMPTY_t`
(`xmodel/k16-dep-locus-fable5-20260903.md:18-33`). But Z-EMPTY is the cone plus
one more homogeneous generator; it is not smaller. At `t=8`, the `b4=0` half is
proved, while the `b4=1` z-chart timed out with 17 generators and 7 effective
variables (`xmodel/k16-dep-locus-fable5-20260903.md:172-201`).

Therefore:

```text
K16 recommendation:
  primary all-t theorem = tail hsop / initial staircase;
  secondary diagnostic = K16-Z-TYPE-RECEIVER;
  fixed-t Z-EMPTY jobs only as falsifiers or controls.
```

Bounded quantities and cheapest tests:

| target | quantity | cheapest test |
|---|---|---|
| tail hsop | prove indexed initial staircase with `#standard monomials = L_t` | parse lead monomials from `t=5,6` both fibres plus `t=7,b0`; run degree-wise modular rank, not full GB, for `t=7,b1`; expected under 10 min for extraction and a first rank window, longer only if the matrix code is not already reusable |
| Z-EMPTY | decide whether z-type receiver is relevant to descended JC data | source-type the explicit z-type family against Prop 6.3/6.4 receiver data; no CAS, one source-read lane |
| `t=8` z-chart | decide `OPEN[K16-Z-EMPTY-T8-B4ONE]` | one quiet-host modular `std/slimgb` at 1800 s if and only if the source-typing says z-type matters |

## 3. Q2 (99,66) Unsplit And Literature

Typed claim Q2.1: case (A) should be attacked structurally before another full
standard-basis run. Case (A) is not a split-branch residue problem. The
necessity dossier states the trichotomy inside the same Moh row:

```text
(A) unsplit: g_sigma = (pi-c)^27, transformed to
    (27,18; M2=21, V2=8, delta2=-1, delta1=0), Jacobian x^4.
(B) split [2,1] at delta=2.
(C) split [1,1,1] at delta=5/2.
```

The split cases (B),(C) are the killed branches; case (A) is outside that joint
chart (`xmodel/g9966-chart-necessity-opus5-20260903.md:131-150`). The later
qualification says exactly why the old case-A empty verdict cannot be used: it
was the same `delta_1'=0` subfamily slice/`-c` artifact exposed by the
row-2515 gate and repaired by the full-basis lane (`AUDIT.md:17712-17720`).

Case (A) and D=108 no-split are in the same zero-slope family:

```text
(n',m')=(3K,2K),  u'=1,  V2'=K-1,  k=4,
M2'=3K-6,  R=k+1=5,  delta1'=0,  delta2'=-1.
```

D=108 no-split has `K=8`; case (A) has `K=9`. The D=108 descent report gives
the family formula and records that the ordinary total-degree chart is not
sourced (`xmodel/g108-nosplit-descent-grok46-20260903.md:327-345`,
`xmodel/g108-nosplit-descent-grok46-20260903.md:420-454`). The corrected
full-basis lane then shows that the zero-slope system no longer has a standalone
`-c` kill and times out at 1800 s for D=108 with 472 equations and 110 unknowns
(`xmodel/order-basis-full-gpt55-20260903.md:187-230`).

I would not spend the next lane on a raw case-A standard basis. The structural
ask is sharper:

```text
ZERO-SLOPE-FAMILY(K):
  For K>=8, decide the full D1 necessary chart for
  (3K,2K; M2=3K-6, V2=K-1; k=4, delta1'=0).
```

The cheapest test is to emit case (A) with `order_basis_full.py` and run only
the linear D1/Jacobian Schur preconditioner: full monomial inventory, cokernel,
shear audit, and the first two h-adic levels. Do not run `std` on a 150MB file
first. If the preconditioner leaves fewer than about 40 nonlinear variables,
then a modular GB is plausible; if it leaves the same 100+ variable core as
D=108, the right product is a theorem about the zero-slope family, not a bigger
timeout.

Typed claim Q2.2: N3/N4 are closed; N1/N2 are not closed by a simple source
read.

N3/N4 are free gauges. The gauge report gives the exact group elements:
translations `(x,y)->(x+c,y+b)` kill both constant centres, and an eigenbasis
2-torus with `gamma^119 delta^44=1` normalizes the `D_1` scale
(`xmodel/g9966-gauge-gaps-grok46-20260903.md:8-24`,
`xmodel/g9966-gauge-gaps-grok46-20260903.md:177-200`,
`xmodel/g9966-gauge-gaps-grok46-20260903.md:259-336`). After that, Theorem 8.1
for split branches carries only N1, N2, and not-addressed case (A)
(`xmodel/g9966-gauge-gaps-grok46-20260903.md:404-456`).

N1 is different in kind. Moh p.202 says the computer program produced four
exceptions satisfying the necessary numerical restrictions, including the
printed `(99,66)` row; but the dossier's verbatim implementation of printed
conditions (1)-(13) leaves eight `(99,66)` `V`-assignments, not one
(`xmodel/g9966-chart-necessity-opus5-20260903.md:98-129`). I extracted Moh
pp.200-202 locally: the text asserts a program and an exception table, then says
the treatment is in Appendix II; it does not print the extra predicate that
removes the seven other `(99,66)` assignments. So a source read of Moh
sections 5-6 is not enough unless it reconstructs that hidden program.

N2 is closer to closable. Xu section 8 explicitly identifies the three claims
needed for Moh's p.209 statement, says he could not find support for them in
Moh, and uses two tools: `den(delta) <= u_s=3` and equation (7.1). For
`delta != 2,5/2`, Xu writes in one sentence that the ODE forces
`q=p^13(pi-c)` and then cannot hold; for `delta=5/2`, he gets an open
three-root case (`refs/xu2016_intersection_numbers_split_minor_roots_arxiv1604.07683v4.pdf`,
section 8; see also `xmodel/g9966-chart-necessity-opus5-20260903.md:728-761`).
This is not a citation-grade proof as printed. It is a finite reconstruction
problem.

Cheapest N2 closure:

1. Prove the denominator bound from Moh p.201 (8)-(11) in the exact `(99,66)`
   notation. This is a one-page Galois denominator lemma, not a CAS job.
2. Run four finite face-ODE computations for
   `delta in {4/3,3/2,5/3,7/3}`. The dossier already says this is the remedy
   (`xmodel/g9966-chart-necessity-opus5-20260903.md:829-837`), and its
   `face_odes.py` completed in 3 s (`xmodel/g9966-chart-necessity-opus5-20260903.md:909-917`).

Cheapest N1 closure:

1. Re-run the `(99,66)` census under each plausible reading of Moh's p.201
   restrictions (especially (10)-(13) and Proposition 5.6 exclusions), and
   require that the printed four rows remain while the seven extra `(99,66)`
   rows disappear for a source-stated reason.
2. If no printed reason exists, do not call N1 literature-closed. Instead open a
   seven-row mini-census: route each extra assignment to split/no-split and see
   whether the current engines dispose of them. This is paper debt, not a
   blocker for Theorem 8.1 with the `(99,66)` row as a hypothesis.

Direct answer to the packet: Theorem 8.1 for the split branches can become
unconditional as a **row theorem** by closing N2; N1 is needed only to upgrade
from "the printed `(99,66)` row" to "all degree `(99,66)` Keller pairs". Case
(A) remains outside the split theorem either way.

## 4. Q3 MINOR-EMPTY

Typed claim Q3.1: the disc route is dead exactly as the packet says, and the
stronger replacement is not currently launchable as a theorem.

The discriminant nonzero condition is the wrong sign for contradiction. The
fresh lane states the linear algebra cleanly: a Vandermonde/confluent
Vandermonde determinant becomes invertible on the distinct-root locus, so the
packet interpolation block is solvable; emptiness requires an additional Schur
row to generate `1`
(`xmodel/minor-empty-disc-sol56-20260903.md:370-405`). The same report records
the corrected status:

```text
OPEN[UNIVERSAL-MINOR-EMPTY-DISCRIMINANT]
REFUTED[VANDERMONDE/DISCRIMINANT-INVERTIBILITY ALONE IMPLIES INCONSISTENCY]
EXACT-OBSTRUCTION[NO UNIFORM AUGMENTED-RANK / SCHUR-COMPATIBILITY THEOREM]
```

(`xmodel/minor-empty-disc-sol56-20260903.md:7-48`). The terminal constants
`6264` and `64` are branch-specific pole/Jacobian Schur residues, not
discriminant values (`xmodel/minor-empty-disc-sol56-20260903.md:31-38`).

Typed claim Q3.2: there is still one theorem route in principle:

```text
H1-AUGMENTED-FITTING:
  for every fully specified principal-minor split datum, construct the ordered
  root-cover joint presentation over A_lambda[disc^-1] and prove that the
  augmented residual Schur/Fitting ideal is the unit ideal.
```

This is type-correct and would imply MINOR-EMPTY by clearing Vandermonde
denominators (`xmodel/minor-empty-disc-sol56-20260903.md:707-748`). But it is
not close enough to be the flagship. The obstruction-core report gives one
filtered block-elimination schema, not one stable final minor: the first
nonzero obstruction appears on different filtration pages for D=108 and the two
`(99,66)` split branches
(`xmodel/two-place-obstruction-core-sol56-20260903.md:6-52`), and the final
Schur scalar depends on branch partition, face normalization, and low-tower
jets (`xmodel/two-place-obstruction-core-sol56-20260903.md:648-663`). That is a
red flag against any theorem stated only in `(d_s,u_s,V_s,delta,weights)`.

Typed claim Q3.3: commit to the census, but change the compute stack before
running 166 raw full systems.

The corrected full-basis systems are too large for naive local GB:

```text
(25,15) [3]        598 equations, 141 unknowns, ~58 MB, timeout 1800 s
(25,15) [2+1]      ~128 MB system emitted
(25,15) [1+1+1]    ~256 MB system emitted
D=108 no-split     472 equations, 110 unknowns, ~182 MB, timeout 1800 s
```

The exact timeout and size evidence is in
`xmodel/order-basis-full-gpt55-20260903.md:147-185`,
`xmodel/order-basis-full-gpt55-20260903.md:197-230`, and
`xmodel/order-basis-full-gpt55-20260903.md:303-317`.

Minimal compute change:

1. Add a linear/affine Schur preconditioner before Singular. Order rows by
   filtration page and variables by coefficient block; pivot all rational-unit
   or monomial-in-top-face linear blocks over the finite field first; emit the
   residual nonlinear core. This is the same algebraic shape as the
   obstruction-core map `S = D - C*A^(-1)*B`
   (`xmodel/two-place-obstruction-core-sol56-20260903.md:542-590`).
2. Replace the single Rabinowitsch GB with staged saturation: compute the
   unsaturated residual core, then colon/saturate by partition factors
   `c`, slope differences, and face discriminants after linear elimination.
   This keeps the nonzero conditions but avoids injecting `T*(c*Omega)-1` into
   a 100-variable raw ring before the easy pivots are gone.
3. Use modular Las Vegas triage, not modular promotion by wish. First primes are
   for rank profile, dimension hints, and unit-certificate shape. Promotion of
   inhomogeneous emptiness over `Q` needs an exact certificate or CRT-recovered
   rational Bezout identity. The campaign already has an msolve/Q-cert hazard in
   the history, so do not promote a first-prime `[1]` without exact replay.
4. Emit block-weighted orders rather than one monolithic `dp/slimgb` file:
   coefficient blocks eliminated by page first, then surviving nonlinear
   branch parameters under a weighted degree order matching the Theorem 1.2
   inequalities. The current `slimgb` timeouts show the parser/GB is chewing on
   emitted bulk, not exploiting the row filtration.

Price:

| scenario | expected cost | promotion value |
|---|---:|---|
| raw current engine on 166 groups | at least 166 * 1800 s first-prime attempts, with no guarantee of a verdict | poor; repeats known timeout class |
| Schur preconditioner on two controls `(25,15)[3]`, D108 `[1]` | one lane; <10 min for rank/pivot extraction, then one 300-900 s modular residual attempt | decisive go/no-go for census engineering |
| full census after residual cores are <40 nonlinear variables | 1-2 lane-days on local/fleet mixed primes, exact certificates only for deaths | credible |
| full census if residual cores remain >80 variables | do not launch locally; either fleet with checkpointing or structural theorem required | credible stop |

Counterexample/falsification attack: search for a survivor in the corrected
full-basis residual core after preconditioning. A survivor with controls would
not be a Keller pair, but it would refute the over-optimistic "corrected engine
will simply empty the batch" assumption and force a new row family. That is
more informative than another timeout.

## 5. Q4 Finish And Reduction

Typed claim Q4.1: the single most valuable next object is a route-to-state
reduction certificate, not another foreground GB file.

The two-half program is a plausible architecture, but not yet a complete
reduction of plane JC2 in the form needed for a proof. The safe theorem should
say:

```text
For every Moh-admissible state in the normalized minimal-degree plane Keller
problem, exactly one of the following typed routes is supplied:

H1: a typed first principal-minor split with ordered-root cover, partition,
    localization, and joint necessary ideal I_joint;

H2: a no-split certificate giving the required delta* floor, a sourced
    Prop 6.3/6.4 substitution map, and a sourced support/order chart for the
    descended monomial-Jacobian datum.
```

Without this route-to-state theorem, "H1 or H2" is only a slogan over a census.
There is no mathematical third case after "principal minor roots split before
the threshold" is defined as a first-separation event and its complement is
defined as no such split. But there is a very real third **unlicensed interface
class**:

```text
u_s > 1, no split in the tested window, delta_1'=0 or support-degenerate
after Prop 6.3, ordinary order chart not sourced or not decidable.
```

This class contains the live D=108 no-split row and the `(99,66)` case (A)
analogue. It is formally in H2, but not in the clean K16 receiver. The packet
is right to call both halves compute-bound; it is wrong if it treats the
H1/H2 split as already paper-grade complete.

Typed claim Q4.2: reranking of bottlenecks.

1. `ROUTE-TO-STATE-COMPLETENESS`: prove the typed H1/H2 interface on the
   current live clients `(99,66)`, D=108, row 2515, and the leftover two-point
   list. This is prerequisite to any all-degree claim.
2. `K16-TAIL-INITIAL`: the best narrow structural theorem object. If solved, it
   promotes the only known cofinal H2 prototype and provides a template for
   receiver regular-sequence proofs.
3. `ZERO-SLOPE-FAMILY`: decide the case-A/D108 no-split family structurally or
   by a preconditioned residual core.
4. `H1-AUGMENTED-FITTING-PREFLIGHT`: keep as a small theorem probe, but do not
   let it delay the census unless it passes on the known clients and at least
   one nontrivial N1 leftover.
5. Corrected H1/H2 census batch: launch only after the preconditioner changes
   the complexity class.

Strongest proof attack: K16 tail initial-ideal/hsop theorem.

Strongest falsification attack: a corrected full-basis survivor in the
zero-slope family or a sourced route-to-state row that is neither covered by
H1's joint ideal nor by H2's descended support chart. Either would challenge
the current finish plan more sharply than a failed theorem attempt.

## 6. Idea Cards

### Card 1: K16 Tail Initial-Ideal Theorem

Label: `NEW`, with direct dependencies on promoted K16 spine/top-tail results.

Mechanism: prove the tail hsop by an indexed homogeneous initial-ideal theorem
in the original variables `(b4,q2_0,...,q(t-1)_0,b3)`. Do not eliminate `b3`.
Use the closed TOPTAIL quadratic form only to identify triangular leading
pieces, then prove that the resulting staircase has length
`2*binom(3t+1,t-1)`.

Dependencies:

```text
17(vvvv): corrected tail regular at t=3..6, literal top-row proposal impossible.
17(wwww): TOPTAIL and Lemma SQUARE.
17(mmmmm): guided Hilbert method validated, fixed-t frontier not moved.
Moh p.208 reading: K16 is an approximate-root tower, not an arbitrary ring.
```

Cheapest discriminator:

1. Extract leading monomial sets from the accepted `t=5,6` two-fibre tail
   outputs and the accepted `t=7,b0` tail output.
2. Search for an indexed Ferrers/staircase rule whose count is `L_t`.
3. Run modular Macaulay rank by weighted degree for `t=7,b1`, stopping at the
   first degree where the predicted Hilbert function fails.

Outcomes:

| outcome | interpretation |
|---|---|
| same staircase rule fits `t=5,6` both fibres and `t=7,b0`; rank windows pass for `t=7,b1` | promote to proof-writing lane; no more fixed-t GB needed |
| staircase fits completed fibres but rank fails for `t=7,b1` | hsop identity likely false or fibre-dependent; pivot to Z-EMPTY/source receiver |
| no stable staircase after extracting completed bases | stop this route; the Hilbert identity may still hold but not by a simple monomial initial ideal |

Stop condition: no stable initial-staircase rule after `t=5,6,7b0`, or a
rank-profile contradiction in `t=7,b1`.

Expected information gain: high. It attacks the one K16 structural object with
a precise invariant and avoids the exhausted GB bottleneck.

### Card 2: Zero-Slope Family And Case (A)

Label: `NEW/CONNECTED`. It connects `(99,66)` case (A) and D=108 no-split.

Mechanism: isolate the family

```text
ZK(K): (3K,2K; M2=3K-6, V2=K-1; u'=1; k=4; delta1'=0)
```

and prove either an adapted full D1 normal form or a structural obstruction
that is uniform in `K`. Case (A) is `K=9`; D=108 no-split is `K=8`. The old
`-c` kill is explicitly forbidden; all `x`-directions required by the
zero-slope inequality must be present.

Dependencies:

```text
17(fffff): D108 no-split descent sourced, ordinary support not sourced.
17(nnnnn): corrected zero-slope full basis, no standalone -c, timeout.
17(jjjjj): (99,66) case (A) is open and on the same full-order-basis path.
```

Cheapest discriminator:

1. Emit case (A) metadata and builders with `order_basis_full.py`.
2. Before any GB, compute the linear D1/Jacobian Schur preconditioner for
   `K=8` and `K=9`: full monomial inventory, coefficient-space cokernel, shear
   audit, first two h-adic levels.
3. Compare the residual nonlinear variable counts and the target-row image.

Outcomes:

| outcome | interpretation |
|---|---|
| `K=8,9` share a small residual core or identical rank defect | launch uniform zero-slope theorem lane |
| case (A) collapses but D108 does not | case (A) can be closed separately; no family theorem yet |
| both residual cores remain >80 nonlinear variables | do not run raw local GB; move to fleet/checkpointing or proof only |
| a controlled survivor appears | H2 receiver chart missing rows or theorem (T) false in this class; immediate round trigger |

Stop condition: residual core not reduced by at least a factor of two relative
to the current 110-variable/472-row D108 and 141-variable/598-row row-2515
objects.

Expected information gain: very high. It hits two live flagship corrections and
tests whether the corrected engine can ever scale.

### Card 3: H1 Augmented Fitting Preflight

Label: `NEW`, but deliberately preflight-sized.

Mechanism: build the ordered-root-cover Schur/Fitting object for fixed
principal-minor split data. The theorem candidate is not "disc is nonzero";
it is "after localizing by disc, the augmented residual Schur/Fitting ideal is
unit", equivalently a power of the discriminant lies in the unlocalized joint
ideal.

Dependencies:

```text
17(lllll): one filtered obstruction map, no universal final minor.
17(ooooo): discriminant invertibility alone refuted; exact missing theorem typed.
17(iiiii): ladder law and availability criterion still useful as tier-0 screens.
17(jjjjj): split branch (99,66) necessity now rests only on N1/N2 plus case A outside.
```

Cheapest discriminator:

1. Implement the Schur preflight on exactly five rows: `(99,66) delta=2`,
   `(99,66) delta=5/2`, D108 delta=3, and two of the seven N1 extra
   `(99,66)` assignments.
2. For each, report page of first obstruction, residual variables, whether
   `disc(p_red)` lies in the incidence ideal, and whether the augmented Schur
   ideal is unit over one good prime.
3. Reject any formula that depends only on `(d_s,u_s,V_s,delta,weights)` unless
   it correctly distinguishes the three known pages.

Outcomes:

| outcome | interpretation |
|---|---|
| same augmented-Fitting construction works on all five, with page data explained by typed branch parameters | promote theorem route to a real lane |
| works only on the three known kills | theorem route is overfit; census remains the H1 plan |
| fails on a known kill | implementation or source typing wrong; do not launch census until fixed |
| an N1 extra row survives preflight | N1 is not a literature footnote; route it explicitly |

Stop condition: no successful two-extra-row preflight, or no typed explanation
of why obstruction page changes.

Expected information gain: medium-high but bounded. It can resurrect an H1
theorem route only if it survives hostile controls; otherwise it prices the
census honestly.

## 7. First Lane

Single first lane after the current running K16 receipt resolves:

```text
route-to-state-core-gpt55-20260904
```

Objective: produce a machine-readable and human-readable route-to-state
certificate for four clients:

```text
(99,66) B/C split branches
(99,66) A unsplit branch
D=108 split/no-split
(25,15;21;2;k=2) descended row
```

Required fields per route:

```text
skeleton id and source page
principal-minor first-separation window and denominator proof status
split partition and ordered-root cover, or no-split delta* floor
Prop 6.3/6.4 applicability and substitution map
descended support/order-chart source status
current certificate or OPEN label
```

Why this, not another solver job: the K16 all-t lane is already running and
must not be duplicated blindly. The biggest proof risk now is that solved
pieces do not compose into a reduction because a route is only inferred from
labels. This lane is small enough to finish from the named reports and source
pages, and it tells the campaign whether the next mathematical lane should be
K16 initial ideal, zero-slope family, or H1 preconditioned census.

Cheapest test: the lane passes only if it can fill all fields for the four
clients without using a prior validator result as a source theorem. A missing
field becomes a typed OPEN with quantity and cheapest test, not prose.

## 8. Running Lane Receipt

Lane: `k16-hsop-length-allt-opus5-20260903`.

Receipt-only recommendation: `CONTINUE` to final status. The receipt available
to me shows `initial_status=RUNNING`, `start_utc=2026-09-04T11:15:55Z`, and no
`final_status`; therefore I did not open its `.md`.

Interpretation rules for harvest:

| receipt/report outcome after final status | recommendation |
|---|---|
| proves an indexed hsop/regular-sequence theorem | promote to hostile review immediately; make K16 the proof-writing target |
| gives only more fixed-t data or another timeout | redesign into Card 1 initial-staircase/rank-profile lane; stop fixed-t GB escalation |
| finds a structural obstruction to hsop | pivot to K16-Z-TYPE-RECEIVER and zero-slope family; do not read it as a failure of theorem (T) without route typing |

## 9. Systems Upgrade

UPGRADE: add a receipt-indexed live-lane and blind-corpus preflight script.

Smallest useful implementation:

```text
ops/round_preflight.py --round 20260904T1200Z --report xmodel/ideation-...
```

Required checks:

1. list every `xmodel/*.run.v2` with `initial_status` but no `final_status`;
2. print the exact `.md` filenames that must not be opened;
3. verify the current report has no `<!-- BODY-END -->` before the final seal;
4. verify there is an `OPENS RAISED` section and a `COLLISIONS` section after
   `ops/open_collision.py` runs;
5. refuse to scan same-round blind submissions except the charged packet and
   the current report.

Smallest test: run it on this round and confirm it flags
`k16-hsop-length-allt-opus5-20260903.md` as forbidden while allowing the named
20260903 reports and the 20260904T0000Z synthesis. This is not mathematical
software; it prevents accidental blind-boundary and seal-contract violations.

## OPENS RAISED

`OPEN[K16-TAIL-INITIAL-IDEAL]` — bounded quantity: decide whether there is an
indexed weight-preserving initial monomial/staircase theorem for
`J_t^tail=<T_{t,t},...,T_{t,2t-1}>` with quotient length
`L_t=2*binom(3t+1,t-1)` for all `t>=3`; cheapest test: extract leading
staircases from completed `t=5,6` two-fibre and `t=7,b0` outputs, then run
degree-wise modular rank windows for `t=7,b1` instead of a full GB.

`OPEN[ZERO-SLOPE-ORDER-NORMAL-FORM]` — bounded quantity: decide the family
`(3K,2K; M2=3K-6,V2=K-1;k=4,delta1'=0)` at least for `K=8,9`, including full
D1 monomial inventory and shear audit, without the old `-c` artifact; cheapest
test: emit case (A) with `order_basis_full.py` and run only the linear
D1/Jacobian Schur preconditioner for `K=8,9`, reporting residual nonlinear
variable count and target-row image.

`OPEN[H1-AUGMENTED-FITTING-PREFLIGHT]` — bounded quantity: decide whether the
fixed-partition principal-minor joint chart has a unit augmented residual
Schur/Fitting ideal over the ordered-root cover localized at `disc(p_red)` on
the three known kills plus two N1 extra `(99,66)` assignments; cheapest test:
one modular Schur preflight reporting obstruction page, residual variables,
`disc in I_inc?`, and unit/nonunit augmented ideal for those five rows.

`OPEN[ROUTE-TO-STATE-COMPLETENESS]` — bounded quantity: decide whether the
current four live clients have fully sourced H1/H2 route records: split window,
denominator proof, ordered-root cover or no-split `delta*` floor, Prop 6.3/6.4
map, descendant support chart, and current certificate/OPEN; cheapest test:
one source/report audit lane over `(99,66)` A/B/C, D108, and row 2515 with a
machine-readable manifest.

No exit-price assertion is made in this report, so no `charge_basis` line is
emitted.

## 11. Collision Scan

Command:

```bash
python3 ops/open_collision.py --root . xmodel/ideation-20260904T1200Z-gpt55.md
```

## COLLISIONS

status: CANDIDATES

### OPEN[K16-TAIL-INITIAL-IDEAL]

- `AUDIT.md:17603` — ## INTEGRATION #17 DELTA (nnnn) (2026-09-03T22:19Z, PROVED-HERE structural results + a proved obstruction, producer Opus): THE q-DIRECTIONS OF THE K = 16 TERMINAL CONE — Lemma CHAIN: dim I_{t,+} = 0 iff the residual variables admit an or...
- `AUDIT.md:17676` — ### 17(vvvv) (PROVED-HERE + refutation of the literal Proposal A, producer Sol): on the K = 16 terminal cone, the literal "t + 2 highest-weight rows form a regular sequence" is IMPOSSIBLE for every t (weight bookkeeping: the Hilbert seri...
- `AUDIT.md:17678` — ### 17(wwww) (PROVED-HERE, producer Fable): Theorem TOPTAIL (uniform in t ≥ 3): on b₄ = 1 the rows T_{t,2t−1−r}, r = 0..t−1, are quadratics Q_r = a_r b₃² + b_r b₃ + c_r with a_r, b_r, c_r the b₄ = 1 images of weighted forms of weights r,...
- `AUDIT.md:17680` — ### 17(xxxx) (gate, Grok): Lemma CHAIN (fibrewise over A_t → K) CONFIRMED; the product-algebra reading typed (must not invert zerodivisors); "sub-chart = TAIL, (C0) not a sub-chart" CONFIRMED; Theorem Q-PLANE (3.6) t ≥ 6 CONFIRMED with A...
- `notes.md:21007` — ## 2026-09-03T22:10Z EVENT — `k16-b4-axis-sol56` sealed (20KB): b₄-axis indexed formula (all t), units t = 2..7; the axis route cannot give b₄ ∈ √I_{t,+} — the global b₄ ≠ 0 chart is the missing statement; the axis/subchart programme is ...
- `notes.md:21011` — ## 2026-09-03T22:19Z EVENT — `k16-subchart-q-opus5` sealed (36KB): Lemma CHAIN; Theorem Q-PLANE (eight units, all t ≥ 3); UNIQUE-POWER; the tail of the chain proved (R = 1..5); the initial-form upgrade FAILS (proved); the residual is the...

- `OPEN[ZERO-SLOPE-ORDER-NORMAL-FORM]` (report:640): NONE

- `OPEN[H1-AUGMENTED-FITTING-PREFLIGHT]` (report:647): NONE

### OPEN[ROUTE-TO-STATE-COMPLETENESS]

- `AUDIT.md:17704` — ## INTEGRATION #17 DELTA (hhhhh) (2026-09-04T09:14Z, PROVED-HERE dossier, producer Opus, the paper-grade necessity half of the (99,66) verdict): THEOREM 8.1 (conditional): over k alg. closed char 0, a Keller pair (F,G) with deg = (99,66)...
- `AUDIT.md:17706` — ## INTEGRATION #17 DELTA (iiiii) (2026-09-04T09:27Z, Card B, producer Opus; REFUTED as posed + two NEW uniform structures for the (H1) flagship): REFUTED[RESIDUE-VALUE-FORMULA]: "the u_s ≥ 2 kill residue is a rational function of the spl...
- `AUDIT.md:17710` — ## INTEGRATION #17 DELTA (jjjjj) (2026-09-04T09:37Z, PROVED-HERE gauge argument, producer Grok, closes 17(hhhhh) N3, N4): BOTH new necessity gaps of the (99,66) dossier are FREE GAUGES. DISCHARGED[N3] GAP[TWO-CENTRE-NORMALISATION]: the s...
- `AUDIT.md:17712` — ## QUALIFICATION[17(tttt) — the unsplit configuration (A) is NOT solidly killed] (2026-09-04T09:37Z, coordinator, from the necessity dossier §0.4 + 17(lll) reconciliation): the (99,66) row has a CONFIGURATION TRICHOTOMY inside the SAME s...
- `AUDIT.md:17714` — ## INTEGRATION #17 DELTA (kkkkk) (2026-09-04T10:03Z, Card C, producer Fable; a structural identity that re-targets the whole K16 (V0) question): DEP (Fable's own round §1.2 conjecture) is REFUTED at t = 2 (exact, PROVED-HERE): on the b₃-...
- `xmodel/grok-census-review.md:129` — | 1 | 11-A co-scaled CLASH `(1,2), M=3, exterior` | CLASH-DEAD | **Yes inside perimeter.** Empty prefix + CAP-DEN + 11A-RES are route-shape-uniform provided no chain inserts a gap above `7/16` into `(g_X, 5/2)`. That proviso is exactly F...
- `xmodel/grok-normalform-review.md:162` — **Completeness without the quotient.** Store the full exact word, the full exact characteristic, and the live coefficient variables. Do not apply `NEUTRAL_STAR`, do not fold cylinders, do not promote a lower bound to an exact ledger. The...
- `xmodel/grok-reduction-review.md:166` — | **4** — td-7 as a *panel* | **Mostly yes, already built.** Unique entry + P3 + §11a + tower uniformity. Remaining hole is “beyond-filed-closure = under-enumeration” (`TOWER-UNIFORM.md:52-53`) plus the Sigray/H5a/E5/Prop. 4.2 trust set....
- `xmodel/ideation-20260829T0002Z-grok46.md:102` — 1. **Full-configuration landing, including off-axis and post-jump `M>=2`, then `RPMC(C)`, then an independent type menu, then a cofinal `td` ceiling (Avenue 2).** This is still the only typed path that can prove JC2. Intrinsic L3/L4/L5, ...
- `xmodel/ideation-20260902T1608Z-gpt55.md:72` — | reducible branch | raise | It is no worse after #16 and is now relatively more valuable because the H2 boundary-ceiling route has lost priority. |
- `xmodel/ideation-20260902T1608Z-opus5.md:366` — | **(f)** A theorem that the boundary route is one-directional | **REFUTED as a program** | `(ORTHO)` is a boundary-tree functional that bounds `N` below. The correct residual negative statement is much narrower and I state it: **`UPPER-...
- `xmodel/ideation-20260902T1608Z-opus5.raw.md:366` — | **(f)** A theorem that the boundary route is one-directional | **REFUTED as a program** | `(ORTHO)` is a boundary-tree functional that bounds `N` below. The correct residual negative statement is much narrower and I state it: **`UPPER-...
- `xmodel/landing-ledger-primary-research-fable5-hostile-review-grok46-20260829.md:220` — The finite sub-boundary residue (at most one further priced unit from the slack-1 route; none from the equality route) is a real compiler obligation and is in scope for `m=2` (no later merge). This is the one place the packet strictly ex...
- `xmodel/sol-probability.md:21` — | **4. Six-cell `td=7` coefficient gluing** | **32%** significant progress; **6%** full `td=7` closure | Four cells are singleton-route pilots, but the only audited next-tier cell survives, the gluing equations are not yet emitted, and `...
<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `38415`.
- Body SHA-256:
  `36d9c94077901576baef74f0f4805dbef6aadcb5e78120adea21c2cc3f176224`.
- Frozen basis: `bb2a1a78491a2c85b4c11ee75803f47f1a43d60f`.
