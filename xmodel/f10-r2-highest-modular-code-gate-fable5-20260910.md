# FIRST Fable 5.1 gate: fixed 89/0 Route P highest special-projective certificate code

status=UNSEALED (no Seal, no charge_basis authored; adapter seals)
lane=f10-r2-highest-modular-code-gate-fable5-20260910
gate_model=claude-fable-5-1
first_action_utc=2026-09-10T15:30:19Z (hash command); stop = earlier of ROOT launch+18 min (launch precedes 15:30:19Z, so no later than 15:48:19Z) and 15:51:00Z USER TERM (KILL 15:51:05Z); final-3-min reserve from 15:45:19Z; never reset
mode=STATIC; manual math/text/hash only; ZERO subprocess/import/AST/syntax/compile/test/CAS; tools date, ls, wc, sha256sum, cat, grep, apply_patch only
owned=xmodel/f10-r2-highest-modular-code-gate-fable5-20260910.md, box/f10-r2-highest-modular-code-gate-fable5-20260910/ (both absent at 15:30:19Z; unsealed skeleton first, then body)
charged_input=box/f10-r2-highest-modular-code-astra-20260910/entry.py
charged_input=box/f10-r2-highest-modular-code-astra-20260910/produce.py
charged_input=box/f10-r2-highest-modular-code-astra-20260910/check.py
charged_input=box/f10-r2-highest-modular-code-astra-20260910/CONTRACT.md
charged_input=xmodel/f10-r2-highest-modular-code-astra-20260910.md
charged_input=box/f10-r2-reconstruction-code-astra-20260910/arithmetic.py
charged_input=box/f10-r2-reconstruction-code-astra-20260910/check_arithmetic.py
charged_input=xmodel/f10-r2-modular-properness-route-astra-20260910.md
charged_input=xmodel/f10-r2-modular-properness-gate-fable5-20260910.md
charged_input=xmodel/f10-r2-highest-bezout-code-gate-fable5-20260910.md

## 0. Custody, read scope, disclosure

All 10 snapshots in /tmp/jc2-lane.I88xmr/inputs (distinct basenames) were hashed by sha256sum at 15:30:19Z BEFORE any body read; all 10 digests equal the charged pins byte for byte (rows copied from the sha256sum output into box/input_custody.md and re-grepped against a fresh sha256sum, each found exactly once; this body carries no hex digest). Both owned targets were absent at 15:30:19Z. All 10 were then read WHOLE in parallel single cat -n reads (line counts in the custody file); no read was clipped, so no recovery read was needed. The baseline packet was not opened, hashed or parsed; its pin is a future literal only. No provenance, ledger, corpus, Git, network, process, other lane, live file, artifact or worker was accessed; nothing was executed, imported, parsed, compiled or computed beyond hand reasoning on the frozen text. The source remains UNEXECUTED; p=89, root 0 are prospective only.

Disclosure: this is a different-model FIRST review of Astra's frozen source. Two charged imports, the properness gate and the exact highest Bezout code gate, were authored by Fable 5.1 in earlier sessions; with Astra's Route P/F route report they are consumed here as accepted premises at their exact stated scope (shapes, F/G degree<=5, denominator-free identities, proper specialization, Route P boundary sufficiency, FIELD import for B=Q[Z]/P7) and are not re-proved. The baseline dac655a7... is checked-but-not-promoted pending its separate 6/10 controls and is not read here.

## 1. Verdicts

| item | verdict |
|---|---|
| A literal P7 both sides, 89 prime, 0 simple root, all denominators incl. positive-z, 7-basis before projection, 24 slots by support, local order at (89,Z), decline is INCONCLUSIVE | CONFIRMED |
| B independent H5/H6/H7 extraction, whole F/G with both degree-5 G terms, Q=0 kept, same-special alpha/beta/gamma by unique X1 exponent, Route P exclusion | CONFIRMED |
| C producer Euclid<=8 and guards, checker independence and 11 positions, entry.py metadata only, no float/zero-denominator/lost-term hazard | CONFIRMED; no source mismatch found |
| D strict wire, pins, exclusive 0444 publication, caps, false-positive versus rejection | CONFIRMED; one liveness-only GAP retained (schema/encoding of the unread baseline) |
| E barrier order, 17-argv, sibling import path, bindings not authentication, ROOT duties | CONFIRMED as bindings; no new source needed |
| F receipts/exit predicates, control design, derived traceback, scope, remaining execution obligations | CONFIRMED; frame count derived = 3 (check.py:168, :158, :151) |

Smallest blocking correction: none. REFUTED: nothing. Retained non-blocking notes: D (liveness GAP), F (producer exit-2 receipt carries no reason; fresh receipt path required for the control).

## A. Place, modulus, integrality, order

CONFIRMED. Literal P7: produce.py:39-47 forms 2KL^2 + (-140 tau)(1+tau-6Z)KL dL + 245 gamma dL^2 with the original arithmetic.py qa/qs/qm (:12-27, P7-independent, so the unset module P7=None is never touched); check.py:45-51 forms the same expression with check_arithmetic plus/times (:15-28) and its own Rational. Degrees 6, 6, 7 give the unique degree-7 term 245*(-120 tau)*12^2, checked as -245*120*144*tau with len 8 at produce.py:48 / check.py:52 BEFORE the division at :49 / :53, so 1/literal[-1] is a nonzero Fraction, never a float or zero divisor. Both compare all eight packet coefficients (produce.py:59-61, check.py:63-64) and the checker also compares the certificate modulus to its own (:137). Irreducibility is nowhere computed; CONTRACT.md:7 names it as the accepted FIELD import. Primality: produce.py:9-13 trial-divides 2..9 (9^2=81<=89<100), check.py:9 tests all d in 2..9; both are `sufficient`, so a failure is typed. Root: at root 0 the Horner loops at produce.py:63-69 reduce to value=a0, derivative=1*a1, and check.py:65-68 tests a0=0, a1!=0 directly; both first reduce all eight coefficients through a unit-checked inverse (produce.py:15-20 Fermat with readback, check.py:11-23 integer extended Euclid, <=16 steps, readback). Integrality: every term's seven coordinates pass rat()/rational() and are reduced through the unit-checked inverse at produce.py:84,:90 and check.py:81,:89 BEFORE the z filter at produce.py:94 / check.py:91, so positive-z denominators and coordinates multiplied by 0^i are all checked. Basis: interface variables/weights/basis '0'..'6'/base checked at produce.py:55-58, check.py:59-62; per term list shapes, exponent strings '0'..'16', e[4]=e[5]=0, exact weight, nonzero vector, strictly increasing tuples (produce.py:78-88, check.py:76-87), all before projection. Slot count: own enumeration of weight-h monomials in (X1,X2,X3,z) with weights (1,2,3,5) gives z-free 5/7/8 and z-bearing 1/1/2, total 24; the code never assumes it, the 4096 row cap is a refusal bound and strict ordering plus weight makes 24 the consequence. Order: with P7 monic and 89-integral, O=Z_(89)[Z]/P7 is free of rank 7 and, given the FIELD import, a domain with fraction field B; O/(89,Z)=F_89[Z]/(P7bar,Z)=F_89 because P7bar(0)=0, and the simple root makes the localization a DVR (properness gate D). Every reduced coefficient lies in O since its dense denominators are 89-units, and its residue is coordinate 0 mod 89, exactly what both Horner/pow loops compute. No map B->F_89, no factor of P7. Decline: `sufficient` raises Inconclusive (entry.py:12-23), a ValueError subclass; produce.main:167-169 turns any ValueError into INCONCLUSIVE_NO_CERTIFICATE exit 2; check.main:159-162 turns Inconclusive into INCONCLUSIVE_FIXED_PLACE exit 2. Neither is a source verdict.

## B. Extraction, eliminants, boundary, Route P

CONFIRMED. Own shape derivation at z=0: a+2b+3c=5 gives c=0:b<=2, c=1:b<=1; =6 gives (3,1,0); =7 gives (3,2,0). Given h, b, c the X1 exponent is unique, so the key (e[1],e[2]) at produce.py:95 and box[j][i] at check.py:96 never merge two terms, and the explicit shape checks (produce.py:98-99, check.py:93-95) are implied by the weight check. part() (produce.py:124-125) reads i<=d for v^j and the shape check guarantees nothing lies outside, so no term is lost; the checker's 3x4 box reads every column. F=Q^2B3-PQB1+B0P^2 at produce.py:130 and G=Q^2C3-PQC2+C1P^2 at :131; check.py:121-128 rebuilds both from its own P,Q and row[0..2], with the PQC2 degree-5 term retained on both sides and c<->F, d<->G order matched (produce.py:145,:156; check.py:147). The denominator-free identities are accepted and hold over any commutative ring, hence over F_89; at Q=0 they read F=B0P^2, G=C1P^2 and remain in the chart ideal, so cF+dG=1 excludes the whole special affine chart including Q=0 without any Q inverse (none exists in the code). Boundary: produce.py:100-101 reads tables[0][(1,1)], tables[1][(3,0)], tables[1][(0,2)], whose implied X1 exponents are 5-2-3=0, 6-6=0, 6-6=0, i.e. the unique X1^0 monomials X2X3, X2^3, X3^2; check.py:98 reads forms[0][1][1], forms[1][0][3], forms[1][2][0], the same three slots with its own index convention box[X3-exp][X2-exp]. Both are computed from the same reduced tables at the same place; the certificate has no boundary key (check.py:131-132), so nothing is imported. Over kbar: alpha X2X3=0 then beta X2^3+gamma X3^2=0 kills the remaining coordinate; weighted scaling by 1/X1 needs no roots and moves every X1!=0 point to the chart. With properness (accepted) the generic fibre over B, hence every complex embedding, is empty: Route P, no Route F leader, no f5/g5. Drops: trim/clean strip zeros; F=0 with constant G gives c=[], d=[G^-1] and passes both readbacks (produce.py:156, check.py:148-151); F=G=0 or nonconstant gcd fails produce.py:152 as Inconclusive; a nonunit denominator is Inconclusive; none of these is a point.

## C. Algorithms, independence, hazards

CONFIRMED. Euclid (produce.py:134-151): divide() inverts the trimmed divisor's leader, so the leading term cancels exactly and len(rem) strictly drops; the invariant r0=c0F+d0G, r1=c1F+d1G holds from the initial (F,G,[1],[],[],[1]) through each rotation; from degrees <=5 the chain (5,5)->(5,4)->...->(1,0)->(0,[]) is 6 divisions, plus one swap step when deg F<deg G, so 7<=8 (:149). The final gcd must be a nonzero constant (:152, typed), c,d are scaled by its inverse, capped at length 6 (:155) and replayed (:156). Intermediate trim cap 17 (:107) is far above the maximal length 6 of Q^2B3 and the cofactor products. Checker: imports only entry and the pinned check_arithmetic (entry.py:9 = charged hash; producer pin entry.py:8 = charged arithmetic.py), never produce.py; own inv, rational, residue, plus/times/clean (cap 11 = the exact c*F length 6+6-1), own row parsing and P7; 11 positions at :148-151 cover every coefficient of degree<=10 products, zeros included, no evaluations. entry.py supplies only refusals, pins, JSON load/encode and write; the shared JSON layer is stdlib with numbers forbidden, so it introduces no mathematical dependence. Hazards: the only divisions are Fraction/Fraction (produce.py:49, check.py:53) and int // int (check.py:19); d>0 is checked before every Fraction(n,d) (produce.py:31, check.py:33); every modular inverse is guarded nonzero; residue precedence (a % p)*inv % p is left-associative as intended; pow(0,0,89)=1 at check.py:90 gives coordinate 0. No CONTRACT/code mismatch found: degree caps 16/10, Euclid 8, inverse 16, cofactor 5, keys, statuses and caps all agree with CONTRACT.md:43-47,:57-66,:82-84.

## D. Wire, pins, publication

CONFIRMED with one liveness-only GAP. load() (entry.py:97-102) decodes ASCII and rejects every numeric token (parse_int/parse_float/parse_constant) and duplicate keys; encode() (:104-105) is sorted, compact, ASCII, newline-terminated, and both roles require encode(load(raw))==raw for the baseline (produce.py:51, check.py:55) and the checker for the certificate (:133). Fractions: signed string <=4096 characters, digits only, no leading zero, no -0, positive denominator, reducedness via Fraction readback (produce.py:22-34, check.py:25-36) under the 4300 guard set before any parse (entry.py:87-88). Sparse: all-zero vectors and unsorted/duplicate exponents rejected (produce.py:85, check.py:83); exponent strings '0'..'16'. Cofactors: strings exactly in '0'..'88' (check.py:142-143), no trailing zero (:145), length<=6 (:141); producer emits str() of residues already reduced and trimmed (:154,:160). Exact key set (:131-132), schema/prime/root/chart/baseline_sha256 (:134-136), modulus compared to the checker's own (:137). Pins: recheck() (entry.py:76-81) rehashes three sources (1 MiB cap) and the 1652675-byte/BASE_SHA baseline before parsing and again inside receipt(); produce.main rechecks before the certificate write (:170); check.main pre-reads the certificate and re-reads it for byte equality on both the success and the Inconclusive path (:156,:160,:163). read() is O_NOFOLLOW, regular-file, size-capped, with dev/ino/size/mtime/ctime drift (:62-71). write() is O_EXCL|O_CREAT|O_NOFOLLOW 0600, full-write check, flush, fchmod 0444, fsync, parent fsync, byte readback (:107-118); receipt absence at :84, certificate absence at :86. Caps: certificate 2097152, receipts 32768, baseline 15728640. Distinction: no path writes a success status before every need/sufficient/identity check; a wire violation is a ValueError, so the producer records INCONCLUSIVE_NO_CERTIFICATE exit 2 and the checker exits 1 without receipt, never a false positive. Baseline integrity beyond interface/P7/three Psi rows is bound by the pinned bytes and the earlier accepted reconstruction checker tier, not replayed here (CONTRACT.md:53). GAP (liveness only, same as the exact code gate): the top-level names format/job_tag/interface/field.P7/rows.Psi, row order 5/6/7 and the canonical encoder of the unread baseline come from the accepted reconstruction schema; a mismatch raises KeyError/TypeError (not ValueError) and ends exit 1 with no receipt, or a ValueError and exit 2, never success.

## E. Barrier and ROOT obligations

CONFIRMED as bindings. entry.py:2-3 imports only os/sys; :27-39 check Linux, DMI 'Amazon EC2', exactly 17 argv with the eight ordered flags and TAG; :41-44 nonzero 64-hex root/self/entry pins; only then hashlib/stat/json (:45-47); canonical absolute realpath-equal paths without newline/quote (:49-55); six distinct paths (:57); per-mode arithmetic pin hard-coded (:8-9,:59-60). The mathematical module is loaded only by arithmetic() (:125-132) after enter(), inside candidate()/verify(); fractions is imported at that exec_module. `-E -s -S -B` keeps sys.path[0] as the script directory, which `from entry import` at produce.py:2 / check.py:2 needs; `-I` would break it, and a stray fractions.py or entry.py sibling would shadow, so ROOT must keep the isolated directory to exactly the three pinned files. entry.py is executed before enter() hashes it, the arithmetic file is re-read by exec_module after its hash read, and any same-user caller can pass any 64-hex pins: the CLI binds, it does not authenticate (CONTRACT.md:76 states all of this). ROOT must also invoke with canonical absolute script paths (realpath equality at :56), provide the separate immutable registration whose hash is echoed into the receipt, freeze sources/baseline/certificate, enforce interpreter (>=3.11 for set_int_max_str_digits, else exit 1 at :87), native closure, ancestry and the still UNREGISTERED 600 s/500 s/2 GiB/16 MiB profiles with controls (CONTRACT.md:78). Existing runtime gates cover the old instruments only. No new source is needed for hardening; nothing here is a concrete blocking doubt.

## F. Receipts, control, scope, remaining obligations

CONFIRMED. No print exists in the three files. Producer: enter() refusals are SystemExit strings (exit 1, message on stderr) or uncaught ValueError; candidate ValueError including Inconclusive gives INCONCLUSIVE_NO_CERTIFICATE with null certificate_sha256 and exit 2 with empty streams (:167-169); success writes the certificate then CANDIDATE_UNCHECKED with its hash and exits 0; a cap overflow at :172 or a receipt failure propagates as exit 1 with no accepted output. Checker: Inconclusive gives INCONCLUSIVE_FIXED_PLACE with the examined hash and exit 2; any other ValueError (wire, pin, shape, identity at :151) is uncaught, exit 1, traceback, no receipt; success is HIGHEST_SPECIAL_PROJECTIVE_CHECKED_NO_SOURCE_OUTCOME, exit 0, empty streams, science_outcome NONE. Non-blocking note: the producer's exit-2 receipt carries no reason, so ROOT cannot tell a declined place from a schema mismatch without inspection; not a soundness issue. Control (CONTRACT.md:86-90): adding 1 mod 89 to the constant of c changes cF+dG by exactly Fbar, nonzero by selection (if Fbar=0 the identity forces Gbar!=0 and d is mutated), so some position among the 11 differs and check.py:151 raises; 88->0 is still +1 in F_89; trimming applies only when the mutated constant is the sole entry and becomes 0, giving [] which :145 accepts; a zero polynomial gets the single slot ['1']; the mutated file must be re-encoded canonically or :133 rejects it as a wire failure, not the control; the receipt path must be fresh or entry.py:84 fails first in enter(). Derived traceback for the frozen check.py: three frames, `<module>` at check.py:168 (`main()`), `main` at :158 (`verify(ctx,raw)`), `verify` at :151 (the raise), final line `ValueError: CHECK FAILED: fixed-place full modular Bezout identity`; each statement spans its whole line, so Python 3.11+ prints no caret lines and the text is 8 lines, but ROOT must pin the interpreter and match the full frozen text, exit 1, empty stdout, absent receipt. Scope: success licenses only special projective emptiness at (89,0), hence by the accepted properness and Route P imports V(H5,H6,H7)={0} over Bbar and the cyclic gate's conditional consequences; no source point, unit, basis, membership, all-r or JC2. Smallest remaining execution obligations: ROOT registration (three siblings at the charged hashes in an isolated directory, both original arithmetic files at separate canonical pinned paths, baseline at its pin, fresh certificate and two fresh receipt paths, registration pin, the -E -s -S -B vector, profiles inside the existing envelope), one producer run, one checker run on the frozen certificate, one mutation control with full traceback match, and the separately owed baseline 6/10 controls before any promotion. No review farm, no second prime, no fallback dispatch.

## OPEN(S) RAISED

None. No new canonical OPEN. Retained unknowns are the producer's own: whether 89/0 passes the dense-integrality, root and same-special boundary audit and admits the bounded modular cofactor, and the original highest regularity question. No charge_basis, no follow-on authority, no new theorem.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly xmodel/f10-r2-highest-modular-code-gate-fable5-20260910.md and box/f10-r2-highest-modular-code-gate-fable5-20260910/input_custody.md, both absent at 15:30:19Z; no shared, frozen, input, baseline or old file was touched; no corpus scan, no other lane, no network, no scientific subprocess, no Git.

## Completion

Authoring attestation: every byte of both owned files was written by apply_patch (skeleton and custody 15:37:08Z, sections 0-1/A-C 15:38:20Z, sections D-F/OPENS/COLLISIONS 15:39:17Z, this block afterwards), each write under 1500 words; no Write/Edit tool, no shell redirection, no helper, no source edit, no execution, no network, no Git, no charge_basis. Both appends landed at the end of the file on their intended anchors; heading order 0,1,A-F,OPEN(S),COLLISIONS,Completion verified by grep. Own WHOLE reread of this report (header, sections 0-1, A-F, OPEN(S) RAISED none, COLLISIONS EMPTY) was done at 15:39:23Z before this block; the ten custody rows in box/input_custody.md were rechecked against a fresh sha256sum of the snapshot directory (each row found exactly once); the body was confirmed to carry no 64-hex digest and the marker was confirmed absent before it was appended. All work finished before the 15:45:19Z reserve; stop no later than 15:48:19Z, never reset. No Seal is authored; the adapter seals. The marker below is the only standalone marker in this file and nothing follows it. All writers idle at the marker.

<!-- BODY-END -->
