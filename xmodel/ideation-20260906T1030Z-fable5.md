# Blind whole-portfolio ideation 20260906T1030Z — Fable 5.1

Basis `0d39df3c9fd69c939a8420c54d03228b9077777d`. Packet
`box/ideation-20260906T1030Z/packet.md` (body 11,656 B, SHA
`db7e289d…3d6`, recomputed). Desk work only: no AWS, no CAS beyond a
minute of local arithmetic, no ledger/tool/adapter/`jc2-lean` access, no
other round-20260906T1030Z report or log read, no live factored-J gate,
Sol2TB or local K16 boundary report read. Written between 10:30Z and
11:30Z.

**Headline.** Proof side: `NO_NEW_MECHANISM`; the all-degree obligation is
untouched by anything since 00:31Z and the honest ranking below keeps K16
`b*B*eta != 0` exclusion and source-to-chart necessity as the two proof
bottlenecks. Disproof side: one genuinely new *instrument*, not a new
theorem: a numerical Gauss–Newton witness / infeasibility-core probe on the
promoted one-way charts (physical factored-J family, `J_core`), with exact
acceptance through the promoted finite-algebra contract, and with the
same probe selecting the smallest row subset for an exact unit certificate.
One new connection: the 192+1 linear coordinates `(C, a)` of the physical
family make the Keller equation a bounded-degree Hamiltonian-derivation
cokernel problem (avenue 29); its rank-drop locus is exactly Ritt
decomposability of `G` (avenue 43); residue rows (avenue 33, R050) are
explicit cokernel functionals; and symbolic elimination of those linear
coordinates is predicted to fill in by the recorded R005 law.
Portfolio: CONTINUE the capped subset solves and K16 boundary lane,
REDESIGN the Sol 2 TB full-T2/T3 solver plan around the factored-J
presentation, STOP R005 deepening and R050 expansion.

## 0. Custody

Manifest `/tmp/jc2-lane.8g6Dhe/charged-inputs.list`, 18 entries, verified
mechanically by recomputing SHA-256 of every file in `inputs/` (18/18 OK,
no extra file); the six ledger hashes in the packet equal the snapshot
hashes; the snapshot copies also equal the current repo copies at write
time, but only the snapshots were read. Packet seal recomputed (body bytes
11,656; SHA matches; exactly one standalone BODY-END in the body).
Archive master table section 1 read completely (46 rows), plus sections
2–3 and the 09-04 overlays; APPROACHES, PROGRESS, COORDINATION read
completely; the newest `LIVE STATE` (10:26Z) and AUDIT 17(ppppppppppp)–
17(wwwwwwwwwww) read completely; all nine charged reports and the preflight
script read completely. External premises: none new; the sweep's two new
primaries were consumed only at the charged reports' scope.

## 1. Disposition vector over the 46 archived avenues

Legend: U unchanged, R raise, L lower, RO reopen. Reasons given for every
change; "unchanged" means the current APPROACHES/AUDIT status stands.

| # | Avenue | Disp. | Reason for change (if any) |
|--:|---|:-:|---|
| 1 | GGV corner families / degree farm | U | principal receiver client (K16 ray, k=4 ray) |
| 2 | Sheet-number ladder / dicritical trees | U | source/geometry interface only |
| 3 | Vertex-gap / strip ODEs / residue functional | U | subsumed by promoted face-ODE screens |
| 4 | Formal-germ certification + algebraization | L | the promoted one-way full-ideal/physical criterion makes a *polynomial* point the direct existence object; germ algebraization now adds nothing a chart point does not already give, and finite-depth survival is the recorded negative control |
| 5 | Jung–van der Kulk descent | U | consumed only as Arrow 4 divisibility |
| 6 | Abhyankar–Moh / coordinate recognition | U | interface only |
| 7 | Nonproperness / A(F) | U | |
| 8 | Formal-inverse combinatorics | U | |
| 9 | Lee–Li / Magnus | U | |
| 10 | HC4 ⇒ JC2 (Meng–Yang; Ni quartic) | L | Ni interface audit: pairing needs `deg G <= 2`, arbitrary `G` returns `J(F,G)^2 = c`; Roy's `d(pq)=0` step withdrawn; quartic HC4 is not HC4 |
| 11 | Mathieu / GMC / Zhao ladder | U | refuted |
| 12 | Face isolation / p-adic multinomials | U | |
| 13 | Dixmier DC(2) | U | |
| 14 | End(A_1) / Zheglov | U | |
| 15 | Spectral surfaces / commuting PDOs | U | |
| 16 | D-module / holonomic index | U | no class named yet |
| 17 | BCW / cubic stabilization | U | |
| 18 | Graded / GIT symmetry | U | closed as CE hunt; the chart's own G_m grading is an instrument (§5), not this avenue |
| 19 | Char-p + Witt lifting | L | 17(vvvvvvvvvvv): no finite lifting depth certifies (`(x, x+p^L)`), and no mod-p point of any full chart exists to seed Hensel; residual value is seed supply only |
| 20 | Reduction mod p / p-curvature | U | |
| 21 | p-adic injectivity / Hensel / model theory | L | as a route; retained as the promoted acceptance component (Hensel + local-containment identities) |
| 22 | Diophantine integral points | U | |
| 23 | Analytic global inverse | U | |
| 24 | Real JC / Pinchuk | U | |
| 25 | Fiber monodromy / passports | U | interface only |
| 26 | Primitive monodromy bound | U | |
| 27 | Links at infinity / splice | U | |
| 28 | Log surfaces / BMY | U | |
| 29 | LND / Hamiltonian derivation / Gauss–Manin | R | the factored family's Keller equation is `D_G(C) + (a/2) D_G(h) = 1 − J(h^3 + 3Dh/2, G)`, a bounded-degree cokernel problem of `D_G`; §4 and Card 2 |
| 30 | Affine-surface classification / ML | U | |
| 31 | Integrality / ZMT | U | |
| 32 | Off-diagonal collision ideal | U | no bounded client |
| 33 | Symplectic exactness / action residues | U | R050 gate: residues are implied by full J and are cokernel functionals of the map in §4; no standalone exclusion |
| 34 | 2D tangent-sweep / pole removal | U | |
| 35 | Descent of dim ≥ 3 CEs | U | |
| 36 | Guided counterexample search | R | for the first time there is a promoted one-way target (every chart point is a CE) and a promoted acceptance contract; the numerical probe of Card 1 is the executable form Grok's "not tried as a search" asked for |
| 37 | Finite-field census | U | a mod-p point of the 600-variable system would be a Hensel seed, but a census cannot find it at this size |
| 38 | Tropical | U | |
| 39 | Cohomological cluster | U | |
| 40 | Free-associative lift | U | |
| 41 | Naive scaling deformation | U | dead |
| 42 | Markus–Yamabe | U | |
| 43 | Ritt decomposition / composite coordinates | U | but now a *named* client: the rank-drop locus of the linear-`C` map is exactly "`G` decomposable" (§4); no separate lane |
| 44 | Moskowicz | U | closed |
| 45 | Differential Galois | U | |
| 46 | Lean / AI certification | U | non-blocking |

## 2. Bottleneck reranking

Proof side (all-degree):
1. **Uniform exclusion of an infinite admissible family.** The census never
   empties; K16 is the prototype and its terminal target is
   `b*B_m*eta_m != 0` excluded on every `V(K_m)`, `m >= 4`, every factor of
   `Q[d]/(3d^2−m)`, plus the `b = 0` boundary (B-UF)+(B-JL). Nothing since
   00:31Z touches it; the whole-polynomial report proves consistency of
   the leading balance at every `m`, so no degree cutoff exists.
2. **Source-to-gauged-stage-8 necessity** for the split branches (99,66)
   and D108: without it a chart unit excludes nothing (Card 3).
3. **Fixed-degree receiver decisions** (64 finite-window rows, K=7 covers,
   (99,66) δ=2, D108): compute-bound; the walls are elimination degree and
   fill-in (R005 law 1.046×/pivot; 466-row subset 9,000 s timeouts; 2 TB
   builder at 1.5 TiB with no solver).
4. **Moh ℓ-shift completeness** for the 44 `u_s = 1` descents (Prop 4.2/4.4
   condition (3) unprinted; the y-degree-only tower yields `J ∈ k[x]`, the
   class the children live in).

Disproof side (fixed degree, one-way):
1. **Properness decision of a promoted one-way chart.** Physical family
   (600 vars, 1,629 rows, degree ≤ 12, 11.3 M terms) or `J_core` of the
   direct ideal. No certificate, no solve authorized, stock msolve overflows.
2. **Witness acceptance**: an exact point in a nonzero `Q[T]/(H)` or a
   Hensel point with containment identities. No candidate; no
   candidate-generating instrument existed before Card 1.
3. **Chart choice**: the physical family without localizers is the largest
   promoted one-way chart (its Keller points contain the projection of the
   T2/T3 chart's Keller points); necessity is irrelevant for this direction.

## 3. Mechanism: `NO_NEW_MECHANISM` (proof) / one new instrument (disproof)

Proof side: `NO_NEW_MECHANISM`. Evidence that capacity is best spent on
existing targets: every proof-side object reviewed since 00:31Z (uniform
characteristic criterion, rank lemma, R050 residue, Ni pairing) is either
a re-expression of Keller on a per-datum chart or automatic on the
normalized locus; none bounds a degree, a support or a nilpotence exponent
uniformly. I looked for a uniform handle on the K16 boundary and found only
a reformulation (§10, retained OPEN, not a card): at `b = 0` with
`L = x^2 A`, `P = xW`, `Λ := x^3 A^2`, (B-UF) reads
`W·(2xW' − W + (3/2)Λ − B) = x·R_0`, `R_0 := (3/16)Λ(Λ−4B)/x − ηB`, and
`W(0) = −B != 0` forces `W | R_0`: all `2m−1` roots of `W` lie on the single
level set `Ψ = ηB` of `Ψ := (3/16)Λ(Λ−4B)/x` (degree `4m−3`), the cofactor
`K = R_0/W` has `K(0) = η`, and `W'(β)` at each simple root solves
`2β w^2 + ((3/2)Λ(β) − B) w − (3/8)(Λ(β)−2B)Λ'(β) + ηB = 0`. This is
equivalent to (B-UF), gives no cutoff, and is recorded only so nobody
re-derives it as news.

Disproof side, NEW as an instrument on the promoted charts (methodology
KNOWN in numerical algebraic geometry; not in avenues 1–46 as applied to an
actual necessary chart with an exact acceptance contract): **evaluation-
based numerical witness and infeasibility-core probe** (Card 1). It never
promotes a numerical point; acceptance is only the promoted
17(vvvvvvvvvvv) contract, so the modular-to-Q and generic-saturation
prohibitions are not touched.

## 4. New connection: avenues 29 ⟷ 43 ⟷ 33 through the factored family

In the physical family `F = h^3 + (3D+a)h/2 + C`, `G = h^2 − bh/3 + D`
(frozen source maps, 439 occurring coordinates), the preflight proves that
`C` is injective-linear in 192 coordinates disjoint from `h, D`, and `a`
enters `F` linearly. Hence, with `F_0 := h^3 + (3/2)Dh`,

```text
J(F,G) = J(F_0, G) + (a/2)·J(h, G) + J(C, G) = J(F_0,G) + (a/2) D_G(h) + D_G(C),
```

`D_G := G_y ∂_x − G_x ∂_y`. The Keller condition is therefore, for fixed
`(h, D, b)` (245 + 1 nonlinear coordinates), the *linear* system
`M(h,D,b)·(c, a) = e_0 − J(F_0, G)` with `M` of size 1,469 × 193 whose
entries are quadratic in the lifted `Hfact`, linear in `D`-coordinates and
in `b`. Three consequences, each checkable in minutes:

1. **Avenue 29.** Existence of `(C, a)` is "`e_0 − J(F_0,G)` lies in the
   image of `D_G` on the 193-dimensional subspace `V_C ⊕ k·h`": the
   bounded-degree version of the mate/cokernel problem whose global form is
   avenue 29's Gauss–Manin class. Residue functionals at places at infinity
   of the fibres `G = T` (avenue 33; the R050 construction with `G` in the
   role of `f`) are explicit left-null vectors of `D_G`, i.e. explicit
   equations on the 246 nonlinear coordinates alone. This is exactly why
   the R050 gate found the residue row "implied by full J" yet "combining
   rows of every degree": it is a cokernel functional, not a band.
2. **Avenue 43.** `ker M != 0` requires a nonzero `C ∈ V_C` with
   `J(C, G) = 0`, hence (char 0, AP Lemma 5) `C, G ∈ k[H]`; with
   `G_top = y^18 (y−x)^48` this forces `deg H ∈ {11, 22, 33, 66}` and, for
   `deg H = 66`, `C` constant. So off the constant direction (the `target_e`-
   type shift gauge, if a constant slot is in `V_C` at all) the rank-drop
   locus is exactly "`G` is Ritt-decomposable as `g(H)`, `deg g ∈ {2,3,6}`".
   On a Keller point `J(F, g(H)) = g'(H) J(F,H)` constant forces `deg g = 1`,
   so **every Keller point has `M` of full column rank and `(C, a)` uniquely
   determined by `(h, D, b)`** (up to that constant). The projection of the
   Keller locus to `(h,D,b)`-space is injective; the census/first-separation
   data live entirely on the nonlinear side.
3. **Negative control on the obvious next step.** Symbolic Schur elimination
   of the 193 linear unknowns produces minors of degree up to `2·193` in the
   nonlinear coordinates; the recorded R005 law (13 useful pivots, then
   1.046× terms per pivot) is the control predicting fill-in. The linear
   structure should be consumed by *evaluation* (numerical or modular LU at
   points, Card 1) and by exact certification at the end, not by symbolic
   elimination. Root's preflight direction is DUPLICATE-adjacent to items 1–2
   by its filename; its hypotheses are sealed and not consumed here.

Field: characteristic 0 throughout; `deg H` divisibility uses the algebraic
closure. Localization: none; the constant direction is a genuine kernel,
not a localization complement. Nilpotents: item 2 is a statement about
geometric points, as the preflight docstring already warns.

## 5. Strongest attacks

**Proof (fixed-degree, chart-level).** Exact unit certificate on the
*smallest numerically infeasible core* of the physical (99,66) δ=2 chart in
the dehomogenized graded form `I_hom + (J_0 − 1)` (see Card 1 outcome B and
Card 2). A subset unit is a unit of the full chart (promoted logic), and
the graded dehomogenization is the recorded 100× cheaper kill form. It
excludes the stage-8 family only, conditional on Card 3's necessity ledger
for the configuration. For the all-degree side the strongest live attack
remains the K16 boundary lane (continue; no new input from here).

**Disproof.** Card 1: random-start Levenberg–Marquardt on the full physical
row set evaluated through the circuit (never through the 11.3 M-term
expansion), on the chart `J_0 = 1`, with gauge slices; a quadratically
converging start is refined in multiprecision, its algebraic degree probed
by LLL on a random linear functional, and an exact `Q[T]/(H)` evaluation of
every row is the only accepted certificate (17(vvvvvvvvvvv)). If the point
is real-algebraic of degree beyond LLL reach, the numerical point is still
the first nonvacuous evidence either way and directs exact work (which
coordinates vanish, which rows bind).

## 6. Software acceleration / decisive experiment

Card 1 is both. Cost model (per iteration, 439 source coordinates):
evaluating `h, D, C` from the maps costs ≈ 10^4 terms; forming `F, G`
(degree 99/66) and `J(F,G)` by dense bivariate arithmetic ≈ 10^7–10^8
flops; the 439 directional derivatives by the chain rule
`∂J = J(∂F, G) + J(F, ∂G)` ≈ 5·10^9 flops, i.e. seconds in numpy, and the
1,469 × 439 least-squares step is negligible. One thousand starts is a few
worker-hours on an ordinary `c7i.4xlarge`; no 2 TB asset, no F4, no msolve
exponent guard. The same code, run at a point modulo a large prime with
exact LU on the 193 linear unknowns, gives the modular-point signal that
17(vvvvvvvvvvv) says is a *signal only*.

## 7. Campaign-systems check: UPGRADE (small)

Measured in this lane: the AUDIT delta headings changed grammar. The 219
entries through 17(tttttttttt) (02:51Z) use `## INTEGRATION #17 DELTA (id)`;
the eight newest, 17(ppppppppppp)–17(wwwwwwwwwww), use `### 17(id) — time —
title`. A heading index built on the old grammar (as I first did) reports
02:51Z as the newest audit state and silently drops every promotion since
09:35Z, including the one-way criterion, the certificate contract and the
R005 decision. Smallest useful implementation: a read-only
`ops/audit_index.py` that extracts `(id, line, UTC)` for both grammars,
asserts that consecutive ids are in file order, and fails if any `17(…)`
token in `LIVE STATE` is absent from the index; run it at every tick and in
packet freeze. Expected on the snapshot: 227 entries, none missing. Cost
≈ 15 minutes; risk none (read-only); benefit: removes a stale-state failure
mode from every mechanical reader, including blind ideators. Secondary,
not proposed now: two seal conventions coexist (canonical `seal.py` footer
vs external CLEAN/BODY_SEALED), and AUDIT already has to warn readers not
to run `seal.py verify` on footerless reports.

## 8. Detailed cards (three)

### Card 1 — Evaluation-based numerical witness / infeasibility-core probe (NEW as instrument)

**Target obstruction.** Disproof bottlenecks 1–2: no decision and no
candidate for the promoted one-way charts; proof bottleneck 3 (which rows
bind). Object: the physical (99,66) δ=2 family first (600 lifted or 439
source coordinates), then δ=5/2 and D108, then `J_core`.

**Dependencies.** Frozen source JSON `778eda93…` and its degree/support
facts (pilot §1); the chain-rule identity (17(sssssssssss), pilot); the
one-way criterion 17(ppppppppppp) (every exact point is a counterexample);
the acceptance contract 17(vvvvvvvvvvv). No dependency on the 436 MB
export, on `.56`, or on any solver. The `J_0 = 1` chart needs the grading
OPEN[PHYSJ-GRADING] (§10); until it is checked, use the inverse row
`Z_j J_0 − 1` instead, which is always valid.

**Cheapest discriminator (in order, all on one ordinary worker).**
(i) Positive control: pick a random complex `s*`, set the target
`J(F,G) = J(F(s*),G(s*))` coefficientwise, and require random-start
Levenberg–Marquardt to reach a zero (residual ≤ 1e−14, quadratic tail) from
≥ 30 % of starts; this exercises the exact same nonlinearity with a known
solution. (ii) Negative control: on the chart `J_0 = 1` no start may approach
the vertex `ρ = 1`, rest 0 (`F = h^3, G = h^2`, `J ≡ 0`); on the inverse-row
form that vertex must appear as `Z_j → ∞`. (iii) The probe: 200 starts on
`J(F,G) = 1`, 6 random affine gauge slices (translations, target
triangular shifts, scaling), residual evaluated through the circuit.

**Gate / price.** 1–4 worker-hours, < 1 GiB, no F4. Report the residual
histogram, the best point, and its convergence order; nothing else is
promoted.

**Outcomes.** A. Some start converges quadratically: refine to 300 digits
(mpmath Newton on a full-rank square subsystem), run LLL/PSLQ on a random
rational linear functional of the point up to degree 64; if a minimal
polynomial `H` is found, build the coordinates in `Q[T]/(H)` and evaluate
*every* row exactly (contract 1a–1b): all zero ⇒ counterexample to JC2 at
degrees (99,66); any nonzero ⇒ numerical artifact, report it. If LLL fails
(degree too large), publish the numerical point as evidence only, with the
list of exactly-vanishing coordinates and the binding rows, and hand the
exact lift to a Hensel-with-containment attempt seeded modulo a prime where
the point reduces. B. Every start stalls at residual ≥ 1e−6: no proof;
extract the smallest row subset whose least-squares minimum stays bounded
away from 0 under 100 restarts (greedy removal), then run the exact
decision on that core only, in graded dehomogenized form; `[1]` over Q
(exact, not first-prime) kills the stage-8 family; nonunit says nothing.
C. Convergence only with `J_0 → 0`: the cone-vertex phenomenon (17(cccccccc)
lineage); diagnostic, no force. D. Positive control fails: instrument bug;
stop and fix before any reading.

**Stop condition.** 1,000 starts without residual < 1e−10, or an exact core
decision, or an exact certificate. **Information gain.** High in both
directions: A is the campaign's terminal event if certified; B converts a
600-variable Gröbner problem into an exact decision on a measured core,
which is the only recorded way past the fill-in wall.

### Card 2 — Linear `(C, a)` structure as evaluation and certification, not elimination (DUPLICATE-adjacent to root's preflight)

**Target obstruction.** Proof bottleneck 3 and disproof bottleneck 1:
representation cost. **Dependencies.** Preflight facts (192 linear
independent `C` parameters disjoint from `h, D`; highest-face identity);
§4 items 1–3; the R005 fill-in law 17(wwwwwwwwwww) as negative control.

**Cheapest discriminator.** (a) 10 minutes: does `V_C` contain the constant
slot `(r,z) = (98,0)`? (decides whether `ker M` is 0 or the shift gauge).
(b) 10 minutes: numerical rank of `M(h,D,b)` at 20 random complex points
(expect 193, or 192 with the constant). (c) 30–60 minutes on a worker:
exact Bareiss pivots on `M` modulo a large prime at 5, 10, 20 pivots;
record term counts against `1.046^k`.

**Outcomes.** (b) full rank ⇒ the uniqueness of `(C,a)` over `(h,D,b)` is
confirmed at generic points (it is proved on Keller points in §4, item 2);
(c) growth ≥ 1.03×/pivot ⇒ symbolic Schur elimination is excluded as a
route and any "linear-C compiler" solver campaign is not launched; growth
< 1.01×/pivot through 20 pivots ⇒ an exact 246-nonlinear-variable Schur
presentation becomes the recommended exact input for Card 1 outcome B; a
rank drop at random points ⇒ `V_C ∩ k[H] != 0` for a decomposable `G`, which
would contradict §4 item 2 and must be traced before anything else.

**Gate / price / stop.** One lane-hour; stop after the 20-pivot
measurement whatever it shows. **Information gain.** Medium: it decides a
representation question that otherwise consumes a worker-day, and it
prevents repeating R005's trajectory on a larger system.

### Card 3 — Necessity ledger for the (99,66) physical family (KNOWN obligation, NEW cheapest step)

**Target obstruction.** Proof bottleneck 2: source-to-gauged-stage-8
necessity. **Dependencies.** Uniform gate (two-point top forces the datum
`M = (−66, 77, 97)`, `d = (99,33,11,1)`); Moh Prop 4.3/4.5 (Keller-assumed,
printed all-degree); the census/first-separation theorems at their promoted
hypotheses; the frozen stage-8 maps and gauge slices `beta = 1`, `jet0 = 0`,
localizer `ρ != 0`, branch `δ = 2` vs `5/2`.

**Cheapest discriminator.** (a) One desk hour, no CAS: take any Keller
pair with the datum, `G` monic in `y` of degree 66, `h_0 :=` the degree-33
polynomial with `deg_y(G − h_0^2) < 33`, and expand `F` `h_0`-adically. The
`h_0^6, h_0^5, h_0^4` cancellations in `G^3 − F^2 + aG^2 + bFG + …` force
`F = h_0^3 + (b/2)h_0^2 + ((3/2)D_0 + (a + b^2/4)/2)h_0 + E_2` (this much I
checked by hand; it matches the gate's `p = d + bc/2 − (a+b^2/4)^2/3`), and
the `h_0^3`-coefficient `−2E_2 + bD_0 + c` must be cancelled by the
`h_0^2, h_0, h_0^0` terms to reach `deg Q = 55`. Compute the exact
y-degree and total-degree bounds on `D_0, E_2` this forces, then compare
with the chart's `deg D ≤ 34`, `deg C ≤ 35` and with the stage-8 supports
(`min_r(B2) = 31`, `min_r(A3) = 63`). (b) One further hour: list every
stage-8 assumption and tag it `PROMOTED-NECESSARY` (census/first-separation
face row), `GAUGE-ATTAINABLE` (with the automorphism that attains it),
`CHOICE` (branch dichotomy, must be exhaustive), or `UNPROVED`.

**Outcomes.** All tags in the first three classes ⇒ a chart unit on the
family excludes the (99,66) two-point configuration at this fixed degree
(still not all-degree). Any `UNPROVED` tag ⇒ the exact residual obligation
is named and a unit is typed conditional on it. If (a) yields bounds
*weaker* than 34/35, the chart is smaller than the necessary family and a
unit excludes only the stage-8 subfamily: this must be known before solver
budget is committed. If (a) yields the chart bounds or stronger, the
degree part of necessity is closed by elementary algebra.

**Gate / price / stop.** Two lane-hours desk; stop when the ledger is
complete. **Information gain.** Medium-high for the proof side: it is the
difference between "family kill" and "configuration kill".

## 9. Continue / redesign / stop for current major work

- `.63` / `.73` capped 20-hour subset solves (due Sep 7 04:26/04:30Z): **continue**, no extension; a subset unit is a full-chart unit, a nonunit or timeout has no force.
- Sol 2 TB full T2/T3 direct-ideal build on `.103` (1.5 TiB RSS, still building, no solver): **redesign** the solver plan, not the custody. The Keller points of the T2/T3 chart project into the physical family's Keller points (Moh necessity of the datum plus the one-way criterion; not an ideal equivalence), and the physical presentation builds in 137 s / 1.7 GiB. Let the build finish under its custody; commit no solver hours to it before Card 1/2 results; termination stays DC-only.
- Fable factored-J gate (running, `.56`): **continue** (noninterruptible); its verdict gates Card 1's target.
- R005 compression: **stop** (terminal, no decision; the fill-in law is now a control).
- R050 residue: **stop** expansion; keep the theorem; the replay debt is low priority.
- K16 `b = 0` boundary product/norm lane (Astra, bounded): **continue**; nothing here improves its target.
- Broad web sweep: **continue** at Sep 7 09:23Z with X/Bluesky/Zulip retried from Sep 3 10:17Z.
- Necessity ledger (Card 3): **start** as a desk lane; it has been "running" nominally since the 09-04 overlay and is still typed unproved.

## OPENS RAISED

- OPEN[PHYSJ-GRADING] — QUANTITY: the weight defect `= 0` for every monomial of the stage-8 maps under `w(h_{rz}) = r`, `w(D_{rz}) = w(C_{rz}) = r + 1`, `w(a) = 66`, `w(b) = 33` (then `w([x^i y^j]J) = 163 − i − j`, `w(J_0) = 163`). CHEAPEST TEST: 5-minute script over the frozen JSON against the site weights; the preflight identities are consistent with it (`64 = 2·32 − 0`). If it fails, use the inverse row, not `J_0 = 1`.
- OPEN[PHYSJ-NUMERICAL-WITNESS] — QUANTITY: `min` over 10^3 random starts of the residual `‖J(F,G) − 1‖_2 ≤ 1e−12` with quadratic convergence, on the `J_0 = 1` chart with 6 gauge slices. CHEAPEST TEST: Card 1 (iii), 1–4 worker-hours after controls (i)–(ii).
- OPEN[LINEAR-C-FILLIN] — QUANTITY: term-count ratio after `k` exact Bareiss pivots on `M(h,D,b)` for `k = 5, 10, 20`, compared with `1.046^k`; decision threshold `< 1.01` per pivot. CHEAPEST TEST: Card 2 (c), 30–60 minutes modulo a large prime.
- OPEN[PHYSICAL-FAMILY-NECESSITY] — QUANTITY: the number of stage-8 assumptions tagged `UNPROVED` `= 0` (target), and the elementary bounds `deg D_0 ≤ ?`, `deg E_2 ≤ ?` forced by `deg Q = 55` versus the chart's `34 / 35`. CHEAPEST TEST: Card 3 (a)–(b), two desk hours.

## Labels and FALLACY-v2 disposition

Retained, not raised here (no new entry): the K16 whole-polynomial target and (BOUNDARY) exactly as typed in the whole-polynomial report; §3 adds only an equivalent divisor form.

Provisional labels: Card 1 `NEW` (instrument on the promoted one-way charts; methodology `KNOWN`); Card 2 `DUPLICATE`-adjacent to root's sealed preflight direction, with `NEW` sub-claims (full-rank on Keller points, projection injectivity, fill-in prediction); Card 3 `KNOWN` obligation with a `NEW` cheapest step; §4 connection `NEW`; systems card `NEW`. No `SCOPE-CONFLICT` found with the charged ledgers; the final checksum follows collection.

FALLACY-v2: no exit-price assertion, hence no `charge_basis` line. Floors are never read as attainment: residual minima are not proofs, a numerical point is never promoted, and only exact evaluation in a nonzero finite Q-algebra certifies. Ring maps are declared (`X = x`, `W = y − x`, determinant +1; source coordinates → lifted `Hfact`; the dehomogenization `J_0 = 1` versus the inverse row, related by the grading only when OPEN[PHYSJ-GRADING] holds). No `sat()`, no raw-remainder, no pole identity is used. All quantifiers are fixed-degree unless stated; nothing here is an all-degree exclusion, a modular-to-Q inference, or a generic-only saturation shortcut.


## COLLISIONS (ops/open_collision.py, lexical, filtered away from every same-round file; hits are review candidates, never closures)

- OPEN[PHYSJ-GRADING] (report:392): NONE
- OPEN[PHYSJ-NUMERICAL-WITNESS] (report:393): NONE
- OPEN[LINEAR-C-FILLIN] (report:394): NONE
- OPEN[PHYSICAL-FAMILY-NECESSITY] (report:395): four lexical candidates, none a closure: AUDIT 17(ooooooo) (child top at u_s >= 2, a different obligation), AUDIT 17(sssssss) (K16 universal recursion), AUDIT 17(fffffffff) (fixed-point set of Moh's sieve), notes.md 09:53Z R050 delta. The stage-8 necessity ledger itself remains typed unproved in the packet.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `28054`.
- Body SHA-256:
  `ba0a59d3c8dfcf8dad92951972b5c41ceea10712cfe787b387403d90145782ad`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
