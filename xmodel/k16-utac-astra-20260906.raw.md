**K16 UNIFORM closing lane: exact two-line geometry at N = 4, 5; a uniform lower bound on nilpotence exponents; the eta termination statement remains OPEN**

Lane `k16-utac-astra-20260906`. Date 2026-09-06. Frozen basis `e584173b8a751ddd6b7fbc5627958ac64cc3d4e7`.
Drivers and compact results: `box/k16-utac-20260906/`.

**VERDICT: the user’s eta version of (U-TAC) is not proved for all N, and no characteristic-zero counterexample is found. No whole-ray theorem (T) is promoted.** What is proved here is more precise than the frozen finite evidence: in characteristic zero, for N = 4 and N = 5,

```
sqrt(I_close,N) = (u, l2, l4,...,lN, l3-2w2, B*w2),
u = 4eta+3l2.
```

Thus the reduced closing variety is **two affine lines meeting at one point**, not one irreducible curve. One is the cubic family `L=-1+a*x^3, P=-L^2/4`; the other is `L=-1, P=-1/4-B*x`. Both embed for every degree bound N >= 3. Their being exhaustive is established here only at finite indices. The same reduced geometry is verified at N = 6 modulo 1000003, with every actual-degree stratum and every coefficient-field factor checked.

A second, genuinely uniform theorem is proved: any membership `eta^e`, `u^e`, or `(B*eta)^e in I_close,N` requires **e >= 2N**. This follows from explicitly constructed solutions over `Q[z]/(z^(2N))` with B=1 and eta=z. A simpler square-root specialization gives the exact image ideal `(z^(N+1))`. There is no constant nilpotence exponent valid at all N. These are statements about the nonreduced schemes, not counterexamples to vanishing on their reductions.

The precise unresolved statement is given in §8 as a family of explicitly reconstructed ideals in the **actual** polynomial degree. The suggested triangular-series proof has two identifiable missing steps: `eta != 0` does not imply `u != 0`, and the series obtained on `u != 0` still depends on a growing set of free P-coefficients. There is also a separate dependency issue: the `b=1` statement controls the open set `b != 0`; the frozen residual target (R) includes `b=0`.

**1. Custody, scope, and corrections to the premise.**

Before opening any charged mathematical input, the receipt `xmodel/k16-utac-astra-20260906.run.v2` was parsed mechanically with `awk -F=`. Its `charged_input_<i>_sha256=` and `_basename=` entries were paired into a manifest, and `sha256sum -c` returned six OK results. The manifest is `box/k16-utac-20260906/manifest.sha256`. All six mathematical inputs were read from the frozen directory `/tmp/jc2-lane.81GwWy/inputs/`. No digest was manually transcribed to perform this verification.

Consumed sources, with short names used below: **Closing**, `k16-t2-closing-opus5-20260906.md`; **Series**, `k16-universal-series-fable5-20260905.md`; **Tacnode**, `k16-tacnode-fable5-20260905.md`; **Radical**, `k16-radical-steps-grok46-20260906.md`; **Xempty**, `k16-xempty-astra-20260905.md`; and **FALLACY-v2**. Their frozen copies, rather than later versions of the reports, govern every source claim here.

Three distinctions matter before promotion.

* Closing §4, lines 201–205, expressly labels its radical and curve calculations `MEASURED-MODULAR`, and says that they are **not** characteristic-zero proofs. Its exact-Q attempts timed out. The prompt’s word “proves” therefore needs this field qualification. The exact radical equalities in this report are new computations and a complete finite-degree argument; they are not a promotion of modular evidence by analogy.
* Closing’s displayed `(U-TAC)` at lines 210–216 is the **u** target for N >= 3. The present user asks for the **eta** target for N >= 4. This report writes `U_eta` for the requested assertion and `U_u` for the older assertion. Neither is silently substituted for the other. At the finite indices, eta and l2 both vanish, which gives u=0. Eta vanishing alone does not give u=0 without a separate statement about l2.
* Xempty §7.3 treats `b=0, B*eta != 0` separately, and its §8 residual (R) is in the unlocalized coefficient ring containing b. Normalizing b to one is lossless on `b != 0`, not on that boundary. Section 9 gives the exact additional implication needed for a whole-ray conclusion.

The new finite calculations are generated directly from the frozen formula (UF). They do not import a ray certificate, a precomputed standard basis, a ray leading-coefficient row, or values of y and omega. The two old closing-driver sources referenced by Closing were inspected read-only to audit its presentation; no output or certificate from those drivers is a mathematical input to the new finite proof. No ledger, `jc2-lean`, or `ideation-*` input was used or edited. All computation was local; no fleet was used.

**2. The closing ideal is exactly a bounded polynomial-solution problem.**

Work over Q, or an algebraically closed characteristic-zero extension when discussing points. The ring and coordinate map are

```
S_N = Q[u,B,w2,l2,...,lN],
eta = (u-3l2)/4,
L = -1 + sum_(j=2)^N l_j*x^j,
P0=-1/4, P1=-B, P2=eta, P3=w2.
```

Here u is the L-pivot coordinate, not eta and not an inverse variable. A superscript prime in a differential formula means differentiation with respect to x; the tilde in the charged conjugate branch is just a label. Put `theta=x*d/dx` and

```
G = (3/2)L(L+1)-Bx,
R = (3/16)L^2*(L(L+2)-4Bx)-eta*x^2*(L/2+Bx),
F(P,L) = (theta-3)(P^2)+G*P-R.
```

For k >= 4, the coefficient of P_k in `[x^k]F` is `-(k-3)/2`. The recursion consequently defines Phi_k by scalar divisions only. With the marked jets, rows 0 through 3 vanish. Define `I_close,N=(Phi_(2N+1),...,Phi_(4N))` as in the task.

**Lemma 2.1, proved here.** A geometric point of `V(I_close,N)` is equivalent to a polynomial solution F=0 with `deg L <= N`, `deg P <= 2N`, and the displayed jets. This includes lower actual degrees and all vanishing leading coefficients.

Proof: compute the marked series only through degree 2N, obtaining P^[N]. Its rows through degree 2N are zero by construction. Let `f_k=[x^k]F(P^[N],L)` for `2N < k <= 4N`. For increasing k, the recursion expresses

```
Phi_k = 2*f_k/(k-3) modulo (Phi_(2N+1),...,Phi_(k-1)).
```

Indeed, the difference between the residuals for P^[N] and for the longer partial series contains a previously computed tail coefficient in every term. The multipliers `2/(k-3)` are units. Induction proves the **ideal equality** `(f_(2N+1),...,f_(4N))=I_close,N`. The residual of P^[N] has degree at most 4N, so these equations kill every remaining row. Conversely, a polynomial solution has the same marked coefficients by the nonzero recursive pivots and hence has zero closing tail. This also justifies the compact direct-residual emitter used for the exponent replay.

The general-b normalization used only on its legitimate open set is explicit:

```
Lbar=L/b, Pbar=P/b^2, Bbar=B/b^2, etabar=eta/b^2,
lbar_j=l_j/b, wbar2=w2/b^2.
```

Every term of (UF) is divided by b^4, and the barred jets have b=1. No assertion about b=0 follows from this map.

**3. The two universal components and their exact geometry.**

For every N >= 3, direct substitution gives the following two families:

| family | parameters in S_N | L and P |
|---|---|---|
| C_B | B=s; u=w2=l2=...=lN=0 | L=-1; P=-1/4-s*x |
| C_3 | w2=v; l3=2v; B=u=l2=l4=...=lN=0 | L=-1+2v*x^3; P=-L^2/4 |

On C_B, eta=w2=0 and B is arbitrary. On C_3, eta=B=0 and w2 is arbitrary. In particular the points s=1 and v=1 are exact witnesses that B and w2, respectively, are not in the radical at **any** N >= 3. This is stronger than failing to find a power up to a search limit.

The ideal of their union is

```
J_N=(u,l2,l4,...,lN,l3-2w2,B*w2),
S_N/J_N = Q[B,w2]/(B*w2).
```

It is radical: it is the intersection of the two prime line ideals obtained by additionally setting w2=0 or B=0. Thus `I_close,N subseteq J_N` for every N >= 3, and `C_B union C_3 subseteq V(I_close,N)` uniformly. The reverse containment is the substantive issue; it is not inferred from this construction.

When these are exhaustive, the curve has two components, each an affine line. Its normalization is the disjoint union `A^1 disjoint_union A^1`. Each normalized component has genus zero. The ordinary projective closure is two projective lines meeting once and has arithmetic genus zero. These specifications avoid assigning the genus of one irreducible curve to a reducible object. Its only singularity is the ordinary crossing B=w2=0 in the reduced coordinate ring.

The cubic component is exactly the `P=-L^2/4` family at **actual degree three**, embedded in every larger degree-bound space. The other component is indispensable. Thus the finite curve is neither just that cubic family nor a single fixed rational image of an irreducible affine line. It is the union of two such images. For example, trying to combine the parameters in `L=-1+a*x^3`, `P=-L^2/4-B*x` gives the exact residual

```
F = (3/2)*B*a*x^4*(a*x^3-1),
```

so the naive two-parameter combination forces B*a=0. This calculation verifies the union and does not purport to classify arbitrary P,L.

**4. Exhaustion at N = 4 and N = 5 in characteristic zero.**

This is an exhaustive stratification by actual degree, rather than an assumption that the coefficient l_N is nonzero. Let m=deg L. Since L'(0)=0, either L=-1 or m >= 2. If L=-1, a polynomial P of degree n >= 2 would contribute the unique highest term `(2n-3)*lc(P)^2*x^(2n)` to F: the other terms have degrees at most n+1 or three. This is impossible. Hence `deg P <= 1`, and the jets give precisely C_B.

If m >= 2, comparison of the highest degrees forces `deg P=2m`. If deg P is smaller, the nonzero degree-4m term of R cannot be matched; if larger, the degree-2deg(P) term of `(theta-3)P^2` cannot cancel. Choose alpha in the algebraic closure with `alpha^m*lc(L)=1`, and substitute x -> alpha*x. The parameter map is

```
l_j -> alpha^j*l_j, B -> alpha*B,
eta -> alpha^2*eta, w2 -> alpha^3*w2, b -> b.
```

The Euler derivation commutes with this substitution and (UF) is preserved. Thus every nonconstant solution is covered by `lc(L)=1`. Temporarily retain b as a variable, so

```
L=-b+l2*x^2+...+l_(m-1)*x^(m-1)+x^m.
```

The top row determines `p=lc(P)` through

```
(4m-3)*p^2+(3/2)*p-3/16=0,
p=1/(4*(2d+1)),  3d^2=m.
```

These constants are derived from (UF); they are not imported ray data. Work factorwise in this coefficient algebra. For m=2,4,5 it is a quadratic field over Q, so its two embeddings cover the two geometric roots. For m=3 the factors d=1 and d=-1 are handled separately.

Starting from `p*x^(2m)`, solve rows `2m+j` in decreasing order `j=2m-1,...,2`. Their pivots are

```
A_j=2*(2m+j-3)*p+3/2
   =(2m+j+6d)/(2*(2d+1)).
```

They are nonzero: a zero would require `(2m+j)^2=12m`, while `(2m+2)^2>12m` for m >= 2. The eta-dependent forcing has degree at most m+2 <= 2m and is absent from these rows. The B-dependent forcing has degree at most 2m+1; induction shows that the reconstructed P_j for j >= 2 are also B-independent. Set `eta=P2`, prescribe `P1=-B`, `P0=-b^2/4`, and solve row 2m+1 for B. Its constant pivot is

```
-((4m-3)*p+3/4)=-3d/2 != 0.
```

The remaining ideal, here named K_m, consists of rows 4 through 2m in

```
K_m subset Q(d)[l2,...,l_(m-1),b],
generator order (l2,...,l_(m-1),b),
weights (m-2,m-3,...,1,m), global order wp.
```

There are 2m-3 rows. Every normalized actual-degree-m solution maps to a zero of this ideal and conversely reconstructs a solution. No coefficient of the residual is discarded: each driver checks the B leader, the identically zero rows 0..3, and all rows 2m+1..4m. At the cubic negative branch the P0 resonance is retained as an equation; it is never divided out.

The clean exact computations from this freshly generated presentation are:

| actual m | field and presentation | exact result |
|---|---|---|
| 2 | Q(d), 3d²=2, b=1 | unit ideal |
| 3 | Q, d=1, b=1 | unit ideal |
| 3 | Q, d=-1, b=1 | standard basis `(l2^2)` |
| 4 | Q(d), 3d²=4, b variable | reduced basis size 17; dimension 0; b^4 in K_m |
| 5 | Q(d), 3d²=5, b variable | reduced basis size 60; dimension 0; b^5 in K_m |

The b powers exclude b=1 at actual degrees four and five. They are computations in these small independent ideals, not asserted minimal powers of eta in I_close. Retaining the homogeneous b variable greatly reduces this finite calculation; its elimination is justified above before the computation is used.

For additional transparent low-degree controls, the m=2 residual is the nonzero constant `-(6d+7)/40`. For m=3,d=-1, put h=l2. Two retained rows are scalar multiples of

```
h^4*(34h^3-775),  h^3*(59h^3-1375).
```

If h != 0 they require the unequal numbers `775/34` and `1375/59` to be equal. Thus h=0. The reconstruction then gives B=eta=0 and `P=-L^2/4`. At d=1, h=0 fails a retained row; for h != 0, putting z=h^3 yields the two quadratics

```
1986398181*z^2-16122815232*z-3806398081024,
48281373*z^2-392411136*z-49433741312.
```

Their exact resultant is `7323721655258186443534573944585584640000`, nonzero. These low-degree checks were also independently reconstructed in `theory_low_degree.py`.

Undoing the leading-coefficient normalization now exhausts every actual degree permitted at N=4 or N=5. Every solution lies on C_B or C_3; both families are solutions. Since J_N is radical, geometric equality and faithful extension to the algebraic closure give

```
sqrt(I_close,4)=J_4,    sqrt(I_close,5)=J_5    over Q.
```

This proves the eta, u, l2, T2 and B*eta radical assertions at those two indices exactly, together with dimension one and the two negative radical assertions. It does not assign the frozen modular exponents 19 and 27 to characteristic zero.

**5. Finite replay, field controls, and saturation checks.**

The low-degree recursion agrees exactly with the charged Phi4 and Phi5 formulas. In particular

```
Phi4=2eta^2-6B*w2-2eta*l2-(3/4)l2^2,
T2=3Phi4+18B*w2-10eta^2=-(1/4)(4eta+3l2)^2.
```

These are polynomial identities, not radical consequences. They are checked in `structure_checks.py` over Q with general b before specialization, and in the compact modular closing emitter.

The modular exponent replay uses `GF(1000003)` with generator order `(u,B,w2,l2,...,lN)` and weights `(2,1,3,2,3,...,N)`. It computes the identical closing ideal using Lemma 2.1, then repeatedly reduces the previous normal form multiplied by the target. The first zero is the minimum in that field, since every smaller exponent has explicitly nonzero normal form. A different monomial order can change basis size without changing the ideal or these minima.

| N | field | basis size | dim | min u exponent | min l2 exponent | min eta exponent | B*eta |
|---|---|---:|---:|---:|---:|---:|---|
| 4 | F_31991 and F_1000003, separately | 854 each | 1 | 19 | 15 | 19 | minimum 19 |
| 5 | F_1000003 | 5249 | 1 | 27 | 20 | 27 | exponent at most 27, from eta membership |

These reproduce the frozen modular minima. At N=4, B and w2 remain outside the ideal through power 45; their nonradical status at every N is proved independently by the explicit families. At N=5 the separate B*eta search was stopped after the eta minimum was established; no minimum for B*eta is asserted. Its 27th-power membership follows immediately by multiplication by B^27. The first one, two and three direct closing rows at N=4 have dimensions 5, 4 and 3 in S_4, respectively, reproducing the nonunit truncation controls without a dummy variable.

At N=6 the geometry test does not infer anything from one convenient split root. All actual degrees up to six are checked over the same prime p=1000003:

| m | coefficient-field treatment | result |
|---|---|---|
| 2 | d=804413 and 195590 in F_p | both unit at b=1 |
| 3 | d=1 and -1 in F_p | unit; `(l2^2)` respectively |
| 4 | irreducible 3d²-4, field F_(p²) | basis 17, dimension 0, b^4 |
| 5 | d=140269 and 859734 in F_p | each basis 60, dimension 0, b^5 |
| 6 | irreducible d²-2, field F_(p²) | basis 230, dimension 0, b^6 |

The square classes m/3 are checked by Euler’s criterion and both roots are checked when the quadratic splits. Every pivot is nonzero at these primes. Since p is larger than every polynomial degree used here, the degree comparison is valid. This proves `sqrt(I_close,6 over F_p)=J_6`, geometrically over the algebraic closure of that finite field. The separate exact-Q degree-six attempt passed coefficient construction but timed out at its 600-second standard-basis cap; it proves no exact degree-six exclusion.

The saturation controls can now be settled exactly at N=4,5 without relying on the finite bound of a power search. In the ring S_N[z], `(I_close,1-z*u)` and `(I_close,1-z*eta)` are unit ideals because u and eta vanish at every point. For the wrong localizers u+1, B, and w2, explicit points on the two families give nonzero values of the respective localizer, so the inverse-equation ideals are nonunit. For u+1 the origin works with z=1; for B use C_B at B=1; for w2 use C_3 at w2=1. This proves the non-vacuity of the same localization operation on the actual closing ideal. No `sat()` wrapper or component was misidentified; the rings and inverse equations are stated explicitly.

The identity `eta=0 => T2=0` should not be used in the free ring. There it would assert that `-(9/4)l2^2` vanishes whenever eta=0, which is false. At the verified varieties l2 also vanishes, and that additional fact supplies the implication. Likewise the prompt’s phrase `eta != 0 (u != 0)` is not a ring identity: eta=1,l2=-4/3 has u=0.

**6. Uniform theorem: scheme thickness forces unbounded exponents.**

Define a ring map, for each N >= 4,

```
rho_N:S_N -> Q[z],
u -> 4z, B -> 0, w2 -> 0, every l_j -> 0.
```

It sends eta to z and L to -1. Specialization commutes with the recursion, which uses only scalar denominators. The differential equation becomes

```
(theta-3)(P^2)=-3/16+(z/2)x^2.
```

Set H=P². Solving coefficientwise gives `H=1/16-(z/2)x²+c*x³`. The marked P0=-1/4 and P3=w2=0 force c=0. Hence the unique marked series is exactly

```
P=-1/4*sqrt(1-8z*x^2),
rho_N(Phi_(2r))=c_r*z^r,
c_r=-(1/4)*binom(1/2,r)*(-8)^r != 0,
rho_N(Phi_(2r+1))=0.
```

Every c_r is a nonzero rational number. The even closing indices are `2N+2,2N+4,...,4N`, so their exponents are N+1 through 2N. Their generated ideal is therefore exactly

```
rho_N(I_close,N)=(z^(N+1)).                         (THICK)
```

If `eta^e in I_close,N`, applying rho_N gives `z^e in (z^(N+1))`, so e >= N+1; the same holds for u. Two observations 19 -> 27 do not exclude every fixed bound, whereas (THICK) already does. The following construction strengthens this and also treats B*eta.

**Theorem 6.1: e >= 2N, including the residual target B*eta.** Work over `A_N=Q[z]/(z^(2N))`, and set B=1, L=-1, eta=z. Put A=-1/4-x and seek

```
P=A+sum_(r=1)^(2N-1) z^r*f_r(x),  f_1=x^2.
T(f)=-(1/2)*(theta-3)f-x*(2theta-3)f.
```

The constant term in z satisfies (UF), and `T(x^2)=x^2/2-x^3` gives the required linear term. For r >= 2 define

```
T(f_r)=-(theta-3)*sum_(i=1)^(r-1) f_i*f_(r-i).
```

Inductively the right side is divisible by x^4 and has degree at most r+2. The operator T is an isomorphism from the span of `x^3,...,x^(r+1)` to the span of `x^4,...,x^(r+2)`: on a monomial

```
T(x^j)=-(j-3)*x^j/2-(2j-3)*x^(j+1).
```

This matrix is triangular with nonzero diagonal `-(2j-3)`, j=3,...,r+1. Thus f_r exists uniquely, is divisible by x^3, and has degree at most r+1. For example `f_2=x^3/3`, `f_3=4x^4/15-2x^3/45`.

The resulting P has degree at most 2N and satisfies (UF) modulo z^(2N), with exactly the marked constant, linear and quadratic jets. Assign `w2=[x^3]P`, u=4z, and every l_j=0. Lemma 2.1 works over this nonreduced Q-algebra because all recursive pivots are units. It therefore defines a ring map `S_N/I_close,N -> A_N` sending eta and B*eta to z, and u to 4z. Their powers of order 2N-1 are nonzero. This proves the claimed lower bound, even after localizing at B. For T2 powers it gives e >= N. The construction and its residual were independently checked exactly at N=4,5,6 in `theory_artin_thickness.py`.

This is not a proof that every conceivable parameterized certificate method fails. A formula with an exponent depending on N remains possible. It is a lower bound on the exponent and hence on the degree of these ordinary power-membership targets. It says nothing by itself about the optimal cofactor representation.

Nor does (THICK) give a reduced point with z != 0. Its reduction is z=0. For z != 0 the displayed square root is not a polynomial: its square has degree two and no linear term, and cannot be the square of a polynomial with the marked nonzero constant term and zero linear coefficient. The termination argument succeeds completely on this restricted slice. What grows with N is its infinitesimal thickness. Thus increasing nilpotence exponents are compatible with an unchanged reduced curve and do not prove that new components appear.

**7. Why the proposed general triangular-series argument does not finish.**

For a fixed P and a chosen l2, the charged L-side recursion on u != 0 has, at b=1, the form

```
l_(k-2) = 4*rest_k/u - 2*(k-3)*P_k/u,   k >= 5.
```

The coefficient of the newest P_k is nonzero. Already l3 varies with P5, l4 with P6, and so on. At degree bound N, the coefficients P5,...,P_(N+2) form a growing list of inputs before both polynomiality constraints have been imposed. The recursion is triangular conditional on those inputs; it does not identify the tail of one fixed series independent of N. Proving that **every** series arising from those choices fails to truncate simultaneously with L is precisely a further polynomial-termination theorem.

In addition, this chart misses the whole possibility `eta != 0, u=0`. On that locus `l2=-4eta/3`, and the next pivot is governed by `2B*l2+l3+2w2`, as in the charged pivot chain. A proof on u != 0 cannot be applied to that locus by continuity or by calling eta the pivot. None of these free-jet examples is being presented as a closing solution; they identify the invalid chart implication before closing is imposed.

The charged conjugate branch supplies no substitute termination argument. Tacnode Theorem 3.1 establishes that for a polynomial solution with b != 0 and actual degree at least three the second branch is algebraic and nonpolynomial, not another polynomial solution. That fact is consistent with the existence of the original polynomial branch. A Wronskian or polynomiality assertion about the conjugate would require a new hypothesis or theorem. No such assertion is introduced here.

The successful square-root calculation in §6 is therefore a controlled special case, not a generic model silently applied to every L. It proves an exact obstruction to fixed exponents, while leaving the growing family of L and P coefficients visible.

**8. The exact unresolved algebraic statement.**

The finite-degree reduction in §4 applies in every actual degree m >= 2 in characteristic zero. For each factor of `Q[d]/(3d²-m)`, let K_m and the reconstructed coefficient `eta_m=P2` be defined by that scalar-pivot algorithm. These definitions use only (UF), not chart certificates or ray constants.

After the complete low-degree classification, the requested assertion is exactly

```
U_eta:
for every actual m >= 4 and every coefficient-field factor,
eta_m in sqrt(K_m : b^infinity).
```

Equivalently there is no zero of K_m with `b*eta_m != 0`. Passing between this formulation and b=1 uses the explicit normalization in §2, and passing between normalized leading coefficient and arbitrary leading coefficient uses §4. Every possible actual degree and each vanished leader are covered.

The stronger candidate that would make the two-line description universal is

```
b in sqrt(K_m) for every m >= 4.
```

That statement would exclude **all** further nonconstant b-nonzero solutions of actual degree at least four. It is sufficient for U_eta but is not necessary: additional components entirely inside eta=0 would preserve U_eta. Thus “the curve is the same for all N” is a stronger conjecture than the task’s target. The finite radical equalities and the uniform inclusion `C_B union C_3 subseteq V(I_close,N)` do not supply its reverse inclusion at an unbounded degree.

What remains missing is a theorem excluding `V(K_m)` on `b*eta_m != 0` at every unbounded m, including the u=0 stratum. Neither the number of equations, the constant pivots, finite basis sizes, the finite genus calculation, nor the exponent lower bound proves that exclusion. This is the typed **OPEN** obstruction. No degree cap, finite-index extrapolation, or unsupported uniqueness of a rational parametrization closes it.

An additional bounded search on the parity stratum `B=0`, with L and P even, found no modular counterexample at actual degrees 8, 10 and 12 over p=1000003. The homogeneous quotient lengths were 206, 1005 and 4914, with both split factors checked where applicable. A degree-16 attempt timed out after 120 seconds. These are restricted modular experiments, not additional full characteristic-zero closing classifications and not evidence sufficient for a uniform conclusion.

**9. The complete dependency chain, and its boundary hypothesis.**

If U_eta were proved, specializing the universal jets to any terminal reconstruction with b != 0 would give eta=0 there, hence B*eta=0. In the terminal homogeneous ideal J_t this proves

```
B*eta in sqrt(J_t : b^infinity).
```

To obtain the unlocalized (R) from this assertion, one must additionally know

```
B*eta in sqrt(J_t+(b)) for every t and every field factor.       (BOUNDARY)
```

Indeed the exact radical decomposition is

```
sqrt(J_t)=sqrt(J_t:b^infinity) intersect sqrt(J_t+(b)).
```

The b=1 free-jet ring cannot prove the second factor merely by a normalization. As a logical control, in Q[b,eta] the ideal (b) becomes the unit ideal on inverting b, while eta is not in its original radical. This example is not asserted to be an (UF) solution; it demonstrates the missing algebraic implication. Xempty’s explicit b=0 treatment is why this issue is material to the stated residual, rather than hypothetical.

With **both** U_eta and (BOUNDARY), the remainder of the charged dependency chain is:

```
B*eta in sqrt(J_t)                                  [(R)]
 => tau in sqrt(I_+),
    tau=-(g*y/3)*B*eta modulo I_+, g*y/3 a unit
 <=> (I_+,T_(t,0)) is the unit ideal                 [charged (8.1)]
 => terminal normalized receiver chart is empty
 => the original K16 ray system has no solution
    [charged constant spine, normalizer, second affine spine]
 => theorem (T) at that t.
```

The row-image transport is consumed from Xempty §8: it is the specified triangular coefficient change and high elimination, not a map inferred from matching variable names. Its boundary identity is `R_boundary=y*eta/3`; the literal constant row is `T_(t,0)=y*g+tau`. Nilpotence of tau gives its finite geometric-series inverse, which is the charged unit-ideal step. This report supplies no new exit-price assertion, so no charge_basis line is applicable.

Neither U_eta nor the uniform boundary assertion is proved here. Accordingly the requested whole-ray `PROVED-HERE` gate cannot be issued. The exact finite closing theorem and (THICK) are labeled proved here in their stated scopes only.

**10. Reproduction, limitations, and final audit.**

The substantive new drivers are `finite_geometry.py` (all degree strata and coefficient images), `check_closing.py` (compact closing-ideal replay), `structure_checks.py` (exact identities, maps and square-root slice), and `theory_low_degree.py` (independent exact low-degree reconstruction). Compact field, pivot, basis and membership results are in their corresponding JSON and output files. No large standard basis is dumped. `finite_summary.json` records the finite-field factor checks and exact finite classifications; the final lane summary records the claim scope separately from the computation status.

The exact degree-six standard-basis attempt and the parity degree-16 attempt were stopped at their stated caps and yielded no claimed exclusions. The optional N=5 B*eta minimum search was stopped after the needed eta result had completed. An initial Phi4 diagnostic had a Singular precedence error; the coefficient construction and standard-basis calculation were unaffected. The repaired coefficient-only replay passes at both N=4 and N=5, as does the independent general-b rational identity. The original N=5 transcript retains that diagnostic error for custody; no failed check is counted as a pass. No lane process is left running at sealing.

FALLACY-v2 audit: all input hashes were checked mechanically; reduced support is separated from nilpotent scheme structure; actual degree is separated from the truncation bound; every nonzero-leading-coefficient normalization has its map and its excluded locus stated; split coefficient algebras are handled factorwise; zero pivots are not divided out; finite-field evidence is not promoted to Q; the saturation statements use explicit inverse equations in declared rings; the finite two-component classification is not asserted at all indices; and neither the conjugate algebraic branch nor a free-jet example is called a polynomial closing solution. The b=0 boundary is retained in the whole-ray dependency chain.

The result is **OPEN for uniform eta termination**, with exact finite reduced geometry and a uniform theorem explaining unavoidable growth of the nilpotence exponent. No ledger change or new whole-ray promotion is made.

<!-- BODY-END -->

**Seal.** Body: 29588 bytes, through the standalone end marker including its newline. Body SHA-256: `a37f89f6c0583df4d5fe6eb8f125f5cb54f880205eca32a5204ac9992ed16e81`. Frozen basis: `e584173b8a751ddd6b7fbc5627958ac64cc3d4e7`.
