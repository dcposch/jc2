# `1120Z` adversarial cross-pollination — AS owner

Date: 2026-08-25.  Blind collection was closed before this comparison.  I
read the four sealed owner reports (root `4430c2cb...`, cube `4860dbe4...`,
AS `ed5cd4f...`, TD6 `61cca3a4...`) and then the post-cutoff Q8, p=2, and
TD6 endpoints.  Post-cutoff facts change execution below but receive no
retroactive blind-vote or novelty credit.

## Deduplicated fingerprints

1. **`Q8-W0-BOUNDARY-NORMAL/VALUATIVE` — four-way independent
   convergence.**  All reports select the finite
   `x5=0`/`x3-2*x5=0` complement first, followed by a source-honest Rees or
   valuative atlas for coefficient infinity and the separate `p=0` chart.
   “Terminally decorated fan,” “normal-divisor compiler,” “conormal/valuation
   sieve,” and “valuative complement atlas” are one operational fingerprint.
2. **`Q8-FINITE-X-TERMINAL-JET` — four-way convergence.**  Every report
   independently targets the generic family
   `x5=0,x3=x1=a,c=1/a,d2=d4=0,r8=0` with terminal and Taylor jets.  The
   post-cutoff endpoint below advances this from a proposed tangent kill to a
   genuine first-order survivor.
3. **`AS-WHOLE-Q5-CARTIER/FITTING` — four-way convergence.**  Globalize
   `[x^2y^2]((det-1)/81)` over the complete Q5 scheme, split every rank and
   nilpotent stratum, and restore Q3--Q0 only on its exact zero locus.  More
   solver seeds and more pointwise deaths are not distinct ideas.
4. **`TD6-SOURCE-DAG/DENOMINATOR-COVER` — four-way convergence.**  Finish
   the current rows with original-row leaves, then give an explicit
   constructible/Bezout cover of every genuine denominator support.  A q3
   telescoping-adjoint test is a successor, not an alternative to source
   closure.
5. **`NORMALIZATION--FITTING--PROOF-DAG COMPILER` — shared software
   mechanism.**  The three mathematical lanes all need a source-hashed
   inhomogeneous cokernel section over a nonreduced/rank-stratified base,
   with normalized boundary valuations and exact original-row ancestry.
6. **`P2-SMALL-STATE AS CONTROL` — shared bounded reserve, now instantiated
   post-cutoff.**  It is an orthogonal support-growth experiment, not a
   replacement for p=3 Q5 and not an all-depth argument.

## Strongest shared hidden assumption

The common Q8 card silently risks identifying an arbitrary projective
homogenization with the closure of the **horizontal actual-source family**.
That is not automatic.  One must remove `w`-torsion before specialization,
perform relative homogenization with the correct saturation, prove the chart
union covers every horizontal valuation, and then attach the terminal/Taylor
rows without clearing a load-bearing denominator.  Raw projective charts can
contain vertical artifacts or miss a branch that exists only after the
relative closure.  Likewise, a generic tangent computation covers neither
exceptional `a` nor higher-order Kuranishi obstruction.

The corresponding AS version is that the Cartier quotient must be proved
integral and regular on the charged full Q5 predecessor scheme.  A polynomial
obtained after generic division or after deleting lower rows cannot be called
a global obstruction.  The TD6 version is exactly the current localization
quarantine: source-DAG membership on named opens is not a union theorem
without denominator coverage.

## Post-cutoff adjudication and rank changes

- **Localized Q8 interior:** hostile review is
  `CONFIRMED_WITH_REPAIRS`.  The scheme isomorphism to
  `Q[v]/(Q8)`, unit `r6`/relative Jacobian, and empty unloaded/non-Q8 loci
  survived.  Required repairs are a gated/independent irreducibility
  certificate, exact source-ideal wording, the transitive fibre pin, and
  preregistration/custody cleanup.  This strengthens, but does not silently
  promote, the interior premise.
- **Q8 finite complement:** producer-internal exact controls reduce the
  loaded finite complement to the `X` family above.  The
  `x3-2*x5=0,x5!=0` chart is empty; the overlap `x5=x3=0` is unloaded
  `A3`, with loaded split empty.  Crucially, over `Q(a)`
  `rank(J_y)=rank([J_y|F_w])=5`: a transverse first-order `w`-arc exists.
  Thus the hoped-for generic first-order kill fails.  Q8 remains first, but
  the decisive finite test moves to second-order terminal/Taylor curvature.
  Raw projective chart outputs are controls only until the corrected
  horizontal/relative saturation is run.
- **p=2 AS pilot:** producer-exact `B_(2,3)(D,D)` over `Z/8` has D=2 UNSAT
  with independently checked DRAT and D=3 SAT with direct integer replay of
  the identity gauge.  Hence `D_min=3` at depth three, pending hostile
  source/compiler review.  This falsifies “p=2 dies trivially” and raises it
  from cost estimate to a small spare-capacity recurrence laboratory.  It
  gives no depth-four, all-depth, p=3, or JC inference.
- **TD6:** current rows 0, 13, 34, and 36--39 are now exact PASS/source-
  liftable; rows 36--39 are zero identities and row 13's nonzero term lifts.
  V54D still reports only the nonunit supports `U,H`.  Seven rows do not form
  the current-row union, so TD6 stays third; the pattern raises the value of
  a symbolic source-DAG recurrence rather than another monolith.
- **AS Q5:** no mathematical endpoint at cross-pollination time.  Several
  pinned structural bases are diagnostic UNSAT; one Z3 endpoint OOMed.  The
  global formula and proof-producing/algebraic route remain the only
  theorem-sized gate.

## Best combination

Run two Q8 subgates in parallel.  On the finite `X` family, compute the
second-order source Kuranishi map together with the first nonzero coefficient
of `r8`, both true-centre Taylor defects, and the terminal/Kummer pullback;
factor every obstruction in `a` and rebuild its zeros and `a=infinity`.
Concurrently, repair the projective job to compute the horizontal relative
closure and its exact Rees charts.  This combines the root/cube normal-order
idea with the AS conormal certificate and TD6's proof-DAG/source-ancestry
discipline.  It also prevents the projective atlas from blocking a cheap
finite-branch decision.

AS continues independently: keep the full Q5/Q4 formula live, derive the
Cartier/Fitting zero locus algebraically, and use p=2 depth four D=3 versus
D=4 only on spare AWS capacity.  TD6 finishes the current source DAG and
denominator union before q3.  Reviews remain nonblocking, but no repaired
Q8/p2 result promotes before source review.

## Cheapest separating experiment

The highest-information cheap discriminator is now the **second-order
generic `X`-branch jet**, not another first-order rank test:

1. work over `Q(a)`, `a!=0`, with `w=s` and
   `y=y0+s*y1+s^2*y2` in all six source rows;
2. quotient the one-dimensional first-order freedom by source
   reparameterization and compute the order-`s^2` obstruction;
3. simultaneously record the leading orders/coefficients of `r6,r8`, the
   terminal equation, and both Taylor defects;
4. factor the resulting numerator(s) in `a` and run raw specializations at
   every zero, pole, and infinity.

If the generic obstruction is a unit and all exceptional points die, the
entire finite loaded complement closes and foreground Q8 effort moves to the
corrected projective/`p=0` atlas.  If it vanishes or a factor survives, that
one explicit formal branch becomes the sole finite selected-leaf client and
should be normalized before any broader saturation.  Either outcome sharply
separates “Q8 is one short boundary computation from a leaf theorem” from
“Q8 has a genuine new rational boundary component.”

No post-cutoff result proves or disproves JC2.  Global landing/coverage and a
cofinal complexity ceiling remain untouched.
