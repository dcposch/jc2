# Blind whole-portfolio delta — TD6 CURRENT support transition

Timestamp: 2026-08-26 07:38Z.  This note was written after independently
reading the canonical 46-row `APPROACHES.md` inventory and current canonical
overlays.  I did not read any sibling submission for this round.  Live AWS
producers and reviews were not blocked on this note.

## Significant new evidence and strict scope

The proof-carrying single-axis CURRENT partition has not yet assembled a joint
theorem, but it gives a sharp producer-level transition.  On the fixed
source-typed A3 presentation, after the reviewed 24-dimensional Stage-A
previous/pole kernel:

- q2 through q10 reached the CURRENT conormal computation and failed closed
  because their exact common denominators have factors beyond the then-licensed
  `U,H,B3` set.  Their coefficient tables were deliberately not emitted.
- q11, q12, q13, q14 have unit denominators and singleton CURRENT entries
  `11 U^6`, `12 U^6`, `13 U^6`, `14 U^6` on `('X0',10)`, ..., `('X0',13)`.
- q16 through q24 have unit denominators and exact zero CURRENT tables.
- q15 is not a missing source modulus: it is the lower target-shear gauge and
  is excluded from the 24-dimensional quotient.

No linear combination, 24-dimensional rank, family, TD6, SP-2, landing, or JC2
claim follows until the low-q denominator atlas, both dead-axis CURRENT columns,
dual monoliths, and exact union replay close.  The base current system is already
inconsistent, so the whole object remains a scheduling adjoint at an empty base.

## Whole-46 scan and changed ranks

Rows 1--7 (GGV/Sigray, boundary books, asymptotic variety) retain the same global
landing/cofinality wall.  Rows 8--18 (formal inverse, Conjecture E, Hessian/DC,
symmetry) gain only a reusable filtered-symbol idea, not a route promotion.
Rows 19--21 (Witt/p-adic) remain valuable disproof diversification but no fixed
bounded support has crossed the polynomial wall.  Rows 22--35 (height, topology,
monodromy, surfaces, LND, integrality, collision ideals, symplectic/pole-removal)
receive no direct theorem upgrade.  Rows 36--38 (structured search/finite-field/
tropical) gain a better constructible-rank search target, not permission for
generic search.  Rows 39--46 (cohomology, free lifts, scaling, Ritt, differential
Galois, Lean) are unchanged as discovery routes; certification remains downstream
of source/landing.

Within the active frontier, exact order four stays retired; exact order two/order
one remains the strongest global-boundary proof lane; D1 moves only on weighted
triple-root/equality-face coverage; TD6 rises because the infinite-looking q-tail
has exposed a finite support boundary, but remains a fixed-section local client.

## Ranked new connections and cheapest falsifiers

### 1. CURRENT symbol theorem at the degree-15 shear boundary

In the source compiler, a q_e perturbation enters `q'` at degree `e-1`, and the
CURRENT equation contains `3 f3 q'`.  The observed singleton is exactly the
Euler coefficient `e` times a common leading third-band scalar `U^6`; the four
surviving rows are precisely degrees 10--13.  The next coefficient q15 is the
target-shear gauge at the degree of p, and degrees 15 and above are absorbed by
the chosen CURRENT pivot image.  This strongly suggests an associated-graded
operator identity, not fourteen unrelated coincidences.

Cheapest falsifier: from original source rows, emit the cokernel-row support and
the direct `3 f3 q'` contribution before reduction, then add a *typed gauge
marker* at q15 solely as a quotient control.  Prove or refute that the CURRENT
symbol is the four-row diagonal `e U^6` for e=11..14 and zero for e>=16 modulo
the q15 shear image.  A single extra non-direct contribution at e>=16 or failure
of the q15 marker to lie in the gauge image kills the proposed theorem.  If it
passes, retire all later q-axis CURRENT work and use a finite tail theorem.

Cross-connection: this is the TD6 analogue of D1's filtered obstruction complex
and order one's support-before-genus screen.  One generic graded-source compiler
should serve all three.

### 2. Denominator factors are the constructible atlas, not arithmetic noise

The q2--q10 failures are positive routing information.  Their factors should be
compared exactly with the reviewed FIRST principal-open factor `R38`, the
licensed chart factors, and previous/pole pivot factors.  Each genuinely new
factor is a raw fibre; a factor already in the Stage-A open is a reporter-scope
bug.  This is the same recursion as the exact order-two square/discriminant split
and D1 equality faces.

Cheapest falsifier: the live reporter-only AWS rerun dumps the exact common
denominator and factorization before preserving the assertion.  Canonicalize
the union of factors, compute containment against the pinned open, and raw-rebuild
one smallest-degree new factor from original rows.  Stop if any supposed factor
is only a pivot-order artefact: launch one alternate row/pivot order and demand
a localized Bezout cover rather than a gcd slogan.

Software consequence: replace boolean `allowed denominator` checks by a
proof-carrying factor ledger that always emits the preassert rank, exact factor,
source ancestry, and a preregistered successor node.

### 3. Exact 24-dimensional CURRENT map before any more K2

The four q11--q14 entries occur on distinct rows, while nine high-q axes vanish.
They do not by themselves kill four directions: low-q and d10/d15 columns may
cancel them.  Assemble one exact matrix on the *same* principal open, including
all rank-drop factors, and compute its kernel.  Only that kernel is eligible for
the next quadratic block.

Cheapest separator: reconcile the 22 shards against both monoliths, append the
dual d10/d15 tables, and RREF the exact 24-column matrix over the source field.
Require omission and column-permutation controls and keep `R38=0` plus every new
denominator divisor outside the generic theorem.  Stop all q-only K2 shards; if
the current kernel is zero, the next work is raw fibres, not quadratic order.

### 4. A shared Rees/Fitting DAG for TD6, D1, and exact order two

All three leading proof lanes now have the same computational shape: a filtered
source module, a generic principal open, exceptional factors, and recursively
typed raw fibres.  The campaign should encode this as one manifest-driven DAG:
original-row module -> selected minor/Fitting factor -> generic certificate ->
factor fibres/intersections -> exact union.  The `A=[x], b=y` countermodel must
remain a unit test against confusing generic cokernel membership with fibrewise
consistency.

Cheapest falsifier: port one already reviewed two-piece TD6 cover and one exact
order-two square/discriminant split into the same schema.  If the schema cannot
express source ancestry and localized Bezout/union identities without hand
exceptions, it is not yet a proof interface and should not replace current
replays.

### 5. Best non-TD6 experiment: order-two componentwise next divided jet

The strongest outside allocation remains the exact order-two recursion, because
it is closer to the boundary/global wall than TD6's fixed-section adjoint.  Run
the next divided jet independently on the square chart
`K=(z^2+s)^2, N=(z^2+s)(alpha z+beta)` and the explicit double-root
discriminant chart, with their intersection and slower slopes kept separate.
Immediately impose Taylor/terminal `[6,2]` constraints on every survivor.

Falsifier/stop rule: a source-valid survivor on either component promotes that
component; a unit obstruction retires it.  Do not return to monolithic Rees
saturation if the componentwise compiler closes source ancestry.

## Allocation delta

- exact order two/order one: 40% (down from 50%, but still first for global
  relevance);
- TD6: 35% until the CURRENT matrix and denominator atlas close, then sharply
  reduce unless a source-valid survivor appears;
- D1 weighted triple-root/equality faces: 20%;
- protected global/AS/formal/certification: 5%.

Reviews remain asynchronous.  AWS producers should treat reasonable exact
confidence as provisional truth and launch the next typed discriminator without
waiting, while every promotion remains review-, scope-, and landing-gated.
