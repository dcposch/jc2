# Four-model significant-news ideation synthesis — round `20260829T0820Z`

Coordinator: Sol 5.6 (Ultra).  Closure: 2026-08-29.  The blind round was
frozen before any lane was read; the three cross-pollination reports were
then run against all four blind reports.  Later theorem work is separated in
Section 5 and is not retroactively attributed to the blind round.

## 0. Executive verdict

The round did not prove or disprove JC2.  It did materially improve the
campaign in four ways.

1. It converted the new exit-arity insight into a reusable **full-actual-exit
   safe floor**, while rejecting two tempting overstatements: multiplicity is
   not a count of physical places, and
   `min(num(delta),2*ceil(delta))` is not a general theorem.
2. It kept the reviewed td8 route kill and td12 B-charge intact after the
   carrier correction, and found a second live td12 terminal with exact
   charge `4+4`, a rigid T1 template, and twin depth-16 source gates.
3. It advanced U2 in both directions: an actual adjacent direct U2 edge is
   finite at its licensed scope, while a one-P0 route supplies an explicit
   infinite family of locally legal records.  The latter is semilinear for
   each fixed scaling residue but not as one two-parameter family; it exposes
   the exact coverage/index debt rather than contradicting the direct-edge
   theorem.
4. It installed a semantic prompt appendix plus an explicit `charge_basis`
   declaration checker.  This is an inference firewall, not a claim that
   prose can be understood by regex.

The principal avenue remains the Sigray/M2 sheet ladder.  Its internal order
changes to: (i) td12 sibling/shared first-child recurrence, (ii) U2
row-to-actual-edge coverage and one-P0 gluing/landing, (iii) Q+E5 fixed-index
equal-regime consumer, then (iv) family-aware landing and cofinal/type control.
No global raise is licensed for avenues 25, 26, 28, or 31, and no monodromy,
Euler-ceiling, numerical-cap, or ambient counterexample search is launched.

## 1. Sealed round custody

Basis commit: `eaad172e59742ef8cfd055eace9bc6d3b2da8463`.

```text
65afb334763023765f701ee9a2140087a47c6ae470877cee0a04b4f4651c0d8f  state packet
cc58a521b92955b8c1e423aacdf60a5b619ba5384c9e05781bafec1ed7647b8c  Fable 5 blind
142d1e3dcd1305dda7397b5cd8f812080c2bf6d6eeb7106203fce9912777e451  Opus 5 blind
ec454b2e958690f396bbd14e901afcb1b5faf9204907911d27b8ab1c972cd98a  Grok 4.6 blind
ad50d1ada197a3ad424f321707ea62105fafc74a650d8b55e34cc1130c2efa84  Sol 5.6 blind

d3cf9d608427e4410ccb8b3ce04539fb22d854b9e58e6354d15115c3b48d564b  Fable cross-pollination
8bef2ec99ae230f0fd6795ad5b9ab6caba1f7275064b7b3fe9177825be772bea  Opus cross-pollination
d564e8afb359a23bbf1b8c36ef432203568f0aa1e36a1546ca988ee155ed024d  Grok cross-pollination
```

Blind body seals are Fable
`7c97c36fb9ba5b76d9d99c1ffd2c93232ac06d8bcee6f61472b921b20fbc3be0`,
Opus
`17dacd754d82ce19ae07b3ec21e6fa82f2134ccda6e15abb1eaafcfeae033c37`,
Grok
`349dfed1ecc23aed09a46b2ef0efb33d41ff96073302bad553ec6ad25482acad`,
and Sol
`06933494288cbf0bce05a24a4ac86c712359ddadf5103b81318e579a46f94a6e`.
Cross-pollination body seals are Fable
`5c3ae2c807d0722bcf43cceb4c11a17526abd8c208b7032c21cbcae9d8874322`,
Opus
`6e8a9d1947903a45a466d9230fda8bcd24efdbfed5ca4fb16dce6396a0813a9b`,
and Grok
`97814f6dbc00ecc9d1584c5654c2285d21822c44a3e72c2b323868ae0bcbd349`.

Agreement is used for scheduling, never as proof.  The decisive corrections
below depend on literal source/formula reconstruction, not a model vote.

## 2. Adversarial adjudication of the round's central proposals

### 2.1 Carrier types and the safe exit floor

Three objects must remain distinct: physical places, conjugate Puiseux
series on a cover, and cv flags.  For a nonzero up child, its multiplicity
`m` counts series, not physical places.  The promoted MFE set selects one
witness per priced direction and is a lower-bound subset, not the exhaustive
set of later actual flags.

The source-backed replacement uses the **full actual exit set**.  Every
physical place through an up child has a unique same-ray cv flag; distinct
flags are charged once, the place-to-flag map may be many-to-one, and no
cardinality equality with `m` is asserted.

Independent Fable review
`56e95db58e53aa030ef1100cc1640d115105a77607d40e3f160e77b46174a34d`
(body `00284d5fefa49f998439f772025f845b5d0050bd38dcb6967db637a1a045aa35`)
returns `PASS_WITH_REPAIR`: agreement/shedding statements are strictly below
the cv level, and exact-contact `kappa` can be choice-sensitive in the
printed convention.  At-level jumps only enter a more expensive branch, so
the theorem and calibrated conclusions are robust.

Let

```text
delta = D/m-kbar > 0.
```

At the reviewed vertex type, the safe lower floor for the total full actual
exit weight is

```text
L_safe(delta) = delta          if delta is a positive integer,
                ceil(2*delta)  if delta is nonintegral.
```

This is a lower bound, not an exact price.  The blind Opus formula
`min(num(delta),2*ceil(delta))` is false in general because integrality
applies to `q*(tau_0-kbar)`, not to `q*delta`.  Its proposed numerator branch
silently assumed `tau_0=D/m` or an equivalent divisibility premise.  The
three calibrating campaign examples did not expose the error because the
false formula and the safe floor coincide there.

The td8 trunk and td12 B results survive.  In the forbidden td8 singleton
`q=1` branch, singleton full coverage prevents physical-place divergence and
`q=1` prevents conjugate shedding, so all `2i` **series**, not `2i` physical
places, remain in the common prefix.  The td8 floor remains three and the
route remains killed.  For td12 B, the same two-mechanism split gives the
unique exact charge eight.

`ARITY-CEIL` is stopped: inserting characteristic vertices along fixed
places is invisible to the proposed exit ledger.  `EXIT-RH` remains an
untyped duplicate of a prior avenue-26 hope.  Neither raises a global avenue.

### 2.2 U2 notation and scope

In the U2 ODE, `C'` is a nonzero scalar label; it is not the derivative of a
polynomial `C`.  The blind Sol resonant family therefore does not refute the
direct-edge ODE theorem.  This is a correlated notation error in the blind
round, not wrong mathematics in the reviewed direct-edge result.  Future
prompts now carry a prime-label/derivative guardrail.

The actual adjacent direct-edge theorem survives at exactly its typed scope:
fixed pre-inner data leave finitely many T1-alive pairs, and the arithmetic
resonance is killed by an ODE with nonzero constant right side.  It cannot be
applied to a contracted stage-R row without adjacency, down-tree typing,
full degrees/indices, and the last `(nu,kbar)` frame.

### 2.3 Global lattice and `A(F)` proposals

`PIC-DISC` has a correct lattice identity, but its present value is as a
validator for a future complete, component-labelled landing record.  It is
not a present JC2 obstruction.  The pole-purity-to-`A(F)` bridge remains
blocked on component-surjectivity: pole purity does not construct the
component-labelled asymptotic packet.  Avenue 7/31 therefore does not move.

## 3. Portfolio disposition

- **Avenue 2, sheet ladder:** unchanged as the principal avenue, internally
  raised toward exit pricing, td12 source recurrence, and U2 coverage.
- **Avenue 3, strip/vertex ODEs:** unchanged as mechanism supply.  The U2
  ODE is a merge-local client; shared vocabulary does not reopen unrelated
  residue/Ore programs.
- **Avenue 4, germ/algebraization:** no pre-arm.  Arm narrowly only after a
  live route emits actual nonzero child coefficient data.
- **Avenue 6, one-place/AM:** no reopen.  A finite-depth pure-power bamboo is
  not yet the required one-place global object.
- **Avenues 7 and 31:** unchanged, blocked on the same component-labelled
  `A(F)` packet.
- **Avenues 25 and 26:** no raise.  Local flag arity is neither target
  monodromy nor a cofinal type bound; the census remains stopped.
- **Avenue 28:** unchanged at its lowered tier; no positive Euler accounting
  has appeared.
- **Avenue 36:** K00 remains the protected bounded characteristic-zero
  counterexample seed.  No ambient search is authorized.
- All other avenue dispositions remain as in the prior canonical survey.

## 4. Deduplicated idea set and decisions

1. **Full-exit safe floor / legacy repricing — launch and promote only after
   hostile source review.**  Recompute every nonintegral legacy defect that
   was recorded merely as `ceil(delta)`.  Representative MFE witnesses and
   raywise prices are not automatically eligible.
2. **td12 sibling first — launched.**  It was cheaper and logically prior to
   a 24-level B-child computation.  The result is in Section 5: the sibling
   survives and saturates its budget.
3. **Shared td12 first-child recurrence — next primary.**  Extract both
   child coefficient vectors from one coupled Keller/source recurrence,
   with degree/gcd or catalecticant tests as equivalent implementations.
   Record passed vectors as forward-generator data, not only booleans.
4. **Direct U2 review and row-to-edge coverage — continue.**  The theorem is
   promoted at actual-edge scope; the census refinement remains open.
5. **One-P0 nested-U2 family — retain as a coverage witness.**  Use it to
   design the family language and to seek an absolute-index/product-of-`nu`
   bound, source landing obstruction, or coefficient-gluing failure.
6. **Q+E5 `delta=0` fixed-index consumer — review before promotion.**  A
   fixed `(M_G,nu_G,kbar_G)` determines degrees and the factor-pattern slots;
   legacy `cell_check` is not a substitute because it searches the wrong
   indices and omits N1.  No engine migration is authorized yet.
7. **`PIC-DISC` / component-labelled landing — bank.**  Add its required
   component/intersection slots to the landing schema, but do not allocate a
   standalone proof lane before those objects exist.
8. **Systems firewall — implement.**  A compact semantic appendix is
   delivered to every model prompt, and declared exit claims may carry a
   machine-checked `charge_basis` record.  Absence is recorded, not guessed.

## 5. Post-cutoff theorem overlay

These results were produced after the blind and cross-pollination cutoff.
They are evidence about the round's proposals, not additional blind ideas.

### 5.1 Promoted/reviewed

- **Full-exit safe floor:** Sol source audit
  `05f68f4b7278a8ac1216ba82b40e7081bfff66f351380ccd671d12a955784d84`
  received Fable `PASS_WITH_REPAIR` `56e95db5...`. Literal “`m` places” is
  withdrawn; full-exit coverage and the piecewise safe lower floor are
  promoted with the strict-below and exact-contact-`kappa` riders.
- **Direct nested U2:** Sol producer
  `99bbe233f8f6f7273b2e5f89705aa5f2189681ce649a8723a66a0f99fa92912b`
  received Fable `PASS_WITH_REPAIR`
  `5cf66cd018e938cbc07cdeb9623baf28c667122ca602736154b8a694ec0142d7`;
  the source/index delta received a second Fable repair pass.  Maximum scope:
  actual adjacent edge only.
- **td12 sibling charge:** Sol producer
  `52ffafa2e79823e275e084d9d3c3a36329401cc9449a6e572379ce1ee0390b69`
  received Fable `PASS`
  `70cf67b2241b1361e958e673b81d883f34a87be9e5fe3631a3a36c35646c3060`.
  The sibling is `(nu,dp,dq)=(17,68,52)` with two distinct nonzero
  directions, exact charge `4+4`, and one `q=1`, `tau_0=17` flag per
  direction.  Its source gate is 16 pure-power levels per direction.
- **td12 sibling T1:** the exact solve gives
  `B+C=9A/4`, `BC=45A^2/32`, hence
  `B/A=(9+/-3i)/8`.  Grok's independent review
  `432a4152387cff943e220a6236912f2481a1ea3004d2c53ae95f14eb267f1ab0`
  returns `PASS`.  The template is one unordered ratio point after common
  dilation, not a continuous modulus.
- **td6 LR2 carrier repair:** the unique-cv conclusion survives after adding
  the missing bridge: pole-mass saturation makes every x-place non-polar,
  Proposition 7.2 supplies a cv vertex on every such ray, and uniqueness
  forces the common vertex.  Splitting/first characteristic strictly above
  it remains possible.

### 5.2 Reviewed with a record repair

The old `t=2` one-P0 U2 ray is uniformly dead by N1/L6.  The repaired
`t=5`, `K=6q+1` ray survives every reviewed local predicate and gives
primitive indices `(1,2,10K)`.  Opus review
`84c648a721f42d8197a9c78648795e9830e5bb75521a3e00a8a9d5a80f495cb6`
returns `PASS_WITH_REPAIR`: the sealed producer displayed the inner absorbed
degree as `dq_0=4`; it must be `3`.  The correction delta is
`4dde1c471b04d88db466293bc197c78529bc16743cc7ccde176e594bceb622dc`.
For each fixed `t=5 mod 6` the route set is semilinear; varying both `t` and
`K` is bilinear and not one semilinear family.  Absolute-index or
`prod(nu_seg)` boundedness remains a decisive uncovered consumer.

### 5.3 Provisional, still review-gated

- The legacy repricing sweep is sealed at `aaa7496b...`/body
  `2d873e68...`: td12 is unchanged, while four LL-1 defect-`2/3` cells rise
  and the frozen alive/fragile inventory provisionally falls from 13 rows to
  7. An Opus review attempt was quarantined as
  `INPUT_MUTATED/NO_PROMOTION` after `ladder/BOOK-OFFAXIS.md` changed during
  the run; diagnostic `847ca047...`/body `58964a03...` preserves no hostile
  pass.  A fresh stable-basis arithmetic/scope review is required before the
  canonical LL-1 ledger changes; generic MFE representative/raywise floors
  are explicitly excluded.
- The Q+E5 equal-regime slot map derives the cap
  `kbar_G <= mu*w*(2+1/(M_G*nu_G))` and a no-partition-search existence
  predicate.  Its Grok review likewise failed closed on a live source
  mutation (`INPUT_MUTATED/NO_PROMOTION`, diagnostic `6d7361b4...`/body
  `d03ddad0...`).  A fresh different-model review is required before
  canonical promotion or software migration.

## 6. Ranked next actions and parallel allocation

1. **Shared td12 `BChildJet` extractor.**  One primary derives the first
   child vectors for both the depth-24 B direction and both depth-16 sibling
   directions.  A different model reviews in the background; descendants
   proceed provisionally on clean exact vectors.
2. **U2 coverage split.**  In parallel, prove stage-R row-to-actual-edge
   refinement for zero-length edges, and analyze source/gluing or an
   absolute-index bound on the one-P0 family.  Positive P0 segments remain a
   separate composition class.
3. **Legacy safe-floor reprice.**  Complete and hostile-review the changed
   cells, then rerank only the affected finite residue books.  Do not apply
   the rule to representative-witness floors without a full-exit theorem.
4. **Q+E5 fixed-index review.**  If it passes, implement a new pure consumer
   with typed `nu_G`, `M_U`, arrival provenance, and N1.  Keep `NUCAP=500`
   and legacy verdicts untouched until regression coverage passes.
5. **Landing/LL-2 schema.**  Preserve family keys, component labels, source
   indices, and `PIC-DISC` intersection slots.  This is infrastructure for a
   future theorem, not a numerical enumeration.
6. **Protected counterexample work.**  Maintain K00 as a bounded parallel
   client; do not let formal td12/U2 records displace an actual
   characteristic-zero source seed.

Peer roles: Fable remains strongest on broad source synthesis and exact
review; Opus uniquely caught both correlated round errors (the false exit
formula and the `C'` misreading) and is the preferred adversary for compact
formula claims; Grok supplied useful skepticism and independently verified
the rigid td12 T1 solve; Sol coordinates, composes theorem interfaces, and
owns cross-lane deductions.  Identity does not count as a vote, and no model
is reserved only for review.

## 7. Systems and resources

`FALLACY.md` is appended exactly once to every `ops/lane.sh` model prompt
from a hash-pinned secure snapshot.  It covers carrier typing, per-ray versus
exit-set charge, pole/interior typing, floor versus attainment, CAS
saturation, quotient remainder degree, ring maps, and prime-label ambiguity.
The lane records original and composed prompt hashes, detects mutation, and
cleans its private temporary directory.  `ops/validate_charge_basis.py`
checks explicitly declared exact rational deltas, branch type, flag count,
and citation; it records `ABSENT` but never infers semantics from prose.
Focused fake-adapter regression covers exact-once delivery, cleanup,
mutation quarantine, missing/oversize failure, valid fixtures, and invalid
`q=1-exact` nonintegral declarations.

Every mathematical action in this round is desk-scale.  AWS remains idle by
design and is reserved for a source-reviewed joint coefficient/CAS packet.
No heavy computation is authorized locally.  The separately owned
formalization tree remains outside the campaign boundary.

## 8. Stop conditions and nonclaims

Stop any inference that identifies a place, a conjugate series, and a flag;
promotes a lower floor to an attained price; reads a prime label as a
derivative; treats a selected MFE witness set as exhaustive; applies an
actual-edge theorem to an untyped stage-R row; or converts a formal route
into a germ/map.  No result here supplies total landing, coefficient gluing,
polynomial realization, a cofinal type/degree ceiling, a counterexample, or
a proof of JC2.

*End of sealed synthesis body.*

---

## Seal (outside the sealed body)

The sealed body is exactly the first `17220` bytes of this file, ending with
the newline immediately after `*End of sealed synthesis body.*`.

Body SHA-256:
`d62e2b7701d24b50aec63bd50d00298a4dc734c40d65d16cf38e1c51dc6e304e`
