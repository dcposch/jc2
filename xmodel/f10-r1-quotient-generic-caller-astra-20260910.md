# F10 quotient/remainder generic caller: narrow STATIC adaptation

Status: COMPLETE STATIC PREPARATION; UNREVIEWED delta and UNEXECUTED. No registration, deployment, retry, mathematical evidence or source acceptance is issued. Owner `/root/model_productivity`; basis `0d39df3c9fd69c939a8420c54d03228b9077777d` is publication provenance only.

## Clock, authority and exact read scope

First action2026-09-10T01:32:10.780464537Z; exact owned box and report were absent. Fixed controlling cap01:51UTC, earlier than first+20minutes; reserve01:48. No clock reset. ROOT-CARD SHA e56d1b76e05e36239b0339da2bc57209a825dfb2b0320a21f740ae2239cd508b was verified before its whole read, then all twelve named inputs matched before body access/reuse. All13 match again in owned `input-pins.json` at01:42:06.444379UTC and will be compared again at terminal custody.

WHOLE fresh reads: root card, accepted old223line dispatch_batch.py, both old static/runtime gates, new generic_controls.py and unchanged execution_gate.py. WHOLE prior same-byte reads explicitly reused after current matching: new quotient static gate, solver.py, checker.py, evidence.py, algebra.py, run_capped.py and probe.py. A selected evidence.py reread supplements, rather than substitutes for, its earlier whole read. Initial combined output truncation was resolved by separate whole reads of both old gates. No provenance links, live R2 packet, other executor body, runtime artifact, current ledger, corpus, network, AWS, process state or protected path was inspected.

The card alone supplies operational context: first attempt stopped at SSH; second stopped after only its disabled probe because its authority digest was recorded post-launch. Neither supplied generic mathematical evidence. No attempt is inspected or resumed here. Old17zb/zc reviewed caller is an implementation parent, not reusable authority. Its constant-v fixture coverage, partly structural counts and missing independent live-child capture remain qualified. New17zh mathematical kernel/harness acceptance is unchanged and does not cover this new caller until its FIRST delta review.

## Deliverables and actual correction

Owned dispatch_batch.py SHA54c55be3db27207a2c22072699a19d80c2be6a882e5de7e237fd2bd6f57f1ed0 is a literal adaptation of the charged old caller, with owned `caller.diff` SHA49ca33133cd2f537774b431cfb08b5192ffcd1a43575c92704b4ec34d5e25dc8. The diff is generated by plain text `diff -u` with fixed labels, not by code execution. No solver, verifier, scientific helper, probe or CAPRUN was copied into this box or modified.

The prospective CLI adds exactly one outer argument: the external ROOT registration SHA256. It is checked against the regular, non-symlink `dispatch.registration.json` before parsing. That externally pinned registration supplies the caller's own hash, interpreter and native closure; all task pins are fixed literals except those explicitly supplied by ROOT. The new outer schema is F10-QUOTIENT-GENERIC-DISPATCH/v1; per-child F10-R1-REGISTERED/v1 is unchanged. Outer source-acceptance keys are forbidden; child authorities carry generic_only=true and univariate_acceptance=null. The disabled template has all runtime selections null and cannot create authority or STOP artifacts merely from failing its disabled/outer preflight.

The critical correction is inside `run`, not an operator convention. Every operation rejects existing output paths including symlinks, creates its authority exclusively, chmods0444 and computes its digest before launch. It checks the entire original fixed map, writes/freezes/hashes a prelaunch metadata record, then repeats original fixed-input and authority checks immediately before the sole Popen. It repeats the wall+15 admission after those checks. The missing-input control deletes only its child-map plan entry; the caller's original fixed map still checks the plan. Post-pins remain additional evidence, never a substitute for prospective checks. Prelaunch timestamp, literal argv, full original fixed map and authority digest are retained before launch; terminal dispatch records bind that prelaunch record and the authoritative telemetry.

During manual draft reading an overbroad no-symlink check on every fixed input was removed: the retained `/usr/bin/python3` spelling may itself be a registered OS symlink. Fixed inputs are checked as files against their exact registered content hashes, preserving the old contract; native-closure paths retain the old resolved-path/disjointness checks. Authority and output symlink refusals remain. This was a static correction, not an observed runtime failure.

## Exact payload and receipt mapping

New generic.plan.json SHAce193898e3114f1db018b86e6ebe0b2ea4fdac5f65296dee76245f94da0b964a contains only synthetic scope, eight/four ordered names and expected branches/refusal reasons, hashes, CLI and proposed caps. Its literal hash is embedded in the caller. No fixture coefficients, new mathematical construction or actual source input exists. It is the probe input and admissibility binding, not a second argument to the mathematical harness.

The same seven controls execute in their old order: five deliberate refusals, gate-valid, dummy-descendant. Each refusal now additionally needs its exact expected error substring; an unrelated nonzero exit does not pass. Gate-valid and dummy require exact POSTAUTHORIZE newline bytes. Dummy retains32MiB sampled group RSS exceedance, exact MATCH/TERM/MATCH/KILL sequence, matching leader/start/namespace/PGID, quiet group and reap checks. CAPRUN and its literal parent/child vectors, preexec file limit and start_new_session=False are retained. No supervisor, inner group wrapper or monitoring framework was added.

Only after those controls does one `generic_controls.py AUTHORITY OUTPUT` child run with operation build. Its mathematical scope remains exactly the frozen8positive/4changed-object controls. Before dispatch, the receipt and all20 exact sidecars must be absent. After CAPRUN finishes and its recorded group is quiet, the caller checks the full summary schema/status/scope, exact ordered names, authority/host/job/instance, engine, solver/harness hashes, positive branch/count dictionaries and four exact ValueError predicates. Duplicate/nonfinite metadata JSON is refused. All20 embedded path/hash bindings are compared with their exact regular files; receipt bytes/hash are retained and rechecked.

Important literal format distinction: the frozen harness embeds sidecar hashes but not byte sizes. The caller measures and records sizes; it does not claim to compare a nonexistent embedded-size field. No fixture JSON, certificate coefficient or mathematical witness is parsed/replayed by the caller. UNIT217-coordinate and SEPARATOR1638-column/217-target checks remain solely in the frozen independent harness; the caller only binds their recorded counts. No old5positive/7negative tally, native-operation count or old compatibility CLI survives.

Profiles remain exactly probe5wall/3CPU/2GiB, dummy5/3/32MiB and generic180/170/2GiB; aggregate240 and16MiB inherited file cap. Absolute deadlines and first-math aggregate deadline are stored and never reset; late return or late final metadata verification prevents PASS. Partial output is preserved on STOP, without retry. The outer SYSTEM containment, live unit/cgroup/CAPRUN/child capture and root stop timer remain separately registered root duties. The caller's inherited CAPRUN namespace/stat capture is not an independent observation of the mathematical child.

## Static checks, limits and handoff

STATIC-NOTES.md contains the exact delta, manual mutation/control reasoning and disabled future interface. Those branches are NOT executed tests: changed authority/fixed-plan bytes, dangling output links, wrong refusal reason, CRLF sentinel, omitted/reordered receipt entries, wrong integer-count types, stale hashes and wrong execution binding were traced only by reading the displayed predicates. Full own caller was read through EOF, its final small edits reread, and literal diff equality checked. No syntax/import/AST/compile/test, fixture generation, CAS, code run, native API exploration or mathematical subprocess of any size occurred. Only documentary reads/hashes/text diff, apply_patch authoring and existing artifact publication were used.

Runtime correctness/timing/native behavior remain unknown. Cooperative freeze/hash barriers do not establish isolation from a malicious same-user writer. FIRST Fable targeted static-delta review and a fresh physical ROOT registration, with independent live capture and terminal intake, remain prerequisites. There is no automated third attempt, worker selection, active deadline, actual-source acceptance, mathematical point/unit verdict or follow-on authority in this packet.

Raised mathematical OPEN count0; no new mechanism or research ID. The smallest remaining check is the specifically requested FIRST static delta review, especially external registration binding, prospective original-pin barrier and exact new summary contract. No review or execution is launched here. Own final/report-transaction collision check and whole-body read precede the unique completion marker. Final custody will bind all owned files and13 unchanged inputs; it excludes itself and precedes root intake. All writers become IDLE after close/finalize/expected verification and that last custody write.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `9438`.
- Body SHA-256:
  `6460fb51623a01615865ae8478cd83aaf7744dd93e7a704d6bcaa551cf1e3a88`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
