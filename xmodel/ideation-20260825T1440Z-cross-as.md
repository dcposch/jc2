# Adversarial cross-scan — AS owner — 2026-08-25 14:40Z

I read the four sealed `1440Z` submissions only after collection closed.  This
is a stress test, not mathematical promotion.  The independent convergence is
real: all four choose source-complete coefficient/cover objects over more
point sampling, and all retain the global landing/cofinal-ceiling firewall.

## 1. The proposed `Z/9` Bockstein gate is fibrewise, not globally constant

Fix one literal integer Q3 point `F_*` and write fresh fixed-support output
digits as

```text
F = F_* + 27 U + 81 V.
```

Modulo 243 the determinant identity is linear in `T=U+3V in Z/9`: the
quadratic fresh term is multiplied by `27^2=729`.  The same term vanishes in
a displayed terminal quotient `/243 mod 3`.  Moreover, on the three reviewed
pointwise Q3 fibres every certified Q3-kernel difference is asserted to be
`81 K` with the same D7 support, so its mixed term with `27 U` is divisible by
2187.  **At that fixed-point/fixed-fibre scope**, an exact integer-source
check of `delta F in 81*M_D7` makes the `Z/9` module claim sound through
determinant modulus 243 (and through `/243 mod 3`).

It is not yet sound over the whole predecessor scheme.  Earlier accepted
coordinates can enter the literal map at order 9, and then

```text
(9 K)*(27 U)/243 = K*U
```

survives modulo 3.  Thus a simultaneous system in predecessor coordinates
and fresh digits is generally bilinear even though it is linear in `U,V`
over a *fixed* coefficient base.  The honest global object is a matrix over
a named base algebra/stratum

```text
R = (Z/9)[predecessor coordinates] / I_predecessor,
```

with its nilpotents and 3-torsion retained.  Rank must be treated by Fitting
localizations; a single F3 RREF is not licensed.  The Bockstein must be
derived from the integral source algebra before reducing, because division
of a relation by 3 is lift-dependent over a nonflat/nonreduced quotient.

Smallest discriminator: on each of the three fixed reviewed Q3 fibres, emit
the exact valuation table for every kernel column and check every mixed
Hessian term against modulus 243.  Separately, include one live order-9
predecessor coordinate and exhibit either the surviving `K*U` coefficient or
an original-row identity killing it.  Freeze the fibrewise `Z/9` theorem if
the first passes; do not globalize it unless the second is killed on every
Fitting stratum.  The present preregistered gate imposes determinant modulo
243 only and explicitly omits terminal-mod-729 closure, so it cannot by
itself be called a chronological Q2/Q1 completion.

## 2. Q8 infinity needs the closure of the source open, not just homogeneous rows

Homogenize-before-specialize is necessary but not sufficient.  The infinity
scheme must be proved equal to the projective closure of
`I:(w*x5*(x3-2*x5))^infinity`; homogenizing the six generators and then
saturating must be compared by contraction with homogenizing the already
saturated source ideal.  All source relations/localizer equations must be
homogenized, followed by irrelevant-ideal saturation.  Otherwise embedded
components created or lost at `C1=0` can change the fibre.

The current `C0=1,C1=0` chart also keeps every other coefficient affine and
fixes `d4=1`.  It therefore cannot close paths where `d4` or another source
coordinate has a pole relative to `c`; those require a joint multi-projective
or weighted chart.  Finally, setting the pointed variables to zero tests one
landing centre, not the whole infinity fibre.  Cheapest fail-closed check:
compute both orders of `saturate/homogenize`, contract them to the same chart,
and compare radicals plus original-row cofactors; then add the first chart in
which `d4` scales with `c`.  A unit only after these checks is a source-honest
infinity exclusion at the selected leaf.

## 3. The TD6 five-piece cover is a valid topology plan, not yet a source theorem

On `D(U)` with `T=V^2/U^3`, the displayed identity

```text
((5T-136) QHnorm - (5T+64) P3norm)/512 = 1
```

is correct in characteristic zero.  It proves only that `P3=QH=0` is empty
there.  Together with `V=0` it gives the advertised five *set-theoretic*
strata: generic `D(U V P3 QH)`, `V=0,D(U)`, `P3=0,D(U)`, `QH=0,D(U)`, and
`U=0`.  It does not prove either divisor stratum empty and it does not carry
the V64 source identity across them.

The normalization uses powers of `U^{-1}`, so every Bezout coefficient must
be cleared back in the original `H=0` source ring and the `U=0` leaf rebuilt
from raw rows.  Embedded/nilpotent structure also matters for transverse
successors even if field-valued emptiness is set-theoretic.  Cheapest
certificate: one manifest whose five leaves each replay the original rows
and whose localized ideals have an explicit radical/Bezout cover.  Until
then, V64 remains exactly the reviewed generic open.

## 4. Greenberg and positive genus: useful only after two missing hypotheses

For one fixed finite-support coefficient scheme over `Z_3`, compactness
already implies that emptiness has some finite congruence witness; Greenberg
can make approximation quantitative.  It does **not** supply a small or
known cutoff, nor does it apply to changing support or filtered/incomplete
states.  The cheapest honest use is to compute an effective Artin/Greenberg
bound on one fully source-complete local coefficient scheme after its
integral presentation and Jacobian/Fitting strata are frozen.

Positive genus can obstruct an actual asymptotic-set component only after a
source component lands and the map to that irreducible component is shown
birational (or otherwise genus-controlling).  A positive-genus cover of a
rational curve is perfectly possible, and genus can misbehave under
untracked specialization/component merger.  The cheapest discriminator is
therefore the function-field degree from the landed boundary component to
its proposed `A(F)` component, plus flat component tracking across the genus
specialization.  Without degree one and honest landing, no cofinal genus wall
follows from passports.

## Allocation verdict

The shared constructible-cover/Fitting compiler is the strongest independent
convergence.  The hidden common failure mode is commuting projection,
localization, or specialization with source closure.  Recommended allocation
is **Q8 40 / AS 30 / TD6 25 / global 5**: Q8 remains the nearest selected-leaf
closure; AS gets a bounded fixed-fibre `Z/9` falsifier plus the global mixed
term test; TD6 gets one exact five-leaf cover; Greenberg/genus stay reserve
until their missing hypotheses are named and checked.
