# f10-all-r-late-unit-composition-gate-fable5-20260910 — FIRST Fable 5.1: universal late-column unitness and actual affine elimination

status: UNSEALED (adapter seals; no Seal section and no charge_basis authored here; the standalone BODY-END marker is appended LAST)
lane: f10-all-r-late-unit-composition-gate-fable5-20260910
first_action_utc: 2026-09-10T22:05:22Z (both owned targets verified absent, then sha256sum -c of all eight inputs: 8 OK before any body read)
stop_utc: publication reserve 22:19:00Z, HARD 22:24:00Z; USER TERM 22:24:00/KILL 22:24:05 armed before launch; RuntimeMax 1200 s may end earlier; never reset; independent of the author's earlier 22:02/22:05 clocks
mode: manual mathematics, static text, hash and metadata only; ZERO subprocess/CAS/import/AST/compile/test of any size; no coefficient, candidate, baseline or fixture body exists or was read; no network/AWS/SSH/process/Git/agents/corpus/protected/shared/live edits
owned: xmodel/f10-all-r-late-unit-composition-gate-fable5-20260910.md and box/f10-all-r-late-unit-composition-gate-fable5-20260910/input_custody.md, both absent at first action; every authored byte via apply_patch only

## 0. Inputs, read attestation and premise scope

Exactly eight charged inputs in /tmp/jc2-lane.DXalaj/inputs; all eight current hashes equal the charged list (sha256sum -c over stdin, 8 OK, exit 0, 22:05 UTC) BEFORE any fresh WHOLE read; the table is in the owned input_custody.md. Each file was then read WHOLE by one bounded unclipped cat reaching its EOF, Seal or standalone marker: ROOT-CARD (14 lines, EOF), the new Astra report under review (187 lines, marker at 178, Seal 180-187), the actual FIRST (87 lines, marker at 87), the ROOT execution report (50 lines, marker at 41), the 17v producer (134 lines, marker at 125) and its gate (77 lines, marker at 77), the 17w producer (303 lines, marker at 294) and its gate (83 lines, marker at 83). The announced sizes (187 lines/16362 B and 87 lines/27884 B) equal the observed sizes; nothing was clipped.

Premise scope. 17v is imported at its accepted scope: for any commutative Q-algebra B with (1) and a a unit, the literal ideal equality (g1,g0)=(H6(alpha),H7(alpha)), the rational pivots delta_l=ml-3(n-m+j) and the residual positions [S^(3r+1+j)]E1, [S^(4r+1+j)]E0. 17w is imported for its literal finite contact expressions, its u=0 partitions and the W guard, with the corrected Cramer sign e_y=+12(1+L)E/Gamma. The actual FIRST and the ROOT card supply P: for every r>=2, 1<=j<=r-1 there is no complex (V,W) with all four contact rows zero at x=nu=(5r+2)/(3r+1), y=alpha=(5r+2+j)/(3r+1) for the normalized cubic 1+t+Vt^2+Wt^3, W=0 points included; ROOT's s-range correction (10/3,11/3) is documentary and enters nothing below. None of these foundations, coefficients, runtimes or sources is re-reviewed. "Derivation" below is my own manual check; "import" is an accepted statement used at its stated scope. Leading unitness is never read as source emptiness.

End of section 0.

## A. Universal leading ring, formal identity (3), whole-ring reciprocal unit, rescaling (4): CONFIRMED

Ring. L_r=Q[F,H,a,b,d1,d2,d3,d4,eta]/(all coefficients of mCD'-nC'D+theta^7, eta*a*b-1) is the universal algebra of the accepted 17v data (1): C monic cubic with C(0)=a, D monic quintic with D(0)=b, a and b units (inverses eta*b, eta*a), the exact ODE. Every B with (1) receives a unique map. It may be zero or nonreduced; nothing below assumes otherwise.

Derivation of (3). Put c=C/a, f=D/b, both with constant term 1; only the guard units a,b are inverted (the producer's line 43 says "only a and the integers"; b is also inverted to form D/b, harmless since b is a guard unit). Then mCD'-nC'D=mab(cf'-nu*c'f), so cf'-nu*c'f=-theta^7/(mab) has order >=7. The binomial series g=c^nu is a well-defined formal series over any Q-algebra (c-1 has zero constant term) and satisfies cg'-nu*c'g=0 identically. For h=f-g, h_0=0 and ch'-nu*c'h=O(theta^7); its theta^(i-1) coefficient is i*h_i-sum_(l=1..min(3,i))((nu+1)l-i)c_l h_(i-l), i=1..7, so h_1=...=h_7=0 by induction, dividing only by the integers 1..7. Hence D/b=(C/a)^nu+O(theta^8); deg D=5 gives H6(nu)=H7(nu)=0 in L_r. This is a finite recurrence with nilpotents allowed, not an analytic branch. CONFIRMED.

Reciprocal unit. u=H/a, v=F/a, w=1/a. In L_r/(u)=L_r/(H) (a is a unit) the 17w partition formula f_N=sum_(i+2j+3l=N)(X)_(i+j+l)u^i v^j w^l/(i!j!l!) keeps only i=0: N=6 gives (0,3,0),(0,0,2), N=7 gives (0,2,1), so H6(nu)=(nu)_2 w^2/2+(nu)_3 v^3/6 and H7(nu)=(nu)_3 v^2 w/2, exactly 17w lines 32-33 and the producer's display. nu is in (5/3,2), so (nu)_2,(nu)_3 are nonzero rationals; w is a unit. H7=0 forces v^2=0, hence v^3=0 (no inference v=0); then H6=0 forces w^2=0, so 1=0 in the quotient. L_r/(H)=0 means H, and u=H/a, are units of L_r (an element is a unit iff the quotient by it is zero; reducedness irrelevant). CONFIRMED.

Rescaling. theta=t/u gives (C/a)(t/u)=1+t+(v/u^2)t^2+(w/u^3)t^3 with v/u^2=Fa/H^2=V and w/u^3=a^2/H^3=W; the binomial series commutes with the continuous substitution theta->t/u, so [t^i]c(t)^X=u^(-i)H_i(X), which is (4). W has inverse H^3/a^2 and zeta*W-1 maps to 0. u_rec=H/a is not 16r's source parameter u_src; no source parameter is inverted or set to 1, no scale silently discarded, and the 17w field-point normalization is upgraded to the whole ring L_r. A CONFIRMED.

## B. Accepted P => T_(r,j)=0 => I=L_r => globally unimodular column: CONFIRMED

T_(r,j)=Q[V,W,zeta]/(H6_c(nu),H7_c(nu),H6_c(alpha),H7_c(alpha),zeta*W-1) is a finitely generated Q-algebra whose C-points are exactly the pairs (V,W) in C^2 with W!=0 and the four literal contact polynomials zero at nu and alpha. Accepted P (actual FIRST E/F at x=nu, y=alpha; a(X)=720H6/(X(X-1)), b(X)=5040H7/(X(X-1)(X-2)) differ from H6,H7 by nonzero rationals at these exponents) says no such point exists, even without the W guard. If T!=0 it has a maximal ideal m; T/m is a field finitely generated as a Q-algebra, hence by Zariski's lemma a finite extension of Q, embeddable in C, giving a C-point. So T=0: the unit ideal, not a radical or nilpotence statement; no whole-locus finiteness, reduced point count, chosen root or field-only argument is used; the zero ring is the conclusion, not an obstacle. Finite generation is all that is needed (finite presentation is automatic).

Map. V->Fa/H^2, W->a^2/H^3, zeta->H^3/a^2 into L_r/I, I=(H6(alpha),H7(alpha)). By (4) the image of H_i_c(X) is u^(-i)H_i(X): zero at X=nu by (3), in I at X=alpha; zeta*W-1 maps to 0. Thus a unital Q-algebra map T->L_r/I exists; 1=0 in T forces L_r/I=0, i.e. I=L_r. 17v's ideal equality in B=L_r gives (g1,g0)=L_r: lambda1,lambda0 in L_r with lambda1*g1+lambda0*g0=1. Since (g1,g0) is built polynomially from a,F,H,d_i with rational pivots, the canonical map L_r->B carries this identity to every B with (1): every factor, nilpotent thickening, quotient and extension, the zero ring included. Existence only: no cofactor, height, degree or cost; per fixed (r,j) with no uniform formula in r,j. No reciprocal chart is missing because u is a unit of L_r itself (A). Nothing here says the SOURCE ideal is the unit ideal. B CONFIRMED.

## C. Actual affine variation, pivots (7), residual rows (8), boundary forcing: CONFIRMED

Derivation of every coefficient of L=mCV'-beta*C'V+j*k*D', beta=n-m+j=2r+1+j, V=v2theta^2+v1theta+v0. CV': [theta^4]=2v2, [theta^3]=v1+2Fv2, [theta^2]=Fv1+2Hv2, [theta^1]=Hv1+2av2, [theta^0]=av1. C'V: [theta^4]=3v2, [theta^3]=3v1+2Fv2, [theta^2]=3v0+2Fv1+Hv2, [theta^1]=2Fv0+Hv1, [theta^0]=Hv0. D': 5, 4d4, 3d3, 2d2, d1. With the complete fixed forcing R_i (targets and earlier mixed brackets, never set to zero) the rows theta^4,3,2 read (2m-3beta)v2+5jk+R4=0, (m-3beta)v1+2(m-beta)Fv2+4jd4k+R3=0, -3beta*v0+(m-2beta)Fv1+(2m-beta)Hv2+3jd3k+R2=0, which is (7) exactly with delta2=2m-3beta=-(3j+1), delta1=delta2-m, delta0=delta2-2m, all nonzero rationals using no source coefficient. Rows theta^1, theta^0 are 2ma*v2+(m-beta)Hv1-2beta*Fv0+2jd2k+R1 and ma*v1-beta*Hv0+jd1k+R0, exactly (8). Substituting V=V_part+kV_lin(1) makes both affine in k with k-coefficient the 17v homogeneous column (g1,g0) and constant F_i carrying every R_i, at the accepted positions [S^(3r+1+j)]E1, [S^(4r+1+j)]E0. The C'V coefficient is -beta, consistent with 17v and its gate.

Boundary j=r-1. The S^0 t^3 slot B3(0)=u_src^2 is fixed with zero k-variation (17v gate A), so it enters R4,R3,R2 through the same operator as fixed forcing; its own theta^5 row and the target -u_src^2 t^5 at weight 5r=4r+1+j stay in the outside ideal K of section 5. Nothing is discarded because deg V<=2; no k_0 gauge, no full-source inventory, no inversion of u_src. C CONFIRMED.

## D. Unimodular two-by-two elimination, two-sided maps, outside ideal: CONFIRMED

U=[[lambda1,lambda0],[-g0,g1]]: det U=lambda1*g1+lambda0*g0=1; the adjugate [[g1,-lambda0],[g0,lambda1]] is U^-1. U applied to (F1+g1k,F0+g0k) gives (f+k, g1F0-g0F1)=(k+f,Psi) with f=lambda1F1+lambda0F0. U is invertible over T, so (F1+g1k,F0+g0k)=(k+f,Psi) in T[k] and T[k]/(F1+g1k,F0+g0k)=T/(Psi) with k->k*=-f and inverse the inclusion, i.e. (9). Signs of (10): F1-g1f=F1(1-lambda1g1)-g1lambda0F0=lambda0(g0F1-g1F0)=-lambda0*Psi and F0-g0f=lambda1*Psi. CONFIRMED. (11) for an arbitrary outside ideal K: T[k]/(k+f,Psi,K)=T/(Psi,K(k*)); guards and the leading unit a*b are untouched because no k enters leading data. All maps are polynomial identities, stable under arbitrary base change and nilpotents; neither entry is divided; Psi is retained.

Own remark (non-blocking): k* is well defined modulo Psi independently of the Bezout choice, since any syzygy of the unimodular pair is a multiple of (g0,-g1) and changes f by a multiple of Psi; so (9) is canonical and only its representative depends on lambda. Successive stages: the k_j-linear terms at weight 4r+1+j come only from J(dA_j,B_n) and J(A_m,dB), so every late column depends on leading data alone and its Bezout identity survives each substitution; rows of smaller j receive k_j through K. D CONFIRMED.

## E. Changed-hypothesis controls: CONFIRMED

j=0: alpha=nu, so H6(nu)=H7(nu)=0 by (3) and g1=g0=0 identically; L(k,0)=0 and k_0 is the A-constant gauge. No unit column exists there, so the theorem genuinely needs j>=1, and P is stated for j>=1 only. (t,1-t) over Q[t]: t+(1-t)=1 with neither entry a unit; t*k=0 alone allows k!=0 on the fibre t=0, while both rows give k=(t+1-t)k=0. This discriminates unit column from unit entry, hence exposes entry division. F=(g1h,g0h): Psi=g1g0h-g0g1h=0, f=h, unique k=-h. F=(-lambda0,lambda1): f=-lambda1lambda0+lambda0lambda1=0 but Psi=lambda1g1+lambda0g0=1, and the rows imply -1=0 over any nonzero base. Together these refute "unit column implies inconsistency" and "the second row is automatic": the scalar reconstruction can hold while the pair fails. All are abstract changed-algebra controls; no actual source point is inferred. The proof uses both contacts, as 17v's ideal equality requires. E CONFIRMED.

## F. Theorem interface: CONFIRMED as scoped; actual compatibility obligation remains (external)

Licensed now that P is accepted at ROOT's scope: for every r>=2, 1<=j<=r-1 and every commutative Q-algebra B carrying the 17v leading data (1), the late column (g1,g0) generates the unit ideal of B, with Bezout coefficients pulled back from L_r; for the actual affine pair F1+g1k=F0+g0k=0 the variable k_j is eliminated as k*=-f in T/(Psi_j), and (11) carries every other row, boundary forcing and guard. Not licensed: a source unit ideal, source emptiness, an r bound, all-F10, global degree, Keller or JC2; the result is late linear algebra over the whole leading base. Owed: the joint vanishing/nonvanishing of the actual compatibilities Psi_j together with the substituted outside ideal, i.e. whether T/(Psi,K(k*)) is zero, which no charged input decides or computes. Explicit Bezout representatives are not needed for the abstract theorem (existence plus the canonical identity (9)); they are needed only to execute a substitution. No component enumeration, highest solve, extra source arrow or foundation review is requested or authorized. F CONFIRMED as scoped.

## Verdicts and smallest defects

| item | verdict |
|---|---|
| A L_r, formal (3), whole-ring unit u=H/a, rescaling (4), guard | CONFIRMED |
| B P => T_(r,j)=0 => I=L_r => unimodular column in every B | CONFIRMED |
| C operator, pivots (7), rows (8), j=r-1 boundary forcing retained | CONFIRMED |
| D det 1, inverse, (9)-(11), signs, outside ideal, base change | CONFIRMED |
| E four controls discriminate closure and entry division | CONFIRMED |
| F interface; remaining actual compatibility obligation | CONFIRMED as scoped; GAP external |

REFUTED: none. Smallest genuinely blocking defect: none. Non-blocking defects: (a) line 43 says the recurrence divides only a and 1..7; b is also inverted to form D/b (guard unit); (b) display (2) at line 21 uses tuple notation for an ideal equality, clarified in the next sentence; (c) the header's "pending FIRST review" wording is historically correct per the ROOT card. The conditional premise P is now discharged at exactly the producer's stated scope (all r>=2, 1<=j<=r-1, complex V,W), so the lemma stands unconditionally there.

## OPEN(S) RAISED

None. No new canonical OPEN ID. The remaining actual compatibility question (F) is the producer's own stated obligation, restated, not raised.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly this report and box/f10-all-r-late-unit-composition-gate-fable5-20260910/input_custody.md, both absent at first action; apply_patch only; no Seal, no charge_basis, no Write/Edit, no redirection into owned files, no helper-generated text, no source edit, no execution of any input, no corpus/provenance/live-body scan.

## Completion

Authoring attestation: custody file 22:11 UTC; skeleton and sections A-B 22:13; sections C-D, E-F with verdicts, and this block afterwards, each a separate apply_patch write under 1500 words. Own checks before the marker: all eight input postpins re-hashed (8 OK), zero standalone markers, zero charge_basis lines and zero Seal headings in both owned files, and the 64-hex tokens in the custody file equal the live input digests. Verdict for a reader of this section alone: A-E CONFIRMED and F CONFIRMED as scoped; REFUTED none; no blocking defect; with P accepted, every prescribed late column is a global unit of the whole leading algebra and its affine coefficient is eliminable with one retained compatibility, while the joint actual compatibility/source question stays open and no source, F10, degree, Keller or JC2 statement follows. ROOT retains custody, promotion and any follow-on; this FIRST authorizes no computation. The marker below is the only standalone marker and nothing follows it. All writers idle at the marker.

<!-- BODY-END -->
