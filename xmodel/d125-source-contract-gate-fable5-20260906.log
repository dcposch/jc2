cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
Everything is done; the report is published and nothing remains to request. Here is the recap.

**Outcome.** `xmodel/d125-source-contract-gate-fable5-20260906.md` is complete (18,150 bytes, SHA-256 `a802587179b6f105bb47555ceb0e6a1d20c94c68a76c6ac4a26bbea1fce7b4e4`), finished at 14:32 UTC against the 14:58 deadline. The skeleton went in first, the sections followed, and the standalone `<!-- BODY-END -->` is the last line with nothing after it. No `charge_basis` line, no seal footer, no edits to launcher, validator, adapters or FALLACY.

**Verdicts, all PROVISIONAL and sufficiency-only:**

- **Arrow 1 CONFIRMED.** Injectivity of Phi, the chain-rule factor `5X^4`, the chart bracket `-z`, the band formula, the multiplier identity `ell*s-m*j = 5(is-kj)`, and the 241-layer range with the target on layer 4 all re-derive by hand and by my own substitution-based arithmetic. Roy's `make_band`, `classify_layer` and `build_payload` match the producer's literal contract band by band. The untranslate-first image test is right; the naive character test fails in both directions.
- **Arrow 2 CONFIRMED** as a sufficiency theorem. A point of the guarded ideal gives an ordinary pair with Jacobian `1/5` and exact degrees 75 and 125, hence a non-automorphism by the cited degree-divisibility theorem. No coverage theorem is used. Properness is unknown and not claimed. The public classifier hashes rows and stores counts, so it is a specification, not a row list.
- **Arrow 3 CONFIRMED.** The transformed and ordinary coefficient ideals coincide by a constant-coefficient linear span equality, so no parameter pivot or localization is involved. Counts 706/1901, 53/136, 91/151 bands and the 4572 index bound recompute by independent loops. A bonus recount reproduces the advertised 13,741 / 1,327,026 / 41,685 from the formula alone. No runtime claim is made.
- **Arrow 4 CONFIRMED** as bookkeeping. Moh parents, the receiver, Strinz's carrier and Roy's charts stay distinct. No reverse lift, coverage claim or verified author digest is imported.

**Principal independent mutation.** An exact rank test shows the endpoint coefficient of each top band can be zero while every jet and normalization row still holds, so the degree guard is a genuine explicit addition. The guard names appear nowhere in the pinned public code.

**Importer eligibility.** A later measured, source-preserving importer is logically eligible if it regenerates every row from the definitions, keeps the guard, never truncates or projects, and reports emptiness only as emptiness of this chart. That is not launch authority. The producer's own tiny controls also replay PASS in normal mode.

A project memory recording the guard-independence result and the cheap metadata recount has been saved and indexed.
DIVERT path=xmodel/d125-source-contract-gate-fable5-20260906.md status=CLEAN body_bytes=18150 body_sha256=a802587179b6f105bb47555ceb0e6a1d20c94c68a76c6ac4a26bbea1fce7b4e4
