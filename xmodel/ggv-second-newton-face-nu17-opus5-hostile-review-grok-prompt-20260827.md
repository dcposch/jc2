# Hostile review: Opus5 second-Newton-face / `NU17` ideation theorem

Review the exact leading mathematical contribution in
`xmodel/ideation-20260827T1606Z-opus5.md` (SHA-256
`9f9d3d053933560c6e555ce357025254d9237d989b4ba78f205dbb870939b4fa`).
You are a different-model adversarial referee, not a collaborator polishing
the proposal. Ignore every producer confidence label and rederive from the
frozen D3 polygons and the Jacobian definition.

Charge and verdict separately:

1. With `x=tau^-4*xi`, `y=tau`, `F=tau^8*f`, `G=tau^12*g`, verify the exact
   chart/Jacobian identity, target sign and exponent 17, and coefficient
   recurrence.
2. Re-enumerate the `(4,-1)` `nu` windows, their complete lattice census,
   `F_17`/`G_17` handles, and the leading-face square/cube form. Distinguish
   the frozen artificial control from what the GGV chain actually forces.
3. Audit the proposed general face-endpoint formula
   `K^(-w(1,1)/m) dxi` and every hypothesis hidden in `a,b,m,N`, shifted
   faces, mode subtraction, rationality, constants, reducible covers, and
   base field.
4. At the unshifted lower face, rederive
   `4Kg'-3K'g=4K`, prove or refute rational-pole impossibility and the
   polynomial degree alternatives, and test the claimed multiplicity
   classification. Either provide a proof of the `gamma == 3 mod 4`
   two-root criterion or mark it computational-only and find counterexamples
   beyond the producer range.
5. Verify the `K=xi*(xi-1)^7` endpoint solution/residual and state exactly
   what it does and does not imply about a raw `nu`-jet.
6. Run a targeted repository-history check: was this second face or endpoint
   already derived, merely named, or genuinely absent? Do not award novelty
   merely because wording differs.
7. Attack the producer's strategic claims: whether the upper face is really
   proxy-only; whether lower-face pinning follows from the frozen chain;
   whether a reachable target row would be a complete face fixture; and
   whether the proposed compiler can truly be obtained by regrading the same
   442 slots.

For each item write `CONFIRMED`, `REFUTED`, or `GAP/REPAIR`, with an explicit
counterexample or derivation where possible. State the maximum promotable
scope and the cleanest bounded successor. Do not review the Fable5 peer
submission, edit canonical ledgers, launch AWS, run heavy local algebra, or
touch/read/status-inspect `jc2-lean`. Small exact scripts in `/tmp` are
allowed.

Write exactly one repository file:
`xmodel/ggv-second-newton-face-nu17-opus5-hostile-review-grok-20260827.md`.
Touch nothing else. End with the report SHA-256 and review verdict.
