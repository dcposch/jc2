# Independent hostile gate: F10 compact counterexample contract and exact boundary module (Fable 5.1)

tag=f10-compact-contract-module-gate-fable5-20260909
frozen_basis=0d39df3c9fd69c939a8420c54d03228b9077777d (provenance only)
reviewer=Claude Fable 5.1 (claude-fable-5-1)
launch=root actual invitation; lane clock opened 11:00:01 UTC; hard stop = earlier of 11:25:01 UTC and 11:22 UTC, i.e. 11:22 UTC, no reset
subprocesses=ZERO mathematical subprocesses. Only date, ls, mkdir, sha256sum, cat/heredoc, grep and wc were run. All algebra below is hand-derived factored prose.

## 0. Custody and read scope

All SIX charged files under /tmp/jc2-lane.6TNSPY/inputs were pinned (sha256sum, box/f10-compact-contract-module-gate-fable5-20260909/input-pins.sha256) BEFORE their WHOLE reads, and the six hashes match the invitation's ordered pins exactly: 6c6fe089 (contract body), b26a1639 (contract artifact: body_bytes 13021, body_sha256 9d0d0603, full 6c6fe089, closed 10:55:29Z, consistent with the contract's own seal), 26687318 (module body), 6458b6fd (module artifact: body_bytes 15665, body_sha256 aa446820, full 26687318, closed 10:54:49Z, consistent with the module's seal), 9b77ece0 (compression, ACCEPTED at stated scope), 651dafb5 (its Fable gate, ACCEPTED). The contract and the module are treated as two independent NEW/UNREVIEWED proofs; neither cites the other. The compression's immutable PROVISIONAL header is noted and ignored as review debt per the invitation. No provenance path, peer, live gate, log, receipt or mutable body was opened; no corpus scan.

Identical-object composition. Contract R = compression R_old - v0 = module S (all three are p z^3 - z^2 + u z up to the v0 shift). Contract Pi, Delta are the compression's (4) read in S; contract condition 3 is the compression's (5) and the module's (16); contract (I) is the compression's (18) after the translation; the contract's z = d - g with d = p^2 + ell p - u is the compression's z = p^2 - g + ell p + h at h = -u. Every parent conclusion is applied to the object it was proved for.

## 1. Target A: explicit source polynomial. CONFIRMED.

With p = V(V^2 U - 1): p/V = V^2 U - 1. Since W = 1 - V d, z = d - V^{-1} = -W/V and t = 1/z = -V/W. Then p z - 1 = p d - p/V - 1 = p d - V^2 U, and -V^2 U W - V d = -V^2 U + d(V^3 U - V) = -V^2 U + d p: the contract's identity p z - 1 = -(V^2 U - 1)W - 1 = -V^2 U W - V d is exact. Hence R = z^2(p z - 1) + u z = (W^2/V^2)(-V^2 U W - V d) - u W/V = -U W^3 - W(d W + u)/V. Next d W + u = (d + u) - V d^2 = p(p + ell) - V d^2 = V[(V^2 U - 1)(p + ell) - d^2], so R = W[d^2 - (V^2 U - 1)(p + ell) - U W^2], which is (2) with every sign as printed. Only V was ever inverted and it cancels identically: no pole and no excluded value of u, ell, U or V. W has constant term 1, so W is nonzero for all parameters.

Degrees: p has degree 4, leading U V^3; d degree 8, leading U^2 V^6 (ell p and u are lower); W degree 9, leading -U^2 V^7 from -V p^2. The three terms of (2) have degrees 9+16 = 25, 9+3+4 = 16 and 1+27 = 28; the last has leading -U(-U^2 V^7)^3 = +U^7 V^21. Unique top, so deg R_source = 28 and top U^7 V^21, and neither u nor ell enters any leading form (their zero values included). t = -V/W has rational degree 1 - 9 = -8 with leading rational function (-V)/(-U^2 V^7) = U^{-2} V^{-6}. No F10 source is assumed anywhere in this section.

## 2. Target B: backward arrow, degrees, endpoint. CONFIRMED.

Polynomiality. Condition 4 says A' := Ahat(p z^3 - z^2 + u z, z^{-1}) lies in K[p,z]. The triangular automorphism (g,p) -> (p, z = d - g) gives A(g,p) := A'(p, d - g) in K[g,p]; then g = V^{-1}, p = V^3 U - V gives an element of K[U,V,V^{-1}]. Under the same field map K(p,z) -> K(U,V) this element equals Ahat(R_source, -V/W), an element of K[U,V,W^{-1}] by Target A. If f = a/V^j with j >= 1 minimal and f = b/W^k, then a W^k = b V^j, V is prime in the UFD K[U,V] and V does not divide W (W = 1 mod V), so V | a, contradiction. Hence P := A and likewise Q are in K[U,V]; the argument works along the whole divisor V = 0 and drops no component.

Bracket. (g,p) -> (p,z): p_g = 0, p_p = 1, z_g = -1, z_p = 2p + ell, determinant +1. (p,z) -> (R,t): R_p = z^3, t_z = -z^{-2}, t_p = 0, determinant -z. (g,p) -> (U,V) with U = g^2 + g^3 p, V = g^{-1}: U_g V_p - U_p V_g = 0 + g^3 g^{-2} = g. Also g = Pi^2 + ell Pi - u - 1/t gives -g t = 1 + u t - ell t Pi - t Pi^2 = Delta. So [A',B']_(p,z) = c Delta (-z) = c(-g t)(-z) = c g, [A,B]_(g,p) = c g, and [P,Q]_(U,V) g = c g, i.e. [P,Q] = c: an identity between polynomials in the common field, hence a polynomial identity.

Degrees. For rational functions deg(a/b) = deg a - deg b is well defined and multiplicative. If f_1 has strictly larger degree than h, then deg(f_1 + h) = deg f_1 and, writing f_1 = a_1/b_1, h = a_h/b_h, from (f_1+h) b_1 b_h = a_1 b_h + a_h b_1 the leading form of the polynomial sum is lead(a_1)/lead(b_1). R^i t^k has degree 28 i - 8 k, and the slot bound i + r k <= e gives 28 i - 8 k <= 28 e - (28 r + 8) k, strictly below 28 e for k >= 1, and 28 i < 28 e for k = 0, i < e. The monic R^e is therefore the unique top summand: deg P = 28 m, top (U^7 V^21)^m = U^(7m) V^(21m), and deg Q = 28 n, top U^(7n) V^(21n), exactly (II). Only i + r k <= e, monicity, conditions 3, 4 and c != 0 are load-bearing; the t-degree caps 3 and 5 are not used for sufficiency (they correctly restrict the ideal as compression outputs).

Endpoint. 5 m - 3 n = -1 gives gcd(m,n) = 1 with m >= 4 and n > m, so m does not divide n and n does not divide m; neither 28 m nor 28 n divides the other. Import: Jung (1942)/van der Kulk (1953): every automorphism of K[x,y] over a field is tame, and consequently the degree of one coordinate divides the degree of the other. So (P,Q) is a Keller pair (c != 0) that is no automorphism over K, hence over no extension. Embedding: the subfield generated over Q by the finitely many coefficients of P, Q and c is finitely generated; a transcendence basis maps to algebraically independent complex numbers and the algebraic remainder embeds since C is algebraically closed. A field embedding preserves [P,Q] = c, the coefficient 1 on the top terms and c != 0. This IS sufficient without any backward reconstruction of the printed F10 chain: "Keller and not an automorphism" is the counterexample property itself, and the parent gate's caution concerned reconstruction of the printed source profile, which this endpoint never claims. The new degree argument is sound.

## 3. Target C: complete ideal and counts. CONFIRMED.

Variables. A slots for k = 0..3: (3r+2)+(2r+2)+(r+2)+2 = 6r+8; B slots for k = 0..5: (5r+3)+(4r+3)+(3r+3)+(2r+3)+(r+3)+3 = 15r+18; total 21r+26, minus the two monic substitutions, plus u, ell, c, eta: 21r+28, i.e. 49 at r = 1. Independently rederived.

Jacobian rows. A_k t^k and B_l t^l contribute (l A_k' B_l - k A_k B_l') t^(k+l-1), so h = k+l-1 runs over 0..7 and the R-degree is at most (m - r k - 1) + (n - r l) = 7r + 2 - r h, using m + n = 8r + 3. With w(R) = 1, w(t) = r, Pi has weight <= m (top R t^3) and Delta = 1 + u t - ell t Pi - t Pi^2 has weight <= r + 2m = 7r + 2 and t-degree 7 (from -t(R t^3)^2 = -R^2 t^7), so its R^a t^h terms obey the same envelope a <= 7r + 2 - r h. Rows: sum over h of (7r + 3 - r h) = 8(7r+3) - 28 r = 28 r + 24.

Inverse rows, every coefficient. Under R -> u z - z^2 + p z^3, R^i z^{-k} expands into p^a z^(3a+2b+c-k) with a+b+c = i; a pole needs 3a+2b+c < k <= 5, so i <= 4, a = 0 when k <= 3, and for k <= 5 a <= 1 with a = 1 forcing b = 0, c <= 1 and exponent >= -2. The complete rows (A_(k,i) = coefficient of R^i in A_k):
A z^-3: A_(3,0); z^-2: A_(2,0) + u A_(3,1); z^-1: A_(1,0) - A_(3,1) + u A_(2,1) (deg A_3 <= 1, so no A_(3,2) slot).
B z^-5: B_(5,0); z^-4: B_(4,0) + u B_(5,1); z^-3: B_(3,0) + u B_(4,1) - B_(5,1) + u^2 B_(5,2); z^-2: p^0 row B_(2,0) + u B_(3,1) - B_(4,1) + u^2 B_(4,2) - 2u B_(5,2), p^1 row B_(5,1); z^-1: p^0 row B_(1,0) + u B_(2,1) - B_(3,1) + u^2 B_(3,2) - 2u B_(4,2) + B_(5,2) + u^3 B_(4,3), p^1 row B_(4,1) + 2u B_(5,2).
That is exactly 3 A scalar rows and 7 B rows, R-powers at most 3 actually occurring (the bound 4 is a correct envelope), no p coefficient dropped and no source expansion. With eta c - 1: 28 r + 35 rows, 63 at r = 1. These are ambient counts and envelopes, not solver facts.

Translation. Ahat_new(S,t) := Ahat_old(S + v0, t) keeps every deg_S bound, keeps the leading (= R^m) coefficient, keeps the bracket since d/dS = d/dR_old and Pi = t - u t^2 + S t^3 is the compression's own (4), and Ahat_old(p z^3 - z^2 + u z + v0, z^{-1}) = Ahat_new(p z^3 - z^2 + u z, z^{-1}). Exact removal of v0. Necessity at the accepted scope: each actual source yields, by the compression, polynomials with (5), (6), monic R^m, R^n, c != 0 and inverse-ordinary (18) because the receiver A' is itself a polynomial; after translation this is a point of V(I_r) over the 16k field with eta = 1/c. Endpoints: I_r proper in Q[vars] gives a Qbar-point by the weak Nullstellensatz, hence a solution of 1-4 with c != 0 over Qbar in C. A point in a nonzero finite Q-algebra Lambda: Lambda has a maximal ideal whose residue field is a finite extension of Q; the image point satisfies every row, eta c = 1 and the literal monic coefficients survive, and the field proof applies. No nilpotent-base theorem is needed. Properness or unit is not claimed by the contract and not judged here.

## 4. Target D: the whole rank-three algebra. CONFIRMED.

Independence. K(S,t) = K(p,z) via z = 1/t, p = t - u t^2 + S t^3 and back via S = p z^3 - z^2 + u z, so S, t are independent; a nonzero F(S,p) of p-degree i > 0 becomes a polynomial of t-degree 3i with leading coefficient S^i times its leading p-coefficient, so S, p are independent. S T^3 - u T^2 + T - p is linear in p with coefficient -1 in K(S)[T], hence irreducible in K(S)[T,p]; its T-coefficients S, -u, 1, -p have gcd 1 in K(S)[p], so Gauss gives irreducibility in K(S,p)[T]: t has degree exactly 3, 1, t, t^2 are K(S,p)-independent, and x = S t, y = S t^2 - u t is triangular with diagonal S, S, so 1, x, y are independent over K(S,p) and over B.

Relations. x^2 = S^2 t^2 = u(S t) + S(S t^2 - u t) = u x + S y. S p - x = S t - u S t^2 + S^2 t^3 - S t = S t(S t^2 - u t) = x y. p x - u p - y = (S t^2 - u S t^3 + S^2 t^4) - (u t - u^2 t^2 + u S t^3) - (S t^2 - u t) = S^2 t^4 - 2u S t^3 + u^2 t^2 = y^2. Presentation (2): the three relations reduce every monomial to the B-span of 1, X, Y; the quotient surjects onto T0 = B + B x + B y and the independence of 1, x, y kills the kernel, so (2) is exact and T0 is free of rank three.

Whole intersection. T0 is in K[p,z] (x = p z^2 - z + u, y = p z - 1) and in K[S,t]. Conversely x - u = z y and y + 1 = p z give z = (x-u)/y and t = p/(y+1), so K[p,z] is in T0[y^{-1}] and K[S,t] in T0[(y+1)^{-1}]. For f in both, y^N f and (y+1)^M f lie in T0; (y+1) - y = 1 so (y^N, (y+1)^M) = T0 and f is a T0-combination of the two. T = T0 with no normality, integral closure or generic-fibre step. Gluing: the chart w = 1 of p z^3 - z^2 w + u z w^2 - S w^3 is B[z]/(S - p z^3 + z^2 - u z) = K[p,z], the chart z = 1 is K[S,w] with w = t, overlap K[p,z,z^{-1}]. On the first, y = 0 forces p z = 1, so z is a unit, and there y + 1 = 1; on the second, y = -1 forces t(S t - u) = -1, t a unit, and there y != 0. So the cubic is D(y) union D(y+1) with K[p,z]_y = T0_y and K[S,t]_(y+1) = T0_(y+1), and these two opens cover Spec T0 since (y, y+1) = (1): the projective cubic is Spec T0, affine and finite free of rank three. Special fibre S = p = u = 0: X^2 = 0, XY + X = 0, Y^2 + Y = 0; -Y is idempotent, its zero component has X = 0, its one component has Y = -1 and X^2 = 0, giving K x K[eps]/(eps^2) of length three, nonreduced. Finite free is neither etale nor a degree-three Keller map; the module says so.

## 5. Target E: complete boundary tests. CONFIRMED.

Mod-three filtration. a(S,p) of p-degree i has t-degree 3i with leading coefficient S^i lc_p(a); b x adds 1, d y adds 2. The three degrees are distinct mod 3, so the maximum in a + b x + d y is attained once and never cancels; (7) follows and is sufficient trivially: 4 slots at N = 3, 6 at N = 5.

Cubic. f = f_0 + a p + b x + d y with a, b, d in K[S]: t^3: S a; t^2: -u a + S d; t^1: a + S b - u d; t^0: f_0 (p, x, y vanish at t = 0). Solving from the top gives (8), and (10) is (8) cleared: d = (S f_2 + u f_3)/S^2, b = (S^2 f_1 - S f_3 + u S f_2 + u^2 f_3)/S^3. Membership in T for t-degree <= 3 is exactly the existence of this form, so the three exact divisibilities are necessary and sufficient, and each is a polynomial equation on the S-adic coefficients (f_30 = 0; f_20 + u f_31 = 0; f_10 - f_31 + u f_21 = 0), not a localization at S.

Quintic. p x = S t^2 - u S t^3 + S^2 t^4; p y = (t - u t^2 + S t^3)(S t^2 - u t) = -u t^2 + (S + u^2) t^3 - 2u S t^4 + S^2 t^5. Both signs rechecked. f = f_0 + a p + (b + b* p) x + (d + d* p) y gives f_5 = S^2 d*, f_4 = S^2 b* - 2u S d*, f_3 = S a - u S b* + (S + u^2) d*, f_2 = -u a + S d + S b* - u d*, f_1 = a + S b - u d, and solving downward gives (11) exactly; five exact divisions, the first being f_5/S^2. Both directions of the conversion match.

Controls. t: a = d = 0, b = 1/S, fails only the last test, so t is not in T (its inverse is z^{-1}). S t^2: a = 0, d = 1, b = u/S: passes iff u = 0, when it equals y; the u = 0 stratum is retained, not imposed. S t^3: a = 1, d = u/S fails for u != 0; for u = 0, d = 0 and b = -1/S fails; its inverse p - z^{-1} + u z^{-2} is never ordinary. The leading-coefficient condition alone is insufficient. u = 0, S = 0 and p = 0 are preserved throughout.

## 6. Target F: module source specialization. CONFIRMED.

Necessity of (13)-(14). With A_3 = kappa_A S: a = kappa_A; d_A = (A_2 + u kappa_A)/S has degree <= (r+1) - 1 = r; b_A = (A_1 - kappa_A + u d_A)/S has degree <= (2r+1) - 1 = 2r; A0 = A_0 of degree <= m. With B_5 = kappa_B S^2: d* = kappa_B; b* = (B_4 + 2u S kappa_B)/S^2, degree <= (r+2) - 2 = r; a_B = (B_3 + u S b* - (S + u^2) kappa_B)/S, degree <= 2r+1; d_B = (B_2 + u a_B - S b* + u kappa_B)/S, degree <= 3r+1; b_B = (B_1 - a_B + u d_B)/S, degree <= 4r+1; B0 of degree <= n. Sufficiency: substituting back, deg f_3 = 1, deg f_2 <= r+1, deg f_1 <= 2r+1 for A, and deg f_5 = 2, f_4 <= r+2, f_3 <= 2r+2, f_2 <= 3r+2, f_1 <= 4r+2 for B, i.e. every bound of (6). So (13)+(14) is exactly: bounds, inverse-ordinary, and the two leading shapes. Monicity, the faces, the ODE and the full bracket are additional equations, as the module states; bare membership forces none of them.

Derivative rules. p_S = t^3, p_t = 1 - 2u t + 3S t^2 = Q, x_S = t, x_t = S, y_S = t^2, y_t = 2S t - u, so for f = a + b x + d y with S, p independent in the coefficients, f_S = W_f + t^3 V_f + b t + d t^2 and f_t = Q V_f + S b + (2S t - u) d: (15) exact. (16) is the contract's condition 3 with the same orientation, all of Delta retained. [x,p]_(S,t) = x_S p_t - x_t p_S = t Q - S t^3 = t - 2u t^2 + 2S t^3 = 2p - t, which is not in T because 2p is and t is not. So T is not a Poisson subalgebra of K[S,t]: an argument that computes the bracket inside the rank-three algebra, or replaces it by the cubic norm, trace or discriminant of T over B, is unlicensed; the bracket must be evaluated in K[S,t] by (15).

## 7. Target G: composition and the parametrized count. CONFIRMED.

Identification. Module S = contract R (translated). Module membership for t-degree <= 3 or <= 5 is precisely contract condition 4, since f in K[S,t] lies in K[p,z] iff f(S(p,z), z^{-1}) has no negative z part. The module's imported leading shapes are forced for every contract point: the z^-3 row gives A_(3,0) = 0 so A_3 = kappa_A S with kappa_A = A_(3,1); the z^-5 row and the z^-2 p^1 row give B_(5,0) = B_(5,1) = 0 so B_5 = kappa_B S^2; the t^7 bracket row is 5 A_3' B_5 - 3 A_3 B_5' = -kappa_A kappa_B S^2 = -c S^2, so kappa_A kappa_B = c, and eta c = 1 makes both nonzero. No gap. Consistency check between the two independent proofs: the module's S-adic conditions number 1+1+1 = 3 for the cubic and 2+2+1+1+1 = 7 for the quintic, and the ten contract rows of Target C reduce to them; e.g. z^-3 with B_(4,1) = -2u B_(5,2) and B_(5,1) = 0 reads B_(3,0) - u^2 B_(5,2) = 0, the S^0 condition of a_B.

Count. The ten inverse rows are triangular in the pivots A_(3,0), A_(2,0), A_(1,0), B_(5,0), B_(5,1), B_(4,0), B_(4,1), B_(3,0), B_(2,0), B_(1,0), each with coefficient 1 and otherwise involving only non-pivot variables and earlier pivots, so eliminating them is an isomorphism of Q-algebras Q[vars]/I_r = Q[vars minus pivots]/J_r, where J_r is generated by the bracket rows and eta c - 1. The module's coefficients are an invertible unit-triangular affine change of the remaining variables over Q[u] (e.g. d_(A,j) = A_(2,j+1), b_(A,j) = A_(1,j+1) + u A_(2,j+2)), so the same quotient. Slots: A: A0 (3r+2) + kappa_A (1) + b_A (2r+1) + d_A (r+1) = 6r+5; B: B0 (5r+3) + a_B (2r+2) + b_B (4r+2) + b* (r+1) + d_B (3r+2) + kappa_B (1) = 15r+11; total 21r+16, minus two monic, plus u, ell, c, eta: 21r+18, i.e. 39 at r = 1, which equals 21r+28 minus the ten pivots. The parametrized pair satisfies the same bounds (6) by the sufficiency above, so the bracket envelope is again 28r+24 rows, plus eta c - 1: 28r+25, i.e. 53 at r = 1. The root count is sound. Neither I_r nor J_r is claimed proper or unit here.

## 8. Target H: limits. CONFIRMED as stated.

Both results pass with JC2 unresolved: the contract is the implication "field point of I_r (or J_r) with c != 0 implies a complex Keller counterexample of degrees (28m, 28n)", and the module is a ring theorem plus an exact reparametrization. Neither supplies a candidate, coefficient builder, solver, reverse actual-chain theorem, nonreduced universal-ring equivalence or measured runtime, and none is manufactured here. Exact remaining obligations: (i) sufficiency direction: exhibit, for ONE r >= 1, a point of V(J_r) over a characteristic-zero field, or prove 1 is not in J_r in Q[21r+18 variables]; the field is then embedded in C by Target B. (ii) exclusion direction: prove 1 in J_r for EVERY r >= 1; a finite computation per r cannot close this infinite family without a uniform-in-r argument, and even then it excludes only the necessary F10 stratum at the accepted compression scope (with that scope's own conditional imports), not JC2. No downstream compute, allocation or review authority follows from these verdicts.

## 9. Verdicts

A CONFIRMED, B CONFIRMED, C CONFIRMED, D CONFIRMED, E CONFIRMED, F CONFIRMED, G CONFIRMED, H CONFIRMED. No failed implication, sign error or dropped stratum was found in either new proof; every displayed identity was rederived by hand, and the two independently authored boundary descriptions (ten pole rows; three plus seven exact S-divisions) were shown to coincide. Strongest exact conclusion: for r >= 1, m = 3r+1, n = 5r+2, any field solution of the 21r+18-variable system J_r (equivalently the 21r+28-variable I_r) with eta c = 1 yields ordinary P, Q in K[U,V] with [P,Q] = c, degrees 28m and 28n, tops U^(7m)V^(21m) and U^(7n)V^(21n), hence by Jung-van der Kulk a Keller non-automorphism, embeddable in C. Whether any such point exists is open in both directions. No authored seal, no exit-price line, no promotion, no follow-on authority.

## OPEN(S) RAISED

None.

## COLLISIONS

status: EMPTY

- NONE — own-only raised-OPEN check, no corpus scan.

<!-- BODY-END -->
