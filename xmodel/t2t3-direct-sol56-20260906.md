# Direct attained-\(T_2+T_3\) chart: semantic substitution and bounded decision

## Disposition

The compressor remains proved. In Moh's formal-variable order
\((f,g)=(G,F)\), attained \(T_2\) lowers the possible degree of a nonzero
\(j=J(F,G)\) to 20 for \((99,66)\) and 25 for \((108,72)\). Attained
\(T_3\), with Proposition 3.2's exact derivative degree, then forces such a
nonzero \(j\) to be a scalar. The charged Theorem B excludes \(j=0\) on the
actual source faces with attained \(T_2\). Thus a compatible canonical
attained-\(T_2+T_3\) datum has \(j\in k^\times\). This is a theorem, not a
normalization: the direct rings have no \(j\) variable, \(j\)-localizer, or
raw Jacobian-tail rows, and no gauge is spent setting \(j=1\).

The earlier circuit presentations contained 7,136 acyclic arithmetic
variables for either 99 chart and 17,308 for D108. Recursive exact
substitution gives direct semantic ideals in 449, 447, and 507 variables,
with mechanically derived final counts 2,754, 2,754, and 3,196 nonzero
polynomial generators. No constraint variable is pivoted; the full 2,754-row
delta-2 stream did not finish, so no full-artifact SHA is claimed below.

The finite outcomes are:

| chart | decision | scope |
|---|---|---|
| \((99,66),\delta=2\) | **PENDING_FINAL_RESULT** | pending final custody |
| \((99,66),\delta=5/2\) | **OPEN — NOT REACHED** | delta 2 had priority |
| \(D=108\), repaired free mean | **OPEN — NOT REACHED** | delta 2 had priority |

A modular unit is only a signal. It becomes a characteristic-zero kill only
after exact-\(\mathbb Q\) confirmation on identical generators or an exact
cofactor identity. A nonunit result for a strict generator subset is
inconclusive for the full ideal. If a complete exact-\(\mathbb Q\) direct
ideal were proper, its permitted positive type would be:

> **PROPER — EXISTENTIAL NONDEGENERATE NECESSARY-CHART SURVIVOR OVER
> \(\overline{\mathbb Q}\); NOT A PAIR, NOT A CANONICAL LIFT.**

Such a survivor would not be a Keller pair and would not provide the missing
triangular lift to the canonical approximate-root recursion.

## 1. Frozen custody, fleet, and disk discipline

I mechanically built the six-line manifest from the indexed
`charged_input_<i>_basename=` and `charged_input_<i>_sha256=` receipt fields
with `awk`, then ran `sha256sum -c` against
`/tmp/jc2-lane.wiULJu/inputs`. All six returned `OK`:

| frozen input | SHA-256 |
|---|---|
| `t2t3-chain-sol56-20260906.md` | `d85ae785c992e73a38d84b73a5fcadee48342844b2fadfe2a5d13082d5a6c45b` |
| `char-degree-instrument-astra-r2-20260905.md` | `d9b95ce9c4588104c9f306d987f91957988611b80d2025a05f14c40568e33b45` |
| `cone-vertex-gate-opus5-20260905.md` | `72dfcd371f338767e303c6da0eab02337f257ad29409a9fc6b60cd2e85c20ecd` |
| `fleet.sh` | `a9da94d341a691942e9d6a6a6d90d97008b2d7baa1cadb2592481cf6b9c11c4c` |
| `dispatch.sh` | `dd1e148c9a0bdf2f7de0bd80a249c743d985a26f982245a375824ca89e4bc599` |
| `FALLACY-v2.md` | `e47fd16cfcc91bc7bfdac4ba1b5f46152db8235e6609dca6e29a5966549c38f5` |

There was no mismatch. I used no `ideation-*` input, made no ledger edit, did
not use `jc2-lean`, and did not modify a charged input.

Exactly one worker was launched with the requested command:

```text
bash ops/fleet/fleet.sh launch 1 r7i.16xlarge
```

Instance `i-0fc259ff68e914869`, private IP `172.30.0.55`, launched at
2026-09-06 02:34:38 UTC. It reported 64 logical CPUs and 532,326,535,168
bytes of RAM. Provisioning completed before use. The installed executables
were msolve 0.10.1 at SHA-256
`0436525b06fe83b1a6a00097a96d9bcafdc40d8de6315c724b9ada9a4c04ff5f`
and Singular 4.3.2 at
`90ab699b7a28486944a167e797d7c8dd38f8f949960b93ee7a6d08ff1c7c46f4`.

All expansion and solver work ran on that worker with run-local `TMPDIR`.
No large presentation, basis, or CAS log was copied back. The host retains
only this report, drivers, and compact JSON under
`box/t2t3-direct-20260906/`; the final persistent byte count is recorded in
§ 9 and is below 2 MB.

## 2. Compressor

Keep Moh's order reversal explicit:

\[
 \widehat T_i(F,G)=T_i^\psi(G,F),\qquad
 B_i={\partial\widehat T_i\over\partial G},\qquad
 J(F,\widehat T_i)=B_iJ(F,G).                    \tag{2.1}
\]

For the two degree pairs the exact characteristic data are

\[
 (M,d,D)=((-66,77,97),(99,33,11,1),(66,55,145))
\]

and

\[
 (M,d,D)=((-72,81,106),(108,36,9,1),(72,63,227)).
\]

The complete constant-target second characteristic family is

\[
 \widehat T_2=G^3-F^2+aG^2+bFG+cF+dG+e_0,
 \qquad B_2=3G^2+2aG+bF+d.
\]

Its leading \(3G^2\) cannot cancel, so \(\deg B_2=2m\). Total-degree
attainment and (2.1) give, for \(j\ne0\),

\[
 \deg j\le n+D_2-2-2m=20\quad\hbox{or}\quad25.
\]

At \(T_3\), Proposition 3.2 gives
\(\deg_yB_3=D_3+M_3=242\) or 333, exactly the upper bound
\(n+D_3-2\) for \(\deg J(F,\widehat T_3)\). Domain degree additivity
therefore forces \(\deg j=0\) when \(j\ne0\). Theorem B, whose proof uses
the actual source faces and attained \(T_2\), excludes \(j=0\). In
particular, \(T_2\) alone supplies only the 20/25 bound; both attained levels
and the source theorem are needed for the compressor.

## 3. Complete targets and source support

Put \(x=t^{-1}\), \(y=(1+z)t^{-1}\); \(t,z\) are extraction symbols and
are not solver variables. For \(k=33\) or 36, define

\[
 H_0=h-\frac b6t^k,
 \quad v=D+\left(\frac a3+\frac{b^2}{18}\right)t^{2k-1},
\]

\[
 V=C-\frac b4t^kD+
 \left(\frac{ab}{12}+\frac{b^3}{54}-\frac c2\right)t^{3k-1},
 \qquad U={8V\over3t}.
\]

Let \(R\) be the part of \(v^2-UH_0\) below \(z\)-degree \(k\); every
high-\(z\) coefficient is a row. With
\(p=d+bc/2-(a+b^2/4)^2/3\), the exact shortened expression is

\[
 Q_*={3\over4}RH_0^2-{1\over8}tvUH_0+tvR-{9\over64}t^2U^2
 +pt^{4k-2}H_0^2+pt^{4k-1}v+qt^{6k-2}.          \tag{3.1}
\]

The \(6k-2\) \(H\)-adic normalizer puts the \(T_2\) faces at depths 141
and 151. They are the physical 143 and 153 rows after the proved two-unit
denominator shedding. That shift is bookkeeping, not a gauge action.

Every strict-below row has target zero. Every equality row subtracts the
entire target:

\[
 T_2:\quad \lambda_2z^{40}(1+z)^{15}\ (99),\qquad
 \lambda_2z^{49}(1+z)^{14}\ (108),
\]

over all 56 or 64 slots, including zero slots, and

\[
 T_3:\quad \lambda_3z^{105}(1+z)^{40}\ (99),\qquad
 \lambda_3z^{176}(1+z)^{51}\ (108),
\]

over all 146 or 228 slots. Separate rows
\(Z_2\lambda_2-1\) and \(Z_3\lambda_3-1\) keep both leaders free and
nonzero; neither leader is set to one.

The active recurrences are

\[
 T_3=T_2^3-\lambda_2^3FG+u_{11}t^{11}FT_2\pmod {t^{21}}, \tag{3.2}
\]

including `t3eq+leader55^3`, and

\[
 T_3=T_2^4-\lambda_2^4FG^2+u_9t^9FGT_2
       +u_{18}t^{18}FT_2^2\pmod {t^{26}},       \tag{3.3}
\]

including `t3eq+leader63^4`. Seven later 99 coefficients and thirteen later
D108 coefficients are omitted only as proved passive polynomial extensions,
never set to zero.

For both 99 charts, the two-point/split and source-support equations are
already composed into the frozen semantic parametrization, leaving zero
residual source rows. D108 instead retains and emits its 14 residual rows.

| chart | base face \(P\) | complete source-support contract |
|---|---|---|
| 99, \(\delta=2\) | \(\zeta^2(\zeta+3\rho)\) | \(K_3:P@9,K_2:P^3@27,F:P^9@81,G:P^6@54\) |
| 99, \(\delta=5/2\) | \(\pi(\pi^2-c)\) | \(K_3:P@21,K_2:P^3@63,F:P^9@189,G:P^6@126\) |
| D108 free mean | \((\pi-\mu)^2-c\) | \(K_3:-P@8,K_2:P^4@32,F:P^{12}@96,G:P^8@64\) |

At-level source rows subtract those whole targets; strictly lower rows have
zero target. The D108 chart retains free `minor_mean` \(\mu\), does not split
the quadratic or choose a root, and retains all 14 source residuals.

## 4. Gauge ledger

The ledger is explicit. `beta=1` spends simultaneous dilation exactly once,
via \(F_\alpha=\alpha^{-n}F(\alpha x,\alpha y)\) and
\(G_\alpha=\alpha^{-m}G(\alpha x,\alpha y)\). On the 99 charts, the
audited diagonal translation licenses `jet0=0`; the omitted coordinate is a
free affine-line factor. D108 instead uses the repaired free-mean chart and
retains `minor_mean`; no inherited mean-zero slice is substituted.

The separation parameter \(\rho\) or \(c\) is free and only inverted by an
explicit equation. The characteristic leaders remain free nonzero scalars.
The Jacobian scalar is not normalized; its omission is a consequence of § 2.
The 141/143 and 151/153 conversion is denominator shedding. No root choice,
split variable, leader normalization, separation normalization, or hidden
centering spends another gauge.

## 5. Direct presentation and substitution proof

Let \(A=\mathbb Q[s_1,\ldots,s_m]\) be the semantic ring and
\(B=A[X_1,\ldots,X_N]\). The circuit rows are

\[
 d_i=X_i-f_i(s,X_1,\ldots,X_{i-1}),              \tag{5.1}
\]

each monic with coefficient +1 and ordered acyclically. Recursive evaluation
defines \(\phi:B\to A\). Successive monic division has a unique remainder in
\(A\), hence

\[
 \ker\phi=(d_1,\ldots,d_N),\qquad
 {B\over(d,c_1,\ldots,c_r)}\simeq
 {A\over(\phi(c_1),\ldots,\phi(c_r))}.           \tag{5.2}
\]

This is ideal equivalence, not merely equality of zero sets; it uses no
division and has no vanished-leader branch. The frozen production audit had
already checked every one of the 7,136/17,308 definitions for leader +1,
preceding dependencies, and exact round trip. The direct builder evaluates
the same graph in the frozen semantic coordinate ring and emits only
primitive integral images of constraint rows.

The exact-\(\mathbb Q\) control used three graph leaders `u,v,w`, checked both
ideal containments and the semantic elimination ideal, and rejected a one-unit
perturbation. Singular 4.3.2 returned rc 0. The control input SHA-256 is
`fb2f063ff340e80519c43b796f6633274c86476511bc103ab5c4e3a7294a1d14`;
output SHA-256 is
`39ebc79c163af1af8ac3e2d0a0df1e40fc51e1fe9c2de853711dfb87b4a0e672`.
Its terminal markers were:

```text
MONIC_SUBSTITUTION_POSITIVE=PASS
UNIT_LEADER_CHECK=PASS
SEMANTIC_ELIMINATION_CHECK=PASS
MONIC_SUBSTITUTION_NEGATIVE=PASS_REJECTED_PERTURBATION
CONTROL_RESULT=PASS
```

| chart | semantic variables | raw slots | zero after substitution | direct generators |
|---|---:|---:|---:|---:|
| 99, \(\delta=2\) | 449 | 4,470 | 1,716 | 2,754 |
| 99, \(\delta=5/2\) | 447 | 4,470 | 1,716 | 2,754 |
| D108 free mean | 507 | 7,022 | 3,826 | 3,196 |

The semantic-order hashes are `ee778e93b4293b9399761911d80dd71dd63b5fa95d21055cf50cd0db51428a4b`,
`20c0e973211f3417eca0284e725cbbc46f5ba698257a05f87e08cedff038fd9c`,
and `4c8952648ab814be891a76e4b0fc8b2f650ccf0689ebfba882a96ac2a83f1ad4`.
The frozen source-map hashes are `778eda933ee9b2acbad20f6dc862c25db94776cb25ffd5782646427a3b63e1ea`,
`3f81dc99770d235099249a818a55b19f0c828d36109e30aa738995177f97dc46`,
and `1c927d83aa4684ae91bfffc8d03500fbabb73a500faa222fa349a6d129a10814`.

## 6. Order and paired fields

For Singular, the three inverse variables occupy a final `dp(3)` block. The
other \(n-3\) variables use a square matrix order: the first two rows are
assigned \(t\)-depth and \(z\)-site, and identity rows complete the matrix
after omitting two columns with nonzero \(2\times2\) site determinant. Every
first-row entry is positive, so this block is global. Custody records the
matrix hash and omitted minor.

This is the requested two-weight computational priority, but the normalized
rows are not genuinely bihomogeneous after `beta=1`, binomial target
subtraction, and localization. It is therefore honestly classified as a
two-site matrix filtration/order, not a preserved mathematical bigrading.
For delta 2 the matrix SHA is `547fabda8bf5874666699a9e296a06b796299774191b59583e156001d6e5cf00`;
its omitted columns are indices 0,1 (`A3c_63_26`,`A3c_63_27`), determinant
63. msolve independently reported its order as DRL.

Each rational row is cleared once to a sign-normalized primitive integer
polynomial. Singular's exact-Q stream and msolve's prime-field stream are
written simultaneously from one term iterator. The exponent/coefficient
stream hash, ordered labels, variable bijection, and both file hashes bind
generator identity before reduction modulo \(p=1073741827\).

msolve used `-g 2 -t 64 -v 2 --random-seed 0` with an exact
429,496,729,600-byte address-space cap. Singular used one thread with a
214,748,364,800-byte aggregate process-group RSS watchdog. Each wall watchdog
was 9,000 seconds; every run used its own worker-local `TMPDIR`.

## 7. Construction and solver custody

The first whole-table construction engine was deliberately stopped at rc 143
before an OOM after 751.32 seconds and 369,602,048 KiB RSS. It emitted no
complete presentation and has no algebraic force. The coefficient-at-a-time
engine held ordinary construction near single-digit GiB until individual
substituted \(Q_*\) coefficients were formed. It was deliberately redirected
after 2,354.64 seconds, 7,695,884 KiB peak RSS, and 669 complete rows (462
upper plus 207 strict); its incomplete paired streams totaled 1,149,564,477
bytes. It did not reach a face row or close the 2,754-generator artifact, so
no full-generator SHA is invented.

An exploratory 449-variable/503-generator paired prefix (500 direct rows plus
the three production localizers) was also sound but nondecisive. msolve
completed F4 degrees 2,3,3,3,4,3,4 before an operator stop (rc 143,
1,076.726 s, 76,724,502,528 bytes); Singular reached `BEGIN_STD` before its
operator stop (rc 143, 560.176 s, 92,059,447,296 bytes). Neither emitted a
result, so neither has classification force.

The stronger literal certificate subset consists of all 462 upper rows, the
top attained face row `T2_face_55`, and all three localizers. It has 449
variables, 466 generators, and 6,448,959 terms. Its canonical primitive-stream
SHA is `a78c6a46e4b50dec6df51712c9c4b8d3c717b52490488d6819563cb5e18044fe`;
the exact/alias input SHAs are `a604ed314ced1a33ff46e43081c2222430f24bde96a10405a3a8562ec6dccbbc`
and `7f593b87e40f02dbdb91f9b22aace6989f4a4d2092d9903276675b3265c7b572`.
An independent streaming audit matched all terms. Construction took 1,185.54
seconds and 89,535,404 KiB peak RSS.

**PENDING_FINAL_RESULT_DETAIL**

For clarity: a unit of this 466-generator ideal is conclusive for the complete
direct ideal because its generators are a literal subset. A nonunit basis or
timeout for it has no properness force, needs no sample-point promotion, and
leaves the full chart OPEN.

## 8. FALLACY-v2 acceptance audit

- Modular unit is only a signal; exact replay on identical generators or an
  exact cofactor is required for a kill.
- Subideal unit implies full-ideal unit. Subideal nonunit implies nothing
  about full-ideal properness.
- A complete exact-Q nonunit result would require dimension and a sample point
  before the loud necessary-survivor type above.
- A necessary-chart survivor is not a pair and not a canonical lift.
- Flag, physical place, and cover series remain distinct. No exit price or
  first-separation count is asserted.
- Strict-below zero rows and at-level coefficient-minus-complete-target rows
  are separate. Proposition 2.2/Theorem A supplies attainment; Proposition
  4.6 supplies the complete \(T_3\) face. No floor is called equality.
- Source map, coefficient field, semantic and solver order, alias map, and
  generator hashes are declared. Matching names are not a ring map.
- No `sat()` wrapper, raw remainder heuristic, root selection, or
  vanished-leader division is used.
- Prime marks are labels unless an explicit derivative is written.
- Fixed characteristic indices are not conflated with arrival/search labels.
- No pole/interior, carrier, family-C child, or canonical-lift claim is filled
  by analogy.

No new exit-price assertion is made, so no `charge_basis=` line is applicable.

## 9. Termination and retained evidence

Before sealing, only instance `i-0fc259ff68e914869` was terminated with
`bash ops/fleet/fleet.sh term i-0fc259ff68e914869`; provider state
**PENDING_TERMINATION_STATE** was confirmed at **PENDING_TERMINATION_UTC**.
Large polynomial streams, bases, and temporary files remained on that
terminated worker.

The retained evidence is under `box/t2t3-direct-20260906/`; compact result
JSON SHA-256 is **PENDING_JSON_SHA**, and total retained host bytes for the
report plus JSON are **PENDING_HOST_BYTES**. The report is sealed against
basis `e8afc3c823540a1dfd048acd796b4c61999875e5`.

<!-- BODY-END -->
