# Hostile review — selected-Q8 infinity passport plus Galois primitivity (bounded retry)

| Field | Value |
|---|---|
| Reports under review | `xmodel/max12-912-order3-nu-q8-infinity-contact-passport-20260824.md`; `xmodel/max12-912-order3-nu-q8-galois-primitivity-20260825.md` |
| Cases under review | `cases/max12_912_order3_nu_q8_infinity_contact_passport_20260824/`; `cases/max12_912_order3_nu_q8_galois_primitivity_aws_20260825/` |
| Parents audited for imports | formal-branch (grok, CONFIRMED), normalization-jet (claude, CONFIRMED), leaf4-descent (grok, CONFIRMED), global-quotient gate (claude, CONFIRMED), terminal Belyi (claude, CONFIRMED with the `Z!=0` erratum) |
| Overall verdict | **CONFIRMED** at the registered scopes (no false identity; non-blocking implicit steps in sections 8 and 10; execution disclosure in section 2) |
| Reviewer / model | Claude (Fable 5, Anthropic); different model family from the producer |
| Constraint compliance | bounded retry: no transcription, no Bash/web/local computation, all producer bytes treated as immutable, only this file written |

Read in full before any verdict: both charged reports; all six infinity-case files; all nine primitivity-case files (including `aws/run.meta` and the empty `aws/stderr.log`); the five parent reviews named above, in full, for the imported hypotheses; the terminal review's section 10 `Z!=0` erratum.

## 1. Charged-hash lattice (audit by reading; see section 2)

- infinity report `89691023…`: equals `MANIFEST.sha256` line 1 and `FREEZE.txt:report_sha256` of the infinity case.
- infinity manifest `a9e8748f…`: equals infinity `FREEZE.txt:manifest_sha256`.
- infinity freeze `8d695040…`: cross-attested in-repo by the primitivity `MANIFEST.sha256` line 10 (the successor pins its predecessor's freeze at exactly the charged value).
- primitivity report `9f37fc3f…`: equals primitivity `MANIFEST.sha256` line 1 and `FREEZE.txt:report_sha256`.
- primitivity manifest `1a3e44b9…`: equals primitivity `FREEZE.txt:manifest_sha256`.
- primitivity freeze `f25fa86f…`: appears only in the launch prompt; it is the newest artifact and nothing pins it yet. Unverifiable by reading.
- AWS attestation: `run.meta` records tag `q8_galois_primitivity_v2`, host `ip-172-30-0-45`, `start_utc=end_utc=2026-08-25T01:04:58Z`, `rc=0`; its two `source_sha256` lines equal manifest lines 6 and 4 (`17579fed…`, `ad3e9383…`); `stdout_sha256` equals the frozen `replay.json` pin `f5b37f95…`; `stderr_sha256` is the SHA-256 of the empty byte string, and the committed `stderr.log` is empty. All values match report section 5 verbatim.
- infinity `replay.py` `PINS`: the four parent freeze hashes (`807ad9…`, `700ddb…`, `c34fff…`, `0c5b86…`) match, digit for digit, the charged-freeze values in the four respective parent reviews; the formal-branch review hash `32750e…` matches the leaf4 review's independent recomputation and primitivity manifest line 9. The four remaining review-file hashes (`e85c20…`, `58fafc…`, `49e8d9…`, `713e41…`) are first pinned by this case; the files themselves were read and say what the successor consumes.

Every hash relation checkable by reading is consistent; none conflicts.

## 2. Execution disclosure (mandatory)

This review session has no shell and the launch constraints forbid local computation and any write outside this file. Therefore I could not recompute any SHA-256, run either replay, or independently execute the finite-field exponentiations. The verdict rests on: complete source reading of both replays; hand re-derivation of every hand-checkable identity (inventoried per item below); and the frozen cross-pinned attestations, including the AWS `run.meta`. The frozen-trusted computational residue is exactly: the two Rabin exponentials `v^(7^8) mod Q8bar` and `v^(53^7) mod F7`, the two gcd certificates `gcd(Q8bar, v^(7^4)-v)=1` and `gcd(F7, v^53-v)=1`, the two squarefreeness gcds, the exhaustive 53-point root scan (mathematically redundant given `F7` irreducible plus my hand check `F7(13)=18!=0`), and the hash pins. Residual byte checks for a shell-bearing session are listed in section 12. Three of the five parent reviews (normalization-jet, gate, terminal) carry the same disclosed shell-less protocol with staged probes; the two grok reviews executed their replays. This conditionality is inherited and disclosed, per repo protocol.

## 3. Item 1 — global map and contact

**Checks.** Field membership of the quotient coordinates; extension of the rational map to a morphism; the lift through the normalization; nonconstancy; surjectivity; dimension of `C_Q8`; the finite-place transfer through the Kummer lift; the set-theoretic conclusion (3.2).

**Decisive evidence.** (i) Leaf4 review claim 1: `theta, pi, q, S` are Kummer-character-zero and lie in `K=C(x)` along an actual trajectory; gate review section 7: `Z=q^9/n^10 in C(x)` with `pi=p^9=nu/n`. So the trajectory defines a rational map from `P1_x`, which extends to a morphism to the projective closure (smooth complete source, projective target), is dominant onto the irreducible curve `C_Q8`, hence factors uniquely through the projective normalization (universal property; `P1` normal). (ii) `C_Q8` is a curve: gate review section 4 proves `Ô ≅ E[[w]]`, a domain, so exactly one algebraic component of dimension one passes through each Q8 contact; the same completion is `Qbar[[w]]` after base change, so the geometric component is also unique and the contact is a smooth, unibranch point — `q_Q8` is the unique normalization point over it. (iii) Nonconstancy: `Z` nonconstant (section 4 below), so the morphism is nonconstant; a nonconstant morphism of complete irreducible curves is surjective, so `q_Q8` has nonempty preimage. (iv) Finite exclusion: if `phi(x0)=q_Q8` at finite `x0`, then `n(phi(x0))=n0` pins `p(x0)^9=nu/n0=pi0` and `theta(phi(x0))=0` forces `a0(x0)=0`, so the trajectory point is one of the coefficient-space Q8 nodes (root `v0`, some ninth root `p0` of `pi0`). The trajectory germ at `x0` is irreducible and nonconstant into `C_Q8`; the parity branch's quotient image meets `C_Q8` in finitely many points (the divided rows vanish on the parity curve only at `det(J_N)=0`, i.e. only at the eight contacts), so the germ lies on the non-parity branch. The normalization-jet review section 6 exclusion then applies verbatim — proved over the field `E=Q[v]/(Q8)`, hence uniform over all eight conjugate contacts, robust to cancellation, ramification, fractional `u`-valuations, and every ninth-root choice of `p0` ("variation of `p0` or `v0`"). Contradiction, so no finite preimage; hence `phi^{-1}(q_Q8)={infinity_x}` set-theoretically, exactly (3.2).

**Flaw / scope.** None false. The report compresses the germ-on-the-non-parity-branch step into "the harmless finite Kummer lift"; the full step is forced by reviewed facts as above. The conclusion remains conditional on the image lying in `C_Q8`, as stated.

## 4. Item 2 — passport parity

**Checks.** Every unit/nonzero assertion in (4.1)–(4.5); positivity and finiteness of `a=ord_infinity(a0)`; the cubing step; the `e_pass`-versus-Kummer-three distinction; the erratum.

**Decisive evidence.** `Z!=0` is not implicit: infinity report section 2 derives `r8` nonconstant from `9*r8'=j/u`, `j!=0`, `u!=0`, hence `S=r8^9` and `Z=S/nu^10` nonzero and nonconstant — exactly the terminal review's section 10 discharge route (i). Unit inventory, each traced to a reviewed parent: `pi0=nu/n0` nonzero (gate `n0` unit, fibre `nu!=0`); `S0=rho0^9` and `S1` units (leaf4 claim 3 gcd certificates over `E`, valid at all eight contacts by `gcd(Q8,Q8')=1`); `Z0=S0/nu^10!=0`; `lambda^3=Z0` so `lambda!=0` and finite (balanced degrees, terminal review section 7); `3*lambda^2!=0` in characteristic zero. Orders: `ord_infinity(pi∘phi)=0` since `pi(q_Q8)=pi0` is a nonzero finite value, so `ord_infinity(theta)=2a` with `a=ord_infinity(a0)`, `a0 in C(x)` (Kummer-fixed). Positivity: `theta(q_Q8)=0` and `phi(infinity)=q_Q8` give `2a>0`; finiteness: `a0 ≡ 0` would force the quotient image into the `w=0` locus, which meets `C_Q8` in finitely many points, contradicting surjectivity. The formal expansion `S-S0=S1*theta+O(theta^2)` pulls back functorially along the completed local map at `infinity`, `S1` is a nonzero constant, and the tail has order `>=4a`, so `ord_infinity(Z-Z0)=2a`, which is (4.2). Cubing: `Z-Z0=(T-lambda)(T^2+lambda*T+lambda^2)` and the cofactor takes the value `3*lambda^2!=0` at `infinity`, so `e_pass=ord_infinity(T-lambda)=2a`, even and `>=2` — the parity of `e_pass` traces to the parity-involution square `theta=a0^2/pi`, not to the Kummer cube; the report keeps `e_pass` and the Kummer order three explicitly distinct. Decisive cross-check: (4.5) gives `deg(h)=3*(e_pass+1)=3*(2a+1)`, which coincides exactly with the normalization-jet review section 7 independently derived necessary condition `deg(h)=3*(2e+1)` with `e=a`. Two independent routes agree.

**Flaw / scope.** None. The failure mode `lambda=0` (which would break the cofactor-unit step) is excluded by `Z0!=0`.

## 5. Item 3 — Mason equality

**Checks.** (5.2); pairwise coprimality; squarefreeness of the finite lambda-fibre; the radical count; the infinity contribution; the non-obstruction reading.

**Decisive evidence.** `ord_infinity(T-lambda)=deg(B_T)-deg(A_T-lambda*B_T)` for balanced degrees, so `deg(C_T)=D-e_pass` exactly — this is the terminal review's reviewed `e=D-deg(G)`. Coprimality: `gcd(A_T,B_T)=1` (reduced), and any common factor of `C_T` with either divides the other since `lambda!=0`. Squarefreeness of `C_T`: identical to the reviewed "every finite lambda-preimage is simple" (terminal review section 8, point 1); the infinity report's re-derivation via the Wronskian-support condition (3.3) is the same argument (a multiple root `x0` of `C_T` has `B_T(x0)!=0`, hence `A_T(x0)!=0`, yet `W(x0)=0`, contradicting `support_finite(W) ⊂ supp(A_T*B_T)`). Radical count: pairwise coprimality gives `deg rad = r+s+(D-e_pass)`, and the reviewed `r+s=e_pass+1` yields `D+1`, valid also at the edge `e_pass=D` (`C_T` a nonzero constant, contributing zero). Mason–Stothers for the coprime, not-all-constant identity `A_T+(-lambda*B_T)=C_T` bounds `max-deg=D <= (D+1)-1=D`: equality. The polynomial Mason statement carries no separate infinity term; the infinity ramification `e_pass-1` over `lambda` is exactly what the reviewed Riemann–Hurwitz saturation `r+s=e_pass+1` encodes, which is why equality holds. Decision, as charged: Mason sharpness is only a non-obstruction — the bound is met, no passport is excluded, and the reviewed `D=1,2` cyclic controls plus the section 6 control witness nonempty survivors. No promotion beyond a classification signal is made.

**Flaw / scope.** None.

## 6. Item 4 — terminal/Kummer converse and the `e_pass=2` control

**Checks.** Every identity in (6.1)–(6.6) and (7.1)–(7.4), by hand; the cube/constant cases; original-row versus ninth-power.

**Decisive evidence.** Control: `W=A'B-AB'=-2x`; `T'=-2/x^3`; `h=T^2/(T')^3=-x^5(x^2+1)^2/8`, degree 9, finite multiplicities `(5,2,2)`, not all divisible by three, so `h` noncube while `3|deg(h)`; passport `(1,1)/(2)/(2)` with `r+s=3=e_pass+1`; `C_T=1`, radical degree `3=D+1`; `h^3(Z')^9=3^9*T^24=3^9*Z^8`, i.e. (2.2) at `nu=1`, `j=3`. Kummer lift: `u^3=-x^9*xi^6/8=-x^9T^2/8=h`; `u^2*R=(x^6*xi^4/4)*(4/(x^4(x^2+1)))=xi=r8` using `xi^4=xi*T`; `r8^9=T^3=Z`; `9*xi'=3T'/xi^2`, so `9*r8'*u=3`, i.e. the original row `9*r8'=j/u` with `j=3` — verified symbolically, not via its ninth power. Characters under `sigma(xi)=zeta^2*xi`: `u` picks up `zeta^4=zeta` (character one), `r8=xi` character two, `R` character zero — matching the reviewed character requirements (`char(r8)=2`, `r8=u^2*R`, `R in K`). General converse: `u^3=alpha^3*xi^6/(T')^3=C*T^2/(T')^3=h`; `beta^9=j^9/(3^9*C^3)=nu^10`, which is exactly the retained scalar, so `r8^9=beta^9*T^3=nu^10*Z`; `9*r8'=3*beta*T'/xi^2=j/u` iff `3*beta=j/alpha`, true by definition of `beta`; `r8/u^2=beta*(T')^2/(alpha^2*T) in C(x)`, so `r8=u^2*R` holds; `C(x)(u)=C(x)(xi)` since `u in C(x)(xi)` and `xi=u^2*(T')^2/(alpha^2*T)`. Hidden cases searched: constant `T` is excluded upstream by `Z` nonconstant (erratum); the cube case cannot occur in the selected scope since `[h]=[T]^{-1}` in `C(x)^*/cubes` (terminal review section 6), so `h` noncube iff `T` noncube, and the report honestly notes the construction degrades to the trivial Kummer branch under a cube specialization. The only root extraction anywhere is `alpha^3=C` of a nonzero constant. The replay's assertions for (6.1)–(6.6), including the cubic-extension arithmetic with the logarithmic-derivative rule `(xi^i)'=i*xi^i*T'/(3T)`, are each hand-true; the frozen payload matches.

**Flaw / scope.** None. The converse is exactly at the Kummer-core/original-row tier; the report states it can still lose all coefficient-fibre information, and section 8 charges the seven-row lift, the global `theta,pi,q,S,tau,Delta` relation, both Taylor families at `r=A_source/9`, coprimality/boundaries, and any actual Keller pair. Accurate.

## 7. Item 5 — finite-field Galois certificates

**Checks.** The octic's identity with the reviewed parent; content; good primes; both monic reductions; the root and quotient at 53; squarefreeness; the Rabin logic; Dedekind for a non-monic polynomial; the primitivity group theory.

**Decisive evidence.** The displayed `Q8(v)` is coefficient-for-coefficient the erratum octic reconfirmed by Gaussian elimination in the formal-branch review, and `Q8_Z` in `replay.py` is its exact negation (leading term `+999`). Content: `999=3^3*37`, `296=2^3*37`, `gcd(37,24)=1`, so content one — hand-verified. Good primes: `999 ≡ 5 (mod 7)` and `999 ≡ 45 (mod 53)`, both nonzero. Hand reduction mod 7 gives ascending `[4,5,6,3,2,5,3,6,5]`; multiplying by `3=5^{-1}` yields the monic `[5,1,4,2,6,1,2,4,1]` — equal to the frozen certificate and, decisively, to the independent mod-7 certificate hand-verified inside the normalization-jet review: two independent artifacts certify the same irreducibility. Hand reduction mod 53 gives ascending `[29,22,42,24,47,21,20,2,45]`; multiplying by `33=45^{-1}` yields `[3,37,8,50,14,4,24,13,1]`, the frozen monic. Hand evaluation with `13^2=10, 13^3=24, 13^4=47, 13^5=28, 13^6=46, 13^7=15, 13^8=36` gives `f(13) ≡ 212 ≡ 0`; synthetic division by `v-13` reproduces the frozen degree-seven quotient `[12,47,3,29,46,44,26,1]` with zero remainder; and `F7(13) ≡ 177 ≡ 18 != 0`, so given `F7` irreducible (frozen), `13` is simple, the pattern is exactly `[1,7]`, and squarefreeness mod 53 follows without trusting the separate gcd. Rabin logic is correct at both degrees: for `n=8` the sole prime divisor is 2, so `gcd(f, v^(p^4)-v)=1` plus `v^(p^8) ≡ v` is necessary and sufficient; for `n=7` the sole prime divisor is 7, giving `gcd(F7, v^53-v)=1` plus `v^(53^7) ≡ v`. Irreducible mod 7 with degree preserved and content one lifts to irreducibility over `Z`, hence `Q`, by Gauss: transitivity. Dedekind at a non-monic `f` with `p` prime to `lc(f)`: pass to the monicization `lc^(n-1)*f(w/lc)`, whose mod-`p` factor pattern equals `f`'s under the invertible substitution `w -> w/lc` and whose roots are the `lc`-scalings of `f`'s roots, so the Frobenius cycle types `[8]` at 7 and `[1,7]` at 53 act on the eight roots as stated. Primitivity: a nontrivial block in a transitive degree-eight action has size 2 or 4 (block size divides the degree); the full stabilizers of such partitions in `S8` are `S2 wr S4` of order `2^4*4!=384=2^7*3` and `S4 wr S2` of order `(4!)^2*2=1152=2^7*3^2`; neither is divisible by 7, so by Lagrange neither contains an element of order 7, and the group, containing a seven-cycle, preserves no nontrivial block system. (Independent route: a seven-cycle's orbits have sizes 1 and 7; a block of size 2 or 4 would be a union of orbit pieces or generate 7 pairwise-disjoint blocks inside 8 points — impossible either way.)

**Flaw / scope.** None. Frozen-trusted residue as inventoried in section 2; the monicization fine print is tacit in the report but standard and holds at both primes.

## 8. Item 6 — geometric-component inference

**Checks.** Well-definedness of the grouping; the equivalence relation; Galois-equivariance; the eight-contact identification with the eight roots; each named attack (reducibility over the coefficient field, branch swapping, intersections, non-geometric components, normalization identifications).

**Decisive evidence.** The correct object is the divided quotient scheme of the gate: six equations `r1/t=r3/t=r5/t=r7/t=r2=r4=0` in `(w,c,d2,d4,x1,x3,x5)`. All eight tails have rational coefficients and the geometric `p=1` parity-quotient substitution (`a0=t(1+w*c^3)`, etc.) has rational coefficients, so this scheme is defined over `Q`; the fibre parameter `nu` does not enter it. The contact coordinates (base values and solved unit tangent data) lie in `Q(v0)` — in particular `v=x3/x5-2` separates them — so the eight contacts are one `Gal(Qbar/Q)`-orbit and the action on contacts is exactly the action on the eight roots of `Q8`. The gate review's algebraization (`Ô ≅ E[[w]]` a domain, hence `Qbar[[w]]` geometrically) gives: exactly one geometric irreducible component through each contact, of dimension one, containing the image of the unique selected non-parity branch, with the contact a smooth unibranch point. Hence `i ~ j` iff `comp(i)=comp(j)` is an honest equivalence relation (fibres of a map). Equivariance: `sigma(comp(i))` is a component through `contact(sigma(i))`, hence equals `comp(sigma(i))` by uniqueness; for `sigma` fixing the root, uniqueness forces `sigma(comp(i))=comp(i)`, so the relation factors through `Gal(L/Q)` for `L` the splitting field — the exact group certified in item 5. A transitive group makes all classes equal-sized; primitivity leaves only the two trivial partitions, which is (1.1). The named attacks: reducibility over the coefficient field is moot because the grouping is by geometric components of a `Q`-scheme; branch swapping is moot because the divided scheme carries exactly one branch per contact (the parity branch does not satisfy the divided rows away from the contacts, since `r_odd/t` restricts on the parity curve to the odd linearization, vanishing only where `det(J_N)=0`, i.e. only at the eight contacts — and independently the parity branch lies in the `Q`-rational involution-fixed locus, so it could never be exchanged with a non-parity branch); intersections of distinct selected components away from contacts do not affect the relation, and intersections at a contact are impossible by uniqueness; non-geometric (i.e. `Q`-rational but geometrically reducible) components are handled because Galois permutes their geometric pieces; the normalization never identifies distinct points (disjoint fibres) and plays no role in the partition itself. Components disjoint from all contacts are exactly the components not through any contact — uniqueness makes "through a contact" and "selected" coincide — and are correctly left unclassified.

**Flaw / scope.** No false step. The report asserts "preserved by Galois" without exhibiting the mechanism; the mechanism (Q-definedness of the divided scheme plus reviewed uniqueness of the component through each contact plus `Q(v0)`-rationality of the contact coordinates) is fully available from the reviewed parents and is supplied above. Recorded as the smallest implicit hypothesis; non-blocking.

## 9. Item 7 — conditional combination and scope

**Checks.** The one-source-point argument; survival of the singleton alternative; the quantifier over contacts of one component; every firewall.

**Decisive evidence.** If a selected component `C` contains contacts `i != j`, both are smooth unibranch points with distinct normalization points `q_i != q_j` (distinct chart points, disjoint normalization fibres). The infinity theorem quantifies over "one of the eight" selecting branches, and both `branch_i` and `branch_j` select the same `C`; the local (2.1) units and the finite-place exclusion are reviewed uniformly over `E`, so the theorem applies at each contact, giving `phi^{-1}(q_i)=phi^{-1}(q_j)={infinity_x}` and the contradiction `q_i=phi(infinity)=q_j`. This is a function-theoretic triviality once (3.2) holds at both points — no Galois input is used at this step, so no base-field leakage from the trajectory side can affect the primitivity statement. Hence the eight-contact alternative of (1.1) carries no actual loaded order-three trajectory with quotient image in it; and since the frozen terminal row makes `Z` — hence the quotient image — automatically nonconstant along any actual loaded trajectory, the primitivity report's compressed phrase "cannot carry an actual trajectory" is exact, not merely conservative. The eight-singleton alternative is expressly not killed (each singleton component may use its one contact at `infinity_x`), and both artifacts say so. Firewalls verified byte-level in both reports, both registrations, both READMEs, both FREEZE scope blocks, and both payload scope strings: no choice between the two partitions; no classification of components disjoint from the Q8 boundary; no coefficient-fibre/Taylor realization; no `(9,12)` exclusion; no maximum-twelve, counterexample, or JC2 statement. The infinity report's section 8 charged list (seven-row lift; global `theta,pi,q,S,tau,Delta` relation; both Taylor families at the true center `r=A_source/9`, explicitly distinguished from the terminal numerator `A_T`; simplicity/coprimality and removed boundaries; an actual Keller pair) is complete and matches the terminal review's residue list.

**Flaw / scope.** None; the combination is stated at exactly the predecessor's conditional scope.

## 10. Smallest missing hypothesis or false step

**No false identity or false step found.** The smallest genuinely implicit hypothesis is the Galois-equivariance mechanism of the contact-to-component map in the primitivity report (section 8 above); it is forced by reviewed facts (Q-defined divided scheme; unique component through each contact; `Q(v0)`-rational contact coordinates) and is therefore non-blocking, but a successor should state it in one line. Second-order implicit steps, each discharged here: the germ-on-the-non-parity-branch step inside "the harmless finite Kummer lift" (section 3); the Dedekind monicization fine print at non-monic `Q8` (section 7); the automatic nonconstancy of the quotient image discharging the compressed trajectory phrase of primitivity section 1 (section 9).

## 11. Promotable sentences

Infinity/passport theorem:

> On the `k=mu=0`, `nu!=0` selected-Q8 lane, for any actual loaded order-three trajectory whose Kummer-fixed quotient image lies in the unique geometric irreducible quotient-curve component `C_Q8` through a corrected-Q8 contact, the image is automatically nonconstant, the induced morphism `P1_x -> `projective normalization is surjective, the complete preimage of the contact's normalization point is set-theoretically `{x=infinity}`, the balanced terminal passport index satisfies `e_pass=ord_infinity(T-lambda)=2*ord_infinity(a0)`, is even and at least two, forces `deg(h)=3*(e_pass+1) ≡ 3 (mod 6)`, and every surviving passport attains Mason–Stothers equality `deg rad(A_T*B_T*C_T)=D+1` with `max(deg A_T,deg B_T,deg C_T)=D`; the exact `T=(x^2+1)/x^2`, `lambda=1`, `e_pass=2` datum is a noncube positive control satisfying the entire terminal/Kummer subsystem including the original row `9*r8'=j/u` with `j=3`; no coefficient-fibre or Taylor realization, trajectory existence or exclusion, `(9,12)`, maximum-twelve, counterexample, or JC2 statement follows.

Primitivity theorem:

> The Galois group of the corrected octic `Q8` over `Q` acts transitively on its eight roots (irreducible mod 7 with degree preserved and content one) and contains a seven-cycle (squarefree pattern `[1,7]` at 53 via Dedekind), hence acts primitively, because a nontrivial block would have size two or four and embed the group in `S2 wr S4` (order 384) or `S4 wr S2` (order 1152), neither divisible by seven; consequently the partition of the eight corrected-Q8 contacts by the unique geometric irreducible component through each contact — Galois-invariant because the divided quotient scheme is defined over `Q` and that component is unique — is either one block of eight contacts or eight singleton blocks, conditional on the frozen finite-field certificates at `p=7` and `p=53`; the result does not choose between the two alternatives and classifies no component disjoint from the contacts.

Conditional combination:

> Conditionally on both frozen registrations, a selected quotient component containing two or more of the eight corrected-Q8 contacts cannot carry an actual loaded order-three trajectory, because each such contact's distinct normalization point would have to equal the single image point of `x=infinity`; combined with primitivity this empties the one-component alternative of the contact partition at exactly the predecessor's scope, while the eight-singleton alternative, every component disjoint from the Q8 contacts, coefficient-fibre/Taylor realization, and all `(9,12)`, maximum-twelve, counterexample, and JC2 statements remain open.

## 12. Residual byte checks for a shell-bearing session

From the repository root: `shasum -a 256 -c` on both case manifests; `shasum -a 256` of both `FREEZE.txt` files against `8d695040…` and `f25fa86f…` (the latter is prompt-only); `python3 replay.py | diff -u replay.json -` for the infinity case; for the primitivity case, re-run only on an allowed remote worker per its README. A failure of any of these would reopen this review; every relation checkable by reading is already consistent.

CONFIRMED
