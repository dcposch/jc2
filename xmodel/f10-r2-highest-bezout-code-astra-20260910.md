# r2 highest chart: prospective exact Bezout certificate instrument

2026-09-10. STATIC SOURCE ONLY, UNREVIEWED, UNEXECUTED. First action13:28:47.336060199UTC; controlling stop13:48:47.336060199UTC; reserve13:45:47.336060199UTC. Targets absent before lease. Basis0d39df3c9fd69c939a8420c54d03228b9077777d is provenance only.

The actual coefficient packet is a future literal pinned input only and has not been read locally. No imported mod89 residue, coefficient computation, syntax/import/test, execution or registration.

## Outcome: exact sufficient instrument, not an actual certificate

Prepared producer, independent checker, small entry boundary and CONTRACT. The original two-identity suggestion simplifies to ONE mandatory cF+dG=1. ROOT explicitly adopted this simplification during the task. No aP+bQ computation/diagnostic is delivered. This reduces both candidate and verification scope and removes all exceptional-fibre division.

For the actual rows at z=0,X1=1,u=X2,v=X3, full weight checks force H5=P+Qv, H6=B3+B1v+B0v², H7=C3+C2v+C1v² with degree bounds(2,1),(3,1,0),(3,2,0). Put F=Q²B3-PQB1+B0P² and G=Q²C3-PQC2+C1P². Direct expansion, over any commutative coefficient ring, gives

    F=Q²H6-H5[QB1+B0(Qv-P)],
    G=Q²H7-H5[QC2+C1(Qv-P)].

Thus a verified cF+dG=1 gives

    1=cQ²H6+dQ²H7
      -{c[QB1+B0(Qv-P)]+d[QC2+C1(Qv-P)]}H5.

This is a complete polynomial ideal certificate for the chart, including Q=0. DegF,degG<=5, but coprimality is NOT assumed. Nonunit/zero/nonconstant gcd, unsupported shape or any cap merely produces INCONCLUSIVE/STOP. No common zero/source point is inferred from a failed search.

## Independence and transport

Producer uses unchanged pinned arithmetic.py for B arithmetic and new small bounded B[u] Euclid. Checker uses ONLY unchanged check_arithmetic.py, independently re-forms literal P7, re-extracts all three original sparse rows to a dense table, reconstructs F/G and checks all77 rational positions of cF+dG. It imports neither producer nor producer arithmetic and trusts no claimed F/G or gcd. Shared entry handles metadata/strict JSON only. Both reject wrong S/vartheta support and wrong full weights BEFORE taking z=0; no leading coefficient is silently assumed nonzero. Full seven coordinates, not an embedding, are retained.

Baseline SHA binds the accepted source reconstruction. This narrower checker does not rerun the whole original source checker, and that packet still needs its separately selected meaningful negatives before promotion. If the future bound identity is checked, the charged X1=0 origin-only endpoint and weighted scaling cover all nonzero geometric points. Then the charged cyclic-core theorem's parameter-sequence condition is discharged, yielding its conditional dimension35/seven-per-class consequences. This does not construct a basis or any full-source membership certificate and does not close r2/F10/JC2.

## Entry, limits and prospective control

The17-entry CLI in CONTRACT is disabled absent explicit nonzero ROOT/self/entry pins and exact EC2/tag checks. These bindings are not authentication; ROOT owns pre-exec pinning, exact isolated script directory, original arithmetic files, native closure, caps, registration and all custody. The old authority interface is neither changed nor reused under a fictitious new mode. No new runner/supervisor.

Input<=15MiB with exact1652675-byte pin; certificate<=2MiB; receipt<=32KiB; fractions canonical/reduced strings,4096-character integer bound and4300 process ceiling. Cofactors<=degree5, Euclid<=8divisions, producer intermediate degree<=16/checker product<=10,32768-bit post-operation bounds. External CPU/RSS/wall remain essential inside unchanged arithmetic. No cap is raised; exact profiles need separate registration within the existing joint envelope. No predicted speed/height/nonzero degree result.

A single prospective meaningful control changes constant coordinate0 of c if F!=0, otherwise d (G must then be nonzero), by+1, preserving canonical representation. It changes the checked identity by a nonzero entire-B polynomial. Expected final ValueError is CHECK FAILED: full B[u] Bezout identity, not parser or resource failure. No control code, fixture or run is supplied. CONTRACT also records zero/constant/degree-drop and omitted-correction countercontrols.

## Read scope and completion

Nine exact current input pins, explicit prior-WHOLE reuse and fresh reads are in READ-SCOPE. Only the permitted final infinity gate was read; no old mod89 report or uncharged residue premise. New source and contract manually read whole; no execution/import/AST/syntax/test/fixture. Publication uses the existing transaction only.

## OPENS RAISED

None new. Existing exact quantity is whether the actual F/G admit this bounded witness; cheapest prospective test is this independently checked Euclid certificate after first static review and ROOT registration, with unknown cost. If unsuccessful, no repeated or enlarged search is authorized.

## COLLISIONS

EMPTY. Exact report, transaction and box absent before lease. Own whole/OPEN check and no-overwrite final collision check precede the unique completion marker. No shared or old frozen files changed.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5236`.
- Body SHA-256:
  `11e8e3ae38c754312dc2f3f1ae9dc743346899c65a2227d462407bc2197e542d`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
