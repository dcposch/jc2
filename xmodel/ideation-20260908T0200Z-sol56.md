# Blind full-spectrum critical scan — Sol 5.6

September 8, 2026; blind against the frozen 02:07:32 packet. JC2 remains
unresolved. I verified all frozen input digests. I used full `AUDIT.md` only
for named/synonym history lookup, as the packet directs. I did not inspect the
live nonodd review or any coordinator/peer blind body. The 15h odd theorem is
PROMOTED; the nonodd theorem is PROVISIONAL/root-checked; the golden and common
discriminators are root-checked controls. Current pins certify frozen bytes and
provenance, not a theorem beyond those tiers.
No new external evidence is assumed or asserted.

## Scope and disposition

| ID | Disposition |
|---:|:---|
| 01 | unchanged |
| 02 | unchanged |
| 03 | unchanged |
| 04 | lower |
| 05 | unchanged |
| 06 | unchanged |
| 07 | unchanged |
| 08 | unchanged |
| 09 | unchanged |
| 10 | unchanged |
| 11 | unchanged |
| 12 | unchanged |
| 13 | unchanged |
| 14 | unchanged |
| 15 | unchanged |
| 16 | unchanged |
| 17 | unchanged |
| 18 | unchanged |
| 19 | unchanged |
| 20 | unchanged |
| 21 | unchanged |
| 22 | unchanged |
| 23 | unchanged |
| 24 | unchanged |
| 25 | unchanged |
| 26 | unchanged |
| 27 | unchanged |
| 28 | unchanged |
| 29 | raise |
| 30 | unchanged |
| 31 | unchanged |
| 32 | unchanged |
| 33 | unchanged |
| 34 | unchanged |
| 35 | unchanged |
| 36 | unchanged |
| 37 | unchanged |
| 38 | raise |
| 39 | unchanged |
| 40 | unchanged |
| 41 | unchanged |
| 42 | unchanged |
| 43 | unchanged |
| 44 | unchanged |
| 45 | unchanged |
| 46 | unchanged |

Changed IDs only:

- **4 lower:** the promoted direct source argument retires the exact odd client
  without a finite-boundary degeneration, while finite-arc enumeration is
  explicitly stopped and never supplied guarded-to-boundary existence. Formal
  algebraization remains an orthogonal disproof avenue, but it should not own
  the active D125 proof budget.
- **29 raise:** Hamiltonian centralizer/approximate-generator extraction is no
  longer merely a reformulation on this frontier: with a source-derived
  filtration and product-of-branches initial, it gives the accepted 15h
  exclusion. The raise is for source-compatible centralizer arguments, not the
  unsupported universal LND conclusion.
- **38 raise narrowly:** the failed positive-`k` grading concerned the literal
  saturated source row `-2484-k`. A receiver-local support-functional test after
  the valid common_4 centering is a different, exact use of associated gradeds;
  it neither tropicalizes away saturation nor asserts a homogeneous source
  quotient. History searches for common_4/centering, adaptive filtration,
  support functional, bifiltration, and Farkas found the recorded transfer gap
  and straight-grading failure, but not this test.

## Ranked bottlenecks and portfolio

The principal **global proof bottleneck** is still a source-compatible uniform
receiver theorem for the arbitrary-degree coverage output: 44 `u_s=1` and 20
split rows are necessary configurations only at `n<=200`, not polynomial pairs
or an all-degree list. The nearest decisive proof test is the independent gate
on the parity-free unequal/Q theorem. If it survives, one complete receiver
client disappears; five field/polygon clients and the global coverage arrow do
not. Next are the squarefree common_4 interface, where polynomial centering is
available, then common_3 (the literal centering has a unique `g^-15` pole), and
the golden repeated-root clients, where reduced-branch injection loses a real
nilpotent. K16's all-`m` product target and separate `b=0` boundary remain a
secondary independent proof front.

The principal **disproof bottleneck** remains properness of a COMPLETE guarded
source ideal, or exact coordinates in a nonzero finite Q-algebra satisfying
every row. Receiver-only points are insufficient. The unchanged and weighted
300-second solves and the hybrid81 solve are spent/inconclusive; no prime,
order, cap, or AWS retry is justified. Fixed-support characteristic-`p` lifting
and source-derived collision geometry remain the strongest orthogonal
construction routes, but neither has a current bounded polynomial candidate.

The genuinely new mechanism proposed here is a **receiver-local adaptive
filtration** for centered common_4: search for a linear support functional that
makes the already accepted cone/centralizer proof go through without claiming
the old unequal weights. This connects avenue 1's exact receiver polygons,
avenue 29's approximate generator, and avenue 38's associated-graded language.
It is not the failed global positive-`k` grading and does not transfer
common_4 to unequal by matching names.

## Detailed cards (three total)

### Card 1 — KNOWN / immediate: hostile gate for the provisional nonodd theorem

**Object/obstruction/mechanism.** The parity-free unequal/Q degree-(15,25)
receiver with all five scalar `B` kernels, product-of-three branch initials,
and positive combined homogeneity replacing oddness.

**Dependencies.** Accepted: the minimal monomial receiver interface and the
generic centralizer lemma now included in promoted 15h. Provisional only:
`d125-nonodd-cone-discriminator-astra-20260908.md`. Missing are independent
checks of recursive `R_s` division, all five kernel shifts and their later
terms, normal-block injection, every rational-`eta` zero pattern, and the Euler
step component by component. There is no map to golden/common or arbitrary
degree.

**Cheapest decisive test.** One <=20-minute different-model desk gate on those
arrows, using only free degree-3/5 scalar identities and changed-object controls.
The strongest falsification attack is to retain `b4,b2,b0`, allow `u` or `v`
to vanish independently on each branch, and seek a nonzero homogeneous lower
`B` initial or commuting monic pair. Confirmation retires unequal/Q only;
REFUTED/GAP leaves it live and identifies the first broken arrow. **Stop** at
that exact verdict: no descendant, source expansion, or solve. Expected gain:
very high and immediate.

### Card 2 — NEW pending reconciliation: `C4-FILTER`

**Object/obstruction/mechanism.** Apply the valid common_4 polynomial centering
`p_old=p+mu`, retain its exact squarefree/Q polygon, and replace the unavailable
legacy bound `w<=3/5` by an unknown rational functional `L(i,j)=ai+bj`.
Enumerate contact order `r=1,...,14` and require exactly the inequalities the
15h proof consumes: division is filtration-nonincreasing; the scalar kernels
are late; support upgrades `H|F_j^2` to the needed first-contact factor;
normal remainders inject into the full squarefree branch product; and the
degree-3/5 initial bracket occurs before order 36.

**Dependencies.** Accepted 15h algebraic identities and accepted minimal
receiver; root-checked common_4 centering only. Missing are existence of such
`L`, an A-reference respecting all centered support, and proof that the full
Jacobian—not faces—supplies every strict inequality. The commuting fixture
`R^3,R^5` is a mandatory negative control and is not a nonzero-J source.

**Cheapest decisive test.** `OPEN[C4-FILTER]`: decide feasibility of the finite
rational inequality system for the centered squarefree/Q support. Cheapest
test: a stdlib exact-Fraction enumerator plus a checkable Farkas witness,
<=20 minutes, no expanded high powers. Feasible means only a candidate proof
port requiring hostile review; infeasible closes support-only single-linear-
filtration ports, not common_4. A false exclusion of the commuting fixture
invalidates the encoding. **Stop** after feasibility/witness; no source ideal
or solver. Expected gain: high relative to cost because either outcome sharply
types the first common_4 map.

### Card 3 — NEW systems UPGRADE: sealed-launch attestation

**Object/obstruction/mechanism.** The frozen prep metadata correctly said
`invitations_launched=false` and root seal “not checked”; after invitation, a
lane can establish ordering only by separately hashing/stat'ing the coordinator
artifact. Add a small immutable launch attestation containing coordinator
artifact-manifest hash, verified full hash/mode, verification UTC, packet and
prompt hashes, and invitation UTC/tag—never body bytes.

**Dependencies/missing map.** Reuse the accepted artifact transaction and lane
launcher. The missing map is machine-readable `coordinator finalized and
verified -> invitations permitted`. **Cheapest test:** two local dry fixtures,
one unsealed (must refuse) and one finalized read-only (must emit attestation),
then verify that changing artifact or prompt rejects; <=10 minutes. If it blocks
and adds negligible packet bytes, retain it; if it requires blind-body access
or races finalization, reject and keep the manual root check. **Stop** before
adapter migration, deployment, or shared edits in this round. Expected gain:
medium: it removes a recurring blind-integrity ambiguity, not a mathematical
bottleneck.

## Connections, attacks, and decisions

The strongest proof attack is Card 1, followed only on confirmation by the
common_4 filtration discriminator. The strongest falsification attack is the
full zero-pattern/kernel attack inside Card 1; for Card 2 it is an exact Farkas
certificate plus the commuting negative control. The mathematical software
priority is the tiny exact `C4-FILTER` discriminator, not another Gröbner
wrapper.

**Continue:** the nonodd independent gate; source-compatible all-degree
receiver work only when it names an actual invariant/map; K16 product-target
research as an independent secondary front. Preserve complete-source
properness as the fastest sufficient counterexample endpoint, but desk-only
until a new structural reduction exists.

**Redesign:** common_4 around its centered exact support rather than an asserted
unequal transfer. Any golden successor must retain the doubled branch/conormal
information; reduced normalization has already failed. Common_3 needs an
actual polynomial/source map, not `p+mu/g`. A future solver must expose a new
elimination theorem or measured decision method, not merely a third order.

**Stop:** all work on the retired odd/lambda2=0 guarded client; serial finite
boundary arcs; axis-Bezout/unit-only variants; the golden squarefree/reduced-
branch port; the literal common_3 Laurent shift; and every automatic rerun of
the 81-variable or unchanged full-row ideals. No promotion, relaunch, external
operation, or new exit-price assertion is made here.

## Systems

Routing remains sensible: Sol for exact interfaces/instruments, Fable for the
pending hostile gate, Astra for high-context source proofs; ceilings are not
occupancy targets. The exact 15h slice plus named-history use of the large audit
keeps retrieval cost controlled. Review propagation should cancel every
descendant whose only client was odd/lambda2=0, without deleting retained
evidence. All owned AWS arithmetic is stopped and EBS custody is retained; no
new spend is recommended. Apart from Card 3, make **NO_CHANGE** during the
48-hour machinery trial and measure report-to-ingestion latency, missed-clock
rate, duplicate launches prevented, and correction cost.

<!-- BODY-END -->
