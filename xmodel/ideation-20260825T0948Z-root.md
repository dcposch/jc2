# Coordinator blind whole-portfolio scan — `20260825T0948Z`

Basis: sealed packet SHA
`73cb3cc174d2f0d1cc3bd4af0150a0e83cc05d705372a624d159bffe5f96265b`.
I did not read any sibling submission before freezing this note.  This is a
strategy report, not mathematical evidence or promotion.

## 1. Disposition vector over all 46 avenues

`R` means raise/reopen at a sharply named client, `L` lower/stop, and `=` no
global change.  An instrumental use does not turn a low-ranked avenue into a
standalone proof route.

```text
 1 =   2 R   3 =   4 R   5 =   6 R   7 R   8 =   9 =  10 L
11 L  12 =  13 =  14 =  15 =  16 =  17 =  18 =  19 R  20 =
21 R  22 =  23 =  24 =  25 R  26 R  27 R  28 R  29 =  30 =
31 R  32 R  33 L  34 =  35 =  36 R  37 =  38 R  39 R  40 =
41 L  42 =  43 =  44 L  45 =  46 R
```

- Rows 2/4/6/7/25--28/31/38/39 are raised only for the concrete
  **rational-source / positive-genus specialization obstruction** now
  instantiated by selected Q8.  This is not a claim that generic topology,
  BMY, tropicalization, or cohomology has independently advanced.
- Rows 19/21/32/36 are raised together for a collision-augmented fixed-support
  Witt tower.  Row 21 is used for compact inverse limits, not for a claimed
  p-adic injectivity theorem.
- Row 46 is raised as exact certification/software.  Rows 10,11,33,41,44
  remain stopped at their banked negative/refuted scope.
- Rows 1 and 3 remain useful finite proof engines but acquire no landing or
  ceiling.  Rows 13--18,20,22--24,29,30,34,35,37,40,42,43,45 receive no new
  primary allocation absent a named client or external theorem.

## 2. Reranked bottlenecks

### Proof side

1. **Promote or break the selected-Q8 genus composition.**  The standalone
   `g(H)>0` certificate is now exact at producer tier.  The shortest route is
   two hostile reviews in parallel: exact finite-field counting, and the
   cross-characteristic/proper-model/component chain.  Generic degree-one and
   seven-coordinate graph reconstruction no longer gate this trajectory
   scope and should not receive foreground capacity unless the genus chain
   gaps.
2. **Turn one success into a coverage mechanism.**  Inventory every other
   max-12 formal boundary branch for which an actual trajectory supplies a
   nonconstant `P1` map and a full arithmetic contact.  For each, search for a
   positive-genus component in one good reduction.  The real question after
   Q8 is whether this is a repeatable leaf killer or a one-off curve.
3. **Global landing and a cofinal ceiling remain untouched.**  Even a promoted
   selected-Q8 exclusion closes only its registered leaf.  `G2-PSC`, pure
   Sigray full landing/coverage, the relevant `G2-BD` uses, and an absolute or
   cofinal type/degree bound remain the proof wall.

### Counterexample side

1. **AS source-complete continuation:** decide the full accepted Q5/H6 fibre,
   not the two displayed points, with literal chronological determinant replay.
2. **Collision compatibility, not map-only longevity:** add an explicit
   off-diagonal collision to the bounded-support AS scheme as early as
   possible.  A map-only Witt tower can specialize from a characteristic-zero
   automorphism and is not a counterexample signal.
3. **Fixed-support inverse limit:** if one finite support has collision-bearing
   points at every depth, a compatible branch gives a characteristic-zero
   counterexample by finite-type nonemptiness; rational/eventually-periodic
   digit coefficients are unnecessary.  Proving the branch, rather than
   rationalizing its digits, is the genuine all-depth debt.

TD6 remains an orthogonal proof-side structural lane: finish the full source
module and exact exceptional support before deciding whether it deserves
more than the present 15--20 percent.

## 3. New mechanism and cross-connections

### A. Residue-genus trajectory sieve

The Q8 success suggests a reusable theorem/compiler, not merely one curve.
Whenever a hypothetical boundary trajectory gives a nonconstant morphism
`P1 -> Y_bar`, a full arithmetic contact attaches `Y` to a special source
component `C`, and `C` maps separably onto a geometrically integral curve
`H_p` with `g(H_p)>0`, regular proper specialization plus Luroth excludes the
trajectory.  Source degree one, global coordinate graphs, and all-contact
no-merger are unnecessary.

This joins rows 2/4 (formal/Sigray trajectory components), 6/7 (rational
one-place/asymptotic objects), 25--28 (passport, monodromy, infinity and
proper-model geometry), and 31/38/39 (integral models, Newton/singularity
data, and the concrete class `H^0(omega)`), with finite-field point counting
as an instrument.  The new test is to apply the same compiler to the next
two genuinely different max-12 leaves.  A second and third success would
justify seeking a cofinal genus-growth theorem; two clean genus-zero controls
would stop that extrapolation.

### B. Collision-augmented Witt compactness

Rows 19,21,32,36 should be fused at the source, not composed at the end.  For
a fixed polynomial support introduce two input points and equations

```text
P(a,b)=P(c,d),   Q(a,b)=Q(c,d),   u*(a-c)=1,
```

using the Artin--Schreier seed where `a-c` is a unit modulo 3.  Include these
variables and equations in every chronological carry state.  If the finite
sets of solutions modulo `3^n` are nonempty at every depth, their reduction
tree is finitely branching; Konig gives a `Z_3` point.  The resulting finite
polynomial system over `Q` has a `Q_3` point, hence is a proper ideal and has
a `Qbar` (therefore complex) point.  The unit equation preserves a genuine
off-diagonal collision.  Thus a fixed-support, collision-bearing compatible
tower already algebraizes existentially to a characteristic-zero polynomial
counterexample.  No periodic digits or rational coefficient reconstruction
is required.

The firewall is equally important: without the two collision equations,
even an all-depth bounded map-only tower proves only existence of a
characteristic-zero Keller map, entirely compatible with an automorphism.

### C. Pivot-free obstruction modules

AS restoration and TD6 denominators are two presentations of the same
question: does a new source row vanish in the cokernel of all earlier source
rows over the *unlocalized* coordinate ring?  Compile the augmented module
and its Fitting/annihilator ideal fraction-free.  A certified unit closes the
family; otherwise its minimal support primes are the exact exceptional
divisors.  Pivot denominators become diagnostics only.  For any surviving
one-dimensional divisor carrying a putative trajectory, feed its normalized
mod-p image into mechanism A rather than proliferating manual pivot orders.

## 4. Three executable idea cards

### Card 1 — promote and propagate the Q8 residue-genus obstruction

- **Dependencies:** frozen positive-genus report/manifest; reviewed `H`
  integrality and mod-127 source component; reviewed `40960>37010` contact;
  arithmetic `R[[w]]` bridge; proper-model specialization; primitive grouping;
  infinity passport.
- **Cheapest discriminator:** one hostile independent replay of the exact
  `F_(127^2)` count and one hostile source-by-source audit of the composition.
  In parallel, make a scope inventory of the remaining max-12 formal leaves.
- **Outcomes:** confirmation promotes exclusion only of actual trajectories on
  selected components meeting corrected Q8 contacts; a gap identifies the
  exact missing parent; refutation rolls back the composition but leaves the
  standalone finite-field theorem at its checked scope.
- **First AWS/software task:** package a generic sharded smooth-point counter
  over `F_(p^n)`, singular-point subtraction, contact splitting field, and a
  certificate checker; apply it to the next two non-Q8 boundary curves.
- **Stop condition:** stop foreground Q8 graph/degree-one jobs after the
  composition is confirmed unless they answer a different coverage question;
  abandon cofinal-genus extrapolation if two independent next leaves are
  genuinely genus zero or lack an arithmetic contact.
- **Expected information gain:** maximal; it can close the live selected-Q8
  proof leaf within one review cycle and test a new repeatable architecture.
- **Forbidden inference:** no whole `(9,12)`, max-12, landing, ceiling, or JC2
  claim from this one leaf.

### Card 2 — collision-augment the AS tree and prove finite-type descent

- **Dependencies:** exact fixed support/cap, source-complete chronological
  determinant compiler, explicit Artin--Schreier collision residues, and a
  unit-separated off-diagonal chart.
- **Cheapest discriminator:** before another deep digit, solve the collision-
  augmented system modulo 9 and modulo 27 over the current honest AS support,
  allowing collision-point digits to vary.  Directly replay determinant and
  both image equalities.
- **Outcomes:** early emptiness sharply lowers AS as a counterexample lane;
  survival creates a materially stronger state root; all-depth compatible
  survival at fixed support yields a complex counterexample by the finite-type
  argument above.
- **First AWS task:** extend the source-DAG SMT emitter with four point
  coordinates, one inverse variable, two image-equality blocks, omission
  controls, direct model replay, and proof-checked UNSAT.
- **Stop condition:** stop treating map-only depth as counterexample progress
  if collision augmentation repeatedly dies; stop rational-periodic digit
  reconstruction once fixed-support collision-bearing compactness is proved.
- **Expected information gain:** very high; it tests the missing counterexample
  property early and removes a falsely separate algebraization burden.
- **Forbidden inference:** finite-depth survival, or any map-only tower, is not
  a counterexample.

### Card 3 — finish TD6 through a fraction-free Fitting certificate

- **Dependencies:** both V50 full-row audits, exact V55/V56 disjoint-union
  checker, V54b's recorded nonunit pivots, and literal source ancestry.
- **Cheapest discriminator:** compute the unlocalized augmented-module Fitting
  ideal and verify the candidate syzygy against every original row.  Compare
  its support primes to the observed `U^4 H` denominator.
- **Outcomes:** a unit/Bezout certificate closes the exact registered family;
  support only on already source-closed divisors permits a finite exact cover;
  a new prime becomes a named boundary family, not a global failure.
- **First AWS task:** complete V54b/V55/V56, then run one fraction-free
  syzygy/Fitting implementation and an independent term-order replay.
- **Stop condition:** no more pivot-order variants after the exact module
  support is known; redesign if the support grows beyond a finite source-
  typed divisor list.
- **Expected information gain:** moderate-high and orthogonal to Q8/AS.
- **Forbidden inference:** quotient-field equality, gcd-one denominators, or
  one pivot order never implies a global cover.

## 5. Allocation and lane decisions

- **Q8: continue composition, redesign breadth.**  Allocate about 40% of
  reasoning until the two reviews close, then move most Q8 compute from
  redundant degree-one reconstruction to residue-genus propagation.
- **AS: continue the global Q5/H6 fibre; add collision early.**  Allocate
  about 30%.  Do not deepen a map-only survivor indefinitely before testing
  the collision chart.
- **TD6: continue at 20%.**  Preserve V50/V53 until the sharded union is
  proved equivalent; pivot enumeration ends when the unlocalized module
  support is known.
- **Global reserve: 10%.**  Keep landing/`G2-PSC`/`G2-BD`/ceiling work alive;
  a local genus victory must not starve the only tasks that can globalize a
  finite collection of leaf exclusions.

All heavy computation is AWS-only.  Reviews and reversible successors run in
parallel.  No current result proves or disproves JC2.
