# Fable 5 — Q+E5 two-pole local-index R3 repair

Work only in `/Users/dc/code/math/jc2`. Repair your R2 packet in a fresh R3; never mutate R2.

Read in full:

- `xmodel/m2-caseiii-two-pole-e5-local-index-repair-fable5-20260829.md`
- `xmodel/m2-caseiii-two-pole-e5-local-index-r2-hostile-review-opus5-20260829.md`
- every file in `cases/m2_caseiii_two_pole_e5_local_index_r2_20260829/`
- only the exact canonical sources cited by those reports.

Preserve the independently confirmed theorem layer: Lemma A, the M-graded bound/equality theorem, exact Q+E5 elimination, the three multiplicity regimes, and the fact that incoming `nu_U` is absent/free. Repair the two blocking findings exactly:

1. Delete the false infinite fixed-`(kbar,nu_G)` pattern-fibre claim and every illegal example/certificate produced by bypassing strict (NE). Replace it with the strongest correct finite-fibre theorem you can prove from the true cell laws; incorporate Opus's closed-form bound and largest-found-nine census only after independently deriving/replaying them. Explicitly mark the six old §7.1 countercells `M=1`/MP2-dead and state the arity-graded scope of any no-MP2-alive result.
2. Replace the false `dp | dq iff kbar in {3,4}` engine migration rule by the correct general identity `kbar in {w+d : d | w}` (with exact hypotheses and rational/integer normalization). Make the solver total on legal `w=1,2,3` and broader regression values; no uncaught `ValueError` may stand in for a mathematical verdict.

Rebuild all manifests/certificates from legal cells only. Add permanent mutations for the old `(38,19)`-style NE bypass, the `w=1`/`w=3` specialisation bug, and any MP2-status misstatement. Ordinary and `python3 -O` suites must agree. Do not edit canonical files or the legacy engine, remove `NUCAP=500`, commit, push, use AWS, or access any nested formalization tree.

Write a fresh packet `cases/m2_caseiii_two_pole_e5_local_index_r3_20260829/` and report `xmodel/m2-caseiii-two-pole-e5-local-index-r3-repair-fable5-20260829.md`. End at `SOURCE_READY_FOR_DIFFERENT_MODEL_REVIEW`, with a literal R2-to-R3 theorem/function inventory, migration-safe pseudocode, scope firewall, and body/full SHA-256 seals.
