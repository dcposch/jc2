cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
Done. Both owned files are complete and the marker is the last line; all writers are idle.

**Outcome.** The gate is written at `xmodel/f10-necessary-rows-design-gate-fable5-20260911.md` with custody in the same-tag box. All eight pre-pins and post-pins match the charged list. Verdicts:

- **A CONFIRMED.** The Laurent-q branch (produce.py 265-277 plus the two length conjuncts at 332; check.py 390-404) is dead arithmetic in both frozen bodies. Its only later readers are inventory length checks. Every callee returns a fresh object and none of the module-level writers (UNITS, DENOMINATORS, WIRE_TERMS, initialize) is reached from the branch. The 150/111/39 partition, 15 units, 125 denominators, SCALE `A_inverse`, scalar and matrix inverses, and the leading guard all remain the same computations. Deleting it only removes the branch's own STOP paths, so acceptance widens but no retained value or predicate changes. This is a whole-domain dataflow argument, not an inference from ROOT's one run.
- **B CONFIRMED** at the banked 23z/19z scopes. Canonical inverse-polynomiality is a hypothesis on the canonical circuit inside 23z, never an observed property of the alternative. The only removed numerical predicate is the Laurent envelope bound, which is implied by the raw envelope checks already enforced upstream, so no theorem premise is lost. Only service is surrendered: the alternative inverse and pole coefficients become NOT_COMPUTED.
- **C CONFIRMED** with one smallest omission: the `authority` module is outside the eight inputs and must be explicitly pinned in the source vector and rebound to the new job tag. I also flagged that a rename sweep of "direct" would break the required rejection literal at check.py 502, and that ASSERTIONS/ROOT pins cite older SHAs than this packet's frozen bodies.
- **D READY** for bounded authoring plus a diff-only DELTA FIRST; no execution, launch or rank outcome is approved. Runtime remains unmeasured because ROOT's failed run died inside the deleted branch before SCALE, diagnostics and the graph stage.

**One disclosure.** The custody file tags the closeout write as 05:46; the patch actually applied at 05:45:48. I left it rather than write after the marker.

**Next.** ROOT seals the report; the cheapest next step is the two bounded deletion authors, then one DELTA FIRST on the diff plus authority/contract pins, then a single registered-place run with the T+X1^27 control.
DIVERT path=xmodel/f10-necessary-rows-design-gate-fable5-20260911.md status=CLEAN body_bytes=13070 body_sha256=8e07e419d321b09ecd13c61a1c5fdc3dbaf9a099c1a1ebedf9b9fe6ef280073b
