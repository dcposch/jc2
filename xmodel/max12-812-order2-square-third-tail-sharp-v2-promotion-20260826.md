# `(8,12)` order two: promotion of the sharpened square third-tail gate

Date: 2026-08-26

Status: **PROMOTED EXACT NECESSARY THEOREM; NARROW INTEGRAL-`Lambda` SCOPE.**

## Promoted theorem

In the matched square chart

```text
L=z^2+p/2,
K=L^2+Lambda*R,
N=L*M+Lambda*S,
k10=Lambda*kappa,
```

with `M,R,S` linear, vanishing of the complete divided third-tail source
rows forces

```text
M=0.
```

This holds for fixed `p` and for the displayed integral tangent
`p=Lambda*p1`.  The exact source identity is the lower-unitriangular Faber
image of the negative Laurent coefficients of

```text
P/(16*L^3),
P=T*(12*B*L^2-T^2),
T=2*L*R+M,
B=R^2+S.
```

Six negative Laurent coefficients suffice by the degree-six denominator;
the producer checked all seven frozen ordinary rows.  For `p!=0`, reduction
modulo the squarefree quadratic `L` gives `P=-M^3 mod L`, hence `M=0`.  For
`p=0`, `L=z^2` and `M=alpha*z+beta`; full divisibility `z^6|P` gives first
`[z^0]P=-beta^3`, then `[z^3](P|beta=0)=-alpha^3`, hence again `M=0` for
arbitrary linear `R,S`.

Consequently the named nonzero zero-load square/discriminant first-contact
directions and their quadruple-root limit fail this necessary gate.

## Custody

```text
producer RESULT
5a966b1d4dfde21e51bbaa6120a7cf118b05e68cfc0878aa2d1c345c6a0f47a0

source FREEZE
96cb2824fb5c901e7485ba725f5d92e8f116b32a8a9dde7fd6c59cb901be013e

evidence RESULTS.sha256
6ae5fe22481ac4ec085006d91919b35b9ff5b85abd05517b502850fdc07bb26b

hostile review
26273432ca285d4ea8947a05a3fed64271552acbb6231948a4690c37c8edcd12
```

The four producer lanes are exact `Q` and `F_65521` control replays of fixed
`p` and `p=Lambda*p1`.  Only exact `Q` carries the characteristic-zero
theorem.  The hostile review independently confirmed every charged hash,
the V1-to-V2 extraction-only repair, the degree count, both p-zero
sentinels, and the scope.

## Firewall

This promotion does not cover fractional or ramified positive-order slopes
of `p` or `k10`, any post-`M=0` cusp or odd face, Newton-fan exhaustiveness,
terminal `[6,2]`, either Taylor family, construction or exclusion of a
strict arc, the whole square component, order two, `(8,12)`, maximum twelve,
or JC2.
