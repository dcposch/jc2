# Grok 4.6 primary audit — finite-pole full-actual scope

You are an equal-standing JC2 researcher. Work independently from clean basis
`92ebe92ad5986a47f01af9ed901260595dfed869`. Read and hash
`xmodel/post1224-next-wave-packet-20260829T1335Z.md`, the three charged
two-pole theorem/review artifacts, the literal Sigray source passages, and
the current canonical scope. Use the generated ROUNDVIEW only as context.

Own `FINITE-POLE-FULL-ACTUAL-SCOPE/v1`. Hostilely determine whether the
fixed-fibre rooted-tree theorem really extends from the two-pole path union
to an arbitrary finite union of pole paths. Reconstruct
`a(P)=max_j min(v_j,O(P,P_j))`; attack shared prefixes, repeated endpoints,
merge vertices, pole arrivals, root-orbit-to-microchild typing, descendants
that might re-enter the union, and pairwise disjointness of complete actual
cv exit sets. Separate the purely combinatorial attachment theorem from the
source typing and single-C7.1 budget consumer.

Then search the current reduced books/history for a real typed `s>=3`
consumer. If none exists, say `NO_CURRENT_CONSUMER`; do not fabricate an EN
splice diagram or reopen Avenue 27. Determine whether this is a genuine scope
promotion, already proved/promoted history, or a duplicate with no action.

Give a binary verdict, exact maximum theorem, literal source citations,
countermodels for any missing hypothesis, consumer inventory, and smallest
next action. No heavy local computation or AWS. No attainment, occurrence,
cross-fibre, or new numerical price claim is licensed.

Write exactly
`xmodel/finite-pole-full-actual-scope-grok46-92e-20260829.md` and no other
repository file. Seal its body with exact byte count and SHA-256. Because
this audits carrier scope rather than declaring a new numerical exit price,
`charge_basis` should normally be absent.
