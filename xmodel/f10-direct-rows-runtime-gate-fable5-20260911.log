cli=2.1.259 (Claude Code) model=fable effort=max mode=bypassPermissions shell=yes auth=claude.ai max_output_tokens=128000
Both owned files are complete and sealed only by the marker. Writers are idle. Nothing else is needed.

**Outcome.** The disabled direct-rows runtime port is a static CONDITIONAL ACCEPT. Acceptance is conditional on the combined source FIRST, on ROOT deriving the seven refusal triples from the final source text, and on a fresh ROOT registration. No blocking static defect was found in dispatch.py, probe.py, mutate.py or the disabled template.

**Verdicts A–F**

- **A CONFIRMED.** Eleven-key authorities match authority.py exactly. The five refusals and two wrong-tag entries call the actual entries with the nine-token argv. Each mutation reaches a named authority.py branch. refuse-caps and refuse-inventory reach the same branch and will produce identical bytes. The scheme is acyclic. The open ROOT obligation is to derive the seven exit/stderr triples from the final produce.py and check.py text, not from an observed run.
- **B CONFIRMED.** The probe never spoofs argv or calls scientific authorize. Its policy digest contains a ROOT path and no ROOT hash. No always-refuse contradiction was found. Two resource points are unmeasured: full-closure rehashing inside the 5 s/3 s profile, and the dummy's RSS staying under 32 MiB before its fork.
- **C CONFIRMED with one GAP.** The mutator authenticates the positive record and reconstructs the check-positive authority without importing science. The edit is exactly coordinate 0 of exponent (27,0,0,0) in slot 24, plus 1 modulo p, with insert and delete cases. The "inverse restoration" is a copy-restore. It proves no collateral change but does not exercise the arithmetic inverse. A wrong-coordinate edit would pass it unnoticed. The optional fix is a true minus-1 re-parse in mutate.py only.
- **D CONFIRMED.** Thirteen phases, no retry, one fixture, frozen 0444 and 0555, fresh negative hashes, and the exact exit-2 stderr predicate with no traceback tolerance.
- **E CONFIRMED.** All caps, budgets, custody and live-capture rules are outside the diff. The diff changes only the intended bindings, literals and the refusal source. The inherited transient-process false STOP is retained.
- **F CONDITIONAL ACCEPT.** Blockers are the source FIRST and the refusal derivation. Everything else is an honestly unformed registration field or an unmeasured resource unknown.

**Disclosures.** One apply_patch write measured about 1740 words, above the 1500-word bound. One D–F attempt failed on a fragment anchor and wrote nothing. The custody post-pin block first landed after the title and was moved to the end by a further patch. The report's wc -w count exceeds 3000 because hashes, table syntax and line-number tokens count as words.

**Custody.** All 13 inputs matched the charged pins before reading, after reading, and again at the end. Every 64-hex token in both owned files matches the live pin list. The marker is the last line of the report. Finished at 04:00:45 UTC, inside the 04:13 reserve.

```
a02f120acda2439197a9fc173ec068ec0a5a208ec8d15f6b96cc9dfe65ee6ff9  xmodel/f10-direct-rows-runtime-gate-fable5-20260911.md
f8ec3eed50b809cb7795968519ac2964fbc208235df20e7e0dfc4eac6eb4412f  box/f10-direct-rows-runtime-gate-fable5-20260911/input_custody.md
```
DIVERT path=xmodel/f10-direct-rows-runtime-gate-fable5-20260911.md status=CLEAN body_bytes=25954 body_sha256=a02f120acda2439197a9fc173ec068ec0a5a208ec8d15f6b96cc9dfe65ee6ff9
