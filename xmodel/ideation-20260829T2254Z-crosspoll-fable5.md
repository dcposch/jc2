# Adversarial cross-pollination — Fable 5 — round `20260829T2254Z`

Researcher: Fable 5 (equal standing; cross-pollination lane).
Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172` (verified `git rev-parse HEAD`).
Packet `xmodel/ideation-20260829T2254Z-crosspoll-packet.md` recomputed
`2cf36dad…c76f9cb`, body `f090063e…`; all thirteen sealed inputs it names were
recomputed and matched before reading (five blind, six reviewed/integrated,
two provisional exact). No file created after the sealed packet was read; the
active Fable normal-cone hostile-review lane's mutable output was not read,
and every computation delegated to it is marked `REVIEW ACTIVE` below, never
confirmed by anticipation. No web, AWS, canonical edit, commit, push, or
`jc2-lean` access. All new checks in this report are pure-Python exact
Gaussian-rational desk computations (seconds each, far under 60 s / 1 GiB)
plus hand algebra; the checker lives in `/tmp` scratch only. No exit price is
asserted or consumed anywhere here, so no `charge_basis` line appears.

Typing discipline: a finite jet is not an arc; an arc is not a germ, map,
occurrence, attainment, or JC2; set equality is not scheme equality; floors
are not attainment; Grok is `UNAVAILABLE_NO_MODEL_WORK`, not an abstention;
post-blind events do not alter blind novelty attribution.

---

## 1. Deduplicated novelty table

| # | Mechanism (origin) | Classification | Notes on convergence / disagreement |
|---|---|---|---|
| 1 | Unloaded seven-quadric degeneracy quartic `Delta` (Opus §1) | `NEW EXACT PRODUCER / REVIEW ACTIVE` — with one **wording refutation** (§3.3 below) | The seven-quadric elimination statement is logically sound conditional on the CAS replay (`REVIEW ACTIVE`, my other lane). The "projectivised normal cone of `V(I)`" sentence is **false as typed**: `J^17 subset I` makes `J/I` nilpotent, so that Proj is empty over every point. Repair to the degree-two leading system, which §3.3 proves is exactly generated by the seven quadrics. |
| 2 | `J^17 subset I` + valuation sandwich (Sol §4) | `NEW EXACT COMPOSITION / KNOWN REES METHOD` — **PROVED** (§3.1–3.2) | Independently re-proved here; minimal exponent sharpened to `N in [5,16]` with `N<=16` equivalent to one explicit membership. Convergence: `I subset J^2` computed independently by Sol (replay) and Opus (blind §1.2); still integration-gated. |
| 3 | Split-coordinate prepass / `JET-IMAGE` / `RAMFAN` (Opus §6, Sol-alt §6, Sol Card C) | `DUPLICATE FAMILY, MERGE` | Merged spec `RAMFAN-TC/v1` in §6. Three independent front ends for one compiler — genuine 3-way convergence on the need. |
| 4 | Off-diagonal pair-square QCS module (Sol-alt Card 1) | `NEW APPLICATION / KNOWN FIBRE-SQUARE OBJECT` | Strongest proof-side object on file; the injection `iota_F` is still **unconstructed**, and compactification independence and `dim C_coll = E_gen` are unproven first discriminators (§5.2). |
| 5 | ACV count `#atypical >= s-1` (Fable Card 3) | `NEW MECHANISM FOR KNOWN GAP` — survives with a **named missing premise** (self-attack §7) | The control battery needs a pinned `s`-dictionary for non-Keller controls and at least one `>=3`-boundary-place control; my blind battery had neither pinned. |
| 6 | Collision-partner Galois descent (Opus Card C) | `SPECULATIVE / MISSING PREMISE NAMED` (§5.2) | The correct nonclosed field is `C(s)` (the boundary parameter; the surrogate's `sqrt(1+3s^2/2)` is literally a `C(s)`-quadratic datum). But polynomial origin does **not** force profile data into `C(s)`: Puiseux-conjugate branch pairs are generic for polynomial curves. Cheap witness search stated. |
| 7 | Intermediate block surface `B_K` (Sol-alt Card 2) | `KNOWN MECHANISM / SHARPENING` — audited (§5.3) | `B_K subset C[x,y]` and finiteness over `C[f,g]` verified; the factor-Jacobian lemma is sound **including the degree-one endpoint** (Ax–Białynicki-Birula); load-bearing gaps: smoothness of `Y_K` is not automatic and nontrivial-block existence is the other horn of the dichotomy. |
| 8 | Tougeron decision layer on the `e=3` fixture (Fable Card 1) | `POST-EVENT DEFER/REDESIGN` (self-attack §7) | The client branch is provisionally dead at G9; worse, my own gradient lemma already implied the Tougeron constant degenerates along surface-hugging jets. Reactivation condition stated verbatim in §4.4. |
| 9 | Rational-termination on the literal nodal chart (Sol Card B; overlaps Fable Card 2) | `REFUTED CLIENT` — **strengthened here** (§5.1) | The literal-chart regularity hypothesis excludes every quasi-finite map (contracted-axis lemma), not merely this control. Both my enlarged-ansatz card and Sol's termination card lose the client; only occurrence/chart-typing survives. |
| 10 | Monomial-boundary divisibility note (Sol post-blind) | `CONFIRMED / SCOPE-DEGENERATE` (§5.1) | Proof independently re-derived; also shown equivalent at its scope to an elementary contracted-axis argument. Valid as a negative design result only. |
| 11 | Sol blind "nodal control passes the first transverse gate" vs Sol post-blind correction | `SCOPE-CONFLICT, resolved` | Both true at their scopes: the free `K[t]` Bezout equation (5.1) passes; the polynomial-support submodule fails at the same coefficient. The blind statement must not be quoted without the support-module qualifier. |
| 12 | `Delta`-branch contact heuristic (Opus §1.8(c), Card A) | `WEAKENED AND SHARPENED` (§4.2–4.3) | New exact identity (machine-checked): within the G9-kill family, G8-compatibility `alpha-eps*8i*beta=-4t^2` **is** the condition `ord_tau Delta >= 4`. The heuristic is retro-validated at G8 and blocked at G9 by a gauge question the kill family cannot express (§4.3). |
| 13 | Avenue-33 period reopen (Fable §3.3) | `REFUTED (self)` — recorded to prevent re-derivation | Unchanged. |
| 14 | Sectioned output / cap retry / preflight / claim lint (all four models) | `SYSTEMS FAMILY, DEDUPLICATE` | One trial selected in §9; Opus's "checkpoint at 80% inside one response" sub-idea is rejected as impossible per the packet; its per-section budget idea is absorbed. 3-of-4 convergence on output-cap recovery as the real gap. |
| 15 | Avenue dispositions | convergence/disagreement preserved | 4/4 blind reports raise Avenue 4 execution; 4/4 keep the global selector as proof gap #1. Disagreement: Sol-alt reopens 16/32 and raises 7/30/31/38 vs my 45-unchanged conservatism vs Sol's {3,4,31,36}. Synthesis: adopt object-gated reopens only — 16/32 get a client only if the pair-square module survives its two discriminators; 31 is reopened as **tool** (Opus's executed Rees client); 27's PALF sub-lane stays lowered. |

---

## 2. Mandated verdicts (binary or typed)

| Claim | Verdict |
|---|---|
| `J^17 subset I` | **PROVED** (pigeonhole over the four promoted quintic identities; §3.1). Char-0/`Q`-algebra scope only (multiplier denominators). |
| Valuation sandwich `2 v(J) <= v(I) <= 17 v(J)` | **PROVED** for valuations nonnegative on the ring with `Q` in the value-0 part (centered equicharacteristic-zero DVR arcs); upper leg rides the promoted quintics, lower leg rides `I subset J^2` (twice-computed, integration-gated). What follows / does not follow: §3.2. |
| Maximum safe `Delta` theorem | **Seven-quadric statement: SOUND conditional on the saturation/elimination replay (`REVIEW ACTIVE`)**; logical elimination audit passes (§3.4). **Actual-normal-cone wording: REFUTED AS TYPED** — `Proj gr_J(A/I)` is empty over every point because `(J/I)^17=0` (§3.3). Safe repaired statement in §3.3. |
| Monomial-chart divisibility | **CONFIRMED** (independent re-proof) and **SCOPE-DEGENERATE**: the hypothesis class contains no quasi-finite map at all (§5.1). |
| G9 scope | **PROVISIONAL, CONSISTENT, NARROWER THAN ADVERTISED PENDING ONE GAUGE LEMMA.** Corroborated at the frozen fixture point by my sealed all-faces F4 probe (row-6 value `i/32` = `eps*i*q^3/32` at `eps=+1,q=1`). The family gauge-fixes the tangential `tau^3` slots (`a=b=0`) without proving their irrelevance at G9; §4.1. Different-model review still required; premise G0–G8 review-gated; `q=0` and non-stable strata open. |

---

## 3. Mandatory attack 1 — unloaded ideal, Rees sandwich, and the cone

### 3.1 `J^17 subset I`: proved, and why 17 is only a bound

`J^17` is generated by the degree-17 monomials in `f0,f1,f2,f5`. Any exponent
vector summing to 17 with all entries `<= 4` would sum to at most 16, so some
exponent is `>= 5` and the monomial is divisible by a certified fifth power,
hence lies in `I`. This consumes exactly the four identities
`f_a^5 = sum_j c_{a,j} r_j`, which are **promoted** (integration `d5294dab`,
reviewer-proved sharp). `17 = 4*(5-1)+1` is the universal pigeonhole constant
for four generators at power five; it is not sharp *a priori* because mixed
identities can put products in `I` even when no single exponent reaches 5.

Sharper bracket, new here: the minimal `N` with `J^N subset I` satisfies
`5 <= N <= 17`, and

```text
J^16 subset I  <=>  f0^4 f1^4 f2^4 f5^4 in I,
```

since `(4,4,4,4)` is the unique degree-16 exponent vector with all entries
`<= 4`; every other generator of `J^16` is covered by a fifth power. The lower
bound is my reviewer-proved `f_a^4 notin I`. Deciding the single membership
(degree-28 polynomial, six variables) is a bounded AWS Groebner task, not desk
work; it is folded into Launch L3. In fact for each `N in {13,...,16}` the
undecided generators are the few all-`<=4` monomials, so the whole tail of the
minimal-`N` question is a short explicit membership list.

### 3.2 The sandwich: exactly what follows

For a valuation `v >= 0` on the ring with `v` zero on `Q^*` (any arc into an
equicharacteristic-zero DVR, `v(A) = min` over generators): `I subset J^2`
gives `v(r_i) >= 2 v(J)` termwise; picking `a` with `v(f_a) = v(J)`,
`f_a^17 in I` gives `17 v(J) = v(f_a^17) >= v(I)`. Hence
`2 v(J) <= v(I) <= 17 v(J)`, with the degenerate cases consistent
(`v(J)=0 => v(I)=0`; `v(J)=infinity => v(I)=infinity` since each `r_i in J^2`).

Follows: linear comparability of the `I`- and `J`-adic orders along every
centered arc; a finite slope window `[2,17]` (really the finite set of Rees
ratios of `I` against `J`, each in that window); pruning power for ramified
calendars (any claimed resonance slope outside the window is instantly wrong).
Does **not** follow: equality of integral closures (`Rees` would need matched
asymptotic exponents, not a two-sided linear bound); any actual Rees value;
any license to recenter the mixed loaded source (the promoted radical
integration's firewall stands verbatim); any map or attainment. One further
consequence is negative and important: see §3.3.

### 3.3 Initial-ideal audit: degree two is exact, the cone wording is not

This is the logical audit the packet assigns to this lane; the heavy
saturation/elimination confirmation is delegated to the active normal-cone
review lane and marked `REVIEW ACTIVE`, not assumed.

**Degree-two exactness (new, logical, unconditional).** Since `I subset J^2`,
every `g in I` writes `g = sum h_i r_i` and its class in `J^2/J^3` is
`sum (h_i mod J) * Q_i`. So the degree-2 graded piece of `in_J(I)` is exactly
the `O_D`-module spanned by `Q_1,...,Q_7` — no cancellation can create or
remove degree-2 leading forms. Cancellations create new generators only in
degrees `>= 3` (`in_J(I) supseteq (Q_1,...,Q_7)`, possibly strictly from
degree 3 up). Consequence: Opus's `Delta` is exactly the discriminant of the
full degree-2 leading system, not of a generator-dependent proxy; the packet's
worry is real only from degree 3 on.

**The cone wording is refuted by the other blind result.** Sol's
`J^17 subset I` (now proved, §3.1) makes `J/I` nilpotent in `A/I`:
`(J/I)^17 = 0`. Hence `gr_J(A/I)` vanishes in all degrees `>= 17`, its
irrelevant ideal is nilpotent, and

```text
Proj gr_J(A/I) = empty  over every point of D.
```

So "the projectivised normal cone of `V(I)` along its reduced support `D` is
empty over exactly the complement of `Delta = 0`" is false as typed: that
Proj is empty everywhere, `Delta` or not. Set-theoretically the normal cone
`C_D(V(I))` is the zero section; all its information is scheme-theoretic.
This is a wording refutation, not a computation refutation — the two blind
results collide head-on, and the collision is the cross-pollination content.

**Maximum safe repaired statement.**

> For the frozen unloaded rows, over any characteristic-zero field: a nonzero
> `nu in Kbar^4` with `Q_i(nu; S_0,T_0) = 0` for all `i` exists iff
> `Delta(S_0,T_0) = 0` [conditional on the CAS elimination — `REVIEW ACTIVE`].
> Equivalently, the projectivized zero-scheme of the **degree-two leading
> system** `in_J(I)_2` in the normal bundle of `D` — exactly the seven
> quadrics, by the audit above — has empty fibre exactly off `Delta`.
> Corollary (one direction only, and the direction all jet clients use): if
> `Delta(S_0,T_0) != 0`, then an unloaded jet centered at `(S_0,T_0)` admits
> no nonzero leading normal deviation direction at any order; for the mixed
> loaded source the same holds at every grade strictly below the literal load
> arrival for that cell's calendar.

Over `Delta = 0` nothing about actual jets is licensed: degree-`>=3` elements
of `in_J(I)` and the loads may still kill every direction. Only a filtered
standard basis converts `Delta = 0` into positive information (Launch L3).

### 3.4 Elimination audit and the interface checks

The elimination `iff` for the seven-quadric system itself is sound: the
incidence scheme lives in `P^3_e x A^2_{S,T}`, the projection is proper, so
the image of the closed incidence set is closed and equals `V(elimination
ideal)` over `Kbar` by the projective elimination theorem; saturation by
`(e0,e1,e2,e5)` is exactly what removes the irrelevant affine `e=0` component
and changes nothing in `P^3` (where `V(m_e)` is empty). Specialization
commutes because the fibre of the saturated incidence over `(S_0,T_0)` is the
projectivized solution set of the specialized quadrics. What is **not**
covered by logic and stays `REVIEW ACTIVE`: that the machine saturation and
the principality claim `(Delta)` are correct.

Independent hand/desk verification performed here (exact `Q(i)` arithmetic,
seconds): the h-vector `(1,4,6,4,1)` sums to `vdim 16` and equals the
regular-sequence-of-four-quadrics Hilbert function `(1+t)^4` — a consistency
check that three of seven quadrics are generically redundant, nothing more.
The singular scheme of `Delta` is exactly the node `(0,0)` plus the two
conjugate cusps `(3S+4, 27T^2+1)` — re-derived by hand from the partials and
re-checked by machine, including `Delta`'s vanishing at the cusps and
`tjurina` count `1+2+2 = vdim 5`. The parametrization
`S(lam) = (lam^4+24lam^2-432)/432, T(lam) = lam(lam^2+36)/432` kills `Delta`
identically (machine), and `lam = 0, ±6i, ±2i*sqrt(3)` land on `(-1,0)`, the
node, and the cusps respectively (hand). The `P_0` interface identities
`f0-4f2 = B - d4^2 + 64 d3^2` and `f5+2f1 = A - 2 d3 d4` are machine-exact,
so `P_0`'s cutting forms restrict to the campaign's `A,B` **and** agree with
the `A(x), B(x)` of the G9 packet's (2.1) — a three-report consistency close.
`A(ell(s,t)) = B(ell(s,t)) = 0` (tangent vectors lie in the leading
four-plane) is machine-exact. Tangential `(S,T)` and normal `(u,v)` were kept
separate throughout; the shared `+-8i` slope phenomenon is treated in §4.2 as
a lemma target, never as an identification.

### 3.5 Cleanest joint descendant

**Filtered standard basis of `I` along `J` in the split coordinates**
(preregistered AWS; Launch L3), because it simultaneously: settles the
`Delta=0` fibres scheme-theoretically (the only way the `iff` gains jet
content), yields the full `in_J(I)` hence the minimal nilpotence exponent `N`
(collapsing §3.1's bracket and sharpening the sandwich), and supplies the
graded operator that the master resonance-calendar needs. The desk-cheap
orthogonal parallel is Opus Card B step (i) (`A10|_D, A6|_D, A2|_D`
divisibility by `Delta`), which concerns the loads and is not subsumed.
Minimal-exponent-alone and a full normal-cone atlas are respectively a
byproduct and a program, not descendants.

---

## 4. Mandatory attack 2 — ramified frontier after the cutoff

### 4.1 G9 verification status and the exact unexamined strata

The provisional kill `G9_6 = eps*i*q^3/32` on `D(q)` is corroborated at one
point by my own sealed review (F4 probe: frozen fixture, row 6 forced
constant `i/32`, row 4 `-51i/32`; the G9 packet's value specializes to
exactly that). The packet's stronger content — independence from freed
`beta, y, z, u, d[7], d[8]` blocks and `k10[0..3]` — is producer-only and
same-model; different-model review remains **required**, and the G0–G8
premise chain is itself review-gated.

**Named review obligation (new, precise).** The family's grade-3 deviation is
`w_eps = cone(a,b,p,q)` with `a = b = 0` imposed. By the cone identity
`cone(a,b,0,0) = ell(b, 8a)` (machine-checked against the published cone
parametrization), the `(a,b)` slots are exactly the tangential `tau^3` data
`(S_3, T_3) = (b, 8a)` — i.e. the family truncates the tangential trajectory
at `tau^2` and gauge-fixes the cubic slot to zero. At `e=2` this was harmless
through G7 (the promoted review proved G7 free of `a,b,c,d`). At `e=3`, grade
9 is the first grade reachable by `D^2R`-polar terms carrying one `tau^3`
curve correction against `[w,w]` (`3+3+3 = 9`), so `(S_3,T_3)` generically
**can** enter G9; nothing in the frozen inputs proves row 6's combination is
free of them. The `e=2` precedent and the row-6 polar degeneracies
(`P_0`-polar row 6 identically zero, per Opus §1.5.3) make independence
plausible; plausible is not proved. The hostile review of the G9 packet must
either free `(a,b)` and re-derive `G9_6`, or prove a tangential-invariance
lemma. Until then the theorem's scope is "the `a=b=0` gauge slice of the
stable rank-one family", not "the full reduced G8-compatible family".

Exact unexamined `e=3,m=1` strata: (i) `q = 0` (rank-zero boundary /
recentering; excluded by the packet's own control 4); (ii) non-stable rank
strata from the review-gated G0–G8 fan; (iii) the `(a,b)` gauge slice above,
unless absorbed; (iv) nilpotent scheme structure (everything is field-valued);
(v) `m >= 2`, `e >= 4`, `C6 = 0`, `k10 = 0`, other supports — outside the
cell. The full G0–G9 fan already running provisionally owns (i)–(ii); **do
not duplicate it**; the `(a,b)` question should be routed to that fan's
review charge, not to a new lane.

### 4.2 Does `Delta` contact have explanatory content after the row-six unit?

Yes — more than the packet's test datum suggests, and less than Opus's
heuristic hoped. Machine-checked here (exact, symbolic in `t, alpha, beta`,
both signs):

```text
S = eps*8i*t*tau + alpha*tau^2,  T = t*tau + beta*tau^2  =>
ord_tau Delta >= 3   automatically (leading slope on the nodal tangent cone),
[tau^3] Delta = 16*eps*i*t*( alpha - 8*eps*i*beta + 4*t^2 ).
```

So on `D(t)`, **contact `>= 4` with `Delta` is exactly the G8 field
compatibility equation** `alpha - eps*8i*beta = -4t^2` — identity (0.1) of
the G9 packet. The reviewed test datum (`ord Delta = 4` for the fixture) is
therefore not a "failed tangency" record: within this family, G8-survival and
fourth-order `Delta`-contact are the same equation. Together with the forced
leading slope `S_1/T_1 = eps*8i` (the nodal tangent cone), the picture through
G8 is a grade-for-grade contact ladder.

At G9 the ladder meets the gauge question of §4.1 head-on: contact `>= 5`
requires the branch's cubic term (`S = 8iT - 4T^2 + 4iT^3 + ...`; machine
check: substituting the branch through the cubic gives `ord Delta = 5`),
i.e. a nonzero tangential `tau^3` slot — exactly the slot the family sets to
zero. Two coherent readings, both testable by the same review computation:
if `(a,b)` are forced to zero by G0–G8, then legal jets *cannot* follow the
branch past quadratic order, the G9 death is explained structurally, and
`Delta` contact is a faithful survival clock that simply runs out; if `(a,b)`
were gauge-dropped, the kill's scope is incomplete and the branch-following
sublocus `(S_3 propto 4i)` is precisely where a G9 survivor would have to
live. Either outcome upgrades or retires Opus Card A in one stroke. This is
the decision the G9 review should be charged with; no new lane is needed.

### 4.3 Verdict on Opus Card A / §1.8(c)

`WEAKENED AND SHARPENED`: as a *sufficiency* heuristic ("survivors ride
branches") it gets no support — the whole family dies at G9 regardless of
contact. As a *necessity* ladder it is now exact through G8 (§4.2), which is
strictly more than the blind report claimed and is derived, not conjectured.
Card A's standalone launch is superseded by folding its integer
(`ord_t Delta`) into the G9 review charge.

### 4.4 Tougeron/algebraization stop, and the reactivation condition

Stopped on the killed branch, per the packet. Exact reactivation condition,
restated so it is checkable: **a constructible transition survivor with a
typed finite system and verified minor/syzygy hypotheses** — concretely, a
`PROPER_NONEMPTY_FINITE_JET_IMAGE` or `DOMINANT_FINITE_JET_TRANSITION`
certificate from the merged compiler (§6) on a live cell, together with a
computed `rank DF` and minor-order `e_r` along the surviving stratum and a
certified syzygy for the residual rows. My own blind card's flaw is recorded
in §7: the gradient lemma I proved in the all-faces review already implies
the first-order minors degenerate identically along surface-hugging jets, so
the Tougeron constant `2e+1` is generically far beyond any available jet
length; the tool as I specified it would have returned `TARGET_GRADE`
unreachable or an obstruction series — informative, but not worth a lane
ahead of the G9 fan.

### 4.5 Rerank (counterexample side)

1. **G9 packet review + gauge lemma (§4.1–4.2)** — K00 critical path;
   different model; desk-scale.
2. **Source completeness** — largest multiplier, currently unowned; with the
   integer atlas now promoted closed at its finite-jet scope (R4-00 uniform
   grade-19 identity composed with R4-02 and prior valuations), every further
   local kill multiplies through this gap. Specification lane, Launch L4.
3. **Running `e=3` G0–G9 fan** — continue; do not duplicate.
4. **General `(e,m)` calendars** — after the fan lands, via the merged
   compiler keyed by leading-system/`Delta` strata and the sandwich window;
   a calendar is not an emptiness theorem.
5. **Reachability** — named owner, prose only; no cheap discriminator known.
6. **All-order lifting** — only on a survivor (§4.4 condition).
7. **Algebraization** — last; typed successor only.

Closing an atlas without an actual-map-to-source arrow is not JC2; the
promoted closures are finite-jet exclusions on one normalized support.

---

## 5. Mandatory attack 3 — proof side: QCS and selector

### 5.1 Monomial-chart divisibility: confirmed, then degraded

Independent re-proof (hand): `x^A y^B -> t^A s^(uA-B)`; for `1 <= k <= u-1`
a contributing monomial needs `A >= 1`, so `t | a_k, b_k` (2.2); the
`[s^(u-2)]` Jacobian coefficient is `sum_{i+j=u-1} (i a_i b_j' - j a_i' b_j)`
(2.3), every summand divisible by `t`; `dx wedge dy = s^(u-2) ds wedge dt`;
a nonzero constant Jacobian is impossible. **CONFIRMED**, including the
endpoint terms and the `u=2` edge case.

**New scope finding (degrading).** The regularity hypothesis says every
monomial has `B <= uA`, i.e. `F(0,y)` and `G(0,y)` are constants. Such a pair
contracts the `y`-axis to a point, so `F_y(0,y) = G_y(0,y) = 0`, the Jacobian
vanishes identically on `x = 0`, and no nonzero constant Jacobian exists —
a two-line classical argument. So the theorem, while correct, is equivalent
at its scope to the contracted-axis observation, and its hypothesis class
contains **no quasi-finite map whatsoever** — in particular it can never see
an actual Keller boundary chart, which is always translated/iterated
precisely to avoid contracting a curve. Verdict: keep as a formal negative
design result (`the literal monomial chart is the wrong client`), do not
build any continuation mathematics on it. Sol's blind (5.1) is the special
case of (2.3) with only the top layer retained; the blind/post-blind
scope conflict is resolved as in table row 11. Translated/iterated charts
evade it trivially (different support semigroup), so the surviving question
is purely occurrence: which chart class the actual Section-7 geometry
occupies. That is selector/source work, not continuation work.

### 5.2 Three QCS mechanisms, attacked

**Pair-square event module (Sol-alt Card 1).** The only proposal with an
actual-cover input. Missing premises, in order of lethality: (a) the event
chain complex is not yet defined functorially, so "compactification
independence" is currently unfalsifiable — the definition must come first;
(b) `dim C_coll(F) = E_gen` is a hoped identification with a reviewed count,
not a computation — the local boundary matrix must reproduce the promoted
collision-capacity-minus-excess formula before any injection is attempted;
(c) the injection `iota_F` is unconstructed; rank bookkeeping
(`s-1 -> E_gen`) is not a map. Cheapest honest gate, unchanged from the
card but ordered: define the complex; prove blowup invariance or exhibit a
dependence (one admissible blowup on one tame non-Keller étale-cover control
suffices to falsify); match the local matrix; only then attempt `iota_F`.
The analytic type-`(2,3)` surrogate must enter as a negative input-control
(it has no rational cover) — if the machinery silently accepts it, the
construction is a costume. **Strongest proof-side launch (L1).**

**ACV count (my Card 3).** Survives with the §7 self-repairs: pin the
`s`-dictionary for controls (for a single primitive `f`, `s` := number of
boundary places of the generic-fibre closure, i.e. distinct points at
infinity after normalization — the Sigray pole-vertex count specializes to
this; the battery is meaningless until this is written down), and include at
least one `>= 3`-place control (top form with three distinct factors, e.g.
`xy(x+y) + x`), because every Broughton-family standard has only two places
and cannot stress the inequality. Keller vacuity check retained: a Keller
member has no affine critical points (`df = 0` would kill the Jacobian), so
for the real client all atypical values are ACVs — the mechanism aims at
exactly the right set. **Falsification-side launch (L2), battery before any
lemma.**

**Galois descent (Opus Card C).** The packet demands the exact nonclosed
field: it is `C(s)`, the function field of the boundary divisor — the
reviewed surrogate's forced datum `r^2 = 1 + 3s^2/2` is literally a
quadratic extension of `C(s)`, so the idea types. The missing premise is
fatal as stated: polynomial origin does **not** force profile data into
`C(s)`; branches of polynomial curves at infinity are routinely conjugate
Puiseux pairs over `C(s)` (`s^{1/2} -> -s^{1/2}`), so "colliding lines are
conjugate over a quadratic extension" is the generic polynomial situation,
not a contradiction. Unless a chart-pinning lemma forces integral (`s`-)
exponents for the specific quotient-line data, descent proves nothing.
Cheapest discriminator flipped to the falsify side: search the reviewed germ
family for one polynomial-origin realization of a conjugate quotient-line
pair; finding one kills the card quickly and cheaply. `SPECULATIVE`; rank
third of three; no launch.

### 5.3 Block-surface sharpening, audited

Verified: `B_K subset C[x,y]` (elements of `K subset C(x,y)` integral over
`C[f,g] subset C[x,y]` are integral over the normal ring `C[x,y]`, hence in
it); `B_K` is module-finite over `C[f,g]` (Noether, char 0, finite field
extension); so `Y_K -> A^2_target` is finite. `A^2 -> Y_K` is dominant
quasi-finite of degree `[C(x,y):K]` but **not** finite for a counterexample
(else `F` would be proper), and not known étale — smoothness of `Y_K` is
exactly what the `A^2`-recognition must supply, since `g compose f` étale
plus `g` finite does not give `f` étale at singular image points.

The factor-Jacobian lemma **holds** under the `B_K ~= C[u,v]` hypothesis,
including the endpoint the card did not argue: with both planes smooth,
`dF = dpi_2 . dpi_1` forces `Jac(pi_1)` to be a unit, hence constant, hence
both factors Keller; if a factor has topological degree 1 it is birational
étale, hence injective (ZMT/open immersion), hence an automorphism by
Ax–Białynicki-Birula, so a **proper nontrivial** block (`1 < [K:C(f,g)] < d`)
yields two non-invertible Keller factors of degree `< d`, contradicting
minimality. Load-bearing gaps, named: (i) existence of a proper nontrivial
block is the imprimitivity horn — the dichotomy is genuinely two-horned and
the primitive horn is Avenue 26's client, not this card's; (ii) the
recognition input: any hypothesis entailing properness/finiteness of
`A^2 -> Y_K` is circular with nonproperness. The genuinely weaker candidate
targets are "`Y_K` smooth" (then Miyanishi–Sugie/Fujita machinery applies
with contractibility) or "`Y_K` has at worst quotient singularities plus
trivial units/class data"; whether either follows from Keller-derived
constraints is the honest open question, and a non-`A^2` normal countermodel
satisfying all derived constraints is a well-posed falsifier regardless.
Keep as the selector-side **bypass** (inside L1's portfolio row, no separate
lane this round).

### 5.4 Selection

Proof-side launch: pair-square desk gate (L1). Falsifier: ACV battery (L2).
Bypass: block-surface dichotomy at specification intensity inside L1's bypass
ownership. The global selector gap is preserved untouched by all three: none
of them lands an arbitrary minimal counterexample on a bounded client, and no
one should read L1 success as doing so.

---

## 6. Mandatory attack 4 — software merge

**`RAMFAN-TC/v1`** — one exact transition compiler subsuming
`K00-SPLIT-COORDINATE-PREPASS` (Opus), `JET-IMAGE` (Sol-alt), `RAMFAN` (Sol):

- **Input:** frozen `tails.json` + compiler conventions (hash-gated), a cell
  manifest `(e, m, load calendar, opens, stratum parametrization)`, and an
  old solution stratum.
- **Stage 1:** exact split-coordinate change `(e0,e1,e2,e5,S,T)` — a
  triangular automorphism, verdict-invariant by construction (Opus's point);
  discard the prepass if any closed-cell verdict changes, never debug it
  into agreement.
- **Stage 2:** literal grade emission; newest-block linear/cokernel rank
  before any nonlinear step; leading-system routing keyed by
  `(tangential centre, Delta-stratum, load calendar, sandwich window)`.
  **Tangential slots (`a,b`-type cone coordinates) are first-class retained
  variables** — the §4.1 gauge gap must be structurally impossible to
  reintroduce.
- **Stage 3:** typed verdicts only:
  `EMPTY_TRANSITION / PROPER_NONEMPTY_FINITE_JET_IMAGE /
  DOMINANT_FINITE_JET_TRANSITION / UNKNOWN`; no `ARC` token exists.
- **Mandatory controls:** custody fail-closed; the **propagated** fixture
  mutation `S: -4 -> -3` (must fail exactly at G8; prose and code must name
  the same object — my F2); K10 row-6 `d3^3` mutation (must shift the G9
  terminal by `k10[0]*t^3`); `q = 0` (must kill the rank-one identity).
- **Acceptance before any new mathematics is trusted:** from one code path,
  (i) reproduce the promoted `e=2,m=1` all-face G7 kill verdict, and
  (ii) reproduce the provisional `e=3` G9 row-six value `eps*i*q^3/32` and
  verdict string against the frozen replay — reproduction is a regression
  gate and promotes nothing.

Exact linear/cokernel stages run locally in seconds; nonlinear image closure
is a preregistered frozen AWS job. First client: the running G0–G9 fan's
review; second: the `(e,m)` calendar sweep of §4.5 item 4.

---

## 7. Attacks on my own blind cards

**Card 1 (`K00-ARC-DECISION`, Tougeron): conceded, `DEFER/REDESIGN`.** Three
independent faults. (a) The client branch is provisionally dead at G9, and my
own F4 probe had already shown the frozen fixture inconsistent at G9 — the
card's headline `LIFTS` outcome was never live for this branch; the honest
expected outcome was branch (iii), which the direct G9 computation delivered
anyway at lower cost. (b) My own gradient lemma (`R subset J^2 =>` all 42
gradient components vanish on `D`) implies `DF` degenerates identically along
surface-hugging jets, so the minor order `e_r` is generically large and
`2e_r+1` exceeds any available jet — the card ignored a mechanism I had
myself proved one review earlier. (c) The syzygy-certification step for the
overdetermined residual rows was hypothesized, never constructed. Reactivate
only per §4.4, retargeted to transition-image semantics.

**Card 2 (`STRICT-COLLIDE-POLY/v2`, enlarged ansatz): conceded, superseded.**
The enlarged ansatz lives in the literal monomial chart, whose
polynomial-origin instances are empty by the support module — and by §5.1's
contracted-axis finding the chart holds no quasi-finite map at all, so no
answer to the rationality question there can transfer to an actual Keller
boundary. The card's residual value (classifying which analytic continuations
are rational) is a mathematics question without a campaign client until an
occurrence result names the actual chart class. Redirect the slot to L1.

**Card 3 (`ATYP-COUNT`): survives with named repairs.** Missing premise: the
`s`-dictionary for non-Keller controls was never pinned (§5.2); battery
design fault: all my listed controls have `<= 2` boundary places, which
cannot stress `#atypical >= s-1`; at least one three-place control is now
mandatory. The falsifier-first ordering was right and is retained.

My avenue-33 reopen remains self-refuted; my one-raise disposition stands,
with the §1 table's object-gated amendments adopted.

---

## 8. Mathematical launches (four)

**L1 — `PAIR-SQUARE-QCS/v1` desk gate** *(proof)*. Dependencies: promoted
Section-7 quotient package, `Xi` margin, an actual nonproper Keller cover as
hypothesis object; no residue-A template. Owner: Opus (prove);
falsify owner: Sol (blowup/analytic-surrogate controls); bypass owner holds
the §5.3 block-surface specification. Reviewer: different model from
producer. Desk only. Order of gates: define complex -> blowup invariance ->
local matrix matches the promoted capacity-minus-excess formula -> attempt
`iota_F`. Outcomes: canonical injection = QCS rank inequality at actual-map
scope (selector still open); labelled kernel = bridge falsified (QCS and JC2
untouched); compactification dependence or forced label-discard = stop
before any topology search. Stop: any gate fails, or the surrogate control
is silently accepted.

**L2 — `ATYP-COUNT/v1a` battery** *(falsification)*. Dependencies: promoted
`E_P` identities for the eventual lemma only; the battery itself is
classical computation. First deliverable: the pinned `s`-dictionary (one
page, typed); then `<= 8` controls including `>= 1` three-place polynomial
(`xy(x+y)+x` class) and one `s=1` sanity row. Owner: Sol; independent
recomputation: Opus; reviewer distinct. Desk. Outcomes: violation with a
Jacobian-one-compatible package = P2b dead as stated, QCS re-decomposes
(major negative, worth its own round); violation only without `J=1` = the
lemma knows its load-bearing hypothesis, stage B licensed; holds everywhere
= sharpened conjecture with witnesses. Stop: battery complete; no
cap-filling by analogy.

**L3 — `UNLOADED-INITIAL-IDEAL/v1`** *(K00 exact geometry)*. Preregistered
AWS: filtered standard basis / full `in_J(I)` certificate in split
coordinates, with byproducts (minimal nilpotence exponent `N`; the §3.1
membership list for `N <= 16`; scheme-theoretic `Delta = 0` fibres). Desk
parallel: Opus Card B step (i) (`A10|_D` etc. divisibility by `Delta`).
Dependencies: promoted radical + quintics; `I subset J^2` (twice-computed,
integration-gated — consume declared, fail closed). Producer: Sol (AWS);
hostile review: Fable — explicitly as the successor of the active
normal-cone review lane, so no computation is duplicated (`REVIEW ACTIVE`
items land here). Outcomes: `in_J(I) = (Q)` in low degrees = `Delta`'s locus
gains jet content and the calendar collapses toward one graded object; extra
generators = the exact resonance corrections are enumerated (equally
useful); load divisibility positive = closed-form calendar cause; negative =
a real unification stopped. Stop: two-hour AWS cap per stage, then emit the
partial basis with a typed frontier.

**L4 — `SOURCE-COMPLETENESS-INTERFACE/v1`** *(source completeness)*. Prose
specification, no compute: the typed functor interface (what a closure-first
finite-jet exclusion on this support asserts about a hypothetical pair; what
Gate-T/receiver must supply), plus the enumerated uncovered faces
(`C6 = 0` tip, `k10 = 0`, `Jdet = 0`, `m >= 2`, other supports/load rays),
each with its cheapest named discriminator. Owner: Fable (after L3 review
duties) or Opus; attacked by all next round. Outcomes: either a coverage
theorem target becomes precise, or the impossibility of stating one exposes
which upstream object (receiver, Gate-T, normalization) is underdefined —
both are the multiplier the atlas now lacks. Stop: specification complete;
no theorem claimed.

Standing background obligations (not launches): different-model review of
the G9 kill **charged with the §4.1 gauge lemma and the §4.2 contact
decision** (recommended owner: Opus); different-model review of the
monomial-divisibility note (this report's §5.1 re-proof may seed it);
continuation of the running G0–G9 fan; the licensed bounded
`QCS-GLUE-OR-KILL` retry only under §9's contract.

---

## 9. Campaign-systems trial (exactly one) and stop/defer

**`SECTIONED-CALLS/v1`.** Long-form deliverables are pre-registered as an
ordered list of sections; the runner makes **independent section-sized
provider calls**, writes each returned section to disk as an immutable
artifact with its own marker and byte count, and a final assembly step seals
the body. No claim is made that a model can be forced to checkpoint inside
one monolithic response — sectioning works precisely because the calls are
separate. A cap or crash yields `TRUNCATED_DURING_SECTION=k` plus all
completed sections instead of nothing (the observed 76-minute/64k-token
total loss becomes bounded).

Offline stub acceptance (no live provider, no fenced tree): (1) a stub
adapter dies mid-section-3 of a three-section fixture: sections 1–2 exist,
byte counts correct, receipt names section 3; (2) rerun uncapped: sections
1–2 byte-identical, full body seals; (3) assembly refuses to seal a partial
without the typed banner; (4) rollback flag restores the monolithic path
byte-identically on a control fixture; (5) ordinary and `-O` Python agree.
Success criterion, measured over the next three real long lanes: any capped
lane yields at least one consumable section plus a typed receipt (today's
baseline is zero), added wall time `< 10%`, and zero mathematical lanes
blocked (opt-in per lane). Roll back if assembly overhead or section-seam
quality costs exceed recovered value.

**Stop/defer list.** Defer: `CAP-RECOVER/v1` (fallback if sectioning fails
acceptance; only one upgrade launches); `ADAPTER-PREFLIGHT` (the 402 path is
already correctly typed and cost no verdict — probe adds one round-trip of
value at most); `CLAIM-CONTRACT-LINT` (good next-round candidate, synthetic
fixtures first). Stop: Opus's in-response 80% checkpoint (impossible as
stated; its per-section budgets are absorbed into sectioned calls); Tougeron
tooling on the dead branch (§4.4 condition to reactivate); rational
termination and any continuation work in the literal monomial chart (§5.1);
enlarged-ansatz sweep (§7); PALF/`A2`-filling comparison (already stopped;
category empty); avenue-33 periods (self-refuted); G10 on the killed G9
branch; coefficient-blind all-`e` induction; duplicating the running G0–G9
fan; heavy Groebner on Opus's mutated rows (AWS-only if ever needed).

---

## 10. Synthesis recommendation (under 300 words)

Promote nothing from this round's blind material directly; route everything
through the named reviews. The two exact blind objects interlock: Sol's
`J^17 subset I` is proved and immediately **refutes the normal-cone wording**
of Opus's otherwise sound `Delta` theorem, whose safe form is the degree-two
leading system — exactly generated by the seven quadrics (proved here). Ship
that repaired statement to the active normal-cone review lane and let L3's
filtered standard basis decide the `Delta = 0` fibres.

The K00 critical path is now one review with two charges: confirm the
provisional G9 kill **and** decide the tangential-`tau^3` gauge question,
which this report shows is simultaneously the kill's scope boundary and the
verdict on `Delta`-contact (G8-compatibility *is* fourth-order contact —
machine-checked). Assign it to Opus; do not duplicate the running fan. The
integer atlas is promoted closed at finite-jet scope; the binding
counterexample-side gaps are source completeness (L4, unowned until now) and
reachability (named, prose).

Proof side: the pair-square gate (L1) is the only mechanism holding an
actual cover — run its two discriminators before any topology. The ACV
battery (L2) is the cheapest decisive falsifier once its `s`-dictionary is
pinned. Galois descent stays parked with its Puiseux hole named; the literal
monomial chart is retired as a client for everything (it contains no
quasi-finite map).

Software: build `RAMFAN-TC/v1` with tangential slots as first-class
variables; its acceptance is reproduction of one promoted `e=2` cell and the
frozen G9 certificate. Systems: trial sectioned calls only; defer the rest.
Total new compute this round: one preregistered AWS job (L3), everything
else desk or prose. No claim here is an arc, a map, occurrence, attainment,
or JC2.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `42096`.
- Body SHA-256:
  `0c2adb5a883121d2151fdd92f2ce7182f0cc7b84b3e15961b2d6bd2bc92da599`.
- Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`.
