# JC2 round `20260829T2254Z` — adversarial cross-pollination packet

Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`  
Lifecycle: blind collection complete; pre-synthesis; no claim promoted by this packet  
Round status: `DEGRADED` only because Grok returned HTTP 402 before model work;
the R4 compute/review worker was explicitly omitted as noninterruptible.

Read every sealed input named below completely. Agreement is evidence about
a calculation, never proof. Events after the blind cutoff are segregated and
must not be attributed to a blind researcher.

## Sealed blind inputs

```text
d1cc6a649b120253675c1694a2afc4b0c929393cd11f66c99f2635b49185236a
  xmodel/ideation-20260829T2254Z-state.md
  body 10764 / 7a5441f090607accbbc80c38c6a703d06d8986cb8dbbe0721e099e756c52dfe5

bc66a6271e0ca9096e96455336d4799cd96224825ad15d27c62d62e109c29399
  xmodel/ideation-20260829T2254Z-sol56.md
  body 17486 / a07d360e0900b7ef498f49fd83cf1c68001d207e4ff519ece151962e7e8951db

72aa958a0e69606886e035fecca8c85790ec2bb3ebc20c065938879fcdddd297
  xmodel/ideation-20260829T2254Z-sol56-alt.md
  body 28201 / 0477ab88d170343f70b1f353e88d7f112bc35308631228cdf85302c472b83fd5

af5eedb745002a12d6e2a92d7ce828db1fe9d7e3d581826bcf5305154872185b
  xmodel/ideation-20260829T2254Z-fable5.md
  body 30831 / fe68a4dbe7e845ab24079f01be92d466703f6335a66277a34808eee32a70da27

d8e7328479232094b1a77ec8560d85e0a70e4288b18e49d506602bd746d6aa36
  xmodel/ideation-20260829T2254Z-opus5.md
  body 34609 / 2b9e2f08f4e58252d984944a34e9d78fa5b596bfe1a350aadc073ed39d393b30
```

All four successful run records are `DONE`, exit zero, and
`charge_basis_status=ABSENT`. Grok's frozen run is `FAILED`, exit one, report
missing, log SHA-256
`11aba62d810085b3513d8c88f6b7ea718116865a2b10da6ce68623f5fa20fd33`;
the exact provider response is HTTP 402 / exhausted balance. Classify it
`UNAVAILABLE_NO_MODEL_WORK`, not a mathematical abstention.

## Post-blind reviewed/promoted evidence

These results completed after the snapshot. They may be consumed in
cross-pollination but do not alter blind novelty attribution.

```text
6f300d9890bb624e7c1023763271e409cc106c3f68e94436809b0ca18cf5a178
  xmodel/k00-unloaded-surface-global-radical-hostile-review-fable5-20260829.md
  body 17402 / 1300566c9b862a1c4624b893e91e426ac7295d004667ea247cab9883005b8c01

e3e84ac9a9aba58fcff731c0b75a279b8f7d62ce252e91253a708234bebcab08
  xmodel/k00-unloaded-surface-global-radical-coordinator-integration-sol56-20260829.md
  body 5312 / d5294dab7f1a8c6c6c311fed667920f7e45385ea621299cebed362595f494d64

7082bf678825bd0b148b2a9b88ddfb7cdd7f8a9e166712188770ffad0a9ed270
  xmodel/k00-r4-00-r1-r2-exact-deciders-hostile-review-opus5-20260829-r1.md
  body 26072 / 8ff0abb2be95f93276c682b617a3be67be53105914d5b3093c30f874fd6c55ca

4f89128098b1f4e4a8cec06f3f04ce14012d797e5885d40154e66fba51abd719
  xmodel/k00-r4-00-closure-coordinator-integration-sol56-20260829.md
  body 8708 / 26fbcfb5dc6fe4a8271bb9613aa6a0f3ee88cd7ff1513829fd57ec6f8516cc18

0ee84a0b98d30c4436461a09118961706d1f18fcfe9686684e7b1a27b5597feb
  xmodel/k00-ram-e2m1-allfaces-uniformity-hostile-review-fable5-20260829.md
  body 22894 / 57de629f083c4d2f7316726cd433d9ac9520a6836a2b478635587f0a79e49205

7c73616d99bd0cf3880dfedc790bcbdc055f68f800a94fd65bcae43ae4711bac
  xmodel/k00-ram-e2m1-allfaces-coordinator-integration-sol56-20260829.md
  body 9174 / 624e3abb301d17136783d1acd870b195397182d5feb40818abb19861c6a9036c
```

Consequences at their exact scopes:

- `sqrt(I)=J` for the unloaded ideal is promoted; each of the four graph
  generators has minimal power five in `I`. This is reduced-set information,
  with an explicit nonreduced firewall.
- all literal integer valuation-four V20R2 field-valued jets die by grade 19;
  this closes that normalized integer finite-jet atlas, not ramified closure,
  occurrence, maps, or JC2;
- every positive-or-infinite boundary load-order face at `e=2,m=1` on the
  declared C6/K10/Jdet opens is empty at G7. The `e=3` G8 fixture is banked as
  a finite-jet countercontrol only.

## Post-blind provisional exact evidence

```text
9fab2e9655d9481719b68b1dd170bcef1ac1e3a45cfc14e39c2c73e0701198d5
  xmodel/collision-monomial-chart-jacobian-divisibility-sol56-20260829.md
  body 6119 / e8a2a5211833529f783a49daa2738ddc7f7bd42a063ff5a0c998d8af69baa231

bbe6ac2fa8ca7b53634e3c477d1cb2f4128e4e22e2a1cf5d4049db82402fe685
  xmodel/k00-ram-e3m1-g9-rankone-kill-sol56-20260829.md
  body 9880 / 0fe19184da754151933294aa7377273d03e07f4b7830ee430d20984c53827b36
  replay 3ef20945b4a009bba4dccbbf7a43461718feb8155e84f513f14c736ce45182e2
```

The first corrects primary Sol's blind claim: the free Bezout equation for
the nodal analytic control passes, but the literal polynomial-origin support
module does not. The second gives, on both stable rank-one signs,

```text
G9_6 = epsilon*i*q^3/32 != 0 on D(q).
```

It kills the full reduced G8-compatible stable-rank-one normal-form family,
not other `e=3` rank strata and not nilpotent scheme structure. Both reports
still require different-model hostile review.

The exact tangential series in the frozen fixture is
`S=8*i*t-4*t^2`, `T=t`. Direct substitution into Opus's proposed quartic
gives

```text
Delta(S,T)=64*t^4*(1+6*i*t-t^2),       ord_t Delta = 4.
```

Thus it follows the nodal branch through the quadratic term but misses the
`+4*i*t^3` coefficient. This is a test datum, not validation of `Delta`.

## Deduplication and history checksum before attack

| Mechanism | Preliminary classification | Reason |
|---|---|---|
| Opus unloaded seven-quadric degeneracy quartic | `NEW EXACT PRODUCER / REVIEW ACTIVE` | no prior quartic or executed global unloaded normal-form discriminant; true-normal-cone wording is unverified |
| Sol `J^17 subset I`, valuation sandwich | `NEW EXACT COMPOSITION / KNOWN REES METHOD` | fifth powers are reviewed; `I subset J^2` is separately exact and independently recomputed by Opus; exponent and Rees consequences still need attack |
| K00 split coordinates / JET-IMAGE / RAMFAN compiler | `DUPLICATE FAMILY, MERGE` | three reports propose one structural coordinate/transition compiler with different front ends |
| alternate Sol actual off-diagonal pair-square QCS module | `NEW APPLICATION / KNOWN FIBRE-SQUARE OBJECT` | prior nearby-cycle/collision proposals lacked an actual cover, base, and off-diagonal functor |
| Fable asymptotic-critical-value count | `NEW MECHANISM FOR KNOWN GAP` | no prior Rabier/ACV client; `#atypical>=s-1` was already named |
| Opus collision-partner Galois descent | `NEW SPECULATIVE MECHANISM / KNOWN STRICTNESS TARGET` | must be typed over a function field; the base field is already algebraically closed |
| alternate Sol intermediate block surface | `KNOWN MECHANISM / SHARPENING` | `sol-lateral3.md` section 4 already proposed minimal topological degree, block field, `A^2` model, and lower Keller factor; the explicit `B_K` dichotomy is useful but not a new avenue |
| Fable Tougeron decision on the G8 fixture | `POST-EVENT DEFER/REDESIGN` | stable rank one now dies exactly at G9; overdetermined Tougeron hypotheses were not yet proved |
| nonlinear rational-termination on the literal nodal chart | `REFUTED CLIENT` | the polynomial support obstruction fires at the first Keller coefficient, even with all lower positive layers retained |
| sectioned output / cap recovery / preflight / claim linter | `SYSTEMS FAMILY, DEDUPLICATE` | one observed 64k failure requires a bounded recovery; provider preflight cannot restore exhausted balance; only one upgrade may launch |

## Mandatory attack 1 — unloaded ideal, Rees sandwich, and the cone

Let `I=(r1,...,r7)` and `J=(f0,f1,f2,f5)` in the frozen six-variable ring.
Attack each claim independently:

1. From four reviewed identities `f_a^5 in I`, prove or refute the universal
   pigeonhole consequence `J^17 subset I`. State why 17 is only a bound and
   test whether mixed identities lower the minimal `N`.
2. With the convention `v(A)=min{v(a):a in A}` for a centered valuation,
   check exactly whether `I subset J^2` and `J^17 subset I` give

```text
2*v(J) <= v(I) <= 17*v(J).
```

   State what follows (linear equivalence of adic topologies, finite slope
   window) and what does not (equality of integral closures, Rees values,
   loaded recentering, or a map).
3. Hostilely separate the ideal generated by the seven degree-two normal
   initial forms from the full filtered initial ideal `in_J(I)`. Initial
   forms of the chosen generators need not generate `in_J(I)` because
   cancellations can create new leading forms. Opus's `Delta` is a theorem
   about the actual normal cone only after a filtered-standard-basis or
   independent full-initial-ideal certificate. Otherwise repair it to the
   **seven-quadric leading system**.
4. Audit the elimination `iff`: saturation plus projective properness may
   make the projected incidence closed, but specialization and irrelevant
   components must be handled explicitly. Attack the h-vector, singularity,
   parametrisation, `P_0`, and polar rank-fan interfaces without confusing
   tangential `(S,T)` with normal `(u,v)`.
5. Decide the cleanest joint descendant: minimal nilpotence exponent,
   filtered standard basis, `Delta`-load restriction, or a normal-cone atlas.
   Do not launch heavy local CAS.

## Mandatory attack 2 — ramified frontier after the cutoff

Reconcile the now-reviewed all-`e=2,m=1` closure, promoted literal integer
closure, and provisional stable-rank-one `e=3,G9` kill.

- Verify the maximum G9 theorem and identify the exact unexamined `e=3`
  rank strata. A full G0--G9 fan is already running provisionally; do not
  duplicate it.
- Decide whether Opus's `Delta` contact has explanatory content after the
  row-six unit, or whether `ord Delta=4` merely records a failed tangency.
- Stop Tougeron/algebraization work on the killed branch. State the exact
  condition under which it should reactivate: a constructible transition
  survivor with a typed finite system and verified minor/syzygy hypotheses.
- Rerank general ramification calendars, source completeness/other supports,
  reachability, and all-order lifting. Closing an atlas without an
  actual-map-to-source arrow is not JC2.

## Mandatory attack 3 — proof side: QCS and selector

1. Independently verify the monomial-chart divisibility formula

```text
[s^(u-2)](f_s g_t-f_t g_s)
 = sum_(i+j=u-1)(i*a_i*b_j' - j*a_i'*b_j) in (t)
```

   for polynomial-origin pairs regular in the literal chart. Decide its
   maximum scope and whether translated/iterated charts evade it.
2. Compare three QCS mechanisms:
   - actual off-diagonal fibre-square event module and proposed injection;
   - ACV separation giving `#atypical>=s-1`;
   - function-field/Galois obstruction to zero-excess collision partners.
   For the pair-square, do not assume compactification independence or
   `dim C_coll=E_gen`; those are the first discriminators. For ACV, design a
   fixed non-Keller control battery before any theorem. For Galois descent,
   name the exact nonclosed function field and explain why conjugacy is
   forced rather than read from one square-root model.
3. Attack the intermediate block-surface sharpening. Verify
   `B_K subset C[x,y]`, finiteness over `C[f,g]`, what morphisms in the
   factor diagram are finite, and the Jacobian factor argument if
   `B_K=C[u,v]`. Identify which `A^2`-recognition hypothesis would be
   genuinely weaker than JC2/nonproperness itself.
4. Select one proof-side launch, one falsifier, and one bypass; preserve the
   global selector/source-completeness gap.

## Mandatory attack 4 — mathematical and campaign software

Merge `K00-SPLIT-COORDINATE-PREPASS`, `JET-IMAGE`, and `RAMFAN` into the
smallest reusable exact transition compiler. Its acceptance must reproduce
one closed `e=2` cell and the current `e=3,G9` row-six result before any new
mathematics is trusted.

For campaign systems, choose exactly one bounded upgrade. The observed
failure was a 76-minute Fable run whose response exceeded 64,000 output
tokens and left no report. Compare:

- independent section-sized calls with immutable partial artifacts and final
  assembly;
- one typed, one-shot cap retry with a much smaller deliverable;
- provider preflight;
- claim-contract lint.

Do not claim a runner can force a model to checkpoint inside one monolithic
provider response. Specify an offline stub acceptance test, rollback, and
measured success criterion. The upgrade must not block mathematical work.

## Required cross-pollination output

1. A deduplicated `NEW / KNOWN / DUPLICATE / SCOPE-CONFLICT / REFUTED` table
   preserving independent convergence and disagreements.
2. Binary or typed verdicts on `J^17`, the valuation sandwich, the maximum
   safe `Delta` theorem, monomial-chart divisibility, and G9 scope.
3. Direct attacks on every mandatory item, with counterexamples or missing
   premises stated explicitly.
4. At most four mathematical launches spanning proof, falsification, K00,
   and source completeness. Give dependencies, owner/reviewer, desk/AWS
   placement, both-outcome meaning, and stop conditions.
5. Exactly one bounded systems trial, plus a stop/defer list and a synthesis
   recommendation under 300 words.

No web, canonical edit, heavy local computation, AWS action, commit, push, or
`jc2-lean` access. Exact desk checks stay below 60 CPU seconds and 1 GiB when
known in advance. Write only the output named by the lane prompt and end with
one standalone `<!-- BODY-END -->`; the coordinator will stamp the seal.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `13554`.
- Body SHA-256:
  `f090063ee7b23b08473cb541fba2c077d94d86295b0e083a1274cc5be69d420d`.
- Frozen basis: `0f7ee003be45ee40d51d4048897cdacf63821172`.
