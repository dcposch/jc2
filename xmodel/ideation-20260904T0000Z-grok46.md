# Blind ideation — Grok 4.6 — round `20260904T0000Z` (fired 08:30Z)

Lane: Grok 4.6 (xAI), equal-standing independent researcher. Blind against
the sealed packet (with §4 addendum) and banked repository reports only.
No other `ideation-20260904T0000Z-*` submission was opened. No in-progress
report of a running lane was opened (`.run.v2` checked: the four follow-up
lanes `g108-delta3-kill-gate-gpt55`, `g9966-chart-necessity-opus5`,
`k16-square-resultant-fable5`, `row2515-order-gate-sol56` have no
`final_status` and were not opened; their `.prompt.md` files were read for
continue/redesign/stop only). Packet-§3 tags
`k16-subchart-q-opus5`, `k16-t6-sol56`, `k16-t11-cone-gpt55`,
`preprocess-native-gpt55`, `g9966-branchB-kill-gate-gpt55`,
`g9966-delta52-stage8-sol56` all have `final_status=DONE`. No canonical
ledger edited. `jc2-lean` not inspected.

## 0. Custody

Charged-input hashes verified mechanically from the lane receipt
`xmodel/ideation-20260904T0000Z-grok46.run.v2`: `awk` paired
`charged_input_<i>_sha256=` with `charged_input_<i>_basename=`, wrote
`/tmp/jc2-hash-manifest.txt`, `sha256sum -c` returned **4/4 OK**. Direct
`sha256sum` of `/tmp/jc2-lane.9NgFCp/inputs/` agrees. No digit retyped.

```text
ba345ef122050b2b7fd6f5ddbfe9db44d71b594f0e5138d691741dd1cc641980  ideation-20260904T0000Z-packet.md
ddbb5770e3b143f946a6d6fc9586362c6e209d45ccf814454602a95fd21158a6  ideation-20260903T1200Z-synthesis.md
e47fd16cfcc91bc7bfdac4ba1b5f46152db8235e6609dca6e29a5966549c38f5  FALLACY-v2.md
c66b70e460a609ff5210fd16015fc7b2ff18dda26f68597a0d817c5b9aeb19d5  COORDINATION.md
```

Packet frozen with addendum covering AUDIT 17(tttt)–(bbbbb). Lane receipt
basis `ce00e907002c40891a1d441badbc569100789e29`. Binding live state:
`notes.md` LIVE STATE 2026-09-04T08:22Z and EVENT 08:22Z. Freshest banked
mathematics: AUDIT 17(tttt)–(ccccc); reports named in the prompt (all
`-20260903`, all `final_status=DONE`). `FALLACY-v2` in force. No new
exit-price ⇒ no `charge_basis`. Desk CAS only (Python/SymPy/stdlib,
existing Singular logs, `pdftotext`; one core; ≪ 4 GB; no AWS).

Firewall: flag ≠ place ≠ cover series; `REPRESENTATIVE` is not
`FULL_ACTUAL_EXIT`; a floor is not attainment; a relaxation being empty
*is* a nonexistence proof (fewer equations, larger variety); a one-point
system being consistent is not a pair. Integers kept apart: campaign
geometric degree `N`; Moh `n = deg_y g = D`; `m = deg_y f`; `K = gcd(m,n)`;
Moh gcd-chain `d_i`; `V_i` of one major tower; descended 4-tuple
`(n', m', M_2', V_2', k)` is not an original skeleton.

Read: the packet including §4; APPROACHES.md 46-row table plus the
2026-09-03 overlays; AUDIT.md 17(tttt)–(ccccc) and the load-bearing
predecessors 17(nnn)–(ssss) as cited; PROGRESS.md 2026-09-03 and the
2026-09-04 morning digest; notes.md from 22:47Z through 08:22Z; the
named DONE reports; COORDINATION.md contract; `FALLACY-v2.md`. Moh
pp.208–210 via `pdftotext` (PDF page = Moh page − 139) and Xu 2016 §8.
No other `20260904T0000Z` submission.

---

## 1. Direct answer: the coordinator's Q4 triad is a false trichotomy,
and (25,15) is not a joint-chart client

The packet (as revised by §4) asks whether the all-degree program is
`(V0)` on the K = 16 ray, emptiness of the joint chart for every screened
skeleton, or a structural statement about Keller pairs at two points at
infinity. It groups (99,66), D = 108 and (25,15) as instances of one
theorem, and asks whether the K = 16 uniform statement is a closed-form
weighted resultant in `A_t`.

Three corrections, then the replacement.

### 1.1 Where the reading is right

The night's seals stand; I am not re-litigating a promoted identity.
(99,66) is closed at skeleton level (17(tttt); certificates 6264 and 64).
D = 108 δ = 3 is dead at incidence (17(aaaaa); this session replays the
lift to `1` and localized Gröbner `[1]`). `(25,15; 21; 2; k = 2)` is
order-chart empty (17(zzzz); 12 unknowns, 0.02 s). K = 16: `(T)` at
t = 1..7, TOPTAIL, Lemma SQUARE as a *sufficient* criterion, Proposal A
impossible, tail regular at t = 3..6, top-weight cannot prove `(C0)`,
fixed-t Gröbner exhausted at t = 7. One-point systems grow; the second
chart kills.

### 1.2 Where the reading is wrong

**Wrong 1 — (25,15) is not a `u_s > 1` joint-chart client.** The addendum
and 17(zzzz)'s parenthetical ("smallest open `u_s > 1` row after (99,66)")
mis-file the object. Primary sources:

- the order-chart report's title line is the descended 4-tuple
  `(25,15; 21; 2; k = 2)` (K = 5, `u' = 3`, `e = 5`, `q = 3`,
  `δ_2' = −1`, `δ_1' = 7/5`);
- bigrows / emitter / preprocess treat the same 4-tuple as one of seven
  *two-point descended* rows, alongside `(24,16; 17; 2; 5)`,
  `(35,20; 31; 2; 2)`, …;
- `box/moh_skeleton_full.py census(25, Kmin=2)` returns **0 rows**;
  `census(25, Kmin=16)` returns 0; `gcd(25,15) = 5 < 16`, so an original
  pair of those degrees is classically closed as a counterexample by the
  GGV/Heitmann gate.

The running gate `row2515-order-gate-sol56` is prompted to "rebuild the
(25,15) skeleton from `moh_skeleton_full.py` (state M, d, V, `u_s`)".
That object does not exist. The kill, if it stands, is a kill of a
descended monomial-Jacobian 4-tuple — theorem `(T)`, the same program as
K = 16 — by Theorem-1.2 order rows, not by a joint two-point chart.

**Wrong 2 — (99,66), D = 108-split, and (25,15) are not instances of
one theorem.** They are two programs sharing a *mechanism type*
(the second expansion of one polynomial pair overconstrains):

| object | program | killing family | presentation |
|---|---|---|---|
| (99,66) δ = 2 | GLUE (original `u_s = 3`) | Jacobian degree 159 | joint chart |
| (99,66) δ = 5/2 | GLUE (original `u_s = 3`) | G pole, local power 16 | joint chart |
| D = 108 δ = 3 | GLUE (original `u_s = 2`) | common-`h_3` incidence | joint chart, stage 0 |
| D = 108 no-split | `(T)` after Prop 6.3 | OPEN map to `(24,16; 18; 7; k = 4)` | descent + order/compiler |
| `(25,15; 21; 2; k = 2)` | `(T)` | Theorem-1.2 order rows | order chart |
| K = 16 ray | `(T)` | terminal cone `I_{t,+}` | Appendix-II / (V0) |

Grouping the first three as "the joint chart" and asking for a single
necessity dossier that also covers (25,15) is a category error. The
D = 108 split is GLUE; (25,15) is `(T)`.

**Wrong 3 — "the joint chart is empty for every screened skeleton" is a
census slogan, not a theorem, and it is already refuted as a uniform
*schedule*.** D = 108 is not killed by the (99,66) pole/Jacobian stages 4
and 8. It is killed by a branch-specific incidence block that those
stages never reach. A tame two-point automorphism `(ax+y, (a−1)x+y)`
survives every band (17(tttt) control; 17(aaaaa) control). So "a Keller
pair cannot carry a principal-minor split at all" is false as stated
(FALLACY: analogy / cap). The tame pair carries two points at infinity
and is invertible.

**Wrong 4 — Q1's "is `Res_w(R_1,…,R_{t−1})` a closed form in `A_t`?" is
the wrong uniform target.** Lemma SQUARE is an implication
`V(R_1..R_{t−1}) = {0} ⇒ (V0)`, sufficient, square, and useful as a
*criterion*. It is not the object to expand. Weighted Bézout degree
`∏_r (4t+4+2r) / (t−1)!` is 180, 2288, 29120, 372096, 4775232 at
t = 3..7 (this session; exact integer). Fable already proved that
Macaulay / top-weight / initial-form arguments certify finiteness of the
`b_4 = 1` fibre and never emptiness (witness `I = ⟨q_2 − b_4², b_3⟩`).
The running lane `k16-square-resultant-fable5` is prompted to attack
uniform non-vanishing by "Macaulay resultant of the leading forms" —
the obstruction of 17(wwww) §5.3, on arrival. The actual CI statement
sitting in 17(vvvv) is the *t-row tail*
`J_t^{tail} = ⟨T_{t,t}, …, T_{t,2t−1}⟩` (t generators in t variables),
regular at t = 3..6 with length `t/(t+1) · binom(3t+1, t)`
(= 90, 572, 3640, 23256; this session checks the formula through t = 8).
That is a structural, non-Gröbner candidate for `(V0)`. Proposal A
(t+2 rows) is impossible because a regular sequence cannot exceed the
number of variables.

**Wrong 5 — a kill in a relaxation is valid.** 17(aaaa) typed the joint
chart as necessary-but-a-relaxation (missing outer D₁ bands and the
T₂/T₃ bridge). 17(dddd) filled the outer bands. The T₂/T₃ bridge is
still OPEN and was *not* used in either (99,66) unit certificate
(17(uuuu): the bridge did not finish; not needed). Fewer equations ⇒
larger variety ⇒ emptiness of the relaxation implies emptiness of any
tightening. The paper-grade remaining work is necessity of the *rows
that appear in the certificates*, not reconstruction of unused schedule.

### 1.3 What the all-degree program should be now

Two typed programs, one mechanism.

**Mechanism (GLUE).** A Keller pair is a single pair of polynomials.
Every local Puiseux chart of `(F,G)` is an expansion of those same
polynomials. One chart is always consistent (the day's one-point
systems, growing freedom). The second chart — another place at infinity,
a principal-minor packet on the same major leader, or the polynomiality
obstruction to an Appendix-II ansatz — is the kill. This is not a
fourth invariant of the original pair. It is the definition of
polynomiality, made computational by Moh's tree.

**Program A, GLUE on `u_s > 1`.** For a screened skeleton with a
principal-minor split, the fibre product of the two local Theorem-1.2
charts over Jacobian = 1 and incidence/order matching is empty. The
killing *family* is not uniform:

- `u_s = 2` with a quadratic face `p = π² − c`: the two packets share
  the major leader; common-`h_{s−1}` incidence is the first test
  (D = 108 template; stage 0).
- `u_s ≥ 3`: incidence is underdetermined; Jacobian bands and pole
  rows kill ((99,66) template; stages 4 and 8).

Uniformisation is a *compiler of row families* indexed by
`(skeleton, split datum)`, with incidence compiled *before* the
continuation scheduler. It is not the (99,66) schedule copied onto
the next client. A D ≤ 200 census of 166 groups is a leaderboard, not
an all-degree theorem. The all-degree form is a sourced identity that
the incidence block (for `u_s = 2`) or the Jacobian/pole matching
(for `u_s ≥ 3`) is always unit on a noninvertible skeleton — a "minor
Prop 5.6" only after that identity is written. Until then: one next
`u_s = 2` client, not 166 jobs.

**Program B, theorem `(T)` on every descended `u_s = 1` 4-tuple.**
Prop 6.3 sends a `u_s = 1` two-point skeleton to a monomial-Jacobian
pair `(n', m', M_2', V_2'; J = c γ^k)`. Emptiness of that pair is
`(T)`. Two presentations:

- the Theorem-1.2 *order chart* on the 4-tuple (cheap for sporadic
  tuples: (25,15) died in 0.02 s at 12 unknowns after the generic
  chart timed out at 135 unknowns / 180 MB);
- the Appendix-II *terminal cone* `I_{t,+}` (the right uniform object
  on the K = 16 ray, because the number of variables is `t`, independent
  of `n' = 12t+4`).

K = 16 is the only known cofinal screened family (17(w), 17(ll)). A
uniform `(V0)` is a proof of JC2 *along that ray*, not JC2. The
remaining two-point open 4-tuples at D ≤ 200 —
`(35,20; 31; 2; 2)`, `(35,25; 31; 3; 2)`, `(30,24; 25; 4; 3)`,
`(49,14; 46; 4; 1)`, `(50,30; 47; 7; 1)`, `(24,16; 17; 2; 5)`, and
the three `δ_1' = 0` instrument-fail rows — are `(T)` clients for the
order chart, not GLUE clients.

**The sentence.** The all-degree program is: prove `(T)` uniformly on
the K = 16 ray by a structural argument on the t-row tail (not by
expanding `Res_w`), empty every remaining descended two-point 4-tuple
by the order chart, and on `u_s > 1` compile incidence-first GLUE
stratified by `u_s` — a theorem if the incidence (resp. Jacobian)
identity can be sourced, a finite engine if it cannot.

That is the theorem (99,66), D = 108-split, and (25,15) are instances
of only after they are split across A and B. The coordinator's framing
is wrong at the grouping, at Q1's target, and at Q3's uniformisation.

---

## 2. Q1 — `(V0)` is the t-row tail, not a closed-form resultant

Revised Q1 asks whether the weighted resultant `ρ_t = Res_w(R_1,…,R_{t−1})`
of Lemma SQUARE is computable in closed form in `t`, as one element of
`A_t = Q[y]/(H_t)`. Original Q1 offered induction in `t`, Hilbert /
regular-sequence, initial ideals, or a cohomological reading.

### 2.1 Typed claims

**Claim Q1.A (MEASURED).** Weighted Bézout numbers of the square system
are 180, 2288, 29120, 372096, 4775232 at t = 3..7. An expanded `ρ_t` is
not a proof object past t = 4. At t = 3 the two resultants already
generate `(1)` on `b_4 = 1` (`D1 unit=1 dim=-1`). Table, not a closed
form.

**Claim Q1.B (banked, 17(wwww) §5.3).** Top-weight / Macaulay of leading
forms / `R_r|_{b_4=0}` certify finiteness of the `b_4 = 1` fibre, never
emptiness. Witness `I = ⟨q_2 − b_4², b_3⟩`. The running square-resultant
prompt's uniform arm is this obstruction.

**Claim Q1.C (banked, 17(vvvv); formula checked here).** The honest
regular-sequence statement is the t-row tail
`J_t^{tail} = ⟨T_{t,t}, …, T_{t,2t−1}⟩` (t generators, t variables,
weights `3t+1,…,2t+2`). Regular ⇒ hsop ⇒ `dim = 0` ⇒ `(V0)`. Forced
length `L = t/(t+1)·binom(3t+1,t)` equals 90, 572, 3640, 23256 at
t = 3..6 (recomputed; t = 7, 8 candidates 149226, 961400). Proposal A
(t+2 rows) is impossible: a regular sequence cannot exceed the variable
count.

**Claim Q1.D (structural).** `(V0)` for all t ≥ 3 is equivalent to:
the t-row tail is a regular sequence for all t ≥ 3. This is a statement
about the Jacobian matrix of `(T_{t,t}, …, T_{t,2t−1})` with respect to
`(b_4, q_{2,0}, …, q_{t−1,0}, b_3)` having full rank on
`V(J_t^{tail}) \ {0}`, or about the Hilbert series identity holding by
the closed coefficientwise recurrence for `T_{t,k}` (17(zzz)). It does
not require expanding `ρ_t`. Lemma SQUARE remains a sufficient
criterion: if someone produces a unit `ρ_t` by a route that uses the
*lower-weight* terms of the T-recurrence (the `A_t`-units `α_t`, `φ_t`,
`H_t`), that also proves `(V0)`. Leading forms will not.

**Claim Q1.E.** No specialization of pairs `t+1 → t` (T-LADDER refuted;
homotheties of Moh's six are not screened). Do not seat a geometric
cone map, or a cohomological renaming of tail regularity (row 39 stays
low; no 10-line class).

### 2.2 Cheapest decisive tests

| attack | cheapest test | reads |
|---|---|---|
| closed-form `ρ_t` | `ρ_3` already UNIT; `N(ρ_4)`, `N(ρ_5)`; stop if not products of banked units | table, not a proof |
| Macaulay of leading forms | §5.3 witness; do not rerun | obstruction |
| t-row tail regularity | Jacobian at t = 3, 4 (desk); t = 7 modular Hilbert vs 149226, not `std` | regular ⇒ `(V0)` |
| initial-ideal pure powers | `b_4`-exponent `2t+2` at t = 3..7; original-row LMs are all powers of `b_4` (dim t−1) | S-polynomials carry the rest |
| induction in t | one symbolic identity `T_{t+1,k}` from `T_{t,·}` | almost certainly NONE |

### 2.3 Answer

The uniform statement is provable by a structural (non-Gröbner) argument
*if* the t-row tail is a regular sequence for all t ≥ 3. That is the
theorem to prove. Closed-form `ρ_t ∈ A_t` is a computational question
that the degree table already prices out of the all-degree seat. The
coordinator's revised Q1 is the wrong question; original Q1(b), repaired
from t+2 rows to t rows, is the right one.

---

## 3. Q2 — (99,66) is closed at skeleton level; the residual is
necessity of two certificate rows

Original Q2 is obsolete (addendum). What remains is hostile scrutiny of
the two kills' dependency gates, and what the engine's success implies.

### 3.1 Typed claims

**Claim Q2.A.** Both branches of the necessary *relaxation* are empty.
A relaxation being empty is a valid nonexistence proof. The T₂/T₃
bridge (17(hhhh), 17(uuuu)) is a consistency check, not a missing
contradiction. Do not impose it as a proof obligation on (99,66).

**Claim Q2.B (certificate rows, not the schedule).** The δ = 2 unit
row is the Jacobian coefficient `stage4_J_d159_k35 = 6264` (degree 159,
`k = 35`). The δ = 5/2 unit row is the pole coefficient
`stage8_G_local16_coord0 = 64` (local power 16 on G, original Laurent
exponent −110). Paper-grade necessity is:

1. `J(F,G) − 1 = 0` as polynomials ⇒ every homogeneous coefficient
   vanishes, including degree 159 (Moh: Jacobian is the constant 1);
2. a polynomial pair in the classified δ = 5/2 branch cannot retain
   forbidden negative Laurent coefficients of G beyond the prescribed
   principal-minor face (Moh Theorem 1.2 / the face support of Xu §7–8);
3. the Q*-pivots that reduced to these rows divide by nonzero rationals
   only (66 + N pivots; the gates list them);
4. localisations `c ≠ 0` and `J_0 ≠ 0` hold on a Keller pair
   (`c` is the face modulus of `p = π(π² − c)`, excluded from the split
   classification as `c = 0`; `J_0` is the constant 1).

The K2c coordinate change, the outer D₂/D₁ bands, and the unused pole
rows of other local powers are *not* in the unit certificates. They may
be necessary as a chart, but the kill does not consume them. The
running dossier should say so, or it will be attacked on unused rows.

**Claim Q2.C (Moh p.209).** Xu §8 quotes Moh p.209's two possibilities
for the minor-disc distribution of (99,66): a unique π-root of order 2
whose `g_σ(π)` is a power of a linear polynomial, or the 9th power of a
cubic with precisely two roots. The first transforms to the descended
tuple `(27, 18; 21; 8; J = X^4)` (10 coefficients). The second is the
Ω-chart (11 variables) that the campaign killed as branch B
(17(pppp)). The δ = 5/2 face is the case Moh did not write and Xu left
open. The campaign's split classification `{2, 5/2}` with N5
(`den(δ) ≤ u_s`, 17(sss)) is the exhaustive reading of that page plus
Xu's ODE (7.1). Hostile scrutiny of "Moh's p.209 case split is
mechanically complete" (17(pppp)) should keep the first possibility
visible: it is a `(T)` tuple `(27,18;21;8;k=4)`, not a joint-chart
branch, and it is *not* the (25,15) tuple.

**Claim Q2.D (what success implies).** The engine's success at (99,66)
does *not* imply "a Keller pair cannot carry a principal-minor split".
It implies: *this* skeleton, with *this* exhaustive split list, has
empty fibre product of the two local charts over J = 1. The tame
automorphism control is the negative: two points at infinity, J = 1,
nonempty. Implication for the 166-group stratum: the engine is a
compiler, not a theorem; the next client is the next `u_s = 2` row
(incidence-first), not a copy of stages 4 and 8.

**Claim Q2.E (counterexample side).** A surviving small-dimensional
family at (99,66) would have to lie outside the declared chart or
outside the split list. Inside the chart both branches are the unit
ideal. Recomposition `F_x G_y − F_y G_x ≡ 1` is a *control* on a
putative lift, not a search. There is no honest CE target at (99,66).
The first honest positive above Moh's table remains a SURVIVES on a
K = 16 member with t ≥ 8 (typed `REPRESENTATIVE` until it is a map),
or on a remaining 4-tuple. Floor, not attainment.

### 3.2 Cheapest tests

- Necessity of the two certificate rows: the running Opus dossier.
  Stop condition: if a row in a certificate cannot be sourced, type
  GAP with the exact missing statement; do not fill by the rest of the
  schedule.
- Moh's first p.209 possibility `(27,18;21;8;k=4)`: one order-chart
  run (the (25,15) instrument; expected cheap). Not a joint-chart job.
- Do not rerun stage 8.

---

## 4. Q3 — uniformise the compiler by `u_s`, not the (99,66) schedule;
(25,15) is a `(T)` client

Revised Q3: the minimal necessity dossier, and which row families are
needed for the D = 108 and (25,15) kills. Original Q3: is the joint
chart uniformisable, cheapest second client, or a "minor Prop 5.6".

### 4.1 Typed claims

**Claim Q3.A.** There is no single necessity dossier for the three
named kills.

- (99,66): Jacobian coefficient (δ = 2) and G-pole coefficient
  (δ = 5/2), plus the split classification and the gauges.
- D = 108 δ = 3: the common-`h_3` incidence block (seven Q* pivots,
  five normal forms, localisation `c ≠ 0`). Pole and Jacobian
  *schedules were unreached*. The (99,66) dossier does not cover this
  kill; the D = 108 gate's hostile question — "is shared `h_3`
  necessary, or a convention?" — *is* the dossier for this row.
- (25,15): Theorem-1.2 order rows on the descended 4-tuple, plus
  Rabinowitsch on a scalar known nonzero on a Keller pair. No minor
  split, no pole row, no Jacobian band of the joint engine.

**Claim Q3.B (uniformisation).** The joint-chart *method* is
uniformisable as a compiler: (skeleton, split datum) ↦ ordered necessary
families (gauges / Thm 1.2; outer D₂/D₁ with derived weight cut; minor
incidence first; poles; Jacobian; optional T₂/T₃), exact Q* after each,
stop at the first unit. The (99,66) *schedule* is one run (incidence
unused, Jacobian stage 4, pole stage 8). D = 108 is another: incidence
unit at step 3, later steps unreached. An engine that starts at step 4
misses the D = 108 kill (17(aaaaa)).

**Claim Q3.C (`u_s`-stratification, PROVED-HERE as a hypothesis to
test, not a theorem).** The difference in killing family tracks `u_s`:

- `u_s = 2`, quadratic face, two equal packets on one major leader:
  incidence is overdetermined (D = 108: 24 principal G-roots split
  12+12, both final at `δ' = 4`; five residues already unit after
  `c ≠ 0`).
- `u_s = 3`, cubic face: incidence leaves a positive-dimensional
  jet; Jacobian/pole matching kills ((99,66): 869 free coefficients
  at stage 7, then a pole row).

This is the cheapest "minor Prop 5.6" candidate: *for `u_s = 2` with
`p = π² − c`, common-`h_{s−1}` incidence is unit after `c ≠ 0`*. It is
not a theorem (one client). It is not Prop 5.6 (that is a major-tree
zero-chain). FALLACY: do not promote by analogy from one row.

**Claim Q3.D (cheapest second *GLUE* client).** Not (120,80)/(120,100)
by size, and not (25,15). The second GLUE client is the smallest
screened `u_s = 2` row other than D = 108, after the D = 108 gate
returns. If it dies at incidence, Claim Q3.C becomes a two-point
statement. If it needs Jacobian bands, D = 108 was rigid (the 12+12
tree) and incidence is not generic.

**Claim Q3.E ((25,15) row families).** The order chart encodes
Theorem-1.2 inequalities on the approximate-root tower of a *descended*
pair, plus three safe gauges and Lemma 2.1 support. It does not encode
minor splits or original-pair poles. Emptiness is a kill of the
4-tuple *if* every order row is necessary on a pair with that 4-tuple
and the saturation variable is nonzero. That is exactly the running
gate's job — once the object is the 4-tuple, not `census(25)`.

**Claim Q3.F (D = 108 no-split).** Conditional Prop 6.3 descent to
`(24,16; M_2' = 18, V_2' = 7; k = 4)`. The charged compiler diagnostic
is DEAD (26 rows, exact Q, certificate `1 = (−T)(−c) − (T c − 1)`),
but the support/variable map is `OPEN[DESCENT-SUPPORT/VARIABLE-MAP]`.
Do not transfer the diagnostic. The order chart on that 4-tuple is the
licensed `(T)` attack (it is already in the large-row list).

### 4.2 Cheapest tests

- D = 108 incidence necessity: the running GPT-5.5 gate. If the block
  is a convention, weaken and rerun (the gate already says this).
- Next `u_s = 2` incidence: one client, same five-residue shape, desk
  if the `h_{s-1}` cap is as small as D = 108's 7 strict coordinates.
- (25,15) 4-tuple gate: redesign the charged object, then continue.
- Do not launch (120,80) as "the" second client by degree.

---

## 5. Q4 — one sentence, one theorem, a price

**The sentence.** A noninvertible Keller pair cannot realise two
independent polynomial expansions of one pair `(F,G)` with Jacobian 1:
one-point systems grow, the second chart is the unit ideal; the
all-degree content is `(T)` on every descended `u_s = 1` 4-tuple
(cofinal: the K = 16 ray, via tail regularity) and GLUE on every
`u_s > 1` skeleton (compiler stratified by `u_s`; a theorem only after
the incidence or Jacobian identity is sourced).

**The theorem that finishes Moh's line.** Not `(V0)` alone (a family),
not "the joint chart is empty for every screened skeleton" (a census),
and not a slogan about two points at infinity (the tame pair is a
countermodel to the slogan). It is the conjunction of Program A and
Program B in §1.3. Moh's printed work is Appendix II (`(T)` at small
4-tuples) plus a p.209 case split (GLUE at (99,66), incomplete). The
campaign has uniformised both halves and closed his gap. What remains
of *his* line is: `(V0)` for all t, the leftover two-point 4-tuples,
and `u_s > 1` beyond (99,66) and D = 108.

**Price.**

- `(V0)`: one algebraic identity (tail regularity / Jacobian criterion)
  in the closed T-recurrence. Fixed-t Gröbner is exhausted; the identity
  is cheap to *state* and has a desk test at t = 3. If it fails, Lemma
  SQUARE with *lower* terms is the fallback, not leading forms.
- Leftover 4-tuples: the order chart, minutes per row after (25,15).
- `u_s > 1` as a theorem: a sourced incidence identity for `u_s = 2`
  (one more client to see if D = 108 repeats) and a Jacobian/pole
  identity for `u_s ≥ 3`. As an engine: 166 groups is a census and is
  not all-degree; do not seat it as the flagship.
- Paper-grade (99,66): the certificate-row dossier (running). This is
  verification of a promoted result; COORDINATION's resolution-first
  rule makes it DEFAULT-ON only because the verdict is still
  *conditional* on necessity. Once the dossier lands, further
  hardening is DEFAULT-OFF.

---

## 6. Disposition vector (APPROACHES rows, changes only)

The 12:34Z overlay's "theorem `(T)` is the program" stands for Program B.
The 16:36Z overlay's "(99,66) is the top target" is now a *conditional
promoted closure*, not a live search. Row scores from 08-31 rank-four
overlays are not reopened.

| # | change | reason |
|--:|---|---|
| 1 | **raise** | `(T)` now has two presentations: the terminal cone (K = 16, t variables) and the Theorem-1.2 order chart (sporadic 4-tuples, (25,15) died in 0.02 s). Row-1 certificates are kill conditions for a cofinal family *and* for the leftover two-point list. Not a next-pair farm. |
| 2 | **retype, do not raise as sieve** | The Moh tree is done as a screen. The leftover of row 2 at coefficient level is GLUE: two-point Theorem-1.2 matching. That is Program A, not "exclude td = 6, 7, 8". |
| 5 | unchanged retype | Prop 6.3 is not Jung–van der Kulk. The D = 108 no-split map to `(24,16)` stays `OPEN[DESCENT-SUPPORT/VARIABLE-MAP]`; do not shear. |
| 6 | **raise** | Appendix II *and* the order chart *are* AM on the descended one-place curve. (25,15) is this row, not MINOR-DICHOTOMY. AM-on-fibres stays wrong. |
| 7 | unchanged | A(F) is still the compactification problem. GLUE is a computational form of "two places at infinity of one pair", not a construction of A(F). |
| 16 / 45 | keep lower as ceiling | PS-GROWTH is shape, not a D-bound. |
| 25 | keep lower as a finish | Hurwitz is tree-determined (17(mmm)); (99,66) `g_c = 7` on both branches, no kill. |
| 29 | keep, not first | `D_g f = 1` is the node ODE; Lemma 2.1 is the monomial-Jacobian specialisation. Downstream of `(T)`. |
| 32 | mild raise as instrument | Off-diagonal collision / injectivity is still JC2. The joint-chart Q* reducer is a *computational* cousin (exact rational pivots, unit certificates). Do not advertise it as the injectivity theorem. |
| 33 | unchanged COSTUME | Generating functions were already refuted as an all-t target (packet). |
| 36 | **raise, retarget** | CE search: a SURVIVES on K = 16 at t ≥ 8, or on a remaining 4-tuple. Not (99,66), not a numerical interpolant of an undecorated D = 108 row, not original `(25,15)` (empty census, gcd 5). Typed `REPRESENTATIVE` until algebraized. |
| 43 | keep mild raise | Ritt shape of a Lemma-2.1 polynomial is what `(T)` attacks. |

Unchanged: 3, 4, 8–15, 17–24, 26–28, 30–31, 34–35, 37–42, 44, 46. Row 18 stays closed as CE hunt (Shaska). Row 44 stays REFUTED-AS-PROOF. Q1–Q4 fronts: stop closed-form `ρ_t` and Macaulay as proof programs; raise tail regularity; stop (99,66) as a search (dossier continues); stop copying the (99,66) schedule; raise incidence-first GLUE; retype (25,15) to `(T)`; replace the Q4 triad by §1.3; lower the 166-group census; raise leftover 4-tuples as order-chart clients.

---

## 7. Reranked bottlenecks

**Proof, in order.**

1. Tail regularity of `J_t^{tail}` for all t ≥ 3 — `(V0)`, hence `(T)`
   on the only known cofinal screened family. Bounded: one family, one
   identity, desk test at t = 3. This is JC2 *along the ray*, not JC2.
2. Order-chart emptiness of the leftover descended 4-tuples at D ≤ 200
   (six large + three `δ_1' = 0` instrument-fail). Bounded: nine
   problems; (25,15) is the method-client.
3. Necessity of the two (99,66) certificate rows (running dossier) —
   converts a conditional promotion into a paper-grade theorem in Moh's
   terms. Bounded: two rows plus gauges plus the split list.
4. D = 108 δ = 3 incidence necessity (running gate). If CONFIRMED,
   Program A has a second client and Claim Q3.C becomes testable.
5. OPEN[DESCENT-SUPPORT/VARIABLE-MAP] for D = 108 no-split — do not
   consume the compiler diagnostic. The order chart on `(24,16; 17; 2; 5)`
   bypasses the map.
6. One next `u_s = 2` GLUE client (incidence-first). Not 166 groups.

**Disproof, in order.**

1. SURVIVES of `I_{t,+}` at some t ≥ 8, or of the t-row tail (a
   non-regular sequence with positive dimension). Typed
   `REPRESENTATIVE` until it is a pair in `k[x,y]`.
2. SURVIVES of a leftover 4-tuple in the order chart.
3. A GLUE client whose incidence *and* Jacobian/pole matching stay
   nonunit after the sourced rows — a candidate for a genuine
   `u_s > 1` pair. None exists at (99,66) or (so far) D = 108-split.

PIN-NOT-CEILING, `C_FULL_TREE`, and Xu's principal floor stay demoted
as proof programs: they generate the target list. The K = 16 ray is
why the list is cofinally nonempty.

---

## 8. New avenue and new connection

**New avenue: `u_s`-stratified GLUE.** Incidence-first for `u_s = 2`
quadratic faces; Jacobian/pole-first for `u_s ≥ 3`. Not in the packet
(the packet uniformises by copying (99,66)). Discriminator: one more
`u_s = 2` client. If it dies at incidence, the avenue is a theorem-
candidate; if not, D = 108 was rigid and the compiler stays a scheduler
with a branch-specific first family.

**New connection: the order chart *is* `(T)` for sporadic 4-tuples;
the terminal cone *is* `(T)` for the K = 16 spine.** (25,15; 21; 2;
k = 2) is the same theorem as K = 16 t = 1..7, in a different
presentation. The generic two-point chart timed out at 135 unknowns /
180 MB; the order chart killed it at 12 unknowns in 0.02 s. K = 16
needs the cone because `n' = 12t+4` grows while the cone has t
variables. Sporadic tuples should never see the generic emitter again
(17(oooo) already said "need ORDER charts, not more compute"; this
round names the *theorem* those charts prove).

A near-miss, recorded rather than promoted: Moh p.209's first (99,66)
possibility is the 4-tuple `(27,18;21;8;k=4)`, which looks like (25,15)
and is *not* it. Do not identify them.

---

## 9. Strongest proof attack, strongest counterexample attack

**Proof.** Prove that `(T_{t,t}, …, T_{t,2t−1})` is a weighted regular
sequence in `A_t[b_4, q_{2,0}, …, q_{t−1,0}, b_3]` for every t ≥ 3.
Mechanism: TOPTAIL gives t quadratics in `b_3` on `b_4 = 1`; the closed
recurrence gives the coefficients as rational functions of `(t,d)` at
each fixed tail index; the Jacobian criterion (or the Hilbert identity
against `∏_{d=2t+2}^{3t+1}(1-s^d)/D_t(s)`) is the regularity test.
Fail-closed gates: t = 3, 4 already regular (banked); Moh `(16,12)`
empty (t = 1); the t = 2 fibre `y = 1/5` is the banked negative
control where the *cone* is the `b_3`-axis and `(V0)` fails — the tail
statement is for t ≥ 3, and `(C0)` still holds on that fibre
(`b_4^7 ∈ I_{2,+}`). Do not prove `(V0)` by enumerating t, and do not
prove it by Macaulay of leading forms.

**Counterexample.** A non-regular t-row tail with `dim J_t^{tail} > 0`
at some t ≥ 8, or a SURVIVES of the order chart on a leftover 4-tuple.
A numerical lift is `REPRESENTATIVE`. Do not advertise emptiness of
original `(25,15)` (there are no such skeletons) or a joint-chart
SURVIVES at (99,66) (both branches are `[1]`).

---

## 10. Software acceleration / decisive experiment

**Experiment.** Jacobian criterion for `J_3^{tail}` in the charged
terminal ring, then the same at t = 4.

- Instrument: `box/k16toptail-20260903/singular_terminal_driver.py`
  (charged `emit`, already compared to frozen records);
  `box/k16hilb-20260903/` for the Hilbert identity at t = 3, 4
  (already MATCH at those t — the new work is the Jacobian, i.e. a
  reason the identity persists). Desk, `< 5 min`, `≪ 4 GB`.
- Controls: t = 3 length 90 and t = 4 length 572 must reproduce;
  the §5.3 witness `⟨q_2 − b_4², b_3⟩` must *fail* the Jacobian test
  (it is not a regular sequence of length 3 in 3 variables — only two
  generators); t = 2, `y = 1/5`, is outside the t ≥ 3 statement.
- Outcomes: Jacobian unit on the punctured cone at t = 3, 4 → method-
  client for symbolic-t (Card I). Jacobian vanishing locus of positive
  dimension → tail regularity fails, fallback to Lemma SQUARE with
  lower terms, stop the CI reading. Timeout → Hilbert-driven, not
  `std`.
- Do not ship a Macaulay matrix of leading forms as this experiment.

---

## 11. Campaign-systems check

**`UPGRADE` — typed-object schema at packet freeze; do not charge
`moh_skeleton_full.py` for a descended 4-tuple.**

Rotate: claim/review propagation and instrument fidelity. Independent
of the mathematics of `(V0)`.

Evidence, this round: (i) the addendum and 17(zzzz) mis-file
`(25,15; 21; 2; k = 2)` as "`u_s > 1`"; (ii) the running gate prompt
asks to rebuild "(25,15) skeleton from `moh_skeleton_full.py` (M, d, V,
`u_s`)", an object of which the enumerator returns 0 rows at `Kmin = 2`
and `Kmin = 16`; (iii) `gcd(25,15) = 5 < 16` would have been
`REFUSE_CLASSICALLY_CLOSED` as an original pair. A one-line schema
check (`object_class` + `gcd` vs GGV cutoff for original pairs +
4-tuple parser for descended rows) would have caught (i)–(iii) before
launch.

Smallest test: parse the four running prompts of this round; flag any
prompt whose charged object is a pair of degrees `(n,m)` with
`gcd < 16` and no 4-tuple. Expected hit: `row2515-order-gate-sol56`.
Implementation: a 30-line preflight next to `ops/frontier_gate.py`,
no change to live lanes except a coordinator note.

Secondary: the 9-hour coordinator pause on credits delayed a
critical-trigger round by 8.5 h. A wake that launches a *already
frozen* packet without the coordinator seat is a machinery item, not
this round's mathematical upgrade. Record, do not build now.

No `NO_CHANGE`: the (25,15) mis-file is live in a running gate.

---

## 12. Idea cards (three)

### Card I — tail regularity of `J_t^{tail}` (the uniform `(V0)`)

**Dependencies.** Theorem TOPTAIL (17(wwww)); closed T-recurrence
(17(zzz)); Hilbert equality at t = 3..6 (17(vvvv)); properness lemma
(17(mmmm)); Lemma CHAIN (17(xxxx)); `A_t = Q[y]/(H_t)` with
`H_t = 12 q² y² − 12 q (t+1) y + (t+1)(3t+2)`, `q = 2t+1`. Does *not*
depend on Lemma SQUARE, on `ρ_t`, or on the joint chart.

**Cheapest discriminator.** Jacobian matrix of `(T_{3,3}, T_{3,4}, T_{3,5})`
w.r.t. `(b_4, q_{2,0}, b_3)` over `A_3`; rank on `V \ {0}`. Then t = 4.
Then t = 7 modular Hilbert vs length 149226 (not `std`).

**Outcomes.** Full rank at t = 3, 4 → lift the criterion to symbolic t
via the truncated spine (TOPTAIL (3.2): each fixed tail index `r` is a
rational function of `(t,d)`). Vanishing locus of positive dimension →
the CI reading is false; stop Card I; fallback Card-I-alt = Lemma
SQUARE with lower-weight terms, *not* leading forms. Hilbert mismatch
at t = 7 → same stop.

**Stop.** Two non-informative Jacobian computations, or a mismatch
with the banked lengths 90 / 572, or any inversion of a zerodivisor of
`A_t` (product-algebra reading of CHAIN: do not invert zerodivisors).

**Information gain.** Success: `(T)` on the ray is a regular-sequence
theorem. Failure: `(V0)` stays OPEN; `ρ_t` is not a proof past t = 5.

### Card II — incidence-first GLUE for `u_s = 2`

**Dependencies.** D = 108 δ = 3 death (17(aaaaa), PROVISIONAL, gate
running); minor classification (17(ssss)); FALLACY flag/place/series
(the physical minor series is declared separately from the major
weight cut). Does *not* depend on (99,66) stages 4 and 8, and does
*not* consume OPEN[DESCENT-SUPPORT/VARIABLE-MAP].

**Cheapest discriminator.** After the D = 108 gate: one smallest
screened `u_s = 2` row other than `(108,72; M = (−72,81,106); V = (7,7))`,
compile common-`h_{s−1}` incidence only, localize at the face modulus,
ask dim. Desk if the strict `h_{s−1}` coordinates are O(10) as at
D = 108 (7 coordinates, 2.03 s, 56 MB).

**Outcomes.** Unit at incidence → Claim Q3.C has two points; write the
identity (two packets, one major leader, `p = π² − c`, `c ≠ 0`).
Nonunit → D = 108 was rigid (12+12, both final at `δ' = 4`); keep the
compiler, do not promote a minor-Prop-5.6. Convention rather than
necessity (gate REFUTED / CONDITIONAL) → weaken the block, rerun D = 108,
and do not launch a second client on the strong block.

**Stop.** Gate REFUTED; or the second client exceeds 10 min / 4 GB at
incidence (then it is not desk, and not this card).

**Information gain.** Generic `u_s = 2` kill vs rigid tree. The only
all-degree candidate on the `u_s > 1` side; a 166-group census is not.

### Card III — order-chart sweep of leftover 4-tuples

**Dependencies.** General order chart CONFIRMED as instrument
(17(zzzz)); K16 count replay PASS; (25,15) SATURATED-EMPTY
PROVISIONAL (gate running, object to be redesigned). List (bigrows
prompt, D ≤ 200 two-point residue): `(35,20; 31; 2; 2)`,
`(35,25; 31; 3; 2)`, `(30,24; 25; 4; 3)`, `(49,14; 46; 4; 1)`,
`(50,30; 47; 7; 1)`, `(24,16; 17; 2; 5)`, plus three `δ_1' = 0`
rows typed INSTRUMENT-FAIL (not this card).

**Cheapest discriminator.** One row, the smallest unknown count after
(25,15). Expected: order chart O(20) unknowns, seconds to a minute,
wrapper controls as in the (25,15) log.

**Outcomes.** SATURATED-EMPTY over Q with controls → that 4-tuple is
dead as `(T)`, same theorem as K = 16 / (25,15). COUNTING-BOUND →
stop the sweep, the instrument does not generalise past (25,15)'s
`u' = 3` / `δ_1' = 7/5` shape. SURVIVES → first honest positive
4-tuple above Moh's table; typed `REPRESENTATIVE`. `δ_1' = 0` rows
stay instrument-fail until a licensed shear exists (17(zzzz):
`α_1` not scalar).

**Stop.** First COUNTING-BOUND, or a row whose order chart inverts a
parameter-dependent pivot, or any identification of a 4-tuple with an
original skeleton of the same numeric pair.

**Information gain.** Prices the two-point `u_s = 1` residue at D ≤ 200
without the 135–276-unknown emitter. Orthogonal to Card I.

---

## 13. Continue / redesign / stop

Receipts only. No in-progress `.md` opened.

### Running now (no `final_status`; start 08:25Z)

| tag | rec | why |
|---|---|---|
| `g108-delta3-kill-gate-gpt55` | **continue** | Receipt: 17 charged inputs, prompt is the hostile incidence-necessity question. That *is* Q3 for D = 108. Do not divert. |
| `g9966-chart-necessity-opus5` | **continue, with a scope note** | Receipt: 11 charged inputs, dossier prompt. Continue. At harvest, accept a dossier that sources the two *certificate* rows and types unused schedule as unused; reject a dossier that treats every scheduled row as load-bearing. Relaxation-empty is valid. |
| `k16-square-resultant-fable5` | **redesign the uniform attack; continue the table** | Receipt: 15 charged inputs. Prompt deliverable (1) (`ρ_t` / `N(ρ_t)` at t = 3, 4, 5) is a cheap table — continue. Deliverable (3) ("Macaulay of leading forms; boundary resultant on `b_4 = 0`") is the §5.3 obstruction — redesign toward Card I (tail Jacobian) or stop the uniform arm. Do not kill the lane: Fable is the TOPTAIL producer. |
| `row2515-order-gate-sol56` | **redesign the object** | Receipt: 12 charged inputs. Prompt charges `moh_skeleton_full.py` for original (25,15). Enumerator: 0 rows. Charge `(25,15; 21; 2; k = 2)` as a descended 4-tuple, the object the order chart actually emptied. Then continue the hostile necessity/saturation audit. |

### Packet §3 tags (all `final_status=DONE`) — **stop / harvest**

`k16-subchart-q-opus5` DONE 22:19Z (CHAIN → 17(xxxx)); `k16-t6-sol56`
DONE 23:13Z (17(rrrr)); `k16-t11-cone-gpt55` DONE after divert 23:52Z
(17(yyyy), Gröbner exhausted); `preprocess-native-gpt55` DONE 22:44Z
(17(oooo)); `g9966-branchB-kill-gate-gpt55` DONE 22:46Z (17(pppp));
`g9966-delta52-stage8-sol56` DONE 23:06Z (17(qqqq) then 17(tttt)).

### Single first lane

**`k16-tail-regular-jacobian`** (Card I). Owner: a different model from
the Hilbert producer (Sol) and the TOPTAIL producer (Fable) — Grok or
GPT-5.5. Inputs: the charged toptail driver, the t = 3, 4 exact row
files, 17(vvvv) Hilbert identities. Stop at 90 min or at the first
Jacobian vanishing locus of positive dimension. This is the all-degree
seat. Do not make it wait on the square-resultant table.

Second, not simultaneous unless seats are free: redesign of
`row2515-order-gate` (object class), then Card III's next 4-tuple.
Card II waits on the D = 108 gate.

---

## OPENS RAISED

**OPEN[TAIL-REGULAR].** Decide whether `J_t^{tail} = ⟨T_{t,t},…,T_{t,2t−1}⟩`
is a regular sequence in the t-variable cone ring for every t ≥ 3.
Quantity: the Hilbert length equals `t/(t+1)·binom(3t+1,t)` (90, 572,
3640, 23256 at t = 3..6; candidates 149226, 961400 at t = 7, 8), and
the Jacobian has rank t on `V \ {0}`. Cheapest test: Jacobian at t = 3
in `box/k16toptail-20260903/` (desk, < 1 min), then t = 4; t = 7
modular Hilbert vs 149226 (not `std`, which timed out at 1800 s).

**OPEN[US2-INCIDENCE].** Decide whether every screened `u_s = 2`
principal-minor split with quadratic face `p = π² − c` dies at
common-`h_{s−1}` incidence after `c ≠ 0`. Quantity: the `u_s = 2`
groups among the 166, starting at one row. Cheapest test: one smallest
`u_s = 2` client other than D = 108, incidence block only, after the
D = 108 gate (expected O(10) coordinates if the D = 108 cap of 7
repeats; 2 s / 56 MB there).

**OPEN[2515-OBJECT].** Decide the parent skeleton of the descended
tuple `(25,15; 21; 2; k = 2)` and whether any running prompt is
charging original degrees (25, 15). Quantity: one 4-tuple, one parent
`(n,m,d_s)`, zero original census rows at n = 25. Cheapest test: this
session already has `census(25) = 0`; the parent is a descent lookup
in the two-point emitter list / `descend_once` log (minutes, no CAS).

**OPEN[DESCENT-SUPPORT/VARIABLE-MAP]** (carried, not new). The Prop 6.3
map from the D = 108 no-split descended pair to the ordinary
`(24,16; 17; 2; 5)` chart. Quantity: one ring map, generator order,
coefficient field, image checks. Cheapest test: do not fill by analogy;
run the order chart on the 4-tuple instead (Card III). Bypass, do not
close.

Not raised: cofinal emptiness of the screen; a closed-form `ρ_t`;
"Keller pairs cannot split"; a 166-group census; a fourth invariant;
Macaulay of leading forms as a proof route; original `(25,15)` as a
CE (empty, classically closed).

---

## 14. Desk CAS

Stdlib + SymPy, one core, no AWS, no `sat()`.

- D = 108 incidence lift: the displayed identity expands to `1` over
  `Q`. Lex Gröbner of `{r60, r70, r71, r80, r81, Zc*c−1}` is `[1]`;
  `nf(1) = 0`. Raw Gröbner is `[jet1², jet2, c]` (nonunit; localisation
  at `c ≠ 0` empties it). Matches 17(aaaaa). Wall < 1 s.
- Tail lengths: `t/(t+1)·binom(3t+1,t)` is the integer 90, 572, 3640,
  23256, 149226, 961400 at t = 3..8. Matches 17(vvvv) at t = 3..6.
- Weighted Bézout numbers of Lemma SQUARE: 180, 2288, 29120, 372096,
  4775232 at t = 3..7 (exact, remainder 0).
- `census(25, Kmin=2)` = 0; `census(25, Kmin=16)` = 0;
  `census(108, Kmin=16)` = 206 raw groups (not the screened count).
- t = 3 D1 log: `unit=1 dim=-1 size=1`. (25,15) order-chart log:
  `MAIN_SATURATED_EMPTY`, 12 unknowns, 21 equations, char 0, controls
  pass.
- Moh pp.208–210 (`pdftotext`, PDF 69–71); Xu §8 (δ = 5/2 left open;
  (ii), (iii) proved). No page-image promotion.

No `charge_basis`. No ledger edit.

---

## 15. What I am not claiming

- `(V0)` for all t. Tail regularity is OPEN[TAIL-REGULAR].
- A theorem that every `u_s = 2` split dies at incidence. One client.
- Necessity of the D = 108 incidence block. Gate running.
- Promotion of (25,15) emptiness. PROVISIONAL, and the running gate is
  charging the wrong object.
- That GLUE is JC2. It is Moh's `u_s > 1` line, computational.
- That `(T)` on K = 16 is JC2. It is JC2 along the only known cofinal
  screened family.
- A closed-form `ρ_t`. Degree table says no.
- A new exit price.
- Any identification of flag, place, and cover series; of (25,15) with
  `(27,18;21;8;k=4)`; of `REPRESENTATIVE` with a pair.


## COLLISIONS

`python3 ops/open_collision.py xmodel/ideation-20260904T0000Z-grok46.md`
returned `status: CANDIDATES` (review candidates, never closures). Same-round
`ideation-20260904T0000Z-*` submissions and unsealed running-lane reports
were excluded by the scanner's corpus guard; they were not opened.

- `OPEN[TAIL-REGULAR]`: hits `AUDIT.md:17676` (17(vvvv): tail regular at
  t = 3..6, all-t OPEN), `k16-hilbert-regseq-sol56-20260903.md:265,466`
  (defines `J_t^{tail}`, regular at t = 3..6), the packet addendum, and
  lexical `max12-812-*` "tail/regular" noise (not opened). The banked
  reports *state* the t = 3..6 equality and leave the all-t question
  open; that is this OPEN, not a closure.
- `OPEN[US2-INCIDENCE]`: NONE.
- `OPEN[2515-OBJECT]`: lexical hits on the word "object" in unrelated
  2026-08 reports (not opened). No banked line identifies the parent of
  `(25,15; 21; 2; k = 2)` or notes `census(25) = 0`.
- `OPEN[DESCENT-SUPPORT/VARIABLE-MAP]`: hits `AUDIT.md:17686` (17(aaaaa),
  the producer of this OPEN) and a dim-3-descent line in
  `grok-approaches.md` (wrong object). Carried, not closed.

## Seal

The skeleton contained no seal marker. No canonical ledger edited.
`jc2-lean` not inspected. No other `ideation-20260904T0000Z-*`
submission read. No running-lane `.md` opened. No `charge_basis` (no new
exit price). Desk CAS only, receipts above.

<!-- BODY-END -->
