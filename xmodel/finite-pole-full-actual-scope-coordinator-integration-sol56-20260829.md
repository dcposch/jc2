# Coordinator integration — finite-pole full-actual first separation

Date: 2026-08-29 UTC  
Coordinator: Sol 5.6  
Lifecycle: `PROMOTED_FINITE_S_SCOPE_NO_CURRENT_CONSUMER`

## 0. Evidence and disposition

The already promoted two-pole theorem was independently derived by Sol and
Opus, compared, and reviewed by Grok:

```text
82d2f6c3eb2c3985569da428def3d5c2e125ca5b0aa29ebfc7973b68ee843a7a
  xmodel/m2-two-pole-full-actual-first-separation-theorem-r1-sol56-20260829.md
f7853d39a17fd7329feaec101f1767ef5edddd07a2f0d5f8030a0cb023f95efe
  xmodel/m2-two-pole-full-exit-attachment-primary-opus5-76c-20260829.md
1b3be27da8ba495d80cbf473d844055583a672d136ccea40611dfe9fc0b7023e
  xmodel/m2-two-pole-full-actual-first-separation-hostile-review-grok46-76c-20260829b.md
8cb13514cc5bd78ea31583af59892ea4cfc04413eef641f25620dec2bc34d95b
  xmodel/m2-two-pole-full-actual-first-separation-coordinator-integration-sol56-20260829.md
```

Those proofs explicitly observed that the rooted-prefix attachment argument
works for any finite pole-path union, but the earlier integration deliberately
promoted only the two-pole scope needed by LL-1.  The charged finite-s audit is

```text
ab0eec5f446b3f598f365bd204d0b66d4300e3df74c0327cb2ed44c05ee8fcb3
  xmodel/finite-pole-full-actual-scope-grok46-92e-20260829.md
  body 25484 bytes:
  ab2a44a86fcf974b84bfe1935c866bf3da639065d1fe73533c2b08aebffae877
9c44f23113c400afe95a6833dcd20674fa61c62a7c8be46a4a0d3c0b5c5541f7
  xmodel/finite-pole-full-actual-scope-grok46-92e-20260829.run.v2
```

The run is `DONE` on basis
`92ebe92ad5986a47f01af9ed901260595dfed869`, with stable prompt, adapter,
validator, `FALLACY-v2.md`, and composed model-prompt hashes;
`charge_basis_status=ABSENT` is correct because no numerical price is
asserted.  Grok's verdict is `PASS`.  Its reconstruction checks shared
prefixes, repeated presentations, pole endpoints, arbitrary-arity and nested
merges, arrivals, root-orbit typing, re-entry, and disjointness.

Binding disposition:

```text
PROMOTE FINITE-s CARRIER SCOPE
NO CURRENT s>=3 CONSUMER
NO NUMERICAL REPRICE OR DESCENDANT
```

## 1. Promoted theorem

Fix one fibre and one physical component.  Let

```text
U = union_(j=1..s) I_(P_j)([0,v_j]),       1<=s<infinity,
```

where each endpoint is a pole flag.  Definition 3.3 makes the physical flag
space a common-prefix tree.  For every physical ray `P`,

```text
a(P)=max_(j=1..s) min(v_j,O(P,P_j))
```

is the last height at which that ray lies in `U`.  Thus `U` is rootward
closed, an off-`U` flag has the unique attachment `I_P(a(P))`, and a ray
cannot leave and later re-enter `U`.

Assume the same inherited source package as the reviewed two-pole theorem:
the corrected `d_(f-a)` reading of Statement 3.13, corrected
`F*(epsilon*c)` reading of Statement 3.18, actual realized microchildren,
and the repaired 6.7/6.8 up-versus-arrival typing.  For every actual up
non-chain microchild outside `U`, all distinct same-ray cv flags below it
attach at its base vertex.  Complete sets belonging to different directions
or different attachment vertices are pairwise disjoint.  Shared pole-path
prefixes occur once, and pole-chain/rootward arrivals are excluded before
pricing.

Consequently their disjoint union, together with a separately certified
x-side witness, is a pairwise-distinct subset of the cv flags of that one
fibre and may be inserted once in the reviewed actual-weight Corollary 7.1
inequality.

The carrier dictionary is now promoted for every finite `s`:

```text
FULL_ACTUAL_EXIT = FULL_ACTUAL_FIRST_SEPARATION
  = complete distinct actual carrier + lower floor only.
```

This is not an attainment statement.  It does not create a numerical floor;
a consumer must separately prove its local lower bound and all direction
typing.

## 2. Scope and consumer audit

The theorem is one-fibre, one-component, finite-s, and physical-flag typed.
It does not transport `kappa` across fibres, prove printed `(22)` or
`(22-cl)`, identify flags with places or cover series, or turn a reduced
record into an occurring configuration.  The jump/max reading of `kappa`
remains mandatory when a numerical weight is formed, and H5a remains a gap
of printed Notation 3.5.

The current `s>=3` objects are not consumers:

- generic MP8/MFE is explicitly `REPRESENTATIVE`;
- the `m=3,4` reduced books are pattern/selected-exit records without a
  `FULL_ACTUAL_FIRST_SEPARATION` carrier tag;
- the killed nested U2 label uses a representative selected-exit budget;
- the td12 `m=3` U1 family is a recurrence/source object, not a finite-pole
  full-actual pricing packet; and
- the td11 three-pole skeleton is a composition obstruction, not this
  carrier.

Therefore no `L_safe` reprice, book shrink, occurrence claim, or new launch
is licensed.  Bank the theorem until a source-typed `s>=3` P0/P2 client is
named.  Avenue ranks do not change, and no AWS work follows.

<!-- END-SEALED-BODY::finite-pole-full-actual-scope-coordinator-integration-sol56-20260829 -->

## Seal (outside the sealed body)

- Body byte count: `4976`.
- Body SHA-256:
  `0bd06bb2bf45fcf6a4e0b79f201e018ec4862f3ac706f88182bc537706f7d62a`.
- Frozen input basis:
  `92ebe92ad5986a47f01af9ed901260595dfed869`.
