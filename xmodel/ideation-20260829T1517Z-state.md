# Significant-news whole-portfolio state packet — `20260829T1517Z`

Snapshot UTC: `2026-08-29T15:17Z`  
Clean basis: `40c1ab3448209e3d87173feb947a733f6fe54f7f`  
Objective: prove the plane Jacobian conjecture in characteristic zero or
construct and verify an explicit polynomial counterexample. A necessary
condition, formal-family survivor, selected branch kill, or plausible proof
outline is not victory.

## 0. Custody and authority

All blind researchers receive this identical packet before seeing any other
submission. The complete 46-avenue inventory, current top overlay, and older
overlay index are in the generated non-authoritative slice

```text
72284172888e3c8ed38d9a619ac195c5060bd0b7dd5abf1b728634e0aa4cfdad
  xmodel/roundview-20260829T1517Z-40c1ab34.md
f781077840a8405b4ef100b718bc6eeef17a9d07948aaadda2eb2d51aecf27ab
  APPROACHES.md consumed by that slice
```

Authoritative current files are

```text
3f35e3a8a5d204a8c24d96821077117e6efe263a4e2765da7a527d0c13686877  AUDIT.md
d7e833c890c6fdc11a646c5feb5125665b9c298e7fb93a6a706e1afdc63102bc  COORDINATION.md
d45f1db6117f67e21065c29e0ff65ff7c1f8304f3ecc37fa796c5cd4b93b4e65  PROGRESS.md
805152bf2e68edad1a5d55b9fff7092e7a19f40171406050c9d9b50e994de608
  xmodel/ideation-20260829T1224Z-synthesis.md
```

`ROUNDVIEW/v1` is context, not a queue or authority. Recompute charged hashes
before using them. Historical statements such as “active” or “under review”
are timestamped history; the newest canonical overlay controls.

No new web evidence arrived after the last sweep. The next broad sweep is due
`2026-08-30T03:40Z`, so this round is triggered by significant internal news,
not an external claim.

## 1. Significant-news delta

### 1.1 Complete K00 grade-three incidence

The reviewed rank-five/rank-at-most-one integration is

```text
d453b9563d8f6cc23319d6bc0d8edce8a83f657b9e3a60cb244b00f181459860
  xmodel/k00-rank5-rankle1-coordinator-integration-sol56-20260829.md
```

Together with the reviewed rank-two packet, exact containments give
`rank(A)<=2` on the leading base `V(B)`, rank-exact-two grade-three
compatibility is empty, rank-exact-one compatibility is empty, and the
rank-zero compatible locus is the reduced plane

```text
Pi: (d0_1,...,d5_1)=(2s,t/8,s,t,s,2t).
```

Set-theoretically over an algebraic closure,

```text
V(B,P3_1,...,P3_7) = Pi x A^6_u.
```

All rank-three through rank-five localizers and selected grade-three rank-one
or rank-two charts are stopped. This is not a full-P6, later-grade, source,
arc, Keller-pair, counterexample, order-two, or JC2 theorem.

Opus's grade-four primary

```text
0d2a9861126c8857e04a9170c8586b6b4eabe67d2e65d3b4d3b7e991774b614f
  xmodel/k00-grade4-rank0-plane-primary-opus5-92e-20260829.md
```

is sealed producer evidence, not promoted. Conditional on `Pi`, it reports
the generatorwise identity

```text
phi(Lambda_(4,r)) = Q_r(u-(s^2,st/8,16t^2,0,0,0)).
```

The localized ideal is proper of dimension 13; its reduced locus is prime,
irreducible `A^12 x G_m`, cut out by two coupled `(s,t,u)` equations, while
the scheme is nonreduced of nilpotency index three. `d*_3` and `k10_0`
disappear at grade four and return at grade five. The smallest provisional
successor is `K00-G5-RANK0-PLANE` in both scheme-input and radical-input
variants. Different-model replay is review debt, not a blocker to preparing
that successor.

### 1.2 TD12 B source bridge passes but does not kill

The binding reviewed integration is

```text
c6c1d5fe6df7796cac468fb81db16b3bbce777c305d869b9ad46cbb88a16f339
  xmodel/td12-global-source-bridge-b-coordinator-integration-sol56-20260829.md
```

At a named actual B25 occurrence, greedy deviation subtraction is finite and
lands exactly at `s*=25(i+r)-17`. The landing ODE is soluble iff `8B=9A`,
with its unique on-weight solution. This reproduces the already reviewed T1
condition and supplies no route kill. The chart is functorial from the pair
plus occurrence and Puiseux prefix, not from the pair alone. There is still
no actual occurrence theorem, serialized `PairRef`, positive-order source
values, uniform support/degree cap, transgression to an original endpoint
class, or JC2 consequence.

The provisional correction

```text
edfec0a0d9abb8b24db9406bdc1147348114f198a4a9e92a10486bbe9131a720
  xmodel/td12-b25-resrow-v1-provisional-sol56-20260829.md
```

rederives the tail recurrence and replaces `p^(I-1)` by `p^(I-i)`. At the
first resonance `j=17`, the right side is an exact derivative, so both
proposed residues vanish identically. The first possibly nonvacuous
resonance is `j=42`, beyond B24 and unusable without source values. This
correction still needs different-model review.

The earlier endpoint-Hermite producer and range erratum are

```text
1272b394387d744ae065680c8327ff79d0179fb994c6d03d6d8c27d41d371c61
  xmodel/td12-inhomogeneous-row-first-invariant-sol56-20260829.md
aa2920dcfe29f4abd8fc1b7866d6efb87ff95b082e808a641ecef4855a645adf
  xmodel/td12-inhomogeneous-row-first-invariant-r1-erratum-sol56-20260829.md
```

They provisionally identify an original endpoint Hermite congruence but no
transgression from terminal T1 to that class; do not conflate the two.

### 1.3 TD12 sibling bridge and finite-pole carrier

Opus's S17 source-bridge producer has a failed body seal. Sol's internal
audit

```text
1a6472f8ff8530cc77deee622160e3095fcf12e9c958ba5ac3d27a70fd339ac9
  xmodel/td12-global-source-bridge-s-opus5-internal-audit-sol56-20260829.md
```

finds the mathematics repairable and provisionally derives a generic source
translation `x -> x+c` arranging `ord_x f=ord_x g=0` while preserving the
normalized type and S17 cell. This would make `FLOOR-COORD` a WLOG gauge, not
a fixed-coordinate theorem; it does not transport future frozen values
without serialized maps. Different-model reconstruction and repaired custody
are mandatory before promotion.

The stripped sibling-ratio lane

```text
f545cbc8edf13c4442e86c5ce7da35ad7538b444029080c47facecc2043945fa
  xmodel/twin-order-derive-or-open-sol56-20260829.md
```

proves the known non-torsion of `(4+3i)/5` but finds no source-backed closed
holonomy producing a torsion equation. It is `OPEN`, with no route kill; do
not recycle the same symmetry idea without a new actual transport/return law.

The reviewed finite-pole attachment theorem

```text
bf51df729783b2f5c65a6b9eaf55e37d2dd4c0526a9658e2c470993ab3e6dbdc
  xmodel/finite-pole-full-actual-scope-coordinator-integration-sol56-20260829.md
```

handles every finite pole-path union on one fixed fibre/component via
`a(P)=max_j min(v_j,O(P,P_j))`, but supplies a complete-carrier lower floor,
never attainment. No current `s>=3` typed consumer exists. Bank it unless a
real source-typed client is produced.

## 2. Current bottlenecks and live work

The principal proof bottleneck remains upstream of td12 coefficient
arithmetic:

1. derive a named B25 or S17 occurrence from an arbitrary hypothetical
   minimal counterexample;
2. serialize a route-separated actual `PairRef`/completion and source values;
3. prove a finite support/degree cap or an invariant of the missing values;
4. construct the original-to-terminal tower transgression, or find a genuinely
   nonabsorbed distant inhomogeneous constraint.

The protected counterexample/falsification bottleneck is now sharply
downstream: decide literal grade five on the grade-four survivor, preserving
both its reduced and nonreduced inputs, `k10_0!=0`, and every variable at its
first occurrence. Do not relaunch dead rank charts or treat a finite formal
jet as a polynomial map.

Background review debt to run without blocking new work:

- different-model replay of Opus K00 grade four;
- different-model reconstruction of the repaired S17 bridge despite the bad
  producer seal;
- different-model review of the B25 `j=17` vacuity / `j=42` frontier;
- later, review any grade-five theorem or AWS packet before promotion.

Systems incident: four ad hoc Singular invocations printed their output and
then hung on open stdin/unreaped-child polling. The next bounded machinery
candidate is a repo-owned wait-authoritative process helper: closed stdin or
explicit `quit;`, wall/CPU/RSS caps, PID/PGID/start telemetry, exact-PGID TERM,
bounded grace, exact-PGID KILL, and one reap. Nine bare `kill -0` loops await a
reviewed migration. Propose a smaller or better upgrade only with a measurable
test and no mathematical scheduling penalty.

## 3. Blind submission contract

Read the entire roundview/46-avenue inventory, current gaps, and every delta
above. Think independently about the whole proof and disproof portfolio; do
not optimize only the lane you recently owned. Victory may require a new
avenue or a connection absent from campaign history.

Your report must provide:

1. a compact `unchanged/raise/lower/reopen` disposition over all 46 numbered
   avenues, explaining every change;
2. reranked principal proof and disproof bottlenecks;
3. at least one genuinely new avenue/mechanism and one new connection between
   existing avenues;
4. the strongest proof attack and strongest counterexample/falsification
   attack;
5. one mathematical-software acceleration or decisive experiment;
6. a separate campaign-systems `UPGRADE` card with the smallest measurable
   test, or `NO_CHANGE` with evidence;
7. at most three detailed idea cards, each with dependencies, cheapest
   discriminator, interpretation of every outcome, stop condition, and
   expected information gain;
8. `continue/redesign/stop` recommendations for the major live lanes.

Search history before claiming novelty. Label every surviving card `NEW`,
`KNOWN`, `DUPLICATE`, or `SCOPE-CONFLICT`. Separate a theorem from a proposed
experiment and a lower floor from attainment. Keep B and S routes separate;
the two S sibling evaluations share one parent family but are not independent
generic coefficients.

No web or external messages. Never read, list, stat, grep, build, modify, or
touch `jc2-lean`. Desk-scale exact algebra is allowed only under 60 CPU seconds
and 1 GiB RSS per process. Heavy or uncertain CAS is AWS-only: preregister
finite inputs, outputs, controls, cap, stop rules, and replay rather than
running it locally. Do not edit canonical files, commit, push, or read another
blind report. Write only the assigned report plus scratch under `/tmp`.

No new exit price is expected; absent an actual new price assertion,
`charge_basis` must be absent. End with exact charged-input hashes and a body
seal (body definition, byte count, body SHA-256, frozen basis). Fail closed.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `10551`.
- Body SHA-256: `a2ed4405151dc5acac0441694604484d29e4e6f7fee9181ed758aa09f1035f92`.
- Frozen basis: `40c1ab3448209e3d87173feb947a733f6fe54f7f`.
