# Hostile review: V26R1F exact leading-base rank-&lt;=4 survivor prepass

**Case:** `cases/max12_812_order2_u2_62_k00_grade7_fitting_atlas_v26_20260827/`
**Reviewed report:** `RESULT_V26R1F_EXACT_PREPASS.md`, SHA-256
`86742147085332881b3c44ba041172ac5a032d656df3992ceff5da7d0a5b46dc` (replayed).
**Reviewer:** nonproducer hostile reviewer (Fable 5), 2026-08-27.
**Scope:** narrow artifact review only. No ledger edits, no descendants, no
arc/closure/counterexample/JC2 inference.

## VERDICT: PASS — intrinsic six-variable exact rank-locus theorem only

The pass covers exactly the intrinsic statement below and nothing else. The
interpretation of this locus as the honest `W=0` descendant **remains
`PROVISIONAL_ROLLBACK_TAG_OPUS5_PENDING`** and may not be promoted: its
dependency (V24R6R1) carries only the internal same-family Sol Ultra audit
`f9d2afcb01c1ad1161783d0ff51d9d47d1a6aa5831b508936072f0c9170012b9`
(byte-verified against
`xmodel/max12-812-order2-k00-v24r6r1-leading-base-dw-hostile-review-sol-ultra-20260827.md`);
the independent Opus5 cross-audit and adjudication have not landed.

### Theorem I believe is actually proved

In `R = Q[d0_1,d1_1,d2_1,d3_1,d4_1,d5_1]`, for the frozen `7 x 7` matrix `A`
(compiled grade-seven newest-variable matrix, byte-bound to
`ATLAS_EXACT_POLYNOMIALS.json`) and
`B = (G2_row1,G2_row2,G2_row3,G2_row4,G2_row5,G2_row7,F10)`
(the six nonzero literal grade-two rows plus the quartic `F10`):

1. every one of the 49 `6 x 6` minors of `A` is identically zero (so
   `rank A <= 5` at every point of `A^6`);
2. the `5 x 5` minor list is nonzero — exactly 90 of the 441 literal `5 x 5`
   minors are nonzero as polynomials, with 54 distinct values falling into
   exactly 2 proportionality classes (sizes 23 and 31);
3. `J = B + I_5(A)` — with `I_5(A)` generated by the **full** literal minor
   list — is a proper ideal (`1 ∉ J`); and
4. `dim(R/J) = 3`.

Hence `Spec(R/J)`, the rank-`<=4` locus of `A` inside `V(B)` on the
six-variable leading base, is a nonempty affine scheme of Krull dimension 3,
and remains nonempty over every algebraically closed field extension of `Q`
(Nullstellensatz). **No rational point is asserted or found.**

## Independent reconstruction (what my verdict actually rests on)

All algebra was re-proved from frozen bytes in pure Python over exact `Q`
(`fractions.Fraction`), with local Singular used **only as a certificate
proposer** whose every output was re-verified arithmetically
(`/tmp/v26r1f/verify_main.py`, 36/36 checks, run twice):

- **Minors recomputed from scratch** by memoized Laplace expansion on the
  independently parsed `A`: all 49 `6 x 6` minors are the zero polynomial,
  checked entrywise/coefficientwise (no ideal-to-scalar or matrix-to-scalar
  comparisons anywhere in my engine). `5 x 5` census: 441 total, 351 zero,
  90 nonzero, 54 distinct — matching the held compiler's
  `A_minor_statistics` exactly.
- **Slot-compaction gap closed.** The producer's `J` has only 9 generator
  slots (7 from `B` + 2), because Singular's ideal `+` compacts the 90
  nonzero minors: I verified the 54 distinct minors form exactly 2 classes
  up to nonzero scalar, `J[8]`/`J[9]` being honest recomputed minors, one
  per class. Independently of that mechanism, I reduced **every one of the
  54 distinct nonzero minors and all 7 `B` generators to zero** against a
  Groebner basis I verified myself — so `B + I_5(A)` with the full minor
  list equals the 9-slot ideal. The producer's theorem is about the honest
  full Fitting ideal, not a proper subideal. (This was the single most
  dangerous soundness gap in the design; it is closed.)
- **Both ideal inclusions machine-verified.** Entrywise over `Q`:
  `matrix(J) * T = matrix(Graw)` (frozen 9x9 `T`, frozen 9-generator
  `Graw`), giving `(Graw) ⊆ B + I_5(A)`. Conversely `Graw * T2 = GSTD`
  entrywise plus every `Graw` generator reducing to zero mod `GSTD` gives
  `(Graw) = (GSTD)`; the reductions of `B` and all 54 minors give
  `B + I_5(A) ⊆ (Graw)`. No `isSB` attribute was trusted anywhere.
- **Groebner property proved, not assumed:** all 36 Buchberger S-pairs of
  the 9-element `GSTD` reduce to zero under my own degrevlex division (no
  skipping criteria). `NF(1) = 1` (properness) and `dim = 3` (maximal
  independent set on the leading monomial ideal) were derived only from
  this verified basis.
- **Mutation controls replayed independently:** zeroing `T[1,1]` breaks the
  exact identity `J*T = Graw` (nonzero residual — the mandated "at least
  one exact identity must fail" is carried by this control); adjoining `1`
  flips the properness marker; deleting the picked nonzero `I5` entry
  changes the object as registered. Note honestly: the registered
  `I5`-deletion control is object-level (entry deleted and differs), not an
  algebra-level identity failure; the algebra-level falsification burden is
  carried by the transform-entry deletion, which I replayed.
- **Same-engine cross-host replay:** frozen `exact_prepass_branch.sing` and
  `exact_prepass_proper_replay.sing` run verbatim under local Singular
  4.4.1 (arm64-Darwin — different OS, CPU architecture, and version lane
  from the producing EC2 host) produce **byte-identical stdouts** and empty
  stderrs. The tracked script (write paths redirected to `/tmp`, sole
  disclosed modification) reproduces the serialized standard basis and
  transform **byte-identically** (`c8aa23e4…`, `29619127…`).

## Mandatory checks 1–9

1. **Rehash — all replayed.** Report `86742147…`; R1F result
   `f455c7b2…`; AWS evidence manifest `f012cc1a…`; portable harvest
   manifest `HARVEST_REPLAY_R1F.sha256` = `a9933ef5…` with **all 18 entries
   OK locally**; R1F source freeze `3d04cc18…` (all 4 entries OK); basis
   `c8aa23e4…`; transform `29619127…`; compiler result `f1a6f1fc…` with all
   8 pinned output artifacts OK and `COMPILED_SOURCE.sha256` (`5de20da0…`)
   entries pairwise identical to the pins; release bridge
   `RELEASE_V26R1_W0.json` = `a3564e58…`; review packet
   `REVIEW_PACKET_R1F.sha256` 12/12 OK; scripts/stdouts/stderrs covered by
   the harvest manifest; `run/prepass.stderr` is the GNU-time log
   confirming the reported 2.17 s wall, 527,804 KiB max RSS, zero swaps,
   99% of one CPU, exit 0. **Absolute-path adjudication:** the producing
   evidence manifest uses `/home/ubuntu/jobs/...` paths and cannot be
   replayed here; the additive portable manifest lists the same 17 files
   with pairwise-identical hashes plus the evidence manifest itself, and
   replays fully against repo-relative paths. It changes no evidence bytes
   and **closes local custody**.
2. **Atlas parse — confirmed.** The prepass ring is literally
   `ring R=0,(d0_1,d1_1,d2_1,d3_1,d4_1,d5_1),dp;` (exactly the six leading
   variables; the 33-variable ambient list and the 7 honest newest
   variables are separate atlas fields, unused here). `A` is `7 x 7`; the
   `.sing` `A` and `B` lines are **byte-equal** to the harness construction
   from the frozen JSON, and identical across all three scripts. Both JSON
   serializations (`singular` strings vs `exact_terms`) parse to identical
   polynomials for all 49 `A` entries, `Q1..Q6`, and `F10`. The grade-2
   literal block has rows 1–7 with row 6 identically zero, and
   `Q1..Q6` equal literal rows `(1,2,3,4,5,7)` by parsed-polynomial
   equality — **no silent substitution of the zero row 6 for row 7**
   (`B[6]` equals literal row 7, a nonzero 6-term quadric).
3. **Minors — recomputed; all assertions hold entrywise** (see above).
   Producer `IPICK=1` is consistent: the first `minor()` slot is nonzero
   (locally observed and the polynomial re-verified as an honest minor).
4. **Preflight markers — confirmed from frozen stdout:**
   `PREFLIGHT=PROPER`, `DIM=3`, `NGEN=9`, `IPICK=1`; empty stderr; rc 0;
   no `?` error and no standard-basis warning anywhere in R1F stdouts (the
   harness fails closed on all of these — verified in the frozen
   `run_v26r1_exact_prepass.py`, which is pinned by the R1F source freeze).
5. **Serialized replay — re-proved, both inclusions, no `isSB` trust**
   (see above). The replay script embeds the frozen basis/transform
   **byte-identically** (`ideal Graw=` + file bytes; `matrix T[9][9]=` +
   file bytes).
6. **Controls — replayed;** the transform-entry deletion makes the exact
   identity fail; forced unit fires; `I5` deletion changes the registered
   entry (object-level; see honesty note above).
7. **Errata history — consistent.** R1B pre-algebra failure (no
   `ideal==ideal` in Singular); R1C validator failure (matrix-vs-scalar-0
   semantics) — its markers not consumed; R1D producer evidence only — its
   frozen replay stdout contains `// ** G is no standard basis` twice;
   R1E validator failure (ideal-vs-scalar-0). R1F is the only endpoint
   consumed by the report and review packet. **Custody sharp edge:**
   `aws_box02_r1d_exact_prepass/output/RESULT.json` carries the
   byte-identical PASS status string as R1F with no in-directory demotion
   marker; a naive consumer could mistake it for the endpoint (its basis
   and transform bytes happen to equal R1F's, but its replay is
   warning-tainted). Recommend an additive `SUPERSEDED` marker in the R1D
   directory. Not verdict-blocking.
8. **`W` usage — decided.** The intrinsic `B + I_5(A)` computation nowhere
   uses `W` or its syzygy: the three `.sing` scripts contain only `A`, the
   seven `B` generators, minors, `Graw`, and `T` (byte-audited), and all
   polynomials involve only the six leading variables. `W` enters solely
   through the *release bridge* (`W = A1*Q1 + A3*Q3 + A4*Q4`, r3) that
   justifies calling `V(B)` the whole prior base on the `W=0` descendant —
   and that identification inherits the unadjudicated V24R6R1 dependency.
   **Therefore: intrinsic algebra PASSes on its own; the `W=0`-descendant
   label stays rollback-tagged until the Opus5 V24R6R1 cross-audit is
   adjudicated. Promotion remains forbidden.**
9. **Overreadings — attempted and disposed.** No rational point (none
   computed; properness gives nonemptiness only after algebraic closure).
   No rank-exactly-5 claim (nonzero `5 x 5` minors give generic rank 5 on
   the ambient space only; their behavior on `V(J)` is undecided). No
   full-`P6` compatibility (`B` has 7 generators; the full prior ideal has
   35 — untouched). No rank-atlas coverage, later grades, Lambda-jet,
   source reachability, `Jdet`, arc, closure, counterexample, or JC2
   conclusion — none is computed anywhere in the R1F evidence, and the
   firewall strings in the report, `RESULT.json`, and launch registration
   forbid each. The preflight status string is a preregistered allowed
   outcome (`…LOWER_RANK_LOCUS_SURVIVES…`; the appended `_ROLLBACK_TAG`
   suffix is faithful nomenclature drift, noted, cosmetic).

## Custody defects and notes

- **(Note, action recommended)** R1D PASS-status collision — see check 7.
- **(Adjudicated closed)** Producing evidence manifest uses absolute AWS
  paths; portable additive manifest closes local custody with identical
  per-file hashes.
- **(Note)** The producer Singular version is not recorded in the evidence;
  reproducibility was nevertheless byte-exact under local 4.4.1.
- **(Note)** The registered `I5`-deletion negative control is object-level,
  not algebra-level (transform deletion carries the identity-failure
  burden).

## Execution disclosure

No AWS submission channel exists in this review session. All independent
computation ran locally (macOS arm64, single core, explicit wall caps of
300–600 s per step): Singular probe &lt; 5 s; pure-Python engine ≈ 24 s CPU,
553 MB peak; verbatim replays &lt; 5 s each. This deviates from the
"registered AWS capacity" instruction and is disclosed; all workloads were
desk-replay scale and fully reproducible from frozen bytes plus
`/tmp/v26r1f/verify_main.py`.

## Lifecycle recommendation

**Consume V26R1F as PASS for the intrinsic six-variable exact rank-locus
theorem only**, retaining `PROVISIONAL_ROLLBACK_TAG_OPUS5_PENDING` on the
`W=0`-descendant interpretation; promotion and deeper descendants remain
forbidden until the Opus5 V24R6R1 cross-audit lands and is adjudicated,
at which point this result may be re-labeled without recomputation (the
intrinsic algebra is independent of that outcome). Add an additive
`SUPERSEDED` marker to `aws_box02_r1d_exact_prepass/`.
