# Sealed ideation packet — round 20260903T1200Z

Frozen 2026-09-03T12:00:32Z. Basis 4250a6e47f514c6c76a9bc708fbaa533db657eb8 (campaign tree clean; the excluded jc2-lean
worktree is outside the boundary). Prior full round: 20260903T1015Z
(COMPLETE 5/5 + coordinator; synthesis ideation-20260903T1015Z-synthesis.md).
This packet is identical for every blind submitter. Do not read other
lanes' submissions for this round. Coordinator: Opus 5 (cloud seat).
Round trigger: CRITICAL — a non-echo change to the promoted ledger and the
frontier (AUDIT integration #17 deltas (p)–(s), 11:03Z–11:31Z): Moh's
WHOLE-MAJOR-TREE obligation was recovered from the source, reviewed and
promoted; the campaign's census was a superset by a factor ~10; D = 105
and D = 117 are EMPTY on the screened census; both fixed-N families of the
previous round are DEAD; the previous round's three "non-skeleton" proof
mechanisms were each priced by a flagship lane, with negative results.

## Mission posture (DC, standing)
Resolve JC2 — proof or counterexample; resolution-first; one different-
model hostile review gates promotion; up to 5 Opus seats; AWS to quota;
formalization non-blocking; DC: "be bold, creative, act with urgency". The
round contract REQUIRES genuinely new avenues and cross-connections.

## Where the campaign stands (direct; reviewed unless marked)

1. THE WHOLE-MAJOR-TREE (delta 17(r), reviewed by Grok; Opus second gate
   RUNNING at freeze — a refutation aborts this round). Moh's Prop 5.3 is a universal conditional and the p.200
   Theorem (4) holds for every subdisc: at every major disc D_j with common
   leading polynomial p_j (deg P_j = V_{j+1}d_j/d_{j+1}) and squarefree q_j
   (deg Q_j = V_{j+1}(n − M_j)/d_{j+1}), EVERY factor of multiplicity
   u > d_j/(n − M_j) is another major disc that must extend. The p.201
   Galois action makes an actual factorisation P_j = b + A_j Σ u_ℓ (b ≡ P_j
   mod A_j; nonzero orbits of size exactly A_j; at most one fixed root; the
   q-capacity Q = ε + A_jS is automatic). The finite screen C_FULL_TREE
   (DP over all orbit partitions, child data recomputed per branch with the
   higher V-tuple, Lemma 5.3 top-chart "danger" flag, Prop 5.6 killing a
   still-centred zero leaf at D₁; ODE sharpening (3.7) P − Qu ≠ 0 at every
   p-root from Prop A.3) is DERIVED, fail-closed on Moh's six, uniform in
   D, and reproduces EXACTLY Moh's (75,50) residue. Counts: n ≤ 100: 658 →
   60 → 58 (ODE) [→ 55 passport (EXTERNAL) → 20 with the edgewise recentring
   (OPEN)]; Moh's six + 52 (ODE) excess rows — OPEN[MOH-PROGRAM-ARTIFACT] not
   closed (needs the CDC listing). Campaign space: D ≤ 120: 1,189 → 96
   groups (passport) / 113 (tree); D ≤ 200: 14,016 → 1,261 / 1,516; NO
   survivor at D ∈ {48, 54, 60, 63, 81, 88, 102, 104, 105, 110, 114, 117,
   130, 152, 153, 154, 170, 174, 182, 184, 186, 190, 195}; D = 108 keeps
   18 rows / 11 groups / 8 UNI-alive (passport) — the first screened
   survivor above 100. The uniform kill mechanism at D = 105/117 and on
   both rays: at j = 2, b_min = P mod A₂ exceeds the threshold h =
   d₂/(n − M₂), forcing a major zero sibling that Prop 5.6 kills.
   [A lane is recomposing the screen with the exact orbit knapsack to
   D ≤ 400 and searching for a screened cofinal family.]
2. WHAT THE TREE DOES NOT CONSTRAIN (the residual named by the second
   reader and by Moh p.143): the simultaneous existence of the sibling
   polynomials (p_j, q_j) at every node over one coefficient field with
   matching leading terms, the ODE at every node, and the Galois action —
   OPEN[SIBLING-COEFFICIENTS] (lane running on a (90,60) excess row and a
   D = 108 survivor). Moh's Appendix II is exactly this computation, done
   by hand after Prop 6.3's descent.
3. THE DESCENT PROGRAMME, STATED (delta 17(q), Opus; unreviewed): when
   u_s = d_s − V_s = 1, Prop 6.3/6.4 descend the pair to a MONOMIAL-
   JACOBIAN pair in k[γ,π] of π-degrees (n/d_s, m/d_s) = total degrees,
   J = −(u_s/b)γ^{v_s−u_s−1}, characteristic data {M_i/d_s, d_i/d_s},
   V_i' = V_i; leading form y^{nv_s/d_s}z^{u_s n/d_s}; the descended pair
   is NOT Keller and the tower does not regenerate (s' = s − 1): a FINITE
   REDUCTION of depth ≤ s − 1 ≤ 4, not an induction. THEOREM (T) that would
   make Moh's line a proof: for every k ≥ 0 and every (n', m') = (u_s n/d_s,
   u_s m/d_s) from a screened skeleton, no pair P, Q ∈ k[γ,π], monic in π,
   total degrees (n', m'), J = cγ^k, with the inherited data, exists;
   (T) + descent + OPEN[MINOR-DICHOTOMY] (u_s > 1; the p.209 alternatives)
   = JC2 along Moh's line. (T) is about monomial-Jacobian pairs of bounded
   tower height (s' = 2) and unbounded degree — APPROACHES row 1's world,
   four integers of one-place data (n', m', M₂', V₂'). Gate: the descended
   radii δ' (10 rationals; lane running measuring them on Moh's shapes).
   Appendix II's (15,10) kill was reproduced mechanically (with an erratum
   in Moh's printed γ; SATURATED-EMPTY one step later).
4. WHAT DIED THIS MORNING (reviewed): M₂ > m is NOT a theorem (12
   automorphism witnesses; Moh p.151 disclaims any constraint on M₂);
   every scalar predicate of (n, m, M_*, V_*) proposed as Moh's gate
   (MAJOR-MULT, M₂ > n − d₂, forced-(10), A_{s−1} | (n − m)) is
   NOT-IN-SOURCE; only A₁ ≥ 2 (= (12) xor (13), p.188) is derived (condition
   (16)); the AM semigroup conditions and the Prop 3.1 bounded-digit
   representation are AUTOMATIC on the census. PS-GROWTH / TRACE-CONSTANCY
   (the trace identity family d/dx Tr f^{k+1} = (k+1)J[y^{n−1}](f^k mod
   (g − c))) is TRUE (PS-1' needs no hypothesis on J; RESIDUE-LEAD: the
   leading coefficient of R_k is the bottom star's power sum) and is NOT a
   ceiling route: PS-3 is generically attained, the residue's "Θ(km)"
   premise is refuted by the composition (R_k ≡ 0 for k ≤ 13), and the
   family sees only (m, n, q_max, e) — it is a shape constraint
   (e ≤ N(ψ+1)); surviving value: c_max and all branch growth exponents
   read off the Newton polygon of χ = Res_y(g − c, T − f) with no tree.
   The DESSIN-TOWER expected dimension is max(k − 2, 0) ≥ 0 everywhere:
   Hurwitz combinatorics cannot finish; 19 rigid candidates (expdim 0) in
   D ≤ 120. The global moment/parity-check page: the decorated skeleton is
   NOT determined by the census + star (two lanes); the raw moment-parity
   matrix is MDS (cokernel 0) on the fixed-N family — no raw obstruction;
   the n − m − 1 degree identities are remainder coefficients over Q (no
   Puiseux data needed) but do not stratify by level. (UNI) is not a
   theorem: the bottom-disc orbit size is |O| = ∏ A_j on (10)-levels; the
   exact nested packing gives 587 / 470 alive at D ≤ 120 UNSCREENED; the
   D = 105 trio was three single-orbit packets with N = 9 — and is now dead
   by the tree.
5. INSTRUMENTS: box/mohprog-drivers-20260903/ (the tree screen, three
   implementations), box/branch-orbits-v2-20260903/nested_pack.py,
   box/mohsieve/ (leaderboard + Prop 6.3 descent engine reproducing p.207),
   box/m2descent-drivers-20260903/ (η-expansion / characteristic data of an
   explicit pair; the Appendix-II (15,10) solver), box/psgrowth-drivers-
   20260903/ (χ Newton polygon certificate), box/globalinterp-drivers-
   20260902/ (the moment engine; orbit-custody hole, repair debt).

## THE QUESTIONS OF THIS ROUND

Q1 — THE SCREENED CENSUS AS A PROOF PROGRAM? After C_FULL_TREE(_ODE) the
skeleton space is ~10× thinner and two cofinal families died by one
uniform mechanism. Is the screened space cofinally nonempty? If a screened
family survives for all D, exhibit it (closed form + the algebra) — that
is the counterexample-side target list and the proof must leave the
skeleton; if the screen thins to nothing, what is the theorem (the
mechanism "b_min = P mod A₂ > h at j = 2 ⇒ forced major zero ⇒ Prop 5.6"
is uniform: can it be turned into an all-degree statement, and what
exactly does it need beyond (1)–(13))?
Q2 — THE COEFFICIENT LEVEL. The tree constrains multiplicities and radii;
Moh finished by coefficient computation (Appendix II) after descent. What
is the uniform form of "the sibling polynomials must exist simultaneously"
(OPEN[SIBLING-COEFFICIENTS])? Is it theorem (T)? Is it the tree-decorated
moment engine (the whole tree supplies exactly the decoration the sealed
framework needs — give the dictionary node ↔ datum)? Which of these is
the cheapest exact discriminator on the first D = 108 screened survivor?
Q3 — THE DESCENT. With (T) stated: attack it. Monomial-Jacobian pairs
[P,Q] = cγ^k of bounded tower height and unbounded degree — what is known
(the campaign's own [P,Q] = x^k certificate machinery of row 1; the
literature on Jacobian pairs with monomial Jacobian; Abhyankar's expansion
techniques); is there a uniform obstruction in (n', m', M₂', V₂'); what
does the erratum-corrected Appendix II computation generalise to?
Q4 — FRESH EYES (required): the boundary computes N (O(1)); the tree
computes the admissible multiplicity structure; the coefficients are the
remaining freedom. Name a mechanism outside all three — or argue that the
proof must be the coefficient computation and design its uniform form.
Also answer directly: which SINGLE lane would you launch first with a
frontier seat.

## Current lanes
sibling-coefficients-sol56 (Q2 on a (90,60) row and a D = 108 survivor);
screened-census-grok46 (Q1: tree × orbit knapsack to D ≤ 400 + cofinality
search); descent-radii-grok46 (the δ' gate of (T); reduces the descended
D = 105 problems to Appendix-II size as calibration); a2six-ray-moment-
sol56 (moment engine on the — now dead — A₂ = 6 ray; instrument
validation); whole-tree-review-opus5 (second gate) (running).
Queued: a tree-decorated moment engine; the P202 residue (52 rows) through
the sibling system; box01 restart for any heavy Gröbner run (needs the
fleet key).

## Submission contract (per COORDINATION.md, full-spectrum section)
Deliver ALL of: a disposition vector over the 46 APPROACHES.md rows
(changes only, with reasons) AND over Q1–Q4's candidates and the queued
fronts; reranked proof and disproof bottlenecks; at least one genuinely
NEW avenue or mechanism; at least one new cross-connection; the strongest
proof attack; the strongest counterexample attack; one software
acceleration or decisive experiment; a campaign-systems check (UPGRADE
card with the smallest useful test, or NO_CHANGE with evidence); at most
three detailed idea cards (dependencies, cheapest discriminator,
interpretation of each outcome, stop condition, expected information
gain); continue / redesign / stop for each current and queued lane; the
single first lane. Blind: cite only this packet and the banked reports
(xmodel/, AUDIT.md, APPROACHES.md, notes.md, PROGRESS.md — AUDIT deltas
17(p)–(s) and the Grok whole-tree review are the freshest); do NOT read any
ideation-20260903T1200Z-* submission and do NOT read the in-progress reports of
the running lanes. State the bounded quantity AND the cheapest test of
every OPEN you raise. The Moh PDF is at refs/moh1983_jram340_
configurations_of_roots.pdf (page N = PDF page N−139; read images).
