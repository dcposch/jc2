# Hostile review request: rank-drop slope-three continuation checkpoint

Act as an independent hostile source/algebra referee.  Work read-only and
write exactly
`xmodel/max12-912-order3-nu-q8-w0-rankdrop-slope3-continuation-review-grok-20260825.md`.
Do not run Bash, local CAS, solver, network, or substantive Python.  Read the
producer report and its complete frozen case, including all V3 endpoints,
replay, manifest/freeze, and the pinned compiler as needed.

Check: exact six-row source identity; correct coefficient extraction orders;
why saturation by `z` is necessary and sufficient for the stated chart;
mutual ideal identity with
`(b-1,U2,3Q1-(3c-1)z,3(X2-Z2)+z^2,9W-2z^3)`; selected-load nonvanishing;
the continuation substitution and equality of the actual/expected upper-row
coefficient ideals; the residual `-108`; all custody and V1/V2 negative
controls.  Attack whether setting `x5=t` loses ramified arcs or earlier base
drift and enforce that the theorem excludes only this unramified chart.

Return `CONFIRMED`, `CONFIRMED_WITH_REPAIRS`, or `REFUTED`, give every exact
repair, and end with the verdict word alone.

