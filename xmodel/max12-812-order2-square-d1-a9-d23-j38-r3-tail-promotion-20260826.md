# Promotion: D1 `a=9`, `d=2,3` closed `R` tails

Date: 2026-08-26

Status: **PROMOTED AFTER INDEPENDENT HOSTILE REVIEW CONFIRMED.**

## Promoted statement

After the frozen generic-square and D1 source gates, in characteristic zero
the complete seven literal Faber equations are empty on the two exact
`A,C`, closed-`R` contact tails

```text
ord(A)=9, ord(C)=11, ord(R)>=9;   (d=2)
ord(A)=9, ord(C)=12, ord(R)>=9.   (d=3)
```

The exact producer open is `D(p*k0*J)`.  On a Keller source `J` is a unit,
so the campaign normally names the common open `D(p*k0)`.

For each cell, the independent complete source census through grade 38 has
exactly thirteen polar primitive families and pole ceiling three.  The first
pole-four family occurs only in grade 42 (`d=2`) or 43 (`d=3`).  Every
source/load/moving-`p`/target jet ceiling is mechanically derived; every
maximal licensed source jet enters; and all seven literal rows bridge
coefficientwise to the independent Laurent source modulo `sigma^39`.

The reviewed moving pole-three identity is

```text
Phi7+(p(sigma)/4)*Phi5+(3*p(sigma)^2/32)*Phi3
    +(5*p(sigma)^3/128)*Phi1 = 0 mod sigma^39.
```

Rows 1,3,5 are target-free and the only odd target is `J/4` in row seven at
grade 38.  Hence the same combination of full equations is exactly
`-sigma^38*J/4`; adjoining `iJ*J-1` gives the unit ideal before radicals.
The identity is polynomial in the homogeneous `R` factor `eta`; substituting
`eta=sigma^s`, `s>=0`, covers every `ord(R)>=9` without inverting `eta` or a
leading coefficient of `R`.

## Immutable custody

```text
producer RESULT_A9
65ecc9cb8c4a51fb699e8ff86fdd4d5cb4b7dce744f77a77d2f0c4bc346c01db

producer freeze
3430b677791372e50b3fc0bc3ad2f504f234832c1f0f1f38f6a891dec64affd1

producer evidence
0e953f19aef02184f0dc68e80b0d61deca5c53b5292d7b057744dfd848c20d53

hostile review
f922fab0828ff0470ada62fa22bd84a9383233b27948bca12726f1c3aa7785f5
```

Exact `Q` and two independently compiled good-prime controls passed for each
cell on three AWS hosts, all rc 0 and zero swap.  The hostile reviewer
rehashed all 108 evidence rows and six nested manifests, independently
re-enumerated both censuses and the moving functional, audited the complete
source/target bridge and `eta` tail, and returned `CONFIRMED`.

## Firewall

The neighboring `(a,d)=(8,3)` cell is explicitly excluded: its high-memory
producer has now falsified this grade-38 bridge route and is under separate
diagnosis, with no emptiness or survival verdict.  This promotion also says
nothing about an equality face, another leading face, positive-order load,
`p=0`, `k0=0`, `J=0`, a terminal/global chart, the whole square component,
order two, `(8,12)`, maximum twelve, or JC2.
