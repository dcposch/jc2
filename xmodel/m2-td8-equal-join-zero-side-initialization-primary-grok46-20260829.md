# Primary research — td=8 equal-join `(0,y)` initialization

Lane: Grok 4.6, independent primary mathematics, not a referee of a later
solve. Date: 2026-08-29. Object: the smallest remaining transport datum
isolated by the Opus coefficient-transport review — whether the formal
free initialization `(C_tr, B_tr, s_0, s_2, s_3, s_4, c_0)` can be the
common `(0,y)` treetop of a single degree-ratio `(2:3)` pair, subject to
the literal source initialization, the Jacobian product law, the shared
tower, and both branch attachments.

No access of any kind to `jc2-lean`. No web, AWS, Singular, msolve, Sage,
PARI, or other heavy CAS. Arithmetic is desk `int` / `Fraction`. Canonical
files were not edited. Nothing was committed or pushed. No packet was
written. Vertex-local ODEs and the twin-gauge identity are cited as
already reviewed and are not re-solved.

Opus repairs, used throughout and never reverted: interior `β = 0` is
forced; the twin identity is `(A_1/A_2)^2 = ω^{-15}` in `C^*`, with
`(L_1/L_2)`-exponent `-5` and `ω`-exponent `-15`.

---

## 0. Verdict

**Sought outcome (2): an explicit compatible formal initialization exists,
for every integer `t ≥ 0`, uniformly.**

Sought outcome (1) — an exact source-level incompatibility valid for every
`t ≥ 0` — does not exist at the printed-statement / formal-treetop tier.
Sought outcome (3) is false in the strong form “the 7-tuple is
underdetermined by the printed `(0,y)` data”: given the 7-tuple, the
`(0,y)` top forms, the silent-stretch layers, and the first splitting
layer are uniquely determined up to discrete roots of unity. Extra
pure-shift stages (`k_j = 1`) remain possible and, as in Opus R7, are
inert for every identity used here.

Narrowest maximum consequence. For every integer `t ≥ 0` the reviewed
td=8 equal-join tree admits an explicit assignment of `(0,y)` top
polynomials, tower constants, Jacobian constant, and elementary-step
maps, written in §4, such that:

- the pair is of type `(α, β) = (2, 3)` in the sense of Notation 2.4;
- `(0,y)` is a Proposition 9.3 case-(IV) terminal of the displayed trunk;
- Statement 3.9(i)(ii)(iii), the exact product law (E), Proposition 4.2
  with one global tower, and both merge-branch attachments all hold as
  formal data;
- the Opus triangular transport from the trunk toward the poles is
  untouched and remains solvable.

This is **formal compatibility of treetop data**, not realization by a
polynomial pair `(f, g) ∈ C[x, y]^2`, not a constant-Jacobian identity
beyond the top product law, not exact `λ`, not a degree bound, and not a
JC2 result. No vertex-local ODE and no twin-gauge identity was re-solved.

---

## 1. Custody

Files read for mathematics, and only these:

| file | sha256 (full) | bytes |
|---|---|---|
| `xmodel/m2-td8-equal-join-zero-side-initialization-primary-grok46-20260829-prompt.md` | `fef9de46d1ec1287ebf4e64883394ca851ffb9e799d3b7d588e1daf9e15ab020` | 1795 |
| `xmodel/m2-td8-equal-join-st39-coefficient-transport-primary-hostile-review-opus5-20260829.md` | `e469e94dbf4393fe820345678c2582ca7f7a528695c25d065fb224b4f796cf61` | 26424 |
| `xmodel/m2-td8-first-extra-jet-exact-lambda-primary-hostile-review-fable5-20260829.md` | `ec3557953c6387ce35dcf4efe1df267fb828771119c479eb00c8f1793e0ee370` | 26123 |
| `xmodel/m2-td8-equal-join-st39-coefficient-transport-primary-fable5-20260829.md` | `910d3216ad7476b743eb920e3d68026f05efc8ffe1a409c476fb90ce277f6ad4` | 19670 |
| `xmodel/m2-td8-first-extra-jet-exact-lambda-primary-opus5-20260829.md` | `991e1b350ad2f8b2b82808fdc84dec71154f9ae17250c18953a36ac8f6588508` | 32759 |
| `xmodel/m2-td8-equal-join-route-family-sol56-20260829.md` | `9a778862816aa96d251c33aac7e98a2fb3bfc1b3fc7c83f4463ac862de31815a` | 5623 |
| `refs/sigray_full.pdf` | `9bf9f0320497dd8d5da6d7fe68ec900c1879663e6f11c121853482ca7e1623ae` | — |

Body hashes independently recomputed against each report’s own footer:
Opus transport review `0de875253b50684042f8913f5b29781e58d4a7ff38d3a640831808e9b55705d8`;
Fable exact-separation review `463a8f4860f950445a9ff784b1c1b41729762bbebd530477b2e1b53717d564a7`;
Fable transport primary `3b7851eaaee3f35c23de8d201eb22ad664ab2dba1614d6d4fc04cba562461171`;
Sol D2 `bd35c43baf91c6306d4bb651a09e4886340ab7eb836ffd41d6b1ba6a85ea02d7`.

Printed source, on-page (`refs/sigray_full.pdf`, printed page = pdf page),
re-read as display lines for every load-bearing formula:

- pp. 8–9: Lemma 2.1(i)–(iv), Notation 2.4 (type `(α, β)`);
- pp. 11–18: Def 3.3–3.4, Not 3.2–3.10, St 3.9, St 3.12, St 3.16–3.18,
  Prop 3.1, Prop 3.2;
- pp. 18–23: Prop 4.1 (chart Jacobian `ξ^{-n/κ}`), Prop 4.2, Not 4.1,
  Prop 4.4–4.6 including (11) and (14)/(17);
- p. 28: Prop 6.1, **Theorem 6.1**;
- pp. 29–32: Not 6.1 (`T_a^&` / `T_a^↗`), St 6.1–6.2, Prop 6.3–6.5,
  Cor 6.1 (explicitly excluding `(0, y)`);
- pp. 39–44: Not 8.1, Prop 8.1(i)–(v), St 8.2–8.5, Prop 8.3(iii);
- pp. 48–51: Not 9.1, **St 9.2**, Prop 9.2, **Prop 9.3(IV)(i)–(m)**.

The stacked-fraction hazard was respected: Lemma 2.1, Prop 4.6 (11)–(17),
Prop 8.1(iv), St 9.2, and Prop 9.3(IV)(k) were re-read as displayed
blocks. Promoted ladder used only as dictionary, never as a theorem:
`ladder/SHEET6-H3.md` §3 for the case-IV shape form of `R` (re-derived
from the printed (k) below); `ladder/SHEET6-LROOT.md` LR1 for the
already-reviewed `λ_{(0,y)} = 0` reading. The td=6 coefficient template
was not used as a lemma.

The two charged reviews are starting data, not lemmas of this report:
every `(0,y)` identity below is re-derived from the printed statements
together with the already-reviewed trunk/merge/A-step numbers. The
Fable transport 7-tuple, the exact product law (E), `β = 0`, the
drop-stage law, and `(A_1/A_2)^2 = ω^{-15}` are consumed as reviewed
formal transport, not re-proved.

---

## 2. What is already pinned, and what is charged

Reviewed route, for `t ∈ N` (Opus transport review, confirming Fable
and the Sol D2 producer): two poles of type `(2, 3)`, two identical
`(21, 15)` cells, affine equal-join `ν_G = 4 + 3t`, rigid trunk
`(dp, dq, ν, M, κ̄, X) = (85, 35, 17, 5, 7, 17)`, and a case-IV
terminal from that trunk to `(0, y)`. Full degrees and chain
multiplicities, in `Z[t]`:

```text
i_H = 2,     i_G = 14,     i_tr = 28(4 + 3t) = 112 + 84t,
deg p_tr = 85 i_tr,     D_tr := κ_tr d_tr = 17 i_tr.
```

Characteristic sequence (Proposition 9.2): `P_e → H_e → G → trunk →
(0, y)`. Elementary-step orientation (increasing `π`): `(0, y)` toward
the poles. The displayed tree has no vertex strictly between the trunk
and `(0, y)`: faces at `π = k/17` for `1 ≤ k ≤ 9` are single-rooted,
hence not in `V_{1,a} ∪ V_{2,a}` by Statement 3.16, and case IV(i)
places the first characteristic exponent at the trunk.

Reviewed free initialization at the trunk, in the free multiplicative
group generated by `C_tr, B_tr, s_0, s_2, s_3, s_4, c_0` and roots of
unity (Fable §5, Opus §2.5, triangular-monomial): `C_tr` is the f-side
leading coefficient, `B_tr` is the q-side top (equivalently the free
`s_4` via the stage-4 enslavement), `s_0, s_2, s_3` are the shared
tower constants of stages `0, 2, 3`, and `c_0 := J(f, g)`. Stage 1 is
a possible pure shift (`k_1 = 1`), inert for `μ`, `∏ k_j`, `T_V`,
`M^*`, `i_V`, and the twin exponent (Opus R7).

Charged question: can this 7-tuple be the common `(0, y)` treetop of
one type-`(2, 3)` pair, with the Jacobian product law, one global
tower, and both merge attachments.

---

## 3. Printed `(0, y)` dictionary (rings named)

All items in this section are printed statements. Arrow convention is
Notation 6.1 as printed: `T_a^& = {d < (1 − π) deg p}` (searrow),
`T_a^↗ = {d > (1 − π) deg p}`. Theorem 6.1 is `l_f < k_f`, hence
`(0, y) ∈ T_a^&`, not `T_a^↗`. (The opposite arrow in the
exact-separation review’s phrasing is a chart slip; the numerical
forcing `λ_{(0,y)} = 0` does not use the arrow.)

**3.1. Type and charts, in `N`.** Lemma 2.1 plus Notation 2.4: a
normalized pair of type `(2, 3)` has `k_f / k_g = l_f / l_g = 2/3`
in `Q`, with `k_f, l_f, k_g, l_g ∈ N^*`, `k_f < k_g`, `l_f < l_g`,
`l_f ≤ k_f`, and `k_g / k_f ∉ N^*`. Statement 3.12 and the proof of
Proposition 6.5 give the chart swap, as equalities in `Z`:

```text
k_f = d_{(0,x)} = deg(p_{(0,y)}),     l_f = deg(p_{(0,x)}) = d_{(0,y)}.
```

Theorem 6.1 upgrades `l_f ≤ k_f` to `l_f < k_f`. Statement 9.2, at
`F = (0, y)`, in `Z`: `D_F = d_F`, `ν_F = 1`, `κ_F(1 − π(F)) = 1`.
Combined with `π((0, y)) = 0` this is `κ_{(0,y)} = 1` and
`D_{(0,y)} = l_f`.

**3.2. Case IV terminal, in `Q` and then `N`.** Proposition 9.3:
`F, G ∈ V_a ∩ T_a^&`, `G = F + c`. Case (IV) is `F = (0, y)` and
`F ∉ V_{1,a} ∪ V_{2,a}`. Then, printed:

```text
(i)  ν_G = κ_G,
(j)  (1 − v) κ_G < ν_G,
(k)  d_F = [D_G + (ν_G − (1 − v)κ_G) deg(p_G)] / ν_G  ∈ N,
(l)  d_F < deg(p_G),
(m)  d_F M_G / deg(p_G) ∈ N.
```

Take `G` to be the trunk (the unique `V_a`-neighbour of `(0, y)` on
this route). Trunk numbers `ν_G = 17`, `(1 − v)κ_G = 7`,
`D_G = 17 i_tr`, `deg(p_G) = 85 i_tr`, `M_G = 5`, all in `Z[t]`. Then
(i) forces `κ_tr = 17`; (j) is `7 < 17`; and (k) in `Q(t)` is

```text
d_{(0,y)} = [17 i_tr + (17 − 7) · 85 i_tr] / 17
          = 867 i_tr / 17
          = 51 i_tr,     since 17 · 51 = 867.
```

So `l_f = 51 i_tr = 1428(4 + 3t) ∈ N` for every `t ≥ 0`. (l) is
`51 i_tr < 85 i_tr`. (m) is `51 i_tr · 5 / (85 i_tr) = 3 ∈ N`. The
H3 shape form `R = ν / (ρ + ν − κ̄)` with `ρ = 17/85 = 1/5` recovers
the same ratio `R = 17 / (1/5 + 17 − 7) = 85/51 = 5/3` in `Q`,
independently of `t`. Thus `ψ = ⌈R⌉ − 1 = 1`, and
`ψ l_f < k_f` will hold once `k_f` is pinned in §3.3.

**3.3. Single root and the degree-ratio pair, in `Z`.** Case IV’s own
hypothesis `F ∉ V_{1,a} ∪ V_{2,a}`, with Definition 3.4, means no two
y-side punctures have contact `0`. Proposition 3.1(`**`) at level `0`
then gives a single root of `p_{(0,y)}`. Statement 3.17(i) along
`trunk = (0, y) + c` gives

```text
k_f = deg(p_{(0,y)}) = mult(p_{(0,y)}, c) = deg(p_tr) = 85 i_tr
    = 2380(4 + 3t) ∈ N.
```

Type `(2, 3)` in `Z`:

```text
k_g = (3/2) k_f = 3570(4 + 3t),     l_g = (3/2) l_f = 2142(4 + 3t),
k_f / k_g = l_f / l_g = 2/3,     gcd(k_f, k_g) / (4 + 3t) = 1190,
k_g / k_f = 3/2 ∉ N^*,     l_f < k_f  (1428 < 2380).
```

All four Lemma 2.1 inequalities and Notation 2.4 hold identically in
`t`. The ratio `k_f / l_f = 85/51 = 5/3` equals `R`, so
`ψ l_f < k_f` is the printed hypothesis of Statement 9.4 at `ψ = 1`.

**3.4. The 0-root, in `C[η]`.** Lemma 2.1 gives
`f^+_{(1,1)} = ⊖ x^{k_f} y^{l_f}`. On a generic fibre `f = a`, the
`(1, 1)` monomial forces `y → 0` as `x → ∞`. Combined with the single
root of §3.3, the common first Puiseux coefficient is `c_R = 0`:

```text
p_{(0,y)}(η) = C_R η^{k_f} ∈ C[η],     C_R ∈ C^*.
```

This is Statement 3.16’s form `p = η^l p̃(η^ν)` at `ν = 1`. Statement
3.18: `0` is a root, so `(0, y) * 0` exists (no deck `ε` is required
for a `0`-root). The unique continuation is the trunk.

**3.5. Jacobian product law at `(0, y)`, in `C[η]` and then `C`.**
Proposition 4.1, with `n = κ π((0, y)) = 0`, gives chart Jacobian
`ξ^0 = 1` exactly (the Opus pinning of the printed `⊖` of Prop 4.1’s
proof is unused here: the exponent is literally `0`). Proposition 4.2
applies because `d = l_f > 0`. The sum `l_f + l_g = (5/2) l_f > 1`
puts `J(f^+, g^+) = 0` at `(0, y)`, so `m_{(0,y)} > 0` and stage `0`
is the type-`(2, 3)` enslavement `(k_0, l_0) = (2, 3)`:

```text
p_{g,(0,y)} = B_R^{(g)} η^{k_g},     (B_R^{(g)})^2 = s_0 C_R^3     in C.
```

The exact product law (E) — the reviewed upgrade of Proposition 4.6(11),
with `∏_{j < m} k_j` and `c_0 := J(f, g)`, chart factor `1` — at
`(0, y)` with Opus `β = 0` is an identity in `C[η]`. Write
`P = C_R η^{k_f}` and `Q = B_R η^{s_0}`, `s_0 = k_f(μ_R − 1) + 1`,
`d_h = μ_R l_f + 1` (Proposition 4.2(iv) at `u = 0`). Then

```text
l_f P Q' − d_h P' Q = C_R B_R (l_f − k_f) η^{μ_R k_f}     in C[η].
```

The exponent matches the right-hand side
`(∏_{j < m_R} k_j) c_0 ∏ p_{h_j}^{k_j − 1} = (∏ k_j) c_0 T_R η^{μ_R k_f}`.
Equating coefficients in `C`, and using `l_f − k_f = −34 i_tr`,

```text
C_R B_R (l_f − k_f) = (∏_{j < m_R} k_j) c_0 T_R,          (★_R)
```

equivalently the Prop 8.1(iv) form at `ν = 1`, `Φ = Ψ = η`,
`δ = l_f / k_f = 3/5`, `1 − u = 1`:

```text
(3/5) η · 1 − 1 · η = −(2/5) η,     R_R = −2/5 ∈ Q,
X_R = D_R / i_R = l_f / k_f = 3/5,     λ_R = δ / X = 1,
i_R C_R B_R R_R = (∏_{j < m_R} k_j) c_0 T_R,
```

since `i_R = deg(p)/M^*_R`. Now `M^*_R = gcd(k_f, …, k_f(μ_R − 1) + 1)
= gcd(k_f, 1) = 1`, so `i_R = k_f = 85 i_tr` and
`85 i_tr · C_R B_R · (−2/5) = −34 i_tr C_R B_R`, matching (★_R).
Statement 8.5 applies (`trunk^∘ = (0, y) ∉ V_{2,a}`): `M_R | M_tr`,
i.e. `1 | 5`. Corrected Proposition 8.4 is nonroot-only, so root
`M = 1` is legal.

The two derivations of (★_R) — (E) in `C[η]`, and Prop 8.1(iv) at
`ν = 1` — agree as an identity in `C`. Neither re-solves a reviewed
vertex ODE: the only ODE used at `(0, y)` is the `ν = 1` monomial
identity above, which is not one of the four reviewed families.

**3.6. Tower depth relative to the trunk, in `N`.** Corollary 6.1
excludes `(0, y)`: a single-root face may satisfy Proposition 4.6(17)
without leaving `V_a`. The trunk, however, has reduced `dq = 35 > 1`,
so the reviewed drop-stage law applies at the trunk: `m` strictly
decreases along the feeding edge. Proposition 6.3 (`G = F * c ∈ T_a^&`
implies `G ≼ F`) gives `m_tr ≤ m_R`; the drop at the trunk upgrades
this to `m_R ≥ m_tr + 1`. In Fable’s stage table the trunk q-side is
stage 4, so the minimal depth is `m_tr = 4`, `m_R = 5`. Extra
`k_j = 1` insertions do not change `∏ k_j` or `T_V`.

---

## 4. The compatible formal initialization (sought outcome 2)

**4.1. Gauges.** Work in the free abelian group of monomials with
coefficients in `C^*`, tensored with the discrete deck
`{ω : ω^{ν_G} = −1}` already reviewed. Choose freely

```text
C_R ∈ C^*,     s_0, s_2, s_3, s_4 ∈ C^*,     c_0 ∈ C^*.
```

Set `C_tr := C_R`. Optional inert data: a finite sequence of pure
shifts `(k_j, l_j) = (1, l_j)` with `l_j ∈ N^*`, and extra enslaved
stages above index 4 with fresh `s_j ∈ C^*` if a non-minimal
`m_R > 5` is chosen. Discrete data: the reviewed roots of unity at
the merge and A-steps; none is new here.

**4.2. `(0, y)` top forms, in `C[η]`.**

```text
p_{f,(0,y)}  = C_R η^{k_f}     = C_R η^{85 i_tr},
p_{g,(0,y)}  = B_R^{(g)} η^{k_g},     (B_R^{(g)})^2 = s_0 C_R^3,
p_{h_j,(0,y)} = t_{j,R} η^{k_f l_j / k_j}     (j < m_R),
               t_{j,R}^{k_j} = s_j C_R^{l_j}.
```

The exponents `k_f l_j / k_j` lie in `Z` for the reviewed stages
(`3 i_V / 2 ∈ Z` at every even `i_V`; `i_G · 6/7 = 12`;
`i_tr · l_3 / k_3 = l_3`) and for stage 4 with
`l_4 / k_4 = Q_tr / D_tr = (17 k_tr + 7) / (17 i_tr)` in `Q` reduced
to a coprime pair in `N` as in Fable §4. Under `β = 0` the q-side at
`(0, y)` is the pure monomial `B_R η^{k_f(μ_R − 1) + 1}`. Identity
(★_R) in `C` pins `B_R` from `(C_R, c_0, T_R)` and the finite product
`∏_{j < m_R} k_j`; every factor is nonzero, so `B_R ∈ C^*`.

**4.3. Elementary-step map `(0, y) → trunk`, in `C[η]`.** Take a
suitable `κ = 17` (licensed by case IV(i): `ν_tr = κ_tr = 17`). Then
`n = κ π((0, y)) = 0`, and `(0, y) * 0 = I_P(1/17)`. Ten microsteps
of full multiplicity `k_f` drop `d` by `k_f / 17 = 5 i_tr` each, in
`Q`:

```text
d_{(0,y)} − 10 · (85 i_tr) / 17 = 51 i_tr − 50 i_tr = i_tr = d_tr.
```

The identity is in `Q` and lands in `Z[t]`. Along the nine silent
faces, Statement 3.16 forbids extra roots, and Statement 3.9(ii)
transports the leading coefficient unchanged. Hence the `κ_F`-graded
layers of `f^{(0,y)}` (Fable exact-separation R1 indexing
`P_{k'} := p_{n − 17 k'}` at ambient suitable `κ`, here already
`κ = κ_F = 17`) satisfy, as an assignment in `C[η]`:

```text
P_0 = C_R η^{k_f},     P_1 = ⋯ = P_9 = 0,
P_10 = C_tr Φ_tr^{i_tr} = C_R \bigl((η^{17} − A_F)^3 (η^{17} − (4/3)A_F)^2\bigr)^{i_tr}.
```

The reduced trunk polynomial and the ratio `B = (4/3)A` are the
reviewed Prop 8.1(iv) family, not re-solved. The gauge `A_F^2` is
pinned by the reviewed trunk equation (★_tr) from the 7-tuple; a
square root in `C^*` is a discrete choice. Because `P_10(0) =
C_R ((−A_F)^3 (−(4/3)A_F)^2)^{i_tr} ≠ 0`, the child has no `0`-root,
matching the reviewed trunk (`ε = 0`).

Statement 3.9(ii) on the composite stretch, parent a pure power,
`Φ_tr` monic, gives `C_tr = C_R` in `C`. Enslaved tops are likewise
invariant: `L_c = lc(η) = 1`, so `t_{j,tr} = t_{j,R}` in `C`. The
drop feed at the trunk is `B_tr = t_{4,R}`, and the stage-4
enslavement at `(0, y)` is `B_tr^{k_4} = s_4 C_tr^{l_4}` in `C`. This
is the reviewed identification “`B_tr` via free `s_4`”: the 7-tuple
has six `C^*` degrees of freedom, not seven.

**4.4. Shared tower and both attachments.** Proposition 4.2 produces
one global sequence `(k_j, l_j, s_j)` on `C^2`; Proposition 4.4 and
Statement 8.3(i) copy it to every face with `j ≤ m_F`. Both merge
branches therefore inherit the same `(s_0, s_2, s_3, s_4, c_0)` from
the single `(0, y)`. The two attachments are the two multiplicity-3
orbits of the reviewed merge pattern; each is fed from the trunk,
which is fed from the single continuation `(0, y) * 0`. Swapping the
two A-cells is the reviewed deck action on `ω`, subject to
`(A_1/A_2)^2 = ω^{-15}` in `C^*` with `(L_1/L_2)`-exponent `-5`. No
new cycle is created: `(0, y)` lies strictly above the merge, the
tree remains a tree, and (★_R) consumes the fresh q-side gauge `B_R`
of the extra stage at `(0, y)` rather than any of the 7-tuple.

**4.5. Triangularity, in the free multiplicative group.** Top-down
from `(0, y)`:

```text
(C_R, s_0, s_2, s_3, s_4, c_0)
    --St 3.9(ii), L_c = 1-->  (C_tr, t_{j,tr}, B_tr = t_4)
    --(★_tr), reviewed-->     A_F^2
    --reviewed transport-->   the whole displayed tree.
```

Independently, (★_R) pins `B_R` and does not feed back. Every pinned
quantity is a monomial in nonzero factors, for every `t ≥ 0`. Extra
`k_j = 1` stages do not appear in `T_V` or `∏ k_j`. This is the
reviewed triangular-monomial structure with one additional vertex
equation and one additional gauge, both at `(0, y)`.

---

## 5. Attacks that do not give sought outcome (1)

Every candidate below was checked against the printed statements and
fails to produce an identity in any of `Z[t]`, `Q(t)`, `C[η]`, or
`C` that is false for some (let alone every) `t ≥ 0`.

- **Type arithmetic.** The four Lemma 2.1 inequalities, Notation 2.4,
  case IV(i)–(m), and `ψ l_f < k_f` hold as identities in `Z[t]` /
  `Q` (§3.2–3.3). No exceptional residue.
- **`M = 1` at the root.** Forced by `gcd(k_f, k_f(μ − 1) + 1) = 1`,
  legal at `(0, y)` (corrected Prop 8.4 is nonroot-only), and
  compatible with Statement 8.5 (`1 | 5`). The Prop 8.4 Bezout
  endgame that would demand `l_f / k_f ∈ N` requires `M_H = 1` at
  the trunk; the trunk has `M = 5`.
- **Chart Jacobian versus `c_0`.** At `(0, y)` the printed Prop 4.1
  factor is `ξ^0 = 1`. Identity (★_R) is consistent with (E) in
  `C[η]` (§3.5). The sign `l_f − k_f < 0` is absorbed by `B_R ∈ C^*`.
- **0-root versus trunk `ε = 0`.** The transported top `C_R η^{k_f}`
  vanishes at `0`; `P_10(0) ≠ 0` for the reviewed trunk family, so
  the child has no `0`-root. Statement 3.18’s `F * 0` exists.
- **Silent-stretch vertices.** Nine faces at `k/17`, `1 ≤ k ≤ 9`,
  remain single-rooted under `P_1 = ⋯ = P_9 = 0`, hence are not in
  `V_a \ {(0, y)}` (St 3.16). Case IV(i) forbids an earlier
  characteristic exponent.
- **Twin law as a treetop constraint.** The identity
  `(A_1/A_2)^2 = ω^{-15}` in `C^*` lives on the two A-cells, below
  the merge. It does not involve `(C_tr, B_tr, s_j, c_0)` except
  through quantities already cancelled in the reviewed ratio. It is
  not a closure condition on the 7-tuple (Opus: derived rigidity).
- **Shared `c_0` over-determination.** Each of (★_R), (★_tr), (★_G),
  (★_{H_e}), (★_{P_e}) consumes one fresh gauge. No new cycle.
- **Statement 8.2 at the 0-continuation.** Reduced `deg q = 1`,
  `mult(p, 0) = k_f`, `deg p = 1`: `k_f ≠ 1`, and
  `deg(q) · mult(p, 0) > deg(p)` is the searrow test for the trunk.
- **Exact-separation jet freedom.** The free extra-branch scalar
  `P_2(c^*)` of the repaired Theorem E lives on the A-step extra
  orbits, not on the chain continuation `(0, y) * 0`. It is a
  different jet, already reviewed as underdetermined, and is not a
  treetop obstruction.

---

## 6. Formal compatibility is not polynomial-pair realization

The initialization of §4 is an assignment of Sigray top polynomials,
tower constants, and `κ = 17` layers at `(0, y)`. It inhabits
`C[η]` together with the free multiplicative group of §4.1.

It does **not** produce `f, g ∈ C[x, y]`. The Newton-rectangle
support of Lemma 2.1(i), the condition that `J(f, g)` equal the
constant `c_0` as an element of `C[x, y]` (not merely that the top
Wronskian (E) hold), and the requirement that layers `P_1, …, P_10`
arise from a polynomial of bidegree `(k_f, l_f)` are outside the
formal treetop tier. The reviewed interior transport from the trunk
to the poles is likewise formal. Nothing here lands a source, kills
or promotes the D2 family at the polynomial-germ gate, or computes
exact `λ`.

Outcome (3) fails because, at the formal treetop tier, the printed
data determine the `(0, y)` tops and the ten-layer jet from the
7-tuple uniquely up to discrete roots. The residual freedom is
exactly the reviewed 7-tuple (six `C^*` gauges) plus inert
pure-shifts plus, if desired, a longer tower tail above index 4,
each extra stage carrying its own `s_j` not used by the displayed
tree.

---

## 7. Cheapest next discriminator, and the stop condition

The 7-tuple is a legal formal `(0, y)` treetop. The cheapest
remaining discriminator that could still kill the family uniformly
in `t` is the **reduced 10th-layer matching**, a statement in
`C[η]` independent of `i_tr`:

> Does there exist a polynomial (or even a Newton-rectangle formal
> series) `f` of type `(2, 3)` with `δ = 3/5`, `p_{(0,y)} = C η^{N}`,
> `P_1 = ⋯ = P_9 = 0`, and `P_10` a scalar multiple of
> `(η^{17} − A)^3 (η^{17} − (4/3)A)^2`, for some `A ∈ C^*`?

The `i_tr`-power on the trunk pattern is a global multiplicity and
factors off; a negative answer at the reduced pattern kills every
`t` at once. The test is desk-scale in the reduced `(dp, dq) = (85, 35)`
chart and does not require re-solving Prop 8.1(iv) or the twin law.
It does not need AWS.

Stop condition.

- If the reduced matching is empty, the family is dead at
  polynomial-germ / Newton-face scope, for every `t`, and the
  completeness program leaves this route.
- If a germ exhibiting that jet exists, the 7-tuple is realized at
  source-germ scope; the next gates are the actual constancy of
  `J(f, g)` in `C[x, y]`, then the already-endorsed Prop 7.1
  integrality ladder at `deg(p_H) = 2` (exact-separation review,
  best next lemma there), in that order.
- If the matching leaves a precisely typed free coefficient in
  `P_10` beyond the reviewed `A_F`, that coefficient is the new
  smallest datum and this report’s uniqueness claim at layer 10
  is to be repaired.

Secondary, strictly more expensive: the Prop 7.1 ladder itself
(t-free A-step data, desk-scale, independent of the `(0, y)` jet).
It remains the cheapest discriminator on the exact-`λ` side and is
not superseded by the present formal survival.

---

## 8. Clause-level claim firewall

| clause | status |
|---|---|
| Type `(2, 3)` degree equations at `(0, y)`, all `t` | PROVED in `Z[t]` |
| Case IV(i)–(m) for the displayed trunk | PROVED in `Q`, landing in `N` |
| `p_{(0,y)} = C_R η^{k_f}`, `c_R = 0` | PROVED in `C[η]` |
| Exact (E) / (★_R) at `(0, y)`, `R_R = −2/5`, `λ_R = 1` | PROVED in `C[η]` and `C` |
| `C_tr = C_R`, `t_{j,tr} = t_{j,R}`, `B_tr^{k_4} = s_4 C_tr^{l_4}` | PROVED in `C` |
| Silent layers `P_1 = ⋯ = P_9 = 0`, `P_10` the reviewed trunk pattern | PROVED as a formal assignment in `C[η]` |
| Shared tower and both merge attachments from one `(0, y)` | PROVED (tree; St 8.3 / Prop 4.2) |
| 7-tuple remains freely choosable (six `C^*` gauges) | PROVED (triangular, no new cycle) |
| Interior `β = 0`; twin `(A_1/A_2)^2 = ω^{-15}` with `(L_1/L_2)`-exponent `-5` | CITED, not re-proved |
| Vertex-local ODEs at H, G, trunk, poles | NOT RE-SOLVED |
| `P_10` realized by a polynomial in the Newton rectangle | NOT CLAIMED |
| `J(f, g) = c_0` as an element of `C[x, y]` | NOT CLAIMED (only the top product law) |
| Exact `λ`, source landing, Keller map, degree ceiling, JC2 | NOT CLAIMED |
| Route killed / route promoted at the polynomial-germ gate | NEITHER |
| AWS; canonical edit; commit/push; `jc2-lean` | NONE |

---

*Report body ends. Self-hash below covers everything above this line.*

report_body_sha256 = 3d07291357a266225f6e269d876c62dc6a18c7271ac3a730999fede430c696f1
