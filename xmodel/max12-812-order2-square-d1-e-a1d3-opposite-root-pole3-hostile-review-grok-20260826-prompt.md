# Hostile review charge: exceptional D1 `E` opposite-root pole-three obstruction

Act as an independent hostile different-model referee.  Work read-only
except for the one required report.  Do not trust producer PASS strings,
validator tokens, finite-field agreement, or the producer's theorem prose.
Rehash custody and rederive the obstruction from the frozen seven-row
source.  Do not run heavy CAS locally; hashing and small independent
hand/symbolic checks are allowed.  Do not mutate any producer, prompt,
ledger, or unrelated file.

Repository: `/Users/dc/code/math/jc2`.

Write exactly this report and no other file:

```text
xmodel/max12-812-order2-square-d1-e-a1d3-opposite-root-pole3-hostile-review-grok-20260826.md
```

Primary frozen custody:

```text
f2b947ddb2b251069666bb8287800127253ca3f118f5fb22587b3b0032bba1f5
  cases/max12_812_order2_square_owner_d1_e_a1d3_opposite_root_pole3_20260826/RESULT.md
00d9e48cb646372b3cd4306c517c4cbd04fae439c963119e3d547f5ae972f826
  cases/max12_812_order2_square_owner_d1_e_a1d3_opposite_root_pole3_20260826/AWS_LAUNCH_METADATA.md
31050d988c6246a48610847a7e7498bc55922a8ea0c9a4e87a1db7a30808e382
  cases/max12_812_order2_square_owner_d1_e_a1d3_opposite_root_pole3_20260826/EVIDENCE.sha256
65f09d0a5b461e5b82e4c776722990d7d5684e6307ba2484af8bd4814349b4cd
  cases/max12_812_order2_square_owner_d1_e_a1d3_opposite_root_pole3_20260826/FREEZE.sha256
65a40079b7b52964a775d01ef651f24ee04dcce05650147ed11327ace5913237
  cases/max12_812_order2_square_owner_d1_e_a1d3_opposite_root_pole3_20260826/PRODUCER_FREEZE.sha256
3678b0072ce5c4ac24681239f00e38bbecc196c4e9bf2f43ef1ab3a04915e1af
  cases/max12_812_order2_square_owner_d1_e_a1d3_opposite_root_pole3_20260826/compile_e_pole3.py
```

Charge every item below.

1. Rehash every row in `FREEZE.sha256`, `EVIDENCE.sha256`, and
   `PRODUCER_FREEZE.sha256`.  Distinguish exact Q, the characteristic-zero
   endpoint, from `F_65519` and `F_65521` screens.  Check source archive,
   input ancestry, engine rc, compiler/Singular diagnostics, caps, and zero
   swap.
2. Independently enumerate every licensed source, load, and target
   primitive through grade 18 for
   `ord(A)=1,ord(C)=4,ord(R)>=2,d=3`.  Confirm the count eight, global pole
   ceiling three, and the sole pole-three family
   `-(1/16)A^3/L^3` first at grade 18.  Identify the earliest omitted
   primitive if the census is incomplete.  Do not import the support
   miner's navigation verdict as proof of completeness.
3. Audit all seven literal Faber rows against the independent analytic
   emitter.  Check moving `p(sigma)`, delayed `k10,k6,k2` loads, every
   mechanically required normal/load jet through grade 18, every recursive
   sigma quotient, and the exact second correction of
   `-(3/8)R*A^2/L^2`.  Verify rows 1,3,5,6 are literally target-free through
   grade 18; name the first target term if not.
4. Rederive the `L^3` proper-numerator reconstruction and recurrence

   ```text
   N3=h1*z^5+h2*z^4+(h3+3P*h1/2)z^3+(h4+3P*h2/2)z^2
      +(h5+3P*h3/2+3P^2*h1/4)z
      +(h6+3P*h4/2+3P^2*h2/4),
   h7+(3P/2)h5+(3P^2/4)h3+(P^3/8)h1=0.
   ```

   Check every coefficient and sign rather than relying on generated PASS
   markers.
5. On `p=-2*lambda^2`, independently derive both Faber identities

   ```text
   N3(+lambda)=Phi6+lambda*(Phi5+(P/4)Phi3+(3P^2/32)Phi1),
   N3(-lambda)=Phi6-lambda*(Phi5+(P/4)Phi3+(3P^2/32)Phi1).
   ```

   Check moving-root corrections and confirm that using only rows 1,3,5,6
   introduces no target.
6. Audit the first-`AC/L` allocation scheme-theoretically.  Prove that on
   the exact `ord(A)=1,ord(C)=4` charts the nonzero leading linear forms are
   allocated to opposite roots, that the two displayed deck orientations
   exhaust the moving-root cover, and that no proportional/equal-root
   chamber was silently discarded.
7. In the orientation
   `A0=au(z-lambda), C0=cv(z+lambda)`, evaluate at the opposite root
   `z=-lambda`.  Verify every pole-at-most-two source, including the full
   `C^2/RA^2` collision and retained correction, is `L`-divisible after
   conversion to an `L^3` numerator.  Recompute the terminal
   `+(1/2)au^3 lambda^3`, the deck-swapped
   `-(1/2)au^3 lambda^3`, and the vanishing of every lower grade.
8. Recompute both localized unit ideals before radicals.  Check that only
   legitimate exact-contact/root/load coefficients are inverted, no
   leading coefficient of `R` is inverted, the entire closed
   `ord(R)>=2` tail is covered, and finite-etale descent over `D(p)` is
   valid.
9. Enforce the firewall.  A confirmed verdict closes only the routed block
   `E` after its cited gates on `D(p*k0)`.  It says nothing about a
   neighboring strict cell, an equality/positive-order-load face, another
   D1 chart, the square component, order two, `(8,12)`, maximum twelve, or
   JC2.

Classify every issue as mathematical, custody/software, or scope wording.
State the smallest failing identity and earliest missing source monomial if
any.  End with exactly one verdict word on its own line: `CONFIRMED`,
`REFUTED`, or `INCONCLUSIVE`.
