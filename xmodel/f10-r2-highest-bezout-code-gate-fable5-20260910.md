# FIRST Fable 5.1 gate: r2 highest-chart exact Bezout source and theorem contract

status=UNSEALED (no Seal, no charge_basis authored; adapter seals)
lane=f10-r2-highest-bezout-code-gate-fable5-20260910
gate_model=claude-fable-5-1
first_action_utc=2026-09-10T13:58:58Z (hash command); stop = earlier of 14:16:58Z (first+18 min) and 14:20:00Z USER TERM; final-3-min reserve from 14:13:58Z; never reset
mode=STATIC; manual math/text/hash only; ZERO subprocess/import/AST/syntax/compile/test/CAS; tools date, ls, wc, sha256sum, cat, grep, apply_patch only
owned=xmodel/f10-r2-highest-bezout-code-gate-fable5-20260910.md, box/f10-r2-highest-bezout-code-gate-fable5-20260910/ (both absent at 13:58:58Z; unsealed skeleton first, then body)

## 0. Custody, read scope, disclosure

All 12 snapshots in /tmp/jc2-lane.2X9qCs/inputs (12 files, all basenames distinct) were hashed by sha256sum at 13:58:58Z BEFORE any body read; all 12 digests equal the charged pins byte for byte (rows copied from the sha256sum output into box/input_custody.md; this body carries no hex digest). Both owned targets were absent at 13:58:58Z. Then all 12 were read WHOLE with cat -n in parallel single reads: entry.py (134 lines), produce.py (149), check.py (141), CONTRACT.md (68), Astra report (58, ends at its Seal), READ-SCOPE (18), arithmetic.py (277), check_arithmetic.py (208), reconstruction gate (63, at marker), cyclic core (228, at Seal), cyclic gate (65, at marker), infinity final gate (66, at marker). No read was clipped, so no recovery read was needed. Old produce.py/check.py of the reconstruction box were NOT opened (uncharged, name collision); their schema is consumed only through the charged reconstruction gate. The baseline packet was not opened, parsed or hashed; its pin is a future literal only. No provenance, ledger, corpus, Git, network, process, other lane or live file was accessed; nothing was executed, imported, parsed or compiled.

Disclosure: this is a different-model FIRST review of Astra's new instrument, but three of the accepted imports (reconstruction code gate, cyclic core gate, infinity final gate) were authored by Fable 5.1 in earlier sessions; they are consumed here at their exact stated scope and not re-proved. No same-byte WHOLE reuse: all twelve reads were fresh in this session.

## 1. Verdicts

| item | verdict |
|---|---|
| A weights/support checks before projection; shapes; F,G degree<=5; cofactor identities; whole-chart sufficiency incl. Q=0; INCONCLUSIVE semantics | CONFIRMED |
| B producer P7/interface/canonical/Euclid; checker independence and 77 positions; wire_q 4096 question | CONFIRMED; hypothesised wire_q leak is NOT present, fail-closed candidate rejection only, no false positive, no edit |
| C entry barriers, flags, path distinctness, bindings not authentication | CONFIRMED as bindings; ROOT duties correctly declared, none silently assumed |
| D exclusive publication, rechecks, receipts, exit semantics | CONFIRMED; no exception or partial-output path is declared successful |
| E one prospective mutation control | CONFIRMED as design; fires exactly at check.py:129 |
| F conditional composition to the cyclic-core parameter sequence | CONFIRMED conditional; unlocks, does not perform |

Smallest blocking correction: none. One liveness-only GAP (not soundness): the baseline top-level key names consumed at produce.py:36-44,71 and check.py:38-45,54 (format, job_tag, interface.{variables,weights,basis,base}, field.P7, rows.Psi, row order 5/6/7, exponent order X1,X2,X3,z,S,vartheta) come from the accepted reconstruction schema, which is charged here only through its gate report, not as code; a mismatch raises KeyError/ValueError before any output, never a success receipt.

## A. Mathematical instrument

CONFIRMED. produce.py:86-87 requires e[4]=e[5]=0 and e0+2e1+3e2+5e3=h for EVERY term before the z filter at :88; check.py:68-69 does the same before `continue` at :70-71. Own shape derivation: with z=0, a+2b+3c=h and a>=0 gives, for h=5, c=0:b<=2, c=1:b<=1, c=2 impossible; h=6: (3,1,0); h=7: c=0:b<=3, c=1:b<=2, c=2:b=0, i.e. (3,2,0). So H5=P+Qv, H6=B3+B1v+B0v^2, H7=C3+C2v+C1v^2 exactly as CONTRACT.md:9-13; the explicit shape checks at produce.py:96-97 and check.py:72-73 are implied by the weight check and therefore harmless. Given h,b,c the exponent tuple is unique, so the table accumulation at produce.py:89-90 and check.py:74 never merges two terms. Degrees: Q^2B3<=5, PQB1<=4, B0P^2<=4, so deg F<=5; Q^2C3<=5, PQC2<=5, C1P^2<=4, so deg G<=5 (produce.py:103, check.py:106). Own expansion of the cofactor identity: (P+Qv)(QB1+B0Qv-B0P) = PQB1 - B0P^2 + Q^2B1 v + B0Q^2 v^2, and Q^2H6 minus this is Q^2B3 - PQB1 + B0P^2 = F; the same with C2,C1 gives G. No division, valid over any commutative ring, so cF+dG=1 in B[u] yields the displayed expansion 1 = cQ^2H6 + dQ^2H7 - {...}H5 in B[u,v] (CONTRACT.md:23-26): 1 lies in the chart ideal, including Q identically zero (then F=B0P^2, G=C1P^2 and the identity reads F=P*B0P). No aP+bQ, no inversion of Q anywhere in the code. Zero/constant/degree drops: bounded() at produce.py:47-55 strips trailing zero vectors; F=0 with G a nonzero constant gives c=[], d=[G^-1] and passes :129; F=G=0 or a nonconstant gcd fails :125 with the INCONCLUSIVE message; a nonunit leading coefficient raises in arithmetic.py:80-81. All are ValueError and become INCONCLUSIVE_NO_CERTIFICATE, never a common-zero claim.

## B. Producer and checker

CONFIRMED. produce.py:23-33 forms 2KL^2 - 140 tau (1+tau-6Z) KL dL + 245 gamma dL^2 with only the last term of degree 7, leading -245*120*144*tau checked at :32, made monic at :33; check.py:27-35 re-forms it with the checker's own times/plus and both compare all eight packet coefficients (:44, :45). Interface at produce.py:39-42 / check.py:41-44; canonical JSON re-encode equality at :35 / :37. Fractions: rat() (:9-21) and rational() (:9-21) both bound the signed string to 4096 characters BEFORE int(), reject leading zeros and -0, require positive denominator and reducedness. Euclid (:106-124): divide cancels the leading term exactly because field representatives are canonical remainders (arithmetic.py:59-63), so len(r) strictly drops; invariant r0=a0F+b0G, r1=a1F+b1G holds at :118 and :124; at most 7 divisions from degree 5 (one extra when deg F<deg G), cap 8 at :122 is safe; the producer replays cF+dG=[one] at :129 before serialising. Inverses go through fi (arithmetic.py:78-88), a whole-B extended Euclid against P7 with product readback and fresh labels 'highest-leading-k'. Checker: imports only entry (metadata) and the pinned check_arithmetic (pin ARITH['check'] at entry.py:9 equals the charged hash); never reads F/G/gcd from the certificate (keys fixed at check.py:108); rebuilds the dense 3x4 box per row (:56-75), F/G at :98-105 in the same order c<->F, d<->G (:101,:117,:123); F() in check_arithmetic.py:60-68 reduces from the top with MODULUS[7]=1, canonical like the producer's qdiv, so cross-implementation equality of vectors is meaningful; coordinate 0 is the rational unit (F(1)=(1,0,..,0)), and the loop at :125-129 tests all 11 u-powers times 7 coordinates including positions beyond the cleaned length. wire_q question: arithmetic.py:256-260 raises ValueError('coefficient decimal bound') for any signed string longer than 4096, and the 4300-digit process guard set at entry.py:79 makes str() itself raise ValueError beyond 4300 digits; both occur inside candidate() (:131-132) and become INCONCLUSIVE_NO_CERTIFICATE/exit 2. Sign counting is identical on all three sides (produce.py:13, arithmetic.py:258, check.py:14). Hence no over-long integer can be emitted, and even if one were, check.py:14 rejects it with exit 1. False-positive risk: none; only candidate rejection. No source edit is warranted.

## C. Entry boundary

CONFIRMED as bindings. entry.py:2-3 imports only os/sys; :18-26 Linux and DMI 'Amazon EC2'; :29 exactly 17 argv with the ordered eight flags and TAG; :32-35 nonzero 64-hex root/self/entry pins; hashlib/stat/json only at :36-38; canonical absolute realpath-equal paths (:40-43); six distinct paths (:48); arithmetic pin hard-coded per mode (:8-9,:51); read() with O_NOFOLLOW, regular-file, size cap and pre/post fstat drift (:53-62); recheck() pins three sources and the 1652675-byte/BASE_SHA baseline (:67-72) before parsing, and again in receipt() (:128); 4300 guard before any parse (:78-79); duplicate keys and every numeric token rejected (:81-93). The mathematical module is loaded only in arithmetic() after enter() (:116-124), from the pinned path. `-E -s -S -B` keeps sys.path[0]=script directory, so `from entry import` at produce.py:2/check.py:2 resolves; `-I` (implying -P) would not. What the code cannot do is stated, not hidden (CONTRACT.md:51-55): a same-user caller can pass any hashes, so the CLI pins bind but do not authenticate; entry.py is already imported when its own hash is checked; a stray fractions.py in the script directory would shadow the stdlib; the arithmetic file is re-read by exec_module after its hash read; interpreter, native closure, ancestry and freeze are ROOT's. No mathematical import precedes the boundary.

## D. Publication and exit semantics

CONFIRMED. write() (entry.py:98-109): cap, O_CREAT|O_EXCL|O_NOFOLLOW 0600, full-write check, flush, fchmod 0444, fsync, parent fsync, byte readback. Receipt path checked absent at :75 and certificate at :76-77. produce.main (:135-145): candidate ValueError -> INCONCLUSIVE receipt, exit 2; otherwise recheck, certificate write, then CANDIDATE_UNCHECKED with the certificate hash. Every non-ValueError exception, a cap overflow at :144 (outside the try), a short write or readback mismatch propagates: exit 1, no receipt, at most an unreceipted 0600/0444 file, which CONTRACT.md:55 correctly calls incomplete. check.main (:132-137): pre-read with drift, verify, post-read equality, then the success receipt; any ValueError in verify (including the identity failure at :129 and UnicodeDecodeError/JSONDecodeError, both ValueError subclasses) is uncaught: exit 1, no receipt. No print exists, so success streams are empty. No path declares success without the full 77-position identity. No runner is emitted; the 600 s/500 s/2 GiB/16 MiB envelope, per-phase profiles, deadlines, native closure, ancestry, freeze and custody are declared unregistered (CONTRACT.md:53).

## E. Prospective control

CONFIRMED as design (CONTRACT.md:65-68, report :35). Adding 1 to coordinate 0 of the constant coefficient of c (F!=0) changes cF+dG by exactly F, a nonzero element of B[u], so at least one of the 77 positions differs and check.py:129 raises. Earlier checks stay green: keys and canonical re-encoding (:108-110) if the mutated file is re-serialised with the same sorted compact encoder; modulus untouched; length unchanged or [] -> one vector; the only trailing-zero case is c=[(-1,0,..,0)] becoming [], which the removal rule covers. If F=0 then G is a nonzero constant and d must be mutated instead. The checker needs a fresh receipt path, else entry.py:75 fails first as a pin failure, not the control. Expected: last stderr line `ValueError: CHECK FAILED: full B[u] Bezout identity`, exit 1, empty stdout, no receipt. No mutation was implemented or run; no coverage is claimed.

## F. Conditional composition and next prerequisite

CONFIRMED conditional. The producer keeps only z-degree-zero terms, so the chart forms are Psihat_h(X,0) at X1=1, the H_h of the cyclic core. Weighted scaling H_h(l X1, l^2 X2, l^3 X3)=l^h H_h moves any common zero with X1!=0 over an algebraic closure to X1=1, where the checked identity forbids it; the accepted infinity endpoint leaves only the origin on X1=0. Together: V(H5,H6,H7)={0}, the parameter-sequence hypothesis of cyclic core :31-34, hence gr R=B[Y]/(H5,H6,H7), dimension 35 and seven per class (core (1),(2), gate B). This unlocks the graded basis, fifteen multiplication blocks and the 7x105 membership test; it does not construct them, and gives no full-source zero/nonzero, point, all-r, bound-196 or JC2 statement. The baseline remains CHECKED_BUT_NOT_PROMOTED pending its separately selected semantic negatives. Single next cheapest execution: ROOT registration (isolated three-file directory at the charged hashes, the two original arithmetic files at separate canonical paths, the baseline at its pin, fresh certificate and two receipt paths, root-registration pin, the -E -s -S -B vector, per-phase profiles inside the existing envelope), one producer run, one checker run on the frozen certificate, then the single E control. No review farm.

## OPEN(S) RAISED

None. No new canonical OPEN. The only quantities left are the producer's own: whether the actual F/G admit a unit gcd (decided only by the future bounded run), the liveness-only schema-key GAP named in section 1, and the separately owed baseline semantic negatives (controls 6 and 10) and result intake. No execution, registration, promotion or follow-on authority is issued here; no charge_basis, no new global theorem.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly xmodel/f10-r2-highest-bezout-code-gate-fable5-20260910.md and box/f10-r2-highest-bezout-code-gate-fable5-20260910/input_custody.md, both absent at 13:58:58Z; no shared, frozen, input or old file was touched; no corpus scan, no other lane, no network, no scientific subprocess.

## Completion

Authoring attestation: every byte of both owned files was written by apply_patch (skeleton 14:06Z, body 14:07Z, this closing block afterwards); no Write/Edit tool, no shell redirection, no helper, no source edit, no execution. Own WHOLE reread of this report (sections 0-1, A-F) was done at 14:07:52Z before this block; the twelve custody rows in box/input_custody.md were rechecked against a fresh sha256sum (each row found exactly once) and the marker was confirmed absent before it was appended. All work finished before the 14:13:58Z reserve; stop 14:16:58Z, never reset. The marker below is the only standalone marker in this file and nothing follows it. All writers idle at the marker.

<!-- BODY-END -->
