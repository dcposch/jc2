# FULL0925 Astra blind — change the allocation, not the standards

September13,2026; independent native Astra. Actual first09:34:31UTC.
Documentary/manual ideation, UNPROMOTED. No current ROOT/peer blind was read.
All seven registered inputs authenticated; APPROACHES, MASTER46, previous
synthesis, tangent FIRST, delta and COMMON read WHOLE without clipping.
COORD77ec0b uses my exact-byte personal WHOLE read from the preceding tranche.
Original reserve09:55/HARD09:58 unchanged. No network, scientific execution,
AWS, additional linked source, shared edit or agent. Only administrative
finalization executes Python. Proposed literature priority remains UNVERIFIED
until ROOT's postblind checksum.

## Outcome and all46 dispositions

The scalar/engineering selection no longer wins the lead allocation. Its
mathematics improved, but the closing implication did not: even a complete
exception-free scalar would not establish full comparison, REG or source
zero. Repeated setup has now consumed multiple tranches. A ready patch is
not new mathematical information or a reason to reset opportunity cost.

The tangent theorem is a useful unbounded exclusion, not a reason to defend
that route or immediately attack its exceptional strata. Its genuine lesson
is to obtain an ACTUAL plane-source hypothesis and use it globally. The
successful nonnegative Euler term came from actual nonproperness curves,
not a more elaborate abstract surface.

Dispositions below concern relative research priority; every mathematical
closed/open/imported scope in the snapshot is retained.

| IDs | Disposition and reason |
|---|---|
| 1 | LOWER its temporary scalar/engineering allocation. Preserve the complete unbounded F10 source problem and accepted finite-exception statements; no mathematical reopening or closure. |
| 6,7 | RAISE narrowly as source-recognition tools: the new theorem turns actual generic cuts into A1 normalizations and a global contradiction. This supplies no general coordinate-pulling ruling or JC2 theorem. |
| 16,29 | LOWER unchanged operator/flow enlargement and source-selection retries after the two Bass and cross-frame nonconstraints. Keep their actual-source selection problems open. |
| 34,35 | LOWER direct coordinate-preserving descent and full-output low-fibre iteration as construction allocations after their unbounded exclusions. Projections, different embeddings and remaining strata are not thereby closed. |
| 2--5,8--15,17--28,30--33,36--46 | UNCHANGED. No new premise in this packet closes their global gaps or warrants another farm. |

This covers every ID exactly once. In particular: 19/21 retain the genuinely
polynomial AS109 lift endpoint, not a formal/Witt substitute; 26 retains
d1=2, second-leg d2>=4 and primitive cases, not a resurrected cubic block;
28/31 retain actual-source log/normality gaps, not more relaxed controls;
30/33 retain an unrestricted regular S/T pair as a direct disproof endpoint;
44 does not regain the already-rejected prime proof; 46 remains nonblocking
and outside the separate repository's inspection boundary. IDs36--38 do
not acquire support/height/degree bounds from finite census results.

## Global ranking and opportunity cost

Proof-side: first, an ACTUAL polynomial-plane-target constraint capable of
forcing normality/properness, rather than freeness, exactness or abstract
radial-form descent already survived by controls. The most sharply identified
objects are the finite canonical graph and the full collision groupoid;
the missing membership/division and source-target distinction remain real.
Second, uniform landing or a strictly decreasing invariant for arbitrary
source data; none of the finite census, genus floors or log hypotheses gives
that arrow. Third, actual Bass/trace selection, conditional on a new source
input rather than another annihilator family. This is a ranking of open
bottlenecks, not a claim that a fresh closing argument was found here.

Disproof-side: first, a COMPLETE regular scalar pair on S/T, with exact global
extension and bracket, because its successful output directly yields a plane
counterexample. Second, genuine properness of a complete unretired source
ideal, not survival of a leading quotient. Third, a polynomial AS109 integral
lift with finite support. A new higher-dimensional projection construction
can compete only when it supplies an actual polynomial-plane map and an
honest noninjectivity witness; ambient degree is not that witness.

F10's accepted critical coefficient and generic scalar unitness make the
scalar a well-defined necessary test, but not the strongest global attack.
Park the resultant and r3 rank jobs for this allocation cycle. Reopening must
name their downstream mathematical consumer and fit a bounded delivery plan
without using primary co-research as continuing preflight staff. Do not replace
the parked scalar with more residue classes. A scarce frontier slot need not
be occupied when the only available proposal repeats an unchanged gap.

## Independent connection: mark the source line, then test what it buys

There is a useful comparison with the tangent proof which does NOT transfer
its conclusion. For an actual plane Keller pair (f,g), choose a generic
linear source coordinate a so f is monic in the complementary coordinate b,
after harmless constant target rescaling if needed.
Then R=C[a,b] is finite over C[a,f]. Moreover C(a,f,g)=C(a,b): a nontrivial
polynomial common right factor in b would make its derivative divide both
f_b and g_b, contradicting f_a*g_b-f_b*g_a=1. The polynomial-Luroth step
here is standard but priority/applicability should be checked postblind.
Thus B=C[f,g,a] has finite birational unramified normalization A2, and its
generic a-cuts normalize to A1. This replaces a chosen radial potential by
an ACTUAL coordinate-line marking.

The crucial difference is that this normalization is UNRAMIFIED. It has no
critical hyperbolas to which the tangent coordinate lemma applies. Normality
does not follow from the marking alone. The remaining obstruction would
have to detect whether a polynomial family of noninjective immersed A1s can
have a globally constant transverse area derivative. Local or formal
symplectic deformations do not decide polynomial dependence on a over the
whole plane. The following card checks the most tempting primitive invariant
and finds that it still fails to distinguish the actual target.

## Card 1 — marked-line action: tested, not a new closing mechanism

Object: an actual J_(a,b)(f,g)=1 polynomial family. Let

    T(a,b)=integral f(a,b)*g_b(a,b) db.

On its SAME-a collision curve, with b1!=b2 and equal f,g values, hand
differentiation gives

    d(T(a,b1)-T(a,b2))=(b1-b2) da.                 (1)

The b-independent integration term cancels. This is a concrete connection
between coordinate recognition, collision geometry and exact primitives,
with no assumed finite cover or source point in a truncated ideal.

Bounded quantity/test: is the one-form on the right nonexact on the known
relaxed collision model? Cheapest test was the following five-minute hand
calculation, completed in this blind. Use the frozen-map control

    u=-x, x^2*(y+v)=2,

and the determinant-one source frame a=x+lambda*y, b=y, lambda!=0.
Imposing equal a on the two points gives

    y=x^-2-x/lambda, v=x^-2+x/lambda,
    a=lambda/x^2, Delta b=-2x/lambda,
    Delta b da=4*x^-2 dx=d(-4/x).

The primitive is itself the difference of the GLOBAL polynomial -2xy
on these two source points. So even the marked-line condition (1) survives
that old control. This does NOT exhibit a polynomial Keller pair with plane
target: the control's source/target qualifications in APPROACHES remain.

Fingerprint: canonical Liouville/collision action, now explicitly restricted
to a source-coordinate pencil. Negative outcome would have supplied a new
discriminator; the actual outcome is exactness and no distinction. STOP
this primitive test, no changed-frame or higher-order control successor.
The global closing gap remains the actual polynomial PLANE TARGET, not
absence of a source coordinate or existence of a polynomial primitive.

## Card 2 — a projection escape has to be genuinely relative

Object: the admitted clean-stratum tangent map Phi=(C,D,E), with its
polynomial derivation W defined by W(C)=W(D)=0, W(E)=1 through the inverse
Jacobian. A source coordinate h with

    W(h)=c+h*K, c!=0,                              (2)

would make (C,D) on the coordinate plane h=0 a Keller pair, after the
constant normalization supplied by a coordinate completion of h. An exact
noninjective pair of points on that plane would finish a counterexample.
Neither the generic degree of Phi nor an arbitrary projection licenses
such a noninjectivity claim. This is the strongest disproof attack I can
state concretely without inventing a new ambient source.

The absolute-slice variant W(h)=c is not a fresh escape. For generic C,D,
the fibre is the w-line minus the finitely many roots of C*D-p(w), hence
geometrically connected; the constant field of W is C(C,D). The map (C,D)
is surjective: use x=0 for C=0, and choose w away from those roots for
C!=0. Its differential has rank two everywhere. Therefore a rational
function of C,D which is polynomial on the source has no denominator
divisor, and ker(W) in R is C[C,D]. Every global slice is c*E+Q(C,D),
which is the pullback of a target polynomial coordinate. The accepted
tangent theorem already excludes that slice from being a source coordinate.
This is a manual composition screen, not a separately promoted theorem.

Bounded quantity: the class of W(h)-c in R/(h), together with an EXHIBITED
coordinate completion and two actual equal-output points. The cheapest
prospective test is a30-minute postblind history/identity check of whether
a proposed globally parameterized h satisfies all three requirements.
No such h was obtained here, so no coefficient search is commissioned.
For h=z-H(x,y), (2) is exactly a polynomial graph-restriction PDE, the
fingerprint of the existing graph-area route. A formal solution, a finite
jet or a genuine global slice is not a new candidate. STOP if the proposal
is merely that old graph PDE with a different degree or seed. A verified
relative solution plus collision would be decisive; a failed ansatz would
close only that ansatz, not projections generally.

## Mechanism verdict and selected next work

**NO_NEW_MECHANISM** at the global closing-arrow level. Card1's new marking does not survive the
existing control; Card2 clarifies an unclosed logical escape but supplies
no new construction and largely meets an already-recorded graph problem.
Neither warrants a new lane merely because a formulation is explicit.
The strongest proof attack remains to obtain a full-plane-target condition
on the actual canonical graph/collision groupoid which the pseudo-plane
control lacks. Its single-form presentation and full descent cocycle are
not automatically supplied by the existing relaxed geometric hypotheses.
Do not replace that missing source input by another abstract control.

The best use of primary research time is a genuinely independent all-degree
attack on that source distinction, or a complete regular S/T construction,
selected after comparing the other BLIND submissions and doing the history
checksum. I nominate no automatic mathematical successor from my two cards.
If none survives cross examination, explicitly record no launch rather than
silently returning to scalar setup, tangent exceptional strata or another
conditional criterion. The existing endpoints are more valuable than an
unjustified new name for their missing hypotheses.

## Systems: NO_CHANGE to software; enforce the existing allocation rule

Measured problem in the frozen packet: nine no-CAS runner groups passed,
then two scientific preflights stopped before algebra; one rejected a
duplicated CORRECT diagnostic, another a legitimate pinned module path and
lost partial metadata. The small corrective patch is already statically
reviewed. This calls for no new controller, ledger, qualification framework
or occupancy model seat. The strongest agent's attention remains the scarce
resource; credits and AWS quota are not presently the limiting factor.

NO_CHANGE is not permission to launch the third worker. Charge all prior
setup and repair to the same mathematical bottleneck under the existing
global-gap guard. A later independently selected job must have one bounded
owner for preflight, science, collection and exact retirement, with partial
logs already durable. Its proposed end-to-end value must beat the research
displaced. Do not claim complete native qualification from the old pilot.

Continue Astra/Fable primary research and risk-prioritized different-model
review; use Sol for a bounded engineering task only when one is independently
worth selecting. Stop scalar/residue/preflight retries this cycle; retain
their evidence and frozen software. Park r3 rank, K16 finite successors,
operator enlargement and relaxed normality-control families. Keep broad
external coverage debt visible for a separately authorized sweep, not this
blind. No idle worker needs preserving; retained EBS remains a real cost,
but this packet gives neither a billing total nor deletion authority.

No resource dollars, exact credit balances, new external theorem, support
cap, global proof, counterexample or promotion is inferred. All46 were
considered before either detailed card. Input postpins and final custody
are recorded only after the complete report readback and writer shutdown.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `13596`.
- Body SHA-256:
  `34ed766b793cdf7de9db17c41a43fa0c3e2a18b3402dd55b5062a502d41f249e`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
