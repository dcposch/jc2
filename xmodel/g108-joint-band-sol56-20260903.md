# `D = 108` joint-chart band engine (packet Q3)

Status: **COMPLETE — READY FOR SEAL**

## 0. Result first

The unique split branch is

```text
delta = 3, partition [1,1]
p = pi^2-c, c != 0
q = p^23 U, dU/dpi = 5p^2, deg U = 5
one modulus e0 after translation/scaling gauge
24 principal G-roots -> 12+12, both final at delta' = 4.
```

The exact joint-chart compiler kills it at **stage 0 preflight**, before a
direct `F/G` pole row or Jacobian row is reached. Seven rational `Q*` pivots
in the common-`h3` incidence leave five normal forms; with the declared
Rabinowitsch equation `Zc*c-1`, their ideal contains `1`. Characteristic-zero
Singular returns dimension `-1`, standard basis `1`, normal form of `1` equal
to `0`, and the same explicit lift certificate. The split verdict is

```text
DEAD[D108 / delta=3 / DECLARED-NECESSARY-JOINT-CHART]
killing family = common-h3 minor incidence
first unit stage = 0 preflight
```

No `F,G` pair is recomposed, and no `REPRESENTATIVE` is claimed. The no-split
alternative is conditional on `delta* >= 7/2`; Proposition 6.3 applies directly
and descends to `(24,16)`. Its exact promoted status is recorded separately in
§7 because the denominator-shed support/ring map must not be supplied by
analogy.

## 1. Custody, scope, and execution contract

The lane receipt `xmodel/g108-joint-band-sol56-20260903.run.v2` declares 18
frozen inputs under `/tmp/jc2-lane.BhSYQx/inputs`. The manifest was generated
mechanically from its `charged_input_<i>_sha256` and
`charged_input_<i>_basename` fields and then passed to `sha256sum -c`; all 18
lines returned `OK`. No digest was retyped. The stage-0 manifest SHA-256 is
`530034c276bc8e84b9a3d4a3078d023d6d13d61bd8547c2172091784567d60a7`.

The charged classification gives the datum and unique branch at
`g108-minor-classification-opus5-20260903.md:4-26`, the rigid `12+12` tree at
`:201-215`, and the split/no-split dichotomy at `:222-238`. It gives the tower,
ambient counts, local leaders, and Jacobian scalars at `:240-301`, while
explicitly leaving the new weight cuts, equality table, `D1` boundary, outer
offsets, and pole schedule for this lane at `:303-326`. Thus every number that
was `OPEN[108-OUTER-WEIGHTS]` is derived here, not copied by analogy.

All engine invocations were foreground processes of the form

```text
timeout 3000 taskset -c 0-3 env \
  OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  NUMEXPR_NUM_THREADS=1 /usr/bin/time -v python3 DRIVER ...
```

The machine exposed CPUs `0-15`; the recorded process affinity was `0-3`.
Software was Python 3.12.3, SymPy 1.12, and Singular 4.3.2. No background job
was launched. Writes are bounded to `box/g108band-20260903/` and this report;
there are no ledger, `jc2-lean`, `ideation-*`, or prohibited lane-report edits.

## 2. Datum, tower, and conventions

The input row is

```text
(n,m) = (108,72)
M = (-72,81,106), d = (108,36,9,1), V = (7,7)
u3 = 2, v3 = 7; major radii = (3/8,1/4,-1).
```

The fixed orientation is `F=degree 108`, `G=degree 72`. It is the opposite of
the historical lower-case `(f,g)` order in some source passages. The
Tschirnhausen tower is

```text
h3 = P,                                  deg_y 9,   top P=y^2(y-x)^7
h2 = P^4 + C2 P^2 + C3 P + C4,          deg_y 36
F  = h2^3 + A2 h2 + A3,                 deg_y 108
G  = h2^2 + B1 h2 + B2.                 deg_y 72
```

Set `z=w-1`,

```text
K3(t,w)=t^9 h3(t^-1,w/t),
K2(t,w)=t^36 h2(t^-1,w/t).
```

At the major `D2` node, `t=s^4`, `z=pi*s^6`; hence the weight is
`4r+6q`, or primitive weight `W=2r+3q`. At `D1`, `s=e^2`, so
`t=e^8`, `z=e^12(1+Pi*e)`. This is the charged §3 convention
(`g108-minor-classification...md:320-325`). Replacing it by `4r+5q` would
identify the major flag coordinate with the later physical minor series; that
is precisely the flag/place/series conflation barred by FALLACY-v2. The two
maps are declared separately below.

The raw block counts are

| block | H | C2 | C3 | C4 | A2 | A3 | B1 | B2 | total |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| raw monic chart | 54 | 135 | 216 | 297 | 1998 | 3294 | 702 | 1998 | 8694 |
| after fixed tops | 45 | 126 | 207 | 288 | 1962 | 3258 | 666 | 1962 | **8514** |

The branch parameters are named `jet1,jet2,c,e0`. Here `U(pi)` is the
degree-five factor in `q=p^23 U`; it is never called `u`, so it cannot be
confused with a jet coefficient or with `u3=2`. The modulus `e0` belongs to
the effective-root face and does not enter the necessary common-`h3` block.
No unproved effective-`T2/T3` bridge identifies it with an `F,G` coefficient.

## 3. Exact adaptation map from `(99,66)`

| charged engine item | `D=108` replacement |
|---|---|
| old receipt/frozen directory, 20 inputs | this receipt, `/tmp/jc2-lane.BhSYQx/inputs`, 18 inputs |
| two branches `delta2`, `delta52` | one branch `delta3`; no `mu_2` equivariance because `e=1` |
| degrees `99/66`, tower `11 subset 33` | degrees `108/72`, tower `9 subset 36` |
| `K3` top `z^8(1+z)^3` | `z^7(1+z)^2` |
| `h3` cap `11-r`, 21 strict coordinates | cap `9-r`, 7 strict coordinates |
| `h2=h3^3+C2*h3+C3` | `h2=h3^4+C2*h3^2+C3*h3+C4` |
| post-`h3` ambient `516` | post-`h3` ambient **628** |
| major weight `3r+4q` | `4r+6q`, primitive `2r+3q` |
| `D1`: `t=e^9`, `z=e^12(1+Pi*e)` | `t=e^8`, `z=e^12(1+Pi*e)` |
| `K2` face `(pi^3-1)^8`; 7 `D1` rows | `pi^12(pi^2-1)^8`; 4 `D1` rows |
| `build_FG`: cube/square in `K2` | unchanged because `n/d2=3`, `m/d2=2` |
| fixed outer degrees `65/98/32/65` | **`71/107/35/71`**, not raw slot degrees `72/108/36/72` |
| outer `D2` rank 5598, `D1` rank 176 | ranks **7656** and **54** |
| local centers for orders `2`, `5/2` | `w=jet1*t^2+jet2*t^3+pi*t^4` for order `3` |
| pole normalizers/shifts branch-dependent | `F: pole 12, shift 96`; `G: pole 8, shift 64` |
| Jacobian scalars `99,-66`, top degree 163 | `108,-72`, top degree 178 |
| already-solved hard-coded `h3_branch_map` | construct incidence rows first; otherwise the decisive residue is erased |
| all positive-degree Jacobian targets zero | additionally subtract `1` at degree zero |
| legacy “nonlinear residue appears” boolean | distinguish zero, rational unit, and genuine nonlinear residue |

The raw outer numbers in the charged D=108 design table are slot degrees.
Using them directly would be an off-by-one error: after fixing each top face,
the coefficient supports have degrees `71,107,35,71`. The source-compatible
driver records this correction explicitly.

## 4. Theorem-1.2 and outer ranks

### 4.1 Major `h3` and the 628 analogue

The fixed-top `H` block has 45 lower coordinates. Relative to boundary
`4r+6q=42`, 36 are strictly below and two are unwanted at-level sites
`(3,5),(6,3)`. These are 36 order rows plus two separate face-incidence rows,
not an undifferentiated count. Rank is `38`; the seven strict coordinates are

```text
Hc_1_7 Hc_1_8 Hc_2_6 Hc_2_7 Hc_3_6 Hc_4_5 Hc_5_4.
```

Consequently the requested 516-analogue is

```text
H 7 + C2 126 + C3 207 + C4 288 = 628.
```

The `K2` boundary is `168`. Exactly 558 projected positions lie strictly
below it and eight are at level:

```text
(r,q)=(3k,28-2k), 1<=k<=8,
value=(-1)^k binom(8,k).
```

These encode `pi^12(pi^2-1)^8`. Among the nominal `558+8=566` labels,
`(1,27)` is an exact approximate-root identity. The other 565 rows form a
unit-triangular block:

| leader block | output q-range | exact pivots |
|---|---:|---:|
| C4 | 0..8 | 288 |
| C3 (times top q=9 of `h3`) | 9..17 | 198 |
| C2 (times top q=18 of `h3^2`) | 18..26 | 79 |
| total | | **565** |

Thus `628-565=63`. At `D1`, the face begins at `e^344`; only primitive
weight `W=85` contributes below it, at `e^340,...,e^343`. The four binomial
rows have rank four. Columns with `q=(25,23,21,19)` give determinant `64`.
The exact inner ledger is therefore

```text
fixed inner ambient                                      666
h3: strict order 36 + face 2, rank                       38
post-h3 simultaneous ambient                             628
h2 D2: 566 labels, one identity, rank                    565
h2 D1: 4 labels, rank                                      4
total rank from fixed inner                              607
free inner dimension                                      59.
```

### 4.2 Outer `D2` support and `D1` boundary

For an outer block, the exact basis is
`K_Q=sum c[r,q] t^r(w-1)^q`, with `r>=0`, `0<=q<36`, `r+q<=D`.
The fixed-top specifications and exact ranks are

| block | D | ambient | primitive `D2` threshold | `D2` rank | retained | `D1` threshold | raw/rank |
|---|---:|---:|---:|---:|---:|---:|---:|
| A2 | 71 | 1962 | 166 | 1920 | 42 | 680 | 40/21 |
| A3 | 107 | 3258 | 250 | 3258 | 0 | 1024 | 0/0 |
| B1 | 35 | 666 | 82 | 558 | 108 | 336 | 12/12 |
| B2 | 71 | 1962 | 166 | 1920 | 42 | 680 | 40/21 |
| total | | **7848** | | **7656** | **192** | | **92/54** |

At `D1`, a coefficient has exponent
`8r+12q+k=4W+k` and scalar `binom(q,k)`. Rational row reduction gives

| primitive offset | 0 | 1 | 2 | 3 | total |
|---:|---:|---:|---:|---:|---:|
| raw rows | 40 | 28 | 16 | 8 | 92 |
| rank | 20 | 16 | 10 | 8 | **54** |

There are 16,057 literal `D1` labels supported entirely on coordinates already
deleted by `D2`; they are redundant labels, not additional rank. Outer rank
is `7656+54=7710`, leaving `138`. In particular `A3` is wholly removed by
the `D2` preblock.

## 5. Minor incidence, pole rows, and Jacobian rows

### 5.1 The decisive common-`h3` incidence

The physical minor series is declared only here:

```text
y=jet1*t+jet2*t^2+pi*t^3,
w=t*y=jet1*t^2+jet2*t^3+pi*t^4.
```

The fixed top is `w^2(w-1)^7=-w^2+O(w^3)`. Hence the scalar-compatible
leader is

```text
K3 = -t^8(pi^2-c)+O(t^9),
```

not literal `+t^8(pi^2-c)`. The charged leader display fixes the face only up
to a nonzero scalar. Literal plus sign would yield the spurious constant
`-2` in the `pi^2` row; the driver includes this as a rejected sign control.

For a term `a[r,j] t^r w^j`, enumerate `a+b+k=j`; its contribution is

```text
a[r,j] * multinomial(j;a,b,k)
  * jet1^a * jet2^b * pi^k * t^(r+2a+3b+4k).
```

Extracting through local power 8 gives 13 coefficient labels, 12 nonzero.
Seven `Q*` pivots (coefficients only in nonzero rationals) resolve

```text
Hc_1_7=0, Hc_1_8=0, Hc_2_6=0, Hc_2_7=2*jet1,
Hc_3_6=0, Hc_4_5=jet1^2, Hc_5_4=0.
```

Neither `c` nor any branch parameter is eligible for a pivot. The five exact
normal forms are

```text
r60 = -jet2^2
r70 =  2*jet1^2*jet2
r71 = -2*jet2
r80 = -c-jet1^4+9*jet1*jet2^2
r81 =  2*jet1^2.
```

The face modulus `e0` is absent: it belongs to `q=p^23 U`, not to this
necessary `h3` leader. The two roots of `p` still encode the two 12-packets;
no equality of the packets, physical places, and flag coordinates is used.

As a robustness control, retaining the two major weight-42 equality directions
enlarges the `h3` chart from 7 to 9 coordinates. Nine rational pivots then
leave `-c-jet1^4` and `2*jet1^2`, whose `c!=0` localization is again the unit
ideal. The death does not depend on fixing those two equality coefficients.

### 5.2 Pole compiler (unreached)

The design leaders are

```text
t^9 h3 : t^8 p       t^36 h2 : t^32 p^4
t^108 F: t^96 p^12   t^72 G  : t^64 p^8.
```

For a lower monomial `x^i y^j`, `a+b+k=j`, the direct-pole support rule is

```text
e_F=12-i+j+b+2k <= 0, local power=e_F+96;
e_G= 8-i+j+b+2k <= 0, local power=e_G+64.
```

Exact enumeration gives 1,200 `F` tags over local powers 1..96 and 544 `G`
tags over 1..64. The six-row prior prefix is powers 1,2,3 for each object.
Stage `s` would add power `4+s`, displayed cleared exponents
`F:-92+s`, `G:-60+s`. At powers 96 and 64 the engine must subtract `p^12`
and `p^8`; merely inserting total coefficients would be wrong. These rows are
compiled metadata only, because §5.1 is already terminal.

### 5.3 Jacobian compiler (unreached)

With normalized `KF,KG`, the exact expression is

```text
108*KF*(KG)_w - t*(KF)_t*(KG)_w
-72*(KF)_w*KG + (KF)_w*t*(KG)_t.
```

The top degree `108+72-2=178` vanishes because the top forms are proportional
powers. The first possible band is degree 177 and `w`-power 15; an independent
generic coefficient check gives
`J[t^1,w^15]=1712*f0=(108-1)*16*f0`. The charged synchronization convention
is retained, and is explicitly not promoted to a theorem:

```text
prior:       degree 177, w powers 15..24 (10 rows)
stage 0:     degree 177, w power 25
stage 1:     degree 177, w powers 26..177
stage n>=2:  degree 178-n, all w powers 0..178-n.
```

At degree zero the target is `1`; positive-degree targets are zero. No row in
this schedule was imposed after the incidence unit was found.

## 6. Stage run and death certificate

The chart has 8,514 fixed-top coordinates and four declared branch parameters.
Before minor incidence,

```text
8514 + 4 - 607 major rank - 7710 outer rank = 201.
```

After the seven incidence pivots the formal pre-residue count is 194. This is
not a family dimension: the residual localization is empty.

| stage | new/reached family | outer rank | joint `Q*` new/cumulative | residue | exact dim | wall | RSS KiB | status |
|---:|---|---:|---:|---:|---:|---:|---:|---|
| 0 preflight | common-`h3` incidence; pole/J scheduled but unreached | 7710 | 7 / 8324 | 5 | **-1** | 2.03 s | 56,004 | **DEAD** |

Let `L=Zc*c-1`. The explicit characteristic-zero certificate is

```text
1 = -(9/2)*jet1*jet2*Zc*r71
    -(1/2)*jet1^2*Zc*r81
    -Zc*r80 - L.
```

Direct expansion in the driver is exactly `1`. The replay ring and generator
order are

```singular
ring R=0,(jet1,jet2,c,Zc),dp;
ideal I=r60,r70,r71,r80,r81,Zc*c-1;
ideal S=std(I);
```

Singular reports `dim(S)=-1`, `size(S)=1`, and `reduce(1,S)=0`. Its
`lift(I,ideal(1))` is

```text
[0, 0, -(9/2)jet1*jet2*Zc, -Zc, -(1/2)jet1^2*Zc, -1]^T,
```

identical to the displayed certificate. Controls give:

| ideal | `dim` | normal form of 1 | meaning |
|---|---:|---:|---|
| raw five residues, no wrapper | 1 (`Zc` free) | 1 | nonunit |
| `c, Zc*c-1` | -1 | 0 | expected empty localization |
| `c-1, Zc*c-1` | 2 (`jet1,jet2` free) | 1 | expected nonempty point control |

Thus the wrapper, coefficient field, generator order, and both positive and
negative controls are explicit. `sat()` is never called. Stage 1 was not
started, and no next-system manifest is valid after `DEAD`.

## 7. No-split alternative: Proposition 6.3 descent

On the alternative `delta* >= v3/u3=7/2`, Proposition 6.3 applies **directly**
for `u3=2`; Proposition 6.4 is neither needed nor applicable. Scaling by
`u3/d3=2/9` and deleting the old top level gives

```text
n'=24, m'=16, M'=(-16,18), d'=(24,8,2), V'=(7,2)
s'=2, v'_2=7, u'_2=1
(g',f',T'_2) pi-degrees = (24,16,14)
k=v3-u3-1=4
characteristic height n'-M'_2=6=k+2
denominator-shed jet height=7.
```

Moh's orientation gives `J(g',f')=-(2/b)Y^4`; after declaring a nonzero scalar
rescaling this is the charged target `J(f',g')=c*x^4`, `c!=0`. Proposition 6.3
also preserves the strict-below/at-level jet
`a0+a1 Y^2+a2 Y^4+a3 Y^6+Pi Y^7`; it does not license replacing it silently by
seven unrelated odd/even terms.

The requested charged two-point compiler is run as a conditional diagnostic:
`h=y^7(y-x)+sum h_j y^j`, the forced `A/B` coefficient
`beta=bp*A+bq*y+br*x+bs`, and the exact `d'=2,e'=3` h-adic high/low generators.
It is an enlargement in the lower `h` coefficients. A unit result is useful,
but promotion still requires a proved map from the denominator-shed
`K[Y,Pi]` pair into this ordinary total-degree chart. If that map is absent,
FALLACY-v2 requires `OPEN[DESCENT-SUPPORT/VARIABLE-MAP]`, not a transferred
Appendix-II kill.

The compiler has 13 coefficient unknowns
`h0,...,h7,bp,bq,br,bs,c`. H-adic monic-`y` normal-form extraction produces
26 exact Jacobian rows, split `18+8` over levels 0 and 1, and then adjoins
`T*c-1`. The raw ideal is nonunit of dimension 10. Exact-Q Singular returns
dimension `-1`, basis `1`, and normal form of `1` equal to zero after
localization; raw, empty, point, and lift controls all pass. The first raw row
is `-c`, so the unit lift is particularly transparent:

```text
1 = (-T)*(-c) - (T*c-1).
```

The foreground run took 4.59 s and 58,260 KiB RSS. Its typed outcomes are

```text
diagnostic: DEAD[CONDITIONAL-ENLARGED-CHARGED-COMPILER]
promoted:   OPEN[DESCENT-SUPPORT/VARIABLE-MAP]
```

This reports the requested `s'=2` compiler run without claiming that the
unproved ordinary-support map is supplied by Proposition 6.3.

## 8. Controls

### 8.1 `(64,48) -> (16,12)` Appendix II

The driver mechanically obtains `d=(64,16,4,2)`, `d_s=4`, `v_s=3`, `u_s=1`
and hence `(64,48)/4=(16,12)`. It builds both readings of Moh p.208: the
printed repeated `c5` and the compiler's absorbed-`alpha1`, distinct-`c6`
reading. Each has 17 coefficient variables and 77 coefficients of the
Jacobian equation. Exact-Q Singular with `Zc*c-1` returns dimension `-1` and
basis `1` for both. The raw ideal is nonunit; empty, point, and lift controls
all pass. Foreground wall time was 1:03.83 and peak RSS 520,888 KiB.

This reproduces the Appendix-II kill in characteristic zero, stronger than the
frozen modular calibration. The control's ordered Jacobian is the negative of
the main engine orientation; localization of the otherwise free nonzero scalar
makes the sign harmless and the convention is recorded.

### 8.2 Tame two-point automorphism

The positive plumbing control survives:

```text
F=a*x+y, G=(a-1)*x+y, J(F,G)=1,
x=F-G, y=a*G-(a-1)*F.
```

The two points `[1:-a:0]` and `[1:1-a:0]` are distinct, their coordinate
difference being `-1`. The shared linear ansatz produces only
`A-C-1`; the slice `(A,C)=(2,1)` gives `(2x+y,x+y)` with Jacobian one. All
symbolic inverse, factorization, determinant, incidence, and witness checks
pass. The special `D=108` tower rows are correctly marked not applicable to
degrees `(1,1)`.

## 9. FALLACY-v2 audit

- **Flag/place/series:** the major `D2/D1` maps, physical minor series, split
  order `delta=3`, combined radius `delta*`, and denominator shedding are all
  separately declared. Strict-below rows and at-level face rows have separate
  counts.
- **Charge/carrier/attainment:** no new exit-price assertion is made, so no
  `charge_basis` declaration is licensed. A screen survivor is not treated as
  an actual exit, and no lower bound is promoted to attainment.
- **Pole/interior:** pole and Jacobian schedules are unreachable metadata; the
  death uses only the necessary common-`h3` incidence, so no interior identity
  is invoked after a missing hypothesis.
- **Localization:** the ideal component is displayed in
  `Q[jet1,jet2,c,Zc]`, `c` is excluded from rational pivots, `Zc*c-1` is
  explicit, and raw/empty/point controls plus a unit lift are run.
- **Remainders and maps:** zero rows are counted; the correct scalar sign is
  checked; the five nonzero normal forms are displayed. `e0` is not mapped by
  name to an `F,G` coordinate. Prime marks in descended data are labels;
  `dU/dpi` is written explicitly when differentiation is meant.
- **Descent:** Proposition 6.3 is used only on the stated no-split premise.
  Proposition 6.4 is not substituted. The missing ordinary-support map is
  typed `OPEN`, rather than filled by a cap or analogy.
- **Merge/arrival:** no merge-free or arrival-index claim is made in this lane.

## 10. Verdict and uniformisation evidence

```text
split alternative:
  DEAD[D108/delta3/DECLARED-NECESSARY-JOINT-CHART]
  exact stage = 0 preflight
  killing family = common-h3 minor incidence
  certificate = localized unit ideal over Q

no-split alternative:
  conditional Prop-6.3 descent to (24,16; M2'=18,V2'=7;k=4)
  charged compiler diagnostic DEAD (26 rows, exact Q)
  promoted status OPEN[DESCENT-SUPPORT/VARIABLE-MAP]

overall D=108 row:
  no REPRESENTATIVE; no recomposed pair; no JC2 counterexample.
```

The second client does not uniformise by reusing only the late pole/Jacobian
schedule. At `(99,66)`, the charged branches first became unit at stage 4
(Jacobian row `6264`) and stage 8 (pole row `64`), as recorded in
`g9966-delta52-stage8-sol56-20260903.md:160-172`. Here the branch-specific
common-`h3` incidence kills before either family is reached. A uniform engine
therefore needs a parameterized **incidence compiler and exact Q-star reducer**
ahead of a shared continuation scheduler; the latter alone misses the row
family that decides this client.

## 11. Reproduction index

Primary boxed files:

```text
box/g108band-20260903/band_engine.py
box/g108band-20260903/outer_order_bands.py
box/g108band-20260903/controls.py
box/g108band-20260903/descent_engine.py
box/g108band-20260903/outer-bands.json
box/g108band-20260903/runs/skeleton/skeleton.json
box/g108band-20260903/runs/delta3/stage0/stage0.json
box/g108band-20260903/runs/delta3/stage0/death_certificate.json
box/g108band-20260903/runs/delta3/stage0/death_replay.sing
box/g108band-20260903/runs/delta3/stage0/death_replay.log
box/g108band-20260903/controls-run/controls.json
box/g108band-20260903/validation/validation.json
box/g108band-20260903/validation/artifact-manifest.sha256
```

The exact commands and external GNU-time logs are retained beside the JSON
outputs. The bounded validator passed all major/outer ranks, the incidence
membership identity, both Singular control suites, the conditional-descent
non-promotion gate, and source hashes. Its 23-entry artifact manifest has
SHA-256
`49f51d7a7a315d91a9c24e18c99004211fec678cd0e03c52f2aeca1e21c0a70d`;
a fresh `sha256sum -c` returned 23 `OK` lines. The independent stage-0
Singular replay is byte-identical to the engine replay, both with SHA-256
`c1245f8b7e10cff37b1dd32bfe19619574d3c4d9d84cd0d7ff84194506b2e431`.
The charge-basis validator reports `ABSENT`, as required because no exit-price
claim is made. A final process audit found no running `g108band-20260903`
process; unrelated workspace lanes were not touched.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `21854`.
- Body SHA-256:
  `a2ab67b56c2471dbb4f2a40c4b573bf5f0a6c83fdc287369ce63cbcb154cb548`.
- Frozen basis: `6a112f0cdd1e32801ea9ec692b189bef957b0e0e`.
