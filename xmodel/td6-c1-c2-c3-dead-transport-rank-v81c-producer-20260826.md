# TD6 V81C dead-transport rank — producer theorem

Verdict: **PRODUCER-EXACT; REVIEW PENDING**.

Over the generic symbolic-center field `E(C,V,U)`, the exact transport
compatibility map on the eleven licensed dead-stretch tangent axes has rank
nine.  Its exact kernel is `span(d10,d15)`.

The certificate is a nine-row lower-triangular original-source minor on
`('f','F0',m-20,0)` for
`m=6,7,8,9,11,12,13,14,16`; every diagonal entry is the same explicit
nonzero E-coordinate with coefficient `15625/3`.  The d10 and d15 generic
tables are empty.  All eleven source computations were independently run on
r6d and Box03; all 22 runs returned `rc=0`, all generic/point tables are byte
identical host-to-host, all 3,470 original pivot rows replay, and all V80B
specialization hashes agree.

Together with the separately hostile-reviewed V78 zero q-transport columns,
the complete 33-axis transport map has rank nine and kernel dimension 24:
the 22 q axes plus d10,d15.  This composition consumes V78 only for its
reviewed zero q columns; V81C itself explicitly does not recompute them.

Scope is strictly the generic fraction-field transport source-incidence
module at the fixed source-typed A3 slice.  Denominator-cleared divisor
coverage, first/previous/current equations, higher order, nonlinear family,
full TD6, SP-2, and JC2 remain open.

Frozen case:
`cases/td6_c1_c2_c3_dead_transport_rank_v81c_aws_20260826/`.
