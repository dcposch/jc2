# Promotion: D1 `a>=10`, `C`-contact source ceiling

Date: 2026-08-26

Status: **PROMOTED AFTER DIFFERENT-MODEL HOSTILE REVIEW AND PARENT
PROMOTION.**

## Frozen custody

```text
d0ef64c39189df96654df50726fd7b65a958e0eaea67ec2204456b7bc9bf3087
  xmodel/max12-812-order2-square-d1-age10-cge-a1-source-ceiling-corollary-20260826.md
7cdf4a707204bcd7fbbbea6e6b21aab687afef11b2a8aad7e33a864b01fac560
  xmodel/max12-812-order2-square-d1-age10-cge-a1-source-ceiling-corollary-20260826.sha256
f75d885da6b39af85127540fa8ede8a17d1f83a3a2d5279bb5c641f2bd98e15b
  xmodel/max12-812-order2-square-d1-age10-cge-a1-source-ceiling-corollary-hostile-review-grok-v2-20260826.md
d4aceedbead2d0c27546e143ae8bb2b820faa32dd08454c58650f280593d0fb2
  xmodel/max12-812-order2-square-d1-age10-j38-order3-promotion-20260826.md
```

The hostile review returned `CONFIRMED_CONDITIONAL`.  It independently
verified the closed specialization, every finite `C`-jet shift, the pole
ceiling, target behavior, and the stated fan coverage.  Its sole condition
was independent confirmation and promotion of the frozen parent producer;
the final SHA above satisfies that condition.  The earlier Claude delivery
failed from an API usage limit before producing a report and has no
mathematical verdict.

## Promoted corollary

In characteristic zero, after exactly the registered upstream square/D1
gates, the complete seven literal-Faber source equations have empty scheme
on `D(J)` throughout the closed source range

```text
A in sigma^a * k[[sigma]][z]_{<=1},
C in sigma^c * k[[sigma]][z]_{<=1},
R in sigma^r * k[[sigma]][z]_{<=1},
a>=10, c>=a+1, r>=a.
```

No displayed leading coefficient of `A`, `C`, or `R` is required to be
nonzero or is inverted.  This is a polynomial specialization of the
promoted grade-38 identity, not a density argument from exact contacts.
Writing `a=10+n`, `c=a+1+m`, and `r=a+s`, the substitutions

```text
theta=sigma^n, eta=sigma^s, Cbar=sigma^m*Chat,  n,m,s>=0
```

preserve the identity.  For `0<=m<=10`, the frozen coefficients through
`C_10` give the literal shifted jet range; at `m=11` the first
`C`-dependent family, `k6*C/L`, moves to grade 39 and vanishes modulo
`sigma^39`.  Raising `C` only delays families containing `C`, cannot reveal
a lower-grade source family, does not increase pole order, and moves the
first pole-four family from grade 43 to `43+m`.  The order-three odd-row
functional therefore still leaves only `-sigma^38*J/4`; adjoining
`iJ*J-1` gives the unit ideal before radicals.

## Exact fan consequence

On the normalized integral unit-load unique-`AC` fan, put

```text
d=c-a in {1,2,3}, s=r-a>=0, a+3*s>d.
```

This promotion closes every such point with `a>=10`.  In particular it
closes all three high-`a` `d=2` classes (`s=0`, `s=1`, and `s>=2`) and both
high-`a` `d=3` classes (`s=0` and `s>=1`).  Only the finite `a<=9`
threshold range remains in those five routed families.

## Firewall

This promotion is only the stated closed source ceiling on `D(J)` after the
same parent gates.  It makes no claim for `a<=9`, `c<a+1`, `r<a`, another
D1 fan face, positive-order leading `k10`, `p=0`, `k0=0`, an excluded
zero/infinity or terminal/Taylor chart, global source landing, the whole
square component, exact order two, `(8,12)`, maximum twelve, or JC2.
