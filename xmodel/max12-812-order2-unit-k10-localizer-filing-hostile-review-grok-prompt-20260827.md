You are Grok 4.6, a different-model hostile source/scope reviewer in the JC2
campaign.  Review exactly
`xmodel/max12-812-order2-unit-k10-localizer-filing-producer-sol-20260827.md`.

Independently inspect the controlling Gate-T obligation table, the literal
V17 emitter's `k10` series construction, the Fable5 staged-calculus review,
the promoted `T-rs` theorem, and the exact-square `k10=0` source review cited
by the producer.  Verify whether `k`, `k0`, and the constant term of `k10`
are the same coefficient; whether unit-`k10` is genuinely part of the named
Gate-T family's registration; whether `D(k)` is therefore scope-intrinsic;
and whether `k10=0` is one sibling load-timing family rather than two chart
residuals.  Attack the distinction between a formal-series unit, a
field-valued nonzero coefficient, and an algebraic localizer in a certificate.

Give per-claim `CONFIRMED`, `REFUTED`, or `GAP` and the smallest safe ledger
wording.  Do not infer that the sibling is closed.  No web, AWS mutation,
heavy local CAS, or `jc2-lean` access.  Your only repository edit is

`xmodel/max12-812-order2-unit-k10-localizer-filing-hostile-review-grok-20260827.md`.

Write the report, verify it exists, and state its SHA-256 in stdout.
