# Golden H-divisible M-source combined gate — preparation only

2026-09-08. READY FOR ROOT REVIEW, NOT LAUNCHED. Exact future output:
`xmodel/golden-hdiv-m-composition-gate-fable5-20260908.md`.
Prompt: `box/golden-hdiv-m-composition-gate-20260908/fable.prompt.md`.

Both freshly terminal source proofs were read WHOLE, after confirming
their exact supplied hashes and transactions. The coalesced producer's
custody/input pins are retained; the separated root producer's complete
transaction is retained without inventing a missing separate box custody.
Their sole mathematical parents are the same accepted source framework
and frozen15m excerpt. No pending simple-L or other theorem is charged.
There is no mathematical code in either new proof.

Ten unique charged snapshots comprise the scope note, two complete proofs,
their transactions, coalesced custody/input pins, whole canonical source
and its transaction, and exact15m excerpt. Original and snapshot SHA256
and byte counts are in `PINS.json`. No full canonical ledger, old checker
farm, live gate receipt or unrelated cross report is included.

## Decisive composition obligation

Both proofs define r using the actual full F Taylor coefficients in the
same canonical moving-M Morse chart: d=2 if C_M!=0, d=3 if LM divides C.
The referee must verify this identity of objects BEFORE treating
kappa>=2r and kappa<2r as complementary. Infinite xi order belongs to
coalesced only; r is finite by the actual nonzero c_d coefficient.

The prompt separately audits all implicit-series bounds, both field
embeddings, both signs, source constants/scalar kernels, lower-B Euler
degrees and target Jacobian factors. It specifically forbids transferring
the coalesced positive-order argument through separated denominators.
The separated proof must establish the GLOBAL earliest G minimum anew,
with possible G linear and constant terms retained at5eta. The final
field common-power arguments and the coalesced two leading coefficient
comparison are distinct obligations, not sample calculations.

Even confirmation excludes only the union of C_M!=0 and LM|C under the
literal hypotheses. M|C,L∤C remains outside; simple-L is neither premise
nor conclusion here. No whole golden/common/D125, nilpotent scheme,
reverse-lift, point, properness or JC2 claim follows from this packet.

## Custody and operational perimeter

The metadata preflight checks ten exact repeated charged_input keys using
the actual lane sed parser, unique basenames, literal placeholder and the
single output destination. It rehashes snapshots and originals, verifies
the two producer transactions and source transaction, and verifies this
prep report's own transaction. No lane is launched and no mathematical
function/control is executed. Root must separately consume/validate and
launch the bounded <=25-minute Fable review.

The accepted source and15m were read in the prior completed task and used
at unchanged pins; both new proofs were wholly read now. Only named
terminal metadata was accessed. No simple-L live body/log/receipt, web,
AWS/SSH, CAS, high powers, full source, protected tree, new agent or
canonical edit occurred. Optional future controls remain degree<=5/free
3/5 scalar only, with actual changed-object evidence if code is claimed.
All writers and metadata transaction children terminal after publication;
STOP/IDLE. This is prepared review input, not mathematical promotion.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `3446`.
- Body SHA-256:
  `4ee05f257da29cf1bae6b554ab6bdffc0ac7ce4587d54d145eb15b7126d0ff0e`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
