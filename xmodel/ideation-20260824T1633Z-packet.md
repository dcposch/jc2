# JC2 significant-news round packet — `20260824T1633Z`

Cutoff: `2026-08-24T16:33:03Z`  
Clean charged basis: `c327bdc8d02472feba42573760325099f34b8cdf`  
Round trigger: reviewed TD6 whole-pencil exclusion, reviewed AS depth-five
law falsification and pointwise Cartier stop, a direct fixed-map-degree AS
compiler, and a frozen provisional closure of the polynomial cube-core
trajectory problem.

This packet is immutable. Events after the cutoff belong to synthesis or a
follow-on round; do not silently update this snapshot. Ideation reports must
not read one another before freezing their own submissions.

## Canonical snapshot

Read every numbered avenue in `APPROACHES.md`, plus `AUDIT.md`, `PROGRESS.md`,
the newest `LIVE STATE` in `notes.md`, `COORDINATION.md`, and
`ladder/REDUCTION.md`. At cutoff their hashes were:

```text
ace8a0942c133173cfef42b87cbb119552dd08042240e5fdc5def963e424395f  APPROACHES.md
e283a318f67ea38e2980e5f109b295f38612e1a1c1b69c7c57735e7f6a64713a  AUDIT.md
2857d377a89c29fd303ed266686a39d411eb42419d956f35bf7066719032228e  PROGRESS.md
d2468dea07727147ac1a0c356523838b0e7080881735c8c279da69d6f4bcea3b  notes.md
b41f4ffca4a528038a1a226d46612b25067f5b777f270ad5781eca7327f30060  COORDINATION.md
f0fca49811349483c437d674ab819065eb09500330516d161978868c71bf6371  ladder/REDUCTION.md
```

## Reviewed delta since round `20260824T1358Z`

1. **TD6 whole licensed q2 pencil is empty.** Producer/review:
   `xmodel/td6-boundary-qb-pencil-gate-20260824.md`, SHA
   `5e1f6b44e143360a41550ae552b20bef02325414f96c5fd469ba4d016eb5eb22`;
   `xmodel/td6-boundary-qb-pencil-review-grok-20260824.md`, SHA
   `5c238f2bd3cf11422093184e1563f7671d0abd4b8f06a6fb5dfd7d380ac51319`.
   All 101 `E[B]` pivots are B-independent units; ranks are
   `3470/3602 -> 38/132 -> 38/94 -> 25/56`; ten current compatibilities
   include
   `N4=rho-(4720/29)B+(11364/145)B^2-(4096/145)B^3+16B^4` and
   `N13=((252-342S+144S^2-36S^3)/25)B`, with exact Bezout identity and gcd
   one. This excludes only `q_B=t+B*t^2+t^25` in the fixed normalized
   section, not centering, dead stretch, other jets, SP-2, landing, or JC2.
2. **AS depth-five simultaneous cap minimum is seven.** Producer/review:
   `xmodel/as-gauge-growth-p3-depth5-d7-minimum-20260824.md`, SHA
   `618a954ea683fd8444dafc4511c9aed708ea88459c524d747b815d48c6618f65`;
   review SHA
   `7006d82c4f6ff9943433bb4447bb95d3f94d6a76ea28ba1088bcc9aac774621f`.
   The exact minima at `p=3`, depths two through five, are `3,5,7,7`; the
   proposed law `(n-1)(p-1)+1` is false. A separate reviewed cap-eight point
   (producer/review SHAs `3cca09d0...dba6f` / `2c3f961a...751c7`) is a
   correct but nonminimal cyclotomic carry-cancellation motif.
3. **The frozen minimum residue is Cartier-terminal at depth six.**
   Producer/review SHAs
   `9bff27d0aed6294633c7e5726a7d1c0d7e0fb8a5629809790da783c62b41fea4` /
   `99f703a8c97afde81ff41065c1f69e34b7739d321f2020ce54949ea144adf567`.
   Its residual `(det J(A,B)-1)/243 mod 3` has nonzero `x^2y^2` coefficient,
   unchanged by every next digit because `U_x+V_y` has zero coefficient
   there in characteristic three. This kills one residue at every cap; it
   does not empty any depth-six system.
4. **Previously promoted proof delta retained.** The aligned
   nontrivial-Kummer `(6,9)` branch is empty, and the independent
   Faber--Laurent cube landing is exact. The target-translation erratum makes
   `kappa` gauge and `C=kappa^2+mu` invariant without changing the aligned
   exclusion. These reviewed facts may be used only at their recorded scopes.

Review-window correction v4, SHA
`727915b4ef5de6b726cbe9da5858f72bc1d80e973909d48b632a03cac180f834`,
changes metadata only and jointly brings the cumulative ledger to 40 reports.

## Frozen provisional proof delta

The exact producer
`xmodel/gcd3-69-cube-trajectory-kuranishi-20260824.md`, SHA
`069f6280332b44d93dcad17801dc7136d4a79fb06ace101c2dce8a4e746b5e7b`,
with case manifest SHA
`6391c23c79dac869a0a11442e5df2dfda496b51602e8796442c31e22721bb766`,
claims the following, **pending hostile different-model review**:

- conditional on the reviewed polynomial cube-core Faber--Laurent landing,
  both finite-pole alternatives `s in k*` and
  `s=C(x-a)^m, m>=2` are empty, separately for `d!=0` and `d=0`;
- every high-constant and first-target weighted stratum is reconstructed
  through exact original boundary rows, not killed from an initial ideal;
- the formerly missing mixed later-target fiber, with
  `mu=6rho3`, `nu=6rho4!=0`, localizes reversibly to
  `72V^2-3A^3-512mu=0` and
  `r5=A^2V/1024-256nu^2/(27A^3)`;
- for `mu!=0` this is genus one, so a rational coefficient path is constant;
  for `mu=0`, normalization gives
  `r5=(27/2)lambda^7-nu^2/(1458lambda^6)`, with two pole places, incompatible
  with either permitted one-pole terminal form;
- `nu=0` uses only the already reviewed universal two-sheet invariant-fibre
  decomposition.

Do not treat this as promoted, as arbitrary `(6,9)` emptiness, or as JC2.
Hostile review is active in the background.

## New provisional disproof/software delta

The simultaneous map/gauge cap is now recognized as a gauge-complexity
statistic, not the clean bounded-counterexample question. The live compiler
instead fixes only the actual map degree and writes, modulo 729,

```text
P=x-x^3+3U,       Q=y+3V,
div(U,V)-x^2+3((U_x-x^2)V_y-U_y V_x)=0 mod 243.
```

At `D=7` this direct F-only system has 70 effective coefficient variables,
91 exact coefficient rows, and 161 assertions after harmless target constants
are normalized, versus 201 gauge trits and 546 assertions in the earlier
fixed-gauge slice. A local Z3 discovery solve is active at cutoff. SAT must be
checked by independent integer expansion. UNSAT is not promotable without a
checkable carry Groebner/exhaustive/independent-core certificate. D7 and D8
are expected negative controls from the characteristic-zero bounded-degree
history; D9 is the first live correction-degree candidate. Canonical
Cartier/de Rham normal forms should quotient only cap-preserving exact or
Hamiltonian directions and must retain Frobenius and cap-boundary modes.

A separate frozen producer-only simultaneous-gauge slice
`a=0,b=x^5,n=6,D=7` is Z3-UNSAT (report SHA
`46b26bc9dd8438083950a1eac45ee35bf2b1d1abc710458918d6e9c3ba176027`),
but has no independent UNSAT certificate or hostile review and is not
promoted. It is mainly a compiler control after the F-only redesign.

## New provisional TD6 delta

For the three common-centering directions, exact differentiated transport
keeps all stage ranks fixed. The full current compatibility map
`E^3 -> E^10` has interim rank three and zero kernel; the two-dimensional
kernel seen at the single `t^4` residue was a projection artifact. The affine
tangent correction is inconsistent and all rank-change flags are zero. This
is not a family kill. A replay with explicit minors is being frozen.

The smallest nonlinear successor is the exact `c1` pencil. Its generic
transport over `Q(c1)` has rank 3470 with pivot factors only `c1` and `c1-3`;
exact rebuilds at both values retain rank 3470 and zero transport
compatibilities. The affine response has been pulled back exactly to the
132-dimensional kernel over `E(c1)` and the later staged elimination is
running. Treat `c1=0,3` as retained schedule strata, not kills.

## Campaign-wide questions for this round

Scan all 46 avenues, not merely the three active roots. In addition to the
standard contract below, explicitly ask:

1. If hostile review confirms the cube-core trajectory closure, what exact
   coverage theorem or smallest missing branch would be needed to turn the
   aligned-plus-cube work into an exclusion of all `(6,9),3|H` pairs? Can the
   coverage be falsified by an explicit leading/common-core stratum?
2. Can the F-only `p=3` tower be converted from nonlinear solver discovery to
   a finite, independently checkable obstruction/certificate recursion? Is
   there a better prime, special fiber, degree, or normal form that attacks
   a genuine polynomial counterexample rather than gauge complexity?
3. Can TD6 centering/dead-stretch/boundary moduli be handled simultaneously
   via a Fitting/rank-stratum ideal or moduli obstruction section rather than
   serial pencils? What is the cheapest honest coverage test?
4. What genuinely new proof, counterexample, or cross-avenue mechanism has
   not appeared in the repository? Search history before labeling it new.

## Active allocation and debt at cutoff

- Internal research: direct F-only `p=3,D=7,n=6`; TD6 centering tangent and
  exact `c1` pencil. The cube producer is frozen and available for a blind
  whole-portfolio scan.
- External review: Grok hostile review of the cube trajectory closure.
- Review debt: cube closure; TD6 centering package once frozen; the fixed
  simultaneous-gauge UNSAT slice remains intentionally unpromoted unless it
  becomes load-bearing.
- AWS: box01's protected legacy `build_tails43.py` core is untouched;
  boxes02/03 are stopped. No packet target yet licenses expansion.
- Broad web sweep #9 is due by `2026-08-24T21:25Z`; credible external claims
  remain immediate triggers.

## Required blind submission contract

Give: (1) a compact `unchanged/raise/lower/reopen` disposition for all 46
numbered avenues, explaining every change; (2) reranked principal proof and
disproof bottlenecks; (3) at least one genuinely new mechanism and one new
cross-avenue connection; (4) strongest proof attack and strongest
counterexample/falsification attack; (5) one software accelerator or decisive
experiment; (6) no more than three detailed idea cards, each with explicit
dependencies, cheapest discriminator, interpretation of every outcome, stop
condition, and expected information gain; and (7) `continue/redesign/stop`
for every current major lane. Mark speculation, run the canonical
history/priority checksum, and do not overread provisional facts. Do not read
other ideation submissions before freezing your own report.
