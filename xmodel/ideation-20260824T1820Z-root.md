# Blind whole-portfolio ideation — root submission `20260824T1820Z`

Basis: sealed packet SHA-256
`12d50a31630f8dfb2664ce8953e371832457cd9faa7f0c13480a32e1e0c4379e`.
I read the packet, all 46 canonical avenues, current correction ledgers, and
local history before reading any peer submission from this round. Statements
below distinguish reviewed facts from producer-exact or speculative inputs.

## 1. Portfolio verdict and bottlenecks

The reviewed maximum-11 theorem changes the campaign's best bounded proof
target from an open-ended degree farm into exactly two maximum-12 cells. The
fastest present route is therefore:

1. close the `(9,12)` order-three fibre by exact approximate-cubic and
   spectral methods, retaining all invariant loads and Taylor boundaries;
2. transfer only mechanisms, not conclusions, to the `(8,12)` order-four and
   order-two leaves and to both polynomial-core leaves;
3. compose those cells fail-closed with the reviewed degree recursion.

The strongest global proof bottleneck remains unchanged: no bounded
partial-degree theorem supplies a universal landing or cofinal degree bound.
`G2-PSC`, `G2-BD`, and a full TD-sheet configuration theorem remain separate
obligations. The strongest local proof backup is the TD6 `(c1,c3)` atlas; its
generic `c1` open is provisionally empty, but every pivot divisor must still
be rebuilt.

The strongest disproof lane remains the fixed-map-degree Artin--Schreier/Witt
compiler. Its carry correction raises trust while lowering the value of any
opaque solver output. The actual bottleneck is a proof-carrying accepted-digit
recursion that either produces a bounded-support all-depth branch or proves
that support/degree must escape. The formal-germ and new quasi-polynomial
routes share the same algebraization problem in different coordinates.

Reranked bottlenecks:

- **Proof P1:** exhaustive maximum-12 fibre and boundary coverage.
- **Proof P2:** a universal landing/cofinality theorem independent of any
  finite partial-degree frontier.
- **Proof P3:** dependency-complete TD6 exceptional-stratum coverage.
- **Disproof D1:** a bounded polynomial, rather than growing-support analytic,
  all-Witt branch.
- **Disproof D2:** polynomial recovery from a reduced quasi-polynomial Keller
  object or a proof that such recovery is impossible.
- **Disproof D3:** algebraization of a certified all-depth formal germ.

## 2. New mechanism and new connection

### Spectral-norm deficit

The polynomial

```text
W=g^3-f^4-3*k*f^2*g-k^3*f^2
```

is the norm of `g-f*t-k*t^2` in the cubic algebra `t^3=f`. This turns the
Faber tail into a low-degree algebraic object: the first invariant loads occur
at `z`-degrees `21,18,16`, and the Keller bracket becomes
`J(f,W)=3(g^2-kf^2)J(f,g)`. This is more than a repackaging of the five Laurent
rows: it exposes factorization, resultants, polynomial-abc deficit, and a
Hurwitz passport on the same object. I found no equivalent spectral norm in
the repository history. Label: **NEW, PRODUCER-DERIVED / UNREVIEWED**.

At `k=mu=nu=0`, coprime `(f^4,g^3)` attains the exact polynomial-abc lower
bound. Equality pairs are governed by weighted plane trees/Davenport--Zannier
data. The new cross-avenue connection is therefore

```text
partial-y Faber tails -> cubic norm W -> polynomial-abc equality
-> finite dessins/passports -> differentiated coefficient trajectory
-> original Keller/Taylor boundary equations.
```

This raises avenue 25 only for this named coupled client; the earlier 169
uncoupled passports remain non-obstructive. It also gives avenue 29 a concrete
nonconstant receiver through `J(f,W)`, though no universal Hamiltonian theorem
is claimed.

### Two algebraization failures in common

Pinchuk's reduced quasi-polynomial objects and the growing-support AS/Witt
limit are both exact-Jacobian objects outside `C[x,y]`. One escapes through
rational/negative exponents and the other through unbounded support. Their
common missing datum is a finite pole/conductor cancellation certificate.
Comparing the quasi-elementary denominator divisor with the AS exterior polar
divisor and `A_infinity` is a **NEW-COMPOSITE** cross-avenue test. A theorem
that either divisor is monotone under the licensed transformations would
simultaneously obstruct two counterexample constructions; a cancellation
example would improve both.

## 3. Compact 46-avenue disposition

`U` means unchanged. Explanations follow every non-`U` entry.

```text
 1 R   2 R   3 U   4 U   5 U   6 U   7 U   8 U   9 U  10 U
11 L  12 U  13 U  14 U  15 U  16 U  17 U  18 U  19 R  20 U
21 U  22 U  23 U  24 U  25 R  26 U  27 U  28 U  29 R  30 U
31 U  32 U  33 U  34 U  35 U  36 U  37 U  38 U  39 U  40 U
41 L  42 U  43 R  44 L  45 U  46 R
```

- **1 raise narrowly:** Pinchuk's claimed reduced form and the maximum-12
  spectral norm give Newton/q-polynomial work a new exact client. Do not
  restart the generic degree farm.
- **2 raise execution, not scope:** the dependency-complete TD6 atlas has a
  provisional generic-open exclusion and a finite exceptional workload.
- **11 lower/stop:** refuted stronger ladders remain closed; the new norm is
  polynomial abc, not a Mathieu revival.
- **19 raise:** the integer-quotient correction plus exact nonreduced cover
  makes the next accepted-digit transition more trustworthy and decisive.
- **25 raise for one named client:** generalized Davenport--Zannier equality
  couples both coordinates and the exact residual degree, unlike the failed
  free passport census.
- **29 raise modestly:** `J(f,W)=3(g^2-kf^2)J(f,g)` supplies a concrete
  spectral receiver. It is not constant and does not prove LND-ness.
- **41 lower/stop:** no new evidence rehabilitates naive scaling.
- **43 raise:** approximate-cubic coordinates and the cubic norm make
  composition/factor strata load-bearing at maximum 12 rather than a generic
  Ritt slogan.
- **44 lower/closed:** retain only the repaired membership theorem; the new
  prime/degree work does not repair the refuted proof.
- **46 raise as infrastructure only:** exact norm/passport, quotient, and
  exceptional-stratum certificates are unusually good formalization clients;
  formalization is still not a discovery proof by itself.

All other avenues stay at their canonical rank and scope. In particular the
terminal ODE positive control prevents raising avenue 3 on ODE rigidity alone;
the Pinchuk author/source does not reopen avenue 24; and a Kuranishi initial
form does not by itself raise tropical avenue 38.

## 4. Strongest attacks

**Proof attack.** On the `(9,12)` order-three leaf, classify the exact fibres
of `(r1,...,r7)` in approximate-cubic coordinates. Use the spectral norm to
replace the `k=mu=nu=0` global Gröbner branch by polynomial-abc equality
passports. For each normalized equality template, differentiate its affine
and scaling orbit over `k(x)`, impose `J(f,g)=j/u`, and finally reconstruct all
Taylor polynomial boundaries. Treat `k!=0`, nonzero `mu,nu`, the cubic
discriminant, components disjoint from the common locus, and the order-one
core as separate mandatory branches.

**Counterexample/falsification attack.** Continue the corrected D7 AS
accepted-digit system from the original nonreduced ideal. Search for an
explicit accepted second digit and then compute its exact next integer
quotient. A surviving component is valuable only if the map-degree cap stays
fixed; a terminal component measures the support-growth law. In parallel,
try the inverse direction of Pinchuk's reduced-form construction on one
generalized Davenport--Zannier q-map, with denominator/pole growth recorded
at every quasi-elementary step. Failure to polynomialize is still useful as
a conductor monotonicity control, not a counterexample.

**Software accelerator.** Build a small exact `spectral_deficit` compiler:
given a Faber polynomial `H`, eliminate its root parameter, emit the norm
relation, its tail-degree filtration, bracket identity, gcd strata,
abc deficit, and all admissible weighted passports. Quotient affine/scaling
orbits before Gröbner decomposition and emit a certificate for each retained
passport. This should decide in minutes whether the DZ route is finite and
smaller than the current coefficient ideal.

## 5. Detailed idea cards

### Card A — `MAX12-DZ-SPECTRAL`

- **Dependencies:** reviewed maximum-11 theorem; hostile confirmation of the
  shared Faber landing; independent verification of the spectral norm; the
  primary polynomial-abc/DZ theorem at exact `(4,3),d=3` scope.
- **Cheapest discriminator:** enumerate every weighted bicolored tree/passport
  compatible with monic degree-9 `f`, degree-12 `g`, coprimality, and
  `deg(g^3-f^4)=16`; solve only normalized representatives and orbit tangent
  directions.
- **Outcomes:** no passports contradicts the stratum immediately; finitely
  many templates turn it into exact ODE/boundary checks; a positive-dimensional
  Hurwitz family falsifies the rigidity shortcut and sends the branch back to
  component algebra; common factors route to the common-cubic exclusion.
- **Stop:** stop the passport route if factorization data do not cut the
  Hurwitz space below the existing fibre width, or if a normalized family has
  genuine positive moduli after exact checking.
- **Information gain:** very high; it can close the hardest `k=0` boundary or
  falsify the proposed bypass cheaply.

### Card B — `PINCHUK-QRED-AUDIT`

- **Dependencies:** Pinchuk 2021 primary text and its cited Newton-polygon
  inputs; no campaign theorem is consumed initially.
- **Cheapest discriminator:** line-audit Theorem 3.4's six-case polygon update,
  identify a well-founded termination measure including rational exponents,
  and replay the transformations on two tame automorphisms plus the paper's
  explicit homogeneous q-Keller example. Check every direction of the paper's
  notion of equivalence and whether polynomial invertibility can be recovered.
- **Outcomes:** a sound reduction creates a new global target class and a
  concrete polynomialization disproof lane; a missing termination or fidelity
  arrow quarantines the global claim while preserving elementary Theorem 4.1;
  an explicit reverse polynomialization would be a counterexample candidate
  requiring direct Jacobian and collision checks.
- **Stop:** four focused audit hours or the first irreparable arrow; do not
  expand into an unbounded quasi-polynomial census without a fidelity theorem.
- **Information gain:** high because it tests a previously absent global
  reduction with very low compute cost.

### Card C — `AS-D10-INTEGER-QUOTIENT`

- **Dependencies:** hostile confirmation of the corrected 30-row D7 ideal and
  exact nonreduced cover; original predecessor ideal retained.
- **Cheapest discriminator:** solve the affine top-digit divergence equation
  and degree-10 mixed-carry rows localization-by-localization, but replay every
  candidate against the intersection and derive the following integer
  quotient before reducing modulo three.
- **Outcomes:** all pieces terminal gives a scoped D7 obstruction; survivors
  produce proof-carrying next nodes; disagreement across the intersection
  exposes a localization error; quotient nonintegrality is `TYPE-FAIL`.
- **Stop:** do not launch a monolithic 56-variable global Gröbner basis; stop
  and redesign after two non-informative decompositions or loss of the
  nonreduced remainder.
- **Information gain:** high on the strongest counterexample lane and directly
  tests whether fixed-degree survival is shrinking or stabilizing.

## 6. Continue / redesign / stop

- **Maximum-12:** continue at highest priority; redesign around
  approximate-cubic and spectral coordinates, with raw Gröbner as a check.
- **AS/Witt:** continue nonblockingly after carry review; enforce the exact
  quotient contract and retain the original nonreduced scheme.
- **TD6:** continue the `(c1,c3)` dual and exact exceptional pivots; stop new
  unrelated serial pencils and make denominator-cleared certificates the
  completion criterion.
- **Pinchuk q-reduction:** start a bounded hostile source audit, not a broad
  construction campaign.
- **AWS:** keep stopped until a certified component or passport workload has a
  measured memory requirement that exceeds local capacity.
- **Generic degree farms, opaque SAT/UNSAT, Mathieu, naive scaling, and the
  refuted prime-td proof:** stop or remain closed.
