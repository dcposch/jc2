# Task: scope the td-11 / td-13 refile + tower port (the next rungs)

You are GPT 5.6 Sol, primary research scoping, repo
/Users/dc/code/math/jc72108 (full read access, python3 allowed).

The td-7 panel just CLOSED: TOWER-UNIFORM.md (read the theorem + lemma
structure) and the AUDIT.md "TD-7 PANEL CLOSURE" entry. The sheet
ladder's next rungs are the td=11 and td=13 off-axis books. Their old
cell counts (12 / 129) were VOIDED as alphabet artifacts (BOOK-OFFAXIS
section 10 P5). Any refile must use: the E5-corrected pin discipline
(kbar_E5 + the promoted n>=1 filter law E5F), the Q+E5 conventions
(H5a resolved — AUDIT), and the priced closure machinery (px5).

## Scope the campaign, four sections

1. REFILE DESIGN: what exactly must be re-enumerated for td-11/13
   (entry census -> chains -> merge cells -> arrivals under E5F ->
   lambda-budget). Which existing engines generalize (book_offaxis.py,
   cases/td7_census_e5.py, px2/px5) and what is genuinely new
   (multi-orbit / mixed / nested merges — see xmodel/sol-probability.md
   branch 7' for your own earlier caution). Estimated census sizes.

2. TOWER PORT: which TOWER-UNIFORM lemmas are td-agnostic AS PROVED
   (L-A? AM? N1-N4? E5F?) vs td-7-specific — especially the entry
   UNIQUENESS that made the clash apparatus panel-constant. td-11 has
   3 L6-surviving entries, td-13 has 6 (BOOK-OFFAXIS section 1a). Does
   the level-1 clash argument survive per-entry? State the port
   obligations precisely.

3. CLASH-WINDOW ARITHMETIC NOW: for the td-11/13 L6-surviving entries,
   do the td-7-rollout analogue in exact fractions — chain-1 freeze
   status per entry, gap(X) ranges, chain-2 menu gaps. The td-7
   arithmetic predicted every verdict; if the windows are empty again
   here, say so LOUDLY (that would mean the ladder falls to the same
   theorem shape). If some entry has a nonempty window, identify it
   precisely — that entry is the next hard object.

4. EXECUTION ESTIMATE: rounds to closure per rung; the single biggest
   risk.

## Deliverable
xmodel/sol-td11-13-scope.md: 5-line exec summary, then the four
sections. Label anything unproven CONJECTURE. No repo modifications
except your output file. No git.
