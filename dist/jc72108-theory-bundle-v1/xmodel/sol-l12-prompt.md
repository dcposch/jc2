# Task: the Q2-l12 / J-window intersection map (your G* lane 3)

You are GPT 5.6 Sol, repo /Users/dc/code/math/jc72108 (full access,
python3, exact work only — no msolve). Execute the intersection-map
lane from your own instrument plan (xmodel/sol-instrument.md §3) and
your earlier branch-8' idea (xmodel/sol-probability.md).

Context: Q2 and the J-window are two independent necessary row
families on the SAME residue-A coefficients. The l13 stratum is
proof-tier EMPTY; l12 adds the low triple (uf24, bg42_24, bg21_24) +
six level-60 highs (your l12 3-chart design, SHEET6-R1.md §19.5a).
The J-side survivor is the §6.V locus V (47 band conditions +
slot-20, now being compressed). Nobody has ever intersected them.

Task: (1) build the VARIABLE MAP between the Q2-l12 chart coordinates
and the J-window registry names (both parameterize residue-A
coefficient data; the correspondence runs through the template
coefficient towers — SHEET6-TEMPLATE.md + the r1_experiment orbit
model; document every identification with file/line and machine-check
the overlap on the banked pickles where both sides pin the same
object); (2) determine which J-window conditions RESTRICT to the
l12 charts (a condition transports iff its support lands in mapped
variables — count them per chart); (3) the PAYOFF assessment: does
the intersection strictly shrink either side (a J-condition that
kills an l12 chart, or an l12 constraint that cuts V)? Even ONE
transported kill would be the first cross-family reduction of the
program. If the maps do not overlap enough to transport anything,
say so — that is a real (dis)connection finding worth banking.

Deliverable: xmodel/sol-l12map.md + any check script as
cases/q2j_map.py. Exec summary 5 lines. No repo modifications except
those two files. No git.
