# Own static review before publication

Completed manual source review, not an independent gate or execution. The original authority accepts a registered operation string; `mutate` remains subject to all unchanged job/physical/argv/code/input/output/clock gates. Fraction is imported and coefficient strings parsed only after successful authorize. Canonical exact strings and prescribed mutations preserve the fixed baseline's remaining object fields; original mathematical bytes were not read during preparation.

The old dispatcher is preserved except the literal DISPATCH.diff: new job/schema/role and fourteen-call inventory, fixed baseline and receipt bindings, four negative checker calls, the direct accepted role-path bindings, and the durable-parent fsync. It retains immutable prospective authority hashes before launch, startup flags, source/native pre/post pins, actual cgroup quiet predicates, CAPRUN telemetry checks, exact dummy events/signals and original absolute/admission/aggregate accounting. There is no replacement CAPRUN, supervisor, checker or authority.

Manual reason tracing against the unchanged checker: U constant+1 fails its positive V cancellation; R constant+1 fails full cofactor identity; j4-to5 fails the maximal excluded-edge-factor comparison; interior Bernstein[1][1]+1 fails full Bernstein expansion. All four must reach normal exit2 with the exact recorded stderr, empty stdout and no checker receipt. Early pin/schema errors and resource caps cannot count as coverage. These are intended predicates only, not observed evidence.

The mutator receipt is metadata, not a proof. The dispatcher checks exactly four changed hashes, ROOT-freezes all four before any negative checker invocation, then separately binds each original checker call to that changed input. The original positive receipt is hash/code-bound; no positive mathematical rerun occurs. Nine preflights include two wrong-job startup refusals, so fourteen total CAPRUN calls are possible, with exactly five scientific children.

Pending quantities: two. (1) Zero FIRST reviews of these new mutator/dispatcher bytes: cheapest next action one whole-source independent static gate. (2) Zero generated fixtures or actual semantic checks: cheapest next action, only after review and separate ROOT authority, one fixed bounded batch. No additional canonical OPEN ID is raised; no assertion of runtime readiness or promotion is made. There is no known unfinished source placeholder, but all runtime claims remain pending.
