# Exact read scope

First action 2026-09-10 10:42:10 UTC. Hard stop 11:04:00 UTC; publication reserve 11:01:00. Targets were absent before the transaction began. PINS.json records the card plus exactly ten permitted inputs, all checked before charged body access at 10:42:57 and checked again before publication.

Current WHOLE reads: ROOT-CARD.md; terminal runtime gate 0562b82a; the 680-byte check.payload positive receipt, metadata only. Explicit same-byte prior WHOLE reuse after fresh pins: old dispatch.py, probe.py, producer.py, checker.py, authority.py, VALIDATION.md, code gate d3b8c092 and CAPRUN 4435279d. The copied source bodies were additionally read as documentary text; no source was imported or executed. All four copied science/probe files compare byte-for-byte with their pinned originals.

Own WHOLE text review covers mutator.py, dispatch.py, CONTRACT.md, disabled registration, this read scope, OWN-REVIEW.md and the substantive report before its completion marker. DISPATCH.diff was generated only by textual difflib comparison. PINS and custody use metadata hashing only. There was no AST/syntax/compile/test or arithmetic subprocess.

NOT READ: the original 854264-byte coefficient candidate, its U/R/Bernstein bodies, any live report or artifact, any uncharged provenance report, corpus, protected project, process state or worker. The baseline candidate hash and size are binding metadata from the card/receipt, not a whole-body read claim. The science-report names retained in authority code are future literal registration dependencies, not additional bodies consumed here.

Only own tagged files and the transaction partial/final are written, using apply_patch. Existing metadata publication tooling is used only for the begin/close/finalize/verify transaction. No ROOT acceptance, enabled registration, fixture, scientific output, worker operation or execution authority is issued.
