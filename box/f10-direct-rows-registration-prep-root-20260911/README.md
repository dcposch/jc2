# Offline direct-row registration packet — DISABLED

ROOT first action2026-09-11 04:21:32UTC; original reserve04:37/HARD04:40.
This separate packet composes metadata for the accepted source/runtime.
It does not edit the frozen deployment or refusal-derivation boxes. No source,
interpreter, import, AST, syntax/compile, test, dummy or scientific execution;
no AWS allocation. ROOT-READY-INPUTS.sha256 remains ABSENT in this and the
preceding deployment box. The disabled JSON is not launch authority.

## Contents and checked derivation

ROOT-REGISTRATION.template.json binds the final five science hashes, three
wrapper hashes, CAPRUN, source-contract feef0643..., fourteen absolute
source-reference pins, fixed source/batch caps, all thirteen literal command
vectors and exact phase/typed-slot policy. Documentary reference paths point
to coordinator copies and are not runtime files or science siblings.
Both FIRST hashes are recorded with the prescribed source power correction
bound by the source contract. Review clearance is not runtime observation.

AUTHORITIES.template.json records all thirteen derived objects: ten exact
eleven-key science authorities and three distinct nine-key ROOT policies.
ROOT compared these against dispatch.py authorize/run/batch text and performed
metadata-only structural equality checks, including all command prefixes and
source/helper suffixes. Each actual written JSON was parsed only as metadata
and compared with the composed metadata, not imported or executed as source.

The five producer refusals mutate exactly: status DISABLED; source CPU600->599;
delete files.check.py; zero files.produce.py; or supply a zero authority digest.
The last does not mutate the authority object. Startup entries use WRONG-JOB;
startup-check input SHA is sixty-four zeros. Caps and inventory share the same
source refusal branch/bytes, but have different metadata mutations.

Valid/dummy/mutate policy files are exact compact recursively ASCII-key-sorted
JSON plus LF, matching dispatcher emission. Valid and dummy digests are already
literal in the commands:
valid2552b6a534c60e688f185cde367a7efcee504dc45f3d5de43cf60d22048ac279;
dummy7324f69d4e5e83b8f31d49dfa55ea2b543b2339ae2dfdc347b4340d82e118f04.
They bind ROOT PATH, not its hash, avoiding a self-cycle. The unused mutate
policy is also recorded, and its artifact is the FROZEN BASELINE because
batch() calls run(mutate,...,target). ROOT caught/corrected an initial writer
path during static comparison before packet closure. These are reference
bytes only: do NOT populate the future authority directory in advance.
The dispatcher requires fresh empty children and emits the runtime objects.

Only check-positive/check-negative use AUTHORIZATION_SHA256 at the named flag;
only mutate uses ROOT_REGISTRATION_SHA256. Checker artifact digest placeholders
refer to the actual future frozen baseline/fixture, never an invented hash.
Producer artifact SHA remains literal UNFORMED as required by its API.
No ROOT self-pin or execution-card hash cycle is introduced.

## Explicit remaining finalization

Enabled and exclusive_no_concurrent_writer both stay false. Actual instance,
host, boot, namespace, durable device, native manifest/subset hashes, Python/
setpriv hashes and four original deadlines remain placeholders. CPU origin0
is valid only for the future independently observed FRESH-ABSENT cgroup;
it is not a reset of an existing run. UID/GID65534 and profiles are JSON
metadata integers; all mathematical values remain decimal strings.

The p523, degree1, phi[0,1], P7_mod_p vector is the untrusted manual input from
the pinned p523 candidate note. No field/admissibility or ten remaining unit
checks have been executed. Both actual evaluators must reconstruct and check
the source; no admissible place or successful rank attachment is claimed.

Before release, collect the separate Python compatibility documentary task;
decide whether its selected distribution/build justifies the seven derived
refusal triples. In a NEW final preflight-policy copy, substitute only the
accepted arithmetic acd07f800707c379aba60bd06f239d38cd83c86867729561367abd2bcc04ce73
hash for UNFORMED, keep exact source-derived bytes, and pin the final policy.
Future worker/native measurements must match that qualification; do not
calibrate refusal bytes from observed failures or admit alternate errors.

Finish all worker-independent source/native-metadata recipe and release-card
preparation before scoped fresh AWS facts/allocation. Immediately arm the
exact returned worker's original retirement deadline; collect physical/native
facts, produce final manifests, fill only identified metadata placeholders,
derive/hash compact nonchecker science authorities, and fill their literal
command digests. Keep the two checker typed slots and mutator ROOT slot.
Record all original setup/admission/math/custody/worker clocks and final pins.
Only then may ROOT issue a separate enabled immutable registration and actual
outer launch with inherited TERM/KILL/cgroup/custody machinery. No extra toy,
smoke, replay, new supervisor, optional mutator rewrite, retry or capraise.

## Read scope and evidence limits

ROOT's final five JSON documents were read WHOLE (registration815lines,
authorities709lines and three one-line policies), with edited lines rechecked.
CONTRACT was freshly WHOLE; dispatch selected metadata/batch ranges were
freshly read after its earlier accepted WHOLE collection. Other accepted
source/design/review inputs use prior-WHOLE/current-pin reuse. The p523 note
was freshly WHOLE at04:21. Twenty-five current input hashes were rechecked
04:31:42. PINS records final bytes, not an independent source review.

No actual baseline, source outcome, speedup, rank, exclusion, all-r theorem
or JC2 resolution follows. Original caps, evidence barriers and cadence stand.

