# Disabled collector attachment — two native roots only

ROOT, September12,2026,17:43UTC. This is inert package metadata, not a
collector execution, enabled release or new live queue.

The launcher FIRST intakee401fd9f017196a8c711b2bf4198b1f0b8b2ff38c8246abe9d34d0c04e83068e
identifies P-TOOLS: ordinary0755 env/systemctl entries in registration.pins
must also be native files to pass the unchanged bootstrap's file-mode rule.

New native-metadata.template.sh in this directory,
SHA601adf816495d9d4c7f72626b0d5ab170bf736b840a4a6d8f19c6e73ed5ca49c,
is copied from the frozen deployment-prep collectorb386a6cd2d732e6618b994fdbdec67d8e2a59fc997206f296b4cf55513684ed0.
ROOT read the new source WHOLE and diffed both exact files: ONLY the root
vector line changes, appending /usr/bin/env and /usr/bin/systemctl. Both use
the unchanged file/alias/dependency visitor. Original files, header grammar,
file/directory caps, expected Python path, absent-zip handling and exact
instance-placeholder refusal are unchanged. Source was mechanically copied
through apply_patch after exact parent footer/vector checks; no script ran.

The assembler44bd5d3b18d5552d811dd42071b8d54a6096e42f5ceac939e04cd1bd1ae70e0e
already admits additional records and is unchanged. Its required-root list
is the original collector list; it does NOT independently assert these two
additional paths exist. Successful qualified execution of the new collector,
exact output custody, ROOT package binding and bootstrap's current pin/mode
checks must together establish the P-TOOLS attachment. Metadata syntax alone
never supplies it. The old assembler report's source-writing claim remains
at its literal scope; this later collector delta is not retroactively in it.

No claim of full admin/transport/native closure, dynamic plugin discovery,
fresh version, loaded-unit semantics, duration, actual observation or launch
authority. Fable FIRST of this exact new collector and assembler is selected
before use; no actual native binary permissions were changed.
