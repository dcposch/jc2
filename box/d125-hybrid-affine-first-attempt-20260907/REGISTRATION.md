# Hybrid81 first attempt: exact prospective owner registration

2026-09-07. NO execution authority is issued. This supersedes no sealed artifact.
Root has accepted the cooperative exact host-control dispatch resolution of
GAP-AUTH, explicitly not production-inert token isolation. Sol code gate
83bd439aa52af686514dcdd6aa4fa400d099abccc8c5d5b5967b208777e47fb4 is accepted;
root harvest d20acd2ab0366be2873a944eda1e6f6118d471c3f567dd1e465611b385447e26.

Frozen underlying readiness registration:
box/d125-hybrid-pilot-readiness-20260907/REGISTRATION.md,
SHA3b04131bc72c3ea352a865144ac152119f8b070e89709249ef91623e0b3d8b9f.
Its exact client, resource, stdout/stderr, group cleanup, output absence,
EBS-preservation and no-solver boundaries remain binding. This owner packet supplies
the now accepted code gate and dynamic-dispatch plan. The worker REGISTRATION.md
will be THIS file, bound by its sealed SHA in each fresh root-authorized packet.

Only i-0da0cebfc97c9fd54 / retained vol-0eb6450d18ffa89f1, new worker directory
/home/ubuntu/d125-hybrid-affine-pilot-20260907. Root alone STARTs/STOPs. NEVER
terminate/delete, touch another worker or overwrite prior source/output bytes.
Before root RUNNING confirmation: read-only AWS inventory only, no SSH. After that
confirmation: exact read-only DMI/boot/resources/process/EBS/executable/new-path and
source-stat custody snapshot; no source content/hash read in this first preflight.
No source row parse, production metadata or arithmetic.
Unexpected owner/process/path/pin is a blocker, not permission to change anything.

Frozen payload: construct.py aa65e7104d3fcf5ad868eea952587a6841d784c6f9469c6c42ea2a02913ef264;
replay.py06475a2a566b4ed617068d65f3a0473ef83c16e2cb357dcb959ee46716781c23;
baseline.pyec2fa2d1e23bc16bc07c9208f648e3b17561cbfddaaf04fed28d7ffc9a428d53;
CAPRUN4435279df8f987c667ebc2fae8e4bd987916032561cb3faf1e07a57789d196c2;
host_control.py3ea4ba7cd62600ba351a27640ee44815adc70fbd27e18729c180f2a550f215ba.
Hybrid math bff1b2073c3fb878458de346fa1b3c87ab378ce5b7a5320e3a634b7b7da0950c,
normalization53f78754d8e567e145db4038f6f5b39f2f9a56c462178f20b1e94a2f6f0e7e10,
source b8db27661d1bfd3dcc5dfe1d1271b78c3f6375e06201654977a51380cd6ebcac.

## Dynamic engineering expiry, only after separate root engineering GREEN

Only the exact guarded host_control authorize/rss dispatch is eligible; never
invoke C.main with an engineering packet. Root must separately authorize which
control(s), exact deployment bytes and authority creation. No authority is written
by this preparation. Caps:10 wall/CPU seconds,512MiB AS,128MiB FSIZE/core0; sampled
RSS512MiB for authorize or64MiB for the one approved96MiB descendant fixture.
Each control's exact runner/payload argv is the frozen readiness argv, expanded to
literal absolute output paths under engineering/ and its literal control name.

Avoid spending a10-second token across SSH/upload/readiness: after all deployments,
pin checks, root-GREEN/registration binding and exclusive output eligibility are
finished, a root-authorized LOCAL-WORKER controller sets expires_unix=time.time()+9.5,
fsyncs a NEW root/authority.json, and immediately spawns the pinned CAPRUN command.
The packet uses the exact shared schema plus engineering_control_only=true,
the selected engineering_control and all fresh root/boot/pin fields. Record the
remaining token lifetime at spawn and actual controller/runner/payload identities.
If already expired, late, capped or rejected, harvest and STOP; do not regenerate
the token or retry. CAPRUN independently has10 wall seconds, including wrapper
authorization/import. No clock synchronization assumption across SSH is required.

The second control, if root authorizes it, may receive its own9.5-second packet
only after the first is terminal and groups absent. Preserve each spent root
authority by a fresh explicit archive name and verify its hash; only root's scoped
instruction may allow that recoverable move before the new exclusive authority
creation. No overwrite/deletion or production upgrade is implicit. Total control
arithmetic<=30 seconds. Parent receipt/cooperative scope remains necessary because
the shared token itself is not production-inert.

## Later production GREEN is separate

Only after terminal engineering acceptance, root may authorize ONE300-second
construction plus all803 original-cover-row replay,81 coordinates and804 labels,
4GiB AS/groupRSS,128MiB FSIZE/file and existing term/pair/bit caps. No solver,
retry, expansion of caps, source subset, gauge, normalization variant or alternate
order. The production authority is newly created, engineering_control_only=false,
with one absolute shared expiry and separate production GREEN. Precheck both
construction.jsonl AND replay-result.json with lexists immediately before dispatch.

Production payload uses /usr/bin/python3 -E -s -B construct.py authority.json;
CAPRUN and host_control use -I -B. Preserve intentional stage stderr, footer/replay
receipt distinction, metadata-only live observation, receipt-first terminal harvest,
exact all-group absence and all original files. Normal construction alone is not
a completed replay or ideal decision. Root alone decides later recoverable STOP.
