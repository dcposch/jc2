ONE INDEPENDENT Fable proof gate, <=25 minutes from actual model start.
PREP ONLY until root separately launches. Frozen inputs ONLY {{LANE_INPUTS}}.
frozen_basis=0d39df3c9fd69c939a8420c54d03228b9077777d

@CHARGED_INPUTS@

@SOURCE_STATUS@

Read PINS.json first and all charged proofs/checkers WHOLE. Unique basenames
are deliberate. Custody paths, old report references and root receipt argv
are metadata, NOT authority to read uncharged siblings, live reports or logs.
Accepted14c/symmetry/15f may be imported at their exact established scopes;
do NOT re-run their old code farms or expand their historical optional claims.
The new conditional consumer and any released source producer remain subject
to this independent review. A root replay is not independent proof approval.

Write only xmodel/d125-cone-source-composition-gate-fable5-20260908.md and own
box/d125-cone-source-composition-gate-fable5-20260908/ evidence. Root alone
launches/promotes. No new agents, AWS/SSH, CAS, solver, builder, moving-lift
work, actual H^3/H^5/R^3/R^5/A15/B25 expansion, full row streams, protected
project, canonical edits, live peer reads or descendants. Read launcher-
appended FALLACY instructions wholly. Do not author charge_basis or any new
exit price: frozen_basis is provenance, not an exit price. End the report
with the literal standalone line <!-- BODY-END --> and its newline, with
no authored Seal section. Finish all evidence writers before completion.

Give CONFIRMED / REFUTED / GAP separately for these SIX targets. Keep a
failed or unavailable source arrow separate from conditional-consumer validity.

1. EXACT SOURCE SCOPE. Check the candidate necessary system: characteristic-
   zero ODD polynomials A,B of total degrees15/25, tops H^3/H^5 with
   H=p^2(g^3+p^3), weights<=3/5 for w(g)=5,w(p)=-7, and [A,B]=c*g^2,
   c!=0. Derive its one-way implication from accepted14c's guarded restricted
   parity/gauge source using its ACTUAL support and guards. Do not infer
   exhaustive arbitrary unequal/D125/JC2 coverage. No moving-lift theorem
   is a premise; the candidate is a direct receiver-polynomial statement.
   Full fixed faces/polynomiality are additional source equations, not
   automatically equations used in this proposed broader necessary system.

2. RELEASED SOURCE DILATION AND REFERENCES. If a source producer is charged,
   derive its literal formal dilation and verify every exponent, top, parity
   and weight. For s^15 A(g/s,p/s), s^25 B(g/s,p/s), the target must be
   c*s^36*g^2. Audit how an odd monic R_s=H+T_s of lower degree/weight is
   constructed, over which field/ring and with what fixed-unit divisions.
   Do not assume quotient ordinaryness or transplant the earlier ell=1
   exceptional-center first-contact theorem to this cone. If no released
   source report is charged, this target is GAP, not a conjectural PASS.

3. COMPLETE FIRST-CONTACT / SCALAR-KERNEL ARROW. Independently verify the
   source alternatives, not just a successful j example. In particular audit
   the source's rational/polynomial centralizer proof over K via the generic
   H fiber, without mistaking reducible V0 for the generic fiber. Does it genuinely
   yield j in{2,4,6,8}, F_j=H*C with C normal/even/homogeneous of degree
   10-j>0 and nonzero modulo V0, ord G>=2j, ord alpha>=10, ord delta>=20,
   and the WHOLE B-beta*A identity? Retain every scalar centralizer kernel
   and beta freedom. Handle alpha at j=10, all j=12/14 possibilities,
   absent F/last-contact cases, zero or exceptional coefficient branches,
   and any claimed termination. A=R_s^3+alpha*R_s+F includes +F; the
   earlier task typo was explicitly corrected. Missing first-contact
   coverage makes the composition GAP even if the consumer is valid.

4. PRODUCT-RING INITIALS, NOT INVALID BRANCH SELECTION. Audit finite lex
   division and bounds, V0|P with weight<=5 =>H|P for normal residues,
   and injection into all THREE branch rings together. Individual branch
   injection is false. Verify that global block minima have regular tuples,
   later implicit poles cannot enter that global minimum, and q>j with
   eta=min(j/2,q/3) yields a monic cubic with a nonconstant coefficient on
   at least one component. Include q=infinity and alpha identically zero.
   Read the charged adversarial cone countercontrol: it obeys raw bounds
   but not the new first-contact/grading premises. Explain precisely why
   complementary supports do not invalidate the proposed GLOBAL argument.

5. ALL B INITIALS, TARGET ORDERS AND3/5 ELIMINATION. Verify scalar alpha and
   delta terms lie above5eta, so below5eta the only B initial is an odd
   regular tuple e, with NO lower dZ. Its Z^2 bracket coefficient is3De;
   componentwise constants and parity must eliminate e despite zero divisors.
   At5eta prove the quintic is monic on EVERY branch and has no Z4 term;
   only then select an A-nonconstant branch. Verify all implicit derivative
   factors and 7eta<=28<36, without assuming branchwise later regularity.
   Independently check the field3/5 identities with c0,d0,e0,f0 retained,
   the shift b->b+3d0/5, first integral and quartic7/3. Constants of the
   product are Kbar^3, not automatically diagonal; explain where source
   scalar provenance matters and where componentwise restriction suffices.

6. FINAL LOGICAL COMPOSITION. Only if ALL required source and consumer
   arrows pass may one conclude nonexistence of the stated broader odd
   receiver pair and therefore exclusion of the restricted14c guarded
   family. Verify its nonzero-c/k guard and the precise finite coefficient
   ring before using Nullstellensatz plus coefficient-field descent to
   infer the guarded ideal is unit, including nonreduced quotients.
   Do not infer an unguarded k=0 scheme is empty, arbitrary D125 is excluded,
   or JC2 is resolved. If any premise is absent or refuted, preserve the
   exact first gap and do not make a composition/unit-ideal claim.

SMALL REPLAY ONLY. Consumer-check.py is standalone. Copy it byte-for-byte
to fresh reviewer scratch and run its five modes normally and with -O:
normal, change-q0, drop-d0-shift, change-quartic, replace-odd-e.
Use /usr/bin/python3 -I -B, each arithmetic child <=30wall/25CPU/512MiB;
set dont_write_bytecode before helper imports. The positive stdout must
match consumer-witness.json. Require intended failed identities for mutated
objects; traceback path hashes may differ. The cone-counter-check.py is
standalone too; its four tiny modes are normal, freeze-moving-root,
change-central-curve, replace-F. Prefer a targeted independent changed-object
control over more redundant parent replays. Source-code replays, IF charged,
use source-first-contact-check.py with positive, --omit-alpha-cross,
--omit-gamma, --partial-shear, normally and -O; it is standalone and needs
no helper or source layout. Compare its positive stdout digest with the
charged source replay/root receipt. Its development double-beta failure is
provenance only, not a passed control; that old checker is not charged.
History14f/14v paths in source metadata are explicitly not proof premises
or authority to re-read those reports. No high powers
or source stream materialization. Do not execute custody/replay scripts just
because old paths occur in metadata.

Record exact input/output hashes, proof reading scope, all exceptions,
normal/-O controls and finished-writer custody. Distinguish sampled valuation
illustrations from universal inequalities and raw toys from full-source
points. This is one bounded review, not a new research round. STOP/IDLE.
