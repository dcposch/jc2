# Filtered C-linear pilot

Owner: /root/nonemptiness_certificate; bounded root assignment 2026-09-06.
Scope: exact source-linear algebra and polynomial-identity controls for the
fixed physical degree pair (99,66), delta2. No solve or Groebner invocation.
The physical total degrees, not weighted/partial-degree proxies, are 99 and
66, with gcd33. The mechanical frontier gate returned NOT_CLOSED_BY_THIS_GATE.

Worker is the already root-owned i-0da0cebfc97c9fd54, 172.30.0.56. Only fresh
/home/ubuntu/linear-c-discriminator-20260906 is writable by this task. The
earlier factored-J artifacts remain immutable. Fable review scratch and all
blind ideation/live reports were out of scope and not read. No worker launch,
termination, or owner-tag change belongs to this task.

Registered job tag: linear-c-filtered-pilot-astra-20260906. Each of the three
sequential arithmetic commands used the unchanged run_capped.py supervisor,
600-second wall cap and 34359738368-byte sampled exact-PGID aggregate RSS cap.
The payloads fail closed on wrong hostname, missing Amazon EC2 DMI identity,
wrong registration tag, or wrong scratch root. Aggregate arithmetic allowance
was 1800 seconds; actual terminal supervisor wall totals under4 seconds.
No arithmetic commands overlapped. Maximum observed task RSS was114323456B.

Replay on the retained worker uses the existing exact root paths and pinned
source/certificate. The producers refuse output overwrite; a replay requires
separately authorized copied scratch and an explicit reviewed adjustment of
the guard path, not writing into charged originals. The DAG producer reads
only the frozen prior pilot's coefficient-label TSV, never its output paths.

    JC2_REGISTERED_JOB=linear-c-filtered-pilot-astra-20260906 python3 run_capped.py --wall-seconds 600 --rss-bytes 34359738368 --stdout trial.stdout --stderr trial.stderr --telemetry trial.telemetry.json -- python3 filtered_basis.py

Subsequent payloads are verify_filtered.py and reconstruction_dag.py, each
with separate output/telemetry names. The complete reduced ideal was not
expanded. Final artifact paths, hashes, terminal job states and no-live-edit
promise are in custody.json.
