# FULL0925 adversarial cross contract

ROOT supplies the same frozen three independent blinds and postblind note
to Astra and Fable, only after all blind authors are independently terminal.
Read all three blinds, POSTBLIND and this contract WHOLE. Exact personal
WHOLE reads of the unchanged APP and COORD snapshots may be reused with an
explicit scope declaration and fresh hashes. Otherwise read them WHOLE in
bounded chunks. Do not call a hash or a clipped output a whole read.

One report, <=2200words, with:

1. Merge cards by object, missing implication, mechanism and decisive test.
   Classify NEW/KNOWN/DUPLICATE/SCOPE-CONFLICT. No vote by model identity.
2. Attack every surviving candidate and ROOT's postblind corrections. Give
   explicit CONFIRMED/REFUTED/GAP for the short S invariant-ring and minimal
   Mobius-family scope arguments, not a fresh general involution theorem.
   Check source/target domains, two-chart coverage and degree conventions.
3. Select at most ONE next proof test and ONE genuinely different disproof
   test, with an exact missing global implication and cheapest discriminator,
   or NO_TEST if none survives. A new statement of the same gap is not a
   changed test. No automatic family/control/prime/degree expansion, no
   revalidation of the >=125 bound or other accepted foundations absent a
   named load-bearing doubt. Keep all46 dispositions and exact old stops.
4. Adjudicate the systems suggestions: at most ONE small UPGRADE or a
   reasoned NO_CHANGE. A bounded read-chunk instruction using existing tools
   is preferable to redundant snapshot formats if it solves clipping.
5. State remaining actual-source assumptions and any disagreement. Do not
   promote ROOT/Astra shared-model reasoning as a different-model gate.

No new external source, linked local source, live peer cross or search of
current outputs. No network, scientific code/interpreter/CAS/import/AST/
syntax/tests, AWS/SSH, new agents, service/process control, shared-ledger
edits, protected tree/mirrors, contact or publication. Manual mathematics,
inert text/hash/date reads, and apply_patch authoring only; unchanged admin
finalizer is allowed for native publication. These are scope and decision
reviews, not computational replays or permission for scientific launches.

Original cross reserve10:10UTC/HARD10:13UTC September13. ROOT collector.
Synthesis target10:20, full backstop10:22:49.724379680. Finish early when
useful; no retiming, no follow-on task or new exit-price declaration.

Hash all charged inputs before reads and after whole OWN final body
readback. Native: private transaction/PINS/custody, expected verify, then
terminal all-writers-idle. External: marker-free skeleton, bounded
apply_patch appends, read own complete report including final qualifications,
postpins, then unique standalone <!-- BODY-END --> LAST; lane.sh seals.
Record actual read scope and original times, not planned completion times.
