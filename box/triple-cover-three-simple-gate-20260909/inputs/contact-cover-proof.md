# Triple-source contact cover after the accepted late endpoint

September9,2026. **SCOPED SOURCE-COVER DERIVATION / NEW COMPOSITION FOR REVIEW.** The desired projected contact budget is valid after checking the generic double-p proof and the canonical normalization, not by degree counting alone:

| Triple-line multiplicity d=ord_(p-g) F_j | Remaining possible j |
|---|---|
|2|1–11|
|3|1–9|
|4|1–8|
|5|1–6|
|>=6|excluded by the already bounded double-p arguments and H²-nondivisibility|

These are necessary first-contact budgets, not a list of attained source points or a whole-family exclusion. The more precise p-multiplicity disjunction appears in §4. No pending cubic-chart result is used.

## 1. Accepted starting object and exact read perimeter

Let K have characteristic zero, L=p-g,H=p²L³. Start with the actual ordinary receiver A,B in K[g,p], totaldegrees15/25, A15=H³,B25=H⁵ and [A,B]_(g,p)=cg²,c!=0. The accepted late-contact producer b5413087... and Fable gate c9a399a8... give the canonical combined-homogeneous dilation/reference, with s,g,p of degree1:

\[
[A_s,B_s]=cs^{36}g^2,\quad R_0=H,\quad\deg R=5,
\quad A_s=R^3+\alpha s^{10}R+a_0s^{15}+F,
\]
\[
B_s=f_B(R)+q_s(R)F+G,\qquad\operatorname{ord}_sG\ge2j,
\quad1\le j=\operatorname{ord}_sF\le11,
\]
\[
\deg F_j=15-j,\qquad pL^2\mid F_j,\qquad H^2\nmid F_j. \tag{1}
\]

All five B reference scalars are retained: f_B(R)=R⁵+sum_(i=0)^4 b_i*s^(25-5i)*R^i and q_s(R)=(5/3)R²+(4/3)b4*s5*R+(b3-5alpha/9)*s10. The coefficients alpha,a0,b_i are field scalars. The first five F coefficients are normal for the monomial g⁶p⁴; F10 is normal for g³p². In particular no monomial divisible by the indicated monomial occurs. F15=0. These are the actual canonical division conditions, stronger than mere H²/H nondivisibility.

For e=ord_p F_j and d=ord_L F_j, the immediate facts are

\[
e\ge1,\quad d\ge2,\quad e+d\le15-j,
\quad\neg(e\ge4\text{ and }d\ge6).          \tag{2}
\]

The whole late-contact producer and its now-terminal accepted Fable gate were read at their exact current hashes. The whole local-consumer d4fd2a0e... was reread, especially §§1–4 and its explicit limitations; its stated two-pattern TOTAL theorem is not applied to triple H. Its local proof is transported explicitly in §2. The whole actual-Moh source07e53340... and gateeade8ec8... supply the named conditional parent attachment only, retaining child-data/major-packet external trust. Their initial partial pi-degrees are not substituted for their subsequently proved actual totaldegrees. All five exact pins/read scopes are in input-pins.json, checked before bodies and at sealing. No pending cubic chart, live gate, current blind/cross or canonical body is charged.

## 2. Why the double-p local arguments really port

The exact generic double-root input is present at p=0. Use transverse/tangent coordinates (y,X)=(p,g). Then

\[
H=y^2(y-X)^3,\quad H_{yy}(0,X)/2=-X^3\ne0,
\qquad[A_s,B_s]_{y,X}=-cs^{36}X^2.           \tag{3}
\]

Over E=Kbar(X^(1/2)), choose a with a²=-1. For the GENERAL polynomial R, not a cubic restriction, the formal critical point y_c(s,X) exists by the nonzero second derivative. Put xi=R(y_c,X). Taylor factorization R(y_c+w,X)=xi+w²U, U0(0,X)=-X³, gives zeta=w*sqrt(U) and an inverse with nonnegative s/zeta powers. Thus R=xi+zeta² exactly, zeta has weight5/2, and y_zeta at s=zeta=0 equals1/(aX^(3/2)). With kappa=ord_s xi>=1 or infinity, the generic target has order36 and nonzero leading value -cX^(1/2)/a after a positive transverse rescaling. The possible dependence of its pure s-order coefficient on zeta causes no lower-order target term. The other, triple factor of H is a UNIT in this generic p-chart and is never counted as a simple factor.

Write the FULL moving F as sum c_n(s,X)zeta^n. For e=ord_p F_j, every ord c_n>=j, ord c_e=j and ord c_n>j for n<e. The entire G retains coefficientwise order>=2j. These claims use the nonnegative-order inverse and its nonzero linear derivative, not a static face substitution. No centralizer, degree_g normal block, root-count condition or information about the other factor is used by the local argument.

Here is the exact usable generic range, re-derived from the local proof's inequalities:

\[
e=1, j\le11;\qquad e=2, j\le9;\qquad e=3, j\le7. \tag{4}
\]

**Coalesced case.** Set r=min_(0<=n<=e) ord(c_n)/(6-n). It is positive finite and <=j/(6-e). If kappa>=2r, the full A initial at order6r is P=(Y²+b)³+U, U!=0,deg U<=e. Terms beyond e have strictly higher order. In all ranges (4), r<5/2, so alpha*s10*R and a0*s15 are later. Since B-R⁵ has coefficientwise positive s-order, a possible earlier B initial at nu<10r has degree n<nu/r. Its top-coefficient Euler factor is

\[
25-\nu-n(5/2-r)>25-5\nu/(2r)>0.
\]

The bracket with monic P vanishes below the source order, since5r+nu<15r and

\[
e=1:15r\le3j\le33;\quad
e=2:15r\le15j/4\le135/4;\quad
e=3:15r\le5j\le35.                         \tag{5}
\]

It follows that there is no earlier B initial. At10r it is monic10, and the two monic Euler-homogeneous initials commute. The factored identity Q³=P5 gives a common monic quadratic W. Since P differs from (Y²+b)³ by degree<=3, the factorization of the difference of two monic quadratic cubes forces U=0, a contradiction. All three margins in (5) are strictly below36. Infinite kappa belongs here.

**Separated case.** If kappa<2r, the exact even/odd regrouping of the full series is

\[
U_F(z)=\sum_{m\ge0}c_{2m}(z-\xi)^m,\quad
V_F(z)=\sum_{m\ge0}c_{2m+1}(z-\xi)^m,
\]
\[
F_\pm=U_F(z)\ \pm\sqrt{-\xi}\sqrt{1-z/\xi}\,V_F(z). \tag{6}
\]

The sums converge coefficientwise s-adically because kappa>0; their coefficients have order>=j. BOTH signs are retained. For ell=0,1 let q_ell=min(ord_s((U_F)_ell),kappa/2+ord_s((V_F)_ell)), where the subscript ell denotes the z coefficient, and put eta=min(q0/3,q1/2). The inequalities ord(c_n)+n*kappa/2>3kappa give eta>kappa. The exact contact coefficient gives

\[
e=1:\ \eta\le(j+\kappa/2)/3<2j/5;\quad
e=2:\ \eta\le j/2;\quad
e=3:\ \eta\le j/2+\kappa/4<2j/3.            \tag{7}
\]

Thus eta<j and eta<5 in (4). After z=s^eta Z, all positive square-root corrections gain order eta-kappa>0, and all z-degrees>=2 are later than3eta. The actual two-sheet A initials are depressed monic cubics Z³+u_pm Z+v_pm with a nonzero correction tuple. One sheet may vanish, but the pair (a+b,a-b) cannot vanish on both sheets unless both bases vanish.

Apply the SAME regrouping to the entire G, with coefficient orders>=2j. Its first order across both sheets is the minimum of the even/odd base orders; later binomial shifts cannot be globally first. Below5eta its degree is absent for e1, zero for e2, and at most1 for e3. All B scalar kernels and lower q_s terms are later than5eta because eta<5; the fixed leading terms supply the monic quintic. The target in the z-chart has order36-kappa/2. The strict margins are

\[
e=1:\ 7\eta+\kappa/2<3j\le33;\quad
e=2:\ 7\eta+\kappa/2<15j/4\le135/4;\quad
e=3:\ 7\eta+\kappa/2<5j\le35.               \tag{8}
\]

Every putative earlier B initial therefore commutes with its monic cubic. The Euler factors are25-nu>0 in degree0 and20-nu+eta>20-4eta>0 in degree1. Both are impossible. At5eta the initials on each sheet are monic3/5 commuting Euler-homogeneous polynomials. They have a common monic linear root; the depressed cubic forces that root to be Z, making every u_pm,v_pm zero, contrary to the nonzero tuple. This excludes the separated case as well, without selecting a nonexistent common nonzero sheet in advance.

Equations(3), the source scalars/G bound, positive common B top, and the inequalities are exactly the hypotheses used in §§3–4 of the accepted local proof. The sign/unit change from its named p-chart does not affect any strict below-target contradiction. Thus (4) is a proved generic-local transport to the present source, not an application of its two-pattern family conclusion. This explicit source-composition is the new review item in the packet. No d_p>=4 theorem or stronger endpoint is smuggled into it.

## 3. Exhausting large triple multiplicities and the dangerous endpoints

From (4), e=1 is entirely excluded. If e=2, then j must be10 or11 unless degree already forbids it. If e=3, then j>=8. If e>=4, (2) forces d<=5.

Now suppose d>=6. For e=2, degree gives j<=15-2-6=7, which is excluded by (4); for e=3 it gives j<=6, also excluded. The e=1 case was excluded, and e>=4 would make H²=p4L6 divide F_j. Thus every d>=6 case is exhausted without a triple-root consumer.

For d=5, e=2 gives j<=8 and e=3 gives j<=7, both in the excluded ranges. Hence e>=4 and j<=6. For d=4, e=2 gives j<=9 and is excluded; e=3 leaves only j=8; e>=4 gives j<=7. For d=3, e=2 gives the apparent endpoint j=10, but then F10 has degree5 and is necessarily a nonzero scalar H, contradicting its canonical H-normality. The remaining e=3 gives j<=9 and e>=4 gives j<=8. Finally d=2 only gives the global j<=11, which is already the accepted late-contact bound.

This explains why degree alone would leave d3/j10, d4/j9, d5/j8 when one merely knows e>=2. The double-p exclusions remove the last two, while the d3/j10 endpoint additionally requires the canonical F10 normalization. The latter cannot be replaced by the weaker H²-nondivisibility assertion.

## 4. Sharper exhaustive (e,d,j) disjunction

Using the actual monomial-normality conditions gives the following precise remaining possibilities:

| p multiplicity e | Surviving necessary possibilities |
|---|---|
|1|none|
|2|ONLY (e,d,j)=(2,2,11)|
|3|d=2 with j=8,9,10; d=3 with j=8,9; d=4 with j=8|
|>=4|2<=d<=5 and max(4,10-j)<=e<=15-j-d, with j>=1|

For the e=2,j10 case even d=2 is impossible, a slightly stronger observation than the projected table needs. A homogeneous degree5 polynomial with exact p multiplicity2 has lex-leading monomial a*g³p², a!=0, forbidden in F10. If d=3 it is the scalar H case already noted. Thus only e2,d2,j11 remains.

For e>=4 and j<=5, let N=15-j. Exact p multiplicity e forces the leading monomial of F_j, in lex g>p, to be a*g^(N-e)*p^e with a!=0. Normality for g⁶p⁴ is therefore equivalent to N-e<=5, or e>=10-j: if N-e>=6 the displayed monomial is forbidden; if N-e<=5 every monomial has g-degree<=5 and is normal. For j>=6, e>=4 already implies e>=10-j, and ordinary degree<10 makes the first-five normality restriction irrelevant. Degree supplies the upper bound e<=15-j-d. This proves the last row without replacing exact multiplicity by a generic-support assumption.

The table is sharp for these **charged first-coefficient filters**, not for full source solvability. The maximal projected endpoints are represented by the factored leading-coefficient controls

\[
(d,j,e)=(2,11,2):p^2L^2;\quad
(3,9,3):p^3L^3;\quad
(4,8,3):p^3L^4;\quad
(5,6,4):p^4L^5.
\]

They have the required ordinary degrees15-j, exact p/L multiplicities, required normality and H²-nondivisibility, and lie outside (4). No high power of H/R or full pair is expanded here. More generally, any admissible tuple in the final row has a homogeneous control p^e L^d g^(15-j-e-d), with the claimed exact multiplicities and monomial normality. These controls do not supply R,A,B, remaining Jacobian equations, formal source jets or a point; they only show why the stated elementary filters cannot lower an endpoint further by themselves.

## 5. Why a broader coalesced target argument does not silently tighten the table

The accepted local proof's coalesced argument explicitly assumes15r<=36 to set every earlier B initial bracket to zero. Its published-in-campaign statement does not license replacing (4) by larger j ranges. There is a useful elementary extension mechanism, but it must be labelled a NEW proof step and does not by itself exclude all remaining cases.

Conditionally on the same monic6 coalesced initial, r<5/2 and B-R⁵ of positive order, an earlier B initial at nu<10r still has degree n<nu/r. If its first bracket order5r+nu equals36, the target's positive-r initial is constant in Y. The coefficient of Y^(5+n) is therefore6q_n'=0, just as for a zero target. The same positive Euler factor excludes it. If5r+nu>36, the left side is too late; if it is smaller, ordinary commutation applies. Thus this modified argument can remove earlier B initials across the target, without claiming that a nonzero initial bracket vanishes. This observation is NOT used in §§2–4's source budget.

At the monic10 stage, however,15r=36 gives the possible resonance r=12/5. Integral s-orders do NOT rule it out: in r=min_(n<=e)ord(c_n)/(6-n), the index n=1 has denominator5 and can have ord(c1)=12. This is consistent with e=2,j11 or e=3,j8–10 at the level of those order constraints (other coefficients must be sufficiently late). The elementary integer-order argument therefore leaves this equality. No second-chart scalar conflict is supplied for this triple receiver. And for r>=5/2, alpha*s10*R and a0*s15 can meet or precede the proposed A initial, while the positive Euler parameter5/2-r fails; their removal would be a new unsupported source step.

The separated proof also has explicit eta<5 and strict target margins; larger e/j endpoints do not automatically retain them. Finally, e>=4 is outside its correction-degree range: two distinct monic quadratics can have cubes differing in degree4, so the degree<=3 contradiction cannot be reused unchanged. These are concrete missing arguments, not a whole-family no-go. A reviewed extended coalesced/resonance or separated theorem would be needed for any stronger source budget. No such theorem is asserted, and the pending cubic chart is neither a premise nor a substitute.

## 6. Scope and custody

The projected table in the opening and the sharper disjunction in §4 follow from the accepted canonical source facts plus the explicitly audited generic double-p transport. They exhaust only these necessary first-contact alternatives. Their attachment to a historical Moh parent retains the exact named child-data/centering assumptions; reverse ordinary Keller lifting and attainment remain unproved. No full triple-line consumer, whole receiver/source exclusion, scheme/unit consequence or JC2 claim follows.

The manual countercontrols are the normalized leading coefficients above, the explicitly forbidden scalar H at F10, the exact leading-monomial obstruction at e2/j10, and the degree4 common-quadratic warning. No computational check is claimed. No mathematical subprocess, CAS/Python/SymPy, high H/R power, full pair/source expansion, solver, AWS/SSH/web, new agent, shared/canonical/protected write or live peer access occurred. Only bounded metadata scripts perform pinning and begin/close/finalize/verify. Exact source and owned-file pins, read scopes and terminal writers are in custody. The task stops at this budget and the named extension gap; all writers are IDLE after sealing, before02:21 UTC.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `14932`.
- Body SHA-256:
  `f8e6ed4bf60e464534ff33dfdaf65e61d7b1d49cd513e9bf68a7b3acd6518576`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
