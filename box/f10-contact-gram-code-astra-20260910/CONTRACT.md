# Fixed contact Gram certificate contract

STATIC / UNEXECUTED. Job f10-contact-gram-certificate-20260910. No registration, runtime, worker, native closure, certificate or positivity result is supplied. Exactly one formation and one genuinely independent check are prospective, under a later ROOT wrapper. No engine, prime, subdivision, degree-elevation or representation fallback.

## Science and arithmetic

The producer reconstructs the accepted d,N,A,I formulas and Phi,Q0,Q1 in Q[s,p,V]. The eight columns are rem(V^j Qi,Phi), i=0,1 and j=0..3, with all zero positions retained. A SINGLE fixed normalization multiplies each WHOLE column by c=denominator-LCM/integer-content, positive rational and constant; a zero column gets c=1. No parameter polynomial division or content removal is allowed. B=M diag(c), G=BB^T, h=adj(G)e0, z=B^T h; U0,U1 are formed from diag(c)z in their original column order. Monic quartic division supplies Uphi. The producer requires the full remainder to vanish.

The checker imports metadata authority before entry authorization and standard exact arithmetic only AFTER authorization; it imports neither producer nor producer arithmetic. It rebuilds all eight literal a3..a0,b3..b0 coefficients, all FOUR X^2-sX+p remainders, derives the W-elimination coefficient and N by collection, clears W denominators by independent coefficient operations, and obtains Phi,Q0,Q1. It checks R=U0 Q0+U1 Q1+Uphi Phi in the full polynomial ring, including every positive V coefficient. This identity, not a determinant header, is the certificate. The checker does NOT independently prove that R equals the producer's Gram determinant. Any correctly bound identity with the verified nonvanishing R suffices, including over unreduced specialized algebras.

Matrix column weights are (6,7,8,9,5,6,7,8), row i subtracts i. Constant scaling changes none. Cauchy--Binet gives R weight<=52. G_ik<=18-i-k and adj(G)_{i0}<=42+i; z_c<=w_c+42. Thus U0<=54, U1<=53 including V powers; the divided numerator has weight<=60, and Uphi<=56. The selected wire bound64 safely contains all cofactors; V-degrees are0,3,3,5 for R,U0,U1,Uphi. Monic reduction preserves weighted bounds. These are manual envelopes, not observed degrees or coefficient counts.

## Domain and sign

a=(6+t)/21; b=(3-3t+7tv)/21. At t=0 the point is(2/7,1/7); at t=1 the edge has a=1/3,b=v/3. For each t, b runs from1-3a to4a-1, since a=(6+t)/21. Thus 0<=t<1,0<=v<=1 covers exactly the prescribed closed triangle minus its limiting a=1/3 edge. All prescribed integer-family pairs lie in this domain. This is a SUFFICIENT stronger certificate, not equivalent to full-window or discrete-family positivity. The excluded reality-converse appendix and gate's incidental 'equivalently' are not premises.

s=4-a-b,p=4-2(a+b)+ab. Weighted R<=52 gives pullback bidegree<=52,52; 21^52 clears substitution denominators (p has denominator21^2). Rational coefficients are still permitted. Producer cached powers and checker nested Horner are different substitution paths; checker uses p=(2-a)(2-b). Remove ONLY the maximal factor(1-t)^j, including j=0; zero R has no usable sign certificate. Fix tensor Bernstein degree(52-j,52), with no elevation/subdivision. Producer uses separable power-to-Bernstein conversion; checker separately expands the two Bernstein bases. All coefficients must be nonnegative and all53 row-t0 entries must equal the same strictly positive endpoint. The i=0 Bernstein term then gives endpoint*(1-t)^(52-j)>0 for every t<1, including retained v=0,1. No t0/v0/v1 factor may be discarded; no sign flip is used.

## CLI, authority and wire

Exact prospective entry flags: ROOT-pinned resolved interpreter -E -s -B, followed by producer.py --job JOB --registration ABS --output ABS, or checker.py --job JOB --registration ABS --input ABS --receipt ABS. No -I/-P; sibling authority import needs script directory. authority compares actual sys.orig_argv and script argv, fixed flags, resolved absolute pin paths, actual Linux/EC2 instance/host/interpreter/cwd and a mode0444 root-owned <=64KiB registration; all required science report/code/input pins are checked. Deadline must have explicit UTC offset. Exclusive absence/current-source/post-deadline checks remain. Arithmetic globals are unset until authorize returns. Metadata imports are not a sandbox or native supervisor.

The wire schema f10-contact-gram-certificate/v1 has EXACT keys schema,status,reason,science,code,variables,bounds,polynomials,j,bernstein. Four polynomials only: R,U0,U1,Uphi, variables[s,p,V]. Terms are sorted unique [[three nonnegative exponent decimal strings],[signed numerator string,positive denominator string]], reduced, nonzero. Empty polynomial is[]. No floats/JSON numbers/nonfinite values/duplicate keys. j and dense degree fields are strings; dense Bernstein rows include every zero slot as["0","1"]. Exponents obey named weights and V caps. MAX_TERMS20000 per polynomial, MAX_BITS12000 per rational numerator/denominator, DECIMAL_LIMIT4096, internal coordinate exponent<=100. Total artifact<=16777216-4096 bytes; checker receipt<=4096 and their sum<=16MiB. Exact verified RAW input bytes are SHA256-compared to their authorized pin and bound in the receipt; no re-read digest substitutes for raw evidence. Wrapper must freeze all inputs and bind outputs, flags, native closure and source hashes. Partial files on any write failure are NOT candidates. File writes are exclusive and fsynced; wrapper owns directory fsync/terminal custody.

## Status and future supervision

Producer normal0 + complete CANDIDATE artifact: unchecked possible positivity certificate. Producer normal2 + complete INCONCLUSIVE artifact: identity/sign-data candidate eligible for the SAME independent checker, even for negative Bernstein coefficients or nonpositive endpoint. Future wrapper must distinguish these by complete artifact/schema/current byte custody, not skip every normal2. Producer structural/resource/zero-R error: normal2 with bounded INCONCLUSIVE stderr, no guaranteed usable artifact; any partial file must be rejected, no retry. No absence-of-certificate result is a contact point.

Checker consumes either allowed producer status WITHOUT trusting its verdict. After full reconstruction/identity/maximal-factor/full Bernstein expansion, a nonpositive endpoint or negative coefficient returns normal2 plus an INCONCLUSIVE receipt explicitly stating identity and expansion checked. Normal0 and receipt PRESCRIBED_CONTACT_EXCLUDED_BY_CERTIFICATE requires every final sign predicate. Structural/schema/identity/expansion/authority errors return normal2, bounded stderr and NO valid receipt; corruptions are mathematical rejections only at the exact specified predicate. A successful identity proves prescribed CONTACT exclusion only, never full-source/JC2.

Prospective joint cap600wall/550CPU seconds,2GiB,16MiB aggregate wire including logs/metadata is UNMEASURED. This metadata gate does not enforce CPU/RSS/group cleanup. ROOT must separately supply a reviewed capped wrapper/native closure/full argv/per-phase caps/exclusive paths, original joint absolute cutoff and terminal PID/group quiet plus durable custody. No authority is minted, no actual clock/worker selected, no installed compatibility assumed.
