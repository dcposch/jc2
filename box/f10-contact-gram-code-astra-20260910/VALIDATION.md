# Exactly two prospective semantic controls

No fixture, mutator, test or runtime has been constructed/executed. First require one complete genuine artifact and the independent checker result; a complete identity-consistent negative-sign artifact remains checkable and may honestly end INCONCLUSIVE. ROOT alone registers any future bounded batch after FIRST code review. No additional controls or arithmetic farm is implemented here.

1. Cofactor corruption: replace Uphi by Uphi+1 canonically, combining/deleting its constant term as appropriate. This always stays within weight64/Vdegree5 and changes the full identity by the NONZERO monic Phi. Provided canonical coefficient/bit/wire limits are retained, checker must reach ValueError('full contact cofactor identity'), CLI normal2, no receipt. This is not parser/authority coverage. If the increment crosses the coefficient/wire cap, declare this control unavailable rather than count an early refusal. This is ONE fixed mutation, not an alternative-cofactor search.

2. Bernstein corruption: increment one EXISTING dense Bernstein coefficient by1, retaining all rows/columns and reduced rational strings. A tensor Bernstein basis element is nonzero, so it must fail ValueError('full Bernstein expansion') after the full cofactor identity and factor checks. CLI normal2, no receipt. Choose an entry whose increment stays in the coefficient/wire cap; otherwise mark unavailable rather than substitute an early parser failure. No sign-label-only mutation counts.

Each future changed artifact must genuinely differ, be exclusively written, re-read and freshly SHA-pinned in its own immutable authority, with exact whole-byte inverse restoration to the original. Revert the selected coefficient/constant term, recanonicalize with the original serializer, and compare the ENTIRE original bytes. Stale original hash refusal does not count. The prospective runtime wrapper, not these sources, owns this fixture creation and exact exception classification. No executable mutator is supplied.

Pending quantities: syntax/installed standard-library compatibility, actual identity formation/sign outcome and wall/CPU/RSS/bytes/terms/bits are all UNMEASURED. One FIRST static review and one separately authorized bounded formation/read-back are the cheapest next checks, not authorization to perform them. Any cap/mismatch is STOP/INCONCLUSIVE without escalation. Source/JC2 conclusions are outside this contract.
