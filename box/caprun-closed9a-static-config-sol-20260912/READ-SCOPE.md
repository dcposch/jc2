# Read scope

Fresh whole reads after exact pre-pin confirmation covered `TASK.md`, the selected PLAN, ORIGINAL-CLOCKS, the corrected frozen-config template, the ported receiver template, and the ported final-installer template. The previously whole-read setup template was not recharged; ROOT supplied its exact filename correction with the already known SHA as provenance. Embedded references were inert and were not followed.

Own whole readback covers `EARLY-CONSTANTS.json`, `FROZEN-CONFIG.preallocation.json`, `PORT-MAP.json`, `PINS.json`, this file, the finalized report, and custody. Administrative operations were literal text substitution, occurrence/census comparison, byte counts, and SHA-256 only. No JSON runtime roundtrip, source/shell execution, syntax/AST/import/compile/test/mock/fixture, process/worker action, private-key read, network/SSH/AWS action, scientific read, or protected-tree access occurred.
