# Exact-linear execution scope

First tool 2026-09-09 23:01:02 UTC; both targets absent. Controlling task cap
23:36:00 UTC, earlier than first+35 minutes; final three minutes reserved.
Initial activity is documentary preparation only. No AWS/SSH, code import,
syntax/AST/test or mathematics before ROOT's separate execution release.

The frozen exact-linear code custody9e2062e4, producer e73075e3 and accepted
17z gate4c3f5932 were current-hashed before reuse. All31 custody entries
(13 inputs plus18 owned) matched. Solver, checker, evidence, controls and
VALIDATION read WHOLE again; caller main read again, unchanged run machinery
reuses the same-byte prior WHOLE read. Parent report/gate and arithmetic/helper/
probe reuse prior same-byte WHOLE reads after current pins. No provenance
report or current peer/gate body followed. Installed environment35a9c322
re-pinned; same-byte prior WHOLE read reused, no package/API probe.

The generic/API runtime is a PROVISIONAL one-generation engineering parent;
its pending gate is NOT an input. No actual-source or global consequence is
promoted by this task. The only intended mathematical execution is the one
frozen caller's seven preflights, solver.main, genuine checker.main and
frozen in-process corruption harness, on the exact ROOT-registered worker.

A guessed historical ROOT-ACCEPTANCE path was checked for existence and was
absent; no body read or expanded search. ROOT will provide the exact immutable
acceptance/source/prior-receipt paths and full staging/registration vector.

ROOT subsequently supplied mode444 dispatch.registration.json af47d8b6...
and ROOT-REGISTRATION.md a2e97775..., both pinned before WHOLE reads including
all115 bindings. Exact14 local staging sources matched; registration's100
package pins and module metadata equal the frozen environment. Incoming
univariate.json was HASH ONLY locally; acceptance90dcf4f3 and genuine prior
fullreceipt0c22af83 were current-pinned and read WHOLE. Admissibility/frontier
were re-pinned and reuse prior same-byte WHOLE reads, without executing the
frontier gate. No normalized/source coefficients or source provenance were
followed. ROOT communicated that the generic/API runtime gate had passed;
only this lifecycle update is used, no new gate body was read. Hence the
execution no longer depends on an unreviewed parent, but its own result still
requires separate independent review.

Explicit execution release preceded fresh exact-worker metadata23:06:45–47.
All116 remote pre-dispatch hashes (115 inputs plus registration) matched.
Only exact fleet run/push/pull and owned-instance describe/STOP are licensed;
ROOT owns start/backup timer. One unchanged SYSTEM caller started23:07:29.
Actual caller2200, CAPRUN2250 and solver2251 were captured live23:07:53.
A transient extra cgroup PID2717 disappeared during the same stat sample;
the resulting metadata-shell exit1 is preserved, not misreported as a math
failure or repaired/rerun batch. No semantic payload read while service live.

At terminal, exact service/cgroup/principal identities were checked before a
77-file SHA manifest. All retained bytes were pulled and matched; all115
registered inputs plus registration matched post-run. Process-only metadata
then enumerated every recorded dispatch/dummy identity, and all18PIDs/nine
PGIDs were independently absent23:18:39 before outcome bodies were charged.
Terminal STOP/preflight/solve/dummy telemetry and refusal stderr were read
WHOLE; authority/argv/caps/hash metadata was checked. The source coefficient
body remained HASH ONLY. No checker/solver or mathematical replay occurred.
One post-pin shell used literal escaped newlines incorrectly; its date error
was retained and a corrected metadata-only hash command matched all116.
Exact worker STOPPED by23:19:07, unchanged Owner/type/IP/EBS; ROOT notified.
