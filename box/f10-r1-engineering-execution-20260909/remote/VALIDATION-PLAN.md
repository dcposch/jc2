# Pending validation, NOT execution authority

Status: INTERNAL-UNREVIEWED, UNEXECUTED. There are no emitted ideal rows yet.
Only source text and metadata have been prepared. All steps below remain pending.

## Registration and existing runner

Root must separately authorize a specific AWS instance/job, choose caps and an
exclusive output set, and create a new authority file from the disabled template.
Do not edit this frozen packet. The authority registers hostname/EC2 instance,
admissibility hash, executable/dependency hashes, and the ELEMENT-FOR-ELEMENT
parent and child argv. No pathname-membership shortcut is used. The checker
input artifact must itself appear in the authority's file hash map.

Each operation uses `/usr/bin/python3 -I -B ABS/ops/run_capped.py` with explicit
wall/CPU/RSS/sampling/grace/cwd/stdout/stderr/telemetry options, then `--` and
`/usr/bin/python3 -I -B ABS/builder.py AUTH OUTPUT`, or the checker's five-argument
shape below. The gate's exact option order is documented in execution_gate.py.
Runner lifecycle/dummy descendant regression, authority rejection controls,
correct identity/caps, and source pin verification remain root obligations before
any mathematical run. This gate is not an allocator or a new supervisor.

Builder child: `builder.py AUTHORITY.json EXACT.json`.
Checker child: `checker.py AUTHORITY.json EXACT.json RECEIPT.json MODE`.
There is no optimization/GB/resultant/prime/point command. The only shared helper
is launch metadata; producer and checker do not share mathematical code.

## Required ordinary-Python modes

1. `full`: valid exact artifact; independent full bracket, all ten inverse rows,
   envelopes, gauges, every zero slot, exact rational wires, guard and restoration.
2. `u-zero`: same full checks after literal coefficient specialization u=0;
   no division by u is available or required.
3. `dropped-constant`: remove the actual -1 monomial from BOTH the saved E0
   polynomial and its S0 row. The deliberately weaker upper-only verifier must
   pass, then the complete determinant check must reject. This is not a claim
   that the full original ideal has a point.
4. `u-zero-dropped-constant`: repeat that actual payload attack on the u=0
   specialization; weak check passes and full check rejects.
5. `upper-coefficient`: change the actual B4 coefficient d0*S²/2 to
   3*d0*S²/2 in BOTH its polynomial and slot wire. B5=S² stays intact;
   the full determinant must reject the changed upper Jacobian coefficient.
6. `guard`: delete -1 from the guard in BOTH storage locations; full checker
   must reject, even though all B reconstructions and bracket rows are untouched.
7. `leading-top`: replace saved a=k4 by 1; full checker must reject false
   restoration/normalization. This is not permission to impose k4=1.

All seven modes are designed, not run. No `-O` evidence is claimed. Code uses
explicit exceptions, not `assert`, but optimized execution still requires its
own review/registration if ever desired. Upper/guard/top mutations change actual
objects; only the two constant-row mutations explicitly compare the deliberately
weakened and complete mathematical verifiers.

Also pending: syntax/import validation ON the registered worker, negative
launch controls (unregistered/local host, wrong full caps/argv/pin, wrong mode,
input file pin missing), exact serialization round-trip and a genuine integer
larger than 2^53 precision attack. The wire requires numerator/denominator
STRINGS and rejects floats anywhere, but no precision test has run here.

## Measurements and acceptance

The later checker receipt measures row/B term counts and row parameter degrees
from the actual artifact. Actual zero rows, total terms, maximum degrees, byte
size, runtime, RSS/CPU, and capped lifecycle outcomes must be recorded then;
none is estimated as performance evidence here. The accepted theorem provides
20 fixed ambient row slots and the upper degree bounds, not measured output.

After successful independent verification, the artifact would encode the
complete normalized r=1 ideal; that is still neither a solution nor a unit
certificate. A separate registered exact decision workflow would require new
authority. No high-degree (112,196) source powers are generated in this design.
