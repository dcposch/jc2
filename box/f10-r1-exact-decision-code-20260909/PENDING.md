# Pending: no runtime evidence yet

1. One combined different-model code/semantic delta review of engine.py, certificate.py, semantic_tests.py and caller.py; no duplicate source-theorem or CAPRUN re-review absent a specific issue.
2. Root's installed Singular path/version/binary hash and option/exporter/CLI compatibility. Null fields in REGISTRATION.disabled.json are intentional. No executable is assumed available.
3. A separately registered validate-only job, under the same caller's five refusals, valid sentinel and descendant RSS/TERM/KILL control, then the finite semantic tests. This invokes no Singular solve. It still requires engine installation metadata because its caller bindings and future decision authority are the same type.
4. Only after validation is reviewed, a separate exclusive decide job with the pinned validation receipt and the identical certificate/test hashes. Both modes repeat fresh caller controls. This plan grants neither job authority nor a worker.
5. Exact full-row Singular input read-back and real unit/proper protocol output remain untested. Source preservation is established only by the future independent row equality; warnings, unsupported numeric formatting and too-fast missing nested /proc argv fail closed.
6. Future scalar, parser or cap failure is INCONCLUSIVE, no patch/retry/order/prime/engine change. Root chooses fresh absolute clocks; this code never resets the old 13:35 deadline. Caller has an additional 120-second aggregate mathematical bound; engine60/50, verifier30/25, semantic validation30/25; 2GiB sampled RSS,16MiB per file. Samples and cleanup are not strict real-time guarantees.

No runtime, row count, witness size, speed, ideal properness, unit certificate or point is claimed by this code preparation.
