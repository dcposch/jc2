# Closed child-scope attachment — disabled static contract

INTERNAL / UNREVIEWED / UNEXECUTED. These are new snapshots, not replacements for ops or old runtime files. No enabled registration, worker, setup script, new supervisor, scientific input or execution is delivered. One focused different-model FIRST must cover these four files and this attachment before ROOT may form any registration. No earlier acceptance covers their new bytes.

## ROOT registration and topology

The new runtime schema is `f10-necessary-rows-runtime/closed-child-v1`; source JOB, five source-file roles, scientific authority schema and exact source/negative-verdict strings are unchanged. All old physical identity, source/native hashes, flags, exact full argv, original deadlines, source limits, phase profiles, working mount and durable-copy fields remain required. `closed_scope_first_sha256` is a fresh ROOT administrative SHA of the focused review, not self-issued clearance.

Add `execution_mode` exactly `PREFLIGHT_ONLY_9` or `FULL_13`, and `allowed_phases` exactly the ordered applicable list below. Add `closed_child_guard` with EXACT keys:

- `schema`: `CAPRUN-closed-child/v1`.
- `outer_device`, `outer_inode`: actual positive JSON integers, freshly observed by ROOT.
- `root_no_migration`: true, a ROOT/exclusive/no-concurrent-writer trust commitment, not a claim to defeat malicious ROOT.
- `leaves`: a map whose keys are exactly allowed phase labels; each value has exactly `path` (canonical absolute string), `device`, `inode` (positive JSON integers). Each path is the direct child `cgroup_path/label`.

ROOT creates **all** registered leaves initially empty before dispatch, with no other child directories under the owned outer cgroup. Every leaf remains a leaf; no threading/delegation, external entrant, migration, replacement, removal, restart or concurrent ROOT writer is permitted during the batch. Outer and leaves must be `domain`, with empty `cgroup.subtree_control`. This is the legal populated-outer topology: controller limits remain on the outer domain; the leaves need no `memory.max`. The dispatcher/CAPRUN and transient monitor queries remain outer. ROOT verifies effective hierarchical memory, CPU and task constraints on the actual kernel and outer unit; no aggregate enforcement claim is added by this attachment.

ROOT owns the directories and migration/delegation interfaces; no group/other write bits, write-granting ACLs, inherited writable cgroup descriptors, user delegation or outside escape destination is allowed. Source UID/GID remain the original positive unprivileged IDs via the exact existing setpriv vector and no-new-privs. ROOT must ensure ancestry/mount ownership and no unexpected agent can attach to the child or move its members. A hostile ROOT is out of scope. ROOT—not these files—creates/removes leaves and handles terminal failures. Do not remove leaves until all original PIDs/groups are quiet, CAPRUN and dispatcher terminal, and durable evidence secured. No deletion, worker control or cgroup setup is performed here.

CAPRUN is opted into by selecting this new snapshot; it cannot fall back. Its eight new required arguments are `--closed-child-cgroup`, `--closed-child-device`, `--closed-child-inode`, `--closed-outer-cgroup`, `--closed-outer-device`, `--closed-outer-inode`, `--closed-boot-id`, `--closed-pid-namespace`. The dispatcher assembles them from immutable ROOT values and compares the **whole expanded command** to the ROOT vector. No new hash-token substitution is introduced. Only the old explicitly typed source authorization/positive-phase slots remain. The pinned CAPRUN hash is literal in the dispatcher; no source/self-hash cycle exists.

All unknown actual host, namespace, inode/device, native closure, review SHA, clocks, paths, commands and setup facts remain **UNFORMED**. This prose is not an enabled registration. A future copy must keep enabled false until ROOT's reviewed setup, exact file/hash/native/authority-vector checks and separate explicit release are complete.

## Placement, completion and failure semantics

The guarded CAPRUN is a single-threaded, ROOT Linux CLI. Before Popen it validates the canonical cgroup2 mount, boot/PID namespace, exact outer/leaf identity, ownership, topology and initial leaf emptiness, and its own outer membership. It refuses a current multi-thread process. It creates a private close-on-exec pipe. Both ends are explicitly preserved only into the trusted preexec stage; that stage closes the read end immediately.

The trusted preexec closure runs before setpriv/workload exec or any workload fork. It writes its own PID into the registered leaf, closes the only cgroup fd, rechecks actual membership/populated=1/session identity, applies the unchanged inherited CPU limit, writes a bounded identity attestation to the private pipe, then closes the write end. No cgroup or attestation fd reaches the workload. The parent verifies that attestation against the held, unreaped PID's actual start/PGID/cgroup and ROOT boot/namespace/leaf identity before permitting normal completion. Fast exit is supported through the zombie anchor and pre-exec pipe; missing/malformed attestation is failure, not an initially-empty success.

Kernel `populated=0` is now necessary on every normal or cleanup/bootstrap completion path. A ps scan without live rows while the leaf is populated continues bounded monitoring and increments a bounded-size scalar diagnostic count. RSS is still sampled in the exact PGID; it is not a kernel instantaneous RSS cap. TERM/KILL retain the original identity validation and bounded waits. The sole direct-PID bootstrap kill uses the held PID without invoking Popen's signal convenience method. Authoritative reaping is gated by kernel emptiness. Cleanup also retains the conservative old sampled-live-list predicate: stale positive scan information may cause a fail-closed STOP, never a false success.

Missing or changed scope/identity, malformed events, placement/pipe/preexec/Popen error, fast-exit identity loss, unreapable leader, unkillable task, surviving/escaped-PGID member or original cutoff violation is RUNNER_FAILURE/STOP. An escaped PGID cannot pass kernel quiet while it remains contained; this code does not add a new cross-PGID kill mechanism. ROOT's outer control-group cleanup remains required on failure. Even an error before a Popen handle exists cannot obtain successful cleanup from an unattested empty leaf. CPython's internal failed-exec/preexec error handling may reap its failed child; the trusted preexec makes no descendants, and the wrapper still requires terminal kernel quiet, while never promoting missing attestation.

`closed_scope` telemetry binds the leaf/outer identities, trusted placement identity, most recent kernel population/timestamp and count of empty-ps/populated observations. The dispatcher requires this evidence before accepting any child result; all input/output/native/argv checks remain. `group_quiet` checks every registered child leaf with kernel population plus the old outer-direct-process check. Thus terminal telemetry/custody cannot be consumed while an owned child subtree is live.

On an instrumentation failure, original bounded outer recovery may be necessary to collect evidence. This snapshot does not promise strict real-time termination: Popen/preexec and kernel I/O can stall, sampled observations overshoot, and cleanup can reach its bounded limit. The enclosing registered outer cap/timer must cover startup and every descendant. No extra sleep, favorable-observation loop, native allowlist relaxation, retry or budget increase is introduced.

## Exact phase modes and unchanged science boundary

Both modes start with exactly: `refuse-status`, `refuse-caps`, `refuse-inventory`, `refuse-source`, `refuse-hash`, `valid`, `startup-produce`, `startup-check`, `dummy`.

`PREFLIGHT_ONLY_9` registers ONLY those nine commands/leaves. `typed_slot_policy` is `{}` and `phase_policy` is null. After the strict unchanged dummy predicate, the caller requires exactly nine completed phase records, emits `CLOSED_SCOPE_PREFLIGHTS_9_CHECKED_NO_SCIENCE`, and returns without forming any of the four scientific phase authorities. The seven source-entry refusals remain actual source entrypoints, not successful scientific computation; the valid/dummy policies remain ROOT metadata-only. No positive source authority is minted in this mode.

`FULL_13` additionally registers `produce`, `check-positive`, `mutate`, `check-negative`, with exactly the old three typed slots and old positive phase policy. Its original wire/status, positive receipt, byte mutation/restoration, exact negative `CHECK FAILED: direct-source graph slot T`, exclusive outputs, freeze, caps and custody logic remain. Merely retaining this mode does not authorize it. The five scientific files were neither read nor changed in this task; ROOT's existing source-interface trust is retained, not broadened.

ROOT corrected the initial claim about mutate.py: its root_policy is embedded, not imported from probe. The explicit fourth-source delta licenses an owned mutator copy changing only that metadata membership/schema/mode attachment and identity metadata. Its mathematical main, coordinate edit and saved-slot restoration are unchanged. The probe's one fork, handshake, parent exit, TERM-ignore, 64MiB touch and pause are unchanged; event identity adds only cgroup membership, and the dispatcher additionally checks inherited leaf membership. Strict RSS, TERM, KILL, start/PGID/namespace, leader-reap and no-live checks remain mandatory.

## FIRST and one future discriminator — not authorized

FIRST must examine actual new source: preexec placement/pipe lifetime, held PID until quiet, all cleanup/error paths, ROOT leaf contract, legal outer topology, exact new argv/source bindings, embedded probe/mutator attachment and preflight-only non-escalation. All code including syntax is UNTESTED. No current worker/kernel/package attestation exists; historical kernel/systemd facts were supplied administratively but are not future bindings.

Only after FIRST and fresh ROOT release, plan **one** AWS-only nine-phase preflight batch, no science, no retry. Preserve control/dummy5wall,3softCPU/4hard inherited per process, dummy33554432B RSS,0.05s sample,1s TERM grace; eight other controls retain their old memory cap. Nine nominal child walls total45s. A120s overall observation/collection planning allowance is UNMEASURED and within, not an increase to, the existing3000wall/2100CPU/8GiB/128MiB outer contract. Leader CPU sums are not descendant aggregate guarantees. No actual runtime deadline is chosen here.

The discriminator requires the unchanged four-event dummy, real orphan contained after leader exit, RSS threshold, identity-matched TERM/KILL, actual closed-scope attestation, kernel quiet and terminal PID/group/custody evidence. No reproduced historical ps interleaving is inferred from ordinary success. Missing live evidence, unexpected outcome, runtime/source/native mismatch, cap or leftover task => STOP/NONDECISION. This is the one missing runtime evidence task after review, not an execution instruction.

## Primary documentary scope and qualification

[Kernel cgroup-v2 documentation](https://docs.kernel.org/admin-guide/cgroup-v2.html), retrieved2026-09-11: Processes (web lines77–98), Unpopulated Notification134–140, and controller/topology/delegation165–179 were read. These specify inheritance, hierarchical live population and the populated-parent restriction when controllers are delegated. Only those sections are relied on; no whole2191-line read or future-kernel verification is claimed.

[Official Python subprocess documentation](https://docs.python.org/3/library/subprocess.html), served as3.14.7, retrieved2026-09-11: preexec_fn/threads warning, close_fds/pass_fds and start_new_session (web lines258–290) were read. Only a standalone single-thread CLI is intended; future installed CPython compatibility still requires the focused review and bounded discriminator. No official source implementation, local interpreter or worker was executed/read for calibration. The two live documentation URLs have no independently saved raw-page digest in this packet; exact cited scope, not a fabricated source pin, is the documentary import.
