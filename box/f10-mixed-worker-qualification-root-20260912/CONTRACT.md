# Mixed-scalar startup and native-metadata adaptation

ROOT,2026-09-12. SOURCE-ONLY, UNREVIEWED, DISABLED, UNEXECUTED.
One client-specific adaptation of already reviewed sources, not a new runtime
framework or revival of the retired r3 deployment. No worker is authorized.

## Concrete decisions

The new dispatcher is the accepted metadata-envelope copy plus exactly
four Python startup flags -E -s -S -B in EACH of its two CAPRUN vectors.
The independent Astra message-only lookup20:31:41--20:32:20 identifies
default site startup and output-owned PYTHONPYCACHEPREFIX as avoidable
qualification obstacles. Choose explicit flags instead of building a venv.
The outer dispatcher must be invoked with these flags too. Science already
has them. CAPRUN's script argv and child argv do not change; science's typed
authority slots6/11 do not move. Existing root-owned pyc still require
provenance; -B is not a promise that Python never reads pyc. No CAPRUN source,
science algebra/checker, phase, schema or physical/scientific cap changes.

Use one future canonical root-owned immutable deployment root
/opt/jc2-mixed-20260912 with science/,runtime/,lib/ as separate directories.
science contains authority/produce/check only, runtime contains dispatcher,
CAPRUN and probe only; no source-directory pyc. lib is a private import
parent containing complete copies of Sympy and mpmath ONLY, including their
retained tests/pyc/resources. No package-member pruning, ZIP or venv.
Its source packages, installed bytes, versions/API and aliases must be
qualified on the actual worker. Imported-package paths are not assumed from
HQ. No globally writable library, site directory or optional third-party
backend is added to sys.path. Full standard-library and native dependencies
remain independently recorded. If private package copies are not compatible
with the actual source/closure or existing caps, STOP, no automatic retry.

The collector changes only file count3000->8192, adds the three exact
science/runtime/lib directory roots, and clarifies that its PYTHON_PATH
records are DECLARED base paths, not observed complete sys.path. All grammar,
recursion/ELF dependency discovery, physical-instance placeholder and other
limits remain. Only ROOT may instantiate that placeholder with the actual
fresh allocated campaign instance; ldd runs native loader code and collection
is AWS-only, separately bounded. The template remains disabled here.

The assembler changes its client label, both3000file checks to8192, its
required-root set to include env/systemctl and the three new roots, and
replaces its old two-binary job_tag/files subset by the exact complete
schema/files projection f10-mixed-native-files/v1. The legacy output filename
source-native-manifest.json now contains ALL full-map files, with a2MiB
serialization cap matching the accepted authority/dispatcher. All other
grammar, duplicate/ownership/alias/direct-child checks and publication code
are unchanged. The old r3-named facts/stats/receipt schemas remain private
administrative metadata and grant no r3 or mixed science authority.
The full six-field map remains<=1MiB, raw<=4MiB/20000lines, stat<=2MiB,
hash-list<=1MiB, aliases<=4000, directories<=1000 and WHOLE output<=4MiB.
These are joint bounds, not a promise that every8192file closure fits. Excess
is STOP. Projection is formed from the same validated dictionary; no subset
or separately asserted hash map stands in for complete files.

## Native trust and next actual-host qualification

ROOT must establish complete nominated stdlib/script/private-package paths,
actual startup path ordering, pyc provenance, dynamic/native dependencies,
aliases/absences and immutable ACL-free traversable ancestry with no writers.
The collector records are not proof of completeness, package behavior or
actual sys.path. New library directories must really contain both full
packages; the parser's mere root-presence test is not that observation.
Retain original authenticated terminal raw/facts and private assembly outputs;
install the consumed projection and qualification under traversable ROOT0444
outside source/native directories and the256MiB runtime tmpfs, then compare
their whole bytes and bind actual new source hashes. No runtime output is
preinstalled. The existing assembler raw/facts/receipt protocol remains.

Whole-job3540seconds INCLUDING cleanup,80percent fair CPU/burst0,32GiB
memory/AS,256MiB tmpfs/FSIZE and inherited CPU limits do not change. Exact
host/boot, one nondelegated/no-escape cgroup, dummy PID/readiness/RSS evidence,
independent original TERM/KILL, final kernel emptiness, durable transfer and
exact worker retirement must still be actually established. Preparation and
science need their own bounded registration before any code runs on AWS.
No sources have executed, even for syntax/version/import/dummy checks.

## Producer validation, no execution

ROOT inspected WHOLE old/new unified diffs and checked exact occurrence
counts while producing copies. Different-model FIRST must check the actual
three files, the vector/authority-index separation, complete projection and
joint limits before use. No extra generic supervisor, old clock bindings,
parameter farm, second gcd or silent cap adjustment follows. A successful
future scalar certificate would still be only the necessary model test, not
REG, complete actual arrays, source zero, allF10 or JC2.
