# Exact documentary scope

First action2026-09-11 15:38:05.188947836UTC. Original reserve15:47/HARD15:50 unchanged. TASK was hashed before WHOLE; all other11 inputs then hashed before fresh HUMAN WHOLE reads. No previous-turn read reuse.

Read exactly TASK, ROOT PLAN, ORIGINAL-CLOCKS and nine batch-b administrative files. Historical files read from line1 through EOF within bounds: physical52, native77, setup96, leaves69, final-install82, card89, policy47, admin-helper113, security38 lines. PLAN and clocks were fully returned in their first reads. No pinned/linked gate, source, native, manifest, policy target, worker or live report body was followed.

All nine output files were compared as complete text with their original after only the exact three TASK substitutions. Equality passed for each. This text comparison and occurrence counting used the orchestration's strings, not input/script execution. All nine outputs were returned for human readback; combined output clipped the middle of final-install, so final-install was reread WHOLE separately to recover all82 lines. The remaining eight outputs were visible WHOLE. Posthashes bind exact disk bytes.

All authored files use apply_patch. No input or output shell was run; no syntax/AST/import/test/dummy/scientific/native execution, worker/network/AWS/SSH/process control or source algorithm read occurred. Current host/clock observations and approval placeholders remain unformed. Source/job/error/phase/cap semantics are unchanged.

Remaining known binding: the card's embedded old batch-b policy SHA is deliberately unchanged because it is outside the three literal substitutions. ROOT was notified; new policy SHA is recorded in PINS.json. The card cannot be treated as a fully bound current execution card until ROOT handles that binding and all other placeholders separately.

Own report and manifest were absent before the lease; each nine-file target was absent before its addition. ROOT's TASK preserved. No new canonical OPEN item. Cheapest remaining documentary check is ROOT's exact port/policy-binding check, planning5minutes UNMEASURED; no runtime latency or readiness claim.
