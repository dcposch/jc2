# Root registration: one univariate representation validation

Registered2026-09-09 after qualified17p/17t/17u acceptance. Root chooses the
entire exact worker/source/cap vector; delegated executor Volta may not change
these files, sources, deadlines, profiles or acceptance. Execution remains
conditional on fresh physical/current pin checks below. No solver authority.

Only i-08d2a40f272ee9fa2, c7i.4xlarge, Owner
f10-r1-exact-artifact-validation-20260909, EBSvol-0574e0fa5aed1f5e3, us-east-1.
Fresh AWS state STOPPED20:49:53; repeat exact identity/ownership/state and
current quota/aggregate usage before start. Use fleet run/push/pull only after
fresh private-IP resolution. Do not run fleet wait (installs software), launch,
term, term-all, ips or new infrastructure. No other workers or disks touched.
Root independent exact-worker STOP timer21:16 UTC must be confirmed ACTIVE
BEFORE start. Executor stops promptly after terminal collection; root cancels
backup only after positive fresh STOPPED. Retain disk and all artifacts.

Fresh absent remote cwd /home/ubuntu/f10-r1-univariate-runtime-validation-20260909.
Actual Linux/Amazon EC2 DMI/board_asset identity, hostname and interpreter
must match dispatch.registration.json BEFORE mathematics, all13staged regular
files and Python hash verified. Create no plain/descendant file. Registration
itself is root-written/externally pinned/chmod444, no concurrent writer.
The accepted normalized receipt is input normalized.full.receipt.json;
the NEW receipt is univariate.full.receipt.json. Root acceptance6451f6b4
is already issued after genuine fullcheck and independent17t evidence review;
its exact payload is copied into this registration, not minted by executor.

One finite batch: seven accepted preflight children, then one adapter.main,
one checker.main, one harness with seven in-process semantic negatives.
No positive rerun, repair, retry, alternate engine, factor, source-power
expansion, source point, ideal decision or automatic follow-on.
Profiles in JSON: adapt120wall/110CPU/2GiB, check120/110/2GiB,
controls240/220/2GiB; probe5/3/2GiB,dummy5/3/32MiB. Aggregate600wall from
first math, original absolute math cutoff21:12, collection/report21:15,
worker stop21:16 UTC. Earlier first-action+20min also controls task; NEVERreset.
15second admission, storedminimum/postreturn check and16MiBfilecap unchanged.
Pilot caps are not a forecast. Every failure is STOP/NONDECISION.

Use remote unit jc2-f10-r1-univariate-runtime-validation-20260909.service,
uid/gidubuntu, exact /usr/bin/python3 -I -B dispatch_batch.py in that cwd,
RuntimeMax900s/TimeoutStop5/control-group/MemoryMax3GiB; exclusively named
outer stdout/stderr. Capture actualstart/MainPID/invocation and promptly
check actual child tree; if already terminal, report that honestly. Do not
read result payload until exactunit/MainPID/ownedcgroup allterminal/absent.
Capture allownedfile hashes before payload reads; pull and verify allbytes;
stopworker promptly. Original accepted admissibility/frontier are unchanged:
actual total112/196, partialU28/49,V84/147, compact4/7nonconstanttarget; this
is not newclassicalopenness or sourcecoverage. No local mathematical process
of ANYsize/import/compile/test; all registered children run on this worker.

Publish own transactional execution report and custody including allcurrent
source/inputpins, terminalremoteoutputs, rootregistration, lifecycle/stop,
actualreceipts/telemetry/readscope. Root collection and FIRST independent
runtime-evidence gate still required. No univariate acceptance is issued by
mere status or this registration; engineering PASS is not unitness/properness.
