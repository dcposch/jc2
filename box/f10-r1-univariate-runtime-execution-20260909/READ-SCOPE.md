# Runtime read scope and chronology

First action20:57:25 UTC, report absent; controlling21:15 task cap,21:12 math
cutoff, root21:16 stop timer unchanged. Three existing root files were pinned
and read WHOLE and never edited. Exactly22 local input pins are recorded in
input-pins.json. The13 staging inputs matched both locally and as regular
remote files; remote Python independently matched the registered hash.
All six frozen adapter/backend/helper/probe bodies and two caller/harness
bodies have same-byte earlier WHOLE reads reused after current hashes.
No source or caller edit, import/compile/test locally, CAS, solver, coefficient
arithmetic or alternate experiment. CAPRUN body was not re-hardened; its
accepted exact hash was checked. Fleet rules/script and two new terminal gates
were read WHOLE; root's exact current registration controls historical examples.

The incoming normalized coefficient body was HASH ONLY locally. No univariate
or fixture coefficient body was parsed for arithmetic or recomputed. After
terminal custody, all10 authority JSONs and all10 telemetry JSONs were loaded
for documentary argv/cap/path/hash comparison; batch/preflight/full receipt/
controls receipt and dummy stdout were read WHOLE. The removed dense-slot
wire in controls.receipt was seen as strings only. Both sentinels, allfive
refusal stderr bodies and three mathematical stdout bodies were read.
All100 remote files were hash-captured before ANY result payload read,
then all100 transferred files passed sha256sum -c. Uncharged provenance
paths, unrelated tasks, ledgers, protected paths and live peer reports were
not followed. No large actual-source expansion occurred anywhere.

Observed initial live owned cgroup at21:00:15: caller2045, CAPRUN2054,
probe2097; a documentary stat/argv summary is retained, not a byte-identical
raw live-tree transcript. At21:00:49 the exact unit was inactive/dead,
caller2045 and cgroup absent. Exact initially observedchild2054/2097 absence
was checked before terminal hashes. Final transient-unit properties had
already reset, so no final outer CPU/peak/exit timestamp is claimed.
The terminal caller receipt and per-childtelemetry carry the actual endings.
AWS STOPPED positively confirmed21:01:40, root independently21:02:08.
Root cancelled its backup afterwards; no further worker operation occurred.

One local metadata comparison initially treated JSON object key order as
significant and stopped before writing output. Key-sorted metadata comparison
corrected that documentary false mismatch; no remote rerun, code change or
scientific predicate was executed. Metadata reconciliation is not a new
mathematical checker. It records all10 currentauthority/telemetry links and
allrootacceptance payloads, with intended refusal defects preserved.
The three root-issued files and all staged source bytes remain immutable.
