# Documentary read and static-delta scope

Owner: /root/model_productivity. First action 2026-09-10T03:08:04.045058959Z;
targets absent. Stop 03:24:00 UTC, reserve from03:22; no reset.

Exactly eight charged inputs are enumerated in input-pins.json with original
paths, current hashes, bytes and WHOLE read scope. All eight matched before
their body reads. All bodies were read completely during this task. No
provenance links, other scientific reports, runtime payloads, source data,
unlisted helper/plan/harness body, ledger, Git state or live peer was read.

The three old code snapshots have the exact original hashes. Authored
newsolver.py differs only by the one minimum-nonzero-degree selection and
two comments. The two separately derived dispatch callers each replace
only one literal solver SHA. Their unchanged portions inherit the complete
reads of their respective original bytes; the three exact changed hunks
were separately read and inspected. The literal diffs are regenerated by
the ordinary text diff command, not a Python/AST/mathematical execution.

Manual control identities and the proof reside in the report. No fixture
or mathematical data file is generated. No new or old scientific Python
was parsed, imported, compiled, tested, executed or type-checked. Only
existing documentary artifact-finalize commands and metadata hashes were
executed. No subprocess involved mathematical computation of any size.

OWNED CODE (not deployed):
- oldsolver.py -> newsolver.py, solver.diff;
- old_generic_dispatch_batch.py -> generic_dispatch_batch.py,
  generic-caller.diff;
- old_actual_dispatch_batch.py -> actual_dispatch_batch.py,
  actual-caller.diff.

Do not interchange the two dispatch callers. No actual-source scientific
sibling, checker, harness, runner, registration, authority or source
acceptance is copied, modified or issued. Future runtime packaging uses
solver.py plus exactly the separately selected dispatch_batch.py and must
be independently reviewed and registered by root. This packet provides no
execution, restart, cap-increase, test or further-task authority.

Own report was read WHOLE before its completion marker. Own-only raised
OPEN search and final-path collision check are performed before closing
the lease; no canonical OPEN ID is created. The final custody lists all
owned files except custody itself and all eight unchanged original pins.
Expected transaction verification is part of terminal documentary custody.
