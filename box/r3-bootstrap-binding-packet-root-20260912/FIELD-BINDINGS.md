# r3 deployment bindings — inert preparation, not launch authority

This packet fixes worker-independent paths and records where each remaining
field must come from. It is NOT an approved spec/card, installed package,
native/host qualification, runtime observation or mathematical result. No
campaign program was run to create it. No worker is selected. The canonical LIVE STATE
in notes.md remains the only execution queue.

## Frozen inputs and selected changes

- PATHS.json: b7bc34efd174324264a381fb0ec35679cd2d23cc49bebac5b55004b001cd8338.
- preflight-policy.json: 6e831ef55d313b487ade1c4aa0ba9f5ae21dc5189e70b4c9535ebb608bf901a1;
  3432 bytes, canonical compact sorted ASCII JSON plus LF.
- ROOT-SOURCE-CONTRACT.json: 6c285301fc08d3960e136eff22dc88da89b7bb8d5e3133f0238462404b27d813;
  4443 bytes, canonical compact sorted ASCII JSON plus LF.
- Ten existing source locations/pins are unchanged from
  ../r3-bootstrap-preparation-root-20260912/STATIC-SOURCES.json,
  SHA8dd2eb7b59e4fb49462019e7d10745c7f3a8b27198fe4be053fe03f91fa4ebc7.
- Bootstrap remains f04f1b7b16bed194e5d7b503a78fe9bd36e3bd58da3696adf6a40bfdbdcd17d2;
  launcher remains c3b42e78f01e469594c9d17f105bdffb42fcff2e79e33a131f72be9bff91b7e7.
  Their accepted source contracts are respectively
  21c1c0bd6840ac7aa80a33e9ff2c7a88bab87f51fd7a669ed6e27ba30b32bfeb and
  c6b034149a550451ad11d69a12be389bde68f3159d6bf688f31cc887fe38780f.

The refusal policy derives ONLY from the September12 batch-a policy
a2531b052eab61dde7517bee8417a20b2bfd4664b93bc1ba2732a879d20ced2e.
Exactly sixteen occurrences of /opt/jc2-closedchild-preflight9-20260912a
become /opt/jc2-r3-bootstrap-20260912. Metadata comparison confirms all
seven entry objects otherwise unchanged, including exit1, empty stdout and
literal LF-terminated stderr. The parent's pretty bytes would fail the
bootstrap's canonical re-encoding comparison, so its old digest is NOT reused.
Current authority/producer line references agree with the recorded traces;
actual installed CPython stderr formatting is still unobserved.

The new contract retains source/review lineage but explicitly excludes ALL
historical host, clock, holder, controller and batch bindings. Its old parent
2b713e37b54dad0ca59262b556b9358e0754fb7d3938a3b1edad593ac8c98f11 is
history, not the current authority. This is a new DATA binding, not a claim
that the old contract bytes are unchanged. No new approval is issued.

## Installation mapping and custody boundary

Let P mean the literal PATHS.json prefix. Ten programs map to P/ROLE using
STATIC-SOURCES.json; launcher.py maps separately to PATHS.launcher. Science
and wrapper directories must each be exactly ROOT0755 with exactly five
and three named source files. Program/source/reference files are ROOT0444,
with canonical nonsymlink paths, ACL-free ROOT-owned traversable ancestry
and no group/other-writable component. These are FUTURE requirements, not
observations about proposed paths or about this HQ preparation directory.

REFERENCE-INSTALL-MAP.json supplies seventeen distinct prospective source_pins
destinations: five science files, the eleven unchanged source references,
and the new contract. Reference destinations retain the complete repository
relative path below P/references; no basename flattening or /home/ubuntu
ancestry is imported. Its source documents retain their existing accepted
tiers; current SHA checks are not fresh whole-body mathematical reviews.

Keep assembler OUT0700 as private preparation custody. Separately install
its TWO consumed JSON manifests at PATHS.native_manifest and
PATHS.source_native_manifest, ROOT0444 under ROOT0755/traversable ACL-free
ancestry. Require whole-byte comparison against the private outputs, actual
installed digests, modes and identities. Retain raw/facts/stat/hash-list and
the assembly receipt privately. The receipt alone does not bind installation.
The source manifest is the exact current-job subset, not a historical alias.

All static pins and these JSON inputs stay outside the working tmpfs.
The metadata directory/registration parent must be OUTSIDE native.directories;
no metadata, manifest or reference file is inserted into exact science/wrapper
inventories. No spec, registration or bootstrap-binding self/generated pin is
allowed. Native inventory lists must not include these future generated files.

Ordinary0755 env/systemctl require their literal CANONICAL file paths in
native.files, not merely aliases. The collector visits both, but its assembler
requires only the original seven roots; the ROOT binding must attach the
additional two actual FILE entries. Proposed /usr/bin/python3.12, setpriv,
env and systemctl are unqualified choices. An alias, unsupported path, unsafe
mode, extra sys.path alias, unexpected native child or byte ceiling means
refusal, not normalization or a silent cap increase.

## Field provenance: bootstrap spec

The passing runtime schema is r3-exec-bootstrap/v1, with exactly SPEC_KEYS
from the pinned bootstrap. No runtime-schema example or passing approval is
emitted here. Canonical UTF-8/ASCII JSON plus LF, ROOT0444, at most256KiB.
STATIC means an existing accepted source fact; OBSERVED means a fresh actual
worker fact; QUALIFIED means the cited method AND installed observations have
really been established. A SHA-shaped string cannot substitute for evidence.

| Spec field(s) | Required origin/binding |
|---|---|
| schema | Literal runtime schema above, only in a later genuine spec. |
| approval | Separately issued ROOT approval after the applicable gates; absent here. |
| bootstrap_sha256 | Unchanged f04f1b7b... program, with actual installed byte equality. |
| bootstrap_first_sha256 | Accepted repaired-bootstrap intake ec471d42f87a156295daa068d3778bba0470bf39fa891f007d7d124e61a1f765; STATIC only. |
| launcher_qualification_sha256, native_qualification_sha256 | Genuine prior method/installed qualification records, still missing; same values in card. Do not substitute a static FIRST or synthetic record. |
| registration_path, binding_path | PATHS.registration and PATHS.binding, both actually absent before bootstrap. |
| binding_deadline_utc, binding_wall_seconds | Original epoch-relative deadline, no later than epoch+120; integer120 unchanged. |
| cgroup_namespace, mount_namespace | OBSERVED actual service/physical-host namespaces, equal to host PID1 namespaces. |
| outer_started_utc | OBSERVED first watchdog activation, floor to its actual UTC second; NOT a planned or later process-start time. |
| outer_resources | OBSERVED exact memory.max8589934592, memory.swap.max0, pids.max32, cpu.max.burst0 and cpu.max6900 10000 for proposed quota69; require BOTH outer and task scope. All resource values strings. |
| external_supervisor | Exact seven keys in source; genuinely already armed, no restart, covers bootstrap/exec/descendants, qualified evidence SHA, unit=PATHS.unit, outer cutoff no later than task cutoff and retirement cutoff equal to worker cutoff. |
| exclusive_root_contract | The exact six source keys: no_concurrent_writer, no_entrants, no_migration, no_replacement, no_delegation, no_inherited_cgroup_fds. Each true ONLY after actual qualification, not asserted now. |
| registration | Disabled exact-key skeleton specified next; bootstrap alone later fills its enumerated late fields. |

## Field provenance: registration skeleton

The source literal is f10-necessary-rows-runtime/closed-child-v1; code controls
over the slash typo in older prose. These grouped rows cover REG_KEYS.

| Registration field(s) | Required origin/binding |
|---|---|
| schema, jobtag | Exact source schema above; jobtag=f10-source-cone-r3-necessary-rows-v1. |
| enabled, commands, typed_slot_policy, phase_policy | false, empty object, empty object, null initially. No precomputed authority commands. |
| exclusive_no_concurrent_writer | true ONLY from the qualified exclusive ROOT contract. |
| execution_mode, allowed_phases | PREFLIGHT_ONLY_9 and exact ordered LABELS in new contract; no scientific phase. |
| closed_scope_first_sha256 | Existing accepted bbd12d303650e5bbba63200076884a1b6d0bf7e5c43c0e1d2857731a0aa2b53d, STATIC scope. |
| source_files | Exact unchanged five-name/hash dictionary in the new contract. |
| review_clearance | Exact status/source_first_sha256/runtime_first_sha256 object. Existing accepted source and runtime refs are both06804680dbb19e08a2ef0ce44a2d97797df31ac8c8ca835741610cc5f9118a48; SOURCE_AND_RUNTIME_FIRST_CONFIRMED retains only that accepted scope. |
| source_limits, limits | Unchanged string-valued900s/600CPU-s/8GiB/128MiB and3000s/2100CPU-s/8GiB/128MiB respectively. |
| profiles | Exact source PROFILES: control[5,3,8589934592], dummy[5,3,33554432], produce/check[900,599,8589934592], mutate[30,20,8589934592]. Small fixed resource integers, not scientific data. |
| environment | Exact PATH=/usr/bin:/bin, LANG=C, LC_ALL=C, TZ=UTC; actual process environment must match. |
| uid, gid | Proposed65534 each, positive integer only after the actual child identity/ownership arrangement is qualified. PATHS stores choices as strings, not a runtime skeleton. |
| instance_id, hostname, boot_id, pid_namespace | OBSERVED current physical EC2 DMI/hostname/boot/namespace facts; no old worker or HQ values. |
| cgroup_path | PATHS.outer_cgroup, checked against actual service membership. |
| closed_child_guard | CAPRUN-closed-child/v1; qualified root_no_migration; outer_device/inode initially null; exact nine direct leaf paths cgroup_path/LABEL, each with device/inode null. Bootstrap creates empty leaves and observes their identities. |
| aggregate_cpu_start_usec | Literal string0; fresh whole-task accounting history is independently qualified, not inferred from one current counter. |
| files | Exact six roles: proposed python/setpriv; caprun=PATHS.caprun; dispatcher/probe/mutator=PATHS.wrapper_dir/{dispatch,probe,mutate}.py. |
| science_dir, wrapper_dir | PATHS fields plus actual exact0755 and contents checks. |
| source_pins, contract_sha256 | Seventeen installed destinations/hashes in REFERENCE-INSTALL-MAP.json; new contract pin6c285301... . Five science paths repeat identically in pins. |
| place | Exact string data: p523, degree1, phi[0,1], P7_mod_p[0,17,249,53,102,191,291,1]. Every scalar is a decimal STRING. No new arithmetic, good-place, rank or source-point claim. |
| outer_argv | Literal [files.python,-I,-S,-B,files.dispatcher,--registration,PATHS.registration], not a shell command. |
| pins | All actual installed five science/six runtime/source-wrapper/bootstrap/launcher and three launcher-tool bindings, plus consumed native manifests/policy and any explicitly chosen additional immutable inputs. Consistent duplicate paths collapse to one key; no self/generated pins. |
| preflight_policy, native_manifest, source_native_manifest | Exact PATHS fields and actual installed digests in pins; canonical JSON caps64KiB/1MiB/64KiB respectively. |
| output_mount, authority_dir, frozen_dir, writer_dir | Exact PATHS fields; fresh root0755 tmpfs at most128MiB. Initially only empty ROOT0755 authority/frozen, empty child-owned0700 writer, and outer.stdout/outer.stderr. |
| durable_dir, durable_device | PATHS.durable_dir actually absent; its traversable ROOT parent on qualified EBS ext4/xfs with freshly observed positive integer st_dev. |
| mathematical_deadline_utc, task_deadline_utc, admission_deadline_utc, worker_deadline_utc | Original registered deadlines satisfying binding<=admission<=math<task<worker, task<=epoch+3000. Explicit +00:00 UTC; never reset from later service/exec times. No actual timestamps chosen here. |

The bootstrap, not an external holder, later fills device/inode identities,
the nine commands and enabled=true, then exclusively publishes registration
and its binding receipt. Its receipt is PREEXEC_BINDING_ONLY_NO_PREFLIGHT_RESULT.
No registration/binding/authority file is created in this packet.

## Field provenance: launcher card and unit commitment

Exactly source KEYS for r3-bootstrap-launch/v1; canonical JSON plus LF,
ROOT0444, at most256KiB. No passing card is emitted here.

| Card field(s) | Required origin/binding |
|---|---|
| schema, approval | Exact schema and separate later ROOT approval; absent as an executable card here. |
| launcher_sha256, launcher_first_sha256 | c3b42e78... unchanged installed code; accepted STATIC intake e401fd9f017196a8c711b2bf4198b1f0b8b2ff38c8246abe9d34d0c04e83068e. |
| launcher_qualification_sha256, native_qualification_sha256 | Genuine prior qualification, identical to spec references; still unobserved. |
| accounting_qualification_sha256, retirement_qualification_sha256 | Genuine original-scope accounting and independent worker-retirement qualification, not source acceptance; still missing. |
| python, env, systemctl, tool_pins | Actual canonical qualified tools and exact three-entry installed hash map; repeated in registration.pins. |
| bootstrap, spec, spec_sha256 | PATHS.bootstrap/PATHS.spec and actual frozen complete spec digest. |
| unit, slice, watchdog_timer, watchdog_service | Four distinct literal PATHS names, with required source regexes. |
| epoch_utc, epoch_timestamp_raw, epoch_timer_monotonic_usec | Actual first activation's UTC second, literal systemd timestamp rendering and positive decimal-string monotonic microseconds. No guessed epoch. |
| task_scope, task_scope_device, task_scope_inode | PATHS.task_scope and actual positive integer directory identities of the fresh accounting ancestor. |
| cpu_quota_percent | Proposed string69 only if qualified; actual quota/period must match BOTH ancestor and outer resource fields. |

Order: qualify prerequisites -> arm independent watchdog ONCE -> observe
original paired epoch -> freeze spec -> freeze spec-pinning card -> install
the exact outer unit containing card SHA -> separately bind all final loaded
unit/readback commitments OUTSIDE the card/spec. The watchdog's two units
depend only on the preselected names. No circular card/unit/spec hash exists.
No installation/rendering/start operation was performed here.

The one selected115+5-second watchdog covers the WHOLE preflight experiment;
it is NOT cancelled after binding. It stops both outer service and task slice;
watchdog itself stays outside that slice. Actual stop propagation, every
administrative member's at-most-five-second termination contract, loaded
unit equivalence/no overrides and independent worker retirement need real
qualification. All post-epoch setup/freezing/reload/start/custody consumes the
same time and the administrative task-slice resource envelope. Entry requires
both clocks less than100seconds and drift at most2seconds. RuntimeMax2875+5
is only the later fallback, not a renewed epoch or permission to continue
past the earlier watchdog. No fit or hard-realtime guarantee is asserted.

## Completion boundary and next discriminator

This finishes the chosen path/source-reference/field-PROVENANCE map, not the
complete host qualification procedure. Remaining work is concrete: specify
and independently assess the actual-host collection/installation/accounting/
watchdog/retirement observation procedure before considering a worker.
Unknown installed/native/host/epoch fields remain unknown. No unchanged-code
re-review, replacement controller, old-batch retry or cap increase is selected.

Current checks are bounded metadata/text/hash checks only. No import, AST,
syntax execution, fixture, native collector, assembler, bootstrap, launcher,
dummy or science execution occurred. All new bindings remain INTERNAL,
UNREVIEWED, UNINSTALLED; accepted program FIRSTs retain their original STATIC
scope. Authentic r3 rows/297rank remain UNRUN and JC2 remains unresolved.
