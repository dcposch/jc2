# One-client mixed-scalar operator procedure, third-attempt preparation

ROOT, September13. This is an INERT prospective procedure, not an allocation,
actual qualification, enabled registration, or scientific result. Scope is
ONE unchanged all-integer-r Bezout DATA-certificate test. Neither a positive
scalar result nor closure of all F10 would resolve JC2.

## Why this is a changed execution plan

The first two workers expired while ROOT prepared separate operator calls.
This attempt instead prepares the entire fixed sequence before allocation:
one PREPARE call executes the four existing administrative stages; literal
dummy and science templates need only physical/clock binding. No review,
source development, package installation, or discretionary compute is planned
while the worker is billed. Any missing preparation item means DO NOT LAUNCH.
At most one r7i.2xlarge, 8vCPU/64GiB; no larger cap or repeated attempt.

## Frozen templates and field bindings

All paths below are relative to the campaign root unless absolute.
Seven staged files in this directory's stage/ are exact, read-only copies:

| File | SHA256 |
|---|---|
| authority.py | ecdd37dfb900a13dca6dac03dcec3a608c7fd575d3ffccb1ec3976fd400b2097 |
| produce.py | 8aee305b5fc32f0a20f2bb8ccfe01ac1c389c98594ea7724070e1badfae2abe0 |
| check.py | 1ab7f3b3e08178eecabd1b2af86620554a9006fbfffe35378c92940c27a4a5a5 |
| dispatch.py | 079f9ad7b92e64af5c5fc6a5e17de14ffa5245627ee5c8a4ce0ef2a018a2fa5d |
| run_capped.py | 4435279df8f987c667ebc2fae8e4bd987916032561cb3faf1e07a57789d196c2 |
| probe.py | 16d723b7fbe6dc218351274e4078df1959add5c2914174079d707fdc14e17158 |
| native_records.py | 63178c8aeab6d2f81bfddf4cdcc732ee00204c73f68f43c5f04761a2dfe4be9a |

Additional sources:

- PREPARE: box/f10-mixed-dac-correction-root-20260913/PREPARE.template.sh,
  482c50538c144aef7d41aac834aca43bb2f8108c8a14017a2e85fa155cd55d6f.
- Collector: box/f10-mixed-native-cat-root-20260912/native-metadata.template.sh,
  f1e94553d2a72462a76d667491c7f8aa99d6cc572d2e3753766c7a8269f0f334.
- Inspector: box/f10-mixed-native-operator-root-20260912/inspect_native.template.py,
  15f3dabc1d15c471b625b0a1bd0dd64dd429bbfe449b4b3c3d3b01e5ea7504cf.
- Import observer: box/f10-mixed-import-qualification-root-20260912/inspect_imports.template.py,
  1a005a7d342b51809fb4234ef706ee49a96d412c077dc4cc4db6c4360cb8eeb5.
- Dummy: box/f10-mixed-dac-correction-root-20260913/COMMANDS.md,
  7809c8ae3733d5e765cec4d5018e57dc4b90d30422dbef66f1f89882fee8cd70.
- Science unit: this directory's science-unit.template.sh,
  338e45cef2e50e5bdedf25618bcd4ede02bba0f1c26f9faea8b0762ef064b788.
- Runtime DATA skeleton: this directory's REGISTRATION.disabled.json,
  5533dba7d3bf8a7557026d6ae3312f4085da0c0f48769b0db1aa4d5789c3d711.

The first three metadata templates substitute ONLY JC2_INSTANCE_PLACEHOLDER.
Store actual derivatives as native-metadata.actual.sh, inspect_native.py and
inspect_imports.py. Compute their actual SHAs; substitute those, the actual
instance ID, dispatcher SHA and original qualification TERM/STOP calendar
strings in PREPARE. No unresolved JC2_*_PLACEHOLDER may remain. Whole-read
and hash each derivative before transfer. The installed base remains exactly
/opt/jc2-mixed-20260912 because accepted sources name it literally.

Dummy binding is mechanical extraction of its sole shell code fence. Bind
actual instance, boot, hostname, PID/cgroup namespaces, UID=GID=65534 only
after inspecting their actual status, exact canonical executables and new
unit/timer names. Use unit jc2-mixed-v3-dummy-20260913 and fresh mount
/opt/jc2-mixed-dummy-v3-20260913. Python/setpriv/jq are /usr/bin/python3.12,
/usr/bin/setpriv,/usr/bin/jq, pinned from the same actual native inventory.
CAPRUN/probe use the runtime paths above. Child argv count12, compact ASCII
JSON PLUS LF SHA e585a9fa1c85c2a84ccec59c96df6857cf3514822edb2c435ce1db01cb0eb8d5.
TERM and KILL are admission+20/+25 seconds, spelled YYYY-MM-DD HH:MM:SS UTC
for systemd. The entire wrapper, comparisons and kernel cleanup must finish
within that original25seconds and before qualification STOP. Register one
future admission, install/hash/whole-read before it, then invoke exactly at
that admission; a missed admission is STOP, not a new clock.

The science UNIT binds actual identity/namespaces/UID/GID, Python and
dispatcher pins, the SHA of the enabled registration, original admission,
deadline-minus5seconds and deadline. Its three source instants use ISO-Z;
the template itself renders systemd calendars with checked equal epochs.
Runtime registration instants MUST use equivalent +00:00 strings, because
the scientific authority requires that spelling. Use unit
jc2-mixed-v3-science-20260913 and distinct science TERM/KILL timer names.
All __...__ tokens must be absent before use. No task renews a clock.

## Allocation and fixed retirement ownership

Before allocation, independently query BOTH regional nonterminal selectors
jc2fleet=1 and Name=jc2-worker-*, Standard quota L-1216C47A and current usage;
verify AMI ami-066263bf15bd856de/root device, subnet-948915c9 and
sg-09ffa8932558f0a79. Profile personal, region us-east-1, key jc2-fleet.
Use minimal cloud-config4027741d03557921c387447f0161ad6ae1974f009a334b7a884aea1fe9ed1df6
from box/f10-mixed-qualification-execution-root-20260912/cloud-config.yaml.
Do NOT use fleet launch/wait userdata, pip upgrades, optional installers or
the baked campaign tree. Root100GiB gp3 has DeleteOnTermination=false AT
LAUNCH. Enable API termination. Never act on HQ i-0252f535410c26ebc.

Freeze actual UTC qualification STOP approximately20minutes after the
allocation decision, TERM five seconds earlier, and worker RETIRE five
minutes after STOP, in a separate actual registration before run-instances.
Bind returned ID/IP/root volume and independently verify termination-enabled
and retained-volume settings. Within60seconds of allocation arm a verified
HQ exact-ID retirement timer; command is the resolved AWS CLI, profile and
region above, ec2 terminate-instances --instance-ids EXACT_ID. That timer
does not depend on SSH or this conversation. No worker survives while ROOT
writes missing procedure/source. Poll authoritative state, not an old IP.

Only a completely qualified and ACTUALLY live registered science unit before
RETIRE can take ownership. First arm the new exact-ID retirement at the
original science deadline+5minutes; keep the old timer until live science
identity/control checks pass. Then stop/verify the old retirement timer.
Otherwise retire on the original clock, with EBS evidence retained.

## Actual preparation and qualification

1. Require cloud-init terminal success; read only system metadata for actual
   EC2 ID/vendor, boot, hostname, namespaces, kernel/systemd/packages,
   canonical executable paths and UID/GID65534. Check full apt SymPy/mpmath
   trees and jq already exist. No package installation fallback. Keep fresh
   source/native targets and read-only pins separate from observations.
2. Transfer the seven staged sources, three bound metadata scripts and bound
   PREPARE to /home/ubuntu/jc2-mixed-v3-stage-20260913. Use scp/SSH with
   /home/ubuntu/.ssh/jc2-fleet, BatchMode=yes and ConnectTimeout=10. Compare
   all actual local/remote SHA values before sudo /usr/bin/bash PREPARE.
   Save its actual shell return code and full bounded stdout/stderr. PREPARE
   installs source files0444, complete private packages and prep256MiB tmpfs,
   arms global qualification TERM/KILL, and executes the existing four calls.
3. Start PREPARE as an observed SSH command with a live tool handle. Within
   60seconds capture exact jc2-mixed-v3-native-20260913.service MainPID,
   ControlGroup, ExecStart, active state and resource properties. Read the
   actual main PID's stat/start identity, exe, cmdline, limits, cgroup and
   namespace links; capture /sys/fs/cgroup/system.slice/UNIT.service's
   cpu.max, cpu.max.burst, memory.max, memory.swap.max and cgroup.procs.
   Expected CPU80000/100000, burst0, memory1073741824, swap0, Tasks64,
   AS1GiB, FSIZE4MiB, runtime595/stop5, no restart, control-group kill.
   Evidence is stored outside the science mount. A successful launcher alone
   is insufficient; missing actual live observation is qualification failure.
4. Require all four saved *.wait-exit to be0 and all four stderrs empty.
   Read the entire assembly receipt and compare every named output SHA/byte
   count. Full manifest<=1MiB, file projection<=2MiB, stat records<=2MiB,
   native.sha256<=1MiB, receipt<=32768, joint assembled total<=4MiB;
   raw<=4MiB/20000lines, files<=8192, directories<=1000, aliases<=4000.
   Verify identical complete file maps, every literal collector root present
   directly or by its recorded alias, independent actual header/package
   facts, and inspector/import statuses. Metadata success does not assert
   a scientific import, operation, native-completeness theorem or result.
   Read imported version/backend/path fields; retain/hash the whole module
   map. Same startup prefix/flags/source-call composition supplies the
   scientific startup attachment; do not call metadata-prefix observation
   an actual science-prefix runtime test.
5. Ensure all four metadata services are terminal and their exact kernel
   cgroups absent. Stop/verify qualification metadata timers when all their
   work is finished; preserve their arming and terminal evidence first.
   Actual --wait results are primary exits. Missing units can show exit0,
   Result=success and ExecMainStatus=0; those defaults prove no job ran.
6. Execute the one separately bound dummy. Preserve wrapper exit, actual
   wait125/ExecMainStatus125, exact two-line output, canonical child identity
   and rlimits, namespace/cgroup facts, zero child CapEff and NoNewPrivs1,
   leader-versus-child PID/shared PGID, RSS>33554432 and TERM then KILL,
   reaping/cleanup_complete and final expected kernel cgroup absence.
   All expected strings/hashes/caps are already in the accepted commands.
   ROOT timestamps completion AFTER final census/hash; entire admission to
   completion must be<=25seconds. Failed or absent predicates mean STOP.
7. Copy prep/assembled/source-native-manifest.json to the fresh traversable
   meta/source-native-manifest.json with root ownership/mode0444; use cmp
   on the complete bytes and match both hashes. Do not expose assembler's
  0700 directory as a science input. All parents root-owned0755/nonwritable,
   ACL-free; private libraries and native originals must remain unchanged.

Create meta/qualification.json ONLY after ROOT actually checks all preceding
predicates. This small DATA receipt records schema, qualified worker/boot/
host/namespaces, actual UTC observations and exit values, source pins, hashes
of native raw/facts/five assembled outputs, native/import stdout, dummy
stdout/telemetry/terminal census and ROOT's signed-scope statement. Keep
each large map in its original artifact, referenced by hash; q<=65536bytes.
Status must distinguish METADATA_AND_DUMMY_CHECKED from mathematics; never
claim JC2, native semantics or scientific correctness. Canonical ASCII JSON
plus LF, root0444, no placeholder/null identity. Whole-read/hash/cmp receipt
and underlying bytes; preserve original artifacts durably before science.

## Enabled runtime registration and six phases

Only after that truthful qualification, mechanically fill the disabled
16-field runtime DATA template with jq --arg for strings and --argjson for
the small UID/GID65534. The previously fixed 64-bit caps are unchanged;
no mathematical integer or rational enters jq numeric processing. Set
enabled=true, worker/boot_id/hostname from the same observed worker,
not_before/deadline to freshly REGISTERED original instants spanning3540s,
uid/gid65534, pins.python/setpriv from actual canonical native files,
native_manifest.sha256 from the complete copied projection, and
qualification.sha256 from the ROOT receipt. All paths, remaining pins,
library_paths, job/schema and caps remain byte-value equivalent to the
disabled template. No other fields, missing field or inherited clock.

Install canonical ASCII compact sorted-key JSON plus LF as a fresh
meta/REGISTRATION.json, root0444, <=65536bytes. Independently compare every
filled field and actual pin, all16 keys and unchanged remainder; record the
actual SHA. Freeze the bound science-unit script before its admission. The
science mount must not exist: the template creates exactly admin0755 and
output0700, BOTH EMPTY until the dispatcher admits. Do not place registration,
timer evidence or outer logs in either sibling beforehand. Initial streams
go to journal; accepted dispatcher079f9 redirects both after admission.

The unchanged phases are dummy, parser-negative, producer, positive checker,
identity-negative and exception-negative. Actual positive outcome requires
all six, exact source-independent polynomial reconstruction, complete
Bezout identity and all integer-r denominator exceptions; accepted output
has exceptional_r=[] and negative exception plant gives ["2"]. Producer
CANDIDATE_UNCHECKED or dispatcher NOT_YET_ROOT_ACCEPTED alone is no result.
Generic gcd failure, actual exceptions, cap, or missing controls remain
NONDECISION. No symbolic replay or scientific import runs on HQ.

Observe actual science unit/dispatcher child and cgroup controls within
60seconds, preserve original absolute TERM/KILL arming and independently
frozen expected cgroup. CPU80%/100ms/burst0; whole3540seconds INCLUDING
cleanup; 32GiB AS/memory, swap0, Tasks64, FSIZE256MiB and common256MiB tmpfs.
Children drop to the qualified nonroot identity; root supervisor has only
the reviewed DAC/KILL/SETUID/SETGID bounding set. Keep current handle on
poll timeouts; never restart because an observation expired. Capture true
terminal --wait return, cgroup absence and post-census completion UTC.

## Durable transfer and retirement on every outcome

Before worker termination, stop scientific writers by their ORIGINAL caps;
prove exact unit/cgroup terminal state. Freeze the explicit source/meta/prep,
dummy and science-output evidence, including unit journal/control captures
and actual exit files. No recursive search of the baked campaign or other
workloads. Copy tmpfs data into a new EBS directory
/opt/jc2-mixed-v3-evidence-20260913, using an explicit tar member list:
base/meta, base/prep, base/science, base/runtime, plus base/science-tmpfs and
the exact dummy mount ONLY IF each was actually created. Include retained
operator observations. The full private lib is retained on EBS without
pruning; its complete native hash inventory is in the archive.

Use sync -f on the evidence archive and its EBS parent (Linux syncfs flushes
that filesystem's data and metadata); check both actual exits. Hash archive,
copy to the new HQ custody directory, independently compare SHA AND a second
whole-byte SSH stream with cmp under pipefail. Run sync -f on local archive
and parent as well. This preserves tmpfs evidence before the fixed worker
retirement. If local transfer fails, the non-deleting root EBS remains the
recovery source; record incomplete custody, do not claim a passing archive.

Terminate ONLY the registered returned instance ID, explicitly reject HQ's
ID, and independently query until TERMINATED. Verify its root EBS remains
retained/available and inspect both nonterminal campaign selectors. Stop the
exact HQ retirement timer only after terminal confirmation, or preserve it
if a known active science ownership transfer already occurred. No volume
deletion, broad termination, new retry or changed cap is authorized.

The filesystem-flush choice uses the [GNU coreutils sync manual](https://www.gnu.org/software/coreutils/manual/html_node/sync-invocation.html):
the file-system option invokes syncfs on the filesystem containing its
argument. It is not a substitute for checking archive equality or EBS
retention, and says nothing about mathematical validity.

## Static review status

The underlying science/map, runtime, CAPRUN, metadata, dummy and late-capture
sources retain their scoped prior FIRST chains. Sol's new
xmodel/f10-mixed-loadstate-gate-sol-20260913.md is terminal CONDITIONAL STATIC
CONFIRMED for current UNIT338e and unchanged PREPARE482c; its PREPARE abort
finding in the earlier operator report is explicitly withdrawn. ROOT's
actual administrative absent-unit query supports this correction. This is
not new native qualification or evidence of a successful service run.

This integrated field/transfer procedure is ROOT-authored and awaits its
one bounded different-model integration check before actual allocation.
No mathematical or runtime hardening farm is implied. Physical bindings,
clocks, truthful qualification and actual registration remain future work.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `16794`.
- Body SHA-256:
  `910cefddd222deda58e1beadffd41868fb1fedca966de4f1abd5968124654872`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
