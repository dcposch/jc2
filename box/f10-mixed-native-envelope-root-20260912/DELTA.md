# Native metadata envelope delta — source-only
ROOT, 2026-09-12. DISABLED, UNEXECUTED, UNREVIEWED.

## Concrete reason and selected scope
Read-only HQ metadata after20:26:49 and before20:29:13UTC enumerated exactly4286 regular files and3
symlinks in /usr/lib/python3.12, /usr/lib/python3/dist-packages/sympy and
/usr/lib/python3/dist-packages/mpmath. These are the literal three trees,
NOT an import/native closure, not a future-worker fact. The 603538-byte
path-plus70-per-entry estimate is not serialized JSON or an actual manifest.
The old1500-file/262144-byte envelope cannot admit even these complete trees.
Discarding package members or assuming a lazy-import trace is complete is
not justified. A ZIP layout would add an unqualified dependency change.

Select ONLY three numeric metadata-admission changes in immutable copies:
- authority.py native manifest read cap262144 ->2097152bytes;
- authority.py file inventory count1500 ->8192;
- dispatch.py matching native manifest read cap262144 ->2097152bytes.
8192 finite entries and2MiB permit bounded headroom above the measured4286
and approximately604KB estimate. Neither bound proves future closure fit.
Excess is STOP; do not enlarge again automatically. The former frozen sources,
their accepted FIRST scopes and historical no-cap-increase promises remain
unchanged. This is an explicitly selected prospective metadata derivative,
not reopening or extending a prior runtime or its deadline.

## Unchanged boundaries
All mathematical produce.py/check.py bytes, coefficient/term/degree/bit
limits, schemas, literal job tag, phases, parser/control predicates and
certificate16MiB limit are unchanged. No execution, source or certificate
input allowance beyond native metadata is enlarged. Per-file native limit
256MiB, whole-job3540seconds including cleanup,80percent CPU/burst0,
32GiB memory/AS,256MiB tmpfs/output and all inherited rlimits are unchanged.
One prospective gcd only; no retry, parameter farm or old r3 framework.
This delta changes the authority and dispatcher hashes, which MUST be bound
as actual installed sources; no old source pin authenticates these copies.
Science files must still be installed together with unchanged produce/check.

## Remaining execution gates
One different-model source DELTA FIRST is required before use. Authentic
complete files/directories/aliases/absences/pyc/default-startup/native-backend
qualification, immutable ancestry and library parent directories, actual
worker/boot, exact cgroup/resource/readiness/cleanup/retirement facts and
durable terminal custody remain required. No arbitrary hash list is complete
merely because it fits the envelope. The old collector/assembler limits and
nominated roots are NOT changed by this artifact and are NOT a matching
qualified collector. No collector, registration, authority or worker is
created or authorized here. No scalar unitness, source exclusion or JC2 result.

Producer inputs:
authority.py 1b7cc411488642a4fa7b7d25a14dc8cbddc9746d0103ae1f1987fe4e8a7d5fa5
dispatch.py 0255e3e7ae779e8ca08007657689a540e6fd5ba438d894ba1e0a3e4a14ac3684
COORDINATION.md 33cfa6106972fe557a95ca81a9a05bb72300a82b2fbb7908915441cf75377597
ops/FLEET.md cf6b43f20c44fc378e4124ace7510c691f7affa561c00681dc3b14bb8512ab9d
