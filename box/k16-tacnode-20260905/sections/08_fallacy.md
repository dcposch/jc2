## 8. FALLACY-v2 check

- **Formal vs convergent branch; involution.** `L̃` is convergent AND algebraic of degree 3; the involution only derives
  identities ((E-), the cubics) and never transports polynomiality; the ∞-description of both branches is a consistency.
- **Prime label/derivative.** `L̃`, `p̃`, `λ̃` are labels; `P'`, `R_Y`, `d_Y`, `theta = x d/dx` are declared derivatives.
- **Variable/ring map.** Every Singular ring is declared (generator order, weights, field `(0,d)` with `minpoly 3d² - N` or
  `GF(31991)` at the declared root); Y is a ring variable; frozen rows compared by `imap` (identity on `(c_i, b)`); the b = 1
  replay uses an explicit `map` whose image list is printed.
- **Raw remainder / `sat()`.** No `sat`; memberships are `reduce` against reduced standard bases with the power reported.
- **Floor/attainment.** `deg D = 3N`, `lc D` exact; `j0 <= 3N` a bound, `j0 = 3` at t = 2 a witness; min powers are search
  minima; every membership is labelled vacuous/non-vacuous against the socle degree. **Modular -> char 0:** nothing modular promoted.
- **Rejected draft.** The total-degree `jet` truncation was caught by an independent resultant and replaced (§6).
- **Flag/place/series, exit sets, pole identities, 8.5, arrival index:** not touched. No exit-price assertion; `charge_basis` inapplicable.
