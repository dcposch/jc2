# Fixed89/root0 positive full-source unit instrument

STATIC / UNREVIEWED / UNEXECUTED. Exactly one place and one finite positive search class; no source result or completeness claim. Baseline dac655a7ce75287b06010171bce66af1a644aff81de9620a452457a5f65a9587,1652675 bytes is future input only, CHECKED_BUT_NOT_PROMOTED as assigned. It was not accessed, hashed, parsed or copied during preparation.

## Exact source and arithmetic

The isolated new sibling directory contains only entry.py, produce.py and check.py. Original arithmetic.py and check_arithmetic.py stay at separate pinned original paths, unchanged; authority.py and original produce.py are read-only specification inputs, never copied as a new scientific build. entry.py shares metadata/JSON/file helpers only and differs from the highest entry only in job/receipt/refusal names.

Each mode reconstructs the literal monic P7 at tau2/7 with its own original rational polynomial arithmetic, then compares all eight original rational coefficients. Both independently prove fixed89 prime and P7bar(0)=0,P7bar'(0)!=0. The producer uses trial division and checked Fermat inverses; the checker uses divisors2..9 and checked integer Euclid inverses. All inversions are nonzero F89 scalars or required89-unit rational denominators, never an arbitrary element of the whole characteristic-zero B. The local reduction is from the integral order/local model to F89, not a ring map from the whole field B to characteristic89.

All twenty used original sparse polynomials are independently authenticated before projection: three Psi rows of weights5,6,7; all seven K1_i of weights14-i; all nine K0_i of weights16-i; and source.U of weight3. Each term has exactly six canonical nonnegative exponent strings0..16 and seven canonical reduced rational strings, sorted distinct exponents and a nonzero dense rational vector. Every dense denominator, including positive-z terms and coordinates killed by beta0, must be an89-unit. No S/vartheta support is allowed. Exact source format, variables/weights/basis/P7, original19-row inventory, guard/targets/scale are checked. Row caps4096 and selected aggregate50000 are conservative refusal bounds, not actual measured sizes. Other baseline fields are bound by the literal input SHA, not reconstructed by this instrument.

Set z=1 and rename X1,X2,X3 as Y1,Y2,Y3, weights1,2,3; never set X1=1 or choose a source scale. The literal eighteen generators, in fixed certificate order, are:

1. Psi5,Psi6,Psi7 at z=1;
2. K1_1,...,K1_6 at z=1;
3. K0_1,...,K0_8 at z=1;
4. K1_0-U*K0_0 at z=1, rebuilt from all three authenticated source polynomials.

All15 additional slots remain, even when zero/dependent or assigned zero cofactor. K1_0 has full original weight14, U*K0_0 has weight19. The mixed row is not falsely homogeneous of one integer weight; after z=1 it has maximum weight19 and class4 modulo5. Its constituent U,K1_0,K0_0 are validated before this multiplication. No scalar rescaling, localization, field factor, prime/root fallback or discarded lower term.

## Positive search only

The one prospective matrix has row monomials of weight<=30,class0. The first three generator blocks use all cofactor monomials of weight<=30-h,class-h mod5. The other15 blocks use all cofactor monomials of weight<=12 and opposite generator class. Expected fixed sizes247 rows,424 first-block columns and330 other columns (754 total) are runtime guards, not an executed enumeration or measurement in this packet.

All products actually fit the weight30,class0 row space. For the first blocks this follows from the cut. For additional rows the crude weight bound is19+12=31; every product term has class0, so its nonnegative integer weight is at most30. The matrix builder explicitly refuses every unlisted product position; no high coefficient is silently dropped. No quotient basis, highest regularity or Macaulay-completeness theorem is used.

Producer Gaussian elimination is deterministic: lexicographic monomial order, fixed generator/block order, left-to-right columns and first nonzero eligible row; normalized pivots, forward elimination and back substitution with free coordinates zero. Loops are bounded by the fixed247x755 augmented array. After extraction, a fresh literal polynomial accumulation of all18 products must equal1. Failure, dimension mismatch, bad fixed place, denominator or other ValueError yields INCONCLUSIVE_NO_CERTIFICATE; no degree/class/prime escalation. Resource/unhandled errors are STOP and may leave no accepted output. Only a candidate is emitted, not a proof flag from rank.

## Independent checker

check.py never imports produce.py or producer arithmetic, matrix, reduced rows, pivots or rank. It independently reconstructs P7 with check_arithmetic.py, re-parses every original selected coefficient/support, evaluates the place, rebuilds all18 actual generators and the mixed product, and parses all18 indexed cofactor slots from the certificate. It checks every term's class and exact cofactor cap25/24/23 or12, all zero slots, canonical residue strings and ordering.

The checker accumulates the entire polynomial identity in an independent dense31x16x11 rectangular array. Every product is first required to have weight<=30 and class0 and to fit that array; then EVERY array position, including absent/zero and outside-simplex positions, is compared to the constant1 target. There is no row-space trust, quotient reduction, finite-point evaluation or rank-only acceptance. Its final semantic error is exactly `CHECK FAILED: fixed-place literal full-source unit identity` (ValueError); other rejection reasons are not that semantic check.

## Canonical wire, CLI and output caps

Canonical ASCII JSON, sorted compact keys plus newline; duplicate keys and every numeric JSON token forbidden. Rational source strings are reduced numerator/positive-denominator, no leading zero/-0, each integer component<=4096 characters, conversion ceiling4300. Cofactor exponents are canonical strings0..30; coefficients are nonzero residue strings1..88; zero polynomial is[], never a zero-valued explicit term. All18 slots required in literal order. Each cofactor and aggregate term count<=754.

Certificate exact keys: schema=f10-r2-fullunit-modular89/v1, baseline_sha256, prime='89',root='0',specialization='z=1',variables=['Y1','Y2','Y3'],weights=['1','2','3'],cut='30',modulus[eight rational strings],cofactors[eighteen objects]. Each object is exactly {generator:literal label,terms:[[[e0,e1,e2],residue],...]}. No source polynomial, rank or purported reduction is trusted from the certificate. Bound2MiB; producer and checker receipts each32768B. Actual coefficient sizes remain unmeasured.

Exact prospective17-entry sys.argv (eight ordered flags), under an authenticated ROOT wrapper only:

    PYTHON -E -s -S -B ROLE.py --registered-job f10-r2-fullunit-modular89-v1 --root-registration-sha256 ROOT_SHA --self-sha256 ROLE_SHA --entry-sha256 ENTRY_SHA --arithmetic ORIGINAL_ROLE_ARITHMETIC --baseline BASELINE --certificate CERTIFICATE --receipt FRESH_RECEIPT

ROLE is produce or check. ROOT must pin scripts BEFORE entry import, freeze the exact three-file sibling directory, separately pin original arithmetic paths/interpreter/native closure and enforce actual identity/argv/caps/output isolation. Binding hashes and Amazon EC2 DMI are NOT authentication of a ROOT invitation. No enabled registration, wrapper/caller, native inventory or worker assignment is minted. Same-file TOCTOU and malicious same-user writers are not solved by script hashes; ROOT owns immutable/exclusive staging and pre-exec checks. Maths starts only after enter's metadata/source/baseline barrier and the externally authenticated launch.

Entry retains canonical distinct no-follow paths, inode/size/mtime/ctime read checks, exclusive0444 outputs, file/directory fsync and byte readback. Sources/baseline rechecked before publication, certificate reread before successful receipt. Missing receipt, changed input or partial file is not success. The ceiling remains600wall/500CPU/2GiB/16MiB, UNREGISTERED for this code; ROOT alone supplies exact positive/check/control child profiles within it, unchanged supervision and absolute deadlines. No forecast, installation, retry, second prime/order or worker action.

Receipt exact keys are schema=f10-r2-fullunit-modular89-receipt/v1,status,science_outcome='NONE',mode,prime,root,root_registration_sha256,sources{absolute role/entry/arithmetic:SHA},baseline_sha256,baseline_bytes='1652675',certificate_sha256. Producer success is CANDIDATE_UNCHECKED only. Checker sufficient-place failure is INCONCLUSIVE_FIXED_PLACE/exit2. Malformed certificate or false identity rejects, never proves properness. Only independent literal success produces FULL_SPECIAL_UNIT_CHECKED_NO_SOURCE_OUTCOME, exit0/empty streams.

## Separate mathematical outcome boundary

A checked full modular identity is sound by itself as1 in the actual18-generator special ideal, regardless of search completeness or highest regularity. It does NOT by itself establish generic/source zero. ROOT separately needs a checked WHOLE special highest-projective certificate for the same place/local model, the accepted17zzw finite-model lift and source/interface scope, and actual prerequisite data acceptance. This packet imports that theorem only conditionally; it neither emits nor consumes an actual highest certificate. No nilpotence target, source point, generic nonunit, all-r or JC2 assertion.

## One meaningful future negative control: design only

After independent positive identity acceptance, reconstruct all18 specialized generators. Select the first index with a nonzero generator AND a nonzero cofactor; such a pair exists because their sum is1. Select the first existing nonzero cofactor term in canonical order, change its coefficient by+1 mod89, removing the term iff it becomes0. Do not insert a constant into a nonzero-class cofactor. Exponent/class/weight and all slots stay unchanged. The identity changes by that monomial times a nonzero generator, nonzero in the polynomial domain F89[Y1,Y2,Y3]; hence the control must reach the final literal-identity failure, not a parser/pin/class failure.

Record index/label/exponent/old coefficient/new coefficient/deletion flag and old/new whole-byte hashes. Undo by restoring the original existing term in its original sorted location (or replacing its coefficient), and REQUIRE that canonical reserialization equals every original certificate byte. Different-byte check mandatory;88->0 remains a genuine+1 change in F89. Use a fresh changed-certificate hash and newly ROOT-bound check authority, not stale-input rejection. No mutator code, fixture or runtime is provided now. ROOT's future exact traceback/telemetry contract must bind the final frozen checker line/frame layout; substring-only stderr acceptance is insufficient.

## Pending validation

FIRST independent source review must examine source extraction/mixed row, fixed-place integrality, matrix cut with no completeness claim, elimination/back substitution, independent all-position identity, wire/caps and the meaningful-control proof. All syntax/import/math tests remain unrun. Cheapest next evidence is one separately registered fixed-place candidate/check sequence, if ROOT chooses, with the one genuine changed-object control only following success. Any failure is INCONCLUSIVE/STOP with no escalation.
