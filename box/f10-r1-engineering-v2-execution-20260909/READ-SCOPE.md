# Read scope and execution custody

The new terminal Fable v2 caller gate (0fa06c13...) was current-hashed before its whole read at the initial 13:25:19 UTC intake. The frozen caller was read whole again before dispatch; its unchanged builder/checker/helper/probe/precision and accepted 16q/r/16u interfaces were already whole-read during their exact prior preparation/reviews. No new mathematical premise was consumed. The current 13-file documentary vector is in current-pins.json.

Only one exact worker was queried and accessed. Initial exact-instance describe at 13:25:19 confirmed RUNNING, private 172.30.0.72, Owner f10-r1-exact-artifact-validation-20260909 and EBS vol-0574e0fa5aed1f5e3. SSH read hostname ip-172-30-0-72, Amazon EC2 vendor, DMI instance i-08d2a40f272ee9fa2, namespace pid:[4026531836] and the pinned interpreter hash. The first combined sed printed vendor only; a separate explicit board_asset_tag read obtained the instance ID before dispatch. Exclusive new remote cwd absence/mkdir succeeded. All ten remote code/input/interpreter hashes were explicitly printed and matched before dispatch at approximately 13:29:22 UTC.

No old remote directory was reused; no broad filesystem lookup or AWS enumeration occurred in this v2 task. Exact frozen files were transported by the existing fleet.sh push command. The unchanged caller was launched once. After its SSH return, all 135 remote files were pulled as raw bytes. The final independent /proc stat audit was filtered to the exact 16 telemetry PGIDs and found live=[] before STOP. Exact instance poststate is retained separately.

Whole terminal batch records, seven checker receipts, precision receipt, all selected telemetry fields and full build/precision/dummy telemetry were read. Raw exact.json was not routed through JavaScript numerical parsing or reserialized. Its SHA and byte length were checked; its full mathematical verification occurred only in the frozen remote checker. Documentary metadata hashing verified all 16 record-to-authority/telemetry bindings and every per-operation input hash, including copied exact.json, with no mismatch. No local builder, checker, precision test or mathematical predicate was run. No extra artifact statistics were computed beyond byte sizes and the frozen checker/telemetry receipts.

No solver, GB, prime, point search, full degree-112/196 source expansion, optimized-mode test, or follow-on computation occurred. Final artifact/report scope is producer engineering, pending independent runtime review.
