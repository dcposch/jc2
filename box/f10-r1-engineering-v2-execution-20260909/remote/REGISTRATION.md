# Explicit v2 engineering execution registration

Registered from root's explicit 2026-09-09 approximately 13:24 UTC invitation, first local timestamp 13:25:19 UTC. This is an enabled invitation for the frozen caller only, not a solver authorization. The earlier disabled registration remains unchanged.

Owner worker: i-08d2a40f272ee9fa2, private 172.30.0.72, hostname ip-172-30-0-72, Amazon EC2 DMI. Retained Owner tag f10-r1-exact-artifact-validation-20260909 is explicitly reassigned by root for this v2 job. Retain EBS vol-0574e0fa5aed1f5e3. Do not allocate, retag, terminate or delete. Stop this instance after evidence collection. Root's independent 13:40 UTC backup stop remains idempotent.

Exclusive new cwd: /home/ubuntu/f10-r1-exact-artifact-validation-v2-20260909. Actual absence check followed by exclusive mkdir passed before transport. Never reuse the old remote directory.

Frozen dispatch_batch.py aaafa62a7aa96ab7823e889c7d8800add04b9699a9e736ccdeb883557e812640; corrected precision.py 56400dd32f4940a0e77b2da79e9b90792b51588d01f0f1a2fded30efebb0af90. External preflight verifies caller self-hash; its full fixed helper/input pins remain mandatory. The caller creates exact per-operation authorities only after physical/cwd/source gates. Exact child/parent argv and immutable input hashes are recorded in each authority and dispatch receipt.

Run only five authority refusals, valid sentinel, descendant-RSS/TERM-ignoring/KILL identity control, then on complete pass: frozen builder, seven ordinary checker modes, corrected precision attack. No optimized evidence, solver, GB, prime, point, source powers or new controls. One child at a time. Caps: builder 60 wall / 50 CPU seconds; checker and precision each 30 wall / 25 CPU; 2 GiB sampled descendant RSS. Controls retain frozen 5 wall / 3 CPU and dummy 32 MiB RSS. Inherited file cap 16 MiB is set before exact CAPRUN exec without a new caller PGID.

Mathematical hard cutoff is 13:35 UTC AND 360 aggregate wall seconds from first mathematical child, whichever earlier. Frozen +15-second admission margin and post-return deadline check remain unchanged. Whole execution task ends by 13:40 UTC. Any cap, mismatch or late return means STOP/NONDECISION, no patch, retry or increased allowance. Sampled RSS and bounded cleanup overshoot are not strict real-time guarantees.

Only exact-worker SSH/rsync transport, exact AWS checks/stop and owned-PGID final audit are authorized. Live reports to root are metadata only. Preserve every artifact/receipt/telemetry as exact bytes. Engineering results are not ideal decisions or promotion. All writers must be idle before terminal handoff.
