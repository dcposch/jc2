# Fresh WHOLE read scope

Exactly the11 current-pinned inputs in PINS.json: ROOT-CARD first, then every named text/source input FRESH_WHOLE. No prior-WHOLE reuse, live scientific author file, peer output, operation provenance or coefficient artifact was read. Old dispatcher1–230,231–450,451–637; the initially clipped execution section was recovered430–570 and the complete source was subsequently re-read as text for the owned copy. Old probe73lines and mutator206lines were read through EOF. Old runtime FIRST was separately reread WHOLE after the aggregate display clipped its middle. CAPRUN1198lines read in ranges1–300,301–600,601–900,901–1198. The remaining texts/JSON were complete untruncated reads.

Old FIRST's cap/performance speculation and reconstruction-dominance claims are NOT imported. Its accepted control/custody/CAPRUN scope supplies the unchanged foundation; new entry/helper/API/mutation changes need FIRST. The two source authors' future five-file hashes and source-specific refusal policy are explicitly UNFORMED. ROOT source_pins documentary references remain metadata bindings, not an authorization to read those referenced bodies here.

Owned dispatch/probe/mutate, full CONTRACT, disabled JSON and literal dispatcher diff were reviewed WHOLE as text. No execution/import/AST/syntax/compile/test, mathematical subprocess, fixture, payload or dummy generation occurred. Shell calls were documentary date/test/sha256sum/wc/sed/rg/ls/diff and the existing artifact_finalize metadata workflow only. All writes used apply_patch, including the bounded source-text copy and literal diff. No scientific/helper/CAPRUN original was edited.

Common metadata-policy text is embedded independently in probe.py and mutate.py; neither imports the other, scientific authority or either scientific arithmetic. Independent mutation means no producer/checker arithmetic or oracle, not an independent reproof of ROOT/native policy. Complete positive source evaluation remains a future prerequisite.
