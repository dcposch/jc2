# Direct rows runtime port — DISABLED / STATIC / UNEXECUTED

Job f10-source-cone-r3-direct-rows-v1; ROOT schema f10-direct-rows-runtime/v1. This is a SOURCE evaluation port, not a rank wrapper or rational-baseline qualification. All authored source is unexecuted, including syntax. Scientific source is being independently authored elsewhere and is NOT read, copied or presumed conforming. BOTH source and runtime FIRST plus a fresh explicit ROOT registration are prerequisites. The disabled template is not authority.

## Unchanged foundation and actual inventory

CAPRUN remains external SHA4435279df8f987c667ebc2fae8e4bd987916032561cb3faf1e07a57789d196c2. The dispatcher is an owned text derivative of f3e51286… with literal diff. Startup/physical/cgroup, inherited file caps, exact argv, telemetry, native closure, budget sampling, freeze and quiet-before-read/durable-copy machinery remain the old source runtime. No superclass, source import, second supervisor or worker control is introduced. Inherited non-atomic /proc/map sampling can fail closed; no ESRCH/map hardening is selected.

ROOT installs five final scientific siblings authority.py/arithmetic.py/produce.py/check_arithmetic.py/check.py in one root0755 directory, each root0444. A distinct root0755 wrapper directory contains exactly dispatch.py/probe.py/mutate.py root0444. The five final hashes are ROOT source_files, UNFORMED here, not old scientific hashes. Every installed absolute source path is repeated in source_pins with the identical source_files and pins value. Other source-reference entries are the exact ROOT-frozen wire vector; those documentary references need not be imported or copied into the worker science directory. The scalar contract_sha256 is ROOT's immutable accepted operation/wire/review manifest binding, not a self-hash. Source FIRST must settle the actual five-file API before activation.

Root registration/files/native are outside outputs. All actual physical, UID/GID, boot, namespace, interpreter/native, paths, four UTC stops, command and clearance facts are UNFORMED. Exactly six wrapper roles: python,setpriv,caprun,dispatcher,probe,mutator. Full native closure retains schema/files/directories/aliases/absent/python_path, max1MiB/3000files/1000directories, all actual mapped files/aliases, exact outer sys.path, startup pyc/ZIP absence and current hashes. SOURCE subset remains the distinct64KiB job_tag/files subset including interpreter, exactly matched to full closure. ROOT supplies both; no discovery here.

Outer flags are -I -S -B. Actual producer/checker and probe use -E -s -S -B; mutator uses -I -S -B. Ordinary-only, no -O/-OO. Exact clear environment PATH=/usr/bin:/bin,LANG=C,LC_ALL=C,TZ=UTC. setpriv drops the configured UID/GID with clear-groups/no-new-privs in CAPRUN's session. Full live native and physical checks remain. Metadata-only embedded policy code in probe/mutator does not import scientific authority or either arithmetic module.

## Thirteen labels and authority distinctions

Exactly once, in order: refuse-status,refuse-caps,refuse-inventory,refuse-source,refuse-hash; valid; startup-produce,startup-check; dummy; produce,check-positive,mutate,check-negative. Same caps and no retry. The first five run ACTUAL produce.py with its true absolute entry, full flags and eight-argument tail. Mutations are status DISABLED; cpu_seconds599; missing files.check.py; files.produce.py zero hash; supplied authorization hash zero. No helper calls scientific authorize on a fake entry. Wrong-tag preflights use actual produce.py/check.py and WRONG-JOB.

Source authorities have EXACT eleven keys job_tag/status/authority/mode/artifact/files/runtime/limits/contract_sha256/source_pins/place, derived from immutable ROOT data. artifact path/UNFORMED applies to producer; checker gets the actual frozen input SHA. Mode is produce/check. place is ROOT's exact prescribed p,degree,ascending phi,P7_mod_p; dispatcher performs metadata shape only, not primality/field arithmetic. It does not reinterpret the scientific four-value return API.

Valid and dummy consume a DIFFERENT ROOT-policy record with schema f10-direct-rows-root-policy/v1, root_registration_path,job_tag,label,artifact,science_outcome,source_files,limits,profile. They independently authenticate ROOT ownership/current pins/host/native, exact actual command suffix, their immutable policy hash and registered fresh output. Valid emits ROOT_POLICY_ONLY_SENTINEL, not successful scientific authorize(). Dummy retains the four-event fork/orphan64MiB/TERM-ignoring scenario and exact CAPRUN identity/RSS/TERM15/KILL9/quiet/reap predicates. These two establish only ROOT-policy/runtime observations.

Probe CLI: PYTHON -E -s -S -B PROBE --registration ROOT --policy AUTH --policy-sha256 HASH --mode valid|dummy --output OUTPUT. Each policy omits ROOT's self-hash: ROOT path is fixed, and immutable ROOT commands bind the deterministic policy digest and full actual suffix. This avoids a cycle. Its trust is explicit trusted ROOT/no concurrent writer, not arbitrary self-authored JSON.

All seven scientific entry refusal predicates are UNFORMED until actual-source FIRST. ROOT must supply an immutable pinned preflight_policy file, schema f10-direct-rows-preflight-policy/v1, exact source_files and entries with precisely the five refusal names and two startup names. Each entry has exact exit_code (integer1..125), stdout="", stderr=the COMPLETE exact bounded ASCII bytes represented as a JSON string, max8192 and ending newline. These are manually derived/reviewed from final source; no guessed old exceptions, substring, wildcard, generic nonzero exit or traceback tolerance. The dispatcher requires NORMAL_EXIT, no resource/signal, exact stderr/empty stdout and absent producer/sentinel target. Wrong parser/field/resource failure is not the intended control merely because it exits.

The full registered CAPRUN vector is unchanged except its literal child: PYTHON -I -S -B CAPRUN --wall-seconds W --cpu-seconds C --rss-bytes R --rss-sample-seconds0.05 --term-grace-seconds1 --stdout-file OUT --stderr-file ERR --telemetry-file TEL --cwd SCIENCE -- SETPRIV --reuid UID --regid GID --clear-groups --no-new-privs -- PYTHON FLAGS ENTRY TAIL. All tokens are separate argv elements. Science tail remains --registered-job TAG --authorization AUTH --authorization-sha256 HASH --output OUT or --input IN.

Only checker authorities have the single typed AUTHORIZATION_SHA256 future slot. Mutator alone has ROOT_REGISTRATION_SHA256 at its named flag. No probe ROOT hash slot or generic substitution. Every other byte is registered literally, including deterministic preflight/helper authority hashes; actual expanded argv is captured before launch. The unused mutator record is ROOT-policy-only, not a fake produce/check source authorization.

## Positive/negative evidence and independent finite-coordinate mutation

Actual producer must return NORMAL_EXIT0, empty stderr, exactly FORMED_UNCHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME plus newline. Its packet is moved/frozen root0444 and hashed, then ONE actual check.py invocation must return NORMAL_EXIT0, empty stderr, exactly CHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME plus newline. The dispatcher does not parse the scientific payload. The ROOT derivative positive record is issued only after that actual exit, exact streams, quiet, revalidation, actual source/input and telemetry binding. It binds ROOT SHA, baseline path/SHA/bytes, five source hashes, positive authority path/SHA, exact checker stdout SHA and phase policy. No candidate or mutator creates its own successful source acceptance.

Mutator CLI: PYTHON -I -S -B MUTATE --registration ROOT --root-registration-sha256 ROOT_SHA --positive-record AUTHORITY/positive.json. It repeats its own metadata/host/native/current-source barriers through embedded metadata-only code, checks the precise positive record and reconstructs the expected eleven-key check-positive authority against ROOT. It does NOT spoof sys.argv, import authority.py, call authorize('check'), run checker, or import any science arithmetic. ROOT ownership and actual dispatcher creation establish the receipt's provenance, not its schema spelling alone. The source reference vector/place/contract in the parsed input must exactly match ROOT.

After those barriers, it parses canonical six-key f10-r3-direct-rows/v1. All30 typed graph polynomials and two dense field scalars are accounted, all prescribed weights, numerical exponent order and zero/duplicate slots preserved. It validates field-vector length f and decimal residues0..p-1 using its own small integer code; prime/irreducibility/source correctness are already part of the genuine positive check, not re-proved here. It adds1 modulo p ONLY to basis-coordinate0 of exponent(27,0,0,0) in graph.slots[24]. A missing term is inserted in numerical tuple order; a changed all-zero vector deletes its term. Other coordinates/fields stay unchanged. Insertion requires spare per-poly20000 and whole400000 term capacity; no truncation. Input plus fixture must fit120MiB. Whole-byte inverse restoration and unequal byte hashes are mandatory. One receipt is FORMED_UNCHECKED with checker_run false; no success assertion by the mutator.

The mutation is nonzero because1 is nonzero in the ROOT-authenticated finite field. Its monomial has exactly weight27, so with all untouched data from the genuine positive input the intended mismatch is the T comparison, not shape/weight/parser refusal. Actual reachability still needs runtime evidence and sufficient resource slack. Mutator failures, unchanged hashes, cap/serialization/metadata refusals STOP; they are not negative-check success.

Fixture and receipt are moved/frozen; complete frozen inventory becomes0555 before ONE check-negative. Required outcome: NORMAL_EXIT2, child_exit_code2, child_signal null, resource null, EMPTY stdout, and stderr bytes exactly CHECK FAILED: direct-source graph slot T followed by newline. NO traceback, chain, additional text, parser or other predicate may count. Final wrapper status can only be DIRECT_ROWS_AND_T_REJECTION_CHECKED_NO_SOURCE_OUTCOME with science_outcome NONE. Other semantic controls remain NOT_RUN_SPEC. No rank, rational-baseline qualification, source-zero, source-point or JC2 inference.

## Same caps, custody and pending validation

Per-source LIMITS remain900wall/600CPU/8GiB/128MiB; aggregate3000wall/2100CPU/8GiB/128MiB. PROFILES remain control5/3/8GiB, dummy5/3/32MiB, produce900/599/8GiB, check900/599/8GiB, mutate30/20/8GiB. The three scientific invocations retain their900/599 soft/hard600 profiles; control soft3/hard4, mutator20/hard21. Literal child allocation2775wall/1857hardCPU is static/unmeasured. Sampled aggregate cutoff2050 leaves50 nominal cleanup CPU seconds, NOT a kernel hard aggregate guarantee. Cgroup origin never resets. Every admission requires wall+15 within original absolute and aggregate deadlines; post-return and durable custody stay inside the original task/3000/2100 limits. ROOT owns separate external stops and actual enforcement.

Same physical tmpfs128MiB-or-smaller, packets120MiB joint plus8MiB ALL metadata/streams. Root0755 authority/frozen dirs, UID/GID0700 writer, sources/native/registration outside. Same RLIMIT_FSIZE120MiB/core0. Group quiet BEFORE any terminal telemetry, stream, receipt, packet or custody reading. Actual producer/positive/mutator/negative runner and Python live captures are required; a missed capture is GAP/STOP, no rerun. Fast preflight missed live capture is disclosed. Same PID/PGID/start/boot/ns/caps/maps and exact CAPRUN telemetry bindings retained.

Same move/freeze, all pre/post pins, untouched working bytes and byte-identical fresh EBS durable copies after quiet. Durable device/path and actual instance/namespace/native/currentcode facts are ROOT-unformed here. Copy/fsync/parent-fsync and original custody cutoff remain; no deletion/unmount/worker action. No overhead/performance forecast is imported from the old FIRST.

Before deployment ROOT must: collect both scientific authors; source-FIRST the exact five modules/API/internal complete evaluator; runtime-FIRST this wrapper/policy/mutator; issue literal source_files/source_pins/contract/place plus actual-reference manifest and seven source-specific refusal predicates; populate registered command hashes without self-cycles; bind final wrapper/current CAPRUN/interpreter/native/physical/clock/cgroup/output/custody values. A single subsequent registered13-call observation, not extra toys or a source-positive replay, is the prospective validation. Any unexpected outcome stops at its exact label. No run or worker is authorized now.

OPEN quantity: final source-specific refusal bytes/API conformity and actual13-phase feasibility. Cheapest next test is the one combined source FIRST and this runtime FIRST on frozen code, then ROOT may decide one registered batch. Planning25–30minutes is UNMEASURED documentary review time, not a scientific runtime forecast. All source here remains STATIC/UNEXECUTED, even syntax. No active registration has been authored.
