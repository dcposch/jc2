# One changed-representation test: the collision-curve residue

Owner /root/nonemptiness_certificate (Astra), manual co-research. Record
first actual UTC. Original reserve September12 11:20/HARD11:23UTC;
no reset, stop earlier when the ONE specified test is decided. Basis
0d39df3c9fd69c939a8420c54d03228b9077777d.

ROOT has terminal-collected, WHOLE-read and expected-verified your previous
primitive report. It is GAP, not a source exclusion. This is a deliberately
changed representation with an explicit normalized-place test, not a
renamed divergence/eliminant/degree search. No gate or promotion yet.

Hash TASK then fresh WHOLE. Three input paths/pins are supplied in ROOT's
invitation; hash before bodies. Same-agent exact-pin WHOLE reuse allowed
with provenance. Inputs only COORDINATION, the old constant-graph Astra
report and ROOT-CANDIDATE in this box. Links inert; no corpus/network.

QUANTITY: verify or correct the candidate collision correspondence and
eta from the literal chart, then compute the residue at u=a-b=0 on
8b^2+8b+1=0. Both conjugates may be treated in one exact quadratic field.
Cheapest test is manual implicit differentiation/local Laurent expansion,
roughly20 minutes UNMEASURED reasoning allocation, no execution forecast.
You may shorten the algebra, but retain the exact source-to-curve maps,
normalization/cover ramification and cancellation of all relevant terms.

Outcomes: a genuine nonzero residue plus the global exactness argument
is a producer-level all-degree obstruction for this fixed subalgebra;
zero residues retire ONLY this specified pole test, not exactness/pairs;
invalid correspondence or inaccessible coefficient is REFUTED/GAP with
the exact reason. No scanning other poles, raising degree, computing an
eliminant, new family, automatic successor or runtime. One meaningful
manual control. Maximum1800 scientific words plus custody. Distinguish
RAW ROOT CANDIDATE from independently derived statements throughout.

Own ONLY xmodel/graph-collision-residue-astra-20260912.md and transaction
manifest, plus this box's PINS.json, READ-SCOPE.md, custody.json. TASK and
ROOT-CANDIDATE are ROOT-owned immutable inputs. No overwrite; own output
absence check before authoring. Manual math and inert UTC/hash/text/JSON
administration, apply_patch, and existing artifact_finalize administrative
transaction only. No science/CAS/helper/code/import/AST/syntax/compile/
test/dummy/empty interpreter, network/AWS/SSH/git/process/agents/corpus/
protected-tree/scratch. No shared ledger edit or new canonical OPEN.
Partial initially unsealed; bounded writes, WHOLE readback, four unchanged
input postpins and quantity/scope/collision check precede marker LAST.
Custody SHA FIRST, exact expected manifest, all input/output pins and
actual UTC ALL WRITERS IDLE. No edits after terminal. ROOT owns collection
and any later different-model FIRST. No current global JC2 conclusion.
