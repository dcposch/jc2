# Inert worker contract — NOT launch authorization

Static implementation only. ROOT must first accept an independent execution
review and bind ONE exact disposable worker, job tag, immutable bundle and
outer retirement deadline. No AWS operation or local scientific execution was
performed. The unchanged runner's old engineering regression is not Sympy,
native-library, formula or resultant qualification. Mathematical tier is
OBSERVATION/UNREVIEWED throughout.

The template is intentionally invalid (disabled schema, extra enabled key,
null pins and unbound arguments). It must not be passed off as runnable. ROOT
creates a NEW manifest with exactly the ten JC2-JOB/v1 keys, removes enabled,
sets schema, actual instance/tag and observer SHA, and substitutes the identical
instance/tag into both argument arrays. Freeze and hash that complete manifest;
do not edit this template or observer during the run. Install the tested runner,
manifest and sole payload root-owned0444 at canonical paths under root-owned,
non-group/other-writable ancestors; record paths, modes, ancestor observations
and hashes. Never install under a writable home ancestry. Runner pin:
9c2255c7dcb7c91a8abeb2bd0ea7db30b4d4e8182abdbbd08cc1dc76f33d094d.

## Finite execution and minimal preflight

One service has a TOTAL 840-second active cap covering smoke plus observation,
16GiB memory, zero swap, CPU200%, Tasks32, 8MiB per file, and existing five-second
termination plus five-second post-hook grace. No retry, second engine, sample
farm, factorization successor or background phase. At least512MiB free EBS
for this job's small fixed output set, logs and archive; RLIMIT_FSIZE is NOT an
aggregate disk quota. ROOT owns a separately armed exact-instance retirement
deadline and retained EBS. Collection failure cannot justify deleting evidence
or extending the worker silently.

Before scientific import, the observer checks exact AWS DMI identity, explicit
HQ rejection, required registered tag, cgroup v2 exact service membership,
nonroot real/effective/saved credentials, canonical cwd and runner environment.
It relies on unchanged job.sh for all four UID/GID fields, actual caps, FSIZE,
memory/CPU/task limits, cgroup descendants/cleanup, immutable source ancestry,
physical isolation and terminal receipts. Its guard is an accidental-use
barrier inside that trusted-job boundary, not a hostile-code sandbox.

Worker-only, separately bounded preflight (maximum60seconds, included in ROOT's
retirement budget; preflight plus service at most900seconds active): record
Python real executable and SHA/version, Sympy
version/location and package provenance/hash inventory, selected exact ground
backend and any loaded native backend/library identity. Confirm Python -I
ignores user site/environment; available system Sympy must import there. No
package installation or source patch is authorized by this contract. Absence
of this dependency is a recorded refusal, not automatic migration. Run source
syntax/import checks only here. The payload also records Python, Sympy version,
Sympy __init__ SHA and observer SHA; __init__ alone is NOT full library pinning.

Negative guard smokes on that same worker, before the registered service:
invoke this frozen source with forbidden HQ and require nonzero plus exact
`HQ forbidden`; invoke a distinct valid instance string and require nonzero
plus exact `instance mismatch`; invoke the real instance/tag outside its
cgroup and require nonzero plus exact `exact job cgroup required`. Confirm no
mode output directory and no scientific module import (review source order;
worker import tracing may supply corroboration). Do not invoke any of these
on HQ. The positive service smoke factorizes (7t-12)(t^2+1), requires candidate
r=2, then deliberately corrupts the unit and requires the exact causal
factor-product rejection. This does NOT independently validate the resultant.
Unexpected smoke failure stops before the scientific phase; never set an
expected nonzero scientific exit to bypass it.

## Observation and recovery

Invoke existing start/status/collect exactly as JOB.md specifies, with pinned
runner and manifest. One start returns after manager admission; no manually
timed inner launch or gate. Both phases use python3 -I -u. Raw runner phase
stdout/stderr remain visible to the unprivileged ubuntu collector; phase JSON
events are flushed and fsynced. The observer exclusively creates one mode
directory and fixed no-overwrite checkpoints, each at most7MiB. Completed
files are fsynced and hard-linked atomically, then the directory is fsynced;
any failed publication retains its .partial. This design bounds the number of
files as well as each DATA file. Do not delete or resume in place.

Checkpoints retain dependencies, literal P0/U/V/B/G, COMPLETE R, COMPLETE Q
and exact remainder BEFORE factorization, raw factors BEFORE reconstruction,
and only then the candidate observation. Coefficients are rational strings;
all exponents, multiplicities and candidate integers are strings. Error
tracebacks go to original phase stderr first and a best-effort error checkpoint;
SIGKILL can leave no error JSON. Missing factor/observation output means
INCOMPLETE, not an empty candidate set. A valid Q checkpoint can be useful
after timeout but remains unreviewed polynomial DATA, not a proven resultant.
No coefficients are obtained from floating discovery or executable evidence.

At the outer deadline or terminal outcome, collect raw logs, all final and
partial DATA, manifest/source, receipt and manager state with unchanged runner.
Require terminal unit plus actual cgroup absence; hash-copy-verify the archive
and whole-read relevant diagnostics before ROOT retires the exact worker.
Idempotent collection may recover a failed copy; it never resumes the science.
Retain the raw archive/EBS on failed collection. Worker termination and job
cleanup remain distinct actions owned by ROOT.

Review must separately check formula fidelity and coordinate normalization.
The accepted degree/cube/denominator premises are imported, not re-proved.
Rational factor labels come from Sympy; product reconstruction alone does not
prove irreducibility or independently certify the resultant. ROOT must select
the cheapest independent check if complete Q appears. Even an eventual all-r
scalar-unit result only removes the named finite-exception prerequisite of
critical-coefficient comparison: no source-zero, all-F10 or JC2 inference.
