# One bounded scalar observation payload — static implementation only

Owner /root/execution_reliability, ROOT collector. Record actual first UTC.
Original reserve September13 07:44UTC / HARD07:47UTC, no extension.
No AWS allocation/execution, local interpreter/CAS/import/AST/syntax/dummy/test,
new agent, public/protected tree or mirror, shared ledger/source edit.
Only the unchanged administrative finalizer may run on HQ.

Selection is deliberate, not an automatic engineering successor: after two
global preselection passes, ROOT selects ONE complete all-parameter resultant
observation. Its payoff is resolving the mixed scalar's finite UNKNOWN integer
exceptions and thereby removing one prerequisite of actual critical-coefficient
comparison. It is NOT source zero or all-F10/JC2. No residue/prime/degree farm.
Do not restart or migrate the frozen v1-v6 authority harness. Do not alter
job.sh, fleet.sh, dispatch.py, old scientific sources or their frozen packets.

Prepare the smallest useful NEW exploratory payload for the already tested
JC2-JOB/v1 runner. Output is mathematical OBSERVATION / UNREVIEWED, not an
independently certified resultant, source outcome or promotion-ready proof.
No new controller, native-assembly framework, scheduler or calendar admission.

Inputs, SHA-before-WHOLE:

- frozen APPROACHES.md here:
  14e011edb64b01a00cec0b3d097815b46a3e020583b5a5030db087a212c8c160
- frozen COORDINATION.md here:
  517fca6f67f3d705f9b4045e10f9039aaf11bf3280dfd27bb90bf003ee4a3ead
  Exact-byte personal previous WHOLE reuse is allowed with its attestation.
- xmodel/f10-mixed-univariate-reduction-root-20260912.md:
  7b8545a623a771ebd423b441c4c1567389d0ca837d4e5b80c8990bc52b1075f8
- xmodel/f10-mixed-resultant-sizing-root-20260913.md:
  8806dbfb7ba5bd035262accb031f017759408ede2dbb14b3b3514e5ed435f432
- ops/fleet/job.sh:
  9c2255c7dcb7c91a8abeb2bd0ea7db30b4d4e8182abdbbd08cc1dc76f33d094d
- ops/fleet/JOB.md:
  3ab3d51402bc15339d6367aebaa8704dddf76f8d699eeb870908e03ee7619515

Curated accepted mathematics: the literal P0=(t-2)P, U,V and mixed B in those
reports describe the SAME rank-seven algebra; U is a unit for actual
t=(5r+2)/(3r+1), integer r>=2. G=U^5 B(t,X,V/U) is polynomial, degree_X<=17,
total degree<=24. R(t)=Res_X(P0,G) is nonzero, degree<=185, with EXACT factor
(3t-5)^34; Q=R/(3t-5)^34 has degree<=151 and Q(5/3)!=0. A scalar failure at
actual r is equivalent to Q(t)=0. Existing accepted generic coprimality,
cube asymptotic and unit-denominator premises are NOT to be re-proved.
Stale UNPROMOTED producer headers are historical; only this stated input scope
is accepted for the new instrument. No old code is inherited as qualified.

Deliver in this box:

1. A small observer.py using exact arithmetic and explicit phases: reconstruct
   the literal P0 and B from the binomial/truncated-power definition, construct
   G with fifth-power clearance, compute the ONE exact resultant, divide the
   exact cube factor, retain Q and its rational factorization/linear factors,
   list any r=(2-t0)/(3*t0-5) that is an integer>=2. Check full factor-product
   reconstruction before classifying candidate exceptions. No float discovery,
   supplied executable certificate, engine verdict-as-proof or hidden retries.
   If factorization hits its outer cap, retain the completed polynomial DATA.
   Use one simple available Python/Sympy stack, not a new dependency project.
2. Durable unbuffered phase logs and bounded atomic JSON DATA checkpoints;
   preserve partials and original tracebacks on failure. Exact coefficients,
   exponents and candidate integers must be strings, not JS numbers. Refuse
   existing outputs. No -O assertion-dependent gates. AWS/exact-instance/HQ
   rejection must precede scientific imports and be tied to a required job tag.
   State the runner-provided physical/cgroup dependencies explicitly.
3. A compact INERT deployment/test contract for ROOT and the different-model
   execution reviewer: proposed <=900 seconds, memory<=16GiB, CPU<=200%,
   Tasks<=32, per-file<=8MiB, plus existing runner cleanup grace. No EC2 calls.
   Separate execution safety from mathematical trust. Name the minimum worker
   dependency/version/hash observations and positive/negative smoke checks;
   don't claim native/library or scientific qualification from the old pilot.
   A manifest template must be visibly DISABLED/unbound, never runnable with
   placeholder hashes/IDs. No root-owned runtime source modification by payload.

One exploratory observation is useful before a promotion-grade certificate
exists. If actual complete Q is observed, ROOT will select the cheapest
independent verification of its claim, not automatically trust its factor label.
The old all-r gcdex is deliberately replaced by a determinant/resultant
observation to avoid paying for potentially large Bezout coefficients before
learning the exception polynomial; no measured speedup is claimed.

Publish report xmodel/f10-mixed-resultant-observation-astra-20260913.md with
unchanged artifact_finalize.py begin/close/finalize/expected-verify at basis
0d39df3c9fd69c939a8420c54d03228b9077777d. Use apply_patch for authored files;
append body marker LAST. Keep PINS and terminal custody here; whole readback
and all postpins. Return custody SHA FIRST and actual ALL WRITERS IDLE time.
Static implementation only; no launch or mathematical promotion authority.
