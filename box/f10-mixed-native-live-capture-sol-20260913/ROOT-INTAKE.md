# ROOT FIRST: same-execution native observation

September13. CONDITIONAL STATIC CONFIRMED for the read-only observation
source at SHA442567468f8452b090f0e481a2ab0bd408e95b1741968989fa19bd7a2114bf66,
with the exact scope qualifications below. No runtime/cap or worker success.

Sol first01:04:47.070421595, final idle01:09:27.927651060 UTC, before
original01:16reserve/01:19hard. ROOT independently observed terminal state
before custody intake. Custodycb7fe3d2731e23220f6dbcb58e94b5a71456cd1a3b5eb286b7c108f1daa49264;
report693bacaf3159f82d85cabe42430c525f9dce9ffc77902e7f6cc6d28cd1afdeeb,
manifest648b7b08abc50ee5766f55836829aea4c03cdb69cd49aee163e4c955994d2486,
PINSA76d4074aef23ef5f692004d29fb4e94bcb2a47b9331eedae5a9c186552f0897
(digest letters are lowercase in the actual file). All five inputs/four
outputs passed strict postpins; expected-basis/manifest verification and
WHOLE custody/report/PINS/manifest/187-line source readbacks complete.

## Exact accepted mechanism

The script is disabled by physical/cutoff placeholders. Actual ROOT/EC2,
instance, boot, host, PID/cgroup namespaces and expected canonical cgroup
are tested before polling. It reads only the specifically named service;
does not launch, restart, stop or signal that service. No target file write,
native collector edit or scientific/CAS interpreter call exists.

One snapshot supplies exact loaded/active positive MainPID, after which
capture never returns to polling. The fixed service argv and every listed
systemd limit are checked against PREPARE. Without a model roundtrip, the
same shell reads /proc/PID, validates the Bash executable and exact two
arguments, root credentials, actual AS/FSIZE limits and expected namespaces/
cgroup, then checks kernel CPU quota/period/burst, memory/swap, pids maximum,
domain type and PID membership. The stat tail starts at kernel field3, so
tail positions2/3/20 correctly extract PPID/PGID/start-time field22.

The second stat and final manager snapshot retain PPID/PGID/start ticks,
MainPID, InvocationID, ControlGroup, loaded/active state and executable.
An observed disappearance or mismatched checked field stops the script; it
cannot silently wait for a replacement process. Complete success emits a
bounded labeled record only at the end. These checks attach actual kernel/
process observations at capture time, not just intended systemd settings.

The literal service name still contains v3. It may be used only in a NEW,
independently registered worker where the unchanged PREPARE names that same
fresh service. It does not identify a live service on the terminated V3
worker or license an old clock/host reuse. Start this observer concurrently
with PREPARE in one orchestration; do not recreate the split discovery/read
mistake. Successful capture remains contingent on the actual live interval.

## Binding qualifications, not new source repairs

1. The source's sixty loop iterations are not a sixty-second hard process-
   tree cap: command time is additional. An external foreground timeout
   around SSH does not prove remote descendant cleanup either. The [GNU
   timeout manual](https://www.gnu.org/software/coreutils/manual/html_node/timeout-invocation.html)
   explicitly excludes command children from foreground timeout coverage;
   installed coreutils9.4 help confirms this. Require observed successful
   completion within the original sixty-second observation interval and
   original qualification cutoff, actual exit0 and empty stderr. A timeout
   is STOP, never permission to restart or claim remote cleanup. Original
   metadata-unit caps and worker retirement remain independent. No hard
   remote sixty-second descendant-lifetime guarantee is promoted here.
2. The producer's sentence that EVERY proc read is capped4096bytes is too
   broad. task_read_bounded uses head4097 and rejects an oversize value,
   but cmdline uses NUL-delimited Bash reads and checks size after a whole
   argument. At this exact source client the validated two arguments have
   the fixed short spellings; successful output remains bounded. This is
   not a universal preallocation bound for an arbitrary cmdline. No such
   arbitrary command is authorized. Bind LC_ALL=C for byte-count semantics.
3. Command substitutions normalize trailing line feeds in semantic metadata;
   the emitted labeled record is not a byte-identical archival copy of every
   proc file. The required numeric/string predicates and PID/start identity
   survive. Retain original tool stderr on failure: V3's local process-text
   file contains stdout only, while the errors and exit1 are in the tool
   record. Do not claim that archive includes those stderr bytes.
4. Fresh physical substitution, literal source hash, trusted exact PATH and
   actual administrative executable/native bindings still precede use.
   Ordinary read-only observer utilities are not a scientific source import;
   do not infer their qualification from a filename. Successful observation
   must be compared with authentic PREPARE terminal receipts. It does not
   qualify the mandatory dummy or science, and requires no larger science
   native inventory or cap by itself.

These narrow qualifications keep the source-static mechanism and avoid
claiming stronger guarantees from the report. No new source derivative or
controller is selected. There was no local/remote dummy, observer execution,
scientific test or AWS allocation during this review. A later actual plan
must resolve its administrative timeout/transport handling, bind truthful
facts and retain every original qualification/science/retirement deadline.
The failed V3 attempt remains closed. No fourth worker is authorized here.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5700`.
- Body SHA-256:
  `46f74ea3b6c9efe74e6a97ea3ce38c5246a3cd9bb7976436d16141c498f2ec21`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
