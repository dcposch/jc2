# ROOT prelaunch preparation — DISABLED, no worker authority

Prepared2026-09-10 21:00UTC. Source/runtime FIRSTs are accepted at the exact scopes in AUDIT17zzzzzzzzzzzz/17zzzzzzzzzzzzz. The Astra deployment packet is now terminal and has been collected custody-FIRST; ROOT's full text/metadata intake and exact fourteen-vector comparison passed. This directory owns only ROOT operational preparation, not another scientific instrument or live queue.

## Before allocation

Finish/intake the disabled packet; check all source hashes, role basenames, fourteen literal vectors, source/science path strings, modes, tmpfs inventory, missing metadata and setup text. Freeze separate ROOT-ready inputs, without changing the author's sealed packet. All mathematical source and fixed phase profiles remain unchanged. No candidate, fixture or native payload is needed before allocation. Readable root0755 ancestry and source/authority/frozen files root0444 are mandatory; writer science0700.

The worker template is deliberately disabled and has unresolved placeholders. Do not execute, syntax-check or test it locally. It derives from the historical fullunit launch script3f21f2589b4e1359950eb5e1f8986694a6ff4307b191eabfb0aef158d71c848c, WHOLE read as text, with updated ROOT task bindings and a separate complete current-input hash list. Reuse exact stdlib-only cloud-config0c5820cd60a44704a6d8118a7570a9327c1f2fe62939ff664b1f9b7cfaf42730; it disables update services and sets a readiness marker, with no package or scientific imports. Do not use the fleet driver's default CAS-provisioning user-data. Keep DeleteOnTerminationfalse, termination protection disabled, one new worker only, and arm an exact-ID original termination backup immediately after allocation, before use. All15 older workers remain retired; protected infrastructure is outside inspection scope.

Before release ROOT independently checks current AWS metadata for the chosen AMI/type/quota and the exact jc2fleet=1 nonterminal query. Use one future batch suffix in paths, unit/cgroup and every timer. Original latest-launch/setup/science/custody/worker-retirement cutoffs are chosen ONCE before allocation and never reset. No worker held idle for review. SSH timeout gets exactly one SG updater before retry; verify new host key against exact-instance console in a job-specific known_hosts file. Shared known_hosts unchanged.

## Proposed ordinary OS outer controls — actual facts still unobserved

ROOT's selected design for finalization is a whole-unit rate ceiling plus independent wall shutdown using existing systemd/cgroup primitives, not a new supervisor: CPUQuota90%, CPUQuotaPeriodSec10ms, RuntimeMaxSec600, TimeoutStopSec5, KillModecontrol-group, MemoryMax2147483648, MemorySwapMax0, TasksMax32, CPUAccountingyes, Restartno, CPUSchedulingPolicyother, RestrictRealtimeyes, no CPU burst. Outer type must not be oneshot/notify; randomized runtime extension must be0. Actual cpu.max should read9000 10000, cpu.max.burst0; no hierarchy escape or unaccounted child group. Capture actual unit settings, cgroup ancestry, process identities and terminal total CPU, not just the script's requested values.

The nominal whole-unit envelope including5seconds shutdown is0.9*605=544.5CPU seconds, with5.5seconds below550 for period/slice carryover and shutdown scheduling. This is an OS-trust envelope to validate on the future worker, NOT a measured runtime or literal hard-real-time theorem. ROOT must refuse mismatched/unsupported controls, exclude realtime/BPF-policy bypass, retain actual observed kernel/systemd/scheduler and CPU accounting, and fail outcome acceptance if actual total usage exceeds550. The600-second outer runtime includes preflight and custody as well as mathematics; it is stricter than separate180+600 windows and may stop earlier. Fixed scientific profiles are NOT changed; success feasibility remains unmeasured. If these ordinary controls cannot honestly meet the registered joint budget, stop before science, retain data, and retire; do not silently fall back to per-process limits.

Primary basis, targeted excerpts only: [systemd255 resource controls](https://raw.githubusercontent.com/systemd/systemd/v255/man/systemd.resource-control.xml), [systemd255 service runtime/shutdown](https://raw.githubusercontent.com/systemd/systemd/v255/man/systemd.service.xml), [kernel bandwidth caveats](https://docs.kernel.org/scheduler/sched-bwc.html), [cgroup v2 accounting/interface](https://docs.kernel.org/admin-guide/cgroup-v2.html). The kernel permits period/local-slice carryover; cpu.max is bandwidth, not a cumulative budget. Version255 is a documented reference, not an observed future worker version.

## Physical finalization and closeout

After separately authorized allocation, retain fresh physical identity/boot/namespace/root-EBS device INTEGER, canonical interpreter/setpriv and six-field raw native observations, including loader cache. Derive the dispatcher's FILES-ONLY native manifest1..1500 entries/256KiB; retain/authenticate directories/aliases/absence/python_path separately under ROOT trust. Do not import r2 six-field caller enforcement or old worker facts. Installed runtime/scientific paths remain immutable; native/source outside the single16MiB working mount. Freeze actual registration only after all observations and ROOT authority; no self-hash cycle.

One scientific formation/check, then either checked-INCONCLUSIVE after11calls or genuine positive plus both precise negative controls after14calls. Source, resource, hash, parse, incomplete or control failures are STOP, not refutation/positivity. Keep the negative-control timing risk; no source/profile fork before evidence. After all exact processes/cgroups are quiet, receipt/custodyFIRST, all source/native/stream/telemetry/prepost pins, durable parent fsync, complete archive/readback/download/hash/sync, then early exact-worker termination with root/EBS retained. Original timers close only after terminal confirmation. No scientific payload body may be read or parsed locally.
