# Worker-independent semantic6/10 deployment preparation

DISABLED METADATA TEMPLATES ONLY. No worker allocated, timer armed, scientific command run, registration authenticated, or deadline selected. This replaces premature allocation during the expired15:47 preparation; it does not reset that closed run's clocks. Source remains the accepted semantic6c232bb0 dispatcher/probe/a896a809 mutator and original5 science files.

Prepared before any future allocation: exact17 installation roles and accepted source pins; complete12-command registration layout; five authority digest roles; complete setup shell template including stage/current-native hashes, aliases and directory closure, root0444/source layouts,16MiB tmpfs, system timers,660/5/control-group/2GiB/Tasks32 outer unit, exact main identity capture; cache-inclusive native collector. The runtime sciences and caps are UNCHANGED. No template is runnable authority; setup explicitly refuses unresolved JC2_*_PLACEHOLDER fields. Registration enabled=false stays disabled until ROOT's exact fresh finalization.

## One-shot finalization order

1. Check all current static source/gate pins and target absence before allocation. Accepted cache correction17zzy is card bd77d02ea15d813a07ddde9ccb3c060dc3f5095473cb2ccceaf0984e18ee51e9 and gate b7a6aa2590f4695a646f776c1493f80f9f4dcb24b9e8c00d78aa4548d694140a. Semantic gate469fd1c701a2af3149ff7401c7592a4fb313ad7ee64b922105d86c27700eb09b. Check current quota and TYPE-only shared capacity; no protected instance details.
2. Only when ROOT can complete the metadata finalization without a queued review, choose new literal suffix and fixed setup/math/task/SYSTEM/USER termination UTC clocks. Record once before use, arm worker termination at launch, never reset. Fresh one-batch worker4vCPU is sufficient for this serial600wall/500CPU/2GiB/16MiB batch; preserve EBS. No scientific bootstrap or package updates.
3. Verify fresh ready/EC2/host/boot/PID namespace/device and cache-inclusive native collection. Any SSH timeout: exactly one ops/sg_autoupdate.sh before retry. No old boot/native hash reuse.
4. Validate the WHOLE fresh structured inventory: all record counts/format/ownership/modes/file hashes/stats/sizes/aliases/directory children/absence/three Python paths. Save fresh native-manifest.json and native.sha256 with canonical schema. Template collector only appends literal /etc/ld.so.cache; do not recursively inventory /etc or relax map checks.
5. Final ROOT card records exact physical/source/native/gate bindings, original clocks, all12 phases and status interpretation. Freeze/hash card before registration. Fill every registration path/value from this actual worker and card; set enabled=true ONLY after these predicates pass. Runtime permits no extra keys merely to record documentary notes.
6. Authority JSON is exact old JOB f10-r2-reconstruction-v1/status REGISTERED/authority ROOT-CAPRUN/files SCIENCE/runtime python_sha256 and native_inventory_sha256/limits600,500,2147483648,16777216 string values. Mutations: status DISABLED; caps cpu499; inventory deletecheck.py; source produce hash zeros. All others use valid bytes. Recursively sorted compact JSON plus ONE newline. Freeze/hash previews; fill authorization flag positions for each vector (refuse-hash remains64zeros). Mutate vector preserves exactly ONE ROOT_REGISTRATION_SHA256 at its unique flag. Registration cannot contain its own literal final SHA.
7. Freeze/hash actual registration, then fill setup and installation hash list including its SHA. Confirm no unresolved placeholders, no old physical/stem/native pins, full12 literal arrays, fixed caps/timers, expected source sets and positive-baseline-rerun absent. SCP each exact17 source plus fresh native.stdout/hashlist/setup to stage, then single ROOT setup BEFORE original admission. Setup verifies all hashes/layouts/native again and captures actual outer PID/PGID/start ticks.
8. At terminal SYSTEM MainPID/ControlPID0 and original PID/cgroup quiet, custody SHA/WHOLE FIRST, all source/native/working/durable hash checks and archive/sync/download before early instance termination. Worker EBS retained; no unchanged retry or cap raise. Both actual6/10 rejections are still owed. A new different failure STOPs; it is not a semantic pass.

Templates are metadata preparation, not a new mathematical instrument or independently reviewed runtime result. ROOT must read final actual values and own the actual registration. No credentials, coefficient body or mathematical integer payload appears here.
