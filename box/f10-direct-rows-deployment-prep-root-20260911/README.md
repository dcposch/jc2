# Direct-row deployment preparation — DISABLED / UNEXECUTED

ROOT input inspection began2026-09-11 04:04:56UTC; template authoring04:07.
Original preparation reserve04:14/HARD04:17, recorded at authoring, not worker
clocks. This is worker-independent metadata preparation, no scientific run,
allocation, new supervisor or enabled registration. All template release and
physical placeholders remain unresolved. ROOT-READY-INPUTS.sha256 must stay
ABSENT until the complete worker-independent registration packet is ready.

## Accepted inputs and pending execution boundary

The scientific FIRST0d6cb121... accepts A-E and prescribes one polynomial-power
correction. The producer owner completed that exact sole hunk in a new file;
ROOT collected whole source/report/documents, expected transaction and literal
regenerated diff, with all postpins matching04:05:45. ROOT-SOURCE-CONTRACT.json
binds the accepted specification, FIRST, correction and final five hashes.
No generic-support estimate or cap-sufficiency claim is adopted. SOURCE-INPUTS
pins the exact five science files, three wrappers and unchanged CAPRUN.

Runtime FIRSTa02f120a... conditionally accepts the unchanged three wrappers.
ROOT collected it receipt-first after original PIDs/cgroup were absent, then
whole report/custody and all current postpins04:03:57. Its optional stronger
mutation inverse test is NOT selected. Saved-slot restoration establishes
outside-slot invariance and serialization fidelity only; it is not an
arithmetic inverse test. The registered T-coordinate edit survives static
review, but actual positive/negative reachability is still unknown.

Seven exact refusal triples are separately being derived from the final source
text. No guessed traceback, wildcard, substring or generic nonzero exit is
accepted. Their final source vector must match SOURCE-INPUTS. CPython build,
absolute paths, source line numbers and error-highlight behavior must agree
with the derivation before release. Caps and inventory share one exception
branch and therefore the same expected bytes; their mutations differ.

## Exact prospective layout and inherited machinery

Administrative basename: jc2-r3-directrows-20260911a. It is not a launch time.
Science: /opt/jc2-r3-directrows-20260911a/science, exactly five Python files;
wrapper: sibling wrapper/, exactly dispatch.py/probe.py/mutate.py;
CAPRUN: runtime/run_capped.py. All root-owned directories0755/files0444.
Metadata remains outside outputs: ROOT-REGISTRATION.json, ROOT execution
card, ROOT-SOURCE-CONTRACT.json, preflight-policy.json, native-manifest.json,
source-native-manifest.json, native.stdout and native.sha256. Setup installs
both new metadata files and checks their stage/installed manifests.

Working /run/jc2-r3-directrows-20260911a is128MiB tmpfs; authority/frozen0755
root, writer0700 UID/GID65534. Durable /var/lib/jc2-r3-directrows-20260911a/
custody must be fresh and on EBS; stage/recovery under /home/ubuntu/
jc2-r3-directrows-20260911a/. No cleanup or deletion script is introduced.

The five templates are text derivatives of the old r3 deployment templates,
whose original pins are retained in PREPARATION-PINS.json. Deltas: unique
administrative paths/tags and direct-row release names; setup additionally
installs and placeholder-checks the source-contract and preflight-policy files.
Native collection itself is unchanged. No template is executed or syntax-tested.

## Registration composition, before allocation

Use the NEW runtime DISABLED-REGISTRATION.json, not the old rational schema.
Source_files are the five final hashes. Source_pins has5..64 absolute keys,
including all five INSTALLED science paths with identical values; documentary
reference paths may refer to the retained coordinator copies and need not be
installed in the exact science directory. Contract_sha256 is the hash of
ROOT-SOURCE-CONTRACT.json, not a self-hash. Review_clearance binds source and
runtime FIRST reports plus this source-contract's explicit prescribed fix.
Place remains an untrusted ROOT input requiring both evaluators' reconstruction;
no current candidate is certified fully admissible.

Preflight policy has schema f10-direct-rows-preflight-policy/v1, source_files
and exactly seven entries, each exit_code JSON integer1..125, stdout empty,
stderr exact ASCII bytes<=8192 ending newline. Pin/install it outside outputs.
All mathematical integers cross metadata as decimal strings; UID/GID/device
and runtime profiles retain their declared JSON metadata integer types.

Thirteen phases are unchanged: five ACTUAL producer refusals, valid ROOT-policy
probe, two ACTUAL wrong-tag entries, dummy, producer, positive checker, isolated
T mutator, negative checker. Science and probe flags -E -s -S -B; outer/CAPRUN/
mutator -I -S -B; clear environment PATH=/usr/bin:/bin LANG=C LC_ALL=C TZ=UTC.
Probe uses --registration ROOT --policy AUTH --policy-sha256 HASH --mode
valid|dummy --output TARGET, not the old source-authority probe CLI. Mutator
uses ROOT registration hash and the genuine positive.json derivative record.

Precompute every non-checker authority/policy digest from the literal NEW
dispatcher derivation. Science authorities have eleven keys; helper policies
have the separate ROOT-policy schema, ROOT PATH not ROOT hash, science_outcome
NONE. Only the two checkers have AUTHORIZATION_SHA256 at their named flag;
only mutator has ROOT_REGISTRATION_SHA256. No hash self-cycle or generic slot.
Actual physical/native facts, four original UTC stops, command vectors,
installed/current pins and phase/typed-slot policies must be fully bound.

## Unchanged caps, future release and closure

Each source900wall/600CPU/8GiB/128MiB; batch3000wall/2100CPU. Science profile
900/599; controls5/3; dummy32MiB; mutator30/20. Joint packets120MiB plus ALL
metadata/streams8MiB. Sampled2050CPU plus50nominal cleanup is not a kernel
hard aggregate guarantee. No cap raise, retry, extra smoke or rank task.
Full six-field native closure<=1MiB/3000files/1000directories is distinct from
the<=64KiB matched source subset containing the interpreter. No downgrade.

Future release needs fresh scoped AWS type/quota/image/network/fleet facts,
complete offline packet, original setup/admission/math/custody/retirement clocks,
and exact-ID backup retirement armed immediately after allocation. Preserve
100GiB gp3 root EBS; termination protection stays off. No worker held for review.
Current templates are DISABLED; c7i.2xlarge is a candidate, not an allocation.

Retain original outer controls: fresh cgroup, TERM/KILL armed before launch,
RuntimeMax3000/MemoryMax8GiB/Swap0/Tasks32/CPUQuota69% at10ms, actual same-call
PID/Invocation/ns/cgroup capture. Actual13-phase observations must meet every
registered predicate; unexpected outcome stops without retry. After all original
writers are absent, collect receipt/custody first, verify pins and exact streams,
secure byte-identical EBS archive, compare/fsync/download/hash replay/fsync,
then retire exact worker with data retained. Do not inspect live payload bodies.

No execution, baseline, speedup, admissible place, source exclusion, rank,
all-r theorem or JC2 resolution is established by this preparation.
