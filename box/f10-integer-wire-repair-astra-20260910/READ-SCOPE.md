# Decimal-integer repair: documentary scope

First action 2026-09-10T04:16:52.462553255Z; own targets absent. Controlling
stop 04:34:00 UTC, final two-minute reserve from04:32, never reset.

All13 local inputs were current-hashed before WHOLE bodies, including the
ROOT-CARD first. input-pins.json records exact paths, full hashes and bytes;
three task-supplied abbreviated pins were expanded from their exact named
current files and matched the supplied prefixes. Every source/report was
read WHOLE in this task. No provenance or unrelated body was followed.

Additional permitted primary API source: official Python3.12 documentation,
rendered current title3.12.14. Selected sys lines850-855 and stdtypes
lines3123-3228 were read for the setter, decimal conversion APIs, zero and
interpreter scope. python-api-response.json retains the web response,
URLs, bounded retrieval interval and exact read scope; its hash is in
custody. It is not a raw HTML snapshot or an installed-version claim.
Initial broad open output was not treated as whole-page coverage.

Root explicitly confirmed the unavoidable additional checker-pin change
in generic_controls.py and its resulting harness pin in the separate
generic caller. It also allowed the three scoped setter placements,
conditional on the demonstrated call order. No extra runtime case was
approved or added. The old plan and all12 case definitions are untouched.

Five original code snapshots are exact-byte copies for documentary diffs.
New source bodies were read as original WHOLE bytes plus all exact changed
hunks; all five regenerated diffs were read. This is source-text reasoning,
not syntax, AST, compilation, import, arithmetic, runtime or fixture testing.
Evidence and semantic-controls were read WHOLE and remain at original
paths unchanged; algebra was not an allowed body and was not read/copied.
No source coefficients or certificate body were read or generated.

Own report and prospective-control specification are read WHOLE before
the report marker; own-only OPEN/collision checks and the ordinary
begin/close/finalize/expected-manifest verify workflow establish publication
custody. No new canonical OPEN ID, authority, deployment, review invitation,
worker action, cap change or dependent task is issued. Terminal custody
lists every owned object except itself and all13 unchanged original pins.
