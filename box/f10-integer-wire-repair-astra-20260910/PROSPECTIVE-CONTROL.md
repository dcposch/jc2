# Disabled, prospective large-decimal regression specification

This file is a test specification, not executable code, an authority,
generated fixture, runtime registration or PASS record. All original
eight positives and four negatives in the frozen generic plan remain
unchanged. Root expressly allowed this separate specification instead of
adding a thirteenth harness case or changing the plan/receipt schema.

One future separately authorized bounded synthetic control can exercise
the actual FLINT-to-Python conversion, raw cofactor export, fresh-parser
boundary and a meaningful changed-object rejection. No actual source
acceptance may be issued for the fixture.

Use a permitted monic squarefree rank-seven coefficient algebra and scalar
coefficients. Let N be the positive integer whose decimal spelling is a
one, then 7420 zeroes, then a one: N=10^7421+1, 7422 digits. Define
f1=T^2, f2=1+N*T+T^3, f3..f9=0, q=1. The minimum pivot is f1, degree2.
The remainder columns of f2 and T*f2 are 1+N*T and T. With the retained
column order, exact RREF yields h2=1-N*T; raw-pivot reconstruction gives
h1=N^2-T+N*T^2. The factored identity is

    (N^2-T+N*T^2)*T^2 + (1-N*T)*(1+N*T+T^3) = 1.

This is a hand specification; neither N nor any coefficient/fixture was
computed, materialized, parsed or tested during this task. Unlike a lone
constant generator, it exercises positive-degree quotient RREF extraction
and large raw-cofactor export, including the larger coefficient N^2.

Old/new discriminator: in separate registered children with the ordinary
4300-digit policy initially observed, the old generic API is expected to
encounter the decimal-conversion ValueError. Its Python int(string) call
on a successfully reached RREF coefficient -N cannot pass that policy;
record the actual first failure point rather than assuming the native
constructor reaches RREF with this fixture. An earlier failure does not
establish RREF-path coverage. The repaired generic API sets zero before
native construction, extraction and export. Require
the new full UNIT certificate to satisfy all217 coordinates through the
independent dense fixture verifier, with all1638 witness entries retained.
Do not claim an old/new result until both actual outcomes are collected.

Fresh checker boundary: a separately registered test child must begin with
the normal finite conversion limit and enter the repaired checker.verify
without calling solver first. To use that ACTUAL-schema verifier, build
the same synthetic identity over its literal fixed modulus, pad the nine
named row envelopes and q/r/h0 envelopes exactly, choose r=h0=q=1, and
label the data synthetic. Do not fabricate ROOT source acceptance or call
checker.main with invented acceptance. Its internal verify API is already
licensed for an explicitly registered test harness. All217 equations must
pass. This prospective source-text specification does not construct those
schema objects, provide harness code or authorize that execution.

Meaningful mutation: change only the canonical constant coefficient of
h1 from N^2 to N^2+1. Keep the full217/1638 dimensions, positive reduced
denominators and all other coefficients. The residual becomes T^2, so
the required failure is ValueError with exact text
`full 217-coordinate sum h_i f_i equals q^5`, not a 4300-digit error.
Retain/check the actually changed artifact and its hashes. A canonical
leading-zero mutation can additionally be assessed at review, but is not
an added runtime case authorized here.

No unbounded test, random farm, coefficient-height prediction, current
source solve, fixture count change, cap change or new acceptance follows.
Runtime staging and limits require a separate root registration after the
first independent static gate. All current CPU/RSS/wall/aggregate/file
caps and both full certificate branches remain requirements.
