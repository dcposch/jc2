# One disabled real-certificate runtime contract

STATIC/UNEXECUTED. No source import, compilation, AST, syntax check, test, fixture, probe, dummy or scientific subprocess occurred. All three accepted science files are byte-identical copies. New source consists only of dispatch.py and probe.py; no new supervisor, mathematics, corruption fixture batch or science implementation is supplied.

## Fixed future sequence

There are **nine** preflight CAPRUN invocations, not seven: five actual authority refusals (disabled, hostname, script argv, caps, missing required producer pin), one authorized sentinel, wrong-job startup through each of the two actual science CLIs, and one descendant/RSS dummy. Then at most one producer and, only after a normal candidate, one fresh independent checker. Thus at most eleven CAPRUN invocations. A failure halts; there is no retry or implicit replacement. The twelve mathematical corruption classes in the accepted parent remain specification only and are not dispatched.

Probe and science full interpreter flags are `-E -s -S -B`; never `-I` or `-P`. The dispatcher and unchanged CAPRUN use `-I -S -B`, per the separately pinned ROOT startup delta. Sibling authority is resolved only in a pinned root-owned, non-writable-to-science source directory. Every path is absolute/canonical; the literal resolved interpreter is used, with no discovery/fallback. Full outer `/proc/self/cmdline` is compared to ROOT's outer vector; every complete CAPRUN/setpriv/Python command is compared to the registered phase vector. `PYTHONPATH`, `LD_PRELOAD`, `LD_LIBRARY_PATH` are explicitly cleared. ROOT must pin any importable installed pyc and ZIP source: -B prevents writes, not reads; -S suppresses site customization.

Dispatcher runs uid0. Each CAPRUN payload is `/usr/bin/setpriv` at its ROOT-pinned canonical path, `--reuid UID --regid GID --clear-groups --no-new-privs -- PYTHON -E -s -S -B SCRIPT ...`; UID/GID must be positive. setpriv exec preserves the CAPRUN child's PID/PGID and namespace. Installed setpriv compatibility is untested: actual startup controls must prove it before science. No inner setsid or GNU timeout exists. CAPRUN alone starts and supervises each group, preserving its reviewed per-process CPU / sampled RSS limitations and cleanup overshoot. ROOT must independently provide the exact outer systemd cgroup/safety cutoff and backup worker stop. Dispatcher sends only SIGTERM to its own unreaped CAPRUN Popen if return is late; it neither invents a group supervisor nor kills unrelated processes. If CAPRUN/group remains live, it refuses payload reads and reports custody incomplete for ROOT cleanup.

## ROOT-only setup and schemas

The JSON template is disabled/null. ROOT must issue a new mode-0444, uid0, immutable-ancestry registration with schema `f10-middle-real-runtime/v1`. It must provide exact files roles `{python,setpriv,caprun,producer,checker,authority,probe,dispatcher}`, their pinned absolute paths, source_root/cwd, root registration full outer argv, all eleven literal commands, positive UID/GID, hostname/instance/DMI, namespace/cgroup, original offset-explicit deadlines, and all output/durable paths. The file gate requires source ancestry owned by root with no group/other write bits: a writable `/home/ubuntu` ancestor is unsuitable; ROOT must choose an appropriate fresh trusted path. No actual host/path/UID/native pin is invented here.

`pins` must include all roles; all eight actual science report files; the accepted implementation and gate reports; and the separate native manifest. Expected science and report hashes are literal in dispatch.py. All listed files are hashed before and after every child. The native manifest schema is `{"schema":"f10-native-closure/v1","files":{ABSOLUTE_CANONICAL_PATH:SHA256,...}}`, root-owned readonly, capped at 256 KiB / 1500 entries. Dispatcher verifies every listed canonical file, including ROOT's resolved interpreter/stdlib/ELF closure and CAPRUN's selected ps executable; no discovery or installation occurs. The manifest hash, rather than its whole closure, is repeated in each <64 KiB authority. This preserves accepted authority.py's hard size limit without modifying it. ROOT must authenticate the completeness of that installed closure; this package cannot infer it.

ROOT provisions one NEW dedicated tmpfs output mount of total `statvfs.f_blocks*f_frsize <= 16777216`, with exact mountinfo tmpfs identity. Its parent is root-owned, not writable by science. Three distinct direct children are required initially empty: writer_dir owned by the registered science UID/GID mode0700, frozen_dir and authority_dir root-only. The source directory and native manifests are outside this working mount. ROOT must put the outer dispatch stdout/stderr on the same tmpfs too; the dispatcher puts every phase stream, CAPRUN stream, telemetry, live identity, authority, pre/post record and scientific payload there. The mount's actual finite capacity mechanically bounds working output, unlike a per-file FSIZE cap. The inherited 16 MiB FSIZE limit is additional, not the aggregate argument. Platform service logs outside these registered streams are operational records, not an extra payload channel; ROOT must not configure an unbounded duplicate scientific stdout sink.

On terminal producer success, its file is read only for schema/status as ordinary JSON, without numerical coefficient interpretation. It is moved within the tmpfs (not duplicated/deleted) to absent `frozen_dir/certificate.json`, chowned root, chmod0444; frozen_dir becomes0555. Its hash is compared across the move. Only then does dispatcher write a separate root-owned0444 checker authority, pinning the frozen input. Authorities are created exclusively, flushed, hashed and recorded BEFORE launch; input/native/authority hashes are checked again after the exact cgroup is quiet. No concurrent authority or source writer is allowed. The accepted read/hash TOCTOU is closed by these ownership/parent guarantees, not by a code patch.

## Original fixed budgets

The registration must use JOINT 360 wall / 330 CPU / 2 GiB sampled RSS / 16 MiB aggregate wire, and the exact phase profiles in code: preflight (5 wall,3 CPU,2 GiB), dummy (5,3,32 MiB), producer (300,279,2 GiB), checker (45,49,2 GiB). Preflight has its own 180-second monotonic aggregate and must precede the same ROOT absolute science stop. Its CPU limits are per process, including the dummy's fork; no false preflight aggregate-CPU promise is made.

The science soft CPU choices 279+49 deliberately reserve CAPRUN's +1 hard-limit allowance in each phase: hard limits sum to330. The child science has no descendants. Accepted per-process/scheduling limitations remain; this is not a strict real-time accounting guarantee. The dispatcher additionally rejects a post-return `RUSAGE_CHILDREN` charge >330, conservatively including CAPRUN and ps overhead. It reports this conservative value and dispatcher self CPU separately; it does not pretend to measure isolated science CPU. Actual timing/usage is unmeasured now.

Science monotonic first-start+360 is stored once, never reset by checker. Each admission requires its phase wall+15 seconds within both original remaining monotonic and absolute budgets. Post-return checks enforce the same original limits. Nominal child walls sum to345, leaving15 for transfer/hash/authority rather than a second formation. Metadata or cleanup consuming that reserve can cause admission refusal/STOP. No cap rise is implied. ROOT's later actual deadlines must leave separate terminal durable-custody/stop time; this static task sets none.

## Prospective control semantics and evidence

Five refusal calls go through actual unchanged `authority.authorize`, require NORMAL_EXIT/2, exact stderr reason and no sentinel. The valid probe authorizes and writes a typed nonroot identity sentinel. The two actual science wrong-job CLIs must produce exactly `INCONCLUSIVE: wrong job or platform`; the checker gets an absolute absent startup input but must reject before inspecting it. These are startup proofs, not scientific evaluations.

The no-math dummy emits four ordered events: parent started; same-group child ignores TERM and signals readiness; parent exit pending; orphan allocates/touches64MiB. CAPRUN's32MiB sampled cap must detect RSS, validate exact leader PID/PGID/start identity before both TERM and KILL, send integer15 then9, reap the leader, and report cleanup complete with no nonzombie descendants. Namespace and four-event identities are compared; a missed event or early sample fails rather than being relabeled. Zombies awaiting system reaping are not claimed absent. This is source only, with zero observed controls.

Actual Python child argv/identity is captured through the exact CAPRUN child's children file when observable, with an explicit post flag if it finishes too early. CAPRUN terminal argv hashes/caps/stream hashes/start identity and absence of its reaped leader are independently checked. The dispatcher additionally reads only its registered systemd cgroup and requires no other nonzombie process before metadata or payload bodies. It does not inspect unrelated cgroups or process names.

## Durable terminal custody

ROOT must register an absent durable_dir with a trusted root-owned EBS ext4/xfs parent and exact device identifier. After all owned groups are quiet, including on STOP where possible, dispatcher byte-copies every regular working-mount file to that new root-owned directory, fsyncs files, checks source/copy hashes, makes copies0444, and writes a bounded custody manifest. The manifest identifies replicas as custody copies, not extra scientific outputs. Working-output16MiB and durable replica storage are separate budgets; ROOT must reserve durable capacity independently. No deletion, unmount, worker stop or restart is implemented. If the tmpfs fills, group is not quiet, or durable transfer fails, STOP_CUSTODY_INCOMPLETE requires ROOT's bounded terminal recovery; no result is accepted. Backup stop must be later than registered durable-custody time. No source bytes are read while live.

Nested durable directories are fsynced bottom-up and the original task deadline is checked during transfer/finalization. After successful custody the dispatcher performs no further stdout write (its registered stdout is itself among the copies). ROOT reads the durable CUSTODY.json only after process termination; exit code alone is not a mathematical verdict. A failed/incomplete copy can report operational STOP on stdout and requires ROOT recovery, not a claimed complete replica.

## Remaining obligations

Two task-local pending quantities, no new canonical OPEN ID: (1) new dispatcher/probe static correctness, zero independent reviews of these new bytes; cheapest next test one FIRST different-model static gate; (2) installed entry/control/runtime and actual certificate, zero executions; cheapest test only after that gate is one ROOT-registered fixed batch with terminal custody. All API choices, tmpfs/ownership/native closure setup, setpriv behavior, timing, resource telemetry and durable copy remain untested. No positive certificate, speed, nonresonance, source exclusion, JC2 claim or launch authority is asserted.
