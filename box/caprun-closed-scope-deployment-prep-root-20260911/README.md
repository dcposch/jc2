# Closed-child deployment preparation — DISABLED, UNEXECUTED

ROOT first preparation2026-09-11 09:40:43UTC. Original closeout reserve09:53,
HARD09:56UTC, never reset. These are administrative templates only. No worker,
active registration, release token, source invocation, test/dummy/import/AST/
syntax check or scientific result. ROOT-READY-INPUTS.sha256 remains ABSENT.

## Materialized administrative steps

- setup-pre-admission.template.sh ports the old necessary-row installer to
  jc2-closedchild-preflight9-20260911a, preserving exact5science/3wrapper/
  separateCAPRUN roles, root0444 installation, native checks,128MiB tmpfs and
  retained durable EBS. Final registration and final execution card are NOT
  installed. A separately frozen admin/holder.py and root0700 admission
  channel/root0600 FIFO are installed outside science/output inventories.
  Pre-admission stage/installed manifests exclude final registration/card.
- physical-metadata.template.sh adds ROOT/PID1 pid/mnt/cgroup/user namespace
  comparison and exact root cgroup2 mount checks to the old read-only physical
  collection. These are observed comparisons only, not an adversarial-root
  or full ACL/delegation proof. Its only existing setup action creates the
  fresh staging directories. No holder/unit is created here.
- native-metadata.template.sh is BYTE-IDENTICAL to the old qualified native
  collector; no native/Python/source execution. It inventories the full
  Python stdlib/binaries/dependencies. ROOT must separately qualify/pin the
  administrative shell/jq/findmnt/mkfifo/coreutils helpers and installed
  signal/FIFO/fd behavior; no new native fact is claimed from this copy.
- launch-worker.template.sh changes only deployment identity/record paths
  and strict hash-list checking. It retains exact-ID coordinator user-systemd
  retirement,100GiB DeleteOnTermination=false, original clocks and one worker.
  It is disabled, and its ready manifest is absent. No actual future AMI,
  allocation deadline, quota, instance or live worker timer exists yet.
- prepare-leaves.template.sh is a disabled ROOT administrative step AFTER
  holder capture. It checks the captured PID/start/InvocationID/argv and
  same namespaces, then creates exactly nine initially absent direct leaves,
  verifies domain/empty controls/procs/population/no descendants and records
  actual device/inode/interface metadata. Mode bits do not prove absence of
  effective ACLs: ROOT must separately establish ownership/no migration/
  delegation/entrant/concurrent ROOT or systemd mutation before authorization.
  It does not construct registration, send a token or clean up on failure.
- final-install.template.sh compares the separately completed ROOT registration
  and its hash against actual holder/nine-leaf identities and current pins,
  requires only nine phases/no typed slots/no phase policy/CPU origin0,
  installs final registration and card once root0444, fsyncs and compares them.
  It neither serializes final facts nor sends a token. Missing/new/changed
  data, a dead holder or cutoff means STOP; partial installation is preserved.
- ROOT-EXECUTION-CARD.template.md is a new nine-phase card, not a relabeled
  old13-phase release. It distinguishes coordinator retirement from worker
  TERM/KILL, records all remaining actual bindings, and forbids science/retry.
- preflight-policy.json ports exactly16 installation-path occurrences in the
  retained seven-refusal policy. Ten documentary JSON comparisons passed:
  schema, unchanged five-source map, exact7count and all7 complete triples
  modulo ONLY that path replacement. The actual authority/produce/check
  source hashes still match; authority74/78/82/95/100 and produce343/369 were
  reread with line numbers. No source was run. Future installed interpreter
  formatting must still match the qualified predicate; new errors are STOP.

## Blocking integration issue — not silently fixed

The collected Astra outer-admission template checks
systemctl is-active JC2_WORKER_STOP_TIMER_PLACEHOLDER ON THE WORKER. The actual
retirement launcher creates the timer with systemd-run --user ON THE
COORDINATOR. The worker has no AWS credentials/retirement timer supplied by
this deployment. Consequently the current combination cannot be released as
ready. A dummy remote timer or unrelated active unit is not a substitute.

The Fable FIRST was already running on nine frozen inputs when ROOT identified
this actual launcher integration mismatch. Its prompt does not include the
worker launcher; do not imply it reviewed that missing interface. Do not edit
the live charged candidate or assume its eventual verdict fixes this issue.
After collection, make the smallest necessary source/interface correction:
keep the exact-ID retirement check on the coordinator immediately before
remote admission, and bind its actual receipt externally; the worker admission
must not pretend to query that user-systemd instance. The precise correction
still needs a focused gate before release. No extra remote retirement service,
fake attestation, cap change or generalized supervisor is selected.

## Remaining release obligations

The new holder/admission gate is live; it can at most accept its exact static
bytes. None of these preparatory files is an actual-host attestation. Before
allocation complete the correction/gate, exact metadata serialization recipe
and helper qualification plan, ready inventory and original clock ordering.
Before admitting a holder, finish all worker-independent/native/source/policy
binding work; only actual outer/leaf identity and final freeze should remain
inside its unchanged120-second ceiling. No clock is reset at admission/exec.

ROOT must authenticate installed native startup and current identity/namespace/
mount/delegation/ACL facts, fill the nine authority objects, hash each exact
sorted compact ASCII JSON plus newline (eight digests; refuse-hash stayszero),
and compare every whole argv with dispatcher generation. Existing disabled
metadata comparisons are not a future-host release. Final successful-science
authority is forbidden. Only PREFLIGHT_ONLY_9 is selected; FULL13 stays off.

After final freeze, ROOT separately binds actual final SHA and live identity
to one bounded one-writer release frame. No restart, retry, partial-stream
recovery, clock rebasing, capraise or relaxed dummy. Exact release/terminal
custody is still unimplemented/unobserved here. Old coordinator retirement
remains obligatory, earlier retirement follows secured evidence, no idle
worker for review. Evidence EBS and protected infrastructure remain untouched.

Read scope: all old administrative templates and the collected new holder/
outer/README/report/PINS/custody were read WHOLE; registration/context metadata
is machine-WHOLE plus selected human fields, not a human-WHOLE claim. No live
Fable payload read. New template readback and actual pins are recorded at
closeout. An initial ROOT text-generation mistake reduced shell $$ to $ in
the physical copy; whole readback caught and corrected it before any execution.
No syntax/AST/test/import was used to discover or repair it.

No r3 baseline, qualified place/rank/source-zero or JC2 result. Canonical
inverse remains a SOURCE HYPOTHESIS; alternative inverse/poles NOT_COMPUTED.
