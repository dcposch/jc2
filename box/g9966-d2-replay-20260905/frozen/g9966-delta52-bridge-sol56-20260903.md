# Nonlinear effective-T2/T3 bridge on the (99,66), delta=5/2 joint chart

Date opened: 2026-09-03; computation completed: 2026-09-04 UTC  
Lane: `g9966-delta52-bridge-sol56-20260903`  
Final type: `COUNTING-BOUND[EXACT-T2-PREFIX-s63]` and
`OPEN[FULL-NONLINEAR-T2-T3-ELIMINATION]`

## Executive verdict

The charged stage-7 endpoint was reconstructed exactly over `Q`, in its own
coordinate order, with **869 free coefficients**.  The four T2 and 34 T3
coefficients were adjoined, the principal-minor pullback was implemented
without a pi cap, and replayable full coefficient-ideal scripts were emitted
over `Q` and three denominator-safe primes.

The requested full elimination did **not** finish.  Three specialized support
runs prove that at least 4,617 distinct T2 coefficient polynomials and at least
43,819 distinct T3 coefficient polynomials are nonzero over `Q`; they do not
compute rank or dimension.  Four chart-valued Singular attempts stopped in
component construction, before coefficient extraction or a Groebner basis.
The direct exact T2 expansion and an exact prefix through `s^100` each reached
the foreground 3,000-second cap before producing all requested rows; the
second run returned 124 with empty redirected logs, so a separate status
record preserves that boundary without pretending the logs contain progress.

There is nevertheless a small exact result.  The first mandatory T2
coefficient is

```text
T2_s56_pi0 = -98304*(B1c_7_19-B1c_7_20+B1c_7_21-B1c_7_22
                     +B1c_7_23-B1c_7_24+B1c_7_25).
```

Its coefficient `-98304` is in `Q*`, so it is the first certified Q-star pivot.
The completed exact prefix through `s^63` contains five rows, all five are
independent Q-star pivots, and its nonlinear residue is zero.  In the stage-7
polynomial chart localized at `c` and then at the nonzero Jacobian degree-zero
coefficient `J0`, after adjoining all 38 T-coefficients, this prefix quotient
has dimension **902**.  Hence the full bridge locus is either empty or has
dimension at most **902**.  On the separate `c=1` slice it is either empty or
has dimension at most **901**.  These are counting bounds, not the dimension
of the uncomputed full bridge.

No rational unit identity was obtained.  Therefore this lane is **not
`DEAD`**, is not a `REPRESENTATIVE`, and does not prove “no Keller pair with
the (99,66) skeleton.”  The old 167-dimensional straight-slice linearization
is not reused.

## 1. Frozen custody and lane scope

The receipt
`xmodel/g9966-delta52-bridge-sol56-20260903.run.v2` was parsed mechanically:
`awk` paired every `charged_input_<i>_sha256` with the corresponding basename,
produced `box/g9966d52bridge-20260903/inputs.sha256`, and `sha256sum -c`
returned `OK` for all **21/21** frozen files.  The captured output is
`inputs.check`.  No digest was transcribed into the checking command.

This lane changes only `box/g9966d52bridge-20260903/` and this report.  It did
not edit a ledger, did not enter `jc2-lean`, and did not continue the parallel
stage-8 Jacobian bands.  The emitted next system explicitly requires union
with those independently promoted rows rather than duplicating them.

The central charged hashes rechecked by the engine are:

| object | SHA-256 |
|---|---|
| `stage7.json` | `a85d602dc4599c600c12b04e2c3140f00dafafbb5793b3b05039050cba6159b3` |
| stage-7 free list | `30d4f3e027d4bb1db62dce3b7226e15f0ac8a74654724d0ede595cc0d10bbbe9` |
| `next-stage8-system.json` | `a050af8c5d4bbb703babb2ce2ee57a70c411eb801226ac9f5cdcffbfa0bfa699` |
| charged `band_engine.py` | `3bd2937d75d59d59edeca1b89eba286fd2711f2d608109e7fd8971b64e059ac9` |

## 2. Stage-7 endpoint and declared ring map

The engine rebuilt the charged major and outer systems rather than matching
variables by name alone.  Before the joint pivots the count is

```text
major inner including c,u,v    102
outer free after D1             826
ambient                         928
joint B1 pivots                 -59
stage-7 free                    869
```

The 176 outer-D1 pivots are already exhausted in the charged endpoint.  The
59 serialized joint pivots all lie in `B1`; recursive substitution sends
their resolved right sides to zero.  This statement is about the resolved
map, not a claim that every raw right side was syntactically zero.

The free families, in the charged lexicographic generator order, are
`A2:220`, `A3:224`, `B1:103`, `B2:220`, `K2:99`, and the three centre
coordinates `c,u,v`.  The loaded 869-name array is byte-for-byte the charged
free list and agrees with the stage-7 and next-system arrays.  Coefficients are
in `Q`.  Joint pivot images were substituted into every localized component
before forming products.  `bridge-results.json` records
`COMPLETE[MAP]`; the exact map-only run exited zero in 29.69 seconds with
69,592 KiB peak RSS.

Let `R7=Q[the 869 listed generators]`.  The branch assumes `c!=0`, represented
by a Rabinowitsch wrapper when elimination is attempted.  The `c=1` equation
used below is reported as a **slice** unless the campaign separately promotes
the scaling action that makes it a global gauge.

## 3. Principal-minor pullback and order conversion

Notation follows the computation: `F` is the degree-99 coordinate (Xu/Moh's
`g`) and `G` the degree-66 coordinate (Xu/Moh's `f`).  Set

```text
t=s^2,
w=u*s^4+v*s^6+pi*s^7,
z=w-1=-1+u*s^4+v*s^6+pi*s^7,
KF=t^99 F=s^198 F,
KG=t^66 G=s^132 G.
```

The charged chart gives localized series `H,A2,A3,B1,B2` and the literal map

```text
KF = H^3 + EF,       EF=A2*H+A3,
KG = H^2 + EG,       EG=B1*H+B2.
```

At the second point and `delta=5/2`, the promoted root datum is

```text
p=pi*(pi^2-c),
q1=-2*integral(p^3 d pi)
  =-pi^10/5+3*c*pi^8/4-c^2*pi^6+c^3*pi^4/2,
```

with the integration constant normalized to zero.  The exact control verifies
`d(q1)/d(pi)=-2*p^3`; `p^10*q1` has pi-degree 40.

Here `c!=0`, and the three roots of `p` carry the charged `9+9+9` packet
data; this is imported root data, not re-derived from the stage-7 equations.
Xu's displayed exponents give

```text
ord_t T2 = 5*(-8+3*delta) = -5/2,
ord_t T3 = 13*(-8+3*delta)-1+delta = -5.
```

Thus `T2` has actual `s`-order `-5`, and `T3` actual `s`-order `-10`.
Multiplying by `s^396` and `s^1188` moves their leaders to `s^391` and
`s^1178`, respectively.  These calculations agree with Xu, section 8,
pp.12--13, and the source audit in
`xmodel/xu-delta52-lift-gpt55-20260903.md`, lines 63--96.

## 4. The 4+34 effective-root coefficients

The Tschirnhausen-normalized T2 is

```text
T2=G^3-F^2+(a1*F+a0)*G+(b1*F+b0),
```

where the displayed `c0*G^2` coefficient has been set to zero.  The four
adjoined chart variables are `T2_a1,T2_a0,T2_b1,T2_b0`.  In normalized form,

```text
U2=s^396*T2
  =KG^3-KF^2
   +T2_a1*s^66*KF*KG
   +T2_a0*s^264*KG
   +T2_b1*s^198*KF
   +T2_b0*s^396.
```

The last column begins after the target window, but `T2_b0` remains adjoined
as required by the global effective-root shape.

For T3 write

```text
T3=G^9-F^6+sum_{i=2}^9 sum_m T3_a{i}_m{m} F^m G^(9-i),
0<=m<=floor(2i/3), excluding (i,m)=(9,6).
```

Here `alpha_1=0`, and `(9,6)` is excluded because the coefficient of `F^6`
is already the fixed monic `-1`.  The index rule gives exactly **34** free
coefficients.  In `U3=s^1188*T3`, a column `(i,m)` receives shift
`s^(132*i-198*m)`.

The expensive monic faces are cancelled algebraically before expansion:

```text
KG^3-KF^2 = 3*H^4*EG+3*H^2*EG^2+EG^3-2*H^3*EF-EF^2,
KG^9-KF^6 = (KG^3-KF^2)*(KG^6+KG^3*KF^2+KF^4).
```

This is where the requested subleading jets enter; the face spans alone do
not supply `p^5` or `p^10*q1`.  The shapes and coefficient counts are the
printed approximate-root data audited against Moh p.152 and Xu section 7.3.

## 5. Coefficient-wise polynomial bridge ideal

The emitted equations are literal coefficients in the chart ring:

```text
E2=U2-s^391*p^5                         modulo s^392,
E3=U3-s^1178*p^10*q1                    modulo s^1179  (J0=1 form),
```

or, before normalizing the nonzero would-be Jacobian constant term,

```text
E3=U3-s^1178*kappa*p^10*q1,
J0-kappa=0,  Zck*c*kappa-1=0.
```

For every occurring pair `(n,k)`, the row is `[s^n*pi^k]E2` or
`[s^n*pi^k]E3`.  Consequently all lower coefficients vanish and every
coefficient of the target polynomial is matched, including target powers
that do not otherwise occur.  There is no pi quotient and no pi-degree cap.
The only bookkeeping quotient is `s^392=0` for T2 or `s^1179=0` for T3;
because all normalized inputs have nonnegative `s` support, normal form after
every product determines all coefficients through the respective target
exactly.

The full rows are polynomial and generally nonlinear in the 869 chart
coordinates and 38 effective-root coefficients.  Calling the system
“nonlinear” does not mean that every early coefficient is nonlinear: the
first surviving equation happens to be the affine Q-star pivot displayed in
the verdict.

### Support census, not elimination

Deterministic, denominator-audited specializations produced:

| field | nonzero T2 slots | nonzero T3 slots | wall | peak RSS |
|---|---:|---:|---:|---:|
| `GF(32003)` | 4,617 | 43,818 | 191.73 s | 113,968 KiB |
| `GF(104729)` | 4,616 | 43,819 | 194.09 s | 127,616 KiB |
| `GF(1299709)` | 4,617 | 43,819 | 192.62 s | 117,128 KiB |

All runs exited zero.  Observed support ranges are `s=56..391`,
`pi=0..53` for T2 and `s=144..1178`, `pi=0..168` for T3.  A nonzero
good-prime value proves the corresponding rational coefficient polynomial is
not identically zero, hence the safe lower bounds 4,617 and 43,819.  Different
counts across specializations show why they cannot be promoted to exact row
counts.  They provide **zero** Q-star rank, Groebner, dimension, or consistency
information.  `modular-support/results.json` carries this scope guard.

## 6. Exact Q-star prefix and dimension accounting

`t2_first_row.py` reconstructed the exact localized blocks through `s^56`,
formed the structurally cancelled monic residual, and found precisely the
row printed above.  Its canonical SymPy-representation hash is
`4726401ff9500a13ee18e5c3b31d1133d7a187fe62400f5903188d14aa599167`.
This monic residual is the full U2 coefficient there: normalized `KF` and
`KG` start at `s^24` and `s^16`, so the four T2 columns first occur at
`s^106`, `s^280`, `s^222`, and `s^396`, respectively.
Exact affine reduction gives

```text
pivot variable: B1c_7_19
pivot coefficient: -98304 in Q*
right side: B1c_7_20-B1c_7_21+B1c_7_22-B1c_7_23+B1c_7_24-B1c_7_25
residual rows: 0
```

The run exited zero after 313.74 seconds with 209,612 KiB peak RSS.  Its
emitted Singular check has eight active ambient variables plus the wrapper
(nine ring variables), and 899 inactive free variables.  The normal form of `1` modulo the row and
`Zc*c-1` is `1` (so the ideal is not unit), and Singular reports active
dimension 7, hence full dimension `7+899=906`.  The negative wrapper control
`(c,Zc*c-1)` is unit; the positive `c=1` wrapper control is not.

This also has a Jacobian-localized positive control.  In the exact stage-7
chart, set

```text
c=1, A3c_97_1=-1, B2c_64_0=1,
all other stage-7 free generators=0.
```

Then the first T2 row is zero and the genuine charged degree-zero coefficient
`J0=[t^163 w^0]J` is exactly `1`.  The independent verifier exited zero and
records the canonical J0 hash
`eced81485fa88f5ecf3f130463c78e2158c6a1bbe63b7457c16cb5130e3c9b04`.
The Q-star pivot makes the one-row quotient a domain; this point proves that
`J0` is nonzero in it.  Its nonempty principal open therefore retains
dimension 906 after localization at `c*J0`.  Adjoining the constant-term
parameter `kappa` with `J0-kappa` has zero net dimension
effect.  Adding the separate slice equation `c-1` gives dimension 905.
This point controls only the one-row localization; it is not claimed to
satisfy the missing minor-face rows or the remaining Jacobian bands.

The wider exact prefix through `s^63` completed in 886.49 seconds at 440,324
KiB peak RSS.  Its five input rows, ending at `T2_s63_pi1`, pivot successively
on

```text
B1c_7_19, B1c_8_18, B1c_9_17, B1c_10_17, B1c_7_20,
```

with coefficients `+/-98304`; its residual and dependent-row counts are both
zero.  The exact input-row hash is
`6c9f6da4b4c2f38b66f5799d1fa04985ef24782c257949ea11fe48a64c1a4ab7`
and the pivot-ledger hash is
`2701193e77b24abb04594fb2ee30a5a892a0f78c038a77de745cad6c61c6ff7a`.
The emitted Singular replay independently reports dimension `902`, dimension
`901` after `c-1`, nonunit positive wrapper, and unit `c=0` control.  The same
explicit `J0=1` point satisfies all five rows, so localization preserves these
dimensions.  An attempted extension through `s^70` was resource-stopped after
1,816.68 seconds and emitted no further row; it changes none of these counts.

No pivot with a chart-dependent, `c`-dependent, or `kappa`-dependent
coefficient was divided out.  Such a coefficient would require explicit
branching or saturation.  This is the Q-star rule required by the raw-leader
and ring-map guards in `FALLACY-v2.md`.

## 7. Full elimination attempts and why they are inconclusive

The exact symbolic T2 driver ran in the foreground under `timeout 3000` and
completed all five localized blocks: `H:1657`, `A2:2013`, `A3:2013`,
`B1:1001`, `B2:2013` coefficient slots.  It timed out before expanded U2,
row hashing, or Q-star reduction.  A second exact attempt through `s^100`
also exited 124 at 3,000 seconds without emitting rows or pivots.

The replayable full qring scripts were then attempted directly:

| field | stop | last completed checkpoint | resource observation |
|---|---|---|---|
| `GF(32003)` | resource guard | five components; no rows | 1,791.65 s; 15,403,156 KiB last RSS |
| `GF(104729)` | resource guard | five components; no rows | 911.52 s; 10,592,556 KiB last RSS |
| `GF(1299709)` | resource guard | five components; no rows | 832.35 s; 10,227,536 KiB peak |
| `Q` | resource guard | five components; no rows | 757.66 s; 10,069,892 KiB peak |

Each log ends before `raw_coefficient_rows`, Q-star output,
`BEGIN_RESULT`, or a basis.  The apparent `halt 1` is process termination,
not the unit polynomial.  Therefore there is no three-prime modular
elimination result, no exact full-bridge dimension, and no rational certificate.
`full-elimination-attempts.json` distinguishes timeouts, resource stops, and
completed checkpoints.

Even matching modular dimensions would not prove the characteristic-zero
claim.  A branch kill requires `reduce(1,G)=0` over `Q` together with a
verified `lift(I,<1>)` identity in the declared source ring/localization.
Neither exists here.

## 8. Missing minor-face precondition

An exact component probe exposed an additional dependency that the charged
stage-7 quotient itself has not imposed.  In normalized indices, generic
`KF` starts at `n=24` and `KG` at `n=16`, while the promoted minor faces would
be

```text
[s^189]KF=p^9,       [s^126]KG=p^6.
```

At all three good-prime evaluations, both identities fail in the reconstructed
stage-7 quotient.  This is evidence that the intervening F/G minor pole and
face rows are absent, not evidence against the root datum after those rows are
imposed.  Accordingly the bridge equations here are a necessary conditional
cut under the promoted delta-5/2 pi-root gate.  They must be intersected with
the missing minor-face rows and the parallel Jacobian bands before their
`s^391` and `s^1178` terms may be called attained leaders.  The first lower
vanishing row remains a valid necessary equation in that simultaneous system.

This separation is essential: the formal pi-root parameter, a physical place,
and an attained cover series are not identified.

The probe's current source includes a post-run correction to optional
dependency-mask bookkeeping.  The report uses only its unaffected exact
component expressions and modular leading-face tests; the omitted mask counts
are not evidence here.

## 9. Controls

**The `(64,48)->(16,12)` control.**  Here `u_s=1`.  Xu Proposition 7.3 / Moh
Proposition 4.4 makes the minor side nonsplitting, so there is no `q1` bridge:
zero bridge rows and zero bridge unknowns.  The control is
`TRIVIAL[u_s=1]/SATISFIED`, not evidence for a nonlinear split calculation.

**Requested tame, high-degree, two-point control.**  No such polynomial
automorphism can be constructed under either relevant interpretation, which
must not be conflated.  A coordinate polynomial's generic fiber has one
physical place at infinity.  Separately, under the control's leading-form
interpretation of two infinity points as two distinct projective zeros or
directions, Jung--van der Kulk forces the top forms of a non-affine tame
automorphism to be powers of a common linear form.  Thus degrees at least six
cannot be combined with the requested two-point condition.  The correct
typed result is

```text
OPEN[POSITIVE-TWO-INFINITY-BRIDGE-CONTROL:
     SPECIFICATION-INCOMPATIBLE-WITH-TAME-AUTOMORPHISMS].
```

The harness therefore separates the properties rather than fabricating a
witness:

* `F=x+y^6`, `G=y+F^7` has degrees `(6,42)`, Jacobian `1`, and checked inverse
  `y=G-F^7`, `x=F-(G-F^7)^6`; it has one shared infinity direction and the
  two-point bridge is not applicable.
* `F=2x+y`, `G=x+y` has Jacobian `1` and two collective projective infinity
  points, but degrees `(1,1)` and no effective T2/T3 tower.
* `P=y^3(y-x)^8`, `F=P^9`, `G=P^6` has the requested degrees and a two-factor
  top, but Jacobian `0`; `G^3-F^2=0` is correctly rejected as degenerate.

`controls.json` symbolically checks the Jacobians, the high-degree inverse,
the q1 derivative, and the degenerate identities.  The `u_s=1` triviality and
the no-tame-two-point conclusion are source-theoretic controls, not outputs of
that symbolic harness.  Because the combined positive control is impossible,
this lane does not promote the full bridge implementation on control evidence.

## 10. Jacobian normalization and localization audit

Two separate full scripts are retained:

1. Scale-covariant candidate: adjoin `kappa`, impose `J0-kappa`, use target
   `kappa*p^10*q1`, and add `Zck*c*kappa-1`.  This follows the Jacobian factor
   in Xu equation (7.1), but its scale convention must remain part of the
   promoted bridge gate.
2. Literal Xu normalization: impose `J0-1`, use target `p^10*q1`, and add
   `Zc*c-1`.

They are not conflated.  Both retained Q scripts were emitted with `c-1`, so
they are executable **slice** systems; the ungauged localization is the
conceptual next system until a global `c=1` gauge action is promoted.  The support census used the second, numeric
normalization only to test polynomial support; it did not check a Jacobian
equation.  The full attempts used the first plus the `c=1` slice.  A toy qring
control passes source-to-target `imap`, coefficient splitting, pivot dimension
correction, and positive/negative `c*kappa` wrappers, but it is not a result
for the 908-variable bridge.

## 11. Next exact system

The continuation contract is
`box/g9966d52bridge-20260903/next-system.json`.  It is a manifest, not an
executable.  The low-prefix driver currently accepts caps below `s^132`, and
the monolithic full emitters are replayable; filtered emitters above `s^131`
and for staged T3 remain to be implemented.  To avoid repeating the
monolithic memory failure, the contract prescribes:

1. continue T2 in caps `70,100,150,220,300,391`;
2. substitute each certified Q-star pivot through every retained row and
   rebuild the component series;
3. add T3 in increasing caps through `1178`;
4. union the promoted F/G minor-face rows and parallel Jacobian bands;
5. adjoin `J0-kappa` and `Zck*c*kappa-1` (or the separately declared `J0=1`
   normalization);
6. run denominator-audited `GF(32003)`, `GF(104729)`, `GF(1299709)`, then
   exact `Q` under `timeout 3000` per stage;
7. only if the exact remainder is unit, emit and independently verify the
   rational lift identity.

The generated monolithic scripts remain useful replay/fallback systems, but
their existence is not a computation result.

## 12. FALLACY-v2 audit and theorem dependency

* Flag/place/series: the cv flag, second physical point, formal pi-root, and
  pulled-back series remain separately typed.
* Carrier/attainment and floor/attainment: support and a necessary row do not
  prove an attained full Keller pair or an exact full-ideal dimension.
* Saturation: rings, maps, wrappers, and controls are explicit; no `sat()`
  output is silently rewrapped.
* Raw leaders: only a rational constant was used as a pivot.  Vanishing
  chart-dependent leaders must branch.
* Variable/ring map: the charged generator order and images are checked; name
  equality is not used as proof.
* Prime labels are characteristics, not derivatives.  `q1'` above denotes the
  explicitly checked derivative with respect to `pi`.
* Merge-free/M-descent and target/arrival indices are not invoked.
* No exit-price assertion is made, so no exit-charge declaration is present.

The theorem “no Keller pair with the (99,66) skeleton” would require, at a
minimum, the already promoted skeleton and branch-B gate, a source-licensed
delta-5/2 root/face bridge, the full minor-face and Jacobian union, safe
localization at every nonzero factor, closure of all remaining branches, and
an exact rational unit certificate.  This lane supplies only a conditional
necessary cut and replayable next systems.  It therefore cannot state that
theorem.

## 13. Reproduction artifacts

Primary artifacts are under `box/g9966d52bridge-20260903/`:

| artifact | purpose |
|---|---|
| `bridge_engine.py` | exact stage-7 loader, localized series algebra, T2/T3/J0 definitions |
| `t2-first-row.json`, `t2-prefix-s63.json`, `t2-prefix-s63-Q.sing` | exact first row and five-pivot prefix; dimensions and wrappers |
| `first-row-jacobian-control.json` | exact common point with row `0`, `c=1`, `J0=1` |
| `probe-jets.json`, `probe_t2.json` | exact component metrics, face-precondition probe, T2 support bounds |
| `modular-support/results.json` | three-prime T2/T3 support-only results |
| `emit_full_qring.py` | full coefficient extractor, Q-star stage, localization, Groebner/lift emitter |
| `full-elimination-attempts.json` | commands, stops, resources, and checkpoint provenance |
| `controls.py`, `controls.json` | exact source and tame/degenerate controls |
| `next-system.json` | filtered continuation contract |
| `artifacts.sha256` | final mechanically generated digest manifest |

The manifest covers 103 non-cache files and hashes to
`6359bc511a689c1c5ac0bb6fddb2119bd8dfe1b522d648ad8aa5877a7cc4ef37`;
its complete `sha256sum -c` transcript is `artifacts.check`.

Every expensive command used one thread and ran in the foreground beneath
`timeout 3000`; no more than four such processes coexisted.  Empty or partial
logs are retained where they prove only a timeout boundary.  Final process
inspection found no lane job running.

## Conclusion

The missing nonlinear bridge has been encoded as a replayable candidate system
and independently checked at its first row and support level.  It begins with
a genuine rational pivot, but its full
elimination is still open.  The defensible result is the exact necessary
`s<=63` prefix dimension bound **902** (or **901** on the `c=1` slice), conditional on
the promoted delta-5/2 root data and subject to intersection with the missing
minor-face and parallel Jacobian rows.  There is no unit ideal, no full pair,
and no (99,66)-skeleton nonexistence theorem from this lane.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `23535`.
- Body SHA-256:
  `d99f61604fd9a5e68bd3900927735fed66bf0c7dfdf048fa6ac6027f1b7370b1`.
- Frozen basis: `9a813822d081ac72494af8fe39666765fb2aaaa2`.
