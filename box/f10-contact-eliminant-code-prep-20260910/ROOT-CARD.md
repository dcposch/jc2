# ROOT bounded contact certificate implementation
2026-09-10 fresh task. Exactly one owner /root/contact_collision_geometry, Astra.
Accepted17zr interface is now promoted after FIRST Fable76d25dfe; no F or
prescribed exclusion exists. Implement ONE small exact Singular candidate
producer and separate exact identity checker as source TEXT ONLY. Do not run
them or any mathematical subprocess of any size. ROOT owns all future AWS
registration/caps/transport/CAPRUN/dummy/native pins and Fable FIRST gates.

Hard mathematical/source-work stop earlier of first action+16min and04:24:00 UTC;
publication reserve final2min, NEVERRESET. If code/report cannot be finished,
bank a bounded partial and exact missing item, not a skeleton or invented test.
No new framework, scheduler, wrapper, CAPRUN edit, source-family migration or
isolated residue client. The value target is an actual forthcoming eliminant
certificate, not another prose existence theorem.

Charged exactly this card; your accepted interface c1ec52ca; FIRST gate76d25dfe.
You may reuse your earlier WHOLE samebyte reads ONLY after matching these pins.
For API verification read primary official Singular docs only, from pinned
https://raw.githubusercontent.com/Singular/Singular/186962946a86748b0b15bd9e34f02a85e51692c2/doc/reference.doc
and directly referenced official library/test source if strictly needed.
Record exact URL/sourcehash/read sections, not a whole-manual claim.
ROOT already verified liftstd: matrix(G)=matrix(I)*H; lift(G,U): matrix(U)=
matrix(G)*L in a GLOBAL polynomial ring; finduni requires reduced zero-dimensional
GB and emits minimal univariate for each ring variable. Exact official
Tst/Manual/liftstd.tst and finduni.tst support this. Serialization/write API
still needs actual primary verification. Do not assume local ring lift matrix
is identity; choose characteristic0 GLOBAL dp or suitable elimination ordering.

Use literal P=Q[z,V,W,X], I1,I2,I3,I4 and A,B,C,c etc from17zr. Prefer the
smallest sensible exact GB/univariate route: liftstd(I,H), reduced zero-
dimensional GB, finduni or direct elimination; emit nonzero F in Q[X] plus
all four Hi with EXACT polynomial identity F=sumHiIi and source/variable
binding. No GB header, floating root list, radical reduction or unverified
library claim is a certificate. Separate checker reconstructs original I
literal source independently (not trusting emitted I), checks characteristic,
F nonzero/univariate, four cofactors and identity. It must reject an actual
cofactor or F mutation. Candidate exports arithmetic as text exact rationals,
never JS numbers. Failures should end visibly even if Singular exit status
alone is not reliable; do not invent engine semantics.
Meaningful controls include omission I4 atV=W0 producing artificial Xline
and a deliberate changedF rejection. These are prospective tests, not run
or PASS. Root may use exact source text/ring identity readback for FIRST gate.
Avoid default huge GB stdout; compact bounded stage markers and certificate
files only. No claimed root classification unless an exact static argument
is actually provided, and no source/JC2 closure. Optional root filtering code
only if it does not dilute primary certificate deliverable; otherwise defer.

Own ONLY new xmodel/f10-contact-eliminant-code-astra-20260910.md plus its
transaction manifest, leased private partial, and
box/f10-contact-eliminant-code-astra-20260910/ (producer/checker/control source,
pins, terminal custody). Verify all targets absent before first write.
Use apply_patch for authored edits. Existing artifact_finalize publication
begin/close/finalize/verify only; no local compile/AST/syntax/math import/test/
CAS/enumeration whatsoever. No AWS/SSH/process inspection, Git, agents,
corpus/provenance/shared/protected work or other-lane reads. No singleclient
308305a9 premise. Current owned report only COLLISIONS extraction.
Finish report WHOLE reread; verify exact current inputs/owned hashes/transaction;
write terminal custody FIRST-to-ROOT with real first/deadline/idle clocks.
All writers IDLE before notifying ROOT. No promotion/execution/follow-on.

Expected final report: code algorithm/certificate contract, primary API
verification, static proof of independent checking, exact differences between
prospective controls and executed evidence (NONE), remaining trust/cap/engine
gates. STOP after terminal packet. Current genuine source computation is ROOT
owned and independent; do not inspect or alter it.
