# d125 hybrid81 engineering helper — independent hostile gate (Sol56 v2)

2026-09-07. Status: **UNSEALED — exact CAPRUN-parent-argv enforcement
REFUTED; fixed helper and tiny verifier otherwise CONFIRMED; all live Singular
properties remain GAP.** This report creates no engineering GREEN, authority,
deployment permission, production phase, or mathematical exit-price claim.

## 1. Scope, custody, and disposition

I read all eleven frozen charged files in `/tmp/jc2-lane.wWImXu/inputs` in full,
including all 73 lines of `engine.py`, all 55 lines of `test_engine.py`, the
registration and erratum, and the complete FALLACY-v2 text appended to the task.
I used no AWS, SSH, network, CAS, live peer, protected source, production source,
actual Singular, solver, authority, deployment, or production host/path. No
charged byte was changed. The earlier failed launch is charged only as a
pre-model/pre-receipt custody event with no mathematical result.

The root-stated 43 pins, 33/33 normal and 33/33 optimized caller controls, and
6/6 normal and 6/6 optimized helper methods in 12.611 seconds are premises, not
substitutes for this gate. The independently terminal caller-delta report remains
charged at SHA-256
`46a6304811af423042f29d9a8f4c3022189d648286e7770d02ae59d478615fac`.
I consume it only for the two repaired generic properties it supports:
post-source validation before receipt publication/readback decisions, and a
fresh default/unblocked `ITIMER_REAL` across a same-PID generic Python exec. It
does not prove pinned-Singular behavior.

| Reviewed surface | Verdict | Boundary |
|---|---|---|
| Literal `hybrid`/`alarm` selection, fixed paths, engineering binding, D/E/H pins, exclusive helper files, and no production read/new lifecycle | **CONFIRMED** | Static code plus focused local controls only |
| Claim that the helper requires the exact CAPRUN parent argv | **REFUTED** | It requires only one pathname token in the parent argv |
| Pinned tiny generator and exact replay verifier | **CONFIRMED** | Synthetic stdout only; no engine ordering claim |
| Supplied `test_engine.py` replay from this flat frozen corpus | **GAP** | It imports/reads uncharged sibling test artifacts |
| Actual four-variable Singular syntax, seven-slot I/T preservation, `I_SIZE 4`, and direct lift | **GAP** | No actual engine was run |
| Actual inherited timer, ready marker, and SIGALRM 14 termination | **GAP** | Generic Python exec evidence cannot establish it |
| Future-engine acceptance plan | **GAP pending closure** | Bounds/failure rules are sound, but exact parent argv must be compared explicitly and both live controls must pass |

Because an explicit dispatch claim is refuted, this helper is not engineering
GREEN as written. Root must either repair and re-review the helper, or declare
full parent-argv comparison a mandatory controller-owned acceptance predicate
before authorizing a future launch, compare the generated vector before spawn,
and then verify the live vector promptly after spawn. Merely retaining a command
is not comparison, and external checking does not make the unchanged helper
enforce full argv.

## 2. Concrete counter-control: parent pathname is not exact argv

The producer report says the helper “requires its exact CAPRUN-parent argv”
(`d125-hybrid81-engine-prep-astra-20260907.md:25`). The implementation instead
splits `/proc/$PPID/cmdline` on NUL and checks only membership of
`/home/ubuntu/d125-hybrid81-exact-solver-20260907/run_capped.py`
(`engine.py:51-52`). `D.context` hashes the on-disk runner and binary
(`driver.py:89-95`), but neither function binds the running parent's interpreter,
flags, wall/CPU/RSS arguments, output paths, or child argv.

My isolated mock control supplied a parent vector containing the required runner
pathname but changed `--wall-seconds` to `999`, changed `--stdout-file`, and, for
predicate-only coverage, replaced the child command. With the preceding
operation, resolved-path, authority-binding, D/E/H-pin, PID=PGID, and
output-absence predicates held, the payload reached a sentinel at
`D.arm_deadline`; the same control without the pathname token rejected. Both
normal and optimized runs reproduced this result. The sentinel fired before
limits or any write, and no runner or engine was executed. The cooperative
mislaunch implication needs no child substitution: changed CAPRUN caps or output
arguments with the correct engine child leave the implemented pathname-membership
predicate unchanged.

This is a concrete predicate counterexample and a potential acceptance/mislaunch
path absent controller comparison, not an end-to-end acceptance result, a claim
of same-UID adversarial isolation, or a forged root credential. A cooperative
mislaunch with a valid authority can satisfy the helper's parent test without
using the registered full vector. `REGISTRATION.md:34-35` prescribes a literal
command and lines 40-42 assign prompt command capture to the controller, but do
not explicitly require element-for-element comparison. Before spawn, root must
compare the generated vector to the literal template. Before acceptance, root
must also compare the live NUL-separated `/proc/$PPID/cmdline` of the exact
parent—including the complete child tail after `--`—to the operation-instantiated
parent portion after the outer `prlimit` prefix, while the process identity is
live, and bind both comparisons/results into retained custody.

All narrower dispatch properties hold:

- The operation is restricted to exactly `hybrid` or `alarm` before any payload
  write, followed by the exact resolved helper/cwd and per-operation authority
  path checks (`engine.py:43-47`). The CLI also requires exactly two arguments
  (`engine.py:71-73`).
- `D.context(authority_path,'control')` enforces the generic authority schema,
  root GREEN flag, engineering mode, exact D pins/caps, EC2 instance/cwd/boot,
  and fresh absolute interval (`driver.py:53-95`). The subsequent exact-dict
  comparison binds operation, actual helper SHA, fixed fixture SHA, and the
  actual physical `engineering/ROOT-GREEN.md` SHA (`engine.py:15-20,47-49`).
- The helper rehashes exact driver, checker, and adapter objects and requires
  payload PID=PGID (`engine.py:11-13,49-50`). The latter is a real future
  observation, not established by the local mock.
- Only the operation-specific `.identity.json` and `.sing` are helper-owned;
  both are prechecked with `lexists` and created with the unchanged exclusive
  `xb` writer (`engine.py:53-67`; `driver.py:41-43`). Streams, telemetry,
  launch/receipt files, authorities, and their absence/custody remain root
  controller duties (`REGISTRATION.md:37-42`).
- The helper has no fork, subprocess, session, runner framework, or production
  source read. It performs one same-PID absolute `/usr/bin/Singular` exec
  (`engine.py:66-69`). `source_access:false` means no production source; the
  declared pinned tiny fixture read at line 66 is intentional.

## 3. Deadline and resource flow

The helper calls the unchanged deadline primitive exactly once, after context,
engineering/pin/process/path checks and before limits, identity writing, fixture
read/parsing, script writing, and exec (`engine.py:47-69`). Thus the one timer
covers every helper write, tiny parse, and actual exec. For `alarm` the parameter
is exactly `min(duration,1.0)`; no `D.CAPS` value changes (`engine.py:57-58`).

The repaired primitive installs `SIG_DFL`, unblocks `SIGALRM`, recomputes the
relative interval against the authority's absolute end, rejects expiry, and has
the sole `setitimer(ITIMER_REAL, remaining)` call with no reset
(`driver.py:142-149`). `D.limits('control')` remains 512 MiB AS, 64 MiB per-file
FSIZE, and core zero (`driver.py:135-139`). Context and the checks preceding the
arm are not covered by the inherited timer. Recomputing against the absolute end
prevents crossing `deadline_utc`, but if that end is more than the returned phase
duration away, pre-arm work can still be followed by a freshly armed full phase
duration. Only the unchanged external CAPRUN bounds whole-child elapsed time.
None of this proves that Singular preserves the timer.

The proposed external controls remain at most 10 seconds wall, 10 seconds CPU,
512 MiB group sampled RSS, 64 MiB runner/payload per-file FSIZE, and core zero
(`REGISTRATION.md:26-35`). The external timeout is failure containment only. It
cannot turn a reset/ignored alarm into PASS.

## 4. Tiny generator and replay verifier

**CONFIRMED locally without an engine.** The 1,698-byte JSONL hashes to the fixed
fixture pin and parses through exactly
`H.read_hybrid(data,production=False)+D.footer(...,control=True)`
(`engine.py:22-29`). Independent derivation produced:

- four variables in exact order `(x,y,k,z)` and seven rows
  `(0,x/2,0,x/2,1-x,0,k*z-1)`;
- the exact ordinary serialization
  `0,(1/2)*x,0,(1/2)*x,1+-1*x,0,-1+1*k*z` with all seven labels;
- a 1,072-byte ASCII script, SHA-256
  `59295067207d4f17eb2acdbd4eac9d4755d16c94bfc36884e2f14d2731b9ad11`,
  ring id
  `2e0d0a769afbc8cfd8392d535b68209270db35b5e003698082ebdb1ead3c4558`;
- exactly one `ideal G=ideal(1)` and one direct
  `matrix T=lift(I,ideal(1))`, with no `slimgb`, `std(`, `dim(`, system,
  write, or library command.

The alarm literal is exactly 102 ASCII bytes, SHA-256
`446daa14cdedeb55ec9d0753ff48582b9ad3945bf889d471f56059911ea1f4b7`.
This confirms `ERRATUM.md:3-7`; the sealed prep report's 107-byte prose is not
silently edited. The literal contains the ready marker and infinite toggle but
was not executed.

`check_hybrid` first binds the fixture and 64 KiB stdout ceiling, invokes the
unchanged strict parser, requires seven engine and seven cofactor entries and
`G=[1]`, runs the exact rational unit certificate, and finally requires map
`[0,1,0,1,4,0,6]` (`engine.py:34-41`). The parser requires byte-empty stderr,
the exact source/ring header, sequential indexed I/G/T blocks, no trailing
stream, and `I_SIZE` equal to the parsed nonzero count (`exact.py:205-236`). The
certificate maps only original rows and recomposes exactly one across every
cofactor slot (`exact.py:173-198`). On the fixed fixture this is the exact
identity `x/2+x/2+(1-x)=1` and forces diagnostic `I_SIZE 4`.

An independently constructed complete stream returned
`TINY_HYBRID_ENGINE_REPLAY_PASS` with seven I/T entries and the required map.
Real changed-byte/object negatives rejected: corrupted `T 5`, `I_SIZE` drift,
whitespace stderr, trailing stdout, omitted guard slot, omitted interior slot,
slot reordering, `G=0`, and wrong fixture bytes. Binding negatives also covered
wrong mode/operation/helper/physical-GREEN value and an extra engineering key.
These controls establish checker behavior only. They do not assert that Singular
will accept the explicit syntax, retain all zero/duplicate positions in this
order, report size four, or return the expected direct-lift orientation.

## 5. Review of the supplied 55-line test

The six methods exercise appropriate narrow properties: exact binding, derived
control footer, synthetic full-slot replay and corruptions, alarm text, early
entry rejection, and static lifecycle/deadline checks (`test_engine.py:17-54`).
No enforcement uses a Python `assert`, and optimized mode does not remove a
security predicate.

The supplied test is not independently executable from the frozen flat corpus.
It imports `result` from an uncharged sibling `test_exact.py`
(`test_engine.py:12-13`), locates the JSONL through an uncharged sibling layout
(line 15), and reads the deliberately unsupplied older `.sing` (line 30).
Direct bounded invocations of the charged file in both normal and optimized mode
stopped before tests with `ModuleNotFoundError: test_exact`. The prep-cited
`test-results.json` and `custody.json` are also not charged here. This is an
independent-reproducibility GAP in the supplied test evidence, not a production
helper counterexample and not a rejection of root's stated 6/6 premise. My own
gate uses only the frozen JSONL/modules and a self-built JC2CERT1 stream.

## 6. Independent controls and immutable custody

Own controls are in
`box/d125-hybrid81-engine-gate-sol56-v2-20260907/`. Each invocation used
`timeout 30s`, `prlimit --cpu=25:25 --as=536870912:536870912
--fsize=67108864:67108864 --core=0:0`, and `/usr/bin/python3 -I -B` before
imports; optimized mode added `-O` after `-B`.

| Mode | Result | External wall | User + system CPU | Peak RSS |
|---|---:|---:|---:|---:|
| normal | 10/10 PASS | 0.10 s | 0.09 + 0.00 s | 26,668 KiB |
| optimized | 10/10 PASS | 0.42 s | 0.41 + 0.01 s | 30,456 KiB |

`gate_checks.py` is 223 lines/10,852 bytes, SHA-256
`2db05fe18541a749190e0536f6b4caa06ad8d1571a01a5120b63d0de6a49a90b`.
`PINS.json` SHA-256 is
`7d3b96d016be81b45ef34a78e992e29b094cd37fad285c270dd3b2b2aa49c150`;
`RUNS.json` SHA-256 is
`286ccb1c9cd8f802d3980d45f6c5b15e934cd2338e0634847406c28add691c2c`.
All frozen pins matched before/after, and all local children/writers are terminal.

| Frozen input | Bytes | SHA-256 |
|---|---:|---|
| `engine.py` | 4,850 | `a87debfce5ca01817c03c2b180fd514d8e45269b1173c6f4a562255883188ae5` |
| `test_engine.py` | 4,174 | `6bbd6499957e05ad02d180b9e2d61d80bbfe2666a68582f857f7d8569781818f` |
| `REGISTRATION.md` | 4,680 | `364a5d8c57a967d6dc3467db13eb0b19ba6c7924a573dc5b3ea67da7eb6f1ee5` |
| `ERRATUM.md` | 444 | `bd312311284fc9617533f349f90306940b4bd105272434ef3627b00b4822a3c1` |
| `driver.py` | 13,466 | `ce599a26a0ed051eddd71125f492bb0b19c4ef86cdf97a6bc887f5cf47cade5a` |
| `exact.py` | 11,088 | `7ca2b24ee5d04ff6d60fd2212a05586c8bba24327a51c7718351434c7eda60e9` |
| `hybrid.py` | 5,419 | `c9755a7172eaa7f1393860931d55c38a0b9d79b5b170eadbf14baff0203c35a3` |
| `tiny-engine-fixture.jsonl` | 1,698 | `d00df83bafddcd3436dad44034bdb1680d306f6d67f8bfa83d4b32de19961bbf` |
| prep report | 6,991 | `481ba6521e86e7a5786e71b9f8e1ac266c483d56bfe9eba164bd59f468812fbf` |
| caller gate | 10,032 | `46a6304811af423042f29d9a8f4c3022189d648286e7770d02ae59d478615fac` |
| `HARVEST.md` | 2,541 | `84746860d736e7bf5fcfd1ad565a68be1aa49b3e85d1059065c95a08a0d898e4` |

The expected but unsupplied dependency pins remain CAPRUN
`4435279df8f987c667ebc2fae8e4bd987916032561cb3faf1e07a57789d196c2`
and Singular
`90ab699b7a28486944a167e797d7c8dd38f8f949960b93ee7a6d08ff1c7c46f4`.
They are scoped reviewed dependencies, not newly inspected objects. The
62-versus-64-hex RUNS transcription issue disclosed at `HARVEST.md:16-23` is a
prose custody typo, not a code delta or counterexample.

## 7. Exact remaining live acceptance obligations

No engine control is accepted now. A later root-authorized attempt must satisfy
all of the following without retry, enlargement, or implicit solver continuation:

1. On retained instance `i-0da0cebfc97c9fd54` and EBS
   `vol-0eb6450d18ffa89f1` only, establish fresh Owner/DMI/hostname, boot, cwd,
   resource, process, and output-absence custody; pin the physical helper,
   fixture, D/E/H, CAPRUN, Singular, and `ROOT-GREEN.md`; create the two distinct
   fresh operation-bound authorities exclusively. Reserve separate
   `engineering/hybrid.*` and `engineering/alarm.*` output families. Full
   production source and construction files remain unopened and untouched.
2. For each control, precheck all controller/helper paths with `lexists`, compare
   the generated launch vector to the literal operation-instantiated template in
   `REGISTRATION.md:35`, and only then spawn it. Promptly capture the live parent
   and child PID/PGID/start ticks/boot/namespace/cgroup, require helper PID=PGID,
   and compare the full live parent argv after the outer `prlimit` prefix,
   including its complete child tail, element-for-element with the prescribed
   vector. A runner-path-token match is insufficient.
3. Run exactly one `hybrid`. Require CAPRUN normal termination and exact child
   return code zero, literally empty stderr, the retained 1,072-byte generated
   control script, and complete stdout within 64 KiB. Replay the same retained
   stdout/stderr bytes through `engine.check_hybrid` normally and with `-O`.
   Acceptance requires all seven indexed I/T positions, `G=[1]`, `I_SIZE 4`, map
   `[0,1,0,1,4,0,6]`, and the exact full-slot rational identity.
4. For each operation, compare its retained `hybrid.identity.json` or
   `alarm.identity.json` operation, PID/PGID/start ticks, complete observed-host
   object, namespace/cgroup, authority/helper hashes, timestamp,
   `source_access:false`, and timer observation with the live root captures and
   CAPRUN telemetry. Separately require the authority, live limits, launch vector,
   and telemetry to agree on all registered caps. Only after terminal hybrid
   acceptance, run exactly one `alarm`. Its identity must record a positive
   one-shot
   `ITIMER_REAL` delay no greater than one second and zero interval. The exact
   child must reach
   `JC2_HYBRID81_ALARM_READY` and then telemetry/raw wait status must establish
   termination by actual `SIGALRM` 14 at about that inherited deadline, before
   the external 10-second fail-safe. External timeout, early parse/start failure,
   missing marker, return code zero, or any other signal is failure, never PASS.
5. Harvest telemetry first, retain every byte even on failure, recheck all
   helper/fixture/code/binary pins, establish fresh all-group terminal absence,
   report the precise result, and STOP the instance under root ownership. No
   result authorizes production parsing, the 81-variable client, retry, solver,
   or any cap change.

Until those observations exist, actual Singular startup/alarm preservation,
zero/duplicate column ordering, seven-slot retention, diagnostic size, explicit
syntax, and direct-lift indexing remain typed GAPs. The later proposed single
300-second decision, conditional 120-second verify, 450-second aggregate,
16 GiB, 64 MiB stream, and 2 MiB input workflow is unchanged and **not
authorized** by this review.

All owned local work is terminal. **STOP/IDLE.**

<!-- BODY-END -->
