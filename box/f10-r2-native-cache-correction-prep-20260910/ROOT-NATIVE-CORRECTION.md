# ROOT minimal native registration correction — DISABLED prospective plan

2026-09-10 15:32 UTC. This changes one metadata collector input, not accepted semantic or mathematical source. No worker exists, no registration is enabled, no runtime permission is claimed from this card.

The completed two-refusal diagnostic (ROOT RESULT.md and terminal CUSTODY/map record charged separately) observed /etc/ld.so.cache at15:11:46 in the second probe child's mappings; that canonical path was absent from its frozen native files and aliases. Record is explicitly UNVALIDATED and non-atomic. It neither proves loaded-byte coherence nor recovers the unknown pathname in the earlier14:09 attempt. No mathematical child or semantic6/10 ran in this diagnostic.

ROOT current metadata-only check of native-manifest.json:
SHA5003b730e59c1c6e246a093c1531128ca869698c89bc061f0074eb45d8b8324c;
schema f10-native-closure/v1; files1225/directories91/aliases31;
files.has('/etc/ld.so.cache')=false, aliases.has('/etc/ld.so.cache')=false.
This is a scoped documentary field check, not a second full snapshot attestation.
The post-observation cachecopy SHAabdf8ebc85486a018ad2f8f61d3f31ae829f99f29291d434958b679b7fce388d is retained evidence ONLY. It is NEVER a pin for another worker.

Smallest next change: on ONE newly allocated worker, after exact EC2 identity and fresh boot/cap/deadline registration, run the same metadata-only native collector with /etc/ld.so.cache appended to its explicit root-path loop. Canonically resolve it, hash and stat it using the unchanged visit routine, register any genuine alias if needed; all other existing file/alias/directory/absentZIP/python_path checks remain. No recursive /etc inventory, no inferred wildcard, guessedlibrary or ignoredmapping, no oldnativehash reuse. The collector's identity literal and every output physical/path pin are freshly worker-bound.

The resulting fresh SIX-FIELD manifest adds the observed required file to files (and a real alias only if resolution requires). The accepted unchanged semantic dispatcher6c232bb0 checks every current nativefilehash and every mappedpath membership, aliases, exact directories and absent paths; it need not require a native file's parent to be recursively inventoried. ROOT/OS are trusted to keep native files and package state unchanged through this bounded batch, with pre/post fullhashes and all childmap checks. If cache is absent, drifts, remains unlisted, or any OTHER map fails: STOP and preserve evidence, no exception list or unchanged retry.

Actual future payload remains exactly the already FIRST-reviewed semantic batch: original five reconstruction science files, probe1e3bea3a, mutatora896a809, dispatcher6c232bb0 and CAPRUN4435279d unchanged. Nine runtime preflights (including exact live dummy process-group/RSS/TERM/KILL check), then one mutator generating two frozen fixtures, unchanged check6 andcheck10. Positivebaseline dac655a7/1652675B is copied/hash-bound without local mathematical body read, no positiveproducer/checker rerun. Joint600wall/500CPU/2GiB/16MiB, existing childprofiles and sampled480CPUstop/admissionreserves unchanged. New ROOT registration must separately bind exact expanded12argv, sources, baseline, nativehash, physical/namespace/cgroup/uidgid, environment, paths/ancestry, deadlines and source import rules. Old terminal registration is documentary only.

Arm original absolute USER workertermination before worker use and SYSTEM TERM/KILL before dispatch, capture actualMain/CAPRUN/probe identities, never reset clocks, no constructor-failure outcome invented. Full-group terminal custody/WHOLE-first/all source-native-output rechecks/archivecompare/fsync/download then EARLY exact-ID termination with DeleteOnTermination=false. No idle compute or EBS deletion. The semantic dispatcher is not changed to include the observer; any missing error detail remains a STOP and ROOT custody obligation.

The independent gate requested now is only whether this observed evidence supports that minimal fresh native-registration correction with unchanged strict semantic instrumentation. It is NOT an actual6/10 result, promoted baseline, regularity, fullsource, solver or JC2 claim. No source hardening, extra observer retry or extra prime is requested.
