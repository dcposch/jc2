# Bounded structural discriminator, not a claim

Target: the four explicit normalized contact equations A_x=B_x=A_y=B_y=0
from accepted17w, with W nonzero and distinct prescribed exponents in
5/3<x<y<2. The complete uniform late-column consumer is accepted17v:
emptiness of both contacts implies unimodularity of the actual homogeneous
late column for k_j; all earlier forcing and companion equations STILL remain.
Neither this nor full contact emptiness alone resolves F10 or JC2.

Instead of another residue table, test whether the OFF-DIAGONAL algebraic
contact locus itself is zero-dimensional after excluding factors known
nonzero throughout the allowed exponent range. Put s=x+y, p=xy,
Gamma=36p+9s^2-114s+181, Omega=15s^2-36p-30s+35 and

 H=W*x*y*(x-1)*(y-1)*(3x-5)*(3y-5)*(2-x)*(2-y)
   *(y-x)*(s-3)*(4-s)*Gamma*Omega.

Every factor is nonzero on the prescribed rational range (Gamma/Omega
positivity is accepted17w). Consider Q[x,y,V,W,H^-1]/(A_x,B_x,A_y,B_y),
or the literal Rabinowitsch presentation with a new variable zeta and
zeta*H-1. No real sign of V,W is assumed. A zero-dimensional result here
would reduce possible prescribed exponents to finitely many algebraic
pairs; it would NOT yet exclude their rational/integer specializations.
An empty result would exclude all prescribed contacts, still not the source.

Candidate geometric lens: the single-contact curve in (X,V,W) projects to
(V,W); off-diagonal exponent pairs are its same-coefficient collisions.
Birationality on every relevant component, with exceptional loci accounted
for, could imply finiteness of those collisions. This is only a possible
proof mechanism. One regular fibre, generic rank, a nonzero resultant,
dimension counting or generic injectivity on ONE component does not suffice.
Accepted17w's finite degree<=3 for each fixed(x,y) does NOT prove that the
total parameterized locus is zero-dimensional. Localization of W in a finite
algebra over a parameter ring also does not automatically preserve a finite
morphism to that parameter ring.

Strongest acceptable outcomes: a manual proof of zero-dimensionality (or
emptiness) with literal maps/identities and every localized divisor handled;
an exact positive-dimensional countercomponent preserving H!=0, typed
carefully against the real prescribed range; or GAP naming the precise
uncertified elimination/nonvanishing statement needed. Do not fabricate
an eliminant or assert a sign on uncomputed coefficients.

History checksum: root searched explicit canonical AUDIT/APPROACHES/REDUCTION
and historical xmodel report text excluding ALL September10 live files.
Known17w gives fixed-exponent cubic/two-quadratic algebra and no uniform gcd
verdict. The inspected17v/17w/17ze entries do not supply this global collision
dimension theorem. Older filename hits concern different receiver/source
scopes; no historical proof body was promoted from a keyword match. This is
a NEW bounded campaign task, not claimed literature novelty or a completed
all-corpus exclusion of synonyms. Any compute remains separately unauthorized.

ROOT correction to the accepted17w GATE: its incidental Cramer expression for
e_y has the wrong minus sign; e_x=12*L*E/Gamma and
e_y=12*(1+L)*E/Gamma, both positive. The producer's determinant/ideal argument
is correct and remains accepted. Its u normalization is a field-point fact
or an explicit u-unit chart, not inversion of a general nonunit.

Manual mathematics only: no code, emitted coefficient artifact, arithmetic
subprocess/AST/import/compile/test/CAS of ANY size, network/AWS/SSH/process,
agent/corpus/protected/shared work. Own apply_patch report/metadata and existing
transactional publication only. This investigation does not gate the two
independent live code tasks and cannot read either live package.
