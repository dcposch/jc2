# Dummy identity attachment — source-only

ROOT,2026-09-12. DISABLED/UNEXECUTED/UNREVIEWED. Concrete missing evidence:
the accepted runtime requires ROOT to bind actual descendant identity and
readiness, but the old probe prints only DESCENDANT_READY and CAPRUN retains
the leader identity/peak RSS, not each descendant PID. Add one bounded JSON
identity record to the existing probe's stdout, preserving the original
marker, fork/parent exit, ignored TERM,64MiB touched payload and infinite
sleep. Stdlib json/Path supply only metadata. No scientific input or result.

ROOT must bind the exact emitted child PID/start_ticks/boot/PID namespace,
PGID and cgroup to actual service and CAPRUN leader/PGID observations, require
the exact two-line output and JSON keyset/status/payload_bytes, validate RSS
and TERM/KILL cleanup telemetry, and establish final kernel cgroup emptiness.
The marker is emitted after every payload page is touched; a cap before the
marker or incomplete/absent/malformed output is STOP, not a valid dummy.
No assumption that -B forbids reading pyc. Native/source pins must bind this
actual new probe; the old probe digest no longer describes it.

No external observer process or framework is added. The unchanged dispatch
dummy phase does not itself parse this new metadata; ROOT's explicit terminal
intake must do so. Metadata alone is not kernel quiescence or proof of no
escape. All prior runtime/cgroup/clock/native/immutable-custody obligations
and physical caps remain. No interpreter, import, syntax, test, dummy, worker
or other execution has occurred. Different-model source DELTA FIRST precedes
use. This is not an old run retry or authorization of science.
