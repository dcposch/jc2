# Disabled normalized batch and finite validation

All new code UNEXECUTED. The accepted generator/checker/arithmetic/helper/probe
copies are byte-identical. No artifact, fixture, authority or receipt was
produced by running them. No syntax, installed compatibility, runtime cost,
speedup or memory estimate is claimed. This packet prepares a single future
engineering batch, not a solver or invitation.

Root alone may create dispatch.registration.json in a new exclusive remote
directory after reviewing this delta. The supplied differently named template
is disabled and has null worker, cwd, deadlines, aggregate and profile values.
Use the exact absolute vector in dispatch_batch.py: eleven local files plus
/usr/bin/python3. The generator, checker, algebra, helper, CAPRUN, probe,
original exact.json, admissibility and frontier hashes are fixed. Caller and
harness self-hashes and interpreter hash come from external root preflight.
The original exact artifact body is not a local preparation input.

The future profiles are probe, dummy, generate, check, controls. Root chooses
all wall/CPU/RSS values, fixed mathematical/task deadlines and aggregate;
the inherited dummy RSS test remains32MiB. Every operation has the same exact
CAPRUN argv and15-second admission margin against stored deadlines. The same
post-return cutoff check,16MiB inherited file cap, one-child discipline,
exclusive outputs, frozen authority/no concurrent writer contract and
namespace/PGID cleanup checks remain. CAPRUN sampling and cleanup can overshoot
nominal timings; no strict realtime guarantee or automatic deadline reset.

Required sequence:

1. Five actual authority refusals: disabled, wronghost, argv, cap, missing
   input pin. Each must exit ordinarily nonzero before its sentinel.
2. Genuine valid postauthorize sentinel.
3. Same-PGID descendant RSS cap with leader exit, ignored TERM, required KILL,
   exact four identity/signal events and no owned live group afterward.
4. One genuine generator.main to normalized.json.
5. One genuine checker.main read-back from original exact.json, covering all
   source rows/coefficient slots, complete target/bracket and actual inverse
   polynomiality, not just inventories. The caller binds status/source/artifact.
6. One capped semantic harness, with eight in-process negative calls listed
   below; it does not rerun a positive checker.verify.

That is seven preflight children and three mathematical children, not eight
production negative entrypoints. The eight negatives are distinctly recorded
as checker.verify IN-PROCESS. Fresh canonical fixtures are written exclusively,
hashed before reread/check and rehashed after rejection. No fixture authority
is claimed: the authorized harness owns these in-process controls. A failure,
unexpected exception/reason, unchanged fixture, cap or late return stops the
batch NONDECISION, with no retry/patch/increase.

| Named fixture | Intended exact rejection |
| --- | --- |
| completed-matrix-entry | ValueError: source-bound completed matrix |
| historic-third-forcing-index | ValueError: full affine forcing including third correction |
| full-Pi-sign | ValueError: full Pi |
| H0-projection | ValueError: H0 whole constant target |
| H1-projection | ValueError: H1 whole linear target |
| float-anywhere | ValueError: float forbidden |
| noncanonical-rational-string | ValueError: canonical rational |
| canonical-wrong-large-integer | ValueError: H0 whole constant target |

The historic index fixture implements the entire old term
(6-i)*(z-U*d0)*D_(i+1), not a random c3 perturbation. It propagates its change
through all three V coefficients to both c3 entries and keeps h3 untouched.
It stores exact delta wires as mutation evidence and requires an actual c3
change. Its rejection is an original-source affine-forcing check, not a late
bracket or pole negative. The report derives this propagation manually.

The precision controls retain coefficients as rational strings. A positive
integer beyond2^53 has a genuine binary64 rounding precondition; the wrong
canonical integer is rejected by exact H0 binding. This is NOT positive
large-payload nested-parser fidelity. The float case is an early global type
refusal and the plus-prefixed rational is a canonical-parser refusal.

Genuine production acceptance is essential and positively checks the complete
bracket and ten inverse poles. None of these changed-pair controls claims
negative late-path coverage. No O-mode matrix, unrelated helper test, optional
hardening, coefficient-factor choice, univariate adapter or solver is added.

Future measured output is the genuine checker's row terms/degrees/bit lengths,
artifact bytes, and the existing caller/CAPRUN timing/RSS/exit/identity records.
The batch receipt additionally binds the algebra hash because the immutable
normalized artifact itself does not embed it. All costs remain unmeasured.
