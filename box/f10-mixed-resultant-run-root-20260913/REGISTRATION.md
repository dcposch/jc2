# One all-parameter resultant observation — ROOT execution registration

September13,2026. Goal JC2 remains unresolved. This ONE bounded tranche may
identify the finite unknown integer exceptions of the actual mixed scalar;
it removes at most one prerequisite of critical-coefficient comparison, NOT
full-array comparison, REG, source zero, all-F10 or JC2. No next-r/prime farm.

Source observer661b739ed3f86267e9875a83537bd1a75ef5699442c99eb182ec2800f3005858,
producer0efea16984298b756e3e33ca5c442dcd023d2e34052a995b656190dd66a3c92a,
different-model FIRST2bc798d366cfbaf39161f6523a416e85ae744a0f25b33847186a0eda7aef29c0.
Both authors were independently terminal before ROOT custody-first intake,
whole reads, postpins and expected transaction checks. Verdict CONDITIONAL
STATIC CONFIRMED for ONE OBSERVATION, not runtime or mathematical acceptance.
Unchanged tested runner9c2255c7dcb7c91a8abeb2bd0ea7db30b4d4e8182abdbbd08cc1dc76f33d094d.

## One worker and immutable bounds

One r7i.xlarge4vCPU/32GiB, personal/us-east-1, ami-066263bf15bd856de,
subnet-948915c9, sg-09ffa8932558f0a79, key jc2-fleet. No IAM profile, public
message, package installation, security-group mutation, old-client migration,
protected/baked campaign tree access, source retry or second worker. Use only
installed system tools/libraries and the explicitly transferred new files.
Root EBS100GiB gp3 MUST have DeleteOnTermination=false, API termination enabled.
Fresh07:51 both nonterminal campaign selectors[];07:45 quota1920vCPU and image
available/x86_64, worker type4vCPU/32GiB. Refresh before actual allocation.

ORIGINAL clocks: allocate no earlier than07:55UTC; latest service admission
08:06UTC; exact-ID independent retirement08:24UTC. No extension. ROOT arms
the HQ user-manager retirement timer within60seconds of allocation, verifies
its exact instance/deadline, and retires earlier after terminal collection.
If setup/preflight misses admission, retain evidence and terminate, no retry.

Worker-only preflight: ONE independently bounded service60seconds active,
TimeoutStop5, KillMode=control-group, Restart=no, User65534/Group65534,
MemoryMax2GiB, MemorySwapMax0, CPUQuota200%, TasksMax32, LimitFSIZE8MiB,
NoNewPrivileges, ProtectSystem=strict, ProtectHome, PrivateTmp and PrivateNetwork.
Its only writable directory is /var/lib/jc2-mixres-preflight, owned65534,
groupubuntu, mode2750. Script is immutable root0444 under canonical root0755
/opt/jc2-mixres20260913a, argv /usr/bin/python3 -I -B -u preflight.py INSTANCE TAG.
It checks physical identity/credentials/cgroup before scientific import; runs
three causal guard refusals and source syntax on the worker; records installed
Python/Sympy/backend/package/loaded-module/mapped-library identities. No claimed
hostile-code sandbox, complete native closure or scientific result validation.

ONE unchanged JC2-JOB/v1 service, tag mixres20260913a, two phases smoke/observe,
joint wall840seconds, memory16384MiB, swap0, CPU200%, Tasks32, per-file8MiB,
plus existing5+5second cleanup/receipt grace. Preflight+science active<=900s.
Each phase runs /usr/bin/python3 -I -u @payload/observer.py with exact INSTANCE,
TAG and mode. Full new job.json is separately bound/pinned after allocation;
the disabled source template is never executed or overwritten. Observer,
runner, manifest and preflight root0444, ancestors root/nonwritable. Require
>=512MiB free EBS. Single observation, no cofactor computation/second engine.

## ROOT operator and outcome path

1. Allocate exact registered worker, record its volume and private address;
   arm independent retirement immediately. Verify console ED25519 host-key
   fingerprint against ssh-keyscan before strict-host-key SSH/SCP. No new
   broad fleet action or permissive host-key acceptance.
2. Verify actual DMI/boot/OS/cloud-init; no package install if unavailable.
   Copy only runner, observer, preflight and actual manifest through a fresh
   staging directory. Install immutable canonical paths and verify physical
   root/nonwritable ancestry, exact hashes and free disk before use.
3. Start preflight once through its detached bounded service, with durable
   stdout/stderr. Require actual success/PREFLIGHT_PASS, all causal controls,
   retained version/hash DATA and cgroup absence. Review whole diagnostics;
   absence/failure is STOP, not an automatic repair or re-run.
4. Before08:06 call the unchanged runner start once with exact instance/tag,
   canonical bundle and actual runner/manifest SHA. Verify actual service or
   authoritative terminal state within60seconds. Preserve manager/kernel
   bounds and phase evidence; do not infer live work from intent/old logs.
5. At terminal service AND absent cgroup, collect using unchanged runner.
   Missing factor/observation DATA is INCOMPLETE, not no exceptions. Complete
   R/Q/factors are OBSERVATION/UNREVIEWED; factor product does not prove
   irreducibility or the resultant. Only after useful DATA choose a cheapest
   independent mathematical check. No claim follows from success exit alone.
6. Retain preflight streams/dependency DATA, installed sources, actual
   manifest, manager/journal snapshots, job raw archive and partials. Copy
   to HQ, compare SHA and a second WHOLE-byte stream, flush copies and parents,
   then terminate the exact worker and verify EBS retained. Collection failure
   never silently extends08:24 or deletes evidence; retained EBS permits recovery.

This is ROOT's finite orchestration of the reviewed deployment contract,
not a new controller, scheduler or qualification framework. Source is unchanged.
