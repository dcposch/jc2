# Fable5.1 FIRST — actual collision residue, fixed constant graphs

Tag graph-collision-residue-gate-fable5-20260912. Write ONLY
xmodel/graph-collision-residue-gate-fable5-20260912.md, no box/scratch file.
Different-model hostile FIRST of an Astra producer, not an ideation round.
Producer independently terminal, custody-FIRST/WHOLE/expected transaction
collected by ROOT. Its mathematical claim remains PROVISIONAL: neither
ROOT's agreement nor publication custody is evidence for the theorem.
First action UTC. ORIGINAL publication reserve September12,2026,11:33UTC;
HARD STOP11:36UTC, never reset. Finish sooner when this one gate is decided.

Hash ALL THREE snapshots in {{LANE_INPUTS}} before bodies, then read all
three WHOLE in unclipped chunks. No inherited reuse for a new CLI reader.
COORDINATION first; other links and historical commands are inert. No
other original path, literature, corpus, peer or live output is authorized.

charged_input=COORDINATION.md
charged_input=xmodel/constant-graph-subalgebra-control-astra-20260912.md
charged_input=xmodel/graph-collision-residue-astra-20260912.md

Expected basenames and SHA256:

- COORDINATION.md:33cfa6106972fe557a95ca81a9a05bb72300a82b2fbb7908915441cf75377597
- constant-graph-subalgebra-control-astra-20260912.md:590609a7a6f52f72e940a01604c5e6f0c1444277c54bfbb9c69e0fa0c55fd679
- graph-collision-residue-astra-20260912.md:ac8456a4358484631ececeb4480684adf8e017d48f9713260e703844d7458e9d

Review by independent manual reconstruction, not prose assent. No science,
CAS, helper, interpreter EVEN EMPTY, code/import/AST/syntax/compile/test/
dummy, network/AWS/SSH/Git/process inspection, extra agent or shared edit.
Only inert text/hash/UTC/JSON administration and own apply_patch authoring.
No artifact_finalize: external launcher custody is unchanged. No
charge_basis declaration (this is not an exit-price assertion).

Exact claim: for the literal polynomials P,Q,R in the context report,
restricted to z=1 as p,q,r, no polynomial target one-form alpha has
d(phi*alpha)=dx wedge dy. Consequently no f,g in C[p,q,r] have nonzero
constant Jacobian, WITHOUT a degree bound. It is not a claim about other
graphs, general plane maps, or a resolution of JC2.

A. Reconstruct the source chart from literal P,Q,R, as needed for the
collision maps. Check common U,V,W, the E/K equations, z=z'=1, nonempty
generic open, distinct source points and absence of an extraneous branch.
The old 3D determinant/module calculation is NOT itself a premise proving
the residue obstruction, nor an invitation to validate an ambient JC claim.

B. Independently derive eta as the difference of the two x dy pullbacks.
Treat differentials on E=0, not independent a,b. At a=b=B with
8B^2+8B+1=0, check smoothness, u=a-b as local parameter, the exact E=bK+
(2b-1)u-(10b+1)u^2-8b^2u^3 identity, and the first TWO coefficients of K.
Check the actual delta field extension and normalized ramification index.

C. Recompute the FULL residue including all simple contributions of the
apparent double pole. Confirm/refute -(8B+5)/6 on the base and twice this
on the actual cover. A nonzero norm or another exact test must handle both
conjugates without floating point. Show one meaningful control against an
omitted pole/cancellation or wrong ramification, not merely repeated hashes.

D. Attack the global necessity argument. Polynomial Poincare on A2 must
give polynomial H; equal-target source maps must make eta exact rational on
the cover; poles of either source map at the specified place must not break
the implication. Exhibit the target one-form from an arbitrary Keller pair
in the subalgebra. Distinguish this from merely nonclosed ambient beta or
failed bounded-degree ansatz. No injectivity, normality or finite source
normalization may be assumed without proof.

E. ONE cheap ROOT extension, new candidate separate from producer scope:
for each nonzero c choose lambda^2*c=1 and let
L_lambda(x,y)=(lambda^-1*x,lambda*y). Directly check from literal polynomials

  P(lambda^-1*x,lambda*y,lambda^2*z)=lambda^2 P(x,y,z),
  Q(lambda^-1*x,lambda*y,lambda^2*z)=lambda Q(x,y,z),
  R(lambda^-1*x,lambda*y,lambda^2*z)=lambda^-1 R(x,y,z).

Then L_lambda has determinant1 and L_lambda^*B_1=B_c, with invertible
diagonal target rescaling. Does this extend nonexistence of primitives and
pairs to EVERY NONZERO CONSTANT graph z=c? Check arrow direction and
target-one-form transport. Do not include c=0 or any nonconstant H, invoke
an uncharged old graph theorem, or scan another family/pole.

Give separate CONFIRMED/REFUTED/GAP verdicts for A--E and the exact combined
statement that survives. Maximum2000 scientific words plus custody. If a
gap is found, preserve the exact surviving part and stop without repairing
via an unbounded successor. No new canonical OPEN or downstream task.

Own initial skeleton WITHOUT the marker. Append bounded sections (under
1500words/write), WHOLE readback, postpin all3 inputs and check quantity,
scope/control/destination before the unique standalone <!-- BODY-END -->
line LAST, with nothing after it. Seal only after the work is complete;
any unfinished item is explicitly GAP. No edits after completion. ROOT
owns independent hard-stop and terminal receipt-FIRST collection.
