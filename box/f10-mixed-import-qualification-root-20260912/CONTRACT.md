# One import/API metadata observation

ROOT,2026-09-12 23:03 UTC. SOURCE-ONLY / UNREVIEWED / DISABLED.
The preceding qualification lacked actual private-package import and API
observations. This small script closes only that data-channel omission.
No authority/produce/check module is imported, and no symbol, polynomial,
gcd, factorization, calibration or source computation is performed.
Importing SymPy executes library startup, so execution is AWS-only and
requires its own accepted static FIRST and fresh qualification registration.

Run only the actual instance-bound copy at
/opt/jc2-mixed-20260912/meta/inspect_imports.py with
/usr/bin/python3.12 -E -s -S -B, under a root whole-cgroup service:
CPU80percent/100ms, memory/AS1GiB, swap0, Tasks64, FSIZE4MiB,
Runtime25seconds/TimeoutStop5seconds, no restart and KillModecontrol-group.
All output goes to the existing bounded preparation tmpfs. Absolute original
qualification deadline dominates; a failure is STOP, not a source edit/retry.
Its source and actual derived copy are separately pinned outside native roots.

The manifest comes from the separately reviewed bounded assembler. The
script verifies its own metadata-directory startup, isolated flags, private
package origins, Python arithmetic backend, presence/callability of the APIs
literally used by the two charged science programs, and byte-pins of currently
loaded file-backed modules. It does NOT test those APIs' algebraic behavior,
prove future dynamic-import closure, authenticate the original collection,
or replace the whole-package/pyc/copy/ancestor native inspection. Ordinary
science/runtime script directory prefixes are different from this metadata
script's prefix and must be attached separately, not asserted observed here.

Only JC2_INSTANCE_PLACEHOLDER may be replaced after FIRST by the freshly
authenticated worker ID. No scientific source, runtime, cap, library-path
policy or existing collector/inspector/assembler changes. No new framework.
