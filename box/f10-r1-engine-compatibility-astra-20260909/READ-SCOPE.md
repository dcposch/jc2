# Exact compatibility correction

First action 2026-09-09 14:41:59 UTC. Controlling stop 14:49:59 UTC (eight minutes, earlier than 14:51). Both output paths were absent before begin.

Three allowed inputs were hashed before WHOLE reads: frozen engine.py, terminal Fable static gate, and root's installed-engine api-metadata.stdout. No provenance paths or other report bodies were followed. The metadata says 2026-09-09T14:35:08Z and records both option names as unknown. Root identifies the installed engine as Singular 4.3.2; that version identification is a root-supplied fact, not independently established by the small stdout file alone.

The new engine.py was created through apply_patch. Its header and every remaining byte equal the old file with exactly old line 40 removed. Literal git diff and cmp against sed '40d' are documentary checks, not execution of the source. No compile, import, predicate, arithmetic, engine, AWS/SSH, worker or mathematical subprocess ran. No old file, other source, flag, receipt, integer-limit handling or authority was edited. The new file was read WHOLE statically after creation.

The gate's conditional deletion is used at its precise scope, not as a runtime or API-wide pass. Future registered validation and any decision remain root-owned. All unrelated gate qualifications remain untouched.
