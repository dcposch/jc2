# Final analytic audit of the proposed small-matrix decision

The source transport, nodal diagonal, exact transverse recurrence, and supported H quotient are valid reductions. They do not presently decide the full localized necessary ideal. No further small exact obstruction was found, and no assignment satisfying every full-chart row was extracted.

The ODE matrix has17/22 unknown q coefficients. It proves q=C*q_hat and lambda=lead*C with lead nonzero rational. After localizing separation*lambda, its own coefficient quotient is a nonzero Laurent polynomial base in C and separation (with the remaining source parameters retained). This proves consistency of that face system for every nonzero C. It is not a certificate for the complete necessary chart.

The exact missing implication is a lift or elimination theorem from this proper face base to the complete coefficient quotient. The complete quotient is an algebra over the face base, and further polynomial coefficient, source, characteristic and termination equations can make that algebra the zero ring. Neither injectivity of the base map, a section supplying a full solution, nor faithful flatness has been proved. The proved two-way coefficient maps identify presentations of the full ideal; they do not determine whether the common quotient is zero. The new3/1/1 H coefficients have the same limitation: they describe one small factor of the construction while R, its remainder, and the other transported equations remain coupled.

The first transverse order exposes no extra scalar contradiction. Put r0=q+B³/4, write the retained target shift as alpha so G0=u²+B−alpha/3, and let U(u) be the free polynomial at the first recurrence step. Then

 G1=−2j/(3B)+2u*U(u),
 R1=−j*(u²+3B/2).

The supported quotient has H0=h0,H1=h1, both scalar in u. Consequently its exact remainder lambda*G=H*R+S has

 S0=lambda*(u²+B−alpha/3)−h0*r0,
 S1=lambda*G1+h0*j*(u²+3B/2)−h1*r0.

These are compatible first-strip identities with nonzero j and lambda. In particular lambda*u² in S0 is permitted by the established total degree and both relevant valuation bounds. The explicit H/reverse-remainder control in h_quotient_preflight.json likewise retains nonzero C, but it intentionally does not claim to solve all full-chart equations.

The order-one identity e=alpha_client*lambda with alpha_client a nonzero rational transports the radical question: separation*e is nilpotent if and only if separation*lambda is nilpotent. It does not prove either side. Polynomial termination and the uneliminated complete equations cannot be replaced by the existence of an infinite formal transverse recurrence.

For the full necessary ideal I and s=separation*lambda, the signs remain:

 D(s) empty iff s belongs sqrt(I) iff I+(Zloc*s−1) is unit in the polynomial coefficient ring extended by the fresh variable Zloc.

 A proper localized ideal gives s nonnilpotent, with the opposite conclusion: an existential nondegenerate necessary-chart survivor, not automatically a realized pair. Here neither full-ideal certificate exists.

The accurate disposition is SOURCE/COEFFICIENT MAPS PASS; THE ADVERTISED SMALL-MATRIX DECISION REDUCTION IS UNPROVED; FULL-CHART DECISION OPEN. Calling the correct recurrence itself a failed source map would misstate the evidence. Calling the proper first-strip or H projection a full-chart survivor would violate the campaign guardrail.
