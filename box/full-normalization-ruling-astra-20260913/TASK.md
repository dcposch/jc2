# Full-normalization ruling: one all-degree discriminator

Owner /root/nonemptiness_certificate; ROOT collector. First action must record
actual UTC. Original publication reserve September13 05:58UTC, hard stop
06:01UTC, no extension. One manual/documentary task, no computation or AWS.

Question: for a hypothetical polynomial Keller map F:A2->A2, let Y be the
normalization of the target in the FULL source field, so A2 embeds openly in
normal affine Y and Y->A2 is finite. Can the actual A2 open chart force an
A1-fibration h:Y->A1 whose pullback to the source is a polynomial coordinate?
Such a coordinate would be integral over C[F]. Do NOT assume either existence
of an affine-base ruling or coordinate pullback from a qualitative A1 ruling.
The complete endpoint is an all-degree actual-source constraint leading to
properness/invertibility, not a new special-degree donor exclusion.

ROOT's prelaunch history check: current APPROACHES section4 separates full
source/primitive from proper blocks, and section8 warns that qualitative A1
ruling gives neither a chosen compactification nor line-degree1. The old
block-descent-galois integration already gives the missed-principal-divisor
and class-lattice obstruction; do not reprove them. Filename/keyword search
found no current dedicated affine-extension/coordinate-integrality result;
this is NOT exhaustive novelty evidence. The old grok-approaches section22
mentions ML/Gizatullin as broad speculation. The September12 pseudo-plane
ML/equivariance check rejects unproved functoriality and inappropriate ML0.

First read WHOLE frozen APPROACHES and COORDINATION snapshots, then do your
own scoped history check BEFORE deriving anything. Charge only exact pinned
inputs, and distinguish whole from selected reads. The cheapest first test
is whether known normal affine surfaces with an A2 open chart force the
stated AFFINE-base/coordinate property, or whether a precise hypothesis is
missing. A generic cylinder need not extend its projection or a Ga action.
Do not upgrade that warning into a blanket theorem about this actual source.

If needed, use bounded primary-source web reading for the exact surface
statement. ROOT found Dubouloz--Kishimoto, Log-uniruled affine varieties
without cylinder-like open subsets, section1.1, at
https://www.numdam.org/item/10.24033/bsmf.2692.pdf . This is a discovery lead,
not an imported theorem. No protected repository, public mirror, social feed,
external contact or broad sweep. No source or interpreter execution anywhere,
including syntax/import/AST/dummy tests; only inert text/hash/orchestration and
the unchanged administrative artifact finalizer are permitted on HQ.

Deliver at most two pages of substantive reasoning plus provenance at
xmodel/full-normalization-ruling-astra-20260913.md, owned PINS and custody
in this box. NO_NEW_CONSTRAINT or KNOWN/GAP is acceptable. If the first test
fails, stop this precise shortcut; do not grow a surface/degree/control farm,
new criterion catalogue or automatic successor. No shared ledger edits,
code changes, new agent, claim promotion or external resource mutation.

Use apply_patch for authored files and transactional begin/close/finalize/
expected verify with basis0d39df3c9fd69c939a8420c54d03228b9077777d. Read whole
own report/manifest/PINS before handing off, compare all consumed pre/post
hashes, append the body marker LAST, then report custody SHA FIRST and actual
ALL WRITERS IDLE time. Do not claim novelty from keyword absence or use a
conditional conclusion as the missing actual-source premise.
