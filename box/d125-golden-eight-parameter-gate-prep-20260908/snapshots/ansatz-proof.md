# Astra adversarial cross — 20260908T0200Z

All four sealed blind hashes were verified against root's collection receipt before reading their bodies. This cross does not rebase them: post-cutoff15i is now accepted at the unequal/Q FIELD-point scope supplied by root, not golden/common/all-D125 or a new scheme claim. The independently sealed uniform-degree proof was NOT released and was not read. No promotion follows from this cross.

## 1. Decisive result: the shared eight-parameter golden ansatz is impossible

Astra Card2 and Fable's proposed j=12 family are the SAME polynomial ansatz after f=tau and renaming R3/R1. Their agreement is not independent evidence for source classification: both use the same golden factor control. Astra offered a sufficient restricted trial; Fable additionally claims every odd golden first contact j=12 lands in it. That extra source theorem requires its own review.

The ansatz itself has a short exact contradiction. Work over any commutative K-algebra with lambda inverted, where K=Q(rho), rho²−3rho+1=0; both embeddings are retained. Put

    t=1−rho, L=p+g, M=p+t g, H=p²LM², D=pLM,
    mu=5lambda²/9, F=lambda D+tau p,
    R=H+u gp²+v p³+w p,
    delta=gamma−beta alpha+5alpha²/9,
    A=R³+alpha R+F,
    B=R⁵+beta R³+gamma R+(5R²/3+beta−5alpha/9)F+mu L.

A and B are exactly the shared displayed ansatz; set c=−lambda*t*mu, as its required inner face forces. Keep ALL coefficient rows of J=[A,B]−c g². The free identity gives

    [A,B]=[R,T]+[F,mu L],
    T=(5lambda²/3)R(R−H)L−(10lambda*tau/3)R D p
      −(5tau²/3)R p²+alpha*mu L−delta F.       (1)

This uses D²=HL only as a factored identity. Define

    U=u−2tau*t/lambda, V=v−2tau/lambda,
    K3=p²(Ug+Vp).

The total-degree9 part of T is (5lambda²/3)H L K3. Thus the degree12 Jacobian block is (5lambda²/3)H[H,LK3]. Its two highest-g rows first force U=V=0. Substituting those relations into (1) gives the EXACT lower expression

    T=(5/3)R K2+alpha*mu L−delta F,
    K2=lambda²*w*p*L−tau²*p².                 (2)

Here is the full triangular row certificate. Each congruence is modulo previously listed consequences; all multipliers displayed beside the new variable are units.

| Literal row of J | Residual/consequence |
|---|---|
| total12, g⁷p⁵ | (10/3)lambda²rho² U ⇒ U=0 |
| total12, g⁶p⁶ | (35/3)lambda²rho² V ⇒ V=0 |
| total10, g⁶p⁴ | (5/3)lambda²rho² w ⇒ w=0 |
| constant | −mu*tau ⇒ tau=0, hence u=v=0 |
| total6, g⁴p² | delta*lambda*rho*t ⇒ delta=0 |
| total4, g³p | −2alpha*rho*mu ⇒ alpha=0 |
| total2, gp | −2mu*lambda, a unit: contradiction |

For clarity, before imposing w=0 the constant row is −mu(alpha*w+tau). After the first four rows R=H,F=lambda D. Equation(1) reduces to T=alpha*mu L−delta*lambda D; its degree6 and degree4 parts give the next two rows. Finally

    [D,L]=−t g²−2gp+(t−2)p²,

so the required monomial target cannot occur. The scalar row factors follow directly from det((3,2),(2,2))=2, det((3,2),(1,3))=7 and the leading coefficient [g³p²]H=rho. No centralizer or reducedness assumption is needed for this certificate. In particular the localized coefficient ideal of THIS ansatz is the unit ideal even over nilpotent coefficient algebras; no explicit expanded Bézout vector or full row stream was computed.

This is stronger than a finite-jet failure and narrower than golden exclusion. It kills neither j<=10 nor nonodd golden sources. If the separately reviewed first-contact classification identifies j=12 exactly with this ansatz, the result then removes that stratum. No reverse-lift work or solver is warranted for it.

## 2. Reconcile the proposed mechanisms and their limits

Fable's golden generic-centralizer outline is plausible: for Y=g/p the cubic equation has constant-term p-valuation−5, so all roots have valuation−5/3; geometric integrality and the polynomial centralizer follow by the already used argument. But transporting every reference/kernel normalization and the j-table remains a mathematical source claim, not something made accepted by a small scalar count. The j=10 degree5 remainder also needs its scalar H reference tracked explicitly. Bank no whole golden conclusion from this cross.

Fable's “sub-second evaluation” and binary “lambda=0 or an explicit point” prediction was unsupported. Evaluating/collecting polynomial equations does not decide their eight-parameter ideal. INCONCLUSIVE is a necessary third outcome for a general capped computation. This particular ansatz is now decided by the seven-row proof above, not by the number of parameters or an executed solver.

The nilpotent ribbon injection is a valid bookkeeping distinction, not a replacement rigidity theorem. Astra's exact dual-number control has nonconstant monic commuting3/5 initials. Root's ramified degrees6/10 control P=(Z²+a(p))³,Q=(Z²+a(p))⁵ independently shows that reduced, regular coefficients alone do not restore coprimality. A next golden theorem must control that common quadratic using additional source equations. Repeating necessary ramified coefficients without such an invariant is not the next priority.

Root CardC, Astra's binomial-family proposal and the Euler/coprime-monic simplification are complementary: source normalization, scalar kernels, injective normal restriction and the contact-to-target margin come FIRST; the Euler consumer is then short. Neither an infinite numerical family of hypotheses nor an all-coprime consumer identifies another actual receiver. Keep one literal attachment requirement and do not count three formulations as three independent avenues or presume K16 has ratio3:5.

## 3. Sol's C4-FILTER is not yet an encoded finite decision problem

The valid common4 centering is useful, but raw support does not decide cancellations in the A-reference, the first nonzero remainder, scalar-kernel removal or a full-Jacobian-forced weight bound. Sol's proposed inequalities still contain those source-dependent predicates. Enumerating14 contact orders does not encode them automatically.

A genuine exact LP could test a SPECIFIED sufficient support-only package. Its infeasibility would refute that encoding, not every single-linear-filtration proof that uses source equations or another reference. Its feasibility would still need a theorem that all actual sources satisfy the encoded premises. The commuting face fixture is a check on support-only reasoning; it cannot falsify a step explicitly using c!=0. Cheapest next action is to write the literal variables, inequalities and universal quantifiers for one such package, or identify the first coefficient cancellation requiring a Jacobian row. Do not write a Farkas engine before that object exists.

## 4. Priority and systems

Highest-information next test: independently verify the seven-row golden certificate and the exact j=12 attachment, then STOP that stratum. For new mathematics, prefer root's common4 full-Jacobian weight implication over an unspecified filtration LP, alongside one nonduplicated uniform-source interface review. None licenses a degree farm or AWS restart.

Reject both proposed machinery additions for now. Fable's static degree/name lint cannot reliably infer actual versus formal polynomial degree and may flag harmless scalar annotations; an advisory warning is not a scope proof. Sol's launch attestation largely duplicates the existing artifact receipt plus root's explicit sealed-before-invitation message. Preserve existing immutable provenance and fix any concrete scheduling defect through the existing parser/receipt, not another grammar or launch gate. The measured improvement here is a few source coefficient implications eliminating a proposed computation.

## Evidence / terminal boundary

Owned checker uses exact Q(rho) pairs, both conjugates, factors of degree<=5 and SINGLE scalar coefficient projections; it never materializes H powers, the full bracket or A/B. Ten normal/−O modes complete in2.5742s: the valid certificate controls pass; actual changed cubic face, quadratic face, D coefficient or target gp row rejects. These controls verify the displayed multipliers, not source classification. No new literature, high-source expansion, CAS, remote operation, model, shared/protected edit or unreleased peer was used. Current input/report pins and transaction are in owned custody. All writers idle before handoff. **STOP; cross result awaits independent review.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `8518`.
- Body SHA-256:
  `dde0522e292620e7ba7ae555ac5b307bf70fa101503a6ae20d6d9b850505fb12`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
