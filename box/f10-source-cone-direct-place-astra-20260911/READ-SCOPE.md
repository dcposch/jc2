# Read scope and documentary custody

Exactly eleven charged inputs listed in PINS.json. Every original matched its supplied SHA before charged body reads. Every input was read FRESH_WHOLE, not reused from memory. Initial aggregate report output clipped; the producer report, source FIRST, rank report and rank FIRST were recovered in separate whole reads. check.py was read in bounded sections through line540; its separate arithmetic and authority were read whole. No provenance traversal.

Rootcard first; ten pinned sources thereafter. Source FIRST and rank FIRST were read again through their final markers at01:23UTC. Producer was reread through line300 at01:24UTC. All reading finished before publication reserve. The explicit source corrections in ROOT-CARD control over incidental gate prose.

Only static text and manual algebra. No actual source, coefficient, baseline, candidate or fixture body; no scientific computation, code authoring, import, syntax, AST, test, network, AWS, process control, Git or other-lane work. Metadata hashes, text length/readback and existing artifact_finalize publication are the only executed documentary tools. No current runtime FIRST is an input.

Own report is a new conditional theorem and a readiness GAP, not an accepted source outcome. In particular, alternative necessary-row rank is not silently called canonical matrix rank. No worker, registration, code change or follow-on authority exists.

Publication: own report WHOLE reread, OPEN quantity/cheapest next check and collision review before marker. All11 originals checked again before seal; report, transaction and metadata hashes recorded in custody.json, which excludes its own recursive hash. All writers IDLE at terminal handoff.
