# Corrected single resultant observation — attempt B registration

ROOT, September13,2026. This is a separately tagged corrected attempt AFTER
attemptA's exact failure was collected and its worker terminated. It is not
an in-place retry, clock extension or reinterpretation of A as successful.
A's immutable report is box/f10-mixed-resultant-run-root-20260913/RESULT.md,
SHAfa988642ea7857a2829afd8bde6bc021dd161b05d8aec825fd6addf9c2203102.

The selected question is unchanged: observe the complete mixed-scalar
resultant and finite unknown integer exceptions, removing at most ONE
critical-comparison prerequisite. No full-array comparison, REG, source zero,
all-F10 or JC2 result follows. No degree/prime farm or increased science cap.

## Source gate before allocation

Observer661b739ed3f86267e9875a83537bd1a75ef5699442c99eb182ec2800f3005858,
runner9c2255c7dcb7c91a8abeb2bd0ea7db30b4d4e8182abdbbd08cc1dc76f33d094d
are unchanged. Different-model observer FIRST
2bc798d366cfbaf39161f6523a416e85ae744a0f25b33847186a0eda7aef29c0 remains
CONDITIONAL STATIC CONFIRMED for one exploratory observation. The only
permitted preflight correction is the separately frozen Sol derivative in
box/f10-mixed-preflight-repair-sol-20260913. ROOT must independently confirm
terminal state, custody FIRST, expected transaction/postpins, WHOLE source and
exact diff, then record the actual accepted hash/FIRST in INSTANCE.md BEFORE
allocation. No uncollected draft is authority. No new controller or source fix.

## Fixed resource and clock contract

One r7i.xlarge4vCPU/32GiB, personal/us-east-1, ami-066263bf15bd856de,
subnet-948915c9, sg-09ffa8932558f0a79, key jc2-fleet. Root100GiB gp3 retained
with DeleteOnTermination=false, API termination enabled, no IAM profile.
Refresh quota and both nonterminal campaign selectors before allocation.
No package installation, security-group change, baked/protected campaign-tree
access, legacy migration, second worker, scientific-source retry or cap rise.

ORIGINAL clocks: allocate no earlier than08:08UTC; latest service admission
08:18UTC; independent exact-instance retirement08:36UTC. No extension.
Arm the HQ user-manager timer within60seconds of allocation and record its
exact-ID argv and deadline. Retire earlier after terminal evidence collection.

ONE preflight service jc2-mixres-preflight.service,60seconds active, stop5s,
control-group kill, no restart, nobody/nogroup,2GiB memory, zero swap, CPU200%,
Tasks32, FSIZE8MiB, NoNewPrivileges, empty caps, namespaces/cgroup writes
disabled, ProtectSystem=strict/ProtectHome, PrivateTmp/PrivateNetwork. Only
/var/lib/jc2-mixres-preflight is writable, owner65534/groupubuntu/mode2750.
Use the corrected immutable preflight with /usr/bin/python3 -I -B -u,
passing the actual worker ID and tag mixres20260913b. Fixed installation paths
/opt/jc2-mixres20260913a are retained literally by the unchanged preflight;
they are NEW paths on this NEW worker, not the old host or old job identity.

ONE JC2-JOB/v1 service with tag mixres20260913b, joint840-second smoke/observe
cap,16384MiB memory, swap0, CPU200%, Tasks32, per-file8MiB, plus existing5+5s
cleanup/receipt grace. Preflight+science active<=900s. Bind a NEW job.json
to this instance/tag and both literal argv arrays; no old manifest/template
mutation. Phases run /usr/bin/python3 -I -u @payload/observer.py with exact
instance/tag and mode. All four source/manifest files root0444 with canonical
root/nonwritable ancestry. Require512MiB free EBS; no aggregate disk-quota claim.

## Operator and evidence gates

Verify console ED25519 fingerprint against scanned key before strict SSH/SCP.
Observe DMI/boot/OS/cloud-final; transfer only four registered files through
a fresh staging directory. Check all source hashes/physical ancestry before
the preflight. No unavailable-dependency installation or automatic repair.

Start preflight once with durable stdout/stderr. Require actual success,
PREFLIGHT_PASS, all three causal refusals, no scientific import/output in the
negative children, installed Python/Sympy/backend/package/loaded native identity
DATA and cgroup absence. Repeated identical exception diagnostics are permitted;
raw hook errors remain visible. Whole diagnostic review precedes science.
If any check fails, collect and retire; do not resume/patch on this worker.

Before08:18 call unchanged job.sh start once with actual instance/tag, bundle,
manifest hash and runner hash. Within60seconds inspect the actual service/
process or authoritative terminal state. The runner enforces exact credentials,
kernel/cgroup limits, phased lifecycle and durable receipts, not a model clock.

Collect only terminal unit with actual cgroup absent. Retain all DATA, partials,
raw phase streams, source/manifest, terminal receipt and manager/journal. Add
the preflight evidence and installed sources to the explicit archive. Flush,
copy to HQ, verify SHA and a second WHOLE-byte stream, flush local copies,
then terminate the exact worker and verify retained EBS. Failed transfer never
extends08:36 or authorizes deletion; EBS permits recovery. No broad fleet kill.

Output remains OBSERVATION/UNREVIEWED. Missing output means incomplete, not
no exceptions. Complete factor-product equality proves neither irreducibility
nor the resultant identity; ROOT selects a cheapest independent check only
after useful DATA. No scientific or all-degree claim is banked from exit0.
