# F10 middle real-domain certificate: disabled static implementation

Producer verdict: **STATIC IMPLEMENTATION COMPLETE / UNEXECUTED / NOT RUNTIME AUTHORITY**. No mathematical certificate has been formed, no program compiled/imported/tested, and no nonresonance or source-exclusion claim is made. Accepted17zzi supplies a certificate contract only.

First actual action was 2026-09-10 08:51:55 UTC; the unchanged controlling cap is 09:13:55, with publication reserve from 09:10:55. Targets were absent before begin. The card and eight exact accepted inputs were hash-verified before body access or documented same-byte prior-WHOLE reuse; post-pins remain exact. READ-SCOPE.md and PINS.json carry the complete nine-object scope. No provenance or unrelated report was consumed.

## Delivered source

The owned package contains producer.py, checker.py and a metadata-only authority.py; a disabled registration template and finite validation plan accompany them. All files are new; accepted inputs remain untouched. No third-party arithmetic engine or solver is used. Prospective exact arithmetic is Python's standard Fraction and integer APIs; installed compatibility, syntax and costs are untested.

The producer reconstructs every literal S/T coefficient, divides by the fixed 10080 leading coefficient, and constructs the padded nine-by-nine matrix after multiplying Frem by d=10080^3. A subset determinant DP computes all row-zero cofactors together. With z=adj(B)e0, Atilde formed from its first five coordinates and Btilde from its last four, U=d*Atilde and Wcof=Btilde-U*Qdiv give the exact intended identity R=U*S+Wcof*T. R is 10080^15 times the unintegerized determinant. The DP sign and conversion were manually derived, not executed. No arbitrary polynomial division or determinant header is trusted.

The independent checker imports neither producer nor its algebra. It reconstructs literal E_X and Q_X in a separate four-variable sparse algebra; Q_nu-7E_nu supplies D/K, and monic synthetic division supplies the alpha/nu divided difference. It checks the complete quotient identity, all coefficients of the cofactor identity including every positive V power, the rectangle pullback, maximal (1-t)^j with j>=1, all Bernstein expansion coefficients, all 22 equal positive collapsed-edge entries, and the signs. Its Horner pullback, descending synthetic factor division and direct Bernstein-basis reconstruction differ from the producer algorithms. An alternative bounded exact identity is sufficient; the checker does not independently recompute the determinant itself.

The wire uses reduced numerator/denominator decimal strings, explicit exponent strings, full inventories and strict schema. Duplicate keys/terms, numeric JSON coefficients, noncanonical rationals, missing dense slots, declared/actual degree mismatch and fixed size/bit/term limits fail closed. Only the checker can prospectively emit CERTIFIED_NONRESONANCE after full success; zero/negative sign outcomes are INCONCLUSIVE. No retained t=0, s=0 or s=1 boundary is discarded.

## Boundaries and pending validation

The authority layer requires the fixed explicit job and a future root-owned mode-0444 registration, Linux/EC2 DMI and exact host/instance/cwd/script argv/interpreter/input pins, absent outputs and unchanged authority. It refuses before Fraction/math imports and polynomial allocations. It is not a supervisor: the future root wrapper must bind full Python flags/native/instrument/source pins and enforce the joint original clock, cgroup/PGID cleanup, aggregate files and reviewed CAPRUN controls. The included template is disabled with null worker/cwd/argv/caps/deadline fields. No worker was inspected, started or assigned.

The proposed joint formation/check cap remains 360 wall / 330 CPU seconds, 2 GiB sampled RSS, 16 MiB aggregate wire, entirely unmeasured. Certificate plus receipt ceilings are enforced by the CLIs; wrapper enforcement of all other logs/files remains a future obligation. No retry, cap escalation, alternate representation, subdivision, engine or branch farm is supplied.

VALIDATION.md gives twelve finite prospective corruption classes, including U+1/positive-V cancellation, R+1, wrong maximal j, illicit retained-edge removal, Bernstein corruption, sign/zero-endpoint counterfeit, duplicate keys/terms, oversize/giant strings and bounds. It explicitly distinguishes early identity/schema rejection from genuinely exercised sign branches. No changed fixture was constructed or run, and no successful certificate is assumed to exist.

There are two task-local pending quantities, no new canonical OPEN identifiers: three unexecuted sources need one whole-code different-model static review; zero actual artifacts/runs need one separately registered capped formation/read-back only if that review passes. Unknown positivity is not a counterexample, and static author review is not an execution result. No speed, actual term/degree/bit-length measurement, source point, ideal decision or JC2 consequence is claimed.

Own code, documents and this substantive report have been read WHOLE as text. An own-only OPEN/collision check precedes the completion marker; publication uses begin/close/finalize/expected verification. All writers become IDLE before custody-first terminal handoff. No follow-on authority is inferred.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5358`.
- Body SHA-256:
  `a1f250a2daa6e0ba57c271efbd0c4987d756c6c84eeb029c69790c4dc12d5a1d`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
