# Exactly four semantic corruption checks — disabled source contract

STATIC/UNEXECUTED. No coefficient body was read in this preparation. No fixture, registration, scientific subprocess, import, syntax/AST/compile/test or worker action occurred. The existing positive receipt is metadata-only evidence binding the future baseline; neither its status nor this new control code promotes any mathematical conclusion.

## Exact immutable inputs and scope

Original candidate SHA256 `219151792c8ffbbaab2e745fa60bd72e9c05c705fcc2ded992bcd97badf05470`, 854264 bytes, is a future ROOT-staged readonly input outside the working tmpfs. It was NOT read or copied here. The pinned terminal positive receipt has SHA256 `1a9bb0f58e96fa5a0be086e72a725f3e1f7eabad89248888c3d8acc13323cf44`; its actual metadata names j=4, degree_t=36, degree_s=21 and the three accepted science hashes. Only that 680-byte receipt was read. No positive producer or checker rerun is in this batch. Wrong-job startup calls through both actual CLIs remain nonmathematical preflights.

Accepted producer.py/checker.py/authority.py and probe.py are exact unchanged copies. CAPRUN remains the external accepted hash `4435279df8f987c667ebc2fae8e4bd987916032561cb3faf1e07a57789d196c2`. New scientific code is only mutator.py; new dispatch.py is a narrow own copy of the qualified runtime dispatcher. DISPATCH.diff records every source delta. No monkeypatch, dynamic producer import, second checker implementation, solver, coefficient expansion or extra mathematical batch is supplied.

## Fixed sequence and actual error predicates

Nine preflights are retained: disabled, wronghost, argv, caps and missing-producer-pin refusals through unchanged authority.authorize; valid sentinel; wrong-job invocation of each accepted science CLI; same-PGID descendant RSS/TERM/KILL dummy. Exact error strings, absence of postauthorize sentinels, actual nonroot identity, startup flags and all dummy identity/signal/quiet checks remain. Then exactly one bounded mutator and four genuine checker.main invocations: at most fourteen CAPRUN calls in the literal registered inventory. There is no ordinary positive scientific execution.

The four mutations and checker outcomes are:

| Label | Only semantic change | Required exact stderr after NORMAL_EXIT/2 |
|---|---|---|
| U | constant coefficient of U increased by one | `INCONCLUSIVE: positive V cancellation` |
| R | constant coefficient of R increased by one | `INCONCLUSIVE: full cofactor identity` |
| j | literal j string 4 changed to5, all other fields kept | `INCONCLUSIVE: wrong maximal excluded-edge factor` |
| bernstein | dense Bernstein coefficient [1][1] increased by one | `INCONCLUSIVE: full Bernstein expansion` |

Each error includes exactly one terminating newline; stdout must be empty and the new checker receipt path absent. A stale pin, early schema rejection, cap/signal, changed reason, unexpected receipt, or nonempty stdout is STOP/NONDECISION, never a control pass. New changed fixture hashes and post-check hashes must agree. The result can only be `FOUR_SEMANTIC_REJECTIONS_CONFIRMED`, not CERTIFIED_NONRESONANCE, a source theorem or JC2.

## Mutator and canonical wire

The mutator CLI is `PYTHON -E -s -S -B mutator.py --job f10-middle-real-certificate-20260910 --registration AUTH --input ORIGINAL --positive-receipt POSITIVE --output-dir WRITER --receipt RECEIPT`. The underlying accepted authority's fixed science JOB is intentionally unchanged; the new outer ROOT job is separately `f10-middle-real-controls-20260910`.

Before importing Fraction or reading coefficient strings, mutator calls the accepted authority with operation `mutate`, exact input path, and five exact outputs (receipt followed by four `mutated-NAME.json` paths). ROOT's authority pins original and positive receipt, all code, eight actual scientific report files, implementation/gate reports and native-manifest hash. The mutator verifies actual baseline bytes/hash, receipt hash/status/input hash/j/degrees/code, and baseline schema/status/science/code/j. This uses only the already fixed baseline; no unverified replacement or fabricated positive evidence is accepted.

The original wire is parsed with duplicate-key and JSON-number rejection. Rational coefficient pairs remain canonical decimal STRINGS. Fraction is used only after authorization for the three coefficient increments; the j change is the literal 4-to5 change. An absent polynomial constant is inserted at the first canonical slot; a new zero coefficient is removed, preserving sparse canonical format. Term/bit/decimal bounds remain valid or the mutator stops before an off-envelope fixture is accepted. Dense zeros remain literal dense slots. There is no float, eval, pickle or integer JSON conversion of coefficients.

Each mutation starts from a deepcopy of the original, uses only its prescribed field, must differ, is exclusively written, and is reread as exact canonical wire. Digest must differ from the baseline. Restoring the selected subtree/field must reproduce the original whole object; the code path confines U/R edits to the constant entry. The bounded mutator receipt supplies all four names, exact paths, changed hashes/byte counts, expected reasons, full readback status and current code hashes. It is not a checker verdict.

## Dispatcher changes and retained custody

The new outer schema is `f10-middle-real-controls/v1`, disabled by default. ROOT must fill the existing exact host/UID/GID/ns/cgroup, trusted readonly source directory, role paths, native manifest, all absolute commands, original clocks/caps and exclusive working/durable paths. The new `files` role is `mutator`; two extra pinned root fields are `original_input` and `positive_receipt`. Role paths for the three science files are now directly tied to accepted hashes, closing the prior gate's basename-only ambiguity. probe's accepted hash is likewise fixed. The baseline receipt code map is checked directly. No accepted scientific source was edited.

The ROOT registration has exactly fourteen command keys: `refuse-disabled`, `refuse-wronghost`, `refuse-argv`, `refuse-caps`, `refuse-missing`, `valid`, `startup-producer`, `startup-checker`, `dummy`, `mutate`, `check-U`, `check-R`, `check-j`, `check-bernstein`. Each complete CAPRUN/setpriv/Python argv must match the code-generated fixed call. External dispatcher/CAPRUN flags remain `-I -S -B`, science/probe/mutator `-E -s -S -B`; setpriv preserves child PID/PGID. All other parent authority, EC2/DMI, pre/post hash, 15-second admission, exact-cgroup quiet, live child capture, immutable native-manifest, file-cap and no-retry machinery is retained.

Only after mutator/CAPRUN and owned cgroup are terminal does ROOT dispatcher read its metadata receipt. It checks all four exact inventory entries and file hashes, moves every changed file to a fresh root-owned0444 frozen input, and makes the entire frozen parent0555 BEFORE the first negative check. Each check receives a separately written root-owned0444 authority with its actual frozen-input hash. Parent and input hashes are rechecked after each terminal checker. The metadata dispatcher never rewrites coefficients. All three accepted source pins remain unchanged in the copied wire and receipt comparison.

The dedicated16MiB tmpfs bounds all four changed fixtures, streams, telemetry, authorities and metadata mechanically. Original candidate and old positive receipt remain readonly outside it, accounted as separate input storage. All source/native/report files must be world-readable and source/authority/frozen ancestry traversable0755; ROOT must use a minimal env-i startup and account for installed pyc/ZIP/ELF closure. The existing code checks immutability and hashes, not the completeness of ROOT's native inventory. There is no discovery, installer or fallback.

On STOP or success where exact groups are quiet, all working bytes are copied byte-for-byte to a separately registered absent durable EBS directory, then hashes/manifests are written and nested directories fsynced. The one recommended durability fix is included: the durable directory's parent is fsynced too. No stdout is modified after a successful copy. These are custody replicas, not extra scientific outputs. Incomplete custody remains explicitly STOP_CUSTODY_INCOMPLETE requiring ROOT recovery; there is no deletion/unmount/worker stop inside the dispatcher.

## Fixed prospective budgets and remaining obligations

The unchanged science joint budget is360wall/330CPU/2GiB/16MiB, with the monotonic clock stored BEFORE mutator and never reset. Mutator profile is10wall/9softCPU; each of four checks45wall/49softCPU. CAPRUN's +1 hard allowances sum to210CPU, not330; no larger cap is requested. All five scientific child walls sum to190, but this is a budget partition, not a speed claim. Separate preflight remains180wall with5wall/3CPU per process (32MiB sampled dummy, other profiles2GiB). Original absolute ROOT deadline and15-second admission/post checks still apply; native hashing and wrapper overhead can conservatively produce STOP. No timer/date is enabled by this template.

Two pending quantities, no new canonical OPEN ID: (1) new mutator/dispatcher static correctness—zero independent reviews of these new bytes, cheapest next test one FIRST whole-source gate; (2) actual four semantic controls—zero fixtures or executions, cheapest test only after review is one fresh ROOT-registered bounded batch. This preparation does not authorize that test. No claim is made for the other eight corruption classes, a repeated positive run, measured performance or a mathematical promotion.
