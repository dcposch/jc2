# Read scope and execution boundary

First action 2026-09-09 15:16:26 UTC; controlling stop 15:28:26 UTC (actual plus twelve minutes, earlier than 15:29). Both final targets were absent before begin. All six allowed inputs were hashed before body access and read WHOLE: the original caller, corrected engine, terminal static code gate, terminal runtime-validation gate, old engine telemetry and old engine stderr. An oversized combined response truncated part of the static gate; it was reread separately through EOF. No provenance paths were followed and no theorem or actual artifact was read.

Only source-text copies/edits via apply_patch, literal git diffs, documentary reads, hashes and publication tooling occurred. No compile, import, source execution, test, mathematical subprocess, API probe, network, AWS/SSH or worker action occurred. String replacement used only literal source text and uniqueness checks, not evaluation. The two resulting source files were read WHOLE statically. Literal diffs preserve the precise permitted changes; originals were not edited.

The old telemetry identifies the wrapper's NORMAL_EXIT 1, no typed resource, 51.59926408 seconds and 139493376 bytes sampled group peak. Its stderr identifies the wrapper's combined rc/hash refusal. These facts do not determine the nested signed return code or prove a CPU timeout. Root's report of twenty row blocks and missing BRANCH/DONE is not independently rechecked here: the full engine transcript is outside this task's inputs.

No new registration is created. Future clocks, worker assignment, controls and decision dispatch remain root-owned after the narrow independent delta review. Existing checker/tests and their retained qualifications are unchanged and not replayed.
