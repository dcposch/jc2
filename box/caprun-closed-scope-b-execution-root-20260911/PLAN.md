# One new strict-nine observation: batch b

ROOT selected this batch after completing the September11 1100 full strategy
round. It is not a resume of the closed batch-a worker. First preparation
2026-09-11 11:40:29UTC; ORIGINAL-CLOCKS.json fixes every deadline, including
preparation reserve11:55/HARD12:00, latest allocation12:05, setup12:25,
latest holder12:25:45, holder cutoff12:27:45, admission12:28, outer TERM12:35,
KILL12:35:05, custody12:42 and instance retirement12:50. Never reset.

Quantity: obtain all nine prescribed runtime outcomes and strict descendant
identity/RSS/TERM/KILL/reap/closed-leaf-quiet predicates. Cheapest test: this
single registered observation on one active AWS worker, with all original
limits unchanged. Preparation/custody20–30minutes was an UNMEASURED planning
estimate. Neither a launch nor an expected failure is a pass. Failure ends
this cycle's engineering allocation; no automatic retry or optional framework.
Success requires the existing different-model actual-evidence gate before
ROOT separately considers any necessary-row scientific observation.

At11:42UTC the personal/us-east-1 tagged nonterminal fleet is empty. The
On-Demand Standard quota is1920vCPUs; AWS/Usage samples at11:25/30/35/40
show84vCPUs, delayed aggregate information rather than a reservation or
inspection of separately owned infrastructure. Selected c7i.2xlarge needs8.
AMI ami-066263bf15bd856de is available/x86_64 with EBS root/dev/sda1.
The exact launch preserves100GiB root EBS with DeleteOnTermination=false
and API termination enabled. Immediately arm the original exact-ID coordinator
USER retirement timer; if arming fails, terminate that exact new worker.
Retire earlier after secured evidence, never hold it idle for review.

The accepted nine source/runtime bytes and holder/admission algorithms remain
unchanged. New SOURCE-CONTRACT adds only batch/clocks/strategy metadata and
historical-parent attribution. All source/specification/runtime scopes and
negative mutations are unchanged; bind its new hash throughout registration
and all applicable authority fields. Preflight-policy path bytes are ported
to the new base, not its source-line/caret/reason/exit predicates. ROOT's
initial disabled port missed several differently named template placeholders
and retained the launcher's template output directory; whole readback caught
these before any execution. No disabled draft was used to launch.

The calendar binding is ordinary metadata: Python retains ISO+00:00 strings;
systemd OnCalendar uses space-separated UTC strings for the SAME instants.
Coordinator administrative parser-only checks11:44 accept12:35,12:35:05,
12:50 and confirm the ISO/space math deadline has equal epoch1789130100.
That is not actual-worker native qualification, lifecycle execution or science.

Finish worker-independent metadata before allocation. Fresh actual host,
boot, namespace, durable-device and installed native/admin inventories are
required; no old observed value is substituted. Before the holder, install
the pre-admission stage and authenticate ROOT/PID1 namespaces, root cgroup2
mount, ownership/effective ACL/no delegation/no migration/no external entrant
and no concurrent ROOT writer. These remain explicit trusted-ROOT premises.
Capture the actual coordinator timer/service/full AWS argv/profile/region/
instance/original deadline immediately before admission and bind its receipt.

Only outer/leaf actual identities and final serialization/freezing/release
remain inside the unchanged120-second holder ceiling. Use the existing exact
documentary binding map and ordinary JSON tools: preserve all nine labels,
CPU origin string0, phase_policy null, typed_slot_policy empty, and canonical
sorted compact ASCII JSON plus newline for each authority. Refuse-hash keeps
its deliberate zero digest. Only genuine ROOT approval can set the three
flags. Freeze final registration once; authenticate actual SHA and one bounded
release frame. No source producer/checker success, FULL13, rank or science.

Custody follows actual terminal quiet, archive/compare/fsync/download/local
hash/fsync, then exact-instance retirement with EBS retained. No mathematical
baseline/place/rank/source conclusion, new all-r claim or JC2 result follows.
