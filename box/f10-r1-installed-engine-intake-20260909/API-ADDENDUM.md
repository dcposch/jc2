# Root metadata-only compatibility addendum, 14:34 UTC

The first intake confirmed installed Singular4.3.2 and Python3.12.3.
Root now authorizes exactly ONE further read-only SSH metadata script,
api-metadata.sh, before the SAME original14:43UTC stop. It runs the fixed
engine startup flags with print/option/quit statements ONLY to observe
option-name availability and quiet stdout framing. No polynomial/ring
declaration, std/lift, solver, source/test/caller execution, installation,
remote file creation or other worker is permitted. The option probe is
nonmathematical CLI metadata, not a small or truncated ideal computation.

The exact physical host, binary SHA and Python SHA are rechecked first.
Single engine invocation has5wall/3CPU/128MiB/1MiB-file limits; SSH20seconds.
This extra script is a separately frozen extension of metadata.sh, not an
edit to prior frozen bytes or a fresh/extended deadline. Root stops the
worker immediately after collection if code changes are needed.
