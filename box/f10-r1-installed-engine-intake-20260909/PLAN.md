# Installed-engine intake ONLY, 2026-09-09

Root authorizes one bounded restart of its STOPPED worker
i-08d2a40f272ee9fa2 (retained c7i.4xlarge, Owner
f10-r1-exact-artifact-validation-20260909, volume vol-0574e0fa5aed1f5e3)
for READ-ONLY installed Singular/Python/CLI/documentation metadata. No
solver, std/lift, polynomial input, source/test/caller execution, package
installation, filesystem rewrite, other worker or protected path is allowed.

Original absolute stop:2026-09-09 14:43:00 UTC. Root owns collection and stop;
an independent user-systemd exact-instance stop timer is scheduled first.
No implicit validation/solve authority follows metadata success. Retain disk.
If compatibility requires code work, stop this worker before that work/review.

Preflight:14:28 exact instance STOPPED, physical allocation/Owner/volume
unchanged. Standard quota1920 freshly queried; AWS/Usage Standard/OnDemand
aggregate maximum212 at14:10,14:15,14:20,14:25. These are dated sampled
observations, not an instantaneous headroom or billing claim. Static code
gate16x completed and qualified before this intake. No new worker is created.

Only metadata.sh (SHA recorded by root) may be streamed over SSH after fresh
instance resolution. It verifies hostname/DMI before bounded package, binary
and CLI help/version observations. Each CLI probe has5wall/3CPU/128MiB limits,
no polynomial input or startup files; overall SSH20seconds. Root preserves
raw output and checks the actual version rather than the review's excluded
uncharged old-version comparison. Failed observation is not permission for
an alternate engine, installed package, solver attempt or deadline extension.
