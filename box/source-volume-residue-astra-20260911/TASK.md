# Co-research: source volume residue on an unequal-depth plane completion

ROOT assignment September11 23:43UTC. Original reserve September12 00:00UTC,
HARD00:03UTC; never reset. Record actual first action. Manual mathematics,
not promotion or a descendant relying on a new lemma as established.

Exactly this TASK plus these two frozen reports; pin all before bodies:
- xmodel/reduced-boundary-fibre-control-root-20260911.md
  fe5345acd67108c770f1d5eca0850739a1ab00dcdcb69148ba5306d8b5331551
- xmodel/danielewski-primitive-boundary-astra-20260911.md
  f119d33d1c15bb0676cfc13c0e6290901ad3327680eb7eb4fb503e7d3c947608
Fresh WHOLE reads required; optional exact-pin same-agent WHOLE reuse only
with explicit source/time disclosure. No other corpus, linked or web input.

Target: use GLOBAL exactness of the source volume, rather than another
numerical rank or class-group budget, to exclude a concrete new construction
scaffold. ROOT proposals below are UNREVIEWED. Independently prove or break.

Let Y be the affine surface in C4[x,z,y,w] defined by

    xy=z(z-1),  xw=zy,  y^2=(z-1)w.

Proposed two A2 charts:
U0(x,u): (x,z,y,w)=(x,xu,u(xu-1),u^2(xu-1));
U1(x,v): (x,z,y,w)=(x,1+x^2 v,xv(1+x^2 v),v(1+x^2 v)^2).
Intersection x!=0, transition u=x v+x^-1. Proposed Y smooth integral,
U0=Y-E with E={x=0,z=1,y=0}, E=A1_w. Check whole affine scheme, not only
dense torus: extra components/nilpotents, chart coverage and inverses matter.

Proposed global form omega=dx wedge du on U0 equals x dx wedge dv on U1,
so div(omega)=E and K_Y=[E]. This repairs the canonical-class mismatch of
the old equal-depth control but is NOT a finite Keller normalization.

The decisive claim: there are NO P,Q in O(Y) with
J_(x,u)(P|U0,Q|U0)=c!=0, without degree/support/homogeneity restrictions.
Such a pair would otherwise yield a polynomial JC2 counterexample: if it
were a polynomial automorphism, O(Y) would contain u and equal C[x,u],
contrary to the pole of u on E. More directly the proposed obstruction is
that omega is NOT globally exact, whereas dP wedge dQ=d(P dQ) is exact.

ROOT elementary Cech calculation (independently check signs and sufficiency):
local primitives alpha0=-u dx, alpha1=-x v dx have derivatives omega.
Their difference on the overlap is -dx/x. Closed polynomial one-forms on
each A2 are exact; a Laurent one-form -dx/x is not exact in
C[x,x^-1,u]. Thus a global primitive would contradict Laurent residue.
No use of finite-map, source-landing, cohomology comparison or unseen theorem.

Sharpness/generalization, bounded not a family farm: on two plane charts
u=x^m v+g(x), m>=0, g Laurent polynomial, the same local primitives differ
by -g(x)dx. A necessary condition for an extending Keller pair is
Res_(x=0) g(x)dx=0. Do NOT claim sufficiency, general affineness or that all
Keller normalizations have these charts. For the explicit controls
g=x^-k (k>=2), ROOT proposes a GLOBAL primitive with chart0 expression

    beta0=-(k*u*dx+x*du)/(k-1),
    beta1=-((m+k)*x^m*v*dx+x^(m+1)*dv)/(k-1).

Check agreement/derivative. This prevents a false universal exclusion:
K_Y=mE does not force a nonzero volume class. Compare only the exact old
T chart transition (m=0,k=2, coefficient2 up to sign); its earlier scalar
pair question should NOT close. Explicitly identify any source-applicability
gap. No new candidate H, truncation, rank scan, positive construction or
affinity claim follows just from zero residue.

Optional ONLY if time remains: exact intersection ring description for
the explicit Y under weights wt(x)=1,wt(u)=-1,wt(z)=0, or a direct algebraic
proof of the same obstruction. Do not substitute homogeneous-only exclusion
for the unrestricted endpoint. Stop after this bounded discriminator.

Write own xmodel/source-volume-residue-astra-20260911.md via normal
artifact_finalize begin/close/finalize/expected verify, basis
0d39df3c9fd69c939a8420c54d03228b9077777d. Own box PINS/custody only; no shared
edits. Target <=1900 words excluding custody; bounded writes <1500 words,
own WHOLE/pins/scope/quantity/cheapest-test/collision check before BODY-END
LAST. No new canonical OPEN. Cheapest test manual <=15minutes UNMEASURED.
Report result and limits even if no new global mechanism. Terminal handoff
only when ALL WRITERS IDLE, custody hash, final/manifest hashes and times.

Prohibited: scientific/local CAS, mathematical code/import/AST/tests/dummy,
payload or remote execution, AWS/SSH/process control, Git operations,
protected project or mirror access, outside input, new agents, Fable launch,
automatic descendants. No external theorem or existing source scope is
promoted. ROOT performs independent primary/history comparison in parallel.
