# ONE mixed-scalar worker qualification attempt

ROOT,2026-09-12, under standing operator fleet authority. This registers
bounded metadata installation/collection/assembly and ONE pre-science live
no-CAS dummy, NOT the algebra computation. Goal remains unresolved JC2.
Current conditional source FIRSTs and exact accepted source vector are in
notes.md20:50UTC and the three named ROOT intakes. All pins must match again.

## Allocation and original cutoffs

At most ONE fresh r7i.2xlarge campaign worker (8vCPU/64GiB), profile personal,
us-east-1, Owner=f10-mixed-qualification-root-20260912. Both fleet selectors
were empty and Standard quota1920 at20:52UTC. No other machine is in scope.
Immediately bind exact returned instance/boot/host/IP/root-volume identity;
retain its root EBS before writing evidence, keep API termination enabled.
Arm an independent exact-ID coordinator retirement for21:25UTC; original
qualification stop21:20UTC, custody/retirement reserve through21:25UTC.
These clocks never reset. Close earlier if the attempt stops or drains.
No source repair/retry/second worker/cap enlargement is authorized by this card.

If, and only if, qualification completes and a separately registered science
job is ACTUALLY running before21:25, ROOT may retire this qualification-only
retirement timer AFTER binding the science job's independent original wall
caps and an exact-ID final22:30UTC retirement. This transfers active-worker
ownership to a new job; it does not extend any qualification execution clock.
Otherwise retire by21:25 with evidence retained. No paid idle preparation.

## Finite qualification sequence

1. Poll fresh worker and cloud-init terminal state, without fleet.sh's
   unbounded wait or optional msolve installation. Authenticate physical
   EC2/instance/boot/hostname/PID namespace and package/kernel/systemd facts.
   Do not enter or inspect the baked campaign or protected repository.
2. Install the six pinned science/runtime files under the accepted exact
   /opt/jc2-mixed-20260912/{science,runtime} layout; copy only complete
   actual /usr/lib/python3/dist-packages/{sympy,mpmath} trees to its private
   lib parent after metadata bounds/ownership inspection. Preserve resources,
   pyc and aliases; no pruning, venv, ZIP or optional native backend. Require
   fresh targets and frozen root-owned traversable ACL-free source ancestry.
3. Instantiate ONLY the collector's physical instance placeholder. Run the
   reviewed collector under a root systemd Type=exec service, whole-cgroup
   CPUQuota80%/period100ms,MemoryMax1GiB,swap0,TasksMax64,FSIZE4MiB,
   RuntimeMaxSec595/TimeoutStopSec5,KillModecontrol-group and no restart.
   All collector stdout/stderr go to one bounded256MiB preparation tmpfs.
   Original21:20 absolute stop dominates every relative phase allowance.
   Independent whole-unit stop is armed before collection; retain terminal
   status, actual unit/cgroup/PID/namespace/limits, source and raw byte pins.
4. Under a separately capped administrative service within the same original
   qualification cutoff, run the reviewed native_records.py with -I -S -B,
   authenticated raw/facts inputs and fresh private OUT. CPU80%,memory1GiB,
   swap0,FSIZE4MiB,whole wall115+5seconds; all writes in bounded preparation
   storage. Require exact positive terminal receipt, every serialized limit,
   same complete files projection, unchanged inputs and durable readback.
   Metadata parsing is not a claim of complete import/native qualification.
5. ROOT checks actual import paths/versions/source APIs, full private package
   contents, aliases/absence/pyc/native mappings/ancestry and no writers.
   Metadata-only Python startup observations are AWS-only and bounded; do
   not import or execute authority/produce/check or perform CAS calibration.
6. Register ONE live CAPRUN/new-probe dummy with exact accepted argv in its
   own no-escape,nondelegated,fair-class cgroup,32GiB memory/AS,swap0,
   FSIZE256MiB and separate256MiB tmpfs, all streams bounded; original
   whole dummy allowance25seconds including cleanup. Require actual kernel
   cpu.max=80000 100000,burst0 and inherited rlimits. Bind identities before
   admission; require resource-rss cap, readiness+canonical child JSON,
   expected boot/ns/cgroup, distinct child/leader PID, shared PGID, peak RSS,
   genuine TERM then KILL/reaping and final kernel cgroup emptiness. Partial,
   malformed or missed observations are STOP, never a dummy pass.
7. Freeze authentic terminal evidence locally and on retained EBS, including
   full raw metadata and all output/source pins. Report exact actual state;
   any failure is NONDECISION. No success label follows from a hash alone.

No enabled science registration is prepared by this document. After complete
qualification, ROOT may separately register the already reviewed six-phase
ONE-gcd all-parameter certificate within its unchanged3540second total wall,
80percent CPU,32GiB memory and256MiB output envelope. Until then no algebra
is authorized, and no science outcome or source exclusion is claimed.
