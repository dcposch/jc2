# Conditional wrong-target control — read perimeter

Owner /root/model_productivity, September9,2026. The only mathematical input is the explicit ODE hypothesis in INPUT.md. No source producer, live ODE gate, root coalesced reduction, peer, external primary page or protected path was read for this task. Earlier source-background knowledge is not charged as an accepted mathematical premise; the displayed additional boundary-face comparison is an explicitly conditional algebraic comparison.

All proof steps are manual and factored: chain rule in polynomial coordinates(p,Z), complete monomial-support inequalities, small reciprocal coefficient projection, exact finite polynomial correction/residual, and integer grading plus a leading-coefficient cokernel argument. There is no computed coefficient expansion, numeric test, sampled parameter, CAS/free-symbol script, source power, checker, solver or new theorem import.

The fixed-A obstruction is an independent elementary polynomial-ring proof. It does not transfer to a different A with the same leading form and does not assume any pending review result. The positive constant-C control is outside the cubic hypothesis and demonstrates the precise limitation rather than a source point.

Allowed metadata only: UTC, existing artifact lease/close/finalize/verify, owned text reads, SHA256/byte pins and existing own-only raised-OPEN extraction. Authored files use apply_patch. No AWS/SSH, external write, mathematical subprocess, additional agent, shared/canonical edit, generic runner or follow-on authority. The full own report is reread before BODY-END; custody is the last metadata write, then IDLE.
