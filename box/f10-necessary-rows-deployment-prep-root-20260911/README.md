# Necessary-row deployment preparation — DISABLED

ROOT first preparation05:56:32.047644270UTC September11; original reserve06:02,
HARD06:05, never reset. This is metadata-only preparatory text, not a worker,
registration, scientific run or source approval.

The five shell templates are exact text derivatives of the corresponding
box/f10-direct-rows-deployment-prep-root-20260911/ files. ONLY substitutions:
directrows -> necessaryrows; direct-rows -> necessary-rows;
DIRECT_ROWS -> NECESSARY_ROWS. No control, cap, native inventory, process,
source/metadata installation, cleanup, authentication or algorithm change.
All placeholders remain unresolved and ROOT-READY-INPUTS.sha256 stays ABSENT.
No template was executed or syntax-tested; no scientific import/test/dummy.

The active source change is the independently reviewed dead Laurent-q branch
deletion. See ../f10-necessary-rows-root-20260911/ROOT-ADOPTED-NECESSARY-ROWS.md
SHA176363939a8aa6861995cd72a99c61f7d04bfeab5a9634f6d3143dcd3d3bd098 and AUDIT28z.
This new necessary-row service retains SAME25rows/guard and all remaining
checks; alternative inverse/pole coefficients are NOT_COMPUTED. No full old
internal-circuit or source-point qualification, no numerical source outcome.

Three source/runtime authors are still live while these templates are prepared;
their bodies/hashes are NOT read or guessed here. Before any release ROOT must:
collect independently completed producer+authority/checker/runtime packets;
verify exact diffs and unchanged two arithmetic engines; collect one combined
source/runtime DELTA FIRST; issue a genuinely new ROOT source-contract and
source vector/authorities, including new authority.py hash; derive seven exact
refusal triples at the actual new source line numbers, installed path and
qualified interpreter formatting; form and verify the thirteen-command packet.
The old source-code authority/receipt/policy may not be relabelled as current.

Prospective administrative basename jc2-r3-necessaryrows-20260911a; exact science
layout /opt/jc2-r3-necessaryrows-20260911a/science (five Python files), sibling
wrapper (three), runtime/run_capped.py and metadata outside working outputs.
The128MiB tmpfs/durable EBS split, UID/GID65534, complete native closure and
source subset,900wall/600CPU/8GiB source profiles,3000wall/2100CPU batch remain.
Physical identity/boot/native facts, actual clocks and complete immutable
installation are unformed. New job f10-source-cone-r3-necessary-rows-v1.
Source schema f10-r3-necessary-rows/v1; metadata identities use necessary-rows.
The exact direct-source graph slot T rejection string is NOT renamed.

No worker until the reviewed offline packet is ready and current fleet/quota/
AMI/network facts are checked. Preserve100GiB EBS and arm exact-ID termination
immediately after allocation. Retire after secured terminal evidence, never
hold idle for review. No capraise, retry, broad farm, new supervisor or rank
client is selected. Last confirmed fleet EMPTY, all18retirement records retain
EBS. Protected infrastructure and retained data remain untouched.

Original five template hashes and exact new-copy checks are recorded in
PREPARATION-PINS.json once this disabled text-only preparation is closed.
No guessed expected traceback, placeholder source hash or defaultsuccess may
be treated as a completed launch gate. Scientific feasibility is UNMEASURED.
