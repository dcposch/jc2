# One finite generic verifier / installed-API batch — DISABLED

STATIC ONLY. No code, syntax, import, arithmetic, fixture, API, matrix or test
was executed. This is the sole provisional software child of the frozen
linear-decision implementation; an adverse parent review quarantines it.
It does not consume an actual source artifact/receipt or source acceptance.

## Binding and exact sequence

Use a NEW ROOT-owned generic-only registration, schema
F10-GENERIC-COMPATIBILITY-DISPATCH/v1. The shipped JSON has enabled=false,
null job/worker/cwd/caps/clocks/package, and no source-acceptance field.
Both the caller and harness reject univariate_acceptance and
normalized_acceptance fields. No ROOT source acceptance is fabricated.

The registered sources are compatibility.py, checker.py, evidence.py,
algebra.py, execution_gate.py, probe.py, dispatch_batch.py, generic.plan.json,
and the unchanged external run_capped.py; also /usr/bin/python3 and ROOT's
separately pinned installed FLINT/native file closure. No solver.py or actual
univariate/normalized/source artifact is staged. evidence.verified() is NEVER
called. evidence.producer_environment() is used solely for the existing
metadata/version/module binding before import. Missing or incompatible
installed API fails closed, with no install/fallback/retry.

Caller sequence: five refusal children (disabled, wronghost, argv, cap,
missing-input), one valid sentinel, one descendant-RSS TERM/KILL dummy,
then ONE compatibility.py mathematical child. Eight CAPRUN dispatches on a
successful whole batch; seven are preflight, one is the generic harness.
The helper's required pinned argv[2] input is generic.plan.json, also used as
the generic admissibility digest. The old actual-source frontier/acceptance
is not smuggled into this generic profile.

Harness argv: compatibility.py AUTHORITY GENERIC_PLAN OUTPUT;
sys.argv length4, complete Python -I -B vector7. It calls unchanged
authorize before every arithmetic/backend/FLINT import and fixture
allocation. Checker calls are IN-PROCESS checker.verify, not production
checker.main. The latter requires actual ROOT source acceptance and is not
used or bypassed as source evidence. Pure generic fixtures lack producer,
input_evidence, ROOT acceptance and production execution metadata.

## Exactly finite designed calls

On complete success: twelve checker.verify calls, five expected positives
and seven negatives; two FLINT fmpq_mat constructors of shape2x5 and two
rref calls;23 explicit Python-level flint.fmpq constructor calls (three
standalone rational checks plus ten entries in each tiny matrix). Internal
native/overloaded operations are not counted or measured. No217x1638 or
217x1856 matrix is constructed and no solver.candidate call occurs.

The two documented tiny arrays are [M|b|I2], with
M=((1,2),(2,4)). For b=(3,6) the exact RREF rows are
(1,2,3,0,1/2),(0,0,0,1,-1/2), pivots0,3. For b=(3,7) they are
(1,2,0,7,-3),(0,0,1,-2,1), pivots0,2. Hand elimination subtracts
twice the first row, then normalizes/eliminates the next pivot. Thus the
target-column criterion is exercised on both tiny cases without solving
the actual matrix. The scalar string checks are0,K,K/2 with
K=9007199254740993. Native installed version/path/hash and exact string,
constructor/index/rref behavior remain future observations, not known now.

## Generic fixtures and exact expected outcomes

Every fi uses the fixed nine row ids and complete dense windows; p is the
entire frozen degree-seven modulus, never a factor. Coefficient strings
are canonical, except the deliberately malformed floating control. The
fixtures exercise rational constant coefficients in B; they do not exhaust
nonconstant B-coefficient or all product-component behaviors.

1. UNIT positive: f1=1, others0,q=r=h0=1; h1=1. Expected generic zero.
2. UNIT negative: h1=2; full217-coordinate identity fails.
3. All-zero-row SEPARATOR positive: all fi=0,q=r=h0=1; lambda is the
   T^0v^0 coefficient functional. Expected generic nonzero.
4. Its negative: lambda multiplied by2; all columns remain annihilated,
   target-normalization fails.
5. Nonzero-row SEPARATOR positive: f1=T,others0,q=r=h0=1; same lambda.
6. Its negative: add the T^1v^0 functional; FIRST column fails with exact
   reason 'dual column annihilation i=0 j=0 ell=0'.
7. Precision positive: f9=K*T^5,others0,q=T^5,r=T,h0=T^4;
   h9=T^20/K. The serialized K and1/K strings are checked after reread,
   then the unchanged checker's nested parser and full identity must PASS.
8. High-window negative: add T^25 to h9; only the extra K*T^30 term
   spoils the equality, proving the high window is actually checked.
9. Rounded canonical-string negative: replace denominator K by
   str(int(float(K))). A real runtime precondition checks K>2^53 and
   int(float(K))=K-1. This remains a canonical exact rational but fails
   the identity, distinct from a parser-type refusal.
10. Float-type negative: use float(K) as denominator, rejected by
    'canonical rational strings only'.
11. Zero-guard positive: all fi=0,q=r=0,h0=1; zero UNIT witness.
12. Zero-guard false separator: lambda constant-functional; all columns
    vanish but lambda*b=0, so target-normalization rejects it.

All fixtures are fresh exclusive JSON objects, labeled GENERIC-VERIFIER-TEST/v1,
reread and compared before the call, hash-bound before/after it. Negatives
must actually differ from their paired positive object and give the exact
ValueError reason; any mismatch stops. No fixtures exist yet. API failure
may stop before fixture generation; no final PASS receipt then exists.
Neither the toy checker statuses nor the final generic receipt represent
an actual source ideal, point, solve, acceptance or counterexample.

## Supervision and prospective caps, NOT authority

caller.diff preserves the exact run/supervision machinery except the
generic plan/admissibility and scope payload. No new supervisor, process
group, runner, bytecode setting or helper patch. The five refusal, valid,
dummy identity/RSS/TERM/KILL checks, full argv, source hashes, fixed stored
cutoffs,15-second admission, post-return check,16MiB inherited file cap,
exclusive outputs and no-concurrent-authority-writer contract remain.
CAPRUN sampling and cleanup are not hard real-time guarantees.

Unmeasured first-cap recommendation ONLY: probe5wall/3CPU/2GiB; dummy
5wall/3CPU/32MiB; the one compatibility child180wall/170CPU/2GiB,
16MiB files, aggregate240wall. ROOT must independently choose actual
caps, exclusive cwd/worker, mathematical/task UTC deadlines and external
stop duty. All template profiles and clocks are null; these recommendations
neither predict performance nor authorize execution. Any cap/refusal or
unexpected result is STOP/NONDECISION, without retry or escalation.

## Remaining review/runtime scope

The parent software gate is pending, not a premise of acceptance. Review
the new harness, exact fixture arithmetic, literal caller delta and caps
plan once at STATIC tier. Installed package/native discovery is ROOT's
later metadata duty. Separately registered execution is then required for
syntax/import/API/control evidence. Passing would only discharge these
finite generic tests; the actual solver and genuine actual-input checker
would remain a separate registered batch and separate result review.
