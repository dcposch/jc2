# Exact reading and source-only boundary

All first11 input hashes matched at13:12:57 before bodies. PINS.json contains
the exact14 current inputs, including three terminal mutator objects released
by ROOT at13:18 and hash-checked before their WHOLE reads. No draft peer body
was opened. No provenance path mentioned inside a report was followed.

WHOLE fresh reads: old dispatch.py/probe.py/CONTRACT, runtime gate,
reconstruction-code gate, actual-result gate, ROOT actual execution report,
authority.py/check.py/check_arithmetic.py; then terminal mutator source,
CONTRACT and report. One aggregate display clipped the actual-result gate;
the whole90-line file was read again without truncation. Other reads were
bounded and ended at EOF. run_capped.py is current-pin verified with explicit
same-byte prior WHOLE reuse at accepted caller/API scope, not a fresh audit.

No actual coefficient artifact was read, copied or parsed. Literal baseline
SHA/size and old producer/arithmetic hashes are metadata from allowed inputs,
not additional bodies. No new gate, worker, live receipt, ledger or corpus.

All source authoring is apply_patch. Metadata Python invocations only generated
copy/diff/pin patches, hashing permitted text; no scientific import, compilation,
syntax, AST, test, arithmetic fixture or source execution occurred. Source text
inspection is not evidence of executable syntax or runtime success.

Own review: complete new dispatcher, unchanged probe through byte-identical
source reuse, disabled registration, contract, diff, input pins and report
read before final marker; no predicate test or runtime control is claimed.

Path incident: the first copy patch used relative paths against /home/ubuntu,
briefly creating ONLY the two same-byte dispatch/probe copies in
/home/ubuntu/box/f10-r2-semantic-runtime-astra-20260910. A failed subsequent
absolute patch exposed it; the two files were recreated at the correct owned
absolute paths and the misplaced copies deleted using apply_patch. No user or
input file was changed. The empty misplaced directory remains disclosed;
no broad cleanup command was issued. Parent was notified before publication.
