# Bounded co-research — conical conductor normality criterion

ROOT September12,2026,13:32UTC. Astra primary co-research, NOT FIRST.
Original reserve13:45UTC / HARD13:48UTC NEVERRESET; finish early if decisive.
Basis0d39df3c9fd69c939a8420c54d03228b9077777d. One scientific report only:
xmodel/conical-conductor-normality-astra-20260912.md.

Hash/read COORDINATION.md first WHOLE, expected
33cfa6106972fe557a95ca81a9a05bb72300a82b2fbb7908915441cf75377597,
then this TASK WHOLE with invitation pin. Exactly these two inputs; no
corpus, prior report, live peer output or linked body. Use existing unchanged
artifact_finalize begin/close/finalize/expected-verify transaction, editing
only its leased partial with apply_patch. Administrative PINS/custody in
this TASK directory allowed. No shared ledger, prior artifact or TASK edit.
No descendants, promotion, new external theorem import or compute.

Independently prove or break the following MANUAL/UNREVIEWED standalone
geometric lemma. Do NOT assume any Keller map makes its hypothesis true.

Let j:A2_C -> Z subset A3_C be a finite birational unramified normalization
of an integral affine hypersurface Z (dimension2). Put
D=j^{-1}(Sing Z)_red. If D is invariant under ordinary scaling
(x,y)->(t*x,t*y) for every t in C*, then D is empty and j is an isomorphism.
If equivalent here, explain D as the support of the conductor of
C[Z] subset C[x,y]. Distinguish support invariance from ideal invariance.

ROOT proposed argument to ATTACK, not accept on authority:
1. A finite unramified map from the smooth normalization locally presents Z
analytically as a union of smooth embedded surface branches, one per fiber
point. One preimage means the map is locally an isomorphism. Supply the
precise finite/unramified or analytic justification; check tacnodes and
unibranch nonnormality, not just normal crossings.
2. Distinct hypersurface branches in C3 meet in curve germs, so the singular
preimage D, if nonempty, is pure1dimensional. C*-invariant reduced curves
in A2 are finitely many distinct lines through0. Call their number k.
3. If k>=2, write j^{-1}(j(0))={0,a1,...,ar}. Each nonzero ai lies on just
ONE conductor line, hence its local D is one smooth irreducible curve.
The origin has k>=2 branches of D. Let Z0,Z1,...,Zr be the corresponding
embedded smooth surface germs at j(0). On each Zi,i>0, the whole pairwise
intersection locus with all other branches has just one irreducible curve
Ci. Any two surface branches have a curve intersection. Thus for distinct
i,j>0 the reduced intersection forces Ci=Cj, and Zi intersect Z0 has the
same curve. If r=1, this conclusion is immediate. Consequently all the
origin branch's intersections have one common curve, contradicting its
k distinct D germs. Handle reducible pair intersections/multiplicities
carefully; unramified rules out internal branch singularity but not tangency.
4. Hence k<=1. If k=1, D=L is an affine line. For a generic point of
C=j(L), singularity implies at least two preimages, all in L; therefore
finite j|L:A1->C has generic degree>=2. But restriction of the immersion
j to L is a polynomial curve immersion. Polynomial Luroth writes all its
coordinate polynomials in one h in C[t]; their derivatives have common
factor h'. No common derivative zero forces h' constant and h linear,
so generic degree1. Alternatively normalize C and justify that the finite
normal-curve quotient of A1 has to be A1, then use its polynomial map.
Do not merely infer that every immersed parametrized curve is embedded:
a nodal curve's polynomial normalization is an immersion of generic degree1.
5. k=0 implies D empty, normal target and j an isomorphism.

If valid, give a complete short proof with precise locality and line-step
justifications. If false, give exact counterexample or earliest irreparable
gap. State what is essential about hypersurface ambient dimension3, the
whole source A2, finiteness and unramifiedness. One meaningful check is the
normalization (t,s)->(t^2-1,t*(t^2-1),s): source A2, nodal hypersurface
v^2=u^2*(u+1), finite birational unramified, nonnormal; its conductor support
t=1 union t=-1 is two parallel lines, not invariant under ordinary scaling.
Verify rather than trust this control. Identity is a positive sanity check.

If the lemma survives, the missing JC2 input is STILL actual conicality or
radial Euler invariance of the singular-preimage support in a finite Keller
graph. No such invariance is an input. Do not claim JC2 or assume a global
action on the graph from a derivation of its fraction field. A consequential
sharpening is welcome only if proved; no control family or automatic farm.
Do not claim literature novelty; ROOT handles primary/history search.

Manual math only. NO scientific/CAS/interpreter/import/AST/test/syntax/helper
execution, network/AWS/SSH, agent or protected-tree operation. Inert text,
hashes, current clock and existing administrative finalizer allowed. NEVER
enter/enumerate/search/read/status/control jc2-lean or its public mirror.
All edits apply_patch only; no redirection/heredoc/tee/shell file writes.
Max report body about10000bytes. Own WHOLE readback and both postpins before
unique BODY-END appended LAST. No charge_basis or canonical OPEN. Final
report/manifest WHOLE, expected verify, custody SHA-FIRST and ALL WRITERS
IDLE with actual UTC before FINAL; no writes after idle.
