# One changed-mechanism strict-nine preparation, batch c

ROOT selection September11 15:36UTC. Failed batch b remains CLOSED.
The actual holder sequencing diagnosis identified post-holder document
composition as the previous failure. CAPRUN-LATEBIND-1 now independently
accepts one pure-data binder that makes those exact substitutions and hashes.
This is a reasoned NEW bounded preparation/observation, not an automatic retry,
clock reset of b, changed runtime algorithm, larger cap or substitute task.
The eventual mathematical consumer remains actual r3 necessary rows and the
accepted rank/identity implication; this nine-phase test itself runs no
successful source producer or checker and can prove no mathematical result.

Original clocks are in ORIGINAL-CLOCKS.json and never reset. At most ONE fresh
c7i.2xlarge worker,100GiB retained-root EBS, only after complete offline
preparation, quota/fleet checks and exact named source/gate pin checks. No
worker exists at selection. No allocation after16:00UTC, no holder after16:12,
holder min(start+120s,16:14), outer TERM16:20/KILL16:20:05, custody16:25,
exact-ID coordinator retirement16:30 or earlier after secured evidence.

ROOT prepares/authenticates all static and fresh worker-bound bytes BEFORE
holder launch. Only actual holder PID/start/invocation, outer/nine leaf pairs,
the immediately checked coordinator timer receipt, explicit metadata decision
and their exact descendant hashes may remain late. Seven non-probe authority
bytes are ready earlier. The candidate helper cannot observe or approve itself.
ROOT separately owns full final readback, physical/native/ACL/namespace and
clock checks, bounded installation, fresh liveness and separate FIFO release.
Registration is absent at admission, frozen only after actual binding.
No source/CAPRUN/dispatcher/probe/mutator algorithm or cap changes.

The FIRST's two external limits are actual obligations: authenticate the
native-list hash as the LIST, not either native JSON inventory; inspect all
prepared text and placeholder-like tokens, not just the helper's uppercase
regex. Keep every candidate with SUMMARY.json. New worker/boot/path/clock
values must come from actual observations/preparation, never historical tests.

Stop on preparation deadline, missing qualification, stale/pole/hash/phase
failure, refusal, unexpected bytes, cap or custody failure. Preserve terminal
evidence, archive/compare/fsync/download/local-pin/fsync before exact retirement;
retained EBS is backup. No replacement worker, holder retry, live code repair,
cap increase, source run or rank/prime/exponent farm follows failure or success
automatically. Success only licenses a separate subsequent decision.

Native Astra owns the bounded nine-file administrative port; ROOT owns this
plan/clocks, registration/authority/holder/admission/launcher bindings and all
eventual external actions. Live ownership belongs in notes.md.
