# Manual negative traces and external qualification checklist

No trace was executed, imported, syntax-checked or compiled.

1. Existing spec/card/unit/registration/binding/outer: all-target absence
   rejects before activation. Existing attempt or any extra evidence entry
   prevents reuse. An O_EXCL collision after the check also refuses.
2. Correctly rehashed approved seed with an old timer activation: pre-arm
   ActiveEnterTimestampMonotonic !=0 refuses. A post-arm timer change fails
   complete paired property equality; never rearm.
3. Seed has a guessed outer_started_utc/card epoch rather than null: early
   null-slot check rejects; only observed activation fills those slots.
4. Rehashed seed changes offsets.outer to121, binding to121, or task above3000:
   fixed integer offset predicate rejects. Activation pushing task beyond the
   fixed independent worker cutoff also rejects; no deadline recomputation.
5. Actual admin outside the selected slice, an extra task child directory,
   a parent cap mismatch, or admin stop mode not control-group: rejects before
   arming. Source does not migrate a process or create a cgroup.
6. Private assembler output path substituted for installed manifest: unsafe
   path/mode or pin/native checks may refuse; ROOT must actually perform the
   separate installed whole-byte/mode/ACL/fsync qualification. Do not claim a
   parser failure establishes installed-manifest equivalence.
7. Native inventory includes metadata directory or future generated file:
   cycle/inventory check refuses before arming. No allowlist expansion.
8. Changed installed spec/card/unit or source pin after freezing: exact raw
   comparison/repeated source hash rejects before start. A changed effective
   property among queried keys rejects; nonqueried mapping remains the explicit
   prior-host qualification, not allegedly covered by this trace.
9. Start(timer) timeout, reload failure, start(outer) timeout or output/fsync
   failure: BIND_STOP, retain partial records/unknown manager-side outcome;
   no retry/cleanup/release bypass. Qualified watchdog and retirement remain.
10. Very fast outer failure: start-returned record reports actual state,
    never a preflight PASS or a promise that launcher/bootstrap were reached.

ROOT must qualify BEFORE supplying a passing seed:

- Actual-code FIRST for this source; unchanged accepted launcher/bootstrap
  bytes and exact science/runtime/source references; no old authority reuse.
- Actual host/DMI/boot/namespaces; interpreter/native startup including this
  administrative stdlib/subprocess usage; tools and administrative transport
  closure; exact loaded-property serialization and effective unit mapping.
- Installed two manifests and canonical env/systemctl FILE bindings; no
  registration-parent/dynamic-file inventory cycle; all source dirs unchanged.
- Fresh task-slice zero-origin history, controllers/resources, root-only
  migration surfaces, admin cgroup placement, loaded service caps, no inherited
  privileged cgroup descriptors, no entrants or concurrent ROOT writer.
- Timer original-activation semantics, outside-slice watchdog execution,
  slice/outer stop propagation and all administrative members' five-second
  shutdown obligation; no ordinary-host hard realtime guarantee.
- Independently active exact worker retirement, externally approved epoch
  window and original offsets. No future host/epoch is selected here.
- Tmpfs/source/stdio/durable layout, quotas and bounded admin record custody;
  terminal ROOT recovery when the strict whole-experiment cutoff prevents
  dispatcher custody. PID1/SSH daemon CPU is excluded from task-owned accounting.

Cheapest next discriminator is ONE different-model actual-code FIRST, then
only a separately ROOT-registered AWS host qualification plus this one
PREFLIGHT_ONLY_9 attempt. Planning window <=120 seconds AFTER the original
epoch is an UNMEASURED cutoff, not expected completion or a runtime estimate.
No seven-refusal calibration replay, science phase, retry or additional
controller is requested. Runtime/host qualification remains OPEN, not ready.
