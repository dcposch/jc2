# r2: cyclic membership reduction and exact cubic source core

## 1. Verdict and charged scope

NEW MANUAL THEOREM, UNREVIEWED. The proposed exact cubic core is proved without
any finiteness hypothesis. The 7-by-105 membership test and algebra-length
bounds are proved CONDITIONALLY on the three highest forms being a homogeneous
parameter sequence. That condition is UNKNOWN here. No mod89 result, actual
coefficient, rank computation, basis, certificate, or source outcome is used.

First action: 2026-09-10 11:09:32 UTC. Exact report and box destinations were
absent. Original fixed cap: 11:16:00 UTC; publication reserve: 11:14:00 UTC.
No clock reset. Basis: 0d39df3c9fd69c939a8420c54d03228b9077777d.
Exactly the ROOT card and four accepted parents are charged. All were pinned
before body use; the card was read WHOLE. Previously completed WHOLE reads of
the four unchanged parents are expressly reused. A redundant aggregate reread
was clipped and is NOT claimed as a fresh WHOLE read. No extra scientific input
was consulted. The accepted transfer is used with its FIRST gate's mandatory
late-operator correction -6, not the producer's printed -18. Only accepted
weights, complete quotient maps, and the faithful scale cover are premises.
ROOT's proposed argument is tested here, not assumed.

## 2. Grading and the conditional highest-form equality

B is the complete accepted degree-seven field over Q. Give Y_i weight i and
z weight five. Put f_h=Psihat_h(Y,1), H_h=Psihat_h(Y,0), h=5,6,7,
and R=B[Y1,Y2,Y3]/(f5,f6,f7). Each f_h is homogeneous modulo five,
of cyclic degree h, since its monomial Y-weights differ by multiples of five.
Hence R is genuinely Z/5-graded, not merely partitioned by a proposed basis.

For THIS SECTION and the finite conclusions below assume H5,H6,H7 are a
homogeneous parameter sequence in B[Y]. Equivalently their only geometric
common zero is the origin. The polynomial ring is Cohen--Macaulay, so this
sequence is regular. In particular each indicated highest form is nonzero.

Here is why the actual lower terms introduce no hidden highest relations.
Adjoin t of weight one and homogenize f_h to

    F_h(Y,t)=t^h f_h(t^-1 Y1,t^-2 Y2,t^-3 Y3).

This is a polynomial, and F_h(Y,0)=H_h. The homogeneous sequence
(t,F5,F6,F7) is regular because its quotient by t has the regular sequence
(H5,H6,H7). Permutability of homogeneous regular sequences in this positively
graded polynomial ring implies t is a nonzerodivisor modulo (F5,F6,F7).
One can check permutability at the homogeneous maximal ideal; homogeneous
kernel modules then vanish globally. Thus (F5,F6,F7) is t-saturated.
After inverting t it is the homogenization of (f5,f6,f7); saturation shows
that it is already the entire homogenized ideal. Reducing at t=0 proves

    gr R = B[Y]/(H5,H6,H7)                         (1)

for the weighted filtration. This proves equality for the actual deformation,
not merely a dimension bound from the three displayed leading terms.

Its Hilbert series is

    (1-t^5)(1-t^6)(1-t^7)/((1-t)(1-t^2)(1-t^3))
      = S5(t) S7(t) (1-t+t^2),  S_n=1+...+t^(n-1). (2)

Indeed (1-t^6)/((1-t^2)(1-t^3))=(1-t+t^2)/(1-t).
Evaluation at t=1 gives 35. Summing coefficients by degree modulo five,
S5*t^a=S5 in Z[t]/(t^5-1), so (2) becomes 7*S5. The filtration is
compatible with the cyclic direct sum, and finite filtration increments in
each residue class sum to its dimension. Therefore dim_B R_j=7 for EACH j.
No roots of unity in B are needed for this grading argument.

## 3. All fifteen generators and the exact 7-by-105 criterion

Let J be the ideal of R generated by all fourteen nonconstant low rows

    K1_i(Y,1), i=1,...,6, degree 14-i modulo 5;
    K0_i(Y,1), i=1,...,8, degree 16-i modulo 5,

and the fifteenth row L1-U0*L0, degree 19 modulo 5. Here
L0=K0_0(Y,1), L1=K1_0(Y,1), U0=U(Y,1), with cyclic degrees 1,4,3.
The mixed row's degree is 19, not a homogeneous-weight-14 replacement.
Write Q=R/J, J0=J intersect R0, and t0=L0^5 in cyclic degree zero.

For a homogeneous generator g of degree d, projecting a general coefficient
times g to degree zero retains exactly the coefficient's R_-d component.
Consequently J0 is the B-span of b*g, using a basis of R_-d for EACH of the
fifteen generators. Under section 2's hypothesis each such basis has seven
elements, giving at most 105 columns in the seven-dimensional R0. Zero or
dependent columns remain legitimate; this statement does not delete a row.

Let C=Q[L0^-1]. Since L0^-1=L0^4/t0, this is also Q[t0^-1]. Localization
is zero iff some power of t0 is zero in Q. That power is in Q0; equivalently
t0 is nilpotent in Q0=R0/J0. Under the finite hypothesis dim_B Q0<=7.
Multiplication by a nilpotent element on a vector space of dimension at most
seven has seventh power zero, including the zero-ring case. Thus

    C=0  iff  t0^7=0 in Q0  iff  L0^35 belongs to J0. (3)

The reverse implication follows directly by localization. Nilpotents are
essential to this argument and are NOT replaced by tests on field points.
In (3), J0 denotes an actual subspace of R0; its element is represented after
reduction modulo the three f_h. No leading-only membership is substituted.

Using a cyclic basis, this is a 7-by-105 B-linear column-space test, plus its
target vector. Over Q, expanding every B scalar in the full seven-element
basis gives 49 rows and 735 columns, not seven rational rows. The unreduced
35-by-525 test uses all seven-dimensional degree blocks for each generator;
the new test projects only the relevant source coefficient block. No basis,
reduction matrix, actual rank, coefficient height, or runtime is supplied.

## 4. Exact core identities WITHOUT the finite hypothesis

Everything in this section uses only the grading and the accepted full cover.
Define E=(R0/J0)[t0^-1]. Localization by the homogeneous degree-zero t0
commutes with taking each cyclic component, so C0=E. Every element of C_j
is uniquely e*L0^j with e in E, because multiplication by the homogeneous
unit L0^j is a bijection C0 -> C_j. Therefore

    C = E[T]/(T^5-t0), T maps to L0,                (4)

with the free E-basis 1,T,T^2,T^3,T^4. Surjectivity follows componentwise;
injectivity follows from the direct sum and the unit in each component.
This works with arbitrary nilpotents and even the zero ring.

The accepted complete common cover gives

    D=A[w]/(w^5-s)=C[w]/(w^3-L0),
    Xi=w^(2i)Yi, s=w^5,

where A is the ORIGINAL nineteen-row source algebra over B, with s inverted.
Combining (4) and w^3=T eliminates T exactly:

    D = E[w]/(w^15-t0).                            (5)

Since t0 is a unit, w is a unit; Laurent inversion imposes no extra relation.

To descend correctly, use the COVER grading, distinct from the original
cyclic grading of C. In A[w]/(w^5-s), A has cover degree zero and w degree
one. Thus Yi=w^(-2i)Xi has cover degree -2i=3i modulo five. Multiplication
by three permutes the original five degrees, fixing degree zero; E still
has cover degree zero, and L0 has cover degree three, consistent with w^3.
The free A-basis 1,w,...,w^4 proves D_cover,0=A, without a geometric
invariant-point argument or a roots-of-unity assumption.

On the right of (5), its free E-basis 1,w,...,w^14 has cover-degree-zero
part exactly E-span(1,w^5,w^10). Setting s=w^5 therefore gives the NEW
exact ring identity

    A = E[s]/(s^3-t0),                              (6)

free of rank three over E. This is stronger than sharing a faithful cover,
but it is NOT A=C and does not set s or L0 equal to one.

For explicit source coordinate maps put epsilon_i=Yi*L0^-i in E. These
elements belong to degree zero and can be written Yi*L0^(5-i)/t0 for i=1,2,3.
Then the forward source map in (6) is

    Xi -> epsilon_i*s^i,  s -> s,  s^-1 -> s^2/t0.  (7)

The inverse E-map can also be written without adjoining a root: represent
e by p(Y)/t0^n, where every monomial Y^a of p has weight 5k(a). Send it to

    sum_a p_a * X^a * s^(-2k(a)-3n) in A.           (8)

Well-definedness follows by the injective monic inclusion A -> D and the
accepted cover maps, which turn (8) back into e term by term. Likewise (7)
and (8) are inverse by Xi=w^(2i)Yi and L0=w^3. No coefficient division,
choice of field embedding, or selected fifth root is used.

All seventeen homogeneous source rows transport by their units w^(2k).
The two low rows transport to w^32(L0-w^3) and w^28(L1-w^3 U0);
the latter is retained through
L1-w^3 U0=(L1-U0 L0)+U0(L0-w^3). Thus all nineteen rows, including BOTH
low rows, vanish in both directions. The guard is omega=W*t5*s^8, still
a unit, and the accepted inverse band maps, Euler mate, inverse-polynomiality,
and fixed gauges read back unchanged. Injectivity A -> D ensures these are
source-ring identities, not merely statements after losing information.

## 5. Conditional lengths, controls, and the precise remaining prerequisite

Restore section 2's UNKNOWN parameter-sequence hypothesis. Then E is a
localization of the finite-dimensional algebra Q0, hence a quotient/direct
factor of it as a B-module; its dimension e satisfies 0<=e<=7. Equations
(4),(6) give dim_B C=5e and dim_B A=3e<=21, hence dim_Q A<=147.
These are algebra lengths. Individual residue-field degrees over B are only
bounded by 21; they need NOT be divisible by three. For example E=B,t0=1
gives a degree-one point s=1 of the rank-three algebra B[s]/(s^3-1).
This is a changed abstract object, not a claimed source point.

Further changed-object controls:

- If L0 has cyclic degree zero, a unit L0 does not identify the five pieces:
  a trivially graded nonzero field with L0=1 has only degree-zero part.
- Removing one of the fifteen generators can change J0 and its membership test;
  keeping all 105 slots prevents silently replacing the source by a subideal.
- Omitting localization retains forbidden L0=0 components. For a nilpotent
  L0, the correct localization is zero, not the original nonzero quotient.
- The weight-compatible triple H5=Y1^5,H6=Y1^6,H7=Y1^7 is not regular and
  leaves Y2,Y3 free. The degrees 5,6,7 alone do not imply 35 or seven.
- Grouping columns by labels without an actual cyclic basis does not produce
  the stated matrix. A basis and complete reduction/read-back are required.

Smallest remaining prerequisite for the 7-by-105 test and finite bounds:
prove the ACTUAL H5,H6,H7 parameter-sequence assertion, then construct and
independently check the actual graded basis and all fifteen multiplication
blocks plus the t0^7 target. None is executed or authorized here. Equation
(6), in contrast, already follows from the accepted algebraic interface and
does not await that parameter-sequence proof. No source point, UNIT certificate,
all-r statement, or JC2 conclusion is asserted.

## 6. Own-only publication checks

OPEN: the precise unknown parameter-sequence assertion and subsequent actual
membership computation above; no new research or execution lane is opened.
COLLISIONS: EMPTY, exact owned destinations absent before begin. No frozen or
shared object changed. No scientific subprocess, code, syntax/import/test/CAS,
network, corpus search, process inspection, or extra scientific input was used.
The existing transaction utility is used solely for documentary publication.
Terminal custody pins the five inputs and every owned file except itself.

Own WHOLE body/readscope review and own-only collision check completed before
the completion marker. All scientific work stopped before publication reserve.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `11461`.
- Body SHA-256:
  `0861ca9d9e45f11217c9a49268516ba0187d72e8ff39041f2327673a9c2ba46f`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
