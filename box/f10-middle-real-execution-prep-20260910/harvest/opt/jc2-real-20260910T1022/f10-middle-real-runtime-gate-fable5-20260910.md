# f10-middle-real-runtime-gate-fable5-20260910 — FIRST Fable5.1 STATIC gate: new real-certificate runtime wrapper (dispatch.py + probe.py)

status: UNSEALED (adapter seals; no Seal section and no charge_basis authored here; the standalone BODY-END marker will be the last line once all sections are complete or an explicit GAP is recorded)
lane: f10-middle-real-runtime-gate-fable5-20260910
first_action_utc: 2026-09-10T10:04:35Z (absence check and sha256sum before any body)
stop_utc: 2026-09-10T10:22:35Z (first+18 min, earlier than 10:23:00Z); reserve 10:19:35Z; independent ROOT TERM 10:23:00/KILL 10:23:05; never reset
mode: manual text/math/hash only; ZERO subprocess/CAS/Python import/AST/compile/syntax/test/fixture; no network/AWS/SSH/proc/Git/agents/corpus/shared/protected/other-lane/live files; every statement is a hand reconstruction from the 15 charged snapshots
owned: xmodel/f10-middle-real-runtime-gate-fable5-20260910.md, box/f10-middle-real-runtime-gate-fable5-20260910/input_custody.md (both absent at 10:04:35Z); all writes apply_patch only

## 0. Charged inputs and read attestation

All 15 immutable snapshots in /tmp/jc2-lane.8QaLZT/inputs were hashed BEFORE any body was read; `sha256sum -c` against the charged basename pins returned OK for all 15 (rc=0; the full live output is reproduced in the owned input_custody.md). Read WHOLE as text after the match: runtime report 36494281…d532 (30 lines), its artifact JSON 41b99ec5…f59f, dispatch.py f2db01a4…5e6e (471 lines), probe.py d0df7ba6…39f4 (64 lines), CONTRACT.md 13ec9624…61fe, DISABLED-REGISTRATION.json f0c1d7fc…9135, READ-SCOPE.md 8702c132…2878, authority.py 45871331…59d73d (133 lines), ROOT-CARD.md 43643a6e…015b, ROOT-STARTUP-DELTA.md 819d5e70…c192, ops/run_capped.py 4435279d…96c2 (all 1198 lines, CLI + lifecycle + telemetry), code report c3111895…7785e9, and the prior code gate d3b8c092…932. EXCERPT ONLY, exactly as charged: producer.py 44859af3…ee0dd2 lines 1..12 and 246..273; checker.py 9369f0f3…8043c lines 1..14 and 280..313 (hash WHOLE, body excerpt; their accepted mathematics is not re-reviewed). The whole-file dispatch.py display clipped in the tool window and was recovered by three bounded line-range reads (1..160, 161..320, 321..471) so nothing was left unread. No provenance was followed; no file outside the 15 was opened; nothing was executed.

## A. External ROOT authority, argv/host/ns/cgroup binding, root/nonroot separation, pinned science: CONFIRMED with one precision GAP

`Batch.__init__` (dispatch.py 119-199) refuses before any payload unless: euid 0 on linux; the registration is a canonical, non-symlink, root-owned regular file with no write bits at all (`immutable(...,True)`), mode exactly 0444, ≤64 KiB, strict-pairs JSON, schema `f10-middle-real-runtime/v1`, enabled True, literal jobtag, exclusivity flag, caps == JOINT, profiles == PROFILES, preflight 180; positive int uid/gid (bool excluded by `type(...) is int`); DMI vendor 'Amazon EC2' and `i-hex` board_asset_tag equal to the registered instance; hostname; the eight exact file roles, each root-owned in root-owned no-0o022 ancestry (sources/probe/dispatcher/caprun additionally 0o222-free); python == realpath(sys.executable); dispatcher == resolved `__file__`; outer argv `[python,-I,-S,-B,dispatcher,--registration,REG]` equal BOTH to the registered vector and to `/proc/self/cmdline` (so flags are compared, not inferred); five sibling sources in source_root and cwd == source_root; exact tmpfs children; durable target; owned cgroup `0::` membership and PID namespace link; two offset-explicit future deadlines; native manifest schema/bounds and its pin; every role path pinned; CAPRUN pin equal to the literal 4435279d…96c2 (the charged run_capped.py hash); and exactly one pin per REQUIRED basename with the accepted hash (8 science reports, 3 sources, 2 reports). The 8-entry SCIENCE dict is byte-identical to authority.py's (own text diff). The disabled template dies at 'disabled root registration' (schema/enabled) before any `r[...]` indexing; malformed registrations raise KeyError/TypeError, all caught by `except Exception` into STOP_NONDECISION. Root/nonroot separation is real: the dispatcher is uid 0, every child is `setpriv --reuid U --regid G --clear-groups --no-new-privs -- python -E -s -S -B SCRIPT …`, and the valid probe's sentinel must report the registered uid/gid. CONFIRMED.

GAP (precision, one-line fix, not blocking): lines 196-198 bind the accepted hashes to *some* pin whose basename matches, not to the role path. If ROOT pinned two `producer.py` paths (one accepted, one modified) the count is still 1 and `files['producer']` may be the modified one; the checker receipt's `code` dict is never compared either. Smallest fix: for the producer/checker/authority roles require `self.pins[str(self.files[role])]==REQUIRED[name]` (as line 195 already does for caprun) and in `execute()` require `result['code']` to equal the three accepted source hashes. What the dispatcher mechanically enforces is absence of write bits, root ownership, canonical paths and hash equality; it does NOT enforce presence of read/execute bits, that source_root lies outside the mount, that its own stdout/stderr fds are on the mount, a minimal child environment (only PYTHONPATH/LD_PRELOAD/LD_LIBRARY_PATH are cleared; LD_AUDIT etc. inherit), or the completeness of the native closure. Those remain ROOT provisioning, exactly as CONTRACT.md states.

## B. Authority API integration, five refusals, sentinel, wrong-job CLIs, nine preflights: CONFIRMED; accessibility GAP for ROOT

Hand-traced through the unchanged `authority.authorize` (133 lines, whole) for each mutation in `Batch.authority`: 'disabled' → line 71 'disabled or mismatched registration'; 'wronghost' (hostname+'-WRONG', instance intact) → line 79-80 'registered physical identity mismatch'; 'argv' (script_argv+['WRONG'] vs sys.argv, which equals `[SCRIPT,…]` because the script path is passed literally and cwd == source) → line 81-82 'cwd or full script argv mismatch'; 'caps' (rss_bytes 1) → passes executable (realpath of the setpriv-exec'd python == files['python']) and SCIENCE, fails line 88-90 'caps mismatch'; 'missing' (producer pin deleted) → deadline tz-aware because the dispatcher forced '+00:00', pins dict ok, required order authority.py then producer.py → line 104-105 'required input pin mismatch'. probe.py prints `'REFUSAL: '+str(exc)` to stderr and exits 2, so `err == 'REFUSAL: '+reason+'\n'`, stdout empty, payload absent, CAPRUN NORMAL_EXIT/2: every literal in `execute()` line 355-356 matches authority.py line by line. Valid probe: authorize → postcheck → `emit` 'xb' 4096 with `identity()` (pid, pgid, uid, gid, start ticks index 19 like CAPRUN, ns link); the dispatcher requires uid/gid == registered. Wrong-job CLIs: both `main()`s call `authorize` at lines 252/287 OUTSIDE their inner try; `tag != JOB` raises at authority line 63-64 before any registration/input stat, caught by the `__main__` handler → stderr exactly 'INCONCLUSIVE: wrong job or platform', exit 2, no output; the checker's absent `frozen/startup-absent.input` is never touched. Sequence in `execute()`: 5 refusals, valid, startup-producer, startup-checker, dummy = nine CAPRUN preflights, then producer (math), then checker only after `status=='CANDIDATE'` — eleven maximum, all before mathematics. `-S` does not remove the script directory from sys.path (that insertion is interpreter path setup, not site.py), so the sibling `import authority` still resolves; every other import is stdlib. No fixture or locally executed control exists anywhere. CONFIRMED.

GAP (ROOT prerequisite, mechanically unenforced): `immutable()` and the directory checks test only `not mode & 0o022`; nothing requires o+rx. "root-only" authority_dir/frozen_dir, a 0700 `/root`-style source ancestry, a 0700 mount root or a 0640 native manifest/report pin would pass every dispatcher check yet be unreadable by the setpriv uid. The first refusal probe would then print 'REFUSAL: [Errno 13] Permission denied: …' and the batch STOPs at 'exact refusal/sentinel absence' — fail-closed, never a false positive, but a wasted registration. ROOT must provision 0755 mount/authority/frozen/source ancestry and o+r on every pinned file (authority.py hashes ALL pins, including setpriv, python, caprun, the reports and the native manifest, as the science uid). Smallest mechanical fix: add `need(st.st_mode & 0o005 == 0o005)` for directories on the science-traversed paths and `& 0o004` for pinned files.

## C. CAPRUN caller compatibility, telemetry, identity, dummy, cgroup quiet: CONFIRMED with two scheduling notes

Interface: `--wall-seconds/--cpu-seconds/--rss-bytes/--rss-sample-seconds 0.05/--term-grace-seconds 1/--stdout-file/--stderr-file/--telemetry-file/--cwd -- CHILD` matches `parser()` (run_capped.py 143-177); labels are unique so the O_EXCL outputs never collide; `--cwd` is the canonical source so telemetry `cwd` equals `str(self.source)`; the leading `--` is stripped at 1186-1187. Every telemetry key the dispatcher reads exists in the `telemetry` dict (713-780) and its terminal updates: schema, argv_sha256 (identical construction: json.dumps ensure_ascii=False, separators (',',':'), +'\n'), argv_count, runner_exit_code (== CAPRUN's process exit, 1177), caps.wall_seconds (float 5.0 == int 5 in Python), cpu_seconds, rss_bytes, pid == pgid (new session, 357), start_identity, status/resource/child_returncode, stdout/stderr path/sha256/bytes (hash_opened_output), termination.term_sent/kill_sent/leader_reaped/cleanup_complete/group_live_before_reap, identity_checks stages 'before-term'/'before-term-send' (signal 15) and 'before-kill'/'before-kill-send' (signal 9) with observed_pid/pgid/start_identity, max_observed_group_rss_bytes. Child argv capture reads CAPRUN's `task/<pid>/children`, skips ps helpers (pgid ≠ pid) and the pre-exec setpriv cmdline, records `python-live.json` only when cmdline == py, and post.json carries `actual_python_live_observed` when the child finished first — live vs finished is explicit. Leader-reaped check: `/proc/<pid>` gone or start identity differs. Dummy: parent writes event 1, forks, child ignores TERM and writes event 2, pipe handshake, parent writes event 3 and `_exit(0)`, orphan touches 64 MiB then writes event 4 and pauses; the shared file description serialises the four appends. CAPRUN keeps the zombie leader as identity anchor (855-871), samples the orphan's RSS > 32 MiB → rss_cap → validate (zombie stat still carries pgid/start) → killpg 15 → 1 s grace → validate → killpg 9 → quiet → reap → RESOURCE_CAP/rss, exit 125. Every dispatcher assertion at 373-395 follows. `group_quiet()` (cgroup.procs of the registered v2 cgroup, zombies tolerated) runs before telemetry, stream hashes, payload or receipt reads (316 precedes 329-348 and 400/410). CONFIRMED.

Scheduling notes (fail-closed STOPs, not false positives, not redesign): (1) event 4 is written after the RSS crosses the cap, so it relies on the 1 s TERM grace exceeding the event write+fsync latency — true unless the host is pathologically loaded; (2) a D-state or unreapable process yields CAPRUN cleanup_complete False, dispatcher 'owned cgroup still has nonzombie child', then STOP_CUSTODY_INCOMPLETE — ROOT's outer stop remains required. Cleanup hole: none found beyond CAPRUN's accepted bounded-wait limits.

## D. Fixed 360/330/2 GiB/16 MiB envelope and time accounting: CONFIRMED; one liveness GAP

JOINT/PROFILES are literal and must equal the registration; CAPRUN gets producer 300/279, checker 45/49, so RLIMIT_CPU hard = soft+1 sums to 280+50 = 330. `math_begin` and `math_cpu_start` (RUSAGE_CHILDREN) are stored once at the producer's admission and never reset; each admission needs `wall+15` within both `360−elapsed` and `absolute−now`; the live loop raises at `before+wall+15` or at the absolute deadline (SIGTERM to CAPRUN, ≤15 s wait, typed STOP either way); post-return requires now<absolute, elapsed≤360 and accounted children CPU ≤330; preflight has its own 180 s monotonic aggregate from `self.begin` with per-process 5/3 caps. No retry, no cap increase, no second formation exists in the code. RSS is CAPRUN's sampled group RSS and RLIMIT_FSIZE 16 MiB is only supplementary, exactly as described. CONFIRMED.

GAP (liveness): the ≤330 check charges RUSAGE_CHILDREN, which includes every `ps -axo` CAPRUN spawns at 0.05 s intervals — up to ~6,900 invocations over the 345 s of science wall. At even 5 ms CPU each that is ≥35 s charged against 330, so a producer that legitimately uses ~270-279 s CPU can be STOPped as 'conservative aggregate CPU including wrappers exceeded' although both RLIMITs held. Likewise the checker admission (needs 60 s left) makes the producer's effective window ≈297 s, not 300. Both are acknowledged in CONTRACT.md as conservative, but ROOT should expect a NONDECISION rather than a certificate if the producer runs near its cap. Smallest options, both ROOT-registered rather than code edits here: raise `--rss-sample-seconds` in the registered commands, or report the conservative figure without gating on it since the per-process hard limits are the enforcement.

## E. Tmpfs aggregate cap, exclusive writes, freeze/move/hash, schema/status, receipt binding: CONFIRMED

Lines 151-168 require writer/frozen/authority to be the three distinct direct children of the registered mount, the mount to be exactly a tmpfs mountinfo record with `f_blocks*f_frsize ≤ 16777216`, root-owned; every phase stream, CAPRUN stream, telemetry, live/pre/post record, authority and payload path is under it; `revalidate()` re-checks mount identity, type and capacity after every run. Every write is O_EXCL (`write_json`, CAPRUN outputs, `authority.emit` 'xb', the probe's dummy fd, the durable copies). After the producer: size ≤ LIMIT−4096, `read_json` reads only `schema`/`status` from the parsed JSON (a full parse, no arithmetic, no int conversion of coefficient strings, no rewrite), `os.rename` within the tmpfs to the absent `frozen/certificate.json`, chown root, chmod 0444, frozen dir 0555, hash equal across the move, and only then a separate 0444 checker authority whose pins add the frozen input (`authority.authorize` re-pins it via `regular(input_path)` and the checker's `input_sha256` must equal `before_sha`). Non-CANDIDATE → 'producer not a candidate; no checker run' STOP. One producer, one conditional checker. The receipt ceiling min(4096, LIMIT−len(raw)) is 4096 whenever the payload passed its cap, matching the dispatcher's 4096 read. Source-outside-mount is not mechanically enforced (a root-owned source on the tmpfs would pass) but only consumes the bounded capacity. CONFIRMED.

## F. Durable EBS custody: CONFIRMED with two small GAPs

`durable_copy` runs after `execute()` on success or on any caught STOP, requires group quiet and now<task_end, creates the absent durable dir (parent root-owned, ext4/xfs mountinfo type, registered st_dev integer), walks the mount without following links, refuses symlinked dirs/files, copies each regular file with 'xb', fsyncs, chmod 0444, compares hashes, caps the inventory at 200, writes CUSTODY.json (result, runs, budget, replica flag) and fsyncs every durable directory bottom-up. Success path prints nothing after the copy, so the copied outer stdout is not mutated. Any exception → single stdout line `STOP_CUSTODY_INCOMPLETE` with `root_terminal_cleanup_and_capture_required`, exit 2; no deletion, unmount, worker stop or result acceptance exists anywhere. CONFIRMED.

GAPs (non-blocking): (1) the durable directory's *parent* is never fsynced, so the new directory entry itself is not crash-durable — add one `os.fsync` of `self.durable.parent` after the loop; (2) the outer stdout/stderr location is a ROOT duty the dispatcher does not verify — smallest check `os.fstat(1).st_dev == mount st_dev` (and fd 2). ROOT custody recovery duty: any external TERM/KILL of the dispatcher (there is no handler), a Batch that never constructed (no copy is attempted), a live group, a full tmpfs, or >200 files all leave the working mount uncopied and require ROOT's bounded terminal capture before any unmount.

## G. Text/liveness, schema, literal APIs, counts: CONFIRMED (no execution, no AST)

Hand-read every statement of dispatch.py and probe.py: nested conditional expressions (260-262), one-line try/except (311-312), `max(matches)[1:]` tuple compare (207), bytes `json.loads` with pairs hook, `iter(lambda: read, b'')`, `Path/str` mixing in os.walk, `os.fspath`-accepting os calls, `resource.getrusage` fields, `statvfs` fields, `shutil.copyfileobj(src,dst,1048576)`, `fromisoformat` with '+00:00' — all real stdlib APIs with the used signatures. /proc parsing uses the same post-comm field indices as CAPRUN (state 0, pgid 2, start 19). `canonical()`'s regex excludes `\`, `+`, `:`, spaces, so ROOT must avoid systemd `\x2d` escapes in the cgroup path and pin merged-usr targets by resolved path (`/usr/bin/ps`, `/usr/lib64/ld-linux-x86-64.so.2`); `durable_device` must be the integer `st_dev`; `/proc/self/cgroup` must be pure v2 (`0::` only). `self.quiet` is assigned but never read (cosmetic). Bounds: 11 labels ≤ 11 registered commands; ≤11 files per run ×11 + certificate + 2 outer streams ≈ 125 ≤ 200; CUSTODY.json ≈ 30 KiB ≤ 64 KiB; pre/post/authority records a few KiB. Concrete blocking source bug: none found. Everything above is unexecuted text reasoning.

## Verdicts, smallest fixes, static acceptance scope

| item | verdict |
|---|---|
| A external authority, full argv/cmdline, EC2/host/ns/cgroup, root/nonroot, pins, no startup break | CONFIRMED; GAP role-path pin binding + receipt `code` (one-line fixes) |
| B five literal refusals, sentinel, both wrong-job CLIs, nine preflights before math | CONFIRMED; GAP o+rx/o+r accessibility is ROOT-provisioned, not enforced |
| C CAPRUN interface/telemetry/identity/argv capture/dummy/quiet-before-readback | CONFIRMED; two fail-closed scheduling notes |
| D 360/330/2 GiB/16 MiB, monotonic+UTC, 300/279 + 45/49 (+1 hard), 180 preflight, no reset/retry | CONFIRMED; GAP ps-CPU charge and 297 s effective producer window (liveness) |
| E tmpfs aggregate cap, exclusive writes, freeze/move/hash, schema/status, receipt binding | CONFIRMED |
| F durable copy, fsync, manifest, no post-copy stdout, typed STOP_CUSTODY_INCOMPLETE | CONFIRMED; GAP parent-dir fsync, outer-stdout location check |
| G Python text/liveness, literal APIs, counts | CONFIRMED |

REFUTED: none. Blocking static code defect: none. Acceptance is STATIC ONLY. Exact ROOT prerequisites before any registered run: 0755 (o+rx) ancestry on mount/authority/frozen/source and o+r on every pinned file; source_root and native manifest outside the tmpfs; dispatcher stdout/stderr on the tmpfs; canonical (resolved, escape-free) paths for cgroup, ps, loader; integer `st_dev`; pure cgroup v2 unit; all eleven full command vectors and the outer vector with the resolved interpreter as argv[0]; a minimal environment; offset-explicit deadlines leaving durable-custody time; native manifest including stdlib pyc/ZIP/ELF closure; installed setpriv behaviour. The actual nine-preflight regression is still owed and is the only evidence that will convert these prospective statements into observed ones. No coefficient, certificate, nonresonance, source, full-F10, all-r or JC2 claim is made or implied.

## OPEN(S) RAISED

None. No new canonical OPEN ID; the producer's two task-local pending quantities stand: (1) this FIRST different-model static gate, now delivered with the A/B/D/F GAP items; (2) one separately ROOT-registered fixed batch with terminal custody, not authorized here.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly xmodel/f10-middle-real-runtime-gate-fable5-20260910.md and box/f10-middle-real-runtime-gate-fable5-20260910/input_custody.md, both absent at first action; apply_patch only; no Seal, no charge_basis, no source edit, no fixture, no subprocess, no registration, no execution.

## Completion

Authoring attestation: both owned files were written by apply_patch only (skeleton 10:06:03 UTC, custody file 10:14:59, sections A–G through COLLISIONS 10:16:45, this section afterwards); no Write/Edit tool, no shell redirection to any file, no helper or source change, no execution of any kind. Own WHOLE read of this report (74 lines before this section) and of the custody file completed 10:16:54 UTC with a standalone-marker-absence check (zero `<!-- BODY-END -->` lines in both files; the status line names the marker in prose only) and a 64-hex-token audit of both owned files against the live sha256sum output of the 15 inputs (zero foreign tokens; the 15 full tokens live in the custody file, this report carries abbreviated forms plus the literal CAPRUN pin, which is one of the 15). Read scope: 13 inputs WHOLE, producer.py/checker.py hash-WHOLE body-EXCERPT as charged, the clipped dispatch.py display recovered by three bounded reads. Raised-OPEN check (none) and collision check (EMPTY) precede the marker and the 10:19:35 reserve. Verdict summary for a reader of this section alone: no blocking static code defect; acceptance STATIC ONLY with the ROOT prerequisites listed under Verdicts; smallest recommended (non-blocking) fixes are the role-path pin binding and receipt `code` comparison (A), the o+rx/o+r accessibility check (B), the durable parent fsync and outer-stdout location check (F); the ps-CPU charge (D) is a registered-command choice. No follow-on, computation, registration, seal or charge_basis is authored. The marker below is the only standalone marker in this file and nothing follows it. All writers idle at the marker.

<!-- BODY-END -->
