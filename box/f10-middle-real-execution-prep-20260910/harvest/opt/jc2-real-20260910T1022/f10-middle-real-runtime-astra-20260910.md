# F10 middle real-certificate runtime: disabled static preparation

STATIC IMPLEMENTATION COMPLETE / UNEXECUTED. Delivered one fixed dispatcher, a no-math authorization/descendant probe, a disabled registration schema, and byte-identical copies of the three accepted scientific sources. No worker, registration, startup, import, syntax/AST/compile/test, fixture or scientific execution was authorized or performed. No certificate result or measured performance is claimed.

First action 2026-09-10 09:40:13 UTC; unchanged controlling cap 10:02:13, publication reserve 09:59:13. Targets were absent before begin. Card plus seven original inputs, and the later narrow startup delta, are current-pinned in PINS.json. Read scope is exact in READ-SCOPE.md; no provenance/native closure/corpus/live lane was consumed.

The fixed sequence has nine preflight invocations: five actual authority refusals, valid sentinel, wrong-job startup through each real science CLI, then the same-PGID orphan/RSS/TERM/KILL dummy. Only full preflight success permits one producer and, only after a normal candidate, a fresh independent checker. There are no twelve mathematical corruption runs here; their accepted specification remains deferred. Accepted science files and CAPRUN remain unchanged.

The dispatcher requires external root-owned0444 authority, exact EC2/host/namespace/cgroup and trusted root-owned source ancestry, positive nonroot UID/GID through pinned setpriv, and every complete argv/source pin. Science/probe flags are -E -s -S -B; sibling-free dispatcher/CAPRUN use -I -S -B. Full flags are compared, not inferred from sys.argv. Environment injection paths are cleared. A separately pinned native manifest is verified file-by-file before and after each child; the eight actual science files and both implementation/gate reports remain inline pins in the <64KiB per-operation registrations. Native closure completeness and installed API behavior remain ROOT prerequisites, not inferred here.

Authorities are written exclusively, flushed and hashed BEFORE launch. Producer output is moved to a root-owned0444 file under a0555 parent only after exact cgroup quiet, then pinned in a separately written checker authority. This preserves the accepted immutability prerequisite without editing its code. Whole certificate mathematics is performed only by the independently accepted checker, never by dispatcher metadata parsing.

The working-output bound is a ROOT-provisioned dedicated tmpfs whose total statvfs capacity is at most16MiB; all working payloads/logs/telemetry/authority records are on it. Per-file FSIZE is only supplementary. Terminal-only byte-for-byte EBS custody copies, hashes and manifest are made to an independently registered absent durable directory, including STOP where possible. The replica budget is distinct from scientific working outputs. All nested directories are fsynced; successful custody is followed by no stdout mutation. There is no deletion, unmount or worker stop. Incomplete cleanup or copy is STOP_CUSTODY_INCOMPLETE for ROOT recovery, not evidence.

The fixed joint science budget is360wall/330CPU/2GiB/16MiB with one stored monotonic start and absolute ROOT deadline. Profiles are producer300wall/279softCPU and checker45wall/49softCPU, reserving CAPRUN's +1 hard allowance per process within330; +15 admission/post checks remain. Conservative CPU accounting includes CAPRUN/ps overhead, with dispatcher self CPU separately reported; exact science-only CPU is not fabricated. Preflight has its separate180wall budget and5wall/3CPU phases. All caps and overshoot limitations are prospective/unmeasured, with no retry/escalation. ROOT must provide independent systemd/worker safety stops and leave durable-custody time.

Own source/contract review covered exact refusal strings, actual startup flags, original-budget propagation, same-group dummy identities and typed signals, authority freeze chronology, root/nonroot separation and durable handoff. It is author-only static review, not observed compatibility or acceptance. Two pending quantities remain: new dispatcher/probe need one FIRST different-model whole-code gate; installed entry/controls/formation/check need one separately ROOT-registered fixed batch only after that gate. No new canonical OPEN ID, source claim, nonresonance conclusion or follow-on authority.

Own report/code/docs were read WHOLE before the own-only collision/OPEN check and completion marker. Publication is transactional with expected verification; custody lists all owned/current pins and terminal all-writers-IDLE.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `4608`.
- Body SHA-256:
  `d63df29ce612da666f2ab34569a6e12f6d62e6fa8bdd02e884e53222470567c5`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
