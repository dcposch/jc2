# Fixed89/0 highest runtime, prospective only

This package is SOURCE ONLY and UNEXECUTED. It grants no AWS, registration, worker, retry, source-acceptance, basis, or JC2 authority. The 14 frozen inputs are the sole source perimeter. ROOT's lifecycle notice says the unchanged highest three files passed their first static gate; that gate body is not an input. This new dispatcher/mutator remains unreviewed.

## Exact layout and authority

ROOT installs three separate immutable directories: the original EXACT five science files; the EXACT three highest siblings entry.py/produce.py/check.py; and this EXACT three-file wrapper dispatch.py/probe.py/mutate.py. The probe is byte-identical. CAPRUN is not copied or altered. All source and baseline/native/registration paths are outside the working tmpfs. No imported highest function is used by the mutator; original arithmetic.py is its only mathematical module.

ROOT supplies and externally authenticates a fresh root-owned0444 registration, source/interpreter/CAPRUN/native pins, actual EC2 instance/host/boot/ns, exclusive cgroup, UID/GID, clear environment, absent durable target and fresh working mount. Hash binding is not authentication. The registration schema is f10-r2-highest-modular-runtime/v1, jobtag f10-r2-highest-modular-runtime-v1. The disabled template is intentionally not runnable or complete.

The original SIXFIELD native schema is unchanged: schema/files/directories/aliases/absent/python_path. Full current files, exact directory children, absent paths, aliases, startup sys.path, and actual mapped paths are checked. ROOT must supply the observed ld.so.cache and its pin if loaded; there is no exemption, ignored mapping or guessed library set.

## Registered calls and resource envelope

Exactly13 ordered labels: refuse-status, refuse-caps, refuse-inventory, refuse-source, refuse-hash, valid, startup-produce, startup-check, dummy, highest-produce, highest-positive, mutate, highest-negative. The first nine use the original five-file authority/startup probe. The dummy must show the samePGID descendant, RSS cap, identity-matched TERM then KILL, leader reap and group quiet. No original positive producer/checker is rerun.

Profiles are literal wall/softCPU/RSS: control5/3/2GiB; dummy5/3/32MiB; highest120/99/2GiB; mutation30/24/2GiB. All13 child wall allowances sum435 seconds; hardCPU ceilings sum361 seconds, leaving overhead within joint600wall/500CPU. These are allotted bounds, not performance predictions. The unchanged caller samples cgroupCPU, preserves20seconds cleanup margin, requires wall+15 admission reserve, and enforces exact2GiB cgroup and16MiB tmpfs. Two certificates each at most2MiB, plus at most1MiB metadata, fit the envelope. Any limit/late/missing-live-capture/shape failure stops without retry.

Every full CAPRUN argv is registered literally. Highest entries use -E -s -S -B, original arithmetic by absolute path, and17 sys.argv entries: script plus eight flag/value pairs registered-job, root-registration-sha256, self-sha256, entry-sha256, arithmetic, baseline, certificate, receipt. The only substituted registration token is ROOT_REGISTRATION_SHA256 as the value immediately after its unique matching flag. It is expanded only after ROOT registration is frozen/authenticated; the complete command is then compared before Popen. No shell interpolation or certificate-digest placeholder is allowed.

## Mutation vector and receipt

Mutation uses -I -S -B and19 sys.argv entries: script plus registered-job=f10-r2-highest-modular-mutation-v1, root-registration-sha256, self-sha256, arithmetic, baseline, certificate, positive-receipt, output, receipt. The fixed positive-receipt path is the root-frozen highest-positive receipt. This removes an otherwise impossible prospective literal argv containing a future unknown certificate SHA. Its exact status/root/baseline/source bindings and current source hashes are checked, then its certificate digest is checked before mathematical import or certificate parsing. ROOT's immutable freeze/authentication, not the receipt alone, licenses this link.

The mutator checks EC2/job/self/arithmetic before payload, canonical JSON with duplicate/numeric rejection, bounded canonical rational strings, literal rebuilt P7, all7 coordinates and dense89-unit denominators of complete Psi rows, complete weights/support, fixed prime/root, and the same special boundary. It independently extracts F,G. It checks the original eleven identity positions, chooses c if F is nonzero and otherwise d with nonzeroG, and adds1mod89 to its constant coefficient. It trims canonically, checks changed bytes, restores the exact old array in the whole decoded certificate and requires original canonical bytes. It never reads a coefficient body in this authoring task.

The receipt schema f10-r2-highest-modular-mutation/v1 has exact keys schema,status,science_outcome,job_tag,root_registration_sha256,sources,baseline_sha256,baseline_bytes,input,output,selection,inverse_edit_restores_whole_input_bytes,checker_run. Status is ONE_COFACTOR_MUTATED_UNCHECKED, scienceNONE, checker_run false. input/output each have path,sha256,bytes; bytes are decimal strings. selection has side c/d,index string0, old/new residue strings0..88, old_length/new_length strings0..6. No coefficient arithmetic occurs in dispatch: it binds actual whole file hashes/sizes and bounded metadata only.

## Acceptance and custody

Producer INCONCLUSIVE_NO_CERTIFICATE/normal2 with no candidate ends the batch after10 calls. Otherwise it must return normal0, empty streams and exact CANDIDATE_UNCHECKED receipt. Candidate and receipt are moved on the same filesystem, chowned ROOT,0444, fsynced/read back, and the frozen directory becomes0555. Positive checker must return normal0/empty streams and exact HIGHEST_SPECIAL_PROJECTIVE_CHECKED_NO_SOURCE_OUTCOME receipt; freeze it before mutation. Freeze changed fixture and mutation receipt before negative checking.

Changing c adds nonzeroF to the identity; changing d adds nonzeroG. Thus at least one of the eleven identity positions fails, even when the changed constant wraps88to0 or its array becomes empty. Parsing, degree bounds, baseline, place and all other fields remain unchanged. This is the single meaningful negative control; it is not an executed result.

Negative acceptance requires NORMAL_EXIT1, child_signal None, empty stdout, no receipt, and the exact traceback header, ordered check.py frames168 <module> main();158 main verify(ctx,raw);151 verify finalraise. Final line is exactly ValueError: CHECK FAILED: fixed-place full modular Bezout identity plus newline. At most one syntactically constrained bounded caret line per frame is accepted, as in the parent caller. Extra/chained/early native/parser/resource failures do not count.

Success status is HIGHEST_CHECKED_NEGATIVE_CONFIRMED with science_outcome NONE. It does not itself promote the original reconstruction or claim source zero, a parameter sequence, basis or JC2. Every call records actual parent/CAPRUN/child argv and identity, stream/telemetry/source/native/input pins. All relevant cgroup processes must be quiet before payload intake and durable copy. ROOT must harvest after the dispatcher exits, verify current sources/receipts/native/quiet custody and arrange independent review. No delete, unmount, worker stop or automatic next task is implemented.
