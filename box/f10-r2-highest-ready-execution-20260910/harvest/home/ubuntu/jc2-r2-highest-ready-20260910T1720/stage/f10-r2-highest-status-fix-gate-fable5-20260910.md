# FIRST Fable 5.1 focused gate: highest runtime status-literal correction

status=UNSEALED (no Seal, no charge_basis authored; adapter seals)
lane=f10-r2-highest-status-fix-gate-fable5-20260910
gate_model=claude-fable-5-1
first_action_utc=2026-09-10T16:36:34Z (inputs dir 16:36Z); stop = earlier of ROOT launch+18 min (no later than 16:54Z) and 16:55:00Z USER TERM; final-3-min reserve from 16:52Z; never reset
mode=STATIC; manual reasoning/text/hash only; ZERO subprocess/import/AST/syntax/compile/test/CAS; tools date, ls, wc, sha256sum, diff, cmp, grep -c, cat, sed, awk line numbering, apply_patch only
owned=xmodel/f10-r2-highest-status-fix-gate-fable5-20260910.md, box/f10-r2-highest-status-fix-gate-fable5-20260910/ (both absent at 16:39:04Z, checked before any write)

## 0. Custody, read scope, disclosure

All 8 snapshots in /tmp/jc2-lane.1AzLNO/inputs were hashed by sha256sum at 16:36:34Z before any body; all 8 equal the charged pins (rows generated from sha256sum output into box/input_custody.md; this body carries no hex digest). WHOLE reads: CONTRACT.md (37 lines), STATUS-DELTA.diff (25), Astra correction report (34) and produce.py (177) each in one numbered read ending at its last line; dispatch.py in four bounded reads 1-175, 176-350, 351-525, 526-686, the last ending at 686; the prior gate report in reads 1-35 and 36-69 ending at 69. Nothing clipped. old-dispatch.py and old-CONTRACT.md were read WHOLE through whole-file GNU diff and cmp against the new copies: diff emits every differing hunk, so each old line is either identical to the corresponding new line or one of the three lines printed; byte sizes confirm (see A). Nothing executed, imported, parsed, compiled or computed by tool; no baseline, candidate, registration, worker, provenance, corpus, Git, network, other lane, mutate.py, probe.py, entry.py, check.py or live file was opened. All sources remain UNEXECUTED; no runtime result or actual authority is claimed.

Disclosure: different-model FIRST review of Astra's correction (dispatch.py, CONTRACT.md, STATUS-DELTA.diff, correction report). The charged prior runtime gate whose D clause is at issue is Fable 5.1 authored, the same model as this reviewer, and it consumed two further Fable 5.1 gates (semantic code gate, highest modular code gate). Those are taken here as accepted scoped imports (A/B/C/E/F and D "otherwise CONFIRMED") over unchanged bytes, not re-reviewed and not re-proved. produce.py is re-read whole only to trace the corrected branch. entry.py is not charged: its receipt() layout and the exception classes raised by need()/sufficient() are imports from the highest modular code gate.

## 1. Verdicts

| item | verdict |
|---|---|
| A exact textual delta: two dispatch literals, one CONTRACT literal, nothing else | CONFIRMED |
| B corrected normal-2 branch: receipt binding, candidate absence, echoed status, 10 calls, no later phases, fail-closed; prior D REFUTED discharged at its stated scope | CONFIRMED (two entry.py facts imported, see B) |
| C CONTRACT and correction report accuracy; ValueError is not a bad-prime diagnosis; scoped imports; obligations | CONFIRMED |

No blocking defect found; no fix required.

## A. Textual delta

CONFIRMED. diff of old-dispatch.py against dispatch.py reports exactly two changed lines, 600 and 602, each replacing INCONCLUSIVE_FIXED_PLACE by INCONCLUSIVE_NO_CERTIFICATE inside otherwise identical text; diff of old-CONTRACT.md against CONTRACT.md reports exactly line 31 with the same single replacement. Sizes agree and exclude anything else: dispatch.py is 6 bytes longer (two substitutions, 14 versus 11 characters), CONTRACT.md 3 bytes longer; cmp -l's first differing offsets in each file are the F/I/X/E/D to N/O/_/C/E bytes of the first substitution. Literal counts: old-dispatch 2 FIXED_PLACE and 0 NO_CERTIFICATE, new 0 and 2; old-CONTRACT 1 and 0, new 0 and 1; produce.py 0 and 1 (line 168). STATUS-DELTA.diff is exactly this delta: its dispatch hunk at 597-605 and CONTRACT hunk at 28-34 reproduce the actual context lines of the new files verbatim, checked line by line against the whole reads. Hence every control, native, argv, budget, layout, custody and success-path line of the dispatcher (:1-599, :601, :603-686) and every CONTRACT clause other than :31 is byte-identical to the accepted original; the MUTATOR_SHA, CAPRUN_SHA, HIGH, SCIENCE, BASELINE, LIMITS, PROFILES and LABELS literals (:26-44) and the receipt schema (:504-511) are unchanged. mutate.py and probe.py are not charged here; the unchanged mutator binding (:31, :191, :202) means the new dispatcher binds exactly the mutator the old one bound, whatever its bytes.

## B. Corrected normal-2 branch against produce.py

CONFIRMED at the traced scope. produce.py:163-169: main() enters, calls candidate(ctx) inside try, and on ValueError writes receipt(ctx,'INCONCLUSIVE_NO_CERTIFICATE') with no digest argument, then SystemExit(2); SystemExit prints nothing, so the streams stay empty as :596 requires. Corrected dispatcher trace: :595 runs highest-produce as the tenth call (five refusals, valid, two startup refusals, dummy, then this; used_labels forbids repeats at :353; :479 demands live capture). :596 requires empty streams. :597 accepts only NORMAL_EXIT with child_returncode 2 and resource None, :598 rechecks it, :599 requires writer/candidate.json absent, :600 requires the writer receipt to be a regular file of at most 32768 bytes equal as a whole dict to the expected 11-key record (:504-511): schema f10-r2-highest-modular89-receipt/v1, the emitted status literal, science_outcome NONE, mode produce, prime 89, root 0, root_registration_sha256 equal to the actual immutable registration hash, sources equal to the exact path-to-digest map for highest produce.py, highest entry.py and science arithmetic.py at the pinned digests, baseline SHA and byte count, certificate_sha256 None. :601 freezes the receipt (group quiet, same-tmpfs rename, root 0444, fsync, digest readback, pin, frozen back to 0555; every later revalidate requires the frozen inventory to be exactly that receipt). :602 returns status INCONCLUSIVE_NO_CERTIFICATE, science_outcome NONE, calls 10, root hash, no candidate field; main() (:669, :675) writes that dict as the result inside durable CUSTODY.json with the ten run records and exits 2 (:682), never 0. The return precedes :604-636, so no positive, check, mutate or negative call, no candidate freeze and no 13-call check occurs; the success path is unchanged. Candidate absence is checked at :599 before the freeze; no further child launches, group_quiet runs at :486 and :639, and durable_copy enumerates every mount file (:642-655), so a candidate could not appear unrecorded.

Two entry.py facts are imports, not verified here: (i) receipt(ctx,status) with the digest omitted emits exactly the 11 keys with certificate_sha256 null; (ii) need() and sufficient() raise ValueError instances. Both lie inside the prior gate's "otherwise CONFIRMED" and the highest modular code gate. If (i) fails, :512 fails and the run ends STOP_NONDECISION; if (ii) fails for some check, the producer dies with a traceback and exit 1, and :596/:604 fail. Both are fail-closed nondecisions, never success.

ValueError scope inside candidate() (produce.py:5-160): about two dozen raise sites. Only four are arithmetic at (89,0): nonunit modulo the fixed prime (:17), fixed simple-root condition (:69), same-special boundary (:100), no unit modular cofactor (:152). The rest are wire and shape checks on the baseline: rational syntax and caps, literal septic, canonical JSON and format, interface lists, eight P7 strings, modulus equality, row count, term caps, exponent bounds, sparse order, support and weight, chart shape, degree caps, Euclid caps, identity readback; json decoding errors are ValueError subclasses too, so a non-JSON baseline takes the same branch. The receipt has no reason field, so the exit-2 path cannot distinguish these, and the recheck and certificate write (:170-173) are never reached.

Fail-closed on other states: exit 2 with a candidate present fails :599; exit 2 with a missing, oversized, differently keyed, differently statused or digest-bearing receipt fails :501, :502 or :512; exit 2 with stream text fails :596; exit 0 goes to the unchanged CANDIDATE_UNCHECKED path; exit 1, a signal, RESOURCE_CAP or missed live capture fails :479 or :597 then :604; every such exception reaches :670 and becomes STOP_NONDECISION with durable custody. Nothing accepts a malformed or mismatched state.

Discharge: the prior D REFUTED clause said exactly that :600/:602 named INCONCLUSIVE_FIXED_PLACE while produce.py:168 emits INCONCLUSIVE_NO_CERTIFICATE, making the documented 10-call end unreachable, and asked that the returned status echo the receipt literal rather than assert a place verdict. Both now hold. Discharged at that precise scope; the D remainder (sequence, freezes, STOP semantics) is unchanged text and stays an import.

## C. Documents, imports, obligations

CONFIRMED. CONTRACT.md:31 now states that producer INCONCLUSIVE_NO_CERTIFICATE/normal2 with no candidate ends the batch after 10 calls, which is exactly :597-603. The correction report (lines 7, 9, 11, 15, 17) states the two-line plus one-line delta, the old fail-closed STOP_NONDECISION, the new exact comparison, the echoed status with science NONE and calls 10, and expressly refuses to read the status as a bad-prime, nonregularity or source-point diagnosis; section B shows that refusal is forced by the producer's raise sites and reason-free receipt. It supersedes the old Astra report's status clause instead of editing frozen files; that old report is not charged and not checked. Prior A/B/C/E/F and the D remainder are consumed as accepted scoped imports over unchanged bytes; no mathematical premise is re-proved, and no place, certificate, highest, source, basis or JC2 result follows. All AWS workers are retired; no actual highest run or registration exists.

Concrete remaining execution obligations: (1) a fresh, externally authenticated, enabled ROOT registration whose dispatcher role pin is the new dispatch.py digest (:183, :197-198 and :280-281 hash the actual dispatcher), with 13 vectors reproducing :394-398 byte for byte and the token once in each of the last four; (2) pins for the seven roles, five science files, three highest siblings and the native manifest with the observed ld.so.cache; (3) a 3.11+ interpreter; (4) fresh 16 MiB tmpfs with empty authority/frozen children and a science-owned 0700 writer; (5) absent durable target; (6) ROOT harvest after exit with independent intake of CUSTODY.json, whose result field and exit code 2 are the only carriers of the 10-call status. No optional hardening is requested.

## OPEN(S) RAISED

None. No new canonical OPEN. The correction is exactly the literal fix the prior gate asked for; the remaining unknowns are execution-only (actual producer outcome at 89/0, entry.py's null-digest receipt form and exception classes as imported, measured budgets, interpreter traceback layout), all fail-closed. No charge_basis, no follow-on authority, no new theorem.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly xmodel/f10-r2-highest-status-fix-gate-fable5-20260910.md and box/f10-r2-highest-status-fix-gate-fable5-20260910/input_custody.md, both absent at 16:39:04Z; no shared, frozen, input, baseline, source or other-lane file was touched; no corpus scan, Git, network, process control or scientific subprocess.

## WHOLE check and attestation

All 8 inputs were read whole as itemised in section 0; the eight digests were recomputed after the body by sha256sum and compared against the custody rows. Authoring: every byte of both owned files was written by apply_patch (custody rows piped from sha256sum output through its stdin, then one label correction; the report as a skeleton with sections 0-C ending on a single anchor line, replaced by this block); no Write/Edit tool, no redirection into any file, no helper, no source or input edit, no Seal, no charge_basis, no execution, import, AST, syntax, compile, test, CAS, network, AWS, SSH, Git, agents, corpus, shared, protected, other-lane or live file. Bound disclosure: the skeleton write measured 1551 by wc -w, above the 1500-word bound, because wc counts each path:line citation as a word; it was not split afterwards; this write and the custody writes are under the bound. Same-model disclosure: the prior runtime gate and its two imported gates are Fable 5.1 authored and consumed only at their stated scope; the correction under review is Astra's. Timeline: hashes 16:36:34Z, whole reads 16:36-16:37Z, absence 16:39:04Z, custody 16:40:32Z, skeleton 16:41:12Z, this block before the 16:52Z reserve; stop no later than 16:54Z, never reset. Heading order verified by grep after this append: 0, 1, A-C, OPEN(S) RAISED, COLLISIONS, this section. The marker below is the only standalone marker in this file and nothing follows it.

<!-- BODY-END -->
