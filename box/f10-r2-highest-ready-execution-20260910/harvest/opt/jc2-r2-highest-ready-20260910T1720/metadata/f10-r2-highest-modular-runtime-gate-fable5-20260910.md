# FIRST Fable 5.1 gate: fixed 89/0 highest runtime and single-cofactor mutation

status=UNSEALED (no Seal, no charge_basis authored; adapter seals)
lane=f10-r2-highest-modular-runtime-gate-fable5-20260910
gate_model=claude-fable-5-1
first_action_utc=2026-09-10T16:07:20Z; stop = earlier of ROOT launch+18 min (inputs dir 16:07, so no later than 16:25Z) and 16:27:00Z USER TERM; final-3-min reserve from 16:22Z; never reset
mode=STATIC; manual reasoning/text/hash only; ZERO subprocess/import/AST/syntax/compile/test/CAS; tools date, ls, wc, sha256sum, cat, sed, apply_patch only
owned=xmodel/f10-r2-highest-modular-runtime-gate-fable5-20260910.md, box/f10-r2-highest-modular-runtime-gate-fable5-20260910/ (both absent at 16:07:20Z)

## 0. Custody, read scope, disclosure

All 12 snapshots in /tmp/jc2-lane.qcBXD9/inputs were hashed by sha256sum at 16:07 UTC before any body; all 12 equal the charged pins (rows are generated from sha256sum output into box/input_custody.md; this body carries no hex digest). All 12 were then read WHOLE in parallel cat -n reads; dispatch.py in two bounded reads (1-350, 350-686) whose second read ended at line 686; nothing clipped. Nothing executed, imported, parsed, compiled or computed by tool; no baseline, candidate, registration, worker, provenance, corpus, Git, network, other lane or live file was opened. All new sources remain UNEXECUTED; no runtime result, loaded-image safety or actual authority is claimed.

Disclosure: different-model FIRST review of Astra's new dispatch.py/mutate.py/CONTRACT/template. The two charged gates (semantic code gate, highest modular code gate) are Fable 5.1 authored, same model as this reviewer; they are consumed as accepted imports at their exact stated scope (parent caller checks, CAPRUN telemetry keys, probe, five science files, the three highest siblings, arithmetic). The highest three files and arithmetic.py are re-read whole here only to trace the new caller/mutator against them, not re-audited mathematically.

## 1. Verdicts

| item | verdict |
|---|---|
| A 13-call order, 5+3+3 isolated directories, metadata-only caller, retained checks, new phase binding | CONFIRMED |
| B literal argv equality, one typed hole per last-four vector, 17/19 argv, receipt-bound candidate digest, set-vs-map source pins | CONFIRMED (set comparison is not a hole under immutable ROOT) |
| C independent P7/89/root/Psi/boundary/F,G reconstruction, selection, wrap/empty/trim, whole-byte inverse | CONFIRMED |
| D state sequence and receipts, freezes, STOP semantics | REFUTED in one clause: producer INCONCLUSIVE branch is unreachable (status literal mismatch) |
| E negative shape, three frames 168/158/151, literal final line, optional caret lines | CONFIRMED |
| F caps 600/500/2GiB/16MiB, 435/361, capture/quiet, custody, success status scope | CONFIRMED; wall/metadata budgets unmeasured |

Smallest blocking correction: dispatch.py:600 (and the returned status at :602) names 'INCONCLUSIVE_FIXED_PLACE', but produce.py:168 emits 'INCONCLUSIVE_NO_CERTIFICATE'; on a producer normal-2 exit highest_receipt (:512) raises 'exact highest bound metadata' and the batch ends STOP_NONDECISION, never the documented 10-call INCONCLUSIVE end. Replace the literal at :600/:602 with 'INCONCLUSIVE_NO_CERTIFICATE' and correct CONTRACT.md:31 and the Astra report line 23. Fail-closed: no false success is possible from this defect.

## A. Call order, layout, retained checks

CONFIRMED. LABELS (dispatch.py:43-44) is exactly the nine accepted preflights plus highest-produce, highest-positive, mutate, highest-negative; execute() runs them in that order (:565-621), used_labels forbids repeats (:353), :626 requires the completed 13-tuple, :240 requires exactly these 13 registered commands. Directories: science_dir/wrapper_dir/highest_dir each root 0755 and pairwise distinct (:184-187); revalidate requires the science listing to be exactly the five SCIENCE names with 0444 and literal SHAs (:273, :282-284), the wrapper listing exactly dispatch.py/probe.py/mutate.py (:274), the highest listing exactly entry.py/produce.py/check.py at 0444 and the HIGH literals (:276-279), which equal the charged pins. Non-shadowing: highest entries run with -E -s -S -B (:390), so sys.path[0] is the highest directory holding only the three siblings and `from entry import` (produce.py:2, check.py:2) resolves there; the mutator runs with -I -S -B, adds no script or cwd directory, imports only stdlib and loads arithmetic.py by absolute path (mutate.py:91-93). CAPRUN cwd is the science directory (:398) but never on any child sys.path. Unchanged CAPRUN literal (:32, :199), SCIENCE literals (:33-38), baseline SHA and byte count (:29-30, :200-201), mutator literal (:31, :202). Probe: byte-identical to the accepted probe (same charged digest as the semantic gate custody), but dispatch.py holds no literal probe pin; its pin is ROOT-registered (:197-198, :280-281), as in the parent. Metadata-only: load() is applied to registration, native manifest, telemetry, valid/dummy payload events, highest receipts (32768 cap) and the mutation receipt; candidate.json, changed.json and the baseline are only hashed and sized, never parsed. Retained: ROOT/Linux, EC2 vendor/instance, host, boot, PID namespace, exact cgroup and memory.max, interpreter, dispatcher identity, cwd, literal outer argv (:160-195), six-field native manifest with sys.path closure, directory children, aliases, absent paths and ps aliases (:207-213, :280-301), tmpfs identity/capacity, outer stream binding, absent durable target, deadlines (:222-239), budget (:252-254), group_quiet (:316-320), check_maps (:308-314). New layout binding: highest outside the output mount (:224) and all role/registration/native paths outside tmpfs (:225-228). check_maps compares mapped path names against the pinned on-disk closure; no atomic or coherent mapped-image attestation is made, and none is claimed by the CONTRACT.

## B. Registration equality and the typed hole

CONFIRMED. run() rebuilds the full CAPRUN vector (:394-398) from fixed literals, role paths, profile numbers and the computed script vector, then requires registered strings (:400) and `command == registered` (:414) before any pre record or Popen. For the last four labels the registered list must contain `--root-registration-sha256` exactly once and the token exactly once, the token in the slot directly after the flag, and only that slot is replaced by self.root_sha (:402-411); for the nine preflights the token is forbidden (:413). count() is element equality, so a substring cannot expand; the env is fixed (:45, :428) and Popen takes a list. self.root_sha is the hash of the immutable 0444 registration (:161-162), rechecked at every revalidate (:257). Highest script vector (:369-373) is script plus eight ordered pairs registered-job/root-registration-sha256/self-sha256/entry-sha256/arithmetic/baseline/certificate/receipt, 17 entries (:374), which is entry.py:36-40's tuple and TAG order; arithmetic is source/arithmetic.py for produce and source/check_arithmetic.py for check (:368), the pins at entry.py:8-9 equal SCIENCE. Mutation vector (:377-384) is script plus nine ordered pairs, 19 entries, equal to mutate.py:19-22's flags and JOB. Nothing in any vector names a candidate digest: the future candidate SHA enters only via the frozen positive receipt path (:381), and the mutator binds it at mutate.py:69-90 before importlib executes arithmetic (:91-93): exact 11-key shape and canonical bytes (:71-73), schema/status/science NONE/mode check/prime/root/root hash equal to its own CLI value/baseline SHA and bytes (:74-79), three source digests (:80-84), each source path absolute, canonical and currently hashing to its digest (:85-86), baseline bytes (:87-88), then certificate digest equal to the receipt's certificate_sha256 (:89-90). Set-versus-map: mutate.py:83-84 compares the source digest set, whereas the caller had already required the exact path-to-digest map on the same bytes (:505-512) before freezing that receipt (:613-615), and every revalidate re-hashes the frozen file (:260-262). A receipt whose map is permuted would already have failed :512; only a forged frozen receipt, outside the immutable-ROOT assumption, could reach the mutator, so this is a weaker duplicate binding, not a hole. Telemetry binds the child vector hash and count (:464-465), the runner's live argv (:435, :475) and the science child's argv (:442), and :479 requires both live captures for the last four labels. Matching digests are bindings; authentication is ROOT's freeze.

## C. Mutator reconstruction and edit

CONFIRMED. Before any coefficient read: platform, EC2 vendor, 19-argv, nonzero 64-hex pins, seven distinct canonical paths, O_NOFOLLOW bounded drift-checked reads, own and arithmetic source pins (mutate.py:16-51), output collision (:52). Fixed prime: 89 trial-divided by 2..9 (:108). Literal P7: tau, dl, kl, ga and the qa/qs/qm expression (:109-113) are the producer's (produce.py:39-47) with int scalars 2, -6, 245 in place of Q(2), Q(-6), Q(245); qs and qm multiply Fractions by ints into Fractions and trim on falsiness, so the lists are equal; :114 checks length 8 and the leading -245*120*144*tau before the division at :115. Baseline: canonical bytes, format, job tag, variables, weights, base, basis (:116-120), eight P7 strings parsed by the own rat() (:95-103, same rules as produce.py:22-34) and equal to the modulus (:121-122). mod() (:104-107) refuses a zero denominator residue and readbacks the Fermat inverse; :123 requires coordinate 0 of P7 zero and coordinate 1 nonzero, the simple root at 0. Rows (:124-139): exactly three, each at most 4096 terms, list/len shapes, exponent strings '0'..'16', full seven-coordinate wires, nonzero and strictly increasing terms, e[4]=e[5]=0, weight 5/6/7; all seven coordinates of every term, positive-z included, pass mod() at :137 before the z filter at :138; tables key (X2,X3) with coordinate 0. Boundary :140 reads (1,1), (3,0), (0,2), the producer's tables[0][(1,1)], tables[1][(3,0)], tables[1][(0,2)] and check.py:98's forms[0][1][1], forms[1][0][3], forms[1][2][0]. Shapes (:153-155) equal produce.py:98 limits. line() equals part(); f and g at :160-161 are Q^2B3-PQB1+B0P^2 and Q^2C3-PQC2+C1P^2, the producer's :130-131 and the checker's :121-128. mul allocates one extra slot that trim removes; empty operands give []. Certificate: canonical bytes, eight exact keys, schema/baseline/prime/root/chart, modulus strings equal to the baseline P7 strings (:163-167; the producer emits wire_q of the same reduced Fractions, and rat() forced the baseline strings canonical, so string equality holds); residue arrays '0'..'88', at most 6, no trailing '0' (:168-171); full identity at all 11 positions (:173-174). Selection :175: c if f nonempty else d, with f or g required. Edit :176-178: old array, old0 = first residue or 0, edited = ints or [0], constant +1 mod 89, canonical trim; empty gives ['1'], ['88'] gives [], ['88','5'] gives ['0','5']. :179 changed bytes differ and are bounded; :180-181 reload the changed bytes, put back the exact old list and require the original bytes. Negative correctness: the new identity is 1+F (or 1+G with F=0, G nonzero); a nonzero polynomial of degree at most 5 changes some position below 11, and the checker's other predicates (canonical encoding, key set, modulus, residue strings, trailing zero, length) still pass, so the checker reaches check.py:151. Only original arithmetic.py is imported (:91); no producer/checker import exists. No source/reference mismatch found.

## D. State sequence, receipts, freezes, STOP semantics

REFUTED in one clause, CONFIRMED otherwise. Sequence: highest-produce runs with the baseline as its input pin (:595), empty streams (:596); on normal 0 the candidate must be a bounded regular writer file (:605-607), the producer receipt must equal the exact 11-key CANDIDATE_UNCHECKED record with the candidate digest (:608, :504-512, matching entry.py:137-141 and produce.py:173), and candidate plus receipt are frozen (:609) before highest-positive (:611). The positive receipt must equal the exact HIGHEST_SPECIAL_PROJECTIVE_CHECKED_NO_SOURCE_OUTCOME record with the frozen candidate digest (:613-614, check.py:164) and is frozen (:615) before mutate (:616). fixture_receipt (:515-542) requires the exact 13 keys mutate.py:196-203 emits, schema/status/science NONE/job/root hash, the exact two-path source map, baseline SHA and byte string, restoration true and checker_run false, input equal to the frozen candidate and output equal to writer/changed.json with actual size and digest readback, distinct digests, and the selection fields with side c/d, index '0', distinct residues '0'..'88' and lengths '0'..'6'; changed.json and the receipt are frozen (:619) before highest-negative (:621). freeze() (:485-497): group quiet, revalidate, frozen reopened 0755, per file fresh target, digest, same-tmpfs rename, chown root, chmod 0444, fsync, immutable readback equal to the digest, pin; writer/frozen/mount fsynced, frozen back to 0555, revalidate, which thereafter requires 0555, every pinned fixture 0444 with its digest and the exact frozen inventory (:260-266) at every later revalidate, i.e. before Popen (:421) and after group quiet (:460), plus input pre/post hashes (:415, :461). Producer INCONCLUSIVE: :597-603 accept normal 2 with resource None and no candidate, then call highest_receipt with status 'INCONCLUSIVE_FIXED_PLACE'; produce.py:167-169 writes 'INCONCLUSIVE_NO_CERTIFICATE'. The exact-dict comparison at :512 fails, RuntimeError propagates to main() (:670-671) and the batch ends STOP_NONDECISION with reason 'exact highest bound metadata' after 10 calls; the receipt is still in the writer, not frozen, but is copied by durable_copy. So the documented INCONCLUSIVE end (CONTRACT.md:31, Astra report line 23) is unreachable: REFUTED, fail-closed. Fix as stated in section 1. Also, after that fix, a producer normal 2 covers every ValueError in candidate() (wire, shape, or place), so the dispatcher's returned status should echo the receipt literal rather than assert a place verdict; today no bad-prime interpretation is emitted because the branch dies first. No alternate prime, fallback, retry or baseline positive rerun exists: single pass, used_labels, :632 literal False. Every other unexpected outcome (checker exit 2, signal, RESOURCE_CAP, missing live capture, stream content) fails a need() and is STOP_NONDECISION.

## E. Negative acceptance

CONFIRMED. :622-624 require NORMAL_EXIT, resource None, child_returncode 1, child_signal None, child_exit_code 1, empty stdout and no writer receipt; the receipt path is fresh so entry.py:84 cannot fire first. traceback() (:544-560) bounds stderr at 8192 bytes, decodes ASCII (paths are restricted to [A-Za-z0-9_./-] by canonical(), :81), requires the literal header, then exactly three ordered frames with the highest check.py path: line 168 in <module> 'main()', line 158 in main 'verify(ctx,raw)', line 151 in verify the raise statement, then the final line and nothing else. Against the frozen check.py: :167-168 is the guarded main() call, :157-158 the try body, :151 the raise inside the position loop; no except handler encloses the raise, so no chained context text; main() catches only Inconclusive (:159). The statements at :548-549 equal the stripped source lines. After each frame one optional line of spaces, tildes and at least one caret, at most 512 bytes, is accepted (:557-558), never assumed absent; a 3.11+ interpreter printing no carets for whole-line statements also passes. Wrong-shape, parser, early-boundary, import, native or resource failures produce different frames, an exit 2 with receipt, a signal or RESOURCE_CAP and fail :622-625 or :612; none counts. No four-frame carryover: the frames list has three entries and :559 forbids extra text. Receipt fields and source scopes: see B; the positive receipt's sources are the highest check.py, highest entry.py and science check_arithmetic.py at the charged digests.

## F. Caps, capture, custody, scope

CONFIRMED with unmeasured budgets. LIMITS 600/500/2147483648/16777216 (:39-40) and PROFILES control 5/3/2GiB, dummy 5/3/32MiB, mutate 30/24/2GiB, highest 120/99/2GiB (:41-42) equal the template; :166 requires registration equality. Child wall 9x5+3x120+30 = 435; hard CPU at soft+1 (accepted CAPRUN import) 9x4+3x100+25 = 361; admission needs wall+15 inside 600 and the original deadline (:356, :421, :253), sampled cgroup CPU cutoff 480 with 20 s cleanup (:254) also inside the live loop (:452), late-return guard wall+15 (:453), post-return 600/500 (:462). Capture/quiet before body: group_quiet and revalidate before authorize (:354), revalidate/budget/size_budget and authority re-hash directly before Popen (:421-422), runner maps and live record (:431-435), science child uid/gid/namespace/caps/maps and live record (:442-449), group_quiet before any telemetry or stream read (:459), revalidate and pin checks after (:460-461), telemetry argv/cwd/caps/leader/stream bindings (:463-472), required live capture for the last four labels (:479). Sizes: candidate/changed at most 2 MiB each (:333), everything else at most 1 MiB (:337), tmpfs capacity at most 16 MiB (:304). Durable: absent ext4/xfs target under a root directory (:234-236), mkdir 0700, exclusive copy, fsync, byte identity, at most 200 files, CUSTODY.json, directory fsyncs, task cutoff at entry, per file and exit (:638-661). The source has no code to stop, retain or reclaim the worker, delete, unmount or retry (:680-682). Success: only the completed 13-call path emits HIGHEST_CHECKED_NEGATIVE_CONFIRMED with science_outcome NONE, the frozen candidate/changed digests, negative reason and selection (:628-635), exit 0 only then (:682). That status attests one producer run, one independent positive check and one single-cofactor rejection at (89,0) under ROOT custody; its mathematical content is the highest code gate's scope (special projective emptiness at 89/0 composed with the accepted properness and Route P imports), not source closure, a point, a basis or JC2, and only with complete actual custody. Unmeasured, fail-closed: 13 runs each with at least three revalidates hashing every pin and native file plus the 1.65 MB baseline, and the roughly 30 KB of records per run against the 1 MiB metadata reserve.

Concrete execution obligations: (1) apply the :600/:602 literal fix and re-pin dispatch.py; (2) an enabled registration with 13 vectors reproducing :394-398 byte for byte, the token once in each of the last four, pins for the seven roles including the probe digest, five science files, three highest siblings and the native manifest with the observed ld.so.cache; (3) interpreter 3.11+ pinned (set_int_max_str_digits at entry.py:87, mutate.py:55); (4) fresh 16 MiB tmpfs with empty authority/frozen children, science-owned 0700 writer, outer streams bound; (5) absent durable target; (6) ROOT harvest after exit and independent intake of the durable custody.

## OPEN(S) RAISED

None. No new canonical OPEN. The one defect found is a literal mismatch with a one-line fix (section 1); the remaining unknowns are execution-only (producer outcome at 89/0, measured wall/CPU/metadata against the fixed profiles, live capture inside the 50 ms poll, interpreter traceback layout), all fail-closed. No charge_basis, no follow-on authority, no new theorem.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly xmodel/f10-r2-highest-modular-runtime-gate-fable5-20260910.md and box/f10-r2-highest-modular-runtime-gate-fable5-20260910/input_custody.md, both absent at 16:07:20Z; no shared, frozen, input, baseline, source or other-lane file was touched; no corpus scan, no Git, no network, no process control, no scientific subprocess.

## WHOLE check and attestation

All 12 inputs were read whole as itemised in section 0 and the custody file; the twelve digests were recomputed after the body and compared to the custody rows. Authoring: every byte of both owned files was written by apply_patch (custody rows piped from sha256sum output through apply_patch's stdin; the report as skeleton with A-C, then D-F, then this block on a single anchor line that is removed here); no Write/Edit tool, no redirection into any file, no helper, no source or input edit, no Seal, no charge_basis, no execution, import, AST, syntax, compile, test, CAS, network, AWS, SSH, Git, agents, corpus, shared, protected, other-lane or live file. Bound disclosure: the first report write measured 1587 by wc -w, above the 1500-word bound, because wc counts each path:line citation as a word; it was not split afterwards, and the two later writes are under the bound. Same-model disclosure: both imported gates are Fable 5.1 authored and consumed only at their stated scope; the new dispatcher, mutator and contract are Astra's and were reviewed here for the first time. Timeline: hashes and absence 16:07:20Z, whole reads 16:07-16:09, custody 16:12:55Z, skeleton with A-C 16:14:04Z, D-F 16:14:52Z, this block before the 16:22Z reserve; stop no later than 16:25Z, never reset. Heading order verified by grep after this append: 0, 1, A-F, OPEN(S) RAISED, COLLISIONS, this section. The marker below is the only standalone marker in this file and nothing follows it.

<!-- BODY-END -->
