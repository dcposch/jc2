# One reviewed-flow strict-nine preparation, September12 batch a

ROOT selection08:01:01UTC. Old September11 b/c/d remain CLOSED. This is
one NEW bounded preparation and conditional administrative observation,
not a reset, enlarged runtime cap, source run or automatic next-r attempt.

The changed mechanism is substantive: four SOURCE-STATIC-reviewed controller
entries are now fixed before holder admission, with exact prepared-byte
binding, a dedicated outside-base administrative tree, corrected card and
separate ROOT-owned admit/attest/install/release steps. Previous inline
post-holder document composition and missing/inconsistent setup dependencies
have been replaced. Accepted gates retain interruption STOP, exclusive
trusted ROOT and original120-second limitations. No claim of measured speed.

Select worker-independent preparation NOW. All remaining code composition,
batch/clock/path constants and transport/program roles must be complete and
qualified before any allocation. ROOT owns registration/authorities/source
contract/policy/config/manifests and external actions. Sol owns only thirteen
specified inert administrative ports in its separate box. Nothing is run by
that authoring task. The four fixed entries stay in their reviewed directory;
do not rerun the composer, mutate old bytes or create a live config yet.

Original clocks in the adjacent ORIGINAL-CLOCKS.json are immutable. Offline
reserve08:30/HARD08:33; no allocation after08:40; no holder after09:00;
holder min(start+120s,09:02); admission09:02:15; outer TERM09:08/KILL09:08:05;
task/custody09:13; exact-instance coordinator retirement09:18 or earlier
after secured evidence. No extension, new batch within this plan or retry.

Only after complete offline qualification, fresh quota/two fleet selectors,
and a separately recorded ROOT allocation decision: at most ONE c7i.2xlarge,
100GiB retained-root EBS, API termination protection false, exact-ID detached
coordinator retirement timer armed and verified immediately. Fresh actual
worker/native/namespace/ACL/helper facts then bind EARLY metadata. They must
not come from old workers. No worker is allocated by this file's creation.

Before holder: setup and preholder security fully bound; exact five-row LF/EOF
admin manifests independently constructed and WHOLE-BYTE compared before
pinning; prepared documents retain only approved LATE tokens; local14 and
remote8 fixed closures complete; ROOT exclusivity/no replacement in force.
All exact bound-source/native/current timer checks precede admission. Nine
authority files are expected runtime OUTPUTS and must NOT be preinstalled.

Only actual holder PID/start/InvocationID, outer/nine leaf identity pairs,
immediately verified timer receipt/observation/ROOT decision and their
approved descendant hashes may remain late. Four phases require separate
ROOT selection. Preserve the reviewed cooperative publication guarantee:
not O_EXCL, not malicious-root protection. On interrupted entry collect only
its known handle under original deadline then STOP, never resume a phase.

Success means all nine exact terminal records plus
CLOSED_SCOPE_PREFLIGHTS_9_CHECKED_NO_SCIENCE. It does not authorize successful
producer/checker execution, mutation, rank, FULL13 or any source conclusion.
Source900wall/600CPU/8GiB/128MiB and aggregate3000wall/2100CPU, outer CPU69%
per10ms/burst0, memory8GiB/swap0/Tasks32, CPU origin0 and strict dummy remain.

STOP on missing qualification, expired clock, stale-positive refusal,
unexpected byte/process/outcome, cap or custody failure. No live code fix or
replacement holder/worker. Establish terminal quiet before artifact intake;
archive/compare/fsync/download/local-pin/fsync before exact retirement, with
retained EBS backup. Do not retain idle compute for review. This preparation
is subordinate to mathematical work; Astra simultaneously tests a distinct
all-degree adjoint-source mechanism. No global reranking or cadence reset.
