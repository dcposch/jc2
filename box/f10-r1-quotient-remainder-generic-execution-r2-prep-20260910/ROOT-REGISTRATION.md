# ROOT fresh registration r2 — private endpoint and pinned SSH identity

This is a NEW single batch, not continuation or retry authority under the
retired first registration. Attempt1 ended before deployment/authority/
control/mathematics: first executor SSH to new public107.21.89.35 failed
host-key verification. Worker STOPPED independently confirmed01:09:01 and
the old backup canceled. Root's own private-IP metadata connection succeeded;
that was not a test or an executor success. The old failure must be sealed
and its executor terminal before this registration is activated.

Local read-only diagnosis: `ssh-keygen -F 172.30.0.72` finds the retained
private host keys; lookup of107.21.89.35 finds none. SSH effective config
has BatchMode=yes and StrictHostKeyChecking=ask. This explains the transport
failure without a mathematical diagnosis or a host-key mismatch claim.
No native/code/control/generic call has yet run on the replacement package.

## Exact incorporated template and overrides

Read WHOLE the frozen command/cap/control/custody specification
box/f10-r1-quotient-remainder-generic-execution-prep-20260910/ROOT-REGISTRATION.md,
SHA5868c7b5ebfb15cbbe9c9517d52cbee0ce9e1857ef5925d4c00abeef21e46158.
It supplies the full eight-operation sequence, per-child F10-R1-REGISTERED/v1
schema, literal arrays, source-safe bounds and acceptance rules ONLY WITH
the following exhaustive overrides. It does not reactivate its old owner,
path, clocks or attempt. No executable code, supervisor or schema is changed.

1. Exclusive executor/derivative-authority writer is Astra
   /root/nonemptiness_certificate, not /root/model_productivity.
2. ROOT-PINS.json is the R2 file in this directory, SHA
   b81544267b0c33032fd33c663125677731d201a66af91a4f30d22ed73204dab5.
   It changes ONLY job/remote paths and adds explicit SSH metadata; all eight
   scientific/execution source hashes, all100native hashes and interpreter
   hash are identical to the prior declaration. Root checked that equality.
3. job_tag=f10-r1-quotient-remainder-generic-execution-r2-20260910.
   Remote cwd=/home/ubuntu/jc2-f10-r1-quotient-remainder-generic-r2-20260910.
   The entire remote directory must be absent before creation; no overwrite.
   SYSTEM unit names=jc2-f10-qr-generic-r2-20260910-NAME.service.
   Per-operation basenames, authority schema, vectors and profiles unchanged
   after substituting this exact cwd/job/unit prefix.
4. Own local outputs ONLY
   box/f10-r1-quotient-remainder-generic-execution-r2-20260910/ and
   xmodel/f10-r1-quotient-remainder-generic-execution-r2-astra-20260910.md.
   Never mutate either prep directory, old attempt or source artifacts.
5. First action/absent-target ACK. Fixed task cap EARLIER first+25min or
   2026-09-10 01:36:00UTC, final3min publication reserve. Mathematical end
   01:30:00UTC AND firstmath+240seconds, with the same wall+15 admission.
   Stop worker immediately after evidence collection/failure and in all
   cases by01:31:00UTC; ROOT independent stop01:32:00UTC. Never reset.
6. Stage this R2 registration as ROOT-REGISTRATION.md, the template as
   TEMPLATE-REGISTRATION.md, R2 ROOT-PINS.json, and the pinned SSH public-key
   file as ROOT-SSH.known_hosts, plus the same8taskfiles. Add hashes of ALL
   four metadata files to each authority's fixed file_sha256. With100native
   and interpreter this is113files; missing-input deliberately removes only
   ROOT-PINS. No authority self-hash or new source acceptance.

## SSH identity is fixed, no public-IP substitution or trust downgrade

Only instance i-08d2a40f272ee9fa2, private172.30.0.72,
hostname ip-172-30-0-72, retained disk vol-0574e0fa5aed1f5e3.
The local file ROOT-SSH.known_hosts in this prep directory has SHA
82c1f45824057dbd28515abf06e4d6f12aec851e6bd4b80fd23ff3911f41f442,
one ed25519 public key copied from the existing exact-host cache.
Fingerprint: SHA256:G1b7nYJvE9srs8y4ivi6D6jeH13GHH+f9czmYnIMcU0.
This is a public host key, not a private credential. Its exact content:
172.30.0.72 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILotfjVhIXRZ8y+pdkIgJqpSaeNvTxKVKwpmk9Oygigs

ALL ssh/scp/rsync transports use ubuntu@172.30.0.72, identity file
/home/ubuntu/.ssh/jc2-fleet, BatchMode=yes, ConnectTimeout=10,
StrictHostKeyChecking=yes, HostKeyAlgorithms=ssh-ed25519, and
UserKnownHostsFile=/home/ubuntu/jc2/box/f10-r1-quotient-remainder-generic-execution-r2-prep-20260910/ROOT-SSH.known_hosts.
Do NOT select the new public IP, use ask/no/accept-new, edit a key cache,
remove a key, or change a fingerprint. Any mismatch/failure is STOP. Before
deployment record current vendor/id/hostname/boot/namespace; after byte-identical
staging and before any payload verify all declared runtime pins, as in the
template. Root activates this attempt
with a fresh start only AFTER old executor terminal and the new backup active.

No source data or acceptance, no code/helper/dispatcher/engine/cap change,
no local mathematics ANYsize, no protected inspection, no broader corpus,
no optional smoke test or automatic retry. Seven exact preflight controls
then at most ONE generic mathematical invocation remain mandatory. Announce
just before generic; capture actual CAPRUN/child in the SAME launch call and
send root exact unit/PIDs promptly. Do not wait for root approval to finish
the already registered batch. Terminal custody and FIRST runtime/migration
review still required before promotion/actual-source use. Retain all failure
artifacts; all owned writers IDLE before the original task cap.
