# Manual static controls — no execution

This packet is `INTERNAL-UNREVIEWED`, disabled and unbound. These are source-level controls, not test results.

1. **Retained sessions:** `admin()` consults the kernel's stored operation record. It resumes only the same recorded session and exact original stop; a terminal, unknown, or handle-free record refuses relaunch. After a stop it permits collection only, then refuses successors.
2. **LF framing:** `oneHash`, `jc2HashReadback`, and the kernel checksum constructor require real terminal LF bytes. No `"\\n"` checksum payload is constructed.
3. **All-five exclusive staging:** the unchanged banked receiver's `--preflight` checks all five names under `set -e`; each later stream ends at `dd conv=excl,fsync oflag=nofollow`. The coordinator wraps both local pipe and SSH in the original-stop administrator. There is no retry or overwrite path.
4. **Fresh coordinator state:** every entry hashes the exact receipt and native-list paths and compares both digests. It separately queries exact configured timer and service property censuses immediately before the phase; install queries them again directly before the unchanged installer.
5. **Late values are DATA:** only the unchanged admission stdout is parsed for PID/start/invocation. The fixed observer, installer path, and fixed release guard receive those three values as positional arguments; no late source generation or interpolation occurs.
6. **Bounded blocking operations:** every SSH, receiver stream, installer, systemd query and release guard is inside `jc2Admin`'s absolute-stop timeout. The final FIFO open/write remains inside the banked two-second timed child.

Authority boundaries are literal: ADMIT only persists admission data; ATTEST is a distinct ROOT-selected entry that creates the binding decision; INSTALL calls the unchanged supplied-input caller and verifies all fourteen outputs but transfers only four installer inputs plus native list; RELEASE is a distinct ROOT-selected entry. The nine authority JSON files remain expected dispatcher runtime outputs. No entry selects a worker, launches science, retries, automatically releases, rebases a clock, or increases a cap.

Preholder binding must replace every placeholder in the config and three fixed shell consumers, install the historical admission and the bound consumers before holder start, compose the four entry sources from the exact banked kernel and reviewed tail, and freeze all ten identities in `PREHOLDER-MANIFEST.sha256`. Any residual placeholder, absent fixed path, digest mismatch, non-exact timer/service property map, or missing executable no-writer/namespace/cgroup predicate is a STOP, not READY.
