# ROOT first-failure intake — concrete composition remains GAP

Collected2026-09-12 05:16--05:22UTC after independently COMPLETED native
Sol state. Custody-FIRST1016b946ec3432e36a5a03b6c7eaf7e00d64fe2f4ee8ae5f537b1174e56308ad;
first04:57:46.654447877, terminal/final05:14:02, original05:24/05:27 met.
About975 author-wall seconds, not billing/CPU/tokens. Report
c857006eca8c32996ef8cfe4069d688777eb6bc069ec29fe2c70331dbbeecd91,
manifest07b6b147b3b8ab922b9a50e26a1826e8d1567813254cee3bf4f4cea62bd7bfda,
body5ce2f64bce97a7507e67f76a7a438023f28c89cffb1ac7fb56637fc9ac903f5a,
PINS84228117177116f04fae180e007fb35e317df1ced0c3b907abffa9d5061d2951.
Custody/PINS WHOLE after matching pins; all17 input and seven new-artifact
pins matched before report/source body reads. Expected-basis/manifest VERIFY
passed. Complete final report/manifest and all seven new sources/controls
were read. All17+7 postpins matched again05:22.

This is a bounded FIRST-FAILURE source review, NOT complete readiness
qualification of every dependency. ROOT freshly read the unchanged kernel,
receiver, FIFO writer, caller, stage_patch, historical installer/admission
WHOLE. Binder lines1--200 were selected reads; no new WHOLE binder claim.
Other historical input bodies are prior context/current-pin reuse, not a
fresh end-to-end review. No code, syntax, AST, import, mock, fixture,
scientific runtime, SSH, AWS or worker action was executed.

## Disposition and exact defects

The packet makes useful concrete progress: four explicit entry functions,
fixed late-data observer and release consumers, bounded transport, and no
authority preinstallation. Its COMPLETE_INERT_COMPOSITION label does not
establish an executable complete flow. Keep DISABLED/UNBOUND/GAP and do not
spend a launch-readiness gate on the following known defects.

1. **Composer's self-rejecting token.** Its first grep rejects any
   JC2_*_PLACEHOLDER token anywhere in its own source. The intended
   JC2_ENTRY_SELECTOR_PLACEHOLDER remains literally in its later sed and
   grep commands. Replacing ordinary metadata/SHA placeholders therefore
   still exits2 before any entry selection. The selection mechanism needs
   a token/guard contract that can actually pass, not a silent bypass.
2. **Wrong-host manifest verification.** The ten-item manifest contains
   five remotely installed script paths, but loadConfig runs local
   sha256sum -c on it. Missing coordinator paths cause refusal; coordinator
   copies, if present, would authenticate the wrong files. Split and bind
   the local and remote inventories and check each on its actual host.
3. **Unproved exclusive local records.** addRecord emits apply_patch
   Add File with no prior absence/private-parent check, while claiming
   exclusive publication. The tool contract supplies no O_EXCL promise.
   Existing admission/observation/decision records are not a permitted
   overwrite target. This is a missing control, not an executed mutation.
4. **Cross-stage holder binding.** validateObservation checks holder fields'
   syntax/range but never compares them to parsed admission DATA d. The
   latter is loaded and then ignored in INSTALL; ATTEST/RELEASE likewise
   lack the direct equality. Make the common holder identity explicit.
5. **Registration expected hash is sampled from the target.** RELEASE
   hashes the current remote registration and passes that same digest as
   expected. It does not consume an exact commitment from accepted INSTALL.
   Agreement with the current staging copy/manifest cannot establish
   continuity with the originally accepted candidate. A changed matching
   staged/installed pair is a source-level countercontrol, not observed
   tampering. Preserve a separate install commitment and compare against it.
6. **Incomplete fresh release predicates.** release-check omits the
   observer's memory.swap.max=0/cpu.max.burst=0, holder argv/exe, root-only
   migration surfaces, and several leaf type/subtree/descendant checks.
   Its full-guard claim needs those actual checks or a precise qualified
   subsequent guard covering each; an unchanged remaining property does
   not check an omitted one. This does not refute the scientific evaluator.
7. **Historical rebinding inventory is incomplete.** The supplied admission
   has literal expired September11 dates, base paths and units, not only
   placeholder tokens. A future bound copy must account for every relevant
   literal and its fresh original clock; replacing placeholders alone is
   not a complete new preholder binding procedure. No old batch is revived.
8. **Entry-level resumption remains unqualified.** Rerunning a whole entry
   encounters the already-used config operation and a newly computed
   seedStop before reaching a later interrupted handle. The banked kernel
   retains handles, but that alone does not prove the new four-entry API
   can resume or collect a partially completed phase. No restart is allowed.

These are static counterchecks/gaps, not runtime observations or an
exhaustive absence-of-other-bugs claim. No original algorithm/guard/cap is
retracted. In particular the dispatcher must still create its nine actual
authority files itself; none may be preinstalled.

## Selected next step

ROOT explicitly selected a new Astra PRIMARY correction, first actual
05:22:01, reserve05:40/HARD05:43, under
box/caprun-lateflow-corrected-astra-20260912 and its named transactional
report. This is new bounded integration work with the above concrete
source controls, not an extension of Sol's terminal or ROOT's closed
04:30 task. Preserve old artifacts; no new framework, worker, runtime
test, source run, authority bypass or cap increase. Fable independently
reviews the mathematical111+49 proof in parallel. ROOT owns both terminal
collections; this intake is not a launch authorization.
