# V6 preallocation source packet: integrated credential change

ROOT, September13,2026. INERT / UNBOUND / UNEXECUTED. This consolidates
already-reviewed bytes; it is NOT an actual registration or qualification.
No worker or science admission has been selected by this packet.

## Binding procedure and exact change

Use the complete existing operator procedure
box/f10-mixed-v3-ready-root-20260913/OPERATOR-PROCEDURE.md,
SHA6b1128f840b1ae5c30a8e5ef03f11c9a2facb469a9f0c95280c066f72737113f,
and its already accepted integration FIRST03d166f4bc7657cbe9c5982a778339255630936bcede43ba0e7c5711278f18ac.
All four metadata stages, mandatory live dummy, truthful qualification DATA,
16-field runtime registration, six science phases, native/physical checks,
whole3540second/CPU80%/32GiB/256MiB caps, durable evidence and retirement
remain binding. That procedure's old 'awaits review' label is historical.

Exactly two source substitutions from that historical procedure:

- DUMMY.template.md98260cfd0ce468aca393128a2deb86cb162bc1c1cbe75591bdebb17c4669dbea;
- science-unit.template.sh783a33f13d0286165c7279c6556a1a1681bab9a9dc4dbcbc9093710050d841c0.

Both carry the SAME four capabilities in AmbientCapabilities as in the
existing bounding set. Their focused different-model conditional STATIC
FIRST is box/f10-mixed-ambient-credential-sol-20260913/ROOT-INTAKE.md,
SHA5bdfd1411d5bc231d4634f1af73348edb14c17234bf33017ccc8b6d4b4ccacb5.
Normal set-UID fixup, ordinary executables without privilege-bearing file
attributes and no independent syscall-policy denial are required. Check
actual child identity/NNP/CapEff0 rather than inferring it from properties.
No CAP_SETPCAP, altered scientific source or same-UID dummy-only shortcut.
This is a capability-side candidate, NOT an attribution of V5's EPERM.

Retain V5's actually successful concurrent observer+PREPARE scheduling.
The accepted observer40bd1116e4cb1bfbfc684afe1d0b0a334af10ed0b719f528efa4ee4a41b483d9
uses both exact limits and optional horizontal trailing padding; FIRST
a945acd17678fe3500c05a80b49b2d6067f7c7f3988eb74b545fd13288ff841b.
Start both calls in ONE orchestration, with separate bounded streams,
actual starts/ends/exit values; no model turn between starting preparation
and starting the observer. Fixed PATH/LC_ALL=C, foreground SSH timeout60
and ConnectTimeout10; timeout is STOP, not a remote cleanup proof or retry.

## What is actually ready

The sixteen files in PINS.json are exact mechanical copies into this new
directory, whose target was absent. No old physical derivative, enabled
registration, worker identity, native result or clock was copied. The seven
stage sources are the unchanged licensed computation. The four metadata/
observer templates and PREPARE remain placeholder-disabled. The runtime
registration remains enabled=false with unbound physical fields. The
cloud-config still disables package update and upgrade.

Use the inherited literal remote base and v3 unit/stage names on a FRESH
worker, avoiding unrelated code edits. Place actual new derivatives and
all custody under a separately registered V6 execution directory. Bind
only the declared physical/source/clock placeholders, hash/WHOLE-read each
actual derivative and compare complete local/remote bytes before execution.
One future dummy admission is fixed after successful metadata; it is never
rebound if missed. The whole original25seconds includes final census/cleanup.

## Allocation boundary and next actual actions

Before allocation recheck BOTH campaign selectors, Standard quota/current
usage, AMI/subnet/SG, disk custody capacity and executable availability.
The03:03UTC read-only API checks found Standard quota1920, the selected
x86_64 EBS AMI available and subnet available; these are not an allocation,
current-usage check or permanent free-capacity assertion.

Register ONE r7i.2xlarge8vCPU/64GiB with root100GiB gp3 retained from launch,
termination enabled, personal/us-east-1, jc2-fleet key, no instance profile.
Freeze original qualification STOP about20minutes after the decision,
TERM five seconds earlier and worker RETIRE five minutes after STOP,
BEFORE the API allocation. Bind the returned ID and arm an independently
verified HQ exact-ID retirement within60seconds. Never touch HQ
i-0252f535410c26ebc. No old worker/IP/clock is reusable.

Then: terminal cloud-init and actual identities/packages/files; bind/transfer;
concurrent observed metadata preparation; ROOT complete receipt/pin/kernel
checks; ONE fully bound dummy; only after genuine success create qualification
and enabled16-field DATA and run the existing six scientific phases. No
package installer, preparation while billed beyond this mechanical binding,
new controller, source repair, cap rise or automatic retry is selected.

Only ACTUALLY live qualified science may replace the original retirement
ownership, after the new exact-ID timer at its ORIGINAL deadline+5minutes
is armed and actual process/kernel controls pass. Every other outcome retires
on the qualification clock, after explicit evidence capture and durable
SHA plus independent whole-byte transfer. EBS recovery preserves failures.
No evidence-volume deletion is authorized.

All sources remain unexecuted by this preparation. The exact scalar
certificate still targets every integer r, not a finite residue sample.
The new manual results neither change its source nor license full-source
comparison, REG, all-F10 or JC2. No new mathematical or runtime review claim
is made by mechanical consolidation. No active ROOT lease remains after
normal finalization of this document.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5605`.
- Body SHA-256:
  `8d074fef6d6cbba4cf75c552f09da151c994c552d46fa898eb8f517519818c4a`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
