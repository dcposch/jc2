# Bounded independent co-research — radial action and finite canonical graph

ROOT September12,2026,12:50UTC. Astra primary co-research, not FIRST.
Original reserve13:02UTC / HARD13:05UTC NEVERRESET; finish early if decisive.
Basis0d39df3c9fd69c939a8420c54d03228b9077777d. One scientific report only:
xmodel/radial-action-finite-graph-astra-20260912.md.

Hash/read COORDINATION.md first WHOLE, expected
33cfa6106972fe557a95ca81a9a05bb72300a82b2fbb7908915441cf75377597,
then this TASK WHOLE with invitation pin. Exactly these two scientific
inputs; no corpus, peer output or linked body. Use the unchanged existing
artifact_finalize begin/close/finalize/expected verify transaction; write
only the leased private partial with apply_patch. Administrative PINS and
custody files in this TASK directory are allowed. Do not edit TASK, shared
ledgers, prior reports or their manifests. No descendants or promotion.

ROOT has two MANUAL/UNREVIEWED candidates. Reconstruct them independently
and expose the EXACT remaining source gap; no automatic stronger theorem.

A. RADIAL FIELD GENERATION. For f,g in C[x,y], J(f,g)=1, put
alpha=(x dy-y dx)/2 and choose polynomial T with dT=alpha-f dg.
Let E=(x partial_x+y partial_y)/2 and K=C(f,g,T). Expected identities:
E(f)=f+T_g, E(g)=-T_f, E(T)=f*T_f. Unique derivation extension over the
finite separable fields should give E(K) subset K. Finite TOTAL-degree
weight interpolation in E then puts the linear homogeneous pieces f1,g1
in K. Their determinant is J(f,g)(0,0)=1, so they span x,y over C and
K=C(x,y). Constants of f,g should not obstruct this. This is a DIFFERENT
potential from dS=xdy-fdg; do not claim the original S proof had a gap.
The older S theorem already passed Fable; no dependence on it is needed.
Give a meaningful punctured/rational negative control and full-plane sanity
checks. No polynomial ring generation follows from the field projection.

B. FINITE CANONICAL GRAPH. Choose determinant-one linear source coordinates
with f_m(0,1)!=0, m=deg f. Recompute T for alpha in these coordinates.
For integer N>max(deg T,2), set H=T+x^N. Leading forms f_m and x^N
have no common point on P1. Prove algebraically, without merely asserting
properness, that (f,H):A2->A2 is finite. A filtered/graded finite-module
argument from the two leading forms should suffice, retaining every lower
term. Hence R=C[x,y] is finite over B=C[f,g,H].
The polynomial symplectic shear X=x,Y=y+2N/(N-2)*x^(N-1) should satisfy
(X dY-Y dX)/2=alpha+d(x^N). Thus H is radial-canonical in the NEW
polynomial source coordinates, and A should give Frac B=C(X,Y)=C(x,y).
If sound, j_H:A2->Spec B is finite birational surjective, with R its
normalization. Check all constants and coordinate directions. Distinguish
finiteness of (f,H) and j_H from finiteness of F=(f,g).

C. GLOBAL GAP. Supply the strongest honest conclusion, then attack the
tempting leap finite j => j isomorphism/Z normal/F invertible. A simply
connected smooth source does not make a finite birational map into a
nonnormal target an isomorphism. Generic finite primitive graphs were
already available; this candidate only keeps the auxiliary a canonical
area potential. No BGV theorem is an input. Do not identify a formal,
punctured or abstract control with a polynomial Keller map on A2.
One short consequential observation is welcome if actually derived, but
no new family/degree/gauge/control farm. Mark remaining hypotheses GAP.

Manual mathematics only: NO scientific/CAS/interpreter/import/AST/test/
syntax/helper execution, network/AWS/SSH, agent or protected-tree operation.
Inert text/hash/current clock and existing administrative finalizer allowed.
NEVER enter/enumerate/search/read/status/control jc2-lean or its public
mirror. ROOT handles primary literature. All edits apply_patch only; no
shell redirection/heredoc/tee/file writes. Max body about10000bytes. Own
WHOLE readback and all input postpins before unique BODY-END appended LAST.
No charge_basis or canonical OPEN. Final report/manifest WHOLE, expected
verify, custody SHA-FIRST, and ALL WRITERS IDLE with actual UTC before FINAL.
No changes after idle. No JC2 resolution is asserted by this task.
