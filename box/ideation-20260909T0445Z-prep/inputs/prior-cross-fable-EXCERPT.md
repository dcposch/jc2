# EXCERPT — prior-cross-fable-EXCERPT.md

Original: xmodel/ideation-20260909T0310Z-cross-fable5.md
Original SHA256: 9535e7ff264bc231a13ed3bb35b05d1ad4744ca1b6fd8b9bcefaae379acdf989
Exact source bytes [8315,17273); scope: EXCERPT: self-correction and competing mechanisms; complete synthesis owns dispositions.
All historical/provisional statements retain their original lifecycle; current15x/y supersedes older priorities.

## 2. Question 2: primitive/proper-power warnings versus my blind's automatic reconstruction (RBU)

**What survives of RBU.** The wedge identity survives verbatim in scalar-reference form. For any a(T)=T^M+kernels, b(T), with Euclidean division b'=a'q+ρ in K[s][T] (deg ρ≤M−2) and A=a(R)+F, B=b(R)+q(R)F+G, I re-derived [A,B]=[R,T]+[F,G] with T=a'(R)G−ρ(R)F−(q'(R)/2)F², exactly the coordinator's Card B display; at (M,N)=(3,5) it is the 15w T with ρ=τR+δ. Replacing H=h^e by its reduced base h turns the exponents into (3e,5e), not (3,5): my blind's sentence that "the constant-J reference/wedge/kernel construction applies to every plane Keller pair" is true for the identity and FALSE for the step ord G≥2j. I withdraw that step.

**Where ord G≥2j breaks, by order ledger (hand, no expansion).** Scalar coefficients are weight-homogeneous: the T^k coefficient of ρ has s-order (N−1−k)D0, so the lowest ρ term is r_{M−2}h^{M−2}F_j at order (2e+1)D0+j. In the e=1 framework that order is 3D+j>2j because j≤3D−1, so ρF never precedes the G target and the kernel-shift induction closes. For e≥2, j can exceed (2e+1)D0 (j ranges to 3eD0−1), and then ρF enters BEFORE 2j. The order-l equations for l<min(ord G,2j,8eD0−2) read [h,(ρ(R)F)_l]=0. At the lone lowest order this gives r_{M−2}h^{M−2}[h,F_j]=0; with the normalization absorbing every K[h]-part of the residual into a(T) one has F_j∉K[h], hence [h,F_j]≠0 for closed h, and r_{M−2} is FORCED to vanish, and so on upward while the ρ term is alone. The induction genuinely stalls when a fresh ρ coefficient r_k first shares its order l=(N−1−k)D0+j with ord G: the equation becomes [h,Mh^{M−1}G_l−r_kh^kF_j]=0, so Mh^{M−1}G_l−r_kh^kF_j∈K[h], and the kernel-shift trick cannot remove a G_l of the shape (r_k/M)F_j h^{k−M+1}. So the honest general bound is ord G≥min(2j, j+(2e+1)D0) with a two-branch case split (all early ρ coefficients vanish, or G inherits F's shape at the first shared order). Copying "G≥2j" from e=1 by notation is invalid, exactly as root and Astra warned; late contact then means ord[F,G]≥j+ord G>(8e−1)D0, which is 3j>(8e−1)D0 only on the first branch.

**H0=xy is not an open Keller control.** From the charged primary lines 360–394: Theorem 2.6 applies to P∈L^(l) with (ρ,σ)∈V_{>0}, v_{ρ,σ}(P)>0 and [P,Q]∈K^×, and its clause (4) states st_{ρ,σ}(P)≁(1,1)≁en_{ρ,σ}(P). At (1,1) an ordinary constant-J P with sole leading monomial λ(xy)^k has a monomial leading form, so st=en=(k,k)∼(1,1), contradicting (4). Linear coordinate changes preserve total degree and constant J, so no Keller pair has top form a power of L1L2 with two distinct linear factors of EQUAL multiplicity. My blind's Card B "two-simple-root base case H0=xy, (A,B)=(8e',12e')" is therefore dead as a control; I withdraw it. Clause (4) does NOT exclude x^r y^s with r≠s (st=en=(kr,ks)≁(1,1)), so the poststate's caution stands: only the balanced example is excluded.

**First honest UNEQUAL proper-power test (one, no expansion, no Keller pretence).** Object: closed h=x^r y^s, gcd(r,s)=1, r<s, D0=r+s; scalar references a(T)=T^{3e}+(kernels of index ≤3e−2), b(T)=T^{5e}+lower, over K[s]; F with first order j and F_j∉K[h]. Smallest e: if (B)'s Cor 7.9 import is accepted, every actual pair has d>2, so e=3 is the first admissible exponent and e=2 is a non-source method control only. Test: build the scalar-order ledger of ρ=b'−a'q at (M,N)=(9,15) by hand (coefficient orders (14−k)D0, k≤7), list the orders (2e+1)D0+j=7D0+j,…, and decide for the window j∈(7D0,9D0−1] whether the lone-order forcing above exhausts ρ before the first shared order with 2j, or whether the inherited-shape branch is unavoidable. Output: a correct replacement inequality for ord G, or the exact first competing term. It is a theorem about a hypothetical source class; its only known member-shape, the physical F2 lift (e=5, h=uv⁴), is already retired by 15q, so NO attached unretired family exists. Do not feed it arbitrary polynomial reference data as if they came from a Keller pair.

## 3. Question 3: strongest proof and counterexample directions beyond the retired exact sources

**Two different statements, kept apart.** Accepted 15w is a LATE-CONTACT BOUND: every actual source with the exact closed-H/constant-J/3:5/D≥3 interface has canonical j≤⌊7D/3⌋. Provisional (B) is a WHOLE-CLASS EMPTINESS statement: that interface has no actual member for any D>0. The second, if it passes, dominates the first for source purposes and leaves it as a reusable abstract instrument. The campaign should therefore not build any early-contact theorem inside the closed-H class: it would have no client. Astra's blind Card 1 reaches (B) by a second route (McKay–Wang two-factor bound + closedness gives h=x^r y^s with gcd(r,s)=1 in linear coordinates; an upper-envelope lemma from Theorem 2.6 and Remark 2.5 supplies the rectangle without ML; then Def 4.3, Prop 5.20 and Cor 7.9 give gcd(r,s)>2, contradiction). I re-derived that upper-envelope lemma from the charged primary lines: for ρ>σ>0 coprime, a polynomial Euler element of weight ρ+σ can only contain xy and, when σ=1, y^{ρ+1}; a monomial F contradicts Remark 2.5 on a nonmonomial face, and F=λxy+μy^{ρ+1} has en(F)=(0,ρ+1), which is neither (1,1) nor parallel to the positive corner (a,b), contradicting clause (3). So the two routes to (B) are independent at the entry (ML rectangle versus McKay–Wang plus Euler envelope) and share only Def 4.3, Prop 5.20 and Cor 7.9. That shared trio is the exact trust perimeter of (B); a single primary correction there would take down both routes at once, which the gate should record.

**Sol's ≥N threshold lemma: abstractly valid, zero live payoff.** Statement: R combined-homogeneous of degree D, R(0)=H with ker[H,−]=K[H], T of combined degree ≤N, ord_s[R,T]≥N ⇒ [R,T]=0. Proof check: the pieces T^(n) of distinct combined degree n≤N have brackets of distinct combined degree n+D−2, so the order bound passes to each piece; for a piece with first order ℓ<n the order-ℓ coefficient [H,T_ℓ]=0 forces T_ℓ=aH^i with iD=n−ℓ, and subtracting a s^ℓ R^i raises the order without changing the bracket; at ℓ=n the piece is a scalar times s^n. Finite induction. Sharpness control H=R=uv, T=s^{N−1}u: [R,T]=−s^{N−1}u≠0, so N−1 fails. This is a changed-object countercontrol, not a predicate flip. Application: in 15w's framework N=7D, ord[R,T]≥min(8D−2,3j)≥7D when 3j≥7D and D≥2, so the equality case 3j=7D is also excluded and the bound sharpens to 3j<7D, i.e. an improvement only when 3|D. Payoff: a method control and a tidier abstract lemma; no live client if (B) passes, and the improvement is at most one unit of j. Not a new client, not a card.

**Strongest proof direction.** If (A) passes: the published-style frontier moves by the same three-step pattern (normalization to a standard pair → GGHV chain/table selection → a campaign receiver exclusion of the selected row). The next finite target is the smallest GGHV row above 125 in maximum degree, each needing its own receiver theorem; the all-degree bridge is the proper-power scalar-reference program of §2 with e≥3, whose value is exactly that it addresses the class (B) leaves open. If (A) fails at the ML arrow: the McKay–Wang route still gives the rectangle for two-factor H, and the one-factor case H=L^D is the residual obligation; if it fails at the table import, F2 selection is the missing arrow. No promotion of either follows from this cross.

**Strongest counterexample direction.** With (A) passing, the complete guarded B0 75/125 envelope (remaining-m90 interface) is existentially a unit, the six F2 receivers are already units, and no CE client of degree ≤125 exists; the coordinator's Card C (source-completeness gate) reduces to "consume (A) and stop". A CE lane needs a NEW complete source with its own acceptance theorem from a GGHV row above 125, or an object outside the standard-pair normalization altogether, and by ML + Prop 5.20 there is none of the latter. K16's b·B_m·η_m locus and b=0 boundary stay the only structurally different falsification target (Sol's design), with no new discriminator here and no launch. Witt/char-p remains a discriminator, not an endpoint.

**Attack classification, as required.** The x−y² versus (x+y², y) cancellation example is a changed-object control on the rectangle premise, not a predicate flip. The (xy)^k exclusion is a factor-identity consequence of a printed theorem, not a source implication. The §2 ρ ledger is evaluation of a few order equations, not a finite ideal decision. The §1 table filter is a finite hand decision over ten printed rows whose exhaustiveness is imported. (A)'s "unit" consequence for the B0 envelope is existential nonexistence of points, not a computed certificate.

