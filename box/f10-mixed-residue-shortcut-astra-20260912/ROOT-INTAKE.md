# Mixed-scalar shortcut intake, determinant correction and rejected ratio

ROOT, September 12, 2026. MANUAL / UNPROMOTED. This is not different-model
FIRST, a source-code review, a unit certificate or a JC2 result.

## Terminal custody and surviving simplification

Independent agent listing confirmed termination before custody-first intake.
Actual first action23:35:48.949689244, final IDLE23:47:33.712449310, before
original23:52 reserve/23:55 HARD. No deadline was reset.
Custody f0f7b4f8742ba6e722b66f8a1db944ea6df75de1bb32de2d6ab46367a64b148c,
report e8bf9ecfbfa5e2105afedcaaffb1e7dd40def8dc5caa9576b4cf5fdf760835de,
manifest c43751bf7617d630c0e1f57a1b2f249f2734b36fe82e6cae8f35ed2d2d33f948,
PINS c6f0d518fc68443ad2fdd508342ac700b6a6989a50e74e21436c3449bd44365d.
All four charged inputs, the unchanged finalizer and owned pins reproduced;
expected basis/manifest verification passed. Custody, report, manifest and
PINS were read WHOLE. No live partial was consumed.

The explicit universal identity z*w*Delta*B(z,w)=-e(z)^T*G*e(w) is a useful
formation simplification. ROOT independently checked the truncated-power
defect, W=Delta/phi'^2 at the three roots, and all five moments. The last
two moments follow immediately from the cubic recurrence. Only six top
coefficients and a fixed polynomial matrix are required. This does not
prove the resulting distinguished pairing is a unit, reduce septic rank,
or establish an actual performance improvement.

ROOT's complementary monic-division/reciprocal-trace derivation is frozen at
xmodel/f10-mixed-reciprocal-trace-root-20260912.md,
SHA2566c357b0b7fee0ba049c2a35960a111b97642aa72f24f637aed4efbcaa959ba61,
manifest3c48543dafd19e7a27046e33d3e34648d5153369cb77ac83e923824356972e2e.
It was sealed and fully verified/read before peer intake. The two notes
shared ROOT's initial residue/truncation suggestions; they are not blind
independent evidence or a different-model promotion chain.

## Binding correction: det(G)=-Y*Delta^2

The peer's displayed polynomial matrix is retained, but its claimed
determinant -Y^3*Delta^2 is WRONG. The correct determinant is

    det(G)=-Y*Delta^2.

In the peer's own factorization, product weights is -Y^3*Delta, while the
squared basis determinant for (1,a,Y*a^2) is Delta/Y^2. Their product gives
the corrected formula. An independent small check at X=0,Y=2 gives

    G=[[8,-12,36],[-12,18,4],[36,4,-12]],
    det(G)=-26912=-2*116^2,    Delta=-116.

Both Y and Delta are already units in the actual source, so nondegeneracy
and the main formation identity survive; neither determinant expression
would prove a particular pairing nonzero. ROOT notified the author and
preserved the frozen erroneous bytes. No repeated matrix/control farm.

## A concrete refutation of ROOT's proposed universal scalar ratio

The suggestion B_r=lambda(t)*Delta*J, with lambda in Q(t) and
J=det(d(d6,d7)/d(X,Y)), is not merely unproved: the proposed universal
leading-algebra identity is false. The following check is outside actual
r, and is used ONLY to reject this rational identity, not to claim an
actual-r scalar zero, unit or source point.

Set t=-1 and phi=(1+u)(1+b*u^2), so X=Y=b. For b=1 or b=i, i^2=-1,
phi divides 1-u^8 and d=(1-u^8)/phi has degree five and constant term one.
Thus phi^-1=d modulo u^8, d6=d7=0 and Y*d5=-1. The two exponents are
alpha=-3, beta=5. Direct differentiation of the leading equations gives

    J=t^2*(v4^2-v3*v5),    v_k=[u^k]phi^(t-1).

At b=1, (v3,v4,v5)=(0,2,-4), Delta=-16 and J=4. At b=i,
(v3,v4,v5)=(4(b-1),2-6b,8b), Delta=8 and J=8b.

ROOT computed B DIRECTLY at the three roots, not by assuming the peer's
matrix identity. For both choices the roots have eighth power one, so
1/a^15=a, and the defining residue formula is

    B=-sum_(phi(a)=0) a*T_alpha(a)*T_beta(a).

At b=1, T_alpha(-1)=32 and T_beta(-1)=-80, giving contribution -2560.
At the other roots a^2=-1, the truncations reduce respectively to
-8-8a and -84-84a. Their combined contribution is2688, hence B=128.

At b=i, T_alpha(-1)=-128b and T_beta(-1)=40(1+b), giving contribution
5120-5120b. At the other roots a^2=b, their even/odd decompositions are

    T_alpha(a)=(-40+32b)+(24-64b)*a,
    T_beta(a) =(16+60b)+(-16+60b)*a.

The coefficient of a in their product is2944-2496b; the two roots therefore
contribute -2b*(2944-2496b)=-4992-5888b. In total B=128-11008b.
Independent substitution into the peer's explicit matrix gives the same
two B values; the determinant correction is not used by either calculation.

The two ratios at the SAME parameter value are consequently

    b=1: B/(Delta*J)=-2,
    b=i: B/(Delta*J)=-172-2i.

They are unequal. A putative rational lambda(t) cannot evade the check by
having a pole at t=-1: the b=1 point has J=4, so the two leading equations
admit a local formal lift in t+1, with Delta*J a unit. On that branch the
polynomial B/(Delta*J) is regular, forcing lambda regular with value -2.
The b=i branch, also with nonzero J, then contradicts the same identity.
Equivalently, the usual coefficient-by-coefficient formal implicit-function
construction uses the invertible two-by-two Jacobian at each point.

This rules out that specific universal rational scalar multiple. It does
not rule out a more general source-dependent unit factor, determine any
actual integer-r value, or make the scalar problem harder than before.
The same representation and source equations remain available.

## Decision, interfaces and cost

Retain the small formation identities as MANUAL/UNPROMOTED alternatives;
drop the simple discriminant-times-Jacobian proportionality shortcut.
No new code, source repair, worker, model-review lane, authority or enlarged
cap is selected. A different-model gate would be needed before promoting
these new identities or using them as load-bearing implementation inputs.
The existing reviewed producer/checker and the selected all-r unit question
are unchanged. Do not mistake a nonsingular pairing or this out-of-range
identity refutation for a solution of that question.

The theorem-interface check yields NO CLOSING HIT: invertibility of the
cubic trace form does not supply the one required nonzero pairing, and
scalar unitness would itself still leave actual comparison/REG/source-zero
gaps. No global ranking change or automatic FULL reset follows.
Native Astra elapsed first-action-to-IDLE was704.76seconds, not billing,
CPU or token use. No Fable or AWS computation ran in this research interval.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `6469`.
- Body SHA-256:
  `36878db2eebec445598e616276c40859f2790d5c6a3e586a34e4055eeba7c6b5`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
