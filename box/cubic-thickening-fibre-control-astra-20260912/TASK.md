# Bounded manual discriminator: a cubic thickening fibre-length inference

ROOT task September12,2026. You are Astra independent co-researcher.
First action acknowledge the exact TASK hash, actual UTC, then fresh WHOLE.
Sole input THIS TASK. No other files, corpus, sources, peer reports, web,
scientific/CAS/code execution, imports/tests/dummies, AWS/SSH/Git/process,
new agent or protected project access. Documentary date/hash/text reads,
apply_patch authoring and existing artifact transactions only.

Write only your leased partial/final
xmodel/cubic-thickening-fibre-control-astra-20260912.md,
its existing artifact transaction, and own box PINS/custody. No shared edits.
Use ops/artifact_finalize.py begin/close/finalize/verify with basis
0d39df3c9fd69c939a8420c54d03228b9077777d. First skeleton WITHOUT marker.
Original reserve00:50/HARD00:53UTC, no extension, report <=1500words.
No following task or promotion is authorized. At completion WHOLE readback,
input postpin, explicit scope/quantity/cheapest-test/collision check; marker
LAST, then close/finalize/expected-manifest verify and final WHOLE read.
Custody-FIRST handoff includes terminal all-writers-idle UTC and all hashes.

Motivation (not an accepted mathematical premise): ROOT is reading an
external cubic-cover argument. It appears to infer length >=2*2=4 for a
fibre of a doubled smooth A1 ramification curve over a singular branch
point, using that its reduced normalization fibre has length >=2 and the
thickening is abstractly A1 times dual numbers. Does that product structure
alone justify multiplying lengths? No source citation is charged here;
test the explicit algebra independently, without a verdict on the paper.

Over C, put S=C[a,t], B=C[a,b], and let B->S send
b to -t^3-a*t. This gives a finite degree3 map Y=SpecS->SpecB.
Let r=a+3t^2, C=V(r), Delta=4a^3+27b^2, and Cplus=V(r^2) in Y.
ROOT proposes:

1. Y is smooth, C is a smooth affine line and the ramification divisor.
   Delta pulls back to r^2*(4a+3t^2). Thus Cplus maps to the cusp
   Z=V(Delta), and C->Z is its normalization, parametrized by
   (a,b)=(-3t^2,2t^3).
2. Cplus is abstractly SpecC[t,epsilon]/epsilon^2 via epsilon=r,
   but its B-action is a=epsilon-3t^2,
   b=2t^3-epsilon*t. It need not be the product B-action through C.
3. Over the origin, C has fibre length2, while Cplus has length3,
   NOT4: S/(r,a,b)=C[t]/t^2 and S/(r^2,a,b)=C[t]/t^3.
   The full finite cover's fibre also has length3.

Independently verify or refute EVERY assertion and identify the exact
extra base-linearity/flatness hypothesis needed by a multiplicative-length
argument. Do not assume a splitting of the nilpotent thickening as a
scheme is a splitting over Z. Can a different embedded doubled curve
supported on C repair the argument at the cusp? State any precise limit.

Essential control: Y_et=D(r) has nonconstant unit r, hence cannot contain
an open A2 or receive a dominant regular A2. This is NOT an actual Keller
map or intermediate Keller-source example. It can refute the bare local
algebra inference, not the external theorem, the classical exclusion of
Keller geometric degree3, arbitrary cubic blocks or JC2.

QUANTITY: exact scheme-fibre lengths 2 and3 and validity of multiplying
by thickening degree2. CHEAPEST TEST: manual quotient substitutions, under
15 author-wall minutes planned, not measured scientific runtime. No new
canonical OPEN. If any step is uncertain, state GAP and seal on time.
