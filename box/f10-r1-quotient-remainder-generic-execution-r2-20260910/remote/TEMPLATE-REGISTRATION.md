# ROOT registration: one finite generic batch, direct unchanged CAPRUN

Issued2026-09-10 after qualified STATIC17zh. This is a single operational
registration, not source acceptance or permission to retry/repair a failure.
The existing per-child authority schema F10-R1-REGISTERED/v1 is unchanged.
No dispatcher, mathematical helper, engine API or supervisor is authored.
The ROOT-PINS.json metadata table has SHA256
78d078e6f327de04f91f04fc9a9335c25302bef91e1c84c7aca851fd4656c417.
Root authored/read that complete exact metadata table; all scientific
integers/rationals remain in the unchanged programs, never JS numbers.

## Owner, physical scope and clocks

Exclusive executor/derivative-authority writer: Astra /root/model_productivity.
Root owns independent live confirmation, terminal collection and AWS backup.
Instance ONLY i-08d2a40f272ee9fa2, retained disk vol-0574e0fa5aed1f5e3,
expected host ip-172-30-0-72/private172.30.0.72, SSH keyjc2-fleet. Root will
start it after installing the independent stop timer. NO other instance,
old output directory, protected project or retained volume is in scope.
Never terminate/delete/resize/install. Remote cwd MUST be newly absent:
/home/ubuntu/jc2-f10-r1-quotient-remainder-generic-20260910.
If it exists, STOP; no alternate directory or overwrite.

Local owned outputs ONLY
box/f10-r1-quotient-remainder-generic-execution-20260910/ and
xmodel/f10-r1-quotient-remainder-generic-execution-astra-20260910.md.
No source/shared/prep-file changes. All local authored files use apply_patch;
metadata/data files may be shipped byte-identically to the registered cwd.
No local mathematical subprocess of ANYsize, including syntax/import/AST/
compile/test, fixture or coefficient generation. No package discovery or
installation. Metadata Python/hash/JSON/clock operations only are permitted
locally; the sole mathematical child is the registered generic harness.

First action and absent-target ACK; task stop EARLIER first+25min or01:27:00UTC,
publication reserve final3min. Mathematical absolute end01:20:00UTC;
first-mathematical-child+240seconds is an additional aggregate hard end.
Before any child require its wall cap+15seconds fits the absolute deadline;
before the generic child require the same against BOTH stored math ends.
Record first_math and never reset it. Abort on a late return, no PASS.
Stop worker immediately after terminal evidence is collected, on any failure,
or no useful authorized work; in all cases by01:22:00UTC. ROOT independent
AWS stop backup01:23:00UTC; never reset. No source batch follows this one.

## Exact bytes and runtime package

Use the eight task files in ROOT-PINS (new solver, independent harness,
unchanged checker/evidence/algebra/execution_gate, CAPRUN and probe), plus
this registration and ROOT-PINS as byte-identical files in the flat cwd.
Verify local pins before reuse and every remote hash before launch; all100
declared native files and /usr/bin/python3 must match the metadata table.
The declaration is the retained installed FLINT0.9.0 closure, not a claim of
hermetic OS/stdlib imports. An absent/different native file is STOP, not repair.
Record current EC2 DMI vendor/id, hostname, boot_id, Python version/path and
PID namespace before any payload. No old boot or stale authority is reused.
No actual source coefficient file, source receipt or ROOT source acceptance
is shipped/read/minted. Classical admissibility is GENERIC-ENGINEERING-ONLY:
synthetic rank7 fixtures, no source/degree/counterexample claim.

The executable six-file package is statically accepted by the FIRST gate
xmodel/f10-r1-quotient-remainder-static-gate-fable5-20260910.md,
SHAa797cbed9ad4483ec453599fc8154e4d5ce2dd64b06d0796a79a0ad69655cbef.
Read that gate and this complete registration. WHOLE program reads or explicit
same-byte prior WHOLE reuse after pins. No unrelated provenance/corpus reads.

## Existing authority, literal vectors, and SYSTEM containment

For each of the EIGHT named operations below, instantiate exactly one fresh,
exclusive read-only NAME.authority.json under the existing schema. These
derivative metadata authorities are licensed by ROOT, not self-issued new
source acceptance. They contain enabled=true (except disabled control), job_tag
f10-r1-quotient-remainder-generic-execution-20260910, exact instance/hostname,
admissibility_sha256=SHA(ROOT-PINS.json), cwd, runner_path=cwd/run_capped.py,
generic_only=true, univariate_acceptance=null, python_flint from ROOT-PINS.
file_sha256 contains all8taskfiles, all100nativefiles, /usr/bin/python3,
the remote ROOT-PINS.json and ROOT-REGISTRATION.md with their current frozen
hashes; the single deliberate missing-input authority removes ROOT-PINS only.
No authority self-hash inside itself. No concurrent authority writer.

Registered child arrays, with every path absolute:
probe: /usr/bin/python3 -I -B cwd/probe.py AUTHORITY cwd/ROOT-PINS.json
       cwd/NAME.sentinel MODE
generic: /usr/bin/python3 -I -B cwd/generic_controls.py AUTHORITY
         cwd/generic.receipt.json
Operation is check for probes, build for generic.

Literal parent array, with no timeout/sh/extra PGID wrapper:
/usr/bin/python3 -I -B cwd/run_capped.py
 --wall-seconds WALL --cpu-seconds CPU --rss-bytes RSS
 --rss-sample-seconds 0.05 --term-grace-seconds 0.25
 --cwd cwd --stdout-file cwd/NAME.stdout --stderr-file cwd/NAME.stderr
 --telemetry-file cwd/NAME.telemetry.json -- CHILD_ARRAY
The authority parent_argv and child_argv are exactly these arrays. caps uses
string values with precisely those argument values. Fixed profiles:
probe WALL5 CPU3 RSS2147483648; dummy WALL5 CPU3 RSS33554432;
generic WALL180 CPU170 RSS2147483648. No enlargement.

Launch that unchanged parent directly in a separate SYSTEM systemd unit
jc2-f10-qr-generic-20260910-NAME.service, exact WorkingDirectory=cwd,
KillMode=control-group, TimeoutStopSec=5, RuntimeMaxSec=20 for each probe/dummy
or210 for generic; MemoryMax=3221225472, TasksMax=64, LimitFSIZE=16777216.
OMP_NUM_THREADS=1, OPENBLAS_NUM_THREADS=1, MKL_NUM_THREADS=1;
LD_PRELOAD and LD_LIBRARY_PATH unset/empty, no user Python path.
The SYSTEM cgroup is outer containment, not an extra inner PGID. Preserve
unit Main/ExecMainPID, invocation id, start/end/status and the exact argv.
Capture actual CAPRUN/child/PGID/namespace identity in the SAME launch
orchestration promptly, and send root the exact live unit/IDs. If a child is
already terminal, do not invent a live capture; record that fact and its
terminal primary metadata. Never infer exit from a tool observation timeout.

## Mandatory seven preflight controls, then ONE generic invocation

Run in this exact order, one at a time, MODE=plain unless stated:
1 gate-disabled: enabled=false. Exact refusal substring 'no active registered authority'.
2 gate-wronghost: hostname='NOT-THE-REGISTERED-HOST'. Refusal 'not the registered AWS host'.
3 gate-argv: parent_argv has one trailing 'EXTRA', actual launch unchanged.
  Refusal 'full parent argv differs from registered capped runner'.
4 gate-cap: authority caps.rss_bytes='1', actual parent unchanged.
  Refusal 'registration is not the exact required CAPRUN argv'.
5 gate-missing-input: remove ROOT-PINS path from authority file_sha256.
  Refusal 'checker input artifact must be explicitly hash-registered'.
Each must be NORMAL_EXIT/nonzero, matching refusal, and no postauthorize
sentinel. An unrelated failure does not pass. The expected control refusals
are not workflow failures; any unexpected outcome is STOP.
6 gate-valid: unchanged authority; NORMAL_EXIT/0; exact sentinel POSTAUTHORIZE\n.
7 dummy-descendant: MODE=descendant, dummy profile. Require runner125,
RESOURCE_CAP/rss, measured group RSS>33554432, TERM then KILL with matching
namespace/leader/start identity, cleanup_complete and leader_reaped, both
reported processes in the recorded PGID, no nonzombie descendant/orphan.
Use the unchanged probe/CAPRUN; no new dummy or supervisor. Store all exact
primary telemetry/authority/output hashes and the seven validation outcomes.

Only after ALL seven pass and all groups are quiet, launch NAME=generic with
the specified build authority/profile. Exactly one generic_controls.py call:
the eight positive fixtures and four negatives are its entire mathematical
scope. No separate syntax/import/engine smoke test or actual-source solve.
No old compatibility harness, modified script, fallback or rerun.

## Output and acceptance boundary

Before instantiating each operation, check its authority/stdout/stderr/
telemetry/sentinel names absent, including symlinks; then freeze the newly
created authority and require that hash at launch (not renewed absence of
the authority just created). Before generic, check
generic.receipt.json and its exact20sidecars absent: 8named positive input/
candidate pairs and4negative files, exactly CASES/NEGATIVES in the frozen
harness. Retain partial files on failure; never overwrite/delete to retry.
All successful output remains SYNTHETIC-ONLY, never source acceptance.

Observe each exact SYSTEM unit terminal and its cgroup/owned PIDs/PGID quiet
BEFORE payload/hash/pull/scientific reading. Collect primary unit metadata,
all authority/telemetry/stdout/stderr and semantic artifacts. Validate exact
generic status ALL-12-GENERIC-CONTROLS-VERIFIED-NO-SOURCE-ACCEPTANCE,
8positives/4negatives, names, branch expectations, exact exception predicates,
all embedded file pins, engine/custody and post-file/authority equality.
Do NOT reconstruct/import mathematical code locally to check these records.
If this happens to finish fast, record actual runtime rather than cap values.

Freeze a compact report, full custody and expected artifact transaction only
after terminal evidence and worker STOPPED. Report exact timers, any missed
live observation, migration boundaries, every input/output pin and actual
wall/CPU/RSS with accounting scopes. ROOT independently verifies physical
STOPPED before canceling its backup, then terminal-custody-FIRST intake.
This direct execution migration has NO blanket acceptance: fresh seven-control
regression and a FIRST different-model review of this exact path and finite
runtime evidence remain mandatory before promotion/actual-source use.
ALL OWNED WRITERS IDLE before the unchanged task cap; no follow-on authority.
