# R2 terminal read scope

Exactly13 charged inputs: four R2/incorporated registration metadata,
the accepted static gate and eight task files. All pins preceded body reads
or same-byte prior-WHOLE reuse. Fresh WHOLE: both registrations, ROOT-PINS,
public pinned SSH key, static gate, generic_controls.py in two complete
ranges and probe.py. Prior same-byte WHOLE reuse after pins: solver.py,
checker.py, evidence.py, algebra.py, execution_gate.py and CAPRUN.

Only one direct SYSTEM CAPRUN invocation of unchanged probe.py ran, disabled
authority mode. It refused before its sentinel. No generic/other control,
local mathematical subprocess/import/AST/compile/test, actual coefficient
artifact/receipt/acceptance, provenance, corpus or live peer body was read.

Transport was exact PRIVATE172.30.0.72 with strict pinned ed25519 identity.
Existing private-key body was unread. No public substitution, trust downgrade,
restart, allocation, deletion or retry. Only exact-owned AWS metadata/stop.

Declared scope excursion: one metadata assertion concatenated empty
ControlGroup into the cgroup-root path and read its cgroup.procs, then failed.
No contents/PIDs were emitted or used. Subsequent checks used the literal
named R2 service cgroup and exact captured PIDs1610/1612 only.

First-control bodies were read only after exact unit/Main0/named-cgroup/PID
quiet and hashes. All23 staged/unrun/output files and113 fixed pins were
hashed before pull; all pulled bytes matched. Inline scripts were metadata
authority JSON/hash/file/exact-unit-state only. No new dispatcher, mathematical
helper or supervisor was authored. Authored edits used apply_patch; frozen
copies used exact scp. Existing artifact_finalize is metadata publication.
No retired derivative authority can be reused for a future dispatch.
