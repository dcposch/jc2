# Corrected four-entry late flow — STATIC / UNEXECUTED
The frozen orchestration kernel, caller, binder, stage builder, installer algorithms and dispatcher are retained. New files are owned derivatives, never replacements in old boxes. No worker or science is authorized. A focused different-model FIRST review of this exact packet and ROOT binding are prerequisites.

## Exact corrections
ADMIT, ATTEST, INSTALL and RELEASE remain four separately selected ROOT entries. Fixed code and physical metadata are frozen before the holder; only admission PID/start/invocation and validated observation/candidate DATA arrive later. All nine authorities remain expected runtime outputs, never installed by this tail.
The selector is @@ENTRY@@, distinct from the JC2_*_PLACEHOLDER refusal pattern. Composition substitutes that selector only and checks four targets absent before creation; no placeholder-bearing bound source is usable.
The local manifest has exactly fourteen roles: four entries, config, caller, binder, stage builder, prepared-pins JSON, the local remote-manifest, and four prepared files. The remote manifest has exactly eight roles: admission, observer, receiver, release-check, FIFO writer, holder, installed-source manifest and native-list. Local digests are obtained locally; remote digests and all entries of the last two closure manifests are obtained through pinned SSH. Config stores manifest paths, not a digest of itself; ROOT externally pins the local manifest after config/entries are frozen. No self-hash cycle.
Records use a canonical, stable dev/inode/uid/gid mode0700 private parent. ADMIT checks all five targets absent; each publication repeats absence and parent checks, uses apply_patch, verifies exact text, freezes0444 and syncs file/parent. This is cooperative exclusive-writer safety, NOT atomic O_EXCL from apply_patch. Candidate creation similarly checks a fresh private directory, all fourteen paths and authority-parent absence, freezes all files and checks every digest. Trusted ROOT must prevent concurrent writers throughout.
ATTEST, INSTALL and RELEASE require observation PID/start/invocation to equal parsed admission DATA, not merely have valid syntax.
INSTALL publishes an immutable local commitment to all fourteen candidate files, five staged files and two installed metadata files before transfer. Its later receipt binds the commitment SHA. RELEASE checks the receipt/commitment/admission chain, all current candidate/staged/installed digests and passes the committed registration SHA to the remote guard. No current-SHA-as-expected substitution is accepted.
Release checks include swap.max0, cpu.max.burst0, holder argv/exe, root and holder namespaces, cgroup mount root/type, outer/leaf canonical identity/owner/modes, all three root-only migration surfaces, empty subtree controls, domain type, exactly nine child leaves, no leaf descendant directories, empty population, installed registered-source pins, FIFO and original clock checks. These are non-atomic checks under trusted exclusive ROOT/no migration, not an adversarial-root proof.

## Interrupted calls
Entry reexecution consults saved phase state BEFORE reading config or calculating a fresh seedStop. Only a known nonterminal exact handle may be collected using its original argv and absolute stop. It then STOPs without a successor. Unknown launch, interrupted publication, completed or failed phase is never automatically replayed. Full resumable continuation is intentionally NOT implemented or claimed. ROOT must preserve evidence and let original retirement/holder limits act; no deadline reset.

## Complete preholder rebinding inventory
Use one new batch satisfying the unchanged binder's jc2-closedchild-preflight9-YYYYMMDDx pattern. The new admission derivative replaces every historical batch token and its three September11 literal deadlines with explicit placeholders; it must not run before full rebinding. ROOT also makes constants-only copies of the unchanged holder and installer: holder ENABLED/RELEASE/TOKEN/INSTANCE/BOOT/PIDNS, CUTOFF, ADMISSION, BASE, CHANNEL and OUTER_CGROUP; installer base/stage/unit/holder argv, physical binding and cutoff. Both historical holder deadline literals must be replaced. All admission unit/base/stage/channel/stream/holder/TERM/KILL timer paths derive the same batch.
Prepared registration, authorities, execution-card and installer paths/identities must agree with config and the two exact manifests. Preserve the binder's exact late tokens: captured PID/start/invocation, final registration/card/manifest/install-release digests and coordinator receipt binding in their original locations/multiplicities. Do not replace them with guessed values. No nine authority files may exist in remote authority output.
ROOT qualifies the actual interpreter, executable paths, SSH key/host trust, installed/native closure, namespace/mount, permissions and any ACL/delegation assumptions. Mode tests do not defeat malicious ROOT. Set the records-parent identity/owner and exclusive-writer contract and prove absence before ADMIT. All five records reside directly in that one private parent.
Composer, four entries and config must be reviewed/frozen before admission. The composer is never run in this task. All physical facts and future clocks remain unbound here.

## Unchanged limits and open conditions
No caps, phase lists, strict dummy predicates or science algorithms change. Original outer CPU69%/10ms, memory8GiB, swap0, Tasks32 and holder age120s checks remain. Original absolute minimum stops govern every successor. Source/runtime registered caps remain those of the accepted original registration; no assertion of aggregate hard real-time guarantees is added.
OPEN1: exact bound installation is not produced: one focused source/integration FIRST plus ROOT's single future binding/review is the cheapest next check. OPEN2: whether added exact hashing/SSH checks fit holder120s is UNMEASURED; one separately authorized nine-only observation is the cheapest runtime discriminator after review, not authorized now. No speculative cap increase/retry. Full phase continuation after interruption is unsupported by design, not an available recovery feature.
