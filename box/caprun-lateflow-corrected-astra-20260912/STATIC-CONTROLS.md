# Manual negative traces — NOT executed
1. Fully metadata-bound composer retains @@ENTRY@@ only in the tail; its JC2 placeholder guard no longer rejects its own selector source. An unbound reviewed-tail SHA still refuses. Four existing targets cause refusal before composition.
2. A remote role placed in the local manifest fails the exact role census. A changed worker file cannot be vindicated by a matching coordinator copy: remote digest collection and both worker closure checks are required before admission and each phase.
3. Preexisting record, symlink/private-parent identity change or candidate-target collision refuses before patch. The retained before/after evidence does not make apply_patch atomic: exclusive trusted writer is required.
4. Different but syntactically valid PID, start or invocation fails exact admission/observation comparison in all three later entries.
5. Changing both staged and installed registration fails the immutable candidate commitment; changing the commitment fails its installed-receipt SHA. This assumes ROOT does not maliciously rewrite all trusted records.
6. Swap, burst, holder executable/argv, root namespace, migration permissions, leaf domain/subtree/path/device/inode/descendant or population drift fails a literal release predicate before FIFO. No substring-only success, sleep or skipped map check is added.
7. New admission has no old September11 deadline/batch literal. Historical holder/installer are REFERENCES requiring the constants-only rebinding inventory, not executable instructions to run unchanged files.
8. Reenter an interrupted phase: saved exact handle is collected before config keys; original stop retained; no downstream phase/action is resumed. Unknown handle or interrupted apply_patch stops. No new seedStop licenses replay.
9. Missing transfer input fails pipefail; changed transfer/readback fails exact digest; no retry.
10. The transfer list is exactly registration/card/install-manifest/installer plus native-list, never the nine generated authorities. Remote output directories stay empty before release.
These are source reasoning traces, not observed outcomes, calibration, tests or proof of liveness. One focused FIRST review must inspect the literal diffs and full sources.
