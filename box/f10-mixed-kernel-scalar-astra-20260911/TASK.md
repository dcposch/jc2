# F10 mixed-kernel scalar: one bounded manual decisive test

ROOT assignment September11 2026. Original publication reserve14:05UTC,
HARD14:08UTC; no clock reset. Record actual first action. Planning15minutes
manual mathematics UNMEASURED; stop early on a decisive result or a precise
unreduced obstruction. This is not a fresh whole-portfolio round.

## Sole scientific input and exact question

This TASK is the sole file input. Work from the equations below, not live or
old reports. ROOT supplies the accepted early/modified inverse interface as
stipulated algebra. No prior unreviewed potential identity is an assumption.

Fix integer r>=2; m=3r+1, n=5r+2, q=2r+1. Work over a commutative
Q-algebra with C(theta)=theta^3+F*theta^2+H*theta+a, monic degree-five
D(theta), D(0)=b, a,b,H units, and ENTIRE identity

    m*C*D'-n*C'*D=-theta^7.

Prime means theta differentiation. Do not identify this with a scalar-Jacobian
plane map. A source-normalized finite-etale rank-seven leading algebra is a
client, but no chosen field factor or explicit septic presentation is supplied.

For h put kappa_h=m+n-h and

    L_h(P,Q)=m*C*Q'-(n-h)*C'*Q+(m-h)*P*D'-n*P'*D.

The unmodified kernel pair (P_-,Q_-) is uniquely determined by
deg P_-<=2, deg Q_-<=4, L_r(P_-,Q_-)=0 and
m*a*Q_-(0)-n*b*P_-(0)=1.
The modified pair (P_+,Q_+) is uniquely determined by the same degree and
constant conditions and L_(r+1)(P_+,Q_+)=-2*e*theta^6,
where e=[theta^2]P_+. Put p=[theta^2]P_-.
The upper rows imply [theta^4]Q_-=p/2, [theta^4]Q_+=2e.

For explicit finite inverses, write T_h=trunc_(<=7)(C/a)^(kappa_h/m).
For the minus pair use T=T_r and

    P_-=(m*C*T'/kappa_r-C'*T)/theta^7,
    Q_-=(n*D*T'/kappa_r-D'*T)/theta^7.

For the plus pair let e=7*[theta^7]T_(r+1)/kappa_(r+1),
T=T_(r+1)-(2e/7)*theta^7, and

    P_+=(m*C*(T'+2e*theta^6)/kappa_(r+1)-C'*T)/theta^7,
    Q_+=(n*D*(T'+2e*theta^6)/kappa_(r+1)-D'*T)/theta^7.

These numerators are exactly polynomial-divisible with the stated degrees;
normalization is homogeneous rho=1, not the whole forced affine rho.

Define the ENTIRE mixed forcing

    W=(m-r)*P_-*Q_+'-(n-r-1)*P_-'*Q_+
       +(m-r-1)*P_+*Q_-'-(n-r)*P_+'*Q_-.

It has degree<=5 and [theta^5]W=2ep. Define the finite functional

    Gamma(W)=(21*a/(2*H))*[theta^7]
                 C(theta)^2*integral_0^theta W(t)/C(t)^2 dt.

QUANTITY: simplify this scalar uniformly in r on the WHOLE stated leading
algebra, ideally proving it a unit, a zero, or identifying its exact remaining
factor. Do not replace it by a leading coefficient, the unrelated last-axis
scalar, a residue-at-infinity balance, or a sampled r. All inversions must be
proved units under these hypotheses; nonzero on a selected factor is not enough.
CHEAPEST TEST: manual finite-power/antiderivative identities using the two
displayed inverses, at most the original15minute planning budget; no execution.

## Why this test and what each outcome means

The current proposed comparison of an actual highest compatibility block with
a regular rational-power model needs this scalar to be a unit (after a
known leading-unit normalization). It has not been evaluated in the prior
campaign report. An exact failure would stop that model comparison on the
affected component, NOT disprove actual highest regularity. A unit answer
only passes this necessary scalar test; it is not a potential, uniform REG,
source-zero result, all-F10 theorem, or JC2 proof. A bare restatement of the
finite inverse is NO_NEW_DISCRIMINATOR, not a new implementation queue.
ROOT independently examines global applicability and the variational target.
No actual-source array, finite-m farm, prime scan or computational descendant.

## Boundaries, ownership and publication

Own only box/f10-mixed-kernel-scalar-astra-20260911/ EXCEPT this ROOT TASK,
and xmodel/f10-mixed-kernel-scalar-astra-20260911.md with its ordinary
transaction artifacts. No canonical/shared/protected edits, no jc2-lean
access of any kind, no extra science sources, network/AWS/SSH, agents,
scientific subprocess/import/AST/syntax/test/CAS or coefficient payload.
Ordinary own-target absence checks, hashing, date, apply_patch, and the
existing documentary artifact_finalize commands are permitted.

Hash TASK before WHOLE read; preserve it. Basis
0d39df3c9fd69c939a8420c54d03228b9077777d. Use existing begin->close->finalize
->verify with expected basis/manifest; initial skeleton has NO terminal
marker, bounded body writes, unique standalone <!-- BODY-END --> LAST only
after final whole readback, all current pins and own-only collision check.
No new canonical OPEN ID. Record manual/unreviewed scope, exact controls,
read scope and timing. Write own PINS.json; final custody.json includes all
input/owned hashes, expected transaction hashes, actual first/readback times
and no execution. Custody final write, then read/hash and terminal handshake:
COMPLETED; ALL WRITERS IDLE with custody SHA. ROOT alone owns intake/gating.
