# Disabled validation and binding plan

Status: STATIC / UNEXECUTED / INTERNAL UNREVIEWED CODE. This file is not a
registration, acceptance document, dispatch invitation or test receipt.

The production bundle is exactly solver.py plus the unchanged checker.py,
evidence.py, algebra.py and execution_gate.py. main() and its CLI remain
byte-identical. A future root registration must bind the new solver hash
through the existing file_sha256 producer field, all four sibling hashes,
the unchanged interpreter/native FLINT environment, genuine univariate
artifact/full prior receipt and root-issued source acceptance. No synthetic
coefficient modulus is allowed through candidate(data, flint).

All operational fields are disabled here: enabled=false; worker=null;
remote_cwd=null; authority=null; source_acceptance=null; interpreter=null;
native_environment=null; profiles=null; mathematical_deadline=null;
aggregate_wall_seconds=null; worker_stop_deadline=null. Root alone can issue
a distinct future registration. This package creates no caller or authority
schema, and changes no supervisor, CAPRUN or engine API.

The shared INTERFACE owns the finite test specification; its independent
harness is prepared by another lane and is not an input to this task. There
are at most eight positive generic cases and four negative cases specified
there. Do not duplicate, extend or execute them here. The additive delta
requires ValueError with exactly `generic modulus must be squarefree` for
the non-squarefree-modulus refusal; unrelated exceptions do not count.

Cheapest next checks, in order:

1. One different-model joint STATIC review of the replacement producer and
   independently frozen harness against the common interface and API delta.
2. If root authorizes it separately, the finite generic batch must check full
   witnesses (all 217 coordinates or all 1638 columns), including splitting,
   the raw 2T pivot, nonreduced T remainder, full degree-25 padding and the
   exact refusal predicate. Synthetic controls never mint source acceptance.
3. Any actual-source use needs its own root registration and the unchanged
   genuine checker.main read-back. A small-matrix rank or construction trace
   is never a decision. Failed/unsupported/capped execution remains a
   nondecision, with no automatic repair, retry, alternative engine or cap.

No runtime, syntax, API, memory, bitlength, speed or source outcome is claimed.
The matrix dimensions below are proved construction bounds, not measurements:
coefficient matrix at most 35 by 280; with b and the full row-operation
identity appended, at most 35 by 316. Zero or one RREF is called, not a
second verification solve. Full old 217-by-1638 search matrix is not built.

OPEN quantities: zero identified missing production API/code paths from
manual review; one unperformed independent static review, one unexecuted
finite generic validation batch, and no actual-source execution authorized.
The cheapest tests are the ordered checks above. This list is a limitation,
not a claim of successful runtime verification.
