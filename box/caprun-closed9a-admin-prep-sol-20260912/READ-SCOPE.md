# Read scope

Fresh whole-file reads after exact pre-pin confirmation:

- `TASK.md`, selected plan, original-clock card, and binding-scope erratum;
- all thirteen source files enumerated by `TASK.md`;
- all thirteen owned derivatives, `PORT-MAP.json`, `READ-SCOPE.md`, final report, and custody during final readback.

The reads treated paths and hashes mentioned inside charged files as inert text. No linked scientific source, uncharged report, protected tree, or public mirror was followed. Text operations were limited to the six ordered literal global substitutions, SHA-256/byte/line counts, placeholder occurrence census, and remaining-old-reference scan. No derivative/source was executed or subjected to syntax, AST, import, compile, mock, fixture, or test processing.

All seventeen charged inputs were re-hashed unchanged after authoring. Output transformation equality means each derivative's full SHA-256 equaled the SHA-256 of a fresh stream transformation of its pinned source using exactly the six substitutions in task order.
