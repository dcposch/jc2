# Read scope and limitations

First action04:04:49UTC: three targets absent and all seven local pins matched before any new body read. Five newly charged local files read FRESH_WHOLE: authority.py1–133; produce.py1–300,301–382; check.py1–285,286–420,421–529. The two FIRST reports were read through EOF; oversized combined output was recovered with separate source-FIRST1–26/27–53 and runtime-FIRST1–22/23–66 reads. Nothing clipped is counted as sufficient.

Owned prior dispatch.py and CONTRACT.md were already read WHOLE in the terminal03:41 preparation. Their same current hashes were checked before explicit REUSED_WHOLE; relevant dispatch205–221,365–420,506–530 freshly reread. No other prior report or provenance was followed.

Primary official CPython v3.12.0 source was consulted ONLY for traceback formatting and relevant compiler source positions, with web raw GitHub views/find and documentary curl text retrieval. Web renderer line indices omit some blank lines; final citations/pins use literal raw-file sed ranges recorded in PINS.json. The three saved excerpts were read WHOLE. Upstream entire files were streamed solely to SHA256, NOT claimed WHOLE read. Initial partial exploratory ranges500–620/717–848 of traceback.c and645–778/1410–1480 of pythonrun.c preceded the final frozen ranges. Python compile.c rg context was limited to call/raise location emission. No source was compiled, parsed as Python/AST, imported or executed.

Only metadata and document text were authored with apply_patch. Ordinary artifact_finalize metadata publication is the explicit transaction-tool exception; no other Python process or code/test/dummy was run. No worker, AWS, SSH, place, mathematical coefficient body, fixture, live author artifact, package, process inspection, agent, shared/canonical edit or network beyond the allowed official CPython lookup. ROOT's final arithmetic/check_arithmetic hashes were accepted as administrative messages only; neither file was opened or hashed.

The policy is a disabled template: arithmetic.py remains UNFORMED, so it cannot equal the dispatcher's valid five-hash source_files. Its seven triples are conditional source derivations, not measured bytes. ROOT must select and authenticate a matching build/formatter/compiler/startup/native/source installation before forming any enabled registration. No alternate observed-error calibration is licensed. Both FIRST scopes are imported without re-reviewing mathematics, optional inverse changes or performance claims.
