# Seven source-derived refusal triples

DISABLED / STATIC / CONDITIONAL ON UNMODIFIED CPython DISPLAY AND INSTALLATION. No bytes were observed from any Python run. Administrative science directory: `/opt/jc2-r3-directrows-20260911a/science`. Ordinary child flags exactly `-E -s -S -B`; environment exactly PATH=/usr/bin:/bin, LANG=C, LC_ALL=C, TZ=UTC. No actual interpreter/build/worker has been selected or verified.

## Branch proof

Every first-five control enters actual produce.py:382 -> main:356 -> authorize. The source has no encompassing catch. The five-file physical inventory and DMI/CLI/entry checks must already succeed. Metadata is correctly serialized/rehashed except the named mutation; the output is absent and canonical. No mathematical import or output write is reached.

| Label | Single changed item | Last frame and outcome |
|---|---|---|
| refuse-status | authority status DISABLED | authority.py:82; ValueError: no ROOT registration |
| refuse-caps | limits.cpu_seconds becomes string599 | authority.py:95; ValueError: registered limits/source inventory |
| refuse-inventory | delete metadata files.check.py (not installed file) | authority.py:95; same exact exception |
| refuse-source | metadata files.produce.py is64 zeros | authority.py:100; ValueError: source pin: produce.py |
| refuse-hash | supplied authority SHA is64 zeros | authority.py:78 -> load_pinned:74; ValueError: authority/inventory byte pin |
| startup-produce | actual producer job token WRONG-JOB | authority.py:29; SystemExit string REFUSED: registered CLI contract |
| startup-check | actual checker job token WRONG-JOB | check.py:529 -> main:511 -> authority.py:29; same SystemExit string |

For refuse-source, FILES order checks authority.py and arithmetic.py before produce.py. Their correct current hashes, readable regular files and an actual produce digest different from64 zeros are prerequisites; no source mismatch is waived. For refuse-hash the real authority digest must differ from64 zeros. Caps and inventory share one condition and identical observable bytes; their independence is only the mutation. Wrong tags fail before entry/inventory/authority-file reads, but after ordinary-mode/Linux/DMI checks. The checker's try/except begins after authorize('check'), so it cannot convert either wrong-tag SystemExit or an initial authorization error into a mathematical outcome.

All seven triples have exit1 and empty stdout under normal CPython top-level exception handling with healthy streams and no resource failure. The exact full stderr strings are in DISABLED-POLICY.template.json. Five are complete tracebacks; two are the single SystemExit line ending LF. No regex, wildcard, optional frame, substring or observed-run calibration is supplied.

## Exact caret derivation

Primary reference is upstream CPython v3.12.0, not a claim about every3.12 distribution/build. Raw-source line numbering is used; web-rendered source line indices differ. `Python/compile.c`3948–3972 emits RAISE_VARARGS with the whole statement location;4911–4930 and5070–5117 pass the whole Call expression location to CALL. `Python/traceback.c`597–913 specializes only BinOp/Subscript; plain calls/raises have no secondary anchors, and a span equal to the entire stripped source line suppresses the indicator. Display strips original indentation and prepends four spaces (468–574).

Consequently the module's `main()` frame and each final `raise ValueError(...)` frame have NO caret line. produce.py:356 retains the literal source `meta=authorize('produce')`: call columns9..29 (zero-based raw line, end exclusive) produce NINE spaces then TWENTY carets. authority.py:78 retains `meta = load_pinned(sys.argv[4], sys.argv[6])`: call columns11..48 produce ELEVEN spaces then THIRTY-SEVEN carets. There are no tildes. The nested sys.argv subscripts are not the failing instructions; their successful evaluation does not add a frame or change the CALL span. Literal path, space, quote, source-line and LF bytes are frozen in the JSON. `pythonrun.c`688–833 prints noninteger SystemExit.code raw plus LF and uses exit1 before normal traceback handling; its887–916 and1495–1570 show the ordinary traceback display route.

The source-derived table is complete CONDITIONAL evidence. Installed-build coverage remains GAP: ROOT must bind the selected executable/native closure to ordinary compiler location tables and the reviewed formatter, unmodified sys.excepthook/no embedded audit or tracing customization, and readable pinned ASCII source at the exact administrative paths. No -Xno_debug_ranges or environment substitute is admitted; -E ignores Python environment settings and -S suppresses site initialization, but these flags do not authenticate a distributor build or an embedded hook. Missing positions/source lines, a patched formatter, stderr/flush failure, import/DMI/ACL/native/cap failure or exception notes would not match and must STOP. This is not authority to accept alternative observed bytes.

## Disabled finalization

The exact policy schema has only schema/source_files/entries; adding enabled:false would itself violate that schema. Therefore arithmetic.py is deliberately UNFORMED, which cannot equal the dispatcher's five final64-hex source_files. ROOT's separately supplied final administrative arithmetic pin is acd07f800707c379aba60bd06f239d38cd83c86867729561367abd2bcc04ce73; this task neither read nor hashed that file. The unchanged check_arithmetic pin9d2aac637dbdeaa192cc3fe531e83694f9853b6573556ad5d49362f754b1163e is likewise ROOT metadata only. ROOT may replace UNFORMED only in a NEW finalized copy after satisfying the build/install obligations and separately issuing genuine registration. This file alone is never launch authority.

No source, runtime, optional inverse-restoration test, native closure or worker was changed. Saved-slot restoration remains byte-faithfulness/outside-slot invariance, not an exercised arithmetic inverse. No place, coefficient payload, mathematical source/result or source admissibility claim is involved.
