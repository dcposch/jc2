# Minimal mixed-univariate runtime composition — disabled

`dispatch.py` is new, unreviewed and unexecuted. It imports no old client
controller. It reuses unchanged CAPRUN and only the three delivered scientific
sources. Sequence is fixed: descendant dummy; parser negative; one producer;
freeze/hash candidate; one positive independent checker; identity mutation;
`(7t-12)` exception mutation; terminal summary. No retry, parameter/prime/r
farm, second gcd, or automatic follow-on exists.

ROOT must create one fresh registration from actual observations. Every path,
source/interpreter/setpriv/CAPRUN hash, complete native manifest, library
ancestry, host/boot identity, UID/GID, absolute time, qualification and output
location is physical data. Registration and authorities are ROOT-owned,
immutable, canonical and exclusively created. The checker authority is formed
only after the candidate SHA exists, avoiding prediction/self-hash cycles.

One systemd service cgroup contains caller, CAPRUN and every descendant.
Qualification must directly prove `cpu.max = 80000 100000`,
`cpu.max.burst = 0`, fair-class-only execution with no RT/deadline/BPF escape,
`memory.max = 34359738368`, no delegation/migration,
and a private 268435456-byte tmpfs containing every writable output, receipt,
telemetry, temporary/cache path and `TMPDIR`. `RuntimeMaxSec` and a separately
armed exact-unit absolute retirement cover setup, all six phases, custody and
cleanup within 3540 seconds, below the 3600 ceiling. With continuous 80%
quota, 100ms period, fair-class confinement and no burst, whole-cgroup CPU service is at most about
2832.08 CPU-seconds before the 3540-second wall boundary, hence below 3300;
this conclusion requires the exact installed-kernel observations and unchanged
containment, not CAPRUN's per-process RLIMIT. CAPRUN uses at most3290 soft/
3291 hard seconds, satisfying authority hard<=3300; its RSS remains sampled
exact-PGID evidence with overshoot, while systemd MemoryMax is the configured
whole-cgroup kernel boundary and may have documented temporary overshoot.
Tmpfs capacity is aggregate writable storage, not a claim
that every output individually may reach256MiB.

The no-CAS dummy must freshly show descendant sampled RSS, identity-matched
TERM/KILL, reaping and no live exact group before science. ROOT additionally
checks terminal cgroup emptiness, complete tmpfs census/hashes, native/source
postpins, systemd CPUUsage/MemoryPeak/result, no leftover descendant, archive
durability and exact worker retirement. Any missing property, timeout, cap,
abnormal phase, partial output or cleanup is STOP/NONDECISION without retry.

Source-static FIRST plus installed Python/Sympy/API/import/native and systemd/
kernel qualification are mandatory. This packet supplies no authority or
runtime success. Even a positive checker covers only the auxiliary scalar;
it does not itself prove forcing attachment, REG, source zero, all F10 or JC2.
