# Root authorization: bounded installed-environment metadata only

Registered2026-09-09 22:08 UTC. This is documentary preflight, not a
mathematical run, code acceptance, actual-source solve or install authority.
Only owned i-08d2a40f272ee9fa2, c7i.4xlarge, Owner
f10-r1-exact-artifact-validation-20260909, us-east-1,
EBSvol-0574e0fa5aed1f5e3. Fresh22:07 state STOPPED/privateIP172.30.0.72;
repeat physical DMI/instance/hostname checks after start. No other worker,
volume, user workload or repository is in scope. Retain all disk data.

Root installs an independent exact-worker STOP timer22:23:00UTC before
start. Metadata task ends at22:21:00UTC at latest, with no deadline reset.
Stop the worker promptly after metadata collection; cancel backup only after
fresh positive STOPPED. No restart within this authority. No fleet wait,
installation, package repair, image/instance launch or termination command.

Read-only shell operations only: identity, regular-file/symlink metadata,
distribution/version text, installed package file listings/hashes and ELF
dynamic dependencies via readelf. Only installed software/interpreter/native
library paths needed for python-flint are within the file read perimeter.
No workspace/old research output/protected-tree inspection, Python/CAS/math
process/import/AST/test/compile, coefficient artifact, fixture or matrix.
Use existing fleet run or bounded direct SSH with ConnectTimeout10; no
detached remote process. Each command is a short documentary operation.

Resolve exact package/module/version and native file closure for a future
root-pinned registration. Missing package or ambiguous import path is a
reported prerequisite, not permission to install or select another engine.
Metadata availability does not prove installed API behavior; that remains
the separately reviewed, registered finite compatibility test's job.

Root owns current inputs, captured metadata, originals and worker stop.
Any later mathematical run needs separate static acceptance, fixed source
and explicit physical/cap registration. No actual-source acceptance is
issued by this metadata task.
