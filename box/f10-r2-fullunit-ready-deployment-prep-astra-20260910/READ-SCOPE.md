# Read scope and authoring boundary

First action2026-09-10 17:29:35UTC. Owned box/final/transaction targets absent; all seven exact input SHA values matched before bodies. Reserve17:45/hard17:48 never reset. Inputs are exactly PINS.json's seven files; no provenance traversal.

Fresh WHOLE reads: accepted fullunit runtime gate1–end through marker; inherited INSTALL-INPUTS template1–end; setup template1–end; native-metadata template1–end; registration template1–260,261–550,551–end. No truncation. Charged fullunit dispatcher and CONTRACT reuse this agent's previous actual WHOLE read after current byte-identical pins (dispatcher original plus exhaustive verified whole-file diff); no additional original science/probe/CAPRUN/body was opened.

New four templates are derived from the four WHOLE-read originals by documentary name/path/hash substitutions. Original science and probe references are retained, not read or copied. New fullunit sibling/mutator constants are from the charged dispatcher. One obsolete status-fix gate deployment record is omitted and the current fullunit runtime gate replaces the old runtime gate. Native collector is copied byte-for-byte; no execution/import/test/syntax/AST/compile occurs. Metadata JSON parsing, list/path/token comparisons and SHA checks are documentary only; no scientific input body/hash/parse or source evaluation occurred.

Owned WHOLE reads: registration's complete key/value metadata and all thirteen command lists were output, with the final four vectors separately reread in full after a combined output clipped part of the positive vector; setup and install templates read1–end. Native template reuses the exact unchanged WHOLE read. Complete checklist/report/PINS/read-scope reads precede seal. The thirteen command lists, four typed self-hash holes, literal install/pin alignment and shared timer suffix are checked as metadata, never run. All physical/native/ROOT/UTC placeholders remain unresolved and enabledfalse. Fixed Python3.12/65534 deployment policy is not an observed worker fact.

No actual highest or fullunit result, baseline/candidate, worker/native/cgroup/proc/network/AWS/SSH/Git/protected/corpus/other-lane input is read. No accepted science edits, actual registration, worker choice, launch or new gate authority. Root-finalization checklist explicitly separates worker-independent work before allocation, actual physical/native observations after allocation, and complete authentication before science. Own WHOLE/OPEN/collision/current-pin checks precede marker and transaction seal. Terminal custody excludes itself; all writers IDLE before handoff.
