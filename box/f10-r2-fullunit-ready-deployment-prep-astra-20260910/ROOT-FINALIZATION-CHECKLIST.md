# ROOT finalization checklist — OFFLINE / DISABLED / NOT AUTHORITY

This packet contains metadata/templates only. It does not allocate, register, authorize or launch anything. Accepted dispatch.py/mutate.py and the eight original/fullunit science files are unchanged REFERENCES, not copies. No baseline/candidate has been accessed, hashed or parsed. The first actual highest result is not an input or premise. The accepted fullunit runtime gate is a static import; runtime remains unexecuted here.

## A. Complete worker-independent preparation BEFORE allocation

- Read this whole packet and verify its custody/input/output hashes. The template remains enabledfalse. Review the fixed profiles, exact thirteen-vector sequence and three disjoint directory inventories before considering any worker. No new scientific instrument or additional science gate is proposed by this metadata migration.
- Confirm references for original five science files, accepted fullunit entry/produce/check and wrapper dispatch/probe/mutate. The unchanged probe reference stays at its inherited highest status-fix source path; fullunit's source box has no copied probe. Native collector is byte-identical to the charged template. CAPRUN is unchanged4435279d. The obsolete historical status-fix gate deployment record is omitted; one current fullunit runtime gate replaces the old runtime gate record. INSTALL-INPUTS has20 references, including deferred metadata and registration, with unique destinations/stage basenames.
- Treat Python3.12 paths and UID/GID65534 as inherited fixed deployment-profile requirements, NOT observations of a new worker. Actual interpreter/setpriv/native hashes, EC2/host/boot/ns, durable device and all UTC choices remain JC2_*_PLACEHOLDER. If the later worker cannot satisfy the fixed profile, stop; do not silently substitute, install, retry or alter an accepted instrument.
- Use one future `JC2_BATCH_SUFFIX_PLACEHOLDER` value for EVERY filesystem prefix, main unit, cgroup, TERM timer, KILL timer, ROOT backup-stop name, capture/harvest reference and cancellation receipt. Do not hand-edit independent clock-like suffixes. Prior T1620/T1720 naming history is a warning only, not a value chosen here.

## B. Observe physical/native facts AFTER separately authorized allocation

Actual instance facts cannot truthfully be known before allocation. After ROOT separately selects/authorizes a worker, observe and record exact instance identity, host, boot ID, PID namespace, retained storage/device, canonical interpreter/setpriv binaries and native closure. No such action is authorized by this packet.

The unchanged native collector is metadata-only and assumes the fixed Python3.12 profile. ROOT must issue its separate narrow metadata authority, replace its instance placeholder, retain raw native.stdout and native.sha256, and derive the exact SIX-field manifest: schema/files/directories/aliases/absent/python_path. Bound every observed canonical file and alias, exact directory children, required ps paths, Python ZIP condition and /etc/ld.so.cache. Inclusion of a path in this template is not evidence that it was observed or that loaded images are atomically attested. Actual caller strict checks remain decisive; a mismatch stops.

Observe hashes after allocation, then finalize native-manifest/raw/hashlist pins and matching interpreter/setpriv placeholders. Verify the fixed absent ZIP requirement of setup and exact sys.path profile; there is no metadata-only license to bypass either. ROOT must independently arrange the exact-worker backup stop before any actual launch. It is not created/canceled by these files.

## C. Freeze/authenticate ALL values BEFORE science

- Resolve every JC2_*_PLACEHOLDER in separate ROOT-owned final copies, not these sealed templates. This includes the single shared batch suffix, physical/native/durable facts, ROOT card/payload hashes, mathematical/task/setup/TERM/KILL times and per-control authority hashes. Keep the prospective copies disabled until all checks and explicit authority are complete. Setup refuses unresolved placeholders in itself and ROOT registration before deployment; that refusal is only statically inspected, not tested here.
- Derive authority SHA values from the unchanged dispatch.authorize metadata schema with the actual Python/native manifest hashes: status-disabled, caps499, inventory-missing, source-zero and valid forms. Refuse-hash intentionally supplies64 zeros. Valid/startup/dummy share the valid authority content but have distinct immutable paths. These hashes depend on observed metadata, not coefficient data.
- Freeze the ROOT execution card before hashing it into the registration; avoid a mutual card/registration hash cycle. The registration does not embed its own final SHA. After final serialization, compute its SHA and pin it in the external setup/install record. The only intentional runtime holes are the FOUR literal `ROOT_REGISTRATION_SHA256` values immediately after the unique matching flag in fullunit-produce/fullunit-positive/mutate/fullunit-negative. Do NOT replace them with a guessed self-hash inside registration, create another token or put any future candidate SHA there.
- Compare every complete CAPRUN vector against the accepted dispatcher construction with the final paths/metadata. First nine labels: five refusals, valid, two original startup refusals, dummy. Last four: new fullunit producer, unchanged positive checker, independent mutator, unchanged negative checker. Token forbidden in first nine. Metadata template vector sizes are51 for probe controls/dummy,46 for two original startup calls,54 for each fullunit entry,55 for mutation. These are string-list counts, not execution evidence.
- Registration limits600wall/500CPU/2GiB/16MiB and profiles control5/3, dummy5/3, fullunit120/99, mutate30/24 remain fixed. Original admission wall+15, sampled480CPU safety cutoff, original mathematical/task clocks and no-retry conditions remain. Outer RuntimeMaxSec660, TimeoutStopSec5, KillModecontrol-group, MemoryMax2GiB, OOMPolicykill, TasksMax32, LimitCPUinfinity stay unchanged. ROOT chooses actual UTC deadlines/ordering and ensures the original admission budget fits before launch; this packet chooses none.

## D. Exact future setup/launch/collection, only under fresh ROOT release

ROOT must verify all actual local staging references and installed hashes/modes, canonical source/native files, root0444/world-readable ancestry, exact five/three/three listings, fresh absent code/work/durable targets, original-root registration authentication and fresh16MiB tmpfs. All code/baseline/native/registration remain outside tmpfs. Outer streams are on the same working mount. Writer is fixed-profile science-owned0700; authority/frozen are ROOT-owned. Durable target is absent EBS ext4/xfs. Setup does not grant itself authority by passing these checks.

The main SYSTEM unit is `jc2-f10-r2-fullunit-ready-JC2_BATCH_SUFFIX_PLACEHOLDER.service`; TERM/KILL timers are `jc2-r2-fullunit-ready-outer-term-JC2_BATCH_SUFFIX_PLACEHOLDER.timer` and `jc2-r2-fullunit-ready-outer-kill-JC2_BATCH_SUFFIX_PLACEHOLDER.timer`. Both target that EXACT main unit and are checked active before launch. ROOT's independent backup-stop and later harvest/cancel actions must use the same suffix resolved from one binding table, not a separately typed T-time. Capture actual MainPID/InvocationID/start/cgroup or honest terminal proof; no fake live attestation.

Producer normal2/empty streams/no candidate plus exact INCONCLUSIVE_NO_CERTIFICATE receipt ends after10calls, exit2/scienceNONE. It is not a place/properness diagnosis. Positive full path requires candidate freeze, genuine FULL_SPECIAL_UNIT_CHECKED_NO_SOURCE_OUTCOME receipt freeze, mutator exact input binding/whole-byte restoration, fixture freeze, and precise check.py198/188/181 normal1 final-identity rejection. Candidate digest comes only from the immutable positive receipt; no prospective candidateSHA is needed. Checker INCONCLUSIVE_FIXED_PLACE or any early parser/native/resource/capture failure is STOP, not the negative pass.

Only the13-call completed path yields FULLUNIT_CHECKED_NEGATIVE_CONFIRMED/scienceNONE. No source conclusion follows here. ROOT separately needs an actual checked highest certificate and all accepted conditional lift/source premises. Do not substitute an unseen highest result.

After all owned groups are quiet, retain byte-for-byte durable custody, all receipts/streams/telemetry/source/native/authority/pre/post pins and actual process identities. Read results only after terminal writers and independently validate custody. Use the exact resolved unit/timer suffixes for harvest and cleanup; worker-stop duty is ROOT's separate authority. No retry, prime/cut/profile change, producer/checker baseline rerun, deletion or new science is authorized.
