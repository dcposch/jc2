# Contact Gram runtime contract — STATIC / UNEXECUTED

This package is a source-only port, not registration or permission to run. ROOT reported the exact contact source FIRST accepted at20:29 UTC without any source correction. That lifecycle notice does not supply a certificate, runtime result, or approval of this new runtime. The optional gate body was not read; there are exactly13 charged inputs.

## Fixed installation and root authority

Job: `f10-contact-gram-certificate-20260910`. ROOT must install in one immutable sibling directory exactly the selected producer.py, checker.py, corrected authority.py, unchanged probe.py, new dispatch.py and mutate.py. Directory ancestry is root-owned0755, scientific files readable0444, no untrusted sibling, concurrent writer or source mutation. Installed role hashes must equal dispatch.ROLE_PINS and CODE, not a coincidentally named pin elsewhere. ROOT separately pins this dispatcher, resolved Python, setpriv, CAPRUN, native manifest and every native/scientific input. There is no self-hash literal cycle.

The exact three SCIENCE basenames and hashes in dispatch/authority are mandatory; ROOT's `science_paths` maps each name to its unique resolved immutable file and the matching pin. These are future inputs, not bodies inspected in this task. The original authority hash is superseded by the charged corrected authority96ca421f; no other scientific source changes.

Outer launch vector is `PYTHON -I -S -B DISPATCH --registration ROOT_REGISTRATION`. Child vector is resolved `PYTHON -E -s -S -B SCRIPT ...`. Actual Python argv[0] must literally equal sys.executable as well as the resolved registered interpreter. ROOT's physical/UID/GID/namespace/cgroup/exclusivity/clear-environment/native closure and all full argv commitments are prerequisites, not inferred from an enabled flag. Authority is not a supervisor.

ROOT JSON schema is `f10-contact-gram-runtime/v1`, `enabled=true` only after separate authenticated registration; absent/disabled/mismatched registration refuses. Required fields are exactly the consumed inherited fields: schema,enabled,jobtag,exclusive_no_concurrent_writer,caps,profiles,preflight_wall_seconds,uid,gid,instance_id,hostname,files,outer_argv,source_root,output_mount,writer_dir,frozen_dir,authority_dir,durable_dir,durable_device,cgroup_path,pid_namespace,mathematical_deadline_utc,task_deadline_utc,native_manifest,pins,commands; this port adds science_paths. No concrete values are supplied for future identity, native inventory, deadlines or registration SHA.

Files roles: python,setpriv,caprun,producer,checker,authority,probe,mutator,dispatcher. Native schema remains accepted `f10-native-closure/v1`; bounded file inventory is checked before/after every child. ROOT must independently establish its completeness, actual mapped paths, aliases, directories, boot identity, environment and outer-supervisor settings; hashing a supplied list does not establish closure. No guessed native hash or observation appears here.

## Commands and caps

The dictionary commands binds fourteen literal CAPRUN vectors: refuse-disabled,refuse-wronghost,refuse-argv,refuse-caps,refuse-missing,valid,startup-producer,startup-checker,dummy,produce,check,mutate,negative-uphi,negative-bernstein. No command substitution at runtime. The script-argument suffixes are built in dispatch.run in this order:

- probe: `--mode authorize|dummy --job JOB --registration AUTH --output PAYLOAD`;
- wrong-job science uses literal WRONG-JOB and no input file for the checker startup probe;
- producer: `--job JOB --registration AUTH --output PAYLOAD`;
- checker: `--job JOB --registration AUTH --input FROZEN --receipt PAYLOAD`;
- mutator: `--job JOB --registration AUTH --input FROZEN --positive-receipt POSITIVE --uphi UPHI --bernstein BERNSTEIN --receipt PAYLOAD`.

The authority operation is probe/produce/check/mutate, with exact sys.argv, python_flags, full_argv and science_paths. Each child authority is prospective, exclusive, root0444 and hash-bound before Popen. The two mutant checker authorities pin their own newly frozen variant, not the original certificate. Mutator authority additionally pins the frozen positive receipt.

Joint science ceilings:600wall,550CPU,2147483648RSS,16777216aggregate working bytes. Separate preflight wall ceiling180. Profiles [wall,softCPU,RSS]: preflight[5,3,2147483648],dummy[5,3,33554432],produce[400,359,2147483648],check[90,89,2147483648],mutate[10,9,2147483648],negative[30,29,2147483648]. Five science phases total560 nominal wall and520 hard child CPU (360+90+10+30+30); remaining40wall/30CPU is not a measured overhead guarantee.

CAPRUN4435279d remains unchanged, including rss-sample0.05 and TERM grace1. Its CPU ceiling is per child process, RSS is sampled group RSS, aggregate children usage includes reaped CAPRUN/ps overhead; dispatcher self CPU is separately reported. ROOT's independent cgroup/outer supervisor must enforce the whole joint budget including dispatcher and unaccounted descendants. Neither profile addition nor sampled accounting is a proof of aggregate hard enforcement. A late, overflow, resource or incomplete-custody case stops without scientific acceptance. No retry, cap change, command-only rsssample edit, fallback engine or prime search.

## Exact lifecycle and outcomes

The five authority refusals preserve their exact reasons, valid nonroot sentinel, two actual WRONG-JOB refusals, and actual same-PGID orphan dummy with64MiB allocation under32MiB cap and identity-matched TERM/KILL/reap checks. No added refusal farm. Quiet-before-body and all pre/post pins apply to every child.

Producer normal0 must have empty stderr and exact `PRODUCER_CANDIDATE sha256=H bytes=N\n`; producer normal2 may proceed ONLY with a complete bounded artifact, empty stderr and exact `PRODUCER_INCONCLUSIVE sha256=H bytes=N\n`. Missing/partial output or generic exception normal2 is STOP. Dispatcher hashes bytes and metadata, never parses certificate coefficients.

The independent checker then must complete either:

1. normal0, exact stdout `PRESCRIBED_CONTACT_EXCLUDED_BY_CERTIFICATE\n`, empty stderr and exact bound positive receipt; or
2. normal2, exact stdout `INCONCLUSIVE\n`, empty stderr and complete bound receipt with one of the two literal checked reasons in source. This proves only that identity/factor/expansion were checked before an inconclusive sign test. It ends after11 calls, status CHECKED_INCONCLUSIVE_NO_SOURCE_OUTCOME, without semantic-control coverage.

The positive receipt explicitly does NOT independently check Gram determinant equality. It covers the prescribed triangle t<1, not full-window equivalence, source existence/emptiness or JC2. Only a positive independent check enables the two controls below.

Mutator normal0 prints only `TWO_MUTATIONS_PREPARED_NO_SCIENCE\n`, has empty stderr, exactly two fixtures plus bounded receipt. Each negative checker must be NORMAL_EXIT2, resource null, empty stdout, NO receipt and exactly:
`INCONCLUSIVE: full contact cofactor identity\n`
or
`INCONCLUSIVE: full Bernstein expansion\n`.
Parser/authority/hash/size/native/resource failures, wrong stderr or receipt presence never count. Success is14 calls and CONTACT_CERTIFICATE_CHECKED_NEGATIVES_CONFIRMED, with science NONE; this source package has no such observed result.

## Independent two-control mutator

No producer/checker arithmetic is imported. Mutator imports only metadata authority before authorization. It checks EC2/Linux/job, root authority, resolved interpreter flags, fixed three code hashes, self pin, positive receipt pin and original raw SHA before Fraction or coefficient parsing. Wire JSON rejects duplicate keys, floating/nonfinite/numeric coefficient tokens; every sparse and dense rational is a canonical reduced decimal-string pair, denominator positive, at most12000bits and4096characters. All four sparse-polynomial inventories, exponent bounds/order, dense rectangle and fixed j are checked independently. Original canonical serializer bytes must equal the full input bytes.

Control1 is exactly Uphi+1. The constant term is inserted if absent, replaced if present, or removed if its new rational is zero. No other subtree changes. Control2 changes the existing dense Bernstein entry [0][0] by+1, with no search or alternate if that fixed entry exceeds wire/bit limits. The whole input is restored after inverse subtree/coordinate edit and reserialized byte-identically before either fixture is written. These are genuine semantic perturbations: the first changes the contact cofactor identity by the nonzero fixed phi; the second changes expansion by the nonzero Bernstein basis term (1-t)^(52-j)(1-v)^52, even when52-j=0. No sign-only test is substituted.

Mutation receipt schema f10-contact-gram-mutations/v1 binds input SHA/bytes, CODE/SCIENCE, mutator SHA, positive receipt SHA, authority SHA, exact two paths/SHA/bytes, original/new rational-string pairs and whole_byte_inverse_restored=true. Uphi metadata includes constant_existed and term_deleted booleans; Bernstein selection is row0,column0. Dispatcher validates metadata and hashes only, not coefficient arithmetic. Rational inverse correctness belongs to the independently reviewed mutator and meaningful checker outcome, not a fabricated dispatcher recomputation.

Baseline plus two variants and all logs/metadata must fit the actual single16MiB tmpfs. Mutator checks both triple-artifact sum and current free space for two outputs/receipt plus65536bytes. This reserve does not guarantee all remaining logs fit: the dedicated mount is the aggregate physical bound and any later ENOSPC is STOP. No duplicate baseline copy, capacity increase or silently missing fixture. Each fixture has a per-file bound LIMIT-4096, receipt32768bytes. All failures can be unavailable/STOP; no alternative mutation is attempted.

Exclusive no-follow0444 fixture writes, fsync/readback, authority postchecks and parent fsync precede normal success. Dispatcher performs root-owned0444 rename/freeze/readback and both writer/frozen-directory fsync before each use. Post-input and authority hashes are checked after quiet return. Source and native files reside outside the working tmpfs.

## Remaining external duties; no deployment package

ROOT must independently authorize the final source hashes, immutable native closure, physical boot and namespaces, complete environment, systemd cgroup limits, absolute deadlines, actual full argv, separate preflight/science windows, independent stop and cleanup, absence/exclusivity, read-only source installation and sufficient actual storage. Source caps/profiles are UNMEASURED and may stop before useful output. Per-process CPU and sampled RSS limitations are inherited, not repaired by rhetoric.

CAPRUN and all owned PIDs/groups must be quiet before accepting bodies or copying durable output. Exact live identities and whether live Python observation was missed are retained; a missed observation is not fabricated. ROOT independently reconciles terminal identities and cgroup quiescence. Durable custody is bounded200files, hash-checked and file/directory-fsynced; ROOT must ensure parent-directory durability, terminal archive/readback, external stop and no further worker writers before intake. The wrapper neither stops a worker nor authorizes one. There is no actual registration, disabled deployment scaffold, source certificate, syntax/import/test result or timing claim here.
