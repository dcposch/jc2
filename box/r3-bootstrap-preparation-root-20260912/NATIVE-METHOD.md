# Native method selection — inert preparation, not qualification

ROOT, September12,2026. This is an input-preparation note, not a new live
queue, executable parser, approved spec or native evidence. No collector,
interpreter, source, syntax check or dummy has been run. No worker exists.

The unchanged collector is
box/caprun-closed-scope-deployment-prep-root-20260911/native-metadata.template.sh,
SHAb386a6cd2d732e6618b994fdbdec67d8e2a59fc997206f296b4cf55513684ed0.
Its exact-instance placeholder keeps it disabled. ROOT read the whole source.
It emits raw metadata, not a JSON manifest. Its ldd calls are native loader
operations on nominated system files, not evidence that no native code runs.
Any future collection remains bounded AWS-only under a separate registration.

The existing raw-record grammar and serialization method is in
box/f10-direct-rows-release-prep-root-20260911/NATIVE-METADATA-RECIPE.md,
SHA584f044f60c64c3e1421271cd2d63ad4fa48d09dbfb50999a5e430356dd1a2f5.
ROOT read all96lines after this pin. Select ONLY its collector custody,
raw-record grammar and six-field serialization through native.sha256.
Do not reuse its old source tag, nominated binary/version as a fresh fact,
thirteen-command policy, actual registration, launch or clock instructions.

## Exact selected transformation

After independent terminal exit0/empty-stderr and raw/script hash checks,
read the bounded raw metadata completely. Retain unchanged raw bytes, stat
records, host/boot/namespaces and package rows. Validate unique start/end,
fixed header, every declared record, safe paths/names, paired SHA256/FILE
records and all counts. Reject an unparsed line, duplicate/conflict, missing
dependency, unsafe ownership/mode, inconsistent alias, undeclared child or
present/absent conflict. This note does not implement that parser or claim
the old script has freshly run.

The full JSON has exactly schema/files/directories/aliases/absent/python_path,
schema f10-native-closure/v1. Files maps canonical paths to literal digests;
directories maps each directory to its sorted unique direct-child list;
aliases maps each alternate path to a declared canonical target. Preserve
all three declared Python paths in order and all absent entries. Require
1..3000 files,1..1000 directories and <=1048576 bytes after JSON+LF.
native.sha256 lists each full-closure file exactly once. Sizes and counts
are metadata; no mathematical integers enter numeric JSON conversion.

The source subset JSON has exactly job_tag/files and <=65536 bytes. Its
CURRENT literal job_tag is f10-source-cone-r3-necessary-rows-v1, not the
recipe's retired direct-rows tag. Both /usr/bin/python3.12 and
/usr/bin/setpriv must be present in the new full closure with the SAME
digests; the subset does not replace that closure. Confirm the selected
actual interpreter path matches registration, not an assumed alias.

The current bootstrap f04f1b7b16bed194e5d7b503a78fe9bd36e3bd58da3696adf6a40bfdbdcd17d2
lines276–333 independently checks these six fields, actual sys.path, repeated
pins, exact directory inventories, aliases/absences, both ps paths and actual
mapped native files. Those source lines and the JOB declaration were read
here; the whole bootstrap's prior FIRST is not a newly repeated code review.
The collector's expected Python paths alone are not observed sys.path.

## Boundary still requiring actual preparation and observation

This selection is not the complete worker-independent qualification package.
An exact assembly/conversion implementation or concrete checked serialization
step still needs to be bound to the actual package. New launcher and any
administrative/transport native closure are separate from the scientific
six-field manifest. Native startup/library semantics, installed refusal
format, ACLs, cgroupfs, devices/namespaces,64KiB snapshot fit and all timing
remain UNQUALIFIED. No old raw output or host hash may stand in for new facts.
New authorities are bootstrap outputs; no old authority card is preinstalled.
No source byte, cap, runtime schema, gate or deployment permission is changed.
