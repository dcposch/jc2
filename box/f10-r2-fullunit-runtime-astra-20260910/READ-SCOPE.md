# Exact read scope

First action2026-09-10 16:55:58UTC: own box/final/transaction absent, all eleven literal paths and SHA values matched the invitation BEFORE any body. PINS.json records the same vector. Hard17:18/reserve17:15 never reset.

WHOLE same-byte prior reads reused after fresh pins: four fullunit entry/produce/check/CONTRACT inputs and original arithmetic.py, all actually read whole during this agent's earlier fullunit source preparation. Current partial re-reads of produce/check add no stronger claim. No original baseline, candidate, receipt, coefficient body or actual artifact was accessed, hashed or enumerated here.

Fresh WHOLE reads: accepted status-fixed dispatcher in1–250,251–500,501–end; old mutator1–end; status-fixed CONTRACT alone1–end; highest runtime gate alone1–end; highest status-fix gate alone1–end. The new fullunit gate was read through its last marker in the first combined output. That combined call clipped later content; status CONTRACT and both highest gates were recovered in separate complete reads. No clipped content is treated as read. No provenance paths, referenced reports, native files, CAPRUN source or probe body were opened. Their unchanged interfaces are accepted scoped imports from the charged runtime gates/caller, not re-reviewed.

Owned mutate.py was read WHOLE in1–175 and176–end. Owned dispatcher was read WHOLE by the already complete original dispatcher plus the exhaustive full-file unified diff: every unchanged byte is the accepted original read above, and every changed hunk was output and manually read. SOURCE-DELTA.diff includes BOTH complete file comparisons and is separately checked byte-for-byte against current sources before seal. No syntax/import/AST/compile/test or mathematical evaluation was performed. Source construction used documentary text substitution and apply_patch, not execution of any source.

Own static review traced metadata-before-math, literal P7 and dense denominator checks, full20-source/18-generator reconstruction, existing-term selection/deletion, canonical whole-byte restoration, updated full receipt/schema/source pins and exact checker198/188/181 error path. Dispatcher retains original preflights/cap/argv/identity/native/quiet/freeze/durable-copy code except the named fullunit migration and selection metadata described in the diff. Whole CONTRACT/disabled plan/report/PINS were read before publication. Own OPEN/collision/current-pin checks precede the marker.

Boundary: source preparation only, disabled/unexecuted/unreviewed new code. All eight original/fullunit science files remain outside this owned package unchanged, as do probe and CAPRUN. No worker, environment, registration, acceptance, source result, actual witness, computed speed or follow-on authority. One FIRST review plus any future ROOT registration remains required; actual runtime, sufficient place/witness and negative behavior are unmeasured.
