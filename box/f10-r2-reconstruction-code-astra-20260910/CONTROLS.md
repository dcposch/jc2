# Prospective exact changed-object controls — NOT EXECUTED

No coefficient packet or fixture exists in this delivery. The following are
future mutations of a freshly formed, independently accepted packet. Each
mutation must be applied alone, in a fresh checker CLI process, retaining
canonical rational strings except where parser rejection is the explicit test.
Original acceptance is a prerequisite, not an observed result here.

1. Wrong late operator: in a separately pinned producer mutation, replace
   `-(12-h)` by `-18` only when h=6 in `op`; keep all other formulas. The
   independent checker uses h-12=-6 and full Euler B, so the altered column,
   fixed-part upper equations or whole mate must fail. A formation failure is
   also a safe rejection, not evidence the reference producer was correct.
2. Critical index: replace the actual y contribution in a mutated formation
   with `(12-2*i)*y*D_(i+1)` for i=0..4, leaving the homogeneous k-column alone.
   The checker independently constructs N and uses UNshifted D_i, AND reads
   the original source Euler coefficients. A homogeneous-column test alone
   would be insufficient.
3. Dropped particular: replace gap1 `part` by zero while retaining the other
   arrays. If that particular happens to be zero, choose the first nonzero
   particular in ordered gaps1..4; if all are zero, change the constant
   particular forcing by +X_h and re-form that band only. The full forcing,
   full mate and rho-subtraction identities reject; do not count an unchanged
   zero-to-zero packet as a negative control.
4. Middle shift: change the reconstructed Vpar constant by +U*d1, preserving
   all other data. If this polynomial is zero, the corresponding changed
   operator control sets formal U=d1=1 only in the shift identity, not in a
   claimed source packet. Literal A and v0-U*d1 checks expose the omitted shift.
5. Late fixed term: delete U^2*theta^3 from gap6 `fixed` and its mate, retaining
   target -U^2*theta^5. The fixed-support and complete Euler checks reject;
   if U^2 is zero in an artifact, use the changed formal forcing control +1
   at that fixed slot and keep the rest, rather than claim an unchanged test.
6. Ell mate: replace gap7 B_var by zero. Its required theta^2 coefficient is
   exactly one, so this is unconditionally changed and the -theta^4 upper
   target/full affine identity rejects it.
7. Deleted compatibility: remove rows.Psi[2], even if its polynomial is zero.
   Exact three-slot length/19-name interface rejects before mathematics.
8. Corrupt inverse: add one to coordinate0 of field.units.W[1]. The inverse
   product changes by the unit W, so whole-B product rejection is guaranteed.
9. Last basis coordinate: add one to coordinate6 of field.W, retaining its
   other six coordinates and all witnesses. Independent W reconstruction
   rejects in the full seven-coordinate basis; no selected embedding is used.
10. Largest retained coefficient: among all nonzero coefficients in
    rows.Psi, rows.K1 and rows.K0, select maximal absolute rational value,
    breaking ties by row order, term order, basis index; replace q by q+1,
    canonically reducing numerator/denominator and deleting a zero sparse
    term if necessary. Keep all other arrays. This necessarily changes the
    selected coefficient; literal Jacobian/Psi readback rejects. If every row
    is zero, add coefficient one to the first row's constant monomial instead.
11. Scale targets: change interface.targets to K1_0-U and K0_0-1, or delete
    the two names from interface.rows. Exact metadata comparison rejects;
    neither z=s^2 nor Laurent s may be silently specialized to one.
12. Duplicate JSON key: duplicate `source.U`; JSON object-pairs rejection.
13. Duplicate sparse term: append an existing nonzero Ahat term; sorted,
    duplicate-free sparse parser rejects before polynomial arithmetic.
14. Oversize: exceed 15 MiB packet bytes, 4096 terms in one polynomial,
    50000 total parsed terms, or exponent16; reject at the respective bound.
15. Giant decimal: use a 4097-digit numerator, or nonreduced 2/2, denominator
    0, -0/1, leading zero, float, NaN or a JSON numeric coefficient; reject
    before Fraction conversion (the CLI's process-local integer cap is4300).

Mutation sources/packets, dummy authorization, registration, native inventory,
execution and success evidence are ROOT-owned future work requiring separate
authority and FIRST Fable code review. This file is a specification, not a
control generator, fixture, run instruction or validation receipt.
