# Fixed89/0 highest special-projective instrument

STATIC / UNREVIEWED / UNEXECUTED. This is exactly ONE prospective place, p=89,beta=0, and Route P only. The place, source integrality, residues, cofactors and boundary conditions are UNKNOWN until a separately registered run. No baseline coefficient has been read locally. No new runtime, mutator, fixture, worker or execution authority is included.

## Mathematical acceptance

The charged accepted properness theorem uses the accepted import that B=Q[Z]/P7 is a FIELD, not merely a degree-seven algebra. The code does not establish irreducibility; ROOT binds this exact accepted source premise. It reconstructs the literal monic P7 and compares all eight rational coefficients against the entire frozen packet. The exact same seven-coordinate representation is used for every row coefficient. It never selects a characteristic-zero factor.

Both implementations independently check primality of the fixed integer89, P7bar(0)=0 and P7bar'(0)!=0. They require every dense rational denominator in the complete three Psi rows and monic P7 to be an89-unit. This deliberately includes all positive-z terms, stronger than the minimal20 z-free slots; no coefficient is skipped before its denominator check. At most24 full-row B-coefficient slots can occur under the verified support/weights, each with seven rational coordinates. A zero missing sparse term is implicitly zero; explicitly stored all-zero rational vectors, duplicate/unsorted sparse terms, numeric JSON, noncanonical fractions or wrong exponents are rejected.

This supplies the concrete integral order O=Z_(89)[Z]/P7 and its localization A at (89,Z), a DVR because of the checked simple root and accepted FIELD premise. Its fraction field is B and residue field F89. This is a reduction A -> F89, never a map B -> F89. Once all special projective zeros are excluded, proper specialization establishes the corresponding generic emptiness over B and hence every complex embedding. ROOT must adjudicate this theorem interface; success is not a source zero/point or basis computation.

For EVERY original term, before taking z=0 or X1=1, both scripts validate variable order X1,X2,X3,z,S,vartheta, weights1,2,3,5,1,2, no S/vartheta support, exact row weight5/6/7, complete7-coordinate coefficient and increasing distinct exponents. They validate every rational coefficient even if it specializes to0 or is multiplied by beta^i=0. They then project z=0,X1=1 and name u=X2,v=X3. Weight bounds force

    H5=P(u)+Q(u)v,
    H6=B3(u)+B1(u)v+B0 v²,
    H7=C3(u)+C2(u)v+C1 v²,

with u-degree bounds(2,1),(3,1,0),(3,2,0). Build, independently on each side,

    F=Q² B3-PQ B1+B0 P²,
    G=Q² C3-PQ C2+C1 P²,

both of degree<=5. The required modular certificate is cF+dG=1. The denominator-free identities

    F=Q²H6-H5[QB1+B0(Qv-P)],
    G=Q²H7-H5[QC2+C1(Qv-P)]

show special affine-chart emptiness INCLUDING Q=0. No P/Q Bezout certificate and no Q inverse are required. G retains the entire PQC2 term; no transplanted leading-coefficient formula is used.

The SAME SPECIAL boundary X1=0 is separately checked from actual extracted coefficients:

    alpha=[X2 X3]H5 !=0,
    beta_boundary=[X2³]H6 !=0,
    gamma=[X3²]H6 !=0.

Then alpha X2X3=0 and beta_boundary X2³+gamma X3²=0 force X2=X3=0 over every extension of F89. The remaining weight7 boundary scalar is unnecessary for this sufficient implication. The characteristic-zero infinity theorem is NOT substituted for these three modular checks. No boundary scalar is trusted from the certificate, and no prior mod89 table is a code input. This is Route P, with no Route F fallback or requirement that f5/g5 be nonzero.

Properness of the weighted projective model now turns special projective emptiness into generic highest-form regularity. The accepted cyclic consequences are only theorem-level follow-up: no basis, multiplication table, membership certificate, actual full-source outcome, all-r or JC2 conclusion is produced by this instrument. The baseline remains CHECKED_BUT_NOT_PROMOTED pending the separately selected6/10 controls; no later lifecycle or live review is consumed here.

## Independence and bounded algorithms

entry.py shares metadata/JSON/file handling and typed exceptions only; no mathematical helper. produce.py loads only the original pinned arithmetic.py after the full metadata barrier, and uses its rational polynomial functions to reconstruct P7. Its own modular coefficient reduction, shape extraction, finite-field polynomial arithmetic and Euclid are new bounded code. Modular inverses use Fermat exponentiation with product readback after trial-division primality check.

check.py loads only original check_arithmetic.py, never produce.py or producer arithmetic. It re-forms P7 with its independent rational polynomial implementation; verifies primality independently by all divisors2..9; computes checked modular inverses by integer extended Euclid; parses original seven-vectors and accumulates a dense v/u box; independently obtains all boundary coefficients and F/G. It consumes no producer-extracted row, F/G, boundary scalar or gcd claim. It verifies ALL11 u-coefficient positions including zeros in cF+dG=1, not selected evaluations.

Both retain zero/constant/degree-drop cases. Producer extended Euclid starts with exact F,G and tracks both cofactors, at most8 divisions; every remainder degree drops because its leader is inverted in the proved finite field. Intermediate modular polynomial degree<=16, output c,d<=5. Checker product degree<=10. All modular arithmetic is reduced to0..88 and inverse loops are bounded (checker<=16 steps). No B-field coefficient is inverted merely to pick a residue component. There is no prime search, alternative residue root, rescaling or formula rebuilding beyond the literal P7.

Bad fixed-prime/simple-root, any selected denominator not an89-unit, or failed special boundary is a NONDECISION. Producer also returns NONDECISION for zero/nonconstant gcd or refusal bounds. A failed witness never proves a source point or nonregularity. For example a chart with P=1,Q=0,H6=H7=0 is empty but gives F=G=0; this sufficient instrument may decline an empty chart. The exact characteristic-zero instrument remains the accepted fallback, but no fallback is dispatched here.

## Wire and source boundary

Future baseline SHA dac655a7ce75287b06010171bce66af1a644aff81de9620a452457a5f65a9587, exactly1652675B, reader cap15MiB. This is a literal specification only in this task. The baseline's other semantic fields are bound by its SHA and earlier independent checker tier; this instrument does not replay the complete reconstruction checker. The three WHOLE Psi rows, interface and P7 are independently inspected at runtime.

Strict canonical ASCII JSON with sorted compact encoding and a trailing newline; duplicate keys and ALL JSON numeric tokens are rejected. Source rational coordinates have reduced numerator/positive denominator strings, no leading zeros or -0, each signed integer string<=4096 characters. Process-local conversion ceiling4300 is retained. The p-unit denominator test follows canonical rational validation. Sparse exponents are canonical strings0..16; row cap4096 is a refusal bound, while unique exact weights imply the much smaller24 total slot bound. Resource caps apply even inside unchanged rational arithmetic.

Certificate exact keys:

    schema='f10-r2-highest-modular89/v1'
    baseline_sha256=BASE_SHA
    prime='89', root='0'
    chart='z=0;X1=1;u=X2;v=X3'
    modulus=[eight canonical rational strings, literal monic P7]
    c,d=[dense ascending-u canonical residue strings '0'..'88']

c,d have at most6 entries; no trailing0; [] is zero. No JSON-number coefficient, modulus factor, producer F/G, row or boundary claim is serialized. Certificate cap2MiB; no actual coefficient-size claim. Sources and baseline are pinned/rechecked before parsing and publication; certificate is pre/post-read on successful checking. Files are no-follow, canonical absolute distinct paths, regular files with read-time inode/size/mtime/ctime checks. Writes are exclusive, fsync/readback/0444, with directory fsync. A partial/unreceipted file is not accepted and is never overwritten.

## Exact disabled CLI and ROOT obligations

Both modes preserve the17-entry ordered vector (eight flags):

    PYTHON -E -s -S -B ROLE.py --registered-job f10-r2-highest-modular89-v1 --root-registration-sha256 ROOT_SHA --self-sha256 ROLE_SHA --entry-sha256 ENTRY_SHA --arithmetic ORIGINAL_ROLE_ARITHMETIC --baseline BASELINE --certificate CERTIFICATE --receipt FRESH_RECEIPT

ROLE is produce or check. Exactly the three new files entry.py/produce.py/check.py occupy an isolated sibling directory. The producer arithmetic and checker arithmetic remain at separately pinned ORIGINAL paths and are unchanged. Linux/exact Amazon EC2 DMI and exact tag/ordered flags are required before JSON or mathematical imports. Missing/zero root/self/entry hashes refuse. No old authority mode or nine-argument signature is repurposed.

The binding hashes are NOT ROOT authentication. ROOT must authenticate its separate immutable registration, pin all scripts BEFORE loading entry.py, freeze source/baseline/certificate, enforce exact ancestry/paths/interpreter/native closure, and keep the sibling import directory free of shadows. -E -s -S -B retains this controlled sibling path; -I would not. The script's post-read source pin does not replace ROOT's pre-exec check or close a same-user TOCTOU race by itself. No authority, supervisor or runtime code is added here.

The ceiling remains the existing joint600wall/500CPU/2GiB/16MiB, still UNREGISTERED for this new code. ROOT must assign exact child profiles and one positive+one meaningful control within it before any execution. Input/native immutable files outside output partition; only one certificate and separate producer/checker receipts, each<=32768B. No performance estimate, cap increase, automatic retry, second prime or worker launch is permitted.

## Receipts and failure predicates

Receipt exact keys: schema='f10-r2-highest-modular89-receipt/v1', status, science_outcome='NONE', mode, prime='89', root='0', root_registration_sha256, sources{absolute role/entry/selected arithmetic:SHA}, baseline_sha256, baseline_bytes='1652675', certificate_sha256. Missing success receipt or mismatched pins is not success.

Producer exit0, empty stdout/stderr, CANDIDATE_UNCHECKED means only a candidate. Any candidate-stage ValueError yields INCONCLUSIVE_NO_CERTIFICATE, null certificate_sha256, exit2 and no candidate; other exceptions/caps are STOP and can leave no accepted output. Checker typed sufficient-condition failure emits INCONCLUSIVE_FIXED_PLACE with the examined certificate hash, exits2 and proves nothing. Invalid wire/schema/pin/shape or invalid identity produces uncaught ValueError/exit1 (or another STOP), never a source verdict. A successful checker has empty streams, exit0 and exact status HIGHEST_SPECIAL_PROJECTIVE_CHECKED_NO_SOURCE_OUTCOME after all place, integrality, boundary and11 identity checks. The declared accepted theorem may then be adjudicated by ROOT at exact scope; code does not promote it.

## Single prospective meaningful control — design only

After a positive certificate, independently reconstruct Fbar,Gbar. If Fbar!=0 mutate c; otherwise mutate d, requiring Gbar!=0 (forced by the positive identity). Add1 MOD89 to the chosen constant coefficient, insert a zero polynomial's constant slot if necessary, and trim trailing zeros only as needed for canonical dense encoding. Interior zeros are retained. Record the original coefficient, original array length, selected side, old/new certificate SHA, and inverse restoration of the WHOLE original object. The change in the identity is nonzero Fbar or Gbar; wrap88->0 does not erase that semantic difference in characteristic89.

With every other input unchanged, the fresh checker must reach final stderr line exactly `ValueError: CHECK FAILED: fixed-place full modular Bezout identity\n`, NORMAL_EXIT1, empty stdout and no success receipt. A parser, pin, denominator, boundary or resource rejection is NOT this control. Traceback prefix/path/line fields must be matched to the frozen checker in ROOT's separate future runtime acceptance contract; no substring-only test. No mutator source, fixture or execution is included.

Manual changed-hypothesis controls: omitting positive-z denominator checks violates this deliberately conservative model contract; omitting same-special boundary allows escape to infinity; testing only residue coordinate0 without first validating all seven denominators drops integrality obligations; changing a cofactor multiplying a zero eliminant is not meaningful; nonzero characteristic-zero boundary coefficients need not remain nonzero modulo89. No actual instance of these controls was generated or run.
