# Scope

First action2026-09-09 20:09:57 UTC. Controlling stop20:24:57 UTC,
actual+15 minutes, earlier than20:26. Own box/report absent before begin.
All four exact input hashes matched before reads. All four reports read
WHOLE at their accepted interface scope; the first aggregate output truncated,
so resonance, early tower and gate were reread separately to EOF. The Euler
report was fully visible and its prior WHOLE read also reusable.

No provenance path, middle-band report, univariate/runtime/code/data artifact,
current ledger/review, live peer or other report was read. No web/network,
AWS/SSH, process audit or agent. ZERO mathematical subprocess, arithmetic
script, import/compile/test, evaluated source coefficient, CAS or solver.
Only manual algebra, documentary reads/hashes, apply_patch and existing
metadata transaction tooling.

Accepted17q is used without any nonemptiness premise. The gate's printed
assignment of -ell*S*t^4 to weight5r+1 is NOT adopted: its weight is4r+1.
Root's immediate operator correction replaces the candidate's -j*C'V by
-(n-m+j)*C'V. Both corrections are independently checked in the report.
Earlier source/standing headers remain frozen; no new acceptance is claimed.
