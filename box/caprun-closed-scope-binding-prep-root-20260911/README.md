# Disabled documentary binding preparation

ROOT began reference inspection 2026-09-11 10:01:36 UTC; original reserve
10:13 and HARD 10:16 UTC, never reset. This directory is a field map and
sequence, not executable source, registration, host qualification or release.
All actual values remain UNFORMED. No worker, candidate, source, import,
syntax/AST check, test, dummy or CAS has been executed for this preparation.

ROOT-BINDING-MAP.json records the four complete source JSON templates and
their exact hashes. Its 89 string occurrences cover 33 distinct placeholders;
29 positive integer fields and three explicit approval flags remain unknown.
The one retained null is phase_policy; it must stay null. Complete recursive
metadata comparison found every mapped occurrence and typed field equal to
the original templates, with no extra or missing string occurrence. This is
documentary comparison, not a trial execution or scientific acceptance.

## Sequence

1. Before allocating, finish administrative/native qualification selection,
   fresh fleet/quota/image checks and original UTC deadlines. Recheck current
   hashes of accepted source, runtime, holder, corrected outer admission and
   deployment templates. No expired clock or old worker identity is reusable.
2. Allocate at most the separately approved one-batch worker. Preserve its
   EBS and arm exact-ID coordinator retirement at the original UTC deadline.
   Before starting any holder, install the pre-admission stage, collect real
   identity/native/physical metadata, authenticate root namespace and cgroup2
   visibility, source/native byte inventories, administrative helper versions,
   ownership/ACLs, no delegation, no migration, no concurrent ROOT writers or
   entrants, and the exact seven-refusal environment. Permissions alone do
   not prove those external premises. No source probe is permitted here.
3. Bind only genuinely observed pre-holder strings and the durable device.
   The preflight policy file has SHA256
   f87600f59c8e298b630faaf72d8a7aa7ed725b2d066df954516c8d5ca97f15b8.
   Keep disabled registration local; final remote registration remains absent.
   Select the holder cutoff strictly before admission, retaining its unchanged
   120-second monotonic ceiling. Do not start it while broad preparation remains.
4. Immediately before invoking remote admission, inspect the COORDINATOR
   user-systemd retirement timer, triggered service, full AWS argv, profile
   personal, region us-east-1, exact actual instance ID and original future
   UTC deadline. Freeze the actual output and card binding. Worker-local
   systemd is not the authority. Missing/mismatch means no admission and
   existing retirement; never fake a remote timer or rearm the clock.
5. Inside the holder ceiling, capture the actual PID/start/InvocationID,
   namespaces, cgroup identity and sole membership. Create and verify exactly
   nine fixed empty direct leaves using the prepared leaf step. Bind actual
   outer and leaf devices/inodes, never samples or plausible integers. The
   common command device placeholder requires independent equality of all
   nine observed leaf devices. Abort on any mismatch; no retry or cap change.
6. Form nine per-label authorities from the complete authority templates.
   Valid/dummy scopes use their own actual leaf identities and must equal
   the standalone policy copies. Serialize each authority separately using
   recursively sorted keys, compact separators, ASCII JSON and one terminal
   newline, matching dispatch.py emit at lines97-103. Hash exact bytes, not
   the bundle. Bind the eight actual command digests; refuse-hash deliberately
   retains its supplied 64-zero digest. Preserve the registered refusal
   mutations, wrong startup job tags, exact commands and fixed limits.
7. Compare all resulting JSON and every argv slot against the templates and
   authenticated observations. Require no unresolved placeholder, exact nine
   labels/order, phase_policy null, typed_slot_policy empty, CPU origin string
   zero, and PREFLIGHT_ONLY_9. Keep unqualified mathematical place metadata
   explicitly unqualified. Only ROOT's genuine approval may set the three
   flags; this document is not that approval. Freeze final registration/card
   once through the prepared final-install step; compare installed bytes.
8. Only separate actual clearance admits one strict nine-phase preflight.
   Any failure is NONDECISION with evidence preservation and retirement.
   FULL13/science, relaxed dummy, counter rebasing and automatic retry remain
   forbidden. Future same-PID exec evidence cannot be claimed beforehand.

The administrative JSON operations may run in the coordinator's ordinary
documentary tools; no new candidate serializer or runtime framework is added.
This map reduces live binding work but does not claim the 120-second window
has been successfully exercised. Kernel/native qualification and actual
metadata binding remain future work. No JC2 or all-R conclusion follows.
