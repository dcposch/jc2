# Necessary-row registration preparation — DISABLED

ROOT reference inspection first06:20:09UTC September11. Original reserve06:30/
HARD06:33 was explicitly recorded at06:22:03 after that inspection; the earlier
first action is retained, not reset. This is inert metadata preparation, not
source execution, a review verdict, an enabled registration or a worker.

The five JSON documents port the predecessor offline registration packet.
Administrative deployment paths become jc2-r3-necessaryrows-20260911a; job and
runtime policy identities become necessary-rows. Six changed source/runtime
hashes are replaced by the independently collected candidates; both arithmetic
engines and CAPRUN remain unchanged. Existing historical documentary reference
paths are NOT renamed. Seventeen source-reference entries include the new
adopted interface, design FIRST and disabled candidate contract. These ROOT
reference paths are documentary, not extra scientific siblings.

The new contract digest is JC2_SOURCE_CONTRACT_SHA_PLACEHOLDER everywhere;
review-clearance status and both FIRST digests remain explicit placeholders.
Neither old registration nor old FIRST hashes are relabelled as current
clearance. enabled and exclusive_no_concurrent_writer stay false. Actual
worker/host/boot/namespace/native/interpreter/device/clock values are unformed.
No ROOT-READY-INPUTS.sha256 or final preflight policy is present.

ROOT independently compared all13 authority/policy objects and all13 complete
command vectors to the candidate dispatcher's metadata-only authorize/run
text. The five refusal mutations, two wrong-tag entries, exact source/helper
flags, profiles, output paths, three allowed future slots and phase ordering
remain. There are51 metadata-only equality checks including written-file
comparisons. No source/import/AST/syntax/compile/test/dummy of any size ran.

Three compact recursively sorted ASCII JSON+LF ROOT policies are newly pinned:
valid24029cb8f17d4159620a3e660b132fadad4d7fb6caa2988acf43abe150ac8591;
dummy889e75f6e95f3b42048939f1d11230b56208f465672bb4e94cec00983b6ffaf7;
mutate2222e4b9ea6bb23f0ba7f009f3ba750872f9f33a7195e7d8198b874313460d6f.
Valid/dummy digests are literal in their commands. They bind ROOT PATH, not
its hash; the unused mutator policy keeps the frozen baseline artifact.
These are reference bytes only: do not prepopulate a future authority directory.
The dispatcher itself must emit fresh runtime authority/policy objects.

The two release-card templates retain unissued original clock/worker/native
fields and now describe the necessary-row service honestly. SAME25rows/guard
and retained diagnostics remain; omitted alternative Laurent-q inverse/pole
coefficients are NOT_COMPUTED. Canonical inverse polynomiality is a source
hypothesis. No old full-internal-circuit or source-point qualification.
The p523/degree1/phi/P7 input remains unchanged decimal strings and UNTRUSTED;
neither these metadata checks nor prior partial evaluation establishes full
admissibility, coefficient agreement or rank.

Read scope: predecessor registration and three policies plus README/cards
were fresh WHOLE; large predecessor authority JSON uses prior ROOT WHOLE with
current pin, selected fresh reads and complete programmatic metadata ingestion.
New JSON files received complete byte/metadata comparisons, NOT a claim of
five new human-WHOLE source reads. New cards and this README are fresh WHOLE.
Candidate source/runtime uses the earlier complete text/diff comparison and
current hashes; dispatch authorize/run context was freshly read. Inputs remain
immutable; no Fable live report/log/receipt or Astra live artifact was read.

Remaining quantity: whether source/runtime DELTA FIRST accepts the pinned
vector, seven exact source-derived refusal triples can be bound to a qualified
actual interpreter/layout, and ONE registered source observation completes
with genuine producer/checker agreement and fresh exact T rejection. Cheapest
next action: collect the already-live Fable gate and Astra refusal port, then
finish actual sourcecontract and release metadata. This preparation's original
10minute envelope is UNMEASURED planning, not a scientific runtime forecast.

No allocation until that reviewed worker-independent packet is ready, followed
by fresh scoped AWS facts. Reuse the completed disabled deployment templates;
native recipe remains the historical documentary recipe, not future-host data.
Original per-source900wall/599soft600hardCPU/8GiB/128MiB and batch3000wall/2100CPU
remain. No capraise, optional mutator hardening, new framework, rank client or
occupancy allocation. Preserve EBS/evidence and retire immediately after custody.

Own targets were new and disjoint; no canonical OPEN ID or collision. This
README and PINS are preparation metadata only. JC2 and the full r>=3 source
remain unresolved; no mathematical promotion or computation follows.
