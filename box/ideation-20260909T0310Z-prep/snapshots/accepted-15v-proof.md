# Triple chart: conditional exclusion of the first one-plus-double split

September9,2026. **NEW PURE-PROOF RESULT / PROVISIONAL.** Under the explicit contact-table and coordinate hypotheses below, the first scaled cubic cannot have one simple root and one double root. All divisions in s are justified before use; all scalar reference kernels are retained. This is a conditional local consumer, not promotion of the contact cover, a full separated-cubic cover, a whole-source exclusion or JC2.

## 1. Exact hypotheses, fields and dependency status

Start over a characteristic-zero field K and enlarge constants to Kbar. The ordinary combined-homogeneous source data have weights s,g,p=1 and degrees A15,B25,R5,F15,G25. They satisfy

\[
[A,B]_{g,p}=cs^{36}g^2,\quad c\ne0,
\quad R_0=p^2(p-g)^3,
\quad A=R^3+\alpha s^{10}R+a_0s^{15}+F,
\]
\[
B=R^5+\sum_{i=0}^4b_i s^{25-5i}R^i+q(R)F+G,
\quad q(R)=\tfrac53R^2+\tfrac43b_4s^5R+(b_3-5\alpha/9)s^{10},
\quad\operatorname{ord}_sG\ge2j,
\quad\operatorname{ord}_sF=j.                \tag{1}
\]

All alpha,a0,b_i are field scalars. The accepted late-contact producer b5413087... and gate c9a399a8... supply this reference interface at their exact scope. We explicitly ASSUME the following contact table, without treating the still-unreviewed contact-cover f3ab28a6... as accepted:

\[
d=2:j\le11;\quad d=3:j\le9;\quad
d=4:j\le8;\quad d=5:j\le6,\qquad j\ge1.    \tag{2}
\]

Use the explicitly given cubic coordinate interface over E=Kbar(X^(1/3)), X=p:

\[
R=\zeta^3+a_s(X)\zeta+b_s(X),\quad
F=\sum_{n\ge0}c_n(s,X)\zeta^n,
\]
\[
\operatorname{ord}c_n\ge j,\quad
\operatorname{ord}c_d=j,\quad
\operatorname{ord}c_n>j\ (n<d),             \tag{3}
\]

with nonnegative integral s/zeta orders and coefficientwise polynomial dependence on zeta. The weights of zeta,a_s,b_s are5/3,10/3,5. All original identities (1) transport exactly. The original coordinate satisfies g=X-X^(-2/3)zeta+O(s), g_zeta=-X^(-2/3)+O(s), and the transformed source bracket is cs36*g²*g_zeta. These statements are explicit hypotheses here; no conclusion about the contact table is obtained from the terminal cubic-chart producer1620f9ef... . Its whole stated interface and proof were read, but no current gate verdict is needed or imported.

Put

\[
r=\min_{0\le n\le d}\frac{\operatorname{ord}c_n}{9-n},\qquad
\lambda=\min(\operatorname{ord}a_s/2,\operatorname{ord}b_s/3),
\quad\lambda<r\le j/(9-d)<5/3.             \tag{4}
\]

The strict last bound follows from (2), but is displayed to fix the regime. Assume the first cubic at zeta=s^lambda Y has exactly one simple and one double root. No three-simple or triple-root case is included. All four input pins/read scopes are recorded; no root three-simple report, live body or pending proof conclusion was consumed.

## 2. The forced first scaling and every F tail

For a depressed cubic Y³+A Y+B to have precisely a double and a simple root, both A and B must be nonzero. If either alone is nonzero its roots are all simple in characteristic zero. Therefore both a_s and b_s attain the minimum in (4): their integral orders are2lambda and3lambda. This forces lambda to be an integer. Positivity and lambda<5/3 give

\[
\lambda=1,\qquad S(Y)=(Y-\theta)^2(Y+2\theta),
\quad\theta\ne0.                          \tag{5}
\]

The double-root parameter theta lies in E and has weight2/3: for S=Y³+A Y+B one has A=-3theta², B=2theta³ and theta=-3B/(2A). It is recovered rationally from the nonzero cubic coefficients, not chosen by an unproved normalization. Hence theta is a nonzero scalar times X^(2/3).

Write hats for substitution zeta=sY. The first order mu of F-hat is an integer. For n<=d, (4) with r>1 gives ord c_n+n>9; for n>d, j>9-d implies j>=10-d, and ord c_n+n>=j+d+1>=11. Thus

\[
10\le\mu\le j+d\le13,\quad j\ge10-d\ge5,
\quad\widehat F_\mu=\phi(Y)\ne0,
\quad\deg_Y\phi\le\min(d,\mu-j).           \tag{6}
\]

The upper bound comes from the actual nonzero coefficient c_d at order j. No term n>d can contribute at mu<=j+d. From the contact table, mu=13 requires d2,j11 and then deg phi<=2; mu=12 implies deg phi<=4; always deg phi<=5. These are bounds on the full rescaled initial, not on a truncated F.

The first rescaled R is R-hat=s³ Rbar, where

\[
\overline R=Y^3+(a_s/s^2)Y+b_s/s^3\in E[Y][[s]],
\qquad\overline R_0=S.
\]

The new weights are s=X=1,Y=2/3; Rbar has weight2. The source A-hat starts at order9 with S³. Its other terms have orders alpha-part13, constant-part15, and F-part at least10.

## 3. Proving Bbar and the old Gbar are ordinary series BEFORE division

No assumption ord G-hat>=15 is made. Below order15, the B reference R⁵ begins too late; its scalar terms have orders25-2i>=17 for i<=4, and qF begins at order6+mu>=16. Thus any B-hat initial at nu<15 comes solely from G-hat. Its degree n in Y satisfies

\[
n\le\nu-2j\le4.                          \tag{7}
\]

The bracket in (Y,X) after zeta=sY has order37, with leading coefficient -cX^(4/3), independent of Y: substitution gains one derivative factor s, and all former zeta-dependent or nonlinear corrections gain positive order. A first B-hat initial Q at nu<15 must consequently commute with S³, since9+nu<37. If q_n is its top coefficient, the top coefficient of [S³,Q] is9q_n', hence q_n is constant in X. Euler homogeneity of Q, of weight25-nu, gives

\[
(25-\nu-(2/3)n)q_n=0,
\]

whose factor is positive for nu<=14,n<=4. This contradicts q_n!=0. Therefore B-hat has no term below15.

At15 its initial Q is S⁵ plus the order15 G-hat correction, of degree<=15-2j<=5. It is monic degree15, Euler weight10. Its bracket with S³ vanishes at order24<37; since [S³,Q]=3S²[S,Q] in the polynomial domain, [S,Q]=0. The homogeneous centralizer lemma proved next forces Q=S⁵. Thus G-hat has no order15 term either (all other B corrections start later), and

\[
\overline A=s^{-9}\widehat A,\quad
\overline B=s^{-15}\widehat B,\quad
\overline F=s^{-9}\widehat F,\quad
G^{old}=s^{-15}\widehat G
\]

are nonnegative s-order series, with G^old of POSITIVE order. Bbar-Rbar⁵ also has positive order. Abar,Bbar,Fbar have weights6,10,6, respectively; set J=ord Fbar=mu-9 in{1,2,3,4}. The exact source bracket is

\[
[\overline A,\overline B]_{Y,X}=s^{13}\mathcal J(s,Y,X),
\qquad\mathcal J_0=-cX^{4/3}\ne0.           \tag{8}
\]

No negative-s series has been hidden by dividing B prematurely.

## 4. Homogeneous centralizer and rebuilt scalar kernels

Here is the elementary lemma used in §3 and below. If Q in E[Y] is nonzero, Euler-homogeneous of weight D under X*d_X+(2/3)Y*d_Y, and [S,Q]=0, then

\[
X[S,Q]=D S_YQ-2S Q_Y=0.
\]

At the SIMPLE root Y=-2theta, if Q has multiplicity m, this identity gives D=2m. Thus i=D/2 is a nonnegative integer. Differentiating Q/S^i in Y now gives zero, so Q=f(X)S^i. Euler homogeneity forces f'=0; hence f belongs to Kbar. This includes constants at D=0. No whole rational-centralizer theorem or generic-fibre assertion is required. The simple root and theta!=0 are load-bearing; there is no assertion for nilpotent coefficient rings.

Direct scaling of every old scalar term yields the exact new reference

\[
\overline A=\overline R^3+\alpha s^4\overline R+a_0s^6+\overline F,
\]
\[
\overline B=\overline R^5+b_4s^2\overline R^4+b_3s^4\overline R^3
 +b_2s^6\overline R^2+b_1s^8\overline R+b_0s^{10}
 +q_*(\overline R)\overline F+G^{old},
\]
\[
q_*(z)=\tfrac53z^2+\tfrac43b_4s^2z+(b_3-5\alpha/9)s^4. \tag{9}
\]

Rebuild the b_i as reference scalars, leaving the actual Abar,Bbar,Rbar,Fbar unchanged. Suppress the s-powers in the notation for b_i and alpha and define tau=2b2-(4/3)b4 alpha, delta=b1-b3 alpha+(5/9)alpha². Their orders are>=6 and>=8. The exact wedge identity is

\[
[\overline A,\overline B]=[\overline R,T]+[\overline F,G],
\quad T=(3\overline R^2+\alpha)G-(\tau\overline R+\delta)\overline F
 -(\tfrac53\overline R+\tfrac23b_4)\overline F^2,       \tag{10}
\]

with T of Euler weight14. This is formal differentiation in Y,X; no old b4/b2 kernel or alpha term is dropped. If a first G_l occurs below2J, its equation is [S,3S²G_l]=0:6+J>2J, the F² term starts at2J, [F,G] is later than l, and13>2J. Thus G_l commutes with S and has weight10-l. The lemma permits only scalar S^(5-l/2), at l=2,4,6,8,10 where applicable. Changing the corresponding b_i removes that coefficient. The exact G changes are

\[
-es^2(\overline R^4+\tfrac43\overline R\overline F),\quad
-es^4(\overline R^3+\overline F),\quad
-es^6\overline R^2,\quad-es^8\overline R,\quad-es^{10}.
\]

They preserve lower orders and the tau/delta bounds. This finite induction on l<2J gives G>=2J even though the old Gbar was only known positive. It works in E[Y][[s]]; it does not invoke a finite-polynomial late-T theorem.

At order2J, (10) gives

\[
[S,3S^2G_{2J}-\tfrac53S\phi^2]=0.
\]

The inner weight is14-2J, so the homogeneous centralizer lemma makes it a scalar S^(7-J). For J1–4 these are all S²-multiples. Hence

\[
S\mid\phi^2.                              \tag{11}
\]

If e=ord_(Y-theta)phi and f=ord_(Y+2theta)phi, then e>=1 and f>=1. This is field-valued polynomial factorization, not a statement about nonreduced source schemes.

## 5. Removing the simple-root case and obtaining the small double table

If e>=2 and f=1, phi=S*C with C nonzero at the simple root. In this case J4 is impossible since deg phi>=3 but (6) gives degree<=2 at J4; thus J<=3. The formal inverse Rbar=z at the simple root exists with nonnegative s,z powers. Fbar has a nonzero linear coefficient at order J, constant coefficient of order>J, and all coefficients of order>=J. Set eta=min(J/2,ord(constant)/3). Then0<eta<=3/2<2. Its full A initial is a depressed monic cubic Z³+uZ+v at3eta, with (u,v)!=0. Higher F terms are later; alpha*s4*z and a0*s6 are later because eta<2.

Every rebuilt B scalar kernel is later than5eta by (5-i)(2-eta)>0; the lower q_* terms are later too. G>=2J implies an earlier G/B initial below5eta has degree0. The simple inverse preserves target order13 with a nonzero coefficient. Since7eta<=21/2<13, each earlier initial commutes with the cubic and its positive Euler factor10-nu excludes it. At5eta the full B initial is monic5 with at most linear/constant additional G terms. Euler homogeneity with h=2-eta>0 and commutation imply a common monic linear root; the depressed cubic forces u=v=0, a contradiction. This re-derives the weight2 reference consumer, rather than importing the weight5 theorem.

Consequently, whenever e>=2 survives, f>=2. From (6) and (11) the exhaustive remaining double-root possibilities are

\[
e=1:\ J\le4;\qquad
e=2:\ J\le3;\qquad
e=3:\ J\le2.                              \tag{12}
\]

Indeed e2,f>=2 needs degree>=4, ruling out J4; e3,f>=2 needs degree>=5, ruling out J3 as well because its degree cap is4. If e>=4 and f>=2, degree>=6 contradicts the global cap5. The case f=1,e1 is deliberately retained in e1 and will be handled there; no unproved simple-root endpoint at J4 is assumed.

## 6. The second Morse chart: coalesced balances

At Y=theta the quadratic coefficient of S is3theta!=0. The formal critical point exists; write Rbar=xi+omega² using the genuine Morse inverse. Here omega has weight1, the inverse has nonnegative s/omega powers, and its leading derivative is a nonzero scalar times X^(-1/3). The coefficient field E already contains the initial square root after adjoining a constant root. The source target retains order13 and, after any positive omega rescaling, has a nonzero leading coefficient independent of the new variable (proportional to X).

Write the full moving Fbar as sum d_n(s,X)omega^n. Its coefficient orders are>=J, with ord d_e=J and lower indices later. Put

\[
r_2=\min_{n\le e}\operatorname{ord}(d_n)/(6-n),\quad
\kappa=\operatorname{ord}\xi\ge1\text{ or infinity}.
\]

In (12), respectively r2<=4/5,3/4,2/3. Suppose kappa>=2r2. The complete A initial is (W²+b)³+U with U!=0,deg U<=e<=3, at6r2. All higher d_n terms are later by the same uniform inequality J+(e+1)r2>=7r2. The scalar A terms are later because r2<1. Bbar-Rbar⁵ has coefficientwise positive s-order, so any earlier B initial at nu<10r2 has degree n<nu/r2. Its Euler factor is

\[
10-\nu-n(1-r_2)>10-\nu/r_2>0.
\]

Every relevant bracket is below the target, since15r2<=12<13. Thus the monic top-coefficient argument kills every earlier B initial. At10r2 the monic6/10 Euler-homogeneous initials commute, with h=1-r2>0. They have a common monic quadratic. The difference of its cube and (W²+b)³, if nonzero, has degree at least4, contradicting deg U<=3. This excludes all coalesced cases, including xi=0. No equality-target step is used here.

## 7. The second Morse chart: separated balances, with both sheets

If kappa<2r2, then kappa is a positive INTEGER less than2, so kappa=1. The exact even/odd regrouping is

\[
U(z)=\sum_{m\ge0}d_{2m}(z-\xi)^m,\quad
V(z)=\sum_{m\ge0}d_{2m+1}(z-\xi)^m,
\quad F_\pm=U(z)\pm\sqrt{-\xi}\sqrt{1-z/\xi}\,V(z).   \tag{13}
\]

The coefficient sums converge s-adically; U_l,V_l have order>=J. In this split, sqrt(xi) can require X^(1/2), so use the harmless finite extension Kbar(X^(1/6)) and a finite fractional power of s. This extension, and the nonzero constant square roots, preserve the X-derivation and its constant field Kbar. Integral orders before this split remain integers. No same-field normalization is claimed.

Define q_l=min(ord U_l,1/2+ord V_l) for l=0,1 and eta=min(q0/3,q1/2). As in the exact even/odd identity, the inequalities ord(d_n)+n/2>3 give eta>1. The known contact coefficient makes eta finite and gives

\[
e1:\ \eta\le(J+1/2)/3\le3/2;\quad
e2:\ \eta\le J/2\le3/2;\quad
e3:\ \eta\le J/2+1/4\le5/4.               \tag{14}
\]

Separation forces J>=3 in e1, J=3 in e2, and J=2 in e3. In particular eta<min(J,2). After z=s^eta Z, positive square-root shifts gain eta-1>0; every analytic z-degree>=2 is later than3eta. The two A initials are Z³+u_pm Z+v_pm with a globally nonzero correction tuple. If its bases at an attained coefficient are a,b, the pair (a+b,a-b) cannot vanish on both sheets unless a=b=0. One whole component may vanish and is NOT used as a separate contradiction.

Apply (13) to the entire rebuilt G, whose coefficients have order>=2J. Its earliest global order is a minimum of the even/odd bases: binomial shifts are strictly later and the nonzero tuple prevents losing a global first coefficient. In e1,5eta<2J, so G is later than5eta. In e2,2J>=4eta, giving degree0 below5eta and at most1 at it. In e3,2J>3eta, giving degree<=1 below and at5eta. All B scalar kernels and lower q_* terms are later than5eta because eta<2.

The inverse derivative is Y_z=Y_omega/(2omega). Its leading s-order is-1/2; the target in (z,X) therefore has order13-1/2=25/2 and a nonzero leading coefficient. On either sheet,

\[
7\eta\le21/2<25/2.                        \tag{15}
\]

An earlier B initial thus commutes with its monic cubic. Its Euler factor is10-nu>0 in degree0 and8-nu+eta>8-4eta>=2>0 in degree1, a contradiction. This removes earlier bases globally, not just one sheet, so their later square-root shifts cannot reappear at the monic level. At5eta both B initials are monic quintics; their only additional terms have degree<=1. The monic3/5 Euler argument with h=2-eta>0 makes each cubic a cube of the monic linear polynomial Z, contradicting the nonzero correction tuple. Hence every separated case in (12) is excluded as well.

## 8. Controls and exact conclusion

The homogeneous centralizer lemma requires the SIMPLE factor: for S=(Y-theta)³, the polynomial Y-theta centralizes S but has weight2/3, not a nonnegative integer multiple of weight2. This changed object is the omitted triple-root case, not a valid one-plus-double initial. Likewise theta=0 destroys the unit separating the roots and is explicitly outside (5).

It would be invalid to divide G-hat by s15 merely from its old bound>=2j (j may be5). Section3 proves its missing orders vanish using actual equations before the division. It would also be invalid to inherit Gnew>=2J without reconstructing the new weight2 scalar kernels; §4 does that reconstruction explicitly. No equation or low scalar is lost by either step.

The tuple control (a+b,a-b) with a=b!=0 gives one zero sheet and one nonzero sheet. It defeats any argument demanding that both individual sheets carry a nonzero first correction. The proof instead uses a global first base and later proves monic commutation on both sheets. For e>=4 a difference of monic quadratic cubes may have degree4; that obstruction is avoided only by deriving the small table(12), not by pretending the local contradiction covers arbitrary multiplicity.

All checks are manual factored identities, derivative signs, degree inequalities and changed-hypothesis controls. No mathematical subprocess of any size, CAS/Python/free-symbol code, high H/R power, full source or 9/15/6/10 pair expansion, AWS/SSH/web, agent, shared/protected/canonical edit or live report read occurred. Metadata-only -I -B scripts hash inputs and perform begin/close/finalize/verify. The source parents and all owned pins/read scopes are in custody; no reviewer gate was consumed beyond the accepted late-contact gate. All writers are IDLE after sealing.

The resulting theorem is CONDITIONAL on (1)–(4), including the explicit contact table and exact cubic-coordinate interface. It excludes the first one-simple-plus-one-double scaled cubic by a justified second blowup and two bounded local consumers. It does not itself promote the contact cover, establish every original source supplies these hypotheses, analyze the three-simple first cubic, assert a whole historical receiver/source exclusion, claim scheme properness/emptiness or resolve JC2. No further regime or descendant is launched. Fixed stopping target02:41 UTC is unchanged.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `17787`.
- Body SHA-256:
  `644d40982863c2703fe3488502eff8eb54461099325037fd3f6173fd205f13a6`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
