# Disabled univariate runtime validation plan

This is CODE PREPARATION ONLY, UNEXECUTED and uncompiled. No fixture, artifact,
coefficient, predicate, dummy or mathematical subprocess has run. Root supplies
any future registration and may decline execution. There is no enabled
registration, worker, cap, timing window or normalized acceptance in this packet.

## Required prior evidence and exact staged names

Before invitation, root must independently accept the genuine normalized
generator/full-original-source checker execution and terminal custody. The
registration's normalized_acceptance must contain:

- status ROOT-ACCEPTED-GENUINE-CHECKER-RECEIPT;
- generator_sha256 d444d022593c99a8d5a22d9cd9cbcac2bea9c88ef1a305d3c1c8ddf1635a6d8e;
- checker_sha256 091bbf35693c52833f2de936eeecd2e734f8404f26e9c68039bb8ab6aaecb551;
- normalized_sha256: root's actual frozen normalized.json digest;
- receipt_sha256: root's actual frozen normalized.full.receipt.json digest.

These two version hashes are METADATA, not code to stage with conflicting
adapter/checker basenames. No current normalized result was read for this task.
A spelled receipt/status alone is not execution evidence; neither caller nor
input_evidence creates root acceptance. The caller copies the root-owned field
into EVERY fresh frozen authority; all immutable inputs are rehashed before
and after every operation. The unchanged evidence helper checks the original
source SHA and complete prior receipt scope: 20 slots, nine G, two H, ten poles,
eight bracket bands.

The exact future file_sha256 vector has 13 local regular files plus the
interpreter: adapter.py, checker.py, input_evidence.py, algebra.py,
execution_gate.py, probe.py, dispatch_batch.py, semantic_controls.py,
run_capped.py, admissibility.md, frontier.json, normalized.json,
normalized.full.receipt.json, and /usr/bin/python3. All paths are absolute.
Caller and harness hashes are root external-preflight values. The caller fixes
the other code/instrument hashes and binds both incoming artifacts to root
acceptance. The future CHECK authority includes adapter.py too.
Root must retain its prior execution/receipt/custody evidence, not just copy
the status field. Original exact.json is not regenerated or staged here; its
exact digest remains bound through the accepted normalized source evidence.

Stage the six unchanged copies from this box and the two new files. CAPRUN,
admissibility and frontier are future externally pinned inputs, not bodies
read or copied here. Keep no file named plain or descendant in the fresh cwd.
The prior receipt is normalized.full.receipt.json; the new receipt is
univariate.full.receipt.json. Neither may substitute for the other.

## One finite sequence, no retries

The old caller's supervision remains unchanged except acceptance propagation:
five actual authority refusals (disabled, wrong host, parent argv extra token,
wrong RSS cap, missing frontier input); one valid post-authorize sentinel;
one reviewed same-PGID descendant RSS dummy with TERM ignored then KILL.
The exact four identity events, PID/PGID/start/namespace equality and integer
signals, leader reaping and absence of live owned PGID are retained.

Only after preflight.PASS may the three mathematical children run:

    python3 -I -B adapter.py AUTH normalized.json normalized.full.receipt.json univariate.json
    python3 -I -B checker.py AUTH normalized.json normalized.full.receipt.json univariate.json univariate.full.receipt.json
    python3 -I -B semantic_controls.py AUTH normalized.json normalized.full.receipt.json univariate.json univariate.full.receipt.json controls.receipt.json

All actual paths must be absolute and full child and CAPRUN argv registered.
The first is build, the others check. This is seven preflight children followed
by three mathematical children; the final child performs exactly seven
in-process negatives. There is NO in-process positive rerun.
The prior normalized positive is a separate mandatory historical prerequisite.

Root chooses all five profile caps, positive aggregate wall budget and both
absolute UTC deadlines. The existing 15-second admission margin, original
stored hard cutoff including aggregate, post-return check, inherited 16 MiB
file limit, exclusive outputs/authorities and no concurrent authority writer
remain unchanged. CAPRUN owns the recorded child group; the caller creates no
new PGID. Sampled RSS and cleanup are not strict real-time guarantees.
Any cap, bad reason, mismatch, changed fixture, stale input, late return or
admission refusal is STOP/NONDECISION, with no retry, escalation or solver.

## Seven changed-object controls (designed, not run)

Every object is copied from the genuine univariate output, changed, written
exclusively, SHA-bound, reread, compared with the changed object, and passed to
checker.verify(normalized, reread). The fixture is rehashed afterwards. These
are IN-PROCESS semantic calls, NOT seven production checker.main calls and
not seven fresh per-operation authority tests. No stale artifact-hash check
stands in for a semantic rejection.

| Mutation | Exact expected ValueError |
|---|---|
| Add B constant 1 to compatibility coefficient a | all Phi3 coefficients |
| Add B constant 1 to c_inverse | actual c inverse identity |
| Add B constant 1 to r[0] | affine r constant |
| Add B constant 1 to E1/S1 coefficient 0 | literal univariate row E1/S1 |
| Remove h0 from the guard factor list, leave q unchanged | guard equation and both factors retained |
| Add B constant 1 to q[0] | entire guard q=r*h0 |
| Remove dense E1/S1 slot 3 | every univariate coefficient slot including zeros |

All additions use the frozen exact B wire path with canonical rational strings,
not float/text polynomial evaluation. c is a verified unit on the genuine
positive, so c_inverse+1 cannot still be its inverse. Every fixture must
actually differ, or the harness stops. The omitted slot is called a DENSE slot,
not a known zero; its actual removed wire is recorded. No COVER mutation,
float/large-integer matrix, generic factor, b division or backend repair was
added. Expected paths are static claims awaiting actual validation.
The unchanged normalized A/B/source remain the accepted prerequisite.
These negatives do not establish new original bracket/pole negative coverage.

## Acceptance and custody boundary

The genuine checker receipt must have status
PASS-NINE-ROW-ADAPTER-NOT-IDEAL-DECISION and bind the new artifact and exact
input_evidence; the harness also requires nine row slots and both guard factors.
The caller checks production return/telemetry before reading it. This typing
does not itself attest execution; recorded caller custody and root review do.
The root-qualified U identification is inherited from the accepted normalized
source checker and Pi mapping; no change to the univariate checker is made.

Future measurements come only from the genuine checker and standard telemetry:
actual degrees, guard degree, basis-term counts, artifact/fixture bytes,
elapsed time and sampled RSS. None is measured or forecast here.
A future PASS is representation/control evidence only, never properness,
nonemptiness, solver permission, source point, speedup or ideal decision.
