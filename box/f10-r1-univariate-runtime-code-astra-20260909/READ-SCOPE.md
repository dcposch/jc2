# Exact read scope

First action 2026-09-09 20:23:30 UTC; controlling cap 20:43:00 UTC.
New box/report absence checked first. All12 input hashes in input-pins.json
matched before bodies or copies. Read WHOLE: both terminal gates, adapter,
checker, input_evidence, algebra, execution_gate, probe, old caller and old
semantic controls, original adapter VALIDATION and producer report. An initial
combined display was truncated; its missing code bodies were re-displayed
separately through EOF. No provenance paths were followed.

Lifecycle scope comes from the current root assignment: accepted17p/17r,
including root's resolved U-slot qualification and exact backend status/version
constants. Frozen files retain their historical provisional/UNEXECUTED wording;
none was edited. No current normalized runtime result, artifact or report was
read. No original coefficient data or theorem body beyond the exact12 inputs
was consumed. CAPRUN, interpreter, admissibility, frontier and the original
source digest occur only as retained future binding constants.

Only metadata/hash/text reads, diff, apply_patch and the existing artifact
publication tool were used. One first patch orchestration failed JS parsing
before any nested tool or file change; it was corrected as text preparation.
ZERO compile/import/execution/test/arithmetic/CAS, network, process audit,
AWS/SSH, agents or shared/frozen-file edits. Copy hashes and all input hashes
are rechecked at publication. New sources are read statically, not tested.
