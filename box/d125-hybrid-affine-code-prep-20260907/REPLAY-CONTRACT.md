# Exact original-source cover replay (preparation only)

Input custody: the immutable unequal/rational269-variable,803-row original
JSONL, SHA b8db27661d1bfd3dcc5dfe1d1271b78c3f6375e06201654977a51380cd6ebcac,
remains on retained EBS. No full input was read or generated by this preparation.

The new constructor emits81 names in order:20 retained A slots,59 retained B
slots, k,z. It preserves all original coefficient-entry metadata and row labels,
including zeros, and appends UNIT/kz. Its coefficient maps describe the normalized
slice, not original coefficients. Both target constants and beta15 are zero;
lambda2=0 and the normalized lambda3=1. Fixed moving faces and scalar are explicit
in construct.fixed and jrow. All660 J rows and105 lift rows are present, with the
remaining original fixed/guard labels; 803 original positions plus one new row.

Let S=Q[a1..a20,b1..b59,k,z,ell]/(kz-1,ell^6*k-1). This is naturally
Q[a1..a20,b1..b59,ell,ell^-1], since ell^6 is invertible. A Laurent key in replay.py
is (ordinary-variable monomial, integer ell exponent), permitting negative powers.
Every normalized polynomial is evaluated by k=ell^-6 and z=ell^6, not by
specializing k to a number. This reduction is exact in S and retains the whole
finite cover; it is not equality in a zero ring or evaluation at a sampled point.

For a source coefficient of member degree D at physical total degree d, its raw
original-variable map is ell^((D-d)/2) times its normalized coefficient map.
Even-degree coefficients vanish, so no half-integral exponent is introduced.
The two original lambda variables map to0 and ell. Every original fixed coefficient
entry is checked separately: e.g. k*ell^6=1, (5k/3)*ell^6=5/3 and
(5k^2/9)*ell^12=5/9. Original FIX rows themselves are serialized zero in the old
source, so comparing their zero terms alone would not check this interface.

For each literal row the replay substitutes these raw maps using its own Laurent
addition/multiplication, without calling the normalized J or lift generators.
It compares against ell^q times the normalized row, with

- q=(38-I-J)/2 for even I+J Jacobian rows;
- q=(D+e-5t)/2 for surviving LIFT/member/t/e rows;
- odd/wrong-parity cases: both sides must be zero;
- fixed and guard rows: q=0, with the independent metadata/guard checks below.

In particular J/2/0 scales by ell^18, sending normalized target5k^3/9 to5/9.
All105 post-Hermite normalized lift rows must be exactly zero. Every retained
coordinate is checked to be its own variable; all selected graph metadata and
constant inverses, including shifted B15, are checked. Therefore the replay is not
an independent proof of the external triangular graph theorem, but it does test
the resulting maps against every actual polynomiality equation.

Normalized vertex/scalar guards with moving power r=1,2,3 use inverse powers z^r
and their original rational inverse constants. Their residual is (kz)^r-1;
the emitted cofactor is sum_(j=0)^(r-1)(kz)^j. Replay checks both the literal guard
polynomial and its exact multiple of UNIT/kz. Unchanged guards are zero. The extra
unit row must be literally kz-1 and reduces to zero in S. These checks prevent
dropping the k=0 exclusion just because old guards were constant zero rows.

The reader requires canonical JSONL, unique JSON keys, complete footer and prefix
hash, exact source hash before/after, exact coefficient metadata, all269 variable
names/indices, all803 source row labels/indices, exactly one extra unit row, and
complete term/zero counts. No post-footer bytes are allowed. Unknown source record
types fail. Coefficient wires must be genuinely rational, never split field parts.

The accepted normalization theorem and provisional hybrid graph/shear composition
remain external dependencies. Via the rank-six faithfully flat cover and whole
target shear, the completed normalized quotient would have the claimed relation
to the complete odd source quotient. This code is not a properness or point
certificate. Full source/constructor execution and independent stream review are
unperformed and require new scoped root authorization. No mathematical GAP beyond
those explicitly retained dependencies is presently identified; no runtime claim.

Future measured job: exact new remote cwd is hard-coded in construct. main performs
one construction and one original-source cover replay in the same process and
absolute deadline, with no descendants, solver, retry or alternative mode. It
requires a fresh registration, physical root-GREEN file, accepted math/code gate
pins, exact current DMI/boot/source/code pins, and caps no larger than300wall/CPU,
4GiB AS,128MiB per regular file,100k terms,1m pairs/retained terms,4096 coefficient
bits. Root's unchanged accepted CAPRUN must additionally enforce exact-group RSS
and reaping, after separately authorized actual-host engineering controls. This
preparation contains no authority JSON, registration authorizing execution, or
remote deployment. Stage telemetry intentionally uses stderr; success requires
normal exit and the explicit complete replay receipt, not an empty-stderr rule.
