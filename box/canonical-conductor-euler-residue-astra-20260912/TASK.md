# Bounded source test — canonical conductor and Euler residues

ROOT September12,2026,13:55UTC. Astra co-research, not FIRST. Original
reserve14:07UTC / HARD14:10UTC NEVERRESET; finish early if decisive.
Basis0d39df3c9fd69c939a8420c54d03228b9077777d. Exactly one scientific report:
xmodel/canonical-conductor-euler-residue-astra-20260912.md.

Hash/read COORDINATION.md first WHOLE, expected
33cfa6106972fe557a95ca81a9a05bb72300a82b2fbb7908915441cf75377597,
then this TASK WHOLE with invitation pin. Exactly these two inputs, no
corpus/prior/peer/linked body. Use unchanged artifact_finalize transaction
begin/close/finalize/expected verify, edit only leased partial via apply_patch.
Administrative PINS/custody here allowed. No shared/prior/TASK edits.

The actual mathematical question is whether the canonical potential gives
a NONTRIVIAL necessary constraint on the ACTUAL conductor, not another
implication from assumed conicality or normality. Reconstruct or refute the
following ROOT MANUAL/UNREVIEWED proposal; stop at its honest source scope.

Assume f,g,H in R=C[x,y], J(f,g)=1,
dH=alpha-f dg, alpha=(x dy-y dx)/2. Assume B=C[f,g,H] subset R is finite
birational with normalization R (a previously constructed source class,
but no prior proof is an input: take precisely this finite hypothesis).
Let P(U,V,W) generate the irreducible height-one kernel of C[U,V,W]->B,
and c=P_W(f,g,H). P_W is nonzero and no monicity in W is assumed.
The map j is unramified because Omega_(R/C[f,g])=0.
E=(x partial_x+y partial_y)/2. No E(B) stability is assumed.

A. EXACT CONDUCTOR. Is the conductor of B subset R exactly cR, up to a
NONZERO CONSTANT only? Reconstruct via hypersurface dualizing form
eta=dU wedge dV/P_W and finite normalization duality, or an explicit
equivalent argument. Pullback eta=dx wedge dy/c. Explain all Gorenstein,
finite Cohen-Macaulay, fractional-ideal and constant-source-unit steps;
do not merely identify the radical or assume a monogenic R over C[f,g].
If a standard duality fact is used, state it exactly and explain its
application rather than claiming a fresh proof. ROOT handles primary sources.

B. ORDINARY DOUBLE LOCUS. At a generic ordinary double point, use target
coordinates U,V on BOTH smooth normalization branches (the Keller map is
etale there). Write H_1(U,V), H_2(U,V), delta=H_1-H_2 with simple zero.
Locally P=unit*(W-H_1)*(W-H_2), with other sheets factored into the unit.
Hence c_1=unit*delta, c_2=-unit*delta and the unit agrees on delta=0.
The branch pullbacks of alpha are alpha_i=dH_i+U dV, so their restrictions
to the common curve agree. The proposed conclusion is the pointwise
identity on the paired conductor sheets:

    E(c)(a)+E(c)(b)=0.

Check this directly using i_{E_i}(dU wedge dV)=alpha_i and
E_1(delta)=E_2(delta), OR through opposite residues of eta. Retain which
generator c is used; changing it by a nonconstant unit is NOT innocent.
Does alpha agreement alone force E(c)=0? It should not: only opposite
paired values are proposed. Higher multiplicity or nonreduced conductor
is not silently treated as an ordinary node.

C. DECISIVE ACTUAL-SOURCE CONSEQUENCE. If A/B survive, test the proposed
exclusion of an exact conductor generator c=h_d(x,y)-a where h_d is a
nonconstant homogeneous polynomial of degree d>0 and a in C* (up to scalar).
Such a level divisor is smooth/reduced by Euler, and E(c)=d*a/2 on it.
Explain why reduced conductor forces generic transverse double branches
here: locally embedded smooth branches give conductor valuation equal
to the total other-branch intersection orders, so valuation1 permits only
one transverse companion. Then B contradicts the nonzero constant value.
Prove or gap that valuation step. The desired output excludes this ACTUAL
conductor shape, not arbitrary Keller maps or all nonconical conductors.
No assumption that an actual source has this shape; no degree/conductor farm.

Controls: the nodal surface (t,s)->(t^2-1,t(t^2-1),s) is finite birational
unramified with smooth source but the displayed first-two projection is
not Keller; choose target U=t^2-1,V=s,W=t(t^2-1), P=W^2-U^2(U+1).
Then P_W=2t(t^2-1) whereas the actual conductor is (t^2-1)R, because
J(U,V)=2t is not a unit. This checks the indispensable Keller volume factor.
One open-plane radial control if useful: R=C[x,x^-1,y], p=x^3,
q=y/(3x^2)+(4/3)x+(10/9)x^2, T=xy/6-x^4/3-4x^5/9,
tau=(pq/2-T)/p=x+x^2, W=tau^3-3p*tau-p-p^2.
The graph conductor is I*R, I=x^2+x+1, but for the CANONICAL T variable
the defining polynomial is W(p,q,(pq/2-T)/p) after clearing p-units.
Do not substitute I or W_tau for P_T without the chain/volume/unit factor.
Choose the nodal control alone if the open calculation adds no information.

Report whether this yields a genuinely source-attached restriction and
where it stops. Actual graph normality, conicality, unit conductor and JC2
remain undecided. Do NOT broaden to all reduced conductors, all levels or
trace identities at arbitrary multiple branches. No descendants or FIRST.

Manual math only: NO scientific/CAS/interpreter/import/AST/test/syntax/helper
execution, network/AWS/SSH/agents/protected-tree operation. Inert text/hash/
UTC and unchanged administrative finalizer allowed. NEVER enter/enumerate/
search/read/status/control jc2-lean or its public mirror. All file edits
apply_patch only, no shell file writes. Max body about10000bytes. Own WHOLE
readback/both postpins before unique BODY-END LAST. No charge_basis or
canonical OPEN. Final report/manifest WHOLE, expected verify, custody SHA
FIRST and ALL WRITERS IDLE actual UTC before FINAL; no later writes.
