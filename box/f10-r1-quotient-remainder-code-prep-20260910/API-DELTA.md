# Shared error predicate clarification

Both static authors receive this same additive clarification. Original PINS
and INTERFACE remain byte-identical; charge this as the15th input. Original
00:39UTC caps and00:36 publication reserve do not change.

generic_candidate must reject a non-squarefree coefficient modulus with
ValueError whose exact str(exception) is:

    generic modulus must be squarefree

The fourth negative control p=v^7 must require precisely that exception class
and string. An unrelated failure does not count as the expected refusal.
This clarifies the existing mandatory precondition, not a new test, source
claim, runtime authority, or permission to inspect a live peer's code.
