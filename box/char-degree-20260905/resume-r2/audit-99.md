# (99,66) resumed acceptance audit

This audit covers the eighteen `g9966/circuit-runs/{delta2,delta52}_stage0..8` presentations. `audit-99.json` contains their latest observed states and complete compact custody. `audit_99_worker.py` streams each remote script through read-only checks and emits this JSON; it never copies scripts or changes worker files. All large Singular programs remain on the worker. No unit or properness is asserted here.

The exact coefficient field is Q, and the Singular ordering is `dp`. The ordered generator list is the full input `names` list followed by fresh coefficient graph variables. Each declared ring is compared literally with its metadata generator order; list length, uniqueness, the exact order hash, and ring declaration hash are recorded. The physical extraction variables t,z do not occur in this final coefficient ring. All five target scalars remain in every ring. `leader55` is free, localized by `Z55*leader55-1`; separation is localized by `Zrho*rho-1` for delta2 and `Zc*c-1` for delta52.

The identical backend is SHA-256 `17a825098e24dcbeb973d7fe6f254c6f5884f2aad9bed196e12166db0520ce35`. Every script hash is compared with its backend metadata, every input hash with its verification record, and source coordinate, gauge-parent, and original-source hashes with their successive stored links. The ten retained proof/control snapshots are also rehashed against `proof-custody.json`. Their source-map verifier has eighteen PASS records with exact original-map, jet0-specialization, rational graph, all raw front rows, retained residual, and API regeneration checks. The resumed audit independently reexecuted these checks on all eighteen hash-bound local source copies, covering10,700 coefficient sites; `audit-99-map-replay.json` binds the two small transcripts. The first120s wrapper stopped after13 completed cases and the remaining5 passed in42.964s. No check failed and no original file was changed. Matching names alone is never used as a map proof.

The characteristic block is complete in the declared reduced coordinate presentation. Each H,v,U coefficient is a rational constant or a new monic polynomial graph variable. All high-y coefficients of `v^2-UH` are retained as equations, while its low-y coefficients receive monic graph definitions R. The remaining graph layers encode the required H² and vU coefficient products. Substituting these acyclic monic equations gives the same coefficient ideal, not a projection that may add points. Every Q coefficient through the inclusive tested depth is imposed using

    Q=(3R/4+p)H²-vUH/8+vR-9U²/64+pv+q.

The normalized ambient bound is196 and the target depth141; these are the same physical degree55 conditions as ambient198 and depth143 before the two-power normalization shift. Truncated products retain exactly the terms able to reach a tested coefficient because all t-exponents are nonnegative. The full high-y remainder is not capped. Prior exact-Q support and graph-projection controls are authenticated by their retained hashes; none is a source-chart survivor.

For each emitted production script, the newly executed target audit checks the actual source zero-band image `H0=z^24(1+z)^9` and the input quotient face `z^16(1+z)^6`. Their product is the whole required target `leader55*z^40(1+z)^15`. Exactly sixteen Q rows subtract the binomial coefficients of `(1+z)^15`; the seventeenth and only other leader occurrence is `Z55*leader55-1`. There is no leader occurrence in a source or graph row and no target coefficient is silently replaced by zero. This checks actual emitted polynomials and exact positions rather than a producer's Boolean assertion.

The source F/G leading-pole issue identified by the charged 17(rrrrrrrrr)/(nnnnnnnnn) repair is separate from the characteristic target. The requested source stages0..8 lie strictly below the nonzero source endpoint faces: delta2 uses local powers4..12, below F81 and G54; delta52 uses powers8..16, below F189 and G126. Consequently these stage pole rows legitimately have target zero. At-level h3 incidence rows are explicitly coefficient-minus-derived-target. No old homogeneous endpoint unit is imported or promoted. The whole characteristic target is separately subtracted as just checked.

The `jet0=0` slice has a full-chart coverage proof. For each normalized coefficient, simultaneous source translation acts as

    t^r z^p -> t^r z^p(1+qt)^(D-r-p).

The actual degree boxes make this polynomial and unit triangular; the inverse is obtained with -q. It sends jet0 to jet0-q, retains rho/c, and transports minor_a2 or v to its old value minus q*u. This includes u=0 without division. The other retained source coefficients and complete face ideals are transported by the same action. Therefore the full free-centre chart is isomorphic to the zero-centre chart times A1. The characteristic whole-top equations are covariant and preserve all target scalars and the leader under this translation. It spends the residual diagonal translation only; the earlier beta normalization uses a different, explicitly recorded simultaneous dilation. No additional leader or separation normalization is used.

Front preprocessing imposes necessary radical consequences, not extra gauges and not falsely claimed ideal identities. Stage0 uses D<=29,C<=60; positive stages use D<=30,C<=62. The positive-stage improvement uses the actual weight190 face, whose D1 moment matrix has full rank11. Every original coefficient image and residual is transported after these reductions. The next D31 coefficient is retained and is not inferred to vanish from a previous equality face. This audit consumes the charged proof of those consequences and the authenticated coordinate-map replay; it makes no new radical inference.

All emitted client scripts observed by this audit print `ALL_ROWS_PARSED` before their blocked Gröbner calculation, and their logged ideal sizes equal the emitted row totals. The original backend places its 0/1 controls after the full Gröbner calculation; timeout logs therefore contain no executed in-run controls. This absence is recorded explicitly. As a fresh, small check, the audit runs leader and separation positive/negative controls in every actual declared stage ring, each yielding 0,1,0,1 without parser errors. The known attained-degree positive backend control and deliberate wrong-target negative control are also rerun from their retained v2 scripts, with expected reduce-one values1 and0 and localization controls0,1. These checks validate the interpretation pipeline; they do not turn incomplete client calculations into units or proper ideals.

Historical completed runs did not record RSS; the address-space limit16GiB is not an RSS measurement. `audit-99.json` marks missing historical peaks null. Parent resume monitoring separately retains observed process VmHWM with timestamps; its last sample is a lower bound on eventual peak usage. Driver rc0 can accompany a CAS timeout rc1 because the Python driver completed its receipt normally. The exact CAS rc, total measured wall, script/input/output hashes, marker absence, and control status are all distinguished in custody.

Other 99 attempts are not omitted or promoted: all eighteen older normalized runs are memory-bound; delta52 stage8 active-front is compute-bound at1801.169s; delta2 stage8 selected `slimgb` is compute-bound at1200.467s; delta52 stage8 remainder is memory-bound at1215.298s. Stale `EXPANDING_FULL_CHARACTERISTIC` or `RUNNING` files need process reconciliation and prove no result. A partial coefficient build, interrupted Gröbner basis, or bare nonzero intermediate polynomial cannot prove properness.

Within each branch, the emitted stage7 and stage8 scripts are byte-identical: delta2 SHA-256 `fe95c97a6f0299c736e9eb9a2fc3faa7898d3917bdd8c3eb891b46ceb4b5c4f4`, delta52 SHA-256 `9f8b1aa94a5df231a4a47c89bd37a7408cc79a63e7c4a0699db26e751f6ce0b7`. Their input and execution receipts remain separately identified by stage. This is consistent with the charged argument that the full characteristic locus already forces the shallow Jacobian bands to vanish; no extra kill or properness claim is extracted from the identical scripts.

Final receipt refresh at2026-09-05T23:33:25.911264+00:00 found all eighteen primary stages completed as COMPUTE_BOUND_OPEN, with driver rc0, CAS rc1, and total walls601.001–601.590s. Every original script/input/backend hash was rechecked unchanged. Both split branches are typed COMPUTE-BOUND / OPEN. There is no certified DEAD gate and no completed proper augmented ideal. The final custody file is118,148bytes with SHA-256 `d79edb8b9e51e8cc948549c34811e3b0e9f8aeadb1b8f8b800302bbfb01115b1`. If a future completed proper ideal passes these checks, Theorem B would imply existence over Qbar of an existential nondegenerate necessary-chart survivor, even without extracted coordinates; it would not imply a Keller pair. Parent handles worker termination and sealing; this audit has neither stopped a job nor terminated the instance.
