# Independent acceptance review in resume round 2

The two harvest schemas contain detailed source/input/script/ring/row custody, but neither status field alone is a complete acceptance validator. The 99 harvest's parser_status field checks ALL_ROWS_PARSED and leading question-mark diagnostics, whereas its standalone-control helper also checks “error occurred.” The D108 harvest's parser_errors field is copied from the inherited wrapper metadata; it is not independently rescanned there. Those limitations do not promote any current production result: all are explicitly incomplete/bounded.

A separate conservative acceptance reader now requires successful return code, stable hash-bound output/script, exactly one of every ALL_ROWS_PARSED/BEGIN_GB/END_GB/BEGIN_RESULT/END_RESULT/BEGIN_CONTROLS/END_CONTROLS marker in order, an integer parsed-row count, a three-value consistent reduce-one/dimension/basis-size result, and exact [0,1] localization controls. It independently rejects question-mark, error-occurred, Singular-error, syntax/fatal-error, segmentation-fault and halt diagnostics. This is an output acceptance check; source-map/full-block/gauge/target obligations remain separate. A clean unit remains a candidate requiring independent gate replay.

`strict-acceptance-controls.json` records 23 successful synthetic checks, including missing/duplicate/out-of-order markers, truncated Groebner output, parser errors despite rc0, missing/wrong control vectors, invalid result values, row-count/hash mismatch, and unsuccessful/missing return codes. Three tiny actual Singular scripts also passed their expected classification: clean proper, clean unit candidate, and malformed rational-expression parser control. The malformed script uses `poly malformed=b^2/4` and actually returns rc0 while printing parser errors; the independent reader correctly rejects it. Existing positive/negative toy replay outputs from audit-99.json also pass. No production Groebner computation is rerun by these controls.

`strict_acceptance_worker.py` sends the reader through SSH stdin and reads production metadata/scripts/output bytes without writing on the worker. `strict-acceptance-production.json` records the 2026-09-05 23:23:08 UTC observation: all18 99 schedule attempts, all18 D108 frozen/full-mean schedule attempts, and all3 selected attempts were inspected. There were **zero complete clean candidates among39 records**. Every actual output contained ALL_ROWS_PARSED and BEGIN_GB but lacked END_GB, completed result and in-run control blocks. Finished jobs additionally showed halt1 or memory/halt14 diagnostics; live jobs had no recorded completion code/output receipt. These are OPEN, with exact resource subtype supplied by their independent run receipts. Script hashes all match their inherited receipts after resolving repository-relative and D108-relative paths in their declared bases.

The observation is time-specific and does not classify later output. Rerun this reader once final stages finish, before worker termination, retaining the final compact receipt under a new name. No script/output substitution, terminal return code, parser marker, or toy result can stand in for a completed production calculation.

Final pre-termination reread: `strict-acceptance-production-final.json`, observed 2026-09-05 23:33:19.906558 UTC, SHA-256 `b51e69690036e43f79405343c1c4e58a673a68d358f6385f3d4cf843139bd492`. All39 attempts had finished:34 returned rc1 with time-bound status and5 returned rc14 with memory-bound status. All39 emitted script hashes and output hashes match their receipts, and all files were stable during the read. Every log has exactly one ALL_ROWS_PARSED and BEGIN_GB and zero END_GB/result/control markers. Therefore the number of complete clean production unit/proper candidates is **zero**. The finite mathematical decision remains compute-bound OPEN for both99 branches and D108 delta3. This final parser harvest performs no worker termination and records no claim of a production solver rerun.
