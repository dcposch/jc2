# Pending controls — none executed in this preparation

The frozen caller-gate 946e07d0… is consumed as a static/dictionary-review result,
not as execution of v2. Old observed cleanup does not become a v2 runtime pass.

Root's proposed metadata controls before a new dispatch:

- Four-event sequence: actual dictionary versus empty/deleted/reordered/extra
  entries, wrong stage/result, GROUP_GONE, wrong signal and float-typed signal.
- MATCH identity: change observed PID, PGID or start identity while keeping
  the apparent MATCH label; require rejection against the top-level leader.
- Helper pin: change the probe or corrected precision bytes; the new literal
  main-entry hash checks must reject before any child or enabled authority.
  Caller self-hash remains root's external preflight responsibility.
- Timing: a metadata-only post-return fixture equal to or greater than its
  stored hard cutoff must be NONDECISION even if its simulated child exit is0.
  An earlier return must not be rejected on this reason alone. The cutoff used
  must be the original admitted min(absolute deadline, first_math+360), never
  reset to the return time. Also test insufficient +15-second admission room.
- Existing disabled/wronghost/fullargv/cap/missinginput refusals, valid sentinel
  and genuine descendant RSS/TERM-ignoring cleanup must be repeated only after
  new root authority, using the new exclusive job directory and output names.

The code sets 13:35 UTC only as the prospective mathematical deadline. No
invitation, restart, SSH or active registration occurs here. Root's prospective
13:40 execution-task cutoff is external and does not extend the math deadline.

Timing limitation: CAPRUN samples RSS/lifecycle and uses bounded cleanup,
identity and ps calls. The 15-second margin is conservative admission policy,
not a proven strict real-time bound under arbitrary scheduling or kernel delay.
The post-return comparison rejects any late mathematical result; it cannot
retroactively guarantee that a delayed process ended earlier. No supervisor,
CAPRUN signal code, PGID layout or retry policy is changed.

After separate dispatch authorization, all seven ordinary checker modes remain
required, followed by the corrected precision control. It first verifies the
unchanged full artifact, then independently tests a float-typed guard mutation
and a rounded integer hidden in a canonical rational numerator STRING. The
second must pass canonical parsing but fail the exact guard equality with
reason exactly `full guard row`. These are negative mutation controls, not
positive runtime proof of all large-number parser behavior. No statement about
the size of actual artifact coefficients is available before an artifact exists.

All controls are PENDING: no predicate replay, import, syntax compile, arithmetic,
builder, checker, precision, AWS/SSH action or test ran during preparation.
