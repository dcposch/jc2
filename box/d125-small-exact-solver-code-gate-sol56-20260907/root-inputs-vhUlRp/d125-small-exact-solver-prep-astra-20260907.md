# D125 unequal/rational: one exact-Q solver attempt — preparation only

Status: **PREPARED / NOT AUTHORIZED / NO SOLVE RUN.** Author `/root/model_productivity`, 2026-09-07. Frozen basis `0d39df3c9fd69c939a8420c54d03228b9077777d`.

Recommendation: one unchanged-ring `slimgb(I)` attempt, with a deferred unit-certificate extraction inside the same job cap. Do not allocate a full transformation matrix before knowing whether it is needed. This is an actual complete-ideal decision attempt, not a new presentation or a promise of completion.

## 1. Exact client and prerequisite

Launch only after root accepts the independent full-stream/Singular semantic gate, accepts this registration and sends a new explicit GREEN. The active gate was not inspected. Root retains worker custody; this document itself grants no execution authority.

Only client: **unequal/rational**, exact coefficient field Q, the frozen 269-variable order and global `dp` order. Keep all803 literal rows:660 ordinary J,105 negative polynomiality,31 fixed-map residuals and7 guards. Keep249 explicit zero rows, both free lambda variables and every source variable; do not add a gauge, remove a guard, localize, compress B, change a field, or substitute a subset.

Frozen remote prefix: `/home/ubuntu/d125-small-source-construction-pilot-20260906/unequal-rational/client-unequal-rational`.

- `.jsonl`: SHA-256 `b8db27661d1bfd3dcc5dfe1d1271b78c3f6375e06201654977a51380cd6ebcac`;1,213,487 bytes;27,577 terms.
- `.sing`: SHA-256 `c089c33301e3889e0c20527386719f411907cf2957ecfad3d93c65f7be24e718`;707,662 bytes.
- Terminal pilot: `xmodel/d125-small-source-construction-pilot-astra-20260906.md`, SHA `4b7eec5397d16c963022eef816b8196375964be7c4157d397e789503e0047ecd`.

The actual source degrees are75/125, not receiver15/25. A certified unit excludes only this complete normalized client. A certified proper ideal would trigger independent mathematical/source review of the accepted sufficient lift theorem; this preparation is neither a point nor a JC counterexample.

## 2. Prospective registration and exact operations

Fresh directory only: `/home/ubuntu/d125-small-exact-solver-20260907/` on root-owned `i-0da0cebfc97c9fd54`, EBS `vol-0eb6450d18ffa89f1`. It has not been created. Before launch resolve current IP/state; recheck Linux/Amazon EC2 DMI `board_asset_tag`, boot, Owner, mount/volume, free disk/memory, no conflicting owner, exact cwd, registration and all source/binary/runner pins. No start/stop/retag/terminate authority is implied.

Use unchanged CAPRUN SHA `4435279df8f987c667ebc2fae8e4bd987916032561cb3faf1e07a57789d196c2`. Proposed solver binary is the previously imported Singular4.3.2, SHA `90ab699b7a28486944a167e797d7c8dd38f8f949960b93ee7a6d08ff1c7c46f4`; fail closed on drift. Retain fresh actual caller/exact-PGID and descendant-cleanup controls before production; reuse existing runner, not a new process supervisor.

Create a fresh input by replacing only the uniquely matched terminal IMPORT_ONLY prints/`quit` in a copy of the frozen `.sing`. Verify its entire ring/ideal prefix byte-for-byte against the frozen file, including the final ideal semicolon. Original inputs remain untouched. Pin both complete files and the precise suffix diff.

Run `Singular --no-rc -q` once, one thread, with `option(redSB); short=0; ideal G=slimgb(I);`. Import, solve, printing and conditional extraction share **300 wall/300 inherited CPU seconds**,16GiB address-space limit and16GiB sampled exact-group aggregate RSS limit. No `std` retry, second order, alternative engine, modular solve, dimension or elimination call.

Immediately emit a uniquely delimited complete indexed basis, its generator count and exact ring/source IDs; emit an end-of-basis marker before certificate work. Record `ncols(matrix(I))` and the complete exact indexed engine-I polynomial list actually used. Do not equate the pilot's `size(I)=554` with803 literal source-row positions or silently renumber cofactors: derive and verify the source-row map by exact polynomial equality, with zeros explicitly accounted for. Before production, a tiny actual-engine control with leading/interior/trailing zero generators must establish `ncols`, `size`, `I[k]` and lift-row correspondence; retain both engine and original maps. Duplicate polynomials may map deterministically to their first identical original row, aggregating cofactors; never guess by position.

If G contains a nonzero constant, normalize the proposed unit and, **within the same remaining300-second cap only**, attempt `matrix T=lift(I,ideal(1));`; print every indexed cofactor and form `matrix CHECK=matrix(I)*T`, checking its1-by-1 dimensions and `CHECK[1,1]==1`, with unambiguous dimensions and terminal markers. `lift` may internally recompute a basis; this cost is acknowledged, not hidden or granted a fresh budget. No up-front `liftstd`, extension or second extraction attempt.

If G has no constant, emit G and stop the solver; do not launch a second basis computation. A separate strictly bounded **120 wall/CPU-second** exact arithmetic verification stage may check the completed certificate below, using the same16GiB ceilings and no basis search. Cap every phase by an overall **450-second** preparation/control/decision/replay deadline; cleanup is then mandatory, never permission for more arithmetic.

## 3. Output limits and acceptance

CAPRUN enforces wall, inherited per-process CPU and sampled group RSS; **it does not meter stdout/stderr bytes**. Its RSS cap is sampled, not an instantaneous hard aggregate-memory bound. The old exporter's aggregate byte limit concerns only its three artifact files and is irrelevant to solver logs.

Before GREEN execution, prepare a tiny inherited `RLIMIT_FSIZE=64MiB` wrapper (plus `RLIMIT_CORE=0`) and run actual small stdout and stderr overflow controls after GREEN. Use only two pre-opened exclusive regular output streams per production stage; no Singular file-writing/link/system/library/startup commands. Bound and count the wrapper's separate receipt/telemetry files. For the two stages this bounds their four registered streams by256MiB, not the entire filesystem or every possible implicit runtime file. Record file inventories and stop on unexpected outputs; no global quota claim.

Strictly parse both streams, not return status or a success marker alone. Require NORMAL_EXIT, rc0, complete indexed objects, exact counts, unique delimiters and final termination marker, valid rational coefficients/variable indices, no trailing unparsed output and no Singular error text. Rehash frozen sources after execution. A truncated basis/cofactor, cap, parser failure or unverified row map is **INCONCLUSIVE**, retaining any candidate separately.

**Unit acceptance:** construct the full803-entry cofactor vector by the checked map, pad omitted zero rows with0, and independently expand the exact rational identity `sum(h_i*f_i)=1` against the frozen literal rows. A completed `[1]`, scalar marker or internal matrix check alone is not a replayed certificate. If extraction/replay caps, record UNIT_CANDIDATE only.

**Properness acceptance:** independently parse the complete exact-Q G, verify all Buchberger S-pair remainders are0, verify `NF_G(1)!=0`, and reduce every one of the803 original rows to0. This proves `I subset (G) != R`, hence I proper; reverse inclusion is unnecessary for this sufficient certificate. Verify monicity/tail reduction separately if labelling G reduced. No modular properness, dimension heuristic, timeout inference or unchecked nonunit list is allowed.

Tiny replay controls after GREEN must reject a changed target, deleted low row/guard, wrong variable/row map, coefficient/order drift, resynchronized truncated stream and altered cofactor. Properness checker control: with I=G the list `x^2, x*y-1` has a nonzero normal form for1 and each input reduces to0, but its S-polynomial `y*x^2-x*(x*y-1)=x` does not reduce to0; it must be rejected, since the generated ideal is actually unit. Also include one genuine small unit and one genuine proper complete certificate, with explicit leading/interior/trailing zero input rows. No production arithmetic was used to prepare these requirements.

## 4. Cost rationale, stop and custody

The pilot imported this exact client in0.241 seconds at about22MB sampled RSS; that supplies no GB runtime estimate. The300-second/16GiB budget is a bounded discriminator, not a predicted completion time. Deferred lifting is motivated by terminal K7/K8 evidence: a275-generator full lift was preempted after38:49 near42.7GiB despite a much faster decision calculation. No past degree99 frontier conclusion is imported.

Read historical `box/k8-b11-q1-20260905/certificate-audit/README.md` (SHA `3704cf766044903f3938081bb17cdf174bdecd5befd45937793ce9dfadf4cad5`), `box/k7-strata-gate-20260905/cert-audit/make_singular.py` (`e3581ed645d3f32cd63e1162675500388028611fd000b82c517291e758f58470`) and `build_cleared_verifier.py` (`155098f1f541e9e238419014560d5484cb1ff29c885b64e3f871744634ebf0f5`). Borrow only exact-Q cofactor/replay principles, not their subset, localization, pruning or old timeout wrappers.

On first pin/control/failure/cap issue, stop the batch; no repair-and-retry or enlargement. Retain exact leader PID/PGID/start ticks/argv/cgroup/namespace, caller receipt, sampled RSS/swap, UTC start/end, rc/cap reason and signal/reaping evidence. Validate and terminate only the owned groups through the reviewed runner; observe them empty before handoff. Retain every input, suffix diff, basis/cofactor, map, verifier and hash, including partial/error outputs.

Keep sole complete source/output streams on retained EBS; pull only compact evidence unless root requests otherwise. Root retains the instance; `DeleteOnTermination=true` remains a preservation boundary. This task executed no AWS/SSH/CAS/full arithmetic, created no executable solver code and inspected no live review material. Its only new artifacts are this report and `box/d125-small-exact-solver-prep-20260907/custody.json` (SHA `7100e93f41ef111c89d05a58567600f07dda73fab6c59d517744088ea3e10dcd`). All writers terminal after sealing; **STOP/IDLE pending explicit root GREEN**.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `10214`.
- Body SHA-256:
  `6b094a1f292814d63e5f06bd247d4dfb9d8f74bd61a3e0175aa4be5a01e5e1f0`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
