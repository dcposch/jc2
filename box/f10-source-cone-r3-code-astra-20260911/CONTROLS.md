# Prospective controls; NOT executed

This is a static specification, not a mutation implementation, baseline, fixture or witnessed refusal. Every future run requires FIRST source clearance plus a separately reviewed ROOT runtime/registration/native/termination packet. Baseline reconstruction and its independent check must complete before controls are credited. No membership solver or coefficient payload is supplied here.

## Exact semantic mutations

Use fresh copies of one authenticated future producer artifact. Keep the tag, all headers, key/array inventories and every other mathematical datum unchanged. Apply rational addition canonically in seven coordinates, sort sparse terms, remove a term only when its entire coefficient becomes zero. Every changed object below remains within the intended algebraic support and schema. A schema, host, resource or header refusal does not pass its mathematical control.

1. WHOLE-B INVERSE: in `base.units.dL[1]`, add `1/1` to coordinate zero. The product defect becomes the literal dL, a unit in the whole nonzero rank-seven B, hence is nonzero. Expected predicate: `all-seven-coordinate inverse product dL`. This neither selects a field factor nor assumes a first nonzero entry is a unit.

2. ACTUAL ELL COLUMN: in `bands[9].B_var`, change the coefficient of theta^2 from whole-B one to whole-B two; exponent vector `(0,0,0,0,0,0,2)`. Keep the literal target variation `-theta^4`. Gap10 has beta=7, m=10, so the theta^4 defect is `(2m-3beta)*theta^4=-theta^4`, and cannot vanish. Expected predicate: `independent column variation` (or an earlier actual operator equality explicitly identifying this defect). No zero-to-zero mutation is possible. The separately checked literal ell polynomial is theta^2+(6F/11)theta+(13H-24F^2/11)/21.

3. COMPLETE SOURCE: in `source.Bhat`, change `[S^2 theta^5]` from one to two, exponent `(0,0,0,0,0,2,5)`. Its Jacobian against the fixed leading A term S theta^3 changes the highest coefficient by `-S^2 theta^7`. Expected predicate: `independently reconstructed full Euler B`; it also breaks the complete upper Jacobian. This is a changed coefficient object, never a source point.

4. LITERAL RESIDUAL READBACK: replace the known zero polynomial `raw.P1[20]` by whole-B one at the all-zero seven-exponent vector. The leading ODE makes that baseline row exactly zero. Leave `raw.jacobian_slots[44]` and all source data unchanged. Expected predicate: `literal 45-slot low readback`. This checks that the emitted redundant/zero low slot is actually read, not silently regenerated and ignored.

5. CRITICAL GRAPH: add X1^7 to `graph.zeta`, exponent `(7,0,0,0)`. Its prescribed critical graph defect is `c*X1^7`, nonzero since c is a unit and X1 is a polynomial variable over the whole B. Expected predicate: `graph substitution literal`. Keep all source, raw Psi7 and c data unchanged. The graph inverse itself also has a distinct optional control: add one to coordinate zero of `graph.c_inverse`; expected `critical inverse literal`.

6. MIXED ROW: add X1^27 to `graph.slots[24]`, exponent `(27,0,0,0)`. The increment is a nonzero whole-B polynomial with the correct weight27. Expected predicate: `all25 literal graph rows T`. This specifically tests the actual `(z P1_0-U P0_0)(X,zeta)` row, not a mislabeled z=1 replacement. The remaining 24 graph slots and all raw data stay unchanged.

Future control protocol must record which exact named mathematical predicate was reached, the baseline and mutated artifact hashes, and the separate fresh checker invocation. An earlier unrelated refusal is INCONCLUSIVE, never evidence of the desired predicate. This static task does not run or authorize any control.

## Resource refusal controls and outcome scope

Separately test duplicate JSON keys, mathematical JSON numbers, noncanonical/reducible rationals, zero sparse terms, duplicate/unsorted exponent tuples, wrong seven/four exponent dimension, missing one of all25 slots, changed unit inventory and each fixed limit. These are parser/interlock controls only. Zero generators and duplicated equation slots remain legal, represented by separate required array positions. Zero sparse terms inside a nonzero polynomial are illegal.

First observation proposal, UNMEASURED: one producer process and one separate checker process, each at most600 CPU seconds,900 wall seconds,8GiB memory; entire run artifact/log aggregate at most128MiB. Root must set a combined deadline/aggregate policy and authenticate all scientific children before any launch. These numbers are prospective refusals, not feasibility claims, automatic escalation or permission to consume a worker. Internal polynomial cap100000 terms, per-wire-polynomial20000 terms, total serialized400000 terms, byte cap128MiB and numerator/denominator4096-character caps may refuse a mathematically valid reconstruction; that is PARTIAL/NONDECISION, never a source result.

`authorize` rejects off-EC2/tag mismatch before mathematical imports, sets the process-local integer conversion ceiling4300, and checks registered source/interpreter/native-inventory bytes. It does not prove registration provenance, native-closure completeness, Python-startup safety or process-tree limits. The independent runtime gate must supply those, including startup/environment/module-path pinning and the external CPU/wall/RAM/aggregate enforcement. Self-authored JSON is not authority.

Registration schema is exact: `job_tag,status,authority,mode,artifact,files,runtime,limits`; artifact has only `path,sha256`. It binds the absolute artifact path and selected mode. A producer registration uses `UNFORMED` for its exclusively created output; a checker registration must pin the actual input SHA and is therefore issued separately after formation. Mutations require separately registered mutated-input pins, so a mere hash mismatch never counts as semantic rejection. `files` has exactly the five owned Python basenames. Runtime keys are `python_sha256,native_inventory_path,native_inventory_sha256`; the separately pinned native JSON has `job_tag,files`, its files mapping absolute paths to SHA values. All number-like metadata values are strings. This schema is prospective and no registration file is supplied.

A future checked reconstruction establishes only the complete literal coefficient interface. A positive weight30 identity needs a new literal cofactor checker and nonzero semantic certificate mutation. No solver, identity, source-zero conclusion or escalation to another r/exponent/prime is implemented or authorized here.
