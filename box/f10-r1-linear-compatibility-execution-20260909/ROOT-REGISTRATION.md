# ROOT registration — finite generic compatibility only

Root issues this administrative authority after accepted STATIC17zb. ONE batch;
no actual-source inputs, source acceptance, solver.py or full217-row matrix.
No code change, installer, fallback, retry or additional experiment is allowed.

Exact worker: i-08d2a40f272ee9fa2, us-east-1, 172.30.0.72, ip-172-30-0-72,
c7i.4xlarge, existing Owner=f10-r1-exact-artifact-validation-20260909,
retained EBS vol-0574e0fa5aed1f5e3. Root verified STOPPED and ownership before
allocation; no other instance is in scope. Start only after independent ROOT
USER jc2-f10-linear-compat-runtime-stop-20260909.timer is ACTIVE for
2026-09-09 22:54:00 UTC. Never reset. Worker STOP at completion, never terminate.
Root verifies the physical DMI/hostname before releasing the execution agent.
The agent may independently repeat exact metadata checks.

Remote cwd: /home/ubuntu/jc2-f10-r1-linear-compatibility-20260909 — must be absent before staging; exclusive new directory.
Stage ONLY the nine task files in dispatch.registration.json's pinned vector,
plus that exact root-issued registration. Pin all110 task/interpreter/native
inputs both before and after. No mathematical data is present. Package/native
metadata comes from environment.json SHA35a9c3229300816d16b53db6d0795ce9d6fd1de1f8a10ff5689c83543036a34f;
its100-file declared closure is NOT a hermetic whole-OS/stdlib/import map.
No Python import/version/syntax probe outside the registered batch.

Seven fixed preflights: five authority refusals, valid sentinel, reviewed
descendant-RSS/TERM/KILL regression. Then ONE compatibility child: twelve
in-process verifier calls (five positive/seven negative), two2x5 RREFs,
23explicit fmpq constructors. Everyfixtureconstant-v; no claim of nonconstant
B reduction runtime coverage. Exact source remains UNEXECUTED by this batch.

Registration fixes probe5s wall/3CPU/2GiB, dummy5s/3CPU/32MiB,
compatibility180s/170CPU/2GiB, aggregate mathematical240s, file16MiB.
Fixed mathematical deadline22:47:00 UTC; dispatch task22:53:00 UTC;
original agent publication cap23:01:54 UTC (reserve begins22:58:54).
All deadlines are original and cannot be reset. Caps are experimental bounds,
not performance predictions. Admission/return checks in frozen caller apply.

Agent may stage via unchanged fleet.sh push/run/pull ONLY, not wait (installs),
launch, ips, term or term-all. Use one exact remote SYSTEM service
jc2-f10-linear-compat-runtime-20260909.service, userubuntu, workingdirectory
above; RuntimeMaxSec=300, TimeoutStopSec=5, KillMode=control-group,
MemoryMax=3221225472, TasksMax=64, LimitFSIZE=16777216.
Explicit empty LD_LIBRARY_PATH/LD_PRELOAD; OMP_NUM_THREADS=1 and
OPENBLAS_NUM_THREADS=1. Exact main /usr/bin/python3 -I -B dispatch_batch.py.
Retain service/PID/cgroup identities and system-level memory/swap only;
no unrelated workload or protected repository inspection. Actual CAPRUN child
vectors and per-operation cooperative authorities remain the unchanged caller's
responsibility. Root registration is cooperative custody, not authentication.

Before any receipt/payload read, establish terminal exactservice/recordedPIDs
and quietownedgroups. Preserve authorities, alltelemetry/dispatch files,
receipt, allfixturebytes, sourcepostpins, local/remote terminalmanifest,
stdout/stderr and observed resource/clock evidence. Once remote collection
is secured, STOP exactworker; root independently verifies STOPPED before
canceling backup. On any failure/cap/missingpin/refusal mismatch: STOP-NONDECISION,
no repair/rerun. Author one transactional report/custody with allwritersIDLE.
First different-model runtime-evidence review remains required before promotion.
