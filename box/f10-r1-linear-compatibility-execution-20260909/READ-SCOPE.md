# Finite generic/API execution — documentary scope

First tool 2026-09-09 22:31:54 UTC. Both targets absent. Controlling task cap
23:01:54 UTC (first+30 minutes, earlier than 23:04); final three minutes
reserved. No deadline reset. Initial authority is documentary staging only;
no worker or mathematical access before the separate ROOT release.

Current hashes checked before bodies: compatibility producer custody and all
29 entries (18 owned plus 11 prior inputs) unchanged; compatibility gate and
terminal receipt match ROOT's two digests. Receipt read before gate WHOLE.
The compatibility caller, harness, VALIDATION, helper and probe read WHOLE
this turn. Own producer/report/checker/evidence/algebra/remaining own docs
reuse prior same-byte WHOLE reads after current pin verification. Parent
17z gate read WHOLE after its current hash and receipt-vector binding; parent
producer same-byte WHOLE reused. No provenance report followed.

ops/FLEET.md and ops/fleet/fleet.sh read WHOLE after hashing. Historical
inventory, pricing and launch recipes confer no authority here. Only exact
ROOT registration may license this worker and batch. run_capped.py current
4435279d pin checked; prior same-byte WHOLE read reused, no code alteration.
artifact_finalize.py used only for metadata publication. Environment JSON
and RESULT.md both current-pinned and read WHOLE; package/native hashes are
documentary pins, not an observed import map or hermetic OS closure.

No actual source coefficient artifact, receipt, source acceptance or solver
is an input. No current peer/gate, ledger, protected tree or worker accessed
during the initial documentary stage. Generic fixtures remain constant-v;
the test set is frozen and will not be extended.

ROOT then released two immutable registration files, both current-pinned
before WHOLE reads: dispatch.registration.json ba1b374b... and
ROOT-REGISTRATION.md 03473812.... The 110-pin vector includes nine task files,
the interpreter and 100 package/native files. Exact registration environment
equals the prior frozen metadata. Only those nine task files plus the root
registration were staged. No active authority was authored independently.

Execution release preceded the exact-worker checks at 22:38:10–11. AWS and
SSH remained confined to the owned worker; only its service/cgroup/PIDs and
the registered file paths were inspected. Mathematical execution was solely
the one frozen caller's eight CAPRUN children. No code or fixture was edited.

After terminal service/cgroup/original caller evidence, all 85 remote files
were hashed before payload reads and transferred; all bytes matched. All
110 registered source/environment pins matched again. Dispatch/telemetry
metadata identified all owned PIDs, independently absent at 22:41:15, with
all eight PGIDs absent. Only then were receipt semantics read. The complete
generic receipt, full batch PASS, compatibility/dummy telemetry, five
refusal stderr files and dummy stdout were read WHOLE. Other telemetry and
authorities were parsed at metadata field/vector/hash scope, not claimed as
whole prose reads. All 12 fixture bytes were hash/size-bound to the receipt;
their mathematical identities were NOT replayed locally. The local metadata
check compares source/authority/argv/stream/cutoff/fixture bindings only.

The worker was positively STOPPED at 22:41:46. Root separately acknowledged
its own 22:42:06 STOPPED check and backup cancellation/inactive state; this is
lifecycle-only communication, not a new scientific input. No stop duty remains.
