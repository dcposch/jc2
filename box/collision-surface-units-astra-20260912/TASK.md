# Full-source collision surface: one unit-group discriminator

ROOT September12,2026,15:22UTC. Astra PRIMARY co-research, not FIRST.
Original reserve15:35UTC / HARD15:38UTC NEVER RESET, includes custody.
Basis0d39df3c9fd69c939a8420c54d03228b9077777d. Exactly one scientific report:
xmodel/collision-surface-units-astra-20260912.md.

Hash then freshly WHOLE read COORDINATION.md FIRST (expected
33cfa6106972fe557a95ca81a9a05bb72300a82b2fbb7908915441cf75377597), then
this TASK with invitation pin. Exactly TWO scientific inputs. No inherited,
previous-task, peer, corpus, linked or external body. Reconstruct all stated
algebra needed, rather than treating ROOT's exploratory description as proof.
Use the existing finalizer transaction; apply_patch ONLY in leased partial.
Own PINS/custody allowed, no shared/TASK/older artifact edits.

## Why this question, and what is NOT new

The general finite-unramified full-plane hypersurface plus free dualizing
and radial-form-descent normality criterion is now refuted by a completed
different-model-reviewed explicit control. Do NOT attempt that criterion
again. ROOT has changed representation to the FULL collision surface of
an actual hypothetical Keller pair, retaining its origin from two copies
of the whole polynomial plane. The collision object and secant idempotent
are old campaign material; neither is a new theorem or an injectivity proof.
Old projective connectedness arguments fail without genuine saturated
boundary data. Old pair-square event-module ranks are not available here.

## Exact one quantity

For every polynomial F=(f,g):A2_C -> A2_C with J(f,g)=1, write

    A=C[f,g] subset R=C[x,y],
    C_F=R tensor_A R
       =C[x,y,u,v]/(f(x,y)-f(u,v),g(x,y)-g(u,v)).

The diagonal is open AND closed (etale and separated), so C_F splits into
its diagonal factor R and an off-diagonal factor S. If S is nonzero, let
S_i be the coordinate ring of any connected component. Etale over the
smooth plane implies smoothness; verify the component/domain assertion.
Both source projections are etale/quasi-finite, NOT assumed finite or
surjective. The scheme is affine and closed in A4 because it is a clopen
component of that affine fibre product.

DECIDE, as far as sound bounded work permits, the proposed source statement:

    Every S_i has S_i^*=C^*.

This is a QUESTION, not a premise. A proof would be a new source restriction,
not automatically JC2. A counterexample must be typed at its true hypotheses:
no full-plane Keller counterexample is presumed available. If undecided,
give an exact GAP and the strongest relevant all-source implication you
can actually prove; do not disguise a restatement of JC2 as progress.

The cheapest test is manual algebra of the true tensor product, units and
norms in this 13-minute reservation. In particular test whether full-plane
R^*=C^*, flatness/etaleness, and symmetry of the two factors really control
units of a connected quotient factor. A norm is in the source FUNCTION
field automatically; it need not be a regular source unit without an
argument about missing branches. Give that argument or locate its precise
failure. Distinguish units on the full disconnected C_F from units on S_i:
idempotent-wise constants are not the issue.

## Symplectic motivation, not an assumed closing theorem

With alpha=(xdy-ydx)/2, a polynomial T satisfies dT=alpha-f dg by polynomial
Poincare. On the off surface, lambda=pr1^*alpha-pr2^*alpha=d(delta T),
and omega1-omega2 vanishes on its tangent bundle. Thus it is an exact
algebraic Lagrangian in A4 equipped with omega1-omega2. Factor swap acts
freely, sends lambda and delta T to their negatives, and preserves the
whole off locus (possibly exchanges components). This by itself has no
known properness or zero-section-intersection implication; do not import
compact symplectic statements into this noncompact algebraic setting.

ROOT is independently testing that geometry. Your owned question is the
UNIT GROUP of the actual source fibre-square, not a second generic
Lagrangian countermodel farm, unit-ideal computation, or canonical graph
normality attempt. No degree bound, source existence, generic parameter
cover or actual finite-flatness is available.

## Controls and output

At least one meaningful matched control is required. An automorphism has
no off component, hence is vacuous, not enough by itself. For a dropped
global-polynomiality control, on C* times A1 the rational Keller map
(x,y)->(x^2,y/(2x)) has a nonempty off graph (u,v)=(-x,-y) and nonconstant
units x^n. Its pole at x=0 is genuine. This shows that abstract etale
correspondences or source-localization analogues do NOT prove the claim.
You may construct a second small manual control only if it decides a
specific false norm/extension step; no family expansion.

Report CONFIRMED/REFUTED/GAP for the exact source assertion, attacks,
explicit all-source dependencies, and what the result would and would not
change. If no new mechanism is obtained say NO_NEW_MECHANISM. At most
11000 scientific body bytes; no canonical OPEN, FIRST, descendant or
automatic research/deployment authorization. No proof by model agreement.

Manual math only: NO scientific interpreter/CAS/helper/import/AST/test/
syntax/compile/dummy, network/AWS/SSH/agent/process/protected-tree action.
Inert text/hash/UTC and existing administrative finalizer only. Never
enter/enumerate/search/read/status/control jc2-lean or its public mirror.
Own WHOLE and BOTH input postpins before unique BODY-END LAST; finalize,
expected verify, report/manifest/PINS WHOLE, custody SHA FIRST and actual
ALL WRITERS IDLE in final handoff. No later writes. ROOT owns independent
status and hard interruption of this exact native task if still live.
