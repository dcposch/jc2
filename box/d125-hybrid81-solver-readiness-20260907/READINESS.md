# Disabled hybrid81 worker/dispatch checklist

PREP ONLY. No authority/deployment/worker access/CAS occurs. Frozen solver
registration remains box/d125-hybrid81-solver-prep-20260907/REGISTRATION.md,
SHA38bc496f9f2eb7f7519c70bbabe0eab8586c56264b7e20c0078fc71cb818cd6d.
This checklist does not replace that hash or edit any gate-charged source.

1. Root accepts terminal Sol adapter/caller gate, reading its receipt/pins first.
   It is currently LIVE and unread here. Its optional independent hybrid fixture
   is unknown/unavailable until that handoff; no fixture is duplicated now.
2. Root alone STARTs retained i-0da0cebfc97c9fd54. After separate read-only GREEN,
   resolve current running IP from EC2; do not assume old172.30.0.56/public IP.
   Check exact Owner coordinator-factored-jacobian-20260906 and EBS attachment
   vol-0eb6450d18ffa89f1. Never terminate/delete/retag or touch another worker.
3. Fresh Linux/Amazon EC2 DMI board_asset_tag must equal that instance; collect
   boot_id, hostname, process list/autostart status, available RAM, swap, disk,
   EBSserial vol0eb6450d18ffa89f1, root mount and storage paths. No unexpected
   compute owner or unrelated arithmetic may be displaced. Historical PIDs are
   not valid signal targets after reboot. Fresh exact worker audit is required.
4. Read-only hash check source/construction, Singular/Python/prlimit and CAPRUN;
   preserve all existing directories. Exact NEW cwd must not lexist:
   /home/ubuntu/d125-hybrid81-exact-solver-20260907.
   Any occupied path/pin/owner/storage discrepancy stops before deployment.
5. Only a later physical engineering GREEN permits exclusive creation/deployment
   of frozen driver.py/exact.py/hybrid.py/CAPRUN and the SAME solver registration.
   A tiny hybrid fixture/helper needs separate frozen hashes in that GREEN.
   No old authority is copied; no fixture/helper is presently written or run.
6. New engineering control must call H.read_hybrid(tiny_bytes,production=False),
   append D.footer(variables,SHA(tiny_bytes),control=True), and exec Singular
   --no-rc -q on the resulting exclusive engineering/hybrid.sing. This tests the
   NEW serialization/import boundary, not D.control_input's old generic ideal.
   Before any fixture read/arithmetic, reuse D.context(authority,'control'),
   D.limits('control'), exact CAPRUN-parent/PGID checks and new fixed helper/fixture
   pins. Do not call driver.payload(decision/verify) or read production SOURCE.
7. Prefer Sol's eventual independently supplied fixture if applicable. Otherwise
   root must approve a concrete tiny fixture and helper later. It must include
   rational coefficients, leading/interior/trailing zeros, a duplicate and a
   nontrivial inverse guard, with an independently known unit cofactor identity.
   Serialize ALL slots; no std/slimgb/dim in this control, only direct lift to1.
   Control <=10wall/CPU512MiB,64MiBFSIZE each stream/core0; one attempt/no retry.
8. Capture complete actual I_SIZE/I/G/T and CHECK/END. Require normal0, literally
   empty stderr and exact source/ring binding; use H.checked_certificate on ALL
   toy slots, validate I/column/T dimensions and zero/duplicate source mapping.
   Normal/-O replay on the SMALL output only. Retain actual identity, pins,
   complete command and telemetry; receipt-first read and fresh group absence.
9. Reuse accepted FSIZE/long-print/descendant controls listed in REUSE.json.
   No default replay of that whole batch. If fresh startup/cwd/identity behavior
   reveals a specific changed-path cleanup gap, root may authorize ONE old pinned
   no-CAS RSS/TERM/KILL fixture; otherwise ordinary current-group cleanup suffices.
   A material binary/runner/limits change is a blocker, not implicit retest scope.
10. Only a later distinct solver GREEN allows mode=solver. Same300decision
    including parse/serialize/import/slimgb/conditional lift, optional120 verifier,
   450total,16GiBAS/sampledRSS,64MiBFSIZE/core0 and2MiB generated-input INCLUDING
   suffix. Caps are not automatically enlarged. No full import-only warmup is
   implied; the first full import is inside the sole decision cap.

All evidence/authority/phase output paths must be absent using lexists, including
decision/verify .sing,.stdout,.stderr,.telemetry.json,.identity.json,.launch.json,
.result.json and prospective engineering/hybrid files. Use a new explicit
engineering authority path and a distinct solver authority path; neither exists
now. Record root GREEN, registration, code/fixture/binary, source, boot and expiry
pins before dispatch. Capture actual controller/runner/payload PID/PGID/start_ticks/
namespace/cgroup and deadline promptly. Observe metadata only while live; no
automatic retry, token refresh, gauge, order or source-subset variant.

Future solver argv remains the frozen driver interface:
/usr/bin/python3 -I -B /home/ubuntu/d125-hybrid81-exact-solver-20260907/driver.py launch --phase decision --authority /home/ubuntu/d125-hybrid81-exact-solver-20260907/solver.authority.json
Conditional verification substitutes --phase verify under the SAME authority and
total expiry, only after complete normal decision receipt. Invalid/partial/capped
output is INCONCLUSIVE. Full sources remain on retained EBS; root alone STOPs.
