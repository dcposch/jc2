# Actual-pair iteration: fixed nonproper target discriminator

ROOT September12,2026. Astra primary co-research. First action UTC/hash,
then this TASK fresh WHOLE; sole scientific input. No corpus/peers,
network, CAS/scientific/code execution, AWS, Git/process inspection,
protected tree or new agents. Manual math and documentary date/hash/text,
apply_patch and existing artifact transaction only.

One report xmodel/pseudoplane-fixed-target-astra-20260912.md <=1600words;
begin/close/finalize/expected verify using ops/artifact_finalize.py,
basis0d39df3c9fd69c939a8420c54d03228b9077777d. Own partial and own box
PINS/custody only. Unsealed skeleton first, bounded sections, whole
readback/postpins then BODY-END LAST. Terminal custody-FIRST ALL WRITERS
IDLE. Original reserve02:41/HARD02:44 UTC, no extension. Honest GAP,
no promotion, canonical OPEN, descendant or automatic refinement task.

This is NOT another arbitrary-module control. Assume a COMPLETE regular
scalar pair F=(h,g):S->A2 actually exists, where
 S={U^2=A+A^2Z}, T={t^2-1=x^2Z},
 q:T->S, q(x,t,Z)=(x^2,xt,Z),
 j:S->T, j(A,U,Z)=(2U,1+2AZ,Z),
 i:A2->T, i(x,y)=(x,1+x^2y,2y+x^2y^2).
q is finite etale degree2, j an open immersion missing
M_-={Z=0,t=-1}, and i an open immersion missing
D_-={x=0,t=-1}. These are DISTINCT affine lines.
The explicit form omega=dx wedge dt/x^2 on T descends to Omega on S;
q*Omega=omega, j*omega=2Omega, i*omega=dx wedge dy.
Assume dh wedge dg=c Omega, c!=0. Define pi=q i:A2->S and
E=q j:S->S. Then F_k=F E^k pi is a polynomial Keller map, scalar
2^k c, with generic degree2^(k+1)*d, d=deggeo(F).
These elementary map/volume statements may be checked from formulas;
they do NOT supply F itself or reduce arbitrary JC2 to this surface.

Let L={A=U=0} and C={Z=0,U^2=A} in S. Both are A1 and meet once.
E(A,U,Z)=(4U^2,2U(1+2AZ),Z); E fixes L pointwise and sends
(U^2,U,0) to ((2U)^2,2U,0) on C. pi and E should be surjective,
etale, nonfinite, each of degree2. For a morphism f:X->Y between
smooth complex affine surfaces, let S_f consist of y with a sequence
x_n leaving every compact subset of X and f(x_n)->y.

QUANTITY: does the EXACT nonproper target of F_k become the FIXED set
 S_F union F(C union L), for EVERY k>=1,
with S_F union F(L) for k=0? Check both containments, closure, and
all exceptional points; do not give merely a containment for generic
divisors. F is not assumed finite, proper or surjective.
CHEAPEST TEST: manual escaping-sequence/finite-cover/open-immersion
argument, <=15minutes. Prove or refute the proposed exact formula.

If the formula holds, assess its real consequence, not just its wording:
it creates arbitrary geometric degrees of actual polynomial-plane
Keller maps with a single nonproper value set, conditional on F.
Does any ELEMENTARY source argument then force a bound? A finite
degree-bound theorem is NOT supplied as a premise. Finitely generated
pi1, Noetherianity, bounded support, fixed number of target components,
Euler additivity and finite morphism descending-chain results must not
silently be converted to such a bound. E and pi are not finite.

If no bound follows, identify one precise datum that this argument has
NOT bounded (e.g. local ramification of the normalizations over S_F,
source complement topology, or actual source degrees). Do NOT assert
its growth without a derivation. The exact formula itself is the main
deliverable; no uncontrolled classification or degree farm.

Scope controls: E itself supplies a surjective nonfinite etale map on
S, NOT on A2; the identity map on S only tests the composition formula,
not the scalar-pair premise into A2. Distinguish source normalizations
and the target nonproper set. Shared target-set equality does not mean
the maps, their images, their finite covers or their monodromy are equal.
No new novelty claim. ROOT separately checks primary literature for a
degree bound; that work is not an input to this task.
