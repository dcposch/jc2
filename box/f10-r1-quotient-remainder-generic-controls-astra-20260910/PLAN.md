# Disabled generic validation plan

STATIC / UNEXECUTED. No syntax, import, AST, compilation, arithmetic, fixture,
matrix, certificate or test was run. This packet does not contain solver.py,
does not copy a live producer, and has no fallback to the old producer.

## Future dispatch, not authority

No authority is issued. enabled=false; worker, hostname, boot/current-host
registration, cwd, argv, output, job/admissibility registration, all caps,
absolute and aggregate clocks, new solver hash, installed FLINT version and
native-file closure are all null/unassigned. Root alone may later create an
ordinary F10-R1-REGISTERED/v1 authority with operation build, after the joint
FIRST static gate and a separately bounded AWS engineering registration.
This document is not such an authority and establishes no deadline or cap.

Future literal child:

    /usr/bin/python3 -I -B /FRESH/FLAT/generic_controls.py AUTHORITY OUTPUT

The future flat package must contain this harness, the new reviewed solver.py,
and byte-identical checker.py, evidence.py, algebra.py and execution_gate.py.
Those are six distinct filenames, all explicitly registered in file_sha256.
The unchanged gate also requires exact registered CAPRUN parent/child vectors,
current Amazon EC2 host identity, cwd and executable/runner hashes. There is
no new supervisor, process group, authority schema, installer, dependency
discovery or native API. Root owns the accepted controller's boot/expiry/cap
custody and complete installed native dependency declaration; this helper
does not claim the inherited gate independently discovers that closure.

Before any arithmetic or FLINT import: the gate's own literal SHA is checked;
authorize verifies the host, vectors and all registered files; evidence.py is
checked at its unchanged SHA and strict-loads the authority; all six package
pins and retained literal hashes are checked; native interpreter, exact
installed module/version declaration, and exclusive outputs are checked.
authorize hashes every declared native file before import. The installed
module's path/hash/version are checked again immediately after importing it.
No input source artifact, incoming source receipt or ROOT source acceptance
is required, read, minted or simulated. univariate_acceptance must be null.
Generic evidence is not eligible for an actual-source acceptance claim.

OUTPUT must be an absolute filename in the flat cwd. All twenty derived
sidecars and OUTPUT must be absent, including dangling symlinks. Each of the
eight positive cases has OUTPUT.ID.input.json and OUTPUT.ID.candidate.json;
each of the four negatives has OUTPUT.ID.negative.json. All opens are
exclusive. Sidecars become read-only, are reread and hashed, and every hash
is checked again before the summary. A failure/cap retains existing sidecars
and produces no successful summary; there is no retry or cleanup deletion.
Root's later registration must authorize these exact deterministic outputs.

## Exactly eight positive expectations, derived by hand

All use p=product_(j=0..6)(v-j), generated only inside a future authorized run,
and e=product_(j=1..6)(v-j)/720. At the seven rational points e is 1 at v=0
and 0 elsewhere, so e^2=e, e(1-e)=0 in the synthetic coefficient algebra.
All unmentioned generators are zero, all nine generator slots are retained.

| ID | Input | Expected branch and exact reason |
|---|---|---|
| allzero-q0 | all fi=0, q=0 | UNIT: q^5=0; zero multipliers work. |
| allzero-q1 | all fi=0, q=1 | SEPARATOR: extraction of T^0*v^0 normalizes the target and kills every column. |
| constant-degree25 | f1=1, q=T^5 | UNIT: h1=T^25, attaining the multiplier envelope. |
| raw-two-T | f1=2T, q=T | UNIT: h1=T^4/2, so the raw coefficient 2 matters. |
| nonreduced-remainder | f1=T^2, f2=1+T, q=1 | UNIT: T^2+(1-T)(1+T)=1; quotient by T^2 cannot be radicalized. |
| lost-component | f1=e+(1-e)T, q=1-e | SEPARATOR: evaluation at v=1,T=0 kills all fi and gives q^5=1. At v=0 f1=1, not zero: only the selected leading coefficient vanishes on that split child. |
| varying-pivot | f1=eT, f2=(1-e)T^2, q=T | UNIT: h1=eT^4, h2=(1-e)T^3 give T^5; maximal-degree pivot changes across components. |
| all-generators | f1=T, f2=T-1, q=1 | UNIT: f1-f2=1. Omitting f2 changes the outcome. |

These are expectations, NOT executed PASS results. The harness accepts any
correct full witness of the expected branch, not just the manual witnesses
above. It never asserts a particular rank or number of splits from a status
flag. The nonconstant e inputs exercise coefficient splitting; the unchanged
full certificate is still the acceptance boundary for each synthetic case.

## Exactly four changed-object negatives

The retained positive object is deep-copied, actually changed, written
exclusively, reread and hashed before checking. Every expected failure must
have exact type ValueError, not a subclass or unrelated exception.

| ID | Change | Exact expected reason |
|---|---|---|
| scaled-separator | Multiply the allzero-q1 witness by 2. All 1638 annihilations remain zero, but target pairing becomes 2. | dual target normalization equals one |
| raw-unit-plus-one | Add 1 to h1's T^0*v^0 coefficient in the raw-two-T witness. The identity changes by nonzero 2T. | full 217-coordinate sum h_i f_i equals q^5 |
| short-witness | Remove the final rational pair from the allzero-q1 separator. | complete rational witness dimension |
| nonreduced-coefficient-modulus | Change only p to v^7 in the otherwise valid raw-two-T input, and call the candidate itself. | generic modulus must be squarefree |

The final message is the exact additive API-DELTA contract, not an invented
producer exception. The dimension and modulus refusals occur at framing or
precondition stage; they are not mislabeled full arithmetic identity tests.
For every framed UNIT test the independent verifier computes and compares
all 217 coordinates. For every framed SEPARATOR test it computes all 1638
column pairings and all 217 target-coordinate contributions before rejecting
an incorrect identity. It does not use the solver's rank, trace, small matrix,
CRT helpers, arithmetic module or precomputed source matrix.

## Limits and handoff

Exactly eight positive candidate calls plus the one invalid-modulus call are
designed; there is no random/property farm or actual-source parser retest.
The independent verifier's own dense Q[v] reduction uses the supplied fixture
modulus; it is not the unchanged literal-source checker. A nonreduced
polynomial remainder algebra is allowed; nonreduced coefficient p is not.

Installed API behavior, syntax and runtime remain UNTESTED. No height, cost,
sparsity, timing, memory or speed benefit is claimed. Runtime success would
validate only this finite generic changed-object packet. Actual source use
still requires its own exact input/receipt/root acceptance and the unchanged
checker. No result from this packet constructs a source point or decides JC2.
