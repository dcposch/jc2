# Six-input source-only read scope

First action2026-09-10T16:18:32.870761185Z. New box/report absent before begin. Fixed stop16:30, reserve16:27. All six invitation hashes matched before any bodies.

Prior same-byte completed WHOLE reuse: old dispatch.py, mutate.py, probe.py, CONTRACT.md, and the highest produce.py. Producer was additionally reread WHOLE in this turn. The terminal runtime gate was freshly read WHOLE. No gate provenance or linked source was followed. The four files being copied were read as exact text and copied by apply_patch; no source evaluator/parser was used.

The actual documentary unified diff contains exactly two dispatch literal substitutions, at lines600/602, and one contract status substitution. New mutate.py and probe.py have exactly their original hashes. All other dispatch bytes, line count, source pins, success path, job tags, schemas, vectors, controls, limits and layouts are unchanged. STATUS-DELTA.diff is the literal content diff with stable old/new labels rather than mtime headers.

No existing disabled registration was read or copied: it is outside the six inputs. No new actual registration is created. Its unchanged schema/job/caps are specified by the copied dispatcher and contract, with fresh external ROOT authentication and the new dispatcher pin still necessary before any future run.

No baseline/coefficient/certificate body was read, hashed, parsed or copied; no tests, imports, AST, compilation, syntax checks, CAS or mathematical subprocess occurred. No AWS/network/process/Git/corpus/protected/shared or live reviewer access occurred. Only text/hash/diff/apply_patch and the existing documentary artifact publication tool were used.
