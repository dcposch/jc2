The first collection attempt completed terminal-state checks, copied both receipts, and validated the19 current input vector before failing at a too-strong canonical byte-seal check on Fable. External prompt contracts request BODY-END only; the lane receipt binds those bytes and says CLEAN/BODY_SEALED, but does not create an artifact-finalize transaction. The collector was corrected to use the actual seal.py marker-boundary parser and distinguish BODY_END_ONLY_RECEIPT_BOUND from CANONICAL_BYTE_SEAL. A second attempt reached all report/current-input/custody checks before a metadata KeyError: closed_utc belongs under the transaction's custody object. That accessor was corrected after reading the metadata manifest. No peer body was displayed or mathematically read. Existing receipt copies are reused only after exact byte comparison; no report/receipt was rewritten. Initial systemctl probes without the jc2-lane prefix returned not-found and were not treated as sufficient terminal evidence; the live original PID's cgroup supplied the correct unit name.
