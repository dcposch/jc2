# Disabled one-place rank runtime — STATIC / UNEXECUTED

This packet is not registration or execution authority. ROOT must independently
qualify the accepted complete all25 rational baseline and freeze its original
source-check execution evidence. No baseline body, prime/factor, candidate,
fixture, actual host or runtime measurements were supplied or generated here.
The four accepted rank science files and CAPRUN remain byte-identical.

## Entry and authority

Deploy exactly dispatch.py/probe.py/mutate.py in a separate ROOT0755 directory,
files ROOT0444. The science directory is exactly the four pinned ROOT0444
siblings in ROOT0755. Native closure is a separate full six-field inventory:
schema/files/directories/aliases/absent/python_path. It must close the actual
isolated interpreter, standard library, ZIP/absent paths, loader/libraries,
setpriv, CAPRUN ps paths and observed mappings. The source's <=64KiB
job_tag/files native subset is not a replacement for that closure.

ROOT alone supplies a fresh enabled registration, EC2 instance/host/boot/PID
namespace, exclusive cgroup and CPU origin, nonroot UID/GID, readonly paths,
clear environment, interpreter/native/source/wrapper pins, literal place,
baseline/qualification/original execution evidence, thirteen complete CAPRUN
argv arrays, and four original ordered UTC stops. The qualification record is
external ROOT authority, never evidence that a JSON assertion caused a prior
source check to occur. The baseline stays on immutable EBS outside tmpfs.

The source authority uses exactly fourteen full_argv entries, with the sole
ROOT_AUTHORIZATION_SHA256 token at index11. Live execution replaces that token
with the newly frozen authorization's SHA. For outer command registration,
only check-positive/check-negative permit the unique AUTHORIZATION_SHA256
token immediately after their unique --authorization-sha256. Only mutate
permits ROOT_REGISTRATION_SHA256 immediately after its unique
--root-registration-sha256. Missing, duplicate, misplaced or cross-phase
tokens fail; every other argument is literal. No shell or generic expansion.
Preflight/finder authorization hashes can be fixed in advance from fixed
metadata. Probe uses a literal ROOT0444 registration path, not a selfhash hole.

## Nine preflights and approved contract adaptation

Five actual finder entries refuse respectively status, caps, source inventory,
source hash and authorization hash. Exact pinned early authority stacks and
final ValueError labels are required, with no artifact/receipt/stdout. Actual
finder and checker wrong-job entries require exit1 and exact stderr
`REFUSED: exact registered CLI\n`, no artifact/receipt/stdout.

The valid sentinel and descendant dummy instead validate ROOT policy/native
metadata. They explicitly do NOT call rank authorize and do NOT prove successful
rank authorization or mathematical startup. This adaptation was expressly
approved by ROOT because rank authorize pins the actual finder/checker entry
path and live argv. The standalone mutator also never spoofs that entry or
calls rank authorize. FIRST must review this adaptation; it is not run evidence.

The dummy records parent and descendant identities, exits its parent, allocates
64MiB in a TERM-ignoring descendant, and requires CAPRUN's RSS/TERM/KILL/identity/
reap/cleanup telemetry. It has not run. Failure of any preflight stops all science.

## Exact rank and mutation path

One finder must exit0 with RANK_CANDIDATE_UNCHECKED and a complete exact receipt.
Rank deficiency is NONDECISION, never a retry or alternate place. After group
quiet, ROOT freezes the certificate and receipt. One fresh checker must exit0
with R3_FULL_ROW_RANK_CHECKED_NO_AUTOMATIC_SOURCE_PROMOTION and a receipt binding
all88209 identity positions, the fixed place, baseline, qualification, source,
runtime and authorization. No full-source producer/checker rerun occurs.

ROOT then freezes a positive-phase record binding that certificate and receipt,
the positive authority and exact fixed mutation policy. The separate mutator
independently validates that policy and current ROOT/source/native/baseline/
qualification/evidence pins before reading the certificate. It changes only
inverse[0][0][0] to (a+1) mod ROOT p. It uses no finder/checker field routine.
Canonical whole-byte restoration after undoing this coordinate is mandatory;
the changed payload and a FORMED_UNCHECKED mutation receipt are exclusive new
files. The baseline body is never parsed by the wrapper or mutator.

After quiet/freeze, a fresh checker authorization binds the changed SHA. Required
negative: NORMAL_EXIT2, no signal, empty stdout, exact stderr
`CHECK FAILED: full inverse identity\n`, and no checker receipt. No substring
PASS, hash refusal, malformed input, failed place or cap termination substitutes.
For a genuine positive square inverse, changing one inverse coefficient by1
changes one product column by a nonzero column of the invertible selected
matrix; thus the full identity must fail. This is prospective, not an outcome.

## Joint limits and custody

Fixed source AND entire batch:600wall/550CPU/8GiB. Nine preflights each5wall/
3softCPU; finder180/170; positive150/130; mutator15/9; negative150/130.
Totals540wall and479 hardCPU (CAPRUN soft+1), leaving60wall/71CPU for hashing,
startup, cleanup and custody. Sampled cgroup cutoff520 leaves nominal30CPU
cleanup; it is not a kernel aggregate CPU guarantee. Every phase must fit the
remaining original stops and joint cap. Source expiry precedes task/worker stop.
All envelopes are UNMEASURED; no fit/runtime claim or cap increase is licensed.

Baseline<=128MiB outside tmpfs, certificate<=16MiB, one fixture<=16MiB, each
source receipt<=32768B, joint static/runtime metadata<=8MiB: logical data bound
168MiB. Working tmpfs<=128MiB but content budget<=40MiB. Durable copying has
transient extra<=40MiB, hence<=208MiB data including baseline; OS and pinned
native/code inventory are separate inputs, not output-budget bytes. No baseline
copy is made. Memory includes processes/page charges under the8GiB cgroup.

CAPRUN is unchanged. Mandatory live finder/checker/mutator PID/PGID/start/argv/
UID/GID/native-map capture can fail if missed; no retry. Groups must be quiet
before receipts/hashes/payload harvesting. Terminal streams/receipts/artifacts/
telemetry/authority/policy/source/native pins and cumulative resources are copied
to fresh EBS and fsynced before the original task stop. ROOT independently
establishes dispatcher terminal/group quiet before intake and owns cleanup and
worker stop; wrapper never deletes/unmounts or authorizes a follow-on.
