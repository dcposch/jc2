# Prospective selected controls6 and10 — disabled, unexecuted

Final ROOT delta selects exactly6 AND10, drops8 entirely. No producer rerun, positive checker rerun, other control, or source verdict. mutate_controls.py is a standalone standard-library fixture writer, not a dispatcher, authority issuer, checker or mathematical acceptance engine. It does not import any scientific module. All nine pinned inputs were read WHOLE; actual coefficient body remains unread locally.

## Exact future CLI and external obligations

```
PYTHON -I -S -B MUTATOR --registered-job f10-r2-semantic-controls6-10-v1 --root-registration-sha256 ROOT_SHA --self-sha256 MUTATOR_SHA --checker CHECK_PATH --checker-arithmetic CHECK_ARITH_PATH --input BASELINE --output6 FRESH6 --output10 FRESH10 --receipt FRESH_RECEIPT
```

This is exactly19 sys.argv entries, ordered as displayed. Paths must be absolute, canonical, distinct and <=512 characters. No args / wrong tag / missing nonzero pins fails before payload processing. Linux and exact Amazon EC2 DMI precede JSON/Fraction imports. This small new interlock does NOT call authorize('mutate'): accepted authority supports only its nine-argument produce/check interface. The ROOT SHA is binding metadata only, not authentication. No enabled authority template is issued.

ROOT must separately authenticate a prospective registration; freeze and hash this source before loading; enforce native/interpreter closure, safe source/stdlib paths, filesystem identities and immutable baseline; install the unchanged five-file science directory (never add this script there); enforce original CAPRUN/child identity/PGID/quiet/custody controls and actual registration/command equality. The three source checks inside the mutator are additive, not replacements for pre-exec barriers. Its --self-sha256 cannot authenticate itself against a malicious caller.

Prospective partition: nine no-math controls5wall/3softCPU, mutator10wall/9softCPU, two checkers210wall/169softCPU, all within unchanged joint600wall/500CPU/2GiB/16MiB. Sum of child walls475, hard child CPUs386; overhead remains external and measured only in a future run. Baseline is outside working tmpfs; two fixtures plus receipt fit the one16MiB output partition. Mutator also conservatively bounds baseline+both serialized fixtures+32768 below16MiB. Every packet <=15MiB, receipt <=32768 bytes; existing1MiB metadata partition remains ROOT's full accounting obligation. Overflow is STOP, no retry/cap raise. No actual performance is claimed.

## Mutations and restoration

Input must be exactly1652675 bytes, SHA dac655a7ce75287b06010171bce66af1a644aff81de9620a452457a5f65a9587. This is a future literal pin, not a locally read payload. JSON duplicate keys and numeric tokens are refused. Every coefficient location is checked as canonical reduced numerator/positive-denominator strings, each integer <=4096 characters under process-local ceiling4300; sparse bounds/order/nonzero terms, field lengths, unit names, bands and row lengths are retained. Interface metadata is preserved byte-for-byte semantically through whole-object canonical serialization, not re-proved mathematically here.

Control6 changes ONLY doc['bands'][6]['B_var'] to []. bands is a list; element6 must have gap string'7'. Its original sparse theta² term is required to have the seven-vector(1,0,...,0), so the edit is genuinely nonzero. The original subtree's canonical hash and term count are recorded. Replacing that subtree restores the entire original canonical input bytes.

Control10 starts afresh from the ORIGINAL baseline. Scan families Psi,K1,K0; within each increasing row, existing sorted term, then basis index0..6. Select maximum absolute NONZERO Fraction, replacing the incumbent only for strict greater-than, so ties keep the first. Change q to q+1 with reduced canonical strings. If its selected coordinate becomes zero, retain the term unless ALL seven coordinates are zero; only then remove that term. On all-zero retained rows, insert the constant unit vector in the empty first Psi row. A reverse rational subtraction, reinsertion at the original position if deleted, or removal of the fallback term restores the whole original byte sequence. No JavaScript numeric coefficient round trip.

Both changed objects are independently parsed/validated; both digests differ from baseline and from each other. All inputs/source pins are rechecked before writes and after fixture readback. Outputs use O_EXCL/O_NOFOLLOW, flush,0444,fsync and parent-directoryfsync. Receipt is written last and read back. Partial files or missing receipt mean STOP/incomplete custody, never success; no deletion or silent overwrite/retry is performed.

## Receipt contract

schema f10-r2-controls6-10-fixtures/v1; status TWO_FIXTURES_WRITTEN_UNCHECKED; science_outcome NONE; job_tag; root_registration_sha256; sources maps absolute self/check/check_arithmetic paths to their hashes; input{path,sha256,bytes}; outputs contains exactly control6 thencontrol10, each path/sha256/bytes. Control6 additionally records mutation, gap, old_subtree_sha256 and old_terms. Control10 selection records family,row,term,basis,exponents,old,new,fallback,term_deleted. old/new are rational STRINGS; indices/counts are metadata integers. Also canonical_wire=true, inverse_edit_restores_whole_input_bytes=true, checker_run=false. Mutator normal exit0 has empty stdout/stderr and means ONLY completed fixture/readback metadata. ROOT independently binds receipt/fixture/current source pins before either fresh checker launch.

## Exact checker-negative acceptance — future wrapper, not implemented here

Use the UNCHANGED check.py through its accepted authority/runtime. Both checkers must have CAPRUN NORMAL_EXIT, child exit1, no signal/resource termination, empty stdout; their input pre/post hashes equal the respective frozen fixture hash. Absence of CHECKED output alone is insufficient.

Require stderr <=8192 bytes and its ENTIRE decoded ASCII content to have:

1. Exactly `Traceback (most recent call last):\n` first.
2. Four ordered frames with literal registered canonical paths, literal line numbers/function names and exact displayed source statements below. Each frame consists of `  File "PATH", line N, in FUNCTION\n`, then four spaces plus the stripped source statement and newline. ROOT must freeze safe paths excluding quotes/newlines. After a source line, permit zero or one CPython diagnostic marker line containing only spaces followed by a nonempty sequence of `~`/`^` (at least one `^`), total <=512 characters. No other optional text, frames, exception chain, escape/color bytes, suffix or prefix.
3. Exactly the selected final ValueError line and final newline below.

Common first two frames: CHECK_PATH:416 `<module>` source `main()`; CHECK_PATH:412 `main` source `print(check_file(path))`.

Control6: CHECK_PATH:329 `check_file` source `col = residual_pair(sub(oper(h, avar, vvar), tvar))`; CHECK_PATH:216 `residual_pair` source `raise ValueError('upper band residual')`; final `ValueError: upper band residual\n`.

Control10 Psi, including fallback: CHECK_PATH:376 `check_file` source `eq([poly(v) for v in array(doc['rows']['Psi'], 3)], psis, 'all three Psi slots')`; CHECK_ARITH_PATH:208 `require` source `raise ValueError('CHECK FAILED: ' + label)`; final `ValueError: CHECK FAILED: all three Psi slots\n`.

Control10 K1/K0: CHECK_PATH:383 `check_file` source `eq(actual, low[i], 'literal retained K coefficient')`; same CHECK_ARITH_PATH:208 frame; final `ValueError: CHECK FAILED: literal retained K coefficient\n`.

This is full bounded grammar acceptance, not a substring search. Unexpected native formatting is a fail-closed STOP requiring separate ROOT decision, not permission to loosen after a failure. No arbitrary traceback from another location counts. The producer's canonical packet order and the unchanged fields allow earlier checks to pass conditional on the accepted positive baseline; the future executions, not this static argument, must demonstrate the stated negative branches. Psi comparison checks all three polynomials and need not change array length. Control6 residual_pair fails BEFORE the later explicit ell-column check.

## Scope

All controls NOT RUN. Source and contract require first independent static review and separately registered execution. A future receipt is not a scientific PASS; exact negative checker evidence remains separate. No source ideal/point/unit/degree/JC2 statement, new supervisor, middle-real authority transplant, or downstream authorization.
