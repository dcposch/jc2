# Symmetric common-quadratic contact discriminator — assignment, not a claim

Use ONLY the literal A_X,B_X equations in accepted17w and its qualified
FIRST gate, plus this card. Their pins are respectively
4bbecd357077b422fbe636de61c9feefc5b304d88eea0a872b476456b1fbdf30 and
4a46e7f605759016829fcfb8ad92586fa9ebe875c4b5a62835b603c8b0ab1e90.
Root previously read both WHOLE and has rechecked their current hashes.
The gate's incidental e_y Cramer sign is positive, not negative; the
u-normalization is only a field-point fact or explicit u-unit chart.
No unreviewed September10 geometry report, formula, or theorem is a premise.

Target remains the ENTIRE complex contact locus with
H=W*x*y*(x-1)*(y-1)*(3x-5)*(3y-5)*(2-x)*(2-y)
 *(y-x)*(x+y-3)*(4-x-y)*Gamma*Omega nonzero,
Gamma=36xy+9(x+y)^2-114(x+y)+181,
Omega=15(x+y)^2-36xy-30(x+y)+35.
Uniform emptiness would address late-contact rank, NOT the earlier forcing,
companion equations, source-family closure or JC2. A finite complex locus
would still need rational/integer specialization exclusion.

NEW REPRESENTATION TO TEST, not an accepted downstream premise:
s=x+y,p=xy, Delta=s^2-4p,
G=W*p*(p-s+1)*(9p-15s+25)*(p-2s+4)*Delta*(s-3)*(4-s)
 *(36p+9s^2-114s+181)*(15s^2-36p-30s+35).
Divide each of the two literal quartics 720*A_X and5040*B_X by
X^2-sX+p. Four remainder coefficients define R over Q[s,p,V,W,G^-1].
Test the exact finite-free etale rank-two presentation R[x]/(x^2-sx+p),
y=s-x, and the converse evaluation/difference identity with x-y a unit.
Preserve nilpotents and every divisor; this may simplify an eventual finite
elimination, without relying on the previous incomplete dimension argument.

Hand reduction identities to independently check:
X^2=sX-p; X^3=(s^2-p)X-sp;
X^4=(s^3-2sp)X+(p^2-s^2*p).
The X^3 coefficients of those monic quartics appear to be -14+30V and
-18+42V, so their difference drops to a cubic with leading4-12V.
This suggests a small Euclidean/subresultant route; V=1/3 MUST be handled,
not silently inverted. Any further leading-coefficient-zero divisor also
needs exact treatment. These suggestions are to be verified, not quoted
as a completed proof or an invitation to invert arbitrary expressions.

Primary target: a genuine whole-locus finiteness/emptiness proof, or an exact
positive-dimensional component retaining G!=0. If neither is achievable in
the cap, retain the exact four small equations and precise remaining
elimination predicate, clearly GAP. Do not replace a missing verdict with
another long abstract rank framework. No component discarded, no generic
count or one-point test elevated to a total-dimension theorem. An exhibited
complex curve need not meet the prescribed rational range5/3<x<y<2.

History/independence: accepted17w supplied the fixed-exponent cubic reduction;
the just-closed global collision attempt did not settle finiteness. This is
one representation change, not a repeat of its proposed generic-dimension
argument and not a dependent use of that unreviewed result. No corpus-wide
novelty claim. Existing code/static review remains a separate task.

Manual ONLY. No mathematical subprocess ANYsize, syntax/import/AST/compile/
test/CAS/coefficient-file generation, network/AWS/SSH/process inspection,
new agents, live code/ledger/report/log reads, corpus/shared/protected/Git
inspection or mutation. Own apply_patch documentary files and the existing
artifact_finalize transaction only. No runtime or follow-on authority.
