# Disabled prospective one-place rank contract

STATIC ONLY. No place, baseline, certificate, registration, worker, or runtime has been supplied. All four sources are UNEXECUTED and await independent review. No deployment wrapper is delivered.

## Conditional mathematics and representation

Use the accepted actual-r3 number FIELD B=Q[V]/P7 at tau=3/10, not a selected characteristic-zero component or an arbitrary degree-seven algebra. A genuinely ROOT-qualified complete source packet supplies all 25 graph slots, including zero and duplicate slots, with the literal interface in wire.py. Slot order is K8,K9,K10,A1_1 through A1_9,A0_1 through A0_12,T; weights are 8,9,10,19 through 11,22 through 11,27. Variables X1..X4 have weights 1,2,3,4.

Rows are all lexicographically ordered weight-30 monomials; columns are slot-major then lexicographic multipliers of weight 30 minus that slot's weight. Dimensions must be 297 by 1453. Finder builds the entire matrix, finds first available pivots in column order, and returns 297 distinct increasing column IDs with the square inverse. Checker independently enumerates the same complete universe, reconstructs entries by row-minus-multiplier lookup, and verifies every one of the 88,209 entries of M W=I, zeros included. It never imports finder.py or source arithmetic.

Both sides independently construct the literal monic P7 from its rational tau=3/10 definition, require exact equality to the packet's eight coefficients, and check exactly one ROOT-specified place. Parameter p is a decimal string representing a prime at most 2147483647. Phi is a dense ascending list of 2..8 canonical residue strings, monic, defining degree f=1..7. Both sides independently verify primality by bounded trial division, phi dividing P7 modulo p, and irreducibility using Frobenius and polynomial gcd checks. Every possible irreducible factor degree at most floor(f/2) is tested and X^(p^f)=X is required. No factor search, retry, alternate place, CRT or rational reconstruction exists.

All seven rational coordinates of every one of the 25 complete graph slots and g, including coefficients that later reduce to zero, must have p-unit denominators. The implementation conservatively also checks Hq,zeta,ell,U,c,c_inverse. Whole homogeneous support is checked before reduction. No denominator clearing or integral-basis change is used. The selected square inverse proves a nonzero residue minor. The accepted integral-model/rank theorem then gives surjectivity over the entire number field, and the accepted cone implication applies only after authentic full-source qualification and later evidence acceptance. The scripts do not mint this promotion.

## Exact entry interface and external authority

The effective vectors are:

    PYTHON -E -s -S -B /ABS/finder.py --registered-job f10-source-cone-r3-fullrank-v1 --authorization /ABS/AUTH.json --authorization-sha256 AUTH_SHA --output /ABS/CERT.json
    PYTHON -E -s -S -B /ABS/checker.py --registered-job f10-source-cone-r3-fullrank-v1 --authorization /ABS/AUTH.json --authorization-sha256 AUTH_SHA --input /ABS/CERT.json

Each script has exactly nine sys.argv entries. The four scientific siblings are authority.py,wire.py,finder.py,checker.py. Only authority.py imports before authorization, and then only os/sys. ROOT must authenticate all four bytes and the interpreter startup environment BEFORE launching Python; the local interlock cannot retroactively prove its own code authenticity or native startup closure.

ROOT must install exactly those four files in a separate source directory, with no documentation, metadata or shadow modules. The development box is intentionally not that deployment directory. The interlock checks this four-file listing and removes the sibling directory from module lookup before metadata-library imports, restoring it only after authority and all pins pass. Existing interpreted native/startup paths remain an external ROOT closure obligation.

Authority JSON has exactly schema,job_tag,authority,status,mode,full_argv,files,runtime,limits,expires_utc,baseline,artifact,receipt,place. Values are strings, arrays or objects, never native JSON numbers. Fixed values are r3-rank-authorization-v1, the job tag above, ROOT-CAPRUN, REGISTERED, and find/check. The full_argv array is the entire 14-entry effective vector with exactly one typed ROOT_AUTHORIZATION_SHA256 hole at zero-based index 11; only this value is expanded to the supplied authority SHA. This avoids a self-hash cycle. All other entries are literal. Actual /proc/self/cmdline must equal the expanded vector and argv[0] must literally equal sys.executable.

files maps exactly four sibling basenames to {path,sha256}. Absolute canonical source paths, ROOT-owned 0755 directory and ROOT-owned 0444 files are mandatory. runtime is {python_sha256,native_path,native_sha256}; the separate ROOT-owned native record is {job_tag,files}, where files is a nonempty canonical-path-to-SHA map. This bounded inventory check is not a native closure policy: ROOT separately supplies authenticated complete startup/native/alias/loaded-map evidence, clear environment, physical instance/boot/namespace/cgroup/UID/GID identity, outside-storage inputs, supervisors, deadlines and actual process-group cleanup. No worker identity or time is invented here.

baseline is {path,sha256,bytes,qualification_path,qualification_sha256}. ROOT-owned frozen baseline is bounded by 134217728 bytes. The qualification record must be genuinely issued after the existing independent complete source checker succeeds; an invented matching record is not evidence. Its exact keys/values are schema=r3-baseline-qualified-v1,authority=ROOT-CAPRUN,status=ROOT_QUALIFIED_COMPLETE_SOURCE,baseline_sha256,baseline_bytes,source_checker_sha256=b9176ee7f0b8f34506e0fce03c2aea548756d0d489a53b732401cc7dbaa8c87f,checker_exit="0",checker_stdout="CHECKED_R3_25_GRAPH_SLOTS_45_LOW_108_JACOBIAN_NO_SOURCE_OUTCOME\n". These last three values bind the accepted source checker, not a new rerun by this package.

artifact and receipt each have {path,sha256}. Finder artifact and every receipt require UNFORMED and absent paths. Checker artifact requires a fresh exact SHA and ROOT-owned 0444 freeze. Every path is absolute, canonical and distinct from protected inputs. Authority expires_utc is a ROOT-chosen future UTC Z timestamp. Actual roots/native files/input and output pins are checked before mathematical imports, whole readback and successful receipts. Input wire uses no-follow reads; outputs use exclusive/no-follow creation, 0444, fsync, directory fsync and hash readback. ROOT owns durable collection only after all groups are quiet.

## Wire, outcome, and cap contract

Canonical rationals are reduced signed numerator / positive denominator strings, with at most 4096 characters per integer and Python's process-local conversion bound fixed at 4300. Zero is 0/1. Native JSON numbers, bool/null/NaN/infinity, duplicate keys, noncanonical integer strings, wrong seven-coordinate lengths, unordered/duplicate sparse exponents and zero sparse terms are rejected. Baseline support bounds are 30,15,10,7 in X1..X4; each extracted polynomial is at most 20,000 terms, and total extracted graph terms at most 400,000. Wire traversal caps are depth 32, nodes 16,000,000, array length 400,000, object size 256, key length 256, string length 8193. These are conservative refusal limits, not claims that a future actual packet fits. Source/raw polynomial equations are NOT rechecked here: the exact frozen qualification is indispensable.

The certificate has exactly format,job_tag,baseline_sha256,qualification_sha256,place,dimensions,finder_sha256,columns,inverse. Format is r3-rank-inverse-v1; dimensions are ["297","1453"]. columns contains 297 distinct increasing decimal indices from 0..1452. inverse is 297 rows of 297 field elements, each a dense ascending fixed-f list of residue strings 0..p-1, including zero coordinates. No producer matrix, rank declaration, determinant, floating value or omitted identity coordinate is accepted. The checker binds the finder source pin for provenance but trusts none of its mathematics.

Certificate bytes are capped at 16777216 and metadata receipts at 32768. A receipt has schema=r3-rank-receipt-v1, job_tag,mode,status,science=NONE,authorization_sha256,baseline_sha256,qualification_sha256,place,certificate_sha256,files,runtime,details. All three modes of evidence remain external ROOT obligations: actual qualified baseline, actual checker execution with complete source/native/cap custody, and independent post-run acceptance. Hashes and this receipt alone do not establish those facts.

The finder exits 0 with stdout exactly RANK_CANDIDATE_UNCHECKED plus newline, empty stderr and complete certificate/receipt, or exits 2 with stdout NONDECISION_RANK_DEFICIENT plus newline, empty stderr, no certificate and a fully post-pinned receipt containing its found rank. Rank deficiency is never negative ideal membership. The checker exits 0 with stdout R3_FULL_ROW_RANK_CHECKED_NO_AUTOMATIC_SOURCE_PROMOTION plus newline, empty stderr and a complete receipt. Its successful receipt details include all 88,209 checked identity positions.

For either program, invalid place, nonintegral baseline, malformed input or internal memory/overflow cap yields exit 2, empty stdout, NO receipt, and exactly respectively NONDECISION: INVALID_PLACE, NONDECISION: NONINTEGRAL_BASELINE, NONDECISION: MALFORMED_INPUT, or NONDECISION: INTERNAL_CAP followed by newline on stderr. The checker mathematical identity failure is separately exit 2, empty stdout, no receipt, stderr exactly CHECK FAILED: full inverse identity plus newline. Authority refusal, filesystem/write failure, interpreter failure, external cap/signal or any unexpected exception is STOP, not one of these semantic conclusions. An artifact left by a failed later write/postcheck is partial and must not be accepted; no retry is authorized.

The frozen proposed limits are joint wall 600 seconds, CPU 550 seconds, memory 8589934592 bytes, baseline 134217728, certificate 16777216, receipt 32768; these exact decimal strings form limits. No supervisor enforces them in this package. Future ROOT must register coherent phase profiles and independent cgroup/CPU/RSS/wall/output caps before mathematical imports. Aggregate baseline, certificate, mutation, logs and archives need a separately checked storage budget; single-file bounds do not establish aggregate fit. All costs are UNMEASURED. A cap or failed place is nondecision; no automatic prime farm, fallback engine or cap increase is allowed.
