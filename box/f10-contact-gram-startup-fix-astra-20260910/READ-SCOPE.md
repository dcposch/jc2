# Exact read scope

Five pinned inputs only, all freshly SHA-matched before WHOLE text reads. Original contact authority/report/CONTRACT and old runtime CONTRACT/gate were read through EOF/marker/seal as applicable. Combined output clipped; old runtime CONTRACT was reread whole, gate in1..45 and46..end, and contact CONTRACT25..end recovered its tail. No provenance, original producer/checker body, coefficient/candidate/fixture, live report or runtime source was accessed.

WHOLE old/new authority comparison is byte/text only: exactly one line must differ by '-S'; no code parsing, AST, syntax, import, compile or test. All writes apply_patch, metadata hashing/publication only. No worker/native/process/network/agents/shared/protected actions. Own WHOLE/report/OPEN/collision and current pins precede marker/transaction; ROOT retains review and all runtime authority.
