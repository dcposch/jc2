# Highest-chart exact certificate: source-only prospective contract

STATIC / UNREVIEWED / UNEXECUTED. Only the actual packet's future literal pin is used; no coefficient body, prior mod89 coefficient table, result, worker or authority is present here. Final ROOT simplification: ONE identity cF+dG=1 is mandatory. No aP+bQ identity or Q inversion is required.

## Exact mathematical certificate

Independently extract ALL terms of the actual three rows rows.Psi[0..2], of weights5/6/7 in (X1,X2,X3,z) with weights(1,2,3,5). Before ANY projection, both implementations require no S/vartheta support, exact weight, six bounded canonical exponents, seven canonical rational coordinates, sorted distinct nonzero terms. Then discard only positive z-degree terms and put X1=1, u=X2, v=X3. This is NOT the old q1 chart. The shapes forced by weights are

    H5=P(u)+Q(u)v,
    H6=B3(u)+B1(u)v+B0 v²,
    H7=C3(u)+C2(u)v+C1 v²,

with u-degree bounds(2,1), (3,1,0), (3,2,0). Zero polynomials and degree drops are allowed. Define

    F=Q²B3-PQB1+B0P²,
    G=Q²C3-PQC2+C1P².

Both have degree<=5. The following identities hold over ANY commutative coefficient ring, with no division:

    F=Q²H6-H5[QB1+B0(Qv-P)],
    G=Q²H7-H5[QC2+C1(Qv-P)].

Consequently cF+dG=1 gives the literal polynomial cofactor expansion

    1=(cQ²)H6+(dQ²)H7
      -{c[QB1+B0(Qv-P)]+d[QC2+C1(Qv-P)]}H5.

This covers Q=0 as well as Q!=0, including Q identically zero. It does not require a selected field embedding, factor, denominator fibre or separate P/Q coprimality. Nonunit gcd or a rejected certificate does not prove a common zero: this is a sufficient certificate instrument, not a completeness theorem for arbitrary H5/H6/H7 emptiness.

The accepted X1=0 origin-only theorem (charged final infinity gate, only its endpoint consumed) plus a FUTURE checked identity for this ACTUAL baseline would leave only the origin for the homogeneous forms. Over the accepted field B this supplies the parameter-sequence prerequisite of the charged cyclic-core theorem: grR equality, dimension35 and seven per cyclic class. This would unlock, not perform, that theorem's actual basis/multiplication/7x105 membership task. No full-source ideal zero/point, finite-basis certificate, bound-196/all-r theorem or JC2 conclusion follows from this identity alone. The baseline is still checked-but-not-promoted until its separately selected semantic negatives are accepted.

## Code separation and bounds

entry.py is metadata/strict-JSON infrastructure only. It imports only os/sys before the Linux/exact Amazon EC2/fixed-argv interlock. It never authorizes a worker or authenticates ROOT. There are no mathematical routines shared between producer and checker.

produce.py explicitly loads the unchanged pinned producer arithmetic.py AFTER the boundary. It re-forms the literal monic P7 from the accepted tau2/7 formulas, compares all eight coefficients to the actual packet, validates the full seven-coordinate basis/interface, extracts sparse chart coefficients, builds F/G, and performs bounded extended Euclid over B[u]. Every inverted leading coefficient passes the existing exact whole-B Euclid/product test. Nonunit, zero/nonconstant gcd, unsupported shape, resource or size limit is INCONCLUSIVE/STOP; no field factor is selected. At most8 Euclid divisions; intermediate u-degree<=16; post-operation numerator/denominator bit lengths<=32768. These are refusal bounds, not measured sufficient resource predictions. Intermediate arithmetic inside the unchanged module remains subject to external CPU/RSS/wall limits.

check.py loads ONLY unchanged check_arithmetic.py. It does not import produce.py or producer arithmetic, call Euclid on F/G, trust producer F/G, or consume producer-extracted forms. It re-forms literal P7 by its independent rational-polynomial implementation and independently extracts the WHOLE three sparse rows into a dense v/u coefficient box. It reconstructs F/G, parses c,d, and checks ALL11 u-powers by ALL7 rational coordinates (77 fixed equality positions), including zero slots, of cF+dG=1. This is exact reduction modulo the complete literal degree-seven modulus, not a chosen factor or evaluation. The full artifact's other rows/units/source reconstruction are bound by the baseline SHA and prior accepted checker evidence; this instrument does NOT replay that whole source checker.

Input packet exactly1652675B/SHA dac655a7ce75287b06010171bce66af1a644aff81de9620a452457a5f65a9587; maximum reader15MiB. Each of three sparse rows<=4096 terms, each exponent0..16; strict duplicate JSON and numeric-token rejection, canonical reduced fraction strings, positive denominator, numerator/denominator strings<=4096 characters, process-local conversion ceiling4300. No coefficient float. Certificate<=2MiB; c,d dense ascending u-coefficient arrays of at most6 complete seven-vectors, no trailing zero vector; zero polynomial[]. Every field coefficient has canonical num/den strings. Final bound5 on each cofactor contains the usual coprime degree<=5 Euclid output over the accepted field; exceeding it is still a typed failure, never a truncated certificate. Checker product degree<=10 and32768-bit post-operation cap. No arbitrary scalar truncation.

## CLI: disabled without future ROOT registration

Each mode is a distinct entry script. Future vectors are

```
PYTHON -E -s -S -B PRODUCE --registered-job f10-r2-highest-bezout-v1 --root-registration-sha256 ROOT_SHA --self-sha256 PRODUCE_SHA --entry-sha256 ENTRY_SHA --arithmetic ORIGINAL_ARITHMETIC --baseline BASELINE --certificate FRESH_CERT --receipt FRESH_PRODUCER_RECEIPT
PYTHON -E -s -S -B CHECK --registered-job f10-r2-highest-bezout-v1 --root-registration-sha256 ROOT_SHA --self-sha256 CHECK_SHA --entry-sha256 ENTRY_SHA --arithmetic ORIGINAL_CHECK_ARITHMETIC --baseline BASELINE --certificate FROZEN_CERT --receipt FRESH_CHECK_RECEIPT
```

Exactly17 sys.argv entries; ordered flags; no supplied pin may be missing/zero. ROOT freezes an isolated script directory containing EXACTLY entry.py,produce.py,check.py and no shadowing modules; original arithmetic files remain unchanged at separately pinned absolute paths. The -E -s -S -B vector retains that isolated sibling import path, unlike -I. No original five-file science directory is modified. Neither the old nine-argument authority nor authorize('mutate') is used. The root/self/entry CLI hashes are bindings only; same-user code cannot authenticate ROOT by hashing caller-provided strings.

ROOT must separately authenticate registration, pin scripts BEFORE import (entry.py is already loaded when its own hash is checked), freeze input/source/certificate between phases, validate exact paths/ancestry/interpreter/native closure and commands, enforce an unchanged capped runner/quiet/custody policy, and bound joint outputs. No runtime/worker registration is emitted. Use no larger than the existing600wall/500CPU/2GiB/16MiB joint envelope; exact per-phase profiles and original wall deadlines must be separately registered, never inferred from this source. Baseline and immutable source/native data outside the output partition; only one candidate and two small receipts, no general harness. No performance estimate or automatic retry.

Entry rechecks source pins and baseline before parsing, before publication, and in final receipt publication. Cert and receipt are O_EXCL/O_NOFOLLOW, flushed/fsynced0444 with parent fsync and byte readback. All paths distinct/canonical. ROOT's external freeze closes inter-read TOCTOU. Any partial write/missing receipt is incomplete, not accepted; no deletion or overwrite. Certificate producer and checker use separate fresh receipts. Root checks the exact certificate hash at launch and again after terminal; checker also performs its own pre/post-byte equality.

## Certificate and receipts

Exact certificate keys: schema='f10-r2-highest-bezout/v1', baseline_sha256, chart='z=0;X1=1;u=X2;v=X3', modulus (eight rational strings), c,d. Canonical ASCII sorted compact JSON plus one newline; ALL numbers in the certificate are rational strings or implicit array indices. No F/G claims are serialized.

Exact receipt keys: schema='f10-r2-highest-bezout-receipt/v1', status, science_outcome='NONE', mode, root_registration_sha256, sources{absolute self/entry/selected arithmetic path:SHA}, baseline_sha256, baseline_bytes='1652675', certificate_sha256. Receipt<=32768B.

Producer NORMAL_EXIT0 with empty stdout/stderr and status CANDIDATE_UNCHECKED means only candidate bytes formed. Candidate-stage ValueError yields INCONCLUSIVE_NO_CERTIFICATE receipt, certificate_sha256=null, exit2 and no candidate. Other failures/resources are STOP, never a conclusion. Checker NORMAL_EXIT0 with empty streams and HIGHEST_CHART_IDENTITY_CHECKED_NO_SOURCE_OUTCOME receipt is only exact certificate verification. Invalid witness gives uncaught ValueError/NORMAL_EXIT1 and no success receipt. Root must combine actual byte-bound evidence and accepted theorem interfaces separately; the code does not promote it.

## One meaningful prospective witness mutation

After an actual positive certificate is independently checked, independently reconstruct F/G in the registered control process. Choose c if F!=0, otherwise d (G must then be nonzero because cF+dG=1). Add1 to its constant coefficient's coordinate0, inserting/removing a trailing zero vector only to maintain canonical dense encoding. Change NOTHING else. The identity changes by nonzero F or G in the entire seven-coordinate algebra. The fresh checker must fail with final stderr line exactly `ValueError: CHECK FAILED: full B[u] Bezout identity\n`, exit1, empty stdout and no success receipt. Bind original/mutated SHA, exact coefficient and inverse restoration. A parser/pin/resource failure or change to the cofactor multiplying a zero eliminant is NOT this control. No mutation code or fixture is executed/emitted here. ROOT separately reviews/registers that one control; no test farm.

Changed-hypothesis checks by hand: H5=P with Q=0 is still covered if cF+dG=1; dropping the H5 correction term in either displayed identity leaves uncancelled v terms; degrees/weights alone cannot prove F/G coprime; a nonconstant gcd is not a source point. Zero rows/constant eliminants/degree drops stay represented; no generic coefficient division or hidden chart omission.
