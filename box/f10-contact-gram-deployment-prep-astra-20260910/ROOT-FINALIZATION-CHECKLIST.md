# ROOT finalization checklist — disabled, provisional

This packet is metadata preparation only. The contact runtime FIRST review was
pending when preparation began. ROOT subsequently supplied a lifecycle-only
notice: FIRST intake completed at 20:55 UTC, A–D/F confirmed and E fail-closed
confirmed with UNMEASURED negative-check liveness risk. No new report was read
or added to the nine inputs. Exact frozen profiles remain unchanged; a timeout
is STOP, never a control pass. Source code, equations and original evidence
remain unchanged. There is no worker selection, registration or launch.

## Offline blockers before allocation

1. ROOT has reported FIRST runtime intake complete at the scope above. ROOT
   retains its receipt/pins/WHOLE; this author does not consume that report.
   The charged code gate itself is not a runtime gate.
2. Validate this template against the frozen dispatcher, especially all 14
   literal CAPRUN/setpriv/Python vectors. No ROOT-registration-token expansion,
   R2 entry wrapper or six-field native validation is imported.
3. Resolve a separately reviewed external-supervisor plan for the full owned
   cgroup: 550 CPU seconds, 600 wall seconds, 2147483648-byte memory maximum,
   including dispatcher and otherwise unaccounted/unreaped descendants.
   Specify the budget start boundary, terminal TERM/KILL/quiet observation and
   durable-custody margin. These are OPEN external settings, not implemented by
   this packet. Per-process RLIMIT_CPU, sampled RSS and RUSAGE_CHILDREN do not
   prove the full aggregate bound; CPUQuota is not a total CPU-time limit.
   No allocation while this offline design blocker remains unresolved.
4. Freeze one shared JC2_BATCH_SUFFIX_PLACEHOLDER for every installed/work/
   durable path, main unit, cgroup, TERM/KILL/worker-stop timer, harvest and
   cancellation reference. Do not mix historical suffixes.

## Future physical finalization — after allocation, before science

Observe exact instance/Owner, host, boot, PID namespace, interpreter/setpriv
canonical paths and hashes, selected nonroot UID/GID, EBS ext4/xfs device,
and native closure under separate ROOT authority. Replace UID/GID and
durable_device placeholders with JSON INTEGERS, not digit strings. Replace
UTC placeholders with explicit +00:00 dates, with original mathematical
deadline before task deadline. Never retain historical physical/native values.

Authenticate the referenced collector's entire observations independently;
freeze the bounded FILES-only manifest and all required source/executable
pins. Check root:root 0755 readable ancestry and 0444 source/metadata files,
canonical paths, and all stage/installed hashes. Resolve every placeholder
before separately authorizing metadata setup. No source copies exist here.

Keep source, native and ROOT registration outside the dedicated physical
16MiB tmpfs. Inside it, authority/frozen are root0755; writer is exact science
UID/GID0700. Baseline certificate is GENERATED there, never preinstalled.
Baseline, both mutants, ALL child/runner streams, authorities and metadata
share this same hard filesystem budget; individual file bounds do not add
another budget. ENOSPC or failed custody is STOP. Durable replicas reside in
a separate absent registered EBS target; they are not extra scientific outputs.
Verify actual durable_device integer and absence before dispatch; retain parent
directory fsync/terminal durability checks as ROOT duties.

## Release and exact sequence

Only ROOT may issue a NEW immutable enabled registration after all acceptance,
physical/native, source, absolute-clock, supervisor and output-absence checks.
Registration does not contain its own hash. The external installation manifest
may pin finalized registration bytes; per-operation authorities are freshly
written and hashed BEFORE launch by the unchanged dispatcher. Future original
certificate/receipt/mutant hashes are obtained by its freeze path, not guessed
or placeholder slots in registration.

Exact sequence: refuse-disabled, refuse-wronghost, refuse-argv, refuse-caps,
refuse-missing, valid, startup-producer, startup-checker, dummy, produce, check,
mutate, negative-uphi, negative-bernstein. Fixed profiles wall/softCPU/RSS:
preflight 5/3/2GiB; dummy 5/3/32MiB; produce 400/359/2GiB;
check 90/89/2GiB; mutate 10/9/2GiB; negative 30/29/2GiB.
Preflight aggregate wall is 180; dispatcher science accounting remains
600/550 with 15-second admission margins, unchanged and not a strict external
full-cgroup guarantee. No cap increase, retry, extra child or fallback.

Science/probe startup is -E -s -S -B; dispatcher/CAPRUN is -I -S -B.
Use the exact registered resolved Python argv, source cwd and environment
PATH=/usr/bin:/bin, LANG=C, LC_ALL=C, TZ=UTC, with LD_PRELOAD,
LD_LIBRARY_PATH and PYTHONPATH empty. ROOT verifies the actual startup/native
contract and main cgroup memory limit; no inherited site or caller assumption.
Independent stop/TERM/KILL timers must be active before release, with the same
batch suffix. Their actual UTC values and reviewed supervisor invocation are
unresolved ROOT launch settings; setup.template.sh deliberately does not launch.

## Outcomes, collection and limits

A complete producer artifact, including its normal-2 INCONCLUSIVE form, goes
to the SAME one independent checker. A checked sign-inconclusive receipt ends
after 11 calls as CHECKED_INCONCLUSIVE_NO_SOURCE_OUTCOME; no mutations run.
A positive checker receipt is frozen before mutator input authentication.
Both mutants are frozen before either negative checker. Full 14-call completion
is CONTACT_CERTIFICATE_CHECKED_NEGATIVES_CONFIRMED with science NONE.
Structural/parser/authority/native/resource/late/cleanup failure is STOP, not
a mathematical negative. No source or JC2 outcome follows.

Capture actual caller/CAPRUN/child identities when observable; retain honest
terminal-only evidence if already finished. Require exact groups quiet before
result reads or terminal durable copy. Hash all retained bytes before body
collection, verify replicas and ROOT parent fsync, then own worker lifecycle
and exact-timer cancellation. Never delete or unmount while custody is pending.
All future execution and its review remain separate from this offline packet.
