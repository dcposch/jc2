# Read scope and provenance boundaries

All nine inputs in PINS.json were freshly current-hashed before any body read;
all nine were then read WHOLE as text/metadata. The long dispatcher and old
registration were read in consecutive complete ranges. The first action and
target-absence check was 2026-09-10T20:50:12+00:00. There is no prior-WHOLE-only
substitution or claimed coefficient read. Selected dispatcher/setup ranges
were subsequently reread to check exact paths, commands, field types and setup.

Input PINS.json was read only as metadata; its provenance references were NOT
followed. Installation references and their literal source SHA values are
derived from charged texts. No fresh read/hash/copy of those scientific,
authority, probe, mutator or CAPRUN bodies was performed. Native collector
is one of the nine whole documentary inputs; it was not copied or executed.
No baseline, candidate, fixture or coefficient artifact was read, hashed or
parsed. No pending FIRST runtime report, other-lane body or external resource
was accessed. ROOT's later accepted-runtime lifecycle notice was recorded as
an instruction/status delta only, without a tenth documentary input.

Own registration/inventory JSON construction and readback are metadata-only;
no dispatcher, setup or scientific source was imported, compiled, syntax/AST
checked, tested or run. All authored content was written through apply_patch.
The existing artifact_finalize workflow is used solely for metadata publication.

The old actual middle registration/setup supply the matching interface pattern,
not worker IDs, devices, hashes, paths, UTC values or native facts for this task.
The R2 native/checklist inputs do not supply the R2 runtime schema here.

Final current-input hashes and owned-file hashes are recorded in custody.json.
The transaction manifest separately authenticates the final report. No source
or registration self-hash is embedded in its own hashed bytes.
