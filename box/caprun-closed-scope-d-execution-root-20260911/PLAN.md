# One tested-transport strict-nine qualification, batch d

ROOT selection September11 18:56:26UTC. Batch-b/c and their clocks remain
CLOSED. Neither tag replacement nor a larger cap is the reason for this
selection. The earlier c failure was ROOT's untested ASCII decoding and
multi-call candidate transport. The new supplied-input caller now has
CAPRUN-CALLER-1 acceptance after Fable FIRST and ROOT's identical170-expectation
replay. The exact real-caller -> one functions.exec patch/readback flow has
actually reproduced14 files/78889bytes in365ms. This supplies a measured
changed-transport basis, not an unmeasured promise that the entire120second
host workflow fits. Further transport hardening or echo review is off.

ONE new bounded qualification is selected because actual r3 source rows
remain the nearest source-attached decision in the1100 synthesis. The
qualification itself contains exactly the existing nine preflight outcomes,
no successful source producer/checker and no mathematical conclusion.
All-F10 would still be insufficient for JC2.

Original clocks in ORIGINAL-CLOCKS.json never reset: offline reserve19:10,
HARD19:13; at most ONE c7i.2xlarge with100GiB retained root EBS, allocated
only after complete offline qualification and fresh quota/fleet checks,
never after19:16. Actual setup/latest holder19:30; SAME holder120second
monotonic cap and absolute19:32, admission19:32:15, outer19:38 TERM and
19:38:05 KILL, custody19:43, exact-ID coordinator retirement19:48 or earlier.
No worker exists at selection; arm retirement immediately upon exact-ID
allocation, before actual host setup. No idle worker is reserved for review.

ROOT must complete source/native/physical/ACL/exclusive-namespace checks,
authenticate native LIST versus inventory roles, and read all prepared text
BEFORE holder. Prepare seven non-probe authorities then. Only actual holder
PID/start/invocation, actual outer/nine-leaf device/inode identities, freshly
checked timer receipt, explicit ROOT metadata decision and descendant hashes
remain late. Use the accepted fixed caller and pretested one-call staging
flow; no live improvisation of decoding or command serialization. Candidate
data and SUMMARY remain together. Full final bytes/diffs/pins must be checked;
ROOT separately owns actual liveness, bounded installation, and explicit
FIFO release. No helper observes or authorizes itself.

Keep every source/runtime/dispatcher/probe/mutator algorithm, phase predicate,
guard, CPU origin0, resource limit and cleanup condition unchanged. The
nine phases remain refuse-status, refuse-caps, refuse-inventory,
refuse-source, refuse-hash, valid, startup-produce, startup-check, dummy.
Strict success is CLOSED_SCOPE_PREFLIGHTS_9_CHECKED_NO_SCIENCE. The holder's
sentinel prefix is not a test phase.

Stop on original preparation deadline, missing qualification, unexpected
bytes, cap/refusal/phase/custody failure. No replacement worker, second holder,
live source repair, cap increase or automatic retry. Archive/compare/fsync/
download/hash-check/fsync terminal evidence before exact retirement; retain
EBS as backup. No retained evidence or separately owned infrastructure deletion.
Even success requires the existing different-model actual-evidence gate
before ROOT separately selects any necessary-row producer/checker.

Astra owns only the exact nine-file administrative port under its TASK.
ROOT owns this plan/clocks, remaining preallocation bindings, actual external
operations and all gates. Current ownership remains in notes.md.
