# Disabled two-refusal startup map observer

STATIC / UNEXECUTED. FIRST different-model instrument gate and a separately frozen ROOT registration are required. This package is not registration, worker authority, a repaired native closure, or a scientific result.

## Exact execution boundary

The only caller labels are refuse-status followed by refuse-caps. Each invokes the unchanged probe.py in authorize mode. The probe imports only the unchanged authority module from the original exact five-file science directory; it does not import arithmetic/produce/check. Status DISABLED and CPU-limit499 are the two existing negative authority mutations. Expected outcomes are NORMAL_EXIT/child_returncode1, no output payload, empty stdout and respectively the exact stderr lines `REFUSED: no registered ROOT CAPRUN contract` and `REFUSED: registered limits/source inventory`, each newline-terminated. Other outcomes STOP; there is no next-label retry.

No positive sentinel, startup-produce/check, dummy, mutator, baseline, fixture, semantic checker or other preflight is dispatched. The unchanged probe source still contains its old dummy branch, but the new caller constructs only authorize mode and requires equality to exactly two registered full command vectors. No inherited class or source execution is used to derive the new copy.

Outer vector is Python `-I -S -B` dispatch.py `--registration` ROOTFILE. Each CAPRUN vector is Python `-I -S -B` CAPRUN followed by wall5/cpu3/rss2147483648/sample0.05/grace1, exact stdout/stderr/telemetry paths, science cwd, and the unchanged setpriv uid/gid/clear-groups/no-new-privs vector. The child is Python `-E -s -S -B` probe.py `--science-dir SCIENCE --mode authorize -- --registered-job f10-r2-reconstruction-v1 --authorization AUTH --authorization-sha256 SHA --output PAYLOAD`. ROOT must precompute the two exact emitted authority hashes and full vectors; no placeholder expansion exists. The distinct outer job is f10-r2-native-map-observer-v1, schema f10-r2-map-observer-runtime/v1. The inner job remains the unchanged authority's existing tag.

## Pins, isolation and budgets

ROOT supplies all null values in a NEW registration, never edits this disabled template. Required roles are Python, setpriv, CAPRUN, this dispatcher and the byte-identical probe. Pin each absolute role, original five science files and the native manifest. The science literal hashes and CAPRUN literal are unchanged in code. Wrapper inventory is exactly dispatch.py/probe.py; science inventory remains exactly authority.py/arithmetic.py/produce.py/check_arithmetic.py/check.py. Both are root-owned0755, code0444; Python/setpriv retain their existing native-mode exception. All code/native/registration paths remain outside tmpfs. Six-field native closure, paths, aliases, absent ZIP and directory inventories are unchanged predicates. Fresh exact instance/host/boot/namespace/cgroup, memory.max, outer actual argv, environment, root traversable ancestry, fd/output-mount identity and private science uid/gid remain mandatory.

Absolute outer maximum remains600 wall/500 CPU/2GiB/16MiB, with sampled480-CPU cutoff, original absolute deadlines and15-second per-child admission/return margin. Two profiles total10 child-wall seconds and8 hard-child-CPU seconds under inherited soft+1 CAPRUN convention. These are ceilings, not measured runtime or strict real-time guarantees. The existing monitoring sleep is unchanged; there is no new delay or favorable-identity sampling loop. No cap escalation or worker operation exists.

All observer output is metadata and must fit the existing1MiB metadata reserve inside the16MiB tmpfs. The unused writer/frozen directories remain required and empty; they preserve layout compatibility but are not source/fixture storage. Diagnostic record maximum65536 bytes, including full JSON snapshot; current working bytes plus record are checked before exclusive creation. Outer streams and all per-label streams stay on the same mount. ROOT provisions a separately absent ext4/xfs durable path; durable replicas are not an additional science output budget.

## Strict observation semantics

identity adds capture-start/capture-finish UTC fields around the same sequential /proc reads; it does not claim atomicity. check_maps retains the same ordered canonical-file-membership and alias-membership rejection. On the first rejection it writes the exclusive fixed path map-rejection.UNVALIDATED.json, fsyncs it and its parent, then raises the same rejection reason. Exact keys: schema, status, science_outcome, root_registration_sha256, native_manifest_sha256, label, phase, role, rejection_utc, reason, snapshot, first_rejected, non_atomic_proc_reads, validation_passed. Status is UNVALIDATED_MAP_REJECTION, scienceNONE, validation_passedfalse. Snapshot contains PID/PGID/start/boot/namespace, argv, mapped paths and the two observation times alongside existing identity metadata. first_rejected holds raw_path/resolved_path/file_member/alias_required/alias_member. It is null for missing identity, whose snapshot is null and result remains STOP.

This evidence is explicitly unvalidated while the process is live, not an accepted runtime result. No file is added to native closure, no unknown mapping is ignored, and no observed pathname is presumed ahead of the run. Oversized record or exhausted metadata budget raises a typed STOP rather than truncating a record into apparent completeness. A write/resolve/deleted-map/proc exception also remains STOP and may leave the detailed observation unavailable; no repair or retry follows.

Initial revalidate/group_quiet moves from the tail of Batch.__init__ to execute, after main has retained the fully initialized object; it still precedes all authority/child dispatch and now allows durable custody of an initial self-map failure. Runner map validation remains immediately after Popen and before runner-live emission. Missing runner identity fails, not silently skips. The child's FileNotFoundError handler now surrounds only reading the children listing; it cannot swallow observation-write errors. Both actual runner equality and an actual probe live identity are required before a run can count as complete. A fast refusal can therefore produce STOP: probe live identity unobserved; no retry, not a falsely complete observation.

## Terminal evidence and scope

On map rejection the existing finally attempts runner termination/wait; main invokes unchanged quiet-before-durable-copy logic. If owned groups remain live, custody/write/deadline fails or initialization did not establish custody paths, result is typed STOP/STOP_CUSTODY_INCOMPLETE with ROOT recovery needed. No dispatcher deletion, unmount, worker stop or output reads before required quiet. Fixed observation file is automatically included in durable inventory and copied byte-for-byte/hash-checked/fsynced whenever custody can complete. The immutable namespace/read-only files remain no-concurrent-writer assumptions, not protection against malicious root.

If both exact refusals and live identities complete without map failure, terminal status is DIAGNOSTIC_NO_MAP_FAILURE_OBSERVED with scienceNONE. This is not a repaired-closure claim or permission to rerun semantics. Otherwise STOP remains terminal. ROOT reads only after all writers/cgroups are terminal and decides any next action. No current native culprit is established by this package.

## Pending checks

One FIRST source/instrument delta review; zero syntax/import/runtime tests performed. Reviewer should trace the two-label reachability, full literal argv/authority hashes, first-failure record and cap paths, the tightened exception scope, early initialization custody, absence of semantic dispatch, and final incomplete-custody status. One separately ROOT-registered observation is the cheapest runtime check after review; no reproduction or fixture batch is authorized here. Live-probe capture, diagnostic filesystem writes and durable custody costs are unmeasured.
