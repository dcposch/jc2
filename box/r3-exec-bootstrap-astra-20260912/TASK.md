# One implementation: same-process closed-scope bootstrap

ROOT author interface, September12,2026. INTERNAL/UNREVIEWED design;
implementation may proceed as reversible local editing. NO worker, runtime,
enabled registration, positive authority or execution is authorized. All old
a/b/c/d attempts remain CLOSED. This is a changed mechanism, not a renamed
retry or cap increase. Original reserve16:09UTC/HARD16:12UTC includes
publication/custody, NEVER RESET. Stop with exact incomplete items if needed.

First record actual UTC and check owned targets absent. Fresh current
SHA-before-WHOLE reads, in order, of exactly five inputs:

1. COORDINATION.md,
   33cfa6106972fe557a95ca81a9a05bb72300a82b2fbb7908915441cf75377597.
2. This TASK.md, ROOT's separately supplied SHA.
3. box/f10-necessary-rows-producer-astra-20260911/authority.py,
   804c7aff56538fc332bf68638b03b7167eccdb49f73337f1a5f1823348b522a4.
4. box/caprun-closed-scope-implementation-astra-20260911/dispatch.py,
   52b3d916f4fc3941bb98c8b4f7698ab940680d09ee4e065de1f167857e3fd436.
5. box/caprun-closed-scope-implementation-astra-20260911/CONTRACT.md,
   af7a2fab4aa6317f7386e438f2f03acc6daffdd04c3deee6a6554b675089ee81.

No other scientific/runtime source, corpus, old peer body, external text,
protected tree/mirror or live report. All four original closed-child runtime
snapshots are statically accepted in AUDIT CAPRUN-CC-1 at the contract's
boundaries. That imports no execution, installed-native or live-host result.
Do NOT inspect/change the closed-child runner; it stays d78d45ef... as pinned
in the literal dispatcher. Generic ops/run_capped.py is NOT a fallback.

## Deliverable and chosen mechanism

Write ONE owned bootstrap.py and ONE CONTRACT.md, plus the normal sealed
report/custody. No framework, old-card port, science source edit, new runner,
provisioner, holder, service launcher, deletion, installer or controller farm.
The code is for future AWS-only qualification; do not execute or import it.

ROOT's WHOLE static read shows: scientific authority has no holder/PID field;
dispatcher outer_argv is just python -I -S -B dispatch.py --registration PATH,
with no expected PID or registration SHA on that argv. Preflight authority
hashes depend on static source/native/contract/place fields, actual leaf
identities for valid/dummy, and the fixed registration PATH, not its SHA.
Thus the preflight registration can be built acyclically before exec.
Check these claims yourself against the supplied exact source; mark any
counterexample rather than silently changing the dispatcher.

Mechanism: ONE trusted ROOT process, already in its genuinely registered
outer systemd cgroup under independent fixed caps, observes late facts,
creates the nine fresh closed leaves, builds/freezes one registration and
an external binding receipt, then execve's the SAME pinned Python into the
UNCHANGED dispatcher. No surviving bootstrap parent, child dispatcher,
human transfer, FIFO, secondary holder, PID replacement or service restart.
The dispatcher remains ROOT. Only CAPRUN's workload becomes unprivileged.
Its group_quiet predicate therefore sees only itself in the outer group.

The input is ONE externally approved, canonical, immutable ROOT-owned
hash-pinned launch specification whose CLI includes its expected SHA. Define
the smallest exact JSON schema needed in CONTRACT; no supplied sample may
pretend to be enabled, bound to a host, or runtime-qualified. It may contain
a disabled preflight registration skeleton with fully specified static
paths/source/runtime/native/contract/place/cap/deadline/review data. These
are supplied later from genuine ROOT qualification, never invented here.
Use a tightly enumerated dynamic field set, not recursive token substitution.
Input SHA and code approval originate OUTSIDE this process; neither an
enabled flag nor self-hashing a JSON document is authentication.

Known before launch: exact host/instance/boot/namespaces and expected cgroup
PATH predicates after separate physical setup; all static files/native
closure and FIRST bindings; all paths; exact environment/UID/GID; original
absolute binding/admission/mathematical/task/retirement deadlines; exact
preflight-only mode, labels, source limits and profiles. Actual outer/leaf
device+inode facts are read inside this process, never guessed.

Before any side effect, require Linux/EC2/ROOT, canonical pinned input/code/
dispatcher, exact expected host/boot/PID+cgroup namespace and unified root
mount, correct outer membership, identity and effective resource conditions.
Retain trusted exclusive ROOT/no replacement/entrant/delegation/ACL-write
assumptions explicitly. The supplied launcher must be independently reviewed
and qualified; this small script is not a provisioning/security sandbox.
Observe current root mount/topology rather than assuming root namespace.

Create ONLY the exact nine direct phase cgroup leaves, each absent initially;
refuse any existing leaf/unexpected child, wrong domain/subtree_control,
write/delegation permission or unexpected outer member. Bind dev/inode and
empty kernel population for every leaf; recheck before exec. Preserve all
closed-child runtime obligations. A partial failure retains evidence and
requires ROOT cleanup; never delete/retry or launch a fallback.

Use aggregate_cpu_start_usec='0' (conservative origin of the fresh owned
cgroup) so bootstrap CPU is not silently omitted. Require and preserve all
fixed absolute deadlines; do not mint now+budget at release. Binding has
at most120seconds and a predeclared absolute stop, BOTH checked. Independent
outer watchdog/retirement must already cover the process and descendants;
its clock cannot reset at exec. State these external requirements literally.
No claim of a hard aggregate CPU or realtime bound from sampled accounting.

Generate the EXACT nine expanded command arrays by independently matching
dispatch.run/authorize. Reconstruct canonical authority bytes in MEMORY to
compute expected digests; do not preinstall those authority files (they are
dispatcher outputs and auth/frozen/writer directories must initially be empty).
No typed future hash slots are allowed in PREFLIGHT_ONLY_9: typed_slot_policy
must be {}, phase_policy null, and FULL_13 must be rejected. Source/place
mathematical integers stay strings with no arithmetic/float conversion.
Retain source schemas, source pins and literal negative expectations.

Registration bytes are serialized once with explicit UTF8/canonical JSON,
bounded size, O_EXCL/no-follow creation, fsync,0444 and whole byte/pin readback;
fsync parent. No preexisting output overwrite or replacement. Binding receipt
is separately exclusive and binds input/code/registration SHA plus observed
facts, original clocks and bootstrap identity. Do not place its own digest
or registration digest inside the registration. Preserve all source/native
pins and the complete native qualification; the dispatcher's independent
revalidation is not permission to skip required prelaunch qualification.

Before exec, retain original stdio binding to the capped working tmpfs,
set exact ENV and wrapper cwd, close bootstrap-only descriptors, revalidate
the immutable observations and deadlines, and exec literal outer_argv.
Successful exec replaces this process; it does not fork or reset external
clocks. It is NOT a successful preflight. Only the unchanged terminal nine-
phase result/custody after kernel quiet is the runtime discriminator.

## Honest qualification plan

This is UNTESTED code; no syntax/import/test/dummy/CAS run locally. One
different-model FIRST of this concrete code/contract plus exact dispatcher
interface must precede any AWS test. Do not commission a separate prose-only
FIRST or recertify accepted math. Name the first bounded AWS-only positive
nine-phase test and a minimal negative-control matrix for stale identities,
wrong pin, preexisting outputs/leaves, wrong mode, expired clock and refused
exec. No authority is granted for that future test by this task.

No timing/speedup/cleanup/source outcome is established by code review alone.
If full static correctness needs unavailable sources or live facts, separate
required INPUT/PRECONDITION from actual evidence. If the design cannot avoid
a literal cycle or weakens a guard, deliver GAP instead of a speculative
executable labeled ready. Prefer a compact code unit over duplicated audits.

## Tool and publication boundary

Allowed only inert UTC/text/hash/own-target absence/byte counts, own
apply_patch writes and unchanged administrative artifact_finalize transaction.
No scientific/helper/import/AST/syntax/test/dummy/fixture execution, network,
AWS, other agents, process control, shared edits, staging, commit, deletion
or protected operations. Read all owned code/contract/report WHOLE before seal.

Owned outputs: box/r3-exec-bootstrap-astra-20260912/{bootstrap.py,CONTRACT.md,
PINS.json,custody.json}; xmodel/r3-exec-bootstrap-astra-20260912.md and generated
manifest via begin/leased partial/marker-last close/finalize/expected VERIFY.
Pin all inputs and outputs, preserve absence/exclusivity, report exact read
scope and actual clocks. Final whole report/manifest/custody readback and
postpins precede actual UTC ALL WRITERS IDLE; no later writes. Basis
0d39df3c9fd69c939a8420c54d03228b9077777d. Finalizer
0f6aaf7d549952de8f318bdd974ae4627e497783d809f94cfaba16607355cb8d.
