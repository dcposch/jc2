# Same-process preflight bootstrap — STATIC / UNEXECUTED

This is one prospective metadata program, not a deployment or enabled
registration. No code was imported, parsed, compiled or run. A focused FIRST
of the actual source and exact dispatcher interface must precede any test.
The original dispatcher, authority, CAPRUN and scientific files are unchanged.

## Exact external input

CLI: `PYTHON -I -S -B BOOTSTRAP --spec SPEC --spec-sha256 SHA256`.
All uppercase words are explanatory metavariables, not substitution tokens.
ROOT must externally approve the exact code and specification hashes and
already arm an independently qualified outer supervisor and retirement.
The bootstrap cannot authenticate the human origin of that approval.

The canonical UTF-8 JSON input has exactly the `SPEC_KEYS` enumerated in
bootstrap.py. Its schema is `r3-exec-bootstrap/v1`; only an externally issued
`approval=ROOT_APPROVED_PREFLIGHT_ONLY_9` can pass. No sample approval is issued
here. The four SHA fields identify code, bootstrap FIRST, launcher
qualification and native qualification. The separate CLI pins the complete
input; the specification must NOT pin itself inside its registration vector.

`registration` has exactly `REG_KEYS` in source. It is a disabled skeleton of
`f10-necessary-rows-runtime/closed-child/v1`: enabled=false, commands={},
PREFLIGHT_ONLY_9, exact LABELS, typed_slot_policy={}, phase_policy=null.
All ordinary registration fields are fixed literal ROOT data, not placeholders
expanded by this program. Only enabled, commands, outer_device/outer_inode
and the nine leaf device/inode pairs are generated. The latter identities
must initially be null; each path is already the exact outer path/label.

The specification additionally fixes registration_path, binding_path,
binding_deadline_utc, binding_wall_seconds=120, cgroup_namespace,
mount_namespace, outer_started_utc, outer_resources, external_supervisor,
and exclusive_root_contract. Exact nested key sets are enforced in source.
outer_resources has memory.max=8589934592, memory.swap.max=0, pids.max=32,
cpu.max.burst=0 and a ROOT-qualified literal positive quota/period cpu.max.
external_supervisor binds a qualified service, already-armed/no-restart/
all-descendant assertions and original outer/retirement deadlines. Every
deadline is offset-explicit UTC. These assertions require genuine external
verification; observing a cgroup file is not proof that a watchdog is armed.

## Files, layout and fixed mechanism

ROOT owns immutable 0444 source/spec/native/policy files with traversable
non-writable ancestry and no POSIX access/default ACL. Science has exactly
five original files; wrapper has exactly dispatch.py/probe.py/mutate.py.
Bootstrap is outside both inventories. All static inputs and the generated
registration are outside the working tmpfs and outside any native directory
inventory that their creation would alter. No native discovery is performed.
The six-field native closure, simple source inventory, seven literal refusal
policy, exact interpreter/environment/argv and source vector are separately
supplied and hash-checked; unsupported ACL/native observation is STOP.

The 128MiB physical working tmpfs initially contains only the three empty
authority/frozen/writer directories and outer.stdout/outer.stderr. The first
two are ROOT0755, writer is child UID/GID0700. Both inherited streams must
be regular files on that exact mount. Binding is the sole added bootstrap
file `bootstrap-binding.json`, counted in the unchanged 8MiB metadata budget.
The later dispatcher durable destination is absent on registered ext4/xfs
EBS with an INTEGER device identity. ROOT owns terminal recovery on failure;
this program neither copies evidence durably nor stops/unmounts/deletes.

One already-running ROOT process must be the sole process/thread in its
fresh outer domain cgroup. Root mount and PID/cgroup/mount namespace bindings,
EC2 instance/hostname/boot, migration permissions and empty subtree_control
are checked. ROOT guarantees no concurrent writer, entrant, replacement,
migration or delegation. Nine absent direct leaves are created once and
attested by device/inode, domain, no subdirectories, empty procs/threads and
kernel populated=0. No child memory controller file is required. Failure
after some creations retains them for ROOT recovery; there is no retry.

Nine exact authority byte strings are reconstructed only in memory. They
contain registration PATH but not registration SHA; valid/dummy additionally
contain observed leaf identities. Their digests yield the nine literal
CAPRUN vectors. Registration is serialized once, O_EXCL/O_NOFOLLOW-created,
0444, fsynced, parent-fsynced and byte-read back. A separate bounded receipt
binds its SHA, input/code SHA, identity, leaf facts and original clocks; it
does not contain its own hash. No authority file is preinstalled.

After repeated pre-exec checks, all descriptors above stderr are closed and
os.execve replaces this process with the SAME pinned Python and exact
dispatcher argv. There is no subprocess, waiting holder or new PID. The
external unit/cgroup clocks persist. aggregate_cpu_start_usec stays '0', so
bootstrap CPU is included. Existing 3000wall/2100CPU/8GiB/128MiB limits and
all phase profiles remain unchanged. The bootstrap's 120-second checks and
64KiB kernel-read cap are fail-closed planning restrictions, NOT measured
liveness or hard I/O guarantees; the external supervisor covers blocking I/O.

## Status, qualification and controls

Receipt status PREEXEC_BINDING_ONLY_NO_PREFLIGHT_RESULT is not success.
Any caught failure exits2 with BOOTSTRAP_STOP/science_outcome NONE; failed
exec cannot fall back. Early failures may leave no binding receipt. ROOT
must recover the original capped streams, frozen spec and any partial leaves.
Only unchanged dispatcher terminal CLOSED_SCOPE_PREFLIGHTS_9_CHECKED_NO_SCIENCE
with its strict dummy and terminal custody is positive runtime evidence.

ONE future AWS-only nine-phase observation is the prospective discriminator,
not authorized here. Required negatives: wrong input/code pin (before writes),
stale boot/ns/outer/leaf identity, preexisting outputs/leaves, FULL_13 or hash
slots, expired original clock, and exec refusal. They must fail without
dispatch/retry; partial creation must remain visible. No negative is executed
or claimed. All native build, launcher, host namespace, quota hierarchy,
descriptor, ACL/cgroupfs and watchdog compatibility remain UNQUALIFIED.
No scientific outcome, timing, cleanup performance or installed readiness is
established. The finite readback/hash loops may themselves consume the
120-second limit; no estimate replaces an eventual capped observation.
