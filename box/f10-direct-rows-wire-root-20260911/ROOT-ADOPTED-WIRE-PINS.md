# ROOT adopted wire pins after the rows-only FIRST

STATIC INTERFACE / NO IMPLEMENTATION / NO EXECUTION. Original publication
reserve03:10UTC/HARD03:13UTC September11, assigned on intake. Original
proposal/addendum/FIRST remain immutable. This document resolves the two
specific blocking ambiguities and adopts the small useful clarifications.
It does not change the circuit,25 generators, guard, theorem or runtime caps.

## Binding definitions

1. phi and P7_mod_p arrays are ordered by ASCENDING powers of V, constant
   coefficient first, leading1 LAST. A field scalar remains ordered
   1,V,...,V^(f-1). All entries are canonical decimal strings in0..p-1.
2. The internal SCALE comparison records are exactly the following
   DIFFERENCES, not raw coefficients or source generators:

   scale-low1:i = [S^i t^1]origResidual
     - (s^-6 * P1_i(z=s^2) - delta(i,0)*origu), i0..20.

   scale-low0:i = [S^i t^0]origResidual
     - (s^-7 * P0_i(z=s^2) - delta(i,0)), i0..23.

   Here coefficient extraction leaves the parameter/s variables untouched.
   These differences must vanish. The raw coefficients of origResidual
   themselves are computed and are NOT required to vanish. No source equation,
   ordinaryness hypothesis, s=1 or z=1 specialization is added.
3. Explicit guard comparison operands:
   orig_a=[S^10 t^0]origA=s^-3*C0;
   orig_b=[S^17 t^0]origB=s^-5*D0;
   guard=W*t5*s^8.
   guard_product=guard*orig_a*orig_b-1 must vanish. In this formula
   C0=1/W and D0=1/t5 denote leading coefficients, not denominator-registry
   label names. They are recomputed, never candidate-owned replacements.
4. CHECKED is emitted only after the checker's OWN full internal diagnostic/
   construction-anomaly partition passes and all displayed fields equal its
   independent reconstruction. Producer FORMED also requires its own full
   partition. No agreement-only bypass.
5. All source_pins values and contract_sha256 are lowercase64-hex SHA-256.
   Their expected vector/manifest are independently ROOT-issued at actual
   registration, not inferred from candidate claims. New metadata-only API
   implementation still needs combined code FIRST.
6. Every nonzero graph term satisfies sum(i*e_i,i=1..4)=the exact declared
   slot weight. Zero and duplicate slots remain, zero polynomial is empty
   terms, no zero sparse coefficient. Exponents use NUMERICAL lexicographic
   order, not decimal-string lexical order.
7. Parser byte round-trip is INPUT-only: reserialize the parsed candidate and
   compare with its own received bytes. It is not an early comparison with
   the checker's independently reconstructed object. Per-slot comparison
   remains explicit. T+X1^27 must reach stderr exactly
   CHECK FAILED: direct-source graph slot T followed by one newline,
   normal exit2, empty stdout, under valid fresh binding/resource slack.
   No traceback or extra stderr for this prescribed semantic rejection.
   Both positive success lines remain exactly the rows-proposal literals.

All other accepted operation and wire obligations remain. This resolves the
scale-low ambiguity of the first ROOT addendum; it does not require the raw
low source rows to vanish or omit any internal computation. Both original
full inverse polynomials remain, only their pole subrecords are diagnostics.

## Scope and inputs

Rows proposal xmodel/f10-direct-rows-wire-root-20260911.md:
90c940b021ba9f99f2298aaf394cb082793812ad4428e249b970ecf06baa6c47.
ROOT-ADOPTED-CORRECTIONS.md in the original direct-circuit box:
39ed9b3910efee02dd2f17ced7951a8b83557e612d100d4ae34f76e0895b4002.
Designated FIRST xmodel/f10-direct-rows-wire-gate-fable5-20260911.md:
68488229c5ff839f8c155662f903ba174d59d4e96bb5e046a53967313d497f1b.
Frozen original checker box/f10-source-cone-r3-code-astra-20260911/check.py:
b9176ee7f0b8f34506e0fce03c2aea548756d0d489a53b732401cc7dbaa8c87f.

The first two were previously ROOT-WHOLE/current-pin checked; FIRST was
fresh-WHOLE read after independent terminal/receipt-FIRST and18pins03:05–06.
Original checker is same-ROOT WHOLE reuse with current earlier pins;
lines460–501 freshly reread03:06 show precisely these two differences and
guard coefficient extractions. No claim of a new WHOLE checker read.
No newly charged code beyond that known source; no mathematical execution.

Review-only overclaims are NOT adopted: zero low arrays alone do not imply
all25 generators or their matrix are zero, and a changed completion need not
change projected rows on every residue. Neither assertion is needed: the
independent checker must follow the fixed rule and authenticate the rows
it computes. Accepted23z/19z still supply only their conditional conclusions.
No code, actual fully admissible place, matrix, rank or JC2 result follows.

No new canonical OPEN identifier. Cheapest next test: two bounded independent
source implementations, then one combined different-model static FIRST.
The30minute authoring figure is UNMEASURED planning, not a runtime forecast.

Own WHOLE read and all four postpins completed03:07:32UTC, no clipping.
Own target absent at begin; own-only collision clear. This closing paragraph
and marker are the final bounded apply_patch write. No scientific execution,
extra body, peer output or clock extension; publication transaction follows.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5296`.
- Body SHA-256:
  `7ef3df9cfc463df7ae712c54d1712d8bbf53fe1a59e20767bcd51aba7e6cc365`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
