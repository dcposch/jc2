# DISABLED prospective hybrid81 exact-Q decision

PREPARATION ONLY, 2026-09-07. This is not root GREEN, a deployment instruction,
an instance action, or authorization to parse the complete client or run CAS.
Exactly one future attempt, conditional on root acceptance of the new adapter
and a separate actual-engine control/dispatch GREEN. No authority file exists.

Client: the frozen normalized Hermite-only parity/gauge slice,81 variables and
all804 indexed rows, including all original803 positions and the added kz−1.
Exact fieldQ, displayed-variable globaldp, same J+5k³g²/9 target. No row deletion,
gauge, specialization, order change, B reconstruction or normalization variant.
Physical source total75/125; receiver15/25 is not physical total degree.

Only prospective worker i-0da0cebfc97c9fd54; root alone START/STOP. Sole source
EBSvol-0eb6450d18ffa89f1 must never be terminated/deleted. Prospective fresh cwd
/home/ubuntu/d125-hybrid81-exact-solver-20260907; no directory is created here.
Input /home/ubuntu/d125-hybrid-affine-pilot-20260907/construction.jsonl,
SHA4ea526b2680ef41154332be3059ae5543c2678fdf8fb6a95d02176e8eb929272.
Original source SHA b8db27661d1bfd3dcc5dfe1d1271b78c3f6375e06201654977a51380cd6ebcac;
its complete coefficient-cover transport is already accepted at14c/14j/14k
and actual all803 execution scope; no additional full replay is imposed here.

Retain reviewed JC2CERT1 I/G/T format and exact.py byte-identically. New adapter
reads the frozen canonical JSONL, emits every literal row into ordinary Singular
syntax, and binds the certificate header to the construction SHA and dp ring.
No cofactor position is presumed to equal a literal index. Use engine equality
mapping including zeros/duplicates, lift to804 slots and verify sum h_i f_i=1.
Properness requires rational Buchberger, NF(1) nonzero and every804-row NF zero;
this certifies containment in a proper superideal, not equality/reducedness.
Existing graph/faithful-cover results are required for source interpretation;
a mere parsed basis or certificate header supplies none of those hypotheses.

Prospective decision: source import/serialization+one slimgb(I)+conditional
lift(I,1) all inside300wall/CPU seconds,16GiBAS/sampled exact-groupRSS. Keep
2MiB generated-input ceiling INCLUDING suffix; actual size is not yet measured.
If exceeded, stop INCONCLUSIVE, do not raise it automatically. stdout/stderr
64MiB FSIZE each, core0. Only normal complete decision permits separate<=120s
exact rational verification; aggregate deadline450s. One attempt, no retry,
algorithm substitution, extension or cap raise. Timeout/partial/lift failure
is INCONCLUSIVE. No solver nor certificate run occurs in this preparation.

Future required concrete controls: independent review of new hybrid adapter and
caller delta; tiny actual hybrid serialized-input import through the unchanged
footer, zero/duplicate I_SIZE/I-column/T correspondence and exact checker.
Prior accepted engine/FSIZE/CAPRUN evidence remains usable; only fresh host/boot,
path/Owner/EBS/pin and named changed-path lifecycle checks are needed, not a
default repetition of every historical control. Actual full serialization and
engine import of this representation have not occurred. No new theorem gate
is inferred merely from the report's earlier generic next-gate wording.

Before any later dispatch root supplies new physical GREEN, accepted adapter
gate hash, exact fresh registration/code/binary/boot pins and authority/deadline.
Engineering and solver authority modes remain distinct. All outputs exclusive;
all-phase identity/argv/stream hashes/telemetry retained, receipt-first terminal
read and fresh exact-group absence required. Root controls retained-instance STOP.
