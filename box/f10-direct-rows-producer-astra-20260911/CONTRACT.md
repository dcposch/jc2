# Direct-row producer source candidate — disabled / UNEXECUTED

This owns only authority.py, arithmetic.py and produce.py. ROOT separately assembles the independent check_arithmetic.py/check.py and conducts the combined first source review. No checker implementation, runtime, registration, place, coefficient packet, test or source outcome is provided. All scientific code is unexecuted, including no import, syntax/AST, compilation, test or dummy.

## Frozen metadata-only API

Future installed directory contains exactly authority.py,arithmetic.py,produce.py,check_arithmetic.py,check.py, no documentation or shadow modules. ROOT supplies genuine externally authenticated registration, current source bytes, native/startup closure, physical/process custody and enforced limits. The local helper checks Linux/Amazon EC2, ordinary interpreter (nonzero optimize refused), exact nine sys.argv entries, actual absolute installed entry matching mode, installed five-file hashes, interpreter and pinned native subset. It removes sibling lookup during metadata imports, restoring it after authorization. ROOT must launch with suitable no-site/no-user-site/no-bytecode/environment isolation; no actual vector/native policy is selected here. The authoring box is not a ready install directory.

Exact CLI:

    PYTHON /ABS/produce.py --registered-job f10-source-cone-r3-direct-rows-v1 --authorization /ABS/META --authorization-sha256 SHA --output /ABS/ROWS
    PYTHON /ABS/check.py --registered-job f10-source-cone-r3-direct-rows-v1 --authorization /ABS/META --authorization-sha256 SHA --input /ABS/ROWS

Only mode produce/check exists. The shared authorize(mode) returns exactly artifact_path,contract_sha256,source_pins,place. There is no mathematical content in authority.py. It does not calculate prime, factor, residue or polynomial values. Each scientific implementation independently does those checks. No API helper or producer arithmetic is licensed as the checker's mathematical oracle.

Metadata's exact eleven keys are job_tag,status,authority,mode,artifact,files,runtime,limits,contract_sha256,source_pins,place. Fixed job above, REGISTERED, ROOT-CAPRUN. artifact has path/sha256; output requires UNFORMED and absence, input requires current SHA and size at most 134217728 before parsing. Paths must be absolute/canonical. files maps exactly five basenames to current hashes. ROOT-approved clarification: source_pins includes the five installed ABSOLUTE source paths as keys, with values equal to files[basename]; remaining contract/source references are frozen by ROOT at actual registration. All values and contract_sha256 are lowercase 64-hex strings. Metadata is at most 64KiB with duplicate-key and JSON-number/constant refusal. No candidate supplies its own expected source vector.

runtime has python_sha256,native_inventory_path,native_inventory_sha256. Native record has job_tag,files mapping absolute paths to matching hashes. This matched subset is NOT complete native or loaded-map custody. limits is exactly wall_seconds="900",cpu_seconds="600",memory_bytes="8589934592",aggregate_bytes="134217728". They are prospective fields only, not enforced by these scripts and not execution authority. ROOT owns prospective actual argv, no-shadow/readonly source layout, artifact immutability, external deadline/cgroup/resource supervision, input postpins and quiet durable collection. Hashing self-authored metadata never authenticates registration.

## Literal place and complete construction

place is exactly p,degree,phi,P7_mod_p. Prime 2<=p<2^31, degree 1..7 and every residue are canonical unsigned decimal strings; arrays phi/P7_mod_p are CONSTANT-FIRST, monic leading1 LAST. Both are checked against the producer's literal rational construction at tau=3/10. Primality is exact bounded trial division. The irreducibility test checks gcd with X^(p^i)-X for every i<=floor(f/2) and the final Frobenius identity; it is an exact equivalent all-small-factor test. There is no place discovery or alternative-place branch.

The arithmetic module evaluates into the prescribed residue field, not a pretend rational degree-seven quotient. Every fixed D0/D1/D2 name and its nonzero inverse product is retained in ordered registry. All 15 scalar inverses retain their actual inputs and checked products: dL,W,t5,H7_5,H7_6,det1..det6,critical_c,pivot8..pivot10. At late gaps 8..10 the ONLY completion rule is c1-first, otherwise c0, otherwise STOP_BAD_COLUMN_PLACE. Gap7 retains the unchanged lambda functional, integration constant0, half factor and coefficient order (theta,1). The implementation does not import the rational Euclidean completion.

Ten formal A/B bands and full mixed forcing are independently computed before the separate installed A/Euler B sequence. The fixed installation retains formal discarded coefficients at (5,2),(6,2), never modifies earlier forcing arrays, and does not use Laurent auxiliaries to repair bands. Pi and BOTH target terms remain. Every Euler index is visited; the two entire polynomial gauge coefficients are zero while their forcing is recorded. All 108 Jacobian,45 low,63 upper positions and22 readbacks are computed. Both complete inverse charts and their pole subrecords are retained, not equated to zero.

All150 labeled records are built; exactly111 canonical-anomaly zeros are required,39 alternative records may be nonzero. Neither class is a rank generator. Both inverse pole subrecords may be nonzero. All six SCALE comparison groups, including 21 and24 LOW DIFFERENCES, must vanish. Raw original residual coefficients are computed but are not imposed zero. Guard operands are the actual original leading coefficients with s^-3/s^-5 factors. There is no s=1 or z=1 substitution. The partition is applied after complete internal construction, before any success artifact publication.

## Output, refusals and exact control

The artifact has exactly schema,job_tag,contract_sha256,source_pins,place,graph. Schema f10-r3-direct-rows/v1 and job f10-source-cone-r3-direct-rows-v1 are distinct from every earlier baseline/full-trace object. graph has Hq,c,c_inverse,zeta,slots,ell,g,U; five extra polynomials plus25 slots and two scalars. Slots are K8,K9,K10,A1_1..9,A0_1..12,T with weights8,9,10;19..11;22..11;27. Extra weights Hq/zeta7,ell23,g30,U4. All zero/duplicate slots remain.

Every graph polynomial is {kind:"graph",terms:[...]}; terms contain four exponent STRINGS and a dense length-f coefficient list of residue STRINGS. Sorted numerical exponent tuples, no zero term/duplicate, exact homogeneous weight; zero is an empty term list. The output is ASCII JSON with sorted object keys, compact separators, one final newline. Only ROOT metadata contributes header pins and place. There are no trace, diagnostic, row-count verdict or rank fields.

The implementation retains typed internal Poly records: band=(X1..4,z,theta) with S slot zero; raw=(X1..4,z,S,theta); laurent_S permits signed S only and theta zero; inverse permits signed q only with the positions explicitly retagged; scale permits signed s and S; graph has final three slots zero. Addition/multiplication reject incompatible nonscalar kinds; fixed retyping and exponent reindex occur explicitly at installation, inverse and scale boundaries. Scalar inversion is called only at statically leading-scalar nodes, with a parameter-support rejection in scalar(). Sparse zero removal is representation, not an omitted fixed source index.

Refusal ceilings: 100000 terms per internal polynomial, absolute exponent256 and stricter type/source/q bounds,20000 per output polynomial,400000 aggregate output terms,134217728 encoded bytes. Scalar residues are bounded by the approved prime. These are STOP ceilings, no truncation and no speed/fit forecast. Bad input, place, denominator, scalar pivot, type, construction or canonical-anomaly, write/resource failure yields refusal or an uncaught ValueError/STOP, empty success stdout. No generic exit1 traceback is a semantic negative-control pass.

Producer success is ONLY normal exit0, empty stderr and exact stdout `FORMED_UNCHECKED_DIRECT_ROWS_25_NO_SOURCE_OUTCOME\n`, after all construction and internal partition checks and complete publication. It writes to a fresh same-directory temporary, fsyncs/readback-hashes, hard-links no-overwrite to the authorized absent target, directory-fsyncs and removes the temporary. Any failure before the success line is STOP; even a complete leftover file is not accepted without successful independently collected evidence. No producer receipt or mathematical acceptance is minted.

Exactly one source semantic control is SPEC/NOT_RUN: independently modify only T by +X1^27 in the received positive ROWS artifact. Constant field coordinate +1 modulo p, preserve all other coordinates; insert a missing sparse term, or remove it ONLY if the entire field vector becomes zero. Numeric exponent order and exact canonical serialization remain. Reverse the whole semantic edit and demand byte-for-byte restoration. ROOT must bind the new actual SHA before an independent checker call. Expected exact checker branch is normal exit2, empty stdout and stderr `CHECK FAILED: direct-source graph slot T\n`, no traceback. The checker must first pass its own complete construction/partition, compare fields separately, and reach T; parser/current-pin/resource/authority failures are not semantic credit. This author neither implements nor reads that peer checker, so reachability in its code remains a combined-FIRST obligation.

No actual place or payload exists. No rational-baseline qualification, canonical 297-minor, unsaturated ideal equality, generic identity, source point, source exclusion or JC2 conclusion follows. The alternative rows attach through the accepted canonical localized source algebra only. ROOT owns the eventual accepted contract reference vector, independent checker assembly, first static review, native/runtime registration, actual evidence and any later rank client.
