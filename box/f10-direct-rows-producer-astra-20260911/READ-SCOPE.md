# Exact source scope

First action 2026-09-11 03:11:41 UTC; own report, artifact manifest and box absent. Original reserve03:36 and hard03:39 unchanged. Exactly card plus11 inputs,117520 bytes; ALL12 FRESH_WHOLE, no earlier-turn reuse. Card SHA matched before body; all11 listed hashes matched before their bodies. Initial multi-output rendering clipped the middle of its combined result; ASSERTIONS, WIRE-AND-VALIDATION and rows proposal were reread individually to EOF. The rows gate was also freshly reread alone to EOF. All12 have complete fresh coverage without unresolved clipping. No provenance links followed.

Allowed inputs establish literal source transcription, operation/anomaly partition, rows projection and metadata API. Precedence is ROOT-ADOPTED-WIRE-PINS, ROOT-ADOPTED-CORRECTIONS, rows proposal, earlier operation/assertion/wire. Header UNREVIEWED labels in accepted historical reports do not widen their accepted conditional scopes. Review overclaims about zero low arrays forcing all rows zero and every completion change surviving projection are explicitly excluded.

Original authority.py and producer/arithmetic are allowed reference sources. Metadata code and portions of producer syntax were mechanically transcribed into new owned files, then adapted and manually read; that text processing did not execute mathematical code. New arithmetic evaluates in a prescribed finite field and carries explicit polynomial type tags. No new checker or shared mathematical module is inspected. The separately approved absolute-installed-path source_pins key clarification changes no top-level API. The only shared implementation is metadata-only authority.py.

Three own source files and all own text are manually read WHOLE before final publication, with corrections followed by whole/range recovery as recorded in custody. No scientific subprocess, import, AST, syntax check, compilation, test, fixture, arithmetic helper execution, baseline/coefficient body/hash, place discovery, network/AWS/SSH/process/Git/corpus/protected or live/peer-source access. The existing artifact_finalize utility is used for documentary publication only. All authored bytes apply_patch.

Own-only OPEN/collision scope: zero new canonical IDs; the existing quantity is faithful complete direct-row reconstruction at one ROOT-provided admissible place. Cheapest next instrument is combined independent static review of both frozen implementations, then only a separately authorized observation and prescribed T control. Thirty-minute source-authoring is unmeasured planning, superseded by the original absolute cap; fixed900wall/600CPU/8GiB/128MiB are prospective API limits, not runtime or performance claims.
