# Disabled separate regression interface

STATIC ONLY. No executable authority or fixture is published here. Root
owns independent first review, registration, CAPRUN/dummy controls, clock,
host/start/stop, native-closure custody and every execution. Old generic
plan/callers/all12 cases are unchanged and not reused as a priming step.

CLI, exactly four arguments after the script:

    /usr/bin/python3 -I -B /ABS/large_decimal_controls.py AUTHORITY INPUT MODE OUTPUT

Three distinct exec children and exclusive authority/output names, in this
order: MODE old, MODE new, MODE fresh-check. Old/new use operation build;
fresh-check uses operation check, so its pinned bundle MUST be INPUT at
sys.argv[2]. No child runs another mode in-process. No caller is authored.

Required flat regular nonsymlink siblings and immutable versions are in
the helper FROZEN mapping: oldsolver.py, solver.py, checker.py,
generic_controls.py, algebra.py, evidence.py, execution_gate.py. Root also
registers the exact helper SHA, unchanged CAPRUN, /usr/bin/python3, input,
and installed native closure. All seven scientific/metadata sibling pins
are checked in every mode, but fresh-check imports neither solver nor
FLINT. Its producer_environment call checks metadata only. Root must pin
the entire native closure; the helper does not discover or prove closure
completeness. Existing gate verifies every declared file and exact parent
argv before science; helper rechecks them and authority hash prospectively.

Use existing F10-R1-REGISTERED/v1 schema, no new authority schema. Besides
its normal required gate fields, the root-written authority must contain:

- univariate_acceptance null (never fabricated source acceptance);
- synthetic_only true; large_decimal_mode exactly the selected MODE;
- file_size_limit_bytes exactly16777216;
- python_flint environment block and its module/interpreter/native pins,
  even in metadata-only native verification for fresh-check.

Root supplies/inherits RLIMIT_FSIZE soft=hard=16777216 BEFORE CAPRUN; the
helper checks, never changes, this cap. Use finite per-child registered
caps no larger than original ceilings. A prospective60wall/55CPU/1GiB
profile per mode is compatible, but not selected or authorized here.
Absolute and aggregate deadlines, live dummy regression, stop-on-failure,
remaining-PGID cleanup and worker-stop duties remain root's registration
and unchanged CAPRUN obligations. No source600/550/2GiB increase follows.

Old/new INPUT is the SAME externally pinned small JSON descriptor, not
coefficient wires or an arithmetic artifact. Its exact parsed content:

    {"schema":"F10-LARGE-DECIMAL-FIXTURE/v1","synthetic_only":true,
     "fixture":"T2__1+NT+T3","N_exponent":7421}

This document does not create that future descriptor. After authorization,
the helper constructs N and Fraction coefficients with integer arithmetic
and the pinned literal modulus function. Default4300 is checked before
the selected API. No decimal parsing/formatting, setter or priming call
occurs before that API. Old mode permits its own zero setting only after
the exact observed decimal-limit ValueError, solely for fixture custody.
The new mode relies on the repaired API's setter and independently checks
all217 equations and all1638 literal cofactor entries after one degree2
RREF (14 rows,112 columns) over the full rank7 modulus.

With absolute root-local OUTPUT base O, reserve these six paths before any
science: O, O.fixture.json, O.certificate.json, O.bundle.json,
O.mutation.json, O.failure.json. Every one must initially be absent,
including broken symlinks, and must not alias a registered/input path.
Old writes fixture+O; new writes fixture+certificate+bundle+O; fresh-check
writes mutation+O. Unexpected failure in the scientific stage writes
O.failure.json then raises, never success or retry. Pre-science refusal
may have no helper failure file; CAPRUN stderr is authoritative then.
Every emitted file is exclusive, finite16MiB, chmod0444 and re-read for
exact bytes/parsed-object equality, with path/hash/size in its summary.
Serialization allocation itself remains subject to external CPU/RSS caps.

Root must compare old/new O.fixture.json SHA and bytes across the two
children before crediting common-input discrimination. Old summary must
retain exception class/text/full traceback/sites and explicit post-failure
setter timing. A failure before RREF is honestly classified by those
sites, not credited as observed RREF extraction. New summary must carry
the full independent count and exact expected-cofactor check.

Fresh INPUT is the exact newly produced new-mode O.bundle.json, separately
root-hash-registered after the new child is terminal. It contains quoted
giant wires and the ACTUAL checker schema, but is SYNTHETIC. Never call
evidence.verified/checker.main or issue ROOT source acceptance for it.
The fresh child proves it is at4300 immediately before checker.verify,
with a >4300-character quoted h1 coefficient. Only that entry sets zero.
It checks all217 coordinates, unchanged positive input, then exact h1
constant N^2 and a sole +1 mutation; fresh read-back must reject with
ValueError `full 217-coordinate sum h_i f_i equals q^5`.

No input scientific integer passes through float, JavaScript Number,
eval or approximate arithmetic. The only JSON numeric metadata before
the first API is small (the fixed descriptor and registration). Helper
does not authenticate root: all authority/pin custody is cooperative,
under the existing registered workflow. No genuine source outcome or
mathematical exclusion follows from any of these synthetic statuses.
