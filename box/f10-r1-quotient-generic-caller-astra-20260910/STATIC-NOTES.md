# Static caller delta — UNEXECUTED

This packet is not a registration. The disabled template selects no worker, cwd, date, native closure or actual caps. Every future input must be supplied and pinned by ROOT after a FIRST Fable static-delta review. No automatic third attempt is licensed.

The prospective caller CLI is `/usr/bin/python3 -I -B <flat-cwd>/dispatch_batch.py <ROOT-registration-SHA256>`. ROOT separately pins the caller and the outer registration in its launch vector. The caller checks that external digest before parsing `dispatch.registration.json`; that registration supplies the caller/interpreter/native hashes and exact future host/cwd/clocks. Startup source/self/registration checks precede the first derivative authority; every operation additionally rechecks them before Popen. The disabled template fails before authority/STOP-file writes; this is a code-reading conclusion, not an executed negative.

## Narrow change inventory

- Retained old `run`, unchanged CAPRUN vector, `preexec_fn=filelimit`, `start_new_session=False`, exact-PGID post-wait quiet check, namespace/stat capture and dummy identity/signal checks. No new supervisor or inner process group.
- New plan hash ce193898e3114f1db018b86e6ebe0b2ea4fdac5f65296dee76245f94da0b964a. The plan contains only scope, names/expected predicates, CLI, source hashes and proposed caps. It contains no fixture coefficients or mathematical code.
- Old compatibility payload removed. Seven probes still use operation `check` and the plan as the explicit input. The sole generic child uses operation `build` and exactly `generic_controls.py AUTHORITY OUTPUT`, not the old plan-bearing harness CLI. All six scientific modules, probe and CAPRUN are referenced by unchanged original hashes, not copied into this box.
- Every operation first rejects existing authority/output/sentinel/prelaunch/dispatch paths with `lexists`; generic also rejects its exact receipt plus all20 sidecars. It creates the authority exclusively, chmods0444, computes its hash, checks all original fixed files, writes/freezes a prospective metadata record, then repeats output absence, original fixed pins and authority hash immediately before `Popen`. The budget admission is repeated after these checks. The missing-input negative removes only the plan entry in the child's copied map; the original fixed map still checks it.
- All five deliberate refusals require ordinary nonzero exit, their exact expected substring and no sentinel. Gate-valid and dummy require exact `POSTAUTHORIZE` newline bytes. Dummy retains32MiB measured aggregate-RSS overrun, MATCH then TERM then MATCH then KILL, cleanup/reap and same namespace/PGID checks.
- Probe5wall/3CPU/2GiB, dummy5/3/32MiB and generic180/170/2GiB are exact profiles; aggregate240 and16MiB inherited file limit are unchanged. The fixed absolute and first-math aggregate cutoffs are stored, never renewed. Late return or late final generic metadata validation cannot yield batch PASS.
- After CAPRUN termination and owned PGID quiet, the generic receipt requires its exact schema/status/scope, complete nine-key summary, authority/host/job/instance, environment, solver/harness hashes, ordered8positive/4negative names, exact branch/count dictionaries and exact negative exception predicates. Duplicate metadata keys and nonfinite JSON are rejected. Sidecars are only hashed as bytes: no fixture JSON parsing, certificate arithmetic or scientific imports occur in the caller.
- The frozen harness embeds sidecar paths/hashes, NOT sizes. The caller compares every embedded hash and records measured sizes, including receipt size; it does not invent an embedded-size check. The receipt is hashed before and after documentary readback. All original sources, native files, registration and authority are checked again. These are cooperative custody barriers, not isolation from a malicious same-user writer.

## Manual controls and unresolved runtime obligations

No code was imported, parsed with AST, compiled, syntax-checked, executed or tested. The following are manual branch checks only, not new runtime fixtures or authority:

1. Changing a fixed plan/native/caller/registration byte after startup reaches `fixed_barrier` before the sole `Popen`; the missing-input negative does not exempt the original plan. Changing frozen authority bytes also refuses there.
2. A dangling output symlink is detected by `lexists`, including any of20 generic sidecars; there is no silent overwrite or cleanup/retry.
3. A wrong-host error during the disabled control is not a PASS: the required disabled-reason substring is absent. A valid sentinel containing CRLF fails the exact byte comparison.
4. A missing/reordered positive, wrong branch/count, bool instead of an integer count, wrong negative reason, duplicate metadata key, stale sidecar hash or wrong authority binding cannot produce batch PASS through the displayed conditions.
5. The genuine future harness summary's literal fields and checks match the caller: UNIT reports217coordinates/9multipliers/degree25; SEPARATOR reports1638columns/217targetcoordinates; no native-operation counter or old5/7 tally is invented.

Actual syntax, prospective freeze timing under failure, installed API, eight mathematical positives/four mutations and same-launch independent child capture remain UNTESTED. ROOT must arrange the separate bounded SYSTEM unit/stop timer, externally frozen registration/caller hash, exact host/boot/cwd/native preflight, live unit/cgroup/CAPRUN/child observation and terminal worker cleanup. The inherited caller observes CAPRUN's namespace/stat; it does not supply independent live mathematical-child evidence. If identity capture or any other condition fails, retain partials and STOP without retry. No old constant-v runtime result transfers to this new batch.
