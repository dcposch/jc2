# Operator native roots; original metadata inspection made host-unbound

ROOT,2026-09-12, SOURCE-ONLY / UNREVIEWED / DISABLED. No worker or execution.
The literal corrected dummy commands use jq/head/cmp and administrative tools
not guaranteed by the old science-only native root vector. Add exactly the
21 explicitly named operator executables to the collector's existing twelve
roots. Its existing recursion includes ELF dependencies, deduplicates paths,
and retains all8192file/1000directory and downstream byte/line/total caps.
No entire /usr/bin scan or size-cap enlargement. Absent executable or oversized
closure is STOP. The unchanged assembler checks required roots by containment,
not exact equality, so additional complete records fit its existing schema.
ROOT must check these added roots actually appear, directly or as aliases to
canonical files; a schema pass alone is not their inclusion.

The38-line old metadata inspector b0ea56644448c310f34a4dbb4db33ad4ba5be6c5ac380adee6ca8eafce0b9d7a
was observational in the prior attempt because source FIRST/prepin was absent.
The new template changes ONLY its old actual worker string to an explicit
placeholder. Review its WHOLE source now before any instantiation/use: it
rehashes manifest paths and package copies, checks ROOT/modes/ACLs/ancestors,
directory censuses and actual isolated sys.path; it does not import science.
Its file reads rely on actual collected sizes plus external1GiB/4MiB FSIZE
and bounded wall, and prove neither dynamic import closure nor library APIs.
Do not describe this as a generic hostile-input inspector or native no-escape
theorem. The same fixed deployment layout/private complete packages remain.

Inputs: old collectorc6f917b6026be95fc76b2a56add5f2085eda66b6696f472b420d68ab29d1d2c0;
unchanged assembler63178c8aeab6d2f81bfddf4cdcc732ee00204c73f68f43c5f04761a2dfe4be9a;
corrected commands5cf94c444a26c1052338518f17051f0a2abd838b2be2cabd001424568320673e.
Only the freshly authenticated instance token may be substituted after review;
exact resulting byte pins precede execution. No old clock/host reuse or
qualification/science authority. One focused native-operator completion, not
a new controller, optional package installation or general hardening loop.
