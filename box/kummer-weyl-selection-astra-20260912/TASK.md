# Full-Weyl algebraic no-line discriminator

ROOT September12,2026. Astra primary co-research. First action UTC/hash,
then THIS TASK fresh WHOLE. Sole scientific input THIS TASK; no peers,
corpus, network, external sources, CAS, scientific/code execution, AWS,
Git/process inspection, protected tree, or new agents. Manual mathematics
and documentary date/hash/text/apply_patch plus the existing artifact
transaction only. Previous tasks are complete and unrelated.

One report xmodel/kummer-weyl-selection-astra-20260912.md, <=1800 words,
begin/close/finalize/expected verify using ops/artifact_finalize.py,
basis0d39df3c9fd69c939a8420c54d03228b9077777d; own private partial and
own box PINS/custody only. Unsealed skeleton first, bounded writes,
BODY-END LAST after whole readback/postpins. Terminal custody-FIRST and
ALL WRITERS IDLE. Original reserve02:18/HARD02:21 UTC, no extension.
Honest GAP if any step remains unproved. No promotion/descendant authority.

Actual campaign gap: for a hypothetical Keller inclusion C[p,q] in
C[x,y], let N=C[x,y]/C[p,q], B=C[ep,eq], E=Frac(B), delta=p*dq,
ep=p*dp,eq=q*dq. Existing abstract rank-two difference controls lack
full Weyl/algebraic finite-monodromy structure. This task tests whether
that extra structure alone can force a delta-stable E-line. It is NOT
an actual Keller-source construction, a novelty certification, or JC2.

Define G(p)=(p-1)(p-2),
 M1=C[p,G^-1] e, dp(h e)=(h'+G' h/(2G))e,
 M2=C[q,(q-1)^-1] f, dq(h f)=(h'+h/(2(q-1)))f,
 M=M1 tensor_C M2,
with p,q multiplication. Formally e=sqrt(G), f=sqrt(q-1).

QUANTITY: Does M give a B-torsion-free, finite-Mellin-rank full Weyl
module of algebraic finite monodromy with NO delta-stable E-line?
CHEAPEST TEST: manual exact connection/Ore/simplicity argument, 15min.
Check or refute the following proposed route; every claim is UNPROVED.

1. Each factor is the simple middle extension of its rank-one Kummer
connection (nonintegral exponents1/2 at all finite deleted points).
It is regular holonomic and algebraic with finite monodromy. Explain
the actual argument or label the standard theorem import; do not just
call the localized ring simple. Every nonzero factor section retains
branching at p=1,2 or q=1, so cannot satisfy a nonzero polynomial in
the Euler operator; prove B-torsion-freeness of the tensor product.

2. With z=ep,w=eq, expected Mellin bases are (e,pe) for M1 and f for
M2. The proposed p matrix on the first factor, with shift z->z-1, is
 A(z)=[[0,-2z/(z-3)],[1,3(2z-3)/(2(z-3))]].
It comes from [2(z-3)p^2-3(2z-3)p+4z]e=0.
On M2, dq f=(w-1/2)f, q f=w/(w-3/2)f.
Verify signs/shifts, spanning AND independence; a quadratic relation
alone proves only a rank upper bound, not rank2 or irreducibility.

3. Hence delta acts by (w-1/2) A(z) and shift (z,w)->(z-1,w+1).
Removing the scalar factor, t=z+w is fixed. A line over C(t)(z)
would specialize at a generic complex t to a p-stable line over C(z).
A p-stable line is p^-1 and dp=p^-1 z stable; its intersection with
M1 is a proper Weyl submodule, contradicting simplicity. Check all
localization/intersection/specialization steps rather than assuming them.

4. If no line, every ep-r+delta H(ep,eq), H polynomial,r>=0, is
injective on M: a nonzero kernel would supply a delta-line, while
H=0 is excluded by torsion-freeness. Does M also have zero algebraic
de Rham cohomology in every degree? M2's Laurent powers (q-1)^k f
have dq coefficient k+1/2, so its two-term de Rham complex appears
bijective/acyclic; tensoring should settle M. Verify if affordable.

5. Explain the exact source mismatch. For actual polynomial Keller
p,q, a polynomial square root of G(p)(q-1) would be impossible:
the distinct reduced target divisors pull back reduced and nonempty
under etaleness and polynomial units, so their product is not a square.
Do not equate differential module sections with elements of an actual
source quotient, and do not claim this excludes arbitrary irreducible
monodromy constituents. This is only a discriminator for the abstract
strengthened selection shortcut.

Negative control: replacing M1 by the analogous single-root Kummer
module should give Mellin rank1 and a delta-line. State exactly which
hypothesis/control fails if the proposed rank2 example does not work.
No separate general source theorem, rank farm or formalization task.
