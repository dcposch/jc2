# First r2 reconstruction runtime / entry caller

Status: SOURCE-ONLY, UNREVIEWED, UNEXECUTED. No runtime authorization issued.
Owner: /root/model_productivity. Basis0d39df3c9fd69c939a8420c54d03228b9077777d
is publication provenance, not execution authority or exit price.

First action2026-09-10T10:42:24.958650122Z. Exact new report/box were absent.
The original controlling deadline is11:06:00 UTC, with final-three-minute
publication reserve11:03. No clock was reset. All thirteen inputs in owned
PINS.json matched before WHOLE reading; READ-SCOPE.md records exact scope.

## Outcome

Prepared one new metadata-only dispatch.py, one no-math probe.py, and a disabled
registration template for the exact accepted five-file r2 science package.
Science and CAPRUN bytes are unchanged and not copied into this publication.
No science code, test, fixture, interpreter import, syntax/AST/compile check or
runtime was executed. The source candidate needs its FIRST different-model
static gate; ROOT alone can subsequently register, deploy, execute and stop it.

The caller's native/control/custody mechanisms follow the charged reviewed
middle-real entry but bind the different r2 API explicitly. This is not a
theorem rename or an assertion that the old runtime review covers new code.
CONTRACT.md gives the exact future setup, eleven literal command slots and
refusal predicates. PINS.json has card plus twelve charged originals.

## Entry and source boundary

The outer interpreter uses -I -S -B in a separate exact two-file wrapper
directory. Scientific entry uses -E -s -S -B in the exact root0444 five-file,
root0755 directory, preserving sibling imports. The probe imports only the
actual authority module after checking that inventory. Both actual producer
and checker are also started with WRONG-JOB as independent no-math startup
controls; no fake checker input or positive mathematical fixture is supplied.

Fresh ROOT registration authenticates literal role paths, code and actual
native inventory hashes, exact directory/alias/ZIP/pyc closure, interpreter,
current host/EC2/boot/namespace/cgroup, UID/GID, environment and argv. It is
required to be0444. Derivative science authorities use the science's EXACT
existing schema, exclusive write/fsync/hash inside the caller, followed by a
fresh fixed-pin/host/cap barrier before Popen. Disabled input fails before
writes. No authority is manufactured in this static task.

The reviewed CAPRUN is unchanged. setpriv execs science in CAPRUN's own child
session, without an inner PGID. Prompt actual runner/child identities include
full argv, startticks, boot/ns, credentials, effective caps, limits and mapped
native files. Missed fast-control capture is reported; either science phase
missing required live capture is a GAP with no retry. Terminal output bodies
and hashes wait for owned-cgroup quiet. Live observation uses metadata only.

## Controls and finite envelope

Five changed-authority refusals, one valid authorize-only sentinel, two actual
entry startup refusals and the descendant RSS TERM/KILL control are required,
then exactly one producer and conditionally one checker. Expected outcomes
are static specifications, not PASS results. Failure stops the sequence.

The fixed allocations are nine times5wall/3softCPU; producer300/239;
checker210/169. Including per-child +1 hard CPU gives446 child CPU and555wall
against original500CPU/600wall, leaving54CPU/45wall for caller/runner/cleanup.
Joint wall and absolute deadlines never reset. The caller samples cgroup CPU,
reserving20seconds for cleanup; this is explicitly not a hard aggregate CPU
quota. Per-child hard limits and ROOT's independent external stop remain
required. Slow metadata or completion beyond reserves is a NONDECISION.

The aggregate working tmpfs is hard16MiB, with packet<=15MiB and metadata<=1MiB
checked without reading live bodies. Partition checks are sampled, not hard
per-directory quotas. Producer output is MOVED/root-frozen before checking,
not duplicated on tmpfs. The terminal checker acceptance is bound to frozen
input SHA, exact stdout, telemetry and current source/native pins, not a
free-standing claimed JSON receipt. No packet coefficients are parsed here.

## Custody and remaining review obligation

The caller preserves all working files. After owned-group quiet it copies all
files to a fresh registered EBS target, verifies byte identity, fsyncs files,
directories and parent, then stops writing. ROOT must wait for terminal before
intake. Any STOP or incomplete copy requires ROOT's original cleanup/capture
path; this caller neither deletes artifacts nor stops/restarts workers. No
retry, cap increase, environment installation, source change or follow-on.

The science gate's qualifications are retained literally: fifteen controls
remain SPEC;103wire_p calls; late coefficient -6; nonlinear Psi length check
occurs late; formal changed forcing need not be an actual packet mutation.
Even a future clean result says only RECONSTRUCTION_CHECKED_NO_SOURCE_OUTCOME,
not point/properness/unit/rank/JC2 or a runtime performance forecast.

Remaining question is FIRST static correctness of this new caller/probe and,
only after separate ROOT authority, actual no-math and reconstruction/checker
behavior in a pinned environment. Cheapest next check is the named static
source audit; no new canonical mathematical OPEN is raised and no computation
is authorized. All original mathematical rows/guards remain untouched.

## Completion record

Own code, documentation, disabled input and input pins have been whole-read;
changed ranges reread. Only documentary tools and apply_patch were used.
Final input hashes, owned hashes and expected transaction are in terminal
custody, which excludes its own hash. This report is complete source-only
preparation, not an invitation or execution receipt.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `5873`.
- Body SHA-256:
  `945a30f6fba3f13a7b11cf6a6674db642efa625d3b26fe40c66f31f960eae0b2`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
