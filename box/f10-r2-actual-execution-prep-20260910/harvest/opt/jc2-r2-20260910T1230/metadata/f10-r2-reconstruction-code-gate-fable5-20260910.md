# f10-r2-reconstruction-code-gate-fable5-20260910: FIRST Fable 5.1 STATIC review of the complete r2 reconstruction code

lane=f10-r2-reconstruction-code-gate-fable5-20260910
reviewer=Fable 5.1 (claude-fable-5-1)
status=UNSEALED (no Seal, no charge_basis authored; adapter seals)
mode=STATIC; nothing executed; no CAS/Python/AST/syntax/import; manual math, text and hash only
started=2026-09-10 09:31:30 UTC; budget end 09:51:30 UTC (first+20min, earlier than 09:55:00); reserve from 09:48:30

## 0. Custody

All 16 snapshots under /tmp/jc2-lane.QfyXyf/inputs were hashed with sha256sum at 09:31:30Z BEFORE any body read; all 16 digests equal the charged list byte for byte (rows generated from the sha256sum output in box/f10-r2-reconstruction-code-gate-fable5-20260910/input_custody.md; no digest is typed in this body). Both owned targets were absent at 09:31:30Z; the skeleton was created at 09:31 by apply_patch. Read WHOLE after the match: the report (176 lines to its Seal), its artifact JSON (body 9113 bytes, body sha 55386ebf, consistent with the Seal block, documentary read only), READ-SCOPE (29 lines), ROOT-CARD (101), CONTROLS (65), authority.py (77), arithmetic.py (277), produce.py (285), check_arithmetic.py (208), check.py (416), and the six accepted parents. The transfer parent (362 lines) clipped at 2 KB in the aggregate read and was recovered by bounded reads 1-120, 121-245, 246-362; every other file ended at its marker or Seal with no clipping. No provenance, live gate, scale-cover report, corpus, Git or other lane was opened. Nothing was executed, imported, parsed or compiled; every statement below is a hand reading of text and hand algebra.

## 1. Verdict

| item | verdict |
|---|---|
| A whole 7-coordinate algebra, inverses, pivots | CONFIRMED |
| B gaps 1,2, modified 3, middle 4, both maps | CONFIRMED |
| C critical 5, late 6 (-6), ell 7, determinant-one columns | CONFIRMED |
| D Euler Bhat, 32/80 slots, inverse chart, 19 relations, guard | CONFIRMED |
| E Python text | CONFIRMED, no defect found on text read; runtime untested |
| F CLI interlock | GAP: sibling-import/flag contract and directory exclusivity are not handled; registration authenticity and native closure are wrapper-only, as the code itself states |
| G fifteen controls | CONFIRMED as specifications; two discriminating-branch attributions (2 and 7) are wrong in CONTROLS.md |

Static reading: the complete intended checker obligations of ROOT-CARD item 5 are implemented as source in check.py; no mathematical, sign, index or text defect was found in the five files. The only gaps are in the entry contract (F) and in two control descriptions (G). No runtime success, coefficient, source outcome, degree-frontier, all-r or JC2 claim follows; the code has never run.

## A. Whole 7-coordinate algebra at tau2/7

CONFIRMED. produce.py:19-28 forms dL, KL, gammaL and Sept exactly as the transfer parent section 2 (ascending coefficient lists; `[1+tau, -6]` is 1+tau-6Z); only the 245*gamma*dL^2 term has degree 7, so the leading-coefficient assertion -245*120*144*tau at :29-31 is correct and P7=Sept/lead is monic. check.py:99-111 re-forms the same septic with the checker's own `times`/`plus` and compares the wire P7 to it; check_arithmetic.py:60-68 reduces modulo the monic MODULUS from the top (index 7 contributes q*1, so r[i] is killed). W=-KL/(210 dL) at produce.py:35 and check.py:115. c=1+T+Z T^2+W T^3 at :38/:119. The producer's binomial series (arithmetic.py:187-195, truncation after each product is sound because v has no constant term) and the checker's differential recurrence (check_arithmetic.py:181-194) are genuinely different algorithms; I rederived the checker's recurrence k f_k = sum_i (alpha i-(k-i)) c_i f_{k-i}, which is what line 191 codes. t5=[T^5]c^(12/7), H7=[T^7]c^(15/7), C=c/W, D=trunc_5/t5, and the leading ODE 7CD'-12C'D=-T^7 are checked on both sides (produce.py:39-47, check.py:120-128). a=[T^0]C=1/W, F=[T^2]C=Z/W, H=[T^1]C=1/W, b=1/t5 match the parent's abar, Fbar, Hbar, bbar. Inverses: `fi` (arithmetic.py:78-88) is monic extended Euclid against P7 with a product readback; the invariant of xgcd (:42-52) is a=r*a0+t*b0, so the returned t is the inverse; the checker's bezout (check_arithmetic.py:46-57) and `inverse` (:83-90) are an independent implementation and check.py:135-141 requires both product=1 and v equal to its own Euclid inverse for all 16 exported units (dL, W, t5, H7 and the twelve gap pivots). The inventory at check.py:130-133 is exact: `completion` does not register units, so no extra label exists. Pivot arrays: invert_matrix (arithmetic.py:209-233) registers each Gauss-Jordan pivot as unit gap{h}:{j} and checks both products; check.py:271-284 replays the same first-nonzero-row elimination on the exported matrix and requires the pivot at step j to equal the exported unit, tying each array to its actual use. No embedding, no parameter inversion (`scalar`/`value` raise on non-constants), and check.py imports only json and check_arithmetic.

## B. Gaps 1, 2, modified 3, middle 4

CONFIRMED. Operator `op` (produce.py:51-55) and `oper` (check.py:210-212) are 7CV'-(12-h)C'V+(7-h)AD'-12A'D; at h=6 the coefficient is -6, the mandated correction. Forcing (produce.py:73-78, check.py:222-224) is sum_{i+j=h}((7-i)A_i B_j'-(12-j)A_i'B_j), i.e. the parent's (5) with j=h-i; the checker rebuilds it from its own band extraction of the source A and B (check.py:197-206), not from the packet. Upper solve: I verified [T^{j+2}] of op(h,0,qT^j) is (7j-3(12-h))q because C is monic, so the pivots at :61 are right; 7j-3(12-h) is never zero for j<=4, h<=4 or j<=2, h>=5, and both sides test this (:62, check.py:240-242). The residual check :67-68 also enforces the T^5/T^6 rows that must cancel. Matrix M column j = ([T^1]r_j,[T^0]r_j,7a V_j(0)-12b A_j(0)); M(A0,A1,A2)=(-c1,-c0,rho) is exactly the full band equation plus the parent's rho=7a V_lin(0)-12b U0, with V_lin=V-V_part (produce.py:105-107, check.py:293-294). Upper uniqueness (nonresonant pivots, support <=4 checked at check.py:244,253) plus invertibility of M (verified two-sided, :262-268) identify this finite solve with the parent's Phi_h in both directions; rho=X_h literally at :296 (var(h-1) at produce.py:92). Modified gap 3: target -2T^6 attached to the j=2 basis vector (:84), full target -2 q T^6 with q=xyz[2] (:102), U=-q (:112, check.py:303), and l=[S]Vpar-U[S^2]Dpar (check.py:304) is the normalized v1-uF. Middle gap 4: I rederived (M^-1)_{2,2}=dU2/drho=(49/15-3)T7 coefficient=(4/15)H7 from the parent's Phi_4 numerator; the code checks it (produce.py:96, check.py:298), pins U2=0 through rho=-(15/4)H7^-1 xyz0[2] (:98) and verifies U2=V4=0 (:109-110, check.py:299-300); the v0-U d1 shift is checked against literal source coefficients (check.py:301). Both quotient maps are verified on the artifact as ring identities in B[X1,X2,X3,z]: forward M*xyz=(-c,rho) and mate=part+sum xyz_j V_j (check.py:290-292), reverse rho subtraction and X_h readback (:293-296). Note the 'critical UNshifted D_i' and shift checks are identities of the checker's own operator or of the literal A structure; they document, they do not discriminate (see G).

## C. Critical 5, late 6, ell 7

CONFIRMED. Gap 5 (produce.py:121-126): d0=[T^2] of the gap-2 band =[S^0]Dpar, y=z-U d0, A_part=yT, target -2zT^5, no fixed term, variation A=1 (k2). The y term of the operator is y(2TD'-12D), so [T^i] is (12-2i)yD_i with unshifted D_i; the k2 derivative is the only shifted index; check.py:316-320 spells this out. The T^5 row is the resonant j=3 slot (7*3-21=0) and must cancel automatically; both sides enforce it through the T>=2 residual raise. Lambda (produce.py:139-145, check.py:336-341) is F[T^6]P-(1/2)[T^5]P with P=C^2 int(R C^-2), C^-2=W^2 c^-2, truncation 6 sufficient for [T^5],[T^6]; the explicit accepted Lambda, not a Euclid completion. Gap 6 (:127-131): fixed U^2T^3 retained in V before the solve (the `upper` seed), target -U^2T^5, operator -6; I checked the gap-6 T^5 row 3U^2+W6_5=-U^2 holds with W6_5=-4U^2 from 16r's Q_3 constant, so the fixed term is forced and verified rather than free. Gap 7 (:132-134, :159-162): A_var empty, target_var -T^4, column = (L1,L0), and V_var is checked literally against T^2+(F/2)T+(6H-F^2)/10; E=value, and check.py:353-357 repeats both. For all three columns: lambda1 c1+lambda0 c0=1 is checked (produce.py:148, check.py:334); `completion` (arithmetic.py:240-248) is the parent's ordered Euclid on (P7,c1) then (gcd,c0), with the zero-input cases correct (c0=0 with c1 a unit gives l0=0; c1=0 gives l1=0). value=-lambda.base, Psi=c1 base0-c0 base1, and I verified by hand that base+value*col equals (-l0 Psi)T+l1 Psi, which produce.py:153-155 and check.py:351-352 require. No equation is omitted: the upper rows are enforced by residual_pair, the two low rows by the pair identity.

## D. Ahat/Bhat, residuals, Jacobian slots, inverse chart, retained relations

CONFIRMED. Producer Euler (produce.py:191-206) is the parent's (3) term by term with aa[2],aa[1],aa[0]=fhat,hhat,khat and [T^{j+2}] of -E T Pihat-T Pihat^2; resonant slots raise only if nonzero and the two kernel slots stay zero (gauges). The checker's assembly (check.py:172-189) is written from the Jacobian contribution (l A_k'B_l-k A_k B_l') with k<=2, which I expanded and found identical to Q_j without copying it; then B is compared whole. I expanded T Pihat^2 and -E T Pihat: the gap-3, 5, 6, 7 components are 2U S T^6, -2zS T^5, -U^2 T^5, -E S T^4, exactly the band targets used; the gap 8-12 targets are upper rows solved by Euler and verified as zero by the 80-slot loop (check.py:365-372: 17+15+...+3=80, j>=2 must vanish). The 32 residual slots (:379-389, produce.py:220-225): h<5 zero, h>=5 equal (-lambda0 Psi, lambda1 Psi) at [S^{14-h}T^1],[S^{16-h}T^0]; the 16 low slots are literal K rows with the two scale targets carried as interface names 'K1_0-U*s^5','K0_0-s^7' (odd in s, so they cannot sit in B[X,z]; this is the parent's own separation). Envelopes: A (7,5,3,1), B (12,10,8,6,4,2), Dpar/Vpar/Kpar S-degrees, weights of U(3),E(7),Pihat(7),Psi(5,6,7),K1_i(14-i),K0_i(16-i), gauges k(0)=0,[S]B3=0,B0(0)=0 all checked (check.py:157-169,190-195,384). Inverse chart (:390-401): S=P q^3-z q^2+U q, T=q^-1 with P in the S slot and q in the T slot; I verified the scaling P=s^3 p, q=zeta/s, z=s^2, U=su from the parent's S=p zeta^3-zeta^2+u zeta, so negative-q cancellation is the accepted condition on both complete polynomials, not a sample. Guard (:404-406): [S^7]Kpar=1/W, [S^12]Bhat_0=1/t5, W t5 * W^-1 t5^-1=1 in B with the s exponents -3-5+8=0 by hand; no s=1 anywhere. The 19 names at produce.py:255-258 and check.py:94-95 are Psi5-7, K1_1..6, K0_1..8, K1_0-U*s^5, K0_0-s^7; empty lists keep zero slots (wire_p({})=[], poly([])={}).

## E. Python text correctness

CONFIRMED on text read; nothing was run. Every imported name exists (produce.py:7-9 against arithmetic.py; check.py:8-11 against check_arithmetic.py); module globals P7/UNITS/WIRE_TERMS are reset in build() and MODULUS is set before the first F call (check.py:110 precedes :113). Zero polynomials: add/mul/product drop zero coefficients, so no zero term ever reaches the wire, and the checker rejects zero terms (check.py:81); `field([])` returns the zero tuple; `fi` on zero reports nonunit; `completion` handles zero column entries. Rational arithmetic is Fraction throughout; wire strings are canonical num/den with positive denominator and '0/1' for zero, and the parser (:40-63) rejects leading zeros, '-0', unreduced, zero or negative denominators, and checks length <=4096 before int(), under the process guard 4300 set at authority.py:32 before any parse; JSON numbers, floats and constants are refused by the parse hooks (:21-29). Sorted sparse terms (arithmetic.py:277) match the strictly increasing tuple test (check.py:81). Bounds: 4096 terms, 50000 aggregate, exponents 0..16, 15 MiB (read cap+1 byte, :25-27) on both sides; the aggregate count is over the same 8+8+3*8+2*14+8+19 polynomials on both sides, so a producer-accepted packet is never rejected by the checker's aggregate cap. Loops: 80-slot enumeration, K1 range 7, K0 range 9, gaps 1..7, and the 32-slot pairs are all bounded exactly. The `fixed` dict is never mutated (the solve rebuilds V by add). Operator precedence at check.py:46 and :81 parses as intended. Intermediate growth (chartS^12 in the inverse-chart check, the Euler recurrence) is uncapped inside the process and remains an external wall/CPU/memory obligation, as the report says.

## F. CLI interlock versus ROOT wrapper obligations

GAP (entry contract), not a mathematical defect. Established by the code: Linux and 'Amazon EC2' DMI before hashlib/json/Fraction (authority.py:11-19); fixed argv shape with a registered tag and a 64-hex authorization pin (:20-28); guard 4300 set before any coefficient parse (:30-32); authorization bytes <=64 KiB, hash equal to the CLI pin, duplicate keys refused, exact key set, tag/REGISTERED/ROOT-CAPRUN, literal limits, five-file inventory with each file hashed and compared (:44-65), interpreter binary hashed against python_sha256 (:71-73); producer exclusive create 'xb' and 15 MiB cap (produce.py:276-280). Not established: (1) sibling imports. produce.py:2 and check.py:2 do `from authority import authorize` at module top, and arithmetic/check_arithmetic are imported later by bare name; this relies on sys.path[0] being the script directory. Under `-I` on 3.11+ (which implies -P) neither the script directory nor the cwd is on sys.path and the import fails; PYTHONPATH is ignored under -E/-I. The report and code do not state the interpreter flags. (2) Directory exclusivity. With the script directory on sys.path, any extra module file there (fractions.py, json.py, hashlib.py) shadows the stdlib; authority checks only the five named files, never the directory listing. (3) Pin timing. authority.py and the entry script are already loaded when their own bytes are hashed; arithmetic.py and check_arithmetic.py are hashed before import (build()/check_file() import them after authorize), which is the correct order for the mathematical modules. (4) Registration authenticity. The JSON is compared only to a CLI-supplied hash; a self-authored JSON passes. The code says so (:74-76); authenticity is entirely the wrapper's. (5) Native closure. native_inventory_sha256 is format-checked only (:66-70); the stdlib is not pinned. (6) Artifact binding. Neither CLI prints the SHA-256 of the bytes it wrote or checked, so the FORMED/CHECKED strings are not bound to an artifact. (7) Input immutability and the 600 s/500 s/2 GiB/16 MiB aggregate limits are external. Smallest code repair versus contract gap: (1),(2),(6) can each be closed in a few lines (see H); (4),(5),(7) are wrapper obligations that no code in the box can close.

## G. Fifteen prospective controls

CONFIRMED as specifications, with two attribution corrections. Actual discriminating branches: 1 the producer's own whole-Euler/band equality (produce.py:207-214) or the checker's independent Euler B (check.py:189) and residual_pair at :328. 2 NOT the N identity at check.py:316-320, which is an identity of the checker's own operator and cannot fail on any packet; the branch is 'actual affine base' (:332) or the T>=2 raise at :328. 3 residual_pair at :246 (forcing alone has T^2..T^6 terms) then :290. 4 'literal A' (:167) and :301. 5 :325 then the T^5 row via :328. 6 :329 raises on the surviving T^4 before :357, unconditionally since Q2=1. 7 array(…,3) at :376 rejects, but that is AFTER the band loop and Jacobian, not 'before mathematics' as CONTROLS.md line 38 says; the early interface test at :96 checks names only. 8 :137, unconditional. 9 :116, unconditional. 10 :383 or :376. 11 :96, early. 12 :13-19. 13 :81. 14 :26-27, :71-75, :79. 15 :41-63 and the parse hooks. Zero-to-zero fallbacks are stated for 3, 4, 5, 10; 6, 8, 9 are unconditional as claimed. All fifteen are full-source mutations of a future artifact, not toy objects; none is a harness, fixture or executed evidence, and the 600/500/2 GiB/16 MiB numbers are unmeasured.

## H. Minimal fixes and regression burden

1. Import/flag contract (F1, F2): either the wrapper runs `python3 -E -s -B <dir>/produce.py …` from a fresh directory holding exactly the five pinned files, or two lines are added at the top of produce.py and check.py inserting os.path.dirname(os.path.abspath(__file__)) into sys.path so that `-I` works; optionally authority.py lists the directory and refuses any entry outside FILES. 2. Artifact binding (F6): print sha256 of `raw` beside the verdict string in both main() functions. 3. CONTROLS.md items 2 and 7: name the real discriminating branches. Regression burden: any edit to a code file changes its hash, so the future registration JSON must re-pin all five files, this review's line citations shift by the inserted lines, and the mutated-producer control 1 must be pinned against the new producer hash; no mathematical re-review is needed for the H1/H2 lines beyond reading them. A stronger checker that rejects a valid packet was not found; the only liveness risks located are the import failure under `-I` and an aggregate-term or decimal-length overflow at formation, both of which fail closed.

## I. WHOLE/OPEN/collision checks and authoring attestation

WHOLE: all 16 inputs read whole as itemized in section 0; this report was reread whole before the marker. OPEN(S) RAISED: none; the only quantities left are the F1/F2/F6 repairs and a separately authorized bounded formation, neither a new canonical OPEN. COLLISIONS: status EMPTY; own-only check, this lane wrote exactly xmodel/f10-r2-reconstruction-code-gate-fable5-20260910.md and box/f10-r2-reconstruction-code-gate-fable5-20260910/input_custody.md, both absent at 09:31:30Z, no corpus scan. Authoring: every byte of both owned files was written by apply_patch; no Write/Edit tool, no shell output redirection, no helper, no source edit, no execution, no network. No Seal, no charge_basis, no follow-on, registration or execution authority. Timeline: hashes 09:31:30Z, skeleton 09:31Z, body written before the 09:48:30Z reserve, budget end 09:51:30Z, never reset. The marker below is the only standalone marker in this file and nothing follows it.

<!-- BODY-END -->
