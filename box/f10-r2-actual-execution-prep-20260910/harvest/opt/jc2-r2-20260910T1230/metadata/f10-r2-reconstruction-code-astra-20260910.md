# Static faithful r2 reconstruction: producer and independent full-source checker

2026-09-10. STATIC IMPLEMENTATION, UNREVIEWED; zero scientific execution.
First action09:06:12 UTC; fixed cap09:33:12 UTC, reserve09:30:12 UTC.
Basis0d39df3c9fd69c939a8420c54d03228b9077777d. Accepted17zzj is consumed with
the mandatory corrected late operator **7CV'-6C'V+kD'**, never the printed-18.
Exactly the ROOT card and six pinned accepted reports are charged.

## Deliverable and mathematical interface

The owned box contains `produce.py`/`arithmetic.py`, independently authored
`check.py`/`check_arithmetic.py`, the common nonmathematical CLI interlock
`authority.py`, prospective controls and read scope. No coefficient JSON,
fixture, certificate, interpreter import, syntax/AST check or test was formed.
The source is prospectively executable, not runtime-validated. No source
UNIT/NONEMPTY, dimension, reducedness, root, solver or performance result is claimed.

Both implementations reconstruct the literal septic over Q at tau=2/7,
then work in the WHOLE seven-coordinate B=Q[Z]/P7. Producer binomial-series
arithmetic and checker differential-series recurrence are separate. Independent
univariate Euclid and complete-basis product checks reconstruct dL,W,t5,H7
inverses. Every matrix pivot is a B constant, explicitly exported and tied to
its actual use by the checker. No earlier-parameter polynomial is inverted;
no field embedding, generic minor, numerical sample or scale cover is used.

Formal coefficient variables are X1,X2,X3,z with weights1,2,3,5. The source is

    Ahat=S*T^3+(S*Dpar-U)*T^2+(z-U*Dpar+S*Vpar)*T+Kpar,
    Pihat=z*T-U*T^2+S*T^3.

The full Euler mate has Bhat5=S² and the two unchanged zero gauges. Its five
upper inversions use exactly -E*T*Pihat-T*Pihat²; the actual low targets are
excluded ONLY from these inversions. The literal final rows are

    Psi5,Psi6,Psi7; K1_1,...,K1_6; K0_1,...,K0_8;
    K1_0-U*s^5; K0_0-s^7,                 z=s², s invertible.

All19 named slots remain even when zero/dependent. Original coefficients are
A_j=s^(j-3)Ahat_j, B_j=s^(j-5)Bhat_j, u=U/s, ell=E/s³;
d=Dpar/s,v=Vpar/s²,k=Kpar/s³ and Yi=Xi/s⁸. Thus the original residuals are
the normalized ones times s^-6 or s^-7, and Psihat=s^10 Psi. The guard is
omega=W*t5*s⁸, with a=1/(W*s³),b=1/(t5*s⁵); their product is checked in B
and the Laurent exponents cancel exactly. No s=1 replacement occurs.

## Exact band matrices and both maps

For each gap h the source code forms every actual mixed forcing term and

    Lh(A,V)=7CV'-(12-h)C'V+(7-h)AD'-12A'D.

The upper solution has fixed rational pivots7j-3(12-h). For gaps1,2,3,4,
let Vj solve the upper equations for A=theta^j, j=0,1,2. At modified gap3
include target-2theta^6 for j=2. The exported matrix M has column j

    ([theta] residual_j, [1] residual_j,
     7a*Vj(0)-12b*[1]theta^j).

Let Vpart be the actual upper solution at A=0 with forcing -Wh, and let
c=(c1,c0) be its residual. The implemented identity is

    M*(A0,A1,A2)^t=(-c1,-c0,rho)^t,
    V=Vpart+sum Aj*Vj.

The checker verifies each entire operator polynomial, BOTH M-inverse products,
every particular term, and rho=7a*(V-Vpart)(0)-12b*A0. Hence rho=Xi exactly
for gaps1..3, not a fresh free coordinate. Upper uniqueness and these identities
identify this finite matrix solve with the parent's prescribed Phi map in BOTH
directions, including its modified third map. At gap4 it additionally checks
(M^-1)_(2,2)=(4/15)H7; this forces A2=V4=0 and determines rho uniquely.
The actual v0-U*d1 and v1-U*F shifts are checked against reconstructed source
coefficients, not inferred from a homogeneous kernel.

Critical gap5 retains y=z-U*d0, target-2ztheta^5 and every UNshifted D_i.
Its left inverse is the literal accepted Lambda=F*P6-P5/2. Gap6 retains the
fixed U²theta³ mate coefficient, target-U²theta^5 and operator coefficient-6.
Gap7 is affine in E and checks the literal theta²+(F/2)theta+(6H-F²)/10 column.
Late/ell completions use ordered Euclid on (P7,c1), then (gcd,c0), reduced in B.
For all three columns, lambda1*c1+lambda0*c0=1 is checked, the scalar is
-lambda·base, and the retained compatibility is c1*base0-c0*base1.
The full pair equals (-lambda0,lambda1)*Psi; this determinant-one identity is
the reverse-map certificate and does not require a chosen entry to be a unit.

## Independent checker coverage and wire contract

The checker imports neither producer file. It independently forms P7, all
inverse products, A from d/v/k/U/z, and COMPLETE B by assembling the coefficient
contributions to the Jacobian rather than trusting producer Qj. It verifies
all80 potential Jacobian slots and all32 residual slots: ten leading/early/
middle rows vanish, three pairs read back as the displayed Psi multiples, and
all16 lower slots are literal K rows with the two explicit scale targets.
It also checks every band matrix/affine reverse map, all unit witnesses,
weighted supports, A degree bounds(7,5,3,1), B bounds(12,10,8,6,4,2), and gauges.

Full inverse-polynomiality is checked independently by substituting
S=P*q³-z*q²+U*q,T=q^-1 into BOTH complete polynomials and requiring every
negative q coefficient to cancel. Here P=s³*p and q is the original inverse
variable divided by s; z is still the coefficient s². This is the full
accepted inverse chart, not a boundary sample or an added source condition.

Wire schema `r2-reconstruction-19-v1` has exactly format/job_tag/interface/
field/source/bands/rows. A polynomial is a sorted list `[exponents, Bvector]`;
exponents are six decimal strings, Bvector exactly seven canonical rational
strings `numerator/positive_denominator`. P7 alone has eight rational entries.
Nested keys, array lengths, duplicate keys/terms, zero terms, canonical
fractions, exponents0..16,4096 terms/polynomial,50000 aggregate terms and a15MiB
packet cap are enforced. No mathematical JSON number, float, eval, pickle or
JavaScript coefficient round-trip is used. Empty lists retain zero rows.
The remaining1MiB of the prospective16MiB aggregate budget is reserved for
wrapper metadata/logging; ROOT must enforce the aggregate cap.

## Runtime perimeter, controls and exact limitation

Each CLI refuses non-Linux/non-EC2 DMI before JSON/hash/Fraction/scientific
imports and requires an explicit fixed job tag plus pinned REGISTERED
ROOT-CAPRUN metadata naming all five code hashes, interpreter/native inventory
and limits. An environment tag is not authority. The process-local decimal
guard is4300; coefficient components are limited to4096 characters before
conversion. ROOT's future unchanged wrapper must authenticate registration,
enforce native pins/PGID/CAPRUN and run dummy controls. No authorization JSON,
registration, wrapper change or worker is supplied here.

600 wall seconds/500 CPU seconds/2GiB/16MiB aggregate are prospective,
UNMEASURED ceilings, not a prediction. Python APIs and syntax were reviewed
only as text; actual compatibility, coefficient heights, success and resource
use are UNKNOWN. FIRST Fable code review is required before any science.
The complete checker obligations above are implemented as source, but none
has passed on an actual artifact; no runtime-faithfulness certificate exists.

CONTROLS.md specifies fifteen future single mutations, including wrong late
operator, critical index, dropped particular, wrong shift/fixed term, deleted
ell mate/Psi, corrupted inverse/final basis coordinate/largest coefficient,
s=1 targets and parser-size attacks. Zero-to-zero mutations are explicitly
not counted as controls. The W-inverse +1 and ell-column deletion controls
are unconditionally changed by the proved unit and theta² coefficient1.

## OPEN(S) RAISED

None new. Exact next prerequisite is FIRST independent static review, then
separately authorized bounded formation/readback and mutation evidence. No
execution, source decision or follow-on authority is granted by this packet.

## COLLISIONS

status: EMPTY

- NONE — exact own targets absent at first action; own-only check, no corpus
  scan. All unrelated dirty work and frozen parents remain untouched.

The terminal custody file records all current input/owned hashes, the full
report and expected transaction hashes, and all-writers-IDLE time. Own WHOLE
source/report/read-scope review precedes final publication. No test ran.

## Owned source pins

The following SHA256 refer to the final manually WHOLE-read source text;
documentary controls/read-scope and transaction pins are also in custody.

    authority.py        2a274ac4cf866762bc1f2734b3af8d515ff7be93c0fe0d4500e56dbca2ef093b
    arithmetic.py       fe9ab1abe9a96fc43b889e1bcb598f8e8c65fd194732c48f3542e13c476e0345
    produce.py          eeb3fe3bbbe232ffe080a2a0a5db2c1f3ac60b83b61dd7798ff56e2f97977b10
    check_arithmetic.py e5dd4bcb6114df2bdd79c8336583942c18d698d99fffd6f4b171e46a5d503d6e
    check.py            e6bc7cca0d9c776a898bda3f0648af883e17a675f2b81bba52031459f4c640a0

Own WHOLE audit and own-only OPEN/collision check complete. No unfinished
scientific writer remains; only documentary sealing/custody follows.

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `9113`.
- Body SHA-256:
  `55386ebfd6a578ba29943a41101e6c9c5042e43eeb87ca5f9002d806b6d4c2ee`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
