# f10-r2-runtime-gate-fable5-20260910 — FIRST Fable 5.1 static gate on the r2 source-reconstruction runtime caller

status: UNSEALED (findings A–G complete; adapter seals; no Seal or charge_basis authored)
opened: 2026-09-10 11:23:17 UTC
deadline: min(11:41:17, 11:42:00) UTC = 11:41:17 UTC; three-minute reserve from 11:38:17 UTC
mode: STATIC ONLY — manual text/hash reading; zero subprocess/import/AST/compile/syntax/test/CAS
writes: apply_patch only; owned targets xmodel/f10-r2-runtime-gate-fable5-20260910.md and box/f10-r2-runtime-gate-fable5-20260910/

## 0. Custody — 13 immutable input snapshots (hashed BEFORE any body read, 11:23:17 UTC)

| sha256 | file | read |
|---|---|---|
| 002ff55c5d60e843011f988562d2e38a3cc36e1f497c5dd7e18e540e7fc7f8e7 | f10-r2-runtime-astra-20260910.md | WHOLE |
| 4838e40051bd19d19c35095eac2ee50de7d11d905b2da403a459c25d91f3a749 | dispatch.py | WHOLE |
| 1e3bea3ad995ca64780f77b33792ed09c9843fb3925aae5f64c7a2abcaa0ecde | probe.py | WHOLE |
| 2c06f0580356172c05ec5e860051d8a31bb6f3dc01a0b0d48075c4680c5180b5 | CONTRACT.md | WHOLE |
| a7dd5846c5ab7bda1bc7020ee4c203b10f0adca90bbd119ec6c61416f58b7304 | DISABLED-REGISTRATION.json | WHOLE |
| efe294852467361e49fe0d77bdfa26c4a121f6aae6ce7759517b0773a66fe642 | READ-SCOPE.md | WHOLE |
| 2a274ac4cf866762bc1f2734b3af8d515ff7be93c0fe0d4500e56dbca2ef093b | authority.py | WHOLE |
| eeb3fe3bbbe232ffe080a2a0a5db2c1f3ac60b83b61dd7798ff56e2f97977b10 | produce.py | hash WHOLE; body EXCERPT lines 1..4, 270..285 |
| e6bc7cca0d9c776a898bda3f0648af883e17a675f2b81bba52031459f4c640a0 | check.py | hash WHOLE; body EXCERPT lines 1..4, 407..416 |
| 947bb434fd80ce8cbe2df01c8f877715aa3e8e82afbf90aa0e1212f5ac73f9e7 | f10-r2-reconstruction-code-astra-20260910.md | WHOLE |
| 3e9a06cf3aced19aa6a8ab5c742c57ffc1d1da21bb8a0270030a83285720502b | f10-r2-reconstruction-code-gate-fable5-20260910.md | WHOLE |
| 0562b82afa2b6f6da1e2c488d261fc65f22f705629c12446bbfeaf85bdccbef2 | f10-middle-real-runtime-gate-fable5-20260910.md | WHOLE |
| 4435279df8f987c667ebc2fae8e4bd987916032561cb3faf1e07a57789d196c2 | run_capped.py | WHOLE (accepted unchanged CAPRUN; charged only for caller API/telemetry compatibility) |

All 13 sha256 values above equal the charged list verbatim (sha256sum at /tmp/jc2-lane.JIfJVO/inputs, 11:23:17 UTC, before any body read). Line counts: dispatch.py 509, probe.py 68, authority.py 77, run_capped.py 1198, CONTRACT.md 136, DISABLED-REGISTRATION.json 36, READ-SCOPE.md 16, produce.py 285, check.py 416.

## 1. Findings A–G

Every statement below is a hand reading of the charged text. Line numbers refer to dispatch.py unless named otherwise. Verdict summary: no REFUTED item; no blocking source, API or syntax defect; acceptance is STATIC ONLY with the ROOT obligations in G.

### A. External ROOT registration, command inventory, environment, directories, native closure — CONFIRMED (obligations listed)

Disabled input: `Batch.__init__` 115-118 requires a canonical, root-owned, world-readable, write-bit-free 0444 registration (immutable + exact-mode test), ≤64 KiB strict-pairs JSON, schema `f10-r2-runtime/v1` and `enabled` True; DISABLED-REGISTRATION.json has `enabled: false`, so it dies at 'disabled ROOT registration' before any `emit`, and every other malformation raises KeyError/TypeError into the `except Exception` STOP_NONDECISION at 493-494. Eleven commands: 217 requires the key set to equal LABELS (5 refusals, valid, startup-produce, startup-check, dummy, produce, check) and 339 requires each freshly built CAPRUN vector to equal the registered one before Popen. Environment: 121 requires the registration's `environment` AND the live `os.environ` to equal exactly {PATH=/usr/bin:/bin, LANG=C, LC_ALL=C, TZ=UTC}; CAPRUN is launched with `env=ENV` (353) and setpriv/python inherit it; because LC_ALL is set, CPython skips PEP 538 coercion and injects no LC_CTYPE, so the four-key equality is satisfiable. Outer vector: 180-181 builds `[python,-I,-S,-B,dispatcher,--registration,REG]` from the resolved interpreter and `__file__` and requires equality with both the registered `outer_argv` and the live `/proc/self/cmdline`; ROOT must therefore exec with argv[0] equal to the canonical interpreter path. Science vector: 331-337 is exactly `setpriv --reuid U --regid G --clear-groups --no-new-privs -- python -E -s -S -B SCRIPT` + the eight authority arguments, so `-S` leaves the script directory on the child's sys.path and `from authority import authorize` (produce.py:2, check.py:2) resolves; probe.py:25-28 additionally inserts only the listed five-file directory before importing authority. Directories: 175-179 require science and wrapper to be distinct root 0755 directories with root-owned, non-g/o-writable, o+rx ancestry, cwd == wrapper, dispatcher and probe as wrapper siblings; 241-243 (every revalidate) require the science listing to be EXACTLY the five names and the wrapper listing EXACTLY dispatch.py/probe.py, science files exactly 0444 with the literal SHAs (246-248), and 186-187 bind each science pin by full path (this closes the earlier middle-real gate's basename-binding gap). Host: 123-132 DMI vendor 'Amazon EC2', `i-hex` board asset tag, hostname, boot_id, PID namespace link, cgroup path under /sys/fs/cgroup, `/proc/self/cgroup` == '0::'+relative path, and `memory.max` == '2147483648'; 235-240 repeat all of these on every revalidate.

Six-field native manifest (reviewed as NEW code, not accepted by shape): 188-194 pin the manifest by SHA, cap it at 1 MiB, require schema `f10-native-closure/v1`, key set exactly {schema, files, directories, aliases, absent, python_path}, `python_path == sys.path` of the -I -S -B outer process, and 0<files≤3000, 0<directories≤1000. Revalidate 244-265: every pin and every native file must be 64-hex and hash-equal, root-owned, o+r, and write-bit-free unless it is a native file or the python/setpriv role (then only g/o-write-free); every listed directory must be root-owned traversable with `sorted(os.listdir)` EXACTLY equal to its entry list and every child present in files, directories or aliases; a real directory can only satisfy that as a `directories` key (files fails S_ISREG, aliases fails `resolve()!=self`), so the closure is recursive by construction; aliases must resolve to a listed target; absent paths (ZIP) must not lexist; every sys.path entry must be a listed directory/file/absent; both /bin/ps and /usr/bin/ps must be files or aliases; 272-278 require every mapped file of the dispatcher (271), the live CAPRUN runner (356) and the live science child (372) to be a pinned file, with non-canonical mapped paths pinned as aliases. Since the stdlib directory is a sys.path entry it is fully inventoried including `__pycache__` pyc bytes; with `-B` nothing is written. CONFIRMED as internally consistent with no false-accept path. Obligations, not defects: (1) the 3000-file bound must hold for the actual recursive stdlib closure (an Ubuntu python3.x stdlib without the test-suite package is on the order of two thousand files; ROOT must count, and a test-suite or multi-variant pyc install would overflow to a fail-closed STOP); (2) both ps paths must exist (merged-usr); (3) `canonical()` forbids spaces, `\`, `:` and `+`, so cgroup paths with systemd escapes and non-resolved paths are unusable; (4) `directory()` rejects any 1777 ancestor, so neither the mount nor the durable parent may sit under /tmp.

### B. Authority integration, five refusals, sentinel, startup refusals, orphan control — CONFIRMED

argv: the tail at 331-333 is `--registered-job TAG --authorization AUTH --authorization-sha256 HEX --output|--input TARGET` (8 items); produce.py/check.py get `[script]+tail` = 9 argv entries as authority.py:88-93 requires (`--input` iff role check, matching `mode=='produce'` → `--output`); probe.py:20-29 requires exactly 14 argv, rewrites `sys.argv=[argv0]+tail` (9) and calls `authorize('produce')`, so `--output` is used for all probe labels. Authority bytes: `authorize()` 301-311 builds exactly the six accepted keys (job_tag, status=REGISTERED, authority=ROOT-CAPRUN, files=five literal SHAs, runtime={python_sha256 from the registered python pin, native_inventory_sha256=sha(manifest)}, limits=the four strings identical to authority.py:121-122), writes them canonical-sorted-compact+newline by `emit` (O_EXCL, fsync, fchmod 0444, ≤64 KiB = authority's read bound), re-checks immutability, and 341 re-hashes the file directly before Popen. Refusal trace through authority.py: refuse-status → line 120 'no registered ROOT CAPRUN contract'; refuse-caps ('499') and refuse-inventory (check.py deleted) → line 124 'registered limits/source inventory'; refuse-source (produce.py zeros; authority.py and arithmetic.py hash first in FILES order) → line 133 'source pin produce.py'; refuse-hash (valid file, supplied zeros) → line 115 'authorization bytes/pin'. Each is a SystemExit string, so stderr is exactly 'REFUSED: …\n', exit 1, no stdout, no payload, which is exactly what 409-416 demand together with CAPRUN NORMAL_EXIT/1; the five strings match the `reasons` dict at 410-412 letter for letter. Valid: all five hashes, runtime pins and the interpreter hash pass, `sys.argv[8]` is returned, probe.py:34-41 creates the payload O_EXCL and writes POST_AUTHORIZE with pid/pgid/uid/gid/start_ticks/boot/ns; 419-421 require the event name, registered uid/gid and empty streams. WRONG-JOB: 330 substitutes the tag for `startup-*`; authority.py:88 refuses 'explicit registered tag required' before any file, hash, JSON, int-guard or input read; check.py:411 and produce.py:271 call `authorize` as the first statement of main, and the absent `--input` path is never opened (425). Order in `execute()` 407-455: 5 refusals → valid → startup-produce → startup-check → dummy → ONE producer → ONE checker only after the producer's literal verdict; `used_labels` (314) forbids any second launch of a label. No fixture, fake input or claimed PASS exists; every expected outcome is a `need` on live CAPRUN output. Scope note: whether produce.py/check.py import arithmetic modules at module level before main() lies in lines outside my charged excerpt; the accepted code gate (charged whole) states they are imported inside build()/check_file() after authorize, and I rely on that statement rather than re-verifying it.

### C. CAPRUN compatibility, capture, dummy, quiet — CONFIRMED with one fail-closed race

Interface: 343-347 use `--wall-seconds/--cpu-seconds/--rss-bytes/--rss-sample-seconds 0.05/--term-grace-seconds 1/--stdout-file/--stderr-file/--telemetry-file/--cwd SOURCE --`, all defined in run_capped.py parser() 143-177 with the leading `--` stripped at 1186-1187; `preexec_fn=file_limits` gives CAPRUN and every descendant RLIMIT_FSIZE 15728640 and core 0; CAPRUN adds RLIMIT_CPU soft/soft+1 (639-659) and `start_new_session` (827). Telemetry binding 388-397: schema, `argv_sha256` recomputed with the identical json.dumps parameters as canonical_argv_hash (184-188), argv_count, runner_exit_code == Popen returncode (CAPRUN exits `int(runner_exit_code)`, 1177), cwd == science dir, caps equal to the profile (5.0 == 5 holds), pid == pgid with `boot=<registered>;start_ticks=` prefix (linux_identity 288-312), leader-reaped test by identity difference (394-395), and stdout/stderr path/sha/bytes against the files. Identities: `identity()` 88-108 records argv, uid/gid quadruples, CapInh/Prm/Eff/Bnd/NoNewPrivs, limits, cgroup, mapped files; the runner is captured immediately after Popen (Popen returns only after the exec succeeded, so cmdline == command), and the science child is found through CAPRUN's `task/<pid>/children`, accepted only when cmdline == the python vector and it is its own group leader (setpriv execs in place, so there is no inner group), with registered uid/gid, namespace, NoNewPrivs=1 and zero caps (369-373). Fast controls may miss the child; `actual_python_live_observed` is recorded honestly and 403-404 turn a miss into a typed GAP only for produce/check, no retry. Dummy: probe.py:42-64 writes parent_started, forks, child ignores TERM and writes child_ready_ignoring_term, pipe handshake, parent writes parent_exit_pending and `_exit(0)`, orphan waits for reparenting, touches 64 MiB, writes orphan_allocated_64MiB, pauses; the handshake orders the four appends on the shared file description. CAPRUN keeps the zombie leader as anchor, samples group RSS > 33554432 → before-term MATCH → SENT 15 → 1 s grace → before-kill MATCH → SENT 9 → quiet → reap, RESOURCE_CAP/rss (855-913, 431-518); 426-441 require exactly that sequence, one check and one send per stage with the leader's pid/pgid/start identity, event order and identity equalities, all pgids equal to the leader and the recorded maximum above the cap. Quiet: `group_quiet()` (280-284, cgroup.procs, zombies tolerated) runs at 384 before telemetry, stream hashes, payload or event reads, and again before durable copy.

GAP (liveness, non-blocking): `identity()` catches only FileNotFoundError. A process that dies between the `children` read and the `/proc/<pid>/maps|cmdline|status` opens can return ESRCH (ProcessLookupError), which escapes 107-108 and the `except FileNotFoundError` at 375-376 into STOP_NONDECISION — a false STOP, never a false positive. Smallest repair: catch `(FileNotFoundError, ProcessLookupError)` at 107. A CAPRUN that ignores the 15 s SIGTERM wait (381-383) leaves TimeoutExpired → STOP_NONDECISION → group_quiet fails → STOP_CUSTODY_INCOMPLETE with ROOT recovery, as designed.

### D. 600/500/2 GiB/16 MiB envelope — CONFIRMED (documentation discrepancy noted)

`self.begin` is taken before any hashing (113) and `cpu_start` from the cgroup's cpu.stat usage_usec (133, 225-227). PROFILES 32-33 are literal: 9×(5,3), produce (300,239), check (210,169): wall 45+300+210 = 555, hard child CPU 9×4+240+170 = 446, leaving 45 wall and 54 CPU of 600/500. `budget(wall+15)` (229-231) is demanded before authority creation (317) and again directly before Popen (341): monotonic elapsed + wall + 15 < 600, now + wall + 15 < the registered mathematical deadline, and sampled cgroup CPU < 480; 387 demands elapsed ≤ 600, now < deadline and cgroup CPU ≤ 500 after every return; 378 aborts a run whose CAPRUN has not returned by wall+15. Nothing resets or retries. Overhead: revalidate re-hashes every pin and native file 34 times (1 in __init__, 3 per run × 11) and the sampled CPU includes the dispatcher and CAPRUN's 20 Hz `ps` polling, so an observed overshoot of the 555/446 arithmetic is possible only in the direction of an earlier fail-closed STOP. Discrepancy: CONTRACT.md 107-108 says the caller 'stops launching at 480', but `self.budget()` at 377 runs inside the live loop, so an in-flight phase is also terminated when sampled CPU reaches 480 (SIGTERM to CAPRUN, typed STOP). The code is the stricter and safe reading; the contract sentence should be aligned, no code change needed. Per-child hard limits and ROOT's independent external stop remain the only hard quotas, exactly as the contract states.

### E. Single tmpfs, partition, freeze, checker binding — CONFIRMED

195-205: mount/authority/frozen root 0755 with checked ancestry, writer registered uid/gid 0700, three distinct empty direct children, exact tmpfs mountinfo record at the mount path, and science/wrapper not under the mount; 267-269 re-check identity and `f_blocks*f_frsize ≤ 16777216` on every revalidate. 206-210 require fd 1 and fd 2 to be regular files on the mount device whose /proc/self/fd links are literally output_mount/outer.stdout and outer.stderr with matching inodes. `size_budget()` (286-299) is stat-only: writer/produce.payload and frozen/artifact.json ≤ 15728640, everything else summed ≤ 1048576, no symlinks, run in the live loop every 50 ms and before/after each launch. Freeze 442-449: literal 'FORMED_UNCHECKED_19_ROWS_NO_SOURCE_OUTCOME\n', empty stderr, 0 < size ≤ cap, hash, absent target, `os.rename` inside the tmpfs, chown root, chmod 0444, syncdir writer and frozen, frozen 0555, hash re-verified through `immutable`. Checker 450-455: input hashed before (340) and after (386) the run through `immutable`, literal 'CHECKED_STATIC_CONTRACT_19_ROWS_32_RESIDUALS_80_SLOTS_NO_SOURCE_OUTCOME\n', empty stderr, and a binding record with input SHA, checker stdout SHA, science pins and root SHA. The packet is never json-loaded; `load()` is applied only to the registration, manifest, telemetry, sentinel and dummy events. The fifteen science controls are never executed or claimed (CONTRACT.md 131-134). Minor precision item: 205 excludes the science/wrapper directories from the mount, but the registration file and native manifest are not required to lie outside it; if placed there they would only consume the 1 MiB metadata reserve (fail-closed), never affect acceptance.

### F. Durable EBS custody — CONFIRMED

`durable_copy` 461-484 runs after `execute()` on success and on every caught STOP (497-498): group quiet, size budget, task deadline, `mkdir(0o700)` of the registered absent directory under a root-owned ext4/xfs parent with the registered st_dev (211-213), walk without following links, refuse symlinked directories/files, copy each regular file with 'xb', flush+fsync, chmod 0444, hash equality, inventory ≤ 200, CUSTODY.json (≤64 KiB by `emit`, with result, root SHA, all run records, file records and `science_outcome: NONE`), fsync of every durable directory bottom-up AND of the durable parent (483), final deadline check. Estimated inventory ≤ 11 files per run × 11 + outer streams + artifact + binding ≈ 125. Success path prints nothing after the copy (503-505). Failure paths: Batch never constructed → single JSON line and exit 2; execute failure → STOP_NONDECISION recorded in CUSTODY.json, exit 2; copy failure → single STOP_CUSTODY_INCOMPLETE line with `ROOT_terminal_cleanup_capture_required`, exit 2. No unlink, rmtree, unmount, worker stop/restart, cap raise or retry exists; the only signal sent is SIGTERM to the caller's own CAPRUN child (382). An external TERM/KILL of the dispatcher has no handler and leaves the mount uncopied for ROOT's original capture path, as CONTRACT.md 66-68 requires.

### G. Defects versus registration choices; acceptance scope

Concrete blocking source/API/syntax defect: NONE found on a whole hand read of dispatch.py and probe.py (real stdlib APIs with the used signatures; the resource module is imported; every `need` predicate parses as intended; dict insertion order gives the contract's refusal order). Non-blocking items with minimal repairs: (1) C — `identity()` should also catch ProcessLookupError (one token; prevents a rare false STOP); (2) D — align CONTRACT.md 107-108 with the live 480 cutoff (text only); (3) E — optionally require the registration and manifest paths to be outside the mount (one `need`). Satisfiable ROOT registration choices, not defects: exact four-key environment at launch; argv[0] equal to the canonical interpreter; `commands` reproducing the vectors at 343-347 byte for byte including '0.05' and '1'; canonical escape-free cgroup, ps and loader paths; integer durable st_dev; pure cgroup v2 membership with memory.max 2147483648; unlimited or ≥240 hard RLIMIT_CPU inherited by the dispatcher; mount and durable parent outside any 1777 directory; native closure counted under 3000 files/1000 directories including stdlib pyc and any mapped library, ZIP registered absent; both ps paths present; offset-explicit deadlines leaving custody time. Acceptance is STATIC ONLY: the nine preflights and both phases have never run, no manifest, coefficients, registration, controls or telemetry exist, and a future clean result means only RECONSTRUCTION_CHECKED_NO_SOURCE_OUTCOME, never basis/rank/source/JC2. No cap raise, unchanged retry, supervisor migration or implementation is proposed.

## 2. Verdict table

| item | verdict |
|---|---|
| A registration, 11 commands, env, -I -S -B / -E -s -S -B, 5-file/2-file directories, ancestry/0444, EC2/boot/ns/cgroup, six-field native closure | CONFIRMED; obligations (file bound, merged-usr ps, canonical paths, no /tmp) |
| B nine-argv authority, frozen fsynced authority, five literal refusals, sentinel, two WRONG-JOB refusals, orphan control, one producer/conditional checker | CONFIRMED; excerpt-scope reliance on the accepted code gate for module-level imports |
| C CAPRUN argv/cwd/PGID/boot/start/ns/telemetry, live capture, dummy four events/TERM 15/KILL 9, quiet before reads, mapping pins | CONFIRMED; GAP: ESRCH race → false STOP (one-token repair) |
| D 600/500/2 GiB/16 MiB, 9×5/3, 300/239, 210/169, 555/446, +15 reserve, 480/500 sampled, no reset/retry | CONFIRMED; contract text understates the live 480 cutoff |
| E tmpfs 16 MiB / 15 MiB / 1 MiB stat-only, outer streams, move-freeze 0444/555/sync, pre/post input SHA, no coefficient parse, 15 controls SPEC | CONFIRMED; registration/manifest-on-mount not excluded (reserve only) |
| F durable copy, hashes, fsync incl. parent, ≤200 files/64 KiB manifest, no post-copy stdout, typed STOP, no deletion/unmount/worker stop | CONFIRMED |
| G blocking defect | NONE; STATIC ONLY acceptance with the obligations above |

## 3. OPEN(S) RAISED

None. No new canonical OPEN; the remaining quantities are the three non-blocking repairs above (ROOT's choice) and the separately ROOT-registered first live batch, not authorized here.

## 4. COLLISIONS

status: EMPTY

- NONE — own-only check: this lane wrote exactly xmodel/f10-r2-runtime-gate-fable5-20260910.md and box/f10-r2-runtime-gate-fable5-20260910/input_custody.md, both absent at 11:23:17 UTC; no corpus scan; no other file touched.

## 5. Completion attestation

Timeline: hashes and absence 11:23:17 UTC; skeleton 11:23 by apply_patch; whole reads 11:24-11:27 (dispatch.py clipped and recovered in three bounded reads); this body and the custody file written by one apply_patch before the 11:38:17 reserve; never reset. Tools: sha256sum, cat, sed, wc, ls, date, apply_patch only; no Write/Edit tool, no shell redirection into any file, no helper, no source or charged-input edit, no execution, import, AST, compile, syntax check, test, CAS, network, AWS, SSH, Git, agents, corpus, shared, protected, other-lane or live file. Read scope: 11 inputs WHOLE, produce.py/check.py hash-WHOLE body-EXCERPT exactly as charged, no provenance link followed. No Seal and no charge_basis are authored; the adapter seals. The standalone marker below is the only such line in this file and nothing follows it.

<!-- BODY-END -->
