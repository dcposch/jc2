# Gate: standard F2 pair to three polynomial monomial-J receivers (Fable 5.1, 2026-09-06)

Bounded 25-minute hostile proof gate of `xmodel/d125-published-chain-discriminator-astra-20260906.md` (lane copy SHA-256 `9b439af2…`, body `e11c5d2c…`) and its `check.py` (`5de0795d…`). Lane inputs `/tmp/jc2-lane.106FqS/inputs`, all 16 charged files present. Status: **PROOF GATE ONLY.** No solve, classifier, source cut, ledger edit or new lemma. Verdicts are on the necessary-direction implication exactly as displayed; the missing reverse and attachment arrows are recorded, not used to refute.

## Verdict table

| item | verdict | one-line basis |
|---|---|---|
| q=5 and both quartic root systems | **CONFIRMED** | Def 6.2/Rem 6.3 make the starting corner type II.b; Thm 7.6(3) with weights 4 and 5 gives `en F=(4,16)` before `A1` is used; own full two-variable bracket and coefficient extraction reproduce both root systems uniquely |
| lower transport to `v_{1,-4}=0` | **CONFIRMED** (own reformulation below) | Cor 7.4 at the joint edge, Prop 7.3's `(ρ̃,σ̃)` chain verified direction by direction; GGV2 Prop 2.1 self-contained; Cor 1.6 not load-bearing |
| upper completeness (three cases) | **CONFIRMED** | Prop 8.2 quoted literally; six candidates; four exclusions replayed; unequal assignment forced |
| full polygon implication | **CONFIRMED conditionally** | conditional on the stated hypothesis (polynomial standard (3,5) pair of degrees 75/125 with the F2 triple), the already gated rectangle lemma, and the quoted GGV statements listed in §5 |
| inverse / Roy–Strinz attachment | **GAP, correctly declared** | every non-affine step is an automorphism of `K[x^±1,y]` only; nothing here returns a polynomial pair or a coefficient dictionary |

No first failed arrow was found in the necessary direction. Two producer arguments are replaced by cheaper exact ones (Prop 2.11(5) for uniqueness is not licensed at `ρ<0`; a direct order-at-zero computation is; the "both polygons have an edge at d" step follows from Prop 1.13 plus the Euler-operator identity for a monomial).

## 1. Quoted results versus new composition

Read in the pinned texts, statement and the proof passages named: GGV1 Thm 2.6 (with proof), Rem 2.5, Rem 2.8, Prop 2.11 (statement and proof of (1)–(3); note its preamble is `V0 = {ρ+σ>0, ρ>0}`), Def 4.3, Def 5.5, Def 6.2, Rem 6.3, Prop 1.13, Def 1.14 (aligned means cross product zero), Prop 7.1 and Prop 7.3 statements including the definition of `(ρ̃,σ̃)`, Cor 7.2 with the first half of its proof, Cor 7.4 ("mimic"), Rem 7.5, Thm 7.6(1)–(8), Prop 8.2 (statement and first page of proof through (8.16)). GGV2: the §2 standing paragraph, Prop 2.1 with its whole proof, Prop 2.2, Cor 1.6 statement. Algorithms paper: §2.2 generated-corner definition, Def 2.12, Thm 2.20(5)–(8) with (2.5), and the F2 row `(5,20) (1,0) (7≀5,2) k=1 m=j+2 n=2j+3`.

The producer's new composition is: the swap to `(x,y)=(v,u)`; the joint closest-lower-edge argument with the `P(x,0)` positivity trick; the unramified `x^{-k}` cuts; the ODE solutions; the successor exclusions at `(-2,7)`, `(-2,9)` and the second Prop 8.2 pass at `(-1,3)`; the half-plane hulls; and the involution `T`. None of these is a quotation.

## 2. Item-by-item attack

**(1) Type II licence and q=5 without A1.** Def 6.2 defines the starting triple by the unique regular corner with `(ρ,σ)∉A(P)`; Rem 6.3 states it is the unique corner of type II.b, so Thm 7.6(3)'s "if A0 is of type II then also j=0" clause is licensed by the standard-pair hypothesis alone. With `(ρ0,σ0)=(5,-1)` (normal to `(15,60)-(3,0)`), `v(F)=ρ+σ=4` and `v(A0)=25-20=5`, so `en F=(4/5)(5,20)=(4,16)` and `q0=5`. Rem 6.3 also records `F∈L`. Lattice solutions of `5i-j=4` are `(1,1),(2,6),(3,11),(4,16)`, so `F=uv f(w)`, `deg f=3`, `f(0)≠0`. The generated-corner relation is (2.5) of Thm 2.20(8): `A1=(k0/(m l0),0)+(mλ/m)(-σ0/ρ0,1)=(1,0)+γ(1/5,1)`, and `(7/5,2)` forces `γ=2`, i.e. multiplicity 6 in `p(z)=r(z^5)^3`, multiplicity exactly 2 in `r`. CONFIRMED; the derivation of q uses only A0 and the corner type.

**(2) Rectangle normalization without Roy's pins.** The consumed statement is Steps 2–5 of the gated normalization packet: for a polynomial pair with constant bracket, supports in `{i+j≤75, 5i-j≤15}` and `{i+j≤125, 5i-j≤25}`, and nonzero corner coefficients, the shears `u→u-sv`, `u→u-r` give the rectangles `15×60`, `25×100` with pure vertical tops. Those hypotheses are supplied by the gate's own hypothesis (degrees 75/125 are stated; the `(5,-1)` half-planes are the starting face with `en=(15,60)`, `(25,100)`; the corners are nonzero by definition of `en`). Nothing about `Φ`, `J=1/5` or the five pins enters. I checked separately that both shears strictly lower `(5,-1)`-weight off the face, so `ℓ_{5,-1}`, the root polynomial `r`, `A0`, `A0'` and `F` are unchanged, and that the shifted pair is still a standard (3,5)-pair (`v_{1,1}=75`, `v_{1,0}=15`, `st_{1,0}=(15,60)` has `v_{1,-1}<0`). CONFIRMED as a consumption of a prior gate, not re-proved here.

**(3) Lower transport.** I re-derived this rather than trusting the prose. Swapped picture: top vertex `(60,15)` is the unique maximal-x point by (2), so the clockwise-adjacent edge `d=(ρ,σ)` has `ρ>0>σ`, `en_d(P)=(60,15)` (orientation `en-st ∝ (-σ,ρ)`, GGV1 Rem 2.8), and `v_d(P)=15(4ρ+σ)`. Let `d` be the larger of the two adjacent edges of `P` and `Q`.

*Positivity chain.* Prop 7.3 defines `(ρ̃,σ̃)` as the least direction of `Dir(P)` in `[(0,-1),(ρ0,σ0)]` such that `v_{ρ',σ'}(P)>0` for **every** `(ρ',σ')` between; Cor 7.4 then covers `(ρ̃,σ̃)≤(ρ,σ)<(ρ0,σ0)`. On the arc from `d` up to `(-1,5)` every direction has `4ρ'+σ'>0` provided `v_d(P)>0`, because `σ'/ρ'≥σ/ρ` on the `ρ'>0` part and `σ'≥5|ρ'|` on the rest. So `(ρ̃,σ̃)≤d` iff `v_d(P)>0`. The producer's argument for that is right: if `P(x,0)` and `Q(x,0)` were both constant the bracket would vanish on `y=0`; a term `x^i`, `i≥1`, gives positive `d`-value, and `v_d(Q)=(5/3)v_d(P)` because both are evaluated at the common corner.

*Both components have the edge.* `v_d(P)+v_d(Q)-(ρ+σ)=159ρ+39σ>3ρ>0`, so Prop 1.13 gives `[ℓ_d(P),ℓ_d(Q)]=0`. If `ℓ_d(P)` were the monomial `x^{60}y^{15}`, the identity `[x^a y^b,G]=x^{a-1}y^{b-1}(a yG_y-b xG_x)` forces `Supp ℓ_d(Q)` onto the ray `(4,1)`, i.e. `d=(1,-4)` with `v_d=0`, contradicting positivity. No common-power lemma is needed.

*Shape.* Cor 7.4 hypotheses: `[P,Q]∈K^×`; `v_{1,1}` and `v_{0,1}` ratios `3/5` (the latter is the original `v_{1,0}` ratio of Def 4.3); `(ρ0,σ0)=(-1,5)∈V≥0`, in `Dir(P)`, value 15; `(1/3)st=(1/5)st=(20,5)`, `5<20`; `st F=(16,4)=(4/5)(20,5)`. So `ℓ_d(P)=R^{15}`, `en R=(4,1)`, `deg_y R=1`, and for a polynomial `P` the minimal x-exponent of `R` is nonnegative, hence `R=x^{4-k}(x^k y-λ)`, `d=(1,-k)`, `λ≠0`, `4-k>0`. The same corollary with `(P,m)` and `(Q,n)` exchanged and `F_Q=(3/5)F` gives `ℓ_d(Q)=R_Q^{25}`; `[R,R_Q]=0` forces the same `λ`, so one cut clears both. GGV2 Prop 2.1 is self-contained ("Let `(P,Q)` be a Jacobian pair in `L`"; hypotheses `a'>b'>0`, `(0,-1)<d<(1,0)` hold with `(60,15)`); its proof uses only Thm 2.6, Rem 2.5, Rem 1.8 and GGV2 Cor 1.6. Cor 1.6 (no slope-1 edge for a polynomial Jacobian pair) I did not replay; it only excludes `k=1`. Had `k=1` occurred the same chain would run one step longer and end at the same bound, so it is not load-bearing.

*After each cut.* `y→y+λx^{-k}` sends `(i,j)` to `(i-kt,j-t)`, so every direction `(1,s)` with `s≥-k` is non-increased, the `(-1,5)` face is untouched for `k<5`, `(60,15)` stays the unique maximal-x point, the y-degree stays 15, the `(1,1)` face is untouched, and `ℓ_d` becomes a monomial. Hence the new adjacent edge `d'` is strictly below `d`. If `κ'=-σ'/ρ'<4`, positivity holds, Cor 7.4 applies in `L^{(1)}` and gives `κ'∈{2,3}`, `κ'>k`. If `κ'≥4`, then every point satisfies `i-60≤κ'(j-15)≤4(j-15)`, so `v_{1,-4}=0`. After at most two cuts the second alternative is forced. The zero-valued boundary `(1,-4)∈Dir` is allowed and harmless: the claim is the half-plane `x≤4y`, not absence of that edge. CONFIRMED. Note the producer never uses GGV2 Prop 2.2's standard-pair hypotheses in the swapped frame.

**(4) Denominator persists.** The lower cuts leave `ℓ_{-1,5}(P)=R0^3` unchanged, so the original polynomial `F` still satisfies (2.9). Prop 2.11(5) cannot be cited at `(-1,5)` because the proposition assumes `ρ>0`; the producer's alternative is correct and I make it elementary: a weight-4 element of `K[x^±1,y]` is `x^{-4}h(w)`, `w=x^5y`, and `[x^a φ, x^b ψ]=x^{a+b+4}(aφψ'-bφ'ψ)` gives `4h(r+wr')=5wh'r`; comparing the lowest `w`-order with `r(0)≠0` gives `4=5e`, impossible, so `h=0`. Uniqueness holds in the Laurent ring, `st F=(16,4)`, `q=5` (control C3). CONFIRMED.

**(5) Root patterns and ODE.** `[E,R0]=R0/3` with `E=uvf(w)`, `R0=ur(w)` expands to `4wfr'-5wf'r-fr=r/3`; I verified this with the full two-variable bracket, not the reduced ODE (C1a). Prop 2.11(3) at `(5,-1)∈V0` with `k=5` makes `f` squarefree containing every root of `r`. Patterns with one root of multiplicity exactly 2 are `(2,1,1)` and `(2,2)`. For `(2,1,1)`, the three root evaluations give `a(a-b)=3(1-b)`, symmetric in `a,b`, hence `a+b=3`, `ab=3`, `c=1/9`, uniquely. For `(2,2)`, dividing by `g^2` leaves `3whg'-5wgh'-gh=1/3`; coefficients give `d=-1/(3a)`, `c=5/(9a(1+a))`, `(1+a)^2=5a`, i.e. `a^2-3a+1=0`; the producer's `c=(1+a)/(9a^2)` is the same field element (C2b). Changed objects `ab=2` and `a∈{2,1/2,3}` are rejected (C1b, C2c). CONFIRMED.

**(6) Successor enumeration.** After the `x^{-5}` cut, `R0'=(y+x^{-5})w^2h(w+1)` has `en=(5,2)`, `h(1)≠0`, so Prop 8.2 applies with `(a,b)=(5,2)`, `m≠n` coprime, `P1,Q1∈L^{(1)}`. Case (1) with `b'<2` (Prop 3.7 plus `ρ2<0`) yields the six candidates; `(1,1)` dies by Thm 2.6(4); `(-2,0)` and `(-4,0)` die because at `(-2,7)` and `(-2,9)` both aligned Euler endpoints are non-lattice (C6b), so `F` is a monomial and Rem 2.5 contradicts `(ρ2,σ2)∈Dir`; `(2,1)` dies by a second Prop 8.2 pass at `(-1,3)` with `(a,b)=(2,1)` (C6c). Case (2) forces `k=1` and the literal endpoints `{(-1,0),(2,1)}`; parallelism fixes `en P1=(2,1)`, `en Q1=(-1,0)` (C6d). CONFIRMED.

**(7) Six hulls, sign, exponents, guards.** I checked each region by half-plane membership without a hull routine: every lattice point of the region maps inside the displayed polygon with nonnegative exponents, every displayed vertex is a region point, and total degrees are 30/50 (C5). Convexity of the displayed vertex chains was checked by hand. `[T P,T Q]=-x^3 T([P,Q])` verified on 20 random Laurent pairs; `T` is an exponent involution (C4). Nonzero vertex guards: `(15,15)` is the corner, `(15,6)` is `en_{-1,5}` with coefficient `h(1)^3λ_P`, the successor endpoints are `en` points by definition, `(0,0)` by translation. CONFIRMED.

**(8) Stopping before x^{-3}.** The three cases are exhaustive at the stage where the `(-1,5)` face has `en=(5,2)`, and each polygon is closed by `y≥0` (after an axis endpoint only the bottom edge can follow, since every later direction lowers `y`). Stopping is therefore legitimate; the witness in C7a confirms an `x^{-3}` cut can raise `v_{1,-4}`. No branch is lost. CONFIRMED.

## 3. Controls

`box/d125-published-chain-gate-20260906/`: producer `check.py` copy runs exit 0 in normal and `-O` mode (`run_normal.json`, `run_O.json`); `--mutate` exits 1 in both with "injected wrong vertex". Own `gate_controls.py` (40 checks, `gate_controls.out`, exact rationals, under one second): C1–C8 as cited above. These are arithmetic controls; they do not prove the lower-interval or transport statements, which rest on §2.

## 4. Boundaries stated honestly

- Hypothesis consumed as given: the pair is polynomial, standard (3,5), degrees 75/125, starting triple `(5,20),(1,0),(5,-1)` and generated corner `(7/5,2)`. The arrow from an arbitrary counterexample to such a pair (Strinz Theorem A, census) is outside this gate.
- Prior-gate consumption: rectangle and pure-top normalization (Steps 2–5 of the triangular normalization gate).
- Quoted without proof replay: Thm 7.6(3), Prop 7.3/Cor 7.4 (proof of Cor 7.2 read only to (7.17)), Prop 8.2 (proof read to (8.16)), Prop 3.7, GGV2 Cor 1.6, Thm 2.20(8).
- Reverse arrow: the composite of shears, swap, cuts and `T` is invertible only in `K[x^±1,y]`; a receiver point with `[U,V]=x^3` gives no polynomial pair without cancellation equations. Roy/Strinz coefficient attachment is absent. Both are GAPs of scope, declared by the producer, and do not touch the necessary implication.

Cheapest next step if root wants more: none is needed for this implication; a novelty or attachment check is a different task.
<!-- BODY-END -->
