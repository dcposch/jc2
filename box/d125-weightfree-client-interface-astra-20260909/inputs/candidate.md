# Candidate supplied by root — unreviewed conditional premise only

Received2026-09-09; this task began00:17:43UTC, bounded to00:29:43UTC.
No pending proof or live reviewer body was read to formulate this premise.

(WF) For every characteristic-zero field K on which the displayed
coefficients are defined, let

    H_Q=p²(g³+p³),
    H_rho=p²(p+g)(p+(1-rho)g)², rho²-3rho+1=0.

For either H, there are no A,B in K[g,p] satisfying

    deg_total A=15, deg_total B=25,
    A_15=H³, B_25=H⁵,
    A_g*B_p-A_p*B_g=c*g², c in K*.

There are NO weight, support-polygon, parity, lower-face, lift-parameter,
reference/first-contact or additional scalar hypotheses. Both rho
embeddings are included. For the golden alternative rho must be an element
of K; extension of a smaller coefficient field is permitted for a
contradiction, not silently added to a fixed computational ring.

This statement is a ROOT-SUPPLIED UNREVIEWED CANDIDATE, not a theorem
proved or independently checked in this typing task. Every exclusion and
unit-ideal consequence in the companion report is explicitly CONDITIONAL
on a proof and acceptance of exactly this statement (or an adequate
explicitly substituted statement). The pending weighted M gate does not
review removal of weights and is not a premise. No queue/solver/worker
action follows from this packet.
