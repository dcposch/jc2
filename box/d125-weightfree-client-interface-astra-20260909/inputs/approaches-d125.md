# Current D125 section only; no whole avenue charge

### D125: current source coverage and proof priorities

The accepted standard-F2 published-chain implication is conditional on an
actual polynomial standard pair and named external theorems. Its exact
minimal-receiver map gives degree15/25 pairs with J=c*g², three polygons
(unequal,common3,common4) and two field/top patterns (squarefree Q and
golden Q(rho), both embeddings). This is a necessary receiver interface,
not all-degree coverage. The separate reviewed reverse-polynomiality
contract adds105 rows and two parameters; an actual full-row point would
satisfy the sufficient counterexample contract independently of that
conditional coverage. A necessary receiver point alone is not a Keller pair.
See [minimal receiver composition](xmodel/d125-minimal-monomial-receiver-composition-astra-20260906.md)
and AUDIT17(mmmmmmmmmmmmm) for the exact arrows.

The six baseline full-row inputs are already constructed and independently
checked at all-row/actual Singular-text scope:269/295/371 variables and
803/816/816 rows per field,27577–47621 terms. The clean root AWS replay
took61.47seconds/170.24MiB with153 actual corruption controls rejected.
Reviewed alternative quotient/Hermite/B presentations are available, but
their coordinate savings must not be added without the proved compatible
map; smaller presentations are not measured solver wins.

**Unequal/Q is now excluded.** Accepted15h first retired the restricted
guarded odd/lambda2=0 client, including its finite coefficient algebra,
by direct source proof and Nullstellensatz—not a computed1 certificate.
Accepted15i removes parity: over every characteristic-zero field there
are no A,B with total degrees15/25, tops H³/H5,H=p²(g³+p³),
weights<=3/5 for w(g)=5,w(p)=-7, and J=c*g²,c!=0.
The accepted minimal-receiver map supplies those hypotheses for the ENTIRE
unequal/Q field-point client. Otherfive/golden/common, allD125/global125
and arbitrary-degree JC2 are NOT excluded. The nonodd gate adds no new
nonreduced-quotient or computed unit-certificate claim.

The method constructs its own reference/first contact and takes GLOBAL
initials on the product of branches before selecting a nonconstant component.
All scalar kernels are retained. It needs no finite-boundary degeneration or
moving-lift premise. See [source](xmodel/d125-cone-first-contact-source-astra-20260908.md),
[consumer](xmodel/d125-cone-global-initial-consumer-astra-20260908.md),
[nonodd proof](xmodel/d125-nonodd-cone-discriminator-astra-20260908.md)
and the exact promoted scopes/corrections in AUDIT15h/i.

**Uniform port: PROMOTED15j, not extra source coverage.**
The [self-contained cone degree theorem](xmodel/uniform-cone-jacobian-degree-astra-20260908.md)
has passed independent Fable review and root whole-proof/code replay.
For a,b>=1,D=a+b,5b>4a+1,V homogeneous degreeb, monicg/squarefree/p∤V,
H=p^a V,wg5,wp=(1-5b)/a, the theorem states that degrees3D/5D,
topsH³/H5 and weights<=3/5 force every NONZERO Jacobian to have total
degree>=D+2. Its Euler/UFD consumer is short; normal-remainder injection and
the actual source hypotheses do the difficult work. No additional unretired
receiver has been attached. One literal new client is required before further
abstraction or a numerical family is counted as a campaign extension.

**Golden: preserve the repeated branch and exact source scope.**
For H=p²(p+g)(p+(1-rho)g)², rho²-3rho+1=0, the
[root-checked algebraic control](xmodel/d125-golden-divisibility-control-astra-20260908.md)
has all degree<=3 H|F² solutions F=lambda*p(p+g)(p+(1-rho)g).
A prescribed nonzero g²p coefficient fixes a nonzero such F, but H∤F;
F is normal and vanishes on both reduced branches. Thus neither the
squarefree divisibility step nor reduced-product detection ports unchanged.
A dual-number monic3/5 commuting pair defeats the unchanged field consumer
on a thickened branch; a common quadratic defeats the coprime conclusion
for ramified6/10 initials. Neither control is a source point.

The [0200 Astra cross](xmodel/ideation-20260908T0200Z-cross-astra.md)
gives the independently reviewed15k triangular coefficient-ideal exclusion
of the shared eight-parameter golden ansatz, even over Q(rho)-algebras with
lambda inverted. The Fable gate confirms the contradiction and shows only
five of the seven rows are needed. The separately reviewed field-valued
odd-j12-to-ansatz map now composes to exclude that stratum (15l), NOT the
whole golden receiver. No solver or reverse-lift work is warranted for the
excluded ansatz. The [whole non-H composition](xmodel/golden-nonh-composition-gate-fable5-20260908.md)
is now independently accepted (15m), WITHOUT parity: both coalesced and
separated source regimes reduce to contact12 resonance, where the nonzero
target coefficient contradicts the weighted face. The H-divisible first
correction F_j=HC remains open (j<=9). The independently reviewed simple-L
discriminator15n now forces L|C; if M|C too, j<=6. The new coalesced and
separated M source proofs cover complementary balances in the two remaining
strata, but are still PROVISIONAL under a separate combined Fable gate.
Neither the resonant consumer nor the excluded ansatz needs another solver.
No full golden result is yet promoted.

**Common polygons: source consequences before a new encoding.**
The [map discriminator](xmodel/d125-common-to-unequal-interface-discriminator-astra-20260908.md)
is root-checked NO COMPLETE TRANSFER in its tested class. Common3 fails the
tested divisor/top-preserving triangular class; p->p+mu/g creates a unique
nonzero g^-15 pole. Common4 admits polynomial centering p->p+mu, but its
full unequal weight bounds need a consequence of the Jacobian equations.
A commuting degree5 face fixture defeats a face-only inference, not a
step explicitly using c!=0. No all-transformations no-go is proved.
C4-FILTER is not yet a literal LP: source cancellation and reference
existence are not raw-support inequalities. Write one quantified source
implication before a feasibility engine. Roy/Strinz coefficientwise
attachment also remains open; matching degree labels is not a map.

One exact common4/Q coefficient is now independently typed (15o):
h=[g8p4]A(g,p+1)=A_g8_p4+5A_g8_p5+15A_g8_p6, IDs95–97 in the
already normalized mu=1 baseline. Appending fresh z_h and z_h*h-1 to ALL
371 variables/816 rows defines a prospective372/817 complete-source
localization. h is ambient-nonzero, not proved nonzero or nonredundant on
the source. Properness would give the sufficient algebraic-closure
counterexample endpoint; a unit would exclude only this h-open. No stream
has been emitted or solved, and no measured solver gain is asserted.
