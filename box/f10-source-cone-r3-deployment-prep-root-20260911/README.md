# r3 deployment preparation — DISABLED / NOT EXECUTED

ROOT began disabled-template preparation01:14 UTC September11. This directory
contains operational preparation, not a worker release, a new supervisor or
mathematical evidence. The runtime parent is PROVISIONAL and its designated
Fable FIRST was already queued at01:10:57. Original ROOT preparation reserve
01:31/HARD01:34, never reset; these are preparation clocks, not run clocks.
At initial preparation ROOT+Fable+rank-author were three active reasoners:
ROOT explicitly allows this bounded one-generation, reversible static-only
exception to25%; it does not authorize compute or promotion. From the separate
direct-place proof launch there are four independent active reasoners, one
dependent on this runtime. No extra work is created to meet an occupancy target.
No further descendant, new runtime code, source edit or AWS allocation here.
If runtime FIRST refutes a consequential premise, keep these templates disabled
and prepare a separately pinned correction, never silently reuse the old pins.

## Current gate and inventory

Frozen runtime report968e4163eadde723a6dc2f9bc806994a0771912be42bd6fc836b905b1a4b1617,
transactiond0600b1150d0cd7f1e4e55794b0cf3ac8b048086240c0116b4185fb4bd836e42,
custody1ea6f2055074f5d24a538e4e074d21e0f62ad940533bffcb86781a270e3b3c5c.
ROOT independently COMPLETED/custody FIRST01:07:04,18+9 pins/expectedVERIFY,
WHOLE all6source/text+report1303lines78924B, postpins01:08:59. Runtime FIRST
f10-source-cone-r3-runtime-gate-fable5-20260911 is LIVE, no live body read.
No accepted actual baseline or execution-readiness exists.
SOURCE-INPUTS.sha256 binds the five source files, three runtime wrapper files,
and unchanged CAPRUN. These are exact bytes, not proof of readiness.

Current parent contract is nine preflights, ONE producer, ONE positive checker,
ONE isolated graph.slots24+X1^27 mutator, ONE fresh negative checker, no others.
Source each900wall/600CPU/8GiB/128MiB; combined3000wall/2100CPU/8GiB/128MiB,
three science900/599softCPU, mutator30/20, controls5/3, dummyRSS32MiB.
Baseline+fixture jointly120MiB; metadata8MiB. All are unmeasured refusal limits.
No rank/solver/prime task or direct-place bypass is in this deployment.

## Layout

Use exact future batch basename jc2-r3-source-20260911a; this is an administrative unique
name, not an invented launch time. Science goes to /opt/jc2-r3-source-20260911a/science
with EXACT five.py files; wrapper has EXACT dispatch.py/probe.py/mutate.py;
runtime/run_capped.py is separate. Metadata under metadata/. All root-owned
directories0755, scientific/wrapper/CAPRUN/metadata files0444. No added file
inside either exact source or wrapper directory. UID/GID65534 for child.
Working /run/jc2-r3-source-20260911a is128MiB tmpfs, root0755 authority/ and frozen/,
UID/GID65534 writer0700. Frozen becomes0555 only at the runtime-defined step.
Durable /var/lib/jc2-r3-source-20260911a/custody must not exist. Stage and later recovery
archive under /home/ubuntu/jc2-r3-source-20260911a/. No cleanup/deletion is included.

## Future worker release, after FIRST and complete worker-independent prep

Keep ROOT-READY-INPUTS.sha256 ABSENT until the runtime FIRST is independently
terminal/collected/accepted and all worker-independent preparation is reviewed.
This plus unresolved placeholders disables the worker template. No instance,
AMI or launch deadline is selected by these templates. Proposed c7i.2xlarge
has more room than the8GiB cgroup budget; verify current instance specs, quotas,
exact tagged fleet, subnet/SG/key and image using scoped read-only AWS before
allocation. No historical headroom/spec/image freshness is assumed. Do not
inspect/control protected infrastructure. Preserve the new100GiB gp3 EBS.
Use the exact unchanged minimal stdlib user-data pin0c5820cd60a44704a6d8118a7570a9327c1f2fe62939ff664b1f9b7cfaf42730.
The prior CLI flag correction --enable-api-termination is retained.

A distinct ROOT release freezes actual original worker/setup/admission/math/
task/termination clocks before allocation, with enough room for3000seconds
plus setup and custody, no resetting after a refusal. Worker termination timer
must bind the exact returned instance ID immediately; if arming fails, retire
that exact worker with EBS retained. Keep launch response and all failures.
No idle worker is kept for review. Inspect only that owned worker.

Resolve actual instance/hostname/boot/namespace, image/default Python/setpriv
canonical paths and byte pins, cgroupv2, memory and durable device. Then the
physical metadata template and native collector can be separately authorized
on that exact worker. Both are currently disabled by actual-instance placeholders.
They do not run scientific Python/import/AST/syntax/tests. Full native collector
preserves directories, aliases, absent ZIP and stdlib/pyc/dynload bytes; no
FILES-ONLY downgrade. All packages/system update processes must be quiet;
the -B/cache fix is retained. If any assumed native layout differs, STOP
and record actual evidence before a separately pinned correction.

## Forming actual registration: metadata only, not science

Build from the runtime's pinned DISABLED-REGISTRATION.json, preserving its
schema and field types. UID/GID and durable_device are JSON integers; profiles
are arrays of metadata integers; source_limits/limits/CPU origin are strings.
Actual mathematical values never pass through JS/native JSON numbers.
ROOT supplies exact observed instance/host/boot/ns/cgroup, literal four UTC
stops ending+00:00, exact paths and immutable own/native/source pins.
Enabled/exclusivity become true only in the separate release, not here.

Derive FULL native-manifest.json from complete terminal collector records,
exact schema/files/directories/aliases/absent/python_path, <=1MiB.
Separately derive source-native-manifest.json with only job_tag/files,
<=64KiB, containing at least actual interpreter and a ROOT-chosen bounded
matched subset of FULL files. Every entry must match FULL; subset never
claims closure by itself. Freeze both, native.stdout/native.sha256 and pins.
Do not replace FULL by source's small schema.

For each of the13 literal command vectors reproduce dispatch.py run():
CAPRUN command uses canonical Python -I -S -B, immutable CAPRUN path,
profile wall/CPU/RSS and exact sample/grace/output/telemetry/science-cwd,
then setpriv UID/GID clear-groups no-new-privs, then exact Python child argv.
Probe has --science-dir/--mode/-- prefix before source tail. Scientific entries
use -E -s -S -B, mutator -I -S -B. Source tag/mode/output/input and authority
file paths are literal. No alternate script or inferred basename.

Precompute NON-checker authority byte hashes from exact dispatch.authorize()
metadata: sorted compact JSON plus newline, source files/limits, actual native
subset/interpreter pins and each phase's literal target. Five refusals mutate
only their specified status/caps/inventory/source/supplied hash; wrong-tag
checker target is absent with zero input pin and rejects before it is read.
Producer output state is UNFORMED. Mutate's unused derivative authority exists
as specified but is not its scientific authorization. Do not add commands.

ONLY check-positive/check-negative contain AUTHORIZATION_SHA256 exactly at
their unique --authorization-sha256 flag; ONLY mutate has ROOT_REGISTRATION_SHA256
at its unique flag. Every other byte literal. Do not precompute future baseline/
fixture hashes or introduce a generic expansion. Runtime creates the exact
root0444 positive-phase record after genuine checker success and quiet/freeze;
mutator rechecks ROOT, frozen baseline and actual source authority.
The canonical phase_policy fields and typed_slot_policy must exactly equal
the runtime, including future frozen artifact/mixed-row and positive.json paths.

Before release perform metadata-only comparison of every actual expanded
command derivation, schema/type/path/pin inventory and all placeholder removal.
Never execute/import/compile the scientific or dummy code locally to do this.
Finalize actual stage and installed manifests only after metadata JSON is frozen,
then bind those manifest hashes in a separately pinned setup script. Setup
installs unchanged science/runtime and creates the fresh tmpfs; it launches nothing.

## Outer controls, execution and closure

The disabled outer template uses existing systemd controls, not a new supervisor:
Type=exec,RuntimeMax3000,KillMode=control-group,TimeoutStop5,MemoryMax8GiB,
MemorySwap0,TasksMax32,OOMPolicykill,Restartno,Delegatefalse, no realtime.
CPUQuota69%/period10ms and cpu.max.burst0 give headroom against2100CPU at
3000wall; do not call sampled dispatcher accounting a kernel hard CPU quota.
Record actual cpu.max,budget origin,whole final CPU and all controls. Origin
string0 is valid only for a genuinely absent/fresh cgroup, never an old active
group; direct prelaunch and same-call identity/Invocation/cgroup checks required.

Outer stdout/err are EXACT outer.stdout/outer.stderr on working tmpfs and
cwd is wrapper, not science. Arm original absolute TERM then KILL timer BEFORE
dispatcher launch, retain independent worker termination. Inspect same-call
actual process identities and cgroup controls within60seconds. All13 scoped
calls remain under original caps; no repeat upon missed capture or parser error.
Do not read live coefficient/fixture bodies. Read terminal CUSTODY only after
independent original Main0/Control0/inactive/empty cgroup/all writer identities
gone. Verify every pin/telemetry/native stream and actual source/negative
predicates. Secure byte-identical remote EBS archive, compare/fsync, download,
safe scoped extraction/hash replay/fsync before exact-ID worker retirement.
No worker held for FIRST actual-result review. Preserve failed records.

These scripts are unexecuted templates, not an accepted runtime or actual
worker receipt. A future actual-result FIRST remains required for any resulting
source-coefficient claim. No mathematical source/rank/degree/JC2 outcome.
