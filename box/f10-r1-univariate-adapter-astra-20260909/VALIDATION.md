# Disabled finite validation plan

This packet is UNEXECUTED: no compile/import/test, arithmetic or emitted
coefficients. No normalized or univariate artifact exists here. The theorem
17l is accepted; the bound normalized software is PROVISIONAL. If its gate
refutes it, this dependent code is quarantined rather than silently relinked.

Prerequisite before any future adapter run: an actually completed registered
normalized generator/full original-source checker pair, terminal custody and
root acceptance of that precise receipt. Root must register the normalized
artifact SHA, receipt SHA and the exact generator/checker code versions in the
existing authority's normalized_acceptance field with status
ROOT-ACCEPTED-GENUINE-CHECKER-RECEIPT. The supplied template leaves it null.
A typed receipt alone is NOT execution evidence. input_evidence.py checks the
root-owned binding; it does not issue acceptance or independently re-prove the
remote lifecycle. This is the existing trust boundary, not a new authentication
or supervision framework.

Future exact entrypoints, only after separate registration:

    python3 -I -B adapter.py AUTHORITY NORMALIZED FULL_CHECKER_RECEIPT OUTPUT
    python3 -I -B checker.py AUTHORITY NORMALIZED FULL_CHECKER_RECEIPT OUTPUT RECEIPT

All argv use absolute registered paths in actual operation. Root chooses caps,
worker and deadlines. The accepted helper/caller/CAPRUN interface is unchanged;
no caller is supplied, modified, invited or run by this task. Mathematical
imports occur only after authorize and the normalized-input evidence checks.

One genuine positive adapter and separate checker acceptance are required.
Receipt metrics measure future actual degrees, basis-term counts and bytes;
they are not measured now. All nine indexed rows and every coefficient slot
through its envelope must remain, even if zero, duplicate or degree-dropped.
No factorization, gcd of residuals, rank test, solver or source expansion.

Finite negative groups to be designed under later authority (none implemented
or executed here):

1. Missing/wrong normalized acceptance, receipt/hash or backend version:
   refuse before mathematics. A fabricated status string is not an accepted
   runtime record and must never be registered as such by root.
2. Modulus replaced by a factor or a named unit witness changed: exact ring
   or product-identity rejection.
3. Drop/reorder/deduplicate a row, or omit an explicit zero coefficient slot:
   literal inventory rejection.
4. Change one of a,...,g or c inverse: exact compatibility reconstruction or
   product c*c_inverse=1 rejection. Do not divide b to construct a fixture.
5. Change one specialized row coefficient, h0/h1/Ubar or r coefficient:
   independent binomial-substitution rejection.
6. Drop r or h0 from the guard, or alter its sign: exact q=r*h0 and guard
   encoding rejection, not a sampled nonvanishing claim.
7. Change degree bounds, forbidden coefficient exponents, float or rounded
   rational string: exact schema/parser/source-reconstruction rejection.
   Rejected large strings do not establish a positive nested-parser test.
8. Alter the cubic or s/y/z back-map contract: literal accepted-contract
   rejection. The cubic is symbolic; no degree112/196 expansion is authorized.

Each changed fixture needs a new hash and exact registration. Stale-hash
refusal is not evidence of reaching the intended semantic check. A cap,
incomplete result or mismatch is STOP/NONDECISION; no retry/escalation or
automatic follow-on is authorized. No throughput, row nonvanishing or ideal
decision is inferred from any future representation PASS.

Independent scope: checker.py does not import adapter.py. It reassembles
Phi3/Phi4 from the seven reported coefficients and substitutes the original
normalized monomials by an explicit binomial route into dense T coefficients;
the adapter uses sparse polynomial substitution. Both intentionally share the
exact frozen B arithmetic copy, so arithmetic independence is limited. The
earlier genuine full-source checker is a mandatory distinct prerequisite.
