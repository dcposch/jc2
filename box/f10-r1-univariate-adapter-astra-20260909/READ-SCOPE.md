# Exact scope

First action19:20:00 UTC; controlling cap19:38:00 UTC (actual+18 minutes).
The new box/report were absent at the first action. The two accepted17l
producer/gate hashes were verified before their WHOLE reads. Their historical
conditional/provisional headers are superseded only at the root-assigned
accepted17j/17l scope, without changing bytes.

The previous normalized backend custody ef601959... was checked before its
metadata body. Previous WHOLE reads of its report, three new code files and
the accepted17j producer/gate were reused. Those exact current hashes were
checked in this task. Only algebra.py and execution_gate.py were copied,
unchanged, by apply_patch; their copy bytes are checked against the frozen
hashes. Copy staging and the fresh explicit per-code hash command occurred in
that order within the same bounded call; no code ran and every hash matched.

No paths occurring merely as earlier provenance were followed. No current
backend gate, other live report, newer science, original coefficient artifact,
runtime packet, source body, ledger, protected path or peer body was read.
No web/network, AWS/SSH, process audit, mathematical subprocess, compile,
import, arithmetic script, test, coefficient construction or solver ran.

Own adapter.py, checker.py, input_evidence.py and documentation were read as
text. The copied arithmetic is PROVISIONAL with its parent; the copied helper
is unchanged accepted authority infrastructure. Neither is promoted here.
This is the single authorized speculative software child, with no descendants.
