# ROOT scope amendment — one coordinate-resonant endpoint

September11 2026,21:09UTC. Original TASK remains immutable and charged;
add this document as a third input with its own pre/post pin. Original
publication reserve21:25/HARD21:28 unchanged; no new clock or added tools.

The same weighted-face argument may settle the endpoint a=1,b=0,c=r>=0:

  Phi=e_p-r+delta g(e),  g nonconstant.

This endpoint is now the highest-value part of the existing task. Test it
by deriving its homogeneous pieces p^r(q-g(r+s)p)^s, s>=0, rather than
assuming the positive-a,b proof includes it. Lowest p-order gives p^r C(q);
q=0 again seems to force superexponential decay of C's coefficients.
Check algebraicity of the leading coefficient and the finitely many zeros
of g. It should need no extra source theorem and no scientific computation.
If this extension cannot be completed before the original deadline, leave
it GAP and bank the original positive-a,b result with its limitations.

Motivation, not a charged theorem premise: ROOT found van den Essen1993
already excludes general actual-Keller operators without an e_p-r factor
in their diagonal term after a generic normalization. Thus this endpoint,
not the positive-a,b subfamily, tests the remaining coordinate resonance.
No claim that an arbitrary Keller annihilator can be put in this form.
No broader operator classification, new agent or source search is requested.
