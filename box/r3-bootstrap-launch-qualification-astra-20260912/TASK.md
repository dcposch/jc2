# One concrete launcher/clock qualification interface

ROOT, September12,2026. Reversible STATIC source implementation only.
No AWS/worker/runtime/native probe, enabled spec or scientific execution.
Original reserve17:20UTC/HARD17:23UTC, includes final custody, NEVER RESET.
First action UTC; all owned outputs absent. Finish early when decisive.

Fresh SHA-before-WHOLE reads, COORDINATION first, exactly five inputs:

1. COORDINATION.md,
   33cfa6106972fe557a95ca81a9a05bb72300a82b2fbb7908915441cf75377597.
2. This TASK.md, ROOT's supplied exact pin.
3. box/r3-exec-bootstrap-repair-root-20260912/bootstrap.py,
   f04f1b7b16bed194e5d7b503a78fe9bd36e3bd58da3696adf6a40bfdbdcd17d2.
4. box/r3-exec-bootstrap-repair-root-20260912/CONTRACT.md,
   21c1c0bd6840ac7aa80a33e9ff2c7a88bab87f51fd7a669ed6e27ba30b32bfeb.
5. box/r3-exec-bootstrap-repair-gate-fable5-20260912/INTAKE.md,
   ec471d42f87a156295daa068d3778bba0470bf39fa891f007d7d124e61a1f765.

The repaired bootstrap has actual-code different-model FIRST at the attached
intake scope. Do not edit/re-review its unchanged dispatcher/guard/science
foundation. No other source, corpus, live peer or linked history body.

## Bounded problem and deliverable

Produce the smallest concrete prospective launcher source and exact contract
that could start this bootstrap on ONE separately qualified AWS host while
honestly binding the original clock. The main unresolved interface is:

    externally approved immutable spec+SHA (including outer_started_utc)
      -> already-armed independent fixed watchdog
      -> sole ROOT process in its fresh outer cgroup
      -> same-PID bootstrap exec into unchanged dispatcher.

Determine whether there is a hidden timestamp/authentication cycle. A source
or launcher may not invent a service start before observing it or call its
own unapproved output external ROOT approval. A genuinely earlier ROOT-owned
outer-task/watchdog epoch MAY be a conservative original start, but only if
the contract defines and observes it exactly, preserves independent monotonic
bounds, and includes every intervening setup/handoff cost. Distinguish it
from the later service process-start timestamp; do not silently identify them.
No future planned time may be asserted to be an observed actual start.

Prefer ordinary externally managed systemd facilities and the already
reviewed bootstrap, with no surviving holder/FIFO/parent in its outer cgroup.
No new orchestration framework, controller family, broad installer or generic
registration generator. Do not merely return a high-level plan if source
is feasible; implement the exact bounded launcher/checks and delimit any
remaining ROOT steps. If an unavoidable cycle or source-incompatibility
prevents it, return precise GAP with the smallest evidence-backed change
required, not fake executable readiness.

Keep bootstrap's exact argv, ENV, root identity, source/native inventories,
exclusive immutable input approval and fresh output paths. Existing fd1/2
are on the128MiB working tmpfs, fd0 is trusted actual null, no extra fd passes.
The outer cgroup has8GiB memory.max, swap.max0,pids.max32,cpu.max.burst0,
the same qualified CPU quota/period, empty subtree_control, domain type and
no extra entrant/child before bootstrap creates its nine leaves. Respect
systemd-injected environment and ExecStart PID replacement semantics.
Independent monotonic limits and original absolute admission/math/task/
retirement limits must survive bootstrap exec; no deadline reset or cap rise.
An absolute UTC timer by itself is not an independent monotonic bound.

Do not read or resurrect the old holder/FIFO administrative scripts or a/b/c/d
batches. Source/static maps alone cannot establish native completeness, host
namespace/device identity,64KiB ASCII snapshot sizes, cgroupfs modes, watchdog
operation, actual launch duration or runtime success. List those exact future
observations and their boundary; no invented evidence hash may pass approval.
Worker-independent preparation must finish before worker allocation; future
host facts can be unbound, but the method to bind/check them must be explicit.
No example may carry live approval, enablement or fabricated host evidence.

Cheapest discriminator is this manual source/API dependency check, followed
ONLY AFTER different-model review and ROOT registration by a bounded AWS
launcher/native qualification. No such execution is authorized here. Include
one precise static negative control for false/expired original epoch and one
for wrong approved spec pin; explain side effects and retained failure data.

## Allowed tools and publication

Only UTC, inert text/hash/byte counts/owned absence; own apply_patch edits;
unchanged administrative artifact_finalize. No scientific interpreter EVEN
EMPTY, import, AST, syntax/compile/test/dummy/fixture, local process control,
network/AWS/SSH/git/agents, shared edit, protected tree/mirror or deletion.
No runtime command may be executed; source authoring is not qualification.

Own files only under box/r3-bootstrap-launch-qualification-astra-20260912/:
launcher.sh (or one justified single-file launcher name), CONTRACT.md,
PINS.json,custody.json. One report
xmodel/r3-bootstrap-launch-qualification-astra-20260912.md and its manifest.
Report via begin -> leased partial -> WHOLE readback -> marker LAST -> close
-> finalize -> expected-manifest verify. Finalizer pin
0f6aaf7d549952de8f318bdd974ae4627e497783d809f94cfaba16607355cb8d,
basis0d39df3c9fd69c939a8420c54d03228b9077777d. All input/output pins, exact
read scope, completed WHOLE own code/contract/report/manifest/custody readback,
postpins and actual UTC ALL WRITERS IDLE. No writes after completion.
Return custody SHA first. Do not claim installed readiness or a JC2 result.
