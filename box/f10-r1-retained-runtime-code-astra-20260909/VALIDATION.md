# Disabled retained-row runtime preparation

No program in this packet has been run, imported, compiled or tested. No
retained artifact has been emitted. This is neither a worker assignment nor
a dispatch registration. It is not an ideal decision.

## One finite batch

Root alone may choose a new exclusive remote directory and exact owned EC2
instance, check ownership/DMI/hostname/interpreter and arm an independent stop
timer. No old worker or deadline is inherited. Root supplies and externally
pins a NEW `dispatch.registration.json`; the supplied DISABLED template is
immutable and has no executable budget. The caller is invoked with Python
`-I -B` in that exact directory. Root fills all five cap profiles and the two
absolute UTC cutoffs plus aggregate mathematical wall cap. No cap increase,
retry, alternate representation or second batch follows a failure.

Stage unchanged transformer/checker/helper/probe plus the exact externally
reviewed CAPRUN, original exact.json, admissibility and frontier. The caller
requires all eleven absolute input paths (eight fixed named files, itself,
semantic harness and interpreter) in root's registration. CAPRUN body,
original data and admissibility/frontier bodies were not read in this task.
The caller pins registration and all input bytes per operation, captures raw
CAPRUN/child argv, inherited 16MiB per-file limit, namespace/start identity,
telemetry and pre/post hashes. Root must ensure no concurrent authority writer;
each new authority is exclusively created, made read-only and never rewritten.
This cooperative rule, not permissions against a hostile same-user process,
addresses the accepted helper reread race. The helper is unchanged.

The sequence is exactly five actual authority refusals (disabled, wrong host,
extra argv, changed RSS cap and absent checker input pin), valid sentinel, the
same descendant-RSS/parent-exit/TERM-ignoring probe, one transformer execution,
one genuine production checker execution, then one finite in-process semantic
harness. There is one child at a time and no new process group in the caller;
CAPRUN retains group ownership. Dummy identity events must be exactly MATCH,
SENT15, MATCH, SENT9 with integer signals, recorded leader identity, RSS excess,
reap/cleanup and no remaining owned live group. No generic fork/O-mode matrix.

Admission uses the original stored cutoff with a fifteen-second margin and a
post-return check; mathematical hard cutoff also includes time from the first
mathematical child. Sampled RSS and CAPRUN cleanup can overshoot; these checks
are not strict realtime guarantees. A late, capped, missing or mismatched result
is STOP/NONDECISION, even if output files exist. Root independently rechecks
owned children and stops its exact worker after the batch. No supervisor edits.

## Positive evidence and finite controls are different

Only `full-check` calls the unchanged production checker entrypoint. Its
accepted receipt binds source, retained artifact and checker hashes and gives
the eight bracket bands, ten inverse-pole slots, all20 original slots/all17
retained rows, forcing, gauge, envelope and top/guard restoration checks.
Source mathematics is already accepted at its exact scope, but these runtime
claims remain unmeasured and unexecuted until that step actually passes.

The harness imports the unchanged checker ONLY after authority, consumes that
genuine positive receipt, and calls `checker.verify` in-process for twelve
changed fixtures. Each fixture is exclusively serialized, reread and hashed;
expected exception class and exact reason are checked. This is semantic API
evidence, NOT twelve production-entrypoint executions. No control passes by a
stale source/artifact hash refusal; the intentionally changed fixture is fed
directly to the mathematical verifier. Baseline registered input hashes remain
unchanged. Unexpected rejection or acceptance stops the entire batch.

1. Dropped retained row: `all20 original slots`.
2. Wrong L inverse: `L inverse identity`.
3. Wrong scale mapping: `retained scale/parameter mapping`.
4. Wrong modulus: `wrong modulus`.
5. Loss of negative s sign: `retained scale/parameter mapping`.
6. Negative lower-parameter exponent: `Laurent/basis exponent domain`.
7. Missing declared pole slot: `all ten pole slots`.
8. Missing leading-a restoration slot: KeyError with string `'a'`.
9. Changed leading B5: `leading B coefficient`.
10. Float anywhere: `floating point forbidden anywhere`.
11. Wrong canonical string 9007199254740993 in E1/S0: row-substitution rejection.
12. Its rounded-integer canonical string: the same row-substitution rejection.

The last pair establishes a genuine beyond-2^53 rounding precondition and two
negative semantic checks. JSON exact-string retention is harness-level only;
it is NOT a successful payload run through the checker's nested rational
parser. No production API is changed to expose that helper. No claim is made
that actual retained coefficients exceed 2^53. The single positive production
run remains the evidence for successful actual-payload semantics.

Changing A/B would fail original coefficient binding before a later bracket
or pole equation; no such late negative coverage is claimed. Omitted pole
inventory is an inventory check, not a changed nonzero pole reaching its final
equation. Full bracket and inverse-pole coverage is positive read-back only.

## Measurements and limits

The genuine checker receipt already measures original/retained row terms,
lower degree, signed s range, v degree and rational bit lengths plus artifact
bytes. Caller dispatch records bind telemetry with elapsed/resource metadata;
the harness records fixture sizes/hashes. No new algebra/statistics loop, solver,
factorization or prime is added. Counts, sizes and runtime are UNKNOWN now.
The one capped harness repeats the verifier for twelve fixtures, some reaching
late checks; its cost may be substantial. Root can choose one initial control
cap, not silently split/retry or run a larger budget after failure. Incomplete
controls remain incomplete even if the genuine positive passed.

Before any execution, the narrow caller/harness delta needs independent static
review, then fresh same-caller preflight under root's registered caps. There is
no authorization to fill the template or launch from this publication alone.
