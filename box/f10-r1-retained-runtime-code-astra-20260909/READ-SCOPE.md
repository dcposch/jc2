# Exact read scope

All eight paths in input-pins.json were hash-checked before WHOLE reads and
rechecked unchanged at17:23:29 UTC. Whole reads were recovered individually
where an initial concatenated display truncated. These are the only scientific
or prior implementation bodies consumed. The accepted17d gate is consumed at
STATIC scope, not as runtime evidence. Four source files were copied through
apply_patch without edits; their final hashes equal the corresponding originals.

The existing artifact_finalize publication tool was read for lifecycle syntax
and used only for metadata/publication. New own files and the literal caller
diff were read statically. No program in this packet was imported, compiled,
executed or tested. No predicate test, numerical toy or symbolic computation
occurred. File hashes, documentary reads/diff and report publication only.

CAPRUN is a required external runtime input identified by the allowed exact
hash, not a body read here. Original exact.json, admissibility.md and frontier.json
are future pinned runtime inputs only; none of their bodies was read. No source
artifact coefficients or unreviewed fifteen-row material were consumed. No
live peer, process state, protected tree, network or worker was accessed.

The own report and new code received a WHOLE static read before report close;
the byte-identical copied files reuse the explicitly verified WHOLE source
reads. The own documents and literal diff were read through at17:25:24 UTC,
with any subsequent small patch read again before close.
Any runtime outcomes described in VALIDATION.md are proposed expectations,
not observations. Source-count constants are imported declarations, not newly
measured artifact statistics.
