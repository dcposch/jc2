# Read scope and provenance

First action 2026-09-09 18:46:50 UTC. Controlling cap 19:11:50 UTC, earlier
than the alternate absolute19:13 cap. All new targets were absent first.

WHOLE after current hashes: accepted17j composition producer and gate. Their
historical provisional labels are superseded at exactly the assigned scope.
The eleven component paths were obtained only from the explicitly charged
PINS.json and each current hash checked before any component body access.

Component read scope: first producer sections2-6 (and its displayed controls);
second producer sections2-7, including completed matrix/rho/forcing; third
correction gate sectionsA-C (correct N_i and V4 correction and literal left
inverse); fourth producer sections1-5, including full response, unimodularity
and accepted Gram prescription; Euler producer sections2-6 and the residual
and coefficient-envelope interface in section7; leading producer sections1-4.
The other component gate files were pinned but their bodies were not required
or read. No claim of WHOLE reading all eleven components is made. Relevant
component definitions were selected using explicit sed ranges, with repeated
ranges covering required material when a combined tool output was truncated.

WHOLE code: the charged retained transformer, retained checker and unchanged
execution helper. They are also preserved as byte-identical owned reference
copies; only the helper is imported by the new entrypoints. The original
exact.json was hashed only; its schema/map/read-back interface was read through
the accepted code. No assertion of reading all88634 coefficient bytes locally.

New algebra.py, generator.py and checker.py are textually derived from the
accepted dense-v producer / flat-monomial checker architecture and the accepted
four-band recipes. They are NEW unreviewed code, not byte-identical accepted
algorithms. The helper is byte-identical. CAPRUN is a required external hash
only and was not read or edited in this task.

No new univariate producer/gate, runtime packet, mutable ledger, other
provenance body, protected project, peer or live file was accessed. No web,
AWS, SSH, process inspection, mathematical subprocess, arithmetic script,
compile/import/test, emitted row, worker assignment or solver action occurred.
Only documentary reads, hashes, apply_patch and existing transaction tools.
