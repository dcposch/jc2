# Disabled normalized-backend validation plan

No command in this packet has been compiled, imported, run or tested. No
normalized coefficient artifact exists. All counts below are schema slots,
not measured nonzero counts. This plan neither registers nor invites a run.

One future review must first inspect the new algebra.py, generator.py and
checker.py together. execution_gate.py is an unchanged accepted copy. The
unchanged retained transformer/checker copies are reference code only; neither
is imported by the new implementation. Do not execute them as extra evidence.

If root later authorizes a finite validation, use its existing exact-argv
caller and unchanged CAPRUN, with fresh per-operation frozen authority,
exclusive paths, physical/input/source pins, fixed deadlines and one child at
a time. Root chooses all caps, worker and absolute stop. No proposed cap here
is permission or a performance estimate. No CAS, solver, factor, alternate
presentation, prime, source power expansion or auto-retry is part of the plan.

The two production entrypoints are exactly:

    /usr/bin/python3 -I -B GENERATOR AUTHORITY ORIGINAL NORMALIZED
    /usr/bin/python3 -I -B CHECKER AUTHORITY ORIGINAL NORMALIZED RECEIPT

The full current hashes of generator.py, checker.py, algebra.py, the unchanged
helper, runner and both input artifacts must be registered. Missing or disabled
authority refuses before mathematical imports. Existing caller refusal and
cleanup controls must precede any newly authorized mathematics; no new caller
or supervisor is supplied here.

Acceptance requires an actual complete generator exit, exact output pin and
a separate genuine checker entrypoint PASS. A produced file or row count alone
is not acceptance. A cap, parse error, mismatch, incomplete artifact or deadline
refusal is STOP/NONDECISION. No patch/retry/increase is authorized by this plan.

## Finite changed-object controls, all still unimplemented/unexecuted

Each fixture must preserve canonical ordering/rational strings and receive its
own new hash and authority. The intended semantic rejection must be observed;
a stale hash rejection is not coverage of the named mathematical check.

1. Drop one G: nine-G inventory rejection.
2. Omit an original zero/guard slot: all20 inventory rejection.
3. Change one completed matrix or inverse entry: original source-column or
   completed inverse identity rejection, never a sampled-rank assertion.
4. Change a third-band c3 coefficient to the old shifted-index rule: full
   original affine-forcing rejection; keep the homogeneous column unchanged.
5. Change the fourth left inverse: literal column identity rejection, unless
   the changed pair is another genuine left inverse. Genuine alternatives
   require recomputing the complete map and are not negative fixtures.
6. Change the normalized target sign or delete its z-dependent term: full
   Pi/Jhat or original full-forcing rejection.
7. Drop/change H0 or H1: exact low projection/original unsquared-target rejection.
8. Alter a leading inverse or p coefficient: fixed-modulus/unit-identity rejection.
9. Add a forbidden s exponent or wrong weight: normalized domain/weight rejection.
10. Omit an inverse-pole inventory slot: inventory rejection only. Changing A/B
    usually fails earlier original-binding checks; do not call that a late
    pole-path negative. Genuine acceptance does check all ten actual poles.
11. Change an upper B coefficient: original complete coefficient/slot binding
    rejection; not a claim that the later bracket check was reached.
12. Precision group: float rejection, noncanonical string rejection, and a
    canonical changed rational above 2^53 rejected by exact source binding.
    A positive large-integer parser fixture, if later proposed, must actually
    exercise the checker parser; Python int retention alone does not prove it.

No optimized-mode, branch-count or runtime control evidence is claimed. Future
receipt quantities are measured only in that authorized run: input/output
bytes, term counts, exponent/weight envelopes, maximum rational bit length,
wall/CPU/sample-RSS and child/caller telemetry. In particular a smaller number
of coordinates may increase coefficient sizes and runtime.

The checker shares trivial linear sparse operations and exact Fraction with
the generator backend; its parser, modulus construction, multiplication,
reduction, powers, source-row route and complete bracket/pole calculations are
separate. This is not two fully unrelated arithmetic libraries. The review
must inspect that remaining shared surface rather than assume independence.
