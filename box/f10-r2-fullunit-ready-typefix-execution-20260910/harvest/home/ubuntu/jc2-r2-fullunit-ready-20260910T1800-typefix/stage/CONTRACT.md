# Fixed89/root0 fullunit runtime — DISABLED source preparation

New dispatcher and mutator only. Original five science files, fullunit entry/produce/check, original arithmetic modules, probe and CAPRUN are unchanged external runtime inputs. Nothing here is runtime-tested or authorizes registration, worker use, retries, source acceptance or a mathematical conclusion. One FIRST independent runtime/mutator review is required.

## Layout, bindings and sequence

ROOT supplies three distinct root-owned0755 directories: `science_dir` with exactly the original five SCIENCE files; `fullunit_dir` with exactly entry.py/produce.py/check.py at the accepted literal hashes; `wrapper_dir` with exactly dispatch.py/probe.py/mutate.py. Science/fullunit/mutator files are0444; the probe stays unchanged and must have its accepted exact path/hash in ROOT's pins. It is not copied in this source-only package. CAPRUN is the unchanged4435279d instrument. No source, baseline, native file or registration is on the working tmpfs.

Schema `f10-r2-fullunit-modular-runtime/v1`; jobtag `f10-r2-fullunit-modular-runtime-v1`. The template is deliberately disabled/incomplete. ROOT alone supplies and authenticates enabled registration bytes, code/interpreter/native/role pins, EC2 identity/boot/namespace, exact cgroup/UID/GID, clear environment, actual original clocks and fresh storage. Self hashes and DMI strings are bindings, not authority.

Exact ordered thirteen labels: refuse-status, refuse-caps, refuse-inventory, refuse-source, refuse-hash, valid, startup-produce, startup-check, dummy, fullunit-produce, fullunit-positive, mutate, fullunit-negative. The first nine are unchanged authority/startup/samePGID descendantRSS/TERM/KILL controls. The original baseline producer/positive checker is NEVER rerun. Fullunit-produce is a new fixed-cut positive search against the future baseline; fullunit-positive is the unchanged independent literal full-source identity checker.

The dispatcher is metadata-only: it hashes/sizes baseline/candidate/changed bytes but never parses coefficient payloads. All source/native/current files, exact six-field native manifest, directory children, aliases, absent paths, sys.path and actual mapped paths retain strict checks. No missing-map exemption, eager import, favorable-identity retry or speculative allowlist is added. Non-atomic process observation and sampled cleanup limitations of the accepted caller remain; a failed map/capture/quiet condition stops.

## Exact commands, unknown candidate and caps

Outer dispatcher/CAPRUN and mutator use `-I -S -B`; original probe/startup and fullunit entries use `-E -s -S -B`. All full CAPRUN argv lists are ROOT-registered and rebuilt/compared before launch. Exactly one `ROOT_REGISTRATION_SHA256` token is allowed in each last-four vector, immediately after its unique `--root-registration-sha256` flag; none in the first nine. Only that slot is replaced after the immutable registration's SHA is known; no general substitution or unknown-candidate-digest token exists.

Fullunit entry has17 sys.argv entries: script followed by ordered pairs registered-job=`f10-r2-fullunit-modular89-v1`, root-registration-sha256, self-sha256, entry-sha256, arithmetic, baseline, certificate, receipt. Produce uses original arithmetic.py; check uses original check_arithmetic.py. Positive candidate is moved/frozen ROOT0444 in0555 before checking. The exact successful receipt, including future candidateSHA, is independently validated by the dispatcher as a whole path-to-hash dictionary and frozen before mutator import.

Mutator has19 sys.argv entries: script followed by registered-job=`f10-r2-fullunit-modular-mutation-v1`, root-registration-sha256, self-sha256, arithmetic, baseline, certificate, positive-receipt, output, receipt. The fixed positive-receipt path is frozen/fullunit-positive.receipt.json. Its schema/status/root/source/baseline/digest bindings and current source hashes precede mathematical import; baseline and candidate bytes must match their pins. The ROOT-authenticated caller has already checked the exact source map; the mutator's duplicate source-digest-set check does not substitute for that map or for ROOT authentication.

Joint limits unchanged600wall/500CPU/2GiB/16MiB, sampledCPU480 cutoff with cleanup reserve. Profiles wall/softCPU/RSS: control5/3/2GiB; dummy5/3/32MiB; fullunit120/99/2GiB; mutate30/24/2GiB. Thirteen allowances total435wall/361hardCPU, not performance or strict real-time guarantees. Each launch requires wall+15 within original and aggregate deadlines; current input/authority/native pins precede Popen, post-return checks remain. Certificates each<=2MiB; receipts<=32768; combined working mount<=16MiB, noncertificate metadata<=1MiB. Required actual runner/Python capture and quiet-before-read barriers are unchanged. No extra native probe, test, prime/cut/order/cap increase or retry is present.

## Independent mutation and semantic guarantee

The mutator imports ONLY original arithmetic.py after metadata authentication/bindings. It reconstructs literal monicP7 at tau2/7, proves89 prime by trial divisors2..9, verifies root0 and nonzero derivative and ALL eight modulus denominators. It parses all20 used source polynomials independently: Psi5/6/7; K1_0..6; K0_0..8; U. Every six-coordinate support, weight and dense seven-coordinate rational wire is checked; all denominators must be89-units BEFORE root0/z1. No S/vartheta, z0/highest chart, X1=1 or quotient reduction is substituted. Mixed generator is K1_0-U*K0_0 with bound19/class4, using Uweight3,K1_0weight14,K0_0weight16.

The literal eighteen labels/slots, canonical certificate bytes/schema, modulus, caps25/24/23 then12, opposite classes and all cofactor positions are checked. The mutator independently rechecks the input whole polynomial identity. It selects the first nonzero specialized generator whose cofactor is nonempty, and its EXISTING first monomial (index0). It adds1 modulo89 to that nonzero residue, deleting just that term at88->0. All eighteen slots remain. No constant or other exponent is inserted. The difference is a monomial times a nonzero polynomial in F89[Y1,Y2,Y3], hence nonzero. Every shape, degree and class predicate stays satisfied, so the unchanged checker must reach its final literal identity error. The whole original certificate is reloaded separately; restoring the exact old term list must reproduce ALL original canonical bytes. No other mathematical test is added.

Mutation receipt schema `f10-r2-fullunit-modular-mutation/v1`, status `ONE_COFACTOR_MUTATED_UNCHECKED`, science_outcome NONE and checker_run false. Exact keys: schema/status/science_outcome/job_tag/root_registration_sha256/sources/baseline_sha256/baseline_bytes/input/output/selection/inverse_edit_restores_whole_input_bytes/checker_run. input/output are exact path/SHA/decimal-byte dictionaries. selection keys: slot(string0..17), generator(label), index(string0), exponents(three canonical0..30 strings), old(1..88 string), new(0..88 string), term_deleted(boolean, equivalent to new0), old_length(1..754 string), new_length(0..754 string). Runtime validates bounded metadata and whole file hash/readback, NOT residue arithmetic. Mathematical correctness is the mutator's reviewed code and the actual unchanged checker rejection.

## Terminal branches, exact negative and durable custody

Producer normal2/resourceNone/empty streams/no candidate plus exact `INCONCLUSIVE_NO_CERTIFICATE` receipt ends at TEN calls with that status and scienceNONE. It is not a bad-prime diagnosis. No later checker/mutator starts. Normal0 requires bounded candidate plus CANDIDATE_UNCHECKED receipt and freeze. Positive checker requires normal0/empty streams plus exact `FULL_SPECIAL_UNIT_CHECKED_NO_SOURCE_OUTCOME` receipt; freeze it before mutation. Positive checker normal2/INCONCLUSIVE_FIXED_PLACE is unexpected STOP, never a negative pass.

After mutation normal0/empty streams, validate actual changed digest and receipt, then freeze changed.json and mutate.receipt.json before the unchanged checker. Expected negative only: NORMAL_EXIT1, resourceNone, no signal, empty stdout, no negative receipt; exact header and three ordered absolute check.py frames198 module/main(),188 main/verify(ctx,raw),181 verify/raise; final `ValueError: CHECK FAILED: fixed-place literal full-source unit identity` plus newline. At most one bounded legitimate caret-decoration line per frame; no extra/chained/early frames or substring acceptance. Any parser/place/native/resource/capture mismatch stops without scientific success.

Thirteen-call wrapper success is `FULLUNIT_CHECKED_NEGATIVE_CONFIRMED`, ALWAYS science_outcome NONE. It records one fullunit certificate and one corruption rejection only. ROOT separately needs an actual checked highest certificate, source/data acceptance and the accepted conditional lift before source-zero; this wrapper never performs that composition or exports a source/JC2 conclusion.

All relevant owned groups must be quiet before payload intake, freezes and durable EBS copy. Same fsync/0444/readback/pin/inventory barriers and absent durable directory apply on success or STOP where possible. Byte-identical custody replicas are outside the working-output budget, not new science results. Dispatcher never deletes, unmounts, stops a worker or retries. ROOT harvests after terminal writers, independently verifies bytes and chooses any next action.
