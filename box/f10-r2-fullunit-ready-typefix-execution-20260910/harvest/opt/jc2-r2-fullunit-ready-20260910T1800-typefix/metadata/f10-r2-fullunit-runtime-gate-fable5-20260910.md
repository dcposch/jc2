# FIRST Fable 5.1 gate: fixed 89/0 fullunit runtime and independent cofactor mutator

status=UNSEALED (no Seal, no charge_basis authored; adapter seals)
lane=f10-r2-fullunit-runtime-gate-fable5-20260910
gate_model=claude-fable-5-1
first_action_utc=2026-09-10T17:14:37Z (inputs dir 17:14Z); stop = earlier of ROOT UNIT LAUNCH+18 min (no later than 17:32Z) and 17:34:00Z USER TERM (KILL 17:34:05Z); final-3-min reserve from 17:29Z; never reset
mode=STATIC; manual reasoning/text/hash only; ZERO subprocess/import/AST/syntax/compile/test/CAS; tools date, ls, wc, sha256sum, diff, cat, sed, awk line numbering, grep, apply_patch only
owned=xmodel/f10-r2-fullunit-runtime-gate-fable5-20260910.md, box/f10-r2-fullunit-runtime-gate-fable5-20260910/ (both absent at 17:15Z, checked before any write)

## 0. Custody, read scope, disclosure

All 13 snapshots in /tmp/jc2-lane.EiN8cu/inputs (distinct basenames) were hashed by sha256sum at 17:14:37Z before any body; all 13 equal the charged pins (rows generated from sha256sum output into box/input_custody.md; this body carries no hex digest). WHOLE reads: mutate.py 254 lines, CONTRACT.md 41, DISABLED-PLAN.json 32, Astra runtime report 44 (through its Seal), arithmetic.py 277, check.py 198, produce.py 216, entry.py 142, each in one numbered read ending at its last line; dispatch.py in three bounded reads 1-240, 241-480, 481-696, the last ending at 696; the three prior gate reports (78, 69, 62 lines) each in one read ending at its marker. previous-dispatch.py (686 lines) was read WHOLE through a whole-file GNU diff against dispatch.py: every old line is either identical to its new counterpart or printed in a hunk, and all hunks were read. Nothing clipped, no recovery read needed. Nothing executed, imported, parsed, compiled or computed by tool; no baseline, candidate, registration, worker, provenance, corpus, Git, network, other lane or live file was opened. All sources remain UNEXECUTED; no runtime result, certificate, source outcome or actual authority is claimed.

Disclosure: different-model FIRST review of Astra's new dispatch.py, mutate.py, CONTRACT and template. The three charged gate reports (fullunit modular code gate, highest modular runtime gate, highest status-fix gate) are Fable 5.1 authored, the same model as this reviewer; they are consumed as accepted scoped imports (fullunit entry/produce/check science and code, the accepted caller's nine controls, CAPRUN telemetry keys, probe, native closure, custody) and are not re-proved. previous-dispatch.py is the byte-identical accepted highest status-fixed source, a comparison basis, not a new implementation. The original coefficient baseline is promoted separately and is not charged for body or hash read here.

## 1. Verdicts

| item | verdict |
|---|---|
| A whole-dispatcher delta versus the accepted highest status-fixed source: first nine controls and ROOT authority/native/map/pins/argv/budget/capture/quiet/freeze/custody unchanged; only fullunit layout, job/schema/hash, phase and selection/traceback literals | CONFIRMED |
| B literal 13 full-path vectors, 17/19 argv, one typed ROOT hash hole in each of the last four only, producer normal-2 INCONCLUSIVE_NO_CERTIFICATE end at ten calls, fixed limits and profiles, 435/361 allowances, statuses equal to the accepted fullunit entry/produce/check | CONFIRMED |
| C candidate and positive receipt frozen root-bound before mutator import, candidate digest from the independent checker receipt with no circular argv token, exact path-to-hash map at the caller, mutator set check only a duplicate, pre/post pins | CONFIRMED |
| D mutator imports only original arithmetic.py; literal P7 at tau 2/7, 89 prime, simple root 0, all seven coordinates of all twenty polynomials before z=1/root 0, eighteen generators with mixed row bound 19 class 4, input identity rechecked, existing-term +1 mod 89 with deletion at 88, whole-byte inverse, domain argument | CONFIRMED |
| E exact negative shape: normal 1, no signal/resource/receipt, empty stdout, three ordered check.py frames 198/188/181 and the literal final line, optional bounded carets; every other outcome STOP; metadata-only dispatcher | CONFIRMED |
| F success FULLUNIT_CHECKED_NEGATIVE_CONFIRMED with science NONE, one special unit check plus one semantic rejection, no source/generic/all-r/JC2, no properness or rank claim, no baseline rerun | CONFIRMED |

Smallest blocking correction: none found. REFUTED: nothing. GAP: none in the source; the remaining unknowns are execution-only and are listed under OPEN(S) RAISED.

## A. Whole-dispatcher delta against the accepted source

CONFIRMED. GNU diff of previous-dispatch.py (686 lines, 44384 bytes) against dispatch.py (696 lines, 45249 bytes) prints exactly 27 hunks, and every changed line is one of: the docstring word highest to fullunit (:1); CONTROL_JOB, MUTATION_JOB, FULL_JOB and the FULL sibling map (:23-28), whose three digests equal the charged entry.py, produce.py and check.py pins; MUTATOR_SHA (:31), equal to the charged mutate.py pin; the profile key and the four phase labels (:42, :44); the registration schema (:164); the fullunit_dir role and its disjointness, pin, outside-tmpfs and revalidate barriers (:186-187, :205-206, :224, :266, :275-279); the fullunit role/argv branch (:364-374) and the positive-receipt path in the mutation vector (:381); the receipt method name, schema and source map (:499-512); the mutation receipt schema and the nine-field selection block (:521, :536-551); the three traceback frames and final line (:557-560); the phase calls, statuses, binding file and exit literal (:605-645, :692). Nothing else differs, so the five refusals, valid, two startup refusals and dummy (:572-604), ROOT/EC2/host/boot/namespace/cgroup/interpreter/dispatcher identity (:160-183), literal outer argv (:194-195), pins for the seven roles, five science files and native manifest (:196-213), tmpfs/output/stream/durable/deadline checks (:214-239), budget (:252-254), revalidate with the native closure and current pins (:256-306), check_maps (:308-314), group_quiet (:316-320), size_budget, authorize, the run() barriers with live capture (:415-479), freeze, durable_copy and main are byte-identical to the accepted highest status-fixed source. The accepted caller's non-atomic process and mapped-file observations and its sampled cleanup remain at their accepted scope and are not raised here as a new barrier.

## B. Vectors, hole, statuses, caps

CONFIRMED. run() rebuilds each CAPRUN vector from literals, role paths, profile numbers and the computed script (:394-398) and requires it equal to the registered list (:400, :414). Fullunit script (:369-373): script plus the eight ordered pairs registered-job FULL_JOB, root-registration-sha256, self-sha256 FULL[mode], entry-sha256 FULL[entry.py], arithmetic, baseline, certificate, receipt, 17 entries (:374); entry.py:36-40 requires exactly that flag order, 17 entries and TAG f10-r2-fullunit-modular89-v1, which is FULL_JOB. Produce receives science arithmetic.py and check receives science check_arithmetic.py (:368), whose SCIENCE digests equal entry.py:8-9's ARITH map. Mutation script (:377-383): script plus nine ordered pairs, 19 entries (:384), equal to mutate.py:19-22's flags and JOB, with the certificate the frozen candidate (:376) and the positive receipt the frozen fullunit-positive receipt (:381). Token: for the last four labels (:402, LABELS[9:]) the registered list must contain the flag once and ROOT_REGISTRATION_SHA256 once, directly after the flag, and only that element is replaced (:405-411); the token is forbidden in the first nine (:413). No candidate digest appears in any vector. Producer end: normal 2, resource None, empty streams, no candidate, then the exact 11-key receipt with status INCONCLUSIVE_NO_CERTIFICATE and digest null (:606-611); produce.py:202-208 emits exactly that on any ValueError and exits 2 with nothing printed; the dispatcher returns that status with science NONE and calls 10 (:612-613), never a bad-prime, regularity or properness reading. Normal 0 requires the bounded candidate and a CANDIDATE_UNCHECKED receipt with its digest (:614-618, produce.py:212). The positive receipt must be FULL_SPECIAL_UNIT_CHECKED_NO_SOURCE_OUTCOME with the frozen candidate digest (:623-624, check.py:194); the 11-key layout and the three-path source map (:504-511) are entry.py:137-141 with sources me/entry/arithmetic at the FULL and SCIENCE digests. Caps: LIMITS 600/500/2147483648/16777216 and PROFILES control 5/3/2GiB, dummy 5/3/32MiB, mutate 30/24/2GiB, fullunit 120/99/2GiB (:39-42) equal DISABLED-PLAN.json:6-7 and are required of the registration (:166). Walls 8x5+5+3x120+30 = 435; hard CPU at soft+1 (accepted CAPRUN import) 9x4+3x100+25 = 361; each launch needs wall+15 inside 600 and the original deadline (:356, :421), sampled CPU cutoff 480 (:254, :452), late-return wall+15 (:453), post-return 600/500 (:462). used_labels forbids any retry (:353); no cap is raised anywhere. These are prospective bounds; no actual cost is inferred from them.

## C. Freeze order, candidate digest, bindings

CONFIRMED. candidate.json and the producer receipt are frozen (:619) before the positive check runs on the frozen path (:620-621); the positive receipt is validated as a whole dict against the exact path-to-hash map with the frozen candidate digest (:623-624) and frozen (:625) before the mutator is launched (:626). freeze() renames on the same tmpfs, chowns root, chmods 0444, fsyncs, rehashes and pins (:485-497), and every later revalidate requires the frozen inventory exact, 0444 and digest-stable (:260-266), including directly before Popen (:421) and after group quiet (:460). The mutator learns the candidate digest only from that immutable receipt: mutate.py:69-90 (before importlib at :91) require the receipt to be canonical bytes with exactly 11 keys, the fullunit receipt schema, the positive status, science NONE, mode check, prime 89, root 0, root hash equal to its own CLI value (the same self.root_sha the dispatcher passed), baseline digest and byte count, three source digests, each source path currently hashing to its digest, the baseline bytes equal to the literal digest and length, and the certificate bytes hashing to the receipt's certificate_sha256. No prospective SHA token exists in argv. The mutator's set comparison (:80-84) is weaker than the caller's map (:507-512), but the caller already checked the map on the same bytes before freezing, so the set check is a duplicate binding, not the authority, exactly as CONTRACT.md:21 states. Self and arithmetic pins are read before any JSON (:47-51); output collision (:52); baseline, certificate and receipt are re-read byte-equal and the three positive sources re-hashed after the mathematics and before the exclusive writes (:234-238). Reads use O_NOFOLLOW with before/after fstat identity (:37-46); the immutability of inputs against a concurrent same-user writer is the accepted ROOT-freeze assumption, unchanged.

## D. Independent mutator: reconstruction, selection, edit

CONFIRMED. The only mathematical import is arithmetic.py loaded by absolute path under a private module name (mutate.py:91-93) after every metadata binding; produce.py, check.py, entry.py and check_arithmetic.py are never imported, and the mutator runs with -I -S -B (dispatch.py:390), so no script directory is on sys.path. Only Q, qa, qs, qm are used, none of the P7-dependent helpers. Literal P7: tau 2/7 and the dl, kl, ga lists and the qa/qs/qm expression (:109-113) are produce.py:39-47 with int scalars 2, -6, 245 in place of Q(2), Q(-6), Q(245); qs multiplies Fractions by the int and qm accumulates into Q(0) slots, so the lists are equal; :114 checks length 8 and the leading coefficient -245*120*144*tau before the monic division (:115). Prime: :108 trial-divides 89 by 2..9, complete since 81 < 89 < 100. mod() (:104-107) refuses a zero denominator residue and readbacks the Fermat inverse; :123 requires coordinate 0 of the reduced modulus zero and coordinate 1 nonzero, the simple root at 0, after :121-122 force the baseline's eight P7 strings to parse (own rat(), :95-103, the producer's rules) equal to the reconstructed modulus. Baseline: canonical bytes, format, job tag, six variables, weights, base, seven basis strings (:116-120), scale, guard, targets and the seventeen-name rows list (:128-130), row families Psi/K1/K0 of lengths 3/7/9 (:131-134). source() (:136-155): row cap 4096, aggregate 50000, pair shape, six exponent strings 0..16, seven-coordinate dense wire, nonzero and strictly increasing terms, e[4] = e[5] = 0 and full weight e0+2e1+3e2+5e3 equal to the row weight; all seven coordinates pass mod() at :152 before coordinate 0 is taken at :154 (root 0) and the key drops e3 (z = 1), summing over z-degree, no X1 = 1 or quotient. Twenty polynomials: Psi at 5,6,7, K1_0..6 at 14-i, K0_0..8 at 16-i, U at 3 (:156-159), as produce.py:113-116 and check.py:115-118. Mixed row K1_0 - U*K0_0 mod 89 (:160-165) as produce.py:117-122; generators psis + k1[1:] + k0[1:] + [mixed], 18 with empty slots retained (:166-167); weights 5,6,7,13..8,15..8,19 and caps 25,24,23 then 12 (:126-127) equal check.py:74-75; :169-170 require every generator term at or below its weight and in its class, giving bound 19 class 4 for the mixed row. Certificate (:171-200): canonical bytes, exactly the ten keys, schema, baseline digest, prime, root, z=1, Y1..Y3, weights, cut 30, modulus strings equal to the baseline P7 strings (the producer emits wire_q of the same reduced Fractions the baseline strings were forced canonical against, so equality holds), 18 slots with the indexed labels, per-slot and total 754 bounds, three exponent strings 0..30, strict order, residues 1..88, weight at or below the cap with weight plus generator weight divisible by 5. Identity (:201-208): every product forced to weight at or below 30 and class 0, accumulated mod 89, required equal to the polynomial 1; this is the mutator's own accumulator, not the checker's box or the producer's matrix. Selection (:209-211): first slot whose specialized generator and cofactor are both nonempty; since the sum is 1, one exists; index 0 is an EXISTING term, never an insertion. Edit (:212-219): old residue 1..88, new = old+1 mod 89, replaced in place with the same exponent list, or deleted exactly when 88 becomes 0; slots, order, exponents, caps and classes are untouched, term counts do not grow. :220-221 require the changed bytes to differ and stay bounded; :222-223 reload the changed bytes, restore the old term list taken from a separate load of the original bytes (:212-213) and require the original bytes back, the whole-byte inverse. Domain argument: the change in the identity sum is (new-old) Y^e g_slot with new-old = 1 mod 89 in both branches and g_slot nonzero in F_89[Y1,Y2,Y3], so it is nonzero and supported on positions the original product box already contained; the unchanged checker therefore passes every predicate before its final loop and must fail there. A constant inserted into a nonzero-class slot would fail check.py:162 before the identity, which the design correctly avoids. Receipt (:240-250): 13 keys, status ONE_COFACTOR_MUTATED_UNCHECKED, science NONE, job, root hash, the two-path source map, baseline digest and bytes, input/output path/SHA/bytes, the nine-field selection with exponents copied as strings, restoration true, checker_run false; dispatch.py:515-552 accepts exactly this shape with slot 0..17, generator equal to the indexed label, index 0, three exponent strings 0..30, old 1..88, new 0..88 distinct, term_deleted equal to new = 0, old_length 1..754, new_length 0..754, plus actual size and digest readback of the frozen candidate and writer changed.json and distinct digests.

## E. Exact negative and STOP semantics

CONFIRMED. After mutation normal 0 with empty streams (:627) and the receipt check, changed.json and the mutation receipt are frozen (:629) before the negative check on the frozen changed path (:631). Acceptance is normal 1, resource None, child_signal None, child_exit_code 1, empty stdout and no writer negative receipt (:632-634); the receipt path is fresh so entry.py:84 cannot fire first. traceback() (:554-570) bounds stderr at 8192 bytes, decodes ASCII (paths are restricted to [A-Za-z0-9_./-] by :81), requires the literal header, then exactly three ordered frames with the fullunit check.py path: line 198 in module main(), line 188 in main verify(ctx,raw), line 181 in verify the raise statement, then the final ValueError line and nothing else. Against the charged check.py: :197-198 is the guarded main() call, :187-188 the try body, :181 the raise inside the position loops with no enclosing handler in verify, so no chained text; main() catches only Inconclusive (:189), and the ValueError is not one. After each frame one optional line of spaces, tildes and at least one caret, at most 512 bytes, is accepted (:567-568), never required. Any other outcome is STOP: checker exit 2 with INCONCLUSIVE_FIXED_PLACE (check.py:189-192) fails normal(t,1); shape, parser, native, import or resource failures give different frames, a signal or RESOURCE_CAP and fail :632-635; a missed live capture fails :479; every such exception becomes STOP_NONDECISION at :680-681 with durable custody, never a pass. The dispatcher parses no coefficient: load() touches only registration, native manifest, telemetry, payload events and receipts (32768 cap); candidate.json, changed.json and the baseline are hashed and sized only (:531-533, :616, :640). Size and cleanup custody are the unchanged size_budget and durable_copy. Existing actual costs cannot be read off the 120/99 and 30/24 profiles; an overrun is RESOURCE_CAP, a fail-closed nondecision with no retry.

## F. Success scope

CONFIRMED. Only the completed 13-call path (:636) emits FULLUNIT_CHECKED_NEGATIVE_CONFIRMED with science_outcome NONE, the frozen candidate and changed digests, the negative reason, the selection and the literal False for any baseline rerun (:638-645), and exit 0 only then (:692). No science produce.py or check.py is called with the real JOB tag except the accepted WRONG-JOB startup refusals (:360, :584-587). CONTRACT.md:39 and the Astra report line 19 state that the status records one checked special unit identity and one rejection, that a source conclusion separately needs an actual checked highest certificate, source/data acceptance and the accepted conditional lift, and that no full-source or JC2 result is claimed; the code carries no lift, no generic, all-r or source-zero field, no rank or properness claim (the producer's inconclusive branch is undiagnosed, B), and no second prime, cut or farm.

## OPEN(S) RAISED

None. No new canonical OPEN. Remaining unknowns are execution-only and fail-closed: the actual producer outcome at 89/0 on the promoted baseline (witness existence inside the weight-30/12 cut and sufficiency of the fixed place are undecided by source); the measured wall/CPU of the fullunit producer, positive checker and negative checker against 120/99 and of the mutator against 30/24, noting that the mutator re-parses all twenty polynomials with Fractions under the tighter profile; live capture inside the 50 ms poll; interpreter traceback layout; the accepted non-atomic process observations. Cheapest next evidence, ROOT's choice only: one fresh, externally authenticated, enabled registration with 13 vectors reproducing dispatch.py:394-398 byte for byte, the token once in each of the last four, pins for the seven roles, five science files, three fullunit siblings and the native manifest, a 16 MiB fresh tmpfs and absent durable target, then independent intake of CUSTODY.json. No cap raise, retry, baseline rerun or extra science farm is requested; no charge_basis, no follow-on authority, no new theorem.

## COLLISIONS

status: EMPTY

- NONE. Own-only check: this lane wrote exactly xmodel/f10-r2-fullunit-runtime-gate-fable5-20260910.md and box/f10-r2-fullunit-runtime-gate-fable5-20260910/input_custody.md, both absent at 17:15Z; no shared, frozen, input, baseline, candidate, source or other-lane file was touched; no corpus scan, Git, network, AWS, SSH, process control, agents or scientific subprocess.

## WHOLE check and attestation

All 13 inputs were read whole as itemised in section 0; the thirteen digests are recomputed by sha256sum after the body and compared against the custody rows before the marker. Authoring: every byte of both owned files was written by apply_patch (custody rows piped from sha256sum output through its stdin; the report as a small skeleton ending on a single anchor line, then A-C, D-F and this tail each replacing the anchor; the marker appended last); no Write/Edit tool, no redirection into any file, no helper, no source or input edit, no Seal, no charge_basis, no execution, import, AST, syntax, compile, test, CAS, network, AWS, SSH, Git, agents, corpus, shared, protected, other-lane or live file. Bound disclosure: the skeleton write measured 622, the A-C write 889, the D-F write 1176 words by wc -w, all under the 1500-word bound. Same-model disclosure: the three imported gates are Fable 5.1 authored and consumed only at their stated scope; the dispatcher, mutator, contract and template under review are Astra's and were reviewed here for the first time. Timeline: hashes 17:14:37Z, whole reads 17:14-17:16Z, absence 17:15Z, custody and skeleton 17:18:31Z, A-C 17:19:11Z, D-F 17:19:55Z, this tail before the 17:29Z reserve; stop no later than 17:32Z, never reset. Heading order verified by grep after this write: 0, 1, A-F, OPEN(S) RAISED, COLLISIONS, this section. The marker below is the only standalone marker in this file and nothing follows it.

<!-- BODY-END -->
