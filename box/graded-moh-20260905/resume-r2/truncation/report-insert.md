The first-power tests are now decisive for two fibres: **c∉I for the 111-parameter V3_8 fibre and the 129-parameter V1_9 fibre.** This rules out N=1 identities; if either completed ideal forces c to be nilpotent, its nilpotence exponent is at least two. It does not decide saturation, supply a surviving c≠0 point, or certify a class kill.

The proof uses specialization only in the valid direction for nonmembership. For a ring map φ, c∈I would imply φ(c)∈φ(I). The positive x-charge grading permits discarding source rows with charge above that of c before applying the map, since they cannot contribute to that homogeneous component. No support specialization is transported as a positive class certificate.

For the 111-parameter fibre, send all 28 charge-zero coordinates to zero and retain the other 65 selected coordinates, including c. Over Q, with order wp(B) and cutoff 3, the 78 relevant direct coefficient rows map into an ideal with a 60-element truncated standard basis G. The normal form of c is exactly c. The independent Python Fraction verifier reduces all 78 original row images to zero, checks all 466 S-pairs whose weighted degree is at most 3, and verifies that every basis element is homogeneous and has degree at most 3. A deliberately corrupted row f+c has nonzero remainder, while a basis row reduces to zero.

For the 129-parameter fibre, send every charge-zero variable z to t^{w(z)}, retain the positive-charge variables, and assign t bidegree (0,1). This is a polynomial map over Q preserving both gradings. In the target ring use positive scalar weight w+42B and cutoff 125=41+42·2. The 70 relevant original row images reduce to zero modulo a 67-element homogeneous basis. The independent verifier checks all 256 S-pairs of scalar degree at most 125 and finds a nonzero 21-term rational normal form of c; both controls pass. A separate clean Singular verification reproduces these checks. The exported basis and rational remainder, not an unverified completion string, carry the result.

For these negative certificates it suffices that every original image belongs to (G): if c is not in the larger ideal (G), it is not in the original image ideal. Truncated Buchberger verification is sufficient because the weights are positive and the target lies exactly at the checked bound. No assertion G⊆φ(I) is needed for this argument. The independent verifiers bind the original source-row hashes, declared variable orders, maps, basis files, and rational arithmetic; their result JSON files are under `resume-r2/truncation/*_N1/{x-specialization,curve-fast}/`.

The round-1 constant-pivot eliminations were also replayed successfully: 42 substitutions for 111 and 37 for 129. Each pivot is a current coefficient generator times a nonzero rational scalar, each eliminated variable has coefficient one after normalization, and the residual rows and target match exactly. The old replay log had a Singular declaration error and attempted unsupported ideal equality, so its markers were rejected; corrected replay compares generators individually. An exploratory curve checker also omitted general.lib, leaving its lcm calls undefined; its success markers were rejected and replaced by the independent rational verifier and the clean Singular check.

Bounded N=2 probes retain every relevant source row: 135 rows for 111 at B≤6 and 123 rows for 129 at B≤4. The former uses the zero-coordinate map above and wp(B) through 6; the latter maps charge-zero A1 variables to t^{w}, other charge-zero variables to zero, and uses w+83B through 414=82+83·4. Both specialized computations completed, with zero normal forms of c². These are only dispositions of the specialized probes: they neither prove nor disprove c²∈I in the original charts and produce no new class certificate.
