Round 2 certifies **c∉I over Q for fibres77 and136**, so any possible nilpotence exponent is at least2. For77, map each zero-xcharge coordinate z to t^w(z), fix positive-xcharge coordinates and c, and use positive weight w+40B through119. The receiver has40 variables; an exact basis with382 elements gives NF(c)=c. Independent Fraction arithmetic verifies all77 input reductions and all775 critical pairs with lcm weight≤119. For136, set zero-xcharge coordinates to0 and use positive xcharge weight through3:79 active variables,76 nonzero input rows,68 basis elements,574 required pairs, NF(c)=c. Specialization is used only to pull back NONmembership; projected membership/UNIT never implies a chart kill. Input maps, hashes, bases and independent replays are in `resume-r2/triangular/{77/curve,136}/`.

The low-weight test also completes without expansion: exact rational triangular DAGs eliminate27/71 pivots from77/136, leaving50/65 coordinates. Compact circuit replays verify every pivot identity and reject perturbing each pivot image by1. In the N=1 component,136 uses49 pivots and60 remaining coordinates, with5,209,906 ambient monomials;77 has18,574,576. The weight<D subsystems contain147/338 rows and admit the exact point c=1, all other coordinates0. Four full N1 runs stopped after these proofs. N2:77 curve gives NF(c²)=0 (receiver only);136 curve times out at600s. Radical status OPEN.
