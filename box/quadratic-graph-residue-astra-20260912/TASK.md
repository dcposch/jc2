# One source-local residue: all quadratic graphs c+k y^2, c*k nonzero

Owner /root/nonemptiness_certificate (Astra), manual co-research, not a
promotion gate. First actual UTC. Original publication reserve September12
11:39/HARD11:42UTC; no reset, stop earlier when this ONE discriminator is
decided. Basis0d39df3c9fd69c939a8420c54d03228b9077777d.

Your preceding constant-graph report was terminal-collected and is under
different-model FIRST. This new calculation is logically independent of
its nonzero-residue theorem: k!=0 makes K_k a UNIT and gives an unramified
source-local residue, not the old ramified K=0 test. Do not read the current
Fable gate or rely on its future outcome. No speculative descendant of its
mathematical assertion is commissioned; literal chart identities must again
be checked where needed.

Hash TASK then WHOLE. Inputs only COORDINATION, the old constant-graph
subalgebra Astra report, and ROOT-CANDIDATE in this box. Three pins in
invitation; prepin before bodies. Same-agent exact-pin WHOLE reuse allowed
with provenance. All links inert; no network/corpus/new input.

QUANTITY: for EVERY c*k!=0, do the proposed actual collision branch and
source-local residue3/(2k) exclude all target-polynomial area primitives
and hence polynomial Keller pairs for H=c+k*y^2? Independently derive or
refute the formulas, handle the exceptional cubic parameter honestly,
check a nonempty generic collision and the normalized local parameter.
One symbolic-parameter family, not sampled coefficients/degree/pole scan.
Cheapest test is the supplied implicit-curve/source differential calculation,
roughly20-minute UNMEASURED manual allocation. No execution forecast.

Outcome exact producer proof or REFUTED/GAP with the precise source failure.
No extension to nonconstant h(xy), zero c/k, another pole or automatic
successor. One meaningful degenerate/exceptional control. Maximum1800
scientific words plus custody. ROOT candidate is RAW, not an accepted input
theorem; independently derive every load-bearing assertion used.

Own ONLY xmodel/quadratic-graph-residue-astra-20260912.md and its transaction
manifest, plus this box's PINS.json, READ-SCOPE.md, custody.json. TASK and
ROOT-CANDIDATE immutable ROOT inputs. Check own output absence before
authoring. Manual math and inert UTC/hash/text/JSON administration,
apply_patch, existing artifact_finalize administrative transaction only.
No science/CAS/helper/code/import/AST/syntax/compile/test/dummy/empty
interpreter, network/AWS/SSH/Git/process/agents/corpus/protected/scratch,
shared ledger edit or canonical OPEN.

Private partial initially unsealed; bounded writes, WHOLE readback, all4
input postpins and quantity/scope/collision check before marker LAST.
Custody SHA FIRST, exact expected manifest, all input/output pins and actual
ALL WRITERS IDLE UTC. No edits after terminal. ROOT owns collection and any
later different-model gate; no present promotion/global JC2 conclusion.
