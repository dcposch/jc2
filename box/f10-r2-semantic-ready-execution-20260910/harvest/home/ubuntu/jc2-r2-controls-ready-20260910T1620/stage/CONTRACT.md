# Disabled r2 semantic runtime: exactly controls 6 and 10

STATIC/UNEXECUTED. FIRST combined code review and a separately authenticated
ROOT registration are required. No worker, enabled registration, fixture or
receipt is supplied. The concurrently authored mutator became terminal and
was released by ROOT during this task; its pinned source is now a charged
input, still NEW/UNREVIEWED until the combined gate. Its three-source receipt
and CLI are implemented literally. Baseline is future HASH ONLY, never parsed
or copied here. No source conclusion or scientific output is claimed.

## Layout and inherited guards

Use the same exact five science files and hashes in dispatch.py: authority,
arithmetic, produce, check_arithmetic, check. None is changed or copied here.
The isolated wrapper has ONLY dispatch.py, unchanged probe.py and the exact
released mutate_controls.py SHA a896a8094be9b38f7ffa3a82cab15d340add0d7ab5f513340b2b7f916e489540.
Both directories are root0755 with root-owned non-g/o-writable o+rx ancestry;
the mutator and baseline are root0444. ROOT installs code, baseline, native
closure and registration outside working tmpfs. The native manifest retains
all six fields and whole directory/alias/absent/pyc/ZIP closure. ROOT alone
supplies installed interpreter/native identities; no discovery or installation.

Outer argv is PYTHON -I -S -B DISPATCH --registration ROOT_REGISTRATION;
clear environment is exactly PATH=/usr/bin:/bin,LANG=C,LC_ALL=C,TZ=UTC.
The new registration schema/job differ from the unchanged scientific authority
tag f10-r2-reconstruction-v1. The five-file authority JSON is unchanged for
the nine preflights and two checkers. A matching authority record is also
saved for the mutator phase solely as inherited launch metadata: the mutator
does NOT consume that JSON. Its ROOT SHA parameter is binding, not authority.
The actual immutable root registration, current pins, caps and exact full
command comparison before Popen authorize that phase externally.

Necessary NEW registration-interface delta: the mutator command includes
the SHA of the ROOT JSON which also contains commands, so a literal final
hash stored inside that same JSON would be self-referential. ROOT's frozen
mutate command must instead contain exactly one ROOT_REGISTRATION_SHA256
token, solely as the argument value immediately after its unique
--root-registration-sha256 flag. Dispatch substitutes ONLY that value with
the actual immutable ROOT JSON SHA, then compares the entire actual argv.
The token is forbidden in every other command and has no shell/environment
expansion semantics. Prelaunch metadata stores the full expanded actual argv.
All other arguments remain literal, including actual mutator SHA. This
proposed narrow delta is part of FIRST combined review, not an accepted
authority change or permission to launch. The disabled template remains null.

The mutator uses PYTHON -I -S -B and exactly its 19-entry sys.argv contract;
checker/probes retain -E -s -S -B. All execute through unchanged CAPRUN and
setpriv in the same recorded child group, without another PGID. Root cwd is
wrapper; CAPRUN child cwd remains the five-file science directory, harmless
under the mutator's -I. Actual fresh instance/boot/namespace/cgroup and literal
outer argv are required; memory.max is exactly2147483648. All source/native
pins, mapped-file checks, authority freeze-before-launch, +15 admission,
post-return deadline checks, actual runner/child capture and group quiet
checks are retained. Missing required science-phase live capture is STOP,
not retry. The accepted ESRCH false-STOP race is left unchanged.

## Exact finite sequence and budget

Five actual authority refusals; valid sentinel; actual WRONG-JOB producer and
checker startup refusals; same-PGID orphan RSS/TERM/KILL dummy; one mutator;
check6; check10. Exactly12 children, not13. The thirteen other scientific
corruption controls are NOT RUN. The two startup refusals are not positive
producer/checker reruns and precede mathematical imports under the accepted
entry contract. A first failure stops; no subsequent child/retry/cap reset.

Original joint600wall/500CPU/2GiB/16MiB is unchanged. Nine controls5wall/3softCPU,
mutator10/9, two checkers210/169 give475 registered child wall and386 hard
child CPU, including CAPRUN's +1 hard CPU per child. Overhead is unmeasured;
sampled480 cgroupCPU stops even a live phase, with500 post-return ceiling.
ROOT sets original mathematical/task deadlines and independent outer/worker
stops separately. Neither budget arithmetic nor the old122-second check is
a forecast or a quota guarantee for the new batch.

All streams/metadata/two fixtures use one root-provisioned16MiB tmpfs.
Each fixture <=15MiB, combined fixtures <=15MiB, all remaining files <=1MiB,
plus the hard aggregate mount bound. The mutator's conservative reserve is
additional. RLIMIT_FSIZE/core stay unchanged. Baseline remains outside mount.
No input body is json-loaded by dispatch: only bounded registration/native,
authority-control metadata/telemetry and the <=32768-byte mutator receipt.

## Receipt, freeze and exact semantic recognition

The receipt must have exactly the released schema fields, status
TWO_FIXTURES_WRITTEN_UNCHECKED, science_outcome NONE, job/root hash, exact
three-source map, literal baseline hash/1652675bytes, ordered two output
records and true canonical/restoration assertions with checker_run false.
Metadata indices and rational STRINGS are bounded; dispatch never subtracts
or compares coefficient magnitudes, computes polynomial identities, or
rechecks mathematical restoration. Those obligations belong to the separately
reviewed mutator and actual negative checker, not metadata flags.

Each regular fixture is hashed and size-checked, distinct from original and
each other. Writer inventory must be exactly the two previous probe payloads,
the two fixtures and receipt. Both fixtures AND receipt move to frozen;
root ownership0444, filefsync and directoryfsync precede frozen-parent0555.
All three frozen hashes and exact inventory are checked before each checker
and after each return. Nothing is checked while science groups are live.

Both checker results must be NORMAL_EXIT1, resource null, signal null,
empty stdout, exact full bounded ASCII traceback. The four frames are:
check.py416 module/main(),412 main/print(check_file(path)); then control6
329check_file/col=residual_pair statement and216residual_pair/upper-band raise;
or control10 Psi376/full-three-Psi comparison, K1/K0 383/retained-K comparison,
followed by check_arithmetic.py208 require. Source text is literal in dispatch.
Only zero/one <=512-character space+tilde/caret line with a caret is allowed
after each source line; no chains, additional frames, text or substring test.
Unexpected CPython formatting is STOP, never relaxed after a failed run.

Psi coefficient mismatch needs no array-length change. A changed coordinate
becoming0 deletes a term only if all seven coordinates vanish; dispatcher
does not infer that from one coordinate. Fallback is precisely firstPsi's
constant term. The exact final ValueErrors are upper band residual; CHECK
FAILED: all three Psi slots; CHECK FAILED: literal retained K coefficient.

Only completion of the exact12-label sequence can produce
TWO_SEMANTIC_REJECTIONS_CONFIRMED, with fixture/receipt/source/stream hashes,
exact reasons, no positive rerun and13other controls NOT_RUN. No source/JC2
outcome. On STOP, retained evidence is not a partial PASS.

## Terminal custody and pending validation

Inherited durable_copy is unchanged: after exact groups quiet, copy all
working bytes into a registered absent root-owned ext4/xfs EBS directory,
verify each SHA, fsync files/directories AND durable parent, small CUSTODY.
Replicas are custody storage, not extra science. ROOT collects only after
dispatcher terminal. No deletion, unmount, worker stop, or post-copy output.

Pending quantities: one combined FIRST static gate, one new ROOT registration
with actual paths/native/interpreter/clock/commands, and one finite12-child
batch. Cheapest runtime check is this exact batch, not a positive reconstruction
rerun or additional semantic farm. No syntax/import/test was performed here.
