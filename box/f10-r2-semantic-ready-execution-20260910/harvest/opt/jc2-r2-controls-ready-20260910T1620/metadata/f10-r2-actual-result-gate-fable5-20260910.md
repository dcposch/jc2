# f10-r2-actual-result-gate-fable5-20260910 — FIRST Fable 5.1 review of the actual r2 reconstruction packet and the minimal remaining scientific controls

status: UNSEALED (adapter seals; no Seal, no charge_basis authored)
lane: f10-r2-actual-result-gate-fable5-20260910
reviewer: Fable 5.1 (claude-fable-5-1), FIRST review
started: 2026-09-10 12:55:36 UTC; deadline min(13:10:36, 13:13:00) = 13:10:36 UTC; reserve from 13:07:36 UTC; never reset
mode: documentary intake; manual text/hash reading only; zero subprocess/import/AST/syntax/test/CAS; artifact.json HASHONLY (never opened)
writes: apply_patch only; owned targets xmodel/f10-r2-actual-result-gate-fable5-20260910.md and box/f10-r2-actual-result-gate-fable5-20260910/input_custody.md

## 0. Custody, read scope, disclosure

All 16 snapshots under /tmp/jc2-lane.0wmifs/inputs were hashed with sha256sum at 12:55:36Z BEFORE any body read; all 16 digests equal the charged list byte for byte. The rows are generated from the sha256sum output in box/f10-r2-actual-result-gate-fable5-20260910/input_custody.md; no digest in this body is typed from memory, every one is copied from that output or from the charged files, and every abbreviated 8-hex prefix in this body was cross-checked against the charged text files at 13:02Z. Both owned targets were absent at 12:55:36Z; the skeleton was created at 12:55Z by apply_patch. Read WHOLE at 12:56-12:57Z: the ROOT report (to its Seal block), the code gate 17zzn (to its marker), the runtime gate 17zzs (to its marker), CONTROLS.md, checker-binding.json, check.stdout, produce/check telemetry, produce/check pre and post, produce/check python-live, and CUSTODY.json. Every read ended at the file's last line; nothing was clipped, no bounded re-read was needed. artifact.json: sha256 and byte count only (dac655a7…a9587, 1652675 bytes), never opened, parsed, or executed. No provenance link, corpus, Git, network, AWS, SSH, agent, other lane, live, shared or protected file was touched; no input was edited.

Disclosure of prior model involvement: the two imported gates 17zzn (3e9a06cf…) and 17zzs (558fa2f1…) were authored by this same model, Fable 5.1, in earlier sessions with no continuity into this one. Their bytes are reused verbatim as charged imports and their findings are consumed as accepted, not re-derived; every line number cited below for produce.py/check.py/dispatch.py is taken from those gates, since no code file is charged here. The Astra code report (947bb434…) and runtime report (002ff55c…) are named by the ROOT report but are not charged and were not opened. No same-byte reuse from any uncharged source; this reviewer's auto-memory holds nothing on f10-r2.

## 1. Verdict table

| item | verdict |
|---|---|
| A actual packet binding | CONFIRMED; two items are ROOT attestation only (frozen 0444/0555 modes; the arithmetic.py and check_arithmetic.py pins have no charged digest outside the ROOT report) |
| B runtime scope | CONFIRMED; the literal child exit 1 of the seven refusals is implied by the accepted dispatcher contract plus the terminal result, not present as a byte in the charged files |
| C mathematical perimeter | CONFIRMED as stated: coefficients only, checked-but-not-promoted, no source outcome; promotion blocked by the absent semantic negative control |
| D minimal control set | two controls: 6 then 10; 8 dropped; 6 alone suffices for the current tier if the mutation designer must keep the artifact HASHONLY |

## A. Actual packet binding — CONFIRMED

Five scientific pins. One identical set of five digests (authority.py 2a274ac4…, arithmetic.py fe9ab1ab…, produce.py eeb3fe3b…, check_arithmetic.py e5dd4bcb…, check.py e6bc7cca…) appears in the ROOT report, in checker-binding.json science_pins, in produce.pre/post and check.pre/post source_pins, in all eleven CUSTODY run records and in the CUSTODY result. Three of the five (authority, produce, check) also equal the charged 17zzs custody table, which hashed those bytes at 11:23Z; 17zzn reviewed all five whole but its digest rows live in its uncharged box file, so for arithmetic.py and check_arithmetic.py the only charged digest is the ROOT report's list. That is consistency across every runtime document plus ROOT attestation, not an independent second source; I name it, I do not treat it as a defect.

Native and registration. The six-field native manifest digest 1e08bbdd… is the native_sha256 of both pre and both post records and of all eleven CUSTODY runs; the registration digest 526edd84… is root_sha256 in both pre records, root_registration_sha256 in the binding, and root_sha256 of CUSTODY and of its result. The authority digest 998f4d59… is shared by produce, check, valid, startup-produce, startup-check, dummy and refuse-hash; that is expected because the authority JSON carries job tag, status, files, runtime and limits but no role (17zzs B), and refuse-hash is specified as the valid file with a wrong CLI pin. The four true refusal authorities have distinct digests, and refuse-inventory is 728 bytes against 806 for the others, consistent with one deleted file entry.

Producer and checker exits. produce.telemetry: status NORMAL_EXIT, child_exit_code 0, child_returncode 0, child_signal null, runner_exit_code 0, resource null, kill_sent/term_sent false; stdout 43 bytes, which is the 42-character FORMED_UNCHECKED_19_ROWS_NO_SOURCE_OUTCOME string plus one newline (counted by hand); stderr 0 bytes with the empty-input SHA-256 e3b0c442…. check.telemetry: the same NORMAL_EXIT/0 fields; stdout 72 bytes, which is the 71-character CHECKED_STATIC_CONTRACT_19_ROWS_32_RESIDUALS_80_SLOTS_NO_SOURCE_OUTCOME string plus one newline; the charged check.stdout has exactly that content, 72 bytes, and its digest 5cab6f3f… equals the telemetry stdout sha and the binding's checker_stdout_sha256. CUSTODY lists produce.stderr, check.stderr, outer.stdout and outer.stderr at 0 bytes each.

Artifact identity. check.pre input_sha256, check.post input_sha256, the binding's input_sha256, the CUSTODY entry frozen/artifact.json (1652675 bytes) and my local sha256sum of the charged snapshot all equal dac655a7…; the local byte count is 1652675. So the checker read the same bytes before and after its run and those are the bytes charged here. Modes 0444 on the artifact and 0555 on frozen/ are not fields in any charged JSON; they rest on the ROOT report and on the accepted freeze path (17zzs E, dispatch.py 442-449). Hardware custody likewise is ROOT attestation, as charged.

CLI and live Python. Both pre records carry the exact 17zzs vector: python3.12 -I -S -B run_capped.py with the profile caps, --rss-sample-seconds 0.05, --term-grace-seconds 1, the three stream/telemetry paths, --cwd science, then setpriv --reuid 65534 --regid 65534 --clear-groups --no-new-privs, then python3.12 -E -s -S -B science/produce.py or check.py with --registered-job f10-r2-reconstruction-v1, --authorization, --authorization-sha256 998f4d59… and --output writer/produce.payload or --input frozen/artifact.json. Telemetry argv_count 22 is the length of the vector after CAPRUN's own arguments (counted). The python-live records show argv equal to pre python_argv, uid and gid quadruples 65534, NoNewPrivs 1, CapEff/CapPrm/CapInh all zero, pid equal to pgid (10190 producer, 10370 checker), start_ticks 50618 and 51702 equal to the telemetry start_identity fields, the same boot 7ed95cce… and cgroup 0::/system.slice/jc2-f10-r2-20260910T1230.service as the root parent 10121 recorded in pre, RLIMIT_CPU 239/240 and 169/170, RLIMIT_FSIZE 15728640, core 0, which is CAPRUN's file_limits plus soft/soft+1 exactly as 17zzs C describes; the mapped files at capture are the interpreter, loader, libc, libexpat, libm and libz only.

Coefficient body and time index. No charged object states any coefficient; the digest proves identity, not content. The trust bridge is precisely the actual executed accepted checker (check.py e6bc7cca…) under the accepted dispatcher, and nothing else. The gates' statements that the code had never run were written at 09:31-09:51Z (17zzn) and 11:23-11:41Z (17zzs) about a run that happened 12:39-12:41Z; they are time-indexed and not contradicted.

## B. Runtime scope — CONFIRMED

Eleven runs. CUSTODY.runs holds exactly eleven records in the accepted execute() order: refuse-status, refuse-caps, refuse-inventory, refuse-source, refuse-hash, valid, startup-produce, startup-check, dummy, produce, check, each label once (no retry is possible without a duplicate label). Every record has runner_live_observed true, actual_python_live_observed true and owned_group_quiet true; resource is null everywhere except dummy, which is resource rss with status RESOURCE_CAP. The timestamps run 12:39:02.62Z to 12:41:22.98Z.

Refusals. The five refusals and both startup calls have status NORMAL_EXIT, stdout 0 bytes and a non-empty stderr whose byte count I checked against the authority.py strings quoted in 17zzs B: 'REFUSED: no registered ROOT CAPRUN contract' plus newline is 44 (refuse-status); 'REFUSED: registered limits/source inventory' plus newline is 44 (refuse-caps and refuse-inventory, which share one stderr digest dae62a80…); 'REFUSED: source pin produce.py' plus newline is 31 (refuse-source); 'REFUSED: authorization bytes/pin' plus newline is 33 (refuse-hash); 'REFUSED: explicit registered tag required' plus newline is 42 (both startup calls, sharing digest fcb37579…). Those are the accepted refusal points, the WRONG-JOB refusal being the one at authority.py:88 before any file, hash, JSON, integer guard or input read. The literal child exit code 1 is not a field of any charged file, since the refusal telemetries are not charged; it is implied because the dispatcher's needs at 409-416 and 419-425 require CAPRUN NORMAL_EXIT with child exit 1 for every refusal, and the CUSTODY result RECONSTRUCTION_CHECKED_NO_SOURCE_OUTCOME with science_outcome NONE is reachable only after all eleven needs, with outer.stdout empty (every STOP path prints a JSON line). Exit 2 belongs only to dispatcher STOP paths, none of which occurred; the older middle-control exit-2 convention is not in play.

Dummy. writer/dummy.payload is 868 bytes and dummy.stdout is 0 bytes, consistent with four event lines and no verdict; the RESOURCE_CAP/rss status with owned_group_quiet true matches the ROOT description (sampled 77537280 above 33554432, TERM 15 then KILL 9, leader reaped). The dummy telemetry itself is uncharged, so the event sequence is ROOT's reading of the accepted CAPRUN, not re-verified here. It carries no scientific inference.

Limits and budget. Profiles [300,239,2147483648] and [210,169,2147483648] in pre equal the telemetry caps; rss_sample 0.05 and term_grace 1.0. Binding and CUSTODY agree on joint wall 141.306285619 and joint cgroup CPU 159.534767; from the ROOT launch capture 12:39:01.667Z to check.post 12:41:22.982Z is 141.3 s. Producer 10.149137086 s wall, max group RSS 25550848; checker 121.775605024 s wall, max group RSS 64172032; both far inside 300/239 and 210/169 and 2 GiB, no RESOURCE_CAP, no signal, no cap raise. The joint figures include the nine controls, the dispatcher's re-hashing and CAPRUN's sampling, as the ROOT report says. The sampled 480 cutoff and the 600/500 envelope live in the accepted dispatcher (17zzs D); at 159.5 cgroup seconds the cutoff never engaged.

Attestation-only items. ROOT's independent terminal observation at 12:42:21Z, the canceled timers, the 249 working/durable/source and 1238 native remote re-hashes, EBS sync, and the worker STOP cannot be verified from this packet; the packet is consistent with them (every durable file is hash-listed in CUSTODY with the same digests as the charged copies). I agree that the vanished transient unit's default Result success is not outer-exit evidence; the outer evidence in this packet is CUSTODY's result plus 0-byte outer streams, and the child exits are the telemetry fields.

## C. Mathematical perimeter — CONFIRMED

What the imports supply. 17zzn A-D CONFIRMED, against the accepted interface 17zzj, the whole 7-coordinate algebra at tau2/7, the 16 constant units with two-sided products, gaps 1-7 including the late operator coefficient -6 at h=6, the actual mixed forcing rebuilt from the source A and B, both quotient maps (forward and rho-subtraction readback), the 19 retained relations (Psi5-7, K1_1..6, K0_1..8, and the scale rows K1_0-U*s^5, K0_0-s^7), the 32 residual slots and the 80 Jacobian slots, the inverse chart with source-scale powers, and the inverse-pole guard with no s=1 anywhere. 17zzs closed 17zzn's F1/F2 by the -E -s -S -B vector and the exact five-name science listing; the actual pre argv shows -E -s -S -B, so the -I import failure 17zzn feared could not have occurred. 17zzn's F6 (verdict string not bound to the artifact by the checker) is closed only externally, by the dispatcher's pre/post input hash and the binding record, never by check.py.

What this artifact supplies. Coefficients of the r2 reconstruction that the unchanged checker accepted under its static contract, identified by digest dac655a7…. It supplies nothing else: no finite basis of R, no dimension d or d=35, no highest-regularity statement, no unit or point of the source, no source emptiness or localization zero, no actual degree closure, and no global JC2 statement. The verdict string, the CUSTODY science_outcome NONE and the result name say so literally. No source solving has run: the two scientific phases are 10 s of formation and 122 s of checking.

Presently usable, as checked-but-not-promoted computation: the byte-identified packet dac655a7… as the unique input of the next tier (the actual highest X1 != 0 chart with all exceptional fibres, or an actual certified finite basis with its dimension), citable only as "formed by produce.py eeb3fe3b… and accepted by check.py e6bc7cca… under 17zzj/17zzn/17zzs, static contract, no source outcome".

Blocked by the absent semantic negative control: promotion of the packet as discriminatingly checked coefficients. The only checker behaviours ever observed are one accept and two WRONG-JOB refusals that exit before arithmetic import; no rejection of a changed object exists, so the accept cannot yet be distinguished from a vacuous accept. Also blocked while HASHONLY: any statement about which retained rows are nonzero, hence any exclusion of the empty-list/zero-row degeneracy that the interface permits (17zzn D: empty lists keep zero slots).

## D. Minimal semantic control set — recommendation (design only, not fixtures, not a pass, not a rerun licence)

Why the nine authorization/runtime controls cannot substitute. They exercise authority.py and dispatch.py only: the science entry refuses at import-order scope before arithmetic.py/check_arithmetic.py are imported, so no unit, band, residual or Jacobian line ran in any of them; the dummy exercises CAPRUN. Why schema/hash refusal cannot count: CONTROLS items 12-15 (duplicate key, duplicate term, oversize, giant decimal) reject in the parse hooks and bound checks (check.py:21-29, :41-63, :81) before any polynomial arithmetic, and an authority pin mismatch rejects at authority.py:115; each proves the parser or the pin, not that the mathematics discriminates. Why a zero-to-zero mutation cannot count: it is either byte-identical (same digest, the same object, no control at all) or a representation change of the same polynomial (an accept is then correct behaviour and proves nothing; a reject could only be a parser event such as a rejected '0/1' term).

Selected control 6: zero the gap-7 mate B_var. Unconditional without reading the artifact: the checker requires the theta^2 coefficient of V_var to be literally 1 (17zzn C, check.py:353-357 against T^2+(F/2)T+(6H-F^2)/10), so zeroing is nonzero-to-zero-object for every possible packet. Encoding: the empty sorted term list, never a '0/1' term (that would be rejected at :81 as a parser event and void the control). Expected semantic branch: the -T^4 upper target of gap 7 survives and check.py:329 raises before :357 (17zzn G item 6). Expected observables: CAPRUN NORMAL_EXIT, child_exit_code nonzero (expected 1; the exact stderr text is whatever :329 raises and must be quoted from the pinned check.py, not from this report), stdout 0 bytes with no CHECKED string, stderr non-empty; pre and post input digests equal to each other and different from dac655a7….

Selected control 10: largest retained row coefficient +1 (CONTROLS item 10 as written, with its tie-break by row, term, basis index; canonical reduction; delete the term if q+1 = 0; if every retained row is zero, add coefficient one to the first row's constant monomial). Named risk it covers and 6 does not: the deliverable rows Psi/K1/K0 are what the next tier consumes, and their only tie to the reconstruction is the 32-slot literal comparison at :379-389; the fallback branch is exactly the vacuity probe for a zero-row packet, which HASHONLY cannot exclude. Expected branch: :383 residual-slot literal mismatch (or :376 only if an array length changed, which an in-place edit must not). It requires ROOT to parse the artifact once to select the coefficient; the selection record (row name, term exponent tuple, basis index, old and new canonical strings) must be published with the mutated digest.

Dropped control 8 (W-inverse coordinate 0 +1): unconditional at :137, but it covers only the unit layer, which 6 and 10 already force to be live (the checker's own Euclid inverses feed both reconstructions, 17zzn A); no named risk remains that only 8 covers, so adding it would manufacture a requirement.

One-control statement. For the current tier (promote this packet as a discriminatingly checked reconstruction, still no source claim), control 6 alone suffices whenever the mutation designer must keep the artifact HASHONLY; control 10 is added for the single named risk of a vacuous zero-row accept at the deliverable layer. Both are check-role runs only, of the UNCHANGED five pins, under a fresh valid ROOT authority and registration that binds the mutated input digest pre and post, with canonical rational strings throughout, no producer rerun, no new review farm, and no run of the remaining thirteen. Corrections carried from 17zzn G, not selected here: item 2's discriminator is the actual affine base at :332 or the T>=2 raise at :328, not the checker's own N identity; item 7 rejects at :376 after the band loop and Jacobian, so it is a late interface control, not "before mathematics" as CONTROLS.md says.

## E. OPEN(S) RAISED, COLLISIONS, attestation

OPEN(S) RAISED: none. No new canonical OPEN; the remaining quantities are the two designed controls above, which need separate ROOT authority, and the existing next-tier source test.

COLLISIONS: status EMPTY. Own-only check: this lane wrote exactly xmodel/f10-r2-actual-result-gate-fable5-20260910.md and box/f10-r2-actual-result-gate-fable5-20260910/input_custody.md, both absent at 12:55:36Z; no corpus scan; no other file touched.

WHOLE check: 15 objects read whole as itemized in section 0, artifact.json hash-only; this body was reread before the marker. Authoring: every byte of both owned files was written by apply_patch (the custody rows by piping sha256sum output into apply_patch's stdin); no Write/Edit tool, no shell redirection into any target or other file (one dry construction of the custody patch text was discarded to /dev/null before the real pipe), no helper, no source or input edit, no Seal, no charge_basis, no execution, import, CAS, network, AWS, SSH, Git, agents, corpus, shared, protected, other-lane or live file. Timeline: hashes and absence 12:55:36Z, skeleton 12:55Z, whole reads 12:56-12:57Z, body 13:01Z, one digest-tail typo in section 0 corrected and this attestation amended 13:03Z, all before the 13:07:36Z reserve, deadline 13:10:36Z, never reset. The standalone marker below is the only such line in this file and nothing follows it.

<!-- BODY-END -->
