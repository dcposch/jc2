# D125 pure exceptional center: first-contact restriction, not exclusion

2026-09-07. **PASS for a necessary first-contact theorem; NO-GAIN for uniform pure-center exclusion.** The repeated factor of R can be controlled by the actual source weight. With exact moving references, a finite arc's first residual must be R times a constrained ordinary coefficient, and cannot occur at the first moving k face. Those conditions still admit a genuine unguarded second-order nilpotent source jet. The remaining higher-order compatibility is not proved. No degeneration-existence, guarded-emptiness or JC2 claim is made.

This packet uses accepted14c source,14f classification/centralizer and14g low rows only. It rederives its polynomial identity; no conclusion of the live tuned gate or its provisional producer is a premise. Root supplied the weight and moving-reference seeds. The bounded history check found the earlier radical argument and the already charged origin warning, but not the stronger filtered transfer below. No exhaustive novelty claim is made.

## 1. The repeated factor is not itself the missing hypothesis

Put

    R=R_{−3}=p²V,     V=g³+p³−3p,     w(g)=5,w(p)=−7.

V is absolutely irreducible: a root in Kbar((p)) of its monic cubic in g would have valuation1/3. Primitivity then gives irreducibility over Kbar[g,p]. Its weight is15.

For EVERY positive integer e and every polynomial P with w(P)≤3,

    V^e | P  implies  R^e | P.                     (W)

Indeed, P=V^eQ gives w(Q)≤3−15e. A monomial of Q with p-exponent≤2e−1 has weight≥−7(2e−1)>3−15e, impossible. Thus p^(2e)|Q. In particular, for any positive N,

    R | P^N and w(P)≤3  imply  R | P.

This is a filtered polynomial statement, not a radical equality for unrestricted ideals or arbitrary nilpotent coefficient rings. Dropping weight makes it false: P=gpV has R|P², with quotient g²V, but R∤P since its p-linear coefficient is g⁴. Its weight is13. No full square is expanded to check this factor identity.

## 2. Exact references and the mixed linear correction

Consider a genuine finite K[[s]] arc, with nonzero k(s) of order m>0 and center

    A₀=R³,       B₀=R⁵+β₀R³,
    t₀=−3,      α₀=γ₀=0.

All full polynomial/lift rows and the generic k-inverse guard are understood; the inverse need not extend to s=0. Define

    t(s)=[p¹³]A/3,       h(s)=t(s)+3,
    α(s)=([p³]A+h(s)³)/t(s),
    R_s=R_{t(s)},        F=A−R_s³−α(s)R_s.

t(s) is a unit because t₀=−3. The formulas use the exact coefficient selections [p¹³]R_t³=3t and [p³]R_t³=−(t+3)³; no cube is materialized. The resulting identities are

    F₀=0,  [p¹³]F=[p³]F=0,
    [p]F=α(s)h(s),
    [g²p]F=k(s),       [gp²]F=x(s)−α(s)h(s).        (L)

In particular, [p]F is NOT generally zero. Every positive F coefficient is odd, ordinary, degree≤13 and weight≤3. F is nonzero; if j=ord_s F, then 1≤j≤m.

The mixed term is real even for an ordinary A satisfying its source support, faces and [p]A=0. Take h=s, an auxiliary a=s, and A=R_h³+aR. Then the reference α is−3a/t, and

    F=(a h/t)(R+3S),       S=p(p²+gp−1).

Its leading coefficient is F₂=−(R+3S)/3, whose p coefficient is1 and p³ coefficient is0. No actual cube is expanded. This is an A-source/reference countercontrol, NOT a full Jacobian point: pairing it with B=R_h⁵ gives bracket5ah R_h⁴[R,S], generally nonzero. It shows why simply importing a no-linear-residual origin bound is invalid.

## 3. First-contact theorem, with all lower orders retained

Choose scalar reference series β,γ with their center values, and put

    a_s=3R_s²+α,   q_s=5R_s²/3+β−5α/9,
    δ_s=γ−βα+5α²/9,
    G=B−(R_s⁵+βR_s³+γR_s)−q_sF.

Direct polynomial differentiation gives the exact identity

    [A,B]=[R_s,a_sG−δ_sF−(5/3)R_sF²]+[F,G].       (I)

This holds for the MOVING R_s and all scalar parameter motion. G₀=0 and positive G coefficients are odd of degree≤23. The target bracket has order3m, so every order≤2j lies before it.

Use the accepted polynomial centralizer K[R]. Whenever the earlier G coefficients vanish and the F terms have not yet started at a given order ℓ, (I) gives G_ℓ∈K[R]. Degree and oddness leave ηR³+θR. Changing only the references β→β+ηs^ℓ,γ→γ+θs^ℓ changes

    G by −ηs^ℓR_s³−θs^ℓR_s−ηs^ℓF,
    δ_s by θs^ℓ−ηs^ℓα(s).

Every additional moving-R term has strictly higher s-order. The actual A,B are unchanged. This permits coefficientwise removal; no nonmultiple orders are deleted.

A first nonzero δ_d with d<j would, after removing earlier G kernels, give

    3R²G_(j+d)−δ_dF_j∈K[R].

Odd scalar remainder division modulo R² yields F_j=cR+R²U. The actual degree/weight bounds leave U odd, degree≤3, weight≤1, hence only p,p³,gp². Ordinaryness forces U=dS: its two complete negative lift rows are −u₃+u₁₂ and −u₁−3u₃+2u₁₂. Division is justified by φ(R) at v=0 equal3u, a nonzero polynomial. The p³ reference first kills c; the p¹³ reference then kills d, since [p¹³]R²S=1. Thus F_j would vanish. Consequently δ below j must be zero in these normalized references, and G can be removed through2j−1.

At2j, (I) therefore gives

    3R²G_(2j)−δ_jF_j−(5/3)R F_j²=b(R).             (Q)

There are no suppressed denominator derivatives: all lower G coefficients are zero, and [F,G] starts at≥3j. The scalar b is odd.

If δ_j≠0, reduction modulo R gives R|F_j. Write F_j=RC; [p³]F_j=0 gives C(0)=0. Divide (Q) by R and evaluate at the receiver origin: the constant of b(R)/R is zero. Reducing again modulo R forces R|C, so F_j=R²U. The same cubic lift and p¹³ argument kill U. Therefore δ_j=0.

For δ_j=0, divide (Q) by R and evaluate at the origin. Again the scalar constant vanishes, now yielding R|F_j². The filtered transfer (W) gives R|F_j despite its repeated factor.

Thus every genuine arc has, in these references,

    F_j=RC,      ord_s δ_s≥j+1,
    C nonzero, even, ordinary, degree≤8.

If j=m, this divisibility contradicts the required nonzero [g²p]F_j, since R is divisible by p². Hence j<m. The absent positive-order weight3 face then improves the bounds to

    w(C)≤0,       C(0)=0,       [p⁸]C=0,       V∤C. (C)

Here [p⁸]C=0 follows from the p¹³ reference and degree≤8. If V|C, (W) gives R|C; F_j=R²U is again killed by the same reference/lift argument. These are necessary first-contact conditions, NOT sufficient source realization.

Equation (L) additionally yields

    ord_s(αh)>j,       ord_s(x)>j.                  (O)

The first follows because RC has no p-linear term. For the second, RC has no gp² term (its p² coefficient is divisible by g³), while αh already vanishes through j. The full low row x²=3ky remains necessary: if x,y are nonzero series, 2ord(x)=m+ord(y), with y=−h³+αt. No even-m conclusion is licensed when y₀=0, and no cancellation between these two y terms is suppressed.

## 4. The first-contact conditions genuinely survive

Take Z=gp+p². It is even, ordinary, degree2, weight−2, has zero constant/p⁸ coefficients and is not divisible by V. Over K[ε]/(ε³), set k=0 and define FACTORED polynomials

    A=R³+εRZ,
    B=R⁵+(5/3)εR³Z+(5/9)ε²RZ².                    (J)

This is a point of the FULL UNGUARDED source at this nilpotent precision. All components obey the actual supports: A variations have degree7, weight−1 and i≤2j; B variations have degrees17,9 and weights1,−3. They lie strictly below the fixed faces. R,Z are ordinary, origins vanish, and x=y=e=[p]A=0, so the saturated low rows hold with k=0. If the optional whole B₁₅ slice is required, subtract [p¹⁵]B times the ENTIRE A; all claims persist.

A formal calculation in independent coordinates (R,Z), followed by the Jacobian chain rule, gives the exact bracket before truncation

    [A,B]=(5/9)ε³ RZ²[R,Z].

Hence all Jacobian rows vanish modulo ε³, while the next coefficient is genuinely nonzero. The checker expands only the formal (R,Z)=(g,p) identity and small actual factors; it does not expand the full pair. This is NOT a guarded point: k is identically zero in the coefficient algebra. In particular, setting k=ε instead fails the actual moving A face. No finite jet implies an arc or a nonzero-k branch.

This survivor blocks immediate exclusion from (C) alone. General compatibility after2j, including higher α,h motion and scalar kernels, remains unproved; the next unconstrained coefficient is2j+1, not automatically3j. The special fixture (J) first fails at3j for j=1. No ramification enumeration or speculative continuation was launched.

## 5. Evidence and stop

Twelve capped normal/−O controls pass with identical rational-string witnesses and zero Assert nodes. Actual mutations omit the weight hypothesis, omit the mixed linear correction, freeze a genuinely moving R, change the second-order jet coefficient, or omit the actual moving k face. Witness SHA256 `f617242b28e40ba1110b0be45d9ec650c1ea484b33ecdb934df3c78391d8eb81`.

Replay ordinary/−O `python3 -B box/d125-exceptional-pure-discriminator-20260907/check.py`. Inputs and owned files are pinned in custody. No actual R³,R⁵,R²S or degree15/25 pair was expanded: only factors of degree≤5, cubic lifts, scalar coefficient selections and formal R=g identities. No full-source parsing/build, CAS, AWS/SSH/network, solver, live peer read, new agent or shared edit occurred. The new first-contact theorem is PROVISIONAL pending review; uniform pure-center exclusion is **NO-GAIN/GAP**. Root's separate degeneration/infinity work is not assumed or duplicated. All writers idle at handoff. **STOP/IDLE.**

<!-- BODY-END -->

## Seal

- Body definition: every byte through the unique standalone `<!-- BODY-END -->` line,
  including its terminating newline; this seal is outside the body.
- Body bytes: `9765`.
- Body SHA-256:
  `bbefb405a68c71e73d0b0dc0494cdbac8f80a8eede9d5f9e4022c1b90df97ef7`.
- Frozen basis: `0d39df3c9fd69c939a8420c54d03228b9077777d`.
