# Frozen primary-proof gate: provenance and exact campaign client

Prepared 2026-09-07; PREP ONLY. This packet is not a proof acceptance, launch
request, new sweep, or source-ideal constraint. The reviewer should reach an
independent verdict, not endorse a producer or previous sweep conclusion.

The new claim is liqsweep, *Polynomial composition rigidity via critical
values*, September 2026. The exact repository commit is
`blueberryvertigo/polynomial-composition-rigidity@b17b6b9f7440b12fd85df2c2db9c98209ecc174d`
(2026-09-07T04:19:19Z). The pinned files are `rigidity.typ`, `proof.pdf`,
`refs.yml`, and `README.md`; the packet gives them unique producer basenames.
Primary URL:
https://github.com/blueberryvertigo/polynomial-composition-rigidity/tree/b17b6b9f7440b12fd85df2c2db9c98209ecc174d
The eight-page PDF and Typst are distinct charged objects: report any semantic
disagreement; do not silently select a later version.

The original author primary is Jean-Philippe Furter, *Polynomial Composition
Rigidity and Plane Polynomial Automorphisms*, author version October 10, 2013:
https://www.math.u-bordeaux.fr/~jpfurter/polynomialCompositionRigidityAndPlanePolynomialAutomorphisms.pdf
Publication: JLMS 91(1), 180–202 (2015), DOI 10.1112/jlms/jdu064. This packet
charges the successful author PDF, NOT the empty failed publisher/mirror fetch.
The accompanying text is a convenience extraction; the PDF controls ambiguous
subscripts, bars, and the partial-order symbol.

Original interface reading targets: introduction/Conjectures R(m,n), R(m),
definitions of E, G, polydegree and the partial order, Theorem B and adjacent
remarks (PDF pp.1–6); Lemma11 and Corollary12 (pp.15–16). Published Theorem B
may remain an explicitly named EXTERNAL-THEOREM dependency at that exact scope.
No claim that this preparer independently reconstructed its whole proof.

The campaign strategy snapshot is frozen through 2026-09-07T07:31Z, not a
claim about later live state. `campaign-torsor.md` is a terminal scoped
geometry report. `campaign-moving-face-14c.md` is the terminal exact unit
normalization report, supplied to prevent ambiguity about 'full14c'. It
assumes the odd unequal/Q sufficient source and lambda2=0, not every JC2
counterexample. All fixed faces, Jacobian coefficients and 105 polynomiality
rows are retained. The normalized ring N includes k inverse (zk-1), moving
faces and Jacobian scalar -5k^3/9. Its relation to the old coefficient quotient
is O0 = N tensor_(Q[k,k^-1]) Q[ell,ell^-1], k=ell^-6, a rank-six faithfully
flat cover. Do not confuse this with an isomorphism over the original field.

For the torsor, v=g^-1, u=g^4*w+lambda2*g^2+lambda3*g^3, w=p+g.
The full source supplies a morphism S -> A2 with ramification divisor 2F,
F=(g=0); it is etale on S minus F. A(0,w), B(0,w) have degrees15 and25.
The exact missing global arrow in this packet is from S open in its finite
normalization N_target to S=N_target (or another valid exclusion). This
N_target is NOT the normalized coefficient ring N in the preceding paragraph.

Potential attachments are questions, not hypotheses: a forced polynomial
composition contact, a source-to-composition-coefficient-map morphism with
enough finiteness/valuation control, or membership in an automorphism
polydegree family. Derive the actual ring/place/coordinate map and every
hypothesis before applying the claim. Merely finding degrees15 and25 does
not identify boundary polynomials with compositional inverses. Even if the
contact theorem is true, a normalized two-factor composition of these degrees
needs contact at least40 for contradiction with the bound39. No such source
identity is provided here. An absent attachment is GAP, not a theorem that
all future applications are impossible.

The sweep custody was checked first: transaction valid and ALL194 current
file pins matched. No frozen evidence was changed. The independent gate's
primary inputs and campaign context are separately hashed in this packet.
