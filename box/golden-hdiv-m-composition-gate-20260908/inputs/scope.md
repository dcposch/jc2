# M-source composition: exact new review, independent of simple L

2026-09-08. PREP ONLY; no launch or promotion. The two new complete proofs
are independently derived from the same ACCEPTED canonical source and15m
poststate. Neither uses the other's unreviewed conclusion or the live
simple-L gate. Read both whole proofs and the whole source framework.
The source's historical UNREVIEWED header is preserved; audit-15m.md
records the accepted exact reference setup, not acceptance of these new
M exclusions. No new code or mathematical test is present in either proof.

Let K have characteristic zero and contain either rho with rho²-3rho+1=0.
t=1-rho, L=p+g, M=p+t*g, H=p²LM²; A/B have degrees15/25, topsH³/H⁵,
w(g)=5,w(p)=-7,w(A)<=3,w(B)<=5, [A,B]=c*g²,c!=0. No parity.
Use the source's SAME canonical A-only R_s,F and all B kernels/G, with
F_j=H*C!=0, 1<=j<=9, deg C=10-j,w(C)<=2,deg_g C<=2.

At the actual moving M-critical point the exact Morse chart has
R_s=xi_s+zeta², g=g(s,zeta,p) in E[[s,zeta]], E=Kbar(p^(1/2)),
and F(g(s,zeta,p),p)=sum c_n(s,p) zeta^n. Set kappa=ord_s xi_s.
The same actual full-F coefficients c_n must define r in BOTH proofs:

| Explicit source stratum | d | Shared balance r | Source bound |
|---|---:|---|---|
| C(-p/t,p)!=0 | 2 | min_(0<=n<=2) ord(c_n)/(6-n) | ord c2=j, r<=j/4<=9/4 |
| LM divides C, C!=0 | 3 | min_(0<=n<=3) ord(c_n)/(6-n) | ord c3=j, j<=6, r<=j/3<=2 |

Zero series have infinite order. The fixed nonzero c_d makes r finite
and positive. Claimed coalesced exclusion is kappa>=2r, including
kappa=infinity. Claimed separated exclusion is kappa<2r, necessarily
finite positive kappa. They are complementary ONLY after the reviewer
checks they refer to this identical source/reference/Morse/full-F/r object.

If both pass, the composition excludes exactly the union of these TWO
strata for every kappa. It does NOT assume L|C. M|C with L not dividing C
is outside the union and must remain outside the conclusion. No simple-L,
whole golden/common/D125, arbitrary-degree, coefficient-scheme, reverse-lift
or JC2 conclusion is licensed. This is field-source proof, not a computed
unit/proper ideal certificate or nilpotent-ring result.

Important asymmetry: coalesced B control uses B_s-R_s⁵ in sK[s,g,p]
and the nonnegative-order Morse inverse. That lemma may NOT be applied
unchanged after separated root denominators. The separated proof instead
regroups EVEN/ODD terms in z=R, retains BOTH field sheets, takes a GLOBAL
earliest G order and then excludes lower B using field homogeneity. The
review must check this difference explicitly, all scalar kernels, and
the possible G linear term AT5eta. No silent transfer of A's selected
branch to G, no dual-number Euler/UFD, no dropped earlier constants.

Root has read/rederived both proofs and checked their terminal pins; this
is not a different-model hostile verdict. Accepted foundations are not
being sent through a checker farm. Transactions/custody are provenance,
not instructions to follow uncharged paths recursively or read live gates.
Only this new source composition is the review target.
