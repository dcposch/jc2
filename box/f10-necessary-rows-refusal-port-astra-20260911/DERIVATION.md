# Necessary-row seven-triple derivation

STATIC / DISABLED / CONDITIONAL. No Python failure bytes have been observed in this task. The administrative source directory is `/opt/jc2-r3-necessaryrows-20260911a/science`. Ordinary CPython child flags are exactly `-E -s -S -B`; the environment is exactly PATH=/usr/bin:/bin, LANG=C, LC_ALL=C, TZ=UTC. No future worker, installed interpreter or native closure has been identified here.

## Fresh source branch and location derivation

All new authority/producer/checker text was read WHOLE after current hashes. Literal numbered text confirms producer module call at line 369, `def main()` at 342, and `meta=authorize('produce')` at 343. These are actual source locations, not merely assumed subtraction from old lines. No encompassing catch handles initial authorization. The build/scientific import is reached only after authorization returns.

| Label | Sole control change | Actual final authority frame |
|---|---|---|
| refuse-status | status becomes DISABLED | line 82: `raise ValueError('no ROOT registration')` |
| refuse-caps | limits.cpu_seconds becomes string 599 | line 95: `raise ValueError('registered limits/source inventory')` |
| refuse-inventory | remove metadata files.check.py, not the installed file | line 95: identical exception and bytes |
| refuse-source | metadata files.produce.py becomes 64 zeros | line 100: `raise ValueError('source pin: ' + name)` with name produce.py |
| refuse-hash | supplied authority digest becomes 64 zeros | authorize line 78 calls load_pinned; line 74 raises `ValueError('authority/inventory byte pin')` |
| startup-produce | actual job argument WRONG-JOB | line 29 raises `SystemExit('REFUSED: registered CLI contract')` |
| startup-check | actual checker job argument WRONG-JOB | same line 29 and SystemExit |

The first five traces start with produce.py:369 in `<module>`, then produce.py:343 in `main`. The hash refusal additionally prints authority.py:78 in `authorize` and :74 in `load_pinned`; other first-five traces end at their listed `authorize` line. All five are uncaught ValueErrors, hence full tracebacks rather than the checker's mathematical error protocol.

The checker module call is actually line 514, its `main` starts at 492, and `ctx = authorize('check')` is line 496. Its scientific import is line 497 and try block begins at 498. The wrong-tag SystemExit occurs before both and its call frames do not print. These two startup triples are one line only: `REFUSED: registered CLI contract` followed by LF. Each of all seven triples has exit 1 and empty stdout, conditional on ordinary healthy top-level exception handling and the preconditions below.

The current pinned dispatcher confirms exactly the five named metadata mutations and WRONG-JOB for both startup labels. Caps and inventory deliberately share one conditional and identical output; independence means different metadata edits, not different code coverage.

## Formatting attachment

The completed DERIVATION and Ubuntu compatibility report are imported documentary trust, not freshly re-proved CPython internals. Their scope is the selected ordinary Ubuntu amd64 `python3.12-minimal` version `3.12.3-1ubuntu0.16`, executable SHA `a92f0f95e883390c7256b2e441484aac06b1002dbe1d924141a77c8d82f96223`, under the accepted binary/source-package correspondence and ASCII formatter assumptions. No upstream source or package is fetched or followed here.

The actual new failing source lines are unchanged ASCII text. On line 343, raw zero-based CALL columns are [9,29); output is nine spaces then twenty `^` characters. On authority line 78, the CALL columns remain [11,48); output is eleven spaces then thirty-seven `^` characters. There are no tildes. The whole stripped `main()` and final whole-line raise spans suppress caret lines. The successful argument subscripts do not contribute extra frames. New absolute source paths and producer line numbers are the only traceback changes. The exact JSON strings retain every path, source line, space, quote and terminal LF; no regex, optional marker, alternative output or calibration is allowed.

## Preconditions, disabled state and remaining work

Each intended branch requires all preceding startup/DMI/CLI/import/inventory/ACL/metadata checks to succeed. Authorities are correctly serialized and hashed except the single named mutation; producer outputs are absent and canonical. The false authority digest and false produce digest differ from the actual digests. For source refusal, authority.py and arithmetic.py precede produce.py in FILES and must match. Wrong-tag checks occur after ordinary-mode/Linux/DMI but before entry, inventory or authority-file checks.

ROOT must identify the actual future worker and qualified executable/native closure, readable pinned ASCII source at the exact paths, ordinary position tables, normal excepthook without embedded tracing/audit changes or notes, healthy stderr/flush, and exact flags/environment. No `-X no_debug_ranges`, stale source/pyc, patched display or missing position information is covered. Package hash is not future-host attestation. Earlier import/DMI/native/resource/stream failures remain STOP, not alternative acceptable triples.

The three-key policy schema is exactly schema/source_files/entries. It intentionally has no enabled key. Although the final five source hashes are recorded separately in PINS, `source_files.arithmetic.py` remains `UNFORMED` in the template, so it cannot match a genuine final source vector. ROOT must make a NEW finalized copy and bind all source/contract/native/registration facts after the pending combined DELTA FIRST; this document grants no release.

Remaining quantity: one future-host/install/native identification and seven exact refusal comparisons, with no observations made here. All execution cost is UNMEASURED; no timing, cap-sufficiency or feasibility forecast is inferred. Cheapest next check is ROOT's literal source/template delta comparison followed by the already-required read-only actual installation/native binding, not an interpreter experiment to calibrate accepted bytes. No scientific source review, code repair, coefficient/place/fixture work, optional inverse-mutation hardening or source outcome is performed.
