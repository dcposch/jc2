# FIRST review — fixed fourteen-file candidate transport (Fable 5.1)

Verdict: ACCEPTABLE AS AN OFFLINE CANDIDATE-TRANSPORT BOUNDARY under its stated trust assumptions. Claims 1, 3, 4 and 5 CONFIRMED; claim 2 CONFIRMED for LF line semantics with one delimited GAP (universal-newline separators, GAP-1 below). No installation, release, worker, cap change, batch-b/c retry, 120-second or mathematical claim is authorized or implied.

First action 2026-09-11 17:55:05 UTC: clock, absent-target check (report path and box directory both absent, apply_patch tool present). Original reserve 18:12:00, HARD 18:15:00, never reset. All 22 inputs matched their charged hashes (`sha256sum -c`, 22 OK, directory count 22) before any read; all 22 were then read WHOLE through EOF with unclipped full-file reads. No linked source, fixture original, runtime, corpus or peer path was opened; the RESULT.md and old CONTRACT.md references in PINS.json were not followed. Postpins and own hashes: `box/caprun-candidate-stage-first-fable5-20260911/input_custody.md`.

## Replay and attack harness

Exact command (ordinary interpreter 3.12.3, no -O):

```
python3 -I -S -B box/caprun-candidate-stage-first-fable5-20260911/review_controls.py /tmp/jc2-lane.TOa5oq/inputs
```

Harness SHA-256 `b28501f3738c19cbdc6c13fd41f46610d08184d5b2012acd323f5f7967ff9a13`; deterministic stdout SHA-256 `d3269b5a916a312b098aa32b2016e1285863da5090f461890ae0e56a47929306` (unittest timing is not printed). The harness loads the charged test_stage_patch.py from the inputs directory (which loads stage_patch.py and historical_exercise.py from that same directory), sets `tests.E.OLD` to the inputs directory in memory, and wraps `tests.E.module` so the loaded test_latebind fixture module has `SOURCE` set to the inputs directory before its `Controls.setUpClass`; the fixture imports latebind.py from the same directory. These are location-only bindings; no inferred original path was read or executed. Result: PREPIN 22/22, four static checks (C01–C04), the nine producer methods 9/9 ok in-process (R01), seven reconstruction checks (H01–H07), twenty attack controls (A01–A20), one delimitation control (D01), POSTPIN 22/22: 35/35 as expected. Every candidate stayed as bytes in memory; nothing was applied, written or executed.

## Verdicts per claim

**1. Pure-data fixed-fourteen API — CONFIRMED.** stage_patch.py imports exactly hashlib, json, re (C01) with no open/os/sys/subprocess/exec tokens (C02). `make_patch` demands `type(candidates) is dict` with key set equal to the 14 NAMES (dict subclass A10 rejected; producer covers missing/extra), `type(raw) is bytes` (bytearray A11 and str A12 rejected), 0 < len ≤ 131072 per file (A15 boundary exact), aggregate ≤ 524288 (A16: non-vacuous, unlike the binder's), trailing LF, no CR/NUL, strict UTF-8. The summary must carry the literal schema (A05), the candidate status, `installation_or_release_authorized is False`, a context in the two-value set (A04), and an `outputs` map whose key set is exactly the other 13 names (A06) with `{'sha256','bytes'}` records (A09) equal to the lowercase digest and exact length (A07 bool and A08 uppercase rejected). The prefix must fullmatch the absolute exercise namespace (A17: 6–24 alphanumerics, no trailing newline/space, no underscore, no relative or `..` form; the eight-digit date is not validated as a calendar date, 99999999 passes, harmless). Bytes are decoded strictly and re-emitted by splitting on LF only, so the em-dash card round-trips (producer test 1; A01; A14 single-LF file; A19 150 random UTF-8 candidates with leading `+`, `-`, `@@`, `*** ` lines). Output is deterministic (A02) and inputs are unchanged (A20). ROOT_ATTESTED_CANDIDATE is accepted as the contract states (A03); that is transport acceptance only, not clearance.

**2. No extra paths from patch-looking content — CONFIRMED for LF line semantics; GAP-1 delimited.** Every content line is emitted with a leading plus; the only unprefixed lines are the header, the 14 `*** Add File:` lines built from the regex-bound prefix plus fixed names, and the trailer. A18 appends every directive keyword (Begin/End Patch, Add/Delete/Update File, Move to, `@@`, End of File, bare `-`/`+`/space lines) to all 13 non-summary files: exactly 14 paths, one End Patch, no Delete/Update/Move directive, bytes exact. GAP-1 (D01): stage_patch rejects only CR and NUL, so the eight characters that Python universal newlines treat as line ends (VT 0x0b, FF 0x0c, FS/GS/RS 0x1c–0x1e, NEL U+0085, LS U+2028, PS U+2029) are accepted inside a content line; an LF-only parser sees 14 paths, a `splitlines()` parser sees 15. The producer's own independent test parser in test_stage_patch.py uses `splitlines(keepends=True)` and shares this blind spot; the real apply_patch tool's line semantics were not examined (no tool traversal) and are exactly the tool-parser edge the claim delimits. None of the 22 inputs contains any of these bytes (grep scan), so the historical exercise is unaffected. Under the stated boundary the binder's JSON outputs are ensure_ascii, so NEL/LS/PS cannot appear raw there, but the binder's `text()` passes all eight in the card and installer, so this is external text hygiene like the recorded lowercase-placeholder gap. Cheap fix, not applied: reject those eight in `make_patch`.

**3. Historical adapter — CONFIRMED.** historical_exercise.py imports hashlib, importlib.util, json, pathlib, sys (C03); its only file reads are the two `read_bytes` pin checks of latebind.py and test_latebind.py before import (C04); the loaded harness's `setUpClass` pins all 8 fixture texts before use; the summary context must be HISTORICAL_TEST; `main()` requires exactly one argument (H07) and prints one JSON object with keys patch, expected_sha256, status, context, candidate_bytes (H06). Replaying `main()` in memory with the exercise directory string as argument (never accessed by the API) produced the identical 14-entry hash map, status, context and 78889 bytes. It applies nothing, observes nothing and installs nothing; it is not an actual-observation CLI.

**4. RECIPE.js — CONFIRMED by static reading; execution is ROOT-attested.** Each step throws before the next on failure: mktemp exit code and an exact 8-alphanumeric prefix regex; generation exit code; `JSON.parse` (truncation or non-JSON fails before any apply); context and status literals; exactly 14 targets under the prefix with a safe charset and 64-hex values; apply_patch `isError`; readback exit code; per-line regex, duplicate detection and equality with the emitter's map; `seen.size === 14`. There is no loop, retry, overwrite or cleanup. The expected hashes come from the emitter, so the recipe trusts the authenticated generator, as claimed. Fresh private directory, absent targets, no concurrent writer and symlink safety remain ROOT duties; Add File is not exclusive-create. Delimitation: the exec tool's `yield_time_ms` and `max_output_tokens` semantics were not examined; the historical run completed within them.

**5. Tests, EXERCISE and timing — CONFIRMED with delimitation.** tests.stdout lists the same nine methods my in-process run executed (R01). All 14 expected hashes in EXERCISE.json were reconstructed independently from the frozen fixtures (H01), the SUMMARY hash 2d78477f… matches (H03), and the four main documents equal the charged input files (H04), so the exercise staged exactly the historical candidates under HISTORICAL_TEST with the release flag false (H05). The 400 ms is ROOT's tool observation of mktemp/generation/apply/readback; I timed nothing, and it says nothing about a 120-second holder cycle. No future actual-observation CLI exists in the inputs.

## Omitted controls, defects, duties

No contract break found. Producer omissions now covered: dict subclass, bytearray/str values, bool and uppercase commitments, extra record key, SUMMARY listed in outputs, schema drift, HISTORICAL context, per-file and aggregate size boundaries, prefix length/charset/date edges, every directive keyword in all 13 files, universal-newline separators, 150-candidate round-trip fuzz, `main()` replay and argument count. External duties unchanged: ROOT authentication of binder, inputs and adapter; fresh directory and target absence; apply_patch tool correctness; physical observation, semantic review, installation, liveness and release. Tested scope: ordinary `python3 -I -S -B`, stdlib only, no candidate bytes written, no input shell/JS/Python document executed, no network, AWS, SSH, process control or /proc, /sys, /run, /opt inspection; inputs unchanged (POSTPIN 22/22). Raised canonical OPEN items: 0. No charge_basis, no exit claim, no Seal.

Own-only collision check: the report path and box directory were absent at 17:55 UTC before my first write at 17:59; only this report, `input_custody.md` and `review_controls.py` were authored, all via apply_patch; no shared ledger, input or protected file was edited; all writers idle at the end of this turn.
Two same-stem files in xmodel/ (`.log`, `.run.v2`, dated 17:54, before my first write) are the external adapter's launch records, not authored or opened by me; they are not my report or manifest. Own full read through EOF and fresh 22/22 postpins completed at 18:02 UTC before this marker.

<!-- BODY-END -->
