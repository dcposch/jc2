# FIRST review — candidate-only closed-nine metadata binder (Fable 5.1)

Verdict: ACCEPTABLE AS A CANDIDATE-ONLY OFFLINE PREPARATION TOOL under its explicit trust boundary, with two minor GAPs and the delimitations below. The failed AWS batch-b cycle remains CLOSED. No worker, retry, installation, release, cap change or 120-second claim is authorized or implied. No source, mathematical or JC2 promotion.

First action 2026-09-11 15:19:13 UTC (pin of the thirteen inputs). Original reserve 15:37:00, HARD 15:40:00, never reset. All thirteen inputs matched their charged hashes before reading; all were read WHOLE through EOF with full-file reads (no clipped ranges, no machine substitution, no linked-body traversal; the native.sha256 reference was not opened). Postpins and own hashes are in `box/caprun-latebinder-first-fable5-20260911/input_custody.md`.

## Replay and attack harness

Exact command (ordinary interpreter, no optimized mode):

```
python3 -I -S -B box/caprun-latebinder-first-fable5-20260911/review_controls.py /tmp/jc2-lane.a27KVq/inputs
```

Harness SHA-256 `7b951a7dd5d8cfef0e20119f933f7dffa77fc0e1d8a4df16c6bcdd011e66b634`. It imports the charged `test_latebind.py` from the inputs directory (which loads the charged `latebind.py` from the same directory), sets `SOURCE` to the inputs directory in memory BEFORE collection, never touches the inferred default path, then runs seven structural checks (C01–C07) and 52 mutation controls (M01–M52). Result: 13/13 producer methods ok (unittest-reported 0.059 s, not wall qualification), C01–C07 pass, 52/52 controls as expected, PREPIN and POSTPIN 13/13. Every candidate stayed in memory; no input document was executed; stdlib only.

## Verdicts per claim

**1. Pure-data bounded API — CONFIRMED.** Imports are exactly copy/hashlib/json/re; no open/subprocess/os/sys/socket/exec (C07). Every type check is `type(x) is ...`, so booleans never pass as integers (M04; producer bool test). Duplicate keys, floats, NaN, 21-digit integers, depth 17, 3000-deep arrays, CR bytes and uppercase digests all raise ValueError (M47–M52). Every failure path raises; success returns the whole 14-entry mapping (C01 deterministic). CONTRACT.md states the caller authenticates bytes, pins, observation and decision; the helper claims no caller authentication and SUMMARY.json always says `installation_or_release_authorized: false`. Note: the 524288 aggregate input bound equals 4 × 131072 and is therefore vacuous but harmless.

**2. Nine phases, complete relations, no positive science, caps and CPU origin — CONFIRMED.** `validate_prepared` rebuilds all nine argv vectors and nine authority objects from the fixed schema and demands equality (M23, M26, M31–M34 reject single-slot template deviations). Phase order, `phase_policy` null, `typed_slot_policy` {}, `aggregate_cpu_start_usec` "0" (M24, M27), source and outer limits (M29) and all five profiles including the dormant produce/check/mutate rows (M25) are literal. Every argv uses the control profile (5 s / 3 s / 8 GiB) or the dummy profile (32 MiB); startup-* carry WRONG-JOB; refuse-* carry the refusal authorities (DISABLED, cpu 599, missing check.py, zeroed produce.py, zero digest). Complete allowed difference set, measured by a leaf-level diff of preholder versus candidate (C02): registration 67 leaves = 3 flags (`enabled`, `exclusive_no_concurrent_writer`, `closed_child_guard.root_no_migration`) + 2 outer fields + 18 leaf fields (9 × device, inode) + 44 argv slots (9 × 4 identity slots + 8 digest slots); authorities 4 leaves (valid/dummy `closed_scope.device`/`inode`); zero unclassified. The reverse-edit check (latebind.py:315–329) restores exactly those classes from the recorded change list and compares whole parsed objects. It is a consistency guard rather than an independent oracle, since the code mutates only through that list, but together with the whole-template equality it closes the set.

**3. Nine individual leaf pairs, placement, canonical authorities, eight hashes — CONFIRMED.** The observation must carry exactly nine leaves, names in PHASES without repeats, path equal to `cgroup_path/name`, positive bounded integer pairs, pairwise distinct and distinct from the outer pair (M06–M11; producer leaf tests). Each leaf's own device and inode go into its own argv slots and guard fields; the producer's independent-device test and C03 confirm no shared device is assumed. Argv placement requires the flag once and the following slot equal to the expected placeholder; C03 verified all 36 identity slots and 8 digest slots. Authorities are sorted compact ASCII plus newline; the seven non-probe objects are byte-identical to the canonical serialization of the preholder objects; valid/dummy differ only in device/inode. Eight digest slots equal SHA-256 of the emitted bytes; the refuse-hash slot is the template's 64 zeros, verified present and left untouched, while its object digest is nonzero (bf80c539…).

**4. Acyclic order and opaque native.sha256 — CONFIRMED, one GAP.** C04: no authority contains the registration, card or manifest hash; the registration carries authority hashes; the card carries the registration hash once; the manifest is exactly three lines (registration, card, native.sha256 with the supplied list digest); the installer carries registration twice, card once, manifest once; the installer's own hash appears nowhere; SUMMARY.json hashes the 13 other outputs and not itself. The list digest is validated only as lowercase hex and copied. In the historical data it is distinct from both inventory digests, and `native_manifest_sha256` is bound to the registration pin (M12). GAP-1: distinctness is not enforced; supplying the full-inventory digest as `native_list_sha256` is accepted (M13). CONTRACT.md asserts distinct roles without claiming enforcement, so this is a contract weakness, not a break.

**5. Enumerated substitutions and decision-gated approval language — CONFIRMED with delimitation.** Card: exactly eight substitutions (PID, ticks, invocation, registration hash, receipt hash, heading, two state sentences); installer: seven (PID, ticks, invocation, registration hash twice, freeze token, card hash, manifest hash). Each requires the exact count (M36, M37, M40, M41) and no `JC2_[A-Z0-9_]+_PLACEHOLDER` may remain in the 13 documents (C05; the SUMMARY change log legitimately names the old tokens). "BOUND FOR ONE PREFLIGHT" and the acceptance sentence are emitted only when the decision passes: literal schema, context equal to the observation's, pins equal, observation hash equal, three flags literally true, literal freeze token (M15–M19). Nothing is written or executed. Caller authentication, not helper validation, must guarantee everything else in the card and installer: the installer's deadline literal (M43 accepts 2099), unit name (M44), stage path, holder cmdline path, every command including appended ones (M45), and all card facts other than the worker/hostname/boot line (M46 accepts an altered EBS id). The helper checks only the presence of `jc2_base=<base>`, `= '<instance>'`, `= '<boot>'` in the installer and the worker/hostname/boot lines in the card (M38, M42 reject). GAP-2: the placeholder regex is uppercase-only, so `JC2_x_PLACEHOLDER` survives (M39); the installer's own grep has the same blind spot, so this is external text hygiene.

**6. Historical reproduction, future preparations, no stale host facts — CONFIRMED with delimitation.** The four final documents reproduce byte-for-byte and the seven non-probe authorities are canonical (producer test 1). The producer's future-binding test rebinds batch 20260912c, new instance, hostname, boot, PID namespace and shifted dates, and every output carries the new values; the base path is constrained to `/opt/jc2-closedchild-preflight9-<8 digits><letter>`, a fixed schema rather than a general engine. `latebind.py` contains zero 64-hex literals and no instance, boot, hostname or date literal (C07); historical identities live only in `test_latebind.py` under HISTORICAL_TEST. Delimitation (C06): the 13 candidate documents are byte-identical between HISTORICAL_TEST and ROOT_ATTESTED_CANDIDATE; only SUMMARY.json carries the context. This follows from the exact-reproduction requirement, but a detached candidate card or installer is indistinguishable from a ROOT-bound one, so custody must keep candidates with their SUMMARY and never read card text as evidence of observation.

## Omitted controls, defects, duties

No contract break found. Producer controls omitted and now covered: extra keys in observation, decision and registration (M01, M15, M22); PID and 2^64 bounds; leaves as dict or ten entries; tenth phase; registration self-pin; nonzero refuse-hash template; dormant profile; `science_outcome`; deep nesting, 21-digit integer, depth 17; CR byte; uppercase pins; decision string "true"; context mismatch. Explicitly external duties, not helper defects: authentication of prepared bytes and pins, physical observation capture, ACL, liveness, clocks, readback, installation, token release, and runtime acceptance of the emitted argv and authorities by the uncharged run_capped/probe/produce/check code, which this review did not examine.

Tested scope: 13 replayed producer methods, 7 structural checks, 52 in-memory controls; ordinary `python3 -I -S -B` (3.12.3), no -O; no candidate bytes written; no input shell or Python document executed; no third-party imports, network, AWS, SSH, process control or /proc, /sys, /run, /opt inspection; inputs unchanged. Raised canonical OPEN items: 0. No charge_basis, no exit claim.

Own-only collision check: the report path and box were absent before my first write (15:24 UTC); only this report, `input_custody.md` and `review_controls.py` were authored, all via apply_patch; no shared ledger, input or protected file was edited; all writers idle at the end of this turn.

<!-- BODY-END -->
