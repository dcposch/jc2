# Small supplied-input candidate emitter, no observation or installation

ROOT selected September11 18:14UTC. Basis
0d39df3c9fd69c939a8420c54d03228b9077777d.
Original reserve18:32UTC / HARD18:35UTC, never reset.

Consumer: the next prospective single-call metadata staging flow. The accepted
historical_exercise CLI cannot consume supplied current preparation/observation/
ROOT-decision bytes. Replace that fixture-only caller boundary with one SMALL
plain CLI composition of the already-reviewed latebind.bind and
stage_patch.make_patch APIs, adding the newly identified text hygiene check.
This is not a supervisor, observation collector, template engine, framework,
holder replacement, cap increase, batch retry or runtime/science release.

Pin these exact dependencies before fresh WHOLE reads:
- box/caprun-latebinder-code-astra-20260911/latebind.py
  ff2beb48b68b55dc5035709a9114bc7202dd38cef488d20b8163d0c55e2366fe
- box/caprun-latebinder-code-astra-20260911/test_latebind.py
  85d5390babe473deed13e9a88c40ea82193c6306284a4224cdb021f65ca5ec06
- xmodel/caprun-latebinder-first-fable5-20260911.md
  3201abe5cae352f9e12ec6c842c6efe7ac463394b515cf2279d8266717296661
- box/caprun-candidate-stage-root-20260911/stage_patch.py
  f7c61db67a9982a99f1febaed6dae0b6e3595199ffccad70647b66556b907066
- box/caprun-candidate-stage-root-20260911/historical_exercise.py
  fbec59b7317689fd71124bc5702ebdd0cd5ddb4ec8854592bb8a0f0447d66ff8
- box/caprun-candidate-stage-root-20260911/EXERCISE.json
  e37735ceeee1d60ee97ce492e196c312a8b9ee5d887f8abc9809af71924ce5b5
- xmodel/caprun-candidate-stage-first-fable5-20260911.md
  d96d979c950bf51faea0ad430af4f2eed1366d8f7b3e952e27d048ca2dff2273

Exactly8 historical TEXT fixtures named and hash-pinned inside test_latebind.py
under box/caprun-closed-scope-b-execution-root-20260911 are additionally
authorized for historical documentary testing, no linked source traversal.
No other corpus, shared ledger, peer/live report or protected access.
Gate custody already complete: ROOT reproduced Fable's35 expected controls,
including D01's eight accepted universal-newline separators; all pins unchanged.
Acceptance retains LF-tool semantics and external caller hygiene; code is frozen.

Implement one command with a literal small interface, e.g.:
caller.py PREPARED_DIR PREPARED_PINS_JSON OBSERVATION_JSON DECISION_JSON PREFIX EXPECTED_CONTEXT
The four prepared basenames are latebind.NAMES. ROOT authenticates CLI,
helper, these supplied files and physical facts externally. The CLI must not
derive approvals, fabricate observations, guess a context, or authenticate a
file by merely hashing the same untrusted file. It takes the ROOT-supplied
expected prepared pins, verifies helper code hashes before import, parses
with the existing strict JSON loader where appropriate, calls bind, checks
the explicit required context, then calls make_patch and prints one complete
JSON object. It never applies the patch, writes a candidate, installs, executes
an input shell/policy, observes /proc/AWS, or releases a token. Preserve the
returned14 hashes, candidate-only status and complete SUMMARY pairing.

Reject U+000B/U+000C/U+001C/U+001D/U+001E/U+0085/U+2028/U+2029 in candidate
text before emitting a patch. Existing CR/NUL/UTF-8/LF/size checks remain.
Do not change either accepted dependency. Keep read-only I/O bounded, fail
before any stdout packet on malformed/missing/mismatched inputs. No claim
that prefix existence, private ownership, symlinks/concurrency, native-list
authentication, physical/liveness facts or complete prepared-text semantics
are established by this command. Those ROOT duties remain unchanged.

Test the EXACT CLI path end-to-end with supplied historical data, not just
another direct API call. Preserve14 historical output hashes/context/78889bytes
against EXERCISE. Supplied observations and decisions in the fixture must be
labelled HISTORICAL_TEST; no frozen old identity becomes current authority.
You may write exactly three additional owned HISTORICAL fixture JSON inputs
(expected pins, observations, metadata-only decisions), with apply_patch,
so the real CLI can read them; no candidate outputs are written. A pure in-
memory harness may capture main stdout and independently reconstruct expected
hashes. If subprocess is needed, it may invoke ONLY this own short stdlib
documentary CLI, never scientific/runtime/dummy/native code. State that scope.
Add meaningful negatives: missing argument/file, bad pins/context/decision,
wrong dependency hash, every8 forbidden separator (with internally updated
commitments, so rejection is nonvacuous), and old ASCII failure still avoided.
Do not imply a full120second cycle or live-observation execution was tested.

Own ONLY box/caprun-candidate-caller-astra-20260911/{TASK.md,caller.py,
test_caller.py,CONTRACT.md,PINS.json,custody.json,fixture-pins.json,
fixture-observation.json,fixture-decision.json} and the transactional
xmodel/caprun-candidate-caller-astra-20260911.md report/partial/manifest.
TASK is ROOT-owned, do not modify. All authored files use apply_patch; report
begin/close/finalize/expected verify, own WHOLE and input/output postpins,
unique markerLAST, then custody and all writers idle. FIRST different-model
review is required before operational use. No direct shared changes or
automatic follow-on. Stop with a precise gap if the small composition is not
feasible by the original reserve. No new canonical OPEN identifier.

No scientific imports/subprocess of any size, CAS, syntax/AST/native/dummy/
runtime execution, AWS/SSH/network, additional agents, protected jc2-lean,
host/process/cgroup control or candidate-shell execution. Only the specified
short standard-library documentary tests, status/hash and own publication.
