# Frozen prospective operation contract — version1

PROSE ONLY / UNIMPLEMENTED / UNREVIEWED. Job `f10-source-cone-r3-direct-evaluation-v1`; object `f10-r3-direct-source-circuit/v1`. This is NOT `r3-source-cone-reconstruction-v1`, not a qualified rational baseline, and not an input to its unchanged checker or rank22z wire. Accepted23z attaches this alternative necessary presentation through the canonical localized source algebra. No unsaturated ideal equality or canonical297-minor claim is made.

## Types and fixed operations

Leading scalars have type B symbolically and k=F_p[V]/phi numerically. ROOT supplies ONE p and ONE monic irreducible phi of degree f in1..7, dividing the literal P7 reduction. For a finite explicit prospective wire choose 2<=p<2^31; primality is checked exactly by divisibility through floor(sqrt(p)), not probable-prime status. Phi irreducibility is checked by V^(p^f)=V modulo phi and gcd(phi,V^(p^(f/l))-V)=1 for each prime divisor l of f. This is verification of one supplied place, not selection/factor search. ROOT must separately approve its implementation/bounds. Bad place means STOP, never substitute another place.

Raw polynomial variables are (X1,X2,X3,X4,z,S,theta), weights(1,2,3,4,7,1,3). Formal band coefficients use (X1..X4,z), weight h; theta is an auxiliary index of weight zero UNTIL installation. Distinct auxiliary types are Laurent-S records and inverse-chart polynomials in (X1..X4,z,P,q), where q may have negative exponent. Do not overload one numerical dictionary's coordinate semantics without a type tag. Graph variables are exactly(X1..X4), weights(1,2,3,4). No X,z,S,theta-dependent expression can be inverted as a B scalar.

Permitted nodes: fixed rational constants; leading scalar addition/product/inverse; polynomial addition/product/nonnegative integer power; fixed coefficient extraction, differentiation, finite series; fixed projection and exponent reindex; fixed finite matrix operations. All structural index ranges below exist even when their coefficient is zero. Sparse zero deletion is representation-only. No coefficient zero test may select a support range, omit a slot, or establish a characteristic-zero identity.

Denominator registry D0: literal rational constants from producer lines12–25, including10,210 and the nonzero residue of L=-245*120*144*(3/10); finite series division j=1..7; critical integration division e+1=1..8; middle ratios (3(h-3)-2)/(27-h) AND their inverses for h=5,6; diagnostic scalars1/2,1/21,6/11,24/11. Conservatively check every numerator used as an inverse and every syntactic denominator; canceled primes need not be optimized away.

Exact rational registry order/labels: `D0:tau`=10, `D0:W`=210, `D0:lead`=L; `D0:series:j`=j for j1..7; `D0:integral:j`=j for j1..8; for h5 then6, `D0:ratio-den:h`=27-h then `D0:ratio-num:h`=3(h-3)-2; then `D0:half`=2, `D0:ell21`=21, `D0:ell11`=11. Follow with D1 labels `D1:h:j` in ascending(h,j), then D2 labels `D2:j:i` in ascending(j,i), excluding the two resonances. Repeated denominator values retain distinct labels; no value-deduplication. L is evaluated only after the tau denominator.

D1: d(h,j)=10j-3(17-h) for h=1..6,j=0..4 and h=7..10,j=0..2. Every one is a nonzero rational integer; require each residue nonzero even if a particular correction happens to vanish. D2: e(j,i)=j-3i for j=0..4,0<=i<=17-3j, EXCLUDING(j,i)=(0,0),(3,1). All D2 residues must be nonzero. The two excluded indices are fixed gauges, never inverted. There are no data-dependent rational pivots.

Scalar registry U: dL,W,t5,H7_5,H7_6, six completed-map determinants det1..det6, critical_c; exactly12 named scalar inverse nodes. Add exactly3 selected-coordinate inverses pivot8,pivot9,pivot10. Each node binds its defining stage and input value, has nonzero residue, and has independently checked inverse product1. This defines its generic inverse in the accepted FIELD; no generic inverse coefficient array is required. Determinants use the actual reconstructed matrices; critical_c is the actual critical coefficient. Reusing a label for another expression is refusal.

## Literal stage graph

| Stage | Fixed construction | Output/type |
|---|---|---|
|L|tau=3/10; ascending lists dL=((1+tau)(2-3tau),-12(1-tau),12), K=(2(2-3tau)(1+tau)(2+tau)(3+tau),42(1+tau)(2+tau)(4tau-3),840(1-tau²),-840), gamma=(tau(1+tau)(2+tau)(3+tau),-30tau(1+tau)(2+tau),180tau(1+tau),-120tau). Form S7=2K²-140tau(1+tau-6V)KdL+245gamma*dL² and P7=S7/L.|Eight literal coefficient positions; monic degree7, not degree8. Reduce leading expressions by the supplied place.|
|L2|W=-K/(210dL); c=1+theta+Vtheta²+Wtheta³; t=trunc_theta<=7(c^(17/10)); t5=[theta5]t. C=c/W; D=trunc<=5(t)/t5; H7_h=[theta7]trunc<=7(c^((27-h)/10)),h5,6.|Leading C,D; a=C0,b=D0,F=C2,H=C1. Producer finite binomial series uses j1..n.|
|U(h)|O_h(A,B)=10CB'-(17-h)C'B+(10-h)AD'-17A'D. Starting B=fixed, for j=Dmax down to0 add -[theta^(j+2)](O_h(A,B)-target)/d(h,j) times theta^j.|Return B, b1=[theta1]residual,b0=[theta0]residual, AND the remaining theta>=2 defect. Never abort because this defect is nonzero.|
|B(h)|For h1..10, retain two SEPARATE sequences A_band,B_band. Full forcing W_h=sum_i=1..h-1 ((10-i)A_i B'_(h-i)-(17-h+i)A'_i B_(h-i)).|S-free coefficients of weight h. Never feed Euler-extracted bands into this recurrence.|
|E(h),h1..6|part=U(h,0,-W_h,4). Basis j0..2 uses A=theta^j,target=-2theta6 ONLY at(h,j)=(4,2), else0. Matrix columns=(b1,b0,10a*Bbasis0-17b*Aj0). Invert by determinant/adjugate. Preliminary xyz=M^-1(-base1,-base0,rho), rho=X_h at h1..4,0 at h5,6. At h5,6 set rho=-xyz2/(ratio_h*H7_h), recompute xyz. A_h=sum_j xyz_j theta^j; B_h=part+sum_j xyz_j Bbasis_j; U=-xyz2 at h4.|Six exact completed matrices; explicit particular subtraction. No middle variable or modified target omitted.|
|C7|d0=[theta2]A_3; Apart=(z-U*d0)theta; target=-2ztheta5; fixed=0,Avar=1,targetvar=0. Particular=U(7,Apart,target-W7,2); variation=U(7,1,0,2).|Keep actual y=z-U*d0; no shifted D-index substitution.|
|C8/9/10|At8: Apart=0,target=-U²theta5,fixed=U²theta3,Avar=1,targetvar=0. At9: zero apart/target/fixed,Avar=1,targetvar=0. At10: zero apart/target/fixed/Avar,targetvar=-theta4. Use U with degree2 for particular and variation.|Gap8 fixed mate term is retained, not subtracted away. E is the eliminated value at gap10.|

At each column stage let(c1,c0) be the variation residual's TWO low coefficients; these are typed leading scalars. At7 use the unchanged functional: invC2=W²*trunc<=6(c^-2); for R=theta,1 set lambda_R=F[theta6](C² integral(R*invC2))-(1/2)[theta5](same), integral constant0. At8,9,10 select c1 if its residue is nonzero, otherwise c0 if nonzero, otherwise STOP_BAD_COLUMN_PLACE. Set lambda=(c1^-1,0) or(0,c0^-1). The first branch does NOT claim generic c0 zero; the second does NOT claim generic c1 zero. Record selected index and product1. No Euclidean completion or arbitrary residue completion is selected.

Set value=-(lambda1*base1+lambda0*base0), Psi_h=c1*base0-c0*base1, A_h=Apart+value*Avar, B_h=part+value*Bvar. Retain full residual, its low-pair identity, and upper defect separately. Do not replace this residual by its low pair. All four Psi7..10 survive until the specified graph elimination.

## Installation, whole mate, boundaries and graph

INSTALL is the FIXED linear map on A_band: A0 has theta indices0..3; A1..A6 indices0..2; A7 indices0..1; A8,A9 index0; A10 is zero. Send coefficient(h,j) to S^(10-h-3j)*theta^j when 10-h-3j>=0. Record the other prescribed coefficients as `install_discard[h,j]`, including(h,j)=(5,2),(6,2), even when zero. Do not choose retained indices by observing modular vanishing. This projection agrees with the canonical polynomial source under the accepted source map. Formal unprojected bands remain available for all later forcing; installation does NOT mutate those arrays.

Let a_j=[theta^j]Ahat,j0..3. Preserve Dformal=(a2+U)/S and Vformal=(a1-z+U*Dformal)/S as explicitly Laurent-S expressions, not parameter-field inverses; keep their negative-S coefficients as diagnostics. K=a0. Their polynomiality is a canonical source condition, not a precondition on the alternative expression circuit. They are auxiliary records, never inputs to the band recurrence or replacement definitions of d0. Pi=ztheta-Utheta²+Stheta³; Delta=-Etheta*Pi-theta*Pi². No factor of either term is dropped.

Euler construction is a DIFFERENT sequence B^E_j. Set B^E_5=S² and B^E_6=B^E_7=B^E_8=0. For j=4,3,2,1,0 form the literal finite polynomial

Q_j=[theta^(j+2)]Delta -(j+1)a2' B^E_(j+1)+2a2(B^E_(j+1))' -(j+2)a1' B^E_(j+2)+a1(B^E_(j+2))' -(j+3)a0' B^E_(j+3),

where primes here mean S-derivatives. For EVERY i=0..17-3j, extract q_ji=[S^i]Q_j. For j-3i!=0 set [S^i]B^E_j=q_ji/(j-3i). At the TWO fixed resonances(0,0),(3,1), record q_ji and set the ENTIRE parameter-polynomial mate coefficient to0. This sets the prescribed gauges, not a forcing equation. Nonzero resonance diagnostics do not abort. Zero coefficients outside the index range follow from the fixed nonnegative weighted polynomial types; any actual type violation is an implementation failure, not an opportunity to shrink support. Bhat=sum_j=0..5 B^E_j theta^j.

Keep all11 differences `Euler_band[h]=extract_(S+3theta=17-h)(Bhat)-B_band[h]`, h0..10. No equality to0 is required for direct-object acceptance. Compute FULL J=Ahat_S*Bhat_theta-Ahat_theta*Bhat_S and R=J-Delta. The complete inventory is j0..7 and i0..23-3j: exactly108 positions. P1=[R_(i,1)]i0..20 and P0=[R_(i,0)]i0..23 give45 positions. The UPPER inventory j2..7 has63 positions (108-45), not the incidental FIRST wording81. Keep these values without imposing zero. All22 band-readback differences at(h,j),h0..10,j1,0 are recorded against0 for h<7 and(-lambda0*Psi_h,lambda1*Psi_h) for h7..10.

BOTH inverse boundaries remain explicit: substitute S=Pq³-zq²+Uq, theta=q^-1 into Ahat and Bhat SEPARATELY; retain the full resulting Laurent-q expressions, especially every negative-q coefficient. Finite bounds: A has iS<=10,jtheta<=3, hence q between-3 and30; B has iS<=17,jtheta<=5, hence q between-5 and51. No negative coefficient is discarded or required zero in the alternative object. Canonical source polynomiality remains a hypothesis via23z; this record does not assert an alternative ordinary source exists.

Keep the separate symbolic scale boundary z=s², theta=s*t, Aoriginal=s^-3*Ahat, Boriginal=s^-5*Bhat, d=s^-1*Dformal,v=s^-2*Vformal,k=s^-3*K,u=s^-1*U,ell_source=s^-3*E. Preserve targets P1_0-U*s^5 and P0_0-s^7, omega=W*t5*s^8, never s=1 or z=1. The checker can verify these by exponent reindex and coefficient-pair bracket; comparisons to the canonical polynomial source are diagnostics, not assertions that Laurent auxiliaries are ordinary. This boundary is not an additional free parameter or coordinate specialization.

Hq=[z0]Psi7; critical_c=[z1]Psi7 is a leading scalar by weight7. The unchanged critical expression is checked to equal Hq+critical_c*z; invert the actual critical_c and set zeta=-Hq/critical_c. Keep the exact25 names/order:

K8,K9,K10 = Psi8,Psi9,Psi10 at zeta; A1_i=P1_i(zeta),i1..9; A0_i=P0_i(zeta),i1..12; T=(zP1_0-U*P0_0)(zeta).

Their weights are8,9,10;19..11;22..11;27. Keep ell=P0_0(zeta) weight23, g=Hq*ell weight30, Hq weight7, U weight4, zeta weight7 and zeta*ell=-critical_c^-1*g. Every named row stays even if zero/duplicate. Extra diagnostics are NEVER added to the selected25-generator rank matrix: they retain the canonical hypotheses and expose differences, not a silently strengthened source test.

## Independent evaluator and finite-place acceptance

The checker must reconstruct from literal constants/place, not candidate matrices, scalar inputs, forcing, bands or graph rows. It shares no producer science/arithmetic module. Use independent coefficient arrays in theta and sparse parameter monomials: finite powers from k*f_k=sum_i=1..k (alpha*i-(k-i))*c_i*f_(k-i), i<=3; polynomial products/direct monic field reduction independently implemented. Reconstruct completed-map determinants by six signed permutations and check both inverse products. Solve upper coefficients by the coefficient-pair operator, not the producer derivative expression. Rebuild band sequences recursively BEFORE separately forming the Euler mate. In forcing, sum ak0..2,bl0..4 with coefficient(10-i)*bl-(17-h+i)*ak; O_h additionally uses C indices0..3,D indices0..5. These fixed ranges include the gap8 theta3 mate.

For Euler use full Jacobian coefficient-pair assembly excluding the unknown leading term, rather than duplicating Q_j syntax; apply the same fixed two-gauge projection. Compute graph substitution by coefficient Horner, not producer per-term powers. Read EVERY emitted field/slot and compare to the independently computed object; a diagnostic may be nonzero but must equal its true reconstructed value. Parser, exact shape, declared weights, scalar typing, fixed projector, inverse use, both arithmetic products and source expression equality are actual acceptance predicates. An untrusted diagnostic label cannot waive those checks.

Source attachment acceptance specifically binds this contract/version, literal constants, all index tables, completion policy and canonical hypotheses. Successful evaluation status is `CHECKED_DIRECT_NECESSARY_25_NO_SOURCE_OUTCOME`. It does NOT say the original rational code passed. Subsequent rank logic needs a NEW binding to these rows and this receipt. A future rank certificate supplies297 distinct(slot,multiplier) labels, each multiplier of exact weight30-row_weight, canonical ascending lexicographic exponent order within slot, and a297-square inverse. Independently reconstruct all columns over ALL297 weight30 monomials and check all88209 product entries. Field/pivot refusal, deficient rank and missing witness remain NONDECISION. No actual rank or place is supplied here.
