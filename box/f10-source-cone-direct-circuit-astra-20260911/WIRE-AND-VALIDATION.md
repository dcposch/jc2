# Distinct direct-object wire and prospective independent checks

STATIC SPECIFICATION / NOT IMPLEMENTED / NOT RUN. Exact top-level keys are `schema,job_tag,contract_sha256,source_pins,place,denominators,leading,bands,physical,raw,diagnostics,graph`. Schema/job are the constants in OPERATIONS.md. Contract SHA binds the final operation/assertion/wire packet through ROOT's immutable manifest, not a self-hash inside that packet. Source pins bind the future producer, independent checker and their separate arithmetic, plus the accepted literal-source references. No acceptance record is minted here.

The `source_pins` mapping must equal ROOT's separately frozen expected vector exactly, with no extra or absent entries; its future paths/hashes are not invented in this prose. Canonical wire bytes are ASCII JSON, lexicographically sorted object keys, no insignificant spaces, one terminal newline; parser reconstructs this serialization and demands exact byte equality. All numeric content is decimal strings as specified below. This binds whole-byte mutation restoration and excludes merely equivalent alternate JSON encodings.

`place` has exact keys `p,degree,phi,P7_mod_p`; p and degree are canonical decimal strings, degree f in1..7, phi has f+1 canonical ground-field coefficients and leading1, P7_mod_p exactly8 with leading1. All ground-field coordinates are decimal strings0..p-1. A k scalar is a DENSE array of EXACTLY f such strings in basis1,V,..,V^(f-1), never seven rational coordinates or a selected characteristic-zero component. No JSON mathematical numbers, floats, nonfinite constants, duplicate keys, signed residues, leading zeros, -0 or implicit reduction of out-of-range entries. The literal rational constant program, rather than candidate-provided rational coefficients, authenticates P7_mod_p. Prime and phi checks in OPERATIONS are mandatory before k arithmetic acceptance.

`denominators` contains `rational,scalars,selections`. Rational entries are in the fixed registry order, each `label,residue,inverse`; an inverse must multiply its independently reconstructed denominator to1. Scalar entries contain exactly the12 base/matrix labels plus pivot8,pivot9,pivot10, each `label,input,inverse`; no omitted zero slot. Selections are exactly3 records `gap,coordinate`, gaps8,9,10 and coordinates1 or0, recomputed by the c1-first rule. Candidate selections cannot choose arbitrary completions.

`leading` keys: `dL,KL,gamma,W,t5,middleH7,C,D`; the first five are k scalars, middleH7 has2 entries ordered5,6, and C,D are theta coefficient arrays of lengths4,6. Literal zero coefficients remain. P7 itself is in `place`, not a forged rational-baseline section. `bands` is exactly10 ordered records. Gaps1..6 use the original named fields `kind,gap,forcing,part,basisV,matrix,inverse,rho,A,B`, with basisV length3 and matrices3x3. Gaps7..10 use `kind,gap,forcing,part,A_part,fixed,target,A_var,target_var,B_var,column,lambda,base,value,Psi,A,B`, column/lambda/base each length2 ordered1,0. These are FORMAL recurrence bands, not Euler-extracted bands.

`physical` keys: `Ahat,Bhat,U,E,Dformal,Vformal,Kpar,Pihat`. `raw` keys `Psi,P1,P0,jacobian_slots` have exact lengths4,21,24,108; never shorten for zero or duplicate entries. `graph` keys `Hq,c,c_inverse,zeta,slots,ell,g,U`; slots length25 with the literal names/order/weights specified in OPERATIONS. Every polynomial is a typed record `kind,terms`; each term is `[exponents,coefficient]`, strictly increasing lexicographic exponent tuples, no duplicate monomial or zero coefficient. Zero polynomial is an empty terms list. `kind` fixes the variable list: band=(X1..X4,z,theta), raw=(X1..X4,z,S,theta), graph=(X1..X4), laurent_S=(X1..X4,z,S), inverse=(X1..X4,z,P,q), scale=(X1..X4,s,S,t). Only the named Laurent coordinate can have signed exponents. All math exponents are canonical decimal strings. Band coefficients have the stated parameter weight, ignoring auxiliary theta; physical and graph have the exact weights, not just upper bounds.

Prospective wire refusal limits, not measured sizes or runtime limits:20000 terms per polynomial,400000 terms aggregate,128MiB total encoded object,100000 terms per internal polynomial; absolute exponent cap256, with signs allowed ONLY at formal S/q/s coordinates. Finer fixed core supports, q bounds and graph weights override that loose limit. These are refusal ceilings, never truncation. Resource/native/CPU/wall/worker authorization remains ROOT's later task. Do not borrow old enabled authorities or imply these byte caps provide supervision.

Signed-coordinate clarification: `laurent_S` permits signed S; `inverse` permits signed q only; `scale` permits signed s AND signed S, because origd/origv may contain the retained Laurent-S auxiliary terms. No other coordinate is signed. Ordinary core raw/graph/band objects remain nonnegative. This does not make S a coefficient-field unit or feed a Laurent auxiliary into the core recurrence.

`diagnostics` has fixed keys `upper,early,middle,install_discard,Euler_resonance,Euler_band,upper_Jacobian,low_band,inverse_A,inverse_B,auxiliary,scale`. Upper has32 ordered `{label,poly}` records: h1..6 particular,basis0,basis1,basis2; h7..10 particular,variation. Early has12 records ordered by h1..6, full-residual then rho. Middle has4: (5,U2),(5,V4),(6,U2),(6,V4). Install has2: (5,2),(6,2). Euler_resonance has2 ordered(0,0),(3,1); Euler_band11; upper_Jacobian63 in(j,i) order; low_band22 ordered h0..10 and j1,0. Each record uses its literal label and typed polynomial, not a sparse inventory of only failures. Inverse_A/B are the full distinct Laurent inverse polynomials; auxiliary is exactly `{Dnegative,Vnegative}`. A diagnostic-cap failure is STOP, not permission to omit a record.

Scale has exact keys `origA,origB,origd,origv,origk,origu,origell,origPi,origUpper,origJ,origResidual,guard,comparisons`. Reindex exactly as OPERATIONS; origPi=t-origu*t²+S*t³, origUpper=-origell*t*origPi-t*origPi², origJ=origA_S*origB_t-origA_t*origB_S, origResidual=origJ-origUpper-1-origu*t, guard=W*t5*s^8. Comparisons has `A_inverse,target_inverse,bracket_scale,low1,low0,guard_product`. A_inverse is the difference from S*t³+(S*origd-origu)*t²+(1-origu*origd+S*origv)*t+origk. Target/bracket differences use s^-7 reindex of Delta/J; low1 has21 differences from s^-6*P1_i(z=s²) minus origu only at i0; low0 has24 from s^-7*P0_i(z=s²) minus1 only at i0. Guard_product is guard times both reconstructed original leading coefficients minus1. All these are exact comparisons, not booleans asserting canonical ordinaryness.

Literal diagnostic label strings: `upper:h:part`, `upper:h:basis:j`, `upper:h:variation`; `early:h:residual`, `early:h:rho`; `middle:h:U2`, `middle:h:V4`; `install:h:j`; `Euler-resonance:j:i`; `Euler-band:h`; `upper-J:j:i`; `low-band:h:j`. Replace h,j,i by their canonical decimal indices, in the already specified array order. Differences are constructed value minus claimed canonical-source value: early residual O+forcing-target; rho actual homogeneous rho minus prescribed rho; Euler band extracted minus formal band; low-band actual minus the specified zero/compatibility. Middle, install, resonances and upper-J carry the coefficient itself. Scale low arrays use labeled records `scale-low1:i` and `scale-low0:i` in increasing i.

Acceptance order: metadata authority/source/place binding; strict wire parse; independently reconstructed leading constants/inverse inputs and products; independent formal band reconstruction; fixed installation/Euler/auxiliary/inverse/scale reconstruction; exact diagnostic comparison; all108/45 raw comparisons; critical graph and all25 row/guard comparisons. The construction equality checks remain hard even where an ambient-zero assertion is demoted. Fixed gauges A10=0, [S0]Kpar=0, [S1]B^E_3=0 and [S0]B^E_0=0 are construction values, not an invitation to accept arbitrary kernel terms. Complete checked output status is only `CHECKED_DIRECT_NECESSARY_25_NO_SOURCE_OUTCOME`, whether diagnostics vanish or not. Malformed source/value mismatch is REJECTED; bad denominator/place, resource exhaustion or missing complete object is STOP/NONDECISION. There is no canonical-baseline qualification status.

## Two semantic controls, SPEC / NOT RUN

Source control: change graph slot24 (literal T) by adding X1^27 with coefficient1 in k, leaving all upstream data and other slots unchanged. If its old coefficient becomes zero, remove that term canonically; if absent, insert it in order. The difference is the nonzero polynomial X1^27 over a field, and preserves weight27. A conforming positive fixture with sufficient resource slack must reach exact predicate `direct-source graph slot T`, where independent reassembly disagrees. Earlier hash/parser/resource failure earns no semantic credit. This control tests source attachment, not canonical membership. Reversing the coefficient change must restore the WHOLE original bytes under canonical serialization; future authorities pin fresh changed bytes, not stale source hashes.

Rank inverse control: ONLY after a genuine independently checked297-square inverse W, add1 to one existing matrix ENTRY (all entries, including0, are present). Fixed shape/canonical residues remain. The change in M*W is M*e_a*e_b^T, nonzero since the selected M is invertible. Expected predicate `direct-rank full inverse product`; check every88209 entry including zeros. Inverse byte-restoration is mandatory. No rank payload or fixture exists now; these are prospective controls, not two observed rejections. A separate source-unit inverse+1 check could detect its product, but is NOT an additional selected control.

## Minimal ownership and readiness

After a FIRST manual review of this exact contract, ROOT may assign one bounded source-authoring task for the direct producer, plus a genuinely separate checker/arithmetic author and one subsequent combined static gate. This is an ownership plan, not delegation or a launch. Numeric planning wall:30 minutes for the next capped source-authoring attempt, UNMEASURED, with honest partial if the complete independent checker cannot be finished; it is NOT an executable/runtime cap or speed forecast. No scientific code, finite-field evaluation, rank matrix or test was authored here.

Remaining implementation obligations: fixed operation graph, strict type/projection/diagnostic schema, independent arithmetic and complete residue reassembly, source/receipt binding, then later separately authorized native/startup/resource/custody mechanics. No new supervisor is selected. Do not copy current source checker's Euler-fed band loop or exact Bézout-representative comparison. No prime/r/cap farm, all-r theorem, properness or source point follows from this specification.
