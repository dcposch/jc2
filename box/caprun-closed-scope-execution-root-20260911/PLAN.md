# One active worker, strict nine-phase lifecycle observation

ROOT first execution preparation: 2026-09-11 10:20:28 UTC. The original
clocks are in ORIGINAL-CLOCKS.json; none may be reset. This work instantiates
the already accepted closed-child runtime and holder, not a new source or
relaxed dummy. The only permitted candidate observation is PREFLIGHT_ONLY_9.
Mathematical producer/checker, rank search and FULL13 are not authorized.

Preallocation facts read from AWS on September11 around10:21UTC:

- The personal/us-east-1 tagged nonterminal jc2fleet is empty.
- Service quota L-1216C47A is1920 Standard On-Demand vCPUs. Aggregate
  AWS/Usage observations10:05/10:10/10:15/10:20 each report84 vCPUs. These
  are delayed aggregate observations, not a reservation or inspection of
  separately owned instances. The selected c7i.2xlarge needs8 vCPUs.
- Image ami-066263bf15bd856de is AVAILABLE, x86_64, HVM, EBS root/dev/sda1,
  created2026-09-05T03:38:13Z as jc2-worker-base-20260905T033812Z. Image
  metadata is not fresh installed-host/native qualification. Its inherited
  DeleteOnTermination=true is overridden to false in the exact launch.

All prepared deployment bodies, nine source/runtime files, the actual
holder.template.py and corrected outer admission must match retained hashes.
The initial lookup incorrectly used PREPARATION-PINS.sha256 and holder.py;
both were absent, and no action was taken on those failed lookups. Actual
PREPARATION-PINS.json and holder.template.py were located and all20 literal
input hashes passed STRICT checking before allocation. No source was run.

The worker release requires its local ready-input hash list, final collection
of Astra's bounded native selection, the fixed original clocks and fresh
AWS state. Arming the exact-ID coordinator USER retirement timer follows
allocation immediately; failure requires immediate exact-ID termination,
with the root EBS already preserved. This is allocation/setup authority only.
Retire earlier after evidence custody. Never retain an idle worker for review.

All original source files are uploaded unchanged by exact source paths in
the accepted ROOT-SOURCE-CONTRACT.json. Fresh physical/native collection
and ordinary-distribution helper identification precede setup. The full
six-field native closure is transcribed from actual raw metadata only;
source subset pins the actual Python and setpriv bytes, never old-host facts.
Only after genuine host/namespace/ACL/ownership/no-entrant/no-migration
qualification may ROOT install the pre-admission stage and admit the holder.

The frozen documentary binding map provides the exact remaining JSON paths.
Only actual captured outer/leaf identity, authority digests, comparison and
final freeze/release belong inside the holder's unchanged120-second ceiling.
ROOT checks and freezes the actual COORDINATOR user retirement timer/service/
AWS argv/profile/region/instance/original future deadline immediately before
remote admission. No comments, old receipt or fake worker timer substitute.

Any cap, mismatch, unexpected outcome or lost custody is STOP/NONDECISION;
retain evidence and retire without retry, cap raise or CPU rebasing. A pass
requires the exact registered nine-phase sentinel, all nine terminal records,
and the strict dummy identity/descendant/RSS/TERM/KILL/reap/closed-leaf-quiet
conditions. Merely starting the holder or exiting a launcher is not a pass.

No actual worker or outcome is claimed by this plan. No r3 baseline, genuine
place, rank, source exclusion, all-r theorem or JC2 resolution follows.
