# Exact execution/read scope

The fifteen exact current input paths and hashes are in input-pins.json.
The new accepted17g report was hashed before a WHOLE read. The prior owned
runtime preparation report, plan and six source bodies were already read
WHOLE in the same lane and reused after byte-identity verification. The fleet
instruction/transport scripts were read WHOLE; only exact-owned run/pull and
equivalent rsync flags were used. CAPRUN remained unchanged; only its argv
fingerprint definition was additionally inspected for metadata read-back.

Original exact.json was hashed/staged as a regular copy, processed only by the
authorized AWS transformer/checker/harness. There was no local mathematical
body replay, import, compile or test. Local postterminal reads were the batch,
full and controls receipts, authority/telemetry JSON, stderr/stdout control
evidence and custody hashes. No live result body was read. All100 remote bytes
were checksum-bound before those terminal result-body reads and matched after
pull. The retained artifact itself was preserved/hash-bound; its whole307729B
coefficient body was not reread by the agent and is not claimed as such.

The existing publication tool handled metadata lifecycle only. All new report
content and own metadata received documentary review before seal. No source
equations, precision API, worker cap or frozen code was changed. No earlier
band proof/body, other worker, protected tree, external publication or new
agent was accessed. The independent metadata comparisons are not another
mathematical checker run or a semantic proof promotion.
