# Explicit root-owned retained17-row engineering registration

Root invitation accepted; first local action2026-09-09T17:57:36Z.
This is ONE finite representation validation, not a solver or ideal decision.
Worker exclusively in scope: i-08d2a40f272ee9fa2, c7i.4xlarge,
Owner=f10-r1-exact-artifact-validation-20260909, EBSvol-0574e0fa5aed1f5e3.
Remote exclusive cwd/job: /home/ubuntu/f10-r1-retained-runtime-validation-20260909.
Root assigned and armed independent STOP timer18:18:00UTC; do not reset it.

Mathematical deadline18:14:00UTC, task deadline18:17:00UTC, aggregate wall750s
from first mathematical admission; original minimum controls. Caller admission
margin15s and post-return cutoff remain unchanged. Per-file RLIMIT_FSIZE16MiB.
Profiles (wall/cpu/RSS bytes): probe5/3/2147483648;
dummy5/3/33554432; transform180/170/2147483648;
check180/170/2147483648; controls300/280/2147483648.

Run only the frozen dispatch_batch.py with Python -I -B, one child at a time,
against an externally pinned enabled dispatch.registration.json. Every new
per-operation authority is exclusive, frozen and has no concurrent writer.
The eleven exact absolute input hashes and all root-chosen fields are in that
JSON. No optional hardening is enabled and no source is edited.

Required before mathematics: exact AWS/DMI/hostname/interpreter/current pins,
fresh directory, five authority refusals, valid sentinel and unchanged owned
RSS/TERM/KILL dummy. Then one transformer, one genuine checker.main and twelve
qualified in-process semantic controls. Any cap, mismatch, refusal or late
return is STOP/NONDECISION without patch/retry or budget change.

After terminal caller and owned children absence, hash/pull the entire owned
remote directory before reading result bodies; stop the exact worker and keep
the recoverable EBS volume. No other worker, allocation, solver, source powers,
prime, factorization, earlier-band reduction or follow-on action is authorized.
